package projects.diffs;

public class WhiteSpaceAfterToString {
    public static void main(String[] args) {
        try {
        process.waitFor();
      } catch (InterruptedException e) {
        LOG.error("Failed to execute chmod " + perms + " " + script.getAbsolutePath() + ", error: " + e.toString()/*WHITESPACE*/);
      }
    }
}
