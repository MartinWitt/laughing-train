# openjpa 
 
# Bad smells
I found 7123 bad smells with 770 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| UNCHECKED_WARNING | 1228 | false |
| UnnecessaryUnicodeEscape | 734 | false |
| DataFlowIssue | 415 | false |
| UnusedAssignment | 392 | false |
| JavadocDeclaration | 392 | false |
| FieldMayBeFinal | 334 | false |
| ConstantValue | 288 | false |
| Deprecation | 283 | false |
| DuplicatedCode | 210 | false |
| CatchMayIgnoreException | 197 | false |
| JavadocReference | 195 | false |
| RedundantCast | 190 | false |
| UnnecessaryReturn | 177 | true |
| ToArrayCallWithZeroLengthArrayArgument | 174 | true |
| UnnecessaryLocalVariable | 157 | true |
| SwitchStatementWithTooFewBranches | 155 | false |
| NullableProblems | 142 | false |
| RedundantArrayCreation | 111 | true |
| EmptyFinallyBlock | 110 | false |
| UnnecessaryModifier | 99 | true |
| TrivialIf | 77 | false |
| SimplifiableConditionalExpression | 69 | false |
| DuplicateBranchesInSwitch | 45 | false |
| SuspiciousMethodCalls | 45 | false |
| CommentedOutCode | 43 | false |
| TrivialStringConcatenation | 40 | false |
| FieldCanBeLocal | 38 | false |
| DeprecatedIsStillUsed | 33 | false |
| EmptyStatementBody | 31 | false |
| AssignmentToCatchBlockParameter | 31 | false |
| UnnecessaryToStringCall | 30 | true |
| RedundantMethodOverride | 29 | false |
| ConditionCoveredByFurtherCondition | 29 | false |
| RedundantClassCall | 28 | false |
| UseBulkOperation | 27 | false |
| NullArgumentToVariableArgMethod | 24 | false |
| StringConcatenationInsideStringBufferAppend | 24 | false |
| StaticInitializerReferencesSubClass | 23 | false |
| CStyleArrayDeclaration | 23 | false |
| InnerClassMayBeStatic | 18 | true |
| StringBufferReplaceableByString | 17 | false |
| EqualsWhichDoesntCheckParameterClass | 17 | false |
| SuspiciousNameCombination | 16 | false |
| PointlessBitwiseExpression | 15 | false |
| DanglingJavadoc | 15 | false |
| IfStatementWithIdenticalBranches | 15 | false |
| RefusedBequest | 14 | false |
| SuspiciousIndentAfterControlStatement | 14 | false |
| WebProperties | 12 | false |
| SuspiciousSystemArraycopy | 12 | false |
| DuplicateExpressions | 12 | false |
| SynchronizeOnNonFinalField | 12 | false |
| RedundantTypeArguments | 11 | false |
| StringEquality | 11 | false |
| JavadocLinkAsPlainText | 11 | false |
| CachedNumberConstructorCall | 10 | false |
| SortedCollectionWithNonComparableKeys | 9 | false |
| UNUSED_IMPORT | 9 | false |
| WrapperTypeMayBePrimitive | 8 | false |
| IgnoreResultOfCall | 8 | false |
| ManualArrayToCollectionCopy | 8 | false |
| StringOperationCanBeSimplified | 8 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 8 | false |
| OverwrittenKey | 8 | false |
| RegExpRedundantEscape | 7 | false |
| TypeParameterHidesVisibleType | 7 | false |
| UnnecessaryContinue | 7 | false |
| CastCanBeRemovedNarrowingVariableType | 7 | false |
| MagicConstant | 6 | false |
| StringConcatenationInLoops | 6 | false |
| CharsetObjectCanBeUsed | 6 | false |
| AutoCloseableResource | 6 | false |
| UnpredictableBigDecimalConstructorCall | 6 | false |
| PointlessNullCheck | 6 | false |
| MismatchedCollectionQueryUpdate | 5 | false |
| DuplicateThrows | 5 | false |
| BusyWait | 4 | false |
| SlowListContainsAll | 4 | false |
| RedundantLengthCheck | 4 | false |
| RedundantCollectionOperation | 4 | false |
| IntegerMultiplicationImplicitCastToLong | 3 | false |
| UnnecessarySemicolon | 3 | false |
| CollectionContainsUrl | 3 | false |
| JavaReflectionInvocation | 3 | false |
| CaughtExceptionImmediatelyRethrown | 3 | false |
| ArraysAsListWithZeroOrOneArgument | 3 | false |
| IfStatementMissingBreakInLoop | 3 | false |
| MismatchedJavadocCode | 3 | false |
| CloneableImplementsClone | 2 | false |
| DuplicateCondition | 2 | false |
| RedundantEscapeInRegexReplacement | 2 | false |
| NonStrictComparisonCanBeEquality | 2 | true |
| SuspiciousToArrayCall | 2 | false |
| ExternalizableWithoutPublicNoArgConstructor | 2 | false |
| UnnecessaryLabelOnBreakStatement | 2 | false |
| RegExpSimplifiable | 2 | false |
| SlowAbstractSetRemoveAll | 2 | false |
| ConditionalExpressionWithIdenticalBranches | 2 | false |
| CloneDeclaresCloneNotSupported | 2 | false |
| CopyConstructorMissesField | 2 | false |
| EqualsBetweenInconvertibleTypes | 1 | false |
| SimplifiableBooleanExpression | 1 | false |
| TextLabelInSwitchStatement | 1 | false |
| UnnecessaryCallToStringValueOf | 1 | false |
| SwitchStatementWithConfusingDeclaration | 1 | false |
| EqualsHashCodeCalledOnUrl | 1 | false |
| PointlessBooleanExpression | 1 | true |
| NewStringBufferWithCharArgument | 1 | false |
| UnnecessaryStringEscape | 1 | true |
| UnnecessaryEmptyArrayUsage | 1 | false |
| NonFinalFieldInEnum | 1 | false |
| ManualMinMaxCalculation | 1 | false |
| CollectionAddedToSelf | 1 | false |
| ThrowFromFinallyBlock | 1 | false |
| EqualsOnSuspiciousObject | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new QueryMetaData\[queries.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                throw new InternalException();

            _queries = (QueryMetaData[]) queries.toArray
                (new QueryMetaData[queries.size()]);
            Arrays.sort(_queries, this);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SequenceMetaData\[seqs.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                throw new InternalException();

            _seqs = (SequenceMetaData[]) seqs.toArray
                (new SequenceMetaData[seqs.size()]);
            Arrays.sort(_seqs, this);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LifecycleCallbacks\[calls\[i\].size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceListenerAdapter.java`
#### Snippet
```java
            if (calls[i] == null)
                continue;
            _callbacks[i] = calls[i].toArray
                (new LifecycleCallbacks[calls[i].size()]);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SequenceMetaData\[seqs.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                throw new InternalException();

            _seqs = (SequenceMetaData[]) seqs.toArray
                (new SequenceMetaData[seqs.size()]);
            Arrays.sort(_seqs, this);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new QueryMetaData\[queries.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                throw new InternalException();

            _queries = (QueryMetaData[]) queries.toArray
                (new QueryMetaData[queries.size()]);
            Arrays.sort(_queries, this);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[header.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
    private void comment(SourceCode source) {
        if (header.size() != 0)
            source.addComment(false, header.toArray(new String[header.size()]));
        String defaultHeader = _loc.get("mmg-tool-sign").getMessage();
        source.addComment(false, defaultHeader);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LifecycleCallbacks\[supCalls\[event\].size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
                if (supCalls[event] == null)
                    continue;
                meta.setNonPCSuperclassCallbacks(event, supCalls[event].toArray
                    (new LifecycleCallbacks[supCalls[event].size()]), 0);
            }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LifecycleCallbacks\[_callbacks\[event\].size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
                continue;
            meta.setDeclaredCallbacks(event, (LifecycleCallbacks[])
                _callbacks[event].toArray
                    (new LifecycleCallbacks[_callbacks[event].size()]),
                _highs[event]);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[_embeddables.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
	        // Reverse iterate the array of remaining deferred embeddables
	        // since elements will be removed as they are processed.
	        Class<?>[] classes = _embeddables.keySet().toArray(
	            new Class<?>[_embeddables.size()]);
	        for (int i = classes.length - 1 ; i >= 0; i--) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LifecycleCallbacks\[callbacks\[event\].size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
            if (callbacks[event] == null)
                continue;
            array = callbacks[event].toArray
                (new LifecycleCallbacks[callbacks[event].size()]);

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[lines.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        }
        lines.add(line.toString());
        return lines.toArray(new String[lines.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TupleElement\[terms.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
            FillStrategy<?> strategy = null;
            if (jakarta.persistence.Tuple.class.isAssignableFrom(resultClass)) {
                TupleFactory factory = new TupleFactory(terms.toArray(new TupleElement[terms.size()]));
                strategy = new FillStrategy.Factory<>(factory,  TupleImpl.PUT);
           } else if (resultClass == Object.class) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TupleElement\[terms.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
        public FillStrategy<jakarta.persistence.Tuple> getFillStrategy() {
            List<Selection<?>> terms = getCompoundSelectionItems();
            TupleFactory factory = new TupleFactory(terms.toArray(new TupleElement[terms.size()]));
            return new FillStrategy.Factory<>(factory, TupleImpl.PUT);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ExpressionImpl\[_joins.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public void acceptVisit(CriteriaExpressionVisitor visitor) {
        Expressions.acceptVisit(visitor, this,
            _joins == null ? null : _joins.toArray(new ExpressionImpl<?>[_joins.size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Expression\[_exps.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PredicateImpl.java`
#### Snippet
```java
    public StringBuilder asValue(AliasContext q) {
        boolean braces = _exps.size() > 1;
        StringBuilder buffer =  Expressions.asValue(q, _exps.toArray(new Expression<?>[_exps.size()]), " " +_op + " ");
        if (braces) buffer.insert(0, "(").append(")");
        if (isNegated()) buffer.insert(0, "NOT ");
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Expression\[_exps.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PredicateImpl.java`
#### Snippet
```java
    @Override
    public void acceptVisit(CriteriaExpressionVisitor visitor) {
        Expressions.acceptVisit(visitor, this, _exps.toArray(new Expression<?>[_exps.size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Selection\[list.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    @Override
    public CriteriaQuery<T> multiselect(List<Selection<?>> list) {
        return multiselect(list.toArray(new Selection<?>[list.size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[iPaths.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        }
        if (!iPaths.isEmpty()) {
            exps.fetchInnerPaths = iPaths.toArray(new String[iPaths.size()]);
        }
        if (!oPaths.isEmpty()) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[oPaths.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        }
        if (!oPaths.isEmpty()) {
            exps.fetchPaths = oPaths.toArray(new String[oPaths.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMetaData\[metas.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        // TODO -- need to handle subqueries

        exps.accessPath = metas.toArray(new ClassMetaData[metas.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Value\[projections.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        List<String> clauses = new ArrayList<>();
        getProjections(exps, selections, projections, aliases, clauses, factory, q, model, exp2Vals);
        exps.projections = projections.toArray(new Value[projections.size()]);
        exps.projectionAliases = aliases.toArray(new String[aliases.size()]);
        exps.projectionClauses = clauses.toArray(new String[clauses.size()]);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[aliases.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        getProjections(exps, selections, projections, aliases, clauses, factory, q, model, exp2Vals);
        exps.projections = projections.toArray(new Value[projections.size()]);
        exps.projectionAliases = aliases.toArray(new String[aliases.size()]);
        exps.projectionClauses = clauses.toArray(new String[clauses.size()]);
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[clauses.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        exps.projections = projections.toArray(new Value[projections.size()]);
        exps.projectionAliases = aliases.toArray(new String[aliases.size()]);
        exps.projectionClauses = clauses.toArray(new String[clauses.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TupleElement\[selections.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
            FillStrategy<?> strategy = new FillStrategy.Assign();
            if (Tuple.class.isAssignableFrom(resultClass)) {
                TupleFactory factory = new TupleFactory(selections.toArray(new TupleElement[selections.size()]));
                strategy = new FillStrategy.Factory<>(factory,  TupleImpl.PUT);
            }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[files.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/ant/AbstractTask.java`
#### Snippet
```java
            }
        }
        return (String[]) files.toArray(new String[files.size()]);
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LogFactory\[_delegates.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/MultiLogFactory.java`
#### Snippet
```java
     */
    public LogFactory[] getDelegates() {
        return (LogFactory[]) _delegates.toArray(new LogFactory[_delegates.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Log\[logs.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/MultiLogFactory.java`
#### Snippet
```java
            }
        }
        return new MultiLog(logs.toArray(new Log[logs.size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[names.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/PluginListValue.java`
#### Snippet
```java
            }
        }
        _names =  names.toArray(new String[names.size()]);
        _props = props.toArray(new String[props.size()]);
        set(null, true);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[props.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/PluginListValue.java`
#### Snippet
```java
        }
        _names =  names.toArray(new String[names.size()]);
        _props = props.toArray(new String[props.size()]);
        set(null, true);
        valueChanged();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Expression\[values.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        public StringBuilder asValue(AliasContext q) {
            return Expressions.asValue(q, "COALESCE", OPEN_BRACE, Expressions.asValue(q, values == null
                    ? null : values.toArray(new Expression<?>[values.size()]), COMMA), CLOSE_BRACE);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ExpressionImpl\[values.size()\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        @Override
        public void acceptVisit(CriteriaExpressionVisitor visitor) {
            Expressions.acceptVisit(visitor, this, values.toArray(new ExpressionImpl[values.size()]));
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ProductDerivation\[derivations.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
        Collections.sort(derivations, new ProductDerivationComparator());
        _derivations =
            derivations.toArray(new ProductDerivation[derivations.size()]);

        List<String> prefixes = new ArrayList<>(2);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[prefixes.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
                prefixes.add(prefix);
        }
        _prefixes = prefixes.toArray(new String[prefixes.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new MethodDescriptor\[descs.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
        }
        _mds = (MethodDescriptor[]) descs.
            toArray(new MethodDescriptor[descs.size()]);
        return _mds;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Value\[_vals.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
    @Override
    public Value[] getValues() {
        return (Value[]) _vals.toArray(new Value[_vals.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[allowed.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
        }
        if (!allowed.isEmpty())
            pd.setValue(ATTRIBUTE_ALLOWED_VALUES, (String[]) allowed.toArray
                (new String[allowed.size()]));

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[ret.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
            ret.add(sb.toString());
        }
        return ret.toArray(new String[ret.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[_comments.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataParser.java`
#### Snippet
```java
        if (_comments == null || _comments.isEmpty())
            return Commentable.EMPTY_COMMENTS;
        return _comments.toArray(new String[_comments.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[names.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
                appendTypeNames(source, itr.getInputStream(), names);
                if (!names.isEmpty()) {
                    map.put(source, names.toArray(new String[names.size()]));
                }
                names.clear();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[col.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
                    J2DoPrivHelper.existsAction(file))) {
                Collection<String> col = getFromMetaDataFile(file);
                return col.toArray(new String[col.size()]);
            }
        } catch (Exception e) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[names.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
                _loc.get("class-arg", source).getMessage(), e);
        }
        return names.toArray(new String[names.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[classes.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
            }
        }
        return (Class[]) classes.toArray(new Class[classes.size()]);
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[resourceList .size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
                addResources((URL) resources.nextElement(), resourceList);

            return (String[]) resourceList.toArray(new String[resourceList
                .size()]);
        } catch (PrivilegedActionException | IOException pae) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[list.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
            List list = new ArrayList(Arrays.asList(classes));
            list.removeAll(invalid);
            return (Class[]) list.toArray(new Class[list.size()]);
        } else {
            return classes;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[remainder.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
        }

        return remainder.toArray(new String[remainder.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Exception\[exceptions.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/AbstractConcurrentEventManager.java`
#### Snippet
```java
        if (exceptions == null)
            return EMPTY_EXCEPTIONS;
        return (Exception[]) exceptions.toArray
            (new Exception[exceptions.size()]);
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Map.Entry\[_nodeInfo.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/graph/DepthFirstAnalysis.java`
#### Snippet
```java
    public List<Object> getSortedNodes() {
        Map.Entry<Object,NodeInfo>[] entries =
        	_nodeInfo.entrySet().toArray(new Map.Entry[_nodeInfo.size()]);
        Arrays.sort(entries, new NodeInfoComparator(_comp));
        return new NodeList(entries);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[names.size()\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
            name = name.substring(pname.length() + ndLen);
        }
        return names.toArray(new String[names.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Throwable \[errors.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java
        }

        Throwable[] t = (Throwable []) errors.toArray(
            new Throwable [errors.size()]);
        throw new InvalidStateException(_loc.get("tm-not-found")).
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[keys.size()\]'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        if (_deferredMappings.size() > 0) {
            Set<Class<?>> keys = _deferredMappings.keySet();
            Class<?>[] classes = keys.toArray(new Class[keys.size()]);
            for (Class<?> aClass : classes) {
                try {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[_drop.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataTool.java`
#### Snippet
```java
        try {
            if (_drop != null && !_drop.isEmpty()
                && !mdf.drop((Class[]) _drop.toArray(new Class[_drop.size()]),
                MODE_META | MODE_MAPPING | MODE_QUERY, null)) {
                Log log = _conf.getLog(OpenJPAConfiguration.LOG_METADATA);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[_includes.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FetchGroup.java`
#### Snippet
```java
    public String[] getDeclaredIncludes() {
        // only used during serialization; no need to cache
        return (_includes == null) ? new String[0] : _includes.toArray(new String[_includes.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FieldMetaData\[_depths.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FetchGroup.java`
#### Snippet
```java
        if (_depths == null)
            return EMPTY_FIELD_ARRAY;
         return _depths.keySet().toArray(new FieldMetaData[_depths.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[keys.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
                keys.add(getKey(key));
        }
        return (String[]) keys.toArray(new String[keys.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[vendors.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
            vendors.add(getVendor(o));
        }
        return (String[]) vendors.toArray(new String[vendors.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[_hintKeys.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/QueryMetaData.java`
#### Snippet
```java
    public String[] getHintKeys() {
        return (_hintKeys == null) ? EMPTY_KEYS
            : (String[]) _hintKeys.toArray(new String[_hintKeys.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[cpath.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
        }
        if (cpath != null) {
            String[] dirs = (String[]) cpath.toArray(new String[cpath.size()]);
            scan(new ClasspathMetaDataIterator(dirs, newMetaDataFilter()),
                cparser, names, true, dirs);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMetaData\[_metas.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            // prevent concurrent mod errors when resolving one metadata
            // introduces others
            ClassMetaData[] metas = _metas.values().toArray(new ClassMetaData[_metas.size()]);
        for (ClassMetaData classMetaData : metas)
            if (classMetaData != null)
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMetaData\[mapped.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            }
        }
        return mapped.toArray(new ClassMetaData[mapped.size()]);
    }
    /**
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SequenceMetaData\[_seqs.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
        if (_locking) {
            synchronized (this) {
                return _seqs.values().toArray(new SequenceMetaData[_seqs.size()]);
            }
        } else {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SequenceMetaData\[_seqs.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            }
        } else {
            return _seqs.values().toArray(new SequenceMetaData[_seqs.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[_registered.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            // copy into new collection to avoid concurrent mod errors on reentrant
            // registrations
            reg = _registered.toArray(new Class[_registered.size()]);
            _registered.clear();
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new NonPersistentMetaData\[_pawares.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            if (_pawares.isEmpty())
                return EMPTY_NON_PERSISTENT;
            return _pawares.values().toArray(new NonPersistentMetaData[_pawares.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Exception\[_errs.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
                re = new MetaDataException(_loc.get("resolve-errs"))
                    .setNestedThrowables(_errs
                    .toArray(new Exception[_errs.size()]));
            }
            _errs.clear();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new QueryMetaData\[_queries.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
        if (_locking) {
            synchronized (this) {
                return _queries.values().toArray(new QueryMetaData[_queries.size()]);
            }
        } else {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new QueryMetaData\[_queries.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            }
        } else {
            return _queries.values().toArray(new QueryMetaData[_queries.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new NonPersistentMetaData\[_nonMapped.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            if (_nonMapped.isEmpty())
                return EMPTY_NON_PERSISTENT;
            return _nonMapped.values().toArray(new NonPersistentMetaData[_nonMapped.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[value.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfigurationImpl.java`
#### Snippet
```java
    @Override
    public void setTypesWithoutEnhancement(Collection<Class<?>> value) {
        typesWithoutEnhancement.set(value.toArray(new Class[value.size()]));
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[_fgSet.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                _fgs = new String[0];
            else
                _fgs = _fgSet.toArray(new String[_fgSet.size()]);
        }
        return _fgs;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[fields.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
            if (fields == null)
                return EMPTY_STRING_ARRAY;
            return fields.keySet().toArray(new String[fields.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FetchGroup\[_fgMap.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
    public FetchGroup[] getDeclaredFetchGroups() {
        if (_fgs == null) {
            _fgs = (_fgMap == null) ? EMPTY_FETCH_GROUP_ARRAY : _fgMap.values().toArray(new FetchGroup[_fgMap.size()]);
        }
        return _fgs;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FetchGroup\[fgs.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
            fgs.remove(FetchGroup.NAME_ALL);

            _customFGs = fgs.values().toArray(new FetchGroup[fgs.size()]);
        }
        return _customFGs;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FieldMetaData\[res.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
                }
            }
            _allProxyFields = res.toArray(new FieldMetaData[res.size()]);
        }
        return _allProxyFields;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FieldMetaData\[res.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
                }
            }
            _allLrsFields = res.toArray(new FieldMetaData[res.size()]);
        }
        return _allLrsFields;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[subs.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
        if (_subs == null) {
            Collection<Class<?>> subs = _repos.getPCSubclasses(_type);
            _subs = subs.toArray(new Class[subs.size()]);
        }
        return _subs;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[result.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
    	}
    	Collections.sort(result);
    	return result.toArray(new String[result.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[_interfaces.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
        if (_interfaces == null)
            return MetaDataRepository.EMPTY_CLASSES;
        return _interfaces.toArray(new Class[_interfaces.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[_allTypes.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AuditManager.java`
#### Snippet
```java
			Broker broker = (Broker)event.getSource();
			AuditCallback cb = new AuditCallback(broker);
			broker.addLifecycleListener(cb, _allTypes.toArray(new Class<?>[_allTypes.size()]));
			_saved.put(broker, cb);
		} finally {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Exception\[exceptions.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/LifecycleEventManager.java`
#### Snippet
```java
            ret = EMPTY_EXCEPTIONS;
        else
            ret = exceptions.toArray
                (new Exception[exceptions.size()]);

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Throwable\[exceps.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachManager.java`
#### Snippet
```java
            throw (RuntimeException) exceps.get(0);

        Throwable[] t = (Throwable[]) exceps.toArray
            (new Throwable[exceps.size()]);
        if (opt && failed != null)
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Throwable\[exceps.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
            throw (RuntimeException) exceps.get(0);
        throw new UserException(_loc.get("nested-exceps")).
            setNestedThrowables((Throwable[]) exceps.toArray
                (new Throwable[exceps.size()]));
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Throwable\[excs.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractBrokerFactory.java`
#### Snippet
```java
        if (!excs.isEmpty())
            throw new InvalidStateException(_loc.get("nested-exceps")).
                setNestedThrowables(excs.toArray(new Throwable[excs.size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMetaData\[metas.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
            if (metas == null)
                return StoreQuery.EMPTY_METAS;
            return (ClassMetaData[]) metas.toArray
                (new ClassMetaData[metas.size()]);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FilterListener\[c.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
        if (hint instanceof Collection) {
            Collection<FilterListener> c = (Collection<FilterListener>) hint;
            return c.toArray(new FilterListener[c.size()]);
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new AggregateListener\[c.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
        if (hint instanceof Collection) {
            Collection<AggregateListener> c = (Collection<AggregateListener>) hint;
            return c.toArray(new AggregateListener[c.size()]);
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[results.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
                    results.addAll(Arrays.asList(actions));
            }
            return (String[]) results.toArray(new String[results.size()]);
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMetaData\[metas.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
            if (metas == null)
                return StoreQuery.EMPTY_METAS;
            return (ClassMetaData[]) metas.toArray
                (new ClassMetaData[metas.size()]);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Value\[_args.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Args.java`
#### Snippet
```java
    @Override
    public Value[] getValues() {
        return _args.toArray(new Value[_args.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMetaData\[_accessPath.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
     */
    protected ClassMetaData[] getAccessPath() {
        return (ClassMetaData[]) _accessPath.toArray
            (new ClassMetaData[_accessPath.size()]);
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new StateManagerImpl\[_derefAdditions.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        StateManagerImpl[] states = _derefAdditions.
            toArray(new StateManagerImpl[_derefAdditions.size()]);
        _derefAdditions = null;

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[result.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
               }
       }
       return Arrays.toString(result.toArray(new String[result.size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Throwable\[exceps.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        boolean fatal = false;
        Throwable[] t = exceps.toArray(new Throwable[exceps.size()]);
        for (Throwable throwable : t) {
            if (throwable instanceof OpenJPAException
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new StateManagerImpl\[_transAdditions.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        // copy the change set, then clear it for the next iteration
        StateManagerImpl[] states = _transAdditions.
            toArray(new StateManagerImpl[_transAdditions.size()]);
        _transAdditions = null;

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Throwable\[exceps.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            return null;

        Throwable[] t = exceps.toArray(new Throwable[exceps.size()]);
        List<Object> failed = new ArrayList<>(t.length);

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[classes.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ClassRedefiner.java`
#### Snippet
```java
            inst = InstrumentationFactory.getInstrumentation(log);

            Class<?>[] array = classes.keySet().toArray(new Class[classes.size()]);
            log.trace(_loc.get("retransform-types", classes.keySet()));

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Value\[projections.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        List<String> clauses = new ArrayList<>();
        evalProjectionsResultShape(expNode, exps, projections, aliases, clauses);
        exps.projections = projections.toArray(new Value[projections.size()]);
        exps.projectionAliases = aliases.toArray(new String[aliases.size()]);
        exps.projectionClauses = clauses.toArray(new String[clauses.size()]);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[aliases.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        evalProjectionsResultShape(expNode, exps, projections, aliases, clauses);
        exps.projections = projections.toArray(new Value[projections.size()]);
        exps.projectionAliases = aliases.toArray(new String[aliases.size()]);
        exps.projectionClauses = clauses.toArray(new String[clauses.size()]);
        return null;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[clauses.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        exps.projections = projections.toArray(new Value[projections.size()]);
        exps.projectionAliases = aliases.toArray(new String[aliases.size()]);
        exps.projectionClauses = clauses.toArray(new String[clauses.size()]);
        return null;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new JPQLNode\[set.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            Collection<JPQLNode> set = new LinkedHashSet<>();
            findChildrenByID(id, set);
            return set.toArray(new JPQLNode[set.size()]);
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[joins.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        if (joins != null)
            exps.fetchPaths = joins.
                toArray(new String[joins.size()]);
        if (innerJoins != null)
            exps.fetchInnerPaths = innerJoins.
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[innerJoins.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        if (innerJoins != null)
            exps.fetchInnerPaths = innerJoins.
                toArray(new String[innerJoins.size()]);

        return filter;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[_partProperties.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/PartitionedDataCache.java`
#### Snippet
```java
        Arrays.fill(types, _type.getName());
        partitions.setClassNames(types);
        partitions.setProperties(_partProperties.toArray(new String[_partProperties.size()]));
        DataCache[] array = (DataCache[])partitions.instantiate(_type, conf);
        for (DataCache part : array) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FieldMetaData\[decs.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
            if (field.getDeclaringType() == meta.getDescribedType())
                decs.add(field);
        return (FieldMetaData[]) decs.toArray(new FieldMetaData[decs.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ObjectData\[vals.size()\]'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStore.java`
#### Snippet
```java
        meta = getLeastDerived(meta);
        Collection vals = getMap(meta).values();
        return (ObjectData[]) vals.toArray(new ObjectData[vals.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[args.size()\]'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/ant/Java2HTMLConverterTask.java`
#### Snippet
```java
        }
        try {
            Java2HTMLConverter.main(args.toArray(new String[args.size()]));
        } catch (Exception e) {
            throw new BuildException(e);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new AuxiliaryEnhancer\[auxEnhancers.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            }
        }
        _auxEnhancers = (AuxiliaryEnhancer[]) auxEnhancers.toArray
        (new AuxiliaryEnhancer[auxEnhancers.size()]);

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[plist.size()\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        // use reflection to return the right method
        String name = prefix + typeName + "Field";
        Class[] params = (Class[]) plist.toArray(new Class[plist.size()]);

        try {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Predicate\[predicates.size()\]'
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
        }
        if (!predicates.isEmpty())
            q.where(predicates.toArray(new Predicate[predicates.size()]));
        q.orderBy(cb.desc(order.get(PurchaseOrder_.placedOn)));

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Predicate\[predicates.size()\]'
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
        // Sets the evaluation criteria
        if (!predicates.isEmpty())
            q.where(predicates.toArray(new Predicate[predicates.size()]));

        return q;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SQLException\[errs.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLExceptions.java`
#### Snippet
```java
            se = se.getNextException();
        }
        return (SQLException[]) errs.toArray(new SQLException[errs.size()]);
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[cols.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java
            }
        }
        return cols == null ? Schemas.EMPTY_COLUMNS : cols.toArray(new Column[cols.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[_cols.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java

    public Column[] getColumns() {
        return _cols.toArray(new Column[_cols.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Index\[idxList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            while (rs != null && rs.next())
                idxList.add(newIndex(rs));
            return (Index[]) idxList.toArray(new Index[idxList.size()]);
        } finally {
            if (rs != null)
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PrimaryKey\[pkList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                pkList.add(newPrimaryKey(rs));
            }
            return pkList.toArray(new PrimaryKey[pkList.size()]);
        } finally {
            if (rs != null)
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ForeignKey\[fkList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                fkList.add(nfk);
            }
            return (ForeignKey[]) fkList.toArray
                (new ForeignKey[fkList.size()]);
        } finally {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[cols.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/VersionMappingInfo.java`
#### Snippet
```java
            Table table = assignEntry.getKey();
            List<Column> cols = assignEntry.getValue();
            Column[] partTemplates = cols.toArray(new Column[cols.size()]);
            def.populateColumns(vers, table, partTemplates);
            result.addAll(Arrays.asList(createColumns(vers, null, partTemplates,
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[result.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/VersionMappingInfo.java`
#### Snippet
```java
    				table, adapt)));
    	}
    	return result.toArray(new Column[result.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PrimaryKey\[pkList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            while (pks != null && pks.next())
                pkList.add(newPrimaryKey(pks));
            return (PrimaryKey[]) pkList.toArray
                (new PrimaryKey[pkList.size()]);
        } finally {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[columnList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            while (cols != null && cols.next())
                columnList.add(newColumn(cols));
            return (Column[]) columnList.toArray
                (new Column[columnList.size()]);
        } finally {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PrimaryKey\[pkList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                pkList.add(pk);
            }
            return (PrimaryKey[]) pkList.toArray
                (new PrimaryKey[pkList.size()]);
        } finally {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Sequence\[seqList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        while (rs != null && rs.next())
            seqList.add(newSequence(rs));
        return (Sequence[]) seqList.toArray(new Sequence[seqList.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Table\[tableList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                tableList.add(newTable(tables));
            }
            return tableList.toArray(new Table[tableList.size()]);
        } finally {
            if (tables != null) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ForeignKey\[importedKeyList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                importedKeyList.add(nfk);
            }
            return (ForeignKey[]) importedKeyList.toArray
                (new ForeignKey[importedKeyList.size()]);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Index\[indexList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            while (indexes != null && indexes.next())
                indexList.add(newIndex(indexes));
            return (Index[]) indexList.toArray(new Index[indexList.size()]);
        } finally {
            if (indexes != null)
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[sql.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        }

        return (String[]) sql.toArray(new String[sql.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMapping\[_dropMap.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
            mappings = repos.getMappings();
        else if (_dropMap != null)
            mappings = (ClassMapping[]) _dropMap.toArray
                (new ClassMapping[_dropMap.size()]);
        else
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[_dropCls.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
        try {
            if (_dropCls != null && !_dropCls.isEmpty()) {
                Class<?>[] cls = (Class[]) _dropCls.toArray
                    (new Class[_dropCls.size()]);
                if (!io.drop(cls, _mode, null))
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[classes.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
        }

        Class<?>[] act = (Class[]) classes.toArray(new Class[classes.size()]);
        if (ACTION_EXPORT.equals(flags.action)) {
            // run exports until the first export succeeds
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new QueryResultMapping\[values.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
            synchronized (this) {
                Collection values = _results.values();
                return (QueryResultMapping[]) values.toArray(new QueryResultMapping[values.size()]);
            }
        } else {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new QueryResultMapping\[values.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        } else {
            Collection values = _results.values();
            return (QueryResultMapping[]) values.toArray(new QueryResultMapping[values.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[keys.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/QueryResultMapping.java`
#### Snippet
```java
            }
            Collection<String> keys = _rawMappings.keySet();
            return keys.toArray(new String[keys.size()]);
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PCResult\[_pcList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/QueryResultMapping.java`
#### Snippet
```java
            }
            else {
                pcs = _pcList.toArray(new PCResult[_pcList.size()]);
            }
            _pcs = pcs;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Unique\[result.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/FieldMappingInfo.java`
#### Snippet
```java
                result.add(unique);
        }
        return result.toArray(new Unique[result.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new DBIdentifier\[_seconds.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
        if (_seconds == null)
            return new DBIdentifier[0];
        return _seconds.keySet().toArray(new DBIdentifier[_seconds.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Unique\[result.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
            }
        }
        return result.toArray(new Unique[result.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Unique\[uniques.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
            return new Unique[0];
        List<Unique> uniques = _uniques.get(table);
        return uniques.toArray(new Unique[uniques.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Index\[result.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
            }
        }
        return result.toArray(new Index[result.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[cols.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                    }
                }
                _cols = (Column[]) cols.toArray(new Column[cols.size()]);
            }
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMapping\[joinable.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                    if (isSubJoinable(sub))
                        joinable.add(sub);
                _joinSubMaps = (ClassMapping[]) joinable.toArray
                    (new ClassMapping[joinable.size()]);
            }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMapping\[independent.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                    independent.removeAll(clear);

                _assignMaps = (ClassMapping[]) independent.toArray
                    (new ClassMapping[independent.size()]);
            }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[cols.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ElementEmbedValueHandler.java`
#### Snippet
```java

        _vm = vm;
        _cols = (Column[]) cols.toArray(new Column[cols.size()]);
        _args = args.toArray();
        return _cols;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[cols.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
        super.map(vm, name, io, adapt, cols, args);

        vm.setColumns((Column[]) cols.toArray(new Column[cols.size()]));
        _args = args.toArray();
        return vm.getColumns();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Integer\[positions.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
                    positions.add((Integer)list.get(j-1));
            }
            _userParamPositions.put(key, positions.toArray(new Integer[positions.size()]));
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassMapping\[subs.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        List subs = new ArrayList(4);
        addSubclasses(mapping, subs);
        return (ClassMapping[]) subs.toArray(new ClassMapping[subs.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Select\[sels.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        if (unionable) {
            Union union = _store.getSQLFactory().newUnion(
                (Select[]) sels.toArray(new Select[sels.size()]));
            BitSet[] paged = populateUnion(union, mappings, subclasses, facts,
                exps, states, ctx, lrs, eager, start, end);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Select\[sels.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        if (unionable) {
            Union union = _store.getSQLFactory().newUnion(
                (Select[]) sels.toArray(new Select[sels.size()]));
            populateUnion(union, mappings, subclasses, facts, exps, states, ctx,
                false, eager, start, end);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Val\[list.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Args.java`
#### Snippet
```java
            }
        }
        _args = list.toArray(new Val[list.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Table\[_tables.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    @Override
    public Table[] getTables() {
        return (Table[]) _tables.toArray(new Table[_tables.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Sequence\[_seqs.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java

    public Sequence[] getSequences() {
        return (Sequence[]) _seqs.toArray(new Sequence[_seqs.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Schema\[_schemaMap.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGroup.java`
#### Snippet
```java
        if (_schemas == null)
            _schemas = (_schemaMap == null) ? new Schema[0] : (Schema[])
                _schemaMap.values().toArray(new Schema[_schemaMap.size()]);
        return _schemas;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ForeignKey\[exports.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGroup.java`
#### Snippet
```java
            }
        }
        return exports.toArray(new ForeignKey[exports.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[_colList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/LocalConstraint.java`
#### Snippet
```java
        if (_cols == null)
            _cols = (_colList == null) ? Schemas.EMPTY_COLUMNS
                : (Column[]) _colList.toArray(new Column[_colList.size()]);
        return _cols;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Sequence\[_seqMap.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java
        if (_seqs == null)
            _seqs = (_seqMap == null) ? new Sequence[0] : (Sequence[])
                _seqMap.values().toArray(new Sequence[_seqMap.size()]);
        return _seqs;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Table\[_tableMap.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java
        if (_tables == null)
            _tables = (_tableMap == null) ? new Table[0] : (Table[])
                _tableMap.values().toArray(new Table[_tableMap.size()]);
        return _tables;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new DBIdentifier\[tables.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
            parsed[idx][0] = entry.getKey();
            if (tables != null)
                parsed[idx][1] = tables.toArray(new DBIdentifier[tables.size()]);
            idx++;
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new DBIdentifier\[names.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
                names.add(((DBIdentifier)path).clone());
            }
            return names.toArray(new DBIdentifier[names.size()]);
        }
        if (sName instanceof DBIdentifier) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[script.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java
            }

            executeSQL(script.toArray(new String[script.size()]));
        } catch (IOException e) {
            _log.error(e.getMessage(), e);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new DBIdentifier\[tables.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java
                }
                if (!tables.isEmpty())
                    gen.generateSchemas(tables.toArray(new DBIdentifier[tables.size()]));
            }
            _db = gen.getSchemaGroup();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Table\[tables.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java
            }
        }
        Table[] tableArray = tables.toArray(new Table[tables.size()]);
        Connection conn = _ds.getConnection();
        try {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[_consts.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_constCols == null)
            _constCols = (_consts == null) ? Schemas.EMPTY_COLUMNS : (Column[])
                _consts.keySet().toArray(new Column[_consts.size()]);
        return _constCols;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[_constsPK.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_constColsPK == null)
            _constColsPK = (_constsPK == null) ? Schemas.EMPTY_COLUMNS :
                (Column[]) _constsPK.keySet().toArray
                    (new Column[_constsPK.size()]);
        return _constColsPK;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[_joins.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_pks == null)
            _pks = (_joins == null) ? Schemas.EMPTY_COLUMNS : (Column[])
                _joins.values().toArray(new Column[_joins.size()]);
        return _pks;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[_joins.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_locals == null)
            _locals = (_joins == null) ? Schemas.EMPTY_COLUMNS : (Column[])
                _joins.keySet().toArray(new Column[_joins.size()]);
        return _locals;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[slices.size()\]'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/ReplicationPolicy.java`
#### Snippet
```java
        public String[] replicate(Object pc, List<String> slices,
			Object context) {
			return slices.toArray(new String[slices.size()]);
		}
	}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[slices.size()\]'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/SliceInfo.java`
#### Snippet
```java
            throw new InternalException();
        _isReplicated = replicated;
        _slices = slices.toArray(new String[slices.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Unique\[_unqList.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
        if (_unqs == null)
            _unqs = (_unqList == null) ? Schemas.EMPTY_UNIQUES : (Unique[])
                _unqList.toArray(new Unique[_unqList.size()]);
        return _unqs;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[autos.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
                }
                _autoAssign = (autos == null) ? Schemas.EMPTY_COLUMNS
                    : (Column[]) autos.toArray(new Column[autos.size()]);
            }
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Column\[rels.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
                }
                _rels = (rels == null) ? Schemas.EMPTY_COLUMNS
                    : (Column[]) rels.toArray(new Column[rels.size()]);
            }
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Index\[_idxMap.size()\]'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
        if (_idxs == null || _idxs.length == 0)
            _idxs = (_idxMap == null) ? Schemas.EMPTY_INDEXES : (Index[])
                _idxMap.values().toArray(new Index[_idxMap.size()]);
        return _idxs;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ResultObjectProvider\[rops.size()\]'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedStoreQuery.java`
#### Snippet
```java
			}

			ResultObjectProvider[] tmp = rops.toArray(new ResultObjectProvider[rops.size()]);
			ResultObjectProvider result = null;
			boolean[] ascending = getAscending(q);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Executor\[usedExecutors.size()\]'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedStoreQuery.java`
#### Snippet
```java
			} else if (isAscending) {
                result = new OrderingMergedResultObjectProvider(tmp, ascending,
                    usedExecutors.toArray(new Executor[usedExecutors.size()]),
					q, params);
			} else {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ResultObjectProvider\[rops.size()\]'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java

			ResultObjectProvider[] tmp = rops
                    .toArray(new ResultObjectProvider[rops.size()]);
			ResultObjectProvider result = null;
			boolean[] ascending = getAscending(q);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Executor\[usedExecutors.size()\]'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
			} else if (isAscending) {
                result = new OrderingMergedResultObjectProvider(tmp, ascending,
                    usedExecutors.toArray(new Executor[usedExecutors.size()]),
					q, params);
			} else {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new URL\[urls.size()\]'
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java

        ClassLoader jpaRealm =
            new URLClassLoader( (URL[]) urls.toArray( new URL[urls.size()] ), getClass().getClassLoader() );

        // set the new ClassLoader as default for this Thread
```

## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
    public boolean hasInverseManagedFields() {
        if (inverseManagedFields == null) {
            Boolean res = Boolean.FALSE;
            for (FieldMetaData fmd : getFields()) {
                if (fmd.getInverseMetaDatas().length > 0) {
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java

        // Default to FALSE, until proven true.
        Boolean temp = Boolean.FALSE;

        FieldMetaData[] pkFields = getPrimaryKeyFields();
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
        // Default to false, set to true only if this type is abstract and
        // declares a PKField.
        Boolean temp = Boolean.FALSE;

        if (isAbstract()) {
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
        int nAuthorPerBook = PropertyHelper.getInteger(config, "openbook.Book.Author.Count", AUTHOR_PER_BOOK);

        Double priceMax = PropertyHelper.getDouble(config, "openbook.Book.Price.Max",  PRICE_RANGE.getMaximum());
        Double priceMin = PropertyHelper.getDouble(config, "openbook.Book.Price.Min",  PRICE_RANGE.getMinimum());

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java

        Double priceMax = PropertyHelper.getDouble(config, "openbook.Book.Price.Max",  PRICE_RANGE.getMaximum());
        Double priceMin = PropertyHelper.getDouble(config, "openbook.Book.Price.Min",  PRICE_RANGE.getMinimum());

        Integer stockMax = PropertyHelper.getInteger(config, "openbook.Inventory.Max", STOCK_RANGE.getMaximum());
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
        Double priceMin = PropertyHelper.getDouble(config, "openbook.Book.Price.Min",  PRICE_RANGE.getMinimum());

        Integer stockMax = PropertyHelper.getInteger(config, "openbook.Inventory.Max", STOCK_RANGE.getMaximum());
        Integer stockMin = PropertyHelper.getInteger(config, "openbook.Inventory.Min", STOCK_RANGE.getMinimum());

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java

        Integer stockMax = PropertyHelper.getInteger(config, "openbook.Inventory.Max", STOCK_RANGE.getMaximum());
        Integer stockMin = PropertyHelper.getInteger(config, "openbook.Inventory.Min", STOCK_RANGE.getMinimum());

        System.err.println("Creating " + nCustomer + " new Customer");
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java

    private int createAlias(Table table, Object key) {
        Integer i = ctx().nextAlias();
//        System.out.println("\t"+
//                ((_parent != null) ? "Sub" :"") +
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/CallbackMode.java`
#### Snippet
```java
    private final int callbackMode;

    private CallbackMode(int value) {
        callbackMode = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/DetachStateType.java`
#### Snippet
```java
    private final int detachStateConstant;

    private DetachStateType(int value) {
        detachStateConstant = value;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
     *
     */
    public static class StringToInteger implements HintValueConverter {
        private String[] strings;
        private Integer[] numbers;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
     *
     */
    public static class StringToInteger implements HintValueConverter {
        private String[] strings;
        private Integer[] numbers;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
     *
     */
    public static class OpenJPAEnumToInteger implements HintValueConverter {
        private OpenJPAEnum<?> _prototype;

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
     *
     */
    public static class OpenJPAEnumToInteger implements HintValueConverter {
        private OpenJPAEnum<?> _prototype;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
    }

    public static class StringToBoolean implements HintValueConverter {
        @Override
        public Object convert(Object v) {
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
    }

    public static class StringToBoolean implements HintValueConverter {
        @Override
        public Object convert(Object v) {
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
     *
     */
    static class EnumToInteger implements HintValueConverter {
        private Class<? extends Enum<?>> _type;
        private Integer[] map;
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/RestoreStateType.java`
#### Snippet
```java
    private final int restoreStateConstant;

    private RestoreStateType(int value) {
        restoreStateConstant = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ConnectionRetainMode.java`
#### Snippet
```java
    private final int connectionRetainConstant;

    private ConnectionRetainMode(int value) {
        connectionRetainConstant = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AutoClearType.java`
#### Snippet
```java
    private final int autoClearConstant;

    private AutoClearType(int value) {
        autoClearConstant = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryOperationType.java`
#### Snippet
```java
    private final int queryOperationConstant;

    private QueryOperationType(int value) {
        queryOperationConstant = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AutoDetachType.java`
#### Snippet
```java
    private final int autoDetachConstant;

    private AutoDetachType(int value) {
        autoDetachConstant = value;
    }
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/CompileTimeLogger.java`
#### Snippet
```java
 */
public class CompileTimeLogger {
    private static enum Level {TRACE, INFO, WARN, ERROR}

    private static Localizer _loc = Localizer.forPackage(CompileTimeLogger.class);
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        private String type;

        private TypeCategory(String type) {
            this.type = type;
        }
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
     *
     */
    private static enum TypeCategory {
        ATTRIBUTE("jakarta.persistence.metamodel.SingularAttribute"),
        COLLECTION("jakarta.persistence.metamodel.CollectionAttribute"),
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java

    // The following is needed for input into the delimitString() method
    protected static enum localDBIdentifiers {
        SEQUENCE_GEN_SEQ_NAME,
        SEQUENCE_GEN_SCHEMA
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/CurrentTimeExpression.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private static enum Now {
		CURRENT_DATE,
		CURRENT_TIME,
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinitionImpl.java`
#### Snippet
```java
	private Predicate _having;

	private static enum Visit {PROJECTION, EXPRESSION, JOINABLE}

    protected static Localizer _loc =
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionVisitor.java`
#### Snippet
```java
     *
     */
    public static class ParameterVisitor extends AbstractVisitor {
        private final CriteriaQueryImpl<?> query;

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionVisitor.java`
#### Snippet
```java
     *
     */
    public static class ParameterVisitor extends AbstractVisitor {
        private final CriteriaQueryImpl<?> query;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionVisitor.java`
#### Snippet
```java
public interface CriteriaExpressionVisitor {
    // Enumerates order of traversal of nodes
    public static enum TraversalStyle {
        INFIX,    // operand1 operator operand2   e.g. a + b
        POSTFIX,  // operand1 operand2 operator   e.g. a b +
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionVisitor.java`
#### Snippet
```java
public interface CriteriaExpressionVisitor {
    // Enumerates order of traversal of nodes
    public static enum TraversalStyle {
        INFIX,    // operand1 operator operand2   e.g. a + b
        POSTFIX,  // operand1 operand2 operator   e.g. a b +
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionVisitor.java`
#### Snippet
```java
     *
     */
    public static abstract class AbstractVisitor implements CriteriaExpressionVisitor {
        protected final Set<CriteriaExpression> _visited = new HashSet<>();

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionVisitor.java`
#### Snippet
```java
     *
     */
    public static abstract class AbstractVisitor implements CriteriaExpressionVisitor {
        protected final Set<CriteriaExpression> _visited = new HashSet<>();

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ComparisonStyle.java`
#### Snippet
```java
 */
public interface ComparisonStyle {
    public static enum StringComparisonMode {
        EXACT,            // compares exactly preserving case
        CASE_INSENSITIVE, // compares ignoring case
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ComparisonStyle.java`
#### Snippet
```java
 */
public interface ComparisonStyle {
    public static enum StringComparisonMode {
        EXACT,            // compares exactly preserving case
        CASE_INSENSITIVE, // compares ignoring case
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ComparisonStyle.java`
#### Snippet
```java
     *
     */
    static class Default implements ComparisonStyle {
        private boolean excludeDefault = true;
        private boolean excludeNull = true;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/MetaDataFilter.java`
#### Snippet
```java
     * Information about a metadata resource.
     */
    public interface Resource {

        /**
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/JavaVendors.java`
#### Snippet
```java
    private String _virtualMachineClass = null;

    private JavaVendors(String vmClass) {
        _virtualMachineClass = vmClass;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementEmbeddedMapping.java`
#### Snippet
```java
public @interface ElementEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementEmbeddedMapping.java`
#### Snippet
```java
public @interface ElementEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementEmbeddedMapping.java`
#### Snippet
```java
public @interface ElementEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XEmbeddedMapping.java`
#### Snippet
```java
public @interface XEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XEmbeddedMapping.java`
#### Snippet
```java
public @interface XEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XEmbeddedMapping.java`
#### Snippet
```java
public @interface XEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/JoinSyntax.java`
#### Snippet
```java
    private final int joinSyntaxConstant;

    private JoinSyntax(int value) {
        joinSyntaxConstant = value;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/KeyEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/KeyEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/KeyEmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/KeyEmbeddedMapping.java`
#### Snippet
```java
public @interface KeyEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/KeyEmbeddedMapping.java`
#### Snippet
```java
public @interface KeyEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/KeyEmbeddedMapping.java`
#### Snippet
```java
public @interface KeyEmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ResultSetType.java`
#### Snippet
```java
    private final int resultSetConstant;

    private ResultSetType(int value) {
        resultSetConstant = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/FetchDirection.java`
#### Snippet
```java
    private final int resultSetConstant;

    private FetchDirection(int value) {
        resultSetConstant = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/FetchMode.java`
#### Snippet
```java
    private final String[] _names;

    private FetchMode(int value, String... names) {
        eagerFetchConstant = value;
        _names = names;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/EmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/EmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/EmbeddedMapping.java`
#### Snippet
```java

    public static final String TRUE = "true";
    public static final String FALSE = "false";

    String nullIndicatorColumnName() default "";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/EmbeddedMapping.java`
#### Snippet
```java
public @interface EmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/EmbeddedMapping.java`
#### Snippet
```java
public @interface EmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/EmbeddedMapping.java`
#### Snippet
```java
public @interface EmbeddedMapping {

    public static final String TRUE = "true";
    public static final String FALSE = "false";

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/LRSSizeAlgorithm.java`
#### Snippet
```java
    private final String[] _names;

    private LRSSizeAlgorithm(int value, String...aliases) {
        lrsConstant = value;
        _names = aliases;
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/IsolationLevel.java`
#### Snippet
```java
    private final String[] _names;

    private IsolationLevel(int connectionConstant, String... names) {
        _connectionConstant = connectionConstant;
        _names = names;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
     * Column types serialized under different names.
     */
    private static enum ColType {

        COL,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/CacheMarshaller.java`
#### Snippet
```java
     * {@link Configuration}.
     */
    public interface ValidationPolicy {
        /**
         * Returns an object that this policy considers to be valid, based
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
     * Track unique column settings.
	 */
	private static enum UniqueFlag
	{
		TRUE,
```

### UnnecessaryModifier
Modifier `transient` is redundant for a 'static' field
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/QueryException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private static final transient Localizer _loc =
            Localizer.forPackage(QueryException.class);

```

### UnnecessaryModifier
Modifier `transient` is redundant for a 'static' field
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/OptimisticException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private static final transient Localizer _loc = Localizer.forPackage
        (OptimisticException.class);

```

### UnnecessaryModifier
Modifier `transient` is redundant for a 'static' field
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ObjectNotFoundException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private static final transient Localizer _loc = Localizer.forPackage
        (ObjectNotFoundException.class);

```

### UnnecessaryModifier
Modifier `transient` is redundant for a 'static' field
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ReferentialIntegrityException.java`
#### Snippet
```java
    public static final int IV_MIXED = 4;

    private static final transient Localizer _loc = Localizer.forPackage
        (ReferentialIntegrityException.class);

```

### UnnecessaryModifier
Modifier `transient` is redundant for a 'static' field
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/LockException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private static final transient Localizer _loc = Localizer.forPackage(LockException.class);

    private int timeout   = -1;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * Populate an instance by simply assigning the 0-th element of the input values.
     */
    public static class Assign<T> implements FillStrategy<T> {
        @Override
        public T fill(Object[] values, Class<?>[] types, String[] aliases) {
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * Populate an instance by simply assigning the 0-th element of the input values.
     */
    public static class Assign<T> implements FillStrategy<T> {
        @Override
        public T fill(Object[] values, Class<?>[] types, String[] aliases) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * element value as argument.
     */
    public static class Bean<T> implements FillStrategy<T> {
        private final Class<T> cls;
        private Method[] setters;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * element value as argument.
     */
    public static class Bean<T> implements FillStrategy<T> {
        private final Class<T> cls;
        private Method[] setters;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * by index else fill the values with alias key.
     */
    public static class Factory<T> implements FillStrategy<T> {
        final ObjectFactory<T> factory;
        final Method putMethod;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * by index else fill the values with alias key.
     */
    public static class Factory<T> implements FillStrategy<T> {
        final ObjectFactory<T> factory;
        final Method putMethod;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * Construct and populate an instance by the given constructor and arguments.
     */
    public static class NewInstance<T> implements FillStrategy<T> {
        private Constructor<? extends T> cons;
        private Class<T> cls;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * Construct and populate an instance by the given constructor and arguments.
     */
    public static class NewInstance<T> implements FillStrategy<T> {
        private Constructor<? extends T> cons;
        private Class<T> cls;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * The instance is a created by the no-argument constructor of the declaring class of the given method.
     */
    public static class Map<T> implements FillStrategy<T> {
        private final Method putMethod;

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * The instance is a created by the no-argument constructor of the declaring class of the given method.
     */
    public static class Map<T> implements FillStrategy<T> {
        private final Method putMethod;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * @param <T> must be an array type.
     */
    public static class Array<T> implements FillStrategy<T> {
        private final Class<?> cls;
        public Array(Class<T> arrayCls) {
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
     * @param <T> must be an array type.
     */
    public static class Array<T> implements FillStrategy<T> {
        private final Class<?> cls;
        public Array(Class<T> arrayCls) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryStatistics.java`
#### Snippet
```java
	 * @param <T>
	 */
	public static class None<T> implements QueryStatistics<T> {
        
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryStatistics.java`
#### Snippet
```java
	 * @param <T>
	 */
	public static class None<T> implements QueryStatistics<T> {
        
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryStatistics.java`
#### Snippet
```java
	 *
	 */
	public static class Default<T> implements QueryStatistics<T> {
	    
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryStatistics.java`
#### Snippet
```java
	 *
	 */
	public static class Default<T> implements QueryStatistics<T> {
	    
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PreparedQueryCache.java`
#### Snippet
```java
	 *
	 */
	public interface Exclusion {
	    /**
	     * Affirms if this exclusion is strong i.e. can never be reversed.
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreQuery.java`
#### Snippet
```java
     * A query result range.
     */
    public static class Range {
        public long start = 0L;
        public long end = Long.MAX_VALUE;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreQuery.java`
#### Snippet
```java
     * A query result range.
     */
    public static class Range {
        public long start = 0L;
        public long end = Long.MAX_VALUE;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreQuery.java`
#### Snippet
```java
     * @author Marc Prud'hommeaux
     */
    public interface Executor {

        /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTCommand.java`
#### Snippet
```java
     * Supported format monikers.
     */
    public static enum Format {xml, json}

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTCommand.java`
#### Snippet
```java
     * Supported format monikers.
     */
    public static enum Format {xml, json}

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/MetamodelHelper.java`
#### Snippet
```java
     *
     */
    public static enum AttributeCategory {
        ID, VERSION, BASIC, ENUM, EMBEDDED, LOB,
        ONE_TO_ONE, MANY_TO_ONE, ONE_TO_MANY, ELEMENT_COLLECTION, MANY_TO_MANY
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/Union.java`
#### Snippet
```java
     * A callback used to create the selects in a SQL union.
     */
    public interface Selector {

        /**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `openjpa-examples/simple/src/main/java/relations/Deity.java`
#### Snippet
```java
    private Set<Deity> children;

    public static enum Gender { MALE, FEMALE }


```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/ReplicationPolicy.java`
#### Snippet
```java
	 *
	 */
	public static class Default implements ReplicationPolicy {
		@Override
        public String[] replicate(Object pc, List<String> slices,
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/ReplicationPolicy.java`
#### Snippet
```java
	 *
	 */
	public static class Default implements ReplicationPolicy {
		@Override
        public String[] replicate(Object pc, List<String> slices,
```

### UnnecessaryModifier
Modifier `transient` is redundant for a 'static' field
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/SliceInfo.java`
#### Snippet
```java
	private String[] _slices;

	private static transient Localizer _loc = Localizer.forPackage(SliceInfo.class);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributionPolicy.java`
#### Snippet
```java
	 *
	 */
	public static class Default implements DistributionPolicy {
		private static Random RNG = new Random();
        @Override
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributionPolicy.java`
#### Snippet
```java
	 *
	 */
	public static class Default implements DistributionPolicy {
		private static Random RNG = new Random();
        @Override
```

## RuleId[id=MagicConstant]
### MagicConstant
Should be one of: Connection.TRANSACTION_NONE, Connection.TRANSACTION_READ_UNCOMMITTED, ...
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/ConfiguringConnectionDecorator.java`
#### Snippet
```java
            conn = new ConfiguringConnection(conn);
        if (_isolation != -1 && _isolation != Connection.TRANSACTION_NONE)
            conn.setTransactionIsolation(_isolation);
        return conn;
    }
```

### MagicConstant
Should be one of: Calendar.ERA, Calendar.YEAR, Calendar.MONTH, Calendar.WEEK_OF_YEAR, Calendar.WEEK_OF_MONTH, ...
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ClearableScheduler.java`
#### Snippet
```java
            if (times[depth] != WILDCARD) {
                for (int i = 0; i < times[depth].length; i++) {
                    time.set(UNITS[depth], times[depth][i]);
                    if (matches(last, now, time, times, depth + 1))
                        return true;
```

### MagicConstant
Should be one of: Calendar.JANUARY, Calendar.FEBRUARY, Calendar.MARCH, Calendar.APRIL, Calendar.MAY, ...
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        final Date date = new Date(val.getTime());
        date.setYear(70);
        date.setMonth(0);
        date.setDate(1);
        val = new Time(date.getTime());
```

### MagicConstant
Should be one of: ResultSet.TYPE_FORWARD_ONLY, ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.TYPE_SCROLL_SENSITIVE
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
            stmnt = conn.prepareStatement(getSQL());
        else
            stmnt = conn.prepareStatement(getSQL(), rsType, rsConcur);
        try {
            setParameters(stmnt);
```

### MagicConstant
Should be one of: ResultSet.FETCH_FORWARD, ResultSet.FETCH_REVERSE, ResultSet.FETCH_UNKNOWN
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                if (rsType != ResultSet.TYPE_FORWARD_ONLY
                    && fetch.getFetchDirection() != ResultSet.FETCH_FORWARD)
                    stmnt.setFetchDirection(fetch.getFetchDirection());
            }
            return stmnt;
```

### MagicConstant
Should be one of: ResultSet.FETCH_FORWARD, ResultSet.FETCH_REVERSE, ResultSet.FETCH_UNKNOWN
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                if (rsType != ResultSet.TYPE_FORWARD_ONLY
                    && fetch.getFetchDirection() != ResultSet.FETCH_FORWARD)
                    stmnt.setFetchDirection(fetch.getFetchDirection());
            }
            return stmnt;
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`if` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        AnnotationBuilder ab = addAnnotation(
            getFieldAnnotationType (fmd, strat), fmd);
        if (fmd.isPrimaryKey() && strat == PersistenceStrategy.EMBEDDED)
            ; // noop
        else if (fmd.isPrimaryKey())
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (fmd.isPrimaryKey() && strat == PersistenceStrategy.EMBEDDED)
            ; // noop
        else if (fmd.isPrimaryKey())
            ; // noop
        else if (fmd.isVersion())
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        else if (fmd.isPrimaryKey())
            ; // noop
        else if (fmd.isVersion())
            ; // noop
        else {
```

### EmptyStatementBody
`else` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
			if (provider == null || provider.equals(_providerImplName)) {
				names.add(unit.getPersistenceUnitName());
			} else {
				// Should trace something, but logging isn't configured yet.
				// Swallow.
```

### EmptyStatementBody
`else` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
                schema.add(extendableXSDIS);
            }
            else {
                // TODO: log/trace
            }
```

### EmptyStatementBody
`else` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
                schema.add(openjpaXSDIS);
            }
            else {
                // TODO: log/trace
            }
```

### EmptyStatementBody
`for` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
                Class<?> component;		// component class
                int dcount;			    // dimension
                for (dcount=1; cname.charAt(dcount)=='['; dcount++) ;
                if (cname.charAt(dcount) == 'L') {
                    component = lookupClass(cname.substring(dcount+1,
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        public Constructor addCodeLine(String line) {
            // This doesn't handle try{ ... catch(){ if{
            if (line.endsWith("{") || line.endsWith("}")) {

            }
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
                throw new IllegalStateException("abstract method " + name + " can not have body");
            // This doesn't handle try{ ... catch(){ if{
            if (line.endsWith("{") || line.endsWith("}")) {

            }
```

### EmptyStatementBody
`for` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/JarFileURLMetaDataIterator.java`
#### Snippet
```java
            ByteArrayOutputStream bout = new ByteArrayOutputStream();
            byte[] buf = new byte[1024];
            for (int r; (r = in.read(buf)) != -1; bout.write(buf, 0, r))
                ;
            content = bout.toByteArray();
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/CFMetaDataParser.java`
#### Snippet
```java
                if (name.equals("entity"))
                    endExtendedClass(name);
                else if (name.equals("attributes")) {
                    // do nothing
                }
```

### EmptyStatementBody
`for` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/FileMetaDataIterator.java`
#### Snippet
```java
                } else {
                    content = new byte[(int) len];
                    for (int r, o = 0; o < content.length && (r = fin.
                        read(content, o, content.length - o)) != -1; o += r);
                }
```

### EmptyStatementBody
`for` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ZipStreamMetaDataIterator.java`
#### Snippet
```java
        ByteArrayOutputStream bout = new ByteArrayOutputStream();
        byte[] buf = new byte[1024];
        for (int r; (r = _stream.read(buf)) != -1; bout.write(buf, 0, r)) ;
        _buf = bout.toByteArray();
        _stream.closeEntry();
```

### EmptyStatementBody
`for` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ZipFileMetaDataIterator.java`
#### Snippet
```java
            ByteArrayOutputStream bout = new ByteArrayOutputStream();
            byte[] buf = new byte[1024];
            for (int r; (r = in.read(buf)) != -1; bout.write(buf, 0, r)) ;
            content = bout.toByteArray();
        } else {
```

### EmptyStatementBody
`for` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/TemporaryClassLoader.java`
#### Snippet
```java
        byte[] b = new byte[1024];
        try {
            for (int n = 0; (n = resource.read(b, 0, b.length)) != -1;
                bout.write(b, 0, n))
                ;
```

### EmptyStatementBody
`for` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Files.java`
#### Snippet
```java
                J2DoPrivHelper.newFileOutputStreamAction(to));
            BufferedOutputStream outbuf = new BufferedOutputStream(out);
            for (int b; (b = inbuf.read()) != -1; outbuf.write(b)) ;
            outbuf.flush();
            return true;
```

### EmptyStatementBody
`for` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
            boolean isDelim = (c == ':' || c == '=');

            for (; pos < line.length()
                && isSpace(c = line.charAt(pos)); pos++)
                ;
```

### EmptyStatementBody
`while` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
                (modifiedKeys.contains(propertyKey) ||
                    newKeys.contains(propertyKey))) {
                while (keys.remove(propertyKey)) ;
                return writeProperty(propertyKey, pw, needsNewline);
            }
```

### EmptyStatementBody
`while` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractReferenceMap.java`
#### Snippet
```java
        super.clear();
        // drain the queue
        while (queue.poll() != null) {
            // empty
        }
```

### EmptyStatementBody
`while` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
        // clear out ref queue. We don't need to expunge entries
        // since table is getting cleared.
        while (queue.poll() != null)
            ;
        table = new Entry[table.length];
```

### EmptyStatementBody
`while` statement has empty body
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
        // additional entries to go stale.    Removing these entries from
        // the reference queue will make them eligible for reclamation.
        while (queue.poll() != null)
            ;
    }
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        Column col = new Column();
        String name = attrs.getValue("name");
        if (!StringUtil.isEmpty(name));
            col.setIdentifier(DBIdentifier.newColumn(name, delimit()));
        String columnDefinition= attrs.getValue("column-definition");
```

### EmptyStatementBody
`else` statement has empty body
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/Proxies.java`
#### Snippet
```java
                } else if (sm.getContext() != null && sm.getContext().getConfiguration() != null) {
                    compat = sm.getContext().getConfiguration().getCompatibilityInstance();
                } else {
                    // no-op - using a StateManager, but no Compatibility settings available
                }
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
                        // skip
                    }
                    if (value2 != null && value2 instanceof Subquery)
                        ;  // complex having with subquery, validation is performed by DBMS
                    else
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            else if (node.id == JJTINNERJOIN)
                exp = addJoin(node, true, exp);
            else if (node.id == JJTINNERFETCHJOIN)
                ; // we handle inner fetch joins in the evalFetchJoins() method
            else if (node.id == JJTOUTERFETCHJOIN)
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            else if (node.id == JJTINNERFETCHJOIN)
                ; // we handle inner fetch joins in the evalFetchJoins() method
            else if (node.id == JJTOUTERFETCHJOIN)
                ; // we handle outer fetch joins in the evalFetchJoins() method
            else
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-examples/openbooks/src/main/java/openbook/tools/util/URIUtils.java`
#### Snippet
```java
        Stack<String> outputSegments = new Stack<>();
        for (int i = 0; i < inputSegments.length; i++) {
            if ((inputSegments[i].length() == 0) || (".".equals(inputSegments[i]))) {
                // Do nothing
            } else if ("..".equals(inputSegments[i])) {
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-examples/openbooks/src/main/java/openbook/server/PersistenceService.java`
#### Snippet
```java
            lock.lock();
            EntityManager em = getEntityManager();
            if (isManaged) {

            } else {
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/FieldMapping.java`
#### Snippet
```java
    @Override
    public void syncMappingInfo() {
        if (isVersion()) {
            // we rely on the fact that the version will setup our mapping
            // info correctly when it is synced
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributedBrokerImpl.java`
#### Snippet
```java
            super.setStateManager(id, sm, status);
        } catch (Exception e) {
            if (status == 0) { // STATUS_INIT
                // ignore
            }
```

### EmptyStatementBody
`if` statement has empty body
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
                String newKey = PREFIX_OPENJPA + key.substring(prefix.length());
                result.put(newKey, value);
            } else if (key.startsWith(PREFIX_SLICE)) {
                // ignore keys that are in 'slice.' namespace but not this slice
            } else if (key.startsWith(PREFIX_OPENJPA)) {
```

## RuleId[id=StaticInitializerReferencesSubClass]
### StaticInitializerReferencesSubClass
Referencing subclass PNewFlushedDeletedState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-New-Flushed-Deleted
     */
    public static final PCState PNEWFLUSHEDDELETED = new PNewFlushedDeletedState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PNonTransDirtyState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-Dirty-Nontransactinoal
     */
    public static final PCState PNONTRANSDIRTY = new PNonTransDirtyState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass EDeletedState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Embedded-Deleted
     */
    public static final PCState EDELETED = new EDeletedState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass TDirtyState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Transient-Dirty
     */
    public static final PCState TDIRTY = new TDirtyState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PCleanState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-Clean
     */
    public static final PCState PCLEAN = new PCleanState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass TCleanState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Transient-Clean
     */
    public static final PCState TCLEAN = new TCleanState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PNewFlushedDeletedFlushedState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-New-Flushed-Deleted-Flushed
     */
    public static final PCState PNEWFLUSHEDDELETEDFLUSHED = new PNewFlushedDeletedFlushedState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PNewState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-New
     */
    public static final PCState PNEW = new PNewState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PNewDeletedState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-New-Deleted
     */
    public static final PCState PNEWDELETED = new PNewDeletedState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PNewProvisionalState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-New-Provisional
     */
    public static final PCState PNEWPROVISIONAL = new PNewProvisionalState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass ECleanState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Embedded-Clean
     */
    public static final PCState ECLEAN = new ECleanState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass TransientState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Transient; unmanaged instance
     */
    public static final PCState TRANSIENT = new TransientState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PDeletedState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-Deleted
     */
    public static final PCState PDELETED = new PDeletedState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass TLoadedState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Transient-Loaded
     */
    public static final PCState TLOADED = new TLoadedState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PDirtyState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-Dirty
     */
    public static final PCState PDIRTY = new PDirtyState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass ENonTransState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Embedded-Nontransactional
     */
    public static final PCState ENONTRANS = new ENonTransState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass EDirtyState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Embedded-Dirty
     */
    public static final PCState EDIRTY = new EDirtyState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PNonTransDeletedState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-Deleted-Nontransactional
     */
    public static final PCState PNONTRANSDELETED = new PNonTransDeletedState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass HollowState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Hollow; exists in data store
     */
    public static final PCState HOLLOW = new HollowState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PNonTransNewState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-New-Nontransactional
     */
    public static final PCState PNONTRANSNEW = new PNonTransNewState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass PDeletedFlushedState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-Deleted-Flushed
     */
    public static final PCState PDELETEDFLUSHED = new PDeletedFlushedState();

    private static Localizer _loc = Localizer.forPackage(PCState.class);
```

### StaticInitializerReferencesSubClass
Referencing subclass PNonTransState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Persistent-Nontransactinoal
     */
    public static final PCState PNONTRANS = new PNonTransState();

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass ECopyState from superclass PCState initializer might lead to class loading deadlock
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
     * Embedded-Copy
     */
    public static final PCState ECOPY = new ECopyState();

    /**
```

## RuleId[id=EmptyFinallyBlock]
### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java

        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java

        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            }
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java

        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java

        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            }
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java

        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java

        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java

        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            state.channel = _channel;
        }
        finally {
        }
    }
```

## RuleId[id=SortedCollectionWithNonComparableKeys]
### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
    @Override
    public java.util.Set<SingularAttribute<? super X, ?>> getSingularAttributes() {
        return filter(attrs, new TreeSet<>(),
                singularAttributeFilter);
    }
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
    @Override
    public java.util.Set<Attribute<X, ?>> getDeclaredAttributes() {
        return filter(attrs, new TreeSet<>(),
                declaredAttributeFilter);
    }
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
    @Override
    public java.util.Set<SingularAttribute<X, ?>> getDeclaredSingularAttributes() {
        return filter(attrs, new TreeSet<>(),
                declaredAttributeFilter,
                singularAttributeFilter);
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/DualTreeBidiMap.java`
#### Snippet
```java
     */
    public DualTreeBidiMap(final Map<? extends K, ? extends V> map) {
        super(new TreeMap<>(), new TreeMap<>());
        putAll(map);
        this.comparator = null;
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/DualTreeBidiMap.java`
#### Snippet
```java
     */
    public DualTreeBidiMap(final Map<? extends K, ? extends V> map) {
        super(new TreeMap<>(), new TreeMap<>());
        putAll(map);
        this.comparator = null;
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/DualTreeBidiMap.java`
#### Snippet
```java
     */
    public DualTreeBidiMap() {
        super(new TreeMap<>(), new TreeMap<>());
        this.comparator = null;
        this.valueComparator = null;
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/DualTreeBidiMap.java`
#### Snippet
```java
     */
    public DualTreeBidiMap() {
        super(new TreeMap<>(), new TreeMap<>());
        this.comparator = null;
        this.valueComparator = null;
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        if (fields == null || fields.length == 0)
            return Collections.EMPTY_SET;
        Set<T> result = new TreeSet<>();
        for (Field f : fields) {
            if (mods == f.getModifiers()
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/PrototypeFactory.java`
#### Snippet
```java
 */
public class PrototypeFactory<K,T> {
    private Map<K, Class<? extends T>> _registry = new TreeMap<>();

    /**
```

## RuleId[id=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of local variable `dim`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
                    component = primitiveType(cname.charAt(dcount));
                }
                int dim[] = new int[dcount];
                for (int i=0; i<dcount; i++) {
                    dim[i]=0;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `tab`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java
        if (value == null) return containsNullValue();

        Entry tab[] = table;
        for (Entry entry : tab) {
            for (Entry e = entry; e != null; e = e.next) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `tab`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java
     */
    private boolean containsNullValue() {
        Entry tab[] = table;
        for (Entry entry : tab) {
            for (Entry e = entry; e != null; e = e.next) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `oldMap`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
    private void rehash() {
        int oldCapacity = table.length;
        Entry oldMap[] = table;

        int newCapacity = oldCapacity * 2 + 1;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `newMap`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java

        int newCapacity = oldCapacity * 2 + 1;
        Entry newMap[] = new Entry[newCapacity];

        for (int i = oldCapacity; i-- > 0;) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `oldEntries`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
            data = new HashEntry[newCapacity];
        } else {
            final HashEntry<K, V> oldEntries[] = data;
            final HashEntry<K, V> newEntries[] = new HashEntry[newCapacity];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `newEntries`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
        } else {
            final HashEntry<K, V> oldEntries[] = data;
            final HashEntry<K, V> newEntries[] = new HashEntry[newCapacity];

            modCount++;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `state`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
            return;
        }
        boolean state[] = new boolean[2];
        try {
            dProxy.setDirectAccess(true);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `vals`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        int nChild = node.getChildCount();

        Object vals[] = new Value[nChild];
        for (int i = 0; i < nChild; i++)
            vals[i] = eval(node.children[i]);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `exp`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

        Object val = eval(lastChild(node));
        Object exp[] = new Expression[nChild - 2];
        for (int i = 1; i < nChild - 1; i++)
            exp[i-1] = eval(node.children[i]);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `c`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
                children = new JPQLNode[i + 1];
            } else if (i >= children.length) {
                JPQLNode c[] = new JPQLNode[i + 1];
                System.arraycopy(children, 0, c, 0, children.length);
                children = c;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `exp`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

        Object val = eval(lastChild(node));
        Object exp[] = new Expression[nChild - 1];
        for (int i = 0; i < nChild - 1; i++)
            exp[i] = eval(node.children[i]);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parmOrder`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        }

        int parmOrder[] = new int[pkfields.size()];
        for (BCMethod method : methods) {
            // constructor must be public
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parmOrder`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                // search for a constructor on the IdClass that can be used
                // to construct the IdClass
                int parmOrder[] = getIdClassConstructorParmOrder(oidType, pkfields, fmds);
                if (parmOrder != null) {
                    // If using a field manager, values must be loaded into locals so they can be properly ordered
```

### CStyleArrayDeclaration
C-style array declaration of local variable `majMin`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
                // The product version looks like "8.3.5".
                String productVersion = metaData.getDatabaseProductVersion();
                String majMin[] = productVersion.split("\\.");
                maj = Integer.parseInt(majMin[0]);
                min = Integer.parseInt(majMin[1]);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `aliases`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            }

            Object aliases[] = _sel._aliases.values().toArray();
            boolean found1 = false;
            boolean found2 = false;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `aliases`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            if (parent._aliases == null)
                return;
            Object aliases[] = parent._aliases.values().toArray();
            boolean found1 = false;
            boolean found2 = false;
```

### CStyleArrayDeclaration
C-style array declaration of parameter `ownerCols`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/RowImpl.java`
#### Snippet
```java
    }

    private void setJoinRefColumn(OpenJPAStateManager inverseSm, Column ownerCols[], Column inverseCol,
                                   Object val) {
        OpenJPAStateManager ownerSm = getPrimaryKey();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `cols`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/RowImpl.java`
#### Snippet
```java
                // look for any single column in this field references the
                // same column as the foreign key target column
                Column cols[] = ((FieldMapping) ownerFM).getColumns();
                if (cols.length == 1            // only support attribute of non-compound foreign key
                        && cols != ownerCols    // not @Id field
```

### CStyleArrayDeclaration
C-style array declaration of local variable `fms`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/JDBCRelatedFieldOrder.java`
#### Snippet
```java
            return elem.getFieldMapping(_fm.getIndex());
        else {
            FieldMapping fms[] = elem.getFieldMappings();
            for (FieldMapping fieldMapping : fms) {
                ValueMapping vm = (ValueMapping) fieldMapping.getValue();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `fms`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/MixedLockManager.java`
#### Snippet
```java
        if(!dict.supportsLockingWithMultipleTables) {
            // look for columns mapped to secondary tables which need to be locked
            FieldMapping fms[] = mapping.getFieldMappings();
            for( FieldMapping fm : fms) {
                DBIdentifier secTableName = fm.getMappingInfo().getTableIdentifier();
```

### CStyleArrayDeclaration
C-style array declaration of field `discValue`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/CollectionParam.java`
#### Snippet
```java
        public ClassMapping[] mapping = null;
        public Discriminator[] disc = null;
        public Object discValue[] = null;

        ParamExpState(Object params) {
```

### CStyleArrayDeclaration
C-style array declaration of field `_compoundIdentifier`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    // can have multi-part names, such as {schema, table} and should be stored
    // as a QualifiedDBIdentifier.
    private static boolean _compoundIdentifier[] = new boolean[DBIdentifierType.values().length];

    static {
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (43 lines)
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintHandler.java`
#### Snippet
```java


//    protected boolean hasPrecedent(String key) {
//        boolean hasPrecedent = true;
//        String[] list = precedenceMap.get(key);
```

### CommentedOutCode
Commented out code (6 lines)
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
        throws SAXException {

//        String elementClassCriteriaString = attrs.getValue("element-class-criteria");
//        if (!StringUtil.isEmpty(elementClassCriteriaString)) {
//            FieldMapping fm = (FieldMapping) fmd;
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java

    protected void evalGrouping(QueryExpressions exps, ExpressionFactory factory, CriteriaQueryImpl<?> q) {
        //    exps.grouping = null; // Value[]
        //    exps.groupingClauses = null; // String[]
        List<Expression<?>> groups = q.getGroupList();
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
                // NOTE: Following was removed to hide Value.INVISIBLE properties, like connectionPassword
                // if key in existing properties, we already know value is up to date
                //if (_props != null && Configurations.containsProperty(val, _props) && val.isVisible())
                //    continue;
                str = val.getString();
```

### CommentedOutCode
Commented out code (6 lines)
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/JDBCFetchPlanImpl.java`
#### Snippet
```java

//        "openjpa.FetchPlan.FetchDirection"
//        _hints.add("openjpa.FetchPlan.LockScope");
//        _hints.add("openjpa.FetchPlan.LockTimeout");
//        _hints.add("openjpa.FetchPlan.MaxFetchDepth");
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WLSManagedRuntime.java`
#### Snippet
```java
    public TransactionManager getTransactionManager()
        throws Exception {
        // return TransactionHelper.getTransactionHelper ().
        //	getTransactionManager ();
        Object o = _txHelperMeth.invoke(null, null);
```

### CommentedOutCode
Commented out code (23 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultShape.java`
#### Snippet
```java
    }

//    /**
//     * Construct a shape with the MAP fill strategy to invoke the given method.
//     *
```

### CommentedOutCode
Commented out code (47 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
    }

//    private static void convertLiteralToAvoidCast(Value val1, Value val2) {
//        boolean l1 = val1 instanceof Literal;
//        boolean l2 = val2 instanceof Literal;
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            : fmd.getValue();
        if (vm.getEmbeddedMetaData() != null) {
            //throw parseException(EX_USER, "bad-predicate",
            //    new Object[]{ currentQuery() }, null);
            String argStr = _loc.get("bad-predicate",
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/DynamicStorageGenerator.java`
#### Snippet
```java
        JumpInstruction ifins = null;
        if (objectCount > 0) {
            // if (objects == null)
            // 		objects = new Object[objectCount];
            code.aload().setThis();
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/DynamicStorageGenerator.java`
#### Snippet
```java
            tabins.addTarget(code.aload().setThis());
            if (typeCode >= JavaTypes.OBJECT) {
                // if (objects == null)
                // 		return null;
                // return objects[objectCount];
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/DynamicStorageGenerator.java`
#### Snippet
```java
            tabins.addTarget(code.aload().setThis());
            if (typeCode >= JavaTypes.OBJECT) {
                // if (objects == null)
                // 		objects = new Object[totalObjects];
                code.aload().setThis();
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java
        // add synchronized isLoaded call.
        // public synchronized boolean isLoaded (int field)
        // {
        // 		return super.isLoaded (field);
        // }
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java
        Code code = meth.getCode(true);

        // if (val == null)
        // 		return null;
        code.aload().setParam(1);
```

### CommentedOutCode
Commented out code (6 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java
        code.istore().setLocal(local);

        // if (type != JavaTypes.COLLECTION &&
        // 	   type != JavaTypes.MAP &&
        // 	   type != JavaTypes.ARRAY)
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
        code = meth.getCode(true);

        // if (sm.getVersion() == null)
        // 		sm.setVersion(version);
        code.aload().setParam(0);
```

### CommentedOutCode
Commented out code (9 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
        getter.makePublic();
        Code code = getter.getCode(true);
        // if (type == null) {
        // 		try {
        // 			type = Class.forName
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
            code.ifnull().setTarget(nullTarget);

            // if (fieldImpl == null)
            // 		fieldImpl = new Object[fields];
            code.aload().setThis();
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
            code.putfield().setField(impl);

            // fieldImpl[arrIdx] = obj;
            // return;
            ifins.setTarget(code.aload().setThis());
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
            code.vreturn();

            // if (fieldImpl != null)
            // 		fieldImpl[index] = null;
            code.next(); // step over nullTarget
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
                new Class[]{ int.class });
        } else {
            // Object val = toData(sm.getMetaData().getField(index),
            // 		sm.fetchField(index, false), sm.getContext());
            int local = code.getNextLocalsIndex();
```

### CommentedOutCode
Commented out code (6 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
            code.astore().setLocal(local);

            // if (val == NULL) {
            // 		val = null;
            // 		loaded.clear(index);
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
            code.vreturn();
        else {
            // if (sm.isImplDataCacheable())
            // 		setImplData(sm.getImplData());
            impl = addBeanField(bc, "implData", Object.class);
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
            code.vreturn();
        else {
            // if (sm.getImplData() == null && implData != null)
            // 		sm.setImplData(impl, true);
            code.aload().setParam(0);
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
            new Class[]{ OpenJPAStateManager.class });
        Code code = meth.getCode(true);
        // return getStorageGenerator().generatePCData
        // 		(sm.getId(), sm.getMetaData());
        code.aload().setThis();
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
                    boolean.class, new Class[]{ int.class });
                jumps.add(code.ifne());
                // Object val = sm.getIntermediate(index);
                // if (val != null)
                // 		objects[objectCount] = val;
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/JMXProvider.java`
#### Snippet
```java
            // Multiple attempts to capture and release the server will result in a
            // runtime exception.
            // MBeanServerFactory.releaseMBeanServer(getMBeanServer());
            // _mbs = null;
            setStarted(false);
```

### CommentedOutCode
Commented out code (6 lines)
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTServlet.java`
#### Snippet
```java
//        log("-----------------------------------------------------------");
        log(r.getRemoteUser() + "@" + r.getRemoteHost() + ":" + r.getRemotePort() + "[" + r.getPathInfo() + "]");
//        log("Request URL    = [" + request.getRequestURL() + "]");
//        log("Request URI    = [" + request.getRequestURI() + "]");
//        log("Servlet Path   = [" + request.getServletPath() + "]");
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
     */
    private void addExtractObjectIdFieldValueCode(Code code, FieldMetaData pk) {
        // if (val != null)
        //  val = ((PersistenceCapable) val).pcFetchObjectId();
        int pc = code.getNextLocalsIndex();
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java

        // for datastore / single-field identity:
        // if (val != null)
        //   val = ((OpenJPAId) val).getId();
        ClassMetaData pkmeta = pk.getDeclaredTypeMetaData();
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        Code code = method.getCode(true);

        // if (pcStateManager != null)
        //	pcStateManager = pcStateManager.replaceStateManager(sm);
        loadManagedInstance(code, false);
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        code.vreturn();

        // SecurityManager sec = System.getSecurityManager ();
        // if (sec != null)
        //		sec.checkPermission (Permission.SET_STATE_MANAGER);
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        }

        // if (sm != null)
        //     return (sm.isDetached ()) ? Boolean.TRUE : Boolean.FALSE;
        loadManagedInstance(code, false);
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java

        // if we use detached state:
        // if (pcGetDetachedState () != null
        //     && pcGetDetachedState != DESERIALIZED)
        //     return Boolean.TRUE;
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        // not detached if state not set to DESERIALIZED
        if (state == null) {
            // if (pcGetDetachedState () == null) // instead of DESERIALIZED
            //     return Boolean.FALSE;
            loadManagedInstance(code, false);
```

### CommentedOutCode
Commented out code (2 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        code.astore().setLocal(inst);

        // if (clear)
        //   pc.pcClearFields ();
        code.iload().setParam((oid) ? 2 : 1);
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        code.putstatic().setField(PRE + "FieldFlags", byte[].class);

        // PCRegistry.register (cls,
        //	pcFieldNames, pcFieldTypes, pcFieldFlags,
        //  pcPCSuperclass, alias, new XXX ());
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        JumpInstruction go2 = null;
        if (detachedState) {
            // if (sm != null)
            //   if (sm.writeDetached (out))
            //      return;
```

### CommentedOutCode
Commented out code (4 lines)
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    private int createAlias(Table table, Object key) {
        Integer i = ctx().nextAlias();
//        System.out.println("\t"+
//                ((_parent != null) ? "Sub" :"") +
//                "Query created alias: "+
```

### CommentedOutCode
Commented out code (4 lines)
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        // not found; create alias
        i = aliasSize(false, null);
//        System.out.println("GetTableIndex\t"+
//                ((_parent != null) ? "Sub" :"") +
//                " created alias: "+
```

### CommentedOutCode
Commented out code (8 lines)
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
                            buf.append('\'');
                        }
//                        // StreamTokenizer can not differentiate the last quoted token as in ^.*'.*$ and ^.*',*'$
//                        // need to check the last quote ends with "'" and process accordingly.
//                        if(endsWithQuote) {
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java


//    protected Table newTable(String name, Schema schema) {
//        return new DynamicTable(name, schema);
//    }
```

### CommentedOutCode
Commented out code (3 lines)
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java
    }

//    protected Column newColumn(String name, Table table) {
//        return new DynamicColumn(name, table);
//    }
```

## RuleId[id=CloneableImplementsClone]
### CloneableImplementsClone
`PropertySource` is 'Cloneable' but does not define 'clone()' method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
    }

    static class PropertySource extends LinkedList
        implements Cloneable, Serializable {

```

### CloneableImplementsClone
`SelectJoins` is 'Cloneable' but does not define 'clone()' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
     * Joins implementation.
     */
    private static class SelectJoins
        extends PathJoinsImpl
        implements Cloneable {
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
            // We need
            if (query != null && _convertPositionalParams && JPQLParser.LANG_JPQL.equals(language)) {
                query = query.replaceAll("[\\?]", "\\:_");
            }
            String qid = query;
```

### RegExpRedundantEscape
Redundant character escape `\\ ` in RegExp
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
     */
    public static String[] wrap(String longLine, int width) {
        String[] words = longLine.split("\\ ");
        List<String> lines = new ArrayList<>();
        StringBuilder line = new StringBuilder();
```

### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/QueryMetaData.java`
#### Snippet
```java
    public void setQueryString(String query) {
        if (query != null && _convertPositionalParametersToNamed && JPQLParser.LANG_JPQL.equals(_language)) {
            query = query.replaceAll("[\\?]", "\\:_");
        }
        _query = query;
```

### RegExpRedundantEscape
Redundant character escape `\\,` in RegExp
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java
        Object value = props.get(key);
        if (value != null)
            return Arrays.asList(value.toString().split("\\,"));
        return def;
    }
```

### RegExpRedundantEscape
Redundant character escape `\\;` in RegExp
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderCacheImpl.java`
#### Snippet
```java
public class FinderCacheImpl
    implements FinderCache<ClassMapping, SelectExecutor, Result> {
    private static final String PATTERN_SEPARATOR = "\\;";
    private static final String EXLUDED_BY_USER = "Excluded by user";

```

### RegExpRedundantEscape
Redundant character escape `\\;` in RegExp
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java
 */
public class PreparedQueryCacheImpl implements PreparedQueryCache {
	private static final String PATTERN_SEPARATOR = "\\;";
	// Key: Query identifier
	private final Map<String, PreparedQuery> _delegate;
```

### RegExpRedundantEscape
Redundant character escape `\\,` in RegExp
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
        boolean explicit = p.containsKey(key);
        if (explicit) {
            String[] values = p.get(key).toString().split("\\,");
            for (String name:values)
                if (!sliceNames.contains(name.trim()))
```

## RuleId[id=Deprecation]
### Deprecation
'isAccessible()' is deprecated
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
                final Method synchronizeMappings = brokerFactory.getClass()
                        .getDeclaredMethod("synchronizeMappings", ClassLoader.class);
                if (!synchronizeMappings.isAccessible()) {
                    synchronizeMappings.setAccessible(true);
                }
```

### Deprecation
'decode(java.lang.String)' is deprecated
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java
                URL url = info.getPersistenceUnitRootUrl();
                if ("file".equals(url.getProtocol())) // exploded jar?
                    file = URLDecoder.decode(url.getPath());
                else
                    jars.append(url);
```

### Deprecation
'newInstance()' is deprecated
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        if (policy != null) {
            try {
                factory = (MetaDataFactory)Class.forName(policy).newInstance();
            } catch (Throwable e) {
                logger.warn(_loc.get("mmg-bad-naming", policy, e));
```

### Deprecation
Overrides deprecated method in 'java.lang.Object'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/ResultObjectProviderIterator.java`
#### Snippet
```java

    @Override
    protected void finalize() {
        close();
    }
```

### Deprecation
'decode(java.lang.String)' is deprecated
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/URLMetaDataIterator.java`
#### Snippet
```java
        if (_url == null)
            return null;
        File file = new File(URLDecoder.decode(_url.getPath()));
        return (AccessController.doPrivileged(
                J2DoPrivHelper.existsAction(file))) ? file:null;
```

### Deprecation
'decode(java.lang.String)' is deprecated
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassMetaDataIterator.java`
#### Snippet
```java
        if (_url == -1 || _url >= _urls.size())
            throw new IllegalStateException();
        File file = new File(URLDecoder.decode((_urls.get(_url)).getFile()));
        return (AccessController.doPrivileged(
                J2DoPrivHelper.existsAction(file))) ? file:null;
```

### Deprecation
'decode(java.lang.String)' is deprecated
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ResourceMetaDataIterator.java`
#### Snippet
```java
        if (_url == -1 || _url >= _urls.size())
            throw new IllegalStateException();
        File file = new File(URLDecoder.decode((_urls.get(_url)).getFile()));
        return (AccessController.doPrivileged(
                J2DoPrivHelper.existsAction(file))) ? file :null;
```

### Deprecation
'decode(java.lang.String)' is deprecated
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Files.java`
#### Snippet
```java
            J2DoPrivHelper.getResourceAction(cls, name + ext));
        if (rsrc != null && rsrc.getProtocol().equals("file"))
            return new File(URLDecoder.decode(rsrc.getFile()));
        return null;
    }
```

### Deprecation
'decode(java.lang.String)' is deprecated
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Files.java`
#### Snippet
```java
            String urlFile = url.getFile();
            if (urlFile != null) {
                File rsrc = new File(URLDecoder.decode(urlFile));
                if (AccessController.doPrivileged(
                        J2DoPrivHelper.existsAction(rsrc)))
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.lib.jdbc.DelegatingPreparedStatement'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
            @Override
            @Deprecated
            public void setUnicodeStream(int i1, InputStream is, int i2)
                throws SQLException {
                setLogParameter(i1, "InputStream", is);
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.lib.jdbc.DelegatingCallableStatement'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
            @Override
            @Deprecated
            public void setUnicodeStream(int i1, InputStream is, int i2)
                throws SQLException {
                setLogParameter(i1, "InputStream", is);
```

### Deprecation
'toURL()' is deprecated
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
            @Override
            public URL run() throws MalformedURLException {
                return file.toURL();
            }
        };
```

### Deprecation
'newInstance()' is deprecated
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
                    InstantiationException {
                if (!Modifier.isAbstract(clazz.getModifiers())) {
                    return clazz.newInstance();
                } else {
                    try {
```

### Deprecation
Overrides deprecated method in 'java.util.Properties'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java

    @Override
    public void save(OutputStream out, String header) {
        try {
            store(out, header);
```

### Deprecation
'setDiscriminatorColumnName(java.lang.String)' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/PersistenceMappingDefaults.java`
#### Snippet
```java
        setOrderLists(false);
        setAddNullIndicator(false);
        setDiscriminatorColumnName("DTYPE");
    }

```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.meta.MappingDefaultsImpl'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/PersistenceMappingDefaults.java`
#### Snippet
```java

    @Override
    public void populateForeignKeyColumn(ValueMapping vm, String name,
        Table local, Table foreign, Column col, Object target, boolean inverse,
        int pos, int cols) {
```

### Deprecation
'newInstance()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java
            mr = (ManagedRuntime) Class.
                forName("org.apache.openjpa.ee.RegistryManagedRuntime").
                    newInstance();
        } catch (Throwable t) {
            // might be JTA version lower than 1.1, which doesn't have
```

### Deprecation
'getTable()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        SequenceMapping seq = (SequenceMapping) meta;
        addAttribute("name", seq.getName());
        String table = seq.getTable();
        if (table != null) {
            int dotIdx = table.indexOf('.');
```

### Deprecation
'getPrimaryKeyColumn()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            }
        }
        if (!StringUtil.isEmpty(seq.getPrimaryKeyColumn()))
            addAttribute("pk-column-name", seq.getPrimaryKeyColumn());
        if (!StringUtil.isEmpty(seq.getSequenceColumn()))
```

### Deprecation
'getPrimaryKeyColumn()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        }
        if (!StringUtil.isEmpty(seq.getPrimaryKeyColumn()))
            addAttribute("pk-column-name", seq.getPrimaryKeyColumn());
        if (!StringUtil.isEmpty(seq.getSequenceColumn()))
            addAttribute("value-column-name", seq.getSequenceColumn());
```

### Deprecation
'getSequenceColumn()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        if (!StringUtil.isEmpty(seq.getPrimaryKeyColumn()))
            addAttribute("pk-column-name", seq.getPrimaryKeyColumn());
        if (!StringUtil.isEmpty(seq.getSequenceColumn()))
            addAttribute("value-column-name", seq.getSequenceColumn());
        if (!StringUtil.isEmpty(seq.getPrimaryKeyValue()))
```

### Deprecation
'getSequenceColumn()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            addAttribute("pk-column-name", seq.getPrimaryKeyColumn());
        if (!StringUtil.isEmpty(seq.getSequenceColumn()))
            addAttribute("value-column-name", seq.getSequenceColumn());
        if (!StringUtil.isEmpty(seq.getPrimaryKeyValue()))
            addAttribute("pk-column-value", seq.getPrimaryKeyValue());
```

### Deprecation
'getSecondaryTableJoinColumns(java.lang.String)' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        List<Column> cols = null;
        if (secondaryInfo != null)
            cols = (List<Column>) secondaryInfo.getSecondaryTableJoinColumns
                (table);

```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java

    private void serializeUniqueConstraint(Unique unique) throws SAXException {
        if (StringUtil.isNotEmpty(unique.getName())) {
            addAttribute("name", unique.getName());
        }
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
    private void serializeUniqueConstraint(Unique unique) throws SAXException {
        if (StringUtil.isNotEmpty(unique.getName())) {
            addAttribute("name", unique.getName());
        }
        startElement("unique-constraint");
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        for (Column column:columns) {
            startElement("column-name");
            addText(column.getName());
            endElement("column-name");
        }
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        boolean unique)
        throws SAXException {
        if (col.getName() != null)
            addAttribute("name", col.getName());
        if (col.getTypeName() != null)
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        throws SAXException {
        if (col.getName() != null)
            addAttribute("name", col.getName());
        if (col.getTypeName() != null)
            addAttribute("column-definition", col.getTypeName());
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        if (col.getName() != null)
            addAttribute("name", col.getName());
        if (col.getTypeName() != null)
            addAttribute("column-definition", col.getTypeName());
        if (col.getTarget() != null
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            addAttribute("name", col.getName());
        if (col.getTypeName() != null)
            addAttribute("column-definition", col.getTypeName());
        if (col.getTarget() != null
            && (type == ColType.JOIN || type == ColType.INVERSE
```

### Deprecation
'getTarget()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        if (col.getTypeName() != null)
            addAttribute("column-definition", col.getTypeName());
        if (col.getTarget() != null
            && (type == ColType.JOIN || type == ColType.INVERSE
            || type == ColType.PK_JOIN))
```

### Deprecation
'getTarget()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            && (type == ColType.JOIN || type == ColType.INVERSE
            || type == ColType.PK_JOIN))
            addAttribute("referenced-column-name", col.getTarget());
        if (type == ColType.COL || type == ColType.JOIN
            || type == ColType.PK_JOIN) {
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            case MANY_ONE:
                serializeColumns(field.getValueInfo(), ColType.JOIN,
                    field.getMappingInfo().getTableName());
                return;
            case ONE_MANY:
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
                    || field.getElementMapping().getValueInfo()
                    .hasSchemaComponents()) {
                    String table = field.getMappingInfo().getTableName();
                    if (table != null) {
                        int index = table.indexOf('.');
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
                    || field.getElementMapping().getValueInfo()
                    .hasSchemaComponents()) {
                    String table = field.getMappingInfo().getTableName();
                    if (table != null) {
                        int index = table.indexOf('.');
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java

        serializeColumns(field.getValueInfo(), ColType.COL,
            field.getMappingInfo().getTableName());
        if (strategy == PersistenceStrategy.BASIC && isLob(field)) {
            startElement("lob");
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        ClassMapping cls = (ClassMapping) mapping;
        ClassMappingInfo info = cls.getMappingInfo();
        serializeTable(info.getTableName(), "table", ClassUtil.getClassName(mapping.getDescribedType()), null,
            info.getUniques(info.getTableName()));
        for (String second : info.getSecondaryTableNames())
```

### Deprecation
'getUniques(java.lang.String)' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        ClassMappingInfo info = cls.getMappingInfo();
        serializeTable(info.getTableName(), "table", ClassUtil.getClassName(mapping.getDescribedType()), null,
            info.getUniques(info.getTableName()));
        for (String second : info.getSecondaryTableNames())
            serializeTable(second, "secondary-table", null, info,
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        ClassMappingInfo info = cls.getMappingInfo();
        serializeTable(info.getTableName(), "table", ClassUtil.getClassName(mapping.getDescribedType()), null,
            info.getUniques(info.getTableName()));
        for (String second : info.getSecondaryTableNames())
            serializeTable(second, "secondary-table", null, info,
```

### Deprecation
'getSecondaryTableNames()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        serializeTable(info.getTableName(), "table", ClassUtil.getClassName(mapping.getDescribedType()), null,
            info.getUniques(info.getTableName()));
        for (String second : info.getSecondaryTableNames())
            serializeTable(second, "secondary-table", null, info,
                    info.getUniques(second));
```

### Deprecation
'getUniques(java.lang.String)' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        for (String second : info.getSecondaryTableNames())
            serializeTable(second, "secondary-table", null, info,
                    info.getUniques(second));
        serializeColumns(info, ColType.PK_JOIN, null);
    }
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        FieldMapping fm = (FieldMapping) fmd;
        serializeColumns(fm.getValueInfo(), ColType.COL, fm.getMappingInfo()
            .getTableName());
    }

```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            col = cols.get(i);
            col2 = cols2.get(i);
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            col = cols.get(i);
            col2 = cols2.get(i);
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
                return true;
            if (col.getSize() != col2.getSize())
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
                return true;
            if (col.getSize() != col2.getSize())
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        Column orderCol = field.getOrderColumn();
        if (orderCol != null) {
            if (orderCol.getName() != null)
                addAttribute("name", orderCol.getName());
            if (orderCol.isNotNull())
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        if (orderCol != null) {
            if (orderCol.getName() != null)
                addAttribute("name", orderCol.getName());
            if (orderCol.isNotNull())
                addAttribute("nullable", "false");
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            if (orderCol.getFlag(Column.FLAG_UNUPDATABLE))
                addAttribute("updatable", "false");
            if (orderCol.getTypeName() != null)
                addAttribute("column-definition", orderCol.getTypeName());
            startElement("order-column");
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
                addAttribute("updatable", "false");
            if (orderCol.getTypeName() != null)
                addAttribute("column-definition", orderCol.getTypeName());
            startElement("order-column");
            endElement("order-column");
```

### Deprecation
'DETACH_FGS' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/DetachOptions.java`
#### Snippet
```java
        @Override
        public int getDetachState() {
            return DETACH_FGS;
        }
    }
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            case MANY_ONE:
                serializeColumns(field.getValueInfo(), ColType.JOIN,
                    field.getMappingInfo().getTableName(), null, fmd);
                return;
            case ONE_MANY:
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
                    AnnotationBuilder abJoinTbl =
                        addAnnotation(JoinTable.class, fmd);
                    String table = field.getMappingInfo().getTableName();
                    if (table != null) {
                        int index = table.indexOf('.');
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java

        serializeColumns(field.getValueInfo(), ColType.COL,
            field.getMappingInfo().getTableName(), null, fmd);
        if (strategy == PersistenceStrategy.BASIC && isLob(field)) {
            addAnnotation(Lob.class, fmd);
```

### Deprecation
'getTable()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        SequenceMapping seq = (SequenceMapping) meta;
        abTblGen.add("name", seq.getName());
        String table = seq.getTable();
        if (table != null) {
            int dotIdx = table.indexOf('.');
```

### Deprecation
'getPrimaryKeyColumn()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            }
        }
        if (!StringUtil.isEmpty(seq.getPrimaryKeyColumn()))
            abTblGen.add("pkColumnName", seq.getPrimaryKeyColumn());
        if (!StringUtil.isEmpty(seq.getSequenceColumn()))
```

### Deprecation
'getPrimaryKeyColumn()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        }
        if (!StringUtil.isEmpty(seq.getPrimaryKeyColumn()))
            abTblGen.add("pkColumnName", seq.getPrimaryKeyColumn());
        if (!StringUtil.isEmpty(seq.getSequenceColumn()))
            abTblGen.add("valueColumnName", seq.getSequenceColumn());
```

### Deprecation
'getSequenceColumn()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        if (!StringUtil.isEmpty(seq.getPrimaryKeyColumn()))
            abTblGen.add("pkColumnName", seq.getPrimaryKeyColumn());
        if (!StringUtil.isEmpty(seq.getSequenceColumn()))
            abTblGen.add("valueColumnName", seq.getSequenceColumn());
        if (!StringUtil.isEmpty(seq.getPrimaryKeyValue()))
```

### Deprecation
'getSequenceColumn()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            abTblGen.add("pkColumnName", seq.getPrimaryKeyColumn());
        if (!StringUtil.isEmpty(seq.getSequenceColumn()))
            abTblGen.add("valueColumnName", seq.getSequenceColumn());
        if (!StringUtil.isEmpty(seq.getPrimaryKeyValue()))
            abTblGen.add("pkColumnValue", seq.getPrimaryKeyValue());
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        ClassMappingInfo info = cls.getMappingInfo();
        AnnotationBuilder abTable = addAnnotation(Table.class, mapping);
        serializeTable(info.getTableName(), ClassUtil.getClassName(mapping.getDescribedType()), null,
            info.getUniques(info.getTableName()), abTable);
        serializeColumns(info, ColType.PK_JOIN, null, abTable, cls);
```

### Deprecation
'getUniques(java.lang.String)' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        AnnotationBuilder abTable = addAnnotation(Table.class, mapping);
        serializeTable(info.getTableName(), ClassUtil.getClassName(mapping.getDescribedType()), null,
            info.getUniques(info.getTableName()), abTable);
        serializeColumns(info, ColType.PK_JOIN, null, abTable, cls);
        for (String second : info.getSecondaryTableNames()) {
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        AnnotationBuilder abTable = addAnnotation(Table.class, mapping);
        serializeTable(info.getTableName(), ClassUtil.getClassName(mapping.getDescribedType()), null,
            info.getUniques(info.getTableName()), abTable);
        serializeColumns(info, ColType.PK_JOIN, null, abTable, cls);
        for (String second : info.getSecondaryTableNames()) {
```

### Deprecation
'getSecondaryTableNames()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            info.getUniques(info.getTableName()), abTable);
        serializeColumns(info, ColType.PK_JOIN, null, abTable, cls);
        for (String second : info.getSecondaryTableNames()) {
            AnnotationBuilder abSecTable =
                addAnnotation(SecondaryTable.class, mapping);
```

### Deprecation
'getUniques(java.lang.String)' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            AnnotationBuilder abSecTable =
                addAnnotation(SecondaryTable.class, mapping);
            serializeTable(second, null, info, info.getUniques(second),
                    abSecTable);
        }
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        AnnotationBuilder abCol = newAnnotationBuilder(
            type.getColumnAnnotationType());
        if (col.getName() != null && (null == fmd ||
            !col.getName().equalsIgnoreCase(fmd.getName())))
            abCol.add("name", col.getName());
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            type.getColumnAnnotationType());
        if (col.getName() != null && (null == fmd ||
            !col.getName().equalsIgnoreCase(fmd.getName())))
            abCol.add("name", col.getName());
        if (col.getTypeName() != null)
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        if (col.getName() != null && (null == fmd ||
            !col.getName().equalsIgnoreCase(fmd.getName())))
            abCol.add("name", col.getName());
        if (col.getTypeName() != null)
            abCol.add("columnDefinition", col.getTypeName());
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            !col.getName().equalsIgnoreCase(fmd.getName())))
            abCol.add("name", col.getName());
        if (col.getTypeName() != null)
            abCol.add("columnDefinition", col.getTypeName());
        if (col.getTarget() != null
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            abCol.add("name", col.getName());
        if (col.getTypeName() != null)
            abCol.add("columnDefinition", col.getTypeName());
        if (col.getTarget() != null
            && (type == ColType.JOIN || type == ColType.INVERSE
```

### Deprecation
'getTarget()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        if (col.getTypeName() != null)
            abCol.add("columnDefinition", col.getTypeName());
        if (col.getTarget() != null
            && (type == ColType.JOIN || type == ColType.INVERSE
            || type == ColType.PK_JOIN))
```

### Deprecation
'getTarget()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            && (type == ColType.JOIN || type == ColType.INVERSE
            || type == ColType.PK_JOIN))
            abCol.add("referencedColumnName", col.getTarget());
        if (type == ColType.COL || type == ColType.JOIN
            || type == ColType.PK_JOIN) {
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            col = cols.get(i);
            col2 = cols2.get(i);
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            col = cols.get(i);
            col2 = cols2.get(i);
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
                return true;
            if (col.getSize() != col2.getSize())
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
                return true;
            if (col.getSize() != col2.getSize())
```

### Deprecation
'getSecondaryTableJoinColumns(java.lang.String)' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        List<Column> cols = null;
        if (secondaryInfo != null)
            cols = secondaryInfo.getSecondaryTableJoinColumns
                (table);

```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            if (sb.length() > 0)
                sb.append(", ");
            sb.append(column.getName());
        }
        if (columns.length > 1)
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            sb.insert(0, "{").append("}");
        ab.add("columnNames", sb.toString());
        if (StringUtil.isNotEmpty(unique.getName())) {
            ab.add("name", unique.getName());
        }
```

### Deprecation
'getName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        ab.add("columnNames", sb.toString());
        if (StringUtil.isNotEmpty(unique.getName())) {
            ab.add("name", unique.getName());
        }
    }
```

### Deprecation
'getTableName()' is deprecated
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        FieldMapping fm = (FieldMapping) fmd;
        serializeColumns(fm.getValueInfo(), ColType.COL, fm.getMappingInfo()
            .getTableName(), ab, fmd);
    }

```

### Deprecation
'Date(java.lang.String)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
            case DATE:
                if (val instanceof String)
                    return new Date(val.toString());
                return val;
            case DOUBLE_OBJ:
```

### Deprecation
'getPackage(java.lang.String)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ClassLoaderProxyService.java`
#### Snippet
```java
                return;
            }
            if (getPackage(model.getName()) == null)
            {
                if (model.isSealed() && protectionDomain != null &&
```

### Deprecation
Overrides deprecated method in 'java.lang.Object'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java

        @Override
        protected void finalize() {
            close ();
		}
```

### Deprecation
Overrides deprecated method in 'java.lang.Object'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java

        @Override
        protected void finalize() {
            close();
		}
```

### Deprecation
'isAccessible()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/MethodLifecycleCallbacks.java`
#### Snippet
```java
    public void makeCallback(Object obj, Object arg, int eventType)
        throws Exception {
        if (!_callback.isAccessible())
            AccessController.doPrivileged(J2DoPrivHelper.setAccessibleAction(
                _callback, true));
```

### Deprecation
'isAccessible()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/BeanLifecycleCallbacks.java`
#### Snippet
```java
        throws Exception {
        Method callback = getCallbackMethod();
        if (!callback.isAccessible())
            AccessController.doPrivileged(J2DoPrivHelper.setAccessibleAction(
                callback, true));
```

### Deprecation
'getParameterTypes()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingQuery.java`
#### Snippet
```java
    public LinkedMap getParameterTypes() {
        try {
            return _query.getParameterTypes();
        } catch (RuntimeException re) {
            throw translate(re);
```

### Deprecation
'newInstance()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
            int i = 0;
            try {
                Object map = putMethod.getDeclaringClass().newInstance();
                for (i = 0; i < values.length; i++)
                    putMethod.invoke(map, aliases[i], values[i]);
```

### Deprecation
'newInstance()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
                    setters = new Method[values.length];
                }
                T bean = cls.newInstance();
                for (i = 0; i < values.length; i++) {
                    if (setters[i] == null) {
```

### Deprecation
Overrides deprecated method in 'java.lang.Object'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FinalizingBrokerImpl.java`
#### Snippet
```java
     */
    @Override
    protected void finalize()
        throws Throwable {
        super.finalize();
```

### Deprecation
'finalize()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FinalizingBrokerImpl.java`
#### Snippet
```java
    protected void finalize()
        throws Throwable {
        super.finalize();
        if (!isClosed())
            free();
```

### Deprecation
'org.apache.openjpa.kernel.exps.StringContains' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
    // maintain support for a couple of deprecated extensions
    private static final FilterListener[] _listeners = new FilterListener[]{
        new StringContains(), new WildcardMatch(),
    };

```

### Deprecation
'org.apache.openjpa.kernel.exps.WildcardMatch' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
    // maintain support for a couple of deprecated extensions
    private static final FilterListener[] _listeners = new FilterListener[]{
        new StringContains(), new WildcardMatch(),
    };

```

### Deprecation
'divide(java.math.BigDecimal, int, int)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            case OP_DIVIDE:
                int scale = Math.max(n1.scale(), n2.scale());
                return n1.divide(n2, scale, BigDecimal.ROUND_HALF_UP);
            case OP_MOD:
                throw new UserException(_loc.get("mod-bigdecimal"));
```

### Deprecation
'ROUND_HALF_UP' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            case OP_DIVIDE:
                int scale = Math.max(n1.scale(), n2.scale());
                return n1.divide(n2, scale, BigDecimal.ROUND_HALF_UP);
            case OP_MOD:
                throw new UserException(_loc.get("mod-bigdecimal"));
```

### Deprecation
'Integer(java.lang.String)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            else if (type == Integer.class && o instanceof String) {
                try {
                    return new Integer(o.toString());
                }
                catch (NumberFormatException e) {
```

### Deprecation
'getParameterTypes(org.apache.openjpa.kernel.StoreQuery)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public LinkedMap getParameterTypes(StoreQuery q) {
            return _executors[0].getParameterTypes(q);
        }

```

### Deprecation
'loadBCClass(serp.bytecode.BCClass, java.lang.ClassLoader)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ManagedClassSubclasser.java`
#### Snippet
```java
                ClassLoader loader = GeneratedClasses.getMostDerivedLoader(
                    cls, PersistenceCapable.class);
                subs.add(GeneratedClasses.loadBCClass(bc, loader));
            }
        }
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java
        contractViolations.add(loc.get
            ("subclasser-contract-violation-field", m.getMessage(),
                fmd.getFullName(), fmd.getDeclaringMetaData().getSourceFile()));
    }
}
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java

        errors.add(loc.get("subclasser-error-field", s,
            fmd.getFullName(),
            fmd.getDeclaringMetaData().getSourceFile()));
    }
```

### Deprecation
'Float(float)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
     */
    public static void set(Object target, Method setter, float value) {
        set(target, setter, new Float(value));
    }

```

### Deprecation
'Double(double)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
     */
    public static void set(Object target, Method setter, double value) {
        set(target, setter, new Double(value));
    }

```

### Deprecation
'isAccessible()' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    private static void makeAccessible(AccessibleObject ao, int mods) {
        try {
            if (!Modifier.isPublic(mods) && !ao.isAccessible())
                AccessController.doPrivileged(J2DoPrivHelper
                    .setAccessibleAction(ao, true));
```

### Deprecation
'PCEnhancer(org.apache.openjpa.conf.OpenJPAConfiguration, serp.bytecode.BCClass, org.apache.openjpa.meta.MetaDataRepository)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCClassFileTransformer.java`
#### Snippet
```java
            AccessController.doPrivileged(J2DoPrivHelper.setContextClassLoaderAction(_tmpLoader));
            try {
                PCEnhancer enhancer = new PCEnhancer(_repos.getConfiguration(),
                        new Project().loadClass(new ByteArrayInputStream(bytes),
                                _tmpLoader), _repos);
```

### Deprecation
'getParameterTypes(org.apache.openjpa.kernel.StoreQuery)' is deprecated
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryCacheStoreQuery.java`
#### Snippet
```java
        @Override
        public LinkedMap getParameterTypes(StoreQuery q) {
            return _ex.getParameterTypes(unwrap(q));
        }

```

### Deprecation
'getConfiguration()' is deprecated
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
        _request = request;
        _response = response;
        OpenJPAConfiguration conf = _emf.getConfiguration();
        _log = conf.getLog("JEST");
        _repos = conf.getMetaDataRepositoryInstance();
```

### Deprecation
'Long(java.lang.String)' is deprecated
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                // for this instance, and set the version
                _object = new ObjectData(oid, meta);
                _object.setVersion(new Long(attrs.getValue("version")));
            }
            else if ("field".equals(qName)) { // field
```

### Deprecation
'Byte(java.lang.String)' is deprecated
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.BYTE:
                case JavaTypes.BYTE_OBJ:
                    return new Byte(str);

                case JavaTypes.CHAR:
```

### Deprecation
'Double(java.lang.String)' is deprecated
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.DOUBLE:
                case JavaTypes.DOUBLE_OBJ:
                    return new Double(str);

                case JavaTypes.FLOAT:
```

### Deprecation
'Float(java.lang.String)' is deprecated
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.FLOAT:
                case JavaTypes.FLOAT_OBJ:
                    return new Float(str);

                case JavaTypes.INT:
```

### Deprecation
'Integer(java.lang.String)' is deprecated
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.INT:
                case JavaTypes.INT_OBJ:
                    return new Integer(str);

                case JavaTypes.LONG:
```

### Deprecation
'Long(java.lang.String)' is deprecated
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.LONG:
                case JavaTypes.LONG_OBJ:
                    return new Long(str);

                case JavaTypes.SHORT:
```

### Deprecation
'Short(java.lang.String)' is deprecated
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.SHORT:
                case JavaTypes.SHORT_OBJ:
                    return new Short(str);

                case JavaTypes.NUMBER:
```

### Deprecation
'Date(java.lang.String)' is deprecated
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java

                case JavaTypes.DATE:
                    return new Date(str);

                case JavaTypes.PC:
```

### Deprecation
'modelToView(int)' is deprecated
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ScrollingTextPane.java`
#### Snippet
```java
            setCaretPosition(doc.getLength());
            replaceSelection(text);
            Rectangle r = modelToView(doc.getLength());
            if (r != null) {
                scrollRectToVisible(r);
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PrimaryRow.java`
#### Snippet
```java
	    	default: buf.append("UNKNOWN");
    	}
    	buf.append(" ").append(getTable().getName()).append("]: ");
    	buf.append(_pk);
    	return buf.toString();
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractSQLServerDictionary.java`
#### Snippet
```java

    @Override
    public Column[] getColumns(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, String colName, Connection conn)
        throws SQLException {
```

### Deprecation
'getColumns(java.sql.DatabaseMetaData, java.lang.String, java.lang.String, java.lang.String, java.lang.String, java.sql.Connection)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractSQLServerDictionary.java`
#### Snippet
```java
        String schemaName, String tableName, String colName, Connection conn)
        throws SQLException {
        Column[] cols = super.getColumns(meta, catalog, schemaName, tableName,
            colName, conn);
        for (int i = 0; cols != null && i < cols.length; i++)
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java

    @Override
    public Column[] getColumns(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, String columnName, Connection conn)
        throws SQLException {
```

### Deprecation
'setYear(int)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        // nail down to Jan 1st 1970, because MariaDB uses getTime LONG  and freaks out.
        final Date date = new Date(val.getTime());
        date.setYear(70);
        date.setMonth(0);
        date.setDate(1);
```

### Deprecation
'setMonth(int)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        final Date date = new Date(val.getTime());
        date.setYear(70);
        date.setMonth(0);
        date.setDate(1);
        val = new Time(date.getTime());
```

### Deprecation
'setDate(int)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        date.setYear(70);
        date.setMonth(0);
        date.setDate(1);
        val = new Time(date.getTime());

```

### Deprecation
'Date(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractResult.java`
#### Snippet
```java
        if (val instanceof Date)
            return (Date) val;
        return new Date(val.toString());
    }

```

### Deprecation
'Date(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractResult.java`
#### Snippet
```java

        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(val.toString()));
        return cal;
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PointbaseDictionary.java`
#### Snippet
```java

    @Override
    public Column[] getColumns(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, String columnName, Connection conn)
        throws SQLException {
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/EmpressDictionary.java`
#### Snippet
```java

    @Override
    public boolean isSystemIndex(String name, Table table) {
        return name.toUpperCase(Locale.ENGLISH).startsWith("SYS_");
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    @Override
    public boolean isSystemSequence(String name, String schema,
        boolean targetSchema) {
        return isSystemSequence(DBIdentifier.newSequence(name),
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    @Override
    public Index[] getIndexInfo(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, boolean unique, boolean approx,
        Connection conn)
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    @Override
    public Column[] getColumns(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, String columnName, Connection conn)
        throws SQLException {
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    @Override
    public ForeignKey[] getImportedKeys(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, Connection conn, boolean partialKeys)
        throws SQLException {
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    @Override
    protected String getSequencesSQL(String schemaName, String sequenceName) {
        return getSequencesSQL(DBIdentifier.newSchema(schemaName), DBIdentifier.newSequence(sequenceName));
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    @Override
    public PrimaryKey[] getPrimaryKeys(DatabaseMetaData meta,
        String catalog, String schemaName, String tableName, Connection conn)
        throws SQLException {
```

### Deprecation
'isAccessible()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                    Method setFixedCharMethod = inner.getClass().getMethod("setFixedCHAR",
                                                         new Class[]{int.class, String.class});
                    if (!setFixedCharMethod.isAccessible()) {
                        setFixedCharMethod.setAccessible(true);
                    }
```

### Deprecation
'newInstance()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
                Class<? extends BooleanRepresentation> booleanRepresentationClass
                        = (Class<? extends BooleanRepresentation>) cl.loadClass(booleanRepresentationKey);
                booleanRepresentation = booleanRepresentationClass.newInstance();
            }
            catch (Exception e) {
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/FoxProDictionary.java`
#### Snippet
```java

    @Override
    public Column[] getColumns(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, String columnName, Connection conn)
        throws SQLException {
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/FoxProDictionary.java`
#### Snippet
```java

    @Override
    public PrimaryKey[] getPrimaryKeys(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, Connection conn)
        throws SQLException {
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/IngresDictionary.java`
#### Snippet
```java
     */
    @Override
    protected String getSequencesSQL(String schemaName, String sequenceName) {
        return getSequencesSQL(DBIdentifier.newSchema(schemaName), DBIdentifier.newSequence(sequenceName));
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java

    @Override
    public boolean isSystemTable(String name, String schema,
        boolean targetSchema) {
        return isSystemTable(DBIdentifier.newTable(name), DBIdentifier.newSchema(schema), targetSchema);
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java

    @Override
    protected String getSequencesSQL(String schemaName, String sequenceName) {
        return getSequencesSQL(DBIdentifier.newSchema(schemaName), DBIdentifier.newSequence(sequenceName));
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java

    @Override
    public boolean isSystemIndex(String name, Table table) {
        return isSystemIndex(DBIdentifier.newIndex(name), table);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java

    @Override
    public boolean isSystemSequence(String name, String schema,
        boolean targetSchema) {
        return isSystemSequence(DBIdentifier.newTable(name), DBIdentifier.newSchema(schema), targetSchema);
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java

    @Override
    protected String getSequencesSQL(String schemaName, String sequenceName) {
        return getSequencesSQL(DBIdentifier.newSchema(schemaName),
            DBIdentifier.newSequence(sequenceName));
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
            Sequence[] seqs = schema.getSequences();
            for (Sequence seq : seqs) {
                String dbSeqName = seq.getName();
                if (dbSeqName != null && dbSeqName.equalsIgnoreCase(seqName))
                    return true;
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java

    @Override
    public Column[] getColumns(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, String columnName, Connection conn)
        throws SQLException {
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java

    @Override
    public boolean isSystemIndex(String name, Table table) {
        return name.toUpperCase(Locale.ENGLISH).startsWith("SYSTEM_");
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java

    @Override
    protected String getSequencesSQL(String schemaName, String sequenceName) {
        return getSequencesSQL(DBIdentifier.newSchema(schemaName), DBIdentifier.newSequence(sequenceName));
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/FirebirdDictionary.java`
#### Snippet
```java
     */
    @Override
    protected String getSequencesSQL(String schemaName, String sequenceName) {
        return getSequencesSQL(DBIdentifier.newSchema(schemaName), DBIdentifier.newSequence(sequenceName));
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java

    @Override
    public boolean isSystemIndex(String name, Table table) {
        return name.toUpperCase(Locale.ENGLISH).startsWith("SYS_");
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java

    @Override
    protected String getSequencesSQL(String schemaName, String sequenceName) {
        return getSequencesSQL(DBIdentifier.newSchema(schemaName), DBIdentifier.newSequence(sequenceName));
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java

    @Override
    public Column[] getColumns(DatabaseMetaData meta, String catalog,
        String schemaName, String tableName, String columnName, Connection conn)
        throws SQLException {
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
     */
    @Override
    public void createIndexIfNecessary(Schema schema, String table,
        Column pkColumn) {
        createIndexIfNecessary(schema, DBIdentifier.newTable(table),
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.sql.DBDictionary'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java

    @Override
    protected String getSequencesSQL(String schemaName, String sequenceName) {
        return getSequencesSQL(DBIdentifier.newSchema(schemaName),
            DBIdentifier.newSequence(sequenceName));
```

### Deprecation
'getValidTableName(java.lang.String, org.apache.openjpa.jdbc.schema.Schema)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingDefaultsImpl.java`
#### Snippet
```java
            replace(IdentifierUtil.DOLLAR_CHAR, IdentifierUtil.UNDERSCORE_CHAR);
        if (!_defMissing)
            name = dict.getValidTableName(name, schema);
        return name;
    }
```

### Deprecation
'getSchemaName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java

        String[] subs;
        String name = replaceInvalidCharacters(table.getSchemaName());
        if (_useSchema && name != null) {
            if (allUpperCase(name))
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        }

        name = replaceInvalidCharacters(table.getName());
        if (allUpperCase(name))
            name = name.toLowerCase();
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
    private void mapColumn(ClassMapping cls, Column col, ForeignKey join,
        boolean outer) {
        String name = getFieldName(col.getName(), cls);
        Class type = getFieldType(col, false);
        FieldMapping field = newFieldMapping(name, type, col, null, cls);
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
    private String getRelationName(Class fieldType, boolean coll,
        ForeignKey fk, boolean inverse, ClassMapping dec) {
        if (_useFK && fk.getName() != null) {
            String name = getFieldName(fk.getName(), dec);
            if (inverse && coll)
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        ForeignKey fk, boolean inverse, ClassMapping dec) {
        if (_useFK && fk.getName() != null) {
            String name = getFieldName(fk.getName(), dec);
            if (inverse && coll)
                name = name + "Inverses";
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
            // first try "TYPE(SIZE,DECIMALS)", then "TYPE(SIZE), then "TYPE"
            String[] propNames = new String[]{
                col.getTypeName() + "(" + col.getSize() + ","
                    + col.getDecimalDigits() + ")",
                col.getTypeName() + "(" + col.getSize() + ")",
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
                col.getTypeName() + "(" + col.getSize() + ","
                    + col.getDecimalDigits() + ")",
                col.getTypeName() + "(" + col.getSize() + ")",
                col.getTypeName()
            };
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
                    + col.getDecimalDigits() + ")",
                col.getTypeName() + "(" + col.getSize() + ")",
                col.getTypeName()
            };

```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
            type = String.class;
            if (_log.isWarnEnabled())
                _log.warn(_loc.get("cant-use-char", col.getFullName()));
        }

```

### Deprecation
'SequenceMapping(java.lang.String, org.apache.openjpa.jdbc.meta.MappingRepository)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
    @Override
    protected SequenceMetaData newSequenceMetaData(String name) {
        return new SequenceMapping(name, this);
    }

```

### Deprecation
'getTableName(org.apache.openjpa.jdbc.meta.FieldMapping, org.apache.openjpa.jdbc.schema.Schema)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/FieldMappingInfo.java`
#### Snippet
```java
                // table name unless necessary
                return field.getMappingRepository().getMappingDefaults().
                    getTableName(field, schema);
            }
            @Override
```

### Deprecation
'getTableName(org.apache.openjpa.jdbc.meta.ClassMapping, org.apache.openjpa.jdbc.schema.Schema)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
                // table name unless necessary
                return cls.getMappingRepository().getMappingDefaults().
                    getTableName(cls, schema);
            }
            @Override
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
        String name = getProperty(defaultName + ".rename");
        if (name == null) {
            name = getProperty(table.getName() + ".class-name");
            if (name == null && table.getSchemaName() != null)
                name = getProperty(table.getFullName() + ".class-name");
```

### Deprecation
'getSchemaName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
        if (name == null) {
            name = getProperty(table.getName() + ".class-name");
            if (name == null && table.getSchemaName() != null)
                name = getProperty(table.getFullName() + ".class-name");
        }
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
            name = getProperty(table.getName() + ".class-name");
            if (name == null && table.getSchemaName() != null)
                name = getProperty(table.getFullName() + ".class-name");
        }

```

### Deprecation
'getTableName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
            + defaultName + ".rename");
        for (int i = 0; name == null && i < cols.length; i++) {
            name = getProperty(cols[i].getTableName() + "."
                + cols[i].getName() + "." + "field-name");
            if (name == null && cols[i].getTable().getSchemaName() != null)
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
        for (int i = 0; name == null && i < cols.length; i++) {
            name = getProperty(cols[i].getTableName() + "."
                + cols[i].getName() + "." + "field-name");
            if (name == null && cols[i].getTable().getSchemaName() != null)
                name = getProperty(cols[i].getTable().getFullName()
```

### Deprecation
'getSchemaName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
            name = getProperty(cols[i].getTableName() + "."
                + cols[i].getName() + "." + "field-name");
            if (name == null && cols[i].getTable().getSchemaName() != null)
                name = getProperty(cols[i].getTable().getFullName()
                    + "." + cols[i].getName() + "." + "field-name");
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
                + cols[i].getName() + "." + "field-name");
            if (name == null && cols[i].getTable().getSchemaName() != null)
                name = getProperty(cols[i].getTable().getFullName()
                    + "." + cols[i].getName() + "." + "field-name");
        }
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
            if (name == null && cols[i].getTable().getSchemaName() != null)
                name = getProperty(cols[i].getTable().getFullName()
                    + "." + cols[i].getName() + "." + "field-name");
        }

```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
    @Override
    public int getTableType(Table table, int defaultType) {
        String type = getProperty(table.getName() + ".table-type");
        if (type == null && table.getSchemaName() != null)
            type = getProperty(table.getFullName() + ".table-type");
```

### Deprecation
'getSchemaName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
    public int getTableType(Table table, int defaultType) {
        String type = getProperty(table.getName() + ".table-type");
        if (type == null && table.getSchemaName() != null)
            type = getProperty(table.getFullName() + ".table-type");
        if (type == null)
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
        String type = getProperty(table.getName() + ".table-type");
        if (type == null && table.getSchemaName() != null)
            type = getProperty(table.getFullName() + ".table-type");
        if (type == null)
            return defaultType;
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
        if ("subclass".equals(type))
            return ReverseMappingTool.TABLE_SUBCLASS;
        throw new IllegalArgumentException(table.getName() + ".table-type: "
            + type);
    }
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/NumberVersionStrategy.java`
#### Snippet
```java
        Map<Column,String> map = new HashMap<>(cols.length);
        for (Column col : cols) {
            map.put(col, col.getName() + " + 1");
        }
        return map;
```

### Deprecation
'mapConstraints(java.lang.String, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java
                RelationStrategies.mapRelationToUnmappedPC(val, "value", adapt);

            val.mapConstraints("value", adapt);

            return;
```

### Deprecation
'setTypeName(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationStrategies.java`
#### Snippet
```java
            col.setIdentifier(tmplate.getIdentifier());
            col.setType(tmplate.getType());
            col.setTypeName(tmplate.getTypeName());
            col.setSize(tmplate.getSize());
            col.setDecimalDigits(tmplate.getDecimalDigits());
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationStrategies.java`
#### Snippet
```java
            col.setIdentifier(tmplate.getIdentifier());
            col.setType(tmplate.getType());
            col.setTypeName(tmplate.getTypeName());
            col.setSize(tmplate.getSize());
            col.setDecimalDigits(tmplate.getDecimalDigits());
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationStrategies.java`
#### Snippet
```java
            }
            else {
                DBIdentifier sName = DBIdentifier.combine(cols[i].getIdentifier(), cols[i].getName());
                cols[i].setIdentifier(sName);
            }
```

### Deprecation
'getTypeJoin(org.apache.openjpa.jdbc.meta.ValueMapping, java.lang.String, boolean, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationHandlerMapTableFieldStrategy.java`
#### Snippet
```java
        if (key.getTypeMapping().isMapped()) {
            ValueMappingInfo vinfo = key.getValueInfo();
            ForeignKey fk = vinfo.getTypeJoin(key, "key", false, adapt);
            key.setForeignKey(fk);
            key.setColumnIO(vinfo.getColumnIO());
```

### Deprecation
'mapConstraints(java.lang.String, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationHandlerMapTableFieldStrategy.java`
#### Snippet
```java
            RelationStrategies.mapRelationToUnmappedPC(key, "key", adapt);

        key.mapConstraints("key", adapt);
        field.mapPrimaryKey(adapt);
    }
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java

        // set type name if not default
        String typeName = col.getTypeName();
        if (typeName != null || copy.getSize() != 0
            || copy.getDecimalDigits() != 0) {
```

### Deprecation
'setTypeName(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                    defName = defName.substring(0, defName.indexOf('('));
                if (!typeName.equalsIgnoreCase(defName))
                    copy.setTypeName(typeName);
            }
        }
```

### Deprecation
'setTypeName(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                tmplate.setJavaType(targets[i].getJavaType());
                tmplate.setType(targets[i].getType());
                tmplate.setTypeName(targets[i].getTypeName());
                tmplate.setSize(targets[i].getSize());
                tmplate.setDecimalDigits(targets[i].getDecimalDigits());
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                tmplate.setJavaType(targets[i].getJavaType());
                tmplate.setType(targets[i].getType());
                tmplate.setTypeName(targets[i].getTypeName());
                tmplate.setSize(targets[i].getSize());
                tmplate.setDecimalDigits(targets[i].getDecimalDigits());
```

### Deprecation
'Integer(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                try {
                    if (targetNameStr.indexOf('.') == -1)
                        target = new Integer(targetNameStr);
                    else
                        target = new Double(targetNameStr);
```

### Deprecation
'Double(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                        target = new Integer(targetNameStr);
                    else
                        target = new Double(targetNameStr);
                } catch (RuntimeException re) {
                    throw new MetaDataException(_loc.get(prefix
```

### Deprecation
'setTypeName(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
            tmplate.setJavaType(tcol.getJavaType());
            tmplate.setType(tcol.getType());
            tmplate.setTypeName(tcol.getTypeName());
            tmplate.setSize(tcol.getSize());
            tmplate.setDecimalDigits(tcol.getDecimalDigits());
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
            tmplate.setJavaType(tcol.getJavaType());
            tmplate.setType(tcol.getType());
            tmplate.setTypeName(tcol.getTypeName());
            tmplate.setSize(tcol.getSize());
            tmplate.setDecimalDigits(tcol.getDecimalDigits());
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
        boolean ttype = true;
        int otype = type;
        String typeName = tmplate.getTypeName();
        Boolean notNull = null;
        if (tmplate.isNotNullExplicit())
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                type = dict.getPreferredType(otype);
            }
            typeName = given.getTypeName();
            if (given.getSize() > 0)
                size = given.getSize();
```

### Deprecation
'setTypeName(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
        if (adapt) {
            if (typeName != null)
                col.setTypeName(typeName);
            if (size != 0)
                col.setSize(size);
```

### Deprecation
'getTypeJoin(org.apache.openjpa.jdbc.meta.ValueMapping, java.lang.String, boolean, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerRelationMapTableFieldStrategy.java`
#### Snippet
```java
            if (val.getTypeMapping().isMapped()) {
                ValueMappingInfo vinfo = val.getValueInfo();
                ForeignKey fk = vinfo.getTypeJoin(val, "value", false, adapt);
                val.setForeignKey(fk);
                val.setColumnIO(vinfo.getColumnIO());
```

### Deprecation
'mapConstraints(java.lang.String, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerRelationMapTableFieldStrategy.java`
#### Snippet
```java
                RelationStrategies.mapRelationToUnmappedPC(val, "value", adapt);

            val.mapConstraints("value", adapt);
        }
        _kio = new ColumnIO();
```

### Deprecation
'getTypeJoin(org.apache.openjpa.jdbc.meta.ValueMapping, java.lang.String, boolean, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationToManyTableFieldStrategy.java`
#### Snippet
```java
            field.mapJoin(adapt, true);
            if (elem.getTypeMapping().isMapped()) {
                ForeignKey fk = vinfo.getTypeJoin(elem, "element", false, adapt);
                elem.setForeignKey(fk);
                elem.setColumnIO(vinfo.getColumnIO());
```

### Deprecation
'mapConstraints(java.lang.String, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationToManyTableFieldStrategy.java`
#### Snippet
```java
                RelationStrategies.mapRelationToUnmappedPC(elem, "element", adapt);
            elem.setUseClassCriteria(criteria);
            elem.mapConstraints("element", adapt);

            FieldMappingInfo finfo = field.getMappingInfo();
```

### Deprecation
'getNullIndicatorColumn(org.apache.openjpa.jdbc.meta.ValueMapping, java.lang.String, org.apache.openjpa.jdbc.schema.Table, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedFieldStrategy.java`
#### Snippet
```java
        // value is mapped so we can tell whether the column is synthetic
        field.getValueMapping().resolve(MetaDataModes.MODE_META | MetaDataModes.MODE_MAPPING);
        Column col = vinfo.getNullIndicatorColumn(field, field.getName(),
            field.getTable(), adapt);
        if (col != null) {
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ClassNameDiscriminatorStrategy.java`
#### Snippet
```java
        Log log = disc.getMappingRepository().getLog();
        if (log.isTraceEnabled())
            log.trace(_loc.get("load-subs", col.getTable().getFullName()));

        ClassLoader loader = getClassLoader(store);
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/FlatClassStrategy.java`
#### Snippet
```java
            if (table != sup.getTable())
                throw new MetaDataException(_loc.get("flat-table", cls,
                    table.getFullName(), sup.getTable().getFullName()));
        }

```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/FlatClassStrategy.java`
#### Snippet
```java
            if (table != sup.getTable())
                throw new MetaDataException(_loc.get("flat-table", cls,
                    table.getFullName(), sup.getTable().getFullName()));
        }

```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MultiColumnVersionStrategy.java`
#### Snippet
```java
    	if (_javaTypes[i] == null) {
    		Column col = (Column)vers.getMappingInfo().getColumns().get(i);
    		if (!StringUtil.isEmpty(col.getTypeName())) {
    			Class javaType = Schemas.getJavaType(col.getType(),
    					col.getSize(), col.getDecimalDigits());
```

### Deprecation
'getInverseTypeJoin(org.apache.openjpa.jdbc.meta.ValueMapping, java.lang.String, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationToManyInverseKeyFieldStrategy.java`
#### Snippet
```java

        // map inverse foreign key in related table
        ForeignKey fk = vinfo.getInverseTypeJoin(elem, field.getName(), adapt);
        if (_uni1MFK) {
            Column[] locals = fk.getColumns();
```

### Deprecation
'mapConstraints(java.lang.String, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationToManyInverseKeyFieldStrategy.java`
#### Snippet
```java
        elem.setJoinDirection(ValueMapping.JOIN_EXPECTED_INVERSE);
        elem.setUseClassCriteria(criteria);
        elem.mapConstraints(field.getName(), adapt);

        field.setOrderColumn(finfo.getOrderColumn(field, fk.getTable(),
```

### Deprecation
'getTypeJoin(org.apache.openjpa.jdbc.meta.ValueMapping, java.lang.String, boolean, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java

            if (!field.isBiMTo1JT()) {
                ForeignKey fk = vinfo.getTypeJoin(field, field.getName(), true,
                    adapt);
                field.setForeignKey(fk);
```

### Deprecation
'mapConstraints(java.lang.String, boolean)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java

        // map constraints after pk so we don't re-index / re-unique pk col
        field.mapConstraints(field.getName(), adapt);
    }

```

### Deprecation
'org.apache.openjpa.kernel.exps.StringContains' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
    static {
        // deprecated extensions
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
```

### Deprecation
'org.apache.openjpa.jdbc.kernel.exps.JDBCStringContains' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
    static {
        // deprecated extensions
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
```

### Deprecation
'org.apache.openjpa.kernel.exps.WildcardMatch' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        // deprecated extensions
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());
```

### Deprecation
'org.apache.openjpa.jdbc.kernel.exps.JDBCWildcardMatch' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        // deprecated extensions
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());
```

### Deprecation
'org.apache.openjpa.jdbc.kernel.exps.SQLExpression' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());

```

### Deprecation
'org.apache.openjpa.jdbc.kernel.exps.SQLExpression' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());

```

### Deprecation
'org.apache.openjpa.jdbc.kernel.exps.SQLValue' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());

        // jdbc-specific extensions
```

### Deprecation
'org.apache.openjpa.jdbc.kernel.exps.SQLValue' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());

        // jdbc-specific extensions
```

### Deprecation
Overrides deprecated method in 'java.lang.Object'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java

        @Override
        protected void finalize() throws SQLException {
            if (!_closed)
                close();
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.schema.SchemaGroup'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/LazySchemaFactory.java`
#### Snippet
```java
    @Deprecated
    @Override
    public Table findTable(String name) {
        if (name == null)
            return null;
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.schema.SchemaGroup'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/LazySchemaFactory.java`
#### Snippet
```java
    @Deprecated
    @Override
    public Sequence findSequence(String name) {
        if (name == null)
            return null;
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.schema.SchemaGroup'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java

    @Override
    public Table findTable(String name) {
        return super.findTable(name);
    }
```

### Deprecation
'findTable(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java
    @Override
    public Table findTable(String name) {
        return super.findTable(name);
    }

```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.schema.Table'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java
        @Deprecated
        @Override
        public Column getColumn(String name) {
            return getColumn(name, null);
        }
```

### Deprecation
'Table(java.lang.String, org.apache.openjpa.jdbc.schema.Schema)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java

        public DynamicTable(String name, Schema schema) {
            super(name, schema);
        }

```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.schema.SchemaGroup'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java

    @Override
    public boolean isKnownTable(String name) {
        return super.findTable(name) != null;
    }
```

### Deprecation
'findTable(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java
    @Override
    public boolean isKnownTable(String name) {
        return super.findTable(name) != null;
    }

```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeTable(Table table)
        throws SAXException {
        addAttribute("name", table.getName());
        startElement("table");

```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeSequence(Sequence seq)
        throws SAXException {
        addAttribute("name", seq.getName());
        if (seq.getInitialValue() != 1)
            addAttribute("initial-value",
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeJoin(Column col, Column pk)
        throws SAXException {
        addAttribute("column", col.getName());
        addAttribute("to-column", pk.getName());
        startElement("join");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        throws SAXException {
        addAttribute("column", col.getName());
        addAttribute("to-column", pk.getName());
        startElement("join");
        endElement("join");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeColumn(Column col)
        throws SAXException {
        addAttribute("name", col.getName());
        addAttribute("type", Schemas.getJDBCName(col.getType()));
        if (!StringUtil.isEmpty(col.getTypeName())
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        addAttribute("name", col.getName());
        addAttribute("type", Schemas.getJDBCName(col.getType()));
        if (!StringUtil.isEmpty(col.getTypeName())
            && !col.getTypeName().equalsIgnoreCase
            (Schemas.getJDBCName(col.getType())))
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        addAttribute("type", Schemas.getJDBCName(col.getType()));
        if (!StringUtil.isEmpty(col.getTypeName())
            && !col.getTypeName().equalsIgnoreCase
            (Schemas.getJDBCName(col.getType())))
            addAttribute("type-name", col.getTypeName());
```

### Deprecation
'getTypeName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
            && !col.getTypeName().equalsIgnoreCase
            (Schemas.getJDBCName(col.getType())))
            addAttribute("type-name", col.getTypeName());
        if (col.isNotNull())
            addAttribute("not-null", "true");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        throws SAXException {
        addAttribute("value", stringifyConstant(val));
        addAttribute("to-column", pk.getName());
        startElement("join");
        endElement("join");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeUnique(Unique unq)
        throws SAXException {
        if (unq.getName() != null)
            addAttribute("name", unq.getName());
        if (unq.isDeferred())
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        throws SAXException {
        if (unq.getName() != null)
            addAttribute("name", unq.getName());
        if (unq.isDeferred())
            addAttribute("deferred", "true");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        Column[] cols = unq.getColumns();
        if (cols.length == 1)
            addAttribute("column", cols[0].getName());
        startElement("unique");

```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeJoin(Column col, Object val)
        throws SAXException {
        addAttribute("column", col.getName());
        addAttribute("value", stringifyConstant(val));
        startElement("join");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeOn(Column col)
        throws SAXException {
        addAttribute("column", col.getName());
        startElement("on");
        endElement("on");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializePrimaryKey(PrimaryKey pk)
        throws SAXException {
        if (pk.getName() != null)
            addAttribute("name", pk.getName());
        if (pk.isLogical())
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        throws SAXException {
        if (pk.getName() != null)
            addAttribute("name", pk.getName());
        if (pk.isLogical())
            addAttribute("logical", "true");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        Column[] cols = pk.getColumns();
        if (cols.length == 1)
            addAttribute("column", cols[0].getName());
        startElement("pk");

```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeForeignKey(ForeignKey fk)
        throws SAXException {
        if (fk.getName() != null)
            addAttribute("name", fk.getName());

```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        throws SAXException {
        if (fk.getName() != null)
            addAttribute("name", fk.getName());

        if (fk.isDeferred())
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        Column[] consts = fk.getConstantColumns();
        Column[] constsPK = fk.getConstantPrimaryKeyColumns();
        addAttribute("to-table", fk.getPrimaryKeyTable().getFullName());
        if (cols.length == 1 && consts.length == 0 && constsPK.length == 0)
            addAttribute("column", cols[0].getName());
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        addAttribute("to-table", fk.getPrimaryKeyTable().getFullName());
        if (cols.length == 1 && consts.length == 0 && constsPK.length == 0)
            addAttribute("column", cols[0].getName());
        startElement("fk");

```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    private void serializeIndex(Index idx)
        throws SAXException {
        addAttribute("name", idx.getName());
        if (idx.isUnique())
            addAttribute("unique", "true");
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
        Column[] cols = idx.getColumns();
        if (cols.length == 1)
            addAttribute("column", cols[0].getName());
        startElement("index");

```

### Deprecation
'getSchemaName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
            obj = value;
            if (obj instanceof Table)
                schemaName = ((Table) obj).getSchemaName();
            else
                schemaName = ((Sequence) obj).getSchemaName();
```

### Deprecation
'addPrimaryKey(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
    private void startPrimaryKey(Attributes attrs) {
        _pk = new PrimaryKeyInfo();
        _pk.pk = _table.addPrimaryKey(attrs.getValue("name"));
        _pk.pk.setLogical("true".equals(attrs.getValue("logical")));

```

### Deprecation
'getColumn(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            for (String s : indexInfo.cols) {
                colName = s;
                col = indexInfo.index.getTable().getColumn(colName);
                if (col == null)
                    throwUserException(_loc.get("index-resolve", new Object[]
```

### Deprecation
'getColumn(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            for (String s : pkInfo.cols) {
                colName = s;
                col = pkInfo.pk.getTable().getColumn(colName);
                if (col == null)
                    throwUserException(_loc.get("pk-resolve", new Object[]
```

### Deprecation
'findTable(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
        for (ForeignKeyInfo info : _fkInfos) {
            fkInfo = info;
            toTable = _group.findTable(fkInfo.toTable);
            if (toTable == null || toTable.getPrimaryKey() == null)
                throwUserException(_loc.get("fk-totable", new Object[]
```

### Deprecation
'getName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            pk = toTable.getPrimaryKey();
            if (fkInfo.cols.size() == 1 && fkInfo.pks.size() == 0)
                fkInfo.pks.add(pk.getColumns()[0].getName());

            // make joins
```

### Deprecation
'getColumn(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            for (cols = fkInfo.cols.iterator(); cols.hasNext(); ) {
                colName = (String) cols.next();
                col = fkInfo.fk.getTable().getColumn(colName);
                if (col == null)
                    throwUserException(_loc.get("fk-nocol",
```

### Deprecation
'getColumn(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java

                pkColName = (String) pks.next();
                pkCol = toTable.getColumn(pkColName);
                if (pkCol == null)
                    throwUserException(_loc.get("fk-nopkcol", new Object[]
```

### Deprecation
'getColumn(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            for (Object value : fkInfo.consts) {
                colName = cols.next();
                col = fkInfo.fk.getTable().getColumn(colName);
                if (col == null)
                    throwUserException(_loc.get("fk-nocol",
```

### Deprecation
'getColumn(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            for (Object o : fkInfo.constsPK) {
                pkColName = pks.next();
                pkCol = toTable.getColumn(pkColName);
                if (pkCol == null)
                    throwUserException(_loc.get("fk-nopkcol", new Object[]
```

### Deprecation
'addForeignKey(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
    private void startForeignKey(Attributes attrs) {
        _fk = new ForeignKeyInfo();
        _fk.fk = _table.addForeignKey(attrs.getValue("name"));

        if ("true".equals(attrs.getValue("deferred")))
```

### Deprecation
'addUnique(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
    private void startUnique(Attributes attrs) {
        _unq = new UniqueInfo();
        _unq.unq = _table.addUnique(attrs.getValue("name"));
        _unq.unq.setDeferred("true".equals(attrs.getValue("deferred")));

```

### Deprecation
'addTable(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java

    private void startTable(Attributes attrs) {
        _table = _schema.addTable(attrs.getValue("name"));
        _table.setSource(getSourceFile(), SourceTracker.SRC_XML);
        Locator locator = getLocation().getLocator();
```

### Deprecation
'Long(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            return val.substring(1, val.length() - 1);
        if (val.indexOf('.') == -1)
            return new Long(val);
        return new Double(val);
    }
```

### Deprecation
'Double(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
        if (val.indexOf('.') == -1)
            return new Long(val);
        return new Double(val);
    }

```

### Deprecation
'addColumn(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java

    private void startColumn(Attributes attrs) {
        Column col = _table.addColumn(attrs.getValue("name"));
        col.setType(_dict.getPreferredType(Schemas.getJDBCType
            (attrs.getValue("type"))));
```

### Deprecation
'setTypeName(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
        col.setType(_dict.getPreferredType(Schemas.getJDBCType
            (attrs.getValue("type"))));
        col.setTypeName(attrs.getValue("type-name"));
        String val = attrs.getValue("size");
        if (val != null)
```

### Deprecation
'addSequence(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java

    private void startSequence(Attributes attrs) {
        Sequence seq = _schema.addSequence(attrs.getValue("name"));
        Locator locator = getLocation().getLocator();
        if (locator != null) {
```

### Deprecation
'getFullName()' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
                seq.setAllocate(Integer.parseInt(val));
        } catch (NumberFormatException nfe) {
            throwUserException(_loc.get("bad-seq-num", seq.getFullName()));
        }
    }
```

### Deprecation
'getColumn(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            for (String s : unqInfo.cols) {
                colName = s;
                col = unqInfo.unq.getTable().getColumn(colName);
                if (col == null)
                    throwUserException(_loc.get("unq-resolve", new Object[]
```

### Deprecation
'addIndex(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
    private void startIndex(Attributes attrs) {
        _index = new IndexInfo();
        _index.index = _table.addIndex(attrs.getValue("name"));
        _index.index.setUnique("true".equals(attrs.getValue("unique")));

```

### Deprecation
'getSchema(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java

        String name = attrs.getValue("name");
        _schema = group.getSchema(name);
        if (_schema == null)
            _schema = group.addSchema(name);
```

### Deprecation
'addSchema(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
        _schema = group.getSchema(name);
        if (_schema == null)
            _schema = group.addSchema(name);
    }

```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.schema.Constraint'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Index.java`
#### Snippet
```java
    @Deprecated
    @Override
    public String getFullName() {
        return getFullIdentifier().getName();
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.openjpa.jdbc.schema.Constraint'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Unique.java`
#### Snippet
```java
    @Deprecated
    @Override
    public void setName(String name) {
        super.setName(name);
    }
```

### Deprecation
'setUnicodeStream(int, java.io.InputStream, int)' is deprecated
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedPreparedStatement.java`
#### Snippet
```java
	     throws SQLException {
			for (PreparedStatement t : this)
				t.setUnicodeStream(arg0, arg1, arg2);
	 }

```

### Deprecation
'getUnicodeStream(java.lang.String)' is deprecated
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedResultSet.java`
#### Snippet
```java
	@Override
    public InputStream getUnicodeStream(String arg0) throws SQLException {
		return current.getUnicodeStream(arg0);
	}

```

### Deprecation
'getBigDecimal(int, int)' is deprecated
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedResultSet.java`
#### Snippet
```java
    @Override
    public BigDecimal getBigDecimal(int arg0, int arg1) throws SQLException {
		return current.getBigDecimal(arg0, arg1);
	}

```

### Deprecation
'getBigDecimal(java.lang.String, int)' is deprecated
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedResultSet.java`
#### Snippet
```java
    @Override
    public BigDecimal getBigDecimal(String arg0, int arg1) throws SQLException {
		return current.getBigDecimal(arg0, arg1);
	}

```

### Deprecation
'getUnicodeStream(int)' is deprecated
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedResultSet.java`
#### Snippet
```java
	@Override
    public InputStream getUnicodeStream(int arg0) throws SQLException {
		return current.getUnicodeStream(arg0);
	}

```

### Deprecation
'Byte(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
            case JavaTypes.BYTE:
            case JavaTypes.BYTE_OBJ:
                _default = new Byte(_defaultStr);
                break;
            case JavaTypes.CHAR:
```

### Deprecation
'Double(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
            case JavaTypes.DOUBLE:
            case JavaTypes.DOUBLE_OBJ:
                _default = new Double(_defaultStr);
                break;
            case JavaTypes.FLOAT:
```

### Deprecation
'Float(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
            case JavaTypes.FLOAT:
            case JavaTypes.FLOAT_OBJ:
                _default = new Float(_defaultStr);
                break;
            case JavaTypes.INT:
```

### Deprecation
'Short(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
            case JavaTypes.SHORT:
            case JavaTypes.SHORT_OBJ:
                _default = new Short(_defaultStr);
                break;
            case JavaTypes.DATE:
```

### Deprecation
'Date(java.lang.String)' is deprecated
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
                break;
            case JavaTypes.DATE:
                _default = new java.util.Date(_defaultStr);
                break;
            case JavaTypes.BIGINTEGER:
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/DetachStateType.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/RestoreStateType.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ConnectionRetainMode.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AutoClearType.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryOperationType.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleImpl.java`
#### Snippet
```java
        }
        Object val = values[i];
        return assertAndConvertType(""+i, val, type);
    }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleImpl.java`
#### Snippet
```java
    public <X> X get(TupleElement<X> tupleElement) {
        int i = factory.getIndex(tupleElement);
        return assertAndConvertType(""+i, values[i], tupleElement.getJavaType());
    }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/JoinSyntax.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ResultSetType.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/FetchDirection.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/FetchMode.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/LRSSizeAlgorithm.java`
#### Snippet
```java

            default:
                throw new IllegalArgumentException(kernelConstant + "");
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            addAttribute("pk-column-value", seq.getPrimaryKeyValue());
        if (seq.getAllocate() != 50 && seq.getAllocate() != -1)
            addAttribute("allocation-size", seq.getAllocate() + "");
        if (seq.getInitialValue() != 0 && seq.getInitialValue() != -1)
            addAttribute("initial-value", seq.getInitialValue() + "");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            addAttribute("allocation-size", seq.getAllocate() + "");
        if (seq.getInitialValue() != 0 && seq.getInitialValue() != -1)
            addAttribute("initial-value", seq.getInitialValue() + "");
        startElement("table-generator");
        endElement("table-generator");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            if (type == ColType.COL) {
                if (col.getSize() > 0 && col.getSize() != 255)
                    addAttribute("length", col.getSize() + "");
                if (col.getDecimalDigits() != 0)
                    addAttribute("scale", col.getDecimalDigits() + "");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
                    addAttribute("length", col.getSize() + "");
                if (col.getDecimalDigits() != 0)
                    addAttribute("scale", col.getDecimalDigits() + "");
            }
        }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            abTblGen.add("pkColumnValue", seq.getPrimaryKeyValue());
        if (seq.getAllocate() != 50 && seq.getAllocate() != -1)
            abTblGen.add("allocationSize", seq.getAllocate() + "");
        if (seq.getInitialValue() != 0 && seq.getInitialValue() != -1)
            abTblGen.add("initialValue", seq.getInitialValue() + "");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            abTblGen.add("allocationSize", seq.getAllocate() + "");
        if (seq.getInitialValue() != 0 && seq.getInitialValue() != -1)
            abTblGen.add("initialValue", seq.getInitialValue() + "");
    }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/MetaDataCacheMaintenance.java`
#### Snippet
```java

        log.info("  Compiled queries: "
            + (qcc == null ? "0" : "" + qcc.size()));
        if (log.isTraceEnabled() && qcc != null)
            for (Object o : qcc.keySet()) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
                    Socket s = AccessController.doPrivileged(J2DoPrivHelper.newSocketAction(_address, _port));
                    if (log.isTraceEnabled()) {
                        log.trace(s_loc.get("tcp-open-connection", _address + ":" + _port, "" + s.getLocalPort()));
                    }
                    return s;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
                _isRunning = false;
                if (_log.isTraceEnabled()) {
                    _log.trace(s_loc.get("tcp-close-listener", _port + ""));
                }
            }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
                try (Socket s = p.getObject()) {
                    if (log.isTraceEnabled()) {
                        log.trace(s_loc.get("tcp-close-sending-socket", _address + ":" + _port, "" + s.getLocalPort()));
                    }
                } catch (Exception e) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLParser.java`
#### Snippet
```java
    public void populate(Object parsed, ExpressionStoreQuery query) {
        if (!(parsed instanceof JPQLExpressionBuilder.ParsedJPQL))
            throw new ClassCastException(parsed == null ? null + ""
                : parsed.getClass().getName());

```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

        if (ctx().parsed == null)
            throw new InternalException(parsedQuery + "");
    }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ClearableScheduler.java`
#### Snippet
```java
    public void run() {
        if (_log.isTraceEnabled())
            _log.trace(_loc.get("scheduler-interval", _interval + ""));

        Date lastRun = new Date();
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
                                    : _request.getServletPath(),
                "${server.name}",     _request.getServerName(),
                "${server.port}",     ""+_request.getServerPort(),

                "${dojo.base}",     Constants.DOJO_BASE_URL,
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/src/main/java/openbook/tools/ant/Java2HTMLConverterTask.java`
#### Snippet
```java
        args.add("-d");                args.add(destDir);
        args.add("-extension");        args.add(extension);
        args.add("-verbose");          args.add(""+verbose);
        args.add("-stylesheet");       args.add(stylesheet);
        args.add("-showLineNumber");   args.add(""+showLineNumber);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/src/main/java/openbook/tools/ant/Java2HTMLConverterTask.java`
#### Snippet
```java
        args.add("-verbose");          args.add(""+verbose);
        args.add("-stylesheet");       args.add(stylesheet);
        args.add("-showLineNumber");   args.add(""+showLineNumber);
        args.add("-anchorLineNumber"); args.add(""+anchorLineNumber);
        args.add("-addLineBreak");     args.add(""+addLineBreak);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/src/main/java/openbook/tools/ant/Java2HTMLConverterTask.java`
#### Snippet
```java
        args.add("-stylesheet");       args.add(stylesheet);
        args.add("-showLineNumber");   args.add(""+showLineNumber);
        args.add("-anchorLineNumber"); args.add(""+anchorLineNumber);
        args.add("-addLineBreak");     args.add(""+addLineBreak);
        args.add("-addExplicitSpace"); args.add(""+addExplicitSpace);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/src/main/java/openbook/tools/ant/Java2HTMLConverterTask.java`
#### Snippet
```java
        args.add("-showLineNumber");   args.add(""+showLineNumber);
        args.add("-anchorLineNumber"); args.add(""+anchorLineNumber);
        args.add("-addLineBreak");     args.add(""+addLineBreak);
        args.add("-addExplicitSpace"); args.add(""+addExplicitSpace);
        args.add("-lineNumberFormat"); args.add(lineNumberFormat);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/src/main/java/openbook/tools/ant/Java2HTMLConverterTask.java`
#### Snippet
```java
        args.add("-anchorLineNumber"); args.add(""+anchorLineNumber);
        args.add("-addLineBreak");     args.add(""+addLineBreak);
        args.add("-addExplicitSpace"); args.add(""+addExplicitSpace);
        args.add("-lineNumberFormat"); args.add(lineNumberFormat);
        args.add("-anchorLineNumber"); args.add(""+anchorLineNumber);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/src/main/java/openbook/tools/ant/Java2HTMLConverterTask.java`
#### Snippet
```java
        args.add("-addExplicitSpace"); args.add(""+addExplicitSpace);
        args.add("-lineNumberFormat"); args.add(lineNumberFormat);
        args.add("-anchorLineNumber"); args.add(""+anchorLineNumber);

        for (Arg a : _args) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/src/main/java/openbook/client/DeliveryPage.java`
#### Snippet
```java
                            List<PurchaseOrder> updated = get(10, TimeUnit.SECONDS);
                            _orders.getDataModel().updateData(updated);
                            _title.setText(updated.size() + "" + " Updated PurchaseOrder");
                        } catch (Exception e) {
                            new ErrorDialog(e).setVisible(true);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/FirebirdDictionary.java`
#### Snippet
```java
    @Override
    protected int matchErrorState(Map<Integer,Set<String>> errorStates, SQLException ex) {
        String errorState = ""+ex.getErrorCode();
        for (Map.Entry<Integer,Set<String>> states : errorStates.entrySet()) {
            if (states.getValue().contains(errorState))
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
                e = ((PrivilegedActionException) e).getException();
            throw new MetaDataException(_loc.get("bad-version-strategy",
                version.getClassMapping(), strat + ""), e);
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MultiColumnVersionStrategy.java`
#### Snippet
```java
    Number nextValue(Object number, int javaTypeCode) {
    	Number result = (number == null) ? 1 : ((Number)number).intValue() + 1;
    	return	(Number)JavaTypes.convert(""+result, javaTypeCode);
    }
}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
            if (_log.isTraceEnabled())
                _log.trace(_loc.get("gen-fk", new Object[]{ name, table,
                    fkColName, pkTableName, pkColName, seq + "" }));

            pkTable = group.findTable(QualifiedDBIdentifier.newPath(pkSchemaName, pkTableName));
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java

        if (len >= max) {
            _log.warn(_loc.get("too-many-indexes", idx, table, max + ""));
            return false;
        }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/web/orders.jsp`
#### Snippet
```java
<table>
  <caption><%= customer.getName() %>, you have placed <%= orders.size() %> (
  <%= pendingOrders.size() == 0 ? "none" : "" + pendingOrders.size()%> pending,
  <%= deliveredOrders.size() == 0 ? " none" : " " + deliveredOrders.size()%> delivered) orders
  </caption>
```

### TrivialStringConcatenation
Empty string used in concatenation
in `openjpa-examples/openbooks/web/orders.jsp`
#### Snippet
```java
  int i = 0;
  for (PurchaseOrder order : orders) {
      session.setAttribute(""+order.getId(), order);
%>
   <TR class="<%= i++%2 == 0 ? ROW_STYLE_EVEN : ROW_STYLE_ODD %>">
```

## RuleId[id=RedundantTypeArguments]
### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/IteratorChain.java`
#### Snippet
```java
        if (currentIterator == null) {
            if (iteratorChain.isEmpty()) {
                currentIterator = EmptyIterator.<E> emptyIterator();
            } else {
                currentIterator = iteratorChain.remove();
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractLinkedMap.java`
#### Snippet
```java
    protected Iterator<Entry<K, V>> createEntrySetIterator() {
        if (size() == 0) {
            return EmptyOrderedIterator.<Entry<K, V>>emptyOrderedIterator();
        }
        return new EntrySetIterator<>(this);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractLinkedMap.java`
#### Snippet
```java
    protected Iterator<V> createValuesIterator() {
        if (size() == 0) {
            return EmptyOrderedIterator.<V>emptyOrderedIterator();
        }
        return new ValuesIterator<>(this);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractLinkedMap.java`
#### Snippet
```java
    protected Iterator<K> createKeySetIterator() {
        if (size() == 0) {
            return EmptyOrderedIterator.<K>emptyOrderedIterator();
        }
        return new KeySetIterator<>(this);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractLinkedMap.java`
#### Snippet
```java
    public OrderedMapIterator<K, V> mapIterator() {
        if (size == 0) {
            return EmptyOrderedMapIterator.<K, V>emptyOrderedMapIterator();
        }
        return new LinkMapIterator<>(this);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/TreeBidiMap.java`
#### Snippet
```java
        public OrderedMapIterator<V, K> mapIterator() {
            if (isEmpty()) {
                return EmptyOrderedMapIterator.<V, K>emptyOrderedMapIterator();
            }
            return new InverseViewMapIterator(DataElement.VALUE);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/TreeBidiMap.java`
#### Snippet
```java
    public OrderedMapIterator<K, V> mapIterator() {
        if (isEmpty()) {
            return EmptyOrderedMapIterator.<K, V>emptyOrderedMapIterator();
        }
        return new ViewMapIterator(DataElement.KEY);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
    public MapIterator<K, V> mapIterator() {
        if (size == 0) {
            return EmptyMapIterator.<K, V>emptyMapIterator();
        }
        return new HashMapIterator<>(this);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
    protected Iterator<K> createKeySetIterator() {
        if (size() == 0) {
            return EmptyIterator.<K>emptyIterator();
        }
        return new KeySetIterator<>(this);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
    protected Iterator<Entry<K, V>> createEntrySetIterator() {
        if (size() == 0) {
            return EmptyIterator.<Entry<K, V>>emptyIterator();
        }
        return new EntrySetIterator<>(this);
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
    protected Iterator<V> createValuesIterator() {
        if (size() == 0) {
            return EmptyIterator.<V>emptyIterator();
        }
        return new ValuesIterator<>(this);
```

## RuleId[id=NullableProblems]
### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java

        @Override
        public int compareTo(ClassSeqs other) {
            if (other == this)
                return 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java

        @Override
        public int compareTo(ClassQueries other) {
            if (other == this)
                return 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java

        @Override
        public int compareTo(ClassSeqs other) {
            if (other == this)
                return 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java

        @Override
        public int compareTo(ClassQueries other) {
            if (other == this)
                return 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractNonSequentialResultList.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] a) {
        assertOpen();
        ArrayList list = new ArrayList();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractResultList.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> c) {
        throw readOnly();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractResultList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends E> c) {
        throw readOnly();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractResultList.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> c) {
        throw readOnly();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractResultList.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends E> c) {
        throw readOnly();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/ListResultList.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] a) {
        assertOpen();
        return _list.toArray(a);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/ListResultList.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection c) {
        assertOpen();
        return _list.containsAll(c);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractSequentialResultList.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] a) {
        assertOpen();
        ArrayList list = new ArrayList();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/DocTypeReader.java`
#### Snippet
```java

    @Override
    public int read(char[] buf) throws IOException {
        return read(buf, 0, buf.length);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/DocTypeReader.java`
#### Snippet
```java

    @Override
    public int read(char[] buf, int off, int len) throws IOException {
        int headerRead = readHeader(buf, off, len);
        off += headerRead;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ZipStreamMetaDataIterator.java`
#### Snippet
```java

        @Override
        public int read(byte[] b, int off, int len) throws IOException {
            return _stream.read(b, off, len);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection coll) {
        return _set.addAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection coll) {
        return _set.removeAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] arr) {
        return _set.toArray(arr);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection coll) {
        return _set.containsAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection coll) {
        return _set.retainAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/OrderedMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> m) {
        _del.putAll(m);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashSet.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> coll) {
        return _set.removeAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashSet.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> coll) {
        return _set.retainAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashSet.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends E> coll) {
        return _set.addAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashSet.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] arr) {
        return _set.toArray(arr);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashSet.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> coll) {
        return _set.containsAll(coll);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableOrderedMap.java`
#### Snippet
```java

    @Override
    public Set<Entry<K, V>> entrySet() {
        final Set<Entry<K, V>> set = super.entrySet();
        return UnmodifiableEntrySet.unmodifiableEntrySet(set);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableEntrySet.java`
#### Snippet
```java
    //-----------------------------------------------------------------------
    @Override
    public Iterator<Map.Entry<K, V>> iterator() {
        return new UnmodifiableEntrySetIterator(decorated().iterator());
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableEntrySet.java`
#### Snippet
```java

    @Override
    public boolean retainAll(final Collection<?> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableEntrySet.java`
#### Snippet
```java

    @Override
    public boolean addAll(final Collection<? extends Map.Entry<K, V>> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableEntrySet.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public <T> T[] toArray(final T[] array) {
        Object[] result = array;
        if (array.length > 0) {
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableEntrySet.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public Object[] toArray() {
        final Object[] array = decorated().toArray();
        for (int i = 0; i < array.length; i++) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableEntrySet.java`
#### Snippet
```java

    @Override
    public boolean removeAll(final Collection<?> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/IterableMap.java`
#### Snippet
```java
     * @see Map#putAll(Map)
     */
    void putAll(Map<? extends K, ? extends V> t);

    /**
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java

        @Override
        public int read(byte[] b, int off, int len) throws IOException {
            int n = super.read(b, off, len);
            if (n > 0)
```

### NullableProblems
Non-annotated parameter 'object' in method 'toArray' from 'AbstractCollectionDecorator' should not override non-null parameter from 'Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractSetDecorator.java`
#### Snippet
```java
 */
public abstract class AbstractSetDecorator<E> extends AbstractCollectionDecorator<E> implements
        Set<E> {

    /** Serialization version */
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java

        @Override
        public Iterator iterator() {
            return new HashIterator(KEYS, table.length - 1);
        }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java

        @Override
        public Iterator iterator() {
            return new HashIterator(VALUES, table.length - 1);
        }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java

        @Override
        public Iterator iterator() {
            return new HashIterator(ENTRIES, table.length - 1);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java

    @Override
    public boolean containsAll(final Collection<?> coll) {
        return map.keySet().containsAll(coll);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(final T[] array) {
        return map.keySet().toArray(array);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(final T[] array) {
        return map.keySet().toArray(array);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java

    @Override
    public Object[] toArray() {
        return map.keySet().toArray();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java

    @Override
    public boolean retainAll(final Collection<?> coll) {
        return map.keySet().retainAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java

    @Override
    public boolean removeAll(final Collection<?> coll) {
        return map.keySet().removeAll(coll);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java

    @Override
    public Iterator<E> iterator() {
        return map.keySet().iterator();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/LinkedMap.java`
#### Snippet
```java

        @Override
        public boolean retainAll(final Collection<?> coll) {
            throw new UnsupportedOperationException();
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/LinkedMap.java`
#### Snippet
```java

        @Override
        public boolean removeAll(final Collection<?> coll) {
            throw new UnsupportedOperationException();
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/LinkedMap.java`
#### Snippet
```java

        @Override
        public boolean containsAll(final Collection<?> coll) {
            return parent.keySet().containsAll(coll);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/LinkedMap.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(final T[] array) {
            return parent.keySet().toArray(array);
        }
```

### NullableProblems
Non-annotated parameter 'object' in method 'toArray' from 'AbstractCollectionDecorator' should not override non-null parameter from 'List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractListDecorator.java`
#### Snippet
```java
 */
public abstract class AbstractListDecorator<E> extends AbstractCollectionDecorator<E>
        implements List<E> {

    /** Serialization version--necessary in an abstract class? */
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractListDecorator.java`
#### Snippet
```java

    @Override
    public boolean addAll(final int index, final Collection<? extends E> coll) {
        return decorated().addAll(index, coll);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableList.java`
#### Snippet
```java

    @Override
    public List<E> subList(final int fromIndex, final int toIndex) {
        final List<E> sub = decorated().subList(fromIndex, toIndex);
        return new UnmodifiableList<>(sub);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableList.java`
#### Snippet
```java

    @Override
    public boolean retainAll(final Collection<?> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableList.java`
#### Snippet
```java

    @Override
    public boolean addAll(final Collection<? extends E> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableList.java`
#### Snippet
```java

    @Override
    public boolean removeAll(final Collection<?> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractCollectionDecorator.java`
#### Snippet
```java

    @Override
    public boolean retainAll(final Collection<?> coll) {
        return decorated().retainAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractCollectionDecorator.java`
#### Snippet
```java

    @Override
    public boolean addAll(final Collection<? extends E> coll) {
        return decorated().addAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractCollectionDecorator.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(final T[] object) {
        return decorated().toArray(object);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractCollectionDecorator.java`
#### Snippet
```java

    @Override
    public boolean removeAll(final Collection<?> coll) {
        return decorated().removeAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractCollectionDecorator.java`
#### Snippet
```java

    @Override
    public boolean containsAll(final Collection<?> coll) {
        return decorated().containsAll(coll);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableSet.java`
#### Snippet
```java

    @Override
    public boolean addAll(final Collection<? extends E> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableSet.java`
#### Snippet
```java

    @Override
    public boolean retainAll(final Collection<?> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableSet.java`
#### Snippet
```java

    @Override
    public boolean removeAll(final Collection<?> coll) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Non-annotated parameter 'object' in method 'toArray' from 'AbstractCollectionDecorator' should not override non-null parameter from 'Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
     * Inner class EntrySet.
     */
    protected static class EntrySet<K, V> extends View<K, V, Entry<K, V>> implements Set<Entry<K, V>> {

        /** Serialization version */
```

### NullableProblems
Non-annotated parameter 'object' in method 'toArray' from 'AbstractCollectionDecorator' should not override non-null parameter from 'Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
     * Inner class Values.
     */
    protected static class Values<V> extends View<Object, V, V> implements Set<V> {

        /** Serialization version */
```

### NullableProblems
Non-annotated parameter 'object' in method 'toArray' from 'AbstractCollectionDecorator' should not override non-null parameter from 'Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
     * Inner class KeySet.
     */
    protected static class KeySet<K> extends View<K, Object, K> implements Set<K> {

        /** Serialization version */
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractReferenceMap.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(final T[] arr) {
            // special implementation to handle disappearing values
            final List<V> list = new ArrayList<>(size());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractReferenceMap.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(final T[] arr) {
            // special implementation to handle disappearing keys
            final List<K> list = new ArrayList<>(size());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractReferenceMap.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(final T[] arr) {
            // special implementation to handle disappearing entries
            final ArrayList<Entry<K, V>> list = new ArrayList<>(size());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierImpl.java`
#### Snippet
```java

    @Override
    public int compareTo(Identifier o) {
        if (_name == null && (o == null || o.getName() == null)) {
            return 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/NonPersistentMetaData.java`
#### Snippet
```java

    @Override
    public int compareTo(Object o) {
        if (o == this)
            return 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/OpenJPAId.java`
#### Snippet
```java

    @Override
    public int compareTo(Object other) {
        if (other == this)
            return 0;
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java

        @Override
        public Iterator iterator() {
            return new EntryIterator(EntryIterator.VALUE);
        }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java

        @Override
        public Iterator iterator() {
            return new EntryIterator(EntryIterator.KEY);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map map) {
        putAll(map, true);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java

        @Override
        public Iterator iterator() {
            return new EntryIterator(EntryIterator.ENTRY);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] a) {
        return asList().toArray(a);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedHashSetProxy.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection c) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedHashSetProxy.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] a) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedHashSetProxy.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection paramCollection) {
        if (_directAccess) {
            return super.removeAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedHashSetProxy.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection paramCollection) {
        if (_directAccess) {
            return super.retainAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedHashSetProxy.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection paramCollection) {
        if (_directAccess) {
            return super.addAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedHashSetProxy.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection paramCollection) {
        if (_directAccess) {
            return super.retainAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedHashSetProxy.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection paramCollection) {
        if (_directAccess) {
            return super.addAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedHashSetProxy.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection c) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedHashSetProxy.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] a) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedHashSetProxy.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection paramCollection) {
        if (_directAccess) {
            return super.removeAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedTreeSetProxy.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection paramCollection) {
        if (_directAccess) {
            return super.retainAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedTreeSetProxy.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] array) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedTreeSetProxy.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection paramCollection) {
        if (_directAccess) {
            return super.removeAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedTreeSetProxy.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection c) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedTreeSetProxy.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection paramCollection) {
        if (_directAccess) {
            return super.addAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedListProxy.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection paramCollection) {
        if (_directAccess) {
            return super.retainAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedListProxy.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection paramCollection) {
        if (_directAccess) {
            return super.addAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedListProxy.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] array) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedListProxy.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection paramCollection) {
        if (_directAccess) {
            return super.removeAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedListProxy.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection c) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java

        @Override
        public Iterator iterator() {
            final Iterator itr = _entries.iterator();
            return new Iterator() {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection paramCollection) {
        if (_directAccess) {
            return super.retainAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] array) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection paramCollection) {
        if (_directAccess) {
            return super.addAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection collection) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection paramCollection) {
        if (_directAccess) {
            return super.removeAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedPriorityQueueProxy.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection paramCollection) {
        if (_directAccess) {
            return super.removeAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedPriorityQueueProxy.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection c) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedPriorityQueueProxy.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] array) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedPriorityQueueProxy.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection paramCollection) {
        if (_directAccess) {
            return super.addAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedPriorityQueueProxy.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection paramCollection) {
        if (_directAccess) {
            return super.retainAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedVectorProxy.java`
#### Snippet
```java

    @Override
    public synchronized boolean containsAll(Collection collection) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedVectorProxy.java`
#### Snippet
```java

    @Override
    public synchronized boolean addAll(Collection paramCollection) {
        if (_directAccess) {
            return super.addAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedVectorProxy.java`
#### Snippet
```java

    @Override
    public synchronized Object[] toArray(Object[] array) {
        if (!_directAccess && isDelayLoad()) {
            load();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedVectorProxy.java`
#### Snippet
```java

    @Override
    public synchronized boolean retainAll(Collection paramCollection) {
        if (_directAccess) {
            return super.retainAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedVectorProxy.java`
#### Snippet
```java

    @Override
    public synchronized boolean removeAll(Collection paramCollection) {
        if (_directAccess) {
            return super.removeAll(paramCollection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java

    @Override
    public int compareTo(ClassMetaData other) {
        if (other == this)
            return 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] a) {
        try {
            return _del.toArray(a);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> c) {
        try {
            return _del.retainAll(c);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> c) {
        try {
            return _del.removeAll(c);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> c) {
        try {
            return _del.containsAll(c);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> c) {
        try {
            return _del.addAll(c);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> c) {
        try {
            return _del.addAll(index, c);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DistinctResultList.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends E> c) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DistinctResultList.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a) {
        try {
            return _del.toArray(a);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DistinctResultList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends E> c) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DistinctResultList.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> c) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DistinctResultList.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> c) {
        throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DistinctResultList.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> c) {
        try {
            return _del.containsAll(c);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

        @Override
        public boolean removeAll(Collection c) {
            return _res.removeAll(c);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

        @Override
        public Object[] toArray(Object[] a) {
            return _res.toArray(a);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

        @Override
        public boolean addAll(int idx, Collection c) {
            return _res.addAll(idx, c);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection c) {
            return _res.addAll(c);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection c) {
            return _res.retainAll(c);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

        @Override
        public boolean containsAll(Collection c) {
            return _res.containsAll(c);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        @Override
        public Object[] toArray(Object[] arr) {
            throw new UnsupportedOperationException();
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection coll) {
            throw new UnsupportedOperationException();
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        @Override
        public boolean removeAll(Collection coll) {
            throw new UnsupportedOperationException();
        }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        @Override
        public Iterator iterator() {
            return new Iterator() {
                private final Iterator _itr = _states.iterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection c) {
            throw new UnsupportedOperationException();
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
     */
    @Override
    public int compareTo(Identifier o) {
        if (o instanceof DBIdentifier) {
            if (o == null || o == DBIdentifier.NULL)
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     */
    @Override
    public int compareTo(Identifier o) {
        if (o instanceof DBIdentifier) {
            if (this == DBIdentifier.NULL && (o == null || o == DBIdentifier.NULL)) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/SliceThread.java`
#### Snippet
```java
        int n = 0;
        @Override
        public Thread newThread(Runnable r) {
            Thread parent = Thread.currentThread();
            return new SliceThread(parent.getName()+"-slice-"+n++, parent, r);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java

    @Override
    public int compareTo(Object other) {
        DBIdentifier name = getFullIdentifier();
        DBIdentifier otherName = ((Table) other).getFullIdentifier();
```

## RuleId[id=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'DBIdentifier' and 'String'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        DBIdentifier baseTableName = (tableName == null) ? DBIdentifier.NULL : tableName.clone();
        for (int i = 0; cols != null && i < cols.length; i++) {
            if (DBIdentifier.isNull(baseTableName) || baseTableName.equals("%")) {
                tableName = cols[i].getTableIdentifier();
            } else {
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `selector == null`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
        Object[] extract = extractSelectExecutor(result);
        SelectExecutor selector = (SelectExecutor)extract[0];
        if (selector == null)
            return new PreparedQueryCacheImpl.StrongExclusion(_id, ((Localizer.Message)extract[1]).getMessage());
        if (selector == null || selector.hasMultipleSelects()
```

### DuplicateCondition
Duplicate condition `selector == null`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
        if (selector == null)
            return new PreparedQueryCacheImpl.StrongExclusion(_id, ((Localizer.Message)extract[1]).getMessage());
        if (selector == null || selector.hasMultipleSelects()
            || ((selector instanceof Union)
            && (((Union)selector).getSelects().length != 1)))
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `Class.getTypeParameters()` is ignored
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationBuilder.java`
#### Snippet
```java
                sb.append(key).append("=");

            List.class.getTypeParameters();
            if (value instanceof List) {
                sb.append("{");
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ManagedClassSubclasser.java`
#### Snippet
```java
            File dir = new File(tmp, "openjpa");
            dir = new File(dir, "pcsubclasses");
            dir.mkdirs();
            dir = Files.getPackageFile(dir, bc.getPackageName(), true);
            File f = new File(dir, bc.getClassName() + ".class");
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/SourceRenderer.java`
#### Snippet
```java
    private FileOutputStream createOutput(File file)  {
        try {
            file.getParentFile().mkdirs();
            if (!file.exists())
                file.createNewFile();
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/SourceRenderer.java`
#### Snippet
```java
            file.getParentFile().mkdirs();
            if (!file.exists())
                file.createNewFile();
            file.setWritable(true);
            FileOutputStream out = new FileOutputStream(file);
```

### IgnoreResultOfCall
Result of `File.setWritable()` is ignored
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/SourceRenderer.java`
#### Snippet
```java
            if (!file.exists())
                file.createNewFile();
            file.setWritable(true);
            FileOutputStream out = new FileOutputStream(file);
            verbose("Output " + file.toURI());
```

### IgnoreResultOfCall
Result of `Matcher.matches()` is ignored
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/FirebirdDictionary.java`
#### Snippet
```java
                    Pattern.CASE_INSENSITIVE);
                Matcher m = p.matcher(productVersion);
                m.matches();
                String majString = m.group(1);
                String minString = m.group(2);
```

### IgnoreResultOfCall
Result of `Class.getName()` is ignored
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfigurationImpl.java`
#### Snippet
```java
        // Instruction.class to be loaded and initialized
        // before TypedInstruction.class
        try { serp.bytecode.lowlevel.Entry.class.getName(); }
        catch (Throwable t) {}
        try { serp.bytecode.Instruction.class.getName(); }
```

### IgnoreResultOfCall
Result of `Class.getName()` is ignored
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfigurationImpl.java`
#### Snippet
```java
        try { serp.bytecode.lowlevel.Entry.class.getName(); }
        catch (Throwable t) {}
        try { serp.bytecode.Instruction.class.getName(); }
        catch (Throwable t) {}

```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `getMember()` is identical to its super method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java

        @Override
        public Member<? extends Z, X> getMember() {
            return (Member<? extends Z, X>) _member;
        }
```

### RedundantMethodOverride
Method `change()` is identical to its super method
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/DelayedCollectionChangeTrackerImpl.java`
#### Snippet
```java

    @Override
    protected void change(Object elem) {
        throw new InternalException();
    }
```

### RedundantMethodOverride
Method `initialize()` is identical to its super method
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PNewFlushedDeletedFlushedState.java`
#### Snippet
```java

    @Override
    void initialize(StateManagerImpl context, PCState previous) {
    }

```

### RedundantMethodOverride
Method `isPacking()` is identical to its super method
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java

        @Override
        public boolean isPacking(StoreQuery q) {
            return false;
        }
```

### RedundantMethodOverride
Method `isVariable()` is identical to its super method
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/GetMapValue.java`
#### Snippet
```java

    @Override
    public boolean isVariable() {
        return false;
    }
```

### RedundantMethodOverride
Method `getLevel()` is identical to its super method
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/DataCacheJMXInstrument.java`
#### Snippet
```java

    @Override
    public InstrumentationLevel getLevel() {
        return InstrumentationLevel.FACTORY;
    }
```

### RedundantMethodOverride
Method `getLevel()` is identical to its super method
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/QueryCacheJMXInstrument.java`
#### Snippet
```java

    @Override
    public InstrumentationLevel getLevel() {
        return InstrumentationLevel.FACTORY;
    }
```

### RedundantMethodOverride
Method `getLevel()` is identical to its super method
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/PreparedQueryCacheJMXInstrument.java`
#### Snippet
```java

    @Override
    public InstrumentationLevel getLevel() {
        return InstrumentationLevel.FACTORY;
    }
```

### RedundantMethodOverride
Method `getDefaultFormat()` is identical to its super method
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/PropertiesCommand.java`
#### Snippet
```java

    @Override
    protected Format getDefaultFormat() {
        return Format.xml;
    }
```

### RedundantMethodOverride
Method `getMinimumArguments()` is identical to its super method
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/QueryCommand.java`
#### Snippet
```java

    @Override
    protected int getMinimumArguments() {
        return 0;
    }
```

### RedundantMethodOverride
Method `isCellEditable()` is identical to its super method
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java

    @Override
    public boolean isCellEditable(int rowIndex, int columnIndex) {
        return false;
    }
```

### RedundantMethodOverride
Method `setValueAt()` is identical to its super method
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/InstanceDataModel.java`
#### Snippet
```java

    @Override
    public void setValueAt(Object value, int rowIndex, int columnIndex) {
    }

```

### RedundantMethodOverride
Method `isCellEditable()` is identical to its super method
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/InstanceDataModel.java`
#### Snippet
```java

    @Override
    public boolean isCellEditable(int rowIndex, int columnIndex) {
        return false;
    }
```

### RedundantMethodOverride
Method `getSchemaCase()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
     */
    @Override
    public String getSchemaCase(){
        return schemaCase;
    }
```

### RedundantMethodOverride
Method `join()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MaxEmbeddedLobFieldStrategy.java`
#### Snippet
```java

    @Override
    public Joins join(Joins joins, boolean forceOuter) {
        return field.join(joins, forceOuter, false);
    }
```

### RedundantMethodOverride
Method `delete()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MaxEmbeddedLobFieldStrategy.java`
#### Snippet
```java

    @Override
    public void delete(OpenJPAStateManager sm, JDBCStore store, RowManager rm)
        throws SQLException {
        field.deleteRow(sm, store, rm);
```

### RedundantMethodOverride
Method `supportsSelect()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java

    @Override
    public int supportsSelect(Select sel, int type, OpenJPAStateManager sm,
        JDBCStore store, JDBCFetchConfiguration fetch) {
        return 0;
```

### RedundantMethodOverride
Method `getResult()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedProjectionResultObjectProvider.java`
#### Snippet
```java

    @Override
    public Result getResult() {
        return _res;
    }
```

### RedundantMethodOverride
Method `isVariable()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Extension.java`
#### Snippet
```java

    @Override
    public boolean isVariable() {
        return false;
    }
```

### RedundantMethodOverride
Method `isAggregate()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Extension.java`
#### Snippet
```java

    @Override
    public boolean isAggregate() {
        return false;
    }
```

### RedundantMethodOverride
Method `isVariable()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Args.java`
#### Snippet
```java

    @Override
    public boolean isVariable() {
        return false;
    }
```

### RedundantMethodOverride
Method `isVariable()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/GetMapValue.java`
#### Snippet
```java

    @Override
    public boolean isVariable() {
        return false;
    }
```

### RedundantMethodOverride
Method `selectColumns()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/BindVariableExpression.java`
#### Snippet
```java

    @Override
    public void selectColumns(Select sel, ExpContext ctx, ExpState state,
        boolean pks) {
    }
```

### RedundantMethodOverride
Method `initialize()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Type.java`
#### Snippet
```java

    @Override
    public ExpState initialize(Select sel, ExpContext ctx, int flags) {
        return initializeValue(sel, ctx, flags);
    }
```

### RedundantMethodOverride
Method `isKnownTable()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java

    @Override
    public boolean isKnownTable(QualifiedDBIdentifier path) {
        return super.findTable(path) != null;
    }
```

### RedundantMethodOverride
Method `isKnownTable()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java

    @Override
    public boolean isKnownTable(Table table) {
        return super.findTable(table) != null;
    }
```

### RedundantMethodOverride
Method `getFullIdentifier()` is identical to its super method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Index.java`
#### Snippet
```java

    @Override
    public DBIdentifier getFullIdentifier() {
        return getQualifiedPath().getIdentifier();
    }
```

### RedundantMethodOverride
Method `isCloseOnCompletion()` is identical to its super method
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedPreparedStatement.java`
#### Snippet
```java

    @Override
    public boolean isCloseOnCompletion() throws SQLException{
    	throw new UnsupportedOperationException();
    }
```

### RedundantMethodOverride
Method `closeOnCompletion()` is identical to its super method
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedPreparedStatement.java`
#### Snippet
```java

    @Override
    public void closeOnCompletion() throws SQLException{
    	throw new UnsupportedOperationException();
    }
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
_interval \* 60 \* 1000: integer multiplication implicitly cast to long
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ClearableScheduler.java`
#### Snippet
```java
        while (!isStopped()) {
            try {
                Thread.sleep(_interval * 60 * 1000);

                Date now = new Date();
```

### IntegerMultiplicationImplicitCastToLong
batch \* 2: integer multiplication implicitly cast to long
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PagingResultObjectProvider.java`
#### Snippet
```java
            if (size <= batch)
                pageSize = (int) size;
            else if (size <= batch * 2) {
                if (size % 2 == 0)
                    pageSize = (int) (size / 2);
```

### IntegerMultiplicationImplicitCastToLong
_allocate \* _increment: integer multiplication implicitly cast to long
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/NativeJDBCSeq.java`
#### Snippet
```java
            }
            _nextValue = getSequence(conn);
            _maxValue = _nextValue + _allocate * _increment;
        } finally {
            closeConnection(conn);
```

## RuleId[id=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import org.apache.openjpa.jdbc.identifier.DBIdentifier;`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ClassTableJDBCSeq.java`
#### Snippet
```java
import org.apache.openjpa.jdbc.conf.JDBCConfiguration;
import org.apache.openjpa.jdbc.conf.JDBCConfigurationImpl;
import org.apache.openjpa.jdbc.identifier.DBIdentifier;
import org.apache.openjpa.jdbc.meta.ClassMapping;
import org.apache.openjpa.jdbc.meta.MappingRepository;
```

### UNUSED_IMPORT
Unused import `import jakarta.persistence.Embeddable;`
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java
import serp.bytecode.Project;

import jakarta.persistence.Embeddable;
import jakarta.persistence.Entity;
import jakarta.persistence.MappedSuperclass;
```

### UNUSED_IMPORT
Unused import `import jakarta.persistence.Entity;`
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java

import jakarta.persistence.Embeddable;
import jakarta.persistence.Entity;
import jakarta.persistence.MappedSuperclass;
import java.io.ByteArrayInputStream;
```

### UNUSED_IMPORT
Unused import `import jakarta.persistence.MappedSuperclass;`
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java
import jakarta.persistence.Embeddable;
import jakarta.persistence.Entity;
import jakarta.persistence.MappedSuperclass;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
```

### UNUSED_IMPORT
Unused import `import java.math.BigDecimal;`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
package org.apache.openjpa.persistence;

import java.math.BigDecimal;
import java.math.BigInteger;
import java.util.HashMap;
```

### UNUSED_IMPORT
Unused import `import java.math.BigInteger;`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java

import java.math.BigDecimal;
import java.math.BigInteger;
import java.util.HashMap;
import java.util.Locale;
```

### UNUSED_IMPORT
Unused import `import java.time.LocalDate;`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
import java.sql.Time;
import java.sql.Timestamp;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.LocalTime;
```

### UNUSED_IMPORT
Unused import `import java.time.LocalDateTime;`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
import java.sql.Timestamp;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.util.Collection;
```

### UNUSED_IMPORT
Unused import `import java.time.LocalTime;`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.util.Collection;
import java.util.Collections;
```

## RuleId[id=SimplifiableBooleanExpression]
### SimplifiableBooleanExpression
`(isMixed(code) && isExplicit(code)) || !isMixed(code)` can be simplified to '!isMixed(code)\|\|isExplicit(code)'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AccessCode.java`
#### Snippet
```java
         && !(isProperty(code) && isField(code)) // both 1 & 2 can not be set
		 && (isProperty(code) || isField(code) || isUnknown(code))
		 && ((isMixed(code) && isExplicit(code)) || !isMixed(code));
	}

```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `s` are queried, but never updated
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryKey.java`
#### Snippet
```java
        // handle the set types; jdk uses different classes for collection,
        // set, and sorted set
        TreeSet<Object> s = new TreeSet<>();
        s_unmod.add(Collections.unmodifiableCollection(s).getClass());
        s_unmod.add(Collections.unmodifiableSet(s).getClass());
```

### MismatchedCollectionQueryUpdate
Contents of collection `l` are queried, but never updated
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryKey.java`
#### Snippet
```java
        // handle the list types; jdk uses different classes for standard
        // and random access lists
        List<Object> l = new LinkedList<>();
        s_unmod.add(Collections.unmodifiableList(l).getClass());
        l = new ArrayList<>(0);
```

### MismatchedCollectionQueryUpdate
Contents of collection `_visited` are updated, but never queried
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SourceCodeViewer.java`
#### Snippet
```java
    private final JComboBox   _bookmarks;
    private IndexedMap<String, URI> _anchors = new IndexedMap<>();
    private LinkedList<String> _visited = new LinkedList<>();

    /**
```

### MismatchedCollectionQueryUpdate
Contents of collection `customIdentifiers` are queried, but never updated
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/HTMLTokenRenderer.java`
#### Snippet
```java
    public static final String CSS_LINE_NO     = "lineno";

    private Set<String> customIdentifiers = new HashSet<>();

    public static final String NEW_LINE = "\r\n";
```

### MismatchedCollectionQueryUpdate
Contents of collection `toolProperties` are queried, but never updated
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java
     */
    @Parameter
    private Properties toolProperties;

    /**
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AccessCode.java`
#### Snippet
```java
	public static int UNKNOWN   = 0;
	public static int EMPTY     = 1;
	public static int FIELD     = 2 << 0;
	public static int PROPERTY  = 2 << 1;
	public static int EXPLICIT  = 2 << 2;
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/LifecycleMetaData.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    public static final int IGNORE_NONE = 0;
    public static final int IGNORE_HIGH = 2 << 0;
    public static final int IGNORE_LOW = 2 << 1;

```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/CallbackModes.java`
#### Snippet
```java
     * Flag to stop executing callbacks on exception immediately.
     */
    int CALLBACK_FAIL_FAST = 2 << 0;

    /**
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreContext.java`
#### Snippet
```java
    BitSet EXCLUDE_ALL = new BitSet(0);

    int OID_NOVALIDATE = 2 << 0;
    int OID_NODELETED = 2 << 1;
    int OID_COPY = 2 << 2;
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AutoDetach.java`
#### Snippet
```java
     * Detach context on close.
     */
    int DETACH_CLOSE = 2 << 0;

    /**
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/OpCallbacks.java`
#### Snippet
```java

    int ACT_NONE = 0;
    int ACT_CASCADE = 2 << 0;
    int ACT_RUN = 2 << 1;

```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
    public static final int LOAD_SERIALIZE = 2;

    private static final int FLAG_SAVE = 2 << 0;
    private static final int FLAG_DEREF = 2 << 1;
    private static final int FLAG_LOADED = 2 << 2;
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/QueryExpressions.java`
#### Snippet
```java
public class QueryExpressions implements Serializable {
    private static final long serialVersionUID = 1L;
    public static final int DISTINCT_AUTO = 2 << 0;
    public static final int DISTINCT_TRUE = 2 << 1;
    public static final int DISTINCT_FALSE = 2 << 2;
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
    static final int STATUS_COMMIT_NEW = 3;

    private static final int FLAG_ACTIVE = 2 << 0;
    private static final int FLAG_STORE_ACTIVE = 2 << 1;
    private static final int FLAG_CLOSE_INVOKED = 2 << 2;
```

### PointlessBitwiseExpression
`1 << 0` can be replaced with '1'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
     * Constant designates to include non-relation fields.
     */
    public static int BASIC_ATTR       = 1 << 0;
    /**
     * Constant designates to include single-valued relation fields.
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java

    public static final int ENHANCE_NONE = 0;
    public static final int ENHANCE_AWARE = 2 << 0;
    public static final int ENHANCE_INTERFACE = 2 << 1;
    public static final int ENHANCE_PC = 2 << 2;
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    implements Select, PathJoins {

    private static final int NONAUTO_DISTINCT = 2 << 0;
    private static final int DISTINCT = 2 << 1;
    private static final int NOT_DISTINCT = 2 << 2;
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/RowImpl.java`
#### Snippet
```java

    public static final Object NULL = new Object();
    protected static final int VALID = 2 << 0;

    public static final int RAW = Integer.MIN_VALUE;
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Val.java`
#### Snippet
```java
     * Initialization flag indicating that this value will be compared to null.
     */
    int NULL_CMP = 2 << 0;

    /**
```

### PointlessBitwiseExpression
`2 << 0` can be replaced with '2'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
public class Column extends ReferenceCounter {
    private static final long serialVersionUID = 1L;
    public static final int FLAG_UNINSERTABLE = 2 << 0;
    public static final int FLAG_UNUPDATABLE = 2 << 1;
    public static final int FLAG_DIRECT_INSERT = 2 << 2;
```

## RuleId[id=TextLabelInSwitchStatement]
### TextLabelInSwitchStatement
Text label `remove:` in 'switch' statement
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/InverseManager.java`
#### Snippet
```java
            switch (_action) {
                case ACTION_MANAGE:
                    remove:
                    for (int i = 0; coll.remove(val); i++)
                        if (i == 0 && coll instanceof Set)
```

## RuleId[id=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivation.java`
#### Snippet
```java
     * @since 1.1.0
     */
    List<String> getAnchorsInFile(File file) throws IOException,
            Exception;

```

### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/ManagedRuntime.java`
#### Snippet
```java
     */
    Object getTransactionKey()
        throws Exception, SystemException;

    /**
```

### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AbstractManagedRuntime.java`
#### Snippet
```java
     */
    @Override
    public Object getTransactionKey() throws Exception, SystemException {
        return getTransactionManager().getTransaction();
    }
```

### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/RegistryManagedRuntime.java`
#### Snippet
```java

    @Override
    public Object getTransactionKey() throws Exception, SystemException {
        return _tm.getTransactionKey();
    }
```

### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java

    @Override
    public Object getTransactionKey() throws Exception, SystemException {
        if(_runtime == null)
            getTransactionManager();
```

## RuleId[id=StringConcatenationInLoops]
### StringConcatenationInLoops
String concatenation `+=` in loop
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
            tab = "";
            for (int i = 0; i < tabCount * TABSIZE; i++) {
                tab += SPACE;
            }
        }
```

### StringConcatenationInLoops
String concatenation `+=` in loop
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
            tab = "";
            for (int i = 0; i < tabCount * TABSIZE; i++) {
                tab += SPACE;
            }
        }
```

### StringConcatenationInLoops
String concatenation `+` in loop
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        ClassName(String name) {
            while (isArray(name)) {
                arrayMarker = arrayMarker + "[]";
                name = getComponentName(name);
            }
```

### StringConcatenationInLoops
String concatenation `+` in loop
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
            }
            else {
                combined = combined + config.getIdentifierConcatenator() + name;
            }
        }
```

### StringConcatenationInLoops
String concatenation `+=` in loop
in `openjpa-examples/openbooks/src/main/java/openbook/tools/util/URIUtils.java`
#### Snippet
```java
            //  one because the last element in the path isn't a directory.
            for (int i = 1; i <= (numDirsUp); i++) {
                relative += ".." + pathSeparator;
            }
        }
```

### StringConcatenationInLoops
String concatenation `+=` in loop
in `openjpa-examples/simple/src/main/java/embeddables/User.java`
#### Snippet
```java

        for (Address a : addresses) {
            res += "\n" + a.toString();
        }
        return res;
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/InvalidStateException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/InvalidStateException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public InvalidStateException(Message msg, Throwable[] nested, Object failed,
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/InvalidStateException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/NonUniqueResultException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/NonUniqueResultException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public NonUniqueResultException(String msg, Throwable[] nested,
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/NonUniqueResultException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public PersistenceException(String msg, Throwable[] nested,
```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OptimisticLockException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public OptimisticLockException(String msg, Throwable[] nested,
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OptimisticLockException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OptimisticLockException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TransactionRequiredException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TransactionRequiredException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TransactionRequiredException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public TransactionRequiredException(Message msg, Throwable[] nested,
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryTimeoutException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryTimeoutException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public QueryTimeoutException(String msg, Throwable[] nested, Object failed) {
```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryTimeoutException.java`
#### Snippet
```java

    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityNotFoundException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public EntityNotFoundException(String msg, Throwable[] nested,
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityNotFoundException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityNotFoundException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/NoResultException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public NoResultException(String msg, Throwable[] nested,
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/NoResultException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/NoResultException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PessimisticLockException.java`
#### Snippet
```java
    private transient boolean _fatal = true;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public PessimisticLockException(String msg, Throwable[] nested, Object failed) {
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PessimisticLockException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = true;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ArgumentException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ArgumentException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ArgumentException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public ArgumentException(String msg, Throwable[] nested, Object failed,
```

### UnusedAssignment
The value `matchGetterAndSetter(getters, setters)` assigned to `getters` is never used
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java

            List<Method> setters = filter(publicMethods, setterFilter);
            return getters = matchGetterAndSetter(getters, setters);
    	}

```

### UnusedAssignment
Variable `_lock` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java

	private String _id;
    private transient ReentrantLock _lock = null;
	private HintHandler _hintHandler;
    private DelegatingQuery _query;
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityExistsException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityExistsException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public EntityExistsException(String msg, Throwable[] nested,
```

### UnusedAssignment
Variable `_fatal` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityExistsException.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;
```

### UnusedAssignment
Variable `_failed` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/LockTimeoutException.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

```

### UnusedAssignment
Variable `_nested` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/LockTimeoutException.java`
#### Snippet
```java
    private transient boolean _fatal = false;
    private transient Object _failed = null;
    private transient Throwable[] _nested = null;

    public LockTimeoutException(String msg, Throwable[] nested,  Object failed) {
```

### UnusedAssignment
The value `Class.forName("org.osgi.framework.BundleActivator")` assigned to `c` is never used
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/osgi/BundleUtils.java`
#### Snippet
```java
            @SuppressWarnings("unused")
            Class<?> c = Class.forName("org.osgi.framework.Bundle");
            c = Class.forName("org.osgi.framework.BundleActivator");
            c = Class.forName("org.osgi.framework.BundleContext");
            c = Class.forName("org.osgi.framework.ServiceRegistration");
```

### UnusedAssignment
The value `Class.forName("org.osgi.framework.BundleContext")` assigned to `c` is never used
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/osgi/BundleUtils.java`
#### Snippet
```java
            Class<?> c = Class.forName("org.osgi.framework.Bundle");
            c = Class.forName("org.osgi.framework.BundleActivator");
            c = Class.forName("org.osgi.framework.BundleContext");
            c = Class.forName("org.osgi.framework.ServiceRegistration");
            return true;
```

### UnusedAssignment
The value `Class.forName("org.osgi.framework.ServiceRegistration")` assigned to `c` is never used
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/osgi/BundleUtils.java`
#### Snippet
```java
            c = Class.forName("org.osgi.framework.BundleActivator");
            c = Class.forName("org.osgi.framework.BundleContext");
            c = Class.forName("org.osgi.framework.ServiceRegistration");
            return true;
        } catch (ClassNotFoundException | NoClassDefFoundError e) {
```

### UnusedAssignment
Variable `urls` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
    private static List<URL> getResourceURLs(String rsrc, ClassLoader loader)
        throws IOException {
        Enumeration<URL> urls = null;
        try {
            urls = AccessController.doPrivileged(J2DoPrivHelper.getResourcesAction(loader, rsrc));
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
     */
    TypeMirror getDeclaredType(Element e) {
        TypeMirror result = null;
        switch (e.getKind()) {
        case FIELD:
```

### UnusedAssignment
Variable `idCls` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
        ClassMetaData meta = (ClassMetaData) currentElement();
        String cls = attrs.getValue("class");
        Class<?> idCls = null;
        try {
            idCls = classForName(cls);
```

### UnusedAssignment
Variable `cascades` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
        boolean puDefault = false;

        Set<CascadeType> cascades = null;
        if (currentElement() instanceof FieldMetaData) {
            if (_cascades == null)
```

### UnusedAssignment
Variable `access` initializer `AccessCode.UNKNOWN` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
        }

        int access = AccessCode.UNKNOWN;
        if (meta == null) {
            int accessCode = toAccessType(attrs.getValue("access"));
```

### UnusedAssignment
Variable `typeCls` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
    private Class<?> parseTypeStr(String typeStr)
        throws SAXException {
        Class<?> typeCls = null;
        try {
            if (typeStr.equalsIgnoreCase("int")) {
```

### UnusedAssignment
Variable `name` initializer `""` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
     */
    static class FetchAttributeImpl {
        private String name = "";
        private int recursionDepth = Integer.MIN_VALUE;

```

### UnusedAssignment
Variable `name` initializer `""` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
     */
    static class FetchGroupImpl {
        private String name = "";
        private boolean postLoad = false;
        private FetchAttributeImpl[] attributes = {};
```

### UnusedAssignment
Variable `recursionDepth` initializer `Integer.MIN_VALUE` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
    static class FetchAttributeImpl {
        private String name = "";
        private int recursionDepth = Integer.MIN_VALUE;

        public FetchAttributeImpl(String name, int recursionDepth) {
```

### UnusedAssignment
Variable `postLoad` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
    static class FetchGroupImpl {
        private String name = "";
        private boolean postLoad = false;
        private FetchAttributeImpl[] attributes = {};
        private String[] fetchGroups = {};
```

### UnusedAssignment
Variable `kType` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
        String beanProp = getBeanPropertyName(original);
        JPAProperties.record(beanProp, original);
        Class<?> kType  = null;
        Object   kValue = null;
        Method setter = Reflection.findSetter(target.getClass(), beanProp, false);
```

### UnusedAssignment
Variable `kValue` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
        JPAProperties.record(beanProp, original);
        Class<?> kType  = null;
        Object   kValue = null;
        Method setter = Reflection.findSetter(target.getClass(), beanProp, false);
        if (setter != null) {
```

### UnusedAssignment
Variable `strategy` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
            Class<?> resultClass = getJavaType();
            List<Selection<?>> terms = getCompoundSelectionItems();
            FillStrategy<?> strategy = null;
            if (jakarta.persistence.Tuple.class.isAssignableFrom(resultClass)) {
                TupleFactory factory = new TupleFactory(terms.toArray(new TupleElement[terms.size()]));
```

### UnusedAssignment
The value `Object[].class` assigned to `resultClass` is never used
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
           } else if (resultClass == Object.class) {
               if (terms.size() > 1) {
                   resultClass = Object[].class;
                   strategy = new FillStrategy.Array<>(Object[].class);
               } else {
```

### UnusedAssignment
Variable `var` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/RootImpl.java`
#### Snippet
```java
    public Value toValue(ExpressionFactory factory, CriteriaQueryImpl<?> c) {
        SubqueryImpl<?> subquery = c.getDelegator();
        Path var = null;
        Value val = null;
        String alias = c.getAlias(this);
```

### UnusedAssignment
Variable `val` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/RootImpl.java`
#### Snippet
```java
        SubqueryImpl<?> subquery = c.getDelegator();
        Path var = null;
        Value val = null;
        String alias = c.getAlias(this);
        if (c.ctx() != null && !alias.equalsIgnoreCase(c.ctx().schemaAlias)
```

### UnusedAssignment
Variable `_aliases` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    private Map<Selection<?>,Value> _variables = new HashMap<>();
    private Map<Selection<?>,Value> _values    = new HashMap<>();
    private Map<Selection<?>,String> _aliases  = null;
    private Map<Selection<?>,Value> _rootVariables = new HashMap<>();

```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        List<Selection<?>> selections = q.getSelectionList();
        Class<?> resultClass = q.getResultType();
        ResultShape<?> result = null;
        if (q.isMultiselect()) {
           result = new ResultShape(resultClass, CompoundSelections.getFillStrategy(q.getSelection()));
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
     */
    ResultShape<?> getShape(CriteriaQueryImpl<?> q, ResultShape<?> parent, Selection<?> s) {
        ResultShape<?> result = null;
        Class<?> type = s.getJavaType();
        if (type == null)
```

### UnusedAssignment
Variable `path` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public Value toValue(ExpressionFactory factory, CriteriaQueryImpl<?> c) {
            org.apache.openjpa.kernel.exps.Path path = null;
            SubqueryImpl<?> subquery = c.getDelegator();
            PathImpl<?,?> parent = getInnermostParentPath();
```

### UnusedAssignment
Variable `allowNull` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
    static class SingularJoin<Z,X> extends FromImpl<Z,X> implements Join<Z,X> {
        private final JoinType joinType;
        private boolean allowNull = false;

        public SingularJoin(FromImpl<?,Z> from, Members.SingularAttributeImpl<? super Z, X> member, JoinType jt) {
```

### UnusedAssignment
The value `factory.newPath(subQ)` assigned to `path` is never used
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                    corrJoins = subquery.getCorrelatedJoins();
                    org.apache.openjpa.kernel.exps.Subquery subQ = subquery.getSubQ();
                    path = factory.newPath(subQ);
                    if ((corrJoin != null || corrRoot != null) && _parent.getCorrelatedPath() != null) {
                        subQ.setSubqAlias(c.getAlias(this));
```

### UnusedAssignment
Variable `parentPath` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                return expr;
            } else {
                org.apache.openjpa.kernel.exps.Path parentPath = null;
                if (!corrJoins.isEmpty() && corrJoins.contains(_parent)) {
                    Value var = getVariableForCorrPath(subquery, correlatedParentPath);
```

### UnusedAssignment
Variable `from` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        Members.Member member = join.getMember();
        JoinType jt = ((Join) join).getJoinType();
        FromImpl<?, ?> from = null;
        if (parent instanceof RootImpl) {
            members.add(member);
```

### UnusedAssignment
Variable `path` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        public Value toValue(ExpressionFactory factory, CriteriaQueryImpl<?> c) {
            ClassMetaData meta = _member.fmd.getDeclaredTypeMetaData();
            org.apache.openjpa.kernel.exps.Path path = null;
            SubqueryImpl<?> subquery = c.getDelegator();
            PathImpl<?,?> parent = getInnermostParentPath();
```

### UnusedAssignment
Variable `allowNull` initializer `false` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
    static abstract class AbstractCollection<Z,C,E> extends FromImpl<Z,E> implements PluralJoin<Z, C, E> {
        private final JoinType joinType;
        private boolean allowNull = false;

        public AbstractCollection(FromImpl<?,Z> from, Members.PluralAttributeImpl<? super Z, C, E> member,
```

### UnusedAssignment
Variable `parentPath` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                return expr;
            } else {
                org.apache.openjpa.kernel.exps.Path parentPath = null;
                if (corrJoins != null && corrJoins.contains(_parent)) {
                    Value var = getVariableForCorrPath(subquery, correlatedParentPath);
```

### UnusedAssignment
Variable `_rows` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/RandomAccessResultList.java`
#### Snippet
```java

    // holds all the row values that have been instantiated so far
    private Map _rows = null;
    private Object[] _full = null;

```

### UnusedAssignment
Variable `_rop` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/RandomAccessResultList.java`
#### Snippet
```java

    // data provider
    private ResultObjectProvider _rop = null;

    // holds all the row values that have been instantiated so far
```

### UnusedAssignment
Variable `_idx` initializer `0` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractNonSequentialResultList.java`
#### Snippet
```java
    private class Itr extends AbstractListIterator {

        private int _idx = 0;
        private Object _next = PAST_END;

```

### UnusedAssignment
Variable `_rop` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/WindowResultList.java`
#### Snippet
```java
    private final Object[] _window;
    private int _pos = -1;
    private ResultObjectProvider _rop = null;
    private boolean _random = false;
    private int _state = OPEN;
```

### UnusedAssignment
Variable `_rop` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/LazyForwardResultList.java`
#### Snippet
```java
    private static final int FREED = 2;

    private ResultObjectProvider _rop = null;
    private final List _list = new ArrayList();
    private int _state = OPEN;
```

### UnusedAssignment
Variable `_idx` initializer `0` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/LazyForwardResultList.java`
#### Snippet
```java
    private class Itr extends AbstractListIterator {

        private int _idx = 0;

        public Itr(int index) {
```

### UnusedAssignment
Variable `_saxFactories` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/XMLFactory.java`
#### Snippet
```java

    // cache parsers and transformers in all possible configurations
    private static SAXParserFactory[] _saxFactories = null;
    private static DocumentBuilderFactory[] _domFactories = null;
    private static ErrorHandler _validating;
```

### UnusedAssignment
Variable `_domFactories` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/XMLFactory.java`
#### Snippet
```java
    // cache parsers and transformers in all possible configurations
    private static SAXParserFactory[] _saxFactories = null;
    private static DocumentBuilderFactory[] _domFactories = null;
    private static ErrorHandler _validating;

```

### UnusedAssignment
Variable `_nullOnNoLocator` initializer `false` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/Location.java`
#### Snippet
```java
    private static final Localizer _loc = Localizer.forPackage(Location.class);

    private boolean _nullOnNoLocator = false;
    private Locator _locator = null;

```

### UnusedAssignment
Variable `_xml` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/DocTypeReader.java`
#### Snippet
```java
public class DocTypeReader extends Reader {

    private Reader _xml = null;
    private Reader _docType = null;

```

### UnusedAssignment
Variable `_docType` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/DocTypeReader.java`
#### Snippet
```java

    private Reader _xml = null;
    private Reader _docType = null;

    // use to hold all header information until the doctype dec should be
```

### UnusedAssignment
The value `Literal.TYPE_CLASS` assigned to `literalType` is never used
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                literalType = Literal.TYPE_ENUM;
            } else if (Class.class.isAssignableFrom(literalClass)) {
                literalType = Literal.TYPE_CLASS;
                Literal lit = factory.newTypeLiteral(value, Literal.TYPE_CLASS);
                ClassMetaData can = ((Types.Entity<X>)q.getRoot().getModel()).meta;
```

### UnusedAssignment
Variable `inExpr` initializer `null` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        org.apache.openjpa.kernel.exps.Expression toKernelExpression(
            ExpressionFactory factory, CriteriaQueryImpl<?> q) {
            org.apache.openjpa.kernel.exps.Expression inExpr = null;
            if (_exps.size() == 1) {
                Expressions.Equal e = (Expressions.Equal)_exps.get(0);
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/SQLFormatter.java`
#### Snippet
```java

            while (sql.length() > 0) {
                String line = null;

                int index = Math.max(sql.toString().indexOf(";\n"),
```

### UnusedAssignment
Variable `end` initializer `-1` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/SQLFormatter.java`
#### Snippet
```java

        int start = 0;
        int end = -1;
        StringBuilder clause;
        List<StringBuilder> clauses = new ArrayList<>();
```

### UnusedAssignment
Variable `ret` initializer `true` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
    private static boolean launchRunnable(Options opts,
        Configurations.Runnable runnable) {
        boolean ret = true;
        try {
            ret = runnable.run(opts);
```

### UnusedAssignment
The value `AccessController.doPrivileged(
J2DoPrivHelper.getContextClassLoaderAction())` assigned to `loader` is never used
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
            return null;
        if (loader == null)
            loader = AccessController.doPrivileged(
                J2DoPrivHelper.getContextClassLoaderAction());
        ConfigurationProvider provider = null;
```

### UnusedAssignment
Variable `provider` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
            loader = AccessController.doPrivileged(
                J2DoPrivHelper.getContextClassLoaderAction());
        ConfigurationProvider provider = null;
        StringBuilder errs = null;
        Throwable err = null;
```

### UnusedAssignment
Variable `provider` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
                J2DoPrivHelper.getContextClassLoaderAction());

        ConfigurationProvider provider = null;
        StringBuilder errs = null;
        String type = (globals) ? "globals" : "defaults";
```

### UnusedAssignment
Variable `provider` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
            loader = AccessController.doPrivileged(
                J2DoPrivHelper.getContextClassLoaderAction());
        ConfigurationProvider provider = null;
        StringBuilder errs = null;
        // most specific to least
```

### UnusedAssignment
Variable `fin` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/FileMetaDataIterator.java`
#### Snippet
```java
            long len = AccessController.doPrivileged(
                    J2DoPrivHelper.lengthAction(_file));
            FileInputStream fin = null;
            try {
                fin = AccessController.doPrivileged(
```

### UnusedAssignment
Variable `fis` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/FileMetaDataIterator.java`
#### Snippet
```java
        if (_file == null)
            throw new IllegalStateException();
        FileInputStream fis = null;
        try {
            fis = AccessController.doPrivileged(
```

### UnusedAssignment
Variable `sysPropHM` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            Properties systemProperties = AccessController.doPrivileged(
                    J2DoPrivHelper.getPropertiesAction());
            HashMap sysPropHM = null;
            synchronized(systemProperties) {
                // Prevent concurrent modification of systemProperties until HashMap ctor is completed.
```

### UnusedAssignment
Variable `loc` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
    private String findLocalized(String key, boolean fatal, Class<?> scope) {
        // find the localizer package that contains this key
        Localizer loc = null;

        // check the package that the value claims to be defined in, if
```

### UnusedAssignment
Variable `ip` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/UUIDGenerator.java`
#### Snippet
```java
        _seq = (short) RANDOM.nextInt(MAX_14BIT);

        byte[] ip = null;
        try {
            ip = InetAddress.getLocalHost().getAddress();
```

### UnusedAssignment
Variable `now` initializer `0` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/UUIDGenerator.java`
#### Snippet
```java
        // Set time info.  Have to do this processing within a synchronized
        // block because of the statics...
        long now = 0;
        synchronized (UUIDGenerator.class) {
            // Get the time to use for this uuid.  This method has the side
```

### UnusedAssignment
Variable `_virtualMachineClass` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/JavaVendors.java`
#### Snippet
```java

    private static final JavaVendors _vendor;
    private String _virtualMachineClass = null;

    private JavaVendors(String vmClass) {
```

### UnusedAssignment
Variable `parser` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataParser.java`
#### Snippet
```java
            _sourceName = sourceName;

            SAXParser parser = null;
            boolean overrideCL = _overrideContextClassloader;
            ClassLoader oldLoader = null;
```

### UnusedAssignment
Variable `n` initializer `0` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/TemporaryClassLoader.java`
#### Snippet
```java
        byte[] b = new byte[1024];
        try {
            for (int n = 0; (n = resource.read(b, 0, b.length)) != -1;
                bout.write(b, 0, n))
                ;
```

### UnusedAssignment
Variable `token` initializer `TOKEN_NONE` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java

        boolean comment = false;
        int token = TOKEN_NONE;
        String pkg = "";
        String name;
```

### UnusedAssignment
The value `TOKEN_NONE` assigned to `token` is never used
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
            if (ch != '<')
                continue;
            token = TOKEN_NONE; // reset token
            last = ch; // update needed for comment detection
            ch = readThroughWhitespace(in);
```

### UnusedAssignment
Variable `urlCon` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
        InputStream in = null;
        BufferedReader reader = null;
        URLConnection urlCon = null;

        try {
```

### UnusedAssignment
Variable `file` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Files.java`
#### Snippet
```java

        pkg = pkg.replace('.', File.separatorChar);
        File file = null;
        try {
            if ((AccessController.doPrivileged(
```

### UnusedAssignment
Variable `err` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java

        // look for a string constructor
        Exception err = null;
        try {
            Constructor<?> cons = type.getConstructor(new Class[]{ String.class });
```

### UnusedAssignment
Variable `subType` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
        // special case: the argument value is a subtype name and a new instance
        // of that type should be set as the object
        Class<?> subType = null;
        try {
            subType = Class.forName(str);
```

### UnusedAssignment
The value `e` assigned to `err` is never used
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
            subType = Class.forName(str);
        } catch (Exception e) {
            err = e;
            throw new ParseException(_loc.get("conf-no-type", str, type), e);
        }
```

### UnusedAssignment
Variable `entry` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
    public Options setInto(Object obj) {
        // set all defaults that have no explicit value
        Map.Entry entry = null;
        if (defaults != null) {
            for (Map.Entry<Object, Object> objectObjectEntry : defaults.entrySet()) {
```

### UnusedAssignment
Variable `name` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
            } finally {
                if (_logs.isJDBCEnabled()) {
                    String name = null;
                    try {
                        name = savepoint.getSavepointName();
```

### UnusedAssignment
Variable `name` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
            } finally {
                if (_logs.isJDBCEnabled()) {
                    String name = null;
                    try {
                        name = savepoint.getSavepointName();
```

### UnusedAssignment
Variable `toReturn` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
     */
    private SQLException wrap(SQLException sqle, Statement stmnt, String sql, int indexOfFailedBatchObject) {
        ReportingSQLException toReturn = null;

        if (sqle instanceof ReportingSQLException) {
```

### UnusedAssignment
The value `line.charAt(pos)` assigned to `c` is never used
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
            if (!isDelim && (c == ':' || c == '=')) {
                pos++;
                while (pos < line.length() && isSpace(c = line.charAt(pos)))
                    pos++;
            }
```

### UnusedAssignment
The value `line.charAt(pos)` assigned to `c` is never used
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
                        pos = 0;
                        while (pos < line.length()
                            && isSpace(c = line.charAt(pos)))
                            pos++;
                        element.ensureCapacity(line.length() - pos +
```

### UnusedAssignment
Variable `orderingBits` initializer `null` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/ComparatorChain.java`
#### Snippet
```java
    private final List<Comparator<E>> comparatorChain;
    /** Order - false (clear) = ascend; true (set) = descend. */
    private BitSet orderingBits = null;
   /** Whether the chain has been "locked". */
    private boolean isLocked = false;
```

### UnusedAssignment
Variable `index` initializer `0` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java

            Entry[] tab = table;
            int index = 0;

            index = (hash & 0x7FFFFFFF) % tab.length;
```

### UnusedAssignment
Variable `pname` initializer `name` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
        // Split names by object delimiter not between name delimiters
        ArrayList<String> names = new ArrayList<>(2);
        String pname = name;

        // for each name
```

### UnusedAssignment
Variable `sName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/PersistenceMappingDefaults.java`
#### Snippet
```java
        // prefix with owning entity name
        FieldMapping[] inverses = fm.getInverseMappings();
        DBIdentifier sName = DBIdentifier.NULL;
        if (inverses.length > 0)
            sName = DBIdentifier.newColumn(inverses[0].getName());
```

### UnusedAssignment
Variable `sName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/PersistenceMappingDefaults.java`
#### Snippet
```java
        Table table = getTable(clm);

        DBIdentifier sName = DBIdentifier.NULL;
        if (fm.isElementCollection())
            sName = DBIdentifier.newTable(clm.getTypeAlias());
```

### UnusedAssignment
Variable `sName2` initializer `DBIdentifier.NULL` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/PersistenceMappingDefaults.java`
#### Snippet
```java
        boolean assoc = rel != null && rel.getTable() != null
            && fm.getTypeCode() != JavaTypes.MAP;
        DBIdentifier sName2 = DBIdentifier.NULL;
        if (assoc) {
            sName2 = rel.getTable().getIdentifier();
```

### UnusedAssignment
Variable `tm` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AbstractManagedRuntime.java`
#### Snippet
```java
    public void doNonTransactionalWork(Runnable runnable) throws
            NotSupportedException {
        TransactionManager tm = null;
        Transaction transaction = null;

```

### UnusedAssignment
Variable `transaction` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AbstractManagedRuntime.java`
#### Snippet
```java
            NotSupportedException {
        TransactionManager tm = null;
        Transaction transaction = null;

        try {
```

### UnusedAssignment
Variable `_sync` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WASManagedRuntime.java`
#### Snippet
```java
    static class WASSynchronization implements com.ibm.websphere.jtaextensions.SynchronizationCallback {

        Synchronization _sync = null;

        WASSynchronization(Synchronization sync) {
```

### UnusedAssignment
Variable `mr` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java

    public AutomaticManagedRuntime() {
        ManagedRuntime mr = null;

        mr = null;
```

### UnusedAssignment
Variable `_name` initializer `""` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Specification.java`
#### Snippet
```java
 */
public class Specification {
    private String _name = "";
    private int    _major = 1;
    private String _minor = "0";
```

### UnusedAssignment
Variable `_major` initializer `1` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Specification.java`
#### Snippet
```java
public class Specification {
    private String _name = "";
    private int    _major = 1;
    private String _minor = "0";
    private String _description = "";
```

### UnusedAssignment
Variable `_minor` initializer `"0"` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Specification.java`
#### Snippet
```java
    private String _name = "";
    private int    _major = 1;
    private String _minor = "0";
    private String _description = "";
    private Compatibility _compatibility;
```

### UnusedAssignment
Variable `fm` initializer `null` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        throws SAXException {
        Object elem = currentElement();
        FieldMapping fm = null;
        if (elem instanceof ClassMapping)
            fm = getAttributeOverride((ClassMapping) elem);
```

### UnusedAssignment
Variable `tableName` initializer `DBIdentifier.newTable("?")` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        Unique unique = (Unique) popElement();
        Object ctx = currentElement();
        DBIdentifier tableName = DBIdentifier.newTable("?");
        if (ctx instanceof ClassMapping) {
        	ClassMappingInfo info = ((ClassMapping) ctx).getMappingInfo();
```

### UnusedAssignment
Variable `cls` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/SequenceMetaData.java`
#### Snippet
```java
            String clsName = plugin.getClassName();

            Class cls = null;
            try {
                cls = Class.forName(clsName, true,
```

### UnusedAssignment
Variable `codeOverride` initializer `JavaTypes.OBJECT` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueMetaDataImpl.java`
#### Snippet
```java

        // check for type extension
        int codeOverride = JavaTypes.OBJECT;
        if (_typeOverride != null) {
            codeOverride = JavaTypes.getTypeCode(_typeOverride);
```

### UnusedAssignment
Variable `is` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                    // a URL with a jar-formated InputStream, as per the JPA specification.  Otherwise, fall back
                    // to URLMetaDataIterator.
                    BufferedInputStream is = null;

                    try {
```

### UnusedAssignment
Variable `joins` initializer `null` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        ClassMappingInfo info = cm.getMappingInfo();

        List<Column> joins = null;
        for (SecondaryTable table : tables) {
            DBIdentifier sName = DBIdentifier.newTable(table.name(), delimit());
```

### UnusedAssignment
Variable `meta` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
        boolean mustExist) {
        Collection<ClassMetaData> mapped = new ArrayList<>(classes.size());
        ClassMetaData meta = null;
        for (Class<?> c : classes) {
            meta = getMetaData(c, envLoader, true);
```

### UnusedAssignment
Variable `_ct` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
    private Class<K> _keyType = null;
    private Class<V> _valueType = null;
    private MapChangeTrackerImpl _ct = null;
    private OpenJPAStateManager _sm = null;
    private int _field = -1;
```

### UnusedAssignment
Variable `_valueType` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java

    private Class<K> _keyType = null;
    private Class<V> _valueType = null;
    private MapChangeTrackerImpl _ct = null;
    private OpenJPAStateManager _sm = null;
```

### UnusedAssignment
Variable `_keyType` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
        (AbstractLRSProxyMap.class);

    private Class<K> _keyType = null;
    private Class<V> _valueType = null;
    private MapChangeTrackerImpl _ct = null;
```

### UnusedAssignment
Variable `lem` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfigurationImpl.java`
#### Snippet
```java
    @Override
    public LifecycleEventManager getLifecycleEventManagerInstance() {
        LifecycleEventManager lem = null;
        if (!getCompatibilityInstance().isSingletonLifecycleEventManager() ||
                (lem = (LifecycleEventManager)lifecycleEventManager.get()) == null) {
```

### UnusedAssignment
Variable `_elementType` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java
        (AbstractLRSProxyCollection.class);

    private Class _elementType = null;
    private CollectionChangeTrackerImpl _ct = null;
    private OpenJPAStateManager _sm = null;
```

### UnusedAssignment
Variable `_ct` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java

    private Class _elementType = null;
    private CollectionChangeTrackerImpl _ct = null;
    private OpenJPAStateManager _sm = null;
    private int _field = -1;
```

### UnusedAssignment
Variable `copy` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java

        Class<?> oidType = oid.getClass();
        Object copy = null;
        try {
            copy = AccessController.doPrivileged(
```

### UnusedAssignment
Variable `key` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
    public static boolean isIdSet(Object id, ClassMetaData meta,
        String mappedByIdFieldName) {
        Object key = null;
        if (meta.isOpenJPAIdentity())
            key = ApplicationIds.getKey(id, meta);
```

### UnusedAssignment
Variable `val` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
        else
            key = ((ObjectId)id).getId();
        Object val = null;
        if (mappedByIdFieldName.length() != 0) {
            if (((ObjectId)id).getId() == null)
```

### UnusedAssignment
Variable `copy` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
        if (Modifier.isAbstract(oidType.getModifiers()))
            throw new UserException(_loc.get("objectid-abstract", meta));
        Object copy = null;
        try {
            copy = AccessController.doPrivileged(
```

### UnusedAssignment
Variable `cls` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
        String clsName = ClassUtil.getPackageName(method);

        Class<?> cls = null;
        Class<?> owner = _owner.getDescribedType();

```

### UnusedAssignment
Variable `_decMeta` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
    private final String _name;
    private Class<?> _dec = null;
    private ClassMetaData _decMeta = null;
    private String _fullName = null;
    private String _embedFullName = null;
```

### UnusedAssignment
Variable `_dec` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
    private final ClassMetaData _owner;
    private final String _name;
    private Class<?> _dec = null;
    private ClassMetaData _decMeta = null;
    private String _fullName = null;
```

### UnusedAssignment
The value changed at `variableNr++` is never used
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            int retPos = -1;
            if (meth.getReturnType() != void.class) {
                retPos = variableNr++;
                mv.visitVarInsn(AsmHelper.getStoreInsn(meth.getReturnType()), retPos);
            }
```

### UnusedAssignment
The value changed at `pos++` is never used
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            params[pos++] = ret;
        if (beforeRet != void.class)
            params[pos++] = beforeRet;
        return params;
    }
```

### UnusedAssignment
Variable `bytes` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            final String proxyClassName = getProxyClassName(cls, false);

            byte[] bytes = null;

            if (Date.class.isAssignableFrom(cls)) {
```

### UnusedAssignment
Variable `proxyClass` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
    protected Class loadBuildTimeProxy(Class type, ClassLoader loader) {
        try {
            Class<?> proxyClass = null;
            if (_delayedCollectionLoading) {
                proxyClass = loadDelayedProxy(type);
```

### UnusedAssignment
Variable `bool` initializer `false` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedPriorityQueueProxy.java`
#### Snippet
```java
        }
        ProxyCollections.beforeAdd(this, paramObject);
        boolean bool = false;
        try {
            setDirectAccess(true);
```

### UnusedAssignment
Variable `_state` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SaveFieldManager.java`
#### Snippet
```java
    private BitSet _saved = null;
    private int[] _copyField = null;
    private transient PersistenceCapable _state = null;

    // used to track field value during store/fetch cycle
```

### UnusedAssignment
Variable `pcVersionInitField` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
        if (version != null
                && JavaTypes.isPrimitiveDefault(version, sm.getMetaData().getVersionField().getTypeCode())) {
            Field pcVersionInitField = null;
            try {
                pcVersionInitField = pc.getClass().getDeclaredField("pcVersionInit");
```

### UnusedAssignment
Variable `citr` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
    public Iterator<T> iterator() {
        _broker.assertNontransactionalRead();
        CloseableIterator citr = null;
        try {
            // create an iterator chain; add pnew objects if transactional
```

### UnusedAssignment
Variable `_ignore` initializer `false` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
    private final FetchConfiguration _fc;
    private final ReentrantLock _lock;
    private boolean _ignore = false;

    // set of open iterators
```

### UnusedAssignment
Variable `orig` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
        // if assigning oid, remove from new cache and put in primary; may
        // not be in new cache if another new instance had same id
        StateManagerImpl orig = null;
        if (_news != null) {
            orig = _news.remove(id);
```

### UnusedAssignment
Variable `exceps` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachManager.java`
#### Snippet
```java
            return null; // won't be reached as the exceps will be rethrown
        } finally {
            List exceps = null;
            if (excep == null || !_failFast)
                exceps = invokeAfterAttach(null);
```

### UnusedAssignment
Variable `_broker` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/LocalManagedRuntime.java`
#### Snippet
```java
        (LocalManagedRuntime.class);

    private Synchronization _broker = null;
    private Synchronization _factorySync = null;
    private boolean _active = false;
```

### UnusedAssignment
Variable `coll` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SingleFieldManager.java`
#### Snippet
```java
            throw new UserException(_loc.get("not-copyable",
                vmd.getFieldMetaData()));
        Collection coll = null;
        try {
            coll = getProxyManager().copyCollection(orig);
```

### UnusedAssignment
Variable `_flushed` initializer `false` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
    private final OpCallbacks _call;
    private final boolean _failFast;
    private boolean _flushed = false;
    private boolean _flushBeforeDetach;
    private boolean _cascadeWithDetach;
```

### UnusedAssignment
Variable `c2` initializer `(Collection) sm.newFieldProxy(fmd.getIndex())` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
            case JavaTypes.COLLECTION:
                ProxyDataList c = (ProxyDataList) data;
                Collection c2 = (Collection) sm.newFieldProxy(fmd.getIndex());
                c2 = toNestedFields(sm, fmd.getElement(), (Collection) data,
                    fetch, context);
```

### UnusedAssignment
Variable `val` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java

        FieldMetaData fmd = getMetaData().getField(field);
        Object val = null;
        if (fmd.getBackingMember() instanceof Field)
            val = Reflection.get(_oid, (Field) fmd.getBackingMember());
```

### UnusedAssignment
Variable `itr` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/MethodStoreQuery.java`
#### Snippet
```java
                    : Boolean.FALSE, null, paramMap, fetch };

                Iterator itr = null;
                Collection coll = q.getContext().getCandidateCollection();
                if (coll == null) {
```

### UnusedAssignment
Variable `getValue2` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
                        cls = cls.getSuperclass();
                    Object value2 = null;
                    Method getValue2 = null;
                    try {
                        getValue2 = cls.getMethod("getValue2");
```

### UnusedAssignment
Variable `tmpBroker` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CandidatePath.java`
#### Snippet
```java
        Object action;
        OpenJPAStateManager sm;
        Broker tmpBroker = null;
        for (Object o : _actions) {
            action = o;
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Param.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private Object _key = null;
    private Class _type = null;
    private int _index = -1;

```

### UnusedAssignment
Variable `_key` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Param.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private Object _key = null;
    private Class _type = null;
    private int _index = -1;
```

### UnusedAssignment
Variable `_fc` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

    // configuration for loading objects
    private FetchConfiguration _fc = null;
    private boolean _ignoreChanges = false;
    private Class<?> _resultMappingScope = null;
```

### UnusedAssignment
Variable `_printParameters` initializer `false` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        new ReferenceHashSet(AbstractReferenceMap.ReferenceStrength.WEAK);

    private boolean _printParameters = false;
    /**
     * Construct a query managed by the given broker.
```

### UnusedAssignment
Variable `_key` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CollectionParam.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private Object _key = null;
    private Class _type = null;
    private int _index = -1;
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CollectionParam.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private Object _key = null;
    private Class _type = null;
    private int _index = -1;

```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/UnboundVariable.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private Class _type = null;
    private Object _val = null;

```

### UnusedAssignment
Variable `_subqAlias` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/SubQ.java`
#### Snippet
```java

    private final String _alias;
    private String _subqAlias = null;
    private Class _type = null;

```

### UnusedAssignment
Variable `_single` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java

    // delegates when providing/replacing instance data
    private SingleFieldManager _single = null;
    private SaveFieldManager _saved = null;
    private FieldManager _fm = null;
```

### UnusedAssignment
Variable `_id` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
    // on successful oid assignment (or flush completion if assignment is
    // during flush)
    private Object _id = null;
    private Object _oid = null;

```

### UnusedAssignment
Variable `_meta` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
    // information about the instance
    private transient PersistenceCapable _pc = null;
    protected transient ClassMetaData _meta = null;
    protected BitSet _loaded = null;

```

### UnusedAssignment
Variable `clean` initializer `false` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
        boolean locked = false;
        boolean newFlush = false;
        boolean clean = false;
        try {
            FieldMetaData fmd = _meta.getField(field);
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/BoundVariable.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private Class _type = null;
    private Object _val = null;

```

### UnusedAssignment
Variable `var` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
        }

        Value var = null;
        if (bind)
            var = factory.newBoundVariable(id, type);
```

### UnusedAssignment
Variable `path` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        // the type will be the declared type for the field
        JPQLNode firstChild = firstChild(node);
        Path path = null;
        if (firstChild.id == JJTQUALIFIEDPATH)
            path = getQualifiedPath(firstChild);
```

### UnusedAssignment
Variable `thiz` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            // handle the case where the class name is the alias
            // for the candidate (we don't use variables for this)
            Value thiz = null;
            if (ctx().subquery == null ||
                ctx().getSchema(name.toLowerCase()) == null) {
```

### UnusedAssignment
Variable `path` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        // i.e., the path "SELECT x.id FROM SomeClass x where x.id > 10"
        // will need to have "x" in the alias map in order to resolve
        Path path = null;

        final String name = firstChild(node).text;
```

### UnusedAssignment
Variable `inEnumPath` initializer `false` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        String text;
        boolean not = false;
        boolean inEnumPath = false;

        public JPQLNode(JPQL parser, int id) {
```

### UnusedAssignment
Variable `val1` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
     */
    private Object eval(JPQLNode node) {
        Value val1 = null;
        Value val2 = null;
        Value val3 = null;
```

### UnusedAssignment
Variable `val2` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
    private Object eval(JPQLNode node) {
        Value val1 = null;
        Value val2 = null;
        Value val3 = null;

```

### UnusedAssignment
Variable `defaultIns` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/DynamicStorageGenerator.java`
#### Snippet
```java
        tabins.setLow(0);
        tabins.setHigh(types.length - 1);
        Instruction defaultIns = null;
        if (typeCode == JavaTypes.OBJECT && handle == POLICY_SILENT) {
            defaultIns = code.constant().setNull();
```

### UnusedAssignment
Variable `_meta` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/CodeGenerator.java`
#### Snippet
```java
    private File _dir = null;
    private CodeFormat _format = null;
    private ClassMetaData _meta = null;
    private Class _type = null;
    private ParameterTemplate _code = null;
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/CodeGenerator.java`
#### Snippet
```java
    private CodeFormat _format = null;
    private ClassMetaData _meta = null;
    private Class _type = null;
    private ParameterTemplate _code = null;

```

### UnusedAssignment
Variable `tmin` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ClearableScheduler.java`
#### Snippet
```java

        public Schedule(String date) {
            int[] tmin = null;
            if (date.startsWith("+")) {
                Calendar cal = Calendar.getInstance();
```

### UnusedAssignment
Variable `args` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
     */
    private Code addLoadMethod(BCClass bc, boolean fields) {
        Class<?>[] args = null;
        if (fields)
            args = new Class[]{ OpenJPAStateManager.class, BitSet.class,
```

### UnusedAssignment
Variable `keys` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
        if (evictPolicy == EvictPolicy.DEFAULT) {
            writeLock();
            Collection keys = null;
            try {
                if (hasListeners())
```

### UnusedAssignment
Variable `res` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryCacheStoreQuery.java`
#### Snippet
```java
                    // make sure we didn't abort
                    if (_maintainCache) {
                        QueryResult res = null;
                        synchronized (this) {
                            res = new QueryResult(_qk, _data.values());
```

### UnusedAssignment
Variable `_platform` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/abstractstore/AbstractStoreBrokerFactory.java`
#### Snippet
```java
    private String _storeCls = null;
    private String _storeProps = null;
    private String _platform = null;

    /**
```

### UnusedAssignment
Variable `_storeProps` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/abstractstore/AbstractStoreBrokerFactory.java`
#### Snippet
```java

    private String _storeCls = null;
    private String _storeProps = null;
    private String _platform = null;

```

### UnusedAssignment
Variable `_storeCls` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/abstractstore/AbstractStoreBrokerFactory.java`
#### Snippet
```java
        (AbstractStoreBrokerFactory.class);

    private String _storeCls = null;
    private String _storeProps = null;
    private String _platform = null;
```

### UnusedAssignment
Variable `smList` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java

        Map<OpenJPAStateManager, BitSet> unloaded = null;
        List<OpenJPAStateManager> smList = null;
        Map<DataCache,List<OpenJPAStateManager>> caches = new HashMap<>();
        DataCache cache;
```

### UnusedAssignment
Variable `found` initializer `false` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
        DataCache cache = _mgr.selectCache(sm);

        boolean found = false;
        int loadedFieldsBefore = sm.getLoaded().cardinality();
        if (cache == null || sm.isEmbedded() || bypass(fetch, StoreManager.FORCE_LOAD_NONE)) {
```

### UnusedAssignment
Variable `dc` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/DataCacheJMXInstrument.java`
#### Snippet
```java
        OpenJPAConfiguration conf = (OpenJPAConfiguration)getProvider().getConfiguration();
        DataCacheManager dcm = conf.getDataCacheManagerInstance();
        DataCache dc = null;
        if (cacheName == null || cacheName.trim().length() == 0) {
            dc = dcm.getSystemDataCache();
```

### UnusedAssignment
Variable `l` initializer `0` is redundant
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
                byte[] b = new byte[1024];
                int i = 0;
                for (int l = 0; (l = in.read(b)) != -1;) {
                    out.write(b, 0, l);
                    i += l;
```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
                _response.setContentLength(i);
            } else {
                for (int c = 0; (c = in.read()) != -1;) {
                    out.write((char)c);
                }
```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/TokenReplacedStream.java`
#### Snippet
```java

        StringBuilder tmp = new StringBuilder();
        for (int c = 0; (c = in.read()) != -1;) {
            int cursor = match((char)c, patterns);
            if (cursor < 0) { // no pattern recognized at all
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObjectFormatter.java`
#### Snippet
```java
     */
    String streamToString(Object value) {
        Reader reader = null;
        if (value instanceof InputStream) {
            reader = new BufferedReader(new InputStreamReader((InputStream)value));
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/XMLFormatter.java`
#### Snippet
```java
     */
    private String streamToString(Object value) {
        Reader reader = null;
        if (value instanceof InputStream) {
            reader = new BufferedReader(new InputStreamReader((InputStream)value));
```

### UnusedAssignment
Variable `_total` initializer `100` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
    class TaskProgress extends SwingWorker<Void, Integer> {
        private long startTimeInMillis;
        private long _total = 100;
        private int _interval = 100;

```

### UnusedAssignment
Variable `_interval` initializer `100` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
        private long startTimeInMillis;
        private long _total = 100;
        private int _interval = 100;

        public TaskProgress(long total, int interval) {
```

### UnusedAssignment
Variable `p2` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/MetamodelView.java`
#### Snippet
```java
                    continue;
                Point p1 = path.get(0);
                Point p2 = null;
                for (int i = 1; i < path.size(); i++) {
                    p2 = path.get(i);
```

### UnusedAssignment
Variable `next` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SwingHelper.java`
#### Snippet
```java
        int i = unit.ordinal();
        TimeUnit[] units = TimeUnit.values();
        TimeUnit next = null;
        int factor = -1;
        if (i < factors.length -1) {
```

### UnusedAssignment
Variable `factor` initializer `-1` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SwingHelper.java`
#### Snippet
```java
        TimeUnit[] units = TimeUnit.values();
        TimeUnit next = null;
        int factor = -1;
        if (i < factors.length -1) {
            next = units[i+1];
```

### UnusedAssignment
Variable `getter` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/MetamodelHelper.java`
#### Snippet
```java
    public static Object getValue(Attribute<?,?> attr, Object instance) {
        Member member = attr.getJavaMember();
        Method getter = null;
        if (member instanceof Method) {
            getter = (Method)member;
```

### UnusedAssignment
Variable `column` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTable.java`
#### Snippet
```java
    private void initColumnSizes() {
        TableModel model   = getModel();
        TableColumn column = null;
        Component comp     = null;
        int headerWidth    = 0;
```

### UnusedAssignment
Variable `comp` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTable.java`
#### Snippet
```java
        TableModel model   = getModel();
        TableColumn column = null;
        Component comp     = null;
        int headerWidth    = 0;

```

### UnusedAssignment
Variable `headerWidth` initializer `0` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTable.java`
#### Snippet
```java
        TableColumn column = null;
        Component comp     = null;
        int headerWidth    = 0;

        TableCellRenderer headerRenderer =  getTableHeader().getDefaultRenderer();
```

### UnusedAssignment
Variable `renderer` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTable.java`
#### Snippet
```java
    public TableCellRenderer getCellRenderer(int row, int column) {
        Attribute<?,?> attr = ((EntityDataModel)getModel()).getAttribute(column);
        TableCellRenderer renderer = null;
        if (attr == null) {
            renderer = super.getCellRenderer(row, column);
```

### UnusedAssignment
Variable `styleFile` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/Java2HTMLConverter.java`
#### Snippet
```java
    @Override
    public TokenRenderer createRenderer(CommandProcessor options, File outFile) {
        File styleFile = null;
        styleFile = new File(getDestinationDirectory(), options.getValue("-stylesheet"));
        if (!styleFile.exists()) {
```

### UnusedAssignment
Variable `result` initializer `""` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/HTMLTokenRenderer.java`
#### Snippet
```java
    public String render(int decision, Token token) {
        String text = token.getText();
        String result = "";
        int type = token.getType();
        switch (type) {
```

### UnusedAssignment
Variable `code` initializer `null` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            (String[]) null);
        String superName = _managedType.getSuperclassName();
        Code code = null;
        if (clone == null) {
            // add clone support for base classes
```

### UnusedAssignment
Variable `fis` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/ant/ReverseMappingToolTask.java`
#### Snippet
```java
        if (propsFile != null && AccessController.doPrivileged(
                J2DoPrivHelper.existsAction(propsFile))) {
            FileInputStream fis = null;
            try {
                fis = AccessController.doPrivileged(
```

### UnusedAssignment
Variable `customer` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
        CriteriaBuilder cb = em.getCriteriaBuilder();
        CriteriaQuery<Customer> q = cb.createQuery(Customer.class);
        Customer customer = null;
        Root<Customer> root = q.from(Customer.class);
        ParameterExpression<String> pName = cb.parameter(String.class);
```

### UnusedAssignment
Variable `_pk` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PrimaryRow.java`
#### Snippet
```java
        (PrimaryRow.class);

    private OpenJPAStateManager _pk = null;
    private ColumnIO _pkIO = null;
    private OpenJPAStateManager[] _fkSet = null;
```

### UnusedAssignment
The value `"FLOAT"` assigned to `floatTypeName` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
        smallintTypeName = "INT8";
        tinyintTypeName = "INT8";
        floatTypeName = "FLOAT";
        bitTypeName = "BOOLEAN";
        blobTypeName = "BYTE";
```

### UnusedAssignment
The value `"NUMERIC(32,20)"` assigned to `doubleTypeName` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
        bitTypeName = "BOOLEAN";
        blobTypeName = "BYTE";
        doubleTypeName = "NUMERIC(32,20)";
        dateTypeName = "DATE";
        timeTypeName = "DATETIME HOUR TO SECOND";
```

### UnusedAssignment
The value `"NUMERIC(32,20)"` assigned to `doubleTypeName` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
        timeTypeName = "DATETIME HOUR TO SECOND";
        timestampTypeName = "DATETIME YEAR TO FRACTION(3)";
        doubleTypeName = "NUMERIC(32,20)";
        floatTypeName = "REAL";
        bigintTypeName = "NUMERIC(32,0)";
```

### UnusedAssignment
Variable `swapSchemaAndCatalog` initializer `true` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
     * value for this property indicates that they should be reversed.
     */
    public boolean swapSchemaAndCatalog = true;

    protected boolean useJCC = false;
```

### UnusedAssignment
Variable `_channel` initializer `DEFAULT_TOKEN_CHANNEL` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        try {
            int _type = WS;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:1011:5: ( ( ' ' | '\\r' | '\\t' | '\\u000C' | '\\n' ) )
            // Java.g:1011:8: ( ' ' | '\\r' | '\\t' | '\\u000C' | '\\n' )
```

### UnusedAssignment
Variable `alt29` initializer `101` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public void mTokens() throws RecognitionException {
        // Java.g:1:8: ( T__25 | T__26 | T__27 | T__28 | T__29 | T__30 | T__31 | T__32 | T__33 | T__34 | T__35 | T__36 | T__37 | T__38 | T__39 | T__40 | T__41 | T__42 | T__43 | T__44 | T__45 | T__46 | T__47 | T__48 | T__49 | T__50 | T__51 | T__52 | T__53 | T__54 | T__55 | T__56 | T__57 | T__58 | T__59 | T__60 | T__61 | T__62 | T__63 | T__64 | T__65 | T__66 | T__67 | T__68 | T__69 | T__70 | T__71 | T__72 | T__73 | T__74 | T__75 | T__76 | T__77 | T__78 | T__79 | T__80 | T__81 | T__82 | T__83 | T__84 | T__85 | T__86 | T__87 | T__88 | T__89 | T__90 | T__91 | T__92 | T__93 | T__94 | T__95 | T__96 | T__97 | T__98 | T__99 | T__100 | T__101 | T__102 | T__103 | T__104 | T__105 | T__106 | T__107 | T__108 | T__109 | T__110 | T__111 | T__112 | T__113 | HexLiteral | DecimalLiteral | OctalLiteral | FloatingPointLiteral | CharacterLiteral | StringLiteral | ENUM | ASSERT | Identifier | WS | COMMENT | LINE_COMMENT )
        int alt29=101;
        alt29 = dfa29.predict(input);
        switch (alt29) {
```

### UnusedAssignment
Variable `alt21` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            match('\'');
            // Java.g:935:14: ( EscapeSequence | ~ ( '\\'' | '\\\\' ) )
            int alt21=2;
            int LA21_0 = input.LA(1);

```

### UnusedAssignment
Variable `_channel` initializer `DEFAULT_TOKEN_CHANNEL` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        try {
            int _type = COMMENT;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:1015:5: ( '/*' ( options {greedy=false; } : . )* '*/' )
            // Java.g:1015:9: '/*' ( options {greedy=false; } : . )* '*/'
```

### UnusedAssignment
Variable `alt23` initializer `3` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        try {
            // Java.g:944:5: ( '\\\\' ( 'b' | 't' | 'n' | 'f' | 'r' | '\\\"' | '\\'' | '\\\\' ) | UnicodeEscape | OctalEscape )
            int alt23=3;
            int LA23_0 = input.LA(1);

```

### UnusedAssignment
Variable `alt4` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            {
            // Java.g:911:18: ( '0' | '1' .. '9' ( '0' .. '9' )* )
            int alt4=2;
            int LA4_0 = input.LA(1);

```

### UnusedAssignment
Variable `alt24` initializer `3` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        try {
            // Java.g:951:5: ( '\\\\' ( '0' .. '3' ) ( '0' .. '7' ) ( '0' .. '7' ) | '\\\\' ( '0' .. '7' ) ( '0' .. '7' ) | '\\\\' ( '0' .. '7' ) )
            int alt24=3;
            int LA24_0 = input.LA(1);

```

### UnusedAssignment
Variable `_channel` initializer `DEFAULT_TOKEN_CHANNEL` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        try {
            int _type = LINE_COMMENT;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:1019:5: ( '//' (~ ( '\\n' | '\\r' ) )* ( '\\r' )? '\\n' )
            // Java.g:1019:7: '//' (~ ( '\\n' | '\\r' ) )* ( '\\r' )? '\\n'
```

### UnusedAssignment
Variable `alt18` initializer `4` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:922:5: ( ( '0' .. '9' )+ '.' ( '0' .. '9' )* ( Exponent )? ( FloatTypeSuffix )? | '.' ( '0' .. '9' )+ ( Exponent )? ( FloatTypeSuffix )? | ( '0' .. '9' )+ Exponent ( FloatTypeSuffix )? | ( '0' .. '9' )+ FloatTypeSuffix )
            int alt18=4;
            alt18 = dfa18.predict(input);
            switch (alt18) {
```

### UnusedAssignment
The value `false` assigned to `supportsLockingWithDistinctClause` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PointbaseDictionary.java`
#### Snippet
```java
        requiresAliasForSubselect = true;

        supportsLockingWithDistinctClause = false;
        supportsLockingWithMultipleTables = false;
        supportsLockingWithDistinctClause = false;
```

### UnusedAssignment
Variable `asString` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        for (Iterator itr = aliases.iterator(); itr.hasNext(); i++) {
            alias = itr.next();
            String asString = null;
            if (alias instanceof SQLBuffer) {
                asString = ((SQLBuffer) alias).getSQL();
```

### UnusedAssignment
Variable `oracleBlob_empty_lob_Method` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
    private Field oraclePreparedStatementFormNvarcharField = null;
    private Method oracleClob_empty_lob_Method = null;
    private Method oracleBlob_empty_lob_Method = null;
    private Method oracleClob_isEmptyLob_Method = null;

```

### UnusedAssignment
The value changed at `idx++` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                setString(stmnt, idx++, convertSchemaCase(schemaName), null);
            if (!DBIdentifier.isNull(tableName))
                setString(stmnt, idx++, convertSchemaCase(tableName), null);

            setTimeouts(stmnt, conf, false);
```

### UnusedAssignment
Variable `oracleClob_isEmptyLob_Method` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
    private Method oracleClob_empty_lob_Method = null;
    private Method oracleBlob_empty_lob_Method = null;
    private Method oracleClob_isEmptyLob_Method = null;


```

### UnusedAssignment
Variable `oracleClob_empty_lob_Method` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    private Field oraclePreparedStatementFormNvarcharField = null;
    private Method oracleClob_empty_lob_Method = null;
    private Method oracleBlob_empty_lob_Method = null;
    private Method oracleClob_isEmptyLob_Method = null;
```

### UnusedAssignment
The value changed at `idx++` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            }
            if (!DBIdentifier.isNull(tableName)) {
                setString(stmnt, idx++, convertSchemaCase(tableName.getUnqualifiedName()), null);
            }
            setTimeouts(stmnt, conf, false);
```

### UnusedAssignment
The value changed at `idx++` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                setString(stmnt, idx++, convertSchemaCase(schemaName), null);
            if (!DBIdentifier.isNull(tableName))
                setString(stmnt, idx++, convertSchemaCase(tableName), null);
            setTimeouts(stmnt, conf, false);
            rs = stmnt.executeQuery();
```

### UnusedAssignment
Variable `delegate` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
     */
    protected Connection getDbcpDelegate(Connection conn) {
        Connection delegate = null;
        try {
            if (dbcpGetDelegate == null) {
```

### UnusedAssignment
Variable `val` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/ResultSetResult.java`
#### Snippet
```java
        obj = translate(obj, joins);

        Object val = null;
        switch (metaTypeCode) {
            case JavaTypes.BOOLEAN:
```

### UnusedAssignment
Variable `set` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            }
        } else {
            JoinSet set = null;
            if (j1.getSelect() == this) {
                if (nullJoins)
```

### UnusedAssignment
Variable `join` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        if (!buf.isEmpty())
            buf.append(" AND ");
        Join join = null;
        for (Iterator itr = ((PathJoins) joins).joins().joinIterator();
            itr.hasNext();) {
```

### UnusedAssignment
Variable `_joinSyntax` initializer `0` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    // flags
    private int _flags = 0;
    private int _joinSyntax = 0;
    private long _startIdx = 0;
    private long _endIdx = Long.MAX_VALUE;
```

### UnusedAssignment
Variable `j` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            if (_joins == null)
                return;
           Join j = null;
           List<Join> removed = new ArrayList<>(5);
           for (Iterator itr = _joins.iterator(); itr.hasNext();) {
```

### UnusedAssignment
Variable `i` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    private Integer findAliasForQuery(Table table, PathJoins pj, Object key,
        boolean create) {
        Integer i = null;
        SelectImpl sel = this;
        String alias = _schemaAlias;
```

### UnusedAssignment
Variable `rs` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        Connection conn = store.getConnection();
        PreparedStatement stmnt = null;
        ResultSet rs = null;
        try {
            if (isLRS)
```

### UnusedAssignment
Variable `create` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
            buf.append("DISK");

        String[] create = null;
        if (lockingMode != null) {
            StringBuilder buf1 = new StringBuilder();
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/MappingFactoryValue.java`
#### Snippet
```java
        // if no mapping setting, return meta factory alone, assuming it handles
        // both metadata and mapping
        MetaDataFactory ret = null;
        if(map == null ) {
            ret = meta;
```

### UnusedAssignment
Variable `dict` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionaryFactory.java`
#### Snippet
```java
    private static DBDictionary newDBDictionary(JDBCConfiguration conf,
        String dclass, String props, Connection conn) {
        DBDictionary dict = null;
        try {
            Class<?> c = Class.forName(dclass, true,
```

### UnusedAssignment
Variable `sync` initializer `false` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/DiscriminatorMappingInfo.java`
#### Snippet
```java
        ClassMapping cls = disc.getClassMapping();
        String strat = disc.getStrategy().getAlias();
        boolean sync = false;

        if (!cls.isMapped()
```

### UnusedAssignment
Variable `fstring` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
    @Override
    public String addCastAsType(String func, Val val) {
        String fstring = null;
        String type = getTypeName(getJDBCType(JavaTypes.getTypeCode(val
            .getType()), false));
```

### UnusedAssignment
Variable `ds` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfigurationImpl.java`
#### Snippet
```java
    public DataSource getDataSource2(StoreContext ctx) {
        Log log = getLog(LOG_RUNTIME);
        DataSource ds = null;

        // Try to obtain from the StoreContext first.
```

### UnusedAssignment
Variable `ds` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfigurationImpl.java`
#### Snippet
```java
    public DataSource getDataSource(StoreContext ctx) {
        Log log = getLog(LOG_RUNTIME);
        DataSource ds = null;

        if(ctx != null && StringUtil.isNotEmpty(ctx.getConnectionFactoryName())) {
```

### UnusedAssignment
Variable `read` initializer `-1` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        char[] copyBuffer = new char[clobBufferSize];
        long bytesCopied = 0;
        int read = -1;

        while ((read = reader.read(copyBuffer, 0, copyBuffer.length)) != -1) {
```

### UnusedAssignment
Variable `sqlSchemaName` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            return null;

        String sqlSchemaName = null;
        if (!DBIdentifier.isNull(schemaName)) {
            sqlSchemaName = schemaName.getName();
```

### UnusedAssignment
The value `schemaName.getName()` assigned to `sqlSchemaName` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        String sqlSchemaName = null;
        if (!DBIdentifier.isNull(schemaName)) {
            sqlSchemaName = schemaName.getName();
        }
        if (!supportsSchemaForGetColumns)
```

### UnusedAssignment
Variable `schemaName` initializer `DBIdentifier.isNull(sqlSchemaName) ? null : sqlSchemaName.getName()` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        throws SQLException {

        String schemaName = DBIdentifier.isNull(sqlSchemaName) ? null : sqlSchemaName.getName();
        if (!supportsSchemaForGetTables)
            schemaName = null;
```

### UnusedAssignment
Variable `read` initializer `-1` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        byte[] copyBuffer = new byte[blobBufferSize];
        long bytesCopied = 0;
        int read = -1;

        while ((read = in.read(copyBuffer, 0, copyBuffer.length)) != -1) {
```

### UnusedAssignment
The value changed at `idx++` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                stmnt.setString(idx++, DBIdentifier.toUpper(schemaName).getName());
            if (!DBIdentifier.isNull(sequenceName))
                stmnt.setString(idx++, sequenceName.getName());
            setQueryTimeout(stmnt, conf.getQueryTimeout());
            rs = executeQuery(conn, stmnt, str);
```

### UnusedAssignment
Variable `curIdx` initializer `-1` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            String s;
            idx = typeName.length();
            int curIdx = -1;
            for (String value : typeModifierSet) {
                s = value;
```

### UnusedAssignment
Variable `classes` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
        // collect the classes to act on
        Log log = conf.getLog(OpenJPAConfiguration.LOG_TOOL);
        Collection<Class<?>> classes = null;
        if (args.length == 0) {
            if (ACTION_IMPORT.equals(flags.action))
```

### UnusedAssignment
Variable `fis` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        if (customFile != null && AccessController.doPrivileged(
                J2DoPrivHelper.existsAction(customFile))) {
            FileInputStream fis = null;
            try {
                fis = AccessController.doPrivileged(
```

### UnusedAssignment
Variable `fullJoin` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
        DBIdentifier best = tableName;
        int pts = 0;
        DBIdentifier fullJoin = DBIdentifier.NULL;
        DBIdentifier join = DBIdentifier.NULL;
        for (DBIdentifier dbIdentifier : _seconds.keySet()) {
```

### UnusedAssignment
Variable `join` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
        int pts = 0;
        DBIdentifier fullJoin = DBIdentifier.NULL;
        DBIdentifier join = DBIdentifier.NULL;
        for (DBIdentifier dbIdentifier : _seconds.keySet()) {
            // award a caseless match without schema 2 points
```

### UnusedAssignment
Variable `row` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/FieldMapping.java`
#### Snippet
```java
        int action)
        throws SQLException {
        Row row = null;
        boolean newOuterRow = false;
        if (_fk != null && _outer && action != Row.ACTION_DELETE) {
```

### UnusedAssignment
Variable `name` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
            return null;

        DBIdentifier name = DBIdentifier.NULL;
        boolean deferred;
        if (_unq != null) {
```

### UnusedAssignment
Variable `joinFK` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationRelationMapTableFieldStrategy.java`
#### Snippet
```java
            @Override
            public void select(Select sel, int idx) {
                ForeignKey joinFK = null;
                if (field.isUni1ToMFK()) {
                    ValueMapping val = field.getElementMapping();
```

### UnusedAssignment
Variable `cls` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/UntypedPCValueHandler.java`
#### Snippet
```java
            getClassResolverInstance().getClassLoader(vm.getType(),
            ctx.getClassLoader());
        Class cls = null;
        try {
            cls = Class.forName(clsName, true, loader);
```

### UnusedAssignment
Variable `owner` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbeddedClassStrategy.java`
#### Snippet
```java
        info.assertNoSchemaComponents(cls, true);

        ClassMapping owner = null;
        if (vm.getValueMappedByMapping() != null)
            owner = vm.getValueMappedByMapping().getDefiningMapping();
```

### UnusedAssignment
Variable `row` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedFieldStrategy.java`
#### Snippet
```java
        boolean newVal = em == null || em.getPCState() == PCState.ECOPY;

        Row row = null;
        if (newVal && field.getJoinForeignKey() != null
            && field.isJoinOuter()) {
```

### UnusedAssignment
Variable `mappedByValue` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StoreCollectionFieldStrategy.java`
#### Snippet
```java
        // for inverseEager field
        FieldMapping mappedByFieldMapping = field.getMappedByMapping();
        PersistenceCapable mappedByValue = null;

        if (mappedByFieldMapping != null) {
```

### UnusedAssignment
The value `null` assigned to `stmnt` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
                        // safe to ignore
                    } finally {
                        stmnt = null;
                    }
                }
```

### UnusedAssignment
Variable `count` initializer `0` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
            PreparedStatement stmnt, SQLBuffer buf)
            throws SQLException {
            int count = 0;
            if (_call && !stmnt.execute()) {
                count = stmnt.getUpdateCount();
```

### UnusedAssignment
Variable `mappedByValue` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
        // for inverseEager field
        FieldMapping mappedByFieldMapping = field.getMappedByMapping();
        PersistenceCapable mappedByValue = null;

        if (mappedByFieldMapping != null) {
```

### UnusedAssignment
Variable `pkCols` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
        ClassMetaData embeddedId = ((ValueMappingImpl)pk.getValue()).
            getEmbeddedMetaData();
        Column[] pkCols = null;
        List cols = new ArrayList();
        String mappedByIdValue = field.getMappedByIdValue();
```

### UnusedAssignment
Variable `secondaryRow` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
                PersistenceCapable invPC = (PersistenceCapable)sm.fetchObject(
                    field.getBi_1ToM_JTField().getIndex());
                Row secondaryRow = null;
                if (invPC != null) {
                    secondaryRow = rm.getSecondaryRow(field.getBi1ToMJoinFK().getTable(),
```

### UnusedAssignment
Variable `tableName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
    public DBIdentifier resolveTableIdentifier(ClassMapping mapping, Table table) {
        DBIdentifier sName = mapping.getTable().getSchemaIdentifier();
        DBIdentifier tableName = DBIdentifier.NULL;

        //OPENJPA-2650: Don't use a schema name if the user has requested,
```

### UnusedAssignment
Variable `stat` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
        JDBCStore store = null;
        ClassMapping mapping = null;
        Status stat = null;
        int alloc;
        boolean updateStatSeq;
```

### UnusedAssignment
Variable `mapping` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java

        JDBCStore store = null;
        ClassMapping mapping = null;
        Status stat = null;
        int alloc;
```

### UnusedAssignment
Variable `tableName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
     */
    private void buildTable() {
        DBIdentifier tableName = DBIdentifier.NULL;
        DBIdentifier schemaName = DBIdentifier.NULL;
        QualifiedDBIdentifier path = QualifiedDBIdentifier.getPath(_table);
```

### UnusedAssignment
Variable `store` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
    protected class AllocateSequenceRunnable implements Runnable {

        JDBCStore store = null;
        ClassMapping mapping = null;
        Status stat = null;
```

### UnusedAssignment
Variable `val` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedStatementManagerImpl.java`
#### Snippet
```java
        OpenJPAStateManager sm = row.getPrimaryKey();
        ClassMapping mapping = (ClassMapping) sm.getMetaData();
        Object val = null;
        for (int i = 0; i < autoAssign.length; i++) {
            if (_dict.supportsGetGeneratedKeys && vals != null &&
```

### UnusedAssignment
Variable `cnt` initializer `0` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
        // DB2/ZOS        1 / 0           1 / 0        -2 / SQLException
        // Oracle        -2 / -2         -2 / -2       -2 / SQLException
        int cnt = 0;
        int updateSuccessCnt = _dict.getBatchUpdateCount(ps);
        Object failed = null;
```

### UnusedAssignment
Variable `rs` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderQueryImpl.java`
#### Snippet
```java
        DBDictionary dict = jstore.getDBDictionary();
        PreparedStatement stmnt = null;
        ResultSet rs = null;
        try {
            stmnt = _select.prepareStatement(conn, _sql);
```

### UnusedAssignment
Variable `failed` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
        int cnt = 0;
        int updateSuccessCnt = _dict.getBatchUpdateCount(ps);
        Object failed = null;
        List<RowImpl> batchedRows = getBatchedRows();
        for (int i = 0; i < count.length; i++) {
```

### UnusedAssignment
Variable `onerow` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
            PreparedStatement ps = null;
            try {
                RowImpl onerow = null;
                ps = prepareStatement(batchedSql);
                if (batchSize == 1) {
```

### UnusedAssignment
Variable `id` initializer `0` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
    public Object evaluate(Object value, Object ob, Object[] params,
        OpenJPAStateManager sm) {
        int id = 0;
        if (value instanceof org.apache.openjpa.jdbc.kernel.exps.Val)
            id = ((org.apache.openjpa.jdbc.kernel.exps.Val)value).getId();
```

### UnusedAssignment
Variable `tmpBroker` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/ConstPath.java`
#### Snippet
```java
        Object action;
        OpenJPAStateManager sm;
        Broker tmpBroker = null;
        for (Object o : _actions) {
            // fail on null value
```

### UnusedAssignment
Variable `mapping` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/CollectionParam.java`
#### Snippet
```java
        public Object[] sqlValue = null;
        public int[] otherLength;
        public ClassMapping[] mapping = null;
        public Discriminator[] disc = null;
        public Object discValue[] = null;
```

### UnusedAssignment
Variable `sqlValue` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/CollectionParam.java`
#### Snippet
```java

        public int size = 0;
        public Object[] sqlValue = null;
        public int[] otherLength;
        public ClassMapping[] mapping = null;
```

### UnusedAssignment
Variable `disc` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/CollectionParam.java`
#### Snippet
```java
        public int[] otherLength;
        public ClassMapping[] mapping = null;
        public Discriminator[] disc = null;
        public Object discValue[] = null;

```

### UnusedAssignment
Variable `discValue` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/CollectionParam.java`
#### Snippet
```java
        public ClassMapping[] mapping = null;
        public Discriminator[] disc = null;
        public Object discValue[] = null;

        ParamExpState(Object params) {
```

### UnusedAssignment
Variable `pos` initializer `0` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ValueMappingInfo.java`
#### Snippet
```java
            return;

        int pos = 0;
        Column cur = cols.get(0);
        Column next;
```

### UnusedAssignment
Variable `alt69` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 53) ) { return ; }
            // Java.g:437:2: ( classOrInterfaceType ( '[' ']' )* | primitiveType ( '[' ']' )* )
            int alt69=2;
            try { dbg.enterDecision(69);

```

### UnusedAssignment
Variable `alt12` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            dbg.location(199,35);
            // Java.g:199:35: ( classDeclaration | interfaceDeclaration )
            int alt12=2;
            try { dbg.enterSubRule(12);
            try { dbg.enterDecision(12);
```

### UnusedAssignment
Variable `alt39` initializer `3` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 24) ) { return ; }
            // Java.g:287:5: ( ';' | ( 'static' )? block | modifiers memberDecl )
            int alt39=3;
            try { dbg.enterDecision(39);

```

### UnusedAssignment
Variable `alt114` initializer `16` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 90) ) { return ; }
            // Java.g:619:5: ( block | ASSERT expression ( ':' expression )? ';' | 'if' parExpression statement ( options {k=1; } : 'else' statement )? | 'for' '(' forControl ')' statement | 'while' parExpression statement | 'do' statement 'while' parExpression ';' | 'try' block ( catches 'finally' block | catches | 'finally' block ) | 'switch' parExpression '{' switchBlockStatementGroups '}' | 'synchronized' parExpression block | 'return' ( expression )? ';' | 'throw' expression ';' | 'break' ( Identifier )? ';' | 'continue' ( Identifier )? ';' | ';' | statementExpression ';' | Identifier ':' statement )
            int alt114=16;
            try { dbg.enterDecision(114);

```

### UnusedAssignment
Variable `alt110` initializer `3` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    dbg.location(626,9);
                    // Java.g:626:9: ( catches 'finally' block | catches | 'finally' block )
                    int alt110=3;
                    try { dbg.enterSubRule(110);
                    try { dbg.enterDecision(110);
```

### UnusedAssignment
Variable `alt32` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 19) ) { return ; }
            // Java.g:266:5: ( normalInterfaceDeclaration | annotationTypeDeclaration )
            int alt32=2;
            try { dbg.enterDecision(32);

```

### UnusedAssignment
Variable `t1` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
    public final void relationalOp() throws RecognitionException {
        int relationalOp_StartIndex = input.index();
        Token t1=null;
        Token t2=null;

```

### UnusedAssignment
Variable `t2` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        int relationalOp_StartIndex = input.index();
        Token t1=null;
        Token t2=null;

        try { dbg.enterRule(getGrammarFileName(), "relationalOp");
```

### UnusedAssignment
Variable `alt137` initializer `4` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 116) ) { return ; }
            // Java.g:778:5: ( ( '<' '=' )=>t1= '<' t2= '=' {...}? | ( '>' '=' )=>t1= '>' t2= '=' {...}? | '<' | '>' )
            int alt137=4;
            try { dbg.enterDecision(137);

```

### UnusedAssignment
Variable `alt45` initializer `5` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 32) ) { return ; }
            // Java.g:328:5: ( interfaceMethodOrFieldDecl | interfaceGenericMethodDecl | 'void' Identifier voidInterfaceMethodDeclaratorRest | interfaceDeclaration | classDeclaration )
            int alt45=5;
            try { dbg.enterDecision(45);

```

### UnusedAssignment
Variable `alt11` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 4) ) { return ; }
            // Java.g:194:5: ( classOrInterfaceDeclaration | ';' )
            int alt11=2;
            try { dbg.enterDecision(11);

```

### UnusedAssignment
Variable `alt102` initializer `5` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 80) ) { return ; }
            // Java.g:570:5: ( type annotationMethodOrConstantRest ';' | normalClassDeclaration ( ';' )? | normalInterfaceDeclaration ( ';' )? | enumDeclaration ( ';' )? | annotationTypeDeclaration ( ';' )? )
            int alt102=5;
            try { dbg.enterDecision(102);

```

### UnusedAssignment
Variable `alt156` initializer `8` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 125) ) { return ; }
            // Java.g:847:5: ( ( '[' ']' )+ '.' 'class' | ( '[' expression ']' )+ | arguments | '.' 'class' | '.' explicitGenericInvocation | '.' 'this' | '.' 'super' arguments | '.' 'new' innerCreator )
            int alt156=8;
            try { dbg.enterDecision(156);

```

### UnusedAssignment
Variable `alt155` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    loop155:
                    do {
                        int alt155=2;
                        try { dbg.enterDecision(155);

```

### UnusedAssignment
Variable `alt158` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 126) ) { return ; }
            // Java.g:858:5: ( nonWildcardTypeArguments createdName classCreatorRest | createdName ( arrayCreatorRest | classCreatorRest ) )
            int alt158=2;
            try { dbg.enterDecision(158);

```

### UnusedAssignment
Variable `alt157` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    dbg.location(859,21);
                    // Java.g:859:21: ( arrayCreatorRest | classCreatorRest )
                    int alt157=2;
                    try { dbg.enterSubRule(157);
                    try { dbg.enterDecision(157);
```

### UnusedAssignment
Variable `alt54` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            dbg.location(364,24);
            // Java.g:364:24: ( type | 'void' )
            int alt54=2;
            try { dbg.enterSubRule(54);
            try { dbg.enterDecision(54);
```

### UnusedAssignment
Variable `alt41` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            dbg.location(302,14);
            // Java.g:302:14: ( methodDeclaration | fieldDeclaration )
            int alt41=2;
            try { dbg.enterSubRule(41);
            try { dbg.enterDecision(41);
```

### UnusedAssignment
Variable `alt123` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 97) ) { return ; }
            // Java.g:673:5: ( enhancedForControl | ( forInit )? ';' ( expression )? ';' ( forUpdate )? )
            int alt123=2;
            try { dbg.enterDecision(123);

```

### UnusedAssignment
Variable `alt103` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 81) ) { return ; }
            // Java.g:578:5: ( annotationMethodRest | annotationConstantRest )
            int alt103=2;
            try { dbg.enterDecision(103);

```

### UnusedAssignment
Variable `alt76` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 58) ) { return ; }
            // Java.g:466:5: ( type | '?' ( ( 'extends' | 'super' ) type )? )
            int alt76=2;
            try { dbg.enterDecision(76);

```

### UnusedAssignment
Variable `t1` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
    public final void assignmentOperator() throws RecognitionException {
        int assignmentOperator_StartIndex = input.index();
        Token t1=null;
        Token t2=null;
        Token t3=null;
```

### UnusedAssignment
Variable `t2` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        int assignmentOperator_StartIndex = input.index();
        Token t1=null;
        Token t2=null;
        Token t3=null;
        Token t4=null;
```

### UnusedAssignment
Variable `t3` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        Token t1=null;
        Token t2=null;
        Token t3=null;
        Token t4=null;

```

### UnusedAssignment
Variable `t4` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        Token t2=null;
        Token t3=null;
        Token t4=null;

        try { dbg.enterRule(getGrammarFileName(), "assignmentOperator");
```

### UnusedAssignment
Variable `alt127` initializer `12` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 106) ) { return ; }
            // Java.g:713:5: ( '=' | '+=' | '-=' | '*=' | '/=' | '&=' | '|=' | '^=' | '%=' | ( '<' '<' '=' )=>t1= '<' t2= '<' t3= '=' {...}? | ( '>' '>' '>' '=' )=>t1= '>' t2= '>' t3= '>' t4= '=' {...}? | ( '>' '>' '=' )=>t1= '>' t2= '>' t3= '=' {...}?)
            int alt127=12;
            try { dbg.enterDecision(127);

```

### UnusedAssignment
Variable `alt176` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        dbg.location(179,9);
        // Java.g:179:9: ( packageDeclaration ( importDeclaration )* ( typeDeclaration )* | classOrInterfaceDeclaration ( typeDeclaration )* )
        int alt176=2;
        try { dbg.enterSubRule(176);
        try { dbg.enterDecision(176);
```

### UnusedAssignment
Variable `alt80` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 62) ) { return ; }
            // Java.g:483:5: ( variableDeclaratorId ( ',' formalParameterDecls )? | '...' variableDeclaratorId )
            int alt80=2;
            try { dbg.enterDecision(80);

```

### UnusedAssignment
Variable `alt169` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 134) ) { return ; }
            // Java.g:899:5: ( arguments | '.' Identifier ( arguments )? )
            int alt169=2;
            try { dbg.enterDecision(169);

```

### UnusedAssignment
Variable `alt164` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            dbg.location(873,9);
            // Java.g:873:9: ( ']' ( '[' ']' )* arrayInitializer | expression ']' ( '[' expression ']' )* ( '[' ']' )* )
            int alt164=2;
            try { dbg.enterSubRule(164);
            try { dbg.enterDecision(164);
```

### UnusedAssignment
Variable `alt162` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    loop162:
                    do {
                        int alt162=2;
                        try { dbg.enterDecision(162);

```

### UnusedAssignment
Variable `alt119` initializer `3` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 96) ) { return ; }
            // Java.g:666:5: ( 'case' constantExpression ':' | 'case' enumConstantName ':' | 'default' ':' )
            int alt119=3;
            try { dbg.enterDecision(119);

```

### UnusedAssignment
Variable `alt51` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            dbg.location(354,9);
            // Java.g:354:9: ( methodBody | ';' )
            int alt51=2;
            try { dbg.enterSubRule(51);
            try { dbg.enterDecision(51);
```

### UnusedAssignment
Variable `alt44` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 31) ) { return ; }
            // Java.g:323:5: ( modifiers interfaceMemberDecl | ';' )
            int alt44=2;
            try { dbg.enterDecision(44);

```

### UnusedAssignment
Variable `alt126` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            dbg.location(709,31);
            // Java.g:709:31: ( assignmentOperator expression )?
            int alt126=2;
            try { dbg.enterSubRule(126);
            try { dbg.enterDecision(126);
```

### UnusedAssignment
Variable `alt43` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 28) ) { return ; }
            // Java.g:310:5: ( ( type | 'void' ) Identifier methodDeclaratorRest | Identifier constructorDeclaratorRest )
            int alt43=2;
            try { dbg.enterDecision(43);

```

### UnusedAssignment
Variable `alt42` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    dbg.location(310,9);
                    // Java.g:310:9: ( type | 'void' )
                    int alt42=2;
                    try { dbg.enterSubRule(42);
                    try { dbg.enterDecision(42);
```

### UnusedAssignment
Variable `alt153` initializer `8` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 124) ) { return ; }
            // Java.g:836:5: ( parExpression | 'this' ( '.' Identifier )* ( identifierSuffix )? | 'super' superSuffix | literal | 'new' creator | Identifier ( '.' Identifier )* ( identifierSuffix )? | primitiveType ( '[' ']' )* '.' 'class' | 'void' '.' 'class' )
            int alt153=8;
            try { dbg.enterDecision(153);

```

### UnusedAssignment
Variable `alt149` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    dbg.location(837,34);
                    // Java.g:837:34: ( identifierSuffix )?
                    int alt149=2;
                    try { dbg.enterSubRule(149);
                    try { dbg.enterDecision(149);
```

### UnusedAssignment
Variable `alt151` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    dbg.location(841,38);
                    // Java.g:841:38: ( identifierSuffix )?
                    int alt151=2;
                    try { dbg.enterSubRule(151);
                    try { dbg.enterDecision(151);
```

### UnusedAssignment
Variable `alt16` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 9) ) { return ; }
            // Java.g:222:5: ( normalClassDeclaration | enumDeclaration )
            int alt16=2;
            try { dbg.enterDecision(16);

```

### UnusedAssignment
Variable `alt85` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 65) ) { return ; }
            // Java.g:496:5: ( ( nonWildcardTypeArguments )? ( 'this' | 'super' ) arguments ';' | primary '.' ( nonWildcardTypeArguments )? 'super' arguments ';' )
            int alt85=2;
            try { dbg.enterDecision(85);

```

### UnusedAssignment
Variable `alt93` initializer `3` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 75) ) { return ; }
            // Java.g:548:5: ( conditionalExpression | annotation | elementValueArrayInitializer )
            int alt93=3;
            try { dbg.enterDecision(93);

```

### UnusedAssignment
Variable `alt46` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 34) ) { return ; }
            // Java.g:340:5: ( constantDeclaratorsRest ';' | interfaceMethodDeclaratorRest )
            int alt46=2;
            try { dbg.enterDecision(46);

```

### UnusedAssignment
Variable `alt14` initializer `8` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 7) ) { return ; }
            // Java.g:207:5: ( annotation | 'public' | 'protected' | 'private' | 'abstract' | 'static' | 'final' | 'strictfp' )
            int alt14=8;
            try { dbg.enterDecision(14);

```

### UnusedAssignment
Variable `alt142` initializer `5` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 121) ) { return ; }
            // Java.g:816:5: ( '+' unaryExpression | '-' unaryExpression | '++' unaryExpression | '--' unaryExpression | unaryExpressionNotPlusMinus )
            int alt142=5;
            try { dbg.enterDecision(142);

```

### UnusedAssignment
Variable `alt124` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 98) ) { return ; }
            // Java.g:678:5: ( localVariableDeclaration | expressionList )
            int alt124=2;
            try { dbg.enterDecision(124);

```

### UnusedAssignment
Variable `t1` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
    public final void shiftOp() throws RecognitionException {
        int shiftOp_StartIndex = input.index();
        Token t1=null;
        Token t2=null;
        Token t3=null;
```

### UnusedAssignment
Variable `t2` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        int shiftOp_StartIndex = input.index();
        Token t1=null;
        Token t2=null;
        Token t3=null;

```

### UnusedAssignment
Variable `t3` initializer `null` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        Token t1=null;
        Token t2=null;
        Token t3=null;

        try { dbg.enterRule(getGrammarFileName(), "shiftOp");
```

### UnusedAssignment
Variable `alt139` initializer `3` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 118) ) { return ; }
            // Java.g:793:5: ( ( '<' '<' )=>t1= '<' t2= '<' {...}? | ( '>' '>' '>' )=>t1= '>' t2= '>' t3= '>' {...}? | ( '>' '>' )=>t1= '>' t2= '>' {...}?)
            int alt139=3;
            try { dbg.enterDecision(139);

```

### UnusedAssignment
Variable `alt73` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 56) ) { return ; }
            // Java.g:457:5: ( 'final' | annotation )
            int alt73=2;
            try { dbg.enterDecision(73);

```

### UnusedAssignment
Variable `alt40` initializer `6` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 25) ) { return ; }
            // Java.g:293:5: ( genericMethodOrConstructorDecl | memberDeclaration | 'void' Identifier voidMethodDeclaratorRest | Identifier constructorDeclaratorRest | interfaceDeclaration | classDeclaration )
            int alt40=6;
            try { dbg.enterDecision(40);

```

### UnusedAssignment
Variable `alt81` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            dbg.location(492,13);
            // Java.g:492:13: ( explicitConstructorInvocation )?
            int alt81=2;
            try { dbg.enterSubRule(81);
            try { dbg.enterDecision(81);
```

### UnusedAssignment
Variable `alt66` initializer `12` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 49) ) { return ; }
            // Java.g:410:5: ( annotation | 'public' | 'protected' | 'private' | 'static' | 'abstract' | 'final' | 'native' | 'synchronized' | 'transient' | 'volatile' | 'strictfp' )
            int alt66=12;
            try { dbg.enterDecision(66);

```

### UnusedAssignment
Variable `alt49` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            dbg.location(347,9);
            // Java.g:347:9: ( methodBody | ';' )
            int alt49=2;
            try { dbg.enterSubRule(49);
            try { dbg.enterDecision(49);
```

### UnusedAssignment
Variable `alt87` initializer `6` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 67) ) { return ; }
            // Java.g:506:5: ( integerLiteral | FloatingPointLiteral | CharacterLiteral | StringLiteral | booleanLiteral | 'null' )
            int alt87=6;
            try { dbg.enterDecision(87);

```

### UnusedAssignment
Variable `alt167` initializer `5` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 133) ) { return ; }
            // Java.g:891:5: ( '.' Identifier ( arguments )? | '.' 'this' | '.' 'super' superSuffix | '.' 'new' innerCreator | '[' expression ']' )
            int alt167=5;
            try { dbg.enterDecision(167);

```

### UnusedAssignment
Variable `alt8` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 1) ) { return ; }
            // Java.g:178:5: ( annotations ( packageDeclaration ( importDeclaration )* ( typeDeclaration )* | classOrInterfaceDeclaration ( typeDeclaration )* ) | ( packageDeclaration )? ( importDeclaration )* ( typeDeclaration )* )
            int alt8=2;
            try { dbg.enterDecision(8);

```

### UnusedAssignment
Variable `alt4` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    dbg.location(179,9);
                    // Java.g:179:9: ( packageDeclaration ( importDeclaration )* ( typeDeclaration )* | classOrInterfaceDeclaration ( typeDeclaration )* )
                    int alt4=2;
                    try { dbg.enterSubRule(4);
                    try { dbg.enterDecision(4);
```

### UnusedAssignment
Variable `alt159` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 127) ) { return ; }
            // Java.g:863:5: ( classOrInterfaceType | primitiveType )
            int alt159=2;
            try { dbg.enterDecision(159);

```

### UnusedAssignment
Variable `alt147` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 123) ) { return ; }
            // Java.g:831:5: ( '(' primitiveType ')' unaryExpression | '(' ( type | expression ) ')' unaryExpressionNotPlusMinus )
            int alt147=2;
            try { dbg.enterDecision(147);

```

### UnusedAssignment
Variable `alt146` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    dbg.location(832,12);
                    // Java.g:832:12: ( type | expression )
                    int alt146=2;
                    try { dbg.enterSubRule(146);
                    try { dbg.enterDecision(146);
```

### UnusedAssignment
Variable `alt62` initializer `2` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 47) ) { return ; }
            // Java.g:401:5: ( arrayInitializer | expression )
            int alt62=2;
            try { dbg.enterDecision(62);

```

### UnusedAssignment
Variable `alt145` initializer `4` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 122) ) { return ; }
            // Java.g:824:5: ( '~' unaryExpression | '!' unaryExpression | castExpression | primary ( selector )* ( '++' | '--' )? )
            int alt145=4;
            try { dbg.enterDecision(145);

```

### UnusedAssignment
Variable `alt106` initializer `3` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( state.backtracking>0 && alreadyParsedRule(input, 86) ) { return ; }
            // Java.g:601:5: ( localVariableDeclarationStatement | classOrInterfaceDeclaration | statement )
            int alt106=3;
            try { dbg.enterDecision(106);

```

### UnusedAssignment
The value `null` assigned to `rop` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
            // if we're selecting independent mappings separately or have
            // custom loads, do individual selects for each class
            rop = null;
            if (rops != null) {
                for (int i = 0; i < mappings.length; i++) {
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Type.java`
#### Snippet
```java
    public Object load(ExpContext ctx, ExpState state, Result res)
        throws SQLException {
        Object type = null;
        if (_disc != null && _disc.getColumns().length > 0) {
            type = res.getObject(_disc.getColumns()[0], null, state.joins);
```

### UnusedAssignment
Variable `_select` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/SubQ.java`
#### Snippet
```java
    private ClassMetaData _meta = null;
    private QueryExpressions _exps = null;
    private Select _select = null;

    /**
```

### UnusedAssignment
Variable `rel` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/InstanceofExpression.java`
#### Snippet
```java
        // is the field?
        ClassMapping relMapping = _path.getClassMapping(pathState);
        Class rel = null;
        if (relMapping == null) {
            FieldMapping field = _path.getFieldMapping(pathState);
```

### UnusedAssignment
The value `field.getKey().getDeclaredType()` assigned to `rel` is never used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/InstanceofExpression.java`
#### Snippet
```java
                case JavaTypes.MAP:
                    if (_path.isKey())
                        rel = field.getKey().getDeclaredType();
                    // no break
                case JavaTypes.ARRAY:
```

### UnusedAssignment
Variable `schemaName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DynamicSchemaFactory.java`
#### Snippet
```java

        // if full name, split
        DBIdentifier schemaName = DBIdentifier.NULL;
        DBIdentifier tableName = path.getUnqualifiedName();
        if (!DBIdentifier.isNull(path.getSchemaName())) {
```

### UnusedAssignment
Variable `field` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        Variable var;
        Iterator itr = (_actions == null) ? null : _actions.iterator();
        FieldMapping field = null;
        Action prevaction = null;
        boolean isCorrelatedPath = false;
```

### UnusedAssignment
Variable `disc` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
    public void appendType(Select sel, ExpContext ctx, ExpState state,
        SQLBuffer sql) {
        Discriminator disc = null;
        ClassMapping sup = _class;
        while (sup.getMappedPCSuperclassMapping() != null)
```

### UnusedAssignment
Variable `cols` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        disc = sup.getDiscriminator();

        Column[] cols = null;
        if (disc != null)
            cols = disc.getColumns();
```

### UnusedAssignment
Variable `tableSchema` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        Schema schema;
        Table table;
        DBIdentifier tableSchema = DBIdentifier.NULL;
        DBIdentifier baseTableName = (tableName == null) ? DBIdentifier.NULL : tableName.clone();
        for (int i = 0; cols != null && i < cols.length; i++) {
```

### UnusedAssignment
Variable `sequenceSchema` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        SchemaGroup group = getSchemaGroup();
        Schema schema;
        DBIdentifier sequenceSchema = DBIdentifier.NULL;
        for (int i = 0; seqs != null && i < seqs.length; i++) {
            sequenceName = seqs[i].getIdentifier();
```

### UnusedAssignment
Variable `schemaName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java

            // generate pks, indexes, fks
            DBIdentifier schemaName = DBIdentifier.NULL;
            DBIdentifier[] tableNames;
            for (Object[] objects : schemaMap) {
```

### UnusedAssignment
Variable `name` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        Schema schema;
        PrimaryKey pk;
        DBIdentifier name = DBIdentifier.NULL;
        DBIdentifier colName = DBIdentifier.NULL;
        for (int i = 0; pks != null && i < pks.length; i++) {
```

### UnusedAssignment
Variable `colName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        PrimaryKey pk;
        DBIdentifier name = DBIdentifier.NULL;
        DBIdentifier colName = DBIdentifier.NULL;
        for (int i = 0; pks != null && i < pks.length; i++) {
            schemaName = DBIdentifier.trimToNull(pks[i].getSchemaIdentifier());
```

### UnusedAssignment
Variable `schema` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java

        Map<DBIdentifier, Collection<DBIdentifier>> schemas = new HashMap<>();
        DBIdentifier schema = DBIdentifier.NULL, table = DBIdentifier.NULL;
        Collection<DBIdentifier> tables = null;
        for (DBIdentifier arg : args) {
```

### UnusedAssignment
Variable `table` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java

        Map<DBIdentifier, Collection<DBIdentifier>> schemas = new HashMap<>();
        DBIdentifier schema = DBIdentifier.NULL, table = DBIdentifier.NULL;
        Collection<DBIdentifier> tables = null;
        for (DBIdentifier arg : args) {
```

### UnusedAssignment
Variable `tables` initializer `null` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        Map<DBIdentifier, Collection<DBIdentifier>> schemas = new HashMap<>();
        DBIdentifier schema = DBIdentifier.NULL, table = DBIdentifier.NULL;
        Collection<DBIdentifier> tables = null;
        for (DBIdentifier arg : args) {
            QualifiedDBIdentifier path = QualifiedDBIdentifier.getPath(arg);
```

### UnusedAssignment
Variable `name` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        Schema schema;
        Index idx;
        DBIdentifier name = DBIdentifier.NULL;
        DBIdentifier colName = DBIdentifier.NULL;
        DBIdentifier pkName = DBIdentifier.NULL;
```

### UnusedAssignment
Variable `colName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        Index idx;
        DBIdentifier name = DBIdentifier.NULL;
        DBIdentifier colName = DBIdentifier.NULL;
        DBIdentifier pkName = DBIdentifier.NULL;
        for (int i = 0; idxs != null && i < idxs.length; i++) {
```

### UnusedAssignment
Variable `pkName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        DBIdentifier name = DBIdentifier.NULL;
        DBIdentifier colName = DBIdentifier.NULL;
        DBIdentifier pkName = DBIdentifier.NULL;
        for (int i = 0; idxs != null && i < idxs.length; i++) {
            schemaName = DBIdentifier.trimToNull(idxs[i].getSchemaIdentifier());
```

### UnusedAssignment
Variable `name` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        Table pkTable;
        ForeignKey fk;
        DBIdentifier name = DBIdentifier.NULL;
        DBIdentifier pkSchemaName = DBIdentifier.NULL;
        DBIdentifier pkTableName = DBIdentifier.NULL;
```

### UnusedAssignment
Variable `pkSchemaName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        ForeignKey fk;
        DBIdentifier name = DBIdentifier.NULL;
        DBIdentifier pkSchemaName = DBIdentifier.NULL;
        DBIdentifier pkTableName = DBIdentifier.NULL;
        DBIdentifier pkColName = DBIdentifier.NULL;
```

### UnusedAssignment
Variable `pkTableName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        DBIdentifier name = DBIdentifier.NULL;
        DBIdentifier pkSchemaName = DBIdentifier.NULL;
        DBIdentifier pkTableName = DBIdentifier.NULL;
        DBIdentifier pkColName = DBIdentifier.NULL;
        DBIdentifier fkColName = DBIdentifier.NULL;
```

### UnusedAssignment
Variable `pkColName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        DBIdentifier pkSchemaName = DBIdentifier.NULL;
        DBIdentifier pkTableName = DBIdentifier.NULL;
        DBIdentifier pkColName = DBIdentifier.NULL;
        DBIdentifier fkColName = DBIdentifier.NULL;
        int seq;
```

### UnusedAssignment
Variable `fkColName` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        DBIdentifier pkTableName = DBIdentifier.NULL;
        DBIdentifier pkColName = DBIdentifier.NULL;
        DBIdentifier fkColName = DBIdentifier.NULL;
        int seq;
        boolean seqWas0 = false; // some drivers incorrectly start at 0
```

### UnusedAssignment
The value `em.createNamedQuery("siblings").
setParameter(1, em.getReference(Deity.class, "Rhea"))` assigned to `q` is never used
in `openjpa-examples/simple/src/main/java/relations/Main.java`
#### Snippet
```java
        }

        q = em.createNamedQuery("siblings").
            setParameter(1, em.getReference(Deity.class, "Rhea"));

```

### UnusedAssignment
Variable `dbId` initializer `DBIdentifier.NULL` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
        }

        DBIdentifier dbId = DBIdentifier.NULL;
        // Create a DBIDentifier for single component names.  Otherwise, create a QualifiedDBIdentifier.
        if (!_compoundIdentifier[id.ordinal()] || delimit) {
```

### UnusedAssignment
Variable `policy` initializer `null` is redundant
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/SliceImplHelper.java`
#### Snippet
```java
	public static SliceInfo getSlicesByPolicy(Object pc, DistributedConfiguration conf, Object ctx) {
		List<String> actives = conf.getActiveSliceNames();
		Object policy = null;
		String[] targets = null;
		boolean replicated = isReplicated(pc, conf);
```

### UnusedAssignment
Variable `owner` initializer `null` is redundant
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedStoreQuery.java`
#### Snippet
```java
        private static final long serialVersionUID = 1L;
        private List<Executor> executors = new ArrayList<>();
		private DistributedStoreQuery owner = null;

        public ParallelExecutor(DistributedStoreQuery dsq, ClassMetaData meta,
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedStoreQuery.java`
#### Snippet
```java

			ResultObjectProvider[] tmp = rops.toArray(new ResultObjectProvider[rops.size()]);
			ResultObjectProvider result = null;
			boolean[] ascending = getAscending(q);
			boolean isAscending = ascending.length > 0;
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
        if (SliceImplHelper.isSliceAssigned(sm))
            return SliceImplHelper.getSliceInfo(sm);
        SliceInfo result = null;
        PersistenceCapable pc = sm.getPersistenceCapable();
        Object ctx = getContext();
```

### UnusedAssignment
Variable `real` initializer `new ArrayList<>()` is redundant
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedDataSource.java`
#### Snippet
```java
public class DistributedDataSource extends DecoratingDataSource implements
		Iterable<DataSource> {
	private List<DataSource> real = new ArrayList<>();
	private DataSource master;

```

### UnusedAssignment
Variable `ok` initializer `false` is redundant
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaEnhancerMojo.java`
#### Snippet
```java
        String[] args = getFilePaths(files);

        boolean ok = false;

        if (!tmpClassLoader) {
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
			ResultObjectProvider[] tmp = rops
                    .toArray(new ResultObjectProvider[rops.size()]);
			ResultObjectProvider result = null;
			boolean[] ascending = getAscending(q);
			boolean isAscending = ascending.length > 0;
```

### UnusedAssignment
Variable `owner` initializer `null` is redundant
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
			SQLStoreQuery.SQLExecutor {
		private List<Executor> executors = new ArrayList<>();
		private DistributedSQLStoreQuery owner = null;

        public ParallelExecutor(DistributedSQLStoreQuery dsq, ClassMetaData meta, boolean p) {
```

### UnusedAssignment
Variable `files` initializer `new ArrayList<>()` is redundant
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java
    protected List<File> findEntityClassFiles() throws MojoExecutionException
    {
        List<File> files = new ArrayList<>();

        try
```

## RuleId[id=SuspiciousNameCombination]
### SuspiciousNameCombination
'x' should probably not be passed as parameter 'y'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <N extends Number> Expression<N> neg(Expression<N> x) {
        return new Expressions.Diff<>(0, x);
    }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Nullif(Expression<T> x, T y) {
            this(x, new Constant<>(y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> GreaterThan(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### SuspiciousNameCombination
'x' should probably not be passed as parameter 'y'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Between(Expression<? extends Y> v, Expression<? extends Y> x, Expression<? extends Y> y) {
            super(new GreaterThanEqual(v,x), new LessThanEqual(v,y));
            e = (ExpressionImpl<? extends Y>)v;
            v1 = (ExpressionImpl<? extends Y>)x;
```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> LessThan(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Product(Expression<? extends Number> x, Number y) {
            this(x, new Constant<>(Number.class, y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Sum(Expression<? extends Number> x, Number y) {
            this(x, new Constant<>(Number.class, y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Concat(Expression<String> x, String y) {
            this(x, new Constant<>(y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> NotEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        }
        public  Mod(Expression<Integer> x, Integer y) {
            this(x,new Constant<>(Integer.class, y));
        }
        public  Mod(Integer x, Expression<Integer> y) {
```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> LessThanEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Quotient(Expression<? extends Number> x, Number y) {
            this(x, new Constant<>(y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> GreaterThanEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Diff(Expression<? extends Number> x, Number y) {
            this(x, new Constant<>(Number.class, y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Between(Expression<? extends Y> v, Y x, Y y) {
            this(v, new Constant<>(x), new Constant<>(y));
        }

```

### SuspiciousNameCombination
'y' should probably not be passed as parameter 'x'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> Equal(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

## RuleId[id=JavadocReference]
### JavadocReference
Cannot resolve symbol `JDBCFetchPlan`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintHandler.java`
#### Snippet
```java
 *
 * 2. Some hint keys are collected from bean-style property names of {@link
 *    JDBCFetchPlan} by {@link Reflection#getBeanStylePropertyNames(Class)
 *    reflection} and prefixed with <code>openjpa.FetchPlan</code>.
 *    Their values are used to set the corresponding property of {@link
```

### JavadocReference
Cannot resolve symbol `hintToSetter(FetchPlan, String, Object)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintHandler.java`
#### Snippet
```java
 *    reflection} and prefixed with <code>openjpa.FetchPlan</code>.
 *    Their values are used to set the corresponding property of {@link
 *    FetchPlan} via {@link #hintToSetter(FetchPlan, String, Object) reflection}
 *
 * 3. Currently defined <code>jakarta.persistence.*</code> hint keys have
```

### JavadocReference
Cannot resolve symbol `sm`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistenceUtil.java`
#### Snippet
```java
     * broker within the persistence unit of the provided EMF instance.
     * @param emf OpenJPAEntityManagerFactory
     * @param sm StateManager
     * @return true if this state manager is managed by a broker within
     * this persistence unit.
```

### JavadocReference
Cannot resolve symbol `name`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchPlan.java`
#### Snippet
```java
     *
     * @param value the value of the hint.
     * @param name the name of the hint.
     *
     * @since 2.0.0
```

### JavadocReference
Cannot resolve symbol `translateInternalException(OpenJPAException)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceExceptions.java`
#### Snippet
```java
     * exceptions that inherit JPA-defined exceptions is generated.
     * If given exception is not further classified to a sub type, then
     * an [@link {@link #translateInternalException(OpenJPAException)} attempt}
     * is made to translate the given OpenJPAException by its internal cause.
     */
```

### JavadocReference
Cannot resolve symbol `getPersistentFields(ClassMetaData)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
    /**
     * Gets the members that are backing members for attributes being persisted.
     * Unlike {@linkplain #getPersistentFields(ClassMetaData)} and
     * {@linkplain #getPersistentMethods(ClassMetaData)} which returns
     * <em>possible</em> candidates, the result of this method is definite.
```

### JavadocReference
Cannot resolve symbol `getPersistentMethods(ClassMetaData)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
     * Gets the members that are backing members for attributes being persisted.
     * Unlike {@linkplain #getPersistentFields(ClassMetaData)} and
     * {@linkplain #getPersistentMethods(ClassMetaData)} which returns
     * <em>possible</em> candidates, the result of this method is definite.
     *
```

### JavadocReference
Cannot resolve symbol `serializeValueMappingContent`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
    /**
     * Serialize field mapping content; this will be called before
     * {@link #serializeValueMappingContent}. Does nothing by default.
     */
    protected void serializeFieldMappingContent(FieldMetaData fmd,
```

### JavadocReference
Cannot resolve symbol `the`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
     *            this is the key to identify the parameter later in the context of this query. Valid key types are
     *            Integer, String or ParameterExpression itself.
     * @param the
     *            parameter instance to be declared
     */
```

### JavadocReference
Cannot resolve symbol `position`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
     * parameter has the side-effect of a positional parameter being declared.
     *
     * @param position
     *            positive, integer position of the parameter
     * @param value
```

### JavadocReference
Cannot resolve symbol `param`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
     * Return the value bound to the parameter.
     *
     * @param param
     *            parameter object
     * @return parameter value
```

### JavadocReference
Cannot resolve symbol `declareParameter(Parameter)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
     *         ParameterExpression itself) and the value is the concrete Parameter instance either constructed as a
     *         result of this call or supplied by declaring the parameter explicitly via
     *         {@linkplain #declareParameter(Parameter)}.
     */
    public Map<Object, Parameter<?>> getDeclaredParameters() {
```

### JavadocReference
Cannot resolve symbol `position`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
     * a value.
     *
     * @param position
     *            specified in the user query.
     * @return parameter object
```

### JavadocReference
Symbol `org.apache.openjpa.jdbc.sql.Result` is inaccessible from here
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
 * A {@link StoredProcedureQuery stored procedure query} differs from other query types because it may return
 * more than one result set, apart from an optional update count, whereas the traditional query processing in OpenJPA
 * via the abstractions of {@link ResultObjectProvider} and {@link org.apache.openjpa.jdbc.sql.Result}
 * assumed that a query will return its
 * result in a single list.
```

### JavadocReference
Cannot resolve symbol `serializeValueMappingContent`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
    /**
     * Serialize field mapping content; this will be called before
     * {@link #serializeValueMappingContent}. Does nothing by default.
     */
    protected void serializeFieldMappingContent(FieldMetaData fmd, PersistenceStrategy strategy, AnnotationBuilder ab) {
```

### JavadocReference
Cannot resolve symbol `T`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetadataProcessor.java`
#### Snippet
```java
 * the source and the representation available for type system in the source.
 *
 * @param T the M2 representation of type based on the nature of the source
 * e.g. {@linkplain TypeElement} for annotation processing of *.java files or
 * {@link Class} for compiled bytecode.
```

### JavadocReference
Cannot resolve symbol `M`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetadataProcessor.java`
#### Snippet
```java
 * {@link Class} for compiled bytecode.
 *
 * @param M the corresponding M2 representation for member of type T
 *
 * @author Pinaki Poddar
```

### JavadocReference
Cannot resolve symbol `cls`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     *  Return the metamodel managed type representing the
     *  entity, mapped superclass, or embeddable class.
     *  @param cls  the type of the represented managed class
     *  @return the metamodel managed type
     *  @throws IllegalArgumentException if not a managed class
```

### JavadocReference
Cannot resolve symbol
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * Validates the given field of the meta class matches the given
     * FieldMetaData and
     * @param <X>
     * @param <Y>
     * @param mField
```

### JavadocReference
Cannot resolve symbol
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * FieldMetaData and
     * @param <X>
     * @param <Y>
     * @param mField
     * @param member
```

### JavadocReference
Cannot resolve symbol `mField`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * @param <X>
     * @param <Y>
     * @param mField
     * @param member
     */
```

### JavadocReference
Cannot resolve symbol `member`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * @param <Y>
     * @param mField
     * @param member
     */
    void validate(Field metaField, FieldMetaData fmd) {
```

### JavadocReference
Cannot resolve symbol `cls`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    /**
     *  Return the metamodel entity type representing the entity.
     *  @param cls  the type of the represented entity
     *  @return the metamodel entity type
     *  @throws IllegalArgumentException if not an entity
```

### JavadocReference
Cannot resolve symbol `type(Class)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    /**
     *  Return the type representing the basic, entity, mapped superclass, or embeddable class.
     *  This method differs from {@linkplain #type(Class)} as it also creates a basic or pesudo
     *  type for the given class argument if not already available in this receiver.
     *
```

### JavadocReference
Cannot resolve symbol `cls`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     *  Return the metamodel embeddable type representing the embeddable class.
     *
     *  @param cls  the type of the represented embeddable class
     *  @return the metamodel embeddable type
     *  @throws IllegalArgumentException if not an embeddable class
```

### JavadocReference
Cannot resolve symbol `parameter`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
         * Specify use of a parameter of the given name.
         *
         * @param parameter name
         * @return an Expression corresponding to a named parameter
         */
```

### JavadocReference
Cannot resolve symbol `character`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
         * Create an Expression corresponding to a character value.
         *
         * @param character value
         * @return the corresponding Expression literal
         */
```

### JavadocReference
Cannot resolve symbol `cls`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryBuilder.java`
#### Snippet
```java
     * entity class.
     *
     * @param cls -
     *            an entity class
     * @return root domain object
```

### JavadocReference
Cannot resolve symbol `clear`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
     * properly prepare an EM for pooling.
     *
     * @deprecated - use {@link clear()} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `element`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
     * @param name simple name i.e. without the class name
     * @param type the expected type of the field.
     * @param element
     *            the expected element type of the field.
     * @param key
```

### JavadocReference
Cannot resolve symbol `key`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
     * @param element
     *            the expected element type of the field.
     * @param key
     *            the expected key type of the field.
     * @param declared
```

### JavadocReference
Cannot resolve symbol `declared`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
     * @param key
     *            the expected key type of the field.
     * @param declared
     *            is this field declared in this receiver
     *
```

### JavadocReference
Cannot resolve symbol `value`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/AliasContext.java`
#### Snippet
```java
     * @param node of query tree
     * @param variable must be a variable
     * @param value path value
     */
    void registerVariable(Selection<?> node, Value variable, Value path);
```

### JavadocReference
Cannot resolve symbol `If`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidationUtils.java`
#### Snippet
```java
     * @param conf
     * @return true if a Validator was created, false otherwise.
     * @throws If a Validator was required but could not be created.
     */
    public static boolean setupValidation(OpenJPAConfiguration conf) {
```

### JavadocReference
Cannot resolve symbol `event`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     * Add the validation group(s) for the specified event.  Event definitions
     * are defined in LifecycleEvent
     * @param event
     * @param group
     */
```

### JavadocReference
Cannot resolve symbol `Z`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
     * Join a plural attribute.
     *
     * @param Z type being joined from
     * @param C Java collection type of the container
     * @param E type of the element being joined to
```

### JavadocReference
Cannot resolve symbol `C`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
     *
     * @param Z type being joined from
     * @param C Java collection type of the container
     * @param E type of the element being joined to
     *
```

### JavadocReference
Cannot resolve symbol `E`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
     * @param Z type being joined from
     * @param C Java collection type of the container
     * @param E type of the element being joined to
     *
     */
```

### JavadocReference
Cannot resolve symbol `assignAutoName(String)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
     * registered} in a Criteriaquery during tree traversal.
     *
     * @see ParameterExpressionImpl#assignAutoName(String)
     */
    @Override
```

### JavadocReference
Symbol `org.apache.openjpa.kernel.QueryResultCallback` is inaccessible from here
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/BatchedResultObjectProvider.java`
#### Snippet
```java
 * <br>
 *
 * @see org.apache.openjpa.kernel.QueryResultCallback
 *
 * @author Pinaki Poddar
```

### JavadocReference
Symbol `setInto` is inaccessible from here
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/GenericConfigurable.java`
#### Snippet
```java
 * generic configuration with any key-value pairs that cannot be set
 * into the object via the normal
 * {@link org.apache.openjpa.lib.util.Options#setInto} means.
 *
 * @author Patrick Linskey
```

### JavadocReference
Cannot resolve symbol `PersistenceProviderImpl.createEntityManagerFactory`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configuration.java`
#### Snippet
```java
     *
     * @param loader a class loader to load custom plugin values
     * @see PersistenceProviderImpl.createEntityManagerFactory(String, Map)
     * @since 2.3.0
     */
```

### JavadocReference
Cannot resolve symbol `Reflection`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Reflectable.java`
#### Snippet
```java

/**
 * Annotates a getter method or field so {@link Reflection reflection
 * utility} to control whether the annotated member is recorded during scanning
 * for bean-style method or field.
```

### JavadocReference
Cannot resolve symbol `HARD`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashSet.java`
#### Snippet
```java
/**
 * A concurrent set whose values may be stored as weak or soft references. If
 * the constructor is invoked with <code>refType</code> set to {@link #HARD},
 * this uses a JDK1.5 {@link ConcurrentHashMap} under the covers. Otherwise,
 * it uses a {@link ConcurrentReferenceHashMap}.
```

### JavadocReference
Cannot resolve symbol `compare(Object, Object)`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/ComparableComparator.java`
#### Snippet
```java
 * Note: In the 2.0 and 2.1 releases of Commons Collections, this class would
 * throw a {@link ClassCastException} if either of the arguments to
 * {@link #compare(Object, Object) compare} were <code>null</code>, not
 * {@link Comparable Comparable}, or for which
 * {@link Comparable#compareTo(Object) compareTo} gave inconsistent results.
```

### JavadocReference
Cannot resolve symbol `compare(Object, Object)`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/ComparableComparator.java`
#### Snippet
```java
 * {@link Comparable Comparable}, or for which
 * {@link Comparable#compareTo(Object) compareTo} gave inconsistent results.
 * This is no longer the case. See {@link #compare(Object, Object) compare} for
 * details.
 * </p>
```

### JavadocReference
Cannot resolve symbol `options`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/instrumentation/InstrumentationProvider.java`
#### Snippet
```java
    /**
     * Gets configuration options for this provider
     * @param options
     */
    String getOptions();
```

### JavadocReference
Cannot resolve symbol `pname`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
     * Gets the first part of a name when single character delimiters are
     * in use.
     * @param pname
     */
    private String splitNameCharDelimiters(String name, String nameDelim) {
```

### JavadocReference
Cannot resolve symbol `BundleContext`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java

    /**
     * Remove a service listener previously started inside {@link #registerServiceListener(BundleContext)}.
     *
     * @param bundleContext The bundle context to call
```

### JavadocReference
Cannot resolve symbol `BundleContext`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
     *
     * @param bundleContext The bundle context to call
     *          {@link BundleContext#removeServiceListener(ServiceListener)} on.
     */
    public static synchronized void deregisterServiceListener(BundleContext bundleContext) {
```

### JavadocReference
Cannot resolve symbol `removeServiceListener(ServiceListener)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
     *
     * @param bundleContext The bundle context to call
     *          {@link BundleContext#removeServiceListener(ServiceListener)} on.
     */
    public static synchronized void deregisterServiceListener(BundleContext bundleContext) {
```

### JavadocReference
Cannot resolve symbol `ServiceListener`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
     *
     * @param bundleContext The bundle context to call
     *          {@link BundleContext#removeServiceListener(ServiceListener)} on.
     */
    public static synchronized void deregisterServiceListener(BundleContext bundleContext) {
```

### JavadocReference
Cannot resolve symbol `BundleActivator`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
 *
 * <p>The support for a transaction manager is usually activated inside an OSGi
 * {@link BundleActivator} implementation using {@link #registerServiceListener(BundleContext)}
 * and {@link #deregisterServiceListener(BundleContext)}.
 * </p>
```

### JavadocReference
Cannot resolve symbol `BundleContext`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
 *
 * <p>The support for a transaction manager is usually activated inside an OSGi
 * {@link BundleActivator} implementation using {@link #registerServiceListener(BundleContext)}
 * and {@link #deregisterServiceListener(BundleContext)}.
 * </p>
```

### JavadocReference
Cannot resolve symbol `BundleContext`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
 * <p>The support for a transaction manager is usually activated inside an OSGi
 * {@link BundleActivator} implementation using {@link #registerServiceListener(BundleContext)}
 * and {@link #deregisterServiceListener(BundleContext)}.
 * </p>
 */
```

### JavadocReference
Cannot resolve symbol `BundleContext`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
    /**
     * a static instance, which is managed by the ServiceListener added to the bundle
     * context by {@link #registerServiceListener(BundleContext)}.
     */
    private static TransactionManager transactionManager;
```

### JavadocReference
Cannot resolve symbol `BundleContext`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
    /**
     * <p>Register a service listener to the given bundle context by
     * {@link BundleContext#addServiceListener(ServiceListener,String)} with a filter
     *  expression of <code>(objectClass=jakarta.transaction.TransactionManager)</code>.</p>
     *
```

### JavadocReference
Cannot resolve symbol `addServiceListener(ServiceListener,String)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
    /**
     * <p>Register a service listener to the given bundle context by
     * {@link BundleContext#addServiceListener(ServiceListener,String)} with a filter
     *  expression of <code>(objectClass=jakarta.transaction.TransactionManager)</code>.</p>
     *
```

### JavadocReference
Cannot resolve symbol `ServiceListener`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
    /**
     * <p>Register a service listener to the given bundle context by
     * {@link BundleContext#addServiceListener(ServiceListener,String)} with a filter
     *  expression of <code>(objectClass=jakarta.transaction.TransactionManager)</code>.</p>
     *
```

### JavadocReference
Cannot resolve symbol `BundleContext`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
     *
     *  <p>The caller is responsible for calling
     *  {@link #deregisterServiceListener(BundleContext)}, when
     *  the bundle context is being stopped.</p>
     *
```

### JavadocReference
Cannot resolve symbol `bundle`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
     *  the bundle context is being stopped.</p>
     *
     * @param bundle The bundle, which is currently being started.
     * @throws InvalidSyntaxException When the filter expression is invalid.
     */
```

### JavadocReference
Cannot resolve symbol `InvalidSyntaxException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/OSGiManagedRuntime.java`
#### Snippet
```java
     *
     * @param bundle The bundle, which is currently being started.
     * @throws InvalidSyntaxException When the filter expression is invalid.
     */
    public static synchronized void registerServiceListener(BundleContext bundleContext)
```

### JavadocReference
Cannot resolve symbol `useJPA2`
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Compatibility.java`
#### Snippet
```java
     * by JPA 2.0: name; "_"; "ORDER" or the pre-JPA 2.0 default name "ordr".
     *
     * @param useJPA2 true if the JPA 2.0 default name should be used.  false if
     * the 1.x name should be used.
     *
```

### JavadocReference
Cannot resolve symbol `returnNullOnAggregate`
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Compatibility.java`
#### Snippet
```java
     *
     * @since
     * @param returnNullOnAggregate whether OpenJPA will return null for aggregate
     * expressions when the query result is empty.
     */
```

### JavadocReference
Cannot resolve symbol `create(String)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/SpecificationPlugin.java`
#### Snippet
```java
     * @param str can be null to set the Specification to null.
     * If non-null, then the String must be in Specification format
     * @see Specification#create(String)
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `fatal`
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/SpecificationPlugin.java`
#### Snippet
```java
     * it must have a major version number equal or less than the current one.
     *
     * @exception fatal UserException if the given Specification is same as
     * the current Specification but has a higher major version.
     *
```

### JavadocReference
Symbol `contextClassLoader` is inaccessible from here
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfiguration.java`
#### Snippet
```java

    /**
     * Indicates if the {@link Thread#contextClassLoader} should be used
     * as fallback if the result class for {@code select new} is not found
     * by the classloader of the entity.
```

### JavadocReference
Symbol `contextClassLoader` is inaccessible from here
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfiguration.java`
#### Snippet
```java

    /**
     * Indicates if the {@link Thread#contextClassLoader} should be used
     * as fallback if the result class for {@code select new} is not found
     * by the classloader of the entity.
```

### JavadocReference
Symbol `contextClassLoader` is inaccessible from here
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfiguration.java`
#### Snippet
```java

    /**
     * Indicates if the {@link Thread#contextClassLoader} should be used
     * as fallback if the result class for {@code select new} is not found
     * by the classloader of the entity.
```

### JavadocReference
Cannot resolve symbol `a`
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfiguration.java`
#### Snippet
```java
     * across named partitions of L2 data cache.
     *
     * @param a plug-in string for {@link CacheDistributionPolicy}.
     * @since 2.0.0
     */
```

### JavadocReference
Cannot resolve symbol `embedType`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
     * This should only occur after the embeddable is mapped.
     *
     * @param embedType  embeddable class
     * @param access class level access for embeddable
     * @throws SAXException
```

### JavadocReference
Cannot resolve symbol `access`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
     *
     * @param embedType  embeddable class
     * @param access class level access for embeddable
     * @throws SAXException
     */
```

### JavadocReference
Cannot resolve symbol `implType`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/StoreFacadeTypeRegistry.java`
#### Snippet
```java
     * @param storeType the store's
     * {@link org.apache.openjpa.kernel.StoreManager} type, or null for generic
     * @param implType the registered implementor
     */
    public Class getImplementation(Class facadeType, Class storeType) {
```

### JavadocReference
Cannot resolve symbol `implType`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/StoreFacadeTypeRegistry.java`
#### Snippet
```java
     * @param storeType the store's
     * {@link org.apache.openjpa.kernel.StoreManager} type, or null for generic
     * @param implType the registered implementor
     * @param defaultType class if no registered implementation is available.
     */
```

### JavadocReference
Cannot resolve symbol `SQLResultSetMappingMetaData`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
    /**
     * Parses the given annotation to create and cache a
     * {@link SQLResultSetMappingMetaData}.
     */
    private void parseSQLResultSetMappings(ClassMapping cm,
```

### JavadocReference
Symbol `printStackTrace` is inaccessible from here
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ExceptionInfo.java`
#### Snippet
```java
     * Stack.
     *
     * @see Throwable#printStackTrace
     */
    void printStackTrace();
```

### JavadocReference
Cannot resolve symbol `Type`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/asm/AsmHelper.java`
#### Snippet
```java
    /**
     * Get the internal names for the given classes
     * @see Type#getInternalName(Class) 
     */
    public static String[] getInternalNames(Class[] classes) {
```

### JavadocReference
Cannot resolve symbol `getInternalName(Class)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/asm/AsmHelper.java`
#### Snippet
```java
    /**
     * Get the internal names for the given classes
     * @see Type#getInternalName(Class) 
     */
    public static String[] getInternalNames(Class[] classes) {
```

### JavadocReference
Cannot resolve symbol `Type`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/asm/AsmHelper.java`
#### Snippet
```java
    /**
     * get the ASM Types for the given classes
     * @see Type#getType(Method)
     */
    public static Type[] getParamTypes(Class[] params) {
```

### JavadocReference
Cannot resolve symbol `getType(Method)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/asm/AsmHelper.java`
#### Snippet
```java
    /**
     * get the ASM Types for the given classes
     * @see Type#getType(Method)
     */
    public static Type[] getParamTypes(Class[] params) {
```

### JavadocReference
Cannot resolve symbol `Queue`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call after invoking {@link Queue#poll} on super.
     */
    public static Object afterPoll(ProxyCollection coll, Object removed) {
```

### JavadocReference
Cannot resolve symbol `poll`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call after invoking {@link Queue#poll} on super.
     */
    public static Object afterPoll(ProxyCollection coll, Object removed) {
```

### JavadocReference
Cannot resolve symbol `Queue`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call after invoking {@link Queue#offer(Object)} on super.
     *
     * @param added whether the object was added
```

### JavadocReference
Cannot resolve symbol `offer(Object)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call after invoking {@link Queue#offer(Object)} on super.
     *
     * @param added whether the object was added
```

### JavadocReference
Cannot resolve symbol `Queue`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call after invoking {@link Queue#remove} on super.
     */
    public static Object afterRemove(ProxyCollection coll, Object removed){
```

### JavadocReference
Cannot resolve symbol `remove`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call after invoking {@link Queue#remove} on super.
     */
    public static Object afterRemove(ProxyCollection coll, Object removed){
```

### JavadocReference
Cannot resolve symbol `Queue`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call before invoking {@link Queue#offer(Object)} on super.
     */
    public static void beforeOffer(ProxyCollection coll, Object value) {
```

### JavadocReference
Cannot resolve symbol `offer(Object)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call before invoking {@link Queue#offer(Object)} on super.
     */
    public static void beforeOffer(ProxyCollection coll, Object value) {
```

### JavadocReference
Cannot resolve symbol `Queue`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call before invoking {@link Queue#poll} on super.
     */
    public static void beforePoll(ProxyCollection coll) {
```

### JavadocReference
Cannot resolve symbol `poll`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call before invoking {@link Queue#poll} on super.
     */
    public static void beforePoll(ProxyCollection coll) {
```

### JavadocReference
Cannot resolve symbol `Queue`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call before invoking {@link Queue#remove} on super.
     */
    public static void beforeRemove(ProxyCollection coll) {
```

### JavadocReference
Cannot resolve symbol `remove`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java

    /**
     * Call before invoking {@link Queue#remove} on super.
     */
    public static void beforeRemove(ProxyCollection coll) {
```

### JavadocReference
Cannot resolve symbol `isObjectIdFieldIdOfPC`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
     * Values for PC primary key fields will be the primary key value or
     * oid value of the related instance (depending on
     * {@link FieldMetaData#isObjectIdFieldIdOfPC}).
     */
    public static Object[] toPKValues(Object oid, ClassMetaData meta) {
```

### JavadocReference
Cannot resolve symbol `isObjectIdFieldIdOfPC`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
     * Values for PC primary key fields should be the primary key value or
     * oid value of the related instance (depending on
     * {@link FieldMetaData#isObjectIdFieldIdOfPC}).
     */
    public static Object fromPKValues(Object[] pks, ClassMetaData meta) {
```

### JavadocReference
Cannot resolve symbol `remove(key, value)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
    /**
     * See OPENJPA-2442.
     * Call after invoking {@link Map#remove(key, value) } on super.
     *
     * @param ret the return value from the super's method
```

### JavadocReference
Cannot resolve symbol `key`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
    /**
     * See OPENJPA-2442.
     * Call after invoking {@link Map#remove(key, value) } on super.
     *
     * @param ret the return value from the super's method
```

### JavadocReference
Cannot resolve symbol `value`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
    /**
     * See OPENJPA-2442.
     * Call after invoking {@link Map#remove(key, value) } on super.
     *
     * @param ret the return value from the super's method
```

### JavadocReference
Cannot resolve symbol `loadXML`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java

    /**
     * Call before invoking {@link Properties#loadXML} on super.
     */
    public static void beforeLoadFromXML(ProxyMap map, InputStream in) {
```

### JavadocReference
Cannot resolve symbol `remove(key, value)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
    /**
     * See OPENJPA-2442.
     * Call before invoking {@link Map#remove(key, value) } on super.
     */
    public static boolean beforeRemove(ProxyMap map, Object key, Object value) {
```

### JavadocReference
Cannot resolve symbol `key`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
    /**
     * See OPENJPA-2442.
     * Call before invoking {@link Map#remove(key, value) } on super.
     */
    public static boolean beforeRemove(ProxyMap map, Object key, Object value) {
```

### JavadocReference
Cannot resolve symbol `value`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
    /**
     * See OPENJPA-2442.
     * Call before invoking {@link Map#remove(key, value) } on super.
     */
    public static boolean beforeRemove(ProxyMap map, Object key, Object value) {
```

### JavadocReference
Cannot resolve symbol `ConnectionRetainMode`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerFactory.java`
#### Snippet
```java
     *     obtained from JNDI.
     * @param managed Whether managed transactions will be used by this Broker
     * @param connRetainMode {@link ConnectionRetainMode}
     * @param findExisting Whether the internal pool of brokers should be used.
     * @param cfName  JTA ConnectionFactory to use
```

### JavadocReference
Cannot resolve symbol `assign()`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreManager.java`
#### Snippet
```java
     * @see org.apache.openjpa.util.ImplHelper#generateFieldValue
     * @see org.apache.openjpa.util.ImplHelper#generateIdentityValue
     * @see org.apache.openjpa.util.ApplicationIds#assign()
     * @since 0.3.3
     */
```

### JavadocReference
Cannot resolve symbol `assign()`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreManager.java`
#### Snippet
```java
     * commit.
     *
     * @see org.apache.openjpa.util.ApplicationIds#assign()
     */
    Collection<Exception> flush(Collection<OpenJPAStateManager> sms);
```

### JavadocReference
Cannot resolve symbol `makeTransient`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
    /**
     * Perform any actions necesssary and return the proper lifecycle state
     * on a call to {@link Broker#makeTransient} with the given instance.
     * Returns the <code>this</code> pointer by default.
     */
```

### JavadocReference
Cannot resolve symbol `fecth`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FinderCache.java`
#### Snippet
```java
     *
     * @param key for which the finder is looked up
     * @param fecth may contain hints to control lookup operation
     *
     * @return FinderQuery for the given mapping.
```

### JavadocReference
Cannot resolve symbol `FETCH_REF`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfiguration.java`
#### Snippet
```java
     * Affirms if the given field requires to be fetched in the context
     * of current fetch operation.  Returns one of {@link #FETCH_NONE},
     * {@link #FETCH_LOAD}, {@link FETCH_REF}.
     *
     * @since 0.4.1
```

### JavadocReference
Cannot resolve symbol `name`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfiguration.java`
#### Snippet
```java
     * Get the hint value for the specific key as originally set by the caller, or null if the hint is not specified.
     *
	 * @param name the hint name
	 * @since 0.4.0
	 */
```

### JavadocReference
Cannot resolve symbol `name`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfiguration.java`
#### Snippet
```java
     * argument being the same as the second.
     *
     * @param name the name of the hint
     * @param value the value of the hint. If the hint has a corresponding setter, then
     * the type of value must be same as the setter argument.
```

### JavadocReference
Cannot resolve symbol `markUncachable(String)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PreparedQueryCache.java`
#### Snippet
```java
	 * due to exclusion.
	 *
	 * @see #markUncachable(String)
	 * @see #setExcludes(String)
	 * @see #addExclusionPattern(String)
```

### JavadocReference
Cannot resolve symbol `markUncachable(String)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PreparedQueryCache.java`
#### Snippet
```java
 * (which is reversible or <em>weak</em>).
 *
 * @see #markUncachable(String)
 * @see #addExclusionPattern(String)
 * @see #setExcludes(String)
```

### JavadocReference
Cannot resolve symbol `ExpressionStoreManagerQuery`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreQuery.java`
#### Snippet
```java
 * Component that executes queries against the datastore. For
 * expression-based queries, consider subclassing
 * {@link ExpressionStoreManagerQuery}.
 *
 * @author Abe White
```

### JavadocReference
Cannot resolve symbol `getOrderingClauses`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreQuery.java`
#### Snippet
```java
        /**
         * Extract the value of the <code>orderIndex</code>th ordering
         * expression in {@link Query#getOrderingClauses} from the
         * given result object. The result object will be an object from
         * the result object provider returned from {@link #executeQuery}.
```

### JavadocReference
Cannot resolve symbol `lrs`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StoreQuery.java`
#### Snippet
```java
         * provider should be an object array containing the projection values.
         *
         * @param lrs true if the query result should be treated as a
         * large result set, assuming the query is not an
         * aggregate and does not have grouping
```

### JavadocReference
Cannot resolve symbol `eval(Object,Object,StoreContext,Map)`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Val.java`
#### Snippet
```java
     * Return a list of values resulting from evaluating each given
     * candidate. By default, this implementation invokes
     * {@link #eval(Object,Object,StoreContext,Map)} for
     * each instance and packs the return value into a new list. Aggregates
     * should override.
```

### JavadocReference
Cannot resolve symbol `ExpressionQuery`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/ExpressionParser.java`
#### Snippet
```java

/**
 * Parser for query languages that will be used by a {@link ExpressionQuery}.
 * A QueryParser is responsible for translating from some string-based query
 * language into {@link Expression}s. Parsers should be stateless.
```

### JavadocReference
Cannot resolve symbol `JPQL`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
/**
 * Builder for JPQL expressions. This class takes the query parsed
 * in {@link JPQL} and converts it to an expression tree using
 * an {@link ExpressionFactory}. Public for unit testing purposes.
 *
```

### JavadocReference
Cannot resolve symbol `JPQL`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
    /**
     * Base node that will be generated by the JPQLExpressionBuilder; base
     * class of the {@link SimpleNode} that is used by {@link JPQL}.
     *
     * @author Marc Prud'hommeaux
```

### JavadocReference
Cannot resolve symbol `Node`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
     *
     * @author Marc Prud'hommeaux
     * @see Node
     * @see SimpleNode
     */
```

### JavadocReference
Cannot resolve symbol `Cacheable`
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/CacheDistributionPolicy.java`
#### Snippet
```java
 * <br>
 * This policy is activated for each instance if and only if the cache settings specified in metadata
 * such as JPA specification defined {@link Cacheable @Cacheable} annotation or OpenJPA specific
 * {@link org.apache.openjpa.persistence.DataCache @DataCache} annotation or configuration property
 * such as <code>jakarta.persistence.sharedCache.mode</code> determined the type of the instance being cachable.
```

### JavadocReference
Symbol `org.apache.openjpa.persistence.DataCache` is inaccessible from here
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/CacheDistributionPolicy.java`
#### Snippet
```java
 * This policy is activated for each instance if and only if the cache settings specified in metadata
 * such as JPA specification defined {@link Cacheable @Cacheable} annotation or OpenJPA specific
 * {@link org.apache.openjpa.persistence.DataCache @DataCache} annotation or configuration property
 * such as <code>jakarta.persistence.sharedCache.mode</code> determined the type of the instance being cachable.
 * <br>
```

### JavadocReference
Cannot resolve symbol `level`
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/InstrumentationManager.java`
#### Snippet
```java
     * Stops all instruments for all managed providers for a given level
     * and context.
     * @param level  instrumentation level
     * @param context  instrumentation context (broker, factory, config,...)
     */
```

### JavadocReference
Cannot resolve symbol `properties`
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/PartitionedDataCache.java`
#### Snippet
```java
     * plug-in string to produce a list of
     *
     * @param properties property string of the form <code>(p1),(p2),(p3)</code>
     */
    private void parsePartitionProperties(String full) {
```

### JavadocReference
Cannot resolve symbol `cref`
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/JMXInstrument.java`
#### Snippet
```java
     * Gets the context reference for the instrument.  Required to register
     * the instrument under a unique id.
     * @param cref the context reference for the instrument
     */
    String getContextRef();
```

### JavadocReference
Cannot resolve symbol `process(ServletRequest, ServletResponse, JPAServletContext)`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTCommand.java`
#### Snippet
```java
     * Parse the given request to populate qualifiers and parameters of this command.
     * A command can interpret and consume certain path segments or parameters of the
     * original request. During {@link #process(ServletRequest, ServletResponse, JPAServletContext) processing}
     * phase, the parameters and qualifiers are accessed from the parsed command itself rather than
     * from the original HTTP request.
```

### JavadocReference
Cannot resolve symbol `DASH`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/IOR.java`
#### Snippet
```java
    /**
     * Stringified representation of a managed instance identity.
     * The simple Java type name and the persistent identity separated by a {@link Constants#DASH dash}.
     *
     * @param sm a managed instance.
```

### JavadocReference
Cannot resolve symbol `response`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ProcessingException.java`
#### Snippet
```java
     * Prints the stack trace in a HTML format on the given response output stream.
     *
     * @param response
     * @throws IOException
     */
```

### JavadocReference
Cannot resolve symbol `writer`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ObjectFormatter.java`
#### Snippet
```java
     *
     * @param model a meta-model of managed types
     * @param writer a text-oriented output stream
     *
     * @throws IOException
```

### JavadocReference
Cannot resolve symbol `writer`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ObjectFormatter.java`
#### Snippet
```java
     * @param desc TODO
     * @param uri TODO
     * @param writer a text-oriented output stream
     * @throws IOException
     */
```

### JavadocReference
Cannot resolve symbol `raises`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JPAServletContext.java`
#### Snippet
```java
     *
     * @return meta-data for the given name.
     * @exception raises runtime exception if the given name can not be identified to a persistent
     * Java type.
     */
```

### JavadocReference
Cannot resolve symbol `EntityManage`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JPAServletContext.java`
#### Snippet
```java

/**
 * An operating context provides a {@link EntityManage persistence context} and utility functions within
 * which all JEST commands execute.
 *
```

### JavadocReference
Cannot resolve symbol `replacements`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/TokenReplacedStream.java`
#### Snippet
```java
     * @param in a non-null input stream
     * @param out a character oriented writer
     * @param replacements an even number of Strings. Any occurrence of the even-indexed i-th String in the
     * input stream will be replaced by the (i+1)-th String in the output writer.
     */
```

### JavadocReference
Cannot resolve symbol `parent`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObjectFormatter.java`
#### Snippet
```java
     *
     * @param sm a managed instance, can be null.
     * @param parent the parent node to which the new node be attached.
     */
    private JSON encodeManagedInstance(final OpenJPAStateManager sm, Metamodel model) {
```

### JavadocReference
Cannot resolve symbol `parent`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObjectFormatter.java`
#### Snippet
```java
     *
     * @param sm the managed instance to be encoded. Can be null.
     * @param parent the parent XML element to which the new XML element be added. Must not be null. Must be
     * owned by a document.
     * @param visited the persistent instances that had been encoded already. Must not be null or immutable.
```

### JavadocReference
Cannot resolve symbol `sm`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/XMLFormatter.java`
#### Snippet
```java
     * according to JEST Instance XML Schema.
     *
     * @param sm a collection of managed instances.
     * @param parent the parent node to which the new node be attached.
     */
```

### JavadocReference
Cannot resolve symbol `parent`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/XMLFormatter.java`
#### Snippet
```java
     *
     * @param sm a collection of managed instances.
     * @param parent the parent node to which the new node be attached.
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `visited`
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/XMLFormatter.java`
#### Snippet
```java
     * @param parent the parent XML element to which the new XML element be added. Must not be null. Must be
     * owned by a document.
     * @param visited the persistent instances that had been encoded already. Must not be null or immutable.
     *
     * @return the new element. The element has been appended as a child to the given parent in this method.
```

### JavadocReference
Cannot resolve symbol `current`
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
     * Updates the buttons.
     *
     * @param current 0-based index of the currently displayed slide.
     */
    private void updateButtons(int index) {
```

### JavadocReference
Cannot resolve symbol `t`
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
     * followed by a pane (that toggles) for detailed stack traces.
     *
     * @param t a non-null exception
     */
    JComponent createContent() {
```

### JavadocReference
Cannot resolve symbol `uri`
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SourceCodeViewer.java`
#### Snippet
```java
     *
     * @param anchor a user visible description to identify the page
     * @param uri the unique resource location
     */
    public void addPage(String anchor, URI url) {
```

### JavadocReference
Cannot resolve symbol `DebugEventListener`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/TokenRenderer.java`
#### Snippet
```java
     * @param decision the index of the decision (or the context) in which
     * the current token is being processed. The index refers to ANTLR
     * {@link DebugEventListener#enterDecision(int)}.
     *
     * @param token the token to be rendered. Can be a hidden token as well.
```

### JavadocReference
Cannot resolve symbol `enterDecision(int)`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/TokenRenderer.java`
#### Snippet
```java
     * @param decision the index of the decision (or the context) in which
     * the current token is being processed. The index refers to ANTLR
     * {@link DebugEventListener#enterDecision(int)}.
     *
     * @param token the token to be rendered. Can be a hidden token as well.
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
     * <li><i>-properties/-p &lt;properties file&gt;</i>: The path to a OpenJPA
     * properties file containing information as outlined in
     * {@link Configuration}; optional.</li>
     * <li><i>-&lt;property name&gt; &lt;property value&gt;</i>: All bean
     * properties of the standard OpenJPA {@link OpenJPAConfiguration} can be
```

### JavadocReference
Symbol `readObject` is inaccessible from here
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
    /**
     * Adds a custom readObject method that delegates to the
     * {@link ObjectInputStream#readObject} method.
     */
    private void modifyReadObjectMethod(BCMethod method, boolean full) {
```

### JavadocReference
Cannot resolve symbol `decorators`
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookService.java`
#### Snippet
```java
     * @param max maximum price
     * @param author name of author
     * @param decorators to modify the executable query such as its range.
     */
    String getQuery(String title,
```

### JavadocReference
Cannot resolve symbol `x`
in `openjpa-examples/openbooks/src/main/java/openbook/domain/Range.java`
#### Snippet
```java
     * Minimum is included, maximum is excluded.
     *
     * @param x a non-null value
     * @return true if the given value is greater than or equals to minimum and less than the maximum.
     */
```

### JavadocReference
Cannot resolve symbol `Select`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/Join.java`
#### Snippet
```java
    /**
     * When joining a relation, set target type and how to deal with
     * subclasses.  See subclass constants in {@link #Select}.
     */
    public void setRelation(ClassMapping target, int subs, Joins joins) {
```

### JavadocReference
Cannot resolve symbol `deliver()`
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
     * Queries for books with low inventory and supply each book in separate
     * transaction. Some of the transactions may fail due to concurrent modification on
     * the {@linkplain Inventory} by the {@linkplain #deliver() delivery} process.
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `deliver()`
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
 * This example service operates on a persistent domain model to browse {@linkplain Book books},
 * occasionally {@linkplain #placeOrder(ShoppingCart) placing} {@linkplain PurchaseOrder purchase orders},
 * while {@linkplain Inventory inventory} gets updated either by {@linkplain #deliver() delivery} or
 * by {@linkplain #supply() supply}.
 * <br>
```

### JavadocReference
Cannot resolve symbol `supply()`
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
 * occasionally {@linkplain #placeOrder(ShoppingCart) placing} {@linkplain PurchaseOrder purchase orders},
 * while {@linkplain Inventory inventory} gets updated either by {@linkplain #deliver() delivery} or
 * by {@linkplain #supply() supply}.
 * <br>
 * The operational model as well as the persistent domain model is influenced by the fact that
```

### JavadocReference
Cannot resolve symbol `shop()`
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java

    /**
     * Range of number of queries executed for a {@linkplain #shop() shopping} trip.
     */
    public static final Range<Double> PRICE_RANGE  = new Range<>(4.99, 120.99);
```

### JavadocReference
Cannot resolve symbol `Val`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/Select.java`
#### Snippet
```java

    /**
     * Return the actual {@link Val}s and {@link Column}s that were
     * selected, in the order that they were selected.
     *
```

### JavadocReference
Cannot resolve symbol `http`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java

/**
 * Dictionary for H2 ({@link http://www.h2database.com}).
 *
 * @since 0.9.7
```

### JavadocReference
Cannot resolve symbol `Driver`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfiguration.java`
#### Snippet
```java
    /**
     * Create an instance of the {@link DriverDataSource} to use
     * for creating a {@link DataSource} from a JDBC {@link Driver}.
     */
    DriverDataSource newDriverDataSourceInstance();
```

### JavadocReference
Cannot resolve symbol `Driver`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfiguration.java`
#### Snippet
```java
    /**
     * The {@link DriverDataSource} to use for creating a {@link DataSource}
     * from a JDBC {@link Driver}.
     */
    String getDriverDataSource();
```

### JavadocReference
Cannot resolve symbol `Driver`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfiguration.java`
#### Snippet
```java
    /**
     * The {@link DriverDataSource} to use for creating a {@link DataSource}
     * from a JDBC {@link Driver}.
     */
    void setDriverDataSource(String driverDataSource);
```

### JavadocReference
Cannot resolve symbol `org.apache.openjpa.sql.Select`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ValueMapping.java`
#### Snippet
```java

    /**
     * Return the {@link org.apache.openjpa.sql.Select} subclasses constant
     * for loading this relation, based on how the related type is mapped,
     * whether this relation is polymorphic, and whether it is configured to
```

### JavadocReference
Symbol `newClassMapping` is inaccessible from here
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseCustomizer.java`
#### Snippet
```java
     * yourself using this method. When mapping, use
     * {@link ReverseMappingTool#generateClass} to create the class,
     * {@link ReverseMappingTool#newClassMapping} to create the class metadata,
     * and then {@link ClassMapping#addDeclaredFieldMapping} to add field
     * metadata.
```

### JavadocReference
Symbol `_indexPhysicalFK` is inaccessible from here
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * others (e.g. Oracle, MS-SQL-Server) do not.
     *
     * See also {@link org.apache.openjpa.jdbc.meta.MappingDefaultsImpl#_indexPhysicalFK}
     * which may disable this feature for backwards compatibility.
     */
```

### JavadocReference
Symbol `getKey` is inaccessible from here
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/FieldMapping.java`
#### Snippet
```java

    /**
     * Convenience method to perform cast from {@link FieldMetaData#getKey}
     */
    public ValueMapping getKeyMapping() {
```

### JavadocReference
Cannot resolve symbol `class`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
     * Resolve a fully qualified table name
     *
     * @param class
     *            mapping to get the schema name
     */
```

### JavadocReference
Cannot resolve symbol `columnsNames`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
    /**
     * Sets the names of the columns on which a unique constraint is set.
     * @param columnsNames are passed as a single String concatenated with
     * a '|' character. This method parses it back to array of Strings.
     */
```

### JavadocReference
Cannot resolve symbol `intValue.`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
     * Set the initial number in the table for the GeneratedValue.TABLE
     * strategy to use as initial number.
     * @param intValue. The initial number
     */
    public void setInitialValue(int intValue) {
```

### JavadocReference
Cannot resolve symbol `class`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
     * Resolve a fully qualified table name
     *
     * @param class
     *            mapping to get the schema name
     * @deprecated
```

### JavadocReference
Cannot resolve symbol `states`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ProjectionResultObjectProvider.java`
#### Snippet
```java
     * @param sel the select to execute
     * @param exps the query expressions
     * @param states the query expression states
     * @param ctx the query execution context
     */
```

### JavadocReference
Cannot resolve symbol `states`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ProjectionResultObjectProvider.java`
#### Snippet
```java
     * @param sel the select to execute
     * @param exps the query expressions
     * @param states the query expression states
     * @param ctx the query execution context
     */
```

### JavadocReference
Cannot resolve symbol `values`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
     * and fill in the given map.
     *
     * @param values a map of integer parameter index to parameter value
     * @param pc a manageable instance
     * @param indices the indices of the column values
```

### JavadocReference
Cannot resolve symbol `corresponding`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
     *
     * @param id an identifier for this query to be used as cache key
     * @param corresponding data store language query string
     * @param compiled a compiled query
     */
```

### JavadocReference
Cannot resolve symbol `newFinder(ClassMapping, Select)`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderCacheImpl.java`
#### Snippet
```java
     * create a new FinderQuery. The attempt, however, may not be successful
     * because all Selects can not be cached.
     * @see FinderQueryImpl#newFinder(ClassMapping, Select).
     *
     * If a entry for the given mapping exists then the value of
```

### JavadocReference
Cannot resolve symbol `Select`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderCacheImpl.java`
#### Snippet
```java
     * create a new FinderQuery. The attempt, however, may not be successful
     * because all Selects can not be cached.
     * @see FinderQueryImpl#newFinder(ClassMapping, Select).
     *
     * If a entry for the given mapping exists then the value of
```

### JavadocReference
Cannot resolve symbol `getJoins`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/BindVariableExpression.java`
#### Snippet
```java
/**
 * Binds a variable to a value. Typically, the {@link #initialize} and
 * {@link #getJoins} methods of this expression are not called. They are
 * only called if the variable being bound is otherwise unused in the filter,
 * in which case we must at least make the joins to the variable because the
```

### JavadocReference
Cannot resolve symbol `getJoins`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Variable.java`
#### Snippet
```java
/**
 * A variable in a filter. Typically, the {@link #initialize} and
 * {@link #getJoins} methods of this value are not called. They are
 * only called if the variable is bound but otherwise unused in the filter,
 * in which case we must at least make the joins to the variable because the
```

### JavadocReference
Cannot resolve symbol `table`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
     * @param checkForUniqueness
```

### JavadocReference
Cannot resolve symbol `checkForUniqueness`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param table
     * @param maxLen
     * @param checkForUniqueness
     */
    DBIdentifier getValidSequenceIdentifier(DBIdentifier name, Schema schema, int maxLen);
```

### JavadocReference
Cannot resolve symbol `checkForUniqueness`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param table
     * @param maxLen
     * @param checkForUniqueness
     */
    DBIdentifier getValidIndexIdentifier(DBIdentifier name, Table table, int maxLen);
```

### JavadocReference
Cannot resolve symbol `columns`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Converts the name of the specified delimiter to the appropriate
     * case as defined by the configuration.
     * @param columns
     */
    DBIdentifier convertSchemaCase(DBIdentifier schema);
```

### JavadocReference
Cannot resolve symbol `names`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Joins two names using the default identifier rules.
     * @param names
     */
    public static String joinNames(String name1, String name2) {
```

### JavadocReference
Cannot resolve symbol `name1`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Removes Hungarian notation from a string.
     * @param name1
     * @param name2
     */
```

### JavadocReference
Cannot resolve symbol `name2`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Removes Hungarian notation from a string.
     * @param name1
     * @param name2
     */
    public static String removeHungarianNotation(String name) {
```

### JavadocReference
Cannot resolve symbol `name1`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Combines multiple names using default identifier rules.
     * @param name1
     * @param name2
     */
```

### JavadocReference
Cannot resolve symbol `name2`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Combines multiple names using default identifier rules.
     * @param name1
     * @param name2
     */
    public static String combine(String...names) {
```

### JavadocReference
Cannot resolve symbol `length`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * Returns a new DBIdentifier with the given string appended.
     * @param name
     * @param length
     */
    public static DBIdentifier append(DBIdentifier name, String str) {
```

### JavadocReference
Cannot resolve symbol `length`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * delimiting rules and appropriate separators.
     * @param name
     * @param length
     */
    public static DBIdentifier combine(DBIdentifier name, String str) {
```

### JavadocReference
Cannot resolve symbol `addSlice(String, Map)`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributedBrokerFactory.java`
#### Snippet
```java
     * added. The keys must have openjpa.slice.&lt;name&gt;.* as prefix.
     *
     * @see DistributedConfiguration#addSlice(String, Map)
     */
    Slice addSlice(String name, Map properties);
```

### JavadocReference
Cannot resolve symbol `name`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributedStoreManager.java`
#### Snippet
```java
     * will participate in the current and subsequent transaction.
     *
     * @param name logical name of the to be added slice. Must be different from
     * any currently available slices.
     * @see DistributedBroker#addSlice(String, Map)
```

### JavadocReference
Cannot resolve symbol `addSlice(String, Map)`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributedStoreManager.java`
#### Snippet
```java
     * @param name logical name of the to be added slice. Must be different from
     * any currently available slices.
     * @see DistributedBroker#addSlice(String, Map)
     * @see DistributedBrokerFactory#addSlice(String, Map)
     *
```

### JavadocReference
Cannot resolve symbol `org.apache.maven.plugin.Mojo`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaEnhancerMojo.java`
#### Snippet
```java
     * {@inheritDoc}
     *
     * @see org.apache.maven.plugin.Mojo#execute()
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `execute()`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaEnhancerMojo.java`
#### Snippet
```java
     * {@inheritDoc}
     *
     * @see org.apache.maven.plugin.Mojo#execute()
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `MojoExecutionException`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaEnhancerMojo.java`
#### Snippet
```java
     *
     * @param files class file resources to enhance.
     * @throws MojoExecutionException if the enhancer encountered a failure
     */
    private void enhance(List<File> files) throws MojoExecutionException {
```

### JavadocReference
Cannot resolve symbol `MojoExecutionException`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java
     * classpaths (e.g. target/classes) needed by the OpenJPA task.
     *
     * @throws MojoExecutionException on any error inside the mojo
     */
    protected void extendRealmClasspath()
```

### JavadocReference
Cannot resolve symbol `MojoExecutionException`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java
     *
     * @param loader
     * @throws MojoExecutionException
     */
    private boolean fixPersistenceXmlIfNeeded(final ClassLoader loader) throws MojoExecutionException
```

### JavadocReference
Cannot resolve symbol `MojoExecutionException`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java
     *
     * @return list of class files.
     * @throws MojoExecutionException if there was an error scanning class file
     *             resources.
     */
```

### JavadocReference
Cannot resolve symbol `org.apache.maven.plugin.Mojo`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMappingToolMojo.java`
#### Snippet
```java
     * {@inheritDoc}
     *
     * @see org.apache.maven.plugin.Mojo#execute()
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `execute()`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMappingToolMojo.java`
#### Snippet
```java
     * {@inheritDoc}
     *
     * @see org.apache.maven.plugin.Mojo#execute()
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `MojoExecutionException`
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMappingToolMojo.java`
#### Snippet
```java
     *
     * @param files class file resources to map.
     * @throws MojoExecutionException if the MappingTool detected an error
     */
    private void mappingTool(List<File> files) throws MojoExecutionException {
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchPlanImpl.java`
#### Snippet
```java
            for (int i = 0; i < keys.length-1; i++) {
                List<String> list = new ArrayList<>(keys.length-i-1);
                for (int j = i+1; j < keys.length; j++) {
                    list.add(keys[j]);
                }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleFactory.java`
#### Snippet
```java
    public TupleFactory(Selection<?>... elems) {
        List<TupleElement<?>> list = new ArrayList<>();
        for (Selection<?> s : elems)
            list.add(s);
        elements = Collections.unmodifiableList(list);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinitionImpl.java`
#### Snippet
```java
			_groupBys.clear();
		}
		for (PathExpression e : pathExprs)
			_groupBys.add(e);
		return this;
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        }
        _orders = new ArrayList<>();
        for (Order o : orders) {
            _orders.add(o);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        }
        _groups = new ArrayList<>();
        for (Expression<?> e : grouping) {
            _groups.add(e);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                        return _cols;
                    }
                    for (Column fieldCol : fieldCols) {
                        cols.add(fieldCol);
                    }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
                return Collections.EMPTY_LIST;
            pkCols = pk.getColumns();
            for (Column pkCol : pkCols) {
                cols.add(pkCol);
            }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java
        for (Schema schema : schemas) {
            Table[] ts = schema.getTables();
            for (Table t : ts) {
                tables.add(t);
            }
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java

        int beforeRetPos = -1;
        int variableNr = helperParams.length;;

        // invoke before
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `openjpa-examples/openbooks/src/main/java/openbook/domain/PurchaseOrder.java`
#### Snippet
```java
     *
     */
    public enum Status {PENDING, DELIVERED};

    /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    // delegators

    public JavaLexer() {;}
    public JavaLexer(CharStream input) {
        this(input, new RecognizerSharedState());
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/SQLFormatter.java`
#### Snippet
```java

            clause = clauses.get(clauses.size() - 1);
            clause.append(sql.substring(start, end));

            clause = new StringBuilder();
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
                        quotIdx = props[i].indexOf(quote);
                        if (quotIdx != -1) {
                            buf.append(props[i].substring(0, quotIdx));
                            if (quotIdx + 1 < props[i].length())
                                buf.append(props[i].substring(quotIdx + 1));
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
                buf.append('\\').append(c);
            else if (c < ' ' || c > '~')
                buf.append("\\u0000".substring(0, 6 - Integer.toHexString(c).
                    length())).append(Integer.toHexString(c));
            else
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
     */
    private String splitNameCharDelimiters(String name, String nameDelim) {
        StringBuilder sname = new StringBuilder("");
        char ld = _config.getLeadingDelimiter().charAt(0);
        char td = _config.getTrailingDelimiter().charAt(0);
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/PartitionedDataCache.java`
#### Snippet
```java
     */
    private void parsePartitionProperties(String full) {
        String properties = new String(full);
        while (true) {
            if (properties == null)
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ProcessingException.java`
#### Snippet
```java
        response.setStatus(_errorCode);

        String uri = ctx.getRequestURI().toString();
        try {
            uri = URLDecoder.decode(uri, "UTF-8");
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                buf = new StringBuffer();
                if (i > 0)
                    buf.append (s.substring (0, i));
			}
			return buf;
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtilImpl.java`
#### Snippet
```java
            return toDBName(columns[0].getIdentifier());
        }
        StringBuilder colsb = new StringBuilder("");
        for (int i = 0; i < columns.length; i++) {
            colsb.append(toDBName(columns[i].getIdentifier()));
```

## RuleId[id=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `X` hides type parameter 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java

    @Override
    public <X,Y> Fetch<X, Y> fetch(String assocName, JoinType jt) {
        Attribute<? super X, ?> assoc = ((ManagedType<X>)_member.getType()).getAttribute(assocName);
        return addFetch((Members.Member<? super X,Y>)assoc, jt);
```

### TypeParameterHidesVisibleType
Type parameter `X` hides type parameter 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java

    @Override
    public <X,Y> Fetch<X, Y> fetch(String assocName) {
        return fetch(assocName, JoinType.INNER);
    }
```

### TypeParameterHidesVisibleType
Type parameter `X` hides type parameter 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    }

    private <X,Y> Fetch<X,Y> addFetch(Members.Member<? super X, Y> member, JoinType jt) {
        Fetch<X,Y> fetch = new FetchPathImpl(this, member, jt);
        if (_fetches == null)
```

### TypeParameterHidesVisibleType
Type parameter `X` hides type parameter 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java

    @Override
    public <X,Y> Fetch<X, Y> fetch(String assocName) {
        return fetch(assocName, JoinType.INNER);
    }
```

### TypeParameterHidesVisibleType
Type parameter `X` hides type parameter 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java

    @Override
    public <X,Y> Fetch<X, Y> fetch(String name, JoinType jt) {
        assertJoinable(type);
        Attribute<? super X,?> attr = ((ManagedType<X>)type).getAttribute(name);
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java
     * Delegating iterator that also performs exception translation.
     */
    public class DelegatingListIterator<T>
        implements ListIterator<T> {

```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedTemplate.java`
#### Snippet
```java

    @Override
    public <T> T unwrap(Class<T> arg0) throws SQLException {
        throw new UnsupportedOperationException();
    }
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                } else if ( type == Character.class ) {
                    if (_dict.storeCharsAsNumbers) {
                        _sql.append(Integer.toString((Character) o));
                    } else {
                        _sql.append("'" + o.toString().replace("'", "''") + "'");
```

## RuleId[id=WebProperties]
### WebProperties
Cannot resolve class or package 'demo'
in `openjpa-examples/jest/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java

		<servlet-name>demo</servlet-name>
		<servlet-class>demo.SimpleApp</servlet-class>
	</servlet>
	<servlet-mapping>
```

### WebProperties
Cannot resolve class 'SimpleApp'
in `openjpa-examples/jest/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java

		<servlet-name>demo</servlet-name>
		<servlet-class>demo.SimpleApp</servlet-class>
	</servlet>
	<servlet-mapping>
```

### WebProperties
Cannot resolve class or package 'apache'
in `openjpa-examples/jest/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
  		</description>
		<servlet-name>jest</servlet-name>
		<servlet-class>org.apache.openjpa.persistence.jest.JESTServlet</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class or package 'openjpa'
in `openjpa-examples/jest/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
  		</description>
		<servlet-name>jest</servlet-name>
		<servlet-class>org.apache.openjpa.persistence.jest.JESTServlet</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class or package 'persistence'
in `openjpa-examples/jest/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
  		</description>
		<servlet-name>jest</servlet-name>
		<servlet-class>org.apache.openjpa.persistence.jest.JESTServlet</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class or package 'jest'
in `openjpa-examples/jest/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
  		</description>
		<servlet-name>jest</servlet-name>
		<servlet-class>org.apache.openjpa.persistence.jest.JESTServlet</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class 'JESTServlet'
in `openjpa-examples/jest/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
  		</description>
		<servlet-name>jest</servlet-name>
		<servlet-class>org.apache.openjpa.persistence.jest.JESTServlet</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class or package 'apache'
in `openjpa-examples/opentrader/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
	<servlet>
		<servlet-name>opentrader</servlet-name>
		<servlet-class>org.apache.openjpa.trader.server.TradingServiceAdapterImpl</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class or package 'openjpa'
in `openjpa-examples/opentrader/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
	<servlet>
		<servlet-name>opentrader</servlet-name>
		<servlet-class>org.apache.openjpa.trader.server.TradingServiceAdapterImpl</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class or package 'trader'
in `openjpa-examples/opentrader/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
	<servlet>
		<servlet-name>opentrader</servlet-name>
		<servlet-class>org.apache.openjpa.trader.server.TradingServiceAdapterImpl</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class or package 'server'
in `openjpa-examples/opentrader/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
	<servlet>
		<servlet-name>opentrader</servlet-name>
		<servlet-class>org.apache.openjpa.trader.server.TradingServiceAdapterImpl</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

### WebProperties
Cannot resolve class 'TradingServiceAdapterImpl'
in `openjpa-examples/opentrader/src/main/resources/WEB-INF/web.xml`
#### Snippet
```java
	<servlet>
		<servlet-name>opentrader</servlet-name>
		<servlet-class>org.apache.openjpa.trader.server.TradingServiceAdapterImpl</servlet-class>
		<init-param>
			<param-name>persistence.unit</param-name>
```

## RuleId[id=DuplicatedCode]
### DuplicatedCode
Duplicated code
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
            if (c == boolean.class) return Boolean.class;
            if (c == byte.class) return Byte.class;
            if (c == char.class) return Character.class;
            if (c == double.class) return Double.class;
            if (c == float.class) return Float.class;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/ant/ReverseMappingToolTask.java`
#### Snippet
```java
        if (propsFile != null && AccessController.doPrivileged(
                J2DoPrivHelper.existsAction(propsFile))) {
            FileInputStream fis = null;
            try {
                fis = AccessController.doPrivileged(
                    J2DoPrivHelper.newFileInputStreamAction(propsFile));
            } catch (PrivilegedActionException pae) {
                 throw (FileNotFoundException) pae.getException();
            }
            customProps.load(fis);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingConstraintUpdateManager.java`
#### Snippet
```java
        exceps = super.flush(rowMgr, psMgr, exceps);
        BatchingPreparedStatementManagerImpl bPsMgr =
            (BatchingPreparedStatementManagerImpl) psMgr;
        try {
            bPsMgr.flushBatch();
        } catch (SQLException se) {
            exceps = addException(exceps, SQLExceptions.getStore(se, dict));
        } catch (OpenJPAException ke) {
            exceps = addException(exceps, ke);
        }

        return exceps;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ClassTableJDBCSeq.java`
#### Snippet
```java
        DBDictionary dict = getConfiguration().getDBDictionaryInstance();
        Column pkColumn = table.addColumn(dict.getValidColumnName(getPrimaryKeyColumnIdentifier(), table));
        pkColumn.setType(dict.getPreferredType(Types.VARCHAR));
        pkColumn.setJavaType(JavaTypes.STRING);
        pkColumn.setSize(dict.characterColumnSize);
        return pkColumn;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        ClassMapping[] mappings = (ClassMapping[]) metas;
        JDBCFetchConfiguration fetch = (JDBCFetchConfiguration)
            ctx.getFetchConfiguration();
        if (exps[0].fetchPaths != null) {
            fetch.addFields(Arrays.asList(exps[0].fetchPaths));
            fetch.addJoins(Arrays.asList(exps[0].fetchPaths));
        }
        if (exps[0].fetchInnerPaths != null)
            fetch.addFetchInnerJoins(Arrays.asList(exps[0].fetchInnerPaths));

        int eager = calculateEagerMode(exps[0], range.start, range.end);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        JDBCFetchConfiguration fetch = (JDBCFetchConfiguration) ctx.
            getFetchConfiguration();
        if (exps[0].fetchPaths != null) {
            fetch.addFields(Arrays.asList(exps[0].fetchPaths));
            fetch.addJoins(Arrays.asList(exps[0].fetchPaths));
        }
        if (exps[0].fetchInnerPaths != null)
            fetch.addFetchInnerJoins(Arrays.asList(exps[0].fetchInnerPaths));

        int eager = calculateEagerMode(exps[0], range.start, range.end);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        int subclassMode = fetch.getSubclassFetchMode((ClassMapping) base);
        DBDictionary dict = _store.getDBDictionary();
        long start = (mappings.length == 1 && dict.supportsSelectStartIndex)
            ? range.start : 0L;
        long end = (dict.supportsSelectEndIndex) ? range.end : Long.MAX_VALUE;

        QueryExpressionsState[] states = new QueryExpressionsState[exps.length];
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Abs.java`
#### Snippet
```java
        Class wrap = Filters.wrap(c);
        if (wrap == Integer.class
            || wrap == Float.class
            || wrap == Double.class
            || wrap == Long.class
            || wrap == BigDecimal.class
            || wrap == BigInteger.class)
            return Filters.unwrap(c);
        return int.class;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Args.java`
#### Snippet
```java
        ExpState[] states = new ExpState[_args.length];
        Joins joins = null;
        for (int i = 0; i < _args.length; i++) {
            states[i] = _args[i].initialize(sel, ctx, flags);
            if (joins == null)
                joins = states[i].joins;
            else
                joins = sel.and(joins, states[i].joins);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EndsWithExpression.java`
#### Snippet
```java
            Object o = ((Const) _val2).getValue(ctx, bstate.state2);
            if (o == null)
                buf.append("1 <> 1");
            else {
                Column col = null;
                if (_val1 instanceof PCPath) {
                    Column[] cols = ((PCPath) _val1).getColumns(bstate.state1);
                    if (cols.length == 1)
                        col = cols[0];
                }

                _val1.appendTo(sel, ctx, bstate.state1, buf, 0);
                buf.append(" LIKE ");
                buf.appendValue("%" + o.toString(), col);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EqualExpression.java`
#### Snippet
```java
            Val val = (val1Null) ? getValue2() : getValue1();
            ExpState state = (val1Null) ? bstate.state2 : bstate.state1;
            if (!isDirectComparison()) {
                int len = val.length(sel, ctx, state);
                for (int i = 0; i < len; i++) {
                    if (i > 0)
                        buf.append(" AND ");
                    val.appendTo(sel, ctx, state, buf, i);
                    buf.append(" IS ").appendValue(null);
                }
            } else
                val.appendIsNull(sel, ctx, state, buf);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EqualExpression.java`
#### Snippet
```java
                for (int i = 0; i < len; i++) {
                    if (i > 0)
                        buf.append(" AND ");

                    val1.appendTo(sel, ctx, bstate.state1, buf, i);
                    buf.append(" = ");
                    val2.appendTo(sel, ctx, bstate.state2, buf, i);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EqualTypeExpression.java`
#### Snippet
```java
        ClassMapping sub = null;
        Val val = val1 instanceof Type ? val2 : val1;
        if (val instanceof TypeLit)
            sub = (ClassMapping) val.getMetaData();
        else if (val instanceof Param)
            sub = ((Param) val).getValueMetaData(ctx);
        if (sub != null)
            ctx.isVerticalStrat = sub.isVerticalStrategy();
        return sub;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EqualTypeExpression.java`
#### Snippet
```java
        Val val1 = getValue1();
        Val val2 = getValue2();
        ClassMapping sub = getSubClassMapping(val1, val2, ctx);
        if (ctx.isVerticalStrat) {
            appendTo(sel, ctx, (BinaryOpExpState)state, buf, false, false);
            return;
        }
        super.appendTo(sel, ctx, state, buf);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/IndexOf.java`
#### Snippet
```java
        BinaryOpExpState bstate = (BinaryOpExpState) state;
        FilterValue str = new FilterValueImpl(sel, ctx, bstate.state1, _val1);
        FilterValue search;
        FilterValue start = null;
        if (_val2 instanceof Args) {
            FilterValue[] filts = ((Args) _val2).newFilterValues(sel, ctx,
                bstate.state2);
            search = filts[0];
            start = filts[1];
        } else
            search = new FilterValueImpl(sel, ctx, bstate.state2, _val2);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/NotEqualExpression.java`
#### Snippet
```java
            Val val = (val1Null) ? getValue2() : getValue1();
            ExpState state = (val1Null) ? bstate.state2 : bstate.state1;
            if (!isDirectComparison()) {
                int len = val.length(sel, ctx, state);
                for (int i = 0; i < len; i++) {
                    if (i > 0)
                        buf.append(" AND ");
                    val.appendTo(sel, ctx, state, buf, i);
                    buf.append(" IS NOT ").appendValue(null);
                }
            } else
                val.appendIsNotNull(sel, ctx, state, buf);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/NotEqualExpression.java`
#### Snippet
```java
                int len = java.lang.Math.max(val1.length(sel, ctx,
                    bstate.state1), val2.length(sel, ctx, bstate.state2));
                buf.append("(");
                for (int i = 0; i < len; i++) {
                    if (i > 0)
                        buf.append(" OR ");
                    val1.appendTo(sel, ctx, bstate.state1, buf, i);
                    buf.append(" <> ");
                    val2.appendTo(sel, ctx, bstate.state2, buf, i);
                }
                buf.append(")");
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
            sm = null;
            tmpBroker = null;
            if (ImplHelper.isManageable(candidate))
                sm = (OpenJPAStateManager) (ImplHelper.toPersistenceCapable(
                        candidate, ctx.getConfiguration())).
                        pcGetStateManager();
            if (sm == null) {
                tmpBroker = ctx.getBroker();
                tmpBroker.transactional(candidate, false, null);
                sm = tmpBroker.getStateManager(candidate);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/ToLowerCase.java`
#### Snippet
```java
        if (getValue() instanceof PCPath) {
            func = dict.getCastFunction(getValue(), func, ((PCPath) getValue()).getFieldMapping(state).getColumns()[0]);
        } else {
            func = dict.getCastFunction(getValue(), func);
        }

        int idx = func.indexOf("{0}");
        buf.append(func.substring(0, idx));
        getValue().appendTo(sel, ctx, state, buf, index);
        buf.append(func.substring(idx + 3));
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
            ColumnIO io = getColumnIO();
            for (int i = 0; i < _cols.length; i++) {
                if (io.isInsertable(i, false))
                    _cols[i].setFlag(Column.FLAG_DIRECT_INSERT, true);
                if (io.isUpdatable(i, false))
                    _cols[i].setFlag(Column.FLAG_DIRECT_UPDATE, true);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
        ForeignKeyDefaults def = new ForeignKeyDefaults() {
            @Override
            public ForeignKey get(Table local, Table foreign, boolean inverse) {
                return cls.getMappingRepository().getMappingDefaults().
                    getJoinForeignKey(cls, local, foreign);
            }

            @Override
            public void populate(Table local, Table foreign, Column col,
                Object target, boolean inverse, int pos, int cols) {
                cls.getMappingRepository().getMappingDefaults().
                    populateJoinColumn(cls, local, foreign, col, target,
                        pos, cols);
            }
        };
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/Discriminator.java`
#### Snippet
```java
        if ((_resMode & mode) == mode)
            return true;
        int cur = _resMode;
        _resMode |= mode;
        if ((mode & MODE_MAPPING) != 0 && (cur & MODE_MAPPING) == 0)
            resolveMapping();
        if ((mode & MODE_MAPPING_INIT) != 0 && (cur & MODE_MAPPING_INIT) == 0)
            _strategy.initialize();
        return false;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/Discriminator.java`
#### Snippet
```java
        MappingRepository repos = getMappingRepository();
        if (_strategy == null)
            repos.getStrategyInstaller().installStrategy(this);
        Log log = repos.getLog();
        if (log.isTraceEnabled())
            log.trace(_loc.get("strategy", this, _strategy.getAlias()));

        // mark columns as mapped
        Column[] cols = getColumns();
        ColumnIO io = getColumnIO();
        for (int i = 0; i < cols.length; i++) {
            if (io.isInsertable(i, false))
                cols[i].setFlag(Column.FLAG_DIRECT_INSERT, true);
            if (io.isUpdatable(i, false))
                cols[i].setFlag(Column.FLAG_DIRECT_UPDATE, true);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/FieldMapping.java`
#### Snippet
```java
        ForeignKey jfk = getJoinForeignKey();
        if (jfk == null)
        	return false;
        FieldMapping mappedBy = getValueMappedByMapping();
        if (mappedBy != null)
        	return false;
        ValueMapping elem = getElementMapping();
        if (elem == null)
        	return false;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/FieldMapping.java`
#### Snippet
```java
                ForeignKey rjfk = rfm.getJoinForeignKey();
                if (rjfk == null)
                    continue;
                if (rjfk.getTable() == jfk.getTable() &&
                        jfk.getTable().getColumns().length ==
                                jfk.getColumns().length + rjfk.getColumns().length) {
                    _bidirectionalJoinTableOwner = true;
                    break;
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingDefaultsImpl.java`
#### Snippet
```java
        for (int i = 0; i < cols.length; i++) {
            if (!DBIdentifier.isNull(_dsIdName) && cols.length == 1)
                cols[i].setIdentifier(_dsIdName);
            else if (!DBIdentifier.isNull(_dsIdName))
                cols[i].setIdentifier(DBIdentifier.append(_dsIdName, Integer.toString(i)));
            correctName(table, cols[i]);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
        FieldMetaData[] fmds = meta.getDeclaredFields();
        for (FieldMetaData fmd : fmds) {
            if (fmd.getDeclaredTypeCode() == JavaTypes.OBJECT
                    && fmd.getDeclaredType() != Object.class)
                fmd.setDeclaredTypeCode(JavaTypes.PC);
        }
        meta.setSource(_file, meta.getSourceType(), _file == null ? "": _file.getPath() );
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/RefreshStrategyInstaller.java`
#### Snippet
```java
        try {
            cls.setStrategy(strat, Boolean.TRUE);
        } catch (MetaDataException mde) {
            // if this is a custom strategy, don't attempt to override
            if (isCustomStrategy(strat))
                throw mde;

            repos.getLog().warn(_loc.get("fatal-change", cls,
                mde.getMessage()));
            cls.clearMapping();
            cls.setStrategy(repos.defaultStrategy(cls, true), Boolean.TRUE);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        ClassMapping[] mappings = getMappings();
        for (ClassMapping classMapping : mappings) {
            classMapping.setResolve(MODE_META | MODE_MAPPING, true);
            classMapping.setUseSchemaElement(getUseSchemaElement());
        }
        // store in user's configured IO
        MetaDataFactory mdf = _conf.newMetaDataFactoryInstance();
        mdf.setRepository(getRepository());
        mdf.setStoreDirectory(_dir);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedFieldStrategy.java`
#### Snippet
```java
        assertNotMappedBy();

        // map join key (if any)
        field.mapJoin(adapt, false);
        field.getKeyMapping().getValueInfo().assertNoSchemaComponents
            (field.getKey(), !adapt);
        field.getElementMapping().getValueInfo().assertNoSchemaComponents
            (field.getElement(), !adapt);

        ValueMappingInfo vinfo = field.getValueInfo();
        vinfo.assertNoJoin(field, true);
        vinfo.assertNoForeignKey(field, !adapt);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerCollectionTableFieldStrategy.java`
#### Snippet
```java
        Object obj = sm.fetchObject(field.getIndex());
        ChangeTracker ct = null;
        if (obj instanceof Proxy) {
            Proxy proxy = (Proxy) obj;
            if (Proxies.isOwner(proxy, sm, field.getIndex()))
                ct = proxy.getChangeTracker();
        }

        Column order = field.getOrderColumn();
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerCollectionTableFieldStrategy.java`
#### Snippet
```java
        Collection rem = ct.getRemoved();
        if (!rem.isEmpty()) {
            Row delRow = rm.getSecondaryRow(field.getTable(),
                Row.ACTION_DELETE);
            delRow.whereForeignKey(field.getJoinForeignKey(), sm);
            for (Object o : rem) {
                HandlerStrategies.where(elem, o, store, delRow,
                        _cols);
                rm.flushSecondaryRow(delRow);
            }
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerFieldStrategy.java`
#### Snippet
```java
            if (row != null) {
                Object value = sm.fetch(field.getIndex());
                if (!HandlerStrategies.set(field, value, store, row, _cols,
                    _io, field.getNullValue() == FieldMetaData.NULL_NONE))
                    if (field.getValueStrategy() != ValueStrategies.AUTOASSIGN)
                        throw new UserException(_loc.get("cant-set-value", row
                            .getFailedObject(), field, value));
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerHandlerMapTableFieldStrategy.java`
#### Snippet
```java
        Map map = (Map) sm.fetchObject(field.getIndex());
        ChangeTracker ct = null;
        if (map instanceof Proxy) {
            Proxy proxy = (Proxy) map;
            if (Proxies.isOwner(proxy, sm, field.getIndex()))
                ct = proxy.getChangeTracker();
        }

        // if no fine-grained change tracking then just delete and reinsert
        if (ct == null || !ct.isTracking()) {
            delete(sm, store, rm);
            insert(sm, store, rm, map);
            return;
        }

        // delete the removes
        ValueMapping key = field.getKeyMapping();
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerRelationMapTableFieldStrategy.java`
#### Snippet
```java
        if (map == null || map.isEmpty())
            return;

        if (!field.isBiMTo1JT() && field.getMappedBy() != null)
            return;

        Row row = null;
        if (!field.isUni1ToMFK()) {
            row = rm.getSecondaryRow(field.getTable(), Row.ACTION_INSERT);
            row.setForeignKey(field.getJoinForeignKey(), field.getJoinColumnIO(),
                sm);
        }
        ValueMapping key = field.getKeyMapping();
        ValueMapping val = field.getElementMapping();
        StoreContext ctx = store.getContext();
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerRelationMapTableFieldStrategy.java`
#### Snippet
```java
            valsm = RelationStrategies.getStateManager(entry.getValue(),
                    ctx);
            if (field.isUni1ToMFK()) {
                row = rm.getRow(field.getElementMapping().getDeclaredTypeMapping().getTable(),
                        Row.ACTION_UPDATE, valsm, true);
                row.wherePrimaryKey(valsm);
                val.setForeignKey(row, sm);
            }
            else {
                val.setForeignKey(row, valsm);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerRelationMapTableFieldStrategy.java`
#### Snippet
```java
        if (field.getMappedBy() != null && !field.isBiMTo1JT())
            return;

        Map map = (Map) sm.fetchObject(field.getIndex());
        ChangeTracker ct = null;
        if (map instanceof Proxy) {
            Proxy proxy = (Proxy) map;
            if (Proxies.isOwner(proxy, sm, field.getIndex()))
                ct = proxy.getChangeTracker();
        }

        // if no fine-grained change tracking then just delete and reinsert
        if (ct == null || !ct.isTracking()) {
            delete(sm, store, rm);
            insert(sm, store, rm, map);
            return;
        }

        ValueMapping key = field.getKeyMapping();
        ValueMapping val = field.getElementMapping();
        StoreContext ctx = store.getContext();
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerRelationMapTableFieldStrategy.java`
#### Snippet
```java
                mkey = o;
                valsm = RelationStrategies.getStateManager(map.get(mkey), ctx);
                if (field.isUni1ToMFK()) {
                    changeRow = rm.getRow(field.getElementMapping().getDeclaredTypeMapping().getTable(),
                            Row.ACTION_UPDATE, valsm, true);
                    changeRow.wherePrimaryKey(valsm);
                    val.setForeignKey(changeRow, sm);
                }
                else {
                    val.setForeignKey(changeRow, valsm);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerRelationMapTableFieldStrategy.java`
#### Snippet
```java
            for (Object value : add) {
                mkey = value;
                valsm = RelationStrategies.getStateManager(map.get(mkey), ctx);
                if (field.isUni1ToMFK()) {
                    addRow = rm.getRow(field.getElementMapping().getDeclaredTypeMapping().getTable(),
                            Row.ACTION_UPDATE, valsm, true);
                    addRow.wherePrimaryKey(valsm);
                    val.setForeignKey(addRow, sm);
                }
                else {
                    val.setForeignKey(addRow, valsm);
                }

                HandlerStrategies.set(key, mkey, store, addRow, _kcols,
                        _kio, true);
                if (!field.isUni1ToMFK())
                    rm.flushSecondaryRow(addRow);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerRelationMapTableFieldStrategy.java`
#### Snippet
```java
        ValueMapping val = field.getElementMapping();
        ClassMapping[] clss = val.getIndependentTypeMappings();
        if (clss.length != 1) {
            if (traverse)
                throw RelationStrategies.unjoinable(val);
            return joins;
        }
        ForeignKey fk = val.getForeignKey(clss[0]);
        if (fk == null)
            return joins;
        if (forceOuter)
            return joins.outerJoinRelation(field.getName(),
                fk, clss[0], val.getSelectSubclasses(),
                false, false);
        return joins.joinRelation(field.getName(),
                fk, clss[0], val.getSelectSubclasses(),
            false, false);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LRSProxyMap.java`
#### Snippet
```java
        final OpenJPAStateManager sm = assertOwner();
        final JDBCStore store = getStore();
        Union union = store.getSQLFactory().newUnion
            (Math.max(1, clss.length));
        union.select(new Union.Selector() {
            @Override
            public void select(Select sel, int idx) {
                ClassMapping cls = (clss.length == 0) ? null : clss[idx];
                sel.whereForeignKey(_strat.getJoinForeignKey(cls),
                    sm.getObjectId(), _strat.getFieldMapping().
                    getDefiningMapping(), store);
            }
        });

        try {
            return union.getCount(store);
        } catch (SQLException se) {
            throw SQLExceptions.getStore(se, store.getDBDictionary());
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LRSProxyCollection.java`
#### Snippet
```java
        union.select(new Union.Selector() {
            @Override
            public void select(Select sel, int idx) {
                ClassMapping elem = (elems.length == 0) ? null : elems[idx];
                sel.whereForeignKey(_strat.getJoinForeignKey(elem),
                    sm.getObjectId(), _strat.getFieldMapping().
                    getDefiningMapping(), store);
            }
        });

        try {
            return union.getCount(store);
        } catch (SQLException se) {
            throw SQLExceptions.getStore(se, store.getDBDictionary());
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LRSProxyCollection.java`
#### Snippet
```java
                Object[] vals = (cols.length == 1) ? null : (Object[]) val;
                SQLBuffer sql = new SQLBuffer(store.getDBDictionary());
                for (int i = 0; i < cols.length; i++) {
                    if (i > 0)
                        sql.append(" AND ");

                    sql.append(sel.getColumnAlias(cols[i]));
                    if (vals == null)
                        sql.append((val == null) ? " IS " : " = ").
                            appendValue(val, cols[i]);
                    else
                        sql.append((vals[i] == null) ? " IS " : " = ").
                            appendValue(vals[i], cols[i]);
                }
                sel.where(sql);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LobFieldStrategy.java`
#### Snippet
```java
        ValueMappingInfo vinfo = field.getValueInfo();
        vinfo.assertNoJoin(field, true);
        vinfo.assertNoForeignKey(field, !adapt);
        DBDictionary dict = field.getMappingRepository().getDBDictionary();
        DBIdentifier fieldName = DBIdentifier.newColumn(field.getName(), dict != null ? dict.delimitAll() : false);

        Column tmpCol = new Column();
        tmpCol.setIdentifier(fieldName);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/PrimitiveFieldStrategy.java`
#### Snippet
```java
        field.setColumns(cols);
        field.setColumnIO(vinfo.getColumnIO());
        field.mapConstraints(fieldName, adapt);

        // add primary key columns to table pk if logical
        field.mapPrimaryKey(adapt);
        PrimaryKey pk = field.getTable().getPrimaryKey();
        if (field.isPrimaryKey() && pk != null && (adapt || pk.isLogical()))
            pk.addColumn(cols[0]);

        // set joinable
        field.getDefiningMapping().setJoinable(field.getColumns()[0], this);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationHandlerMapTableFieldStrategy.java`
#### Snippet
```java
        if (!rem.isEmpty()) {
            Row delRow = rm.getSecondaryRow(field.getTable(),
                Row.ACTION_DELETE);
            delRow.whereForeignKey(field.getJoinForeignKey(), sm);
            for (Object o : rem) {
                keysm = RelationStrategies.getStateManager(o, ctx);
                key.whereForeignKey(delRow, keysm);
                rm.flushSecondaryRow(delRow);
            }
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationMapInverseKeyFieldStrategy.java`
#### Snippet
```java
        ValueMapping val = field.getElementMapping();
        final ClassMapping[] vals = val.getIndependentTypeMappings();
        Union union = store.getSQLFactory().newUnion(vals.length);
        if (fetch.getSubclassFetchMode(val.getTypeMapping())
            != EagerFetchModes.EAGER_JOIN)
            union.abortUnion();
        union.setLRS(lrs);
        union.select(new Union.Selector() {
            @Override
            public void select(Select sel, int idx) {
                joins[1] = selectAll(sel, vals[idx], sm, store, fetch,
                    eagerMode);
            }
        });
        Result res = union.execute(store, fetch);
        return new Result[]{ res, res };
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationMapInverseKeyFieldStrategy.java`
#### Snippet
```java
        ValueMapping key = field.getKeyMapping();
        if (key.isEmbedded())
            return joins;

        ClassMapping[] clss = key.getIndependentTypeMappings();
        if (clss.length != 1) {
            if (traverse)
                throw RelationStrategies.unjoinable(key);
            return joins;
        }
        if (forceOuter)
            return joins.outerJoinRelation(field.getName(),
                key.getForeignKey(clss[0]), clss[0], key.getSelectSubclasses(),
                false, false);
        return joins.joinRelation(field.getName(),
            key.getForeignKey(clss[0]), clss[0], key.getSelectSubclasses(),
            false, false);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationRelationMapTableFieldStrategy.java`
#### Snippet
```java
        ClassMapping[] clss = key.getIndependentTypeMappings();
        if (clss.length != 1) {
            if (traverse)
                throw RelationStrategies.unjoinable(key);
            return joins;
        }
        if (forceOuter)
            return joins.outerJoinRelation(field.getName(),
                key.getForeignKey(clss[0]), clss[0], key.getSelectSubclasses(),
                false, false);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationRelationMapTableFieldStrategy.java`
#### Snippet
```java
                mkey = o;
                Object mval = map.get(mkey);
                if (mval == null) {
                    Set<Map.Entry> entries = map.entrySet();
                    for (Map.Entry entry : entries) {
                        if (entry.getKey().equals(mkey))
                            mval = entry.getValue();
                    }
                }
                if (mval == null)
                    continue;
                keysm = RelationStrategies.getStateManager(mkey, ctx);
                valsm = RelationStrategies.getStateManager(mval, ctx);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationRelationMapTableFieldStrategy.java`
#### Snippet
```java
            for (Object value : add) {
                mkey = value;
                Object mval = map.get(mkey);
                if (mval == null) {
                    Set<Map.Entry> entries = map.entrySet();
                    for (Map.Entry entry : entries) {
                        if (entry.getKey().equals(mkey))
                            mval = entry.getValue();
                    }
                }
                if (mval == null)
                    continue;
                keysm = RelationStrategies.getStateManager(mkey, ctx);
                valsm = RelationStrategies.getStateManager(mval, ctx);
                if (field.isUni1ToMFK()) {
                    addRow = rm.getRow(field.getElementMapping().getDeclaredTypeMapping().getTable(),
                            Row.ACTION_UPDATE, valsm, true);
                    addRow.wherePrimaryKey(valsm);
                    key.setForeignKey(addRow, keysm);
                    val.setForeignKey(addRow, sm);
                }
                else {
                    key.setForeignKey(addRow, keysm);
                    val.setForeignKey(addRow, valsm);
                    rm.flushSecondaryRow(addRow);
                }
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        Table pkTable = toPK.getTable();
        if (_pkTable != null && !_pkTable.equals(pkTable))
            throw new InvalidStateException(_loc.get("fk-mismatch",
                pkTable, _pkTable));

        _pkTable = pkTable;
        if (_joins == null)
            _joins = new LinkedHashMap();
        _joins.put(local, toPK);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java
        if (group != null) {
            group.addName(name, true);
            tab = group.newTable(name, this);
        } else
            tab = new Table(name, this);
        if (_tableMap == null)
            _tableMap = new TreeMap<>();
        DBIdentifier sName = DBIdentifier.toUpper(name);
        _tableMap.put(sName, tab);
        _tables = null;
        return tab;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java
        if (!cur.equals(tab))
            return false;

        _tableMap.remove(sName);
        _tables = null;
        SchemaGroup group = getSchemaGroup();
        if (group != null)
            group.removeName(tab.getIdentifier());
        tab.remove();
        return true;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java
        DBIdentifier name = getIdentifier();
        DBIdentifier otherName = other.getIdentifier();
        if (DBIdentifier.isNull(name) && DBIdentifier.isNull(otherName)) {
            return 0;
        }
        if (DBIdentifier.isNull(name))
            return 1;
        if (DBIdentifier.isNull(otherName))
            return -1;
        return name.compareTo(otherName);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
        addName(name, true);
        Schema schema = getSchema();
        Column col;
        if (schema != null && schema.getSchemaGroup() != null) {
            col = schema.getSchemaGroup().newColumn(name, this);
        } else {
            col = new Column(name, this);
        }
        if (_colMap == null)
            _colMap = new LinkedHashMap<>();
        _colMap.put(normalizeColumnKey(name), col);
        _cols = null;
        return col;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
        if (fk == null || _fkList == null)
            return false;

        if (!_fkList.remove(fk))
            return false;

        Schema schema = getSchema();
        if (schema != null && schema.getSchemaGroup() != null)
            schema.getSchemaGroup().removeName(fk.getIdentifier());
        _fks = null;
        fk.remove();
        return true;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractSQLServerDictionary.java`
#### Snippet
```java
        Column[] cols = super.getColumns(meta, catalog, schemaName, tableName,
            colName, conn);
        for (int i = 0; cols != null && i < cols.length; i++)
            if (cols[i].getType() == Types.LONGVARCHAR)
                cols[i].setType(Types.CLOB);
        return cols;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
        Object val = sel.getSelects().get(idx);
        boolean toCast = (val instanceof Lit) &&
            ((Lit)val).getParseType() != Literal.TYPE_DATE &&
            ((Lit)val).getParseType() != Literal.TYPE_TIME &&
            ((Lit)val).getParseType() != Literal.TYPE_TIMESTAMP;

        if (toCast)
            selectSQL.append("CAST(");

        // ... and add the select per super's behavior...
        super.appendSelect(selectSQL, alias, sel, idx);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder();
        buf.append(sequenceSQL);
        if (!DBIdentifier.isNull(schemaName) || !DBIdentifier.isNull(sequenceName))
            buf.append(" WHERE ");
        if (!DBIdentifier.isNull(schemaName)) {
            buf.append(sequenceSchemaSQL);
            if (!DBIdentifier.isNull(sequenceName))
                buf.append(" AND ");
        }
        if (!DBIdentifier.isNull(sequenceName))
            buf.append(sequenceNameSQL);
        return buf.toString();
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
                if (rs != null) {
                    try {
                        rs.close();
                    } catch (SQLException se) {
                        // ignore
                    }
                }
                if (stmnt != null) {
                    try {
                        stmnt.close();
                    } catch (SQLException se) {
                        // ignore
                    }
                }
                if (conn != null) {
                    try {
                        conn.close();
                    }
                    catch(SQLException se) {
                        // ignore
                    }
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
        if (!isUsingRange(start, end)) {
            return super.toSelect(select, fetch, tables, where, group, having,
                order, distinct, forUpdate, 0, Long.MAX_VALUE, sel);
        }

        // if no skip, ordering, or distinct can use rownum directly
        SQLBuffer buf = new SQLBuffer(this);
        if (!requiresSubselectForRange(start, end, distinct, order)) {
            if (where != null && !where.isEmpty())
                buf.append(where).append(" AND ");
            buf.append("ROWNUM <= ").appendValue(end);
            return super.toSelect(select, fetch, tables, buf, group, having,
                order, distinct, forUpdate, 0, Long.MAX_VALUE, sel);
        }

        // if there is ordering, skip, or distinct we have to use subselects
        SQLBuffer newsel = super.toSelect(select, fetch, tables, where,
            group, having, order, distinct, forUpdate, 0, Long.MAX_VALUE,
            sel);

        // if no skip, can use single nested subselect
        if (!isUsingOffset(start)) {
            buf.append(getSelectOperation(fetch) + " * FROM (");
            buf.append(newsel);
            buf.append(") WHERE ROWNUM <= ").appendValue(end);
            return buf;
        }

        // with a skip, we have to use a double-nested subselect to put
        // where conditions on the rownum
        buf.append(getSelectOperation(fetch))
           .append(" * FROM (SELECT r.*, ROWNUM RNUM FROM (");
        buf.append(newsel);
        buf.append(") r");
        if (isUsingLimit(end))
            buf.append(" WHERE ROWNUM <= ").appendValue(end);
        buf.append(") WHERE RNUM > ").appendValue(start);
        return buf;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder();
        buf.append("CREATE TABLE ").append(getFullName(table, false))
            .append(" (");

        Column[] cols = table.getColumns();
        for (int i = 0; i < cols.length; i++) {
            if (i > 0)
                buf.append(", ");
            buf.append(getDeclareColumnSQL(cols[i], false));
        }

        PrimaryKey pk = table.getPrimaryKey();
        String pkStr;
        if (pk != null) {
            pkStr = getPrimaryKeyConstraintSQL(pk);
            if (!StringUtil.isEmpty(pkStr))
                buf.append(", ").append(pkStr);
        }

        Unique[] unqs = table.getUniques();
        String unqStr;
        for (Unique unq : unqs) {
            unqStr = getUniqueConstraintSQL(unq);
            if (unqStr != null)
                buf.append(", ").append(unqStr);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java
        Column[] cols = table.getColumns();
        for (int i = 0; i < cols.length; i++) {
            if (i > 0)
                buf.append(", ");
            buf.append(getDeclareColumnSQL(cols[i], false));
        }

        PrimaryKey pk = table.getPrimaryKey();
        String pkStr;
        if (pk != null) {
            pkStr = getPrimaryKeyConstraintSQL(pk);
            if (!StringUtil.isEmpty(pkStr))
                buf.append(", ").append(pkStr);
        }

        Unique[] unqs = table.getUniques();
        String unqStr;
        for (Unique unq : unqs) {
            unqStr = getUniqueConstraintSQL(unq);
            if (unqStr != null)
                buf.append(", ").append(unqStr);
        }

        buf.append(")");
        return new String[]{ buf.toString() };
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SybaseDictionary.java`
#### Snippet
```java
        Unique[] unqs = table.getUniques();
        String unqStr;
        for (Unique unq : unqs) {
            unqStr = getUniqueConstraintSQL(unq);
            if (unqStr != null)
                buf.append(", ").append(unqStr);
        }

        buf.append(")");
        return new String[]{ buf.toString() };
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java
        if (!DBIdentifier.isNull(schemaName) || !DBIdentifier.isNull(sequenceName))
            buf.append(" WHERE ");
        if (!DBIdentifier.isNull(schemaName)) {
            buf.append("SEQUENCE_SCHEMA = ?");
            if (!DBIdentifier.isNull(sequenceName))
                buf.append(" AND ");
        }
        if (!DBIdentifier.isNull(sequenceName))
            buf.append("SEQUENCE_NAME = ?");
        return buf.toString();
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java
        buf.append("LOCATE(");
        find.appendTo(buf);
        buf.append(", ");
        str.appendTo(buf);
        if (start != null) {
            buf.append(", ");
            start.appendTo(buf);
        }
        buf.append(")");
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JDataStoreDictionary.java`
#### Snippet
```java
        buf.append("(POSITION(");
        find.appendTo(buf);
        buf.append(" IN ");
        if (start != null)
            substring(buf, str, start, null);
        else
            str.appendTo(buf);
        buf.append(")");
        if (start != null) {
            buf.append(" - 1 + ");
            start.appendTo(buf);
        }
        buf.append(")");
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SybaseDictionary.java`
#### Snippet
```java
        if (start != null)
            substring(buf, str, start, null);
        else
            str.appendTo(buf);
        buf.append(")");
        if (start != null) {
            buf.append(" - 1 + ");
            start.appendTo(buf);
        }
        buf.append(")");
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        int beginIndex = 0;

        versionStr = versionStr.trim();
        char[] charArr = versionStr.toCharArray();
        for (int i = 0; i < charArr.length; i++) {
            if (Character.isDigit(charArr[i])) {
                beginIndex = i;
                break;
            }
        }

        int endIndex = charArr.length;
        for (int i = beginIndex+1; i < charArr.length; i++) {
            if (charArr[i] != '.' && !Character.isDigit(charArr[i])) {
                endIndex = i;
                break;
            }
        }

        String[] arr = versionStr.substring(beginIndex, endIndex).split("\\.");
        if (arr.length < 2)
            throw new IllegalArgumentException();

        int maj = Integer.parseInt(arr[0]);
        int min = Integer.parseInt(arr[1]);
        return new int[]{maj, min};
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        if (DBIdentifier.isNull(fk.getIdentifier())) {
            DBIdentifier fkName = fk.loadIdentifierFromDB(this,conn);
            String[] retVal = (fkName == null) ?  new String[0] :
                new String[]{ "ALTER TABLE "
                + getFullName(fk.getTable(), false)
                + " DROP FOREIGN KEY " + toDBName(fkName) };
            return retVal;
        }
        return new String[]{ "ALTER TABLE "
            + getFullName(fk.getTable(), false)
            + " DROP FOREIGN KEY " + toDBName(fk.getIdentifier()) };
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        String[] sql = super.getAddPrimaryKeySQL(pk);

        // mysql requires that a column be declared NOT NULL before
        // it can be made a primary key.
        Column[] cols = pk.getColumns();
        String[] ret = new String[cols.length + sql.length];
        for (int i = 0; i < cols.length; i++) {
            ret[i] = "ALTER TABLE " + getFullName(cols[i].getTable(), false)
                + " CHANGE " + toDBName(cols[i].getIdentifier())
                + " " + toDBName(cols[i].getIdentifier()) // name twice
                + " " + getTypeName(cols[i]) + " NOT NULL";
        }

        System.arraycopy(sql, 0, ret, cols.length, sql.length);
        return ret;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        if (!optimizeMultiTableDeletes)
            return super.getDeleteTableContentsSQL(tables,conn);
        else {
            StringBuilder buf = new StringBuilder(tables.length * 8);
            buf.append("DELETE FROM ");
            for (int i = 0; i < tables.length; i++) {
                buf.append(toDBName(tables[i].getFullIdentifier()));
                if (i < tables.length - 1)
                    buf.append(", ");
            }
            return new String[] { buf.toString() };
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        if (type == Types.CLOB && !useClobs) {
            return Types.LONGVARCHAR;
        }
        else if (type == Types.TIME_WITH_TIMEZONE) {
            // MariaDB doesn't support SQL-2003 'WITH TIMEZONE' nor the respective JDBC types.
            return Types.TIME;
        }
        else if (type == Types.TIMESTAMP_WITH_TIMEZONE) {
            // MariaDB doesn't support SQL-2003 'WITH TIMEZONE' nor the respective JDBC types.
            return Types.TIMESTAMP;
        }

        return super.getPreferredType(type);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        super.appendXmlComparison(buf, op, lhs, rhs, lhsxml, rhsxml);
        if (lhsxml)
            appendXmlValue(buf, lhs);
        else
            lhs.appendTo(buf);
        buf.append(" ").append(op).append(" ");
        if (rhsxml)
            appendXmlValue(buf, rhs);
        else
            rhs.appendTo(buf);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        Set<String> result = new HashSet<>();
        List<String> selects = sel.getIdentifierAliases();
        for (String s : selects) {
            String tableAlias = s.substring(0, s.indexOf('.'));
            result.add(tableAlias);
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        if (col.getType() == Types.BLOB && col.getTypeIdentifier().getType() == DBIdentifierType.NULL) {
            if (col.getSize() <= 0)   // unknown size
                return blobTypeName;  // return old default of 64KB
            else if (col.getSize() <= 255)
                return tinyBlobTypeName;
            else if (col.getSize() <= 65535)
                return blobTypeName;  // old default of 64KB
            else if (col.getSize() <= 16777215)
                return mediumBlobTypeName;
            else
                return longBlobTypeName;
        } else if (col.getType() == Types.CLOB && col.getTypeIdentifier().getType() == DBIdentifierType.NULL) {
            if (col.getSize() <= 0)   // unknown size
                return clobTypeName;  // return old default of 64KB
            else if (col.getSize() <= 255)
                return tinyTextTypeName;
            else if (col.getSize() <= 65535)
                return clobTypeName;  // old default of 64KB
            else if (col.getSize() <= 16777215)
                return mediumTextTypeName;
            else
                return longTextTypeName;
        } else {
            return super.getTypeName(col);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MergedResult.java`
#### Snippet
```java
        int least = -1;
        Object orderVal = null;
        for (int i = 0; i < _order.length; i++) {
            if (_status[i] != CURRENT)
                continue;
            if (least == -1 || _comp.compare(_order[i], orderVal) < 0) {
                least = i;
                orderVal = _order[i];
            }
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            int idx = 1;
            if (!DBIdentifier.isNull(schemaName))
                setString(stmnt, idx++, convertSchemaCase(schemaName), null);
            if (!DBIdentifier.isNull(tableName))
                setString(stmnt, idx++, convertSchemaCase(tableName), null);

            setTimeouts(stmnt, conf, false);
            rs = stmnt.executeQuery();
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        if (seqs == null)
            return create;

        // combine create table sql and create seqences sql
        String[] sql = new String[create.length + seqs.size()];
        System.arraycopy(create, 0, sql, 0, create.length);
        for (int i = 0; i < seqs.size(); i++)
            sql[create.length + i] = (String) seqs.get(i);
        return sql;
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
            stmnt = sql.prepareStatement(conn, fetch,
                ResultSet.TYPE_SCROLL_SENSITIVE, ResultSet.CONCUR_UPDATABLE);
            setTimeouts(stmnt, fetch, true);
            res = stmnt.executeQuery();
            if (!res.next()) {
                throw new InternalException(_loc.get("stream-exception"));
            }
            int oid = res.getInt(1);
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PrimaryRow.java`
#### Snippet
```java
            if (canSetAny(io, fk.getColumns().length
                + fk.getConstantColumns().length, false))
                setValid(true);

            if (_fkSet == null)
                _fkSet = new OpenJPAStateManager[getTable().
                    getForeignKeys().length];
            _fkSet[fk.getIndex()] = sm;

            if (_fkIO != null)
                _fkIO[fk.getIndex()] = io;
            else if (io != null && ((getAction() == ACTION_INSERT
                && !io.isAllInsertable(fk, false))
                || (getAction() != ACTION_INSERT
                && !io.isAllUpdatable(fk, false)))) {
                _fkIO = new ColumnIO[_fkSet.length];
                _fkIO[fk.getIndex()] = io;
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PrimaryRow.java`
#### Snippet
```java
        if (sm == null || sm.getObjectId() != null || !sm.isNew()
            || sm.isFlushed() || !isPrimaryKeyAutoAssigned(sm))
            super.setRelationId(col, sm, rel);
        else {
            if (_relSet == null) {
                Column[] cols = getTable().getRelationIdColumns();
                _relSet = new OpenJPAStateManager[cols.length];
                _callbacks = new RelationId[cols.length];
            }
            int idx = getRelationIdIndex(col);
            _relSet[idx] = sm;
            _callbacks[idx] = rel;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PrimaryRow.java`
#### Snippet
```java
            if (_fkSet != null) {
                ForeignKey[] fks = getTable().getForeignKeys();
                ColumnIO io;
                for (int i = 0; i < _fkSet.length; i++) {
                    if (_fkSet[i] != null) {
                        io = (_fkIO == null) ? null : _fkIO[i];
                        super.setForeignKey(fks[i], io, _fkSet[i]);
                    }
                }
            }
            if (_relSet != null) {
                Column[] cols = getTable().getRelationIdColumns();
                for (int i = 0; i < _relSet.length; i++)
                    if (_relSet[i] != null)
                        super.setRelationId(cols[i], _relSet[i], _callbacks[i]);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                if (buf._userParams != null) {
                    if (_userParams == null)
                        _userParams = new ArrayList();
                   _userParams.addAll(paramIndex, buf._userParams);
                }
                if (buf._userIndex != null) {
                    if (_userIndex == null)
                        _userIndex = new ArrayList();
                    _userIndex.addAll(buf._userIndex);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
        try {
            setParameters(stmnt);
            if (fetch != null) {
                if (fetch.getFetchBatchSize() > 0)
                    stmnt.setFetchSize(
                        _dict.getBatchFetchSize(fetch.getFetchBatchSize()));
                if (rsType != ResultSet.TYPE_FORWARD_ONLY
                    && fetch.getFetchDirection() != ResultSet.FETCH_FORWARD)
                    stmnt.setFetchDirection(fetch.getFetchDirection());
            }
            return stmnt;
        } catch (SQLException se) {
            try {
                stmnt.close();
            } catch (SQLException se2) {
            }
            throw se;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder().append(OBJECT_START);
        buf.append(encodeField(_ref ? REF_MARKER : ID_MARKER, ior(), 0));
        if (_ref) {
            return buf.append(OBJECT_END);
        }
        StringBuilder tab = newIndent(indent+1);
        for (Map.Entry<String, Object> e : _values.entrySet()) {
            buf.append(FIELD_SEPARATOR).append(NEWLINE);
            buf.append(tab).append(encodeField(e.getKey(), e.getValue(), indent+1));
        }
        buf.append(NEWLINE)
           .append(newIndent(indent))
           .append(OBJECT_END);
        return buf;
```

### DuplicatedCode
Duplicated code
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
            StringBuilder buf = new StringBuilder().append(ARRAY_START);
            StringBuilder tab = JSONObject.newIndent(indent+1);
            for (Object o : _members) {
                if (buf.length() > 1) buf.append(MEMBER_SEPARATOR);
                buf.append(NEWLINE).append(tab);
                if (o instanceof JSON)
                    buf.append(((JSON)o).asString(indent+1));
                else
                    buf.append(o);
            }
            buf.append(NEWLINE)
               .append(JSONObject.newIndent(indent))
               .append(ARRAY_END);

            return buf;
```

### DuplicatedCode
Duplicated code
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
            StringBuilder buf = new StringBuilder().append(ARRAY_START);
            StringBuilder tab = JSONObject.newIndent(indent+1);
            for (Map.Entry<Object, Object> e : _entries.entrySet()) {
                if (buf.length() > 1) buf.append(MEMBER_SEPARATOR);
                buf.append(NEWLINE).append(tab);
                Object key = e.getKey();
                if (key instanceof JSON) {
                    buf.append(((JSON)key).asString(indent+1));
                } else {
                    buf.append(key);
                }
                buf.append(VALUE_SEPARATOR);
                Object value = e.getValue();
                if (value instanceof JSON) {
                    buf.append(((JSON)value).asString(indent+2));
                } else {
                    buf.append(value);
                }

            }
            buf.append(NEWLINE)
               .append(JSONObject.newIndent(indent))
               .append(ARRAY_END);
            return buf;
```

### DuplicatedCode
Duplicated code
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
        JSONObject o = new JSONObject("Person", 1234, false);
        JSONObject r = new JSONObject("Person", 1234, true);
        JSONObject f = new JSONObject("Person", 2345, false);
        Array  a = new Array();
        a.add(f);
        a.add(3456);
        a.add(null);
        a.add(r);
        a.add(null);
        KVMap map = new KVMap();
        map.put("k1", r);
        map.put("k2", f);
        map.put("k3", null);
        map.put("k4", 3456);
        map.put(null, 6789);

        f.set("name", "Mary");
        f.set("age", 30);
        f.set("friend", r);
        o.set("name", "John");
        o.set("age", 20);
        o.set("friend", f);
        o.set("friends", a);
        o.set("map", map);
        // START - ALLOW PRINT STATEMENTS
        System.err.println(o);
```

### DuplicatedCode
Duplicated code
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObjectFormatter.java`
#### Snippet
```java
        Reader reader = null;
        if (value instanceof InputStream) {
            reader = new BufferedReader(new InputStreamReader((InputStream)value));
        } else if (value instanceof Reader) {
            reader = (Reader)value;
        } else {
            throw new RuntimeException();
        }
        CharArrayWriter writer = new CharArrayWriter();
        try {
            for (int c; (c = reader.read()) != -1;) {
                writer.write(c);
            }
        } catch (IOException ex) {
            throw new RuntimeException(ex);
        }
        return writer.toString();
```

### DuplicatedCode
Duplicated code
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/PrototypeFactory.java`
#### Snippet
```java
                Class<?>[] paramTypes = cons.getParameterTypes();
                boolean match = false;
                if (paramTypes.length == types.length) {
                    for (int i = 0; i < paramTypes.length; i++) {
                        match = paramTypes[i].isAssignableFrom(Filters.wrap(types[i]));
                        if (!match)
                            break;
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
        if (o != null && o.isTimedOut()) {
            o = null;
            removeInternal(key);
            if (log.isTraceEnabled())
                log.trace(s_loc.get("cache-timeout", key));
        }

        if (log.isTraceEnabled()) {
            if (o == null)
                log.trace(s_loc.get("cache-miss", key));
            else
                log.trace(s_loc.get("cache-hit", key));
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
            ConcurrentReferenceHashMap statsMap =
                new ConcurrentReferenceHashMap(ReferenceStrength.HARD, ReferenceStrength.HARD, CONCURRENCY, LOAD_FACTOR);
            statsMap.setMaxSize(FIXED_SIZE);
            stats = statsMap;

            ConcurrentReferenceHashMap aStatsMap =
                new ConcurrentReferenceHashMap(ReferenceStrength.HARD, ReferenceStrength.HARD, CONCURRENCY, LOAD_FACTOR);
            aStatsMap.setMaxSize(FIXED_SIZE);
            astats = aStatsMap;
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
            String header = "Query Statistics starting from " + start;
            out.print(header);
            if (since == start) {
                out.println();
                out.println("Total Query Execution: " + toString(astat));
                out.println("\tTotal \t\tQuery");
            } else {
                out.println(" last reset on " + since);
                out.println("Total Query Execution since start " +
                        toString(astat)  + " since reset " + toString(stat));
                out.println("\tSince Start \tSince Reset \t\tQuery");
            }
            int i = 0;
            for (T key : stats.keySet()) {
                i++;
                long[] arow = astats.get(key);
                if (since == start) {
                    out.println(i + ". \t" + toString(arow) + " \t" + key);
                } else {
                    long[] row  = stats.get(key);
                    out.println(i + ". \t" + toString(arow) + " \t"  + toString(row) + " \t\t" + key);
                }
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ConcurrentDataCache.java`
#### Snippet
```java
        super.initialize(mgr);
        conf.getRemoteCommitEventManager().addInternalListener(this);
        // Wait to instantiate _cache so that we know the proper value of _cache
        _cache = newCacheMap();
        if (_cacheSize != Integer.MIN_VALUE) {
            _cache.setCacheSize(_cacheSize);
        }
        if (_softRefs != Integer.MIN_VALUE) {
            _cache.setSoftReferenceSize(_softRefs);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
        String pkg;
        for (Object o : pkgs) {
            pkg = (String) o;
            if (pkg.length() > 0 && !"java.lang".equals(pkg)
                    && !base.equals(pkg)) {
                if (imports.length() > 0)
                    imports.endl();
                imports.append("import ").append(pkg).append(".*;");
            }
        }
        return imports.toString();
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/CodeGenerator.java`
#### Snippet
```java
            templ = new ParameterTemplate();
            templ.append(custom);
            templ.setParameter("fieldName", fieldName);
            templ.setParameter("capFieldName", capFieldName);
            templ.setParameter("propertyName", propertyName);
            templ.setParameter("fieldType", fieldType);
            templ.setParameter("keyType", keyType);
            templ.setParameter("elementType", elementType);
            templ.setParameter("fieldValue", fieldValue);
            decs.append(templ.toString());
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/DynamicStorageGenerator.java`
#### Snippet
```java
        BCMethod method = bc.declareMethod(prefix + name, type, null);
        method.makePublic();
        Code code = method.getCode(true);
        code.aload().setThis();
        code.getfield().setField(field);
        code.xreturn().setType(type);
        code.calculateMaxStack();
        code.calculateMaxLocals();
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/DynamicStorageGenerator.java`
#### Snippet
```java
        method = bc.declareMethod("set" + name, void.class,
            new Class[]{ type });
        method.makePublic();
        code = method.getCode(true);
        code.aload().setThis();
        code.xload().setParam(0).setType(type);
        code.putfield().setField(field);
        code.vreturn();
        code.calculateMaxStack();
        code.calculateMaxLocals();
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedStateManager.java`
#### Snippet
```java
        accessingField(idx);
        if (cur == next || !_loaded.get(idx))
            return;
        lock();
        try {
            _dirty.set(idx);
            longval = next;
            pc.pcReplaceField(idx);
        } finally {
            unlock();
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
            int low = Integer.MAX_VALUE;
            Object obj;
            int val;
            for (Object param : params) {
                obj = param;
                if (!(obj instanceof Number))
                    return 0; // use 0 base when params are mixed types

                val = ((Number) obj).intValue();
                if (val == 0)
                    return val;
                if (val < low)
                    low = val;
            }
            return low;
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
            if (_exps[0].projections.length > 0) {
                String ordering = _exps[0].orderingClauses[orderIndex];
                for (int i = 0; i < _exps[0].projectionClauses.length; i++)
                    if (ordering.equals(_exps[0].projectionClauses[i]))
                        return ((Object[]) resultObject)[i];

                throw new InvalidStateException(_loc.get
                    ("merged-order-with-result", q.getContext().getLanguage(),
                        q.getContext().getQueryString(), ordering));
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfigurationImpl.java`
#### Snippet
```java
        if (_state.ctx == null)
            return this;

        if (level != DEFAULT
            && level != LockLevels.LOCK_NONE
            && level != LockLevels.LOCK_READ
            && level != MixedLockLevels.LOCK_OPTIMISTIC
            && level != LockLevels.LOCK_WRITE
            && level != MixedLockLevels.LOCK_OPTIMISTIC_FORCE_INCREMENT
            && level != MixedLockLevels.LOCK_PESSIMISTIC_READ
            && level != MixedLockLevels.LOCK_PESSIMISTIC_WRITE
            && level != MixedLockLevels.LOCK_PESSIMISTIC_FORCE_INCREMENT)
            throw new IllegalArgumentException(_loc.get(
                "bad-lock-level", level).getMessage());

        lock();
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            if (!w1Number) {
                if (w2Number && (w1 == Character.class || w1 == String.class))
                    return (w2 == Byte.class || w2 == Short.class)
                        ? Integer.class : unwrap(c2);
                if (!w2Number && w1 == Character.class && w2 == String.class)
                    return String.class;
                if (w2Number)
                    return unwrap(c2);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
        switch (op) {
            case OP_ADD:
                tot = n1 + n2;
                break;
            case OP_SUBTRACT:
                tot = n1 - n2;
                break;
            case OP_MULTIPLY:
                tot = n1 * n2;
                break;
            case OP_DIVIDE:
                tot = n1 / n2;
                break;
            case OP_MOD:
                tot = n1 % n2;
                break;
            default:
                throw new InternalException();
        }
        return tot;
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        lock();
        try {
            assertOpen();
            if (_class != null || _compiled != null || _query == null
                || _broker == null)
                return _class;

            // check again after compilation; maybe encoded in string
            compileForCompilation();
            return _class;
        } finally {
            unlock();
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SaveFieldManager.java`
#### Snippet
```java
        FieldMetaData fmd = _sm.getMetaData().getField(field);
        switch (fmd.getDeclaredTypeCode()) {
            case JavaTypes.ARRAY:
                _field = proxy.copyArray(curVal);
                break;
            case JavaTypes.COLLECTION:
                _field = proxy.copyCollection((Collection) curVal);
                break;
            case JavaTypes.MAP:
                _field = proxy.copyMap((Map) curVal);
                break;
            case JavaTypes.DATE:
                _field = proxy.copyDate((Date) curVal);
                break;
            case JavaTypes.OBJECT:
                _field = proxy.copyCustom(curVal);
                if (_field == null)
                    _field = curVal;
                break;
            default:
                _field = curVal;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
        if (t1 == TYPE_STRING && val1 instanceof Literal
            && ((Literal) val1).getParseType() == Literal.TYPE_SQ_STRING) {
            String s = (String) ((Literal) val1).getValue();
            if (s.length() > 1) {
                ((Literal) val1).setValue(s.substring(0, 1));
                val1.setImplicitType(Character.TYPE);
            }
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
        if (t1 == TYPE_STRING && val1 instanceof Literal
            && ((Literal) val1).getParseType() == Literal.TYPE_STRING) {
            String s = (String) ((Literal) val1).getValue();
            ((Literal) val1).setValue(StringUtil.parse(s, Filters.wrap(t2)));
            val1.setImplicitType(t2);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/All.java`
#### Snippet
```java
        Class wrap = Filters.wrap(c);
        if (wrap == Integer.class
            || wrap == Float.class
            || wrap == Double.class
            || wrap == Long.class
            || wrap == BigDecimal.class
            || wrap == BigInteger.class)
            return c;
        return int.class;
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CompareExpression.java`
#### Snippet
```java
        Object o1 = _val1.eval(candidate, orig, ctx, params);
        Object o2 = _val2.eval(candidate, orig, ctx, params);
        if (o1 != null && o2 != null) {
            Class c = Filters.promote(o1.getClass(), o2.getClass());
            o1 = Filters.convert(o1, c);
            o2 = Filters.convert(o2, c);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CompareExpression.java`
#### Snippet
```java
        if (o1 != null && o2 != null) {
            Class c = Filters.promote(o1.getClass(), o2.getClass());
            o1 = Filters.convert(o1, c);
            o2 = Filters.convert(o2, c);
        }
        return compare(o1, o2);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
        try {
            // if the extent was empty, then alias the variable to null
            if (!itr.hasNext()) {
                var.setValue(null);
                return matches(exp, candidate, ctx, params, i + 1);
            }

            // try every value, short-circuiting on match
            while (itr.hasNext()) {
                // set the variable to each extent value and recurse
                var.setValue(itr.next());
                if (matches(exp, candidate, ctx, params, i + 1))
                    return true;
            }

            // no match
            return false;
        } finally {
            ImplHelper.close(itr);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/NullIf.java`
#### Snippet
```java
        if (o1 != null && o2 != null) {
            Class c = Filters.promote(o1.getClass(), o2.getClass());
            o1 = Filters.convert(o1, c);
            o2 = Filters.convert(o2, c);
        }
        if (compare(o1, o2))
            return null;
        else
            return o1;
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            JPQLNode aliasNode = parent.children.length > 1 ? right(parent)
                : null;
            Value proj = getValue(node);
            String alias = aliasNode != null ? aliasNode.text :
                projectionClause(node.id == JJTSCALAREXPRESSION ?
                        firstChild(node) : node);
            if (aliasNode != null)
                proj.setAlias(alias);
            projections.add(proj);
            projectionClauses.add(alias);
            projectionAliases.add(alias);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        if (fmd == null)
            return null;

        ClassMetaData cmd = null;
        ValueMetaData vmd;

        if ((vmd = fmd.getElement()) != null)
            cmd = vmd.getDeclaredTypeMetaData();
        else if ((vmd = fmd.getKey()) != null)
            cmd = vmd.getDeclaredTypeMetaData();
        else if ((vmd = fmd.getValue()) != null)
            cmd = vmd.getDeclaredTypeMetaData();

        if (cmd == null || cmd.getDescribedType() == Object.class)
            cmd = fmd.getDeclaredTypeMetaData();
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
            if (query.getSourceFile() == null) {
                File defaultFile = defaultSourceFile(query, clsNames);
                query.setSource(defaultFile, query.getSourceScope(), query.getSourceType(),
                        defaultFile == null ? "" : defaultFile.getPath());
            }
            if (AccessController.doPrivileged(
                    J2DoPrivHelper.existsAction(query.getSourceFile()))) {
                if (files == null)
                    files = new HashSet();
                files.add(query.getSourceFile());
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                        clss = cparser.parseTypeNames(new URLMetaDataIterator(url));
                        List<String> newNames = Arrays.asList(clss);
                        if (log.isTraceEnabled())
                            log.trace(_loc.get("scan-found-names", newNames, url));
                        names.addAll(newNames);
                        mapPersistentTypeNames(url, clss);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            ClassMetaData meta = null;
            switch (getTypeCode()) {
                case JavaTypes.PC:
                    meta = getTypeMetaData();
                    break;
                case JavaTypes.ARRAY:
                case JavaTypes.COLLECTION:
                case JavaTypes.MAP:
                    meta = _elem.getTypeMetaData();
                    break;
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
        FieldMetaData field = meta.getField(mappedBy);
        if (field != null)
            return field;
        int dotIdx = mappedBy.indexOf(".");
        if ( dotIdx == -1)
            return null;
        String fieldName = mappedBy.substring(0, dotIdx);
        FieldMetaData field1 = meta.getField(fieldName);
        if (field1 == null)
            return null;
        ClassMetaData meta1 = field1.getEmbeddedMetaData();
        if (meta1 == null)
            return null;
        String mappedBy1 = mappedBy.substring(dotIdx + 1);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InMemoryRelatedFieldOrder.java`
#### Snippet
```java
        PersistenceCapable pc1 = ImplHelper.toPersistenceCapable(o1, _conf);
        PersistenceCapable pc2 = ImplHelper.toPersistenceCapable(o2, _conf);
        OpenJPAStateManager sm1 = (OpenJPAStateManager) pc1.pcGetStateManager();
        OpenJPAStateManager sm2 = (OpenJPAStateManager) pc2.pcGetStateManager();
        if (sm1 == null || sm2 == null)
            return 0;
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/LifecycleMetaData.java`
#### Snippet
```java
        if (_resolved)
            throw new InternalException(_loc.get("lifecycle-resolved",
                _meta, Arrays.asList(callbacks)));

        if (_declared == null) {
            _declared = new LifecycleCallbacks
                [LifecycleEvent.ALL_EVENTS.length][];
            _high = new int[LifecycleEvent.ALL_EVENTS.length];
        }
        _declared[eventType] = callbacks;
        _high[eventType] = highPriority;
        _activated = true;
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java
        if (sm != null && _origOwner != null
            && (_origOwner != sm || _origField != field)) {
            throw new InvalidStateException(_loc.get("transfer-lrs",
                _origOwner.getMetaData().getField(_origField)));
        }

        _sm = sm;
        _field = field;

        // keep track of original owner so we can detect transfer attempts
        if (sm != null) {
            _origOwner = sm;
            _origField = field;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            _last = (Map.Entry) _itr.next();
            switch (_mode) {
                case MODE_KEY:
                    return _last.getKey();
                case MODE_VALUE:
                    return _last.getValue();
                default:
                    return _last;
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/Exceptions.java`
#### Snippet
```java
        Throwable[] nested = e.getNestedThrowables();
        int i = (JavaVersions.VERSION >= 4) ? 1 : 0;
        if (i < nested.length) {
            out.println("NestedThrowables:");
            for (; i < nested.length; i++)
                // guard against a nasty null in the array
                if (nested[i] != null)
                    nested[i].printStackTrace(out);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        if (type.getName().endsWith(PROXY_SUFFIX))
            type = type.getSuperclass();
        else if (type.isInterface()) {
            type = toConcreteType(type, _stdCollections);
            if (type == null)
                throw new UnsupportedException(_loc.get("no-proxy-intf", type));
        } else if (Modifier.isAbstract(type.getModifiers()))
            throw new UnsupportedException(_loc.get("no-proxy-abstract", type));
        return type;
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        ClassWriter cw = new ClassWriter(ClassWriter.COMPUTE_FRAMES);
        cw.visit(Opcodes.V11, Opcodes.ACC_PUBLIC + Opcodes.ACC_SUPER, proxyClassDef,
                null, superClassFileNname, interfaceNames);

        ClassWriterTracker ct = new ClassWriterTracker(cw);
        String classFileName = runtime ? type.getName() : proxyClassDef;
        cw.visitSource(classFileName + ".java", null);

        delegateConstructors(ct, type, superClassFileNname);
        addInstanceVariables(ct);
        addProxyMethods(ct, true, proxyClassDef, type);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        ClassWriter cw = new ClassWriter(ClassWriter.COMPUTE_FRAMES);
        cw.visit(Opcodes.V11, Opcodes.ACC_PUBLIC + Opcodes.ACC_SUPER, proxyClassDef,
                null, superClassFileNname, interfaceNames);

        ClassWriterTracker ct = new ClassWriterTracker(cw);
        String classFileName = runtime ? type.getName() : proxyClassDef;
        cw.visitSource(classFileName + ".java", null);

        delegateConstructors(ct, type, superClassFileNname);
        addInstanceVariables(ct);
        addProxyMethods(ct, false, proxyClassDef, type);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            int beanVarPos = params.length+2; // params+DUP

            if (params.length == 0) {
                mv.visitVarInsn(Opcodes.ASTORE, beanVarPos);
                copyBeanProperties(mv, type, beanVarPos);
                mv.visitVarInsn(Opcodes.ALOAD, beanVarPos);
            }

            mv.visitInsn(Opcodes.ARETURN);
            mv.visitMaxs(-1, -1);
            mv.visitEnd();
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            Class[] params = (cons == null) ? new Class[0]
                    : cons.getParameterTypes();

            MethodVisitor mv = ct.visitMethod(Modifier.PUBLIC, "copy",
                    Type.getMethodDescriptor(TYPE_OBJECT, TYPE_OBJECT)
                    , null, null);
            mv.visitCode();
            mv.visitTypeInsn(Opcodes.NEW, Type.getInternalName(type));
            mv.visitInsn(Opcodes.DUP);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        mv.visitVarInsn(Opcodes.ALOAD, 0);
        for (int i = 1; i < helperParams.length; i++)
        {
            mv.visitVarInsn(AsmHelper.getLoadInsn(helperParams[i]), i);
        }

        mv.visitMethodInsn(Opcodes.INVOKESTATIC, Type.getInternalName(helper.getDeclaringClass()), helper.getName(),
                Type.getMethodDescriptor(helper), false);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            mv.visitCode();
            mv.visitTypeInsn(Opcodes.NEW, proxyClassDef);
            mv.visitInsn(Opcodes.DUP);
            mv.visitMethodInsn(Opcodes.INVOKESPECIAL, proxyClassDef, "<init>",
                    Type.getMethodDescriptor(Type.VOID_TYPE), false);

            mv.visitInsn(Opcodes.ARETURN);
            mv.visitMaxs(-1, -1);
            mv.visitEnd();
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            mv.visitCode();

            mv.visitVarInsn(Opcodes.ALOAD, 0);
            mv.visitInsn(Opcodes.ICONST_1);
            mv.visitMethodInsn(Opcodes.INVOKESTATIC, Type.getInternalName(Proxies.class), "dirty",
                    Type.getMethodDescriptor(Type.VOID_TYPE, Type.getType(Proxy.class), Type.BOOLEAN_TYPE), false);

            mv.visitVarInsn(Opcodes.ALOAD, 0);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java
        if (sm != null && detaching(paramOpenJPAStateManager, paramInt)) {
            _detached = true;
            _delayedSm = sm;
            _delayedField = field;
        } else {
            _detached = false;
        }

        this.sm = paramOpenJPAStateManager;
        if (sm != null && sm.getPersistenceCapable() != null) {
            _ownerSm = (OpenJPAStateManager) sm.getPersistenceCapable()
                    .pcGetStateManager();
        }
        this.field = paramInt;
        if (sm != null && sm.getContext() != null) {
            _brokerFactory = sm.getContext().getBroker().getBrokerFactory();
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java
        if (_directAccess) {
            return super.remove(paramObject);
        }
        ProxyCollections.beforeRemove(this, paramObject);
        setDirectAccess(true);
        boolean bool = super.remove(paramObject);
        setDirectAccess(false);
        return ProxyCollections.afterRemove(this, paramObject, bool);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java
        if (_directAccess) {
            return super.remove(paramInt);
        }
        if (isDelayLoad()) {
            load();
        }
        ProxyCollections.beforeRemove(this, paramInt);
        Object localObject = super.remove(paramInt);
        return ProxyCollections.afterRemove(this, paramInt, localObject);
```

### DuplicatedCode
Duplicated code
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java
        if (_directAccess) {
            return super.set(paramInt, paramObject);
        }
        if (isDelayLoad()) {
            load();
        }
        ProxyCollections.beforeSet(this, paramInt, paramObject);
        Object localObject = super.set(paramInt, paramObject);
        return ProxyCollections.afterSet(this, paramInt, paramObject,
                localObject);
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
            SQLException err = null;
            try {
                PreparedStatement stmnt = super.prepareStatement
                    (sql, autoGeneratedKeys, false);
                return newLoggingPreparedStatement(stmnt, sql);
            } catch (SQLException se) {
                err = wrap(se, sql);
                throw err;
            } finally {
                handleSQLErrors(err);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                _sql = sql;
                logSQL(this);
                long start = System.currentTimeMillis();
                SQLException err = null;
                try {
                    return super.executeUpdate(sql, i);
                } catch (SQLException se) {
                    err = wrap(se, LoggingStatement.this, sql);
                    throw err;
                } finally {
                    logTime(start);
                    handleSQLErrors(LoggingStatement.this, err);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                _sql = sql;
                logSQL(this);
                long start = System.currentTimeMillis();
                SQLException err = null;
                try {
                    return super.execute(sql, i);
                } catch (SQLException se) {
                    err = wrap(se, LoggingStatement.this, sql);
                    throw err;
                } finally {
                    logTime(start);
                    handleSQLErrors(LoggingStatement.this, err);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
if (se instanceof BatchUpdateException
                        && _paramBatch != null && shouldTrackParameters()) {
                        int[] count = ((BatchUpdateException) se).
                            getUpdateCounts();
                        if (count != null && count.length <= _paramBatch.size())
                        {
                            for (int i = 0; i < count.length; i++) {
                                // -3 is Statement.STATEMENT_FAILED, but is
                                // only available in JDK 1.4+
                                if (count[i] == Statement.EXECUTE_FAILED) {
                                    indexOfFirstFailedObject = i;
                                    break;
                                }
                            }

                            // no -3 element: it may be that the server stopped
                            // processing, so the size of the count will be
                            // the index
                            //See the Javadoc for 'getUpdateCounts'; a provider
                            //may stop processing when the first failure occurs,
                            //as such, it may only return 'UpdateCounts' for the
                            //first few which pass.  As such, the failed
                            //index is 'count.length', NOT count.length+1.  That
                            //is, if the provider ONLY returns the first few that
                            //passes (i.e. say an array of [1,1] is returned) then
                            //length is 2, and since _paramBatch starts at 0, we
                            //don't want to use length+1 as that will give us the
                            //wrong index.
                            if (indexOfFirstFailedObject == -1){
                                indexOfFirstFailedObject = count.length;
                            }

                            //Finally, whatever the index is at this point, add batchedRowsBaseIndex
                            //to it to get the final index.  Recall, we need to start our index from the
                            //last batch which successfully executed.
                            indexOfFirstFailedObject += batchedRowsBaseIndex;

                            // set the current params to the saved values
                            if (indexOfFirstFailedObject < _paramBatch.size())
                                _params = (List<String>) _paramBatch.get(indexOfFirstFailedObject);
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                logSQL(this);
                long start = System.currentTimeMillis();
                SQLException err = null;
                try {
                    return super.executeUpdate(s, i);
                } catch (SQLException se) {
                    err = wrap(se, LoggingPreparedStatement.this);
                    throw err;
                } finally {
                    logTime(start);
                    clearLogParameters(true);
                    handleSQLErrors(LoggingPreparedStatement.this, err);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                logSQL(this);
                long start = System.currentTimeMillis();
                SQLException err = null;
                try {
                    return super.execute(s, i);
                } catch (SQLException se) {
                    err = wrap(se, LoggingPreparedStatement.this);
                    throw err;
                } finally {
                    logTime(start);
                    clearLogParameters(true);
                    handleSQLErrors(LoggingPreparedStatement.this, err);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                if (_logs.isSQLEnabled())
                    _logs.logSQL("batching " + this, LoggingConnection.this);
                long start = System.currentTimeMillis();
                try {
                    super.addBatch();
                    if (shouldTrackParameters()) {
                        // make sure our list is initialized
                        if (_paramBatch == null)
                            _paramBatch = new ArrayList<>();
                        // copy parameters since they will be re-used
                        if (_params != null) {
                            List<String> copyParms =
                                    new ArrayList<>(_params);
                            _paramBatch.add(copyParms);
                        }
                        else
                            _paramBatch.add(null);
                    }
                }
                finally {
                    logTime(start);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                buf.append(" ");
                if (_formatter != null) {
                    buf.append(SEP);
                    buf.append(_formatter.prettyPrint(_sql));
                    buf.append(SEP);
                } else {
                    buf.append(_sql);
                }

                StringBuilder paramBuf = null;
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                if (_params == null)
                    _params = new ArrayList<>();
                while (_params.size() < index)
                    _params.add(null);
                if (val.length() > 80)
                    val = val.substring(0, 77) + "...";
                _params.set(index - 1, val);
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                logSQL(this);
                long start = System.currentTimeMillis();
                SQLException err = null;
                try {
                    return super.executeUpdate(s, i);
                } catch (SQLException se) {
                    err = wrap(se, LoggingCallableStatement.this);
                    throw err;
                } finally {
                    logTime(start);
                    clearLogParameters(true);
                    handleSQLErrors(LoggingCallableStatement.this, err);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                logSQL(this);
                long start = System.currentTimeMillis();
                SQLException err = null;
                try {
                    return super.execute(s, i);
                } catch (SQLException se) {
                    err = wrap(se, LoggingCallableStatement.this);
                    throw err;
                } finally {
                    logTime(start);
                    clearLogParameters(true);
                    handleSQLErrors(LoggingCallableStatement.this, err);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/JarFileURLMetaDataIterator.java`
#### Snippet
```java
        byte[] content;
        if (size < 0) {
            ByteArrayOutputStream bout = new ByteArrayOutputStream();
            byte[] buf = new byte[1024];
            for (int r; (r = in.read(buf)) != -1; bout.write(buf, 0, r))
                ;
            content = bout.toByteArray();
        } else {
            content = new byte[(int) size];
            int offset = 0;
            int read;
            while (offset < size && (read = in.read(content, offset, (int) size - offset)) != -1) {
                offset += read;
            }
        }
        in.close();
        return content;
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractNonSequentialResultList.java`
#### Snippet
```java
        assertOpen();
        ArrayList list = new ArrayList();
        Object obj;
        for (int i = 0; true; i++) {
            obj = getInternal(i);
            if (obj == PAST_END)
                break;
            list.add(obj);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/LazyForwardResultList.java`
#### Snippet
```java
        try {
            _size = _rop.size();
            return _size;
        } catch (RuntimeException re) {
            close();
            throw re;
        } catch (Exception e) {
            close();
            _rop.handleCheckedException(e);
            return -1;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/SimpleResultList.java`
#### Snippet
```java
        assertOpen();
        if (_size != -1)
            return _size;
        try {
            _size = _rop.size();
            return _size;
        } catch (RuntimeException re) {
            close();
            throw re;
        } catch (Exception e) {
            close();
            _rop.handleCheckedException(e);
            return -1;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/CodeFormat.java`
#### Snippet
```java
        if (num == 0)
            return "";
        if (num == 1)
            return _sep;

        StringBuilder buf = new StringBuilder(_sep.length() * num);
        for (int i = 0; i < num; i++)
            buf.append(_sep);
        return buf.toString();
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java
        if (forward) {
            for (int i = start; i < table.length; i++)
                if (table[i] != null)
                    return i;
            return (searchedOther || start == 0) ? -1
                : findEntry(start - 1, false, true);
        } else {
            for (int i = start; i >= 0; i--)
                if (table[i] != null)
                    return i;
            return (searchedOther || start == table.length - 1) ? -1
                : findEntry(start + 1, true, true);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java
            if (entry != null) {
                return true;
            }
            while (index >= stopIndex) {
                if ((entry = table[index--]) != null) {
                    return true;
                }
            }
            if (stopIndex == 0) {
                index = table.length - 1;
                stopIndex = startIndex + 1;
                while (index >= stopIndex) {
                    if ((entry = table[index--]) != null) {
                        return true;
                    }
                }
            }
            return false;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
            if (StringUtil.isEmpty(over.name()))
                throw new MetaDataException(_loc.get("no-override-name", cm));
            sup = (FieldMapping) cm.getDefinedSuperclassField(over.name());
            if (sup == null)
                sup = (FieldMapping) cm.addDefinedSuperclassField(over.name(),
                    Object.class, Object.class);
            populate(sup, over);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        Column col = new Column();
        if (!StringUtil.isEmpty(id.name()))
            col.setIdentifier(DBIdentifier.newColumn(id.name(), delimit()));
        if (!StringUtil.isEmpty(id.columnDefinition()))
            col.setTypeIdentifier(DBIdentifier.newColumnDefinition(id.columnDefinition()));
        if (id.precision() != 0)
            col.setSize(id.precision());
        col.setFlag(Column.FLAG_UNINSERTABLE, !id.insertable());
        col.setFlag(Column.FLAG_UNUPDATABLE, !id.updatable());
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        if (!enabled) {
            info.setCanForeignKey(false);
            return;
        }

        org.apache.openjpa.jdbc.schema.ForeignKey fk =
            new org.apache.openjpa.jdbc.schema.ForeignKey();
        if (!StringUtil.isEmpty(name))
            fk.setIdentifier(DBIdentifier.newForeignKey(name, delimit()));
        fk.setDeferred(deferred);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        if (!enabled) {
            info.setCanIndex(false);
            return;
        }

        org.apache.openjpa.jdbc.schema.Index idx =
            new org.apache.openjpa.jdbc.schema.Index();
        if (!StringUtil.isEmpty(name))
            idx.setIdentifier(DBIdentifier.newConstraint(name, delimit()));
        idx.setUnique(unique);
        info.setIndex(idx);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        Column col = new Column();
        if (!StringUtil.isEmpty(anno.name()))
            col.setIdentifier(DBIdentifier.newColumn(anno.name(), delimit));
        if (!StringUtil.isEmpty(anno.columnDefinition()))
            col.setTypeIdentifier(DBIdentifier.newColumnDefinition(anno.columnDefinition()));
        if (anno.precision() != 0)
            col.setSize(anno.precision());
        else if (anno.length() != 255)
            col.setSize(anno.length());
        col.setNotNull(!anno.nullable());
        col.setDecimalDigits(anno.scale());
        col.setFlag(Column.FLAG_UNINSERTABLE, !anno.insertable());
        col.setFlag(Column.FLAG_UNUPDATABLE, !anno.updatable());
        return col;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        if (!StringUtil.isEmpty(anno.name()))
            col.setIdentifier(DBIdentifier.newColumn(anno.name(),delimit));
        if (!StringUtil.isEmpty(anno.columnDefinition()))
            col.setTypeIdentifier(DBIdentifier.newColumnDefinition(anno.columnDefinition()));
        if (anno.precision() != 0)
            col.setSize(anno.precision());
        else if (anno.length() != 255)
            col.setSize(anno.length());
        col.setNotNull(!anno.nullable());
        col.setDecimalDigits(anno.scale());
        col.setFlag(Column.FLAG_UNINSERTABLE, !anno.insertable());
        col.setFlag(Column.FLAG_UNUPDATABLE, !anno.updatable());
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        if (!StringUtil.isEmpty(anno.columnDefinition()))
            col.setTypeIdentifier(DBIdentifier.newColumnDefinition(anno.columnDefinition()));
        if (anno.precision() != 0)
            col.setSize(anno.precision());
        else if (anno.length() != 255)
            col.setSize(anno.length());
        col.setNotNull(!anno.nullable());
        col.setDecimalDigits(anno.scale());
        col.setFlag(Column.FLAG_UNINSERTABLE, !anno.insertable());
        col.setFlag(Column.FLAG_UNUPDATABLE, !anno.updatable());
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        if (!StringUtil.isEmpty(join.referencedAttributeName()))
            col.setTargetField(join.referencedAttributeName());
        col.setNotNull(!join.nullable());
        col.setFlag(Column.FLAG_UNINSERTABLE, !join.insertable());
        col.setFlag(Column.FLAG_UNUPDATABLE, !join.updatable ());
        return col;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        boolean isKey = false;
        boolean isValue = false;
        if (attrName != null && attrName.startsWith("key."))
            isKey = true;
        else if (attrName != null && attrName.startsWith("value."))
            isValue = true;
        if (isKey || isValue)
            attrName = attrName.substring(attrName.indexOf(".")+1);

        int typeCode = fm.getValue().getDeclaredTypeCode();
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        Column col = (Column) cols.get(0);
        switch (anno.value()) {
            case DATE:
                col.setType(Types.DATE);
                break;
            case TIME:
                col.setType(Types.TIME);
                break;
            case TIMESTAMP:
                col.setType(Types.TIMESTAMP);
                break;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        switch (anno.value()) {
            case DATE:
                col.setType(Types.DATE);
                break;
            case TIME:
                col.setType(Types.TIME);
                break;
            case TIMESTAMP:
                col.setType(Types.TIMESTAMP);
                break;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        Column col = new Column();
        if (!StringUtil.isEmpty(join.name()))
            col.setIdentifier(DBIdentifier.newColumn(join.name(), delimit));
        if (!StringUtil.isEmpty(join.columnDefinition()))
            col.setTypeIdentifier(DBIdentifier.newColumnDefinition(join.columnDefinition()));
        if (!StringUtil.isEmpty(join.referencedColumnName()))
            setTargetIdentifier(col, join.referencedColumnName());
        if (!StringUtil.isEmpty(join.referencedAttributeName()))
            col.setTargetField(join.referencedAttributeName());
        col.setNotNull(!join.nullable());
        col.setFlag(Column.FLAG_UNINSERTABLE, !join.insertable());
        col.setFlag(Column.FLAG_UNUPDATABLE, !join.updatable());
        return col;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
                cls.syncMappingInfo();
                cls.getDiscriminator().syncMappingInfo();
                cls.getVersion().syncMappingInfo();
                FieldMapping[] fields;
                if (cls.getEmbeddingMetaData() == null)
                    fields = cls.getDefinedFieldMappings();
                else
                    fields = cls.getFieldMappings();
                for (FieldMapping f : fields)
                    f.syncMappingInfo();
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        if (orig == null || fmd == orig)
            return false;

        FieldMapping field = (FieldMapping) fmd;
        FieldMapping field2 = (FieldMapping) orig;
        if (field.getMappingInfo().hasSchemaComponents()
            || field2.getMappingInfo().hasSchemaComponents())
            return true;

        ValueMappingInfo info = field.getValueInfo();
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        if (cols2 == null || cols2.size() != cols.size())
            return true;
        if (cols.size() != 1)
            return true;

        Column col;
        Column col2;
        for (int i = 0; i < cols.size(); i++) {
            col = cols.get(i);
            col2 = cols2.get(i);
            if (!Objects.equals(col.getName(), col2.getName()))
                return true;
            if (!Objects.equals(col.getTypeName(), col2.getTypeName()))
                return true;
            if (col.getSize() != col2.getSize())
                return true;
            if (col.getDecimalDigits() != col2.getDecimalDigits())
                return true;
            if (col.getFlag(Column.FLAG_UNINSERTABLE)
                != col2.getFlag(Column.FLAG_UNINSERTABLE))
                return true;
            if (col.getFlag(Column.FLAG_UNUPDATABLE)
                != col2.getFlag(Column.FLAG_UNUPDATABLE))
                return true;
        }
        return false;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        PersistenceStrategy strat = super.getStrategy(fmd);
        FieldMapping field = (FieldMapping) fmd;
        switch (strat) {
            case MANY_MANY:
                // we can differentiate a one-many by the fact that there is no
                // secondary table join, or the fk is unique
                if (field.getMappedBy() == null
                    && (field.getMappingInfo().getJoinDirection()
                    == MappingInfo.JOIN_NONE
                    || field.getElementMapping().getValueInfo().getUnique()
                    != null))
                    return PersistenceStrategy.ONE_MANY;
                break;
            case MANY_ONE:
                // inverse join cols or unique fk?
                if (field.getValueInfo().getJoinDirection()
                    == MappingInfo.JOIN_INVERSE
                    || field.getValueInfo().getUnique() != null)
                    return PersistenceStrategy.ONE_ONE;

                // scan for primary-key-join-column
                List<Column> cols = field.getValueInfo().getColumns();
                boolean pkJoin = cols != null && cols.size() > 0;
                for (int i = 0; pkJoin && i < cols.size(); i++)
                    pkJoin = cols.get(i).getFlag(Column.FLAG_PK_JOIN);
                if (pkJoin)
                    return PersistenceStrategy.ONE_ONE;
                break;
        }
        return strat;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            if (col.getType() == def) {
                continue;
            }
            switch (col.getType()) {
                case Types.DATE:
                    return TemporalType.DATE;
                case Types.TIME:
                    return TemporalType.TIME;
                case Types.TIMESTAMP:
                    return TemporalType.TIMESTAMP;
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        List<QueryResultMapping> result = null;
        for (int i = 0; i < _results.size(); i++) {
            QueryResultMapping element = _results.get(i);
            if ((cm == null && element.getSourceScope() != null) || (cm != null
                && element.getSourceScope() != cm.getDescribedType()))
                continue;

            if (result == null)
                result = new ArrayList<>(_results.size() - i);
            result.add(element);
        }
        return (result == null)
            ? (List<QueryResultMapping>) Collections.EMPTY_LIST : result;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
            if (!(o1 instanceof QueryResultMapping))
                return super.compareUnknown(o1, o2);

            QueryResultMapping res1 = (QueryResultMapping) o1;
            QueryResultMapping res2 = (QueryResultMapping) o2;

            // system scope before class scope
            Object scope1 = res1.getSourceScope();
            Object scope2 = res2.getSourceScope();
            if (scope1 == null && scope2 != null)
                return -1;
            if (scope1 != null && scope2 == null)
                return 1;

            // compare on listing index, or if none/same, use name
            int listingIndex1 = res1.getListingIndex();
            int listingIndex2 = res2.getListingIndex();
            if (listingIndex1 != listingIndex2)
                return listingIndex1 - listingIndex2;
            return res1.getName ().compareTo (res2.getName ());
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        Object cur = currentElement();
        Object scope = (cur instanceof ClassMetaData)
            ? ((ClassMetaData) cur).getDescribedType() : null;
        seq.setSource(getSourceFile(), scope, SourceTracker.SRC_XML);
        Locator locator = getLocation().getLocator();
        if (locator != null) {
            seq.setLineNumber(locator.getLineNumber());
            seq.setColNumber(locator.getColumnNumber());
        }
        pushElement(seq);
        return true;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        switch (_mapKeyTemporal) {
            case DATE:
                col.setType(Types.DATE);
                break;
            case TIME:
                col.setType(Types.TIME);
                break;
            case TIMESTAMP:
                col.setType(Types.TIMESTAMP);
                break;
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        String text = currentText();
        if (StringUtil.isEmpty(text))
            return;
        EnumType type = Enum.valueOf(EnumType.class, text);

        FieldMapping fm = (FieldMapping) currentElement();
        String strat = EnumValueHandler.class.getName() + "(StoreOrdinal="
            + (type == EnumType.ORDINAL) + ")";
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        val = attrs.getValue("column-definition");
        if (val != null)
            col.setTypeIdentifier(DBIdentifier.newColumnDefinition(val));
        val = attrs.getValue("precision");
        if (val != null)
            col.setSize(Integer.parseInt(val));
        val = attrs.getValue("length");
        if (val != null)
            col.setSize(Integer.parseInt(val));
        val = attrs.getValue("scale");
        if (val != null)
            col.setDecimalDigits(Integer.parseInt(val));
        val = attrs.getValue("nullable");
        if (val != null)
            col.setNotNull("false".equals(val));
        val = attrs.getValue("insertable");
        if (val != null)
            col.setFlag(Column.FLAG_UNINSERTABLE, "false".equals(val));
        val = attrs.getValue("updatable");
        if (val != null)
            col.setFlag(Column.FLAG_UNUPDATABLE, "false".equals(val));
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
                    String table = field.getMappingInfo().getTableName();
                    if (table != null) {
                        int index = table.indexOf('.');
                        if (index < 0)
                            addAttribute("name", table);
                        else {
                            addAttribute("schema", table.substring(0, index));
                            addAttribute("name", table.substring(index + 1));
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
        fmd.setInDefaultFetchGroup(anno.fetch() == FetchType.EAGER);
        if (isMappingOverrideMode() && !StringUtil.isEmpty(anno.mappedBy()))
            fmd.setMappedBy(anno.mappedBy());
        if (anno.targetEntity() != void.class)
            fmd.getElement().setDeclaredType(anno.targetEntity());
        setCascades(fmd.getElement(), anno.cascade());
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (meta == null)
            return;

        List seqs = null;
        String defName = null;
        if (meta.getSourceScope() instanceof Class)
            defName = ((Class) meta.getSourceScope()).getName();
        if (_seqs == null)
            _seqs = new HashMap<>();
        else
            seqs = _seqs.get(defName);

        if (seqs == null) {
            seqs = new ArrayList(3); // don't expect many seqs / class
            seqs.add(meta);
            _seqs.put(defName, seqs);
        } else if (!seqs.contains(meta))
            seqs.add(meta);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
        String defName = null;
        if (meta.getSourceScope() instanceof Class)
            defName = ((Class) meta.getSourceScope()).getName();
        if (_queries == null)
            _queries = new HashMap<>();
        else
            queries = _queries.get(defName);

        if (queries == null) {
            queries = new ArrayList(3); // don't expect many queries / class
            queries.add(meta);
            _queries.put(defName, queries);
        } else if (!queries.contains(meta))
            queries.add(meta);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (_seqs == null || meta == null)
            return false;
        String defName = null;
        if (meta.getSourceScope() instanceof Class)
            defName = ((Class) meta.getSourceScope()).getName();
        List seqs = _seqs.get(defName);
        if (seqs == null)
            return false;
        if (!seqs.remove(meta))
            return false;
        if (seqs.isEmpty())
            _seqs.remove(defName);
        return true;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (repos == null)
            return false;

        boolean removed = false;
        ClassMetaData[] metas = repos.getMetaDatas();
        for (ClassMetaData meta : metas) {
            removed |= removeMetaData(meta);
        }
        SequenceMetaData[] seqs = repos.getSequenceMetaDatas();
        for (SequenceMetaData seq : seqs) {
            removed |= removeSequenceMetaData(seq);
        }
        QueryMetaData[] queries = repos.getQueryMetaDatas();
        for (QueryMetaData query : queries) {
            removed |= removeQueryMetaData(query);
        }
        return removed;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (_seqs == null)
            return;

        for (Map.Entry entry : _seqs.entrySet()) {
            if (entry.getKey() == null)
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
                    entry.getValue()));
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (o instanceof ClassMetaData)
            return TYPE_META;
        if (o instanceof QueryMetaData)
            return TYPE_QUERY;
        if (o instanceof SequenceMetaData)
            return TYPE_SEQ;
        if (o instanceof ClassQueries)
            return TYPE_CLASS_QUERIES;
        if (o instanceof ClassSeqs)
            return TYPE_CLASS_SEQS;
        return -1;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        String plugin = meta.getSequencePlugin();
        String clsName = Configurations.getClassName(plugin);
        String props = Configurations.getProperties(plugin);
        String ds = null;
        if (props != null) {
            Properties map = Configurations.parseProperties(props);
            ds = (String) map.remove("Sequence");
            if (ds != null) {
                props = Configurations.serializeProperties(map);
                plugin = Configurations.getPlugin(clsName, props);
            }
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (isQueryMode()) {
            List queries = (_queries == null) ? null : _queries.get
                (meta.getDescribedType().getName());
            if (queries != null) {
                serializationSort(queries);
                for (Object query : queries) {
                    serializeQuery((QueryMetaData) query);
                }
            }
            if (isMappingMode())
                serializeQueryMappings(meta);
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        Collections.sort(fields, new FieldComparator());

        // serialize attr-override
        if (isMappingMode()) {
            FieldMetaData fmd;
            FieldMetaData orig;
            for (Iterator<FieldMetaData> it = fields.iterator(); it.hasNext();)
            {
                fmd = it.next();
                if (meta.getDefinedSuperclassField(fmd.getName()) == null)
                    continue;
                orig = meta.getPCSuperclassMetaData().getField(fmd.getName());
                if (serializeAttributeOverride(fmd, orig))
                    serializeAttributeOverrideContent(fmd, orig);
                it.remove();
            }
        }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (fmd.getManagement() == FieldMetaData.MANAGE_NONE)
            return PersistenceStrategy.TRANSIENT;

        if (fmd.isSerialized()
            || fmd.getDeclaredType() == byte[].class
            || fmd.getDeclaredType() == Byte[].class
            || fmd.getDeclaredType() == char[].class
            || fmd.getDeclaredType() == Character[].class)
            return PersistenceStrategy.BASIC;

        FieldMetaData mappedBy;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        List all = new ArrayList();
        if (isQueryMode())
            addQueryMetaDatas(all);
        if (isMappingMode())
            addSequenceMetaDatas(all);
        if ((isMetaDataMode() || isMappingMode()) && _metas != null)
            all.addAll(_metas.values());
        if (isMappingMode())
            addSystemMappingElements(all);
        serializationSort(all);
        return all;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            if (o1 == o2)
                return 0;
            if (o1 == null)
                return 1;
            if (o2 == null)
                return -1;

            int t1 = type(o1);
            int t2 = type(o2);
            if (t1 != t2)
                return t1 - t2;

            switch (t1) {
                case TYPE_META:
                    return compare((ClassMetaData) o1, (ClassMetaData) o2);
                case TYPE_QUERY:
                    return compare((QueryMetaData) o1, (QueryMetaData) o2);
                case TYPE_SEQ:
                    return compare((SequenceMetaData) o1,
                        (SequenceMetaData) o2);
                case TYPE_CLASS_QUERIES:
                    return ((Comparable) o1).compareTo(o2);
                case TYPE_CLASS_SEQS:
                    return ((Comparable) o1).compareTo(o2);
                default:
                    return compareUnknown(o1, o2);
            }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            int li1 = o1.getListingIndex();
            int li2 = o2.getListingIndex();
            if (li1 == -1 && li2 == -1) {
                MetaDataTag t1 = getEntityTag(o1);
                MetaDataTag t2 = getEntityTag(o2);
                if (t1.compareTo(t2) != 0)
                    return t1.compareTo(t2);
                int inher = super.compare(o1, o2);
                if (inher != 0)
                    return inher;
                return o1.getDescribedType().getName().compareTo
                    (o2.getDescribedType().getName());
            }

            if (li1 == -1)
                return 1;
            if (li2 == -1)
                return -1;
            return li1 - li2;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            FieldMetaData fmd1 = (FieldMetaData) o1;
            FieldMetaData fmd2 = (FieldMetaData) o2;
            if (fmd1.isPrimaryKey()) {
                if (fmd2.isPrimaryKey())
                    return fmd1.compareTo(fmd2);
                return -1;
            }
            if (fmd2.isPrimaryKey())
                return 1;

            if (fmd1.isVersion()) {
                if (fmd2.isVersion())
                    return compareListingOrder(fmd1, fmd2);
                return getStrategy(fmd2) == PersistenceStrategy.BASIC ? 1 : -1;
			}
			if (fmd2.isVersion())
                return getStrategy(fmd1) == PersistenceStrategy.BASIC ? -1 : 1;

            int stcmp = getStrategy(fmd1).compareTo(getStrategy(fmd2));
            if (stcmp != 0)
                return stcmp;
            return compareListingOrder(fmd1, fmd2);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
        List<T> result = new ArrayList<>();
        for (T e : array) {
            boolean include = true;
            for (InclusiveFilter f : filters) {
                if (f != null && !f.includes(e)) {
                    include = false;
                    break;
                }
            }
            if (include)
                result.add(e);
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
            XMLVersionParser vp = new XMLVersionParser("persistence");
            try {
                vp.parse(url);
                _persistenceVersion = vp.getVersion();
                _schemaLocation = vp.getSchemaLocation();
            } catch (Throwable t) {
                    log(_loc.get("version-check-error",
                        _source.toString()).toString());
            }
            super.parse(url);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        if (grouping == null) {
            _groups = null;
            return this;
        }
        _groups = new ArrayList<>();
        for (Expression<?> e : grouping) {
            _groups.add(e);
        }
        return this;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
            StringBuilder buffer = new StringBuilder("CASE ");
            int size = whens.size();
            for (int i = 0; i < size; i++) {
                buffer.append(Expressions.asValue(q, " WHEN ", whens.get(i), " THEN ", thens.get(i)));
            }
            buffer.append(Expressions.asValue(q, " ELSE ", otherwise, " END"));
            return buffer;
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                        if (c.isRegistered(_parent)) {
                            Value var = c.getRegisteredVariable(_parent);
                            path = factory.newPath(var);
                        } else {
                            path = factory.newPath(subQ);
                        }
                        path.setMetaData(meta);
                        path.get(_member.fmd, allowNull);
                        path.setSchemaAlias(c.getAlias(_parent));
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                } else if (c.isRegistered(_parent)) {
                    Value var = c.getRegisteredVariable(_parent);
                    path = factory.newPath(var);
                    path.setMetaData(meta);
                    path.get(_member.fmd, allowNull);
                } else {
                    path = (org.apache.openjpa.kernel.exps.Path)toValue(factory, c);
                }
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            if (getJoins() != null) {
                for (Join<?, ?> join1 : getJoins()) {
                    filter = Expressions.and(factory,
                             ((FromImpl<?,?>)join1).toKernelExpression(factory, c), filter);
                }
            }
            org.apache.openjpa.kernel.exps.Expression expr = Expressions.and(factory, join, filter);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            AbstractQuery<?> parent = subquery.getParent();
            if (parent instanceof CriteriaQueryImpl) {
                return ((CriteriaQueryImpl<?>)parent).getRegisteredVariable(path);
            }
            Value var = ((SubqueryImpl<?>)parent).getDelegate().getRegisteredVariable(path);
            if (var != null)
                return var;
            return getVariableForCorrPath((SubqueryImpl<?>)parent, path);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        Set<ExecutableElement> allMethods = (Set<ExecutableElement>)
            filter(allMembers, methodFilter, nonTransientFilter);

        Set<ExecutableElement> getters = filter(allMethods, getterFilter);
        Set<ExecutableElement> setters = filter(allMethods, setterFilter);
        getters = matchGetterAndSetter(getters, setters);
```

### DuplicatedCode
Duplicated code
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
            if (line.endsWith("{") || line.endsWith("}")) {

            }
            if (!line.endsWith(SEMICOLON)
                    && !(line.isEmpty() || line.endsWith("{") || line.endsWith("}") || line.startsWith("if")))
                line = line + SEMICOLON;
            codeLines.add(tab + line);
            return this;
```

### DuplicatedCode
Duplicated code
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
			List<Future<ResultObjectProvider>> futures =
				new ArrayList<>();
            final List<Executor> usedExecutors = new ArrayList<>();
			final List<ResultObjectProvider> rops =
				new ArrayList<>();
			List<SliceStoreManager> targets = findTargets();
			QueryContext ctx = q.getContext();
			boolean isReplicated = containsReplicated(ctx);
            ExecutorService threadPool = SliceThread.getPool();
```

### DuplicatedCode
Duplicated code
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
			for (Future<ResultObjectProvider> future : futures) {
				try {
					rops.add(future.get());
				} catch (InterruptedException e) {
					throw new RuntimeException(e);
				} catch (ExecutionException e) {
					throw new StoreException(e.getCause());
				}
			}

			ResultObjectProvider[] tmp = rops
                    .toArray(new ResultObjectProvider[rops.size()]);
			ResultObjectProvider result = null;
			boolean[] ascending = getAscending(q);
			boolean isAscending = ascending.length > 0;
			boolean isAggregate = ctx.isAggregate();
			boolean hasRange = ctx.getEndRange() != Long.MAX_VALUE;
			if (isAggregate) {
				result = new UniqueResultObjectProvider(tmp, q,
						getQueryExpressions());
			} else if (isAscending) {
                result = new OrderingMergedResultObjectProvider(tmp, ascending,
                    usedExecutors.toArray(new Executor[usedExecutors.size()]),
					q, params);
			} else {
				result = new MergedResultObjectProvider(tmp);
			}
			if (hasRange) {
                result = new RangeResultObjectProvider(result,
                        ctx.getStartRange(), ctx.getEndRange());
			}
			return result;
```

### DuplicatedCode
Duplicated code
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
			Class<?> candidate = query.getCandidateType();
			DistributedConfiguration conf = (DistributedConfiguration)query.getStoreContext()
			    .getConfiguration();
			if (candidate != null) {
			    return conf.isReplicated(candidate);
			}
			ClassMetaData[] metas = query.getAccessPathMetaDatas();
			if (metas == null || metas.length < 1)
				return false;
			for (ClassMetaData meta : metas)
				if (conf.isReplicated(meta.getDescribedType()))
					return true;
			return false;
```

### DuplicatedCode
Duplicated code
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
			for (Future<Number> future : futures) {
				try {
					Number n = future.get();
					if (n != null)
						result += n.intValue();
				} catch (InterruptedException e) {
					throw new RuntimeException(e);
				} catch (ExecutionException e) {
					throw new StoreException(e.getCause());
				}
			}
			return result;
```

### DuplicatedCode
Duplicated code
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
			Iterator<StoreQuery> qs = owner._queries.iterator();
			List<Future<Number>> futures = null;
			int result = 0;
            ExecutorService threadPool = SliceThread.getPool();
			for (Executor ex : executors) {
				if (futures == null)
                    futures = new ArrayList<>();
				UpdateExecutor call = new UpdateExecutor();
				call.executor = ex;
				call.query = qs.next();
				call.params = params;
				futures.add(threadPool.submit(call));
			}
			for (Future<Number> future : futures) {
				try {
					Number n = future.get();
                    result += (n == null) ? 0 : n.intValue();
				} catch (InterruptedException e) {
					throw new RuntimeException(e);
				} catch (ExecutionException e) {
					throw new StoreException(e.getCause());
				}
			}
			return result;
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Set `unparsed` may contain URL objects
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
        if (_unparsed != null && !_unparsed.isEmpty()
            && (mode & MODE_META) != 0) {
            Set<URL> unparsed = new HashSet<>(_unparsed);
            for (URL url : unparsed) {
                parseXML(url, cls, mode, envLoader);
```

### CollectionContainsUrl
Set `_unparsed` may contain URL objects
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
    private XMLPersistenceMetaDataParser _xmlParser = null;
    private Map<URL, Set<String>> _xml = null; // xml rsrc -> class names
    private Set<URL> _unparsed = null; // xml rsrc
    private boolean _fieldOverride = true;

```

### CollectionContainsUrl
Map `_xml` may contain URL objects
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
    private AnnotationPersistenceXMLMetaDataParser _annoXMLParser = null;
    private XMLPersistenceMetaDataParser _xmlParser = null;
    private Map<URL, Set<String>> _xml = null; // xml rsrc -> class names
    private Set<URL> _unparsed = null; // xml rsrc
    private boolean _fieldOverride = true;
```

## RuleId[id=RedundantEscapeInRegexReplacement]
### RedundantEscapeInRegexReplacement
Redundant escape of ':'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
            // We need
            if (query != null && _convertPositionalParams && JPQLParser.LANG_JPQL.equals(language)) {
                query = query.replaceAll("[\\?]", "\\:_");
            }
            String qid = query;
```

### RedundantEscapeInRegexReplacement
Redundant escape of ':'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/QueryMetaData.java`
#### Snippet
```java
    public void setQueryString(String query) {
        if (query != null && _convertPositionalParametersToNamed && JPQLParser.LANG_JPQL.equals(_language)) {
            query = query.replaceAll("[\\?]", "\\:_");
        }
        _query = query;
```

## RuleId[id=DanglingJavadoc]
### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchPlanImpl.java`
#### Snippet
```java
    protected static Map<String,HintValueConverter[]> _hints = new HashMap<>();

    /**
     * Statically registers supported hint keys with their ranking and converters.
     */
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AuditManager.java`
#### Snippet
```java


	/**
	 * Support functions.
	 */
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfigurationImpl.java`
#### Snippet
```java
    private static Map<String, Method> _hintSetters = new HashMap<>();

    /**
     * Registers hint keys that have a corresponding setter method.
     * The hint keys are registered in <code>openjpa.FetchPlan</code> and <code>openjpa</code> as prefix.
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
    }

    /**
     * Converts the values of given <code>params</code> Map into an array in
     * consultation of the <code>paramTypes</code> Map.
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java


    /**
     * Registers known commands in a {@link PrototypeFactory registry}.
     *
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-examples/openbooks/src/main/java/openbook/client/SupplyPage.java`
#### Snippet
```java
        });

        /**
         * Supplies each inventory displayed.
         */
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
        BUILTIN_BOOLEAN_REPRESENTATIONS = new HashMap<>();

        /**
         * Booleans are natively supported by this very database.
         * The database column is e.g. a NUMBER(1)
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
        BUILTIN_BOOLEAN_REPRESENTATIONS.put("BOOLEAN", BOOLEAN);

        /**
         * Booleans are stored as numeric int 1 and int 0 values.
         * The database column is e.g. a NUMBER(1)
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
        BUILTIN_BOOLEAN_REPRESENTATIONS.put("INT_10", INT_10);

        /**
         * Booleans are stored as String "1" for {@code true}
         * and String "0" for {@code false}.
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
        BUILTIN_BOOLEAN_REPRESENTATIONS.put("STRING_10", new StringBooleanRepresentation("1", "0"));

        /**
         * Booleans are stored as String "Y" for {@code true}
         * and String "N" for {@code false}.
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
        BUILTIN_BOOLEAN_REPRESENTATIONS.put("STRING_YN", new StringBooleanRepresentation("Y", "N"));

        /**
         * Booleans are stored as String "y" for {@code true}
         * and String "n" for {@code false}.
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
        BUILTIN_BOOLEAN_REPRESENTATIONS.put("STRING_YN_LOWERCASE", new StringBooleanRepresentation("y", "n"));

        /**
         * Booleans are stored as String "T" for {@code true}
         * and String "F" for {@code false}.
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
        BUILTIN_BOOLEAN_REPRESENTATIONS.put("STRING_TF", new StringBooleanRepresentation("T", "F"));

        /**
         * Booleans are stored as String "t" for {@code true}
         * and String "f" for {@code false}.
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationToManyInverseKeyFieldStrategy.java`
#### Snippet
```java

            ForeignKey fk = mapped.getForeignKey();
            /** Foreign key may be null if declared type of the mapped field is
             * abstract and under table-per-class inheritance strategy will have
             * no mapped table.
```

### DanglingJavadoc
Dangling Javadoc comment
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCFetchConfigurationImpl.java`
#### Snippet
```java
    private static final Localizer _loc = Localizer.forPackage(JDBCFetchConfigurationImpl.class);

    /**
     * Hint keys that correspond to a mutable bean-style setter in this receiver.
     * These keys are registered with both <code>openjpa.FetchPlan</code> and <code>openjpa.jdbc</code> as prefix.
```

## RuleId[id=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `smd` declared in one 'switch' branch and used in another
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                return UUIDType4StringSeq.getInstance();
            case ValueStrategies.SEQUENCE:
                SequenceMetaData smd = (fmd == null)
                    ? meta.getIdentitySequenceMetaData()
                    : fmd.getValueSequenceMetaData();
```

## RuleId[id=TrivialIf]
### TrivialIf
`if` statement can be simplified
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistenceUtil.java`
#### Snippet
```java
        // Assert the emf associated with the PC is the same as the provided emf
        OpenJPAEntityManagerFactory eemf = JPAFacadeHelper.toEntityManagerFactory(broker.getBrokerFactory());
        if (eemf == emf && eemf.isOpen()) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
            warn(meta, _loc.get("no-pers-strat", meta.getDescribedTypeString() + "." + name));
            return false;
        } else if (strat == PersistenceStrategy.TRANSIENT) {
            return false;
        } else {
```

### TrivialIf
`if` statement can be simplified
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
            return true;
        }
        if(getLineNum() != meta.getLineNumber()) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
        }

        if (getLineNum() != meta.getLineNumber()) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        }

        if (toString().equals(other.toString()))
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
            return true;
        }
        if ((sel instanceof From<?,?>) && ((From<?,?>)sel).isCorrelated()) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/TraversableResolverImpl.java`
#### Snippet
```java
         * which doesn't ask all the providers on the classpath about the obj.
         */
        if (OpenJPAPersistenceUtil.isLoaded(traversableObject, traversableProperty.getName()) == LoadState.NOT_LOADED) {
            return false;
        } else {
```

### TrivialIf
`if` statement can be simplified
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidationUtils.java`
#### Snippet
```java
            return false;

        if (e.getClass().getName().equals(
            "jakarta.validation.ConstraintViolationException"))
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/SimpleRegex.java`
#### Snippet
```java
                int match = indexOf(target, target.length() - len, exprPos,
                    len, true);
                if (match != -1)
                    return true;
                return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java
                Object v1 = getValue();
                Object v2 = e.getValue();
                if (v1 == v2 || (v1 != null && v1.equals(v2)))
                    return true;
            }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierRule.java`
#### Snippet
```java
        // Finally, look for reserved words
        if (getDelimitReservedWords()) {
            if (isReservedWord(identifier)) {
                return true;
            }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
            throw new IllegalArgumentException(_loc.get("no-rules-provided").getMessage());
        }
        if (rule.getCanDelimit() && !isDelimited(config, rule, name) && rule.requiresDelimiters(name)) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            return false;
        Options o = Configurations.parseProperties(Configurations.getProperties(conf.getMetaDataRepository()));
        if (o.getBooleanProperty(PRELOAD_STR) || o.getBooleanProperty(PRELOAD_STR.toLowerCase())) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java
        if (_ct.getRemoved().contains(o))
            return false;
        if (!has(o))
            return false;
        return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedHashSetProxy.java`
#### Snippet
```java
    private boolean detaching(OpenJPAStateManager paramOpenJPAStateManager,
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedHashSetProxy.java`
#### Snippet
```java
    private boolean detaching(OpenJPAStateManager paramOpenJPAStateManager,
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedTreeSetProxy.java`
#### Snippet
```java

    private boolean detaching(OpenJPAStateManager paramOpenJPAStateManager, int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1) ||
            (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedListProxy.java`
#### Snippet
```java
    private boolean detaching(OpenJPAStateManager paramOpenJPAStateManager,
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        // Default scoped class, we can only extend if it is in the same package as the generated proxy. Ideally
        // we'd fix the code gen portion and place proxies in the same pacakge as the types being proxied.
        if(cls.getPackage().getName().equals("org.apache.openjpa.util")) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java
    private boolean detaching(OpenJPAStateManager paramOpenJPAStateManager,
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedPriorityQueueProxy.java`
#### Snippet
```java
    private boolean detaching(OpenJPAStateManager paramOpenJPAStateManager,
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedVectorProxy.java`
#### Snippet
```java
    private boolean detaching(OpenJPAStateManager paramOpenJPAStateManager,
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
            Broker associatedBroker = smi.getBroker();

            if (broker != associatedBroker) {
                return true;
            }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
            if (!_subs && type != _type)
                return false;
            if (_subs && !_type.isAssignableFrom(type))
                return false;
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java

            boolean cascade = false;
            if(_cascadeWithDetach
                || fmd.getCascadeDetach() ==
                    ValueMetaData.CASCADE_IMMEDIATE
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
        if ((c1 == java.util.Date.class ||c1 == java.sql.Timestamp.class) && c2 == java.sql.Time.class)
            return false;
        if (isTemporalType(c1) && isTemporalType(c2))
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedStateManager.java`
#### Snippet
```java
        if (sm.getContext() != null && sm.getContext().getConfiguration() != null) {
            Compatibility compat = sm.getContext().getConfiguration().getCompatibilityInstance();
            if (compat != null && !compat.getIgnoreDetachedStateFieldForProxySerialization())
                _useDSFForUnproxy = true;      // new 2.0 behavior
            else
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/BindVariableExpression.java`
#### Snippet
```java
        // if the collection is empty no possible variable evals to true
        Collection vals = getVariableValues(candidate, orig, ctx, params);
        if (vals == null || vals.isEmpty())
            return false;
        return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
        if (isDeleted())
            return false;
        if (isNew() && !isFlushed())
            return false;
        return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
            return true;
        // TODO:JRB Intercepting
        if (AccessCode.isProperty(getMetaData().getAccessType())
            && _pc instanceof DynamicPersistenceCapable)
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
        if (path.last().getElement() == null) return false;
        if (path.last().getElement().getDeclaredTypeMetaData() == null) return false;
        if (path.last().getElement().getDeclaredTypeMetaData().getField(field) == null) return false;
        return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
    protected boolean isSeenVariable(String var) {
        Context c = ctx().findContext(var);
        if (c != null)
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheManagerImpl.java`
#### Snippet
```java
        }
        if (_excludedTypes != null && _excludedTypes.size() > 0) {
            if (_excludedTypes.contains(meta.getDescribedType().getName())) {
                return false;
            } else {
```

### TrivialIf
`if` statement can be simplified
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
        if (fetch.getCacheRetrieveMode() == DataCacheRetrieveMode.BYPASS)
            return true;
        if (load == StoreManager.FORCE_LOAD_REFRESH)
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-examples/openbooks/src/main/java/openbook/domain/Range.java`
#### Snippet
```java
            return false;
        if (type == null) {
            if (other.type != null)
                return false;
        } else if (!type.equals(other.type))
```

### TrivialIf
`if` statement can be simplified
in `openjpa-examples/openbooks/src/main/java/openbook/domain/Range.java`
#### Snippet
```java
            if (other.type != null)
                return false;
        } else if (!type.equals(other.type))
            return false;
        return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-examples/openbooks/src/main/java/openbook/domain/Book.java`
#### Snippet
```java
        Book other = (Book) obj;
        if (ISBN == null) {
            if (other.ISBN != null)
                return false;
        } else if (!ISBN.equals(other.ISBN))
```

### TrivialIf
`if` statement can be simplified
in `openjpa-examples/openbooks/src/main/java/openbook/domain/Book.java`
#### Snippet
```java
            if (other.ISBN != null)
                return false;
        } else if (!ISBN.equals(other.ISBN))
            return false;
        return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PrimaryRow.java`
#### Snippet
```java
        if (!fk.isDeferred() && !fk.isLogical())
            return true;
        if (fk.isPrimaryKeyAutoAssigned())
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
       // primary key, so don't create another index again
       PrimaryKey pk = table.getPrimaryKey();
       if (pk != null && idx.columnsMatch(pk.getColumns()))
           return false;
       return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            if (fk.getConstants().length > 0)
                return false;
            if (fk.getPrimaryKeyConstants().length > 0)
                return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/IngresDictionary.java`
#### Snippet
```java
        int dbMinorVersion = meta.getDatabaseMinorVersion();

        if ((dbMajorVersion == 9 && dbMinorVersion >= 2) ||
                dbMajorVersion > 9)
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
            return true;

        if (isOwnedSequence(name, schema, conn)) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
            }
            String val = getString(rs, 1);
            if (val == null || val.length() == 0) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                    return false;
                if (pjs == null) {
                    if (other.pjs != null)
                        return false;
                } else if (!pjs.equals(other.pjs))
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                    if (other.pjs != null)
                        return false;
                } else if (!pjs.equals(other.pjs))
                    return false;
                return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    @Override
    public void setParent(Select parent, String path) {
        if (path != null)
            _subPath = path;
        else
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
        boolean startsWithSYS_SEQ_ = name.isDelimited() ? seqName.startsWith("\"SYS_SEQ_") :
            seqName.startsWith("SYS_SEQ_");
        if (startsWith_SYSTEM && startsWithSYS_SEQ_)
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
    public String getCastFunction(Val val, String func, Column col) {
        boolean doCast = false;
        if (val instanceof Lit || val instanceof Param) {
            doCast = true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        // the generic dbdictionary is not considered a supported dict; all
        // other concrete dictionaries are
        if (c == DBDictionary.class)
            return false;
        return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        if (isNonDefaultMappingAllowed()) {
            if (field.getAssociationType() == FieldMetaData.ONE_TO_MANY ) {
                if (mapped != null && hasJoinTable(mapped))
                    return true;
            } else if (field.getAssociationType() == FieldMetaData.MANY_TO_ONE) {
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
                    return true;
            } else if (field.getAssociationType() == FieldMetaData.MANY_TO_ONE) {
                if (getBi_1ToM_JoinTableField(field) != null)
                    return true;
            }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
    protected boolean hasInheritanceStrategy(ClassMapping mapping) {
        ClassMappingInfo info = mapping.getMappingInfo();
        if (info != null && info.getHierarchyStrategy() != null)
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java
                        RelationStrategies.getStateManager(entry.getKey(), ctx);
                    ValueMapping key = fm.getKeyMapping();
                    if (keysm != null)
                        key.setForeignKey(newRow, keysm);
                    else
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java
            }
        }
        if (found)
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
    public boolean isVerticalStrategy() {
        String strat = getMappingInfo().getHierarchyStrategy();
        if (strat != null && strat.equals(VerticalClassStrategy.ALIAS))
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/SubclassJoinDiscriminatorStrategy.java`
#### Snippet
```java
            return false;
        ClassMapping[] subs = base.getJoinablePCSubclassMappings();
        if (subs.length == 0)
            return false;
        return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/InValueDiscriminatorStrategy.java`
#### Snippet
```java
        // if no subclasses or superclass, no need for conditions
        ClassMapping[] subs = base.getJoinablePCSubclassMappings();
        if (subs.length == 0 && base.getJoinablePCSuperclassMapping() == null)
            return false;

```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MaxEmbeddedByteArrayFieldStrategy.java`
#### Snippet
```java
        throws SQLException {
        byte[] b = (byte[]) getValue(sm);
         if (b == null)
            row.setBytes(field.getColumns()[0], null);
        else
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbeddedClassStrategy.java`
#### Snippet
```java
            && val.equals(JavaSQLTypes.getEmptyValue(cols[0].getJavaType())))
            return true;
        if (cols[0].getDefaultString() != null
            && val.toString().equals(cols[0].getDefaultString()))
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedFieldStrategy.java`
#### Snippet
```java
        JDBCFetchConfiguration fetch, Object val)
        throws SQLException {
        if (val != null)
            sm.storeObject(field.getIndex(), val);
        else
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedFieldStrategy.java`
#### Snippet
```java
        // a non-lazy relationship in the embeddable
        int type = fm.getTypeCode();
        if ((type == JavaTypes.ARRAY ||
             type == JavaTypes.COLLECTION ||
             type == JavaTypes.MAP)
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
        List cols = fm.getValueInfo().getColumns();
        if (cols.size() == 0) {
            if (fm.getName().equals(refColName.getName()))
                return true;
        } else {
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
                return true;
        } else {
            if (((Column)cols.get(0)).getIdentifier().equals(refColName))
                return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
    private boolean isPaginated() {
        if (select instanceof SelectImpl) {
            if (((SelectImpl)select).getStartIndex() != 0 ||
                ((SelectImpl)select).getEndIndex() != Long.MAX_VALUE)
                return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/JDBCExpressionFactory.java`
#### Snippet
```java
        ClassMapping cm = (ClassMapping)((Type)val).getMetaData();
        String strat = cm.getMappingInfo().getHierarchyStrategy();
        if (strat != null && strat.equals(VerticalClassStrategy.ALIAS))
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
     */
    protected boolean isEmptyResult(Result res) throws SQLException {
        if (res != null && !res.next())
            return true;
        return false;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
        Joins joins = (outer) ? sel.newOuterJoins() : null;
        boolean includeSubs = false;
        if (subs == Select.SUBS_JOINABLE || subs == Select.SUBS_ANY_JOINABLE) {
            includeSubs = true;
        }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Lit.java`
#### Snippet
```java
                StringBuilder value = new StringBuilder();
                boolean isOrdinal = false;
                if (lstate.sqlValue instanceof Integer)
                    isOrdinal = true;
                if (!isOrdinal)
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java

    public boolean isSubqueryPath() {
        if (_actions != null && _actions.size() == 1 &&
           ((Action)_actions.get(0)).op == Action.SUBQUERY)
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
        }
        if (normalizer.isDelimited(defaultRule, name1)) {
            if (!Objects.equals(name1, name2)) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
            }
        } else {
            if (!StringUtil.equalsIgnoreCase(name1, name2)) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
            }
            // Assert PK table name and schema
            if (!getFk().getPrimaryKeySchemaIdentifier().equals(fk.getPrimaryKeySchemaIdentifier()) ||
                !getFk().getPrimaryKeyTableIdentifier().equals(fk.getPrimaryKeyTableIdentifier()) ||
                !getFk().getSchemaIdentifier().equals(fk.getSchemaIdentifier()) ||
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
            fk.getConstantPrimaryKeyColumns()))
            return false;
        if (!match(getPrimaryKeyConstants(), fk.getPrimaryKeyConstants()))
            return false;
        return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-tools/openjpa-fetch-statistics/src/main/java/org/apache/openjpa/enhance/stats/FetchStatisticsAuxEnhancer.java`
#### Snippet
```java
        if (AccessCode.isField(fmd) && methName.toLowerCase(Locale.ENGLISH).startsWith("pcget")) {
            return true;
        } else if (AccessCode.isProperty(fmd) && methName.toLowerCase(Locale.ENGLISH).startsWith("get")
            || methName.toLowerCase(Locale.ENGLISH).startsWith("pcis")) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
            // do an additional lookup in case the java.sql.Types are different but
            // they map to the same representation in the DB
            if (dict.getTypeName(this).equals(dict.getTypeName(col))) {
                return true;
            }
```

### TrivialIf
`if` statement can be simplified
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
            return false;
        }
        if (getType() == Types.VARCHAR && getSize() > 0 && col.getSize() > 0
            && getSize() != col.getSize())
            return false;
```

## RuleId[id=EqualsHashCodeCalledOnUrl]
### EqualsHashCodeCalledOnUrl
Call to `equals()` on URL object
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                                urls.add(url);
                            }
                            else if (puORMUrl != null && puORMUrl.equals(url)) {
                                // Check URL equality to support encapsulating URL protocols
                                urls.add(url);
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/StringListValue.java`
#### Snippet
```java
        // defer to super.unalias
        String[] aliases = getAliases();
        if (aliases.length <= 0 || str == null)
            return super.unalias(str);
        str = str.trim();
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/StringListValue.java`
#### Snippet
```java
            return super.unalias(str);
        str = str.trim();
        if (str.length() <= 0)
            return super.unalias(str);

```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
StandardCharsets.ISO_8859_1 can be used instead
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/UUIDGenerator.java`
#### Snippet
```java
        byte[] bytes = next(type);
        try {
            return new String(bytes, "ISO-8859-1");
        } catch (Exception e) {
            return new String(bytes);
```

### CharsetObjectCanBeUsed
StandardCharsets.ISO_8859_1 can be used instead
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
        // Must be ISO-8859-1 ecoding according to Properties.load javadoc
        PrintWriter writer = new PrintWriter
            (new OutputStreamWriter(out, "ISO-8859-1"), false);

        if (header != null)
```

### CharsetObjectCanBeUsed
StandardCharsets.ISO_8859_1 can be used instead
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
            // Must be ISO-8859-1 ecoding according to Properties.load javadoc
            super(new InputStreamReader(new LineEndingStream(in, source),
                "ISO-8859-1"));
        }

```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ProcessingException.java`
#### Snippet
```java
        String uri = ctx.getRequestURI().toString();
        try {
            uri = URLDecoder.decode(uri, "UTF-8");
        } catch (Exception e) {
        }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/AbstractCommand.java`
#### Snippet
```java
        String query = request.getQueryString();
        if (query != null) {
            query = URLDecoder.decode(request.getQueryString(),"UTF-8");
        }
        writer.println("Query           = [" + query + "]"); // and this one
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `openjpa-examples/openbooks/src/main/java/openbook/util/JSPUtility.java`
#### Snippet
```java
    private static String encode(String s) {
        try {
            return URLEncoder.encode(s, "UTF-8");
        } catch (UnsupportedEncodingException e) {
            return s;
```

## RuleId[id=AutoCloseableResource]
### AutoCloseableResource
'BrokerBytesInputStream' used without 'try'-with-resources statement
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
                AbstractBrokerFactory.getPooledFactoryForKey(factoryKey);
            byte[] brokerBytes = (byte[]) in.readObject();
            ObjectInputStream innerIn = new BrokerBytesInputStream(brokerBytes,
                factory.getConfiguration());

```

### AutoCloseableResource
'InputStream' used without 'try'-with-resources statement
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/TemporaryClassLoader.java`
#### Snippet
```java

        String resourceName = name.replace('.', '/') + ".class";
        InputStream resource = getResourceAsStream(resourceName);
        if (resource == null) {
            throw new ClassNotFoundException(name);
```

### AutoCloseableResource
'InputStream' used without 'try'-with-resources statement
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
        _response.setHeader("Cache-Control", "public");
        _response.setHeader("Expires", ONE_YEAR_FROM_NOW);
        InputStream in = getClass().getResourceAsStream(rsrc);
        if (in == null) { // try again as a relative path
            if (rsrc.startsWith(CONTEXT_ROOT)) {
```

### AutoCloseableResource
'InputStream' used without 'try'-with-resources statement
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
        if (in == null) { // try again as a relative path
            if (rsrc.startsWith(CONTEXT_ROOT)) {
                in = getClass().getResourceAsStream(rsrc.substring(1));
            }
            if (in == null) {
```

### AutoCloseableResource
'InputStream' used without 'try'-with-resources statement
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java
        if (resource == null)
            return false;
        InputStream stream = Thread.currentThread().getContextClassLoader().getResourceAsStream(resource);
        if (stream != null)
            return true;
```

### AutoCloseableResource
'Writer' used without 'try'-with-resources statement
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
    public void putChars(Clob clob, char[] data)
        throws SQLException {
        Writer writer = clob.setCharacterStream(1L);
        try {
            writer.write(data);
```

## RuleId[id=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'jakarta.persistence.criteria.Join\[\]' expected, 'org.apache.openjpa.persistence.criteria.ExpressionImpl\[\]' found
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public void acceptVisit(CriteriaExpressionVisitor visitor) {
        Expressions.acceptVisit(visitor, this,
            _joins == null ? null : _joins.toArray(new ExpressionImpl<?>[_joins.size()]));
    }

```

### SuspiciousToArrayCall
Array of type 'jakarta.persistence.criteria.Expression\[\]' expected, 'org.apache.openjpa.persistence.criteria.ExpressionImpl\[\]' found
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        @Override
        public void acceptVisit(CriteriaExpressionVisitor visitor) {
            Expressions.acceptVisit(visitor, this, values.toArray(new ExpressionImpl[values.size()]));
        }

```

## RuleId[id=RedundantCast]
### RedundantCast
Casting `new PersistenceProviderImpl().createEntityManagerFactory(...)` to `OpenJPAEntityManagerFactory` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistence.java`
#### Snippet
```java
    public static OpenJPAEntityManagerFactory createEntityManagerFactory
        (String name, String resource, Map map) {
        return (OpenJPAEntityManagerFactory) new PersistenceProviderImpl().
            createEntityManagerFactory(name, resource, map);
    }
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `Method` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
                // check for setters for methods
                Method setter =
                    (Method) AccessController.doPrivileged(J2DoPrivHelper.getDeclaredMethodAction(
                        meta.getDescribedType(), setterName, new Class[] { ((Method) member).getReturnType() }));
                if (setter == null && !isAnnotatedTransient(member)) {
```

### RedundantCast
Casting `provider` to `String` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
            }
            try {
                if (!((String) provider).equals(org.apache.openjpa.persistence.PersistenceProviderImpl.class.getName())) {
                    return false;
                }
```

### RedundantCast
Casting `queries.toArray(...)` to `QueryMetaData[]` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                throw new InternalException();

            _queries = (QueryMetaData[]) queries.toArray
                (new QueryMetaData[queries.size()]);
            Arrays.sort(_queries, this);
```

### RedundantCast
Casting `seqs.toArray(...)` to `SequenceMetaData[]` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                throw new InternalException();

            _seqs = (SequenceMetaData[]) seqs.toArray
                (new SequenceMetaData[seqs.size()]);
            Arrays.sort(_seqs, this);
```

### RedundantCast
Casting `seqs.toArray(...)` to `SequenceMetaData[]` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                throw new InternalException();

            _seqs = (SequenceMetaData[]) seqs.toArray
                (new SequenceMetaData[seqs.size()]);
            Arrays.sort(_seqs, this);
```

### RedundantCast
Casting `queries.toArray(...)` to `QueryMetaData[]` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                throw new InternalException();

            _queries = (QueryMetaData[]) queries.toArray
                (new QueryMetaData[queries.size()]);
            Arrays.sort(_queries, this);
```

### RedundantCast
Casting `cls.getAnnotation(...)` to `SqlResultSetMapping` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
            if (AccessController.doPrivileged(J2DoPrivHelper
                    .isAnnotationPresentAction(cls, SqlResultSetMapping.class)) &&
                hasRSMapping(rsMappingName, (SqlResultSetMapping) cls.
                getAnnotation(SqlResultSetMapping.class)))
                return cls;
```

### RedundantCast
Casting `cls.getAnnotation(...)` to `SqlResultSetMappings` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
            if (AccessController.doPrivileged(J2DoPrivHelper
                    .isAnnotationPresentAction(cls, SqlResultSetMappings.class)) &&
                hasRSMapping(rsMappingName, ((SqlResultSetMappings) cls.
                getAnnotation(SqlResultSetMappings.class)).value()))
                return cls;
```

### RedundantCast
Casting `cls.getAnnotation(...)` to `NamedQuery` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
            if (AccessController.doPrivileged(J2DoPrivHelper
                    .isAnnotationPresentAction(cls, NamedQuery.class)) && hasNamedQuery
                (queryName, (NamedQuery) cls.getAnnotation(NamedQuery.class)))
                return cls;
            if (AccessController.doPrivileged(J2DoPrivHelper
```

### RedundantCast
Casting `cls.getAnnotation(...)` to `NamedQueries` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
            if (AccessController.doPrivileged(J2DoPrivHelper
                    .isAnnotationPresentAction(cls, NamedQueries.class)) &&
                hasNamedQuery(queryName, ((NamedQueries) cls.
                    getAnnotation(NamedQueries.class)).value()))
                return cls;
```

### RedundantCast
Casting `cls.getAnnotation(...)` to `NamedNativeQuery` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
            if (AccessController.doPrivileged(J2DoPrivHelper
                    .isAnnotationPresentAction(cls, NamedNativeQuery.class)) &&
                hasNamedNativeQuery(queryName, (NamedNativeQuery) cls.
                    getAnnotation(NamedNativeQuery.class)))
                return cls;
```

### RedundantCast
Casting `cls.getAnnotation(...)` to `NamedNativeQueries` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
            if (AccessController.doPrivileged(J2DoPrivHelper
                    .isAnnotationPresentAction(cls, NamedNativeQueries.class)) &&
                hasNamedNativeQuery(queryName, ((NamedNativeQueries) cls.
                    getAnnotation(NamedNativeQueries.class)).value()))
                return cls;
```

### RedundantCast
Casting `e` to `TypeElement` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java

        Elements eUtils = processingEnv.getElementUtils();
        String originalClass = eUtils.getBinaryName((TypeElement) e).toString();
        String originalSimpleClass = e.getSimpleName().toString();
        String metaClass = factory.getMetaModelClassName(originalClass);
```

### RedundantCast
Casting `(StringUtil.isEmpty(...) ? true : Boolean.parseBoolean(...))` to `Boolean` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
            throws SAXException {
        String enabledStr = attrs.getValue("enabled");
        boolean enabled = (Boolean) (StringUtil.isEmpty(enabledStr) ? true :
            Boolean.parseBoolean(enabledStr));

```

### RedundantCast
Casting `(StringUtil.isEmpty(...) ? Integer.MIN_VALUE : Integer.parseInt(...))` to `Integer` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java

        String timeoutStr = attrs.getValue("timeout");
        int timeout = (Integer) (StringUtil.isEmpty(timeoutStr) ? Integer.MIN_VALUE :
            Integer.parseInt(timeoutStr));

```

### RedundantCast
Casting `_elems.get(...)` to `Object` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
    protected boolean startClassElement(String name, Attributes attrs)
        throws SAXException {
        Object tag = (Object) _elems.get(name);
        boolean ret = false;
        if (tag == null) {
```

### RedundantCast
Casting `_callbacks[event].toArray(...)` to `LifecycleCallbacks[]` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
            if (_callbacks[event] == null)
                continue;
            meta.setDeclaredCallbacks(event, (LifecycleCallbacks[])
                _callbacks[event].toArray
                    (new LifecycleCallbacks[_callbacks[event].size()]),
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `Method[]` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
        Set<MethodKey> seen = new HashSet<>();
        do {
            for (Method m : (Method[]) AccessController.doPrivileged(J2DoPrivHelper.getDeclaredMethodsAction(sup))) {
                mods = m.getModifiers();
                if (Modifier.isStatic(mods) || Modifier.isFinal(mods) ||
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `Annotation[]` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
        OpenJPAConfiguration conf = repos.getConfiguration();
        for (Method m : methods) {
            for (Annotation anno : (Annotation[]) AccessController
                .doPrivileged(J2DoPrivHelper
                    .getDeclaredAnnotationsAction(m))) {
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `Field[]` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                meta.setDetachedState(detached.fieldName());
        } else {
            Field[] fields = (Field[]) AccessController.doPrivileged(
                J2DoPrivHelper.getDeclaredFieldsAction(
                    meta.getDescribedType()));
```

### RedundantCast
Casting `el.getAnnotation(...)` to `Basic` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
            switch (pstrat) {
                case BASIC:
                    parseBasic(fmd, (Basic) el.getAnnotation(Basic.class), lob);
                    break;
                case MANY_ONE:
```

### RedundantCast
Casting `el.getAnnotation(...)` to `ManyToOne` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case MANY_ONE:
                    parseManyToOne(fmd, (ManyToOne) el.getAnnotation
                        (ManyToOne.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `OneToOne` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case ONE_ONE:
                    parseOneToOne(fmd, (OneToOne) el.getAnnotation
                        (OneToOne.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `Embedded` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case EMBEDDED:
                    parseEmbedded(fmd, (Embedded) el.getAnnotation
                        (Embedded.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `OneToMany` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case ONE_MANY:
                    parseOneToMany(fmd, (OneToMany) el.getAnnotation
                        (OneToMany.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `ManyToMany` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case MANY_MANY:
                    parseManyToMany(fmd, (ManyToMany) el.getAnnotation
                        (ManyToMany.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `Persistent` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case PERS:
                    parsePersistent(fmd, (Persistent) el.getAnnotation
                        (Persistent.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `PersistentCollection` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case PERS_COLL:
                    parsePersistentCollection(fmd, (PersistentCollection)
                        el.getAnnotation(PersistentCollection.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `ElementCollection` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case ELEM_COLL:
                    parseElementCollection(fmd, (ElementCollection)
                        el.getAnnotation(ElementCollection.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `PersistentMap` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                    break;
                case PERS_MAP:
                    parsePersistentMap(fmd, (PersistentMap)
                        el.getAnnotation(PersistentMap.class));
                    break;
```

### RedundantCast
Casting `el.getAnnotation(...)` to `OrderBy` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
                case ORDER_BY:
                    parseOrderBy(fmd,
                        (OrderBy) el.getAnnotation(OrderBy.class));
                    break;
                case SEQ_GENERATOR:
```

### RedundantCast
Casting `' '` to `char` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java

        public Delimiter() {
            this((char)' ', (char)' ');
        }

```

### RedundantCast
Casting `' '` to `char` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java

        public Delimiter() {
            this((char)' ', (char)' ');
        }

```

### RedundantCast
Casting `parentJoin` to `Joins.List` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
    @Override
    public <X,Y> ListJoin<X,Y> correlate(ListJoin<X,Y> parentJoin) {
        Join corrJoin = Joins.clone((Joins.List)parentJoin);
        ((PathImpl<?,?>)corrJoin).setCorrelatedPath((PathImpl<?,?>)parentJoin);
        if (_corrJoins == null)
```

### RedundantCast
Casting `parentJoin` to `Joins.Set` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
    @Override
    public <X,Y> SetJoin<X,Y> correlate(SetJoin<X,Y> parentJoin) {
        Join corrJoin = Joins.clone((Joins.Set)parentJoin);
        ((PathImpl<?,?>)corrJoin).setCorrelatedPath((PathImpl<?,?>)parentJoin);
        if (_corrJoins == null)
```

### RedundantCast
Casting `parentJoin` to `Joins.Map` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
    @Override
    public <X,K,V> MapJoin<X,K,V> correlate(MapJoin<X,K,V> parentJoin) {
        Join corrJoin = Joins.clone((Joins.Map)parentJoin);
        ((PathImpl<?,?>)corrJoin).setCorrelatedPath((PathImpl<?,?>)parentJoin);
        if (_corrJoins == null)
```

### RedundantCast
Casting `parentJoin` to `Joins.Collection` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
    @Override
    public <X,Y> CollectionJoin<X,Y> correlate(CollectionJoin<X,Y> parentJoin) {
        Join corrJoin = Joins.clone((Joins.Collection)parentJoin);
        ((PathImpl<?,?>)corrJoin).setCorrelatedPath((PathImpl<?,?>)parentJoin);
        if (_corrJoins == null)
```

### RedundantCast
Casting `violations` to `Set` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
                    _loc.get("valdiate-property-failed",
                        arg0.getClass().getName(),property).getMessage(),
                    (Set)violations),
                true);
        }
```

### RedundantCast
Casting `violations` to `Set` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
                    _loc.get("validate-value-failed", arg0.getName(),
                        arg1, arg2.toString()).getMessage(),
                    (Set)violations),
                true);
        }
```

### RedundantCast
Casting `violations` to `Set` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
                    _loc.get("validate-failed",
                        arg0.getClass().getName()).getMessage(),
                    (Set)violations),
                true);
        }
```

### RedundantCast
Casting `files.toArray(...)` to `String[]` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/ant/AbstractTask.java`
#### Snippet
```java
            }
        }
        return (String[]) files.toArray(new String[files.size()]);
    }
}
```

### RedundantCast
Casting `_member` to `Attribute` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public Attribute<? super Z, ?> getAttribute() {
            return  (Attribute<? super Z, ?> )_member;
        }

```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `FileOutputStream` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/LogFactoryImpl.java`
#### Snippet
```java
            File f = Files.getFile(file, null);
            try {
                _out = new PrintStream((FileOutputStream)
                    AccessController.doPrivileged(
                        J2DoPrivHelper.newFileOutputStreamAction(
```

### RedundantCast
Casting `_delegates.toArray(...)` to `LogFactory[]` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/MultiLogFactory.java`
#### Snippet
```java
     */
    public LogFactory[] getDelegates() {
        return (LogFactory[]) _delegates.toArray(new LogFactory[_delegates.size()]);
    }

```

### RedundantCast
Casting `null` to `T` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <T> Expression<T> nullLiteral(Class<T> t) {
        return new Expressions.Constant<>(t, (T)null);
    }

```

### RedundantCast
Casting `null` to `Expression` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Substring(Expression<String> s) {
            this(s, (Expression<Integer>)null, (Expression<Integer>)null);
        }

```

### RedundantCast
Casting `from` to `ExpressionImpl` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Substring(Expression<String> s, Expression<Integer> from) {
            this(s, (ExpressionImpl<Integer>)from, null);
        }

```

### RedundantCast
Casting `arg` to `Collection` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                return new StringBuilder(((Class)arg).getSimpleName());
            } else if (Collection.class.isAssignableFrom(literalClass)) {
                return new StringBuilder(((Collection)arg).toString());
            }
            return new StringBuilder(arg.toString());
```

### RedundantCast
Casting `loaderCache.get(...)` to `Class` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
            _loaders.put(key, loaderCache);
        } else {  // We have a cache for this loader.
            cls = (Class<?>) loaderCache.get(clsName);
        }
        if (cls == null) {
```

### RedundantCast
Casting `J2DoPrivHelper.isFileAction(...).run()` to `Boolean` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java

                File f = new File(propertiesLocation);
                if ((Boolean) J2DoPrivHelper.isFileAction(f).run()) {
                    addAll(fqAnchors, propertiesLocation,
                        _derivations[i].getAnchorsInFile(f));
```

### RedundantCast
Casting `J2DoPrivHelper.isFileAction(...).run()` to `Boolean` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
                    f = new File("META-INF" + File.separatorChar
                        + propertiesLocation);
                    if ((Boolean) J2DoPrivHelper.isFileAction(f).run()) {
                        addAll(fqAnchors, propertiesLocation,
                            _derivations[i].getAnchorsInFile(f));
```

### RedundantCast
Casting `_itrs.get(...)` to `MetaDataIterator` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/MetaDataIteratorChain.java`
#### Snippet
```java
        MetaDataIterator itr;
        for (; _cur < _itrs.size(); _cur++) {
            itr = (MetaDataIterator) _itrs.get(_cur);
            if (itr.hasNext()) {
                _itr = itr;
```

### RedundantCast
Casting `files.get(...)` to `Collection` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataSerializer.java`
#### Snippet
```java
            }

            fileObjs = (Collection<Object>) files.get(file);
            if (fileObjs == null) {
                fileObjs = new LinkedList<>();
```

### RedundantCast
Casting `writer` to `StringWriter` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataSerializer.java`
#### Snippet
```java

                if (output != null)
                    output.put(file, ((StringWriter) writer).toString());
            } catch (SAXException | TransformerConfigurationException se) {
                throw new IOException(se.toString());
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `File[]` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/FileMetaDataIterator.java`
#### Snippet
```java
                metas.add(file);
            else {
                File[] files = (File[]) AccessController
                    .doPrivileged(J2DoPrivHelper.listFilesAction(file));
                if (files != null)
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `ClassLoader` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassMetaDataIterator.java`
#### Snippet
```java
            if (cls != null)
            {
                ClassLoader clsLoader = (ClassLoader)
                    AccessController.doPrivileged(
                        J2DoPrivHelper.getClassLoaderAction(cls));
```

### RedundantCast
Casting `o` to `String` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            if (o instanceof String) {
                // OPENJPA-1830 Do not overwrite existing string values with "******"
                if ((!Objects.equals((String) o, val.getString())) &&
                        (!Objects.equals((String) o, Value.INVISIBLE)))
                    val.setString((String) o);
```

### RedundantCast
Casting `o` to `String` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
                // OPENJPA-1830 Do not overwrite existing string values with "******"
                if ((!Objects.equals((String) o, val.getString())) &&
                        (!Objects.equals((String) o, Value.INVISIBLE)))
                    val.setString((String) o);
            } else {
```

### RedundantCast
Casting `descs.toArray(...)` to `MethodDescriptor[]` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            }
        }
        _mds = (MethodDescriptor[]) descs.
            toArray(new MethodDescriptor[descs.size()]);
        return _mds;
```

### RedundantCast
Casting `_vals.get(...)` to `Value` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
        Value val;
        for (int i = 0; i < _vals.size(); i++) {
            val = (Value) _vals.get(i);
            try {
                _pds[i] = getPropertyDescriptor(val);
```

### RedundantCast
Casting `_vals.toArray(...)` to `Value[]` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
    @Override
    public Value[] getValues() {
        return (Value[]) _vals.toArray(new Value[_vals.size()]);
    }

```

### RedundantCast
Casting `null` to `Method` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            // be thrown. Try to create a PD with no read or write methods.
            pd = new PropertyDescriptor(Introspector.decapitalize(prop),
                (Method) null, (Method) null);
        }
        pd.setDisplayName(findLocalized(prop + "-name", true, val.getScope()));
```

### RedundantCast
Casting `null` to `Method` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            // be thrown. Try to create a PD with no read or write methods.
            pd = new PropertyDescriptor(Introspector.decapitalize(prop),
                (Method) null, (Method) null);
        }
        pd.setDisplayName(findLocalized(prop + "-name", true, val.getScope()));
```

### RedundantCast
Casting `allowed.toArray(...)` to `String[]` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
        }
        if (!allowed.isEmpty())
            pd.setValue(ATTRIBUTE_ALLOWED_VALUES, (String[]) allowed.toArray
                (new String[allowed.size()]));

```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `ClassLoader` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataParser.java`
#### Snippet
```java
                if (overrideCL) {
                    oldLoader =
                        (ClassLoader) AccessController.doPrivileged(J2DoPrivHelper.getContextClassLoaderAction());
                    newLoader = XMLMetaDataParser.class.getClassLoader();
                    AccessController.doPrivileged(J2DoPrivHelper.setContextClassLoaderAction(newLoader));
```

### RedundantCast
Casting `_loaders.get(...)` to `ClassLoader` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/MultiClassLoader.java`
#### Snippet
```java
     */
    public ClassLoader getClassLoader(int index) {
        ClassLoader loader = (ClassLoader) _loaders.get(index);
        if (loader == THREAD_LOADER)
            loader = AccessController.doPrivileged(
```

### RedundantCast
Casting `_localizers.get(...)` to `Localizer` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Localizer.java`
#### Snippet
```java
        // no locking; ok if bundle created multiple times
        // check for cached version
        Localizer loc = (Localizer) _localizers.get(key);
        if (loc != null)
            return loc;
```

### RedundantCast
Casting `pae.getException()` to `IOException` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Files.java`
#### Snippet
```java
        } catch (PrivilegedActionException pae) {
            throw new RuntimeException(
                (IOException) pae.getException());
        } catch (IOException ioe) {
            throw new RuntimeException(ioe);
```

### RedundantCast
Casting `objectObjectEntry` to `Map.Entry` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
        if (defaults != null) {
            for (Map.Entry<Object, Object> objectObjectEntry : defaults.entrySet()) {
                entry = (Map.Entry) objectObjectEntry;
                if (!containsKey(entry.getKey()))
                    setInto(obj, entry);
```

### RedundantCast
Casting `objectObjectEntry` to `Map.Entry` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
        Map.Entry e;
        for (Map.Entry<Object, Object> objectObjectEntry : entrySet()) {
            e = (Map.Entry) objectObjectEntry;
            if (!setInto(obj, e)) {
                if (invalidEntries == null)
```

### RedundantCast
Casting `_paramBatch.get(...)` to `List` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                            // set the current params to the saved values
                            if (indexOfFirstFailedObject < _paramBatch.size())
                                _params = (List<String>) _paramBatch.get(indexOfFirstFailedObject);
                        }
                    }
```

### RedundantCast
Casting `_paramBatch.get(...)` to `List` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                            // set the current params to the saved values
                            if (indexOfFirstFailedObject < _paramBatch.size()){
                                _params = (List<String>) _paramBatch.get(indexOfFirstFailedObject);
                            }
                        }
```

### RedundantCast
Casting `null` to `Object` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
            public Object run() {
                aObj.setAccessible(flag);
                return (Object) null;
            }
        };
```

### RedundantCast
Casting `element.getAnnotation(...)` to `T` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
            @Override
            public T run() {
                return (T) element.getAnnotation(annotationClazz);
            }
        };
```

### RedundantCast
Casting `errors.toArray(...)` to `Throwable []` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java
        }

        Throwable[] t = (Throwable []) errors.toArray(
            new Throwable [errors.size()]);
        throw new InvalidStateException(_loc.get("tm-not-found")).
```

### RedundantCast
Casting `field.getValueInfo().getColumns()` to `List` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            .getDBDictionaryInstance();
        int def = dict.getJDBCType(field.getTypeCode(), false);
        for (Column col : (List<Column>) field.getValueInfo().getColumns()) {
            if (col.getType() == def)
                continue;
```

### RedundantCast
Casting `field.getValueInfo().getColumns()` to `List` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
     */
    private boolean isLob(FieldMapping field) {
        for (Column col : (List<Column>) field.getValueInfo().getColumns())
            if (col.getType() == Types.BLOB || col.getType() == Types.CLOB)
                return true;
```

### RedundantCast
Casting `secondaryInfo.getSecondaryTableJoinColumns(...)` to `List` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        List<Column> cols = null;
        if (secondaryInfo != null)
            cols = (List<Column>) secondaryInfo.getSecondaryTableJoinColumns
                (table);

```

### RedundantCast
Casting `info.getColumns()` to `List` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java

        ValueMappingInfo info = field.getValueInfo();
        List<Column> cols = (List<Column>) info.getColumns();
        if (cols == null || cols.size() == 0)
            return false;
```

### RedundantCast
Casting `info2.getColumns()` to `List` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
            return false;
        ValueMappingInfo info2 = field2.getValueInfo();
        List<Column> cols2 = (List<Column>) info2.getColumns();
        if (cols2 == null || cols2.size() != cols.size())
            return true;
```

### RedundantCast
Casting `info.getColumns()` to `List` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        String secondary)
        throws SAXException {
        List<Column> cols = (List<Column>) info.getColumns();
        if (cols == null)
            return;
```

### RedundantCast
Casting `_marshallers.get(...)` to `CacheMarshaller` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/CacheMarshallersValue.java`
#### Snippet
```java
        initialize();

        CacheMarshaller cm = (CacheMarshaller) _marshallers.get(id);
        if (cm == null) {
            if (getLog().isTraceEnabled())
```

### RedundantCast
Casting `cols.get(...)` to `Column` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        }

        Column col = (Column) cols.get(0);
        switch (_mapKeyTemporal) {
            case DATE:
```

### RedundantCast
Casting `defMap._fm` to `FieldMapping` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java

        for (DeferredEmbeddableOverrides defMap : defMappings) {
            FieldMapping fm = (FieldMapping)defMap._fm;
            if (defMap == null)
                return;
```

### RedundantCast
Casting `meta` to `Object` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
        if (cls == null)
            throw new MetaDataException(_loc.get("bad-class", name,
                (vmd == null) ? (Object) meta : (Object) vmd));

        return cls;
```

### RedundantCast
Casting `vmd` to `Object` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
        if (cls == null)
            throw new MetaDataException(_loc.get("bad-class", name,
                (vmd == null) ? (Object) meta : (Object) vmd));

        return cls;
```

### RedundantCast
Casting `o` to `Boolean` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
        switch (typeCode) {
            case BOOLEAN:
                return ((Boolean) o).equals(Boolean.FALSE) ? true : false;
            case BYTE:
                return ((Byte) o) == 0 ? true : false;
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `Method[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InterfaceImplGenerator.java`
#### Snippet
```java
    private void invalidateNonBeanMethods(BCClass bc, Class<?> iface,
        Set<Method> methods) {
        Method[] meths = (Method[]) AccessController.doPrivileged(
            J2DoPrivHelper.getDeclaredMethodsAction(iface));
        BCMethod meth;
```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InterfaceImplGenerator.java`
#### Snippet
```java
            Method meth = AccessController.doPrivileged(
                J2DoPrivHelper.getDeclaredMethodAction(iface, "is" +
                    StringUtil.capitalize(fmd.getName()), (Class[]) null));
            return meth == null;
        } catch (PrivilegedActionException pae) {}
```

### RedundantCast
Casting `facadeType` to `Object` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/StoreFacadeTypeRegistry.java`
#### Snippet
```java
    public void registerImplementation(Class facadeType, Class storeType,
        Class implType) {
        Object key = (storeType == null) ? (Object) facadeType
            : new Key(facadeType, storeType);
        _impls.put(key, implType);
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `InputStream` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                        log.trace(_loc.get("scanning-jar-at-url", url));
                    try {
                        InputStream is = (InputStream)
                                AccessController.doPrivileged(
                                        J2DoPrivHelper.openStreamAction(url));
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `InputStream` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java

                    try {
                        is = new BufferedInputStream((InputStream) AccessController.
                                doPrivileged(J2DoPrivHelper.openStreamAction(url)));
                    }
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `InputStream` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                            log.trace(_loc.get("scanning-jar-stream-url", url));
                        try {
                            InputStream is = (InputStream)
                                    AccessController.doPrivileged(
                                            J2DoPrivHelper.openStreamAction(url));
```

### RedundantCast
Casting `cpath.toArray(...)` to `String[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
        }
        if (cpath != null) {
            String[] dirs = (String[]) cpath.toArray(new String[cpath.size()]);
            scan(new ClasspathMetaDataIterator(dirs, newMetaDataFilter()),
                cparser, names, true, dirs);
```

### RedundantCast
Casting `cols.get(...)` to `Column` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        }

        Column col = (Column) cols.get(0);
        switch (anno.value()) {
            case DATE:
```

### RedundantCast
Casting `cols.get(...)` to `Column` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        }

        Column col = (Column) cols.get(0);
        switch (anno.value()) {
            case DATE:
```

### RedundantCast
Casting `cols.get(...)` to `Column` is redundant
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        for (int i = 0; i < pcols.length; i++) {
            if (cols.size() > i)
                setupColumn((Column) cols.get(i), pcols[i], delimit());
            else {
                if (cols.isEmpty())
```

### RedundantCast
Casting `addValue(...)` to `MetaDataRepositoryValue` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfigurationImpl.java`
#### Snippet
```java
        metaFactoryPlugin = addPlugin("MetaDataFactory", false);

        metaRepositoryPlugin = (MetaDataRepositoryValue) addValue(new MetaDataRepositoryValue());

        connectionFactory = addObject("ConnectionFactory");
```

### RedundantCast
Casting `addValue(...)` to `CacheMarshallersValue` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfigurationImpl.java`
#### Snippet
```java
        runtimeUnenhancedClasses.setAliasListComprehensive(true);

        cacheMarshallerPlugins = (CacheMarshallersValue) addValue(new CacheMarshallersValue(this));

        eagerInitialization = addBoolean("InitializeEagerly");
```

### RedundantCast
Casting `null` to `ClassLoader` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/Id.java`
#### Snippet
```java
     */
    public Id(String str) {
        this(str, (ClassLoader) null);
    }

```

### RedundantCast
Casting `objectObjectEntry` to `Map.Entry` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
        Object extValue, fieldValue;
        for (Map.Entry<Object, Object> objectObjectEntry : values.entrySet()) {
            entry = (Map.Entry) objectObjectEntry;
            fieldValue = transform((String) entry.getKey(),
                    getDeclaredTypeCode());
```

### RedundantCast
Casting `_map` to `Map` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
            switch (_view) {
                case VIEW_KEYS:
                    return ((Map) _map).keySet();
                case VIEW_VALUES:
                    return ((Map) _map).values();
```

### RedundantCast
Casting `_map` to `Map` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
                    return ((Map) _map).keySet();
                case VIEW_VALUES:
                    return ((Map) _map).values();
                default:
                    return ((Map) _map).entrySet();
```

### RedundantCast
Casting `_map` to `Map` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
                    return ((Map) _map).values();
                default:
                    return ((Map) _map).entrySet();
            }
        }
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `Method[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/MethodLifecycleCallbacks.java`
#### Snippet
```java
        Class currentClass = cls;
        do {
            Method[] methods = (Method[]) AccessController.doPrivileged(
                J2DoPrivHelper.getDeclaredMethodsAction(currentClass));
            for (Method value : methods) {
```

### RedundantCast
Casting `curVal` to `long` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/TransferFieldManager.java`
#### Snippet
```java
    public void storeCharField(int field, char curVal) {
        this.field = field;
        longval = (long) curVal;
    }

```

### RedundantCast
Casting `pae.getException()` to `IOException` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
                    return s;
                } catch (PrivilegedActionException pae) {
                    throw (IOException) pae.getException();
                }
            }
```

### RedundantCast
Casting `values[i]` to `ProxyCalendar` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultShapePacker.java`
#### Snippet
```java
                    for (int i = 0; i < values.length; i++) {
                        if (values[i] instanceof ProxyCalendar) {
                            values[i] = ((ProxyCalendar) values[i]).copy((ProxyCalendar) values[i]);
                        }
                    }
```

### RedundantCast
Casting `data` to `Locale` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                return ((Date) data).clone();
            case JavaTypes.LOCALE:
                return (Locale) data;
            case JavaTypes.PC:
                if (vmd.isEmbedded())
```

### RedundantCast
Casting `value` to `Locale` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
            case JavaTypes.LOCALE:
                for (Object value : data) {
                    ret.add((Locale) value);
                }
                return ret;
```

### RedundantCast
Casting `val` to `Locale` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                    return ((Date) val).clone();
            case JavaTypes.LOCALE:
                return (Locale) val;
            case JavaTypes.OBJECT:
                if (val instanceof Proxy)
```

### RedundantCast
Casting `entry.getValue()` to `Class` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
                            userParams.size(), userParams}));
                Object value = userParams.get(key);
                validateParameterValue(key, value, (Class)entry.getValue());
                arr[idx] = value;
            }
```

### RedundantCast
Casting `metas.toArray(...)` to `ClassMetaData[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
            if (metas == null)
                return StoreQuery.EMPTY_METAS;
            return (ClassMetaData[]) metas.toArray
                (new ClassMetaData[metas.size()]);
        }
```

### RedundantCast
Casting `arg` to `Val` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Args.java`
#### Snippet
```java
        visitor.enter(this);
        for (Value arg : _args) {
            ((Val) arg).acceptVisit(visitor);
        }
        visitor.exit(this);
```

### RedundantCast
Casting `_args.get(...)` to `Val` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Args.java`
#### Snippet
```java
        Class[] c = new Class[_args.size()];
        for (int i = 0; i < _args.size(); i++)
            c[i] = ((Val) _args.get(i)).getType();
        return c;
    }
```

### RedundantCast
Casting `_accessPath.toArray(...)` to `ClassMetaData[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
     */
    protected ClassMetaData[] getAccessPath() {
        return (ClassMetaData[]) _accessPath.toArray
            (new ClassMetaData[_accessPath.size()]);
    }
```

### RedundantCast
Casting `_metas.get(...)` to `Meta` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCRegistry.java`
#### Snippet
```java
     */
    private static Meta getMeta(Class<?> pcClass) {
        Meta ret = (Meta) _metas.get(pcClass);
        if (ret == null)
            throw new IllegalStateException(_loc.get("no-meta", pcClass).
```

### RedundantCast
Casting `AccessController.doPrivileged(...)` to `Method[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    static Method getDeclaredMethod(Class cls, String name,
        Class param) {
        Method[] methods = (Method[]) AccessController.doPrivileged(
            J2DoPrivHelper.getDeclaredMethodsAction(cls));
        Method candidate = null;
```

### RedundantCast
Casting `sm.getObjectId()` to `OpenJPAId` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java

            for (OpenJPAStateManager sm : smList) {
                oidList.add((OpenJPAId) sm.getObjectId());
            }

```

### RedundantCast
Casting `modMap.get(...)` to `Modifications` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
     */
    private static Modifications getModifications(Map<DataCache,Modifications> modMap, DataCache cache) {
        Modifications mods = (Modifications) modMap.get(cache);
        if (mods == null) {
            mods = new Modifications();
```

### RedundantCast
Casting `v2` to `Comparable` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/abstractstore/AbstractStoreManager.java`
#### Snippet
```java
            return VERSION_DIFFERENT;

        int compare = ((Comparable) v1).compareTo((Comparable) v2);
        if (compare < 0)
            return VERSION_EARLIER;
```

### RedundantCast
Casting `in.readObject()` to `String` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryKey.java`
#### Snippet
```java
        _subclasses = in.readBoolean();
        _accessPathClassNames = (Set<String>) in.readObject();
        _query = (String) in.readObject();
        _ignoreChanges = in.readBoolean();
        _params = (Map<Object,Object>) in.readObject();
```

### RedundantCast
Casting `t` to `ProcessingException` is redundant
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTServlet.java`
#### Snippet
```java
    protected void handleError(JPAServletContext ctx, Throwable t) throws IOException {
        if (t instanceof ProcessingException) {
            ((ProcessingException)t).printStackTrace();
        } else {
            new ProcessingException(ctx, t).printStackTrace();
```

### RedundantCast
Casting `'Z'` to `int` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/util/Randomizer.java`
#### Snippet
```java
    private static final Random rng = new Random(System.currentTimeMillis());
    private static final int MIN_ALPHA = (int)'A';
    private static final int MAX_ALPHA = (int)'Z';

    /**
```

### RedundantCast
Casting `'A'` to `int` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/util/Randomizer.java`
#### Snippet
```java
public class Randomizer {
    private static final Random rng = new Random(System.currentTimeMillis());
    private static final int MIN_ALPHA = (int)'A';
    private static final int MAX_ALPHA = (int)'Z';

```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getDeclaredMethodAction(
                        SMTYPE, "get" + CONTEXTNAME, (Class[]) null)), false);

            // pcFetchObjectId
```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getDeclaredMethodAction(
                        SMTYPE, "fetchObjectId", (Class[]) null)), false);

            // pcIsDeleted
```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getDeclaredMethodAction(
                        SMTYPE, "isDeleted", (Class[]) null)), false);

            // pcIsDirty
```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getDeclaredMethodAction(
                        SMTYPE, "isDirty", (Class[]) null)), true);

            // pcIsNew
```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getDeclaredMethodAction(
                        SMTYPE, "isNew", (Class[]) null)), false);

            // pcIsPersistent
```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getDeclaredMethodAction(
                        SMTYPE, "isPersistent", (Class[]) null)), false);

            // pcIsTransactional
```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getDeclaredMethodAction(
                        SMTYPE, "isTransactional", (Class[]) null)), false);

            // pcSerializing
```

### RedundantCast
Casting `null` to `Class[]` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getDeclaredMethodAction(
                        SMTYPE, "serializing", (Class[]) null)), false);

            // pcDirty
```

### RedundantCast
Casting `pae.getException()` to `FileNotFoundException` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/ant/ReverseMappingToolTask.java`
#### Snippet
```java
                    J2DoPrivHelper.newFileInputStreamAction(propsFile));
            } catch (PrivilegedActionException pae) {
                 throw (FileNotFoundException) pae.getException();
            }
            customProps.load(fis);
```

### RedundantCast
Casting `errs.toArray(...)` to `SQLException[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLExceptions.java`
#### Snippet
```java
            se = se.getNextException();
        }
        return (SQLException[]) errs.toArray(new SQLException[errs.size()]);
    }
}
```

### RedundantCast
Casting `idxList.toArray(...)` to `Index[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            while (rs != null && rs.next())
                idxList.add(newIndex(rs));
            return (Index[]) idxList.toArray(new Index[idxList.size()]);
        } finally {
            if (rs != null)
```

### RedundantCast
Casting `fkList.toArray(...)` to `ForeignKey[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                fkList.add(nfk);
            }
            return (ForeignKey[]) fkList.toArray
                (new ForeignKey[fkList.size()]);
        } finally {
```

### RedundantCast
Casting `seqs.get(...)` to `String` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        System.arraycopy(create, 0, sql, 0, create.length);
        for (int i = 0; i < seqs.size(); i++)
            sql[create.length + i] = (String) seqs.get(i);
        return sql;
    }
```

### RedundantCast
Casting `null` to `Joins` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    @Override
    public void groupBy(SQLBuffer sql) {
        groupBy(sql, (Joins) null);
    }

```

### RedundantCast
Casting `null` to `Joins` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    @Override
    public boolean selectIdentifier(Column col) {
        return selectIdentifier(col, (Joins) null);
    }

```

### RedundantCast
Casting `null` to `Joins` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    public boolean orderBy(SQLBuffer sql, boolean asc, boolean sel, Value selAs)
    {
        return orderBy(sql, asc, (Joins) null, sel, selAs);
    }

```

### RedundantCast
Casting `null` to `Joins` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    @Override
    public void groupBy(String sql) {
        groupBy(sql, (Joins) null);
    }

```

### RedundantCast
Casting `sql` to `Object` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    boolean orderBy(String sql, boolean asc, Joins joins, boolean sel,
        boolean aliasOrder) {
        return orderBy((Object) sql, asc, joins, sel, aliasOrder, null);
    }

```

### RedundantCast
Casting `_subsels.get(...)` to `SelectImpl` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                SelectImpl sub, selSub;
                for (int j = 0; j < _subsels.size(); j++) {
                    sub = (SelectImpl) _subsels.get(j);
                    selSub = (SelectImpl) sub.fullClone(1);
                    selSub._parent = sel;
```

### RedundantCast
Casting `null` to `Joins` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
    @Override
    public boolean select(Column col) {
        return select(col, (Joins) null);
    }

```

### RedundantCast
Casting `res` to `AbstractResult` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/LogicalUnion.java`
#### Snippet
```java
        if (sels.length == 1) {
            Result res = sels[0].execute(store, fetch, lockLevel);
            ((AbstractResult) res).setBaseMapping(mappings[0]);
            return res;
        }
```

### RedundantCast
Casting `'a'` to `char` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/JavaSQLTypes.java`
#### Snippet
```java

    private static final Byte NONZERO_BYTE = (byte) 1;
    private static final Character NONZERO_CHAR = (char) 'a';
    private static final Double NONZERO_DOUBLE = 1d;
    private static final Float NONZERO_FLOAT = 1f;
```

### RedundantCast
Casting `importedKeyList.toArray(...)` to `ForeignKey[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                importedKeyList.add(nfk);
            }
            return (ForeignKey[]) importedKeyList.toArray
                (new ForeignKey[importedKeyList.size()]);
        }
```

### RedundantCast
Casting `sql.toArray(...)` to `String[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        }

        return (String[]) sql.toArray(new String[sql.size()]);
    }

```

### RedundantCast
Casting `val` to `int` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        if ((col != null && col.isCompatible(Types.INTEGER, null, 0, 0))
            || (col == null && storeCharsAsNumbers))
            setInt(stmnt, idx, (int) val, col);
        else
            setString(stmnt, idx, String.valueOf(val), col);
```

### RedundantCast
Casting `_dropMap.toArray(...)` to `ClassMapping[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
            mappings = repos.getMappings();
        else if (_dropMap != null)
            mappings = (ClassMapping[]) _dropMap.toArray
                (new ClassMapping[_dropMap.size()]);
        else
```

### RedundantCast
Casting `cols.get(...)` to `Column` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
            return false;

        Column col = (Column) cols.get(0);
        if (col.getSize() != -1 && col.getType() != Types.CLOB)
            return false;
```

### RedundantCast
Casting `_results.get(...)` to `QueryResultMapping` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        if (_locking) {
            synchronized (this) {
                return (QueryResultMapping) _results.get(getQueryResultKey(cls, name));
            }
        } else {
```

### RedundantCast
Casting `_results.get(...)` to `QueryResultMapping` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
            }
        } else {
            return (QueryResultMapping) _results.get(getQueryResultKey(cls, name));
        }
    }
```

### RedundantCast
Casting `this` to `FieldMetaData` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/FieldMapping.java`
#### Snippet
```java
        // is different than the defining metadata.
        if (isVersion()){
            ClassMapping cm = (ClassMapping)((FieldMetaData)this).getDefiningMetaData();
            return cm.getVersion().getColumns();
        }else
```

### RedundantCast
Casting `info.getColumns().get(...)` to `Column` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ColumnVersionStrategy.java`
#### Snippet
```java
        	for (int i = 0; i < info.getColumns().size(); i++) {
                templates[i] = new Column();
        		Column infoColumn = (Column)info.getColumns().get(i);
        		templates[i].setTableIdentifier(infoColumn.getTableIdentifier());
        		templates[i].setType(infoColumn.getType());
```

### RedundantCast
Casting `given.get(...)` to `Column` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
        Column col;
        for (int i = 0; i < tmplates.length; i++) {
            col = (given.isEmpty()) ? null : (Column) given.get(i);
            cols[i] = mergeColumn(context, prefix, tmplates[i], true, col,
                table, adapt, fill);
```

### RedundantCast
Casting `null` to `Object` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedFieldStrategy.java`
#### Snippet
```java
            return null;

        Object obj = res.getObject(cols[0], -1, (Object) null);
        if (((EmbeddedClassStrategy) field.getEmbeddedMapping().getStrategy()).
            indicatesNull(obj))
```

### RedundantCast
Casting `vm` to `FieldMapping` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
        if (embeddedMeta == null)
            return;
        List mapsIdColList = ((FieldMapping)vm).getValueInfo().getMapsIdColumns();
        if (mapsIdColList.size() > 0 ) {
            setMapsIdCols(mapsIdColList, embeddedMeta);
```

### RedundantCast
Casting `vers.getMappingInfo().getColumns().get(...)` to `Column` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MultiColumnVersionStrategy.java`
#### Snippet
```java
    	}
    	if (_javaTypes[i] == null) {
    		Column col = (Column)vers.getMappingInfo().getColumns().get(i);
    		if (!StringUtil.isEmpty(col.getTypeName())) {
    			Class javaType = Schemas.getJavaType(col.getType(),
```

### RedundantCast
Casting `pk.getValue()` to `ValueMappingImpl` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java

    private List getMappedByIdColumns(FieldMapping pk) {
        ClassMetaData embeddedId = ((ValueMappingImpl)pk.getValue()).
            getEmbeddedMetaData();
        Column[] pkCols = null;
```

### RedundantCast
Casting `_stats.get(...)` to `Status` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ClassTableJDBCSeq.java`
#### Snippet
```java
            return null;
        String key = getKey(mapping, false);
        Status stat = (Status) _stats.get(key);
        if (stat == null) {
            stat = new Status();
```

### RedundantCast
Casting `select` to `SelectImpl` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
    private boolean isPaginated() {
        if (select instanceof SelectImpl) {
            if (((SelectImpl)select).getStartIndex() != 0 ||
                ((SelectImpl)select).getEndIndex() != Long.MAX_VALUE)
                return true;
```

### RedundantCast
Casting `select` to `SelectImpl` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
        if (select instanceof SelectImpl) {
            if (((SelectImpl)select).getStartIndex() != 0 ||
                ((SelectImpl)select).getEndIndex() != Long.MAX_VALUE)
                return true;
        }
```

### RedundantCast
Casting `batchedRows.get(...)` to `RowImpl` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
        for (int i = 0; i < count.length; i++) {
            cnt = count[i];
            RowImpl row = (RowImpl) batchedRows.get(batchedRowsBaseIndex + i);
            failed = row.getFailedObject();
            switch (cnt) {
```

### RedundantCast
Casting `_batchedRows.get(...)` to `RowImpl` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
                // single entry in cache, direct SQL execution.
                try {
                    super.flushAndUpdate((RowImpl) _batchedRows.get(0));
                } finally {
                    _batchedSql = null;
```

### RedundantCast
Casting `_exclusionPatterns` to `List` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderCacheImpl.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public List<String> getExcludes() {
        return (List<String>)_exclusionPatterns == null
            ? Collections.EMPTY_LIST
            : Collections.unmodifiableList(_exclusionPatterns);
```

### RedundantCast
Casting `cols.get(...)` to `Column` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ValueMappingInfo.java`
#### Snippet
```java
        // extract given null-ind column
        List<Column> cols = getColumns();
        Column given = (cols.isEmpty()) ? null : (Column) cols.get(0);
        MappingDefaults def = val.getMappingRepository().getMappingDefaults();
        if (given == null && (!adapt && !def.defaultMissingInfo()))
```

### RedundantCast
Casting `val` to `Type` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/JDBCExpressionFactory.java`
#### Snippet
```java
        if (!(val instanceof Type))
            return false;
        ClassMapping cm = (ClassMapping)((Type)val).getMetaData();
        String strat = cm.getMappingInfo().getHierarchyStrategy();
        if (strat != null && strat.equals(VerticalClassStrategy.ALIAS))
```

### RedundantCast
Casting `_val` to `PCPath` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/FilterValueImpl.java`
#### Snippet
```java
    @Override
    public PCPath getXPath() {
        if (isPath() && ((PCPath) _val).isXPath())
            return (PCPath) _val;
        else
```

### RedundantCast
Casting `_tables.toArray(...)` to `Table[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
    @Override
    public Table[] getTables() {
        return (Table[]) _tables.toArray(new Table[_tables.size()]);
    }

```

### RedundantCast
Casting `_seqs.toArray(...)` to `Sequence[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java

    public Sequence[] getSequences() {
        return (Sequence[]) _seqs.toArray(new Sequence[_seqs.size()]);
    }

```

### RedundantCast
Casting `cols.next()` to `String` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            pks = fkInfo.pks.iterator();
            for (cols = fkInfo.cols.iterator(); cols.hasNext(); ) {
                colName = (String) cols.next();
                col = fkInfo.fk.getTable().getColumn(colName);
                if (col == null)
```

### RedundantCast
Casting `pks.next()` to `String` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
                            fkInfo.fk, colName, fkInfo.fk.getTable()));

                pkColName = (String) pks.next();
                pkCol = toTable.getColumn(pkColName);
                if (pkCol == null)
```

### RedundantCast
Casting `_schemaMap.values().toArray(...)` to `Schema[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGroup.java`
#### Snippet
```java
    public Schema[] getSchemas() {
        if (_schemas == null)
            _schemas = (_schemaMap == null) ? new Schema[0] : (Schema[])
                _schemaMap.values().toArray(new Schema[_schemaMap.size()]);
        return _schemas;
```

### RedundantCast
Casting `lastFieldAction()` to `Action` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java

    public void verifyIndexedField() {
        Action lastAction = (Action) lastFieldAction();
        FieldMapping fm = (FieldMapping) lastAction.data;
        if (fm.getOrderColumn() == null)
```

### RedundantCast
Casting `last.data` to `String` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java

        Action last = _actions == null ? null : (Action) _actions.getLast();
        if (last != null && last.op == Action.VAR && ((String)last.data).equals(last.var)) {
            _cid = true;
            return;
```

### RedundantCast
Casting `pCtx.subquery` to `SubQ` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
            return null;
        if (pCtx.getSchema(_schemaAlias) != null)
            return ((SubQ)pCtx.subquery).getCandidateAlias();
        return findSubqAlias(pSel);
    }
```

### RedundantCast
Casting `_colList.toArray(...)` to `Column[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/LocalConstraint.java`
#### Snippet
```java
        if (_cols == null)
            _cols = (_colList == null) ? Schemas.EMPTY_COLUMNS
                : (Column[]) _colList.toArray(new Column[_colList.size()]);
        return _cols;
    }
```

### RedundantCast
Casting `_seqMap.values().toArray(...)` to `Sequence[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java
    public Sequence[] getSequences() {
        if (_seqs == null)
            _seqs = (_seqMap == null) ? new Sequence[0] : (Sequence[])
                _seqMap.values().toArray(new Sequence[_seqMap.size()]);
        return _seqs;
```

### RedundantCast
Casting `_tableMap.values().toArray(...)` to `Table[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java
    public Table[] getTables() {
        if (_tables == null)
            _tables = (_tableMap == null) ? new Table[0] : (Table[])
                _tableMap.values().toArray(new Table[_tableMap.size()]);
        return _tables;
```

### RedundantCast
Casting `null` to `DBIdentifier[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java

            if (schemaMap == null) {
                generateSchema(DBIdentifier.NULL, (DBIdentifier[]) null);

                // estimate the number of schema objects we will need to visit
```

### RedundantCast
Casting `clone()` to `QualifiedDBIdentifier` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
    @Override
    public DBIdentifier getUnqualifiedName() {
        QualifiedDBIdentifier newName = (QualifiedDBIdentifier)clone();
        newName.setObjectTableName(DBIdentifier.NULL);
        newName.setSchemaName(DBIdentifier.NULL);
```

### RedundantCast
Casting `name1` to `DBIdentifier` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
            return false;
        }
        return ((DBIdentifier)name1).equals(name2, false);
    }

```

### RedundantCast
Casting `_unqList.toArray(...)` to `Unique[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
    public Unique[] getUniques() {
        if (_unqs == null)
            _unqs = (_unqList == null) ? Schemas.EMPTY_UNIQUES : (Unique[])
                _unqList.toArray(new Unique[_unqList.size()]);
        return _unqs;
```

### RedundantCast
Casting `autos.toArray(...)` to `Column[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
                }
                _autoAssign = (autos == null) ? Schemas.EMPTY_COLUMNS
                    : (Column[]) autos.toArray(new Column[autos.size()]);
            }
        }
```

### RedundantCast
Casting `rels.toArray(...)` to `Column[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
                }
                _rels = (rels == null) ? Schemas.EMPTY_COLUMNS
                    : (Column[]) rels.toArray(new Column[rels.size()]);
            }
        }
```

### RedundantCast
Casting `_idxMap.values().toArray(...)` to `Index[]` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
    public Index[] getIndexes() {
        if (_idxs == null || _idxs.length == 0)
            _idxs = (_idxMap == null) ? Schemas.EMPTY_INDEXES : (Index[])
                _idxMap.values().toArray(new Index[_idxMap.size()]);
        return _idxs;
```

### RedundantCast
Casting `urls.toArray(...)` to `URL[]` is redundant
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java

        ClassLoader jpaRealm =
            new URLClassLoader( (URL[]) urls.toArray( new URL[urls.size()] ), getClass().getClassLoader() );

        // set the new ClassLoader as default for this Thread
```

## RuleId[id=JavadocDeclaration]
### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/InverseLogical.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/LRS.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/CallbackMode.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum CallbackMode {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ManagedInterface.java`
#### Snippet
```java
 *
 * @since 1.1.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAEntityManagerFactory.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
public interface OpenJPAEntityManagerFactory
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ExternalValues.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCache.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.1
 * @published
 */
public interface StoreCache extends Cache {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchGroups.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/Externalizer.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
`@param key` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintHandler.java`
#### Snippet
```java
    /**
     * Affirms if the given key starts with any of the known prefix.
     * @param key
     */
    protected boolean isKnownPrefix(String key) {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchAttribute.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({})
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/DetachStateType.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum DetachStateType {
```

### JavadocDeclaration
`@param strings` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
         * Both arrays must not be null, must not contain null elements and must have the same dimension.
         *
         * @param strings
         * @param numbers
         */
```

### JavadocDeclaration
`@param numbers` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
         *
         * @param strings
         * @param numbers
         */
        public StringToInteger(String[] strings, int[] numbers) {
```

### JavadocDeclaration
Wrong tag `unpublished`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
 * @author Pinaki Poddar
 * @since 2.0.0
 * @unpublished
 */
public interface HintValueConverter {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistentMap.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
`@param entity` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistenceUtil.java`
#### Snippet
```java
    /**
     * Returns the identifier of the persistent entity.
     * @param entity
     * @return The identifier of the entity or null if the entity
     * is not persistent.
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/RestoreStateType.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum RestoreStateType {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ConnectionRetainMode.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum ConnectionRetainMode {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AutoClearType.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum AutoClearType {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/DetachedState.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchGroup.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/KeyType.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryOperationType.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum QueryOperationType {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistentCollection.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/Persistent.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchPlan.java`
#### Snippet
```java
 * @author Pinaki Poddar
 * @since 0.4.1
 * @published
 */
public interface FetchPlan {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistence.java`
#### Snippet
```java
 *
 * @author Abe White
 * @published
 * @since 0.4.0
 */
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ElementDependent.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ReadOnly.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/LoadFetchGroup.java`
#### Snippet
```java
 * @author Pinaki Poddar
 * @since 1.0.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/Type.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/Dependent.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/KeyDependent.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ElementType.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/Factory.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AutoDetachType.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum AutoDetachType {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/DataStoreId.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAEnum.java`
#### Snippet
```java
     * but the conversion is done w.r.t. all enums of the generic type.
     *
     * @exception throw IllegalArgumentException if no enum instance of the generic type matches the given String.
     */
    int convertToKernelConstant(String s);
```

### JavadocDeclaration
Wrong tag value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAEnum.java`
#### Snippet
```java
     * but the conversion is done w.r.t. all enums of the generic type.
     *
     * @exception throw IllegalArgumentException if no enum instance of the generic type matches the given integer.
     */
    int convertToKernelConstant(int i);
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/UpdateAction.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
public enum UpdateAction {
```

### JavadocDeclaration
Wrong tag value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
     * LifecycleEventManager type based on if the jakarta.validation APIs are
     * available and a ValidatorImpl is required by the configuration.
     * @throws if validation setup failed and was required by the config
     */
    private void loadValidator(BrokerFactory factory) {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
 *
 * @see jakarta.persistence.Persistence#createEntityManagerFactory(String,Map)
 * @published
 */
public class PersistenceProviderImpl
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/Extent.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
public interface Extent<T>
```

### JavadocDeclaration
Wrong tag value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
     * Unwraps this receiver to an instance of the given class, if possible.
     *
     * @exception if the given class is null, generic <code>Object.class</code> or a class
     * that is not wrapped by this receiver.
     *
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryResultCache.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.1
 * @published
 */
public interface QueryResultCache {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAEntityTransaction.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public interface OpenJPAEntityTransaction
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAQuery.java`
#### Snippet
```java
 * @since 0.4.0
 * @author Abe White
 * @published
 */
public interface OpenJPAQuery<X> extends TypedQuery<X> {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/DataCache.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
     * Gets the kernel delegate that is handles actual execution on behalf of this receiver.
     *
     * @return
     */
    public org.apache.openjpa.kernel.Query getExecutableQuery() {
```

### JavadocDeclaration
`@param meta` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
     * Construct a query for executing a Stored Procedure.
     *  @param procedureName name of the database stored procedure.
     * @param meta
     * @param delegate      the delegate which manages bind parameters on behalf of this
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
     * Gets the facade delegate that manages bind parameters on behalf of this receiver.
     *
     * @return
     */
    public OpenJPAQuery<?> getDelegate() {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/Generator.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.1
 * @published
 */
public interface Generator {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAEntityManager.java`
#### Snippet
```java
 * @since 0.4.0
 * @author Abe White
 * @published
 */
public interface OpenJPAEntityManager
```

### JavadocDeclaration
`@param ` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * Validates the given field of the meta class matches the given
     * FieldMetaData and
     * @param <X>
     * @param <Y>
     * @param mField
```

### JavadocDeclaration
`@param ` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * FieldMetaData and
     * @param <X>
     * @param <Y>
     * @param mField
     * @param member
```

### JavadocDeclaration
`@param mField` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * @param <X>
     * @param <Y>
     * @param mField
     * @param member
     */
```

### JavadocDeclaration
`@param member` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * @param <Y>
     * @param mField
     * @param member
     */
    void validate(Field metaField, FieldMetaData fmd) {
```

### JavadocDeclaration
`@param ` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    /**
     * Instantiate
     * @param <X>
     * @param <V>
     * @param cls
```

### JavadocDeclaration
`@param ` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * Instantiate
     * @param <X>
     * @param <V>
     * @param cls
     * @param container
```

### JavadocDeclaration
`@param cls` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * @param <X>
     * @param <V>
     * @param cls
     * @param container
     * @param expected
```

### JavadocDeclaration
`@param container` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * @param <V>
     * @param cls
     * @param container
     * @param expected
     */
```

### JavadocDeclaration
`@param expected` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
     * @param cls
     * @param container
     * @param expected
     */
    private <X,V extends ManagedType<?>> void instantiate(Class<X> cls, ClassMetaData meta,
```

### JavadocDeclaration
`@param conf` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
     * core configuration.
     *
     * @param conf
     */
    private void configureBeanValidation(OpenJPAConfigurationImpl conf) {
```

### JavadocDeclaration
`@param predicate` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * previous having restriction(s), if any.
     *
     * @param predicate
     * @return the modified QueryDefinition Instance
     */
```

### JavadocDeclaration
`@param arg1` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * first expression if they are not.
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
```

### JavadocDeclaration
`@param arg2` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
     */
```

### JavadocDeclaration
`@param exp1` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * first expression if they are not.
     *
     * @param exp1
     * @param exp2
     * @return Expression corresponding to the given nullif expression
```

### JavadocDeclaration
`@param exp2` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     *
     * @param exp1
     * @param exp2
     * @return Expression corresponding to the given nullif expression
     */
```

### JavadocDeclaration
`@param arg1` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * first expression if they are not.
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
```

### JavadocDeclaration
`@param arg2` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
     */
```

### JavadocDeclaration
`@param arg1` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * first expression if they are not.
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
```

### JavadocDeclaration
`@param arg2` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
     */
```

### JavadocDeclaration
`@param arg1` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * first expression if they are not.
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
```

### JavadocDeclaration
`@param arg2` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
     */
```

### JavadocDeclaration
`@param arg1` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * first expression if they are not.
     *
     * @param arg1
     * @param arg2 Criteria API Java Persistence 2.0, Public Review Draft
     *             Criteria API Interfaces 10/31/08 158 JSR-317 Public Review
```

### JavadocDeclaration
Wrong tag `result`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     *             select items that correspond to result types that are valid
     *             as arguments to the constructor
     * @result SelectItem instance representing the constructor
     */
    SelectItem newInstance(Class cls, SelectItem... args);
```

### JavadocDeclaration
`@param pathExprs` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * Replaces the previous group-by list, if any.
     *
     * @param pathExprs
     * @return the modified QueryDefinition instance
     */
```

### JavadocDeclaration
`@param pathExprList` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * Replaces the previous group-by list, if any.
     *
     * @param pathExprList
     * @return the modified QueryDefinition instance
     */
```

### JavadocDeclaration
`@param arg1` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     * first expression if they are not.
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
```

### JavadocDeclaration
`@param arg2` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinition.java`
#### Snippet
```java
     *
     * @param arg1
     * @param arg2
     * @return Expression corresponding to the given nullif expression
     */
```

### JavadocDeclaration
`@param type` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
     * null otherwise.
     *
     * @param type
     * @return FIELD or PROPERTY
     */
```

### JavadocDeclaration
Wrong tag value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
     *
     * @param e a field or method.
     * @exception if given member is neither a field nor a method.
     */
    TypeMirror getDeclaredType(Element e) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
     * @param embedType embeddable class
     * @param access class level access for embeddable
     * @throws SAXException
     */
    protected void addDeferredEmbeddableMetaData(Class<?> embedType,
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
     * given pattern.
     *
     * @param pattern
     * @param escapeChar
     * @return conditional predicate
```

### JavadocDeclaration
`@param escapeChar` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
     *
     * @param pattern
     * @param escapeChar
     * @return conditional predicate
     */
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
     * given pattern.
     *
     * @param pattern
     * @return conditional predicate
     */
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
     * the given pattern.
	 *
	 * @param pattern
	 * @param escapeChar
	 * @return conditional predicate
```

### JavadocDeclaration
`@param escapeChar` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
	 *
	 * @param pattern
	 * @param escapeChar
	 * @return conditional predicate
	 */
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
     * the given pattern.
	 *
	 * @param pattern
	 * @param escapeChar
	 * @return conditional predicate
```

### JavadocDeclaration
`@param escapeChar` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
	 *
	 * @param pattern
	 * @param escapeChar
	 * @return conditional predicate
	 */
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
	 * given pattern.
	 *
	 * @param pattern
	 * @param escapeChar
	 * @return conditional predicate
```

### JavadocDeclaration
`@param escapeChar` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
	 *
	 * @param pattern
	 * @param escapeChar
	 * @return conditional predicate
	 */
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/PredicateOperand.java`
#### Snippet
```java
     * the given pattern.
	 *
	 * @param pattern
	 * @return conditional predicate
	 */
```

### JavadocDeclaration
`@param meta` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
    * Parse fetch group input for the FetchGroup and FetchGroups annotations
    * as well as for the fetch-group and fetch-groups XML metadata
    * @param meta
    * @param groups
    */
```

### JavadocDeclaration
`@param groups` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
    * as well as for the fetch-group and fetch-groups XML metadata
    * @param meta
    * @param groups
    */
   static void parseFetchGroups(ClassMetaData meta, FetchGroupImpl... groups) {
```

### JavadocDeclaration
`@param path` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/AliasContext.java`
#### Snippet
```java
	 * alias name. If the alias name is assigned by this context, then a
	 * different alias is generated.
	 * @param path
	 * @return the alias name
	 */
```

### JavadocDeclaration
`@param properties` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
     * new configuration pushed (and later popped) during a single operation.
     *
     * @param properties
     */
    private void configureCurrentCacheModes(FetchPlan fetch, Map<String, Object> properties) {
```

### JavadocDeclaration
Wrong tag `Deprecated`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java

    /**
     * @Deprecated -- Use org.apache.openjpa.persistence.EntityManagerImpl.newQueryImpl(Query kernelQuery, QueryMetaData
     *             qmd)
     * <br>
```

### JavadocDeclaration
Wrong tag value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
     * Unwraps this receiver to an instance of the given class, if possible.
     *
     * @exception if the given class is null, generic <code>Object.class</code> or a class
     * that is not wrapped by this receiver.
     */
```

### JavadocDeclaration
`@param params` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/Expression.java`
#### Snippet
```java
     * of the argument list.
     *
     * @param params
     * @return conditional predicate
     */
```

### JavadocDeclaration
`@param subquery` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/Expression.java`
#### Snippet
```java
     * of a subquery result.
     *
     * @param subquery
     * @return conditional predicate
     */
```

### JavadocDeclaration
`@param strings` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/Expression.java`
#### Snippet
```java
     * of the argument list.
     *
     * @param strings
     * @return conditional predicate
     */
```

### JavadocDeclaration
`@param classes` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/Expression.java`
#### Snippet
```java
     * of the argument list.
     *
     * @param classes
     * @return conditional predicate
     */
```

### JavadocDeclaration
`@param enums` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/Expression.java`
#### Snippet
```java
     * of the argument list.
     *
     * @param enums
     * @return conditional predicate
     */
```

### JavadocDeclaration
`@param nums` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/Expression.java`
#### Snippet
```java
     * of the argument list.
     *
     * @param nums
     * @return conditional predicate
     */
```

### JavadocDeclaration
`@param builder` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinitionImpl.java`
#### Snippet
```java
	/**
	 *
	 * @param builder
	 */
	protected QueryDefinitionImpl(QueryBuilderImpl builder) {
```

### JavadocDeclaration
`@param out` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
    /**
     * Prints the class to the given Writer.
     * @param out
     */
    public void write(PrintWriter out) {
```

### JavadocDeclaration
`@param root` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
     * Registers a variable for the given root expression.
     * A root expression is registered only for cross join.
     * @param root
     * @param var
     */
```

### JavadocDeclaration
`@param var` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
     * A root expression is registered only for cross join.
     * @param root
     * @param var
     */
    void registerRoot(Root<?> root, Value var) {
```

### JavadocDeclaration
`@param conf` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidationUtils.java`
#### Snippet
```java
     * are available and then create a Validator if required by the
     * provided configuration.
     * @param conf
     * @return true if a Validator was created, false otherwise.
     * @throws If a Validator was required but could not be created.
```

### JavadocDeclaration
`@param exps` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
     * and aliases.
     *
     * @param exps
     * @param selections
     * @param projections list of kernel values for projections
```

### JavadocDeclaration
`@param selections` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
     *
     * @param exps
     * @param selections
     * @param projections list of kernel values for projections
     * @param aliases list of kernel projection aliases
```

### JavadocDeclaration
`@param event` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     * Add the validation group(s) for the specified event.  Event definitions
     * are defined in LifecycleEvent
     * @param event
     * @param group
     */
```

### JavadocDeclaration
`@param group` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     * are defined in LifecycleEvent
     * @param event
     * @param group
     */
    public void addValidationGroup(String validationGroupName, String group) {
```

### JavadocDeclaration
`@param group` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     * Converts a comma separated list of validation groups into an array
     * of classes.
     * @param group
     */
    private Class<?>[] getValidationGroup(String vgName, String group) {
```

### JavadocDeclaration
`@param ` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     * Returns whether validation is active for the given event.
     *
     * @param <T>
     * @param arg0 Type being validated
     * @param event event type
```

### JavadocDeclaration
`@param event` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     * the validation group to null.
     *
     * @param event
     * @param validationGroup
     */
```

### JavadocDeclaration
`@param validationGroup` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     *
     * @param event
     * @param validationGroup
     */
    public void addValidationGroup(Integer event, Class<?>... validationGroup) {
```

### JavadocDeclaration
`@param validationGroupName` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     * the validation group to null.
     *
     * @param validationGroupName
     * @param vgs
     */
```

### JavadocDeclaration
`@param vgs` tag description is missing
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
     *
     * @param validationGroupName
     * @param vgs
     */
    public void addValidationGroup(String validationGroupName, Class<?>...vgs) {
```

### JavadocDeclaration
Wrong tag value
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Value.java`
#### Snippet
```java
    /**
     * Sets key under which this value was loaded.
     * @exception if called with a non-null key which is different from an
     * already loaded key.
     */
```

### JavadocDeclaration
`@param clsName` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
     * Loads the given class name by the given loader.
     * For efficiency, a cache per class loader is maintained of classes already loader.
     * @param clsName
     * @param loader
     */
```

### JavadocDeclaration
`@param loader` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
     * For efficiency, a cache per class loader is maintained of classes already loader.
     * @param clsName
     * @param loader
     */
    static Class<?> loadClass(String clsName, ClassLoader loader) {
```

### JavadocDeclaration
`@param gitinfo` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/git/GitUtils.java`
#### Snippet
```java
     *  For example: fef543bM to fef543b (267342907)
     *  
     *  @param gitinfo
     *  @return The formatted int version, or -1 if gitinfo is null or unparsable.
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return URL
     * @throws MalformedURLException
     */
    public static PrivilegedExceptionAction<URL> toURLAction(
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return InputStream
     * @throws IOException
     */
    public static PrivilegedExceptionAction<InputStream> openStreamAction(
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return ServerSocket
     * @throws IOException
     */
    public static PrivilegedExceptionAction<ServerSocket>
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return ZipFile
     * @throws IOException
     */
    public static PrivilegedExceptionAction<ZipFile> newZipFileAction(
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return JarFile
     * @throws IOException
     */
    public static PrivilegedExceptionAction<JarFile> getJarFileAction(final JarURLConnection con)
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return FileOutputStream
     * @throws FileNotFoundException
     */
    public static PrivilegedExceptionAction<FileOutputStream>
```

### JavadocDeclaration
`@exception` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return Method
     * @exception NoSuchMethodException
     */
    public static PrivilegedExceptionAction<Method>
```

### JavadocDeclaration
NoSuchMethodException is not declared to be thrown by method getDeclaredMethodAction
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return Method
     * @exception NoSuchMethodException
     */
    public static PrivilegedExceptionAction<Method>
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return Object
     * @throws IOException
     */
    public static PrivilegedExceptionAction<Object> getContentAction(
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return Socket
     * @throws IOException
     */
    public static PrivilegedExceptionAction<Socket> newSocketAction(
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return FileInputStream
     * @throws FileNotFoundException
     */
    public static PrivilegedExceptionAction<FileInputStream>
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return JarEntry
     * @throws IOException
     */
    public static PrivilegedExceptionAction<JarEntry> getJarEntryAction(final JarURLConnection con)
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return FileOutputStream
     * @throws FileNotFoundException
     */
    public static PrivilegedExceptionAction<FileOutputStream>
```

### JavadocDeclaration
`@exception` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return String
     * @exception IOException
     */
    public static PrivilegedExceptionAction<String>
```

### JavadocDeclaration
`@exception` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return A new instance of the provided class.
     * @exception IllegalAccessException
     * @exception InstantiationException
     */
```

### JavadocDeclaration
`@exception` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     * @return A new instance of the provided class.
     * @exception IllegalAccessException
     * @exception InstantiationException
     */
    public static <T> PrivilegedExceptionAction<T> newInstanceAction(
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return InetAddress
     * @throws UnknownHostException
     */
    public static PrivilegedExceptionAction<InetAddress> getByNameAction(
```

### JavadocDeclaration
`@exception` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return Field
     * @exception NoSuchFieldException
     */
    public static PrivilegedExceptionAction<Field> getDeclaredFieldAction(
```

### JavadocDeclaration
NoSuchFieldException is not declared to be thrown by method getDeclaredFieldAction
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return Field
     * @exception NoSuchFieldException
     */
    public static PrivilegedExceptionAction<Field> getDeclaredFieldAction(
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return Socket
     * @throws IOException
     */
    public static PrivilegedExceptionAction<Socket> acceptAction(
```

### JavadocDeclaration
`@exception` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     *
     * @return Enumeration
     * @exception IOException
     */
    public static PrivilegedExceptionAction<Enumeration<URL>>
```

### JavadocDeclaration
`@param instrument` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/instrumentation/InstrumentationProvider.java`
#### Snippet
```java
     * instrument will participate in context-based lifecycle routines,
     * depending on the instrumentation level.
     * @param instrument
     */
    void addInstrument(Instrument instrument);
```

### JavadocDeclaration
`@param options` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/instrumentation/InstrumentationProvider.java`
#### Snippet
```java
    /**
     * Gets configuration options for this provider
     * @param options
     */
    String getOptions();
```

### JavadocDeclaration
`@param options` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/instrumentation/InstrumentationProvider.java`
#### Snippet
```java
    /**
     * Sets configuration options for this provider
     * @param options
     */
    void setOptions(String options);
```

### JavadocDeclaration
`@param started` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/instrumentation/Instrument.java`
#### Snippet
```java
    /**
     * Sets whether the instrument is an available state.
     * @param started
     */
    void setStarted(boolean started);
```

### JavadocDeclaration
Tag `return` is not allowed here
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/instrumentation/Instrument.java`
#### Snippet
```java
     * Sets the context of the instrument.  Typically, a reference to a broker
     * or broker factory.
     * @return the context associated with the instrument.
     */
    void setContext(Object context);
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XSecondaryTables.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ForeignKeyAction.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
public enum ForeignKeyAction {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementJoinColumn.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XJoinColumn.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/EagerFetchMode.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/VersionStrategy.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/JoinSyntax.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum JoinSyntax {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/SubclassFetchMode.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/Columns.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementIndex.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementForeignKey.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementNonpolymorphic.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementClassCriteria.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ContainerTable.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XTable.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/OrderColumn.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/MappingOverride.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ResultSetType.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum ResultSetType implements OpenJPAEnum<ResultSetType>{
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/Unique.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/FetchDirection.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum FetchDirection {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/FetchMode.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
public enum FetchMode implements OpenJPAEnum<FetchMode>{
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/Strategy.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE, METHOD, FIELD })
```

### JavadocDeclaration
`@param rule` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
    /**
     * Join names using a single naming rule and specified delimiter
     * @param rule
     * @param names
     */
```

### JavadocDeclaration
`@param names` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
     * Join names using a single naming rule and specified delimiter
     * @param rule
     * @param names
     */
    public String joinNames(IdentifierConfiguration config, IdentifierRule rule, String[] names, String delimiter) {
```

### JavadocDeclaration
`@param pname` tag description is missing
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
     * Gets the first part of a name when single character delimiters are
     * in use.
     * @param pname
     */
    private String splitNameCharDelimiters(String name, String nameDelim) {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/MappingOverrides.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XSecondaryTable.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/EmbeddedMapping.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ClassCriteria.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/DataStoreIdColumn.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/LRSSizeAlgorithm.java`
#### Snippet
```java
 *
 * @since 1.0.0
 * @published
 */
public enum LRSSizeAlgorithm implements OpenJPAEnum<Enum<?>> {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XJoinColumns.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/VersionColumns.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/NonpolymorphicType.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
public enum NonpolymorphicType {
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ElementJoinColumns.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/Nonpolymorphic.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/VersionColumn.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/ForeignKey.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE, METHOD, FIELD })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/IsolationLevel.java`
#### Snippet
```java
 *
 * @since 0.9.7
 * @published
 */
public enum IsolationLevel implements OpenJPAEnum<IsolationLevel>{
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/DiscriminatorStrategy.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ TYPE })
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/Index.java`
#### Snippet
```java
 * @author Abe White
 * @since 0.4.0
 * @published
 */
@Target({ METHOD, FIELD })
```

### JavadocDeclaration
Since is missing after @since tag
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Compatibility.java`
#### Snippet
```java
     *
     * @return true if the result of an aggregate with an empty query result returns null.
     * @since
     *
     */
```

### JavadocDeclaration
Since is missing after @since tag
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Compatibility.java`
#### Snippet
```java
     * In compliance with the JPA specification, the default value is true.
     *
     * @since
     * @param returnNullOnAggregate whether OpenJPA will return null for aggregate
     * expressions when the query result is empty.
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfiguration.java`
#### Snippet
```java
     * Specifies how OpenJPA handles unenhanced types.
     *
     * @see {@link #getRuntimeUnenhancedClasses()}
     * @since 1.0.0
     */
```

### JavadocDeclaration
Tag `param` is not allowed here
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
     * that the given reference name that begins with a single quote represents
     * special meaning of a constant join column and hence not to be delimited.
     * @param col
     * @param refColumnName
     * @see <a href="http://issues.apache.org/jira/browse/OPENJPA-1979">OPENJPA-1979</a>
```

### JavadocDeclaration
Tag `param` is not allowed here
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
     * special meaning of a constant join column and hence not to be delimited.
     * @param col
     * @param refColumnName
     * @see <a href="http://issues.apache.org/jira/browse/OPENJPA-1979">OPENJPA-1979</a>
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
     * @param embedType  embeddable class
     * @param access class level access for embeddable
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
Wrong tag `published`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/JDBCFetchPlan.java`
#### Snippet
```java
 * @author Abe White
 * @author Pinaki Poddar
 * @published
 */
public interface JDBCFetchPlan
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MultiQueryMetaData.java`
#### Snippet
```java
     * Affirms if this metadata is ephimeral.
     * Ephimenral metadata is removed from the repository after usage.
     * @return
     */
    public boolean isEphimeral() {
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MultiQueryMetaData.java`
#### Snippet
```java
     * Gets the component metadata at the given part index.
     * @param i a valid integer index
     * @return
     */
    public QueryMetaData getComponent(int i) {
```

### JavadocDeclaration
`@param p` tag description is missing
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MultiQueryMetaData.java`
#### Snippet
```java
    /**
     * Registers the given parameter.
     * @param p
     */
    public void registerParameter(Parameter p) {
```

### JavadocDeclaration
Tag `param` is not allowed here
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
     * that the given reference name that begins with a single quote represents
     * special meaning of a constant join column and hence not to be delimited.
     * @param col
     * @param refColumnName
     * @see <a href="http://issues.apache.org/jira/browse/OPENJPA-1979">OPENJPA-1979</a>
```

### JavadocDeclaration
Tag `param` is not allowed here
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
     * special meaning of a constant join column and hence not to be delimited.
     * @param col
     * @param refColumnName
     * @see <a href="http://issues.apache.org/jira/browse/OPENJPA-1979">OPENJPA-1979</a>
     */
```

### JavadocDeclaration
`@param proxyClassDef` tag description is missing
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
     * @param defaultChangeTracker whether to implement a null change tracker; if false
     * the change tracker method is left unimplemented
     * @param proxyClassDef
     */
    private void addProxyMethods(ClassWriterTracker ct, boolean defaultChangeTracker, String proxyClassDef, Class<?> parentClass) {
```

### JavadocDeclaration
Javadoc pointing to itself
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/InverseManager.java`
#### Snippet
```java

    /**
     * Constant representing the {@link #ACTION_EXCEPTION} action
     */
    public static final int ACTION_EXCEPTION = 2;
```

### JavadocDeclaration
Javadoc pointing to itself
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/InverseManager.java`
#### Snippet
```java

    /**
     * Constant representing the {@link #ACTION_MANAGE} action
     */
    public static final int ACTION_MANAGE = 0;
```

### JavadocDeclaration
Javadoc pointing to itself
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/InverseManager.java`
#### Snippet
```java

    /**
     * Constant representing the {@link #ACTION_WARN} action
     */
    public static final int ACTION_WARN = 1;
```

### JavadocDeclaration
Javadoc pointing to itself
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
     * Delete the query using the given executor, and parameter
     * values. All other execute methods delegate to this one or to
     * {@link #delete(StoreQuery, StoreQuery.Executor, Object[])} after validation and locking.
     * The return value will be a Number indicating the number of
     * instances deleted.
```

### JavadocDeclaration
Javadoc pointing to itself
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
     * Update the query using the given compilation, executor, and parameter
     * values. All other execute methods delegate to this one or to
     * {@link #update(StoreQuery, StoreQuery.Executor, Object[])} after validation and locking.
     * The return value will be a Number indicating the number of
     * instances updated.
```

### JavadocDeclaration
`@param ` tag description is missing
in `openjpa-kernel/src/main/java/org/apache/openjpa/validation/Validator.java`
#### Snippet
```java
     * type and event.
     *
     * @param <T>
     * @param arg0 Type being validated
     * @param event event type
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTCommand.java`
#### Snippet
```java
    /**
     * Process the given request and write the output on to the given response in the given context.
     * @throws ProcessingException
     *
     */
```

### JavadocDeclaration
`@param field` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
    /**
     * Encoding a JSON field is a quoted field name, followed by a :, followed by a value (which itself can be JSON)
     * @param field
     * @param value
     * @param indent
```

### JavadocDeclaration
`@param value` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
     * Encoding a JSON field is a quoted field name, followed by a :, followed by a value (which itself can be JSON)
     * @param field
     * @param value
     * @param indent
     */
```

### JavadocDeclaration
`@param indent` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
     * @param field
     * @param value
     * @param indent
     */
    private static StringBuilder encodeField(String field, Object value, int indent) {
```

### JavadocDeclaration
`@param o` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
     * If the value is JSON, outputs the string rendition of value
     * Otherwise quoted value
     * @param o
     */
    private static StringBuilder quoteFieldValue(Object o, int indent) {
```

### JavadocDeclaration
`@param response` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ProcessingException.java`
#### Snippet
```java
     * Prints the stack trace in a HTML format on the given response output stream.
     *
     * @param response
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ProcessingException.java`
#### Snippet
```java
     *
     * @param response
     * @throws IOException
     */
    @Override
```

### JavadocDeclaration
IOException is not declared to be thrown by method printStackTrace
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ProcessingException.java`
#### Snippet
```java
     *
     * @param response
     * @throws IOException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ObjectFormatter.java`
#### Snippet
```java
     * @param writer a text-oriented output stream
     *
     * @throws IOException
     */
    T writeOut(Metamodel model, String title, String desc, String uri, OutputStream out) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ObjectFormatter.java`
#### Snippet
```java
     * @param uri TODO
     * @param writer a text-oriented output stream
     * @throws IOException
     */
    T writeOut(Collection<OpenJPAStateManager> objs, Metamodel model,
```

### JavadocDeclaration
`@param header` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ExceptionFormatter.java`
#### Snippet
```java
    /**
     * Creates a XML Document with given header and stack trace of the given error.
     * @param header
     * @param e
     */
```

### JavadocDeclaration
`@param e` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ExceptionFormatter.java`
#### Snippet
```java
     * Creates a XML Document with given header and stack trace of the given error.
     * @param header
     * @param e
     */
    public Document createXML(String header, Throwable e) {
```

### JavadocDeclaration
`@param rsrc` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
     * A resource is always looked up with respect to this class.
     *
     * @param rsrc
     * @throws ProcessingException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
     *
     * @param rsrc
     * @throws ProcessingException
     */
    void findResource(String rsrc) throws ProcessingException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
     *
     * @see TokenReplacedStream
     * @throws IOException
     */
    private void getRootResource() throws IOException {
```

### JavadocDeclaration
`@param path` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
    /**
     * Is this path a context root?
     * @param path
     */
    boolean isContextRoot(String path) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
     * <br>
     *
     * @throws Exception
     */
    public void execute() throws Exception {
```

### JavadocDeclaration
`@param attr` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/MetamodelHelper.java`
#### Snippet
```java
     * Gets name of the attribute type. For collection and map type attribute, the name is
     * appended with generic type argument names.
     * @param attr
     */
    public static String getAttributeTypeName(Attribute<?, ?> attr) {
```

### JavadocDeclaration
`@param attr` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/MetamodelHelper.java`
#### Snippet
```java
     * Gets a ordinal value of enumerated persistent attribute category.
     *
     * @param attr
     */
    public static AttributeCategory getAttributeCategory(Attribute<?,?> attr) {
```

### JavadocDeclaration
`@param type` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/MetamodelHelper.java`
#### Snippet
```java
    /**
     * Gets the attributes of the given type in defined order.
     * @param type
     */
    public static List<Attribute<?,?>> getAttributesInOrder(ManagedType<?> type) {
```

### JavadocDeclaration
`@param patterns` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/TokenReplacedStream.java`
#### Snippet
```java
    /**
     * Gets the pattern if any in matched state
     * @param patterns
     */
    Pattern matched(Pattern...patterns) {
```

### JavadocDeclaration
`@param c` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/TokenReplacedStream.java`
#### Snippet
```java
        /**
         * Match the given character with the current cursor and advance the matching length.
         * @param c
         * @return the matching length. -1 denotes the pattern did not match the character.
         */
```

### JavadocDeclaration
`@param patterns` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/TokenReplacedStream.java`
#### Snippet
```java
    /**
     * Resets all the patterns.
     * @param patterns
     */
    void reset(Pattern...patterns) {
```

### JavadocDeclaration
`@param field` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObject.java`
#### Snippet
```java
    /**
     * Encoding a JSON field is a quoted field name, followed by a :, followed by a value (which itself can be JSON)
     * @param field
     * @param value
     * @param indent
```

### JavadocDeclaration
`@param value` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObject.java`
#### Snippet
```java
     * Encoding a JSON field is a quoted field name, followed by a :, followed by a value (which itself can be JSON)
     * @param field
     * @param value
     * @param indent
     */
```

### JavadocDeclaration
`@param indent` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObject.java`
#### Snippet
```java
     * @param field
     * @param value
     * @param indent
     */
    private static StringBuilder encodeField(String field, Object value, int indent) {
```

### JavadocDeclaration
`@param o` tag description is missing
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObject.java`
#### Snippet
```java
     * If the value is JSON, outputs the string rendition of value
     * Otherwise quoted value
     * @param o
     */
    private static StringBuilder quoteFieldValue(Object o, int indent) {
```

### JavadocDeclaration
`@param newline` tag description is missing
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/ParseTokenListener.java`
#### Snippet
```java
     * renderer to end the current line and start a new line. Otherwise, does nothing.
     *
     * @param newline
     */
    void changeLine(int newline) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/SourceRenderer.java`
#### Snippet
```java
     * @param args command-line arguments.
     *
     * @throws Exception
     */

```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
     * -1 if the new value is unavailable and should therefore be looked
     * up.
     * @throws NoSuchMethodException
     */
    private void addNotifyMutation(Code code, FieldMetaData fmd, int val,
```

### JavadocDeclaration
Class reference expected
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
     *            - A non-null org.apache.openjpa.lib.log.Log.
     *
     * @throws - IllegalStateException if cls doesn't implement org.apache.openjpa.enhance.PersistenceCapable.
     *
     * @return true if the provided Class is down level from the current PCEnhancer.ENHANCER_VERSION. False
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-examples/openbooks/src/main/java/openbook/server/DataLoader.java`
#### Snippet
```java
     *             1-st argument is the path to the data file
     *
     * @throws Exception
     */
    public static void main(String[] args) throws Exception {
```

### JavadocDeclaration
`@param context` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/Joins.java`
#### Snippet
```java
     * Set subquery context when traversing into the next join is
     * in transition from parent context to subquery.
     * @param context
     */
    Joins setJoinContext(Context context);
```

### JavadocDeclaration
`@param flag` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java
    /**
     * Counts the number of columns with the given flag.
     * @param flag
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java
     * Counts the number of columns with the given flag.
     * @param flag
     * @return
     */
    int countColumn(short flag) {
```

### JavadocDeclaration
`@param lang` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java
    /**
     * Sets the language whose parameter passing convention will be used to pass paramater values.
     * @param lang
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java
     * Sets the language whose parameter passing convention will be used to pass paramater values.
     * @param lang
     * @return
     */
    public StoredProcedure setParameterStyle(String lang) {
```

### JavadocDeclaration
`@param oper` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
    /**
     * Replace SQL '?' with CAST string if required by DB platform
     * @param oper
     * @param val
     */
```

### JavadocDeclaration
`@param val` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
     * Replace SQL '?' with CAST string if required by DB platform
     * @param oper
     * @param val
     */
    public void addCastForParam(String oper, Val val) {
```

### JavadocDeclaration
`@param newString` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
     * @param start replace start position
     * @param end replace end position
     * @param newString
     */
    public void replaceSqlString(int start, int end, String newString) {
```

### JavadocDeclaration
`@param schemaAlias` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/Select.java`
#### Snippet
```java
    /**
     * Record the initial schemaAlias of a join path
     * @param schemaAlias
     */
    void setSchemaAlias(String schemaAlias);
```

### JavadocDeclaration
`@param context` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/Select.java`
#### Snippet
```java
    /**
     * Set JPQL query context for this select
     * @param context
     */
    void setContext(Context context);
```

### JavadocDeclaration
`@param strName` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
     * names can contain underscores so permutations of these names must be
     * produced for ownership verification.
     * @param strName
     * @return If strName cannot be split into three or more parts null will be returned.
     *  Otherwise a String[][] of the potential sequence names will be returned.
```

### JavadocDeclaration
`@param rs` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
     * java.sql.Timestamp
     *
     * @param rs
     * @param column
     * @param map
```

### JavadocDeclaration
`@param column` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
     *
     * @param rs
     * @param column
     * @param map
     *
```

### JavadocDeclaration
`@param map` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
     * @param rs
     * @param column
     * @param map
     *
     * @exception SQLException
```

### JavadocDeclaration
`@exception` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
     * @param map
     *
     * @exception SQLException
     */
    @Override
```

### JavadocDeclaration
`@param strName` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
     * db.  This logic assumes that any sequence suffixed with _SEQ is an
     * owned sequence (identical to the behavior of prior versions of OpenJPA).
     * @param strName
     */
    private boolean isOwnedSequence(String strName) {
```

### JavadocDeclaration
`@param select` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DerbyDictionary.java`
#### Snippet
```java
     * accordingly the number of rows in the result set needs to be modified.
     *
     * @param select
     * @param count
     * @return
```

### JavadocDeclaration
`@param count` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DerbyDictionary.java`
#### Snippet
```java
     *
     * @param select
     * @param count
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DerbyDictionary.java`
#### Snippet
```java
     * @param select
     * @param count
     * @return
     */

```

### JavadocDeclaration
`@param asString` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
     * @param func original string
     * @param target substring to look for
     * @param asString
     * @return updated string (func)
     */
```

### JavadocDeclaration
`@param adapt` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingDefaultsImpl.java`
#### Snippet
```java
     *
     * @param disc The discriminator that needs a default value
     * @param adapt
     *
     * @return A new object containing the generated Discriminator value.
```

### JavadocDeclaration
Class reference expected
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * @param qualified If true the qualified name of the DBIdentifier will be used.
     *
     * @throws {@link UserException} with the given message key if the given name is indeed longer.
     * @return the same name.
     */
```

### JavadocDeclaration
Wrong tag `parma`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     *
     * @param val operand value
     * @parma func the sql function statement
     * @return a String with the correct CAST function syntax
     */
```

### JavadocDeclaration
`@param stmnt` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
    /**
     * Provides the default validation handling of setting a query timeout.
     * @param stmnt
     * @param timeout in milliseconds
     * @throws SQLException
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * @param stmnt
     * @param timeout in milliseconds
     * @throws SQLException
     */
    public void setQueryTimeout(PreparedStatement stmnt, int timeout)
```

### JavadocDeclaration
`@param stmnt` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * setting query timeouts, while preserving the default timeout logic
     * in the public setQueryTimeout method.
     * @param stmnt
     * @param timeout in milliseconds
     * @throws SQLException
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * @param stmnt
     * @param timeout in milliseconds
     * @throws SQLException
     */
    protected void setStatementQueryTimeout(PreparedStatement stmnt,
```

### JavadocDeclaration
Wrong tag `{code`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
    /**
     * set of types that might have an optional fraction (sub-second) part.
     * @{code @Column(scale=-1)} sets the fraction to zero.
     * @{code @Column(scale=0)} will use the default behaviour (no fraction information generated)
     */
```

### JavadocDeclaration
Wrong tag `{code`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * set of types that might have an optional fraction (sub-second) part.
     * @{code @Column(scale=-1)} sets the fraction to zero.
     * @{code @Column(scale=0)} will use the default behaviour (no fraction information generated)
     */
    public final Set<String> fractionalTypeNameSet = new HashSet<>();
```

### JavadocDeclaration
Class reference expected
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * @param msgKey    message identifier for the exception.
     *
     * @throws {@link UserException} with the given message key if the given name is indeed longer.
     * @return the same name.
     */
```

### JavadocDeclaration
`@param column` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
    /**
     * Return version column name
     * @param column
     * @param tableAlias : this is needed for platform specific version column
     */
```

### JavadocDeclaration
`@param stmnt` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * implementation of setting query and lock timeouts.
     *
     * @param stmnt
     * @param fetch - optional lock and query timeouts in milliseconds
     * @param forUpdate - true if we should also try setting a lock timeout
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * @param fetch - optional lock and query timeouts in milliseconds
     * @param forUpdate - true if we should also try setting a lock timeout
     * @throws SQLException
     */
    public void setTimeouts(PreparedStatement stmnt,
```

### JavadocDeclaration
`@param stmnt` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * implementation of setting query and lock timeouts.
     *
     * @param stmnt
     * @param conf - optional lock and query timeouts in milliseconds
     * @param forUpdate - true if we should also try setting a lock timeout
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * @param conf - optional lock and query timeouts in milliseconds
     * @param forUpdate - true if we should also try setting a lock timeout
     * @throws SQLException
     */
    public void setTimeouts(PreparedStatement stmnt, JDBCConfiguration conf,
```

### JavadocDeclaration
`@param val` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     * add CAST for a function operator where operand is a param
     * @param func  function name
     * @param val
     * @return updated func
     */
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/SequenceMapping.java`
#### Snippet
```java
    /**
     * @deprecated
     * @param name
     */
    @Deprecated
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/SequenceMapping.java`
#### Snippet
```java
    /**
     * @deprecated
     * @param name
     * @param repos
     */
```

### JavadocDeclaration
`@param repos` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/SequenceMapping.java`
#### Snippet
```java
     * @deprecated
     * @param name
     * @param repos
     */
    @Deprecated
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/SequenceMapping.java`
#### Snippet
```java
     * Sequence names are a kernel object so DBIdentifiers must be converted to
     * strings
     * @param name
     * @param repos
     */
```

### JavadocDeclaration
`@param repos` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/SequenceMapping.java`
#### Snippet
```java
     * strings
     * @param name
     * @param repos
     */
    public SequenceMapping(DBIdentifier name, MappingRepository repos) {
```

### JavadocDeclaration
`@param cols` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/SequenceMapping.java`
#### Snippet
```java
    /**
     * @deprecated
     * @param cols
     */
    @Deprecated
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGroup.java`
#### Snippet
```java
    /**
     * @deprecated
     * @param name
     */
    @Deprecated
```

### JavadocDeclaration
`@param props` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
     * Normalize properties for Commons DBCP.  This should be done for every call from DataSourceFactory,
     * as we do not have a pre-configured Driver to reuse.
     * @param props
     * @return updated properties
     */
```

### JavadocDeclaration
`@param props` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
    /**
     * Merge the passed in properties with a copy of the existing _connectionProperties
     * @param props
     * @return Merged properties
     */
```

### JavadocDeclaration
`@param props` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
    /**
     * Case-insensitive search of the given properties for the given key.
     * @param props
     * @param key
     * @return Key name as found in properties or null if it was not found.
```

### JavadocDeclaration
`@param key` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
     * Case-insensitive search of the given properties for the given key.
     * @param props
     * @param key
     * @return Key name as found in properties or null if it was not found.
     */
```

### JavadocDeclaration
`@param props` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
    /**
     * Case-insensitive search of the given properties for the given key.
     * @param props
     * @param key
     * @param defaultKey
```

### JavadocDeclaration
`@param key` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
     * Case-insensitive search of the given properties for the given key.
     * @param props
     * @param key
     * @param defaultKey
     * @return Key name as found in properties or the given defaultKey if it was not found.
```

### JavadocDeclaration
`@param defaultKey` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
     * @param props
     * @param key
     * @param defaultKey
     * @return Key name as found in properties or the given defaultKey if it was not found.
     */
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Returns a valid foreign key identifier, based upon the configuration and
     * provided parameters.
     * @param name
     * @param table
     * @param toTable
```

### JavadocDeclaration
`@param table` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * provided parameters.
     * @param name
     * @param table
     * @param toTable
     * @param maxLen
```

### JavadocDeclaration
`@param toTable` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param name
     * @param table
     * @param toTable
     * @param maxLen
     */
```

### JavadocDeclaration
`@param maxLen` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param table
     * @param toTable
     * @param maxLen
     */
    DBIdentifier getValidForeignKeyIdentifier(DBIdentifier name, Table table, Table toTable, int maxLen);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Returns a valid index identifier, based upon the configuration and
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
```

### JavadocDeclaration
`@param table` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
     * @param checkForUniqueness
```

### JavadocDeclaration
`@param maxLen` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param name
     * @param table
     * @param maxLen
     * @param checkForUniqueness
     */
```

### JavadocDeclaration
`@param checkForUniqueness` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param table
     * @param maxLen
     * @param checkForUniqueness
     */
    DBIdentifier getValidSequenceIdentifier(DBIdentifier name, Schema schema, int maxLen);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Returns a valid table identifier, based upon the configuration and provided
     * parameters.
     * @param name
     * @param schema
     * @param maxLen
```

### JavadocDeclaration
`@param schema` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * parameters.
     * @param name
     * @param schema
     * @param maxLen
     */
```

### JavadocDeclaration
`@param maxLen` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param name
     * @param schema
     * @param maxLen
     */
    DBIdentifier getValidTableIdentifier(DBIdentifier name, Schema schema, int maxLen);
```

### JavadocDeclaration
`@param columns` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
    /**
     * Appends multiple columns names together into comma delimited string.
     * @param columns
     */
    String appendColumns(Column[] columns);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
    /**
     * Converts the specified string to a format required by the database.
     * @param name
     */
    String toDBName(String name);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Converts the specified identifier to a format required by the database,
     * optionally delimiting the name.
     * @param name
     * @param delimit
     */
```

### JavadocDeclaration
`@param delimit` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * optionally delimiting the name.
     * @param name
     * @param delimit
     */
    String toDBName(DBIdentifier name, boolean delimit);
```

### JavadocDeclaration
`@param columns` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Converts the name of the specified delimiter to the appropriate
     * case as defined by the configuration.
     * @param columns
     */
    DBIdentifier convertSchemaCase(DBIdentifier schema);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Converts the specified string to a format required by the database,
     * optionally delimiting the name.
     * @param name
     */
    String toDBName(String name, boolean delimit);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Converts the name returned by the database to an identifier of the
     * specified type.
     * @param name
     */
    DBIdentifier fromDBName(String name, DBIdentifierType id);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
    /**
     * Converts the specified identifier to a format required by the database.
     * @param name
     */
    String toDBName(DBIdentifier name);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Returns a valid column name/identifier, based upon the configuration and
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
```

### JavadocDeclaration
`@param table` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
     * @param checkForUniqueness
```

### JavadocDeclaration
`@param maxLen` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param name
     * @param table
     * @param maxLen
     * @param checkForUniqueness
     */
```

### JavadocDeclaration
`@param checkForUniqueness` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param table
     * @param maxLen
     * @param checkForUniqueness
     */
    DBIdentifier getValidColumnIdentifier(DBIdentifier name, Table table, int maxLen,
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Returns a valid unique constraint identifier, based upon the configuration and
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
```

### JavadocDeclaration
`@param table` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
     */
```

### JavadocDeclaration
`@param maxLen` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param name
     * @param table
     * @param maxLen
     */
    DBIdentifier getValidUniqueIdentifier(DBIdentifier name, Table table, int maxLen);
```

### JavadocDeclaration
`@param resultId` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
    /**
     * Appends multiple names together using the appropriate name delimiter.
     * @param resultId
     * @param names
     */
```

### JavadocDeclaration
`@param names` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Appends multiple names together using the appropriate name delimiter.
     * @param resultId
     * @param names
     */
    DBIdentifier append(DBIdentifierType resultId, DBIdentifier...names);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Returns a valid index identifier, based upon the configuration and
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
```

### JavadocDeclaration
`@param table` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * provided parameters.
     * @param name
     * @param table
     * @param maxLen
     * @param checkForUniqueness
```

### JavadocDeclaration
`@param maxLen` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param name
     * @param table
     * @param maxLen
     * @param checkForUniqueness
     */
```

### JavadocDeclaration
`@param checkForUniqueness` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * @param table
     * @param maxLen
     * @param checkForUniqueness
     */
    DBIdentifier getValidIndexIdentifier(DBIdentifier name, Table table, int maxLen);
```

### JavadocDeclaration
`@param alias` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
    /**
     * Converts a provided alias to a format specified in the configuration.
     * @param alias
     */
    String convertAlias(String alias);
```

### JavadocDeclaration
`@param col` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
    /**
     * Returns a generated key sequence identifier for the column.
     * @param col
     * @param maxLen
     */
```

### JavadocDeclaration
`@param maxLen` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtil.java`
#### Snippet
```java
     * Returns a generated key sequence identifier for the column.
     * @param col
     * @param maxLen
     */
    DBIdentifier getGeneratedKeySequenceName(Column col, int maxLen);
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java

    /**
     * @param name
     * @param tableNames
     * @deprecated
```

### JavadocDeclaration
`@param tableNames` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
    /**
     * @param name
     * @param tableNames
     * @deprecated
     */
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Splits a name into normalized components using the specified
     * name delimiter (ex. schema:table, delim = : --> { schema, table }
     * @param name
     */
    public static String[] splitName(String name, String delim) {
```

### JavadocDeclaration
`@param names` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Joins two names using the default identifier rules.
     * @param names
     */
    public static String joinNames(String name1, String name2) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Splits a name into normalized components
     * @param name
     */
    public static String[] splitName(String name) {
```

### JavadocDeclaration
`@param name1` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * case comparison.  If not delimited, does a case insensitive
     * comparison.
     * @param name1
     * @param name2
     */
```

### JavadocDeclaration
`@param name2` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * comparison.
     * @param name1
     * @param name2
     */
    public static boolean namesEqual(String name1, String name2) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Returns true if the name is delimited with default delimiters
     * @param name
     */
    public static boolean isDelimited(String name) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Normalizes a multi-part name
     * @param name
     */
    public static String normalizeMulti(String name) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Normalizes a name, if not delimited, converts to uppercase.
     * @param name
     */
    public static String normalizeUpper(String name) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Normalizes a single part name
     * @param name
     */
    public static String normalizeString(String name) {
```

### JavadocDeclaration
`@param name1` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Combines multiple names using default identifier rules.
     * @param name1
     * @param name2
     */
```

### JavadocDeclaration
`@param name2` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Combines multiple names using default identifier rules.
     * @param name1
     * @param name2
     */
    public static String combine(String...names) {
```

### JavadocDeclaration
`@param name1` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Splits names into individual components and compares individually
     * for equality
     * @param name1
     * @param name2
     */
```

### JavadocDeclaration
`@param name2` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * for equality
     * @param name1
     * @param name2
     */
    public static boolean fullNamesEqual(String name1, String name2) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Truncates a name to the specified length while maintaining
     * delimiters.
     * @param name
     * @param length
     */
```

### JavadocDeclaration
`@param length` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * delimiters.
     * @param name
     * @param length
     */
    public static String truncate(String name, int length) {
```

### JavadocDeclaration
`@param names` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Joins multiple names using default identifier rules.
     * @param names
     */
    public static String joinNames(String[] names) {
```

### JavadocDeclaration
`@param name1` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Appends one string to another using default identifier rules.
     * @param name1
     * @param name2
     */
```

### JavadocDeclaration
`@param name2` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Appends one string to another using default identifier rules.
     * @param name1
     * @param name2
     */
    public static String append(String name1, String name2) {
```

### JavadocDeclaration
`@param name1` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Combines two names using default identifier rules.
     * @param name1
     * @param name2
     */
```

### JavadocDeclaration
`@param name2` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Combines two names using default identifier rules.
     * @param name1
     * @param name2
     */
    public static String combine(String name1, String name2) {
```

### JavadocDeclaration
`@param config` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * naming rule.
     * Note: Currently only delimiters are converted.
     * @param config
     * @param rule
     * @param name
```

### JavadocDeclaration
`@param rule` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Note: Currently only delimiters are converted.
     * @param config
     * @param rule
     * @param name
     */
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * @param config
     * @param rule
     * @param name
     */
    public static String convert(IdentifierConfiguration config, String rule, String name) {
```

### JavadocDeclaration
`@param name1` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Removes Hungarian notation from a string.
     * @param name1
     * @param name2
     */
```

### JavadocDeclaration
`@param name2` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
     * Removes Hungarian notation from a string.
     * @param name1
     * @param name2
     */
    public static String removeHungarianNotation(String name) {
```

### JavadocDeclaration
`@param names` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
    /**
     * Joins multiple names using the specified delimiter.
     * @param names
     */
    public static String joinNames(String[] names, String delimiter) {
```

### JavadocDeclaration
`@param sNames` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
     * Set the identifiers that make up the path.  Identifiers must be specified
     * in path order.  (ex. [ table, column ] )
     * @param sNames
     */
    public void setPath(DBIdentifier...sNames) {
```

### JavadocDeclaration
`@param sName` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
    /**
     * Creates a qualified path from an identifier.
     * @param sName
     */
    public static QualifiedDBIdentifier getPath(DBIdentifier sName) {
```

### JavadocDeclaration
`@param names` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
     * Creates a qualified identifier based upon an array of DBIdentifiers.  Identifiers
     * must be specified in order.
     * @param names
     */
    public static QualifiedDBIdentifier newPath(DBIdentifier...names) {
```

### JavadocDeclaration
`@param path1` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
    /**
     * Compares two qualified identifiers for equality.
     * @param path1
     * @param path2
     */
```

### JavadocDeclaration
`@param path2` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
     * Compares two qualified identifiers for equality.
     * @param path1
     * @param path2
     */
    public static boolean pathEqual(QualifiedDBIdentifier path1, QualifiedDBIdentifier path2) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
    /**
     * Set the base name component of this compound identifier
     * @param name
     */
    protected void setBaseName(String name) {
```

### JavadocDeclaration
`@param sName` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
    /**
     * Splits a qualified path into separate identifiers.
     * @param sName
     */
    public static DBIdentifier[] splitPath(DBIdentifier sName) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns a new DBIdentifier with the given string appended.
     * @param name
     * @param length
     */
```

### JavadocDeclaration
`@param length` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * Returns a new DBIdentifier with the given string appended.
     * @param name
     * @param length
     */
    public static DBIdentifier append(DBIdentifier name, String str) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns true if the identifier is null.
     * @param name
     */
    public static boolean isNull(DBIdentifier name) {
```

### JavadocDeclaration
`@param id` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * provided type, optionally delimiting the names.
     * @param names columnNames or other DB identifier names
     * @param id
     */
    public static DBIdentifier[] toArray(String[] names, DBIdentifierType id, boolean delimit) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns a new DBIdentifier with Hungarian notation removed.
     * @param name
     */
    public static DBIdentifier removeHungarianNotation(DBIdentifier name) {
```

### JavadocDeclaration
`@param name1` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Static equality method for comparing two identifiers.
     * @param name1
     * @param name2
     */
```

### JavadocDeclaration
`@param name2` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * Static equality method for comparing two identifiers.
     * @param name1
     * @param name2
     */
    public static boolean equal(DBIdentifier name1, DBIdentifier name2) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns a new DBIdentifier truncated to length
     * @param name
     * @param length
     */
```

### JavadocDeclaration
`@param length` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * Returns a new DBIdentifier truncated to length
     * @param name
     * @param length
     */
    public static DBIdentifier truncate(DBIdentifier name, int length) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns a new DBIdentifier converted to upper case - if not delimited.
     * @param name
     */
    public static DBIdentifier toUpper(DBIdentifier name) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns true if the identifier is null or the name is null or empty.
     * @param name
     */
    public static boolean isEmpty(DBIdentifier name) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * Returns a new DBIdentifier converted to upper case.  If delimited,
     * force to upper case using force option.
     * @param name
     */
    public static DBIdentifier toUpper(DBIdentifier name, boolean force) {
```

### JavadocDeclaration
`@param columnNames` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * Converts the provided set of names to an array of identifiers of the
     * provided type
     * @param columnNames
     * @param id
     */
```

### JavadocDeclaration
`@param id` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * provided type
     * @param columnNames
     * @param id
     */
    public static DBIdentifier[] toArray(String[] columnNames, DBIdentifierType id) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * Returns a new DBIdentifier converted to lower case.  If delimited,
     * force to lower case using force option.
     * @param name
     */
    public static DBIdentifier toLower(DBIdentifier name, boolean force) {
```

### JavadocDeclaration
`@param sNames` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns a string array of names based upon the provided set of identifiers.
     * @param sNames
     */
    public static String[] toStringArray(DBIdentifier[] sNames) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns a new DBIdentifier converted to lower case - if not delimited.
     * @param name
     */
    public static DBIdentifier toLower(DBIdentifier name) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * type and heuristics to determine the types and placement of the resulting
     * components.
     * @param name
     */
    public static DBIdentifier[] split(DBIdentifierType resultType, String name) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns a copy of an identifier with name trimmed to null.
     * @param name
     */
    public static DBIdentifier trimToNull(DBIdentifier name) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    /**
     * Returns a new DBIdentifier with the specified leading string removed.
     * @param name
     */
    public static DBIdentifier removeLeading(DBIdentifier name, String leadingStr) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * Returns a new DBIdentifier with the given string combined using
     * delimiting rules and appropriate separators.
     * @param name
     * @param length
     */
```

### JavadocDeclaration
`@param length` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     * delimiting rules and appropriate separators.
     * @param name
     * @param length
     */
    public static DBIdentifier combine(DBIdentifier name, String str) {
```

### JavadocDeclaration
`@param fk` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
    /**
     * Joins the column of a single column FK to this FK.
     * @param fk
     */
    public void addColumn(ForeignKey fk) {
```

### JavadocDeclaration
Wrong tag `parm`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtilImpl.java`
#### Snippet
```java
     *  Converts the name to a name which can be used within a SQL statement.  Uses
     *  the appropriate delimiters and separators.
     *  @parm name a DBIdentifier
     *  @param delimit If true, allows the name to be delimited, if necessary.
     *  Otherwise, the identifier is not delimited.
```

### JavadocDeclaration
Wrong tag `parm`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtilImpl.java`
#### Snippet
```java
     *  Converts the name to a name which can be used within a SQL statement.  Uses
     *  the appropriate delimiters and separators.
     *  @parm name a DBIdentifier
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributedConfiguration.java`
#### Snippet
```java
	/**
	 * Gets the available slice identifiers irrespective of their status.
	 * @return
	 */
    List<String> getAvailableSliceNames();
```

### JavadocDeclaration
`@return` tag description is missing
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
    /**
     * Return the list of column names, used only for informative (error) messages.
     * @return
     */
    public String[] getColumnNames() {
```

### JavadocDeclaration
`@param loader` tag description is missing
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java
     *
     *
     * @param loader
     * @throws MojoExecutionException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `openjpa-tools/openjpa-maven-plugin/src/main/java/org/apache/openjpa/tools/maven/AbstractOpenJpaMojo.java`
#### Snippet
```java
     *
     * @param loader
     * @throws MojoExecutionException
     */
    private boolean fixPersistenceXmlIfNeeded(final ClassLoader loader) throws MojoExecutionException
```

## RuleId[id=FieldMayBeFinal]
### FieldMayBeFinal
Field `_type` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
     */
    static class EnumToInteger implements HintValueConverter {
        private Class<? extends Enum<?>> _type;
        private Integer[] map;

```

### FieldMayBeFinal
Field `_prototype` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
     */
    public static class OpenJPAEnumToInteger implements HintValueConverter {
        private OpenJPAEnum<?> _prototype;

        public OpenJPAEnumToInteger(OpenJPAEnum<?> prototype) {
```

### FieldMayBeFinal
Field `map` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
    static class EnumToInteger implements HintValueConverter {
        private Class<? extends Enum<?>> _type;
        private Integer[] map;

        public EnumToInteger(Class<? extends Enum<?>> enumType, int[] numbers) {
```

### FieldMayBeFinal
Field `_names` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
    public static final String VALIDATE_GROUP_DEFAULT = "jakarta.validation.groups.Default";

    private static Map<String,String> _names = new HashMap<>();

    /**
```

### FieldMayBeFinal
Field `components` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationBuilder.java`
#### Snippet
```java

    private Class<? extends Annotation> type;
    private List<AnnotationEntry> components =
        new ArrayList<>();

```

### FieldMayBeFinal
Field `type` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationBuilder.java`
#### Snippet
```java
public class AnnotationBuilder {

    private Class<? extends Annotation> type;
    private List<AnnotationEntry> components =
        new ArrayList<>();
```

### FieldMayBeFinal
Field `UNDERSCORE` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
    }

    private static String UNDERSCORE = "_";

    @Override
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/CompileTimeLogger.java`
#### Snippet
```java
    private static enum Level {TRACE, INFO, WARN, ERROR}

    private static Localizer _loc = Localizer.forPackage(CompileTimeLogger.class);
    private static Level DEFAULT_LEVEL = Level.WARN;
    private int logLevel;
```

### FieldMayBeFinal
Field `logLevel` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/CompileTimeLogger.java`
#### Snippet
```java
    private static Localizer _loc = Localizer.forPackage(CompileTimeLogger.class);
    private static Level DEFAULT_LEVEL = Level.WARN;
    private int logLevel;
    private Messager messager;

```

### FieldMayBeFinal
Field `DEFAULT_LEVEL` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/CompileTimeLogger.java`
#### Snippet
```java

    private static Localizer _loc = Localizer.forPackage(CompileTimeLogger.class);
    private static Level DEFAULT_LEVEL = Level.WARN;
    private int logLevel;
    private Messager messager;
```

### FieldMayBeFinal
Field `messager` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/CompileTimeLogger.java`
#### Snippet
```java
    private static Level DEFAULT_LEVEL = Level.WARN;
    private int logLevel;
    private Messager messager;

    public CompileTimeLogger(ProcessingEnvironment env, String level) {
```

### FieldMayBeFinal
Field `_embeddables` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    private Map<Class<?>, EntityType<?>> _entities = new HashMap<>();
    private Set<EntityType<?>> _entitiesOnlySet = null;
    private Map<Class<?>, EmbeddableType<?>> _embeddables = new HashMap<>();
    private Map<Class<?>, MappedSuperclassType<?>> _mappedsupers = new HashMap<>();
    private Map<Class<?>, Types.PseudoEntity<?>> _pseudos = new HashMap<>();
```

### FieldMayBeFinal
Field `_basics` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
public class MetamodelImpl implements Metamodel, Resolver {
    private final MetaDataRepository repos;
    private Map<Class<?>, Type<?>> _basics = new HashMap<>();
    private Map<Class<?>, EntityType<?>> _entities = new HashMap<>();
    private Set<EntityType<?>> _entitiesOnlySet = null;
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    private Map<Class<?>, Types.PseudoEntity<?>> _pseudos = new HashMap<>();

    private static Localizer _loc = Localizer.forPackage(MetamodelImpl.class);

    /**
```

### FieldMayBeFinal
Field `_entities` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    private final MetaDataRepository repos;
    private Map<Class<?>, Type<?>> _basics = new HashMap<>();
    private Map<Class<?>, EntityType<?>> _entities = new HashMap<>();
    private Set<EntityType<?>> _entitiesOnlySet = null;
    private Map<Class<?>, EmbeddableType<?>> _embeddables = new HashMap<>();
```

### FieldMayBeFinal
Field `_mappedsupers` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    private Set<EntityType<?>> _entitiesOnlySet = null;
    private Map<Class<?>, EmbeddableType<?>> _embeddables = new HashMap<>();
    private Map<Class<?>, MappedSuperclassType<?>> _mappedsupers = new HashMap<>();
    private Map<Class<?>, Types.PseudoEntity<?>> _pseudos = new HashMap<>();

```

### FieldMayBeFinal
Field `_pseudos` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    private Map<Class<?>, EmbeddableType<?>> _embeddables = new HashMap<>();
    private Map<Class<?>, MappedSuperclassType<?>> _mappedsupers = new HashMap<>();
    private Map<Class<?>, Types.PseudoEntity<?>> _pseudos = new HashMap<>();

    private static Localizer _loc = Localizer.forPackage(MetamodelImpl.class);
```

### FieldMayBeFinal
Field `_puNameCollisions` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        (PersistenceProductDerivation.class);

    private HashMap<String, PUNameCollision> _puNameCollisions
        = new HashMap<>();

```

### FieldMayBeFinal
Field `_resources` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
    private static class PUNameCollision{
        private String _puName;
        private Set<String> _resources;

        PUNameCollision(String puName, String file1, String file2) {
```

### FieldMayBeFinal
Field `_puName` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
     */
    private static class PUNameCollision{
        private String _puName;
        private Set<String> _resources;

```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        mappingAnnos.add(ManyToMany.class);
    }
    private static Localizer _loc = Localizer.forPackage(SourceAnnotationHandler.class);

	/**
```

### FieldMayBeFinal
Field `CLASSNAMES_COLLECTION` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        "java.util.Properties", "java.util.SortedMap",
        "java.util.TreeMap"});
    private static List<String> CLASSNAMES_COLLECTION = Arrays.asList(
        new String[]{
        "java.util.Collection", "java.util.AbstractCollection",
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
    private List<String> header = new ArrayList<>();
    private boolean active;
    private static Localizer _loc =  Localizer.forPackage(AnnotationProcessor6.class);
    private SourceVersion supportedSourceVersion;
    private String addGeneratedOption;
```

### FieldMayBeFinal
Field `CLASSNAMES_MAP` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        "java.util.HashSet", "java.util.LinkedList", "java.util.LinkedHashSet",
        "java.util.SortedSet", "java.util.TreeSet"});
    private static List<String> CLASSNAMES_MAP = Arrays.asList(
        new String[]{
        "java.util.Map", "java.util.AbstractMap", "java.util.EnumMap",
```

### FieldMayBeFinal
Field `type` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        MAP("jakarta.persistence.metamodel.MapAttribute");

        private String type;

        private TypeCategory(String type) {
```

### FieldMayBeFinal
Field `CLASSNAMES_LIST` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
     * into corresponding JPA meta-model member type.
     */
    private static List<String> CLASSNAMES_LIST = Arrays.asList(
        new String[]{
        "java.util.List", "java.util.AbstractList",
```

### FieldMayBeFinal
Field `header` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
    private int generatedSourceVersion = 6;
    private CompileTimeLogger logger;
    private List<String> header = new ArrayList<>();
    private boolean active;
    private static Localizer _loc =  Localizer.forPackage(AnnotationProcessor6.class);
```

### FieldMayBeFinal
Field `CLASSNAMES_SET` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        "java.util.AbstractSequentialList", "java.util.ArrayList",
        "java.util.Stack", "java.util.Vector"});
    private static List<String> CLASSNAMES_SET = Arrays.asList(
        new String[]{
        "java.util.Set", "java.util.AbstractSet", "java.util.EnumSet",
```

### FieldMayBeFinal
Field `_parseList` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
    protected Class<?> _cls = null;
    // List of classes currently being parsed
    private ArrayList<Class<?>> _parseList = new ArrayList<>();
    private int _fieldPos = 0;
    private int _clsPos = 0;
```

### FieldMayBeFinal
Field `_operating` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/AliasContext.java`
#### Snippet
```java

class AliasContext {
	private Stack<Object> _operating = new Stack<>();
	private Map<ExpressionImpl, String> _aliases =
		new HashMap<>();
```

### FieldMayBeFinal
Field `_aliases` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/AliasContext.java`
#### Snippet
```java
class AliasContext {
	private Stack<Object> _operating = new Stack<>();
	private Map<ExpressionImpl, String> _aliases =
		new HashMap<>();
	private final MetaDataRepository _repos;
```

### FieldMayBeFinal
Field `conf` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
    private static class BrokerBytesInputStream extends ObjectInputStream {

        private OpenJPAConfiguration conf;

        BrokerBytesInputStream(byte[] bytes, OpenJPAConfiguration conf)
```

### FieldMayBeFinal
Field `_plans` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
    private DelegatingBroker _broker;
    private EntityManagerFactoryImpl _emf;
    private Map<FetchConfiguration,FetchPlan> _plans = new IdentityHashMap<>(1);
    protected RuntimeExceptionTranslator _ret = PersistenceExceptions.getRollbackTranslator(this);
    private boolean _convertPositionalParams = false;
```

### FieldMayBeFinal
Field `attrs` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
    public final ClassMetaData meta;

    private java.util.Set<Attribute<? super X, ?>> attrs = new HashSet<>();

    private final DeclaredAttributeFilter<X> declaredAttributeFilter;
```

### FieldMayBeFinal
Field `_whens` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/CaseExpressionImpl.java`
#### Snippet
```java
	
    private static final long serialVersionUID = 1L;
    private LinkedList<WhenClause> _whens = new LinkedList<>();
	private final Object _caseOperand;

```

### FieldMayBeFinal
Field `codeLines` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        private boolean isAbstract;
        private List<Argument<ClassName,String>> args = new ArrayList<>();
        private List<String> codeLines = new ArrayList<>();
        int tabCount = 0;
        String tab = "";
```

### FieldMayBeFinal
Field `methods` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        private List<ClassName> interfaces = new ArrayList<>();
        private Set<Field> fields   = new TreeSet<>();
        private Set<Method> methods = new TreeSet<>();
        private Set<Constructor> constructors = new TreeSet<>();

```

### FieldMayBeFinal
Field `args` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
    public class Annotation {
        private String name;
        private List<Argument<?,?>> args = new ArrayList<>();

        Annotation(String n) {
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
 */
public class SourceCode {
    private static Localizer _loc = Localizer.forPackage(SourceCode.class);

    /**
```

### FieldMayBeFinal
Field `name` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
     */
    public class Annotation {
        private String name;
        private List<Argument<?,?>> args = new ArrayList<>();

```

### FieldMayBeFinal
Field `args` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
    public class Method extends Element<Method> {
        private boolean isAbstract;
        private List<Argument<ClassName,String>> args = new ArrayList<>();
        private List<String> codeLines = new ArrayList<>();
        int tabCount = 0;
```

### FieldMayBeFinal
Field `constructors` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        private Set<Field> fields   = new TreeSet<>();
        private Set<Method> methods = new TreeSet<>();
        private Set<Constructor> constructors = new TreeSet<>();

        public Class(String name) {
```

### FieldMayBeFinal
Field `interfaces` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        private boolean isFinal;
        private ClassName superCls;
        private List<ClassName> interfaces = new ArrayList<>();
        private Set<Field> fields   = new TreeSet<>();
        private Set<Method> methods = new TreeSet<>();
```

### FieldMayBeFinal
Field `fields` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        private ClassName superCls;
        private List<ClassName> interfaces = new ArrayList<>();
        private Set<Field> fields   = new TreeSet<>();
        private Set<Method> methods = new TreeSet<>();
        private Set<Constructor> constructors = new TreeSet<>();
```

### FieldMayBeFinal
Field `name` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java

    static class Package {
        private String name;

        Package(String p) {
```

### FieldMayBeFinal
Field `codeLines` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
    public class Constructor extends Element<Constructor> {
        private List<Argument<ClassName,String>> args = new ArrayList<>();
        private List<String> codeLines = new ArrayList<>();
        int tabCount = 0;
        String tab = "";
```

### FieldMayBeFinal
Field `args` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java

    public class Constructor extends Element<Constructor> {
        private List<Argument<ClassName,String>> args = new ArrayList<>();
        private List<String> codeLines = new ArrayList<>();
        int tabCount = 0;
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
 */
class CompoundSelections {
    private static Localizer _loc = Localizer.forPackage(CompoundSelections.class);
    /**
     * Gets the strategy to fill a given compound selection.
```

### FieldMayBeFinal
Field `strategy` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
     */
    static class NewInstance<X> extends CompoundSelectionImpl<X> {
        private FillStrategy.NewInstance<X> strategy;
        public NewInstance(Class<X> cls, Selection<?>... selections) {
            super(cls, selections);
```

### FieldMayBeFinal
Field `type` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    private java.util.Set<Join<X, ?>> _joins;
    private java.util.Set<Fetch<X, ?>> _fetches;
    private Type<X> type;

    /**
```

### FieldMayBeFinal
Field `ALIAS_BASE` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    // AliasContext
    private int aliasCount = 0;
    private static String ALIAS_BASE = "autoAlias";

    private Map<Selection<?>,Value> _variables = new HashMap<>();
```

### FieldMayBeFinal
Field `_variables` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    private static String ALIAS_BASE = "autoAlias";

    private Map<Selection<?>,Value> _variables = new HashMap<>();
    private Map<Selection<?>,Value> _values    = new HashMap<>();
    private Map<Selection<?>,String> _aliases  = null;
```

### FieldMayBeFinal
Field `_contexts` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java

    // SubqueryContext
    private ThreadLocal<Stack<Context>> _contexts = new  ThreadLocal<Stack<Context>>(){
        @Override
        protected Stack<Context> initialValue() {
```

### FieldMayBeFinal
Field `_rootVariables` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    private Map<Selection<?>,Value> _values    = new HashMap<>();
    private Map<Selection<?>,String> _aliases  = null;
    private Map<Selection<?>,Value> _rootVariables = new HashMap<>();

    // SubqueryContext
```

### FieldMayBeFinal
Field `_values` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java

    private Map<Selection<?>,Value> _variables = new HashMap<>();
    private Map<Selection<?>,Value> _values    = new HashMap<>();
    private Map<Selection<?>,String> _aliases  = null;
    private Map<Selection<?>,Value> _rootVariables = new HashMap<>();
```

### FieldMayBeFinal
Field `_validationGroups` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java

    // A map storing the validation groups to use for a particular event type
    private Map<Integer, Class<?>[]> _validationGroups = new HashMap<>();

    // Lookup table for event to group property mapping
```

### FieldMayBeFinal
Field `_vgMapping` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java

    // Lookup table for event to group property mapping
    private static HashMap<String, Integer> _vgMapping = new HashMap<> ();

    static {
```

### FieldMayBeFinal
Field `_log` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/SLF4JLogFactory.java`
#### Snippet
```java
    public static class LogAdapter implements Log {

        private Logger _log;

        private LogAdapter(Logger wrapee) {
```

### FieldMayBeFinal
Field `_logs` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/LogFactoryAdapter.java`
#### Snippet
```java

    // cache category to log adapters
    private Map<String, Log> _logs = new ConcurrentHashMap<>();

    @Override
```

### FieldMayBeFinal
Field `_log` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/Log4J2LogFactory.java`
#### Snippet
```java
    public static class LogAdapter implements Log {

        private Logger _log;

        private LogAdapter(Logger wrapee) {
```

### FieldMayBeFinal
Field `_factory` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/CommonsLogFactory.java`
#### Snippet
```java
public class CommonsLogFactory extends LogFactoryAdapter {

    private org.apache.commons.logging.LogFactory _factory;

    public CommonsLogFactory() {
```

### FieldMayBeFinal
Field `_log` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/CommonsLogFactory.java`
#### Snippet
```java
    public static class LogAdapter implements org.apache.openjpa.lib.log.Log {

        private org.apache.commons.logging.Log _log;

        private LogAdapter(org.apache.commons.logging.Log wrapee) {
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/LogFactoryImpl.java`
#### Snippet
```java
    implements LogFactory, GenericConfigurable, Configurable {

    private static Localizer _loc = Localizer.forPackage(LogFactoryImpl.class);
    private static Localizer _locEn = Localizer.forPackage(
        LogFactoryImpl.class, Locale.ENGLISH);
```

### FieldMayBeFinal
Field `_locEn` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/LogFactoryImpl.java`
#### Snippet
```java

    private static Localizer _loc = Localizer.forPackage(LogFactoryImpl.class);
    private static Localizer _locEn = Localizer.forPackage(
        LogFactoryImpl.class, Locale.ENGLISH);

```

### FieldMayBeFinal
Field `_configuredLevels` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/LogFactoryImpl.java`
#### Snippet
```java
     * Storage for logging level configuration specified at configuration time.
     */
    private Map<String, Short> _configuredLevels =
        new HashMap<>();

```

### FieldMayBeFinal
Field `_logs` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/LogFactoryImpl.java`
#### Snippet
```java
     * The {@link Log}s that this factory manages, keyed by log channel name.
     */
    private Map<String, LogImpl> _logs =
        new ConcurrentHashMap<>();

```

### FieldMayBeFinal
Field `_log` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/Log4JLogFactory.java`
#### Snippet
```java
    public static class LogAdapter implements Log {

        private Logger _log;

        private LogAdapter(Logger wrapee) {
```

### FieldMayBeFinal
Field `_logs` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/MultiLogFactory.java`
#### Snippet
```java
    private static class MultiLog implements Log {

        private Log[] _logs;

        public MultiLog(Log[] logs) {
```

### FieldMayBeFinal
Field `_delegates` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/MultiLogFactory.java`
#### Snippet
```java
public class MultiLogFactory implements LogFactory {

    private List<LogFactory> _delegates;

    /**
```

### FieldMayBeFinal
Field `_endl` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/XMLWriter.java`
#### Snippet
```java
public class XMLWriter extends FilterWriter {

    private static String _endl = J2DoPrivHelper.getLineSeparator();

    private int _lastChar = ' ';
```

### FieldMayBeFinal
Field `_validating` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/XMLFactory.java`
#### Snippet
```java
    private static SAXParserFactory[] _saxFactories = null;
    private static DocumentBuilderFactory[] _domFactories = null;
    private static ErrorHandler _validating;

    static {
```

### FieldMayBeFinal
Field `_values` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/MergedResultObjectProvider.java`
#### Snippet
```java
    private final Comparator _comp;
    private final byte[] _status;
    private Object[] _values;
    private Object[] _orderValues;
    private Object _cur = null;
```

### FieldMayBeFinal
Field `_orderValues` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/MergedResultObjectProvider.java`
#### Snippet
```java
    private final byte[] _status;
    private Object[] _values;
    private Object[] _orderValues;
    private Object _cur = null;
    private int _size = -1;
```

### FieldMayBeFinal
Field `val1` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

    public static class Nullif<T> extends ExpressionImpl<T> {
        private Expression<T> val1;
        private Expression<?> val2;

```

### FieldMayBeFinal
Field `len` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
    public static class Substring extends UnaryFunctionalExpression<String> {
        private ExpressionImpl<Integer> from;
        private ExpressionImpl<Integer> len;

        public Substring(Expression<String> s, Expression<Integer> from, Expression<Integer> len) {
```

### FieldMayBeFinal
Field `_distinct` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

    public static class Count extends UnaryFunctionalExpression<Long> {
        private boolean _distinct;
        public  Count(Expression<?> x) {
            this(x, false);
```

### FieldMayBeFinal
Field `ts` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        static Expression<Character> defaultTrim = new Constant<>(Character.class, ' ');
        static Trimspec defaultSpec = Trimspec.BOTH;
        private Trimspec ts;

        public Trim(Expression<String> x, Expression<Character> y, Trimspec ts) {
```

### FieldMayBeFinal
Field `from` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

    public static class Substring extends UnaryFunctionalExpression<String> {
        private ExpressionImpl<Integer> from;
        private ExpressionImpl<Integer> len;

```

### FieldMayBeFinal
Field `path` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        private ExpressionImpl<String> pattern;
        private ExpressionImpl<Integer> from;
        private ExpressionImpl<String> path;

        public Locate(Expression<String> path, Expression<String> pattern, Expression<Integer> from) {
```

### FieldMayBeFinal
Field `from` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
    public static class Locate extends ExpressionImpl<Integer> {
        private ExpressionImpl<String> pattern;
        private ExpressionImpl<Integer> from;
        private ExpressionImpl<String> path;

```

### FieldMayBeFinal
Field `pattern` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

    public static class Locate extends ExpressionImpl<Integer> {
        private ExpressionImpl<String> pattern;
        private ExpressionImpl<Integer> from;
        private ExpressionImpl<String> path;
```

### FieldMayBeFinal
Field `val2` may be 'final'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
    public static class Nullif<T> extends ExpressionImpl<T> {
        private Expression<T> val1;
        private Expression<?> val2;

        public Nullif(Expression<T> x, Expression<?> y) {
```

### FieldMayBeFinal
Field `_classloaderCache` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ObjectValue.java`
#### Snippet
```java

    // cache the types' classloader
    private static ConcurrentReferenceHashMap _classloaderCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.HARD, ReferenceStrength.WEAK);

```

### FieldMayBeFinal
Field `_decorators` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DecoratingDataSource.java`
#### Snippet
```java
public class DecoratingDataSource extends DelegatingDataSource {

    private List<ConnectionDecorator> _decorators =
        new CopyOnWriteArrayList<>();

```

### FieldMayBeFinal
Field `_supportedKeys` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
    private String _auto = null;
    private final List<Value> _vals = new ArrayList<>();
    private Set<String> _supportedKeys = new TreeSet<>();

    // property listener helper
```

### FieldMayBeFinal
Field `_rootElement` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLVersionParser.java`
#### Snippet
```java
    static private final String SCHEMA_LOCATION = "schemaLocation";

    private String _rootElement;
    private String _version;
    private String _schemaLocation;
```

### FieldMayBeFinal
Field `nano0` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/TimestampHelper.java`
#### Snippet
```java
    // fraction of seconds passed 1970/1/1 00:00:00 GMT, offset by
    // the base System.nanoTime (nano0), in nanosecond unit.
    private static long nano0;

    static {
```

### FieldMayBeFinal
Field `sec0` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/TimestampHelper.java`
#### Snippet
```java

    // number of seconds passed 1970/1/1 00:00:00 GMT.
    private static long sec0;
    // fraction of seconds passed 1970/1/1 00:00:00 GMT, offset by
    // the base System.nanoTime (nano0), in nanosecond unit.
```

### FieldMayBeFinal
Field `_pkg` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Localizer.java`
#### Snippet
```java

    private String _file;
    private String _pkg;
    private ResourceBundle _bundle = null;
    private Locale _locale;
```

### FieldMayBeFinal
Field `_loaders` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/MultiClassLoader.java`
#### Snippet
```java
            J2DoPrivHelper.getSystemClassLoaderAction());

    private List<ClassLoader> _loaders = new ArrayList<>(5);

    /**
```

### FieldMayBeFinal
Field `_loader` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Localizer.java`
#### Snippet
```java
    private ResourceBundle _bundle = null;
    private Locale _locale;
    private ClassLoader _loader;

    private Localizer(String pkg, String f, Locale locale, ClassLoader loader) {
```

### FieldMayBeFinal
Field `_locale` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Localizer.java`
#### Snippet
```java
    private String _pkg;
    private ResourceBundle _bundle = null;
    private Locale _locale;
    private ClassLoader _loader;

```

### FieldMayBeFinal
Field `_file` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Localizer.java`
#### Snippet
```java
    }

    private String _file;
    private String _pkg;
    private ResourceBundle _bundle = null;
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
        { short.class, Short.class, (short) 0}, };

    private static Localizer _loc = Localizer.forPackage(Options.class);

    /**
```

### FieldMayBeFinal
Field `_primWrappers` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java

    // maps primitive types to the appropriate wrapper class and default value
    private static Object[][] _primWrappers = new Object[][]{
        { boolean.class, Boolean.class, Boolean.FALSE },
        { byte.class, Byte.class, (byte) 0},
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private static Localizer _loc = Localizer.forPackage
        (FormatPreservingProperties.class);

```

### FieldMayBeFinal
Field `scanUntilRemovable` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/LRUMap.java`
#### Snippet
```java
    private transient int maxSize;
    /** Scan behaviour */
    private boolean scanUntilRemovable;

    /**
```

### FieldMayBeFinal
Field `_wildcard` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierRule.java`
#### Snippet
```java
    private Set<String> _reservedWords = null;
    private boolean _delimitReservedWords = false;
    private String _wildcard = "%";

    public void setName(String name) {
```

### FieldMayBeFinal
Field `_instruments` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/instrumentation/AbstractInstrumentationProvider.java`
#### Snippet
```java
public abstract class AbstractInstrumentationProvider implements InstrumentationProvider, Configurable {

    private Map<String, Instrument> _instruments = new ConcurrentHashMap<>();

    private boolean _started = false;
```

### FieldMayBeFinal
Field `key` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java

        private int hash;
        private Object key;
        private Object value;
        private Entry next;
```

### FieldMayBeFinal
Field `hash` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
    private class HardEntry implements Entry {

        private int hash;
        private Object key;
        private Object value;
```

### FieldMayBeFinal
Field `hash` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
    private class SoftEntry extends SoftReference implements Entry {

        private int hash;
        private Object hard;
        private boolean keyRef;
```

### FieldMayBeFinal
Field `keyRef` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
        private int hash;
        private Object hard;
        private boolean keyRef;
        private Entry next;

```

### FieldMayBeFinal
Field `keyType` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
     * The key reference type.
     */
    private AbstractReferenceMap.ReferenceStrength keyType;

    /**
```

### FieldMayBeFinal
Field `valueType` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
     * The value reference type.
     */
    private AbstractReferenceMap.ReferenceStrength valueType;

    /**
```

### FieldMayBeFinal
Field `loadFactor` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
     * The load factor for the HashMap.
     */
    private float loadFactor;

    /**
```

### FieldMayBeFinal
Field `hash` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
    private class WeakEntry extends WeakReference implements Entry {

        private int hash;
        private Object hard;
        private boolean keyRef;
```

### FieldMayBeFinal
Field `keyRef` may be 'final'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
        private int hash;
        private Object hard;
        private boolean keyRef;
        private Entry next;

```

### FieldMayBeFinal
Field `_txManagerMeth` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/SunOneManagedRuntime.java`
#### Snippet
```java

    private Method _switchMeth;
    private Method _txManagerMeth;

    public SunOneManagedRuntime()
```

### FieldMayBeFinal
Field `_switchMeth` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/SunOneManagedRuntime.java`
#### Snippet
```java
    implements ManagedRuntime {

    private Method _switchMeth;
    private Method _txManagerMeth;

```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AbstractManagedRuntime.java`
#### Snippet
```java
public abstract class AbstractManagedRuntime implements ManagedRuntime {

    private static Localizer _loc =
        Localizer.forPackage(AbstractManagedRuntime.class);
    /**
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/RegistryManagedRuntime.java`
#### Snippet
```java
    private TransactionManagerRegistryFacade _tm = null;

    private static Localizer _loc =
        Localizer.forPackage(RegistryManagedRuntime.class);

```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java
    private final WASRegistryManagedRuntime WAS_REG;

    private static Localizer _loc = Localizer.forPackage
        (AutomaticManagedRuntime.class);

```

### FieldMayBeFinal
Field `ALIASES` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/AutoDetachValue.java`
#### Snippet
```java
    public static final String DETACH_NONE = "none";

    private static String[] ALIASES = new String[]{
        DETACH_CLOSE, String.valueOf(AutoDetach.DETACH_CLOSE),
        DETACH_COMMIT, String.valueOf(AutoDetach.DETACH_COMMIT),
```

### FieldMayBeFinal
Field `_conf` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/CacheMarshallersValue.java`
#### Snippet
```java
        Localizer.forPackage(CacheMarshallersValue.class);

    private Configuration _conf;
    private Map<String,CacheMarshaller> _marshallers;
    private boolean _initialized;
```

### FieldMayBeFinal
Field `_conf` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/SpecificationPlugin.java`
#### Snippet
```java
 */
public class SpecificationPlugin extends ObjectValue implements ValueListener {
    private Configuration _conf;
    protected static final Localizer _loc = Localizer.forPackage
        (SpecificationPlugin.class);
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Specification.java`
#### Snippet
```java
    private Compatibility _compatibility;

    private static Localizer _loc = Localizer.forPackage(Specification.class);

    /**
```

### FieldMayBeFinal
Field `_code` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/XMLClassMetaData.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private Class _type;
    private int _code = JavaTypes.OBJECT;
    private int _xmltype = XMLTYPE;
    private String _name = null;
```

### FieldMayBeFinal
Field `_fieldMap` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/XMLClassMetaData.java`
#### Snippet
```java
    private String _xmlnamespace = null;
    private boolean _isXMLRootElement = false;
    private HashMap _fieldMap = new HashMap();

    /**
```

### FieldMayBeFinal
Field `_unique` may be 'final'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
    private boolean _lob = false;
    private TemporalType _temporal = null;
    private EnumSet<UniqueFlag> _unique = EnumSet.noneOf(UniqueFlag.class);
    private DiscriminatorType _discType;
    private Column _discCol;
```

### FieldMayBeFinal
Field `_fm` may be 'final'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
            _defTable = DBIdentifier.NULL;
        }
        private FieldMapping _fm;
        private List<Column> _defCols;
        private List<Column> _defElemJoinCols;
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AccessCode.java`
#### Snippet
```java
	public static int MIXED     = 2 << 3;

	private static Localizer _loc = Localizer.forPackage(AccessCode.class);

	/**
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataTool.java`
#### Snippet
```java
    };

    private static Localizer _loc = Localizer.forPackage(MetaDataTool.class);

    private final OpenJPAConfiguration _conf;
```

### FieldMayBeFinal
Field `_repos` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/SequenceMetaData.java`
#### Snippet
```java
        (SequenceMetaData.class);

    private MetaDataRepository _repos;
    private SequenceFactory _factory = null;

```

### FieldMayBeFinal
Field `_decType` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/XMLFieldMetaData.java`
#### Snippet
```java
    private String _xmlname = null;
    private String _xmlnamespace = null;
    private Class _decType = Object.class;
    private int _decCode = JavaTypes.OBJECT;
    private Class _type = Object.class;
```

### FieldMayBeFinal
Field `_decCode` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/XMLFieldMetaData.java`
#### Snippet
```java
    private String _xmlnamespace = null;
    private Class _decType = Object.class;
    private int _decCode = JavaTypes.OBJECT;
    private Class _type = Object.class;
    private int _code = JavaTypes.OBJECT;
```

### FieldMayBeFinal
Field `_convertPositionalParametersToNamed` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/QueryMetaData.java`
#### Snippet
```java
    private int _colNum;
    private String _srcName;
    private boolean _convertPositionalParametersToNamed;
    private OrderedMap<Object,Class<?>> _paramTypes;

```

### FieldMayBeFinal
Field `_impls` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/StoreFacadeTypeRegistry.java`
#### Snippet
```java
public class StoreFacadeTypeRegistry {

    private Map _impls = new ConcurrentHashMap();

    /**
```

### FieldMayBeFinal
Field `_dict` may be 'final'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        new HashMap<>();

    private DBDictionary _dict;

    static {
```

### FieldMayBeFinal
Field `_typeCache` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/OpenJPAId.java`
#### Snippet
```java

    // cache the types' generated hash codes
    private static ConcurrentReferenceHashMap _typeCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);

```

### FieldMayBeFinal
Field `createdMethods` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ClassWriterTracker.java`
#### Snippet
```java

    private final ClassWriter cw;
    private List<String> createdMethods = new ArrayList<>();

    public ClassWriterTracker(ClassWriter cw) {
```

### FieldMayBeFinal
Field `_metamodel` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    private Map<Class<?>, NonPersistentMetaData> _nonMapped =
        Collections.synchronizedMap(new HashMap<>());
    private Map<Class<?>, Class<?>> _metamodel = Collections.synchronizedMap(new HashMap<>());

    // map of classes to lists of their subclasses
```

### FieldMayBeFinal
Field `_seqs` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    private Map<Class<?>, Class<?>> _ifaces = Collections.synchronizedMap(new HashMap<>());
    private Map<String, QueryMetaData> _queries = new HashMap<>();
    private Map<String, SequenceMetaData> _seqs = new HashMap<>();
    private Map<String, List<Class<?>>> _aliases = Collections.synchronizedMap(new HashMap<>());
    private Map<Class<?>, NonPersistentMetaData> _pawares =
```

### FieldMayBeFinal
Field `_metaStringMap` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    // to metadatas
    private Map<Class<?>, ClassMetaData> _metas = new HashMap<>();
    private Map<String, ClassMetaData> _metaStringMap = new ConcurrentHashMap<>();
    private Map<Class<?>, Class<?>> _oids = Collections.synchronizedMap(new HashMap<>());
    private Map<Class<?>, Collection<Class<?>>> _impls =
```

### FieldMayBeFinal
Field `_queries` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
        Collections.synchronizedMap(new HashMap<>());
    private Map<Class<?>, Class<?>> _ifaces = Collections.synchronizedMap(new HashMap<>());
    private Map<String, QueryMetaData> _queries = new HashMap<>();
    private Map<String, SequenceMetaData> _seqs = new HashMap<>();
    private Map<String, List<Class<?>>> _aliases = Collections.synchronizedMap(new HashMap<>());
```

### FieldMayBeFinal
Field `_metas` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    // cache of parsed metadata, oid class to class, and interface class
    // to metadatas
    private Map<Class<?>, ClassMetaData> _metas = new HashMap<>();
    private Map<String, ClassMetaData> _metaStringMap = new ConcurrentHashMap<>();
    private Map<Class<?>, Class<?>> _oids = Collections.synchronizedMap(new HashMap<>());
```

### FieldMayBeFinal
Field `supportedOptions` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfigurationImpl.java`
#### Snippet
```java
    public AutoDetachValue autoDetach;

    private Collection<String> supportedOptions = new HashSet<>(33);
    private final StoreFacadeTypeRegistry _storeFacadeRegistry = new StoreFacadeTypeRegistry();
    private BrokerFactoryEventManager _brokerFactoryEventManager = new BrokerFactoryEventManager(this);
```

### FieldMayBeFinal
Field `_brokerFactoryEventManager` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfigurationImpl.java`
#### Snippet
```java
    private Collection<String> supportedOptions = new HashSet<>(33);
    private final StoreFacadeTypeRegistry _storeFacadeRegistry = new StoreFacadeTypeRegistry();
    private BrokerFactoryEventManager _brokerFactoryEventManager = new BrokerFactoryEventManager(this);
    private Map<String, Object> _peMap; //contains persistence environment-specific info
    private boolean _allowSetLifeCycleEventManager = true;
```

### FieldMayBeFinal
Field `_ctx` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/Serialization.java`
#### Snippet
```java
        extends ObjectOutputStream {

        private StoreContext _ctx;

        /**
```

### FieldMayBeFinal
Field `s_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/JMSRemoteCommitProvider.java`
#### Snippet
```java
    implements Configurable, GenericConfigurable, ExceptionListener {

    private static Localizer s_loc = Localizer.forPackage
        (JMSRemoteCommitProvider.class);

```

### FieldMayBeFinal
Field `s_providers` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/SingleJVMRemoteCommitProvider.java`
#### Snippet
```java
    extends AbstractRemoteCommitProvider {

    private static Set s_providers = new ConcurrentReferenceHashSet(ReferenceStrength.HARD);

    public SingleJVMRemoteCommitProvider() {
```

### FieldMayBeFinal
Field `eventType` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/BrokerFactoryEvent.java`
#### Snippet
```java
    public static final int BROKER_FACTORY_CREATED = 0;

    private int eventType;

    public BrokerFactoryEvent(BrokerFactory brokerFactory, int eventType) {
```

### FieldMayBeFinal
Field `_repos` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
    private static final String[] EMPTY_STRING_ARRAY = new String[0];

    private MetaDataRepository _repos;
    private transient ClassLoader _loader = null;

```

### FieldMayBeFinal
Field `_expressionParsers` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryLanguages.java`
#### Snippet
```java
    public static final String LANG_METHODQL = "openjpa.MethodQL";

    private static Map _expressionParsers = new HashMap();
    static {
        // Load and cache all the query languages available in the system.
```

### FieldMayBeFinal
Field `_addListeners` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/LifecycleEventManager.java`
#### Snippet
```java
    private ListenerList _listeners = null;
    // odd-element: Listener even-element: Class[]
    private List<Object> _addListeners = new LinkedList<>();
    private List<Object> _remListeners = new LinkedList<>();
    private List<Exception> _exceps = new LinkedList<>();
```

### FieldMayBeFinal
Field `_exceps` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/LifecycleEventManager.java`
#### Snippet
```java
    private List<Object> _addListeners = new LinkedList<>();
    private List<Object> _remListeners = new LinkedList<>();
    private List<Exception> _exceps = new LinkedList<>();
    private boolean _firing = false;
    private boolean _fail = false;
```

### FieldMayBeFinal
Field `_remListeners` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/LifecycleEventManager.java`
#### Snippet
```java
    // odd-element: Listener even-element: Class[]
    private List<Object> _addListeners = new LinkedList<>();
    private List<Object> _remListeners = new LinkedList<>();
    private List<Exception> _exceps = new LinkedList<>();
    private boolean _firing = false;
```

### FieldMayBeFinal
Field `_receiverThreads` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
        private ServerSocket _receiveSocket;
        private Thread _acceptThread;
        private Set<Thread> _receiverThreads = new HashSet<>();
        private final Set<TCPRemoteCommitProvider> _providers = new HashSet<>();

```

### FieldMayBeFinal
Field `_receiveSocket` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java

        private final Log _log;
        private ServerSocket _receiveSocket;
        private Thread _acceptThread;
        private Set<Thread> _receiverThreads = new HashSet<>();
```

### FieldMayBeFinal
Field `_port` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
         * by TCPRemoteCommitProvider.
         */
        private int _port;

        /**
```

### FieldMayBeFinal
Field `broker` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
    private Collection<StateManagerImpl> _embeds = null; // embedded/non-persistent sms
    private Collection<StateManagerImpl> _untracked = null; // hard refs to untracked sms
    private BrokerImpl broker;

    /**
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
    public static final PCState PDELETEDFLUSHED = new PDeletedFlushedState();

    private static Localizer _loc = Localizer.forPackage(PCState.class);

    /**
```

### FieldMayBeFinal
Field `_meta` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedValueStateManager.java`
#### Snippet
```java
    private PersistenceCapable _pc;
    private StoreContext _ctx;
    private ClassMetaData _meta;

    public DetachedValueStateManager(Object pc, StoreContext ctx) {
```

### FieldMayBeFinal
Field `_ctx` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedValueStateManager.java`
#### Snippet
```java

    private PersistenceCapable _pc;
    private StoreContext _ctx;
    private ClassMetaData _meta;

```

### FieldMayBeFinal
Field `_pc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedValueStateManager.java`
#### Snippet
```java
        (DetachedValueStateManager.class);

    private PersistenceCapable _pc;
    private StoreContext _ctx;
    private ClassMetaData _meta;
```

### FieldMayBeFinal
Field `_reloadOnDetach` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
    private boolean _flushBeforeDetach;
    private boolean _cascadeWithDetach;
    private boolean _reloadOnDetach;

    // if we're not detaching full, we need to track all detached objects;
```

### FieldMayBeFinal
Field `_cascadeWithDetach` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
    private boolean _flushed = false;
    private boolean _flushBeforeDetach;
    private boolean _cascadeWithDetach;
    private boolean _reloadOnDetach;

```

### FieldMayBeFinal
Field `_flushBeforeDetach` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
    private final boolean _failFast;
    private boolean _flushed = false;
    private boolean _flushBeforeDetach;
    private boolean _cascadeWithDetach;
    private boolean _reloadOnDetach;
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
    implements DetachState {

    private static Localizer _loc = Localizer.forPackage(DetachManager.class);

    private final BrokerImpl _broker;
```

### FieldMayBeFinal
Field `s_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Bootstrap.java`
#### Snippet
```java
        new Class<?>[] { ConfigurationProvider.class, ClassLoader.class };

    private static Localizer s_loc = Localizer.forPackage(Bootstrap.class);

    /**
```

### FieldMayBeFinal
Field `_facts` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
        private boolean _subs;
        private ExpressionParser _parser;
        private ExpressionFactory[] _facts;
        private QueryExpressions[] _exps;
        private Class[] _projTypes;
```

### FieldMayBeFinal
Field `_parser` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
        private ClassMetaData[] _metas;
        private boolean _subs;
        private ExpressionParser _parser;
        private ExpressionFactory[] _facts;
        private QueryExpressions[] _exps;
```

### FieldMayBeFinal
Field `_metas` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
        private static final long serialVersionUID = 1L;
        private ClassMetaData _meta;
        private ClassMetaData[] _metas;
        private boolean _subs;
        private ExpressionParser _parser;
```

### FieldMayBeFinal
Field `_exps` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
        private ExpressionParser _parser;
        private ExpressionFactory[] _facts;
        private QueryExpressions[] _exps;
        private Class[] _projTypes;
        private Value[] _inMemOrdering;
```

### FieldMayBeFinal
Field `_meta` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
        
        private static final long serialVersionUID = 1L;
        private ClassMetaData _meta;
        private ClassMetaData[] _metas;
        private boolean _subs;
```

### FieldMayBeFinal
Field `_projTypes` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
        private ExpressionFactory[] _facts;
        private QueryExpressions[] _exps;
        private Class[] _projTypes;
        private Value[] _inMemOrdering;

```

### FieldMayBeFinal
Field `_subs` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
        private ClassMetaData _meta;
        private ClassMetaData[] _metas;
        private boolean _subs;
        private ExpressionParser _parser;
        private ExpressionFactory[] _facts;
```

### FieldMayBeFinal
Field `_hintSetters` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfigurationImpl.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private static final Localizer _loc = Localizer.forPackage(FetchConfigurationImpl.class);
    private static Map<String, Method> _hintSetters = new HashMap<>();

    /**
```

### FieldMayBeFinal
Field `_collected` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/QueryExpressions.java`
#### Snippet
```java
    private static class ParameterExpressionVisitor extends AbstractExpressionVisitor {
        private FieldMetaData _parameterized;
        private List<FieldMetaData> _collected = new ArrayList<>();
        /**
         * Enters the current node.
```

### FieldMayBeFinal
Field `o` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ReflectingPersistenceCapable.java`
#### Snippet
```java
    
    private static final long serialVersionUID = 1L;
    private Object o;
    private StateManager sm;

```

### FieldMayBeFinal
Field `_cacheFinderQuery` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
    private boolean _orderDirty = false;
    private boolean _cachePreparedQuery = true;
    private boolean _cacheFinderQuery = true;
    private boolean _suppressBatchOLELogging = false;
    private boolean _allowReferenceToSiblingContext = false;
```

### FieldMayBeFinal
Field `_supportedPropertyNames` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
    // Set of supported property keys. The keys in this set correspond to bean-style setter methods
    // that can be set by reflection. The keys are not qualified by any prefix.
    private static Set<String> _supportedPropertyNames;
    static {
        _supportedPropertyNames = new HashSet<>();
```

### FieldMayBeFinal
Field `setterMethodCache` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    private static Map<Class<?>, Map<String, Method>> getterMethodCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
    private static Map<Class<?>, Map<String, Method>> setterMethodCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
    private static Map<Class<?>, Set<String>> beanPropertiesNameCache =
```

### FieldMayBeFinal
Field `getterMethodCache` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java

    // Weak HashMap caches of getter/setter/beanProperty methods
    private static Map<Class<?>, Map<String, Method>> getterMethodCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
    private static Map<Class<?>, Map<String, Method>> setterMethodCache =
```

### FieldMayBeFinal
Field `beanPropertiesNameCache` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    private static Map<Class<?>, Map<String, Method>> setterMethodCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
    private static Map<Class<?>, Set<String>> beanPropertiesNameCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);

```

### FieldMayBeFinal
Field `_log` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ClearableScheduler.java`
#### Snippet
```java
    private boolean _stop = false;
    private int _interval = 1;
    private Log _log;
    private Thread _thread;

```

### FieldMayBeFinal
Field `_clearables` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ClearableScheduler.java`
#### Snippet
```java
    private static final Localizer _loc = Localizer.forPackage(ClearableScheduler.class);

    private Map<Clearable,Schedule> _clearables = new ConcurrentHashMap<>();
    private boolean _stop = false;
    private int _interval = 1;
```

### FieldMayBeFinal
Field `start` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/CacheStatisticsImpl.java`
#### Snippet
```java
    private Map<String, long[]> totalStats = new HashMap<>();

    private Date start = new Date();
    private Date since = new Date();
    private boolean enabled = false;
```

### FieldMayBeFinal
Field `totalStat` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/CacheStatisticsImpl.java`
#### Snippet
```java
    private static final long serialVersionUID = 9014495759588003166L;
    private static final int ARRAY_SIZE = 3;
    private long[] totalStat = new long[ARRAY_SIZE];
    private long[] stat = new long[ARRAY_SIZE];
    private Map<String, long[]> stats = new HashMap<>();
```

### FieldMayBeFinal
Field `totalStats` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/CacheStatisticsImpl.java`
#### Snippet
```java
    private long[] stat = new long[ARRAY_SIZE];
    private Map<String, long[]> stats = new HashMap<>();
    private Map<String, long[]> totalStats = new HashMap<>();

    private Date start = new Date();
```

### FieldMayBeFinal
Field `stats` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/CacheStatisticsImpl.java`
#### Snippet
```java
    private long[] totalStat = new long[ARRAY_SIZE];
    private long[] stat = new long[ARRAY_SIZE];
    private Map<String, long[]> stats = new HashMap<>();
    private Map<String, long[]> totalStats = new HashMap<>();

```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/DataCacheJMXInstrument.java`
#### Snippet
```java
    DataCacheJMXInstrumentMBean {

    private static Localizer _loc = Localizer.forPackage(DataCacheJMXInstrument.class);

    private static final String MBEAN_TYPE = "DataCache";
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/QueryCacheJMXInstrument.java`
#### Snippet
```java
    implements JMXInstrument, QueryCacheJMXInstrumentMBean {

    private static Localizer _loc = Localizer.forPackage(QueryCacheJMXInstrument.class);

    private static final String MBEAN_TYPE = "QueryCache";
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/PreparedQueryCacheJMXInstrument.java`
#### Snippet
```java
    implements JMXInstrument, PreparedQueryCacheJMXInstrumentMBean {

    private static Localizer _loc = Localizer.forPackage(PreparedQueryCacheJMXInstrument.class);

    private static final String MBEAN_TYPE = "QuerySQLCache";
```

### FieldMayBeFinal
Field `s_unmod` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryKey.java`
#### Snippet
```java
    // to avoid cloning collections that are not modifiable,
    // provided that they do not contain mutable objects.
    private static Collection<Class<?>> s_unmod = new HashSet<>();

    static {
```

### FieldMayBeFinal
Field `_registry` may be 'final'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/PrototypeFactory.java`
#### Snippet
```java
 */
public class PrototypeFactory<K,T> {
    private Map<K, Class<? extends T>> _registry = new TreeMap<>();

    /**
```

### FieldMayBeFinal
Field `_oid` may be 'final'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
    implements Cloneable {

    private Object _oid;
    private Object[] _data;
    private Long _version;
```

### FieldMayBeFinal
Field `_data` may be 'final'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java

    private Object _oid;
    private Object[] _data;
    private Long _version;
    private ClassMetaData _meta;
```

### FieldMayBeFinal
Field `_meta` may be 'final'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
    private Object[] _data;
    private Long _version;
    private ClassMetaData _meta;

    /**
```

### FieldMayBeFinal
Field `_visited` may be 'final'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/Closure.java`
#### Snippet
```java
 */
public class Closure implements Iterable<OpenJPAStateManager> {
    private Set<OpenJPAStateManager> _visited = new LinkedHashSet<>();

    public Closure(OpenJPAStateManager root) {
```

### FieldMayBeFinal
Field `_entries` may be 'final'
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
     */
    public static class KVMap implements JSON {
        private Map<Object,Object> _entries = new LinkedHashMap<>();

        public void put(Object k, Object v) {
```

### FieldMayBeFinal
Field `_members` may be 'final'
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
     */
    public static class Array implements JSON {
        private List<Object> _members = new ArrayList<>();

        public void add(Object o) {
```

### FieldMayBeFinal
Field `ext` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/FileScanner.java`
#### Snippet
```java

public class FileScanner {
    private String ext;
    boolean recursive;

```

### FieldMayBeFinal
Field `_members` may be 'final'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObject.java`
#### Snippet
```java
     */
    public static class Array implements JSON {
        private List<Object> _members = new ArrayList<>();

        public void add(Object o) {
```

### FieldMayBeFinal
Field `_entries` may be 'final'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObject.java`
#### Snippet
```java
     */
    public static class KVMap implements JSON {
        private Map<Object,Object> _entries = new LinkedHashMap<>();

        public void put(Object k, Object v) {
```

### FieldMayBeFinal
Field `_formatterFactory` may be 'final'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/AbstractCommand.java`
#### Snippet
```java
    protected final JPAServletContext _ctx;

    private static PrototypeFactory<Format,ObjectFormatter<?>> _formatterFactory =
        new PrototypeFactory<>();
    protected static Localizer _loc = Localizer.forPackage(AbstractCommand.class);
```

### FieldMayBeFinal
Field `EMPTY_CLASSES` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private List<Attribute<? super T,?>> attributes;
    private static Object[] EMPTY_ARGS = null;
    private static Class<?>[] EMPTY_CLASSES = null;

    private boolean showsRowCount;
```

### FieldMayBeFinal
Field `EMPTY_ARGS` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private List<T>        data;
    private List<Attribute<? super T,?>> attributes;
    private static Object[] EMPTY_ARGS = null;
    private static Class<?>[] EMPTY_CLASSES = null;

```

### FieldMayBeFinal
Field `showsBasicAttr` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java

    private boolean showsRowCount;
    private boolean showsBasicAttr;
    private boolean showsSingularAttr;
    private boolean showsPluralAttr;
```

### FieldMayBeFinal
Field `methods` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private List<String>   columnNames;
    private List<Class<?>> columnClasses;
    private List<Method>   methods;
    private List<T>        data;
    private List<Attribute<? super T,?>> attributes;
```

### FieldMayBeFinal
Field `attributes` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private List<Method>   methods;
    private List<T>        data;
    private List<Attribute<? super T,?>> attributes;
    private static Object[] EMPTY_ARGS = null;
    private static Class<?>[] EMPTY_CLASSES = null;
```

### FieldMayBeFinal
Field `columnNames` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    public static int ROW_COUNT        = 1 << 3;

    private List<String>   columnNames;
    private List<Class<?>> columnClasses;
    private List<Method>   methods;
```

### FieldMayBeFinal
Field `showsRowCount` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private static Class<?>[] EMPTY_CLASSES = null;

    private boolean showsRowCount;
    private boolean showsBasicAttr;
    private boolean showsSingularAttr;
```

### FieldMayBeFinal
Field `showsSingularAttr` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private boolean showsRowCount;
    private boolean showsBasicAttr;
    private boolean showsSingularAttr;
    private boolean showsPluralAttr;

```

### FieldMayBeFinal
Field `columnClasses` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java

    private List<String>   columnNames;
    private List<Class<?>> columnClasses;
    private List<Method>   methods;
    private List<T>        data;
```

### FieldMayBeFinal
Field `showsPluralAttr` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private boolean showsBasicAttr;
    private boolean showsSingularAttr;
    private boolean showsPluralAttr;

    /**
```

### FieldMayBeFinal
Field `_table` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTableView.java`
#### Snippet
```java
public class EntityTableView<T> extends JPanel implements ListSelectionListener {
    private static final long serialVersionUID = 1L;
    private EntityTable<T> _table;
    private JTextArea _details;

```

### FieldMayBeFinal
Field `_defaultStyle` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/GraphicOutputStream.java`
#### Snippet
```java
    private int count;
    private Map<String, AttributeSet> _styles = new HashMap<>();
    private static AttributeSet _defaultStyle = StyleContext.getDefaultStyleContext()
                                                            .getStyle(StyleContext.DEFAULT_STYLE);

```

### FieldMayBeFinal
Field `_styles` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/GraphicOutputStream.java`
#### Snippet
```java
    private char[] buffer = new char[1024];
    private int count;
    private Map<String, AttributeSet> _styles = new HashMap<>();
    private static AttributeSet _defaultStyle = StyleContext.getDefaultStyleContext()
                                                            .getStyle(StyleContext.DEFAULT_STYLE);
```

### FieldMayBeFinal
Field `buffer` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/GraphicOutputStream.java`
#### Snippet
```java
public class GraphicOutputStream extends OutputStream {
    ScrollingTextPane _sink;
    private char[] buffer = new char[1024];
    private int count;
    private Map<String, AttributeSet> _styles = new HashMap<>();
```

### FieldMayBeFinal
Field `_slides` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
public class PowerPointViewer extends JPanel {
    private static final long serialVersionUID = 1L;
    private List<WeakReference<ImageIcon>> _slides = new ArrayList<>();
    private int _total;      // Total number of slides in the deck.
    private String _dir;
```

### FieldMayBeFinal
Field `_total` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private List<WeakReference<ImageIcon>> _slides = new ArrayList<>();
    private int _total;      // Total number of slides in the deck.
    private String _dir;
    private List<URL> _slideURLs;
```

### FieldMayBeFinal
Field `MAX_BUTTONS` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
    private JSpinner _goto;
    private JButton[] _navButtons;
    private int MAX_BUTTONS = 10; // Total number of navigation buttons excluding the PREVIOUS and NEXT button.

    // The slide
```

### FieldMayBeFinal
Field `_view` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java

    // The slide
    private JLabel _view;

    // The key for client property in the navigation buttons denoting the 0-based index of the slides.
```

### FieldMayBeFinal
Field `_slideURLs` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
    private int _total;      // Total number of slides in the deck.
    private String _dir;
    private List<URL> _slideURLs;
    private JButton _prev, _next;
    private JSpinner _goto;
```

### FieldMayBeFinal
Field `_dir` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
    private List<WeakReference<ImageIcon>> _slides = new ArrayList<>();
    private int _total;      // Total number of slides in the deck.
    private String _dir;
    private List<URL> _slideURLs;
    private JButton _prev, _next;
```

### FieldMayBeFinal
Field `filters` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
public class ErrorDialog extends JDialog {
    private static final long serialVersionUID = 1L;
    private static List<String> filters = Arrays.asList(
            "java.awt.",
            "javax.swing.",
```

### FieldMayBeFinal
Field `TOTAL_SIZE` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
    private static Dimension MESSAGE_SIZE    = new Dimension(600,200);
    private static Dimension STACKTRACE_SIZE = new Dimension(600,300);
    private static Dimension TOTAL_SIZE      = new Dimension(600,500);


```

### FieldMayBeFinal
Field `MESSAGE_SIZE` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
            "sun.reflect.",
            "java.util.concurrent.");
    private static Dimension MESSAGE_SIZE    = new Dimension(600,200);
    private static Dimension STACKTRACE_SIZE = new Dimension(600,300);
    private static Dimension TOTAL_SIZE      = new Dimension(600,500);
```

### FieldMayBeFinal
Field `_main` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
    private boolean      _isFiltering = true;
    private JComponent _message;
    private JComponent   _main;
    private JScrollPane  _details;
    private JTextPane    _stacktrace;
```

### FieldMayBeFinal
Field `STACKTRACE_SIZE` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
            "java.util.concurrent.");
    private static Dimension MESSAGE_SIZE    = new Dimension(600,200);
    private static Dimension STACKTRACE_SIZE = new Dimension(600,300);
    private static Dimension TOTAL_SIZE      = new Dimension(600,500);

```

### FieldMayBeFinal
Field `_message` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
    private boolean      _showingDetails;
    private boolean      _isFiltering = true;
    private JComponent _message;
    private JComponent   _main;
    private JScrollPane  _details;
```

### FieldMayBeFinal
Field `_instance` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/InstanceDataModel.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    List<Attribute<? super T, ?>> attributes;
    private T _instance;

    public InstanceDataModel(EntityType<T> type, T instance) {
```

### FieldMayBeFinal
Field `messageText` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
    private JProgressBar progressBar;
    private JProgressBar memoryBar;
    private JLabel messageText;
    private TaskProgress task;

```

### FieldMayBeFinal
Field `memoryBar` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private JProgressBar progressBar;
    private JProgressBar memoryBar;
    private JLabel messageText;
    private TaskProgress task;
```

### FieldMayBeFinal
Field `progressBar` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
public class StatusBar extends JPanel implements PropertyChangeListener {
    private static final long serialVersionUID = 1L;
    private JProgressBar progressBar;
    private JProgressBar memoryBar;
    private JLabel messageText;
```

### FieldMayBeFinal
Field `fmt` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ScrollingTextPane.java`
#### Snippet
```java

        Timer timer = new Timer(1000, new ActionListener() {
            SimpleDateFormat fmt = new SimpleDateFormat("HH:mm:ss");
            @Override
            public void actionPerformed(ActionEvent e) {
```

### FieldMayBeFinal
Field `_anchors` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SourceCodeViewer.java`
#### Snippet
```java
    private final JEditorPane _editor;
    private final JComboBox   _bookmarks;
    private IndexedMap<String, URI> _anchors = new IndexedMap<>();
    private LinkedList<String> _visited = new LinkedList<>();

```

### FieldMayBeFinal
Field `_visited` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SourceCodeViewer.java`
#### Snippet
```java
    private final JComboBox   _bookmarks;
    private IndexedMap<String, URI> _anchors = new IndexedMap<>();
    private LinkedList<String> _visited = new LinkedList<>();

    /**
```

### FieldMayBeFinal
Field `factors` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SwingHelper.java`
#### Snippet
```java
    }

    private static int[] factors = {1000, 1000, 1000, 1000, 60, 60, 24};

    public static String getTimeString(long value, TimeUnit unit) {
```

### FieldMayBeFinal
Field `EMPTY_CLASSES` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/MetamodelHelper.java`
#### Snippet
```java
    private static Map<Attribute<?,?>, Method> members = new HashMap<>();
    private static Object[] EMPTY_ARGS = null;
    private static Class<?>[] EMPTY_CLASSES = null;
    /**
     * Gets the value of the given persistent attribute for the given instance.
```

### FieldMayBeFinal
Field `EMPTY_ARGS` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/MetamodelHelper.java`
#### Snippet
```java
    }
    private static Map<Attribute<?,?>, Method> members = new HashMap<>();
    private static Object[] EMPTY_ARGS = null;
    private static Class<?>[] EMPTY_CLASSES = null;
    /**
```

### FieldMayBeFinal
Field `members` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/MetamodelHelper.java`
#### Snippet
```java
      }
    }
    private static Map<Attribute<?,?>, Method> members = new HashMap<>();
    private static Object[] EMPTY_ARGS = null;
    private static Class<?>[] EMPTY_CLASSES = null;
```

### FieldMayBeFinal
Field `instanceCellRenderer` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTable.java`
#### Snippet
```java
public class EntityTable<T> extends JTable {
    private static final long serialVersionUID = 1L;
    private InstanceCellRenderer instanceCellRenderer;
    private CollectionCellRenderer collectionCellRenderer;

```

### FieldMayBeFinal
Field `collectionCellRenderer` may be 'final'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTable.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private InstanceCellRenderer instanceCellRenderer;
    private CollectionCellRenderer collectionCellRenderer;

    public EntityTable(Class<T> cls, List<T> data, int styleBits, EntityManagerFactory unit) {
```

### FieldMayBeFinal
Field `requiresInput` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/CommandProcessor.java`
#### Snippet
```java
         * Does the option require a value?
         */
        private boolean requiresInput;

        /**
```

### FieldMayBeFinal
Field `aliases` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/CommandProcessor.java`
#### Snippet
```java
         * All aliases must start with a dash (<code>-</code>).
         */
        private String[] aliases;

        /**
```

### FieldMayBeFinal
Field `_args` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/ant/Java2HTMLConverterTask.java`
#### Snippet
```java
    private boolean verbose          = false;

    private List<Arg> _args = new ArrayList<>();

    public String getSourcepath() {
```

### FieldMayBeFinal
Field `_stream` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/ParseTokenListener.java`
#### Snippet
```java
    private int decision;
    private TokenRenderer _renderer;
    private PrintStream _stream;

    /**
```

### FieldMayBeFinal
Field `_renderer` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/ParseTokenListener.java`
#### Snippet
```java
    private int backtracking = 0;
    private int decision;
    private TokenRenderer _renderer;
    private PrintStream _stream;

```

### FieldMayBeFinal
Field `_useExternal` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SourceCodeBrowser.java`
#### Snippet
```java
 */
public class SourceCodeBrowser {
    private boolean _useExternal;
    private SourceCodeViewer _internal = new SourceCodeViewer();
    private URI _rootURI;
```

### FieldMayBeFinal
Field `_internal` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SourceCodeBrowser.java`
#### Snippet
```java
public class SourceCodeBrowser {
    private boolean _useExternal;
    private SourceCodeViewer _internal = new SourceCodeViewer();
    private URI _rootURI;
    private File _rootDir;
```

### FieldMayBeFinal
Field `_rootURI` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SourceCodeBrowser.java`
#### Snippet
```java
    private boolean _useExternal;
    private SourceCodeViewer _internal = new SourceCodeViewer();
    private URI _rootURI;
    private File _rootDir;

```

### FieldMayBeFinal
Field `red` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SQLLogger.java`
#### Snippet
```java
public class SQLLogger extends AbstractJDBCListener {
    private PrintStream out = null;
    private static AttributeSet red, green, blue, magenta;
    static {
        StyleContext ctx = StyleContext.getDefaultStyleContext();
```

### FieldMayBeFinal
Field `green` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SQLLogger.java`
#### Snippet
```java
public class SQLLogger extends AbstractJDBCListener {
    private PrintStream out = null;
    private static AttributeSet red, green, blue, magenta;
    static {
        StyleContext ctx = StyleContext.getDefaultStyleContext();
```

### FieldMayBeFinal
Field `magenta` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SQLLogger.java`
#### Snippet
```java
public class SQLLogger extends AbstractJDBCListener {
    private PrintStream out = null;
    private static AttributeSet red, green, blue, magenta;
    static {
        StyleContext ctx = StyleContext.getDefaultStyleContext();
```

### FieldMayBeFinal
Field `blue` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SQLLogger.java`
#### Snippet
```java
public class SQLLogger extends AbstractJDBCListener {
    private PrintStream out = null;
    private static AttributeSet red, green, blue, magenta;
    static {
        StyleContext ctx = StyleContext.getDefaultStyleContext();
```

### FieldMayBeFinal
Field `customIdentifiers` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/HTMLTokenRenderer.java`
#### Snippet
```java
    public static final String CSS_LINE_NO     = "lineno";

    private Set<String> customIdentifiers = new HashSet<>();

    public static final String NEW_LINE = "\r\n";
```

### FieldMayBeFinal
Field `images` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Images.java`
#### Snippet
```java
public class Images {

    private static Map<String, Icon> images = new HashMap<>();

    public static Icon NEXT    = getIcon("images/arrow_right.jpg", 24, -1);
```

### FieldMayBeFinal
Field `REORDER_QUANTITY` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SupplyPage.java`
#### Snippet
```java

    private static int REORDER_LIMIT    = 10;
    private static int REORDER_QUANTITY = 40;

    public SupplyPage(final OpenBookService service) {
```

### FieldMayBeFinal
Field `REORDER_LIMIT` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/SupplyPage.java`
#### Snippet
```java
    private final JLabel  _title;

    private static int REORDER_LIMIT    = 10;
    private static int REORDER_QUANTITY = 40;

```

### FieldMayBeFinal
Field `_oids` may be 'final'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
    private final ClassMetaData _meta;
    private final Log _log;
    private Collection _oids = null;
    private boolean _defCons = true;
    private boolean _redefine = false;
```

### FieldMayBeFinal
Field `_orders` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/DeliveryPage.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private final JButton _deliver;
    private EntityTableView<PurchaseOrder> _orders;
    private final OpenBookService _service;
    private final JLabel _title;
```

### FieldMayBeFinal
Field `items` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/domain/ShoppingCart.java`
#### Snippet
```java
     * The items in the cart and their respective quantity.
     */
    private Map<Book, Integer> items;

    /**
```

### FieldMayBeFinal
Field `customer` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/domain/ShoppingCart.java`
#### Snippet
```java
     * The owner of this cart.
     */
    private Customer customer;

    /**
```

### FieldMayBeFinal
Field `placedOn` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/domain/PurchaseOrder.java`
#### Snippet
```java

    @Temporal(TemporalType.TIMESTAMP)
    private Timestamp placedOn;

    @Temporal(TemporalType.TIMESTAMP)
```

### FieldMayBeFinal
Field `lock` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/server/PersistenceService.java`
#### Snippet
```java

    private ThreadLocal<EntityManager> thread = new ThreadLocal<>();
    private ReentrantLock lock = new ReentrantLock();

    protected PersistenceService(String unit, EntityManagerFactory emf, boolean managed,
```

### FieldMayBeFinal
Field `thread` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/server/PersistenceService.java`
#### Snippet
```java
    private final PersistenceContextType scope;

    private ThreadLocal<EntityManager> thread = new ThreadLocal<>();
    private ReentrantLock lock = new ReentrantLock();

```

### FieldMayBeFinal
Field `_statusBar` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private JTabbedPane _tabbedPane;
    private JTabbedPane _outputPane;
    private StatusBar   _statusBar;
    private ScrollingTextPane   _sqlLog;
    private SQLLogger _sqlListener;
```

### FieldMayBeFinal
Field `_navigator` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
     */
    private JToolBar    _toolBar;
    private JTree       _navigator;
    private JTabbedPane _tabbedPane;
    private JTabbedPane _outputPane;
```

### FieldMayBeFinal
Field `_viewConfig` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private Action _deliver;
    private Action _supply;
    private Action _viewConfig;
    private Action _viewDomain;
    private Action _viewSource;
```

### FieldMayBeFinal
Field `_viewQuery` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private Action _viewDomain;
    private Action _viewSource;
    private Action _viewQuery;

    /**
```

### FieldMayBeFinal
Field `_tabbedPane` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private JToolBar    _toolBar;
    private JTree       _navigator;
    private JTabbedPane _tabbedPane;
    private JTabbedPane _outputPane;
    private StatusBar   _statusBar;
```

### FieldMayBeFinal
Field `_outputPane` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private JTree       _navigator;
    private JTabbedPane _tabbedPane;
    private JTabbedPane _outputPane;
    private StatusBar   _statusBar;
    private ScrollingTextPane   _sqlLog;
```

### FieldMayBeFinal
Field `_deliver` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private Action _about;
    private Action _buyBook;
    private Action _deliver;
    private Action _supply;
    private Action _viewConfig;
```

### FieldMayBeFinal
Field `_viewDomain` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private Action _supply;
    private Action _viewConfig;
    private Action _viewDomain;
    private Action _viewSource;
    private Action _viewQuery;
```

### FieldMayBeFinal
Field `_config` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private OpenBookService     _service;
    private Customer            _customer;
    private Map<String, Object> _config;

    /**
```

### FieldMayBeFinal
Field `_about` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    */
    private Action _root;
    private Action _about;
    private Action _buyBook;
    private Action _deliver;
```

### FieldMayBeFinal
Field `_viewSource` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private Action _viewConfig;
    private Action _viewDomain;
    private Action _viewSource;
    private Action _viewQuery;

```

### FieldMayBeFinal
Field `_buyBook` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private Action _root;
    private Action _about;
    private Action _buyBook;
    private Action _deliver;
    private Action _supply;
```

### FieldMayBeFinal
Field `_supply` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private Action _buyBook;
    private Action _deliver;
    private Action _supply;
    private Action _viewConfig;
    private Action _viewDomain;
```

### FieldMayBeFinal
Field `_root` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
     * The actions invoked by this sample demonstration.
    */
    private Action _root;
    private Action _about;
    private Action _buyBook;
```

### FieldMayBeFinal
Field `_toolBar` may be 'final'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
     * The primary graphic widgets used to invoke and display the results of the actions.
     */
    private JToolBar    _toolBar;
    private JTree       _navigator;
    private JTabbedPane _tabbedPane;
```

### FieldMayBeFinal
Field `_params` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java
    private DBIdentifier _name;
    private List<Column> _cols = new ArrayList<>();
    private List<String> _params = new ArrayList<>();


```

### FieldMayBeFinal
Field `_sql` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java


    private List<String> _sql = new ArrayList<>();
    private final boolean _fromDatabase;

```

### FieldMayBeFinal
Field `_cols` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java
    private DBIdentifier _schema;
    private DBIdentifier _name;
    private List<Column> _cols = new ArrayList<>();
    private List<String> _params = new ArrayList<>();

```

### FieldMayBeFinal
Field `defaultBatchLimit` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    // batch limit
    private int defaultBatchLimit = 100;

    public OracleDictionary() {
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SybaseDictionary.java`
#### Snippet
```java
    extends AbstractSQLServerDictionary {

    private static Localizer _loc = Localizer.forPackage
        (SybaseDictionary.class);

```

### FieldMayBeFinal
Field `schemaCase` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLServerDictionary.java`
#### Snippet
```java
        Localizer.forPackage(SQLServerDictionary.class);

    private String schemaCase = SCHEMA_CASE_PRESERVE;
    /**
     * Flag whether to treat UNIQUEIDENTIFIER as VARBINARY or VARCHAR
```

### FieldMayBeFinal
Field `ALIASES` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/FetchModeValue.java`
#### Snippet
```java
    public static final String EAGER_PARALLEL = "parallel";

    private static String[] ALIASES = new String[]{
        EAGER_PARALLEL, String.valueOf(EagerFetchModes.EAGER_PARALLEL),
        EAGER_JOIN, String.valueOf(EagerFetchModes.EAGER_JOIN),
```

### FieldMayBeFinal
Field `_oneBuffer` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java
    private int violation_of_unique_index_or_constraint;

    private SQLBuffer _oneBuffer = new SQLBuffer(this).append("1");

    public HSQLDictionary() {
```

### FieldMayBeFinal
Field `defaultBatchLimit` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java


    private int defaultBatchLimit = 100;
    public boolean appendExtendedExceptionText = true;

```

### FieldMayBeFinal
Field `namingRules` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
    // Naming utility and naming rules
    private DBIdentifierUtil namingUtil = null;
    private Map<String, IdentifierRule> namingRules = new HashMap<>();
    private IdentifierRule defaultNamingRule = null;  // cached for performance

```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
    public static final String ACCESS_TYPE_PROPERTY = "property";

    private static Localizer _loc = Localizer.forPackage
        (ReverseMappingTool.class);

```

### FieldMayBeFinal
Field `_results` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java

    // object->queryresultmapping
    private Map<Object, QueryResultMapping> _results = new HashMap<>();
    private SchemaGroup _schema = null;
    private StrategyInstaller _installer = null;
```

### FieldMayBeFinal
Field `_indices` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
    private Map<DBIdentifier,List<Unique>> _uniques;

    private Map<DBIdentifier,List<Index>> _indices = new HashMap<>();
    /**
     * The described class name.
```

### FieldMayBeFinal
Field `_store` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
     */
    protected class CurrentSequenceRunnable implements Runnable {
        private JDBCStore _store;
        private ClassMapping _mapping;

```

### FieldMayBeFinal
Field `_mapping` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
    protected class CurrentSequenceRunnable implements Runnable {
        private JDBCStore _store;
        private ClassMapping _mapping;

        CurrentSequenceRunnable(JDBCStore store, ClassMapping mapping) {
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
 */
public class PreparedQueryImpl implements PreparedQuery {
    private static Localizer _loc =
        Localizer.forPackage(PreparedQueryImpl.class);

```

### FieldMayBeFinal
Field `_batchedRows` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java

    private String _batchedSql = null;
    private List<RowImpl> _batchedRows = new ArrayList<>();
    private int _batchLimit;
    private boolean _disableBatch = false;
```

### FieldMayBeFinal
Field `_lock` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderCacheImpl.java`
#### Snippet
```java
    private List<String> _exclusionPatterns;
    private QueryStatistics<ClassMapping> _stats;
    private ReentrantLock _lock = new ReentrantLock();
    private boolean _enableStats = false;

```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/NativeJDBCSeq.java`
#### Snippet
```java
    public static final String ACTION_GET = "get";

    private static Localizer _loc = Localizer.forPackage(NativeJDBCSeq.class);

    private JDBCConfiguration _conf = null;
```

### FieldMayBeFinal
Field `localContext` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java

    private final transient JDBCStore _store;
    private static ThreadLocalContext localContext = new ThreadLocalContext();

    /**
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java
	private Lock _readLock;
	private Log _log;
    private static Localizer _loc = Localizer.forPackage(PreparedQueryCacheImpl.class);

	public PreparedQueryCacheImpl() {
```

### FieldMayBeFinal
Field `STRONG` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java

        private static Localizer _loc = Localizer.forPackage(PreparedQueryCacheImpl.class);
        private static String STRONG = _loc.get("strong-exclusion").getMessage();
        private static String WEAK   = _loc.get("weak-exclusion").getMessage();

```

### FieldMayBeFinal
Field `_writeLock` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java
	private boolean _statsEnabled;

	private Lock _writeLock;
	private Lock _readLock;
	private Log _log;
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java
        private final String  _reason;

        private static Localizer _loc = Localizer.forPackage(PreparedQueryCacheImpl.class);
        private static String STRONG = _loc.get("strong-exclusion").getMessage();
        private static String WEAK   = _loc.get("weak-exclusion").getMessage();
```

### FieldMayBeFinal
Field `WEAK` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java
        private static Localizer _loc = Localizer.forPackage(PreparedQueryCacheImpl.class);
        private static String STRONG = _loc.get("strong-exclusion").getMessage();
        private static String WEAK   = _loc.get("weak-exclusion").getMessage();

        public ExclusionPattern(boolean strong, String pattern, String reason) {
```

### FieldMayBeFinal
Field `_readLock` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java

	private Lock _writeLock;
	private Lock _readLock;
	private Log _log;
    private static Localizer _loc = Localizer.forPackage(PreparedQueryCacheImpl.class);
```

### FieldMayBeFinal
Field `_type` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/MapKey.java`
#### Snippet
```java
    private ClassMetaData _meta = null;
    private Class _cast = null;
    private Class _type = null;

    /**
```

### FieldMayBeFinal
Field `_cast` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/MapKey.java`
#### Snippet
```java
    private final Val _key;
    private ClassMetaData _meta = null;
    private Class _cast = null;
    private Class _type = null;

```

### FieldMayBeFinal
Field `_cancelStatementPool` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
    // pool statements so that we can try to reuse rather than recreate
    private List<CancelPreparedStatement> _cancelPreparedStatementsPool = new ArrayList<>();
    private List<CancelStatement> _cancelStatementPool = new ArrayList<>();

    @Override
```

### FieldMayBeFinal
Field `_cancelPreparedStatementsPool` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java

    // pool statements so that we can try to reuse rather than recreate
    private List<CancelPreparedStatement> _cancelPreparedStatementsPool = new ArrayList<>();
    private List<CancelStatement> _cancelStatementPool = new ArrayList<>();

```

### FieldMayBeFinal
Field `_stmnts` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java

    // track the pending statements so we can cancel them
    private List<Statement> _stmnts = Collections.synchronizedList(new ArrayList<>());

    // pool statements so that we can try to reuse rather than recreate
```

### FieldMayBeFinal
Field `_cast` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/MapEntry.java`
#### Snippet
```java
    private final Val _val;
    private ClassMetaData _meta = null;
    private Class _cast = null;
    private Class _type = null;

```

### FieldMayBeFinal
Field `_type` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/MapEntry.java`
#### Snippet
```java
    private ClassMetaData _meta = null;
    private Class _cast = null;
    private Class _type = null;

    /**
```

### FieldMayBeFinal
Field `_ptype` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Lit.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private Object _val;
    private int _ptype;
    private boolean _isRaw;

```

### FieldMayBeFinal
Field `_ptype` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/TypeLit.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private Object _val;
    private int _ptype;

    /**
```

### FieldMayBeFinal
Field `DBCPPACKAGENAME` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
extends SimpleDriverDataSource implements Configurable, Closeable {

    private static String DBCPPACKAGENAME = "org.apache.commons.dbcp2";
    private static String DBCPBASICDATASOURCENAME = "org.apache.commons.dbcp2.BasicDataSource";
    private static Class<?> _dbcpClass;
```

### FieldMayBeFinal
Field `DBCPBASICDATASOURCENAME` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java

    private static String DBCPPACKAGENAME = "org.apache.commons.dbcp2";
    private static String DBCPBASICDATASOURCENAME = "org.apache.commons.dbcp2.BasicDataSource";
    private static Class<?> _dbcpClass;
    private static Boolean _dbcpAvail;
```

### FieldMayBeFinal
Field `normalizingRule` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DefaultIdentifierConfiguration.java`
#### Snippet
```java
public class DefaultIdentifierConfiguration implements IdentifierConfiguration {

    private DBIdentifierRule normalizingRule = new DBIdentifierRule();
    private Map<String, IdentifierRule> normalizingRules = new HashMap<>();
    private final String conversionKey = getLeadingDelimiter() + getIdentifierDelimiter() + getTrailingDelimiter();
```

### FieldMayBeFinal
Field `normalizingRules` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DefaultIdentifierConfiguration.java`
#### Snippet
```java

    private DBIdentifierRule normalizingRule = new DBIdentifierRule();
    private Map<String, IdentifierRule> normalizingRules = new HashMap<>();
    private final String conversionKey = getLeadingDelimiter() + getIdentifierDelimiter() + getTrailingDelimiter();

```

### FieldMayBeFinal
Field `normalizer` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
public class Normalizer {

    private static IdentifierUtil normalizer =
        new DBIdentifierUtilImpl(new DefaultIdentifierConfiguration());

```

### FieldMayBeFinal
Field `defaultRule` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/Normalizer.java`
#### Snippet
```java
        new DBIdentifierUtilImpl(new DefaultIdentifierConfiguration());

    private static IdentifierRule defaultRule;

    static {
```

### FieldMayBeFinal
Field `addresses` may be 'final'
in `openjpa-examples/simple/src/main/java/embeddables/User.java`
#### Snippet
```java
    @ElementCollection
    @CollectionTable(name = "user_addresses")
    private Set<Address> addresses = new HashSet<>();
    @Basic
    private String user_name;
```

### FieldMayBeFinal
Field `_compoundIdentifier` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    // can have multi-part names, such as {schema, table} and should be stored
    // as a QualifiedDBIdentifier.
    private static boolean _compoundIdentifier[] = new boolean[DBIdentifierType.values().length];

    static {
```

### FieldMayBeFinal
Field `_fk` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
    public static class FKMapKey {

        private ForeignKey _fk;

        public FKMapKey(ForeignKey fk) {
```

### FieldMayBeFinal
Field `_conf` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributedQueryImpl.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private final ReentrantSliceLock _lock;
    private DistributedConfiguration _conf;

    public DistributedQueryImpl(Broker broker, String language, StoreQuery storeQuery) {
```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/SliceInfo.java`
#### Snippet
```java
	private String[] _slices;

	private static transient Localizer _loc = Localizer.forPackage(SliceInfo.class);

    /**
```

### FieldMayBeFinal
Field `_slices` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/SliceInfo.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private final boolean  _isReplicated;
	private String[] _slices;

	private static transient Localizer _loc = Localizer.forPackage(SliceInfo.class);
```

### FieldMayBeFinal
Field `RNG` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributionPolicy.java`
#### Snippet
```java
	 */
	public static class Default implements DistributionPolicy {
		private static Random RNG = new Random();
        @Override
        public String distribute(Object pc, List<String> slices, Object ctx) {
```

### FieldMayBeFinal
Field `_parser` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedStoreQuery.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private List<StoreQuery> _queries = new ArrayList<>();
	private ExpressionParser _parser;

	public DistributedStoreQuery(JDBCStore store, ExpressionParser parser) {
```

### FieldMayBeFinal
Field `executors` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedStoreQuery.java`
#### Snippet
```java
			ExpressionStoreQuery.DataStoreExecutor {
        private static final long serialVersionUID = 1L;
        private List<Executor> executors = new ArrayList<>();
		private DistributedStoreQuery owner = null;

```

### FieldMayBeFinal
Field `_queries` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedStoreQuery.java`
#### Snippet
```java
class DistributedStoreQuery extends JDBCStoreQuery {
    private static final long serialVersionUID = 1L;
    private List<StoreQuery> _queries = new ArrayList<>();
	private ExpressionParser _parser;

```

### FieldMayBeFinal
Field `master` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedDataSource.java`
#### Snippet
```java
		Iterable<DataSource> {
	private List<DataSource> real = new ArrayList<>();
	private DataSource master;

	public DistributedDataSource(List<DataSource> dataSources) {
```

### FieldMayBeFinal
Field `executors` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
	public static class ParallelExecutor extends
			SQLStoreQuery.SQLExecutor {
		private List<Executor> executors = new ArrayList<>();
		private DistributedSQLStoreQuery owner = null;

```

### FieldMayBeFinal
Field `_queries` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
class DistributedSQLStoreQuery extends SQLStoreQuery {
    private static final long serialVersionUID = 1L;
    private List<StoreQuery> _queries = new ArrayList<>();

	public DistributedSQLStoreQuery(JDBCStore store) {
```

### FieldMayBeFinal
Field `_replicatedTypes` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
     */
    private static class ReplicatedTypeRepository {
        private Set<Class<?>> _replicatedTypes = new HashSet<>();
        private Set<Class<?>> _nonreplicatedTypes = new HashSet<>();

```

### FieldMayBeFinal
Field `_loc` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
    public static final String PREFIX_SLICE = ProductDerivation.PREFIX_SLICE + DOT;
    public static final String PREFIX_OPENJPA = "openjpa.";
    private static Localizer _loc = Localizer.forPackage(DistributedJDBCConfigurationImpl.class);

    /**
```

### FieldMayBeFinal
Field `_nonreplicatedTypes` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
    private static class ReplicatedTypeRepository {
        private Set<Class<?>> _replicatedTypes = new HashSet<>();
        private Set<Class<?>> _nonreplicatedTypes = new HashSet<>();


```

### FieldMayBeFinal
Field `_used` may be 'final'
in `openjpa-tools/openjpa-fetch-statistics/src/main/java/org/apache/openjpa/enhance/stats/FetchStatsCollector.java`
#### Snippet
```java
public final class FetchStatsCollector {
    // Fully qualified persistent field name -> number of access
    private static ConcurrentHashMap<String, AtomicInteger> _used = new ConcurrentHashMap<>();
    private static Set<String> _entities = new TreeSet<>();

```

### FieldMayBeFinal
Field `_entities` may be 'final'
in `openjpa-tools/openjpa-fetch-statistics/src/main/java/org/apache/openjpa/enhance/stats/FetchStatsCollector.java`
#### Snippet
```java
    // Fully qualified persistent field name -> number of access
    private static ConcurrentHashMap<String, AtomicInteger> _used = new ConcurrentHashMap<>();
    private static Set<String> _entities = new TreeSet<>();

    private static Log _log;
```

### FieldMayBeFinal
Field `comps` may be 'final'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedResultSet.java`
#### Snippet
```java
public class DistributedResultSet implements ResultSet {

	private LinkedList<ResultSet> comps = new LinkedList<>();
	private ResultSet current;
	private int cursor = -1;
```

### FieldMayBeFinal
Field `_constraints` may be 'final'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
    private boolean _XML = false;
    private boolean _isUni1MFK = false;
    private Set<Constraint> _constraints = new HashSet<>();

    /**
```

## RuleId[id=JavaReflectionInvocation]
### JavaReflectionInvocation
Argument is not assignable to 'java.lang.String'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingDataSource.java`
#### Snippet
```java
        try {
            Method setter = target.getClass().getMethod(method, new Class[]{String.class});
            setter.invoke(target, val);
            return true;
        } catch (Throwable t) {
```

### JavaReflectionInvocation
Array item is not assignable to 'short'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                            new Object[]{
                                    idx,
                                oraclePreparedStatementFormNvarcharField.get(null)
                            });
                } catch (Exception e) {
```

### JavaReflectionInvocation
Array item is not assignable to 'org.apache.openjpa.jdbc.conf.JDBCConfiguration'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/MappingRepositoryValue.java`
#### Snippet
```java
                    J2DoPrivHelper.getClassLoaderAction(type)));
            return cls.getConstructor(new Class[]{ JDBCConfiguration.class }).
                newInstance(new Object[]{ conf });
        } catch (RuntimeException e) {
            throw e;
```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
                return _trans.transform(cl, name, previousVersion, pd, bytes);
            }
            catch (IllegalClassFormatException e) {
                throw e;
            }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `se` is immediately rethrown
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/CFMetaDataParser.java`
#### Snippet
```java
                return startPackageElement(name, attrs);
            return startSystemElement(name, attrs);
        } catch (SAXException se) {
            throw se;
        } catch (NullPointerException npe) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `se` is immediately rethrown
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/CFMetaDataParser.java`
#### Snippet
```java
            else
                endSystemElement(name);
        } catch (SAXException se) {
            throw se;
        } catch (NullPointerException npe) {
```

## RuleId[id=UnnecessaryUnicodeEscape]
### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00C0` can be replaced with 'À'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00D6` can be replaced with 'Ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00D8` can be replaced with 'Ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00F6` can be replaced with 'ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00F8` can be replaced with 'ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3300` can be replaced with '㌀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u337F` can be replaced with '㍿'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3400` can be replaced with '㐀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3D2D` can be replaced with '㴭'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u4E00` can be replaced with '一'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\uF900` can be replaced with '豈'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( input.LA(1)=='$'||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0161` can be replaced with 'š'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\10\1\uffff\1\26\2\uffff\1\36\1\17\1\120\2\uffff\1\35";
    static final String DFA29_specialS =
        "\u0161\uffff}>";
    static final String[] DFA29_transitionS = {
            "\2\56\1\uffff\2\56\22\uffff\1\56\1\47\1\54\1\uffff\1\55\1\46"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0660` can be replaced with '٠'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0669` can be replaced with '٩'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u06F0` can be replaced with '۰'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u06F9` can be replaced with '۹'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0966` can be replaced with '०'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u096F` can be replaced with '९'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u09E6` can be replaced with '০'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u09EF` can be replaced with '৯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0A66` can be replaced with '੦'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0A6F` can be replaced with '੯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0AE6` can be replaced with '૦'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0AEF` can be replaced with '૯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0B66` can be replaced with '୦'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0B6F` can be replaced with '୯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0BE7` can be replaced with '௧'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0BEF` can be replaced with '௯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0C66` can be replaced with '౦'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0C6F` can be replaced with '౯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0CE6` can be replaced with '೦'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0CEF` can be replaced with '೯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0D66` can be replaced with '൦'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0D6F` can be replaced with '൯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0E50` can be replaced with '๐'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0E59` can be replaced with '๙'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0ED0` can be replaced with '໐'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0ED9` can be replaced with '໙'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1049` can be replaced with '၉'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            // Java.g:
            {
            if ( (input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='\u0660' && input.LA(1)<='\u0669')||(input.LA(1)>='\u06F0' && input.LA(1)<='\u06F9')||(input.LA(1)>='\u0966' && input.LA(1)<='\u096F')||(input.LA(1)>='\u09E6' && input.LA(1)<='\u09EF')||(input.LA(1)>='\u0A66' && input.LA(1)<='\u0A6F')||(input.LA(1)>='\u0AE6' && input.LA(1)<='\u0AEF')||(input.LA(1)>='\u0B66' && input.LA(1)<='\u0B6F')||(input.LA(1)>='\u0BE7' && input.LA(1)<='\u0BEF')||(input.LA(1)>='\u0C66' && input.LA(1)<='\u0C6F')||(input.LA(1)>='\u0CE6' && input.LA(1)<='\u0CEF')||(input.LA(1)>='\u0D66' && input.LA(1)<='\u0D6F')||(input.LA(1)>='\u0E50' && input.LA(1)<='\u0E59')||(input.LA(1)>='\u0ED0' && input.LA(1)<='\u0ED9')||(input.LA(1)>='\u1040' && input.LA(1)<='\u1049') ) {
                input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\30\1\55\1\26\1\55\1\1\1\55\1\40\1\4\1\24\1\55\1\21\1\37\3"+
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\30\1\55\1\26\1\55\1\1\1\55\1\40\1\4\1\24\1\55\1\21\1\37\3"+
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\30\1\55\1\26\1\55\1\1\1\55\1\40\1\4\1\24\1\55\1\21\1\37\3"+
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\30\1\55\1\26\1\55\1\1\1\55\1\40\1\4\1\24\1\55\1\21\1\37\3"+
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\17\1\44\1\20\1\50\101\uffff\27\55\1\uffff\37\55\1\uffff"+
            "\u1f08\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff"+
            "\u092e\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\57\20\uffff\1\61\2\uffff\1\60",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\174\1\173",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\174\1\173",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\176\20\uffff\1\177",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\176\20\uffff\1\177",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\176\20\uffff\1\177",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\176\20\uffff\1\177",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\176\20\uffff\1\177",
            "\1\u0080",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\176\20\uffff\1\177",
            "\1\u0080",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\176\20\uffff\1\177",
            "\1\u0080",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u009f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\24"+
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\24"+
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\24"+
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\24"+
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\24"+
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
            "\1\u00a3",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
            "\1\u00a3",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
            "\1\u00a3",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u00a0\5\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
            "\1\u00a3",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a2` can be replaced with '¢'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
            "\1\u00a3",
            "\1\u00a4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a3` can be replaced with '£'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00a2",
            "\1\u00a3",
            "\1\u00a4",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a4` can be replaced with '¤'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00a2",
            "\1\u00a3",
            "\1\u00a4",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a6` can be replaced with '¦'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\74\1\uffff\12\165\12\uffff\3\74\35\uffff\3\74",
            "\1\u00a6",
            "\1\u00a7",
            "\1\u00a8",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a7` can be replaced with '§'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\74\1\uffff\12\165\12\uffff\3\74\35\uffff\3\74",
            "\1\u00a6",
            "\1\u00a7",
            "\1\u00a8",
            "\1\u00a9",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a8` can be replaced with '¨'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00a6",
            "\1\u00a7",
            "\1\u00a8",
            "\1\u00a9",
            "\1\u00ab\2\uffff\1\u00aa",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a9` can be replaced with '©'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00a7",
            "\1\u00a8",
            "\1\u00a9",
            "\1\u00ab\2\uffff\1\u00aa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ab` can be replaced with '\<\<'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00a8",
            "\1\u00a9",
            "\1\u00ab\2\uffff\1\u00aa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00aa` can be replaced with 'ª'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00a8",
            "\1\u00a9",
            "\1\u00ab\2\uffff\1\u00aa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ac` can be replaced with '¬'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00ab\2\uffff\1\u00aa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00ab\2\uffff\1\u00aa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\4"+
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u00ac\25\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ae` can be replaced with '®'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ae",
            "",
            "\1\u00af",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00af` can be replaced with '¯'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00ae",
            "",
            "\1\u00af",
            "\1\u00b0",
            "\1\u00b1",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b0` can be replaced with '°'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u00af",
            "\1\u00b0",
            "\1\u00b1",
            "\1\u00b2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b1` can be replaced with '±'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00af",
            "\1\u00b0",
            "\1\u00b1",
            "\1\u00b2",
            "\1\u00b3",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b2` can be replaced with '²'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b0",
            "\1\u00b1",
            "\1\u00b2",
            "\1\u00b3",
            "\1\u00b4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b3` can be replaced with '³'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b1",
            "\1\u00b2",
            "\1\u00b3",
            "\1\u00b4",
            "\1\u00b5",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b4` can be replaced with '´'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b2",
            "\1\u00b3",
            "\1\u00b4",
            "\1\u00b5",
            "\1\u00b6",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b5` can be replaced with 'µ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b3",
            "\1\u00b4",
            "\1\u00b5",
            "\1\u00b6",
            "\1\u00b7",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b6` can be replaced with '¶'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b4",
            "\1\u00b5",
            "\1\u00b6",
            "\1\u00b7",
            "\1\u00b8",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b7` can be replaced with '·'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b5",
            "\1\u00b6",
            "\1\u00b7",
            "\1\u00b8",
            "\1\u00b9",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b8` can be replaced with '¸'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b6",
            "\1\u00b7",
            "\1\u00b8",
            "\1\u00b9",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b9` can be replaced with '¹'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b7",
            "\1\u00b8",
            "\1\u00b9",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b9",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00b9",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
            "\1\u00bc",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
            "\1\u00bc",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
            "\1\u00bc",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00bb` can be replaced with '\>\>'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
            "\1\u00bc",
            "\1\u00bd",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00bc` can be replaced with '¼'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00bb",
            "\1\u00bc",
            "\1\u00bd",
            "\1\u00be",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00bd` can be replaced with '½'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00bb",
            "\1\u00bc",
            "\1\u00bd",
            "\1\u00be",
            "\1\u00bf",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00be` can be replaced with '¾'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00bc",
            "\1\u00bd",
            "\1\u00be",
            "\1\u00bf",
            "\1\u00c0",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00bf` can be replaced with '¿'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00bd",
            "\1\u00be",
            "\1\u00bf",
            "\1\u00c0",
            "\1\u00c1",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c0` can be replaced with 'À'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00be",
            "\1\u00bf",
            "\1\u00c0",
            "\1\u00c1",
            "\1\u00c2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c1` can be replaced with 'Á'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00bf",
            "\1\u00c0",
            "\1\u00c1",
            "\1\u00c2",
            "\1\u00c3",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c2` can be replaced with 'Â'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c0",
            "\1\u00c1",
            "\1\u00c2",
            "\1\u00c3",
            "\1\u00c4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c3` can be replaced with 'Ã'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c1",
            "\1\u00c2",
            "\1\u00c3",
            "\1\u00c4",
            "\1\u00c5",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c4` can be replaced with 'Ä'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c2",
            "\1\u00c3",
            "\1\u00c4",
            "\1\u00c5",
            "\1\u00c6",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c5` can be replaced with 'Å'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c3",
            "\1\u00c4",
            "\1\u00c5",
            "\1\u00c6",
            "\1\u00c7",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c6` can be replaced with 'Æ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c4",
            "\1\u00c5",
            "\1\u00c6",
            "\1\u00c7",
            "\1\u00c8",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c7` can be replaced with 'Ç'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c5",
            "\1\u00c6",
            "\1\u00c7",
            "\1\u00c8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c8` can be replaced with 'È'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c6",
            "\1\u00c7",
            "\1\u00c8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00c8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
            "\1\u00cb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
            "\1\u00cb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
            "\1\u00cb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ca` can be replaced with 'Ê'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
            "\1\u00cb",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00cb` can be replaced with 'Ë'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ca",
            "\1\u00cb",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00cb",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00cb",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
            "\1\u00ce",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
            "\1\u00ce",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
            "\1\u00ce",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00cd` can be replaced with 'Í'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
            "\1\u00ce",
            "\1\u00cf",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ce` can be replaced with 'Î'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00cd",
            "\1\u00ce",
            "\1\u00cf",
            "\1\u00d0",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00cf` can be replaced with 'Ï'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00cd",
            "\1\u00ce",
            "\1\u00cf",
            "\1\u00d0",
            "\1\u00d1",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d0` can be replaced with 'Ð'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00ce",
            "\1\u00cf",
            "\1\u00d0",
            "\1\u00d1",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d1` can be replaced with 'Ñ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00cf",
            "\1\u00d0",
            "\1\u00d1",
            "",
            "\1\u00d2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d2` can be replaced with 'Ò'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00d1",
            "",
            "\1\u00d2",
            "\1\u00d3",
            "\1\u00d4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d3` can be replaced with 'Ó'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u00d2",
            "\1\u00d3",
            "\1\u00d4",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d4` can be replaced with 'Ô'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00d2",
            "\1\u00d3",
            "\1\u00d4",
            "",
            "\1\u00d5",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d5` can be replaced with 'Õ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00d4",
            "",
            "\1\u00d5",
            "\1\u00d6",
            "\1\u00d7",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d6` can be replaced with 'Ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u00d5",
            "\1\u00d6",
            "\1\u00d7",
            "\1\u00d8",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d7` can be replaced with '×'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00d5",
            "\1\u00d6",
            "\1\u00d7",
            "\1\u00d8",
            "\1\u00d9",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d8` can be replaced with 'Ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00d6",
            "\1\u00d7",
            "\1\u00d8",
            "\1\u00d9",
            "\1\u00da",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00d9` can be replaced with 'Ù'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00d7",
            "\1\u00d8",
            "\1\u00d9",
            "\1\u00da",
            "\1\u00db",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00da` can be replaced with 'Ú'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00d8",
            "\1\u00d9",
            "\1\u00da",
            "\1\u00db",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00db` can be replaced with 'Û'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00d9",
            "\1\u00da",
            "\1\u00db",
            "",
            "\1\u00dc",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00dc` can be replaced with 'Ü'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00db",
            "",
            "\1\u00dc",
            "\1\u00dd",
            "\1\u00de",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00dd` can be replaced with 'Ý'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u00dc",
            "\1\u00dd",
            "\1\u00de",
            "\1\u00df",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00de` can be replaced with 'Þ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00dc",
            "\1\u00dd",
            "\1\u00de",
            "\1\u00df",
            "\1\u00e0",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00df` can be replaced with 'ß'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00dd",
            "\1\u00de",
            "\1\u00df",
            "\1\u00e0",
            "\1\u00e1",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e0` can be replaced with 'à'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00de",
            "\1\u00df",
            "\1\u00e0",
            "\1\u00e1",
            "\1\u00e2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e1` can be replaced with 'á'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00df",
            "\1\u00e0",
            "\1\u00e1",
            "\1\u00e2",
            "\1\u00e3",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e2` can be replaced with 'â'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e0",
            "\1\u00e1",
            "\1\u00e2",
            "\1\u00e3",
            "\1\u00e4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e3` can be replaced with 'ã'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e1",
            "\1\u00e2",
            "\1\u00e3",
            "\1\u00e4",
            "\1\u00e5",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e4` can be replaced with 'ä'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e2",
            "\1\u00e3",
            "\1\u00e4",
            "\1\u00e5",
            "\1\u00e6",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e5` can be replaced with 'å'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e3",
            "\1\u00e4",
            "\1\u00e5",
            "\1\u00e6",
            "\1\u00e7",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e6` can be replaced with 'æ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e4",
            "\1\u00e5",
            "\1\u00e6",
            "\1\u00e7",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e7` can be replaced with 'ç'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e5",
            "\1\u00e6",
            "\1\u00e7",
            "",
            "\1\u00e8",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e8` can be replaced with 'è'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e7",
            "",
            "\1\u00e8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00e8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
            "\1\u00eb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
            "\1\u00eb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
            "\1\u00eb",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ea` can be replaced with 'ê'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
            "\1\u00eb",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00eb` can be replaced with 'ë'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ea",
            "\1\u00eb",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00eb",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00eb",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ed",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ed",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ed",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ed",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ed",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ed",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ed",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ed` can be replaced with 'í'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00ed",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00ed",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00ed",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
            "\1\u00f2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
            "\1\u00f2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
            "\1\u00f2",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f1` can be replaced with 'ñ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
            "\1\u00f2",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f2` can be replaced with 'ò'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f1",
            "\1\u00f2",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00f2",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00f2",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f4",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f4",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f4",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f4",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f4` can be replaced with 'ô'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00f4",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00f4",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00f4",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u00f6",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u00f6",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u00f6",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f6` can be replaced with 'ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u00f6",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00f6",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00f6",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u00f8",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u00f8",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u00f8",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f8` can be replaced with 'ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u00f8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00f8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00f8",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fa",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fa",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fa",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fa",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00fa` can be replaced with 'ú'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00fa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00fa",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
            "\1\u00fd",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
            "\1\u00fd",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
            "\1\u00fd",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00fc` can be replaced with 'ü'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
            "\1\u00fd",
            "\1\u00fe",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00fd` can be replaced with 'ý'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u00fc",
            "\1\u00fd",
            "\1\u00fe",
            "\1\u00ff",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00fe` can be replaced with 'þ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00fc",
            "\1\u00fd",
            "\1\u00fe",
            "\1\u00ff",
            "\1\u0100",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ff` can be replaced with 'ÿ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00fd",
            "\1\u00fe",
            "\1\u00ff",
            "\1\u0100",
            "\1\u0101",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0100` can be replaced with 'Ā'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00fe",
            "\1\u00ff",
            "\1\u0100",
            "\1\u0101",
            "\1\u0102",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0101` can be replaced with 'ā'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u00ff",
            "\1\u0100",
            "\1\u0101",
            "\1\u0102",
            "\1\u0103",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0102` can be replaced with 'Ă'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0100",
            "\1\u0101",
            "\1\u0102",
            "\1\u0103",
            "\1\u0104",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0103` can be replaced with 'ă'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0101",
            "\1\u0102",
            "\1\u0103",
            "\1\u0104",
            "\1\u0105",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0104` can be replaced with 'Ą'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0102",
            "\1\u0103",
            "\1\u0104",
            "\1\u0105",
            "\1\u0106",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0105` can be replaced with 'ą'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0103",
            "\1\u0104",
            "\1\u0105",
            "\1\u0106",
            "\1\u0107",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0106` can be replaced with 'Ć'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0104",
            "\1\u0105",
            "\1\u0106",
            "\1\u0107",
            "\1\u0108",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0107` can be replaced with 'ć'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0105",
            "\1\u0106",
            "\1\u0107",
            "\1\u0108",
            "\1\u0109",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0108` can be replaced with 'Ĉ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0106",
            "\1\u0107",
            "\1\u0108",
            "\1\u0109",
            "\1\u010a",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0109` can be replaced with 'ĉ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0107",
            "\1\u0108",
            "\1\u0109",
            "\1\u010a",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u010a` can be replaced with 'Ċ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0108",
            "\1\u0109",
            "\1\u010a",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u010a",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u010a",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
            "\1\u010e",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
            "\1\u010e",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
            "\1\u010e",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u010d` can be replaced with 'č'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
            "\1\u010e",
            "\1\u010f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u010e` can be replaced with 'Ď'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u010d",
            "\1\u010e",
            "\1\u010f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\13"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u010f` can be replaced with 'ď'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u010d",
            "\1\u010e",
            "\1\u010f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\13"+
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0110` can be replaced with 'Đ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u010f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\13"+
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u010f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\13"+
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\13"+
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\13"+
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\13"+
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\13"+
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u0110\16\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0115",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0115",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0115",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0115` can be replaced with 'ĕ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0115",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0115",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0115",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0117",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0117",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0117",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0117` can be replaced with 'ė'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0117",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0118` can be replaced with 'Ę'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "",
            "\1\u0118",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\22"+
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0119` can be replaced with 'ę'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0118",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\22"+
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0118",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\22"+
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\22"+
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\22"+
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\22"+
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\22"+
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011b",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011b",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011b",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\1\u0119\7\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08"+
            "\55\u1040\uffff\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e"+
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011b",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u011b` can be replaced with 'ě'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\u10d2\uffff\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011b",
            "",
            "\1\u011c",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u011c` can be replaced with 'Ĝ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u011b",
            "",
            "\1\u011c",
            "",
            "\1\u011d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u011d` can be replaced with 'ĝ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u011c",
            "",
            "\1\u011d",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u011f` can be replaced with 'ğ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u011f",
            "\1\u0120",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0120` can be replaced with 'Ġ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u011f",
            "\1\u0120",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0120",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0120",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
            "\1\u0123",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
            "\1\u0123",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
            "\1\u0123",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0122` can be replaced with 'Ģ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
            "\1\u0123",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0123` can be replaced with 'ģ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0122",
            "\1\u0123",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0123",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0123",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
            "\1\u0126",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
            "\1\u0126",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
            "\1\u0126",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0125` can be replaced with 'ĥ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
            "\1\u0126",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0126` can be replaced with 'Ħ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0125",
            "\1\u0126",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0126",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0126",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
            "\1\u0129",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
            "\1\u0129",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
            "\1\u0129",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0128` can be replaced with 'Ĩ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
            "\1\u0129",
            "\1\u012a",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0129` can be replaced with 'ĩ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0128",
            "\1\u0129",
            "\1\u012a",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u012a` can be replaced with 'Ī'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0128",
            "\1\u0129",
            "\1\u012a",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u012a",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u012a",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
            "\1\u012d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
            "\1\u012d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
            "\1\u012d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u012c` can be replaced with 'Ĭ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
            "\1\u012d",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u012d` can be replaced with 'ĭ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012c",
            "\1\u012d",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u012f` can be replaced with 'į'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u012f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u012f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u012f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0131",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0131",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0131",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0131",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0131",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0131",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0131",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0131` can be replaced with 'ı'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0131",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0132` can be replaced with 'Ĳ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "",
            "\1\u0132",
            "",
            "\1\u0133",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0133` can be replaced with 'ĳ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0132",
            "",
            "\1\u0133",
            "\1\u0134",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0134` can be replaced with 'Ĵ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u0133",
            "\1\u0134",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0134",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0134",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0136",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0136",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0136",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0136` can be replaced with 'Ķ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u0136",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0136",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0136",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0138",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0138",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0138",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0138",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0138",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0138",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0138",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0138` can be replaced with 'ĸ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0138",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u013a",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u013a",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u013a",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u013a",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u013a",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u013a",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u013a",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u013a` can be replaced with 'ĺ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u013a",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u013d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u013d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u013d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u013d` can be replaced with 'Ľ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u013d",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u013d",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u013d",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u013f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u013f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u013f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u013f` can be replaced with 'Ŀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\u013f",
            "\1\u0140",
            "\1\u0141",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0140` can be replaced with 'ŀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u013f",
            "\1\u0140",
            "\1\u0141",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0141` can be replaced with 'Ł'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u013f",
            "\1\u0140",
            "\1\u0141",
            "",
            "\1\u0142",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0142` can be replaced with 'ł'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0141",
            "",
            "\1\u0142",
            "\1\u0143",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0143` can be replaced with 'Ń'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u0142",
            "\1\u0143",
            "",
            "\1\u0144",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0144` can be replaced with 'ń'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0143",
            "",
            "\1\u0144",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0146",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0146",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0146",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0146",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0146",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0146",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0146",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0146` can be replaced with 'ņ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0146",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0146",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0146",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0148",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0148",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0148",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0148",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0148",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0148",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0148",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0148` can be replaced with 'ň'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0148",
            "",
            "\1\u0149",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0149` can be replaced with 'ŉ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0148",
            "",
            "\1\u0149",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u014c` can be replaced with 'Ō'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "",
            "\1\u014c",
            "",
            "\1\u014d",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u014d` can be replaced with 'ō'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u014c",
            "",
            "\1\u014d",
            "\1\u014e",
            "\1\u014f",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u014e` can be replaced with 'Ŏ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\u014d",
            "\1\u014e",
            "\1\u014f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u014f` can be replaced with 'ŏ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u014d",
            "\1\u014e",
            "\1\u014f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u014f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u014f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0151",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0151",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0151",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0151",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0151",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0151",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0151",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0151` can be replaced with 'ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0151",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0151",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0151",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0155",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0155",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0155",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0155",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0155",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0155",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0155",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0155` can be replaced with 'ŕ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0155",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0157",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0157",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0157",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0157",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0157",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0157",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0157",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0157` can be replaced with 'ŗ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0157",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0157",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0157",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0159",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0159",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0159",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0159",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0159",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0159",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0159",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0159` can be replaced with 'ř'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u0159",
            "",
            "\1\u015a",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u015a` can be replaced with 'Ś'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u0159",
            "",
            "\1\u015a",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u015e",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u015e",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u015e",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u015e",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u015e",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u015e",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u015e",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u015e` can be replaced with 'Ş'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            "\1\u015e",
            "",
            "",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u015f` can be replaced with 'ş'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "",
            "",
            "\1\u015f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1f08` can be replaced with 'Ἀ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u015f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u1040` can be replaced with '၀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\u015f",
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            ""
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0170` can be replaced with 'Ű'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            ""
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u092e` can be replaced with 'म'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            ""
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u10d2` can be replaced with 'გ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\1\55\13\uffff\12\55\7\uffff\32\55\4\uffff\1\55\1\uffff\32"+
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            ""
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5200` can be replaced with '刀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            ""
    };
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u5900` can be replaced with '夀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            ""
    };
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0200` can be replaced with 'Ȁ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            "\55\105\uffff\27\55\1\uffff\37\55\1\uffff\u1f08\55\u1040\uffff"+
            "\u0150\55\u0170\uffff\u0080\55\u0080\uffff\u092e\55\u10d2\uffff"+
            "\u5200\55\u5900\uffff\u0200\55",
            ""
    };
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0161` can be replaced with 'š'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
    static final String DFA29_eofS =
        "\u0161\uffff";
    static final String DFA29_minS =
        "\1\11\1\141\1\uffff\1\146\1\150\1\56\1\75\1\142\2\141\1\154\3\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00C0` can be replaced with 'À'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00D6` can be replaced with 'Ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00D8` can be replaced with 'Ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00F6` can be replaced with 'ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00F8` can be replaced with 'ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3300` can be replaced with '㌀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u337F` can be replaced with '㍿'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3400` can be replaced with '㐀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3D2D` can be replaced with '㴭'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u4E00` can be replaced with '一'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\uF900` can be replaced with '豈'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                int LA25_0 = input.LA(1);

                if ( (LA25_0=='$'||(LA25_0>='0' && LA25_0<='9')||(LA25_0>='A' && LA25_0<='Z')||LA25_0=='_'||(LA25_0>='a' && LA25_0<='z')||(LA25_0>='\u00C0' && LA25_0<='\u00D6')||(LA25_0>='\u00D8' && LA25_0<='\u00F6')||(LA25_0>='\u00F8' && LA25_0<='\u1FFF')||(LA25_0>='\u3040' && LA25_0<='\u318F')||(LA25_0>='\u3300' && LA25_0<='\u337F')||(LA25_0>='\u3400' && LA25_0<='\u3D2D')||(LA25_0>='\u4E00' && LA25_0<='\u9FFF')||(LA25_0>='\uF900' && LA25_0<='\uFAFF')) ) {
                    alt25=1;
                }
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00C0` can be replaced with 'À'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00D6` can be replaced with 'Ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00D8` can be replaced with 'Ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00F6` can be replaced with 'ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00F8` can be replaced with 'ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3300` can be replaced with '㌀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u337F` can be replaced with '㍿'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3400` can be replaced with '㐀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3D2D` can be replaced with '㴭'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u4E00` can be replaced with '一'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\uF900` can be replaced with '豈'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	    // Java.g:
            	    {
            	    if ( input.LA(1)=='$'||(input.LA(1)>='0' && input.LA(1)<='9')||(input.LA(1)>='A' && input.LA(1)<='Z')||input.LA(1)=='_'||(input.LA(1)>='a' && input.LA(1)<='z')||(input.LA(1)>='\u00C0' && input.LA(1)<='\u00D6')||(input.LA(1)>='\u00D8' && input.LA(1)<='\u00F6')||(input.LA(1)>='\u00F8' && input.LA(1)<='\u1FFF')||(input.LA(1)>='\u3040' && input.LA(1)<='\u318F')||(input.LA(1)>='\u3300' && input.LA(1)<='\u337F')||(input.LA(1)>='\u3400' && input.LA(1)<='\u3D2D')||(input.LA(1)>='\u4E00' && input.LA(1)<='\u9FFF')||(input.LA(1)>='\uF900' && input.LA(1)<='\uFAFF') ) {
            	        input.consume();

```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a1` can be replaced with '¡'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\2\uffff\1\55\1\123\4\55\5\uffff\2\55\1\141\1\144\1\150\1\153"+
        "\1\155\1\157\1\161\1\uffff\2\164\4\uffff\5\55\1\175\5\55\5\uffff"+
        "\15\55\3\uffff\3\55\2\uffff\7\55\1\u00a1\3\55\24\uffff\1\u00a5\1"+
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a5` can be replaced with '¥'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\2\uffff\1\55\1\123\4\55\5\uffff\2\55\1\141\1\144\1\150\1\153"+
        "\1\155\1\157\1\161\1\uffff\2\164\4\uffff\5\55\1\175\5\55\5\uffff"+
        "\15\55\3\uffff\3\55\2\uffff\7\55\1\u00a1\3\55\24\uffff\1\u00a5\1"+
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ba` can be replaced with 'º'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\155\1\157\1\161\1\uffff\2\164\4\uffff\5\55\1\175\5\55\5\uffff"+
        "\15\55\3\uffff\3\55\2\uffff\7\55\1\u00a1\3\55\24\uffff\1\u00a5\1"+
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00c9` can be replaced with 'É'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\155\1\157\1\161\1\uffff\2\164\4\uffff\5\55\1\175\5\55\5\uffff"+
        "\15\55\3\uffff\3\55\2\uffff\7\55\1\u00a1\3\55\24\uffff\1\u00a5\1"+
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00cc` can be replaced with 'Ì'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\15\55\3\uffff\3\55\2\uffff\7\55\1\u00a1\3\55\24\uffff\1\u00a5\1"+
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00e9` can be replaced with 'é'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ec` can be replaced with 'ì'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ee` can be replaced with 'î'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00ef` can be replaced with 'ï'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f0` can be replaced with 'ð'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f3` can be replaced with 'ó'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\uffff\1\164\5\55\1\u00ad\1\55\1\uffff\13\55\1\u00ba\16\55\1\u00c9"+
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f5` can be replaced with 'õ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f7` can be replaced with '÷'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00f9` can be replaced with 'ù'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00fb` can be replaced with 'û'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\2\55\1\u00cc\5\55\1\uffff\3\55\1\uffff\7\55\1\uffff\14\55\1\uffff"+
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u010b` can be replaced with 'ċ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u010c` can be replaced with 'Č'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0111` can be replaced with 'đ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0112` can be replaced with 'Ē'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0113` can be replaced with 'ē'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0114` can be replaced with 'Ĕ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u00e9\2\55\1\u00ec\1\55\1\u00ee\1\u00ef\1\u00f0\2\55\1\u00f3"+
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0116` can be replaced with 'Ė'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u011a` can be replaced with 'Ě'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u00f5\1\uffff\1\55\1\u00f7\1\uffff\1\55\1\u00f9\1\55\1\u00fb"+
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u011e` can be replaced with 'Ğ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0121` can be replaced with 'ġ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0124` can be replaced with 'Ĥ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\17\55\1\u010b\1\u010c\3\55\1\u0111\1\u0112\1\u0113\1\u0114\1\uffff"+
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0127` can be replaced with 'ħ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u012b` can be replaced with 'ī'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u012e` can be replaced with 'Į'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0130` can be replaced with 'İ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\u0116\1\uffff\1\55\3\uffff\1\55\1\u011a\1\uffff\1\55\1\uffff"+
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0135` can be replaced with 'ĵ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0137` can be replaced with 'ķ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\55\1\uffff\1\55\1\uffff\1\u011e\1\uffff\2\55\1\u0121\2\55\1\u0124"+
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0139` can be replaced with 'Ĺ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u013b` can be replaced with 'Ļ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u013c` can be replaced with 'ļ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u013e` can be replaced with 'ľ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\2\55\1\u0127\3\55\1\u012b\2\55\2\uffff\1\u012e\1\55\1\u0130\1\55"+
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0145` can be replaced with 'Ņ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0147` can be replaced with 'Ň'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\4\uffff\1\55\1\uffff\2\55\1\u0135\1\uffff\1\55\1\u0137\1\55\1\uffff"+
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u014a` can be replaced with 'Ŋ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u014b` can be replaced with 'ŋ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\u0139\1\55\1\uffff\1\u013b\1\u013c\1\uffff\1\55\1\u013e\1\uffff"+
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0150` can be replaced with 'Ő'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0152` can be replaced with 'Œ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0153` can be replaced with 'œ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0154` can be replaced with 'Ŕ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\3\55\1\uffff\2\55\1\uffff\1\55\1\uffff\1\u0145\1\55\1\u0147\1\55"+
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0156` can be replaced with 'Ŗ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
    static final String DFA29_eofS =
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0158` can be replaced with 'Ř'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
    static final String DFA29_eofS =
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u015b` can be replaced with 'ś'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\1\uffff\1\55\1\uffff\1\u014a\1\uffff\1\u014b\2\uffff\1\55\1\uffff"+
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
    static final String DFA29_eofS =
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u015c` can be replaced with 'Ŝ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
    static final String DFA29_eofS =
        "\u0161\uffff";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u015d` can be replaced with 'ŝ'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
    static final String DFA29_eofS =
        "\u0161\uffff";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u0160` can be replaced with 'Š'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
        "\3\55\1\u0150\1\55\1\u0152\1\uffff\1\u0153\1\uffff\1\u0154\1\55"+
        "\2\uffff\1\u0156\1\55\1\u0158\1\55\1\uffff\1\55\3\uffff\1\u015b"+
        "\1\uffff\1\u015c\1\uffff\1\u015d\1\55\3\uffff\1\55\1\u0160\1\uffff";
    static final String DFA29_eofS =
        "\u0161\uffff";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00C0` can be replaced with 'À'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *  As a result, alternative(s) 4 were disabled for that input
 *  java.g:734:35: Decision can match input such as "{'$', 'A'..'Z',
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00D6` can be replaced with 'Ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *  As a result, alternative(s) 4 were disabled for that input
 *  java.g:734:35: Decision can match input such as "{'$', 'A'..'Z',
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00D8` can be replaced with 'Ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *  As a result, alternative(s) 4 were disabled for that input
 *  java.g:734:35: Decision can match input such as "{'$', 'A'..'Z',
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00F6` can be replaced with 'ö'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *  As a result, alternative(s) 4 were disabled for that input
 *  java.g:734:35: Decision can match input such as "{'$', 'A'..'Z',
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00F8` can be replaced with 'ø'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *  java.g:734:35: Decision can match input such as "{'$', 'A'..'Z',
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
 *    using multiple alternatives: 1, 2
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3300` can be replaced with '㌀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *  java.g:734:35: Decision can match input such as "{'$', 'A'..'Z',
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
 *    using multiple alternatives: 1, 2
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u337F` can be replaced with '㍿'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *  java.g:734:35: Decision can match input such as "{'$', 'A'..'Z',
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
 *    using multiple alternatives: 1, 2
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3400` can be replaced with '㐀'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
 *    using multiple alternatives: 1, 2
 *  As a result, alternative(s) 2 were disabled for that input
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u3D2D` can be replaced with '㴭'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
 *    using multiple alternatives: 1, 2
 *  As a result, alternative(s) 2 were disabled for that input
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u4E00` can be replaced with '一'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
 *    using multiple alternatives: 1, 2
 *  As a result, alternative(s) 2 were disabled for that input
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\uF900` can be replaced with '豈'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
 *    '_', 'a'..'z', '\u00C0'..'\u00D6', '\u00D8'..'\u00F6',
 *    '\u00F8'..'\u1FFF', '\u3040'..'\u318F', '\u3300'..'\u337F',
 *    '\u3400'..'\u3D2D', '\u4E00'..'\u9FFF', '\uF900'..'\uFAFF'}"
 *    using multiple alternatives: 1, 2
 *  As a result, alternative(s) 2 were disabled for that input
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00EE` can be replaced with 'î'
in `openjpa-examples/simple/src/main/java/mapping/BuildUTF8SchemaPC.java`
#### Snippet
```java

    @Basic
    @Column(name = "cha\u00EEne", length = 50)
    private String stringField = null;

```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `systemProperties`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
                    J2DoPrivHelper.getPropertiesAction());
            HashMap sysPropHM = null;
            synchronized(systemProperties) {
                // Prevent concurrent modification of systemProperties until HashMap ctor is completed.
                sysPropHM = new HashMap(systemProperties);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `o`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ImplHelper.java`
#### Snippet
```java

        // if we had a putIfAbsent() method, we wouldn't need to sync here
        synchronized (o) {
            PersistenceCapable pc = (PersistenceCapable)
                _unenhancedInstanceMap.get(o);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `map`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            Class<?> key, Class<?> value, boolean inheritance) {
        if (_locking) {
            synchronized (map) {
                addToCollectionInternal(map, key, value, inheritance);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `vals`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
        if (vals != null) {
            if (_locking) {
                synchronized (vals) {
                    mapped = getImplementorMetaDatasInternal(vals, envLoader, mustExist);
                }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `compCache`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
                if (comp.storeData != null) {

                    synchronized (compCache) {
                        Compilation existingComp = (Compilation) compCache.get(key);
                        if (existingComp == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `stat`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java

        // setup new sequence range
        synchronized (stat) {
            if (updateStatSeq && stat.seq < cur)
                stat.seq = cur;
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `stat`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
        while (true) {
            int available;
            synchronized (stat) {
                available = (int) (stat.max - stat.seq);
                if (available >= count)
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `stat`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java

        while (true) {
            synchronized (stat) {
                // make sure seq is at least 1, since autoassigned ids of 0 can
                // conflict with uninitialized values
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `meta` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java

        // find / create metadata
        XMLMetaData meta = getXMLMetaData(cls);

        return meta;
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
    @Override
    public Set<Parameter<?>> getParameters() {
        Set<Parameter<?>> result = new HashSet<>(getDeclaredParameters().values());
        return result;
    }
```

### UnnecessaryLocalVariable
Local variable `impl` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleFactory.java`
#### Snippet
```java
    @Override
    public TupleImpl newInstance() {
        TupleImpl impl = new TupleImpl(this);
        return impl;
    }
```

### UnnecessaryLocalVariable
Local variable `t` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
        compat.setCascadeWithDetach(true);
        try {
            T t = (T)_broker.detach(entity, this);
            return t;
        } finally {
```

### UnnecessaryLocalVariable
Local variable `exists` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        org.apache.openjpa.kernel.exps.Expression toKernelExpression(
            ExpressionFactory factory, CriteriaQueryImpl<?> q) {
            org.apache.openjpa.kernel.exps.Expression exists =
                factory.isNotEmpty(Expressions.toValue(e, factory, q));
            return exists;
```

### UnnecessaryLocalVariable
Local variable `contains` is redundant
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        public org.apache.openjpa.kernel.exps.Expression toKernelExpression(
            ExpressionFactory factory, CriteriaQueryImpl<?> q) {
            org.apache.openjpa.kernel.exps.Expression contains = factory.contains(
                Expressions.toValue(collection, factory, q),
                Expressions.toValue(element, factory, q));
```

### UnnecessaryLocalVariable
Local variable `ioe` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataParser.java`
#### Snippet
```java
                finish();
            } catch (SAXException se) {
                IOException ioe = new IOException(se.toString(), se);
                throw ioe;
            } finally {
```

### UnnecessaryLocalVariable
Local variable `frameworkClass` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/TemporaryClassLoader.java`
#### Snippet
```java
            if (isAnnotation(classBytes) || isEnum(classBytes)) {
                try {
                    Class<?> frameworkClass = Class.forName(name, resolve,
                            getClass().getClassLoader());
                    return frameworkClass;
```

### UnnecessaryLocalVariable
Local variable `pl` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
                return null;

            PropertyLine pl = new PropertyLine(l);
            return pl;
        }
```

### UnnecessaryLocalVariable
Local variable `id` is redundant
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
        }
        if (isDelimited(config, rule, name)) {
            String id = name.substring(leading.length(),
                (name.length() - trailing.length()));
            return id;
```

### UnnecessaryLocalVariable
Local variable `re` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WASRegistryManagedRuntime.java`
#### Snippet
```java
        }
        catch(Exception e ) {
            RuntimeException re = new RuntimeException(e.getMessage(), e);
            throw re;
        }
```

### UnnecessaryLocalVariable
Local variable `ise` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/CacheMarshallerImpl.java`
#### Snippet
```java
                _inputURL = (URL) list.get(0);
        } catch (IOException ioe) {
            IllegalStateException ise = new IllegalStateException(
                _loc.get("cache-marshaller-bad-url", getId(),
                    _inputResourceLocation).getMessage(), ioe);
```

### UnnecessaryLocalVariable
Local variable `fCode` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
    public int getAccessType() {
        if (AccessCode.isUnknown(_access)) {
        	int fCode = AccessCode.toFieldCode(getDeclaringMetaData()
        			.getAccessType());
        	return fCode;
```

### UnnecessaryLocalVariable
Local variable `ioe` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                }
            } catch (SecurityException e) {
                IOException ioe = new IOException(e.getMessage(), e);
                throw ioe;
            } catch (PrivilegedActionException pae) {
```

### UnnecessaryLocalVariable
Local variable `ioe` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                throw ioe;
            } catch (PrivilegedActionException pae) {
                IOException ioe = new IOException(
                    pae.getException().getMessage(), pae);
                throw ioe;
```

### UnnecessaryLocalVariable
Local variable `variable` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Context.java`
#### Snippet
```java
     */
    public Value getVariable(String alias) {
        Value variable = alias == null ? null
            : variables.get(alias.toLowerCase());
        return variable;
```

### UnnecessaryLocalVariable
Local variable `m0` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    public static Method getDeclaringMethod(Class c, Method m) {
        try {
            Method m0 = c.getMethod(m.getName(), m.getParameterTypes());
            return m0;
        } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `res` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ConcurrentQueryCache.java`
#### Snippet
```java
     */
    protected CacheMap newCacheMap() {
        CacheMap res = new CacheMap(_lru);

        return res;
```

### UnnecessaryLocalVariable
Local variable `res` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ConcurrentDataCache.java`
#### Snippet
```java
     */
    protected CacheMap newCacheMap() {
        CacheMap res = new CacheMap(_lru) {
            @Override
            protected void entryRemoved(Object key, Object value, boolean expired) {
```

### UnnecessaryLocalVariable
Local variable `val` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
        }
        Object row = data.get(rowIndex);
        Object val = getValueByReflection(rowIndex, row, columnIndex, method);
        return val;
    }
```

### UnnecessaryLocalVariable
Local variable `pane` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
        _stacktrace = new JTextPane();
        _stacktrace.setEditable(false);
        JScrollPane pane = new JScrollPane(_stacktrace,
                ScrollPaneConstants.VERTICAL_SCROLLBAR_AS_NEEDED,
                ScrollPaneConstants.HORIZONTAL_SCROLLBAR_AS_NEEDED);
```

### UnnecessaryLocalVariable
Local variable `label` is redundant
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/AttributeLegendView.java`
#### Snippet
```java
            for (int j = 0; j < height; j++)
                bimage.setRGB(i, j, c.getRGB());
        JLabel label = new JLabel(text, new ImageIcon(bimage), SwingConstants.LEADING);
        return label;
    }
```

### UnnecessaryLocalVariable
Local variable `fFi` is redundant
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                    // StateManager.settingXXX method.
                    loadManagedInstance(code, false);
                    final FieldInstruction fFi = fi;
                    code.getfield().setField(
                        AccessController.doPrivileged(J2DoPrivHelper
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__49() throws RecognitionException {
        try {
            int _type = T__49;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:32:7: ( ']' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__78() throws RecognitionException {
        try {
            int _type = T__78;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:61:7: ( 'for' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__84() throws RecognitionException {
        try {
            int _type = T__84;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:67:7: ( 'return' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mHexLiteral() throws RecognitionException {
        try {
            int _type = HexLiteral;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:909:12: ( '0' ( 'x' | 'X' ) ( HexDigit )+ ( IntegerTypeSuffix )? )
```

### UnnecessaryLocalVariable
Local variable `eee` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	default :
            	    if ( cnt1 >= 1 ) break loop1;
                        EarlyExitException eee =
                            new EarlyExitException(1, input);
                        throw eee;
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__38() throws RecognitionException {
        try {
            int _type = T__38;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:21:7: ( 'extends' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mWS() throws RecognitionException {
        try {
            int _type = WS;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:1011:5: ( ( ' ' | '\\r' | '\\t' | '\\u000C' | '\\n' ) )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__90() throws RecognitionException {
        try {
            int _type = T__90;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:73:7: ( '+=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__48() throws RecognitionException {
        try {
            int _type = T__48;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:31:7: ( '[' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__101() throws RecognitionException {
        try {
            int _type = T__101;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:84:8: ( '^' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__34() throws RecognitionException {
        try {
            int _type = T__34;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:17:7: ( 'abstract' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mCOMMENT() throws RecognitionException {
        try {
            int _type = COMMENT;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:1015:5: ( '/*' ( options {greedy=false; } : . )* '*/' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__88() throws RecognitionException {
        try {
            int _type = T__88;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:71:7: ( 'catch' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mDecimalLiteral() throws RecognitionException {
        try {
            int _type = DecimalLiteral;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:911:16: ( ( '0' | '1' .. '9' ( '0' .. '9' )* ) ( IntegerTypeSuffix )? )
```

### UnnecessaryLocalVariable
Local variable `nvae` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            }
            else {
                NoViableAltException nvae =
                    new NoViableAltException("", 4, 0, input);

```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__102() throws RecognitionException {
        try {
            int _type = T__102;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:85:8: ( '==' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__89() throws RecognitionException {
        try {
            int _type = T__89;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:72:7: ( 'case' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__94() throws RecognitionException {
        try {
            int _type = T__94;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:77:7: ( '&=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__105() throws RecognitionException {
        try {
            int _type = T__105;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:88:8: ( '+' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__72() throws RecognitionException {
        try {
            int _type = T__72;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:55:7: ( 'false' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__104() throws RecognitionException {
        try {
            int _type = T__104;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:87:8: ( 'instanceof' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__65() throws RecognitionException {
        try {
            int _type = T__65;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:48:7: ( 'super' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__56() throws RecognitionException {
        try {
            int _type = T__56;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:39:7: ( 'boolean' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__83() throws RecognitionException {
        try {
            int _type = T__83;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:66:7: ( 'switch' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__103() throws RecognitionException {
        try {
            int _type = T__103;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:86:8: ( '!=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__92() throws RecognitionException {
        try {
            int _type = T__92;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:75:7: ( '*=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__54() throws RecognitionException {
        try {
            int _type = T__54;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:37:7: ( 'transient' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__42() throws RecognitionException {
        try {
            int _type = T__42;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:25:7: ( '>' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__79() throws RecognitionException {
        try {
            int _type = T__79;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:62:7: ( 'while' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__77() throws RecognitionException {
        try {
            int _type = T__77;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:60:7: ( 'else' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__57() throws RecognitionException {
        try {
            int _type = T__57;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:40:7: ( 'char' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__98() throws RecognitionException {
        try {
            int _type = T__98;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:81:7: ( '||' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__46() throws RecognitionException {
        try {
            int _type = T__46;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:29:7: ( 'interface' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__109() throws RecognitionException {
        try {
            int _type = T__109;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:92:8: ( '++' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__80() throws RecognitionException {
        try {
            int _type = T__80;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:63:7: ( 'do' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__69() throws RecognitionException {
        try {
            int _type = T__69;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:52:7: ( 'this' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__67() throws RecognitionException {
        try {
            int _type = T__67;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:50:7: ( ')' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__43() throws RecognitionException {
        try {
            int _type = T__43;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:26:7: ( '&' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__35() throws RecognitionException {
        try {
            int _type = T__35;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:18:7: ( 'final' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__64() throws RecognitionException {
        try {
            int _type = T__64;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:47:7: ( '?' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__45() throws RecognitionException {
        try {
            int _type = T__45;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:28:7: ( '}' )
```

### UnnecessaryLocalVariable
Local variable `nvae` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                }
                else {
                    NoViableAltException nvae =
                        new NoViableAltException("", 24, 1, input);

```

### UnnecessaryLocalVariable
Local variable `nvae` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            }
            else {
                NoViableAltException nvae =
                    new NoViableAltException("", 24, 0, input);

```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__41() throws RecognitionException {
        try {
            int _type = T__41;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:24:7: ( ',' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mLINE_COMMENT() throws RecognitionException {
        try {
            int _type = LINE_COMMENT;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:1019:5: ( '//' (~ ( '\\n' | '\\r' ) )* ( '\\r' )? '\\n' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__91() throws RecognitionException {
        try {
            int _type = T__91;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:74:7: ( '-=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__60() throws RecognitionException {
        try {
            int _type = T__60;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:43:7: ( 'int' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__40() throws RecognitionException {
        try {
            int _type = T__40;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:23:7: ( '<' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__112() throws RecognitionException {
        try {
            int _type = T__112;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:95:8: ( '!' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__31() throws RecognitionException {
        try {
            int _type = T__31;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:14:7: ( 'public' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__63() throws RecognitionException {
        try {
            int _type = T__63;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:46:7: ( 'double' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__50() throws RecognitionException {
        try {
            int _type = T__50;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:33:7: ( 'throws' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__96() throws RecognitionException {
        try {
            int _type = T__96;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:79:7: ( '^=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__51() throws RecognitionException {
        try {
            int _type = T__51;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:34:7: ( '=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__95() throws RecognitionException {
        try {
            int _type = T__95;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:78:7: ( '|=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__97() throws RecognitionException {
        try {
            int _type = T__97;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:80:7: ( '%=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__29() throws RecognitionException {
        try {
            int _type = T__29;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:12:7: ( '.' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__99() throws RecognitionException {
        try {
            int _type = T__99;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:82:7: ( '&&' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__110() throws RecognitionException {
        try {
            int _type = T__110;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:93:8: ( '--' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__25() throws RecognitionException {
        try {
            int _type = T__25;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:8:7: ( 'package' )
```

### UnnecessaryLocalVariable
Local variable `nvae` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    break;
                default:
                    NoViableAltException nvae =
                        new NoViableAltException("", 23, 1, input);

```

### UnnecessaryLocalVariable
Local variable `nvae` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            }
            else {
                NoViableAltException nvae =
                    new NoViableAltException("", 23, 0, input);

```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__30() throws RecognitionException {
        try {
            int _type = T__30;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:13:7: ( '*' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__106() throws RecognitionException {
        try {
            int _type = T__106;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:89:8: ( '-' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__87() throws RecognitionException {
        try {
            int _type = T__87;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:70:7: ( 'continue' )
```

### UnnecessaryLocalVariable
Local variable `eee` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	default :
            	    if ( cnt20 >= 1 ) break loop20;
                        EarlyExitException eee =
                            new EarlyExitException(20, input);
                        throw eee;
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__74() throws RecognitionException {
        try {
            int _type = T__74;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:57:7: ( 'default' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__28() throws RecognitionException {
        try {
            int _type = T__28;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:11:7: ( 'static' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mFloatingPointLiteral() throws RecognitionException {
        try {
            int _type = FloatingPointLiteral;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:922:5: ( ( '0' .. '9' )+ '.' ( '0' .. '9' )* ( Exponent )? ( FloatTypeSuffix )? | '.' ( '0' .. '9' )+ ( Exponent )? ( FloatTypeSuffix )? | ( '0' .. '9' )+ Exponent ( FloatTypeSuffix )? | ( '0' .. '9' )+ FloatTypeSuffix )
```

### UnnecessaryLocalVariable
Local variable `eee` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    	default :
                    	    if ( cnt8 >= 1 ) break loop8;
                                EarlyExitException eee =
                                    new EarlyExitException(8, input);
                                throw eee;
```

### UnnecessaryLocalVariable
Local variable `eee` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    	default :
                    	    if ( cnt12 >= 1 ) break loop12;
                                EarlyExitException eee =
                                    new EarlyExitException(12, input);
                                throw eee;
```

### UnnecessaryLocalVariable
Local variable `eee` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    	default :
                    	    if ( cnt15 >= 1 ) break loop15;
                                EarlyExitException eee =
                                    new EarlyExitException(15, input);
                                throw eee;
```

### UnnecessaryLocalVariable
Local variable `eee` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    	default :
                    	    if ( cnt17 >= 1 ) break loop17;
                                EarlyExitException eee =
                                    new EarlyExitException(17, input);
                                throw eee;
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__58() throws RecognitionException {
        try {
            int _type = T__58;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:41:7: ( 'byte' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__36() throws RecognitionException {
        try {
            int _type = T__36;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:19:7: ( 'strictfp' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__37() throws RecognitionException {
        try {
            int _type = T__37;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:20:7: ( 'class' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mCharacterLiteral() throws RecognitionException {
        try {
            int _type = CharacterLiteral;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:935:5: ( '\\'' ( EscapeSequence | ~ ( '\\'' | '\\\\' ) ) '\\'' )
```

### UnnecessaryLocalVariable
Local variable `nvae` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            }
            else {
                NoViableAltException nvae =
                    new NoViableAltException("", 21, 0, input);

```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__93() throws RecognitionException {
        try {
            int _type = T__93;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:76:7: ( '/=' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__86() throws RecognitionException {
        try {
            int _type = T__86;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:69:7: ( 'break' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__62() throws RecognitionException {
        try {
            int _type = T__62;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:45:7: ( 'float' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__59() throws RecognitionException {
        try {
            int _type = T__59;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:42:7: ( 'short' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__100() throws RecognitionException {
        try {
            int _type = T__100;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:83:8: ( '|' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__26() throws RecognitionException {
        try {
            int _type = T__26;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:9:7: ( ';' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__113() throws RecognitionException {
        try {
            int _type = T__113;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:96:8: ( 'new' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__61() throws RecognitionException {
        try {
            int _type = T__61;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:44:7: ( 'long' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__82() throws RecognitionException {
        try {
            int _type = T__82;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:65:7: ( 'finally' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mOctalLiteral() throws RecognitionException {
        try {
            int _type = OctalLiteral;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:913:14: ( '0' ( '0' .. '7' )+ ( IntegerTypeSuffix )? )
```

### UnnecessaryLocalVariable
Local variable `eee` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	default :
            	    if ( cnt6 >= 1 ) break loop6;
                        EarlyExitException eee =
                            new EarlyExitException(6, input);
                        throw eee;
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__33() throws RecognitionException {
        try {
            int _type = T__33;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:16:7: ( 'private' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__81() throws RecognitionException {
        try {
            int _type = T__81;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:64:7: ( 'try' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mStringLiteral() throws RecognitionException {
        try {
            int _type = StringLiteral;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:939:5: ( '\"' ( EscapeSequence | ~ ( '\\\\' | '\"' ) )* '\"' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__39() throws RecognitionException {
        try {
            int _type = T__39;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:22:7: ( 'implements' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__76() throws RecognitionException {
        try {
            int _type = T__76;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:59:7: ( 'if' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__68() throws RecognitionException {
        try {
            int _type = T__68;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:51:7: ( '...' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__85() throws RecognitionException {
        try {
            int _type = T__85;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:68:7: ( 'throw' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mIdentifier() throws RecognitionException {
        try {
            int _type = Identifier;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:969:5: ( Letter ( Letter | JavaIDDigit )* )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__44() throws RecognitionException {
        try {
            int _type = T__44;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:27:7: ( '{' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__107() throws RecognitionException {
        try {
            int _type = T__107;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:90:8: ( '/' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__53() throws RecognitionException {
        try {
            int _type = T__53;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:36:7: ( 'synchronized' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__27() throws RecognitionException {
        try {
            int _type = T__27;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:10:7: ( 'import' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__108() throws RecognitionException {
        try {
            int _type = T__108;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:91:8: ( '%' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__111() throws RecognitionException {
        try {
            int _type = T__111;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:94:8: ( '~' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__73() throws RecognitionException {
        try {
            int _type = T__73;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:56:7: ( '@' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__55() throws RecognitionException {
        try {
            int _type = T__55;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:38:7: ( 'volatile' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__52() throws RecognitionException {
        try {
            int _type = T__52;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:35:7: ( 'native' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__32() throws RecognitionException {
        try {
            int _type = T__32;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:15:7: ( 'protected' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__66() throws RecognitionException {
        try {
            int _type = T__66;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:49:7: ( '(' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__47() throws RecognitionException {
        try {
            int _type = T__47;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:30:7: ( 'void' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__75() throws RecognitionException {
        try {
            int _type = T__75;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:58:7: ( ':' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__70() throws RecognitionException {
        try {
            int _type = T__70;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:53:7: ( 'null' )
```

### UnnecessaryLocalVariable
Local variable `_type` is redundant
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    public final void mT__71() throws RecognitionException {
        try {
            int _type = T__71;
            int _channel = DEFAULT_TOKEN_CHANNEL;
            // Java.g:54:7: ( 'true' )
```

### UnnecessaryLocalVariable
Local variable `retVal` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        if (DBIdentifier.isNull(fk.getIdentifier())) {
            DBIdentifier fkName = fk.loadIdentifierFromDB(this,conn);
            String[] retVal = (fkName == null) ?  new String[0] :
                new String[]{ "ALTER TABLE "
                + getFullName(fk.getTable(), false)
```

### UnnecessaryLocalVariable
Local variable `retVal` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MySQLDictionary.java`
#### Snippet
```java
        if (DBIdentifier.isNull(fk.getIdentifier())) {
            DBIdentifier fkName = fk.loadIdentifierFromDB(this,conn);
            String[] retVal = (fkName == null) ?  new String[0] :
                new String[]{ "ALTER TABLE "
                + getFullName(fk.getTable(), false)
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java

    private boolean validParamLiteralType(Class<?> type) {
        boolean ret = type == String.class
                || type == Integer.class
                || type == Character.class
```

### UnnecessaryLocalVariable
Local variable `cols` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java
        DBIdentifier schemaName, DBIdentifier tableName, DBIdentifier columnName, Connection conn)
        throws SQLException {
        Column[] cols = super.getColumns(meta, catalog, schemaName, tableName,
            columnName, conn);
        return cols;
```

### UnnecessaryLocalVariable
Local variable `hasJoinColumn` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java

    public boolean hasJoinColumn(FieldMapping field) {
        boolean hasJoinColumn = (field.getValueInfo().getColumns().size() > 0 ? true : false);
        return hasJoinColumn;
    }
```

### UnnecessaryLocalVariable
Local variable `hasJoinTable` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java

    public boolean hasJoinTable(FieldMapping field) {
        boolean hasJoinTable = !DBIdentifier.isNull(field.getMappingInfo().getTableIdentifier()) ? true : false;
        return hasJoinTable;
    }
```

### UnnecessaryLocalVariable
Local variable `dat` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MaxEmbeddedBlobFieldStrategy.java`
#### Snippet
```java
            return null;
        sm.setImplData(field.getIndex(), null);
        DBDictionary.SerializedData dat = new DBDictionary.SerializedData(b);
        return dat;
    }
```

### UnnecessaryLocalVariable
Local variable `count` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
                dict.setTimeouts(stmnt, fetch, true);

                int count = executeUpdate(store, conn, stmnt, buf);

                return count;
```

### UnnecessaryLocalVariable
Local variable `sqlEx` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
                }
                catch(NotSupportedException nse) {
                    SQLException sqlEx = new SQLException(
                            nse.getLocalizedMessage(), nse);
                    throw sqlEx;
```

### UnnecessaryLocalVariable
Local variable `re` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
                    current = cur;
            } catch (SQLException sqle) {
                RuntimeException re = new RuntimeException(sqle.getMessage(), sqle);
                throw re;
            } finally {
```

### UnnecessaryLocalVariable
Local variable `re` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
                    closeConnection(conn);
                }
                RuntimeException re = new RuntimeException(e.getMessage(), e);
                throw re;
            }
```

### UnnecessaryLocalVariable
Local variable `key` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/MapKey.java`
#### Snippet
```java
        throws SQLException {
        KeyExpState estate = (KeyExpState) state;
        Object key = _key.load(ctx, estate.key, res);
        return key;
    }
```

### UnnecessaryLocalVariable
Local variable `schema` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGroup.java`
#### Snippet
```java
            return null;
        DBIdentifier sName = DBIdentifier.toUpper(name);
        Schema schema = _schemaMap.get(sName);
        return schema;
    }
```

### UnnecessaryLocalVariable
Local variable `to` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
         if (base != _class) {
             ClassMapping from = _class;
             ClassMapping to = base;
             _type = OBJECT_PATH;
             for (; from != null && from != to; from = from.getJoinablePCSuperclassMapping()) {
```

### UnnecessaryLocalVariable
Local variable `seq` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java

        DBIdentifier sName = DBIdentifier.toUpper(name);
        Sequence seq = _seqMap.get(sName);
        return seq;
    }
```

### UnnecessaryLocalVariable
Local variable `schema` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
            rs = stmnt.executeQuery();
            rs.next();
            String schema = (_schemaColumn.getType() == Types.CLOB) ?
                dict.getClobString(rs, 1) : dict.getString(rs, 1);
            return schema;
```

### UnnecessaryLocalVariable
Local variable `id2` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
        DBIdentifierType id0 = DBIdentifierType.DEFAULT;
        DBIdentifierType id1 = DBIdentifierType.DEFAULT;
        DBIdentifierType id2 = baseId;
        if (baseId != DBIdentifierType.SCHEMA &&
            baseId != DBIdentifierType.COLUMN) {
```

### UnnecessaryLocalVariable
Local variable `id3` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
        DBIdentifierType id1 = DBIdentifierType.DEFAULT;
        DBIdentifierType id2 = DBIdentifierType.DEFAULT;
        DBIdentifierType id3 = baseId;
        if (baseId == DBIdentifierType.COLUMN) {
            id0 = DBIdentifierType.DATABASE;
```

### UnnecessaryLocalVariable
Local variable `id1` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
        DBIdentifier[] sNames = new DBIdentifier[2];
        DBIdentifierType id0 = DBIdentifierType.DEFAULT;
        DBIdentifierType id1 = baseId;
        if (baseId != DBIdentifierType.COLUMN &&
                baseId != DBIdentifierType.SCHEMA) {
```

### UnnecessaryLocalVariable
Local variable `totalCost` is redundant
in `openjpa-examples/simple/src/main/java/reversemapping/Main.java`
#### Snippet
```java
                + NumberFormat.getCurrencyInstance().format(totalFuelCost));

            double totalCost = totalFuelCost;

            print("  total profit: " + NumberFormat.getCurrencyInstance().
```

### UnnecessaryLocalVariable
Local variable `max` is redundant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierUtilImpl.java`
#### Snippet
```java
        DBIdentifier tname = col.getTableIdentifier();
        DBIdentifier cname = col.getIdentifier();
        int max = maxLen;
        int extraChars = -max + tname.getName().length() + 1 // <tname> + '_'
            + cname.getName().length() + 4; // <cname> + '_SEQ'
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
        if (dataSources.isEmpty())
            throw new UserException(_loc.get("no-slice"));
        DistributedDataSource result = new DistributedDataSource(dataSources);
        return result;
    }
```

## RuleId[id=UnpredictableBigDecimalConstructorCall]
### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
                    return val;
                if (val instanceof Number)
                    return new BigDecimal(((Number) val).doubleValue());
                if (val instanceof String)
                    return new BigDecimal(val.toString());
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
public class Filters {

    private static final BigDecimal ZERO_BIGDECIMAL = new BigDecimal(0D);
    private static final BigInteger ZERO_BIGINTEGER = new BigInteger("0");

```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/JavaSQLTypes.java`
#### Snippet
```java
    private static final Float ZERO_FLOAT = 0f;
    private static final Short ZERO_SHORT = (short) 0;
    private static final BigDecimal ZERO_BIGDECIMAL = new BigDecimal(0d);

    private static final Byte NONZERO_BYTE = (byte) 1;
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/JavaSQLTypes.java`
#### Snippet
```java
    private static final Short NONZERO_SHORT = (short) 1;
    private static final BigInteger NONZERO_BIGINTEGER = new BigInteger("1");
    private static final BigDecimal NONZERO_BIGDECIMAL = new BigDecimal(1d);

    /**
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ColumnVersionStrategy.java`
#### Snippet
```java
        if (v1.getClass() != v2.getClass()) {
            if (v1 instanceof Number && !(v1 instanceof BigDecimal))
                v1 = new BigDecimal(((Number) v1).doubleValue());

            if (v2 instanceof Number && !(v2 instanceof BigDecimal))
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ColumnVersionStrategy.java`
#### Snippet
```java

            if (v2 instanceof Number && !(v2 instanceof BigDecimal))
                v2 = new BigDecimal(((Number) v2).doubleValue());
        }

```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/JMSRemoteCommitProvider.java`
#### Snippet
```java
                    log.info(s_loc.get("jms-reconnect-fail", _topicName), e);
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException ie) {
                    break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
        while(!_broadcastThreads.isEmpty()) {
            try {
                Thread.sleep(500);
            } catch (InterruptedException ie) {
                // Ignore.
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ClearableScheduler.java`
#### Snippet
```java
        while (!isStopped()) {
            try {
                Thread.sleep(_interval * 60 * 1000);

                Date now = new Date();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
                    int pctComplete = (int)((100*(current - startTimeInMillis))/_total);
                    setProgress(pctComplete);
                    Thread.sleep(_interval);
                } catch (InterruptedException ignore) {
                }
```

## RuleId[id=ArraysAsListWithZeroOrOneArgument]
### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/DomainCommand.java`
#### Snippet
```java
 */
class DomainCommand extends AbstractCommand {
    private static final List<String> _validQualifiers = Arrays.asList("format");

    public DomainCommand(JPAServletContext ctx) {
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/FindCommand.java`
#### Snippet
```java
class FindCommand extends AbstractCommand {
    private static final String ARG_TYPE = "type";
    private static final List<String> _mandatoryArgs   = Arrays.asList(ARG_TYPE);
    private static final List<String> _validQualifiers = Arrays.asList("format", "plan");

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/QueryCommand.java`
#### Snippet
```java
    public static final String QUALIFIER_NAMED       = "named";
    public static final String QUALIFIER_SINGLE      = "single";
    private static final List<String> _mandatoryArgs   = Arrays.asList(ARG_QUERY);
    private static final List<String> _validQualifiers = Arrays.asList(
        QUALIFIER_FORMAT, QUALIFIER_PLAN, QUALIFIER_NAMED, QUALIFIER_SINGLE,
```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`c instanceof EntityTypeView == false` can be simplified to '!(c instanceof EntityTypeView)'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/MetamodelView.java`
#### Snippet
```java
        super.paint(g);
        for (Component c : getComponents()) {
            if (c instanceof EntityTypeView == false) {
                continue;
            }
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleFactory.java`
#### Snippet
```java
        List<TupleElement<?>> list = new ArrayList<>();
        for (Selection<?> s : elems)
            list.add(s);
        elements = Collections.unmodifiableList(list);
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
        HashSet<T> result = new HashSet<>();
        for (T t : coll)
            result.add(t);
        return result;
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinitionImpl.java`
#### Snippet
```java
		_distinct = isDistinct;
		for (SelectItem item : items)
			_projections.add(item);
		return this;
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinitionImpl.java`
#### Snippet
```java
		}
		for (PathExpression e : pathExprList)
			_groupBys.add(e);
		return this;
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinitionImpl.java`
#### Snippet
```java
		}
		for (PathExpression e : pathExprs)
			_groupBys.add(e);
		return this;
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        _orders = new ArrayList<>();
        for (Order o : orders) {
            _orders.add(o);
        }
        return this;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        _groups = new ArrayList<>();
        for (Expression<?> e : grouping) {
            _groups.add(e);
        }
        return this;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        _groups = new ArrayList<>();
        for (Expression<?> e : grouping) {
            _groups.add(e);
        }
        return this;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        _orders = new ArrayList<>();
        for (Order o : orders) {
            _orders.add(o);
        }
        return this;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/SimpleResultList.java`
#### Snippet
```java
        List list = new ArrayList();
        for (Object o : this) {
            list.add(o);
        }
        return list;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/RandomAccessResultList.java`
#### Snippet
```java
        ArrayList list = new ArrayList();
        for (Object o : this) {
            list.add(o);
        }
        return list;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/WindowResultList.java`
#### Snippet
```java
        List list = new ArrayList();
        for (Object o : this) {
            list.add(o);
        }
        return list;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractReferenceMap.java`
#### Snippet
```java
            final List<V> list = new ArrayList<>(size());
            for (final V value : this) {
                list.add(value);
            }
            return list.toArray(arr);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractReferenceMap.java`
#### Snippet
```java
            final List<K> list = new ArrayList<>(size());
            for (final K key : this) {
                list.add(key);
            }
            return list.toArray(arr);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
        ensureCapacity(calculateNewCapacity(newSize));
        for (final Entry<? extends K, ? extends V> entry: map.entrySet()) {
            put(entry.getKey(), entry.getValue());
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
        List<StateManagerImpl> copy = new ArrayList<>(size);
        for (StateManagerImpl sm : _main.values())
            copy.add(sm);
        if (_conflicts != null && !_conflicts.isEmpty())
            for (StateManagerImpl sm : _conflicts.values())
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
        if (_conflicts != null && !_conflicts.isEmpty())
            for (StateManagerImpl sm : _conflicts.values())
                copy.add(sm);
        if (_news != null && !_news.isEmpty())
        	 for (StateManagerImpl sm : _news.values())
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
        if (_news != null && !_news.isEmpty())
        	 for (StateManagerImpl sm : _news.values())
                 copy.add(sm);
        if (_embeds != null && !_embeds.isEmpty())
        	for (StateManagerImpl sm : _embeds)
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
        if (_embeds != null && !_embeds.isEmpty())
        	for (StateManagerImpl sm : _embeds)
                copy.add(sm);
        return copy;
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java

            for (Entry<Object, Object> entry : ((Map<Object, Object>) orig).entrySet()) {
                map.put(entry.getKey(), entry.getValue());
            }
            return map;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if (_dirty != null)
                for (StateManagerImpl stateManager : _dirty) {
                    copy.add(stateManager);
                }
            if (_clean != null)
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if (_clean != null)
                for (StateManagerImpl stateManager : _clean) {
                    copy.add(stateManager);
                }
            return copy;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/CacheStatisticsImpl.java`
#### Snippet
```java
        Map<String, long[]> res = new HashMap<>();
        for(Entry<String, long[]> s  : stats.entrySet()){
            res.put(s.getKey(), s.getValue());
        }
        return res;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
            Set<PluralAttribute<? super T, ?, ?>> pAttrs = entityType.getPluralAttributes();
            for (PluralAttribute<? super T, ?, ?> attr : pAttrs) {
                attributes.add(attr);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                    }
                    for (Column fieldCol : fieldCols) {
                        cols.add(fieldCol);
                    }
                }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
            pkCols = pk.getColumns();
            for (Column pkCol : pkCols) {
                cols.add(pkCol);
            }
            return cols;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java
            Table[] ts = schema.getTables();
            for (Table t : ts) {
                tables.add(t);
            }
        }
```

## RuleId[id=NewStringBufferWithCharArgument]
### NewStringBufferWithCharArgument
`new StringBuilder()` with argument of type 'char'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java

        public Annotation addArgument(String key, String[] vs) {
            StringBuilder tmp = new StringBuilder(BLOCK_DELIMITER.start);
            for (int i=0; i < vs.length; i++) {
                tmp.append(quote(vs[i]));
```

## RuleId[id=OverwrittenKey]
### OverwrittenKey
Duplicate Set element
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        _hints.add("openjpa.FetchPlan.ExtendedPathLookup");
        _hints.add("openjpa.FetchBatchSize");
        _hints.add("openjpa.FetchPlan.FetchBatchSize");
        _hints.add("openjpa.MaxFetchDepth");
        _hints.add("openjpa.FetchPlan.MaxFetchDepth");
```

### OverwrittenKey
Duplicate Set element
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        _hints.add("openjpa.FetchPlan.FetchBatchSize");
        _hints.add("openjpa.MaxFetchDepth");
        _hints.add("openjpa.FetchPlan.MaxFetchDepth");
        _hints.add("openjpa.LockTimeout");
        _hints.add("openjpa.FetchPlan.LockTimeout");
```

### OverwrittenKey
Duplicate Set element
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        _hints.add("openjpa.FetchPlan.MaxFetchDepth");
        _hints.add("openjpa.LockTimeout");
        _hints.add("openjpa.FetchPlan.LockTimeout");
        _hints.add("openjpa.QueryTimeout");
        _hints.add("openjpa.FetchPlan.QueryTimeout");
```

### OverwrittenKey
Duplicate Set element
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        _hints.add("openjpa.FetchPlan.LockTimeout");
        _hints.add("openjpa.QueryTimeout");
        _hints.add("openjpa.FetchPlan.QueryTimeout");
        _hints.add("openjpa.FlushBeforeQueries");
        _hints.add("openjpa.FetchPlan.FlushBeforeQueries");
```

### OverwrittenKey
Duplicate Set element
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        _hints.add("openjpa.WriteLockLevel");
        _hints.add("openjpa.FetchPlan.WriteLockLevel");
        _hints.add("openjpa.FetchPlan.FetchBatchSize");
        _hints.add("openjpa.FetchPlan.LockScope");
        _hints.add("openjpa.FetchPlan.LockTimeout");
```

### OverwrittenKey
Duplicate Set element
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        _hints.add("openjpa.FetchPlan.FetchBatchSize");
        _hints.add("openjpa.FetchPlan.LockScope");
        _hints.add("openjpa.FetchPlan.LockTimeout");
        _hints.add("openjpa.FetchPlan.MaxFetchDepth");
        _hints.add("openjpa.FetchPlan.QueryTimeout");
```

### OverwrittenKey
Duplicate Set element
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        _hints.add("openjpa.FetchPlan.LockScope");
        _hints.add("openjpa.FetchPlan.LockTimeout");
        _hints.add("openjpa.FetchPlan.MaxFetchDepth");
        _hints.add("openjpa.FetchPlan.QueryTimeout");
        _hints.add("openjpa.FetchPlan.ReadLockMode");
```

### OverwrittenKey
Duplicate Set element
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        _hints.add("openjpa.FetchPlan.LockTimeout");
        _hints.add("openjpa.FetchPlan.MaxFetchDepth");
        _hints.add("openjpa.FetchPlan.QueryTimeout");
        _hints.add("openjpa.FetchPlan.ReadLockMode");
        _hints.add("openjpa.FetchPlan.WriteLockMode");
```

## RuleId[id=AssignmentToCatchBlockParameter]
### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceListenerAdapter.java`
#### Snippet
```java
            } catch (Throwable t) {
                if (t instanceof InvocationTargetException)
                    t = t.getCause();
                if (t instanceof RuntimeException)
                    throw (RuntimeException) t;
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `re`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
        } catch (RuntimeException re) {
            try {
                re = _ret.translate(re);
            } catch (Exception e) {
                // ignore
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `re`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
        } catch (RuntimeException re) {
            try {
                re = _ret.translate(re);
            } catch (Exception e) {
                // ignore
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException) {
                e = ((PrivilegedActionException) e).getException();
            }
            RuntimeException re = new RuntimeException(_loc.get("obj-create", cls).getMessage(), e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
            } catch (Throwable t) {
                if (t instanceof PrivilegedActionException)
                    t = ((PrivilegedActionException) t).getException();
                _derivationErrors[i] = t;
                errors = true;
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            } catch (Throwable t) {
                if (t instanceof InvocationTargetException)
                    t = ((InvocationTargetException) t).getTargetException();
                if (errs == null) {
                    errs = new StringWriter();
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractMetaDataDefaults.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new UserException(e);
        }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/SequenceMetaData.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new MetaDataException(_loc.get("cant-init-seq", _name)).
                setCause(e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
        } catch (Throwable t) {
            if (t instanceof PrivilegedActionException)
                t = ((PrivilegedActionException) t).getException();
            throw new GeneralException(t);
        }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
        } catch (Throwable t) {
            if (t instanceof PrivilegedActionException)
                t = ((PrivilegedActionException) t).getException();
            throw new GeneralException(t);
        }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                    if (t instanceof Error)
                        throw (Error) t;
                    e = (Exception) t;

                    // allow null values to cause NPEs and illegal arg exceptions
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                    throw (OpenJPAException) e;
                if (e instanceof PrivilegedActionException)
                    e = ((PrivilegedActionException) e).getException();
                throw new MetaDataException(_loc.get("factory-err", this,
                    Exceptions.toString(val), e.toString())).setCause(e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/BeanLifecycleCallbacks.java`
#### Snippet
```java
        } catch (Throwable t) {
            if (t instanceof PrivilegedActionException)
                t = ((PrivilegedActionException) t).getException();
            throw new UserException(_loc.get("bean-constructor",
                cls.getName()), t);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
                } catch (Exception e) {
                    if (e instanceof PrivilegedActionException) {
                        e = ((PrivilegedActionException) e).getException();
                    }
                    if (!(e instanceof SocketException) || _isRunning) {
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
            } catch (Exception e) {
                if (e instanceof PrivilegedActionException)
                    e = ((PrivilegedActionException) e).getException();
                throw new GeneralException(e);
            }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            } catch (Exception e) {
                if (e instanceof PrivilegedActionException)
                    e = ((PrivilegedActionException) e).getException();
                cause = e;
            }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            } catch (Exception e) {
                if (e instanceof PrivilegedActionException)
                    e = ((PrivilegedActionException) e).getException();
                cause = e;
            }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            } catch (Exception e) {
                if (e instanceof PrivilegedActionException)
                    e = ((PrivilegedActionException) e).getException();
                cause = e;
            }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            } catch (Exception e) {
                if (e instanceof PrivilegedActionException)
                    e = ((PrivilegedActionException) e).getException();
                cause = e;
            }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        } catch (Throwable t) {
            if (t instanceof InvocationTargetException)
                t = ((InvocationTargetException) t).getTargetException();
            if (t instanceof SQLException)
                throw(SQLException) t;
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
            } catch (Throwable t) {
                if (t instanceof InvocationTargetException)
                    t = ((InvocationTargetException) t).getTargetException();
                if (t instanceof SQLException)
                    throw (SQLException) t;
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionaryFactory.java`
#### Snippet
```java
            } catch (Exception e) {
                if (e instanceof PrivilegedActionException)
                    e = ((PrivilegedActionException) e).getException();
                throw new UserException(e).setFatal(true);
            }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionaryFactory.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new UserException(e).setFatal(true);
        }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `t`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
        } catch (Throwable t) {
            if (t instanceof PrivilegedActionException)
                t = ((PrivilegedActionException) t).getException();
            throw new InternalException(_loc.get("importexport-instantiate"),t);
        }
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new MetaDataException(_loc.get("bad-value-handler",
                val, name), e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new MetaDataException(_loc.get("bad-cls-strategy",
                cls, name), e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new MetaDataException(_loc.get("bad-version-strategy",
                version.getClassMapping(), strat + ""), e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new MetaDataException(_loc.get("bad-field-strategy",
                field, name), e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new MetaDataException(_loc.get("bad-mapped-strategy",
                val, name), e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new MetaDataException(_loc.get("bad-discrim-strategy",
                discrim.getClassMapping(), name), e);
```

### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SimpleDriverDataSource.java`
#### Snippet
```java
                throw(RuntimeException) e;
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new StoreException(e);
        }
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `!=`, not 'equals()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java
    private void validateCfNameProps(OpenJPAConfiguration conf, String cfName, String cf2Name) {
        if (StringUtil.isNotEmpty(cfName) || StringUtil.isNotEmpty(cf2Name)) {
            if (conf.getDataCache() != "false" && conf.getDataCache() != null) {
                throw new ArgumentException(_loc.get("invalid-cfname-prop", new Object[] {
                    "openjpa.DataCache (L2 Cache)",
```

### StringEquality
String values are compared using `!=`, not 'equals()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java

            }
            if (conf.getQueryCache() != "false" && conf.getQueryCache() != null) {
                throw new ArgumentException(_loc.get("invalid-cfname-prop", new Object[] {
                    "openjpa.QueryCache",
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
        _query = new DelegatingQuery(query, ret);
        _lock = new ReentrantLock();
        if(query.getLanguage() == QueryLanguages.LANG_SQL) {
            _convertPositionalParams = false;
        }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
    public static boolean equalsIgnoreCase(String str1, String str2) {
        if (str1 == null || str2 == null) {
            return str1 == str2;
        }
        else if (str1 == str2) {
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
            return str1 == str2;
        }
        else if (str1 == str2) {
            return true;
        }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueMetaDataImpl.java`
#### Snippet
```java
    @Override
    public String getValueMappedBy() {
        if (_mappedBy == MAPPED_BY_PK) {
            // use this instead of getting meta from element b/c that
            // requires element to be resolved
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedStateManager.java`
#### Snippet
```java
        String next, int set) {
        accessingField(idx);
        if (cur == next || (cur != null && cur.equals(next))
                || !_loaded.get(idx))
            return;
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java

        // base classes might need to define a custom tokenizer
        if (superOidClass == null && getTokenizer(false) == TOKENIZER_CUSTOM)
            code.afterSection().append(getCustomTokenizerClass());

```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
                code.append("new ").append(toke).openParen(true).
                    append("str");
                if (toke == TOKENIZER_STD)
                    code.append(", \"").append(_token).append("\"");
                code.closeParen();
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderCacheImpl.java`
#### Snippet
```java
        try {
            String cls = mapping.getDescribedType().getName();
            boolean excludedByUser = _uncachables.get(cls) == EXLUDED_BY_USER;
            if (!excludedByUser)
                _uncachables.put(cls, reason);
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderCacheImpl.java`
#### Snippet
```java
        lock();
        try {
            boolean excludedByUser = _uncachables.get(cls) == EXLUDED_BY_USER;
            if (!excludedByUser)
                _uncachables.put(cls, reason);
```

## RuleId[id=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
Source parameter type 'java.lang.Object\[\]' is not assignable to destination parameter `array` of type 'T\[\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/UnmodifiableEntrySet.java`
#### Snippet
```java

        // copy back into input array to fulfill the method contract
        System.arraycopy(result, 0, array, 0, result.length);
        if (array.length > result.length) {
            array[result.length] = null;
```

### SuspiciousSystemArraycopy
`orig` is not of an array type
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                getComponentType(), length);

            System.arraycopy(orig, 0, array, 0, length);
            return array;
        } catch (Exception e) {
```

### SuspiciousSystemArraycopy
`array` is not of an array type
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                getComponentType(), length);

            System.arraycopy(orig, 0, array, 0, length);
            return array;
        } catch (Exception e) {
```

### SuspiciousSystemArraycopy
`array` is not of an array type
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
        public T fill(Object[] values, Class<?>[] types, String[] aliases) {
            Object array = java.lang.reflect.Array.newInstance(cls, values.length);
            System.arraycopy(values, 0, array, 0, values.length);
            return (T)array;
        }
```

### SuspiciousSystemArraycopy
`val` is not of an array type
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                    a = Array.newInstance(fmd.getElement().getDeclaredType(),
                        length);
                    System.arraycopy(val, 0, a, 0, length);
                } else {
                    Object[] data = new Object[length];
```

### SuspiciousSystemArraycopy
`a` is not of an array type
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                    a = Array.newInstance(fmd.getElement().getDeclaredType(),
                        length);
                    System.arraycopy(val, 0, a, 0, length);
                } else {
                    Object[] data = new Object[length];
```

### SuspiciousSystemArraycopy
`data` is not of an array type
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java

                if (isImmutableType(fmd.getElement())) {
                    System.arraycopy(data, 0, a, 0, length);
                } else {
                    for (int i = 0; i < length; i++) {
```

### SuspiciousSystemArraycopy
`a` is not of an array type
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java

                if (isImmutableType(fmd.getElement())) {
                    System.arraycopy(data, 0, a, 0, length);
                } else {
                    for (int i = 0; i < length; i++) {
```

### SuspiciousSystemArraycopy
`val` is not of an array type
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
                else {
                    cval = new Object[ecols.length];
                    System.arraycopy(val, idx, cval, 0, ecols.length);
                    idx += ecols.length;
                }
```

### SuspiciousSystemArraycopy
`cval` is not of an array type
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
                else {
                    cval = new Object[ecols.length];
                    System.arraycopy(val, idx, cval, 0, ecols.length);
                    idx += ecols.length;
                }
```

### SuspiciousSystemArraycopy
`cval` is not of an array type
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
            } else {
                Object rval = rvals.get(0);
                System.arraycopy(cval, 0, rval, idx, ecols.length);
                idx += ecols.length;
            }
```

### SuspiciousSystemArraycopy
`rval` is not of an array type
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
            } else {
                Object rval = rvals.get(0);
                System.arraycopy(cval, 0, rval, idx, ecols.length);
                idx += ecols.length;
            }
```

## RuleId[id=ExternalizableWithoutPublicNoArgConstructor]
### ExternalizableWithoutPublicNoArgConstructor
Externalizable class `MethodLifecycleCallbacks` has no 'public' no-arg constructor
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/MethodLifecycleCallbacks.java`
#### Snippet
```java
 * @author Steve Kim
 */
public class MethodLifecycleCallbacks
    implements LifecycleCallbacks, Externalizable {

```

### ExternalizableWithoutPublicNoArgConstructor
Externalizable class `BeanLifecycleCallbacks` has no 'public' no-arg constructor
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/BeanLifecycleCallbacks.java`
#### Snippet
```java
 * @author Steve Kim
 */
public class BeanLifecycleCallbacks
    extends MethodLifecycleCallbacks {

```

## RuleId[id=RedundantClassCall]
### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java
            if (emEmptyPropsProperties == null) {
                emEmptyPropsProperties = em.getProperties();
            } else if (EntityManagerImpl.class.isInstance(em)) {
                EntityManagerImpl.class.cast(em).setProperties(emEmptyPropsProperties);
            }
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java
                emEmptyPropsProperties = em.getProperties();
            } else if (EntityManagerImpl.class.isInstance(em)) {
                EntityManagerImpl.class.cast(em).setProperties(emEmptyPropsProperties);
            }
        }
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java

    private Object synchronizeMappings(final OpenJPAEntityManagerFactory factory) {
        if (EntityManagerFactoryImpl.class.isInstance(factory)) {
            final EntityManagerFactoryImpl entityManagerFactory = EntityManagerFactoryImpl.class.cast(factory);
            final BrokerFactory brokerFactory = entityManagerFactory.getBrokerFactory();
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
    private Object synchronizeMappings(final OpenJPAEntityManagerFactory factory) {
        if (EntityManagerFactoryImpl.class.isInstance(factory)) {
            final EntityManagerFactoryImpl entityManagerFactory = EntityManagerFactoryImpl.class.cast(factory);
            final BrokerFactory brokerFactory = entityManagerFactory.getBrokerFactory();
            if (!AbstractBrokerFactory.class.isInstance(brokerFactory)) {
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
            final EntityManagerFactoryImpl entityManagerFactory = EntityManagerFactoryImpl.class.cast(factory);
            final BrokerFactory brokerFactory = entityManagerFactory.getBrokerFactory();
            if (!AbstractBrokerFactory.class.isInstance(brokerFactory)) {
                throw new IllegalArgumentException("expected AbstractBrokerFactory but got " + brokerFactory);
            }
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
            } catch (final InvocationTargetException e) {
                final Throwable targetException = e.getTargetException();
                if (RuntimeException.class.isInstance(targetException)) {
                    throw RuntimeException.class.cast(targetException);
                }
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
                final Throwable targetException = e.getTargetException();
                if (RuntimeException.class.isInstance(targetException)) {
                    throw RuntimeException.class.cast(targetException);
                }
                throw new IllegalStateException(targetException);
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
        try {
            final Object obj = synchronizeMappings(factory);
            return Boolean.class.cast(obj) ? Boolean.class.cast(obj) : true;
        } finally {
            factory.close();
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
        try {
            final Object obj = synchronizeMappings(factory);
            return Boolean.class.cast(obj) ? Boolean.class.cast(obj) : true;
        } finally {
            factory.close();
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
    public <T> OpenJPAQuery<X> setParameter(Parameter<T> p, T arg1) {
        bindValue(p, arg1);
        if (BindableParameter.class.isInstance(p)) {
            BindableParameter.class.cast(p).setValue(arg1);
        }
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
        bindValue(p, arg1);
        if (BindableParameter.class.isInstance(p)) {
            BindableParameter.class.cast(p).setValue(arg1);
        }
        return this;
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java

    <X,Y> void validate(Field mField, Member<X, Y> member) {
        if (!ParameterizedType.class.isInstance(mField.getGenericType())) {
            throw new IllegalArgumentException(_loc.get("meta-bad-field",
                mField).getMessage());
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
    @Override
    public boolean afterSpecificationSet(Configuration c) {
        if (!OpenJPAConfigurationImpl.class.isInstance(c)
         && !SPEC_JPA.isSame(((OpenJPAConfiguration) c).getSpecification()))
            return false;
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
     */
    public static boolean isMethod(Element e) {
        return e != null && ExecutableElement.class.isInstance(e)
            && e.getKind() == ElementKind.METHOD;
    }
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
            Member member = _repos.getMetaDataFactory().getDefaults()
     	        .getMemberByProperty(meta, name, fldAccess, false);
            Class<?> type = Field.class.isInstance(member) ?
                ((Field)member).getType() : ((Method)member).getReturnType();

```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
    public StoredProcedureQuery createNamedStoredProcedureQuery(String name) {
        QueryMetaData meta = getQueryMetadata(name);
        if (!MultiQueryMetaData.class.isInstance(meta)) {
            throw new RuntimeException(name + " is not an identifier for a Stored Procedure Query");
        }
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
            if (cls.isAssignableFrom(Connection.class)) {
                Object o = getConnection();
                if(Connection.class.isInstance(o)){
                    return (T) o;
                }else{
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

                // array and Collection
                if (BindableParameter.class.isInstance(e2) && BindableParameter.class.cast(e2).value() != null &&
                    ((e2JavaType.isArray() && e2JavaType.getComponentType().equals(e1JavaType))
                    || (Class.class.isInstance(e2JavaType) ||
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

                // array and Collection
                if (BindableParameter.class.isInstance(e2) && BindableParameter.class.cast(e2).value() != null &&
                    ((e2JavaType.isArray() && e2JavaType.getComponentType().equals(e1JavaType))
                    || (Class.class.isInstance(e2JavaType) ||
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                if (BindableParameter.class.isInstance(e2) && BindableParameter.class.cast(e2).value() != null &&
                    ((e2JavaType.isArray() && e2JavaType.getComponentType().equals(e1JavaType))
                    || (Class.class.isInstance(e2JavaType) ||
                        (ParameterizedType.class.isInstance(e2JavaType)
                            && ParameterizedType.class.cast(e2JavaType).getActualTypeArguments().length > 0
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                            && e1JavaType.equals(ParameterizedType.class.cast(e2JavaType).getActualTypeArguments()[0]))
                       ))) {
                    final BindableParameter bp = BindableParameter.class.cast(e2);
                    final Object value = bp.value();

```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                            add(new Expressions.Equal(e1, Array.get(value, i)));
                        }
                    } else if (Collection.class.isInstance(value)) {
                        for (final Object item : Collection.class.cast(value)) {
                            add(new Expressions.Equal(e1, item));
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                        }
                    } else if (Collection.class.isInstance(value)) {
                        for (final Object item : Collection.class.cast(value)) {
                            add(new Expressions.Equal(e1, item));
                        }
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java
    private Method getBackingMember(FieldMetaData fmd) {
    	Member back = fmd.getBackingMember();
    	if (Method.class.isInstance(back))
    		return (Method)back;

```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/StoredProcedureQuery.java`
#### Snippet
```java
                CallableStatement stmnt = conn.prepareCall(_proc.getCallSQL());

                final StoredProcedureQuery spq = StoredProcedureQuery.class.cast(q);
                for (Column c : spq.getProcedure().getInColumns()) {
                    dict.setUnknown(stmnt, c.getIndex() + 1, params[c.getIndex()], c);
```

### RedundantClassCall
Redundant call to `cast()`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/StoredProcedureQuery.java`
#### Snippet
```java
            Object[] array = new Object[userParams.size()];
            int i = 0;
            StoredProcedureQuery storedProcedureQuery = StoredProcedureQuery.class.cast(q);
            for (final Column[] columns : asList(
                    storedProcedureQuery.getProcedure().getInColumns(),
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
            else {
                if (mapping.getDiscriminator() != null
                    && SuperclassDiscriminatorStrategy.class.isInstance(mapping.getDiscriminator().getStrategy())
                    && mapping.getMappingRepository().getConfiguration().getCompatibilityInstance()
                        .getSuperclassDiscriminatorStrategyByDefault()) {
```

### RedundantClassCall
Redundant call to `isInstance()`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
        String prefix = PREFIX_SLICE + DOT + name + DOT;
        for (Object key : newProps.keySet()) {
            if (!String.class.isInstance(key)
             && key.toString().startsWith(prefix))
                throw new UserException(_loc.get("slice-add-wrong-key", key));
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifierRule.java`
#### Snippet
```java
    public static final String DEFAULT_SQL_92 = "DefaultSQL92";

    public static final String SPECIAL_CHARS = " #$&%!?,.:;\"\'";

    public DBIdentifierRule() {
```

## RuleId[id=UnnecessaryLabelOnBreakStatement]
### UnnecessaryLabelOnBreakStatement
Unnecessary label `read` on break statement
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
                    ch = in.read();
                    if (ch == -1)
                        break read;
                }
                continue;
```

### UnnecessaryLabelOnBreakStatement
Unnecessary label `remove` on break statement
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/InverseManager.java`
#### Snippet
```java
                    for (int i = 0; coll.remove(val); i++)
                        if (i == 0 && coll instanceof Set)
                            break remove;
                    break;
                case ACTION_WARN:
```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
     */
    @Override
    public Object clone() {
        try {
            Constructor cons = getClass().getConstructor
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/CodeFormat.java`
#### Snippet
```java
     */
    @Override
    public Object clone() {
        CodeFormat format = new CodeFormat();
        format._tab = _tab;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java

        @Override
        protected Object clone() {
            // It is the callers responsibility to set the next field
            // correctly.
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java
     */
    @Override
    public final Object clone() {
        return new ConcurrentHashMap(this);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java

    @Override
    public Object clone() {
        try {
            return _fetch.clone();
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfigurationImpl.java`
#### Snippet
```java
     */
    @Override
    public Object clone() {
        FetchConfigurationImpl clone = newInstance(null);
        clone._state.ctx = _state.ctx;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
     */
    @Override
    public Object clone() {
        ObjectData data = new ObjectData(_oid, _meta);
        data.setVersion(_version);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
     */
    @Override
    public Object clone() {
        return new SQLBuffer(this);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/RowImpl.java`
#### Snippet
```java
     */
    @Override
    public Object clone() {
        RowImpl clone = newInstance(getColumns(), getAction());
        copyInto(clone, false);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
     */
    @Override
    public Object clone() {
        ReverseMappingTool tool = new ReverseMappingTool(_conf);
        tool.setSchemaGroup(getSchemaGroup());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/CollectionParam.java`
#### Snippet
```java

    @Override
    public CollectionParam clone() {
        CollectionParam c = new CollectionParam(this._key, this._type);
        c._idx = this._idx;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGroup.java`
#### Snippet
```java

    @Override
    public Object clone() {
        SchemaGroup clone = newInstance();
        clone.copy(this);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
     */
    @Override
    public QualifiedDBIdentifier clone() {
        QualifiedDBIdentifier sPath = new QualifiedDBIdentifier();
        sPath.setObjectTableName(getObjectTableName().clone());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
     */
    @Override
    public DBIdentifier clone() {
        DBIdentifier sName = new DBIdentifier();
        sName.setNameInternal(getNameInternal());
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
        try {
            if (Objects.equals(xmlAccessorValue.invoke(cls.getAnnotation(
                xmlAccessorTypeClass), new Object[]{}).toString(), "FIELD"))
                members = cls.getDeclaredFields();
            else
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleImpl.java`
#### Snippet
```java
    static {
        try {
            PUT = TupleImpl.class.getMethod("put", new Class[]{Integer.class, Object.class});
        } catch (Exception e) {
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java
            for (ClassLoader classLoader : ((MultiClassLoader) contextClassLoader).getClassLoaders()){
                try {
                    Method getClassPathMethod = classLoader.getClass().getMethod("getClasspath", new Class[]{});
                    classPath = (String) getClassPathMethod.invoke(classLoader, new Object[]{});
                    if (classPath != null)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        "java.util.TreeMap"});
    private static List<String> CLASSNAMES_COLLECTION = Arrays.asList(
        new String[]{
        "java.util.Collection", "java.util.AbstractCollection",
        "java.util.AbstractQueue", "java.util.Queue",
        "java.util.PriorityQueue"});

    /**
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        "java.util.SortedSet", "java.util.TreeSet"});
    private static List<String> CLASSNAMES_MAP = Arrays.asList(
        new String[]{
        "java.util.Map", "java.util.AbstractMap", "java.util.EnumMap",
        "java.util.HashMap",  "java.util.Hashtable",
        "java.util.IdentityHashMap",  "java.util.LinkedHashMap",
        "java.util.Properties", "java.util.SortedMap",
        "java.util.TreeMap"});
    private static List<String> CLASSNAMES_COLLECTION = Arrays.asList(
        new String[]{
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
     */
    private static List<String> CLASSNAMES_LIST = Arrays.asList(
        new String[]{
        "java.util.List", "java.util.AbstractList",
        "java.util.AbstractSequentialList", "java.util.ArrayList",
        "java.util.Stack", "java.util.Vector"});
    private static List<String> CLASSNAMES_SET = Arrays.asList(
        new String[]{
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        "java.util.Stack", "java.util.Vector"});
    private static List<String> CLASSNAMES_SET = Arrays.asList(
        new String[]{
        "java.util.Set", "java.util.AbstractSet", "java.util.EnumSet",
        "java.util.HashSet", "java.util.LinkedList", "java.util.LinkedHashSet",
        "java.util.SortedSet", "java.util.TreeSet"});
    private static List<String> CLASSNAMES_MAP = Arrays.asList(
        new String[]{
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/xml/Location.java`
#### Snippet
```java
            return _loc.get("no-locator").getMessage();
        }
        return MessageFormat.format(format, new Object []{
            _locator.getLineNumber(),
            _locator.getColumnNumber(), _locator.getPublicId(),
            _locator.getSystemId() });
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
            int anchorPosition = props.indexOf("#");
            if (anchorPosition > -1)
                return Arrays.asList(new String[] { props });
        }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingDataSource.java`
#### Snippet
```java
    private boolean setBeanProperty(Object target, String method, Object val) {
        try {
            Method setter = target.getClass().getMethod(method, new Class[]{String.class});
            setter.invoke(target, val);
            return true;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
        try {
            Constructor cons = getClass().getConstructor
                (new Class[]{ boolean.class });
            ConfigurationImpl clone = (ConfigurationImpl) cons.newInstance
                (new Object[]{ Boolean.FALSE });
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
                + StringUtil.capitalize(prop), (Class[]) null));
            pd.setWriteMethod(getClass().getMethod("set"
                + StringUtil.capitalize(prop), new Class[]
                { pd.getReadMethod().getReturnType() }));
        } catch (Throwable t) {
            // if an error occurs, it might be because the value is a
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Localizer.java`
#### Snippet
```java
    private static final Collection<ResourceBundleProvider> _providers =
        new CopyOnWriteArraySet<>
        (Arrays.asList(new ResourceBundleProvider[]{
            new SimpleResourceBundleProvider(),
            new StreamResourceBundleProvider(),
            new ZipResourceBundleProvider(), }));

    /**
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
        Exception err = null;
        try {
            Constructor<?> cons = type.getConstructor(new Class[]{ String.class });
            if (type == Boolean.class && "t".equalsIgnoreCase(str))
                str = "true";
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
            if (type == Boolean.class && "t".equalsIgnoreCase(str))
                str = "true";
            return cons.newInstance(new Object[]{ str });
        } catch (Exception e) {
            err = new ParseException(_loc.get("conf-no-constructor", str, type), e);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WLSManagedRuntime.java`
#### Snippet
```java
            transaction.getClass().
                getMethod("setRollbackOnly", new Class[] { Throwable.class }).
                    invoke(transaction, new Object[] { cause });
        } catch (Throwable e) {
            // some problem occurred: fall back to the traditional call
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        List<Column> cols = fm.getKeyMapping().getValueInfo().getColumns();
        if (cols.isEmpty()) {
            cols = Arrays.asList(new Column[]{ new Column() });
            fm.getKeyMapping().getValueInfo().setColumns(cols);
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        col.setFlag(Column.FLAG_UNINSERTABLE, !Boolean.parseBoolean(attrs.getValue("insertable")));
        col.setFlag(Column.FLAG_UNUPDATABLE, !Boolean.parseBoolean(attrs.getValue("updatable")));
        cm.getMappingInfo().setColumns(Arrays.asList(new Column[]{ col }));

        return true;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
                    break;
            }
            dinfo.setColumns(Arrays.asList(new Column[]{ _discCol }));
        }
        clearClassInfo();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/UpdateStrategies.java`
#### Snippet
```java
            return RESTRICT;

        List opts = Arrays.asList(new String[]{ "none", "ignore", "restrict" });
        String closest = StringDistance.getClosestLevenshteinDistance(val,
            opts, .5F);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
            if (!"true".equals(nullInd.getName()))
                col.setIdentifier(nullInd);
            info.setColumns(Arrays.asList(new Column[]{ col }));
        }
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
                String.valueOf(cols.size()), "1"));
        if (cols.isEmpty()) {
            cols = Arrays.asList(new Column[]{ new Column() });
            if (fm.isElementCollection()) {
                if (!fm.getElementMapping().getValueInfo().getColumns().isEmpty())
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        }
        cm.getDiscriminator().getMappingInfo().setColumns
            (Arrays.asList(new Column[]{ col }));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
                String.valueOf(cols.size()), "1"));
        if (cols.isEmpty()) {
            cols = Arrays.asList(new Column[]{ new Column() });
            fm.getKeyMapping().getValueInfo().setColumns(cols);
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
            col.setType(Types.BLOB);
        if (fmd.isElementCollection())
            ((FieldMapping) fmd).getElementMapping().getValueInfo().setColumns(Arrays.asList(new Column[]{ col }));
        else
            ((FieldMapping) fmd).getValueInfo().setColumns(Arrays.asList(new Column[]{ col }));
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
            ((FieldMapping) fmd).getElementMapping().getValueInfo().setColumns(Arrays.asList(new Column[]{ col }));
        else
            ((FieldMapping) fmd).getValueInfo().setColumns(Arrays.asList(new Column[]{ col }));

    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        col.setFlag(Column.FLAG_UNINSERTABLE, !id.insertable());
        col.setFlag(Column.FLAG_UNUPDATABLE, !id.updatable());
        cm.getMappingInfo().setColumns(Arrays.asList(new Column[]{ col }));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                if (Modifier.isStatic(externalizer.getModifiers())) {
                    if (externalizer.getParameterTypes().length == 1)
                        return externalizer.invoke(null, new Object[]{ val });
                    return externalizer.invoke(null, new Object[]{ val, ctx });
                }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                    if (externalizer.getParameterTypes().length == 1)
                        return externalizer.invoke(null, new Object[]{ val });
                    return externalizer.invoke(null, new Object[]{ val, ctx });
                }
                if (val == null)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                if (externalizer.getParameterTypes().length == 0)
                    return externalizer.invoke(val, (Object[]) null);
                return externalizer.invoke(val, new Object[]{ ctx });
            } catch (OpenJPAException ke) {
                throw ke;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                        return null;
                    return ((Constructor) factory).newInstance
                        (new Object[]{ val });
                }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                Method meth = (Method) factory;
                if (meth.getParameterTypes().length == 1)
                    return meth.invoke(null, new Object[]{ val });
                return meth.invoke(null, new Object[]{ val, ctx });
            } catch (Exception e) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                if (meth.getParameterTypes().length == 1)
                    return meth.invoke(null, new Object[]{ val });
                return meth.invoke(null, new Object[]{ val, ctx });
            } catch (Exception e) {
                // unwrap cause
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                    if (_factName == null)
                        _factMethod = getDeclaredType().getConstructor
                            (new Class[]{ getType() });
                    else
                    	_factMethod = findMethodByNameAndType(_factName, getType());
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        int utils = opts.removeIntProperty("utils", "u", 0);
        if (utils >= 4) {
            types.addAll(Arrays.asList(new String[] {
                java.sql.Date.class.getName(),
                java.sql.Time.class.getName(),
                java.sql.Timestamp.class.getName(),
                java.util.ArrayList.class.getName(),
                java.util.Date.class.getName(),
                java.util.GregorianCalendar.class.getName(),
                java.util.HashMap.class.getName(),
                java.util.HashSet.class.getName(),
                java.util.Hashtable.class.getName(),
                java.util.LinkedList.class.getName(),
                java.util.Properties.class.getName(),
                java.util.TreeMap.class.getName(),
                java.util.TreeSet.class.getName(),
                java.util.Vector.class.getName(),
            }));
        }
        if (utils >= 5) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        }
        if (utils >= 5) {
            types.addAll(Arrays.asList(new String[] {
                "java.util.EnumMap",
                "java.util.IdentityHashMap",
                "java.util.LinkedHashMap",
                "java.util.LinkedHashSet",
                "java.util.PriorityQueue",
            }));
        }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
    private static Constructor findComparatorConstructor(Class cls) {
        try {
            return cls.getConstructor(new Class[] { Comparator.class });
        } catch (NoSuchMethodException nsme) {
            return null;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/MethodLifecycleCallbacks.java`
#### Snippet
```java

        if (_arg)
            _callback.invoke(obj, new Object[]{ arg });
        else
            _callback.invoke(obj, (Object[]) null);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/BeanLifecycleCallbacks.java`
#### Snippet
```java
                callback, true));
        if (requiresArgument())
            callback.invoke(_listener, new Object[]{ obj, rel });
        else
            callback.invoke(_listener, new Object[]{ obj });
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/BeanLifecycleCallbacks.java`
#### Snippet
```java
            callback.invoke(_listener, new Object[]{ obj, rel });
        else
            callback.invoke(_listener, new Object[]{ obj });
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
        Method method;
        try {
            method = oid.getMethod("equals", new Class[]{ Object.class });
        } catch (Exception e) {
            throw new GeneralException(e).setFatal(true);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultPacker.java`
#### Snippet
```java
                if (_sets[i] instanceof Method) {
                    Method meth = (Method) _sets[i];
                    meth.invoke(user, new Object[]{ Filters.convert
                        (result[i], meth.getParameterTypes()[0]) });
                } else if (_sets[i] instanceof Field) {
                    Field field = (Field) _sets[i];
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultPacker.java`
#### Snippet
```java
                        field.getType()));
                } else if (_put != null) {
                    _put.invoke(user, new Object[]{ _aliases[i], result[i] });
                }
            }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
                    return Collections.EMPTY_LIST;
                // Collections.singletonList is JDK 1.3, so...
                return Arrays.asList(new Object[]{ single });
            }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
    static {
        _supportedPropertyNames = new HashSet<>();
        _supportedPropertyNames.addAll(Arrays.asList(new String[] {
                "AutoClear",
                "AutoDetach",
                "CacheFinderQuery",
                "CachePreparedQuery",
                "DetachedNew",
                "DetachState",
                "EvictFromDataCache",
                "IgnoreChanges",
                "LifecycleListenerCallbackMode",
                "Multithreaded",
                "NontransactionalRead",
                "NontransactionalWrite",
                "Optimistic",
                "PopulateDataCache",
                "RestoreState",
                "RetainState",
                }));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java
            return fmd.getDeclaringType().getDeclaredMethod(
                "set" + StringUtil.capitalize(fmd.getName()),
                new Class[]{ fmd.getDeclaredType() });
        }
        catch (NoSuchMethodException e) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java

        try {
            Constructor c = superclass.getDeclaredConstructor(new Class[0]);
            if (!(Modifier.isProtected(c.getModifiers())
                || Modifier.isPublic(c.getModifiers())))
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/InstrumentationFactory.java`
#### Snippet
```java
            Object vm =
                vmClass.getMethod("attach", new Class<?>[] { String.class })
                    .invoke(null, new Object[] { pid });
            // now deploy the actual agent, which will wind up calling
            // agentmain()
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/InstrumentationFactory.java`
#### Snippet
```java
            // agentmain()
            vmClass.getMethod("loadAgent", new Class[] { String.class })
                .invoke(vm, new Object[] { agentJar });
            vmClass.getMethod("detach", new Class[] {}).invoke(vm,
                new Object[] {});
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/InstrumentationFactory.java`
#### Snippet
```java
                .invoke(vm, new Object[] { agentJar });
            vmClass.getMethod("detach", new Class[] {}).invoke(vm,
                new Object[] {});
        } catch (Throwable t) {
            if (log.isTraceEnabled()) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
                Class<?> rtype = m.getReturnType();
                try {
                  Method setter = c.getMethod("set"+prop, new Class<?>[]{rtype});
                  if (setter.getReturnType() == void.class ||
                      setter.getReturnType().isAssignableFrom(c))
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        makeAccessible(setter, setter.getModifiers());
        try {
            setter.invoke(target, new Object[] { value });
        } catch (Throwable t) {
            throw wrapReflectionException(t, _loc.get("set-method", new Object[]{target, setter, value,
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java

    private static final Set _synchs = new HashSet(Arrays.asList
        (new String []{ "getData", "setData", "clearData", "getImplData",
            "setImplData", "setIntermediate", "getIntermediate",
            "isLoaded", "setLoaded", "setVersion", "getVersion", "store"
        }));

    public DataCachePCDataGenerator(OpenJPAConfiguration conf) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                    String idStr = XMLEncoder.decode(str.substring(idx + 1));
                    Constructor cons = idClass.getConstructor(ARGS);
                    return cons.newInstance(new Object[]{ idStr });

                case JavaTypes.LOCALE:
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        Class oidType = _meta.getObjectIdType();
        try {
            oidType.getConstructor(new Class[]{ Class.class, String.class });
            return Boolean.TRUE;
        } catch (Throwable t) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        }
        try {
            oidType.getConstructor(new Class[]{ String.class });
            return Boolean.FALSE;
        } catch (Throwable t) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        if (type.isPrimitive())
            name += StringUtil.capitalize(type.getName());
        return Reflection.class.getMethod(name, new Class[] { Object.class,
            argType });
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractSQLServerDictionary.java`
#### Snippet
```java

    public AbstractSQLServerDictionary() {
        reservedWordSet.addAll(Arrays.asList(new String[]{ "FILE", "INDEX" }));
        systemTableSet.add("DTPROPERTIES");
        validationSQL = "SELECT GETDATE()";
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractSQLServerDictionary.java`
#### Snippet
```java
        doubleTypeName = "FLOAT(32)";
        integerTypeName = "INT";
        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "IMAGE", "TEXT", "NTEXT", "MONEY", "SMALLMONEY", "INT",
            "DOUBLE PRECISION", "DATETIME", "SMALLDATETIME",
            "EXTENDED TYPE", "SYSNAME", "SQL_VARIANT", "INDEX",
            "DATETIME2",
        }));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
        bigintTypeName = "NUMERIC(32,0)";
        doubleTypeName = "DOUBLE PRECISION";
        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "BYTE", "DOUBLE PRECISION", "INTERVAL", "SMALLFLOAT", "TEXT",
            "INT8",
        }));

        // OpenJPA-2045: NAME has been removed from common reserved words to
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        timestampTypeName = "DATETIME";
        xmlTypeName = "TEXT";
        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "BOOL", "LONG VARBINARY", "MEDIUMBLOB", "LONGBLOB",
            "TINYBLOB", "LONG VARCHAR", "MEDIUMTEXT", "LONGTEXT", "TEXT",
            "TINYTEXT", "DOUBLE PRECISION", "ENUM", "SET", "DATETIME",
        }));
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "AUTO_INCREMENT", "BINARY", "BLOB", "CHANGE", "ENUM", "INFILE",
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
            "TINYTEXT", "DOUBLE PRECISION", "ENUM", "SET", "DATETIME",
        }));
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "AUTO_INCREMENT", "BINARY", "BLOB", "CHANGE", "ENUM", "INFILE",
            "INT1", "INT2", "INT4", "FLOAT1", "FLOAT2", "FLOAT4", "LOAD",
            "MEDIUMINT", "OUTFILE", "REPLACE", "STARTING", "TEXT", "UNSIGNED",
            "ZEROFILL", "INDEX",
        }));

        // reservedWordSet subset that CANNOT be used as valid column names
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
new String[] {
            "ADD", "ALL", "ALTER", "ANALYZE", "AND", "AS", "ASC", "ASENSITIVE", "BEFORE", "BETWEEN", "BIGINT", "BINARY",
            "BLOB", "BOTH", "BY", "CALL", "CASCADE", "CASE", "CHANGE", "CHAR", "CHARACTER", "CHECK", "COLLATE", "COLUMN", "CONDITION",
            "CONSTRAINT", "CONTINUE", "CONVERT", "CREATE", "CROSS", "CURRENT_DATE", "CURRENT_ROLE", "CURRENT_TIME", "CURRENT_TIMESTAMP",
            "CURRENT_USER", "CURSOR", "DATABASE", "DATABASES", "DAY_HOUR", "DAY_MICROSECOND", "DAY_MINUTE", "DAY_SECOND", "DEC",
            "DECIMAL", "DECLARE", "DEFAULT", "DELAYED", "DELETE", "DESC", "DESCRIBE", "DETERMINISTIC", "DISTINCT", "DISTINCTROW",
            "DIV", "DOUBLE", "DROP", "DUAL", "EACH", "ELSE", "ELSEIF", "ENCLOSED", "END-EXEC", "ESCAPED", "EXCEPT", "EXISTS",
            "EXIT", "EXPLAIN", "FALSE", "FETCH", "FLOAT", "FLOAT4", "FLOAT8", "FOR", "FORCE", "FOREIGN", "FROM", "FULLTEXT",
            "GRANT", "GROUP", "HAVING", "HIGH_PRIORITY", "HOUR_MICROSECOND", "HOUR_MINUTE", "HOUR_SECOND", "IF", "IGNORE",
            "IN", "INDEX", "INFILE", "INNER", "INOUT", "INSENSITIVE", "INSERT", "INT", "INT1", "INT2", "INT3", "INT4", "INT8",
            "INTEGER", "INTERSECT", "INTERVAL", "INTO", "IS", "ITERATE", "JOIN", "KEY", "KEYS", "KILL", "LEADING", "LEAVE",
            "LEFT", "LIKE", "LIMIT", "LINES", "LOAD", "LOCALTIME", "LOCALTIMESTAMP", "LOCK", "LONG", "LONGBLOB", "LONGTEXT",
            "LOOP", "LOW_PRIORITY", "MATCH", "MAXVALUE", "MEDIUMBLOB", "MEDIUMINT", "MEDIUMTEXT", "MIDDLEINT", "MINUTE_MICROSECOND",
            "MINUTE_SECOND", "MOD", "MODIFIES", "NATURAL", "NO_WRITE_TO_BINLOG", "NOT", "NULL", "NUMERIC", "ON", "OPTIMIZE",
            "OPTIONALLY", "OR", "ORDER", "OUT", "OUTER", "OUTFILE", "OVER", "PARTITION", "PRECISION", "PRIMARY", "PROCEDURE",
            "PURGE", "RANGE", "READ", "READS", "REAL", "RECURSIVE", "REFERENCES", "REGEXP", "RELEASE", "RENAME", "REPEAT",
            "REPLACE", "REQUIRE", "RESIGNAL", "RESTRICT", "RETURN", "REVOKE", "RIGHT", "RLIKE", "ROWS", "SCHEMA", "SCHEMAS",
            "SECOND_MICROSECOND", "SENSITIVE", "SEPARATOR", "SET", "SHOW", "SIGNAL", "SMALLINT", "SPATIAL", "SPECIFIC", "SQL",
            "SQL_BIG_RESULT", "SQL_CALC_FOUND_ROWS", "SQL_SMALL_RESULT", "SQLEXCEPTION", "SQLSTATE", "SQLWARNING", "SSL", "STARTING",
            "STRAIGHT_JOIN", "TABLE", "TERMINATED", "THEN", "TINYBLOB", "TINYINT", "TINYTEXT", "TO", "TRAILING", "TRIGGER",
            "TRUE", "UNDO", "UNION", "UNIQUE", "UNLOCK", "UNSIGNED", "UPDATE", "USAGE", "USE", "USING", "UTC_DATE", "UTC_TIME",
            "UTC_TIMESTAMP", "VALUES", "VARBINARY", "VARCHAR", "VARCHARACTER", "VARYING", "WHEN", "WHERE", "WHILE", "WITH",
            "WRITE", "XOR", "YEAR_MONTH", "ZEROFILL",
            // end generated.
            // the following keywords used to be defined as reserved words in the past, but now seem to work
            // we still add them for compat reasons
            "OPTION", "SELECT",
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
        searchStringEscape = "\\\\";

        typeModifierSet.addAll(Arrays.asList(new String[] { "UNSIGNED", "ZEROFILL" }));

        setLeadingDelimiter(DELIMITER_BACK_TICK);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MySQLDictionary.java`
#### Snippet
```java
        timestampTypeName = "DATETIME";
        xmlTypeName = "TEXT";
        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "BOOL", "LONG VARBINARY", "MEDIUMBLOB", "LONGBLOB",
            "TINYBLOB", "LONG VARCHAR", "MEDIUMTEXT", "LONGTEXT", "TEXT",
            "TINYTEXT", "DOUBLE PRECISION", "ENUM", "SET", "DATETIME",
        }));
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "AUTO_INCREMENT", "BINARY", "BLOB", "CHANGE", "ENUM", "INFILE",
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MySQLDictionary.java`
#### Snippet
```java
            "TINYTEXT", "DOUBLE PRECISION", "ENUM", "SET", "DATETIME",
        }));
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "AUTO_INCREMENT", "BINARY", "BLOB", "CHANGE", "ENUM", "INFILE",
            "INT1", "INT2", "INT4", "FLOAT1", "FLOAT2", "FLOAT4", "LOAD",
            "MEDIUMINT", "OUTFILE", "REPLACE", "STARTING", "TEXT", "UNSIGNED",
            "ZEROFILL", "INDEX",
        }));

        // reservedWordSet subset that CANNOT be used as valid column names
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MySQLDictionary.java`
#### Snippet
```java
new String[] {
            "ADD", "ALL", "ALTER", "ANALYZE", "AND", "AS", "ASC", "ASENSITIVE", "BEFORE", "BETWEEN", "BIGINT", "BINARY",
            "BLOB", "BOTH", "BY", "CALL", "CASCADE", "CASE", "CHANGE", "CHAR", "CHARACTER", "CHECK", "COLLATE", "COLUMN", "CONDITION",
            "CONSTRAINT", "CONTINUE", "CONVERT", "CREATE", "CROSS", "CURRENT_DATE", "CURRENT_TIME", "CURRENT_TIMESTAMP", "CURRENT_USER",
            "CURSOR", "DATABASE", "DATABASES", "DAY_HOUR", "DAY_MICROSECOND", "DAY_MINUTE", "DAY_SECOND", "DEC", "DECIMAL",
            "DECLARE", "DEFAULT", "DELAYED", "DELETE", "DESC", "DESCRIBE", "DETERMINISTIC", "DISTINCT", "DISTINCTROW", "DIV",
            "DOUBLE", "DROP", "DUAL", "EACH", "ELSE", "ELSEIF", "ENCLOSED", "END-EXEC", "ESCAPED", "EXISTS", "EXIT", "EXPLAIN",
            "FALSE", "FETCH", "FLOAT", "FLOAT4", "FLOAT8", "FOR", "FORCE", "FOREIGN", "FROM", "FULLTEXT", "GENERATED", "GET",
            "GRANT", "GROUP", "HAVING", "HIGH_PRIORITY", "HOUR_MICROSECOND", "HOUR_MINUTE", "HOUR_SECOND", "IF", "IGNORE",
            "IN", "INDEX", "INFILE", "INNER", "INOUT", "INSENSITIVE", "INSERT", "INT", "INT1", "INT2", "INT3", "INT4", "INT8",
            "INTEGER", "INTERVAL", "INTO", "IS", "ITERATE", "JOIN", "KEY", "KEYS", "KILL", "LEADING", "LEAVE", "LEFT", "LIKE",
            "LIMIT", "LINES", "LOAD", "LOCALTIME", "LOCALTIMESTAMP", "LOCK", "LONG", "LONGBLOB", "LONGTEXT", "LOOP", "LOW_PRIORITY",
            "MATCH", "MAXVALUE", "MEDIUMBLOB", "MEDIUMINT", "MEDIUMTEXT", "MIDDLEINT", "MINUTE_MICROSECOND", "MINUTE_SECOND",
            "MOD", "MODIFIES", "NATURAL", "NO_WRITE_TO_BINLOG", "NOT", "NULL", "NUMERIC", "ON", "OPTIMIZE", "OPTION", "OPTIONALLY",
            "OR", "ORDER", "OUT", "OUTER", "OUTFILE", "PARTITION", "PRECISION", "PRIMARY", "PROCEDURE", "PURGE", "RANGE", "READ",
            "READS", "REAL", "REFERENCES", "REGEXP", "RELEASE", "RENAME", "REPEAT", "REPLACE", "REQUIRE", "RESIGNAL", "RESTRICT",
            "RETURN", "REVOKE", "RIGHT", "RLIKE", "SCHEMA", "SCHEMAS", "SECOND_MICROSECOND", "SENSITIVE", "SEPARATOR", "SET",
            "SHOW", "SIGNAL", "SMALLINT", "SPATIAL", "SPECIFIC", "SQL", "SQL_BIG_RESULT", "SQL_CALC_FOUND_ROWS", "SQL_SMALL_RESULT",
            "SQLEXCEPTION", "SQLSTATE", "SQLWARNING", "SSL", "STARTING", "STRAIGHT_JOIN", "TABLE", "TERMINATED", "THEN", "TINYBLOB",
            "TINYINT", "TINYTEXT", "TO", "TRAILING", "TRIGGER", "TRUE", "UNDO", "UNION", "UNIQUE", "UNLOCK", "UNSIGNED", "UPDATE",
            "USAGE", "USE", "USING", "UTC_DATE", "UTC_TIME", "UTC_TIMESTAMP", "VALUES", "VARBINARY", "VARCHAR", "VARCHARACTER",
            "VARYING", "WHEN", "WHERE", "WHILE", "WITH", "WRITE", "XOR", "YEAR_MONTH", "ZEROFILL",
            // end generated.
            // the following keywords used to be defined as reserved words in the past, but now seem to work
            // we still add them for compat reasons
            "INDEX", "SELECT"
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MySQLDictionary.java`
#### Snippet
```java
        searchStringEscape = "\\\\";

        typeModifierSet.addAll(Arrays.asList(new String[] { "UNSIGNED", "ZEROFILL" }));

        setLeadingDelimiter(DELIMITER_BACK_TICK);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/EmpressDictionary.java`
#### Snippet
```java
        bitTypeName = "SMALLINT";

        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "TEXT", "BULK", "LONGFLOAT", "INTEGER64", "SHORTINTEGER",
            "LONGINTEGER",
        }));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            try {
                _putChars = clob.getClass().getMethod("putChars",
                    new Class[]{ long.class, char[].class });
            } catch (Exception e) {
                throw new StoreException(e);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            try {
                _putBytes = blob.getClass().getMethod("putBytes",
                    new Class[]{ long.class, byte[].class });
            } catch (Exception e) {
                throw new StoreException(e);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        throws SQLException {
        try {
            method.invoke(target, new Object[]{ 1L, data });
        } catch (InvocationTargetException ite) {
            Throwable t = ite.getTargetException();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            try {
                _putString = clob.getClass().getMethod("putString",
                    new Class[]{ long.class, String.class });
            } catch (Exception e) {
                throw new StoreException(e);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                        new Class[]{ int.class, short.class }).
                        invoke(inner,
                            new Object[]{
                                    idx,
                                oraclePreparedStatementFormNvarcharField.get(null)
                            });
                } catch (Exception e) {
                    log.warn(e);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                try {
                    Method setFixedCharMethod = inner.getClass().getMethod("setFixedCHAR",
                                                         new Class[]{int.class, String.class});
                    if (!setFixedCharMethod.isAccessible()) {
                        setFixedCharMethod.setAccessible(true);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                    }

                    setFixedCharMethod.invoke(inner, new Object[]{idx, val });
                    return;
                } catch (Exception e) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        supportsSelectEndIndex = true;

        systemSchemaSet.addAll(Arrays.asList(new String[]{
            "CTXSYS", "MDSYS", "SYS", "SYSTEM", "WKSYS", "WMSYS", "XDB",
        }));

        supportsXMLColumn = true;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

        varcharTypeName = "VARCHAR2{0}";
        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "LONG RAW", "RAW", "LONG", "REF",
        }));
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "ACCESS", "AUDIT", "CLUSTER", "COMMENT", "COMPRESS", "EXCLUSIVE",
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            "LONG RAW", "RAW", "LONG", "REF",
        }));
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "ACCESS", "AUDIT", "CLUSTER", "COMMENT", "COMPRESS", "EXCLUSIVE",
            "FILE", "IDENTIFIED", "INCREMENT", "INDEX", "INITIAL", "LOCK",
            "LONG", "MAXEXTENTS", "MINUS", "MODE", "NOAUDIT", "NOCOMPRESS",
            "NOWAIT", "OFFLINE", "ONLINE", "PCTFREE", "ROW",
        }));

        // reservedWordSet subset that CANNOT be used as valid column names
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        // (i.e., without surrounding them with double-quotes)
        // generated at 2021-05-02T16:36:42.608 via org.apache.openjpa.reservedwords.ReservedWordsIT
        invalidColumnWordSet.addAll(Arrays.asList(new String[] {
            "ACCESS", "ADD", "ALL", "ALTER", "AND", "ANY", "AS", "ASC", "AUDIT", "BETWEEN", "BY", "CHAR", "CHECK", "CLUSTER",
            "COLUMN", "COMMENT", "COMPRESS", "CONNECT", "CREATE", "CURRENT", "DATE", "DECIMAL", "DEFAULT", "DELETE", "DESC",
            "DISTINCT", "DROP", "ELSE", "END-EXEC", "EXCLUSIVE", "EXISTS", "FILE", "FLOAT", "FOR", "FROM", "GRANT", "GROUP",
            "HAVING", "IDENTIFIED", "IMMEDIATE", "IN", "INCREMENT", "INDEX", "INITIAL", "INSERT", "INTEGER", "INTERSECT", "INTO",
            "IS", "LEVEL", "LIKE", "LOCK", "LONG", "MAXEXTENTS", "MINUS", "MLSLABEL", "MODE", "MODIFY", "NOAUDIT", "NOCOMPRESS",
            "NOT", "NOWAIT", "NULL", "NUMBER", "OF", "OFFLINE", "ON", "ONLINE", "OPTION", "OR", "ORDER", "PCTFREE", "PRIOR",
            "PUBLIC", "RAW", "RENAME", "RESOURCE", "REVOKE", "ROW", "ROWID", "ROWNUM", "ROWS", "SELECT", "SESSION", "SET",
            "SHARE", "SIZE", "SMALLINT", "START", "SUCCESSFUL", "SYNONYM", "SYSDATE", "TABLE", "THEN", "TO", "TRIGGER", "UID",
            "UNION", "UNIQUE", "UPDATE", "USER", "VALIDATE", "VALUES", "VARCHAR", "VARCHAR2", "VIEW", "WHENEVER", "WHERE",
            "WITH",
            // end generated.
            // The following keywords used to be defined as reserved words in the past, but now seem to work
            // we still add them for compat reasons
            "PRIVILEGES"
        }));

        substringFunctionName = "SUBSTR";
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JDataStoreDictionary.java`
#### Snippet
```java
        autoAssignClause = "AUTOINCREMENT";

        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "SHORT", "INT", "LONG", "DOUBLE PRECISION", "BOOLEAN",
        }));

        requiresSearchStringEscapeForLike = true;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
        doubleTypeName = "DOUBLE PRECISION";
        timestampTypeName = "TIMESTAMP";
        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "BOOL", "BYTEA", "NAME", "INT8", "INT2", "INT2VECTOR", "INT4",
            "REGPROC", "TEXT", "OID", "TID", "XID", "CID", "OIDVECTOR",
            "SET", "FLOAT4", "FLOAT8", "ABSTIME", "RELTIME", "TINTERVAL",
            "MONEY",
        }));
        booleanRepresentation = BooleanRepresentationFactory.BOOLEAN;

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
        supportsLockingWithOuterJoin = false;

        reservedWordSet.addAll(Arrays.asList(new String[]{
            "ABORT", "ACL", "AGGREGATE", "APPEND", "ARCHIVE", "ARCH_STORE",
            "BACKWARD", "BINARY", "CHANGE", "CLUSTER", "COPY", "DATABASE",
            "DELIMITER", "DELIMITERS", "DO", "EXPLAIN", "EXTEND",
            "FORWARD", "HEAVY", "INDEX", "INHERITS", "ISNULL", "LIGHT",
            "LISTEN", "LOAD", "MERGE", "NOTHING", "NOTIFY", "NOTNULL",
            "OID", "OIDS", "PURGE", "RECIPE", "RENAME", "REPLACE",
            "RETRIEVE", "RETURNS", "RULE", "SETOF", "STDIN", "STDOUT",
            "STORE", "VACUUM", "VERBOSE", "VERSION",
        }));

        // reservedWordSet subset that CANNOT be used as valid column names
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
        // (i.e., without surrounding them with double-quotes)
        // generated at 2021-05-03T10:44:58.562 via org.apache.openjpa.reservedwords.ReservedWordsIT
        invalidColumnWordSet.addAll(Arrays.asList(new String[] {
            "ALL", "ANALYSE", "ANALYZE", "AND", "ANY", "ARRAY", "AS", "ASC", "ASYMMETRIC", "AUTHORIZATION", "BINARY", "BOTH",
            "CASE", "CAST", "CHECK", "COLLATE", "COLLATION", "COLUMN", "CONSTRAINT", "CREATE", "CROSS", "CURRENT_DATE", "CURRENT_ROLE",
            "CURRENT_TIME", "CURRENT_TIMESTAMP", "CURRENT_USER", "DEFAULT", "DEFERRABLE", "DESC", "DISTINCT", "DO", "ELSE",
            "END", "END-EXEC", "EXCEPT", "FALSE", "FETCH", "FOR", "FOREIGN", "FREEZE", "FROM", "FULL", "GRANT", "GROUP", "HAVING",
            "ILIKE", "IN", "INITIALLY", "INNER", "INTERSECT", "INTO", "IS", "ISNULL", "JOIN", "LATERAL", "LEADING", "LEFT",
            "LIKE", "LIMIT", "LOCALTIME", "LOCALTIMESTAMP", "NATURAL", "NOT", "NOTNULL", "NULL", "OFFSET", "ON", "ONLY", "OR",
            "ORDER", "OUTER", "OVERLAPS", "PLACING", "PRIMARY", "REFERENCES", "RIGHT", "SELECT", "SESSION_USER", "SIMILAR",
            "SOME", "SYMMETRIC", "TABLE", "TABLESAMPLE", "THEN", "TO", "TRAILING", "TRUE", "UNION", "UNIQUE", "USER", "USING",
            "VERBOSE", "WHEN", "WHERE", "WINDOW", "WITH",
            // end generated.
            // The following keywords used to be defined as reserved words in the past, but now seem to work
            // we still add them for compat reasons
            "BETWEEN",
        }));

        _timestampTypes.add("ABSTIME");
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SybaseDictionary.java`
#### Snippet
```java

        // these tables should not be reflected on
        systemTableSet.addAll(Arrays.asList(new String[]{
            "IJDBC_FUNCTION_ESCAPES", "JDBC_FUNCTION_ESCAPES",
            "SPT_IJDBC_CONVERSION", "SPT_IJDBC_MDA", "SPT_IJDBC_TABLE_TYPES",
            "SPT_JDBC_CONVERSION", "SPT_JDBC_TABLE_TYPES", "SPT_JTEXT",
            "SPT_LIMIT_TYPES", "SPT_MDA", "SPT_MONITOR", "SPT_VALUES",
            "SYBLICENSESLOG",
        }));

        // reserved words specified at:
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SybaseDictionary.java`
#### Snippet
```java
        // reserved words specified at:
        // http://manuals.sybase.com/onlinebooks/group-as/asg1250e/refman/@Generic__BookTextView/26603
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "ARITH_OVERFLOW", "BREAK", "BROWSE", "BULK", "CHAR_CONVERT",
            "CHECKPOINT", "CLUSTERED", "COMPUTE", "CONFIRM", "CONTROLROW",
            "DATABASE", "DBCC", "DETERMINISTIC", "DISK DISTINCT", "DUMMY",
            "DUMP", "ENDTRAN", "ERRLVL", "ERRORDATA", "ERROREXIT", "EXCLUSIVE",
            "EXIT", "EXP_ROW_SIZE", "FILLFACTOR", "FUNC", "FUNCTION",
            "HOLDLOCK", "IDENTITY_GAP", "IDENTITY_INSERT", "IDENTITY_START",
            "IF", "INDEX", "INOUT", "INSTALL", "INTERSECT", "JAR", "KILL",
            "LINENO", "LOAD", "LOCK", "MAX_ROWS_PER_PAGE", "MIRROR",
            "MIRROREXIT", "MODIFY", "NEW", "NOHOLDLOCK", "NONCLUSTERED",
            "NUMERIC_TRUNCATION", "OFF", "OFFSETS", "ONCE", "ONLINE", "OUT",
            "OVER", "PARTITION", "PERM", "PERMANENT", "PLAN", "PRINT", "PROC",
            "PROCESSEXIT", "PROXY_TABLE", "QUIESCE", "RAISERROR", "READ",
            "READPAST", "READTEXT", "RECONFIGURE", "REFERENCES REMOVE", "REORG",
            "REPLACE", "REPLICATION", "RESERVEPAGEGAP", "RETURN", "RETURNS",
            "ROLE", "ROWCOUNT", "RULE", "SAVE", "SETUSER", "SHARED",
            "SHUTDOWN", "SOME", "STATISTICS", "STRINGSIZE", "STRIPE",
            "SYB_IDENTITY", "SYB_RESTREE", "SYB_TERMINATE", "TEMP", "TEXTSIZE",
            "TRAN", "TRIGGER", "TRUNCATE", "TSEQUAL", "UNPARTITION", "USE",
            "USER_OPTION", "WAITFOR", "WHILE", "WRITETEXT",
        }));

        // Sybase does not allow reserved words to be used as column names.
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLServerDictionary.java`
#### Snippet
```java
new String[] {
            "ADD", "ALL", "ALTER", "AND", "ANY", "AS", "ASC", "AUTHORIZATION", "BACKUP", "BEGIN", "BETWEEN", "BREAK", "BROWSE",
            "BULK", "BY", "CASCADE", "CASE", "CHECK", "CHECKPOINT", "CLOSE", "CLUSTERED", "COALESCE", "COLLATE", "COLUMN",
            "COMMIT", "COMPUTE", "CONSTRAINT", "CONTAINS", "CONTAINSTABLE", "CONTINUE", "CONVERT", "CREATE", "CROSS", "CURRENT",
            "CURRENT_DATE", "CURRENT_TIME", "CURRENT_TIMESTAMP", "CURRENT_USER", "CURSOR", "DATABASE", "DBCC", "DEALLOCATE",
            "DECLARE", "DEFAULT", "DELETE", "DENY", "DESC", "DISTINCT", "DISTRIBUTED", "DOUBLE", "DROP", "ELSE", "END", "END-EXEC",
            "ERRLVL", "ESCAPE", "EXCEPT", "EXEC", "EXECUTE", "EXISTS", "EXIT", "EXTERNAL", "FETCH", "FILE", "FILLFACTOR", "FOR",
            "FOREIGN", "FREETEXT", "FREETEXTTABLE", "FROM", "FULL", "FUNCTION", "GOTO", "GRANT", "GROUP", "HAVING", "HOLDLOCK",
            "IDENTITY", "IDENTITY_INSERT", "IDENTITYCOL", "IF", "IN", "INDEX", "INNER", "INSERT", "INTERSECT", "INTO", "IS",
            "JOIN", "KEY", "KILL", "LEFT", "LIKE", "LINENO", "MERGE", "NATIONAL", "NOCHECK", "NONCLUSTERED", "NOT", "NULL",
            "NULLIF", "OF", "OFF", "OFFSETS", "ON", "OPEN", "OPENDATASOURCE", "OPENQUERY", "OPENROWSET", "OPENXML", "OPTION",
            "OR", "ORDER", "OUTER", "OVER", "PERCENT", "PLAN", "PRIMARY", "PRINT", "PROC", "PROCEDURE", "PUBLIC", "RAISERROR",
            "READ", "READTEXT", "RECONFIGURE", "REFERENCES", "REPLICATION", "RESTORE", "RESTRICT", "RETURN", "REVOKE", "RIGHT",
            "ROLLBACK", "ROWCOUNT", "ROWGUIDCOL", "RULE", "SAVE", "SCHEMA", "SELECT", "SESSION_USER", "SET", "SETUSER", "SHUTDOWN",
            "SOME", "STATISTICS", "SYSTEM_USER", "TABLE", "TABLESAMPLE", "TEXTSIZE", "THEN", "TO", "TOP", "TRAN", "TRANSACTION",
            "TRIGGER", "TRUNCATE", "TSEQUAL", "UNION", "UNIQUE", "UPDATE", "UPDATETEXT", "USE", "USER", "VALUES", "VARYING",
            "VIEW", "WAITFOR", "WHEN", "WHERE", "WHILE", "WITH", "WRITETEXT",
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
        sequenceNameSQL = "SEQNAME = ?";

        reservedWordSet.addAll(Arrays.asList(new String[]{
            "BIGINT", "BINARY", "DATE", "TIME",
            "TINYINT", "VARBINARY"
        }));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
    @Override
    protected String getGenKeySeqName(String query, Column col) {
        return MessageFormat.format(query, new Object[]{getAutoGenSeqName(col)});
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DerbyDictionary.java`
#### Snippet
```java
        // Derby does still not support 'WITH TIMEZONE' from the SQL92 standard

        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "BIGINT", "INTEGER", "TEXT"
        }));
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "BOOLEAN", "CALL", "ENDEXEC", "EXPLAIN", "FUNCTION",
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DerbyDictionary.java`
#### Snippet
```java
            "BIGINT", "INTEGER", "TEXT"
        }));
        reservedWordSet.addAll(Arrays.asList(new String[]{
            "BOOLEAN", "CALL", "ENDEXEC", "EXPLAIN", "FUNCTION",
            "GET_CURRENT_CONNECTION", "INOUT", "LONGINT", "LTRIM", "NONE",
            "NVARCHAR", "OFF", "OUT", "RTRIM", "SUBSTR", "XML", "XMLEXISTS",
            "XMLPARSE", "XMLSERIALIZE",
        }));

        // reservedWordSet subset that CANNOT be used as valid column names
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DerbyDictionary.java`
#### Snippet
```java
new String[] {
            "ADD", "ALL", "ALLOCATE", "ALTER", "AND", "ANY", "ARE", "AS", "ASC", "ASSERTION", "AT", "AUTHORIZATION", "AVG",
            "BEGIN", "BETWEEN", "BIGINT", "BIT", "BOOLEAN", "BOTH", "BY", "CALL", "CASCADE", "CASCADED", "CASE", "CAST", "CHAR",
            "CHARACTER", "CHARACTER_LENGTH", "CHECK", "CLOSE", "COLLATE", "COLLATION", "COLUMN", "COMMIT", "CONNECT", "CONNECTION",
            "CONSTRAINT", "CONSTRAINTS", "CONTINUE", "CONVERT", "CORRESPONDING", "CREATE", "CROSS", "CURRENT", "CURRENT_DATE",
            "CURRENT_ROLE", "CURRENT_TIME", "CURRENT_TIMESTAMP", "CURRENT_USER", "CURSOR", "DEALLOCATE", "DEC", "DECIMAL",
            "DECLARE", "DEFAULT", "DEFERRABLE", "DEFERRED", "DEFINER", "DELETE", "DESC", "DESCRIBE", "DETERMINISTIC", "DIAGNOSTICS",
            "DISCONNECT", "DISTINCT", "DOUBLE", "DROP", "ELSE", "END", "END-EXEC", "ESCAPE", "EXCEPT", "EXCEPTION", "EXEC",
            "EXECUTE", "EXISTS", "EXPLAIN", "EXTERNAL", "FALSE", "FETCH", "FIRST", "FLOAT", "FOR", "FOREIGN", "FOUND", "FROM",
            "FULL", "FUNCTION", "GET", "GETCURRENTCONNECTION", "GLOBAL", "GO", "GOTO", "GRANT", "GROUP", "HAVING", "HOUR",
            "IDENTITY", "IMMEDIATE", "IN", "INDICATOR", "INITIALLY", "INNER", "INOUT", "INPUT", "INSENSITIVE", "INSERT", "INT",
            "INTEGER", "INTERSECT", "INTO", "INVOKER", "IS", "ISOLATION", "JOIN", "KEY", "LAST", "LEADING", "LEFT", "LIKE",
            "LOWER", "LTRIM", "MATCH", "MAX", "MIN", "MINUTE", "NATIONAL", "NATURAL", "NCHAR", "NEXT", "NO", "NONE", "NOT",
            "NULL", "NULLIF", "NUMERIC", "NVARCHAR", "OF", "ON", "ONLY", "OPEN", "OPTION", "OR", "ORDER", "OUT", "OUTER", "OUTPUT",
            "OVERLAPS", "PAD", "PARTIAL", "PREPARE", "PRESERVE", "PRIMARY", "PRIOR", "PRIVILEGES", "PROCEDURE", "PUBLIC", "READ",
            "REAL", "REFERENCES", "RELATIVE", "RESTRICT", "REVOKE", "RIGHT", "ROLLBACK", "ROWS", "RTRIM", "SCHEMA", "SCROLL",
            "SECOND", "SELECT", "SESSION_USER", "SET", "SMALLINT", "SOME", "SPACE", "SQL", "SQLCODE", "SQLERROR", "SQLSTATE",
            "SUBSTR", "SUBSTRING", "SUM", "SYSTEM_USER", "TABLE", "TEMPORARY", "TIMEZONE_HOUR", "TIMEZONE_MINUTE", "TO", "TRAILING",
            "TRANSACTION", "TRANSLATE", "TRANSLATION", "TRIM", "TRUE", "UNION", "UNIQUE", "UNKNOWN", "UPDATE", "UPPER", "USER",
            "USING", "VALUES", "VARCHAR", "VARYING", "VIEW", "WHENEVER", "WHERE", "WINDOW", "WITH", "WORK", "WRITE", "XML",
            "XMLEXISTS", "XMLPARSE", "XMLQUERY", "XMLSERIALIZE", "YEAR",
            // end generated.
            // The following keywords used to be defined as reserved words in the past, but now seem to work
            // we still add them for compat reasons
            "COALESCE", "OVER", "ROW_NUMBER",
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java
        timeWithZoneTypeName = "TIME";

        reservedWordSet.addAll(Arrays.asList(new String[] {
                "ALL",
                "CHECK",
                "CONSTRAINT",
                "CROSS",
                "CURRENT_DATE",
                "CURRENT_TIME",
                "CURRENT_TIMESTAMP",
                "DISTINCT",
                "EXCEPT",
                "EXISTS",
                "FALSE",
                "FETCH",
                "FOR",
                "FOREIGN",
                "FROM",
                "FULL",
                "GROUP",
                "HAVING",
                "INNER",
                "INTERSECT",
                "IS",
                "JOIN",
                "LIKE",
                "LIMIT",
                "MINUS",
                "NATURAL",
                "NOT",
                "NULL",
                "OFFSET",
                "ON",
                "ORDER",
                "PRIMARY",
                "ROWNUM",
                "SELECT",
                "SYSDATE",
                "SYSTIME",
                "SYSTIMESTAMP",
                "TODAY",
                "TRUE",
                "UNION",
                "UNIQUE",
                "WHERE",
                "WITH"
        }));

        // reservedWordSet subset that CANNOT be used as valid column names
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java
        // (i.e., without surrounding them with double-quotes)
        // generated at 2021-05-02T14:32:50.704 via org.apache.openjpa.reservedwords.ReservedWordsIT
        invalidColumnWordSet.addAll(Arrays.asList(new String[] {
            "CHECK", "CONSTRAINT", "CROSS", "CURRENT_DATE", "CURRENT_TIME", "CURRENT_TIMESTAMP", "DISTINCT", "END-EXEC",
            "EXCEPT", "EXISTS", "FALSE", "FETCH", "FOR", "FOREIGN", "FROM", "FULL", "GROUP", "HAVING", "INNER", "INTERSECT",
            "IS", "JOIN", "LIKE", "LIMIT", "MINUS", "NATURAL", "NOT", "NULL", "OFFSET", "ON", "ORDER", "PRIMARY", "ROWNUM",
            "SELECT", "SYSDATE", "TRUE", "UNION", "UNIQUE", "WHERE", "WITH",
        }));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/FirebirdDictionary.java`
#### Snippet
```java
.asList(new String[] { "ACTIVE", "ADMIN",
            "AFTER", "ASCENDING", "AUTO", "AUTODDL", "BASED", "BASENAME",
            "BASE_NAME", "BEFORE", "BIGINT", "BLOB", "BLOBEDIT", "BUFFER",
            "CACHE", "CHECK_POINT_LEN", "CHECK_POINT_LENGTH", "COMPILETIME",
            "COMPUTED", "CLOSE", "CONDITIONAL", "CONTAINING", "CSTRING",
            "CURRENT_CONNECTION", "CURRENT_ROLE", "CURRENT_TRANSACTION",
            "DATABASE", "DB_KEY", "DEBUG", "DESCENDING", "DO", "ECHO", "EDIT",
            "ENTRY_POINT", "EVENT", "EXIT", "EXTERN", "FILE", "FILTER",
            "FREE_IT", "FUNCTION", "GDSCODE", "GENERATOR", "GEN_ID", "GLOBAL",
            "GOTO", "GROUP_COMMIT_WAIT", "GROUP_COMMIT_WAIT_TIME", "HELP",
            "IF", "INACTIVE", "INDEX", "INIT", "INPUT_TYPE", "ISQL",
            "LC_MESSAGES", "LC_TYPE", "LEV", "LOGFILE", "LOG_BUFFER_SIZE",
            "LOG_BUF_SIZE", "LONG", "MANUAL", "MAXIMUM", "MAXIMUM_SEGMENT",
            "MAX_SEGMENT", "MERGE", "MESSAGE", "MINUTE", "MODULE_NAME",
            "NOAUTO", "NUM_LOG_BUFS", "NUM_LOG_BUFFERS", "OUTPUT_TYPE",
            "OVERFLOW", "PAGE", "PAGELENGTH", "PAGES", "PAGE_SIZE",
            "PARAMETER", "PASSWORD", "PLAN", "POST_EVENT", "PROCEDURE",
            "PROTECTED", "QUIT", "RAW_PARTITIONS", "RDB$DB_KEY",
            "RECORD_VERSION", "RECREATE", "RELEASE", "RESERV", "RESERVING",
            "RETAIN", "RETURN", "RETURNING_VALUES", "RETURNS", "ROLE",
            "RUNTIME", "SAVEPOINT", "SEGMENT", "SHADOW", "SHARED", "SHELL",
            "SHOW", "SINGULAR", "SNAPSHOT", "SORT", "SQLWARNING", "STABILITY",
            "START", "STARTING", "STARTS", "STATEMENT", "STATIC", "STATISTICS",
            "SUB_TYPE", "SUSPEND", "TERMINATOR", "TRIGGER", "VARIABLE",
            "VERSION", "WAIT", "WEEKDAY", "WHILE" }));

   
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/FirebirdDictionary.java`
#### Snippet
```java
                "long-seq-name");
        String createSeq =
            MessageFormat.format(createSequenceSQL, new Object[] { seqName });
        if (seq.getInitialValue() == 0)
            return new String[] { createSeq };
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/FirebirdDictionary.java`
#### Snippet
```java
        // Use String.valueOf to get rid of possible number formatting.
        String alterSeq =
            MessageFormat.format(alterSequenceSQL, new Object[] { seqName,
                String.valueOf(seq.getInitialValue()) });
        return new String[] { createSeq, alterSeq };
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/MappingRepositoryValue.java`
#### Snippet
```java
                    J2DoPrivHelper.getClassLoaderAction(type)));
            return cls.getConstructor(new Class[]{ JDBCConfiguration.class }).
                newInstance(new Object[]{ conf });
        } catch (RuntimeException e) {
            throw e;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java
        requiresCastForComparisons = true;

        reservedWordSet.addAll(Arrays.asList(new String[]{
            "BEFORE", "BIGINT", "BINARY", "CACHED", "DATETIME", "LIMIT",
            "LONGVARBINARY", "LONGVARCHAR", "OBJECT", "OTHER",
            "SAVEPOINT", "TEMP", "TEXT", "TRIGGER", "TINYINT",
            "VARBINARY", "VARCHAR_IGNORECASE",
        }));

        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java
        }));

        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "TEXT"
        }));
        fixedSizeTypeNameSet.remove("NUMERIC");
        fixedSizeTypeNameSet.remove("DECIMAL");
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java
        // (i.e., without surrounding them with double-quotes)
        // generated at 2021-05-02T18:24:22.807 via org.apache.openjpa.reservedwords.ReservedWordsIT
        invalidColumnWordSet.addAll(Arrays.asList(new String[] {
            "ALL", "AND", "ANY", "AS", "AT", "BETWEEN", "BOTH", "BY", "CALL", "CASE", "CAST", "CHECK", "COALESCE", "CONSTRAINT",
            "CONVERT", "CORRESPONDING", "CREATE", "CROSS", "CUBE", "DEFAULT", "DISTINCT", "DO", "DROP", "ELSE", "END-EXEC",
            "EVERY", "EXCEPT", "EXISTS", "FETCH", "FOR", "FOREIGN", "FROM", "FULL", "GRANT", "GROUP", "GROUPING", "HAVING",
            "IN", "INNER", "INTERSECT", "INTO", "IS", "JOIN", "LEADING", "LEFT", "LIKE", "NATURAL", "NOT", "NULLIF", "ON",
            "OR", "ORDER", "OUTER", "PRIMARY", "REFERENCES", "RIGHT", "ROLLUP", "SELECT", "SET", "SOME", "SUM", "TABLE", "THEN",
            "TO", "TRAILING", "TRIGGER", "UNION", "UNIQUE", "USING", "VALUES", "WHEN", "WHERE", "WITH",
        }));
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
        storeCharsAsNumbers = false;

        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "LONG VARCHAR FOR BIT DATA", "LONG VARCHAR", "LONG VARGRAPHIC",
        }));
        systemSchemas = "SYSCAT,SYSIBM,SYSSTAT,SYSIBMADM,SYSTOOLS";
        maxConstraintNameLength = 18;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
new String[]{
            "AFTER", "ALIAS", "ALLOW", "APPLICATION", "ASSOCIATE", "ASUTIME",
            "AUDIT", "AUX", "AUXILIARY", "BEFORE", "BINARY", "BUFFERPOOL",
            "CACHE", "CALL", "CALLED", "CAPTURE", "CARDINALITY", "CCSID",
            "CLUSTER", "COLLECTION", "COLLID", "COMMENT", "CONCAT",
            "CONDITION", "CONTAINS", "COUNT_BIG", "CURRENT_LC_CTYPE",
            "CURRENT_PATH", "CURRENT_SERVER", "CURRENT_TIMEZONE", "CYCLE",
            "DATABASE", "DAYS", "DB2GENERAL", "DB2GENRL", "DB2SQL",
            "DBINFO", "DEFAULTS", "DEFINITION", "DETERMINISTIC", "DISALLOW",
            "DO", "DSNHATTR", "DSSIZE", "DYNAMIC", "EACH", "EDITPROC", "ELSEIF",
            "ENCODING", "END-EXEC1", "ERASE", "EXCLUDING", "EXIT", "FENCED",
            "FIELDPROC", "FILE", "FINAL", "FREE", "FUNCTION", "GENERAL",
            "GENERATED", "GRAPHIC", "HANDLER", "HOLD", "HOURS", "IF",
            "INCLUDING", "INCREMENT", "INDEX", "INHERIT", "INOUT", "INTEGRITY",
            "ISOBID", "ITERATE", "JAR", "JAVA", "LABEL", "LC_CTYPE", "LEAVE",
            "LINKTYPE", "LOCALE", "LOCATOR", "LOCATORS", "LOCK", "LOCKMAX",
            "LOCKSIZE", "LONG", "LOOP", "MAXVALUE", "MICROSECOND",
            "MICROSECONDS", "MINUTES", "MINVALUE", "MODE", "MODIFIES", "MONTHS",
            "NEW", "NEW_TABLE", "NOCACHE", "NOCYCLE", "NODENAME", "NODENUMBER",
            "NOMAXVALUE", "NOMINVALUE", "NOORDER", "NULLS", "NUMPARTS", "OBID",
            "OLD", "OLD_TABLE", "OPTIMIZATION", "OPTIMIZE", "OUT", "OVERRIDING",
            "PACKAGE", "PARAMETER", "PART", "PARTITION", "PATH", "PIECESIZE",
            "PLAN", "PRIQTY", "PROGRAM", "PSID", "QUERYNO", "READS", "RECOVERY",
            "REFERENCING", "RELEASE", "RENAME", "REPEAT", "RESET", "RESIGNAL",
            "RESTART", "RESULT", "RESULT_SET_LOCATOR", "RETURN", "RETURNS",
            "ROUTINE", "ROW", "RRN", "RUN", "SAVEPOINT", "SCRATCHPAD",
            "SECONDS", "SECQTY", "SECURITY", "SENSITIVE", "SIGNAL", "SIMPLE",
            "SOURCE", "SPECIFIC", "SQLID", "STANDARD", "START", "STATIC",
            "STAY", "STOGROUP", "STORES", "STYLE", "SUBPAGES", "SYNONYM",
            "SYSFUN", "SYSIBM", "SYSPROC", "SYSTEM", "TABLESPACE", "TRIGGER",
            "UNDO", "UNTIL", "VALIDPROC", "VARIABLE", "VARIANT", "VCAT",
            "VOLUMES", "WHILE", "WLM", "YEARS",
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
        // reservedWordSet subset that CANNOT be used as valid column names
        // (i.e., without surrounding them with double-quotes)
        invalidColumnWordSet.addAll(Arrays.asList(new String[] {
            "CONSTRAINT", "END-EXEC", "END-EXEC1",
        }));


```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
                             .getMethod(GETSQLCA,null);
            Object sqlca = sqlcaM2.invoke(sqle.getNextException(),
                                          new Object[] {});
            Method  getSqlErrpMethd = sqlca.getClass().
            getMethod("getSqlErrp", null);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
            exceptionMsg.append("SQLCA OUTPUT");
            exceptionMsg.append("[Errp=");
            exceptionMsg.append(getSqlErrpMethd.invoke(sqlca, new Object[]{}));
            exceptionMsg.append(", Errd=");
            exceptionMsg.append(errdStr);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java

    public DBDictionary() {
        fixedSizeTypeNameSet.addAll(Arrays.asList(new String[]{
            "BIGINT", "BIT", "BLOB", "CLOB", "DATE", "DECIMAL", "DISTINCT",
            "DOUBLE", "FLOAT", "INTEGER", "JAVA_OBJECT", "NULL", "NUMERIC",
            "OTHER", "REAL", "REF", "SMALLINT", "STRUCT", "TIME", "TIMESTAMP",
            "TINYINT",
        }));

        // initialize the set of reserved SQL92 words from resource
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java

    protected String getGenKeySeqName(String query, Column col) {
        return MessageFormat.format(query, new Object[]{
                toDBName(col.getIdentifier()), getFullName(col.getTable(), false),
                getGeneratedKeySequenceName(col),
            });
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     */
    public String[] getDropTableSQL(Table table) {
        String drop = MessageFormat.format(dropTableSQL, new Object[]{
            getFullName(table, false) });
        return new String[]{ drop };
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/NativeJDBCSeq.java`
#### Snippet
```java
        // Increment step is needed for Firebird which uses non-standard sequence fetch syntax.
        // Use String.valueOf to get rid of possible locale-specific number formatting.
        _select = MessageFormat.format(format, new Object[]{name, String.valueOf(_allocate * _increment)});

        type = dict.nativeSequenceType;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java

        // not a recognized action; check for typo
        List recognized = Arrays.asList(new String[]{ "none", "exception",
            "restrict", "cascade", "null", "default", });
        String closest = StringDistance.getClosestLevenshteinDistance(name,
            recognized, .5F);
```

## RuleId[id=UnnecessaryEmptyArrayUsage]
### UnnecessaryEmptyArrayUsage
Zero length array can be changed to constant
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
                (new ClassMapping[_dropMap.size()]);
        else
            mappings = new ClassMapping[0];

        try {
```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                    return ((Comparable) o1).compareTo(o2);
                case TYPE_CLASS_SEQS:
                    return ((Comparable) o1).compareTo(o2);
                default:
                    return compareUnknown(o1, o2);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                    return ((Comparable) o1).compareTo(o2);
                case TYPE_CLASS_SEQS:
                    return ((Comparable) o1).compareTo(o2);
                default:
                    return compareUnknown(o1, o2);
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
    protected MultiQueryMetaData.Parameter.Mode toKernelParameterMode(ParameterMode mode) {
        switch (mode) {
            case IN : return MultiQueryMetaData.Parameter.Mode.IN;
            case OUT: return MultiQueryMetaData.Parameter.Mode.OUT;
            case INOUT: return MultiQueryMetaData.Parameter.Mode.INOUT;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.BYTE:
            case JavaTypes.BYTE_OBJ:
                attrs.add(new Members.SingularAttributeImpl<X, Byte>(this, f));
                break;
            case JavaTypes.CHAR:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.CHAR:
            case JavaTypes.CHAR_OBJ:
                attrs.add(new Members.SingularAttributeImpl<X, Character>(this, f));
                break;
            case JavaTypes.DOUBLE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.DOUBLE:
            case JavaTypes.DOUBLE_OBJ:
                attrs.add(new Members.SingularAttributeImpl<X, Double>(this, f));
                break;
            case JavaTypes.FLOAT:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.FLOAT:
            case JavaTypes.FLOAT_OBJ:
                attrs.add(new Members.SingularAttributeImpl<X, Float>(this, f));
                break;
            case JavaTypes.INT:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.INT:
            case JavaTypes.INT_OBJ:
                attrs.add(new Members.SingularAttributeImpl<X, Integer>(this, f));
                break;
            case JavaTypes.LONG:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.LONG:
            case JavaTypes.LONG_OBJ:
                attrs.add(new Members.SingularAttributeImpl<X, Long>(this, f));
                break;
            case JavaTypes.SHORT:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.SHORT:
            case JavaTypes.SHORT_OBJ:
                attrs.add(new Members.SingularAttributeImpl<X, Short>(this, f));
                break;
            case JavaTypes.STRING:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.STRING:
                attrs.add(new Members.SingularAttributeImpl<X, String>(this, f));
                break;
            case JavaTypes.NUMBER:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.NUMBER:
                attrs.add(new Members.SingularAttributeImpl<X, Number>(this, f));
                break;
            case JavaTypes.DATE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.DATE:
                attrs.add(new Members.SingularAttributeImpl<X, Date>(this, f));
                break;
            case JavaTypes.LOCAL_DATE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.LOCAL_DATE:
                attrs.add(new Members.SingularAttributeImpl<X, LocalDate>(this, f));
                break;
            case JavaTypes.LOCAL_TIME:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.LOCAL_TIME:
                attrs.add(new Members.SingularAttributeImpl<X, LocalTime>(this, f));
                break;
            case JavaTypes.LOCAL_DATETIME:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.LOCAL_DATETIME:
                attrs.add(new Members.SingularAttributeImpl<X, LocalDateTime>(this, f));
                break;
            case JavaTypes.OFFSET_TIME:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.OFFSET_TIME:
                attrs.add(new Members.SingularAttributeImpl<X, OffsetTime>(this, f));
                break;
            case JavaTypes.OFFSET_DATETIME:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.OFFSET_DATETIME:
                attrs.add(new Members.SingularAttributeImpl<X, OffsetDateTime>(this, f));
                break;
            case JavaTypes.CALENDAR:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.CALENDAR:
                attrs.add(new Members.SingularAttributeImpl<X, Calendar>(this, f));
                break;
            case JavaTypes.BIGDECIMAL:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.BIGDECIMAL:
                attrs.add(new Members.SingularAttributeImpl<X, BigDecimal>(this, f));
                break;
            case JavaTypes.BIGINTEGER:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.BIGINTEGER:
                attrs.add(new Members.SingularAttributeImpl<X, BigInteger>(this, f));
                break;
            case JavaTypes.LOCALE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.LOCALE:
                attrs.add(new Members.SingularAttributeImpl<X, Locale>(this, f));
                break;
            case JavaTypes.OBJECT:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.INPUT_READER:
            case JavaTypes.INPUT_STREAM:
                attrs.add(new Members.SingularAttributeImpl(this, f));
                break;
            case JavaTypes.ARRAY:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.ENUM:
                attrs.add(new Members.SingularAttributeImpl(this, f));
                break;
            default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
                break;
            case COLS:
                ret = true;
                break;
            case JOIN_COL:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
                break;
            case NAME:
                ret = true;
                break;
            case TEMPORAL:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
            case MAP_KEY_ENUMERATED:
            case MAP_KEY_TEMPORAL:
                ret = true;
                break;
            case SQL_RESULT_SET_MAPPING:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
                break;
            case COLUMN_NAME:
                ret = true;
                break;
            case COLLECTION_TABLE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
                break;
            case FK_COL_NAME:
                ret = true;
                break;
            case VERSION_COLS:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedValueStateManager.java`
#### Snippet
```java
                return fm.fetchStringField(field);
            case JavaTypes.OBJECT:
                return fm.fetchObjectField(field);
            case JavaTypes.BOOLEAN:
                return (fm.fetchBooleanField(field)) ? Boolean.TRUE
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
                return fetchStringField(field);
            case JavaTypes.OBJECT:
                return fetchObjectField(field);
            case JavaTypes.BOOLEAN:
                return (fetchBooleanField(field)) ? Boolean.TRUE
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

            case JJTCASE:
                return eval(onlyChild(node));

            case JJTSIMPLECASE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

            case JJTPATH:
                return getPathOrConstant(node);

            case JJTIDENTIFIER:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            case JJTAGGREGATE:
                // simply pass-through while asserting a single child
                return eval(onlyChild(node));

            case JJTCOUNT:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
                    // the instance from cache in the hopes that the cache is
                    // out of sync.
                    remove = true;
                    break;
                case StoreManager.VERSION_EARLIER:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
                    // store enough information to optimize for expected
                    // failures. Clean up the cache.
                    remove = true;
                    break;
                default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
            case Log.INFO:  _log.info(message); break;
            case Log.ERROR: _log.fatal(message); break;
            case Log.FATAL: _log.fatal(message); break;
            case Log.TRACE: _log.trace(message); break;
            case Log.WARN:  _log.warn(message); break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/MetamodelHelper.java`
#### Snippet
```java
    public static Color getColor(Attribute.PersistentAttributeType type) {
        switch (type) {
        case BASIC :             return Color.BLACK;
        case EMBEDDED:           return Color.GRAY;
        case ONE_TO_ONE:         return Color.BLUE;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                return numericTypeName;
            case Types.OTHER:
                return otherTypeName;
            case Types.REAL:
                return realTypeName;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                return getPreferredType(Types.NUMERIC);
            case JavaTypes.NUMBER:
                if (storeLargeNumbersAsStrings)
                    return getPreferredType(Types.VARCHAR);
                return getPreferredType(Types.NUMERIC);
            case JavaTypes.CALENDAR:
            case JavaTypes.DATE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                return getPreferredType(Types.TIME);
            case JavaTypes.LOCAL_DATETIME:
                return getPreferredType(Types.TIMESTAMP);
            case JavaTypes.OFFSET_TIME:
                return getPreferredType(Types.TIME_WITH_TIMEZONE);
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            case JavaSQLTypes.BLOB:
            case JavaSQLTypes.BYTES:
                return getPreferredType(Types.BLOB);
            case JavaSQLTypes.CLOB:
                return getPreferredType(Types.CLOB);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                return getPreferredType(Types.CLOB);
            case JavaSQLTypes.SQL_DATE:
                return getPreferredType(Types.DATE);
            case JavaSQLTypes.TIME:
                return getPreferredType(Types.TIME);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                return getPreferredType(Types.DATE);
            case JavaSQLTypes.TIME:
                return getPreferredType(Types.TIME);
            case JavaSQLTypes.TIMESTAMP:
                return getPreferredType(Types.TIMESTAMP);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                return getPreferredType(Types.TIME);
            case JavaSQLTypes.TIMESTAMP:
                return getPreferredType(Types.TIMESTAMP);
            default:
                return getPreferredType(Types.BLOB);
```

## RuleId[id=NonFinalFieldInEnum]
### NonFinalFieldInEnum
Non-final field `type` in enum 'TypeCategory'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
        MAP("jakarta.persistence.metamodel.MapAttribute");

        private String type;

        private TypeCategory(String type) {
```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`[\\?]` can be simplified to '\\?'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
            // We need
            if (query != null && _convertPositionalParams && JPQLParser.LANG_JPQL.equals(language)) {
                query = query.replaceAll("[\\?]", "\\:_");
            }
            String qid = query;
```

### RegExpSimplifiable
`[\\?]` can be simplified to '\\?'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/QueryMetaData.java`
#### Snippet
```java
    public void setQueryString(String query) {
        if (query != null && _convertPositionalParametersToNamed && JPQLParser.LANG_JPQL.equals(_language)) {
            query = query.replaceAll("[\\?]", "\\:_");
        }
        _query = query;
```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `value.toString().trim().toUpperCase(Locale.ENGLISH)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
            // works because enum values are identical String
            if (value instanceof DataCacheRetrieveMode) {
                return CacheRetrieveMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            } else if (value instanceof DataCacheStoreMode) {
                return CacheStoreMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
```

### DuplicateExpressions
Multiple occurrences of `value.toString().trim().toUpperCase(Locale.ENGLISH)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
                return CacheRetrieveMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            } else if (value instanceof DataCacheStoreMode) {
                return CacheStoreMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            }
        }
```

### DuplicateExpressions
Multiple occurrences of `value.toString().trim().toUpperCase(Locale.ENGLISH)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
            // works because enum values are identical String
            if (value instanceof CacheRetrieveMode || (value instanceof String && CACHE_RETRIEVE_MODE.equals(key))) {
                return (T)DataCacheRetrieveMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            } else if (value instanceof CacheStoreMode || (value instanceof String && CACHE_STORE_MODE.equals(key))) {
                return (T)DataCacheStoreMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
```

### DuplicateExpressions
Multiple occurrences of `value.toString().trim().toUpperCase(Locale.ENGLISH)`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
                return (T)DataCacheRetrieveMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            } else if (value instanceof CacheStoreMode || (value instanceof String && CACHE_STORE_MODE.equals(key))) {
                return (T)DataCacheStoreMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            }

```

### DuplicateExpressions
Multiple occurrences of `Character.isLetter(propName .charAt(i - 1))`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            // surround sequences of digits with dashes.
            if (i != 0
                && ((!Character.isLetter(c) && Character.isLetter(propName
                    .charAt(i - 1)))
                || (Character.isLetter(c) && !Character.isLetter(propName
                    .charAt(i - 1)))))
```

### DuplicateExpressions
Multiple occurrences of `Character.isLetter(propName .charAt(i - 1))`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
                && ((!Character.isLetter(c) && Character.isLetter(propName
                    .charAt(i - 1)))
                || (Character.isLetter(c) && !Character.isLetter(propName
                    .charAt(i - 1)))))
                buf.append('-');

```

### DuplicateExpressions
Multiple occurrences of `destType == java.lang.Integer.TYPE || destType == java.lang.Long.TYPE || destType == java.la...`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
			    destType == java.lang.Double.TYPE;
		} else if (sourceType == java.lang.Short.TYPE) {
			return destType == java.lang.Integer.TYPE ||
				destType == java.lang.Long.TYPE ||
				destType == java.lang.Float.TYPE ||
				destType == java.lang.Double.TYPE;
		} else if (sourceType == java.lang.Character.TYPE) {
			return destType == java.lang.Integer.TYPE ||
```

### DuplicateExpressions
Multiple occurrences of `destType == java.lang.Integer.TYPE || destType == java.lang.Long.TYPE || destType == jav...`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
				destType == java.lang.Double.TYPE;
		} else if (sourceType == java.lang.Character.TYPE) {
			return destType == java.lang.Integer.TYPE ||
			  	destType == java.lang.Long.TYPE ||
			  	destType == java.lang.Float.TYPE ||
			  	destType == java.lang.Double.TYPE;
		} else if (sourceType == java.lang.Integer.TYPE) {
			return destType == java.lang.Long.TYPE ||
```

### DuplicateExpressions
Multiple occurrences of `fieldName.length() > 1 && Character.isLowerCase(fieldName.charAt(0)) && Character.is...`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/CodeGenerator.java`
#### Snippet
```java
            else
                code.append("get");
            if (fieldName.length() > 1 && Character.isLowerCase(fieldName.charAt(0))
                && Character.isUpperCase(fieldName.charAt(1))) {
                code.append(fieldName).parens();
            } else {
```

### DuplicateExpressions
Multiple occurrences of `fieldName.length() > 1 && Character.isLowerCase(fieldName.charAt(0)) && Character.is...`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/CodeGenerator.java`
#### Snippet
```java

            // setter
            if (fieldName.length() > 1 && Character.isLowerCase(fieldName.charAt(0))
                && Character.isUpperCase(fieldName.charAt(1))) {
                code.tab().append("public void set").append(fieldName);
            } else {
```

### DuplicateExpressions
Multiple occurrences of `ch == '>' ? ">" : ch == '<' ? "<" : ch`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/util/TextProcessingUtility.java`
#### Snippet
```java
                        buf.append(ch);
                    } else {
                        buf.append(ch == '>' ? "&gt;" : ch == '<' ? "&lt;" : ch);
                    }
                } else {
```

### DuplicateExpressions
Multiple occurrences of `ch == '>' ? ">" : ch == '<' ? "<" : ch`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/util/TextProcessingUtility.java`
#### Snippet
```java
                    }
                } else {
                    buf.append(ch == '>' ? "&gt;" : ch == '<' ? "&lt;" : ch);
                }
            } else { // inside a hyperlink tag. Do not translate characters
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationBuilder.java`
#### Snippet
```java

    static String enumToString(Enum e) {
        StringBuilder sb = new StringBuilder();
        sb.append(ClassUtil.getClassName(e.getClass())).
            append(".").append(e);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
    String toDetails(Element e) {
        TypeMirror mirror = e.asType();
        return new StringBuilder(e.getKind().toString()).append(" ")
                           .append(e.toString())
                           .append("Mirror ")
```

### StringBufferReplaceableByString
`StringBuilder tmp` can be replaced with 'String'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/WhenClause.java`
#### Snippet
```java

	public String toJPQL(AliasContext ctx) {
		StringBuilder tmp = new StringBuilder();
		tmp.append(" WHEN ").append(JPQLHelper.toJPQL(ctx, when))
		   .append(" THEN ").append(JPQLHelper.toJPQL(ctx, then));
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/JoinPath.java`
#### Snippet
```java
	@Override
	public String asJoinable(AliasContext ctx) {
		return new StringBuilder(getOperator().toString())
		   .append(getParent().asProjection(ctx))
		   .append(NAVIGATION)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/LocateExpression.java`
#### Snippet
```java
        String start = _start == null ? EMPTY : COMMA +
                ((Visitable)_start).asExpression(ctx);
		return new StringBuilder(_op.toString())
		    .append(OPEN_BRACE)
		    .append(((Visitable)_e1).asExpression(ctx))
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }

        return new StringBuilder(strLen)
                .append(Character.toTitleCase(firstChar))
                .append(str.substring(1))
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }

        return new StringBuilder(strLen)
                .append(Character.toLowerCase(firstChar))
                .append(str.substring(1))
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractKeyValue.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder()
            .append(getKey())
            .append('=')
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
        @Override
        public String toString() {
            return new StringBuilder().append(getKey()).append('=').append(getValue()).toString();
        }
    }
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder buf = new StringBuilder(255);
        buf.append("Query: ").append(super.toString());
        buf.append("; candidate class: ").append(_class);
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryKey.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder buf = new StringBuilder(1024);
        buf.append(super.toString()).
            append("[query:[").append(_query).append("]").
```

### StringBufferReplaceableByString
`StringBuilder delAction` can be replaced with 'String'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        DBIdentifier schemaName, DBIdentifier tableName, Connection conn, boolean partialKeys)
        throws SQLException {
        StringBuilder delAction = new StringBuilder("DECODE(t1.DELETE_RULE").
            append(", 'NO ACTION', ").append(DatabaseMetaData.importedKeyNoAction).
            append(", 'RESTRICT', ").append(DatabaseMetaData.importedKeyRestrict).
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                    tableAlias = join.getAlias2();
                if (tableAlias != null)
                    return new StringBuilder(tableAlias).append(".").
                        append(_dict.getNamingUtil().toDBName(col.getIdentifier())).toString();
            }
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
    protected String getOptimizeClause(Select sel) {
        if (sel != null && sel.getExpectedResultCount() > 0) {
            StringBuilder buf = new StringBuilder();
            buf.append(" ").append(optimizeClause).append(" ")
                .append(sel.getExpectedResultCount())
```

### StringBufferReplaceableByString
`StringBuilder sql` can be replaced with 'String'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StoreCollectionFieldStrategy.java`
#### Snippet
```java
                Select sel = store.getSQLFactory().newSelect();
                sel.setAggregate(true);
                StringBuilder sql = new StringBuilder();
                sql.append("MAX(").
                    append(sel.getColumnAlias(field.getOrderColumn())).
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        Val value2 = concatVal.getVal2();
        Object val2 = getValue(value2, ob, params, sm);
        return new StringBuilder(100).append(val1).append(val2).toString();
    }

```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
     */
    public String getDescription() {
        StringBuilder buf = new StringBuilder();
        buf.append("Full Name: ").append(getFullName()).append("\n");
        buf.append("Type: ").append(Schemas.getJDBCName(getType())).
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
         || QueryLanguages.LANG_PREPARED_SQL.equals(language)
         || OpenJPACriteriaBuilder.LANG_CRITERIA.equals(language)) {
            return;
        } else {
            throw new IllegalStateException(_loc.get("not-jpql-or-criteria-query").getMessage());
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
        if (event != null) {
            _validationGroups.put(event, vgs);
            return;
        } else {
            // There were no events found for group "{0}".
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObjectFormatter.java`
#### Snippet
```java
            throw new RuntimeException(this + " does not know how to encode " + obj);
        }
        return;
    }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java

            if (found1 && found2)
                return;
            else if (!found1 && !found2) {
                j.setIsNotMyJoin();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            else if (!found1 && !found2) {
                j.setIsNotMyJoin();
                return;
            }
            else {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java

            field.setUseClassCriteria(criteria);
            return;
        } else {
            // Uni-/OneToMany/ForeingKey
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java
            val.mapConstraints("value", adapt);

            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractJDBCSeq.java`
#### Snippet
```java
           		throw SQLExceptions.getStore(se);
           	}
            return;
        }
        else {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PessimisticLockManager.java`
#### Snippet
```java
        if (!rs.next())
            throw new LockException(sm.getManagedInstance(), timeout);
        return;
    }
}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "inclusiveOrExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "elementValuePair"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        if (state.failed) return ;
        dbg.location(874,44);
        match(input,49,FOLLOW_49_in_synpred262_Java5485); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "formalParameter"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "interfaceMemberDecl"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotationTypeElementRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotation"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "creator"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "constantExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "catchClause"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "constantDeclaratorsRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "forControl"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "methodBody"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "interfaceMethodOrFieldDecl"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "arguments"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,40,FOLLOW_40_in_synpred211_Java4597); if (state.failed) return ;
        dbg.location(778,14);
        match(input,51,FOLLOW_51_in_synpred211_Java4599); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "classOrInterfaceModifiers"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "classCreatorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "innerCreator"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "additiveExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "enumConstants"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "statementExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "primary"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "conditionalOrExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "enhancedForControl"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/NotEqualTypeExpression.java`
#### Snippet
```java
            }
            buf.append("1=1");
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "explicitConstructorInvocation"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "variableDeclarators"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "elementValue"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "classOrInterfaceModifier"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "forInit"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "packageDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "expressionList"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "methodDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "memberDecl"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "literal"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "elementValueArrayInitializer"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "classBody"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "booleanLiteral"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "enumConstant"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "type"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "integerLiteral"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "arrayInitializer"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "classBodyDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "modifiers"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "voidInterfaceMethodDeclaratorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "relationalOp"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "explicitGenericInvocation"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "block"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "memberDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        if (state.failed) return ;
        dbg.location(496,64);
        match(input,26,FOLLOW_26_in_synpred117_Java2493); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotationMethodOrConstantRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "qualifiedName"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "assignmentOperator"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,42,FOLLOW_42_in_synpred199_Java4244); if (state.failed) return ;
        dbg.location(727,22);
        match(input,51,FOLLOW_51_in_synpred199_Java4246); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "constantDeclaratorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "relationalExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "exclusiveOrExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotationTypeBody"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotationTypeElementDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "arrayCreatorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "equalityExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "switchLabel"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "importDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,29,FOLLOW_29_in_synpred236_Java5106); if (state.failed) return ;
        dbg.location(837,21);
        match(input,Identifier,FOLLOW_Identifier_in_synpred236_Java5108); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "interfaceBodyDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "typeBound"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "interfaceMethodDeclaratorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,40,FOLLOW_40_in_synpred198_Java4202); if (state.failed) return ;
        dbg.location(722,18);
        match(input,51,FOLLOW_51_in_synpred198_Java4204); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "catches"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "constructorDeclaratorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "normalInterfaceDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "variableModifier"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,40,FOLLOW_40_in_synpred215_Java4724); if (state.failed) return ;
        dbg.location(793,14);
        match(input,40,FOLLOW_40_in_synpred215_Java4726); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "modifier"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "enumConstantName"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        if (state.failed) return ;
        dbg.location(666,35);
        match(input,75,FOLLOW_75_in_synpred180_Java3802); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "compilationUnit"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "createdName"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "switchBlockStatementGroups"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotationConstantRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotationName"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "castExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "variableInitializer"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "primitiveType"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "unaryExpressionNotPlusMinus"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "switchBlockStatementGroup"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "enumBody"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "enumDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,29,FOLLOW_29_in_synpred242_Java5160); if (state.failed) return ;
        dbg.location(841,25);
        match(input,Identifier,FOLLOW_Identifier_in_synpred242_Java5162); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "typeArgument"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "packageOrTypeName"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "typeParameter"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "multiplicativeExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "nonWildcardTypeArguments"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "typeList"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "superSuffix"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "forUpdate"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotations"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "genericMethodOrConstructorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "variableModifiers"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "elementValuePairs"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "interfaceMethodOrFieldRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "unaryExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "qualifiedNameList"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "shiftOp"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        if (state.failed) return ;
        dbg.location(667,33);
        match(input,75,FOLLOW_75_in_synpred181_Java3816); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "typeName"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "constructorBody"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "localVariableDeclarationStatement"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "enumBodyDeclarations"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,42,FOLLOW_42_in_synpred212_Java4631); if (state.failed) return ;
        dbg.location(781,14);
        match(input,51,FOLLOW_51_in_synpred212_Java4633); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "andExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "blockStatement"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,42,FOLLOW_42_in_synpred216_Java4760); if (state.failed) return ;
        dbg.location(796,18);
        match(input,42,FOLLOW_42_in_synpred216_Java4762); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "classOrInterfaceDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "statement"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "variableDeclarator"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "genericMethodOrConstructorDecl"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,42,FOLLOW_42_in_synpred200_Java4287); if (state.failed) return ;
        dbg.location(734,18);
        match(input,51,FOLLOW_51_in_synpred200_Java4289); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "interfaceDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "typeDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "identifierSuffix"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "interfaceBody"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "classOrInterfaceType"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "constantDeclarator"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "interfaceGenericMethodDecl"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "fieldDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "instanceOfExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "normalClassDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotationMethodRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "formalParameterDeclsRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "variableDeclaratorId"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "voidMethodDeclaratorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "expression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "shiftExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "classDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        match(input,42,FOLLOW_42_in_synpred217_Java4798); if (state.failed) return ;
        dbg.location(801,14);
        match(input,42,FOLLOW_42_in_synpred217_Java4800); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "typeParameters"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "conditionalAndExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "localVariableDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "typeArguments"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "conditionalExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "annotationTypeDeclaration"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "methodDeclaratorRest"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "selector"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "parExpression"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "formalParameterDecls"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        if (state.failed) return ;
        dbg.location(848,25);
        match(input,49,FOLLOW_49_in_synpred249_Java5245); if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

        state._fsp--;
        if (state.failed) return ;

        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "defaultValue"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }

        return ;
    }
    // $ANTLR end "formalParameters"
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EqualTypeExpression.java`
#### Snippet
```java
            }
            buf.append("1=1");
            return;
        }
    }
```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/RangeResultObjectProvider.java`
#### Snippet
```java
            return size;
        size = Math.min(_endIdx, size) - _startIdx;
        return (size < 0) ? 0 : size;
    }

```

## RuleId[id=SimplifiableConditionalExpression]
### SimplifiableConditionalExpression
`StringUtil.isEmpty(enabledStr) ? true :
Boolean.parseBoolean(enabledStr)` can be simplified to 'StringUtil.isEmpty(enabledStr) \|\| Boolean.parseBoolean(enabledStr)'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
            throws SAXException {
        String enabledStr = attrs.getValue("enabled");
        boolean enabled = (Boolean) (StringUtil.isEmpty(enabledStr) ? true :
            Boolean.parseBoolean(enabledStr));

```

### SimplifiableConditionalExpression
`(val != null && val.equals("LAZY")) ? false : true` can be simplified to 'val==null \|\| !val.equals("LAZY")'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
        throws SAXException {
        String val = attrs.getValue("fetch");
        boolean dfg = (val != null && val.equals("LAZY")) ? false : true;

        // We need to toggle the DFG explicit flag here because this is used for an optimization when selecting an
```

### SimplifiableConditionalExpression
`(val != null && val.equals("LAZY")) ? false : true` can be simplified to 'val==null \|\| !val.equals("LAZY")'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
        throws SAXException {
        String val = attrs.getValue("fetch");
        boolean dfg = (val != null && val.equals("LAZY")) ? false : true;

        // We need to toggle the DFG explicit flag here because this is used for an optimization when selecting an
```

### SimplifiableConditionalExpression
`_invert ? !result : result` can be simplified to '_invert != result'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
        public boolean selects(Attribute<? super X, ?> attr) {
            boolean result = _type == null || Filters.canConvert(attr.getJavaType(), _type, false);
            return _invert ? !result : result;
        }

```

### SimplifiableConditionalExpression
`_invert ? !result : result` can be simplified to '_invert != result'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            FieldMetaData fmd = ((Members.Member<?, ?>) attr).fmd;
            boolean result = fmd.isVersion();
            return _invert ? !result : result;
        }

```

### SimplifiableConditionalExpression
`_invert ? !attr.getName().equals(_name) : attr.getName()
.equals(_name)` can be simplified to '_invert != attr.getName().equals(_name)'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
        @Override
        public boolean selects(Attribute<? super X, ?> attr) {
            return _invert ? !attr.getName().equals(_name) : attr.getName()
                    .equals(_name);
        }
```

### SimplifiableConditionalExpression
`_invert ? attr.getDeclaringType() != owner : attr
.getDeclaringType() == owner` can be simplified to '_invert == (attr.getDeclaringType() != owner)'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
        @Override
        public boolean selects(Attribute<? super X, ?> attr) {
            return _invert ? attr.getDeclaringType() != owner : attr
                    .getDeclaringType() == owner;
        }
```

### SimplifiableConditionalExpression
`_invert ? !result : result` can be simplified to '_invert != result'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            boolean result = (attr instanceof PluralAttribute<?, ?, ?>)
                    && ((PluralAttribute<?, ?, ?>) attr).getCollectionType() == _category;
            return _invert ? !result : result;
        }

```

### SimplifiableConditionalExpression
`_invert ? !result : result` can be simplified to '_invert != result'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                    && (_valueType == null
                    || ((MapAttribute<?, ?, ?>) attr).getElementType().getJavaType() == _valueType);
            return _invert ? !result : result;
        }

```

### SimplifiableConditionalExpression
`_invert ? !result : result` can be simplified to '_invert != result'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                    || ((PluralAttribute<?, ?, ?>) attr).getElementType().getJavaType()
                         == _elementType);
            return _invert ? !result : result;
        }

```

### SimplifiableConditionalExpression
`_invert ? !result : result` can be simplified to '_invert != result'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
        public boolean selects(Attribute<? super X, ?> attr) {
            boolean result = ((Members.Member<?, ?>) attr).fmd.isPrimaryKey();
            return _invert ? !result : result;
        }

```

### SimplifiableConditionalExpression
`_invert ? attr.isCollection() : !attr.isCollection()` can be simplified to '_invert == attr.isCollection()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
        @Override
        public boolean selects(Attribute<? super X, ?> attr) {
            return _invert ? attr.isCollection() : !attr.isCollection();
        }

```

### SimplifiableConditionalExpression
`_autoAliased == null ? true : _autoAliased` can be simplified to '_autoAliased == null \|\| _autoAliased'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SelectionImpl.java`
#### Snippet
```java
     */
    boolean isAutoAliased() {
        return _autoAliased == null ? true : _autoAliased;
    }

```

### SimplifiableConditionalExpression
`(delegator == null) ? false : getDelegatorParent().isRegistered(selection)` can be simplified to 'delegator!=null \&\& getDelegatorParent().isRegistered(selection)'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
            return true;
        SubqueryImpl<?> delegator = getDelegator();
        return (delegator == null) ? false : getDelegatorParent().isRegistered(selection);
    }

```

### SimplifiableConditionalExpression
`_parent == null ? false : _parent instanceof Join
&& ((Join``)_parent).getJoinType() != JoinType.INNER` can be simplified to '_parent!=null \&\& _parent instanceof Join \&\& ((Join)_parent).getJoinType() != JoinType.INNER'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
        org.apache.openjpa.kernel.exps.Path path = null;
        SubqueryImpl<?> subquery = q.getDelegator();
        boolean allowNull = _parent == null ? false : _parent instanceof Join
            && ((Join<?,?>)_parent).getJoinType() != JoinType.INNER;
        PathImpl<?,?> corrJoin = getCorrelatedJoin(this);
```

### SimplifiableConditionalExpression
`parent == null ? false : parent instanceof Join
&& ((Join``)parent).getJoinType() != JoinType.INNER` can be simplified to 'parent!=null \&\& parent instanceof Join \&\& ((Join)parent).getJoinType() != JoinType.INNER'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java

    protected void traversePath(PathImpl<?,?> parent,  org.apache.openjpa.kernel.exps.Path path, FieldMetaData fmd) {
        boolean allowNull = parent == null ? false : parent instanceof Join
            && ((Join<?,?>)parent).getJoinType() != JoinType.INNER;
        FieldMetaData fmd1 = parent._member == null ? null : parent._member.fmd;
```

### SimplifiableConditionalExpression
`subquery.getRoots() == null ? false : subquery.getRoots().contains(this)` can be simplified to 'subquery.getRoots()!=null \&\& subquery.getRoots().contains(this)'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
     */
    public boolean inSubquery(SubqueryImpl<?> subquery) {
        return subquery != null && (subquery.getRoots() == null ? false : subquery.getRoots().contains(this));
    }

```

### SimplifiableConditionalExpression
`((Boolean) o).equals(Boolean.FALSE) ? true : false` can be simplified to '((Boolean) o).equals(Boolean.FALSE)'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
        switch (typeCode) {
            case BOOLEAN:
                return ((Boolean) o).equals(Boolean.FALSE) ? true : false;
            case BYTE:
                return ((Byte) o) == 0 ? true : false;
```

### SimplifiableConditionalExpression
`((Byte) o) == 0 ? true : false` can be simplified to '((Byte) o) == 0'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
                return ((Boolean) o).equals(Boolean.FALSE) ? true : false;
            case BYTE:
                return ((Byte) o) == 0 ? true : false;
            case SHORT:
                return ((Short) o) == 0 ? true : false;
```

### SimplifiableConditionalExpression
`((Short) o) == 0 ? true : false` can be simplified to '((Short) o) == 0'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
                return ((Byte) o) == 0 ? true : false;
            case SHORT:
                return ((Short) o) == 0 ? true : false;
            case INT:
                return ((Integer) o) == 0 ? true : false;
```

### SimplifiableConditionalExpression
`((Integer) o) == 0 ? true : false` can be simplified to '((Integer) o) == 0'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
                return ((Short) o) == 0 ? true : false;
            case INT:
                return ((Integer) o) == 0 ? true : false;
            case LONG:
                return ((Long) o) == 0L ? true : false;
```

### SimplifiableConditionalExpression
`((Long) o) == 0L ? true : false` can be simplified to '((Long) o) == 0L'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
                return ((Integer) o) == 0 ? true : false;
            case LONG:
                return ((Long) o) == 0L ? true : false;
            case FLOAT:
                return ((Float) o) == 0.0F ? true : false;
```

### SimplifiableConditionalExpression
`((Float) o) == 0.0F ? true : false` can be simplified to '((Float) o) == 0.0F'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
                return ((Long) o) == 0L ? true : false;
            case FLOAT:
                return ((Float) o) == 0.0F ? true : false;
            case CHAR:
                return ((Character) o) == '\u0000' ? true : false;
```

### SimplifiableConditionalExpression
`((Character) o) == '\u0000' ? true : false` can be simplified to '((Character) o) == '\\u0000''
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
                return ((Float) o) == 0.0F ? true : false;
            case CHAR:
                return ((Character) o) == '\u0000' ? true : false;
            case DOUBLE:
                return ((Double) o) == 0.0d ? true : false;
```

### SimplifiableConditionalExpression
`((Double) o) == 0.0d ? true : false` can be simplified to '((Double) o) == 0.0d'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/JavaTypes.java`
#### Snippet
```java
                return ((Character) o) == '\u0000' ? true : false;
            case DOUBLE:
                return ((Double) o) == 0.0d ? true : false;
        }
        return false;
```

### SimplifiableConditionalExpression
`(str == null) ? false : Boolean.valueOf(str)` can be simplified to 'str!=null \&\& Boolean.valueOf(str)'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
    public boolean getBooleanExtension(String vendor, String key) {
        String str = getStringExtension(vendor, key);
        return (str == null) ? false : Boolean.valueOf(str);
    }

```

### SimplifiableConditionalExpression
`(retrieve(field) == Boolean.TRUE) ? true : false` can be simplified to 'retrieve(field) == Boolean.TRUE'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
        @Override
        public boolean fetchBooleanField(int field) {
            return (retrieve(field) == Boolean.TRUE) ? true : false;
        }

```

### SimplifiableConditionalExpression
`val == null ? false : (Boolean)val` can be simplified to 'val!=null \&\& (Boolean)val'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
                        throw new ClassCastException("!(x instanceof Boolean)");
                    return new BooleanId(meta.getDescribedType(),
                        val == null ? false : (Boolean)val);
                default:
                    throw new InternalException();
```

### SimplifiableConditionalExpression
`_interface == null ? false : _interface` can be simplified to '_interface!=null \&\& _interface'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
        if (!_type.isInterface())
            return false;
        return _interface == null ? false : _interface;
    }

```

### SimplifiableConditionalExpression
`(_closed) ? false : super.hasNext()` can be simplified to '!_closed \&\& super.hasNext()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
        @Override
        public boolean hasNext() {
            return (_closed) ? false : super.hasNext();
        }

```

### SimplifiableConditionalExpression
`(_closed) ? false : super.hasNext()` can be simplified to '!_closed \&\& super.hasNext()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
        @Override
        public boolean hasNext() {
            return (_closed) ? false : super.hasNext();
        }

```

### SimplifiableConditionalExpression
`val == null ? false : (Boolean) val` can be simplified to 'val!=null \&\& (Boolean) val'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
                case JavaTypes.BOOLEAN:
                    sm.settingBooleanField(into, i, sm.fetchBooleanField(i),
                            val == null ? false : (Boolean) val,
                            set);
                    break;
```

### SimplifiableConditionalExpression
`_affirmation ? matches : !matches` can be simplified to '_affirmation == matches'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/MatchesExpression.java`
#### Snippet
```java
        SimpleRegex re = new SimpleRegex(str, uncase);
        boolean matches = re.matches(o1.toString());
        return _affirmation ? matches : !matches;
    }
}
```

### SimplifiableConditionalExpression
`(_flags & FLAG_EMBED_DEREF) == 0 ? false : true` can be simplified to '(_flags \& FLAG_EMBED_DEREF)!=0'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java

    public boolean getDereferencedEmbedDependent() {
        return ((_flags & FLAG_EMBED_DEREF) == 0 ? false : true);
    }

```

### SimplifiableConditionalExpression
`value == null ? false :
(Boolean) value` can be simplified to 'value!=null \&\& (Boolean) value'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ReflectingPersistenceCapable.java`
#### Snippet
```java
        switch (meta.getField(i).getDeclaredTypeCode()) {
            case JavaTypes.BOOLEAN:
                sm.providedBooleanField(this, i, value == null ? false :
                        (Boolean) value);
                break;
```

### SimplifiableConditionalExpression
`(o == null) ? false : (Boolean) o` can be simplified to 'o!=null \&\& (Boolean) o'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    public static boolean getBoolean(Object target, Method getter) {
        Object o = get(target, getter);
        return (o == null) ? false : (Boolean) o;
    }

```

### SimplifiableConditionalExpression
`node.getChildCount() <= 1 ||
lastChild(node).id == JJTASCENDING ? true : false` can be simplified to 'node.getChildCount() \<= 1 \|\| lastChild(node).id == JJTASCENDING'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

                // ommission of ASC/DESC token implies ascending
                exps.ascending[i] = node.getChildCount() <= 1 ||
                    lastChild(node).id == JJTASCENDING ? true : false;
            }
            // check if order by select item result alias
```

### SimplifiableConditionalExpression
`(val.getXmlMapping() == null) ? false
: val.getXmlMapping().isXmlAttribute()` can be simplified to 'val.getXmlMapping()!=null \&\& val.getXmlMapping().isXmlAttribute()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
        Class rc = Filters.wrap(val.getType());
        int type = getJDBCType(JavaTypes.getTypeCode(rc), false);
        boolean isXmlAttribute = (val.getXmlMapping() == null) ? false
                : val.getXmlMapping().isXmlAttribute();
        SQLBuffer newBufer = new SQLBuffer(this);
```

### SimplifiableConditionalExpression
`(obj instanceof Column) ? ((Column) obj).getType() == Types.CLOB && !((Column) obj).isXML()
: false` can be simplified to 'obj instanceof Column \&\& ((Column) obj).getType() == Types.CLOB \&\& !((Column) obj).isXML()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/ResultSetResult.java`
#### Snippet
```java
            metaTypeCode = ((Column) obj).getJavaType();

        boolean isClob = (obj instanceof Column) ? ((Column) obj).getType() == Types.CLOB && !((Column) obj).isXML()
                : false;
        obj = translate(obj, joins);
```

### SimplifiableConditionalExpression
`(val.getXmlMapping() == null)
? false : val.getXmlMapping().isXmlAttribute()` can be simplified to 'val.getXmlMapping()!=null \&\& val.getXmlMapping().isXmlAttribute()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLServerDictionary.java`
#### Snippet
```java
        Class<?> rc = Filters.wrap(val.getType());
        int type = getJDBCType(JavaTypes.getTypeCode(rc), false);
        boolean isXmlAttribute = (val.getXmlMapping() == null)
            ? false : val.getXmlMapping().isXmlAttribute();
        buf.append(val.getColumnAlias(val.getFieldMapping().getColumns()[0]))
```

### SimplifiableConditionalExpression
`(isJDBC3) ? metaData.supportsGetGeneratedKeys() : false` can be simplified to 'isJDBC3 \&\& metaData.supportsGetGeneratedKeys()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            // Auto-detect generated keys retrieval support unless user specified it.
            if (supportsGetGeneratedKeys == null) {
                supportsGetGeneratedKeys =  (isJDBC3) ? metaData.supportsGetGeneratedKeys() : false;
            }
            if (log.isInfoEnabled()) {
```

### SimplifiableConditionalExpression
`supportsDelimitedIdentifiers == null ? false : supportsDelimitedIdentifiers` can be simplified to 'supportsDelimitedIdentifiers!=null \&\& supportsDelimitedIdentifiers'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
    @Override
    public boolean getSupportsDelimitedIdentifiers() {
        return (supportsDelimitedIdentifiers == null ? false : supportsDelimitedIdentifiers);
    }

```

### SimplifiableConditionalExpression
`(list == null) ? false : list.indexOf(key) != -1` can be simplified to 'list!=null \&\& list.indexOf(key) != -1'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java

    private static boolean contains(String list, String key) {
    	return (list == null) ? false : list.indexOf(key) != -1;
    }

```

### SimplifiableConditionalExpression
`field.getValueInfo().getColumns().size() > 0 ? true : false` can be simplified to 'field.getValueInfo().getColumns().size() \> 0'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java

    public boolean hasJoinColumn(FieldMapping field) {
        boolean hasJoinColumn = (field.getValueInfo().getColumns().size() > 0 ? true : false);
        return hasJoinColumn;
    }
```

### SimplifiableConditionalExpression
`!DBIdentifier.isNull(field.getMappingInfo().getTableIdentifier()) ? true : false` can be simplified to '!DBIdentifier.isNull(field.getMappingInfo().getTableIdentifier())'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java

    public boolean hasJoinTable(FieldMapping field) {
        boolean hasJoinTable = !DBIdentifier.isNull(field.getMappingInfo().getTableIdentifier()) ? true : false;
        return hasJoinTable;
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EnumValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/XMLValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MaxEmbeddedLobFieldStrategy.java`
#### Snippet
```java

        DBDictionary dict = field.getMappingRepository().getDBDictionary();
        DBIdentifier fieldName = DBIdentifier.newColumn(field.getName(), dict != null ? dict.delimitAll() : false);

        // get value columns
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LobFieldStrategy.java`
#### Snippet
```java
        vinfo.assertNoForeignKey(field, !adapt);
        DBDictionary dict = field.getMappingRepository().getDBDictionary();
        DBIdentifier fieldName = DBIdentifier.newColumn(field.getName(), dict != null ? dict.delimitAll() : false);

        Column tmpCol = new Column();
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ColumnVersionStrategy.java`
#### Snippet
```java
           tmplate.setJavaType(getJavaType());
           DBDictionary dict = vers.getMappingRepository().getDBDictionary();
           DBIdentifier versName = DBIdentifier.newColumn("versn", dict != null ? dict.delimitAll() : false);
           tmplate.setIdentifier(versName);

```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/UntypedPCValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/InValueDiscriminatorStrategy.java`
#### Snippet
```java
        tmplate.setJavaType(getJavaType());
        DBDictionary dict = cls.getMappingRepository().getDBDictionary();
        DBIdentifier typName = DBIdentifier.newColumn("typ", dict != null ? dict.delimitAll() : false);
        tmplate.setIdentifier(typName);

```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/PrimitiveFieldStrategy.java`
#### Snippet
```java
        // Determine whether to delimit the base field name
        DBDictionary dict = field.getMappingRepository().getDBDictionary();
        DBIdentifier fieldName = DBIdentifier.newColumn(field.getName(), dict != null ? dict.delimitAll() : false);
        // get value columns
        Column tmpCol = new Column();
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/CharArrayStreamValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/CharArrayValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ElementEmbedValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ImmutableValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerStrategies.java`
#### Snippet
```java

        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        Column[] cols = vm.getHandler().map(vm, colName.getName(), io, adapt);
        if (cols.length > 0 && cols[0].getTable() == null) {
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ClobValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
        boolean adapt, List cols, List args) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        map(vm, colName, io, adapt, cols, args);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/BlobValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ByteArrayValueHandler.java`
#### Snippet
```java
        boolean adapt) {
        DBDictionary dict = vm.getMappingRepository().getDBDictionary();
        DBIdentifier colName = DBIdentifier.newColumn(name, dict != null ? dict.delimitAll() : false);
        return map(vm, colName, io, adapt);
    }
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/FullClassStrategy.java`
#### Snippet
```java
            Column id = new Column();
            DBDictionary dict = cls.getMappingRepository().getDBDictionary();
            DBIdentifier idName = DBIdentifier.newColumn("id", dict != null ? dict.delimitAll() : false);
            id.setIdentifier(idName);
            id.setJavaType(JavaTypes.LONG);
```

### SimplifiableConditionalExpression
`dict != null ? dict.delimitAll() : false` can be simplified to 'dict != null \&\& dict.delimitAll()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StringFieldStrategy.java`
#### Snippet
```java

        DBDictionary dict = field.getMappingRepository().getDBDictionary();
        DBIdentifier fieldName = DBIdentifier.newColumn(field.getName(), dict != null ? dict.delimitAll() : false);

        // get value columns
```

### SimplifiableConditionalExpression
`fetch.hasFetchInnerJoin(field.getFullName(false)) ?
true : false` can be simplified to 'fetch.hasFetchInnerJoin(field.getFullName(false))'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
        // produces invalid SQL
        ClassMapping cls = field.getIndependentTypeMappings()[0];
        boolean forceInner = fetch.hasFetchInnerJoin(field.getFullName(false)) ?
                true : false;
        sel.select(cls, field.getSelectSubclasses(), store, fetch,
```

### SimplifiableConditionalExpression
`(lockLevel >= MixedLockLevels.LOCK_PESSIMISTIC_READ)
? super.selectForUpdate(sel, lockLevel) : false` can be simplified to 'lockLevel \>= MixedLockLevels.LOCK_PESSIMISTIC_READ \&\& super.selectForUpdate(sel, lockLevel)'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/MixedLockManager.java`
#### Snippet
```java
    @Override
    public boolean selectForUpdate(Select sel, int lockLevel) {
        return (lockLevel >= MixedLockLevels.LOCK_PESSIMISTIC_READ)
            ? super.selectForUpdate(sel, lockLevel) : false;
    }
```

### SimplifiableConditionalExpression
`useLiteral != null ? !(Boolean)useLiteral : true` can be simplified to 'useLiteral==null \|\| !(Boolean)useLiteral'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Lit.java`
#### Snippet
```java
        }
        Object useLiteral = ctx.fetch.getHint(QueryHints.HINT_USE_LITERAL_IN_SQL);
        boolean useParamToken = useLiteral != null ? !(Boolean)useLiteral : true;
        sql.appendValue(lstate.sqlValue, lstate.getColumn(index), null, useParamToken);
    }
```

### SimplifiableConditionalExpression
`pc == null ? false : conf.isReplicated(pc.getClass())` can be simplified to 'pc!=null \&\& conf.isReplicated(pc.getClass())'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/SliceImplHelper.java`
#### Snippet
```java
	 */
    public static boolean isReplicated(Object pc, DistributedConfiguration conf) {
        return pc == null ? false : conf.isReplicated(pc.getClass());
	}

```

## RuleId[id=JavadocLinkAsPlainText]
### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/UUIDGenerator.java`
#### Snippet
```java
/**
 * UUID value generator.  Type 1 generator is based on the time-based generator
 * in the Apache Commons Id project:  http://jakarta.apache.org/commons/sandbox
 * /id/uuid.html  The type 4 generator uses the standard Java UUID generator.
 *
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/UUIDGenerator.java`
#### Snippet
```java
 *
 * In spirit, implements the IETF UUID draft specification, found here:<br />
 * http://www1.ics.uci.edu/~ejw/authoring/uuid-guid/draft-leach-uuids-guids-01
 * .txt
 *
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTCommand.java`
#### Snippet
```java
 * A JEST command often attaches special semantics to a standard URI syntax. For example, all JEST
 * URI enforces that the first segment of a servlet path denotes the command moniker e.g. the URI<br>
 * <code>http://www.jpa.com/jest/find/plan=myPlan?type=Person&1234</code><br>
 * with context root <code>http://www.jpa.com/jest</code> has the servlet path <code>/find/plan=myPlan</code>
 * and query string <code>type=Person&1234</code>.
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTCommand.java`
#### Snippet
```java
 * URI enforces that the first segment of a servlet path denotes the command moniker e.g. the URI<br>
 * <code>http://www.jpa.com/jest/find/plan=myPlan?type=Person&1234</code><br>
 * with context root <code>http://www.jpa.com/jest</code> has the servlet path <code>/find/plan=myPlan</code>
 * and query string <code>type=Person&1234</code>.
 * <br>
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
     * <br>
     * For example, let the original request URL be:<br>
     * <code>http://host:port/demo/jest/find?type=Actor&Robert</code>
     * <br>
     * The response to this request is a HTML page that contained a hyperlink to <code>jest.css</code> stylesheet
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JESTContext.java`
#### Snippet
```java
     * <br>
     * The browser will resolve the hyperlink by sending back another request as<br>
     * <code>http://host:port/demo/jest/find/jest.css</code>
     * <br>
     *
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
     * can be found here:
     *
     * http://publib.boulder.ibm.com/infocenter/idshelp/v10/index.jsp?topic=/com.ibm.em.doc/errors_ids100.html
     *
     * @param errorStates classification of SQL error states by their specific nature. The keys of the
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
     * Defaults to false, since this may fail in the presence of InnoDB tables
     * with foreign keys.
     * @link http://dev.mysql.com/doc/refman/5.0/en/delete.html
     */
    public boolean optimizeMultiTableDeletes = false;
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MySQLDictionary.java`
#### Snippet
```java
     * Defaults to false, since this may fail in the presence of InnoDB tables
     * with foreign keys.
     * @link http://dev.mysql.com/doc/refman/5.0/en/delete.html
     */
    public boolean optimizeMultiTableDeletes = false;
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
     * system assigned sequence generator of the format: table_column_seq
     *
     * @link http://www.postgresql.org/docs/current/static/functions-info.html
     */
    public boolean isOwnedSequence(DBIdentifier name, DBIdentifier schema, Connection conn) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java

/**
 * Dictionary for H2 ({@link http://www.h2database.com}).
 *
 * @since 0.9.7
```

## RuleId[id=CollectionAddedToSelf]
### CollectionAddedToSelf
'removeAll()' called on collection `_columnNamesList` with itself as argument
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        if (_columnNamesList.size() > 0) {
            _columnNames = _columnNamesList.toArray(_columnNames);
            _columnNamesList.removeAll(_columnNamesList);
        }
    }
```

## RuleId[id=FieldCanBeLocal]
### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java

    // Hold fetch group info
    private FetchGroupImpl[] _fgs = null;
    private List<FetchGroupImpl> _fgList = null;
    private List<String> _referencedFgList = null;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/AliasContext.java`
#### Snippet
```java
	private Map<ExpressionImpl, String> _aliases =
		new HashMap<>();
	private final MetaDataRepository _repos;

	public AliasContext(MetaDataRepository repos) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinitionImpl.java`
#### Snippet
```java
	
    private static final long serialVersionUID = 1L;
    private final QueryBuilderImpl _builder;
	private List<AbstractDomainObject> _domains;
	private List<PathExpression> _groupBys;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    // AliasContext
    private int aliasCount = 0;
    private static String ALIAS_BASE = "autoAlias";

    private Map<Selection<?>,Value> _variables = new HashMap<>();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/JarFileURLMetaDataIterator.java`
#### Snippet
```java
    private final MetaDataFilter _filter;
    private final JarFile _jarFile;
    private final JarEntry _jarTargetEntry;

    private int index = 0;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierRule.java`
#### Snippet
```java
    private Set<String> _reservedWords = null;
    private boolean _delimitReservedWords = false;
    private String _wildcard = "%";

    public void setName(String name) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WASManagedRuntime.java`
#### Snippet
```java

    private com.ibm.websphere.jtaextensions.ExtendedJTATransaction _extendedTransaction = null;
    private OpenJPAConfiguration _conf = null;
    private Log _log = null;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/XMLClassMetaData.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private Class _type;
    private int _code = JavaTypes.OBJECT;
    private int _xmltype = XMLTYPE;
    private String _name = null;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/XMLFieldMetaData.java`
#### Snippet
```java
    private String _xmlnamespace = null;
    private Class _decType = Object.class;
    private int _decCode = JavaTypes.OBJECT;
    private Class _type = Object.class;
    private int _code = JavaTypes.OBJECT;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
    private boolean _orderDirty = false;
    private boolean _cachePreparedQuery = true;
    private boolean _cacheFinderQuery = true;
    private boolean _suppressBatchOLELogging = false;
    private boolean _allowReferenceToSiblingContext = false;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryCacheStoreQuery.java`
#### Snippet
```java
    private final QueryCache _cache;
    private StoreContext _sctx;
    private MetaDataRepository _repos;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java
    extends AbstractStoreManager {

    private XMLConfiguration _conf;
    private XMLStore _store;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java

    private boolean showsRowCount;
    private boolean showsBasicAttr;
    private boolean showsSingularAttr;
    private boolean showsPluralAttr;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private boolean showsRowCount;
    private boolean showsBasicAttr;
    private boolean showsSingularAttr;
    private boolean showsPluralAttr;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityDataModel.java`
#### Snippet
```java
    private boolean showsBasicAttr;
    private boolean showsSingularAttr;
    private boolean showsPluralAttr;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
    private JSpinner _goto;
    private JButton[] _navButtons;
    private int MAX_BUTTONS = 10; // Total number of navigation buttons excluding the PREVIOUS and NEXT button.

    // The slide
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
    private String _dir;
    private List<URL> _slideURLs;
    private JButton _prev, _next;
    private JSpinner _goto;
    private JButton[] _navButtons;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
    private String _dir;
    private List<URL> _slideURLs;
    private JButton _prev, _next;
    private JSpinner _goto;
    private JButton[] _navButtons;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PowerPointViewer.java`
#### Snippet
```java
    private List<WeakReference<ImageIcon>> _slides = new ArrayList<>();
    private int _total;      // Total number of slides in the deck.
    private String _dir;
    private List<URL> _slideURLs;
    private JButton _prev, _next;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private JProgressBar progressBar;
    private JProgressBar memoryBar;
    private JLabel messageText;
    private TaskProgress task;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
     */
    class TaskProgress extends SwingWorker<Void, Integer> {
        private long startTimeInMillis;
        private long _total = 100;
        private int _interval = 100;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/SupplyPage.java`
#### Snippet
```java

    private final JButton _supply;
    private final JButton _view;
    private final JLabel  _title;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/SupplyPage.java`
#### Snippet
```java
    private final JButton _supply;
    private final JButton _view;
    private final JLabel  _title;

    private static int REORDER_LIMIT    = 10;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/SupplyPage.java`
#### Snippet
```java
    private final EntityTableView<Inventory> _lowInventories;

    private final JButton _supply;
    private final JButton _view;
    private final JLabel  _title;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/DeliveryPage.java`
#### Snippet
```java
    private final JRadioButton _showPending;
    private final JRadioButton _showDelivered;
    private final JRadioButton _showBoth;

    public DeliveryPage(final OpenBookService service) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/DeliveryPage.java`
#### Snippet
```java
    private final JLabel _title;
    private final JRadioButton _showPending;
    private final JRadioButton _showDelivered;
    private final JRadioButton _showBoth;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/DeliveryPage.java`
#### Snippet
```java
    private final OpenBookService _service;
    private final JLabel _title;
    private final JRadioButton _showPending;
    private final JRadioButton _showDelivered;
    private final JRadioButton _showBoth;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/DeliveryPage.java`
#### Snippet
```java
public class DeliveryPage extends JPanel {
    private static final long serialVersionUID = 1L;
    private final JButton _deliver;
    private EntityTableView<PurchaseOrder> _orders;
    private final OpenBookService _service;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/BuyBookPage.java`
#### Snippet
```java
    private final Customer          _customer;
    private final SearchPanel       _searchPanel;
    private final BuyPanel          _buyPanel;
    private final SelectBookPanel   _selectPanel;
    private final ShoppingCartPanel _cartPanel;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/BuyBookPage.java`
#### Snippet
```java
    private final OpenBookService   _service;
    private final Customer          _customer;
    private final SearchPanel       _searchPanel;
    private final BuyPanel          _buyPanel;
    private final SelectBookPanel   _selectPanel;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    private JTabbedPane _tabbedPane;
    private JTabbedPane _outputPane;
    private StatusBar   _statusBar;
    private ScrollingTextPane   _sqlLog;
    private SQLLogger _sqlListener;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
     * The primary graphic widgets used to invoke and display the results of the actions.
     */
    private JToolBar    _toolBar;
    private JTree       _navigator;
    private JTabbedPane _tabbedPane;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java

    // batch limit
    private int defaultBatchLimit = 100;

    public OracleDictionary() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLServerDictionary.java`
#### Snippet
```java
        Localizer.forPackage(SQLServerDictionary.class);

    private String schemaCase = SCHEMA_CASE_PRESERVE;
    /**
     * Flag whether to treat UNIQUEIDENTIFIER as VARBINARY or VARCHAR
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/JDBCExpressionFactory.java`
#### Snippet
```java
    private int _getMapValueAlias = 0;

    private boolean _isBooleanLiteralAsNumeric = true;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
extends SimpleDriverDataSource implements Configurable, Closeable {

    private static String DBCPPACKAGENAME = "org.apache.commons.dbcp2";
    private static String DBCPBASICDATASOURCENAME = "org.apache.commons.dbcp2.BasicDataSource";
    private static Class<?> _dbcpClass;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/UniqueResultObjectProvider.java`
#### Snippet
```java
public class UniqueResultObjectProvider implements ResultObjectProvider {
    private final ResultObjectProvider[] _rops;
    private final StoreQuery _query;
    private final QueryExpressions[] _exps;
    private Object _single;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `openjpa-tools/openjpa-fetch-statistics/src/main/java/org/apache/openjpa/enhance/stats/FetchStatisticsAuxEnhancer.java`
#### Snippet
```java
public class FetchStatisticsAuxEnhancer implements AuxiliaryEnhancer {

    private final String IGNORE_METHODS_REGEX = "(pc(.)*GenericContext)?(pc(.)*StateManager)?"
        + "(pc(.)*DetachedState)?(pc(.)*EnhancementContractVersion)?(pc(.)*ManagedFieldCount)?(pc(.)*GetVersion)?";

```

## RuleId[id=IfStatementMissingBreakInLoop]
### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        Throwable[] t = exceps.toArray(new Throwable[exceps.size()]);
        for (Throwable throwable : t) {
            if (throwable instanceof OpenJPAException
                    && ((OpenJPAException) throwable).isFatal())
                fatal = true;
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerFieldStrategy.java`
#### Snippet
```java
            boolean marked = false;
            for (int i = 0; !marked && i < _cols.length; i++)
                if (_cols[i].isAutoAssigned())
                    marked = true;
            if (!marked)
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedFieldStrategy.java`
#### Snippet
```java
                cols = fields[i].getColumns();
                for (Column column : cols)
                    if (column.equals(col))
                        found = true;
            }
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
                        ps.close();
                    } catch (SQLException sqex) {
                        throw SQLExceptions.getStore(sqex, ps, _dict);
                    }
                }
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
            if (v instanceof String)
                return Boolean.valueOf(v.toString());
            if (v instanceof Boolean)
                return v;
            return v;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
            return false;
        List<? extends AnnotationMirror> mirrors = e.getAnnotationMirrors();
        if (annos == null) {
            for (AnnotationMirror mirror : mirrors) {
                String name = mirror.getAnnotationType().toString();
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                ClassMetaData meta;
                if (fmd != null) {
                    if (op == AbstractExpressionBuilder.CONTAINS_TYPE_ELEMENT ||
                        op == AbstractExpressionBuilder.CONTAINS_TYPE_VALUE) {
                        val2.setImplicitType(fmd.getElement().getDeclaredType());
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/CodeFormat.java`
#### Snippet
```java
        if (_braceOnSameLine)
            return " throws";
        if (_braceAtSameTabLevel)
            return getEndl() + getTab(tabLevel) + "throws";
        return getEndl() + getTab(tabLevel) + "throws";
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/CodeFormat.java`
#### Snippet
```java
        if (_braceOnSameLine)
            return " implements";
        if (_braceAtSameTabLevel)
            return getEndl() + getTab(tabLevel) + "implements";
        return getEndl() + getTab(tabLevel) + "implements";
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/CodeFormat.java`
#### Snippet
```java
        if (_braceOnSameLine)
            return " extends";
        if (_braceAtSameTabLevel)
            return getEndl() + getTab(tabLevel) + "extends";
        return getEndl() + getTab(tabLevel) + "extends";
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Localizer.java`
#### Snippet
```java
        // check for cached version
        Localizer loc = (Localizer) _localizers.get(key);
        if (loc != null)
            return loc;
        else {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
            ClassMetaData meta;
            if (fmd != null) {
                if (op == CONTAINS_TYPE_ELEMENT || op == CONTAINS_TYPE_VALUE) {
                    val2.setImplicitType(fmd.getElement().getDeclaredType());
                    meta = fmd.getElement().getDeclaredTypeMetaData();
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCDataGenerator.java`
#### Snippet
```java
        int objectCount = 0;
        for (int i = 0; i < fmds.length; i++) {
            if (fields) {
                //  if (fields != null && fields.get(index))
                setTarget(code.aload().setParam(1), jumps);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
    public void startTimer(long duration, int interval, TimeUnit unit) {
        progressBar.setEnabled(true);
        if (duration > 0) {
            progressBar.setStringPainted(true);
            progressBar.setMaximum(100);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java
        // check for named inverse
        FieldMapping mapped = field.getMappedByMapping();
        if (mapped != null) {
            field.getValueInfo().assertNoSchemaComponents(field, !adapt);
            mapped.resolve(MetaDataModes.MODE_META | MetaDataModes.MODE_MAPPING);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
        List cols = new ArrayList();
        String mappedByIdValue = field.getMappedByIdValue();
        if (embeddedId != null) {
            FieldMetaData[] fmds = embeddedId.getFields();
            for (FieldMetaData fmd : fmds) {
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/DBCPDriverDataSource.java`
#### Snippet
```java
    protected DataSource getDBCPDataSource(Properties props) {
        if (isDBCPLoaded(getClassLoader())) {
            if (_ds == null) {
                try {
                    Properties dbcpProps = updateDBCPProperties(props);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-examples/openbooks/web/orders.jsp`
#### Snippet
```java
      List<LineItem> items = order.getItems();
      if (order.isDelivered()) {
         if (items != null && items.isEmpty()) {
%>
             Order <%= order.getId() %> has been delivered. Line items of delivered orders are automatically
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `openjpa-examples/openbooks/web/intro.jsp`
#### Snippet
```java
    Object service = session.getAttribute(KEY_SERVICE);

    if (service == null) {
%>
<A name="login"></A>
```

## RuleId[id=SlowListContainsAll]
### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/ListResultList.java`
#### Snippet
```java
    public boolean containsAll(Collection c) {
        assertOpen();
        return _list.containsAll(c);
    }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedVectorProxy.java`
#### Snippet
```java
            load();
        }
        return super.containsAll(collection);
    }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java
    public boolean containsAll(Collection<?> c) {
        try {
            return _del.containsAll(c);
        } catch (RuntimeException re) {
            throw translate(re);
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public boolean containsAll(Collection c) {
            return _res.containsAll(c);
        }

```

## RuleId[id=EqualsOnSuspiciousObject]
### EqualsOnSuspiciousObject
Suspicious call to 'equals()' on 'StringBuilder' object
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java

        SQLBuffer buf = (SQLBuffer) other;
        return _sql.equals(buf._sql)
            && Objects.equals(_params, buf._params);
    }
```

## RuleId[id=SlowAbstractSetRemoveAll]
### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java

        }
        getters.removeAll(unmatched);
        return getters;
    }
```

### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                }
                if (clear != null)
                    independent.removeAll(clear);

                _assignMaps = (ClassMapping[]) independent.toArray
```

## RuleId[id=ConditionalExpressionWithIdenticalBranches]
### ConditionalExpressionWithIdenticalBranches
Conditional expression `(val1 instanceof Type) ? val1.getMetaData() : val1.getMetaData()` with identical branches
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/NotEqualTypeExpression.java`
#### Snippet
```java
            SQLBuffer buf) {
        ClassMapping sub = getSubClassMapping(val1, val2, ctx);
        ClassMapping cm1 = (ClassMapping)((val1 instanceof Type) ? val1.getMetaData() :
            val1.getMetaData());
        if (sub != null && sub.isVerticalStrategy()) {
            ClassMetaData[] subs = cm1.getPCSubclassMetaDatas();
```

### ConditionalExpressionWithIdenticalBranches
Conditional expression `(val1 instanceof Type) ? val1.getMetaData() : val1.getMetaData()` with identical branches
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EqualTypeExpression.java`
#### Snippet
```java
            sel.setJoinedTableClassMeta(selectFrom);
        }
        ClassMapping cm1 = (ClassMapping)((val1 instanceof Type) ? val1.getMetaData() :
            val1.getMetaData());
        if (sub.isVerticalStrategy()) {
            ClassMetaData[] subs = cm1.getPCSubclassMetaDatas();
```

## RuleId[id=PointlessNullCheck]
### PointlessNullCheck
Unnecessary 'null' check before 'isInstance()' call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
     */
    public static boolean isMethod(Element e) {
        return e != null && ExecutableElement.class.isInstance(e)
            && e.getKind() == ElementKind.METHOD;
    }
```

### PointlessNullCheck
Unnecessary 'null' check before 'isInstance()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/Specification.java`
#### Snippet
```java
        if (this == other)
            return true;
        if (other == null || !this.getClass().isInstance(other))
            return false;
        Specification that = (Specification)other;
```

### PointlessNullCheck
Unnecessary 'null' check before 'isSerializable()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/Exceptions.java`
#### Snippet
```java
        // and the id is meaningless anyway
        Object oid = getObjectId(ob);
        if (oid != null && isSerializable(oid))
            return oid;

```

### PointlessNullCheck
Unnecessary 'null' check before 'isInstance()' call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/MethodStoreQuery.java`
#### Snippet
```java
                    while (itr.hasNext()) {
                        obj = itr.next();
                        if (obj == null
                            || !_meta.getDescribedType().isInstance(obj))
                            continue;
```

### PointlessNullCheck
Unnecessary 'null' check before 'equalsIgnoreCase()' call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        // filter out generated sequences used for auto-assign
        String strName = DBIdentifier.isNull(name) ? "" : name.getName();
        return (autoAssignSequenceName != null
            && strName.equalsIgnoreCase(autoAssignSequenceName))
            || (autoAssignSequenceName == null
```

### PointlessNullCheck
Unnecessary 'null' check before 'equals()' call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
            name = idxs[i].getIdentifier();
            if (DBIdentifier.isEmpty(name)
                || (pkName != null && name.equals(pkName))
                || _dict.isSystemIndex(name, table))
                continue;
```

## RuleId[id=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
            try {
                _type = enumType;
                Enum<?>[] values = (Enum<?>[])enumType.getMethod("values", null).invoke(null, (Class<?>[])null);
                map = new Integer[values.length];
                int i = 0;
```

### NullArgumentToVariableArgMethod
Confusing argument `(Class``[])null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
            try {
                _type = enumType;
                Enum<?>[] values = (Enum<?>[])enumType.getMethod("values", null).invoke(null, (Class<?>[])null);
                map = new Integer[values.length];
                int i = 0;
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlTypeClass = Class.forName(
                "javax.xml.bind.annotation.XmlType");
            xmlTypeName = xmlTypeClass.getMethod("name", null);
            xmlTypeNamespace = xmlTypeClass.getMethod("namespace", null);
            xmlRootElementClass = Class.forName(
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                "javax.xml.bind.annotation.XmlType");
            xmlTypeName = xmlTypeClass.getMethod("name", null);
            xmlTypeNamespace = xmlTypeClass.getMethod("namespace", null);
            xmlRootElementClass = Class.forName(
                "javax.xml.bind.annotation.XmlRootElement");
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlRootElementClass = Class.forName(
                "javax.xml.bind.annotation.XmlRootElement");
            xmlRootName = xmlRootElementClass.getMethod("name", null);
            xmlRootNamespace = xmlRootElementClass.getMethod("namespace", null);
            xmlAccessorTypeClass = Class.forName(
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                "javax.xml.bind.annotation.XmlRootElement");
            xmlRootName = xmlRootElementClass.getMethod("name", null);
            xmlRootNamespace = xmlRootElementClass.getMethod("namespace", null);
            xmlAccessorTypeClass = Class.forName(
                "javax.xml.bind.annotation.XmlAccessorType");
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlAccessorTypeClass = Class.forName(
                "javax.xml.bind.annotation.XmlAccessorType");
            xmlAccessorValue = xmlAccessorTypeClass.getMethod("value", null);
            xmlAttributeClass = Class.forName(
                "javax.xml.bind.annotation.XmlAttribute");
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlAttributeClass = Class.forName(
                "javax.xml.bind.annotation.XmlAttribute");
            xmlAttributeName = xmlAttributeClass.getMethod("name", null);
            xmlAttributeNamespace = xmlAttributeClass.getMethod("namespace"
                , null);
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlAttributeName = xmlAttributeClass.getMethod("name", null);
            xmlAttributeNamespace = xmlAttributeClass.getMethod("namespace"
                , null);
            xmlElementClass = Class.forName(
                "javax.xml.bind.annotation.XmlElement");
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlElementClass = Class.forName(
                "javax.xml.bind.annotation.XmlElement");
            xmlElementName = xmlElementClass.getMethod("name", null);
            xmlElementNamespace = xmlElementClass.getMethod("namespace", null);
        } catch (Exception e) {
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                "javax.xml.bind.annotation.XmlElement");
            xmlElementName = xmlElementClass.getMethod("name", null);
            xmlElementNamespace = xmlElementClass.getMethod("namespace", null);
        } catch (Exception e) {
        }
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <T> Predicate qbe(From<?, T> from, T example, ComparisonStyle style) {
        return qbe(from, example, style, null);
    }

```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <T> Predicate qbe(From<?, T> from, T example) {
        return qbe(from, example, qbeStyle(), null);
    }

```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/InvocationManagedRuntime.java`
#### Snippet
```java
                getClassLoader(getClass(), null);
            _method = Class.forName(_clazz, true, loader)
                .getMethod(_methodName, null);
        }
        return (TransactionManager) _method.invoke(null, null);
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/InvocationManagedRuntime.java`
#### Snippet
```java
                .getMethod(_methodName, null);
        }
        return (TransactionManager) _method.invoke(null, null);
    }

```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WLSManagedRuntime.java`
#### Snippet
```java
        Class txHelper = Class.forName
            ("weblogic.transaction.TransactionHelper");
        _txHelperMeth = txHelper.getMethod("getTransactionHelper", null);
        _txManagerMeth = txHelper.getMethod("getTransactionManager", null);
    }
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WLSManagedRuntime.java`
#### Snippet
```java
            ("weblogic.transaction.TransactionHelper");
        _txHelperMeth = txHelper.getMethod("getTransactionHelper", null);
        _txManagerMeth = txHelper.getMethod("getTransactionManager", null);
    }

```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WLSManagedRuntime.java`
#### Snippet
```java
        // return TransactionHelper.getTransactionHelper ().
        //	getTransactionManager ();
        Object o = _txHelperMeth.invoke(null, null);
        return (TransactionManager) _txManagerMeth.invoke(o, null);
    }
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WLSManagedRuntime.java`
#### Snippet
```java
        //	getTransactionManager ();
        Object o = _txHelperMeth.invoke(null, null);
        return (TransactionManager) _txManagerMeth.invoke(o, null);
    }

```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
       try {
            Method sqlcaM2 = sqle.getNextException().getClass()
                             .getMethod(GETSQLCA,null);
            Object sqlca = sqlcaM2.invoke(sqle.getNextException(),
                                          new Object[] {});
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
                                          new Object[] {});
            Method  getSqlErrpMethd = sqlca.getClass().
            getMethod("getSqlErrp", null);
            Method  getSqlWarnMethd = sqlca.getClass().
            getMethod("getSqlWarn", null);
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
            getMethod("getSqlErrp", null);
            Method  getSqlWarnMethd = sqlca.getClass().
            getMethod("getSqlWarn", null);
            Method  getSqlErrdMethd = sqlca.getClass().
            getMethod("getSqlErrd", null);
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
            getMethod("getSqlWarn", null);
            Method  getSqlErrdMethd = sqlca.getClass().
            getMethod("getSqlErrd", null);
            StringBuilder errdStr = new StringBuilder();

```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/JDBCExpressionFactory.java`
#### Snippet
```java
    public Arguments newArgumentList(Value... vs) {
        if (vs == null)
           return new Args(null);
        Val[] vals = new Val[vs.length];
        int i = 0;
```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `_seq == null` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/GeneratorImpl.java`
#### Snippet
```java
        if ((other == null) || (other.getClass() != this.getClass()))
            return false;
        if (_seq == null)
            return false;

```

### ConstantValue
Condition `_seq == null` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/GeneratorImpl.java`
#### Snippet
```java
    @Override
    public int hashCode() {
        return ((_seq == null) ? 0  : _seq.hashCode());
    }

```

### ConstantValue
Condition `_cache == null` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryResultCacheImpl.java`
#### Snippet
```java
    @Override
    public int hashCode() {
        return (_cache == null) ? 0 : _cache.hashCode();
    }

```

### ConstantValue
Condition `_cache == null` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryResultCacheImpl.java`
#### Snippet
```java
        if ((other == null) || (other.getClass() != this.getClass()))
            return false;
        if (_cache == null)
        	return false;

```

### ConstantValue
Condition `rtrn instanceof StringId` is always `true` when reached
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAFacadeHelper.java`
#### Snippet
```java
                        }
                    } else {
                        if (!(rtrn instanceof StringId) || rtrn instanceof StringId && oid instanceof String) {
                            return rtrn;
                        }
```

### ConstantValue
Value `obj` is always 'true'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
        try {
            final Object obj = synchronizeMappings(factory);
            return Boolean.class.cast(obj) ? Boolean.class.cast(obj) : true;
        } finally {
            factory.close();
```

### ConstantValue
Condition `cascades == null` is always `true`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
        Collection<String> cascades = null;
        if (vmd.getCascadePersist() == ValueMetaData.CASCADE_IMMEDIATE) {
            if (cascades == null)
                cascades = new ArrayList<>();
            cascades.add("cascade-persist");
```

### ConstantValue
Value `v` is always 'null'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
            if (expectedType.isPrimitive())
                throw new IllegalArgumentException(_loc.get("param-null-primitive", param).getMessage());
            return v;
        }
        if (getRelaxBindParameterTypeChecking()) {
```

### ConstantValue
Condition `_fetch != null && _hintHandler != null` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
			return true;
		}
		if (_fetch != null && _hintHandler != null) {
			switch (_fetch.getReadLockMode()) {
			case PESSIMISTIC_READ:
```

### ConstantValue
Condition `_hintHandler != null` is always `false` when reached
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
			return true;
		}
		if (_fetch != null && _hintHandler != null) {
			switch (_fetch.getReadLockMode()) {
			case PESSIMISTIC_READ:
```

### ConstantValue
Value `fcPushed` is always 'false'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
			}
		}
		return fcPushed;
	}

```

### ConstantValue
Condition `contextLoader != null` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
            // so that it could be used in the jar validation
            MultiClassLoader mutliClassLoader = (MultiClassLoader) loader;
            contextLoader = (contextLoader != null) ? contextLoader
                    : AccessController.doPrivileged(J2DoPrivHelper.getContextClassLoaderAction());
            mutliClassLoader.addClassLoader(contextLoader);
```

### ConstantValue
Condition `_name == null` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ParameterExpressionImpl.java`
#### Snippet
```java

        // if name is given, then we ignore the index
        if (_name == null && _index != that._index)
            return false;

```

### ConstantValue
Condition `_name == null && _index != that._index` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ParameterExpressionImpl.java`
#### Snippet
```java

        // if name is given, then we ignore the index
        if (_name == null && _index != that._index)
            return false;

```

### ConstantValue
Condition `_parent == null` is always `true`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
                return val;
            }
        } else if (_parent == null) {
            path = factory.newPath();
            path.setMetaData(q.getMetamodel().getRepository().getCachedMetaData(getJavaType()));
```

### ConstantValue
Value `brc` is always 'false'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidationUtils.java`
#### Snippet
```java
                            "No available jakarta.validation APIs"));
                    }
                    return brc;
                }
            }
```

### ConstantValue
Value `brc` is always 'false'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidationUtils.java`
#### Snippet
```java
                    String msg = e.getMessage();
                    log.warn(_loc.get("vlem-creation-warn", msg == null ? e : msg ));
                    return brc;
                }
            }
```

### ConstantValue
Value `o` is always 'null'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/SLF4JLogFactory.java`
#### Snippet
```java
        private String objectToString(Object o) {
            if (o == null)
                return (String) o;
            else
                return o.toString();
```

### ConstantValue
Condition `corrJoins != null` is always `true`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            } else {
                org.apache.openjpa.kernel.exps.Path parentPath = null;
                if (corrJoins != null && corrJoins.contains(_parent)) {
                    Value var = getVariableForCorrPath(subquery, correlatedParentPath);
                    parentPath = factory.newPath(var);
```

### ConstantValue
Condition `(e2JavaType.isArray() && e2JavaType.getComponentType().equals(e1JavaType)) || (C...` is always `true`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                // array and Collection
                if (BindableParameter.class.isInstance(e2) && BindableParameter.class.cast(e2).value() != null &&
                    ((e2JavaType.isArray() && e2JavaType.getComponentType().equals(e1JavaType))
                    || (Class.class.isInstance(e2JavaType) ||
                        (ParameterizedType.class.isInstance(e2JavaType)
                            && ParameterizedType.class.cast(e2JavaType).getActualTypeArguments().length > 0
                            && e1JavaType.equals(ParameterizedType.class.cast(e2JavaType).getActualTypeArguments()[0]))
                       ))) {
                    final BindableParameter bp = BindableParameter.class.cast(e2);
                    final Object value = bp.value();
```

### ConstantValue
Condition `Class.class.isInstance(e2JavaType)` is always `true` when reached
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                if (BindableParameter.class.isInstance(e2) && BindableParameter.class.cast(e2).value() != null &&
                    ((e2JavaType.isArray() && e2JavaType.getComponentType().equals(e1JavaType))
                    || (Class.class.isInstance(e2JavaType) ||
                        (ParameterizedType.class.isInstance(e2JavaType)
                            && ParameterizedType.class.cast(e2JavaType).getActualTypeArguments().length > 0
```

### ConstantValue
Condition `Class.class.isInstance(e2JavaType) || (ParameterizedType.class.isInstance(e2...` is always `true` when reached
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                if (BindableParameter.class.isInstance(e2) && BindableParameter.class.cast(e2).value() != null &&
                    ((e2JavaType.isArray() && e2JavaType.getComponentType().equals(e1JavaType))
                    || (Class.class.isInstance(e2JavaType) ||
                        (ParameterizedType.class.isInstance(e2JavaType)
                            && ParameterizedType.class.cast(e2JavaType).getActualTypeArguments().length > 0
                            && e1JavaType.equals(ParameterizedType.class.cast(e2JavaType).getActualTypeArguments()[0]))
                       ))) {
                    final BindableParameter bp = BindableParameter.class.cast(e2);
```

### ConstantValue
Condition `values == null` is always `false`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        @Override
        public StringBuilder asValue(AliasContext q) {
            return Expressions.asValue(q, "COALESCE", OPEN_BRACE, Expressions.asValue(q, values == null
                    ? null : values.toArray(new Expression<?>[values.size()]), COMMA), CLOSE_BRACE);
        }
```

### ConstantValue
Condition `EMBEDDABLE.equals(descriptor)` is always `false`
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java
                    public AnnotationVisitor visitAnnotation(final String descriptor, final boolean visible) {
                        if (ENTITY.equals(descriptor) ||
                                EMBEDDABLE.equals(descriptor) ||
                                MAPPED_SUPERCLASS.equals(descriptor) ||
                                ENTITY2.equals(descriptor) ||
```

### ConstantValue
Condition `MAPPED_SUPERCLASS.equals(descriptor)` is always `false`
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java
                        if (ENTITY.equals(descriptor) ||
                                EMBEDDABLE.equals(descriptor) ||
                                MAPPED_SUPERCLASS.equals(descriptor) ||
                                ENTITY2.equals(descriptor) ||
                                EMBEDDABLE2.equals(descriptor) ||
```

### ConstantValue
Condition `EMBEDDABLE2.equals(descriptor)` is always `false`
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java
                                MAPPED_SUPERCLASS.equals(descriptor) ||
                                ENTITY2.equals(descriptor) ||
                                EMBEDDABLE2.equals(descriptor) ||
                                MAPPED_SUPERCLASS2.equals(descriptor)) {
                            throw new MissingEnhancement(); // we already went into visit() so we miss the enhancement
```

### ConstantValue
Condition `MAPPED_SUPERCLASS2.equals(descriptor)` is always `false`
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java
                                ENTITY2.equals(descriptor) ||
                                EMBEDDABLE2.equals(descriptor) ||
                                MAPPED_SUPERCLASS2.equals(descriptor)) {
                            throw new MissingEnhancement(); // we already went into visit() so we miss the enhancement
                        }
```

### ConstantValue
Condition `conf == null` is always `false`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
            if (fatal)
              throw getCreateException(clsName, val, new ClassNotFoundException(clsName));
            Log log = (conf == null) ? null : conf.getConfigurationLog();
	        if (log != null && log.isErrorEnabled())
	            log.error(_loc.get("plugin-creation-exception", val));
```

### ConstantValue
Condition `names == null` is always `false`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java

        String[] names = getImplementors(serviceName, loader);
        if (names == null)
            return new Class[0];

```

### ConstantValue
Condition `!StringUtil.isEmpty(value)` is always `false`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
                key = trimQuote(key);
                if (key != null) {
                    if (!StringUtil.isEmpty(value))
                        setProperty(key, trimQuote(value));
                    else
```

### ConstantValue
Value `value` is always 'null'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
                key = trimQuote(key);
                if (key != null) {
                    if (!StringUtil.isEmpty(value))
                        setProperty(key, trimQuote(value));
                    else
```

### ConstantValue
Value `e` is always 'null'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java
    public Object remove(Object key) {
        Entry e = removeEntryForKey(key);
        return (e == null ? e : e.value);
    }

```

### ConstantValue
Condition `cycle != null` is always `true`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/graph/DepthFirstAnalysis.java`
#### Snippet
```java
        }
        List<Edge> cycle = buildCycle(edge, path, pos);
        assert (cycle != null): _loc.get("cycle-null", edge).getMessage();
        return cycle;
    }
```

### ConstantValue
Value `delimit` is always 'true'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
            name = removeDelimiters(config, orule, name, config.getLeadingDelimiter(),
                config.getTrailingDelimiter());
            return delimit(getIdentifierConfiguration(), nrule, name, delimit);
        }
        return name;
```

### ConstantValue
Value `table` is always 'null'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/PersistenceMappingDefaults.java`
#### Snippet
```java
                    (ValueMappingImpl)clm.getEmbeddingMetaData();
            if (value == null)
                return table;
            FieldMetaData field = value.getFieldMetaData();
            clm = (ClassMapping)field.getDefiningMetaData();
```

### ConstantValue
Condition `defMap == null` is always `false`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        for (DeferredEmbeddableOverrides defMap : defMappings) {
            FieldMapping fm = (FieldMapping)defMap._fm;
            if (defMap == null)
                return;
            fm = getAttributeOverrideForEmbeddable(fm, defMap._attrName, true);
```

### ConstantValue
Condition `code == EMPTY` is always `false` when reached
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AccessCode.java`
#### Snippet
```java
		if (code == EMPTY)
			return true;
		return (code%2 == 0 || code == EMPTY)
		 && code >= UNKNOWN
		 && code <= (MIXED|EXPLICIT|PROPERTY)
```

### ConstantValue
Condition `e instanceof PrivilegedActionException` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractMetaDataDefaults.java`
#### Snippet
```java
            throw ke;
        } catch (Exception e) {
            if (e instanceof PrivilegedActionException)
                e = ((PrivilegedActionException) e).getException();
            throw new UserException(e);
```

### ConstantValue
Value `value` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/SequenceMetaData.java`
#### Snippet
```java
            return "\"" + value + "\"";
        }
        return value;
    }

```

### ConstantValue
Condition `i1 > i2` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InheritanceComparator.java`
#### Snippet
```java
        if (i1 < i2)
            return -1;
        else if (i1 > i2)
            return 1;
        else
```

### ConstantValue
Value `proxy` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/Proxies.java`
#### Snippet
```java
         */
        if (proxy == null) {
            return proxy;
        } else if (!detachable) {
            // OPENJPA-1571 - using our runtime generated proxies, so remove any $proxy
```

### ConstantValue
Condition `type != null` is always `true` when reached
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ImplHelper.java`
#### Snippet
```java
    public static boolean isManagedType(OpenJPAConfiguration conf, Class type) {
        return (PersistenceCapable.class.isAssignableFrom(type)
            || (type != null
                && (conf == null || conf.getRuntimeUnenhancedClassesConstant()
                    == RuntimeUnenhancedClassesModes.SUPPORTED)
```

### ConstantValue
Condition `joinTbl != null` is always `true`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
                }
                setColumns(sup, sup.getValueInfo(), jcols, unique);
            } else if (joinTbl != null) {
                parseJoinTable(sup, joinTbl);
            }
```

### ConstantValue
Condition `isValue` is always `true`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
                if (isKey)
                    embed = getEmbeddedMapping(fm.getKeyMapping(), mustExist);
                else if (isValue)
                    embed = getEmbeddedMapping(fm.getElementMapping(),
                        mustExist);
```

### ConstantValue
Condition `isValue` is always `true`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
                if (isKey)
                    embed = fm.getKeyMapping();
                else if (isValue)
                    embed = fm.getElementMapping();
                break;
```

### ConstantValue
Condition `joinTbl != null` is always `true`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
                }
                setColumns(efm, efm.getValueInfo(), jcols, unique);
            } else if (joinTbl != null) {
                parseJoinTable(efm, joinTbl);
            }
```

### ConstantValue
Value `alias` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    public ClassMetaData getMetaData(String alias, ClassLoader envLoader, boolean mustExist) {
        if (alias == null && mustExist)
            throw new MetaDataException(_loc.get("no-alias-meta", alias, _aliases));
        if (alias == null)
            return null;
```

### ConstantValue
Condition `alias != null` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
            return;
        try {
            if (alias != null) {
                List<Class<?>> classes = _aliases.get(alias);
                if (classes == null)
```

### ConstantValue
Condition `cls == null` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
                    if (cls.isInterface())
                        getMetaData(cls, clsLoader, false);
                } else if (cls == null && mustExist) {
                    throw new MetaDataException(_loc.get("eager-class-not-found", className));
                }
```

### ConstantValue
Condition `existing == null` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java

                _oids.put(oid.getClass(), sup);
            } else if (existing == null || cls.isAssignableFrom(existing))
                _oids.put(oid.getClass(), cls);
        }
```

### ConstantValue
Condition `existing == null || cls.isAssignableFrom(existing)` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java

                _oids.put(oid.getClass(), sup);
            } else if (existing == null || cls.isAssignableFrom(existing))
                _oids.put(oid.getClass(), cls);
        }
```

### ConstantValue
Value `cls` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    public ClassMetaData[] getImplementorMetaDatas(Class<?> cls, ClassLoader envLoader, boolean mustExist) {
        if (cls == null && mustExist)
            throw new MetaDataException(_loc.get("no-meta", cls));
        if (cls == null)
            return EMPTY_METAS;
```

### ConstantValue
Value `oid` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    public ClassMetaData getMetaData(Object oid, ClassLoader envLoader, boolean mustExist) {
        if (oid == null && mustExist)
            throw new MetaDataException(_loc.get("no-oid-meta", oid, "?", _oids.toString()));
        if (oid == null)
            return null;
```

### ConstantValue
Condition `itr instanceof FilterIterator` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            for (Iterator itr = _itr; itr.hasNext();) {
                itr.next();
                if (itr instanceof FilterIterator)
                    itr = ((FilterIterator) itr).getIterator();
                ImplHelper.close(itr);
```

### ConstantValue
Condition `itr instanceof FilterIterator` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java
            for (Iterator itr = _itr; itr.hasNext();) {
                itr.next();
                if (itr instanceof FilterIterator)
                    itr = ((FilterIterator) itr).getIterator();
                ImplHelper.close(itr);
```

### ConstantValue
Value `_delayCapable` is always 'false'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
        if (getTypeCode() != JavaTypes.COLLECTION || isLRS()) {
           _delayCapable = Boolean.FALSE;
           return _delayCapable;
        } else {
            // Verify the proxy manager is configured to handle delay loading
```

### ConstantValue
Value `type` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            type = toConcreteType(type, _stdMaps);
            if (type == null)
                throw new UnsupportedException(_loc.get("no-proxy-intf", type));
        } else if (Modifier.isAbstract(type.getModifiers()))
            throw new UnsupportedException(_loc.get("no-proxy-abstract", type));
```

### ConstantValue
Condition `Class.forName(getProxyClassName(cls, false), true, GeneratedClasses.getMostD...` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            cls = Class.forName((String) type);
            try {
                if (Class.forName(getProxyClassName(cls, false), true,
                        GeneratedClasses.getMostDerivedLoader(cls, Proxy.class))
                        != null)
                    continue;
            }
```

### ConstantValue
Value `type` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            type = toConcreteType(type, _stdCollections);
            if (type == null)
                throw new UnsupportedException(_loc.get("no-proxy-intf", type));
        } else if (Modifier.isAbstract(type.getModifiers()))
            throw new UnsupportedException(_loc.get("no-proxy-abstract", type));
```

### ConstantValue
Condition `type.getDeclaredConstructor(paramTypes) != null` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
    private boolean hasConstructor(Class type, Class<?>... paramTypes) {
        try {
            return type.getDeclaredConstructor(paramTypes) != null;
        }
        catch (NoSuchMethodException e) {
```

### ConstantValue
Condition `from == null` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/MethodLifecycleCallbacks.java`
#### Snippet
```java
            return to == null || to.length == 0;
        if (to == null)
            return from == null || from.length == 0;

        if (from.length != to.length)
```

### ConstantValue
Value `version` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
            if (into == null)
                throw new OptimisticException(_loc.get("attach-version-del",
                    ImplHelper.getManagedInstance(pc).getClass(), id, version))
                    .setFailedObject(toAttach);

```

### ConstantValue
Condition `this instanceof PNewFlushedDeletedFlushedState` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
        if (this instanceof PNewFlushedDeletedState)
            return PNEWFLUSHEDDELETED;
        if (this instanceof PNewFlushedDeletedFlushedState)
			return PNEWFLUSHEDDELETEDFLUSHED;
		if (this instanceof PDeletedFlushedState)
```

### ConstantValue
Condition `this instanceof PDeletedFlushedState` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/PCState.java`
#### Snippet
```java
        if (this instanceof PNewFlushedDeletedFlushedState)
			return PNEWFLUSHEDDELETEDFLUSHED;
		if (this instanceof PDeletedFlushedState)
			return PDELETEDFLUSHED;
		throw new InternalException ();
```

### ConstantValue
Value `values` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
                return (T)values[0];
            } catch (Exception e) {
                throw new RuntimeException(_loc.get("fill-assign-error", Arrays.toString(values),
                        Arrays.toString(types)).getMessage(), e);
            }
```

### ConstantValue
Condition `!children.isEmpty()` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultShape.java`
#### Snippet
```java
            buf.append(child);
        }
        if (!children.isEmpty())
            buf.append("}");
        return buf.toString();
```

### ConstantValue
Value `exceps` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
            if (re instanceof CallbackException && _failFast)
                failFast = true;
            exceps = add(exceps, re);
        } finally {
            if (!failFast)
```

### ConstantValue
Condition `!reloadOnDetach` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
            if (detachMode != DETACH_LOADED ||
                    reloadOnDetach ||
                    (!reloadOnDetach && !full)) {
                sm.load(broker.getFetchConfiguration(), loadMode, exclude,
                    null, false);
```

### ConstantValue
Result of `def - 1` is always '0'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfigurationImpl.java`
#### Snippet
```java
            int def = FetchGroup.RECURSION_DEPTH_DEFAULT;
            return (traverse && ImplHelper.isAssignable(
                    meta.getDescribedType(), type)) ? def - 1 : def;
        }
        if (avail == Integer.MIN_VALUE || avail == FetchGroup.DEPTH_INFINITE)
```

### ConstantValue
Value `s` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
    public static String clip(String s, String first, String last, boolean fail) {
        if (s == null)
            return s;
        if (s.startsWith(first) && s.endsWith(last)) {
            return s.substring(first.length(), s.length()-last.length()).trim();
```

### ConstantValue
Condition `!w2Number` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
                    return unwrap(c2);
            }
            if (!w2Number) {
                if (w1Number && (w2 == Character.class || w2 == String.class))
                    return (w1 == Byte.class || w1 == Short.class)
```

### ConstantValue
Condition `!w1Number` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            // is assignable from the other but one is a standard type, assume
            // the other can be converted to that standard type
            if (!w1Number && !w2Number) {
                if (w1 == Object.class)
                    return unwrap(c2);
```

### ConstantValue
Condition `!w1Number && !w2Number` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            // is assignable from the other but one is a standard type, assume
            // the other can be converted to that standard type
            if (!w1Number && !w2Number) {
                if (w1 == Object.class)
                    return unwrap(c2);
```

### ConstantValue
Condition `!w2Number` is always `true` when reached
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            // is assignable from the other but one is a standard type, assume
            // the other can be converted to that standard type
            if (!w1Number && !w2Number) {
                if (w1 == Object.class)
                    return unwrap(c2);
```

### ConstantValue
Condition `_broker != null` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        _storeQuery.setContext(this);
        _printParameters = _broker.getPrintParameters();
        if (_broker != null && _broker.getMultithreaded())
            _lock = new ReentrantLock();
    }
```

### ConstantValue
Condition `resultClass != null` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
                // into some result class
                _packer = new ResultPacker(_class, getAlias(), resultClass);
            } else if (resultClass != null) { // projection
                Class<?>[] types = ex.getProjectionTypes(q);
                _packer = new ResultPacker(types, aliases, resultClass);
```

### ConstantValue
Value `_oid` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
            if (hasGeneratedKey() && _state instanceof PNewState &&
                _oid == null)
                return _oid;
            assignObjectId(true);
            if (_oid == null || !_broker.getConfiguration().
```

### ConstantValue
Condition `o2` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/AbstractExpressionBuilder.java`
#### Snippet
```java
            if (val2.getMetaData() == null && !val1.isXPath())
                val2.setMetaData(val1.getMetaData());
        } else if (o1 && o2 && expected != null) {
            // we never expect a pc type, so don't bother with metadata
            val1.setImplicitType(expected);
```

### ConstantValue
Condition `intfs != null` is always `true`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        // and throw a better error that mentions the class loaders
        Class<?>[] intfs = obj.getClass().getInterfaces();
        for (int i = 0; intfs != null && i < intfs.length; i++) {
            if (intfs[i].getName().equals(PersistenceCapable.class.getName())) {
                throw new UserException(_loc.get("pc-loader-different",
```

### ConstantValue
Value `exceps` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        try {
            exceps = add(exceps, endStoreManagerTransaction(rollback));
        } catch (RuntimeException re) {
            rollback = true;
```

### ConstantValue
Value `exceps` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        } catch (RuntimeException re) {
            rollback = true;
            exceps = add(exceps, re);
        }

```

### ConstantValue
Condition `flush || reason == FLUSH_COMMIT` is always `true` when reached
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                if ((_transEventManager.hasFlushListeners()
                    || _transEventManager.hasEndListeners())
                    && (flush || reason == FLUSH_COMMIT)) {
                    // fire events
                    mobjs = new ManagedObjectCollection(transactional);
```

### ConstantValue
Condition `reason == FLUSH_COMMIT` is always `true` when reached
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                if ((_transEventManager.hasFlushListeners()
                    || _transEventManager.hasEndListeners())
                    && (flush || reason == FLUSH_COMMIT)) {
                    // fire events
                    mobjs = new ManagedObjectCollection(transactional);
```

### ConstantValue
Value `exceps` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if (flush && reason != FLUSH_LOGICAL) {
                _flags |= FLAG_STORE_FLUSHING;
                exceps = add(exceps,
                    newFlushException(_store.flush(transactional)));
            }
```

### ConstantValue
Value `oid` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        oid = call.processArgument(oid);
        if (oid == null)
            return call.processReturn(oid, null);

        beginOperation(true);
```

### ConstantValue
Value `oid` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if ((flags & OID_NOVALIDATE) == 0)
                throw new ObjectNotFoundException(_loc.get("null-oid"));
            return call.processReturn(oid, null);
        }
        if (fetch == null)
```

### ConstantValue
Condition `methods == null` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        }
        Method[] methods = c.getMethods();
        if (methods == null || methods.length < 2)
            return Collections.emptySet();
        result = new TreeSet<>();
```

### ConstantValue
Condition `fields == null` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
            return Collections.EMPTY_SET;
        Field[] fields = c.getFields();
        if (fields == null || fields.length == 0)
            return Collections.EMPTY_SET;
        Set<T> result = new TreeSet<>();
```

### ConstantValue
Condition `fields == null` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
            return Collections.EMPTY_SET;
        Field[] fields = c.getFields();
        if (fields == null || fields.length == 0)
            return Collections.EMPTY_SET;
        Set<String> result = new TreeSet<>();
```

### ConstantValue
Value `recurse` is always 'true'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

                if (recurse) {
                    JPQLNode found = child.findChildByID(id, recurse);
                    if (found != null)
                        return found;
```

### ConstantValue
Value `meta` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            ? factory.newCollectionValuedParameter(paramKey, TYPE_OBJECT)
            : factory.newParameter(paramKey, TYPE_OBJECT);
        param.setMetaData(meta);
        param.setIndex(index);

```

### ConstantValue
Value `filter` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        if (from == null)
            from = evalFromClause(root().id == JJTSELECT);
        filter = and(from, filter);
        filter = and(evalWhereClause(), filter);
        filter = and(evalSelectClause(exps), filter);
```

### ConstantValue
Value `exp` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            projectionAliases.add(alias);
        }
        return exp;
    }

```

### ConstantValue
Value `join` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        } else {
            bind(var);
            join = and(join, factory.bindVariable(var, path));
        }

```

### ConstantValue
Value `join` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        {
            if (bound)
                join = and(join, factory.contains(path, var));

            setImplicitContainsTypes(path, var, CONTAINS_TYPE_ELEMENT);
```

### ConstantValue
Condition `cls == null` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
                // not a schema: treat it as a class
                String cls = assertSchemaName(n);
                if (cls == null)
                    throw parseException(EX_USER, "not-schema-name",
                        new Object[]{ root() }, null);
```

### ConstantValue
Value `bind` is always 'true'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

        if (bind && getDefinedVariable(id) == null)
            return createVariable(id, bind);

        return super.getVariable(id.toLowerCase(), bind);
```

### ConstantValue
Value `filter` is always 'null'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
                toArray(new String[innerJoins.size()]);

        return filter;
    }

```

### ConstantValue
Condition `t instanceof IllegalClassFormatException` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCClassFileTransformer.java`
#### Snippet
```java
            if (t instanceof RuntimeException)
                throw (RuntimeException) t;
            if (t instanceof IllegalClassFormatException)
                throw (IllegalClassFormatException) t;
            throw new GeneralException(t);
```

### ConstantValue
Condition `properties == null` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/PartitionedDataCache.java`
#### Snippet
```java
        String properties = new String(full);
        while (true) {
            if (properties == null)
                break;
            properties = properties.trim();
```

### ConstantValue
Condition `mbs == null` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/JMXProvider.java`
#### Snippet
```java
        try {
            for (MBeanServer s : mbs) {
                if (mbs == null || mbs.size() == 0) {
                    throw new UserException("jmx-server-failed-creation");
                }
```

### ConstantValue
Condition `mbs == null || mbs.size() == 0` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/JMXProvider.java`
#### Snippet
```java
        try {
            for (MBeanServer s : mbs) {
                if (mbs == null || mbs.size() == 0) {
                    throw new UserException("jmx-server-failed-creation");
                }
```

### ConstantValue
Condition `mbs.size() == 0` is always `false` when reached
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/jmx/JMXProvider.java`
#### Snippet
```java
        try {
            for (MBeanServer s : mbs) {
                if (mbs == null || mbs.size() == 0) {
                    throw new UserException("jmx-server-failed-creation");
                }
```

### ConstantValue
Value `replace` is always 'null'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/TokenReplacedStream.java`
#### Snippet
```java
                throw new IllegalArgumentException("Pattern [" + s + "] can not be empty or null ");
            if (replace == null)
                throw new IllegalArgumentException("Replacement [" + replace + "] is null for pattern [" + s + "]");
            chars = s.toCharArray();
            _cursor = -1;
```

### ConstantValue
Value `format` is always 'null'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/AbstractCommand.java`
#### Snippet
```java
                    format = Format.valueOf(rformat);
                } catch (Exception e) {
                    throw new ProcessingException(_ctx, _loc.get("format-not-supported", new Object[]{format,
                        _ctx.getRequest().getPathInfo(), _formatterFactory.getRegisteredKeys()}), HTTP_BAD_REQUEST);
                }
```

### ConstantValue
Condition `ch == '>'` is always `false`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/util/TextProcessingUtility.java`
#### Snippet
```java
                        buf.append(ch);
                    } else {
                        buf.append(ch == '>' ? "&gt;" : ch == '<' ? "&lt;" : ch);
                    }
                } else {
```

### ConstantValue
Condition `ch == '<'` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/util/TextProcessingUtility.java`
#### Snippet
```java
                        buf.append(ch);
                    } else {
                        buf.append(ch == '>' ? "&gt;" : ch == '<' ? "&lt;" : ch);
                    }
                } else {
```

### ConstantValue
Condition `ch == '<'` is always `false`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/util/TextProcessingUtility.java`
#### Snippet
```java
                    }
                } else {
                    buf.append(ch == '>' ? "&gt;" : ch == '<' ? "&lt;" : ch);
                }
            } else { // inside a hyperlink tag. Do not translate characters
```

### ConstantValue
Condition `hasAutoAssignedPK` is always `false`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        // if has auto-assigned pk and we get to this point, must have default
        // value, so must be new instance
        if (hasAutoAssignedPK) {
            code.getstatic().setField(Boolean.class, "FALSE", Boolean.class);
            code.areturn();
```

### ConstantValue
Value `c` is always 'null'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
        Component c = locateTab(pane, tab);
        if (c == null) {
            pane.setSelectedComponent(c);
        }
    }
```

### ConstantValue
Condition `maxPrice == null` is always `true` when reached
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
            Predicate matchPrice = cb.between(book.get(Book_.price), minPrice, maxPrice);
            predicates.add(matchPrice);
        } else if (minPrice != null && maxPrice == null) {
            Predicate matchPrice = cb.ge(book.get(Book_.price), minPrice);
            predicates.add(matchPrice);
```

### ConstantValue
Condition `minPrice == null` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
            Predicate matchPrice = cb.ge(book.get(Book_.price), minPrice);
            predicates.add(matchPrice);
        } else if (minPrice == null && maxPrice != null) {
            Predicate matchPrice = cb.le(book.get(Book_.price), maxPrice);
            predicates.add(matchPrice);
```

### ConstantValue
Value `forUpdate` is always 'false'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
        boolean forUpdate) {
        return !forUpdate && !sel.isLob()
            && super.supportsRandomAccessResultSet(sel, forUpdate);
    }

```

### ConstantValue
Condition `m == null` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/StoredProcedure.java`
#### Snippet
```java
        try {
            Method m = cls.getMethod(method, paramTypes);
            if (m == null || !Modifier.isStatic(m.getModifiers())) {
                throw new RuntimeException("No static method " + method + " with arguments "
                        + Arrays.toString(paramTypes) + " in " + cls);
```

### ConstantValue
Value `cal` is always 'null'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        if (cal == null) {
            try {
                return super.getTimestamp(rs, column, cal);
            } catch (ArrayIndexOutOfBoundsException ae) {
                // CR295604: issue a warning this this bug can be gotten
```

### ConstantValue
Value `alias` is always 'null'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            }
        }
        return alias;
    }

```

### ConstantValue
Condition `j2Empty` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                if (!j2.isEmpty())
                    _flags |= IMPLICIT_DISTINCT;
            } else if (j2Empty && !j1Empty) {
                collectOuterJoins(j1);
                if (!j1.isEmpty())
```

### ConstantValue
Condition `mayBeDuplicate` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
                continue;
            for (int j = 0, k = 0; j < ucols.length && k < icols.length; j++, k++) {
                if (mayBeDuplicate && ucols[j].getQualifiedPath().equals(icols[k].getQualifiedPath())) {
                    if (k == icols.length - 1) {
                        isDuplicate = true;
```

### ConstantValue
Condition `mayBeDuplicate && ucols[j].getQualifiedPath().equals(icols[k].getQualifiedPath())` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
                continue;
            for (int j = 0, k = 0; j < ucols.length && k < icols.length; j++, k++) {
                if (mayBeDuplicate && ucols[j].getQualifiedPath().equals(icols[k].getQualifiedPath())) {
                    if (k == icols.length - 1) {
                        isDuplicate = true;
```

### ConstantValue
Condition `isDuplicate` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
                    mayBeDuplicate = false;
            }
            if (isDuplicate)
                break;
        }
```

### ConstantValue
Value `isDuplicate` is always 'false'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
                break;
        }
       return isDuplicate;
    }

```

### ConstantValue
Condition `cols != null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
        List seqs = null;
        String seq, trig;
        for (int i = 0; cols != null && i < cols.length; i++) {
            if (!cols[i].isAutoAssigned())
                continue;
```

### ConstantValue
Condition `errorCode == -952` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
        }
        if ((subtype == StoreException.QUERY && "57014".equals(errorState) &&
            (errorCode == -952 || errorCode == -905))) {
            return false;
        }
```

### ConstantValue
Value `col` is always 'null'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
        if (db2ServerType == db2ZOSV8xOrLater) {
            if (col == null && val != null && "java.util.Date".equals(val.getClass().getName())) {
                setDate(stmnt, idx, new java.sql.Date(val.getTime()), null, col);
                return;
            }
```

### ConstantValue
Value `forUpdate` is always 'false'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
        boolean forUpdate) {
        return !forUpdate
            && super.supportsRandomAccessResultSet(sel, forUpdate);
    }

```

### ConstantValue
Condition `joins != null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        if (sel.getWhere() != null)
            where.append(sel.getWhere());
        if (joins != null)
            sel.append(where, joins);
        return where;
```

### ConstantValue
Condition `e1 instanceof SQLException` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            if (e1 instanceof RuntimeException)
                throw(RuntimeException) e1;
            if (e1 instanceof SQLException)
                throw(SQLException) e1;
        }
```

### ConstantValue
Value `disableBatch` is always 'false'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        // go to each Dictionary to validate the batch capability
        if (!disableBatch)
            disableBatch = validateDBSpecificBatchProcess(disableBatch, row,
                autoAssign, sm, cmd);
        return disableBatch;
```

### ConstantValue
Condition `size != null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            // replace '{0}' with size
            String ret = typeName.substring(0, idx);
            if (size != null)
                ret = ret + size;
            if (typeName.length() > idx + 3)
```

### ConstantValue
Condition `vrel` is always `true` when reached
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        if (!krel && !vrel)
            return new HandlerHandlerMapTableFieldStrategy();
        if (!krel && vrel)
            return new HandlerRelationMapTableFieldStrategy();
        if (krel && !vrel)
```

### ConstantValue
Condition `krel` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        if (!krel && vrel)
            return new HandlerRelationMapTableFieldStrategy();
        if (krel && !vrel)
            return new RelationHandlerMapTableFieldStrategy();
        return new RelationRelationMapTableFieldStrategy();
```

### ConstantValue
Condition `explicitStrat != null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
            if (explicitStrat instanceof FieldStrategy)
                return (FieldStrategy) explicitStrat;
            if (explicitStrat != null) {
                if (installHandlers)
                    field.setHandler((ValueHandler) explicitStrat);
```

### ConstantValue
Condition `i < copy.size()` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/QueryResultMapping.java`
#### Snippet
```java
                // record at previous path
                List<MetaDataContext> copy = new ArrayList<>(path.size() - 1);
                for (int i = 0; i < copy.size(); i++)
                    copy.add(path.get(i));

```

### ConstantValue
Result of `copy.size()` is always '0'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/QueryResultMapping.java`
#### Snippet
```java
                // record at previous path
                List<MetaDataContext> copy = new ArrayList<>(path.size() - 1);
                for (int i = 0; i < copy.size(); i++)
                    copy.add(path.get(i));

```

### ConstantValue
Condition `!adapt` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                // try default table
                given = getColumns("");
                if ((!adapt && !fill) && given.size() != tmplates.length) {
                    throw new MetaDataException(_loc.get(prefix + "-num-cols",
                            context, String.valueOf(tmplates.length),
```

### ConstantValue
Condition `!adapt && !fill` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                // try default table
                given = getColumns("");
                if ((!adapt && !fill) && given.size() != tmplates.length) {
                    throw new MetaDataException(_loc.get(prefix + "-num-cols",
                            context, String.valueOf(tmplates.length),
```

### ConstantValue
Condition `!fill` is always `true` when reached
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                // try default table
                given = getColumns("");
                if ((!adapt && !fill) && given.size() != tmplates.length) {
                    throw new MetaDataException(_loc.get(prefix + "-num-cols",
                            context, String.valueOf(tmplates.length),
```

### ConstantValue
Condition `!adapt` is always `true` when reached
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                colName=dict.getValidColumnName(colName, new Table());
                col = table.getColumn(colName);
                if (col == null && !adapt) {
                    throw new MetaDataException(_loc.
                        get(prefix + "-bad-col-name", context, colName, table));
```

### ConstantValue
Condition `tmplate != null` is always `true` when reached
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                upAction = tmplate.getUpdateAction();
                deferred = tmplate.isDeferred();
            } else if (_fk != null && tmplate != null) {
                // merge user and template data, always letting user info win
                name = _fk.getIdentifier();
```

### ConstantValue
Condition `vres != null` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationRelationMapTableFieldStrategy.java`
#### Snippet
```java
            if (kres != null)
                kres.close();
            if (vres != null)
                vres.close();
            throw se;
```

### ConstantValue
Condition `val == null` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerStrategies.java`
#### Snippet
```java
                }
        } else if (cols.length == 1) {
            if (canSet(row, io, 0, val == null)) {
                isSet = true;
                set(row, cols[0], val, handler, nullNone);
```

### ConstantValue
Condition `stmnt != null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
                dict.setTimeouts(stmnt, fetch, false);
                ResultSet rs = executeQuery(store, conn, stmnt, buf, paramList);
                ResultSetResult res = stmnt != null ?
                    new ResultSetResult(conn, stmnt, rs, store) :
                    new ResultSetResult(conn, rs, dict);
```

### ConstantValue
Condition `row == null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
            updateInverse(sm, rel, store, rm);
        else {
            if (row == null) row =  field.getRow(sm, store, rm, Row.ACTION_INSERT);
            if (row != null && !field.isBiMTo1JT()) {
                field.setForeignKey(row, rel);
```

### ConstantValue
Condition `rs != null` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
                        Row.ACTION_UPDATE);
            } finally {
                if (rs != null)
                    try { rs.close(); } catch (SQLException se) {}
                if (stmnt != null)
```

### ConstantValue
Condition `err != null` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
                    if (!setSequence(mapping, stat, alloc, updateStatSeq, conn))
                    {
                        throw (err != null) ? err : new SQLException(_loc.get(
                                "no-seq-row", mapping, _table).getMessage());
                    }
```

### ConstantValue
Condition `selector == null` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
        if (selector == null)
            return new PreparedQueryCacheImpl.StrongExclusion(_id, ((Localizer.Message)extract[1]).getMessage());
        if (selector == null || selector.hasMultipleSelects()
            || ((selector instanceof Union)
            && (((Union)selector).getSelects().length != 1)))
```

### ConstantValue
Condition `ps != null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
                                batchedRowsBaseIndex += _batchLimit;

                                if (ps != null)
                                    onerow.flush(ps, _dict, _store);
                                addBatch(ps, onerow, count);
```

### ConstantValue
Condition `canCache` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderQueryImpl.java`
#### Snippet
```java
        boolean canCache = (pkCols.length == buffer.getParameters().size());
        for(int i=0; i < pkCols.length  && canCache; i++){
            canCache = canCache && buffer.getColumns().contains(pkCols[i]);
        }

```

### ConstantValue
Condition `joins == null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/SimpleCaseExpression.java`
#### Snippet
```java

        states[0] = _caseOperand.initialize(sel, ctx, 0);
        if (joins == null)
            joins = states[0].joins;
        else
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_9);
                        if ( s>=0 ) return s;
                        break;
                    case 1 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_5);
                        if ( s>=0 ) return s;
                        break;
                    case 2 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_2);
                        if ( s>=0 ) return s;
                        break;
                    case 3 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_11);
                        if ( s>=0 ) return s;
                        break;
                    case 4 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_6);
                        if ( s>=0 ) return s;
                        break;
                    case 5 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_1);
                        if ( s>=0 ) return s;
                        break;
                    case 6 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_10);
                        if ( s>=0 ) return s;
                        break;
                    case 7 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_7);
                        if ( s>=0 ) return s;
                        break;
                    case 8 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_4);
                        if ( s>=0 ) return s;
                        break;
                    case 9 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_8);
                        if ( s>=0 ) return s;
                        break;
                    case 10 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred188_Java()) ) {s = 13;}

                        else if ( (true) ) {s = 12;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index126_3);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred186_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index124_3);
                        if ( s>=0 ) return s;
                        break;
                    case 1 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred186_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index124_4);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_59);
                        if ( s>=0 ) return s;
                        break;
                    case 1 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_60);
                        if ( s>=0 ) return s;
                        break;
                    case 2 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_61);
                        if ( s>=0 ) return s;
                        break;
                    case 3 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_62);
                        if ( s>=0 ) return s;
                        break;
                    case 4 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_63);
                        if ( s>=0 ) return s;
                        break;
                    case 5 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_64);
                        if ( s>=0 ) return s;
                        break;
                    case 6 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_65);
                        if ( s>=0 ) return s;
                        break;
                    case 7 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_66);
                        if ( s>=0 ) return s;
                        break;
                    case 8 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_67);
                        if ( s>=0 ) return s;
                        break;
                    case 9 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_68);
                        if ( s>=0 ) return s;
                        break;
                    case 10 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_69);
                        if ( s>=0 ) return s;
                        break;
                    case 11 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_70);
                        if ( s>=0 ) return s;
                        break;
                    case 12 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_71);
                        if ( s>=0 ) return s;
                        break;
                    case 13 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_72);
                        if ( s>=0 ) return s;
                        break;
                    case 14 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_73);
                        if ( s>=0 ) return s;
                        break;
                    case 15 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_74);
                        if ( s>=0 ) return s;
                        break;
                    case 16 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_75);
                        if ( s>=0 ) return s;
                        break;
                    case 17 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_78);
                        if ( s>=0 ) return s;
                        break;
                    case 18 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_79);
                        if ( s>=0 ) return s;
                        break;
                    case 19 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_80);
                        if ( s>=0 ) return s;
                        break;
                    case 20 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_98);
                        if ( s>=0 ) return s;
                        break;
                    case 21 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_104);
                        if ( s>=0 ) return s;
                        break;
                    case 22 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred182_Java()) ) {s = 124;}

                        else if ( (true) ) {s = 5;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index123_129);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred234_Java()) ) {s = 4;}

                        else if ( (true) ) {s = 3;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index146_1);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred151_Java()) ) {s = 45;}

                        else if ( (true) ) {s = 14;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index106_3);
                        if ( s>=0 ) return s;
                        break;
                    case 3 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred151_Java()) ) {s = 45;}

                        else if ( (true) ) {s = 14;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index106_4);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred117_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 3;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index85_2);
                        if ( s>=0 ) return s;
                        break;
                    case 1 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred117_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 3;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index85_4);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred262_Java()) ) {s = 32;}

                        else if ( (true) ) {s = 2;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index162_1);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred237_Java()) ) {s = 2;}

                        else if ( (true) ) {s = 4;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index149_1);
                        if ( s>=0 ) return s;
                        break;
                    case 1 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred237_Java()) ) {s = 2;}

                        else if ( (true) ) {s = 4;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index149_3);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred249_Java()) ) {s = 32;}

                        else if ( (true) ) {s = 1;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index155_2);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred5_Java()) ) {s = 16;}

                        else if ( (true) ) {s = 2;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index8_1);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred243_Java()) ) {s = 2;}

                        else if ( (true) ) {s = 4;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index151_1);
                        if ( s>=0 ) return s;
                        break;
                    case 1 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred243_Java()) ) {s = 2;}

                        else if ( (true) ) {s = 4;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index151_3);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_2);
                        if ( s>=0 ) return s;
                        break;
                    case 1 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_3);
                        if ( s>=0 ) return s;
                        break;
                    case 2 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_4);
                        if ( s>=0 ) return s;
                        break;
                    case 3 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_5);
                        if ( s>=0 ) return s;
                        break;
                    case 4 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_6);
                        if ( s>=0 ) return s;
                        break;
                    case 5 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_7);
                        if ( s>=0 ) return s;
                        break;
                    case 6 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_8);
                        if ( s>=0 ) return s;
                        break;
                    case 7 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_9);
                        if ( s>=0 ) return s;
                        break;
                    case 8 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_10);
                        if ( s>=0 ) return s;
                        break;
                    case 9 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_11);
                        if ( s>=0 ) return s;
                        break;
                    case 10 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_12);
                        if ( s>=0 ) return s;
                        break;
                    case 11 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_13);
                        if ( s>=0 ) return s;
                        break;
                    case 12 :
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred113_Java()) ) {s = 1;}

                        else if ( (true) ) {s = 15;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index81_14);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        if ( (synpred229_Java()) ) {s = 16;}

                        else if ( (true) ) {s = 4;}


```

### ConstantValue
Condition `s>=0` is always `true`
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

                        input.seek(index145_3);
                        if ( s>=0 ) return s;
                        break;
            }
```

### ConstantValue
Condition is always true
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    alt147=1;
                }
                else if ( (true) ) {
                    alt147=2;
                }
```

### ConstantValue
Condition `prevaction != null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
            }
            prevaction = action;
            if (prevaction != null && prevaction.context != null) {
                Context jCtx = JDBCStoreQuery.getThreadLocalContext(prevaction.context);
                pstate.joins = pstate.joins.setJoinContext(jCtx);
```

### ConstantValue
Condition `col == null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/LocalConstraint.java`
#### Snippet
```java
    	if (col == null)
            throw new InvalidStateException(_loc.get("table-mismatch",
                col == null ? null : col.getTable(),
                col == null ? null : getTable()));

```

### ConstantValue
Condition `col == null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/LocalConstraint.java`
#### Snippet
```java
            throw new InvalidStateException(_loc.get("table-mismatch",
                col == null ? null : col.getTable(),
                col == null ? null : getTable()));

        if (_colList == null)
```

### ConstantValue
Condition `sNames.length != 1` is always `true` when reached
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
                continue;
            }
            if (i == (sNames.length - 1) && sNames.length != 1) {
                setName(sName.clone());
            } else {
```

### ConstantValue
Condition `o == null` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
    public int compareTo(Identifier o) {
        if (o instanceof DBIdentifier) {
            if (o == null || o == DBIdentifier.NULL)
                return -1;
            return super.compareTo(o);
```

### ConstantValue
Condition `o == null` is always `false` when reached
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
    public int compareTo(Identifier o) {
        if (o instanceof DBIdentifier) {
            if (this == DBIdentifier.NULL && (o == null || o == DBIdentifier.NULL)) {
                return 0;
            }
```

### ConstantValue
Condition `o == null` is always `false`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
            if (this == DBIdentifier.NULL)
                return 1;
            if (o == null || o == DBIdentifier.NULL)
                return -1;
        }
```

### ConstantValue
Condition `baseId == DBIdentifierType.SCHEMA` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/DBIdentifier.java`
#### Snippet
```java
            id0 = DBIdentifierType.TABLE;
        }
        else if (baseId == DBIdentifierType.SCHEMA) {
            id0 = DBIdentifierType.DATABASE;
        }
```

### ConstantValue
Condition `dbTable != null` is always `true`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java

                        if (dropForeignKey(foreignKey))
                            if (dbTable != null)
                                dbTable.removeForeignKey(fk);
                            else
```

### ConstantValue
Condition `current == null` is always `false`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/UniqueResultObjectProvider.java`
#### Snippet
```java
        }
        throw new UnsupportedOperationException(_loc.get("aggregate-unsupported-on-type",
        		"MAX()", (current == null ? other : current).getClass().getName()).toString());
    }

```

### ConstantValue
Condition `current == null` is always `false`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/UniqueResultObjectProvider.java`
#### Snippet
```java
        }
        throw new UnsupportedOperationException(_loc.get("aggregate-unsupported-on-type",
        		"SUM()", (current == null ? other : current).getClass().getName()).toString());
    }

```

### ConstantValue
Condition `current == null` is always `false`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/UniqueResultObjectProvider.java`
#### Snippet
```java
        }
        throw new UnsupportedOperationException(_loc.get("aggregate-unsupported-on-type",
        		"MIN()", (current == null ? other : current).getClass().getName()).toString());
    }

```

### ConstantValue
Value `res` is always 'null'
in `openjpa-tools/openjpa-fetch-statistics/src/main/java/org/apache/openjpa/enhance/stats/FetchStatisticsAuxEnhancer.java`
#### Snippet
```java
        String fieldName = null;
        if (methName.matches(IGNORE_METHODS_REGEX)) {
            return res;
        } else if (methName.startsWith("pcGet")) {
            // field access
```

### ConstantValue
Condition `forward` is always `true`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedResultSet.java`
#### Snippet
```java
		boolean forward = arg0 > 0;
		for (int i = 0; i < arg0; i++) {
			if (forward) {
				if (!next()) {
					return false;
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ParameterImpl.java`
#### Snippet
```java
    public String toString() {
        StringBuilder buf = new StringBuilder("Parameter");
        buf.append("<" + getParameterType().getSimpleName() + ">");
        if (_name != null) {
            buf.append("('" + _name + "')");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ParameterImpl.java`
#### Snippet
```java
        buf.append("<" + getParameterType().getSimpleName() + ">");
        if (_name != null) {
            buf.append("('" + _name + "')");
        } else if (_position != null) {
            buf.append("(" + _position + ")");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ParameterImpl.java`
#### Snippet
```java
            buf.append("('" + _name + "')");
        } else if (_position != null) {
            buf.append("(" + _position + ")");
        } else {
            buf.append("(?)");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataFactory.java`
#### Snippet
```java
                else
                    buf.append(", ");
                buf.append(meta.getDescribedTypeString() + "." + fmd);
            }
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        int i = 0;
        for (Element e : elements) {
            tmp.append(e.getSimpleName() + (++i == elements.size() ? "" : ","));
        }
        return tmp.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `PrintWriter.append()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
            tab(out, tab);
            if (access != null)
                out.append(access.toString().toLowerCase(Locale.ENGLISH) + SPACE);
            if (isStatic)
                out.append("static" + SPACE);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ParameterExpressionImpl.java`
#### Snippet
```java
    public String toString() {
        StringBuilder buf = new StringBuilder("ParameterExpression");
        buf.append("<" + getJavaType().getSimpleName() + ">");
        if (_name != null)
            buf.append("('"+ _name +"')");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ParameterExpressionImpl.java`
#### Snippet
```java
        buf.append("<" + getJavaType().getSimpleName() + ">");
        if (_name != null)
            buf.append("('"+ _name +"')");

        return buf.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
                err = t;
                errs = (errs == null) ? new StringBuilder() : errs.append("\n");
                errs.append(_derivations[i].getClass().getName() + ":" + t);
            }
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
                err = t;
                errs = (errs == null) ? new StringBuilder() : errs.append("\n");
                errs.append(_derivations[i].getClass().getName() + ":" + t);
            }
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
                err = t;
                errs = (errs == null) ? new StringBuilder() : errs.append("\n");
                errs.append(_derivations[i].getClass().getName() + ":" + t);
            }
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
                err = t;
                errs = (errs == null) ? new StringBuilder() : errs.append("\n");
                errs.append(_derivations[i].getClass().getName() + ":" + t);
            }
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
    public String toString() {
        StringBuilder buf = new StringBuilder();
        buf.append("Class: (" + _meta.getDescribedType().getName() + ")\n");
        buf.append("Object Id: (" + _oid + ")\n");
        buf.append("Version: (" + _version + ")\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder();
        buf.append("Class: (" + _meta.getDescribedType().getName() + ")\n");
        buf.append("Object Id: (" + _oid + ")\n");
        buf.append("Version: (" + _version + ")\n");
        FieldMetaData[] fmds = _meta.getFields();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
        buf.append("Class: (" + _meta.getDescribedType().getName() + ")\n");
        buf.append("Object Id: (" + _oid + ")\n");
        buf.append("Version: (" + _version + ")\n");
        FieldMetaData[] fmds = _meta.getFields();
        for (int i = 0; i < fmds.length; i++) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
        FieldMetaData[] fmds = _meta.getFields();
        for (int i = 0; i < fmds.length; i++) {
            buf.append("  Field: (" + i + ")\n");
            buf.append("  Name: (" + fmds[i].getName() + ")\n");
            buf.append("  Value: (" + _data[i] + ")\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
        for (int i = 0; i < fmds.length; i++) {
            buf.append("  Field: (" + i + ")\n");
            buf.append("  Name: (" + fmds[i].getName() + ")\n");
            buf.append("  Value: (" + _data[i] + ")\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
            buf.append("  Field: (" + i + ")\n");
            buf.append("  Name: (" + fmds[i].getName() + ")\n");
            buf.append("  Value: (" + _data[i] + ")\n");
		}
		return buf.toString ();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
     */
    StringBuilder generateStackTrace(Throwable t, StringBuilder buffer) {
        buffer.append(t.getClass().getName() + ": " + t.getMessage() + NEWLINE);
        buffer.append(toString(t.getStackTrace()));
        Throwable cause = t.getCause();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
            } else {
                if (type == String.class) {
                    _sql.append("'" + o.toString().replace("'", "''") + "'");

                } else if ( type == Character.class ) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                        _sql.append(Integer.toString((Character) o));
                    } else {
                        _sql.append("'" + o.toString().replace("'", "''") + "'");
                    }
                } else if (type == Boolean.class) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StateComparisonVersionStrategy.java`
#### Snippet
```java
                    buf.append(" AND ");
                if (val == NULL)
                    buf.append(dict.getColumnDBName(col) + " IS NULL");
                else
                    buf.append(dict.getColumnDBName(col) + " = ?");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StateComparisonVersionStrategy.java`
#### Snippet
```java
                    buf.append(dict.getColumnDBName(col) + " IS NULL");
                else
                    buf.append(dict.getColumnDBName(col) + " = ?");
                hasWhere = true;
            }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `openjpa-tools/openjpa-fetch-statistics/src/main/java/org/apache/openjpa/enhance/stats/FetchStatsCollector.java`
#### Snippet
```java

        for (String field : zeroAccessFieldSet) {
            message.append("\n\t" + field);

        }
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PredicateImpl.java`
#### Snippet
```java
    protected PredicateImpl(BooleanOperator op, Predicate...restrictions) {
        this(op);
        if (restrictions == null || restrictions.length == 0) return;

    	for (Predicate p : restrictions) {
```

### RedundantLengthCheck
Redundant array length check
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ClassMetaData.java`
#### Snippet
```java
        if (isAbstract()) {
            FieldMetaData[] declaredFields = getDeclaredFields();
            if (declaredFields != null && declaredFields.length != 0) {
                for (FieldMetaData fmd : declaredFields) {
                    if (fmd.isPrimaryKey()) {
```

### RedundantLengthCheck
Redundant array length check
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/LifecycleEventManager.java`
#### Snippet
```java
        LifecycleCallbacks[] callbacks = meta.getLifecycleMetaData().
            getCallbacks(type);
        if (callbacks.length == 0)
            return false;
        for (LifecycleCallbacks callback : callbacks)
```

### RedundantLengthCheck
Redundant array length check
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataImpl.java`
#### Snippet
```java
        for (int i = 0; i < fields.length; i++) {
            FieldMetaData[] inverses = fields[i].getInverseMetaDatas();
            if (inverses.length == 0)
                continue;
            for (FieldMetaData inverse : inverses) {
```

## RuleId[id=UNCHECKED_WARNING]
### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'. Reason: 'impl' has raw type, so result of getNamedParameters is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryResultCacheImpl.java`
#### Snippet
```java
                impl.getPositionalParameters());
        return QueryKey.newInstance(impl.getDelegate(),
            impl.getNamedParameters());
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCacheImpl.java`
#### Snippet
```java
    public void evictAll(Class cls, Collection oids) {
        if (_cache.getDelegate() != null)
            _cache.removeAll(JPAFacadeHelper.toOpenJPAObjectIds
                (getMetaData(cls), oids));
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCacheImpl.java`
#### Snippet
```java
        if (_cache.getDelegate() != null)
            _cache.removeAll(JPAFacadeHelper.toOpenJPAObjectIds
                (getMetaData(cls), oids));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCacheImpl.java`
#### Snippet
```java
            return oids.isEmpty();

        BitSet set = _cache.containsAll(JPAFacadeHelper.toOpenJPAObjectIds
            (getMetaData(cls), oids));
        for (int i = 0; i < oids.size(); i++)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCacheImpl.java`
#### Snippet
```java

        BitSet set = _cache.containsAll(JPAFacadeHelper.toOpenJPAObjectIds
            (getMetaData(cls), oids));
        for (int i = 0; i < oids.size(); i++)
            if (!set.get(i))
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCacheImpl.java`
#### Snippet
```java
    public void unpinAll(Class cls, Collection oids) {
        if (_cache.getDelegate() != null)
            _cache.unpinAll(JPAFacadeHelper.toOpenJPAObjectIds
                (getMetaData(cls), oids));
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCacheImpl.java`
#### Snippet
```java
        if (_cache.getDelegate() != null)
            _cache.unpinAll(JPAFacadeHelper.toOpenJPAObjectIds
                (getMetaData(cls), oids));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCacheImpl.java`
#### Snippet
```java
    public void pinAll(Class cls, Collection oids) {
        if (_cache.getDelegate() != null)
            _cache.pinAll(JPAFacadeHelper.toOpenJPAObjectIds
                (getMetaData(cls), oids));
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoreCacheImpl.java`
#### Snippet
```java
        if (_cache.getDelegate() != null)
            _cache.pinAll(JPAFacadeHelper.toOpenJPAObjectIds
                (getMetaData(cls), oids));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'. Reason: '_extent' has raw type, so result of list is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ExtentImpl.java`
#### Snippet
```java
    public List<T> list() {
        _em.assertNotCloseInvoked();
        return _extent.list();
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.kernel.Extent' to 'org.apache.openjpa.kernel.Extent'. Reason: '_extent' has raw type, so result of getDelegate is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ExtentImpl.java`
#### Snippet
```java
    @Override
    public org.apache.openjpa.kernel.Extent<T> getDelegate() {
        return _extent.getDelegate();
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Iterator' to 'java.util.Iterator'. Reason: '_extent' has raw type, so result of iterator is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ExtentImpl.java`
#### Snippet
```java
    public Iterator<T> iterator() {
        _em.assertNotCloseInvoked();
        return _extent.iterator();
    }

```

### UNCHECKED_WARNING
Unchecked call to 'DelegatingExtent(Extent, RuntimeExceptionTranslator)' as a member of raw type 'org.apache.openjpa.kernel.DelegatingExtent'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ExtentImpl.java`
#### Snippet
```java
    public ExtentImpl(EntityManagerImpl em, org.apache.openjpa.kernel.Extent<T> extent) {
        _em = em;
        _extent = new DelegatingExtent(extent,
            PersistenceExceptions.getRollbackTranslator(em));
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'. Reason: '_extent' has raw type, so result of getElementType is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/ExtentImpl.java`
#### Snippet
```java
    @Override
    public Class<T> getElementClass() {
        return _extent.getElementType();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.openjpa.persistence.OpenJPAEnum\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/HintValueConverter.java`
#### Snippet
```java
        public Object convert(Object e) {
            if (e.getClass() == _prototype.getClass())
                return ((OpenJPAEnum<Enum<?>>)e).toKernelConstant();
            if (e instanceof String) {
                return _prototype.convertToKernelConstant(e.toString());
```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
    private void parseXmlRootElement(Class type, XMLMetaData meta) {
        try {
            if (type.getAnnotation(xmlRootElementClass) != null) {
                meta.setXmlRootElement(true);
                meta.setXmlname((String) xmlRootName.invoke(type.getAnnotation
```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            if (type.getAnnotation(xmlRootElementClass) != null) {
                meta.setXmlRootElement(true);
                meta.setXmlname((String) xmlRootName.invoke(type.getAnnotation
                    (xmlRootElementClass), new Object[]{}));
                meta.setXmlnamespace((String) xmlRootNamespace.invoke(type
```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                meta.setXmlname((String) xmlRootName.invoke(type.getAnnotation
                    (xmlRootElementClass), new Object[]{}));
                meta.setXmlnamespace((String) xmlRootNamespace.invoke(type
                    .getAnnotation(xmlRootElementClass), new Object[]{}));
            }
            else {
```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            }
            else {
                meta.setXmlname((String) xmlTypeName.invoke(type.getAnnotation
                    (xmlTypeClass), new Object[]{}));
                meta.setXmlnamespace((String) xmlTypeNamespace.invoke(type
```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                meta.setXmlname((String) xmlTypeName.invoke(type.getAnnotation
                    (xmlTypeClass), new Object[]{}));
                meta.setXmlnamespace((String) xmlTypeNamespace.invoke(type
                    .getAnnotation(xmlTypeClass), new Object[]{}));
            }
        } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
        if (cls == null || xmlTypeClass == null
            || !(AccessController.doPrivileged(J2DoPrivHelper
                .isAnnotationPresentAction(cls, (Class<? extends Annotation>) xmlTypeClass))
                && AccessController
                .doPrivileged(J2DoPrivHelper.isAnnotationPresentAction(cls,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                && AccessController
                .doPrivileged(J2DoPrivHelper.isAnnotationPresentAction(cls,
                        (Class<? extends Annotation>) xmlRootElementClass))))
            return null;

```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlTypeClass = Class.forName(
                "javax.xml.bind.annotation.XmlType");
            xmlTypeName = xmlTypeClass.getMethod("name", null);
            xmlTypeNamespace = xmlTypeClass.getMethod("namespace", null);
            xmlRootElementClass = Class.forName(
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                "javax.xml.bind.annotation.XmlType");
            xmlTypeName = xmlTypeClass.getMethod("name", null);
            xmlTypeNamespace = xmlTypeClass.getMethod("namespace", null);
            xmlRootElementClass = Class.forName(
                "javax.xml.bind.annotation.XmlRootElement");
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlRootElementClass = Class.forName(
                "javax.xml.bind.annotation.XmlRootElement");
            xmlRootName = xmlRootElementClass.getMethod("name", null);
            xmlRootNamespace = xmlRootElementClass.getMethod("namespace", null);
            xmlAccessorTypeClass = Class.forName(
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                "javax.xml.bind.annotation.XmlRootElement");
            xmlRootName = xmlRootElementClass.getMethod("name", null);
            xmlRootNamespace = xmlRootElementClass.getMethod("namespace", null);
            xmlAccessorTypeClass = Class.forName(
                "javax.xml.bind.annotation.XmlAccessorType");
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlAccessorTypeClass = Class.forName(
                "javax.xml.bind.annotation.XmlAccessorType");
            xmlAccessorValue = xmlAccessorTypeClass.getMethod("value", null);
            xmlAttributeClass = Class.forName(
                "javax.xml.bind.annotation.XmlAttribute");
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlAttributeClass = Class.forName(
                "javax.xml.bind.annotation.XmlAttribute");
            xmlAttributeName = xmlAttributeClass.getMethod("name", null);
            xmlAttributeNamespace = xmlAttributeClass.getMethod("namespace"
                , null);
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                "javax.xml.bind.annotation.XmlAttribute");
            xmlAttributeName = xmlAttributeClass.getMethod("name", null);
            xmlAttributeNamespace = xmlAttributeClass.getMethod("namespace"
                , null);
            xmlElementClass = Class.forName(
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlElementClass = Class.forName(
                "javax.xml.bind.annotation.XmlElement");
            xmlElementName = xmlElementClass.getMethod("name", null);
            xmlElementNamespace = xmlElementClass.getMethod("namespace", null);
        } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                "javax.xml.bind.annotation.XmlElement");
            xmlElementName = xmlElementClass.getMethod("name", null);
            xmlElementNamespace = xmlElementClass.getMethod("namespace", null);
        } catch (Exception e) {
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
        // handle inheritance at sub-element level
        if (AccessController.doPrivileged(J2DoPrivHelper
                .isAnnotationPresentAction(superclass, (Class<? extends Annotation>) xmlTypeClass)))
            populateFromReflection(superclass, meta);

```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java

        try {
            if (Objects.equals(xmlAccessorValue.invoke(cls.getAnnotation(
                xmlAccessorTypeClass), new Object[]{}).toString(), "FIELD"))
                members = cls.getDeclaredFields();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                AnnotatedElement el = (AnnotatedElement) member;
                XMLMetaData field = null;
                if (el.getAnnotation(xmlElementClass) != null) {
                    String xmlname = (String) xmlElementName.invoke(el
                            .getAnnotation(xmlElementClass), new Object[]{});
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                if (el.getAnnotation(xmlElementClass) != null) {
                    String xmlname = (String) xmlElementName.invoke(el
                            .getAnnotation(xmlElementClass), new Object[]{});
                    // avoid JAXB XML bind default name
                    if (Objects.equals(XMLMetaData.defaultName, xmlname))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                    if (AccessController.doPrivileged(J2DoPrivHelper
                            .isAnnotationPresentAction(((Field) member).getType(),
                                    (Class<? extends Annotation>) xmlTypeClass))) {
                        field = _repos.addXMLClassMetaData(((Field) member).getType());
                        parseXmlRootElement(((Field) member).getType(), field);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                        field.setXmlname(xmlname);
                        field.setXmlnamespace((String) xmlElementNamespace
                                .invoke(el.getAnnotation(xmlElementClass)
                                        , new Object[]{}));
                    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                    }
                }
                else if (el.getAnnotation(xmlAttributeClass) != null) {
                    field = _repos.newXMLFieldMetaData(((Field) member)
                            .getType(), member.getName());
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                    field.setXmltype(XMLMetaData.ATTRIBUTE);
                    String xmlname = (String) xmlAttributeName.invoke(
                            el.getAnnotation(xmlAttributeClass), new Object[]{});
                    // avoid JAXB XML bind default name
                    if (Objects.equals(XMLMetaData.defaultName, xmlname))
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                    field.setXmlname("@" + xmlname);
                    field.setXmlnamespace((String) xmlAttributeNamespace.invoke(
                            el.getAnnotation(xmlAttributeClass), new Object[]{}));
                }
                if (field != null)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistence.java`
#### Snippet
```java
    public static Collection toOpenJPAObjectIds(
        org.apache.openjpa.meta.ClassMetaData meta, Collection oids) {
        return JPAFacadeHelper.toOpenJPAObjectIds(meta, oids);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleImpl.java`
#### Snippet
```java
        try {
            if (type == null || value == null) {
                return (X) value;
            } else {
                return (X) Filters.convert(value, type);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleImpl.java`
#### Snippet
```java
                return (X) value;
            } else {
                return (X) Filters.convert(value, type);
            }
        } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'E'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
            return null;
        if (type.isInstance(val))
            return (E)val;
        if (val instanceof String) {
            return Enum.valueOf(type, val.toString().trim().toUpperCase(Locale.ENGLISH));
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.kernel.DataCacheRetrieveMode' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
            // works because enum values are identical String
            if (value instanceof CacheRetrieveMode || (value instanceof String && CACHE_RETRIEVE_MODE.equals(key))) {
                return (T)DataCacheRetrieveMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            } else if (value instanceof CacheStoreMode || (value instanceof String && CACHE_STORE_MODE.equals(key))) {
                return (T)DataCacheStoreMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.kernel.DataCacheStoreMode' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
                return (T)DataCacheRetrieveMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            } else if (value instanceof CacheStoreMode || (value instanceof String && CACHE_STORE_MODE.equals(key))) {
                return (T)DataCacheStoreMode.valueOf(value.toString().trim().toUpperCase(Locale.ENGLISH));
            }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAProperties.java`
#### Snippet
```java
            }
        }
        return (T) value;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
    	}

    	return Collections.EMPTY_LIST;
    }

```

### UNCHECKED_WARNING
Unchecked call to 'includes(T)' as a member of raw type 'org.apache.openjpa.persistence.PersistenceMetaDataDefaults.InclusiveFilter'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
            boolean include = true;
            for (InclusiveFilter f : filters) {
                if (f != null && !f.includes(e)) {
                    include = false;
                    break;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
        	    explicit ? (isField ? null : fieldAccessFilter) : null);
    	}
    	return Collections.EMPTY_LIST;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
    	if (isEmpty) {
    		warn(meta, _loc.get("access-empty", meta));
    		return Collections.EMPTY_LIST;
    	}
    	if (explicit) {
```

### UNCHECKED_WARNING
Unchecked call to 'includes(T)' as a member of raw type 'org.apache.openjpa.persistence.PersistenceMetaDataDefaults.InclusiveFilter'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java
            boolean include = true;
            for (InclusiveFilter f : filters) {
                if (f != null && !f.includes(e)) {
                    include = false;
                    break;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.reflect.Constructor' to 'java.lang.reflect.Constructor'. Reason: 'cls' has raw type, so result of getConstructor is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java
                    getImplementation(FetchPlan.class, storeType,
                    		FetchPlanImpl.class);
                _plan = cls.getConstructor(FetchConfiguration.class);
            }
            return _plan.newInstance(fetch);
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java
                    getImplementation(FetchPlan.class, storeType,
                    		FetchPlanImpl.class);
                _plan = cls.getConstructor(FetchConfiguration.class);
            }
            return _plan.newInstance(fetch);
```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java
        }
        else if (!props.isEmpty()) {
            props = new HashMap(props);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'props' has raw type, so result of entrySet is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java
        // allow setting of other bean properties of EM
        if (!props.isEmpty()) {
            Set<Map.Entry> entrySet = props.entrySet();
            for (Map.Entry entry : entrySet) {
                em.setProperty(entry.getKey().toString(), entry.getValue());
```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
    @Override
    public void generateSchema(final PersistenceUnitInfo info, final Map map) {
        final Map runMap = map == null ? new HashMap<>() : new HashMap<>(map);

        if (!acceptProvider(runMap)) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
        }

        runMap.put("jakarta.persistence.schema-generation.database.action", "create");
        final OpenJPAEntityManagerFactory factory = createContainerEntityManagerFactory(info, runMap);
        try {
```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
    @Override
    public boolean generateSchema(final String persistenceUnitName, final Map map) {
        final Map runMap = map == null ? new HashMap<>() : new HashMap<>(map);

        if (!acceptProvider(runMap)) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProviderImpl.java`
#### Snippet
```java
        }

        runMap.put("jakarta.persistence.schema-generation.database.action", "create");
        final OpenJPAEntityManagerFactory factory = createEntityManagerFactory(persistenceUnitName, runMap);
        try {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.XMLPersistenceMetaDataSerializer.FieldComparator' to 'java.util.Comparator'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
        List<FieldMetaData> fields = new ArrayList<>(Arrays.asList
            (meta.getDefinedFieldsInListingOrder()));
        Collections.sort(fields, new FieldComparator());

        // serialize attr-override
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                        (SequenceMetaData) o2);
                case TYPE_CLASS_QUERIES:
                    return ((Comparable) o1).compareTo(o2);
                case TYPE_CLASS_SEQS:
                    return ((Comparable) o1).compareTo(o2);
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                    return ((Comparable) o1).compareTo(o2);
                case TYPE_CLASS_SEQS:
                    return ((Comparable) o1).compareTo(o2);
                default:
                    return compareUnknown(o1, o2);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java

    @Override
    protected Collection getObjects() {
        List all = new ArrayList();
        if (isQueryMode())
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
            addSequenceMetaDatas(all);
        if ((isMetaDataMode() || isMappingMode()) && _metas != null)
            all.addAll(_metas.values());
        if (isMappingMode())
            addSystemMappingElements(all);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
        for (Map.Entry entry : _queries.entrySet()) {
            if (entry.getKey() == null)
                all.addAll((List) entry.getValue());
            else if (_mode == MODE_QUERY || _metas == null
                || !_metas.containsKey(entry.getKey()))
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
            else if (_mode == MODE_QUERY || _metas == null
                || !_metas.containsKey(entry.getKey()))
                all.add(new ClassQueries((List<QueryMetaData>)
                    entry.getValue()));
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'. Reason: 'entry' has raw type, so result of getValue is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
            else if (_mode == MODE_QUERY || _metas == null
                || !_metas.containsKey(entry.getKey()))
                all.add(new ClassQueries((List<QueryMetaData>)
                    entry.getValue()));
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
        for (Map.Entry entry : _seqs.entrySet()) {
            if (entry.getKey() == null)
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
                    entry.getValue()));
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'. Reason: 'entry' has raw type, so result of getValue is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
                    entry.getValue()));
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (_comp == null)
            _comp = newSerializationComparator();
        Collections.sort(objs, _comp);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.XMLPersistenceMetaDataSerializer.SerializationComparator' to 'java.util.Comparator'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (_comp == null)
            _comp = newSerializationComparator();
        Collections.sort(objs, _comp);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java

        if (queries == null) {
            queries = new ArrayList(3); // don't expect many queries / class
            queries.add(meta);
            _queries.put(defName, queries);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
        for (Object key : getDeclaredParameterKeys()) {
            if (keyType.isInstance(key))
                result.add((T) key);
        }
        return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
                .getMessage());
        }
        return (T) _boundParams.get(p);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'ParameterImpl(Integer, Class)' as a member of raw type 'org.apache.openjpa.persistence.ParameterImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java

                if (key instanceof Integer) {
                    param = new ParameterImpl((Integer) key, expectedValueType);
                } else if (key instanceof String) {
                    param = new ParameterImpl((String) key, expectedValueType);
```

### UNCHECKED_WARNING
Unchecked call to 'ParameterImpl(String, Class)' as a member of raw type 'org.apache.openjpa.persistence.ParameterImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
                    param = new ParameterImpl((Integer) key, expectedValueType);
                } else if (key instanceof String) {
                    param = new ParameterImpl((String) key, expectedValueType);
                } else if (key instanceof Parameter) {
                    param = (Parameter<?>) key;
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.Parameter\>' to 'jakarta.persistence.Parameter'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
        if (param.getParameterType().isAssignableFrom(type))
            throw new IllegalArgumentException(param + " does not match the requested type " + type);
        return (Parameter<T>) param;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.Parameter\>' to 'jakarta.persistence.Parameter'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
        if (param.getParameterType().isAssignableFrom(type))
            throw new IllegalArgumentException(param + " does not match the requested type " + type);
        return (Parameter<T>) param;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set' to 'java.util.Set'. Reason: 'params' has raw type, so result of entrySet is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AbstractQuery.java`
#### Snippet
```java
            clearBinding();
            if (params != null) {
                for (Map.Entry e : (Set<Map.Entry>) params.entrySet()) {
                    setParameter((String) e.getKey(), e.getValue());
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchPlanImpl.java`
#### Snippet
```java
    @Override
    public Collection<String> getFetchGroups() {
        return _fetch.getFetchGroups();
    }

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchPlanImpl.java`
#### Snippet
```java
        if (fields.isEmpty())
            return fields;
        Collection names = new ArrayList(fields);
        for (Object field : fields) {
            names.add(toFieldName(cls, (String) field));
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchPlanImpl.java`
#### Snippet
```java
        Collection names = new ArrayList(fields);
        for (Object field : fields) {
            names.add(toFieldName(cls, (String) field));
        }
        return names;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/FetchPlanImpl.java`
#### Snippet
```java
    @Override
    public Collection<String> getFields() {
        return (Collection<String>) _fetch.getFields();
    }

```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
                    Map rep = pq.reparametrize(params, _em.getBroker());
                    params.clear();
                    params.putAll(rep);
                } catch (UserException ue) {
                    invalidatePreparedQuery();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.List', required 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java

	@Override
    public List getResultList() {
		_em.assertNotCloseInvoked();
		boolean queryFetchPlanUsed = pushQueryFetchPlan();
```

### UNCHECKED_WARNING
Unchecked call to 'DistinctResultList(ResultList, RuntimeExceptionTranslator)' as a member of raw type 'org.apache.openjpa.kernel.DistinctResultList'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
			        RuntimeExceptionTranslator trans = PersistenceExceptions.getRollbackTranslator(_em);
			        if (_query.isDistinct()) {
			            return new DistinctResultList((ResultList) ret, trans);
			        } else {
			            return new DelegatingResultList((ResultList) ret, trans);
```

### UNCHECKED_WARNING
Unchecked call to 'DelegatingResultList(ResultList, RuntimeExceptionTranslator)' as a member of raw type 'org.apache.openjpa.kernel.DelegatingResultList'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
			            return new DistinctResultList((ResultList) ret, trans);
			        } else {
			            return new DelegatingResultList((ResultList) ret, trans);
			        }
			    } else {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'X'. Reason: 'result' has raw type, so result of get is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
                        getQueryString(), result.size()).getMessage());
		    try {
		        return (X)result.get(0);
		    } catch (Exception e) {
                throw new NoResultException(_loc.get("no-result", getQueryString())
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/QueryImpl.java`
#### Snippet
```java
        for (Object o : delegates) {
            if (cls != null && cls != Object.class && cls.isInstance(o))
                return (T)o;
        }
        // Set this transaction to rollback only (as per spec) here because the raised exception
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java
     */
    private static void replaceAsOpenJPAProperty(Map map, Set<String> added, String key, Object val) {
        map.put("openjpa." + key, val);
        added.add(key);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java

            // add all the non-conflicting props in the <properties> section
            map.putAll(props);

            // this isn't a real config property; remove it
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java

        if (!Configurations.containsProperty("Id", map))
            map.put("openjpa.Id", info.getPersistenceUnitName());

        Properties metaFactoryProps = new Properties();
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java
            String key = ProductDerivations.getConfigurationKey
                ("MetaDataFactory", map);
            map.put(key, Configurations.combinePlugins((String) map.get(key),
                Configurations.serializeProperties(metaFactoryProps)));
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java
        // always record provider name for product derivations to access
        if (info.getPersistenceProviderClassName() != null)
            map.put(JPAProperties.PROVIDER, info.getPersistenceProviderClassName());

        // convert validation-mode enum to a StringValue
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java
        // convert validation-mode enum to a StringValue
        if (info.getValidationMode() != null)
            map.put(JPAProperties.VALIDATE_MODE, info.getValidationMode());

        if (info.getPersistenceXMLSchemaVersion() != null) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java

        if (info.getPersistenceXMLSchemaVersion() != null) {
            map.put(PERSISTENCE_VERSION, info.getPersistenceXMLSchemaVersion());
        }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceUnitInfoImpl.java`
#### Snippet
```java

        if (info.getSharedCacheMode() != null) {
            map.put(JPAProperties.CACHE_MODE, info.getSharedCacheMode());
        }

```

### UNCHECKED_WARNING
Unchecked call to 'DelegatingResultList(ResultList, RuntimeExceptionTranslator)' as a member of raw type 'org.apache.openjpa.kernel.DelegatingResultList'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
            RuntimeExceptionTranslator trans = PersistenceExceptions
                    .getRollbackTranslator(_delegate.getEntityManager());
            return new DelegatingResultList(result, trans).iterator().next();
        } catch (Exception ex) {
            throw new jakarta.persistence.PersistenceException(ex);
```

### UNCHECKED_WARNING
Unchecked call to 'DelegatingResultList(ResultList, RuntimeExceptionTranslator)' as a member of raw type 'org.apache.openjpa.kernel.DelegatingResultList'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
            RuntimeExceptionTranslator trans = PersistenceExceptions
                    .getRollbackTranslator(_delegate.getEntityManager());
            return new DelegatingResultList((ResultList) list, trans);
        } catch (Exception ex) {
            throw new jakarta.persistence.PersistenceException(ex);
```

### UNCHECKED_WARNING
Unchecked call to 'ParameterImpl(Integer, Class)' as a member of raw type 'org.apache.openjpa.persistence.ParameterImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
    public StoredProcedureQuery registerStoredProcedureParameter(int position, Class type, ParameterMode mode) {
        buildParametersIfNeeded();
        ParameterImpl param = new ParameterImpl(position, type);
        _delegate.declareParameter(position, param);
        return this;
```

### UNCHECKED_WARNING
Unchecked call to 'ParameterImpl(Integer, Class)' as a member of raw type 'org.apache.openjpa.persistence.ParameterImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
                if (entry.getName() == null) {
                    key = entry.getPosition();
                    param = new ParameterImpl(entry.getPosition(), entry.getType());
                } else {
                    key = entry.getName();
```

### UNCHECKED_WARNING
Unchecked call to 'ParameterImpl(String, Class)' as a member of raw type 'org.apache.openjpa.persistence.ParameterImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
                } else {
                    key = entry.getName();
                    param = new ParameterImpl(entry.getName(), entry.getType());
                }
                _delegate.declareParameter(key, param);
```

### UNCHECKED_WARNING
Unchecked call to 'ParameterImpl(String, Class)' as a member of raw type 'org.apache.openjpa.persistence.ParameterImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/StoredProcedureQueryImpl.java`
#### Snippet
```java
    public StoredProcedureQuery registerStoredProcedureParameter(String name, Class type, ParameterMode mode) {
        buildParametersIfNeeded();
        ParameterImpl param = new ParameterImpl(name, type);
        _delegate.declareParameter(name, param);
        return this;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        for (Map.Entry entry : _seqs.entrySet()) {
            if (entry.getKey() == null)
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
                    entry.getValue()));
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'. Reason: 'entry' has raw type, so result of getValue is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
                    entry.getValue()));
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (seqs == null) {
            seqs = new ArrayList(3); // don't expect many seqs / class
            seqs.add(meta);
            _seqs.put(defName, seqs);
        } else if (!seqs.contains(meta))
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            _seqs.put(defName, seqs);
        } else if (!seqs.contains(meta))
            seqs.add(meta);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (_comp == null)
            _comp = newSerializationComparator();
        Collections.sort(objs, _comp);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.AnnotationPersistenceMetaDataSerializer.SerializationComparator' to 'java.util.Comparator'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (_comp == null)
            _comp = newSerializationComparator();
        Collections.sort(objs, _comp);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        for(AnnotationBuilder ab: builders)
            annos.add(ab.toString());
        output.put(meta, annos);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        }

        List<FieldMetaData> fields = new ArrayList(Arrays.asList
            (meta.getDefinedFieldsInListingOrder()));
        Collections.sort(fields, new FieldComparator());
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        }

        List<FieldMetaData> fields = new ArrayList(Arrays.asList
            (meta.getDefinedFieldsInListingOrder()));
        Collections.sort(fields, new FieldComparator());
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.AnnotationPersistenceMetaDataSerializer.FieldComparator' to 'java.util.Comparator'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        List<FieldMetaData> fields = new ArrayList(Arrays.asList
            (meta.getDefinedFieldsInListingOrder()));
        Collections.sort(fields, new FieldComparator());

        // serialize attr-override
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            addSequenceMetaDatas(all);
        if ((isMetaDataMode() || isMappingMode()) && _metas != null)
            all.addAll(_metas.values());
        if (isMappingMode())
            addSystemMappingElements(all);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        for (Map.Entry entry : _queries.entrySet()) {
            if (entry.getKey() == null)
                all.addAll((List) entry.getValue());
            else if (_mode == MetaDataModes.MODE_QUERY || _metas == null
                || !_metas.containsKey(entry.getKey()))
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            else if (_mode == MetaDataModes.MODE_QUERY || _metas == null
                || !_metas.containsKey(entry.getKey()))
                all.add(new ClassQueries((List<QueryMetaData>)
                    entry.getValue()));
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'. Reason: 'entry' has raw type, so result of getValue is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            else if (_mode == MetaDataModes.MODE_QUERY || _metas == null
                || !_metas.containsKey(entry.getKey()))
                all.add(new ClassQueries((List<QueryMetaData>)
                    entry.getValue()));
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                        (SequenceMetaData) o2);
                case TYPE_CLASS_QUERIES:
                    return ((Comparable) o1).compareTo(o2);
                case TYPE_CLASS_SEQS:
                    return ((Comparable) o1).compareTo(o2);
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                    return ((Comparable) o1).compareTo(o2);
                case TYPE_CLASS_SEQS:
                    return ((Comparable) o1).compareTo(o2);
                default:
                    return compareUnknown(o1, o2);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
        if (queries == null) {
            queries = new ArrayList(3); // don't expect many queries / class
            queries.add(meta);
            _queries.put(defName, queries);
        } else if (!queries.contains(meta))
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            _queries.put(defName, queries);
        } else if (!queries.contains(meta))
            queries.add(meta);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.ManagedType\>' to 'jakarta.persistence.metamodel.IdentifiableType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/Types.java`
#### Snippet
```java
            if (superMeta == null)
                return null;
            return (IdentifiableType<? super X>) model.managedType(superMeta.getDescribedType());
        }

```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/Members.java`
#### Snippet
```java
            }

            return fmd.getMappedByMetaData() == null || !fmd.getType().isAssignableFrom(Collection.class)
                 ? PersistentAttributeType.ONE_TO_ONE
                 : PersistentAttributeType.ONE_TO_MANY;
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.EmbeddableType\>' to 'jakarta.persistence.metamodel.EmbeddableType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    public <X> ManagedType<X> managedType(Class<X> clazz) {
        if (_embeddables.containsKey(clazz))
            return (EmbeddableType<X>) _embeddables.get(clazz);
        if (_entities.containsKey(clazz))
            return (EntityType<X>) _entities.get(clazz);
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.EntityType\>' to 'jakarta.persistence.metamodel.EntityType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
            return (EmbeddableType<X>) _embeddables.get(clazz);
        if (_entities.containsKey(clazz))
            return (EntityType<X>) _entities.get(clazz);
        if (_mappedsupers.containsKey(clazz))
            return (MappedSuperclassType<X>) _mappedsupers.get(clazz);
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.MappedSuperclassType\>' to 'jakarta.persistence.metamodel.MappedSuperclassType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
            return (EntityType<X>) _entities.get(clazz);
        if (_mappedsupers.containsKey(clazz))
            return (MappedSuperclassType<X>) _mappedsupers.get(clazz);
        throw new IllegalArgumentException(_loc.get("type-not-managed", clazz)
            .getMessage());
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.EntityType\>' to 'jakarta.persistence.metamodel.EntityType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    @Override
    public <X> EntityType<X> entity(Class<X> clazz) {
        return (EntityType<X>) find(clazz, _entities, ENTITY, false);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Type\>' to 'jakarta.persistence.metamodel.Type'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
        } catch (IllegalArgumentException ex) {
            if (_basics.containsKey(cls))
                return (Type<X>)_basics.get(cls);
            if (_pseudos.containsKey(cls))
                return (Type<X>)_pseudos.get(cls);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Types.PseudoEntity\>' to 'jakarta.persistence.metamodel.Type'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
                return (Type<X>)_basics.get(cls);
            if (_pseudos.containsKey(cls))
                return (Type<X>)_pseudos.get(cls);
            if (java.util.Map.class.isAssignableFrom(cls)) {
                Types.PseudoEntity<X> pseudo = new Types.PseudoEntity(cls, this);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Types.PseudoEntity' to 'org.apache.openjpa.persistence.meta.Types.PseudoEntity'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
                return (Type<X>)_pseudos.get(cls);
            if (java.util.Map.class.isAssignableFrom(cls)) {
                Types.PseudoEntity<X> pseudo = new Types.PseudoEntity(cls, this);
                _pseudos.put(cls, new Types.PseudoEntity(cls, this));
                return pseudo;
```

### UNCHECKED_WARNING
Unchecked call to 'PseudoEntity(Class, MetamodelImpl)' as a member of raw type 'org.apache.openjpa.persistence.meta.Types.PseudoEntity'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
                return (Type<X>)_pseudos.get(cls);
            if (java.util.Map.class.isAssignableFrom(cls)) {
                Types.PseudoEntity<X> pseudo = new Types.PseudoEntity(cls, this);
                _pseudos.put(cls, new Types.PseudoEntity(cls, this));
                return pseudo;
```

### UNCHECKED_WARNING
Unchecked call to 'PseudoEntity(Class, MetamodelImpl)' as a member of raw type 'org.apache.openjpa.persistence.meta.Types.PseudoEntity'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
            if (java.util.Map.class.isAssignableFrom(cls)) {
                Types.PseudoEntity<X> pseudo = new Types.PseudoEntity(cls, this);
                _pseudos.put(cls, new Types.PseudoEntity(cls, this));
                return pseudo;
            } else {
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.EmbeddableType\>' to 'jakarta.persistence.metamodel.EmbeddableType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
    @Override
    public <X> EmbeddableType<X> embeddable(Class<X> clazz) {
        return (EmbeddableType<X>)find(clazz, _embeddables, EMBEDDABLE, false);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.EntityType\>' to 'jakarta.persistence.metamodel.EntityType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java

    public <X> EntityType<X> entityImpl(Class<X> clazz) {
        return (EntityType<X>) find(clazz, _entities, ENTITY, true);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        ConfigurationParser parser = new ConfigurationParser(null);
        parser.parse(file);
        return load(findUnit((List<PersistenceUnitInfoImpl>)
            parser.getResults(), anchor, null), null);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
        for (URL url : urls) {
            parser.parse(url);
            pinfos.addAll((List<PersistenceUnitInfoImpl>) parser.getResults());
        }
        return findUnit(pinfos, name, loader);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java

    private List<String> getUnitNames(ConfigurationParser parser) {
        List<PersistenceUnitInfoImpl> units = parser.getResults();
        List<String> names = new ArrayList<>();
        for (PersistenceUnitInfoImpl unit : units){
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.List', required 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java

    @Override
    public List getAnchorsInResource(String resource) throws Exception {
        ConfigurationParser parser = new ConfigurationParser(null);
        try {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
                for (URL url : urls) {
                    parser.parse(url);
                    results.addAll(getUnitNames(parser));
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set\>' to 'java.util.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
     {
        List<? extends Element> allMembers = type.getEnclosedElements();
        Set<ExecutableElement> allMethods = (Set<ExecutableElement>)
            filter(allMembers, methodFilter, nonTransientFilter);

        Set<ExecutableElement> getters = filter(allMethods, getterFilter);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set\>' to 'java.util.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
    private Set<Element> getFieldAccessPersistentMembers(TypeElement type) {
        List<? extends Element> allMembers = type.getEnclosedElements();
        Set<VariableElement> allFields = (Set<VariableElement>)
           filter(allMembers, fieldFilter, nonTransientFilter);
        Set<ExecutableElement> allMethods = (Set<ExecutableElement>)
            filter(allMembers, methodFilter, nonTransientFilter);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set\>' to 'java.util.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        Set<VariableElement> allFields = (Set<VariableElement>)
           filter(allMembers, fieldFilter, nonTransientFilter);
        Set<ExecutableElement> allMethods = (Set<ExecutableElement>)
            filter(allMembers, methodFilter, nonTransientFilter);
        Set<ExecutableElement> getters = filter(allMethods, getterFilter,
        		propertyAccessFilter, annotatedFilter);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set\>' to 'java.util.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
    private int getImplicitAccessType(TypeElement type) {
        List<? extends Element> allMembers = type.getEnclosedElements();
        Set<VariableElement> allFields = (Set<VariableElement>) filter(allMembers, fieldFilter, nonTransientFilter);
        Set<ExecutableElement> allMethods = (Set<ExecutableElement>) filter(allMembers, methodFilter,
                nonTransientFilter);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set\>' to 'java.util.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        List<? extends Element> allMembers = type.getEnclosedElements();
        Set<VariableElement> allFields = (Set<VariableElement>) filter(allMembers, fieldFilter, nonTransientFilter);
        Set<ExecutableElement> allMethods = (Set<ExecutableElement>) filter(allMembers, methodFilter,
                nonTransientFilter);

        Set<VariableElement> annotatedFields = filter(allFields, annotatedFilter);
```

### UNCHECKED_WARNING
Unchecked call to 'includes(T)' as a member of raw type 'org.apache.openjpa.persistence.meta.SourceAnnotationHandler.InclusiveFilter'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
            boolean include = true;
            for (InclusiveFilter f : filters) {
                if (!f.includes(e)) {
                    include = false;
                    break;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set\>' to 'java.util.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        List<? extends Element> allMembers = type.getEnclosedElements();
        if (AccessCode.isField(access)) {
            Set<VariableElement> allFields = (Set<VariableElement>)
                filter(allMembers, fieldFilter, nonTransientFilter);
            result.addAll(allFields);
        } else {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set\>' to 'java.util.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
            result.addAll(allFields);
        } else {
            Set<ExecutableElement> allMethods = (Set<ExecutableElement>)
               filter(allMembers, methodFilter, nonTransientFilter);
            Set<ExecutableElement> getters = filter(allMethods, getterFilter);
            Set<ExecutableElement> setters = filter(allMethods, setterFilter);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\[\]' to 'java.util.Collection\[\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java

        if (_callbacks == null) {
            _callbacks = (Collection<LifecycleCallbacks>[])
                new Collection[LifecycleEvent.ALL_EVENTS.length];
            if (!system)
                _highs = new int[LifecycleEvent.ALL_EVENTS.length];
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\[\]' to 'java.util.Collection\[\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java

        if (_callbacks == null) {
            _callbacks = (Collection<LifecycleCallbacks>[])
                new Collection[LifecycleEvent.ALL_EVENTS.length];
            if (!system)
                _highs = new int[LifecycleEvent.ALL_EVENTS.length];
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\[\]' to 'java.util.Collection\[\]'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java

                if (callbacks == null)
                    callbacks = (Collection<LifecycleCallbacks>[])
                        new Collection[LifecycleEvent.ALL_EVENTS.length];

                for (int e : events) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
    public <T> T createInstance(Class<T> cls) {
        assertNotCloseInvoked();
        return (T) _broker.newInstance(cls);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
    @Override
    public OpenJPAQuery createNativeQuery(String query, Class cls) {
        checkTuple(cls);
        return createNativeQuery(query).setResultClass(cls);
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.QueryImpl' to 'org.apache.openjpa.persistence.QueryImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
        org.apache.openjpa.kernel.Query kernelQuery =_broker.newQuery(OpenJPACriteriaBuilder.LANG_CRITERIA, criteriaQuery);

        QueryImpl<T> facadeQuery = newQueryImpl(kernelQuery, null).setId(criteriaQuery.toString());
        Set<ParameterExpression<?>> params = criteriaQuery.getParameters();

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.kernel.Extent' to 'org.apache.openjpa.kernel.Extent'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
    public <T> Extent<T> createExtent(Class<T> cls, boolean subclasses) {
        assertNotCloseInvoked();
        return new ExtentImpl<T>(this, _broker.newExtent(cls, subclasses));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
         if (result == null)
             notFoundException("version-not-found", "", type);
         return (SingularAttribute<? super X, Y>)result;
     }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-decl-single",name, type);

        return (SingularAttribute<X, Y>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'E'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            if ((f1 == null || f1.selects(t)) && (f2 == null || f2.selects(t))
             && (f3 == null || f3.selects(t)) && (f4 == null || f4.selects(t)))
                result.add((E) t);
        }
        return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                 new IdAttributeFilter<>());
         if (result != null)
             return (SingularAttribute<X, Y>) result;
         throw new IllegalArgumentException();
     }
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.SetAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-decl-set", name, elementType);

        return (SetAttribute<X, E>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
         if (result == null)
             notFoundException("decl-version-not-found", "", type);
         return (SingularAttribute<X, Y>)result;
     }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.SetAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-set",name, elementType);

        return (SetAttribute<? super X, E>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                 new IdAttributeFilter<>());
         if (result != null)
             return (SingularAttribute<? super X, Y>) result;
         throw new IllegalArgumentException();
     }
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.ListAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-decl-list", name, elementType);

        return (ListAttribute<X, E>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.CollectionAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-decl-coll", name, elementType);

        return (CollectionAttribute<X, E>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.MapAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-map", name, keyType, valueType);

        return (MapAttribute<? super X, K, V>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-decl-single",name, type);

        return (Attribute<X, Y>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.CollectionAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-coll", name, elementType);

        return (CollectionAttribute<? super X, E>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.MapAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-decl-map", name, keyType, valueType);

        return (MapAttribute<X, K, V>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found", name, type);

        return (Attribute<? super X, Y>)result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.ListAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-list",name, elementType);

        return (ListAttribute<? super X, E>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
     */
    public AbstractManagedType(ClassMetaData meta, MetamodelImpl model) {
        super((Class<X>) meta.getDescribedType());

        this.model = model;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.SingularAttributeImpl' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.INPUT_READER:
            case JavaTypes.INPUT_STREAM:
                attrs.add(new Members.SingularAttributeImpl(this, f));
                break;
            case JavaTypes.ARRAY:
```

### UNCHECKED_WARNING
Unchecked call to 'SingularAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.SingularAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            case JavaTypes.INPUT_READER:
            case JavaTypes.INPUT_STREAM:
                attrs.add(new Members.SingularAttributeImpl(this, f));
                break;
            case JavaTypes.ARRAY:
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.ListAttributeImpl' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                Compatibility compat = meta.getRepository().getConfiguration().getCompatibilityInstance();
                if(compat.getUseListAttributeForArrays() || f.isPersistentCollection()) {
                    attrs.add(new Members.ListAttributeImpl(this, f));
                }
                else {
```

### UNCHECKED_WARNING
Unchecked call to 'ListAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.ListAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                Compatibility compat = meta.getRepository().getConfiguration().getCompatibilityInstance();
                if(compat.getUseListAttributeForArrays() || f.isPersistentCollection()) {
                    attrs.add(new Members.ListAttributeImpl(this, f));
                }
                else {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.SingularAttributeImpl' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                }
                else {
                    attrs.add(new Members.SingularAttributeImpl(this, f));
                }
                break;
```

### UNCHECKED_WARNING
Unchecked call to 'SingularAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.SingularAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                }
                else {
                    attrs.add(new Members.SingularAttributeImpl(this, f));
                }
                break;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.CollectionAttributeImpl' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                switch (MetamodelImpl.categorizeCollection(f.getDeclaredType())) {
                case COLLECTION:
                    attrs.add(new Members.CollectionAttributeImpl(this, f));
                    break;
                case LIST:
```

### UNCHECKED_WARNING
Unchecked call to 'CollectionAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.CollectionAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                switch (MetamodelImpl.categorizeCollection(f.getDeclaredType())) {
                case COLLECTION:
                    attrs.add(new Members.CollectionAttributeImpl(this, f));
                    break;
                case LIST:
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.ListAttributeImpl' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                    break;
                case LIST:
                    attrs.add(new Members.ListAttributeImpl(this, f));
                    break;
                case SET:
```

### UNCHECKED_WARNING
Unchecked call to 'ListAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.ListAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                    break;
                case LIST:
                    attrs.add(new Members.ListAttributeImpl(this, f));
                    break;
                case SET:
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.SetAttributeImpl' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                    break;
                case SET:
                    attrs.add(new Members.SetAttributeImpl(this, f));
                    break;
                }
```

### UNCHECKED_WARNING
Unchecked call to 'SetAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.SetAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                    break;
                case SET:
                    attrs.add(new Members.SetAttributeImpl(this, f));
                    break;
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.MapAttributeImpl' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.MAP:
                attrs.add(new Members.MapAttributeImpl(this, f));
                break;
            case JavaTypes.ENUM:
```

### UNCHECKED_WARNING
Unchecked call to 'MapAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.MapAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.MAP:
                attrs.add(new Members.MapAttributeImpl(this, f));
                break;
            case JavaTypes.ENUM:
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.SingularAttributeImpl' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.ENUM:
                attrs.add(new Members.SingularAttributeImpl(this, f));
                break;
            default:
```

### UNCHECKED_WARNING
Unchecked call to 'SingularAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.SingularAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
                break;
            case JavaTypes.ENUM:
                attrs.add(new Members.SingularAttributeImpl(this, f));
                break;
            default:
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
            notFoundException("attr-not-found-single", name, type);

        return (SingularAttribute<? super X, Y>) result;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Expression\>' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ExpressionImpl.java`
#### Snippet
```java
    public Predicate in(Expression<Collection<?>> values) {
        In<X> result = new Expressions.In<>(this);
        result.value((Expression<? extends X>)values);
        return result;
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Expression\>' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ExpressionImpl.java`
#### Snippet
```java
        In<X> result = new Expressions.In<>(this);
        for (Expression<?> e : values)
        	result.value((Expression<? extends X>)e);
        return result;
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ExpressionImpl.java`
#### Snippet
```java
        In<X> result = new Expressions.In<>(this);
        for (Object v : values)
        	result.value((X)v);
        return result;
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'X'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ExpressionImpl.java`
#### Snippet
```java
        In<X> result = new Expressions.In<>(this);
        for (Object e : values)
        	result.value((X)e);
        return result;
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.criteria.ExpressionImpl' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/ExpressionImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> as(Class<Y> type) {
       return type == getJavaType() ? (Expression<Y>)this : new Expressions.CastAs<>(type, this);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.PluralAttribute,capture,Y\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    @Override
    public <Y> Fetch<X, Y> fetch(PluralAttribute<? super X, ?, Y> assoc, JoinType jt) {
        return addFetch((Members.Member<? super X,Y>)assoc, jt);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.SingularAttribute,Y\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    @Override
    public <Y> Fetch<X, Y> fetch(SingularAttribute<? super X, Y> assoc) {
        return addFetch((Members.Member<? super X,Y>)assoc, JoinType.INNER);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Members.Member,capture\>' to 'jakarta.persistence.metamodel.Attribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    @Override
    public Attribute<Z, X> getAttribute() {
        return (Attribute<Z, X>)_member;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Type\>' to 'jakarta.persistence.metamodel.ManagedType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    @Override
    public <X,Y> Fetch<X, Y> fetch(String assocName, JoinType jt) {
        Attribute<? super X, ?> assoc = ((ManagedType<X>)_member.getType()).getAttribute(assocName);
        return addFetch((Members.Member<? super X,Y>)assoc, jt);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    public <X,Y> Fetch<X, Y> fetch(String assocName, JoinType jt) {
        Attribute<? super X, ?> assoc = ((ManagedType<X>)_member.getType()).getAttribute(assocName);
        return addFetch((Members.Member<? super X,Y>)assoc, jt);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.PluralAttribute,capture,Y\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    @Override
    public <Y> Fetch<X, Y> fetch(PluralAttribute<? super X, ?, Y> assoc) {
        return addFetch((Members.Member<? super X,Y>)assoc, JoinType.INNER);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.SingularAttribute,Y\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    @Override
    public <Y> Fetch<X, Y> fetch(SingularAttribute<? super X, Y> assoc, JoinType jt) {
        return addFetch((Members.Member<? super X,Y>)assoc, jt);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.criteria.PathImpl,Z\>' to 'jakarta.persistence.criteria.FetchParent'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
    @Override
    public FetchParent<?, Z> getParent() {
        return (FetchParent<?, Z>)_parent;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.FetchPathImpl' to 'jakarta.persistence.criteria.Fetch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java

    private <X,Y> Fetch<X,Y> addFetch(Members.Member<? super X, Y> member, JoinType jt) {
        Fetch<X,Y> fetch = new FetchPathImpl(this, member, jt);
        if (_fetches == null)
            _fetches = new HashSet<>();
```

### UNCHECKED_WARNING
Unchecked call to 'FetchPathImpl(FetchParent, Member, JoinType)' as a member of raw type 'org.apache.openjpa.persistence.criteria.FetchPathImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java

    private <X,Y> Fetch<X,Y> addFetch(Members.Member<? super X, Y> member, JoinType jt) {
        Fetch<X,Y> fetch = new FetchPathImpl(this, member, jt);
        if (_fetches == null)
            _fetches = new HashSet<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.FetchParent,Z\>' to 'org.apache.openjpa.persistence.criteria.PathImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java

    FetchPathImpl(FetchParent<?,Z> parent, Members.Member<? super Z,X> member, JoinType type) {
        super((PathImpl<?,Z>)parent, member, member.getJavaType());
        this.joinType = type;
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.criteria.Fetch' to 'jakarta.persistence.criteria.Fetch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FetchPathImpl.java`
#### Snippet
```java
        Set<Fetch<X,?>> result = new HashSet<>();
        for (Fetch f : _fetches) {
            result.add(f);
        }
        return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.util.SourceCode.Element' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        public T makeFinal() {
            isFinal = true;
            return (T)this;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.util.SourceCode.Element' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        public T makeStatic() {
            isStatic = true;
            return (T)this;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.util.SourceCode.Element' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        public T makePrivate() {
            access = ACCESS.PRIVATE;
            return (T)this;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.util.SourceCode.Element' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        public T makeProtected() {
            access = ACCESS.PROTECTED;
            return (T)this;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.util.SourceCode.Element' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
        public T makePublic() {
            access = ACCESS.PUBLIC;
            return (T)this;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.util.SourceCode.Element' to 'T'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
                comment.append(line);
            }
            return (T)this;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
            super(cls);
//            assertNoCompoundSelection(args);
            _args = args == null ? (List<Selection<?>>)Collections.EMPTY_LIST : Arrays.asList(args);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'Array(Class)' as a member of raw type 'org.apache.openjpa.kernel.FillStrategy.Array'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
           } else {
               strategy = resultClass.isArray()
                        ? new FillStrategy.Array(resultClass)
                        : new FillStrategy.NewInstance(resultClass);
           }
```

### UNCHECKED_WARNING
Unchecked call to 'NewInstance(Class)' as a member of raw type 'org.apache.openjpa.kernel.FillStrategy.NewInstance'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
               strategy = resultClass.isArray()
                        ? new FillStrategy.Array(resultClass)
                        : new FillStrategy.NewInstance(resultClass);
           }
            return (FillStrategy<T>)strategy;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.kernel.FillStrategy\>' to 'org.apache.openjpa.kernel.FillStrategy'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompoundSelections.java`
#### Snippet
```java
                        : new FillStrategy.NewInstance(resultClass);
           }
            return (FillStrategy<T>)strategy;
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'CompareByExample(CriteriaBuilder, ManagedType, Path, T, ComparisonStyle, Attribute...)' as a member of raw type 'org.apache.openjpa.persistence.criteria.CompareByExample'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CompareByExample.java`
#### Snippet
```java
            }
            if (attr.isAssociation()) {
                p = new CompareByExample(builder, (ManagedType<?>)attr.getType(),
                        from.get(attr), value, style, excludes);
            } else if (attr.getJavaType() == String.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.SetJoin\>' to 'jakarta.persistence.criteria.SetJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <W,Y> SetJoin<W, Y>  joinSet(String attr) {
        assertJoinable(type);
        return (SetJoin<W, Y>)join(((ManagedType<X>)type).getSet(attr));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Type\>' to 'jakarta.persistence.metamodel.Type'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    protected FromImpl(PathImpl<?,Z> parent, Members.Member<? super Z, ?> m, Class<X> x) {
        super(parent, m, x);
        this.type = (Type<X>)m.getType();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.PluralAttribute,capture,Y\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <Y> Fetch<X, Y> fetch(PluralAttribute<? super X, ?, Y> assoc,
        JoinType jt) {
        return addFetch((Members.Member<? super X, Y>)assoc, jt);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.MapJoin,capture\>' to 'jakarta.persistence.criteria.MapJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <W,K,V> MapJoin<W,K,V>  joinMap(String attr, JoinType jt) {
        assertJoinable(type);
        return (MapJoin<W,K,V>)join(((ManagedType<X>)type).getMap(attr));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.MapJoin,capture\>' to 'jakarta.persistence.criteria.MapJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <W,K,V> MapJoin<W,K,V> joinMap(String attr) {
        assertJoinable(type);
        return (MapJoin<W,K,V>)join(((ManagedType<X>)type).getMap(attr));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.criteria.PathImpl,capture\>' to 'jakarta.persistence.criteria.From'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    @Override
    public From<Z,X> getCorrelationParent() {
        return (From<Z,X>)getCorrelatedPath();
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Type' to 'jakarta.persistence.metamodel.ManagedType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <X,Y> Fetch<X, Y> fetch(String name, JoinType jt) {
        assertJoinable(type);
        Attribute<? super X,?> attr = ((ManagedType<X>)type).getAttribute(name);
        if (attr.isCollection()) {
            return fetch((PluralAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.criteria.Fetch' to 'jakarta.persistence.criteria.Fetch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
        Attribute<? super X,?> attr = ((ManagedType<X>)type).getAttribute(name);
        if (attr.isCollection()) {
            return fetch((PluralAttribute)attr, jt);
        } else {
            return fetch(((SingularAttribute)attr), jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.PluralAttribute' to 'jakarta.persistence.metamodel.PluralAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
        Attribute<? super X,?> attr = ((ManagedType<X>)type).getAttribute(name);
        if (attr.isCollection()) {
            return fetch((PluralAttribute)attr, jt);
        } else {
            return fetch(((SingularAttribute)attr), jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.criteria.Fetch' to 'jakarta.persistence.criteria.Fetch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return fetch((PluralAttribute)attr, jt);
        } else {
            return fetch(((SingularAttribute)attr), jt);
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.SingularAttribute' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return fetch((PluralAttribute)attr, jt);
        } else {
            return fetch(((SingularAttribute)attr), jt);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.SingularAttribute,Y\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    @Override
    public <Y> Fetch<X, Y> fetch(SingularAttribute<? super X, Y> assoc, JoinType jt) {
        return addFetch((Members.Member<? super X, Y>)assoc, jt);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.CollectionJoin\>' to 'jakarta.persistence.criteria.CollectionJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <W,Y> CollectionJoin<W, Y> joinCollection(String attr, JoinType jt) {
        assertJoinable(type);
        return (CollectionJoin<W,Y>)join(((ManagedType<X>)type).getCollection(attr), jt);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.SetJoin\>' to 'jakarta.persistence.criteria.SetJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <W,Y> SetJoin<W, Y>  joinSet(String attr, JoinType jt) {
        assertJoinable(type);
        return (SetJoin<W, Y>)join(((ManagedType<X>)type).getSet(attr), jt);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.ListJoin\>' to 'jakarta.persistence.criteria.ListJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <W,Y> ListJoin<W,Y> joinList(String attr, JoinType jt) {
        assertJoinable(type);
        return (ListJoin<W,Y>)join(((ManagedType<X>)type).getList(attr), jt);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.CollectionJoin\>' to 'jakarta.persistence.criteria.CollectionJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <W,Y> CollectionJoin<W, Y> joinCollection(String attr) {
        assertJoinable(type);
        return (CollectionJoin<W,Y>)join(((ManagedType<X>)type).getCollection(attr), JoinType.INNER);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.criteria.Join' to 'jakarta.persistence.criteria.Join'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
        assertJoinable(attr.getDeclaringType());
        if (attr instanceof SingularAttribute) {
            return join((SingularAttribute)attr, jt);
        } else if (attr instanceof ListAttribute) {
            return join((ListAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.SingularAttribute' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
        assertJoinable(attr.getDeclaringType());
        if (attr instanceof SingularAttribute) {
            return join((SingularAttribute)attr, jt);
        } else if (attr instanceof ListAttribute) {
            return join((ListAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.criteria.ListJoin' to 'jakarta.persistence.criteria.Join'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return join((SingularAttribute)attr, jt);
        } else if (attr instanceof ListAttribute) {
            return join((ListAttribute)attr, jt);
        } else if (attr instanceof SetAttribute) {
            return join((SetAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.ListAttribute' to 'jakarta.persistence.metamodel.ListAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return join((SingularAttribute)attr, jt);
        } else if (attr instanceof ListAttribute) {
            return join((ListAttribute)attr, jt);
        } else if (attr instanceof SetAttribute) {
            return join((SetAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.criteria.SetJoin' to 'jakarta.persistence.criteria.Join'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return join((ListAttribute)attr, jt);
        } else if (attr instanceof SetAttribute) {
            return join((SetAttribute)attr, jt);
        } else if (attr instanceof CollectionAttribute) {
            return join((CollectionAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.SetAttribute' to 'jakarta.persistence.metamodel.SetAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return join((ListAttribute)attr, jt);
        } else if (attr instanceof SetAttribute) {
            return join((SetAttribute)attr, jt);
        } else if (attr instanceof CollectionAttribute) {
            return join((CollectionAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.criteria.CollectionJoin' to 'jakarta.persistence.criteria.Join'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return join((SetAttribute)attr, jt);
        } else if (attr instanceof CollectionAttribute) {
            return join((CollectionAttribute)attr, jt);
        } else if (attr instanceof MapAttribute) {
            return join((MapAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.CollectionAttribute' to 'jakarta.persistence.metamodel.CollectionAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return join((SetAttribute)attr, jt);
        } else if (attr instanceof CollectionAttribute) {
            return join((CollectionAttribute)attr, jt);
        } else if (attr instanceof MapAttribute) {
            return join((MapAttribute)attr, jt);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.criteria.MapJoin' to 'jakarta.persistence.criteria.Join'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return join((CollectionAttribute)attr, jt);
        } else if (attr instanceof MapAttribute) {
            return join((MapAttribute)attr, jt);
        } else {
            throw new IllegalArgumentException(name);
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.MapAttribute' to 'jakarta.persistence.metamodel.MapAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
            return join((CollectionAttribute)attr, jt);
        } else if (attr instanceof MapAttribute) {
            return join((MapAttribute)attr, jt);
        } else {
            throw new IllegalArgumentException(name);
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.ListJoin\>' to 'jakarta.persistence.criteria.ListJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    public <W,Y> ListJoin<W, Y> joinList(String attr) {
        assertJoinable(type);
        return (ListJoin<W,Y>)join(((ManagedType<X>)type).getList(attr), JoinType.INNER);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.FetchPathImpl' to 'jakarta.persistence.criteria.Fetch'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    private <Y> Fetch<X,Y> addFetch(Members.Member<? super X, Y> member,
            JoinType jt) {
        Fetch<X,Y> fetch = new FetchPathImpl(this, member, jt);
        if (_fetches == null)
            _fetches = new HashSet<>();
```

### UNCHECKED_WARNING
Unchecked call to 'FetchPathImpl(FetchParent, Member, JoinType)' as a member of raw type 'org.apache.openjpa.persistence.criteria.FetchPathImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/FromImpl.java`
#### Snippet
```java
    private <Y> Fetch<X,Y> addFetch(Members.Member<? super X, Y> member,
            JoinType jt) {
        Fetch<X,Y> fetch = new FetchPathImpl(this, member, jt);
        if (_fetches == null)
            _fetches = new HashSet<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Collection\>'. Reason: 'j' has raw type, so result of getJoins is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
        for (Join j : joins) {
            buffer.append(((CriteriaExpression)j).asVariable(this)).append(" ");
            renderJoins(buffer, j.getJoins());
            renderFetches(buffer, j.getFetches());
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'j' has raw type, so result of getFetches is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
            buffer.append(((CriteriaExpression)j).asVariable(this)).append(" ");
            renderJoins(buffer, j.getJoins());
            renderFetches(buffer, j.getFetches());
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.CompoundSelections.MultiSelection' to 'jakarta.persistence.criteria.Selection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    public CriteriaQuery<T> multiselect(Selection<?>... selections) {
        _selections = Arrays.asList(selections); // do not telescope
        _selection  = new CompoundSelections.MultiSelection(_resultClass, selections);
        return this;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'MultiSelection(Class, Selection\[\])' as a member of raw type 'org.apache.openjpa.persistence.criteria.CompoundSelections.MultiSelection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    public CriteriaQuery<T> multiselect(Selection<?>... selections) {
        _selections = Arrays.asList(selections); // do not telescope
        _selection  = new CompoundSelections.MultiSelection(_resultClass, selections);
        return this;
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Collection\>'. Reason: 'r' has raw type, so result of getJoins is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
            buffer.append(((ExpressionImpl<?>)r).asVariable(this));
            if (++i != roots.size()) buffer.append(", ");
            renderJoins(buffer, r.getJoins());
            renderFetches(buffer, r.getFetches());
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'r' has raw type, so result of getFetches is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
            if (++i != roots.size()) buffer.append(", ");
            renderJoins(buffer, r.getJoins());
            renderFetches(buffer, r.getFetches());
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    public Set<ParameterExpression<?>> getParameters() {
        collectParameters(new CriteriaExpressionVisitor.ParameterVisitor(this));
        return (Set) _params.keySet();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Selection\>' to 'jakarta.persistence.criteria.Selection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java
    @Override
    public Selection<T> getSelection() {
        return (Selection<T>)_selection;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.meta.Members.SingularAttributeImpl' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
        if (_parent.isEmbedded) {
            FieldMetaData fmd = getEmbeddedFieldMetaData(member.fmd);
            _member = new Members.SingularAttributeImpl(member.owner, fmd);
        } else {
            _member = member;
```

### UNCHECKED_WARNING
Unchecked call to 'SingularAttributeImpl(AbstractManagedType, FieldMetaData)' as a member of raw type 'org.apache.openjpa.persistence.meta.Members.SingularAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
        if (_parent.isEmbedded) {
            FieldMetaData fmd = getEmbeddedFieldMetaData(member.fmd);
            _member = new Members.SingularAttributeImpl(member.owner, fmd);
        } else {
            _member = member;
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.SingularAttribute' to 'jakarta.persistence.metamodel.SingularAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
    public <Y> Path<Y> get(SingularAttribute<? super X, Y> attr) {
    	if (getType() != attr.getDeclaringType()) {
    		attr = (SingularAttribute)((ManagedType)getType()).getAttribute(attr.getName());
    	}
        return new PathImpl<>(this, (Members.SingularAttributeImpl<? super X, Y>)attr, attr.getJavaType());
```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.MapAttribute' to 'jakarta.persistence.metamodel.MapAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
    public <K, V, M extends java.util.Map<K, V>> Expression<M> get(MapAttribute<X, K, V> map) {
    	if (getType() != map.getDeclaringType()) {
    		map = (MapAttribute)((ManagedType)getType()).getAttribute(map.getName());
    	}
        return new PathImpl<>(this, (Members.MapAttributeImpl<? super X,K,V>)map, (Class<M>)map.getJavaType());
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
    		map = (MapAttribute)((ManagedType)getType()).getAttribute(map.getName());
    	}
        return new PathImpl<>(this, (Members.MapAttributeImpl<? super X,K,V>)map, (Class<M>)map.getJavaType());
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'jakarta.persistence.metamodel.PluralAttribute' to 'jakarta.persistence.metamodel.PluralAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
    public <E, C extends java.util.Collection<E>> Expression<C>  get(PluralAttribute<X, C, E> coll) {
    	if (getType() != coll.getDeclaringType()) {
    		coll = (PluralAttribute)((ManagedType)getType()).getAttribute(coll.getName());
    	}
        return new PathImpl<>(this, (Members.PluralAttributeImpl<? super X, C, E>)coll, coll.getJavaType());
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Members.Member,capture\>' to 'jakarta.persistence.metamodel.Bindable'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
            throw new IllegalArgumentException(this + " represents a basic path and not a bindable");
        }
        return (Bindable<X>)_member;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute\>,capture\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
        }

        Members.Member<? super X, Y> next = (Members.Member<? super X, Y>)
           ((ManagedType<? super X>)type).getAttribute(attName);
        return new PathImpl<>(this, next, next.getJavaType());
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Type\>' to 'jakarta.persistence.metamodel.ManagedType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java

        Members.Member<? super X, Y> next = (Members.Member<? super X, Y>)
           ((ManagedType<? super X>)type).getAttribute(attName);
        return new PathImpl<>(this, next, next.getJavaType());
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.concurrent.CopyOnWriteArraySet' to 'java.util.Set\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
    @Override
    public Set<Join<?,?>> getCorrelatedJoins() {
        return _corrJoins == null ? Collections.emptySet() : new CopyOnWriteArraySet(_corrJoins);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'CopyOnWriteArraySet(Collection)' as a member of raw type 'java.util.concurrent.CopyOnWriteArraySet'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
    @Override
    public Set<Join<?,?>> getCorrelatedJoins() {
        return _corrJoins == null ? Collections.emptySet() : new CopyOnWriteArraySet(_corrJoins);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Join' to 'jakarta.persistence.criteria.ListJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
            _corrJoins = new ArrayList<>();
        _corrJoins.add(corrJoin);
        return (ListJoin<X,Y>)corrJoin;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Join' to 'jakarta.persistence.criteria.SetJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
            _corrJoins = new ArrayList<>();
        _corrJoins.add(corrJoin);
        return (SetJoin<X,Y>)corrJoin;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Join,capture\>' to 'jakarta.persistence.criteria.Join'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
            _corrJoins = new ArrayList<>();
        _corrJoins.add(corrJoin);
        return (Join<X,Y>)corrJoin;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Join' to 'jakarta.persistence.criteria.MapJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
            _corrJoins = new ArrayList<>();
        _corrJoins.add(corrJoin);
        return (MapJoin<X,K,V>)corrJoin;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Join' to 'jakarta.persistence.criteria.CollectionJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
            _corrJoins = new ArrayList<>();
        _corrJoins.add(corrJoin);
        return (CollectionJoin<X,Y>)corrJoin;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'root' has raw type, so result of getFetches is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        Set<Root<?>> roots = q.getRoots();
        for (Root root : roots) {
            Set<Fetch> fetches = root.getFetches();
            if (fetches == null)
                continue;
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        ResultShape<?> result = null;
        if (q.isMultiselect()) {
           result = new ResultShape(resultClass, CompoundSelections.getFillStrategy(q.getSelection()));
           for (Selection<?> term : selections) {
               result.nest(getShape(q, result, term));
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
                strategy = new FillStrategy.Factory<>(factory,  TupleImpl.PUT);
            }
            result = new ResultShape(resultClass, strategy);
            if (q.getSelectionList() == null) {
                return result;
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
        if (s.isCompoundSelection()) {
            CompoundSelection<?> cs = (CompoundSelection<?>)s;
            result = new ResultShape(s.getJavaType(), CompoundSelections.getFillStrategy(cs));
            List<Selection<?>> terms = cs.getCompoundSelectionItems();
            for (Selection<?> term : terms) {
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy, boolean)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
                Class<?> componentType = parent.getType().getComponentType();
                if (componentType == Tuple.class) {
                    result = new ResultShape(componentType,
                         new FillStrategy.Factory(new TupleFactory(s), TupleImpl.PUT), false);
                } else {
```

### UNCHECKED_WARNING
Unchecked call to 'Factory(ObjectFactory, Method)' as a member of raw type 'org.apache.openjpa.kernel.FillStrategy.Factory'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
                if (componentType == Tuple.class) {
                    result = new ResultShape(componentType,
                         new FillStrategy.Factory(new TupleFactory(s), TupleImpl.PUT), false);
                } else {
                    result = new ResultShape(componentType, new FillStrategy.Assign(), true);
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy, boolean)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
                         new FillStrategy.Factory(new TupleFactory(s), TupleImpl.PUT), false);
                } else {
                    result = new ResultShape(componentType, new FillStrategy.Assign(), true);
                }
            } else {
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy, boolean)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
                }
            } else {
                result = new ResultShape(type, new FillStrategy.Assign(), true);
            }
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
                    _loc.get("valdiate-property-failed",
                        arg0.getClass().getName(),property).getMessage(),
                    (Set)violations),
                true);
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
                    _loc.get("validate-value-failed", arg0.getName(),
                        arg1, arg2.toString()).getMessage(),
                    (Set)violations),
                true);
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/validation/ValidatorImpl.java`
#### Snippet
```java
                    _loc.get("validate-failed",
                        arg0.getClass().getName()).getMessage(),
                    (Set)violations),
                true);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Members.Member,capture\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public Attribute<? super Z, E> getAttribute() {
            return (Member<? super Z, E>)_member;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Members.Member,capture\>' to 'org.apache.openjpa.persistence.meta.Members.Member'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public Member<? extends Z, X> getMember() {
            return (Member<? extends Z, X>) _member;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.criteria.Joins.Map' to 'org.apache.openjpa.persistence.criteria.FromImpl\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            KeyAttributeImpl<java.util.Map<K,V>, K> keyAttr =
              new Members.KeyAttributeImpl<>(pseudoOwner, _member.fmd);
            _keyJoin = new KeyJoin<>((FromImpl<?,java.util.Map<K,V>>)this, keyAttr, jt);
            return _keyJoin;
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class\>'. Reason: '((MapAttribute)joinMap.getAttribute())' has raw type, so result of getJavaType is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java

       public MapEntry(Map<?,K,V> joinMap){
           super(((MapAttribute)joinMap.getAttribute()).getJavaType());
           this.map = joinMap;
       }
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Type\>' to 'jakarta.persistence.metamodel.PluralAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public PluralAttribute<? super Z, C, E> getModel() {
            return (PluralAttribute<? super Z, C, E>) _member.getType();
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'jakarta.persistence.metamodel.MapAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java

       public MapKey(Map<Z,K,?> joinMap){
           super(((MapAttribute<Z, K, ?>)joinMap.getAttribute()).getKeyJavaType());
           attr = ((MapAttributeImpl<Z, K, ?>)joinMap.getAttribute());
           this.map = joinMap;
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.Attribute,capture\>' to 'org.apache.openjpa.persistence.meta.Members.MapAttributeImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
       public MapKey(Map<Z,K,?> joinMap){
           super(((MapAttribute<Z, K, ?>)joinMap.getAttribute()).getKeyJavaType());
           attr = ((MapAttributeImpl<Z, K, ?>)joinMap.getAttribute());
           this.map = joinMap;
       }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Members.Member,capture\>' to 'jakarta.persistence.metamodel.MapAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public MapAttribute<? super Z, K,V> getModel() {
            return (MapAttribute<? super Z, K,V>) _member;
        }

```

### UNCHECKED_WARNING
Unchecked call to 'SingularJoin(FromImpl, SingularAttributeImpl, JoinType)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Joins.SingularJoin'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            JoinType jt) {
        if (member instanceof Members.SingularAttributeImpl)
            return new Joins.SingularJoin(from,
                    (Members.SingularAttributeImpl) member, jt);
        else if (member instanceof Members.CollectionAttributeImpl)
```

### UNCHECKED_WARNING
Unchecked call to 'Collection(FromImpl, CollectionAttributeImpl, JoinType)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Joins.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                    (Members.SingularAttributeImpl) member, jt);
        else if (member instanceof Members.CollectionAttributeImpl)
            return new Joins.Collection(from,
                    (Members.CollectionAttributeImpl) member, jt);
        else if (member instanceof Members.ListAttributeImpl)
```

### UNCHECKED_WARNING
Unchecked call to 'List(FromImpl, ListAttributeImpl, JoinType)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Joins.List'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                    (Members.CollectionAttributeImpl) member, jt);
        else if (member instanceof Members.ListAttributeImpl)
            return new Joins.List(from, (Members.ListAttributeImpl) member, jt);
        else if (member instanceof Members.SetAttributeImpl)
            return new Joins.Set(from, (Members.SetAttributeImpl) member, jt);
```

### UNCHECKED_WARNING
Unchecked call to 'Set(FromImpl, SetAttributeImpl, JoinType)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Joins.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            return new Joins.List(from, (Members.ListAttributeImpl) member, jt);
        else if (member instanceof Members.SetAttributeImpl)
            return new Joins.Set(from, (Members.SetAttributeImpl) member, jt);
        else if (member instanceof Members.MapAttributeImpl)
            return new Joins.Map(from, (Members.MapAttributeImpl) member, jt);
```

### UNCHECKED_WARNING
Unchecked call to 'Map(FromImpl, MapAttributeImpl, JoinType)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Joins.Map'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            return new Joins.Set(from, (Members.SetAttributeImpl) member, jt);
        else if (member instanceof Members.MapAttributeImpl)
            return new Joins.Map(from, (Members.MapAttributeImpl) member, jt);
        return null;
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Members.Member,capture\>' to 'jakarta.persistence.metamodel.ListAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public ListAttribute<? super Z, E> getModel() {
            return (ListAttribute<? super Z, E>)_member;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Members.Member,capture\>' to 'jakarta.persistence.metamodel.SetAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public SetAttribute<? super Z, E> getModel() {
            return (SetAttribute<? super Z, E>)_member;
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.meta.Members.Member,capture\>' to 'jakarta.persistence.metamodel.CollectionAttribute'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
        @Override
        public CollectionAttribute<? super Z, E> getModel() {
            return (CollectionAttribute<? super Z, E>)_member;
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Expression\>' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    private <T> Expression<T> replaceExpressionForBinaryOperator(final Expression<T> expression) {
        if (expression == PredicateImpl.TRUE()) {
            return (Expression<T>) PredicateImpl.TRUE_CONSTANT;
        }
        if (expression == PredicateImpl.FALSE()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Expression\>' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
        }
        if (expression == PredicateImpl.FALSE()) {
            return (Expression<T>) PredicateImpl.FALSE_CONSTANT;
        }
        return expression;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce' to 'jakarta.persistence.criteria.CriteriaBuilder.Coalesce'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <T> Coalesce<T> coalesce() {
        return new Expressions.Coalesce(Object.class);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Coalesce(Class)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <T> Coalesce<T> coalesce() {
        return new Expressions.Coalesce(Object.class);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Predicate' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    public <T> Expression<T> literal(T value) {
        if (Boolean.TRUE.equals(value))
            return (Expression<T>)PredicateImpl.TRUE();
        if (Boolean.FALSE.equals(value))
            return (Expression<T>)PredicateImpl.FALSE();
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Predicate' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
            return (Expression<T>)PredicateImpl.TRUE();
        if (Boolean.FALSE.equals(value))
            return (Expression<T>)PredicateImpl.FALSE();
        return new Expressions.Constant<>(value);
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Nullif' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> nullif(Expression<Y> x, Expression<?> y) {
        return new Expressions.Nullif(x, y);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Nullif(Expression, Expression)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Nullif'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> nullif(Expression<Y> x, Expression<?> y) {
        return new Expressions.Nullif(x, y);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce' to 'jakarta.persistence.criteria.Expression'. Reason: 'new Expressions.Coalesce(x.getJavaType()).value(x)' has raw type, so result of value is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> coalesce(Expression<? extends Y> x, Y y) {
    	return new Expressions.Coalesce(x.getJavaType()).value(x).value(y);
   }

```

### UNCHECKED_WARNING
Unchecked call to 'value(Expression)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> coalesce(Expression<? extends Y> x, Y y) {
    	return new Expressions.Coalesce(x.getJavaType()).value(x).value(y);
   }

```

### UNCHECKED_WARNING
Unchecked call to 'value(T)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> coalesce(Expression<? extends Y> x, Y y) {
    	return new Expressions.Coalesce(x.getJavaType()).value(x).value(y);
   }

```

### UNCHECKED_WARNING
Unchecked call to 'Coalesce(Class)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> coalesce(Expression<? extends Y> x, Y y) {
    	return new Expressions.Coalesce(x.getJavaType()).value(x).value(y);
   }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.DatabaseFunction' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    public <T> Expression<T> function(String name, Class<T> type,
        Expression<?>... args) {
        return new Expressions.DatabaseFunction(name, type, args);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'DatabaseFunction(String, Class, Expression...)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.DatabaseFunction'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    public <T> Expression<T> function(String name, Class<T> type,
        Expression<?>... args) {
        return new Expressions.DatabaseFunction(name, type, args);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Exists(Subquery)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Exists'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public Predicate exists(Subquery<?> subquery) {
        return new Expressions.Exists(subquery);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Between(Expression, Expression, Expression)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Between'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    public <Y extends Comparable<? super Y>> Predicate between(Expression<? extends Y> v, Expression<? extends Y> x,
        Expression<? extends Y> y) {
        return new Expressions.Between(v,x,y);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Nullif' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> nullif(Expression<Y> x, Y y) {
        return new Expressions.Nullif(x, y);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Nullif(Expression, T)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Nullif'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> nullif(Expression<Y> x, Y y) {
        return new Expressions.Nullif(x, y);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce' to 'jakarta.persistence.criteria.Expression'. Reason: 'new Expressions.Coalesce(x.getJavaType()).value(x)' has raw type, so result of value is erased
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> coalesce(Expression<? extends Y> x, Expression<? extends Y> y) {
    	return new Expressions.Coalesce(x.getJavaType()).value(x).value(y);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'value(Expression)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> coalesce(Expression<? extends Y> x, Expression<? extends Y> y) {
    	return new Expressions.Coalesce(x.getJavaType()).value(x).value(y);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'value(Expression)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> coalesce(Expression<? extends Y> x, Expression<? extends Y> y) {
    	return new Expressions.Coalesce(x.getJavaType()).value(x).value(y);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Coalesce(Class)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Coalesce'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y> Expression<Y> coalesce(Expression<? extends Y> x, Expression<? extends Y> y) {
    	return new Expressions.Coalesce(x.getJavaType()).value(x).value(y);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Case' to 'jakarta.persistence.criteria.CriteriaBuilder.Case'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <R> Case<R> selectCase() {
        return new Expressions.Case(Object.class);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Case(Class)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Case'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <R> Case<R> selectCase() {
        return new Expressions.Case(Object.class);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Constant' to 'jakarta.persistence.criteria.Expression\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <V, M extends Map<?, V>> Expression<Collection<V>> values(M map) {
        return new Expressions.Constant<>(map == null ? Collections.EMPTY_LIST : map.values());
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Constant(X)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <V, M extends Map<?, V>> Expression<Collection<V>> values(M map) {
        return new Expressions.Constant<>(map == null ? Collections.EMPTY_LIST : map.values());
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.Collection'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <V, M extends Map<?, V>> Expression<Collection<V>> values(M map) {
        return new Expressions.Constant<>(map == null ? Collections.EMPTY_LIST : map.values());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.ManagedType\>' to 'jakarta.persistence.metamodel.ManagedType'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
            return from.isNull();
        }
        ManagedType<T> type = (ManagedType<T>)_model.managedType(example.getClass());
        return new CompareByExample<>(this, type, from, example,
            style == null ? qbeStyle() : style, excludes);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Constant' to 'jakarta.persistence.criteria.Expression\>'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <K, M extends Map<K, ?>> Expression<Set<K>> keys(M map) {
        return new Expressions.Constant<>(map == null ? Collections.EMPTY_SET : map.keySet());
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Constant(X)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <K, M extends Map<K, ?>> Expression<Set<K>> keys(M map) {
        return new Expressions.Constant<>(map == null ? Collections.EMPTY_SET : map.keySet());
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <K, M extends Map<K, ?>> Expression<Set<K>> keys(M map) {
        return new Expressions.Constant<>(map == null ? Collections.EMPTY_SET : map.keySet());
    }

```

### UNCHECKED_WARNING
Unchecked call to 'Between(Expression, Y, Y)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Between'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <Y extends Comparable<? super Y>> Predicate between(Expression<? extends Y> v, Y x, Y y) {
        return new Expressions.Between(v,x,y);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.SimpleCase' to 'jakarta.persistence.criteria.CriteriaBuilder.SimpleCase'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <C, R> SimpleCase<C, R> selectCase(Expression<? extends C> expression) {
        return new Expressions.SimpleCase(expression);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'SimpleCase(Expression)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.SimpleCase'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaBuilderImpl.java`
#### Snippet
```java
    @Override
    public <C, R> SimpleCase<C, R> selectCase(Expression<? extends C> expression) {
        return new Expressions.SimpleCase(expression);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/EagerResultList.java`
#### Snippet
```java
            rop.open();
            while (rop.next())
                getDelegate().add(rop.getResultObject());
        } catch (RuntimeException re) {
            throw re;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/SimpleResultList.java`
#### Snippet
```java
        List list = new ArrayList();
        for (Object o : this) {
            list.add(o);
        }
        return list;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/RandomAccessResultList.java`
#### Snippet
```java

            // cache the result
            _rows.put(i, ob);

            // check to see if our map is full
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/RandomAccessResultList.java`
#### Snippet
```java
        ArrayList list = new ArrayList();
        for (Object o : this) {
            list.add(o);
        }
        return list;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractNonSequentialResultList.java`
#### Snippet
```java
            if (obj == PAST_END)
                break;
            list.add(obj);
        }
        return list.toArray(a);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractNonSequentialResultList.java`
#### Snippet
```java
            list.add(obj);
        }
        return list.toArray(a);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractNonSequentialResultList.java`
#### Snippet
```java
            if (obj == PAST_END)
                break;
            list.add(obj);
        }
        return list.toArray();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/WindowResultList.java`
#### Snippet
```java
        List list = new ArrayList();
        for (Object o : this) {
            list.add(o);
        }
        return list;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/LazyForwardResultList.java`
#### Snippet
```java
                return false;
            }
            _list.add(_rop.getResultObject());
            return true;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/ListResultList.java`
#### Snippet
```java
    public Object[] toArray(Object[] a) {
        assertOpen();
        return _list.toArray(a);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'containsAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/ListResultList.java`
#### Snippet
```java
    public boolean containsAll(Collection c) {
        assertOpen();
        return _list.containsAll(c);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractSequentialResultList.java`
#### Snippet
```java
        ArrayList list = new ArrayList();
        for (Iterator itr = itr(0); itr.hasNext();)
            list.add(itr.next());
        return list.toArray(a);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractSequentialResultList.java`
#### Snippet
```java
        for (Iterator itr = itr(0); itr.hasNext();)
            list.add(itr.next());
        return list.toArray(a);
    }
}
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/AbstractSequentialResultList.java`
#### Snippet
```java
        ArrayList list = new ArrayList();
        for (Iterator itr = itr(0); itr.hasNext();)
            list.add(itr.next());
        return list.toArray();
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/AbstractProductDerivation.java`
#### Snippet
```java
    @Override
    public Set<String> getSupportedQueryHints() {
        return Collections.EMPTY_SET;
    }
}
```

### UNCHECKED_WARNING
Unchecked call to 'compare(T, T)' as a member of raw type 'java.util.Comparator'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/MergedResultObjectProvider.java`
#### Snippet
```java
            if (_status[i] != VALUE)
                continue;
            if (least == -1 || _comp.compare(_orderValues[i], orderVal) < 0) {
                least = i;
                orderVal = _orderValues[i];
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/MapConfigurationProvider.java`
#### Snippet
```java
        if (_props == null)
            _props = new HashMap();
        return _props.put(key, value);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/MapConfigurationProvider.java`
#### Snippet
```java
        if (_props == null)
            _props = new HashMap();
        _props.putAll(props);
    }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/MapConfigurationProvider.java`
#### Snippet
```java

    @Override
    public Map getProperties() {
        return (_props == null) ? Collections.EMPTY_MAP : _props;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'toValue(ExpressionFactory, CriteriaQueryImpl)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.ListArgument'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        public Value toValue(ExpressionFactory factory, CriteriaQueryImpl<?> q) {
            return factory.newFunction(functionName, getJavaType(),
                new Expressions.ListArgument(resultType, args).toValue(factory, q));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'ListArgument(Class, ExpressionImpl...)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.ListArgument'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        public Value toValue(ExpressionFactory factory, CriteriaQueryImpl<?> q) {
            return factory.newFunction(functionName, getJavaType(),
                new Expressions.ListArgument(resultType, args).toValue(factory, q));
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.persistence.criteria.ExpressionImpl\>' to 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                }
            }
            Character t = (Character)((Constant<Character>)e2).arg;
            Constant<String> e2 = new Constant<>(String.class, t.toString());
            return factory.trim(
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Constant' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> GreaterThan(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'Constant(X)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> GreaterThan(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Nullif(Expression<T> x, Expression<?> y) {
            super((Class<T>)x.getJavaType());
            val1 = x;
            val2 = y;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Constant' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> LessThan(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'Constant(X)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> LessThan(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public Constant(X x) {
            this(x == null ? null : (Class<X>)x.getClass(), x);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Constant' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> NotEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'Constant(X)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> NotEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public SubqueryExpression(Subquery<X> x) {
            super((Class<X>)x.getJavaType());
            e = (SubqueryImpl<X>)x;
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Constant' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> LessThanEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'Constant(X)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> LessThanEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
    public static class Diff<N extends Number> extends BinarayFunctionalExpression<N> {
        public Diff(Expression<? extends Number> x, Expression<? extends Number> y) {
            super((Class<N>)x.getJavaType(), x, y);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'toValue(ExpressionFactory, CriteriaQueryImpl)' as a member of raw type 'org.apache.openjpa.persistence.criteria.ParameterExpressionImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
            Class<?> literalClass = getJavaType();
            if (arg instanceof ParameterExpressionImpl) {
                return ((ParameterExpressionImpl)arg).toValue(factory, q);
            }
            int literalType = Literal.TYPE_UNKNOWN;
```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.metamodel.EntityType\>' to 'org.apache.openjpa.persistence.meta.Types.Entity'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                literalType = Literal.TYPE_CLASS;
                Literal lit = factory.newTypeLiteral(value, Literal.TYPE_CLASS);
                ClassMetaData can = ((Types.Entity<X>)q.getRoot().getModel()).meta;
                Class<?> candidate = can.getDescribedType();
                if (candidate.isAssignableFrom((Class)value)) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Constant' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> GreaterThanEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'Constant(X)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> GreaterThanEqual(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
    public static class Product<N extends Number> extends BinarayFunctionalExpression<N> {
        public Product(Expression<? extends Number> x, Expression<? extends Number> y) {
            super((Class<N>)x.getJavaType(), x, y);
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
    public static class Quotient<N extends Number> extends BinarayFunctionalExpression<N> {
        public Quotient(Expression<? extends Number> x, Expression<? extends Number> y) {
            super((Class<N>)x.getJavaType(), x, y);
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'jakarta.persistence.criteria.Expression\>' to 'org.apache.openjpa.persistence.criteria.ExpressionImpl'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
        public In(Expression<?> e) {
            super();
            this.e = (ExpressionImpl<T>)e;
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
    public static class Type<X extends Class> extends UnaryFunctionalExpression<X> {
        public Type(PathImpl<?, ?> path) {
            super((Class)Class.class, path);
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
    public static class Sum<N extends Number> extends BinarayFunctionalExpression<N> {
        public Sum(Expression<? extends Number> x, Expression<? extends Number> y) {
            super((Class<N>)x.getJavaType(), x, y);
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public UnaryFunctionalExpression(Expression<X> e) {
            this((Class<X>)e.getJavaType(), e);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.persistence.criteria.Expressions.Constant' to 'jakarta.persistence.criteria.Expression'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> Equal(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'Constant(X)' as a member of raw type 'org.apache.openjpa.persistence.criteria.Expressions.Constant'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java

        public <X> Equal(Expression<X> x, Object y) {
            this(x, new Constant(y));
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Value.java`
#### Snippet
```java
     */
    public Set<String> getEquivalentKeys() {
        return otherNames == null ? Collections.EMPTY_SET
            : Collections.unmodifiableSet(otherNames);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.sql.Statement' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingStatement.java`
#### Snippet
```java
    public <T> T unwrap(Class<T> iface) throws SQLException {
        if (isWrapperFor(iface))
            return (T) getDelegate();
        else
            return null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.sql.Connection' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingConnection.java`
#### Snippet
```java
    public <T> T unwrap(Class<T> iface) throws SQLException {
        if (isWrapperFor(iface)) {
            return (T) getDelegate();
        } else {
            return getDelegate().unwrap(iface);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
        Class<?> cls = null;
        Object key = loader == null ? NULL_LOADER : loader;
        Map<String,Class<?>> loaderCache = (Map<String,Class<?>>) _loaders.get(key);
        if (loaderCache == null) { // We don't have a cache for this loader.
            //OPENJPA-2636: Changed to HARD/WEAK to avoid Classloader leak:
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map\>'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
        if (loaderCache == null) { // We don't have a cache for this loader.
            //OPENJPA-2636: Changed to HARD/WEAK to avoid Classloader leak:
            loaderCache = new ConcurrentReferenceHashMap(ReferenceStrength.HARD,
                ReferenceStrength.WEAK);
            _loaders.put(key, loaderCache);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.lang.Object', required 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingDataSource.java`
#### Snippet
```java

    @Override
    public Object unwrap(Class iface) {
        if (isWrapperFor(iface))
            return getDelegate();
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingDataSource.java`
#### Snippet
```java
    @Override
    public boolean isWrapperFor(Class iface) {
        return iface.isAssignableFrom(getDelegate().getClass());
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ProductDerivations.java`
#### Snippet
```java
            String fqLoc = base + "#" + newMember;
            if (!collection.contains(fqLoc))
                collection.add(fqLoc);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.sql.DatabaseMetaData' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingDatabaseMetaData.java`
#### Snippet
```java
    public <T> T unwrap(Class<T> iface) throws SQLException {
        if (isWrapperFor(iface))
            return (T) getDelegate();
        else
            return null;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataSerializer.java`
#### Snippet
```java

                if (output != null)
                    output.put(file, ((StringWriter) writer).toString());
            } catch (SAXException | TransformerConfigurationException se) {
                throw new IOException(se.toString());
```

### UNCHECKED_WARNING
Unchecked cast: 'java.sql.PreparedStatement' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingPreparedStatement.java`
#### Snippet
```java
    public <T> T unwrap(Class<T> iface) throws SQLException {
        if (isWrapperFor(iface))
            return (T) getDelegate();
        else
            return null;
```

### UNCHECKED_WARNING
Unchecked call to 'LinkedHashMap(Map)' as a member of raw type 'java.util.LinkedHashMap'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            map = (Map) ((Properties) map).clone();
        else
            map = new LinkedHashMap(map);

        Map remaining = new HashMap(map);
```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            map = new LinkedHashMap(map);

        Map remaining = new HashMap(map);
        boolean ser = true;
        Object o;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
    public List<String> getPropertyKeys(String propertyName) {
        Value value = getValue(propertyName);
        return value == null ? Collections.EMPTY_LIST : value.getPropertyKeys();
    }

```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
                (new Object[]{ Boolean.FALSE });
            clone.fromProperties(toProperties(true));
            clone._props = (_props == null) ? null : new HashMap(_props);
            clone._globals = _globals;
            return clone;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
        Object external = val.isHidden() ? Value.INVISIBLE :
            val instanceof ObjectValue ? val.getString() : val.get();
        map.put(key, external);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            synchronized(systemProperties) {
                // Prevent concurrent modification of systemProperties until HashMap ctor is completed.
                sysPropHM = new HashMap(systemProperties);
            }
            fromProperties(sysPropHM);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
     */
    @Override
    public Map toProperties(boolean storeDefaults) {
        // clone properties before making any modifications; we need to keep
        // the internal properties instance consistent to maintain equals and
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            clone = new TreeMap<>();
        else if (_props instanceof Properties)
            clone = (Map) ((Properties) _props).clone();
        else
            clone = new TreeMap<String, String>(_props);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            clone = (Map) ((Properties) _props).clone();
        else
            clone = new TreeMap<String, String>(_props);

        // if no existing properties or the properties should contain entries
```

### UNCHECKED_WARNING
Unchecked call to 'TreeMap(Map)' as a member of raw type 'java.util.TreeMap'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            }
            if (_props == null)
                _props = new TreeMap(clone);
        }
        return clone;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.sql.CallableStatement' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingCallableStatement.java`
#### Snippet
```java
    public <T> T unwrap(Class<T> iface) throws SQLException {
        if (isWrapperFor(iface))
            return (T) getDelegate();
        else
            return null;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java
    @Override
    public boolean addAll(Collection coll) {
        return _set.addAll(coll);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java
    @Override
    public boolean add(Object obj) {
        return _set.add(obj);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.ReferenceMap' to 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java
            _set = new HashSet();
        else {
            _set = MapBackedSet.mapBackedSet(new ReferenceMap(refType, AbstractReferenceMap.ReferenceStrength.HARD), DUMMY_VAL);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'removeAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java
    @Override
    public boolean removeAll(Collection coll) {
        return _set.removeAll(coll);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java
    @Override
    public Object[] toArray(Object[] arr) {
        return _set.toArray(arr);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'containsAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java
    @Override
    public boolean containsAll(Collection coll) {
        return _set.containsAll(coll);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'retainAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashSet.java`
#### Snippet
```java
    @Override
    public boolean retainAll(Collection coll) {
        return _set.retainAll(coll);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.sql.ResultSet' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/DelegatingResultSet.java`
#### Snippet
```java
    public <T> T unwrap(Class<T> iface) throws SQLException {
        if (isWrapperFor(iface))
            return (T) getDelegate();
        else
            return null;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringDistance.java`
#### Snippet
```java
        thresholdPercentage = Math.max(thresholdPercentage, 0.0f);

        return getClosestLevenshteinDistance(str, candidates,
            (int) (str.length() * thresholdPercentage));
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringDistance.java`
#### Snippet
```java
    public static String getClosestLevenshteinDistance(String str,
        Collection candidates) {
        return getClosestLevenshteinDistance(str, candidates,
            Integer.MAX_VALUE);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'removeLRU(LinkEntry)' as a member of raw type 'org.apache.openjpa.lib.util.collections.LRUMap'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/LRUMap.java`
#### Snippet
```java
    protected boolean removeLRU(LinkEntry entry) {
        overflowRemoved(entry.getKey(), entry.getValue());
        return super.removeLRU(entry);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'LRUMap(Map)' as a member of raw type 'org.apache.openjpa.lib.util.collections.LRUMap'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/LRUMap.java`
#### Snippet
```java

    public LRUMap(Map map) {
        super(map);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Byte' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        // handle primitives
        if (type == byte.class) {
            return (T) (val == null ? BYTE_ZERO : Byte.valueOf(val));
        }
        if (type == char.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Character' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == char.class) {
            return (T) (val == null ? CHAR_ZERO : parseCharString(val));
        }
        if (type == double.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Double' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == double.class) {
            return (T) (val == null ? DOUBLE_ZERO : Double.valueOf(val));
        }
        if (type == float.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Float' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == float.class) {
            return (T) (val == null ? FLOAT_ZERO : Float.valueOf(val));
        }
        if (type == int.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Integer' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == int.class) {
            return (T) (val == null ? INTEGER_ZERO : Integer.valueOf(val));
        }
        if (type == long.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Long' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == long.class) {
            return (T) (val == null ? LONG_ZERO : Long.valueOf(val));
        }
        if (type == short.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Short' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == short.class) {
            return (T) (val == null ? SHORT_ZERO : Short.valueOf(val));
        }
        if (type == boolean.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Boolean' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == boolean.class) {
            return (T) (val == null ? Boolean.FALSE : Boolean.valueOf(val));
        }
        if (type == void.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Byte' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        // handle wrapper types
        if (type == Byte.class) {
            return (T) (val == null ? null : Byte.valueOf(val));
        }
        if (type == Character.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Character' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == Character.class) {
            return (T) (val == null ? null : parseCharString(val));
        }
        if (type == Double.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Double' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == Double.class) {
            return (T) (val == null ? null : Double.valueOf(val));
        }
        if (type == Float.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Float' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == Float.class) {
            return (T) (val == null ? null : Float.valueOf(val));
        }
        if (type == Integer.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Integer' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == Integer.class) {
            return (T) (val == null ? null : Integer.valueOf(val));
        }
        if (type == Long.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Long' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == Long.class) {
            return (T) (val == null ? null : Long.valueOf(val));
        }
        if (type == Short.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Short' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == Short.class) {
            return (T) (val == null ? null : Short.valueOf(val));
        }
        if (type == Boolean.class) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Boolean' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StringUtil.java`
#### Snippet
```java
        }
        if (type == Boolean.class) {
            return (T) (val == null ? null : Boolean.valueOf(val));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataParser.java`
#### Snippet
```java
        if (_log != null && _log.isTraceEnabled())
            _log.trace(_loc.get("end-parse", getSourceName()));
        _results = new ArrayList(_curResults);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/XMLMetaDataParser.java`
#### Snippet
```java
        if (_log != null && _log.isTraceEnabled())
            _log.trace(_loc.get("add-result", result));
        _curResults.add(result);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'ReferenceEntry(AbstractReferenceMap, HashEntry, int, K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.AbstractReferenceMap.ReferenceEntry'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashMap.java`
#### Snippet
```java
        public AccessibleEntry(AbstractReferenceMap map, HashEntry next,
                               int hashCode, Object key, Object value) {
            super(map, next, hashCode, key, value);
            parent = (ReferenceHashMap)map;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'addMapping(int, int, K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.AbstractHashedMap'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ReferenceHashMap.java`
#### Snippet
```java
        if (_maxSize != Integer.MAX_VALUE)
            removeOverflow(_maxSize - 1);
        super.addMapping(hashIndex, hashCode, key, value);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ParameterTemplate.java`
#### Snippet
```java
     */
    public Object setParameter(String name, Object val) {
        return _params.put(name, val);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ParameterTemplate.java`
#### Snippet
```java
     */
    public void setParameters(Map params) {
        _params.putAll(params);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
        for (String name : names) {
            try {
                classes.add(Class.forName(name, false, loader));
            }
            catch (UnsupportedClassVersionError ecve) {
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
            }
        }
        return (Class[]) classes.toArray(new Class[classes.size()]);
    }
}
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
                addResources((URL) resources.nextElement(), resourceList);

            return (String[]) resourceList.toArray(new String[resourceList
                .size()]);
        } catch (PrivilegedActionException | IOException pae) {
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
        // the specified service class.
        for (Class aClass : classes)
            if (!serviceClass.isAssignableFrom(aClass))
                invalid.add(aClass);
        if (invalid.size() != 0) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
        for (Class aClass : classes)
            if (!serviceClass.isAssignableFrom(aClass))
                invalid.add(aClass);
        if (invalid.size() != 0) {
            List list = new ArrayList(Arrays.asList(classes));
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
                invalid.add(aClass);
        if (invalid.size() != 0) {
            List list = new ArrayList(Arrays.asList(classes));
            list.removeAll(invalid);
            return (Class[]) list.toArray(new Class[list.size()]);
```

### UNCHECKED_WARNING
Unchecked call to 'removeAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
        if (invalid.size() != 0) {
            List list = new ArrayList(Arrays.asList(classes));
            list.removeAll(invalid);
            return (Class[]) list.toArray(new Class[list.size()]);
        } else {
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
            List list = new ArrayList(Arrays.asList(classes));
            list.removeAll(invalid);
            return (Class[]) list.toArray(new Class[list.size()]);
        } else {
            return classes;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
                        next = next.trim();
                        if (next.length() > 0 && !next.startsWith("#"))
                            set.add(next);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.MapIterator' to 'org.apache.openjpa.lib.util.collections.MapIterator'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/EmptyMapIterator.java`
#### Snippet
```java
     */
    public static <K, V> MapIterator<K, V> emptyMapIterator() {
        return INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.MapBackedSet' to 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashSet.java`
#### Snippet
```java
            _set = MapBackedSet.mapBackedSet(new ConcurrentHashMap<>(), DUMMY_VAL);
        else {
            _set = MapBackedSet.mapBackedSet(new ConcurrentReferenceHashMap
                (refType, AbstractReferenceMap.ReferenceStrength.HARD), DUMMY_VAL);
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashSet.java`
#### Snippet
```java
            _set = MapBackedSet.mapBackedSet(new ConcurrentHashMap<>(), DUMMY_VAL);
        else {
            _set = MapBackedSet.mapBackedSet(new ConcurrentReferenceHashMap
                (refType, AbstractReferenceMap.ReferenceStrength.HARD), DUMMY_VAL);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/AbstractConcurrentEventManager.java`
#### Snippet
```java
                if (exceptions == null)
                    exceptions = new LinkedList();
                exceptions.add(e);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/AbstractConcurrentEventManager.java`
#### Snippet
```java
        if (exceptions == null)
            return EMPTY_EXCEPTIONS;
        return (Exception[]) exceptions.toArray
            (new Exception[exceptions.size()]);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/AbstractConcurrentEventManager.java`
#### Snippet
```java
    public void addListener(Object listener) {
        if (listener != null)
            _listeners.add(listener);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.OrderedIterator' to 'org.apache.openjpa.lib.util.collections.OrderedIterator'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/EmptyOrderedIterator.java`
#### Snippet
```java
     */
    public static <E> OrderedIterator<E> emptyOrderedIterator() {
        return INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.security.PrivilegedAction' to 'java.security.PrivilegedAction'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
     */
    public static PrivilegedAction<MultiClassLoader> newMultiClassLoaderAction() {
        return new PrivilegedAction() {
            @Override
            public MultiClassLoader run() {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/J2DoPrivHelper.java`
#### Snippet
```java
                } else {
                    try {
                        return (T)clazz.getMethod("newInstance",
                            new Class[]{}).invoke(null, new Object[]{});
                    } catch (Throwable t) {
                        throw new InstantiationException(t.toString());
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java

        for (PropertyLine l;
            (l = reader.readPropertyLine()) != null && source.add(l);) {
            String line = l.line.toString();

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
            }

            if (!loadedKeys.add(key.toString()) && !allowDuplicates)
                throw new DuplicateKeyException(key.toString(),
                    getProperty(key.toString()), element.toString());
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
        // first write all the existing props as they were initially read
        if (source != null)
            lines.addAll(source);

        // next write out new keys, then the rest of the keys
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.AbstractCollection'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
        // next write out new keys, then the rest of the keys
        LinkedHashSet keys = new LinkedHashSet();
        keys.addAll(newKeys);
        keys.addAll(keySet());

```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.AbstractCollection'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
        LinkedHashSet keys = new LinkedHashSet();
        keys.addAll(newKeys);
        keys.addAll(keySet());

        lines.addAll(keys);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
        keys.addAll(keySet());

        lines.addAll(keys);

        keys.remove(null);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.HashSet'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
        if (!isLoading && isNotDeserializing && !val.equals(o)) {
            if (o != null)
                modifiedKeys.add(key);
            else if (!newKeys.contains(key))
                newKeys.add(key);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.HashSet'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
                modifiedKeys.add(key);
            else if (!newKeys.contains(key))
                newKeys.add(key);
        }
        return o;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.AbstractCollection'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
        if (newKeys != null) {
            c.newKeys = new LinkedHashSet();
            c.newKeys.addAll(newKeys);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.OrderedMapIterator' to 'org.apache.openjpa.lib.util.collections.OrderedMapIterator'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/EmptyOrderedMapIterator.java`
#### Snippet
```java
     */
    public static <K, V> OrderedMapIterator<K, V> emptyOrderedMapIterator() {
        return INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.ComparableComparator' to 'org.apache.openjpa.lib.util.collections.ComparableComparator'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/ComparableComparator.java`
#### Snippet
```java
     */
    public static <E extends Comparable<? super E>> ComparableComparator<E> comparableComparator() {
        return INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.ResettableIterator' to 'org.apache.openjpa.lib.util.collections.ResettableIterator'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/EmptyIterator.java`
#### Snippet
```java
     */
    public static <E> ResettableIterator<E> resettableEmptyIterator() {
        return RESETTABLE_INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Iterator' to 'java.util.Iterator'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/EmptyIterator.java`
#### Snippet
```java
     */
    public static <E> Iterator<E> emptyIterator() {
        return INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map.Entry\[\]' to 'java.util.Map.Entry\[\]'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/graph/DepthFirstAnalysis.java`
#### Snippet
```java
    public List<Object> getSortedNodes() {
        Map.Entry<Object,NodeInfo>[] entries =
        	_nodeInfo.entrySet().toArray(new Map.Entry[_nodeInfo.size()]);
        Arrays.sort(entries, new NodeInfoComparator(_comp));
        return new NodeList(entries);
```

### UNCHECKED_WARNING
Unchecked call to 'setValue(V)' as a member of raw type 'java.util.Map.Entry'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
                    Object old = e.getValue();
                    if (valueType == AbstractReferenceMap.ReferenceStrength.HARD)
                        e.setValue(value);
                    else {
                        e = newEntry(hash, e.getKey(), value, e.getNext());
```

### UNCHECKED_WARNING
Unchecked call to 'WeakReference(T, ReferenceQueue)' as a member of raw type 'java.lang.ref.WeakReference'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
        WeakEntry(int hash, Object key, Object value, boolean keyRef,
            Entry next, ReferenceQueue queue) {
            super((keyRef) ? key : value, queue);
            this.hash = hash;
            this.hard = (keyRef) ? value : key;
```

### UNCHECKED_WARNING
Unchecked call to 'SoftReference(T, ReferenceQueue)' as a member of raw type 'java.lang.ref.SoftReference'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentReferenceHashMap.java`
#### Snippet
```java
        SoftEntry(int hash, Object key, Object value, boolean keyRef,
            Entry next, ReferenceQueue queue) {
            super((keyRef) ? key : value, queue);
            this.hash = hash;
            this.hard = (keyRef) ? value : key;
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/SunOneManagedRuntime.java`
#### Snippet
```java
        throws ClassNotFoundException, NoSuchMethodException {
        Class swtch = Class.forName("com.sun.enterprise.Switch");
        _switchMeth = swtch.getMethod("getSwitch", (Class[]) null);
        _txManagerMeth = swtch.getMethod("getTransactionManager",
            (Class[]) null);
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/SunOneManagedRuntime.java`
#### Snippet
```java
        Class swtch = Class.forName("com.sun.enterprise.Switch");
        _switchMeth = swtch.getMethod("getSwitch", (Class[]) null);
        _txManagerMeth = swtch.getMethod("getTransactionManager",
            (Class[]) null);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WLSManagedRuntime.java`
#### Snippet
```java
        Class txHelper = Class.forName
            ("weblogic.transaction.TransactionHelper");
        _txHelperMeth = txHelper.getMethod("getTransactionHelper", null);
        _txManagerMeth = txHelper.getMethod("getTransactionManager", null);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WLSManagedRuntime.java`
#### Snippet
```java
            ("weblogic.transaction.TransactionHelper");
        _txHelperMeth = txHelper.getMethod("getTransactionHelper", null);
        _txManagerMeth = txHelper.getMethod("getTransactionManager", null);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java

            Class<WASRegistryManagedRuntime> mrClass =
                (Class<WASRegistryManagedRuntime>) J2DoPrivHelper
                        .getForNameAction(
                                WASRegistryManagedRuntime.class.getName(),
                                true, cl).run();
            mr = J2DoPrivHelper.newInstanceAction(mrClass).run();
        } catch (Throwable t) {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
    protected void addSystemMappingElements(Collection toSerialize) {
        if (isQueryMode())
            toSerialize.addAll(getQueryResultMappings(null));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
    private List<QueryResultMapping> getQueryResultMappings(ClassMetaData cm) {
        if (_results == null || _results.isEmpty())
            return (List<QueryResultMapping>) Collections.EMPTY_LIST;

        List<QueryResultMapping> result = null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
        }
        return (result == null)
            ? (List<QueryResultMapping>) Collections.EMPTY_LIST : result;
    }

```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
    protected void addSystemMappingElements(Collection toSerialize) {
        if (isQueryMode())
            toSerialize.addAll(getQueryResultMappings(null));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
    private List<QueryResultMapping> getQueryResultMappings(ClassMetaData cm) {
        if (_results == null || _results.isEmpty())
            return Collections.EMPTY_LIST;

        List<QueryResultMapping> result = null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
        }
        return (result == null)
            ? (List<QueryResultMapping>) Collections.EMPTY_LIST : result;
    }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.lang.Class', required 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/AutoDetachValue.java`
#### Snippet
```java

    @Override
    public Class getValueType() {
        return String[].class;
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/QueryCompilationCacheValue.java`
#### Snippet
```java
                    org.apache.openjpa.lib.util.concurrent.ConcurrentMap)
            && !(map instanceof java.util.concurrent.ConcurrentMap))
            map = Collections.synchronizedMap(map);
        return map;
	}
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/MetaDataRepositoryValue.java`
#### Snippet
```java
                // together; maybe we can move the caching logic somewhere
                // else?
                conf.getQueryCompilationCacheInstance().putAll((Map) os[1]);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/CacheMarshallerImpl.java`
#### Snippet
```java
            for (Enumeration e = cl.getResources(_inputResourceLocation);
                e.hasMoreElements(); )
                list.add(e.nextElement());

            if (list.size() > 1) {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/NoneMetaDataFactory.java`
#### Snippet
```java

    @Override
    public Set getPersistentTypeNames(boolean classpath,
        ClassLoader envLoader) {
        return null;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/DelegatingMetaDataFactory.java`
#### Snippet
```java
    public boolean store(ClassMetaData[] metas, QueryMetaData[] queries,
        SequenceMetaData[] seqs, int mode, Map output) {
        return _delegate.store(metas, queries, seqs, mode, output);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/XMLClassMetaData.java`
#### Snippet
```java
    @Override
    public void addField(String name, XMLMetaData field) {
        _fieldMap.put(name, field);
    }
}
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/DelegatingMetaDataFactory.java`
#### Snippet
```java

    @Override
    public Set getPersistentTypeNames(boolean classpath,
        ClassLoader envLoader) {
        return _delegate.getPersistentTypeNames(classpath, envLoader);
```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractMetaDataFactory.java`
#### Snippet
```java
    public void setTypes(String types) {
        this.types = (StringUtil.isEmpty(types)) ? null
            : new HashSet(Arrays.asList(StringUtil.split(types, ";", 0)));
    }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractMetaDataFactory.java`
#### Snippet
```java

    @Override
    public Set getPersistentTypeNames(boolean devpath, ClassLoader envLoader) {
        return types;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataTool.java`
#### Snippet
```java
            return;
        if (ACTION_DROP.equals(_action))
            _drop.add(cls);
        else if (ACTION_ADD.equals(_action))
            add(cls);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataTool.java`
#### Snippet
```java
        try {
            if (_drop != null && !_drop.isEmpty()
                && !mdf.drop((Class[]) _drop.toArray(new Class[_drop.size()]),
                MODE_META | MODE_MAPPING | MODE_QUERY, null)) {
                Log log = _conf.getLog(OpenJPAConfiguration.LOG_METADATA);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataTool.java`
#### Snippet
```java
            }
            if (!mdf.store(metas, new QueryMetaData[0],
                new SequenceMetaData[0], MODE_META, output))
                throw new MetaDataException(_loc.get("bad-store"));
            if (_writer != null) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java

    static {
        _map.put("none", NONE);
        _map.put("native", NATIVE);
        _map.put("sequence", SEQUENCE);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java
    static {
        _map.put("none", NONE);
        _map.put("native", NATIVE);
        _map.put("sequence", SEQUENCE);
        _map.put("autoassign", AUTOASSIGN);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java
        _map.put("none", NONE);
        _map.put("native", NATIVE);
        _map.put("sequence", SEQUENCE);
        _map.put("autoassign", AUTOASSIGN);
        _map.put("increment", INCREMENT);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java
        _map.put("native", NATIVE);
        _map.put("sequence", SEQUENCE);
        _map.put("autoassign", AUTOASSIGN);
        _map.put("increment", INCREMENT);
        _map.put("uuid-string", UUID_STRING);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java
        _map.put("sequence", SEQUENCE);
        _map.put("autoassign", AUTOASSIGN);
        _map.put("increment", INCREMENT);
        _map.put("uuid-string", UUID_STRING);
        _map.put("uuid-hex", UUID_HEX);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java
        _map.put("autoassign", AUTOASSIGN);
        _map.put("increment", INCREMENT);
        _map.put("uuid-string", UUID_STRING);
        _map.put("uuid-hex", UUID_HEX);
        _map.put("uuid-type4-string", UUID_TYPE4_STRING);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java
        _map.put("increment", INCREMENT);
        _map.put("uuid-string", UUID_STRING);
        _map.put("uuid-hex", UUID_HEX);
        _map.put("uuid-type4-string", UUID_TYPE4_STRING);
        _map.put("uuid-type4-hex", UUID_TYPE4_HEX);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java
        _map.put("uuid-string", UUID_STRING);
        _map.put("uuid-hex", UUID_HEX);
        _map.put("uuid-type4-string", UUID_TYPE4_STRING);
        _map.put("uuid-type4-hex", UUID_TYPE4_HEX);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.TreeBidiMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/ValueStrategies.java`
#### Snippet
```java
        _map.put("uuid-hex", UUID_HEX);
        _map.put("uuid-type4-string", UUID_TYPE4_STRING);
        _map.put("uuid-type4-hex", UUID_TYPE4_HEX);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.security.PrivilegedExceptionAction' to 'java.security.PrivilegedExceptionAction'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/SequenceMetaData.java`
#### Snippet
```java
            if (Seq.class.isAssignableFrom(cls)) {
                seq = (Seq) AccessController.doPrivileged(
                    J2DoPrivHelper.newInstanceAction(cls));
                Configurations.configureInstance(seq,
                    _repos.getConfiguration(), props.toString());
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/SequenceMetaData.java`
#### Snippet
```java
            if (Seq.class.isAssignableFrom(cls)) {
                seq = (Seq) AccessController.doPrivileged(
                    J2DoPrivHelper.newInstanceAction(cls));
                Configurations.configureInstance(seq,
                    _repos.getConfiguration(), props.toString());
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InMemoryRelatedFieldOrder.java`
#### Snippet
```java
        if (v2 == null)
            return (_asc) ? 1 : -1;
        int cmp = ((Comparable) v1).compareTo(v2);
        return (_asc) ? cmp : -cmp;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InMemoryValueOrder.java`
#### Snippet
```java
        if (!(ImplHelper.isManageable(o1))
            || !(ImplHelper.isManageable(o2))) {
            cmp = ((Comparable) o1).compareTo(o2);
            return (_asc) ? cmp : -cmp;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InMemoryValueOrder.java`
#### Snippet
```java
            if (pk2[i] == null)
                return (_asc) ? 1 : -1;
            cmp = ((Comparable) pk1[i]).compareTo(pk2[i]);
            if (cmp != 0)
                return (_asc) ? cmp : -cmp;
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InheritanceComparator.java`
#### Snippet
```java
        if (i1 == i2) {
            // sort simple interfaces as well as simple order test will fail.
            if (c1.isAssignableFrom(c2))
                return -1;
            if (c2.isAssignableFrom(c1))
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InheritanceComparator.java`
#### Snippet
```java
            if (c1.isAssignableFrom(c2))
                return -1;
            if (c2.isAssignableFrom(c1))
                return 1;
            return c1.getName().compareTo(c2.getName());
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
                entry = (Map.Entry) o;
                if (!_exts.containsKey(entry.getKey()))
                    _exts.put(entry.getKey(), entry.getValue());
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
                if (embedded == null) {
                    embedded = new EmbeddedExtensions(this);
                    _embed.put(entry.getKey(), embedded);
                }
                embedded.copy((Extensions) entry.getValue());
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
        if (_exts == null)
            _exts = new HashMap();
        _exts.put(getHashKey(vendor, key), value);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
            key = o;
            if (vendor.equals(getVendor(key)))
                keys.add(getKey(key));
        }
        return (String[]) keys.toArray(new String[keys.size()]);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
                keys.add(getKey(key));
        }
        return (String[]) keys.toArray(new String[keys.size()]);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
        if (exts == null) {
            exts = new EmbeddedExtensions(this);
            _embed.put(hk, exts);

            // required to recognize embedded extensions without values
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
                _exts = new HashMap();
            if (!_exts.containsKey(hk))
                _exts.put(hk, null);
        }
        return exts;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
        Set vendors = new TreeSet();
        for (Object o : _exts.keySet()) {
            vendors.add(getVendor(o));
        }
        return (String[]) vendors.toArray(new String[vendors.size()]);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java
            vendors.add(getVendor(o));
        }
        return (String[]) vendors.toArray(new String[vendors.size()]);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/QueryMetaData.java`
#### Snippet
```java
            _hintVals = new LinkedList();
        }
        _hintKeys.add(key);
        _hintVals.add(value);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/QueryMetaData.java`
#### Snippet
```java
        }
        _hintKeys.add(key);
        _hintVals.add(value);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/QueryMetaData.java`
#### Snippet
```java
    public String[] getHintKeys() {
        return (_hintKeys == null) ? EMPTY_KEYS
            : (String[]) _hintKeys.toArray(new String[_hintKeys.size()]);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/StoreFacadeTypeRegistry.java`
#### Snippet
```java
    	if (result == null)
    		result = defaultType;
    	if (facadeType == null || !facadeType.isAssignableFrom(result))
    		throw new InternalException();
    	return result;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/StoreFacadeTypeRegistry.java`
#### Snippet
```java
        Object key = (storeType == null) ? (Object) facadeType
            : new Key(facadeType, storeType);
        _impls.put(key, implType);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ImplHelper.java`
#### Snippet
```java

            pc = new ReflectingPersistenceCapable(o, conf);
            _unenhancedInstanceMap.put(o, pc);
            return pc;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ImplHelper.java`
#### Snippet
```java
    public static void registerPersistenceCapable(
        ReflectingPersistenceCapable pc) {
        _unenhancedInstanceMap.put(pc.getManagedInstance(), pc);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ImplHelper.java`
#### Snippet
```java
            assignableTo = new ConcurrentReferenceHashMap(ReferenceStrength.WEAK,
                ReferenceStrength.HARD);
            _assignableTypes.put(from, assignableTo);
        } else { // "to" cache exists...
            isAssignable = (Boolean) assignableTo.get(to);
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ImplHelper.java`
#### Snippet
```java

        if (isAssignable == null) {// we don't have a record of this pair...
            isAssignable = from.isAssignableFrom(to);
            assignableTo.put(to, isAssignable);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ImplHelper.java`
#### Snippet
```java
        if (isAssignable == null) {// we don't have a record of this pair...
            isAssignable = from.isAssignableFrom(to);
            assignableTo.put(to, isAssignable);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ImplHelper.java`
#### Snippet
```java
        if (failed == null)
            failed = new ArrayList();
        failed.add(sm.getId());
        return failed;
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                    newClassArgParser(), names, false, null);
            else // we don't cache a full dev cp scan
                _typeNames = names;

            if (log.isTraceEnabled())
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                log.trace(_loc.get("found-pcs", String.valueOf(names.size()),
                    String.valueOf(System.currentTimeMillis() - start)));
            return (names.isEmpty()) ? null : names;
        } catch (IOException ioe) {
            throw new GeneralException(ioe);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
            if (log.isTraceEnabled())
                log.trace(_loc.get("scan-found-names", newNames, debugContext));
            names.addAll(newNames);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'. Reason: 'peMap' has raw type, so result of get is erased
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                    URL puUrl = peMap == null ? null : (URL) peMap.get(PERSISTENCE_UNIT_ROOT_URL);
                    List<String> mappingFileNames =
                            peMap == null ? null : (List<String>) peMap.get(MAPPING_FILE_NAMES);
                    List<URL> jars = peMap == null ? null : (List<URL>) peMap.get(JAR_FILE_URLS);
                    String puUrlString = puUrl == null ? null : puUrl.toString();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'. Reason: 'peMap' has raw type, so result of get is erased
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                    List<String> mappingFileNames =
                            peMap == null ? null : (List<String>) peMap.get(MAPPING_FILE_NAMES);
                    List<URL> jars = peMap == null ? null : (List<URL>) peMap.get(JAR_FILE_URLS);
                    String puUrlString = puUrl == null ? null : puUrl.toString();
                    if (log.isTraceEnabled())
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
        }
        if (types != null)
            names.addAll(types);

        if (log.isTraceEnabled())
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
            for (Class cl : cls) {
                if (cl == null)
                    clsNames.add(null);
                else
                    clsNames.add(cl.getName());
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                    clsNames.add(null);
                else
                    clsNames.add(cl.getName());
                meta = pr.getMetaData(cl, envLoader, false);
                if (meta != null) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                if (meta != null) {
                    if (getSourceFile(meta) != null)
                        files.add(getSourceFile(meta));
                    if ((mode & MODE_META) != 0)
                        pr.removeMetaData(meta);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
        for (QueryMetaData query : queries) {
            if (!isMappingOnlyFactory() && query.getSourceFile() != null)
                files.add(query.getSourceFile());
            def = query.getDefiningType();
            rem = (query.getSourceMode() & mode) != 0
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                pr.removeQueryMetaData(query);
            if (qqs != null && query.getSourceMode() == MODE_QUERY && !rem)
                qqs.add(query);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                if (files == null)
                    files = new HashSet();
                files.add(getSourceFile(meta));
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                if (files == null)
                    files = new HashSet();
                files.add(query.getSourceFile());
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                    if (files == null)
                        files = new HashSet();
                    files.add(getSourceFile(seq));
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
                if (files == null)
                    files = new HashSet();
                files.add(query.getSourceFile());
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
                && AccessController.doPrivileged(J2DoPrivHelper
                    .isAnnotationPresentAction(fm.getDeclaredType(),
                            (Class<? extends Annotation>) xmlRootElementClass))) {
                DBDictionary dict = ((MappingRepository) getRepository())
                    .getDBDictionary();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CollectionChangeTrackerImpl.java`
#### Snippet
```java
                if (rem == null)
                    rem = newSet();
                rem.add(elem);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CollectionChangeTrackerImpl.java`
#### Snippet
```java
                        add = newSet();
                }
                add.add(elem);
            }
        } else if (_order)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CollectionChangeTrackerImpl.java`
#### Snippet
```java
            if (change == null)
                change = newSet();
            change.add(elem);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/OpenJPAId.java`
#### Snippet
```java

        OpenJPAId id = (OpenJPAId) o;
        return idEquals(id) && (id.type.isAssignableFrom(type)
            || (subs && type.isAssignableFrom(id.type)));
    }
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/OpenJPAId.java`
#### Snippet
```java
        OpenJPAId id = (OpenJPAId) o;
        return idEquals(id) && (id.type.isAssignableFrom(type)
            || (subs && type.isAssignableFrom(id.type)));
    }

```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/OpenJPAId.java`
#### Snippet
```java
        if (other == null)
            return 1;
        return ((Comparable) getIdObject()).compareTo(((OpenJPAId) other).getIdObject ());
	}
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ClassLoaderProxyService.java`
#### Snippet
```java
    public <T> Class<T> defineAndLoad(final String name, final byte[] bytecode, final Class<T> proxiedClass)
    {
        return (Class<T>) loader.getOrRegister(
                name, bytecode, proxiedClass.getPackage(), proxiedClass.getProtectionDomain());
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.meta.InheritanceComparator' to 'java.util.Comparator\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
                InheritanceComparator comp = new InheritanceComparator();
                comp.setBase(key);
                coll = new TreeSet<Class<?>>(comp);
            } else
                coll = new LinkedList<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.IdentityHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractChangeTracker.java`
#### Snippet
```java
            if (cur.isEmpty())
                return null;
            Set replace = MapBackedSet.mapBackedSet(new IdentityHashMap());
            replace.addAll(cur);
            return replace;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractChangeTracker.java`
#### Snippet
```java
                return null;
            Set replace = MapBackedSet.mapBackedSet(new IdentityHashMap());
            replace.addAll(cur);
            return replace;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractChangeTracker.java`
#### Snippet
```java
            if (cur.isEmpty())
                return null;
            return new HashSet(cur);
		}
		return cur;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.IdentityHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractChangeTracker.java`
#### Snippet
```java
    protected Set newSet() {
        if (_identity == Boolean.TRUE)
            return MapBackedSet.mapBackedSet(new IdentityHashMap());
        return new HashSet();
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/DelayedCollectionChangeTrackerImpl.java`
#### Snippet
```java
                    add = newSet();
            }
            add.add(elem);
        } else {
            if (change == null)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/DelayedCollectionChangeTrackerImpl.java`
#### Snippet
```java
            if (change == null)
                change = newSet();
            change.add(elem);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/DelayedCollectionChangeTrackerImpl.java`
#### Snippet
```java
            if (rem == null)
                rem = newSet();
            rem.add(elem);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
        IteratorChain chain = new IteratorChain<>();
        if (_map != null)
            chain.addIterator(new ArrayList(_map.entrySet()).iterator());
        chain.addIterator(new FilterIterator(itr(), this));
        return new Itr(mode, chain);
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
        IteratorChain chain = new IteratorChain<>();
        if (_map != null)
            chain.addIterator(new ArrayList(_map.entrySet()).iterator());
        chain.addIterator(new FilterIterator(itr(), this));
        return new Itr(mode, chain);
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
        if (_map != null)
            chain.addIterator(new ArrayList(_map.entrySet()).iterator());
        chain.addIterator(new FilterIterator(itr(), this));
        return new Itr(mode, chain);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'FilterIterator(Iterator, Predicate)' as a member of raw type 'org.apache.openjpa.lib.util.collections.FilterIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
        if (_map != null)
            chain.addIterator(new ArrayList(_map.entrySet()).iterator());
        chain.addIterator(new FilterIterator(itr(), this));
        return new Itr(mode, chain);
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.util.AbstractLRSProxyMap.Itr' to 'java.util.Iterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            @Override
            public Iterator<K> iterator() {
                return AbstractLRSProxyMap.this.iterator(MODE_KEY);
            }
        };
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            Map.Entry<K,V> entry;
            while (itr.hasNext()) {
                entry = (Map.Entry<K,V>) itr.next();
                Proxies.removed(this, entry.getKey(), true);
                Proxies.removed(this, entry.getValue(), false);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.util.AbstractLRSProxyMap.Itr' to 'java.util.Iterator\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            @Override
            public Iterator<Map.Entry<K, V>> iterator() {
                return AbstractLRSProxyMap.this.iterator(MODE_ENTRY);
            }
        };
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            Map.Entry<K,V> entry;
            while (itr.hasNext()) {
                entry = (Map.Entry<K,V>) itr.next();
                map.put(entry.getKey(), entry.getValue());
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.util.AbstractLRSProxyMap.Itr' to 'java.util.Iterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            @Override
            public Iterator<V> iterator() {
                return AbstractLRSProxyMap.this.iterator(MODE_VALUE);
            }
        };
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/MapChangeTrackerImpl.java`
#### Snippet
```java
            if (change == null)
                change = newSet();
            change.add(obj);
        } else {
            // after a point it becomes inefficient to track changes
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/MapChangeTrackerImpl.java`
#### Snippet
```java
                if (add == null)
                    add = newSet();
                add.add(obj);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/MapChangeTrackerImpl.java`
#### Snippet
```java
                if (rem == null)
                    rem = newSet();
                rem.add(obj);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/MapChangeTrackerImpl.java`
#### Snippet
```java
            if (change == null)
                change = newSet();
            change.add(key);
        }
    }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAConfigurationImpl.java`
#### Snippet
```java

    @Override
    public Map getCacheMarshallerInstances() {
        return cacheMarshallerPlugins.getInstancesAsMap();
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.security.PrivilegedAction' to 'java.security.PrivilegedAction'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/Serialization.java`
#### Snippet
```java
            super(delegate);
            _ctx = ctx;
            AccessController.doPrivileged(new PrivilegedAction() {
                @Override
                public Object run() {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.security.PrivilegedAction' to 'java.security.PrivilegedAction'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/Serialization.java`
#### Snippet
```java
            super(delegate);
            _ctx = ctx;
            AccessController.doPrivileged(new PrivilegedAction() {
                @Override
                public Object run() {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java
     */
    protected Object put(Map map, Object key, Object value) {
        return map.put(key, value);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java
        public EntryIterator(int type) {
            _type = type;
            _itr.addIterator(new FilterIterator(getView(pinnedMap), this));
            _itr.addIterator(getView(cacheMap));
            _itr.addIterator(getView(softMap));
```

### UNCHECKED_WARNING
Unchecked call to 'FilterIterator(Iterator, Predicate)' as a member of raw type 'org.apache.openjpa.lib.util.collections.FilterIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java
        public EntryIterator(int type) {
            _type = type;
            _itr.addIterator(new FilterIterator(getView(pinnedMap), this));
            _itr.addIterator(getView(cacheMap));
            _itr.addIterator(getView(softMap));
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java
            _type = type;
            _itr.addIterator(new FilterIterator(getView(pinnedMap), this));
            _itr.addIterator(getView(cacheMap));
            _itr.addIterator(getView(softMap));
        }
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/CacheMap.java`
#### Snippet
```java
            _itr.addIterator(new FilterIterator(getView(pinnedMap), this));
            _itr.addIterator(getView(cacheMap));
            _itr.addIterator(getView(softMap));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java

        IteratorChain chain = new IteratorChain();
        chain.addIterator(new FilterIterator(itr(), this));

        // note have to copy _ct.getAdded to prevent concurrent mod errors
```

### UNCHECKED_WARNING
Unchecked call to 'FilterIterator(Iterator, Predicate)' as a member of raw type 'org.apache.openjpa.lib.util.collections.FilterIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java

        IteratorChain chain = new IteratorChain();
        chain.addIterator(new FilterIterator(itr(), this));

        // note have to copy _ct.getAdded to prevent concurrent mod errors
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java

        // note have to copy _ct.getAdded to prevent concurrent mod errors
        chain.addIterator(new ArrayList(_ct.getAdded()).iterator());
        return new Itr(chain);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java

        // note have to copy _ct.getAdded to prevent concurrent mod errors
        chain.addIterator(new ArrayList(_ct.getAdded()).iterator());
        return new Itr(chain);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java
            List list = new ArrayList();
            while (itr.hasNext())
                list.add(itr.next());
            return list;
        } finally {
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyCollection.java`
#### Snippet
```java
    @Override
    public Object[] toArray(Object[] a) {
        return asList().toArray(a);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'removeIf(Predicate)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
    public static boolean retainAll(ProxyCollection coll, Collection<?> vals) {
        int size = coll.size();
        coll.removeIf(o -> !vals.contains(o));
        return coll.size() < size;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(int, E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
        List list = (List) coll;
        for (Iterator itr = values.iterator(); itr.hasNext(); index++)
            list.add(index, itr.next());
        return values.size() > 0;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
        boolean added = false;
        for (Object value : values) {
            added |= coll.add(value);
        }
        return added;
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
            if (ct != null && ct.isTracking() ) {
                if (!ct.getAdded().isEmpty()) {
                    added = new ArrayList(ct.getAdded());
                }
                if (!ct.getRemoved().isEmpty()) {
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
                }
                if (!ct.getRemoved().isEmpty()) {
                    removed = new ArrayList(ct.getRemoved());
                }
                tracking = true;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
            if (added != null && added.size() > 0) {
                dProxy.setDirectAccess(false);
                proxy.addAll(added);
                added.clear();
            }
```

### UNCHECKED_WARNING
Unchecked call to 'removeAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
            if (removed != null && removed.size() > 0) {
                dProxy.setDirectAccess(false);
                proxy.removeAll(removed);
                removed.clear();
            }
```

### UNCHECKED_WARNING
Unchecked call to 'set(E)' as a member of raw type 'java.util.ListIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
                assertAllowedType(o, coll.getElementType());
                dirty(coll, false);
                itr.set(o);
                if (coll.getChangeTracker() != null)
                    coll.getChangeTracker().stopTracking();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.ListIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyCollections.java`
#### Snippet
```java
                assertAllowedType(o, coll.getElementType());
                dirty(coll, false);
                itr.add(o);
                if (coll.getChangeTracker() != null) {
                    if (hasNext())
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
        }
        if (_fgSet == null && field._fgSet != null)
            _fgSet = new HashSet(field._fgSet);
        if (_lfg == null)
            _lfg = field.getLoadFetchGroup();
```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
        }
        if (_fgSet == null && field._fgSet != null)
            _fgSet = new HashSet(field._fgSet);
        if (_lfg == null)
            _lfg = field.getLoadFetchGroup();
```

### UNCHECKED_WARNING
Unchecked method 'valueOf(Class, String)' invocation
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                return val;
            case JavaTypes.ENUM:
                return Enum.valueOf((Class<? extends Enum>)getDeclaredType(), val);
        }
        throw new MetaDataException(_loc.get("bad-external-type", this));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                return val;
            case JavaTypes.ENUM:
                return Enum.valueOf((Class<? extends Enum>)getDeclaredType(), val);
        }
        throw new MetaDataException(_loc.get("bad-external-type", this));
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                // TODO support CDI (OPENJPA-2714)
                if (_converterConstructor == null) {
                    _converterConstructor = converter.getDeclaredConstructor();
                }
                Object instance = _converterConstructor.newInstance();
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                // see AttributeConverter.java from the JPA specs
                if (_converterExtMethod == null) {
                    _converterExtMethod = converter.getDeclaredMethod("convertToDatabaseColumn", Object.class);
                }
                return _converterExtMethod.invoke(instance, val);
```

### UNCHECKED_WARNING
Unchecked call to 'ComparatorChain(List\>)' as a member of raw type 'org.apache.openjpa.lib.util.collections.ComparatorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                comp = null;
            else
                comp = new ComparatorChain(comps);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
        switch (getTypeCode()) {
            case JavaTypes.ARRAY:
                List l = JavaTypes.toList(val, _elem.getType(), true);
                Collections.sort(l, (Comparator<? super Order>) comp);
                return JavaTypes.toArray(l, _elem.getType());
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            case JavaTypes.ARRAY:
                List l = JavaTypes.toList(val, _elem.getType(), true);
                Collections.sort(l, (Comparator<? super Order>) comp);
                return JavaTypes.toArray(l, _elem.getType());
            case JavaTypes.COLLECTION:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Comparator\>' to 'java.util.Comparator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            case JavaTypes.ARRAY:
                List l = JavaTypes.toList(val, _elem.getType(), true);
                Collections.sort(l, (Comparator<? super Order>) comp);
                return JavaTypes.toArray(l, _elem.getType());
            case JavaTypes.COLLECTION:
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            case JavaTypes.COLLECTION:
                if (val instanceof List)
                    Collections.sort((List) val, (Comparator<? super Order>) comp);
                return val;
            default:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Comparator\>' to 'java.util.Comparator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            case JavaTypes.COLLECTION:
                if (val instanceof List)
                    Collections.sort((List) val, (Comparator<? super Order>) comp);
                return val;
            default:
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            extValue = transform((String) entry.getValue(), getTypeCode());

            extValues.put(fieldValue, extValue);
            fieldValues.put(extValue, fieldValue);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java

            extValues.put(fieldValue, extValue);
            fieldValues.put(extValue, fieldValue);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.security.PrivilegedExceptionAction' to 'java.security.PrivilegedExceptionAction'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                if (val == null && getNullValue() == NULL_DEFAULT)
                    return AccessController.doPrivileged(
                        J2DoPrivHelper.newInstanceAction(getDeclaredType()));

                // invoke either the constructor for the field type,
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                if (val == null && getNullValue() == NULL_DEFAULT)
                    return AccessController.doPrivileged(
                        J2DoPrivHelper.newInstanceAction(getDeclaredType()));

                // invoke either the constructor for the field type,
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                // TODO support CDI (OPENJPA-2714)
                if (_converterConstructor == null) {
                    _converterConstructor = converter.getDeclaredConstructor();
                }
                Object instance = _converterConstructor.newInstance();
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                // see AttributeConverter.java from the JPA specs
                if (_converterFactMethod == null) {
                    _converterFactMethod = converter.getDeclaredMethod("convertToEntityAttribute", Object.class);
                }
                return _converterFactMethod.invoke(instance, val);
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                try {
                    if (_factName == null)
                        _factMethod = getDeclaredType().getConstructor
                            (new Class[]{ getType() });
                    else
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
        for (Object o : values.entrySet()) {
            entry = (Map.Entry) o;
            map.put(entry.getKey(), entry.getValue());
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                return con;

            if (params[0].isAssignableFrom(cls) && (matchParam == null
                    || matchParam.isAssignableFrom(params[0]))) {
                // track most derived collection constructor
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java

            if (params[0].isAssignableFrom(cls) && (matchParam == null
                    || matchParam.isAssignableFrom(params[0]))) {
                // track most derived collection constructor
                match = con;
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        args = opts.setFromCmdLine(args);

        List types = new ArrayList(Arrays.asList(args));
        int utils = opts.removeIntProperty("utils", "u", 0);
        if (utils >= 4) {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        int utils = opts.removeIntProperty("utils", "u", 0);
        if (utils >= 4) {
            types.addAll(Arrays.asList(new String[] {
                java.sql.Date.class.getName(),
                java.sql.Time.class.getName(),
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        }
        if (utils >= 5) {
            types.addAll(Arrays.asList(new String[] {
                "java.util.EnumMap",
                "java.util.IdentityHashMap",
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
    private static Constructor findComparatorConstructor(Class cls) {
        try {
            return cls.getConstructor(new Class[] { Comparator.class });
        } catch (NoSuchMethodException nsme) {
            return null;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            Collection c = (Collection) newCollectionProxy(orig.getClass(),
                null, comp, autoOff);
            c.addAll((Collection) orig);
            return (Proxy) c;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                ? ((SortedMap) orig).comparator() : null;
            Map m = (Map) newMapProxy(orig.getClass(), null, null, comp, autoOff);
            m.putAll((Map) orig);
            return (Proxy) m;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        try {
            if (cons != null)
                return (Proxy) cls.getConstructor(cons.getParameterTypes()).
                    newInstance(args);
            return (Proxy) AccessController.doPrivileged(
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.security.PrivilegedExceptionAction' to 'java.security.PrivilegedExceptionAction'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                    newInstance(args);
            return (Proxy) AccessController.doPrivileged(
                J2DoPrivHelper.newInstanceAction(cls));
        } catch (InstantiationException ie) {
            throw new UnsupportedException(_loc.get("cant-newinstance",
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                    newInstance(args);
            return (Proxy) AccessController.doPrivileged(
                J2DoPrivHelper.newInstanceAction(cls));
        } catch (InstantiationException ie) {
            throw new UnsupportedException(_loc.get("cant-newinstance",
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
    private static final Map _stdMaps = new HashMap();
    static {
        _stdCollections.put(Collection.class, ArrayList.class);
        _stdCollections.put(Set.class, HashSet.class);
        _stdCollections.put(SortedSet.class, TreeSet.class);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
    static {
        _stdCollections.put(Collection.class, ArrayList.class);
        _stdCollections.put(Set.class, HashSet.class);
        _stdCollections.put(SortedSet.class, TreeSet.class);
        _stdCollections.put(List.class, ArrayList.class);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        _stdCollections.put(Collection.class, ArrayList.class);
        _stdCollections.put(Set.class, HashSet.class);
        _stdCollections.put(SortedSet.class, TreeSet.class);
        _stdCollections.put(List.class, ArrayList.class);
        _stdCollections.put(Queue.class, LinkedList.class);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        _stdCollections.put(Set.class, HashSet.class);
        _stdCollections.put(SortedSet.class, TreeSet.class);
        _stdCollections.put(List.class, ArrayList.class);
        _stdCollections.put(Queue.class, LinkedList.class);
        _stdMaps.put(Map.class, HashMap.class);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        _stdCollections.put(SortedSet.class, TreeSet.class);
        _stdCollections.put(List.class, ArrayList.class);
        _stdCollections.put(Queue.class, LinkedList.class);
        _stdMaps.put(Map.class, HashMap.class);
        _stdMaps.put(SortedMap.class, TreeMap.class);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        _stdCollections.put(List.class, ArrayList.class);
        _stdCollections.put(Queue.class, LinkedList.class);
        _stdMaps.put(Map.class, HashMap.class);
        _stdMaps.put(SortedMap.class, TreeMap.class);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        _stdCollections.put(Queue.class, LinkedList.class);
        _stdMaps.put(Map.class, HashMap.class);
        _stdMaps.put(SortedMap.class, TreeMap.class);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        Method getter;
        try {
            getter = type.getMethod("get" + name, (Class[]) null);
            if (getter.getReturnType().isAssignableFrom(param)
                || param.isAssignableFrom(getter.getReturnType()))
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
            getter = type.getMethod("get" + name, (Class[]) null);
            if (getter.getReturnType().isAssignableFrom(param)
                || param.isAssignableFrom(getter.getReturnType()))
                return getter;
        } catch (NoSuchMethodException nsme) {
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
        if (param == boolean.class || param == Boolean.class) {
            try {
                getter = type.getMethod("is" + name, (Class[]) null);
                if (getter.getReturnType().isAssignableFrom(param)
                    || param.isAssignableFrom(getter.getReturnType()))
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                getter = type.getMethod("is" + name, (Class[]) null);
                if (getter.getReturnType().isAssignableFrom(param)
                    || param.isAssignableFrom(getter.getReturnType()))
                    return getter;
            } catch (NoSuchMethodException nsme) {
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
    private boolean hasConstructor(Class type, Class<?>... paramTypes) {
        try {
            return type.getDeclaredConstructor(paramTypes) != null;
        }
        catch (NoSuchMethodException e) {
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/MethodLifecycleCallbacks.java`
#### Snippet
```java

        for (int i = 0; i < from.length; i++) {
            if (from[i] != null && !from[i].isAssignableFrom(to[i]))
                return false;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/SingleJVMRemoteCommitProvider.java`
#### Snippet
```java

    public SingleJVMRemoteCommitProvider() {
        s_providers.add(this);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/RemoteCommitEventManager.java`
#### Snippet
```java
                        if (persIds == null)
                            persIds = new ArrayList();
                        persIds.add(oid);
                    }
                    if (addClassNames == null)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/RemoteCommitEventManager.java`
#### Snippet
```java
                    if (addClassNames == null)
                        addClassNames = new HashSet();
                    addClassNames.add(obj.getClass().getName());
                }
                else if (sm.isDeleted()) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/RemoteCommitEventManager.java`
#### Snippet
```java
                    if (deletes == null)
                        deletes = new ArrayList();
                    deletes.add(oid);
                }
                else {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/RemoteCommitEventManager.java`
#### Snippet
```java
                    if (updates == null)
                        updates = new ArrayList();
                    updates.add(oid);
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(int, E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/RemoteCommitEventManager.java`
#### Snippet
```java
        if (_provider == null)
            throw new UserException(_loc.get("no-provider"));
        ((List) _listeners).add(0, listen);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/RemoteCommitEventManager.java`
#### Snippet
```java
            return null;

        List names = new ArrayList(clss);
        for (int i = 0; i < names.size(); i++)
            names.set(i, ((Class) names.get(i)).getName());
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/RemoteCommitEventManager.java`
#### Snippet
```java
        List names = new ArrayList(clss);
        for (int i = 0; i < names.size(); i++)
            names.set(i, ((Class) names.get(i)).getName());
        return names;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'org.apache.openjpa.lib.util.collections.AbstractHashedMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractStoreQuery.java`
#### Snippet
```java
        public LinkedMap getParameterTypes(StoreQuery q) {
            LinkedMap result = new LinkedMap();
            result.putAll(getOrderedParameterTypes(q));
            return result;
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.security.PrivilegedExceptionAction' to 'java.security.PrivilegedExceptionAction'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryLanguages.java`
#### Snippet
```java
            try {
                ep = (ExpressionParser) AccessController.doPrivileged(
                        J2DoPrivHelper.newInstanceAction(aClass));
            }
            catch (PrivilegedActionException pae) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryLanguages.java`
#### Snippet
```java
            try {
                ep = (ExpressionParser) AccessController.doPrivileged(
                        J2DoPrivHelper.newInstanceAction(aClass));
            }
            catch (PrivilegedActionException pae) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryLanguages.java`
#### Snippet
```java
                throw new InternalException(e);
            }
            _expressionParsers.put(ep.getLanguage(), ep);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
                    o);
            if (copy != null)
                copy.add(attached);
        }
        return (copy == null) ? coll : copy;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
                val = attachInPlace(manager, sm, fmd.getElement(), val);
            if (copy != null)
                copy.put(key, val);
        }
        return (copy == null) ? map : copy;
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingQuery.java`
#### Snippet
```java

    @Override
    public Collection getAggregateListeners() {
        try {
            return _query.getAggregateListeners();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingQuery.java`
#### Snippet
```java

    @Override
    public Collection getFilterListeners() {
        try {
            return _query.getFilterListeners();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingQuery.java`
#### Snippet
```java

    @Override
    public Map getUpdates() {
        try {
            return _query.getUpdates();
```

### UNCHECKED_WARNING
Unchecked call to 'FilterIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.FilterIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java

        public FilterNewIterator() {
            super(_broker.getTransactionalObjects().iterator());
            setPredicate(this);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'setPredicate(Predicate)' as a member of raw type 'org.apache.openjpa.lib.util.collections.FilterIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
        public FilterNewIterator() {
            super(_broker.getTransactionalObjects().iterator());
            setPredicate(this);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'FilterIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.FilterIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java

        public FilterDeletedIterator(Iterator itr) {
            super(itr);
            setPredicate(this);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'setPredicate(Predicate)' as a member of raw type 'org.apache.openjpa.lib.util.collections.FilterIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
        public FilterDeletedIterator(Iterator itr) {
            super(itr);
            setPredicate(this);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'setRemoveOnClose(ExtentImpl)' as a member of raw type 'org.apache.openjpa.kernel.ExtentImpl.CloseableIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
            for (Object openItr : _openItrs) {
                citr = (CloseableIterator) openItr;
                citr.setRemoveOnClose(null);
                try {
                    citr.close();
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
            boolean trans = !_ignore && _broker.isActive();
            if (trans)
                chain.addIterator(new FilterNewIterator());

            // add database iterators for each implementing class
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
                        _subs, _fc);
                if (rop != null)
                    chain.addIterator(new ResultObjectProviderIterator(rop));
            }

```

### UNCHECKED_WARNING
Unchecked call to 'setRemoveOnClose(ExtentImpl)' as a member of raw type 'org.apache.openjpa.kernel.ExtentImpl.CloseableIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
            else
                citr = chain;
            citr.setRemoveOnClose(this);
        } catch (OpenJPAException ke) {
            throw ke;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.kernel.ExtentImpl.CloseableIterator' to 'java.util.Iterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
            unlock();
        }
        return citr;
    }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java

    @Override
    public Set getRootInstances() {
        try {
            return _fetch.getRootInstances();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java
    public FetchConfiguration removeFetchGroups(Collection groups) {
        try {
            _fetch.removeFetchGroups(groups);
            return this;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java
    public FetchConfiguration removeFields(Collection fields) {
        try {
            _fetch.removeFields(fields);
            return this;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java
    public FetchConfiguration addFields(Collection fields) {
        try {
            _fetch.addFields(fields);
            return this;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java

    @Override
    public Set getFields() {
        try {
            return _fetch.getFields();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java

    @Override
    public Set getFetchGroups() {
        try {
            return _fetch.getFetchGroups();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java

    @Override
    public Set getRootClasses() {
        try {
            return _fetch.getRootClasses();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java
    public FetchConfiguration addFetchGroups(Collection groups) {
        try {
            _fetch.addFetchGroups(groups);
            return this;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingFetchConfiguration.java`
#### Snippet
```java
    public FetchConfiguration setRootClasses(Collection roots) {
        try {
            _fetch.setRootClasses(roots);
            return this;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.ReferenceHashSet' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
        if (!sm.isPersistent() || sm.isEmbedded()) {
            if (_embeds == null)
                _embeds = new ReferenceHashSet(AbstractReferenceMap.ReferenceStrength.WEAK);
            _embeds.add(sm);
            return;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
    ManagedCache(BrokerImpl broker) {
        this.broker = broker;
        _main = (Map<Object, StateManagerImpl>) broker.newManagedObjectCache();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
     */
    public void clear() {
        _main = (Map<Object, StateManagerImpl>) broker.newManagedObjectCache();
        if (_conflicts != null)
            _conflicts = null;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ManagedCache.java`
#### Snippet
```java
            size += _embeds.size();
        if (size == 0)
            return Collections.EMPTY_LIST;

        List<StateManagerImpl> copy = new ArrayList<>(size);
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/OrderingMergedResultObjectProvider.java`
#### Snippet
```java
            if (o2 == null)
                return (asc) ? -1 : 1;
            int cmp = ((Comparable) o1).compareTo(o2);
            if (!asc)
                cmp *= -1;
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.lang.Object\[\]', required 'T\[\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] a) {
        try {
            return _del.toArray(a);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.openjpa.kernel.DelegatingResultList.DelegatingListIterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingResultList.java`
#### Snippet
```java
                return true;
            if (other instanceof DelegatingListIterator)
                other = ((DelegatingListIterator) other).getInnermostDelegate();
            try {
                return getInnermostDelegate().equals(other);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.IdentityHashMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachManager.java`
#### Snippet
```java
     */
    void setAttachedCopy(Object from, PersistenceCapable into) {
        _attached.put(from, into);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachManager.java`
#### Snippet
```java
            throw (RuntimeException) exceps.get(0);

        Throwable[] t = (Throwable[]) exceps.toArray
            (new Throwable[exceps.size()]);
        if (opt && failed != null)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachManager.java`
#### Snippet
```java
        if (list == null)
            list = new LinkedList();
        list.add(obj);
        return list;
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachManager.java`
#### Snippet
```java
    private final boolean _failFast;
    private final IdentityHashMap _attached = new IdentityHashMap();
    private final Collection<StateManagerImpl> _visitedNodes = new ArrayList();

    // reusable strategies
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
                for (i = 0; i < values.length; i++)
                    putMethod.invoke(map, aliases[i], values[i]);
                return (T)map;
            } catch (InvocationTargetException t) {
                throw new RuntimeException(_loc.get("fill-map-error", putMethod, aliases[i], values[i]).getMessage(),
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.reflect.Constructor\>' to 'java.lang.reflect.Constructor'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
                        }
                        if (match) {
                            return (Constructor<X>)cons;
                        }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
        public T fill(Object[] values, Class<?>[] types, String[] aliases) {
            try {
                return (T)values[0];
            } catch (Exception e) {
                throw new RuntimeException(_loc.get("fill-assign-error", Arrays.toString(values),
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FillStrategy.java`
#### Snippet
```java
            Object array = java.lang.reflect.Array.newInstance(cls, values.length);
            System.arraycopy(values, 0, array, 0, values.length);
            return (T)array;
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryStatistics.java`
#### Snippet
```java
                new ConcurrentReferenceHashMap(ReferenceStrength.HARD, ReferenceStrength.HARD, CONCURRENCY, LOAD_FACTOR);
            statsMap.setMaxSize(FIXED_SIZE);
            stats = statsMap;

            ConcurrentReferenceHashMap aStatsMap =
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryStatistics.java`
#### Snippet
```java
                new ConcurrentReferenceHashMap(ReferenceStrength.HARD, ReferenceStrength.HARD, CONCURRENCY, LOAD_FACTOR);
            aStatsMap.setMaxSize(FIXED_SIZE);
            astats = aStatsMap;
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'X\[\]'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultShape.java`
#### Snippet
```java
     */
    <X> X[] chop(X[] values, int start, int finish) {
        X[] result = (X[])Array.newInstance(values.getClass().getComponentType(), finish-start);
        System.arraycopy(values, start, result, 0, finish-start);
        return result;
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, boolean)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultShape.java`
#### Snippet
```java
        assertNotPrimitive();
        for (Class<?> c : classes) {
            children.add(new ResultShape(c, true));
        }
        return this;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.kernel.FillStrategy' to 'org.apache.openjpa.kernel.FillStrategy'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultShape.java`
#### Snippet
```java
    public <X> ResultShape<X> nest(Class<X> cls, FillStrategy strategy, Class<?>... classes) {
        assertNotPrimitive();
        ResultShape<X> child = new ResultShape<X>(cls, strategy, true);
        this.nest(child.add(classes));
        return child;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultShape.java`
#### Snippet
```java
     */
    public Set<ResultShape<?>> getParents() {
        return parents == null ? Collections.EMPTY_SET : Collections.unmodifiableSet(parents);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SingleFieldManager.java`
#### Snippet
```java
        coll.clear();
        for (Object o : orig) {
            coll.add(embed(vmd, o));
        }
        return coll;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SingleFieldManager.java`
#### Snippet
```java
                if (valEmbed)
                    val = embed(fmd.getElement(), val);
                map.put(key, val);
            }
        } else {
```

### UNCHECKED_WARNING
Unchecked call to 'setValue(V)' as a member of raw type 'java.util.Map.Entry'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SingleFieldManager.java`
#### Snippet
```java
            for (Object o : map.entrySet()) {
                entry = (Map.Entry) o;
                entry.setValue(embed(fmd.getElement(),
                        entry.getValue()));
            }
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SingleFieldManager.java`
#### Snippet
```java
                    proxy = (Proxy) _sm.newFieldProxy(field);
                    if (objval != null)
                        ((Collection) proxy).addAll((Collection) objval);
                    ret = true;
                }
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SingleFieldManager.java`
#### Snippet
```java
                    proxy = (Proxy) _sm.newFieldProxy(field);
                    if (objval != null)
                        ((Map) proxy).putAll((Map) objval);
                    ret = true;
                }
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
                            sm.getBroker().getConfiguration().
                            getCompatibilityInstance().getAutoOff());
                        ((Collection) newVal).addAll((Collection) curVal);
                    } else
                        newVal = _proxy.copyCollection((Collection) curVal);
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
                                sm.getBroker().getConfiguration().
                                getCompatibilityInstance().getAutoOff());
                        ((Map) newVal).putAll((Map) curVal);
                    } else
                        newVal = _proxy.copyMap((Map) curVal);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
                detach = detachInternal(instance);
                if (_copy)
                    detached.add(detach);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
            throw (RuntimeException) exceps.get(0);
        throw new UserException(_loc.get("nested-exceps")).
            setNestedThrowables((Throwable[]) exceps.toArray
                (new Throwable[exceps.size()]));
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
                        val = detachInternal(val);
                    if (_copy)
                        map.put(key, val);
                }
            } else {
```

### UNCHECKED_WARNING
Unchecked call to 'setValue(V)' as a member of raw type 'java.util.Map.Entry'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
                for (Object o : map.entrySet()) {
                    entry = (Map.Entry) o;
                    entry.setValue(detachInternal(entry.getValue()));
                }
			}
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
        } catch (CallbackException ce) {
            exceps = new ArrayList(1);
            exceps.add(ce);
            return null; // won't be reached as exception will be rethrown
        } finally {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.IdentityHashMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
            detachedPC = pc;
        if (_detached != null)
            _detached.put(toDetach, detachedPC);

        // detach fields and set detached variables
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
                detached = detachInternal(o);
                if (_copy)
                    coll.add(detached);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
        if (exceps == null)
            exceps = new LinkedList();
        exceps.add(re);
        return exceps;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/InverseManager.java`
#### Snippet
```java
            switch (_action) {
                case ACTION_MANAGE:
                    coll.add(val);
                    break;
                case ACTION_WARN:
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
    protected Object[] toRelationFields(OpenJPAStateManager sm,
        Object data, FetchConfiguration fetch) {
        return sm.getContext().findAll((Collection) data, fetch, null, null, 0);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.ArrayList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                            c2.nextSequence = size;
                    }
                    c2.add(val);
                }
                return c2;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                    if (m2 == null)
                        m2 = new HashMap(m.size());
                    m2.put(val, val2);
                }
                return m2;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
            case JavaTypes.DATE:
                for (Object item : data) {
                    ret.add(((Date) item).clone());
                }
                return ret;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
            case JavaTypes.LOCALE:
                for (Object value : data) {
                    ret.add((Locale) value);
                }
                return ret;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                if (vmd.isEmbedded()) {
                    for (Object datum : data) {
                        ret.add(toEmbeddedField(sm, vmd, datum, fetch
                                , context));
                    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                    for (Object o : r)
                        if (o != null)
                            ret.add(o);
                        else {
                            ret.add(sm.getContext().getConfiguration().
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                            ret.add(o);
                        else {
                            ret.add(sm.getContext().getConfiguration().
                                    getOrphanedKeyActionInstance().
                                    orphan(data, sm, vmd));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set' to 'java.lang.Iterable'. Reason: 'm' has raw type, so result of entrySet is erased
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                Collection values = new ArrayList(m.size());

                for (Map.Entry e : (Iterable<Map.Entry>) m.entrySet()) {
                    keys.add(e.getKey());
                    values.add(e.getValue());
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java

                for (Map.Entry e : (Iterable<Map.Entry>) m.entrySet()) {
                    keys.add(e.getKey());
                    values.add(e.getValue());
                }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                for (Map.Entry e : (Iterable<Map.Entry>) m.entrySet()) {
                    keys.add(e.getKey());
                    values.add(e.getValue());
                }
                Object[] keyArray = toNestedFields(sm, fmd.getKey(),
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractPCData.java`
#### Snippet
```java
                    values, fetch, context).toArray();
                for (int idx = 0; idx < keyArray.length; idx++)
                    m2.put(keyArray[idx], valueArray[idx]);

                return m2;
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Bootstrap.java`
#### Snippet
```java
        Method meth;
        try {
            meth = cls.getMethod(methodName, argTypes);
        } catch (NoSuchMethodException nsme) {
            // handle cases where there is a mismatch between loaders by falling
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Bootstrap.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getClassLoaderAction(conf.getClass())));
            meth = cls.getMethod(methodName, argTypes);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
        } catch (Exception e) {
            Collection coll = (Collection) sm.newFieldProxy(fmd.getIndex());
            coll.addAll(orig);
            return coll;
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
            return manager.getProxyManager().copyMap(orig);
        } catch (Exception e) {
            Map<Object, Object> map = (Map<Object, Object>) sm.newFieldProxy(fmd.getIndex());

            for (Entry<Object, Object> entry : ((Map<Object, Object>) orig).entrySet()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
            Map<Object, Object> map = (Map<Object, Object>) sm.newFieldProxy(fmd.getIndex());

            for (Entry<Object, Object> entry : ((Map<Object, Object>) orig).entrySet()) {
                map.put(entry.getKey(), entry.getValue());
            }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
            if (!tom.containsKey(entry.getKey())
                    || !equals(tom.get(entry.getKey()), entry.getValue(), valPC)) {
                tom.put(entry.getKey(), entry.getValue());
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
                        val = manager.attach(val, null, sm, valmd, false);
                }
                map.put(key, val);
            }
        } else {
```

### UNCHECKED_WARNING
Unchecked call to 'setValue(V)' as a member of raw type 'java.util.Map.Entry'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
                    val = manager.attach(entry.getValue(), null, sm, valmd,
                            false);
                entry.setValue(val);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
            // just add the extra elements in frml to tol and return tol
            while (frmi.hasNext())
                tol.add(frmi.next());
            return tol;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
            else
                elem = manager.attach(itr.next(), null, sm, vmd, false);
            coll.add(elem);
        }
        return coll;
```

### UNCHECKED_WARNING
Unchecked call to 'retainAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java

        // remove all elements from the toc collection that aren't in frmc
        toc.retainAll(frmc);

        // now add all elements that are in frmc but not toc
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachStrategy.java`
#### Snippet
```java
            for (Object ob : frmc) {
                if (!toc.contains(ob))
                    toc.add(ob);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.openjpa.kernel.DelegatingExtent'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingExtent.java`
#### Snippet
```java
            return true;
        if (other instanceof DelegatingExtent)
            other = ((DelegatingExtent<T>) other).getInnermostDelegate();
        return getInnermostDelegate().equals(other);
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java
    public void nontransactionalAll(Collection objs, OpCallbacks call) {
        try {
            _broker.nontransactionalAll(objs, call);
        } catch (RuntimeException re) {
            throw translate(re, objs);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java

    @Override
    public Collection getPersistedTypes() {
        try {
            return _broker.getPersistedTypes();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Iterator', required 'java.util.Iterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java

    @Override
    public Iterator extentIterator(Class cls, boolean subs,
        FetchConfiguration fetch, boolean ignoreChanges) {
        try {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java

    @Override
    public Collection getUpdatedTypes() {
        try {
            return _broker.getUpdatedTypes();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java
    public void retrieveAll(Collection objs, boolean fgOnly, OpCallbacks call) {
        try {
            _broker.retrieveAll(objs, fgOnly, call);
        } catch (RuntimeException re) {
            throw translate(re);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java
        OpCallbacks call) {
        try {
            _broker.transactionalAll(objs, updateVersion, call);
        } catch (RuntimeException re) {
            throw translate(re, objs);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java

    @Override
    public Collection getDeletedTypes() {
        try {
            return _broker.getDeletedTypes();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java
        BitSet exclude, Object edata, int flags) {
        try {
            return _broker.findAll(oids, fetch, exclude, edata, flags);
        } catch (RuntimeException re) {
            throw translate(re, oids);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java

    @Override
    public Collection getDirtyObjects() {
        try {
            return _broker.getDirtyObjects();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java

    @Override
    public Collection getTransactionalObjects() {
        try {
            return _broker.getTransactionalObjects();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java
        FindCallbacks call) {
        try {
            return _broker.findAll(oids, validate, call);
        } catch (RuntimeException re) {
            throw translate(re, oids);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java

    @Override
    public Collection getManagedObjects() {
        try {
            return _broker.getManagedObjects();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DelegatingBroker.java`
#### Snippet
```java

    @Override
    public Collection getPendingTransactionalObjects() {
        try {
            return _broker.getPendingTransactionalObjects();
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/MethodStoreQuery.java`
#### Snippet
```java
                for (Iterator itr = paramTypes.keySet().iterator();
                    itr.hasNext(); idx++)
                    paramMap.put(itr.next(), params[idx]);
            }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/MethodStoreQuery.java`
#### Snippet
```java
                        args[OBJ_INDEX] = obj;
                        if ((Boolean) invoke(q, args))
                            results.add(obj);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/MethodStoreQuery.java`
#### Snippet
```java
            Class[] types = (_inMem) ? ARGS_INMEM : ARGS_DATASTORE;
            try {
                meth = cls.getMethod(methName, types);
            } catch (Exception e) {
                String msg = (_inMem) ? "bad-inmem-method"
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.MapBackedSet' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractBrokerFactory.java`
#### Snippet
```java

        if (FinalizingBrokerImpl.class.isAssignableFrom(bv.getTemplateBrokerType(_conf))) {
            return MapBackedSet.mapBackedSet(new ConcurrentHashMap(), new Object() { });
        } else {
            return new ConcurrentReferenceHashSet<>(ReferenceStrength.WEAK);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.concurrent.ConcurrentHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AbstractBrokerFactory.java`
#### Snippet
```java

        if (FinalizingBrokerImpl.class.isAssignableFrom(bv.getTemplateBrokerType(_conf))) {
            return MapBackedSet.mapBackedSet(new ConcurrentHashMap(), new Object() { });
        } else {
            return new ConcurrentReferenceHashSet<>(ReferenceStrength.WEAK);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Cast.java`
#### Snippet
```java
        Collection casts = new ArrayList(res.size());
        for (Object re : res) {
            casts.add(Filters.convert(re, _cast));
        }
        return casts;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Val.java`
#### Snippet
```java
        for (Object o : candidates) {
            candidate = o;
            ret.add(evaluate(candidate, (orig == null) ? candidate : orig,
                    ctx, params));
        }
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/LessThanEqualExpression.java`
#### Snippet
```java
        if (!(o1 instanceof Comparable))
            return false;
        return ((Comparable) o1).compareTo(o2) <= 0;
    }
}
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
                    Method getValue2 = null;
                    try {
                        getValue2 = cls.getMethod("getValue2");
                        getValue2.setAccessible(true);
                        value2 = getValue2.invoke(exps.having, (Object[]) null);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java

        @Override
        public final Map getUpdates(StoreQuery q) {
            return assertQueryExpression().updates;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
                    if (_factory.matches(_exps[0], _meta, _subs, obj, ctx,
                        params))
                        results.add(obj);
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
                    c = (Collection) itr.next();
                    if (_factory.matches(_exps[0], c, ctx, params))
                        matches.add(c);
                }
                results = matches;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
                        if (_grouped == null)
                            _grouped = new HashSet();
                        _grouped.add(val);
                    }
                } else if (_agg == null) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CandidatePath.java`
#### Snippet
```java
        if (_actions == null)
            _actions = new LinkedList();
        _actions.add(type);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CandidatePath.java`
#### Snippet
```java
        if (_actions == null)
            _actions = new LinkedList();
        _actions.add(new Traversal(field, nullTraversal));
    }

```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/GreaterThanExpression.java`
#### Snippet
```java
        if (!(o1 instanceof Comparable))
            return false;
        return ((Comparable) o1).compareTo(o2) > 0;
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            return new FilterListener[]{ (FilterListener) hint };
        if (hint instanceof Collection) {
            Collection<FilterListener> c = (Collection<FilterListener>) hint;
            return c.toArray(new FilterListener[c.size()]);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            return new AggregateListener[]{ (AggregateListener) hint };
        if (hint instanceof Collection) {
            Collection<AggregateListener> c = (Collection<AggregateListener>) hint;
            return c.toArray(new AggregateListener[c.size()]);
        }
```

### UNCHECKED_WARNING
Unchecked method 'valueOf(Class, String)' invocation
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
                }
            } else if (o instanceof String && type.isEnum()) {
                return Enum.valueOf((Class<Enum>)type, o.toString());
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
                }
            } else if (o instanceof String && type.isEnum()) {
                return Enum.valueOf((Class<Enum>)type, o.toString());
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'org.apache.openjpa.lib.util.collections.AbstractHashedMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        try {
            LinkedMap wrap = new LinkedMap();
            wrap.putAll(compileForExecutor().getOrderedParameterTypes(_storeQuery));
            return wrap;
        } finally {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
                actions = executor.getDataStoreActions(q, params, ropRange);
                if (actions != null && actions.length > 0)
                    results.addAll(Arrays.asList(actions));
            }
            return (String[]) results.toArray(new String[results.size()]);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
                    results.addAll(Arrays.asList(actions));
            }
            return (String[]) results.toArray(new String[results.size()]);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public boolean add(Object o) {
            return _res.add(o);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public Object[] toArray(Object[] a) {
            return _res.toArray(a);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'addAll(int, Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public boolean addAll(int idx, Collection c) {
            return _res.addAll(idx, c);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(int, E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public void add(int idx, Object o) {
            _res.add(idx, o);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public Object set(int idx, Object o) {
            return _res.set(idx, o);
        }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

        @Override
        public Map getUpdates(StoreQuery q) {
            return _executors[0].getUpdates(q);
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
                    ex.toParameterArray(_storeQuery, params);
                if (_log.isTraceEnabled())
                    logExecution(operation, params);

                if (operation == OP_SELECT)
```

### UNCHECKED_WARNING
Unchecked call to 'removeAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public boolean removeAll(Collection c) {
            return _res.removeAll(c);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.ReferenceHashSet' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
    // can free their resources when close or closeAll is called
    private transient final Collection<RemoveOnCloseResultList> _resultLists =
        new ReferenceHashSet(AbstractReferenceMap.ReferenceStrength.WEAK);

    private boolean _printParameters = false;
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

    @Override
    public Map getUpdates() {
        lock();
        try {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public boolean addAll(Collection c) {
            return _res.addAll(c);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
            List metas = null;
            for (StoreQuery.Executor executor : _executors)
                metas = Filters.addAccessPathMetaDatas(metas, executor.
                        getAccessPathMetaDatas(q));
            if (metas == null)
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
            if (metas == null)
                return StoreQuery.EMPTY_METAS;
            return (ClassMetaData[]) metas.toArray
                (new ClassMetaData[metas.size()]);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'retainAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public boolean retainAll(Collection c) {
            return _res.retainAll(c);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'containsAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        @Override
        public boolean containsAll(Collection c) {
            return _res.containsAll(c);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/LessThanExpression.java`
#### Snippet
```java
        if (!(o1 instanceof Comparable))
            return false;
        return ((Comparable) o1).compareTo(o2) < 0;
    }
}
```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Distinct.java`
#### Snippet
```java
        StoreContext ctx, Object[] params) {
        Collection args = _val.eval(candidates, orig, ctx, params);
        return new HashSet(args);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
                    OpenJPAConfiguration conf = _broker.getConfiguration();
                    Class confCls = Class.forName("org.apache.openjpa.jdbc.conf.JDBCConfigurationImpl");
                    if (confCls.isAssignableFrom(conf.getClass())) {
                        Object o = conf.getClass().getMethod("getDBDictionaryInstance").invoke(conf, (Object[]) null);
                        _datePrecision = o.getClass().getField("datePrecision").getInt(o);
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/GreaterThanEqualExpression.java`
#### Snippet
```java
        if (!(o1 instanceof Comparable))
            return false;
        return ((Comparable) o1).compareTo(o2) >= 0;
    }
}
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Min.java`
#### Snippet
```java
        for (Object o : os) {
            cur = (Comparable) o;
            if (cur != null && (min == null || min.compareTo(cur) > 0))
                min = cur;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/BoundVariable.java`
#### Snippet
```java
     */
    public boolean setValue(Object value) {
        if (value != null && !_type.isAssignableFrom(value.getClass()))
            return false;
        _val = value;
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/BoundVariable.java`
#### Snippet
```java
    public void castTo(Class type) {
        // incompatible types?
        if (!_type.isAssignableFrom(type)
            && !type.isAssignableFrom(_type))
            throw new ClassCastException(_type.getName());
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/BoundVariable.java`
#### Snippet
```java
        // incompatible types?
        if (!_type.isAssignableFrom(type)
            && !type.isAssignableFrom(_type))
            throw new ClassCastException(_type.getName());
        _type = type;
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Max.java`
#### Snippet
```java
        for (Object o : os) {
            cur = (Comparable) o;
            if (cur != null && (max == null || max.compareTo(cur) < 0))
                max = cur;
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map,org.apache.openjpa.enhance.PCRegistry.Meta\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCRegistry.java`
#### Snippet
```java

    // map of persistent classes to meta structures; weak so the VM can GC classes
    private static final Map<Class<?>,Meta> _metas = new ConcurrentReferenceHashMap(
            AbstractReferenceMap.ReferenceStrength.WEAK,
            AbstractReferenceMap.ReferenceStrength.HARD);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
            // the ordering value already after the projection values
            idx = (results > 0) ? results + i : -1;
            Collections.sort(matches,
                new OrderValueComparator((Val) orderValues[i],
                    asc == null || asc[i], idx, ctx, params));
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.kernel.exps.InMemoryExpressionFactory.OrderValueComparator' to 'java.util.Comparator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
            idx = (results > 0) ? results + i : -1;
            Collections.sort(matches,
                new OrderValueComparator((Val) orderValues[i],
                    asc == null || asc[i], idx, ctx, params));
        }
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
            try {
                if (_asc)
                    return ((Comparable) o1).compareTo(o2);
                return ((Comparable) o2).compareTo(o1);
            } catch (ClassCastException cce) {
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
                if (_asc)
                    return ((Comparable) o1).compareTo(o2);
                return ((Comparable) o2).compareTo(o1);
            } catch (ClassCastException cce) {
                Localizer loc = Localizer.forPackage
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
        List projected = new ArrayList(matches.size());
        for (Object match : matches)
            projected.add(project(match, exps, exps.grouping.length > 0,
                    ctx, params));
        return projected;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
            if (!eq) {
                if (group != null)
                    grouped.add(group);
                group = new ArrayList();
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
                group = new ArrayList();
            }
            group.add(pc);
            System.arraycopy(curs, 0, prevs, 0, curs.length);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
        // add the last group formed
        if (group != null)
            grouped.add(group);

        return grouped;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
            key = (len > 0 && cur != null) ? new ArrayKey((Object[]) cur) : cur;

            if (seen.add(key)) {
                // key hasn't been seen before; if we've created a distinct
                // list, keep adding to it
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
                // list, keep adding to it
                if (distinct != null)
                    distinct.add(cur);
            } else if (distinct == null) {
                // we need to copy the matches list because the distinct list
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
                // all the elements we've skipped over so far
                distinct = new ArrayList(matches.size());
                distinct.addAll(matches.subList(0, li.previousIndex()));
            }
        }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

    @Override
    public Collection getDirtyObjects() {
        beginOperation(false);
        try {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

    @Override
    public Collection getManagedObjects() {
        beginOperation(false);
        try {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            } else {
                if (_savepointCache == null)
                    save.save(Collections.EMPTY_SET);
                else {
                    save.save(_savepointCache);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'org.apache.openjpa.lib.util.collections.AbstractHashedMap'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                }
            }
            _savepoints.put(name, save);
        } catch (OpenJPAException ke) {
            throw ke;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            return;
        }
        Collection<StateManagerImpl> states = getManagedStates();
        StateManagerImpl sm;
        for (Iterator<StateManagerImpl> itr = states.iterator(); itr.hasNext();) {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

    @Override
    public Collection getUpdatedTypes() {
        if (_updatedClss == null || _updatedClss.isEmpty())
            return Collections.EMPTY_SET;
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Iterator', required 'java.util.Iterator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

    @Override
    public Iterator extentIterator(Class type, boolean subclasses,
        FetchConfiguration fetch, boolean ignoreChanges) {
        Extent extent = newExtent(type, subclasses, fetch);
```

### UNCHECKED_WARNING
Unchecked call to 'ExtentImpl(Broker, Class, boolean, FetchConfiguration)' as a member of raw type 'org.apache.openjpa.kernel.ExtentImpl'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        beginOperation(true);
        try {
            ExtentImpl extent = new ExtentImpl(this, type, subclasses, fetch);
            if (_extents == null)
                _extents = new ReferenceHashSet(AbstractReferenceMap.ReferenceStrength.WEAK);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        try {
            // evict all PClean and PNonTrans objects
            Collection<StateManagerImpl> c = getManagedStates();
            StateManagerImpl sm;
            for (StateManagerImpl stateManager : c) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if (_dirty != null)
                for (StateManagerImpl stateManager : _dirty) {
                    copy.add(stateManager);
                }
            if (_clean != null)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if (_clean != null)
                for (StateManagerImpl stateManager : _clean) {
                    copy.add(stateManager);
                }
            return copy;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        // keep local transactional list copy up to date
        transactional.addAll(_transAdditions);

        // copy the change set, then clear it for the next iteration
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

    @Override
    public Collection getTransactionalObjects() {
        beginOperation(false);
        try {
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            IteratorChain chain = new IteratorChain();
            if (_dirty != null && !_dirty.isEmpty())
                chain.addIterator(_dirty.iterator());
            if (_clean != null && !_clean.isEmpty())
                chain.addIterator(_clean.iterator());
```

### UNCHECKED_WARNING
Unchecked call to 'addIterator(Iterator)' as a member of raw type 'org.apache.openjpa.lib.util.collections.IteratorChain'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                chain.addIterator(_dirty.iterator());
            if (_clean != null && !_clean.isEmpty())
                chain.addIterator(_clean.iterator());
            return chain;
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        try {
            // evict all PClean and PNonTrans objects in extent
            Collection<StateManagerImpl> c = getManagedStates();
            StateManagerImpl sm;
            Class<?> cls;
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                    if (cls == extent.getElementType()
                            || (extent.hasSubclasses()
                            && extent.getElementType().isAssignableFrom(cls)))
                        evict(sm.getManagedInstance(), call);
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.ReferenceHashSet' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        private boolean addCleanInternal(StateManagerImpl sm) {
            if (_clean == null)
                _clean = new ReferenceHashSet(AbstractReferenceMap.ReferenceStrength.SOFT);
            return _clean.add(sm);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        // remember these additions in case one becomes derefed again later
        derefs.addAll(_derefAdditions);

        StateManagerImpl[] states = _derefAdditions.
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
    @Override
    public Collection<Object> getTransactionListeners() {
        return _transEventManager.getListeners();
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.MapBackedSet' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if (_dirty == null) {
                if (_orderDirty)
                    _dirty = MapBackedSet.mapBackedSet(new LinkedMap());
                else
                    _dirty = new HashSet<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.collections.LinkedMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if (_dirty == null) {
                if (_orderDirty)
                    _dirty = MapBackedSet.mapBackedSet(new LinkedMap());
                else
                    _dirty = new HashSet<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                _flags |= FLAG_STORE_FLUSHING;
                exceps = add(exceps,
                    newFlushException(_store.flush(transactional)));
            }
        } finally {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                _flags |= FLAG_STORE_FLUSHING;
                exceps = add(exceps,
                    newFlushException(_store.flush(transactional)));
            }
        } finally {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'. Reason: '_savepoints' has raw type, so result of values is erased
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

            save = (OpenJPASavepoint) _savepoints.remove(index);
            Collection saved = save.rollback(_savepoints.values());
            if (_savepointCache != null)
                _savepointCache.clear();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

    @Override
    public Collection getDeletedTypes() {
        if (_deletedClss == null || _deletedClss.isEmpty())
            return Collections.EMPTY_SET;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            return new LinkedHashSet<>();
        }
        return _transCache.copy();
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                    getDataCacheManagerInstance().getSystemQueryCache();
                    if (queryCache != null) {
                        Collection<Class<?>> pers = getPersistedTypes();
                        Collection<Class<?>> del = getDeletedTypes();
                        Collection<Class<?>> up = getUpdatedTypes();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                    if (queryCache != null) {
                        Collection<Class<?>> pers = getPersistedTypes();
                        Collection<Class<?>> del = getDeletedTypes();
                        Collection<Class<?>> up = getUpdatedTypes();
                        int size = pers.size() + del.size() + up.size();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                        Collection<Class<?>> pers = getPersistedTypes();
                        Collection<Class<?>> del = getDeletedTypes();
                        Collection<Class<?>> up = getUpdatedTypes();
                        int size = pers.size() + del.size() + up.size();
                        if (size > 0) {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

    @Override
    public Collection getPersistedTypes() {
        if (_persistedClss == null || _persistedClss.isEmpty())
            return Collections.EMPTY_SET;
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

    @Override
    public Collection getPendingTransactionalObjects() {
        beginOperation(false);
        try {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java
            errors = new ArrayList();

        errors.add(loc.get("subclasser-error-meta", s,
            cls.getDescribedType().getName(),
            cls.getSourceFile()));
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java
            contractViolations = new ArrayList();

        contractViolations.add(loc.get
            ("subclasser-contract-violation-field", m.getMessage(),
                fmd.getFullName(), fmd.getDeclaringMetaData().getSourceFile()));
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java
            errors = new ArrayList();

        errors.add(loc.get("subclasser-error-field", s,
            fmd.getFullName(),
            fmd.getDeclaringMetaData().getSourceFile()));
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCSubclassValidator.java`
#### Snippet
```java

        try {
            Constructor c = superclass.getDeclaredConstructor(new Class[0]);
            if (!(Modifier.isProtected(c.getModifiers())
                || Modifier.isPublic(c.getModifiers())))
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map,java.util.Map\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
    private static Map<Class<?>, Map<String, Method>> setterMethodCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
    private static Map<Class<?>, Set<String>> beanPropertiesNameCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        Map<String, Method> clsMap = setterMethodCache.get(cls);
        if (clsMap == null) {
            clsMap = new ConcurrentReferenceHashMap(ReferenceStrength.HARD,
                ReferenceStrength.WEAK);
            setterMethodCache.put(cls, clsMap);
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
            Class ret1 = meth1.getReturnType();
            Class ret2 = meth2.getReturnType();
            if (ret1.isAssignableFrom(ret2))
                return meth2;
            else if (ret2.isAssignableFrom(ret1))
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
            if (ret1.isAssignableFrom(ret2))
                return meth2;
            else if (ret2.isAssignableFrom(ret1))
                return meth1;
            else
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
                        .getMessage());
        } else {
            if (cls1.isAssignableFrom(cls2))
                return meth2;
            else if (cls2.isAssignableFrom(cls1))
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
            if (cls1.isAssignableFrom(cls2))
                return meth2;
            else if (cls2.isAssignableFrom(cls1))
                return meth1;
            else
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map,java.util.Map\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    // Weak HashMap caches of getter/setter/beanProperty methods
    private static Map<Class<?>, Map<String, Method>> getterMethodCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
    private static Map<Class<?>, Map<String, Method>> setterMethodCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    static boolean canReflect(Field field) {
        Class cls = field.getDeclaringClass();
        return canReflect((Reflectable)cls.getAnnotation(Reflectable.class),
            field.getAnnotation(Reflectable.class));
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    public static <T> Set<T> getFieldValues(Class c, int mods, Class<T> t){
        if (c == null)
            return Collections.EMPTY_SET;
        Field[] fields = c.getFields();
        if (fields == null || fields.length == 0)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        Field[] fields = c.getFields();
        if (fields == null || fields.length == 0)
            return Collections.EMPTY_SET;
        Set<T> result = new TreeSet<>();
        for (Field f : fields) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
            && canReflect(f)) {
                try {
                    result.add((T)f.get(null));
                } catch (IllegalArgumentException | IllegalAccessException e) {
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    public static Set<String> getPublicFieldNames(Class c) {
        if (c == null)
            return Collections.EMPTY_SET;
        Field[] fields = c.getFields();
        if (fields == null || fields.length == 0)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        Field[] fields = c.getFields();
        if (fields == null || fields.length == 0)
            return Collections.EMPTY_SET;
        Set<String> result = new TreeSet<>();
        for (Field f : fields) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        Map<String, Method> clsMap = getterMethodCache.get(cls);
        if (clsMap == null) {
            clsMap = new ConcurrentReferenceHashMap(ReferenceStrength.HARD,
                ReferenceStrength.WEAK);
            getterMethodCache.put(cls, clsMap);
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
    public static Method getDeclaringMethod(Class c, Method m) {
        try {
            Method m0 = c.getMethod(m.getName(), m.getParameterTypes());
            return m0;
        } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        if (cls != method.getDeclaringClass())
            method = getDeclaringMethod(cls, method);
        return canReflect((Reflectable)cls.getAnnotation(Reflectable.class),
            method.getAnnotation(Reflectable.class));
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map,java.util.Set\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);
    private static Map<Class<?>, Set<String>> beanPropertiesNameCache =
        new ConcurrentReferenceHashMap(ReferenceStrength.WEAK, ReferenceStrength.HARD);

    private static Method getGetterMethod(Class<?> cls, String prop) {
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
            // muti-selection
            resultClass = Object[].class;
            resultShape = new ResultShape(resultClass, new FillStrategy.Array<>(Object[].class));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'NewInstance(Class)' as a member of raw type 'org.apache.openjpa.kernel.FillStrategy.NewInstance'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
                // now assign the arguments to the select clause as the projections
                assignProjections(right(node), exps, terms, aliases, clauses);
                FillStrategy fill = new FillStrategy.NewInstance(constructor);
                ResultShape<?> cons = new ResultShape(constructor, fill);
                for (Value val : terms) {
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
                assignProjections(right(node), exps, terms, aliases, clauses);
                FillStrategy fill = new FillStrategy.NewInstance(constructor);
                ResultShape<?> cons = new ResultShape(constructor, fill);
                for (Value val : terms) {
                    Class<?> type = val.getType();
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy, boolean)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
                for (Value val : terms) {
                    Class<?> type = val.getType();
                    cons.nest(new ResultShape(type, new FillStrategy.Assign(), type.isPrimitive()));
                }
                if (count == 1) {
```

### UNCHECKED_WARNING
Unchecked call to 'ResultShape(Class, FillStrategy, boolean)' as a member of raw type 'org.apache.openjpa.kernel.ResultShape'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
                projectionAliases.add(alias);
                Class<?> type = proj.getType();
                ResultShape<?> projShape = new ResultShape(type, new FillStrategy.Assign(), type.isPrimitive());

                if (count == 1)
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/DynamicStorageGenerator.java`
#### Snippet
```java
        try {
            Class cls = Class.forName(bc.getName(), false, _loader);
            Constructor cons = cls.getConstructor((Class[]) null);
            DynamicStorage data = (DynamicStorage) cons.newInstance
                ((Object[]) null);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/CodeGenerator.java`
#### Snippet
```java
    public Set getImportPackages() {
        Set pkgs = new TreeSet();
        pkgs.add(ClassUtil.getPackageName(_type.getSuperclass()));

        FieldMetaData[] fields = _meta.getDeclaredFields();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/CodeGenerator.java`
#### Snippet
```java
        FieldMetaData[] fields = _meta.getDeclaredFields();
        for (FieldMetaData fieldMetaData : fields) {
            pkgs.add(ClassUtil.getPackageName(fieldMetaData.getDeclaredType()));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/CodeGenerator.java`
#### Snippet
```java
        fields = _meta.getPrimaryKeyFields();
        for (FieldMetaData field : fields) {
            pkgs.add(ClassUtil.getPackageName(field.getDeclaredType()));
        }

```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java
    public static final String POSTFIX = "datacache";

    private static final Set _synchs = new HashSet(Arrays.asList
        (new String []{ "getData", "setData", "clearData", "getImplData",
            "setImplData", "setIntermediate", "getIntermediate",
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java
        code.iload().setLocal(local);
        code.constant().setValue(JavaTypes.COLLECTION);
        jumps.add(code.ificmpeq());
        code.iload().setLocal(local);
        code.constant().setValue(JavaTypes.MAP);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java
        code.iload().setLocal(local);
        code.constant().setValue(JavaTypes.MAP);
        jumps.add(code.ificmpeq());
        code.iload().setLocal(local);
        code.constant().setValue(JavaTypes.ARRAY);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java
        code.iload().setLocal(local);
        code.constant().setValue(JavaTypes.ARRAY);
        jumps.add(code.ificmpeq());
        code.aload().setThis();
        code.aload().setParam(0);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCachePCDataGenerator.java`
#### Snippet
```java
        code.areturn();
        setTarget(code.getstatic().setField
            (AbstractPCData.class, "NULL", Object.class), jumps);
        code.areturn();
        code.calculateMaxStack();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
            oid = o;
            if (oid instanceof Id)
                classes.add(((Id) oid).getType());
            else {
                // ok if no metadata for oid; that just means the pc type
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
                meta = repos.getMetaData(oid, null, false);
                if (meta != null)
                    classes.add(meta.getDescribedType());
            }
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
                for (Object o: keys) {
                    qk = (QueryKey) o;
                if (qk.changeInvalidatesQuery(ev.getTypes())) {
                    if (removes == null)
                        removes = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
                new ConcurrentReferenceHashMap(ReferenceStrength.HARD, ReferenceStrength.HARD, CONCURRENCY, LOAD_FACTOR);
            statsMap.setMaxSize(FIXED_SIZE);
            stats = statsMap;

            ConcurrentReferenceHashMap aStatsMap =
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.concurrent.ConcurrentReferenceHashMap' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
                new ConcurrentReferenceHashMap(ReferenceStrength.HARD, ReferenceStrength.HARD, CONCURRENCY, LOAD_FACTOR);
            aStatsMap.setMaxSize(FIXED_SIZE);
            astats = aStatsMap;
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
        // drop all committed classes
        Set classes = Caches.addTypesByName(conf,
            event.getPersistedTypeNames(), null);
        if (event.getPayloadType() == RemoteCommitEvent.PAYLOAD_EXTENTS) {
            classes = Caches.addTypesByName(conf, event.getUpdatedTypeNames(),
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
            event.getPersistedTypeNames(), null);
        if (event.getPayloadType() == RemoteCommitEvent.PAYLOAD_EXTENTS) {
            classes = Caches.addTypesByName(conf, event.getUpdatedTypeNames(),
                classes);
            classes = Caches.addTypesByName(conf, event.getDeletedTypeNames(),
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
        if (event.getPayloadType() == RemoteCommitEvent.PAYLOAD_EXTENTS) {
            classes = Caches.addTypesByName(conf, event.getUpdatedTypeNames(),
                classes);
            classes = Caches.addTypesByName(conf, event.getDeletedTypeNames(),
                classes);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
            classes = Caches.addTypesByName(conf, event.getUpdatedTypeNames(),
                classes);
            classes = Caches.addTypesByName(conf, event.getDeletedTypeNames(),
                classes);
        } else {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractQueryCache.java`
#### Snippet
```java
                classes);
            classes = Caches.addTypesByName(conf, event.getDeletedTypeNames(),
                classes);
        } else {
            classes = addTypes(event.getUpdatedObjectIds(), classes);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryCacheStoreQuery.java`
#### Snippet
```java

        @Override
        public Map getUpdates(StoreQuery q) {
            return _ex.getUpdates(unwrap(q));
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection\>'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryCacheStoreQuery.java`
#### Snippet
```java
        @Override
        public void onTypesChanged(TypesChangedEvent ev) {
            if (_qk.changeInvalidatesQuery(ev.getTypes()))
                abortCaching();
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/ConcurrentDataCache.java`
#### Snippet
```java
        if (log.isWarnEnabled())
            log.warn(_loc.get("cache-class-unpin-all", getName()));
        unpinAll(_cache.getPinnedKeys());
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractDataCache.java`
#### Snippet
```java

        if (event.getPayloadType() == RemoteCommitEvent.PAYLOAD_EXTENTS) {
            removeAllTypeNamesInternal(event.getUpdatedTypeNames());
            removeAllTypeNamesInternal(event.getDeletedTypeNames());
        } else {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractDataCache.java`
#### Snippet
```java
        if (event.getPayloadType() == RemoteCommitEvent.PAYLOAD_EXTENTS) {
            removeAllTypeNamesInternal(event.getUpdatedTypeNames());
            removeAllTypeNamesInternal(event.getDeletedTypeNames());
        } else {
            // drop all the committed OIDs, excepting brand
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractDataCache.java`
#### Snippet
```java
            // new OIDs. brand new OIDs either won't be in
            // the cache, or if they are, will be more up to date
            removeAllInternal(event.getUpdatedObjectIds());
            removeAllInternal(event.getDeletedObjectIds());
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/AbstractDataCache.java`
#### Snippet
```java
            // the cache, or if they are, will be more up to date
            removeAllInternal(event.getUpdatedObjectIds());
            removeAllInternal(event.getDeletedObjectIds());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/PartitionedDataCache.java`
#### Snippet
```java
        ClassLoader ctxLoader = AccessController.doPrivileged(J2DoPrivHelper.getContextClassLoaderAction());
        ClassLoader loader = conf.getClassResolverInstance().getClassLoader(null, ctxLoader);
        _type = (Class<? extends DataCache>) AccessController.doPrivileged(
                J2DoPrivHelper.getForNameAction(value.unalias(type), true, loader));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/abstractstore/AbstractStoreManager.java`
#### Snippet
```java
            && pNewFlushedDeleted.isEmpty() && pDirty.isEmpty()
            && pDeleted.isEmpty())
            return Collections.EMPTY_LIST;

        return flush(pNew, pNewUpdated, pNewFlushedDeleted, pDirty, pDeleted);
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/abstractstore/AbstractStoreManager.java`
#### Snippet
```java
            return VERSION_DIFFERENT;

        int compare = ((Comparable) v1).compareTo((Comparable) v2);
        if (compare < 0)
            return VERSION_EARLIER;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/abstractstore/AbstractStoreManager.java`
#### Snippet
```java
    public Collection<Object> loadAll(Collection<OpenJPAStateManager> sms, PCState state, int load,
        FetchConfiguration fetch, Object edata) {
        return ImplHelper.loadAll(sms, this, state, load, fetch, edata);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryKey.java`
#### Snippet
```java
        _candidateClassName = (String) in.readObject();
        _subclasses = in.readBoolean();
        _accessPathClassNames = (Set<String>) in.readObject();
        _query = (String) in.readObject();
        _ignoreChanges = in.readBoolean();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryKey.java`
#### Snippet
```java
        _query = (String) in.readObject();
        _ignoreChanges = in.readBoolean();
        _params = (Map<Object,Object>) in.readObject();
        _rangeStart = in.readLong();
        _rangeEnd = in.readLong ();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/QueryKey.java`
#### Snippet
```java

            if (v instanceof Collection) {
                Collection<Object> c = (Collection<Object>) v;
                boolean contentsAreDates = false;
                if (c.iterator().hasNext()) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
        for (FieldMetaData field : fields)
            if (field.getDeclaringType() == meta.getDescribedType())
                decs.add(field);
        return (FieldMetaData[]) decs.toArray(new FieldMetaData[decs.size()]);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
            if (field.getDeclaringType() == meta.getDescribedType())
                decs.add(field);
        return (FieldMetaData[]) decs.toArray(new FieldMetaData[decs.size()]);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
            classes = new HashSet();
            for (String arg : args) {
                classes.addAll(Arrays.asList(cap.parseTypes(arg)));
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
    public Set getImportPackages() {
        Set pkgs = new TreeSet();
        pkgs.add(ClassUtil.getPackageName(_type));

        Class superOidClass = null;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
            superOidClass = _meta.getPCSuperclassMetaData().getObjectIdType();
        if (superOidClass != null)
            pkgs.add(ClassUtil.getPackageName(superOidClass));

        pkgs.add("java.io");
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
            pkgs.add(ClassUtil.getPackageName(superOidClass));

        pkgs.add("java.io");
        pkgs.add("java.util");
        Class type;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java

        pkgs.add("java.io");
        pkgs.add("java.util");
        Class type;
        for (FieldMetaData field : _fields) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
            if (type != byte[].class && type != char[].class
                    && !type.getName().startsWith("java.sql.")) {
                pkgs.add(ClassUtil.getPackageName(type));
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStore.java`
#### Snippet
```java
        for (Object o : datas) {
            ObjectData data = (ObjectData) o;
            m.put(data.getId(), data);
        }
        _metaOidMaps.put(meta, m);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStore.java`
#### Snippet
```java
            m.put(data.getId(), data);
        }
        _metaOidMaps.put(meta, m);
        return m;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStore.java`
#### Snippet
```java
                for (ObjectData data : updates) {
                    ClassMetaData meta = getLeastDerived(data.getMetaData());
                    getMap(meta).put(data.getId(), data);
                    dirty.add(meta);
                }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStore.java`
#### Snippet
```java
                    ClassMetaData meta = getLeastDerived(data.getMetaData());
                    getMap(meta).put(data.getId(), data);
                    dirty.add(meta);
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStore.java`
#### Snippet
```java
                    ClassMetaData meta = getLeastDerived(data.getMetaData());
                    getMap(meta).remove(data.getId());
                    dirty.add(meta);
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStore.java`
#### Snippet
```java
        meta = getLeastDerived(meta);
        Collection vals = getMap(meta).values();
        return (ObjectData[]) vals.toArray(new ObjectData[vals.size()]);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.reflect.Constructor\>' to 'java.lang.reflect.Constructor'
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/PrototypeFactory.java`
#### Snippet
```java
                    }
                    if (match) {
                        return (Constructor<? extends T>)cons;
                    }
            }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java

    @Override
    protected Collection getUnsupportedOptions() {
        Collection c = super.getUnsupportedOptions();

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java

        // and add some that we don't support but the abstract store does
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_RELATION);
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_COLLECTION_RELATION);
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_MAP_RELATION);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java
        // and add some that we don't support but the abstract store does
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_RELATION);
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_COLLECTION_RELATION);
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_MAP_RELATION);
        return c;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_RELATION);
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_COLLECTION_RELATION);
        c.add(OpenJPAConfiguration.OPTION_EMBEDDED_MAP_RELATION);
        return c;
    }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java

    @Override
    protected Collection flush(Collection<OpenJPAStateManager> pNew, Collection<OpenJPAStateManager> pNewUpdated,
        Collection<OpenJPAStateManager> pNewFlushedDeleted, Collection<OpenJPAStateManager> pDirty, Collection<OpenJPAStateManager> pDeleted) {
        // we don't support incremental flushing, so pNewUpdated and
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java
            // opt lock violation
            if (data == null || !data.getVersion().equals(sm.getVersion())) {
                exceps.add(new OptimisticException
                        (sm.getManagedInstance()));
                continue;
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java
            Class c = data.getMetaData().getDescribedType();
            if (c != candidate && (!subclasses
                    || !candidate.isAssignableFrom(c)))
                continue;

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStoreManager.java`
#### Snippet
```java
            // if it is present; this is particularly important in systems
            // where a trip to the store can be expensive.
            pcs.add(ctx.find(data.getId(), fetch, null, data, 0));
        }
        return new ListResultObjectProvider(pcs);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
                Collection c2 = new ArrayList();
                for (Object value : c) {
                    c2.add(toNestedStorable(fmd.getElement(), value, ctx));
                }
                return c2;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
                c = new ArrayList();
                for (int i = 0, len = Array.getLength(val); i < len; i++)
                    c.add(toNestedStorable(fmd.getElement(), Array.get(val, i),
                        ctx));
                return c;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
                for (Object o : m.entrySet()) {
                    Map.Entry e = (Map.Entry) o;
                    m2.put(toNestedStorable(fmd.getKey(), e.getKey(), ctx),
                            toNestedStorable(fmd.getElement(), e.getValue(), ctx));
                }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
                // converting it to the right type from its stored form
                for (Object value : c)
                    c2.add(toNestedLoadable(sm, fmd.getElement(), value,
                            fetch));
                return c2;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
                for (Object o : m.entrySet()) {
                    Map.Entry e = (Map.Entry) o;
                    m2.put(toNestedLoadable(sm, fmd.getKey(), e.getKey(), fetch),
                            toNestedLoadable(sm, fmd.getElement(), e.getValue(),
                                    fetch));
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
                case JavaTypes.COLLECTION:
                case JavaTypes.ARRAY:
                    data.setField(i, new ArrayList((Collection) val));
                    break;
                case JavaTypes.MAP:
```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/ObjectData.java`
#### Snippet
```java
                    break;
                case JavaTypes.MAP:
                    data.setField(i, new HashMap((Map) val));
                    break;
                default:
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
            if ("object".equals(qName)) {
                // add the object to our results
                _extent.add(_object);
            }
            else if ("field".equals(qName)) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                val = fromXMLString(_fmd.getElement().getTypeCode(),
                        _fmd.getElement().getTypeMetaData(), _buf.toString());
                ((Collection) _fieldVal).add(val);
            }
            else if ("key".equals(qName)) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                    _fmd.getElement().getTypeMetaData(), _buf.toString());
                Map map = (Map) _fieldVal;
                map.put(_keyVal, val);
            }

```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                    Class idClass = classForName(str.substring(0, idx));
                    String idStr = XMLEncoder.decode(str.substring(idx + 1));
                    Constructor cons = idClass.getConstructor(ARGS);
                    return cons.newInstance(new Object[]{ idStr });

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: '((Map) value)' has raw type, so result of entrySet is erased
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObjectFormatter.java`
#### Snippet
```java
                        break;
                    }
                    Set<Map.Entry> entries = ((Map) value).entrySet();
                    JSONObject.KVMap map = new JSONObject.KVMap();
                    root.set(fmd.getName(), map);
```

### UNCHECKED_WARNING
Unchecked cast: 'javax.swing.table.TableModel' to 'jpa.tools.swing.EntityDataModel'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTableView.java`
#### Snippet
```java

    public EntityDataModel<T> getDataModel() {
        return (EntityDataModel<T>)_table.getModel();
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'. Reason: '((Map)value)' has raw type, so result of entrySet is erased
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/XMLFormatter.java`
#### Snippet
```java
                        break;
                    }
                    Set<Map.Entry<?,?>> entries = ((Map)value).entrySet();
                    boolean basicKey   = fmd.getElement().getTypeMetaData() == null;
                    boolean basicValue = fmd.getValue().getTypeMetaData() == null;
```

### UNCHECKED_WARNING
Unchecked call to 'addElement(E)' as a member of raw type 'javax.swing.DefaultComboBoxModel'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SourceCodeViewer.java`
#### Snippet
```java
    public void addPage(String anchor, URI url) {
        _anchors.put(anchor, url);
        ((DefaultComboBoxModel)_bookmarks.getModel()).addElement(anchor);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'JComboBox(ComboBoxModel)' as a member of raw type 'javax.swing.JComboBox'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/SourceCodeViewer.java`
#### Snippet
```java

        DefaultComboBoxModel model = new DefaultComboBoxModel();
        _bookmarks = new JComboBox(model);
        _bookmarks.setEditable(false);

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java

    public static Map<String,String> getMap(Map<String,Object> props, String key) {
        return getMap(props, key, Collections.EMPTY_MAP);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java

    public static Map<String,String> getMap(Map<String,Object> props, String key) {
        return getMap(props, key, Collections.EMPTY_MAP);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java

    public static List<String> getStringList(Map<String,Object> props, String key) {
        return getStringList(props, key, Collections.EMPTY_LIST);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java

    public static List<String> getStringList(Map<String,Object> props, String key) {
        return getStringList(props, key, Collections.EMPTY_LIST);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        Class oidType = _meta.getObjectIdType();
        try {
            oidType.getConstructor(new Class[]{ Class.class, String.class });
            return Boolean.TRUE;
        } catch (Throwable t) {
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        }
        try {
            oidType.getConstructor(new Class[]{ String.class });
            return Boolean.FALSE;
        } catch (Throwable t) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        for (Class aClass : classes) {
            try {
                auxEnhancers.add(AccessController.doPrivileged(
                        J2DoPrivHelper.newInstanceAction(aClass)));
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.security.PrivilegedExceptionAction' to 'java.security.PrivilegedExceptionAction'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            try {
                auxEnhancers.add(AccessController.doPrivileged(
                        J2DoPrivHelper.newInstanceAction(aClass)));
            }
            catch (Throwable t) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            try {
                auxEnhancers.add(AccessController.doPrivileged(
                        J2DoPrivHelper.newInstanceAction(aClass)));
            }
            catch (Throwable t) {
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            }
        }
        _auxEnhancers = (AuxiliaryEnhancer[]) auxEnhancers.toArray
        (new AuxiliaryEnhancer[auxEnhancers.size()]);

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        List plist = new ArrayList(4);
        if (haspc)
            plist.add(PCTYPE);
        plist.add(int.class);
        if (!get || curValue)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        if (haspc)
            plist.add(PCTYPE);
        plist.add(int.class);
        if (!get || curValue)
            plist.add(type);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        plist.add(int.class);
        if (!get || curValue)
            plist.add(type);
        if (!get && curValue) {
            plist.add(type);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            plist.add(type);
        if (!get && curValue) {
            plist.add(type);
            plist.add(int.class);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        if (!get && curValue) {
            plist.add(type);
            plist.add(int.class);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        // use reflection to return the right method
        String name = prefix + typeName + "Field";
        Class[] params = (Class[]) plist.toArray(new Class[plist.size()]);

        try {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                    && !field.getName().startsWith(PRE)
                    && _meta.getDeclaredField(field.getName()) == null)
                unmgd.add(field);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        if (_violations == null)
            _violations = new HashSet();
        _violations.add(_loc.get(key, args));
        _fail |= fatal;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            classes = new HashSet();
            for (String arg : args) {
                classes.addAll(Arrays.asList(cap.parseTypes(arg)));
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            }
            else if (status == ENHANCE_AWARE) {
                persAwareClasses.add(o);
                enhancer.record();
            }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        if (_backingFields == null)
            _backingFields = new HashMap();
        _backingFields.put(method.getName(), field.getName());

        if (_attrsToFields == null)
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        if (_attrsToFields == null)
            _attrsToFields = new HashMap();
        _attrsToFields.put(fmd.getName(), field.getName());

        if (_fieldsToAttrs == null)
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        if (_fieldsToAttrs == null)
            _fieldsToAttrs = new HashMap();
        _fieldsToAttrs.put(field.getName(), fmd.getName());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'javax.swing.table.TableModel' to 'jpa.tools.swing.EntityDataModel'
in `openjpa-examples/openbooks/src/main/java/openbook/client/BuyBookPage.java`
#### Snippet
```java
            if (col == -1)
                return;
            EntityDataModel<Book> model = (EntityDataModel<Book>) table.getModel();
            Book book = model.getRow(row);
            showBookDetails(book);
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
                prev.next = node.next;
            else {
                _graph.set(idx, node.next);
                // trim to size
                while (!_graph.isEmpty() && _graph.get(idx) == null
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
                if (!seen.get(sidx)) {
                    seen.set(sidx);
                    queue.add(n);
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
                idx = (n.forward) ? n.join.getIndex2() : n.join.getIndex1();
                if (!seen.get(idx)) {
                    sorted.add((n.forward) ? n.join : n.join.reverse());
                    seen.set(idx);
                }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
                    if (!seen.get(sidx)) {
                        seen.set(sidx);
                        queue.add(n);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
        for (int i = 0; i < copy._graph.size(); i++) {
            if (copy._graph.get(i) == null)
                _graph.add(null);
            else
                _graph.add(((Node) copy._graph.get(i)).clone());
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
                _graph.add(null);
            else
                _graph.add(((Node) copy._graph.get(i)).clone());
        }
        _size = copy._size;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
        int size = Math.max(join.getIndex1(), join.getIndex2()) + 1;
        while (_graph.size() < size)
            _graph.add(null);

        Node node = (Node) _graph.get(join.getIndex1());
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
        Node node = (Node) _graph.get(join.getIndex1());
        if (node == null)
            _graph.set(join.getIndex1(), new Node(join, true));
        else {
            while (node.next != null)
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
        node = (Node) _graph.get(join.getIndex2());
        if (node == null)
            _graph.set(join.getIndex2(), new Node(join, false));
        else {
            while (node.next != null)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-examples/openbooks/src/main/java/openbook/server/OpenBookServiceImpl.java`
#### Snippet
```java
        EntityManager em = begin();
        if (config == null) {
            config = Collections.EMPTY_MAP;
        }
        int nCustomer  = PropertyHelper.getInteger(config, "openbook.Customer.Count",  CUSTOMER_COUNT);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.lib.util.ReferenceHashSet' to 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
    // weak set of connections we've already executed lock mode sql on
    private final Collection _seenConnections = Collections.synchronizedSet(
            new ReferenceHashSet(AbstractReferenceMap.ReferenceStrength.WEAK));

    public boolean disableRetainUpdateLocksSQL=false;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
        // if we haven't already done so, initialize the lock mode of the
        // connection
        if (_seenConnections.add(conn)) {
            if (lockModeEnabled) {
                String sql = "SET LOCK MODE TO WAIT";
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MariaDBDictionary.java`
#### Snippet
```java
    protected Collection<String> getSelectTableAliases(Select sel) {
        Set<String> result = new HashSet<>();
        List<String> selects = sel.getIdentifierAliases();
        for (String s : selects) {
            String tableAlias = s.substring(0, s.indexOf('.'));
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractResult.java`
#### Snippet
```java
            setEagerMap(map);
        }
        map.put(key, res);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MySQLDictionary.java`
#### Snippet
```java
    protected Collection<String> getSelectTableAliases(Select sel) {
        Set<String> result = new HashSet<>();
        List<String> selects = sel.getIdentifierAliases();
        for (String s : selects) {
            String tableAlias = s.substring(0, s.indexOf('.'));
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    _cols = new ArrayList();
                    while (_cols.size() < _params.size())
                        _cols.add(null);
                }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                }

                _params.add(o);
                if (userParam != null) {
                    Object param = userParam;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    if (userParam instanceof CollectionParam)
                        param = ((CollectionParam) userParam).clone();
                    _userParams.add(param);
                    if (_userIndex == null)
                        _userIndex = new ArrayList();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                        _userIndex = new ArrayList();
                    int index = _params.size()-1;
                    _userIndex.add(index);
                    _userIndex.add(param);
                }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    int index = _params.size()-1;
                    _userIndex.add(index);
                    _userIndex.add(param);
                }
                else
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                }
                else
                    _userParams.add(o);
                if (_cols != null)
                    _cols.add(col);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    _userParams.add(o);
                if (_cols != null)
                    _cols.add(col);
            } else {
                if (type == String.class) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    _subsels = new ArrayList(buf._subsels.size());
                for (int i = 0; i < buf._subsels.size(); i++)
                    _subsels.add(((Subselect) buf._subsels.get(i)).
                        clone(sqlIndex, paramIndex));
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                _cols = new ArrayList();
                while (_cols.size() < _params.size())
                    _cols.add(null);
            }

```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java

            if (paramIndex == _params.size()) {
                _params.addAll(buf._params);
                if (buf._userParams != null) {
                    if (_userParams == null)
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(int, Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    if (_userParams == null)
                        _userParams = new ArrayList();
                   _userParams.addAll(paramIndex, buf._userParams);
                }
                if (buf._userIndex != null) {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    if (_userIndex == null)
                        _userIndex = new ArrayList();
                    _userIndex.addAll(buf._userIndex);
                }
                if (buf._cols != null)
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                }
                if (buf._cols != null)
                    _cols.addAll(buf._cols);
                else if (_cols != null)
                    while (_cols.size() < _params.size())
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                else if (_cols != null)
                    while (_cols.size() < _params.size())
                        _cols.add(null);
            } else {
                _params.addAll(paramIndex, buf._params);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(int, Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                        _cols.add(null);
            } else {
                _params.addAll(paramIndex, buf._params);
                if ( buf._userParams != null) {
                    if (_userParams == null)
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(int, Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    if (_userParams == null)
                        _userParams = new ArrayList();
                    _userParams.addAll(paramIndex, buf._userParams);
                }
                if (buf._userIndex != null) {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                     if (_userIndex == null)
                         _userIndex = new ArrayList();
                     _userIndex.addAll(buf._userIndex);
                }
                if (buf._cols != null)
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(int, Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                }
                if (buf._cols != null)
                    _cols.addAll(paramIndex, buf._cols);
                else if (_cols != null)
                    while (_cols.size() < _params.size())
```

### UNCHECKED_WARNING
Unchecked call to 'add(int, E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                else if (_cols != null)
                    while (_cols.size() < _params.size())
                        _cols.add(paramIndex, null);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
            	Object previousParam = (i > 0) ? _userIndex.get(i-1) : null;
            	if ( param != previousParam) {
            		_userIndex.set(i, _userParams.indexOf(param));
            	}else{
            		//if there are multiple parameters for the in clause or the combined PK field,
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                	for(Object next : _userParams){
                        if (next == param && userParamindex > previousUserIndex) {
                            _userIndex.set(i, userParamindex);
                            break;
                      }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
        if (_subsels == null)
            _subsels = new ArrayList(2);
        _subsels.add(sub);
        return this;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'compare(T, T)' as a member of raw type 'java.util.Comparator'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/MergedResult.java`
#### Snippet
```java
            if (_status[i] != CURRENT)
                continue;
            if (least == -1 || _comp.compare(_order[i], orderVal) < 0) {
                least = i;
                orderVal = _order[i];
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/BooleanRepresentationFactory.java`
#### Snippet
```java
            try {
                Class<? extends BooleanRepresentation> booleanRepresentationClass
                        = (Class<? extends BooleanRepresentation>) cl.loadClass(booleanRepresentationKey);
                booleanRepresentation = booleanRepresentationClass.newInstance();
            }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            if (_selectAs == null)
                _selectAs = new HashMap((int) (5 * 1.33 + 1));
            _selectAs.put(id, as);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java

            int idx;
            if (_aliases.put(id, alias) != null)
                idx = _ids.indexOf(id);
            else {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                idx = _ids.indexOf(id);
            else {
                _ids.add(id);
                idx = _ids.size() - 1;

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                    if (_idents == null)
                        _idents = new ArrayList(3);
                    _idents.add(id);
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'push(E)' as a member of raw type 'java.util.Stack'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        if (_preJoins != null && !_preJoins.isEmpty()) {
            sel._preJoins = new Stack();
            sel._preJoins.push(((SelectJoins) _preJoins.peek()).
                clone(sel));
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                if (_ordered == null)
                    _ordered = new ArrayList(5);
                _ordered.add(id);
            }
            if (aliasOrder) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        if (_parent != null) {
            if (_parent._subsels == null)
                _parent._subsels = new ArrayList(2);
            _parent._subsels.add(this);
            if (_parent._joinSyntax == JoinSyntaxes.SYNTAX_SQL92)
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        public void setAlias(int idx, Object alias) {
            Object id = _ids.get(idx);
            _aliases.put(id, alias);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                res.setEagerMap(eager);
            }
            eager.put(entry.getKey(), eres);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        if (_eagerKeys == null)
            _eagerKeys = new HashSet();
        _eagerKeys.add(key);

        SelectExecutor sel;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        if (_eager == null)
            _eager = new HashMap();
        _eager.put(toEagerKey(key, getJoins(null, false)), sel);
        return sel;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'push(E)' as a member of raw type 'java.util.Stack'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                if (_preJoins == null)
                    _preJoins = new Stack();
                _preJoins.push(joins);
            }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                setEagerMap(map);
            }
            map.put(SelectImpl.toEagerKey(key, getJoins(null)), res);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            sel._schemaAlias = _schemaAlias;
            if (_aliases != null)
                sel._aliases = new HashMap(_aliases);
            if (_tables != null)
                sel._tables = new TreeMap(_tables);
```

### UNCHECKED_WARNING
Unchecked call to 'TreeMap(SortedMap)' as a member of raw type 'java.util.TreeMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                sel._aliases = new HashMap(_aliases);
            if (_tables != null)
                sel._tables = new TreeMap(_tables);
            if (_joins != null)
                sel._joins = _joins.clone(sel);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            }
            if (_subsels != null) {
                sel._subsels = new ArrayList(_subsels.size());
                SelectImpl sub, selSub;
                for (int j = 0; j < _subsels.size(); j++) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        List idxs = new ArrayList(_ordered.size());
        for (Object o : _ordered) {
            idxs.add(_selects.indexOf(o));
        }
        return idxs;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        if (_aliases == null)
            _aliases = new HashMap();
        _aliases.put(key, alias);

        String tableString = _dict.getFullName(table, false) + " "
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        if (_tables == null)
            _tables = new TreeMap();
        _tables.put(alias, tableString);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java

            _grouping.append(sql);
            _grouped.add(sql);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        public void addAll(Selects sels) {
            if (_ids == null && sels._ids != null)
                _ids = new ArrayList(sels._ids);
            else if (sels._ids != null)
                _ids.addAll(sels._ids);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                _ids = new ArrayList(sels._ids);
            else if (sels._ids != null)
                _ids.addAll(sels._ids);

            if (_idents == null && sels._idents != null)
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java

            if (_idents == null && sels._idents != null)
                _idents = new ArrayList(sels._idents);
            else if (sels._idents != null)
                _idents.addAll(sels._idents);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                _idents = new ArrayList(sels._idents);
            else if (sels._idents != null)
                _idents.addAll(sels._idents);

            if (_aliases == null && sels._aliases != null)
```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java

            if (_aliases == null && sels._aliases != null)
                _aliases = new HashMap(sels._aliases);
            else if (sels._aliases != null)
                _aliases.putAll(sels._aliases);
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                _aliases = new HashMap(sels._aliases);
            else if (sels._aliases != null)
                _aliases.putAll(sels._aliases);

            if (_selectAs == null && sels._selectAs != null)
```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java

            if (_selectAs == null && sels._selectAs != null)
                _selectAs = new HashMap(sels._selectAs);
            else if (sels._selectAs != null)
                _selectAs.putAll(sels._selectAs);
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                _selectAs = new HashMap(sels._selectAs);
            else if (sels._selectAs != null)
                _selectAs.putAll(sels._selectAs);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
         */
        public void insertAlias(int idx, Object id, Object alias) {
            _aliases.put(id, alias);
            if (idx >= 0)
                _ids.add(idx, id);
```

### UNCHECKED_WARNING
Unchecked call to 'add(int, E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            _aliases.put(id, alias);
            if (idx >= 0)
                _ids.add(idx, id);
            else
                _ids.add(_ids.size() + idx, id);
```

### UNCHECKED_WARNING
Unchecked call to 'add(int, E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                _ids.add(idx, id);
            else
                _ids.add(_ids.size() + idx, id);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            if (_ordered == null)
                _ordered = new ArrayList(5);
            _ordered.add(selAs == null ? sql : selAs);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'push(E)' as a member of raw type 'java.util.Stack'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            if (_preJoins == null)
                _preJoins = new Stack();
            _preJoins.push(pj);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
            seq = getAutoGenSeqName(cols[i]);
            if (sequenceExists(table.getSchemaIdentifier().getName(), seq, group))
                seqs.add("DROP SEQUENCE " + seq);
            seqs.add("CREATE SEQUENCE " + seq);

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
            if (sequenceExists(table.getSchemaIdentifier().getName(), seq, group))
                seqs.add("DROP SEQUENCE " + seq);
            seqs.add("CREATE SEQUENCE " + seq);

            if (openjpa3GeneratedKeyNames)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
            // END;

            seqs.add("CREATE TRIGGER " + trig
                + " ON " + toDBName(table.getIdentifier())
                + " BEFORE INSERT REFERENCING NEW " + toDBName(cols[i].getIdentifier())
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/LogicalUnion.java`
#### Snippet
```java
            if (!(o1 instanceof Object[])) {
                if (!(o2 instanceof Object[])) {
                    cmp = ((Comparable) o1).compareTo(o2);
                    return (_desc.get(0)) ? -cmp : cmp;
                }
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/LogicalUnion.java`
#### Snippet
```java
                }

                cmp = ((Comparable) o1).compareTo(((Object[]) o2)[0]);
                if (cmp != 0)
                    return (_desc.get(0)) ? -cmp : cmp;
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/LogicalUnion.java`
#### Snippet
```java

            if (!(o2 instanceof Object[])) {
                cmp = ((Comparable) ((Object[]) o1)[0]).compareTo(o2);
                if (cmp != 0)
                    return (_desc.get(0)) ? -cmp : cmp;
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/LogicalUnion.java`
#### Snippet
```java
            Object[] a2 = (Object[]) o2;
            for (int i = 0; i < a1.length; i++) {
                cmp = ((Comparable) a1[i]).compareTo(a2[i]);
                if (cmp != 0)
                    return (_desc.get(i)) ? -cmp : cmp;
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/MappingRepositoryValue.java`
#### Snippet
```java
                AccessController.doPrivileged(
                    J2DoPrivHelper.getClassLoaderAction(type)));
            return cls.getConstructor(new Class[]{ JDBCConfiguration.class }).
                newInstance(new Object[]{ conf });
        } catch (RuntimeException e) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MetaDataPlusMappingFactory.java`
#### Snippet
```java
        if ((mode & MODE_MAPPING) != 0)
            store &= _map.store(metas, queries, seqs, mode & ~MODE_META,
                output);
        return store;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingDefaultsImpl.java`
#### Snippet
```java
            _fieldMap.remove(valueType);
        else
            _fieldMap.put(valueType, handlerType);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingDefaultsImpl.java`
#### Snippet
```java
        Properties props = Configurations.parseProperties(fieldMapString);
        if (props != null)
            _fieldMap.putAll(props);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            List pkList = new ArrayList();
            while (pks != null && pks.next())
                pkList.add(newPrimaryKey(pks));
            return (PrimaryKey[]) pkList.toArray
                (new PrimaryKey[pkList.size()]);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            while (pks != null && pks.next())
                pkList.add(newPrimaryKey(pks));
            return (PrimaryKey[]) pkList.toArray
                (new PrimaryKey[pkList.size()]);
        } finally {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            List columnList = new ArrayList();
            while (cols != null && cols.next())
                columnList.add(newColumn(cols));
            return (Column[]) columnList.toArray
                (new Column[columnList.size()]);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            while (cols != null && cols.next())
                columnList.add(newColumn(cols));
            return (Column[]) columnList.toArray
                (new Column[columnList.size()]);
        } finally {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                pk.setTableIdentifier(tableName);
                pk.setColumnIdentifier(fromDBName(pks.getString("COLUMN_NAME"), DBIdentifierType.COLUMN));
                pkList.add(pk);
            }
            return (PrimaryKey[]) pkList.toArray
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                pkList.add(pk);
            }
            return (PrimaryKey[]) pkList.toArray
                (new PrimaryKey[pkList.size()]);
        } finally {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
    protected SQLBuffer getDeleteTargets(Select sel) {
      SQLBuffer deleteTargets = new SQLBuffer(this);
      Collection<String> aliases = sel.getTableAliases();
      // Assumes aliases are of the form "TABLENAME t0"
      // or "\"TABLE NAME\" t0"
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        List seqList = new ArrayList();
        while (rs != null && rs.next())
            seqList.add(newSequence(rs));
        return (Sequence[]) seqList.toArray(new Sequence[seqList.size()]);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        while (rs != null && rs.next())
            seqList.add(newSequence(rs));
        return (Sequence[]) seqList.toArray(new Sequence[seqList.size()]);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java

    protected Collection<String> getSelectTableAliases(Select sel) {
        return sel.getTableAliases();
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map\>'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        if (map == null)
            return rs.getObject(column);
        return rs.getObject(column, map);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            List indexList = new ArrayList();
            while (indexes != null && indexes.next())
                indexList.add(newIndex(indexes));
            return (Index[]) indexList.toArray(new Index[indexList.size()]);
        } finally {
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            while (indexes != null && indexes.next())
                indexList.add(newIndex(indexes));
            return (Index[]) indexList.toArray(new Index[indexList.size()]);
        } finally {
            if (indexes != null)
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
            Set pkgs = super.getImportPackages();
            if (_appid != null)
                pkgs.addAll(_appid.getImportPackages());
            return pkgs;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        if (_detachable)
            mapping.setDetachable(true);
        _tables.put(table, mapping);
        return mapping;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java

            for (int i = 0; i < keywords.length; i += 2)
                _javaKeywords.put(keywords[i], keywords[i + 1]);
        } catch (IOException ioe) {
            throw new InternalException(ioe);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        public Set getImportPackages() {
            Set pkgs = super.getImportPackages();
            pkgs.add("jakarta.persistence");
            return pkgs;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
            if (output == null) {
                gen.writeCode();
                written.add(gen.getFile());
                if (aid != null && !aid.isInnerClass())
                    aid.record();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
                if (_abandonedFieldNames == null)
                    _abandonedFieldNames = new HashSet();
                _abandonedFieldNames.add(dec.getDescribedType().getName()
                    + "." + name);
                if (newName == null)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        mdf.setStoreDirectory(_dir);
        mdf.store(mappings, new QueryMetaData[0], new SequenceMetaData[0],
            MODE_META | MODE_MAPPING | MODE_ANN_MAPPING, output);
        _annos = output;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
                    if (subs == null)
                        subs = new HashSet();
                    subs.add(table);
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
                for (Column col : cols)
                    if (col.getRefCount() == 0)
                        unmappedCols.add(col);

                if (unmappedCols.size() == cols.length) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
            mdf.setStoreMode(MetaDataFactory.STORE_PER_CLASS);
        mdf.store(mappings, new QueryMetaData[0], new SequenceMetaData[0],
            MODE_META | MODE_MAPPING, output);

        Set files = new TreeSet();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        for (ClassMapping mapping : mappings)
            if (mapping.getSourceFile() != null)
                files.add(mapping.getSourceFile());
        return files;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
            synchronized (this) {
                Collection values = _results.values();
                return (QueryResultMapping[]) values.toArray(new QueryResultMapping[values.size()]);
            }
        } else {
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingRepository.java`
#### Snippet
```java
        } else {
            Collection values = _results.values();
            return (QueryResultMapping[]) values.toArray(new QueryResultMapping[values.size()]);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'TreeSet(Collection)' as a member of raw type 'java.util.TreeSet'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/PropertiesReverseCustomizer.java`
#### Snippet
```java
    public void setConfiguration(Properties props) {
        _props = props;
        _unaccessed = new TreeSet(props.keySet());
    }

```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EnumValueHandler.java`
#### Snippet
```java
        // that returns an array of all the enum values
        try {
            Method m = vm.getType().getMethod("values", (Class[]) null);
            _vals = (Enum[]) m.invoke(null, (Object[]) null);
        } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked method 'valueOf(Class, String)' invocation
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EnumValueHandler.java`
#### Snippet
```java
        if (_ordinal)
            return _vals[((Number) val).intValue()];
        return Enum.valueOf(vm.getType(), (String) val);
    }
}
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Collection'. Reason: 'mapObj' has raw type, so result of entrySet is erased
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java
            return false;
        Map mapObj = (Map)valsm.fetchObject(fm.getIndex());
        Collection<Map.Entry> entrySets = mapObj.entrySet();
        boolean found = false;
        for (Map.Entry entry : entrySets) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MapTableFieldStrategy.java`
#### Snippet
```java
                key = loadKey(sm, store, fetch, res[0], joins[0]);
                val = loadValue(sm, store, fetch, res[1], joins[1]);
                map.put(key, val);
            }
            sm.storeObject(field.getIndex(), map);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                    }
                    for (Column fieldCol : fieldCols) {
                        cols.add(fieldCol);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                    }
                }
                _cols = (Column[]) cols.toArray(new Column[cols.size()]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                for (ClassMapping sub : subs)
                    if (isSubJoinable(sub))
                        joinable.add(sub);
                _joinSubMaps = (ClassMapping[]) joinable.toArray
                    (new ClassMapping[joinable.size()]);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                    if (isSubJoinable(sub))
                        joinable.add(sub);
                _joinSubMaps = (ClassMapping[]) joinable.toArray
                    (new ClassMapping[joinable.size()]);
            }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                Set independent = new LinkedHashSet(size);
                if (isMapped())
                    independent.add(this);
                independent.addAll(Arrays.asList(subs));

```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                if (isMapped())
                    independent.add(this);
                independent.addAll(Arrays.asList(subs));

                // remove all mappings that have a superclass mapping in the set
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                        if (clear == null)
                            clear = new ArrayList(independent.size() - 1);
                        clear.add(map);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked call to 'removeAll(Collection)' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                }
                if (clear != null)
                    independent.removeAll(clear);

                _assignMaps = (ClassMapping[]) independent.toArray
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
                    independent.removeAll(clear);

                _assignMaps = (ClassMapping[]) independent.toArray
                    (new ClassMapping[independent.size()]);
            }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
        if (join == null || (join.getFieldIndex() != -1
            && getField(join.getFieldIndex()).getPrimaryKeyIndex() == -1))
            _joinables.put(col, joinable);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LobFieldStrategy.java`
#### Snippet
```java
            field.setTypeCode(JavaTypes.INT);
        } else {
            if (owner.getType().isAssignableFrom(InputStream.class)) {
                isBlob = true;
                fieldType = Types.BLOB;
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LobFieldStrategy.java`
#### Snippet
```java
                isBlob = true;
                fieldType = Types.BLOB;
            } else if (owner.getType().isAssignableFrom(Reader.class)) {
                isBlob = false;
                fieldType = Types.CLOB;
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ColumnVersionStrategy.java`
#### Snippet
```java
        }

        return ((Comparable) v1).compareTo(v2);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/SubclassJoinDiscriminatorStrategy.java`
#### Snippet
```java
            // possible that a sibling class cols were already discovered, in
            // which case we can skip this sub
            if (!derived.isAssignableFrom(sub.getDescribedType()))
                continue;

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'map' has raw type, so result of entrySet is erased
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationRelationMapTableFieldStrategy.java`
#### Snippet
```java
                Object mval = map.get(mkey);
                if (mval == null) {
                    Set<Map.Entry> entries = map.entrySet();
                    for (Map.Entry entry : entries) {
                        if (entry.getKey().equals(mkey))
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'map' has raw type, so result of entrySet is erased
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationRelationMapTableFieldStrategy.java`
#### Snippet
```java
                Object mval = map.get(mkey);
                if (mval == null) {
                    Set<Map.Entry> entries = map.entrySet();
                    for (Map.Entry entry : entries) {
                        if (entry.getKey().equals(mkey))
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'map' has raw type, so result of entrySet is erased
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationRelationMapTableFieldStrategy.java`
#### Snippet
```java
                    Object mval = map.get(mkey);
                    if (mval == null) {
                        Set<Map.Entry> entries = map.entrySet();
                        for (Map.Entry entry : entries) {
                            if (entry.getKey().equals(mkey))
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Map', required 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/AbstractVersionStrategy.java`
#### Snippet
```java

    @Override
    public Map getBulkUpdateValues() {
        return Collections.EMPTY_MAP;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'addFirst(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ElementEmbedValueHandler.java`
#### Snippet
```java
            _nullIdx = cols.indexOf(nullInd);
            if (_nullIdx == -1) {
                cols.addFirst(nullInd);
                args.addFirst(null);
                _nullIdx = 0;
```

### UNCHECKED_WARNING
Unchecked call to 'addFirst(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ElementEmbedValueHandler.java`
#### Snippet
```java
            if (_nullIdx == -1) {
                cols.addFirst(nullInd);
                args.addFirst(null);
                _nullIdx = 0;
                _synthetic = true;
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ElementEmbedValueHandler.java`
#### Snippet
```java

        _vm = vm;
        _cols = (Column[]) cols.toArray(new Column[cols.size()]);
        _args = args.toArray();
        return _cols;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
        if (cols.size() == 1) {
            fm.setMapsIdCols(true);
            fm.getValueInfo().setMapsIdColumns(cols);
            return;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
            if (isReferenceField(fm, refColName)) {
                List colList1 = new ArrayList();
                colList1.add(col);
                fm.setMapsIdCols(true);
                fm.getValueInfo().setMapsIdColumns(colList1);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
                colList1.add(col);
                fm.setMapsIdCols(true);
                fm.getValueInfo().setMapsIdColumns(colList1);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
        super.map(vm, name, io, adapt, cols, args);

        vm.setColumns((Column[]) cols.toArray(new Column[cols.size()]));
        _args = args.toArray();
        return vm.getColumns();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
            if (fm != null) {
                List colList1 = new ArrayList();
                colList1.add(col);
                fm.setMapsIdCols(true);
                fm.getValueInfo().setMapsIdColumns(colList1);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ObjectIdValueHandler.java`
#### Snippet
```java
                colList1.add(col);
                fm.setMapsIdCols(true);
                fm.getValueInfo().setMapsIdColumns(colList1);
            }
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StoreCollectionFieldStrategy.java`
#### Snippet
```java
        if (field.getTypeCode() == JavaTypes.COLLECTION)
            return (Collection) val;
        return JavaTypes.toList(val, field.getElement().getType(), false);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StoreCollectionFieldStrategy.java`
#### Snippet
```java
                else
                    coll = sm.newProxy(field.getIndex());
                rels.put(oid, coll);
            }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StoreCollectionFieldStrategy.java`
#### Snippet
```java
     */
    protected void add(JDBCStore store, Object coll, Object obj) {
        ((Collection<Object>) coll).add(obj);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationMapTableFieldStrategy.java`
#### Snippet
```java
    protected void add(JDBCStore store, Object coll, Object obj) {
        if (obj != null)
            ((Map) coll).put(deriveKey(store, obj), obj);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationMapInverseKeyFieldStrategy.java`
#### Snippet
```java
    protected void add(JDBCStore store, Object coll, Object obj) {
        if (obj != null)
            ((Map) coll).put(deriveKey(store, obj), obj);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'AbstractLRSProxyMap(Class, Class)' as a member of raw type 'org.apache.openjpa.util.AbstractLRSProxyMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LRSProxyMap.java`
#### Snippet
```java

    public LRSProxyMap(LRSMapFieldStrategy strat) {
        super(strat.getFieldMapping().getKey().getDeclaredType(),
            strat.getFieldMapping().getElement().getDeclaredType());
        _strat = strat;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/LRSProxyMap.java`
#### Snippet
```java
            res = union.execute(store, fetch);
            while (res.next())
                keys.add(_strat.loadKey(sm, store, fetch, res,
                    resJoins[res.indexOf()]));
            return keys;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerCollectionTableFieldStrategy.java`
#### Snippet
```java
            if (esm != null) {
                if (!esm.isEmbedded())
                    rels.add(esm);
                else
                    getRelations(esm, rels, ctx);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/HandlerCollectionTableFieldStrategy.java`
#### Snippet
```java
        Collection coll;
        if (field.getTypeCode() == JavaTypes.ARRAY)
            coll = JavaTypes.toList(vals, field.getElement().getType(),
                false);
        else
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
        List rvals = new ArrayList();
        if (rval != null)
            rvals.add(rval);

        toDataStoreValue1(em, vm, store, cols, rvals, idx);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
            Column newCol = new Column();
            newCol.copy(pkCol);
            cols.add(newCol);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
            if (cols.length == 1) {
                // rvals is empty
                rvals.add(cval); // save the return value
            } else if (ecols.length == 1) {
                Object rval = rvals.get(0);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
                io.setUpdatable(cols.size(), curIO.isUpdatable(j, false));
                io.setNullUpdatable(cols.size(), curIO.isUpdatable(j, true));
                cols.add(curCols[j]);
            }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
            curArgs = ((Embeddable) fm.getStrategy()).getResultArguments();
            if (curCols.length == 1)
                args.add(curArgs);
            else if (curCols.length > 1)
                for (int j = 0; j < curCols.length; j++)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
            else if (curCols.length > 1)
                for (int j = 0; j < curCols.length; j++)
                    args.add((curArgs == null) ? null
                            : ((Object[]) curArgs)[j]);
        }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/TimestampVersionStrategy.java`
#### Snippet
```java
        Object d = nextVersion(null);
        for (Column col : cols) {
            map.put(col, d);
        }
        return map;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractJDBCSavepointManager.java`
#### Snippet
```java
        public void save(Collection states) {
            AbstractJDBCSavepointManager.this.setDataStore(this);
            super.save(states);
        }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractJDBCSavepointManager.java`
#### Snippet
```java

        @Override
        public Collection rollback(Collection previous) {
            AbstractJDBCSavepointManager.this.rollbackDataStore(this);
            return super.rollback(previous);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractJDBCSavepointManager.java`
#### Snippet
```java
        public Collection rollback(Collection previous) {
            AbstractJDBCSavepointManager.this.rollbackDataStore(this);
            return super.rollback(previous);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/OperationOrderUpdateManager.java`
#### Snippet
```java
            if (updates == null)
                updates = new ArrayList();
            updates.add(update);
        }
        return updates;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/OperationOrderUpdateManager.java`
#### Snippet
```java
            if (updates == null)
                updates = new ArrayList();
            updates.add(update);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
            String sql = q.getContext().getQueryString();

            List paramList = new ArrayList(Arrays.asList(params));
            SQLBuffer buf = new SQLBuffer(dict).append(sql);

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
            String sql = q.getContext().getQueryString();

            List paramList = new ArrayList(Arrays.asList(params));
            SQLBuffer buf = new SQLBuffer(dict).append(sql);
            Connection conn = store.getConnection();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
            ValueMappingInfo info = val.getValueInfo();
            if (info.getColumns().size() == 0)
                info.setColumns(getMappedByIdColumns(fm));
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'mapObj' has raw type, so result of entrySet is erased
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
            value = ((ReflectingPersistenceCapable)value).getManagedInstance();

        Set<Map.Entry> entries = mapObj.entrySet();
        for (Map.Entry entry : entries) {
            if (entry.getValue() == value)
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
            pkCols = pk.getColumns();
            for (Column pkCol : pkCols) {
                cols.add(pkCol);
            }
            return cols;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
                cls = clss[0];
            oid = owner.getObjectId(store, res, null, true, null);
            rels.put(oid, res.load(cls, store, fetch, joins));
        }
        res.close();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/DelegatingJDBCFetchConfiguration.java`
#### Snippet
```java
    public JDBCFetchConfiguration addFetchInnerJoins(Collection fields) {
        try {
            getJDBCDelegate().addFetchInnerJoins(fields);
            return this;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/DelegatingJDBCFetchConfiguration.java`
#### Snippet
```java

    @Override
    public Set getFetchInnerJoins() {
        try {
            return getJDBCDelegate().getFetchInnerJoins();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/DelegatingJDBCFetchConfiguration.java`
#### Snippet
```java
    public JDBCFetchConfiguration addJoins(Collection fields) {
        try {
            getJDBCDelegate().addJoins(fields);
            return this;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/DelegatingJDBCFetchConfiguration.java`
#### Snippet
```java
    public JDBCFetchConfiguration removeJoins(Collection fields) {
        try {
            getJDBCDelegate().removeJoins(fields);
            return this;
        } catch (RuntimeException re) {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/DelegatingJDBCFetchConfiguration.java`
#### Snippet
```java

    @Override
    public Set getJoins() {
        try {
            return getJDBCDelegate().getJoins();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ConstraintUpdateManager.java`
#### Snippet
```java
            update.setNull((Column) edge.getUserObject());

        deleteUpdates.add(update);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ConstraintUpdateManager.java`
#### Snippet
```java
                sm = ((Row) insert).getPrimaryKey();
                if (sm != null && sm.getObjectId() != null)
                    insertMap.put(sm.getObjectId(), sm);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ConstraintUpdateManager.java`
#### Snippet
```java
        }

        insertUpdates.add(update);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'. Reason: 'user' has raw type, so result of entrySet is erased
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java
        Map<Integer, Object> result = new HashMap<>(_template);

        Set<Map.Entry<Object,Object>> userSet = user.entrySet();
        for (Map.Entry<Object,Object> userEntry : userSet) {
            Object key = userEntry.getKey();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        for (Object obj : selectFrom) {
            if (!exSelectFrom.contains(obj))
                retList.add(obj);
        }
        return retList;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        //   subclass, rather than one per mapped subclass, as is happening now

        subs.add(mapping);
        if (!hasVerticalSubclasses(mapping))
            return;
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        List subs = new ArrayList(4);
        addSubclasses(mapping, subs);
        return (ClassMapping[]) subs.toArray(new ClassMapping[subs.size()]);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
                for (int j = 0; j < size; j++) {
                    ClassMapping vert = (ClassMapping)selectFrom.get(j);
                    selMappings.add(vert);
                    if (j == size - 1) {
                        nextBits.set(sels.size());
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
                        nextBits.set(sels.size());
                        sel.select(vert.getPrimaryKeyColumns(), null);
                        sels.add(sel);
                    } else {
                        SelectImpl selClone = (SelectImpl)sel.fullClone(1);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
                        SelectImpl selClone = (SelectImpl)sel.fullClone(1);
                        selClone.select(vert.getPrimaryKeyColumns(), null);
                        sels.add(selClone);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
            } else {
                for (int j = 0; j < verts.length; j++) {
                    selMappings.add(verts[j]);
                    if (j == verts.length - 1) {
                        nextBits.set(sels.size());
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
                    if (j == verts.length - 1) {
                        nextBits.set(sels.size());
                        sels.add(sel);
                    } else
                        sels.add(sel.fullClone(1));
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
                        sels.add(sel);
                    } else
                        sels.add(sel.fullClone(1));
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        if (unionable) {
            Union union = _store.getSQLFactory().newUnion(
                (Select[]) sels.toArray(new Select[sels.size()]));
            BitSet[] paged = populateUnion(union, mappings, subclasses, facts,
                exps, states, ctx, lrs, eager, start, end);
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        if (unionable) {
            Union union = _store.getSQLFactory().newUnion(
                (Select[]) sels.toArray(new Select[sels.size()]));
            populateUnion(union, mappings, subclasses, facts, exps, states, ctx,
                false, eager, start, end);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
    static {
        // deprecated extensions
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        // deprecated extensions
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        _listeners.put(StringContains.TAG, new JDBCStringContains());
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        _listeners.put(WildcardMatch.TAG, new JDBCWildcardMatch());
        _listeners.put(SQLExpression.TAG, new SQLExpression());
        _listeners.put(SQLValue.TAG, new SQLValue());

        // jdbc-specific extensions
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java

        // jdbc-specific extensions
        _listeners.put(GetColumn.TAG, new GetColumn());
        _listeners.put(SQLEmbed.TAG, new SQLEmbed());
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
        // jdbc-specific extensions
        _listeners.put(GetColumn.TAG, new GetColumn());
        _listeners.put(SQLEmbed.TAG, new SQLEmbed());
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Stack' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/MappedQueryResultObjectProvider.java`
#### Snippet
```java
            if (_pc == null || ret != null)
                return ret;
            return (_pc.hasEager(_requests, key)) ? this : null;
        }

```

### UNCHECKED_WARNING
Unchecked call to 'push(E)' as a member of raw type 'java.util.Stack'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/MappedQueryResultObjectProvider.java`
#### Snippet
```java
        @Override
        public void startDataRequest(Object mapping) {
            _requests.push(mapping);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Stack' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/MappedQueryResultObjectProvider.java`
#### Snippet
```java
        @Override
        protected Object translate(Object obj, Joins joins) {
            return (_pc == null) ? obj : _pc.map(_requests, obj, joins);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Stack' to 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/MappedQueryResultObjectProvider.java`
#### Snippet
```java
            // anything additional
            return ((JDBCStoreManager) store).load(mapping, fetch,
                _pc.getExcludes(_requests), this);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set\>'. Reason: 'userParams' has raw type, so result of entrySet is erased
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedSQLStoreQuery.java`
#### Snippet
```java
            Object[] array = new Object[userParams.size()];

            Set<Map.Entry<Object,Object>> userSet = userParams.entrySet();
            for (Map.Entry<Object,Object> userEntry : userSet) {
                int idx = (Integer) userEntry.getKey();
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.util.CacheMap' to 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java

	public PreparedQueryCacheImpl() {
		_delegate = new CacheMap();
		_uncachables = new CacheMap();
		_exclusionPatterns = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.openjpa.util.CacheMap' to 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryCacheImpl.java`
#### Snippet
```java
	public PreparedQueryCacheImpl() {
		_delegate = new CacheMap();
		_uncachables = new CacheMap();
		_exclusionPatterns = new ArrayList<>();

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractUpdateManager.java`
#### Snippet
```java
                StateManagerImpl sm = (StateManagerImpl) obj;
                if (sm.getMappedByIdFields() != null)
                    mappedByIdStates.add(sm);
                else exceps = populateRowManager(sm, rowMgr, store, exceps,
                        customs);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractUpdateManager.java`
#### Snippet
```java
        Boolean custom = strat.isCustomDelete(sm, store);
        if (!Boolean.FALSE.equals(custom))
            customs.add(new CustomMapping(CustomMapping.DELETE, sm, strat));
        return Boolean.TRUE.equals(custom);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractUpdateManager.java`
#### Snippet
```java
        if (exceps == null)
            exceps = new LinkedList();
        exceps.add(err);
        return exceps;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractUpdateManager.java`
#### Snippet
```java
        Boolean custom = strat.isCustomInsert(sm, store);
        if (!Boolean.FALSE.equals(custom))
            customs.add(new CustomMapping(CustomMapping.INSERT, sm, strat));
        return Boolean.TRUE.equals(custom);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractUpdateManager.java`
#### Snippet
```java
        Boolean custom = strat.isCustomUpdate(sm, store);
        if (!Boolean.FALSE.equals(custom))
            customs.add(new CustomMapping(CustomMapping.UPDATE, sm, strat));
        return Boolean.TRUE.equals(custom);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/ConstPath.java`
#### Snippet
```java
    @Override
    public void get(FieldMetaData field, boolean nullTraversal) {
        _actions.add(field);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/ConstPath.java`
#### Snippet
```java
    @Override
    public void setImplicitType(Class type) {
        _actions.add(type);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/OrExpression.java`
#### Snippet
```java
        Map contains2 = null;
        if (contains != null)
            contains2 = new HashMap(contains);

        ExpState s1 = _exp1.initialize(sel, ctx, contains);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/OrExpression.java`
#### Snippet
```java
            val1 = (Integer) contains.get(entry.getKey());
            if (val1 == null || val2 > val1)
                contains.put(entry.getKey(), val2);
        }
        return ret;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/NotEqualTypeExpression.java`
#### Snippet
```java
            }
            List selectFrom = sel.getJoinedTableClassMeta();
            exSelectFrom.add(sub);
            if (selectFrom == null) {
                selectFrom = new ArrayList();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/NotEqualTypeExpression.java`
#### Snippet
```java
                if (!Modifier.isAbstract(classMetaData.getDescribedType().getModifiers()) &&
                        !selectFrom.contains(classMetaData))
                    selectFrom.add(classMetaData);
            }
            buf.append("1=1");
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/ContainsExpression.java`
#### Snippet
```java
            else
                count = count + 1;
            contains.put(path, count);

            sql.setContainsId(count.toString());
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/CompareExpression.java`
#### Snippet
```java
        // For purposes of the 'canConvert', when dealing with a Lit with Raw
        // use a String type since Raw contains a String.
        if (_val1 instanceof Lit && val1Type.isAssignableFrom(Raw.class)){
            val1Type = String.class;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/CompareExpression.java`
#### Snippet
```java
            val1Type = String.class;
        }
        if (_val2 instanceof Lit && val2Type.isAssignableFrom(Raw.class)){
            val2Type = String.class;
        }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/InExpression.java`
#### Snippet
```java
            list = new ArrayList(coll.size());
            for (Object o : coll)
                list.add(_val.toDataStoreValue(sel, ctx, istate.valueState,
                        o));
        }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java

    @Override
    public Collection flush(Collection<OpenJPAStateManager> sms) {
        try {
            if (_conn != null && _conn.getInnermostDelegate().isReadOnly())
```

### UNCHECKED_WARNING
Unchecked call to 'get(K, FetchConfiguration)' as a member of raw type 'org.apache.openjpa.kernel.FinderCache'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
        FinderCache cache = getFinderCache();
        return cache == null
             ? null : (FinderQueryImpl)cache.get(mapping, fetch);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'cache(K, V, FetchConfiguration)' as a member of raw type 'org.apache.openjpa.kernel.FinderCache'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
        FetchConfiguration fetch) {
        FinderCache cache = getFinderCache();
        return cache != null && cache.cache(mapping, select, fetch) != null;
    }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java

    @Override
    public Collection loadAll(Collection sms, PCState state, int load,
        FetchConfiguration fetch, Object context) {
        return ImplHelper.loadAll(sms, this, state, load, fetch, context);
```

### UNCHECKED_WARNING
Unchecked call to 'Entry(K, V)' as a member of raw type 'org.apache.openjpa.jdbc.kernel.exps.MapEntry.Entry'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/MapEntry.java`
#### Snippet
```java
        if (key == null || val == null)
            return null;
        return new Entry(key, val);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EqualTypeExpression.java`
#### Snippet
```java
                if (!Modifier.isAbstract(classMetaData.getDescribedType().getModifiers()) && classMetaData == sub
                        && !selectFrom.contains(classMetaData))
                    selectFrom.add(classMetaData);
            }
            buf.append("1=1");
```

### UNCHECKED_WARNING
Unchecked call to 'isAssignableFrom(Class)' as a member of raw type 'java.lang.Class'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/InstanceofExpression.java`
#### Snippet
```java
        InstanceofExpState istate = (InstanceofExpState) state;
        if (istate.discrim == null) {
            if (_cls.isAssignableFrom(istate.rel))
                sql.append("1 = 1");
            else
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java

    @Override
    protected Collection getObjects() {
        if (_seqs.isEmpty())
            return _tables;
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
            if (schemaObjs == null) {
                schemaObjs = new LinkedList();
                schemas.put(schemaName, schemaObjs);
            }
            schemaObjs.add(obj);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaSerializer.java`
#### Snippet
```java
                schemas.put(schemaName, schemaObjs);
            }
            schemaObjs.add(obj);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
            // bound variable; copy path
            _type = UNACCESSED_VAR;
            _actions.addAll(other._actions);
            _key = other._key;

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
            _correlationVar = other._correlationVar;
        }
        _actions.add(action);
        _cast = var.getType(); // initial type is var type
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        action.op = Action.SUBQUERY;
        action.data = sub.getCandidateAlias();
        _actions.add(action);
        _cast = sub.getType(); // initial type is subquery type
        _varName = sub.getCandidateAlias();
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        if (_actions == null)
            _actions = new LinkedList();
        _actions.add(action);
        _cid = true;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        action.op = Action.GET_XPATH;
        action.data = meta;
        _actions.add(action);
        _cast = null;
        _key = false;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        action.op = Action.GET_XPATH;
        action.data = meta.getFieldMapping(name);
        _actions.add(action);
        _cast = null;
        _key = false;
```

### UNCHECKED_WARNING
Unchecked call to 'add(int, E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        int pos = _actions.indexOf(action);
        _actions.remove(action);
        _actions.add(pos, key);
        _cast = null;
        _key = true;
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.LinkedList'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        action.op = (nullTraversal) ? Action.GET_OUTER : Action.GET;
        action.data = field;
        _actions.add(action);
        if (_type == UNACCESSED_VAR)
            _type = BOUND_VAR;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/relations/Main.java`
#### Snippet
```java
        // Go through each of the entities and print out each of their
        // messages, as well as the date on which it was created
        for (Deity m : (List<Deity>) q.getResultList()) {
            System.out.println(m.getName());
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/relations/Main.java`
#### Snippet
```java
            + "where x.father.name = 'Zeus'");

        for (Deity m : (List<Deity>) q.getResultList()) {
            System.out.println("Child of Zeus: " + m.getName());
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/relations/Main.java`
#### Snippet
```java
            setParameter(1, em.getReference(Deity.class, "Rhea"));

        for (Deity m : (List<Deity>) em.createNamedQuery("siblings").
            setParameter(1, em.getReference(Deity.class, "Rhea")).
            getResultList()) {
            System.out.println("Siblings of Rhea: " + m.getName());
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/relations/Main.java`
#### Snippet
```java
        }

        for (Deity m : (List<Deity>) em.createNamedQuery("half-siblings").
            setParameter(1, em.getReference(Deity.class, "Apollo")).
            getResultList()) {
            System.out.println("Half-siblings of Apollo: " + m.getName());
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/relations/Main.java`
#### Snippet
```java
        }

        for (Deity m : (List<Deity>) em.createNamedQuery("cousins").
            setParameter(1, em.getReference(Deity.class, "Leto")).
            getResultList()) {
            System.out.println("Cousins of Leto: " + m.getName());
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/embeddables/Main.java`
#### Snippet
```java
        Query q = em.createQuery("SELECT u FROM User u , in (u.addresses) a " + "WHERE a.state='xx'");

        List<User> res = q.getResultList();
        for (User b : res) {
            System.out.println(b);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/embeddables/Main.java`
#### Snippet
```java

        Query q1 = em.createQuery("SELECT u FROM User u , in (u.addresses) a " + "WHERE a.coordinates.longitude='38'");
        List<User> res1 = q1.getResultList();
        for (User b : res1) {
            System.out.println(b);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/embeddables/Main.java`
#### Snippet
```java
            em.createQuery("SELECT DISTINCT u FROM User u " + "WHERE u.contactInfo.homePhone.number='507-555-5555'"
                + "AND u.contactInfo.homePhone.type='cell'");
        res = q.getResultList();
        for (User b : res) {
            System.out.println(b);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-examples/simple/src/main/java/embeddables/Main.java`
#### Snippet
```java
            em.createQuery("SELECT u FROM User u " + "WHERE u.contactInfo.homePhone.number='507-555-5555'"
                + "AND u.contactInfo.homePhone.type='cell'");
        res = q.getResultList();
        for (User b : res) {
            System.out.println(b);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java

        ForeignKey[] fks = _pkTable.getForeignKeys();
        seen.add(this);
        for (Column col : cols) {
            for (ForeignKey fk : fks) {
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_constCols == null)
            _constCols = (_consts == null) ? Schemas.EMPTY_COLUMNS : (Column[])
                _consts.keySet().toArray(new Column[_consts.size()]);
        return _constCols;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_joins == null)
            _joins = new LinkedHashMap();
        _joins.put(local, toPK);
        local.addConstraint(this);
        if (_joinsPK == null)
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_joinsPK == null)
            _joinsPK = new LinkedHashMap();
        _joinsPK.put(toPK, local);

        // force re-cache
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_constColsPK == null)
            _constColsPK = (_constsPK == null) ? Schemas.EMPTY_COLUMNS :
                (Column[]) _constsPK.keySet().toArray
                    (new Column[_constsPK.size()]);
        return _constColsPK;
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Collection'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_pks == null)
            _pks = (_joins == null) ? Schemas.EMPTY_COLUMNS : (Column[])
                _joins.values().toArray(new Column[_joins.size()]);
        return _pks;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'toArray(T\[\])' as a member of raw type 'java.util.Set'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_locals == null)
            _locals = (_joins == null) ? Schemas.EMPTY_COLUMNS : (Column[])
                _joins.keySet().toArray(new Column[_joins.size()]);
        return _locals;
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_consts == null)
            _consts = new LinkedHashMap();
        _consts.put(col, val);

        // force re-cache
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
        if (_constsPK == null)
            _constsPK = new LinkedHashMap();
        _constsPK.put(toPK, val);

        // force re-cache
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributedQueryImpl.java`
#### Snippet
```java
            QueryTargetPolicy policy = _conf.getQueryTargetPolicyInstance();
            if (policy != null) {
                String[] targets = policy.getTargets(getQueryString(), Collections.unmodifiableMap(params),
                        getLanguage(), _conf.getActiveSliceNames(), this.getBroker());
                fetch.setTargets(targets);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java

    @Override
    public Collection loadAll(Collection sms, PCState state, int load,
            FetchConfiguration fetch, Object edata) {
        Map<String, StateManagerSet> subsets = bin(sms, edata);
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
            Collection tmp = slice.loadAll(subset, state, load, fetch, edata);
            if (tmp != null && !tmp.isEmpty())
                result.addAll(tmp);
        }
        return result;
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.Collection'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
    private void collectException(Collection error,  Collection holder) {
        if (!(error == null || error.isEmpty())) {
            holder.addAll(error);
        }
    }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Collection', required 'java.util.Collection'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
     */
    @Override
    public Collection flush(Collection<OpenJPAStateManager> sms) {
        Collection exceptions = new ArrayList();
        List<Future<Collection>> futures = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
        if (slice != null)
            throw new UserException(_loc.get("slice-exists", name));
        Map<String,String> original = super.toProperties(true);
        original.putAll(newProps);
         slice = newSlice(name, original);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
            throw new UserException(_loc.get("slice-exists", name));
        Map<String,String> original = super.toProperties(true);
        original.putAll(newProps);
         slice = newSlice(name, original);
        _slices.add(slice);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
            if (key.startsWith(prefix)) {
                String newKey = PREFIX_OPENJPA + key.substring(prefix.length());
                result.put(newKey, value);
            } else if (key.startsWith(PREFIX_SLICE)) {
                // ignore keys that are in 'slice.' namespace but not this slice
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
                String newKey = prefix + key.substring(PREFIX_OPENJPA.length());
                if (!original.containsKey(newKey))
                    result.put(key, value);
            } else { // keys that are neither "openjpa" nor "slice" namespace
                result.put(key, value);
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
                    result.put(key, value);
            } else { // keys that are neither "openjpa" nor "slice" namespace
                result.put(key, value);
            }
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCConfigurationImpl.java`
#### Snippet
```java
    public List<Slice> getSlices(Slice.Status...statuses) {
        if (statuses == null)
            return _slices == null ? Collections.EMPTY_LIST : Collections.unmodifiableList(_slices);
        List<Slice> result = new ArrayList<>();
        for (Slice slice:_slices) {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.openjpa.jdbc.schema.Constraint' to 'T'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
        for (Constraint c : _constraints) {
            if (c.getClass() == type) {
                result.add((T)c);
            }
        }
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Casting `oid` to `Number` may produce `ClassCastException`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/JPAFacadeHelper.java`
#### Snippet
```java
                if (oid instanceof String && ((String) oid).startsWith(StateManagerId.STRING_PREFIX))
                    return new StateManagerId((String) oid);
                return new Id(cls, ((Number) oid).longValue());
            case ClassMetaData.ID_APPLICATION:
                if (ImplHelper.isAssignable(meta.getObjectIdType(), oid.getClass())) {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java

            List<Method> setters = filter(publicMethods, setterFilter);
            return getters = matchGetterAndSetter(getters, setters);
    	}

```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java

        List<Method> setters = filter(methods, setterFilter);
        getters =  matchGetterAndSetter(getters, setters);

        boolean mixed = !fields.isEmpty() && !getters.isEmpty();
```

### DataFlowIssue
Condition `EntityManagerImpl.class.isInstance(em)` is redundant and can be replaced with a null check
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerFactoryImpl.java`
#### Snippet
```java
            if (emEmptyPropsProperties == null) {
                emEmptyPropsProperties = em.getProperties();
            } else if (EntityManagerImpl.class.isInstance(em)) {
                EntityManagerImpl.class.cast(em).setProperties(emEmptyPropsProperties);
            }
```

### DataFlowIssue
Method invocation `getJavaType` may produce `NullPointerException`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/MetamodelImpl.java`
#### Snippet
```java
            } catch (Exception e) {
                throw new RuntimeException(_loc.get("meta-field-mismatch",
                        new Object[] { mf.getName(), mcls.getName(), toTypeName(mfType), f.getJavaType().toString() })
                        .getMessage(), e);
            }
```

### DataFlowIssue
Casting `c` to `OpenJPAConfigurationImpl` may produce `ClassCastException`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
            return false;

        OpenJPAConfigurationImpl conf = (OpenJPAConfigurationImpl) c;
        conf.metaFactoryPlugin.setDefault(SPEC_JPA.getName());
        conf.metaFactoryPlugin.setString(SPEC_JPA.getName());
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        Set<ExecutableElement> getters = filter(allMethods, getterFilter);
        Set<ExecutableElement> setters = filter(allMethods, setterFilter);
        getters = matchGetterAndSetter(getters, setters);

        return merge(filter(allMembers, fieldFilter, nonTransientFilter,
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        		propertyAccessFilter, annotatedFilter);
        Set<ExecutableElement> setters = filter(allMethods, setterFilter);
        getters = matchGetterAndSetter(getters, setters);

        return merge(getters, allFields);
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        Set<ExecutableElement> getters = filter(allMethods, getterFilter, annotatedFilter);
        Set<ExecutableElement> setters = filter(allMethods, setterFilter);
        getters = matchGetterAndSetter(getters, setters);

        boolean isFieldAccess = !annotatedFields.isEmpty();
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
            Set<ExecutableElement> getters = filter(allMethods, getterFilter);
            Set<ExecutableElement> setters = filter(allMethods, setterFilter);
            getters = matchGetterAndSetter(getters, setters);
            result.addAll(getters);
        }
```

### DataFlowIssue
Casting `adapter` to `BeanLifecycleCallbacks` may produce `ClassCastException`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
            if (_listener != null) {
                MetaDataParsers.validateMethodsForSameCallback(_listener,
                        _callbacks[event], ((BeanLifecycleCallbacks) adapter).
                                getCallbackMethod(), callback, _conf, getLog());
            }
```

### DataFlowIssue
Argument `component` might be null
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/EntityManagerImpl.java`
#### Snippet
```java
                    dim[i]=0;
                }
                return Array.newInstance(component, dim).getClass();
            } else {
                return lookupClass(cname);
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AbstractManagedType.java`
#### Snippet
```java
        throw new IllegalArgumentException(
            _loc.get(msg, new Object[]{name, (t1 == null ? "any" : t1.getName()),
                    (t2 == null ? "any" : t1.getName()), meta}).getMessage());
    }
    // --------------------------------------------------------------------------
```

### DataFlowIssue
Argument `_parent` might be null
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
            path = factory.newPath(subQ);
            path.setMetaData(subQ.getMetaData());
            path.setSchemaAlias(q.getAlias(_parent));
            traversePath(_parent, path, _member.fmd);
        } else if (_parent != null) {
```

### DataFlowIssue
Dereference of `parent` may produce `NullPointerException`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
        boolean allowNull = parent == null ? false : parent instanceof Join
            && ((Join<?,?>)parent).getJoinType() != JoinType.INNER;
        FieldMetaData fmd1 = parent._member == null ? null : parent._member.fmd;
        PathImpl<?,?> parent1 = parent._parent;
        if (parent1 == null || parent1.getCorrelatedPath() != null) {
```

### DataFlowIssue
Method invocation `getModel` may produce `NullPointerException`
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
        }

        return ((AbstractManagedType<?>)root.getModel()).meta;
    }

```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                case LEADING  : spec = true;  break;
                case TRAILING : spec = false; break;
                case BOTH     : spec = null;  break;
                }
            }
```

### DataFlowIssue
Method invocation `setInto` may produce `NullPointerException`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            }

            ProductDerivations.load(resourceName, anchor, getClass().getClassLoader()).setInto(this);
        }

```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
        } finally {
            try {
                reader.close();
            } catch (IOException ioe) {
                // silently consume exception
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Services.java`
#### Snippet
```java
            }
            try {
                in.close();
            } catch (IOException ioe) {
                // silently consume exception
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Options.java`
#### Snippet
```java
                else {
                    key = args[i].substring(1);
                    value = null;
                }
            } else if (key != null) {
```

### DataFlowIssue
Dereference of `header` may produce `NullPointerException`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/LRUMap.java`
#### Snippet
```java
                if (reuse == null) {
                    throw new IllegalStateException(
                        "Entry.after=null, header.after=" + header.after + " header.before=" + header.before +
                        " key=" + key + " value=" + value + " size=" + size + " maxSize=" + maxSize +
                        " This should not occur if your keys are immutable, and you have used synchronization properly.");
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/identifier/IdentifierUtilImpl.java`
#### Snippet
```java
        }

        return combined.toString();
    }

```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java
        ManagedRuntime mr = null;

        mr = null;
        try {
            mr = (ManagedRuntime) Class.
```

### DataFlowIssue
Dereference of `_mapKeyTemporal` may produce `NullPointerException`
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java

        Column col = (Column) cols.get(0);
        switch (_mapKeyTemporal) {
            case DATE:
                col.setType(Types.DATE);
```

### DataFlowIssue
Method invocation `values` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataTool.java`
#### Snippet
```java
            if (_writer != null) {
                PrintWriter out = new PrintWriter(_writer);
                for (Object o : output.values()) {
                    out.println((String) o);
                }
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InMemoryRelatedFieldOrder.java`
#### Snippet
```java
        PersistenceCapable pc1 = ImplHelper.toPersistenceCapable(o1, _conf);
        PersistenceCapable pc2 = ImplHelper.toPersistenceCapable(o2, _conf);
        OpenJPAStateManager sm1 = (OpenJPAStateManager) pc1.pcGetStateManager();
        OpenJPAStateManager sm2 = (OpenJPAStateManager) pc2.pcGetStateManager();
        if (sm1 == null || sm2 == null)
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InMemoryRelatedFieldOrder.java`
#### Snippet
```java
        PersistenceCapable pc2 = ImplHelper.toPersistenceCapable(o2, _conf);
        OpenJPAStateManager sm1 = (OpenJPAStateManager) pc1.pcGetStateManager();
        OpenJPAStateManager sm2 = (OpenJPAStateManager) pc2.pcGetStateManager();
        if (sm1 == null || sm2 == null)
            return 0;
```

### DataFlowIssue
Method invocation `getFetchGroup` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FetchGroup.java`
#### Snippet
```java
            if (name.equals(_name))
                throw new MetaDataException(_loc.get("cyclic-fg", this, name));
            fg = _meta.getFetchGroup(name);
            if (fg == null)
                throw new MetaDataException(_loc.get("bad-fg-include", this, name));
```

### DataFlowIssue
Method invocation `getFetchGroup` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FetchGroup.java`
#### Snippet
```java
        FetchGroup fg;
        for (String included : _includes) {
            fg = _meta.getFetchGroup(included);
            if (fg != null && fg.isPostLoad())
                return true;
```

### DataFlowIssue
Method invocation `getFetchGroup` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FetchGroup.java`
#### Snippet
```java
        FetchGroup fg;
        for (String included : _includes) {
            fg = _meta.getFetchGroup(included);
            depth = (fg == null) ? null : fg.findRecursionDepth(fm);
            if (depth != null && (max == null || depth.intValue() > max.intValue()))
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InMemoryValueOrder.java`
#### Snippet
```java
        PersistenceCapable pc1 = ImplHelper.toPersistenceCapable(o1, _conf);
        PersistenceCapable pc2 = ImplHelper.toPersistenceCapable(o2, _conf);
        OpenJPAStateManager sm1 = (OpenJPAStateManager) pc1.pcGetStateManager();
        OpenJPAStateManager sm2 = (OpenJPAStateManager) pc2.pcGetStateManager();
        if (sm1 == null || sm2 == null)
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InMemoryValueOrder.java`
#### Snippet
```java
        PersistenceCapable pc2 = ImplHelper.toPersistenceCapable(o2, _conf);
        OpenJPAStateManager sm1 = (OpenJPAStateManager) pc1.pcGetStateManager();
        OpenJPAStateManager sm2 = (OpenJPAStateManager) pc2.pcGetStateManager();
        if (sm1 == null || sm2 == null)
            return 0;
```

### DataFlowIssue
Method invocation `contains` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java
            def = query.getDefiningType();
            rem = (query.getSourceMode() & mode) != 0
                    && clsNames.contains((def == null) ? null : def.getName());
            if (rem || (!isMappingOnlyFactory()
                    && query.getSourceMode() == MODE_QUERY))
```

### DataFlowIssue
Argument `attrName` might be null
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
            return null;
        }
        return getAttributeOverrideField(attrName, fm, embed);
    }

```

### DataFlowIssue
Method invocation `pcNewObjectIdInstance` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ApplicationIds.java`
#### Snippet
```java
            PersistenceCapable pc = PCRegistry.newInstance(type, null, oid,
                 false);
            Object copy = pc.pcNewObjectIdInstance();
            pc.pcCopyKeyFieldsToObjectId(copy);
            return copy;
```

### DataFlowIssue
Argument `box(sourceType)` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
			return isConvertibleToByIdentityPrimitiveConversion(sourceType, destType)
				|| isConvertibleToByWideningPrimitive(sourceType, destType)
				|| destType.isAssignableFrom(box(sourceType));
		} else {
			// Note that unbox will return null if the sourceType is not a wrapper.
```

### DataFlowIssue
Argument `pc` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
        // is being managed by different broker.  If it is, then it should not be
        // considered "new".
        boolean isNew = !broker.isDetached(pc) && !isManagedByAnotherPCtx(pc, broker);
        Object version = null;
        StateManagerImpl sm;
```

### DataFlowIssue
Method invocation `pcNewInstance` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
            || broker.getStateManager(into) == null)) {
            if (into == null)
                into = pc.pcNewInstance(null, false);
            sm = (StateManagerImpl) broker.embed(into, null, owner, ownerMeta);
            into = sm.getPersistenceCapable();
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
            if (into == null)
                throw new OptimisticException(_loc.get("attach-version-del",
                    ImplHelper.getManagedInstance(pc).getClass(), id, version))
                    .setFailedObject(toAttach);

```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
        // assign the detached pc the same state manager as the object we're
        // copying into during the attach process
        StateManager smBefore = pc.pcGetStateManager();
        pc.pcReplaceStateManager(sm);
        int detach = (isNew) ? DETACH_ALL : broker.getDetachState();
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachManager.java`
#### Snippet
```java
        PersistenceCapable pc = ImplHelper.toPersistenceCapable(toAttach,
            getBroker().getConfiguration());
        if (pc.pcGetStateManager() instanceof AttachStrategy)
            return (AttachStrategy) pc.pcGetStateManager();

```

### DataFlowIssue
Method invocation `doubleValue` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
    @Override
    public double fetchDouble(int field) {
        return ((Number) getValue(field)).doubleValue();
    }

```

### DataFlowIssue
Method invocation `shortValue` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
    @Override
    public short fetchShort(int field) {
        return ((Number) getValue(field)).shortValue();
    }

```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
    @Override
    public long fetchLong(int field) {
        return ((Number) getValue(field)).longValue();
    }

```

### DataFlowIssue
Method invocation `intValue` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
    @Override
    public int fetchInt(int field) {
        return ((Number) getValue(field)).intValue();
    }

```

### DataFlowIssue
Method invocation `floatValue` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
    @Override
    public float fetchFloat(int field) {
        return ((Number) getValue(field)).floatValue();
    }

```

### DataFlowIssue
Unboxing of `getValue(field)` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
    @Override
    public char fetchChar(int field) {
        return (Character) getValue(field);
    }

```

### DataFlowIssue
Unboxing of `getValue(field)` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
    @Override
    public boolean fetchBoolean(int field) {
        return (Boolean) getValue(field);
    }

```

### DataFlowIssue
Method invocation `byteValue` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ObjectIdStateManager.java`
#### Snippet
```java
    @Override
    public byte fetchByte(int field) {
        return ((Number) getValue(field)).byteValue();
    }

```

### DataFlowIssue
Method invocation `pcGetDetachedState` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedStateAttachStrategy.java`
#### Snippet
```java
        switch (meta.getIdentityType()) {
            case ClassMetaData.ID_DATASTORE:
                Object[] state = (Object[]) pc.pcGetDetachedState();
                if (state == null)
                    return null;
```

### DataFlowIssue
Method invocation `pcGetDetachedState` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedStateAttachStrategy.java`
#### Snippet
```java
            manager.getBroker().getConfiguration());

        Object[] state = (Object[]) pc.pcGetDetachedState();
        boolean embedded = ownerMeta != null && ownerMeta.isEmbeddedPC();
        int offset;
```

### DataFlowIssue
Argument `fetchGroupList` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfigurationImpl.java`
#### Snippet
```java

        if (_state.ctx != null)
            addFetchGroups(Arrays.asList(fetchGroupList));

        verifyDefaultPUFetchGroups();
```

### DataFlowIssue
Argument `fetchGroupList` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfigurationImpl.java`
#### Snippet
```java
        clearFetchGroups((fetchGroupList == null || fetchGroupList.length == 0));

        addFetchGroups(Arrays.asList(fetchGroupList));
        setMaxFetchDepth(conf.getMaxFetchDepth());

```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CandidatePath.java`
#### Snippet
```java
                sm = (OpenJPAStateManager) (ImplHelper.toPersistenceCapable(
                        candidate, ctx.getConfiguration())).
                        pcGetStateManager();
            if (sm == null) {
                tmpBroker = ctx.getBroker();
```

### DataFlowIssue
Method invocation `getParameterTypes` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            else {
                try {
                    value = StringUtil.parse((String) value, setter.getParameterTypes()[0]);
                } catch (Exception e) {
                    throw new UserException(_loc.get("bad-setter-hint-arg",
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
                    nonspace = true;
            }
            escape = false;
        }

```

### DataFlowIssue
Casting `o` to `Number` may produce `ClassCastException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            return ((Number) o).intValue();
        } else if (type == Float.class && allowNumericConversion(o.getClass(), type, strictNumericConversion)) {
            return ((Number) o).floatValue();
        } else if (type == Double.class) {
            return ((Number) o).doubleValue();
```

### DataFlowIssue
Casting `o` to `Number` may produce `ClassCastException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            return ((Number) o).floatValue();
        } else if (type == Double.class) {
            return ((Number) o).doubleValue();
        } else if (type == Long.class && allowNumericConversion(o.getClass(), type, strictNumericConversion)) {
            return ((Number) o).longValue();
```

### DataFlowIssue
Casting `o` to `Number` may produce `ClassCastException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            return ((Number) o).doubleValue();
        } else if (type == Long.class && allowNumericConversion(o.getClass(), type, strictNumericConversion)) {
            return ((Number) o).longValue();
        } else if (type == BigDecimal.class) {
            // the BigDecimal constructor doesn't handle the
```

### DataFlowIssue
Casting `o` to `Number` may produce `ClassCastException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            // does it handle infinity; we need to instead use the Double
            // and Float versions, despite wanting to cast it to BigDecimal
            double dval = ((Number) o).doubleValue();
            if (Double.isNaN(dval) || Double.isInfinite(dval)) {
                return dval;
```

### DataFlowIssue
Casting `o` to `Number` may produce `ClassCastException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
                return dval;
            }
            float fval = ((Number) o).floatValue();
            if (Float.isNaN(fval) || Float.isInfinite(fval)) {
                return fval;
```

### DataFlowIssue
Casting `o` to `Number` may produce `ClassCastException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            return new BigInteger(o.toString());
        } else if (type == Short.class && allowNumericConversion(o.getClass(), type, strictNumericConversion)) {
            return ((Number) o).shortValue();
        } else if (type == Byte.class && allowNumericConversion(o.getClass(), type, strictNumericConversion)) {
            return ((Number) o).byteValue();
```

### DataFlowIssue
Casting `o` to `Number` may produce `ClassCastException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            return ((Number) o).shortValue();
        } else if (type == Byte.class && allowNumericConversion(o.getClass(), type, strictNumericConversion)) {
            return ((Number) o).byteValue();
        } else if (type == Character.class) {
            return (char) ((Number) o).intValue();
```

### DataFlowIssue
Casting `o` to `Number` may produce `ClassCastException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            return (char) ((Number) o).intValue();
        } else if (!strictNumericConversion) {
            return ((Number) o).intValue();
        } else {
            throw new ClassCastException(_loc.get("cant-convert", o, o.getClass(), type).getMessage());
```

### DataFlowIssue
Method invocation `pcReplaceStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachedStateManager.java`
#### Snippet
```java
            sm = (StateManagerImpl) broker.embed(_pc, _oid, owner, ownerMeta);
            ImplHelper.toPersistenceCapable(toAttach, broker.getConfiguration())
                .pcReplaceStateManager(this);
        } else {
            PCState state = (_dirty.length() > 0) ? PCState.PDIRTY
```

### DataFlowIssue
Method invocation `pcGetGenericContext` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
        if (val != null
            && (ImplHelper.toPersistenceCapable(val,
                 getContext().getConfiguration())).pcGetGenericContext()!= null)
            throw translate(new InvalidStateException(_loc.get
                ("managed-oid", Exceptions.toString(val),
```

### DataFlowIssue
Method invocation `pcReplaceStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
            pc = (PersistenceCapable) o;

        pc.pcReplaceStateManager(this);
        return pc;
    }
```

### DataFlowIssue
Method invocation `dirty` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
                else
                    // notify owner of change
                    _owner.dirty(_ownerIndex, Boolean.TRUE, loadFetchGroup);
            }

```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java
                group = new ArrayList();
            }
            group.add(pc);
            System.arraycopy(curs, 0, prevs, 0, curs.length);
        }
```

### DataFlowIssue
Method invocation `pcIsPersistent` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        if (ImplHelper.isManageable(obj))
            return (ImplHelper.toPersistenceCapable(obj, _conf)).
                    pcIsPersistent();
        return false;
    }
```

### DataFlowIssue
Method invocation `pcCopyFields` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                    for (int i = 0; i < fields.length; i++)
                        fields[i] = i;
                    copy.pcCopyFields(pc, fields);
                    state = PCState.ECOPY;
                    copy.pcReplaceStateManager(null);
```

### DataFlowIssue
Method invocation `pcGetVersion` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        assertOpen();
        if (ImplHelper.isManageable(obj))
            return (ImplHelper.toPersistenceCapable(obj, _conf)).pcGetVersion();
        return null;
    }
```

### DataFlowIssue
Method invocation `pcIsDirty` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        if (ImplHelper.isManageable(obj)) {
            PersistenceCapable pc = ImplHelper.toPersistenceCapable(obj, _conf);
            return pc.pcIsDirty();
        }
        return false;
```

### DataFlowIssue
Method invocation `pcIsDeleted` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        assertOpen();
        if (ImplHelper.isManageable(obj))
            return (ImplHelper.toPersistenceCapable(obj, _conf)).pcIsDeleted();
        return false;
    }
```

### DataFlowIssue
Method invocation `pcGetGenericContext` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        if (ImplHelper.isManageable(obj)) {
            PersistenceCapable pc = ImplHelper.toPersistenceCapable(obj, _conf);
            BrokerImpl pcBroker = (BrokerImpl)pc.pcGetGenericContext();
            if (pcBroker == this || isFromWriteBehindCallback())
                return (StateManagerImpl) pc.pcGetStateManager();
```

### DataFlowIssue
Method invocation `pcIsNew` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        assertOpen();
        if (ImplHelper.isManageable(obj))
            return (ImplHelper.toPersistenceCapable(obj, _conf)).pcIsNew();
        return false;
    }
```

### DataFlowIssue
Method invocation `pcIsTransactional` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        if (ImplHelper.isManageable(obj))
            return (ImplHelper.toPersistenceCapable(obj, _conf))
                .pcIsTransactional();
        return false;
    }
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java

        PersistenceCapable pc = ImplHelper.toPersistenceCapable(obj, _conf);
        if (pc.pcGetStateManager() instanceof DetachedStateManager)
            return true;
        Boolean detached = pc.pcIsDetached();
```

### DataFlowIssue
Method invocation `getAnnotation` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
            method = getDeclaringMethod(cls, method);
        return canReflect((Reflectable)cls.getAnnotation(Reflectable.class),
            method.getAnnotation(Reflectable.class));
    }

```

### DataFlowIssue
Dereference of `alias` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        // identification_variable_declaration()
        if (inner && ctx().getParent() != null && ctx().schemaAlias == null) {
            return getSubquery(alias.text, path, exp);
        }

```

### DataFlowIssue
Condition `val instanceof Value` is redundant and can be replaced with a null check
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        } else if (val instanceof Path) {
            return val;
        } else if (val instanceof Value) {
            if (val.isVariable()) {
                // can be an entity type literal
```

### DataFlowIssue
Dereference of `selectClause` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        JPQLNode selectChild = firstChild(expNode);

        if (selectClause.parent.id == JJTSUBSELECT) {
            exps.distinct &= ~QueryExpressions.DISTINCT_AUTO;
            return assignSubselectProjection(onlyChild(selectChild), exps);
```

### DataFlowIssue
Condition `val instanceof Value` is redundant and can be replaced with a null check
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
        final Value val = getVariable(name, false);

        if (val instanceof Value && val.isVariable()) {
            Class<?> c = resolver.classForName(name, null);
            if (c != null) {
```

### DataFlowIssue
Argument `val3` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java
                setImplicitType(val2, Integer.TYPE);
                if (node.children.length == 3)
                    setImplicitType(val3, Integer.TYPE);

                return convertSubstringArguments(factory, val1, val2, val3);
```

### DataFlowIssue
Method invocation `getFile` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/asm/AsmSpi9.java`
#### Snippet
```java

            OutputStream out = new FileOutputStream(
                    URLDecoder.decode(type.getResource(name + ".class").getFile()));
            try {
                writeJava7(bc, out);
```

### DataFlowIssue
Argument `modMap` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
                        continue;

                    mods = getModifications(modMap, cache);
                    data = newPCData(sm, cache);
                    data.store(sm);
```

### DataFlowIssue
Argument `modMap` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
                    // object.
                    data = cache.get(sm.getObjectId());
                    mods = getModifications(modMap, cache);

                    // data should always be non-null, since the object is
```

### DataFlowIssue
Argument `modMap` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
                        continue;

                    mods = getModifications(modMap, cache);
                    mods.deletes.add(sm.getObjectId());
                }
```

### DataFlowIssue
Method invocation `keys` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/instrumentation/AbstractQueryCacheInstrument.java`
#### Snippet
```java
    private QueryKey findKey(String key) {
        QueryStatistics<QueryKey> stats = getStatistics();
        for (QueryKey qk : stats.keys()) {
            if (qk.toString().equals(key)) {
                return qk;
```

### DataFlowIssue
Argument `file` might be null
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
            File file = getFile();
            Files.backup(file, false);
            writer = new FileWriter(file);
        }

```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                        // &amp;
                        buf = initializeBuffer(buf, s, i);
                        c = '&';
                        i += 4;
                    }
```

### DataFlowIssue
Dereference of `all` may produce `NullPointerException`
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/FileScanner.java`
#### Snippet
```java
            return;
        File[] all = dir.listFiles();
        for (File f : all) {
            if (ext == null || f.getName().endsWith(ext)) {
                bag.add(f);
```

### DataFlowIssue
Dereference of `classes` may produce `NullPointerException`
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/Finder.java`
#### Snippet
```java
        });
        String clsName = cls+CLASS_SUFFIX;
        for (File c : classes) {
            String name = c.getName().replace(File.separatorChar, DOT);
            if (name.endsWith(clsName))
```

### DataFlowIssue
Dereference of `jars` may produce `NullPointerException`
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/Finder.java`
#### Snippet
```java
        });

        for (File jar : jars) {
            JarFile jarFile = new JarFile(jar);
            scan(jarFile, cls);
```

### DataFlowIssue
Dereference of `dirs` may produce `NullPointerException`
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/Finder.java`
#### Snippet
```java
            }
        });
        for (File cdir : dirs)
            scan(cdir, cls);
    }
```

### DataFlowIssue
Method invocation `getModifiers` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                } else if (isFieldAccess(fmds[i])) {
                    field = Reflection.findField(oidType, name, true);
                    if (Modifier.isPublic(field.getModifiers()))
                        code.getfield().setField(field);
                    else {
```

### DataFlowIssue
Method invocation `getModifiers` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                } else {
                    getter = Reflection.findGetter(oidType, name, true);
                    if (Modifier.isPublic(getter.getModifiers()))
                        code.invokevirtual().setMethod(getter);
                    else {
```

### DataFlowIssue
Method invocation `getModifiers` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                setter = null;
                field = Reflection.findField(oidType, name, true);
                reflect = !Modifier.isPublic(field.getModifiers());
                if (reflect) {
                    code.classconstant().setClass(oidType);
```

### DataFlowIssue
Method invocation `getModifiers` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                field = null;
                setter = Reflection.findSetter(oidType, name, type, true);
                reflect = !Modifier.isPublic(setter.getModifiers());
                if (reflect) {
                    code.classconstant().setClass(oidType);
```

### DataFlowIssue
Method invocation `getDeclaredField` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
                // if we're directly accessing a field in another class
                // hierarchy that uses property access, something is wrong
                if (owner != _meta && owner.getDeclaredField(name) != null &&
                    _meta != null && !owner.getDescribedType()
                        .isAssignableFrom(_meta.getDescribedType()))
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
        } else {
            code.getfield().setField(declarer.getName(), fieldName,
                field.getType().getName());
        }
    }
```

### DataFlowIssue
Method invocation `isOpen` may produce `NullPointerException`
in `openjpa-examples/openbooks/src/main/java/openbook/server/PersistenceService.java`
#### Snippet
```java
        String thread = Thread.currentThread().getName();
        assertTrue("No persistent context is associated with " + thread, em != null);
        assertTrue("Persistent context " + em + " associated with " + thread + " has been closed", em.isOpen());
        if (!isManaged) {
            assertTrue("Persistent context " + em + " associated with " + thread + " has no active transaction",
```

### DataFlowIssue
Method invocation `getImage` may produce `NullPointerException`
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
        Thread.currentThread().setUncaughtExceptionHandler(new ErrorHandler());
        _config = PropertyHelper.load(System.getProperty("openbook.client.config", "demo.properties"));
        setIconImage(((ImageIcon)Images.LOGO_OPENBOOKS).getImage());
        setTitle("OpenBooks: A Sample JPA 2.0 Application");

```

### DataFlowIssue
Dereference of `getNode(join, join.getIndex2())` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/JoinSet.java`
#### Snippet
```java
        if (node != null) {
            node.join = join;
            getNode(join, join.getIndex2()).join = join;
            _sorted = null;
        } else
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            }
            else if ( ((LA21_0>='\u0000' && LA21_0<='&')||(LA21_0>='(' && LA21_0<='[')||(LA21_0>=']' && LA21_0<='\uFFFF')) ) {
                alt21=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java

                    if ( (LA26_1=='/') ) {
                        alt26=2;
                    }
                    else if ( ((LA26_1>='\u0000' && LA26_1<='.')||(LA26_1>='0' && LA26_1<='\uFFFF')) ) {
```

### DataFlowIssue
Switch label `'\"'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            if ( (LA23_0=='\\') ) {
                switch ( input.LA(2) ) {
                case '\"':
                case '\'':
                case '\\':
```

### DataFlowIssue
Switch label `'\''` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                switch ( input.LA(2) ) {
                case '\"':
                case '\'':
                case '\\':
                case 'b':
```

### DataFlowIssue
Switch label `'\\'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '\"':
                case '\'':
                case '\\':
                case 'b':
                case 'f':
```

### DataFlowIssue
Switch label `'b'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '\'':
                case '\\':
                case 'b':
                case 'f':
                case 'n':
```

### DataFlowIssue
Switch label `'f'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '\\':
                case 'b':
                case 'f':
                case 'n':
                case 'r':
```

### DataFlowIssue
Switch label `'n'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case 'b':
                case 'f':
                case 'n':
                case 'r':
                case 't':
```

### DataFlowIssue
Switch label `'r'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case 'f':
                case 'n':
                case 'r':
                case 't':
                    {
```

### DataFlowIssue
Switch label `'t'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case 'n':
                case 'r':
                case 't':
                    {
                    alt23=1;
```

### DataFlowIssue
Switch label `'u'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    }
                    break;
                case 'u':
                    {
                    alt23=2;
```

### DataFlowIssue
Switch label `'0'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    }
                    break;
                case '0':
                case '1':
                case '2':
```

### DataFlowIssue
Switch label `'1'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    break;
                case '0':
                case '1':
                case '2':
                case '3':
```

### DataFlowIssue
Switch label `'2'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '0':
                case '1':
                case '2':
                case '3':
                case '4':
```

### DataFlowIssue
Switch label `'3'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '1':
                case '2':
                case '3':
                case '4':
                case '5':
```

### DataFlowIssue
Switch label `'4'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '2':
                case '3':
                case '4':
                case '5':
                case '6':
```

### DataFlowIssue
Switch label `'5'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '3':
                case '4':
                case '5':
                case '6':
                case '7':
```

### DataFlowIssue
Switch label `'6'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '4':
                case '5':
                case '6':
                case '7':
                    {
```

### DataFlowIssue
Switch label `'7'` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                case '5':
                case '6':
                case '7':
                    {
                    alt23=3;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            }
            else if ( ((LA4_0>='1' && LA4_0<='9')) ) {
                alt4=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    }
                    else {
                        alt24=3;}
                }
                else if ( ((LA24_1>='4' && LA24_1<='7')) ) {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    }
                    else {
                        alt24=3;}
                }
                else {
```

### DataFlowIssue
Method invocation `isXML` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            super.setNull(stmnt, idx, Types.TIMESTAMP, col);
        // the Oracle driver does not support Types.OTHER with setNull
        else if (colType == Types.OTHER || col.isXML())
            super.setNull(stmnt, idx, Types.NULL, col);
        else
```

### DataFlowIssue
Condition `innerConn instanceof PGConnection` is redundant and can be replaced with a null check
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
    protected PGConnection getPGConnection(DelegatingConnection conn) {
        Connection innerConn = conn.getInnermostDelegate();
        if (innerConn instanceof PGConnection) {
            return (PGConnection) innerConn;
        }
```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
            return (PGConnection) innerConn;
        }
        if (innerConn.getClass().getName().startsWith("org.apache.commons.dbcp2")) {
            return (PGConnection) getDbcpDelegate(innerConn);
        }
```

### DataFlowIssue
Casting `obj` to `Number` may produce `ClassCastException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/ResultSetResult.java`
#### Snippet
```java
                    }
                }
                return _dict.getObject(_rs, ((Number) obj).intValue(), null);
        }
        return (_rs.wasNull()) ? null : val;
```

### DataFlowIssue
Method invocation `toLowerCase` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLServerDictionary.java`
#### Snippet
```java
            requiresWarnings) ||
            VENDOR_DATADIRECT.equalsIgnoreCase(driverVendor)) &&
            (url.toLowerCase(Locale.ENGLISH).indexOf("selectmethod=cursor") == -1))
            log.warn(_loc.get("sqlserver-cursor", url));

```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                    alias2 = _sel.getTableIndex(table2, this, false);
                    if (alias2 == -1)
                        createIndex = true;
                    else
                        created = true;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                }
                else if (context == _sel.ctx())
                   createIndex = true;
                else if (correlatedVar != null)
                    createIndex = false;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
                }
                else
                    mayBeDuplicate = false;
            }
            if (isDuplicate)
```

### DataFlowIssue
Method invocation `startsWith` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
            return true;
        String schemaName = DBIdentifier.isNull(schema) ? null : schema.getName();
        boolean startsWith_SYSTEM = schema.isDelimited() ? schemaName.startsWith("\"_SYSTEM") :
            schemaName.startsWith("_SYSTEM");

```

### DataFlowIssue
Method invocation `startsWith` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
        String schemaName = DBIdentifier.isNull(schema) ? null : schema.getName();
        boolean startsWith_SYSTEM = schema.isDelimited() ? schemaName.startsWith("\"_SYSTEM") :
            schemaName.startsWith("_SYSTEM");

        String seqName = DBIdentifier.isNull(name) ? null : name.getName();
```

### DataFlowIssue
Method invocation `startsWith` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java

        String seqName = DBIdentifier.isNull(name) ? null : name.getName();
        boolean startsWithSYS_SEQ_ = name.isDelimited() ? seqName.startsWith("\"SYS_SEQ_") :
            seqName.startsWith("SYS_SEQ_");
        if (startsWith_SYSTEM && startsWithSYS_SEQ_)
```

### DataFlowIssue
Method invocation `startsWith` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SolidDBDictionary.java`
#### Snippet
```java
        String seqName = DBIdentifier.isNull(name) ? null : name.getName();
        boolean startsWithSYS_SEQ_ = name.isDelimited() ? seqName.startsWith("\"SYS_SEQ_") :
            seqName.startsWith("SYS_SEQ_");
        if (startsWith_SYSTEM && startsWithSYS_SEQ_)
            return true;
```

### DataFlowIssue
Array access `sels[0]` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/LogicalUnion.java`
#### Snippet
```java
        if (sels != null && sels.length > 1)
            return true;
        return sels[0].hasMultipleSelects();
    }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MetaDataPlusMappingFactory.java`
#### Snippet
```java
     */
    public MetaDataPlusMappingFactory(MetaDataFactory meta, MetaDataFactory map) {
        this(meta, map, null);

    }
```

### DataFlowIssue
Method invocation `supportsGetGeneratedKeys` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            // Auto-detect generated keys retrieval support unless user specified it.
            if (supportsGetGeneratedKeys == null) {
                supportsGetGeneratedKeys =  (isJDBC3) ? metaData.supportsGetGeneratedKeys() : false;
            }
            if (log.isInfoEnabled()) {
```

### DataFlowIssue
Method invocation `getDatabaseProductName` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            if (log.isInfoEnabled()) {
                log.info(_loc.get("dict-info", new Object[] {
                        metaData.getDatabaseProductName(), getMajorVersion(), getMinorVersion(),
                        metaData.getDriverName(), metaData.getDriverVersion()}));
            }
```

### DataFlowIssue
Array access `commonCreateAlterSequenceSQL(seq, false)[0]` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java

    public String getAlterSequenceSQL(Sequence seq) {
        return commonCreateAlterSequenceSQL(seq, false)[0];
    }

```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                    setBlobObject(stmnt, idx, val, col, store);
                else
                    setObject(stmnt, idx, val, col.getType(), col);
        }
    }
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        switch (getMillisecondBehavior()) {
            case DROP :
                fractional = 0;
                break;
            case RETAIN :
```

### DataFlowIssue
Argument `in` might be null
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
    private Collection<String> loadFromResource(String resourcename) {
        try (InputStream in = DBDictionary.class.getResourceAsStream(resourcename)) {
            String keywords = new BufferedReader(new InputStreamReader(in)).readLine();
            return Arrays.asList(StringUtil.split(keywords, ",", 0));
        } catch (IOException ioe) {
```

### DataFlowIssue
Method invocation `values` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
            if (_mappingWriter != null) {
                PrintWriter out = new PrintWriter(_mappingWriter);
                for (String s : output.values()) {
                    out.println(s);
                }
```

### DataFlowIssue
Method invocation `setJoinForeignKey` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java

            sub = newClassMapping(table, base.getDescribedType());
            sub.setJoinForeignKey(fk);
            sub.setPrimaryKeyColumns(fk.getColumns());
            sub.setIdentityType(base.getIdentityType());
```

### DataFlowIssue
Argument `in` might be null
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
        try {
            String[] keywords = StringUtil.split(new BufferedReader
                (new InputStreamReader(in)).readLine(), ",", 0);

            for (int i = 0; i < keywords.length; i += 2)
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMapping.java`
#### Snippet
```java
        	PersistenceCapable pc = ImplHelper.toPersistenceCapable(obj,
                    getRepository().getConfiguration());
            sm = (OpenJPAStateManager) pc.pcGetStateManager();
            if (sm == null) {
            	ret = getValueFromUnmanagedInstance(obj, cols, true);
```

### DataFlowIssue
Method invocation `getIdentifier` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
            deferred = _unq.isDeferred();
        } else {
            name = tmplate.getIdentifier();
            deferred = tmplate.isDeferred();
        }
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/UntypedPCValueHandler.java`
#### Snippet
```java
            PersistenceCapable pc = ImplHelper.toPersistenceCapable(val,
                store.getConfiguration());
            if (pc.pcGetStateManager() != null)
                return pc.pcGetStateManager();
        }
```

### DataFlowIssue
Array access `((Object[]) rval)[idx++]` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ElementEmbedValueHandler.java`
#### Snippet
```java
            if (_cols.length == 1)
                return cval;
            ((Object[]) rval)[idx++] = cval;
        }

```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/StoreCollectionFieldStrategy.java`
#### Snippet
```java

            // for inverse relation field
            setMappedBy(oid.equals(sm.getObjectId()) ?
                sm.getPersistenceCapable() : oid, res);
            Object val = loadElement(null, store, fetch, res, dataJoins);
```

### DataFlowIssue
Method invocation `getContext` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/EmbedValueHandler.java`
#### Snippet
```java
            if (vm1.getEmbeddedMapping() != null) {
                if (em instanceof StateManagerImpl) {
                    em1 = store.getContext().embed(null, null, em, vm1);
                    idx = toObjectValue1(em1, vm1, val, store, fetch, cols, idx);
                }
```

### DataFlowIssue
Argument `stmnt` might be null
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java

                dict.setTimeouts(stmnt, fetch, false);
                ResultSet rs = executeQuery(store, conn, stmnt, buf, paramList);
                ResultSetResult res = stmnt != null ?
                    new ResultSetResult(conn, stmnt, rs, store) :
```

### DataFlowIssue
Method invocation `storeObject` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java

        if (oid == null)
            sm.storeObject(field.getIndex(), null);
        else
            sm.setIntermediate(field.getIndex(), oid);
```

### DataFlowIssue
Method invocation `setIntermediate` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/RelationFieldStrategy.java`
#### Snippet
```java
            sm.storeObject(field.getIndex(), null);
        else
            sm.setIntermediate(field.getIndex(), oid);
    }

```

### DataFlowIssue
Condition `select instanceof SelectImpl` is redundant and can be replaced with a null check
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedQueryImpl.java`
#### Snippet
```java

    private boolean isPaginated() {
        if (select instanceof SelectImpl) {
            if (((SelectImpl)select).getStartIndex() != 0 ||
                ((SelectImpl)select).getEndIndex() != Long.MAX_VALUE)
```

### DataFlowIssue
Argument `stmnt` might be null
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedStatementManagerImpl.java`
#### Snippet
```java
            row.flush(stmnt, _dict, _store);
        try {
            int count = executeUpdate(stmnt, sql, row);
            if (count != 1) {
                logSQLWarnings(stmnt);
```

### DataFlowIssue
Argument `stmnt` might be null
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderQueryImpl.java`
#### Snippet
```java
            }
            dict.setTimeouts(stmnt, (JDBCFetchConfiguration)fetch, forUpdate);
            rs = _select.executeQuery(conn, stmnt, getQueryString(), jstore,
                params, _pkCols);
            return _select.getEagerResult(conn, stmnt, rs, jstore,
```

### DataFlowIssue
Argument `ps` might be null
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
        if (ps != null)
            row.flush(ps, _dict, _store);
        int count = executeUpdate(ps, row.getSQL(_dict), row);
        if (count != 1) {
            logSQLWarnings(ps);
```

### DataFlowIssue
Argument `ps` might be null
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/BatchingPreparedStatementManagerImpl.java`
#### Snippet
```java
                                if (ps != null)
                                    onerow.flush(ps, _dict, _store);
                                addBatch(ps, onerow, count);
                                count++;
                            } else {
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/ConstPath.java`
#### Snippet
```java
                        cstate.value,
                        this.getMetaData().getRepository().getConfiguration())).
                        pcGetStateManager();
            if (sm == null) {
                tmpBroker = ctx.store.getContext().getBroker();
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( ((LA69_0>=56 && LA69_0<=63)) ) {
                alt69=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA12_0==46||LA12_0==73) ) {
                alt12=2;
            }
            else {
```

### DataFlowIssue
Switch label `26` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case 26:
                {
                alt39=1;
```

### DataFlowIssue
Switch label `28` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 28:
                {
                int LA39_2 = input.LA(2);
```

### DataFlowIssue
Switch label `44` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 44:
                {
                alt39=2;
```

### DataFlowIssue
Switch label `Identifier` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case Identifier:
            case ENUM:
            case 31:
```

### DataFlowIssue
Switch label `ENUM` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case Identifier:
            case ENUM:
            case 31:
            case 32:
```

### DataFlowIssue
Switch label `31` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case Identifier:
            case ENUM:
            case 31:
            case 32:
            case 33:
```

### DataFlowIssue
Switch label `32` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case ENUM:
            case 31:
            case 32:
            case 33:
            case 34:
```

### DataFlowIssue
Switch label `33` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 31:
            case 32:
            case 33:
            case 34:
            case 35:
```

### DataFlowIssue
Switch label `34` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 32:
            case 33:
            case 34:
            case 35:
            case 36:
```

### DataFlowIssue
Switch label `35` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 33:
            case 34:
            case 35:
            case 36:
            case 37:
```

### DataFlowIssue
Switch label `36` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 34:
            case 35:
            case 36:
            case 37:
            case 40:
```

### DataFlowIssue
Switch label `37` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 35:
            case 36:
            case 37:
            case 40:
            case 46:
```

### DataFlowIssue
Switch label `40` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 36:
            case 37:
            case 40:
            case 46:
            case 47:
```

### DataFlowIssue
Switch label `46` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 37:
            case 40:
            case 46:
            case 47:
            case 52:
```

### DataFlowIssue
Switch label `47` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 40:
            case 46:
            case 47:
            case 52:
            case 53:
```

### DataFlowIssue
Switch label `52` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 46:
            case 47:
            case 52:
            case 53:
            case 54:
```

### DataFlowIssue
Switch label `53` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 47:
            case 52:
            case 53:
            case 54:
            case 55:
```

### DataFlowIssue
Switch label `54` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 52:
            case 53:
            case 54:
            case 55:
            case 56:
```

### DataFlowIssue
Switch label `55` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 53:
            case 54:
            case 55:
            case 56:
            case 57:
```

### DataFlowIssue
Switch label `56` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 54:
            case 55:
            case 56:
            case 57:
            case 58:
```

### DataFlowIssue
Switch label `57` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 55:
            case 56:
            case 57:
            case 58:
            case 59:
```

### DataFlowIssue
Switch label `58` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 56:
            case 57:
            case 58:
            case 59:
            case 60:
```

### DataFlowIssue
Switch label `59` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 57:
            case 58:
            case 59:
            case 60:
            case 61:
```

### DataFlowIssue
Switch label `60` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 58:
            case 59:
            case 60:
            case 61:
            case 62:
```

### DataFlowIssue
Switch label `61` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 59:
            case 60:
            case 61:
            case 62:
            case 63:
```

### DataFlowIssue
Switch label `62` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 60:
            case 61:
            case 62:
            case 63:
            case 73:
```

### DataFlowIssue
Switch label `63` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 61:
            case 62:
            case 63:
            case 73:
                {
```

### DataFlowIssue
Switch label `73` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 62:
            case 63:
            case 73:
                {
                alt39=3;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    }
                    else if ( (LA110_0==82) ) {
                        alt110=3;
                    }
                    else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA32_0==73) ) {
                alt32=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                else if ( (LA137_2==Identifier||(LA137_2>=FloatingPointLiteral && LA137_2<=DecimalLiteral)||LA137_2==47||(LA137_2>=56 && LA137_2<=63)||(LA137_2>=65 && LA137_2<=66)||(LA137_2>=69 && LA137_2<=72)||(LA137_2>=105 && LA137_2<=106)||(LA137_2>=109 && LA137_2<=113)) ) {
                    alt137=4;
                }
                else {
```

### DataFlowIssue
Switch label `Identifier` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case Identifier:
            case 56:
            case 57:
```

### DataFlowIssue
Switch label `56` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            switch ( input.LA(1) ) {
            case Identifier:
            case 56:
            case 57:
            case 58:
```

### DataFlowIssue
Switch label `57` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case Identifier:
            case 56:
            case 57:
            case 58:
            case 59:
```

### DataFlowIssue
Switch label `58` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 56:
            case 57:
            case 58:
            case 59:
            case 60:
```

### DataFlowIssue
Switch label `59` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 57:
            case 58:
            case 59:
            case 60:
            case 61:
```

### DataFlowIssue
Switch label `60` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 58:
            case 59:
            case 60:
            case 61:
            case 62:
```

### DataFlowIssue
Switch label `61` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 59:
            case 60:
            case 61:
            case 62:
            case 63:
```

### DataFlowIssue
Switch label `62` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 60:
            case 61:
            case 62:
            case 63:
                {
```

### DataFlowIssue
Switch label `63` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 61:
            case 62:
            case 63:
                {
                alt45=1;
```

### DataFlowIssue
Switch label `40` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 40:
                {
                alt45=2;
```

### DataFlowIssue
Switch label `47` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 47:
                {
                alt45=3;
```

### DataFlowIssue
Switch label `46` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 46:
            case 73:
                {
```

### DataFlowIssue
Switch label `73` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case 46:
            case 73:
                {
                alt45=4;
```

### DataFlowIssue
Switch label `ENUM` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case ENUM:
            case 37:
                {
```

### DataFlowIssue
Switch label `37` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case ENUM:
            case 37:
                {
                alt45=5;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA11_0==26) ) {
                alt11=2;
            }
            else {
```

### DataFlowIssue
Switch label `Identifier` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case Identifier:
            case 56:
            case 57:
```

### DataFlowIssue
Switch label `56` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            switch ( input.LA(1) ) {
            case Identifier:
            case 56:
            case 57:
            case 58:
```

### DataFlowIssue
Switch label `57` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case Identifier:
            case 56:
            case 57:
            case 58:
            case 59:
```

### DataFlowIssue
Switch label `58` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 56:
            case 57:
            case 58:
            case 59:
            case 60:
```

### DataFlowIssue
Switch label `59` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 57:
            case 58:
            case 59:
            case 60:
            case 61:
```

### DataFlowIssue
Switch label `60` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 58:
            case 59:
            case 60:
            case 61:
            case 62:
```

### DataFlowIssue
Switch label `61` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 59:
            case 60:
            case 61:
            case 62:
            case 63:
```

### DataFlowIssue
Switch label `62` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 60:
            case 61:
            case 62:
            case 63:
                {
```

### DataFlowIssue
Switch label `63` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 61:
            case 62:
            case 63:
                {
                alt102=1;
```

### DataFlowIssue
Switch label `37` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 37:
                {
                alt102=2;
```

### DataFlowIssue
Switch label `46` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 46:
                {
                alt102=3;
```

### DataFlowIssue
Switch label `ENUM` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case ENUM:
                {
                alt102=4;
```

### DataFlowIssue
Switch label `73` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 73:
                {
                alt102=5;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA158_0==Identifier||(LA158_0>=56 && LA158_0<=63)) ) {
                alt158=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    }
                    else if ( (LA157_0==66) ) {
                        alt157=2;
                    }
                    else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA54_0==47) ) {
                alt54=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                else if ( (LA41_1==26||LA41_1==41||LA41_1==48||LA41_1==51) ) {
                    alt41=2;
                }
                else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                else if ( (LA103_1==26||LA103_1==41||LA103_1==48||LA103_1==51) ) {
                    alt103=2;
                }
                else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA76_0==64) ) {
                alt76=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        }
        else if ( (LA176_0==ENUM||LA176_0==28||(LA176_0>=31 && LA176_0<=37)||LA176_0==46||LA176_0==73) ) {
            alt176=2;
        }
        else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA80_0==68) ) {
                alt80=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA169_0==29) ) {
                alt169=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA164_0==Identifier||(LA164_0>=FloatingPointLiteral && LA164_0<=DecimalLiteral)||LA164_0==47||(LA164_0>=56 && LA164_0<=63)||(LA164_0>=65 && LA164_0<=66)||(LA164_0>=69 && LA164_0<=72)||(LA164_0>=105 && LA164_0<=106)||(LA164_0>=109 && LA164_0<=113)) ) {
                alt164=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA119_0==74) ) {
                alt119=3;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA51_0==26) ) {
                alt51=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA44_0==26) ) {
                alt44=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                else if ( (LA43_1==66) ) {
                    alt43=2;
                }
                else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    }
                    else if ( (LA42_0==47) ) {
                        alt42=2;
                    }
                    else {
```

### DataFlowIssue
Switch label `66` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case 66:
                {
                alt153=1;
```

### DataFlowIssue
Switch label `69` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 69:
                {
                alt153=2;
```

### DataFlowIssue
Switch label `65` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 65:
                {
                alt153=3;
```

### DataFlowIssue
Switch label `FloatingPointLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
```

### DataFlowIssue
Switch label `CharacterLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
```

### DataFlowIssue
Switch label `StringLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
```

### DataFlowIssue
Switch label `HexLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
```

### DataFlowIssue
Switch label `OctalLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
            case 70:
```

### DataFlowIssue
Switch label `DecimalLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
            case 70:
            case 71:
```

### DataFlowIssue
Switch label `70` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case OctalLiteral:
            case DecimalLiteral:
            case 70:
            case 71:
            case 72:
```

### DataFlowIssue
Switch label `71` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case DecimalLiteral:
            case 70:
            case 71:
            case 72:
                {
```

### DataFlowIssue
Switch label `72` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 70:
            case 71:
            case 72:
                {
                alt153=4;
```

### DataFlowIssue
Switch label `113` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 113:
                {
                alt153=5;
```

### DataFlowIssue
Switch label `Identifier` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case Identifier:
                {
                alt153=6;
```

### DataFlowIssue
Switch label `56` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 56:
            case 57:
            case 58:
```

### DataFlowIssue
Switch label `57` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case 56:
            case 57:
            case 58:
            case 59:
```

### DataFlowIssue
Switch label `58` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 56:
            case 57:
            case 58:
            case 59:
            case 60:
```

### DataFlowIssue
Switch label `59` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 57:
            case 58:
            case 59:
            case 60:
            case 61:
```

### DataFlowIssue
Switch label `60` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 58:
            case 59:
            case 60:
            case 61:
            case 62:
```

### DataFlowIssue
Switch label `61` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 59:
            case 60:
            case 61:
            case 62:
            case 63:
```

### DataFlowIssue
Switch label `62` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 60:
            case 61:
            case 62:
            case 63:
                {
```

### DataFlowIssue
Switch label `63` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 61:
            case 62:
            case 63:
                {
                alt153=7;
```

### DataFlowIssue
Switch label `47` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 47:
                {
                alt153=8;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA16_0==ENUM) ) {
                alt16=2;
            }
            else {
```

### DataFlowIssue
Switch label `Identifier` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case Identifier:
            case FloatingPointLiteral:
            case CharacterLiteral:
```

### DataFlowIssue
Switch label `FloatingPointLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            switch ( input.LA(1) ) {
            case Identifier:
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
```

### DataFlowIssue
Switch label `CharacterLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case Identifier:
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
```

### DataFlowIssue
Switch label `StringLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
```

### DataFlowIssue
Switch label `HexLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
```

### DataFlowIssue
Switch label `OctalLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
            case 47:
```

### DataFlowIssue
Switch label `DecimalLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
            case 47:
            case 56:
```

### DataFlowIssue
Switch label `47` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case OctalLiteral:
            case DecimalLiteral:
            case 47:
            case 56:
            case 57:
```

### DataFlowIssue
Switch label `56` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case DecimalLiteral:
            case 47:
            case 56:
            case 57:
            case 58:
```

### DataFlowIssue
Switch label `57` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 47:
            case 56:
            case 57:
            case 58:
            case 59:
```

### DataFlowIssue
Switch label `58` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 56:
            case 57:
            case 58:
            case 59:
            case 60:
```

### DataFlowIssue
Switch label `59` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 57:
            case 58:
            case 59:
            case 60:
            case 61:
```

### DataFlowIssue
Switch label `60` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 58:
            case 59:
            case 60:
            case 61:
            case 62:
```

### DataFlowIssue
Switch label `61` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 59:
            case 60:
            case 61:
            case 62:
            case 63:
```

### DataFlowIssue
Switch label `62` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 60:
            case 61:
            case 62:
            case 63:
            case 65:
```

### DataFlowIssue
Switch label `63` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 61:
            case 62:
            case 63:
            case 65:
            case 66:
```

### DataFlowIssue
Switch label `65` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 62:
            case 63:
            case 65:
            case 66:
            case 69:
```

### DataFlowIssue
Switch label `66` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 63:
            case 65:
            case 66:
            case 69:
            case 70:
```

### DataFlowIssue
Switch label `69` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 65:
            case 66:
            case 69:
            case 70:
            case 71:
```

### DataFlowIssue
Switch label `70` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 66:
            case 69:
            case 70:
            case 71:
            case 72:
```

### DataFlowIssue
Switch label `71` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 69:
            case 70:
            case 71:
            case 72:
            case 105:
```

### DataFlowIssue
Switch label `72` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 70:
            case 71:
            case 72:
            case 105:
            case 106:
```

### DataFlowIssue
Switch label `105` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 71:
            case 72:
            case 105:
            case 106:
            case 109:
```

### DataFlowIssue
Switch label `106` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 72:
            case 105:
            case 106:
            case 109:
            case 110:
```

### DataFlowIssue
Switch label `109` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 105:
            case 106:
            case 109:
            case 110:
            case 111:
```

### DataFlowIssue
Switch label `110` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 106:
            case 109:
            case 110:
            case 111:
            case 112:
```

### DataFlowIssue
Switch label `111` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 109:
            case 110:
            case 111:
            case 112:
            case 113:
```

### DataFlowIssue
Switch label `112` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 110:
            case 111:
            case 112:
            case 113:
                {
```

### DataFlowIssue
Switch label `113` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 111:
            case 112:
            case 113:
                {
                alt93=1;
```

### DataFlowIssue
Switch label `73` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 73:
                {
                alt93=2;
```

### DataFlowIssue
Switch label `44` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 44:
                {
                alt93=3;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA46_0==66) ) {
                alt46=2;
            }
            else {
```

### DataFlowIssue
Switch label `73` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case 73:
                {
                alt14=1;
```

### DataFlowIssue
Switch label `31` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 31:
                {
                alt14=2;
```

### DataFlowIssue
Switch label `32` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 32:
                {
                alt14=3;
```

### DataFlowIssue
Switch label `33` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 33:
                {
                alt14=4;
```

### DataFlowIssue
Switch label `34` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 34:
                {
                alt14=5;
```

### DataFlowIssue
Switch label `28` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 28:
                {
                alt14=6;
```

### DataFlowIssue
Switch label `35` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 35:
                {
                alt14=7;
```

### DataFlowIssue
Switch label `36` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 36:
                {
                alt14=8;
```

### DataFlowIssue
Switch label `105` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case 105:
                {
                alt142=1;
```

### DataFlowIssue
Switch label `106` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 106:
                {
                alt142=2;
```

### DataFlowIssue
Switch label `109` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 109:
                {
                alt142=3;
```

### DataFlowIssue
Switch label `110` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 110:
                {
                alt142=4;
```

### DataFlowIssue
Switch label `Identifier` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case Identifier:
            case FloatingPointLiteral:
            case CharacterLiteral:
```

### DataFlowIssue
Switch label `FloatingPointLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case Identifier:
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
```

### DataFlowIssue
Switch label `CharacterLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case Identifier:
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
```

### DataFlowIssue
Switch label `StringLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case FloatingPointLiteral:
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
```

### DataFlowIssue
Switch label `HexLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case CharacterLiteral:
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
```

### DataFlowIssue
Switch label `OctalLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case StringLiteral:
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
            case 47:
```

### DataFlowIssue
Switch label `DecimalLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
            case 47:
            case 56:
```

### DataFlowIssue
Switch label `47` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case OctalLiteral:
            case DecimalLiteral:
            case 47:
            case 56:
            case 57:
```

### DataFlowIssue
Switch label `56` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case DecimalLiteral:
            case 47:
            case 56:
            case 57:
            case 58:
```

### DataFlowIssue
Switch label `57` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 47:
            case 56:
            case 57:
            case 58:
            case 59:
```

### DataFlowIssue
Switch label `58` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 56:
            case 57:
            case 58:
            case 59:
            case 60:
```

### DataFlowIssue
Switch label `59` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 57:
            case 58:
            case 59:
            case 60:
            case 61:
```

### DataFlowIssue
Switch label `60` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 58:
            case 59:
            case 60:
            case 61:
            case 62:
```

### DataFlowIssue
Switch label `61` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 59:
            case 60:
            case 61:
            case 62:
            case 63:
```

### DataFlowIssue
Switch label `62` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 60:
            case 61:
            case 62:
            case 63:
            case 65:
```

### DataFlowIssue
Switch label `63` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 61:
            case 62:
            case 63:
            case 65:
            case 66:
```

### DataFlowIssue
Switch label `65` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 62:
            case 63:
            case 65:
            case 66:
            case 69:
```

### DataFlowIssue
Switch label `66` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 63:
            case 65:
            case 66:
            case 69:
            case 70:
```

### DataFlowIssue
Switch label `69` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 65:
            case 66:
            case 69:
            case 70:
            case 71:
```

### DataFlowIssue
Switch label `70` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 66:
            case 69:
            case 70:
            case 71:
            case 72:
```

### DataFlowIssue
Switch label `71` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 69:
            case 70:
            case 71:
            case 72:
            case 111:
```

### DataFlowIssue
Switch label `72` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 70:
            case 71:
            case 72:
            case 111:
            case 112:
```

### DataFlowIssue
Switch label `111` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 71:
            case 72:
            case 111:
            case 112:
            case 113:
```

### DataFlowIssue
Switch label `112` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 72:
            case 111:
            case 112:
            case 113:
                {
```

### DataFlowIssue
Switch label `113` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 111:
            case 112:
            case 113:
                {
                alt142=5;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA73_0==73) ) {
                alt73=2;
            }
            else {
```

### DataFlowIssue
Switch label `40` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case 40:
                {
                alt40=1;
```

### DataFlowIssue
Switch label `Identifier` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case Identifier:
                {
                int LA40_2 = input.LA(2);
```

### DataFlowIssue
Switch label `56` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 56:
            case 57:
            case 58:
```

### DataFlowIssue
Switch label `57` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case 56:
            case 57:
            case 58:
            case 59:
```

### DataFlowIssue
Switch label `58` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 56:
            case 57:
            case 58:
            case 59:
            case 60:
```

### DataFlowIssue
Switch label `59` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 57:
            case 58:
            case 59:
            case 60:
            case 61:
```

### DataFlowIssue
Switch label `60` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 58:
            case 59:
            case 60:
            case 61:
            case 62:
```

### DataFlowIssue
Switch label `61` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 59:
            case 60:
            case 61:
            case 62:
            case 63:
```

### DataFlowIssue
Switch label `62` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 60:
            case 61:
            case 62:
            case 63:
                {
```

### DataFlowIssue
Switch label `63` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case 61:
            case 62:
            case 63:
                {
                alt40=2;
```

### DataFlowIssue
Switch label `47` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 47:
                {
                alt40=3;
```

### DataFlowIssue
Switch label `46` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 46:
            case 73:
                {
```

### DataFlowIssue
Switch label `73` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case 46:
            case 73:
                {
                alt40=5;
```

### DataFlowIssue
Switch label `ENUM` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case ENUM:
            case 37:
                {
```

### DataFlowIssue
Switch label `37` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case ENUM:
            case 37:
                {
                alt40=6;
```

### DataFlowIssue
Switch label `73` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case 73:
                {
                alt66=1;
```

### DataFlowIssue
Switch label `31` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 31:
                {
                alt66=2;
```

### DataFlowIssue
Switch label `32` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 32:
                {
                alt66=3;
```

### DataFlowIssue
Switch label `33` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 33:
                {
                alt66=4;
```

### DataFlowIssue
Switch label `28` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 28:
                {
                alt66=5;
```

### DataFlowIssue
Switch label `34` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 34:
                {
                alt66=6;
```

### DataFlowIssue
Switch label `35` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 35:
                {
                alt66=7;
```

### DataFlowIssue
Switch label `52` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 52:
                {
                alt66=8;
```

### DataFlowIssue
Switch label `53` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 53:
                {
                alt66=9;
```

### DataFlowIssue
Switch label `54` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 54:
                {
                alt66=10;
```

### DataFlowIssue
Switch label `55` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 55:
                {
                alt66=11;
```

### DataFlowIssue
Switch label `36` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 36:
                {
                alt66=12;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA49_0==26) ) {
                alt49=2;
            }
            else {
```

### DataFlowIssue
Switch label `HexLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java

            switch ( input.LA(1) ) {
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
```

### DataFlowIssue
Switch label `OctalLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            switch ( input.LA(1) ) {
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
                {
```

### DataFlowIssue
Switch label `DecimalLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            case HexLiteral:
            case OctalLiteral:
            case DecimalLiteral:
                {
                alt87=1;
```

### DataFlowIssue
Switch label `FloatingPointLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case FloatingPointLiteral:
                {
                alt87=2;
```

### DataFlowIssue
Switch label `CharacterLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case CharacterLiteral:
                {
                alt87=3;
```

### DataFlowIssue
Switch label `StringLiteral` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case StringLiteral:
                {
                alt87=4;
```

### DataFlowIssue
Switch label `71` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 71:
            case 72:
                {
```

### DataFlowIssue
Switch label `72` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                break;
            case 71:
            case 72:
                {
                alt87=5;
```

### DataFlowIssue
Switch label `70` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                break;
            case 70:
                {
                alt87=6;
```

### DataFlowIssue
Switch label `Identifier` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            if ( (LA167_0==29) ) {
                switch ( input.LA(2) ) {
                case Identifier:
                    {
                    alt167=1;
```

### DataFlowIssue
Switch label `69` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    }
                    break;
                case 69:
                    {
                    alt167=2;
```

### DataFlowIssue
Switch label `65` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    }
                    break;
                case 65:
                    {
                    alt167=3;
```

### DataFlowIssue
Switch label `113` is unreachable
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    }
                    break;
                case 113:
                    {
                    alt167=4;
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA167_0==48) ) {
                alt167=5;
            }
            else {
```

### DataFlowIssue
Switch label `5` is the only reachable in the whole switch
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    }
                    break;
                case 5 :
                    dbg.enterAlt(5);

```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    }
                    else if ( (LA4_0==ENUM||LA4_0==28||(LA4_0>=31 && LA4_0<=37)||LA4_0==46||LA4_0==73) ) {
                        alt4=2;
                    }
                    else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( ((LA159_0>=56 && LA159_0<=63)) ) {
                alt159=2;
            }
            else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                }
                else if ( (true) ) {
                    alt147=2;
                }
                else {
```

### DataFlowIssue
Variable is already assigned to this value
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            }
            else if ( (LA62_0==Identifier||(LA62_0>=FloatingPointLiteral && LA62_0<=DecimalLiteral)||LA62_0==47||(LA62_0>=56 && LA62_0<=63)||(LA62_0>=65 && LA62_0<=66)||(LA62_0>=69 && LA62_0<=72)||(LA62_0>=105 && LA62_0<=106)||(LA62_0>=109 && LA62_0<=113)) ) {
                alt62=2;
            }
            else {
```

### DataFlowIssue
Method invocation `getSubselContexts` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/SelectConstructor.java`
#### Snippet
```java
            sel.setContext(lctx);

        if (parent == null && lctx.getSubselContexts() != null) {
            // this is the case subselect was created before parent got created
            List<Context> subselCtxs = lctx.getSubselContexts();
```

### DataFlowIssue
Method invocation `indexOf` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Concat.java`
#### Snippet
```java
        String func = dict.concatenateFunction;
        dict.assertSupport(func != null, "ConcatenateFunction");
        int part1idx = func.indexOf("{0}");
        int part2idx = func.indexOf("{1}");
        String part1 = func.substring(0, Math.min(part1idx, part2idx));
```

### DataFlowIssue
Method invocation `pcFetchObjectId` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/ConstGetObjectId.java`
#### Snippet
```java
        return (ImplHelper.toPersistenceCapable(o,
            this.getMetaData().getRepository().getConfiguration()))
            .pcFetchObjectId();
    }

```

### DataFlowIssue
Method invocation `getBaseMapping` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
            Class<?> type;
            if ((type = getType(res, mapping)) == null) {
                if (res.getBaseMapping() != null)
                    mapping = res.getBaseMapping();
                res.startDataRequest(mapping.getDiscriminator());
```

### DataFlowIssue
Method invocation `isPrimitive` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/UnaryOp.java`
#### Snippet
```java
                return null;
            }
            else if (type.isPrimitive() || Number.class.isAssignableFrom(type)) {
                value = Filters.getDefaultForNull(Filters.wrap(type));
            }
```

### DataFlowIssue
Argument `XMLSchemaParser.class .getResourceAsStream("schemas-doctype.rsrc")` might be null
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
    protected Reader getDocType()
        throws IOException {
        return new InputStreamReader(XMLSchemaParser.class
            .getResourceAsStream("schemas-doctype.rsrc"));
    }

```

### DataFlowIssue
Method invocation `getKey` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
                return mapping.getObjectId(ctx.store, res, null, true,
                    pstate.joins);
            if (_key && pstate.field.getKey().isEmbedded())
                return loadEmbeddedMapKey(ctx, state, res);
            if (pstate.isEmbedElementColl)
```

### DataFlowIssue
Method invocation `loadProjection` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
                return loadEmbeddedMapKey(ctx, state, res);
            if (pstate.isEmbedElementColl)
                return pstate.field.loadProjection(ctx.store, ctx.fetch, res,
                    pstate.joins);
            return res.load(mapping, ctx.store, ctx.fetch, pstate.joins);
```

### DataFlowIssue
Dereference of `lastAction` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
    public void verifyIndexedField() {
        Action lastAction = (Action) lastFieldAction();
        FieldMapping fm = (FieldMapping) lastAction.data;
        if (fm.getOrderColumn() == null)
            throw new UserException(_loc.get("no-order-column", fm.getName()));
```

### DataFlowIssue
Method invocation `getTable` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
                    pstate.joins = pstate.joins.setVariable(var.getName());

                pstate.joins = pstate.joins.crossJoin(_candidate.getTable(),
                    rel.getTable());
                if (!itr.hasNext() && isVariable()) {
```

### DataFlowIssue
Dereference of `action` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
        Action key = new Action();
        key.op = Action.GET_KEY;
        key.data = action.data;
        int pos = _actions.indexOf(action);
        _actions.remove(action);
```

### DataFlowIssue
Method invocation `pcGetStateManager` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
                sm = (OpenJPAStateManager) (ImplHelper.toPersistenceCapable(
                        candidate, ctx.getConfiguration())).
                        pcGetStateManager();
            if (sm == null) {
                tmpBroker = ctx.getBroker();
```

### DataFlowIssue
Dereference of `tableNames` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
        // if database can't handle null table name, recurse on each known name
        if (cols == null && DBIdentifier.isNull(tableName)) {
            for (DBIdentifier name : tableNames) {
                generateTables(schemaName, name, conn, meta);
            }
```

### DataFlowIssue
Condition `sName instanceof DBIdentifier` is redundant and can be replaced with a null check
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/identifier/QualifiedDBIdentifier.java`
#### Snippet
```java
            return names.toArray(new DBIdentifier[names.size()]);
        }
        if (sName instanceof DBIdentifier) {
            return new DBIdentifier[] { sName.clone() };
        }
```

### DataFlowIssue
Method invocation `remove` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
                _locals = null;
                _pks = null;
                _joins.remove(rem);
                remd = true;
            }
```

### DataFlowIssue
Dereference of `locCols` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
                        }
                    }
                    for (int j = 0; j < locCols.length; j++) {
                        if (!fkTemp.containsColumn(
                                localtable.getColumn(locCols[j].getIdentifier()))) {
```

### DataFlowIssue
Array access `pkCols[j]` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
                                localtable.getColumn(locCols[j].getIdentifier()))) {
                            fkTemp.join(localtable.getColumn(locCols[j].getIdentifier()),
                                    pkTable.getColumn(pkCols[j].getIdentifier()));
                        }
                    }
```

### DataFlowIssue
Method invocation `getColumn` may produce `NullPointerException`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
            for (int i = 0; i < cols.length; i++)
                copy.join(getColumn(cols[i].getIdentifier()),
                    joined.getColumn(pks[i].getIdentifier()));

            cols = fk.getConstantColumns();
```

### DataFlowIssue
Dereference of `futures` may produce `NullPointerException`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedStoreQuery.java`
#### Snippet
```java
				futures.add(threadPool.submit(call));
			}
			for (Future<Number> future : futures) {
				try {
					Number n = future.get();
```

### DataFlowIssue
Method invocation `syncVersion` may produce `NullPointerException`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
        for (String replica : targets) {
            SliceStoreManager slice = lookup(replica);
            sync &= slice.syncVersion(sm, edata);
        }
        return sync;
```

### DataFlowIssue
Method invocation `assignField` may produce `NullPointerException`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
    public boolean assignField(OpenJPAStateManager sm, int field,
            boolean preFlush) {
        return selectStore(sm, null).assignField(sm, field, preFlush);
    }

```

### DataFlowIssue
Method invocation `load` may produce `NullPointerException`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
    public boolean load(OpenJPAStateManager sm, BitSet fields,
            FetchConfiguration fetch, int lockLevel, Object edata) {
        return selectStore(sm, edata).load(sm, fields, fetch, lockLevel, edata);
    }

```

### DataFlowIssue
Method invocation `initialize` may produce `NullPointerException`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
            String origin = estimateSlice(sm, edata);
            if (origin != null) {
                if (lookup(origin).initialize(sm, state, fetch, edata)) {
                    assignSlice(sm, origin);
                    return true;
```

### DataFlowIssue
Method invocation `compareVersion` may produce `NullPointerException`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
    @Override
    public int compareVersion(OpenJPAStateManager sm, Object v1, Object v2) {
        return selectStore(sm, null).compareVersion(sm, v1, v2);
    }

```

### DataFlowIssue
Dereference of `futures` may produce `NullPointerException`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
				futures.add(threadPool.submit(call));
			}
			for (Future<Number> future : futures) {
				try {
					Number n = future.get();
```

### DataFlowIssue
Dereference of `futures` may produce `NullPointerException`
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedSQLStoreQuery.java`
#### Snippet
```java
				futures.add(threadPool.submit(call));
			}
			for (Future<Number> future : futures) {
				try {
					Number n = future.get();
```

### DataFlowIssue
Method invocation `getId` may produce `NullPointerException`
in `openjpa-examples/openbooks/web/checkout.jsp`
#### Snippet
```java
<p>
<table>
  <caption>Order : <%= order.getId() %> on <%= JSPUtility.format(order.getPlacedOn()) %>
     for <%= order.getItems().size() %> Book<%= order.getItems().size() == 0 ? "" : "s" %>
  </caption>
```

### DataFlowIssue
Method invocation `select` may produce `NullPointerException`
in `openjpa-examples/openbooks/web/query.jsp`
#### Snippet
```java
  String author   = request.getParameter(FORM_AUTHOR);

  List<Book> books = service.select(title, minPrice, maxPrice, author);
  String query = service.getQuery(title, minPrice, maxPrice, author);
%>
```

### DataFlowIssue
Method invocation `deliver` may produce `NullPointerException`
in `openjpa-examples/openbooks/web/orders.jsp`
#### Snippet
```java
       String oid = request.getParameter(KEY_OID);
       PurchaseOrder order = (PurchaseOrder)session.getAttribute(oid);
       service.deliver(order);
   }

```

### DataFlowIssue
Method invocation `getOrders` may produce `NullPointerException`
in `openjpa-examples/openbooks/web/orders.jsp`
#### Snippet
```java

   Customer customer = (Customer)session.getAttribute(KEY_USER);
   List<PurchaseOrder> pendingOrders   = service.getOrders(PurchaseOrder.Status.DELIVERED, customer);
   List<PurchaseOrder> deliveredOrders = service.getOrders(PurchaseOrder.Status.PENDING, customer);
   List<PurchaseOrder> orders = new ArrayList<PurchaseOrder>(pendingOrders);
```

### DataFlowIssue
Method invocation `count` may produce `NullPointerException`
in `openjpa-examples/openbooks/web/search.jsp`
#### Snippet
```java
   <ol>
   <li>You can leave one, more or all fields empty.</li>
   <li>OpenBooks database currently contains <%= service.count(Book.class) %> books.</li>
   <li>Book titles are <code>Book-1</code>, <code>Book-2</code>, <code>Book-3</code>,...</li>
   <li>Author names are <code>Author-1</code>, <code>Author-2</code>, <code>Author-3</code>,...</li>
```

### DataFlowIssue
Switch label `Types.OTHER` is unreachable
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
                    case Types.LONGVARBINARY:
                    case Types.VARBINARY:
                    case Types.OTHER:
                        return true;
                    default:
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'getLRSSize' is still used
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/JDBCFetchPlan.java`
#### Snippet
```java
     * @deprecated use {@link #getLRSSizeAlgorithm()} instead.
     */
    @Deprecated int getLRSSize();

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'loadBCClass' is still used
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/GeneratedClasses.java`
#### Snippet
```java
     * @deprecated move to ASM
     */
    public static Class loadBCClass(BCClass bc, ClassLoader loader) {
        BCClassLoader bcloader = AccessController
                .doPrivileged(J2DoPrivHelper.newBCClassLoaderAction(bc
```

### DeprecatedIsStillUsed
Deprecated member 'DETACH_FGS' is still used
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachState.java`
#### Snippet
```java
     * @deprecated
     */
    @Deprecated int DETACH_FGS = 0;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'StringContains' is still used
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/StringContains.java`
#### Snippet
```java
 */
@Deprecated
public class StringContains
    implements FilterListener {

```

### DeprecatedIsStillUsed
Deprecated member 'WildcardMatch' is still used
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/WildcardMatch.java`
#### Snippet
```java
 */
@Deprecated
public class WildcardMatch
    implements FilterListener {

```

### DeprecatedIsStillUsed
Deprecated member 'logSupportsSetClobWarning' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    private boolean logSupportsSetClobWarning = true;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'useTriggersForAutoAssign' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    public boolean useTriggersForAutoAssign = false;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'supportsSetClob' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    public boolean supportsSetClob = false;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'autoAssignSequenceName' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    public String autoAssignSequenceName = null;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'setDiscriminatorColumnName' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingDefaultsImpl.java`
#### Snippet
```java
     */
    @Deprecated
    public void setDiscriminatorColumnName(String discName) {
        setDiscriminatorColumnIdentifier(DBIdentifier.newColumn(discName));
    }
```

### DeprecatedIsStillUsed
Deprecated member 'roundTimeToMillisec' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    public boolean roundTimeToMillisec = true;

    /*
```

### DeprecatedIsStillUsed
Deprecated member 'getValidTableName' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    public String getValidTableName(String name, Schema schema) {
        return getValidTableName(DBIdentifier.newTable(name), schema).getName();
    }
```

### DeprecatedIsStillUsed
Deprecated member 'useNativeSequenceCache' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    public boolean useNativeSequenceCache = true;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'makeNameValid' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    protected String makeNameValid(String name, NameSet set, int maxLen,
        int nameType) {
        return makeNameValid(name, set, maxLen, nameType, true);
```

### DeprecatedIsStillUsed
Deprecated member 'logNativeSequenceCacheWarning' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
     */
    @Deprecated
    private boolean logNativeSequenceCacheWarning = true;

    protected JDBCConfiguration conf = null;
```

### DeprecatedIsStillUsed
Deprecated member 'getSecondaryTableNames' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
     */
    @Deprecated
    public String[] getSecondaryTableNames() {
        if (_seconds == null)
            return new String[0];
```

### DeprecatedIsStillUsed
Deprecated member 'getSecondaryTableJoinColumns' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ClassMappingInfo.java`
#### Snippet
```java
     */
    @Deprecated
    public List<Column> getSecondaryTableJoinColumns(String tableName) {
        return getSecondaryTableJoinColumns(DBIdentifier.newTable(tableName));
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getSequenceColumn' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/SequenceMapping.java`
#### Snippet
```java
     */
    @Deprecated
    public String getSequenceColumn() {
        return getSequenceColumnIdentifier().getName();
    }
```

### DeprecatedIsStillUsed
Deprecated member 'JDBCStringContains' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/JDBCStringContains.java`
#### Snippet
```java
 */
@Deprecated
public class JDBCStringContains
    extends StringContains
    implements JDBCFilterListener {
```

### DeprecatedIsStillUsed
Deprecated member 'SQLExpression' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/SQLExpression.java`
#### Snippet
```java
 */
@Deprecated
public class SQLExpression
    extends SQLEmbed {

```

### DeprecatedIsStillUsed
Deprecated member 'getNullIndicatorColumn' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ValueMappingInfo.java`
#### Snippet
```java
     */
    @Deprecated
    public Column getNullIndicatorColumn(ValueMapping val, String name,
        Table table, boolean adapt) {
        return getNullIndicatorColumn(val, DBIdentifier.newColumn(name), table, adapt);
```

### DeprecatedIsStillUsed
Deprecated member 'getTypeJoin' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ValueMappingInfo.java`
#### Snippet
```java
     */
    @Deprecated
    public ForeignKey getTypeJoin(final ValueMapping val, final String name,
        boolean inversable, boolean adapt) {
        return getTypeJoin(val, DBIdentifier.newForeignKey(name), inversable, adapt);
```

### DeprecatedIsStillUsed
Deprecated member 'getInverseTypeJoin' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ValueMappingInfo.java`
#### Snippet
```java
     */
    @Deprecated
    public ForeignKey getInverseTypeJoin(final ValueMapping val,
        final String name, boolean adapt) {
        return getInverseTypeJoin(val, DBIdentifier.newForeignKey(name), adapt);
```

### DeprecatedIsStillUsed
Deprecated member 'SQLValue' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/SQLValue.java`
#### Snippet
```java
 */
@Deprecated
public class SQLValue
    extends SQLEmbed {

```

### DeprecatedIsStillUsed
Deprecated member 'JDBCWildcardMatch' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/JDBCWildcardMatch.java`
#### Snippet
```java
 */
@Deprecated
public class JDBCWildcardMatch
    extends WildcardMatch
    implements JDBCFilterListener {
```

### DeprecatedIsStillUsed
Deprecated member 'findSequence' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGroup.java`
#### Snippet
```java
     */
    @Deprecated
    public Sequence findSequence(String name) {
        if (name == null)
            return null;
```

### DeprecatedIsStillUsed
Deprecated member 'isNameTaken' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/NameSet.java`
#### Snippet
```java
     */
    @Deprecated
    public boolean isNameTaken(String name) {
        return isNameTaken(DBIdentifier.toUpper(DBIdentifier.newDefault(name)));
    }
```

### DeprecatedIsStillUsed
Deprecated member 'addSequence' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Schema.java`
#### Snippet
```java
     */
    @Deprecated
    public Sequence addSequence(String name) {
        return addSequence(DBIdentifier.newIdentifier(name, DBIdentifierType.SEQUENCE, true));
    }
```

### DeprecatedIsStillUsed
Deprecated member 'loadNameFromDB' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
     */
    @Deprecated
    public String loadNameFromDB(DBDictionary dbdict, Connection conn) {
        return loadIdentifierFromDB(dbdict, conn).getName();
    }
```

### DeprecatedIsStillUsed
Deprecated member 'addUnique' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
     */
    @Deprecated
    public Unique addUnique(String name) {
        return addUnique(DBIdentifier.newConstraint(name));
    }
```

### DeprecatedIsStillUsed
Deprecated member 'addIndex' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Table.java`
#### Snippet
```java
     */
    @Deprecated
    public Index addIndex(String name) {
        return addIndex(DBIdentifier.newIndex(name));
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getTarget' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
     */
    @Deprecated
    public String getTarget() {
        return getTargetIdentifier().getName();
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setTypeName' is still used
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
     */
    @Deprecated
    public void setTypeName(String typeName) {
        setTypeIdentifier(DBIdentifier.newColumnDefinition(typeName));
    }
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/Configurations.java`
#### Snippet
```java
        // relies on knowing what getFullyQualifiedAnchorsInPropertiesLocation
        // looks for.
        if (opts.containsKey("p"))
            opts.remove("p");

```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
            if (o != null)
                modifiedKeys.add(key);
            else if (!newKeys.contains(key))
                newKeys.add(key);
        }
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/ProxyMaps.java`
#### Snippet
```java
        assertAllowedType(key, map.getKeyType());
        // Java 8 solution/workaround due to containsKey() calling get!=null, which could cause infinite loop
        return map.keySet().contains(key);
    }

```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

        if (numericParms) {
            if (!parameterTypes.keySet().contains(1)) {
                throw new UserException(_loc.get("missing-positional-parameter", resolver.getQueryContext()
                    .getQueryString(), parameterTypes.keySet().toString()));
```

## RuleId[id=SwitchStatementWithTooFewBranches]
### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
        for (Object object : objects) {
            meta = object;
            switch (type(meta)) {
                case TYPE_META:
                    ClassMetaData cls = (ClassMetaData) meta;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
            }

            switch (tag) {
                case TABLE_GEN:
                    parseTableGenerator(pkg, (TableGenerator) anno);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

    private String projectionClause(JPQLNode node) {
        switch (node.id) {
        case JJTTYPE:
            return projectionClause(firstChild(node));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                switch (alt6) {
            	case 1 :
            	    // Java.g:913:21: '0' .. '7'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                alt7=1;
            }
            switch (alt7) {
                case 1 :
                    // Java.g:913:32: IntegerTypeSuffix
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                switch (alt26) {
            	case 1 :
            	    // Java.g:1015:42: .
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                alt19=1;
            }
            switch (alt19) {
                case 1 :
                    // Java.g:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                switch (alt20) {
            	case 1 :
            	    // Java.g:929:34: '0' .. '9'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                        switch (alt3) {
                    	case 1 :
                    	    // Java.g:911:34: '0' .. '9'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                alt5=1;
            }
            switch (alt5) {
                case 1 :
                    // Java.g:911:45: IntegerTypeSuffix
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                switch (alt25) {
            	case 1 :
            	    // Java.g:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                switch (alt27) {
            	case 1 :
            	    // Java.g:1019:12: ~ ( '\\n' | '\\r' )
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                alt28=1;
            }
            switch (alt28) {
                case 1 :
                    // Java.g:1019:26: '\\r'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                        switch (alt8) {
                    	case 1 :
                    	    // Java.g:922:10: '0' .. '9'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                        switch (alt9) {
                    	case 1 :
                    	    // Java.g:922:26: '0' .. '9'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                        alt10=1;
                    }
                    switch (alt10) {
                        case 1 :
                            // Java.g:922:37: Exponent
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                        alt11=1;
                    }
                    switch (alt11) {
                        case 1 :
                            // Java.g:922:47: FloatTypeSuffix
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                        switch (alt12) {
                    	case 1 :
                    	    // Java.g:923:14: '0' .. '9'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                        alt13=1;
                    }
                    switch (alt13) {
                        case 1 :
                            // Java.g:923:25: Exponent
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                        alt14=1;
                    }
                    switch (alt14) {
                        case 1 :
                            // Java.g:923:35: FloatTypeSuffix
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                        switch (alt15) {
                    	case 1 :
                    	    // Java.g:924:10: '0' .. '9'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                        alt16=1;
                    }
                    switch (alt16) {
                        case 1 :
                            // Java.g:924:30: FloatTypeSuffix
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                        switch (alt17) {
                    	case 1 :
                    	    // Java.g:925:10: '0' .. '9'
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java


                switch (alt1) {
            	case 1 :
            	    // Java.g:909:28: HexDigit
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                alt2=1;
            }
            switch (alt2) {
                case 1 :
                    // Java.g:909:38: IntegerTypeSuffix
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PointbaseDictionary.java`
#### Snippet
```java
    @Override
    public int getPreferredType(int type) {
        switch (type) {
            case Types.LONGVARCHAR:
                return Types.CLOB;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/H2Dictionary.java`
#### Snippet
```java
    public int getJDBCType(int metaTypeCode, boolean lob) {
        int type = super.getJDBCType(metaTypeCode, lob);
        switch (type) {
        case Types.BIGINT:
            if (metaTypeCode == JavaTypes.BIGINTEGER)
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(131);}

                switch (alt131) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(117);}

                switch (alt117) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(118);}

                switch (alt118) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(24);}

            switch (alt24) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(25);}

            switch (alt25) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(26);}

            switch (alt26) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(98);}

                    switch (alt98) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(99);}

                    switch (alt99) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(100);}

                    switch (alt100) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(101);}

                    switch (alt101) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(90);}

            switch (alt90) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(23);}

            switch (alt23) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(59);}

                switch (alt59) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(120);}

                    switch (alt120) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(121);}

                    switch (alt121) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(122);}

                    switch (alt122) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(75);}

                    switch (alt75) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(21);}

            switch (alt21) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(141);}

                switch (alt141) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            TokenStream input = (TokenStream)_input;
        	int _s = s;
            switch ( s ) {
                    case 0 :
                        int LA145_3 = input.LA(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(170);}

            switch (alt170) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(13);}

                switch (alt13) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(35);}

                switch (alt35) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(165);}

            switch (alt165) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(168);}

                    switch (alt168) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(160);}

            switch (alt160) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(140);}

                switch (alt140) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(27);}

                switch (alt27) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(88);}

                switch (alt88) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(148);}

                        switch (alt148) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(149);}

                    switch (alt149) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(150);}

                        switch (alt150) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(151);}

                    switch (alt151) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(152);}

                        switch (alt152) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(129);}

                switch (alt129) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(107);}

                switch (alt107) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(92);}

                switch (alt92) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            TokenStream input = (TokenStream)_input;
        	int _s = s;
            switch ( s ) {
                    case 0 :
                        int LA155_2 = input.LA(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(83);}

                    switch (alt83) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(84);}

                    switch (alt84) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(57);}

                switch (alt57) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(125);}

                switch (alt125) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(77);}

                switch (alt77) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(81);}

            switch (alt81) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(82);}

                switch (alt82) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(31);}

                switch (alt31) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(95);}

            switch (alt95) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(94);}

                        switch (alt94) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(96);}

            switch (alt96) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(36);}

                switch (alt36) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(133);}

                switch (alt133) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(28);}

            switch (alt28) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(29);}

            switch (alt29) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(30);}

            switch (alt30) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(67);}

                        switch (alt67) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(68);}

                        switch (alt68) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(65);}

            switch (alt65) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(63);}

                        switch (alt63) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(64);}

                    switch (alt64) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(38);}

                    switch (alt38) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(15);}

                switch (alt15) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(108);}

                    switch (alt108) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(109);}

                    switch (alt109) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(111);}

                    switch (alt111) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(112);}

                    switch (alt112) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(113);}

                    switch (alt113) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(58);}

            switch (alt58) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            TokenStream input = (TokenStream)_input;
        	int _s = s;
            switch ( s ) {
                    case 0 :
                        int LA146_1 = input.LA(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(55);}

            switch (alt55) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            TokenStream input = (TokenStream)_input;
        	int _s = s;
            switch ( s ) {
                    case 0 :
                        int LA162_1 = input.LA(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(154);}

                        switch (alt154) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(155);}

                        switch (alt155) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(37);}

                switch (alt37) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(70);}

            switch (alt70) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(72);}

                switch (alt72) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            	    } finally {dbg.exitDecision(71);}

            	    switch (alt71) {
            	        case 1 :
            	            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(105);}

                switch (alt105) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
        } finally {dbg.exitDecision(184);}

        switch (alt184) {
            case 1 :
                dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(86);}

                switch (alt86) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(135);}

            switch (alt135) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(60);}

                switch (alt60) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(136);}

                switch (alt136) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(132);}

                switch (alt132) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(17);}

            switch (alt17) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(18);}

            switch (alt18) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(19);}

            switch (alt19) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(97);}

                switch (alt97) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(173);}

                    switch (alt173) {
                	case 1 :
                	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(174);}

                    switch (alt174) {
                	case 1 :
                	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(175);}

                    switch (alt175) {
                	case 1 :
                	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(104);}

            switch (alt104) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(79);}

                    switch (alt79) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(61);}

                switch (alt61) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(161);}

                        switch (alt161) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(162);}

                        switch (alt162) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(163);}

                        switch (alt163) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(134);}

                switch (alt134) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(50);}

            switch (alt50) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(9);}

            switch (alt9) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(10);}

            switch (alt10) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(22);}

                switch (alt22) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(126);}

            switch (alt126) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(52);}

                switch (alt52) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(53);}

            switch (alt53) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(138);}

                switch (alt138) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(20);}

                switch (alt20) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            TokenStream input = (TokenStream)_input;
        	int _s = s;
            switch ( s ) {
                    case 0 :
                        int LA8_1 = input.LA(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(130);}

                switch (alt130) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(115);}

                switch (alt115) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(56);}

            switch (alt56) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(33);}

            switch (alt33) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(34);}

            switch (alt34) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(74);}

                switch (alt74) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(128);}

            switch (alt128) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(47);}

                switch (alt47) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(48);}

            switch (alt48) {
                case 1 :
                    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(166);}

                    switch (alt166) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                                } finally {dbg.exitDecision(1);}

                                switch (alt1) {
                            	case 1 :
                            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                                } finally {dbg.exitDecision(2);}

                                switch (alt2) {
                            	case 1 :
                            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                                } finally {dbg.exitDecision(3);}

                                switch (alt3) {
                            	case 1 :
                            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(5);}

                    switch (alt5) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(6);}

                        switch (alt6) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(7);}

                        switch (alt7) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(116);}

                switch (alt116) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                } finally {dbg.exitDecision(91);}

                switch (alt91) {
            	case 1 :
            	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                        } finally {dbg.exitDecision(143);}

                        switch (alt143) {
                    	case 1 :
                    	    dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
                    } finally {dbg.exitDecision(144);}

                    switch (alt144) {
                        case 1 :
                            dbg.enterAlt(1);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
            } finally {dbg.exitDecision(78);}

            switch (alt78) {
                case 1 :
                    dbg.enterAlt(1);
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/OpenJPASupport.java`
#### Snippet
```java
public @interface OpenJPASupport {
    /**
     * @return the list of class names (don't use {@code MyEntity.class}) to enhance if {@code auto} is false.
     */
    String[] entities() default {};
```

### MismatchedJavadocCode
Method is specified to return set but the return type is array
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
     * receiver.  This does not include the "default" and "all" fetch groups.
     *
     * @return the set of fetch group names, not including the default and
     * all fetch groups.
     */
```

### MismatchedJavadocCode
Method is specified to return array but the return type is list
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/PathFinder.java`
#### Snippet
```java
     * @param x2 the x-coordinate of the target point
     * @param y2 the y-coordinate of the target point
     * @return a array of points in the form of x1,y1, x2,y2, ....
     */
    public List<Point> findPath(int x1, int y1, int x2, int y2) {
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                    .getAnnotation(xmlTypeClass), new Object[]{}));
            }
        } catch (Exception e) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
            xmlElementName = xmlElementClass.getMethod("name", null);
            xmlElementNamespace = xmlElementClass.getMethod("namespace", null);
        } catch (Exception e) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceXMLMetaDataParser.java`
#### Snippet
```java
                    meta.addField(member.getName(), field);
            }
        } catch(Exception e) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/TupleImpl.java`
#### Snippet
```java
        try {
            PUT = TupleImpl.class.getMethod("put", new Class[]{Integer.class, Object.class});
        } catch (Exception e) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/AnnotationProcessor6.java`
#### Snippet
```java
                    header.add(s.nextLine());
                }
            } catch (Throwable t) {

            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataParser.java`
#### Snippet
```java
        try {
            _file = new File(url.toURI());
        } catch (URISyntaxException e) {
        } catch (IllegalArgumentException iae) {
            // this is thrown when the URI is non-hierarchical (aka JBoss)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/ResultObjectProviderIterator.java`
#### Snippet
```java
            try {
                _rop.close();
            } catch (Exception e) {
            }
            _open = Boolean.FALSE;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/EagerResultList.java`
#### Snippet
```java
            try {
                rop.close();
            } catch (Exception e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/SimpleResultList.java`
#### Snippet
```java
            try {
                _rop.close();
            } catch (Exception e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/RandomAccessResultList.java`
#### Snippet
```java
            try {
                _rop.close();
            } catch (Exception e) {
            }
            _rows = null;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/ListResultObjectProvider.java`
#### Snippet
```java
            try {
                ((Closeable) _list).close();
            } catch (Exception e) {
            }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/WindowResultList.java`
#### Snippet
```java
            try {
                _rop.close();
            } catch (Exception e) {
            }
            _state = FREED;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/rop/LazyForwardResultList.java`
#### Snippet
```java
            try {
                _rop.close();
            } catch (Exception e) {
            }
            _state = FREED;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/JarFileURLMetaDataIterator.java`
#### Snippet
```java
            if (_jarFile != null)
                _jarFile.close();
        } catch (IOException ioe) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/CFMetaDataParser.java`
#### Snippet
```java
                return ClassUtil.toClass(name, resolve, loader);
            return ClassUtil.toClass(pkg + "." + name, resolve, loader);
        } catch (RuntimeException re) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/CFMetaDataParser.java`
#### Snippet
```java
            try {
                return ClassUtil.toClass(name, resolve, loader);
            } catch (RuntimeException re) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/CFMetaDataParser.java`
#### Snippet
```java
                    return ClassUtil.toClass(aPackage + name, resolve, loader);
                }
                catch (RuntimeException re) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/FileMetaDataIterator.java`
#### Snippet
```java
                return content;
            } finally {
                try { fin.close(); } catch (IOException ioe) {}
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ZipStreamMetaDataIterator.java`
#### Snippet
```java
        try {
            _stream.close();
        } catch (IOException ioe) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
                try {
                    ((Closeable) val.get()).close();
                } catch (Exception e) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
                if (!aliases.contains(impl))
                    allowed.add(impl);
        } catch (Throwable t) {
        }
        if (!allowed.isEmpty())
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            try {
                return loc.getFatal(key).getMessage();
            } catch (MissingResourceException mse) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
            try {
                return loc.getFatal(key).getMessage();
            } catch (MissingResourceException mse) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ZipFileMetaDataIterator.java`
#### Snippet
```java
           if (_file != null)
               _file.close();
        } catch (IOException ioe) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/SimpleResourceBundleProvider.java`
#### Snippet
```java
            try {
                bundle = ResourceBundle.getBundle(name, locale, loader);
            } catch (Throwable t) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/SimpleResourceBundleProvider.java`
#### Snippet
```java
            try {
                bundle = ResourceBundle.getBundle(name, locale);
            } catch (Throwable t) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StreamResourceBundleProvider.java`
#### Snippet
```java
            try {
                return new PropertyResourceBundle(in);
            } catch (Exception e) {
            } finally {
                try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/StreamResourceBundleProvider.java`
#### Snippet
```java
                try {
                    in.close();
                } catch (IOException ioe) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ZipResourceBundleProvider.java`
#### Snippet
```java
                zip.closeEntry();
            }
        } catch (Exception e) {
        } finally {
            try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ZipResourceBundleProvider.java`
#### Snippet
```java
            try {
                zip.close();
            } catch (IOException ioe) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
        } finally {
            if (in != null)
                try { in.close(); } catch (IOException ioe) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
            try {
                in.close();
            } catch (IOException ioe) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
                try {
                    fin.close();
                } catch (IOException ioe) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/meta/ClassArgParser.java`
#### Snippet
```java
                try {
                    in.close();
                } catch (IOException ioe) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ParameterTemplate.java`
#### Snippet
```java
            try {
                writer.close();
            } catch (IOException ioe) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/ParameterTemplate.java`
#### Snippet
```java
            try {
                reader.close();
            } catch (IOException ioe) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/MultiClassLoader.java`
#### Snippet
```java
                return Class.forName(name, false, loader);
            }
            catch (Throwable t) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Files.java`
#### Snippet
```java
                try {
                    in.close();
                } catch (Exception e) {
                }
            if (out != null)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Files.java`
#### Snippet
```java
                try {
                    out.close();
                } catch (Exception e) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
        try {
            store(out, header);
        } catch (IOException ex) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java
        try {
            mr = new WLSManagedRuntime();
        } catch (Throwable t) {
        }
        WLS = (WLSManagedRuntime) mr;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java
        try {
            mr = new SunOneManagedRuntime();
        } catch (Throwable t) {
        }
        SUNONE = (SunOneManagedRuntime) mr;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/AutomaticManagedRuntime.java`
#### Snippet
```java
        try {
            mr = new WASManagedRuntime();
        } catch (Throwable t) {
        }
        WAS = (WASManagedRuntime) mr;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAVersion.java`
#### Snippet
```java
                }
            }
        } catch (Exception e) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/CacheMarshallerImpl.java`
#### Snippet
```java
        } finally {
            if (in != null)
                try { in.close(); } catch (IOException e) { }
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/CacheMarshallerImpl.java`
#### Snippet
```java
        } finally {
            if (out != null) {
                try { out.close(); } catch (IOException ioe) { }
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/InterfaceImplGenerator.java`
#### Snippet
```java
                    StringUtil.capitalize(fmd.getName()), (Class[]) null));
            return meth == null;
        } catch (PrivilegedActionException pae) {}
        return true;
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/SequenceMetaData.java`
#### Snippet
```java
            try {
                _instance.close();
            } catch (Exception e) {
            }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingParser.java`
#### Snippet
```java
        try {
            xmlRootElementClass = Class.forName("javax.xml.bind.annotation.XmlRootElement");
        } catch (Exception e) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
                    Class.forName(cls.getName(), true, AccessController.doPrivileged(J2DoPrivHelper
                        .getClassLoaderAction(cls)));
                } catch (Throwable t) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
                try {
                    Class.forName(className.substring(0, i), true, cl);
                } catch (Exception e) {
                } // consume all exceptions
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                type.getDeclaredMethod(setterName, getType());
                return setterName;
            } catch (Exception e) {
            }
            setterName = "set" + StringUtil.capitalize(_name);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            try {
                type.getDeclaredMethod(setterName, getType());
            } catch (Exception e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
                } catch (OpenJPAException ke) {
                    throw ke;
                } catch (Exception e) {
                }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                || param.isAssignableFrom(getter.getReturnType()))
                return getter;
        } catch (NoSuchMethodException nsme) {
        } catch (Exception e) {
            throw new GeneralException(e);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                    || param.isAssignableFrom(getter.getReturnType()))
                    return getter;
            } catch (NoSuchMethodException nsme) {
            } catch (Exception e) {
                throw new GeneralException(e);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/ProxyManagerImpl.java`
#### Snippet
```java
                        + StringUtil.capitalize(meth.getName()), afterParams);
            }
            catch (NoSuchMethodException nsme) {
            }
            catch (Exception e) {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/event/TCPRemoteCommitProvider.java`
#### Snippet
```java
            try {
                _socketPool.invalidateObject(s);
            } catch (Exception e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExtentImpl.java`
#### Snippet
```java
                    citr.close();
                }
                catch (Exception e) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ResultPacker.java`
#### Snippet
```java
                try {
                    cons = _resultClass.getConstructor(types);
                } catch (NoSuchMethodException nsme) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
                    if (temporal != null && type.isAssignableFrom(temporal.getClass()))
                        return temporal;
                } catch (IllegalArgumentException e) {

                }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        try {
            return ClassUtil.toClass(name, _loader);
        } catch (RuntimeException | NoClassDefFoundError re) {
        }
        return null;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        } catch (Exception e) {
            if (rop != null)
                try { rop.close(); } catch (Exception e2) {}
            throw e;
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                    AccessController.doPrivileged(
                        J2DoPrivHelper.getClassLoaderAction(cls)));
            } catch (Throwable t) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
            if (!rollback) {
                forcedRollback = true;
                try { _store.rollback(); } catch (RuntimeException re2) {}
            }
            err = re;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                try {
                    ((Query) itr.next()).closeResources();
                } catch (RuntimeException re) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
                    e.closeAll();
                }
                catch (RuntimeException re) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/BrokerImpl.java`
#### Snippet
```java
        }

        try { releaseConnection(); } catch (RuntimeException re) {}

        _lm.close();
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
                      setter.getReturnType().isAssignableFrom(c))
                  result.add(prop);
                } catch (NoSuchMethodException e) {

                }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
                try {
                    result.add((T)f.get(null));
                } catch (IllegalArgumentException | IllegalAccessException e) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ApplicationIdTool.java`
#### Snippet
```java
        try {
            return Class.forName(name, false, loader);
        } catch (Throwable t) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLStore.java`
#### Snippet
```java
            try {
                wait();
            } catch (InterruptedException ie) {
            }
        _locked = true;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/ProcessingException.java`
#### Snippet
```java
        try {
            uri = URLDecoder.decode(uri, "UTF-8");
        } catch (Exception e) {
        }
        Throwable t = this.getCause() == null ? this : getCause();
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                try {
                    fw.close();
                } catch (IOException ioe) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ScrollingTextPane.java`
#### Snippet
```java
                scrollRectToVisible(r);
            }
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java
            if (value != null)
                result = Integer.parseInt(value.toString());
        } catch (NumberFormatException nfe) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java
            if (value != null)
                result = Double.parseDouble(value.toString());
        } catch (NumberFormatException nfe) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            oidType.getConstructor(new Class[]{ Class.class, String.class });
            return Boolean.TRUE;
        } catch (Throwable t) {
        }
        try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            oidType.getConstructor(new Class[]{ String.class });
            return Boolean.FALSE;
        } catch (Throwable t) {
        }
        return null;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/PCEnhancer.java`
#### Snippet
```java
            }
            rev = GitUtils.convertGitInfoToPCEnhancerVersion(revisionProps.getProperty("openjpa.enhancer.revision"));
        } catch (Exception e) {
        }
        if (rev > 0) {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
            try {
                conn.rollback();
            } catch (SQLException se) {
            }
        return conn;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/InformixDictionary.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/AbstractResult.java`
#### Snippet
```java
                        ((Closeable) res).close();
                    }
                    catch (Exception e) {
                    }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
            try {
                stmnt.close();
            } catch (SQLException se2) {
            }
            throw se;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
            try {
                stmnt.close();
            } catch (SQLException se2) {
            }
            throw se;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                try {
                    rs.close();
                } catch (Exception e) {
                }
            try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            try {
                stmnt.close();
            } catch (Exception e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
        } finally {
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
            try { stmnt.close(); } catch (SQLException se) {}
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
            try { stmnt.close(); } catch (SQLException se) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                try {
                    conn.close();
                } catch (SQLException se) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
                try {
                    rs.close();
                } catch (Exception e) {
                }
            try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/OracleDictionary.java`
#### Snippet
```java
            try {
                stmnt.close();
            } catch (Exception e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
        } finally {
            if (res != null)
                try { res.close (); } catch (SQLException e) {}
            if (stmnt != null)
                try { stmnt.close (); } catch (SQLException e) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
                try { res.close (); } catch (SQLException e) {}
            if (stmnt != null)
                try { stmnt.close (); } catch (SQLException e) {}
            if (conn != null)
                try { conn.close (); } catch (SQLException e) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
                try { stmnt.close (); } catch (SQLException e) {}
            if (conn != null)
                try { conn.close (); } catch (SQLException e) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
                try {
                    rs.close();
                } catch (Throwable t) {}
            }
            if (ps != null) {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
                try {
                    ps.close();
                } catch (Throwable t) {}
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
        } finally {
            if (res != null)
                try { res.close (); } catch (SQLException e) {}
            if (stmnt != null)
                try { stmnt.close (); } catch (SQLException e) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
                try { res.close (); } catch (SQLException e) {}
            if (stmnt != null)
                try { stmnt.close (); } catch (SQLException e) {}
            if (conn != null)
                try { conn.close (); } catch (SQLException e) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/PostgresDictionary.java`
#### Snippet
```java
                try { stmnt.close (); } catch (SQLException e) {}
            if (conn != null)
                try { conn.close (); } catch (SQLException e) {}
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/ResultSetResult.java`
#### Snippet
```java
            try {
                stmnt = rs.getStatement();
            } catch (Throwable t) {
            }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/ResultSetResult.java`
#### Snippet
```java
        try {
            _rs.close();
        } catch (SQLException se) {
        }
        if (_stmnt != null && _closeStatement)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/ResultSetResult.java`
#### Snippet
```java
            try {
                _stmnt.close();
            } catch (SQLException se) {
            }
        if (_closeConn)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/ResultSetResult.java`
#### Snippet
```java
            try {
                _conn.close();
            } catch (SQLException se) {
            }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            // clean up statement
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se2) {}
            try { conn.close(); } catch (SQLException se2) {}
            throw se;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se2) {}
            try { conn.close(); } catch (SQLException se2) {}
            throw se;
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
        } finally {
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                try { rs.close(); } catch (SQLException se) {}
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
            if (conn != null)
                try { conn.close(); } catch (SQLException se) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SelectImpl.java`
#### Snippet
```java
                try { stmnt.close(); } catch (SQLException se) {}
            if (conn != null)
                try { conn.close(); } catch (SQLException se) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/HSQLDictionary.java`
#### Snippet
```java
            Field fld = cls.getField(fieldName);
            violation_of_unique_index_or_constraint = fld.getInt(null);
        } catch (Exception e) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionaryFactory.java`
#### Snippet
```java
                try {
                    conn.close();
                } catch (SQLException se) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
            // try to obtain a connection from the primary datasource
            ds = conf.getDataSource(null);
    	} catch (UserException uex) {
    	}
    	if (ds==null) {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DB2Dictionary.java`
#### Snippet
```java
                // use datasource 2 if available
                ds = conf.getDataSource2(null);
        	} catch (UserException uex2) {
        	}
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfigurationImpl.java`
#### Snippet
```java
        // before TypedInstruction.class
        try { serp.bytecode.lowlevel.Entry.class.getName(); }
        catch (Throwable t) {}
        try { serp.bytecode.Instruction.class.getName(); }
        catch (Throwable t) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/conf/JDBCConfigurationImpl.java`
#### Snippet
```java
        catch (Throwable t) {}
        try { serp.bytecode.Instruction.class.getName(); }
        catch (Throwable t) {}

        supportedOptions().add(OPTION_QUERY_SQL);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            try {
                c.rollback();
            } catch (SQLException sqle) {
            }
            try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                if (!c.getAutoCommit())
                    c.setAutoCommit(true);
            } catch (SQLException sqle) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        } finally {
            if (res != null)
                try { res.close (); } catch (SQLException e) {}
            if (stmnt != null)
                try { stmnt.close (); } catch (SQLException e) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try { res.close (); } catch (SQLException e) {}
            if (stmnt != null)
                try { stmnt.close (); } catch (SQLException e) {}
            if (conn != null)
                try { conn.close (); } catch (SQLException e) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try { stmnt.close (); } catch (SQLException e) {}
            if (conn != null)
                try { conn.close (); } catch (SQLException e) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        } finally {
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try { rs.close(); } catch (SQLException se) {}
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                    tables.close();
                }
                catch (Exception e) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try {
                    rs.close();
                } catch (SQLException se) {
                }
            if (stmnt != null)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try {
                    indexes.close();
                } catch (Exception e) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            try {
                in.close();
            } catch (IOException ioe) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                    try {
                        stmnt.close();
                    } catch (SQLException se) {
                    }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try {
                    pks.close();
                } catch (Exception e) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try {
                    cols.close();
                } catch (Exception e) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try {
                    pks.close();
                } catch (Exception e) {
                }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        } finally {
            if (res != null)
                try { res.close (); } catch (SQLException e) {}
            if (stmnt != null)
                try { stmnt.close (); } catch (SQLException e) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try { res.close (); } catch (SQLException e) {}
            if (stmnt != null)
                try { stmnt.close (); } catch (SQLException e) {}
            if (conn != null)
                try { conn.close (); } catch (SQLException e) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                try { stmnt.close (); } catch (SQLException e) {}
            if (conn != null)
                try { conn.close (); } catch (SQLException e) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
                        try {
                            return getInt(rs, column);
                        } catch (Exception e5) {
                        }
                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingTool.java`
#### Snippet
```java
        try {
            mapping = repos.getMapping(cls, null, false);
        } catch (Exception e) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ReverseMappingTool.java`
#### Snippet
```java
            try {
                in.close();
            } catch (IOException e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MaxEmbeddedLobFieldStrategy.java`
#### Snippet
```java
                try {
                    rs.close();
                } catch (SQLException se) {
                }
            if (stmnt != null)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MaxEmbeddedLobFieldStrategy.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
            try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/MaxEmbeddedLobFieldStrategy.java`
#### Snippet
```java
            try {
                conn.close();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ClassNameDiscriminatorStrategy.java`
#### Snippet
```java
                try {
                    rs.close();
                } catch (SQLException se) {
                }
            if (stmnt != null)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ClassNameDiscriminatorStrategy.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
            try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/strats/ClassNameDiscriminatorStrategy.java`
#### Snippet
```java
            try {
                conn.close();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
                try {
                    conn.close();
                } catch (SQLException se) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
            } catch (SQLException se) {
                if (stmnt != null)
                    try { stmnt.close(); } catch (SQLException se2) {}
                try { conn.close(); } catch (SQLException se2) {}
                throw SQLExceptions.getStore(se, dict);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/SQLStoreQuery.java`
#### Snippet
```java
                if (stmnt != null)
                    try { stmnt.close(); } catch (SQLException se2) {}
                try { conn.close(); } catch (SQLException se2) {}
                throw SQLExceptions.getStore(se, dict);
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/StoredProcedureQuery.java`
#### Snippet
```java
                try {
                    conn.close();
                } catch (SQLException ex) {

                }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ValueTableJDBCSeq.java`
#### Snippet
```java
                try {
                    conn.close();
                } catch (SQLException se) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/ClassTableJDBCSeq.java`
#### Snippet
```java
                try {
                    conn.close();
                } catch (SQLException se) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
            } finally {
                if (rs != null)
                    try { rs.close(); } catch (SQLException se) {}
                if (stmnt != null)
                    try { stmnt.close(); } catch (SQLException se) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
                    try { rs.close(); } catch (SQLException se) {}
                if (stmnt != null)
                    try { stmnt.close(); } catch (SQLException se) {}
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
        } finally {
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
            if (!wasAuto && !suspendInJTA())
                conn.setAutoCommit(false);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
        } finally {
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
                try { rs.close(); } catch (SQLException se) {}
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/TableJDBCSeq.java`
#### Snippet
```java
                return false;
            } finally {
                try { conn.close(); } catch (SQLException se) {}
            }
        } else
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractJDBCSeq.java`
#### Snippet
```java
                throw SQLExceptions.getStore(se);
            } finally {
                try { conn.close(); } catch (SQLException se) {}
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedStatementManagerImpl.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedStatementManagerImpl.java`
#### Snippet
```java
                    warn = warn.getNextWarning();
                }
            } catch (SQLException e) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PagingResultObjectProvider.java`
#### Snippet
```java
                        ((Closeable) res).close();
                    }
                    catch (Exception e) {
                    }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderQueryImpl.java`
#### Snippet
```java
        } catch (SQLException se) {
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se2) {}
            try { conn.close(); } catch (SQLException se2) {}
            throw new RuntimeException(se);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/FinderQueryImpl.java`
#### Snippet
```java
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se2) {}
            try { conn.close(); } catch (SQLException se2) {}
            throw new RuntimeException(se);
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/NativeJDBCSeq.java`
#### Snippet
```java
            // clean up our resources
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/NativeJDBCSeq.java`
#### Snippet
```java
                try { rs.close(); } catch (SQLException se) {}
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/NativeJDBCSeq.java`
#### Snippet
```java
                // STOP - ALLOW PRINT STATEMENTS
            } finally {
                try { conn.close(); } catch (SQLException se) {}
            }
        } else
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/NativeJDBCSeq.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
                            stmnt.close();
                        }
                        catch (SQLException se) {
                        }
                }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreQuery.java`
#### Snippet
```java
            try {
                conn.close();
            } catch (SQLException se) {

            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCBrokerFactory.java`
#### Snippet
```java
            JDBCConfiguration conf = (JDBCConfiguration) getConfiguration();
            db = conf.getDBDictionaryInstance().platform;
        } catch (RuntimeException re) {
        }
        props.put("Platform", "OpenJPA JDBC Edition: " + db + " Database");
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PessimisticLockManager.java`
#### Snippet
```java
        } finally {
            if (stmnt != null)
                try { stmnt.close(); } catch (SQLException se) {}
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PessimisticLockManager.java`
#### Snippet
```java
                try { stmnt.close(); } catch (SQLException se) {}
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
            try { conn.close(); } catch (SQLException se) {}
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PessimisticLockManager.java`
#### Snippet
```java
            if (rs != null)
                try { rs.close(); } catch (SQLException se) {}
            try { conn.close(); } catch (SQLException se) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedSQLStoreQuery.java`
#### Snippet
```java
            } catch (SQLException se) {
                if (stmnt != null)
                    try { stmnt.close(); } catch (SQLException se2) {}
                try { conn.close(); } catch (SQLException se2) {}
                throw SQLExceptions.getStore(se, dict);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/PreparedSQLStoreQuery.java`
#### Snippet
```java
                if (stmnt != null)
                    try { stmnt.close(); } catch (SQLException se2) {}
                try { conn.close(); } catch (SQLException se2) {}
                throw SQLExceptions.getStore(se, dict);
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/AbstractUpdateManager.java`
#### Snippet
```java
            return flush(states, store, psMgr);
        } finally {
            try { conn.close(); } catch (SQLException se) {}
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
            if (_conn != null && _conn.getInnermostDelegate().isReadOnly())
                _conn.setReadOnly(false);
        } catch (SQLException e) {
        }
        return _conf.getUpdateManagerInstance().flush(sms, this);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
            try {
                getDelegate().close();
            } catch (SQLException se) {
            }
            _freed = true;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
            try {
                _conn.rollback();
            } catch (SQLException se2) {
            }
            throw SQLExceptions.getStore(se, _dict);
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/LazySchemaFactory.java`
#### Snippet
```java
                    try {
                        _conn.close();
                    } catch (SQLException se) {
                    }
                    _conn = null;
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SimpleDriverDataSource.java`
#### Snippet
```java
            if (_driver != null)
                return _driver;
        } catch (Exception e) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SimpleDriverDataSource.java`
#### Snippet
```java
        try {
            Class.forName(_connectionDriverName, true, _classLoader);
        } catch (Exception e) {
        }
        try {
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SimpleDriverDataSource.java`
#### Snippet
```java
            if (_driver != null)
                return _driver;
        } catch (Exception e) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
            try {
                _conn.commit();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
            try {
                _conn.commit();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
            try {
                _conn.commit();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaGenerator.java`
#### Snippet
```java
            try {
                _conn.commit();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
                try {
                    rs.close();
                } catch (SQLException se) {
                }
            if (stmnt != null)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
            if (wasAuto != embedded)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
            try {
                conn.close();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
                try {
                    rs.close();
                } catch (SQLException se) {
                }
            if (stmnt != null)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
            if (!wasAuto)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
            try {
                conn.close();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
                try {
                    stmnt.close();
                } catch (SQLException se) {
                }
            if (!wasAuto)
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/TableSchemaFactory.java`
#### Snippet
```java
            try {
                conn.close();
            } catch (SQLException se) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java
                                conn.rollback();
                            }
                            catch (Exception e) {
                            }
                        }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java
                            conn.commit();
                        }
                        catch (Exception e) {
                        }
                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/SchemaTool.java`
#### Snippet
```java
                                statement.close();
                            }
                            catch (SQLException se) {
                            }
                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/DistributedBrokerImpl.java`
#### Snippet
```java
        try {
            return super.endOperation();
        } catch (Exception ex) {

        }
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceProductDerivation.java`
#### Snippet
```java
                } else {
                    // Set the spec level based on the persistence version
                    oconf.setSpecification("jpa " + persistenceVersion.toString());
                }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
        TypeMirror mirror = e.asType();
        return new StringBuilder(e.getKind().toString()).append(" ")
                           .append(e.toString())
                           .append("Mirror ")
                           .append(mirror.getKind().toString())
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/meta/SourceAnnotationHandler.java`
#### Snippet
```java
                           .append("Mirror ")
                           .append(mirror.getKind().toString())
                           .append(mirror.toString()).toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/LogFactoryImpl.java`
#### Snippet
```java
                t.printStackTrace(pwriter);
                pwriter.flush();
                buf.append(swriter.toString());
            }
            return buf.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/log/LogFactoryImpl.java`
#### Snippet
```java
            } catch (IOException ioe) {
                throw new IllegalArgumentException(_loc.get("log-bad-file",
                    file) + " " + ioe.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Expressions.java`
#### Snippet
```java
                return new StringBuilder(arg.toString());
            } else if (String.class.isAssignableFrom(literalClass)) {
                return new StringBuilder("'").append(arg.toString()).append("'");
            } else if (Enum.class.isAssignableFrom(literalClass)) {
                return new StringBuilder(arg.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/Localizer.java`
#### Snippet
```java
            else
                return "localized message key: " + _localizedMessage
                    + "; substitutions: " + Arrays.asList(_subs).toString();
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                        buf.append(" ");
                    buf.append("[params=").
                        append(paramBuf.toString()).append("]");
                }
                super.appendInfo(buf);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/jdbc/LoggingConnectionDecorator.java`
#### Snippet
```java
                        buf.append(" ");
                    }
                    buf.append("[params=").append(paramBuf.toString()).append("]");
                }
                super.appendInfo(buf);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
                if (needsNewline)
                    pw.println();
                pw.print(line.toString());
                return true;
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/FormatPreservingProperties.java`
#### Snippet
```java
                if (needsNewline)
                    pw.println();
                pw.print(line.toString());

                keys.remove(propertyKey);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-kernel/src/main/java/org/apache/openjpa/conf/OpenJPAVersion.java`
#### Snippet
```java
    public static void main(String[] args) {
        // START - ALLOW PRINT STATEMENTS
        System.out.println(new OpenJPAVersion().toString());
        // STOP - ALLOW PRINT STATEMENTS
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Bootstrap.java`
#### Snippet
```java
            return getFactoryClass(conf, loader).getName();
        } catch (Exception e) {
            return "<" + e.toString() + ">";
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/FetchConfigurationImpl.java`
#### Snippet
```java

    protected String toString(Object o) {
        return o == null ? "null" : o.toString() + "[" + o.getClass().getName() + "]";
    }
}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/ManagedClassSubclasser.java`
#### Snippet
```java
                            + ".prepareUnenhancedClasses()"
                            + " - The following classes are unenhanced "
                            + unenhanced.toString());
                    }
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/jpql/JPQLExpressionBuilder.java`
#### Snippet
```java

        public String toString(String prefix) {
            return prefix + toString();
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java
    private static StringBuilder quoted(Object o) {
        if (o == null) return new StringBuilder(NULL_LITERAL);
        return new StringBuilder().append(QUOTE).append(o.toString()).append(QUOTE);
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jest/src/main/java/org/apache/openjpa/json/JSONObject.java`
#### Snippet
```java

    public void write(PrintWriter writer) {
        writer.println(toString());
    }
    @Override
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObject.java`
#### Snippet
```java
    private static StringBuilder quoted(Object o) {
        if (o == null) return new StringBuilder(NULL_LITERAL);
        return new StringBuilder().append(QUOTE).append(o.toString()).append(QUOTE);
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jest/src/main/java/org/apache/openjpa/persistence/jest/JSONObject.java`
#### Snippet
```java

    public void write(PrintWriter writer) {
        writer.println(toString());
    }
    @Override
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-examples/openbooks/src/main/java/openbook/tools/converter/SourceRenderer.java`
#### Snippet
```java
            return new FileInputStream(file);
        } catch (Exception e) {
            warn("Input file " + file.getAbsolutePath() + " failed due to " + e.toString());
        }
        return null;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
            _sql.append("NULL");
        else if (o instanceof Raw)
            _sql.append(o.toString());
        else {
            Class<?> type = Filters.wrap(o.getClass());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                    _sql.append(_dict.getBooleanRepresentation().getRepresentation(b));
                } else {
                    _sql.append(o.toString());
                }
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
        if (!paramOnly) {
            if (sqlIndex == _sql.length())
                _sql.append(buf._sql.toString());
            else
                _sql.insert(sqlIndex, buf._sql.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
                _sql.append(buf._sql.toString());
            else
                _sql.insert(sqlIndex, buf._sql.toString());
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/RowImpl.java`
#### Snippet
```java
        }

        buf.append(") VALUES (").append(vals.toString()).append(")");
        return buf.toString();
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
        }

        buf.append(endBuf.toString());
        buf.append(")");
        return new String[]{ buf.toString() };
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/StartsWithExpression.java`
#### Snippet
```java
                _val1.appendTo(sel, ctx, bstate.state1, buf, 0);
                buf.append(" LIKE ");
                buf.appendValue(o.toString() + "%", col);
            }
        } else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/EndsWithExpression.java`
#### Snippet
```java
                _val1.appendTo(sel, ctx, bstate.state1, buf, 0);
                buf.append(" LIKE ");
                buf.appendValue("%" + o.toString(), col);
            }
        } else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/Lit.java`
#### Snippet
```java
                Object dbRepresentation = ctx.store.getDBDictionary().getBooleanRepresentation().getRepresentation(boolVal);
                if (dbRepresentation instanceof String) {
                    lstate.sqlValue = new Raw("'" + dbRepresentation.toString() + "'");
                } else if (dbRepresentation instanceof Boolean ||
                           dbRepresentation instanceof Integer) {
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `AnnotationEntry` may be 'static'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationBuilder.java`
#### Snippet
```java
    }

    class AnnotationEntry {

        String key;
```

### InnerClassMayBeStatic
Inner class `Argument` may be 'static'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
     *
     */
    public class Argument<K,V> {
        final private K key;
        final private V value;
```

### InnerClassMayBeStatic
Inner class `ClassName` may be 'static'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
     * @see SourceCode#getOrCreateImport(String)
     */
    private class ClassName implements Comparable<ClassName> {
        public final String fullName;
        public final String simpleName;
```

### InnerClassMayBeStatic
Inner class `Comment` may be 'static'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/util/SourceCode.java`
#### Snippet
```java
    }

    class Comment {
        List<String> lines = new ArrayList<>();
        private boolean inline = false;
```

### InnerClassMayBeStatic
Inner class `DelegatingUOWAction` may be 'static'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/WASRegistryManagedRuntime.java`
#### Snippet
```java
     * {@link Runnable} to be passed in to the WebSphere UOWManager.
     */
    class DelegatingUOWAction implements UOWAction {
        Runnable _del;

```

### InnerClassMayBeStatic
Inner class `TransactionManagerRegistryFacade` may be 'static'
in `openjpa-kernel/src/main/java/org/apache/openjpa/ee/RegistryManagedRuntime.java`
#### Snippet
```java
     *  @author  Marc Prud'hommeaux
     */
    public class TransactionManagerRegistryFacade
        implements TransactionManager, Transaction {
        private final TransactionSynchronizationRegistry _registry;
```

### InnerClassMayBeStatic
Inner class `DeferredEmbeddableOverrides` may be 'static'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java

    // Inner class for storing override information
    class DeferredEmbeddableOverrides {
        DeferredEmbeddableOverrides(FieldMapping fm, String attrName) {
            _fm = fm;
```

### InnerClassMayBeStatic
Inner class `Unknown` may be 'static'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
    	return _relationType;
    }
    private class Unknown{}

    public boolean isDelayCapable() {
```

### InnerClassMayBeStatic
Inner class `TaskProgress` may be 'static'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
     * Emits progress property from a background thread.
     */
    class TaskProgress extends SwingWorker<Void, Integer> {
        private long startTimeInMillis;
        private long _total = 100;
```

### InnerClassMayBeStatic
Inner class `MemoryDisplay` may be 'static'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/StatusBar.java`
#### Snippet
```java
    }

    public class MemoryDisplay implements ActionListener {
        JProgressBar bar;
        public MemoryDisplay(JProgressBar bar) {
```

### InnerClassMayBeStatic
Inner class `InstanceCellRenderer` may be 'static'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTable.java`
#### Snippet
```java
     *
     */
    public class InstanceCellRenderer extends DefaultTableCellRenderer {
        
        private static final long serialVersionUID = 1L;
```

### InnerClassMayBeStatic
Inner class `CollectionCellRenderer` may be 'static'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/EntityTable.java`
#### Snippet
```java
     *
     */
    public class CollectionCellRenderer extends DefaultTableCellRenderer {
        
        private static final long serialVersionUID = 1L;
```

### InnerClassMayBeStatic
Inner class `OpenBookAction` may be 'static'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
     *
     */
    public abstract class OpenBookAction extends AbstractAction {
        
        private static final long serialVersionUID = 1L;
```

### InnerClassMayBeStatic
Inner class `TypedTreeCellRenderer` may be 'static'
in `openjpa-examples/openbooks/src/main/java/openbook/client/Demo.java`
#### Snippet
```java
    }

    public class TypedTreeCellRenderer extends DefaultTreeCellRenderer {
        
        private static final long serialVersionUID = 1L;
```

### InnerClassMayBeStatic
Inner class `DFA29` may be 'static'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    }

    class DFA29 extends DFA {

        public DFA29(BaseRecognizer recognizer) {
```

### InnerClassMayBeStatic
Inner class `DFA18` may be 'static'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
    }

    class DFA18 extends DFA {

        public DFA18(BaseRecognizer recognizer) {
```

### InnerClassMayBeStatic
Inner class `DFA114` may be 'static'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
    }

    class DFA114 extends DFA {

        public DFA114(BaseRecognizer recognizer) {
```

### InnerClassMayBeStatic
Inner class `DFA156` may be 'static'
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaParser.java`
#### Snippet
```java
    }

    class DFA156 extends DFA {

        public DFA156(BaseRecognizer recognizer) {
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                all.addAll((List) entry.getValue());
            else if (_mode == MODE_QUERY || _metas == null
                || !_metas.containsKey(entry.getKey()))
                all.add(new ClassQueries((List<QueryMetaData>)
                    entry.getValue()));
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
            if (entry.getKey() == null)
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
                    entry.getValue()));
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
            if (entry.getKey() == null)
                all.addAll((List) entry.getValue());
            else if (_metas == null || !_metas.containsKey(entry.getKey()))
                all.add(new ClassSeqs((List<SequenceMetaData>)
                    entry.getValue()));
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/AnnotationPersistenceMetaDataSerializer.java`
#### Snippet
```java
                all.addAll((List) entry.getValue());
            else if (_mode == MetaDataModes.MODE_QUERY || _metas == null
                || !_metas.containsKey(entry.getKey()))
                all.add(new ClassQueries((List<QueryMetaData>)
                    entry.getValue()));
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/AliasContext.java`
#### Snippet
```java
	 */
	public boolean hasAlias(Expression path) {
		return _aliases.containsKey(path);
	}

```

### SuspiciousMethodCalls
Suspicious call to 'List.contains()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/query/QueryDefinitionImpl.java`
#### Snippet
```java

	boolean hasDomain(PathExpression path) {
		return _domains != null && _domains.contains(path);
	}

```

### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
            return null;
        PathImpl<?,?> root = getInnermostParentPath();
        if (subquery.getRoots() != null && subquery.getRoots().contains(this))
            return root;
        return null;
```

### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/PathImpl.java`
#### Snippet
```java
     */
    public boolean inSubquery(SubqueryImpl<?> subquery) {
        return subquery != null && (subquery.getRoots() == null ? false : subquery.getRoots().contains(this));
    }

```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
                    clauses, factory, q, model, exp2Vals);
            } else {
                Value val = (exp2Vals != null && exp2Vals.containsKey(s)
                        ? exp2Vals.get(s) : ((ExpressionImpl<?>)s).toValue(factory, q));
                String alias = s.getAlias();
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaExpressionBuilder.java`
#### Snippet
```java
            } else {
                Value val = (exp2Vals != null && exp2Vals.containsKey(s)
                        ? exp2Vals.get(s) : ((ExpressionImpl<?>)s).toValue(factory, q));
                String alias = s.getAlias();
                val.setAlias(alias);
```

### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            } else {
                org.apache.openjpa.kernel.exps.Path parentPath = null;
                if (!corrJoins.isEmpty() && corrJoins.contains(_parent)) {
                    Value var = getVariableForCorrPath(subquery, correlatedParentPath);
                    parentPath = factory.newPath(var);
```

### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
                    corrJoins = subquery.getCorrelatedJoins();
                    org.apache.openjpa.kernel.exps.Subquery subQ = subquery.getSubQ();
                    if ((!corrJoins.isEmpty() && corrJoins.contains(_parent)) ||
                        (corrJoins.isEmpty() && parent.inSubquery(subquery) && _parent.getCorrelatedPath() != null)) {
                        path = factory.newPath(subQ);
```

### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/Joins.java`
#### Snippet
```java
            } else {
                org.apache.openjpa.kernel.exps.Path parentPath = null;
                if (corrJoins != null && corrJoins.contains(_parent)) {
                    Value var = getVariableForCorrPath(subquery, correlatedParentPath);
                    parentPath = factory.newPath(var);
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java
    @Override
    public boolean contains(final Object obj) {
        return map.containsKey(obj);
    }

```

### SuspiciousMethodCalls
Suspicious call to 'AbstractHashedMap.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/LinkedMap.java`
#### Snippet
```java
        @Override
        public boolean contains(final Object obj) {
            return parent.containsKey(obj);
        }

```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
        @Override
        public boolean remove(final Object key) {
            if (parent.normalMap.containsKey(key)) {
                final Object value = parent.normalMap.remove(key);
                parent.reverseMap.remove(value);
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
        @Override
        public boolean contains(final Object value) {
            return parent.reverseMap.containsKey(value);
        }

```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
    public K removeValue(final Object value) {
        K key = null;
        if (reverseMap.containsKey(value)) {
            key = reverseMap.remove(value);
            normalMap.remove(key);
```

### SuspiciousMethodCalls
Suspicious call to 'Map.remove()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
        K key = null;
        if (reverseMap.containsKey(value)) {
            key = reverseMap.remove(value);
            normalMap.remove(key);
        }
```

### SuspiciousMethodCalls
Suspicious call to 'Map.remove()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
            final Object value = last.getValue();
            super.remove();
            parent.reverseMap.remove(value);
            last = null;
            canRemove = false;
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
        @Override
        public boolean remove(final Object value) {
            if (parent.reverseMap.containsKey(value)) {
                final Object key = parent.reverseMap.remove(value);
                parent.normalMap.remove(key);
```

### SuspiciousMethodCalls
Suspicious call to 'AbstractDualBidiMap.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
            final Entry<?, ?> entry = (Entry<?, ?>) obj;
            final Object key = entry.getKey();
            if (parent.containsKey(key)) {
                final V value = parent.normalMap.get(key);
                if (value == null ? entry.getValue() == null : value.equals(entry.getValue())) {
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
            final Object key = entry.getKey();
            if (parent.containsKey(key)) {
                final V value = parent.normalMap.get(key);
                if (value == null ? entry.getValue() == null : value.equals(entry.getValue())) {
                    parent.normalMap.remove(key);
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
    @Override
    public boolean containsValue(final Object value) {
        return reverseMap.containsKey(value);
    }

```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
    public V remove(final Object key) {
        V value = null;
        if (normalMap.containsKey(key)) {
            value = normalMap.remove(key);
            reverseMap.remove(value);
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
    @Override
    public K getKey(final Object value) {
        return reverseMap.get(value);
    }

```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java
        @Override
        public boolean contains(final Object key) {
            return parent.normalMap.containsKey(key);
        }

```

### SuspiciousMethodCalls
Suspicious call to 'AbstractHashedMap.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
        @Override
        public boolean contains(final Object key) {
            return parent.containsKey(key);
        }

```

### SuspiciousMethodCalls
Suspicious call to 'AbstractHashedMap.containsValue()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
        @Override
        public boolean contains(final Object value) {
            return parent.containsValue(value);
        }

```

### SuspiciousMethodCalls
Suspicious call to 'AbstractHashedMap.containsKey()'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
        @Override
        public boolean remove(final Object key) {
            final boolean result = parent.containsKey(key);
            parent.remove(key);
            return result;
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingSerializer.java`
#### Snippet
```java
                    keySet = classMetaData.keySet().toArray();
                }
                if((keySet != null) && (keySet.length > 0) && classMetaData.get(keySet[0]).getUseSchemaElement())
                {
                    addAttribute("schema", table.substring(0, index));
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/AnnotationPersistenceMappingSerializer.java`
#### Snippet
```java
                    keySet = classMetaData.keySet().toArray();
                }
                if((keySet != null) && (keySet.length > 0) && classMetaData.get(keySet[0]).getUseSchemaElement())
                {
                    ab.add("schema", table.substring(0, index));
```

### SuspiciousMethodCalls
Suspicious call to 'Map.remove()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java

            if (_map != null)
                _map.remove(key);
            _ct.removed(key, value);
            _last = null;
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            || (!_ct.getTrackKeys()
            && !_ct.getRemoved().contains(entry.getValue())))
            && (_map == null || !_map.containsKey(entry.getKey()));
    }

```

### SuspiciousMethodCalls
'Collection' may not contain objects of type 'Object'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
        if (keys == null || keys.isEmpty())
            return false;
        keys.removeAll(_ct.getRemoved());
        keys.removeAll(_ct.getChanged());
        return keys.size() > 0;
```

### SuspiciousMethodCalls
'Collection' may not contain objects of type 'Object'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/AbstractLRSProxyMap.java`
#### Snippet
```java
            return false;
        keys.removeAll(_ct.getRemoved());
        keys.removeAll(_ct.getChanged());
        return keys.size() > 0;
    }
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AuditManager.java`
#### Snippet
```java
		_lock.lock();
		try {
			AuditCallback cb = _saved.get(event.getSource());
			if (cb != null) {
				cb.audit();
```

### SuspiciousMethodCalls
Suspicious call to 'Collection.contains()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/AttachManager.java`
#### Snippet
```java
        if ((action & OpCallbacks.ACT_RUN) == 0 &&
            (action & OpCallbacks.ACT_CASCADE) != 0) {
            if(_visitedNodes.contains(_broker.getStateManager(toAttach)))
                return toAttach;
            return handleCascade(toAttach,owner);
```

### SuspiciousMethodCalls
Suspicious call to 'ArrayList.indexOf()'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DistinctResultList.java`
#### Snippet
```java
    public int lastIndexOf(Object o) {
        try {
            return _del.indexOf(o);
        } catch (RuntimeException re) {
            throw translate(re);
```

### SuspiciousMethodCalls
Suspicious call to 'List.indexOf()'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/IndexedMap.java`
#### Snippet
```java

    public int indexOf(Object key) {
        return _keys.indexOf(key);
    }
}
```

### SuspiciousMethodCalls
Suspicious call to 'List.contains()'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/IndexedMap.java`
#### Snippet
```java
    @Override
    public boolean containsValue(Object value) {
        return _values.contains(value);
    }

```

### SuspiciousMethodCalls
Suspicious call to 'List.contains()'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/IndexedMap.java`
#### Snippet
```java
    @Override
    public boolean containsKey(Object key) {
        return _keys.contains(key) && !_nulls.contains(key);
    }

```

### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/IndexedMap.java`
#### Snippet
```java
    @Override
    public boolean containsKey(Object key) {
        return _keys.contains(key) && !_nulls.contains(key);
    }

```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `openjpa-examples/openbooks/src/main/java/openbook/util/PropertyHelper.java`
#### Snippet
```java
        Properties properties = System.getProperties();
        for (Object syskey : properties.keySet()) {
            if (original.containsKey(syskey)) {
                original.put(syskey.toString(), properties.get(syskey));
            }
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/DBDictionary.java`
#### Snippet
```java
            return getDefaultIdentifierRule();
        }
        IdentifierRule nrule = namingRules.get(t);
        if (nrule == null) {
            return getDefaultIdentifierRule();
```

## RuleId[id=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `_supportedKeys`
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/conf/ConfigurationImpl.java`
#### Snippet
```java
    @Override
    public Set<String> getPropertyKeys() {
        synchronized (_supportedKeys) {
            if (_supportedKeys.size() == 0) {
                for (Value val : _vals) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_nonMapped`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    public NonPersistentMetaData[] getNonMappedInterfaces() {
        if (_locking) {
            synchronized (_nonMapped) {
                return getNonMappedInterfacesInternal();
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_aliases`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
        // We need to synchronize on _aliases because a ConcurrentModificationException can if there
        // is a thread in getAliasNames() AND this class isn't using any locking.
        synchronized (_aliases) {
            // record that this is an invalid type
            _aliases.put(alias, null);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_impls`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    void addDeclaredInterfaceImpl(ClassMetaData meta, Class<?> iface) {
        if (_locking) {
            synchronized (_impls) {
                addDeclaredInterfaceImplInternal(meta, iface);
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_impls`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
        // pc implementing types
        if (_locking) {
            synchronized (_impls) {
                updateImpls(cls, leastDerived, cls);
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_pawares`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    public NonPersistentMetaData[] getPersistenceAwares() {
        if (_locking) {
            synchronized (_pawares) {
                return getPersistenceAwaresInternal();
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_aliases`
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    public Collection<String> getAliasNames() {
        if (_locking) {
            synchronized (_aliases) {
                return getAliasNamesInternal();
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_cancelPreparedStatementsPool`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java

    private PreparedStatement getCancelPreparedStatement(PreparedStatement stmnt, Connection conn) {
        synchronized (_cancelPreparedStatementsPool) {
            if (!_cancelPreparedStatementsPool.isEmpty()) {
                CancelPreparedStatement res = _cancelPreparedStatementsPool.remove(0);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_cancelStatementPool`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
        public void close() throws SQLException {
        	super.close();
    		synchronized (_cancelStatementPool) {
    			_cancelStatementPool.add(this);
    		}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_cancelPreparedStatementsPool`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java
        public void close() throws SQLException {
        	super.close();
    		synchronized (_cancelPreparedStatementsPool) {
    			_cancelPreparedStatementsPool.add(this);
    		}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_stmnts`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java

        List<Statement> stmnts;
        synchronized (_stmnts) {
            if (_stmnts.isEmpty())
                return false;
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_cancelStatementPool`
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/JDBCStoreManager.java`
#### Snippet
```java

    private Statement getCancelStatement(Statement stmnt, Connection conn) {
        synchronized (_cancelStatementPool) {
            if (!_cancelStatementPool.isEmpty()) {
                CancelStatement res = _cancelStatementPool.remove(0);
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'obj == null' covered by subsequent condition '!(obj instanceof PersistenceCapable)'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistenceUtil.java`
#### Snippet
```java

    private static OpenJPAStateManager getStateManager(Object obj) {
        if (obj == null || !(obj instanceof PersistenceCapable)) {
            return null;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'sm == null' covered by subsequent condition '!(sm instanceof OpenJPAStateManager)'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistenceUtil.java`
#### Snippet
```java
        PersistenceCapable pc = (PersistenceCapable)obj;
        StateManager sm = pc.pcGetStateManager();
        if (sm == null || !(sm instanceof OpenJPAStateManager)) {
            return null;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'sm != null' covered by subsequent condition 'sm instanceof OpenJPAStateManager'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistenceUtil.java`
#### Snippet
```java
            PersistenceCapable pc = (PersistenceCapable)obj;
            StateManager sm = pc.pcGetStateManager();
            if (sm != null && sm instanceof OpenJPAStateManager) {
                return isLoaded((OpenJPAStateManager)sm, attr, null);
            }
```

### ConditionCoveredByFurtherCondition
Condition 'sm != null' covered by subsequent condition 'sm instanceof OpenJPAStateManager'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/OpenJPAPersistenceUtil.java`
#### Snippet
```java
            StateManager sm = pc.pcGetStateManager();

            if (sm != null && sm instanceof OpenJPAStateManager) {
                OpenJPAStateManager osm = (OpenJPAStateManager)sm;
                return osm.getObjectId();
```

### ConditionCoveredByFurtherCondition
Condition 'member != null' covered by subsequent condition 'member instanceof AnnotatedElement'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java

    private boolean isAnnotated(Member member) {
    	return member != null && member instanceof AnnotatedElement
    	    && annotatedFilter.includes((AnnotatedElement)member);
    }
```

### ConditionCoveredByFurtherCondition
Condition 'member != null' covered by subsequent condition 'member instanceof AnnotatedElement'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/PersistenceMetaDataDefaults.java`
#### Snippet
```java

    private boolean isNotTransient(Member member) {
        return member != null && member instanceof AnnotatedElement
            && nonTransientFilter.includes((AnnotatedElement)member);
    }
```

### ConditionCoveredByFurtherCondition
Condition 'ENTITY.equals(...)' covered by subsequent condition 'EMBEDDABLE.equals(...)'
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java
                    @Override
                    public AnnotationVisitor visitAnnotation(final String descriptor, final boolean visible) {
                        if (ENTITY.equals(descriptor) ||
                                EMBEDDABLE.equals(descriptor) ||
                                MAPPED_SUPERCLASS.equals(descriptor) ||
```

### ConditionCoveredByFurtherCondition
Condition 'ENTITY2.equals(...)' covered by subsequent condition 'EMBEDDABLE2.equals(...)'
in `openjpa-junit5/src/main/java/org/apache/openjpa/junit5/internal/OpenJPADirectoriesEnhancer.java`
#### Snippet
```java
                                EMBEDDABLE.equals(descriptor) ||
                                MAPPED_SUPERCLASS.equals(descriptor) ||
                                ENTITY2.equals(descriptor) ||
                                EMBEDDABLE2.equals(descriptor) ||
                                MAPPED_SUPERCLASS2.equals(descriptor)) {
```

### ConditionCoveredByFurtherCondition
Condition 'paramOpenJPAStateManager != null' covered by subsequent condition 'paramOpenJPAStateManager instanceof DetachedStateManager'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedHashSetProxy.java`
#### Snippet
```java
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'paramOpenJPAStateManager != null' covered by subsequent condition 'paramOpenJPAStateManager instanceof DetachedStateManager'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedHashSetProxy.java`
#### Snippet
```java
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'paramOpenJPAStateManager != null' covered by subsequent condition 'paramOpenJPAStateManager instanceof DetachedStateManager'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedTreeSetProxy.java`
#### Snippet
```java
    private boolean detaching(OpenJPAStateManager paramOpenJPAStateManager, int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1) ||
            (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'paramOpenJPAStateManager != null' covered by subsequent condition 'paramOpenJPAStateManager instanceof DetachedStateManager'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedLinkedListProxy.java`
#### Snippet
```java
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'paramOpenJPAStateManager != null' covered by subsequent condition 'paramOpenJPAStateManager instanceof DetachedStateManager'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedArrayListProxy.java`
#### Snippet
```java
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'paramOpenJPAStateManager != null' covered by subsequent condition 'paramOpenJPAStateManager instanceof DetachedStateManager'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedPriorityQueueProxy.java`
#### Snippet
```java
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'paramOpenJPAStateManager != null' covered by subsequent condition 'paramOpenJPAStateManager instanceof DetachedStateManager'
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/proxy/DelayedVectorProxy.java`
#### Snippet
```java
            int paramInt) {
        if ((paramOpenJPAStateManager == null && paramInt == -1)
                || (paramOpenJPAStateManager != null && paramOpenJPAStateManager instanceof DetachedStateManager)) {
            return true;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'sm != null' covered by subsequent condition 'sm instanceof StateManagerImpl'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/VersionAttachStrategy.java`
#### Snippet
```java
    private static boolean isManagedByAnotherPCtx(PersistenceCapable pc, BrokerImpl broker) {
        StateManager sm = pc.pcGetStateManager();
        if (sm != null && sm instanceof StateManagerImpl) {
            StateManagerImpl smi = (StateManagerImpl) sm;
            Broker associatedBroker = smi.getBroker();
```

### ConditionCoveredByFurtherCondition
Condition 'init != null' covered by subsequent condition 'init instanceof TimeZone'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/SingleFieldManager.java`
#### Snippet
```java
                } else {
                    Object init = fmd.getInitializer();
                    if (init != null && init instanceof TimeZone) {
                        ((Calendar) proxy).setTimeZone((TimeZone)init);
                    }
```

### ConditionCoveredByFurtherCondition
Condition 'obj != null' covered by subsequent condition 'obj instanceof Proxy'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/DetachManager.java`
#### Snippet
```java
         */
        private Object reproxy(Object obj, int field) {
            if (obj != null && _detSM != null && obj instanceof Proxy)
                ((Proxy) obj).setOwner(_detSM, field);
            return obj;
```

### ConditionCoveredByFurtherCondition
Condition 'value2 != null' covered by subsequent condition 'value2 instanceof Subquery'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/ExpressionStoreQuery.java`
#### Snippet
```java
                        // skip
                    }
                    if (value2 != null && value2 instanceof Subquery)
                        ;  // complex having with subquery, validation is performed by DBMS
                    else
```

### ConditionCoveredByFurtherCondition
Condition 'curVal != null' covered by subsequent condition 'curVal instanceof PersistenceCapable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
                            //    return;
                            //else {
                                if (curVal != null && newVal != null &&
                                    curVal instanceof PersistenceCapable && newVal instanceof PersistenceCapable) {
                                    PersistenceCapable curPc = (PersistenceCapable) curVal;
```

### ConditionCoveredByFurtherCondition
Condition 'newVal != null' covered by subsequent condition 'newVal instanceof PersistenceCapable'
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/StateManagerImpl.java`
#### Snippet
```java
                            //    return;
                            //else {
                                if (curVal != null && newVal != null &&
                                    curVal instanceof PersistenceCapable && newVal instanceof PersistenceCapable) {
                                    PersistenceCapable curPc = (PersistenceCapable) curVal;
```

### ConditionCoveredByFurtherCondition
Condition 'icon != null' covered by subsequent condition 'icon instanceof ImageIcon'
in `openjpa-examples/openbooks/src/main/java/jpa/tools/swing/ErrorDialog.java`
#### Snippet
```java
        setTitle(t.getClass().getName());
        setModal(true);
        if (icon != null && icon instanceof ImageIcon)
            setIconImage(((ImageIcon)icon).getImage());
        setDefaultCloseOperation(WindowConstants.DISPOSE_ON_CLOSE);
```

### ConditionCoveredByFurtherCondition
Condition 'ret != null' covered by subsequent condition 'ret instanceof ProxyCalendar'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/InstanceResultObjectProvider.java`
#### Snippet
```java
            mapping = _mapping;
        Object ret = res.load(mapping, getStore(), getFetchConfiguration());
        if (ret != null && ret instanceof ProxyCalendar) {
            ret = ((ProxyCalendar) ret).copy(ret);
        }
```

### ConditionCoveredByFurtherCondition
Condition 'param != null' covered by subsequent condition 'param instanceof Null'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/JDBCExpressionFactory.java`
#### Snippet
```java
                        get("type-argument-unsupported", path.last().getName()));
                if (isNotEqual) {
                    if (param != null && param instanceof Null)
                        throw new UserException(_loc.
                            get("type-in-expression-unsupported", path.getSchemaAlias()));
```

### ConditionCoveredByFurtherCondition
Condition 'param != null' covered by subsequent condition 'param instanceof CollectionParam'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/JDBCExpressionFactory.java`
#### Snippet
```java
                }
            }
            if (param != null && param instanceof CollectionParam)
                throw new UserException(_loc.
                    get("collection-param-unsupported"));
```

### ConditionCoveredByFurtherCondition
Condition 'strategy == null' covered by subsequent condition '!(strategy instanceof LRSMapFieldStrategy)'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java

    private void validateMapStrategy(Strategy strategy) {
        if (strategy == null ||
            !(strategy instanceof LRSMapFieldStrategy))
            throw new RuntimeException("Invalid map field strategy:"+strategy);
```

### ConditionCoveredByFurtherCondition
Condition 'fkObj == null' covered by subsequent condition '!(fkObj instanceof FKMapKey)'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/ForeignKey.java`
#### Snippet
```java
                return true;
            }
            if (fkObj == null || !(fkObj instanceof FKMapKey)) {
                return false;
            }
```

### ConditionCoveredByFurtherCondition
Condition 'hint == null' covered by subsequent condition '!(hint instanceof String \|\| hint instanceof String\[\])'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
            return _slices;
        Object hint = fetch.getHint(SlicePersistence.HINT_TARGET);
        if (hint == null || !(hint instanceof String || hint instanceof String[]))
            return _slices;
        String[] targetNames = hint instanceof String
```

### ConditionCoveredByFurtherCondition
Condition 'edata == null' covered by subsequent condition '!(edata instanceof ConnectionInfo)'
in `openjpa-slice/src/main/java/org/apache/openjpa/slice/jdbc/DistributedJDBCStoreManager.java`
#### Snippet
```java
     */
    private String estimateSlice(OpenJPAStateManager sm, Object edata) {
        if (edata == null || !(edata instanceof ConnectionInfo))
            return null;

```

## RuleId[id=CachedNumberConstructorCall]
### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/Filters.java`
#### Snippet
```java
            else if (type == Integer.class && o instanceof String) {
                try {
                    return new Integer(o.toString());
                }
                catch (NumberFormatException e) {
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                // for this instance, and set the version
                _object = new ObjectData(oid, meta);
                _object.setVersion(new Long(attrs.getValue("version")));
            }
            else if ("field".equals(qName)) { // field
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.BYTE:
                case JavaTypes.BYTE_OBJ:
                    return new Byte(str);

                case JavaTypes.CHAR:
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.INT:
                case JavaTypes.INT_OBJ:
                    return new Integer(str);

                case JavaTypes.LONG:
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.LONG:
                case JavaTypes.LONG_OBJ:
                    return new Long(str);

                case JavaTypes.SHORT:
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-xmlstore/src/main/java/org/apache/openjpa/xmlstore/XMLFileHandler.java`
#### Snippet
```java
                case JavaTypes.SHORT:
                case JavaTypes.SHORT_OBJ:
                    return new Short(str);

                case JavaTypes.NUMBER:
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/MappingInfo.java`
#### Snippet
```java
                try {
                    if (targetNameStr.indexOf('.') == -1)
                        target = new Integer(targetNameStr);
                    else
                        target = new Double(targetNameStr);
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/XMLSchemaParser.java`
#### Snippet
```java
            return val.substring(1, val.length() - 1);
        if (val.indexOf('.') == -1)
            return new Long(val);
        return new Double(val);
    }
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
            case JavaTypes.BYTE:
            case JavaTypes.BYTE_OBJ:
                _default = new Byte(_defaultStr);
                break;
            case JavaTypes.CHAR:
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/schema/Column.java`
#### Snippet
```java
            case JavaTypes.SHORT:
            case JavaTypes.SHORT_OBJ:
                _default = new Short(_defaultStr);
                break;
            case JavaTypes.DATE:
```

## RuleId[id=CloneDeclaresCloneNotSupported]
### CloneDeclaresCloneNotSupported
`clone()` does not declare 'CloneNotSupportedException'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/concurrent/ConcurrentHashMap.java`
#### Snippet
```java

        @Override
        protected Object clone() {
            // It is the callers responsibility to set the next field
            // correctly.
```

### CloneDeclaresCloneNotSupported
`clone()` does not declare 'CloneNotSupportedException'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractHashedMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected AbstractHashedMap<K, V> clone() {
        try {
            final AbstractHashedMap<K, V> cloned = (AbstractHashedMap<K, V>) super.clone();
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/GeneralCase.java`
#### Snippet
```java
                        eval(candidate, orig, ctx, params);
            else
                continue;
        }
        return _val.eval(candidate, orig, ctx, params);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/GeneralCase.java`
#### Snippet
```java
                        eval(candidate, null, ctx, params);
            else
                continue;
        }
        return _val.eval(candidate, null, ctx, params);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/SimpleCase.java`
#### Snippet
```java
                        eval(candidate, null, ctx, params);
            else
                continue;
        }
        return _val.eval(candidate, null, ctx, params);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/SimpleCase.java`
#### Snippet
```java
                        eval(candidate, orig, ctx, params);
            else
                continue;
        }
        return _val.eval(candidate, orig, ctx, params);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Coalesce.java`
#### Snippet
```java
               return o1;
           else
               continue;
        }
        return _val[_val.length-1].eval(candidate, orig, ctx, params);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/Coalesce.java`
#### Snippet
```java
               return o1;
           else
               continue;
        }
        return _val[_val.length-1].eval(candidate, null, ctx, params);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `openjpa-kernel/src/main/java/org/apache/openjpa/datacache/DataCacheStoreManager.java`
#### Snippet
```java
            if (oldpc != null && compareVersion(holder.sm,
                holder.sm.getVersion(), oldpc.getVersion()) == VERSION_EARLIER)
                continue;
            else
                transformed.add(holder.pcdata);
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy field '_loaders'
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/MultiClassLoader.java`
#### Snippet
```java
     * Construct with the class loaders of another multi loader.
     */
    public MultiClassLoader(MultiClassLoader other) {
        super(null);
        addClassLoaders(other);
```

### CopyConstructorMissesField
Copy constructor does not copy 4 fields
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/SQLBuffer.java`
#### Snippet
```java
     * Copy constructor.
     */
    public SQLBuffer(SQLBuffer buf) {
        _dict = buf._dict;
        append(buf);
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/CriteriaQueryImpl.java`
#### Snippet
```java

    @Override
    public boolean equals(Object other) {
        if(other == null) {
            return false;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractSetDecorator.java`
#### Snippet
```java

    @Override
    public boolean equals(final Object object) {
        return object == this || decorated().equals(object);
    }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractMapEntryDecorator.java`
#### Snippet
```java

    @Override
    public boolean equals(final Object object) {
        if (object == this) {
            return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/MapBackedSet.java`
#### Snippet
```java

    @Override
    public boolean equals(final Object obj) {
        return map.keySet().equals(obj);
    }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractListDecorator.java`
#### Snippet
```java

    @Override
    public boolean equals(final Object object) {
        return object == this || decorated().equals(object);
    }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractMapDecorator.java`
#### Snippet
```java

    @Override
    public boolean equals(final Object object) {
        if (object == this) {
            return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java

    @Override
    public boolean equals(final Object obj) {
        return normalMap.equals(obj);
    }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/AbstractDualBidiMap.java`
#### Snippet
```java

        @Override
        public boolean equals(final Object object) {
            return object == this || decorated().equals(object);
        }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/TreeBidiMap.java`
#### Snippet
```java
     */
    @Override
    public boolean equals(final Object obj) {
        return this.doEquals(obj, DataElement.KEY);
    }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-lib/src/main/java/org/apache/openjpa/lib/util/collections/TreeBidiMap.java`
#### Snippet
```java

        @Override
        public boolean equals(final Object obj) {
            return TreeBidiMap.this.doEquals(obj, DataElement.VALUE);
        }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/Extensions.java`
#### Snippet
```java

        @Override
        public boolean equals(Object other) {
            if (other == this)
                return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/util/StoreFacadeTypeRegistry.java`
#### Snippet
```java

        @Override
        public boolean equals(Object other) {
            if (other == this)
                return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/CandidatePath.java`
#### Snippet
```java

        @Override
        public boolean equals(Object other) {
            if (other == this)
                return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java

        @Override
        public boolean equals(Object o) {
            return _res.equals(o);
        }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/exps/InMemoryExpressionFactory.java`
#### Snippet
```java

        @Override
        public boolean equals(Object other) {
            if (other == this)
                return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/sql/RowManagerImpl.java`
#### Snippet
```java

        @Override
        public boolean equals(Object other) {
            if (other == null)
                return false;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java

        @Override
        public boolean equals(Object other) {
            if (other == null)
                return false;
```

## RuleId[id=SuspiciousIndentAfterControlStatement]
### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-persistence-jdbc/src/main/java/org/apache/openjpa/persistence/jdbc/XMLPersistenceMappingParser.java`
#### Snippet
```java
        String name = attrs.getValue("name");
        if (!StringUtil.isEmpty(name));
            col.setIdentifier(DBIdentifier.newColumn(name, delimit()));
        String columnDefinition= attrs.getValue("column-definition");
        if (!StringUtil.isEmpty(columnDefinition))
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
    public void clear() {
            if (_log.isTraceEnabled())
            _log.trace(_loc.get("clear-repos", this));
        if (_locking) {
            synchronized (this) {
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'for' statement
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/MetaDataRepository.java`
#### Snippet
```java
                getMetaData(classMetaData.getDescribedType(), classMetaData.getEnvClassLoader(), true);

            List<ClassMetaData> resolved = new ArrayList<>(_metas.size());
            for (ClassMetaData meta : _metas.values()) {
                if (meta != null)
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        for (Object actual : params.keySet()) {
            if (!paramTypes.containsKey(actual))
            throw new UserException(_loc.get("unbound-params",
                actual, paramTypes.keySet()));
        }
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-kernel/src/main/java/org/apache/openjpa/kernel/QueryImpl.java`
#### Snippet
```java
        for (Object expected : paramTypes.keySet()) {
            if (!params.containsKey(expected))
            throw new UserException(_loc.get("unbound-params",
                expected, paramTypes.keySet()));
        }
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-kernel/src/main/java/org/apache/openjpa/enhance/Reflection.java`
#### Snippet
```java
                  if (setter.getReturnType() == void.class ||
                      setter.getReturnType().isAssignableFrom(c))
                  result.add(prop);
                } catch (NoSuchMethodException e) {

```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	default :
            	    if ( cnt6 >= 1 ) break loop6;
                        EarlyExitException eee =
                            new EarlyExitException(6, input);
                        throw eee;
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	default :
            	    if ( cnt20 >= 1 ) break loop20;
                        EarlyExitException eee =
                            new EarlyExitException(20, input);
                        throw eee;
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    	default :
                    	    if ( cnt8 >= 1 ) break loop8;
                                EarlyExitException eee =
                                    new EarlyExitException(8, input);
                                throw eee;
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    	default :
                    	    if ( cnt12 >= 1 ) break loop12;
                                EarlyExitException eee =
                                    new EarlyExitException(12, input);
                                throw eee;
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    	default :
                    	    if ( cnt15 >= 1 ) break loop15;
                                EarlyExitException eee =
                                    new EarlyExitException(15, input);
                                throw eee;
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
                    	default :
                    	    if ( cnt17 >= 1 ) break loop17;
                                EarlyExitException eee =
                                    new EarlyExitException(17, input);
                                throw eee;
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-examples/openbooks/src/main/java/openbook/tools/parser/JavaLexer.java`
#### Snippet
```java
            	default :
            	    if ( cnt1 >= 1 ) break loop1;
                        EarlyExitException eee =
                            new EarlyExitException(1, input);
                        throw eee;
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/kernel/exps/PCPath.java`
#### Snippet
```java
            appendTo(sel, state, sql, cols[i]);
            if (i < cols.length -1)
            sql.append(", ");
        }
    }
```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'seq' to 'SequenceMetaData'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataSerializer.java`
#### Snippet
```java
                serializationSort(seqs);
                for (Object seq : seqs) {
                    serializeSequence((SequenceMetaData) seq);
                }
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'adapter' to 'MethodLifecycleCallbacks'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/XMLPersistenceMetaDataParser.java`
#### Snippet
```java
            else {
                MetaDataParsers.validateMethodsForSameCallback(_cls,
                        _callbacks[event], ((MethodLifecycleCallbacks) adapter).
                                getCallbackMethod(), callback, _conf, getLog());

```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'corrJoin' to 'Join'
in `openjpa-persistence/src/main/java/org/apache/openjpa/persistence/criteria/SubqueryImpl.java`
#### Snippet
```java
            _corrJoins = new ArrayList<>();
        _corrJoins.add(corrJoin);
        return (Join<X,Y>)corrJoin;
    }

```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'obj' to 'URL'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/AbstractCFMetaDataFactory.java`
#### Snippet
```java

                    for (Object obj : urls) {
                        url = (URL) obj;
                        clss = cparser.parseTypeNames(new URLMetaDataIterator
                                (url));
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'comp' to 'Comparator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            case JavaTypes.ARRAY:
                List l = JavaTypes.toList(val, _elem.getType(), true);
                Collections.sort(l, (Comparator<? super Order>) comp);
                return JavaTypes.toArray(l, _elem.getType());
            case JavaTypes.COLLECTION:
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'comp' to 'Comparator'
in `openjpa-kernel/src/main/java/org/apache/openjpa/meta/FieldMetaData.java`
#### Snippet
```java
            case JavaTypes.COLLECTION:
                if (val instanceof List)
                    Collections.sort((List) val, (Comparator<? super Order>) comp);
                return val;
            default:
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'cachedFK' to 'ForeignKey'
in `openjpa-jdbc/src/main/java/org/apache/openjpa/jdbc/meta/ValueMappingImpl.java`
#### Snippet
```java
                Object cachedFK = _targetFKs.get(target);
                if (cachedFK != null)
                    return (ForeignKey) cachedFK;
            } else
                _targetFKs = new HashMap<>();
```

