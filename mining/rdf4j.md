# rdf4j 
 
# Bad smells
I found 7703 bad smells with 731 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| MarkedForRemoval | 1216 | false |
| MissortedModifiers | 799 | false |
| JUnit5AssertionsConverter | 665 | false |
| BoundedWildcard | 446 | false |
| UnnecessaryFullyQualifiedName | 415 | false |
| AssignmentToMethodParameter | 355 | false |
| ReturnNull | 322 | false |
| InstanceofCatchParameter | 314 | false |
| UnnecessaryModifier | 291 | true |
| RedundantFieldInitialization | 275 | false |
| UtilityClassWithoutPrivateConstructor | 160 | true |
| NestedAssignment | 156 | false |
| DeprecatedIsStillUsed | 151 | false |
| StringConcatenationInsideStringBufferAppend | 144 | false |
| PublicFieldAccessedInSynchronizedContext | 131 | false |
| SystemOutErr | 88 | false |
| DataFlowIssue | 87 | false |
| ThrowablePrintStackTrace | 84 | false |
| ConstantValue | 83 | false |
| NonProtectedConstructorInAbstractClass | 81 | true |
| SizeReplaceableByIsEmpty | 76 | true |
| FieldAccessedSynchronizedAndUnsynchronized | 67 | false |
| OptionalGetWithoutIsPresent | 63 | false |
| StringBufferReplaceableByString | 51 | false |
| DuplicateThrows | 50 | false |
| TrivialStringConcatenation | 44 | false |
| Convert2MethodRef | 44 | false |
| OptionalUsedAsFieldOrParameterType | 41 | false |
| EmptyMethod | 38 | false |
| CommentedOutCode | 36 | false |
| FinalStaticMethod | 34 | false |
| ZeroLengthArrayInitialization | 32 | false |
| SynchronizeOnThis | 30 | false |
| UnnecessaryLocalVariable | 30 | true |
| IgnoreResultOfCall | 27 | false |
| UnnecessaryToStringCall | 26 | true |
| ClassNameSameAsAncestorName | 25 | false |
| CodeBlock2Expr | 23 | true |
| UnstableApiUsage | 22 | false |
| UnnecessarySuperQualifier | 21 | false |
| StaticCallOnSubclass | 20 | false |
| UnusedAssignment | 19 | false |
| ReplaceAssignmentWithOperatorAssignment | 18 | false |
| DynamicRegexReplaceableByCompiledPattern | 17 | false |
| StaticPseudoFunctionalStyleMethod | 17 | false |
| FieldMayBeStatic | 17 | false |
| AbstractClassNeverImplemented | 16 | false |
| RegExpRedundantEscape | 15 | false |
| KeySetIterationMayUseEntrySet | 15 | false |
| UnnecessaryQualifierForThis | 15 | false |
| OctalLiteral | 14 | false |
| StringEqualsEmptyString | 14 | false |
| SynchronizeOnNonFinalField | 14 | false |
| ToArrayCallWithZeroLengthArrayArgument | 13 | true |
| RedundantMethodOverride | 13 | false |
| EqualsAndHashcode | 13 | false |
| StringOperationCanBeSimplified | 13 | false |
| ThreadDumpStack | 12 | false |
| ObsoleteCollection | 12 | false |
| NonShortCircuitBoolean | 12 | false |
| ReplaceNullCheck | 12 | false |
| CStyleArrayDeclaration | 11 | false |
| RefusedBequest | 11 | false |
| TestCaseWithConstructor | 10 | false |
| NonFinalFieldOfException | 10 | false |
| InnerClassMayBeStatic | 10 | true |
| SimplifyStreamApiCallChains | 9 | false |
| ProtectedMemberInFinalClass | 9 | true |
| RedundantSuppression | 9 | false |
| StringEquality | 9 | false |
| ThrowFromFinallyBlock | 9 | false |
| SimplifyOptionalCallChains | 9 | false |
| IntegerMultiplicationImplicitCastToLong | 8 | false |
| NonSerializableFieldInSerializableClass | 8 | false |
| MissingDeprecatedAnnotation | 8 | false |
| StringRepeatCanBeUsed | 7 | false |
| OptionalIsPresent | 7 | false |
| Convert2Lambda | 7 | false |
| WhileCanBeForeach | 6 | false |
| BusyWait | 6 | false |
| RedundantArrayCreation | 6 | true |
| TypeParameterExtendsObject | 6 | false |
| ConditionCoveredByFurtherCondition | 6 | false |
| CopyConstructorMissesField | 6 | false |
| EmptyStatementBody | 5 | false |
| EnumSwitchStatementWhichMissesCases | 5 | false |
| JUnitMalformedDeclaration | 5 | false |
| StringBufferReplaceableByStringBuilder | 5 | false |
| MagicConstant | 4 | false |
| AccessStaticViaInstance | 4 | false |
| NonSynchronizedMethodOverridesSynchronizedMethod | 4 | false |
| SuspiciousToArrayCall | 4 | false |
| ArrayEquality | 4 | false |
| AssignmentToForLoopParameter | 4 | false |
| Java8MapApi | 4 | false |
| WrapperTypeMayBePrimitive | 3 | false |
| EqualsBetweenInconvertibleTypes | 3 | false |
| UnnecessaryUnboxing | 3 | false |
| OptionalAssignedToNull | 3 | false |
| UnnecessaryInitCause | 3 | false |
| UnnecessaryStringEscape | 3 | true |
| WhileLoopSpinsOnField | 3 | false |
| RedundantStringFormatCall | 3 | false |
| MethodOverridesStaticMethod | 3 | false |
| CatchMayIgnoreException | 3 | false |
| MismatchedJavadocCode | 3 | false |
| NestedSynchronizedStatement | 3 | false |
| UnnecessaryContinue | 3 | false |
| RegExpUnnecessaryNonCapturingGroup | 3 | false |
| EqualsWhichDoesntCheckParameterClass | 3 | false |
| PointlessArithmeticExpression | 2 | false |
| DuplicateCondition | 2 | false |
| CastConflictsWithInstanceof | 2 | false |
| IndexOfReplaceableByContains | 2 | false |
| ManualArrayToCollectionCopy | 2 | false |
| OptionalContainsCollection | 2 | false |
| PointlessBooleanExpression | 2 | true |
| UseBulkOperation | 2 | false |
| DuplicateBranchesInSwitch | 2 | false |
| DuplicateExpressions | 2 | false |
| IfStatementWithIdenticalBranches | 2 | false |
| RedundantImplements | 2 | false |
| FuseStreamOperations | 2 | false |
| NullArgumentToVariableArgMethod | 2 | false |
| IOResource | 2 | false |
| RedundantOperationOnEmptyContainer | 2 | false |
| RedundantLengthCheck | 2 | false |
| ComparatorCombinators | 2 | false |
| AbstractMethodCallInConstructor | 2 | false |
| UnnecessaryBoxing | 2 | false |
| CastCanBeRemovedNarrowingVariableType | 2 | false |
| TestCaseWithNoTestMethods | 2 | false |
| Finalize | 1 | false |
| MismatchedArrayReadWrite | 1 | false |
| AssignmentToStaticFieldFromInstanceMethod | 1 | false |
| HtmlWrongAttributeValue | 1 | false |
| CallToStringConcatCanBeReplacedByOperator | 1 | false |
| UnnecessarySemicolon | 1 | false |
| TypeParameterHidesVisibleType | 1 | false |
| MethodOverloadsParentMethod | 1 | false |
| UnnecessaryCallToStringValueOf | 1 | false |
| CollectionContainsUrl | 1 | false |
| ComparatorMethodParameterNotUsed | 1 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 1 | false |
| AssignmentToLambdaParameter | 1 | false |
| WaitNotInLoop | 1 | false |
| ConditionalBreakInInfiniteLoop | 1 | false |
| RedundantClassCall | 1 | false |
| LongLiteralsEndingWithLowercaseL | 1 | false |
| RegExpSimplifiable | 1 | false |
| UnnecessaryReturn | 1 | true |
| SillyAssignment | 1 | false |
| NegativeIntConstantInLongContext | 1 | false |
| RedundantStreamOptionalCall | 1 | false |
| ExceptionNameDoesntEndWithException | 1 | false |
| NonExceptionNameEndsWithException | 1 | false |
| Java9CollectionFactory | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Resource\[contexts.size()\]'
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionSAXParser.java`
#### Snippet
```java
		}

		return contexts.toArray(new Resource[contexts.size()]);
	}
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Resource\[list.size()\]'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
					}
				}
				return list.toArray(new Resource[list.size()]);
			}
		}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new MemResource\[contextSet.size()\]'
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
			}

			memContexts = contextSet.toArray(new MemResource[contextSet.size()]);
			smallestList = statements;
		}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Value\[values.size()\]'
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/csv/SPARQLResultsCSVMappingStrategy.java`
#### Snippet
```java
			values.add(v);
		}
		return new ListBindingSet(bindingNames, values.toArray(new Value[values.size()]));
	}
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Value\[values.size()\]'
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVMappingStrategy.java`
#### Snippet
```java
			values.add(parseValue(valueString));
		}
		return new ListBindingSet(bindingNames, values.toArray(new Value[values.size()]));
	}

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new IRI\[objects.size()\]'
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			Set<Value> objects = model.filter(resource, READ_CONTEXT, null).objects();
			setReadContexts(objects.toArray(new IRI[objects.size()]));

			objects = model.filter(resource, ADD_CONTEXT, null).objects();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new IRI\[objects.size()\]'
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			objects = model.filter(resource, ADD_CONTEXT, null).objects();
			setAddContexts(objects.toArray(new IRI[objects.size()]));

			objects = model.filter(resource, REMOVE_CONTEXT, null).objects();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new IRI\[objects.size()\]'
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			objects = model.filter(resource, REMOVE_CONTEXT, null).objects();
			setRemoveContexts(objects.toArray(new IRI[objects.size()]));

			objects = model.filter(resource, ARCHIVE_CONTEXT, null).objects();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new IRI\[objects.size()\]'
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			objects = model.filter(resource, ARCHIVE_CONTEXT, null).objects();
			setArchiveContexts(objects.toArray(new IRI[objects.size()]));

			Models.objectIRI(model.getStatements(resource, INSERT_CONTEXT, null))
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new IRI\[removeGraphs.size()\]'
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/RDFSailRemover.java`
#### Snippet
```java
					final Set<IRI> removeGraphs = uc.getDataset().getDefaultRemoveGraphs();
					if (!removeGraphs.isEmpty()) {
						IRI[] ctxts = removeGraphs.toArray(new IRI[removeGraphs.size()]);
						con.removeStatement(uc, subj, pred, obj, ctxts);
					} else {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new IRI\[set.size()\]'
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
		}

		return set.toArray(new IRI[set.size()]);
	}

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Resource\[list.size()\]'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
					}
				}
				return list.toArray(new Resource[list.size()]);
			}
		}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new BindingSet\[collection.size()\]'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java

	private Stream<BindingSet> sort(Collection<BindingSet> collection) {
		BindingSet[] array = collection.toArray(new BindingSet[collection.size()]);
		Arrays.parallelSort(array, comparator);
		Stream<BindingSet> stream = Stream.of(array);
```

## RuleId[id=Finalize]
### Finalize
'finalize()' should not be overridden
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java

				@Override
				protected void finalize() throws Throwable {
					logger.debug("finalizing {}", dataDir);
					if (disk == this) {
```

## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
	public LmdbValue getLazyValue(long id) throws IOException {
		// Check value cache
		Long cacheID = id;
		LmdbValue resultValue = cachedValue(cacheID);

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
	public LmdbValue getValue(long id) throws IOException {
		// Check value cache
		Long cacheID = id;
		LmdbValue resultValue = cachedValue(cacheID);

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/string/StrLen.java`
#### Snippet
```java
				// literal. Shouldn't createLiteral(int) return an xsd:integer
				// rather than an xsd:int?
				Integer length = literal.getLabel().length();
				return valueFactory.createLiteral(length.toString(), XSD.INTEGER);
			} else {
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTQNAME = 126;
	public int JJTBLANKNODE = 127;
	public int JJTGRAPHREFALL = 128;
	public int JJTGRAPHORDEFAULT = 129;
	public int JJTUNPARSEDQUADDATABLOCK = 130;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCONCAT = 95;
	public int JJTCONTAINS = 96;
	public int JJTENCODEFORURI = 97;
	public int JJTIF = 98;
	public int JJTIN = 99;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDESCRIBEQUERY = 11;
	public int JJTDESCRIBE = 12;
	public int JJTASKQUERY = 13;
	public int JJTDATASETCLAUSE = 14;
	public int JJTWHERECLAUSE = 15;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCEIL = 83;
	public int JJTFLOOR = 84;
	public int JJTROUND = 85;
	public int JJTSUBSTR = 86;
	public int JJTSTRLEN = 87;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSHA512 = 71;
	public int JJTNOW = 72;
	public int JJTYEAR = 73;
	public int JJTMONTH = 74;
	public int JJTDAY = 75;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBOUND = 106;
	public int JJTSAMETERM = 107;
	public int JJTISIRI = 108;
	public int JJTISBLANK = 109;
	public int JJTISLITERAL = 110;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTLIMIT = 25;
	public int JJTOFFSET = 26;
	public int JJTGRAPHPATTERNGROUP = 27;
	public int JJTBASICGRAPHPATTERN = 28;
	public int JJTOPTIONALGRAPHPATTERN = 29;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTREGEXEXPRESSION = 119;
	public int JJTEXISTSFUNC = 120;
	public int JJTNOTEXISTSFUNC = 121;
	public int JJTRDFLITERAL = 122;
	public int JJTTRUE = 123;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTISBLANK = 109;
	public int JJTISLITERAL = 110;
	public int JJTISNUMERIC = 111;
	public int JJTBNODEFUNC = 112;
	public int JJTIRIFUNC = 113;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTR = 102;
	public int JJTLANG = 103;
	public int JJTLANGMATCHES = 104;
	public int JJTDATATYPE = 105;
	public int JJTBOUND = 106;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMAX = 62;
	public int JJTAVG = 63;
	public int JJTSAMPLE = 64;
	public int JJTGROUPCONCAT = 65;
	public int JJTMD5 = 66;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTWHERECLAUSE = 15;
	public int JJTBINDINGSCLAUSE = 16;
	public int JJTINLINEDATA = 17;
	public int JJTBINDINGSET = 18;
	public int JJTBINDINGVALUE = 19;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRUUID = 117;
	public int JJTBIND = 118;
	public int JJTREGEXEXPRESSION = 119;
	public int JJTEXISTSFUNC = 120;
	public int JJTNOTEXISTSFUNC = 121;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTUPDATECONTAINER = 1;
	public int JJTQUERYCONTAINER = 2;
	public int JJTVOID = 3;
	public int JJTBASEDECL = 4;
	public int JJTPREFIXDECL = 5;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTIRI = 44;
	public int JJTPATHONEINPROPERTYSET = 45;
	public int JJTPATHMOD = 46;
	public int JJTBLANKNODEPROPERTYLIST = 47;
	public int JJTCOLLECTION = 48;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDATASETCLAUSE = 14;
	public int JJTWHERECLAUSE = 15;
	public int JJTBINDINGSCLAUSE = 16;
	public int JJTINLINEDATA = 17;
	public int JJTBINDINGSET = 18;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCLEAR = 133;
	public int JJTDROP = 134;
	public int JJTADD = 135;
	public int JJTMOVE = 136;
	public int JJTCOPY = 137;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
public interface SyntaxTreeBuilderTreeConstants {
	public int JJTUPDATESEQUENCE = 0;
	public int JJTUPDATECONTAINER = 1;
	public int JJTQUERYCONTAINER = 2;
	public int JJTVOID = 3;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTUNPARSEDQUADDATABLOCK = 130;
	public int JJTQUADSNOTTRIPLES = 131;
	public int JJTLOAD = 132;
	public int JJTCLEAR = 133;
	public int JJTDROP = 134;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTISNUMERIC = 111;
	public int JJTBNODEFUNC = 112;
	public int JJTIRIFUNC = 113;
	public int JJTSTRDT = 114;
	public int JJTSTRLANG = 115;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTORDERCONDITION = 24;
	public int JJTLIMIT = 25;
	public int JJTOFFSET = 26;
	public int JJTGRAPHPATTERNGROUP = 27;
	public int JJTBASICGRAPHPATTERN = 28;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCONSTTRIPLEREF = 51;
	public int JJTOR = 52;
	public int JJTAND = 53;
	public int JJTCOMPARE = 54;
	public int JJTINFIX = 55;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSERVICEGRAPHPATTERN = 33;
	public int JJTCONSTRAINT = 34;
	public int JJTFUNCTIONCALL = 35;
	public int JJTTRIPLESSAMESUBJECT = 36;
	public int JJTPROPERTYLIST = 37;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTRDFLITERAL = 122;
	public int JJTTRUE = 123;
	public int JJTFALSE = 124;
	public int JJTSTRING = 125;
	public int JJTQNAME = 126;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDATATYPE = 105;
	public int JJTBOUND = 106;
	public int JJTSAMETERM = 107;
	public int JJTISIRI = 108;
	public int JJTISBLANK = 109;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSHA256 = 69;
	public int JJTSHA384 = 70;
	public int JJTSHA512 = 71;
	public int JJTNOW = 72;
	public int JJTYEAR = 73;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTFUNCTIONCALL = 35;
	public int JJTTRIPLESSAMESUBJECT = 36;
	public int JJTPROPERTYLIST = 37;
	public int JJTOBJECTLIST = 38;
	public int JJTTRIPLESSAMESUBJECTPATH = 39;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTVAR = 49;
	public int JJTTRIPLEREF = 50;
	public int JJTCONSTTRIPLEREF = 51;
	public int JJTOR = 52;
	public int JJTAND = 53;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTIRIFUNC = 113;
	public int JJTSTRDT = 114;
	public int JJTSTRLANG = 115;
	public int JJTUUID = 116;
	public int JJTSTRUUID = 117;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTGROUPCONDITION = 22;
	public int JJTHAVINGCLAUSE = 23;
	public int JJTORDERCONDITION = 24;
	public int JJTLIMIT = 25;
	public int JJTOFFSET = 26;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPATHSEQUENCE = 42;
	public int JJTPATHELT = 43;
	public int JJTIRI = 44;
	public int JJTPATHONEINPROPERTYSET = 45;
	public int JJTPATHMOD = 46;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMONTH = 74;
	public int JJTDAY = 75;
	public int JJTHOURS = 76;
	public int JJTMINUTES = 77;
	public int JJTSECONDS = 78;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDAY = 75;
	public int JJTHOURS = 76;
	public int JJTMINUTES = 77;
	public int JJTSECONDS = 78;
	public int JJTTIMEZONE = 79;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCONSTRAINT = 34;
	public int JJTFUNCTIONCALL = 35;
	public int JJTTRIPLESSAMESUBJECT = 36;
	public int JJTPROPERTYLIST = 37;
	public int JJTOBJECTLIST = 38;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCOPY = 137;
	public int JJTCREATE = 138;
	public int JJTINSERTDATA = 139;
	public int JJTDELETEDATA = 140;
	public int JJTDELETEWHERE = 141;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPATHONEINPROPERTYSET = 45;
	public int JJTPATHMOD = 46;
	public int JJTBLANKNODEPROPERTYLIST = 47;
	public int JJTCOLLECTION = 48;
	public int JJTVAR = 49;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRDT = 114;
	public int JJTSTRLANG = 115;
	public int JJTUUID = 116;
	public int JJTSTRUUID = 117;
	public int JJTBIND = 118;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTTIMEZONE = 79;
	public int JJTTZ = 80;
	public int JJTRAND = 81;
	public int JJTABS = 82;
	public int JJTCEIL = 83;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSELECT = 7;
	public int JJTPROJECTIONELEM = 8;
	public int JJTCONSTRUCTQUERY = 9;
	public int JJTCONSTRUCT = 10;
	public int JJTDESCRIBEQUERY = 11;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSAMPLE = 64;
	public int JJTGROUPCONCAT = 65;
	public int JJTMD5 = 66;
	public int JJTSHA1 = 67;
	public int JJTSHA224 = 68;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTGROUPCONCAT = 65;
	public int JJTMD5 = 66;
	public int JJTSHA1 = 67;
	public int JJTSHA224 = 68;
	public int JJTSHA256 = 69;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTFALSE = 124;
	public int JJTSTRING = 125;
	public int JJTQNAME = 126;
	public int JJTBLANKNODE = 127;
	public int JJTGRAPHREFALL = 128;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMODIFY = 144;

	public String[] jjtNodeName = {
			"UpdateSequence",
			"UpdateContainer",
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTUPDATESEQUENCE = 0;
	public int JJTUPDATECONTAINER = 1;
	public int JJTQUERYCONTAINER = 2;
	public int JJTVOID = 3;
	public int JJTBASEDECL = 4;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRLANG = 115;
	public int JJTUUID = 116;
	public int JJTSTRUUID = 117;
	public int JJTBIND = 118;
	public int JJTREGEXEXPRESSION = 119;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTLOWERCASE = 89;
	public int JJTSTRSTARTS = 90;
	public int JJTSTRENDS = 91;
	public int JJTSTRBEFORE = 92;
	public int JJTSTRAFTER = 93;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTFLOOR = 84;
	public int JJTROUND = 85;
	public int JJTSUBSTR = 86;
	public int JJTSTRLEN = 87;
	public int JJTUPPERCASE = 88;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTLANG = 103;
	public int JJTLANGMATCHES = 104;
	public int JJTDATATYPE = 105;
	public int JJTBOUND = 106;
	public int JJTSAMETERM = 107;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTISIRI = 108;
	public int JJTISBLANK = 109;
	public int JJTISLITERAL = 110;
	public int JJTISNUMERIC = 111;
	public int JJTBNODEFUNC = 112;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTAVG = 63;
	public int JJTSAMPLE = 64;
	public int JJTGROUPCONCAT = 65;
	public int JJTMD5 = 66;
	public int JJTSHA1 = 67;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBLANKNODEPROPERTYLIST = 47;
	public int JJTCOLLECTION = 48;
	public int JJTVAR = 49;
	public int JJTTRIPLEREF = 50;
	public int JJTCONSTTRIPLEREF = 51;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTORDERCLAUSE = 21;
	public int JJTGROUPCONDITION = 22;
	public int JJTHAVINGCLAUSE = 23;
	public int JJTORDERCONDITION = 24;
	public int JJTLIMIT = 25;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTGRAPHPATTERNGROUP = 27;
	public int JJTBASICGRAPHPATTERN = 28;
	public int JJTOPTIONALGRAPHPATTERN = 29;
	public int JJTGRAPHGRAPHPATTERN = 30;
	public int JJTUNIONGRAPHPATTERN = 31;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBIND = 118;
	public int JJTREGEXEXPRESSION = 119;
	public int JJTEXISTSFUNC = 120;
	public int JJTNOTEXISTSFUNC = 121;
	public int JJTRDFLITERAL = 122;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMD5 = 66;
	public int JJTSHA1 = 67;
	public int JJTSHA224 = 68;
	public int JJTSHA256 = 69;
	public int JJTSHA384 = 70;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTRAND = 81;
	public int JJTABS = 82;
	public int JJTCEIL = 83;
	public int JJTFLOOR = 84;
	public int JJTROUND = 85;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTINLINEDATA = 17;
	public int JJTBINDINGSET = 18;
	public int JJTBINDINGVALUE = 19;
	public int JJTGROUPCLAUSE = 20;
	public int JJTORDERCLAUSE = 21;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTQUADSNOTTRIPLES = 131;
	public int JJTLOAD = 132;
	public int JJTCLEAR = 133;
	public int JJTDROP = 134;
	public int JJTADD = 135;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSUM = 60;
	public int JJTMIN = 61;
	public int JJTMAX = 62;
	public int JJTAVG = 63;
	public int JJTSAMPLE = 64;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBLANKNODE = 127;
	public int JJTGRAPHREFALL = 128;
	public int JJTGRAPHORDEFAULT = 129;
	public int JJTUNPARSEDQUADDATABLOCK = 130;
	public int JJTQUADSNOTTRIPLES = 131;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTGRAPHORDEFAULT = 129;
	public int JJTUNPARSEDQUADDATABLOCK = 130;
	public int JJTQUADSNOTTRIPLES = 131;
	public int JJTLOAD = 132;
	public int JJTCLEAR = 133;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTOBJECTLIST = 38;
	public int JJTTRIPLESSAMESUBJECTPATH = 39;
	public int JJTPROPERTYLISTPATH = 40;
	public int JJTPATHALTERNATIVE = 41;
	public int JJTPATHSEQUENCE = 42;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTGRAPHREFALL = 128;
	public int JJTGRAPHORDEFAULT = 129;
	public int JJTUNPARSEDQUADDATABLOCK = 130;
	public int JJTQUADSNOTTRIPLES = 131;
	public int JJTLOAD = 132;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTTRIPLEREF = 50;
	public int JJTCONSTTRIPLEREF = 51;
	public int JJTOR = 52;
	public int JJTAND = 53;
	public int JJTCOMPARE = 54;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMINUSGRAPHPATTERN = 32;
	public int JJTSERVICEGRAPHPATTERN = 33;
	public int JJTCONSTRAINT = 34;
	public int JJTFUNCTIONCALL = 35;
	public int JJTTRIPLESSAMESUBJECT = 36;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBASICGRAPHPATTERN = 28;
	public int JJTOPTIONALGRAPHPATTERN = 29;
	public int JJTGRAPHGRAPHPATTERN = 30;
	public int JJTUNIONGRAPHPATTERN = 31;
	public int JJTMINUSGRAPHPATTERN = 32;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDELETEDATA = 140;
	public int JJTDELETEWHERE = 141;
	public int JJTDELETECLAUSE = 142;
	public int JJTINSERTCLAUSE = 143;
	public int JJTMODIFY = 144;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCREATE = 138;
	public int JJTINSERTDATA = 139;
	public int JJTDELETEDATA = 140;
	public int JJTDELETEWHERE = 141;
	public int JJTDELETECLAUSE = 142;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTADD = 135;
	public int JJTMOVE = 136;
	public int JJTCOPY = 137;
	public int JJTCREATE = 138;
	public int JJTINSERTDATA = 139;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTNOTEXISTSFUNC = 121;
	public int JJTRDFLITERAL = 122;
	public int JJTTRUE = 123;
	public int JJTFALSE = 124;
	public int JJTSTRING = 125;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBNODEFUNC = 112;
	public int JJTIRIFUNC = 113;
	public int JJTSTRDT = 114;
	public int JJTSTRLANG = 115;
	public int JJTUUID = 116;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java

public interface SyntaxTreeBuilderTreeConstants {
	public int JJTUPDATESEQUENCE = 0;
	public int JJTUPDATECONTAINER = 1;
	public int JJTQUERYCONTAINER = 2;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTINSERTDATA = 139;
	public int JJTDELETEDATA = 140;
	public int JJTDELETEWHERE = 141;
	public int JJTDELETECLAUSE = 142;
	public int JJTINSERTCLAUSE = 143;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPATHMOD = 46;
	public int JJTBLANKNODEPROPERTYLIST = 47;
	public int JJTCOLLECTION = 48;
	public int JJTVAR = 49;
	public int JJTTRIPLEREF = 50;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTENCODEFORURI = 97;
	public int JJTIF = 98;
	public int JJTIN = 99;
	public int JJTNOTIN = 100;
	public int JJTCOALESCE = 101;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTHOURS = 76;
	public int JJTMINUTES = 77;
	public int JJTSECONDS = 78;
	public int JJTTIMEZONE = 79;
	public int JJTTZ = 80;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPROPERTYLISTPATH = 40;
	public int JJTPATHALTERNATIVE = 41;
	public int JJTPATHSEQUENCE = 42;
	public int JJTPATHELT = 43;
	public int JJTIRI = 44;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCONTAINS = 96;
	public int JJTENCODEFORURI = 97;
	public int JJTIF = 98;
	public int JJTIN = 99;
	public int JJTNOTIN = 100;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPREFIXDECL = 5;
	public int JJTSELECTQUERY = 6;
	public int JJTSELECT = 7;
	public int JJTPROJECTIONELEM = 8;
	public int JJTCONSTRUCTQUERY = 9;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTTZ = 80;
	public int JJTRAND = 81;
	public int JJTABS = 82;
	public int JJTCEIL = 83;
	public int JJTFLOOR = 84;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMATH = 56;
	public int JJTNOT = 57;
	public int JJTNUMERICLITERAL = 58;
	public int JJTCOUNT = 59;
	public int JJTSUM = 60;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTASKQUERY = 13;
	public int JJTDATASETCLAUSE = 14;
	public int JJTWHERECLAUSE = 15;
	public int JJTBINDINGSCLAUSE = 16;
	public int JJTINLINEDATA = 17;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTGROUPCLAUSE = 20;
	public int JJTORDERCLAUSE = 21;
	public int JJTGROUPCONDITION = 22;
	public int JJTHAVINGCLAUSE = 23;
	public int JJTORDERCONDITION = 24;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTLANGMATCHES = 104;
	public int JJTDATATYPE = 105;
	public int JJTBOUND = 106;
	public int JJTSAMETERM = 107;
	public int JJTISIRI = 108;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDESCRIBE = 12;
	public int JJTASKQUERY = 13;
	public int JJTDATASETCLAUSE = 14;
	public int JJTWHERECLAUSE = 15;
	public int JJTBINDINGSCLAUSE = 16;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTTRIPLESSAMESUBJECTPATH = 39;
	public int JJTPROPERTYLISTPATH = 40;
	public int JJTPATHALTERNATIVE = 41;
	public int JJTPATHSEQUENCE = 42;
	public int JJTPATHELT = 43;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBINDINGSET = 18;
	public int JJTBINDINGVALUE = 19;
	public int JJTGROUPCLAUSE = 20;
	public int JJTORDERCLAUSE = 21;
	public int JJTGROUPCONDITION = 22;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBASEDECL = 4;
	public int JJTPREFIXDECL = 5;
	public int JJTSELECTQUERY = 6;
	public int JJTSELECT = 7;
	public int JJTPROJECTIONELEM = 8;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTHAVINGCLAUSE = 23;
	public int JJTORDERCONDITION = 24;
	public int JJTLIMIT = 25;
	public int JJTOFFSET = 26;
	public int JJTGRAPHPATTERNGROUP = 27;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCONSTRUCTQUERY = 9;
	public int JJTCONSTRUCT = 10;
	public int JJTDESCRIBEQUERY = 11;
	public int JJTDESCRIBE = 12;
	public int JJTASKQUERY = 13;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTVOID = 3;
	public int JJTBASEDECL = 4;
	public int JJTPREFIXDECL = 5;
	public int JJTSELECTQUERY = 6;
	public int JJTSELECT = 7;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTNOT = 57;
	public int JJTNUMERICLITERAL = 58;
	public int JJTCOUNT = 59;
	public int JJTSUM = 60;
	public int JJTMIN = 61;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRLEN = 87;
	public int JJTUPPERCASE = 88;
	public int JJTLOWERCASE = 89;
	public int JJTSTRSTARTS = 90;
	public int JJTSTRENDS = 91;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTOPTIONALGRAPHPATTERN = 29;
	public int JJTGRAPHGRAPHPATTERN = 30;
	public int JJTUNIONGRAPHPATTERN = 31;
	public int JJTMINUSGRAPHPATTERN = 32;
	public int JJTSERVICEGRAPHPATTERN = 33;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTISLITERAL = 110;
	public int JJTISNUMERIC = 111;
	public int JJTBNODEFUNC = 112;
	public int JJTIRIFUNC = 113;
	public int JJTSTRDT = 114;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTREPLACE = 94;
	public int JJTCONCAT = 95;
	public int JJTCONTAINS = 96;
	public int JJTENCODEFORURI = 97;
	public int JJTIF = 98;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTUPPERCASE = 88;
	public int JJTLOWERCASE = 89;
	public int JJTSTRSTARTS = 90;
	public int JJTSTRENDS = 91;
	public int JJTSTRBEFORE = 92;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTROUND = 85;
	public int JJTSUBSTR = 86;
	public int JJTSTRLEN = 87;
	public int JJTUPPERCASE = 88;
	public int JJTLOWERCASE = 89;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRSTARTS = 90;
	public int JJTSTRENDS = 91;
	public int JJTSTRBEFORE = 92;
	public int JJTSTRAFTER = 93;
	public int JJTREPLACE = 94;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTTRIPLESSAMESUBJECT = 36;
	public int JJTPROPERTYLIST = 37;
	public int JJTOBJECTLIST = 38;
	public int JJTTRIPLESSAMESUBJECTPATH = 39;
	public int JJTPROPERTYLISTPATH = 40;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTOFFSET = 26;
	public int JJTGRAPHPATTERNGROUP = 27;
	public int JJTBASICGRAPHPATTERN = 28;
	public int JJTOPTIONALGRAPHPATTERN = 29;
	public int JJTGRAPHGRAPHPATTERN = 30;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTEXISTSFUNC = 120;
	public int JJTNOTEXISTSFUNC = 121;
	public int JJTRDFLITERAL = 122;
	public int JJTTRUE = 123;
	public int JJTFALSE = 124;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTYEAR = 73;
	public int JJTMONTH = 74;
	public int JJTDAY = 75;
	public int JJTHOURS = 76;
	public int JJTMINUTES = 77;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSECONDS = 78;
	public int JJTTIMEZONE = 79;
	public int JJTTZ = 80;
	public int JJTRAND = 81;
	public int JJTABS = 82;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRBEFORE = 92;
	public int JJTSTRAFTER = 93;
	public int JJTREPLACE = 94;
	public int JJTCONCAT = 95;
	public int JJTCONTAINS = 96;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTABS = 82;
	public int JJTCEIL = 83;
	public int JJTFLOOR = 84;
	public int JJTROUND = 85;
	public int JJTSUBSTR = 86;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPATHALTERNATIVE = 41;
	public int JJTPATHSEQUENCE = 42;
	public int JJTPATHELT = 43;
	public int JJTIRI = 44;
	public int JJTPATHONEINPROPERTYSET = 45;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCOLLECTION = 48;
	public int JJTVAR = 49;
	public int JJTTRIPLEREF = 50;
	public int JJTCONSTTRIPLEREF = 51;
	public int JJTOR = 52;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRENDS = 91;
	public int JJTSTRBEFORE = 92;
	public int JJTSTRAFTER = 93;
	public int JJTREPLACE = 94;
	public int JJTCONCAT = 95;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTINFIX = 55;
	public int JJTMATH = 56;
	public int JJTNOT = 57;
	public int JJTNUMERICLITERAL = 58;
	public int JJTCOUNT = 59;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPATHELT = 43;
	public int JJTIRI = 44;
	public int JJTPATHONEINPROPERTYSET = 45;
	public int JJTPATHMOD = 46;
	public int JJTBLANKNODEPROPERTYLIST = 47;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPROJECTIONELEM = 8;
	public int JJTCONSTRUCTQUERY = 9;
	public int JJTCONSTRUCT = 10;
	public int JJTDESCRIBEQUERY = 11;
	public int JJTDESCRIBE = 12;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSELECTQUERY = 6;
	public int JJTSELECT = 7;
	public int JJTPROJECTIONELEM = 8;
	public int JJTCONSTRUCTQUERY = 9;
	public int JJTCONSTRUCT = 10;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSHA1 = 67;
	public int JJTSHA224 = 68;
	public int JJTSHA256 = 69;
	public int JJTSHA384 = 70;
	public int JJTSHA512 = 71;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTUNIONGRAPHPATTERN = 31;
	public int JJTMINUSGRAPHPATTERN = 32;
	public int JJTSERVICEGRAPHPATTERN = 33;
	public int JJTCONSTRAINT = 34;
	public int JJTFUNCTIONCALL = 35;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBINDINGVALUE = 19;
	public int JJTGROUPCLAUSE = 20;
	public int JJTORDERCLAUSE = 21;
	public int JJTGROUPCONDITION = 22;
	public int JJTHAVINGCLAUSE = 23;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSAMETERM = 107;
	public int JJTISIRI = 108;
	public int JJTISBLANK = 109;
	public int JJTISLITERAL = 110;
	public int JJTISNUMERIC = 111;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDROP = 134;
	public int JJTADD = 135;
	public int JJTMOVE = 136;
	public int JJTCOPY = 137;
	public int JJTCREATE = 138;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTIF = 98;
	public int JJTIN = 99;
	public int JJTNOTIN = 100;
	public int JJTCOALESCE = 101;
	public int JJTSTR = 102;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTNOW = 72;
	public int JJTYEAR = 73;
	public int JJTMONTH = 74;
	public int JJTDAY = 75;
	public int JJTHOURS = 76;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRAFTER = 93;
	public int JJTREPLACE = 94;
	public int JJTCONCAT = 95;
	public int JJTCONTAINS = 96;
	public int JJTENCODEFORURI = 97;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCONSTRUCT = 10;
	public int JJTDESCRIBEQUERY = 11;
	public int JJTDESCRIBE = 12;
	public int JJTASKQUERY = 13;
	public int JJTDATASETCLAUSE = 14;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMINUTES = 77;
	public int JJTSECONDS = 78;
	public int JJTTIMEZONE = 79;
	public int JJTTZ = 80;
	public int JJTRAND = 81;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSHA224 = 68;
	public int JJTSHA256 = 69;
	public int JJTSHA384 = 70;
	public int JJTSHA512 = 71;
	public int JJTNOW = 72;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTNUMERICLITERAL = 58;
	public int JJTCOUNT = 59;
	public int JJTSUM = 60;
	public int JJTMIN = 61;
	public int JJTMAX = 62;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTAND = 53;
	public int JJTCOMPARE = 54;
	public int JJTINFIX = 55;
	public int JJTMATH = 56;
	public int JJTNOT = 57;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTBINDINGSCLAUSE = 16;
	public int JJTINLINEDATA = 17;
	public int JJTBINDINGSET = 18;
	public int JJTBINDINGVALUE = 19;
	public int JJTGROUPCLAUSE = 20;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTOR = 52;
	public int JJTAND = 53;
	public int JJTCOMPARE = 54;
	public int JJTINFIX = 55;
	public int JJTMATH = 56;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTIN = 99;
	public int JJTNOTIN = 100;
	public int JJTCOALESCE = 101;
	public int JJTSTR = 102;
	public int JJTLANG = 103;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSTRING = 125;
	public int JJTQNAME = 126;
	public int JJTBLANKNODE = 127;
	public int JJTGRAPHREFALL = 128;
	public int JJTGRAPHORDEFAULT = 129;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTUUID = 116;
	public int JJTSTRUUID = 117;
	public int JJTBIND = 118;
	public int JJTREGEXEXPRESSION = 119;
	public int JJTEXISTSFUNC = 120;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTNOTIN = 100;
	public int JJTCOALESCE = 101;
	public int JJTSTR = 102;
	public int JJTLANG = 103;
	public int JJTLANGMATCHES = 104;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDELETEWHERE = 141;
	public int JJTDELETECLAUSE = 142;
	public int JJTINSERTCLAUSE = 143;
	public int JJTMODIFY = 144;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTTRUE = 123;
	public int JJTFALSE = 124;
	public int JJTSTRING = 125;
	public int JJTQNAME = 126;
	public int JJTBLANKNODE = 127;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTDELETECLAUSE = 142;
	public int JJTINSERTCLAUSE = 143;
	public int JJTMODIFY = 144;

	public String[] jjtNodeName = {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSUBSTR = 86;
	public int JJTSTRLEN = 87;
	public int JJTUPPERCASE = 88;
	public int JJTLOWERCASE = 89;
	public int JJTSTRSTARTS = 90;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTGRAPHGRAPHPATTERN = 30;
	public int JJTUNIONGRAPHPATTERN = 31;
	public int JJTMINUSGRAPHPATTERN = 32;
	public int JJTSERVICEGRAPHPATTERN = 33;
	public int JJTCONSTRAINT = 34;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCOALESCE = 101;
	public int JJTSTR = 102;
	public int JJTLANG = 103;
	public int JJTLANGMATCHES = 104;
	public int JJTDATATYPE = 105;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMIN = 61;
	public int JJTMAX = 62;
	public int JJTAVG = 63;
	public int JJTSAMPLE = 64;
	public int JJTGROUPCONCAT = 65;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTSHA384 = 70;
	public int JJTSHA512 = 71;
	public int JJTNOW = 72;
	public int JJTYEAR = 73;
	public int JJTMONTH = 74;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTQUERYCONTAINER = 2;
	public int JJTVOID = 3;
	public int JJTBASEDECL = 4;
	public int JJTPREFIXDECL = 5;
	public int JJTSELECTQUERY = 6;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCOUNT = 59;
	public int JJTSUM = 60;
	public int JJTMIN = 61;
	public int JJTMAX = 62;
	public int JJTAVG = 63;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTLOAD = 132;
	public int JJTCLEAR = 133;
	public int JJTDROP = 134;
	public int JJTADD = 135;
	public int JJTMOVE = 136;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTCOMPARE = 54;
	public int JJTINFIX = 55;
	public int JJTMATH = 56;
	public int JJTNOT = 57;
	public int JJTNUMERICLITERAL = 58;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTMOVE = 136;
	public int JJTCOPY = 137;
	public int JJTCREATE = 138;
	public int JJTINSERTDATA = 139;
	public int JJTDELETEDATA = 140;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTreeConstants.java`
#### Snippet
```java
	public int JJTPROPERTYLIST = 37;
	public int JJTOBJECTLIST = 38;
	public int JJTTRIPLESSAMESUBJECTPATH = 39;
	public int JJTPROPERTYLISTPATH = 40;
	public int JJTPATHALTERNATIVE = 41;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTAskQuery node, Object data) throws VisitorException;

	public Object visit(ASTDatasetClause node, Object data) throws VisitorException;

	public Object visit(ASTWhereClause node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTOptionalGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTGraphGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTUnionGraphPattern node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTReplace node, Object data) throws VisitorException;

	public Object visit(ASTConcat node, Object data) throws VisitorException;

	public Object visit(ASTContains node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTTz node, Object data) throws VisitorException;

	public Object visit(ASTRand node, Object data) throws VisitorException;

	public Object visit(ASTAbs node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSample node, Object data) throws VisitorException;

	public Object visit(ASTGroupConcat node, Object data) throws VisitorException;

	public Object visit(ASTMD5 node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTInfix node, Object data) throws VisitorException;

	public Object visit(ASTMath node, Object data) throws VisitorException;

	public Object visit(ASTNot node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTCeil node, Object data) throws VisitorException;

	public Object visit(ASTFloor node, Object data) throws VisitorException;

	public Object visit(ASTRound node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTFalse node, Object data) throws VisitorException;

	public Object visit(ASTString node, Object data) throws VisitorException;

	public Object visit(ASTQName node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBlankNodePropertyList node, Object data) throws VisitorException;

	public Object visit(ASTCollection node, Object data) throws VisitorException;

	public Object visit(ASTVar node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTIf node, Object data) throws VisitorException;

	public Object visit(ASTIn node, Object data) throws VisitorException;

	public Object visit(ASTNotIn node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSHA512 node, Object data) throws VisitorException;

	public Object visit(ASTNow node, Object data) throws VisitorException;

	public Object visit(ASTYear node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTGraphPatternGroup node, Object data) throws VisitorException;

	public Object visit(ASTBasicGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTOptionalGraphPattern node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDrop node, Object data) throws VisitorException;

	public Object visit(ASTAdd node, Object data) throws VisitorException;

	public Object visit(ASTMove node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTObjectList node, Object data) throws VisitorException;

	public Object visit(ASTTriplesSameSubjectPath node, Object data) throws VisitorException;

	public Object visit(ASTPropertyListPath node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTLoad node, Object data) throws VisitorException;

	public Object visit(ASTClear node, Object data) throws VisitorException;

	public Object visit(ASTDrop node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBindingValue node, Object data) throws VisitorException;

	public Object visit(ASTGroupClause node, Object data) throws VisitorException;

	public Object visit(ASTOrderClause node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTIsNumeric node, Object data) throws VisitorException;

	public Object visit(ASTBNodeFunc node, Object data) throws VisitorException;

	public Object visit(ASTIRIFunc node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTQueryContainer node, Object data) throws VisitorException;

	public Object visit(ASTBaseDecl node, Object data) throws VisitorException;

	public Object visit(ASTPrefixDecl node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTQName node, Object data) throws VisitorException;

	public Object visit(ASTBlankNode node, Object data) throws VisitorException;

	public Object visit(ASTGraphRefAll node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTPathMod node, Object data) throws VisitorException;

	public Object visit(ASTBlankNodePropertyList node, Object data) throws VisitorException;

	public Object visit(ASTCollection node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTMin node, Object data) throws VisitorException;

	public Object visit(ASTMax node, Object data) throws VisitorException;

	public Object visit(ASTAvg node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTUpdateContainer node, Object data) throws VisitorException;

	public Object visit(ASTQueryContainer node, Object data) throws VisitorException;

	public Object visit(ASTBaseDecl node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTTriplesSameSubject node, Object data) throws VisitorException;

	public Object visit(ASTPropertyList node, Object data) throws VisitorException;

	public Object visit(ASTObjectList node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDescribe node, Object data) throws VisitorException;

	public Object visit(ASTAskQuery node, Object data) throws VisitorException;

	public Object visit(ASTDatasetClause node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTInsertClause node, Object data) throws VisitorException;

	public Object visit(ASTModify node, Object data) throws VisitorException;
}
/* JavaCC - OriginalChecksum=daffbeaaacea4dca824eb7969ebdb172 (do not edit this line) */
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTGraphGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTUnionGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTMinusGraphPattern node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTWhereClause node, Object data) throws VisitorException;

	public Object visit(ASTBindingsClause node, Object data) throws VisitorException;

	public Object visit(ASTInlineData node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTPrefixDecl node, Object data) throws VisitorException;

	public Object visit(ASTSelectQuery node, Object data) throws VisitorException;

	public Object visit(ASTSelect node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTStrLen node, Object data) throws VisitorException;

	public Object visit(ASTUpperCase node, Object data) throws VisitorException;

	public Object visit(ASTLowerCase node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTNow node, Object data) throws VisitorException;

	public Object visit(ASTYear node, Object data) throws VisitorException;

	public Object visit(ASTMonth node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTRound node, Object data) throws VisitorException;

	public Object visit(ASTSubstr node, Object data) throws VisitorException;

	public Object visit(ASTStrLen node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTTimezone node, Object data) throws VisitorException;

	public Object visit(ASTTz node, Object data) throws VisitorException;

	public Object visit(ASTRand node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTUUID node, Object data) throws VisitorException;

	public Object visit(ASTSTRUUID node, Object data) throws VisitorException;

	public Object visit(ASTBind node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTProjectionElem node, Object data) throws VisitorException;

	public Object visit(ASTConstructQuery node, Object data) throws VisitorException;

	public Object visit(ASTConstruct node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBNodeFunc node, Object data) throws VisitorException;

	public Object visit(ASTIRIFunc node, Object data) throws VisitorException;

	public Object visit(ASTStrDt node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSHA256 node, Object data) throws VisitorException;

	public Object visit(ASTSHA384 node, Object data) throws VisitorException;

	public Object visit(ASTSHA512 node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTString node, Object data) throws VisitorException;

	public Object visit(ASTQName node, Object data) throws VisitorException;

	public Object visit(ASTBlankNode node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTPathOneInPropertySet node, Object data) throws VisitorException;

	public Object visit(ASTPathMod node, Object data) throws VisitorException;

	public Object visit(ASTBlankNodePropertyList node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTMax node, Object data) throws VisitorException;

	public Object visit(ASTAvg node, Object data) throws VisitorException;

	public Object visit(ASTSample node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTHavingClause node, Object data) throws VisitorException;

	public Object visit(ASTOrderCondition node, Object data) throws VisitorException;

	public Object visit(ASTLimit node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTContains node, Object data) throws VisitorException;

	public Object visit(ASTEncodeForURI node, Object data) throws VisitorException;

	public Object visit(ASTIf node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTCoalesce node, Object data) throws VisitorException;

	public Object visit(ASTStr node, Object data) throws VisitorException;

	public Object visit(ASTLang node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTExistsFunc node, Object data) throws VisitorException;

	public Object visit(ASTNotExistsFunc node, Object data) throws VisitorException;

	public Object visit(ASTRDFLiteral node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTStrDt node, Object data) throws VisitorException;

	public Object visit(ASTStrLang node, Object data) throws VisitorException;

	public Object visit(ASTUUID node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTOrderClause node, Object data) throws VisitorException;

	public Object visit(ASTGroupCondition node, Object data) throws VisitorException;

	public Object visit(ASTHavingClause node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(SimpleNode node, Object data) throws VisitorException;

	public Object visit(ASTUpdateSequence node, Object data) throws VisitorException;

	public Object visit(ASTUpdateContainer node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTRDFLiteral node, Object data) throws VisitorException;

	public Object visit(ASTTrue node, Object data) throws VisitorException;

	public Object visit(ASTFalse node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTMinutes node, Object data) throws VisitorException;

	public Object visit(ASTSeconds node, Object data) throws VisitorException;

	public Object visit(ASTTimezone node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTNumericLiteral node, Object data) throws VisitorException;

	public Object visit(ASTCount node, Object data) throws VisitorException;

	public Object visit(ASTSum node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTLang node, Object data) throws VisitorException;

	public Object visit(ASTLangMatches node, Object data) throws VisitorException;

	public Object visit(ASTDatatype node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTPropertyList node, Object data) throws VisitorException;

	public Object visit(ASTObjectList node, Object data) throws VisitorException;

	public Object visit(ASTTriplesSameSubjectPath node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTConstraint node, Object data) throws VisitorException;

	public Object visit(ASTFunctionCall node, Object data) throws VisitorException;

	public Object visit(ASTTriplesSameSubject node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSHA224 node, Object data) throws VisitorException;

	public Object visit(ASTSHA256 node, Object data) throws VisitorException;

	public Object visit(ASTSHA384 node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTConcat node, Object data) throws VisitorException;

	public Object visit(ASTContains node, Object data) throws VisitorException;

	public Object visit(ASTEncodeForURI node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTOffset node, Object data) throws VisitorException;

	public Object visit(ASTGraphPatternGroup node, Object data) throws VisitorException;

	public Object visit(ASTBasicGraphPattern node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java

public interface SyntaxTreeBuilderVisitor {
	public Object visit(SimpleNode node, Object data) throws VisitorException;

	public Object visit(ASTUpdateSequence node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBlankNode node, Object data) throws VisitorException;

	public Object visit(ASTGraphRefAll node, Object data) throws VisitorException;

	public Object visit(ASTGraphOrDefault node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTMath node, Object data) throws VisitorException;

	public Object visit(ASTNot node, Object data) throws VisitorException;

	public Object visit(ASTNumericLiteral node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTMD5 node, Object data) throws VisitorException;

	public Object visit(ASTSHA1 node, Object data) throws VisitorException;

	public Object visit(ASTSHA224 node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTAbs node, Object data) throws VisitorException;

	public Object visit(ASTCeil node, Object data) throws VisitorException;

	public Object visit(ASTFloor node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTCompare node, Object data) throws VisitorException;

	public Object visit(ASTInfix node, Object data) throws VisitorException;

	public Object visit(ASTMath node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTNot node, Object data) throws VisitorException;

	public Object visit(ASTNumericLiteral node, Object data) throws VisitorException;

	public Object visit(ASTCount node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTQuadsNotTriples node, Object data) throws VisitorException;

	public Object visit(ASTLoad node, Object data) throws VisitorException;

	public Object visit(ASTClear node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTRand node, Object data) throws VisitorException;

	public Object visit(ASTAbs node, Object data) throws VisitorException;

	public Object visit(ASTCeil node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTAnd node, Object data) throws VisitorException;

	public Object visit(ASTCompare node, Object data) throws VisitorException;

	public Object visit(ASTInfix node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTMove node, Object data) throws VisitorException;

	public Object visit(ASTCopy node, Object data) throws VisitorException;

	public Object visit(ASTCreate node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTPathAlternative node, Object data) throws VisitorException;

	public Object visit(ASTPathSequence node, Object data) throws VisitorException;

	public Object visit(ASTPathElt node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTCount node, Object data) throws VisitorException;

	public Object visit(ASTSum node, Object data) throws VisitorException;

	public Object visit(ASTMin node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTMinusGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTServiceGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTConstraint node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTStrBefore node, Object data) throws VisitorException;

	public Object visit(ASTStrAfter node, Object data) throws VisitorException;

	public Object visit(ASTReplace node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTYear node, Object data) throws VisitorException;

	public Object visit(ASTMonth node, Object data) throws VisitorException;

	public Object visit(ASTDay node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTConstruct node, Object data) throws VisitorException;

	public Object visit(ASTDescribeQuery node, Object data) throws VisitorException;

	public Object visit(ASTDescribe node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTGroupConcat node, Object data) throws VisitorException;

	public Object visit(ASTMD5 node, Object data) throws VisitorException;

	public Object visit(ASTSHA1 node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTOr node, Object data) throws VisitorException;

	public Object visit(ASTAnd node, Object data) throws VisitorException;

	public Object visit(ASTCompare node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTNotExistsFunc node, Object data) throws VisitorException;

	public Object visit(ASTRDFLiteral node, Object data) throws VisitorException;

	public Object visit(ASTTrue node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDeleteClause node, Object data) throws VisitorException;

	public Object visit(ASTInsertClause node, Object data) throws VisitorException;

	public Object visit(ASTModify node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTStrAfter node, Object data) throws VisitorException;

	public Object visit(ASTReplace node, Object data) throws VisitorException;

	public Object visit(ASTConcat node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTConstTripleRef node, Object data) throws VisitorException;

	public Object visit(ASTOr node, Object data) throws VisitorException;

	public Object visit(ASTAnd node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTRegexExpression node, Object data) throws VisitorException;

	public Object visit(ASTExistsFunc node, Object data) throws VisitorException;

	public Object visit(ASTNotExistsFunc node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTServiceGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTConstraint node, Object data) throws VisitorException;

	public Object visit(ASTFunctionCall node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTIsLiteral node, Object data) throws VisitorException;

	public Object visit(ASTIsNumeric node, Object data) throws VisitorException;

	public Object visit(ASTBNodeFunc node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTFunctionCall node, Object data) throws VisitorException;

	public Object visit(ASTTriplesSameSubject node, Object data) throws VisitorException;

	public Object visit(ASTPropertyList node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTLimit node, Object data) throws VisitorException;

	public Object visit(ASTOffset node, Object data) throws VisitorException;

	public Object visit(ASTGraphPatternGroup node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSum node, Object data) throws VisitorException;

	public Object visit(ASTMin node, Object data) throws VisitorException;

	public Object visit(ASTMax node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTIRIFunc node, Object data) throws VisitorException;

	public Object visit(ASTStrDt node, Object data) throws VisitorException;

	public Object visit(ASTStrLang node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTClear node, Object data) throws VisitorException;

	public Object visit(ASTDrop node, Object data) throws VisitorException;

	public Object visit(ASTAdd node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBound node, Object data) throws VisitorException;

	public Object visit(ASTSameTerm node, Object data) throws VisitorException;

	public Object visit(ASTIsIRI node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTPathElt node, Object data) throws VisitorException;

	public Object visit(ASTIRI node, Object data) throws VisitorException;

	public Object visit(ASTPathOneInPropertySet node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTPropertyListPath node, Object data) throws VisitorException;

	public Object visit(ASTPathAlternative node, Object data) throws VisitorException;

	public Object visit(ASTPathSequence node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTInsertData node, Object data) throws VisitorException;

	public Object visit(ASTDeleteData node, Object data) throws VisitorException;

	public Object visit(ASTDeleteWhere node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBasicGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTOptionalGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTGraphGraphPattern node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTIn node, Object data) throws VisitorException;

	public Object visit(ASTNotIn node, Object data) throws VisitorException;

	public Object visit(ASTCoalesce node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTCollection node, Object data) throws VisitorException;

	public Object visit(ASTVar node, Object data) throws VisitorException;

	public Object visit(ASTTripleRef node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTInlineData node, Object data) throws VisitorException;

	public Object visit(ASTBindingSet node, Object data) throws VisitorException;

	public Object visit(ASTBindingValue node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTEncodeForURI node, Object data) throws VisitorException;

	public Object visit(ASTIf node, Object data) throws VisitorException;

	public Object visit(ASTIn node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTVar node, Object data) throws VisitorException;

	public Object visit(ASTTripleRef node, Object data) throws VisitorException;

	public Object visit(ASTConstTripleRef node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBindingsClause node, Object data) throws VisitorException;

	public Object visit(ASTInlineData node, Object data) throws VisitorException;

	public Object visit(ASTBindingSet node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTTrue node, Object data) throws VisitorException;

	public Object visit(ASTFalse node, Object data) throws VisitorException;

	public Object visit(ASTString node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTLowerCase node, Object data) throws VisitorException;

	public Object visit(ASTStrStarts node, Object data) throws VisitorException;

	public Object visit(ASTStrEnds node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTAvg node, Object data) throws VisitorException;

	public Object visit(ASTSample node, Object data) throws VisitorException;

	public Object visit(ASTGroupConcat node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTOrderCondition node, Object data) throws VisitorException;

	public Object visit(ASTLimit node, Object data) throws VisitorException;

	public Object visit(ASTOffset node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTUpdateSequence node, Object data) throws VisitorException;

	public Object visit(ASTUpdateContainer node, Object data) throws VisitorException;

	public Object visit(ASTQueryContainer node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBindingSet node, Object data) throws VisitorException;

	public Object visit(ASTBindingValue node, Object data) throws VisitorException;

	public Object visit(ASTGroupClause node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDay node, Object data) throws VisitorException;

	public Object visit(ASTHours node, Object data) throws VisitorException;

	public Object visit(ASTMinutes node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDeleteWhere node, Object data) throws VisitorException;

	public Object visit(ASTDeleteClause node, Object data) throws VisitorException;

	public Object visit(ASTInsertClause node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTHours node, Object data) throws VisitorException;

	public Object visit(ASTMinutes node, Object data) throws VisitorException;

	public Object visit(ASTSeconds node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTTripleRef node, Object data) throws VisitorException;

	public Object visit(ASTConstTripleRef node, Object data) throws VisitorException;

	public Object visit(ASTOr node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSelectQuery node, Object data) throws VisitorException;

	public Object visit(ASTSelect node, Object data) throws VisitorException;

	public Object visit(ASTProjectionElem node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTStr node, Object data) throws VisitorException;

	public Object visit(ASTLang node, Object data) throws VisitorException;

	public Object visit(ASTLangMatches node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSHA384 node, Object data) throws VisitorException;

	public Object visit(ASTSHA512 node, Object data) throws VisitorException;

	public Object visit(ASTNow node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSHA1 node, Object data) throws VisitorException;

	public Object visit(ASTSHA224 node, Object data) throws VisitorException;

	public Object visit(ASTSHA256 node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTCopy node, Object data) throws VisitorException;

	public Object visit(ASTCreate node, Object data) throws VisitorException;

	public Object visit(ASTInsertData node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTIsIRI node, Object data) throws VisitorException;

	public Object visit(ASTIsBlank node, Object data) throws VisitorException;

	public Object visit(ASTIsLiteral node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSubstr node, Object data) throws VisitorException;

	public Object visit(ASTStrLen node, Object data) throws VisitorException;

	public Object visit(ASTUpperCase node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTIsBlank node, Object data) throws VisitorException;

	public Object visit(ASTIsLiteral node, Object data) throws VisitorException;

	public Object visit(ASTIsNumeric node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDeleteData node, Object data) throws VisitorException;

	public Object visit(ASTDeleteWhere node, Object data) throws VisitorException;

	public Object visit(ASTDeleteClause node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDatatype node, Object data) throws VisitorException;

	public Object visit(ASTBound node, Object data) throws VisitorException;

	public Object visit(ASTSameTerm node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTPathSequence node, Object data) throws VisitorException;

	public Object visit(ASTPathElt node, Object data) throws VisitorException;

	public Object visit(ASTIRI node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTGraphRefAll node, Object data) throws VisitorException;

	public Object visit(ASTGraphOrDefault node, Object data) throws VisitorException;

	public Object visit(ASTUnparsedQuadDataBlock node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBind node, Object data) throws VisitorException;

	public Object visit(ASTRegexExpression node, Object data) throws VisitorException;

	public Object visit(ASTExistsFunc node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTCreate node, Object data) throws VisitorException;

	public Object visit(ASTInsertData node, Object data) throws VisitorException;

	public Object visit(ASTDeleteData node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTUnionGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTMinusGraphPattern node, Object data) throws VisitorException;

	public Object visit(ASTServiceGraphPattern node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTStrEnds node, Object data) throws VisitorException;

	public Object visit(ASTStrBefore node, Object data) throws VisitorException;

	public Object visit(ASTStrAfter node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTTriplesSameSubjectPath node, Object data) throws VisitorException;

	public Object visit(ASTPropertyListPath node, Object data) throws VisitorException;

	public Object visit(ASTPathAlternative node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTBaseDecl node, Object data) throws VisitorException;

	public Object visit(ASTPrefixDecl node, Object data) throws VisitorException;

	public Object visit(ASTSelectQuery node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTAdd node, Object data) throws VisitorException;

	public Object visit(ASTMove node, Object data) throws VisitorException;

	public Object visit(ASTCopy node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTNotIn node, Object data) throws VisitorException;

	public Object visit(ASTCoalesce node, Object data) throws VisitorException;

	public Object visit(ASTStr node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTGroupCondition node, Object data) throws VisitorException;

	public Object visit(ASTHavingClause node, Object data) throws VisitorException;

	public Object visit(ASTOrderCondition node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTConstructQuery node, Object data) throws VisitorException;

	public Object visit(ASTConstruct node, Object data) throws VisitorException;

	public Object visit(ASTDescribeQuery node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTUnparsedQuadDataBlock node, Object data) throws VisitorException;

	public Object visit(ASTQuadsNotTriples node, Object data) throws VisitorException;

	public Object visit(ASTLoad node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDescribeQuery node, Object data) throws VisitorException;

	public Object visit(ASTDescribe node, Object data) throws VisitorException;

	public Object visit(ASTAskQuery node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSelect node, Object data) throws VisitorException;

	public Object visit(ASTProjectionElem node, Object data) throws VisitorException;

	public Object visit(ASTConstructQuery node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTMonth node, Object data) throws VisitorException;

	public Object visit(ASTDay node, Object data) throws VisitorException;

	public Object visit(ASTHours node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTIRI node, Object data) throws VisitorException;

	public Object visit(ASTPathOneInPropertySet node, Object data) throws VisitorException;

	public Object visit(ASTPathMod node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSTRUUID node, Object data) throws VisitorException;

	public Object visit(ASTBind node, Object data) throws VisitorException;

	public Object visit(ASTRegexExpression node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSeconds node, Object data) throws VisitorException;

	public Object visit(ASTTimezone node, Object data) throws VisitorException;

	public Object visit(ASTTz node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTStrLang node, Object data) throws VisitorException;

	public Object visit(ASTUUID node, Object data) throws VisitorException;

	public Object visit(ASTSTRUUID node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTSameTerm node, Object data) throws VisitorException;

	public Object visit(ASTIsIRI node, Object data) throws VisitorException;

	public Object visit(ASTIsBlank node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTGroupClause node, Object data) throws VisitorException;

	public Object visit(ASTOrderClause node, Object data) throws VisitorException;

	public Object visit(ASTGroupCondition node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTGraphOrDefault node, Object data) throws VisitorException;

	public Object visit(ASTUnparsedQuadDataBlock node, Object data) throws VisitorException;

	public Object visit(ASTQuadsNotTriples node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTLangMatches node, Object data) throws VisitorException;

	public Object visit(ASTDatatype node, Object data) throws VisitorException;

	public Object visit(ASTBound node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTUpperCase node, Object data) throws VisitorException;

	public Object visit(ASTLowerCase node, Object data) throws VisitorException;

	public Object visit(ASTStrStarts node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTDatasetClause node, Object data) throws VisitorException;

	public Object visit(ASTWhereClause node, Object data) throws VisitorException;

	public Object visit(ASTBindingsClause node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTFloor node, Object data) throws VisitorException;

	public Object visit(ASTRound node, Object data) throws VisitorException;

	public Object visit(ASTSubstr node, Object data) throws VisitorException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderVisitor.java`
#### Snippet
```java
	public Object visit(ASTStrStarts node, Object data) throws VisitorException;

	public Object visit(ASTStrEnds node, Object data) throws VisitorException;

	public Object visit(ASTStrBefore node, Object data) throws VisitorException;
```

## RuleId[id=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `defaultIgnoredTests` are read, but never written to
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL12QueryComplianceTest.java`
#### Snippet
```java
public abstract class SPARQL12QueryComplianceTest extends SPARQLQueryComplianceTest {

	private static final String[] defaultIgnoredTests = {};

	private static final List<String> excludedSubdirs = List.of();
```

## RuleId[id=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java

	// TODO: make configurable, buffer for reading object values
	private final static int BUFLEN = 1 * 1024 * 1024;
	// for debugging purposes
	protected final String name;
```

### PointlessArithmeticExpression
`i + 0` can be replaced with 'i'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/IDFile.java`
#### Snippet
```java
		cacheLine = new Long[cacheLineSize];
		for (int i = 0; i < bytes.length; i += ITEM_SIZE) {
			long l = ((long) (bytes[i + 0] & 0xff) << 56)
					| ((long) bytes[i + 1] & 0xff) << 48
					| ((long) bytes[i + 2] & 0xff) << 40
```

## RuleId[id=MagicConstant]
### MagicConstant
Should be one of: Calendar.JANUARY, Calendar.FEBRUARY, Calendar.MARCH, Calendar.APRIL, Calendar.MAY, ...
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDateTime.java`
#### Snippet
```java
			// Insert values into a GregorianCalendar object.
			// Note: GregorianCalendar uses 0-based months
			Calendar cal = new GregorianCalendar(iYear, iMonths - 1, iDays, iHours, iMinutes, iSeconds);
			if (isNegativeYear) {
				cal.set(Calendar.ERA, GregorianCalendar.BC);
```

### MagicConstant
Should be one of: Pattern.UNIX_LINES, Pattern.CASE_INSENSITIVE, Pattern.COMMENTS, Pattern.MULTILINE, ... or their combination
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/RegexValueEvaluationStepSupplier.java`
#### Snippet
```java
			String ptn = ((Literal) parg).getLabel();
			int f = extractRegexFlags(farg);
			Pattern pattern = Pattern.compile(ptn, f);

			return bindings -> {
```

### MagicConstant
Should be one of: Pattern.UNIX_LINES, Pattern.CASE_INSENSITIVE, Pattern.COMMENTS, Pattern.MULTILINE, ... or their combination
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/RegexValueEvaluationStepSupplier.java`
#### Snippet
```java
			String ptn = ((Literal) parg).getLabel();
			int f = extractRegexFlags(farg);
			Pattern pattern = Pattern.compile(ptn, f);
			boolean result = pattern.matcher(text).find();
			BooleanLiteral valueOf = BooleanLiteral.valueOf(result);
```

### MagicConstant
Should be one of: Pattern.UNIX_LINES, Pattern.CASE_INSENSITIVE, Pattern.COMMENTS, Pattern.MULTILINE, ... or their combination
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/RegexValueEvaluationStepSupplier.java`
#### Snippet
```java
				// TODO should this Pattern be cached?
				int f = extractRegexFlags(farg);
				Pattern pattern = Pattern.compile(ptn, f);
				boolean result = pattern.matcher(text).find();
				return BooleanLiteral.valueOf(result);
```

## RuleId[id=MarkedForRemoval]
### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/TripleSourceIterationWrapper.java`
#### Snippet
```java
		}
		try {
			return delegate.next();
		} catch (NoSuchElementException e) {
			close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/TripleSourceIterationWrapper.java`
#### Snippet
```java
		}
		try {
			boolean result = delegate.hasNext();
			if (!result) {
				close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/TripleSourceIterationWrapper.java`
#### Snippet
```java
		}
		try {
			delegate.remove();
		} catch (UnsupportedOperationException | IllegalStateException | QueryEvaluationException e) {
			throw e;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailClosingIteration.java`
#### Snippet
```java

	/**
	 * Creates a new {@link Iteration} that automatically closes the given {@link SailClosable}s.
	 *
	 * @param iter   The underlying Iteration, must not be <var>null</var>.
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailClosingIteration.java`
#### Snippet
```java

	/**
	 * Creates a new {@link Iteration} that automatically closes the given {@link SailClosable}s.
	 *
	 * @param iter   The underlying Iteration, must not be <var>null</var>.
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailClosingIteration.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.IterationWrapper;
import org.eclipse.rdf4j.sail.SailException;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailClosingIteration.java`
#### Snippet
```java

/**
 * An {@link Iteration} that holds on to a {@link SailClosable} until the Iteration is closed. Upon closing, the
 * underlying Iteration is closed before the lock is released. This iterator closes itself as soon as all elements have
 * been read.
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/DistinctModelReducingUnionIteration.java`
#### Snippet
```java

		// first run through the statements from the base store
		if (iterator.hasNext()) {
			next = iterator.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/DistinctModelReducingUnionIteration.java`
#### Snippet
```java
		// first run through the statements from the base store
		if (iterator.hasNext()) {
			next = iterator.next();

			// remove the statement from the approved model in the Changeset, in case the approved model has a duplicate
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
				}
				Namespace toCheckNext = next;
				while (toCheckNext == null && namespaces.hasNext()) {
					toCheckNext = next = namespaces.next();
					if (removedSet != null && removedSet.contains(toCheckNext.getPrefix())) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
				Namespace toCheckNext = next;
				while (toCheckNext == null && namespaces.hasNext()) {
					toCheckNext = next = namespaces.next();
					if (removedSet != null && removedSet.contains(toCheckNext.getPrefix())) {
						toCheckNext = next = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
				}
				Resource toCheckNext = next;
				while (toCheckNext == null && contextIDs.hasNext()) {
					toCheckNext = next = contextIDs.next();
					if (removedSet != null && removedSet.contains(toCheckNext)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
				Resource toCheckNext = next;
				while (toCheckNext == null && contextIDs.hasNext()) {
					toCheckNext = next = contextIDs.next();
					if (removedSet != null && removedSet.contains(toCheckNext)) {
						toCheckNext = next = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> subjectStatements = derivedFrom
				.getStatements(triple, null, null)) {
			while (subjectStatements.hasNext()) {
				Statement st = subjectStatements.next();
				if (!deprecatedStatements.contains(st)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
				.getStatements(triple, null, null)) {
			while (subjectStatements.hasNext()) {
				Statement st = subjectStatements.next();
				if (!deprecatedStatements.contains(st)) {
					return false;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> objectStatements = derivedFrom
				.getStatements(null, null, triple)) {
			while (objectStatements.hasNext()) {
				Statement st = objectStatements.next();
				if (!deprecatedStatements.contains(st)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
				.getStatements(null, null, triple)) {
			while (objectStatements.hasNext()) {
				Statement st = objectStatements.next();
				if (!deprecatedStatements.contains(st)) {
					return false;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLCrossProductIteration.java`
#### Snippet
```java
		if (currentInputBinding == null) {
			inputBindingsIterator = inputBindings.iterator();
			if (resultIteration.hasNext()) {
				currentInputBinding = resultIteration.next();
			} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLCrossProductIteration.java`
#### Snippet
```java
			inputBindingsIterator = inputBindings.iterator();
			if (resultIteration.hasNext()) {
				currentInputBinding = resultIteration.next();
			} else {
				return null; // no more results
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
			}
			if (nextRightIter != null) {
				if (nextRightIter.hasNext()) {
					return nextRightIter.next();
				} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
			if (nextRightIter != null) {
				if (nextRightIter.hasNext()) {
					return nextRightIter.next();
				} else {
					rightIter = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ConnectionHelper.java`
#### Snippet
```java
				.getStatements(null, null, null, false)) {

			while (statements.hasNext()) {
				Statement next = statements.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ConnectionHelper.java`
#### Snippet
```java

			while (statements.hasNext()) {
				Statement next = statements.next();

				transfer.transfer(next.getSubject(), next.getPredicate(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
import org.eclipse.rdf4j.common.iteration.EmptyIteration;
import org.eclipse.rdf4j.common.iteration.ExceptionConvertingIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.SingletonIteration;
import org.eclipse.rdf4j.http.client.HttpClientDependent;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	protected Iteration<Statement, QueryEvaluationException> toStatementIteration(TupleQueryResult iter,
			final Resource subj, final IRI pred, final Value obj) {

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> iter = dataset.getStatements(subj, pred, obj,
				contexts)) {
			return iter.hasNext();
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
			try (CloseableIteration<? extends BindingSet, QueryEvaluationException> evaluate = evaluate(tupleExpr,
					dataset, bindings, includeInferred)) {
				while (evaluate.hasNext()) {
					if (Thread.interrupted()) {
						break;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
						break;
					}
					evaluate.next();
				}
			} catch (Exception e) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> iter = dataset.getStatements(subj, pred, obj,
				contexts)) {
			while (iter.hasNext()) {
				Statement st = iter.next();
				sink.deprecate(st);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
				contexts)) {
			while (iter.hasNext()) {
				Statement st = iter.next();
				sink.deprecate(st);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
		// note that this may be blocking depending on the underlying iterator
		List<BindingSet> allBindings = new LinkedList<>();
		while (bindings.hasNext()) {
			allBindings.add(bindings.next());
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
		List<BindingSet> allBindings = new LinkedList<>();
		while (bindings.hasNext()) {
			allBindings.add(bindings.next());
		}

```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
				closeQuietly(conn);
			}
			Iterations.closeCloseable(result);
			if (service.isSilent()) {
				return new CollectionIteration<>(allBindings);
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
				closeQuietly(conn);
			}
			Iterations.closeCloseable(result);
			if (service.isSilent()) {
				return new CollectionIteration<>(allBindings);
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
				closeQuietly(conn);
			}
			Iterations.closeCloseable(result);
			// suppress special exceptions (e.g. UndeclaredThrowable with wrapped
			// QueryEval) if silent
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
		@Override
		protected void handleBindings() throws Exception {
			while (!isClosed() && leftIter.hasNext()) {

				ArrayList<BindingSet> blockBindings = new ArrayList<>(blockSize);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
				ArrayList<BindingSet> blockBindings = new ArrayList<>(blockSize);
				for (int i = 0; i < blockSize; i++) {
					if (!leftIter.hasNext()) {
						break;
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
						break;
					}
					blockBindings.add(leftIter.next());
				}
				CloseableIteration<BindingSet, QueryEvaluationException> materializedIter = new CollectionIteration<>(
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
		@Override
		public boolean hasNext() throws QueryEvaluationException {
			return delegate.hasNext();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
		@Override
		public void remove() throws QueryEvaluationException {
			delegate.remove();

		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
		@Override
		public BindingSet next() throws QueryEvaluationException {
			return delegate.next();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SingleCloseablePlanNode.java`
#### Snippet
```java
		@Override
		public void remove() throws SailException {
			parentIterator.remove();
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SingleCloseablePlanNode.java`
#### Snippet
```java
		@Override
		public ValidationTuple next() throws SailException {
			return parentIterator.next();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SingleCloseablePlanNode.java`
#### Snippet
```java
		@Override
		public boolean hasNext() throws SailException {
			return parentIterator.hasNext();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
				}

				while (results == null || !results.hasNext()) {
					try {
						if (results != null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
						}

						while (statements == null || !statements.hasNext()) {
							calculateNextStatementMatcher();
							if (statements == null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
				}

				if (results.hasNext()) {
					BindingSet nextBinding = results.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java

				if (results.hasNext()) {
					BindingSet nextBinding = results.next();

					if (nextBinding.size() == 1) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
				bulk.clear();

				for (int i = 0; i < BULK_SIZE && statements.hasNext(); i++) {
					Statement next = statements.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java

				for (int i = 0; i < BULK_SIZE && statements.hasNext(); i++) {
					Statement next = statements.next();

					Binding[] bindings = new Binding[varNames.size()];
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java

			public void calculateNextStatementMatcher() {
				if (statements != null && statements.hasNext()) {
					return;
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
							currentStatementMatcher.getObjectValue(), false, dataGraph);

				} while (!statements.hasNext());

				parsedQuery = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TargetChainPopper.java`
#### Snippet
```java
			public void calculateNext() {
				if (!iterator.hasNext()) {
					if (parentIterator.hasNext()) {
						List<ValidationTuple> validationTuples = parentIterator.next().pop();
						iterator = validationTuples.iterator();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TargetChainPopper.java`
#### Snippet
```java
				if (!iterator.hasNext()) {
					if (parentIterator.hasNext()) {
						List<ValidationTuple> validationTuples = parentIterator.next().pop();
						iterator = validationTuples.iterator();
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedSplitter.java`
#### Snippet
```java
			tuplesBuffer = new ArrayList<>();
			try (CloseableIteration<? extends ValidationTuple, SailException> iterator = parent.iterator()) {
				while (iterator.hasNext()) {
					ValidationTuple next = iterator.next();
					tuplesBuffer.add(next);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedSplitter.java`
#### Snippet
```java
			try (CloseableIteration<? extends ValidationTuple, SailException> iterator = parent.iterator()) {
				while (iterator.hasNext()) {
					ValidationTuple next = iterator.next();
					tuplesBuffer.add(next);
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnionNode.java`
#### Snippet
```java
					if (peekList[i] == null) {
						var iterator = iterators[i];
						if (iterator.hasNext()) {
							peekList[i] = iterator.next();
						}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnionNode.java`
#### Snippet
```java
						var iterator = iterators[i];
						if (iterator.hasNext()) {
							peekList[i] = iterator.next();
						}
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnionNode.java`
#### Snippet
```java
				@Override
				protected ValidationTuple loggingNext() throws SailException {
					return iterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnionNode.java`
#### Snippet
```java
				@Override
				protected boolean localHasNext() throws SailException {
					return iterator.hasNext();
				}
			};
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToPropertyShape.java`
#### Snippet
```java
			public void calculateNext() {
				if (!iterator.hasNext()) {
					if (parentIterator.hasNext()) {
						List<ValidationTuple> validationTuples = parentIterator.next().shiftToPropertyShapeScope();
						iterator = validationTuples.iterator();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToPropertyShape.java`
#### Snippet
```java
				if (!iterator.hasNext()) {
					if (parentIterator.hasNext()) {
						List<ValidationTuple> validationTuples = parentIterator.next().shiftToPropertyShapeScope();
						iterator = validationTuples.iterator();
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
	@Override
	public boolean incrementIterator() {
		if (iterator.hasNext()) {
			iterator.next();
			return true;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
	public boolean incrementIterator() {
		if (iterator.hasNext()) {
			iterator.next();
			return true;
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
				}

				while (parentIterator.hasNext() && next == null) {
					ValidationTuple temp = parentIterator.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java

				while (parentIterator.hasNext() && next == null) {
					ValidationTuple temp = parentIterator.next();

					if (checkTuple(temp)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationReportNode.java`
#### Snippet
```java
			@Override
			public ValidationTuple loggingNext() throws SailException {
				ValidationTuple next = iterator.next();
				return next.addValidationResult(validationResultFunction);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationReportNode.java`
#### Snippet
```java
			@Override
			public boolean localHasNext() throws SailException {
				return iterator.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
				}

				if (nextLeft == null && leftIterator.hasNext()) {
					nextLeft = leftIterator.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java

				if (nextLeft == null && leftIterator.hasNext()) {
					nextLeft = leftIterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
				}

				if (nextRight == null && rightIterator.hasNext()) {
					nextRight = rightIterator.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java

				if (nextRight == null && rightIterator.hasNext()) {
					nextRight = rightIterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java

							if (compareTo < 0) {
								if (leftIterator.hasNext()) {
									nextLeft = leftIterator.next();
								} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
							if (compareTo < 0) {
								if (leftIterator.hasNext()) {
									nextLeft = leftIterator.next();
								} else {
									nextLeft = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
								}
							} else {
								if (rightIterator.hasNext()) {
									nextRight = rightIterator.next();
								} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
							} else {
								if (rightIterator.hasNext()) {
									nextRight = rightIterator.next();
								} else {
									nextRight = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AllTargetsPlanNode.java`
#### Snippet
```java
			@Override
			protected boolean localHasNext() throws SailException {
				return iterator.hasNext();
			}
		};
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AllTargetsPlanNode.java`
#### Snippet
```java
			@Override
			protected ValidationTuple loggingNext() throws SailException {
				return iterator.next();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToNodeShape.java`
#### Snippet
```java
			public void calculateNext() {
				if (!iterator.hasNext()) {
					if (parentIterator.hasNext()) {
						List<ValidationTuple> validationTuples = parentIterator.next().shiftToNodeShape();
						iterator = validationTuples.iterator();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToNodeShape.java`
#### Snippet
```java
				if (!iterator.hasNext()) {
					if (parentIterator.hasNext()) {
						List<ValidationTuple> validationTuples = parentIterator.next().shiftToNodeShape();
						iterator = validationTuples.iterator();
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToNodeShape.java`
#### Snippet
```java
						iterator = validationTuples.iterator();
					}
					assert iterator.hasNext() || !parentIterator.hasNext();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SetFilterNode.java`
#### Snippet
```java

			private void calulateNext() {
				while (next == null && iterator.hasNext()) {
					ValidationTuple temp = iterator.next();
					boolean contains = targetNodeList.contains(temp.getActiveTarget());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SetFilterNode.java`
#### Snippet
```java
			private void calulateNext() {
				while (next == null && iterator.hasNext()) {
					ValidationTuple temp = iterator.next();
					boolean contains = targetNodeList.contains(temp.getActiveTarget());
					if (returnValid && contains) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TrimToTarget.java`
#### Snippet
```java
			protected ValidationTuple loggingNext() throws SailException {

				return parentIterator.next().trimToTarget();

			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TrimToTarget.java`
#### Snippet
```java
			@Override
			protected boolean localHasNext() throws SailException {
				return parentIterator.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
			@Override
			public ValidationTuple next() throws SailException {
				ValidationTuple next = iterator.next();
				if (debugPoint != null) {
					debugPoint.accept(next);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
			@Override
			public boolean hasNext() throws SailException {
				return iterator.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
			@Override
			public void remove() throws SailException {
				iterator.remove();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
			void calculateNext() {
				if (filterOnPredicates == null) {
					if (!parentIterator.hasNext()) {
						return;
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
				}

				while (next == null && parentIterator.hasNext()) {
					ValidationTuple temp = parentIterator.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java

				while (next == null && parentIterator.hasNext()) {
					ValidationTuple temp = parentIterator.next();

					Value subject = temp.getActiveTarget();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TupleMapper.java`
#### Snippet
```java
			@Override
			protected ValidationTuple loggingNext() throws SailException {
				return function.apply(parentIterator.next());
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TupleMapper.java`
#### Snippet
```java
			@Override
			protected boolean localHasNext() throws SailException {
				return parentIterator.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
				}

				if (nextLeft == null && leftIterator.hasNext()) {
					nextLeft = leftIterator.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java

				if (nextLeft == null && leftIterator.hasNext()) {
					nextLeft = leftIterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
				}

				if (nextRight == null && rightIterator.hasNext()) {
					nextRight = rightIterator.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java

				if (nextRight == null && rightIterator.hasNext()) {
					nextRight = rightIterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
									prevLeft = nextLeft;
									next = nextLeft;
								} else if (leftIterator.hasNext()) {
									nextLeft = leftIterator.next();
								} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
									next = nextLeft;
								} else if (leftIterator.hasNext()) {
									nextLeft = leftIterator.next();
								} else {
									nextLeft = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
								}
							} else {
								if (rightIterator.hasNext()) {
									nextRight = rightIterator.next();
								} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
							} else {
								if (rightIterator.hasNext()) {
									nextRight = rightIterator.next();
								} else {
									nextRight = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
					} else {
						if (prevLeft == nextLeft) {
							if (leftIterator.hasNext()) {
								nextLeft = leftIterator.next();
							} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
						if (prevLeft == nextLeft) {
							if (leftIterator.hasNext()) {
								nextLeft = leftIterator.next();
							} else {
								break;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
			protected ValidationTuple loggingNext() throws SailException {
				init();
				return mapper.apply(bindingSet.next());
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
			protected boolean localHasNext() throws SailException {
				init();
				return bindingSet.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
						alreadySorted = true;
						ValidationTuple prev = null;
						while (iterator.hasNext()) {
							ValidationTuple next = iterator.next();
							sortedTuples.add(next);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
						ValidationTuple prev = null;
						while (iterator.hasNext()) {
							ValidationTuple next = iterator.next();
							sortedTuples.add(next);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
							// quick break out if sortedTuples is guaranteed to be of size 1 since we don't need to sort
							// it then
							if (sortedTuples.size() == 1 && !iterator.hasNext()) {
								sortedTuplesIterator = sortedTuples.iterator();
								return;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
						}

						assert !iterator.hasNext() : "Iterator: " + iterator;
					}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java

				ValidationTuple prevLeft = nextLeft;
				if (nextLeft == null && leftIterator.hasNext()) {
					nextLeft = leftIterator.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
				ValidationTuple prevLeft = nextLeft;
				if (nextLeft == null && leftIterator.hasNext()) {
					nextLeft = leftIterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
				}

				if (nextRight == null && rightIterator.hasNext()) {
					nextRight = rightIterator.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java

				if (nextRight == null && rightIterator.hasNext()) {
					nextRight = rightIterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
					if (discardedLeft != null) {
						discardedLeft.push(nextLeft);
						while (leftIterator.hasNext()) {
							discardedLeft.push(leftIterator.next());
						}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
						discardedLeft.push(nextLeft);
						while (leftIterator.hasNext()) {
							discardedLeft.push(leftIterator.next());
						}
						assert !leftIterator.hasNext() : leftIterator.toString();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
							discardedLeft.push(leftIterator.next());
						}
						assert !leftIterator.hasNext() : leftIterator.toString();
					}
					nextLeft = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
					if (discardedRight != null) {
						discardedRight.push(nextRight);
						while (rightIterator.hasNext()) {
							discardedRight.push(rightIterator.next());
						}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
						discardedRight.push(nextRight);
						while (rightIterator.hasNext()) {
							discardedRight.push(rightIterator.next());
						}
						assert !rightIterator.hasNext() : rightIterator.toString();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
							discardedRight.push(rightIterator.next());
						}
						assert !rightIterator.hasNext() : rightIterator.toString();

					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
									discardedLeft.push(nextLeft);
								}
								if (leftIterator.hasNext()) {
									nextLeft = leftIterator.next();
								} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
								}
								if (leftIterator.hasNext()) {
									nextLeft = leftIterator.next();
								} else {
									nextLeft = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
									discardedRight.push(nextRight);
								}
								if (rightIterator.hasNext()) {
									nextRight = rightIterator.next();
								} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
								}
								if (rightIterator.hasNext()) {
									nextRight = rightIterator.next();
								} else {
									nextRight = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
					} else {
						if (discardedLeft != null) {
							while (leftIterator.hasNext()) {
								discardedLeft.push(leftIterator.next());
							}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
						if (discardedLeft != null) {
							while (leftIterator.hasNext()) {
								discardedLeft.push(leftIterator.next());
							}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
						}

						assert !rightIterator.hasNext() : rightIterator.toString();

						return;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
					if (discardedLeft != null) {

						while (leftIterator.hasNext()) {
							discardedLeft.push(leftIterator.next());
						}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java

						while (leftIterator.hasNext()) {
							discardedLeft.push(leftIterator.next());
						}
						assert !leftIterator.hasNext() : leftIterator.toString();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
							discardedLeft.push(leftIterator.next());
						}
						assert !leftIterator.hasNext() : leftIterator.toString();
					}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
					if (discardedRight != null) {

						while (rightIterator.hasNext()) {
							discardedRight.push(rightIterator.next());
						}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java

						while (rightIterator.hasNext()) {
							discardedRight.push(rightIterator.next());
						}
						assert !rightIterator.hasNext() : rightIterator.toString();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
							discardedRight.push(rightIterator.next());
						}
						assert !rightIterator.hasNext() : rightIterator.toString();
					}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java

		while (true) {
			if (iterator.hasNext()) {
				ValidationTuple next = iterator.next();
				if (joined != null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
		while (true) {
			if (iterator.hasNext()) {
				ValidationTuple next = iterator.next();
				if (joined != null) {
					joined.push(next);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java
				}

				while (next == null && (tempNext != null || parentIterator.hasNext())) {
					if (tempNext == null) {
						tempNext = parentIterator.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java
				while (next == null && (tempNext != null || parentIterator.hasNext())) {
					if (tempNext == null) {
						tempNext = parentIterator.next();
					}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java
						tempNext = null;

						if (parentIterator.hasNext()) {
							tempNext = parentIterator.next();
						}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java

						if (parentIterator.hasNext()) {
							tempNext = parentIterator.next();
						}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
				}

				if (nextLeft == null && leftIterator.hasNext()) {
					nextLeft = leftIterator.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java

				if (nextLeft == null && leftIterator.hasNext()) {
					nextLeft = leftIterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
				}

				if (nextRight == null && rightIterator.hasNext()) {
					nextRight = rightIterator.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java

				if (nextRight == null && rightIterator.hasNext()) {
					nextRight = rightIterator.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java

							if (compareTo < 0) {
								if (leftIterator.hasNext()) {
									nextLeft = leftIterator.next();
								} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
							if (compareTo < 0) {
								if (leftIterator.hasNext()) {
									nextLeft = leftIterator.next();
								} else {
									nextLeft = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
								}
							} else {
								if (rightIterator.hasNext()) {
									nextRight = rightIterator.next();
								} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
							} else {
								if (rightIterator.hasNext()) {
									nextRight = rightIterator.next();
								} else {
									nextRight = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NotValuesIn.java`
#### Snippet
```java
			{
				try (CloseableIteration<? extends ValidationTuple, SailException> iterator = notIn.iterator()) {
					while (iterator.hasNext()) {
						notInValueSet.add(iterator.next().getValue());
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NotValuesIn.java`
#### Snippet
```java
				try (CloseableIteration<? extends ValidationTuple, SailException> iterator = notIn.iterator()) {
					while (iterator.hasNext()) {
						notInValueSet.add(iterator.next().getValue());
					}
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NotValuesIn.java`
#### Snippet
```java
			void calculateNext() {

				while (next == null && parentIterator.hasNext()) {
					ValidationTuple temp = parentIterator.next();
					if (!notInValueSet.contains(temp.getValue())) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NotValuesIn.java`
#### Snippet
```java

				while (next == null && parentIterator.hasNext()) {
					ValidationTuple temp = parentIterator.next();
					if (!notInValueSet.contains(temp.getValue())) {
						next = temp;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java
				}

				while (next == null && (tempNext != null || parentIterator.hasNext())) {
					if (tempNext == null) {
						tempNext = parentIterator.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java
				while (next == null && (tempNext != null || parentIterator.hasNext())) {
					if (tempNext == null) {
						tempNext = parentIterator.next();
					}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java
						tempNext = null;

						if (parentIterator.hasNext()) {
							tempNext = parentIterator.next();
						}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java

						if (parentIterator.hasNext()) {
							tempNext = parentIterator.next();
						}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ReduceTargets.java`
#### Snippet
```java
			void calculateNext() {

				while (next == null && parentIterator.hasNext()) {
					ValidationTuple temp = parentIterator.next();
					if (!reductionSourceSet.contains(temp.getActiveTarget())) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ReduceTargets.java`
#### Snippet
```java

				while (next == null && parentIterator.hasNext()) {
					ValidationTuple temp = parentIterator.next();
					if (!reductionSourceSet.contains(temp.getActiveTarget())) {
						next = temp;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ReduceTargets.java`
#### Snippet
```java
				try (CloseableIteration<? extends ValidationTuple, SailException> iterator = reductionSource
						.iterator()) {
					while (iterator.hasNext()) {
						reductionSourceSet.add(iterator.next().getActiveTarget());
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ReduceTargets.java`
#### Snippet
```java
						.iterator()) {
					while (iterator.hasNext()) {
						reductionSourceSet.add(iterator.next().getActiveTarget());
					}
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
			void calculateNext() {

				if (!parentIterator.hasNext()) {
					return;
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
				}

				while (next == null && parentIterator.hasNext()) {
					ValidationTuple temp = parentIterator.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java

				while (next == null && parentIterator.hasNext()) {
					ValidationTuple temp = parentIterator.next();

					Value value;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
				}

				assert next != null || !parentIterator.hasNext() : parentIterator.toString();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NonUniqueTargetLang.java`
#### Snippet
```java
		}

		while (next == null && parentIterator.hasNext()) {
			next = parentIterator.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NonUniqueTargetLang.java`
#### Snippet
```java

		while (next == null && parentIterator.hasNext()) {
			next = parentIterator.next();

			if ((previous != null)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ExternalFilterByQuery.java`
#### Snippet
```java

		try (var bindingSet = connection.evaluate(query, dataset, bindings, false)) {
			return bindingSet.hasNext();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java
			protected ValidationTuple loggingNext() throws SailException {

				return mapper.apply(statements.next());
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java
			@Override
			protected boolean localHasNext() throws SailException {
				return statements.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
				}

				while (joined.isEmpty() && leftNodeIterator.hasNext()) {

					while (left.size() < BULK_SIZE && leftNodeIterator.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
				while (joined.isEmpty() && leftNodeIterator.hasNext()) {

					while (left.size() < BULK_SIZE && leftNodeIterator.hasNext()) {
						left.addFirst(leftNodeIterator.next());
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java

					while (left.size() < BULK_SIZE && leftNodeIterator.hasNext()) {
						left.addFirst(leftNodeIterator.next());
					}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
				}

				while (left.size() < BULK_SIZE && leftNodeIterator.hasNext()) {
					left.addFirst(leftNodeIterator.next());
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java

				while (left.size() < BULK_SIZE && leftNodeIterator.hasNext()) {
					left.addFirst(leftNodeIterator.next());
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java

	private void calculateNext() {
		if (tupleIterator.hasNext()) {
			conforms = false;
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java
		}

		if (tupleIterator.hasNext()) {
			if (limit < 0 || counter < limit) {
				ValidationTuple invalidTuple = tupleIterator.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java
		if (tupleIterator.hasNext()) {
			if (limit < 0 || counter < limit) {
				ValidationTuple invalidTuple = tupleIterator.next();

				Set<ValidationTuple> invalidTuples;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java
			}

			if (limit >= 0 && counter >= limit && tupleIterator.hasNext()) {
				truncated = true;
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java
		List<ValidationTuple> actualList = new ArrayList<>();
		long localCounter = 0;
		while (tupleIterator.hasNext() && (limit < 0 || localCounter++ < limit + 1)) {
			actualList.add(tupleIterator.next());
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java
		long localCounter = 0;
		while (tupleIterator.hasNext() && (limit < 0 || localCounter++ < limit + 1)) {
			actualList.add(tupleIterator.next());
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
			protected ValidationTuple loggingNext() throws SailException {
				calculateNext();
				return mapper.apply(bindingSet.next());
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
			public void calculateNext() {

				while (bindingSet == null || !bindingSet.hasNext()) {

					if (bindingSet != null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
					}

					if (bulk.isEmpty() && !iterator.hasNext()) {
						return;
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
					ValidationTuple next;
					if (bulk.isEmpty()) {
						next = iterator.next();
						bulk.add(next);
					} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
					}

					while (bulk.size() < bulkSize && iterator.hasNext()) {
						bulk.add(iterator.next());
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java

					while (bulk.size() < bulkSize && iterator.hasNext()) {
						bulk.add(iterator.next());
					}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
					bulk = null;
					parsedQuery = null;
					assert !iterator.hasNext();
					iterator.close();
				} finally {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
			protected boolean localHasNext() throws SailException {
				calculateNext();
				return bindingSet != null && bindingSet.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/CloseablePeakableIteration.java`
#### Snippet
```java
	public E peek() throws X {
		if (peek == null) {
			peek = parent.next();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/CloseablePeakableIteration.java`
#### Snippet
```java
	@Override
	public void remove() throws X {
		parent.remove();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/CloseablePeakableIteration.java`
#### Snippet
```java
			return true;
		}
		return parent.hasNext();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/CloseablePeakableIteration.java`
#### Snippet
```java
			peek = null;
		} else {
			next = parent.next();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
				ExtensibleStatement next = null;

				while (next == null && iterator.hasNext()) {
					SearchHit nextSearchHit = iterator.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java

				while (next == null && iterator.hasNext()) {
					SearchHit nextSearchHit = iterator.next();

					Map<String, Object> sourceAsMap = nextSearchHit.getSourceAsMap();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
				inferred, contexts)) {
			List<ExtensibleStatement> statementsToDelete = new ArrayList<>();
			for (int i = 0; i < 1000 && statements.hasNext(); i++) {
				statementsToDelete.add(statements.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
			List<ExtensibleStatement> statementsToDelete = new ArrayList<>();
			for (int i = 0; i < 1000 && statements.hasNext(); i++) {
				statementsToDelete.add(statements.next());
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
			}

			if (!statements.hasNext()) {
				for (ExtensibleStatement statement : statementsToDelete) {
					removeStatement(statement);
```

### MarkedForRemoval
'meet(org.eclipse.rdf4j.query.algebra.Like)' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Like.java`
#### Snippet
```java
	@Override
	public <X extends Exception> void visit(QueryModelVisitor<X> visitor) throws X {
		visitor.meet(this);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAny.java`
#### Snippet
```java
	@Override
	public int hashCode() {
		return super.hashCode() ^ operator.hashCode() ^ "CompareAny".hashCode();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAny.java`
#### Snippet
```java
	@Override
	public boolean equals(Object other) {
		if (other instanceof CompareAny && super.equals(other)) {
			CompareAny o = (CompareAny) other;
			return operator.equals(o.getOperator());
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAny.java`
#### Snippet
```java

@Deprecated(forRemoval = true, since = "4.2.1")
public class CompareAny extends CompareSubQueryValueOperator {

	/*-----------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAny.java`
#### Snippet
```java
	@Override
	public CompareAny clone() {
		return (CompareAny) super.clone();
	}
}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Filter.java`
#### Snippet
```java
			result = Stream
					.concat(result.stream(),
							((Exists) condition).getSubQuery().getBindingNames().stream())
					.collect(Collectors.toSet());
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/In.java`
#### Snippet
```java
 * Checks whether a certain value is contained in a set of results produced by a query.
 */
public class In extends CompareSubQueryValueOperator {

	/*--------------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/In.java`
#### Snippet
```java
	@Override
	public int hashCode() {
		return super.hashCode() ^ "In".hashCode();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/In.java`
#### Snippet
```java
	@Override
	public In clone() {
		return (In) super.clone();
	}
}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/In.java`
#### Snippet
```java
	@Override
	public boolean equals(Object other) {
		return other instanceof In && super.equals(other);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareSubQueryValueOperator.java`
#### Snippet
```java

@Deprecated(forRemoval = true, since = "4.2.1")
public abstract class CompareSubQueryValueOperator extends SubQueryValueOperator {

	/*-----------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareSubQueryValueOperator.java`
#### Snippet
```java
	@Override
	public CompareSubQueryValueOperator clone() {
		CompareSubQueryValueOperator clone = (CompareSubQueryValueOperator) super.clone();
		clone.setArg(getArg().clone());
		return clone;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareSubQueryValueOperator.java`
#### Snippet
```java
	@Override
	public int hashCode() {
		return super.hashCode() ^ arg.hashCode();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareSubQueryValueOperator.java`
#### Snippet
```java
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		arg.visit(visitor);
		super.visitChildren(visitor);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareSubQueryValueOperator.java`
#### Snippet
```java
	@Override
	public boolean equals(Object other) {
		if (other instanceof CompareSubQueryValueOperator && super.equals(other)) {
			CompareSubQueryValueOperator o = (CompareSubQueryValueOperator) other;
			return arg.equals(o.getArg());
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareSubQueryValueOperator.java`
#### Snippet
```java
			setArg((ValueExpr) replacement);
		} else {
			super.replaceChildNode(current, replacement);
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/QueryModelVisitor.java`
#### Snippet
```java
	void meet(CompareAll node) throws X;

	void meet(CompareAny node) throws X;

	void meet(DescribeOperator node) throws X;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/QueryModelVisitor.java`
#### Snippet
```java

	@Deprecated(forRemoval = true)
	void meet(Like node) throws X;

	void meet(Load load) throws X;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/QueryModelVisitor.java`
#### Snippet
```java
	void meet(Compare node) throws X;

	void meet(CompareAll node) throws X;

	void meet(CompareAny node) throws X;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Exists.java`
#### Snippet
```java
	@Override
	public Exists clone() {
		return (Exists) super.clone();
	}
}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Exists.java`
#### Snippet
```java
 * Checks whether the wrapped Query produces any results.
 */
public class Exists extends SubQueryValueOperator {

	/*--------------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Exists.java`
#### Snippet
```java
	@Override
	public int hashCode() {
		return super.hashCode() ^ "Exists".hashCode();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Exists.java`
#### Snippet
```java
	@Override
	public boolean equals(Object other) {
		return other instanceof Exists && super.equals(other);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAll.java`
#### Snippet
```java
	@Override
	public CompareAll clone() {
		return (CompareAll) super.clone();
	}
}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAll.java`
#### Snippet
```java
	@Override
	public boolean equals(Object other) {
		if (other instanceof CompareAll && super.equals(other)) {
			CompareAll o = (CompareAll) other;
			return operator.equals(o.getOperator());
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAll.java`
#### Snippet
```java

@Deprecated(forRemoval = true, since = "4.2.1")
public class CompareAll extends CompareSubQueryValueOperator {

	/*-----------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAll.java`
#### Snippet
```java
	@Override
	public int hashCode() {
		return super.hashCode() ^ operator.hashCode() ^ "CompareAll".hashCode();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java

	@Override
	public void meet(CompareAny node) throws Exception {
		((SubQueryValueOperator) node).visitChildren(this);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	public void meet(CompareAny node) throws Exception {
		((SubQueryValueOperator) node).visitChildren(this);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	public void meet(CompareAny node) throws Exception {
		((SubQueryValueOperator) node).visitChildren(this);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Coalesce;
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Copy;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Copy;
import org.eclipse.rdf4j.query.algebra.Count;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.LangMatches;
import org.eclipse.rdf4j.query.algebra.LeftJoin;
import org.eclipse.rdf4j.query.algebra.Like;
import org.eclipse.rdf4j.query.algebra.ListMemberOperator;
import org.eclipse.rdf4j.query.algebra.Load;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.StatementPattern;
import org.eclipse.rdf4j.query.algebra.Str;
import org.eclipse.rdf4j.query.algebra.SubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Sum;
import org.eclipse.rdf4j.query.algebra.TripleRef;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	public void meet(In node) throws Exception {
		((SubQueryValueOperator) node).visitChildren(this);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	public void meet(In node) throws Exception {
		((SubQueryValueOperator) node).visitChildren(this);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	public void meet(Exists node) throws Exception {
		node.visitChildren(this);
	}

```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.query.algebra.QueryModelVisitor'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	@Deprecated(forRemoval = true)
	public void meet(Like node) throws Exception {
		// From SERQL should not be seen
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	@Deprecated(forRemoval = true)
	public void meet(Like node) throws Exception {
		// From SERQL should not be seen
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java

	@Override
	public void meet(CompareAll node) throws Exception {
		((SubQueryValueOperator) node).visitChildren(this);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	public void meet(CompareAll node) throws Exception {
		((SubQueryValueOperator) node).visitChildren(this);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
	@Override
	public void meet(CompareAll node) throws Exception {
		((SubQueryValueOperator) node).visitChildren(this);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
		} else if (node instanceof BinaryTupleOperator) {
			((BinaryTupleOperator) node).visitChildren(this);
		} else if (node instanceof CompareSubQueryValueOperator) {
			((CompareSubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof SubQueryValueOperator) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
			((BinaryTupleOperator) node).visitChildren(this);
		} else if (node instanceof CompareSubQueryValueOperator) {
			((CompareSubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof SubQueryValueOperator) {
			((SubQueryValueOperator) node).visitChildren(this);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
			((BinaryTupleOperator) node).visitChildren(this);
		} else if (node instanceof CompareSubQueryValueOperator) {
			((CompareSubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof SubQueryValueOperator) {
			((SubQueryValueOperator) node).visitChildren(this);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
		} else if (node instanceof CompareSubQueryValueOperator) {
			((CompareSubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof SubQueryValueOperator) {
			((SubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof UnaryValueOperator) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
			((CompareSubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof SubQueryValueOperator) {
			((SubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof UnaryValueOperator) {
			((UnaryValueOperator) node).visitChildren(this);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
			((CompareSubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof SubQueryValueOperator) {
			((SubQueryValueOperator) node).visitChildren(this);
		} else if (node instanceof UnaryValueOperator) {
			((UnaryValueOperator) node).visitChildren(this);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java

	@Override
	public void meet(CompareAny node) throws X {
		meetCompareSubQueryValueOperator(node);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java

	@Override
	public void meet(CompareAll node) throws X {
		meetCompareSubQueryValueOperator(node);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java

	/**
	 * Method called by all <var>meet</var> methods with a {@link CompareSubQueryValueOperator} node as argument.
	 * Forwards the call to {@link #meetSubQueryValueOperator} by default.
	 *
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
	 * @param node The node that is being visited.
	 */
	protected void meetCompareSubQueryValueOperator(CompareSubQueryValueOperator node) throws X {
		meetSubQueryValueOperator(node);
	}
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.query.algebra.QueryModelVisitor'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
	@Override
	@Deprecated(forRemoval = true)
	public void meet(Like node) throws X {
		// From SERQL should not be seen
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
	@Override
	@Deprecated(forRemoval = true)
	public void meet(Like node) throws X {
		// From SERQL should not be seen
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java

	/**
	 * Method called by all <var>meet</var> methods with a {@link SubQueryValueOperator} node as argument.
	 *
	 * @param node The node that is being visited.
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
	 * @param node The node that is being visited.
	 */
	protected void meetSubQueryValueOperator(SubQueryValueOperator node) throws X {
		node.visitChildren(this);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
	 */
	protected void meetSubQueryValueOperator(SubQueryValueOperator node) throws X {
		node.visitChildren(this);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
		} else if (node instanceof BinaryTupleOperator) {
			meetBinaryTupleOperator((BinaryTupleOperator) node);
		} else if (node instanceof CompareSubQueryValueOperator) {
			meetCompareSubQueryValueOperator((CompareSubQueryValueOperator) node);
		} else if (node instanceof SubQueryValueOperator) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
			meetBinaryTupleOperator((BinaryTupleOperator) node);
		} else if (node instanceof CompareSubQueryValueOperator) {
			meetCompareSubQueryValueOperator((CompareSubQueryValueOperator) node);
		} else if (node instanceof SubQueryValueOperator) {
			meetSubQueryValueOperator((SubQueryValueOperator) node);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
		} else if (node instanceof CompareSubQueryValueOperator) {
			meetCompareSubQueryValueOperator((CompareSubQueryValueOperator) node);
		} else if (node instanceof SubQueryValueOperator) {
			meetSubQueryValueOperator((SubQueryValueOperator) node);
		} else if (node instanceof UnaryValueOperator) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
			meetCompareSubQueryValueOperator((CompareSubQueryValueOperator) node);
		} else if (node instanceof SubQueryValueOperator) {
			meetSubQueryValueOperator((SubQueryValueOperator) node);
		} else if (node instanceof UnaryValueOperator) {
			meetUnaryValueOperator((UnaryValueOperator) node);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Coalesce;
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Copy;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Copy;
import org.eclipse.rdf4j.query.algebra.Count;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.LangMatches;
import org.eclipse.rdf4j.query.algebra.LeftJoin;
import org.eclipse.rdf4j.query.algebra.Like;
import org.eclipse.rdf4j.query.algebra.ListMemberOperator;
import org.eclipse.rdf4j.query.algebra.Load;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.StatementPattern;
import org.eclipse.rdf4j.query.algebra.Str;
import org.eclipse.rdf4j.query.algebra.SubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Sum;
import org.eclipse.rdf4j.query.algebra.TripleRef;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java

	@Override
	public void meet(CompareAny node) throws X {
		meetCompareSubQueryValueOperator(node);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Coalesce;
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Copy;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Copy;
import org.eclipse.rdf4j.query.algebra.Count;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.LangMatches;
import org.eclipse.rdf4j.query.algebra.LeftJoin;
import org.eclipse.rdf4j.query.algebra.Like;
import org.eclipse.rdf4j.query.algebra.ListMemberOperator;
import org.eclipse.rdf4j.query.algebra.Load;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.StatementPattern;
import org.eclipse.rdf4j.query.algebra.Str;
import org.eclipse.rdf4j.query.algebra.SubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.Sum;
import org.eclipse.rdf4j.query.algebra.TripleRef;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
		} else if (node instanceof BinaryTupleOperator) {
			meetBinaryTupleOperator((BinaryTupleOperator) node);
		} else if (node instanceof CompareSubQueryValueOperator) {
			meetCompareSubQueryValueOperator((CompareSubQueryValueOperator) node);
		} else if (node instanceof SubQueryValueOperator) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
			meetBinaryTupleOperator((BinaryTupleOperator) node);
		} else if (node instanceof CompareSubQueryValueOperator) {
			meetCompareSubQueryValueOperator((CompareSubQueryValueOperator) node);
		} else if (node instanceof SubQueryValueOperator) {
			meetSubQueryValueOperator((SubQueryValueOperator) node);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
		} else if (node instanceof CompareSubQueryValueOperator) {
			meetCompareSubQueryValueOperator((CompareSubQueryValueOperator) node);
		} else if (node instanceof SubQueryValueOperator) {
			meetSubQueryValueOperator((SubQueryValueOperator) node);
		} else if (node instanceof UnaryValueOperator) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
			meetCompareSubQueryValueOperator((CompareSubQueryValueOperator) node);
		} else if (node instanceof SubQueryValueOperator) {
			meetSubQueryValueOperator((SubQueryValueOperator) node);
		} else if (node instanceof UnaryValueOperator) {
			meetUnaryValueOperator((UnaryValueOperator) node);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java

	/**
	 * Method called by all <var>meet</var> methods with a {@link SubQueryValueOperator} node as argument. Forwards the
	 * call to {@link #meetNode} by default.
	 *
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
	 * @param node The node that is being visited.
	 */
	protected void meetSubQueryValueOperator(SubQueryValueOperator node) throws X {
		meetNode(node);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java

	/**
	 * Method called by all <var>meet</var> methods with a {@link CompareSubQueryValueOperator} node as argument.
	 * Forwards the call to {@link #meetSubQueryValueOperator} by default.
	 *
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
	 * @param node The node that is being visited.
	 */
	protected void meetCompareSubQueryValueOperator(CompareSubQueryValueOperator node) throws X {
		meetSubQueryValueOperator(node);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java

	@Override
	public void meet(CompareAll node) throws X {
		meetCompareSubQueryValueOperator(node);
	}
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.query.algebra.QueryModelVisitor'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
	@Override
	@Deprecated(forRemoval = true)
	public void meet(Like node) throws X {
		// From SERQL should not be seen
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java
	@Override
	@Deprecated(forRemoval = true)
	public void meet(Like node) throws X {
		// From SERQL should not be seen
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinFunctionParser.java`
#### Snippet
```java
	@Override
	public Function parse(IRI funcUri, TripleSource store) throws RDF4JException {
		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinFunctionParser.java`
#### Snippet
```java
	@Override
	public Function parse(IRI funcUri, TripleSource store) throws RDF4JException {
		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinFunctionParser.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.TripleSource;
import org.eclipse.rdf4j.query.algebra.evaluation.function.Function;
import org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources;
import org.eclipse.rdf4j.query.parser.ParsedGraphQuery;
import org.eclipse.rdf4j.query.parser.ParsedQuery;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionParser.java`
#### Snippet
```java
	@Override
	public TupleFunction parse(IRI funcUri, TripleSource store) throws RDF4JException {
		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionParser.java`
#### Snippet
```java
	@Override
	public TupleFunction parse(IRI funcUri, TripleSource store) throws RDF4JException {
		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionParser.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.TripleSource;
import org.eclipse.rdf4j.query.algebra.evaluation.function.TupleFunction;
import org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources;
import org.eclipse.rdf4j.query.parser.ParsedQuery;
import org.eclipse.rdf4j.spin.Argument;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.function.Function;
import org.eclipse.rdf4j.query.algebra.evaluation.function.TupleFunction;
import org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources;
import org.eclipse.rdf4j.spin.SpinParser;

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
	@Override
	public Function parse(IRI funcUri, TripleSource store) throws RDF4JException {
		Statement magicPropStmt = TripleSources.single(funcUri, RDF.TYPE, SPIN.MAGIC_PROPERTY_CLASS, store);
		if (magicPropStmt == null) {
			return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
	@Override
	public Function parse(IRI funcUri, TripleSource store) throws RDF4JException {
		Statement magicPropStmt = TripleSources.single(funcUri, RDF.TYPE, SPIN.MAGIC_PROPERTY_CLASS, store);
		if (magicPropStmt == null) {
			return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
		}

		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
		}

		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
					try (CloseableIteration<? extends List<? extends Value>, QueryEvaluationException> iter = tupleFunc
							.evaluate(valueFactory, args)) {
						if (iter.hasNext()) {
							return iter.next().get(0);
						} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
							.evaluate(valueFactory, args)) {
						if (iter.hasNext()) {
							return iter.next().get(0);
						} else {
							return null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/EvalFunction.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.ValueExprEvaluationException;
import org.eclipse.rdf4j.query.algebra.evaluation.function.Function;
import org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources;
import org.eclipse.rdf4j.query.parser.ParsedBooleanQuery;
import org.eclipse.rdf4j.query.parser.ParsedQuery;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/EvalFunction.java`
#### Snippet
```java

	private boolean isQuery(Resource r, TripleSource store) throws RDF4JException {
		try (CloseableIteration<? extends IRI, QueryEvaluationException> typeIter = TripleSources.getObjectURIs(r,
				RDF.TYPE,
				store)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/EvalFunction.java`
#### Snippet
```java

	private boolean isQuery(Resource r, TripleSource store) throws RDF4JException {
		try (CloseableIteration<? extends IRI, QueryEvaluationException> typeIter = TripleSources.getObjectURIs(r,
				RDF.TYPE,
				store)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/EvalFunction.java`
#### Snippet
```java
				RDF.TYPE,
				store)) {
			while (typeIter.hasNext()) {
				IRI type = typeIter.next();
				if (SP.SELECT_CLASS.equals(type) || SP.ASK_CLASS.equals(type) || SPIN.TEMPLATES_CLASS.equals(type)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/EvalFunction.java`
#### Snippet
```java
				store)) {
			while (typeIter.hasNext()) {
				IRI type = typeIter.next();
				if (SP.SELECT_CLASS.equals(type) || SP.ASK_CLASS.equals(type) || SPIN.TEMPLATES_CLASS.equals(type)) {
					return true;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/ObjectFunction.java`
#### Snippet
```java
			try (CloseableIteration<? extends Statement, QueryEvaluationException> stmts = qp.getTripleSource()
					.getStatements((Resource) subj, (IRI) pred, null)) {
				if (stmts.hasNext()) {
					return stmts.next().getObject();
				} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/ObjectFunction.java`
#### Snippet
```java
					.getStatements((Resource) subj, (IRI) pred, null)) {
				if (stmts.hasNext()) {
					return stmts.next().getObject();
				} else {
					throw new ValueExprEvaluationException("No value");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SelectTupleFunction.java`
#### Snippet
```java
			}
			try {
				queryResult.remove();
			} catch (IllegalStateException e) {
				close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/ConstructTupleFunction.java`
#### Snippet
```java
			}
			try {
				queryResult.remove();
			} catch (IllegalStateException e) {
				close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinxFunctionParser.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.TripleSource;
import org.eclipse.rdf4j.query.algebra.evaluation.function.Function;
import org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources;
import org.eclipse.rdf4j.spin.Argument;
import org.eclipse.rdf4j.spin.SpinParser;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinxFunctionParser.java`
#### Snippet
```java
	@Override
	public Function parse(IRI funcUri, TripleSource store) throws RDF4JException {
		Value codeValue = TripleSources.singleValue(funcUri, SPINX.JAVA_SCRIPT_CODE_PROPERTY, store);
		String code = (codeValue instanceof Literal) ? ((Literal) codeValue).getLabel() : null;
		Value fileValue = TripleSources.singleValue(funcUri, SPINX.JAVA_SCRIPT_FILE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinxFunctionParser.java`
#### Snippet
```java
	@Override
	public Function parse(IRI funcUri, TripleSource store) throws RDF4JException {
		Value codeValue = TripleSources.singleValue(funcUri, SPINX.JAVA_SCRIPT_CODE_PROPERTY, store);
		String code = (codeValue instanceof Literal) ? ((Literal) codeValue).getLabel() : null;
		Value fileValue = TripleSources.singleValue(funcUri, SPINX.JAVA_SCRIPT_FILE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinxFunctionParser.java`
#### Snippet
```java
		Value codeValue = TripleSources.singleValue(funcUri, SPINX.JAVA_SCRIPT_CODE_PROPERTY, store);
		String code = (codeValue instanceof Literal) ? ((Literal) codeValue).getLabel() : null;
		Value fileValue = TripleSources.singleValue(funcUri, SPINX.JAVA_SCRIPT_FILE_PROPERTY, store);
		String file = (fileValue instanceof Literal) ? ((Literal) fileValue).getLabel() : null;
		if (code == null && file == null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinxFunctionParser.java`
#### Snippet
```java
		Value codeValue = TripleSources.singleValue(funcUri, SPINX.JAVA_SCRIPT_CODE_PROPERTY, store);
		String code = (codeValue instanceof Literal) ? ((Literal) codeValue).getLabel() : null;
		Value fileValue = TripleSources.singleValue(funcUri, SPINX.JAVA_SCRIPT_FILE_PROPERTY, store);
		String file = (fileValue instanceof Literal) ? ((Literal) fileValue).getLabel() : null;
		if (code == null && file == null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinxFunctionParser.java`
#### Snippet
```java
		}

		Value returnValue = TripleSources.singleValue(funcUri, SPIN.RETURN_TYPE_PROPERTY, store);

		Map<IRI, Argument> templateArgs = parser.parseArguments(funcUri, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinxFunctionParser.java`
#### Snippet
```java
		}

		Value returnValue = TripleSources.singleValue(funcUri, SPIN.RETURN_TYPE_PROPERTY, store);

		Map<IRI, Argument> templateArgs = parser.parseArguments(funcUri, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/Name.java`
#### Snippet
```java
			try {

				try (Stream<Literal> stream = TripleSources
						.getObjectLiterals((Resource) args[0], RDFS.LABEL, qp.getTripleSource())
						.stream()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/Name.java`
#### Snippet
```java

				try (Stream<Literal> stream = TripleSources
						.getObjectLiterals((Resource) args[0], RDFS.LABEL, qp.getTripleSource())
						.stream()) {
					return stream.findFirst().orElseGet(() -> valueFactory.createLiteral(args[0].stringValue()));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/Name.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.ValueExprEvaluationException;
import org.eclipse.rdf4j.query.algebra.evaluation.function.Function;
import org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources;
import org.eclipse.rdf4j.spin.function.AbstractSpinFunction;

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
package org.eclipse.rdf4j.spin.function.spif;

import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.model.IRI;
import org.eclipse.rdf4j.model.Resource;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.ValueExprEvaluationException;
import org.eclipse.rdf4j.query.algebra.evaluation.function.Function;
import org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources;
import org.eclipse.rdf4j.spin.function.AbstractSpinFunction;

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
		Resource list = (Resource) args[2];
		try {
			Iteration<Value, QueryEvaluationException> iter = TripleSources.list(list, qp.getTripleSource());
			while (iter.hasNext()) {
				Value obj = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
		Resource list = (Resource) args[2];
		try {
			Iteration<Value, QueryEvaluationException> iter = TripleSources.list(list, qp.getTripleSource());
			while (iter.hasNext()) {
				Value obj = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
		Resource list = (Resource) args[2];
		try {
			Iteration<Value, QueryEvaluationException> iter = TripleSources.list(list, qp.getTripleSource());
			while (iter.hasNext()) {
				Value obj = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
		try {
			Iteration<Value, QueryEvaluationException> iter = TripleSources.list(list, qp.getTripleSource());
			while (iter.hasNext()) {
				Value obj = iter.next();
				if (TripleSources.single(subj, pred, obj, qp.getTripleSource()) == null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
			Iteration<Value, QueryEvaluationException> iter = TripleSources.list(list, qp.getTripleSource());
			while (iter.hasNext()) {
				Value obj = iter.next();
				if (TripleSources.single(subj, pred, obj, qp.getTripleSource()) == null) {
					return BooleanLiteral.FALSE;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
			while (iter.hasNext()) {
				Value obj = iter.next();
				if (TripleSources.single(subj, pred, obj, qp.getTripleSource()) == null) {
					return BooleanLiteral.FALSE;
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/HasAllObjects.java`
#### Snippet
```java
			while (iter.hasNext()) {
				Value obj = iter.next();
				if (TripleSources.single(subj, pred, obj, qp.getTripleSource()) == null) {
					return BooleanLiteral.FALSE;
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/DirectTypeHierarchyInferencer.java`
#### Snippet
```java
				ValueFactory vf = getValueFactory();

				while (bindingsIter.hasNext()) {
					BindingSet bindings = bindingsIter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/DirectTypeHierarchyInferencer.java`
#### Snippet
```java

				while (bindingsIter.hasNext()) {
					BindingSet bindings = bindingsIter.next();

					Value subj = bindings.getValue("subject");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/CustomGraphQueryInferencer.java`
#### Snippet
```java
					.evaluate(query.getTupleExpr(), null, EmptyBindingSet.getInstance(), true)) {
				ValueFactory factory = getValueFactory();
				while (bindingsIter.hasNext()) {
					BindingSet bindings = bindingsIter.next();
					Value subj = bindings.getValue("subject");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/CustomGraphQueryInferencer.java`
#### Snippet
```java
				ValueFactory factory = getValueFactory();
				while (bindingsIter.hasNext()) {
					BindingSet bindings = bindingsIter.next();
					Value subj = bindings.getValue("subject");
					Value pred = bindings.getValue("predicate");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements((Resource) xxx, RDFS.SUBCLASSOF, null, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Value yyy = t1.getObject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
			t1Iter = getWrappedConnection().getStatements(aaa, RDFS.DOMAIN, null, true);

			while (t1Iter.hasNext()) {
				Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

			while (t1Iter.hasNext()) {
				Statement t1 = t1Iter.next();

				Value zzz = t1.getObject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements(null, RDFS.SUBCLASSOF, yyy, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Resource xxx = t1.getSubject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements((Resource) yyy, RDFS.SUBCLASSOF, null, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Value zzz = t1.getObject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements(null, RDF.TYPE, xxx, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Resource aaa = t1.getSubject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements(null, (IRI) aaa, null, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Resource xxx = t1.getSubject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements(aaa, RDFS.RANGE, null, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Value zzz = t1.getObject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements(null, RDFS.SUBPROPERTYOF, bbb, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Resource aaa = t1.getSubject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements(null, (IRI) aaa, null, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Resource xxx = t1.getSubject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
			t1Iter = getWrappedConnection().getStatements(aaa, RDFS.SUBPROPERTYOF, null, true);

			while (t1Iter.hasNext()) {
				Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

			while (t1Iter.hasNext()) {
				Statement t1 = t1Iter.next();

				Value bbb = t1.getObject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements((Resource) bbb, RDFS.SUBPROPERTYOF, null, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Value ccc = t1.getObject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
				t1Iter = getWrappedConnection().getStatements(null, (IRI) aaa, null, true);

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

				while (t1Iter.hasNext()) {
					Statement t1 = t1Iter.next();

					Value uuu = t1.getObject();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencerConnection.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> statements = connection.getStatements(null, null,
				null, sail.useInferredToCreateSchema)) {
			while (statements.hasNext()) {
				Statement next = statements.next();
				processForSchemaCache(sail.getValueFactory()
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencerConnection.java`
#### Snippet
```java
				null, sail.useInferredToCreateSchema)) {
			while (statements.hasNext()) {
				Statement next = statements.next();
				processForSchemaCache(sail.getValueFactory()
						.createStatement(next.getSubject(), next.getPredicate(), next.getObject()));
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencerConnection.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> statements = connection.getStatements(null, null,
				null, false)) {
			while (statements.hasNext()) {
				Statement next = statements.next();
				addStatement(false, next.getSubject(), next.getPredicate(), next.getObject(), next.getContext());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencerConnection.java`
#### Snippet
```java
				null, false)) {
			while (statements.hasNext()) {
				Statement next = statements.next();
				addStatement(false, next.getSubject(), next.getPredicate(), next.getObject(), next.getContext());
				if (logger.isDebugEnabled()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ContextStore.java`
#### Snippet
```java
		logger.debug("initializing context cache");
		try (CloseableIteration<Resource, SailException> contextIter = store.getContexts()) {
			while (contextIter.hasNext()) {
				increment(contextIter.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ContextStore.java`
#### Snippet
```java
		try (CloseableIteration<Resource, SailException> contextIter = store.getContexts()) {
			while (contextIter.hasNext()) {
				increment(contextIter.next());
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinRenderer.java`
#### Snippet
```java
			handler.handleStatement(valueFactory.createStatement(subject, SP.ELEMENTS_PROPERTY, elementsList));
			ListContext elementsCtx = newList(elementsList);
			node.getSubQuery().visit(this);
			endList(elementsCtx);
			subject = currentSubj;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
		stmts = dataset.getStatements(subj, pred, obj, contexts);
		try {
			return stmts.hasNext();
		} finally {
			stmts.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			spaces = dataset().getNamespaces();
			try {
				while (spaces.hasNext()) {
					set.add(spaces.next());
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			try {
				while (spaces.hasNext()) {
					set.add(spaces.next());
				}
			} finally {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
				iter = dataset().getStatements(null, null, null);
				try {
					while (iter.hasNext()) {
						iter.next();
						size++;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
				try {
					while (iter.hasNext()) {
						iter.next();
						size++;
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
						try {
							long size = 0;
							while (iter.hasNext()) {
								iter.next();
								if (size++ >= Integer.MAX_VALUE) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
							long size = 0;
							while (iter.hasNext()) {
								iter.next();
								if (size++ >= Integer.MAX_VALUE) {
									return Integer.MAX_VALUE;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			stmts = dataset().getStatements(subj, pred, obj, contexts);
			try {
				while (stmts.hasNext()) {
					Statement st = stmts.next();
					sink().deprecate(st);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			try {
				while (stmts.hasNext()) {
					Statement st = stmts.next();
					sink().deprecate(st);
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
				stmts = dataset().getStatements(subj, pred, obj, contexts);
				try {
					while (stmts.hasNext()) {
						Statement st = stmts.next();
						sink().deprecate(st);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
				try {
					while (stmts.hasNext()) {
						Statement st = stmts.next();
						sink().deprecate(st);
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
		public boolean hasNext() {
			try {
				if (stmts.hasNext()) {
					return true;
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
		public Statement next() {
			try {
				last = stmts.next();
				if (last == null) {
					stmts.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserQuery.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.TimeLimitIteration;
import org.eclipse.rdf4j.query.BindingSet;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserQuery.java`
#### Snippet
```java
	protected class QueryInterruptIteration extends TimeLimitIteration<BindingSet, QueryEvaluationException> {

		public QueryInterruptIteration(Iteration<? extends BindingSet, ? extends QueryEvaluationException> iter,
				long timeLimit) {
			super(iter, timeLimit);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserQuery.java`
#### Snippet
```java
	protected class QueryInterruptIteration extends TimeLimitIteration<BindingSet, QueryEvaluationException> {

		public QueryInterruptIteration(Iteration<? extends BindingSet, ? extends QueryEvaluationException> iter,
				long timeLimit) {
			super(iter, timeLimit);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserQuery.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.TimeLimitIteration;
import org.eclipse.rdf4j.query.BindingSet;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitFilter(Resource r) throws RDF4JException {
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			tupleNode = new Filter(tupleNode, valueExpr);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitFilter(Resource r) throws RDF4JException {
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			tupleNode = new Filter(tupleNode, valueExpr);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				group = new Group();
			}
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(groupby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				group = new Group();
			}
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(groupby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				group = new Group();
			}
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(groupby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			}
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(groupby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ValueExpr groupByExpr = visitExpression(r);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(groupby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ValueExpr groupByExpr = visitExpression(r);
				if (!(groupByExpr instanceof Var)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	private void parseArguments(IRI moduleUri, TripleSource store, Map<IRI, Argument> args) throws RDF4JException {
		try (CloseableIteration<Resource, QueryEvaluationException> argIter = TripleSources
				.getObjectResources(moduleUri, SPIN.CONSTRAINT_PROPERTY, store)) {

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	private void parseArguments(IRI moduleUri, TripleSource store, Map<IRI, Argument> args) throws RDF4JException {
		try (CloseableIteration<Resource, QueryEvaluationException> argIter = TripleSources
				.getObjectResources(moduleUri, SPIN.CONSTRAINT_PROPERTY, store)) {

			while (argIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				.getObjectResources(moduleUri, SPIN.CONSTRAINT_PROPERTY, store)) {

			while (argIter.hasNext()) {
				Resource possibleArg = argIter.next();
				Statement argTmpl = TripleSources.single(possibleArg, RDF.TYPE, SPL.ARGUMENT_TEMPLATE, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			while (argIter.hasNext()) {
				Resource possibleArg = argIter.next();
				Statement argTmpl = TripleSources.single(possibleArg, RDF.TYPE, SPL.ARGUMENT_TEMPLATE, store);
				if (argTmpl != null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			while (argIter.hasNext()) {
				Resource possibleArg = argIter.next();
				Statement argTmpl = TripleSources.single(possibleArg, RDF.TYPE, SPL.ARGUMENT_TEMPLATE, store);
				if (argTmpl != null) {
					Value argPred = TripleSources.singleValue(possibleArg, SPL.PREDICATE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			while (argIter.hasNext()) {
				Resource possibleArg = argIter.next();
				Statement argTmpl = TripleSources.single(possibleArg, RDF.TYPE, SPL.ARGUMENT_TEMPLATE, store);
				if (argTmpl != null) {
					Value argPred = TripleSources.singleValue(possibleArg, SPL.PREDICATE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				Statement argTmpl = TripleSources.single(possibleArg, RDF.TYPE, SPL.ARGUMENT_TEMPLATE, store);
				if (argTmpl != null) {
					Value argPred = TripleSources.singleValue(possibleArg, SPL.PREDICATE_PROPERTY, store);
					Value valueType = TripleSources.singleValue(possibleArg, SPL.VALUE_TYPE_PROPERTY, store);
					boolean optional = TripleSources.booleanValue(possibleArg, SPL.OPTIONAL_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				Statement argTmpl = TripleSources.single(possibleArg, RDF.TYPE, SPL.ARGUMENT_TEMPLATE, store);
				if (argTmpl != null) {
					Value argPred = TripleSources.singleValue(possibleArg, SPL.PREDICATE_PROPERTY, store);
					Value valueType = TripleSources.singleValue(possibleArg, SPL.VALUE_TYPE_PROPERTY, store);
					boolean optional = TripleSources.booleanValue(possibleArg, SPL.OPTIONAL_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				if (argTmpl != null) {
					Value argPred = TripleSources.singleValue(possibleArg, SPL.PREDICATE_PROPERTY, store);
					Value valueType = TripleSources.singleValue(possibleArg, SPL.VALUE_TYPE_PROPERTY, store);
					boolean optional = TripleSources.booleanValue(possibleArg, SPL.OPTIONAL_PROPERTY, store);
					Value defaultValue = TripleSources.singleValue(possibleArg, SPL.DEFAULT_VALUE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				if (argTmpl != null) {
					Value argPred = TripleSources.singleValue(possibleArg, SPL.PREDICATE_PROPERTY, store);
					Value valueType = TripleSources.singleValue(possibleArg, SPL.VALUE_TYPE_PROPERTY, store);
					boolean optional = TripleSources.booleanValue(possibleArg, SPL.OPTIONAL_PROPERTY, store);
					Value defaultValue = TripleSources.singleValue(possibleArg, SPL.DEFAULT_VALUE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value argPred = TripleSources.singleValue(possibleArg, SPL.PREDICATE_PROPERTY, store);
					Value valueType = TripleSources.singleValue(possibleArg, SPL.VALUE_TYPE_PROPERTY, store);
					boolean optional = TripleSources.booleanValue(possibleArg, SPL.OPTIONAL_PROPERTY, store);
					Value defaultValue = TripleSources.singleValue(possibleArg, SPL.DEFAULT_VALUE_PROPERTY, store);
					IRI argUri = (IRI) argPred;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value argPred = TripleSources.singleValue(possibleArg, SPL.PREDICATE_PROPERTY, store);
					Value valueType = TripleSources.singleValue(possibleArg, SPL.VALUE_TYPE_PROPERTY, store);
					boolean optional = TripleSources.booleanValue(possibleArg, SPL.OPTIONAL_PROPERTY, store);
					Value defaultValue = TripleSources.singleValue(possibleArg, SPL.DEFAULT_VALUE_PROPERTY, store);
					IRI argUri = (IRI) argPred;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value valueType = TripleSources.singleValue(possibleArg, SPL.VALUE_TYPE_PROPERTY, store);
					boolean optional = TripleSources.booleanValue(possibleArg, SPL.OPTIONAL_PROPERTY, store);
					Value defaultValue = TripleSources.singleValue(possibleArg, SPL.DEFAULT_VALUE_PROPERTY, store);
					IRI argUri = (IRI) argPred;
					args.put(argUri, new Argument(argUri, (IRI) valueType, optional, defaultValue));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value valueType = TripleSources.singleValue(possibleArg, SPL.VALUE_TYPE_PROPERTY, store);
					boolean optional = TripleSources.booleanValue(possibleArg, SPL.OPTIONAL_PROPERTY, store);
					Value defaultValue = TripleSources.singleValue(possibleArg, SPL.DEFAULT_VALUE_PROPERTY, store);
					IRI argUri = (IRI) argPred;
					args.put(argUri, new Argument(argUri, (IRI) valueType, optional, defaultValue));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitCreate(Resource query) throws RDF4JException {
			Value graph = TripleSources.singleValue(query, SP.GRAPH_IRI_PROPERTY, store);
			Create create = new Create(new ValueConstant(graph));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitCreate(Resource query) throws RDF4JException {
			Value graph = TripleSources.singleValue(query, SP.GRAPH_IRI_PROPERTY, store);
			Create create = new Create(new ValueConstant(graph));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value graph = TripleSources.singleValue(query, SP.GRAPH_IRI_PROPERTY, store);
			Create create = new Create(new ValueConstant(graph));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
			create.setSilent(isSilent);
			updateRoot = create;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value graph = TripleSources.singleValue(query, SP.GRAPH_IRI_PROPERTY, store);
			Create create = new Create(new ValueConstant(graph));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
			create.setSilent(isSilent);
			updateRoot = create;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				throws RDF4JException {
			ProjectionElemList projElemList = new ProjectionElemList();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(resultVars,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				throws RDF4JException {
			ProjectionElemList projElemList = new ProjectionElemList();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(resultVars,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				throws RDF4JException {
			ProjectionElemList projElemList = new ProjectionElemList();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(resultVars,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(resultVars,
					store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ProjectionElem projElem = visitResultVariable(r, previousProjElems);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ProjectionElem projElem = visitResultVariable(r, previousProjElems);
				projElemList.addElement(projElem);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitDescribe(Resource describe) throws RDF4JException {
			Value resultNodes = TripleSources.singleValue(describe, SP.RESULT_NODES_PROPERTY, store);
			if (!(resultNodes instanceof Resource)) {
				throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitDescribe(Resource describe) throws RDF4JException {
			Value resultNodes = TripleSources.singleValue(describe, SP.RESULT_NODES_PROPERTY, store);
			if (!(resultNodes instanceof Resource)) {
				throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					expr = createVar(varName);
				} else {
					Set<IRI> exprTypes = Iterations.asSet(TripleSources.getObjectURIs(r, RDF.TYPE, store));
					exprTypes.remove(RDF.PROPERTY);
					exprTypes.remove(RDFS.RESOURCE);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					expr = createVar(varName);
				} else {
					Set<IRI> exprTypes = Iterations.asSet(TripleSources.getObjectURIs(r, RDF.TYPE, store));
					exprTypes.remove(RDF.PROPERTY);
					exprTypes.remove(RDFS.RESOURCE);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
								for (Iterator<IRI> iter = exprTypes.iterator(); iter.hasNext();) {
									IRI f = iter.next();
									Value abstractValue = TripleSources.singleValue(f, SPIN.ABSTRACT_PROPERTY, store);
									if (BooleanLiteral.TRUE.equals(abstractValue)) {
										iter.remove();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
								for (Iterator<IRI> iter = exprTypes.iterator(); iter.hasNext();) {
									IRI f = iter.next();
									Value abstractValue = TripleSources.singleValue(f, SPIN.ABSTRACT_PROPERTY, store);
									if (BooleanLiteral.TRUE.equals(abstractValue)) {
										iter.remove();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitClear(Resource query) throws RDF4JException {
			Value graph = TripleSources.singleValue(query, SP.GRAPH_IRI_PROPERTY, store);
			Clear clear = new Clear(new ValueConstant(graph));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitClear(Resource query) throws RDF4JException {
			Value graph = TripleSources.singleValue(query, SP.GRAPH_IRI_PROPERTY, store);
			Clear clear = new Clear(new ValueConstant(graph));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value graph = TripleSources.singleValue(query, SP.GRAPH_IRI_PROPERTY, store);
			Clear clear = new Clear(new ValueConstant(graph));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
			clear.setSilent(isSilent);
			updateRoot = clear;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value graph = TripleSources.singleValue(query, SP.GRAPH_IRI_PROPERTY, store);
			Clear clear = new Clear(new ValueConstant(graph));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
			clear.setSilent(isSilent);
			updateRoot = clear;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
import org.eclipse.rdf4j.common.exception.RDF4JException;
import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.Iterations;
import org.eclipse.rdf4j.model.IRI;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.function.TupleFunction;
import org.eclipse.rdf4j.query.algebra.evaluation.function.TupleFunctionRegistry;
import org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources;
import org.eclipse.rdf4j.query.algebra.helpers.AbstractQueryModelVisitor;
import org.eclipse.rdf4j.query.algebra.helpers.TupleExprs;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			tupleNode = stub;
			TupleExpr insertExpr;
			Value insert = TripleSources.singleValue(query, SP.DATA_PROPERTY, store);
			if (!(insert instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.DATA_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			tupleNode = stub;
			TupleExpr insertExpr;
			Value insert = TripleSources.singleValue(query, SP.DATA_PROPERTY, store);
			if (!(insert instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.DATA_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private UnaryTupleOperator visitTemplates(Resource templates) throws RDF4JException {
			List<ProjectionElemList> projElemLists = new ArrayList<>();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(templates,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private UnaryTupleOperator visitTemplates(Resource templates) throws RDF4JException {
			List<ProjectionElemList> projElemLists = new ArrayList<>();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(templates,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private UnaryTupleOperator visitTemplates(Resource templates) throws RDF4JException {
			List<ProjectionElemList> projElemLists = new ArrayList<>();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(templates,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(templates,
					store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ProjectionElemList projElems = visitTemplate(r);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ProjectionElemList projElems = visitTemplate(r);
				projElemLists.add(projElems);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	public Map<IRI, RuleProperty> parseRuleProperties(TripleSource store) throws RDF4JException {
		Map<IRI, RuleProperty> rules = new HashMap<>();
		try (CloseableIteration<IRI, QueryEvaluationException> rulePropIter = TripleSources
				.getSubjectURIs(RDFS.SUBPROPERTYOF, SPIN.RULE_PROPERTY, store)) {

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Map<IRI, RuleProperty> rules = new HashMap<>();
		try (CloseableIteration<IRI, QueryEvaluationException> rulePropIter = TripleSources
				.getSubjectURIs(RDFS.SUBPROPERTYOF, SPIN.RULE_PROPERTY, store)) {

			while (rulePropIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				.getSubjectURIs(RDFS.SUBPROPERTYOF, SPIN.RULE_PROPERTY, store)) {

			while (rulePropIter.hasNext()) {
				IRI ruleProp = rulePropIter.next();
				RuleProperty ruleProperty = new RuleProperty(ruleProp);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			while (rulePropIter.hasNext()) {
				IRI ruleProp = rulePropIter.next();
				RuleProperty ruleProperty = new RuleProperty(ruleProp);

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	private ParsedOperation parseText(Resource queryResource, IRI queryType, TripleSource store) throws RDF4JException {
		Value text = TripleSources.singleValue(queryResource, SP.TEXT_PROPERTY, store);
		if (text != null) {
			if (QUERY_TYPES.contains(queryType)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	private ParsedOperation parseText(Resource queryResource, IRI queryType, TripleSource store) throws RDF4JException {
		Value text = TripleSources.singleValue(queryResource, SP.TEXT_PROPERTY, store);
		if (text != null) {
			if (QUERY_TYPES.contains(queryType)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			if (varName == null) {
				// check for a varName statement
				Value nameValue = TripleSources.singleValue(r, SP.VAR_NAME_PROPERTY, store);
				if (nameValue instanceof Literal) {
					varName = ((Literal) nameValue).getLabel();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			if (varName == null) {
				// check for a varName statement
				Value nameValue = TripleSources.singleValue(r, SP.VAR_NAME_PROPERTY, store);
				if (nameValue instanceof Literal) {
					varName = ((Literal) nameValue).getLabel();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitInsert(Resource insert) throws RDF4JException {
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(insert,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitInsert(Resource insert) throws RDF4JException {
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(insert,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitInsert(Resource insert) throws RDF4JException {
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(insert,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(insert,
					store);
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				Value type = TripleSources.singleValue(r, RDF.TYPE, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					store);
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				Value type = TripleSources.singleValue(r, RDF.TYPE, store);
				visitPattern(r, (type != null) ? Collections.singleton((IRI) type) : Collections.<IRI>emptySet(), null);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				Value type = TripleSources.singleValue(r, RDF.TYPE, store);
				visitPattern(r, (type != null) ? Collections.singleton((IRI) type) : Collections.<IRI>emptySet(), null);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				Value type = TripleSources.singleValue(r, RDF.TYPE, store);
				visitPattern(r, (type != null) ? Collections.singleton((IRI) type) : Collections.<IRI>emptySet(), null);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			UnaryTupleOperator op = (UnaryTupleOperator) group.getParentNode();
			op.setArg(new Extension(group));
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(having, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			UnaryTupleOperator op = (UnaryTupleOperator) group.getParentNode();
			op.setArg(new Extension(group));
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(having, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			UnaryTupleOperator op = (UnaryTupleOperator) group.getParentNode();
			op.setArg(new Extension(group));
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(having, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			op.setArg(new Extension(group));
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(having, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ValueExpr havingExpr = visitExpression(r);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(having, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ValueExpr havingExpr = visitExpression(r);
				Filter filter = new Filter(op.getArg(), havingExpr);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private Projection visitResultNodes(Resource resultNodes) throws RDF4JException {
			ProjectionElemList projElemList = new ProjectionElemList();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(resultNodes,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private Projection visitResultNodes(Resource resultNodes) throws RDF4JException {
			ProjectionElemList projElemList = new ProjectionElemList();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(resultNodes,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private Projection visitResultNodes(Resource resultNodes) throws RDF4JException {
			ProjectionElemList projElemList = new ProjectionElemList();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(resultNodes,
					store);
			while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(resultNodes,
					store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ProjectionElem projElem = visitResultNode(r);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				ProjectionElem projElem = visitResultNode(r);
				projElemList.addElement(projElem);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			tupleNode = stub;
			TupleExpr deleteExpr;
			Value delete = TripleSources.singleValue(query, SP.DATA_PROPERTY, store);
			if (!(delete instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.DATA_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			tupleNode = stub;
			TupleExpr deleteExpr;
			Value delete = TripleSources.singleValue(query, SP.DATA_PROPERTY, store);
			if (!(delete instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.DATA_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = new Not(args.get(0));
			} else if (SP.COUNT_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Count count = new Count(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = new Not(args.get(0));
			} else if (SP.COUNT_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Count count = new Count(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.COUNT_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Count count = new Count(visitExpression(arg), distinct);
				aggregates.add(count);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.COUNT_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Count count = new Count(visitExpression(arg), distinct);
				aggregates.add(count);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = count;
			} else if (SP.MAX_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Max max = new Max(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = count;
			} else if (SP.MAX_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Max max = new Max(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.MAX_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Max max = new Max(visitExpression(arg), distinct);
				aggregates.add(max);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.MAX_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Max max = new Max(visitExpression(arg), distinct);
				aggregates.add(max);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = max;
			} else if (SP.MIN_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Min min = new Min(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = max;
			} else if (SP.MIN_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Min min = new Min(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.MIN_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Min min = new Min(visitExpression(arg), distinct);
				aggregates.add(min);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.MIN_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Min min = new Min(visitExpression(arg), distinct);
				aggregates.add(min);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = min;
			} else if (SP.SUM_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Sum sum = new Sum(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = min;
			} else if (SP.SUM_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Sum sum = new Sum(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.SUM_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Sum sum = new Sum(visitExpression(arg), distinct);
				aggregates.add(sum);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.SUM_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Sum sum = new Sum(visitExpression(arg), distinct);
				aggregates.add(sum);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = sum;
			} else if (SP.AVG_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Avg avg = new Avg(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = sum;
			} else if (SP.AVG_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Avg avg = new Avg(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.AVG_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Avg avg = new Avg(visitExpression(arg), distinct);
				aggregates.add(avg);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.AVG_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Avg avg = new Avg(visitExpression(arg), distinct);
				aggregates.add(avg);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = avg;
			} else if (SP.GROUP_CONCAT_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				GroupConcat groupConcat = new GroupConcat(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = avg;
			} else if (SP.GROUP_CONCAT_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				GroupConcat groupConcat = new GroupConcat(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.GROUP_CONCAT_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				GroupConcat groupConcat = new GroupConcat(visitExpression(arg), distinct);
				aggregates.add(groupConcat);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.GROUP_CONCAT_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				GroupConcat groupConcat = new GroupConcat(visitExpression(arg), distinct);
				aggregates.add(groupConcat);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = groupConcat;
			} else if (SP.SAMPLE_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Sample sample = new Sample(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = groupConcat;
			} else if (SP.SAMPLE_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Sample sample = new Sample(visitExpression(arg), distinct);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.SAMPLE_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Sample sample = new Sample(visitExpression(arg), distinct);
				aggregates.add(sample);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else if (SP.SAMPLE_CLASS.equals(func)) {
				Value arg = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
				boolean distinct = TripleSources.booleanValue(r, SP.DISTINCT_PROPERTY, store);
				Sample sample = new Sample(visitExpression(arg), distinct);
				aggregates.add(sample);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = sample;
			} else if (SP.EXISTS.equals(func)) {
				Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
				if (!(elements instanceof Resource)) {
					throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				expr = sample;
			} else if (SP.EXISTS.equals(func)) {
				Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
				if (!(elements instanceof Resource)) {
					throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				tupleNode = currentNode;
			} else if (SP.NOT_EXISTS.equals(func)) {
				Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
				if (!(elements instanceof Resource)) {
					throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				tupleNode = currentNode;
			} else if (SP.NOT_EXISTS.equals(func)) {
				Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
				if (!(elements instanceof Resource)) {
					throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				if (funcName == null) {
					// check if it is a SPIN function
					Statement funcTypeStmt = TripleSources.single(func, RDF.TYPE, SPIN.FUNCTION_CLASS, store);
					if (funcTypeStmt != null) {
						funcName = func.stringValue();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				if (funcName == null) {
					// check if it is a SPIN function
					Statement funcTypeStmt = TripleSources.single(func, RDF.TYPE, SPIN.FUNCTION_CLASS, store);
					if (funcTypeStmt != null) {
						funcName = func.stringValue();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			throws RDF4JException {
		Set<IRI> possibleTmplTypes;
		try (Stream<IRI> stream = TripleSources.getObjectURIs(tmplUri, RDF.TYPE, store).stream()) {
			possibleTmplTypes = stream
					.filter(TEMPLATE_TYPES::contains)
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			throws RDF4JException {
		Set<IRI> possibleTmplTypes;
		try (Stream<IRI> stream = TripleSources.getObjectURIs(tmplUri, RDF.TYPE, store).stream()) {
			possibleTmplTypes = stream
					.filter(TEMPLATE_TYPES::contains)
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Template tmpl = new Template(tmplUri);

		Value body = TripleSources.singleValue(tmplUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			throw new MalformedSpinException(String.format("Template body is not a resource: %s", body));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Template tmpl = new Template(tmplUri);

		Value body = TripleSources.singleValue(tmplUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			throw new MalformedSpinException(String.format("Template body is not a resource: %s", body));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				if (varName != null) {
					// var
					Value expr = TripleSources.singleValue((Resource) v, SP.EXPRESSION_PROPERTY, store);
					if (expr != null) {
						// AS
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				if (varName != null) {
					// var
					Value expr = TripleSources.singleValue((Resource) v, SP.EXPRESSION_PROPERTY, store);
					if (expr != null) {
						// AS
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private Order visitOrderBy(Resource orderby) throws RDF4JException {
			Order order = new Order();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(orderby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private Order visitOrderBy(Resource orderby) throws RDF4JException {
			Order order = new Order();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(orderby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private Order visitOrderBy(Resource orderby) throws RDF4JException {
			Order order = new Order();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(orderby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Order order = new Order();
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(orderby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				OrderElem orderElem = visitOrderByCondition(r);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> iter = TripleSources.listResources(orderby, store);
			while (iter.hasNext()) {
				Resource r = iter.next();
				OrderElem orderElem = visitOrderByCondition(r);
				order.addElement(orderElem);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	private List<IRI> getNextRules(Resource ruleProp, TripleSource store) throws RDF4JException {
		return Iterations.asList((TripleSources.getObjectURIs(ruleProp,
				SPIN.NEXT_RULE_PROPERTY_PROPERTY, store)));
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	private List<IRI> getNextRules(Resource ruleProp, TripleSource store) throws RDF4JException {
		return Iterations.asList((TripleSources.getObjectURIs(ruleProp,
				SPIN.NEXT_RULE_PROPERTY_PROPERTY, store)));
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitDelete(Resource delete) throws RDF4JException {
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(delete,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitDelete(Resource delete) throws RDF4JException {
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(delete,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitDelete(Resource delete) throws RDF4JException {
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(delete,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(delete,
					store);
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				Value type = TripleSources.singleValue(r, RDF.TYPE, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					store);
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				Value type = TripleSources.singleValue(r, RDF.TYPE, store);
				visitPattern(r, (type != null) ? Collections.singleton((IRI) type) : Collections.<IRI>emptySet(), null);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				Value type = TripleSources.singleValue(r, RDF.TYPE, store);
				visitPattern(r, (type != null) ? Collections.singleton((IRI) type) : Collections.<IRI>emptySet(), null);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				Value type = TripleSources.singleValue(r, RDF.TYPE, store);
				visitPattern(r, (type != null) ? Collections.singleton((IRI) type) : Collections.<IRI>emptySet(), null);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitModify(Resource query) throws RDF4JException {
			Value with = TripleSources.singleValue(query, SP.WITH_PROPERTY, store);
			if (with != null) {
				namedGraph = TupleExprs.createConstVar(with);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitModify(Resource query) throws RDF4JException {
			Value with = TripleSources.singleValue(query, SP.WITH_PROPERTY, store);
			if (with != null) {
				namedGraph = TupleExprs.createConstVar(with);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			tupleNode = stub;
			TupleExpr deleteExpr;
			Value delete = TripleSources.singleValue(query, SP.DELETE_PATTERN_PROPERTY, store);
			if (delete != null) {
				visitDelete((Resource) delete);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			tupleNode = stub;
			TupleExpr deleteExpr;
			Value delete = TripleSources.singleValue(query, SP.DELETE_PATTERN_PROPERTY, store);
			if (delete != null) {
				visitDelete((Resource) delete);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			tupleNode = stub;
			TupleExpr insertExpr;
			Value insert = TripleSources.singleValue(query, SP.INSERT_PATTERN_PROPERTY, store);
			if (insert != null) {
				visitInsert((Resource) insert);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			tupleNode = stub;
			TupleExpr insertExpr;
			Value insert = TripleSources.singleValue(query, SP.INSERT_PATTERN_PROPERTY, store);
			if (insert != null) {
				visitInsert((Resource) insert);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			TupleExpr whereExpr;
			Value where = TripleSources.singleValue(query, SP.WHERE_PROPERTY, store);
			if (where != null) {
				whereExpr = visitGroupGraphPattern((Resource) where);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			TupleExpr whereExpr;
			Value where = TripleSources.singleValue(query, SP.WHERE_PROPERTY, store);
			if (where != null) {
				whereExpr = visitGroupGraphPattern((Resource) where);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitLoad(Resource query) throws RDF4JException {
			Value document = TripleSources.singleValue(query, SP.DOCUMENT_PROPERTY, store);
			Value into = TripleSources.singleValue(query, SP.INTO_PROPERTY, store);
			Load load = new Load(new ValueConstant(document));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitLoad(Resource query) throws RDF4JException {
			Value document = TripleSources.singleValue(query, SP.DOCUMENT_PROPERTY, store);
			Value into = TripleSources.singleValue(query, SP.INTO_PROPERTY, store);
			Load load = new Load(new ValueConstant(document));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		public void visitLoad(Resource query) throws RDF4JException {
			Value document = TripleSources.singleValue(query, SP.DOCUMENT_PROPERTY, store);
			Value into = TripleSources.singleValue(query, SP.INTO_PROPERTY, store);
			Load load = new Load(new ValueConstant(document));
			load.setGraph(new ValueConstant(into));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		public void visitLoad(Resource query) throws RDF4JException {
			Value document = TripleSources.singleValue(query, SP.DOCUMENT_PROPERTY, store);
			Value into = TripleSources.singleValue(query, SP.INTO_PROPERTY, store);
			Load load = new Load(new ValueConstant(document));
			load.setGraph(new ValueConstant(into));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Load load = new Load(new ValueConstant(document));
			load.setGraph(new ValueConstant(into));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
			load.setSilent(isSilent);
			updateRoot = load;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Load load = new Load(new ValueConstant(document));
			load.setGraph(new ValueConstant(into));
			boolean isSilent = TripleSources.booleanValue(query, SP.SILENT_PROPERTY, store);
			load.setSilent(isSilent);
			updateRoot = load;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			Map<Resource, Set<IRI>> patternTypes = new LinkedHashMap<>();
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(group,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			Map<Resource, Set<IRI>> patternTypes = new LinkedHashMap<>();
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(group,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			Map<Resource, Set<IRI>> patternTypes = new LinkedHashMap<>();
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(group,
					store);
			while (groupIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Iteration<Resource, QueryEvaluationException> groupIter = TripleSources.listResources(group,
					store);
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				patternTypes.put(r, Iterations.asSet(TripleSources.getObjectURIs(r, RDF.TYPE, store)));
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					store);
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				patternTypes.put(r, Iterations.asSet(TripleSources.getObjectURIs(r, RDF.TYPE, store)));
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				patternTypes.put(r, Iterations.asSet(TripleSources.getObjectURIs(r, RDF.TYPE, store)));
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			while (groupIter.hasNext()) {
				Resource r = groupIter.next();
				patternTypes.put(r, Iterations.asSet(TripleSources.getObjectURIs(r, RDF.TYPE, store)));
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitConstruct(Resource construct) throws RDF4JException {
			Value templates = TripleSources.singleValue(construct, SP.TEMPLATES_PROPERTY, store);
			if (!(templates instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.TEMPLATES_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitConstruct(Resource construct) throws RDF4JException {
			Value templates = TripleSources.singleValue(construct, SP.TEMPLATES_PROPERTY, store);
			if (!(templates instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.TEMPLATES_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private ProjectionElemList visitTemplate(Resource r) throws RDF4JException {
			ProjectionElemList projElems = new ProjectionElemList();
			Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
			projElems.addElement(createProjectionElem(subj, "subject", null));
			Value pred = TripleSources.singleValue(r, SP.PREDICATE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private ProjectionElemList visitTemplate(Resource r) throws RDF4JException {
			ProjectionElemList projElems = new ProjectionElemList();
			Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
			projElems.addElement(createProjectionElem(subj, "subject", null));
			Value pred = TripleSources.singleValue(r, SP.PREDICATE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
			projElems.addElement(createProjectionElem(subj, "subject", null));
			Value pred = TripleSources.singleValue(r, SP.PREDICATE_PROPERTY, store);
			projElems.addElement(createProjectionElem(pred, "predicate", null));
			Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
			projElems.addElement(createProjectionElem(subj, "subject", null));
			Value pred = TripleSources.singleValue(r, SP.PREDICATE_PROPERTY, store);
			projElems.addElement(createProjectionElem(pred, "predicate", null));
			Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value pred = TripleSources.singleValue(r, SP.PREDICATE_PROPERTY, store);
			projElems.addElement(createProjectionElem(pred, "predicate", null));
			Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
			projElems.addElement(createProjectionElem(obj, "object", null));
			return projElems;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value pred = TripleSources.singleValue(r, SP.PREDICATE_PROPERTY, store);
			projElems.addElement(createProjectionElem(pred, "predicate", null));
			Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
			projElems.addElement(createProjectionElem(obj, "object", null));
			return projElems;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			if (!args.isEmpty()) {
				for (IRI arg : args) {
					Value value = TripleSources.singleValue(r, arg, store);
					if (value != null) {
						ValueExpr argValue = visitExpression(value);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			if (!args.isEmpty()) {
				for (IRI arg : args) {
					Value value = TripleSources.singleValue(r, arg, store);
					if (value != null) {
						ValueExpr argValue = visitExpression(value);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				do {
					IRI arg = toArgProperty(i++);
					value = TripleSources.singleValue(r, arg, store);
					if (value != null) {
						ValueExpr argValue = visitExpression(value);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				do {
					IRI arg = toArgProperty(i++);
					value = TripleSources.singleValue(r, arg, store);
					if (value != null) {
						ValueExpr argValue = visitExpression(value);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public TupleExpr visitWhere(Resource query) throws RDF4JException {
			Value where = TripleSources.singleValue(query, SP.WHERE_PROPERTY, store);
			if (!(where instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.WHERE_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public TupleExpr visitWhere(Resource query) throws RDF4JException {
			Value where = TripleSources.singleValue(query, SP.WHERE_PROPERTY, store);
			if (!(where instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.WHERE_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Set<IRI> possibleQueryTypes = new HashSet<>();
		Set<IRI> possibleTemplates = new HashSet<>();
		try (CloseableIteration<IRI, QueryEvaluationException> typeIter = TripleSources
				.getObjectURIs(queryResource, RDF.TYPE, store)) {
			while (typeIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Set<IRI> possibleTemplates = new HashSet<>();
		try (CloseableIteration<IRI, QueryEvaluationException> typeIter = TripleSources
				.getObjectURIs(queryResource, RDF.TYPE, store)) {
			while (typeIter.hasNext()) {
				IRI type = typeIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		try (CloseableIteration<IRI, QueryEvaluationException> typeIter = TripleSources
				.getObjectURIs(queryResource, RDF.TYPE, store)) {
			while (typeIter.hasNext()) {
				IRI type = typeIter.next();
				if (isQueryElseTemplate == null && SPIN.TEMPLATES_CLASS.equals(type)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				.getObjectURIs(queryResource, RDF.TYPE, store)) {
			while (typeIter.hasNext()) {
				IRI type = typeIter.next();
				if (isQueryElseTemplate == null && SPIN.TEMPLATES_CLASS.equals(type)) {
					isQueryElseTemplate = Boolean.FALSE;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				for (Iterator<IRI> iter = possibleTemplates.iterator(); iter.hasNext();) {
					IRI t = iter.next();
					boolean isAbstract = TripleSources.booleanValue(t, SPIN.ABSTRACT_PROPERTY, store);
					if (isAbstract) {
						abstractTemplates.add(t);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				for (Iterator<IRI> iter = possibleTemplates.iterator(); iter.hasNext();) {
					IRI t = iter.next();
					boolean isAbstract = TripleSources.booleanValue(t, SPIN.ABSTRACT_PROPERTY, store);
					if (isAbstract) {
						abstractTemplates.add(t);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			for (Argument arg : tmpl.getArguments()) {
				IRI argPred = (IRI) arg.getPredicate();
				Value argValue = TripleSources.singleValue(queryResource, argPred, store);
				argValues.put(argPred, argValue);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			for (Argument arg : tmpl.getArguments()) {
				IRI argPred = (IRI) arg.getPredicate();
				Value argValue = TripleSources.singleValue(queryResource, argPred, store);
				argValues.put(argPred, argValue);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private OrderElem visitOrderByCondition(Resource r) throws RDF4JException {
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			Statement descStmt = TripleSources.single(r, RDF.TYPE, SP.DESC_CLASS, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private OrderElem visitOrderByCondition(Resource r) throws RDF4JException {
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			Statement descStmt = TripleSources.single(r, RDF.TYPE, SP.DESC_CLASS, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			Statement descStmt = TripleSources.single(r, RDF.TYPE, SP.DESC_CLASS, store);
			boolean asc = (descStmt == null);
			return new OrderElem(valueExpr, asc);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			Statement descStmt = TripleSources.single(r, RDF.TYPE, SP.DESC_CLASS, store);
			boolean asc = (descStmt == null);
			return new OrderElem(valueExpr, asc);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitSelect(Resource select) throws RDF4JException {
			Value resultVars = TripleSources.singleValue(select, SP.RESULT_VARIABLES_PROPERTY, store);
			if (!(resultVars instanceof Resource)) {
				throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		public void visitSelect(Resource select) throws RDF4JException {
			Value resultVars = TripleSources.singleValue(select, SP.RESULT_VARIABLES_PROPERTY, store);
			if (!(resultVars instanceof Resource)) {
				throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			projection.setArg(whereExpr);

			Value groupBy = TripleSources.singleValue(select, SP.GROUP_BY_PROPERTY, store);
			if (groupBy instanceof Resource) {
				visitGroupBy((Resource) groupBy);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			projection.setArg(whereExpr);

			Value groupBy = TripleSources.singleValue(select, SP.GROUP_BY_PROPERTY, store);
			if (groupBy instanceof Resource) {
				visitGroupBy((Resource) groupBy);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			}

			Value having = TripleSources.singleValue(select, SP.HAVING_PROPERTY, store);
			if (having instanceof Resource) {
				TupleExpr havingExpr = visitHaving((Resource) having);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			}

			Value having = TripleSources.singleValue(select, SP.HAVING_PROPERTY, store);
			if (having instanceof Resource) {
				TupleExpr havingExpr = visitHaving((Resource) having);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			projElems = oldProjElems;

			Value orderby = TripleSources.singleValue(select, SP.ORDER_BY_PROPERTY, store);
			if (orderby instanceof Resource) {
				Order order = visitOrderBy((Resource) orderby);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			projElems = oldProjElems;

			Value orderby = TripleSources.singleValue(select, SP.ORDER_BY_PROPERTY, store);
			if (orderby instanceof Resource) {
				Order order = visitOrderBy((Resource) orderby);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			}

			boolean distinct = TripleSources.booleanValue(select, SP.DISTINCT_PROPERTY, store);
			if (distinct) {
				tupleRoot = new Distinct(tupleRoot);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			}

			boolean distinct = TripleSources.booleanValue(select, SP.DISTINCT_PROPERTY, store);
			if (distinct) {
				tupleRoot = new Distinct(tupleRoot);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			long offset = -1L;
			Value offsetValue = TripleSources.singleValue(select, SP.OFFSET_PROPERTY, store);
			if (offsetValue instanceof Literal) {
				offset = ((Literal) offsetValue).longValue();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

			long offset = -1L;
			Value offsetValue = TripleSources.singleValue(select, SP.OFFSET_PROPERTY, store);
			if (offsetValue instanceof Literal) {
				offset = ((Literal) offsetValue).longValue();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			}
			long limit = -1L;
			Value limitValue = TripleSources.singleValue(select, SP.LIMIT_PROPERTY, store);
			if (limitValue instanceof Literal) {
				limit = ((Literal) limitValue).longValue();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			}
			long limit = -1L;
			Value limitValue = TripleSources.singleValue(select, SP.LIMIT_PROPERTY, store);
			if (limitValue instanceof Literal) {
				limit = ((Literal) limitValue).longValue();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	public boolean isThisUnbound(Resource subj, TripleSource store) throws RDF4JException {
		return TripleSources.booleanValue(subj, SPIN.THIS_UNBOUND_PROPERTY, store);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	public boolean isThisUnbound(Resource subj, TripleSource store) throws RDF4JException {
		return TripleSources.booleanValue(subj, SPIN.THIS_UNBOUND_PROPERTY, store);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitBind(Resource r) throws RDF4JException {
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			Value varValue = TripleSources.singleValue(r, SP.VARIABLE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

		private void visitBind(Resource r) throws RDF4JException {
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			Value varValue = TripleSources.singleValue(r, SP.VARIABLE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			Value varValue = TripleSources.singleValue(r, SP.VARIABLE_PROPERTY, store);
			if (!(varValue instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.VARIABLE_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			Value expr = TripleSources.singleValue(r, SP.EXPRESSION_PROPERTY, store);
			ValueExpr valueExpr = visitExpression(expr);
			Value varValue = TripleSources.singleValue(r, SP.VARIABLE_PROPERTY, store);
			if (!(varValue instanceof Resource)) {
				throw new MalformedSpinException(String.format("Value of %s is not a resource", SP.VARIABLE_PROPERTY));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	private int getMaxIterationCount(Resource ruleProp, TripleSource store) throws RDF4JException {
		Value v = TripleSources.singleValue(ruleProp, SPIN.RULE_PROPERTY_MAX_ITERATION_COUNT_PROPERTY, store);
		if (v == null) {
			return -1;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	private int getMaxIterationCount(Resource ruleProp, TripleSource store) throws RDF4JException {
		Value v = TripleSources.singleValue(ruleProp, SPIN.RULE_PROPERTY_MAX_ITERATION_COUNT_PROPERTY, store);
		if (v == null) {
			return -1;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private void visitPattern(Resource r, Set<IRI> types, TupleExpr currentGroupExpr) throws RDF4JException {
			TupleExpr currentNode = tupleNode;
			Value pred = TripleSources.singleValue(r, SP.PREDICATE_PROPERTY, store);
			if (pred != null) {
				// only triple patterns have sp:predicate
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		private void visitPattern(Resource r, Set<IRI> types, TupleExpr currentGroupExpr) throws RDF4JException {
			TupleExpr currentNode = tupleNode;
			Value pred = TripleSources.singleValue(r, SP.PREDICATE_PROPERTY, store);
			if (pred != null) {
				// only triple patterns have sp:predicate
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			if (pred != null) {
				// only triple patterns have sp:predicate
				Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
				Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
				Scope stmtScope = (namedGraph != null) ? Scope.NAMED_CONTEXTS : Scope.DEFAULT_CONTEXTS;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			if (pred != null) {
				// only triple patterns have sp:predicate
				Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
				Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
				Scope stmtScope = (namedGraph != null) ? Scope.NAMED_CONTEXTS : Scope.DEFAULT_CONTEXTS;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				// only triple patterns have sp:predicate
				Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
				Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
				Scope stmtScope = (namedGraph != null) ? Scope.NAMED_CONTEXTS : Scope.DEFAULT_CONTEXTS;
				tupleNode = new StatementPattern(stmtScope, getVar(subj), getVar(pred), getVar(obj), namedGraph);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				// only triple patterns have sp:predicate
				Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
				Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
				Scope stmtScope = (namedGraph != null) ? Scope.NAMED_CONTEXTS : Scope.DEFAULT_CONTEXTS;
				tupleNode = new StatementPattern(stmtScope, getVar(subj), getVar(pred), getVar(obj), namedGraph);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				if (types.contains(SP.NAMED_GRAPH_CLASS)) {
					Var oldGraph = namedGraph;
					Value graphValue = TripleSources.singleValue(r, SP.GRAPH_NAME_NODE_PROPERTY, store);
					namedGraph = getVar(graphValue);
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				if (types.contains(SP.NAMED_GRAPH_CLASS)) {
					Var oldGraph = namedGraph;
					Value graphValue = TripleSources.singleValue(r, SP.GRAPH_NAME_NODE_PROPERTY, store);
					namedGraph = getVar(graphValue);
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value graphValue = TripleSources.singleValue(r, SP.GRAPH_NAME_NODE_PROPERTY, store);
					namedGraph = getVar(graphValue);
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value graphValue = TripleSources.singleValue(r, SP.GRAPH_NAME_NODE_PROPERTY, store);
					namedGraph = getVar(graphValue);
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					namedGraph = oldGraph;
				} else if (types.contains(SP.UNION_CLASS)) {
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					namedGraph = oldGraph;
				} else if (types.contains(SP.UNION_CLASS)) {
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					}

					Iteration<Resource, QueryEvaluationException> iter = TripleSources
							.listResources((Resource) elements, store);
					TupleExpr prev = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					}

					Iteration<Resource, QueryEvaluationException> iter = TripleSources
							.listResources((Resource) elements, store);
					TupleExpr prev = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

					Iteration<Resource, QueryEvaluationException> iter = TripleSources
							.listResources((Resource) elements, store);
					TupleExpr prev = null;
					while (iter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
							.listResources((Resource) elements, store);
					TupleExpr prev = null;
					while (iter.hasNext()) {
						Resource entry = iter.next();
						TupleExpr groupExpr = visitGroupGraphPattern(entry);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					TupleExpr prev = null;
					while (iter.hasNext()) {
						Resource entry = iter.next();
						TupleExpr groupExpr = visitGroupGraphPattern(entry);
						if (prev != null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					}
				} else if (types.contains(SP.OPTIONAL_CLASS)) {
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					}
				} else if (types.contains(SP.OPTIONAL_CLASS)) {
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					currentNode = null;
				} else if (types.contains(SP.MINUS_CLASS)) {
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					currentNode = null;
				} else if (types.contains(SP.MINUS_CLASS)) {
					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					currentNode = null;
				} else if (types.contains(SP.SUB_QUERY_CLASS)) {
					Value q = TripleSources.singleValue(r, SP.QUERY_PROPERTY, store);
					TupleExpr oldRoot = tupleRoot;
					visitSelect((Resource) q);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					currentNode = null;
				} else if (types.contains(SP.SUB_QUERY_CLASS)) {
					Value q = TripleSources.singleValue(r, SP.QUERY_PROPERTY, store);
					TupleExpr oldRoot = tupleRoot;
					visitSelect((Resource) q);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					BindingSetAssignment bsa = new BindingSetAssignment();
					Set<String> varNames = new LinkedHashSet<>();
					Value varNameList = TripleSources.singleValue(r, SP.VAR_NAMES_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> varNameIter = TripleSources
							.list((Resource) varNameList, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					BindingSetAssignment bsa = new BindingSetAssignment();
					Set<String> varNames = new LinkedHashSet<>();
					Value varNameList = TripleSources.singleValue(r, SP.VAR_NAMES_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> varNameIter = TripleSources
							.list((Resource) varNameList, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Set<String> varNames = new LinkedHashSet<>();
					Value varNameList = TripleSources.singleValue(r, SP.VAR_NAMES_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> varNameIter = TripleSources
							.list((Resource) varNameList, store);
					while (varNameIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Set<String> varNames = new LinkedHashSet<>();
					Value varNameList = TripleSources.singleValue(r, SP.VAR_NAMES_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> varNameIter = TripleSources
							.list((Resource) varNameList, store);
					while (varNameIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value varNameList = TripleSources.singleValue(r, SP.VAR_NAMES_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> varNameIter = TripleSources
							.list((Resource) varNameList, store);
					while (varNameIter.hasNext()) {
						Value v = varNameIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Iteration<Value, QueryEvaluationException> varNameIter = TripleSources
							.list((Resource) varNameList, store);
					while (varNameIter.hasNext()) {
						Value v = varNameIter.next();
						if (v instanceof Literal) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
							.list((Resource) varNameList, store);
					while (varNameIter.hasNext()) {
						Value v = varNameIter.next();
						if (v instanceof Literal) {
							varNames.add(((Literal) v).getLabel());
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					bsa.setBindingNames(varNames);
					List<BindingSet> bindingSets = new ArrayList<>();
					Value bindingsList = TripleSources.singleValue(r, SP.BINDINGS_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> bindingsIter = TripleSources
							.list((Resource) bindingsList, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					bsa.setBindingNames(varNames);
					List<BindingSet> bindingSets = new ArrayList<>();
					Value bindingsList = TripleSources.singleValue(r, SP.BINDINGS_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> bindingsIter = TripleSources
							.list((Resource) bindingsList, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					List<BindingSet> bindingSets = new ArrayList<>();
					Value bindingsList = TripleSources.singleValue(r, SP.BINDINGS_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> bindingsIter = TripleSources
							.list((Resource) bindingsList, store);
					while (bindingsIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					List<BindingSet> bindingSets = new ArrayList<>();
					Value bindingsList = TripleSources.singleValue(r, SP.BINDINGS_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> bindingsIter = TripleSources
							.list((Resource) bindingsList, store);
					while (bindingsIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value bindingsList = TripleSources.singleValue(r, SP.BINDINGS_PROPERTY, store);
					Iteration<Value, QueryEvaluationException> bindingsIter = TripleSources
							.list((Resource) bindingsList, store);
					while (bindingsIter.hasNext()) {
						Value valueList = bindingsIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Iteration<Value, QueryEvaluationException> bindingsIter = TripleSources
							.list((Resource) bindingsList, store);
					while (bindingsIter.hasNext()) {
						Value valueList = bindingsIter.next();
						QueryBindingSet bs = new QueryBindingSet();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
							.list((Resource) bindingsList, store);
					while (bindingsIter.hasNext()) {
						Value valueList = bindingsIter.next();
						QueryBindingSet bs = new QueryBindingSet();
						Iterator<String> nameIter = varNames.iterator();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
						QueryBindingSet bs = new QueryBindingSet();
						Iterator<String> nameIter = varNames.iterator();
						Iteration<Value, QueryEvaluationException> valueIter = TripleSources
								.list((Resource) valueList, store);
						while (nameIter.hasNext() && valueIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
						QueryBindingSet bs = new QueryBindingSet();
						Iterator<String> nameIter = varNames.iterator();
						Iteration<Value, QueryEvaluationException> valueIter = TripleSources
								.list((Resource) valueList, store);
						while (nameIter.hasNext() && valueIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
						Iterator<String> nameIter = varNames.iterator();
						Iteration<Value, QueryEvaluationException> valueIter = TripleSources
								.list((Resource) valueList, store);
						while (nameIter.hasNext() && valueIter.hasNext()) {
							String name = nameIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
						Iteration<Value, QueryEvaluationException> valueIter = TripleSources
								.list((Resource) valueList, store);
						while (nameIter.hasNext() && valueIter.hasNext()) {
							String name = nameIter.next();
							Value value = valueIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
						while (nameIter.hasNext() && valueIter.hasNext()) {
							String name = nameIter.next();
							Value value = valueIter.next();
							if (!SP.UNDEF.equals(value)) {
								bs.addBinding(name, value);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					bsa.setBindingSets(bindingSets);
					tupleNode = bsa;
				} else if (types.contains(RDF.LIST) || (TripleSources.singleValue(r, RDF.FIRST, store) != null)) {
					tupleNode = visitGroupGraphPattern(r);
				} else if (types.contains(SP.TRIPLE_PATH_CLASS)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					bsa.setBindingSets(bindingSets);
					tupleNode = bsa;
				} else if (types.contains(RDF.LIST) || (TripleSources.singleValue(r, RDF.FIRST, store) != null)) {
					tupleNode = visitGroupGraphPattern(r);
				} else if (types.contains(SP.TRIPLE_PATH_CLASS)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					tupleNode = visitGroupGraphPattern(r);
				} else if (types.contains(SP.TRIPLE_PATH_CLASS)) {
					Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
					Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
					Resource path = (Resource) TripleSources.singleValue(r, SP.PATH_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					tupleNode = visitGroupGraphPattern(r);
				} else if (types.contains(SP.TRIPLE_PATH_CLASS)) {
					Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
					Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
					Resource path = (Resource) TripleSources.singleValue(r, SP.PATH_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				} else if (types.contains(SP.TRIPLE_PATH_CLASS)) {
					Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
					Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
					Resource path = (Resource) TripleSources.singleValue(r, SP.PATH_PROPERTY, store);
					Set<IRI> pathTypes = Iterations.asSet(TripleSources.getObjectURIs(path, RDF.TYPE, store));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				} else if (types.contains(SP.TRIPLE_PATH_CLASS)) {
					Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
					Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
					Resource path = (Resource) TripleSources.singleValue(r, SP.PATH_PROPERTY, store);
					Set<IRI> pathTypes = Iterations.asSet(TripleSources.getObjectURIs(path, RDF.TYPE, store));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
					Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
					Resource path = (Resource) TripleSources.singleValue(r, SP.PATH_PROPERTY, store);
					Set<IRI> pathTypes = Iterations.asSet(TripleSources.getObjectURIs(path, RDF.TYPE, store));
					if (pathTypes.contains(SP.MOD_PATH_CLASS)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value subj = TripleSources.singleValue(r, SP.SUBJECT_PROPERTY, store);
					Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
					Resource path = (Resource) TripleSources.singleValue(r, SP.PATH_PROPERTY, store);
					Set<IRI> pathTypes = Iterations.asSet(TripleSources.getObjectURIs(path, RDF.TYPE, store));
					if (pathTypes.contains(SP.MOD_PATH_CLASS)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
					Resource path = (Resource) TripleSources.singleValue(r, SP.PATH_PROPERTY, store);
					Set<IRI> pathTypes = Iterations.asSet(TripleSources.getObjectURIs(path, RDF.TYPE, store));
					if (pathTypes.contains(SP.MOD_PATH_CLASS)) {
						Resource subPath = (Resource) TripleSources.singleValue(path, SP.SUB_PATH_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value obj = TripleSources.singleValue(r, SP.OBJECT_PROPERTY, store);
					Resource path = (Resource) TripleSources.singleValue(r, SP.PATH_PROPERTY, store);
					Set<IRI> pathTypes = Iterations.asSet(TripleSources.getObjectURIs(path, RDF.TYPE, store));
					if (pathTypes.contains(SP.MOD_PATH_CLASS)) {
						Resource subPath = (Resource) TripleSources.singleValue(path, SP.SUB_PATH_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Set<IRI> pathTypes = Iterations.asSet(TripleSources.getObjectURIs(path, RDF.TYPE, store));
					if (pathTypes.contains(SP.MOD_PATH_CLASS)) {
						Resource subPath = (Resource) TripleSources.singleValue(path, SP.SUB_PATH_PROPERTY, store);
						Literal minPath = (Literal) TripleSources.singleValue(path, SP.MOD_MIN_PROPERTY, store);
						Literal maxPath = (Literal) TripleSources.singleValue(path, SP.MOD_MAX_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Set<IRI> pathTypes = Iterations.asSet(TripleSources.getObjectURIs(path, RDF.TYPE, store));
					if (pathTypes.contains(SP.MOD_PATH_CLASS)) {
						Resource subPath = (Resource) TripleSources.singleValue(path, SP.SUB_PATH_PROPERTY, store);
						Literal minPath = (Literal) TripleSources.singleValue(path, SP.MOD_MIN_PROPERTY, store);
						Literal maxPath = (Literal) TripleSources.singleValue(path, SP.MOD_MAX_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					if (pathTypes.contains(SP.MOD_PATH_CLASS)) {
						Resource subPath = (Resource) TripleSources.singleValue(path, SP.SUB_PATH_PROPERTY, store);
						Literal minPath = (Literal) TripleSources.singleValue(path, SP.MOD_MIN_PROPERTY, store);
						Literal maxPath = (Literal) TripleSources.singleValue(path, SP.MOD_MAX_PROPERTY, store);
						if (maxPath == null || maxPath.intValue() != -2) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					if (pathTypes.contains(SP.MOD_PATH_CLASS)) {
						Resource subPath = (Resource) TripleSources.singleValue(path, SP.SUB_PATH_PROPERTY, store);
						Literal minPath = (Literal) TripleSources.singleValue(path, SP.MOD_MIN_PROPERTY, store);
						Literal maxPath = (Literal) TripleSources.singleValue(path, SP.MOD_MAX_PROPERTY, store);
						if (maxPath == null || maxPath.intValue() != -2) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
						Resource subPath = (Resource) TripleSources.singleValue(path, SP.SUB_PATH_PROPERTY, store);
						Literal minPath = (Literal) TripleSources.singleValue(path, SP.MOD_MIN_PROPERTY, store);
						Literal maxPath = (Literal) TripleSources.singleValue(path, SP.MOD_MAX_PROPERTY, store);
						if (maxPath == null || maxPath.intValue() != -2) {
							throw new UnsupportedOperationException("Unsupported mod path");
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
						Resource subPath = (Resource) TripleSources.singleValue(path, SP.SUB_PATH_PROPERTY, store);
						Literal minPath = (Literal) TripleSources.singleValue(path, SP.MOD_MIN_PROPERTY, store);
						Literal maxPath = (Literal) TripleSources.singleValue(path, SP.MOD_MAX_PROPERTY, store);
						if (maxPath == null || maxPath.intValue() != -2) {
							throw new UnsupportedOperationException("Unsupported mod path");
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					}
				} else if (types.contains(SP.SERVICE_CLASS)) {
					Value serviceUri = TripleSources.singleValue(r, SP.SERVICE_URI_PROPERTY, store);

					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					}
				} else if (types.contains(SP.SERVICE_CLASS)) {
					Value serviceUri = TripleSources.singleValue(r, SP.SERVICE_URI_PROPERTY, store);

					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value serviceUri = TripleSources.singleValue(r, SP.SERVICE_URI_PROPERTY, store);

					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					Value serviceUri = TripleSources.singleValue(r, SP.SERVICE_URI_PROPERTY, store);

					Value elements = TripleSources.singleValue(r, SP.ELEMENTS_PROPERTY, store);
					if (!(elements instanceof Resource)) {
						throw new MalformedSpinException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					TupleExpr groupExpr = visitGroupGraphPattern((Resource) elements);

					boolean isSilent = TripleSources.booleanValue(r, SP.SILENT_PROPERTY, store);
					String exprString;
					try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					TupleExpr groupExpr = visitGroupGraphPattern((Resource) elements);

					boolean isSilent = TripleSources.booleanValue(r, SP.SILENT_PROPERTY, store);
					String exprString;
					try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	public ConstraintViolation parseConstraintViolation(Resource subj, TripleSource store) throws RDF4JException {
		Value labelValue = TripleSources.singleValue(subj, RDFS.LABEL, store);
		Value rootValue = TripleSources.singleValue(subj, SPIN.VIOLATION_ROOT_PROPERTY, store);
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java

	public ConstraintViolation parseConstraintViolation(Resource subj, TripleSource store) throws RDF4JException {
		Value labelValue = TripleSources.singleValue(subj, RDFS.LABEL, store);
		Value rootValue = TripleSources.singleValue(subj, SPIN.VIOLATION_ROOT_PROPERTY, store);
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	public ConstraintViolation parseConstraintViolation(Resource subj, TripleSource store) throws RDF4JException {
		Value labelValue = TripleSources.singleValue(subj, RDFS.LABEL, store);
		Value rootValue = TripleSources.singleValue(subj, SPIN.VIOLATION_ROOT_PROPERTY, store);
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
		Value valueValue = TripleSources.singleValue(subj, SPIN.VIOLATION_VALUE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	public ConstraintViolation parseConstraintViolation(Resource subj, TripleSource store) throws RDF4JException {
		Value labelValue = TripleSources.singleValue(subj, RDFS.LABEL, store);
		Value rootValue = TripleSources.singleValue(subj, SPIN.VIOLATION_ROOT_PROPERTY, store);
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
		Value valueValue = TripleSources.singleValue(subj, SPIN.VIOLATION_VALUE_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Value labelValue = TripleSources.singleValue(subj, RDFS.LABEL, store);
		Value rootValue = TripleSources.singleValue(subj, SPIN.VIOLATION_ROOT_PROPERTY, store);
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
		Value valueValue = TripleSources.singleValue(subj, SPIN.VIOLATION_VALUE_PROPERTY, store);
		Value levelValue = TripleSources.singleValue(subj, SPIN.VIOLATION_LEVEL_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Value labelValue = TripleSources.singleValue(subj, RDFS.LABEL, store);
		Value rootValue = TripleSources.singleValue(subj, SPIN.VIOLATION_ROOT_PROPERTY, store);
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
		Value valueValue = TripleSources.singleValue(subj, SPIN.VIOLATION_VALUE_PROPERTY, store);
		Value levelValue = TripleSources.singleValue(subj, SPIN.VIOLATION_LEVEL_PROPERTY, store);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Value rootValue = TripleSources.singleValue(subj, SPIN.VIOLATION_ROOT_PROPERTY, store);
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
		Value valueValue = TripleSources.singleValue(subj, SPIN.VIOLATION_VALUE_PROPERTY, store);
		Value levelValue = TripleSources.singleValue(subj, SPIN.VIOLATION_LEVEL_PROPERTY, store);
		String label = (labelValue instanceof Literal) ? labelValue.stringValue() : null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Value rootValue = TripleSources.singleValue(subj, SPIN.VIOLATION_ROOT_PROPERTY, store);
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
		Value valueValue = TripleSources.singleValue(subj, SPIN.VIOLATION_VALUE_PROPERTY, store);
		Value levelValue = TripleSources.singleValue(subj, SPIN.VIOLATION_LEVEL_PROPERTY, store);
		String label = (labelValue instanceof Literal) ? labelValue.stringValue() : null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
		Value valueValue = TripleSources.singleValue(subj, SPIN.VIOLATION_VALUE_PROPERTY, store);
		Value levelValue = TripleSources.singleValue(subj, SPIN.VIOLATION_LEVEL_PROPERTY, store);
		String label = (labelValue instanceof Literal) ? labelValue.stringValue() : null;
		String root = (rootValue instanceof Resource) ? rootValue.stringValue() : null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		Value pathValue = TripleSources.singleValue(subj, SPIN.VIOLATION_PATH_PROPERTY, store);
		Value valueValue = TripleSources.singleValue(subj, SPIN.VIOLATION_VALUE_PROPERTY, store);
		Value levelValue = TripleSources.singleValue(subj, SPIN.VIOLATION_LEVEL_PROPERTY, store);
		String label = (labelValue instanceof Literal) ? labelValue.stringValue() : null;
		String root = (rootValue instanceof Resource) ? rootValue.stringValue() : null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionalRepositoryConnection.java`
#### Snippet
```java
	@Override
	public <E extends Exception> void remove(
			Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		throwExceptionIfReadonly();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionalRepositoryConnection.java`
#### Snippet
```java
	@Override
	public <E extends Exception> void add(
			Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		throwExceptionIfReadonly();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionalRepositoryConnection.java`
#### Snippet
```java
import java.net.URL;

import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.model.IRI;
import org.eclipse.rdf4j.model.Resource;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/CachingRepositoryConnection.java`
#### Snippet
```java
	@Override
	public <E extends Exception> void remove(
			Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		super.remove(statementIter, contexts);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/CachingRepositoryConnection.java`
#### Snippet
```java
	@Override
	public <E extends Exception> void add(
			Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		super.add(statementIter, contexts);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/CachingRepositoryConnection.java`
#### Snippet
```java
import java.net.URL;

import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.model.IRI;
import org.eclipse.rdf4j.model.Resource;
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.repository.RepositoryConnection'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/AbstractComplianceTest.java`
#### Snippet
```java
		@Override
		@Deprecated(since = "4.1.0", forRemoval = true)
		public <E extends Exception> void add(Iteration<? extends Statement, E> statements, Resource... contexts)
				throws RepositoryException, E {
			delegate.add(statements, contexts);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/AbstractComplianceTest.java`
#### Snippet
```java
		@Override
		@Deprecated(since = "4.1.0", forRemoval = true)
		public <E extends Exception> void add(Iteration<? extends Statement, E> statements, Resource... contexts)
				throws RepositoryException, E {
			delegate.add(statements, contexts);
```

### MarkedForRemoval
'add(org.eclipse.rdf4j.common.iteration.Iteration, org.eclipse.rdf4j.model.Resource...)' is deprecated and marked for removal
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/AbstractComplianceTest.java`
#### Snippet
```java
		public <E extends Exception> void add(Iteration<? extends Statement, E> statements, Resource... contexts)
				throws RepositoryException, E {
			delegate.add(statements, contexts);
		}

```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.repository.RepositoryConnection'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/AbstractComplianceTest.java`
#### Snippet
```java
		@Override
		@Deprecated(since = "4.1.0", forRemoval = true)
		public <E extends Exception> void remove(Iteration<? extends Statement, E> statements, Resource... contexts)
				throws RepositoryException, E {
			delegate.remove(statements, contexts);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/AbstractComplianceTest.java`
#### Snippet
```java
		@Override
		@Deprecated(since = "4.1.0", forRemoval = true)
		public <E extends Exception> void remove(Iteration<? extends Statement, E> statements, Resource... contexts)
				throws RepositoryException, E {
			delegate.remove(statements, contexts);
```

### MarkedForRemoval
'remove(org.eclipse.rdf4j.common.iteration.Iteration, org.eclipse.rdf4j.model.Resource...)' is deprecated and marked for removal
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/AbstractComplianceTest.java`
#### Snippet
```java
		public <E extends Exception> void remove(Iteration<? extends Statement, E> statements, Resource... contexts)
				throws RepositoryException, E {
			delegate.remove(statements, contexts);
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/AbstractComplianceTest.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.transaction.IsolationLevel;
import org.eclipse.rdf4j.common.transaction.TransactionSetting;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/LoggingRepositoryConnection.java`
#### Snippet
```java
import java.net.URL;

import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.model.IRI;
import org.eclipse.rdf4j.model.Resource;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/LoggingRepositoryConnection.java`
#### Snippet
```java
	@Override
	public <E extends Exception> void add(
			Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		operationLog.runWithLog(
```

### MarkedForRemoval
'add(org.eclipse.rdf4j.common.iteration.Iteration, org.eclipse.rdf4j.model.Resource...)' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/LoggingRepositoryConnection.java`
#### Snippet
```java
		operationLog.runWithLog(
				PseudoOperation.forAdd(statementIter, contexts),
				wrapInRuntimeException(() -> getDelegate().add(statementIter, contexts)));
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/LoggingRepositoryConnection.java`
#### Snippet
```java
	@Override
	public <E extends Exception> void remove(
			Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		operationLog.runWithLog(
```

### MarkedForRemoval
'remove(org.eclipse.rdf4j.common.iteration.Iteration, org.eclipse.rdf4j.model.Resource...)' is deprecated and marked for removal
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/LoggingRepositoryConnection.java`
#### Snippet
```java
		operationLog.runWithLog(
				PseudoOperation.forRemove(statementIter, contexts),
				wrapInRuntimeException(() -> getDelegate().remove(statementIter, contexts)));
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/FileIO.java`
#### Snippet
```java
	private void writeNamespaces(SailDataset store, DataOutputStream dataOut) throws IOException, SailException {
		try (CloseableIteration<? extends Namespace, SailException> iter = store.getNamespaces()) {
			while (iter.hasNext()) {
				Namespace ns = iter.next();
				dataOut.writeByte(NAMESPACE_MARKER);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/FileIO.java`
#### Snippet
```java
		try (CloseableIteration<? extends Namespace, SailException> iter = store.getNamespaces()) {
			while (iter.hasNext()) {
				Namespace ns = iter.next();
				dataOut.writeByte(NAMESPACE_MARKER);
				writeString(ns.getPrefix(), dataOut);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/FileIO.java`
#### Snippet
```java
			int quadMarker, DataOutputStream dataOut) throws IOException, SailException {
		try (stIter) {
			while (stIter.hasNext()) {
				Statement st = stIter.next();
				Resource context = st.getContext();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/FileIO.java`
#### Snippet
```java
		try (stIter) {
			while (stIter.hasNext()) {
				Statement st = stIter.next();
				Resource context = st.getContext();
				if (context == null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
					try (CloseableIteration<MemStatement, SailException> iter = createStatementIterator(subj, pred, obj,
							null, -1, contexts)) {
						while (iter.hasNext()) {
							MemStatement st = iter.next();
							int since = st.getSinceSnapshot();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
							null, -1, contexts)) {
						while (iter.hasNext()) {
							MemStatement st = iter.next();
							int since = st.getSinceSnapshot();
							int till = st.getTillSnapshot();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
						statement.getSubject(), statement.getPredicate(), statement.getObject(), explicit, nextSnapshot,
						statement.getContext())) {
					while (iter.hasNext()) {
						MemStatement st = iter.next();
						st.setTillSnapshot(nextSnapshot);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
						statement.getContext())) {
					while (iter.hasNext()) {
						MemStatement st = iter.next();
						st.setTillSnapshot(nextSnapshot);
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
			try (CloseableIteration<MemStatement, SailException> iter = createStatementIterator(subj, pred, obj,
					explicit, nextSnapshot, contexts)) {
				while (iter.hasNext()) {
					deprecated = true;
					MemStatement st = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
				while (iter.hasNext()) {
					deprecated = true;
					MemStatement st = iter.next();
					st.setTillSnapshot(nextSnapshot);
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
			try (CloseableIteration<MemStatement, SailException> iter = createStatementIterator(null, null, null,
					explicit, nextSnapshot, contexts)) {
				while (iter.hasNext()) {
					MemStatement st = iter.next();
					st.setTillSnapshot(nextSnapshot);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
					explicit, nextSnapshot, contexts)) {
				while (iter.hasNext()) {
					MemStatement st = iter.next();
					st.setTillSnapshot(nextSnapshot);
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.SimpleStatement' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Statement createStatement(Resource subject, IRI predicate, Value object) {
		return new SimpleStatement(subject, predicate, object);
	}

```

### MarkedForRemoval
'DecimalLiteral(java.math.BigDecimal, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype)' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	protected Literal createNumericLiteral(Number number, XSD.Datatype datatype) {
		if (number instanceof BigDecimal) {
			return new DecimalLiteral((BigDecimal) number, datatype);
		}
		if (number instanceof BigInteger) {
```

### MarkedForRemoval
'IntegerLiteral(java.math.BigInteger, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype)' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
		}
		if (number instanceof BigInteger) {
			return new IntegerLiteral((BigInteger) number, datatype);
		}
		return new NumericLiteral(number, datatype);
```

### MarkedForRemoval
'NumericLiteral(java.lang.Number, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype)' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
			return new IntegerLiteral((BigInteger) number, datatype);
		}
		return new NumericLiteral(number, datatype);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.ContextStatement' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Statement createStatement(Resource subject, IRI predicate, Value object, Resource context) {
		return new ContextStatement(subject, predicate, object, context);
	}

```

### MarkedForRemoval
'SimpleLiteral(java.lang.String, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype)' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DecimalLiteral.java`
#### Snippet
```java
	@Deprecated(since = "4.0.0", forRemoval = true)
	protected DecimalLiteral(BigDecimal value, XSD.Datatype datatype) {
		super(value.toPlainString(), datatype);
		this.value = value;
	}
```

### MarkedForRemoval
'SimpleLiteral(java.lang.String, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype)' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/IntegerLiteral.java`
#### Snippet
```java
	protected IntegerLiteral(BigInteger value, XSD.Datatype datatype) {
		// TODO: maybe IntegerLiteralImpl should not extend LiteralImpl?
		super(value.toString(), datatype);
		this.value = value;
	}
```

### MarkedForRemoval
'getXsdDatatype(org.eclipse.rdf4j.model.Literal)' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/LexicalValueComparator.java`
#### Snippet
```java
			if (rightDatatype != null) {
				// Both literals have datatypes
				Optional<XSD.Datatype> leftXmlDatatype = Literals.getXsdDatatype(leftLit);
				Optional<XSD.Datatype> rightXmlDatatype = Literals.getXsdDatatype(rightLit);

```

### MarkedForRemoval
'getXsdDatatype(org.eclipse.rdf4j.model.Literal)' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/LexicalValueComparator.java`
#### Snippet
```java
				// Both literals have datatypes
				Optional<XSD.Datatype> leftXmlDatatype = Literals.getXsdDatatype(leftLit);
				Optional<XSD.Datatype> rightXmlDatatype = Literals.getXsdDatatype(rightLit);

				if (leftXmlDatatype.isPresent() && rightXmlDatatype.isPresent()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.ContextStatement' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
			Value obj = st.getObject();
			Resource ctx = st.getContext();
			s.writeObject(new ContextStatement(subj, pred, obj, ctx));
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.ContextStatement' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
	}

	public static class ModelStatement extends ContextStatement {

		private static final long serialVersionUID = 2200404772364346279L;
```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.ContextStatement' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
	}

	static class TreeStatement extends ContextStatement {

		private static final long serialVersionUID = -7720419322256724495L;
```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.SimpleStatement' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/ContextStatement.java`
#### Snippet
```java

/**
 * An extension of {@link SimpleStatement} that adds a context field.
 *
 * @deprecated Use {@link GenericStatement instead}
```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.SimpleStatement' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/ContextStatement.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0", forRemoval = true)
public class ContextStatement extends SimpleStatement {

	/*-----------*
```

### MarkedForRemoval
'getXsdDatatype()' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
	public static Optional<XSD.Datatype> getXsdDatatype(Literal l) {
		if (l instanceof SimpleLiteral) {
			return ((SimpleLiteral) l).getXsdDatatype();
		}
		return Optional.empty();
```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.ContextStatement' is deprecated and marked for removal
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleStatement.java`
#### Snippet
```java
/**
 * A simple default implementation of the {@link Statement} interface for statements that don't have an associated
 * context. For statements that do have an associated context, {@link ContextStatement} can be used.
 *
 * @see org.eclipse.rdf4j.model.impl.SimpleValueFactory
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/CloseableIterationIterator.java`
#### Snippet
```java
	@Override
	public E next() {
		return iteration.next();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/CloseableIterationIterator.java`
#### Snippet
```java
			@Override
			public void remove() throws RuntimeException {
				iteration.remove();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/CloseableIterationIterator.java`
#### Snippet
```java
			@Override
			public E next() throws RuntimeException {
				return iteration.next();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/CloseableIterationIterator.java`
#### Snippet
```java
	@Override
	public void remove() {
		iteration.remove();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/CloseableIterationIterator.java`
#### Snippet
```java
			@Override
			public boolean hasNext() throws RuntimeException {
				return iteration.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/CloseableIterationIterator.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;

/**
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/CloseableIterationIterator.java`
#### Snippet
```java
	@Override
	public boolean hasNext() {
		boolean hasMore = iteration.hasNext();
		if (!hasMore) {
			try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/CloseableIterationIterator.java`
#### Snippet
```java

	@Deprecated(since = "4.1.0", forRemoval = true)
	public CloseableIterationIterator(Iteration<? extends E, ? extends RuntimeException> iteration) {
		this.iteration = new CloseableIteration<>() {
			@Override
```

### MarkedForRemoval
'stream(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iteration.java`
#### Snippet
```java
	 */
	default Stream<E> stream() {
		return Iterations.stream(this);
	}
}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ReducedIteration.java`
#### Snippet
```java
	private E previousObject;

	public ReducedIteration(Iteration<? extends E, ? extends X> delegate) {
		super(delegate);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IteratorIteration.java`
#### Snippet
```java

/**
 * An Iteration that can convert an {@link Iterator} to a {@link Iteration}.
 */
@Deprecated(since = "4.1.0")
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IteratorIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class IteratorIteration<E, X extends Exception> implements Iteration<E, X> {

	private final Iterator<? extends E> iter;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/TimeLimitIteration.java`
#### Snippet
```java
	private final AtomicBoolean isInterrupted = new AtomicBoolean(false);

	protected TimeLimitIteration(Iteration<? extends E, ? extends X> iter, long timeLimit) {
		super(iter);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
	 * needed.
	 */
	protected abstract Iteration<? extends E, ? extends X> createIteration() throws X;

	/**
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			throw new NoSuchElementException("Iteration has been closed");
		}
		Iteration<? extends E, ? extends X> resultIter = iter;
		if (resultIter == null) {
			// Underlying iterator has not yet been initialized
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
		}

		return resultIter.next();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			return false;
		}
		Iteration<? extends E, ? extends X> resultIter = iter;
		if (resultIter == null) {
			// Underlying iterator has not yet been initialized
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
		}

		return resultIter.hasNext();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			super.handleClose();
		} finally {
			Iteration<? extends E, ? extends X> toClose = iter;
			if (toClose != null) {
				Iterations.closeCloseable(toClose);
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			Iteration<? extends E, ? extends X> toClose = iter;
			if (toClose != null) {
				Iterations.closeCloseable(toClose);
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
	 *-----------*/

	private Iteration<? extends E, ? extends X> iter;

	/*--------------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			throw new IllegalStateException("The iteration has been closed.");
		}
		Iteration<? extends E, ? extends X> resultIter = iter;
		if (resultIter == null) {
			throw new IllegalStateException("Underlying iteration was null");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
		}

		resultIter.remove();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DistinctIteration.java`
#### Snippet
```java
	 * @param iter The underlying iterator.
	 */
	public DistinctIteration(Iteration<? extends E, ? extends X> iter) {
		super(iter);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DistinctIteration.java`
#### Snippet
```java
	}

	public DistinctIteration(Iteration<? extends E, ? extends X> iter, Supplier<Set<E>> setMaker) {
		super(iter);
		excludeSet = setMaker.get();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationWrapper.java`
#### Snippet
```java
		}
		try {
			return wrappedIter.next();
		} catch (NoSuchElementException e) {
			close();
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationWrapper.java`
#### Snippet
```java
			super.handleClose();
		} finally {
			Iterations.closeCloseable(wrappedIter);
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationWrapper.java`
#### Snippet
```java
	 */
	@Deprecated
	protected final Iteration<? extends E, ? extends X> wrappedIter;

	/*--------------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationWrapper.java`
#### Snippet
```java
		}
		try {
			wrappedIter.remove();
		} catch (IllegalStateException e) {
			close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationWrapper.java`
#### Snippet
```java
			return false;
		}
		boolean result = wrappedIter.hasNext();
		if (!result) {
			close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationWrapper.java`
#### Snippet
```java
	 * @param iter The wrapped Iteration for this <var>IterationWrapper</var>, must not be <var>null</var>.
	 */
	protected IterationWrapper(Iteration<? extends E, ? extends X> iter) {
		assert iter != null;
		wrappedIter = iter;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/FilterIteration.java`
#### Snippet
```java
	 * @param iter
	 */
	protected FilterIteration(Iteration<? extends E, ? extends X> iter) {
		super(iter);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
	 */
	@Override
	public <E extends Exception> void remove(Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		final IRI[] removeContexts = getRemoveContexts();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
import org.eclipse.rdf4j.common.iteration.CloseableIteratorIteration;
import org.eclipse.rdf4j.common.iteration.ConvertingIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.model.IRI;
import org.eclipse.rdf4j.model.Resource;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void add(Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		final IRI insertContext = getInsertContext();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
		boolean needsToBeClosed = true;
		try {
			if (iteration.hasNext()) {
				action.accept(iteration.next());
				// since the iteration might have more elements we don't need to close it
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
		try {
			if (iteration.hasNext()) {
				action.accept(iteration.next());
				// since the iteration might have more elements we don't need to close it
				needsToBeClosed = false;
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
			if (needsToBeClosed) {
				try {
					Iterations.closeCloseable(iteration);
				} catch (Exception ignored) {
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java

	/**
	 * Creates a {@link Spliterator} implementation that wraps the supplied {@link Iteration}. It handles occurrence of
	 * checked exceptions by wrapping them in RuntimeExceptions, and in addition ensures that the wrapped Iteration is
	 * closed when exhausted (if it's a {@link CloseableIteration}).
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
	 * @param iteration the iteration to wrap
	 */
	public IterationSpliterator(final Iteration<T, ? extends Exception> iteration) {
		super(Long.MAX_VALUE, Spliterator.IMMUTABLE | Spliterator.NONNULL);
		this.iteration = iteration;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java

/**
 * A {@link Spliterator} implementation that wraps an {@link Iteration}. It handles occurrence of checked exceptions by
 * wrapping them in RuntimeExceptions, and in addition ensures that the wrapped Iteration is closed when exhausted (if
 * it's a {@link CloseableIteration}).
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
public class IterationSpliterator<T> extends Spliterators.AbstractSpliterator<T> {

	private final Iteration<T, ? extends Exception> iteration;

	/**
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
		Objects.requireNonNull(action, "action may not be null");
		try {
			while (iteration.hasNext()) {
				action.accept(iteration.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
		try {
			while (iteration.hasNext()) {
				action.accept(iteration.next());
			}
		} catch (Exception e) {
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
		} finally {
			try {
				Iterations.closeCloseable(iteration);
			} catch (Exception ignored) {
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIteration.java`
#### Snippet
```java

/**
 * An {@link Iteration} that can be closed to free resources that it is holding. CloseableIterations automatically free
 * their resources when exhausted. If not read until exhaustion or if you want to make sure the iteration is properly
 * closed, any code using the iterator should be placed in a try-with-resources block, closing the iteration
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIteration.java`
#### Snippet
```java
 * </pre>
 *
 * @deprecated In the future this interface will stop extending {@link Iteration} and instead declare the same interface
 *             methods directly. The interface will also stop requiring implementations to automatically close when
 *             exhausted, instead making this an optional feature and requiring the user to always call close. This
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public interface CloseableIteration<E, X extends Exception> extends Iteration<E, X>, AutoCloseable {

	/**
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
		} finally {
			try {
				Iterations.closeCloseable(iter);
			} catch (Exception e) {
				if (e instanceof InterruptedException) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
		}
		try {
			boolean result = iter.hasNext();
			if (!result) {
				close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
		}
		try {
			return iter.next();
		} catch (NoSuchElementException e) {
			close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
	 *             <var>null</var>.
	 */
	protected ExceptionConvertingIteration(Iteration<? extends E, ? extends Exception> iter) {
		this.iter = Objects.requireNonNull(iter, "The iterator was null");
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
	 * The underlying Iteration.
	 */
	private final Iteration<? extends E, ? extends Exception> iter;

	/*--------------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
		}
		try {
			iter.remove();
		} catch (UnsupportedOperationException | IllegalStateException e) {
			throw e;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			StringBuilder sb)
			throws X {
		while (iteration.hasNext()) {
			sb.append(iteration.next());

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			throws X {
		while (iteration.hasNext()) {
			sb.append(iteration.next());

			if (iteration.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			sb.append(iteration.next());

			if (iteration.hasNext()) {
				sb.append(separator);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <X extends Exception> void toString(Iteration<?, X> iteration, String separator, StringBuilder sb)
			throws X {
		while (iteration.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	public static <X extends Exception> void toString(Iteration<?, X> iteration, String separator, StringBuilder sb)
			throws X {
		while (iteration.hasNext()) {
			sb.append(iteration.next());

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			throws X {
		while (iteration.hasNext()) {
			sb.append(iteration.next());

			if (iteration.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			sb.append(iteration.next());

			if (iteration.hasNext()) {
				sb.append(separator);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			Supplier<Set<E>> setMaker) throws X {
		Set<E> set = setMaker.get();
		while (iteration.hasNext()) {
			set.add(iteration.next());
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
		Set<E> set = setMaker.get();
		while (iteration.hasNext()) {
			set.add(iteration.next());
		}
		return set;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <E, X extends Exception> Set<E> asSet(Iteration<? extends E, X> iter) throws X {
		try (Stream<? extends E> stream = iter.stream()) {
			return stream.collect(Collectors.toSet());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <E, X extends Exception> Set<E> asSet(Iteration<? extends E, X> iter) throws X {
		try (Stream<? extends E> stream = iter.stream()) {
			return stream.collect(Collectors.toSet());
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <X extends Exception> String toString(Iteration<?, X> iteration, String separator) throws X {
		StringBuilder sb = new StringBuilder();
		toString(iteration, separator, sb);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <E, X extends Exception> List<E> asList(Iteration<? extends E, X> iter) throws X {
		// stream.collect is slightly slower than addAll for lists
		List<E> list = new ArrayList<>();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			C collection) throws X {
		try {
			while (iter.hasNext()) {
				collection.add(iter.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
		try {
			while (iter.hasNext()) {
				collection.add(iter.next());
			}
		} finally {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <E, X extends Exception, C extends Collection<E>> C addAll(Iteration<? extends E, X> iter,
			C collection) throws X {
		try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			C collection) throws X {
		try {
			while (iter.hasNext()) {
				collection.add(iter.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
		try {
			while (iter.hasNext()) {
				collection.add(iter.next());
			}
		} finally {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <X extends Exception> void closeCloseable(Iteration<?, X> iteration) throws X {
		if (iteration instanceof CloseableIteration<?, ?>) {
			((CloseableIteration<?, X>) iteration).close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <E, X extends Exception> Set<E> asSet(Iteration<? extends E, ? extends X> iteration,
			Supplier<Set<E>> setMaker) throws X {
		Set<E> set = setMaker.get();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
			Supplier<Set<E>> setMaker) throws X {
		Set<E> set = setMaker.get();
		while (iteration.hasNext()) {
			set.add(iteration.next());
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
		Set<E> set = setMaker.get();
		while (iteration.hasNext()) {
			set.add(iteration.next());
		}
		return set;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java

	/**
	 * Get a sequential {@link Stream} with the supplied {@link Iteration} as its source. If the source iteration is a
	 * {@link CloseableIteration}, it will be automatically closed by the stream when done. Any checked exceptions
	 * thrown at any point during stream processing will be propagated wrapped in a {@link RuntimeException}.
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 * thrown at any point during stream processing will be propagated wrapped in a {@link RuntimeException}.
	 *
	 * @param iteration a source {@link Iteration} for the stream.
	 * @return a sequential {@link Stream} object which can be used to process the data from the source iteration.
	 */
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <T> Stream<T> stream(Iteration<T, ? extends Exception> iteration) {
		Spliterator<T> spliterator = new IterationSpliterator<>(iteration);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 * @param rightArg An Iteration containing the set of elements that should be filtered from the main set.
	 */
	public MinusIteration(Iteration<? extends E, X> leftArg, Iteration<? extends E, X> rightArg) {
		this(leftArg, rightArg, false);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/OffsetIteration.java`
#### Snippet
```java
	 * @param offset The number of elements to skip, must be larger than or equal to 0.
	 */
	public OffsetIteration(Iteration<? extends E, X> iter, long offset) {
		super(iter);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/LimitIteration.java`
#### Snippet
```java
	 * @param limit The number of query answers to return, must be &gt;= 0.
	 */
	public LimitIteration(Iteration<? extends E, X> iter, long limit) {
		super(iter);

```

### MarkedForRemoval
'asSet(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
		if (!initialized) {
			// Build set of elements-to-include from second argument
			includeSet = Iterations.asSet(arg2);
			initialized = true;
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2,
			boolean distinct, Supplier<Set<E>> setMaker) {
		super(arg1);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2,
			boolean distinct, Supplier<Set<E>> setMaker) {
		super(arg1);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	 *-----------*/

	protected final Iteration<? extends E, ? extends X> arg2;

	private final boolean distinct;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ConvertingIteration.java`
#### Snippet
```java
			throw new NoSuchElementException("The iteration has been closed.");
		}
		return convert(iter.next());
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ConvertingIteration.java`
#### Snippet
```java
			return false;
		}
		boolean result = iter.hasNext();
		if (!result) {
			close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java

	// this method does not seem to "addSecondSet" since the second set seems to be ignored
	public Set<E> addSecondSet(Iteration<? extends E, ? extends X> arg2, Set<E> set) throws X {
		return Iterations.addAll(arg2, setMaker.get());
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ConvertingIteration.java`
#### Snippet
```java
	 * @param iter The source type iteration for this <var>ConvertingIteration</var>, must not be <var>null</var>.
	 */
	protected ConvertingIteration(Iteration<? extends S, ? extends X> iter) {
		this.iter = Objects.requireNonNull(iter, "The iterator was null");
	}
```

### MarkedForRemoval
'addAll(org.eclipse.rdf4j.common.iteration.Iteration, C)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	// this method does not seem to "addSecondSet" since the second set seems to be ignored
	public Set<E> addSecondSet(Iteration<? extends E, ? extends X> arg2, Set<E> set) throws X {
		return Iterations.addAll(arg2, setMaker.get());
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ConvertingIteration.java`
#### Snippet
```java
			throw new IllegalStateException("The iteration has been closed.");
		}
		iter.remove();
	}

```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
			super.handleClose();
		} finally {
			Iterations.closeCloseable(arg2);
		}
	}
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ConvertingIteration.java`
#### Snippet
```java
			super.handleClose();
		} finally {
			Iterations.closeCloseable(iter);
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2,
			boolean distinct) {
		super(arg1);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ConvertingIteration.java`
#### Snippet
```java
	 * The source type iteration.
	 */
	private final Iteration<? extends S, ? extends X> iter;

	/*--------------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 * @param rightArg An Iteration containing the set of elements that should be filtered from the main set.
	 */
	public MinusIteration(Iteration<? extends E, X> leftArg, Iteration<? extends E, X> rightArg) {
		this(leftArg, rightArg, false);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2,
			boolean distinct) {
		super(arg1);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 *-----------*/

	private final Iteration<? extends E, X> rightArg;

	private final boolean distinct;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	 * @param arg2 An Iteration containing the second set of elements.
	 */
	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2) {
		this(arg1, arg2, false);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public MinusIteration(Iteration<? extends E, X> leftArg, Iteration<? extends E, X> rightArg, boolean distinct,
			Supplier<Set<E>> setMaker) {
		super(leftArg);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	 * @param arg2 An Iteration containing the second set of elements.
	 */
	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2) {
		this(arg1, arg2, false);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public MinusIteration(Iteration<? extends E, X> leftArg, Iteration<? extends E, X> rightArg, boolean distinct,
			Supplier<Set<E>> setMaker) {
		super(leftArg);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	}

	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2,
			Supplier<Set<E>> setMaker) {
		this(arg1, arg2, false, setMaker);
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
			super.handleClose();
		} finally {
			Iterations.closeCloseable(rightArg);
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	}

	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2,
			Supplier<Set<E>> setMaker) {
		this(arg1, arg2, false, setMaker);
```

### MarkedForRemoval
'asSet(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
		if (!initialized) {
			// Build set of elements-to-exclude from right argument
			excludeSet = Iterations.asSet(rightArg);
			initialized = true;
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public MinusIteration(Iteration<? extends E, X> leftArg, Iteration<? extends E, X> rightArg, boolean distinct) {
		super(leftArg);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public MinusIteration(Iteration<? extends E, X> leftArg, Iteration<? extends E, X> rightArg, boolean distinct) {
		super(leftArg);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
	 *-----------*/

	private final Iterator<? extends Iteration<? extends E, X>> argIter;

	private Iteration<? extends E, X> currentIter;
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
				while (argIter.hasNext()) {
					try {
						Iterations.closeCloseable(argIter.next());
					} catch (Throwable e) {
						if (e instanceof InterruptedException) {
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
				}
			} finally {
				Iterations.closeCloseable(currentIter);
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
		while (true) {

			Iteration<? extends E, X> nextCurrentIter = currentIter;
			if (nextCurrentIter != null && nextCurrentIter.hasNext()) {
				return nextCurrentIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java

			Iteration<? extends E, X> nextCurrentIter = currentIter;
			if (nextCurrentIter != null && nextCurrentIter.hasNext()) {
				return nextCurrentIter.next();
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
			Iteration<? extends E, X> nextCurrentIter = currentIter;
			if (nextCurrentIter != null && nextCurrentIter.hasNext()) {
				return nextCurrentIter.next();
			}

```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java

			// Current Iteration exhausted, continue with the next one
			Iterations.closeCloseable(nextCurrentIter);

			if (argIter.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
	private final Iterator<? extends Iteration<? extends E, X>> argIter;

	private Iteration<? extends E, X> currentIter;

	/*--------------*
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
	 */
	@SafeVarargs
	public UnionIteration(Iteration<? extends E, X>... args) {
		this(Arrays.asList(args));
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
	 * @param args The Iterations containing the elements to iterate over.
	 */
	public UnionIteration(Iterable<? extends Iteration<? extends E, X>> args) {
		argIter = args.iterator();

```

### MarkedForRemoval
'asList(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
	 * @throws RepositoryException if a problem occurred during retrieval of the results.
	 * @see #addTo(Collection)
	 * @deprecated Use {@link Iterations#asList(Iteration)} instead.
	 */
	@Deprecated
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
	 * @throws RepositoryException if a problem occurred during retrieval of the results.
	 * @see #addTo(Collection)
	 * @deprecated Use {@link Iterations#asList(Iteration)} instead.
	 */
	@Deprecated
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
	@Override
	public void remove() throws RepositoryException {
		wrappedIter.remove();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
	@Override
	public T next() throws RepositoryException {
		return wrappedIter.next();
	}

```

### MarkedForRemoval
'addAll(org.eclipse.rdf4j.common.iteration.Iteration, C)' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
	 * @return A reference to the collection that was supplied.
	 * @throws RepositoryException if a problem occurred during retrieval of the results.
	 * @deprecated Use {@link Iterations#addAll(Iteration, Collection)} instead.
	 */
	@Deprecated
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
	 * @return A reference to the collection that was supplied.
	 * @throws RepositoryException if a problem occurred during retrieval of the results.
	 * @deprecated Use {@link Iterations#addAll(Iteration, Collection)} instead.
	 */
	@Deprecated
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
			super.handleClose();
		} finally {
			Iterations.closeCloseable(wrappedIter);
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
	@Override
	public boolean hasNext() throws RepositoryException {
		return wrappedIter.hasNext();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.DistinctIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.Iterations;
import org.eclipse.rdf4j.common.iterator.CloseableIterationIterator;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
	public E getNextElement() throws X {
		if (iteration1 == null && iteration2 != null) {
			if (iteration2.hasNext()) {
				return iteration2.next();
			} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
		if (iteration1 == null && iteration2 != null) {
			if (iteration2.hasNext()) {
				return iteration2.next();
			} else {
				iteration2.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
			}
		} else if (iteration1 != null) {
			if (iteration1.hasNext()) {
				return iteration1.next();
			} else if (iteration2.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
		} else if (iteration1 != null) {
			if (iteration1.hasNext()) {
				return iteration1.next();
			} else if (iteration2.hasNext()) {
				iteration1.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
			if (iteration1.hasNext()) {
				return iteration1.next();
			} else if (iteration2.hasNext()) {
				iteration1.close();
				iteration1 = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
				iteration1.close();
				iteration1 = null;
				return iteration2.next();
			} else {
				iteration1.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
		Objects.requireNonNull(action, "action may not be null");
		try (iteration) {
			while (iteration.hasNext()) {
				action.accept(iteration.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
		try (iteration) {
			while (iteration.hasNext()) {
				action.accept(iteration.next());
			}
		} catch (Exception e) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java

		try {
			if (iteration.hasNext()) {
				action.accept(iteration.next());
				return true;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
		try {
			if (iteration.hasNext()) {
				action.accept(iteration.next());
				return true;
			} else {
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.repository.RepositoryConnection'
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void remove(Iteration<? extends Statement, E> statements, Resource... contexts)
			throws RepositoryException, E {
		try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void remove(Iteration<? extends Statement, E> statements, Resource... contexts)
			throws RepositoryException, E {
		try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java

			try {
				while (statements.hasNext()) {
					remove(statements.next(), contexts);
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java
			try {
				while (statements.hasNext()) {
					remove(statements.next(), contexts);
				}

```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java
			}
		} finally {
			Iterations.closeCloseable(statements);
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java
import java.util.concurrent.atomic.AtomicBoolean;

import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.Iterations;
import org.eclipse.rdf4j.common.transaction.IsolationLevel;
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.repository.RepositoryConnection'
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void add(Iteration<? extends Statement, E> statements, Resource... contexts)
			throws RepositoryException, E {
		try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void add(Iteration<? extends Statement, E> statements, Resource... contexts)
			throws RepositoryException, E {
		try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java

			try {
				while (statements.hasNext()) {
					addWithoutCommit(statements.next(), contexts);
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java
			try {
				while (statements.hasNext()) {
					addWithoutCommit(statements.next(), contexts);
				}

```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java
			}
		} finally {
			Iterations.closeCloseable(statements);
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java
	default <E extends Exception> void add(CloseableIteration<? extends Statement, E> statements, Resource... contexts)
			throws RepositoryException, E {
		add(((Iteration<? extends Statement, E>) statements), contexts);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	<E extends Exception> void remove(Iteration<? extends Statement, E> statements, Resource... contexts)
			throws RepositoryException, E;

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java
			Resource... contexts)
			throws RepositoryException, E {
		remove((Iteration<Statement, RepositoryException>) statements, contexts);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.transaction.IsolationLevel;
import org.eclipse.rdf4j.common.transaction.TransactionSetting;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	<E extends Exception> void add(Iteration<? extends Statement, E> statements, Resource... contexts)
			throws RepositoryException, E;

```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.repository.RepositoryConnection'
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void remove(Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		if (isDelegatingRemove()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void remove(Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		if (isDelegatingRemove()) {
```

### MarkedForRemoval
'remove(org.eclipse.rdf4j.common.iteration.Iteration, org.eclipse.rdf4j.model.Resource...)' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java
			throws RepositoryException, E {
		if (isDelegatingRemove()) {
			getDelegate().remove(statementIter, contexts);
		} else {
			super.remove(statementIter, contexts);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java
import java.net.URL;

import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.transaction.IsolationLevel;
import org.eclipse.rdf4j.common.transaction.TransactionSetting;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java
	 * @see #clear(Resource...)
	 * @see #remove(Iterable, Resource...)
	 * @see #remove(Iteration, Resource...)
	 * @see #remove(Statement, Resource...)
	 * @see #remove(Resource, IRI, Value, Resource...)
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.repository.RepositoryConnection'
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void add(Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		if (isDelegatingAdd()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java

	@Override
	public <E extends Exception> void add(Iteration<? extends Statement, E> statementIter, Resource... contexts)
			throws RepositoryException, E {
		if (isDelegatingAdd()) {
```

### MarkedForRemoval
'add(org.eclipse.rdf4j.common.iteration.Iteration, org.eclipse.rdf4j.model.Resource...)' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java
			throws RepositoryException, E {
		if (isDelegatingAdd()) {
			getDelegate().add(statementIter, contexts);
		} else {
			super.add(statementIter, contexts);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java
	 * @throws RepositoryException
	 * @see #add(Iterable, Resource...)
	 * @see #add(Iteration, Resource...)
	 * @see #add(Statement, Resource...)
	 * @see #add(File, String, RDFFormat, Resource...)
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/DataStructureInterface.java`
#### Snippet
```java
				null,
				inferred, contexts)) {
			while (statements.hasNext()) {
				removeStatement(statements.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/DataStructureInterface.java`
#### Snippet
```java
				inferred, contexts)) {
			while (statements.hasNext()) {
				removeStatement(statements.next());
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/DataStructureInterface.java`
#### Snippet
```java
				obj,
				inferred, contexts)) {
			while (statements.hasNext()) {
				removeStatement(statements.next());
				deleted = true;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/DataStructureInterface.java`
#### Snippet
```java
				inferred, contexts)) {
			while (statements.hasNext()) {
				removeStatement(statements.next());
				deleted = true;
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/FilteringIteration.java`
#### Snippet
```java
	protected E getNextElement() throws X {

		while (wrappedIteration.hasNext()) {
			E next = wrappedIteration.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/FilteringIteration.java`
#### Snippet
```java

		while (wrappedIteration.hasNext()) {
			E next = wrappedIteration.next();

			if (subject != null && !next.getSubject().equals(subject)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailStore.java`
#### Snippet
```java

		long counter = 0;
		while (statements.hasNext()) {
			ExtensibleStatement next = statements.next();
			instance.add(next);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailStore.java`
#### Snippet
```java
		long counter = 0;
		while (statements.hasNext()) {
			ExtensibleStatement next = statements.next();
			instance.add(next);

```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.SimpleStatement' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/valuefactory/ExtensibleStatementImpl.java`
#### Snippet
```java
import org.eclipse.rdf4j.model.Value;
import org.eclipse.rdf4j.model.ValueFactory;
import org.eclipse.rdf4j.model.impl.SimpleStatement;
import org.eclipse.rdf4j.model.impl.SimpleValueFactory;

```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.SimpleStatement' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/valuefactory/ExtensibleStatementImpl.java`
#### Snippet
```java
import org.eclipse.rdf4j.model.impl.SimpleValueFactory;

public class ExtensibleStatementImpl extends SimpleStatement implements ExtensibleStatement {

	private final boolean inferred;
```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.ContextStatement' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/valuefactory/ExtensibleContextStatement.java`
#### Snippet
```java
import org.eclipse.rdf4j.model.impl.ContextStatement;

public class ExtensibleContextStatement extends ContextStatement implements ExtensibleStatement {
	final boolean inferred;

```

### MarkedForRemoval
'org.eclipse.rdf4j.model.impl.ContextStatement' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/valuefactory/ExtensibleContextStatement.java`
#### Snippet
```java
import org.eclipse.rdf4j.model.Resource;
import org.eclipse.rdf4j.model.Value;
import org.eclipse.rdf4j.model.impl.ContextStatement;

public class ExtensibleContextStatement extends ContextStatement implements ExtensibleStatement {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
			@Override
			public boolean hasNext() throws SailException {
				return statements.hasNext();
			}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
			@Override
			public void close() throws SailException {
				if (!statements.hasNext()) {
					submitToCache(localCacheTicket, partialStatement, cache);
				} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
			public ExtensibleStatement next() throws SailException {

				ExtensibleStatement next = statements.next();

				if (cache != null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailSource.java`
#### Snippet
```java
					private Resource internalNext() {

						while (statements.hasNext()) {
							Statement next = statements.next();
							if (!contexts.contains(next.getContext())) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailSource.java`
#### Snippet
```java

						while (statements.hasNext()) {
							Statement next = statements.next();
							if (!contexts.contains(next.getContext())) {
								contexts.add(next.getContext());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCommittedWrapper.java`
#### Snippet
```java
							if (left.hasNext()) {
								tempNext = left.next();
							} else if (right.hasNext()) {
								tempNext = right.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCommittedWrapper.java`
#### Snippet
```java
								tempNext = left.next();
							} else if (right.hasNext()) {
								tempNext = right.next();

								if (!internalAddedLocal.isEmpty() && internalAddedLocal.contains(tempNext)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCommittedWrapper.java`
#### Snippet
```java
							}

						} while (next == null && (left.hasNext() || right.hasNext()));

						return next;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/SailConnection.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> stIter = getStatements(subj, pred, obj,
				includeInferred, contexts)) {
			return stIter.hasNext();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
				includeInferred, contexts)) {
			long counter = 0;
			while (stmts.hasNext()) {
				stmts.next();
				counter++;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
			long counter = 0;
			while (stmts.hasNext()) {
				stmts.next();
				counter++;
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, RepositoryException> stmts = con.getStatements(subj, pred, null,
				false)) {
			if (!stmts.hasNext()) {
				return null;
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
				return null;
			}
			Value obj = stmts.next().getObject();
			if (stmts.hasNext()) {
				org.junit.Assert.fail("multiple literals: " + obj + " and " + stmts.next());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
			}
			Value obj = stmts.next().getObject();
			if (stmts.hasNext()) {
				org.junit.Assert.fail("multiple literals: " + obj + " and " + stmts.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
			Value obj = stmts.next().getObject();
			if (stmts.hasNext()) {
				org.junit.Assert.fail("multiple literals: " + obj + " and " + stmts.next());
			}
			return (Literal) obj;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
			try (CloseableIteration<? extends Statement, RepositoryException> stmts = con.getStatements(null, null,
					null, false)) {
				while (stmts.hasNext()) {
					Statement st = stmts.next();
					counter++;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
					null, false)) {
				while (stmts.hasNext()) {
					Statement st = stmts.next();
					counter++;
					if (counter < size) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java
	@Override
	public E next() throws X {
		return delegate.next();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java
	@Override
	public void remove() throws X {
		delegate.remove();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java
	@Override
	public boolean hasNext() throws X {
		return delegate.hasNext();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/CleanerTupleQueryResult.java`
#### Snippet
```java
	@Override
	public void remove() {
		delegate.remove();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/LockingIteration.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	public LockingIteration(Lock lock, Iteration<? extends E, X> iter) {
		super(iter);
		if (iter instanceof EmptyIteration) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/LockingIteration.java`
#### Snippet
```java
import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.EmptyIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.IterationWrapper;

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/CleanerGraphQueryResult.java`
#### Snippet
```java
	@Override
	public void remove() {
		delegate.remove();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSailConnection.java`
#### Snippet
```java
			Resource[] contexts) {
		try (var iteration = getStatementsInternal(subj, pred, obj, includeInferred, contexts)) {
			return iteration.hasNext();
		}
	}
```

### MarkedForRemoval
'QueueIteration(java.util.concurrent.BlockingQueue, java.lang.ref.WeakReference)' is deprecated and marked for removal
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/QueueCursor.java`
#### Snippet
```java
	@Deprecated(since = "4.1.2", forRemoval = true)
	public QueueCursor(BlockingQueue<E> queue, WeakReference<?> callerReference) {
		super(queue, callerReference);
	}

```

### MarkedForRemoval
'QueueIteration(int, boolean, java.lang.ref.WeakReference)' is deprecated and marked for removal
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/QueueCursor.java`
#### Snippet
```java
	@Deprecated(since = "4.1.2", forRemoval = true)
	public QueueCursor(int capacity, boolean fair, WeakReference<?> callerReference) {
		super(capacity, fair, callerReference);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailInterruptTest.java`
#### Snippet
```java
		try (SailConnection con = store.getConnection();
				CloseableIteration<?, SailException> iter = con.getStatements(null, null, null, true)) {
			while (iter.hasNext()) {
				iter.next();
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailInterruptTest.java`
#### Snippet
```java
				CloseableIteration<?, SailException> iter = con.getStatements(null, null, null, true)) {
			while (iter.hasNext()) {
				iter.next();
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResults.java`
#### Snippet
```java
		@Override
		public void remove() {
			delegate.remove();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailConcurrencyTest.java`
#### Snippet
```java
						try (CloseableIteration<? extends Resource, SailException> contextIter = connection
								.getContextIDs()) {
							while (contextIter.hasNext()) {
								Resource context = contextIter.next();
								assertNotNull(context);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailConcurrencyTest.java`
#### Snippet
```java
								.getContextIDs()) {
							while (contextIter.hasNext()) {
								Resource context = contextIter.next();
								assertNotNull(context);
							}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/MutableTupleQueryResult.java`
#### Snippet
```java
	@Deprecated(since = "4.1.0", forRemoval = true)
	public <E extends Exception> MutableTupleQueryResult(Collection<String> bindingNames,
			Iteration<? extends BindingSet, E> bindingSetIter) throws E {
		this.bindingNames.addAll(bindingNames);
		Iterations.addAll(bindingSetIter, this.bindingSets);
```

### MarkedForRemoval
'addAll(org.eclipse.rdf4j.common.iteration.Iteration, C)' is deprecated and marked for removal
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/MutableTupleQueryResult.java`
#### Snippet
```java
			Iteration<? extends BindingSet, E> bindingSetIter) throws E {
		this.bindingNames.addAll(bindingNames);
		Iterations.addAll(bindingSetIter, this.bindingSets);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/MutableTupleQueryResult.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.Iterations;
import org.eclipse.rdf4j.query.BindingSet;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
			try (CloseableIteration<? extends Statement, SailException> stmts = con.getStatements(null, null, null,
					false)) {
				while (stmts.hasNext()) {
					Statement st = stmts.next();
					counter++;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
					false)) {
				while (stmts.hasNext()) {
					Statement st = stmts.next();
					counter++;
					if (counter < size) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
				includeInferred, contexts)) {
			long counter = 0;
			while (stmts.hasNext()) {
				stmts.next();
				counter++;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
			long counter = 0;
			while (stmts.hasNext()) {
				stmts.next();
				counter++;
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> stmts = con.getStatements(subj, pred, null,
				false)) {
			if (!stmts.hasNext()) {
				return null;
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
				return null;
			}
			Value obj = stmts.next().getObject();
			if (stmts.hasNext()) {
				Assertions.fail("multiple literals: " + obj + " and " + stmts.next());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
			}
			Value obj = stmts.next().getObject();
			if (stmts.hasNext()) {
				Assertions.fail("multiple literals: " + obj + " and " + stmts.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
			Value obj = stmts.next().getObject();
			if (stmts.hasNext()) {
				Assertions.fail("multiple literals: " + obj + " and " + stmts.next());
			}
			return (Literal) obj;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
	private <T, X extends Exception> T first(CloseableIteration<T, X> iter) throws X {
		try (iter) {
			if (iter.hasNext()) {
				return iter.next();
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		try (iter) {
			if (iter.hasNext()) {
				return iter.next();
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		con.clearNamespaces();
		con.commit();
		assertTrue(!con.getNamespaces().hasNext());
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java

		try (iter) {
			while (iter.hasNext()) {
				iter.next();
				count++;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		try (iter) {
			while (iter.hasNext()) {
				iter.next();
				count++;
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, SailException> stIter = con.getStatements(null, null, null,
				false)) {
			assertTrue(stIter.hasNext());

			Statement st = stIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
			assertTrue(stIter.hasNext());

			Statement st = stIter.next();
			assertEquals(subj, st.getSubject());
			assertEquals(pred, st.getPredicate());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
			assertEquals(pred, st.getPredicate());
			assertEquals(obj, st.getObject());
			assertFalse(stIter.hasNext());
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java

		try {
			assertTrue(iter.hasNext());

			BindingSet bindings = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
			assertTrue(iter.hasNext());

			BindingSet bindings = iter.next();
			assertEquals(subj, bindings.getValue("S"));
			assertEquals(pred, bindings.getValue("P"));
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
			assertEquals(pred, bindings.getValue("P"));
			assertEquals(obj, bindings.getValue("O"));
			assertTrue(!iter.hasNext());
		} finally {
			iter.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		int resultCount = 0;

		while (resultIter.hasNext()) {
			BindingSet resultBindings = resultIter.next();
			resultCount++;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java

		while (resultIter.hasNext()) {
			BindingSet resultBindings = resultIter.next();
			resultCount++;

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		con.begin();

		while (iter.hasNext()) {
			BindingSet bindings = iter.next();
			Value c = bindings.getValue("C");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java

		while (iter.hasNext()) {
			BindingSet bindings = iter.next();
			Value c = bindings.getValue("C");
			if (c instanceof Resource) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		iter = con.evaluate(tupleQuery.getTupleExpr(), null, EmptyBindingSet.getInstance(), false);

		while (iter.hasNext()) {
			BindingSet bindings = iter.next();
			Value p = bindings.getValue("P");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java

		while (iter.hasNext()) {
			BindingSet bindings = iter.next();
			Value p = bindings.getValue("P");
			if (p instanceof IRI) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java

		try (CloseableIteration<? extends Namespace, SailException> namespaces = con.getNamespaces()) {
			assertTrue(namespaces.hasNext());
			Namespace rdf = namespaces.next();
			assertEquals("rdf", rdf.getPrefix());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		try (CloseableIteration<? extends Namespace, SailException> namespaces = con.getNamespaces()) {
			assertTrue(namespaces.hasNext());
			Namespace rdf = namespaces.next();
			assertEquals("rdf", rdf.getPrefix());
			assertEquals(RDF.NAMESPACE, rdf.getName());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
			assertEquals("rdf", rdf.getPrefix());
			assertEquals(RDF.NAMESPACE, rdf.getName());
			assertFalse(namespaces.hasNext());
		}
	}
```

### MarkedForRemoval
'QueueCursor(int, java.lang.ref.WeakReference)' is deprecated and marked for removal
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/BackgroundGraphResult.java`
#### Snippet
```java
	public BackgroundGraphResult(RDFParser parser, InputStream in, Charset charset, String baseURI,
			WeakReference<?> callerRef) {
		this(new QueueCursor<>(10, callerRef), parser, in, charset, baseURI);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailBooleanQuery.java`
#### Snippet
```java
			bindingsIter2 = enforceMaxQueryTime(bindingsIter1);

			return bindingsIter2.hasNext();
		} catch (SailException e) {
			throw new QueryEvaluationException(e.getMessage(), e);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
							};
						}
						while (contextIDs.hasNext()) {
							con.clear(contextIDs.next());
						}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
						}
						while (contextIDs.hasNext()) {
							con.clear(contextIDs.next());
						}
					} finally {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
			}

			while (statements.hasNext()) {
				Statement st = statements.next();
				con.addStatement(uc, st.getSubject(), st.getPredicate(), st.getObject(), (Resource) destination);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

			while (statements.hasNext()) {
				Statement st = statements.next();
				con.addStatement(uc, st.getSubject(), st.getPredicate(), st.getObject(), (Resource) destination);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
			try (CloseableIteration<? extends BindingSet, QueryEvaluationException> sourceBindings = evaluateWhereClause(
					whereClause, uc, maxExecutionTime)) {
				while (sourceBindings.hasNext()) {
					BindingSet sourceBinding = sourceBindings.next();
					deleteBoundTriples(sourceBinding, modify.getDeleteExpr(), uc);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
					whereClause, uc, maxExecutionTime)) {
				while (sourceBindings.hasNext()) {
					BindingSet sourceBinding = sourceBindings.next();
					deleteBoundTriples(sourceBinding, modify.getDeleteExpr(), uc);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
			}

			while (statements.hasNext()) {
				Statement st = statements.next();
				con.addStatement(uc, st.getSubject(), st.getPredicate(), st.getObject(), (Resource) destination);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

			while (statements.hasNext()) {
				Statement st = statements.next();
				con.addStatement(uc, st.getSubject(), st.getPredicate(), st.getObject(), (Resource) destination);
				con.removeStatement(uc, st.getSubject(), st.getPredicate(), st.getObject(), (Resource) source);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
			}

			while (statements.hasNext()) {
				Statement st = statements.next();
				con.addStatement(uc, st.getSubject(), st.getPredicate(), st.getObject(), (Resource) destination);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

			while (statements.hasNext()) {
				Statement st = statements.next();
				con.addStatement(uc, st.getSubject(), st.getPredicate(), st.getObject(), (Resource) destination);
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

			try (CloseableIteration<? extends Resource, SailException> contextIDs = con.getContextIDs()) {
				while (contextIDs.hasNext()) {
					Resource contextID = contextIDs.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
			try (CloseableIteration<? extends Resource, SailException> contextIDs = con.getContextIDs()) {
				while (contextIDs.hasNext()) {
					Resource contextID = contextIDs.next();

					if (namedGraph.equals(contextID)) {
```

### MarkedForRemoval
'QueueCursor(int, java.lang.ref.WeakReference)' is deprecated and marked for removal
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/helpers/BackgroundTupleResult.java`
#### Snippet
```java

	public BackgroundTupleResult(TupleQueryResultParser parser, InputStream in, WeakReference<?> callerRef) {
		this(new QueueCursor<>(10, callerRef), parser, in);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		TupleExpr te = graphPattern.buildTupleExpr();

		e.setSubQuery(te);

		graphPattern = parentGP;
```

### MarkedForRemoval
'setObjectVar(org.eclipse.rdf4j.query.algebra.Var)' is deprecated and marked for removal
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
					if (subj.equals(left) || subj.equals(right)) {
						if (obj.equals(left) || obj.equals(right)) {
							sp.setObjectVar(subj.clone());
						}
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		TupleExpr te = graphPattern.buildTupleExpr();

		e.setSubQuery(te);

		graphPattern = parentGP;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		}

		VarNameCollector varNameCollector = new VarNameCollector();
		expr.visit(varNameCollector);
		Set<String> varNames = varNameCollector.getVarNames();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		}

		VarNameCollector varNameCollector = new VarNameCollector();
		expr.visit(varNameCollector);
		Set<String> varNames = varNameCollector.getVarNames();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		VarNameCollector varNameCollector = new VarNameCollector();
		expr.visit(varNameCollector);
		Set<String> varNames = varNameCollector.getVarNames();

		for (String varName : varNames) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.helpers.AbstractQueryModelVisitor;
import org.eclipse.rdf4j.query.algebra.helpers.TupleExprs;
import org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector;
import org.eclipse.rdf4j.query.algebra.helpers.collectors.StatementPatternCollector;
import org.eclipse.rdf4j.query.impl.ListBindingSet;
```

### MarkedForRemoval
'setAnonymous(boolean)' is deprecated and marked for removal
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	public Var visit(ASTVar node, Object data) throws VisitorException {
		Var var = new Var(node.getName());
		var.setAnonymous(node.isAnonymous());
		return var;
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ContextStore.java`
#### Snippet
```java
		logger.debug("initializing context cache");
		try (CloseableIteration<Resource, SailException> contextIter = store.getContexts()) {
			while (contextIter.hasNext()) {
				increment(contextIter.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ContextStore.java`
#### Snippet
```java
		try (CloseableIteration<Resource, SailException> contextIter = store.getContexts()) {
			while (contextIter.hasNext()) {
				increment(contextIter.next());
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
		stmts = dataset.getStatements(subj, pred, obj, contexts);
		try {
			return stmts.hasNext();
		} finally {
			stmts.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
				iter = dataset().getStatements(null, null, null);
				try {
					while (iter.hasNext()) {
						iter.next();
						size++;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
				try {
					while (iter.hasNext()) {
						iter.next();
						size++;
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			stmts = dataset().getStatements(subj, pred, obj, contexts);
			try {
				while (stmts.hasNext()) {
					Statement st = stmts.next();
					sink().deprecate(st);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			try {
				while (stmts.hasNext()) {
					Statement st = stmts.next();
					sink().deprecate(st);
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
						try {
							long size = 0;
							while (iter.hasNext()) {
								iter.next();
								if (size++ >= Integer.MAX_VALUE) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
							long size = 0;
							while (iter.hasNext()) {
								iter.next();
								if (size++ >= Integer.MAX_VALUE) {
									return Integer.MAX_VALUE;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			spaces = dataset().getNamespaces();
			try {
				while (spaces.hasNext()) {
					set.add(spaces.next());
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			try {
				while (spaces.hasNext()) {
					set.add(spaces.next());
				}
			} finally {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
		public boolean hasNext() {
			try {
				if (stmts.hasNext()) {
					return true;
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
				stmts = dataset().getStatements(subj, pred, obj, contexts);
				try {
					while (stmts.hasNext()) {
						Statement st = stmts.next();
						sink().deprecate(st);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
				try {
					while (stmts.hasNext()) {
						Statement st = stmts.next();
						sink().deprecate(st);
					}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
		public Statement next() {
			try {
				last = stmts.next();
				if (last == null) {
					stmts.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.BindingAssigner' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
	public Iterable<QueryOptimizer> getOptimizers() {
		return Arrays.asList(
				new BindingAssigner(),
				new BindingSetAssignmentInliner(),
				new ConstantOptimizer(strategy),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.BindingSetAssignmentInliner' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
		return Arrays.asList(
				new BindingAssigner(),
				new BindingSetAssignmentInliner(),
				new ConstantOptimizer(strategy),
				new RegexAsStringFunctionOptimizer(tripleSource.getValueFactory()),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.ConstantOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new BindingAssigner(),
				new BindingSetAssignmentInliner(),
				new ConstantOptimizer(strategy),
				new RegexAsStringFunctionOptimizer(tripleSource.getValueFactory()),
				new CompareOptimizer(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.RegexAsStringFunctionOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new BindingSetAssignmentInliner(),
				new ConstantOptimizer(strategy),
				new RegexAsStringFunctionOptimizer(tripleSource.getValueFactory()),
				new CompareOptimizer(),
				new ConjunctiveConstraintSplitter(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.CompareOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new ConstantOptimizer(strategy),
				new RegexAsStringFunctionOptimizer(tripleSource.getValueFactory()),
				new CompareOptimizer(),
				new ConjunctiveConstraintSplitter(),
				new DisjunctiveConstraintOptimizer(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.ConjunctiveConstraintSplitter' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new RegexAsStringFunctionOptimizer(tripleSource.getValueFactory()),
				new CompareOptimizer(),
				new ConjunctiveConstraintSplitter(),
				new DisjunctiveConstraintOptimizer(),
				new SameTermFilterOptimizer(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.DisjunctiveConstraintOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new CompareOptimizer(),
				new ConjunctiveConstraintSplitter(),
				new DisjunctiveConstraintOptimizer(),
				new SameTermFilterOptimizer(),
				new UnionScopeChangeOptimizer(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.SameTermFilterOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new ConjunctiveConstraintSplitter(),
				new DisjunctiveConstraintOptimizer(),
				new SameTermFilterOptimizer(),
				new UnionScopeChangeOptimizer(),
				new QueryModelNormalizer(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.UnionScopeChangeOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new DisjunctiveConstraintOptimizer(),
				new SameTermFilterOptimizer(),
				new UnionScopeChangeOptimizer(),
				new QueryModelNormalizer(),
				new ProjectionRemovalOptimizer(), // Make sure this is after the UnionScopeChangeOptimizer
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.QueryModelNormalizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new SameTermFilterOptimizer(),
				new UnionScopeChangeOptimizer(),
				new QueryModelNormalizer(),
				new ProjectionRemovalOptimizer(), // Make sure this is after the UnionScopeChangeOptimizer
				new FilterOptimizer(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.ProjectionRemovalOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new UnionScopeChangeOptimizer(),
				new QueryModelNormalizer(),
				new ProjectionRemovalOptimizer(), // Make sure this is after the UnionScopeChangeOptimizer
				new FilterOptimizer(),
				new QueryJoinOptimizer(evaluationStatistics),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.FilterOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new QueryModelNormalizer(),
				new ProjectionRemovalOptimizer(), // Make sure this is after the UnionScopeChangeOptimizer
				new FilterOptimizer(),
				new QueryJoinOptimizer(evaluationStatistics),
				new IterativeEvaluationOptimizer(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.QueryJoinOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new ProjectionRemovalOptimizer(), // Make sure this is after the UnionScopeChangeOptimizer
				new FilterOptimizer(),
				new QueryJoinOptimizer(evaluationStatistics),
				new IterativeEvaluationOptimizer(),
				new OrderLimitOptimizer(),
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.IterativeEvaluationOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new FilterOptimizer(),
				new QueryJoinOptimizer(evaluationStatistics),
				new IterativeEvaluationOptimizer(),
				new OrderLimitOptimizer(),
				new ParentReferenceCleaner());
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.OrderLimitOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new QueryJoinOptimizer(evaluationStatistics),
				new IterativeEvaluationOptimizer(),
				new OrderLimitOptimizer(),
				new ParentReferenceCleaner());
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.ParentReferenceCleaner' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
				new IterativeEvaluationOptimizer(),
				new OrderLimitOptimizer(),
				new ParentReferenceCleaner());
	}

```

### MarkedForRemoval
'setValue(org.eclipse.rdf4j.model.Value)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/BindingAssigner.java`
#### Snippet
```java
			if (!var.hasValue() && bindings.hasBinding(var.getName())) {
				Value value = bindings.getValue(var.getName());
				var.setValue(value);
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/AbstractQueryPreparer.java`
#### Snippet
```java
				bindingsIter2 = enforceMaxQueryTime(bindingsIter1);

				return bindingsIter2.hasNext();
			} finally {
				try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/FilterOptimizer.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.QueryOptimizer;
import org.eclipse.rdf4j.query.algebra.helpers.AbstractQueryModelVisitor;
import org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector;

/**
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/FilterOptimizer.java`
#### Snippet
```java
		public FilterRelocator(Filter filter) {
			this.filter = filter;
			filterVars = VarNameCollector.process(filter.getCondition());
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/FilterOptimizer.java`
#### Snippet
```java
		public FilterRelocator(Filter filter) {
			this.filter = filter;
			filterVars = VarNameCollector.process(filter.getCondition());
		}

```

### MarkedForRemoval
'setValue(org.eclipse.rdf4j.model.Value)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/SameTermFilterOptimizer.java`
#### Snippet
```java
		public void meet(Var var) {
			if (var.getName().equals(varName)) {
				var.setValue(value);
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.QueryModelNormalizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryModelPruner.java`
#### Snippet
```java
 *
 * @author Arjohn Kampman
 * @deprecated Replaced by {@link QueryModelNormalizer}.
 */
@Deprecated
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/TupleFunctionEvaluationStrategy.java`
#### Snippet
```java
			public BindingSet getNextElement() throws QueryEvaluationException {
				QueryBindingSet resultBindings = null;
				while (resultBindings == null && iter.hasNext()) {
					resultBindings = new QueryBindingSet(bindings);
					List<? extends Value> values = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/TupleFunctionEvaluationStrategy.java`
#### Snippet
```java
				while (resultBindings == null && iter.hasNext()) {
					resultBindings = new QueryBindingSet(bindings);
					List<? extends Value> values = iter.next();
					if (resultVars.size() != values.size()) {
						throw new QueryEvaluationException("Incorrect number of result vars: require " + values.size());
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.query.algebra.evaluation.impl.StrictEvaluationStrategy'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/TupleFunctionEvaluationStrategy.java`
#### Snippet
```java
	@Deprecated(forRemoval = true)
	@Override
	public CloseableIteration<BindingSet, QueryEvaluationException> evaluate(TupleExpr expr, BindingSet bindings)
			throws QueryEvaluationException {
		if (expr instanceof TupleFunctionCall) {
```

### MarkedForRemoval
'evaluate(org.eclipse.rdf4j.query.algebra.TupleExpr, org.eclipse.rdf4j.query.BindingSet)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/TupleFunctionEvaluationStrategy.java`
#### Snippet
```java
			return evaluate((TupleFunctionCall) expr, bindings);
		} else {
			return super.evaluate(expr, bindings);
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ConjunctiveConstraintSplitter.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.QueryOptimizer;
import org.eclipse.rdf4j.query.algebra.helpers.AbstractQueryModelVisitor;
import org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector;

/**
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ConjunctiveConstraintSplitter.java`
#### Snippet
```java

		private boolean isWithinBindingScope(ValueExpr condition, TupleExpr node) {
			return node.getBindingNames().containsAll(VarNameCollector.process(condition));
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ConjunctiveConstraintSplitter.java`
#### Snippet
```java

		private boolean isWithinBindingScope(ValueExpr condition, TupleExpr node) {
			return node.getBindingNames().containsAll(VarNameCollector.process(condition));
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryModelNormalizer.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.util.QueryEvaluationUtility;
import org.eclipse.rdf4j.query.algebra.helpers.AbstractQueryModelVisitor;
import org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector;

/**
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryModelNormalizer.java`
#### Snippet
```java
	 */
	private boolean isWellDesigned(LeftJoin leftJoin) {
		VarNameCollector optionalVarCollector = new VarNameCollector();
		leftJoin.getRightArg().visit(optionalVarCollector);
		if (leftJoin.hasCondition()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryModelNormalizer.java`
#### Snippet
```java
	 */
	private boolean isWellDesigned(LeftJoin leftJoin) {
		VarNameCollector optionalVarCollector = new VarNameCollector();
		leftJoin.getRightArg().visit(optionalVarCollector);
		if (leftJoin.hasCondition()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryModelNormalizer.java`
#### Snippet
```java
		}

		Set<String> problemVars = optionalVarCollector.getVarNames();
		problemVars.removeAll(leftJoin.getLeftArg().getBindingNames());

```

### MarkedForRemoval
'setValue(org.eclipse.rdf4j.model.Value)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/BindingSetAssignmentInliner.java`
#### Snippet
```java
		public void meet(Var var) {
			if (bindingSet != null && bindingSet.hasBinding(var.getName())) {
				var.setValue(bindingSet.getValue(var.getName()));
			}
		}
```

### MarkedForRemoval
'setValue(org.eclipse.rdf4j.model.Value)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ConstantOptimizer.java`
#### Snippet
```java
						Value value = bindings.getValue(name);
						if (value != null) {
							lostVar.setValue(value);
						}
						ext.addElement(new ExtensionElem(lostVar, name));
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.query.algebra.evaluation.impl.StrictEvaluationStrategy'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ExtendedEvaluationStrategy.java`
#### Snippet
```java
	@Deprecated(forRemoval = true)
	@Override
	public Value evaluate(Compare node, BindingSet bindings)
			throws QueryEvaluationException {
		Value leftVal = evaluate(node.getLeftArg(), bindings);
```

### MarkedForRemoval
'evaluate(org.eclipse.rdf4j.query.algebra.ValueExpr, org.eclipse.rdf4j.query.BindingSet)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ExtendedEvaluationStrategy.java`
#### Snippet
```java
	public Value evaluate(Compare node, BindingSet bindings)
			throws QueryEvaluationException {
		Value leftVal = evaluate(node.getLeftArg(), bindings);
		Value rightVal = evaluate(node.getRightArg(), bindings);

```

### MarkedForRemoval
'evaluate(org.eclipse.rdf4j.query.algebra.ValueExpr, org.eclipse.rdf4j.query.BindingSet)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ExtendedEvaluationStrategy.java`
#### Snippet
```java
			throws QueryEvaluationException {
		Value leftVal = evaluate(node.getLeftArg(), bindings);
		Value rightVal = evaluate(node.getRightArg(), bindings);

		// return result of non-strict comparisson.
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'org.eclipse.rdf4j.query.algebra.evaluation.impl.StrictEvaluationStrategy'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ExtendedEvaluationStrategy.java`
#### Snippet
```java
	@Deprecated(forRemoval = true)
	@Override
	public Value evaluate(MathExpr node, BindingSet bindings)
			throws QueryEvaluationException {
		Value leftVal = evaluate(node.getLeftArg(), bindings);
```

### MarkedForRemoval
'evaluate(org.eclipse.rdf4j.query.algebra.ValueExpr, org.eclipse.rdf4j.query.BindingSet)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ExtendedEvaluationStrategy.java`
#### Snippet
```java
	public Value evaluate(MathExpr node, BindingSet bindings)
			throws QueryEvaluationException {
		Value leftVal = evaluate(node.getLeftArg(), bindings);
		Value rightVal = evaluate(node.getRightArg(), bindings);

```

### MarkedForRemoval
'evaluate(org.eclipse.rdf4j.query.algebra.ValueExpr, org.eclipse.rdf4j.query.BindingSet)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ExtendedEvaluationStrategy.java`
#### Snippet
```java
			throws QueryEvaluationException {
		Value leftVal = evaluate(node.getLeftArg(), bindings);
		Value rightVal = evaluate(node.getRightArg(), bindings);

		return mathOperationApplier(node, leftVal, rightVal);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.impl.UnionScopeChangeOptimizer' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ProjectionRemovalOptimizer.java`
#### Snippet
```java
 *
 * @author Jerven Bolleman
 * @see UnionScopeChangeOptimizer
 */
@Deprecated(forRemoval = true, since = "4.1.0")
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.QueryEvaluationStep.DelayedEvaluationIteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/MinusQueryEvaluationStep.java`
#### Snippet
```java
public class MinusQueryEvaluationStep implements QueryEvaluationStep {
	private final QueryEvaluationStep leftQes;
	private final Function<BindingSet, DelayedEvaluationIteration> rightQes;

	public MinusQueryEvaluationStep(QueryEvaluationStep leftQes, QueryEvaluationStep rightQes) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.QueryEvaluationStep.DelayedEvaluationIteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/MinusQueryEvaluationStep.java`
#### Snippet
```java
	public MinusQueryEvaluationStep(QueryEvaluationStep leftQes, QueryEvaluationStep rightQes) {
		this.leftQes = leftQes;
		this.rightQes = bs -> new DelayedEvaluationIteration(rightQes, bs);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/LeftJoinQueryEvaluationStep.java`
#### Snippet
```java
	public LeftJoinQueryEvaluationStep(QueryEvaluationStep right, QueryValueEvaluationStep condition,
			QueryEvaluationStep left, LeftJoin leftJoin,
			org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector optionalVarCollector) {
		this.right = right;
		this.condition = condition;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.VarNameCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/LeftJoinQueryEvaluationStep.java`
#### Snippet
```java

		Set<String> leftBindingNames = leftJoin.getLeftArg().getBindingNames();
		Set<String> optionalVars = optionalVarCollector.getVarNames();

		boolean optionalVarsContainLeftBindingName = false;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/ReificationRdfStarQueryEvaluationStep.java`
#### Snippet
```java
						.getStatements(theNode, predicate, null)) {

					while (valueIter.hasNext()) {
						Statement valueStatement = valueIter.next();
						if (theNode.equals(valueStatement.getSubject())) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/ReificationRdfStarQueryEvaluationStep.java`
#### Snippet
```java

					while (valueIter.hasNext()) {
						Statement valueStatement = valueIter.next();
						if (theNode.equals(valueStatement.getSubject())) {
							if (value == null || value.equals(valueStatement.getObject())) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/ReificationRdfStarQueryEvaluationStep.java`
#### Snippet
```java
			protected BindingSet getNextElement()
					throws QueryEvaluationException {
				while (iter.hasNext()) {
					Resource theNode = iter.next();
					MutableBindingSet result = context.createBindingSet(bindings);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/ReificationRdfStarQueryEvaluationStep.java`
#### Snippet
```java
					throws QueryEvaluationException {
				while (iter.hasNext()) {
					Resource theNode = iter.next();
					MutableBindingSet result = context.createBindingSet(bindings);
					// does it match the subjectValue/subjVar
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.QueryEvaluationStep.DelayedEvaluationIteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/IntersectionQueryEvaluationStep.java`
#### Snippet
```java
		this.setMaker = setMaker;
		this.leftArg = leftArg;
		rightArgDelayed = bs -> new DelayedEvaluationIteration(rightArg, bs);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.QueryEvaluationStep.DelayedEvaluationIteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/IntersectionQueryEvaluationStep.java`
#### Snippet
```java

	private final QueryEvaluationStep leftArg;
	private final Function<BindingSet, DelayedEvaluationIteration> rightArgDelayed;
	private final Supplier<Set<BindingSet>> setMaker;

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
	}

	public static Iteration<Resource, QueryEvaluationException> listResources(final Resource subj,
			final TripleSource store) throws QueryEvaluationException {
		return new ConvertingIteration<Value, Resource, QueryEvaluationException>(
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
			throw new NullPointerException("RDF list subject cannot be null");
		}
		return new Iteration<Value, QueryEvaluationException>() {

			Resource list = subj;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
	}

	public static Iteration<Value, QueryEvaluationException> list(final Resource subj,
			final TripleSource store) throws QueryEvaluationException {
		if (subj == null) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
import org.eclipse.rdf4j.common.iteration.ConvertingIteration;
import org.eclipse.rdf4j.common.iteration.FilterIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.model.IRI;
import org.eclipse.rdf4j.model.Literal;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
		try (CloseableIteration<? extends Statement, QueryEvaluationException> stmts = store.getStatements(subj, pred,
				obj)) {
			if (stmts.hasNext()) {
				stmt = stmts.next();
				while (stmts.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
				obj)) {
			if (stmts.hasNext()) {
				stmt = stmts.next();
				while (stmts.hasNext()) {
					Statement nextStmt = stmts.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
			if (stmts.hasNext()) {
				stmt = stmts.next();
				while (stmts.hasNext()) {
					Statement nextStmt = stmts.next();
					if (!org.eclipse.rdf4j.model.util.Statements.isSameTriple(stmt, nextStmt)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
				stmt = stmts.next();
				while (stmts.hasNext()) {
					Statement nextStmt = stmts.next();
					if (!org.eclipse.rdf4j.model.util.Statements.isSameTriple(stmt, nextStmt)) {
						throw new QueryEvaluationException(
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/Statements.java`
#### Snippet
```java

/**
 * @deprecated since 2.0. Use {@link TripleSources} instead.
 */
@Deprecated
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.evaluation.util.TripleSources' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/Statements.java`
#### Snippet
```java
 */
@Deprecated
public final class Statements extends TripleSources {

}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/limited/iterator/LimitedSizeIteratorUtil.java`
#### Snippet
```java
	 * @throws QueryEvaluationException trigerred when maxSize is smaller than the used value
	 */
	public static Set<BindingSet> addAll(Iteration<? extends BindingSet, ? extends QueryEvaluationException> arg2,
			Set<BindingSet> includeSet, AtomicLong used, long maxSize) throws QueryEvaluationException {
		while (arg2.hasNext()) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/limited/iterator/LimitedSizeIteratorUtil.java`
#### Snippet
```java
	public static Set<BindingSet> addAll(Iteration<? extends BindingSet, ? extends QueryEvaluationException> arg2,
			Set<BindingSet> includeSet, AtomicLong used, long maxSize) throws QueryEvaluationException {
		while (arg2.hasNext()) {
			if (includeSet.add(arg2.next()) && used.incrementAndGet() > maxSize) {
				throw new QueryEvaluationException("Size limited reached inside intersect operator");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/limited/iterator/LimitedSizeIteratorUtil.java`
#### Snippet
```java
			Set<BindingSet> includeSet, AtomicLong used, long maxSize) throws QueryEvaluationException {
		while (arg2.hasNext()) {
			if (includeSet.add(arg2.next()) && used.incrementAndGet() > maxSize) {
				throw new QueryEvaluationException("Size limited reached inside intersect operator");
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/limited/iterator/LimitedSizeIteratorUtil.java`
#### Snippet
```java
import java.util.concurrent.atomic.AtomicLong;

import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.query.BindingSet;
import org.eclipse.rdf4j.query.QueryEvaluationException;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/limited/iterator/LimitedSizeDistinctIteration.java`
#### Snippet
```java
	 * @param iter
	 */
	public LimitedSizeDistinctIteration(Iteration<? extends BindingSet, ? extends QueryEvaluationException> iter,
			AtomicLong used, long maxSize) {
		super(iter);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/limited/iterator/LimitedSizeDistinctIteration.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.DistinctIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.query.BindingSet;
import org.eclipse.rdf4j.query.QueryEvaluationException;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		@Override
		public boolean hasNext() throws QueryEvaluationException {
			return iteration.hasNext();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		@Override
		public boolean hasNext() throws QueryEvaluationException {
			return iteration.hasNext();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		@Override
		public BindingSet next() throws QueryEvaluationException {
			return convert(iteration.next());
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		@Override
		public void remove() throws QueryEvaluationException {
			iteration.remove();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		@Override
		public void remove() throws QueryEvaluationException {
			iteration.remove();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		@Override
		public BindingSet next() throws QueryEvaluationException {
			return convert(iteration.next());
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.StatementPatternCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.QueryOptimizer;
import org.eclipse.rdf4j.query.algebra.helpers.AbstractQueryModelVisitor;
import org.eclipse.rdf4j.query.algebra.helpers.StatementPatternCollector;
import org.eclipse.rdf4j.query.algebra.helpers.TupleExprs;

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.StatementPatternCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java

		protected List<Var> getStatementPatternVars(TupleExpr tupleExpr) {
			List<StatementPattern> stPatterns = StatementPatternCollector.process(tupleExpr);
			List<Var> varList = new ArrayList<>(stPatterns.size() * 4);
			for (StatementPattern sp : stPatterns) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.helpers.StatementPatternCollector' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java

		protected List<Var> getStatementPatternVars(TupleExpr tupleExpr) {
			List<StatementPattern> stPatterns = StatementPatternCollector.process(tupleExpr);
			List<Var> varList = new ArrayList<>(stPatterns.size() * 4);
			for (StatementPattern sp : stPatterns) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/MultiProjectionIterator.java`
#### Snippet
```java
					return result;
				}
			} else if (iter.hasNext()) {
				// Continue with the next result
				currentBindings = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/MultiProjectionIterator.java`
#### Snippet
```java
			} else if (iter.hasNext()) {
				// Continue with the next result
				currentBindings = iter.next();
				nextProjectionIdx = 0;
			} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/JoinIterator.java`
#### Snippet
```java

		try {
			while (rightIter.hasNext() || leftIter.hasNext()) {
				if (rightIter.hasNext()) {
					return rightIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/JoinIterator.java`
#### Snippet
```java

		try {
			while (rightIter.hasNext() || leftIter.hasNext()) {
				if (rightIter.hasNext()) {
					return rightIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/JoinIterator.java`
#### Snippet
```java
		try {
			while (rightIter.hasNext() || leftIter.hasNext()) {
				if (rightIter.hasNext()) {
					return rightIter.next();
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/JoinIterator.java`
#### Snippet
```java
			while (rightIter.hasNext() || leftIter.hasNext()) {
				if (rightIter.hasNext()) {
					return rightIter.next();
				}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/JoinIterator.java`
#### Snippet
```java
				rightIter.close();

				if (leftIter.hasNext()) {
					rightIter = preparedRight.evaluate(leftIter.next());
				}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/JoinIterator.java`
#### Snippet
```java

				if (leftIter.hasNext()) {
					rightIter = preparedRight.evaluate(leftIter.next());
				}
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DelayedEvaluationIteration.java`
#### Snippet
```java
		initialise();

		return iter.hasNext();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DelayedEvaluationIteration.java`
#### Snippet
```java
		initialise();

		return iter.next();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DelayedEvaluationIteration.java`
#### Snippet
```java
		}

		iter.remove();
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/LeftJoinIterator.java`
#### Snippet
```java
		try {
			CloseableIteration<BindingSet, QueryEvaluationException> nextRightIter = rightIter;
			while (nextRightIter.hasNext() || leftIter.hasNext()) {
				BindingSet leftBindings = null;

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/LeftJoinIterator.java`
#### Snippet
```java
		try {
			CloseableIteration<BindingSet, QueryEvaluationException> nextRightIter = rightIter;
			while (nextRightIter.hasNext() || leftIter.hasNext()) {
				BindingSet leftBindings = null;

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/LeftJoinIterator.java`
#### Snippet
```java
				BindingSet leftBindings = null;

				if (!nextRightIter.hasNext()) {
					// Use left arg's bindings in case join fails
					leftBindings = leftIter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/LeftJoinIterator.java`
#### Snippet
```java
				if (!nextRightIter.hasNext()) {
					// Use left arg's bindings in case join fails
					leftBindings = leftIter.next();

					nextRightIter.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/LeftJoinIterator.java`
#### Snippet
```java
				}

				while (nextRightIter.hasNext()) {
					BindingSet rightBindings = nextRightIter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/LeftJoinIterator.java`
#### Snippet
```java

				while (nextRightIter.hasNext()) {
					BindingSet rightBindings = nextRightIter.next();

					try {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		public BindingSet next() throws QueryEvaluationException {
			queryModelNode.setResultSizeActual(queryModelNode.getResultSizeActual() + 1);
			return iterator.next();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		@Override
		public boolean hasNext() throws QueryEvaluationException {
			return iterator.hasNext();
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	@Deprecated(forRemoval = true)
	public Value evaluate(Exists node, BindingSet bindings) throws QueryEvaluationException {
		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			return BooleanLiteral.valueOf(iter.hasNext());
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	public Value evaluate(Exists node, BindingSet bindings) throws QueryEvaluationException {
		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			return BooleanLiteral.valueOf(iter.hasNext());
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		} else if (expr instanceof Coalesce) {
			return new QueryValueEvaluationStep.Minimal(this, expr);
		} else if (expr instanceof Like) {
			return new QueryValueEvaluationStep.Minimal(this, expr);
		} else if (expr instanceof FunctionCall) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		} else if (expr instanceof In) {
			return new QueryValueEvaluationStep.Minimal(this, expr);
		} else if (expr instanceof CompareAny) {
			return new QueryValueEvaluationStep.Minimal(this, expr);
		} else if (expr instanceof CompareAll) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		} else if (expr instanceof CompareAny) {
			return new QueryValueEvaluationStep.Minimal(this, expr);
		} else if (expr instanceof CompareAll) {
			return new QueryValueEvaluationStep.Minimal(this, expr);
		} else if (expr instanceof Exists) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

	@Deprecated(forRemoval = true)
	public Value evaluate(CompareAll node, BindingSet bindings) throws QueryEvaluationException {
		Value leftValue = evaluate(node.getArg(), bindings);

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	@Deprecated(forRemoval = true)
	public Value evaluate(CompareAll node, BindingSet bindings) throws QueryEvaluationException {
		Value leftValue = evaluate(node.getArg(), bindings);

		// Result is true until a mismatch has been found
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

		// Use first binding name from tuple expr to compare values
		String bindingName = node.getSubQuery().getBindingNames().iterator().next();

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		String bindingName = node.getSubQuery().getBindingNames().iterator().next();

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();

				Value rightValue = bindingSet.getValue(bindingName);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

				try {
					result = QueryEvaluationUtil.compare(leftValue, rightValue, node.getOperator());
				} catch (ValueExprEvaluationException e) {
					// Exception thrown by ValueCompare.isTrue(...)
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	 */
	@Deprecated(forRemoval = true)
	public Value evaluate(Like node, BindingSet bindings) throws QueryEvaluationException {
		Value val = evaluate(node.getArg(), bindings);
		String strVal = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		}

		if (!node.isCaseSensitive()) {
			// Convert strVal to lower case, just like the pattern has been done
			strVal = strVal.toLowerCase();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		int valIndex = 0;
		int prevPatternIndex = -1;
		int patternIndex = node.getOpPattern().indexOf('*');

		if (patternIndex == -1) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		if (patternIndex == -1) {
			// No wildcards
			return BooleanLiteral.valueOf(node.getOpPattern().equals(strVal));
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		if (patternIndex > 0) {
			// Pattern does not start with a wildcard, first part must match
			snippet = node.getOpPattern().substring(0, patternIndex);
			if (!strVal.startsWith(snippet)) {
				return BooleanLiteral.FALSE;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			valIndex += snippet.length();
			prevPatternIndex = patternIndex;
			patternIndex = node.getOpPattern().indexOf('*', patternIndex + 1);
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		while (patternIndex != -1) {
			// Get snippet between previous wildcard and this wildcard
			snippet = node.getOpPattern().substring(prevPatternIndex + 1, patternIndex);

			// Search for the snippet in the value
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			valIndex += snippet.length();
			prevPatternIndex = patternIndex;
			patternIndex = node.getOpPattern().indexOf('*', patternIndex + 1);
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

		// Part after last wildcard
		snippet = node.getOpPattern().substring(prevPatternIndex + 1);

		if (snippet.length() > 0) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		} else if (expr instanceof Coalesce) {
			return evaluate((Coalesce) expr, bindings);
		} else if (expr instanceof Like) {
			return evaluate((Like) expr, bindings);
		} else if (expr instanceof FunctionCall) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			return evaluate((Coalesce) expr, bindings);
		} else if (expr instanceof Like) {
			return evaluate((Like) expr, bindings);
		} else if (expr instanceof FunctionCall) {
			return evaluate((FunctionCall) expr, bindings);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		} else if (expr instanceof In) {
			return evaluate((In) expr, bindings);
		} else if (expr instanceof CompareAny) {
			return evaluate((CompareAny) expr, bindings);
		} else if (expr instanceof CompareAll) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			return evaluate((In) expr, bindings);
		} else if (expr instanceof CompareAny) {
			return evaluate((CompareAny) expr, bindings);
		} else if (expr instanceof CompareAll) {
			return evaluate((CompareAll) expr, bindings);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		} else if (expr instanceof CompareAny) {
			return evaluate((CompareAny) expr, bindings);
		} else if (expr instanceof CompareAll) {
			return evaluate((CompareAll) expr, bindings);
		} else if (expr instanceof Exists) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			return evaluate((CompareAny) expr, bindings);
		} else if (expr instanceof CompareAll) {
			return evaluate((CompareAll) expr, bindings);
		} else if (expr instanceof Exists) {
			return evaluate((Exists) expr, bindings);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	private QueryValueEvaluationStep prepare(Exists node, QueryEvaluationContext context)
			throws QueryEvaluationException {
		QueryEvaluationStep subQuery = precompile(node.getSubQuery(), context);
		return bindings -> {
			try (CloseableIteration<BindingSet, QueryEvaluationException> iter = subQuery.evaluate(bindings)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		return bindings -> {
			try (CloseableIteration<BindingSet, QueryEvaluationException> iter = subQuery.evaluate(bindings)) {
				return BooleanLiteral.valueOf(iter.hasNext());
			}
		};
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	@Deprecated(forRemoval = true)
	public Value evaluate(In node, BindingSet bindings) throws QueryEvaluationException {
		Value leftValue = evaluate(node.getArg(), bindings);

		// Result is false until a match has been found
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

		// Use first binding name from tuple expr to compare values
		String bindingName = node.getSubQuery().getBindingNames().iterator().next();

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		String bindingName = node.getSubQuery().getBindingNames().iterator().next();

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (!result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (!result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (!result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();

				Value rightValue = bindingSet.getValue(bindingName);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		public BindingSet next() throws QueryEvaluationException {
			stopwatch.start();
			BindingSet next = iterator.next();
			stopwatch.stop();
			return next;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

	@Deprecated(forRemoval = true)
	public Value evaluate(CompareAny node, BindingSet bindings) throws QueryEvaluationException {
		Value leftValue = evaluate(node.getArg(), bindings);

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	@Deprecated(forRemoval = true)
	public Value evaluate(CompareAny node, BindingSet bindings) throws QueryEvaluationException {
		Value leftValue = evaluate(node.getArg(), bindings);

		// Result is false until a match has been found
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

		// Use first binding name from tuple expr to compare values
		String bindingName = node.getSubQuery().getBindingNames().iterator().next();

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		String bindingName = node.getSubQuery().getBindingNames().iterator().next();

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (!result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (!result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		try (CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(node.getSubQuery(), bindings)) {
			while (!result && iter.hasNext()) {
				BindingSet bindingSet = iter.next();

				Value rightValue = bindingSet.getValue(bindingName);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

				try {
					result = QueryEvaluationUtil.compare(leftValue, rightValue, node.getOperator());
				} catch (ValueExprEvaluationException e) {
					// ignore, maybe next value will match
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.Compare.CompareOp;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.Datatype;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Compare.CompareOp;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.Datatype;
import org.eclipse.rdf4j.query.algebra.DescribeOperator;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.Like' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.LangMatches;
import org.eclipse.rdf4j.query.algebra.LeftJoin;
import org.eclipse.rdf4j.query.algebra.Like;
import org.eclipse.rdf4j.query.algebra.ListMemberOperator;
import org.eclipse.rdf4j.query.algebra.LocalName;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/QueryContextIteration.java`
#### Snippet
```java
		queryContext.begin();
		try {
			return iter.hasNext();
		} finally {
			queryContext.end();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/QueryContextIteration.java`
#### Snippet
```java
		queryContext.begin();
		try {
			iter.remove();
		} finally {
			queryContext.end();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/QueryContextIteration.java`
#### Snippet
```java
		queryContext.begin();
		try {
			return iter.next();
		} finally {
			queryContext.end();
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/FilterIterator.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Filter;
import org.eclipse.rdf4j.query.algebra.QueryModelNode;
import org.eclipse.rdf4j.query.algebra.SubQueryValueOperator;
import org.eclipse.rdf4j.query.algebra.evaluation.EvaluationStrategy;
import org.eclipse.rdf4j.query.algebra.evaluation.QueryValueEvaluationStep;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/FilterIterator.java`
#### Snippet
```java
	@Deprecated(forRemoval = true, since = "4.2.1")
	public static boolean isPartOfSubQuery(QueryModelNode node) {
		if (node instanceof SubQueryValueOperator) {
			return true;
		}
```

### MarkedForRemoval
'setAnonymous(boolean)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ZeroLengthPathIteration.java`
#### Snippet
```java
	public Var createAnonVar(String varName) {
		Var var = new Var(varName);
		var.setAnonymous(true);
		return var;
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ZeroLengthPathIteration.java`
#### Snippet
```java
			}

			while (iter.hasNext()) {
				BindingSet bs = iter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ZeroLengthPathIteration.java`
#### Snippet
```java

			while (iter.hasNext()) {
				BindingSet bs = iter.next();

				boolean isSubjOrObj = bs.getValue(ANON_SEQUENCE_VAR).stringValue().equals("subject");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	 * @param rightArg An Iteration containing the set of elements that should be filtered from the main set.
	 */
	public SPARQLMinusIteration(Iteration<BindingSet, X> leftArg, Iteration<BindingSet, X> rightArg) {
		super(leftArg);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	 * @param rightArg An Iteration containing the set of elements that should be filtered from the main set.
	 */
	public SPARQLMinusIteration(Iteration<BindingSet, X> leftArg, Iteration<BindingSet, X> rightArg) {
		super(leftArg);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	}

	protected Set<BindingSet> makeSet(Iteration<BindingSet, X> rightArg) throws X {
		return Iterations.asSet(rightArg);
	}
```

### MarkedForRemoval
'asSet(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java

	protected Set<BindingSet> makeSet(Iteration<BindingSet, X> rightArg) throws X {
		return Iterations.asSet(rightArg);
	}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java

import org.eclipse.rdf4j.common.iteration.FilterIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.Iterations;
import org.eclipse.rdf4j.query.BindingSet;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	 * @return Returns the rightArg.
	 */
	protected Iteration<BindingSet, X> getRightArg() {
		return rightArg;
	}
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
			super.handleClose();
		} finally {
			Iterations.closeCloseable(getRightArg());
		}
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	 *-----------*/

	private final Iteration<BindingSet, X> rightArg;

	private boolean initialized;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.0.0", forRemoval = true)
	public SPARQLMinusIteration(Iteration<BindingSet, X> leftArg, Iteration<BindingSet, X> rightArg, boolean distinct) {
		this(leftArg, rightArg);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.0.0", forRemoval = true)
	public SPARQLMinusIteration(Iteration<BindingSet, X> leftArg, Iteration<BindingSet, X> rightArg, boolean distinct) {
		this(leftArg, rightArg);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
	protected BindingSet getNextElement() throws QueryEvaluationException {
		again: while (true) {
			while (currentIter != null && !currentIter.hasNext()) {
				Iterations.closeCloseable(currentIter);
				createIteration();
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
		again: while (true) {
			while (currentIter != null && !currentIter.hasNext()) {
				Iterations.closeCloseable(currentIter);
				createIteration();
				// stop condition: if the iter is an EmptyIteration
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
			}

			while (currentIter != null && currentIter.hasNext()) {
				BindingSet potentialNextElement = currentIter.next();
				MutableBindingSet nextElement;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java

			while (currentIter != null && currentIter.hasNext()) {
				BindingSet potentialNextElement = currentIter.next();
				MutableBindingSet nextElement;
				// if it is not a compatible type of BindingSet
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
						// new arbitrary-length path semantics: filter out
						// duplicates
						if (currentIter.hasNext()) {
							continue;
						} else {
```

### MarkedForRemoval
'closeCloseable(org.eclipse.rdf4j.common.iteration.Iteration)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
			super.handleClose();
		} finally {
			Iterations.closeCloseable(currentIter);
		}

```

### MarkedForRemoval
'setAnonymous(boolean)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
	public Var createAnonVar(String varName) {
		Var var = new Var(varName);
		var.setAnonymous(true);
		return var;
	}
```

### MarkedForRemoval
'setAnonymous(boolean)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
					Var startReplacement = createAnonVar(JOINVAR_PREFIX + currentLength + "_" + this.hashCode());
					Var endReplacement = createAnonVar("END_" + JOINVAR_PREFIX + this.hashCode());
					startReplacement.setAnonymous(false);
					endReplacement.setAnonymous(false);

```

### MarkedForRemoval
'setAnonymous(boolean)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
					Var endReplacement = createAnonVar("END_" + JOINVAR_PREFIX + this.hashCode());
					startReplacement.setAnonymous(false);
					endReplacement.setAnonymous(false);

					Value v = currentVp.getEndValue();
```

### MarkedForRemoval
'setValue(org.eclipse.rdf4j.model.Value)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java

					Value v = currentVp.getEndValue();
					startReplacement.setValue(v);

					VarReplacer replacer = new VarReplacer(startVar, startReplacement, 0, false);
```

### MarkedForRemoval
'setValue(org.eclipse.rdf4j.model.Value)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java

					Var replacement = createAnonVar(JOINVAR_PREFIX + currentLength + "-" + this.hashCode());
					replacement.setValue(v);

					VarReplacer replacer = new VarReplacer(toBeReplaced, replacement, 0, false);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/CrossProductIteration.java`
#### Snippet
```java
		if (currentInputBinding == null) {
			inputBindingsIterator = inputBindings.iterator();
			if (resultIteration.hasNext()) {
				currentInputBinding = resultIteration.next();
			} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/CrossProductIteration.java`
#### Snippet
```java
			inputBindingsIterator = inputBindings.iterator();
			if (resultIteration.hasNext()) {
				currentInputBinding = resultIteration.next();
			} else {
				return null; // no more results
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
			Map<Key, Entry> entries = new LinkedHashMap<>();

			if (!iter.hasNext()) {
				emptySolutionSpecialCase(aggregates, entries);
			} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
				emptySolutionSpecialCase(aggregates, entries);
			} else {
				while (iter.hasNext()) {
					BindingSet sol = iter.next();
					Key key = new Key(sol, hashMaker.apply(sol));
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
			} else {
				while (iter.hasNext()) {
					BindingSet sol = iter.next();
					Key key = new Key(sol, hashMaker.apply(sol));
					Entry entry = entries.get(key);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
				currentScanElem = removeFirstElement(scanList);
			} else {
				if (restIter.hasNext()) {
					currentScanElem = restIter.next();
				} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
			} else {
				if (restIter.hasNext()) {
					currentScanElem = restIter.next();
				} else {
					// no more elements available
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
		List<BindingSet> rightArgResults = makeList();

		while (leftIter.hasNext() && rightIter.hasNext()) {
			add(leftArgResults, leftIter.next());
			add(rightArgResults, rightIter.next());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
		List<BindingSet> rightArgResults = makeList();

		while (leftIter.hasNext() && rightIter.hasNext()) {
			add(leftArgResults, leftIter.next());
			add(rightArgResults, rightIter.next());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java

		while (leftIter.hasNext() && rightIter.hasNext()) {
			add(leftArgResults, leftIter.next());
			add(rightArgResults, rightIter.next());
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
		while (leftIter.hasNext() && rightIter.hasNext()) {
			add(leftArgResults, leftIter.next());
			add(rightArgResults, rightIter.next());
		}

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
		List<BindingSet> smallestResult;

		if (leftIter.hasNext()) { // leftArg is the greater relation
			smallestResult = rightArgResults;
			scanList = leftArgResults;
```

### MarkedForRemoval
'setValue(org.eclipse.rdf4j.model.Value)' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/SameTermFilterOptimizer.java`
#### Snippet
```java
		public void meet(Var var) {
			if (var.getName().equals(varName)) {
				var.setValue(value);
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
				disposeCache(scanList); // exhausted so can free

				if (restIter.hasNext()) {
					currentScanElem = restIter.next();
				} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java

				if (restIter.hasNext()) {
					currentScanElem = restIter.next();
				} else {
					// no more elements available
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
			leftArgResults = makeIterationCache(leftIter);

			while (leftIter.hasNext() && rightIter.hasNext()) {
				add(leftArgResults, leftIter.next());
				add(rightArgResults, rightIter.next());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
			leftArgResults = makeIterationCache(leftIter);

			while (leftIter.hasNext() && rightIter.hasNext()) {
				add(leftArgResults, leftIter.next());
				add(rightArgResults, rightIter.next());
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java

			while (leftIter.hasNext() && rightIter.hasNext()) {
				add(leftArgResults, leftIter.next());
				add(rightArgResults, rightIter.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
			while (leftIter.hasNext() && rightIter.hasNext()) {
				add(leftArgResults, leftIter.next());
				add(rightArgResults, rightIter.next());
			}
		} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
			leftArgResults = Collections.emptyList();

			while (rightIter.hasNext()) {
				add(rightArgResults, rightIter.next());
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java

			while (rightIter.hasNext()) {
				add(rightArgResults, rightIter.next());
			}
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
		Collection<BindingSet> smallestResult;

		if (leftJoin || leftIter.hasNext()) { // leftArg is the greater relation
			smallestResult = rightArgResults;
			scanList = leftArgResults.iterator();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
			CloseableIteration<BindingSet, QueryEvaluationException> bindings, String baseUri)
			throws QueryEvaluationException {
		if (!bindings.hasNext()) {
			return QueryEvaluationStep.EMPTY_ITERATION;
		}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java

		List<CloseableIteration<BindingSet, QueryEvaluationException>> resultIters = new ArrayList<>();
		while (bindings.hasNext()) {
			BindingSet bs = bindings.next();
			Value[] argValues = new Value[argExprs.size()];
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
		List<CloseableIteration<BindingSet, QueryEvaluationException>> resultIters = new ArrayList<>();
		while (bindings.hasNext()) {
			BindingSet bs = bindings.next();
			Value[] argValues = new Value[argExprs.size()];
			for (int i = 0; i < argExprs.size(); i++) {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
					return false;
				}
				boolean result = iter.hasNext();
				if (!result) {
					close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
				}
				try {
					iter.remove();
				} catch (IllegalStateException e) {
					close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
		try (final CloseableIteration<BindingSet, QueryEvaluationException> iter = evaluate(service,
				new SingletonIteration<>(bindings), baseUri)) {
			if (iter.hasNext()) {
				BindingSet bs = iter.next();
				String firstVar = service.getBindingNames().iterator().next();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
				new SingletonIteration<>(bindings), baseUri)) {
			if (iter.hasNext()) {
				BindingSet bs = iter.next();
				String firstVar = service.getBindingNames().iterator().next();
				return QueryEvaluationUtil.getEffectiveBooleanValue(bs.getValue(firstVar));
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
				try {
					QueryBindingSet projected = new QueryBindingSet();
					BindingSet result = iter.next();
					for (String var : projectionVars) {
						Value v = result.getValue(var);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
import org.eclipse.rdf4j.common.iteration.CloseableIteration;
import org.eclipse.rdf4j.common.iteration.EmptyIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.LookAheadIteration;
import org.eclipse.rdf4j.model.BNode;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
		}

		while (!currentDescribeExprIter.hasNext() && !nodeQueue.isEmpty()) {
			// process next node in queue
			BNode nextNode = nodeQueue.poll();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
			processedNodes.add(nextNode);

			if (nodeQueue.isEmpty() && !currentDescribeExprIter.hasNext()) {
				// we have hit a blank node that has no further expansion. reset to
				// initialize next in value expression queue.
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
		}

		if (currentDescribeExprIter.hasNext()) {
			BindingSet bs = currentDescribeExprIter.next();

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java

		if (currentDescribeExprIter.hasNext()) {
			BindingSet bs = currentDescribeExprIter.next();

			String varname = currentMode == Mode.OUTGOING_LINKS ? VARNAME_OBJECT : VARNAME_SUBJECT;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
			}

			if (!currentDescribeExprIter.hasNext() && nodeQueue.isEmpty()) {
				currentDescribeExprIter.close();
				currentDescribeExprIter = null;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
		while (currentDescribeExprIter == null) {
			if (currentBindings == null && startValue == null) {
				if (sourceIter.hasNext()) {
					currentBindings = sourceIter.next();
				} else {
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
			if (currentBindings == null && startValue == null) {
				if (sourceIter.hasNext()) {
					currentBindings = sourceIter.next();
				} else {
					// no more bindings, therefore no more results to return.
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
			case OUTGOING_LINKS:
				currentDescribeExprIter = createNextIteration(startValue, null);
				if (!currentDescribeExprIter.hasNext()) {
					// start value has no outgoing links.
					currentDescribeExprIter.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
			case INCOMING_LINKS:
				currentDescribeExprIter = createNextIteration(null, startValue);
				if (!currentDescribeExprIter.hasNext()) {
					// no incoming links for this start value.
					currentDescribeExprIter.close();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
	private Mode currentMode = Mode.OUTGOING_LINKS;

	private final Iteration<BindingSet, QueryEvaluationException> sourceIter;

	public DescribeIteration(Iteration<BindingSet, QueryEvaluationException> sourceIter, EvaluationStrategy strategy,
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
	private final Iteration<BindingSet, QueryEvaluationException> sourceIter;

	public DescribeIteration(Iteration<BindingSet, QueryEvaluationException> sourceIter, EvaluationStrategy strategy,
			Set<String> describeExprNames, BindingSet parentBindings) {
		this.strategy = strategy;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java

	@Override
	protected Iteration<BindingSet, QueryEvaluationException> createIteration() throws QueryEvaluationException {
		BindingSet threshold = null;
		List<BindingSet> list = new LinkedList<>();
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
		int syncThreshold = (int) Math.min(iterationSyncThreshold, Integer.MAX_VALUE);
		try {
			while (iter.hasNext()) {
				if (list.size() >= syncThreshold && list.size() < limit) {
					SerializedQueue<BindingSet> queue = new SerializedQueue<>("orderiter");
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
					}
				}
				BindingSet next = iter.next();
				if (threshold == null || comparator.compare(next, threshold) < 0) {
					list.add(next);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
import org.eclipse.rdf4j.common.iteration.CloseableIteratorIteration;
import org.eclipse.rdf4j.common.iteration.DelayedIteration;
import org.eclipse.rdf4j.common.iteration.Iteration;
import org.eclipse.rdf4j.common.iteration.LimitIteration;
import org.eclipse.rdf4j.query.BindingSet;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/ServiceJoinIterator.java`
#### Snippet
```java
			// => use a fallback to the naive evaluation.
			// exceptions occurring here must NOT be silenced!
			while (!isClosed() && leftIter.hasNext()) {
				addResult(strategy.evaluate(service, leftIter.next()));
			}
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/ServiceJoinIterator.java`
#### Snippet
```java
			// exceptions occurring here must NOT be silenced!
			while (!isClosed() && leftIter.hasNext()) {
				addResult(strategy.evaluate(service, leftIter.next()));
			}
			return;
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
								stmt.getContext()
						)) {
							if (statements.hasNext()) {
								Value object = statements.next().getObject();
								addValue = object.isIRI() && objects.contains((IRI) object);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
						)) {
							if (statements.hasNext()) {
								Value object = statements.next().getObject();
								addValue = object.isIRI() && objects.contains((IRI) object);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
							subject, null, null, false
					)) {
						while (statements.hasNext()) {
							Statement statement = statements.next();
							statement = sail.mapStatement(statement);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
					)) {
						while (statements.hasNext()) {
							Statement statement = statements.next();
							statement = sail.mapStatement(statement);

```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
						subject, null, null, false
				)) {
					while (statements.hasNext()) {
						Statement statement = statements.next();
						statement = sail.mapStatement(statement);
```

### MarkedForRemoval
'org.eclipse.rdf4j.common.iteration.Iteration' is deprecated and marked for removal
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
				)) {
					while (statements.hasNext()) {
						Statement statement = statements.next();
						statement = sail.mapStatement(statement);

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
	@Override
	public void meet(In theOp) throws Exception {
		theOp.getArg().visit(this);
		mBuffer.append(" in ");
		mBuffer.append("(");
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
		mBuffer.append(" in ");
		mBuffer.append("(");
		mBuffer.append(renderTupleExpr(theOp.getSubQuery()));
		mBuffer.append(")");
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Bound;
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.Count;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.Count;
import org.eclipse.rdf4j.query.algebra.Datatype;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
	 */
	@Override
	public void meet(CompareAny theOp) throws Exception {
		mBuffer.append("(");
		theOp.getArg().visit(this);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
	public void meet(CompareAny theOp) throws Exception {
		mBuffer.append("(");
		theOp.getArg().visit(this);
		mBuffer.append(" ").append(theOp.getOperator().getSymbol()).append(" any ");
		mBuffer.append("(");
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
		mBuffer.append("(");
		theOp.getArg().visit(this);
		mBuffer.append(" ").append(theOp.getOperator().getSymbol()).append(" any ");
		mBuffer.append("(");
		mBuffer.append(renderTupleExpr(theOp.getSubQuery()));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
		mBuffer.append(" ").append(theOp.getOperator().getSymbol()).append(" any ");
		mBuffer.append("(");
		mBuffer.append(renderTupleExpr(theOp.getSubQuery()));
		mBuffer.append(")");
		mBuffer.append(")");
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
	 */
	@Override
	public void meet(CompareAll theOp) throws Exception {
		mBuffer.append("(");
		theOp.getArg().visit(this);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
	public void meet(CompareAll theOp) throws Exception {
		mBuffer.append("(");
		theOp.getArg().visit(this);
		mBuffer.append(" ").append(theOp.getOperator().getSymbol()).append(" all ");
		mBuffer.append("(");
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
		mBuffer.append("(");
		theOp.getArg().visit(this);
		mBuffer.append(" ").append(theOp.getOperator().getSymbol()).append(" all ");
		mBuffer.append("(");
		mBuffer.append(renderTupleExpr(theOp.getSubQuery()));
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
		mBuffer.append(" ").append(theOp.getOperator().getSymbol()).append(" all ");
		mBuffer.append("(");
		mBuffer.append(renderTupleExpr(theOp.getSubQuery()));
		mBuffer.append(")");
		mBuffer.append(")");
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlValueExprRenderer.java`
#### Snippet
```java
	public void meet(Exists theOp) throws Exception {
		mBuffer.append(" exists(");
		mBuffer.append(renderTupleExpr(theOp.getSubQuery()));
		mBuffer.append(")");
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java

	@Override
	public void meet(CompareAll node) throws RuntimeException {
		super.meet(node);

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.SubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
	public void meet(Exists node) throws RuntimeException {
		builder.append("EXISTS {");
		node.getSubQuery().visit(this);
		builder.append("} ");
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java

	@Override
	public void meet(CompareAny node) throws RuntimeException {
		super.meet(node);

```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Coalesce;
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.Copy;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.Copy;
import org.eclipse.rdf4j.query.algebra.Count;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Coalesce;
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.Copy;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.Compare;
import org.eclipse.rdf4j.query.algebra.CompareAll;
import org.eclipse.rdf4j.query.algebra.CompareAny;
import org.eclipse.rdf4j.query.algebra.Copy;
import org.eclipse.rdf4j.query.algebra.Count;
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAll' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java

	@Override
	public void meet(CompareAll node) throws RuntimeException {
		ensureNonAnonymousVar(node.getArg());
		super.meet(node);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	@Override
	public void meet(CompareAll node) throws RuntimeException {
		ensureNonAnonymousVar(node.getArg());
		super.meet(node);
	}
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareAny' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java

	@Override
	public void meet(CompareAny node) throws RuntimeException {
		ensureNonAnonymousVar(node.getArg());
		super.meet(node);
```

### MarkedForRemoval
'org.eclipse.rdf4j.query.algebra.CompareSubQueryValueOperator' is deprecated and marked for removal
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	@Override
	public void meet(CompareAny node) throws RuntimeException {
		ensureNonAnonymousVar(node.getArg());
		super.meet(node);
	}
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`if` statement has empty body
in `core/rio/binary/src/main/java/org/eclipse/rdf4j/rio/binary/BinaryRDFParser.java`
#### Snippet
```java

			// Check format version (parser is backward-compatible with version 1 and version 2)
			if (formatVersion == FORMAT_V1) {
			} else if (formatVersion == FORMAT_V2) {
				charset = Charset.forName(readString());
```

### EmptyStatementBody
`if` statement has empty body
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/StatementPatternVisitor.java`
#### Snippet
```java
		} else if (node instanceof BinaryValueOperator) {
			((BinaryValueOperator) node).visitChildren(this);
		} else if (node instanceof UpdateExpr) {

		}
```

### EmptyStatementBody
`if` statement has empty body
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
			try {
				int code = response.getStatusLine().getStatusCode();
				if (code == HttpURLConnection.HTTP_OK) {
				} else {
					throw new RepositoryException("unable to prepare transaction. HTTP error code " + code);
```

### EmptyStatementBody
`while` statement has empty body
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSail.java`
#### Snippet
```java
	 */
	protected void removeSupportedIsolationLevel(IsolationLevel level) {
		while (this.supportedIsolationLevels.remove(level)) {
		}
	}
```

### EmptyStatementBody
`while` statement has empty body
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/RangeIterator.java`
#### Snippet
```java
					tree.btreeLock.readLock().lock();
					try {
						while (popStacks()) {
						}

```

## RuleId[id=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceBranch.java`
#### Snippet
```java
			}
			Iterator<Changeset> iter = changes.iterator();
			while (iter.hasNext()) {
				derivedFrom = new SailDatasetImpl(derivedFrom, iter.next());
			}
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/ServiceJoinConversionIteration.java`
#### Snippet
```java
		int bIndex = -1;
		Iterator<Binding> bIter = bIn.iterator();
		while (bIter.hasNext()) {
			Binding b = bIter.next();
			String name = b.getName();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java

			Iterator<Binding> bIter = bindings.iterator();
			while (bIter.hasNext()) {
				Binding b = bIter.next();
				if (service.getServiceVars().contains(b.getName())) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java

			Iterator<Binding> bIter = bindings.iterator();
			while (bIter.hasNext()) {
				Binding b = bIter.next();
				if (service.getServiceVars().contains(b.getName())) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/AbstractSPARQLJSONWriter.java`
#### Snippet
```java

			Iterator<Binding> bindingIter = bindingSet.iterator();
			while (bindingIter.hasNext()) {
				Binding binding = bindingIter.next();
				jg.writeFieldName(binding.getName());
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
			int bindingCount = 0;
			Iterator<Binding> bindingIter = resultBindings.iterator();
			while (bindingIter.hasNext()) {
				bindingIter.next();
				bindingCount++;
```

## RuleId[id=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of local variable `b`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionSAXParser.java`
#### Snippet
```java
			currentDataset = null;
		} else if (TransactionXMLConstants.BINDINGS.equals(tagName)) {
			Binding b[] = bindings.toArray(new Binding[0]);
			currentSPARQLUpdate.setBindings(b);
			bindings.clear();
```

### CStyleArrayDeclaration
C-style array declaration of field `quad`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java

	static class Record {
		long quad[];
		boolean add;
	}
```

### CStyleArrayDeclaration
C-style array declaration of local variable `quad`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
				}
				if (recordCache != null) {
					long quad[] = new long[] { subj, pred, obj, context };
					if (explicit && foundImplicit) {
						// remove implicit statement
```

### CStyleArrayDeclaration
C-style array declaration of local variable `point`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneDocument.java`
#### Snippet
```java
				}
			} else if (shape instanceof double[]) { // WKT:POINT
				double point[] = (double[]) shape;
				doc.add(new LatLonPoint(POINT_FIELD_PREFIX + field, point[1], point[0]));
			} else if (shape instanceof Rectangle) { // WKT:ENVELOPE / RECTANGLE
```

### CStyleArrayDeclaration
C-style array declaration of parameter `cbuf`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IndentingWriter.java`
#### Snippet
```java

	@Override
	public void write(char cbuf[], int off, int len) throws IOException {
		if (!indentationWritten) {
			for (int i = 0; i < indentationLevel; i++) {
```

### CStyleArrayDeclaration
C-style array declaration of field `MAGIC_NUMBER`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/GZipUtil.java`
#### Snippet
```java
	 * little endian).
	 */
	private final static byte MAGIC_NUMBER[] = { (byte) 0x1f, (byte) 0x8b };

	/**
```

### CStyleArrayDeclaration
C-style array declaration of field `MAGIC_NUMBER`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ZipUtil.java`
#### Snippet
```java
	 * Magic number for ZIP files (4 bytes: <var>0x04034b50</var>).
	 */
	private final static byte MAGIC_NUMBER[] = { (byte) 0x50, (byte) 0x4B, (byte) 0x03, (byte) 0x04 };

	/**
```

### CStyleArrayDeclaration
C-style array declaration of local variable `b`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTHeader.java`
#### Snippet
```java
	 */
	private byte[] parseHeaderData(InputStream is, int len) throws IOException {
		byte b[] = new byte[len];
		is.read(b);
		return b;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `strs`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java
		}

		String strs[] = new String(props, 0, props.length, StandardCharsets.US_ASCII).split(";");
		for (String str : strs) {
			String prop[] = str.split("=");
```

### CStyleArrayDeclaration
C-style array declaration of local variable `prop`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java
		String strs[] = new String(props, 0, props.length, StandardCharsets.US_ASCII).split(";");
		for (String str : strs) {
			String prop[] = str.split("=");
			if (prop.length == 2) {
				map.put(prop[0], prop[1]);
```

### CStyleArrayDeclaration
C-style array declaration of field `buffer`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTArrayLog64.java`
#### Snippet
```java
 */
class HDTArrayLog64 extends HDTArray {
	private byte buffer[];

	@Override
```

## RuleId[id=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `max` from instance context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/StatementMatcher.java`
#### Snippet
```java
			// this isn't really threadsafe, but that is ok because the variable is just used as a guide
			if (counter > max) {
				max = counter;
			}
			return current();
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (4 lines)
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclUnsupportedException.java`
#### Snippet
```java
	}

//	@Override
//	public synchronized Throwable fillInStackTrace() {
//		return this;
```

### CommentedOutCode
Commented out code (6 lines)
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/DashAllObjects.java`
#### Snippet
```java
			StatementMatcher.StableRandomVariableProvider stableRandomVariableProvider) {

//		return targetObjectsOf.stream()
//			.map(target -> "\n{BIND(<" + target + "> as " + tempVar + ")\n" + objectVariable + " "
//				+ tempVar + " " + subjectVariable
```

### CommentedOutCode
Commented out code (6 lines)
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/DashAllSubjects.java`
#### Snippet
```java
			StatementMatcher.StableRandomVariableProvider stableRandomVariableProvider) {

//		return targetObjectsOf.stream()
//			.map(target -> "\n{ BIND(<" + target + "> as " + tempVar + ")\n" + objectVariable + " "
//				+ tempVar + " " + subjectVariable
```

### CommentedOutCode
Commented out code (4 lines)
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
			properties.getTarget()
					.forEach(target -> {
//									if (shapeSource.hasStatement(sparqlTarget, RDF.TYPE, SHACL.SPARQL_TARGET, true)) {
//										propertyShapes.add(new SparqlTarget(shapeId, shaclSail, shapeSource,
//												shaclProperties.isDeactivated(), target));
```

### CommentedOutCode
Commented out code (2 lines)
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/UniqueLangConstraintComponent.java`
#### Snippet
```java
	public PlanNode generateTransactionalValidationPlan(ConnectionsGroup connectionsGroup,
			ValidationSettings validationSettings, PlanNodeProvider overrideTargetNode, Scope scope) {
//		assert !negateChildren : "There are no subplans!";
//		assert !negatePlan;

```

### CommentedOutCode
Commented out code (13 lines)
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java
					validationResultsRet.add(validationResult1);

//					ValidationResult parent = null;
//
//					// we iterate in reverse order to get the most recent validation result first
```

### CommentedOutCode
Commented out code (4 lines)
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/ValidationResult.java`
#### Snippet
```java

		// TODO: Figure out how sh:detail should work!
//		if (detail != null) {
//			model.add(getId(), SHACL.DETAIL, detail.getId());
//			detail.asModel(model);
```

### CommentedOutCode
Commented out code (2 lines)
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
			throw new AssertionError("Unexpected rule: " + rule);
		}
		// ThreadLog.trace("Rule " + RDFSRules.RULENAMES[rule] + " inferred " +
		// result + " new triples.");
		return result;
```

### CommentedOutCode
Commented out code (8 lines)
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/config/HTTPRepositoryConfig.java`
#### Snippet
```java
			graph.add(implNode, REPOSITORYURL, SimpleValueFactory.getInstance().createIRI(url));
		}
		// if (username != null) {
		// graph.add(implNode, USERNAME,
		// graph.getValueFactory().createLiteral(username));
```

### CommentedOutCode
Commented out code (2 lines)
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/RioConfig.java`
#### Snippet
```java
				settings.put((RioSetting<Object>) setting, value);

				// this.log.trace("Overriding previous setting for {}",
				// setting.getKey());
			}
```

### CommentedOutCode
Commented out code (2 lines)
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/NegativeParserTest.java`
#### Snippet
```java
		try {
			// Try parsing the input; this should result in an error being
			// reported.
			// targetParser.setDatatypeHandling(RDFParser.DatatypeHandling.IGNORE);

```

### CommentedOutCode
Commented out code (3 lines)
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/n3/N3ParserTestCase.java`
#### Snippet
```java
			if (!Models.isomorphic(inputCollection, outputCollection)) {
				System.err.println("===models not equal===");
				// System.err.println("Expected: " + outputCollection);
				// System.err.println("Actual : " + inputCollection);
				// System.err.println("======================");
```

### CommentedOutCode
Commented out code (6 lines)
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/turtle/TurtleParserTestCase.java`
#### Snippet
```java
			String nextBaseUrl = testBaseUrl + nextTestFile;

			// if (nextTestName.contains("CARRIAGE_RETURN")) {
			// // FIXME: Sesame seems not to preserve the CARRIAGE_RETURN character
			// // right now
```

### CommentedOutCode
Commented out code (8 lines)
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java

			if (outputURL != null) {
				// System.out.println(this.outputURL);
				//
				// NTriplesWriter nTriplesWriter = new NTriplesWriter(System.out);
```

### CommentedOutCode
Commented out code (3 lines)
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
				compareTupleQueryResults(queryResult, expectedResult);

				// Graph queryGraph = RepositoryUtil.asGraph(queryResult);
				// Graph expectedGraph = readExpectedTupleQueryResult();
				// compareGraphs(queryGraph, expectedGraph);
```

### CommentedOutCode
Commented out code (4 lines)
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemValueFactory.java`
#### Snippet
```java
	 */

//	private final Cache<Value, MemLiteral> literalCache = CacheBuilder.newBuilder().concurrencyLevel(Runtime.getRuntime().availableProcessors()).weakKeys().weakValues().initialCapacity(1000).maximumSize(1000).build();
//	private final Cache<Value, MemIRI> iriCache = CacheBuilder.newBuilder().concurrencyLevel(Runtime.getRuntime().availableProcessors()).weakKeys().weakValues().initialCapacity(1000).maximumSize(1000).build();
//	private final Cache<Value, MemBNode> bNodeCache = CacheBuilder.newBuilder().concurrencyLevel(Runtime.getRuntime().availableProcessors()).weakKeys().weakValues().initialCapacity(1000).maximumSize(1000).build();
```

### CommentedOutCode
Commented out code (3 lines)
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
	}

	// public static boolean isLegalName(String name) {
	// return isPN_LOCAL(name);
	// }
```

### CommentedOutCode
Commented out code (5 lines)
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/DestinationSourceManagementQuery.java`
#### Snippet
```java
		appendSilent(query);

//		if(fromDefault) {
//			query.append(DEFAULT);
//		} else {
```

### CommentedOutCode
Commented out code (5 lines)
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/DestinationSourceManagementQuery.java`
#### Snippet
```java
		query.append(to.filter(t -> !toDefault).map(Iri::getQueryString).orElse(DEFAULT));

//		if(toDefault) {
//			query.append(DEFAULT);
//		} else {
```

### CommentedOutCode
Commented out code (2 lines)
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/AbstractRepositoryImplConfig.java`
#### Snippet
```java
	public static RepositoryImplConfig create(Model model, Resource resource) throws RepositoryConfigException {
		try {
			// Literal typeLit = GraphUtil.getOptionalObjectLiteral(graph,
			// implNode, REPOSITORYTYPE);

```

### CommentedOutCode
Commented out code (4 lines)
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RDFSchemaRepositoryConnectionTest.java`
#### Snippet
```java
		// already existed in some other context. There is no longer a check for if an inferred statement exists in
		// other contexts.
		// assertFalse("input triple should not have been re-added as inferred", testCon.hasStatement(FOAF.PERSON,
		// RDF.TYPE, RDFS.CLASS, true, (Resource) null));
		// assertFalse("input triple should not have been re-added as inferred", testCon.hasStatement(FOAF.PERSON,
```

### CommentedOutCode
Commented out code (3 lines)
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java

		// Only useful for debugging.
		// if (deferredElement == null) {
		// throw new RuntimeException("no deferred start element available");
		// }
```

### CommentedOutCode
Commented out code (2 lines)
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	GraphPattern graphPattern = new GraphPattern();

	// private Map<ValueConstant, Var> mappedValueConstants = new
	// HashMap<ValueConstant, Var>();

```

### CommentedOutCode
Commented out code (2 lines)
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDWriter.java`
#### Snippet
```java
		try {
			final JsonLdOptions opts = new JsonLdOptions();
			// opts.addBlankNodeIDs =
			// getWriterConfig().get(BasicParserSettings.PRESERVE_BNODE_IDS);
			WriterConfig writerConfig = getWriterConfig();
```

### CommentedOutCode
Commented out code (9 lines)
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/Main.java`
#### Snippet
```java
	}

//	public static void main(String[] args) throws RunnerException {
//		Options opt = new OptionsBuilder().include(
//				"org.eclipse.rdf4j.benchmark.ReasoningBenchmark.forwardChainingSchemaCachingRDFSInferencerMultipleTransactions$")
```

### CommentedOutCode
Commented out code (6 lines)
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
		nodeSize = 8 + (branchFactor - 1) * slotSize;

		// System.out.println("blockSize=" + this.blockSize);
		// System.out.println("valueSize=" + this.valueSize);
		// System.out.println("slotSize=" + this.slotSize);
```

### CommentedOutCode
Commented out code (4 lines)
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtil.java`
#### Snippet
```java
	}

//	public static boolean isPlainLiteral(Literal l) {
//		return l.getCoreDatatype().filter(d -> d == CoreDatatype.XSD.STRING).isPresent();
////		return l.getCoreDatatype().orElse(null) == CoreDatatype.XSD.STRING;
```

### CommentedOutCode
Commented out code (4 lines)
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtility.java`
#### Snippet
```java
	}

//	public static boolean isPlainLiteral(Literal l) {
//		return l.getCoreDatatype().filter(d -> d == CoreDatatype.XSD.STRING).isPresent();
////		return l.getCoreDatatype().orElse(null) == CoreDatatype.XSD.STRING;
```

### CommentedOutCode
Commented out code (2 lines)
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
			new QueryModelNormalizerOptimizer().optimize(tupleExpr, dataset, bindings);
			new QueryJoinOptimizer(new TupleFunctionEvaluationStatistics()).optimize(tupleExpr, dataset, bindings);
			// new SubSelectJoinOptimizer().optimize(tupleExpr, dataset,
			// bindings);
			new IterativeEvaluationOptimizer().optimize(tupleExpr, dataset, bindings);
```

### CommentedOutCode
Commented out code (2 lines)
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/AbstractSPARQLXMLParser.java`
#### Snippet
```java
					}
					// if there were no exceptions up to this point, return the
					// boolean
					// result;
					return result;
```

### CommentedOutCode
Commented out code (3 lines)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedUpdate.java`
#### Snippet
```java
	public UpdateExpr updateExpr = null;

//    public Group groupBy = null;
//    public Order orderBy = null;
//    public Filter having = null;
```

### CommentedOutCode
Commented out code (4 lines)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlTupleExprRenderer.java`
#### Snippet
```java
	}

	// @Override
	// public void meet(Coalesce node) throws Exception {
	// mJoinBuffer.append(renderValueExpr(node));
```

### CommentedOutCode
Commented out code (7 lines)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlTupleExprRenderer.java`
#### Snippet
```java
		SparqlTupleExprRenderer aRenderer = new SparqlTupleExprRenderer();

		// aRenderer.mProjection = new ArrayList<ProjectionElemList>(mProjection);
		// aRenderer.mDistinct = mDistinct;
		// aRenderer.mReduced = mReduced;
```

### CommentedOutCode
Commented out code (3 lines)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlTupleExprRenderer.java`
#### Snippet
```java
		mJoinBuffer.append(indent());

		// if (aNeedsNewScope) {
		// mJoinBuffer.append("{ ");
		// }
```

### CommentedOutCode
Commented out code (3 lines)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlTupleExprRenderer.java`
#### Snippet
```java
		mJoinBuffer.append("filter ").append(aFilter).append(".");

		// if (aNeedsNewScope) {
		// mJoinBuffer.append("}.");
		// }
```

### CommentedOutCode
Commented out code (12 lines)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
	}

//    @Override
//    public void meetOther(QueryModelNode node) throws RuntimeException {
//        if (node instanceof NaryJoin) {
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
			String replacement = valueToString(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				select = select.replaceAll(pattern, "(" + Matcher.quoteReplacement(replacement) + " as ?" + name + ")");

```

### RegExpRedundantEscape
Redundant character escape `\\$` in RegExp
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
			String replacement = valueToString(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				select = select.replaceAll(pattern, "(" + Matcher.quoteReplacement(replacement) + " as ?" + name + ")");

```

### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
			String replacement = valueToString(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				// we use Matcher.quoteReplacement to make sure things like newlines
				// in literal values are preserved
```

### RegExpRedundantEscape
Redundant character escape `\\$` in RegExp
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
			String replacement = valueToString(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				// we use Matcher.quoteReplacement to make sure things like newlines
				// in literal values are preserved
```

### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
			String replacement = getReplacement(bindings.getValue(name));
			if (replacement != null) {
				String pattern = ".*[\\?\\$]" + name + "\\W.*";
				if (Pattern.compile(pattern, Pattern.MULTILINE | Pattern.DOTALL).matcher(select).matches()) {
					names.add(name);
```

### RegExpRedundantEscape
Redundant character escape `\\$` in RegExp
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
			String replacement = getReplacement(bindings.getValue(name));
			if (replacement != null) {
				String pattern = ".*[\\?\\$]" + name + "\\W.*";
				if (Pattern.compile(pattern, Pattern.MULTILINE | Pattern.DOTALL).matcher(select).matches()) {
					names.add(name);
```

### RegExpRedundantEscape
Redundant character escape `\\?` in RegExp
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
			String replacement = getReplacement(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				select = select.replaceAll(pattern, "");
				where = where.replaceAll(pattern, replacement);
```

### RegExpRedundantEscape
Redundant character escape `\\$` in RegExp
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
			String replacement = getReplacement(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				select = select.replaceAll(pattern, "");
				where = where.replaceAll(pattern, replacement);
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/config/CustomGraphQueryInferencerConfig.java`
#### Snippet
```java
	static {
		int flags = Pattern.CASE_INSENSITIVE | Pattern.DOTALL;
		SPARQL_PATTERN = Pattern.compile("^(.*construct\\s+)(\\{.*\\}\\s*)where.*$", flags);
	}

```

### RegExpRedundantEscape
Redundant character escape `\\+` in RegExp
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
			writeURI(datatype);
		} else if (label.length() > 0 && encoded.equals(label) && label.charAt(0) != '<' && label.charAt(0) != '_'
				&& !label.matches("^[\\+\\-]?[\\d\\.].*")) {
			// no need to include double quotes
			writer.write(encoded);
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
			writeURI(datatype);
		} else if (label.length() > 0 && encoded.equals(label) && label.charAt(0) != '<' && label.charAt(0) != '_'
				&& !label.matches("^[\\+\\-]?[\\d\\.].*")) {
			// no need to include double quotes
			writer.write(encoded);
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/ConfigTemplate.java`
#### Snippet
```java
	 *-----------*/

	private static final Pattern TOKEN_PATTERN = Pattern.compile("\\{%[\\p{Print}&&[^\\}]]+%\\}");

	/*-----------*
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/ConfigTemplate.java`
#### Snippet
```java
	 *-----------*/

	private static final Pattern TOKEN_PATTERN = Pattern.compile("\\{%[\\p{Print}&&[^\\}]]+%\\}");

	/*-----------*
```

### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/ReasoningUpdateBenchmark.java`
#### Snippet
```java

	private InputStream resourceAsStream(String resourceName) {
		String[] split = param.split("\\:\\:");
		this.expectedCount = Integer.parseInt(split[1]);
		String filename = split[0] + "/" + resourceName;
```

### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/ReasoningUpdateBenchmark.java`
#### Snippet
```java

	private InputStream resourceAsStream(String resourceName) {
		String[] split = param.split("\\:\\:");
		this.expectedCount = Integer.parseInt(split[1]);
		String filename = split[0] + "/" + resourceName;
```

## RuleId[id=ThreadDumpStack]
### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

### ThreadDumpStack
Call to `Thread.dumpStack()` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java
		}
		if (dumpingStack) {
			Thread.dumpStack();
		}
	}
```

## RuleId[id=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `prefixDeclarations.keySet()` may be replaced with 'entrySet()' iteration
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Service.java`
#### Snippet
```java

		StringBuilder sb = new StringBuilder();
		for (String prefix : prefixDeclarations.keySet()) {
			String uri = prefixDeclarations.get(prefix);
			sb.append("PREFIX ").append(prefix).append(":").append(" <").append(uri).append("> ");
```

### KeySetIterationMayUseEntrySet
Iteration over `inputNamedGraphs.keySet()` may be replaced with 'entrySet()' iteration
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11UpdateComplianceTest.java`
#### Snippet
```java
			}

			for (String ng : inputNamedGraphs.keySet()) {
				URL graphURL = new URL(inputNamedGraphs.get(ng).stringValue());
				conn.add(graphURL, null,
```

### KeySetIterationMayUseEntrySet
Iteration over `resultNamedGraphs.keySet()` may be replaced with 'entrySet()' iteration
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11UpdateComplianceTest.java`
#### Snippet
```java
			}

			for (String ng : resultNamedGraphs.keySet()) {
				URL graphURL = new URL(resultNamedGraphs.get(ng).stringValue());
				conn.add(graphURL, null,
```

### KeySetIterationMayUseEntrySet
Iteration over `inputNamedGraphs.keySet()` may be replaced with 'entrySet()' iteration
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
			}

			for (String ng : inputNamedGraphs.keySet()) {
				URL graphURL = new URL(inputNamedGraphs.get(ng).stringValue());
				conn.add(graphURL, null,
```

### KeySetIterationMayUseEntrySet
Iteration over `resultNamedGraphs.keySet()` may be replaced with 'entrySet()' iteration
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
			}

			for (String ng : resultNamedGraphs.keySet()) {
				URL graphURL = new URL(resultNamedGraphs.get(ng).stringValue());
				conn.add(graphURL, null,
```

### KeySetIterationMayUseEntrySet
Iteration over `namespacesBuffer.keySet()` may be replaced with 'entrySet()' iteration
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/ndjsonld/NDJSONLDWriter.java`
#### Snippet
```java
								context);
						jsonldWriter.startRDF();
						for (String key : namespacesBuffer.keySet()) {
							jsonldWriter.handleNamespace(key, namespacesBuffer.get(key));
						}
```

### KeySetIterationMayUseEntrySet
Iteration over `currentNode.keySet()` may be replaced with 'values()' iteration
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDHierarchicalProcessor.java`
#### Snippet
```java
				visited.add(currentTreeNode.getNodeID());
				Map<String, Object> currentNode = currentTreeNode.node;
				for (String predicate : currentNode.keySet()) {
					Object object = currentNode.get(predicate);
					if (object instanceof List<?>) {
```

### KeySetIterationMayUseEntrySet
Iteration over `namespaces.keySet()` may be replaced with 'entrySet()' iteration
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
		ByteArrayOutputStream out = new ByteArrayOutputStream(4096);
		TupleQueryResultWriter writer = QueryResultIO.createTupleWriter(format, out);
		for (String nextPrefix : namespaces.keySet()) {
			writer.handleNamespace(nextPrefix, namespaces.get(nextPrefix));
		}
```

### KeySetIterationMayUseEntrySet
Iteration over `namespaces.keySet()` may be replaced with 'entrySet()' iteration
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
		ByteArrayOutputStream out = new ByteArrayOutputStream(4096);
		BooleanQueryResultWriter writer = QueryResultIO.createBooleanWriter(format, out);
		for (String nextPrefix : namespaces.keySet()) {
			writer.handleNamespace(nextPrefix, namespaces.get(nextPrefix));
		}
```

### KeySetIterationMayUseEntrySet
Iteration over `namespaces.keySet()` may be replaced with 'entrySet()' iteration
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
		}

		for (String nextPrefix : namespaces.keySet()) {
			writer.handleNamespace(nextPrefix, namespaces.get(nextPrefix));
		}
```

### KeySetIterationMayUseEntrySet
Iteration over `map.keySet()` may be replaced with 'entrySet()' iteration
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/EvaluationStrategies.java`
#### Snippet
```java
		// approach (less chance of accidental lingering references that prevent
		// GC)
		for (UUID key : map.keySet()) {
			// we use identity comparison in line with how guava caches behave
			// when softValues are used.
```

### KeySetIterationMayUseEntrySet
Iteration over `mapping.keySet()` may be replaced with 'entrySet()' iteration
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
				} else {
					// not inside the update statement, searching with the connection
					for (IRI predicate : mapping.keySet()) {
						Set<IRI> objects = mapping.get(predicate);
						try (CloseableIteration<? extends Statement, SailException> statements = getStatements(
```

### KeySetIterationMayUseEntrySet
Iteration over `paintingsMap.keySet()` may be replaced with 'entrySet()' iteration
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java

	private void listPaintingsByArtist(Map<Artist, Set<Painting>> paintingsMap) {
		for (Artist a : paintingsMap.keySet()) {
			System.out.println(String.format("%s %s", a.getFirstName(), a.getLastName()));
			for (Painting p : paintingsMap.get(a)) {
```

### KeySetIterationMayUseEntrySet
Iteration over `map.keySet()` may be replaced with 'entrySet()' iteration
in `core/sail/model/src/main/java/org/eclipse/rdf4j/sail/model/NonSerializables.java`
#### Snippet
```java
		// approach (less chance of accidental lingering references that prevent
		// GC)
		for (UUID key : map.keySet()) {
			// we use identity comparison in line with how guava caches behave
			// when softValues are used.
```

### KeySetIterationMayUseEntrySet
Iteration over `namespaceTable.keySet()` may be replaced with 'entrySet()' iteration
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/AbstractSPARQLXMLWriter.java`
#### Snippet
```java
				}

				for (String nextPrefix : namespaceTable.keySet()) {
					this.log.debug("Adding custom prefix for <{}> to map to <{}>", nextPrefix,
							namespaceTable.get(nextPrefix));
```

## RuleId[id=UnnecessaryQualifierForThis]
### UnnecessaryQualifierForThis
Qualifier `Shape` on 'this' is unnecessary in this context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
		if (validationApproach == ValidationApproach.SPARQL) {
			if (connectionsGroup.isSparqlValidation()
					&& Shape.this.getOptimalBulkValidationApproach() == ValidationApproach.SPARQL) {
				logger.debug("Use validation approach {} for shape {}", validationApproach, this);
				return Shape.this.generateSparqlValidationQuery(connectionsGroup, validationSettings, false, false,
```

### UnnecessaryQualifierForThis
Qualifier `Shape` on 'this' is unnecessary in this context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
					&& Shape.this.getOptimalBulkValidationApproach() == ValidationApproach.SPARQL) {
				logger.debug("Use validation approach {} for shape {}", validationApproach, this);
				return Shape.this.generateSparqlValidationQuery(connectionsGroup, validationSettings, false, false,
						Scope.none)
						.getValidationPlan(connectionsGroup.getBaseConnection(), validationSettings.getDataGraph(),
```

### UnnecessaryQualifierForThis
Qualifier `Shape` on 'this' is unnecessary in this context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
						this);

				return Shape.this.generateTransactionalValidationPlan(connectionsGroup, validationSettings,
						() -> Shape.this.getTargetChain()
								.getEffectiveTarget(
```

### UnnecessaryQualifierForThis
Qualifier `Shape` on 'this' is unnecessary in this context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java

				return Shape.this.generateTransactionalValidationPlan(connectionsGroup, validationSettings,
						() -> Shape.this.getTargetChain()
								.getEffectiveTarget(
										this instanceof NodeShape ? Scope.nodeShape : Scope.propertyShape,
```

### UnnecessaryQualifierForThis
Qualifier `Shape` on 'this' is unnecessary in this context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
			if (this.requiresEvaluation(connectionsGroup, Scope.none, validationSettings.getDataGraph(),
					stableRandomVariableProvider)) {
				return Shape.this.generateTransactionalValidationPlan(connectionsGroup, validationSettings, null,
						Scope.none);
			} else {
```

### UnnecessaryQualifierForThis
Qualifier `BindSelect` on 'this' is unnecessary in this context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
		values.append("){}\n");

		String query = BindSelect.this.query;

		query = query.replace(AbstractConstraintComponent.VALUES_INJECTION_POINT, values.toString());
```

### UnnecessaryQualifierForThis
Qualifier `LuceneIndex` on 'this' is unnecessary in this context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		}
		return Iterables.transform(Arrays.asList(docs.scoreDocs),
				(ScoreDoc doc) -> new LuceneDocumentScore(doc, highlighter, LuceneIndex.this));
	}

```

### UnnecessaryQualifierForThis
Qualifier `LuceneIndex` on 'this' is unnecessary in this context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		}
		return Iterables.transform(Arrays.asList(docs.scoreDocs),
				(ScoreDoc doc) -> new LuceneDocumentResult(doc, LuceneIndex.this, fields));
	}

```

### UnnecessaryQualifierForThis
Qualifier `LuceneIndex` on 'this' is unnecessary in this context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
				(ScoreDoc doc) -> new LuceneDocumentDistance(doc, geoField, units, boundingCircle.getCenter(),
						requireContext,
						LuceneIndex.this));
	}

```

### UnnecessaryQualifierForThis
Qualifier `StrictEvaluationStrategy` on 'this' is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			throws QueryEvaluationException {
		QueryEvaluationStep child = precompile(node.getArg(), context);
		return bs -> new DescribeIteration(child.evaluate(bs), StrictEvaluationStrategy.this, node.getBindingNames(),
				bs);
	}
```

### UnnecessaryQualifierForThis
Qualifier `StrictEvaluationStrategy` on 'this' is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java

	protected QueryEvaluationStep prepare(Group node, QueryEvaluationContext context) throws QueryEvaluationException {
		return bindings -> new GroupIterator(StrictEvaluationStrategy.this, node, bindings, iterationCacheSyncThreshold,
				context);
	}
```

### UnnecessaryQualifierForThis
Qualifier `StrictEvaluationStrategy` on 'this' is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		final Var contextVar = alp.getContextVar();
		final long minLength = alp.getMinLength();
		return bindings -> new PathIteration(StrictEvaluationStrategy.this, scope, subjectVar, pathExpression, objVar,
				contextVar, minLength, bindings);
	}
```

### UnnecessaryQualifierForThis
Qualifier `StrictEvaluationStrategy` on 'this' is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		return bindings -> {
			CloseableIteration<BindingSet, QueryEvaluationException> evaluate = child.evaluate(bindings);
			return new DistinctIteration<>(evaluate, StrictEvaluationStrategy.this::makeSet);
		};

```

### UnnecessaryQualifierForThis
Qualifier `StrictEvaluationStrategy` on 'this' is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		// in the other case nothing can pass the filter so we can return the empty set.
		if (ves.isConstant()) {
			if (StrictEvaluationStrategy.this.isTrue(ves, EmptyBindingSet.getInstance())) {
				return arg;
			} else {
```

### UnnecessaryQualifierForThis
Qualifier `StrictEvaluationStrategy` on 'this' is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			}
		}
		return bs -> new FilterIterator(node, arg.evaluate(bs), ves, StrictEvaluationStrategy.this);
	}

```

## RuleId[id=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`bindings.size() == 0` can be replaced with 'bindings.isEmpty()'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
	 */
	public static String getTupleQueryString(String queryString, BindingSet bindings) {
		if (bindings.size() == 0) {
			return queryString;
		}
```

### SizeReplaceableByIsEmpty
`bindings.size() == 0` can be replaced with 'bindings.isEmpty()'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
	 */
	public static String getGraphQueryString(String queryString, BindingSet bindings) {
		if (bindings.size() == 0) {
			return queryString;
		}
```

### SizeReplaceableByIsEmpty
`bindings.size() == 0` can be replaced with 'bindings.isEmpty()'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java

	protected Set<String> getBindingNames() {
		if (bindings.size() == 0) {
			return Collections.EMPTY_SET;
		}
```

### SizeReplaceableByIsEmpty
`base.length() > 0` can be replaced with '!base.isEmpty()'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
		this.operation = operation;
		this.client = client;
		boolean abs = base != null && base.length() > 0 && ParsedIRI.create(base).isAbsolute();
		if (abs && !operation.toUpperCase().contains("BASE")) {
			this.operation = "BASE <" + base + "> " + operation;
```

### SizeReplaceableByIsEmpty
`bindings.size() == 0` can be replaced with 'bindings.isEmpty()'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java

	protected String getQueryString() {
		if (bindings.size() == 0) {
			return operation;
		}
```

### SizeReplaceableByIsEmpty
`flags.length() > 0` can be replaced with '!flags.isEmpty()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PatternFilter.java`
#### Snippet
```java
	public PatternFilter(PlanNode parent, String pattern, String flags) {
		super(parent);
		if (flags != null && flags.length() > 0) {

			int flag = 0b0;
```

### SizeReplaceableByIsEmpty
`sparqlDefaultGraphs.size() > 0` can be replaced with '!sparqlDefaultGraphs.isEmpty()'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserUpdate.java`
#### Snippet
```java
		// if there are default graphs in the SPARQL update but it's not a WITH
		// clause, it's a USING clause
		final boolean hasUsingClause = !hasWithClause && sparqlDefaultGraphs != null ? sparqlDefaultGraphs.size() > 0
				: false;

```

### SizeReplaceableByIsEmpty
`sparqlNamedGraphs.size() > 0` can be replaced with '!sparqlNamedGraphs.isEmpty()'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserUpdate.java`
#### Snippet
```java

		final Set<IRI> sparqlNamedGraphs = sparqlDefinedDataset.getNamedGraphs();
		final boolean hasUsingNamedClause = sparqlNamedGraphs != null ? sparqlNamedGraphs.size() > 0 : false;

		if (hasUsingClause) {
```

### SizeReplaceableByIsEmpty
`txn.size() > 0` can be replaced with '!txn.isEmpty()'
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
		if (this.getRepository().useCompatibleMode()) {
			synchronized (txn) {
				if (txn.size() > 0) {
					try {
						client.sendTransaction(txn);
```

### SizeReplaceableByIsEmpty
`sparqlDefaultGraphs.size() > 0` can be replaced with '!sparqlDefaultGraphs.isEmpty()'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserUpdate.java`
#### Snippet
```java
		// if there are default graphs in the SPARQL update but it's not a WITH
		// clause, it's a USING clause
		final boolean hasUsingClause = !hasWithClause && sparqlDefaultGraphs != null ? sparqlDefaultGraphs.size() > 0
				: false;

```

### SizeReplaceableByIsEmpty
`sparqlNamedGraphs.size() > 0` can be replaced with '!sparqlNamedGraphs.isEmpty()'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserUpdate.java`
#### Snippet
```java

		final Set<IRI> sparqlNamedGraphs = sparqlDefinedDataset.getNamedGraphs();
		final boolean hasUsingNamedClause = sparqlNamedGraphs != null ? sparqlNamedGraphs.size() > 0 : false;

		if (hasUsingClause) {
```

### SizeReplaceableByIsEmpty
`triple.length() > 0` can be replaced with '!triple.isEmpty()'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
			int offset = 2;

			while (triple.length() > 0 && Character.isWhitespace(triple.charAt(0))) {
				triple = triple.substring(1);
				++offset;
```

### SizeReplaceableByIsEmpty
`triple.length() > 0` can be replaced with '!triple.isEmpty()'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
					object = v;
				}
				while (triple.length() > 0 && Character.isWhitespace(triple.charAt(0))) {
					triple = triple.substring(1);
					++offset;
```

### SizeReplaceableByIsEmpty
`uriSpec.length() > 0` can be replaced with '!uriSpec.isEmpty()'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java

			if (getParserConfig().get(BasicParserSettings.VERIFY_RELATIVE_URIS)) {
				if (uriSpec.length() > 0 && !uriSpec.startsWith("#") && baseURI.isOpaque()) {
					reportError("Relative URI '" + uriSpec + "' cannot be resolved using the opaque base URI '"
							+ baseURI + "'", BasicParserSettings.VERIFY_RELATIVE_URIS);
```

### SizeReplaceableByIsEmpty
`origin.length() == 0` can be replaced with 'origin.isEmpty()'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
		String origin = getParserConfig().get(BasicParserSettings.SKOLEMIZE_ORIGIN);

		if (origin == null || origin.length() == 0) {
			if (skolemOrigin != null) {
				skolemOrigin = null;
```

### SizeReplaceableByIsEmpty
`this.inputNamedGraphs.size() > 0` can be replaced with '!this.inputNamedGraphs.isEmpty()'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11UpdateComplianceTest.java`
#### Snippet
```java
		ds.setDefaultInsertGraph(null);

		if (this.inputNamedGraphs.size() > 0) {
			for (String ng : inputNamedGraphs.keySet()) {
				IRI namedGraph = SimpleValueFactory.getInstance().createIRI(ng);
```

### SizeReplaceableByIsEmpty
`this.inputNamedGraphs.size() > 0` can be replaced with '!this.inputNamedGraphs.isEmpty()'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
		ds.setDefaultInsertGraph(null);

		if (this.inputNamedGraphs.size() > 0) {
			for (String ng : inputNamedGraphs.keySet()) {
				IRI namedGraph = SimpleValueFactory.getInstance().createIRI(ng);
```

### SizeReplaceableByIsEmpty
`sink.size() > 0` can be replaced with '!sink.isEmpty()'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/UpdateWithModelBuilder.java`
#### Snippet
```java
		List<Statement> sink = new ArrayList<>();
		consumer.accept(sink);
		if (sink.size() > 0) {
			sink.stream()
					.forEach(
```

### SizeReplaceableByIsEmpty
`oldmonitors.size() > 0` can be replaced with '!oldmonitors.isEmpty()'
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
				try {
					synchronized (oldmonitors) {
						if (oldmonitors.size() > 0) {
							logger.warn(
									"LuceneSail: On shutdown {} IndexReaders were not closed. This is due to non-closed Query Iterators, which must be closed!",
```

### SizeReplaceableByIsEmpty
`contextStatements.size() == 0` can be replaced with 'contextStatements.isEmpty()'
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java

			// Filter resources that are not used as context identifier
			if (contextStatements.size() == 0) {
				return false;
			}
```

### SizeReplaceableByIsEmpty
`_path.length() == 0` can be replaced with '_path.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedURI.java`
#### Snippet
```java
	 */
	public boolean isSelfReference() {
		return _scheme == null && _authority == null && _query == null && _path.length() == 0;
	}

```

### SizeReplaceableByIsEmpty
`relURI._path.length() == 0` can be replaced with 'relURI._path.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedURI.java`
#### Snippet
```java

		// RFC, step 2:
		if (relURI._authority == null && relURI._query == null && relURI._path.length() == 0) {
			// Reference to this URI
			ParsedURI result = (ParsedURI) this.clone();
```

### SizeReplaceableByIsEmpty
`relURI._path.length() == 0` can be replaced with 'relURI._path.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedURI.java`
#### Snippet
```java
			if (relURI._path.startsWith("/")) {
				path = relURI._path;
			} else if (relURI._path.length() == 0) {
				path = this._path;
			} else {
```

### SizeReplaceableByIsEmpty
`path.length() == 0` can be replaced with 'path.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedURI.java`
#### Snippet
```java
					}

					if (path.length() == 0) {
						// No path means: start at root.
						path = "/";
```

### SizeReplaceableByIsEmpty
`baseURI.length() > 0` can be replaced with '!baseURI.isEmpty()'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/query/AbstractHTTPQuery.java`
#### Snippet
```java
		// this is used in the SPARQL repository in several places, e.g. in
		// getStatements
		this.baseURI = baseURI != null && baseURI.length() > 0 ? baseURI : null;
	}

```

### SizeReplaceableByIsEmpty
`first.size() == 0` can be replaced with 'first.isEmpty()'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
		}
		BindingSet first = result.next();
		if (!result.hasNext() && first.size() == 0) {
			return Stream.empty();
		}
```

### SizeReplaceableByIsEmpty
`relative.getPath().length() == 0` can be replaced with 'relative.getPath().isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java

		// RFC, step 2:
		if (relative.getHost() == null && relative.getQuery() == null && relative.getPath().length() == 0) {

			// Inherit any fragment identifier from relURI
```

### SizeReplaceableByIsEmpty
`relative.getPath().length() == 0` can be replaced with 'relative.getPath().isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
			return new ParsedIRI(this.getScheme(), this.getUserInfo(), this.getHost(), this.getPort(), this.getPath(),
					this.getQuery(), fragment);
		} else if (relative.getHost() == null && relative.getPath().length() == 0) {

			// Inherit any query or fragment from relURI
```

### SizeReplaceableByIsEmpty
`path.length() == 0` can be replaced with 'path.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
					}

					if (path.length() == 0) {
						// No path means: start at root.
						path = "/";
```

### SizeReplaceableByIsEmpty
`host.length() > 0` can be replaced with '!host.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
				sb.append('@');
			}
			if (host.length() > 0) {
				sb.append(IDN.toASCII(host, IDN.ALLOW_UNASSIGNED));
			}
```

### SizeReplaceableByIsEmpty
`path.length() == 0` can be replaced with 'path.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java

	private String pctEncodingNormalization(String path) {
		if (path == null || path.length() == 0 || path.indexOf('%') < 0) {
			return path; // no pct encodings
		}
```

### SizeReplaceableByIsEmpty
`host.length() == 0` can be replaced with 'host.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
				&& ("".equals(host) || "localhost".equals(host));
		String _host = localhost ? null
				: host == null || host.length() == 0 ? host
						: IDN.toUnicode(pctEncodingNormalization(toLowerCase(host)),
								IDN.USE_STD3_ASCII_RULES | IDN.ALLOW_UNASSIGNED);
```

### SizeReplaceableByIsEmpty
`p.length() > 0` can be replaced with '!p.isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
				advance(1);
				String p = parseMember(DIGIT, '/');
				if (p.length() > 0) {
					port = Integer.parseInt(p);
				} else {
```

### SizeReplaceableByIsEmpty
`baseURI.trim().length() != 0` can be replaced with '!baseURI.trim().isEmpty()'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
				url.addParameter(Protocol.CONTEXT_PARAM_NAME, encodedContext);
			}
			if (baseURI != null && baseURI.trim().length() != 0) {
				String encodedBaseURI = Protocol.encodeValue(SimpleValueFactory.getInstance().createIRI(baseURI));
				url.setParameter(Protocol.BASEURI_PARAM_NAME, encodedBaseURI);
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
	public static boolean isValidLocalName(String name) {
		// Empty names are legal
		if (name.length() == 0) {
			return true;
		}
```

### SizeReplaceableByIsEmpty
`namespace.length() == 0` can be replaced with 'namespace.isEmpty()'
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
		assert localName != null : "localName must not be null";

		if (namespace.length() == 0) {
			return false;
		}
```

### SizeReplaceableByIsEmpty
`langTag.length() > 0` can be replaced with '!langTag.isEmpty()'
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
		boolean result = false;
		if (langRange.equals("*")) {
			result = langTag.length() > 0;
		} else if (langTag.length() == langRange.length()) {
			result = langTag.equalsIgnoreCase(langRange);
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
	public static boolean isPN_LOCAL(String name) {
		// Empty names are legal
		if (name.length() == 0) {
			return true;
		}
```

### SizeReplaceableByIsEmpty
`prefix.length() == 0` can be replaced with 'prefix.isEmpty()'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
	public static boolean isPN_PREFIX(String prefix) {
		// Empty prefixes are not legal, they should always have a colon
		if (prefix.length() == 0) {
			logger.debug("PN_PREFIX was not valid (empty)");
			return false;
```

### SizeReplaceableByIsEmpty
`s.length() > 0` can be replaced with '!s.isEmpty()'
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	private static String trimPlusSign(String s) {
		if (s.length() > 0 && s.charAt(0) == '+') {
			return s.substring(1);
		} else {
```

### SizeReplaceableByIsEmpty
`directive.length() == 0` can be replaced with 'directive.isEmpty()'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
			}
			parseBase();
		} else if (directive.length() == 0) {
			reportFatalError("Directive name is missing, expected @prefix or @base");
		} else {
```

### SizeReplaceableByIsEmpty
`prefix.length() == 0` can be replaced with 'prefix.isEmpty()'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
				// specified prefix

				boolean isLegalPrefix = prefix.length() == 0 || TurtleUtil.isPN_PREFIX(prefix);

				if (!isLegalPrefix || namespaceTable.containsValue(prefix)) {
```

### SizeReplaceableByIsEmpty
`prefix.length() == 0` can be replaced with 'prefix.isEmpty()'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
					// generate a legal unique prefix

					if (prefix.length() == 0 || !isLegalPrefix) {
						prefix = "ns";
					}
```

### SizeReplaceableByIsEmpty
`label.length() > 0` can be replaced with '!label.isEmpty()'
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
			writer.write("^^");
			writeURI(datatype);
		} else if (label.length() > 0 && encoded.equals(label) && label.charAt(0) != '<' && label.charAt(0) != '_'
				&& !label.matches("^[\\+\\-]?[\\d\\.].*")) {
			// no need to include double quotes
```

### SizeReplaceableByIsEmpty
`dataset.getDefaultRemoveGraphs().size() > 0` can be replaced with '!dataset.getDefaultRemoveGraphs().isEmpty()'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java

		if (dataset != null) {
			if (dataset.getDefaultRemoveGraphs().size() > 0) {
				if (!(dataset.getDefaultRemoveGraphs().equals(dataset.getDefaultGraphs()))) {
					logger.warn(
```

### SizeReplaceableByIsEmpty
`errInfo.toString().length() > 0` can be replaced with '!errInfo.toString().isEmpty()'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
								new StringReader(errInfo.toString()), "", format));

					} else if (errInfo.toString().length() > 0) {
						throw new RepositoryException(errInfo.toString());
					} else {
```

### SizeReplaceableByIsEmpty
`var.length() == 0` can be replaced with 'var.isEmpty()'
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/ConfigTemplate.java`
#### Snippet
```java
			List<String> tokens = Arrays.asList(tokensArray);
			String var = tokens.get(0).trim();
			if (var.length() == 0) {
				throw new IllegalArgumentException("Illegal template token: " + matcher.group());
			}
```

### SizeReplaceableByIsEmpty
`server.length() == 0` can be replaced with 'server.isEmpty()'
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryProvider.java`
#### Snippet
```java
		int idx = url.lastIndexOf(REPOSITORIES);
		String server = url.substring(0, idx);
		if (server.length() == 0) {
			server = ".";
		}
```

### SizeReplaceableByIsEmpty
`baseName.length() > 0` can be replaced with '!baseName.isEmpty()'
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

		// First try if we can use the base name without an appended index
		if (baseName != null && baseName.length() > 0 && !hasRepositoryConfig(baseName)) {
			return baseName;
		}
```

### SizeReplaceableByIsEmpty
`baseName.length() == 0` can be replaced with 'baseName.isEmpty()'
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

		// When the base name is null or empty, generate one
		if (baseName == null || baseName.length() == 0) {
			baseName = "repository-";
		} else if (!baseName.endsWith("-")) {
```

### SizeReplaceableByIsEmpty
`olds.length() == 0` can be replaced with 'olds.isEmpty()'
in `core/common/text/src/main/java/org/eclipse/rdf4j/common/text/StringUtil.java`
#### Snippet
```java
	@Deprecated
	public static String gsub(String olds, String news, String text) {
		if (olds == null || olds.length() == 0) {
			// Nothing to substitute.
			return text;
```

### SizeReplaceableByIsEmpty
`((BindingSet) o).size() == 0` can be replaced with '((BindingSet) o).isEmpty()'
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/EmptyBindingSet.java`
#### Snippet
```java
	public boolean equals(Object o) {
		if (o instanceof BindingSet) {
			return ((BindingSet) o).size() == 0;
		}

```

### SizeReplaceableByIsEmpty
`statements.size() != 0` can be replaced with '!statements.isEmpty()'
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
								.asList(connection.getStatements(null, null, null, false));
						connection.commit();
						if (statements.size() != 0) {
							if (statements.size() != count) {
								logger.error("Size was {}. Expected 0 or {}", statements.size(), count);
```

### SizeReplaceableByIsEmpty
`charBuf.toString().trim().length() > 0` can be replaced with '!charBuf.toString().trim().isEmpty()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
					// if the element is not empty we need to process it as such. Otherwise,
					// we keep the start element deferred for now.
					if (deferredElement != null && charBuf.toString().trim().length() > 0) {
						reportDeferredStartElement();
					}
```

### SizeReplaceableByIsEmpty
`s.trim().length() > 0` can be replaced with '!s.trim().isEmpty()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java

					// ignore whitespace-only nodes
					if (s.trim().length() > 0) {
						rdfParser.text(s);
					}
```

### SizeReplaceableByIsEmpty
`predicateStack.size() > 0` can be replaced with '!predicateStack.isEmpty()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/util/RDFXMLPrettyWriter.java`
#### Snippet
```java
				writeNodeEndTag(nextElement);

				if (predicateStack.size() > 0) {
					IRI nextPredicate = predicateStack.pop();

```

### SizeReplaceableByIsEmpty
`line.length() > 0` can be replaced with '!line.isEmpty()'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java
		int emptyLineCount = 0;
		while ((line = in.readLine()) != null) {
			if (line.length() > 0) {
				emptyLineCount = 0;
				buf.append(' ').append(line).append('\n');
```

### SizeReplaceableByIsEmpty
`queryStr.length() > 0` can be replaced with '!queryStr.isEmpty()'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java
				emptyLineCount = 0;
				String queryStr = buf.toString().trim();
				if (queryStr.length() > 0) {
					try {
						long start = System.currentTimeMillis();
```

### SizeReplaceableByIsEmpty
`errorAfter.length() == 0` can be replaced with 'errorAfter.isEmpty()'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/TokenMgrError.java`
#### Snippet
```java
				errorColumn + ".  Encountered: " + //
				(EOFSeen ? "<EOF>" : ("'" + addEscapes(String.valueOf(curChar)) + "' (" + (int) curChar + "),")) + //
				(errorAfter == null || errorAfter.length() == 0 ? ""
						: " after prefix \"" + addEscapes(errorAfter) + "\""))
				+ //
```

### SizeReplaceableByIsEmpty
`uniqueBindings.size() > 0` can be replaced with '!uniqueBindings.isEmpty()'
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
						Set<String> uniqueBindings = new HashSet<>(uc.getBindingSet().getBindingNames());
						uniqueBindings.removeAll(sourceBinding.getBindingNames());
						if (uniqueBindings.size() > 0) {
							MapBindingSet mergedSet = new MapBindingSet();
							for (String bindingName : sourceBinding.getBindingNames()) {
```

### SizeReplaceableByIsEmpty
`collector.getOperators().size() > 0` can be replaced with '!collector.getOperators().isEmpty()'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
				valueExpr.visit(collector);

				if (collector.getOperators().size() > 0) {
					elem.setAggregateOperatorInExpression(true);
					for (AggregateOperator operator : collector.getOperators()) {
```

### SizeReplaceableByIsEmpty
`extension.getElements().size() > 0` can be replaced with '!extension.getElements().isEmpty()'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		}

		if (extension.getElements().size() > 0 && !(arg instanceof Extension)) {
			extension.setArg(arg);
			group.setArg(extension);
```

### SizeReplaceableByIsEmpty
`bindings.size() > 0` can be replaced with '!bindings.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/BindingAssigner.java`
#### Snippet
```java
	@Override
	public void optimize(TupleExpr tupleExpr, Dataset dataset, BindingSet bindings) {
		if (bindings.size() > 0) {
			tupleExpr.visit(new VarVisitor(bindings));
		}
```

### SizeReplaceableByIsEmpty
`bindings.size() == 0` can be replaced with 'bindings.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/BindingSetAssignmentQueryEvaluationStep.java`
#### Snippet
```java
	public CloseableIteration<BindingSet, QueryEvaluationException> evaluate(BindingSet bindings) {
		final Iterator<BindingSet> assignments = node.getBindingSets().iterator();
		if (bindings.size() == 0) {
			// we can just return the assignments directly without checking existing bindings
			return new CloseableIteratorIteration<>(assignments);
```

### SizeReplaceableByIsEmpty
`label.length() > 0` can be replaced with '!label.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtil.java`
#### Snippet
```java

			if (datatype == CoreDatatype.XSD.STRING) {
				return label.length() > 0;
			} else if (datatype == CoreDatatype.XSD.BOOLEAN) {
				// also false for illegal values
```

### SizeReplaceableByIsEmpty
`label.length() > 0` can be replaced with '!label.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtility.java`
#### Snippet
```java

			if (datatype == CoreDatatype.XSD.STRING) {
				return Result.fromBoolean(label.length() > 0);
			} else if (datatype == CoreDatatype.XSD.BOOLEAN) {
				// also false for illegal values
```

### SizeReplaceableByIsEmpty
`joinArgs.size() > 0` can be replaced with '!joinArgs.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
				// We order all remaining join arguments based on cardinality and
				// variable frequency statistics
				if (joinArgs.size() > 0) {
					// Build maps of cardinalities and vars per tuple expression
					Map<TupleExpr, Double> cardinalityMap = new HashMap<>();
```

### SizeReplaceableByIsEmpty
`priorityArgs.size() > 0` can be replaced with '!priorityArgs.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
				// Build new join hierarchy
				TupleExpr priorityJoins = null;
				if (priorityArgs.size() > 0) {
					priorityJoins = priorityArgs.get(0);
					for (int i = 1; i < priorityArgs.size(); i++) {
```

### SizeReplaceableByIsEmpty
`orderedJoinArgs.size() > 0` can be replaced with '!orderedJoinArgs.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
				}

				if (orderedJoinArgs.size() > 0) {
					// Note: generated hierarchy is right-recursive to help the
					// IterativeEvaluationOptimizer to factor out the left-most join
```

### SizeReplaceableByIsEmpty
`snippet.length() > 0` can be replaced with '!snippet.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		snippet = node.getOpPattern().substring(prevPatternIndex + 1);

		if (snippet.length() > 0) {
			// Pattern does not end with a wildcard.

```

### SizeReplaceableByIsEmpty
`bindings.size() > 0` can be replaced with '!bindings.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/BindingAssignerOptimizer.java`
#### Snippet
```java
	@Override
	public void optimize(TupleExpr tupleExpr, Dataset dataset, BindingSet bindings) {
		if (bindings.size() > 0) {
			tupleExpr.visit(new VarVisitor(bindings));
		}
```

### SizeReplaceableByIsEmpty
`scanList.size() > 0` can be replaced with '!scanList.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java

		while (currentScanElem == null) {
			if (scanList.size() > 0) {
				currentScanElem = removeFirstElement(scanList);
			} else {
```

### SizeReplaceableByIsEmpty
`bindingSet.size() == 0` can be replaced with 'bindingSet.isEmpty()'
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultWriter.java`
#### Snippet
```java

		try {
			if (bindingSet.size() == 0) {
				writeEmptyRow();
			} else {
```

### SizeReplaceableByIsEmpty
`joinArgs.size() > 0` can be replaced with '!joinArgs.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
				// We order all remaining join arguments based on cardinality and
				// variable frequency statistics
				if (joinArgs.size() > 0) {
					// Build maps of cardinalities and vars per tuple expression
					Map<TupleExpr, Double> cardinalityMap = Collections.emptyMap();
```

### SizeReplaceableByIsEmpty
`priorityArgs.size() > 0` can be replaced with '!priorityArgs.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
				// Build new join hierarchy
				TupleExpr priorityJoins = null;
				if (priorityArgs.size() > 0) {
					priorityJoins = priorityArgs.get(0);
					for (int i = 1; i < priorityArgs.size(); i++) {
```

### SizeReplaceableByIsEmpty
`orderedJoinArgs.size() > 0` can be replaced with '!orderedJoinArgs.isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
				}

				if (orderedJoinArgs.size() > 0) {
					// Note: generated hierarchy is right-recursive to help the
					// IterativeEvaluationOptimizer to factor out the left-most join
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the ID of the repository.
	 */
	public static final String getRepositoryID(String repositoryLocation) {
		String repositoryID = repositoryLocation.substring(repositoryLocation.lastIndexOf('/') + 1);
		return repositoryID;
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the statements resource for the specified repository
	 */
	public static final String getStatementsLocation(String repositoryLocation) {
		return repositoryLocation + "/" + STATEMENTS;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the transaction resources for the specified repository
	 */
	public static final String getTransactionsLocation(String repositoryLocation) {
		return repositoryLocation + "/" + TRANSACTIONS;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the configuration resource for the specified repository
	 */
	public static final String getRepositoryConfigLocation(String repositoryLocation) {
		return repositoryLocation + "/" + CONFIG;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the 'size' resource for a specific repository on the specified server
	 */
	public static final String getSizeLocation(String repositoryLocation) {
		return repositoryLocation + "/" + SIZE;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the server resource for the specified repository.
	 */
	public static final String getServerLocation(String repositoryLocation) {
		String serverLocation = repositoryLocation.substring(0, repositoryLocation.lastIndexOf('/'));
		serverLocation = serverLocation.substring(0, serverLocation.lastIndexOf('/'));
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the repository list resource on the specified server
	 */
	public static final String getRepositoriesLocation(String serverLocation) {
		return getServerDir(serverLocation) + REPOSITORIES;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the namespaces lists resource for a specific repository on the specified server
	 */
	public static final String getNamespacesLocation(String repositoryLocation) {
		return repositoryLocation + "/" + NAMESPACES;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the contexts lists resource for the specified repository
	 */
	public static final String getContextsLocation(String repositoryLocation) {
		return repositoryLocation + "/" + CONTEXTS;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of a specific repository resource on the specified server
	 */
	public static final String getRepositoryLocation(String serverLocation, String repositoryID) {
		return getRepositoriesLocation(serverLocation) + "/" + repositoryID;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the protocol resource on the specified server
	 */
	public static final String getProtocolLocation(String serverLocation) {
		return getServerDir(serverLocation) + PROTOCOL;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 *         server
	 */
	public static final String getNamespacePrefixLocation(String repositoryLocation, String prefix) {
		return getNamespacesLocation(repositoryLocation) + "/" + prefix;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * @return the location of the server configuration resource on the specified server
	 */
	public static final String getConfigLocation(String serverLocation) {
		return getServerDir(serverLocation) + CONFIG;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/RDFParserHelper.java`
#### Snippet
```java
	 *                           settings in the given parser config.
	 */
	public static final Literal createLiteral(String label, String lang, IRI datatype, ParserConfig parserConfig,
			ParseErrorListener errListener, ValueFactory valueFactory) throws RDFParseException {
		return createLiteral(label, lang, datatype, parserConfig, errListener, valueFactory, -1, -1);
```

### FinalStaticMethod
'static' method declared `final`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLComplianceTest.java`
#### Snippet
```java
	}

	protected static final void printBindingSet(BindingSet bs, StringBuilder appendable) {
		List<String> names = new ArrayList<>(bs.getBindingNames());
		Collections.sort(names);
```

### FinalStaticMethod
'static' method declared `final`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/TypeMappingUtils.java`
#### Snippet
```java
	}

	public static final String toString(Value from) {
		return from.toString();
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	}

	public static final boolean isNCNameStartChar(char c) {
		return c == '_' || isLetter(c);
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	}

	public static final boolean isDigit(char c) {
		return _isAsciiDigit(c) || _isNonAsciiDigit(c);
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	}

	public static final boolean isIdeographic(char c) {
		return _charInRange(c, 0x4E00, 0x9FA5) || c == 0x3007 || _charInRange(c, 0x3021, 0x3029);
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	}

	public static final boolean isNCNameChar(char c) {
		return _isAsciiBaseChar(c) || _isAsciiDigit(c) || c == '.' || c == '-' || c == '_' || _isNonAsciiBaseChar(c)
				|| _isNonAsciiDigit(c) || isIdeographic(c) || isCombiningChar(c) || isExtender(c);
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	}

	public static final boolean isExtender(char c) {
		return c == 0x00B7 || c == 0x02D0 || c == 0x02D1 || c == 0x0387 || c == 0x0640 || c == 0x0E46 || c == 0x0EC6
				|| c == 0x3005 || _charInRange(c, 0x3031, 0x3035) || _charInRange(c, 0x309D, 0x309E)
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	}

	public static final boolean isCombiningChar(char c) {
		return _charInRange(c, 0x0300, 0x0345) || _charInRange(c, 0x0360, 0x0361) || _charInRange(c, 0x0483, 0x0486)
				|| _charInRange(c, 0x0591, 0x05A1) || _charInRange(c, 0x05A3, 0x05B9) || _charInRange(c, 0x05BB, 0x05BD)
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	}

	public static final boolean isLetter(char c) {
		return _isAsciiBaseChar(c) || _isNonAsciiBaseChar(c) || isIdeographic(c);
	}
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	 * <a href="http://www.w3.org/TR/REC-xml-names/#NT-NCName"> http://www.w3.org/TR/REC-xml-names/#NT-NCName </a>.
	 */
	public static final boolean isNCName(String name) {
		int nameLength = name.length();

```

### FinalStaticMethod
'static' method declared `final`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
	 * @throws IOException
	 */
	public static final long transfer(InputStream in, OutputStream out) throws IOException {
		long totalBytes = 0;
		int bytesInBuf;
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
	 * @see java.io.FileWriter
	 */
	public static final long transfer(Reader reader, File file) throws IOException {
		try (FileWriter writer = new FileWriter(file)) {
			return transfer(reader, writer);
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
	 * @throws IOException
	 */
	public static final long transfer(Reader in, Writer out) throws IOException {
		long totalChars = 0;
		int charsInBuf;
```

### FinalStaticMethod
'static' method declared `final`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
	 * @throws IOException If an I/O error occurred while trying to write the data to the file.
	 */
	public static final long transfer(InputStream in, File file) throws IOException {
		try (FileOutputStream out = new FileOutputStream(file)) {
			return transfer(in, out);
```

### FinalStaticMethod
'static' method declared `final`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/GeoUnits.java`
#### Snippet
```java
	}

	public static final double fromKilometres(double kms, IRI units) {
		double dist;
		if (GEOF.UOM_METRE.equals(units)) {
```

### FinalStaticMethod
'static' method declared `final`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/GeoUnits.java`
#### Snippet
```java
	}

	public static final double fromDegrees(double degs, IRI units) {
		double dist;
		if (GEOF.UOM_METRE.equals(units)) {
```

### FinalStaticMethod
'static' method declared `final`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/GeoUnits.java`
#### Snippet
```java
	}

	public static final double toMiles(double distance, IRI units) {
		final double miles;
		if (GEOF.UOM_METRE.equals(units)) {
```

### FinalStaticMethod
'static' method declared `final`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/GeoUnits.java`
#### Snippet
```java
	}

	public static final double toKilometres(double distance, IRI units) {
		final double kms;
		if (GEOF.UOM_METRE.equals(units)) {
```

### FinalStaticMethod
'static' method declared `final`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/GeoUnits.java`
#### Snippet
```java
	}

	public static final double fromMiles(double miles, IRI units) {
		double dist;
		if (GEOF.UOM_METRE.equals(units)) {
```

### FinalStaticMethod
'static' method declared `final`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/GeoUnits.java`
#### Snippet
```java
	}

	public static final double toDegrees(double distance, IRI units) {
		final double degs;
		if (GEOF.UOM_METRE.equals(units)) {
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetClass.java`
#### Snippet
```java
						"BIND(rdf:type as " + stableRandomVariableProvider.next().asSparqlVariable() + ")",
						"BIND(" + r + " as " + objectVariable + ")",
						"" + subjectVariable + " " + stableRandomVariableProvider.current().asSparqlVariable()
								+ objectVariable + ".",
						"}"
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SingleCloseablePlanNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnBufferedPlanNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedPlanNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TargetChainPopper.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedSplitter.java`
#### Snippet
```java
		@Override
		public String getId() {
			return System.identityHashCode(bufferedSplitter) + "";
		}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedSplitter.java`
#### Snippet
```java

	public String getId() {
		int length = (idCounter.get() + "").length();
		return String.format("%0" + length + "d", id);
	}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnionNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToPropertyShape.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationReportNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AllTargetsPlanNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToNodeShape.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SetFilterNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TrimToTarget.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TupleMapper.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Unique.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NotValuesIn.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ReduceTargets.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NonUniqueTargetLang.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EmptyNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValuesBackedNode.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MaxInclusiveConstraintComponent.java`
#### Snippet
```java
			return literalToString(maxInclusive) + " >= ?" + varName;
		} else {
			return literalToString(maxInclusive) + " < ?" + varName + "";
		}
	}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MinInclusiveConstraintComponent.java`
#### Snippet
```java
			return literalToString(minInclusive) + " <= ?" + varName;
		} else {
			return literalToString(minInclusive) + " > ?" + varName + "";
		}
	}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MaxExclusiveConstraintComponent.java`
#### Snippet
```java
			return literalToString(maxExclusive) + " > ?" + varName;
		} else {
			return literalToString(maxExclusive) + " <= ?" + varName + "";
		}
	}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
	@Override
	public String getId() {
		return System.identityHashCode(this) + "";
	}

```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/AbstractQueryModelNode.java`
#### Snippet
```java
			humanReadbleString = Math.round(number / 100) / 10.0 + "K";
		} else if (number >= 0) {
			humanReadbleString = Math.round(number) + "";
		} else {
			humanReadbleString = "UNKNOWN";
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/QueryModelTreePrinter.java`
#### Snippet
```java
			humanReadbleString = Math.round(number / 100) / 10.0 + "K";
		} else if (number >= 0) {
			humanReadbleString = Math.round(number) + "";
		} else {
			humanReadbleString = "UNKNOWN";
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
			humanReadbleString = Math.round(number / 100) / 10.0 + "K";
		} else if (number >= 0) {
			humanReadbleString = Math.round(number) + "";
		} else {
			humanReadbleString = UNKNOWN;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
			humanReadbleString = number / 100 / 10.0 + "K";
		} else if (number >= 0) {
			humanReadbleString = number + "";
		} else {
			humanReadbleString = UNKNOWN;
```

## RuleId[id=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
No class found which is a subtype of both 'Statement' and 'SailSourceModel'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			for (int i = 0; i < 100; i++) {
				Statement e = it.next();
				sb.append(e == this ? "(this Collection)" : e);
				if (!it.hasNext()) {
					return sb.append(']').toString();
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'IRI' and 'XSD'
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractLiteral.java`
#### Snippet
```java
					final IRI datatype = getDatatype();

					return datatype.equals(CoreDatatype.XSD.STRING) ? label
							: label + "^^<" + datatype.stringValue() + ">";

```

### EqualsBetweenInconvertibleTypes
No class found which is a subtype of both 'Statement' and 'SailSourceModel'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			for (int i = 0; i < 100; i++) {
				Statement e = it.next();
				sb.append(e == this ? "(this Collection)" : e);
				if (!it.hasNext()) {
					return sb.append(']').toString();
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `updateExpr instanceof Load`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
		con.startUpdate(uc);
		try {
			if (updateExpr instanceof Load) {
				executeLoad((Load) updateExpr, uc);
			} else if (updateExpr instanceof Modify) {
```

### DuplicateCondition
Duplicate condition `updateExpr instanceof Load`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
			} else if (updateExpr instanceof Move) {
				executeMove((Move) updateExpr, uc, maxExecutionTime);
			} else if (updateExpr instanceof Load) {
				throw new SailException("load operations can not be handled directly by the SAIL");
			}
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbStore.java`
#### Snippet
```java
							.map(Path::toFile)
							.sorted(Comparator.reverseOrder()) // delete files before directory
							.forEach(File::delete);
				} catch (IOException ioe) {
					logger.error("Could not delete temp file " + dataDir);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
	private void open() throws IOException {
		// create directory if it not exists
		dir.mkdirs();

		try (MemoryStack stack = stackPush()) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
		close();

		new File(dir, "data.mdb").delete();
		new File(dir, "lock.mdb").delete();

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java

		new File(dir, "data.mdb").delete();
		new File(dir, "lock.mdb").delete();

		Arrays.fill(valueCache, null);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java

		// create directory if it not exists
		this.dir.mkdirs();

		try (MemoryStack stack = stackPush()) {
```

### IgnoreResultOfCall
Result of `File.mkdir()` is ignored
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxTest.java`
#### Snippet
```java
						File f = new File(tmpDir + File.separator + file.getName());
						if (file.isDirectory()) {
							f.mkdir();
							continue;
						}
```

### IgnoreResultOfCall
Result of `Assertions.assertThat()` is ignored
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java
		y.add(vf.createBNode("node1ejfmfm4dx3"), RDF.VALUE, vf.createBNode("node1ejfmfm4dx4"));

		assertThat(Models.isomorphic(x, y));
		assertThat(x.equals(y)).isFalse();
	}
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/FileIO.java`
#### Snippet
```java
			// tolerate renameTo that does not work if destination exists
			if (syncFile.exists() && dataFile.exists()) {
				dataFile.delete();
				renamed = syncFile.renameTo(dataFile);
			}
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/FileUtil.java`
#### Snippet
```java
		}

		dir.delete();
	}
}
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ZipUtil.java`
#### Snippet
```java

		if (entry.isDirectory()) {
			outFile.mkdirs();
		} else {
			outFile.getParentFile().mkdirs();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ZipUtil.java`
#### Snippet
```java
			outFile.mkdirs();
		} else {
			outFile.getParentFile().mkdirs();

			try (InputStream in = zipFile.getInputStream(entry)) {
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTDictionarySectionPFC.java`
#### Snippet
```java

			buffer = new byte[buflen];
			cis.read(buffer);
			checkCRC(cis, is, 4);
		}
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTBitmap.java`
#### Snippet
```java

			buffer = new byte[(int) bytes];
			cis.read(buffer);

			checkCRC(cis, is, 4);
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTHeader.java`
#### Snippet
```java
	private byte[] parseHeaderData(InputStream is, int len) throws IOException {
		byte b[] = new byte[len];
		is.read(b);
		return b;
	}
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java
	protected static void checkFormat(InputStream is, byte[] format) throws IOException {
		byte[] b = new byte[format.length];
		is.read(b);
		if (!Arrays.equals(b, format)) {
			throw new IOException("Unknown format, expected " + new String(format, StandardCharsets.US_ASCII));
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java
			throw new IOException("Unknown format, expected " + new String(format, StandardCharsets.US_ASCII));
		}
		is.read(); // also read null byte
	}

```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java
	protected static void checkControl(InputStream is, HDTPart.Type ctype) throws IOException {
		byte[] cookie = new byte[COOKIE.length];
		is.read(cookie);
		if (!Arrays.equals(cookie, COOKIE)) {
			throw new IOException("$HDT marker not found");
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java

		byte[] checksum = new byte[len];
		is.read(checksum);

		long expect = 0L;
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTArrayLog64.java`
#### Snippet
```java

			buffer = new byte[(int) bytes];
			cis.read(buffer);

			checkCRC(cis, is, 4);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
		File dataDir = getRepositoryDir(config.getID());
		if (!dataDir.exists()) {
			dataDir.mkdirs();
		}
		if (!dataDir.isDirectory()) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/DirectoryLockManager.java`
#### Snippet
```java
		File lockedFile = new File(lockDir, LOCK_FILE_NAME);
		File infoFile = new File(lockDir, INFO_FILE_NAME);
		lockedFile.delete();
		infoFile.delete();
		return lockDir.delete();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/DirectoryLockManager.java`
#### Snippet
```java
		File infoFile = new File(lockDir, INFO_FILE_NAME);
		lockedFile.delete();
		infoFile.delete();
		return lockDir.delete();
	}
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/InferencingTest.java`
#### Snippet
```java
		name = name.replace("/", "_");
		File tmpFile = new File(tmpDir, "junit-" + name + ".nt");
		tmpFile.createNewFile();

		try (OutputStream export = new BufferedOutputStream(new FileOutputStream(tmpFile))) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeStore.java`
#### Snippet
```java
							.map(Path::toFile)
							.sorted(Comparator.reverseOrder()) // delete files before directory
							.forEach(File::delete);
				} catch (IOException ioe) {
					logger.error("Could not delete temp file " + dataDir);
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/HashFile.java`
#### Snippet
```java
			// Discard the temp file
		}
		tmpFile.delete();
	}

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/rio/RDFTestDataset.java`
#### Snippet
```java
			if (!downloadFile.equals(dataFile)) {
				extract(downloadFile, dataFile);
				downloadFile.delete();
			}
		} catch (IOException e) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
				input.close();
			}
			file.delete();
		}

```

## RuleId[id=UnnecessaryUnboxing]
### UnnecessaryUnboxing
Unnecessary unboxing
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java

		if (cachedID != null) {
			int id = cachedID.intValue();

			if (isOwnValue) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java

		if (cachedID != null) {
			int id = cachedID.intValue();

			if (isOwnValue) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultWriter.java`
#### Snippet
```java

		out.writeByte(QNAME_RECORD_MARKER);
		out.writeInt(nsID.intValue());
		writeString(uri.getLocalName());
	}
```

## RuleId[id=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'org.eclipse.rdf4j.sail.lmdb.Statistics.MAX_BUCKETS' accessed via instance reference
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
							Varint.readListUnsigned(keyData.mv_data(), s.maxValues);
							// this is required to correctly estimate the range size at a later point
							s.startValues[s.MAX_BUCKETS] = s.maxValues;
						} else {
							break;
```

### AccessStaticViaInstance
Static member 'org.eclipse.rdf4j.sail.lmdb.Statistics.MAX_BUCKETS' accessed via instance reference
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
						int bucket = 0;
						boolean endOfRange = false;
						for (; bucket < s.MAX_BUCKETS && !endOfRange; bucket++) {
							if (bucket != 0) {
								bucketStart((double) bucket / s.MAX_BUCKETS, s.minValues, s.maxValues, s.values);
```

### AccessStaticViaInstance
Static member 'org.eclipse.rdf4j.sail.lmdb.Statistics.MAX_BUCKETS' accessed via instance reference
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
						for (; bucket < s.MAX_BUCKETS && !endOfRange; bucket++) {
							if (bucket != 0) {
								bucketStart((double) bucket / s.MAX_BUCKETS, s.minValues, s.maxValues, s.values);
								keyBuf.clear();
								Varint.writeListUnsigned(keyBuf, s.values);
```

### AccessStaticViaInstance
Static member 'org.eclipse.rdf4j.sail.lmdb.Statistics.MAX_SAMPLES_PER_BUCKET' accessed via instance reference
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
							int currentSamplesCount = 0;
							rc = mdb_cursor_get(cursor, keyData, valueData, MDB_SET_RANGE);
							while (rc == 0 && currentSamplesCount < s.MAX_SAMPLES_PER_BUCKET) {
								if (mdb_cmp(txn, dbi, keyData, maxKey) >= 0) {
									endOfRange = true;
```

## RuleId[id=ClassNameSameAsAncestorName]
### ClassNameSameAsAncestorName
Class name `SPARQLStarResultsJSONWriter` is the same as one of its superclass' names
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlstarjson/SPARQLStarResultsJSONWriter.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLStarResultsJSONWriter
		extends org.eclipse.rdf4j.query.resultio.sparqljson.SPARQLStarResultsJSONWriter {

```

### ClassNameSameAsAncestorName
Class name `SPARQLStarResultsJSONWriterFactory` is the same as one of its superclass' names
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlstarjson/SPARQLStarResultsJSONWriterFactory.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLStarResultsJSONWriterFactory
		extends org.eclipse.rdf4j.query.resultio.sparqljson.SPARQLStarResultsJSONWriterFactory {
}
```

### ClassNameSameAsAncestorName
Class name `SPARQLStarResultsJSONParserFactory` is the same as one of its superclass' names
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlstarjson/SPARQLStarResultsJSONParserFactory.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLStarResultsJSONParserFactory
		extends org.eclipse.rdf4j.query.resultio.sparqljson.SPARQLStarResultsJSONParserFactory {
}
```

### ClassNameSameAsAncestorName
Class name `SPARQLStarResultsJSONParser` is the same as one of its superclass' names
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlstarjson/SPARQLStarResultsJSONParser.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLStarResultsJSONParser
		extends org.eclipse.rdf4j.query.resultio.sparqljson.SPARQLStarResultsJSONParser {

```

### ClassNameSameAsAncestorName
Class name `SPARQLStarResultsTSVParserFactory` is the same as one of its superclass' names
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/textstar/tsv/SPARQLStarResultsTSVParserFactory.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLStarResultsTSVParserFactory
		extends org.eclipse.rdf4j.query.resultio.text.tsv.SPARQLStarResultsTSVParserFactory {
}
```

### ClassNameSameAsAncestorName
Class name `SPARQLStarResultsTSVParser` is the same as one of its superclass' names
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/textstar/tsv/SPARQLStarResultsTSVParser.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLStarResultsTSVParser extends org.eclipse.rdf4j.query.resultio.text.tsv.SPARQLStarResultsTSVParser {
}

```

### ClassNameSameAsAncestorName
Class name `SPARQLStarResultsTSVWriterFactory` is the same as one of its superclass' names
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/textstar/tsv/SPARQLStarResultsTSVWriterFactory.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLStarResultsTSVWriterFactory
		extends org.eclipse.rdf4j.query.resultio.text.tsv.SPARQLStarResultsTSVWriterFactory {
}
```

### ClassNameSameAsAncestorName
Class name `SPARQLStarResultsTSVWriter` is the same as one of its superclass' names
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/textstar/tsv/SPARQLStarResultsTSVWriter.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLStarResultsTSVWriter extends org.eclipse.rdf4j.query.resultio.text.tsv.SPARQLStarResultsTSVWriter {

	public SPARQLStarResultsTSVWriter(OutputStream out) {
```

### ClassNameSameAsAncestorName
Class name `NTriplesUtil` is the same as one of its superclass' names
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesUtil.java`
#### Snippet
```java
 */
@Deprecated
public class NTriplesUtil extends org.eclipse.rdf4j.rio.helpers.NTriplesUtil {
}

```

### ClassNameSameAsAncestorName
Class name `RepositoryResolver` is the same as one of its superclass' names
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/config/RepositoryResolver.java`
#### Snippet
```java
 */
@Deprecated
public interface RepositoryResolver extends org.eclipse.rdf4j.repository.RepositoryResolver {

	@Override
```

### ClassNameSameAsAncestorName
Class name `RepositoryResolverClient` is the same as one of its superclass' names
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/config/RepositoryResolverClient.java`
#### Snippet
```java
 */
@Deprecated
public interface RepositoryResolverClient extends org.eclipse.rdf4j.repository.RepositoryResolverClient {
}

```

### ClassNameSameAsAncestorName
Class name `SPARQLUpdateDataBlockParser` is the same as one of its superclass' names
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SPARQLUpdateDataBlockParser.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLUpdateDataBlockParser extends org.eclipse.rdf4j.query.parser.sparql.SPARQLUpdateDataBlockParser {

	@Deprecated
```

### ClassNameSameAsAncestorName
Class name `FloatCast` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/FloatCast.java`
#### Snippet
```java
 */
@Deprecated
public class FloatCast extends org.eclipse.rdf4j.query.algebra.evaluation.function.xsd.FloatCast {

}
```

### ClassNameSameAsAncestorName
Class name `BooleanCast` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/BooleanCast.java`
#### Snippet
```java
 */
@Deprecated
public class BooleanCast extends org.eclipse.rdf4j.query.algebra.evaluation.function.xsd.BooleanCast {
}

```

### ClassNameSameAsAncestorName
Class name `DecimalCast` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/DecimalCast.java`
#### Snippet
```java
 */
@Deprecated
public class DecimalCast extends org.eclipse.rdf4j.query.algebra.evaluation.function.xsd.DecimalCast {
}

```

### ClassNameSameAsAncestorName
Class name `DateTimeCast` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/DateTimeCast.java`
#### Snippet
```java
 */
@Deprecated
public class DateTimeCast extends org.eclipse.rdf4j.query.algebra.evaluation.function.xsd.DateTimeCast {

}
```

### ClassNameSameAsAncestorName
Class name `StringCast` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/StringCast.java`
#### Snippet
```java
 */
@Deprecated
public class StringCast extends org.eclipse.rdf4j.query.algebra.evaluation.function.xsd.StringCast {
}

```

### ClassNameSameAsAncestorName
Class name `DoubleCast` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/DoubleCast.java`
#### Snippet
```java
 */
@Deprecated
public class DoubleCast extends org.eclipse.rdf4j.query.algebra.evaluation.function.xsd.DoubleCast {
}

```

### ClassNameSameAsAncestorName
Class name `IntegerCast` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/IntegerCast.java`
#### Snippet
```java
 */
@Deprecated
public class IntegerCast extends org.eclipse.rdf4j.query.algebra.evaluation.function.xsd.IntegerCast {

}
```

### ClassNameSameAsAncestorName
Class name `SilentIteration` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SilentIteration.java`
#### Snippet
```java
@Deprecated
@InternalUseOnly
public class SilentIteration
		extends org.eclipse.rdf4j.common.iteration.SilentIteration<BindingSet, QueryEvaluationException> {

```

### ClassNameSameAsAncestorName
Class name `CollectionIteration` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/CollectionIteration.java`
#### Snippet
```java
 */
@Deprecated
public class CollectionIteration<E, X extends Exception>
		extends org.eclipse.rdf4j.repository.sparql.federation.CollectionIteration<E, X> {

```

### ClassNameSameAsAncestorName
Class name `SPARQLFederatedService` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/SPARQLFederatedService.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQLFederatedService extends org.eclipse.rdf4j.repository.sparql.federation.SPARQLFederatedService {

	public SPARQLFederatedService(String serviceUrl, HttpClientSessionManager client) {
```

### ClassNameSameAsAncestorName
Class name `RepositoryFederatedService` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/RepositoryFederatedService.java`
#### Snippet
```java
 */
@Deprecated
public class RepositoryFederatedService
		extends org.eclipse.rdf4j.repository.sparql.federation.RepositoryFederatedService {

```

### ClassNameSameAsAncestorName
Class name `JoinExecutorBase` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/JoinExecutorBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class JoinExecutorBase<T> extends org.eclipse.rdf4j.repository.sparql.federation.JoinExecutorBase<T> {

	public JoinExecutorBase(CloseableIteration<T, QueryEvaluationException> leftIter, TupleExpr rightArg,
```

### ClassNameSameAsAncestorName
Class name `ServiceJoinConversionIteration` is the same as one of its superclass' names
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/ServiceJoinConversionIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "2.3", forRemoval = true)
public class ServiceJoinConversionIteration
		extends org.eclipse.rdf4j.repository.sparql.federation.ServiceJoinConversionIteration {

```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `getDefaultIsolationLevel()` only delegates to its super method
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailBaseConfiguration.java`
#### Snippet
```java

	@Override
	public IsolationLevel getDefaultIsolationLevel() {
		return super.getDefaultIsolationLevel();
	}
```

### RedundantMethodOverride
Method `getPreferredValidationApproach()` is identical to its super method
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java

	@Override
	public ValidationApproach getPreferredValidationApproach(ConnectionsGroup connectionsGroup) {
		return ValidationApproach.Transactional;
	}
```

### RedundantMethodOverride
Method `setParserConfig()` only delegates to its super method
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java

	@Override
	public void setParserConfig(ParserConfig parserConfig) {
		super.setParserConfig(parserConfig);
	}
```

### RedundantMethodOverride
Method `removeNamespace()` only delegates to its super method
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/LoggingRepositoryConnection.java`
#### Snippet
```java

	@Override
	public void removeNamespace(String prefix) throws RepositoryException {
		super.removeNamespace(prefix);
	}
```

### RedundantMethodOverride
Method `getValue()` is identical to its super method
in `core/common/transaction/src/main/java/org/eclipse/rdf4j/common/transaction/IsolationLevel.java`
#### Snippet
```java

	@Override
	default String getValue() {
		return this.toString();
	}
```

### RedundantMethodOverride
Method `isEmpty()` only delegates to its super method
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/GroupGraphPattern.java`
#### Snippet
```java

	@Override
	public boolean isEmpty() {
		return super.isEmpty();
	}
```

### RedundantMethodOverride
Method `startTransactionInternal()` only delegates to its super method
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStoreConnection.java`
#### Snippet
```java

	@Override
	protected void startTransactionInternal() throws SailException {
		super.startTransactionInternal();
	}
```

### RedundantMethodOverride
Method `setBaseSail()` only delegates to its super method
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/NotifyingSailWrapper.java`
#### Snippet
```java

	@Override
	public void setBaseSail(Sail baseSail) {
		super.setBaseSail((NotifyingSail) baseSail);
	}
```

### RedundantMethodOverride
Method `stream()` is identical to its super method
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResult.java`
#### Snippet
```java
	 * @return stream a {@link Stream} of query result elements.
	 */
	default Stream<T> stream() {
		return QueryResults.stream(this);
	}
```

### RedundantMethodOverride
Method `setBaseURI()` only delegates to its super method
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java

	@Override
	protected void setBaseURI(ParsedIRI baseURI) {
		// Note: we need to override this method to allow SAXFilter to access it
		super.setBaseURI(baseURI);
```

### RedundantMethodOverride
Method `jjtAccept()` is identical to its super method
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTPathNegatedPropertySet.java`
#### Snippet
```java
	/** Accept the visitor. **/
	@Override
	public Object jjtAccept(SyntaxTreeBuilderVisitor visitor, Object data) throws VisitorException {
		return visitor.visit(this, data);
	}
```

### RedundantMethodOverride
Method `getMessage()` only delegates to its super method
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/TokenMgrError.java`
#### Snippet
```java
	 */
	@Override
	public String getMessage() {
		return super.getMessage();
	}
```

### RedundantMethodOverride
Method `visit()` only delegates to its super method
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java

	@Override
	public Object visit(ASTTriplesSameSubjectPath node, Object data) throws VisitorException {
		return super.visit(node, data);
	}
```

## RuleId[id=OptionalAssignedToNull]
### OptionalAssignedToNull
Optional value is compared with null
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	@Override
	public Optional<String> getLanguage() {
		if (optionalLanguageCache == null) {
			optionalLanguageCache = Optional.ofNullable(language);
		}
```

### OptionalAssignedToNull
Null is used for 'Optional' type in declaration
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	// Cache Optional instance for the language, or null if not yet computed. Marked as transient because Optional is
	// not serializable.
	transient private Optional<String> optionalLanguageCache = null;

	/**
```

### OptionalAssignedToNull
Optional value is compared with null
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java

	public static String getLabel(Optional<Value> v, String fallback) {
		return v != null ? getLabel(v.orElseGet(null), fallback) : fallback;
	}

```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
2 \* Pointer.POINTER_SIZE: integer multiplication implicitly cast to long
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbUtil.java`
#### Snippet
```java
	 */
	private static long mdbTxnMtNextPgno(long txn) {
		return MemoryUtil.memGetAddress(txn + 2 * Pointer.POINTER_SIZE);
	}

```

### IntegerMultiplicationImplicitCastToLong
nrbits \* entries: integer multiplication implicitly cast to long
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTArrayLog64.java`
#### Snippet
```java
				CheckedInputStream cis = new CheckedInputStream(uis, new CRC32())) {
			// read bytes, minimum 1
			long bytes = (nrbits * entries + 7) / 8;
			if (bytes > Integer.MAX_VALUE) {
				throw new UnsupportedOperationException("Maximum number of bytes in array exceeded: " + bytes);
```

### IntegerMultiplicationImplicitCastToLong
cacheLookupIndex \<\< cacheLineShift: integer shift implicitly cast to long
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/IDFile.java`
#### Snippet
```java

			// doing one big read is considerably faster than doing a single read per id
			byte[] bytes = nioFile.readBytes(ITEM_SIZE * (cacheLookupIndex << cacheLineShift),
					(int) (ITEM_SIZE * cacheLineSize));

```

### IntegerMultiplicationImplicitCastToLong
countk \* 1000: integer multiplication implicitly cast to long
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/QueryOrderBenchmark.java`
#### Snippet
```java
					assert count <= limit;
				} else {
					assert count <= countk * 1000;
				}
			}
```

### IntegerMultiplicationImplicitCastToLong
countk \* 1000: integer multiplication implicitly cast to long
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/QueryOrderBenchmark.java`
#### Snippet
```java
			assert count == limit;
		} else {
			assert count == countk * 1000;
		}
	}
```

### IntegerMultiplicationImplicitCastToLong
countk \* 1000: integer multiplication implicitly cast to long
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/QueryOrderBenchmark.java`
#### Snippet
```java
					assert count <= limit;
				} else {
					assert count <= countk * 1000;
				}
			}
```

### IntegerMultiplicationImplicitCastToLong
countk \* 1000: integer multiplication implicitly cast to long
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/QueryOrderBenchmark.java`
#### Snippet
```java
			assert count == limit;
		} else {
			assert count == countk * 1000;
		}
	}
```

### IntegerMultiplicationImplicitCastToLong
ITEM_SIZE \* slotID: integer multiplication implicitly cast to long
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/HashFile.java`
#### Snippet
```java
				diff.rewind();

				nioFile.write(diff, bucketOffset + ITEM_SIZE * slotID);
				break;
			} else {
```

## RuleId[id=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`path = path + "/shapes-graph/"` could be simplified to 'path += "/shapes-graph/"'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java
				path = path.substring(0, path.length() - 1);
			}
			path = path + "/shapes-graph/";

			logger.info("Shapes will be persisted in: " + path);
```

### ReplaceAssignmentWithOperatorAssignment
`flag = flag | Pattern.CASE_INSENSITIVE` could be simplified to 'flag \|= Pattern.CASE_INSENSITIVE'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PatternFilter.java`
#### Snippet
```java

			if (flags.contains("i")) {
				flag = flag | Pattern.CASE_INSENSITIVE;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`flag = flag | Pattern.UNIX_LINES` could be simplified to 'flag \|= Pattern.UNIX_LINES'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PatternFilter.java`
#### Snippet
```java

			if (flags.contains("d")) {
				flag = flag | Pattern.UNIX_LINES;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`flag = flag | Pattern.MULTILINE` could be simplified to 'flag \|= Pattern.MULTILINE'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PatternFilter.java`
#### Snippet
```java

			if (flags.contains("m")) {
				flag = flag | Pattern.MULTILINE;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`flag = flag | Pattern.DOTALL` could be simplified to 'flag \|= Pattern.DOTALL'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PatternFilter.java`
#### Snippet
```java

			if (flags.contains("s")) {
				flag = flag | Pattern.DOTALL;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`flag = flag | Pattern.UNICODE_CASE` could be simplified to 'flag \|= Pattern.UNICODE_CASE'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PatternFilter.java`
#### Snippet
```java

			if (flags.contains("u")) {
				flag = flag | Pattern.UNICODE_CASE;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`flag = flag | Pattern.COMMENTS` could be simplified to 'flag \|= Pattern.COMMENTS'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PatternFilter.java`
#### Snippet
```java

			if (flags.contains("x")) {
				flag = flag | Pattern.COMMENTS;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`flag = flag | Pattern.UNICODE_CHARACTER_CLASS` could be simplified to 'flag \|= Pattern.UNICODE_CHARACTER_CLASS'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PatternFilter.java`
#### Snippet
```java

			if (flags.contains("U")) {
				flag = flag | Pattern.UNICODE_CHARACTER_CLASS;
			}

```

### ReplaceAssignmentWithOperatorAssignment
`i = i + prefix.length() + 7` could be simplified to 'i += prefix.length() + 7'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/QueryPrologLexer.java`
#### Snippet
```java
					prefix = ""; // prevent NPE on bad input
				}
				i = i + prefix.length() + 7; // 6 for prefix keyword, 1 for ':'
				break;
			case 'b':
```

### ReplaceAssignmentWithOperatorAssignment
`i = i + prefix.length() + 7` could be simplified to 'i += prefix.length() + 7'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/QueryPrologLexer.java`
#### Snippet
```java
				String prefix = readPrefix(input, i);
				result.add(new Token(TokenType.PREFIX, prefix.trim()));
				i = i + prefix.length() + 7; // 6 for prefix keyword, 1 for ':'
				break;
			case 'b':
```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 1` could be simplified to 'index += 1'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ResourceUtil.java`
#### Snippet
```java
			while (index < callStack.length && (result == ResourceUtil.class || result == CallerResolver.class)) {
				result = callStack[index];
				index = index + 1;
			}
		}
```

### ReplaceAssignmentWithOperatorAssignment
`removed = removed | statements.remove( SimpleValueFactory.getInstance().createStatemen...` could be simplified to 'removed \|= statements.remove( SimpleValueFactory.getInstance().createStatement(subj, pred, obj, context)) != null'
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
			boolean removed = false;
			for (Resource context : contexts) {
				removed = removed
						| statements.remove(
								SimpleValueFactory.getInstance().createStatement(subj, pred, obj, context)) != null;
			}
			return removed;
```

### ReplaceAssignmentWithOperatorAssignment
`added = added | statements.put(statement, statement) == null` could be simplified to 'added \|= statements.put(statement, statement) == null'
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
			for (Resource context : contexts) {
				Statement statement = SimpleValueFactory.getInstance().createStatement(subj, pred, obj, context);
				added = added
						| statements.put(statement, statement) == null;
			}
			return added;
```

### ReplaceAssignmentWithOperatorAssignment
`pos = pos + 2` could be simplified to 'pos += 2'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/PrefixDeclarations.java`
#### Snippet
```java
				if (isEscapeChar(queryString, pos)) {
					sb.append(queryString, pos, pos + 2);
					pos = pos + 2;
					lastPos = pos;
					continue;
```

### ReplaceAssignmentWithOperatorAssignment
`pos = pos + 3` could be simplified to 'pos += 3'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/PrefixDeclarations.java`
#### Snippet
```java
					isInsideString = false;
					isInsideMultlineQuotes = false;
					pos = pos + 3;
					lastPos = pos;
					continue;
```

### ReplaceAssignmentWithOperatorAssignment
`pos = pos + 3` could be simplified to 'pos += 3'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/PrefixDeclarations.java`
#### Snippet
```java
					isInsideString = true;
					isInsideMultlineQuotes = true;
					pos = pos + 3;
					lastPos = pos;
					continue;
```

### ReplaceAssignmentWithOperatorAssignment
`minutes = minutes - (hours * 60)` could be simplified to 'minutes -= (hours \* 60)'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/xsd/DateTimeCast.java`
#### Snippet
```java
							int minutes = Math.abs(timezoneOffset);
							int hours = minutes / 60;
							minutes = minutes - (hours * 60);
							if (timezoneOffset > 0) {
								dtBuilder.append("+");
```

### ReplaceAssignmentWithOperatorAssignment
`minutes = minutes - (hours * 60)` could be simplified to 'minutes -= (hours \* 60)'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/datetime/Timezone.java`
#### Snippet
```java
						int minutes = Math.abs(timezoneOffset);
						int hours = minutes / 60;
						minutes = minutes - (hours * 60);

						StringBuilder tzDuration = new StringBuilder();
```

## RuleId[id=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceBranch.java`
#### Snippet
```java
					boolean locked = false;
					try {
						locked = semaphore.tryLock(tryLockMillis *= 2, TimeUnit.MILLISECONDS);
						if (locked) {
							pending.remove(this);
```

### NestedAssignment
Result of assignment expression used
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
				Namespace toCheckNext = next;
				while (toCheckNext == null && namespaces.hasNext()) {
					toCheckNext = next = namespaces.next();
					if (removedSet != null && removedSet.contains(toCheckNext.getPrefix())) {
						toCheckNext = next = null;
```

### NestedAssignment
Result of assignment expression used
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
					toCheckNext = next = namespaces.next();
					if (removedSet != null && removedSet.contains(toCheckNext.getPrefix())) {
						toCheckNext = next = null;
					}
				}
```

### NestedAssignment
Result of assignment expression used
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
				Resource toCheckNext = next;
				while (toCheckNext == null && contextIDs.hasNext()) {
					toCheckNext = next = contextIDs.next();
					if (removedSet != null && removedSet.contains(toCheckNext)) {
						toCheckNext = next = null;
```

### NestedAssignment
Result of assignment expression used
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
					toCheckNext = next = contextIDs.next();
					if (removedSet != null && removedSet.contains(toCheckNext)) {
						toCheckNext = next = null;
					}
				}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLRepository.java`
#### Snippet
```java
				result = client;
				if (result == null) {
					result = client = dependentClient = new SharedHttpClientSessionManager();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLRepository.java`
#### Snippet
```java
				result = client;
				if (result == null) {
					result = client = dependentClient = new SharedHttpClientSessionManager();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLServiceResolver.java`
#### Snippet
```java
				result = client;
				if (result == null) {
					result = client = dependentClient = new SharedHttpClientSessionManager();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLServiceResolver.java`
#### Snippet
```java
				result = client;
				if (result == null) {
					result = client = dependentClient = new SharedHttpClientSessionManager();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
			CloseableIteration<T, QueryEvaluationException> nextRightIter = rightIter;
			if (nextRightIter == null) {
				nextRightIter = rightIter = rightQueue.next();
			}
			if (nextRightIter != null) {
```

### NestedAssignment
Result of assignment expression used
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
				result = sessionManager;
				if (result == null) {
					result = sessionManager = dependentSessionManager = new SharedHttpClientSessionManager();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
				result = sessionManager;
				if (result == null) {
					result = sessionManager = dependentSessionManager = new SharedHttpClientSessionManager();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
					// handling. If the server is older, we need to run in
					// backward-compatible mode.
					result = compatibleMode = (getServerProtocolVersion() < 7);
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java

			long[] quad;
			while ((quad = iter.next()) != null) {
				if (recordCache == null) {
					if (requiresResize()) {
```

### NestedAssignment
Result of assignment expression used
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java

				Record r;
				while ((r = it.next()) != null) {
					if (requiresResize()) {
						// resize map if required
```

### NestedAssignment
Result of assignment expression used
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
							RecordIterator it = sourceIter[0];
							long[] quad;
							while ((quad = it.next()) != null) {
								keyBuf.clear();
								addedIndex.toKey(keyBuf, quad[SUBJ_IDX], quad[PRED_IDX], quad[OBJ_IDX],
```

### NestedAssignment
Result of assignment expression used
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			CompareOp compareOp;
			MathOp mathOp;
			if ((compareOp = toCompareOp(func)) != null) {
				List<ValueExpr> args = getArgs(r, func, SP.ARG1_PROPERTY, SP.ARG2_PROPERTY);
				if (args.size() != 2) {
```

### NestedAssignment
Result of assignment expression used
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				}
				expr = new Compare(args.get(0), args.get(1), compareOp);
			} else if ((mathOp = toMathOp(func)) != null) {
				List<ValueExpr> args = getArgs(r, func, SP.ARG1_PROPERTY, SP.ARG2_PROPERTY);
				if (args.size() != 2) {
```

### NestedAssignment
Result of assignment expression used
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractIRI.java`
#### Snippet
```java
				return split;

			} else if ((split = iri.indexOf('#') + 1) > 0) {

				return split;
```

### NestedAssignment
Result of assignment expression used
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractIRI.java`
#### Snippet
```java
				return split;

			} else if ((split = iri.lastIndexOf('/') + 1) > 0) {

				return split;
```

### NestedAssignment
Result of assignment expression used
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractIRI.java`
#### Snippet
```java
				return split;

			} else if ((split = iri.lastIndexOf(':') + 1) > 0) {

				return split;
```

### NestedAssignment
Result of assignment expression used
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/FileIO.java`
#### Snippet
```java
			try (DataInputStream dataIn = new DataInputStream(new GZIPInputStream(in))) {
				int recordTypeMarker;
				while ((recordTypeMarker = dataIn.readByte()) != EOF_MARKER) {
					switch (recordTypeMarker) {
					case NAMESPACE_MARKER:
```

### NestedAssignment
Result of assignment expression used
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		if (docs != null) {
			int docId;
			while ((docId = docs.nextDoc()) != PostingsEnum.NO_MORE_DOCS) {
				Bits liveDocs = reader.getLiveDocs();
				// Maybe some of the docs have been deleted! Check that too..
```

### NestedAssignment
Result of assignment expression used
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
					};

					toCheckSnapshotCleanupThread = snapshotCleanupThread = new Thread(runnable,
							"MemoryStore snapshot cleanup");
					toCheckSnapshotCleanupThread.setDaemon(true);
					toCheckSnapshotCleanupThread.start();
```

### NestedAssignment
Result of assignment expression used
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
		byte[] buf = new byte[4096];

		while ((bytesInBuf = in.read(buf)) != -1) {
			out.write(buf, 0, bytesInBuf);
			totalBytes += bytesInBuf;
```

### NestedAssignment
Result of assignment expression used
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
		int charsRead;

		while ((charsRead = r.read(buf)) != -1) {
			result.write(buf, 0, charsRead);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
		char[] buf = new char[4096];

		while ((charsInBuf = in.read(buf)) != -1) {
			out.write(buf, 0, charsInBuf);
			totalChars += charsInBuf;
```

### NestedAssignment
Result of assignment expression used
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
				result = httpClient;
				if (result == null) {
					result = httpClient = dependentClient = createHttpClient();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
				result = httpClient;
				if (result == null) {
					result = httpClient = dependentClient = createHttpClient();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
	public SharedHttpClientSessionManager(CloseableHttpClient dependentClient,
			ScheduledExecutorService dependentExecutorService) {
		this.httpClient = this.dependentClient = Objects.requireNonNull(dependentClient, "HTTP client was null");
		this.executor = Objects.requireNonNull(dependentExecutorService, "Executor service was null");
	}
```

### NestedAssignment
Result of assignment expression used
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/FileFormat.java`
#### Snippet
```java

		int dotIdx;
		while ((dotIdx = fileName.lastIndexOf('.')) >= 0) {
			String ext = fileName.substring(dotIdx + 1);

```

### NestedAssignment
Result of assignment expression used
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		List<String> list = new ArrayList<>();
		int pct = -1;
		while ((pct = path.indexOf('%', pct + 1)) > 0) {
			int start = pct;
			// optimize common encoded members by grouping separately
```

### NestedAssignment
Result of assignment expression used
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
		@Override
		public ModelStatement next() {
			return last = iter.next();
		}

```

### NestedAssignment
Result of assignment expression used
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
		@Override
		public Statement next() {
			return last = iter.next();
		}

```

### NestedAssignment
Result of assignment expression used
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDateTime.java`
#### Snippet
```java
			} else if ("Z".equals(token)) {
				isNegativeTimezone = false;
				hoursTimezone = minutesTimezone = "00";
			}

```

### NestedAssignment
Result of assignment expression used
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDateTime.java`
#### Snippet
```java

			if (hoursTimezone != null) {
				iHoursTimezone = iMinutesTimezone = 0;
				hoursTimezone = minutesTimezone = "00";
				isNegativeTimezone = false;
```

### NestedAssignment
Result of assignment expression used
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDateTime.java`
#### Snippet
```java
			if (hoursTimezone != null) {
				iHoursTimezone = iMinutesTimezone = 0;
				hoursTimezone = minutesTimezone = "00";
				isNegativeTimezone = false;
			}
```

### NestedAssignment
Result of assignment expression used
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
			flushNamespaces();
			Statement st;
			while ((st = nextStatement()) != null) {
				delegate.handleStatement(st);
			}
```

### NestedAssignment
Result of assignment expression used
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
		} else {
			if (stmts == null) {
				stmtBySubject.put(key, stmts = new TreeSet<>(comparator));
			}
			stmts.add(st);
```

### NestedAssignment
Result of assignment expression used
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
		Set<Statement> set = stmtBySubject.get(key);
		if (set == null) {
			stmtBySubject.put(key, set = new TreeSet<>(comparator));
		}
		set.addAll(matches);
```

### NestedAssignment
Result of assignment expression used
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java

				// New node becomes the current
				subject = bNode = newNode;

				parseObject();
```

### NestedAssignment
Result of assignment expression used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			resultIter = iter;
			if (resultIter == null) {
				resultIter = iter = createIteration();
			}
		}
```

### NestedAssignment
Result of assignment expression used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			resultIter = iter;
			if (resultIter == null) {
				resultIter = iter = createIteration();
			}
		}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RemoteRepositoryManager.java`
#### Snippet
```java
				result = client;
				if (result == null) {
					result = client = new SharedHttpClientSessionManager();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryProvider.java`
#### Snippet
```java
				sync = managers.get(uri);
			} else {
				managers.put(uri, sync = new SynchronizedManager(url));
			}
		}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
				result = client;
				if (result == null) {
					result = client = new SharedHttpClientSessionManager();
				}
			}
```

### NestedAssignment
Result of assignment expression used
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
				result = serviceResolver;
				if (result == null) {
					result = serviceResolver = new SPARQLServiceResolver();
					result.setHttpClientSessionManager(getSesameClient());
				}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java

		int emptyLineCount = 0;
		while ((line = in.readLine()) != null) {
			if (line.length() > 0) {
				emptyLineCount = 0;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java
		}

		if ((buffer[bufpos] = c = ReadByte()) == '\\') {
			UpdateLineColumn(c);

```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java
		}

		if ((buffer[bufpos] = c = ReadByte()) == '\\') {
			UpdateLineColumn(c);

```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java

				try {
					if ((buffer[bufpos] = c = ReadByte()) != '\\') {
						UpdateLineColumn(c);
						// found a non-backslash char.
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java

				try {
					if ((buffer[bufpos] = c = ReadByte()) != '\\') {
						UpdateLineColumn(c);
						// found a non-backslash char.
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java
			try {
				if (c == 'u') {
					buffer[bufpos] = c = (char) (hexval(ReadByte()) << 12 | hexval(ReadByte()) << 8
							| hexval(ReadByte()) << 4 | hexval(ReadByte()));
					column += 4;
				} else if (c == 'U') {
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java
					int cp = Integer.parseInt(hex, 16);
					char[] chrs = Character.toChars(cp); // length of 1 or 2
					buffer[bufpos] = c = chrs[0];
					if (chrs.length > 1) {
						if (++bufpos == available) {
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/TokenMgrError.java`
#### Snippet
```java
				continue;
			default:
				if ((ch = str.charAt(i)) < 0x20 || ch > 0x7e) {
					String s = "0000" + Integer.toString(ch, 16);
					retval.append("\\u").append(s.substring(s.length() - 4));
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		int strPos = jjmatchedPos;
		int seenUpto;
		input_stream.backup(seenUpto = curPos + 1);
		try {
			curChar = input_stream.readChar();
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			}
			++curPos;
			if ((i = jjnewStateCnt) == (startsAt = 157 - (jjnewStateCnt = startsAt))) {
				break;
			}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			}
			++curPos;
			if ((i = jjnewStateCnt) == (startsAt = 157 - (jjnewStateCnt = startsAt))) {
				break;
			}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			}
			++curPos;
			if ((i = jjnewStateCnt) == (startsAt = 157 - (jjnewStateCnt = startsAt))) {
				break;
			}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa10_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(9);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa10_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(9);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa3_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(2);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa3_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(2);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa3_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(2);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa2_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(1);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa2_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(1);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa2_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(1);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa9_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(8);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa9_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(8);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa4_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(3);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa4_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(3);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa4_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(3);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa6_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(5);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa6_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(5);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa6_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(5);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa5_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(4);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa5_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(4);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa5_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(4);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
	public void ReInit(CharStream stream) {

		jjmatchedPos = jjnewStateCnt = 0;
		curLexState = defaultLexState;
		input_stream = stream;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
						} else {
							matchedToken.specialToken = specialToken;
							specialToken = (specialToken.next = matchedToken);
						}
					}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa7_0(long old0, long active0, long old1, long active1, long old2) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(6);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa7_0(long old0, long active0, long old1, long active1, long old2) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(6);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa12_0(long old1, long active1) {
		if (((active1 &= old1)) == 0L) {
			return jjMoveNfa_0(11);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa13_0(long old1, long active1) {
		if (((active1 &= old1)) == 0L) {
			return jjMoveNfa_0(12);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa11_0(long old0, long old1, long active1) {
		if (((active1 &= old1)) == 0L) {
			return jjMoveNfa_0(10);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa8_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(7);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa8_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(7);
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
				continue;
			default:
				if ((ch = str.charAt(i)) < 0x20 || ch > 0x7e) {
					String s = "0000" + Integer.toString(ch, 16);
					retval.append("\\u" + s.substring(s.length() - 4, s.length()));
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		}

		available = (bufsize += 2048);
		tokenBegin = 0;
	}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

		inBuf += amount;
		if ((bufpos -= amount) < 0) {
			bufpos += bufsize;
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		int i;
		if (maxNextCharInd == nextCharBuf.length) {
			maxNextCharInd = nextCharInd = 0;
		}

```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

		try {
			if ((i = inputStream.read(nextCharBuf, maxNextCharInd, nextCharBuf.length - maxNextCharInd)) == -1) {
				inputStream.close();
				throw IO_EXCEPTION;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		int nextColDiff, columnDiff = 0;

		while (i < len && bufline[j = start % bufsize] == bufline[k = ++start % bufsize]) {
			bufline[j] = newLine;
			nextColDiff = columnDiff + bufcolumn[k] - bufcolumn[j];
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		int nextColDiff, columnDiff = 0;

		while (i < len && bufline[j = start % bufsize] == bufline[k = ++start % bufsize]) {
			bufline[j] = newLine;
			nextColDiff = columnDiff + bufcolumn[k] - bufcolumn[j];
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

			while (i++ < len) {
				if (bufline[j = start % bufsize] != bufline[++start % bufsize]) {
					bufline[j] = newLine++;
				} else {
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

		if (buffer == null || buffersize != buffer.length) {
			available = bufsize = buffersize;
			buffer = new char[buffersize];
			bufline = new int[buffersize];
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
			nextCharBuf = new char[buffersize];
		}
		prevCharIsLF = prevCharIsCR = false;
		tokenBegin = inBuf = maxNextCharInd = 0;
		nextCharInd = bufpos = -1;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		}
		prevCharIsLF = prevCharIsCR = false;
		tokenBegin = inBuf = maxNextCharInd = 0;
		nextCharInd = bufpos = -1;
	}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		}
		prevCharIsLF = prevCharIsCR = false;
		tokenBegin = inBuf = maxNextCharInd = 0;
		nextCharInd = bufpos = -1;
	}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		prevCharIsLF = prevCharIsCR = false;
		tokenBegin = inBuf = maxNextCharInd = 0;
		nextCharInd = bufpos = -1;
	}

```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		}

		if ((buffer[bufpos] = c = ReadByte()) == '\\') {
			UpdateLineColumn(c);

```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		}

		if ((buffer[bufpos] = c = ReadByte()) == '\\') {
			UpdateLineColumn(c);

```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

				try {
					if ((buffer[bufpos] = c = ReadByte()) != '\\') {
						UpdateLineColumn(c);
						// found a non-backslash char.
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

				try {
					if ((buffer[bufpos] = c = ReadByte()) != '\\') {
						UpdateLineColumn(c);
						// found a non-backslash char.
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
			// Here, we have seen an odd number of backslash's followed by a 'u'
			try {
				while ((c = ReadByte()) == 'u') {
					++column;
				}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
				}

				buffer[bufpos] = c = (char) (hexval(c) << 12 | hexval(ReadByte()) << 8 | hexval(ReadByte()) << 4
						| hexval(ReadByte()));

				column += 4;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		if (prevCharIsLF) {
			prevCharIsLF = false;
			line += (column = 1);
		} else if (prevCharIsCR) {
			prevCharIsCR = false;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
				prevCharIsLF = true;
			} else {
				line += (column = 1);
			}
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		column = startcolumn - 1;

		available = bufsize = buffersize;
		buffer = new char[buffersize];
		bufline = new int[buffersize];
```

### NestedAssignment
Result of assignment expression used
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/ProxyRepository.java`
#### Snippet
```java
					assert null != proxiedID : "Expected proxiedID to be set.";
					try {
						result = proxiedRepository = resolver.getRepository(proxiedID);
					} catch (RDF4JException ore) {
						throw new IllegalStateException(ore);
```

### NestedAssignment
Result of assignment expression used
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/ndjsonld/NDJSONLDParser.java`
#### Snippet
```java
		try (BufferedReader bufferedReader = new BufferedReader(reader)) {
			String line;
			while ((line = bufferedReader.readLine()) != null) {
				if (!line.isEmpty()) {
					JsonParser nextParser = factory
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
		int xla = 2;
		jj_la = xla;
		jj_lastpos = jj_scanpos = token;
		try {
			return !jj_3_4();
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
					if (p.gen > jj_gen) {
						jj_la = p.arg;
						jj_lastpos = jj_scanpos = p.first;
						switch (i) {
						case 0:
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jj_la--;
			if (jj_scanpos.next == null) {
				jj_lastpos = jj_scanpos = jj_scanpos.next = token_source.getNextToken();
			} else {
				jj_lastpos = jj_scanpos = jj_scanpos.next;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jj_la--;
			if (jj_scanpos.next == null) {
				jj_lastpos = jj_scanpos = jj_scanpos.next = token_source.getNextToken();
			} else {
				jj_lastpos = jj_scanpos = jj_scanpos.next;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jj_lastpos = jj_scanpos = jj_scanpos.next = token_source.getNextToken();
			} else {
				jj_lastpos = jj_scanpos = jj_scanpos.next;
			}
		} else {
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
		int xla = 2;
		jj_la = xla;
		jj_lastpos = jj_scanpos = token;
		try {
			return !jj_3_2();
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
		int xla = 2;
		jj_la = xla;
		jj_lastpos = jj_scanpos = token;
		try {
			return !jj_3_3();
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
		while (p.gen > jj_gen) {
			if (p.next == null) {
				p = p.next = new JJCalls();
				break;
			}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				t = t.next;
			} else {
				t = t.next = token_source.getNextToken();
			}
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java

	private int jj_ntk_f() {
		if ((jj_nt = token.next) == null) {
			return (jj_ntk = (token.next = token_source.getNextToken()).kind);
		} else {
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	private int jj_ntk_f() {
		if ((jj_nt = token.next) == null) {
			return (jj_ntk = (token.next = token_source.getNextToken()).kind);
		} else {
			return (jj_ntk = jj_nt.kind);
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	private int jj_ntk_f() {
		if ((jj_nt = token.next) == null) {
			return (jj_ntk = (token.next = token_source.getNextToken()).kind);
		} else {
			return (jj_ntk = jj_nt.kind);
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			return (jj_ntk = (token.next = token_source.getNextToken()).kind);
		} else {
			return (jj_ntk = jj_nt.kind);
		}
	}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
		int xla = 2;
		jj_la = xla;
		jj_lastpos = jj_scanpos = token;
		try {
			return !jj_3_7();
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	private Token jj_consume_token(int kind) throws ParseException {
		Token oldToken;
		if ((oldToken = token).next != null) {
			token = token.next;
		} else {
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			token = token.next;
		} else {
			token = token.next = token_source.getNextToken();
		}
		jj_ntk = -1;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			}
			if (pos != 0) {
				jj_lasttokens[(jj_endpos = pos) - 1] = kind;
			}
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
		int xla = 2;
		jj_la = xla;
		jj_lastpos = jj_scanpos = token;
		try {
			return !jj_3_1();
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
		int xla = 2;
		jj_la = xla;
		jj_lastpos = jj_scanpos = token;
		try {
			return !jj_3_5();
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			token = token.next;
		} else {
			token = token.next = token_source.getNextToken();
		}
		jj_ntk = -1;
```

### NestedAssignment
Result of assignment expression used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
		int xla = 2;
		jj_la = xla;
		jj_lastpos = jj_scanpos = token;
		try {
			return !jj_3_6();
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/AbstractRecordCache.java`
#### Snippet
```java
			try (RecordIterator recIter = otherCache.getRecords()) {
				byte[] record;
				while ((record = recIter.next()) != null && recordCount.incrementAndGet() <= maxRecords) {
					storeRecordInternal(record);
				}
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/RangeIterator.java`
#### Snippet
```java

	private void findMinimum() throws IOException {
		Node nextCurrentNode = currentNode = tree.readRootNode();

		if (nextCurrentNode == null) {
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
			try {
				byte[] data;
				while ((data = iter.next()) != null) {
					byte flags = data[FLAG_IDX];
					boolean wasAdded = (flags & ADDED_FLAG) != 0;
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
			try {
				byte[] data;
				while ((data = iter.next()) != null) {
					byte flags = data[FLAG_IDX];
					boolean wasAdded = (flags & ADDED_FLAG) != 0;
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
					sourceIter = sourceIndex.getBTree().iterateAll();
					byte[] value;
					while ((value = sourceIter.next()) != null) {
						addedBTree.insert(value);
					}
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
			byte[] result;

			while ((result = wrappedIter.next()) != null) {
				byte flags = result[TripleStore.FLAG_IDX];
				boolean explicit = (flags & TripleStore.EXPLICIT_FLAG) != 0;
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
			byte[] result;

			while ((result = wrappedIter.next()) != null) {
				byte flags = result[TripleStore.FLAG_IDX];
				boolean explicit = (flags & TripleStore.EXPLICIT_FLAG) != 0;
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java

				try (RecordIterator recIter = removedTriplesCache.getRecords()) {
					while ((data = recIter.next()) != null) {
						btree.insert(data);
					}
```

### NestedAssignment
Result of assignment expression used
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/DataStore.java`
#### Snippet
```java
		HashFile.IDIterator iter = hashFile.getIDIterator(hash);
		try {
			while ((id = iter.next()) >= 0) {
				long offset = idFile.getOffset(id);
				byte[] data = dataFile.getData(offset);
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryModelNormalizer.java`
#### Snippet
```java
		QueryModelNode node = leftJoin;
		QueryModelNode parent;
		while ((parent = node.getParentNode()) != null) {
			bindingCollector.setNodeToIgnore(node);
			parent.visitChildren(bindingCollector);
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/LeftJoinIterator.java`
#### Snippet
```java

					nextRightIter.close();
					nextRightIter = rightIter = prepareRightArg.evaluate(leftBindings);
				}

```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			valIndex = strVal.indexOf(snippet, valIndex);
			int i;
			while ((i = strVal.indexOf(snippet, valIndex + 1)) != -1) {
				// A later occurence was found.
				valIndex = i;
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
	static {
		// noinspection AssertWithSideEffects
		assert assertsEnabled = true;
	}

```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryModelNormalizerOptimizer.java`
#### Snippet
```java
		QueryModelNode node = leftJoin;
		QueryModelNode parent;
		while ((parent = node.getParentNode()) != null) {
			bindingCollector.setNodeToIgnore(node);
			parent.visitChildren(bindingCollector);
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
		Map<BindingSetHashKey, List<BindingSet>> nextHashTable = hashTable;
		if (nextHashTable == null) {
			nextHashTable = hashTable = setupHashTable();
		}

```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
					Collection<List<BindingSet>> values = nextHashTable.values();
					boolean empty = values.isEmpty() || values.size() == 1 && values.contains(null);
					nextHashTableValues = hashTableValues = empty ? new EmptyIterator<>() : new UnionIterator<>(values);
					if (!nextHashTableValues.hasNext()) {
						currentScanElem = null;
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
						currentScanElem = null;
						closeHashValue(nextHashTableValues);
						nextHashTableValues = hashTableValues = null;
					}
				} else {
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
					List<BindingSet> hashValue = nextHashTable.get(key);
					if (hashValue != null && !hashValue.isEmpty()) {
						nextHashTableValues = hashTableValues = hashValue.iterator();
					} else if (leftJoin) {
						nextHashTableValues = hashTableValues = Collections.singletonList(EmptyBindingSet.getInstance())
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
						nextHashTableValues = hashTableValues = hashValue.iterator();
					} else if (leftJoin) {
						nextHashTableValues = hashTableValues = Collections.singletonList(EmptyBindingSet.getInstance())
								.iterator();
					} else {
						currentScanElem = null;
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
						currentScanElem = null;
						closeHashValue(nextHashTableValues);
						nextHashTableValues = hashTableValues = null;
					}
				}
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
				currentScanElem = null;
				closeHashValue(nextHashTableValues);
				nextHashTableValues = hashTableValues = null;
			}
			return result;
```

### NestedAssignment
Result of assignment expression used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
				return next;
			} else {
				return next = poll();
			}
		}
```

### NestedAssignment
Result of assignment expression used
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
	 */
	public void reset() {
		mLimit = mOffset = -1;
		mDistinct = mReduced = false;

```

### NestedAssignment
Result of assignment expression used
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
	public void reset() {
		mLimit = mOffset = -1;
		mDistinct = mReduced = false;

		mExtensions.clear();
```

## RuleId[id=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `sparqlTransaction` is accessed in both synchronized and unsynchronized contexts
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
	private final ModelFactory modelFactory = new DynamicModelFactory();

	private StringBuilder sparqlTransaction;

	private final Object transactionLock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pendingAdds` is accessed in both synchronized and unsynchronized contexts
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
	private final Object transactionLock = new Object();

	private Model pendingAdds;
	private Model pendingRemoves;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pendingRemoves` is accessed in both synchronized and unsynchronized contexts
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java

	private Model pendingAdds;
	private Model pendingRemoves;

	private final int maxPendingSize = DEFAULT_MAX_PENDING_SIZE;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `shapesRepo` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java
	 * an initialized {@link Repository} for storing/retrieving Shapes data
	 */
	private SailRepository shapesRepo;

	// lockManager used for read/write locks used to synchronize validation so that SNAPSHOT isolation is sufficient to
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cachedShapes` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java

	// shapesCacheLockManager used to keep track of changes to the cache
	private StampedLockManager.Cache<List<ContextWithShapes>> cachedShapes;

	// true if the base sail supports IsolationLevels.SNAPSHOT
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `managedConn` is accessed in both synchronized and unsynchronized contexts
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
	protected boolean shutDown;

	private RepositoryConnection managedConn = null;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stats` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	public final ShaclSail sail;

	private Stats stats;

	RdfsSubClassOfReasoner rdfsSubClassOfReasoner;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `transactionSettings` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	private IsolationLevel currentIsolationLevel = null;

	private Settings transactionSettings;
	private TransactionSetting[] transactionSettingsRaw = new TransactionSetting[0];
	private volatile boolean closed;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `shapesModifiedInCurrentTransaction` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java

	private boolean shapeRefreshNeeded = false;
	private boolean shapesModifiedInCurrentTransaction = false;

	public final ShaclSail sail;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `addedStatements` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	private IRI[] shapesGraphs;

	Sail addedStatements;
	Sail removedStatements;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentIsolationLevel` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	private boolean connectionListenerActive = false;

	private IsolationLevel currentIsolationLevel = null;

	private Settings transactionSettings;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `rdfsSubClassOfReasoner` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	private Stats stats;

	RdfsSubClassOfReasoner rdfsSubClassOfReasoner;

	private boolean prepareHasBeenCalled = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `removedStatements` is accessed in both synchronized and unsynchronized contexts
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java

	Sail addedStatements;
	Sail removedStatements;

	private final HashSet<Statement> addedStatementsSet = new HashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/UserProvidedClientProvider.java`
#### Snippet
```java
	final private Client client;

	transient boolean closed;

	public UserProvidedClientProvider(Client client) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `client` is accessed in both synchronized and unsynchronized contexts
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/SingletonClientProvider.java`
#### Snippet
```java
public class SingletonClientProvider implements ClientProvider {

	transient private Client client;
	private transient boolean closed = false;
	private final String hostname;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/SingletonClientProvider.java`
#### Snippet
```java

	transient private Client client;
	private transient boolean closed = false;
	private final String hostname;
	private final int port;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `BUFFER_THRESHOLD` is accessed in both synchronized and unsynchronized contexts
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
	private static final String MAPPING;

	private int BUFFER_THRESHOLD = 1024 * 16;
	private final ClientProvider clientProvider;
	private Set<ExtensibleStatement> addStatementBuffer = new HashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `addStatementBuffer` is accessed in both synchronized and unsynchronized contexts
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
	private int BUFFER_THRESHOLD = 1024 * 16;
	private final ClientProvider clientProvider;
	private Set<ExtensibleStatement> addStatementBuffer = new HashSet<>();
	private Set<ElasticsearchId> deleteStatementBuffer = new HashSet<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `domainStatements` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	private Collection<Statement> rangeStatements = new HashSet<>();

	private Collection<Statement> domainStatements = new HashSet<>();

	// Forward chained schema cache as lookup tables
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subPropertyOfStatements` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	private Collection<Statement> subClassOfStatements = new HashSet<>();

	private Collection<Statement> subPropertyOfStatements = new HashSet<>();

	private Collection<Statement> rangeStatements = new HashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `calculatedProperties` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	private Map<Resource, Set<Resource>> calculatedTypes = new HashMap<>();

	private Map<Resource, Set<Resource>> calculatedProperties = new HashMap<>();

	private Map<Resource, Set<Resource>> calculatedRange = new HashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `calculatedDomain` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	private Map<Resource, Set<Resource>> calculatedRange = new HashMap<>();

	private Map<Resource, Set<Resource>> calculatedDomain = new HashMap<>();

	// The inferencer has been instantiated from another inferencer and shares it's schema with that one
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `rangeStatements` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	private Collection<Statement> subPropertyOfStatements = new HashSet<>();

	private Collection<Statement> rangeStatements = new HashSet<>();

	private Collection<Statement> domainStatements = new HashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subClassOfStatements` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	private Collection<Resource> types = new HashSet<>();

	private Collection<Statement> subClassOfStatements = new HashSet<>();

	private Collection<Statement> subPropertyOfStatements = new HashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `calculatedTypes` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java

	// Forward chained schema cache as lookup tables
	private Map<Resource, Set<Resource>> calculatedTypes = new HashMap<>();

	private Map<Resource, Set<Resource>> calculatedProperties = new HashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `types` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	private Collection<Resource> properties = new HashSet<>();

	private Collection<Resource> types = new HashSet<>();

	private Collection<Statement> subClassOfStatements = new HashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `properties` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java

	// Schema cache
	private Collection<Resource> properties = new HashSet<>();

	private Collection<Resource> types = new HashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `calculatedRange` is accessed in both synchronized and unsynchronized contexts
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	private Map<Resource, Set<Resource>> calculatedProperties = new HashMap<>();

	private Map<Resource, Set<Resource>> calculatedRange = new HashMap<>();

	private Map<Resource, Set<Resource>> calculatedDomain = new HashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `multiThreadingActive` is accessed in both synchronized and unsynchronized contexts
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java
	private volatile Throwable tripleStoreException;
	private final AtomicBoolean running = new AtomicBoolean(false);
	private boolean multiThreadingActive;
	private volatile boolean asyncTransactionFinished;
	private volatile boolean nextTransactionAsync;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dependentServiceResolver` is accessed in both synchronized and unsynchronized contexts
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbStore.java`
#### Snippet
```java
	 * dependent life cycle
	 */
	private SPARQLServiceResolver dependentServiceResolver;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `active` is accessed in both synchronized and unsynchronized contexts
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
	private final RDF4JProtocolSession client;

	private boolean active;

	private Model toAdd;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `disk` is accessed in both synchronized and unsynchronized contexts
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	transient SailStore store;

	transient SailSourceModel disk;

	private long baseline = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dataDir` is accessed in both synchronized and unsynchronized contexts
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	private LinkedHashModel memory;

	transient File dataDir;

	transient SailStore store;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `store` is accessed in both synchronized and unsynchronized contexts
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	transient File dataDir;

	transient SailStore store;

	transient SailSourceModel disk;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `memory` is accessed in both synchronized and unsynchronized contexts
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	final Logger logger = LoggerFactory.getLogger(MemoryOverflowModel.class);

	private LinkedHashModel memory;

	transient File dataDir;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dependentServiceResolver` is accessed in both synchronized and unsynchronized contexts
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemoryStore.java`
#### Snippet
```java
	 * dependent life cycle
	 */
	private SPARQLServiceResolver dependentServiceResolver;

	/*--------------*
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `store` is accessed in both synchronized and unsynchronized contexts
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemoryStore.java`
#### Snippet
```java
	 * Factory/cache for MemValue objects.
	 */
	private SailStore store;

	private volatile boolean persist = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `observations` is accessed in both synchronized and unsynchronized contexts
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
		private int nextSnapshot;

		private Set<StatementPattern> observations;

		private volatile boolean txnLock;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nextSnapshot` is accessed in both synchronized and unsynchronized contexts
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
		private final SnapshotMonitor.ReservedSnapshot reservedSnapshot;

		private int nextSnapshot;

		private Set<StatementPattern> observations;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `requireCleanup` is accessed in both synchronized and unsynchronized contexts
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
		private volatile boolean txnLock;

		private boolean requireCleanup;

		public MemorySailSink(boolean explicit, boolean serializable) throws SailException {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `statements` is accessed in both synchronized and unsynchronized contexts
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
	private static final Resource[] NULL_CTX = new Resource[] { null };

	private Map<Statement, Statement> statements = new LinkedHashMap<>();
	final Set<Namespace> namespaces = new LinkedHashSet<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `client` is accessed in both synchronized and unsynchronized contexts
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java
	private final Logger logger = LoggerFactory.getLogger(getClass());

	private SolrClient client;

	private Function<? super String, ? extends SpatialContext> geoContextMapper;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `ping` is accessed in both synchronized and unsynchronized contexts
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	private final ScheduledExecutorService pingScheduler;

	private ScheduledFuture<?> ping;

	private long pingDelay = PINGDELAY;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `transactionURL` is accessed in both synchronized and unsynchronized contexts
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	private String serverURL;

	private String transactionURL;

	private final ScheduledExecutorService pingScheduler;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `repeatBlankNodes` is accessed in both synchronized and unsynchronized contexts
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
	private final RDFWriter delegate;

	private boolean repeatBlankNodes;

	private int targetQueueSize;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetQueueSize` is accessed in both synchronized and unsynchronized contexts
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
	private boolean repeatBlankNodes;

	private int targetQueueSize;

	private int queueSize = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lineNumber` is accessed in both synchronized and unsynchronized contexts
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
	protected Value object;

	private int lineNumber = 1;

	private final StringBuilder parsingBuilder = new StringBuilder();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `reader` is accessed in both synchronized and unsynchronized contexts
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
	 *-----------*/

	private PushbackReader reader;

	protected Resource subject;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inlineBNodes` is accessed in both synchronized and unsynchronized contexts
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
	private Boolean xsdStringToPlainLiteral;
	private Boolean prettyPrint;
	private boolean inlineBNodes;
	private Boolean abbreviateNumbers;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bufferSize` is accessed in both synchronized and unsynchronized contexts
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
	 * everything until the end (necessary when blank node inlining).
	 */
	private long bufferSize = DEFAULT_BUFFER_SIZE;
	protected Model bufferedStatements;
	private final Object bufferLock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bufferedStatements` is accessed in both synchronized and unsynchronized contexts
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
	 */
	private long bufferSize = DEFAULT_BUFFER_SIZE;
	protected Model bufferedStatements;
	private final Object bufferLock = new Object();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentIndex` is accessed in both synchronized and unsynchronized contexts
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java
	protected BufferedReader reader;
	protected char[] lineChars;
	protected int currentIndex;
	protected long lineNo;
	protected Resource subject;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lineNo` is accessed in both synchronized and unsynchronized contexts
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java
	protected char[] lineChars;
	protected int currentIndex;
	protected long lineNo;
	protected Resource subject;
	protected IRI predicate;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lineChars` is accessed in both synchronized and unsynchronized contexts
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java

	protected BufferedReader reader;
	protected char[] lineChars;
	protected int currentIndex;
	protected long lineNo;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sailStore` is accessed in both synchronized and unsynchronized contexts
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	private static final Logger logger = LoggerFactory.getLogger(ExtensibleStore.class);

	protected ExtensibleSailStore sailStore;

	protected N namespaceStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `evalStratFactory` is accessed in both synchronized and unsynchronized contexts
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	final boolean cacheEnabled;

	private EvaluationStrategyFactory evalStratFactory;
	private SPARQLServiceResolver dependentServiceResolver;
	private FederatedServiceResolver serviceResolver;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serviceResolver` is accessed in both synchronized and unsynchronized contexts
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	private EvaluationStrategyFactory evalStratFactory;
	private SPARQLServiceResolver dependentServiceResolver;
	private FederatedServiceResolver serviceResolver;

	public ExtensibleStore() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `internalRemoved` is accessed in both synchronized and unsynchronized contexts
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCommittedWrapper.java`
#### Snippet
```java

	private Map<ExtensibleStatement, ExtensibleStatement> internalAdded = new HashMap<>();
	private Map<ExtensibleStatement, ExtensibleStatement> internalRemoved = new HashMap<>();

	ReadCommittedWrapper(DataStructureInterface dataStructure) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initialized` is accessed in both synchronized and unsynchronized contexts
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	protected Map<String, Repository> initializedRepositories;

	private boolean initialized;

	/*--------------*
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initializedRepositories` is accessed in both synchronized and unsynchronized contexts
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	private ModelFactory modelFactory = new TreeModelFactory();

	protected Map<String, Repository> initializedRepositories;

	private boolean initialized;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failed` is accessed in both synchronized and unsynchronized contexts
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
	private String failedMessage;

	private Throwable failed;

	/*---------*
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failed` is accessed in both synchronized and unsynchronized contexts
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
	private String failedMessage;

	private Throwable failed;

	/*---------*
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `xmlLang` is accessed in both synchronized and unsynchronized contexts
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
	 * reported.
	 */
	private String xmlLang;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `documentURI` is accessed in both synchronized and unsynchronized contexts
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
	 * not be changed during parsing.
	 */
	private String documentURI;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `proxiedID` is accessed in both synchronized and unsynchronized contexts
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/ProxyRepository.java`
#### Snippet
```java
	private volatile Repository proxiedRepository;

	private String proxiedID;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dependentServiceResolver` is accessed in both synchronized and unsynchronized contexts
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeStore.java`
#### Snippet
```java
	 * dependent life cycle
	 */
	private SPARQLServiceResolver dependentServiceResolver;

	/**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `indexName` is accessed in both synchronized and unsynchronized contexts
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
	private String clusterName;

	private String indexName;

	private String documentType;
```

## RuleId[id=EqualsAndHashcode]
### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLQueryBindingSet.java`
#### Snippet
```java
 * dependency between the algebra-evaluation module and the sparql-repository module.
 */
public class SPARQLQueryBindingSet extends AbstractBindingSet {

	private static final long serialVersionUID = -2010715346095527301L;
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/operations/RemoveStatementsOperation.java`
#### Snippet
```java
 * @author Leo Sauermann
 */
public class RemoveStatementsOperation extends StatementOperation implements Serializable {

	/**
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/operations/ClearOperation.java`
#### Snippet
```java
 * @author Leo Sauermann
 */
public class ClearOperation extends ContextOperation implements Serializable {

	/**
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/operations/AddStatementOperation.java`
#### Snippet
```java
 * @author Leo Sauermann
 */
public class AddStatementOperation extends StatementOperation implements Serializable {

	/**
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/model/LmdbBNode.java`
#### Snippet
```java
import org.eclipse.rdf4j.sail.lmdb.ValueStoreRevision;

public class LmdbBNode extends SimpleBNode implements LmdbResource {

	/*-----------*
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/model/LmdbIRI.java`
#### Snippet
```java
import org.eclipse.rdf4j.sail.lmdb.ValueStoreRevision;

public class LmdbIRI extends SimpleIRI implements LmdbResource {

	private static final long serialVersionUID = -5888138591826143179L;
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java
	}

	private static class WktShape<S extends Shape> implements Shape {

		final S s;
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/NumericLiteral.java`
#### Snippet
```java
 * @author Jerven Bolleman
 */
public class NumericLiteral extends AbstractLiteral {

	private static final long serialVersionUID = 3004497457768807919L;
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
	}

	public static class ModelStatement extends ContextStatement {

		private static final long serialVersionUID = 2200404772364346279L;
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/model/NativeLiteral.java`
#### Snippet
```java
import org.eclipse.rdf4j.sail.nativerdf.ValueStoreRevision;

public class NativeLiteral extends SimpleLiteral implements NativeValue {

	/*-----------*
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/model/NativeIRI.java`
#### Snippet
```java
import org.eclipse.rdf4j.sail.nativerdf.ValueStoreRevision;

public class NativeIRI extends SimpleIRI implements NativeResource {

	private static final long serialVersionUID = -5888138591826143179L;
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/model/NativeBNode.java`
#### Snippet
```java
import org.eclipse.rdf4j.sail.nativerdf.ValueStoreRevision;

public class NativeBNode extends SimpleBNode implements NativeResource {

	/*-----------*
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/QueryBindingSet.java`
#### Snippet
```java
 * Binding objects are created lazily.
 */
public class QueryBindingSet extends AbstractBindingSet implements MutableBindingSet {

	private static final long serialVersionUID = -2010715346095527301L;
```

## RuleId[id=HtmlWrongAttributeValue]
### HtmlWrongAttributeValue
Wrong attribute value
in `log/indexing-diagnostic/project.15375f63/diagnostic-2023-03-09-03-55-16.414.html`
#### Snippet
```java
              <td>0</td>
              <td>0</td>
              <td><textarea rows="10" cols="75" readonly="true" placeholder="empty" style="white-space: pre; border: none">Not collected for refresh</textarea></td>
            </tr>
          </tbody>
```

## RuleId[id=CallToStringConcatCanBeReplacedByOperator]
### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/InternedIRI.java`
#### Snippet
```java
		this.namespace = namespace;
		this.localName = localName;
		this.stringValue = namespace.concat(localName).intern();
		this.hashCode = stringValue.hashCode();
	}
```

## RuleId[id=StringRepeatCanBeUsed]
### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationExecutionLogger.java`
#### Snippet
```java
	private static String leadingSpace(int depth) {
		StringBuilder builder = new StringBuilder();
		for (int i = depth; i > 0; i--) {
			builder.append("  ");
		}
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/QueryModelTreePrinter.java`
#### Snippet
```java
	@Override
	protected void meetNode(QueryModelNode node) {
		for (int i = 0; i < indentLevel; i++) {
			sb.append(indentString);
		}
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
		// insert zeros if hexVal has less than stringLength characters:
		int nofZeros = stringLength - hexVal.length();
		for (int i = 0; i < nofZeros; i++) {
			sb.append('0');
		}
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		// last segment is empty or file name
		for (int i = same; i < paths.length - 1; i++) {
			sb.append("../");
		}
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDateTime.java`
#### Snippet
```java
		if (zeroCount > 0) {
			StringBuilder sb = new StringBuilder(minDigits);
			for (int i = 0; i < zeroCount; i++) {
				sb.append('0');
			}
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `core/common/text/src/main/java/org/eclipse/rdf4j/common/text/StringUtil.java`
#### Snippet
```java
	 */
	public static void appendN(char c, int n, StringBuilder sb) {
		for (int i = n; i > 0; i--) {
			sb.append(c);
		}
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlTupleExprRenderer.java`
#### Snippet
```java
	private String indent() {
		final StringBuilder aBuilder = new StringBuilder();
		for (int i = 0; i < mIndent; i++) {
			aBuilder.append(" ");
		}
```

## RuleId[id=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `core/rio/rdfjson/src/main/java/org/eclipse/rdf4j/rio/rdfjson/RDFJSONParser.java`
#### Snippet
```java

	private void rdfJsonToHandlerInternal(final RDFHandler handler, final ValueFactory vf, final JsonParser jp)
			throws IOException, JsonParseException, RDFParseException, RDFHandlerException {
		if (jp.nextToken() != JsonToken.START_OBJECT) {
			reportFatalError("Expected RDF/JSON document to start with an Object", jp.getCurrentLocation());
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java

	@Override
	public boolean isActive() throws UnknownTransactionStateException, RepositoryException {
		synchronized (transactionLock) {
			return sparqlTransaction != null;
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java

	@Override
	public boolean isActive() throws UnknownTransactionStateException, RepositoryException {
		return active;
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/AbstractComplianceTest.java`
#### Snippet
```java

		@Override
		public boolean isActive() throws UnknownTransactionStateException, RepositoryException {
			return delegate.isActive();
		}
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `testsuites/shacl/src/main/java/org/eclipse/rdf4j/testsuite/shacl/manifest/SHACLManifestTestSuiteFactory.java`
#### Snippet
```java
	}

	private Model readTurtle(String url) throws RDFParseException, MalformedURLException, IOException {
		Model model = new LinkedHashModel();
		readTurtle(model, new URL(url), url);
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	protected void upload(final Reader contents, String baseURI, final RDFFormat dataFormat, boolean overwrite,
			boolean preserveNodeIds, Action action, Resource... contexts)
			throws IOException, RDFParseException, RepositoryException, UnauthorizedException {
		final Charset charset = dataFormat.hasCharset() ? dataFormat.getCharset() : StandardCharsets.UTF_8;

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.common.exception.RDF4JException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public synchronized void beginTransaction(TransactionSetting... transactionSettings)
			throws RDF4JException, IOException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	protected void upload(InputStream contents, String baseURI, RDFFormat dataFormat, boolean overwrite,
			boolean preserveNodeIds, Action action, Resource... contexts)
			throws IOException, RDFParseException, RepositoryException, UnauthorizedException {
		// Set Content-Length to -1 as we don't know it and we also don't want to
		// cache
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public void addData(InputStream contents, String baseURI, RDFFormat dataFormat, Resource... contexts)
			throws UnauthorizedException, RDFParseException, RepositoryException, IOException {
		upload(contents, baseURI, dataFormat, false, true, Action.ADD, contexts);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public TupleQueryResult getRepositoryList()
			throws IOException, RepositoryException, UnauthorizedException, QueryInterruptedException {
		try {
			TupleQueryResultBuilder builder = new TupleQueryResultBuilder();
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public void removeData(InputStream contents, String baseURI, RDFFormat dataFormat, Resource... contexts)
			throws UnauthorizedException, RDFParseException, RepositoryException, IOException {
		upload(contents, baseURI, dataFormat, false, true, Action.DELETE, contexts);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public void getRepositoryList(TupleQueryResultHandler handler) throws IOException, TupleQueryResultHandlerException,
			RepositoryException, UnauthorizedException, QueryInterruptedException {
		checkServerURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.common.exception.RDF4JException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	}

	public synchronized void prepareTransaction() throws RDF4JException, IOException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	protected void upload(HttpEntity reqEntity, String baseURI, boolean overwrite, boolean preserveNodeIds,
			Action action, Resource... contexts)
			throws IOException, RDFParseException, RepositoryException, UnauthorizedException {
		Objects.requireNonNull(contexts,
				"contexts argument may not be null; either the value should be cast to Resource or an empty array should be supplied");
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	public void upload(InputStream contents, String baseURI, RDFFormat dataFormat, boolean overwrite,
			boolean preserveNodeIds, Resource... contexts)
			throws IOException, RDFParseException, RepositoryException, UnauthorizedException {
		upload(contents, baseURI, dataFormat, overwrite, preserveNodeIds, Action.ADD, contexts);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	}

	public String getNamespace(String prefix) throws IOException, RepositoryException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	}

	public void removeNamespacePrefix(String prefix) throws IOException, RepositoryException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public TupleQueryResult getContextIDs()
			throws IOException, RepositoryException, UnauthorizedException, QueryInterruptedException {
		try {
			TupleQueryResultBuilder builder = new TupleQueryResultBuilder();
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public void setNamespacePrefix(String prefix, String name)
			throws IOException, RepositoryException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public void getContextIDs(TupleQueryResultHandler handler) throws IOException, TupleQueryResultHandlerException,
			RepositoryException, UnauthorizedException, QueryInterruptedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	}

	public void clearNamespaces() throws IOException, RepositoryException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	 *-------------------------*/

	public long size(Resource... contexts) throws IOException, RepositoryException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	public void upload(final Reader contents, String baseURI, final RDFFormat dataFormat, boolean overwrite,
			boolean preserveNodeIds, Resource... contexts)
			throws UnauthorizedException, RDFParseException, RepositoryException, IOException {
		upload(contents, baseURI, dataFormat, overwrite, preserveNodeIds, Action.ADD, contexts);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public TupleQueryResult getNamespaces()
			throws IOException, RepositoryException, UnauthorizedException, QueryInterruptedException {
		try {
			TupleQueryResultBuilder builder = new TupleQueryResultBuilder();
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.common.exception.RDF4JException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	}

	public synchronized void commitTransaction() throws RDF4JException, IOException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public void getNamespaces(TupleQueryResultHandler handler) throws IOException, TupleQueryResultHandlerException,
			RepositoryException, UnauthorizedException, QueryInterruptedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.common.exception.RDF4JException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	}

	public synchronized void rollbackTransaction() throws RDF4JException, IOException, UnauthorizedException {
		checkRepositoryURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	 *------------------*/

	public String getServerProtocol() throws IOException, RepositoryException, UnauthorizedException {
		checkServerURL();

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.common.exception.RDF4JException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public synchronized void beginTransaction(IsolationLevel isolationLevel)
			throws RDF4JException, IOException, UnauthorizedException {
		beginTransaction((TransactionSetting) isolationLevel);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

	public void getStatements(Resource subj, IRI pred, Value obj, boolean includeInferred, RDFHandler handler,
			Resource... contexts) throws IOException, RDFHandlerException, RepositoryException, UnauthorizedException,
			QueryInterruptedException {
		checkRepositoryURL();
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			WeakReference<?> callerRef)
			throws IOException, RDFHandlerException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {

		boolean submitted = false;
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			int maxQueryTime, TupleQueryResultHandler handler, Binding... bindings)
			throws IOException, TupleQueryResultHandlerException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {
		HttpUriRequest method = getQueryMethod(ql, query, baseURI, dataset, includeInferred, maxQueryTime, bindings);
		getTupleQueryResult(method, handler);
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	public void sendGraphQuery(QueryLanguage ql, String query, String baseURI, Dataset dataset, boolean includeInferred,
			int maxQueryTime, RDFHandler handler, Binding... bindings) throws IOException, RDFHandlerException,
			RepositoryException, MalformedQueryException, UnauthorizedException, QueryInterruptedException {
		HttpUriRequest method = getQueryMethod(ql, query, baseURI, dataset, includeInferred, maxQueryTime, bindings);
		getRDF(method, handler, false);
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	public void sendUpdate(QueryLanguage ql, String update, String baseURI, Dataset dataset, boolean includeInferred,
			Binding... bindings) throws IOException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {
		sendUpdate(ql, update, baseURI, dataset, includeInferred, 0, bindings);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	public void sendGraphQuery(QueryLanguage ql, String query, Dataset dataset, boolean includeInferred,
			RDFHandler handler, Binding... bindings) throws IOException, RDFHandlerException, RepositoryException,
			MalformedQueryException, UnauthorizedException, QueryInterruptedException {
		sendGraphQuery(ql, query, null, dataset, includeInferred, 0, handler, bindings);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	public boolean sendBooleanQuery(QueryLanguage ql, String query, String baseURI, Dataset dataset,
			boolean includeInferred, int maxQueryTime, Binding... bindings) throws IOException, RepositoryException,
			MalformedQueryException, UnauthorizedException, QueryInterruptedException {
		HttpUriRequest method = getQueryMethod(ql, query, baseURI, dataset, includeInferred, maxQueryTime, bindings);
		try {
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			WeakReference<?> callerRef,
			Binding... bindings) throws IOException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {
		return sendGraphQuery(ql, query, null, dataset, includeInferred, 0, callerRef, bindings);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			boolean includeInferred, int maxQueryTime, WeakReference<?> callerRef, Binding... bindings)
			throws IOException, RepositoryException,
			MalformedQueryException, UnauthorizedException, QueryInterruptedException {
		HttpUriRequest method = getQueryMethod(ql, query, baseURI, dataset, includeInferred, maxQueryTime, bindings);
		return getBackgroundTupleQueryResult(method, callerRef);
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			boolean includeInferred, int maxQueryTime, WeakReference<?> callerRef, Binding... bindings)
			throws IOException, RepositoryException,
			MalformedQueryException, UnauthorizedException, QueryInterruptedException {
		try {
			HttpUriRequest method = getQueryMethod(ql, query, baseURI, dataset, includeInferred, maxQueryTime,
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	protected void getTupleQueryResult(HttpUriRequest method, TupleQueryResultHandler handler)
			throws IOException, TupleQueryResultHandlerException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {
		// Specify which formats we support
		Set<QueryResultFormat> tqrFormats = TupleQueryResultParserRegistry.getInstance().getKeys();
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	protected void getRDF(HttpUriRequest method, RDFHandler handler, boolean requireContext)
			throws IOException, RDFHandlerException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {
		// Specify which formats we support using Accept headers
		Set<RDFFormat> rdfFormats = RDFParserRegistry.getInstance().getKeys();
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	public boolean sendBooleanQuery(QueryLanguage ql, String query, Dataset dataset, boolean includeInferred,
			Binding... bindings) throws IOException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {
		return sendBooleanQuery(ql, query, null, dataset, includeInferred, 0, bindings);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			WeakReference<?> callerRef,
			Binding... bindings) throws IOException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {
		return sendTupleQuery(ql, query, null, dataset, includeInferred, 0, callerRef, bindings);
	}
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	public void sendUpdate(QueryLanguage ql, String update, String baseURI, Dataset dataset, boolean includeInferred,
			int maxQueryTime, Binding... bindings) throws IOException, RepositoryException, MalformedQueryException,
			UnauthorizedException, QueryInterruptedException {
		HttpUriRequest method = getUpdateMethod(ql, update, baseURI, dataset, includeInferred, maxQueryTime, bindings);

```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.repository.RepositoryException', in the throws list already.
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java

	@Override
	public boolean isActive() throws UnknownTransactionStateException, RepositoryException {
		return getDelegate().isActive();
	}
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java

	private byte[] literal2data(String label, Optional<String> lang, IRI dt, boolean create)
			throws IOException, UnsupportedEncodingException {
		// Get datatype ID
		int datatypeID = NativeValue.UNKNOWN_ID;
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.query.QueryResultHandlerException', in the throws list already.
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java

	protected void assertQueryResultsEqual(TupleQueryResult expected, TupleQueryResult output)
			throws QueryEvaluationException, TupleQueryResultHandlerException, QueryResultHandlerException,
			UnsupportedEncodingException {
		assertTrue(QueryResults.equals(expected, output));
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.query.QueryEvaluationException', in the throws list already.
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/EvaluationStrategy.java`
#### Snippet
```java

	boolean isTrue(QueryValueEvaluationStep expr, BindingSet bindings)
			throws ValueExprEvaluationException, QueryEvaluationException;

	/**
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.query.QueryEvaluationException', in the throws list already.
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/EvaluationStrategy.java`
#### Snippet
```java
	 */
	boolean isTrue(ValueExpr expr, BindingSet bindings)
			throws ValueExprEvaluationException, QueryEvaluationException;

	boolean isTrue(QueryValueEvaluationStep expr, BindingSet bindings)
```

### DuplicateThrows
There is a more general exception, 'org.eclipse.rdf4j.query.QueryEvaluationException', in the throws list already.
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/EvaluationStrategy.java`
#### Snippet
```java
	 */
	Value evaluate(ValueExpr expr, BindingSet bindings)
			throws ValueExprEvaluationException, QueryEvaluationException;

	/**
```

## RuleId[id=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
		Iterator<Resource> added = null;
		Set<Resource> removed = null;
		synchronized (this) {
			Set<Resource> approvedContexts = changes.getApprovedContexts();
			if (approvedContexts != null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
		Iterator<Map.Entry<String, String>> added = null;
		Set<String> removed;
		synchronized (this) {
			Map<String, String> addedNamespaces = changes.getAddedNamespaces();
			if (addedNamespaces != null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLRepository.java`
#### Snippet
```java
		HttpClientSessionManager result = client;
		if (result == null) {
			synchronized (this) {
				result = client;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLRepository.java`
#### Snippet
```java
	@Override
	public void setHttpClientSessionManager(HttpClientSessionManager client) {
		synchronized (this) {
			this.client = client;
			// If they set a client, we need to check whether we need to shutdown any existing dependentClient
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLServiceResolver.java`
#### Snippet
```java
		HttpClientSessionManager result = client;
		if (result == null) {
			synchronized (this) {
				result = client;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLServiceResolver.java`
#### Snippet
```java
	@Override
	public void setHttpClientSessionManager(HttpClientSessionManager client) {
		synchronized (this) {
			this.client = client;
			// If they set a client, we need to check whether we need to
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/SingletonClientProvider.java`
#### Snippet
```java
		}

		synchronized (this) {
			if (closed) {
				throw new IllegalStateException("Elasticsearch Client Provider is closed!");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java

		try {
			synchronized (this) {
				if (addStatementBuffer.isEmpty()) {
					return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
		} finally {
			if (workingBuffer != null && !workingBuffer.isEmpty()) {
				synchronized (this) {
					addStatementBuffer.addAll(workingBuffer);
				}
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
		}

		synchronized (this) {
			if (!unmodifiable) {

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/model/LmdbBNode.java`
#### Snippet
```java
	protected void init() {
		if (!initialized) {
			synchronized (this) {
				if (!initialized) {
					revision.resolveValue(internalID, this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/model/LmdbIRI.java`
#### Snippet
```java
	protected void init() {
		if (!initialized) {
			synchronized (this) {
				if (!initialized) {
					revision.resolveValue(internalID, this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/model/LmdbLiteral.java`
#### Snippet
```java
	protected void init() {
		if (!initialized) {
			synchronized (this) {
				if (!initialized) {
					revision.resolveValue(internalID, this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
		HttpClientSessionManager result = sessionManager;
		if (result == null) {
			synchronized (this) {
				result = sessionManager;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
	@Override
	public void setHttpClientSessionManager(HttpClientSessionManager client) {
		synchronized (this) {
			this.sessionManager = client;
			// If they set a client, we need to check whether we need to
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
		Boolean result = compatibleMode;
		if (result == null) {
			synchronized (this) {
				result = compatibleMode;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
		private void acquireExclusiveTransactionLock() throws SailException {
			if (!txnLock) {
				synchronized (this) {
					if (!txnLock) {
						txnLockManager.lock();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
		HttpClient result = httpClient;
		if (result == null) {
			synchronized (this) {
				result = httpClient;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
			HttpConnection conn = clientContext.getConnection();

			synchronized (this) {
				try {
					logger.info("Cleaning up closed connection");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
			HttpConnection conn = clientContext.getConnection();
			if (conn != null) {
				synchronized (this) {
					if (conn.isStale()) {
						try {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
	@Override
	public void setHttpClient(HttpClient httpClient) {
		synchronized (this) {
			this.httpClient = Objects.requireNonNull(httpClient, "HTTP Client cannot be null");
			// If they set a client, we need to check whether we need to
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
			// clients should ping before server timeouts transaction
			long nextPingDelay = timeout / 2;
			synchronized (this) {
				if (pingDelay != nextPingDelay) {
					pingDelay = nextPingDelay;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RemoteRepositoryManager.java`
#### Snippet
```java
		SharedHttpClientSessionManager result = client;
		if (result == null) {
			synchronized (this) {
				result = client;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
		SharedHttpClientSessionManager result = client;
		if (result == null) {
			synchronized (this) {
				result = client;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
		SPARQLServiceResolver result = serviceResolver;
		if (result == null) {
			synchronized (this) {
				result = serviceResolver;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSailConnection.java`
#### Snippet
```java
		if ((statementsAdded || statementsRemoved) && isActive()) {
			if (isActive()) {
				synchronized (this) {
					if ((statementsAdded || statementsRemoved) && isActive()) {
						flush();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/ProxyRepository.java`
#### Snippet
```java
		Repository result = proxiedRepository;
		if (result == null) {
			synchronized (this) {
				result = proxiedRepository;
				if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/RangeIterator.java`
#### Snippet
```java
	public void close() throws IOException {
		if (!closed) {
			synchronized (this) {
				if (!closed) {
					closed = true;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/MemoryOverflowModel.java`
#### Snippet
```java
			return memory;
		} else {
			synchronized (this) {
				return disk;
			}
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/IDFile.java`
#### Snippet
```java
			cacheLine = convertBytesToLongs(bytes);

			synchronized (this) {
				// we try not to overwrite an existing cache line
				if (cache.getIfPresent(cacheLineLookupIndex) == null) {
```

## RuleId[id=CastConflictsWithInstanceof]
### CastConflictsWithInstanceof
Cast to 'Literal' type conflicts with preceding 'instanceof Triple' check
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
			return str_cmp;
		}
		Literal lit1 = (Literal) o1;
		Literal lit2 = (Literal) o2;
		int dt_cmp = lit1.getDatatype().stringValue().compareTo(lit2.getDatatype().stringValue());
```

### CastConflictsWithInstanceof
Cast to 'ArrayList\>' type conflicts with preceding 'instanceof List' check
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDHierarchicalProcessor.java`
#### Snippet
```java
					Object object = currentNode.get(predicate);
					if (object instanceof List<?>) {
						ArrayList<Map<String, Object>> objectsPredSubjPairs = (ArrayList<Map<String, Object>>) object;
						for (int i = 0; i < objectsPredSubjPairs.size(); i++) {
							if (objectsPredSubjPairs.get(i) instanceof Map
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
The value changed at `j++` is never used
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
					if (currentStatementMatcher.getObjectValue() == null
							&& currentStatementMatcher.getObjectName() != null) {
						bindings[j++] = new SimpleBinding(currentStatementMatcher.getObjectName(), next.getObject());
					}
					if (bindings.length == 1) {
```

### UnusedAssignment
The value changed at `i++` is never used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/StatementPattern.java`
#### Snippet
```java
		}
		if (contextVar != null) {
			vars[i++] = contextVar;
		}

```

### UnusedAssignment
The value `result.next()` assigned to `bindings` is never used
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java

				assertTrue(result.hasNext());
				bindings = result.next();
				result.close();
				connection.commit();
```

### UnusedAssignment
The value `result.next()` assigned to `bindings` is never used
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java

				assertTrue(result.hasNext());
				bindings = result.next();
				result.close();
			}
```

### UnusedAssignment
Variable `pos` initializer `0` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
					int dbi = index.getDB(explicit);

					int pos = 0;
					long cursor = 0;

```

### UnusedAssignment
The value changed at `i++` is never used
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			assertThat(collect.get(i++).getValue("aggregate"))
					.isEqualTo(literal("2022-01-01T01:01:01.000000001Z", CoreDatatype.XSD.DATETIME));
			assertThat(collect.get(i++).getValue("aggregate")).isEqualTo(literal("3"));
		}
	}
```

### UnusedAssignment
The value changed at `i++` is never used
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			assertThat(collect.get(i++).getValue("aggregate"))
					.isEqualTo(literal("2022-01-01T01:01:01.000000001Z", CoreDatatype.XSD.DATETIME));
			assertThat(collect.get(i++).getValue("aggregate")).isEqualTo(literal("3"));
		}

```

### UnusedAssignment
The value changed at `i++` is never used
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			assertThat(collect.get(i++).getValue("aggregate")).isNull();
			assertThat(collect.get(i++).getValue("aggregate")).isEqualTo(literal(15.055));
			assertThat(collect.get(i++).getValue("aggregate"))
					.isEqualTo(literal(new BigDecimal("17.89573152964782569447729443134684709566550468")));
		}
```

### UnusedAssignment
The value changed at `i++` is never used
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			assertThat(collect.get(i++).getValue("aggregate")).isNull();
			assertThat(collect.get(i++).getValue("aggregate")).isEqualTo(literal(30.11));
			assertThat(collect.get(i++).getValue("aggregate"))
					.isEqualTo(literal(new BigDecimal("55.4786576482391284723864721567342354783275234")));
		}
```

### UnusedAssignment
The value changed at `i++` is never used
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			assertThat(collect.get(i++).getValue("aggregate")).isNull();
			assertThat(collect.get(i++).getValue("aggregate")).isEqualTo(literal(30.11));
			assertThat(collect.get(i++).getValue("aggregate"))
					.isEqualTo(literal(new BigDecimal("89.4786576482391284723864721567342354783275234")));

```

### UnusedAssignment
The value changed at `i++` is never used
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java

			assertThat(collect.get(i++).getValue("aggregate")).isEqualTo(literal(15.055));
			assertThat(collect.get(i++).getValue("aggregate"))
					.isEqualTo(literal(new BigDecimal("18.492885882746376157462157")));
		}
```

### UnusedAssignment
The value changed at `idx++` is never used
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
		}
		if (subj == null) {
			index[idx++] = 's';
		}
		StatementTree tree = new StatementTree(index);
```

### UnusedAssignment
The value `readCodePoint()` assigned to `c` is never used
in `core/rio/trig/src/main/java/org/eclipse/rdf4j/rio/trig/TriGParser.java`
#### Snippet
```java
		// the two parsing methods for blank nodes to use
		if (c == '[') {
			c = readCodePoint();
			skipWSC();
			c = peekCodePoint();
```

### UnusedAssignment
The value `readCodePoint()` assigned to `c` is never used
in `core/rio/trig/src/main/java/org/eclipse/rdf4j/rio/trig/TriGParser.java`
#### Snippet
```java
			c = peekCodePoint();
			if (c == ']') {
				c = readCodePoint();
				subject = createNode();
				skipWSC();
```

### UnusedAssignment
The value `readCodePoint()` assigned to `c` is never used
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		// the two parsing methods for blank nodes to use
		if (c == '[') {
			c = readCodePoint();
			skipWSC();
			c = peekCodePoint();
```

### UnusedAssignment
The value `readCodePoint()` assigned to `c` is never used
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
			c = peekCodePoint();
			if (c == ']') {
				c = readCodePoint();
				subject = createNode();
				skipWSC();
```

### UnusedAssignment
The value `hashTableValues = null` assigned to `nextHashTableValues` is never used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
				currentScanElem = null;
				closeHashValue(nextHashTableValues);
				nextHashTableValues = hashTableValues = null;
			}
			return result;
```

### UnusedAssignment
The value `null` assigned to `leftArgResults` is never used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java

		// help free memory before allocating the hash table
		leftArgResults = null;
		rightArgResults = null;

```

### UnusedAssignment
The value `null` assigned to `rightArgResults` is never used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
		// help free memory before allocating the hash table
		leftArgResults = null;
		rightArgResults = null;

		// create the hash table for our join
```

## RuleId[id=IndexOfReplaceableByContains]
### IndexOfReplaceableByContains
`_path.indexOf("/../") == -1` can be replaced with '!_path.contains("/../")'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedURI.java`
#### Snippet
```java
		}

		if (_path.indexOf("/../") == -1 && !_path.endsWith("/..")) {
			// There are no '..' segments that can be removed. We're done and
			// don't have to execute the time-consuming code following this
```

### IndexOfReplaceableByContains
`expected.indexOf(supplied) == -1` can be replaced with '!expected.contains(supplied)'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		final String supplied = new String(Character.toChars(codePoint));

		if (expected.indexOf(supplied) == -1) {
			StringBuilder msg = new StringBuilder(32);
			msg.append("Expected ");
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
					count++;
					idArray = doc.getValues("id");
					for (String id : idArray) {
						ids.add(id);
					}
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryOptimizerList.java`
#### Snippet
```java
	public QueryOptimizerList(QueryOptimizer... optimizers) {
		this.optimizers = new ArrayList<>(optimizers.length);
		for (QueryOptimizer optimizer : optimizers) {
			this.optimizers.add(optimizer);
		}
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `spring-components/spring-boot-sparql-web/src/main/java/org/eclipse/rdf4j/http/server/readonly/sparql/QueryTypes.java`
#### Snippet
```java
	}

	;

	protected FileFormat bestFormat(String acceptHeader) {
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/UpdateWithModelBuilder.java`
#### Snippet
```java
		if (sink.size() > 0) {
			sink.stream()
					.forEach(
							s -> modelBuilder.add(s.getSubject(), s.getPredicate(), s.getObject()));
		}
```

### SimplifyStreamApiCallChains
Can be replaced with 'java.util.ArrayList' constructor
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/OperationUtils.java`
#### Snippet
```java
		if (logger.isDebugEnabled() && bindings != null) {
			logger.debug("bindings: {}", bindings);
			List<String> keys = bindings.keySet().stream().collect(Collectors.toList());
			logger.debug(
					"values block:\n\nVALUES ( {} ) { ( {} ) }\n",
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/OperationUtils.java`
#### Snippet
```java
			bindings.entrySet()
					.stream()
					.forEach(entry -> operation.setBinding(entry.getKey(), entry.getValue()));
		}
	}
```

### SimplifyStreamApiCallChains
Can be replaced with 'peek'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ReusableTupleQueryResult.java`
#### Snippet
```java
			return recordingThrowable(
					() -> originalResult.stream()
							.map(
									bindings -> {
										bindingSets.add(bindings);
```

### SimplifyStreamApiCallChains
Can be replaced with 'peek'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ReusableGraphQueryResult.java`
#### Snippet
```java
			return recordingThrowable(
					() -> originalResult.stream()
							.map(
									bindings -> {
										statements.add(bindings);
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	@Override
	public void meet(Coalesce node) throws RuntimeException {
		node.getArguments().stream().forEach(arg -> ensureNonAnonymousVar(arg));
		super.meet(node);
	}
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
		node.getProjections()
				.stream()
				.forEach(
						projList -> projList.getElements()
								.stream()
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
						projList -> projList.getElements()
								.stream()
								.forEach(
										elem -> fakeProjection.getProjectionElemList().addElement(elem)));
		fakeProjection.setArg(node.getArg().clone());
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	@Override
	public void meet(FunctionCall node) throws RuntimeException {
		node.getArgs().stream().forEach(arg -> ensureNonAnonymousVar(arg));
		super.meet(node);
	}
```

## RuleId[id=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		@Override
		public <T> T[] toArray(T[] arg0) {
			return wrapped.toArray(arg0);
		}
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java

		while (backSlashIdx != -1) {
			sb.append(s.substring(startIdx, backSlashIdx));

			if (backSlashIdx + 1 >= sLength) {
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryComplianceTest.java`
#### Snippet
```java
						String testName = bs.getValue("testName").stringValue();
						String displayName = filename.substring(0, filename.lastIndexOf('/'));
						displayName = displayName.substring(displayName.lastIndexOf('/') + 1, displayName.length())
								+ ": " + testName;

```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
			int colonIndex = text.indexOf(';', ampIndex);

			sb.append(text.substring(prevIndex, ampIndex));
			sb.append(resolveEntity(text.substring(ampIndex + 1, colonIndex)));

```

### StringOperationCanBeSimplified
`substring()` call can be replaced with 'charAt()'
in `core/rio/trig/src/main/java/org/eclipse/rdf4j/rio/trig/TriGParser.java`
#### Snippet
```java
			// SPARQL BASE and PREFIX lines do not end in .
		} else if (directive.length() >= 6 && directive.substring(0, 5).equalsIgnoreCase("GRAPH")
				&& directive.substring(5, 6).equals(":")) {
			// If there was a colon immediately after the graph keyword then
			// assume it was a pname and not the SPARQL GRAPH keyword
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java

		while (backSlashIdx != -1) {
			sb.append(s.substring(startIdx, backSlashIdx));

			if (backSlashIdx + 1 >= sLength) {
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java

	protected void parseDirective(String directive) throws IOException, RDFParseException, RDFHandlerException {
		if (directive.length() >= 7 && directive.substring(0, 7).equals("@prefix")) {
			if (directive.length() > 7) {
				unread(directive.substring(7));
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
			}
			parsePrefixID();
		} else if (directive.length() >= 5 && directive.substring(0, 5).equals("@base")) {
			if (directive.length() > 5) {
				unread(directive.substring(5));
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVMappingStrategy.java`
#### Snippet
```java

		while (backSlashIdx != -1) {
			sb.append(s.substring(startIdx, backSlashIdx));

			if (backSlashIdx + 1 >= sLength) {
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/common/text/src/main/java/org/eclipse/rdf4j/common/text/StringUtil.java`
#### Snippet
```java
			// First, add the text between the previous and the current
			// occurence.
			buf.append(text.substring(prevIndex, oldsIndex));

			// Then add the substition pattern
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
				if ((ch = str.charAt(i)) < 0x20 || ch > 0x7e) {
					String s = "0000" + Integer.toString(ch, 16);
					retval.append("\\u" + s.substring(s.length() - 4, s.length()));
				} else {
					retval.append(ch);
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/RegexAsStringFunctionOptimizer.java`
#### Snippet
```java

		private void strstartsCandidate(Regex node, String regex) {
			final String potential = regex.substring(1, regex.length());
			if (plain(potential)) {
				ValueConstant vc = new ValueConstant(vf.createLiteral(potential));
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/string/StrAfter.java`
#### Snippet
```java
				if (index > -1) {
					index += substring.length() - 1;
					substringAfter = lexicalValue.substring(index + 1, lexicalValue.length());
				} else {
					// no match, return empty string with no language or datatype
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/RegexAsStringFunctionOptimizer.java`
#### Snippet
```java

		private void strstartsCandidate(Regex node, String regex) {
			final String potential = regex.substring(1, regex.length());
			if (plain(potential)) {
				ValueConstant vc = new ValueConstant(vf.createLiteral(potential));
```

## RuleId[id=MethodOverloadsParentMethod]
### MethodOverloadsParentMethod
Method `Object()` overloads a compatible method of a superclass, when overriding might have been intended
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Object() throws ParseException {
		GraphNode();
	}
```

## RuleId[id=OptionalContainsCollection]
### OptionalContainsCollection
'Optional' contains collection `Model`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailValidationReportHelper.java`
#### Snippet
```java
	 * @return an optional with the validation report, or empty.
	 */
	public static Optional<Model> getValidationReport(Throwable t) {
		Throwable throwable;
		for (throwable = t; throwable != null; throwable = throwable.getCause()) {
```

### OptionalContainsCollection
'Optional' contains collection `Model`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailValidationReportHelper.java`
#### Snippet
```java
	 */
	public static void printValidationReport(Throwable t, OutputStream out) {
		Optional<Model> reportOpt = getValidationReport(t);
		if (reportOpt.isPresent()) {
			Rio.write(reportOpt.get(), out, RDFFormat.TURTLE, WRITER_CONFIG);
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriples.java`
#### Snippet
```java
			if (i != HDTTriples.Order.SPO.getValue()) {
				throw new UnsupportedOperationException(
						"Triples section: order " + Integer.toString(i) + ", but only SPO order is supported");
			}

```

## RuleId[id=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `shutDownInternal()` overrides synchronized method
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchStore.java`
#### Snippet
```java

	@Override
	protected void shutDownInternal() throws SailException {
		if (shutdown.compareAndSet(false, true)) {
			super.shutDownInternal();
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `initializeInternal()` overrides synchronized method
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchStore.java`
#### Snippet
```java

	@Override
	protected void initializeInternal() throws SailException {
		if (shutdown.get()) {
			throw new SailException("Can not be initialized after calling shutdown!");
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `fillInStackTrace()` overrides synchronized method
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	static private final class LookaheadSuccess extends java.lang.Error {
		@Override
		public Throwable fillInStackTrace() {
			return this;
		}
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `fillInStackTrace()` overrides synchronized method
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ValueExprEvaluationException.java`
#### Snippet
```java

	@Override
	public Throwable fillInStackTrace() {
		// Exception used for excessive flow control. Collecting the stack trace is a slow operation, so skip it.
		return this;
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Map `lastModified` may contain URL objects
in `core/repository/dataset/src/main/java/org/eclipse/rdf4j/repository/dataset/DatasetRepository.java`
#### Snippet
```java
public class DatasetRepository extends RepositoryWrapper {

	private final Map<URL, Long> lastModified = new ConcurrentHashMap<>();

	public DatasetRepository() {
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
	}

	protected RecordCacheIterator getRecords(boolean explicit) throws IOException {
		return new RecordCacheIterator(explicit ? dbiExplicit : dbiInferred);
	}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
	}

	protected void removeRecord(long[] quad, boolean explicit) throws IOException {
		update(quad, explicit, false);
	}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
	}

	protected boolean storeRecord(long[] quad, boolean explicit) throws IOException {
		return update(quad, explicit, true);
	}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
	}

	protected boolean update(long[] quad, boolean explicit, boolean add) throws IOException {
		if (LmdbUtil.requiresResize(mapSize, pageSize, writeTxn, 0)) {
			// resize map if required
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
	}

	protected void commit() throws IOException {
		if (writeTxn != 0) {
			E(mdb_txn_commit(writeTxn));
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriples.java`
#### Snippet
```java
		private final int value;

		protected int getValue() {
			return value;
		}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTDictionarySection.java`
#### Snippet
```java
		private final int value;

		protected int getValue() {
			return value;
		}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java
		 * @return value 1,2 or 3
		 */
		protected byte getValue() {
			return value;
		}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/ZeroLengthPathEvaluationStep.java`
#### Snippet
```java
	}

	protected ZeroLengthPathIteration getZeroLengthPathIterator(final BindingSet bindings, final Var subjectVar,
			final Var objVar, final Var contextVar, Value subj, Value obj, QueryEvaluationContext context) {
		return new ZeroLengthPathIteration(strategy, subjectVar, objVar, subj, obj, contextVar, bindings, context);
```

## RuleId[id=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `closed` accessed in synchronized context
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/UserProvidedClientProvider.java`
#### Snippet
```java
	@Override
	synchronized public void close() throws Exception {
		if (!closed) {
			closed = true;
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closed` accessed in synchronized context
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/UserProvidedClientProvider.java`
#### Snippet
```java
	synchronized public void close() throws Exception {
		if (!closed) {
			closed = true;
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	public FederatedService getService(String serviceUrl) throws QueryEvaluationException {
		FederatedService service;
		synchronized (endpointToService) {
			service = endpointToService.get(serviceUrl);
			if (service == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
		FederatedService service;
		synchronized (endpointToService) {
			service = endpointToService.get(serviceUrl);
			if (service == null) {
				service = createService(serviceUrl);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
			if (service == null) {
				service = createService(serviceUrl);
				endpointToService.put(serviceUrl, service);
			}
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	 */
	public void registerService(String serviceUrl, FederatedService service) {
		synchronized (endpointToService) {
			endpointToService.put(serviceUrl, service);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	public void registerService(String serviceUrl, FederatedService service) {
		synchronized (endpointToService) {
			endpointToService.put(serviceUrl, service);
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	public void unregisterService(String serviceUrl) {
		FederatedService service;
		synchronized (endpointToService) {
			service = endpointToService.remove(serviceUrl);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
		FederatedService service;
		synchronized (endpointToService) {
			service = endpointToService.remove(serviceUrl);
		}
		if (service != null && service.isInitialized()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java

	public void unregisterAll() {
		synchronized (endpointToService) {
			for (FederatedService service : endpointToService.values()) {
				try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	public void unregisterAll() {
		synchronized (endpointToService) {
			for (FederatedService service : endpointToService.values()) {
				try {
					service.shutdown();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
				}
			}
			endpointToService.clear();
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	 */
	public boolean hasService(String serviceUrl) {
		synchronized (endpointToService) {
			return endpointToService.containsKey(serviceUrl);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointToService` accessed in synchronized context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	public boolean hasService(String serviceUrl) {
		synchronized (endpointToService) {
			return endpointToService.containsKey(serviceUrl);
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java

	private synchronized SailDataset dataset() throws SailException {
		if (sink != null) {
			try {
				sink.flush();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
		if (sink != null) {
			try {
				sink.flush();
			} finally {
				sink.close();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
				sink.flush();
			} finally {
				sink.close();
				sink = null;
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			} finally {
				sink.close();
				sink = null;
			}
			if (dataset != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
				sink = null;
			}
			if (dataset != null) {
				dataset.close();
				dataset = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			}
			if (dataset != null) {
				dataset.close();
				dataset = null;
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			if (dataset != null) {
				dataset.close();
				dataset = null;
			}
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			}
		}
		if (dataset == null) {
			dataset = source.dataset(level);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
		}
		if (dataset == null) {
			dataset = source.dataset(level);
		}
		return dataset;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			dataset = source.dataset(level);
		}
		return dataset;
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java

	private synchronized SailSink sink() throws SailException {
		if (sink == null) {
			sink = source.sink(level);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
	private synchronized SailSink sink() throws SailException {
		if (sink == null) {
			sink = source.sink(level);
		}
		return sink;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
			sink = source.sink(level);
		}
		return sink;
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java

	private synchronized void checkMemoryOverflow() {
		if (disk == null) {
			int size = size();
			if (size >= LARGE_BLOCK && size % LARGE_BLOCK == 0) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	public synchronized void closeIterator(Iterator<?> iter) {
		super.closeIterator(iter);
		if (disk != null) {
			disk.closeIterator(iter);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
		super.closeIterator(iter);
		if (disk != null) {
			disk.closeIterator(iter);
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	private synchronized void overflowToDisk() {
		try {
			assert disk == null;
			dataDir = Files.createTempDirectory("model").toFile();
			logger.debug("memory overflow using temp directory {}", dataDir);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataDir` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
		try {
			assert disk == null;
			dataDir = Files.createTempDirectory("model").toFile();
			logger.debug("memory overflow using temp directory {}", dataDir);
			store = createSailStore(dataDir);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataDir` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
			assert disk == null;
			dataDir = Files.createTempDirectory("model").toFile();
			logger.debug("memory overflow using temp directory {}", dataDir);
			store = createSailStore(dataDir);
			disk = new SailSourceModel(store) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `store` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
			dataDir = Files.createTempDirectory("model").toFile();
			logger.debug("memory overflow using temp directory {}", dataDir);
			store = createSailStore(dataDir);
			disk = new SailSourceModel(store) {

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataDir` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
			dataDir = Files.createTempDirectory("model").toFile();
			logger.debug("memory overflow using temp directory {}", dataDir);
			store = createSailStore(dataDir);
			disk = new SailSourceModel(store) {

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
			logger.debug("memory overflow using temp directory {}", dataDir);
			store = createSailStore(dataDir);
			disk = new SailSourceModel(store) {

				@Override
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
				}
			};
			disk.addAll(memory);
			memory = new LinkedHashModel(memory.getNamespaces(), LARGE_BLOCK);
			logger.debug("overflow synced to disk");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataDir` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
			logger.debug("overflow synced to disk");
		} catch (IOException | SailException e) {
			String path = dataDir != null ? dataDir.getAbsolutePath() : "(unknown)";
			logger.error("Error while writing to overflow directory " + path, e);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataDir` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
			logger.debug("overflow synced to disk");
		} catch (IOException | SailException e) {
			String path = dataDir != null ? dataDir.getAbsolutePath() : "(unknown)";
			logger.error("Error while writing to overflow directory " + path, e);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	public synchronized void removeTermIteration(Iterator<Statement> iter, Resource subj, IRI pred, Value obj,
			Resource... contexts) {
		if (disk == null) {
			memory.removeTermIteration(iter, subj, pred, obj, contexts);
		} else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
			memory.removeTermIteration(iter, subj, pred, obj, contexts);
		} else {
			disk.removeTermIteration(iter, subj, pred, obj, contexts);
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java

	synchronized Model getDelegate() {
		if (disk == null) {
			return memory;
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `disk` accessed in synchronized context
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
			return memory;
		}
		return disk;
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `maxDocs` accessed in synchronized context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
	public synchronized TopDocs search(Query query) throws IOException {
		int nDocs;
		if (maxDocs > 0) {
			nDocs = maxDocs;
		} else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `maxDocs` accessed in synchronized context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		int nDocs;
		if (maxDocs > 0) {
			nDocs = maxDocs;
		} else {
			nDocs = Math.max(getIndexReader().numDocs(), 1);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `currentMonitor` accessed in synchronized context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
			throw new SailException("Index has been closed");
		}
		if (currentMonitor == null) {
			currentMonitor = new ReaderMonitor(this, directory);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `currentMonitor` accessed in synchronized context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		}
		if (currentMonitor == null) {
			currentMonitor = new ReaderMonitor(this, directory);
		}
		return currentMonitor;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `currentMonitor` accessed in synchronized context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
			currentMonitor = new ReaderMonitor(this, directory);
		}
		return currentMonitor;
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `currentMonitor` accessed in synchronized context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		synchronized (oldmonitors) {
			// Move current monitor to old monitors and set null
			if (currentMonitor != null) {
				// we do NOT close it directly as it may be used by an open
				// result
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `currentMonitor` accessed in synchronized context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
				// list of oldmonitors where it is handled as other older
				// monitors
				oldmonitors.add(currentMonitor);
			}
			currentMonitor = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `currentMonitor` accessed in synchronized context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
				oldmonitors.add(currentMonitor);
			}
			currentMonitor = null;

			// close all monitors if possible
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java
		}

		if (rdfHandler != null) {
			rdfHandler.startRDF();
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java

		if (rdfHandler != null) {
			rdfHandler.startRDF();
		}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `valueFactory` accessed in synchronized context
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java
			byte[] p = predicates.get(t[1]);
			byte[] o = getSO(t[2], size, shared, objects);
			Statement stmt = valueFactory.createStatement(createSubject(s), createPredicate(p), createObject(o));

			if (rdfHandler != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java
			Statement stmt = valueFactory.createStatement(createSubject(s), createPredicate(p), createObject(o));

			if (rdfHandler != null) {
				rdfHandler.handleStatement(stmt);
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java

			if (rdfHandler != null) {
				rdfHandler.handleStatement(stmt);
			}
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java
		}

		if (rdfHandler != null) {
			rdfHandler.endRDF();
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java

		if (rdfHandler != null) {
			rdfHandler.endRDF();
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.handler` accessed in synchronized context
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/BooleanTextParser.java`
#### Snippet
```java
		}

		if (this.handler != null) {
			try {
				this.handler.handleBoolean(result);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.handler` accessed in synchronized context
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/BooleanTextParser.java`
#### Snippet
```java
		if (this.handler != null) {
			try {
				this.handler.handleBoolean(result);
			} catch (QueryResultHandlerException e) {
				if (e.getCause() != null && e.getCause() instanceof IOException) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
			}

			if (rdfHandler != null) {
				rdfHandler.startRDF();
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java

			if (rdfHandler != null) {
				rdfHandler.startRDF();
			}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		}

		if (rdfHandler != null) {
			rdfHandler.endRDF();
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java

		if (rdfHandler != null) {
			rdfHandler.endRDF();
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bufferedStatements` accessed in synchronized context
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
		if (isBuffering()) {
			synchronized (bufferLock) {
				bufferedStatements.add(st);
				if (bufferedStatements.size() >= this.bufferSize) {
					processBuffer();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bufferedStatements` accessed in synchronized context
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
			synchronized (bufferLock) {
				bufferedStatements.add(st);
				if (bufferedStatements.size() >= this.bufferSize) {
					processBuffer();
				}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java
			}

			if (rdfHandler != null) {
				rdfHandler.startRDF();
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java

			if (rdfHandler != null) {
				rdfHandler.startRDF();
			}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.reader` accessed in synchronized context
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java

			if (reader instanceof BufferedReader) {
				this.reader = (BufferedReader) reader;
			} else {
				this.reader = new BufferedReader(reader);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.reader` accessed in synchronized context
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java
				this.reader = (BufferedReader) reader;
			} else {
				this.reader = new BufferedReader(reader);
			}
			lineNo = 0;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lineNo` accessed in synchronized context
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java
				this.reader = new BufferedReader(reader);
			}
			lineNo = 0;

			reportLocation(lineNo, 1);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lineNo` accessed in synchronized context
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java
			lineNo = 0;

			reportLocation(lineNo, 1);

			while (readLine()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java
		}

		if (rdfHandler != null) {
			rdfHandler.endRDF();
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rdfHandler` accessed in synchronized context
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java

		if (rdfHandler != null) {
			rdfHandler.endRDF();
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sailStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	@Override
	synchronized protected void initializeInternal() throws SailException {
		if (sailStore != null) {
			sailStore.close();
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sailStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	synchronized protected void initializeInternal() throws SailException {
		if (sailStore != null) {
			sailStore.close();
		}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.dataStructure` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
		}

		DataStructureInterface dataStructure = Objects.requireNonNull(this.dataStructure);

		if (cacheEnabled) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sailStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
		}

		sailStore = new ExtensibleSailStore(dataStructure,
				Objects.requireNonNull(namespaceStore), getEvaluationStatisticsType(), getExtensibleStatementHelper());

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `namespaceStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java

		sailStore = new ExtensibleSailStore(dataStructure,
				Objects.requireNonNull(namespaceStore), getEvaluationStatisticsType(), getExtensibleStatementHelper());

		sailStore.init();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sailStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
				Objects.requireNonNull(namespaceStore), getEvaluationStatisticsType(), getExtensibleStatementHelper());

		sailStore.init();
		namespaceStore.init();
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `namespaceStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java

		sailStore.init();
		namespaceStore.init();
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sailStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	@Override
	synchronized protected void shutDownInternal() throws SailException {
		sailStore.close();
		sailStore = null;
		dataStructure = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sailStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	synchronized protected void shutDownInternal() throws SailException {
		sailStore.close();
		sailStore = null;
		dataStructure = null;
		namespaceStore = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataStructure` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
		sailStore.close();
		sailStore = null;
		dataStructure = null;
		namespaceStore = null;
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `namespaceStore` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
		sailStore = null;
		dataStructure = null;
		namespaceStore = null;
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `cache` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java

	synchronized public void clearCache() {
		cache.invalidateAll();
		// overflow is not a problem since we use == to compare in submitToCache
		cacheTicket++;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `cache` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
			List<ExtensibleStatement> statements) {
		if (localCacheTicket == cacheTicket && statements != null) {
			cache.put(partialStatement, statements);
		}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `cache` accessed in synchronized context
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
	synchronized private CloseableIteration<? extends ExtensibleStatement, SailException> getCached(
			PartialStatement partialStatement) {
		List<ExtensibleStatement> statements = cache.getIfPresent(partialStatement);

		if (statements != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	 */
	public Collection<Repository> getInitializedRepositories() {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return new ArrayList<>(initializedRepositories.values());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return new ArrayList<>(initializedRepositories.values());
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		// FIXME: uninitialized, removed repositories won't be cleaned up.
		try {
			synchronized (initializedRepositories) {
				Iterator<Map.Entry<String, Repository>> iter = initializedRepositories.entrySet().iterator();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		try {
			synchronized (initializedRepositories) {
				Iterator<Map.Entry<String, Repository>> iter = initializedRepositories.entrySet().iterator();

				while (iter.hasNext()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

	protected void updateInitializedRepositories() {
		synchronized (initializedRepositories) {
			Iterator<Repository> iter = initializedRepositories.values().iterator();
			while (iter.hasNext()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	protected void updateInitializedRepositories() {
		synchronized (initializedRepositories) {
			Iterator<Repository> iter = initializedRepositories.values().iterator();
			while (iter.hasNext()) {
				Repository next = iter.next();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		boolean isRemoved = hasRepositoryConfig(repositoryID);

		synchronized (initializedRepositories) {
			if (isRemoved) {
				logger.debug("Shutdown repository {} after removal of configuration.", repositoryID);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
			if (isRemoved) {
				logger.debug("Shutdown repository {} after removal of configuration.", repositoryID);
				Repository repository = initializedRepositories.remove(repositoryID);

				if (repository != null && repository.isInitialized()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	@Override
	public Repository getRepository(String identity) throws RepositoryConfigException, RepositoryException {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			Repository result = initializedRepositories.get(identity);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			Repository result = initializedRepositories.get(identity);

			if (result != null && !result.isInitialized()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
				// repository exists but has been shut down. throw away the old
				// object so we can re-read from the config.
				initializedRepositories.remove(identity);
				result = null;
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

				if (result != null) {
					initializedRepositories.put(identity, result);
				}
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

	Repository getInitializedRepository(String repositoryID) {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return initializedRepositories.get(repositoryID);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return initializedRepositories.get(repositoryID);
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

	Repository removeInitializedRepository(String repositoryID) {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return initializedRepositories.remove(repositoryID);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return initializedRepositories.remove(repositoryID);
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	 */
	public void shutDown() {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			for (Repository repository : initializedRepositories.values()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			for (Repository repository : initializedRepositories.values()) {
				try {
					if (repository.isInitialized()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
			}

			initializedRepositories.clear();
			initialized = false;
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	 */
	public Set<String> getInitializedRepositoryIDs() {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return new HashSet<>(initializedRepositories.keySet());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initializedRepositories` accessed in synchronized context
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return new HashSet<>(initializedRepositories.keySet());
		}
	}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionTimeOut` accessed in synchronized context
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSail.java`
#### Snippet
```java
					logger.debug("Waiting for active connections to close before shutting down...");
					try {
						activeConnections.wait(connectionTimeOut);
					} catch (InterruptedException e) {
						// ignore and continue
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java

	private synchronized SailDataset dataset() throws SailException {
		if (sink != null) {
			try {
				sink.flush();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
		if (sink != null) {
			try {
				sink.flush();
			} finally {
				sink.close();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
				sink.flush();
			} finally {
				sink.close();
				sink = null;
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			} finally {
				sink.close();
				sink = null;
			}
			if (dataset != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
				sink = null;
			}
			if (dataset != null) {
				dataset.close();
				dataset = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			}
			if (dataset != null) {
				dataset.close();
				dataset = null;
			}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			if (dataset != null) {
				dataset.close();
				dataset = null;
			}
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			}
		}
		if (dataset == null) {
			dataset = source.dataset(level);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
		}
		if (dataset == null) {
			dataset = source.dataset(level);
		}
		return dataset;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dataset` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			dataset = source.dataset(level);
		}
		return dataset;
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java

	private synchronized SailSink sink() throws SailException {
		if (sink == null) {
			sink = source.sink(level);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
	private synchronized SailSink sink() throws SailException {
		if (sink == null) {
			sink = source.sink(level);
		}
		return sink;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sink` accessed in synchronized context
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
			sink = source.sink(level);
		}
		return sink;
	}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `handler` accessed in synchronized context
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java
		columnHeaders = Collections.unmodifiableList(columnHeaders);

		if (handler != null) {
			handler.startQueryResult(columnHeaders);
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `handler` accessed in synchronized context
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java

		if (handler != null) {
			handler.startQueryResult(columnHeaders);
		}

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `handler` accessed in synchronized context
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java
				processNamespace();
			} else if (recordTypeMarker == EMPTY_ROW_RECORD_MARKER) {
				if (handler != null) {
					handler.handleSolution(EmptyBindingSet.getInstance());
				}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `handler` accessed in synchronized context
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java
			} else if (recordTypeMarker == EMPTY_ROW_RECORD_MARKER) {
				if (handler != null) {
					handler.handleSolution(EmptyBindingSet.getInstance());
				}
			} else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `handler` accessed in synchronized context
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java
					currentTuple = new ArrayList<>(columnCount);

					if (handler != null) {
						handler.handleSolution(new ListBindingSet(columnHeaders, previousTuple));
					}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `handler` accessed in synchronized context
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java

					if (handler != null) {
						handler.handleSolution(new ListBindingSet(columnHeaders, previousTuple));
					}
				}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `handler` accessed in synchronized context
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java
		}

		if (handler != null) {
			handler.endQueryResult();
		}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `handler` accessed in synchronized context
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java

		if (handler != null) {
			handler.endQueryResult();
		}
	}
```

## RuleId[id=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/HttpClientSessionManager.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
public interface HttpClientSessionManager {

	/**
```

### RedundantSuppression
Redundant suppression
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SortedRecordCache.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
final class SortedRecordCache extends AbstractRecordCache {

	/*------------*
```

### RedundantSuppression
Redundant suppression
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
class TripleStore implements Closeable {

	/*-----------*
```

### RedundantSuppression
Redundant suppression
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java
	@Experimental
	@SuppressWarnings("NullableProblems")
	protected static class RevivableExecutorService implements ExecutorService {
		private final Supplier<ExecutorService> supplier;
		ExecutorService delegate;
```

### RedundantSuppression
Redundant suppression
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/SparqlAggregate.java`
#### Snippet
```java
 */
@SuppressWarnings("javadoc") // acceptable, as this won't be public for long
public enum SparqlAggregate implements SparqlOperator {
	AVG("AVG"),
	COUNT("COUNT"),
```

### RedundantSuppression
Redundant suppression
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/SparqlFunction.java`
#### Snippet
```java
 */
@SuppressWarnings("javadoc") // acceptable, as this won't be public for long
public enum SparqlFunction implements SparqlOperator {
	ABS("ABS"),
	BNODE("BNODE"),
```

### RedundantSuppression
Redundant suppression
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/TargetedGraphManagementQuery.java`
#### Snippet
```java

@SuppressWarnings("javadoc")
public abstract class TargetedGraphManagementQuery<T extends TargetedGraphManagementQuery<T>>
		extends GraphManagementQuery<TargetedGraphManagementQuery<T>> {
	private static final String GRAPH = "GRAPH";
```

### RedundantSuppression
Redundant suppression
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/TargetedGraphManagementQuery.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	public T graph(IRI graph) {
		return graph(iri(graph));
	}
```

### RedundantSuppression
Redundant suppression
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/TriplePattern.java`
#### Snippet
```java
public interface TriplePattern extends GraphPattern {
	@SuppressWarnings("javadoc")
	String SUFFIX = " .";

	/**
```

## RuleId[id=UnnecessaryInitCause]
### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/DocumentUtil.java`
#### Snippet
```java
	private static IOException toIOE(String message, Exception e) {
		IOException result = new IOException(message);
		result.initCause(e);
		return result;
	}
```

### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
	protected synchronized void assertNotFailed() {
		if (failed != null) {
			throw (AssertionError) new AssertionError(failedMessage).initCause(failed);
		}
	}
```

### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
	protected synchronized void assertNotFailed() {
		if (failed != null) {
			throw (AssertionError) new AssertionError(failedMessage).initCause(failed);
		}
	}
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'path'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java
			ValidationSettings validationSettings, PlanNodeProvider overrideTargetNode, Scope scope, boolean negatePlan,
			StatementMatcher.StableRandomVariableProvider stableRandomVariableProvider, EffectiveTarget effectiveTarget,
			Optional<Path> path) {
		PlanNode planNode;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'value'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/ValidationResult.java`
#### Snippet
```java

	private Resource id;
	private final Optional<Value> value;
	private final Shape shape;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'lang'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
	}

	private byte[] literal2data(String label, Optional<String> lang, IRI dt, boolean create)
			throws IOException {
		// Get datatype ID
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'required'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/OperationUtils.java`
#### Snippet
```java
	 * Returns the object contained in the specified {@link Optional}, throwing an Exception if it is empty.
	 */
	public static <T> T require(Optional<T> required) {
		if (required.isEmpty()) {
			throw new IncorrectResultSetSizeException(
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'optional'
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/CoreDatatype.java`
#### Snippet
```java

		// Creating optionals are expensive so we precompute one
		private final Optional<GEO> optional;

		GEO(IRI iri) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'optional'
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/CoreDatatype.java`
#### Snippet
```java

		// Creating optionals are expensive so we precompute one
		private final Optional<XSD> optional;

		XSD(IRI iri, boolean primitive, boolean duration, boolean integer, boolean derived, boolean decimal,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'optional'
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/CoreDatatype.java`
#### Snippet
```java

		// Creating optionals are expensive so we precompute one
		private final Optional<RDF> optional;

		RDF(IRI iri) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'x'
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractLiteral.java`
#### Snippet
```java
	}

	private boolean equals(Optional<String> x, Optional<String> y) {

		final boolean px = x.isPresent();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'y'
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractLiteral.java`
#### Snippet
```java
	}

	private boolean equals(Optional<String> x, Optional<String> y) {

		final boolean px = x.isPresent();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'optionalLanguageCache'
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	// Cache Optional instance for the language, or null if not yet computed. Marked as transient because Optional is
	// not serializable.
	transient private Optional<String> optionalLanguageCache = null;

	/**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'v'
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
	}

	public static String getLabel(Optional<Value> v, String fallback) {
		return v != null ? getLabel(v.orElseGet(null), fallback) : fallback;
	}
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'languageTag'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/rdf/RdfLiteral.java`
#### Snippet
```java

		private Optional<Iri> dataType = Optional.empty();
		private Optional<String> languageTag = Optional.empty();

		StringLiteral(String stringValue) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'dataType'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/rdf/RdfLiteral.java`
#### Snippet
```java
		private static final String LANG_TAG_SPECIFIER = "@";

		private Optional<Iri> dataType = Optional.empty();
		private Optional<String> languageTag = Optional.empty();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'graph'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/TargetedGraphManagementQuery.java`
#### Snippet
```java

	private String target = DEFAULT;
	private Optional<Iri> graph = Optional.empty();

	/**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'graphName'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/UpdateDataQuery.java`
#### Snippet
```java
abstract class UpdateDataQuery<T extends UpdateDataQuery<T>> extends UpdateQuery<T> {
	protected TriplesTemplate triplesTemplate = SparqlBuilder.triplesTemplate();
	protected Optional<GraphName> graphName = Optional.empty();

	protected T addTriples(TriplePattern... triples) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'to'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/LoadQuery.java`
#### Snippet
```java

	private Iri from;
	private Optional<Iri> to = Optional.empty();

	LoadQuery() {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'to'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/DestinationSourceManagementQuery.java`
#### Snippet
```java

	private Optional<Iri> from = Optional.empty();
	private Optional<Iri> to = Optional.empty();
	private boolean fromDefault = false, toDefault = false;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'from'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/DestinationSourceManagementQuery.java`
#### Snippet
```java
	private static final String TO = "TO";

	private Optional<Iri> from = Optional.empty();
	private Optional<Iri> to = Optional.empty();
	private boolean fromDefault = false, toDefault = false;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'graphName'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/UpdateQuery.java`
#### Snippet
```java
	}

	protected void appendNamedTriplesTemplates(StringBuilder queryString, Optional<GraphName> graphName,
			TriplesTemplate triples) {
		queryString.append(graphName
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'prefixes'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/UpdateQuery.java`
#### Snippet
```java
abstract class UpdateQuery<T extends UpdateQuery<T>> implements QueryElement {
	private Optional<Base> base = Optional.empty();
	private Optional<PrefixDeclarations> prefixes = Optional.empty();

	UpdateQuery() {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'base'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/UpdateQuery.java`
#### Snippet
```java
@SuppressWarnings("unchecked")
abstract class UpdateQuery<T extends UpdateQuery<T>> implements QueryElement {
	private Optional<Base> base = Optional.empty();
	private Optional<PrefixDeclarations> prefixes = Optional.empty();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'base'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/OuterQuery.java`
#### Snippet
```java
@SuppressWarnings("unchecked")
public abstract class OuterQuery<T extends OuterQuery<T>> extends Query<T> {
	protected Optional<Base> base = Optional.empty();
	protected Optional<PrefixDeclarations> prefixes = Optional.empty();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'prefixes'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/OuterQuery.java`
#### Snippet
```java
public abstract class OuterQuery<T extends OuterQuery<T>> extends Query<T> {
	protected Optional<Base> base = Optional.empty();
	protected Optional<PrefixDeclarations> prefixes = Optional.empty();

	/**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'deleteTriples'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/ModifyQuery.java`
#### Snippet
```java
	private boolean usingNamed = false;

	private Optional<TriplesTemplate> deleteTriples = Optional.empty();
	private Optional<TriplesTemplate> insertTriples = Optional.empty();
	private Optional<GraphName> deleteGraph = Optional.empty();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'using'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/ModifyQuery.java`
#### Snippet
```java

	private Optional<Iri> with = Optional.empty();
	private Optional<Iri> using = Optional.empty();
	private boolean usingNamed = false;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'deleteGraph'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/ModifyQuery.java`
#### Snippet
```java
	private Optional<TriplesTemplate> deleteTriples = Optional.empty();
	private Optional<TriplesTemplate> insertTriples = Optional.empty();
	private Optional<GraphName> deleteGraph = Optional.empty();
	private Optional<GraphName> insertGraph = Optional.empty();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'with'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/ModifyQuery.java`
#### Snippet
```java
	private static final String NAMED = "NAMED";

	private Optional<Iri> with = Optional.empty();
	private Optional<Iri> using = Optional.empty();
	private boolean usingNamed = false;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'insertTriples'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/ModifyQuery.java`
#### Snippet
```java

	private Optional<TriplesTemplate> deleteTriples = Optional.empty();
	private Optional<TriplesTemplate> insertTriples = Optional.empty();
	private Optional<GraphName> deleteGraph = Optional.empty();
	private Optional<GraphName> insertGraph = Optional.empty();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'insertGraph'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/ModifyQuery.java`
#### Snippet
```java
	private Optional<TriplesTemplate> insertTriples = Optional.empty();
	private Optional<GraphName> deleteGraph = Optional.empty();
	private Optional<GraphName> insertGraph = Optional.empty();

	private final QueryPattern where = SparqlBuilder.where();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'orderBy'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/Query.java`
#### Snippet
```java
	protected QueryPattern where = SparqlBuilder.where();
	protected Optional<GroupBy> groupBy = Optional.empty();
	protected Optional<OrderBy> orderBy = Optional.empty();
	protected Optional<Having> having = Optional.empty();
	protected int limit = -1, offset = -1, varCount = -1, bnodeCount = -1;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'groupBy'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/Query.java`
#### Snippet
```java
	protected Optional<Dataset> from = Optional.empty();
	protected QueryPattern where = SparqlBuilder.where();
	protected Optional<GroupBy> groupBy = Optional.empty();
	protected Optional<OrderBy> orderBy = Optional.empty();
	protected Optional<Having> having = Optional.empty();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'from'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/Query.java`
#### Snippet
```java
	protected static final String OFFSET = "OFFSET";

	protected Optional<Dataset> from = Optional.empty();
	protected QueryPattern where = SparqlBuilder.where();
	protected Optional<GroupBy> groupBy = Optional.empty();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'having'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/Query.java`
#### Snippet
```java
	protected Optional<GroupBy> groupBy = Optional.empty();
	protected Optional<OrderBy> orderBy = Optional.empty();
	protected Optional<Having> having = Optional.empty();
	protected int limit = -1, offset = -1, varCount = -1, bnodeCount = -1;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'operatorName'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/StandardQueryElementCollection.java`
#### Snippet
```java
 */
public abstract class StandardQueryElementCollection<T extends QueryElement> extends QueryElementCollection<T> {
	private Optional<String> operatorName = Optional.empty();
	private Function<String, String> wrapperMethod = Function.identity();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'stringOptional'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/util/SparqlBuilderUtils.java`
#### Snippet
```java
	}

	public static void appendStringIfPresent(Optional<String> stringOptional, StringBuilder builder, String prefix,
			String suffix) {
		Optional<String> preOpt = Optional.ofNullable(prefix);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'elementOptional'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/util/SparqlBuilderUtils.java`
#### Snippet
```java
	}

	public static void appendAndNewlineIfPresent(Optional<? extends QueryElement> elementOptional,
			StringBuilder builder) {
		appendQueryElementIfPresent(elementOptional, builder, null, "\n");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'optional'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/util/SparqlBuilderUtils.java`
#### Snippet
```java
	private static final String PAD = " ";

	public static <O> Optional<O> getOrCreateAndModifyOptional(Optional<O> optional, Supplier<O> getter,
			UnaryOperator<O> operator) {
		return Optional.of(operator.apply(optional.orElseGet(getter)));
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'queryElementOptional'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/util/SparqlBuilderUtils.java`
#### Snippet
```java
	}

	public static void appendQueryElementIfPresent(Optional<? extends QueryElement> queryElementOptional,
			StringBuilder builder, String prefix, String suffix) {
		appendStringIfPresent(queryElementOptional.map(QueryElement::getQueryString), builder, prefix, suffix);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'from'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/GroupGraphPattern.java`
#### Snippet
```java
	private static final String GRAPH = "GRAPH ";

	private Optional<GraphName> from = Optional.empty();
	private List<Filter> filters = new ArrayList<>();
	protected boolean isOptional = false;
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for field 'constraint'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/Filter.java`
#### Snippet
```java
class Filter implements QueryElement {
	private static final String FILTER = "FILTER";
	private Optional<Expression<?>> constraint = Optional.empty();

	Filter() {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'lang'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java
	}

	private byte[] literal2data(String label, Optional<String> lang, IRI dt, boolean create)
			throws IOException, UnsupportedEncodingException {
		// Get datatype ID
```

## RuleId[id=SystemOutErr]
### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailIndexedPropertiesTest.java`
#### Snippet
```java
			assertTrue(connection.hasStatement(SUBJECT_3, FOAFPLAN,
					vf.createLiteral("groucho did not smoke cigars nor cigarillos"), false));
			System.err.println("--- after");
		}
	}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailGeoSPARQLTest.java`
#### Snippet
```java

			for (BindingSet bindings : result) {
				System.out.println(bindings);
				IRI subj = (IRI) bindings.getValue("toUri");
				// check ordering
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		endLatch.await();
		for (Throwable e : exceptions) {
			e.printStackTrace(System.err);
		}
		assertEquals(0, exceptions.size(), "Exceptions occurred during testMultithreadedAdd, see stacktraces above");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/NamespaceStore.java`
#### Snippet
```java

		for (Namespace ns : nsStore) {
			System.out.println(ns.getPrefix() + " = " + ns.getName());
		}
	}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/n3/N3ParserTestCase.java`
#### Snippet
```java
			// Check equality of the two models
			if (!Models.isomorphic(inputCollection, outputCollection)) {
				System.err.println("===models not equal===");
				// System.err.println("Expected: " + outputCollection);
				// System.err.println("Actual : " + inputCollection);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/n3/N3ParserTestCase.java`
#### Snippet
```java

				if (!missing.isEmpty()) {
					System.err.println("Missing   : " + missing);
				}
				if (!unexpected.isEmpty()) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/n3/N3ParserTestCase.java`
#### Snippet
```java
				}
				if (!unexpected.isEmpty()) {
					System.err.println("Unexpected: " + unexpected);
				}

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
				// Check equality of the two models
				if (!Models.isomorphic(inputCollection, outputCollection)) {
					System.err.println("===models not equal===");
					System.err.println("Expected: " + outputCollection);
					System.err.println("Actual  : " + inputCollection);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
				if (!Models.isomorphic(inputCollection, outputCollection)) {
					System.err.println("===models not equal===");
					System.err.println("Expected: " + outputCollection);
					System.err.println("Actual  : " + inputCollection);
					System.err.println("======================");
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
					System.err.println("===models not equal===");
					System.err.println("Expected: " + outputCollection);
					System.err.println("Actual  : " + inputCollection);
					System.err.println("======================");

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
					System.err.println("Expected: " + outputCollection);
					System.err.println("Actual  : " + inputCollection);
					System.err.println("======================");

					fail("models not equal");
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL10ManifestTest.java`
#### Snippet
```java
							FileUtil.deleteDir(tmpDir);
						} catch (IOException e) {
							System.err.println(
									"Unable to clean up temporary directory '" + tmpDir + "': " + e.getMessage());
						}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxTest.java`
#### Snippet
```java
							FileUtil.deleteDir(tmpDir);
						} catch (IOException e) {
							System.err.println(
									"Unable to clean up temporary directory '" + tmpDir + "': " + e.getMessage());
						}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11ManifestTest.java`
#### Snippet
```java
							FileUtil.deleteDir(tmpDir);
						} catch (IOException e) {
							System.err.println(
									"Unable to clean up temporary directory '" + tmpDir + "': " + e.getMessage());
						}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11ManifestTest.java`
#### Snippet
```java
							FileUtil.deleteDir(tmpDir);
						} catch (IOException e) {
							System.err.println(
									"Unable to clean up temporary directory '" + tmpDir + "': " + e.getMessage());
						}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java

			if (name.contains("pp34")) {
				System.out.println(name);
			}

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
				message.append(" =======================\n");

				System.out.print(message.toString());
			} else if (missingBindings.isEmpty() && unexpectedBindings.isEmpty()) {
				message.append("unexpected duplicate in result.\n");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
				message.append(" =======================\n");

				System.out.print(message.toString());
			}

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/Rio.java`
#### Snippet
```java
			throws IOException, RDFParseException, RDFHandlerException, UnsupportedRDFormatException {
		if (args.length < 2) {
			System.out.println("Usage: java org.eclipse.rdf4j.rio.Rio <inputFile> <outputFile>");
			System.exit(1);
			return;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		while (result.hasNext()) {
			System.out.println(result.next().toString());
		}

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		while (result.hasNext()) {
			System.out.println(result.next());
		}
		assertFalse(con.hasStatement(alice, age, null, true));
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/shacl/src/main/java/org/eclipse/rdf4j/testsuite/shacl/manifest/SHACLManifestTestSuiteFactory.java`
#### Snippet
```java
							FileUtil.deleteDir(tmpDir);
						} catch (IOException e) {
							System.err.println(
									"Unable to clean up temporary directory '" + tmpDir + "': " + e.getMessage());
						}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
		byte[] bytes = baos.toByteArray();
		for (byte b : bytes) {
			System.out.print(" ");
			System.out.print(Integer.toBinaryString(0xff & b));
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
		for (byte b : bytes) {
			System.out.print(" ");
			System.out.print(Integer.toBinaryString(0xff & b));
		}

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
		}

		System.out.println();

		ByteArrayInputStream bis = new ByteArrayInputStream(
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
		ByteArrayInputStream bis = new ByteArrayInputStream(
				new byte[] { (byte) 0b10100000, (byte) 0b10001101, (byte) 0b00000110 });
		System.out.println("read: " + readVarInt(bis));
	}

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example07WriteTurtle.java`
#### Snippet
```java
		// Instead of simply printing the statements to the screen, we use a Rio writer to
		// write the model in Turtle syntax:
		Rio.write(model, System.out, RDFFormat.TURTLE);

	}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example03LiteralDatatypes.java`
#### Snippet
```java
			if (value instanceof Literal) {
				Literal literal = (Literal) value;
				System.out.println("datatype: " + literal.getDatatype());

				// get the value of the literal directly as a Java primitive.
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example03LiteralDatatypes.java`
#### Snippet
```java
				if (property.getLocalName().equals("peopleDepicted")) {
					int peopleDepicted = literal.intValue();
					System.out.println(peopleDepicted + " people are depicted in this painting");
				} else if (property.getLocalName().equals("creationDate")) {
					LocalDate date = LocalDate.from(literal.temporalAccessorValue());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example03LiteralDatatypes.java`
#### Snippet
```java
				} else if (property.getLocalName().equals("creationDate")) {
					LocalDate date = LocalDate.from(literal.temporalAccessorValue());
					System.out.println("The painting was created on " + date);
				}

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example03LiteralDatatypes.java`
#### Snippet
```java

				// you can also just get the lexical value (a string) without worrying about the datatype
				System.out.println("Lexical value: '" + literal.getLabel() + "'");
			}
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example10PropertyValues.java`
#### Snippet
```java

				// write the info about this painting to the console in Turtle format
				System.out.println("--- information about painting: " + painting);
				Rio.write(paintingProperties, System.out, RDFFormat.TURTLE);
				System.out.println();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example10PropertyValues.java`
#### Snippet
```java
				// write the info about this painting to the console in Turtle format
				System.out.println("--- information about painting: " + painting);
				Rio.write(paintingProperties, System.out, RDFFormat.TURTLE);
				System.out.println();
			}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example10PropertyValues.java`
#### Snippet
```java
				System.out.println("--- information about painting: " + painting);
				Rio.write(paintingProperties, System.out, RDFFormat.TURTLE);
				System.out.println();
			}
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example06WriteRdfXml.java`
#### Snippet
```java
		// Instead of simply printing the statements to the screen, we use a Rio writer to
		// write the model in RDF/XML syntax:
		Rio.write(model, System.out, RDFFormat.RDFXML);

		// Note that instead of writing to the screen using `System.out` you could also provide
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example05BlankNode.java`
#### Snippet
```java
		// To see what's in our model, let's just print it to the screen
		for (Statement st : model) {
			System.out.println(st);
		}
	}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example01BuildModel.java`
#### Snippet
```java
		// to see what's in our model, let's just print it to the screen
		for (Statement st : model) {
			System.out.println(st);
		}
	}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example08ReadTurtle.java`
#### Snippet
```java

		// To check that we have correctly read the file, let's print out the model to the screen again
		model.forEach(System.out::println);
	}
}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example12BuildModelWithNamedGraphs.java`
#### Snippet
```java
		// each named graph is stored as a separate context in our Model
		for (Resource context : model.contexts()) {
			System.out.println("Named graph " + context + " contains: ");

			// write _only_ the statements in the current named graph to the console, in N-Triples format
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example12BuildModelWithNamedGraphs.java`
#### Snippet
```java

			// write _only_ the statements in the current named graph to the console, in N-Triples format
			Rio.write(model.filter(null, null, null, context), System.out, RDFFormat.NTRIPLES);
			System.out.println();
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example12BuildModelWithNamedGraphs.java`
#### Snippet
```java
			// write _only_ the statements in the current named graph to the console, in N-Triples format
			Rio.write(model.filter(null, null, null, context), System.out, RDFFormat.NTRIPLES);
			System.out.println();
		}

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example04LanguageTags.java`
#### Snippet
```java
			if (value instanceof Literal) {
				Literal title = (Literal) value;
				System.out.println("language: " + title.getLanguage().orElse("unknown"));
				System.out.println(" title: " + title.getLabel());
			}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example04LanguageTags.java`
#### Snippet
```java
				Literal title = (Literal) value;
				System.out.println("language: " + title.getLanguage().orElse("unknown"));
				System.out.println(" title: " + title.getLabel());
			}
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example09Filter.java`
#### Snippet
```java
			// let's print out the statement in a nice way. We ignore the namespaces and only print the
			// local name of each IRI
			System.out.print(subject.getLocalName() + " " + predicate.getLocalName() + " ");
			if (object.isLiteral()) {
				// it's a literal value. Let's print it out nicely, in quotes, and without any ugly
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example09Filter.java`
#### Snippet
```java
				// it's a literal value. Let's print it out nicely, in quotes, and without any ugly
				// datatype stuff
				System.out.println("\"" + ((Literal) object).getLabel() + "\"");
			} else if (object.isIRI()) {
				// it's an IRI. Just print out the local part (without the namespace)
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example09Filter.java`
#### Snippet
```java
			} else if (object.isIRI()) {
				// it's an IRI. Just print out the local part (without the namespace)
				System.out.println(((IRI) object).getLocalName());
			} else {
				// it's a blank node or an RDF-star Triple. Just print it out as-is.
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example09Filter.java`
#### Snippet
```java
			} else {
				// it's a blank node or an RDF-star Triple. Just print it out as-is.
				System.out.println(object);
			}
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example02BuildModel.java`
#### Snippet
```java
		// To see what's in our model, let's just print it to the screen
		for (Statement st : model) {
			System.out.println(st);
		}
	}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example11SinglePropertyValue.java`
#### Snippet
```java
			String firstName = Models.objectString(firstNameTriples).orElse("(unknown)");

			System.out.println(artist + " has first name '" + firstName + "'");
		}

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example16SPARQLConstructQuery.java`
#### Snippet
```java
			GraphQuery query = conn.prepareGraphQuery(queryString);

			RDFHandler turtleWriter = Rio.createWriter(RDFFormat.TURTLE, System.out);
			query.evaluate(turtleWriter);

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example16SPARQLConstructQuery.java`
#### Snippet
```java
					Statement st = result.next();
					// ... and print them out
					System.out.println(st);
				}
			}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example14AddRDFToDatabase.java`
#### Snippet
```java
			try (RepositoryResult<Statement> result = conn.getStatements(null, null, null)) {
				for (Statement st : result) {
					System.out.println("db contains: " + st);
				}
			}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example13AddRDFToDatabase.java`
#### Snippet
```java
			try (RepositoryResult<Statement> result = conn.getStatements(null, null, null)) {
				for (Statement st : result) {
					System.out.println("db contains: " + st);
				}
			}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example15SimpleSPARQLQuery.java`
#### Snippet
```java
				for (BindingSet solution : result) {
					// ... and print out the value of the variable binding for ?s and ?n
					System.out.println("?s = " + solution.getValue("s"));
					System.out.println("?n = " + solution.getValue("n"));
				}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example15SimpleSPARQLQuery.java`
#### Snippet
```java
					// ... and print out the value of the variable binding for ?s and ?n
					System.out.println("?s = " + solution.getValue("s"));
					System.out.println("?n = " + solution.getValue("n"));
				}
			}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailConcurrencyTest.java`
#### Snippet
```java

	protected synchronized void fail(String message, Throwable t) {
		System.err.println(message);
		t.printStackTrace();
		m_failed = true;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java

	public static void main(String[] args) throws java.io.IOException {
		System.out.println("Your SPARQL query:");

		BufferedReader in = new BufferedReader(new InputStreamReader(System.in));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java
						long finish = System.currentTimeMillis();

						System.out.println("Parsed query: ");
						System.out.println(parsedQuery.toString());
						System.out.println();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java

						System.out.println("Parsed query: ");
						System.out.println(parsedQuery.toString());
						System.out.println();
						System.out.println("parsed in " + (finish - start) + " ms.");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java
						System.out.println("Parsed query: ");
						System.out.println(parsedQuery.toString());
						System.out.println();
						System.out.println("parsed in " + (finish - start) + " ms.");

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java
						System.out.println(parsedQuery.toString());
						System.out.println();
						System.out.println("parsed in " + (finish - start) + " ms.");

					} catch (Exception e) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java

					} catch (Exception e) {
						System.err.println(e.getMessage());
						e.printStackTrace();
					}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SimpleNode.java`
#### Snippet
```java

	public void dump(String prefix) {
		System.out.println(toString(prefix));
		if (children != null) {
			for (int i = 0; i < children.length; ++i) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
	 * Debug output.
	 */
	public java.io.PrintStream debugStream = System.out;

	/**
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractTupleQueryResultWriterTest.java`
#### Snippet
```java
	@Test
	public void testGetSupportedSettings() throws Exception {
		TupleQueryResultWriter writer = getWriterFactory().getWriter(System.out);

		Collection<RioSetting<?>> supportedSettings = writer.getSupportedSettings();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NamespaceStore.java`
#### Snippet
```java

		for (Namespace ns : nsStore) {
			System.out.println(ns.getPrefix() + " = " + ns.getName());
		}
	}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java
			if (valueStore.isNamespaceData(data)) {
				String ns = valueStore.data2namespace(data);
				System.out.println("[" + id + "] " + ns);
			} else {
				Value value = valueStore.data2value(id, data);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java
			} else {
				Value value = valueStore.data2value(id, data);
				System.out.println("[" + id + "] " + value.toString());
			}
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
	private void checkAllCommitted() throws IOException {
		for (TripleIndex index : indexes) {
			System.out.println("Checking " + index + " index");
			BTree btree = index.getBTree();
			try (RecordIterator iter = btree.iterateAll()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
					boolean wasToggled = (flags & TOGGLE_EXPLICIT_FLAG) != 0;
					if (wasAdded || wasRemoved || wasToggled) {
						System.out.println("unexpected triple: " + ByteArrayUtil.toHexString(data));
					}
				}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/DataStore.java`
#### Snippet
```java
	public static void main(String[] args) throws Exception {
		if (args.length < 2) {
			System.err.println(
					"Usage: java org.eclipse.rdf4j.sesame.sailimpl.nativerdf.datastore.DataStore <data-dir> <file-prefix>");
			return;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/DataStore.java`
#### Snippet
```java
		}

		System.out.println("Dumping DataStore contents...");
		File dataDir = new File(args[0]);
		DataFile.DataIterator iter;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/DataStore.java`
#### Snippet
```java
				byte[] data = iter.next();

				System.out.println(ByteArrayUtil.toHexString(data));
			}
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/rio/RDFSizeBenchmarks.java`
#### Snippet
```java

		long size = os.getByteCount();
		System.out.printf(
				"%20s %8.2f MB in %-14s - %s%n",
				dataset,
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/rio/RDFTestDataset.java`
#### Snippet
```java

	private void extractFromZip(File downloadFile, File dataFile) throws IOException {
		System.out.println("Extracting " + archiveEntryName + " from " + downloadFile + " to " + dataFile.getName());
		try (ZipFile zf = new ZipFile(downloadFile)) {
			ZipEntry entry = zf.getEntry(archiveEntryName);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/rio/RDFTestDataset.java`
#### Snippet
```java

	private void downloadTo(File downloadFile) throws IOException {
		System.out.println("Downloading " + url);
		try (InputStream is = new BufferedInputStream(url.openStream())) {
			IOUtil.transfer(is, downloadFile);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/rio/RDFTestDataset.java`
#### Snippet
```java

	private void extractFromGzip(File downloadFile, File dataFile) throws IOException {
		System.out.println("Extracting " + downloadFile + " to " + dataFile.getName());
		GZIPInputStream in = new GZIPInputStream(new FileInputStream(downloadFile));
		IOUtil.writeStream(in, dataFile);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java

	private void listArtistsWithoutPaintings() {
		System.out.println("Artists without paintings:");
		Set<Artist> a = artService.getArtistsWithoutPaintings();
		if (a.isEmpty()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
		Set<Artist> a = artService.getArtistsWithoutPaintings();
		if (a.isEmpty()) {
			System.out.println("\t[none]");
		} else {
			for (Artist artist : a) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
		} else {
			for (Artist artist : a) {
				System.out.println(String.format("%s %s", artist.getFirstName(), artist.getLastName()));
			}
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
	private void listPaintingsByArtist(Map<Artist, Set<Painting>> paintingsMap) {
		for (Artist a : paintingsMap.keySet()) {
			System.out.println(String.format("%s %s", a.getFirstName(), a.getLastName()));
			for (Painting p : paintingsMap.get(a)) {
				System.out.println(String.format("\t%s (%s)", p.getTitle(), p.getTechnique()));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
			System.out.println(String.format("%s %s", a.getFirstName(), a.getLastName()));
			for (Painting p : paintingsMap.get(a)) {
				System.out.println(String.format("\t%s (%s)", p.getTitle(), p.getTechnique()));
			}
		}
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
	@Override
	public void run(String... args) {
		System.out.println("\nData read from 'artists.ttl':");
		Map<Artist, Set<Painting>> paintingsMap = artService.getPaintingsGroupedByArtist();
		listPaintingsByArtist(paintingsMap);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
		Map<Artist, Set<Painting>> paintingsMap = artService.getPaintingsGroupedByArtist();
		listPaintingsByArtist(paintingsMap);
		System.out.println("\nNow adding some data...");
		addPaintingWithArtist();
		System.out.println("\nReloaded data:");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
		System.out.println("\nNow adding some data...");
		addPaintingWithArtist();
		System.out.println("\nReloaded data:");
		paintingsMap = artService.getPaintingsGroupedByArtist();
		listPaintingsByArtist(paintingsMap);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
		paintingsMap = artService.getPaintingsGroupedByArtist();
		listPaintingsByArtist(paintingsMap);
		System.out.println("\n");
		listArtistsWithoutPaintings();
		System.out.println("\n");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
		System.out.println("\n");
		listArtistsWithoutPaintings();
		System.out.println("\n");
	}

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/util/DebugRepositoryConnectionListener.java`
#### Snippet
```java

	public DebugRepositoryConnectionListener() {
		this(System.err);
	}

```

## RuleId[id=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java

		return stringWriter.toString()
				.replaceAll("(?m)^(@prefix)(.*)(\\.)$", "") // remove all lines that are prefix declarations
				.trim();
	}
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinRenderer.java`
#### Snippet
```java
		}

		String[] sourceStrings = update.getSourceString().split("\\s*;\\s*");
		List<UpdateExpr> updateExprs = update.getUpdateExprs();
		Map<UpdateExpr, Dataset> datasets = update.getDatasetMapping();
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11UpdateComplianceTest.java`
#### Snippet
```java
			for (String namedGraph : inputNamedGraphs.keySet()) {
				logger.info("checking named graph {}", namedGraph);
				IRI contextURI = con.getValueFactory().createIRI(namedGraph.replaceAll("\"", ""));
				compareGraphs(Iterations.asList(con.getStatements(null, null, null, true, contextURI)),
						Iterations.asList(erCon.getStatements(null, null, null, true, contextURI)));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	public SPARQLQueryTest(String testURI, String name, String queryFileURL, String resultFileURL, Dataset dataSet,
			boolean laxCardinality, boolean checkOrder, String... ignoredTests) {
		super(name.replaceAll("\\(", " ").replaceAll("\\)", " "));

		this.testURI = testURI;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	public SPARQLQueryTest(String testURI, String name, String queryFileURL, String resultFileURL, Dataset dataSet,
			boolean laxCardinality, boolean checkOrder, String... ignoredTests) {
		super(name.replaceAll("\\(", " ").replaceAll("\\)", " "));

		this.testURI = testURI;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
			for (String namedGraph : inputNamedGraphs.keySet()) {
				logger.info("checking named graph {}", namedGraph);
				IRI contextURI = con.getValueFactory().createIRI(namedGraph.replaceAll("\"", ""));
				compareGraphs(Iterations.asList(con.getStatements(null, null, null, true, contextURI)),
						Iterations.asList(erCon.getStatements(null, null, null, true, contextURI)));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `testsuites/shacl/src/main/java/org/eclipse/rdf4j/testsuite/shacl/manifest/AbstractSHACLTest.java`
#### Snippet
```java
	public AbstractSHACLTest(String testURI, String label, Model shapesGraph, Model dataGraph, boolean failure,
			boolean conforms) {
		super(label.replaceAll("\\(", " ").replaceAll("\\)", " "));

		this.testURI = testURI;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `testsuites/shacl/src/main/java/org/eclipse/rdf4j/testsuite/shacl/manifest/AbstractSHACLTest.java`
#### Snippet
```java
	public AbstractSHACLTest(String testURI, String label, Model shapesGraph, Model dataGraph, boolean failure,
			boolean conforms) {
		super(label.replaceAll("\\(", " ").replaceAll("\\)", " "));

		this.testURI = testURI;
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
			if (subtag.length() == 2) {
				// exception 1: two-letter subtags not at the starte and not preceded by a singleton are upper case
				if (subtags[i - 1].length() > 1 && subtag.matches("\\w\\w")) {
					subtags[i] = subtag.toUpperCase();
				}
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
			} else if (subtag.length() == 4) {
				// exception 2: four-letter subtags not at the start and not preceded by a singleton are title case
				if (subtags[i - 1].length() > 1 && subtag.matches("\\w\\w\\w\\w")) {
					subtags[i] = subtag.substring(0, 1).toUpperCase() + subtag.substring(1);
				}
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
			writeURI(datatype);
		} else if (label.length() > 0 && encoded.equals(label) && label.charAt(0) != '<' && label.charAt(0) != '_'
				&& !label.matches("^[\\+\\-]?[\\d\\.].*")) {
			// no need to include double quotes
			writer.write(encoded);
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/ReasoningUpdateBenchmark.java`
#### Snippet
```java

	private InputStream resourceAsStream(String resourceName) {
		String[] split = param.split("\\:\\:");
		this.expectedCount = Integer.parseInt(split[1]);
		String filename = split[0] + "/" + resourceName;
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/ReasoningBenchmark.java`
#### Snippet
```java

	private InputStream resourceAsStream(String resourceName) {
		String[] split = param.split("::");

		this.expectedCount = Integer.parseInt(split[1]);
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
		String wktFieldParam = parameters.getProperty(LuceneSail.WKT_FIELDS);
		if (wktFieldParam != null) {
			wktFields = Sets.newHashSet(wktFieldParam.split("\\s+"));
		}

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java

			indexedLangs = new HashSet<>();
			indexedLangs.addAll(Arrays.asList(indexedlangString.toLowerCase().split("\\s+")));
		}

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
				String keyStr = key.toString();
				Set<IRI> objects = new HashSet<>();
				for (String obj : prop.getProperty(keyStr).split("\\s+")) {
					objects.add(vf.createIRI(obj));
				}
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/RenderUtils.java`
#### Snippet
```java
	 */
	public static String escape(String theString) {
		theString = theString.replaceAll("\"", "\\\\\"");

		StringBuffer aBuffer = new StringBuffer();
```

## RuleId[id=ComparatorMethodParameterNotUsed]
### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
	static final Comparator<ByteBuffer> COMPARATOR = new Comparator<ByteBuffer>() {
		@Override
		public int compare(ByteBuffer b1, ByteBuffer b2) {
			int b1Len = b1.remaining();
			int b2Len = b2.remaining();
```

## RuleId[id=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'org.eclipse.rdf4j.model.Value\[\]' expected, 'org.eclipse.rdf4j.model.IRI\[\]' found
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			Set<Value> objects = model.filter(resource, READ_CONTEXT, null).objects();
			setReadContexts(objects.toArray(new IRI[objects.size()]));

			objects = model.filter(resource, ADD_CONTEXT, null).objects();
```

### SuspiciousToArrayCall
Array of type 'org.eclipse.rdf4j.model.Value\[\]' expected, 'org.eclipse.rdf4j.model.IRI\[\]' found
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			objects = model.filter(resource, ADD_CONTEXT, null).objects();
			setAddContexts(objects.toArray(new IRI[objects.size()]));

			objects = model.filter(resource, REMOVE_CONTEXT, null).objects();
```

### SuspiciousToArrayCall
Array of type 'org.eclipse.rdf4j.model.Value\[\]' expected, 'org.eclipse.rdf4j.model.IRI\[\]' found
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			objects = model.filter(resource, REMOVE_CONTEXT, null).objects();
			setRemoveContexts(objects.toArray(new IRI[objects.size()]));

			objects = model.filter(resource, ARCHIVE_CONTEXT, null).objects();
```

### SuspiciousToArrayCall
Array of type 'org.eclipse.rdf4j.model.Value\[\]' expected, 'org.eclipse.rdf4j.model.IRI\[\]' found
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			objects = model.filter(resource, ARCHIVE_CONTEXT, null).objects();
			setArchiveContexts(objects.toArray(new IRI[objects.size()]));

			Models.objectIRI(model.getStatements(resource, INSERT_CONTEXT, null))
```

## RuleId[id=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `DelegatingSailDataset()` of an abstract class should not be declared 'public'
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/DelegatingSailDataset.java`
#### Snippet
```java
	 * @param delegate
	 */
	public DelegatingSailDataset(SailDataset delegate) {
		this.delegate = delegate;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `SailClosingIteration()` of an abstract class should not be declared 'public'
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailClosingIteration.java`
#### Snippet
```java
	 * @param closes The {@link SailClosable}s to {@link SailClosable#close()} when the itererator is closed.
	 */
	public SailClosingIteration(CloseableIteration<? extends T, X> iter, SailClosable... closes) {
		super(iter);
		this.closes = closes;
```

### NonProtectedConstructorInAbstractClass
Constructor `ShaclSailBaseConfiguration()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailBaseConfiguration.java`
#### Snippet
```java
	}

	public ShaclSailBaseConfiguration() {
		super();
		this.sparqlValidation = !"false"
```

### NonProtectedConstructorInAbstractClass
Constructor `ShaclSailBaseConfiguration()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailBaseConfiguration.java`
#### Snippet
```java
	private Set<IRI> shapesGraphs = ShaclSailConfig.SHAPES_GRAPHS_DEFAULT;

	public ShaclSailBaseConfiguration(NotifyingSail baseSail) {
		super(baseSail);
		this.sparqlValidation = !"false"
```

### NonProtectedConstructorInAbstractClass
Constructor `Path()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/paths/Path.java`
#### Snippet
```java
	Resource id;

	public Path(Resource id) {
		this.id = id;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `Shape()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
	Resource[] contexts;

	public Shape() {
	}

```

### NonProtectedConstructorInAbstractClass
Constructor `Shape()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
	}

	public Shape(Shape shape) {
		this.deactivated = shape.deactivated;
		this.message = shape.message;
```

### NonProtectedConstructorInAbstractClass
Constructor `FilterPlanNode()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
	abstract boolean checkTuple(ValidationTuple t);

	public FilterPlanNode(PlanNode parent) {
		this.parent = PlanNodeHelper.handleSorting(this, parent);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `LoggingCloseableIteration()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LoggingCloseableIteration.java`
#### Snippet
```java
	private boolean closed;

	public LoggingCloseableIteration(PlanNode planNode, ValidationExecutionLogger validationExecutionLogger) {
		this.planNode = planNode;
		this.validationExecutionLogger = validationExecutionLogger;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBulkJoinPlanNode()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AbstractBulkJoinPlanNode.java`
#### Snippet
```java
	ValidationExecutionLogger validationExecutionLogger;

	public AbstractBulkJoinPlanNode() {
//		this.stackTrace = Thread.currentThread().getStackTrace();
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConstraintComponent()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/AbstractConstraintComponent.java`
#### Snippet
```java
	}

	public AbstractConstraintComponent() {

	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConstraintComponent()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/AbstractConstraintComponent.java`
#### Snippet
```java
	private TargetChain targetChain;

	public AbstractConstraintComponent(Resource id) {
		this.id = id;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `LogicalOperatorConstraintComponent()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/LogicalOperatorConstraintComponent.java`
#### Snippet
```java

public abstract class LogicalOperatorConstraintComponent extends AbstractConstraintComponent {
	public LogicalOperatorConstraintComponent(Resource id) {
		super(id);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `SimpleAbstractConstraintComponent()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java
	}

	public SimpleAbstractConstraintComponent() {

	}
```

### NonProtectedConstructorInAbstractClass
Constructor `SimpleAbstractConstraintComponent()` of an abstract class should not be declared 'public'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java
	TargetChain targetChain;

	public SimpleAbstractConstraintComponent(Resource id) {
		this.id = id;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSimpleQueryModelVisitor()` of an abstract class should not be declared 'public'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
	}

	public AbstractSimpleQueryModelVisitor(boolean meetStatementPatternChildren) {
		this.meetStatementPatternChildren = meetStatementPatternChildren;
		this.meetProjectionElemListChildren = true;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSimpleQueryModelVisitor()` of an abstract class should not be declared 'public'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
	private final boolean meetProjectionElemListChildren;

	public AbstractSimpleQueryModelVisitor() {
		this(true);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractForwardChainingInferencer()` of an abstract class should not be declared 'public'
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/AbstractForwardChainingInferencer.java`
#### Snippet
```java
	 *--------------*/

	public AbstractForwardChainingInferencer() {
		super();
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractForwardChainingInferencer()` of an abstract class should not be declared 'public'
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/AbstractForwardChainingInferencer.java`
#### Snippet
```java
	}

	public AbstractForwardChainingInferencer(NotifyingSail baseSail) {
		super(baseSail);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractForwardChainingInferencerConnection()` of an abstract class should not be declared 'public'
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/AbstractForwardChainingInferencerConnection.java`
#### Snippet
```java
	 *--------------*/

	public AbstractForwardChainingInferencerConnection(Sail sail, InferencerConnection con) {
		super(con);
		this.sail = sail;
```

### NonProtectedConstructorInAbstractClass
Constructor `MemoryOverflowModel()` of an abstract class should not be declared 'public'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	}

	public MemoryOverflowModel(Set<Namespace> namespaces) {
		memory = new LinkedHashModel(namespaces, LARGE_BLOCK);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `MemoryOverflowModel()` of an abstract class should not be declared 'public'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	}

	public MemoryOverflowModel(Model model) {
		this(model.getNamespaces());
		addAll(model);
```

### NonProtectedConstructorInAbstractClass
Constructor `MemoryOverflowModel()` of an abstract class should not be declared 'public'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	}

	public MemoryOverflowModel(Set<Namespace> namespaces, Collection<? extends Statement> c) {
		this(namespaces);
		addAll(c);
```

### NonProtectedConstructorInAbstractClass
Constructor `MemoryOverflowModel()` of an abstract class should not be declared 'public'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	SimpleValueFactory vf = SimpleValueFactory.getInstance();

	public MemoryOverflowModel() {
		memory = new LinkedHashModel(LARGE_BLOCK);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `RDF4JException()` of an abstract class should not be declared 'public'
in `core/common/exception/src/main/java/org/eclipse/rdf4j/common/exception/RDF4JException.java`
#### Snippet
```java
	}

	public RDF4JException(Throwable t) {
		super(t);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `RDF4JException()` of an abstract class should not be declared 'public'
in `core/common/exception/src/main/java/org/eclipse/rdf4j/common/exception/RDF4JException.java`
#### Snippet
```java
	private static final long serialVersionUID = 169031751338226782L;

	public RDF4JException() {
		super();
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `RDF4JException()` of an abstract class should not be declared 'public'
in `core/common/exception/src/main/java/org/eclipse/rdf4j/common/exception/RDF4JException.java`
#### Snippet
```java
	}

	public RDF4JException(String msg, Throwable t) {
		super(msg, t);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `RDF4JException()` of an abstract class should not be declared 'public'
in `core/common/exception/src/main/java/org/eclipse/rdf4j/common/exception/RDF4JException.java`
#### Snippet
```java
	}

	public RDF4JException(String msg) {
		super(msg);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQL12QueryComplianceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL12QueryComplianceTest.java`
#### Snippet
```java
	}

	public SPARQL12QueryComplianceTest(String displayName, String testURI, String name, String queryFileURL,
			String resultFileURL, Dataset dataset, boolean ordered, boolean laxCardinality) {
		super(displayName, testURI, name, queryFileURL, resultFileURL, dataset, ordered, laxCardinality);
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQL10QueryComplianceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL10QueryComplianceTest.java`
#### Snippet
```java
	 * @param ordered
	 */
	public SPARQL10QueryComplianceTest(String displayName, String testURI, String name, String queryFileURL,
			String resultFileURL, Dataset dataset, boolean ordered, boolean laxCardinality) {
		super(displayName, testURI, name, queryFileURL, resultFileURL, dataset, ordered, laxCardinality);
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQL11SyntaxComplianceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxComplianceTest.java`
#### Snippet
```java
	}

	public SPARQL11SyntaxComplianceTest(String displayName, String testURI, String name, String queryFileURL,
			boolean positiveTest) {
		super(displayName, testURI, name, queryFileURL, positiveTest);
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQL11QueryComplianceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11QueryComplianceTest.java`
#### Snippet
```java
	}

	public SPARQL11QueryComplianceTest(String displayName, String testURI, String name, String queryFileURL,
			String resultFileURL, Dataset dataset, boolean ordered, boolean laxCardinality) {
		super(displayName, testURI, name, queryFileURL, resultFileURL, dataset, ordered, laxCardinality);
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQLSyntaxComplianceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLSyntaxComplianceTest.java`
#### Snippet
```java
	}

	public SPARQLSyntaxComplianceTest(String displayName, String testURI, String name, String queryFileURL,
			boolean positiveTest) {

```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQLComplianceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLComplianceTest.java`
#### Snippet
```java
	 * @param name
	 */
	public SPARQLComplianceTest(String displayName, String testURI, String name) {
		this.displayName = displayName;
		this.testURI = testURI;
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQL11SyntaxTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxTest.java`
#### Snippet
```java
	 *--------------*/

	public SPARQL11SyntaxTest(String testURI, String name, String queryFileURL, boolean positiveTest) {
		super(name);
		this.testURI = testURI;
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQL11UpdateComplianceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11UpdateComplianceTest.java`
#### Snippet
```java
	}

	public SPARQL11UpdateComplianceTest(String displayName, String testURI, String name, String requestFile,
			IRI defaultGraphURI, Map<String, IRI> inputNamedGraphs, IRI resultDefaultGraphURI,
			Map<String, IRI> resultNamedGraphs) {
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQLQueryComplianceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryComplianceTest.java`
#### Snippet
```java
	 * @param ordered
	 */
	public SPARQLQueryComplianceTest(String displayName, String testURI, String name, String queryFileURL,
			String resultFileURL, Dataset dataset, boolean ordered, boolean laxCardinality) {
		super(displayName, testURI, name);
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQLQueryTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	}

	public SPARQLQueryTest(String testURI, String name, String queryFileURL, String resultFileURL, Dataset dataSet,
			boolean laxCardinality, boolean checkOrder, String... ignoredTests) {
		super(name.replaceAll("\\(", " ").replaceAll("\\)", " "));
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQLQueryTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	 *--------------*/

	public SPARQLQueryTest(String testURI, String name, String queryFileURL, String resultFileURL, Dataset dataSet,
			boolean laxCardinality, String... ignoredTests) {
		this(testURI, name, queryFileURL, resultFileURL, dataSet, laxCardinality, false);
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQLUpdateConformanceTest()` of an abstract class should not be declared 'public'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
	 *--------------*/

	public SPARQLUpdateConformanceTest(String testURI, String name, String requestFile, IRI defaultGraphURI,
			Map<String, IRI> inputNamedGraphs, IRI resultDefaultGraphURI, Map<String, IRI> resultNamedGraphs) {
		super(name);
```

### NonProtectedConstructorInAbstractClass
Constructor `GeoSPARQLManifestTest()` of an abstract class should not be declared 'public'
in `testsuites/geosparql/src/main/java/org/eclipse/rdf4j/testsuite/query/algebra/geosparql/GeoSPARQLManifestTest.java`
#### Snippet
```java
public abstract class GeoSPARQLManifestTest extends SPARQLQueryComplianceTest {

	public GeoSPARQLManifestTest(String displayName, String testURI, String name, String queryFileURL,
			String resultFileURL, Dataset dataset, boolean ordered, boolean laxCardinality) {
		super(displayName, testURI, name, queryFileURL, resultFileURL, dataset, ordered, laxCardinality);
```

### NonProtectedConstructorInAbstractClass
Constructor `RDF4JDao()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/RDF4JDao.java`
#### Snippet
```java
	private final Map<String, NamedSparqlSupplier> namedSparqlSuppliers = new ConcurrentHashMap<>();

	public RDF4JDao(RDF4JTemplate rdf4JTemplate) {
		this.rdf4JTemplate = rdf4JTemplate;
		prepareNamedSparqlSuppliers(new NamedSparqlSupplierPreparer());
```

### NonProtectedConstructorInAbstractClass
Constructor `SimpleRDF4JCRUDDao()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/SimpleRDF4JCRUDDao.java`
#### Snippet
```java
	}

	public SimpleRDF4JCRUDDao(RDF4JTemplate rdf4JTemplate) {
		super(rdf4JTemplate);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `SimpleRDF4JCRUDDao()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/SimpleRDF4JCRUDDao.java`
#### Snippet
```java
@Experimental
public abstract class SimpleRDF4JCRUDDao<ENTITY, ID> extends RDF4JCRUDDao<ENTITY, ENTITY, ID> {
	public SimpleRDF4JCRUDDao(RDF4JTemplate rdf4JTemplate, Class<ID> idClass) {
		super(rdf4JTemplate, idClass);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `RDF4JCRUDDao()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/RDF4JCRUDDao.java`
#### Snippet
```java
	 * Constructor to be used by implementations that use IRI for the ID type.
	 */
	public RDF4JCRUDDao(RDF4JTemplate rdf4JTemplate) {
		this(rdf4JTemplate, (Class<ID>) IRI.class);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `RDF4JCRUDDao()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/RDF4JCRUDDao.java`
#### Snippet
```java
	 * ID is anything but IRI.
	 */
	public RDF4JCRUDDao(RDF4JTemplate rdf4JTemplate, Class<ID> idClass) {
		super(rdf4JTemplate);
		this.idClass = idClass;
```

### NonProtectedConstructorInAbstractClass
Constructor `DelegatingUpdate()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/query/DelegatingUpdate.java`
#### Snippet
```java
	private final Update delegate;

	public DelegatingUpdate(Update delegate) {
		this.delegate = delegate;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `DelegatingTupleQuery()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/query/DelegatingTupleQuery.java`
#### Snippet
```java
	private final TupleQuery delegate;

	public DelegatingTupleQuery(TupleQuery delegate) {
		this.delegate = delegate;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `DelegatingRepositoryConnectionFactory()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/connectionfactory/DelegatingRepositoryConnectionFactory.java`
#### Snippet
```java
	private final RepositoryConnectionFactory delegate;

	public DelegatingRepositoryConnectionFactory(RepositoryConnectionFactory delegate) {
		this.delegate = delegate;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `DelegatingGraphQuery()` of an abstract class should not be declared 'public'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/query/DelegatingGraphQuery.java`
#### Snippet
```java
	private final GraphQuery delegate;

	public DelegatingGraphQuery(GraphQuery delegate) {
		this.delegate = delegate;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSHACLTest()` of an abstract class should not be declared 'public'
in `testsuites/shacl/src/main/java/org/eclipse/rdf4j/testsuite/shacl/manifest/AbstractSHACLTest.java`
#### Snippet
```java
	 *--------------*/

	public AbstractSHACLTest(String testURI, String label, Model shapesGraph, Model dataGraph, boolean failure,
			boolean conforms) {
		super(label.replaceAll("\\(", " ").replaceAll("\\)", " "));
```

### NonProtectedConstructorInAbstractClass
Constructor `SPARQLResultsXSVMappingStrategy()` of an abstract class should not be declared 'public'
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/SPARQLResultsXSVMappingStrategy.java`
#### Snippet
```java
	private static final String WRITING_UNSUPPORTED = "This mapping strategy does not write.";

	public SPARQLResultsXSVMappingStrategy(ValueFactory valueFactory) {
		this.valueFactory = valueFactory;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `ExtensibleStore()` of an abstract class should not be declared 'public'
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	}

	public ExtensibleStore(boolean cacheEnabled) {
		this.cacheEnabled = cacheEnabled;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `ExtensibleStore()` of an abstract class should not be declared 'public'
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java
	private FederatedServiceResolver serviceResolver;

	public ExtensibleStore() {
		this(true);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `ExtensibleEvaluationStatistics()` of an abstract class should not be declared 'public'
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleEvaluationStatistics.java`
#### Snippet
```java
	final ExtensibleSailStore extensibleSailStore;

	public ExtensibleEvaluationStatistics(ExtensibleSailStore extensibleSailStore) {
		this.extensibleSailStore = extensibleSailStore;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDelegatingSailImplConfig()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/AbstractDelegatingSailImplConfig.java`
#### Snippet
```java
	 * Create a new RepositoryConfigImpl.
	 */
	public AbstractDelegatingSailImplConfig() {
		super();
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDelegatingSailImplConfig()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/AbstractDelegatingSailImplConfig.java`
#### Snippet
```java
	 * Create a new RepositoryConfigImpl.
	 */
	public AbstractDelegatingSailImplConfig(String type) {
		super(type);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDelegatingSailImplConfig()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/AbstractDelegatingSailImplConfig.java`
#### Snippet
```java
	 * Create a new RepositoryConfigImpl.
	 */
	public AbstractDelegatingSailImplConfig(String type, SailImplConfig delegate) {
		this(type);
		setDelegate(delegate);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSailImplConfig()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/AbstractSailImplConfig.java`
#### Snippet
```java
	 * Create a new RepositoryConfigImpl.
	 */
	public AbstractSailImplConfig() {
	}

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSailImplConfig()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/AbstractSailImplConfig.java`
#### Snippet
```java
	 * Create a new RepositoryConfigImpl.
	 */
	public AbstractSailImplConfig(String type) {
		this();
		setType(type);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractNotifyingSailConnection()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractNotifyingSailConnection.java`
#### Snippet
```java
	private final List<SailConnectionListener> listeners = new CopyOnWriteArrayList<>();

	public AbstractNotifyingSailConnection(AbstractSail sailBase) {
		super(sailBase);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `NotifyingSailConnectionBase()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/NotifyingSailConnectionBase.java`
#### Snippet
```java
public abstract class NotifyingSailConnectionBase extends AbstractNotifyingSailConnection {

	public NotifyingSailConnectionBase(AbstractSail sailBase) {
		super(sailBase);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSail()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSail.java`
#### Snippet
```java
	 */

	public AbstractSail() {
		super();
		this.addSupportedIsolationLevel(IsolationLevels.READ_UNCOMMITTED);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractReadWriteLockManager()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/AbstractReadWriteLockManager.java`
#### Snippet
```java
	}

	public AbstractReadWriteLockManager(boolean trackLocks, int waitToCollect) {
		this("", waitToCollect, LockDiagnostics.fromLegacyTracking(trackLocks));
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractReadWriteLockManager()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/AbstractReadWriteLockManager.java`
#### Snippet
```java
	final int writePreference;

	public AbstractReadWriteLockManager() {
		this(false);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractReadWriteLockManager()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/AbstractReadWriteLockManager.java`
#### Snippet
```java
	}

	public AbstractReadWriteLockManager(boolean trackLocks) {
		this(trackLocks, LockMonitoring.INITIAL_WAIT_TO_COLLECT);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractReadWriteLockManager()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/AbstractReadWriteLockManager.java`
#### Snippet
```java
	}

	public AbstractReadWriteLockManager(String alias, LockDiagnostics... lockDiagnostics) {
		this(alias, LockMonitoring.INITIAL_WAIT_TO_COLLECT, lockDiagnostics);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractReadWriteLockManager()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/AbstractReadWriteLockManager.java`
#### Snippet
```java
	}

	public AbstractReadWriteLockManager(String alias, int waitToCollect, LockDiagnostics... lockDiagnostics) {

		this.tryWriteLockMillis = Math.min(1000, waitToCollect);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSailConnection()` of an abstract class should not be declared 'public'
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSailConnection.java`
#### Snippet
```java
	 *--------------*/

	public AbstractSailConnection(AbstractSail sailBase) {
		this.sailBase = sailBase;
		txnActive = false;
```

### NonProtectedConstructorInAbstractClass
Constructor `RDFStoreTest()` of an abstract class should not be declared 'public'
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
	 *--------------*/

	public RDFStoreTest() {
	}

```

### NonProtectedConstructorInAbstractClass
Constructor `AggregateFunction()` of an abstract class should not be declared 'public'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/aggregate/AggregateFunction.java`
#### Snippet
```java
	protected final Function<BindingSet, Value> evaluationStep;

	public AggregateFunction(Function<BindingSet, Value> evaluationStep) {
		this.evaluationStep = evaluationStep;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRecordCache()` of an abstract class should not be declared 'public'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/AbstractRecordCache.java`
#### Snippet
```java
	}

	public AbstractRecordCache(long maxRecords) {
		this.maxRecords = maxRecords;
		this.recordCount = new AtomicLong();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRecordCache()` of an abstract class should not be declared 'public'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/AbstractRecordCache.java`
#### Snippet
```java
	 *--------------*/

	public AbstractRecordCache() {
		this(Long.MAX_VALUE);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractQueryResultIOTest()` of an abstract class should not be declared 'public'
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
	 *
	 */
	public AbstractQueryResultIOTest() {
		super();
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `MemoryOverflowModel()` of an abstract class should not be declared 'public'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/MemoryOverflowModel.java`
#### Snippet
```java
	}

	public MemoryOverflowModel(Set<Namespace> namespaces, Collection<? extends Statement> c) {
		this(namespaces);
		addAll(c);
```

### NonProtectedConstructorInAbstractClass
Constructor `MemoryOverflowModel()` of an abstract class should not be declared 'public'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/MemoryOverflowModel.java`
#### Snippet
```java
	}

	public MemoryOverflowModel(Set<Namespace> namespaces) {
		memory = new LinkedHashModel(namespaces, LARGE_BLOCK);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `MemoryOverflowModel()` of an abstract class should not be declared 'public'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/MemoryOverflowModel.java`
#### Snippet
```java
	}

	public MemoryOverflowModel(Model model) {
		this(model.getNamespaces());
		addAll(model);
```

### NonProtectedConstructorInAbstractClass
Constructor `MemoryOverflowModel()` of an abstract class should not be declared 'public'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/MemoryOverflowModel.java`
#### Snippet
```java
	SimpleValueFactory vf = SimpleValueFactory.getInstance();

	public MemoryOverflowModel() {
		memory = new LinkedHashModel(LARGE_BLOCK);
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractQueryPreparer()` of an abstract class should not be declared 'public'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/AbstractQueryPreparer.java`
#### Snippet
```java
	private final TripleSource tripleSource;

	public AbstractQueryPreparer(TripleSource tripleSource) {
		this.tripleSource = tripleSource;
	}
```

### NonProtectedConstructorInAbstractClass
Constructor `StatisticCollector()` of an abstract class should not be declared 'public'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/aggregate/StatisticCollector.java`
#### Snippet
```java
	protected ValueExprEvaluationException typeError;

	public StatisticCollector(boolean population) {
		this.population = population;
		this.statistics = new SummaryStatistics();
```

### NonProtectedConstructorInAbstractClass
Constructor `JoinExecutorBase()` of an abstract class should not be declared 'public'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/JoinExecutorBase.java`
#### Snippet
```java
public abstract class JoinExecutorBase<T> extends org.eclipse.rdf4j.repository.sparql.federation.JoinExecutorBase<T> {

	public JoinExecutorBase(CloseableIteration<T, QueryEvaluationException> leftIter, TupleExpr rightArg,
			BindingSet bindings) throws QueryEvaluationException {
		super(leftIter, rightArg, bindings);
```

## RuleId[id=TestCaseWithConstructor]
### TestCaseWithConstructor
Initialization logic in constructor `NegativeParserTest()` instead of 'setup()' life cycle method
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/NegativeParserTest.java`
#### Snippet
```java
	 *--------------*/

	public NegativeParserTest(IRI testUri, String caseURI, String inputURL, String baseURL, RDFParser targetParser,
			FailureMode failureMode) throws MalformedURLException {
		super(caseURI);
```

### TestCaseWithConstructor
Initialization logic in constructor `PositiveParserTest()` instead of 'setup()' life cycle method
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/PositiveParserTest.java`
#### Snippet
```java
	 *--------------*/

	public PositiveParserTest(IRI testUri, String testName, String inputURL, String outputURL, String baseURL,
			RDFParser targetParser, RDFParser ntriplesParser) throws MalformedURLException {
		super(testName);
```

### TestCaseWithConstructor
Initialization logic in constructor `SPARQLSyntaxComplianceTest()` instead of 'setup()' life cycle method
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLSyntaxComplianceTest.java`
#### Snippet
```java
	}

	public SPARQLSyntaxComplianceTest(String displayName, String testURI, String name, String queryFileURL,
			boolean positiveTest) {

```

### TestCaseWithConstructor
Initialization logic in constructor `SPARQLComplianceTest()` instead of 'setup()' life cycle method
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLComplianceTest.java`
#### Snippet
```java
	 * @param name
	 */
	public SPARQLComplianceTest(String displayName, String testURI, String name) {
		this.displayName = displayName;
		this.testURI = testURI;
```

### TestCaseWithConstructor
Initialization logic in constructor `SPARQL11SyntaxTest()` instead of 'setup()' life cycle method
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxTest.java`
#### Snippet
```java
	 *--------------*/

	public SPARQL11SyntaxTest(String testURI, String name, String queryFileURL, boolean positiveTest) {
		super(name);
		this.testURI = testURI;
```

### TestCaseWithConstructor
Initialization logic in constructor `SPARQLQueryComplianceTest()` instead of 'setup()' life cycle method
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryComplianceTest.java`
#### Snippet
```java
	 * @param ordered
	 */
	public SPARQLQueryComplianceTest(String displayName, String testURI, String name, String queryFileURL,
			String resultFileURL, Dataset dataset, boolean ordered, boolean laxCardinality) {
		super(displayName, testURI, name);
```

### TestCaseWithConstructor
Initialization logic in constructor `SPARQLQueryTest()` instead of 'setup()' life cycle method
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	}

	public SPARQLQueryTest(String testURI, String name, String queryFileURL, String resultFileURL, Dataset dataSet,
			boolean laxCardinality, boolean checkOrder, String... ignoredTests) {
		super(name.replaceAll("\\(", " ").replaceAll("\\)", " "));
```

### TestCaseWithConstructor
Initialization logic in constructor `SPARQLQueryTest()` instead of 'setup()' life cycle method
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	 *--------------*/

	public SPARQLQueryTest(String testURI, String name, String queryFileURL, String resultFileURL, Dataset dataSet,
			boolean laxCardinality, String... ignoredTests) {
		this(testURI, name, queryFileURL, resultFileURL, dataSet, laxCardinality, false);
```

### TestCaseWithConstructor
Initialization logic in constructor `SPARQLUpdateConformanceTest()` instead of 'setup()' life cycle method
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
	 *--------------*/

	public SPARQLUpdateConformanceTest(String testURI, String name, String requestFile, IRI defaultGraphURI,
			Map<String, IRI> inputNamedGraphs, IRI resultDefaultGraphURI, Map<String, IRI> resultNamedGraphs) {
		super(name);
```

### TestCaseWithConstructor
Initialization logic in constructor `AbstractSHACLTest()` instead of 'setup()' life cycle method
in `testsuites/shacl/src/main/java/org/eclipse/rdf4j/testsuite/shacl/manifest/AbstractSHACLTest.java`
#### Snippet
```java
	 *--------------*/

	public AbstractSHACLTest(String testURI, String label, Model shapesGraph, Model dataGraph, boolean failure,
			boolean conforms) {
		super(label.replaceAll("\\(", " ").replaceAll("\\)", " "));
```

## RuleId[id=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `head`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclAstLists.java`
#### Snippet
```java
			}

			head = shapeSource.getRdfRest(head);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `head`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclAstLists.java`
#### Snippet
```java
				Resource next = bnode();
				model.add(head, RDF.REST, next);
				head = next;
			} else {
				model.add(head, RDF.REST, RDF.NIL);
```

### AssignmentToMethodParameter
Assignment to method parameter `head`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclAstLists.java`
#### Snippet
```java
			Value value = shapeSource.getRdfFirst(head);
			ret.add(value);
			head = shapeSource.getRdfRest(head);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `tupleExpr`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
		if (cloneTupleExpression) {
			// Clone the tuple expression to allow for more aggressive optimizations
			tupleExpr = tupleExpr.clone();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `tupleExpr`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
			// Add a dummy root node to the tuple expressions to allow the
			// optimizers to modify the actual root node
			tupleExpr = new QueryRoot(tupleExpr);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `tupleExpr`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
			}

			tupleExpr = strategy.optimize(tupleExpr, store.getEvaluationStatistics(), bindings);

			logger.trace("Optimized query model:\n{}", tupleExpr);
```

### AssignmentToMethodParameter
Assignment to method parameter `statementMatchers`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/StatementMatcher.java`
#### Snippet
```java

	public static List<StatementMatcher> reduce(Set<String> varNames, List<StatementMatcher> statementMatchers) {
		statementMatchers = statementMatchers
				.stream()
				.map(s -> {
```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PlanNodeHelper.java`
#### Snippet
```java
		if (requiresSorted) {
			if (!parent.producesSorted()) {
				parent = new Sort(parent);
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `includeTargetsAffectedByRemoval`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/EffectiveTarget.java`
#### Snippet
```java
		assert !chain.isEmpty();

		includeTargetsAffectedByRemoval = includeTargetsAffectedByRemoval && connectionsGroup.getStats().hasRemoved();

		if (chain.size() == 1 && !(includeTargetsAffectedByRemoval && optional != null)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `includeTargetsAffectedByRemoval`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/EffectiveTarget.java`
#### Snippet
```java
			if (chain.getFirst().target instanceof RSXTargetShape) {
				statementMatchersRemoval.addAll(chain.getFirst().getStatementMatcher().collect(Collectors.toList()));
				includeTargetsAffectedByRemoval = true;
			}

```

### AssignmentToMethodParameter
Assignment to method parameter `queryFragment`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java

			// empty set
			queryFragment = "" +
					"?a <http://fjiewojfiwejfioewhgurh8924y.com/f289h8fhn> ?c.\n" +
					"FILTER (NOT EXISTS {?a <http://fjiewojfiwejfioewhgurh8924y.com/f289h8fhn> ?c})";
```

### AssignmentToMethodParameter
Assignment to method parameter `compress`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Unique.java`
#### Snippet
```java

			if (!compress) {
				compress = parentUnique.compress;
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `queryFragment`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ExternalFilterByQuery.java`
#### Snippet
```java
		this.filterOn = filterOn;

		queryFragment = "SELECT ?" + queryVariable.getName() + " WHERE {\n" + queryFragment + "\n}";
		this.queryString = StatementMatcher.StableRandomVariableProvider.normalize(queryFragment);
		try {
```

### AssignmentToMethodParameter
Assignment to method parameter `leftNode`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
			String query,
			Function<BindingSet, ValidationTuple> mapper) {
		leftNode = PlanNodeHelper.handleSorting(this, leftNode);
		this.leftNode = leftNode;
		this.query = StatementMatcher.StableRandomVariableProvider.normalize(query);
```

### AssignmentToMethodParameter
Assignment to method parameter `query`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AbstractBulkJoinPlanNode.java`
#### Snippet
```java
		// #VALUES_INJECTION_POINT# is an annotation in the query where there is a "new scope" due to the bottom up
		// semantics of SPARQL but where we don't actually want a new scope.
		query = query.replace(AbstractConstraintComponent.VALUES_INJECTION_POINT, "\nVALUES (?a) {}\n");
		String completeQuery = "select * where {\nVALUES (?a) {}\n" + query + "\n}";
		return SparqlQueryParserCache.get(completeQuery);
```

### AssignmentToMethodParameter
Assignment to method parameter `allTargets`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java
	private PlanNode getFilterAttacherWithNegation(boolean negatePlan, PlanNode allTargets) {
		if (negatePlan) {
			allTargets = getFilterAttacher().apply(allTargets).getTrueNode(UnBufferedPlanNode.class);
		} else {
			allTargets = getFilterAttacher().apply(allTargets).getFalseNode(UnBufferedPlanNode.class);
```

### AssignmentToMethodParameter
Assignment to method parameter `allTargets`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java
			allTargets = getFilterAttacher().apply(allTargets).getTrueNode(UnBufferedPlanNode.class);
		} else {
			allTargets = getFilterAttacher().apply(allTargets).getFalseNode(UnBufferedPlanNode.class);
		}
		return allTargets;
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionSAXParser.java`
#### Snippet
```java

			if (encoding != null && "base64".equalsIgnoreCase(encoding)) {
				text = new String(javax.xml.bind.DatatypeConverter.parseBase64Binary(text));
			}
			Literal lit;
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/StatementPattern.java`
#### Snippet
```java
			}
			if (unique) {
				names[i++] = name;
			}
			return i;
```

### AssignmentToMethodParameter
Assignment to method parameter `topTupleExpr`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/QueryModelTreeToGenericPlanNode.java`
#### Snippet
```java
	public QueryModelTreeToGenericPlanNode(QueryModelNode topTupleExpr) {
		if (topTupleExpr instanceof QueryRoot) {
			topTupleExpr = ((QueryRoot) topTupleExpr).getArg();
		}
		this.topTupleExpr = topTupleExpr;
```

### AssignmentToMethodParameter
Assignment to method parameter `bytes`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/Varint.java`
#### Snippet
```java
	 */
	private static long readSignificantBits(ByteBuffer bb, int pos, int bytes) {
		bytes--;
		long value = (long) (bb.get(pos++) & 0xFF) << (bytes * 8);
		while (bytes-- > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `pos`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/Varint.java`
#### Snippet
```java
	private static long readSignificantBits(ByteBuffer bb, int pos, int bytes) {
		bytes--;
		long value = (long) (bb.get(pos++) & 0xFF) << (bytes * 8);
		while (bytes-- > 0) {
			value |= (long) (bb.get(pos++) & 0xFF) << (bytes * 8);
```

### AssignmentToMethodParameter
Assignment to method parameter `bytes`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/Varint.java`
#### Snippet
```java
		bytes--;
		long value = (long) (bb.get(pos++) & 0xFF) << (bytes * 8);
		while (bytes-- > 0) {
			value |= (long) (bb.get(pos++) & 0xFF) << (bytes * 8);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `pos`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/Varint.java`
#### Snippet
```java
		long value = (long) (bb.get(pos++) & 0xFF) << (bytes * 8);
		while (bytes-- > 0) {
			value |= (long) (bb.get(pos++) & 0xFF) << (bytes * 8);
		}
		return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `bytes`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/Varint.java`
#### Snippet
```java
	 */
	private static void writeSignificantBits(ByteBuffer bb, long value, int bytes) {
		while (bytes-- > 0) {
			bb.put((byte) (0xFF & (value >>> (bytes * 8))));
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `bytes`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/Varint.java`
#### Snippet
```java
	 */
	private static long readSignificantBits(ByteBuffer bb, int bytes) {
		bytes--;
		long value = (long) (bb.get() & 0xFF) << (bytes * 8);
		while (bytes-- > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bytes`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/Varint.java`
#### Snippet
```java
		bytes--;
		long value = (long) (bb.get() & 0xFF) << (bytes * 8);
		while (bytes-- > 0) {
			value |= (long) (bb.get() & 0xFF) << (bytes * 8);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `level`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencerConnection.java`
#### Snippet
```java
		addedInferredStatementsCount = 0;
		if (level == null) {
			level = sail.getDefaultIsolationLevel();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `mapSize`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbUtil.java`
#### Snippet
```java
	 */
	static long autoGrowMapSize(long mapSize, long pageSize, long requiredSize) {
		mapSize = Math.max(mapSize * 2, Math.max(requiredSize, MIN_AUTOGROW_SIZE));
		// align map size to page size
		return mapSize % pageSize == 0 ? mapSize : mapSize + (mapSize / pageSize + 1) * pageSize;
```

### AssignmentToMethodParameter
Assignment to method parameter `entry`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinRenderer.java`
#### Snippet
```java
			}
			if (entry == null) {
				entry = valueFactory.createBNode();
			}
			handler.handleStatement(valueFactory.createStatement(list, RDF.FIRST, entry));
```

### AssignmentToMethodParameter
Assignment to method parameter `dataFormat`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
		if (this.getRepository().useCompatibleMode()) {

			dataFormat = getBackwardCompatibleFormat(dataFormat);

			if (!isActive()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `dataFormat`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
		if (this.getRepository().useCompatibleMode()) {

			dataFormat = getBackwardCompatibleFormat(dataFormat);

			if (!isActive()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
		if (baseURI == null) {
			// default baseURI to file
			baseURI = file.toURI().toString();
		}
		if (dataFormat == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `dataFormat`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
		}
		if (dataFormat == null) {
			dataFormat = Rio.getParserFormatForFileName(file.getName())
					.orElseThrow(Rio.unsupportedFormat(file.getName()));
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
			throws IOException, RDFParseException, RepositoryException {
		if (baseURI == null) {
			baseURI = url.toExternalForm();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `dataFormat`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
				mimeType = mimeType.substring(0, semiColonIdx);
			}
			dataFormat = Rio.getParserFormatForMIMEType(mimeType)
					.orElse(Rio.getParserFormatForFileName(url.getPath()).orElseThrow(Rio.unsupportedFormat(mimeType)));
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `subject`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java

		if (subject == null) {
			subject = SESAME.WILDCARD;
		}
		if (predicate == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `predicate`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
		}
		if (predicate == null) {
			predicate = SESAME.WILDCARD;
		}
		if (object == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `object`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
		}
		if (object == null) {
			object = SESAME.WILDCARD;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `bindingsIter`
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserQuery.java`
#### Snippet
```java
			CloseableIteration<? extends BindingSet, QueryEvaluationException> bindingsIter) {
		if (getMaxExecutionTime() > 0) {
			bindingsIter = new QueryInterruptIteration(bindingsIter, 1000L * getMaxExecutionTime());
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `bindingsIter`
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserQuery.java`
#### Snippet
```java
			CloseableIteration<? extends BindingSet, QueryEvaluationException> bindingsIter) {
		if (getMaxExecutionTime() > 0) {
			bindingsIter = new QueryInterruptIteration(bindingsIter, 1000L * getMaxExecutionTime());
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `subj`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java

		void getMaxKey(ByteBuffer bb, long subj, long pred, long obj, long context) {
			subj = subj <= 0 ? Long.MAX_VALUE : subj;
			pred = pred <= 0 ? Long.MAX_VALUE : pred;
			obj = obj <= 0 ? Long.MAX_VALUE : obj;
```

### AssignmentToMethodParameter
Assignment to method parameter `pred`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
		void getMaxKey(ByteBuffer bb, long subj, long pred, long obj, long context) {
			subj = subj <= 0 ? Long.MAX_VALUE : subj;
			pred = pred <= 0 ? Long.MAX_VALUE : pred;
			obj = obj <= 0 ? Long.MAX_VALUE : obj;
			context = context < 0 ? Long.MAX_VALUE : context;
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
			subj = subj <= 0 ? Long.MAX_VALUE : subj;
			pred = pred <= 0 ? Long.MAX_VALUE : pred;
			obj = obj <= 0 ? Long.MAX_VALUE : obj;
			context = context < 0 ? Long.MAX_VALUE : context;
			toKey(bb, subj, pred, obj, context);
```

### AssignmentToMethodParameter
Assignment to method parameter `context`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
			pred = pred <= 0 ? Long.MAX_VALUE : pred;
			obj = obj <= 0 ? Long.MAX_VALUE : obj;
			context = context < 0 ? Long.MAX_VALUE : context;
			toKey(bb, subj, pred, obj, context);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `subj`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java

		void getMinKey(ByteBuffer bb, long subj, long pred, long obj, long context) {
			subj = subj <= 0 ? 0 : subj;
			pred = pred <= 0 ? 0 : pred;
			obj = obj <= 0 ? 0 : obj;
```

### AssignmentToMethodParameter
Assignment to method parameter `pred`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
		void getMinKey(ByteBuffer bb, long subj, long pred, long obj, long context) {
			subj = subj <= 0 ? 0 : subj;
			pred = pred <= 0 ? 0 : pred;
			obj = obj <= 0 ? 0 : obj;
			context = context <= 0 ? 0 : context;
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
			subj = subj <= 0 ? 0 : subj;
			pred = pred <= 0 ? 0 : pred;
			obj = obj <= 0 ? 0 : obj;
			context = context <= 0 ? 0 : context;
			toKey(bb, subj, pred, obj, context);
```

### AssignmentToMethodParameter
Assignment to method parameter `context`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
			pred = pred <= 0 ? 0 : pred;
			obj = obj <= 0 ? 0 : obj;
			context = context <= 0 ? 0 : context;
			toKey(bb, subj, pred, obj, context);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfxml/RDFXMLParserTestCase.java`
#### Snippet
```java
		public Literal createLiteral(String value, IRI datatype) {
			if (RDF.XMLLITERAL.equals(datatype)) {
				value = canonicalizeXmlLiteral(value);
			}

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfxml/RDFXMLParserTestCase.java`
#### Snippet
```java
		public Literal createLiteral(String value, CoreDatatype datatype) {
			if (datatype == CoreDatatype.RDF.XMLLITERAL) {
				value = canonicalizeXmlLiteral(value);
			}

```

### AssignmentToMethodParameter
Assignment to method parameter `urlString`
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfxml/RDFXMLParserTestCase.java`
#### Snippet
```java
		if (urlString.startsWith(W3C_TESTS_DIR)) {
			// resolve to local copy
			urlString = LOCAL_TESTS_DIR + "w3c-approved/" + urlString.substring(W3C_TESTS_DIR.length());
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfxml/RDFXMLParserTestCase.java`
#### Snippet
```java
			if (coreDatatype == CoreDatatype.RDF.XMLLITERAL) {
				assert RDF.XMLLITERAL.equals(datatype);
				value = canonicalizeXmlLiteral(value);
			} else {
				assert !RDF.XMLLITERAL.equals(datatype);
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL10ManifestTest.java`
#### Snippet
```java
				"contexts argument may not be null; either the value should be cast to Resource or an empty array should be supplied");
		if (baseURI == null) {
			baseURI = url.toExternalForm();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11ManifestTest.java`
#### Snippet
```java
			throws IOException, RepositoryException, RDFParseException {
		if (baseURI == null) {
			baseURI = url.toExternalForm();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/RDF4JCRUDDao.java`
#### Snippet
```java
		}
		if (!idPresent) {
			id = generateNewId(id);
		}
		return id;
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	 */
	public static String escapeCharacterData(String text) {
		text = text.replace("&", "&amp;");
		text = text.replace("<", "&lt;");
		text = text.replace(">", "&gt;");
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	public static String escapeCharacterData(String text) {
		text = text.replace("&", "&amp;");
		text = text.replace("<", "&lt;");
		text = text.replace(">", "&gt;");
		text = text.replace("\r", "&#xD;");
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
		text = text.replace("&", "&amp;");
		text = text.replace("<", "&lt;");
		text = text.replace(">", "&gt;");
		text = text.replace("\r", "&#xD;");
		return text;
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
		text = text.replace("<", "&lt;");
		text = text.replace(">", "&gt;");
		text = text.replace("\r", "&#xD;");
		return text;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java

	private static String _escapeAttValue(String value) {
		value = value.replace("&", "&amp;");
		value = value.replace("<", "&lt;");
		value = value.replace(">", "&gt;");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	private static String _escapeAttValue(String value) {
		value = value.replace("&", "&amp;");
		value = value.replace("<", "&lt;");
		value = value.replace(">", "&gt;");
		value = value.replace("\t", "&#x9;");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
		value = value.replace("&", "&amp;");
		value = value.replace("<", "&lt;");
		value = value.replace(">", "&gt;");
		value = value.replace("\t", "&#x9;");
		value = value.replace("\n", "&#xA;");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
		value = value.replace("<", "&lt;");
		value = value.replace(">", "&gt;");
		value = value.replace("\t", "&#x9;");
		value = value.replace("\n", "&#xA;");
		value = value.replace("\r", "&#xD;");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
		value = value.replace(">", "&gt;");
		value = value.replace("\t", "&#x9;");
		value = value.replace("\n", "&#xA;");
		value = value.replace("\r", "&#xD;");
		return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
		value = value.replace("\t", "&#x9;");
		value = value.replace("\n", "&#xA;");
		value = value.replace("\r", "&#xD;");
		return value;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	 */
	public static String escapeSingleQuotedAttValue(String value) {
		value = _escapeAttValue(value);
		value = value.replace("'", "&apos;");
		return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	public static String escapeSingleQuotedAttValue(String value) {
		value = _escapeAttValue(value);
		value = value.replace("'", "&apos;");
		return value;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	 */
	public static String escapeDoubleQuotedAttValue(String value) {
		value = _escapeAttValue(value);
		value = value.replace("\"", "&quot;");
		return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
	public static String escapeDoubleQuotedAttValue(String value) {
		value = _escapeAttValue(value);
		value = value.replace("\"", "&quot;");
		return value;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `testsuites/shacl/src/main/java/org/eclipse/rdf4j/testsuite/shacl/manifest/SHACLManifestTestSuiteFactory.java`
#### Snippet
```java
			throws IOException, RDFParseException {
		if (baseURI == null) {
			baseURI = url.toExternalForm();
		}
		ParsedIRI baseIRI = ParsedIRI.create(baseURI);
```

### AssignmentToMethodParameter
Assignment to method parameter `fromIndex`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ByteArrayUtil.java`
#### Snippet
```java

		if (fromIndex < 0) {
			fromIndex = 0;
		}
		toIndex = Math.min(toIndex, a.length);
```

### AssignmentToMethodParameter
Assignment to method parameter `toIndex`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ByteArrayUtil.java`
#### Snippet
```java
			fromIndex = 0;
		}
		toIndex = Math.min(toIndex, a.length);

		for (int i = fromIndex; fromIndex < toIndex && result == -1 && i < toIndex; i++) {
```

### AssignmentToMethodParameter
Assignment to method parameter `prioritiseCleaning`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
						"Low free memory ({} MB)! Prioritising cleaning of removed statements from the MemoryStore.",
						freeToAllocateMemory / 1024 / 1024);
				prioritiseCleaning = true;
				this.statements.setPrioritiseCleanup(true);
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
		while (value > 127) {
			out.write(value & 0b01111111 | 0b10000000);
			value >>>= 7;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `props`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
				all.put(propName, propValue);
			}
			props = all;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `fileName`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/FileFormat.java`
#### Snippet
```java
		int lastPathSepIdx = Math.max(fileName.lastIndexOf('/'), fileName.lastIndexOf('\\'));
		if (lastPathSepIdx >= 0) {
			fileName = fileName.substring(lastPathSepIdx + 1);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `fileName`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/FileFormat.java`
#### Snippet
```java
			// No match, check if the file name has more "extensions" (e.g.
			// example.rdf.gz)
			fileName = fileName.substring(0, dotIdx);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `_path`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java

		// Remove any '.' segments:
		_path = _path.replace("/./", "/");
		if (_path == null) {
			return null;
```

### AssignmentToMethodParameter
Assignment to method parameter `_path`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		if (_path.startsWith("./")) {
			// Remove both characters
			_path = _path.substring(2);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `_path`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		if (_path.endsWith("/.")) {
			// Remove only the last dot, not the slash!
			_path = _path.substring(0, _path.length() - 1);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `reason`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		if (pos > -1 && pos < iri.length()) {
			int cp = iri.codePointAt(pos);
			reason = reason + " U+" + Integer.toHexString(cp).toUpperCase();
		}
		return new URISyntaxException(iri, reason, pos);
```

### AssignmentToMethodParameter
Assignment to method parameter `contexts`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
	public boolean add(Resource subj, IRI pred, Value obj, Resource... contexts) {
		if (contexts.length == 0) {
			contexts = NULL_CTX;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
	public boolean remove(Resource s, IRI p, Value o, Resource... c) {
		if (s == null) {
			s = subj;
		}
		if (p == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (p == null) {
			p = pred;
		}
		if (o == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `o`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (o == null) {
			o = obj;
		}
		if (c != null && c.length == 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (c != null && c.length == 0) {
			c = contexts;
		}
		if (!accept(s, p, o, c)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
	public boolean add(Resource s, IRI p, Value o, Resource... c) {
		if (s == null) {
			s = subj;
		}
		if (p == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (p == null) {
			p = pred;
		}
		if (o == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `o`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (o == null) {
			o = obj;
		}
		if (c != null && c.length == 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (c != null && c.length == 0) {
			c = contexts;
		}
		if (!accept(s, p, o, c)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
	public final void removeTermIteration(Iterator<Statement> iter, Resource s, IRI p, Value o, Resource... c) {
		if (s == null) {
			s = subj;
		}
		if (p == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (p == null) {
			p = pred;
		}
		if (o == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `o`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (o == null) {
			o = obj;
		}
		if (c != null && c.length == 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (c != null && c.length == 0) {
			c = contexts;
		}
		if (!accept(s, p, o, c)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
	public boolean contains(Resource s, IRI p, Value o, Resource... c) {
		if (s == null) {
			s = subj;
		}
		if (p == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (p == null) {
			p = pred;
		}
		if (o == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `o`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (o == null) {
			o = obj;
		}
		if (c != null && c.length == 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (c != null && c.length == 0) {
			c = contexts;
		}
		if (!accept(s, p, o, c)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
	public Model filter(Resource s, IRI p, Value o, Resource... c) {
		if (s == null) {
			s = subj;
		}
		if (p == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (p == null) {
			p = pred;
		}
		if (o == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `o`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (o == null) {
			o = obj;
		}
		if (c != null && c.length == 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
		}
		if (c != null && c.length == 0) {
			c = contexts;
		}
		if (!accept(s, p, o, c)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `contexts`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java

	private Set<ModelStatement> choose(Resource subj, IRI pred, Value obj, Resource... contexts) {
		contexts = notNull(contexts);
		Set<ModelStatement> s = null;
		Set<ModelStatement> p = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `fromElement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
			boolean fromInclusive = true;
			if (comparator().compare(fromElement, lo) < 0) {
				fromElement = lo;
				fromInclusive = loInclusive;
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `toElement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
			boolean toInclusive = false;
			if (comparator().compare(hi, toElement) < 0) {
				toElement = hi;
				toInclusive = hiInclusive;
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `fromElement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
			boolean toInclusive = false;
			if (comparator().compare(fromElement, lo) < 0) {
				fromElement = lo;
				fromInclusive = loInclusive;
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `toElement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
			}
			if (comparator().compare(hi, toElement) < 0) {
				toElement = hi;
				toInclusive = hiInclusive;
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `lowestFound`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
				if (lowestFound == null
						|| clonedPartitioning.getMappingSize().compareTo(partitioning.getMappingSize()) < 0) {
					lowestFound = clonedPartitioning.getCurrentNodeMapping();
				}
			} else {
```

### AssignmentToMethodParameter
Assignment to method parameter `lowestFound`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
					continue;
				}
				lowestFound = distinguish(m, clonedPartitioning, lowestFound, fixpoints, finePartitionMappings);
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `partitioning`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
		if (partitioning == null) {
			final Set<BNode> blankNodes = getBlankNodes(m);
			partitioning = new Partitioning(blankNodes);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `otherDT`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDateTime.java`
#### Snippet
```java
		if (otherDT.hoursTimezone != null && (otherDT.iHoursTimezone != 0 || otherDT.iMinutesTimezone != 0)) {
			// Create a normalized copy of this DateTime object
			otherDT = (XMLDateTime) otherDT.clone();
			otherDT.normalize();
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC32.java`
#### Snippet
```java
	@Override
	public void update(byte[] b, int off, int len) {
		while (len-- > 0) {
			value ^= 0xFFFFFFFFL;
			int j = (int) ((value ^ b[off++]) & 0xFF);
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC32.java`
#### Snippet
```java
		while (len-- > 0) {
			value ^= 0xFFFFFFFFL;
			int j = (int) ((value ^ b[off++]) & 0xFF);
			value = (value >>> 8) ^ TABLE[j] & 0xFFFFFFFFL;
			value ^= 0xFFFFFFFFL;
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC16.java`
#### Snippet
```java
	@Override
	public void update(byte[] b, int off, int len) {
		while (len-- > 0) {
			int j = (int) (value ^ b[off++]) & 0xFF;
			value = (TABLE[j] ^ value >>> 8) & 0xFFFF;
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC16.java`
#### Snippet
```java
	public void update(byte[] b, int off, int len) {
		while (len-- > 0) {
			int j = (int) (value ^ b[off++]) & 0xFF;
			value = (TABLE[j] ^ value >>> 8) & 0xFFFF;
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC8.java`
#### Snippet
```java
	@Override
	public void update(byte[] b, int off, int len) {
		while (len-- > 0) {
			value = TABLE[(byte) (value ^ b[off++]) & 0xFF];
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC8.java`
#### Snippet
```java
	public void update(byte[] b, int off, int len) {
		while (len-- > 0) {
			value = TABLE[(byte) (value ^ b[off++]) & 0xFF];
		}
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `st`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
				object = createReifiedStatement(vf, reifiedIdMapper, (Triple) object, st.getContext(), consumer);
			}
			st = vf.createStatement(subject, st.getPredicate(), object, st.getContext());
		}
		consumer.accept(st);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
	@Deprecated
	public static String encodeURIString(String s) {
		s = StringUtil.gsub("\\", "\\u005C", s);
		s = StringUtil.gsub("\t", "\\u0009", s);
		s = StringUtil.gsub("\n", "\\u000A", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
	public static String encodeURIString(String s) {
		s = StringUtil.gsub("\\", "\\u005C", s);
		s = StringUtil.gsub("\t", "\\u0009", s);
		s = StringUtil.gsub("\n", "\\u000A", s);
		s = StringUtil.gsub("\r", "\\u000D", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("\\", "\\u005C", s);
		s = StringUtil.gsub("\t", "\\u0009", s);
		s = StringUtil.gsub("\n", "\\u000A", s);
		s = StringUtil.gsub("\r", "\\u000D", s);
		s = StringUtil.gsub("\"", "\\u0022", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("\t", "\\u0009", s);
		s = StringUtil.gsub("\n", "\\u000A", s);
		s = StringUtil.gsub("\r", "\\u000D", s);
		s = StringUtil.gsub("\"", "\\u0022", s);
		s = StringUtil.gsub("`", "\\u0060", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("\n", "\\u000A", s);
		s = StringUtil.gsub("\r", "\\u000D", s);
		s = StringUtil.gsub("\"", "\\u0022", s);
		s = StringUtil.gsub("`", "\\u0060", s);
		s = StringUtil.gsub("^", "\\u005E", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("\r", "\\u000D", s);
		s = StringUtil.gsub("\"", "\\u0022", s);
		s = StringUtil.gsub("`", "\\u0060", s);
		s = StringUtil.gsub("^", "\\u005E", s);
		s = StringUtil.gsub("|", "\\u007C", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("\"", "\\u0022", s);
		s = StringUtil.gsub("`", "\\u0060", s);
		s = StringUtil.gsub("^", "\\u005E", s);
		s = StringUtil.gsub("|", "\\u007C", s);
		s = StringUtil.gsub("<", "\\u003C", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("`", "\\u0060", s);
		s = StringUtil.gsub("^", "\\u005E", s);
		s = StringUtil.gsub("|", "\\u007C", s);
		s = StringUtil.gsub("<", "\\u003C", s);
		s = StringUtil.gsub(">", "\\u003E", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("^", "\\u005E", s);
		s = StringUtil.gsub("|", "\\u007C", s);
		s = StringUtil.gsub("<", "\\u003C", s);
		s = StringUtil.gsub(">", "\\u003E", s);
		s = StringUtil.gsub(" ", "\\u0020", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("|", "\\u007C", s);
		s = StringUtil.gsub("<", "\\u003C", s);
		s = StringUtil.gsub(">", "\\u003E", s);
		s = StringUtil.gsub(" ", "\\u0020", s);
		return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("<", "\\u003C", s);
		s = StringUtil.gsub(">", "\\u003E", s);
		s = StringUtil.gsub(" ", "\\u0020", s);
		return s;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		// the ones that form sequences of 3 or more double quotes, and the ones
		// at the end of a string.
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		// at the end of a string.
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		return s;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
	 */
	public static String encodeString(String s) {
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
	public static String encodeString(String s) {
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\"", "\\\"", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		return s;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareUnsignedLongs(String int1, String int2) {
		int1 = normalizeUnsignedLong(int1);
		int2 = normalizeUnsignedLong(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareUnsignedLongs(String int1, String int2) {
		int1 = normalizeUnsignedLong(int1);
		int2 = normalizeUnsignedLong(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static BigInteger parseInteger(String s) {
		s = trimPlusSign(s);
		return new BigInteger(s);
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private static String normalizeFPNumber(String value, String minMantissa, String maxMantissa, String minExponent,
			String maxExponent) {
		value = collapseWhiteSpace(value);

		if (value.contains(" ")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static int compareIntegers(String int1, String int2) {
		int1 = normalizeInteger(int1);
		int2 = normalizeInteger(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareIntegers(String int1, String int2) {
		int1 = normalizeInteger(int1);
		int2 = normalizeInteger(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareUnsignedShorts(String int1, String int2) {
		int1 = normalizeUnsignedShort(int1);
		int2 = normalizeUnsignedShort(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareUnsignedShorts(String int1, String int2) {
		int1 = normalizeUnsignedShort(int1);
		int2 = normalizeUnsignedShort(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareNonPositiveIntegers(String int1, String int2) {
		int1 = normalizeNonPositiveInteger(int1);
		int2 = normalizeNonPositiveInteger(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareNonPositiveIntegers(String int1, String int2) {
		int1 = normalizeNonPositiveInteger(int1);
		int2 = normalizeNonPositiveInteger(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareBytes(String int1, String int2) {
		int1 = normalizeByte(int1);
		int2 = normalizeByte(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareBytes(String int1, String int2) {
		int1 = normalizeByte(int1);
		int2 = normalizeByte(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `float1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static int compareFloats(String float1, String float2) {
		float1 = normalizeFloat(float1);
		float2 = normalizeFloat(float2);

```

### AssignmentToMethodParameter
Assignment to method parameter `float2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareFloats(String float1, String float2) {
		float1 = normalizeFloat(float1);
		float2 = normalizeFloat(float2);

		return compareCanonicalFloats(float1, float2);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static String normalizeBoolean(String value) {
		value = collapseWhiteSpace(value);

		if (value.equals("1")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int comparePositiveIntegers(String int1, String int2) {
		int1 = normalizePositiveInteger(int1);
		int2 = normalizePositiveInteger(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int comparePositiveIntegers(String int1, String int2) {
		int1 = normalizePositiveInteger(int1);
		int2 = normalizePositiveInteger(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static long parseLong(String s) {
		s = trimPlusSign(s);
		return Long.parseLong(s);
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareInts(String int1, String int2) {
		int1 = normalizeInt(int1);
		int2 = normalizeInt(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareInts(String int1, String int2) {
		int1 = normalizeInt(int1);
		int2 = normalizeInt(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareLongs(String int1, String int2) {
		int1 = normalizeLong(int1);
		int2 = normalizeLong(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareLongs(String int1, String int2) {
		int1 = normalizeLong(int1);
		int2 = normalizeLong(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `double1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static int compareDoubles(String double1, String double2) {
		double1 = normalizeDouble(double1);
		double2 = normalizeDouble(double2);

```

### AssignmentToMethodParameter
Assignment to method parameter `double2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareDoubles(String double1, String double2) {
		double1 = normalizeDouble(double1);
		double2 = normalizeDouble(double2);

		return compareCanonicalDoubles(double1, double2);
```

### AssignmentToMethodParameter
Assignment to method parameter `decimal`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static String normalizeDecimal(String decimal) {
		decimal = collapseWhiteSpace(decimal);

		int decLength = decimal.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			return Float.NaN;
		} else {
			s = trimPlusSign(s);
			return Float.parseFloat(s);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static int parseInt(String s) {
		s = trimPlusSign(s);
		return Integer.parseInt(s);
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			return Double.NaN;
		} else {
			s = trimPlusSign(s);
			return Double.parseDouble(s);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static byte parseByte(String s) {
		s = trimPlusSign(s);
		return Byte.parseByte(s);
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `dec1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static int compareDecimals(String dec1, String dec2) {
		dec1 = normalizeDecimal(dec1);
		dec2 = normalizeDecimal(dec2);

```

### AssignmentToMethodParameter
Assignment to method parameter `dec2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareDecimals(String dec1, String dec2) {
		dec1 = normalizeDecimal(dec1);
		dec2 = normalizeDecimal(dec2);

		return compareCanonicalDecimals(dec1, dec2);
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareUnsignedInts(String int1, String int2) {
		int1 = normalizeUnsignedInt(int1);
		int2 = normalizeUnsignedInt(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareUnsignedInts(String int1, String int2) {
		int1 = normalizeUnsignedInt(int1);
		int2 = normalizeUnsignedInt(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareUnsignedBytes(String int1, String int2) {
		int1 = normalizeUnsignedByte(int1);
		int2 = normalizeUnsignedByte(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareUnsignedBytes(String int1, String int2) {
		int1 = normalizeUnsignedByte(int1);
		int2 = normalizeUnsignedByte(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareNonNegativeIntegers(String int1, String int2) {
		int1 = normalizeNonNegativeInteger(int1);
		int2 = normalizeNonNegativeInteger(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareNonNegativeIntegers(String int1, String int2) {
		int1 = normalizeNonNegativeInteger(int1);
		int2 = normalizeNonNegativeInteger(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareNegativeIntegers(String int1, String int2) {
		int1 = normalizeNegativeInteger(int1);
		int2 = normalizeNegativeInteger(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareNegativeIntegers(String int1, String int2) {
		int1 = normalizeNegativeInteger(int1);
		int2 = normalizeNegativeInteger(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `integer`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	private static String normalizeIntegerValue(String integer, String minValue, String maxValue) {
		integer = collapseWhiteSpace(integer);

		int intLength = integer.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static short parseShort(String s) {
		s = trimPlusSign(s);
		return Short.parseShort(s);
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `int1`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compareShorts(String int1, String int2) {
		int1 = normalizeShort(int1);
		int2 = normalizeShort(int2);

```

### AssignmentToMethodParameter
Assignment to method parameter `int2`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static int compareShorts(String int1, String int2) {
		int1 = normalizeShort(int1);
		int2 = normalizeShort(int2);

		return compareCanonicalIntegers(int1, int2);
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java

					if (prefix.length() == 0 || !isLegalPrefix) {
						prefix = "ns";
					}

```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
					}

					prefix += number;
				}

```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java

	private static String encodeString(String s) {
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
	private static String encodeString(String s) {
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\"", "\\\"", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		return s;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `core/rio/ntriples/src/main/java/org/eclipse/rdf4j/rio/ntriples/NTriplesParser.java`
#### Snippet
```java
	protected IRI createURI(String uri) throws RDFParseException {
		try {
			uri = unescapeString(uri);
		} catch (IllegalArgumentException e) {
			reportError(e.getMessage(), NTriplesParserSettings.FAIL_ON_INVALID_LINES);
```

### AssignmentToMethodParameter
Assignment to method parameter `update`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			if (baseURI != null && !baseURI.isEmpty()) {
				// prepend update string with base URI declaration
				update = "BASE <" + baseURI + "> \n" + update;
			}
			queryParams.add(new BasicNameValuePair(Protocol.UPDATE_PARAM_NAME, update));
```

### AssignmentToMethodParameter
Assignment to method parameter `query`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			if (baseURI != null && !baseURI.isEmpty()) {
				// prepend query string with base URI declaration
				query = "BASE <" + baseURI + "> \n" + query;
			}
			queryParams.add(new BasicNameValuePair(Protocol.QUERY_PARAM_NAME, query));
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws MalformedQueryException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		return initOperation(super.prepareUpdate(ql, update, baseURI));
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws IOException, RDFParseException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		if (isNilContext(contexts) && !dataFormat.supportsContexts()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws IOException, RDFParseException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		if (isNilContext(contexts) && !dataFormat.supportsContexts()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws MalformedQueryException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		return initQuery(super.prepareGraphQuery(ql, query, baseURI));
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws IOException, RDFParseException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		if (isNilContext(contexts) && !dataFormat.supportsContexts()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws MalformedQueryException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		return initQuery(super.prepareQuery(ql, query, baseURI));
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws IOException, RDFParseException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		if (isNilContext(contexts) && !dataFormat.supportsContexts()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws MalformedQueryException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		return initQuery(super.prepareBooleanQuery(ql, query, baseURI));
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
			throws MalformedQueryException, RepositoryException {
		if (baseURI == null) {
			baseURI = getBaseURI();
		}
		return initQuery(super.prepareTupleQuery(ql, query, baseURI));
```

### AssignmentToMethodParameter
Assignment to method parameter `contexts`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java
	protected void removeWithoutCommit(Statement st, Resource... contexts) throws RepositoryException {
		if (contexts.length == 0 && st.getContext() != null) {
			contexts = new Resource[] { st.getContext() };
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `contexts`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepositoryConnection.java`
#### Snippet
```java
	protected void addWithoutCommit(Statement st, Resource... contexts) throws RepositoryException {
		if (contexts.length == 0 && st.getContext() != null) {
			contexts = new Resource[] { st.getContext() };
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java
			throws IOException, RDFParseException, RDFHandlerException {
		if (baseURI == null) {
			baseURI = url.toExternalForm();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `dataFormat`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java
						mimeType = mimeType.substring(0, semiColonIdx);
					}
					dataFormat = Rio.getParserFormatForMIMEType(mimeType)
							.orElseGet(() -> Rio.getParserFormatForFileName(url.getPath())
									.orElseThrow(() -> new UnsupportedRDFormatException(
```

### AssignmentToMethodParameter
Assignment to method parameter `in`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java
			throws IOException, RDFParseException, RDFHandlerException {
		if (!in.markSupported()) {
			in = new BufferedInputStream(in, 1024);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java
		if (baseURI == null) {
			// default baseURI to file
			baseURI = file.toURI().toString();
		}
		if (dataFormat == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `dataFormat`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java
		}
		if (dataFormat == null) {
			dataFormat = Rio.getParserFormatForFileName(file.getName())
					.orElseThrow(() -> new UnsupportedRDFormatException(
							"Could not find RDF format for file: " + file.getName()));
```

### AssignmentToMethodParameter
Assignment to method parameter `dataStructure`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailStore.java`
#### Snippet
```java

		if (evaluationStatistics instanceof DynamicStatistics) {
			dataStructure = new EvaluationStatisticsWrapper(dataStructure, (DynamicStatistics) evaluationStatistics);
			startEvaluationStatisticsMaintainerThread();
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `baseName`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		if (baseName != null) {
			// Filter exotic characters from the base name
			baseName = baseName.trim();

			int length = baseName.length();
```

### AssignmentToMethodParameter
Assignment to method parameter `baseName`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
			}

			baseName = buffer.toString();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `baseName`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		// When the base name is null or empty, generate one
		if (baseName == null || baseName.length() == 0) {
			baseName = "repository-";
		} else if (!baseName.endsWith("-")) {
			baseName += "-";
```

### AssignmentToMethodParameter
Assignment to method parameter `baseName`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
			baseName = "repository-";
		} else if (!baseName.endsWith("-")) {
			baseName += "-";
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `expectedSize`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
			// add 500 because this is our minimum margin of error
			estimatedSize += 500;
			expectedSize += 500;

			double diff = Math.abs(estimatedSize - expectedSize);
```

### AssignmentToMethodParameter
Assignment to method parameter `readLock`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/StampedLockManager.java`
#### Snippet
```java
						data = dataSupplier.get();
					}
					readLock = stampedLockManager.convertToReadLock(writeLock);
				} catch (Throwable t) {
					if (writeLock.isActive()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `isolationLevel`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSailConnection.java`
#### Snippet
```java
	public void begin(IsolationLevel isolationLevel) throws SailException {
		if (isolationLevel == null) {
			isolationLevel = sailBase.getDefaultIsolationLevel();
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/InferencingTest.java`
#### Snippet
```java
		// Dump results to tmp file for debugging
		String tmpDir = System.getProperty("java.io.tmpdir");
		name = name.replace("/", "_");
		File tmpFile = new File(tmpDir, "junit-" + name + ".nt");
		tmpFile.createNewFile();
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLWriter.java`
#### Snippet
```java
			// generate a legal unique prefix
			if (!isLegalPrefix) {
				prefix = "ns";
			}
			int number = 1;
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLWriter.java`
#### Snippet
```java
				number++;
			}
			prefix += number;
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
	 */
	public static String escape(String s) {
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
	public static String escape(String s) {
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
		s = StringUtil.gsub("\\", "\\\\", s);
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\b", "\\b", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
		s = StringUtil.gsub("\t", "\\t", s);
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\b", "\\b", s);
		s = StringUtil.gsub("\f", "\\f", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
		s = StringUtil.gsub("\n", "\\n", s);
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\b", "\\b", s);
		s = StringUtil.gsub("\f", "\\f", s);
		s = StringUtil.gsub("\"", "\\\"", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
		s = StringUtil.gsub("\r", "\\r", s);
		s = StringUtil.gsub("\b", "\\b", s);
		s = StringUtil.gsub("\f", "\\f", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		s = StringUtil.gsub("'", "\\'", s);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
		s = StringUtil.gsub("\b", "\\b", s);
		s = StringUtil.gsub("\f", "\\f", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		s = StringUtil.gsub("'", "\\'", s);
		return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
		s = StringUtil.gsub("\f", "\\f", s);
		s = StringUtil.gsub("\"", "\\\"", s);
		s = StringUtil.gsub("'", "\\'", s);
		return s;
	}
```

### AssignmentToMethodParameter
Assignment to method parameter `baseURI`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java

				if (uc.getBaseDecl() != null) {
					baseURI = uc.getBaseDecl().getIRI();
				}

```

### AssignmentToMethodParameter
Assignment to method parameter `data`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SimpleNode.java`
#### Snippet
```java
			for (Node childNode : children) {
				// Note: modified JavaCC code, child's data no longer ignored
				data = childNode.jjtAccept(visitor, data);
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `num`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JJTSyntaxTreeBuilderState.java`
#### Snippet
```java
	public void closeNodeScope(Node n, int num) {
		mk = marks.remove(marks.size() - 1);
		while (num-- > 0) {
			Node c = popNode();
			c.jjtSetParent(n);
```

### AssignmentToMethodParameter
Assignment to method parameter `curPos`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			throw new Error("Internal Error");
		}
		curPos = 0;
		int startsAt = 0;
		jjnewStateCnt = 157;
```

### AssignmentToMethodParameter
Assignment to method parameter `curPos`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				kind = 0x7fffffff;
			}
			++curPos;
			if ((i = jjnewStateCnt) == (startsAt = 157 - (jjnewStateCnt = startsAt))) {
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa10_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(9);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa10_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(9);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa3_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(2);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa3_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(2);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active2`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa3_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(2);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa2_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(1);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa2_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(1);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active2`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa2_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(1);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		long l = 1L << curChar;
		do {
			switch (jjstateSet[--i]) {
			case 0:
				if ((0x3ff000000000000L & l) != 0L) {
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				if ((0x3ff000000000000L & l) != 0L) {
					if (kind > 151) {
						kind = 151;
					}
					{
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				} else if ((0x100003600L & l) != 0L) {
					if (kind > 2) {
						kind = 2;
					}
				} else if (curChar == 46) {
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				} else if (curChar == 58) {
					if (kind > 145) {
						kind = 145;
					}
					{
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				} else if (curChar == 35) {
					if (kind > 3) {
						kind = 3;
					}
					{
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 3) {
					kind = 3;
				} {
				jjCheckNAdd(2);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 3) {
					kind = 3;
				} {
				jjCheckNAdd(2);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 5:
				if (curChar == 41 && kind > 30) {
					kind = 30;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 14:
				if (curChar == 62 && kind > 144) {
					kind = 144;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 147) {
					kind = 147;
				} {
				jjCheckNAddTwoStates(17, 18);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 18:
				if ((0x3ff200000000000L & l) != 0L && kind > 147) {
					kind = 147;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 148) {
					kind = 148;
				} {
				jjCheckNAdd(22);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 149) {
					kind = 149;
				} {
				jjCheckNAdd(25);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 150) {
					kind = 150;
				} {
				jjCheckNAddTwoStates(28, 29);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 34:
				if (curChar == 39 && kind > 164) {
					kind = 164;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 39:
				if (curChar == 34 && kind > 165) {
					kind = 165;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 47:
				if (curChar == 39 && kind > 166) {
					kind = 166;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 58:
				if (curChar == 34 && kind > 167) {
					kind = 167;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 74:
				if (curChar == 58 && kind > 145) {
					kind = 145;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 146) {
					kind = 146;
				} {
				jjCheckNAddStates(44, 47);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 80:
				if ((0x7ff200000000000L & l) != 0L && kind > 146) {
					kind = 146;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 87:
				if ((0x3ff000000000000L & l) != 0L && kind > 146) {
					kind = 146;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 88:
				if ((0xa800ff7e00000000L & l) != 0L && kind > 146) {
					kind = 146;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 146) {
					kind = 146;
				} {
				jjCheckNAddStates(44, 47);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 146) {
					kind = 146;
				} {
				jjCheckNAddStates(44, 47);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 145) {
					kind = 145;
				} {
				jjCheckNAddStates(17, 19);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 151) {
					kind = 151;
				} {
				jjCheckNAddStates(0, 6);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 151) {
					kind = 151;
				} {
				jjCheckNAdd(96);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 154) {
					kind = 154;
				} {
				jjCheckNAdd(99);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 157) {
					kind = 157;
				} {
				jjCheckNAdd(105);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 157) {
					kind = 157;
				} {
				jjCheckNAdd(109);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 152) {
					kind = 152;
				} {
				jjCheckNAdd(111);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 155) {
					kind = 155;
				} {
				jjCheckNAdd(114);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 162) {
					kind = 162;
				} {
				jjCheckNAdd(119);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 162) {
					kind = 162;
				} {
				jjCheckNAdd(126);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 162) {
					kind = 162;
				} {
				jjCheckNAdd(130);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 153) {
					kind = 153;
				} {
				jjCheckNAdd(132);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 156) {
					kind = 156;
				} {
				jjCheckNAdd(135);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 163) {
					kind = 163;
				} {
				jjCheckNAdd(140);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 163) {
					kind = 163;
				} {
				jjCheckNAdd(147);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 163) {
					kind = 163;
				} {
				jjCheckNAdd(151);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 157) {
					kind = 157;
				} {
				jjCheckNAdd(156);
```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa9_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(8);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa9_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(8);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa4_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(3);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa4_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(3);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active2`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa4_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(3);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa6_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(5);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa6_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(5);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active2`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa6_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(5);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa5_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(4);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa5_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(4);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active2`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa5_0(long old0, long active0, long old1, long active1, long old2, long active2) {
		if (((active0 &= old0) | (active1 &= old1) | (active2 &= old2)) == 0L) {
			return jjMoveNfa_0(4);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `start`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		do {
			jjstateSet[jjnewStateCnt++] = jjnextStates[start];
		} while (start++ != end);
	}

```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa7_0(long old0, long active0, long old1, long active1, long old2) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(6);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa7_0(long old0, long active0, long old1, long active1, long old2) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(6);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		long l2 = 1L << (curChar & 077);
		do {
			switch (jjstateSet[--i]) {
			case 0:
				if (jjCanMove_1(hiByte, i1, i2, l1, l2)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 3) {
					kind = 3;
				}
				jjstateSet[jjnewStateCnt++] = 2;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 147) {
					kind = 147;
				} {
				jjCheckNAddTwoStates(17, 18);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 18:
				if (jjCanMove_2(hiByte, i1, i2, l1, l2) && kind > 147) {
					kind = 147;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 148) {
					kind = 148;
				} {
				jjCheckNAdd(22);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 148) {
					kind = 148;
				} {
				jjCheckNAdd(22);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 149) {
					kind = 149;
				} {
				jjCheckNAdd(25);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 149) {
					kind = 149;
				} {
				jjCheckNAdd(25);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 146) {
					kind = 146;
				} {
				jjCheckNAddStates(44, 47);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 80:
				if (jjCanMove_2(hiByte, i1, i2, l1, l2) && kind > 146) {
					kind = 146;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa12_0(long old1, long active1) {
		if (((active1 &= old1)) == 0L) {
			return jjMoveNfa_0(11);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		long l = 1L << (curChar & 077);
		do {
			switch (jjstateSet[--i]) {
			case 0:
				if ((0x7fffffe07fffffeL & l) != 0L) {
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 2:
				if (kind > 3) {
					kind = 3;
				}
				jjstateSet[jjnewStateCnt++] = 2;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 8:
				if (curChar == 93 && kind > 31) {
					kind = 31;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 9:
				if ((0x20000000200L & l) != 0L && kind > 76) {
					kind = 76;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 147) {
					kind = 147;
				} {
				jjCheckNAddTwoStates(17, 18);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 18:
				if ((0x7fffffe87fffffeL & l) != 0L && kind > 147) {
					kind = 147;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 148) {
					kind = 148;
				} {
				jjCheckNAdd(22);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 149) {
					kind = 149;
				} {
				jjCheckNAdd(25);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 150) {
					kind = 150;
				} {
				jjCheckNAddTwoStates(27, 28);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 150) {
					kind = 150;
				} {
				jjCheckNAddTwoStates(28, 29);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 63:
				if ((0x20000000200L & l) != 0L && kind > 66) {
					kind = 66;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 146) {
					kind = 146;
				} {
				jjCheckNAddStates(44, 47);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 80:
				if ((0x7fffffe87fffffeL & l) != 0L && kind > 146) {
					kind = 146;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 87:
				if ((0x7e0000007eL & l) != 0L && kind > 146) {
					kind = 146;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			case 88:
				if ((0x4000000080000001L & l) != 0L && kind > 146) {
					kind = 146;
				}
				break;
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 146) {
					kind = 146;
				} {
				jjCheckNAddStates(44, 47);
```

### AssignmentToMethodParameter
Assignment to method parameter `kind`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				}
				if (kind > 146) {
					kind = 146;
				} {
				jjCheckNAddStates(44, 47);
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa13_0(long old1, long active1) {
		if (((active1 &= old1)) == 0L) {
			return jjMoveNfa_0(12);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `start`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		do {
			jjCheckNAdd(jjnextStates[start]);
		} while (start++ != end);
	}

```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa11_0(long old0, long old1, long active1) {
		if (((active1 &= old1)) == 0L) {
			return jjMoveNfa_0(10);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active0`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa8_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(7);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `active1`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveStringLiteralDfa8_0(long old0, long active0, long old1, long active1) {
		if (((active0 &= old0) | (active1 &= old1)) == 0L) {
			return jjMoveNfa_0(7);
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `newLine`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

		if (i < len) {
			bufline[j] = newLine++;
			bufcolumn[j] = newCol + columnDiff;

```

### AssignmentToMethodParameter
Assignment to method parameter `newLine`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
			while (i++ < len) {
				if (bufline[j = start % bufsize] != bufline[++start % bufsize]) {
					bufline[j] = newLine++;
				} else {
					bufline[j] = newLine;
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/AbstractQueryResultParser.java`
#### Snippet
```java
	public QueryResultParser setQueryResultHandler(QueryResultHandler handler) {
		if (getParserConfig().get(BasicParserSettings.PROCESS_ENCODED_RDF_STAR)) {
			handler = new RDFStarDecodingQueryResultHandler(handler);
		}
		this.handler = handler;
```

### AssignmentToMethodParameter
Assignment to method parameter `data`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java

			Node childNode = node.jjtGetChild(i);
			data = childNode.jjtAccept(this, data);

			if (childNode instanceof ASTOptionalGraphPattern) {
```

### AssignmentToMethodParameter
Assignment to method parameter `varName`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
						}

						varName = element.getName();
						break;
					}
```

### AssignmentToMethodParameter
Assignment to method parameter `group`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
			// create an implicit group
			if (group == null) {
				group = new Group(tupleExpr);
			}

```

### AssignmentToMethodParameter
Assignment to method parameter `tupleExpr`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java

			extension.setArg(group);
			tupleExpr = new Filter(extension, expr);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `tupleExpr`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
				if (!extension.getElements().isEmpty()) {
					extension.setArg(tupleExpr);
					tupleExpr = extension;
				}
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `tupleExpr`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
			}

			tupleExpr = new Order(tupleExpr, orderElements);

		}
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	private static String _trimString(String s, int n) {
		if (s.length() >= 2 * n) {
			s = s.substring(n, s.length() - n);
		}
		return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `contexts`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeSailStore.java`
#### Snippet
```java

				if (contexts.length == 0) {
					contexts = new Resource[] { null };
				}

```

### AssignmentToMethodParameter
Assignment to method parameter `indexSpecStr`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
			if (indexSpecs.isEmpty()) {
				logger.debug("No indexes specified, using default indexes: {}", DEFAULT_INDEXES);
				indexSpecStr = DEFAULT_INDEXES;
				indexSpecs = parseIndexSpecList(indexSpecStr);
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `indexSpecStr`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
			if (reqIndexSpecs.isEmpty()) {
				// No indexes specified, use the existing ones
				indexSpecStr = properties.getProperty(INDEXES_KEY);
			} else if (!reqIndexSpecs.equals(indexSpecs)) {
				// Set of indexes needs to be changed
```

### AssignmentToMethodParameter
Assignment to method parameter `bucketOffset`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/HashFile.java`
#### Snippet
```java

				// Continue searching for an empty slot in the overflow bucket
				bucketOffset = getOverflowBucketOffset(overflowID);
				bucket.clear();
			}
```

### AssignmentToMethodParameter
Assignment to method parameter `optionalVars`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/LeftJoinQueryEvaluationStep.java`
#### Snippet
```java
		if (!leftBindingNames.isEmpty()) {
			if (leftBindingNames.containsAll(optionalVars)) {
				optionalVars = Set.of();
			} else {
				for (String leftBindingName : leftBindingNames) {
```

### AssignmentToMethodParameter
Assignment to method parameter `optionalVars`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/LeftJoinQueryEvaluationStep.java`
#### Snippet
```java
				for (String leftBindingName : leftBindingNames) {
					if (optionalVars.contains(leftBindingName)) {
						optionalVars = new HashSet<>(optionalVars);
						optionalVars.removeAll(leftBindingNames);
						break;
```

### AssignmentToMethodParameter
Assignment to method parameter `bindings`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/ServiceQueryEvaluationStep.java`
#### Snippet
```java
				allBindings.setBinding(boundVar.getName(), boundVar.getValue());
			}
			bindings = allBindings;

			String baseUri = service.getBaseURI();
```

### AssignmentToMethodParameter
Assignment to method parameter `filter`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
			boolean subEqConVar = subjVar.equals(conVar);
			if (subEqPredVar || subEqObjVar || subEqConVar) {
				filter = andThen(filter, subjectVariableHasEquals(subEqPredVar, subEqObjVar, subEqConVar));
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `filter`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
			boolean predEqConVar = predVar.equals(conVar);
			if (predEqObjVar || predEqConVar) {
				filter = andThen(filter, predicateVariableHasEquals(predEqObjVar, predEqConVar));
			}
		}
```

### AssignmentToMethodParameter
Assignment to method parameter `filter`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
			boolean objEqConVar = objVar.equals(conVar);
			if (objEqConVar) {
				filter = andThen(filter, (st) -> {
					Value obj = st.getObject();
					Resource context = st.getContext();
```

### AssignmentToMethodParameter
Assignment to method parameter `ancestor`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ProjectionIterator.java`
#### Snippet
```java
	private boolean determineOuterProjection(QueryModelNode ancestor) {
		while (ancestor.getParentNode() != null) {
			ancestor = ancestor.getParentNode();
			if (ancestor instanceof Projection || ancestor instanceof MultiProjection) {
				return false;
```

### AssignmentToMethodParameter
Assignment to method parameter `bindingNames`
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultWriter.java`
#### Snippet
```java

		// Copy supplied column headers list and make it unmodifiable
		bindingNames = new ArrayList<>(bindingNames);
		this.bindingNames = Collections.unmodifiableList(bindingNames);

```

### AssignmentToMethodParameter
Assignment to method parameter `highlightedValue`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/SearchFields.java`
#### Snippet
```java
				separator = "...";
			}
			highlightedValue = buf.toString();
		}
		return highlightedValue;
```

### AssignmentToMethodParameter
Assignment to method parameter `tupleExpr`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
			Dataset dataset, BindingSet bindings, boolean includeInferred) throws SailException {
		// Don't modify the original tuple expression
		tupleExpr = tupleExpr.clone();

		if (!(tupleExpr instanceof QueryRoot)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `tupleExpr`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
			// Add a dummy root node to the tuple expressions to allow the
			// optimizers to modify the actual root node
			tupleExpr = new QueryRoot(tupleExpr);
		}

```

### AssignmentToMethodParameter
Assignment to method parameter `statement`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
			// we only consider statements that contain literals or type declaration
			if (statement.getObject() instanceof Literal) {
				statement = sail.mapStatement(statement);
				if (statement == null) {
					return;
```

### AssignmentToMethodParameter
Assignment to method parameter `statement`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
			// we only consider statements that contain literals or type declaration
			if (statement.getObject() instanceof Literal) {
				statement = sail.mapStatement(statement);
				if (statement == null) {
					return;
```

### AssignmentToMethodParameter
Assignment to method parameter `theString`
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/RenderUtils.java`
#### Snippet
```java
	 */
	public static String escape(String theString) {
		theString = theString.replaceAll("\"", "\\\\\"");

		StringBuffer aBuffer = new StringBuffer();
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `m`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/ModelCollector.java`
#### Snippet
```java
	public BiConsumer<Model, Statement> accumulator() {
		return (m, s) -> {
			synchronized (m) {
				m.add(s);
			}
```

## RuleId[id=ReturnNull]
### ReturnNull
Return of `null`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/config/BaseSailConfig.java`
#### Snippet
```java
	public EvaluationStrategyFactory getEvaluationStrategyFactory() throws SailConfigException {
		if (evalStratFactoryClassName == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/rio/binary/src/main/java/org/eclipse/rdf4j/rio/binary/BinaryRDFParser.java`
#### Snippet
```java

		reportFatalError("Invalid RDF-star triple value");
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/binary/src/main/java/org/eclipse/rdf4j/rio/binary/BinaryRDFParser.java`
#### Snippet
```java
		switch (valueType) {
		case NULL_VALUE:
			return null;
		case VALUE_REF:
			return readValueRef();
```

### ReturnNull
Return of `null`
in `core/rio/binary/src/main/java/org/eclipse/rdf4j/rio/binary/BinaryRDFParser.java`
#### Snippet
```java
		default:
			reportFatalError("Unknown value type: " + valueType);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
		Set<String> removedPrefixes = changes.getRemovedPrefixes();
		if (removedPrefixes != null && removedPrefixes.contains(prefix) || changes.isNamespaceCleared()) {
			return null;
		}
		return derivedFrom.getNamespace(prefix);
```

### ReturnNull
Return of `null`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLRepository.java`
#### Snippet
```java
	@Override
	public File getDataDir() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/InsertBindingSetCursor.java`
#### Snippet
```java
		BindingSet next = super.next();
		if (next == null) {
			return null;
		}
		int size = bindings.size() + next.size();
```

### ReturnNull
Return of `null`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLQueryBindingSet.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLCrossProductIteration.java`
#### Snippet
```java
				currentInputBinding = resultIteration.next();
			} else {
				return null; // no more results
			}
		}
```

### ReturnNull
Return of `null`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLCrossProductIteration.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
		try {

			return observed == null ? null : Collections.unmodifiableSet(observed);
		} finally {
			readWriteLock.unlockReader(readLock);
```

### ReturnNull
Return of `null`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
		try {
			if (observed == null) {
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
		assert !closed;
		if (set == null) {
			return null;
		}
		return new HashSet<>(set);
```

### ReturnNull
Return of `null`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
		assert !closed;
		if (deprecatedContexts == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
	@Override
	public String getNamespace(String prefix) throws RepositoryException {
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java
					.map(g -> {
						if (g.equals(RDF4J.NIL)) {
							return null;
						}
						return g;
```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/paths/Path.java`
#### Snippet
```java
	static public Path buildPath(ShapeSource shapeSource, Resource id) {
		if (id == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
						.map(callable -> {
							if (Thread.currentThread().isInterrupted()) {
								return null;
							}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
						}

						return null;

					})
```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
					.map(callable -> {
						if (Thread.currentThread().isInterrupted()) {
							return null;
						}
						if (parallelValidation) {
```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
		shapesGraphs = sail.getShapesGraphs().stream().map(g -> {
			if (g.equals(RDF4J.NIL)) {
				return null;
			}
			return g;
```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SingletonBindingSet.java`
#### Snippet
```java
			return cachedBinding;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SingletonBindingSet.java`
#### Snippet
```java
			return value;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PlanNodeHelper.java`
#### Snippet
```java
	public static Dataset asDefaultGraphDataset(Resource[] dataGraph) {
		if (dataGraph.length == 0) {
			return null;
		} else if (dataGraph.length == 1 && dataGraph[0] == null) {
			return rdf4jNilDataset;
```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SimpleBindingSet.java`
#### Snippet
```java
			return binding.getValue();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SimpleBindingSet.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
	@Override
	public String getId() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java

				}
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
				}

				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTuple.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/shape/ShapeSource.java`
#### Snippet
```java
		private static Resource handleDefaultGraph(Resource graph) {
			if (RDF4J.NIL.equals(graph)) {
				return null;
			}
			return graph;
```

### ReturnNull
Return of `null`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchNamespaceStore.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/config/ElasticsearchStoreFactory.java`
#### Snippet
```java
		}

		return null;
	}
}
```

### ReturnNull
Return of `null`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	public static Resource decodeContext(String encodedValue, ValueFactory valueFactory) {
		if (encodedValue == null) {
			return null;
		} else if (NULL_PARAM_VALUE.equals(encodedValue)) {
			return null;
```

### ReturnNull
Return of `null`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
			return null;
		} else if (NULL_PARAM_VALUE.equals(encodedValue)) {
			return null;
		} else {
			Resource context = decodeResource(encodedValue, valueFactory);
```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinFunctionParser.java`
#### Snippet
```java
		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
		}
		ParsedQuery query = parser.parseQuery((Resource) body, store);
```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionParser.java`
#### Snippet
```java
		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
		}
		ParsedQuery query = parser.parseQuery((Resource) body, store);
```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
		Statement magicPropStmt = TripleSources.single(funcUri, RDF.TYPE, SPIN.MAGIC_PROPERTY_CLASS, store);
		if (magicPropStmt == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
		Value body = TripleSources.singleValue(funcUri, SPIN.BODY_PROPERTY, store);
		if (!(body instanceof Resource)) {
			return null;
		}
		final TupleFunction tupleFunc = parser.parseMagicProperty(funcUri, store);
```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunctionAsFunctionParser.java`
#### Snippet
```java
							return iter.next().get(0);
						} else {
							return null;
						}
					}
```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinxFunctionParser.java`
#### Snippet
```java
		String file = (fileValue instanceof Literal) ? ((Literal) fileValue).getLabel() : null;
		if (code == null && file == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
			}
			close();
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
				E(mdb_stat(txn, dbiExplicit, stat));
				pageSize = stat.ms_psize();
				return null;
			});

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbStatementIterator.java`
#### Snippet
```java
			long[] quad = recordIt.next();
			if (quad == null) {
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbEvaluationStatistics.java`
#### Snippet
```java

		protected Value getConstantValue(Var var) {
			return (var != null) ? var.getValue() : null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbRecordIterator.java`
#### Snippet
```java
					if (lastResult != 0) {
						close();
						return null;
					}
				}
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbRecordIterator.java`
#### Snippet
```java
			}
			close();
			return null;
		} finally {
			txnLock.unlockRead(stamp);
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
				mapSize = configMapSize;
			}
			return null;
		});
	}
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
			return value;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
				return valueBytes;
			}
			return null;
		});
	}
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
			if (datatypeID == LmdbValue.UNKNOWN_ID) {
				// Unknown datatype means unknown literal
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
				}
				if (!create) {
					return null;
				}
				// id was not found, create a new one
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
					E(mdb_put(writeTxn, dbi, dataVal, idVal, 0));
					E(mdb_put(writeTxn, dbi, idVal, dataVal, 0));
					return null;
				});
				return newId;
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
					// no value for hash exists
					if (!create) {
						return null;
					}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
						E(mdb_put(writeTxn, dbi, idVal, dataVal, MDB_RESERVE));
						dataVal.mv_data().put(data);
						return null;
					});
					return newId;
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java

				if (!create) {
					return null;
				}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
					E(mdb_put(txn, dbi, idVal, dataVal, MDB_RESERVE));
					dataVal.mv_data().put(data);
					return null;
				});
				return newId;
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
		if (nsID == -1) {
			// Unknown namespace means unknown URI
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
					nextId = 1;
				}
				return null;
			} finally {
				if (cursor != 0) {
```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/RioFileTypeDetector.java`
#### Snippet
```java
		// Specification says to return null if we could not
		// conclusively determine the file type
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/ClassRioSetting.java`
#### Snippet
```java
	public T convert(String stringValue) {
		if (stringValue == null || stringValue.isEmpty()) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
					}

					return null;
				});
			}
```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
				}
			}
			return null;
		});

```

### ReturnNull
Return of `null`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
				mapSize = configMapSize;
			}
			return null;
		});
	}
```

### ReturnNull
Return of `null`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxComplianceTest.java`
#### Snippet
```java
	@Override
	protected Repository getDataRepository() {
		return null; // not needed in syntax tests
	}

```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
				parsedSkolemOrigin = null;
			}
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
		} catch (Exception e) {
			reportFatalError(e);
			return null; // required by compiler
		}
	}
```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
		} catch (Exception e) {
			reportFatalError(e);
			return null; // required by compiler
		}
	}
```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
		} catch (Exception e) {
			reportFatalError(e);
			return null; // required by compiler
		}
	}
```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
		} catch (Exception e) {
			reportFatalError(e);
			return null; // required by compiler
		}
	}
```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
			} catch (URISyntaxException e) {
				reportError(e.getMessage(), BasicParserSettings.VERIFY_URI_SYNTAX);
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
		} catch (Exception e) {
			reportFatalError(e);
			return null; // required by compiler
		}
	}
```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				return MathOp.DIVIDE;
			} else {
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
				return CompareOp.GT;
			} else {
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			}
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/RDF4JCRUDDao.java`
#### Snippet
```java
	protected IRI convertIdToIri(ID id) {
		if (id == null) {
			return null;
		}
		if (idClass.equals(IRI.class)) {
```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/OperationUtils.java`
#### Snippet
```java
								"Expected zero or one result, found " + size, 1, size);
					} else if (size == 0) {
						return null;
					}
					return list.get(0);
```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/TypeMappingUtils.java`
#### Snippet
```java

	public static Boolean toBooleanMaybe(Value from) {
		return (from == null) ? null : toBoolean(from);
	}

```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/TypeMappingUtils.java`
#### Snippet
```java

	public static Boolean toBooleanMaybe(String from) {
		return (from == null) ? null : toBoolean(from);
	}

```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/LRUResultCache.java`
#### Snippet
```java
			clearCachedResults();
			debug("returning null");
			return null;
		}
		if (isBypass()) {
```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/LRUResultCache.java`
#### Snippet
```java
		if (isBypass()) {
			debug("bypassing cache, returning null");
			return null;
		}
		Entry<T> entry = cache.get(key);
```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/LRUResultCache.java`
#### Snippet
```java
		if (entry == null) {
			debug("nothing found in cache, returning null");
			return null;
		}
		if (entry.isExpired()) {
```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/LRUResultCache.java`
#### Snippet
```java
			cache.remove(key);
			debug("cached object is expired, returning null");
			return null;
		}
		debug("returning cached object");
```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ResultCachingTupleQuery.java`
#### Snippet
```java
			return result.recreateTupleQueryResult();
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ResultCachingGraphQuery.java`
#### Snippet
```java
			return result.recreateGraphQueryResult();
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `core/rio/n3/src/main/java/org/eclipse/rdf4j/rio/n3/N3Parser.java`
#### Snippet
```java
		} else {
			reportFatalError("Illegal predicate value: " + predicate);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneDocumentScore.java`
#### Snippet
```java
		List<String> values = getDocument().getProperty(field);
		if (values == null) {
			return null;
		}
		return Iterables.transform(values, (String text) -> index.getSnippet(field, text, highlighter));
```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneDocumentScore.java`
#### Snippet
```java

	private static Set<String> requiredFields(boolean all) {
		return all ? null : Collections.singleton(SearchFields.URI_FIELD_NAME);
	}

```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemEvaluationStatistics.java`
#### Snippet
```java
			}

			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementIterator.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractLiteral.java`
#### Snippet
```java
					.map(normalized -> normalized.equals("true") || normalized.equals("1") ? Boolean.TRUE
							: normalized.equals("false") || normalized.equals("0") ? Boolean.FALSE
									: null
					)

```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemTripleIterator.java`
#### Snippet
```java

		// No more matching statements.
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/WeakObjectRegistry.java`
#### Snippet
```java
	public E get(K key) {
		if (key == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/WeakObjectRegistry.java`
#### Snippet
```java
				return weakRef.get(); // may be null
			} else {
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementList.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		}
		// no such Document
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
	protected synchronized SearchDocument getDocument(String id) throws IOException {
		Document document = getDocument(idTerm(id));
		return (document != null) ? new LuceneDocument(document, geoStrategyMapper) : null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
			return SpatialOperation.Contains;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		SpatialOperation op = toSpatialOp(relation);
		if (op == null) {
			return null;
		}
		final String geoField = SearchFields.getPropertyField(geoProperty);
```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		} catch (CorruptIndexException e) {
			logger.error("The index seems to be corrupted:", e);
			return null;
		} catch (IOException e) {
			logger.error("Could not read from index:", e);
```

### ReturnNull
Return of `null`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		} catch (IOException e) {
			logger.error("Could not read from index:", e);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemValueFactory.java`
#### Snippet
```java
	public MemIRI getMemURI(IRI uri) {
		if (uri == null) {
			return null;
		} else if (isOwnMemIRI(uri)) {
			return (MemIRI) uri;
```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemValueFactory.java`
#### Snippet
```java
	public MemResource getMemResource(Resource resource) {
		if (resource == null) {
			return null;
		} else if (resource.isIRI()) {
			return getMemURI((IRI) resource);
```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemValueFactory.java`
#### Snippet
```java
	public MemValue getMemValue(Value value) {
		if (value == null) {
			return null;
		} else if (value.isIRI()) {
			return getMemURI((IRI) value);
```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemValueFactory.java`
#### Snippet
```java
	private MemTriple getMemTriple(Triple triple) {
		if (triple == null) {
			return null;
		} else if (isOwnMemTriple(triple)) {
			return (MemTriple) triple;
```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemValueFactory.java`
#### Snippet
```java
	public MemLiteral getMemLiteral(Literal literal) {
		if (literal == null) {
			return null;
		} else if (isOwnMemLiteral(literal)) {
			return (MemLiteral) literal;
```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemValueFactory.java`
#### Snippet
```java
	public MemBNode getMemBNode(BNode bnode) {
		if (bnode == null) {
			return null;
		} else if (isOwnMemBnode(bnode)) {
			return (MemBNode) bnode;
```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ResourceUtil.java`
#### Snippet
```java
		try (InputStream in = ResourceUtil.getInputStream(resourceName)) {
			if (in == null) {
				return null;
			}
			return IOUtil.readString(in);
```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ResourceUtil.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java

				if (statementAlreadyExists(explicit, memSubj, memPred, memObj, memContext, nextSnapshot)) {
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/FileFormat.java`
#### Snippet
```java
	public String getDefaultFileExtension() {
		if (fileExtensions.isEmpty()) {
			return null;
		} else {
			return fileExtensions.get(0);
```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
	private String pathSegmentNormalization(String _path) {
		if (_path == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		_path = _path.replace("/./", "/");
		if (_path == null) {
			return null;
		}
		if (_path.startsWith("./")) {
```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		} else {
			pos = start; // reset
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
	private String toUpperCase(String string) {
		if (string == null) {
			return null;
		}
		boolean changed = false;
```

### ReturnNull
Return of `null`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
	private String toLowerCase(String string) {
		if (string == null) {
			return null;
		}
		boolean changed = false;
```

### ReturnNull
Return of `null`
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java
			return "IsWithin";
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java
		String spatialOp = toSpatialOp(relation);
		if (spatialOp == null) {
			return null;
		}
		String qstr = "\"" + spatialOp + "(" + wkt + ")\"";
```

### ReturnNull
Return of `null`
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java
			throw new IOException(e);
		}
		return (doc != null) ? new SolrSearchDocument(doc) : null;
	}

```

### ReturnNull
Return of `null`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
			} else {
				EntityUtils.consume(response.getEntity());
				return null;
			}
		} catch (RepositoryException e) {
```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Namespaces.java`
#### Snippet
```java
					}
				}
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/AbstractModel.java`
#### Snippet
```java
					}
				}
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
		public Resource getContext() {
			if (ctx == null) {
				return null;
			}
			return ctx.getValue();
```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
				return last;
			} catch (NoSuchElementException e) {
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
				return first;
			} catch (NoSuchElementException e) {
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
			return first;
		} catch (NoSuchElementException e) {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
			return last;
		} catch (NoSuchElementException e) {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/ModelBuilder.java`
#### Snippet
```java
	private IRI convertPrefixedName(String prefixedName) {
		if (prefixedName.indexOf(':') < 0) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/ModelBuilder.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
		if (stmtBySubject.isEmpty() && blanks.isEmpty()) {
			assert queueSize == 0;
			return null;
		}
		Set<Statement> stmts = null;
```

### ReturnNull
Return of `null`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
				: firstMatch;
		if (matches.isEmpty()) {
			return null;
		}
		Set<Statement> set = stmtBySubject.get(key);
```

### ReturnNull
Return of `null`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		} else if (c == -1) {
			throwEOFException();
			return null;
		} else {
			reportFatalError("Expected an RDF value here, found '" + new String(Character.toChars(c)) + "'");
```

### ReturnNull
Return of `null`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		} else {
			reportFatalError("Expected an RDF value here, found '" + new String(Character.toChars(c)) + "'");
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
				// skip otherwise.
				reportError("Invalid datatype IRI for literal '" + label + "'", BasicParserSettings.VERIFY_URI_SYNTAX);
				return null;
			} else if (!(datatype instanceof IRI)) {
				reportFatalError("Illegal datatype value: " + datatype);
```

### ReturnNull
Return of `null`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		} else {
			reportFatalError("Illegal predicate value: " + predicate);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVReader.java`
#### Snippet
```java
		String line = getNextLine();
		if (line == null) {
			return null;
		}
		String[] fields = line.split("\t", -1);
```

### ReturnNull
Return of `null`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/util/SparqlBuilderUtils.java`
#### Snippet
```java
	public static String getEscapedString(String value) {
		if (value == null) {
			return null;
		}
		return value
```

### ReturnNull
Return of `null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleStatement.java`
#### Snippet
```java
	@Override
	public Resource getContext() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/UnionIterator.java`
#### Snippet
```java
		} else {
			// All elements have been returned
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/QueueIteration.java`
#### Snippet
```java
	public E getNextElement() throws T {
		if (isClosed()) {
			return null;
		}
		try {
```

### ReturnNull
Return of `null`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/QueueIteration.java`
#### Snippet
```java
				done(); // put afterLast back for others
				checkException();
				return null;
			}
			checkException();
```

### ReturnNull
Return of `null`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/QueueIteration.java`
#### Snippet
```java
				}
			}
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
	protected E getNextElement() throws X {
		if (isClosed()) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
			} else {
				// All elements have been returned
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/AbstractRepositoryImplConfig.java`
#### Snippet
```java
			}

			return null;
		} catch (ModelException e) {
			throw new RepositoryConfigException(e.getMessage(), e);
```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigUtil.java`
#### Snippet
```java
		if (idStatement == null) {
			// No such config
			return null;
		}
		Resource repositoryNode = idStatement.getSubject();
```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigUtil.java`
#### Snippet
```java
			if (idStatement == null) {
				// No such config
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigUtil.java`
#### Snippet
```java
		if (idStatement == null) {
			// No such config
			return null;
		}
		return model.filter(null, null, null, idStatement.getContext());
```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigUtil.java`
#### Snippet
```java
			return idStatementList.get(0);
		} else if (idStatementList.isEmpty()) {
			return null;
		} else {
			throw new RepositoryConfigException("Multiple ID-statements for repository ID " + repositoryID);
```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigUtil.java`
#### Snippet
```java
			return idStatementList.iterator().next();
		} else if (idStatementList.isEmpty()) {
			return null;
		} else {
			throw new RepositoryConfigException("Multiple ID-statements for repository ID " + repositoryID);
```

### ReturnNull
Return of `null`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/FilteringIteration.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
						return iterator.next();
					}
					return null;
				}
			};
```

### ReturnNull
Return of `null`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailSource.java`
#### Snippet
```java
						}

						return null;

					}
```

### ReturnNull
Return of `null`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RemoteRepositoryManager.java`
#### Snippet
```java
		SharedHttpClientSessionManager nextClient = client;
		if (nextClient == null) {
			return null;
		} else {
			return nextClient.getHttpClient();
```

### ReturnNull
Return of `null`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/util/TypeFilteringRepositoryManager.java`
#### Snippet
```java
			}

			return null;
		} catch (RepositoryConfigException e) {
			throw new RepositoryException(e.getMessage(), e);
```

### ReturnNull
Return of `null`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
		RepositoryConfig config = getRepositoryConfig(id);
		if (config == null) {
			return null;
		}
		RepositoryInfo repInfo = new RepositoryInfo();
```

### ReturnNull
Return of `null`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
		SharedHttpClientSessionManager nextClient = client;
		if (nextClient == null) {
			return null;
		} else {
			return nextClient.getHttpClient();
```

### ReturnNull
Return of `null`
in `core/common/text/src/main/java/org/eclipse/rdf4j/common/text/StringUtil.java`
#### Snippet
```java
		}
		if (text == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
		} catch (RepositoryException e) {
			exceptionHandler.accept(e);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
		} catch (RepositoryException e) {
			exceptionHandler.accept(e);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
		get(repository, conn -> {
			processFunction.accept(conn);
			return null;
		});
	}
```

### ReturnNull
Return of `null`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
		getNoTransaction(repository, conn -> {
			processFunction.accept(conn);
			return null;
		});
	}
```

### ReturnNull
Return of `null`
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
				false)) {
			if (!stmts.hasNext()) {
				return null;
			}
			Value obj = stmts.next().getObject();
```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/SailConfigUtil.java`
#### Snippet
```java
			}

			return null;
		} catch (ModelException e) {
			throw new SailConfigException(e.getMessage(), e);
```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/SailUtil.java`
#### Snippet
```java
	public static <C extends Sail> C findSailInStack(Sail topSail, Class<C> sailClass) {
		if (sailClass == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/DirectoryLockManager.java`
#### Snippet
```java

		if (!lockDir.mkdir()) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/DirectoryLockManager.java`
#### Snippet
```java
		} catch (IOException e) {
			logger.warn(e.toString(), e);
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/AbstractReadWriteLockManager.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/AbstractReadWriteLockManager.java`
#### Snippet
```java
			readLockMonitoring.runCleanup();
			writeLockMonitoring.runCleanup();
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/ExclusiveLockManager.java`
#### Snippet
```java
		} else {
			lockMonitoring.runCleanup();
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryLanguage.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockCleaner.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/StampedLockManager.java`
#### Snippet
```java
			return new OptimisticReadLock(stampedLock, optimisticReadStamp);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/StampedLockManager.java`
#### Snippet
```java
			return new ReadLock(stampedLock, stamp);
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/StampedLockManager.java`
#### Snippet
```java
			return new WriteLock(stampedLock, stamp);
		} else {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockTracking.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/ListBindingSet.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/ListBindingSet.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/MapBindingSet.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/EmptyBindingSet.java`
#### Snippet
```java
	@Override
	public Value getValue(String bindingName) {
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/EmptyBindingSet.java`
#### Snippet
```java
	@Override
	public Binding getBinding(String bindingName) {
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java

		if (totalTimeActual == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java

	public List<GenericPlanNode> getPlans() {
		return plans.isEmpty() ? null : plans; // for simplified json
	}

```

### ReturnNull
Return of `null`
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
				false)) {
			if (!stmts.hasNext()) {
				return null;
			}
			Value obj = stmts.next().getObject();
```

### ReturnNull
Return of `null`
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/Atts.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/Atts.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/Atts.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/Atts.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/WildcardProjectionProcessor.java`
#### Snippet
```java
			if (alias != null) {
				variableNames.add(alias);
				return null;
			} else {
				return super.visit(node, data);
```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/WildcardProjectionProcessor.java`
#### Snippet
```java
		public Object visit(ASTConstraint node, Object data) throws VisitorException {
			// ignore variables in filter expressions
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/WildcardProjectionProcessor.java`
#### Snippet
```java
			if (alias != null) {
				variableNames.add(alias);
				return null;
			} else {
				return super.visit(node, data);
```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/PrefixDeclProcessor.java`
#### Snippet
```java
			qnameNode.jjtReplaceWith(iriNode);

			return null;
		}

```

### ReturnNull
Return of `null`
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLUpdateDataBlockParser.java`
#### Snippet
```java
		// TODO for now, we do not implement this as a fully compatible Rio
		// parser, and we're not introducing a new RDFFormat constant.
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/UpdateExprBuilder.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/BlankNodeVarProcessor.java`
#### Snippet
```java
		private String findVarName(String bnodeID) throws VisitorException {
			if (bnodeID == null) {
				return null;
			}
			String varName = conversionMap.get(bnodeID);
```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTPropertyListPath.java`
#### Snippet
```java
			return (ASTPropertyListPath) children[2];
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/Token.java`
#### Snippet
```java
	 */
	public Object getValue() {
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SimpleNode.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTPropertyList.java`
#### Snippet
```java
			return (ASTPropertyList) children[2];
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTRDFLiteral.java`
#### Snippet
```java
			return (ASTIRI) children[1];
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTModify.java`
#### Snippet
```java
			return (ASTDatasetClause) jjtGetChild(0);
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTProjectionElem.java`
#### Snippet
```java
		}

		return null;
	}
}
```

### ReturnNull
Return of `null`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepository.java`
#### Snippet
```java
			return findSailOf(((StackableSail) sail).getBaseSail(), type);
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepository.java`
#### Snippet
```java
			return stack.getHttpClientSessionManager();
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepository.java`
#### Snippet
```java
			return stack.getHttpClient();
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepositoryConnection.java`
#### Snippet
```java
			return ((HttpClientDependent) sailConnection).getHttpClient();
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepositoryConnection.java`
#### Snippet
```java
			return ((SessionManagerDependent) sailConnection).getHttpClientSessionManager();
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

		if (subject == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

		if (predicate == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

		if (object == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		graphPattern.addOptionalTE(optional, optionalConstraints);

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	private ASTObjectList getObjectList(Node node) {
		if (node == null) {
			return null;
		}
		if (node instanceof ASTPropertyListPath) {
```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		graphPattern = parentGP;

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		graphPattern.setStatementPatternScope(oldScope);

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		graphPattern = parentGP;

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
			return (ValueExpr) node.jjtGetChild(0).jjtAccept(this, data);
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java

		if (serviceExpr instanceof SingletonSet) {
			return null; // do not add an empty service block
		}

```

### ReturnNull
Return of `null`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
		graphPattern = parentGP;

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalRDFParser.java`
#### Snippet
```java
	private String getResourceValue(Resource subject) {
		if (subject == null) {
			return null;
		} else if (subject instanceof IRI) {
			return subject.stringValue();
```

### ReturnNull
Return of `null`
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDHierarchicalProcessor.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/InMemRecordCache.java`
#### Snippet
```java
					return records.get(index++);
				}
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SequentialRecordCache.java`
#### Snippet
```java
			fillReadAheadBuffer();
			if (!readAheadBuffer.hasRemaining()) {
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeEvaluationStatistics.java`
#### Snippet
```java

		protected Value getConstantValue(Var var) {
			return (var != null) ? var.getValue() : null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeStatementIterator.java`
#### Snippet
```java

			if (nextValue == null) {
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java
			if (datatypeID == NativeValue.UNKNOWN_ID) {
				// Unknown datatype means unknown literal
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java
		if (nsID == -1) {
			// Unknown namespace means unknown URI
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/RangeIterator.java`
#### Snippet
```java
		Node nextCurrentNode = currentNode;
		if (nextCurrentNode == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/DataStore.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
			return readNode(rootNodeID);
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
			if (node == null) {
				// Empty BTree
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
					// value not found
					node.release();
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
		int index = getIndex(bindingName);
		if (index == -1) {
			return null;
		}
		return (a) -> a.values[index];
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
		if (index == -1) {
			assert false : "variable not known to ArrayBindingSet : " + bindingName;
			return null;
		}
		return (v, a) -> {
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
		if (index == -1) {
			assert false : "variable not known to ArrayBindingSet : " + bindingName;
			return null;
		}
		return (v, a) -> {
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
		int index = getIndex(bindingName);
		if (index == -1) {
			return null;
		}
		return (a) -> a.whichBindingsHaveBeenSet[index];
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
		int index = getIndex(bindingName);
		if (index == -1) {
			return null;
		}
		return (a) -> {
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
				return new SimpleBinding(bindingName, value);
			} else {
				return null;
			}
		};
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
				return new SimpleBinding(name, value);
			} else {
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/QueryBindingSet.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/SameTermFilterOptimizer.java`
#### Snippet
```java
				return ((Var) valueExpr).getValue();
			} else {
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryEvaluationContext.java`
#### Snippet
```java
			Binding binding = getBinding.apply(bs);
			if (binding == null) {
				return null;
			} else {
				return binding.getValue();
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		return (bs) -> {
			if (bs.isEmpty()) {
				return null;
			}
			if (bs instanceof ArrayBindingSet) {
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		public Value apply(BindingSet bs) {
			if (bs.isEmpty()) {
				return null;
			}
			if (bs instanceof ArrayBindingSet) {
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/ReificationRdfStarQueryEvaluationStep.java`
#### Snippet
```java
					return result;
				}
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/EvaluationStrategies.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/TripleSources.java`
#### Snippet
```java
	public static Value singleValue(Resource subj, IRI pred, TripleSource store) throws QueryEvaluationException {
		Statement stmt = single(subj, pred, null, store);
		return (stmt != null) ? stmt.getObject() : null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtil.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtility.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtility.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java

		if (contexts == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java

		if ((subject != null && !subject.isResource()) || (predicate != null && !predicate.isIRI())) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
			iteration = tripleSource.getStatements((Resource) subject, (IRI) predicate, object, contexts);
			if (iteration instanceof EmptyIteration) {
				return null;
			}
			iteration = handleFilter(contexts, (Resource) subject, (IRI) predicate, object, iteration);
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
	private static Function<BindingSet, Value> makeGetVarValue(Var var, QueryEvaluationContext context) {
		if (var == null) {
			return null;
		} else if (var.hasValue()) {
			Value value = var.getValue();
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
	public static final Resource[] DEFAULT_CONTEXT = { null };
	public static final Resource[] ALL_CONTEXT = new Resource[0];
	private static final Function<Value, Resource[]> RETURN_NULL_VALUE_RESOURCE_ARRAY = (v) -> null;

	private final StatementPattern statementPattern;
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
							// Statement pattern specifies a context that is not part of
							// the dataset
							return null;
						}
					} else {
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		Resource[] contexts = contextSup.apply(contextValue);
		if (contexts == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		Value subject = getSubjectVar != null ? getSubjectVar.apply(bindings) : null;
		if (subject != null && !subject.isResource()) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		Value predicate = getPredicateVar != null ? getPredicateVar.apply(bindings) : null;
		if (predicate != null && !predicate.isIRI()) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
			iteration = tripleSource.getStatements((Resource) subject, (IRI) predicate, object, contexts);
			if (iteration instanceof EmptyIteration) {
				return null;
			}
			iteration = handleFilter(contexts, (Resource) subject, (IRI) predicate, object, iteration);
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/MultiProjectionIterator.java`
#### Snippet
```java
		while (true) {
			if (isClosed()) {
				return null;
			}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/MultiProjectionIterator.java`
#### Snippet
```java
			} else {
				// no more results
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/JoinIterator.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/LeftJoinIterator.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
			// in case of type error, if-construction should result in empty
			// binding.
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	public static Value getVarValue(Var var, BindingSet bindings) {
		if (var == null) {
			return null;
		} else if (var.hasValue()) {
			return var.getValue();
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/FilterIterator.java`
#### Snippet
```java
				return context.getBinding(variableName);
			} else {
				return (bs) -> null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/FilterIterator.java`
#### Snippet
```java
				return context.getValue(variableName);
			} else {
				return (bs) -> null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ZeroLengthPathIteration.java`
#### Snippet
```java
			// resources
			reportedValues = null;
			return null;
		} else {
			if (result != null) {
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
			unreportedValues.clear();
			valueQueue.clear();
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/CrossProductIteration.java`
#### Snippet
```java
				currentInputBinding = resultIteration.next();
			} else {
				return null; // no more results
			}
		}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/CrossProductIteration.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
				return evaluationStep.evaluate(bindings);
			} catch (ValueExprEvaluationException e) {
				return null; // treat missing or invalid expressions as null
			}
		}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
				} else {
					// no more elements available
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/SameTermFilterOptimizer.java`
#### Snippet
```java
				return ((Var) valueExpr).getValue();
			} else {
				return null;
			}
		}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
				} else {
					// no more elements available
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
		resetCurrentDescribeExprIter();
		if (currentDescribeExprIter == null) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
				resetCurrentDescribeExprIter();
				if (currentDescribeExprIter == null) {
					return null;
				}
			}
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
		}

		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
					try {
						queue.close();
						return null;
					} catch (IOException e) {
						return e;
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
		public E peek() {
			if (size <= 0) {
				return null;
			} else if (next != null) {
				return next;
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
			}
			if (head.isEmpty()) {
				return null;
			} else {
				Entry<E, List<Integer>> e = head.firstEntry();
```

### ReturnNull
Return of `null`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
				return (E) input.readObject();
			} catch (IOException | ClassNotFoundException exc) {
				return null;
			} finally {
				next = null;
```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/DistanceQuerySpec.java`
#### Snippet
```java
			return (IRI) value;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/DistanceQuerySpec.java`
#### Snippet
```java
			}
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/DistanceQuerySpec.java`
#### Snippet
```java
			return (Literal) value;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/QuerySpec.java`
#### Snippet
```java
			return (IRI) typePattern.getObjectVar().getValue();
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/PropertyCache.java`
#### Snippet
```java

	private Set<String> getCachedValues(String name) {
		return (cachedProperties != null) ? cachedProperties.get(name) : null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/SearchFields.java`
#### Snippet
```java
			return ((Literal) object).getLabel();
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/SearchFields.java`
#### Snippet
```java
	public static Resource createContext(String idString) {
		if (CONTEXT_NULL.equals(idString)) {
			return null;
		} else {
			return createResource(idString);
```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/GeoRelationQuerySpecBuilder.java`
#### Snippet
```java
			return (Literal) value;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/GeoRelationQuerySpecBuilder.java`
#### Snippet
```java
			}
		}
		return null;
	}
}
```

### ReturnNull
Return of `null`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java
		Set<IRI> nextIndexedFields = indexedFields;
		if (nextIndexedFields != null && !nextIndexedFields.contains(p)) {
			return null;
		}

```

### ReturnNull
Return of `null`
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchDocumentScore.java`
#### Snippet
```java
		HighlightField highlightField = hit.getHighlightFields().get(ElasticsearchIndex.toPropertyFieldName(property));
		if (highlightField == null) {
			return null;
		}
		return Iterables.transform(Arrays.asList(highlightField.getFragments()),
```

### ReturnNull
Return of `null`
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
		ShapeRelation spatialOp = toSpatialOp(relation);
		if (spatialOp == null) {
			return null;
		}
		final String fieldName = toGeoShapeFieldName(SearchFields.getPropertyField(geoProperty));
```

### ReturnNull
Return of `null`
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
		}
		// no such Document
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
			return ShapeRelation.WITHIN;
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/dao/PaintingDao.java`
#### Snippet
```java
	@Override
	protected RDF4JDao.NamedSparqlSupplierPreparer prepareNamedSparqlSuppliers(NamedSparqlSupplierPreparer preparer) {
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/sail/model/src/main/java/org/eclipse/rdf4j/sail/model/NonSerializables.java`
#### Snippet
```java
			}
		}
		return null;
	}

```

### ReturnNull
Return of `null`
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
				return aVar.getValue();
			} else {
				return null;
			}
		} else {
```

### ReturnNull
Return of `null`
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
			}
		} else {
			return null;
		}
	}
```

### ReturnNull
Return of `null`
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/base/InterceptingRepositoryWrapper.java`
#### Snippet
```java
		}
		if (conn == null) {
			return null;
		}

```

## RuleId[id=AssignmentToLambdaParameter]
### AssignmentToLambdaParameter
Assignment to lambda parameter `val`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
		index.compute(Math.abs(indexHash % SINGLE_DIMENSION_INDEX_SIZE), (key, val) -> {
			if (val == null) {
				val = getHLL();
			}
			val.addRaw(statementHash);
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `qry` is redundant
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
		}

		String qry = queryString;
		int b = qry.indexOf('{');
		String select = qry.substring(0, b);
```

### UnnecessaryLocalVariable
Local variable `validValue` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DatatypeFilter.java`
#### Snippet
```java
		if (xsdDatatype != null) {
			if (literal.getCoreDatatype() == xsdDatatype) {
				boolean validValue = XMLDatatypeUtil.isValidValue(literal.stringValue(), xsdDatatype);
				return validValue;
			}
```

### UnnecessaryLocalVariable
Local variable `falseNode` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/ClassConstraintComponent.java`
#### Snippet
```java

			// filter by type against the base sail
			PlanNode falseNode = new FilterByPredicateObject(
					connectionsGroup.getBaseConnection(),
					validationSettings.getDataGraph(), RDF.TYPE, clazzSet,
```

### UnnecessaryLocalVariable
Local variable `falseNode` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/DashHasValueInConstraintComponent.java`
#### Snippet
```java
			}

			PlanNode falseNode = new ValueInFilter(addedTargets, hasValueIn)
					.getFalseNode(UnBufferedPlanNode.class);

```

### UnnecessaryLocalVariable
Local variable `falseNode` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/HasValueConstraintComponent.java`
#### Snippet
```java
			}

			PlanNode falseNode = new ValueInFilter(addedTargets, new HashSet<>(Collections.singletonList(hasValue)))
					.getFalseNode(UnBufferedPlanNode.class);

```

### UnnecessaryLocalVariable
Local variable `repositoryID` is redundant
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 */
	public static final String getRepositoryID(String repositoryLocation) {
		String repositoryID = repositoryLocation.substring(repositoryLocation.lastIndexOf('/') + 1);
		return repositoryID;
	}
```

### UnnecessaryLocalVariable
Local variable `value` is redundant
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionSAXParser.java`
#### Snippet
```java
			}

			String value = text;
			String name = atts.get(TransactionXMLConstants.NAME_ATT);

```

### UnnecessaryLocalVariable
Local variable `cInt` is redundant
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
		for (int i = 0; i < labelLength; i++) {
			char c = label.charAt(i);
			int cInt = c;

			if (c == '\\') {
```

### UnnecessaryLocalVariable
Local variable `service` is redundant
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
					prefixDecls.put(SPIN.PREFIX, SPIN.NAMESPACE);
					prefixDecls.put(SPL.PREFIX, SPL.NAMESPACE);
					Service service = new Service(getVar(serviceUri), tupleNode, exprString, prefixDecls, null,
							isSilent);
					tupleNode = service;
```

### UnnecessaryLocalVariable
Local variable `declarations` is redundant
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
	 */
	protected String getNamespaceDeclarations() {
		String declarations = "PREFIX rdf: <" + RDF.NAMESPACE + "> \n" +
				"PREFIX rdfs: <" + RDFS.NAMESPACE + "> \n" +
				"PREFIX dc: <" + DC.NAMESPACE + "> \n" +
```

### UnnecessaryLocalVariable
Local variable `wkt` is redundant
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneDocument.java`
#### Snippet
```java
		LuceneIndex.addStoredOnlyPredicateField(field, value, doc);
		try {
			String wkt = value;
			Object shape = SimpleWKTShapeParser.parse(wkt);
			indexShape(shape, field);
```

### UnnecessaryLocalVariable
Local variable `subjects` is redundant
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/Model.java`
#### Snippet
```java
	 */
	default Set<Resource> contexts() {
		Set<Resource> subjects = stream().map(st -> st.getContext()).collect(Collectors.toSet());
		return subjects;
	}
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `core/rio/languages/src/main/java/org/eclipse/rdf4j/rio/languages/RFC3066LanguageHandler.java`
#### Snippet
```java
		// language tag is RFC3066-conformant if it matches this regex:
		// [a-zA-Z]{1,8}(-[a-zA-Z0-9]{1,8})*
		boolean result = matcher.matcher(languageTag).matches();

		return result;
```

### UnnecessaryLocalVariable
Local variable `n` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
	 */
	private static boolean isUnreserved(char c) {
		final int n = (int) c;
		// check if alphanumeric
		boolean unreserved = (47 < n && n < 58) || (96 < n && n < 123) || (64 < n && n < 91);
```

### UnnecessaryLocalVariable
Local variable `lowestNonTrivialPartition` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
			});

			Collection<BNode> lowestNonTrivialPartition = sortedPartitions.stream()
					.filter(part -> part.size() > 1)
					.findFirst()
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
		 */
		public BigInteger getMappingSize() {
			BigInteger size = currentNodeMapping.values()
					.stream()
					.map(h -> new BigInteger(1, h.asBytes()))
```

### UnnecessaryLocalVariable
Local variable `current` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java
		ValueFactory vf = SimpleValueFactory.getInstance();

		Resource current = container;

		for (int annotatedMembershipPropertyCounter = 1; true; annotatedMembershipPropertyCounter++) {
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java
	@Override
	public Collection<RioSetting<?>> getSupportedSettings() {
		Set<RioSetting<?>> result = new HashSet<>();
		return result;
	}
```

### UnnecessaryLocalVariable
Local variable `skolemizedBNode` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java

		if (value instanceof IRI) {
			boolean skolemizedBNode = value.stringValue().contains("/.well-known/genid/");
			return skolemizedBNode;
		}
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
		try {
			conn = repository.getConnection();
			T result = processFunction.apply(conn);
			return result;
		} finally {
```

### UnnecessaryLocalVariable
Local variable `rightArg` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	public ValueExpr visit(ASTInfix node, Object data) throws VisitorException {
		ValueExpr leftArg = (ValueExpr) node.jjtGetChild(0).jjtAccept(this, data);
		ValueExpr rightArg = (ValueExpr) node.jjtGetChild(1).jjtAccept(this, leftArg);

		return rightArg;
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/ndjsonld/NDJSONLDWriter.java`
#### Snippet
```java
	@Override
	public Collection<RioSetting<?>> getSupportedSettings() {
		Collection<RioSetting<?>> result = new HashSet<>(Arrays.asList(
				BasicWriterSettings.BASE_DIRECTIVE,
				JSONLDSettings.COMPACT_ARRAYS,
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
				solution6, solution7, solution8, solution9, solution10);

		IteratingTupleQueryResult result = new IteratingTupleQueryResult(bindingNames, bindingSetList);

		return result;
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
				solution5);

		IteratingTupleQueryResult result = new IteratingTupleQueryResult(bindingNames, bindingSetList);

		return result;
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
		List<? extends BindingSet> bindingSetList = Collections.emptyList();

		IteratingTupleQueryResult result = new IteratingTupleQueryResult(bindingNames, bindingSetList);

		return result;
```

### UnnecessaryLocalVariable
Local variable `newFilter` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ConjunctiveConstraintSplitter.java`
#### Snippet
```java

			for (int i = conjunctiveConstraints.size() - 1; i >= 1; i--) {
				Filter newFilter = new Filter(filterArg, conjunctiveConstraints.get(i));
				filterArg = newFilter;
			}
```

### UnnecessaryLocalVariable
Local variable `uri` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/rdfterm/UUID.java`
#### Snippet
```java
		}

		IRI uri = valueFactory.createIRI("urn:uuid:" + java.util.UUID.randomUUID().toString());

		return uri;
```

### UnnecessaryLocalVariable
Local variable `literal` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/rdfterm/STRUUID.java`
#### Snippet
```java
		}

		Literal literal = valueFactory.createLiteral(java.util.UUID.randomUUID().toString());

		return literal;
```

### UnnecessaryLocalVariable
Local variable `iter` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ZeroLengthPathIteration.java`
#### Snippet
```java

	private CloseableIteration<BindingSet, QueryEvaluationException> createIteration() throws QueryEvaluationException {
		CloseableIteration<BindingSet, QueryEvaluationException> iter = precompile.evaluate(bindings);
		return iter;
	}
```

### UnnecessaryLocalVariable
Local variable `newFilter` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/ConjunctiveConstraintSplitterOptimizer.java`
#### Snippet
```java

			for (int i = conjunctiveConstraints.size() - 1; i >= 1; i--) {
				Filter newFilter = new Filter(filterArg, conjunctiveConstraints.get(i));
				filterArg = newFilter;
			}
```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchStore.java`
#### Snippet
```java

			try {
				Thread.sleep(1000);
			} catch (InterruptedException ignored) {

```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchStore.java`
#### Snippet
```java
				// don't hang forever
				try {
					Thread.sleep(100);
				} catch (InterruptedException e) {
					// should never be interrupted
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
//							 sleep for up to 5 seconds unless we are low on memory
							for (int i = 0; i < 100 * 5 && !memoryIsLow(getFreeToAllocateMemory() * 2); i++) {
								Thread.sleep(10);
							}

```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
							if (queue.isEmpty()) {
								try {
									Thread.sleep(2);
								} catch (InterruptedException ignored) {

```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/TupleQueryResultTest.java`
#### Snippet
```java
				while (unclosedQueryResults.stream().map(Reference::get).anyMatch(Objects::nonNull)) {
					System.gc();
					Thread.sleep(100);
					assertTrue(stopWatch.getTime(TimeUnit.SECONDS) < 60, "Test timed out after 60 seconds");

```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSailConnection.java`
#### Snippet
```java
			System.gc();
			try {
				Thread.sleep(1);
			} catch (InterruptedException e) {
				Thread.currentThread().interrupt();
```

## RuleId[id=WaitNotInLoop]
### WaitNotInLoop
Call to `wait()` is not in loop
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSail.java`
#### Snippet
```java
					logger.debug("Waiting for active connections to close before shutting down...");
					try {
						activeConnections.wait(connectionTimeOut);
					} catch (InterruptedException e) {
						// ignore and continue
```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`triggers[i] == true` can be simplified to 'triggers\[i\]'
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java

			for (int i = 0; i < RDFSRules.RULECOUNT; i++) {
				if (triggers[i] == true) {
					checkRuleNextIter[i] = true;
				}
```

### PointlessBooleanExpression
`needsSync == false` can be simplified to '!needsSync'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/AllocatedNodesList.java`
#### Snippet
```java

	private void scheduleSync() throws IOException {
		if (needsSync == false) {
			nioFile.truncate(0);
			needsSync = true;
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
					idArray = doc.getValues("id");
					for (String id : idArray) {
						ids.add(id);
					}

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryOptimizerList.java`
#### Snippet
```java
		this.optimizers = new ArrayList<>(optimizers.length);
		for (QueryOptimizer optimizer : optimizers) {
			this.optimizers.add(optimizer);
		}
	}
```

## RuleId[id=UnstableApiUsage]
### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
			}
			return staticValueMapping.computeIfAbsent(value,
					v -> hashFunction.hashString(v.stringValue(), StandardCharsets.UTF_8));

		}
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java

	protected static HashCode hashTuple(HashCode... hashCodes) {
		return Hashing.combineOrdered(Arrays.asList(hashCodes));
	}

```

### UnstableApiUsage
'combineOrdered(java.lang.Iterable)' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java

	protected static HashCode hashTuple(HashCode... hashCodes) {
		return Hashing.combineOrdered(Arrays.asList(hashCodes));
	}

```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	private static final HashCode outgoing = hashFunction.hashString("+", StandardCharsets.UTF_8);
	private static final HashCode incoming = hashFunction.hashString("-", StandardCharsets.UTF_8);
	private static final HashCode distinguisher = hashFunction.hashString("@", StandardCharsets.UTF_8);

	/**
```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java

	private static final HashCode initialHashCode = hashFunction.hashString("", StandardCharsets.UTF_8);
	private static final HashCode outgoing = hashFunction.hashString("+", StandardCharsets.UTF_8);
	private static final HashCode incoming = hashFunction.hashString("-", StandardCharsets.UTF_8);
	private static final HashCode distinguisher = hashFunction.hashString("@", StandardCharsets.UTF_8);
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java

	protected static HashCode hashBag(HashCode... hashCodes) {
		return Hashing.combineUnordered(Arrays.asList(hashCodes));
	}

```

### UnstableApiUsage
'combineUnordered(java.lang.Iterable)' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java

	protected static HashCode hashBag(HashCode... hashCodes) {
		return Hashing.combineUnordered(Arrays.asList(hashCodes));
	}

```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
						List<HashCode> hashSequence = new ArrayList<>(3);

						hashSequence.add(hashFunction.hashString(l.getLabel(), StandardCharsets.UTF_8));

						// Per BCP47, language tags are case-insensitive. Use normalized form to ensure consistency if
```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
						// possible, otherwise just use lower-case.
						l.getLanguage()
								.map(lang -> hashFunction.hashString(
										Literals.isValidLanguageTag(lang) ? Literals.normalizeLanguageTag(lang)
												: lang.toLowerCase(),
```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
								.ifPresent(h -> hashSequence.add(h));
						hashSequence
								.add(hashFunction.hashString(l.getDatatype().stringValue(), StandardCharsets.UTF_8));
						return Hashing.combineOrdered(hashSequence);
					}
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
						hashSequence
								.add(hashFunction.hashString(l.getDatatype().stringValue(), StandardCharsets.UTF_8));
						return Hashing.combineOrdered(hashSequence);
					}
			);
```

### UnstableApiUsage
'combineOrdered(java.lang.Iterable)' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
						hashSequence
								.add(hashFunction.hashString(l.getDatatype().stringValue(), StandardCharsets.UTF_8));
						return Hashing.combineOrdered(hashSequence);
					}
			);
```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	private static final HashCode initialHashCode = hashFunction.hashString("", StandardCharsets.UTF_8);
	private static final HashCode outgoing = hashFunction.hashString("+", StandardCharsets.UTF_8);
	private static final HashCode incoming = hashFunction.hashString("-", StandardCharsets.UTF_8);
	private static final HashCode distinguisher = hashFunction.hashString("@", StandardCharsets.UTF_8);

```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
			}
			return staticValueMapping.computeIfAbsent(value,
					v -> hashFunction.hashString(v.stringValue(), StandardCharsets.UTF_8));
		}

```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	private static final HashFunction hashFunction = Hashing.sha256();

	private static final HashCode initialHashCode = hashFunction.hashString("", StandardCharsets.UTF_8);
	private static final HashCode outgoing = hashFunction.hashString("+", StandardCharsets.UTF_8);
	private static final HashCode incoming = hashFunction.hashString("-", StandardCharsets.UTF_8);
```

### UnstableApiUsage
'com.google.common.hash.HashFunction' is marked unstable with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	private static final Logger logger = LoggerFactory.getLogger(GraphComparisons.class);

	private static final HashFunction hashFunction = Hashing.sha256();

	private static final HashCode initialHashCode = hashFunction.hashString("", StandardCharsets.UTF_8);
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	private static final Logger logger = LoggerFactory.getLogger(GraphComparisons.class);

	private static final HashFunction hashFunction = Hashing.sha256();

	private static final HashCode initialHashCode = hashFunction.hashString("", StandardCharsets.UTF_8);
```

### UnstableApiUsage
'sha256()' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	private static final Logger logger = LoggerFactory.getLogger(GraphComparisons.class);

	private static final HashFunction hashFunction = Hashing.sha256();

	private static final HashCode initialHashCode = hashFunction.hashString("", StandardCharsets.UTF_8);
```

### UnstableApiUsage
'hashString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.HashFunction' marked with @Beta
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
							Statement statement = poll.statement;
							long statementHash = HASH_FUNCTION
									.hashString(statement.toString(), StandardCharsets.UTF_8)
									.asLong();

```

### UnstableApiUsage
'com.google.common.hash.HashFunction' is marked unstable with @Beta
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
	AtomicInteger queueSize = new AtomicInteger();

	private final HashFunction HASH_FUNCTION = Hashing.murmur3_128();

	private final HLL EMPTY_HLL = getHLL();
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
	AtomicInteger queueSize = new AtomicInteger();

	private final HashFunction HASH_FUNCTION = Hashing.murmur3_128();

	private final HLL EMPTY_HLL = getHLL();
```

### UnstableApiUsage
'murmur3_128()' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
	AtomicInteger queueSize = new AtomicInteger();

	private final HashFunction HASH_FUNCTION = Hashing.murmur3_128();

	private final HLL EMPTY_HLL = getHLL();
```

## RuleId[id=EnumSwitchStatementWhichMissesCases]
### EnumSwitchStatementWhichMissesCases
`switch (action) { case ADD: if (toRemove != null) { removeModel(toRemove); toRemove...` statement on enum type 'org.eclipse.rdf4j.http.protocol.Protocol.Action' misses case 'PING'
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
		if (isActive()) {
			int maxBufferSize = getParserConfig().get(HTTPRepositorySettings.MAX_STATEMENT_BUFFER_SIZE);
			switch (action) {
			case ADD:
				if (toRemove != null) {
					removeModel(toRemove);
					toRemove = null;
				}
				if (toAdd != null && maxBufferSize <= toAdd.size()) {
					addModel(toAdd);
					toAdd = null;
				}
				break;
			case DELETE:
				if (toAdd != null) {
					addModel(toAdd);
					toAdd = null;
				}
				if (toRemove != null && maxBufferSize <= toRemove.size()) {
					removeModel(toRemove);
					toRemove = null;
				}
				break;
			case GET:
			case UPDATE:
			case COMMIT:
			case PREPARE:
			case QUERY:
			case SIZE:
				if (toAdd != null) {
					addModel(toAdd);
					toAdd = null;
				}
				if (toRemove != null) {
					removeModel(toRemove);
					toRemove = null;
				}
				break;
			case ROLLBACK:
				toAdd = null;
				toRemove = null;
				break;

			}
		}
	}
```

### EnumSwitchStatementWhichMissesCases
`switch (datatype) { case DECIMAL: return normalizeDecimal(value); case INTEGER: return nor...` statement on enum type 'org.eclipse.rdf4j.model.base.CoreDatatype.XSD' misses cases: 'ENTITIES', 'ENTITY', 'ID', 'IDREF', 'IDREFS', ...
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static String normalize(String value, CoreDatatype.XSD datatype) {

		switch (datatype) {
		case DECIMAL:
			return normalizeDecimal(value);
		case INTEGER:
			return normalizeInteger(value);
		case NEGATIVE_INTEGER:
			return normalizeNegativeInteger(value);
		case NON_POSITIVE_INTEGER:
			return normalizeNonPositiveInteger(value);
		case NON_NEGATIVE_INTEGER:
			return normalizeNonNegativeInteger(value);
		case POSITIVE_INTEGER:
			return normalizePositiveInteger(value);
		case LONG:
			return normalizeLong(value);
		case INT:
			return normalizeInt(value);
		case SHORT:
			return normalizeShort(value);
		case BYTE:
			return normalizeByte(value);
		case UNSIGNED_LONG:
			return normalizeUnsignedLong(value);
		case UNSIGNED_INT:
			return normalizeUnsignedInt(value);
		case UNSIGNED_SHORT:
			return normalizeUnsignedShort(value);
		case UNSIGNED_BYTE:
			return normalizeUnsignedByte(value);
		case FLOAT:
			return normalizeFloat(value);
		case DOUBLE:
			return normalizeDouble(value);
		case BOOLEAN:
			return normalizeBoolean(value);
		case DATETIME:
			return normalizeDateTime(value);
		case ANYURI:
			return collapseWhiteSpace(value);
		}

		return value;
```

### EnumSwitchStatementWhichMissesCases
`switch (datatype) { case DECIMAL: return isValidDecimal(value); case INTEGER: return isVal...` statement on enum type 'org.eclipse.rdf4j.model.base.CoreDatatype.XSD' misses cases: 'ENTITIES', 'ENTITY', 'ID', 'IDREF', 'IDREFS', ...
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
Value(String value, CoreDatatype.XSD datatype) {
		switch (datatype) {
		case DECIMAL:
			return isValidDecimal(value);
		case INTEGER:
			return isValidInteger(value);
		case NEGATIVE_INTEGER:
			return isValidNegativeInteger(value);
		case NON_POSITIVE_INTEGER:
			return isValidNonPositiveInteger(value);
		case NON_NEGATIVE_INTEGER:
			return isValidNonNegativeInteger(value);
		case POSITIVE_INTEGER:
			return isValidPositiveInteger(value);
		case LONG:
			return isValidLong(value);
		case INT:
			return isValidInt(value);
		case SHORT:
			return isValidShort(value);
		case BYTE:
			return isValidByte(value);
		case UNSIGNED_LONG:
			return isValidUnsignedLong(value);
		case UNSIGNED_INT:
			return isValidUnsignedInt(value);
		case UNSIGNED_SHORT:
			return isValidUnsignedShort(value);
		case UNSIGNED_BYTE:
			return isValidUnsignedByte(value);
		case FLOAT:
			return isValidFloat(value);
		case DOUBLE:
			return isValidDouble(value);
		case BOOLEAN:
			return isValidBoolean(value);
		case DATETIME:
			return isValidDateTime(value);
		case DATETIMESTAMP:
			return isValidDateTimeStamp(value);
		case DATE:
			return isValidDate(value);
		case TIME:
			return isValidTime(value);
		case GDAY:
			return isValidGDay(value);
		case GMONTH:
			return isValidGMonth(value);
		case GMONTHDAY:
			return isValidGMonthDay(value);
		case GYEAR:
			return isValidGYear(value);
		case GYEARMONTH:
			return isValidGYearMonth(value);
		case DURATION:
			return isValidDuration(value);
		case DAYTIMEDURATION:
			return isValidDayTimeDuration(value);
		case YEARMONTHDURATION:
			return isValidYearMonthDuration(value);
		case QNAME:
			return isValidQName(value);
		case ANYURI:
			return isValidAnyURI(value);
		case LANGUAGE:
			return Literals.isValidLanguageTag(value);
		}

		return true;
```

### EnumSwitchStatementWhichMissesCases
`switch (txnStatus) { case COMMITTING: logger.info("Detected uncompleted commit, trying to compl...` statement on enum type 'org.eclipse.rdf4j.sail.nativerdf.TxnStatusFile.TxnStatus' misses case 'NONE'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TripleStore.java`
#### Snippet
```java

	private void processUncompletedTransaction(TxnStatus txnStatus) throws IOException {
		switch (txnStatus) {
		case COMMITTING:
			logger.info("Detected uncompleted commit, trying to complete");
			try {
				commit();
				logger.info("Uncompleted commit completed successfully");
			} catch (IOException e) {
				logger.error("Failed to restore from uncompleted commit", e);
				throw e;
			}
			break;
		case ROLLING_BACK:
			logger.info("Detected uncompleted rollback, trying to complete");
			try {
				rollback();
				logger.info("Uncompleted rollback completed successfully");
			} catch (IOException e) {
				logger.error("Failed to restore from uncompleted rollback", e);
				throw e;
			}
			break;
		case ACTIVE:
			logger.info("Detected unfinished transaction, trying to roll back");
			try {
				rollback();
				logger.info("Unfinished transaction rolled back successfully");
			} catch (IOException e) {
				logger.error("Failed to roll back unfinished transaction", e);
				throw e;
			}
			break;
		case UNKNOWN:
			logger.info("Read invalid or unknown transaction status, trying to roll back");
			try {
				rollback();
				logger.info("Successfully performed a rollback for invalid or unknown transaction status");
			} catch (IOException e) {
				logger.error("Failed to perform rollback for invalid or unknown transaction status", e);
				throw e;
			}
			break;
		}
	}

```

### EnumSwitchStatementWhichMissesCases
`switch (operator) { case EQ: return true; case NE: return false; }` statement on enum type 'org.eclipse.rdf4j.query.algebra.Compare.CompareOp' misses cases: 'LT', 'LE', 'GE', and 'GT'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtil.java`
#### Snippet
```java
					// One of the basic-type method calls failed, try syntactic match before throwing an error
					if (leftLit.equals(rightLit)) {
						switch (operator) {
						case EQ:
							return true;
						case NE:
							return false;
						}
					}

```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/InternedIRI.java`
#### Snippet
```java
		if (o instanceof InternedIRI) {
			// Because the stringValue is interned we can simply compare the reference.
			return stringValue == ((InternedIRI) o).stringValue;
		}

```

### StringEquality
String values are compared using `!=`, not 'equals()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/FileFormat.java`
#### Snippet
```java
				return true;
			}
			if (mimeType != type && mt.equalsIgnoreCase(type)) {
				return true;
			}
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
	public Value getValue(String bindingName) {
		for (int i = 0; i < bindingNames.length; i++) {
			if (bindingNames[i] == bindingName && whichBindingsHaveBeenSet[i]) {
				return values[i];
			}
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
	private int getIndex(String bindingName) {
		for (int i = 0; i < bindingNames.length; i++) {
			if (bindingNames[i] == bindingName) {
				return i;
			}
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		if (initialized) {
			for (int i = 0; i < allVariables.length; i++) {
				if (allVariables[i] == variableName) {
					return getBinding[i];
				}
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		if (initialized) {
			for (int i = 0; i < allVariables.length; i++) {
				if (allVariables[i] == variableName) {
					return setBinding[i];
				}
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		if (initialized) {
			for (int i = 0; i < allVariables.length; i++) {
				if (allVariables[i] == variableName) {
					return hasBinding[i];
				}
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		if (initialized) {
			for (int i = 0; i < allVariables.length; i++) {
				if (allVariables[i] == variableName) {
					return getValue[i];
				}
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		if (initialized) {
			for (int i = 0; i < allVariables.length; i++) {
				if (allVariables[i] == variableName) {
					return addBinding[i];
				}
```

## RuleId[id=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/WritePrefReadWriteLockManager.java`
#### Snippet
```java
		}

		while (true) {
			readersLocked.increment();
			if (!stampedLock.isWriteLocked()) {
```

## RuleId[id=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `includeSubManifest()` declared in class 'org.eclipse.rdf4j.testsuite.query.parser.sparql.manifest.SPARQLComplianceTest' but referenced via subclass 'org.eclipse.rdf4j.testsuite.query.parser.sparql.manifest.SPARQLQueryComplianceTest'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryComplianceTest.java`
#### Snippet
```java
					for (BindingSet bindingSet : manifestResults) {
						String subManifestFile = bindingSet.getValue("manifestFile").stringValue();
						if (SPARQLQueryComplianceTest.includeSubManifest(subManifestFile, excludedSubdirs)) {
							getSubManifests().add(subManifestFile);
						}
```

### StaticCallOnSubclass
Static method `matchFileName()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.rio.RDFFormat'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/Rio.java`
#### Snippet
```java
	 */
	public static Optional<RDFFormat> getParserFormatForFileName(String fileName) {
		return RDFFormat.matchFileName(fileName, RDFParserRegistry.getInstance().getKeys());
	}

```

### StaticCallOnSubclass
Static method `matchFileName()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.rio.RDFFormat'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/Rio.java`
#### Snippet
```java
	 */
	public static Optional<RDFFormat> getWriterFormatForFileName(String fileName) {
		return RDFFormat.matchFileName(fileName, RDFWriterRegistry.getInstance().getKeys());
	}

```

### StaticCallOnSubclass
Static method `matchMIMEType()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.rio.RDFFormat'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/Rio.java`
#### Snippet
```java
	 */
	public static Optional<RDFFormat> getWriterFormatForMIMEType(String mimeType) {
		return RDFFormat.matchMIMEType(mimeType, RDFWriterRegistry.getInstance().getKeys());
	}

```

### StaticCallOnSubclass
Static method `matchMIMEType()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.rio.RDFFormat'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/Rio.java`
#### Snippet
```java
	 */
	public static Optional<RDFFormat> getParserFormatForMIMEType(String mimeType) {
		return RDFFormat.matchMIMEType(mimeType, RDFParserRegistry.getInstance().getKeys());
	}

```

### StaticCallOnSubclass
Static method `matchMIMEType()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.query.resultio.TupleQueryResultFormat'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			// if we get here, HTTP code is 200
			String mimeType = getResponseMIMEType(response);
			QueryResultFormat format = TupleQueryResultFormat.matchMIMEType(mimeType, tqrFormats)
					.orElseThrow(() -> new RepositoryException(
							"Server responded with an unsupported file format: " + mimeType));
```

### StaticCallOnSubclass
Static method `matchMIMEType()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.rio.RDFFormat'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			// if we get here, HTTP code is 200
			String mimeType = getResponseMIMEType(response);
			RDFFormat format = RDFFormat.matchMIMEType(mimeType, rdfFormats)
					.orElseThrow(() -> new RepositoryException(
							"Server responded with an unsupported file format: " + mimeType));
```

### StaticCallOnSubclass
Static method `matchMIMEType()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.query.resultio.TupleQueryResultFormat'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			String mimeType = getResponseMIMEType(response);
			try {
				QueryResultFormat format = TupleQueryResultFormat.matchMIMEType(mimeType, tqrFormats)
						.orElseThrow(() -> new RepositoryException(
								"Server responded with an unsupported file format: " + mimeType));
```

### StaticCallOnSubclass
Static method `matchMIMEType()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.rio.RDFFormat'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			String mimeType = getResponseMIMEType(response);
			try {
				RDFFormat format = RDFFormat.matchMIMEType(mimeType, rdfFormats)
						.orElseThrow(() -> new RepositoryException(
								"Server responded with an unsupported file format: " + mimeType));
```

### StaticCallOnSubclass
Static method `matchMIMEType()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.rio.RDFFormat'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
				logger.debug("SHACL validation report is serialised as: " + serialisation.toString());

				Optional<RDFFormat> rdfFormat = RDFFormat.matchMIMEType(serialisation.toString(), rdfFormats);

				if (rdfFormat.isPresent()) {
```

### StaticCallOnSubclass
Static method `matchMIMEType()` declared in class 'org.eclipse.rdf4j.common.lang.FileFormat' but referenced via subclass 'org.eclipse.rdf4j.query.resultio.BooleanQueryResultFormat'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
			String mimeType = getResponseMIMEType(response);
			try {
				QueryResultFormat format = BooleanQueryResultFormat.matchMIMEType(mimeType, booleanFormats)
						.orElseThrow(() -> new RepositoryException(
								"Server responded with an unsupported file format: " + mimeType));
```

### StaticCallOnSubclass
Static method `asList()` declared in class 'org.eclipse.rdf4j.common.iteration.Iterations' but referenced via subclass 'org.eclipse.rdf4j.query.QueryResults'
in `examples/src/main/java/org/eclipse/rdf4j/examples/function/ExistingPalindromeFunction.java`
#### Snippet
```java
		// check if a triple with the rdfs:label predicate and this palindrome as its value already exists in the
		// database
		boolean existing = !QueryResults.asList(tripleSource.getStatements(null, RDFS.LABEL, (Literal) arg)).isEmpty();

		// return a new boolean literal that is true if the input argument is a palindrome and exists as a label
```

### StaticCallOnSubclass
Static method `asList()` declared in class 'org.eclipse.rdf4j.common.iteration.Iterations' but referenced via subclass 'org.eclipse.rdf4j.query.QueryResults'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/RemoveIsolationTest.java`
#### Snippet
```java

		try (RepositoryResult<Statement> stats = con.getStatements(null, null, null, true)) {
			assertEquals(Collections.emptyList(), QueryResults.asList(stats));
		}
		con.rollback();
```

### StaticCallOnSubclass
Static method `asList()` declared in class 'org.eclipse.rdf4j.common.iteration.Iterations' but referenced via subclass 'org.eclipse.rdf4j.query.QueryResults'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/RemoveIsolationTest.java`
#### Snippet
```java

		try (RepositoryResult<Statement> stats = con.getStatements(null, null, null, true)) {
			assertEquals(Collections.emptyList(), QueryResults.asList(stats));
		}
		con.rollback();
```

### StaticCallOnSubclass
Static method `asList()` declared in class 'org.eclipse.rdf4j.common.iteration.Iterations' but referenced via subclass 'org.eclipse.rdf4j.query.QueryResults'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/LinearTest.java`
#### Snippet
```java
			throws Exception {
		try (RepositoryResult<Statement> statements = con.getStatements(subj, pred, obj, inf, ctx)) {
			return QueryResults.asList(statements).size();
		}
	}
```

### StaticCallOnSubclass
Static method `asList()` declared in class 'org.eclipse.rdf4j.common.iteration.Iterations' but referenced via subclass 'org.eclipse.rdf4j.query.QueryResults'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/MonotonicTest.java`
#### Snippet
```java
	private int size(RepositoryConnection con, Resource subj, IRI pred, Value obj, boolean inf, Resource... ctx)
			throws Exception {
		return QueryResults.asList(con.getStatements(subj, pred, obj, inf, ctx)).size();
	}

```

### StaticCallOnSubclass
Static method `asList()` declared in class 'org.eclipse.rdf4j.common.iteration.Iterations' but referenced via subclass 'org.eclipse.rdf4j.query.QueryResults'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SerializableTest.java`
#### Snippet
```java
	private int size(RepositoryConnection con, Resource subj, IRI pred, Value obj, boolean inf, Resource... ctx)
			throws Exception {
		return QueryResults.asList(con.getStatements(subj, pred, obj, inf, ctx)).size();
	}

```

### StaticCallOnSubclass
Static method `asList()` declared in class 'org.eclipse.rdf4j.common.iteration.Iterations' but referenced via subclass 'org.eclipse.rdf4j.query.QueryResults'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
	private int size(RepositoryConnection con, Resource subj, IRI pred, Value obj, boolean inf, Resource... ctx)
			throws Exception {
		return QueryResults.asList(con.getStatements(subj, pred, obj, inf, ctx)).size();
	}

```

### StaticCallOnSubclass
Static method `stream()` declared in class 'org.eclipse.rdf4j.common.iteration.Iterations' but referenced via subclass 'org.eclipse.rdf4j.query.QueryResults'
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResult.java`
#### Snippet
```java
	 */
	default Stream<T> stream() {
		return QueryResults.stream(this);
	}

```

### StaticCallOnSubclass
Static method `isSPOElemList()` declared in class 'org.eclipse.rdf4j.queryrender.BaseTupleExprRenderer' but referenced via subclass 'org.eclipse.rdf4j.queryrender.sparql.SparqlTupleExprRenderer'
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SPARQLQueryRenderer.java`
#### Snippet
```java

			for (ProjectionElemList aList : mRenderer.getProjection()) {
				if (SparqlTupleExprRenderer.isSPOElemList(aList)) {
					if (!aFirst) {
						aQuery.append(System.lineSeparator());
```

## RuleId[id=RedundantClassCall]
### RedundantClassCall
Redundant call to `cast()`
in `core/sail/model/src/main/java/org/eclipse/rdf4j/sail/model/SailModel.java`
#### Snippet
```java
	private void readObject(ObjectInputStream in) throws IOException, ClassNotFoundException {
		in.defaultReadObject();
		this.conn = SailConnection.class.cast(NonSerializables.get(this.connKey));
	}
}
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java

		// First character should be '"' or "'"
		verifyCharacterOrFail(c1, "\"\'");

		// Check for long-string, which starts and ends with three double quotes
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/TokenMgrError.java`
#### Snippet
```java
				continue;
			case '\'':
				retval.append("\\\'");
				continue;
			case '\\':
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
				continue;
			case '\'':
				retval.append("\\\'");
				continue;
			case '\\':
```

## RuleId[id=ObsoleteCollection]
### ObsoleteCollection
Obsolete collection type `Stack` used
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
	 * Stack of ElementInfo objects.
	 */
	private final Stack<ElementInfo> elInfoStack = new Stack<>();

	/**
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
	 * Stack of ElementInfo objects.
	 */
	private final Stack<ElementInfo> elInfoStack = new Stack<>();

	/**
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/util/RDFXMLPrettyWriter.java`
#### Snippet
```java
	 * Stack for remembering the nodes (subjects/objects) of statements at each level.
	 */
	private final Stack<Node> nodeStack = new Stack<>();

	/**
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/util/RDFXMLPrettyWriter.java`
#### Snippet
```java
	 * Stack for remembering the nodes (subjects/objects) of statements at each level.
	 */
	private final Stack<Node> nodeStack = new Stack<>();

	/**
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/util/RDFXMLPrettyWriter.java`
#### Snippet
```java
	 * Stack for remembering the predicate of statements at each level.
	 */
	private final Stack<IRI> predicateStack = new Stack<>();

	private boolean writingEnded;
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/util/RDFXMLPrettyWriter.java`
#### Snippet
```java
	 * Stack for remembering the predicate of statements at each level.
	 */
	private final Stack<IRI> predicateStack = new Stack<>();

	private boolean writingEnded;
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
	 * A stack of node- and property elements.
	 */
	private final Stack<Object> elementStack = new Stack<>();

	/**
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
	 * A stack of node- and property elements.
	 */
	private final Stack<Object> elementStack = new Stack<>();

	/**
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PropertyPathSerializer.java`
#### Snippet
```java

	private StringBuilder builder;
	private final Stack<VarInfo> currentSubjectVarStack = new Stack<>();
	private AbstractSerializableParsedQuery currentQueryProfile;

```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PropertyPathSerializer.java`
#### Snippet
```java

	private StringBuilder builder;
	private final Stack<VarInfo> currentSubjectVarStack = new Stack<>();
	private AbstractSerializableParsedQuery currentQueryProfile;

```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	public Map<Projection, SerializableParsedTupleQuery> queriesByProjection = new HashMap<>();

	public Stack<SerializableParsedTupleQuery> queryProfilesStack = new Stack<>();

	public SerializableParsedTupleQuery currentQueryProfile = new SerializableParsedTupleQuery();
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	public Map<Projection, SerializableParsedTupleQuery> queriesByProjection = new HashMap<>();

	public Stack<SerializableParsedTupleQuery> queryProfilesStack = new Stack<>();

	public SerializableParsedTupleQuery currentQueryProfile = new SerializableParsedTupleQuery();
```

## RuleId[id=LongLiteralsEndingWithLowercaseL]
### LongLiteralsEndingWithLowercaseL
'long' literal `1000l` ends with lowercase 'l'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
	private static final int LINE_WRAP = 80;

	private static final long DEFAULT_BUFFER_SIZE = 1000l;

	private static final IRI FIRST = new SimpleIRI(RDF.FIRST.stringValue()) {
```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/InsertData.java`
#### Snippet
```java

	@Override
	public InsertData clone() {
		return new InsertData(dataBlock);
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Add.java`
#### Snippet
```java

	@Override
	public Add clone() {
		Add clone = new Add();
		clone.setSilent(isSilent());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Modify.java`
#### Snippet
```java

	@Override
	public Modify clone() {

		TupleExpr deleteClone = deleteExpr != null ? deleteExpr.clone() : null;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Clear.java`
#### Snippet
```java

	@Override
	public Clear clone() {
		Clear clone = new Clear();
		clone.setSilent(isSilent());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Load.java`
#### Snippet
```java

	@Override
	public Load clone() {
		Load clone = new Load(source.clone());
		clone.setSilent(isSilent());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/DeleteData.java`
#### Snippet
```java

	@Override
	public DeleteData clone() {
		return new DeleteData(dataBlock);
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Move.java`
#### Snippet
```java

	@Override
	public Move clone() {
		Move clone = new Move();
		clone.setSilent(isSilent());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Copy.java`
#### Snippet
```java

	@Override
	public Copy clone() {
		Copy clone = new Copy();
		clone.setSilent(isSilent());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Var.java`
#### Snippet
```java

	@Override
	public Var clone() {
		return new Var(name, value, anonymous, constant);
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/ValueExprTripleRef.java`
#### Snippet
```java

	@Override
	public ValueExprTripleRef clone() {
		return new ValueExprTripleRef(exprVarName, subjectVar.clone(), predicateVar.clone(), objectVar.clone());
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Create.java`
#### Snippet
```java

	@Override
	public Create clone() {
		Create clone = new Create();
		clone.setSilent(isSilent());
```

## RuleId[id=StaticPseudoFunctionalStyleMethod]
### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneDocumentScore.java`
#### Snippet
```java
			return null;
		}
		return Iterables.transform(values, (String text) -> index.getSnippet(field, text, highlighter));
	}
}
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneQuery.java`
#### Snippet
```java
			docs = index.search(query);
		}
		return Iterables.transform(Arrays.asList(docs.scoreDocs), new Function<>() {

			@Override
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
			docs = search(q);
		}
		return Iterables.transform(Arrays.asList(docs.scoreDocs),
				(ScoreDoc doc) -> new LuceneDocumentScore(doc, highlighter, LuceneIndex.this));
	}
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
			fields.add(SearchFields.CONTEXT_FIELD_NAME);
		}
		return Iterables.transform(Arrays.asList(docs.scoreDocs),
				(ScoreDoc doc) -> new LuceneDocumentResult(doc, LuceneIndex.this, fields));
	}
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		TopDocs docs = search(q);
		final boolean requireContext = (contextVar != null && !contextVar.hasValue());
		return Iterables.transform(Arrays.asList(docs.scoreDocs),
				(ScoreDoc doc) -> new LuceneDocumentDistance(doc, geoField, units, boundingCircle.getCenter(),
						requireContext,
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
	protected synchronized Iterable<? extends SearchDocument> getDocuments(String resourceId) throws IOException {
		List<Document> docs = getDocuments(new Term(SearchFields.URI_FIELD_NAME, resourceId));
		return Iterables.transform(docs, (Document doc) -> new LuceneDocument(doc, geoStrategyMapper));
	}

```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrSearchQuery.java`
#### Snippet
```java
		SolrDocumentList results = response.getResults();
		final Map<String, Map<String, List<String>>> highlighting = response.getHighlighting();
		return Iterables.transform(results, (SolrDocument document) -> {
			SolrSearchDocument doc = new SolrSearchDocument(document);
			Map<String, List<String>> docHighlighting = (highlighting != null) ? highlighting.get(doc.getId())
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java
			throw new IOException(e);
		}
		return Iterables.transform(docs, SolrSearchDocument::new);
	}

```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java

		SolrDocumentList results = response.getResults();
		return Iterables.transform(results, (SolrDocument document) -> {
			SolrSearchDocument doc = new SolrSearchDocument(document);
			return new SolrDocumentResult(doc);
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java

		SolrDocumentList results = response.getResults();
		return Iterables.transform(results, (SolrDocument document) -> {
			SolrSearchDocument doc = new SolrSearchDocument(document);
			return new SolrDocumentDistance(doc, units);
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java
		SolrDocumentList results = response.getResults();
		final Map<String, Map<String, List<String>>> highlighting = response.getHighlighting();
		return Iterables.transform(results, (SolrDocument document) -> {
			SolrSearchDocument doc = new SolrSearchDocument(document);
			Map<String, List<String>> docHighlighting = (highlighting != null) ? highlighting.get(doc.getId())
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchQuery.java`
#### Snippet
```java
			hits = index.search(request, qb);
		}
		return Iterables.transform(hits, new Function<>() {

			@Override
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchDocumentScore.java`
#### Snippet
```java
			return null;
		}
		return Iterables.transform(Arrays.asList(highlightField.getFragments()),
				(Text fragment) -> SearchFields.getSnippet(fragment.string()));
	}
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
		SearchHits hits = search(request, qb);
		final GeoPoint srcPoint = new GeoPoint(lat, lon);
		return Iterables.transform(hits, (Function<SearchHit, DocumentDistance>) hit -> {
			return new ElasticsearchDocumentDistance(hit, geoContextMapper, fieldName, units, srcPoint, unit);
		});
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
			hits = search(request, qb);
		}
		return Iterables.transform(hits, new Function<>() {

			@Override
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
	protected Iterable<? extends SearchDocument> getDocuments(String resourceId) throws IOException {
		SearchHits hits = getDocuments(QueryBuilders.termQuery(SearchFields.URI_FIELD_NAME, resourceId));
		return Iterables.transform(hits, new Function<>() {

			@Override
```

### StaticPseudoFunctionalStyleMethod
Pseudo functional style code
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
		SearchRequestBuilder request = client.prepareSearch();
		SearchHits hits = search(request, QueryBuilders.boolQuery().must(qb).filter(fb));
		return Iterables.transform(hits, new Function<>() {

			@Override
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepositoryConnection.java`
#### Snippet
```java
			addModel(m);
		} else {
			logger.debug("adding statement in txn: {} {} {} {}", new Object[] { subject, predicate, object, contexts });
			super.add(subject, predicate, object, contexts);
		}
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
	 */
	private static final Set<Character> mark = new HashSet<>(
			Arrays.asList(new Character[] { '-', '_', '.', '!', '~', '*', '\'', '(', ')' }));

	/**
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
	 */
	private static final Set<Character> reserved = new HashSet<>(
			Arrays.asList(new Character[] { ';', '/', '?', ':', '@', '&', '=', '+', '$', ',' }));

	/**
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/util/TypeFilteringRepositoryManager.java`
#### Snippet
```java
				logger.debug(
						"Surpressing retrieval of repository {}: repository type {} did not match expected type {}",
						new Object[] { result.getID(), result.getRepositoryImplConfig().getType(), type });

				result = null;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepositoryConnection.java`
#### Snippet
```java
		try {
			// always call receiveTransactionSettings(...) before calling begin();
			sailConnection.setTransactionSettings(new TransactionSetting[0]);

			if (level != null) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepositoryConnection.java`
#### Snippet
```java
		try {
			// always call receiveTransactionSettings(...) before calling begin();
			sailConnection.setTransactionSettings(new TransactionSetting[0]);

			if (getIsolationLevel() != null) {
```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/IsolationLevelAdapter.java`
#### Snippet
```java
			return determineIsolationLevel(sail, IsolationLevels.READ_UNCOMMITTED);
		case TransactionDefinition.ISOLATION_REPEATABLE_READ:
			throw new InvalidIsolationLevelException(
					"Unsupported isolation level for sail: " + sail + ": " + springIsolation);
		case TransactionDefinition.ISOLATION_SERIALIZABLE:
			return determineIsolationLevel(sail, IsolationLevels.SERIALIZABLE);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/TxnStatusFile.java`
#### Snippet
```java
			break;
		case TxnStatus.OLD_NONE_BYTE:
			status = TxnStatus.NONE;
			break;
		case TxnStatus.ACTIVE_BYTE:
```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`[\\p{Print}&&[^\\}]]` can be simplified to '\\p{Print}\&\&\[\^\\}\]'
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/ConfigTemplate.java`
#### Snippet
```java
	 *-----------*/

	private static final Pattern TOKEN_PATTERN = Pattern.compile("\\{%[\\p{Print}&&[^\\}]]+%\\}");

	/*-----------*
```

## RuleId[id=OctalLiteral]
### OctalLiteral
Octal integer `0664`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java

			dbDir = Files.createTempDirectory(cacheDir.toPath(), "txncache");
			E(mdb_env_open(env, dbDir.toAbsolutePath().toString(), flags, 0664));
			dbiExplicit = openDatabase(env, "quads", MDB_CREATE, null);
			dbiInferred = openDatabase(env, "quads-inf", MDB_CREATE, null);
```

### OctalLiteral
Octal integer `0664`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
			flags |= MDB_NOSYNC | MDB_NOMETASYNC;
		}
		E(mdb_env_open(env, dir.getAbsolutePath(), flags, 0664));

		// Open database
```

### OctalLiteral
Octal integer `0664`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
			flags |= MDB_NOSYNC | MDB_NOMETASYNC;
		}
		E(mdb_env_open(env, this.dir.getAbsolutePath(), flags, 0664));

		txnManager = new TxnManager(env, Mode.RESET);
```

### OctalLiteral
Octal integer `01`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java

		conn.add(node4, FOAF.AGE, literal(19));
		conn.add(node4, FOAF.AGE, literal(ZonedDateTime.of(2022, 01, 01, 01, 01, 01, 01, ZoneId.of("UTC"))));

		conn.add(node1, FOAF.AGE, literal(23));
```

### OctalLiteral
Octal integer `01`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java

		conn.add(node4, FOAF.AGE, literal(19));
		conn.add(node4, FOAF.AGE, literal(ZonedDateTime.of(2022, 01, 01, 01, 01, 01, 01, ZoneId.of("UTC"))));

		conn.add(node1, FOAF.AGE, literal(23));
```

### OctalLiteral
Octal integer `01`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java

		conn.add(node4, FOAF.AGE, literal(19));
		conn.add(node4, FOAF.AGE, literal(ZonedDateTime.of(2022, 01, 01, 01, 01, 01, 01, ZoneId.of("UTC"))));

		conn.add(node1, FOAF.AGE, literal(23));
```

### OctalLiteral
Octal integer `01`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java

		conn.add(node4, FOAF.AGE, literal(19));
		conn.add(node4, FOAF.AGE, literal(ZonedDateTime.of(2022, 01, 01, 01, 01, 01, 01, ZoneId.of("UTC"))));

		conn.add(node1, FOAF.AGE, literal(23));
```

### OctalLiteral
Octal integer `01`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java

		conn.add(node4, FOAF.AGE, literal(19));
		conn.add(node4, FOAF.AGE, literal(ZonedDateTime.of(2022, 01, 01, 01, 01, 01, 01, ZoneId.of("UTC"))));

		conn.add(node1, FOAF.AGE, literal(23));
```

### OctalLiteral
Octal integer `01`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java

		conn.add(node4, FOAF.AGE, literal(19));
		conn.add(node4, FOAF.AGE, literal(ZonedDateTime.of(2022, 01, 01, 01, 01, 01, 01, ZoneId.of("UTC"))));

		conn.add(node1, FOAF.AGE, literal(23));
```

### OctalLiteral
Octal integer `077`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
					input_stream.backup(curPos - jjmatchedPos - 1);
				}
				if ((jjtoToken[jjmatchedKind >> 6] & (1L << (jjmatchedKind & 077))) != 0L) {
					matchedToken = jjFillToken();
					matchedToken.specialToken = specialToken;
```

### OctalLiteral
Octal integer `077`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
					return matchedToken;
				} else {
					if ((jjtoSpecial[jjmatchedKind >> 6] & (1L << (jjmatchedKind & 077))) != 0L) {
						matchedToken = jjFillToken();
						if (specialToken == null) {
```

### OctalLiteral
Octal integer `077`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		int hiByte = (curChar >> 8);
		int i1 = hiByte >> 6;
		long l1 = 1L << (hiByte & 077);
		int i2 = (curChar & 0xff) >> 6;
		long l2 = 1L << (curChar & 077);
```

### OctalLiteral
Octal integer `077`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		long l1 = 1L << (hiByte & 077);
		int i2 = (curChar & 0xff) >> 6;
		long l2 = 1L << (curChar & 077);
		do {
			switch (jjstateSet[--i]) {
```

### OctalLiteral
Octal integer `077`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	private int jjMoveNfa_0_curCharLessThan128(int startsAt, int i, int kind) {
		long l = 1L << (curChar & 077);
		do {
			switch (jjstateSet[--i]) {
```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `absolute.iri.charAt(iri.length())`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		if (_frag != null) {
			if (this.getFragment() == null) {
				if (absolute.iri.startsWith(this.iri) && absolute.iri.charAt(iri.length()) == '#') {
					return new ParsedIRI(null, null, null, -1, "", null, _frag);
				}
```

### DuplicateExpressions
Multiple occurrences of `absolute.iri.charAt(iri.length())`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		if (_query != null) {
			if (this.getQuery() == null && this.getFragment() == null) {
				if (absolute.iri.startsWith(this.iri) && absolute.iri.charAt(iri.length()) == '?') {
					return new ParsedIRI(null, null, null, -1, "", _query, _frag);
				}
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/error/ErrorInfo.java`
#### Snippet
```java
	public String toString() {
		if (errorType != null) {
			StringBuilder sb = new StringBuilder(64);
			sb.append(errorType);
			sb.append(": ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Filter.java`
#### Snippet
```java
	@Override
	public String getSignature() {
		StringBuilder sb = new StringBuilder(128);

		sb.append(super.getSignature());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/ValueConstant.java`
#### Snippet
```java
	@Override
	public String getSignature() {
		StringBuilder sb = new StringBuilder(64);

		sb.append(super.getSignature());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/TupleFunctionCall.java`
#### Snippet
```java
	@Override
	public String getSignature() {
		StringBuilder sb = new StringBuilder(64);
		sb.append(super.getSignature());
		sb.append(" (").append(uri);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/AggregateFunctionCall.java`
#### Snippet
```java
	@Override
	public String getSignature() {
		StringBuilder sb = new StringBuilder(64);

		sb.append(super.getSignature());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/FunctionCall.java`
#### Snippet
```java
	@Override
	public String getSignature() {
		StringBuilder sb = new StringBuilder(64);

		sb.append(super.getSignature());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/TripleRef.java`
#### Snippet
```java
	@Override
	public String getSignature() {
		StringBuilder sb = new StringBuilder(128);

		sb.append(super.getSignature());
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			// prepare the query
			// search for the term "charly", but only in predicate 1
			StringBuilder buffer = new StringBuilder();
			buffer.append("SELECT ?Resource ?Score ?Snippet \n");
			buffer.append("WHERE { \n");
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			// prepare the query
			// search for the term "charly" in all predicates
			StringBuilder buffer = new StringBuilder();
			buffer.append("SELECT ?Resource ?Score ?Snippet \n");
			buffer.append("WHERE { \n");
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
	public void testComplexQueryOne() throws MalformedQueryException, RepositoryException, QueryEvaluationException {
		// prepare the query
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Resource ?Matching ?Score ");
		buffer.append("WHERE { ?Resource <" + PREDICATE_3 + "> ?Matching. ");
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		// prepare the query
		// search for the term "one", but only in predicate 1
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Resource ?Score \n");
		buffer.append("WHERE {\n");
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java

	static {
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Subject ?Score\n");
		buffer.append("WHERE { ?Subject <" + MATCHES + "> [\n");
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		// prepare the query
		// search for the term "one" with 80% fuzzyness
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Resource ?Score \n");
		buffer.append("WHERE { \n");
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
	public void testComplexQueryTwo() throws MalformedQueryException, RepositoryException, QueryEvaluationException {
		// prepare the query
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Resource ?Matching ?Score ");
		buffer.append("WHERE { ?Resource <" + PREDICATE_3 + "> ?Matching. ");
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
	@Test
	public void testPropertyVar() throws MalformedQueryException, RepositoryException, QueryEvaluationException {
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Resource ?Property \n");
		buffer.append("WHERE {\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/nquads/AbstractNQuadsParserTest.java`
#### Snippet
```java
	private void parsePositiveNQuadsSyntaxTests(TestSuite suite, String fileBasePath, String testLocationBaseUri,
			RepositoryConnection con) throws Exception {
		StringBuilder positiveQuery = new StringBuilder();
		positiveQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		positiveQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder negativeQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/nquads/AbstractNQuadsParserTest.java`
#### Snippet
```java
	private void parseNegativeNQuadsSyntaxTests(TestSuite suite, String fileBasePath, String testLocationBaseUri,
			RepositoryConnection con) throws Exception {
		StringBuilder negativeQuery = new StringBuilder();
		negativeQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		negativeQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveEvalQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/trig/TriGParserTestCase.java`
#### Snippet
```java
	private void parsePositiveTriGEvalTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String manifestBaseUrl, RepositoryConnection con) throws Exception {
		StringBuilder positiveEvalQuery = new StringBuilder();
		positiveEvalQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		positiveEvalQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder negativeEvalQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/trig/TriGParserTestCase.java`
#### Snippet
```java
	private void parseNegativeTriGEvalTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String manifestBaseUrl, RepositoryConnection con) throws Exception {
		StringBuilder negativeEvalQuery = new StringBuilder();
		negativeEvalQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		negativeEvalQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/trig/TriGParserTestCase.java`
#### Snippet
```java
	private void parsePositiveTriGSyntaxTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String testLocationBaseUri, RepositoryConnection con) throws Exception {
		StringBuilder positiveQuery = new StringBuilder();
		positiveQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		positiveQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder negativeQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/trig/TriGParserTestCase.java`
#### Snippet
```java
	private void parseNegativeTriGSyntaxTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String manifestBaseUrl, RepositoryConnection con) throws Exception {
		StringBuilder negativeQuery = new StringBuilder();
		negativeQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		negativeQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder negativeQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/turtle/TurtleParserTestCase.java`
#### Snippet
```java
	private void parseNegativeNTriplesSyntaxTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String manifestBaseUrl, RepositoryConnection con) throws Exception {
		StringBuilder negativeQuery = new StringBuilder();
		negativeQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		negativeQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/turtle/TurtleParserTestCase.java`
#### Snippet
```java
	private void parsePositiveTurtleSyntaxTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String testLocationBaseUri, RepositoryConnection con) throws Exception {
		StringBuilder positiveQuery = new StringBuilder();
		positiveQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		positiveQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveEvalQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/turtle/TurtleParserTestCase.java`
#### Snippet
```java
	private void parsePositiveTurtleEvalTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String manifestBaseUrl, RepositoryConnection con) throws Exception {
		StringBuilder positiveEvalQuery = new StringBuilder();
		positiveEvalQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		positiveEvalQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder negativeEvalQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/turtle/TurtleParserTestCase.java`
#### Snippet
```java
	private void parseNegativeTurtleEvalTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String manifestBaseUrl, RepositoryConnection con) throws Exception {
		StringBuilder negativeEvalQuery = new StringBuilder();
		negativeEvalQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		negativeEvalQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/turtle/TurtleParserTestCase.java`
#### Snippet
```java
	private void parsePositiveNTriplesSyntaxTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String manifestBaseUrl, RepositoryConnection con) throws Exception {
		StringBuilder positiveQuery = new StringBuilder();
		positiveQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		positiveQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder negativeQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/turtle/TurtleParserTestCase.java`
#### Snippet
```java
	private void parseNegativeTurtleSyntaxTests(TestSuite suite, String fileBasePath, String testBaseUrl,
			String manifestBaseUrl, RepositoryConnection con) throws Exception {
		StringBuilder negativeQuery = new StringBuilder();
		negativeQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		negativeQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
			con.add(inputStream, BASE_URL, RDFFormat.TURTLE);

			StringBuilder positiveQuery = new StringBuilder();
			positiveQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
			positiveQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder negativeQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
			}

			StringBuilder negativeQuery = new StringBuilder();
			negativeQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
			negativeQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveEvalQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
			}

			StringBuilder positiveEvalQuery = new StringBuilder();
			positiveEvalQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
			positiveEvalQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder positiveQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/ntriples/AbstractNTriplesParserTest.java`
#### Snippet
```java
	private void parsePositiveNTriplesSyntaxTests(TestSuite suite, String fileBasePath, String testLocationBaseUri,
			RepositoryConnection con) throws Exception {
		StringBuilder positiveQuery = new StringBuilder();
		positiveQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		positiveQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder negativeQuery` can be replaced with 'String'
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/ntriples/AbstractNTriplesParserTest.java`
#### Snippet
```java
	private void parseNegativeNTriplesSyntaxTests(TestSuite suite, String fileBasePath, String testLocationBaseUri,
			RepositoryConnection con) throws Exception {
		StringBuilder negativeQuery = new StringBuilder();
		negativeQuery.append(" PREFIX mf:   <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#>\n");
		negativeQuery.append(" PREFIX qt:   <http://www.w3.org/2001/sw/DataAccess/tests/test-query#>\n");
```

### StringBufferReplaceableByString
`StringBuilder query` can be replaced with 'String'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLSyntaxComplianceTest.java`
#### Snippet
```java
				}

				StringBuilder query = new StringBuilder(512);
				query.append("PREFIX mf: <http://www.w3.org/2001/sw/DataAccess/tests/test-manifest#> ");
				query.append("PREFIX qt: <http://www.w3.org/2001/sw/DataAccess/tests/test-query#> ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleTriple.java`
#### Snippet
```java
	@Override
	public String stringValue() {
		StringBuilder sb = new StringBuilder(256);

		sb.append("<<");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	public String toString() {
		if (Literals.isLanguageLiteral(this)) {
			StringBuilder sb = new StringBuilder(label.length() + language.length() + 3);
			sb.append('"').append(label).append('"');
			sb.append('@').append(language);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
			return sb.toString();
		} else if (org.eclipse.rdf4j.model.vocabulary.XSD.STRING.equals(datatype) || datatype == null) {
			StringBuilder sb = new StringBuilder(label.length() + 2);
			sb.append('"').append(label).append('"');
			return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
			return sb.toString();
		} else {
			StringBuilder sb = new StringBuilder(label.length() + datatype.stringValue().length() + 6);
			sb.append('"').append(label).append('"');
			sb.append("^^<").append(datatype.toString()).append(">");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/ContextStatement.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder(256);

		sb.append(super.toString());
```

### StringBufferReplaceableByString
`StringBuilder graphTemplate` can be replaced with 'String'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/GraphTemplate.java`
#### Snippet
```java
	@Override
	public String getQueryString() {
		StringBuilder graphTemplate = new StringBuilder();

		graphTemplate.append(CONSTRUCT).append(" ").append(triplesTemplate.getQueryString());
```

### StringBufferReplaceableByString
`StringBuilder inExpression` can be replaced with 'String'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/In.java`
#### Snippet
```java
	@Override
	public String getQueryString() {
		StringBuilder inExpression = new StringBuilder();
		inExpression.append(searchTerm.getQueryString()).append(" ");
		inExpression.append(super.getQueryString());
```

### StringBufferReplaceableByString
`StringBuilder subSelect` can be replaced with 'String'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/SubSelect.java`
#### Snippet
```java
	@Override
	public String getQueryString() {
		StringBuilder subSelect = new StringBuilder();

		subSelect.append(super.getQueryString());
```

### StringBufferReplaceableByString
`StringBuilder queryBuilder` can be replaced with 'String'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		testCon.add(context1, publisher, nameBob);

		StringBuilder queryBuilder = new StringBuilder();
		queryBuilder.append(PREFIX_FOAF + FOAF_NS + "> ");
		queryBuilder.append(ASK);
```

### StringBufferReplaceableByString
`StringBuilder queryBuilder` can be replaced with 'String'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		testCon.add(context1, publisher, nameBob);

		StringBuilder queryBuilder = new StringBuilder(128);
		queryBuilder.append(" PREFIX foaf: <" + FOAF_NS + "> \n");
		queryBuilder.append(" CONSTRUCT\n");
```

### StringBufferReplaceableByString
`StringBuilder queryBuilder` can be replaced with 'String'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		testCon.add(bob, mbox, mboxBob, context1);
		testCon.add(context1, publisher, nameBob);
		StringBuilder queryBuilder = new StringBuilder();
		queryBuilder.append(" PREFIX foaf: <" + FOAF_NS + "> \n");
		queryBuilder.append(" SELECT ?name ?mbox \n");
```

### StringBufferReplaceableByString
`StringBuilder queryBuilder` can be replaced with 'String'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		testCon.add(alexander, name, Александър);

		StringBuilder queryBuilder = new StringBuilder();
		queryBuilder.append(" PREFIX foaf: <" + FOAF_NS + "> \n");
		queryBuilder.append(" SELECT ?person \n");
```

### StringBufferReplaceableByString
`StringBuilder queryBuilder` can be replaced with 'String'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		testCon.add(bob, mbox, mboxBob, context1);
		testCon.add(context1, publisher, nameBob);
		StringBuilder queryBuilder = new StringBuilder(128);
		queryBuilder.append(" PREFIX foaf: <" + FOAF_NS + "> \n");
		queryBuilder.append(" SELECT ?name ?mbox");
```

### StringBufferReplaceableByString
`StringBuilder queryBuilder` can be replaced with 'String'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		testCon.add(context1, publisher, nameBob);

		StringBuilder queryBuilder = new StringBuilder(64);
		queryBuilder.append(PREFIX_FOAF + FOAF_NS + "> ");
		queryBuilder.append(ASK);
```

### StringBufferReplaceableByString
`StringBuilder queryBuilder` can be replaced with 'String'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		testCon.add(context1, publisher, nameBob);
		testCon.commit();
		StringBuilder queryBuilder = new StringBuilder(128);
		queryBuilder.append(" PREFIX foaf: <" + FOAF_NS + "> \n");
		queryBuilder.append(" CONSTRUCT\n");
```

### StringBufferReplaceableByString
`StringBuilder queryBuilder` can be replaced with 'String'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java

		testCon.add(alexander, name, Александър);
		StringBuilder queryBuilder = new StringBuilder(128);
		queryBuilder.append(" PREFIX foaf: <" + FOAF_NS + "> \n");
		queryBuilder.append(" SELECT ?person");
```

### StringBufferReplaceableByString
`StringBuilder idBuilder` can be replaced with 'String'
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/SearchFields.java`
#### Snippet
```java

	public static String formIdString(String resourceId, String contextId) {
		StringBuilder idBuilder = new StringBuilder(resourceId);
		idBuilder.append("|");
		idBuilder.append(contextId);
```

### StringBufferReplaceableByString
`StringBuffer sb` can be replaced with 'String'
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlTupleExprRenderer.java`
#### Snippet
```java

	String renderPattern(StatementPattern thePattern) throws Exception {
		StringBuffer sb = new StringBuffer();
		sb.append(renderValueExpr(thePattern.getSubjectVar())).append(" ");
		sb.append(renderValueExpr(thePattern.getPredicateVar())).append(" ");
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/UnionScopeChangeOptimizer.java`
#### Snippet
```java
		public void meet(Projection subselect) {
			// do not check deeper in the tree
			return;
		}

```

## RuleId[id=NonShortCircuitBoolean]
### NonShortCircuitBoolean
Non-short-circuit boolean expression `removed | statements.remove( SimpleValueFactory.getInstance().createStatement(subj, pr...`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
			boolean removed = false;
			for (Resource context : contexts) {
				removed = removed
						| statements.remove(
								SimpleValueFactory.getInstance().createStatement(subj, pred, obj, context)) != null;
			}
			return removed;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `added | statements.put(statement, statement) == null`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
			for (Resource context : contexts) {
				Statement statement = SimpleValueFactory.getInstance().createStatement(subj, pred, obj, context);
				added = added
						| statements.put(statement, statement) == null;
			}
			return added;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `codePoint == ':' | codePoint == '\\'`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
	 */
	private static boolean isNameChar(int codePoint) {
		return isPN_CHARS(codePoint) || codePoint == '.' || codePoint == ':' | codePoint == '\\' || codePoint == '%';
	}

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `modified |= remove(i.next())`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/AbstractModel.java`
#### Snippet
```java
			try {
				while (i.hasNext()) {
					modified |= remove(i.next());
				}
			} finally {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `modified |= remove(i.next())`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/AbstractModel.java`
#### Snippet
```java
			try {
				while (i.hasNext()) {
					modified |= remove(i.next());
				}
			} finally {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= addModelStatement(st)`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
			ModelNode<Resource> c = asNode((Resource) ctx);
			ModelStatement st = new ModelStatement(s, p, o, c);
			changed |= addModelStatement(st);
		}
		return changed;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= remove(subj, pred, obj, (Resource[]) null)`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
			for (Resource ctx : contexts) {
				if (ctx == null) {
					changed |= remove(subj, pred, obj, (Resource[]) null);
				} else {
					Iterator<Statement> iter = matchPattern(subj, pred, obj, ctx);
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= tree.add(st)`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
				Statement st = new TreeStatement(subj, pred, obj, (Resource) ctx);
				for (StatementTree tree : trees) {
					changed |= tree.add(st);
				}
			}
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `codePoint == ':' | codePoint == '\\'`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
	 */
	public static boolean isNameChar(int codePoint) {
		return isPN_CHARS(codePoint) || codePoint == '.' || codePoint == ':' | codePoint == '\\' || codePoint == '%';
	}

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `set.remove(SESAME.NIL) | set.remove(RDF4J.NIL)`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
		}

		if (set.remove(SESAME.NIL) | set.remove(RDF4J.NIL)) {
			set.add(null);
		}
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result |= wasNew`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeSailStore.java`
#### Snippet
```java
						contextStore.increment(context);
					}
					result |= wasNew;
				}
			} catch (IOException e) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `success &= nioFile.delete()`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java

			boolean success = allocatedNodesList.delete();
			success &= nioFile.delete();
			return success;
		}
```

## RuleId[id=JUnit5AssertionsConverter]
### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
	}

```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			operation.execute();
		} catch (ClassCastException e) {
			fail("subject-literal triple pattern should be silently ignored");
		}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		}

		assertTrue(
				con.hasStatement((Resource) null, RDF.TYPE, con.getValueFactory().createIRI("urn:TestSubject"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI complexAge = f.createIRI(EX_NS, "complexAge");

		assertTrue(con.hasStatement(bob, age, null, true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		RepositoryResult<Statement> sts = con.getStatements(bob, complexAge, null, true);

		assertTrue(sts.hasNext());

		Value v1 = sts.next().getObject();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		sts = con.getStatements(null, RDF.VALUE, null, true);

		assertTrue(sts.hasNext());

		Value v2 = sts.next().getSubject();
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Value v2 = sts.next().getSubject();

		assertEquals(v1, v2);

		sts.close();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		TupleQueryResult result = con.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate();

		assertTrue(result.hasNext());

		BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		BindingSet bs = result.next();

		assertFalse(result.hasNext());

	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI book1 = f.createIRI(EX_NS, "book1");

		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true, graph1));
		assertFalse(con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true, graph1));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true, graph1));
		assertFalse(con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true, graph1));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "two new statements about ex:book1 should have been inserted in graph1";
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true, graph1));
		assertTrue(msg, con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "two new statements about ex:book1 should have been inserted in graph1";
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true, graph1));
		assertTrue(msg, con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertFalse(con.hasStatement(bob, RDFS.LABEL, null, true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertFalse(con.hasStatement(bob, RDFS.LABEL, null, true));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
				f.createIRI("urn:g1")));

		assertFalse(con.hasStatement(f.createIRI("urn:s2"), f.createIRI("urn:p2"), f.createIRI("urn:o2"), true,
				f.createIRI("urn:g1")));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
				f.createIRI("urn:g1")));
		operation.execute();
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s2"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s2"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s2"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
		assertTrue(con.hasStatement(f.createIRI("urn:s2"), f.createIRI("urn:p2"), f.createIRI("urn:o2"), true,
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
		assertTrue(con.hasStatement(f.createIRI("urn:s2"), f.createIRI("urn:p2"), f.createIRI("urn:o2"), true,
				f.createIRI("urn:g1")));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		// Verify statements exist in the named graphs.
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify statements exist in the default graph.
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		// Verify statements exist in the named graphs.
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify statements exist in the default graph.
		assertTrue(con.hasStatement(null, null, null, false, new Resource[] { null }));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify statements exist in the default graph.
		assertTrue(con.hasStatement(null, null, null, false, new Resource[] { null }));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify that no statements remain in the 'default' graph.
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify that no statements remain in the 'default' graph.
		assertFalse(con.hasStatement(null, null, null, false, new Resource[] { null }));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify that no statements remain in the 'default' graph.
		assertFalse(con.hasStatement(null, null, null, false, new Resource[] { null }));

	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false));

	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertFalse(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertFalse(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertFalse(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));

	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		updInsert.execute();

		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, true, graph2));
		assertTrue(con.hasStatement(alice, FOAF.MBOX, f.createLiteral("alice@example.org"), true, graph2));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, true, graph2));
		assertTrue(con.hasStatement(alice, FOAF.MBOX, f.createLiteral("alice@example.org"), true, graph2));

		String update = getNamespaceDeclarations() +
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "statement should have been deleted.";
		assertFalse(msg, con.hasStatement(alice, FOAF.KNOWS, bob, true, graph2));
		assertFalse(msg, con.hasStatement(alice, FOAF.MBOX, f.createLiteral("alice@example.org"), true, graph2));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "statement should have been deleted.";
		assertFalse(msg, con.hasStatement(alice, FOAF.KNOWS, bob, true, graph2));
		assertFalse(msg, con.hasStatement(alice, FOAF.MBOX, f.createLiteral("alice@example.org"), true, graph2));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.setBinding("z", f.createLiteral("person"));

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("person"), true));
		assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("person"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("person"), true));
		assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("person"), true));

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("person"), true));

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String message = "label should have been inserted in default graph, for ex:bob only";
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String message = "label should have been inserted in default graph, for ex:bob only";
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "statements about book1 should have been removed from bookStore";
		assertFalse(msg, con.hasStatement(book1, null, null, true, bookStore));

		msg = "statements about book1 should have been added to bookStore2";
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		msg = "statements about book1 should have been added to bookStore2";
		assertTrue(msg, con.hasStatement(book1, RDF.TYPE, null, true, bookStore2));
		assertTrue(msg, con.hasStatement(book1, DC.DATE, null, true, bookStore2));
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, null, true, bookStore2));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		msg = "statements about book1 should have been added to bookStore2";
		assertTrue(msg, con.hasStatement(book1, RDF.TYPE, null, true, bookStore2));
		assertTrue(msg, con.hasStatement(book1, DC.DATE, null, true, bookStore2));
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, null, true, bookStore2));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(msg, con.hasStatement(book1, RDF.TYPE, null, true, bookStore2));
		assertTrue(msg, con.hasStatement(book1, DC.DATE, null, true, bookStore2));
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, null, true, bookStore2));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true, graph1));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true, graph2));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true, graph1));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true, graph2));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "statements about bob should have been removed from graph1";
		assertFalse(msg, con.hasStatement(bob, null, null, true, graph1));

		msg = "statements about bob should have been added to graph2";
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		msg = "statements about bob should have been added to graph2";
		assertTrue(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true, graph2));
		assertTrue(msg, con.hasStatement(bob, FOAF.MBOX, null, true, graph2));
		assertTrue(msg, con.hasStatement(bob, FOAF.KNOWS, alice, true, graph2));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		msg = "statements about bob should have been added to graph2";
		assertTrue(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true, graph2));
		assertTrue(msg, con.hasStatement(bob, FOAF.MBOX, null, true, graph2));
		assertTrue(msg, con.hasStatement(bob, FOAF.KNOWS, alice, true, graph2));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true, graph2));
		assertTrue(msg, con.hasStatement(bob, FOAF.MBOX, null, true, graph2));
		assertTrue(msg, con.hasStatement(bob, FOAF.KNOWS, alice, true, graph2));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false));

	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI book2 = f.createIRI(EX_NS, "book2");

		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertFalse(con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));
		assertFalse(con.hasStatement(book2, DC.CREATOR, f.createLiteral("George"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertFalse(con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));
		assertFalse(con.hasStatement(book2, DC.CREATOR, f.createLiteral("George"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertFalse(con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));
		assertFalse(con.hasStatement(book2, DC.CREATOR, f.createLiteral("George"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "newly inserted statement missing";
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertTrue(msg, con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));
		assertTrue(msg, con.hasStatement(book2, DC.CREATOR, f.createLiteral("George"), true));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "newly inserted statement missing";
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertTrue(msg, con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));
		assertTrue(msg, con.hasStatement(book2, DC.CREATOR, f.createLiteral("George"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertTrue(msg, con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));
		assertTrue(msg, con.hasStatement(book2, DC.CREATOR, f.createLiteral("George"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI age = f.createIRI(EX_NS, "age");

		assertFalse(con.hasStatement(alice, age, null, true));
		assertTrue(con.hasStatement(bob, age, null, true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(alice, age, null, true));
		assertTrue(con.hasStatement(bob, age, null, true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		}

		assertTrue(con.hasStatement(bob, age, f.createLiteral("43", XSD.INTEGER), true));

		result = con.getStatements(alice, age, null, true);
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			System.out.println(result.next());
		}
		assertFalse(con.hasStatement(alice, age, null, true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.ALT));
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.BAG));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF4J.NIL));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.ALT));
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.BAG));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF4J.NIL));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.ALT));
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.BAG));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF4J.NIL));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, (Resource) null));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.BAG));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF4J.NIL));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, (Resource) null));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update1.toString());

			assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

			assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

			operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

			// update should be visible to own connection.
			assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			// update should be visible to own connection.
			assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

			String update2 = getNamespaceDeclarations() +
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			// update should not have resulted in any inserts: where clause is
			// empty.
			assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
		} catch (Exception e) {
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			// empty.
			assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
		} catch (Exception e) {
			if (con.isActive()) {
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String message = "label should have been inserted in graph2, for ex:bob only";
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String message = "label should have been inserted in graph2, for ex:bob only";
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, true, graph1));
		assertFalse(con.hasStatement(alice, FOAF.KNOWS, bob, true, graph2));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, true, graph1));
		assertFalse(con.hasStatement(alice, FOAF.KNOWS, bob, true, graph2));
		operation.execute();

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "statement should have not have been deleted from graph1";
		assertTrue(msg, con.hasStatement(alice, FOAF.KNOWS, bob, true, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));

	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI book1 = f.createIRI(EX_NS, "book1");

		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1", "en"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "new statement about ex:book1 should have been inserted";
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1", "en"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		// Verify statements exist in the named graphs.
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify statements exist in the default graph.
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		// Verify statements exist in the named graphs.
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify statements exist in the default graph.
		assertTrue(con.hasStatement(null, null, null, false, new Resource[] { null }));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify statements exist in the default graph.
		assertTrue(con.hasStatement(null, null, null, false, new Resource[] { null }));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify that no statements remain in the 'default' graph.
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify that no statements remain in the 'default' graph.
		assertFalse(con.hasStatement(null, null, null, false, new Resource[] { null }));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		// Verify that no statements remain in the 'default' graph.
		assertFalse(con.hasStatement(null, null, null, false, new Resource[] { null }));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertFalse(con.hasStatement(null, null, null, false, graph1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertFalse(con.hasStatement(null, null, null, false, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, (Resource) null));
		assertFalse(con.hasStatement(null, null, null, false, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertFalse(con.hasStatement(null, DC.TITLE, f.createLiteral("book 1"), true));
		assertFalse(con.hasStatement(null, DC.CREATOR, f.createLiteral("Ringo"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(null, DC.TITLE, f.createLiteral("book 1"), true));
		assertFalse(con.hasStatement(null, DC.CREATOR, f.createLiteral("Ringo"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		RepositoryResult<Statement> titleStatements = con.getStatements(null, DC.TITLE, f.createLiteral("book 1"),
				true);
		assertNotNull(titleStatements);

		RepositoryResult<Statement> creatorStatements = con.getStatements(null, DC.CREATOR, f.createLiteral("Ringo"),
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		RepositoryResult<Statement> creatorStatements = con.getStatements(null, DC.CREATOR, f.createLiteral("Ringo"),
				true);
		assertNotNull(creatorStatements);

		BNode bookNode = null;
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		if (titleStatements.hasNext()) {
			Statement ts = titleStatements.next();
			assertFalse(titleStatements.hasNext());

			Resource subject = ts.getSubject();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

			Resource subject = ts.getSubject();
			assertTrue(subject instanceof BNode);
			bookNode = (BNode) subject;
		}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		}
		titleStatements.close();
		assertNotNull(bookNode);
		assertFalse("_:foo".equals(bookNode.getID()));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		titleStatements.close();
		assertNotNull(bookNode);
		assertFalse("_:foo".equals(bookNode.getID()));

		if (creatorStatements.hasNext()) {
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		if (creatorStatements.hasNext()) {
			Statement cs = creatorStatements.next();
			assertFalse(creatorStatements.hasNext());

			Resource subject = cs.getSubject();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

			Resource subject = cs.getSubject();
			assertTrue(subject instanceof BNode);
			assertEquals(bookNode, subject);
		} else {
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			Resource subject = cs.getSubject();
			assertTrue(subject instanceof BNode);
			assertEquals(bookNode, subject);
		} else {
			fail("at least one creator statement expected");
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			assertEquals(bookNode, subject);
		} else {
			fail("at least one creator statement expected");
		}
		creatorStatements.close();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, true));
		operation.execute();

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "statement should have been deleted.";
		assertFalse(msg, con.hasStatement(alice, FOAF.KNOWS, bob, true));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
				f.createIRI("urn:g1")));
		operation.execute();
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertFalse(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertFalse(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertFalse(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		msg = "foaf:name properties with value 'foo' should have been added";
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		msg = "foaf:name properties with value 'foo' should have been added";
		assertTrue(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("foo"), true));
		assertTrue(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("foo"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		msg = "foaf:name properties with value 'foo' should have been added";
		assertTrue(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("foo"), true));
		assertTrue(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("foo"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI book1 = f.createIRI(EX_NS, "book1");

		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertFalse(con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertFalse(con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "two new statements about ex:book1 should have been inserted";
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertTrue(msg, con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "two new statements about ex:book1 should have been inserted";
		assertTrue(msg, con.hasStatement(book1, DC.TITLE, f.createLiteral("book 1"), true));
		assertTrue(msg, con.hasStatement(book1, DC.CREATOR, f.createLiteral("Ringo"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(alice, FOAF.NAME, null, false, graph2));
		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph2));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(alice, FOAF.NAME, null, false, graph2));
		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph2));
		assertFalse(con.hasStatement(alice, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph2));
		assertFalse(con.hasStatement(alice, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph2));
		assertFalse(con.hasStatement(alice, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI book1 = f.createIRI(EX_NS, "book1");

		assertFalse(con.hasStatement(book1, DC.TITLE, f.createLiteral("the number four", XSD.INTEGER), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "new statement about ex:book1 should have been inserted";

		assertTrue(msg,
				con.hasStatement(book1, DC.TITLE, f.createLiteral("the number four", CoreDatatype.XSD.INTEGER), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.setBinding("y", f.createLiteral("Bob"));

		assertFalse(con.hasStatement(bob, RDFS.LABEL, null, true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertFalse(con.hasStatement(bob, RDFS.LABEL, null, true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertFalse(con.hasStatement(null, null, null, false, newGraph));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertFalse(con.hasStatement(null, null, null, false, newGraph));
		assertTrue(con.hasStatement(null, null, null, false));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertFalse(con.hasStatement(null, null, null, false, newGraph));
		assertTrue(con.hasStatement(null, null, null, false));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertFalse(con.hasStatement(null, null, null, false, newGraph));
		assertTrue(con.hasStatement(null, null, null, false));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		msg = "foaf:knows properties should not have been deleted";
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		msg = "foaf:knows properties should not have been deleted";
		assertTrue(msg, con.hasStatement(bob, FOAF.KNOWS, null, true));
		assertTrue(msg, con.hasStatement(alice, FOAF.KNOWS, null, true));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		msg = "foaf:knows properties should not have been deleted";
		assertTrue(msg, con.hasStatement(bob, FOAF.KNOWS, null, true));
		assertTrue(msg, con.hasStatement(alice, FOAF.KNOWS, null, true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		msg = "foaf:name properties with value 'foo' should not have been added";
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		msg = "foaf:name properties with value 'foo' should not have been added";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("foo"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("foo"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		msg = "foaf:name properties with value 'foo' should not have been added";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("foo"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("foo"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(alice, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(alice, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(alice, FOAF.NAME, null, false, graph2));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Literal inCorrectAgeValue = f.createLiteral("46", XSD.INTEGER);

		assertTrue(con.hasStatement(bob, age, originalAgeValue, true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertFalse(con.hasStatement(bob, age, originalAgeValue, true));
		assertTrue(con.hasStatement(bob, age, correctAgeValue, true));
		assertFalse(con.hasStatement(bob, age, inCorrectAgeValue, true));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, age, originalAgeValue, true));
		assertTrue(con.hasStatement(bob, age, correctAgeValue, true));
		assertFalse(con.hasStatement(bob, age, inCorrectAgeValue, true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(bob, age, originalAgeValue, true));
		assertTrue(con.hasStatement(bob, age, correctAgeValue, true));
		assertFalse(con.hasStatement(bob, age, inCorrectAgeValue, true));
	}

```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			operation.execute();

			fail("creation of existing graph should have resulted in error.");
		} catch (UpdateExecutionException e) {
			// expected behavior
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.setBinding("x", bob);

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Literal mboxBob = f.createLiteral("bob@example.org");
		Literal mboxAlice = f.createLiteral("alice@example.org");
		assertTrue(con.hasStatement(bob, FOAF.MBOX, mboxBob, true));
		assertTrue(con.hasStatement(alice, FOAF.MBOX, mboxAlice, true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Literal mboxAlice = f.createLiteral("alice@example.org");
		assertTrue(con.hasStatement(bob, FOAF.MBOX, mboxBob, true));
		assertTrue(con.hasStatement(alice, FOAF.MBOX, mboxAlice, true));

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(alice, FOAF.MBOX, mboxAlice, true));

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertFalse(con.hasStatement(bob, FOAF.MBOX, mboxBob, true));
		assertTrue(con.hasStatement(alice, FOAF.MBOX, mboxAlice, true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, FOAF.MBOX, mboxBob, true));
		assertTrue(con.hasStatement(alice, FOAF.MBOX, mboxAlice, true));

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(alice, FOAF.MBOX, mboxAlice, true));

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(human, RDFS.SUBCLASSOF, mammal, true, graph1));
		assertTrue(con.hasStatement(mammal, RDFS.SUBCLASSOF, null, true, graph1));
		assertTrue(con.hasStatement(george, RDF.TYPE, human, true, graph1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(human, RDFS.SUBCLASSOF, mammal, true, graph1));
		assertTrue(con.hasStatement(mammal, RDFS.SUBCLASSOF, null, true, graph1));
		assertTrue(con.hasStatement(george, RDF.TYPE, human, true, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(human, RDFS.SUBCLASSOF, mammal, true, graph1));
		assertTrue(con.hasStatement(mammal, RDFS.SUBCLASSOF, null, true, graph1));
		assertTrue(con.hasStatement(george, RDF.TYPE, human, true, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.setBinding("x", bob);

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bobbie"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, null, true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bobbie"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, null, true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bobbie"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bobbie"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, true));
		assertTrue(con.hasStatement(alice, FOAF.MBOX, f.createLiteral("alice@example.org"), true));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, true));
		assertTrue(con.hasStatement(alice, FOAF.MBOX, f.createLiteral("alice@example.org"), true));
		operation.execute();

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "statement should have been deleted.";
		assertFalse(msg, con.hasStatement(alice, FOAF.KNOWS, bob, true));
		assertFalse(msg, con.hasStatement(alice, FOAF.MBOX, f.createLiteral("alice@example.org"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "statement should have been deleted.";
		assertFalse(msg, con.hasStatement(alice, FOAF.KNOWS, bob, true));
		assertFalse(msg, con.hasStatement(alice, FOAF.MBOX, f.createLiteral("alice@example.org"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		msg = "ex:containsPerson properties should not have been deleted";
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		msg = "ex:containsPerson properties should not have been deleted";
		assertTrue(msg, con.hasStatement(graph1, f.createIRI(EX_NS, "containsPerson"), bob, true));
		assertTrue(msg, con.hasStatement(graph2, f.createIRI(EX_NS, "containsPerson"), alice, true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		msg = "ex:containsPerson properties should not have been deleted";
		assertTrue(msg, con.hasStatement(graph1, f.createIRI(EX_NS, "containsPerson"), bob, true));
		assertTrue(msg, con.hasStatement(graph2, f.createIRI(EX_NS, "containsPerson"), alice, true));

	}
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			operation.execute();
		} catch (ClassCastException e) {
			fail("subject-literal triple pattern should be silently ignored");
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, true, graph1));
		operation.execute();

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "statement should have been deleted from graph1";
		assertFalse(msg, con.hasStatement(alice, FOAF.KNOWS, bob, true, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update.toString());

			assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

			assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

			operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

			// update should be visible to own connection.
			assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
			assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			// update should be visible to own connection.
			assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
			assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

			assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			assertTrue(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

			assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

			assertFalse(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
			assertFalse(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

			try (RepositoryConnection con2 = rep.getConnection()) {
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			try (RepositoryConnection con2 = rep.getConnection()) {
				// update should not yet be visible to separate connection
				assertFalse(con2.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
				assertFalse(con2.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
				// update should not yet be visible to separate connection
				assertFalse(con2.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
				assertFalse(con2.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

				assertTrue(con2.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
				assertFalse(con2.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

				assertTrue(con2.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
				assertTrue(con2.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

				assertTrue(con2.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
				assertTrue(con2.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

				con.commit();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

				// after commit, update should be visible to separate connection.
				assertTrue(con2.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
				assertTrue(con2.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
				// after commit, update should be visible to separate connection.
				assertTrue(con2.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
				assertTrue(con2.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

				assertFalse(con2.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
				assertTrue(con2.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

				assertFalse(con2.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
				assertFalse(con2.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));
			}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

				assertFalse(con2.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
				assertFalse(con2.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));
			}
		} catch (Exception e) {
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String message = "labels should have been inserted in corresponding named graphs only.";
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertTrue(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph2));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String message = "labels should have been inserted in corresponding named graphs only.";
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertTrue(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertTrue(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertTrue(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.ALT));
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.BAG));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF4J.NIL));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.ALT));
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.BAG));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF4J.NIL));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.ALT));
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.BAG));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF4J.NIL));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, (Resource) null));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF.BAG));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, RDF4J.NIL));
		assertFalse(con.hasStatement(RDF.FIRST, RDF.FIRST, RDF.FIRST, true, (Resource) null));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertTrue(con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String msg = "foaf:name properties should have been deleted";
		assertFalse(msg, con.hasStatement(bob, FOAF.NAME, f.createLiteral("Bob"), true));
		assertFalse(msg, con.hasStatement(alice, FOAF.NAME, f.createLiteral("Alice"), true));

	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.setBinding("z", comment);

		assertFalse(con.hasStatement(null, RDFS.COMMENT, comment, true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.COMMENT, comment, true));
		assertFalse(con.hasStatement(alice, RDFS.COMMENT, comment, true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, RDFS.COMMENT, comment, true));
		assertFalse(con.hasStatement(alice, RDFS.COMMENT, comment, true));

	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.setBinding("y", f.createLiteral("Bob"));

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(null, null, null, false, graph1));
		assertTrue(con.hasStatement(null, null, null, false, graph2));
		assertTrue(con.hasStatement(null, null, null, false));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(i18n, FOAF.KNOWS, bob, true));
		operation.execute();

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String msg = "statement should have been deleted.";
		assertFalse(msg, con.hasStatement(i18n, FOAF.KNOWS, bob, true));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.setBinding("x", bob);

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertFalse(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
				f.createIRI("urn:g1")));
		operation.execute();
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), RDF.TYPE, null, true, (Resource) null));
		assertTrue(con.hasStatement(f.createIRI("urn:s1"), f.createIRI("urn:p1"), f.createIRI("urn:o1"), true,
				f.createIRI("urn:g1")));
	}
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			operation.execute();
		} catch (ClassCastException e) {
			fail("subject-literal triple pattern should be silently ignored");
		}
		assertFalse(con.hasStatement(null, FOAF.NAME, null, true));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
			fail("subject-literal triple pattern should be silently ignored");
		}
		assertFalse(con.hasStatement(null, FOAF.NAME, null, true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, f.createIRI(EX_NS, "graph3")));
		assertFalse(con.hasStatement(null, null, null, false, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, f.createIRI(EX_NS, "graph3")));
		assertFalse(con.hasStatement(null, null, null, false, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, f.createIRI(EX_NS, "graph3")));

	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, f.createIRI(EX_NS, "graph3")));
		assertTrue(con.hasStatement(bob, FOAF.NAME, null, false, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		String message = "label should have been inserted in graph1 only, for ex:bob only";
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph2));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		String message = "label should have been inserted in graph1 only, for ex:bob only";
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph1));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph1));
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph1));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertFalse(message, con.hasStatement(bob, RDFS.LABEL, f.createLiteral("Bob"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph2));
		assertFalse(message, con.hasStatement(alice, RDFS.LABEL, f.createLiteral("Alice"), true, graph1));
	}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update);

		assertFalse(con.hasStatement(bob, RDFS.LABEL, null, true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, null, true));

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertFalse(con.hasStatement(bob, RDFS.LABEL, null, true));
		assertFalse(con.hasStatement(alice, RDFS.LABEL, null, true));

		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();

		assertTrue(con.hasStatement(bob, RDFS.LABEL, null, true));
		assertTrue(con.hasStatement(alice, RDFS.LABEL, null, true));
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		assertTrue(con.hasStatement(bob, RDFS.LABEL, null, true));
		assertTrue(con.hasStatement(alice, RDFS.LABEL, null, true));
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

		IRI graph3 = f.createIRI(EX_NS, "graph3");
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI graph3 = f.createIRI(EX_NS, "graph3");
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph1));
		operation.execute();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph1));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph1));
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, graph3));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		operation.execute();
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, graph3));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, graph3));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, graph3));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, graph3));
		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, (Resource) null));
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, graph3));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, graph3));
		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph1));
		assertFalse(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph3));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph1, DC.PUBLISHER, null, false, graph3));
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, graph3));
		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph1));
		assertFalse(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph3));
	}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		assertTrue(con.hasStatement(graph2, DC.PUBLISHER, null, false, graph3));
		assertTrue(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph1));
		assertFalse(con.hasStatement(alice, FOAF.KNOWS, bob, false, graph3));
	}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			while (result.hasNext()) {
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			while (result.hasNext()) {
				BindingSet bs = result.next();
				assertNotNull(bs);

				// System.out.println(bs);
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (Stream<BindingSet> result = tq.evaluate().stream()) {
			long count = result.count();
			assertEquals(0, count);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			while (result.hasNext()) {
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			while (result.hasNext()) {
				BindingSet bs = result.next();
				assertNotNull(bs);

				Resource s = (Resource) bs.getValue("s");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Resource s = (Resource) bs.getValue("s");

				assertNotNull(s);
				assertNotEquals(bob, s); // should not be present in default
				// graph
```

### JUnit5AssertionsConverter
Call to `assertNotEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				assertNotNull(s);
				assertNotEquals(bob, s); // should not be present in default
				// graph
				assertNotEquals(alice, s); // should not be present in
```

### JUnit5AssertionsConverter
Call to `assertNotEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertNotEquals(bob, s); // should not be present in default
				// graph
				assertNotEquals(alice, s); // should not be present in
				// default
				// graph
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		String query = "SELECT (COUNT(*) as ?c) WHERE { VALUES ?v { 1 2 undef 3 4 }}";
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("4", result.next().getValue("c").stringValue());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("4", result.next().getValue("c").stringValue());
			assertFalse(result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("4", result.next().getValue("c").stringValue());
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.hasNext());
			assertEquals("4", result.next().getValue("c").stringValue());
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());

			BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Value n = bs.getValue("n");

			assertNotNull(p);
			assertNotNull(n);
			assertEquals(p, n);
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(p);
			assertNotNull(n);
			assertEquals(p, n);
		}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(p);
			assertNotNull(n);
			assertEquals(p, n);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);

			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
			assertTrue(result.contains(urn4, p2, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
			assertTrue(result.contains(urn4, p2, null));
			assertTrue(result.contains(urn4, blank, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
			assertTrue(result.contains(urn4, p2, null));
			assertTrue(result.contains(urn4, blank, null));
		}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(urn2, p2, null));
			assertTrue(result.contains(urn4, p2, null));
			assertTrue(result.contains(urn4, blank, null));
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult evaluate = tq.evaluate()) {
			List<BindingSet> result = QueryResults.asList(evaluate);
			assertNotNull(result);

			IRI a = vf.createIRI(EX_NS, "a");
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			IRI test = vf.createIRI(EX_NS, "Test");

			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", a)));
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", test)));
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", c)));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", a)));
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", test)));
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", c)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", d)));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", a)));
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", test)));
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", c)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", d)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", e)));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", test)));
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", c)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", d)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", e)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", test)));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", c)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", d)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", e)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", test)));

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", d)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", e)));
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", test)));

			assertFalse(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", b)));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(containsSolution(result, new SimpleBinding("s", d), new SimpleBinding("o", test)));

			assertFalse(containsSolution(result, new SimpleBinding("s", a), new SimpleBinding("o", b)));

		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		String query = "SELECT (COUNT(?v) as ?c) WHERE { VALUES ?v { 1 2 undef 3 4 }}";
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("4", result.next().getValue("c").stringValue());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("4", result.next().getValue("c").stringValue());
			assertFalse(result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("4", result.next().getValue("c").stringValue());
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.hasNext());
			assertEquals("4", result.next().getValue("c").stringValue());
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			// first execute without binding
			assertNotNull(result);

			// System.out.println("--- testArbitraryLengthPathWithBinding7
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				// System.out.println(bs);

				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);

			// execute again, but this time setting a binding
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			try (TupleQueryResult result2 = tq.evaluate()) {
				assertNotNull(result2);

				count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					count++;
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Resource s = (Resource) bs.getValue("s");

				assertNotNull(s);
				assertThat(s).withFailMessage("%s should not be present in nil graph", bob).isNotEqualTo(bob);
				assertThat(s).withFailMessage("%s should not be present in nil graph", alice).isNotEqualTo(alice);
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				count++;
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(2, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(2, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			while (result.hasNext()) {
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			while (result.hasNext()) {
				BindingSet bs = result.next();
				assertNotNull(bs);

				Resource s = (Resource) bs.getValue("s");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Resource s = (Resource) bs.getValue("s");

				assertNotNull(s);
				assertNotEquals(bob, s); // should not be present in default
				// graph
```

### JUnit5AssertionsConverter
Call to `assertNotEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				assertNotNull(s);
				assertNotEquals(bob, s); // should not be present in default
				// graph
				assertNotEquals(alice, s); // should not be present in
```

### JUnit5AssertionsConverter
Call to `assertNotEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertNotEquals(bob, s); // should not be present in default
				// graph
				assertNotEquals(alice, s); // should not be present in
				// default
				// graph
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, qry).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("13.815", result.next().getValue("sec").stringValue());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, qry).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("13.815", result.next().getValue("sec").stringValue());
			assertFalse(result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("13.815", result.next().getValue("sec").stringValue());
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.hasNext());
			assertEquals("13.815", result.next().getValue("sec").stringValue());
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			// first execute without binding
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				count++;
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);

			// execute again, but this time setting a binding
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			try (TupleQueryResult result2 = tq.evaluate()) {
				assertNotNull(result2);

				count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					count++;
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		List<BindingSet> result = QueryResults.asList(q.evaluate());

		assertEquals(2, result.size());

		IRI v1 = f.createIRI("http://example.org/v1");
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);

			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(urn1, blank, null));
			assertTrue(result.contains(urn2, p2, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(urn1, blank, null));
			assertTrue(result.contains(urn2, p2, null));
		}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(urn1, blank, null));
			assertTrue(result.contains(urn2, p2, null));
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			// first execute without binding
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				count++;
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);

			// execute again, but this time setting a binding
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			try (TupleQueryResult result2 = tq.evaluate()) {
				assertNotNull(result2);

				count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					count++;
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(4, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(4, count);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, qb);
		try (TupleQueryResult evaluate = tq.evaluate()) {
			assertFalse("The query should not return a result", evaluate.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		GraphQuery gq = conn.prepareGraphQuery(qry);
		try (GraphQueryResult result = gq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			int resultNo = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (GraphQueryResult result = gq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			int resultNo = 0;
			while (result.hasNext()) {
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Statement st = result.next();
				assertThat(resultNo).isLessThan(correctResult.length);
				assertEquals(correctResult[resultNo], st);
				resultNo++;
			}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				resultNo++;
			}
			assertEquals(correctResult.length, resultNo);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Literal l2 = (Literal) bs.getValue("l2");

			assertNotNull(l1);
			assertNotEquals(l1, l2);
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `assertNotEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(l1);
			assertNotEquals(l1, l2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			Literal uid1 = (Literal) result.next().getValue("uid");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Literal uid2 = (Literal) result.next().getValue("uid");

			assertNotNull(uid1);
			assertNotEquals(uid1, uid2);
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `assertNotEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(uid1);
			assertNotEquals(uid1, uid2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);

			assertNotNull(result);
			assertEquals(4, result.size());
			Set<Value> objects = result.filter(f, p, null).objects();
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(result);
			assertEquals(4, result.size());
			Set<Value> objects = result.filter(f, p, null).objects();
			assertNotNull(objects);
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(4, result.size());
			Set<Value> objects = result.filter(f, p, null).objects();
			assertNotNull(objects);
			for (Value object : objects) {
				if (object instanceof BNode) {
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				if (object instanceof BNode) {
					Set<Value> childObjects = result.filter((BNode) object, null, null).objects();
					assertNotNull(childObjects);
					for (Value childObject : childObjects) {
						if (childObject instanceof BNode) {
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					for (Value childObject : childObjects) {
						if (childObject instanceof BNode) {
							assertTrue(result.contains((BNode) childObject, null, null));
						}
					}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, qry).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("13", result.next().getValue("sec").stringValue());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, qry).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("13", result.next().getValue("sec").stringValue());
			assertFalse(result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("13", result.next().getValue("sec").stringValue());
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.hasNext());
			assertEquals("13", result.next().getValue("sec").stringValue());
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult evaluate = tq.evaluate()) {
			List<BindingSet> result = QueryResults.asList(evaluate);
			assertNotNull(result);
			assertEquals(1, result.size());

```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			List<BindingSet> result = QueryResults.asList(evaluate);
			assertNotNull(result);
			assertEquals(1, result.size());

			assertEquals(vf.createLiteral(Float.NEGATIVE_INFINITY), result.get(0).getValue("min"));
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(1, result.size());

			assertEquals(vf.createLiteral(Float.NEGATIVE_INFINITY), result.get(0).getValue("min"));
			assertEquals(vf.createLiteral(Float.POSITIVE_INFINITY), result.get(0).getValue("max"));
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertEquals(vf.createLiteral(Float.NEGATIVE_INFINITY), result.get(0).getValue("min"));
			assertEquals(vf.createLiteral(Float.POSITIVE_INFINITY), result.get(0).getValue("max"));
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			// first execute without binding
			assertNotNull(result);

			// System.out.println("--- testArbitraryLengthPathWithBinding6
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				// System.out.println(bs);

				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);

			// execute again, but this time setting a binding
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			try (TupleQueryResult result2 = tq.evaluate()) {
				assertNotNull(result2);

				count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					count++;
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				count++;
				assertNotNull(bs);

				// System.out.println(bs);
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				Value l = bs.getValue("l");
				assertTrue(l instanceof Literal);
				assertEquals("label", ((Literal) l).getLabel());

```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Value l = bs.getValue("l");
				assertTrue(l instanceof Literal);
				assertEquals("label", ((Literal) l).getLabel());

				Value opt1 = bs.getValue("opt1");
```

### JUnit5AssertionsConverter
Call to `assertNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				Value opt1 = bs.getValue("opt1");
				assertNull(opt1);

				Value opt2 = bs.getValue("opt2");
```

### JUnit5AssertionsConverter
Call to `assertNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				Value opt2 = bs.getValue("opt2");
				assertNull(opt2);
			}
			assertEquals(1, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertNull(opt2);
			}
			assertEquals(1, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());

			BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			BindingSet bs = result.next();
			Value y = bs.getValue("y");
			assertNotNull(y);
			assertTrue(y instanceof Literal);
			assertEquals(f.createLiteral("1", CoreDatatype.XSD.INTEGER), y);
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Value y = bs.getValue("y");
			assertNotNull(y);
			assertTrue(y instanceof Literal);
			assertEquals(f.createLiteral("1", CoreDatatype.XSD.INTEGER), y);
		}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(y);
			assertTrue(y instanceof Literal);
			assertEquals(f.createLiteral("1", CoreDatatype.XSD.INTEGER), y);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (Stream<BindingSet> result = tq.evaluate().stream()) {
			long count = result.count();
			assertEquals(1, count);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, query);
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			while (result.hasNext()) {
				BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				IRI p = (IRI) bs.getValue("p");
				assertNotNull(p);
				assertEquals(RDF.TYPE, p);
			}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				IRI p = (IRI) bs.getValue("p");
				assertNotNull(p);
				assertEquals(RDF.TYPE, p);
			}
		} catch (Exception e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (Exception e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);
			Set<Resource> subjects = result.filter(null, p, b).subjects();
			assertNotNull(subjects);
			for (Value subject : subjects) {
				if (subject instanceof BNode) {
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			for (Value subject : subjects) {
				if (subject instanceof BNode) {
					assertTrue(result.contains(null, null, subject));
				}
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (GraphQueryResult evaluate = gq.evaluate()) {
			Model result = QueryResults.asModel(evaluate);
			assertTrue(result.contains(a, p, null));
			assertTrue(result.contains(b, RDFS.LABEL, null));
			assertTrue(result.contains(c, RDFS.LABEL, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);
			assertTrue(result.contains(a, p, null));
			assertTrue(result.contains(b, RDFS.LABEL, null));
			assertTrue(result.contains(c, RDFS.LABEL, null));
			assertTrue(result.contains(null, p, b));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(a, p, null));
			assertTrue(result.contains(b, RDFS.LABEL, null));
			assertTrue(result.contains(c, RDFS.LABEL, null));
			assertTrue(result.contains(null, p, b));
			assertTrue(result.contains(e, RDFS.LABEL, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(b, RDFS.LABEL, null));
			assertTrue(result.contains(c, RDFS.LABEL, null));
			assertTrue(result.contains(null, p, b));
			assertTrue(result.contains(e, RDFS.LABEL, null));
			assertTrue(result.contains(null, p, e));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(c, RDFS.LABEL, null));
			assertTrue(result.contains(null, p, b));
			assertTrue(result.contains(e, RDFS.LABEL, null));
			assertTrue(result.contains(null, p, e));
			assertFalse(result.contains(f, null, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(null, p, b));
			assertTrue(result.contains(e, RDFS.LABEL, null));
			assertTrue(result.contains(null, p, e));
			assertFalse(result.contains(f, null, null));
			Set<Value> objects = result.filter(a, p, null).objects();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(e, RDFS.LABEL, null));
			assertTrue(result.contains(null, p, e));
			assertFalse(result.contains(f, null, null));
			Set<Value> objects = result.filter(a, p, null).objects();
			assertNotNull(objects);
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertFalse(result.contains(f, null, null));
			Set<Value> objects = result.filter(a, p, null).objects();
			assertNotNull(objects);
			for (Value object : objects) {
				if (object instanceof BNode) {
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			for (Value object : objects) {
				if (object instanceof BNode) {
					assertTrue(result.contains((Resource) object, null, null));
					assertEquals(2, result.filter((Resource) object, null, null).size());
				}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				if (object instanceof BNode) {
					assertTrue(result.contains((Resource) object, null, null));
					assertEquals(2, result.filter((Resource) object, null, null).size());
				}
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (GraphQueryResult evaluate = gq.evaluate()) {
			Model result = QueryResults.asModel(evaluate);
			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);
			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
		}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		String query = "SELECT (MAX(?v) as ?max) WHERE { VALUES ?v { 1 2 7 undef 3 4 }}";
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("7", result.next().getValue("max").stringValue());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("7", result.next().getValue("max").stringValue());
			assertFalse(result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("7", result.next().getValue("max").stringValue());
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.hasNext());
			assertEquals("7", result.next().getValue("max").stringValue());
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				count++;
				assertNotNull(bs);

				// System.out.println(bs);
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Value l = bs.getValue("l");

				assertTrue(prop1 instanceof Literal || l instanceof Literal);
				if (l instanceof Literal) {
					Value opt1 = bs.getValue("opt1");
```

### JUnit5AssertionsConverter
Call to `assertNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				if (l instanceof Literal) {
					Value opt1 = bs.getValue("opt1");
					assertNull(opt1);

					Value opt2 = bs.getValue("opt2");
```

### JUnit5AssertionsConverter
Call to `assertNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

					Value opt2 = bs.getValue("opt2");
					assertNull(opt2);
				}
			}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				}
			}
			assertEquals(2, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				count++;
				assertNotNull(bs);

				// System.out.println(bs);
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Value x = bs.getValue("x");

				assertTrue(mbox instanceof Literal || x instanceof IRI);
			}
			assertEquals(3, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(mbox instanceof Literal || x instanceof IRI);
			}
			assertEquals(3, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);

			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(urn1, blank, null));
			assertTrue(result.contains(null, blank, urn1));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(urn1, blank, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(urn1, p1, null));
			assertTrue(result.contains(urn1, blank, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
		}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.contains(urn1, blank, null));
			assertTrue(result.contains(null, blank, urn1));
			assertTrue(result.contains(urn2, p2, null));
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			// first execute without binding
			assertNotNull(result);
			// System.out.println("--- testArbitraryLengthPathWithBinding8
			// ---");
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				// System.out.println(bs);

				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);

			// execute again, but this time setting a binding
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			try (TupleQueryResult result2 = tq.evaluate()) {
				assertNotNull(result2);

				count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					count++;
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);

			assertNotNull(result);
			assertFalse(result.isEmpty());
			assertTrue(result.contains(x, p, x));
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(result);
			assertFalse(result.isEmpty());
			assertTrue(result.contains(x, p, x));
		}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertFalse(result.isEmpty());
			assertTrue(result.contains(x, p, x));
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		List<BindingSet> result = QueryResults.asList(conn.prepareTupleQuery(qb).evaluate());

		assertEquals("query should return 2 solutions", 2, result.size());
	}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			while (result.hasNext()) {
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			while (result.hasNext()) {
				BindingSet bs = result.next();
				assertNotNull(bs);

				Value concat = bs.getValue("concat");
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Value concat = bs.getValue("concat");

				assertTrue(concat instanceof Literal);

				String lexValue = ((Literal) concat).getLabel();
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				int occ = countCharOccurrences(lexValue, 'a');
				assertEquals(1, occ);
				occ = countCharOccurrences(lexValue, 'b');
				assertEquals(1, occ);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertEquals(1, occ);
				occ = countCharOccurrences(lexValue, 'b');
				assertEquals(1, occ);
				occ = countCharOccurrences(lexValue, 'c');
				assertEquals(1, occ);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertEquals(1, occ);
				occ = countCharOccurrences(lexValue, 'c');
				assertEquals(1, occ);
				occ = countCharOccurrences(lexValue, 'd');
				assertEquals(1, occ);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertEquals(1, occ);
				occ = countCharOccurrences(lexValue, 'd');
				assertEquals(1, occ);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);

			assertNotNull(result);
			assertTrue(result.contains(null, p, e));
			assertFalse(result.contains(e, null, null));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(result);
			assertTrue(result.contains(null, p, e));
			assertFalse(result.contains(e, null, null));
			Set<Value> objects = result.filter(d, p, null).objects();
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertTrue(result.contains(null, p, e));
			assertFalse(result.contains(e, null, null));
			Set<Value> objects = result.filter(d, p, null).objects();
			assertNotNull(objects);
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertFalse(result.contains(e, null, null));
			Set<Value> objects = result.filter(d, p, null).objects();
			assertNotNull(objects);
			for (Value object : objects) {
				if (object instanceof BNode) {
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				if (object instanceof BNode) {
					Set<Value> childObjects = result.filter((BNode) object, null, null).objects();
					assertNotNull(childObjects);
					for (Value childObject : childObjects) {
						if (childObject instanceof BNode) {
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					for (Value childObject : childObjects) {
						if (childObject instanceof BNode) {
							assertTrue(result.contains((BNode) childObject, null, null));
						}
					}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			Literal r1 = (Literal) result.next().getValue("r");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Literal r3 = (Literal) result.next().getValue("r");

			assertNotNull(r1);

			// there is a small chance that two successive calls to the random
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			// three successive calls (still theoretically possible to be
			// identical, but phenomenally unlikely).
			assertFalse(r1.equals(r2) && r1.equals(r3));
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		String query1 = "prefix : <http://example.org/> ASK WHERE { :IBM ^(:|!:) :Jane } ";

		assertTrue(conn.prepareBooleanQuery(query1).evaluate());

		String query2 = "prefix : <http://example.org/> ASK WHERE { :IBM ^(:|!:) ?a } ";
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		String query2 = "prefix : <http://example.org/> ASK WHERE { :IBM ^(:|!:) ?a } ";
		assertTrue(conn.prepareBooleanQuery(query2).evaluate());

		String query3 = "prefix : <http://example.org/> ASK WHERE { :IBM (^(:|!:))* :Mary } ";
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		String query3 = "prefix : <http://example.org/> ASK WHERE { :IBM (^(:|!:))* :Mary } ";
		assertTrue(conn.prepareBooleanQuery(query3).evaluate());

	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, query);
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			while (result.hasNext()) {
				BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				IRI p = (IRI) bs.getValue("p");
				assertNotNull(p);
				assertEquals(RDF.TYPE, p);
			}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				IRI p = (IRI) bs.getValue("p");
				assertNotNull(p);
				assertEquals(RDF.TYPE, p);
			}
		} catch (Exception e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (Exception e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);
			Set<Value> objects = result.filter(a, p, null).objects();
			assertNotNull(objects);
			for (Value object : objects) {
				if (object instanceof BNode) {
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			for (Value object : objects) {
				if (object instanceof BNode) {
					assertTrue(result.contains((Resource) object, null, null));
					assertEquals(2, result.filter((Resource) object, null, null).size());
				}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				if (object instanceof BNode) {
					assertTrue(result.contains((Resource) object, null, null));
					assertEquals(2, result.filter((Resource) object, null, null).size());
				}
			}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(query);
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				count++;
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(4, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(4, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {

			assertNotNull(result);
			assertTrue(result.hasNext());

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(result);
			assertTrue(result.hasNext());

			Value x = result.next().getValue("x");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			Value x = result.next().getValue("x");
			assertNotNull(x);
			assertTrue(x instanceof IRI);
			assertEquals("urn:s1", x.stringValue());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Value x = result.next().getValue("x");
			assertNotNull(x);
			assertTrue(x instanceof IRI);
			assertEquals("urn:s1", x.stringValue());
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(x);
			assertTrue(x instanceof IRI);
			assertEquals("urn:s1", x.stringValue());
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			// first execute without binding
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				count++;
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);

			// execute again, but this time setting a binding
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			try (TupleQueryResult result2 = tq.evaluate()) {
				assertNotNull(result2);

				count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					count++;
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(4, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(4, count);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
			assertFalse("only one result expected", result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
			assertFalse("only one result expected", result.hasNext());
			assertEquals(a, bs.getValue("s"));
			assertEquals(b, bs.getValue("o"));
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			BindingSet bs = result.next();
			assertFalse("only one result expected", result.hasNext());
			assertEquals(a, bs.getValue("s"));
			assertEquals(b, bs.getValue("o"));
		}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertFalse("only one result expected", result.hasNext());
			assertEquals(a, bs.getValue("s"));
			assertEquals(b, bs.getValue("o"));
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			IRI uuid1 = (IRI) result.next().getValue("uid");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			IRI uuid2 = (IRI) result.next().getValue("uid");

			assertNotNull(uuid1);
			assertNotNull(uuid2);
			assertNotEquals(uuid1, uuid2);
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(uuid1);
			assertNotNull(uuid2);
			assertNotEquals(uuid1, uuid2);
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `assertNotEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(uuid1);
			assertNotNull(uuid2);
			assertNotEquals(uuid1, uuid2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			assertTrue(result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);

			assertTrue(result.hasNext());
			BindingSet s = result.next();
			Literal count = (Literal) s.getValue("c");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			BindingSet s = result.next();
			Literal count = (Literal) s.getValue("c");
			assertNotNull(count);

			assertEquals(3, count.intValue());
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(count);

			assertEquals(3, count.intValue());
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result1 = tq1.evaluate(); TupleQueryResult result2 = tq2.evaluate()) {
			assertNotNull(result1);
			assertNotNull(result2);

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result1 = tq1.evaluate(); TupleQueryResult result2 = tq2.evaluate()) {
			assertNotNull(result1);
			assertNotNull(result2);

			List<BindingSet> qr1 = QueryResults.asList(result1);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			// if optionals are not kept in same order, query results will be
			// different size.
			assertEquals(qr1.size(), qr2.size());

		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, qb);
		try (TupleQueryResult evaluate = tq.evaluate()) {
			assertFalse("The query should not return a result", evaluate.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (Stream<BindingSet> result = query.evaluate().stream()) {
			long count = result.count();
			assertEquals(2, count);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, query);
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
			IRI uri = (IRI) bs.getValue("uri");
```

### JUnit5AssertionsConverter
Call to `assertNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			BindingSet bs = result.next();
			IRI uri = (IRI) bs.getValue("uri");
			assertNull("uri result for invalid URI should be unbound", uri);
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, query);
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
			IRI uri = (IRI) bs.getValue("uri");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			BindingSet bs = result.next();
			IRI uri = (IRI) bs.getValue("uri");
			assertNotNull("uri result for valid URI reference should be bound", uri);
		}
	}
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					@Override
					public void handleSolution(BindingSet bindingSet) {
						fail("nobody is self published");
					}
				});
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (Stream<BindingSet> result = tq.evaluate().stream()) {
			long count = result.count();
			assertEquals(1, count);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			// first execute without binding
			assertNotNull(result);

			// System.out.println("--- testArbitraryLengthPathWithBinding5
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				// System.out.println(bs);

				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);

			// execute again, but this time setting a binding
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			try (TupleQueryResult result2 = tq.evaluate()) {
				assertNotNull(result2);

				count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					count++;
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				count++;
				assertNotNull(bs);

				Value s = bs.getValue("s");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Value a = bs.getValue("a");

				assertNotNull(s);
				assertNotNull(a);
				assertEquals(f.createIRI("http://example.org/a"), s);
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				assertNotNull(s);
				assertNotNull(a);
				assertEquals(f.createIRI("http://example.org/a"), s);
				assertEquals(f.createIRI("http://example.org/b"), a);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertNotNull(s);
				assertNotNull(a);
				assertEquals(f.createIRI("http://example.org/a"), s);
				assertEquals(f.createIRI("http://example.org/b"), a);
			}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertNotNull(a);
				assertEquals(f.createIRI("http://example.org/a"), s);
				assertEquals(f.createIRI("http://example.org/b"), a);
			}
			assertEquals(1, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertEquals(f.createIRI("http://example.org/b"), a);
			}
			assertEquals(1, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				+ "BIND (IF(BOUND(?var), 'VISIBLE', 'HIDDEN') as ?visibility)" + "}";
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				// System.out.println(bs);
				IRI a = (IRI) bs.getValue("a");
				assertNotNull(a);
				Value isX = bs.getValue("isX");
				Literal name = (Literal) bs.getValue("name");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Value isX = bs.getValue("isX");
				Literal name = (Literal) bs.getValue("name");
				assertNotNull(name);
				if (a.stringValue().endsWith("a1")) {
					assertNotNull(isX);
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertNotNull(name);
				if (a.stringValue().endsWith("a1")) {
					assertNotNull(isX);
				} else if (a.stringValue().endsWith(("a2"))) {
					assertNull(isX);
```

### JUnit5AssertionsConverter
Call to `assertNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertNotNull(isX);
				} else if (a.stringValue().endsWith(("a2"))) {
					assertNull(isX);
				}
			}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				}
			}
			assertEquals(2, count);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, qb);
		try (TupleQueryResult result = tq.evaluate()) {
			assertTrue("The query should return a result", result.hasNext());
			BindingSet b = result.next();
			assertTrue("?x is from the mandatory part of the query and should be bound", b.hasBinding("x"));
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue("The query should return a result", result.hasNext());
			BindingSet b = result.next();
			assertTrue("?x is from the mandatory part of the query and should be bound", b.hasBinding("x"));
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		List<BindingSet> result = QueryResults.asList(q.evaluate());

		assertEquals(1, result.size());

		assertEquals(conn.getValueFactory().createLiteral("a"), result.get(0).getValue("a"));
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		assertEquals(1, result.size());

		assertEquals(conn.getValueFactory().createLiteral("a"), result.get(0).getValue("a"));
		assertNull(result.get(0).getValue("b"));
	}
```

### JUnit5AssertionsConverter
Call to `assertNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		assertEquals(conn.getValueFactory().createLiteral("a"), result.get(0).getValue("a"));
		assertNull(result.get(0).getValue("b"));
	}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, query, "http://example.com/foo/");
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			BindingSet bs = result.next();
			IRI actual = (IRI) bs.getValue("Iri");
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			IRI actual = (IRI) bs.getValue("Iri");
			IRI expected = f.createIRI("http://example.com/bar");
			assertEquals("IRI result for relative IRI should be normalized", expected, actual);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		String query = "SELECT (MIN(?v) as ?min) WHERE { VALUES ?v { 1 2 undef 3 4 }}";
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("1", result.next().getValue("min").stringValue());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("1", result.next().getValue("min").stringValue());
			assertFalse(result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("1", result.next().getValue("min").stringValue());
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.hasNext());
			assertEquals("1", result.next().getValue("min").stringValue());
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			// first execute without binding
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				count++;
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(7, count);

			// execute again, but this time setting a binding
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			try (TupleQueryResult result2 = tq.evaluate()) {
				assertNotNull(result2);

				count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					count++;
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					BindingSet bs = result2.next();
					assertTrue(bs.hasBinding("child"));
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
					assertTrue(bs.hasBinding("parent"));
				}
				assertEquals(2, count);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Model result = QueryResults.asModel(evaluate);
			Set<Value> objects = result.filter(a, p, null).objects();
			assertNotNull(objects);
			for (Value object : objects) {
				if (object instanceof BNode) {
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			for (Value object : objects) {
				if (object instanceof BNode) {
					assertTrue(result.contains((Resource) object, null, null));
					assertEquals(2, result.filter((Resource) object, null, null).size());
				}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				if (object instanceof BNode) {
					assertTrue(result.contains((Resource) object, null, null));
					assertEquals(2, result.filter((Resource) object, null, null).size());
				}
			}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			Literal d1 = (Literal) result.next().getValue("d");
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Literal d2 = (Literal) result.next().getValue("d");

			assertNotNull(d1);
			assertEquals(d1, d2);
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			assertNotNull(d1);
			assertEquals(d1, d2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (Stream<BindingSet> result = tq.evaluate().stream()) {
			long count = result.count();
			assertEquals(4, count);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			int count = 0;
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				count++;
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				BindingSet bs = result.next();
				assertTrue(bs.hasBinding("child"));
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(4, count);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertTrue(bs.hasBinding("parent"));
			}
			assertEquals(4, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		List<BindingSet> result = QueryResults.asList(tq.evaluate());
		assertEquals("single result expected", 1, result.size());
		assertEquals("http://subj1", result.get(0).getValue("s").stringValue());
	}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		List<BindingSet> result = QueryResults.asList(tq.evaluate());
		assertEquals("single result expected", 1, result.size());
		assertEquals("http://subj1", result.get(0).getValue("s").stringValue());
	}

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		String query = "ask {filter (regex(\"Валовой\", \"валовой\", \"i\")) }";

		assertTrue("case-insensitive match on Cyrillic should succeed", conn.prepareBooleanQuery(query).evaluate());

		query = "ask {filter (regex(\"Валовой\", \"валовой\")) }";
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		query = "ask {filter (regex(\"Валовой\", \"валовой\")) }";

		assertFalse("case-sensitive match on Cyrillic should fail", conn.prepareBooleanQuery(query).evaluate());

	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);

			while (result.hasNext()) {
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			while (result.hasNext()) {
				BindingSet bs = result.next();
				assertNotNull(bs);

				Value concat = bs.getValue("concat");
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				Value concat = bs.getValue("concat");

				assertTrue(concat instanceof Literal);

				String lexValue = ((Literal) concat).getLabel();
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

				int occ = countCharOccurrences(lexValue, 'a');
				assertEquals(1, occ);
				occ = countCharOccurrences(lexValue, 'b');
				assertEquals(2, occ);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertEquals(1, occ);
				occ = countCharOccurrences(lexValue, 'b');
				assertEquals(2, occ);
				occ = countCharOccurrences(lexValue, 'c');
				assertEquals(2, occ);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertEquals(2, occ);
				occ = countCharOccurrences(lexValue, 'c');
				assertEquals(2, occ);
				occ = countCharOccurrences(lexValue, 'd');
				assertEquals(1, occ);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
				assertEquals(2, occ);
				occ = countCharOccurrences(lexValue, 'd');
				assertEquals(1, occ);
			}
		} catch (QueryEvaluationException e) {
```

### JUnit5AssertionsConverter
Call to `fail()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}

```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		String query = "SELECT (SUM(?v) as ?s) WHERE { VALUES ?v { 1 2 undef 3 4 }}";
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("10", result.next().getValue("s").stringValue());
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = conn.prepareTupleQuery(QueryLanguage.SPARQL, query).evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("10", result.next().getValue("s").stringValue());
			assertFalse(result.hasNext());
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(result);
			assertTrue(result.hasNext());
			assertEquals("10", result.next().getValue("s").stringValue());
			assertFalse(result.hasNext());
		}
```

### JUnit5AssertionsConverter
Call to `assertFalse()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertTrue(result.hasNext());
			assertEquals("10", result.next().getValue("s").stringValue());
			assertFalse(result.hasNext());
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());

```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		try (TupleQueryResult result = tq.evaluate()) {
			assertNotNull(result);
			assertTrue(result.hasNext());

			BindingSet bs = result.next();
```

### JUnit5AssertionsConverter
Call to `assertNotNull()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			BindingSet bs = result.next();
			Value y = bs.getValue("y");
			assertNotNull(y);
			assertTrue(y instanceof Literal);
			assertEquals(f.createLiteral("1", XSD.INTEGER), y);
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			Value y = bs.getValue("y");
			assertNotNull(y);
			assertTrue(y instanceof Literal);
			assertEquals(f.createLiteral("1", XSD.INTEGER), y);
		}
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotNull(y);
			assertTrue(y instanceof Literal);
			assertEquals(f.createLiteral("1", XSD.INTEGER), y);
		}
	}
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
		TupleQuery tq = conn.prepareTupleQuery(QueryLanguage.SPARQL, qb);
		try (TupleQueryResult evaluate = tq.evaluate()) {
			assertTrue("The query should return a result", evaluate.hasNext());

			List<BindingSet> result = QueryResults.asList(evaluate);
```

### JUnit5AssertionsConverter
Call to `assertEquals()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

			List<BindingSet> result = QueryResults.asList(evaluate);
			assertEquals(10, result.size());
			for (BindingSet bs : result) {
				Literal label = (Literal) bs.getValue("label");
```

### JUnit5AssertionsConverter
Call to `assertTrue()` from 'Assert' should be replaced with a call to a method from 'org.junit.jupiter.api.Assertions'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			for (BindingSet bs : result) {
				Literal label = (Literal) bs.getValue("label");
				assertTrue("wrong label value (expected '01' or '02', but got '" + label.stringValue() + "')",
						label.stringValue().equals("01") || label.stringValue().equals("02"));
			}
```

## RuleId[id=SillyAssignment]
### SillyAssignment
Variable 'transactionObject' is assigned to itself
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionalRepositoryConnection.java`
#### Snippet
```java
	public TransactionalRepositoryConnection(Repository repository, RepositoryConnection delegate) {
		super(repository, delegate);
		this.transactionObject = transactionObject;
	}

```

## RuleId[id=BoundedWildcard]
### BoundedWildcard
Can generalize to `? extends Changeset.SimpleStatementPattern`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSink.java`
#### Snippet
```java
	}

	default void observeAll(Set<Changeset.SimpleStatementPattern> observed) {
		for (Changeset.SimpleStatementPattern p : observed) {
			Resource subj = p.getSubject();
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSink.java`
#### Snippet
```java
	}

	default void deprecateAll(Set<Statement> deprecated) {
		for (Statement statement : deprecated) {
			deprecate(statement);
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSink.java`
#### Snippet
```java
	}

	default void approveAll(Set<Statement> approved, Set<Resource> approvedContexts) {
		for (Statement statement : approved) {
			approve(statement);
```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/DistinctModelReducingUnionIteration.java`
#### Snippet
```java
	private final Supplier<Iterable<Statement>> approvedSupplier;

	DistinctModelReducingUnionIteration(CloseableIteration<? extends Statement, SailException> iterator,
			Consumer<Statement> approvedRemover,
			Supplier<Iterable<Statement>> approvedSupplier) {
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/DistinctModelReducingUnionIteration.java`
#### Snippet
```java

	DistinctModelReducingUnionIteration(CloseableIteration<? extends Statement, SailException> iterator,
			Consumer<Statement> approvedRemover,
			Supplier<Iterable<Statement>> approvedSupplier) {
		this.iterator = iterator;
```

### BoundedWildcard
Can generalize to `? extends Iterable`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/DistinctModelReducingUnionIteration.java`
#### Snippet
```java
	DistinctModelReducingUnionIteration(CloseableIteration<? extends Statement, SailException> iterator,
			Consumer<Statement> approvedRemover,
			Supplier<Iterable<Statement>> approvedSupplier) {
		this.iterator = iterator;
		this.approvedRemover = approvedRemover;
```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java

	private CloseableIteration<? extends Statement, SailException> difference(
			CloseableIteration<? extends Statement, SailException> result, Function<Statement, Boolean> excluded) {
		return new FilterIteration<Statement, SailException>(result) {

```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java

	private CloseableIteration<? extends Statement, SailException> difference(
			CloseableIteration<? extends Statement, SailException> result, Function<Statement, Boolean> excluded) {
		return new FilterIteration<Statement, SailException>(result) {

```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java

	private CloseableIteration<? extends Triple, SailException> triplesDifference(
			CloseableIteration<? extends Triple, SailException> result, Function<Triple, Boolean> excluded) {
		return new FilterIteration<Triple, SailException>(result) {

```

### BoundedWildcard
Can generalize to `? super Triple`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java

	private CloseableIteration<? extends Triple, SailException> triplesDifference(
			CloseableIteration<? extends Triple, SailException> result, Function<Triple, Boolean> excluded) {
		return new FilterIteration<Triple, SailException>(result) {

```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/rio/rdfjson/src/main/java/org/eclipse/rdf4j/rio/rdfjson/RDFJSONWriter.java`
#### Snippet
```java
	 * @throws JsonGenerationException
	 */
	public static void writeObject(final Value object, final Set<Resource> contexts, final JsonGenerator jg)
			throws JsonGenerationException, IOException {
		jg.writeStartObject();
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java

	@Override
	public void deprecateAll(Set<Statement> deprecate) {
		long writeLock = readWriteLock.writeLock();
		try {
```

### BoundedWildcard
Can generalize to `? extends SimpleStatementPattern`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java

	@Override
	public void observeAll(Set<SimpleStatementPattern> observed) {
		assert !closed;
		long writeLock = readWriteLock.writeLock();
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java

	@Override
	public void approveAll(Set<Statement> approve, Set<Resource> approveContexts) {
		long writeLock = readWriteLock.writeLock();
		try {
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java

	@Override
	public void approveAll(Set<Statement> approve, Set<Resource> approveContexts) {
		long writeLock = readWriteLock.writeLock();
		try {
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
	}

	private <T> Set<T> cloneSet(Set<T> set) {
		assert !closed;
		if (set == null) {
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ConnectionHelper.java`
#### Snippet
```java

	static CloseableIteration<Statement, SailException> getCloseableIteration(
			RepositoryResult<Statement> repositoryResults) {

		return new CloseableIteration<>() {
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java

	private <T, X extends Exception> CloseableIteration<T, QueryEvaluationException> interlock(
			CloseableIteration<T, QueryEvaluationException> iter, SailClosable... closes) {
		return new SailClosingIteration<T, QueryEvaluationException>(iter, closes) {

```

### BoundedWildcard
Can generalize to `? extends ExecutorService`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java
		boolean initialized = false;

		public RevivableExecutorService(Supplier<ExecutorService> supplier) {
			this.supplier = supplier;
		}
```

### BoundedWildcard
Can generalize to `? super Resource`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Cache.java`
#### Snippet
```java
	Map<Resource, Shape> cache = new HashMap<>();

	public Shape computeIfAbsent(Resource id, Function<Resource, Shape> mappingFunction) {
		return cache.computeIfAbsent(id, mappingFunction);
	}
```

### BoundedWildcard
Can generalize to `? extends Shape`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Cache.java`
#### Snippet
```java
	Map<Resource, Shape> cache = new HashMap<>();

	public Shape computeIfAbsent(Resource id, Function<Resource, Shape> mappingFunction) {
		return cache.computeIfAbsent(id, mappingFunction);
	}
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
		private final RepositoryConnection connection;

		private CloseConnectionIteration(CloseableIteration<BindingSet, QueryEvaluationException> delegate,
				RepositoryConnection connection) {
			super();
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
		private final RepositoryConnection connection;

		private CloseConnectionIteration(CloseableIteration<BindingSet, QueryEvaluationException> delegate,
				RepositoryConnection connection) {
			super();
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
	 */
	protected CloseableIteration<BindingSet, QueryEvaluationException> evaluateInternal(Service service,
			CloseableIteration<BindingSet, QueryEvaluationException> bindings, String baseUri)
			throws QueryEvaluationException {

```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
	 */
	protected CloseableIteration<BindingSet, QueryEvaluationException> evaluateInternal(Service service,
			CloseableIteration<BindingSet, QueryEvaluationException> bindings, String baseUri)
			throws QueryEvaluationException {

```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
	 * @throws QueryEvaluationException
	 */
	private String buildVALUESClause(List<BindingSet> bindings, List<String> relevantBindingNames)
			throws QueryEvaluationException {

```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
	 * @return the list of relevant bindings (if empty: the cross product needs to be formed)
	 */
	private List<String> getRelevantBindingNames(List<BindingSet> bindings, Set<String> serviceVars) {

		// get the bindings variables
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java

		public FallbackServiceIteration(Service service,
				List<BindingSet> allBindings, String baseUri) {
			super(null, null, null);
			this.service = service;
```

### BoundedWildcard
Can generalize to `? extends SparqlFragment`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/SparqlFragment.java`
#### Snippet
```java
	}

	public static boolean isFilterCondition(List<SparqlFragment> sparqlFragments) {
		boolean isFilterCondtion = sparqlFragments
				.stream()
```

### BoundedWildcard
Can generalize to `? extends StatementMatcher`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/SparqlFragment.java`
#### Snippet
```java
	}

	public void addStatementMatchers(List<StatementMatcher> statementMatchers) {
		this.statementMatchers.addAll(statementMatchers);
	}
```

### BoundedWildcard
Can generalize to `? extends ContextWithShapes`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	}

	private ValidationReport performValidation(List<ContextWithShapes> shapes, boolean validateEntireBaseSail,
			ConnectionsGroup connectionsGroup) throws InterruptedException {
		long beforeValidation = 0;
```

### BoundedWildcard
Can generalize to `? extends ContextWithShapes`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	}

	private boolean isEmpty(List<ContextWithShapes> shapesList) {
		if (shapesList == null) {
			return true;
```

### BoundedWildcard
Can generalize to `? extends Shape`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
		}

		private static List<Shape> split(List<Shape> collect) {
			// split into shapes by target and constraint component to be able to run them in parallel
			return collect.stream().flatMap(s -> {
```

### BoundedWildcard
Can generalize to `? extends Shape`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
		}

		private static void calculateIfProducesValidationResult(List<Shape> split) {
			for (Shape shape : split) {
				assert shape.constraintComponents.size() == 1;
```

### BoundedWildcard
Can generalize to `? extends Shape`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
		}

		private static void calculateTargetChain(List<Shape> parsed) {
			for (Shape shape : parsed) {
				assert (shape.target.size() == 1);
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValueInFilter.java`
#### Snippet
```java
	private final Set<Value> valueSet;

	public ValueInFilter(PlanNode parent, Set<Value> valueSet) {
		super(parent);
		this.valueSet = valueSet;
```

### BoundedWildcard
Can generalize to `? super PlanNode`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/EffectiveTarget.java`
#### Snippet
```java
	public PlanNode getPlanNode(ConnectionsGroup connectionsGroup, Resource[] dataGraph,
			ConstraintComponent.Scope scope,
			boolean includeTargetsAffectedByRemoval, Function<PlanNode, PlanNode> filter) {
		assert !chain.isEmpty();

```

### BoundedWildcard
Can generalize to `? extends PlanNode`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/EffectiveTarget.java`
#### Snippet
```java
	public PlanNode getPlanNode(ConnectionsGroup connectionsGroup, Resource[] dataGraph,
			ConstraintComponent.Scope scope,
			boolean includeTargetsAffectedByRemoval, Function<PlanNode, PlanNode> filter) {
		assert !chain.isEmpty();

```

### BoundedWildcard
Can generalize to `? super PlanNode`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/EffectiveTarget.java`
#### Snippet
```java
	public PlanNode extend(PlanNode source, ConnectionsGroup connectionsGroup, Resource[] dataGraph,
			ConstraintComponent.Scope scope,
			Extend direction, boolean includePropertyShapeValues, Function<PlanNode, PlanNode> filter) {

		if (source instanceof AllTargetsPlanNode && !includePropertyShapeValues) {
```

### BoundedWildcard
Can generalize to `? extends PlanNode`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/EffectiveTarget.java`
#### Snippet
```java
	public PlanNode extend(PlanNode source, ConnectionsGroup connectionsGroup, Resource[] dataGraph,
			ConstraintComponent.Scope scope,
			Extend direction, boolean includePropertyShapeValues, Function<PlanNode, PlanNode> filter) {

		if (source instanceof AllTargetsPlanNode && !includePropertyShapeValues) {
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SetFilterNode.java`
#### Snippet
```java
	private ValidationExecutionLogger validationExecutionLogger;

	public SetFilterNode(Set<Value> targetNodeList, PlanNode parent, int index, boolean returnValid) {
		this.parent = PlanNodeHelper.handleSorting(this, parent);
		this.targetNodeList = targetNodeList;
```

### BoundedWildcard
Can generalize to `? super ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
	}

	public DebugPlanNode(PlanNode parent, Consumer<ValidationTuple> debugPoint) {
		this(parent, (String) null);
		this.debugPoint = debugPoint;
```

### BoundedWildcard
Can generalize to `? super ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
	private ValidationExecutionLogger validationExecutionLogger;

	public DebugPlanNode(PlanNode parent, String message, Consumer<ValidationTuple> debugPoint) {
		this(parent, message);
		this.debugPoint = debugPoint;
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
	}

	public FilterByPredicate(SailConnection connection, Set<IRI> filterOnPredicates, PlanNode parent,
			On on, Resource[] dataGraph) {
		this.dataGraph = dataGraph;
```

### BoundedWildcard
Can generalize to `? super ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TupleMapper.java`
#### Snippet
```java
	private ValidationExecutionLogger validationExecutionLogger;

	public TupleMapper(PlanNode parent, Function<ValidationTuple, ValidationTuple> function) {
		this.parent = PlanNodeHelper.handleSorting(this, parent);
		this.function = function;
```

### BoundedWildcard
Can generalize to `? extends ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TupleMapper.java`
#### Snippet
```java
	private ValidationExecutionLogger validationExecutionLogger;

	public TupleMapper(PlanNode parent, Function<ValidationTuple, ValidationTuple> function) {
		this.parent = PlanNodeHelper.handleSorting(this, parent);
		this.function = function;
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java

	public Select(SailConnection connection, String queryFragment, String orderBy,
			Function<BindingSet, ValidationTuple> mapper, Resource[] dataGraph) {
		this.connection = connection;
		assert this.connection != null;
```

### BoundedWildcard
Can generalize to `? extends ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java

	public Select(SailConnection connection, String queryFragment, String orderBy,
			Function<BindingSet, ValidationTuple> mapper, Resource[] dataGraph) {
		this.connection = connection;
		assert this.connection != null;
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
	}

	public Select(SailConnection connection, String query, Function<BindingSet, ValidationTuple> mapper,
			Resource[] dataGraph) {
		assert !query.toLowerCase().contains("order by") : "Queries with order by are not supported.";
```

### BoundedWildcard
Can generalize to `? extends ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
	}

	public Select(SailConnection connection, String query, Function<BindingSet, ValidationTuple> mapper,
			Resource[] dataGraph) {
		assert !query.toLowerCase().contains("order by") : "Queries with order by are not supported.";
```

### BoundedWildcard
Can generalize to `? super Long`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java
	private ValidationExecutionLogger validationExecutionLogger;

	public GroupByCountFilter(PlanNode parent, Function<Long, Boolean> filter) {
		this.parent = PlanNodeHelper.handleSorting(this, parent);
		this.filter = filter;
```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Unique.java`
#### Snippet
```java

		public TargetAndValueSortIterator(
				CloseablePeakableIteration<? extends ValidationTuple, SailException> iterator) {
			this.iterator = iterator;
		}
```

### BoundedWildcard
Can generalize to `? super Collection`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java
	private ValidationExecutionLogger validationExecutionLogger;

	public GroupByFilter(PlanNode parent, Function<Collection<ValidationTuple>, Boolean> filter) {
		this.parent = PlanNodeHelper.handleSorting(this, parent);
		this.filter = filter;
```

### BoundedWildcard
Can generalize to `? super ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ExternalFilterByQuery.java`
#### Snippet
```java
			String queryFragment,
			StatementMatcher.Variable queryVariable,
			Function<ValidationTuple, Value> filterOn) {
		super(parent);
		this.connection = connection;
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ExternalFilterByQuery.java`
#### Snippet
```java
			String queryFragment,
			StatementMatcher.Variable queryVariable,
			Function<ValidationTuple, Value> filterOn) {
		super(parent);
		this.connection = connection;
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java

	public UnorderedSelect(SailConnection connection, Resource subject, IRI predicate, Value object,
			Resource[] dataGraph, Function<Statement, ValidationTuple> mapper) {
		this.connection = connection;
		assert this.connection != null;
```

### BoundedWildcard
Can generalize to `? extends ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java

	public UnorderedSelect(SailConnection connection, Resource subject, IRI predicate, Value object,
			Resource[] dataGraph, Function<Statement, ValidationTuple> mapper) {
		this.connection = connection;
		assert this.connection != null;
```

### BoundedWildcard
Can generalize to `? extends ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AbstractBulkJoinPlanNode.java`
#### Snippet
```java
	}

	private List<BindingSet> buildBindingSets(ArrayDeque<ValidationTuple> left, SailConnection connection,
			boolean skipBasedOnPreviousConnection, SailConnection previousStateConnection, Resource[] dataGraph) {
		return left.stream()
```

### BoundedWildcard
Can generalize to `? super ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AbstractBulkJoinPlanNode.java`
#### Snippet
```java
	}

	private void executeQuery(ArrayDeque<ValidationTuple> right, SailConnection connection,
			Dataset dataset, TupleExpr parsedQuery) {

```

### BoundedWildcard
Can generalize to `? super ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTuple.java`
#### Snippet
```java

	private Set<ValidationTuple> enrichCompressedTuples(
			Function<ValidationTuple, ValidationTuple> validationTupleValidationTupleFunction) {
		if (compressedTuples.isEmpty()) {
			return compressedTuples;
```

### BoundedWildcard
Can generalize to `? extends ValidationTuple`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTuple.java`
#### Snippet
```java

	private Set<ValidationTuple> enrichCompressedTuples(
			Function<ValidationTuple, ValidationTuple> validationTupleValidationTupleFunction) {
		if (compressedTuples.isEmpty()) {
			return compressedTuples;
```

### BoundedWildcard
Can generalize to `? extends Shape`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/LogicalOperatorConstraintComponent.java`
#### Snippet
```java
			StatementMatcher.Variable object,
			RdfsSubClassOfReasoner rdfsSubClassOfReasoner, Scope scope,
			StatementMatcher.StableRandomVariableProvider stableRandomVariableProvider, List<Shape> shapes,
			TargetChain targetChain,
			Function<List<SparqlFragment>, SparqlFragment> bgpCombiner,
```

### BoundedWildcard
Can generalize to `? super List`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/LogicalOperatorConstraintComponent.java`
#### Snippet
```java
			StatementMatcher.StableRandomVariableProvider stableRandomVariableProvider, List<Shape> shapes,
			TargetChain targetChain,
			Function<List<SparqlFragment>, SparqlFragment> bgpCombiner,
			Function<List<SparqlFragment>, SparqlFragment> filterCombiner) {

```

### BoundedWildcard
Can generalize to `? extends SparqlFragment`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/LogicalOperatorConstraintComponent.java`
#### Snippet
```java
			StatementMatcher.StableRandomVariableProvider stableRandomVariableProvider, List<Shape> shapes,
			TargetChain targetChain,
			Function<List<SparqlFragment>, SparqlFragment> bgpCombiner,
			Function<List<SparqlFragment>, SparqlFragment> filterCombiner) {

```

### BoundedWildcard
Can generalize to `? super List`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/LogicalOperatorConstraintComponent.java`
#### Snippet
```java
			TargetChain targetChain,
			Function<List<SparqlFragment>, SparqlFragment> bgpCombiner,
			Function<List<SparqlFragment>, SparqlFragment> filterCombiner) {

		List<SparqlFragment> sparqlFragments = shapes.stream()
```

### BoundedWildcard
Can generalize to `? extends SparqlFragment`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/LogicalOperatorConstraintComponent.java`
#### Snippet
```java
			TargetChain targetChain,
			Function<List<SparqlFragment>, SparqlFragment> bgpCombiner,
			Function<List<SparqlFragment>, SparqlFragment> filterCombiner) {

		List<SparqlFragment> sparqlFragments = shapes.stream()
```

### BoundedWildcard
Can generalize to `? extends ValidationResultIterator`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/LazyValidationReport.java`
#### Snippet
```java
	private final long limit;

	public LazyValidationReport(List<ValidationResultIterator> validationResultIterators, long limit) {

		this.validationResultIterators = validationResultIterators;
```

### BoundedWildcard
Can generalize to `? extends Path`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java
			ValidationSettings validationSettings, PlanNodeProvider overrideTargetNode, Scope scope, boolean negatePlan,
			StatementMatcher.StableRandomVariableProvider stableRandomVariableProvider, EffectiveTarget effectiveTarget,
			Optional<Path> path) {
		PlanNode planNode;

```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java
	private CloseableIteration<? extends ValidationTuple, SailException> tupleIterator;

	public ValidationResultIterator(CloseableIteration<? extends ValidationTuple, SailException> tupleIterator,
			long limit) {
		this.limit = limit;
```

### BoundedWildcard
Can generalize to `? extends StatementMatcher.Variable`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java

	public BindSelect(SailConnection connection, Resource[] dataGraph, String query,
			List<StatementMatcher.Variable> vars, PlanNode source,
			List<String> varNames, ConstraintComponent.Scope scope, int bulkSize, EffectiveTarget.Extend direction,
			boolean includePropertyShapeValues) {
```

### BoundedWildcard
Can generalize to `? extends E`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/CloseablePeakableIteration.java`
#### Snippet
```java
	E peek;

	public CloseablePeakableIteration(CloseableIteration<E, X> parent) {
		this.parent = parent;
	}
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/CloseablePeakableIteration.java`
#### Snippet
```java
	E peek;

	public CloseablePeakableIteration(CloseableIteration<E, X> parent) {
		this.parent = parent;
	}
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/RdfsSubClassOfReasoner.java`
#### Snippet
```java
	}

	private void calculateSubClassOf(Collection<Statement> subClassOfStatements) {
		if (subClassOfStatements.isEmpty()) {
			return;
```

### BoundedWildcard
Can generalize to `? extends Set`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/RdfsSubClassOfReasoner.java`
#### Snippet
```java
	}

	private Set<Resource> resolveTypes(Resource value, Map<Resource, Set<Resource>> forwardChainCache) {
		Set<Resource> iris = forwardChainCache.get(value);
		return iris != null ? iris : Collections.emptySet();
```

### BoundedWildcard
Can generalize to `? extends ExtensibleStatement`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java

	@Override
	public void addStatement(Collection<ExtensibleStatement> statements) {
		addStatementBuffer.addAll(statements);
		if (addStatementBuffer.size() >= BUFFER_THRESHOLD) {
```

### BoundedWildcard
Can generalize to `? extends ExtensionElem`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Extension.java`
#### Snippet
```java
	}

	public void addElements(Iterable<ExtensionElem> elements) {
		for (ExtensionElem pe : elements) {
			addElement(pe);
```

### BoundedWildcard
Can generalize to `? extends ValueExpr`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/TupleFunctionCall.java`
#### Snippet
```java
	}

	public void addArgs(Iterable<ValueExpr> args) {
		for (ValueExpr arg : args) {
			addArg(arg);
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/TupleFunctionCall.java`
#### Snippet
```java
	}

	public void addResultVars(Iterable<Var> resultVars) {
		for (Var var : resultVars) {
			addResultVar(var);
```

### BoundedWildcard
Can generalize to `? extends ProjectionElemList`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/MultiProjection.java`
#### Snippet
```java
	}

	public void addProjections(Iterable<ProjectionElemList> projections) {
		for (ProjectionElemList projection : projections) {
			addProjection(projection);
```

### BoundedWildcard
Can generalize to `? extends OrderElem`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Order.java`
#### Snippet
```java
	}

	public void addElements(Iterable<OrderElem> elements) {
		for (OrderElem pe : elements) {
			addElement(pe);
```

### BoundedWildcard
Can generalize to `? extends ValueExpr`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/FunctionCall.java`
#### Snippet
```java
	}

	public void addArgs(Iterable<ValueExpr> args) {
		for (ValueExpr arg : args) {
			addArg(arg);
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/BGPCollector.java`
#### Snippet
```java
	private List<StatementPattern> statementPatterns;

	public BGPCollector(QueryModelVisitor<X> visitor) {
		this.visitor = visitor;
	}
```

### BoundedWildcard
Can generalize to `? extends ProjectionElem`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/ProjectionElemList.java`
#### Snippet
```java
	}

	public void setElements(List<ProjectionElem> elements) {
		elements.forEach(projectionElem -> projectionElem.setParentNode(this));

```

### BoundedWildcard
Can generalize to `? extends GroupElem`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Group.java`
#### Snippet
```java
	}

	public void setGroupElements(Iterable<GroupElem> elements) {
		this.groupElements.clear();
		Iterables.addAll(groupElements, elements);
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/Template.java`
#### Snippet
```java
	}

	public ParsedOperation call(Map<IRI, Value> argValues) throws MalformedSpinException {
		MapBindingSet args = new MapBindingSet();
		for (Argument arg : arguments) {
```

### BoundedWildcard
Can generalize to `? extends Argument`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinTupleFunction.java`
#### Snippet
```java
	}

	private static void addBindings(Query query, List<Argument> arguments, Value... args) {
		for (int i = 0; i < args.length; i++) {
			Argument argument = arguments.get(i);
```

### BoundedWildcard
Can generalize to `? extends Argument`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/SpinFunction.java`
#### Snippet
```java
	}

	private static void addBindings(Query query, List<Argument> arguments, Value... args) {
		for (int i = 0; i < args.length; i++) {
			Argument argument = arguments.get(i);
```

### BoundedWildcard
Can generalize to `? super IRI`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/KnownFunctionParser.java`
#### Snippet
```java
	private final Function<IRI, String> wellKnownFunctions;

	public KnownFunctionParser(FunctionRegistry functionRegistry, Function<IRI, String> wellKnownFunctions) {
		this.functionRegistry = functionRegistry;
		this.wellKnownFunctions = wellKnownFunctions;
```

### BoundedWildcard
Can generalize to `? extends E`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/SingleValueToListTransformer.java`
#### Snippet
```java

	@SuppressWarnings("unchecked")
	static <E> Iterator<List<? extends E>> transform(Iterator<E> iter) {
		return Iterators.transform(iter, (SingleValueToListTransformer<E>) INSTANCE);
	}
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/DirectTypeHierarchyInferencer.java`
#### Snippet
```java
		}

		private void evaluateIntoStatements(ParsedGraphQuery query, Collection<Statement> statements)
				throws SailException, RDFHandlerException, QueryEvaluationException {
			try (CloseableIteration<? extends BindingSet, QueryEvaluationException> bindingsIter = getWrappedConnection()
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/CustomGraphQueryInferencer.java`
#### Snippet
```java
		}

		private void evaluateIntoStatements(ParsedGraphQuery query, Collection<Statement> statements)
				throws SailException, RDFHandlerException, QueryEvaluationException {
			try (CloseableIteration<? extends BindingSet, QueryEvaluationException> bindingsIter = getWrappedConnection()
```

### BoundedWildcard
Can generalize to `? extends List`>
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
	}

	private void evaluate(String[] queries, ArrayList<List<Map<String, String>>> expectedResults)
			throws MalformedQueryException, RepositoryException, QueryEvaluationException {
		try (RepositoryConnection connection = repository.getConnection()) {
```

### BoundedWildcard
Can generalize to `? super Resource`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	}

	private void addAll(Set<Resource> res, List<Set<Resource>> from) {
		if (from.size() == 1) {
			var forwardChained = from.get(0);
```

### BoundedWildcard
Can generalize to `? extends Set`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	}

	private void addAll(Set<Resource> res, List<Set<Resource>> from) {
		if (from.size() == 1) {
			var forwardChained = from.get(0);
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	}

	private void calculateSubPropertyOf(Collection<Statement> subPropertyOfStatemenets) {
		logger.debug("Calculate rdfs:subPropertyOf inference map.");

```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	}

	private void calculateDomainAndRange(Collection<Statement> rangeOrDomainStatements,
			Map<Resource, Set<Resource>> calculatedRangeOrDomain) {

```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	}

	private void calculateSubClassOf(Collection<Statement> subClassOfStatements) {
		logger.debug("Calculate rdfs:subClassOf inference map.");

```

### BoundedWildcard
Can generalize to `? super ByteBuffer`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbUtil.java`
#### Snippet
```java
	}

	static int openDatabase(long env, String name, int flags, Comparator<ByteBuffer> comparator) throws IOException {
		return transaction(env, (stack, txn) -> {
			IntBuffer ip = stack.mallocInt(1);
```

### BoundedWildcard
Can generalize to `? super String`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinRenderer.java`
#### Snippet
```java
	}

	public SpinRenderer(Output output, Function<String, IRI> wellKnownVarMapper,
			Function<String, IRI> wellKnownFuncMapper, ValueFactory vf) {
		this.output = output;
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinRenderer.java`
#### Snippet
```java
	}

	public SpinRenderer(Output output, Function<String, IRI> wellKnownVarMapper,
			Function<String, IRI> wellKnownFuncMapper, ValueFactory vf) {
		this.output = output;
```

### BoundedWildcard
Can generalize to `? super String`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinRenderer.java`
#### Snippet
```java

	public SpinRenderer(Output output, Function<String, IRI> wellKnownVarMapper,
			Function<String, IRI> wellKnownFuncMapper, ValueFactory vf) {
		this.output = output;
		this.wellKnownVars = wellKnownVarMapper;
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinRenderer.java`
#### Snippet
```java

	public SpinRenderer(Output output, Function<String, IRI> wellKnownVarMapper,
			Function<String, IRI> wellKnownFuncMapper, ValueFactory vf) {
		this.output = output;
		this.wellKnownVars = wellKnownVarMapper;
```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
		Statement last;

		StatementIterator(CloseableIteration<? extends Statement, SailException> closeableIteration) {
			this.stmts = closeableIteration;
		}
```

### BoundedWildcard
Can generalize to `? extends RioSetting`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/ParserConfig.java`
#### Snippet
```java
	 * @return Either a copy of this config, if it is immutable, or this object, to allow chaining of method calls.
	 */
	public ParserConfig setNonFatalErrors(Set<RioSetting<?>> nonFatalErrors) {
		this.nonFatalErrors = new HashSet<>(nonFatalErrors);
		return this;
```

### BoundedWildcard
Can generalize to `? extends ProjectionElem`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
		}

		private void addSourceExpressions(UnaryTupleOperator op, Collection<ProjectionElem> elems) {
			Extension ext = null;
			for (ProjectionElem projElem : elems) {
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	}

	private Map<IRI, Argument> parseTemplateArguments(IRI tmplUri, Set<IRI> abstractTmpls, TripleSource store)
			throws RDF4JException {
		Map<IRI, Argument> args = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? super IRI`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	}

	private void parseArguments(IRI moduleUri, TripleSource store, Map<IRI, Argument> args) throws RDF4JException {
		try (CloseableIteration<Resource, QueryEvaluationException> argIter = TripleSources
				.getObjectResources(moduleUri, SPIN.CONSTRAINT_PROPERTY, store)) {
```

### BoundedWildcard
Can generalize to `? super Argument`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	}

	private void parseArguments(IRI moduleUri, TripleSource store, Map<IRI, Argument> args) throws RDF4JException {
		try (CloseableIteration<Resource, QueryEvaluationException> argIter = TripleSources
				.getObjectResources(moduleUri, SPIN.CONSTRAINT_PROPERTY, store)) {
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	}

	public static List<IRI> orderArguments(Set<IRI> args) {
		SortedSet<IRI> sortedArgs = new TreeSet<>(
				(IRI uri1, IRI uri2) -> uri1.getLocalName().compareTo(uri2.getLocalName()));
```

### BoundedWildcard
Can generalize to `? super IRI`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	}

	public SpinParser(Input input, Function<IRI, String> wellKnownVarsMapper,
			Function<IRI, String> wellKnownFuncMapper) {
		this.input = input;
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLComplianceTest.java`
#### Snippet
```java
	}

	protected void compareGraphs(Iterable<Statement> queryResult, Iterable<Statement> expectedResult) throws Exception {
		if (!Models.isomorphic(expectedResult, queryResult)) {
			StringBuilder message = new StringBuilder(128);
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLComplianceTest.java`
#### Snippet
```java
	}

	protected void compareGraphs(Iterable<Statement> queryResult, Iterable<Statement> expectedResult) throws Exception {
		if (!Models.isomorphic(expectedResult, queryResult)) {
			StringBuilder message = new StringBuilder(128);
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11UpdateComplianceTest.java`
#### Snippet
```java

	public SPARQL11UpdateComplianceTest(String displayName, String testURI, String name, String requestFile,
			IRI defaultGraphURI, Map<String, IRI> inputNamedGraphs, IRI resultDefaultGraphURI,
			Map<String, IRI> resultNamedGraphs) {
		super(displayName, testURI, name);
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11UpdateComplianceTest.java`
#### Snippet
```java
	public SPARQL11UpdateComplianceTest(String displayName, String testURI, String name, String requestFile,
			IRI defaultGraphURI, Map<String, IRI> inputNamedGraphs, IRI resultDefaultGraphURI,
			Map<String, IRI> resultNamedGraphs) {
		super(displayName, testURI, name);
		this.requestFile = requestFile;
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	}

	protected final void compareGraphs(Set<Statement> queryResult, Set<Statement> expectedResult) throws Exception {
		if (!Models.isomorphic(expectedResult, queryResult)) {
			// Don't use RepositoryUtil.difference, it reports incorrect diffs
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	}

	protected final void compareGraphs(Set<Statement> queryResult, Set<Statement> expectedResult) throws Exception {
		if (!Models.isomorphic(expectedResult, queryResult)) {
			// Don't use RepositoryUtil.difference, it reports incorrect diffs
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java

	public SPARQLUpdateConformanceTest(String testURI, String name, String requestFile, IRI defaultGraphURI,
			Map<String, IRI> inputNamedGraphs, IRI resultDefaultGraphURI, Map<String, IRI> resultNamedGraphs) {
		super(name);

```

### BoundedWildcard
Can generalize to `? extends IRI`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java

	public SPARQLUpdateConformanceTest(String testURI, String name, String requestFile, IRI defaultGraphURI,
			Map<String, IRI> inputNamedGraphs, IRI resultDefaultGraphURI, Map<String, IRI> resultNamedGraphs) {
		super(name);

```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/Rio.java`
#### Snippet
```java
	 * @throws RDFHandlerException Thrown if there is an error writing the statements.
	 */
	public static void write(Iterable<Statement> model, RDFHandler writer) throws RDFHandlerException {
		writer.startRDF();

```

### BoundedWildcard
Can generalize to `? extends RDFFormat`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/RDFFormat.java`
#### Snippet
```java
	 *         format for each type.
	 */
	public static List<String> getAcceptParams(Iterable<RDFFormat> rdfFormats, boolean requireContext,
			RDFFormat preferredFormat) {
		List<String> acceptParams = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionObject.java`
#### Snippet
```java
	}

	public void wrapConnection(Function<RepositoryConnection, RepositoryConnection> wrapper) {
		this.connection = wrapper.apply(connection);
	}
```

### BoundedWildcard
Can generalize to `? extends RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionObject.java`
#### Snippet
```java
	}

	public void wrapConnection(Function<RepositoryConnection, RepositoryConnection> wrapper) {
		this.connection = wrapper.apply(connection);
	}
```

### BoundedWildcard
Can generalize to `? super String`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/sparql/PreparedSparqlManager.java`
#### Snippet
```java
	private final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

	public <T> T get(String sparqlString, RepositoryConnection con, Function<String, T> preparer) {
		String key = sparqlString + "@con" + con.hashCode();
		logger.debug("obtaining prepared sparql operation...");
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/UpdateWithModelBuilder.java`
#### Snippet
```java
	}

	public void acceptConnection(Consumer<RepositoryConnection> connectionConsumer) {
		connectionConsumer.accept(this.con);
	}
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/UpdateWithModelBuilder.java`
#### Snippet
```java
	}

	public <T> T applyToConnection(Function<RepositoryConnection, T> function) {
		return function.apply(con);
	}
```

### BoundedWildcard
Can generalize to `? super Collection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/UpdateWithModelBuilder.java`
#### Snippet
```java
	}

	public UpdateWithModelBuilder withSink(Consumer<Collection<Statement>> consumer) {
		List<Statement> sink = new ArrayList<>();
		consumer.accept(sink);
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/RelationMapBuilder.java`
#### Snippet
```java
	}

	private static <T> Set<T> mergeSets(Set<T> left, Set<T> right) {
		Set<T> merged = new HashSet<>(left);
		merged.addAll(right);
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/RelationMapBuilder.java`
#### Snippet
```java
	}

	private static <T> Set<T> mergeSets(Set<T> left, Set<T> right) {
		Set<T> merged = new HashSet<>(left);
		merged.addAll(right);
```

### BoundedWildcard
Can generalize to `? extends Value`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/OperationUtils.java`
#### Snippet
```java
	}

	private static void debugLogBindings(Map<String, Value> bindings) {
		if (logger.isDebugEnabled() && bindings != null) {
			logger.debug("bindings: {}", bindings);
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/pool/PooledRepositoryConnection.java`
#### Snippet
```java

	public PooledRepositoryConnection(
			RepositoryConnection delegate, ObjectPool<RepositoryConnection> pool) {
		super(delegate.getRepository(), delegate);
		Objects.requireNonNull(delegate);
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/RepositoryConnectionWrappingUtils.java`
#### Snippet
```java
	public static <T> RepositoryConnection wrapOnce(
			RepositoryConnection con,
			Function<RepositoryConnection, RepositoryConnection> wrapper,
			Class<T> wrapperClass) {
		if (!isWrapped(con, wrapperClass)) {
```

### BoundedWildcard
Can generalize to `? extends RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/RepositoryConnectionWrappingUtils.java`
#### Snippet
```java
	public static <T> RepositoryConnection wrapOnce(
			RepositoryConnection con,
			Function<RepositoryConnection, RepositoryConnection> wrapper,
			Class<T> wrapperClass) {
		if (!isWrapped(con, wrapperClass)) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/query/DelegatingIterator.java`
#### Snippet
```java
	private final Iterator<T> delegate;

	public DelegatingIterator(Iterator<T> delegate) {
		this.delegate = delegate;
	}
```

### BoundedWildcard
Can generalize to `? super BindingsBuilder`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/opbuilder/OperationBuilder.java`
#### Snippet
```java
	}

	public SUB withBindings(Consumer<BindingsBuilder> consumer) {
		consumer.accept(this.bindingsBuilder);
		return (SUB) this;
```

### BoundedWildcard
Can generalize to `? extends Value`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/opbuilder/OperationBuilder.java`
#### Snippet
```java
	}

	public SUB withNullableBindings(Map<String, Value> bindings) {
		if (bindings != null) {
			bindings.forEach((key, value) -> bindingsBuilder.add(key, value));
```

### BoundedWildcard
Can generalize to `? extends Value`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/opbuilder/OperationBuilder.java`
#### Snippet
```java
	}

	public SUB withBindings(Map<String, Value> bindings) {
		bindings.forEach((key, value) -> bindingsBuilder.add(key, value));
		return (SUB) this;
```

### BoundedWildcard
Can generalize to `? super Integer`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ResultCachingTupleQuery.java`
#### Snippet
```java

	private TupleQueryResult recreateCachedResultIfPossible(
			ResultCache<Integer, ReusableTupleQueryResult> cache,
			BindingSet currentBindings,
			Integer cacheKey) {
```

### BoundedWildcard
Can generalize to `? extends ReusableTupleQueryResult`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ResultCachingTupleQuery.java`
#### Snippet
```java

	private TupleQueryResult recreateCachedResultIfPossible(
			ResultCache<Integer, ReusableTupleQueryResult> cache,
			BindingSet currentBindings,
			Integer cacheKey) {
```

### BoundedWildcard
Can generalize to `? super Integer`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ResultCachingGraphQuery.java`
#### Snippet
```java
			BindingSet currentBindings,
			Integer cacheKey,
			ResultCache<Integer, ReusableGraphQueryResult> cache) {
		ReusableGraphQueryResult result;
		result = cache.get(cacheKey);
```

### BoundedWildcard
Can generalize to `? extends ReusableGraphQueryResult`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ResultCachingGraphQuery.java`
#### Snippet
```java
			BindingSet currentBindings,
			Integer cacheKey,
			ResultCache<Integer, ReusableGraphQueryResult> cache) {
		ReusableGraphQueryResult result;
		result = cache.get(cacheKey);
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/RDF4JTemplate.java`
#### Snippet
```java
	}

	public <T> T applyToConnection(final Function<RepositoryConnection, T> fun) {
		RepositoryConnection con = getRepositoryConnection();
		if (logger.isDebugEnabled()) {
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/RDF4JTemplate.java`
#### Snippet
```java
	}

	public void consumeConnection(final Consumer<RepositoryConnection> fun) {
		RepositoryConnection con = getRepositoryConnection();
		if (logger.isDebugEnabled()) {
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/RDF4JTemplate.java`
#### Snippet
```java
			IRI fromResource,
			IRI property,
			Collection<IRI> toResources,
			boolean deleteOtherOutgoing,
			boolean deleteOtherIcoming) {
```

### BoundedWildcard
Can generalize to `? extends PropertyPath`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/RDF4JTemplate.java`
#### Snippet
```java
	 * @param propertyPaths paths by which to reach more resources to be deleted.
	 */
	public void delete(IRI start, List<PropertyPath> propertyPaths) {
		List<Variable> targets = new ArrayList<>();
		int i = 0;
```

### BoundedWildcard
Can generalize to `? extends Operation`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationcache/CachingOperationInstantiator.java`
#### Snippet
```java
			Class<?> owner,
			String operationName,
			Supplier<Operation> operationSupplier) {
		String key = makeOperationCacheKey(type, owner, operationName);
		if (logger.isDebugEnabled()) {
```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/FileIO.java`
#### Snippet
```java
	}

	public void writeStatement(CloseableIteration<? extends Statement, SailException> stIter, int tripleMarker,
			int quadMarker, DataOutputStream dataOut) throws IOException, SailException {
		try (stIter) {
```

### BoundedWildcard
Can generalize to `? super ChronoField`
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractLiteral.java`
#### Snippet
```java
		}

		private static int key(Predicate<ChronoField> include, ChronoField... fields) {

			int index = 0;
```

### BoundedWildcard
Can generalize to `? super String`
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/AbstractLiteral.java`
#### Snippet
```java
	 * @throws NullPointerException if {@code mapper} is {@code null}
	 */
	private <V> V value(Function<String, V> mapper) {
		return Optional
				.of(getLabel())
```

### BoundedWildcard
Can generalize to `? extends E`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/WeakObjectRegistry.java`
#### Snippet
```java
	}

	public E getOrAdd(K key, Supplier<E> supplier) {

		int index = getIndex(key);
```

### BoundedWildcard
Can generalize to `? extends MemStatement`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementIteratorCache.java`
#### Snippet
```java
		private Iterator<MemStatement> iter;

		public CachedIteration(Iterator<MemStatement> iter) {
			this.iter = iter;
		}
```

### BoundedWildcard
Can generalize to `? super Document`
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
	}

	private static void addDocuments(LeafReader reader, Term term, Collection<Document> documents) throws IOException {
		PostingsEnum docs = reader.postings(term);
		if (docs != null) {
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java

		@Override
		public synchronized void approveAll(Set<Statement> approved, Set<Resource> approvedContexts) {
			acquireExclusiveTransactionLock();
			invalidateCache();
```

### BoundedWildcard
Can generalize to `? super Integer`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java

			public ReservedSnapshot(int snapshot, Object reservedBy, boolean debug,
					LongAdder frequency, ConcurrentHashMap<Integer, LongAdder> activeSnapshots,
					AtomicInteger highestEverReservedSnapshot) {
				this.snapshot = snapshot;
```

### BoundedWildcard
Can generalize to `? extends LongAdder`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java

			public ReservedSnapshot(int snapshot, Object reservedBy, boolean debug,
					LongAdder frequency, ConcurrentHashMap<Integer, LongAdder> activeSnapshots,
					AtomicInteger highestEverReservedSnapshot) {
				this.snapshot = snapshot;
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java

		@Override
		public synchronized void deprecateAll(Set<Statement> deprecated) {
			acquireExclusiveTransactionLock();
			invalidateCache();
```

### BoundedWildcard
Can generalize to `? extends FF`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/FileFormat.java`
#### Snippet
```java
	 * @return A FileFormat object if the MIME type was recognized, or {@link Optional#empty()} otherwise.
	 */
	public static <FF extends FileFormat> Optional<FF> matchMIMEType(String mimeType, Iterable<FF> fileFormats) {
		// First try to match with the default MIME type
		for (FF fileFormat : fileFormats) {
```

### BoundedWildcard
Can generalize to `? extends FF`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/FileFormat.java`
#### Snippet
```java
	 * @return A FileFormat that matches the file name extension, or {@link Optional#empty()} otherwise.
	 */
	public static <FF extends FileFormat> Optional<FF> matchFileName(String fileName, Iterable<FF> fileFormats) {
		// Strip any directory info from the file name
		int lastPathSepIdx = Math.max(fileName.lastIndexOf('/'), fileName.lastIndexOf('\\'));
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, List<V>> toMapOfList(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? extends K`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, List<V>> toMapOfList(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, List<V>> toMapOfList(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? extends V`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, List<V>> toMapOfList(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? extends O`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java

	public <T, O> Optional<O> toSingletonOptional(
			BindingSetMapper<T> mapper, MappingPostProcessor<T, O> postProcessor) {
		return Optional.ofNullable(toSingletonMaybe(mapper, postProcessor));
	}
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 * specified mapper.
	 */
	public <T> Optional<T> toSingletonOptional(BindingSetMapper<T> mapper) {
		return Optional.ofNullable(toSingletonMaybe(mapper));
	}
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	}

	private <T> Stream<T> toStreamInternal(Function<BindingSet, T> mapper) {
		return applyToResult(
				result -> getBindingStream(result)
```

### BoundedWildcard
Can generalize to `? super TupleQueryResult`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 * Passes the {@link TupleQueryResult} to the consumer and closes the result afterwards.
	 */
	public void consumeResult(Consumer<TupleQueryResult> consumer) {
		try {
			consumer.accept(tupleQueryResult);
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 * Maps the query result to a {@link Map}, throwing an Exception if there are multiple values for one key.
	 */
	public <K, V> Map<K, V> toMap(Function<BindingSet, Map.Entry<K, V>> entryMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? extends Map.Entry`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 * Maps the query result to a {@link Map}, throwing an Exception if there are multiple values for one key.
	 */
	public <K, V> Map<K, V> toMap(Function<BindingSet, Map.Entry<K, V>> entryMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, V> toMap(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(mapper, Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? super T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, V> toMap(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(mapper, Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? extends K`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, V> toMap(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(mapper, Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? super T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, V> toMap(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(mapper, Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? extends V`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, V> toMap(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(mapper, Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, Set<V>> toMapOfSet(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper, Collectors.groupingBy(keyMapper, mapping(valueMapper, Collectors.toSet())));
```

### BoundedWildcard
Can generalize to `? super T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, Set<V>> toMapOfSet(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper, Collectors.groupingBy(keyMapper, mapping(valueMapper, Collectors.toSet())));
```

### BoundedWildcard
Can generalize to `? extends K`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, Set<V>> toMapOfSet(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper, Collectors.groupingBy(keyMapper, mapping(valueMapper, Collectors.toSet())));
```

### BoundedWildcard
Can generalize to `? super T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, Set<V>> toMapOfSet(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper, Collectors.groupingBy(keyMapper, mapping(valueMapper, Collectors.toSet())));
```

### BoundedWildcard
Can generalize to `? extends V`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, Set<V>> toMapOfSet(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper, Collectors.groupingBy(keyMapper, mapping(valueMapper, Collectors.toSet())));
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	private <T, O> Function<BindingSet, O> andThenOrElseNull(
			BindingSetMapper<T> mapper, MappingPostProcessor<T, O> postProcessor) {
		return bindingSet -> Optional.ofNullable(mapper.apply(bindingSet))
				.map(postProcessor)
```

### BoundedWildcard
Can generalize to `? super T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	private <T, O> Function<BindingSet, O> andThenOrElseNull(
			BindingSetMapper<T> mapper, MappingPostProcessor<T, O> postProcessor) {
		return bindingSet -> Optional.ofNullable(mapper.apply(bindingSet))
				.map(postProcessor)
```

### BoundedWildcard
Can generalize to `? extends O`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	private <T, O> Function<BindingSet, O> andThenOrElseNull(
			BindingSetMapper<T> mapper, MappingPostProcessor<T, O> postProcessor) {
		return bindingSet -> Optional.ofNullable(mapper.apply(bindingSet))
				.map(postProcessor)
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	}

	public <T, A, R> R mapAndCollect(Function<BindingSet, T> mapper, Collector<T, A, R> collector) {
		return applyToResult(
				result -> getBindingStream(result)
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	}

	public <T, A, R> R mapAndCollect(Function<BindingSet, T> mapper, Collector<T, A, R> collector) {
		return applyToResult(
				result -> getBindingStream(result)
```

### BoundedWildcard
Can generalize to `? super T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	}

	public <T, A, R> R mapAndCollect(Function<BindingSet, T> mapper, Collector<T, A, R> collector) {
		return applyToResult(
				result -> getBindingStream(result)
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, Set<V>> toMapOfSet(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? extends K`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, Set<V>> toMapOfSet(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, Set<V>> toMapOfSet(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? extends V`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, Set<V>> toMapOfSet(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(
				Function.identity(),
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, List<V>> toMapOfList(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper,
```

### BoundedWildcard
Can generalize to `? super T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, List<V>> toMapOfList(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper,
```

### BoundedWildcard
Can generalize to `? extends K`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, List<V>> toMapOfList(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper,
```

### BoundedWildcard
Can generalize to `? super T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, List<V>> toMapOfList(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper,
```

### BoundedWildcard
Can generalize to `? extends V`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <T, K, V> Map<K, List<V>> toMapOfList(
			BindingSetMapper<T> mapper, Function<T, K> keyMapper, Function<T, V> valueMapper) {
		return mapAndCollect(
				mapper,
```

### BoundedWildcard
Can generalize to `? extends T`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 * Maps the whole {@link TupleQueryResult} to one {@link Optional}.
	 */
	public <T> Optional<T> toSingletonOptionalOfWholeResult(TupleQueryResultMapper<T> mapper) {
		return Optional.ofNullable(toSingletonMaybeOfWholeResult(mapper));
	}
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, V> toMap(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(Function.identity(), Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? extends K`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, V> toMap(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(Function.identity(), Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, V> toMap(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(Function.identity(), Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? extends V`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 */
	public <K, V> Map<K, V> toMap(
			Function<BindingSet, K> keyMapper, Function<BindingSet, V> valueMapper) {
		return mapAndCollect(Function.identity(), Collectors.toMap(keyMapper, valueMapper));
	}
```

### BoundedWildcard
Can generalize to `? super TupleQueryResult`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/TupleQueryResultConverter.java`
#### Snippet
```java
	 * Applies the function to the {@link TupleQueryResult} and closes the result afterwards.
	 */
	public <T> T applyToResult(Function<TupleQueryResult, T> function) {
		try {
			return function.apply(tupleQueryResult);
```

### BoundedWildcard
Can generalize to `? extends List`
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrDocumentScore.java`
#### Snippet
```java
	private final Map<String, List<String>> highlighting;

	public SolrDocumentScore(SolrSearchDocument doc, Map<String, List<String>> highlighting) {
		super(doc);
		this.highlighting = highlighting;
```

### BoundedWildcard
Can generalize to `? super String`
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrSearchDocument.java`
#### Snippet
```java
	}

	private static void addField(String name, String value, Map<String, Object> document) {
		Object oldValue = document.get(name);
		Object newValue;
```

### BoundedWildcard
Can generalize to `? extends S`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/PatternIterator.java`
#### Snippet
```java
	private boolean nextCalled;

	public PatternIterator(Iterator<S> iter, Value subj, Value pred, Value obj, Value... contexts) {
		this.filteredIter = iter;
		this.subj = subj;
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Namespaces.java`
#### Snippet
```java
	 *         the prefix and names from {@link Namespace}s in the input set.
	 */
	public static Map<String, String> asMap(Set<Namespace> namespaces) {
		Map<String, String> result = new HashMap<>();

```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
		ModelNode<Resource> ctx;

		public ModelStatement(ModelNode<Resource> subj, ModelNode<IRI> pred, ModelNode<Value> obj,
				ModelNode<Resource> ctx) {
			super(subj.getValue(), pred.getValue(), obj.getValue(), ctx.getValue());
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
		ModelNode<Resource> ctx;

		public ModelStatement(ModelNode<Resource> subj, ModelNode<IRI> pred, ModelNode<Value> obj,
				ModelNode<Resource> ctx) {
			super(subj.getValue(), pred.getValue(), obj.getValue(), ctx.getValue());
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java

		public ModelStatement(ModelNode<Resource> subj, ModelNode<IRI> pred, ModelNode<Value> obj,
				ModelNode<Resource> ctx) {
			super(subj.getValue(), pred.getValue(), obj.getValue(), ctx.getValue());
			this.subj = subj;
```

### BoundedWildcard
Can generalize to `? extends ModelStatement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
		private ModelStatement last;

		public ModelIterator(Iterator<ModelStatement> iter, Set<ModelStatement> owner) {
			this.iter = iter;
			this.owner = owner;
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
	}

	public LinkedHashModel(Set<Namespace> namespaces, int size) {
		this(size);
		this.namespaces.addAll(namespaces);
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
		}

		public ModelStatement(ModelNode<Resource> subj, ModelNode<IRI> pred, ModelNode<Value> obj,
				ModelNode<Resource> ctx, Statement statement) {
			super(subj.getValue(), pred.getValue(), obj.getValue(), ctx.getValue());
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
		}

		public ModelStatement(ModelNode<Resource> subj, ModelNode<IRI> pred, ModelNode<Value> obj,
				ModelNode<Resource> ctx, Statement statement) {
			super(subj.getValue(), pred.getValue(), obj.getValue(), ctx.getValue());
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java

		public ModelStatement(ModelNode<Resource> subj, ModelNode<IRI> pred, ModelNode<Value> obj,
				ModelNode<Resource> ctx, Statement statement) {
			super(subj.getValue(), pred.getValue(), obj.getValue(), ctx.getValue());
			this.subj = subj;
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
	}

	public LinkedHashModel(Set<Namespace> namespaces, Collection<? extends Statement> c) {
		this(c);
		this.namespaces.addAll(namespaces);
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
	}

	public LinkedHashModel(Set<Namespace> namespaces) {
		this();
		this.namespaces.addAll(namespaces);
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
	}

	private void removeAll(TreeSet<Statement> owner, StatementTree chosen, Iterator<Statement> iter) {
		while (iter.hasNext()) {
			Statement last = iter.next();
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
	}

	public TreeModel(Set<Namespace> namespaces) {
		this();
		this.namespaces.addAll(namespaces);
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
		private Statement last;

		public ModelIterator(Iterator<Statement> iter, StatementTree owner) {
			this.iter = iter;
			this.owner = owner.tree;
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
	}

	public TreeModel(Set<Namespace> namespaces, Collection<? extends Statement> c) {
		this(c);
		this.namespaces.addAll(namespaces);
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
	}

	public static String getLabel(Optional<Value> v, String fallback) {
		return v != null ? getLabel(v.orElseGet(null), fallback) : fallback;
	}
```

### BoundedWildcard
Can generalize to `? extends BNode`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	}

	protected static Map<BNode, BNode> findCompatibleAutomorphism(List<BNode> fixpoints,
			List<Map<BNode, HashCode>> partitionMappings) {
		// check if two mappings with identical hash codes exist
```

### BoundedWildcard
Can generalize to `? extends Map`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java

	protected static Map<BNode, BNode> findCompatibleAutomorphism(List<BNode> fixpoints,
			List<Map<BNode, HashCode>> partitionMappings) {
		// check if two mappings with identical hash codes exist
		for (Map<BNode, HashCode> mapping : partitionMappings) {
```

### BoundedWildcard
Can generalize to `? extends BNode`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java

	private static Map<BNode, HashCode> distinguish(Model m, Partitioning partitioning,
			Map<BNode, HashCode> lowestFound, List<BNode> parentFixpoints,
			List<Map<BNode, HashCode>> finePartitionMappings) {

```

### BoundedWildcard
Can generalize to `? extends BNode`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	}

	protected static Multimap<HashCode, BNode> partitionMapping(Map<BNode, HashCode> blankNodeMapping) {
		return Multimaps.invertFrom(Multimaps.forMap(blankNodeMapping),
				MultimapBuilder.hashKeys(blankNodeMapping.keySet().size()).arrayListValues().build());
```

### BoundedWildcard
Can generalize to `? extends HashCode`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	}

	protected static Multimap<HashCode, BNode> partitionMapping(Map<BNode, HashCode> blankNodeMapping) {
		return Multimaps.invertFrom(Multimaps.forMap(blankNodeMapping),
				MultimapBuilder.hashKeys(blankNodeMapping.keySet().size()).arrayListValues().build());
```

### BoundedWildcard
Can generalize to `? extends HashCode`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	}

	private static boolean mappingsIncompatible(Map<BNode, HashCode> mapping1, Map<BNode, HashCode> mapping2) {
		if (mapping1.size() != mapping2.size()) {
			return true;
```

### BoundedWildcard
Can generalize to `? extends HashCode`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
	}

	private static boolean mappingsIncompatible(Map<BNode, HashCode> mapping1, Map<BNode, HashCode> mapping2) {
		if (mapping1.size() != mapping2.size()) {
			return true;
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java
	public static <E extends RDF4JException> void extract(IRI containerType, GetStatementOptional statementSupplier,
			Resource container,
			Consumer<Statement> collectionConsumer, Function<String, Supplier<E>> exceptionSupplier,
			Resource... contexts) throws E {
		Objects.requireNonNull(contexts,
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java
	 */

	public static void consumeValues(final Model m, Resource container, IRI containerType, Consumer<Value> consumer,
			Resource... contexts)
			throws ModelException {
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Values.java`
#### Snippet
```java
	 * @since 3.6.0
	 */
	public static IRI iri(Iterable<Namespace> namespaces, String prefixedName) throws IllegalArgumentException {
		if (prefixedName.indexOf(':') < 0) {
			throw new IllegalArgumentException("Invalid prefixed name: '" + prefixedName + "'");
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
	 */
	public static void consume(ValueFactory vf, Resource subject, IRI predicate, Value object,
			Consumer<Statement> consumer, Resource... contexts) {
		Objects.requireNonNull(contexts,
				"contexts argument may not be null; either the value should be cast to Resource or an empty array should be supplied");
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFCollections.java`
#### Snippet
```java
	 */
	public static <E extends RDF4JException> void extract(GetStatementOptional statementSupplier, Resource head,
			Consumer<Statement> collectionConsumer, Function<String, Supplier<E>> exceptionSupplier,
			Resource... contexts) throws E {
		Objects.requireNonNull(contexts,
```

### BoundedWildcard
Can generalize to `? super String`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFCollections.java`
#### Snippet
```java
	 */
	public static <E extends RDF4JException> void extract(GetStatementOptional statementSupplier, Resource head,
			Consumer<Statement> collectionConsumer, Function<String, Supplier<E>> exceptionSupplier,
			Resource... contexts) throws E {
		Objects.requireNonNull(contexts,
```

### BoundedWildcard
Can generalize to `? extends Supplier`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFCollections.java`
#### Snippet
```java
	 */
	public static <E extends RDF4JException> void extract(GetStatementOptional statementSupplier, Resource head,
			Consumer<Statement> collectionConsumer, Function<String, Supplier<E>> exceptionSupplier,
			Resource... contexts) throws E {
		Objects.requireNonNull(contexts,
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFCollections.java`
#### Snippet
```java
	 *      vocabulary</a>
	 */
	public static void consumeValues(final Model m, Resource head, Consumer<Value> consumer, Resource... contexts)
			throws ModelException {
		Objects.requireNonNull(consumer, "consumer may not be null");
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
	}

	private void getUsedNamespaces(Set<Statement> stmts, Set<String> used) {
		for (Statement st : stmts) {
			if (st.getSubject() instanceof IRI) {
```

### BoundedWildcard
Can generalize to `? super String`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
	}

	private void getUsedNamespaces(Set<Statement> stmts, Set<String> used) {
		for (Statement st : stmts) {
			if (st.getSubject() instanceof IRI) {
```

### BoundedWildcard
Can generalize to `? super IRI`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java

	private void processPredicate(Model contextData, Resource subject, IRI predicate, Set<Resource> processedSubjects,
			Set<IRI> processedPredicates) {
		for (Statement st : contextData.getStatements(subject, predicate, null)) {
			boolean canInlineObject = canInlineValue(contextData, st.getObject());
```

### BoundedWildcard
Can generalize to `? super EmptyPropertyPathBuilder`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/rdf/RdfBlankNode.java`
#### Snippet
```java
		 * @return
		 */
		public PropertiesBlankNode andHas(Consumer<EmptyPropertyPathBuilder> propertyPathConfigurer,
				RdfObject... objects) {
			EmptyPropertyPathBuilder pathBuilder = new EmptyPropertyPathBuilder();
```

### BoundedWildcard
Can generalize to `? super EmptyPropertyPathBuilder`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/rdf/RdfPredicateObjectList.java`
#### Snippet
```java
	}

	private static String buildPath(Consumer<EmptyPropertyPathBuilder> propertyPathConfigurer) {
		EmptyPropertyPathBuilder pathBuilder = new EmptyPropertyPathBuilder();
		propertyPathConfigurer.accept(pathBuilder);
```

### BoundedWildcard
Can generalize to `? super EmptyPropertyPathBuilder`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/rdf/RdfPredicateObjectListCollection.java`
#### Snippet
```java
	 * @return
	 */
	public RdfPredicateObjectListCollection andHas(Consumer<EmptyPropertyPathBuilder> propertyPathConfigurer,
			RdfObject... objects) {
		EmptyPropertyPathBuilder pathBuilder = new EmptyPropertyPathBuilder();
```

### BoundedWildcard
Can generalize to `? extends GraphName`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/UpdateQuery.java`
#### Snippet
```java
	}

	protected void appendNamedTriplesTemplates(StringBuilder queryString, Optional<GraphName> graphName,
			TriplesTemplate triples) {
		queryString.append(graphName
```

### BoundedWildcard
Can generalize to `? super String`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/StandardQueryElementCollection.java`
#### Snippet
```java
	}

	protected void setWrapperMethod(Function<String, String> wrapperMethod) {
		this.wrapperMethod = wrapperMethod;
	}
```

### BoundedWildcard
Can generalize to `? extends O`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/util/SparqlBuilderUtils.java`
#### Snippet
```java
	private static final String PAD = " ";

	public static <O> Optional<O> getOrCreateAndModifyOptional(Optional<O> optional, Supplier<O> getter,
			UnaryOperator<O> operator) {
		return Optional.of(operator.apply(optional.orElseGet(getter)));
```

### BoundedWildcard
Can generalize to `? super EmptyPropertyPathBuilder`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/GraphPatterns.java`
#### Snippet
```java
	 * @return the triple pattern
	 */
	public static TriplePattern tp(RdfSubject subject, Consumer<EmptyPropertyPathBuilder> propertyPathConfigurer,
			RdfObject... objects) {
		EmptyPropertyPathBuilder builder = new EmptyPropertyPathBuilder();
```

### BoundedWildcard
Can generalize to `? super EmptyPropertyPathBuilder`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/propertypath/builder/PropertyPathBuilder.java`
#### Snippet
```java

	private PropertyPathBuilder withSubtree(
			Consumer<EmptyPropertyPathBuilder> subtreeBuilder,
			BiFunction<PropertyPath, PropertyPath, PropertyPath> assembler) {
		Objects.requireNonNull(head);
```

### BoundedWildcard
Can generalize to `? super PropertyPath`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/propertypath/builder/PropertyPathBuilder.java`
#### Snippet
```java
	private PropertyPathBuilder withSubtree(
			Consumer<EmptyPropertyPathBuilder> subtreeBuilder,
			BiFunction<PropertyPath, PropertyPath, PropertyPath> assembler) {
		Objects.requireNonNull(head);
		EmptyPropertyPathBuilder b = new EmptyPropertyPathBuilder();
```

### BoundedWildcard
Can generalize to `? super PropertyPath`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/propertypath/builder/PropertyPathBuilder.java`
#### Snippet
```java
	private PropertyPathBuilder withSubtree(
			Consumer<EmptyPropertyPathBuilder> subtreeBuilder,
			BiFunction<PropertyPath, PropertyPath, PropertyPath> assembler) {
		Objects.requireNonNull(head);
		EmptyPropertyPathBuilder b = new EmptyPropertyPathBuilder();
```

### BoundedWildcard
Can generalize to `? extends PropertyPath`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/propertypath/builder/PropertyPathBuilder.java`
#### Snippet
```java
	private PropertyPathBuilder withSubtree(
			Consumer<EmptyPropertyPathBuilder> subtreeBuilder,
			BiFunction<PropertyPath, PropertyPath, PropertyPath> assembler) {
		Objects.requireNonNull(head);
		EmptyPropertyPathBuilder b = new EmptyPropertyPathBuilder();
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @see Model#objects()
	 */
	public static Set<Resource> objectResources(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #predicate(Model)}.
	 */
	public static Optional<IRI> predicate(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false).map(st -> st.getPredicate()).findAny();
	}
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #subjectBNodes(Model)}.
	 */
	public static Set<BNode> subjectBNodes(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getSubject())
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #subjectIRIs(Model)}.
	 */
	public static Set<IRI> subjectIRIs(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getSubject())
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #objectString(Model)}.
	 */
	public static Optional<String> objectString(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false).map(st -> st.getObject().stringValue()).findAny();
	}
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @see Model#objects()
	 */
	public static Set<String> objectStrings(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject().stringValue())
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #objectIRI(Model)}.
	 */
	public static Optional<IRI> objectIRI(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	}

	private static boolean bnodeValueMatching(Map<Resource, Resource> bNodeMapping, Value obj1, Value obj2) {
		if (isBlank(obj1) && isBlank(obj2)) {
			Resource mappedBNode = bNodeMapping.get(obj1);
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #subjectBNode(Model)}.
	 */
	public static Optional<BNode> subjectBNode(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getSubject())
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #object(Model)}.
	 */
	public static Optional<Value> object(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false).map(st -> st.getObject()).findAny();
	}
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	}

	private static Map<Resource, Resource> createNewBnodeMapping(Map<Resource, Resource> bNodeMapping, Statement st1,
			Statement st2) {
		Map<Resource, Resource> newBNodeMapping = new HashMap<>(bNodeMapping);
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	}

	private static Map<Resource, Resource> createNewBnodeMapping(Map<Resource, Resource> bNodeMapping, Statement st1,
			Statement st2) {
		Map<Resource, Resource> newBNodeMapping = new HashMap<>(bNodeMapping);
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @see Model#objects()
	 */
	public static Set<Literal> objectLiterals(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	}

	private static boolean matchModels(Set<Statement> model1, Model model2) {
		// Compare statements without blank nodes first, save the rest for later
		List<Statement> model1BNodes = new ArrayList<>(model1.size());
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @see Model#objects()
	 */
	public static Set<IRI> objectIRIs(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #subjectIRI(Model)}.
	 */
	public static Optional<IRI> subjectIRI(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getSubject())
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #subject(Model)}.
	 */
	public static Optional<Resource> subject(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false).map(st -> st.getSubject()).findAny();
	}
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #objectLiteral(Model)}.
	 */
	public static Optional<Literal> objectLiteral(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 */
	@Experimental
	public static void convertReificationToRDFStar(ValueFactory vf, Model model, Consumer<Statement> consumer) {
		Map<Resource, Triple> convertedStatements = new HashMap<>();
		model.filter(null, RDF.TYPE, RDF.STATEMENT).forEach((s) -> {
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 * @apiNote this method signature is new in 3.2.0, and is a generalization of {@link #objectResource(Model)}.
	 */
	public static Optional<Resource> objectResource(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
```

### BoundedWildcard
Can generalize to `? extends QueryResultFormat`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	 * @throws MalformedQueryException
	 */
	private HttpResponse sendTupleQueryViaHttp(HttpUriRequest method, Set<QueryResultFormat> tqrFormats)
			throws RepositoryException, IOException, QueryInterruptedException, MalformedQueryException {

```

### BoundedWildcard
Can generalize to `? extends QueryResultFormat`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	}

	private HttpResponse sendBooleanQueryViaHttp(HttpUriRequest method, Set<QueryResultFormat> booleanFormats)
			throws IOException, RDF4JException {

```

### BoundedWildcard
Can generalize to `? extends Set`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DistinctIteration.java`
#### Snippet
```java
	}

	public DistinctIteration(Iteration<? extends E, ? extends X> iter, Supplier<Set<E>> setMaker) {
		super(iter);
		excludeSet = setMaker.get();
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
	 * @param iteration the iteration to wrap
	 */
	public IterationSpliterator(final Iteration<T, ? extends Exception> iteration) {
		super(Long.MAX_VALUE, Spliterator.IMMUTABLE | Spliterator.NONNULL);
		this.iteration = iteration;
```

### BoundedWildcard
Can generalize to `? extends Set`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	 */
	public static <E, X extends Exception> Set<E> asSet(CloseableIteration<? extends E, ? extends X> iteration,
			Supplier<Set<E>> setMaker) throws X {
		Set<E> set = setMaker.get();
		while (iteration.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends Set`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
	@Deprecated(since = "4.1.0", forRemoval = true)
	public static <E, X extends Exception> Set<E> asSet(Iteration<? extends E, ? extends X> iteration,
			Supplier<Set<E>> setMaker) throws X {
		Set<E> set = setMaker.get();
		while (iteration.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public MinusIteration(Iteration<? extends E, X> leftArg, Iteration<? extends E, X> rightArg, boolean distinct,
			Supplier<Set<E>> setMaker) {
		super(leftArg);
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
	 * @param distinct Flag indicating whether duplicate elements should be filtered from the result.
	 */
	public MinusIteration(Iteration<? extends E, X> leftArg, Iteration<? extends E, X> rightArg, boolean distinct) {
		super(leftArg);

```

### BoundedWildcard
Can generalize to `? extends Set`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
	 */
	public IntersectIteration(Iteration<? extends E, ? extends X> arg1, Iteration<? extends E, ? extends X> arg2,
			boolean distinct, Supplier<Set<E>> setMaker) {
		super(arg1);

```

### BoundedWildcard
Can generalize to `? extends T`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/SilentIteration.java`
#### Snippet
```java
	private static final Logger logger = LoggerFactory.getLogger(SilentIteration.class);

	public SilentIteration(CloseableIteration<T, E> iter) {
		super(iter);
	}
```

### BoundedWildcard
Can generalize to `? extends E`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/SilentIteration.java`
#### Snippet
```java
	private static final Logger logger = LoggerFactory.getLogger(SilentIteration.class);

	public SilentIteration(CloseableIteration<T, E> iter) {
		super(iter);
	}
```

### BoundedWildcard
Can generalize to `? extends RepositoryException`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryResult.java`
#### Snippet
```java
	private CloseableIteration<? extends T, RepositoryException> wrappedIter;

	public RepositoryResult(CloseableIteration<? extends T, RepositoryException> iter) {
		assert iter != null;
		wrappedIter = iter;
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
	private boolean closed = false;

	private DualUnionIteration(CloseableIteration<? extends E, X> iteration1,
			CloseableIteration<? extends E, X> iteration2) {
		this.iteration1 = iteration1;
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java

	private DualUnionIteration(CloseableIteration<? extends E, X> iteration1,
			CloseableIteration<? extends E, X> iteration2) {
		this.iteration1 = iteration1;
		this.iteration2 = iteration2;
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
	 * @param iteration the iteration to wrap
	 */
	public CloseableIterationSpliterator(CloseableIteration<T, E> iteration) {
		super(Long.MAX_VALUE, Spliterator.IMMUTABLE | Spliterator.NONNULL);
		this.iteration = iteration;
```

### BoundedWildcard
Can generalize to `? extends BNode`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RepositoryUtil.java`
#### Snippet
```java
	}

	private static boolean statementsMatch(Statement st1, Statement st2, Map<BNode, BNode> bNodeMapping) {
		IRI pred1 = st1.getPredicate();
		IRI pred2 = st2.getPredicate();
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/RepositoryConnectionWrapper.java`
#### Snippet
```java
	 * supplied RDFHandler.
	 */
	protected void exportStatements(RepositoryResult<Statement> stIter, RDFHandler handler)
			throws RepositoryException, RDFHandlerException {
		try (stIter) {
```

### BoundedWildcard
Can generalize to `? extends ExtensibleStatement`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/DataStructureInterface.java`
#### Snippet
```java
	void removeStatement(ExtensibleStatement statement);

	default void addStatement(Collection<ExtensibleStatement> statements) {
		for (ExtensibleStatement statement : statements) {
			addStatement(statement);
```

### BoundedWildcard
Can generalize to `? extends ExtensibleStatement`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/DataStructureInterface.java`
#### Snippet
```java
	}

	default void removeStatement(Collection<ExtensibleStatement> statements) {
		for (ExtensibleStatement statement : statements) {
			removeStatement(statement);
```

### BoundedWildcard
Can generalize to `? extends E`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/FilteringIteration.java`
#### Snippet
```java
	private final Resource[] context;

	public FilteringIteration(CloseableIteration<E, X> wrappedIteration, Resource subject, IRI predicate, Value object,
			boolean inferred, Resource... context) {
		this.wrappedIteration = wrappedIteration;
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/FilteringIteration.java`
#### Snippet
```java
	private final Resource[] context;

	public FilteringIteration(CloseableIteration<E, X> wrappedIteration, Resource subject, IRI predicate, Value object,
			boolean inferred, Resource... context) {
		this.wrappedIteration = wrappedIteration;
```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailStore.java`
#### Snippet
```java

	private void addToStats(DynamicStatistics instance,
			CloseableIteration<? extends ExtensibleStatement, SailException> statements) {

		long estimatedSize = dataStructure.getEstimatedSize();
```

### BoundedWildcard
Can generalize to `? super ExtensibleSailStore`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/EvaluationStatisticsEnum.java`
#### Snippet
```java

	EvaluationStatisticsEnum(String comment,
			Function<ExtensibleSailStore, ExtensibleEvaluationStatistics> evaluationStatisticsSupplier) {
		this.evaluationStatisticsSupplier = evaluationStatisticsSupplier;
	}
```

### BoundedWildcard
Can generalize to `? extends ExtensibleEvaluationStatistics`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/EvaluationStatisticsEnum.java`
#### Snippet
```java

	EvaluationStatisticsEnum(String comment,
			Function<ExtensibleSailStore, ExtensibleEvaluationStatistics> evaluationStatisticsSupplier) {
		this.evaluationStatisticsSupplier = evaluationStatisticsSupplier;
	}
```

### BoundedWildcard
Can generalize to `? super Integer`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
	}

	private void indexOneValue(long statementHash, Map<Integer, HLL> index, int indexHash) {
		index.compute(Math.abs(indexHash % SINGLE_DIMENSION_INDEX_SIZE), (key, val) -> {
			if (val == null) {
```

### BoundedWildcard
Can generalize to `? super RepositoryException`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 */
	public static <T> T get(Repository repository, Function<RepositoryConnection, T> processFunction,
			Consumer<RepositoryException> exceptionHandler)
			throws RepositoryException, UnknownTransactionStateException {
		try {
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 *                                          exception)
	 */
	public static <T> T getNoTransaction(Repository repository, Function<RepositoryConnection, T> processFunction)
			throws RepositoryException, UnknownTransactionStateException {
		RepositoryConnection conn = null;
```

### BoundedWildcard
Can generalize to `? super RepositoryException`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 */
	public static void consume(Repository repository, Consumer<RepositoryConnection> processFunction,
			Consumer<RepositoryException> exceptionHandler)
			throws RepositoryException, UnknownTransactionStateException {
		try {
```

### BoundedWildcard
Can generalize to `? super RepositoryException`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 */
	public static <T> T getNoTransaction(Repository repository, Function<RepositoryConnection, T> processFunction,
			Consumer<RepositoryException> exceptionHandler)
			throws RepositoryException, UnknownTransactionStateException {
		try {
```

### BoundedWildcard
Can generalize to `? super GraphQueryResult`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 */
	public static <T> T graphQueryNoTransaction(Repository repository, String query,
			Function<GraphQueryResult, T> processFunction) throws RepositoryException, UnknownTransactionStateException,
			MalformedQueryException, QueryEvaluationException {
		return getNoTransaction(repository, conn -> {
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 *                                          exception)
	 */
	public static void consume(Repository repository, Consumer<RepositoryConnection> processFunction)
			throws RepositoryException, UnknownTransactionStateException {
		get(repository, conn -> {
```

### BoundedWildcard
Can generalize to `? super TupleQueryResult`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 * @throws QueryEvaluationException         If there was an error evaluating the query
	 */
	public static <T> T tupleQuery(Repository repository, String query, Function<TupleQueryResult, T> processFunction)
			throws RepositoryException, UnknownTransactionStateException, MalformedQueryException,
			QueryEvaluationException {
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 *                                          exception)
	 */
	public static void consumeNoTransaction(Repository repository, Consumer<RepositoryConnection> processFunction)
			throws RepositoryException, UnknownTransactionStateException {
		getNoTransaction(repository, conn -> {
```

### BoundedWildcard
Can generalize to `? super GraphQueryResult`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 * @throws QueryEvaluationException         If there was an error evaluating the query
	 */
	public static <T> T graphQuery(Repository repository, String query, Function<GraphQueryResult, T> processFunction)
			throws RepositoryException, UnknownTransactionStateException, MalformedQueryException,
			QueryEvaluationException {
```

### BoundedWildcard
Can generalize to `? super RepositoryConnection`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 *                                          exception)
	 */
	public static <T> T get(Repository repository, Function<RepositoryConnection, T> processFunction)
			throws RepositoryException, UnknownTransactionStateException {
		RepositoryConnection conn = null;
```

### BoundedWildcard
Can generalize to `? super TupleQueryResult`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 */
	public static <T> T tupleQueryNoTransaction(Repository repository, String query,
			Function<TupleQueryResult, T> processFunction) throws RepositoryException, UnknownTransactionStateException,
			MalformedQueryException, QueryEvaluationException {
		return getNoTransaction(repository, conn -> {
```

### BoundedWildcard
Can generalize to `? super RepositoryException`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Repositories.java`
#### Snippet
```java
	 */
	public static void consumeNoTransaction(Repository repository, Consumer<RepositoryConnection> processFunction,
			Consumer<RepositoryException> exceptionHandler)
			throws RepositoryException, UnknownTransactionStateException {
		try {
```

### BoundedWildcard
Can generalize to `? extends E`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java
	private final CleanableState<E, X> state;

	public CleanerIteration(CloseableIteration<E, X> delegate, ConcurrentCleaner cleaner) {
		this.delegate = delegate;
		this.state = new CleanableState<>(delegate);
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java
	private final CleanableState<E, X> state;

	public CleanerIteration(CloseableIteration<E, X> delegate, ConcurrentCleaner cleaner) {
		this.delegate = delegate;
		this.state = new CleanableState<>(delegate);
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java
		private boolean closed = false;

		public CleanableState(CloseableIteration<E, X> iteration) {
			this.iteration = iteration;
		}
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/Lock.java`
#### Snippet
```java
			Supplier<T> tryLockSupplier;

			private Wrapper(Supplier<T> getLockSupplier, Supplier<T> tryLockSupplier) {
				this.getLockSupplier = getLockSupplier;
				this.tryLockSupplier = tryLockSupplier;
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/Lock.java`
#### Snippet
```java
			Supplier<T> tryLockSupplier;

			private Wrapper(Supplier<T> getLockSupplier, Supplier<T> tryLockSupplier) {
				this.getLockSupplier = getLockSupplier;
				this.tryLockSupplier = tryLockSupplier;
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockCleaner.java`
#### Snippet
```java
	private final boolean stacktrace;

	public LockCleaner(boolean stacktrace, String alias, Logger logger, Lock.ExtendedSupplier<T> supplier) {
		this.stacktrace = stacktrace;
		this.supplier = supplier;
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/StampedLockManager.java`
#### Snippet
```java
		private final StampedLockManager stampedLockManager;

		public Cache(StampedLockManager stampedLockManager, Supplier<T> dataSupplier) {
			this.dataSupplier = dataSupplier;
			this.stampedLockManager = stampedLockManager;
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockTracking.java`
#### Snippet
```java

	public LockTracking(boolean stacktrace, String alias, Logger logger, int waitToCollect,
			Lock.ExtendedSupplier<T> supplier) {
		this.stacktrace = stacktrace;
		this.supplier = supplier;
```

### BoundedWildcard
Can generalize to `? extends BNode`
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResults.java`
#### Snippet
```java
	}

	private static boolean bindingSetsMatch(BindingSet bs1, BindingSet bs2, Map<BNode, BNode> bNodeMapping) {

		if (bs1.size() != bs2.size()) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResults.java`
#### Snippet
```java
	 * @return a List containing all elements obtained from the specified query result.
	 */
	public static <T> List<T> asList(QueryResult<T> queryResult) throws QueryEvaluationException {
		// stream.collect is slightly slower than addAll for lists
		List<T> list = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLQueries.java`
#### Snippet
```java
	 * @since 3.6.0
	 */
	public static String getPrefixClauses(Iterable<Namespace> namespaces) {
		final StringBuilder sb = new StringBuilder();
		for (Namespace namespace : namespaces) {
```

### BoundedWildcard
Can generalize to `? super String`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/PrefixDeclProcessor.java`
#### Snippet
```java
	}

	private static int insertDefaultPrefix(Map<String, String> prefixMap, String prefix, String namespace) {
		if (!prefixMap.containsKey(prefix) && !prefixMap.containsValue(namespace)) {
			prefixMap.put(prefix, namespace);
```

### BoundedWildcard
Can generalize to `? super String`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/PrefixDeclProcessor.java`
#### Snippet
```java
	}

	private static int insertDefaultPrefix(Map<String, String> prefixMap, String prefix, String namespace) {
		if (!prefixMap.containsKey(prefix) && !prefixMap.containsValue(namespace)) {
			prefixMap.put(prefix, namespace);
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
	}

	private int verifyQueryResult(CloseableIteration<? extends BindingSet, QueryEvaluationException> resultIter,
			int expectedBindingCount) throws QueryEvaluationException {
		int resultCount = 0;
```

### BoundedWildcard
Can generalize to `? extends StatementPattern`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ConstructorBuilder.java`
#### Snippet
```java
	 * and object variables from the supplied statement patterns, but ignores any context variables.
	 */
	private Set<Var> getConstructVars(Collection<StatementPattern> statementPatterns) {
		Set<Var> vars = new LinkedHashSet<>(statementPatterns.size() * 2);

```

### BoundedWildcard
Can generalize to `? extends ValueExpr`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/GraphPattern.java`
#### Snippet
```java
	}

	public void addConstraints(Collection<ValueExpr> constraints) {
		this.constraints.addAll(constraints);
	}
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/BackgroundGraphResult.java`
#### Snippet
```java
	}

	public BackgroundGraphResult(QueueCursor<Statement> queue, RDFParser parser, InputStream in, Charset charset,
			String baseURI) {
		super(queue);
```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepositoryConnection.java`
#### Snippet
```java
	 * Wraps a CloseableIteration coming from a Sail in a RepositoryResult object, applying the required conversions
	 */
	protected <E> RepositoryResult<E> createRepositoryResult(CloseableIteration<? extends E, SailException> sailIter) {
		return new RepositoryResult<>(new SailCloseableIteration<E>(sailIter));
	}
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/ValueMappingBindingSet.java`
#### Snippet
```java
	private final Function<Value, Value> mapper;

	ValueMappingBindingSet(BindingSet delegate, Function<Value, Value> mapper) {
		this.delegate = delegate;
		this.mapper = mapper;
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/ValueMappingBindingSet.java`
#### Snippet
```java
	private final Function<Value, Value> mapper;

	ValueMappingBindingSet(BindingSet delegate, Function<Value, Value> mapper) {
		this.delegate = delegate;
		this.mapper = mapper;
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/helpers/BackgroundTupleResult.java`
#### Snippet
```java
	}

	public BackgroundTupleResult(QueueCursor<BindingSet> queue, TupleQueryResultParser parser, InputStream in) {
		super(Collections.emptyList(), queue);
		this.queue = queue;
```

### BoundedWildcard
Can generalize to `? extends SameTerm`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java

	private List<StatementPattern> replaceSameTermVars(List<StatementPattern> statementPatterns,
			Set<SameTerm> sameTermConstraints) {
		if (sameTermConstraints != null) {
			for (SameTerm st : sameTermConstraints) {
```

### BoundedWildcard
Can generalize to `? extends PropertySetElem`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	}

	private TupleExpr createTupleExprForNegatedPropertySets(List<PropertySetElem> nps,
			PathSequenceContext pathSequenceContext) {
		Var subjVar = pathSequenceContext.startVar;
```

### BoundedWildcard
Can generalize to `? extends StatementPattern`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	 * and object variables from the supplied statement patterns, but ignores any context variables.
	 */
	private Set<Var> getConstructVars(Collection<StatementPattern> statementPatterns) {
		Set<Var> vars = new LinkedHashSet<>(statementPatterns.size() * 2);

```

### BoundedWildcard
Can generalize to `? super String`
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalTripleCallback.java`
#### Snippet
```java

	public JSONLDInternalTripleCallback(RDFHandler nextHandler, ValueFactory vf, ParserConfig parserConfig,
			ParseErrorListener parseErrorListener, Function<String, Resource> namedBNodeCreator,
			Supplier<Resource> anonymousBNodeCreator) {
		this.handler = nextHandler;
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalTripleCallback.java`
#### Snippet
```java

	public JSONLDInternalTripleCallback(RDFHandler nextHandler, ValueFactory vf, ParserConfig parserConfig,
			ParseErrorListener parseErrorListener, Function<String, Resource> namedBNodeCreator,
			Supplier<Resource> anonymousBNodeCreator) {
		this.handler = nextHandler;
```

### BoundedWildcard
Can generalize to `? extends Resource`
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalTripleCallback.java`
#### Snippet
```java
	public JSONLDInternalTripleCallback(RDFHandler nextHandler, ValueFactory vf, ParserConfig parserConfig,
			ParseErrorListener parseErrorListener, Function<String, Resource> namedBNodeCreator,
			Supplier<Resource> anonymousBNodeCreator) {
		this.handler = nextHandler;
		this.vf = vf;
```

### BoundedWildcard
Can generalize to `? super String`
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDWriter.java`
#### Snippet
```java
	 * @param namespaces set of RDF name spaces
	 */
	private static void addPrefixes(Map<String, Object> ctx, Set<Namespace> namespaces) {
		for (final Namespace ns : namespaces) {
			ctx.put(ns.getPrefix().isEmpty() ? JsonLdConsts.VOCAB : ns.getPrefix(), ns.getName());
```

### BoundedWildcard
Can generalize to `? extends Namespace`
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDWriter.java`
#### Snippet
```java
	 * @param namespaces set of RDF name spaces
	 */
	private static void addPrefixes(Map<String, Object> ctx, Set<Namespace> namespaces) {
		for (final Namespace ns : namespaces) {
			ctx.put(ns.getPrefix().isEmpty() ? JsonLdConsts.VOCAB : ns.getPrefix(), ns.getName());
```

### BoundedWildcard
Can generalize to `? super Query`
in `spring-components/spring-boot-sparql-web/src/main/java/org/eclipse/rdf4j/http/server/readonly/sparql/QueryTypes.java`
#### Snippet
```java
	private final Predicate<Query> typeChecker;

	QueryTypes(Predicate<Query> typeChecker, FileFormat... formats) {
		this.typeChecker = typeChecker;
		this.formats = formats;
```

### BoundedWildcard
Can generalize to `? extends SailException`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
		Statement last;

		StatementIterator(CloseableIteration<? extends Statement, SailException> closeableIteration) {
			this.stmts = closeableIteration;
		}
```

### BoundedWildcard
Can generalize to `? super NodeListener`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/Node.java`
#### Snippet
```java
	}

	private void notifySafeListeners(Function<NodeListener, Boolean> notifier) {
		Iterator<NodeListener> iter = listeners.iterator();

```

### BoundedWildcard
Can generalize to `? super Integer`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/ConcurrentNodeCache.java`
#### Snippet
```java
	};

	public ConcurrentNodeCache(Function<Integer, Node> reader) {
		super(0); // cleanUp, when actually run, will try to completely purge the cache (but retain currently used
		// nodes)
```

### BoundedWildcard
Can generalize to `? extends Node`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/ConcurrentNodeCache.java`
#### Snippet
```java
	};

	public ConcurrentNodeCache(Function<Integer, Node> reader) {
		super(0); // cleanUp, when actually run, will try to completely purge the cache (but retain currently used
		// nodes)
```

### BoundedWildcard
Can generalize to `? extends PathSegment`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
	}

	private long getValueCountEstimate(List<PathSegment> minValuePath, List<PathSegment> maxValuePath)
			throws IOException {
		int commonListSize = Math.min(minValuePath.size(), maxValuePath.size());
```

### BoundedWildcard
Can generalize to `? extends PathSegment`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
	}

	private long getValueCountEstimate(List<PathSegment> minValuePath, List<PathSegment> maxValuePath)
			throws IOException {
		int commonListSize = Math.min(minValuePath.size(), maxValuePath.size());
```

### BoundedWildcard
Can generalize to `? super Statement`
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/rio/util/BlackHoleRDFHandler.java`
#### Snippet
```java

	// to avoid JVM "optimizing" the execution of unused code
	public void setBlackHoleConsumer(Consumer<Statement> blackHoleConsumer) {
		this.blackHoleConsumer = blackHoleConsumer;
	}
```

### BoundedWildcard
Can generalize to `? super CloseableIteration`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/QueryEvaluationStep.java`
#### Snippet
```java
	 */
	static QueryEvaluationStep wrap(QueryEvaluationStep qes,
			Function<CloseableIteration<BindingSet, QueryEvaluationException>, CloseableIteration<BindingSet, QueryEvaluationException>> wrap) {
		return bs -> wrap.apply(qes.evaluate(bs));
	}
```

### BoundedWildcard
Can generalize to `? extends CloseableIteration`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/QueryEvaluationStep.java`
#### Snippet
```java
	 */
	static QueryEvaluationStep wrap(QueryEvaluationStep qes,
			Function<CloseableIteration<BindingSet, QueryEvaluationException>, CloseableIteration<BindingSet, QueryEvaluationException>> wrap) {
		return bs -> wrap.apply(qes.evaluate(bs));
	}
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/TupleFunctionEvaluationStrategy.java`
#### Snippet
```java

	public static CloseableIteration<BindingSet, QueryEvaluationException> evaluate(TupleFunction func,
			final List<Var> resultVars, final BindingSet bindings, ValueFactory valueFactory, Value... argValues)
			throws QueryEvaluationException {
		return new LookAheadIteration<>() {
```

### BoundedWildcard
Can generalize to `? super ValueExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ConjunctiveConstraintSplitter.java`
#### Snippet
```java
		}

		protected void getConjunctiveConstraints(ValueExpr valueExpr, List<ValueExpr> conjunctiveConstraints) {
			if (valueExpr instanceof And) {
				And and = (And) valueExpr;
```

### BoundedWildcard
Can generalize to `? super ArrayBindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		private final Function<ArrayBindingSet, Boolean> directHasVariable;

		public HasBinding(String variableName, Function<ArrayBindingSet, Boolean> directHasVariable) {
			this.variableName = variableName;
			this.directHasVariable = directHasVariable;
```

### BoundedWildcard
Can generalize to `? super ArrayBindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		private final Function<ArrayBindingSet, Value> directAccessForVariable;

		public ValueGetter(String variableName, Function<ArrayBindingSet, Value> directAccessForVariable) {

			this.variableName = variableName;
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		private final Function<ArrayBindingSet, Value> directAccessForVariable;

		public ValueGetter(String variableName, Function<ArrayBindingSet, Value> directAccessForVariable) {

			this.variableName = variableName;
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/EvaluationStatistics.java`
#### Snippet
```java
		}

		protected int countConstantVars(Iterable<Var> vars) {
			int constantVarCount = 0;

```

### BoundedWildcard
Can generalize to `? extends QueryOptimizer`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryOptimizerList.java`
#### Snippet
```java
	}

	public QueryOptimizerList(List<QueryOptimizer> optimizers) {
		this.optimizers = new ArrayList<>(optimizers);
	}
```

### BoundedWildcard
Can generalize to `? super MutableBindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		private ConvertStatementToBindingSetIterator(
				CloseableIteration<? extends Statement, ? extends QueryEvaluationException> iteration,
				BiConsumer<MutableBindingSet, Statement> action, QueryEvaluationContext context) {
			assert iteration != null;
			this.iteration = iteration;
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		private ConvertStatementToBindingSetIterator(
				CloseableIteration<? extends Statement, ? extends QueryEvaluationException> iteration,
				BiConsumer<MutableBindingSet, Statement> action, QueryEvaluationContext context) {
			assert iteration != null;
			this.iteration = iteration;
```

### BoundedWildcard
Can generalize to `? extends IRI`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
	}

	private static Resource[] fillContextsFromDatasSetGraphs(Set<IRI> graphs) {
		Resource[] contexts = new Resource[graphs.size()];
		int i = 0;
```

### BoundedWildcard
Can generalize to `? super MutableBindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		private JoinStatementWithBindingSetIterator(
				CloseableIteration<? extends Statement, ? extends QueryEvaluationException> iteration,
				BiConsumer<MutableBindingSet, Statement> action, BindingSet bindings, QueryEvaluationContext context) {
			assert iteration != null;
			this.iteration = iteration;
```

### BoundedWildcard
Can generalize to `? super Statement`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		private JoinStatementWithBindingSetIterator(
				CloseableIteration<? extends Statement, ? extends QueryEvaluationException> iteration,
				BiConsumer<MutableBindingSet, Statement> action, BindingSet bindings, QueryEvaluationContext context) {
			assert iteration != null;
			this.iteration = iteration;
```

### BoundedWildcard
Can generalize to `? extends List`<var></var>
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java

		protected double getTupleExprCost(TupleExpr tupleExpr, Map<TupleExpr, Double> cardinalityMap,
				Map<TupleExpr, List<Var>> varsMap, Map<Var, Integer> varFreqMap, Set<String> boundVars) {

			double cost = cardinalityMap.get(tupleExpr);
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected List<TupleExpr> getExtensions(List<TupleExpr> expressions) {
			List<TupleExpr> extensions = new ArrayList<>();
			for (TupleExpr expr : expressions) {
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		private TupleExpr getNextSubselect(List<TupleExpr> currentList, List<TupleExpr> joinArgs) {

			// determine union of names of all elements currently in the list: this
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		private TupleExpr getNextSubselect(List<TupleExpr> currentList, List<TupleExpr> joinArgs) {

			// determine union of names of all elements currently in the list: this
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected List<Var> getConstantVars(Iterable<Var> vars) {
			List<Var> constantVars = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected <M extends Map<Var, Integer>> M getVarFreqMap(List<Var> varList, M varFreqMap) {
			for (Var var : varList) {
				Integer freq = varFreqMap.get(var);
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
		 * bound in other tuple expressions over variables with a fixed value.
		 */
		protected TupleExpr selectNextTupleExpr(List<TupleExpr> expressions, Map<TupleExpr, Double> cardinalityMap,
				Map<TupleExpr, List<Var>> varsMap, Map<Var, Integer> varFreqMap, Set<String> boundVars) {
			TupleExpr result = null;
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected List<Var> getUnboundVars(Iterable<Var> vars) {
			List<Var> unboundVars = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected List<TupleExpr> getSubSelects(List<TupleExpr> expressions) {
			List<TupleExpr> subselects = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/MultiProjectionIterator.java`
#### Snippet
```java

	public MultiProjectionIterator(MultiProjection multiProjection,
			CloseableIteration<BindingSet, QueryEvaluationException> iter, BindingSet bindings) {
		this.projections = multiProjection.getProjections();
		this.iter = iter;
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/MultiProjectionIterator.java`
#### Snippet
```java

	public MultiProjectionIterator(MultiProjection multiProjection,
			CloseableIteration<BindingSet, QueryEvaluationException> iter, BindingSet bindings) {
		this.projections = multiProjection.getProjections();
		this.iter = iter;
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/aggregate/StatisticalAggregateFunction.java`
#### Snippet
```java

	@Override
	public void processAggregate(BindingSet bindingSet, Predicate<Value> distinctValue, StatisticCollector collector)
			throws QueryEvaluationException {
		if (collector.hasError()) {
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ExtensionIterator.java`
#### Snippet
```java
	}

	private static void setValue(BiConsumer<Value, MutableBindingSet> setBinding, QueryValueEvaluationStep prepared,
			MutableBindingSet targetBindings) {
		try {
```

### BoundedWildcard
Can generalize to `? super MutableBindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ExtensionIterator.java`
#### Snippet
```java
	}

	private static void setValue(BiConsumer<Value, MutableBindingSet> setBinding, QueryValueEvaluationStep prepared,
			MutableBindingSet targetBindings) {
		try {
```

### BoundedWildcard
Can generalize to `? super MutableBindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ExtensionIterator.java`
#### Snippet
```java

	public ExtensionIterator(CloseableIteration<BindingSet, QueryEvaluationException> iter,
			Consumer<MutableBindingSet> setter, QueryEvaluationContext context) throws QueryEvaluationException {
		super(iter);
		this.setter = setter;
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	 */
	protected QueryValueEvaluationStep supplyUnaryValueEvaluation(UnaryValueOperator node,
			java.util.function.Function<Value, Value> operation, QueryEvaluationContext context) {
		QueryValueEvaluationStep argStep = precompile(node.getArg(), context);
		if (argStep.isConstant()) {
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	 */
	protected QueryValueEvaluationStep supplyUnaryValueEvaluation(UnaryValueOperator node,
			java.util.function.Function<Value, Value> operation, QueryEvaluationContext context) {
		QueryValueEvaluationStep argStep = precompile(node.getArg(), context);
		if (argStep.isConstant()) {
```

### BoundedWildcard
Can generalize to `? extends ValueExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	 */
	private boolean determineIfFunctionCallWillBeAConstant(QueryEvaluationContext context, Function function,
			List<ValueExpr> args, QueryValueEvaluationStep[] argSteps) {
		boolean allConstant = true;
		if (function.mustReturnDifferentResult()) {
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	 */
	protected QueryValueEvaluationStep supplyBinaryValueEvaluation(BinaryValueOperator node,
			BiFunction<Value, Value, Value> operation, QueryEvaluationContext context) {
		QueryValueEvaluationStep leftStep = precompile(node.getLeftArg(), context);
		QueryValueEvaluationStep rightStep = precompile(node.getRightArg(), context);
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	 */
	protected QueryValueEvaluationStep supplyBinaryValueEvaluation(BinaryValueOperator node,
			BiFunction<Value, Value, Value> operation, QueryEvaluationContext context) {
		QueryValueEvaluationStep leftStep = precompile(node.getLeftArg(), context);
		QueryValueEvaluationStep rightStep = precompile(node.getRightArg(), context);
```

### BoundedWildcard
Can generalize to `? extends Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	 */
	protected QueryValueEvaluationStep supplyBinaryValueEvaluation(BinaryValueOperator node,
			BiFunction<Value, Value, Value> operation, QueryEvaluationContext context) {
		QueryValueEvaluationStep leftStep = precompile(node.getLeftArg(), context);
		QueryValueEvaluationStep rightStep = precompile(node.getRightArg(), context);
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		Stopwatch stopwatch = Stopwatch.createUnstarted();

		public TimedIterator(CloseableIteration<BindingSet, QueryEvaluationException> iterator,
				QueryModelNode queryModelNode) {
			super(iterator);
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		Stopwatch stopwatch = Stopwatch.createUnstarted();

		public TimedIterator(CloseableIteration<BindingSet, QueryEvaluationException> iterator,
				QueryModelNode queryModelNode) {
			super(iterator);
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		QueryModelNode queryModelNode;

		public ResultSizeCountingIterator(CloseableIteration<BindingSet, QueryEvaluationException> iterator,
				QueryModelNode queryModelNode) {
			super(iterator);
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		QueryModelNode queryModelNode;

		public ResultSizeCountingIterator(CloseableIteration<BindingSet, QueryEvaluationException> iterator,
				QueryModelNode queryModelNode) {
			super(iterator);
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/QueryContextIteration.java`
#### Snippet
```java
	private final QueryContext queryContext;

	public QueryContextIteration(CloseableIteration<? extends BindingSet, QueryEvaluationException> iter,
			QueryContext queryContext) {
		this.iter = iter;
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	}

	protected Set<BindingSet> makeSet(Iteration<BindingSet, X> rightArg) throws X {
		return Iterations.asSet(rightArg);
	}
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
	}

	protected Set<BindingSet> makeSet(Iteration<BindingSet, X> rightArg) throws X {
		return Iterations.asSet(rightArg);
	}
```

### BoundedWildcard
Can generalize to `? super ValuePair`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
	 * @param vp
	 */
	protected boolean add(Set<ValuePair> valueSet, ValuePair vp) throws QueryEvaluationException {
		return valueSet.add(vp);
	}
```

### BoundedWildcard
Can generalize to `? super ValuePair`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
	 * @param vp
	 */
	protected boolean addToQueue(Queue<ValuePair> valueQueue2, ValuePair vp) throws QueryEvaluationException {
		return valueQueue2.add(vp);
	}
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		@Override
		public void processAggregate(BindingSet s, Predicate<Value> distinctValue, AvgCollector avg)
				throws QueryEvaluationException {
			if (avg.hasError()) {
```

### BoundedWildcard
Can generalize to `? extends AggregatePredicateCollectorSupplier`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
		}

		public void addSolution(BindingSet bs, List<AggregatePredicateCollectorSupplier<?, ?>> operators) {
			for (int i = 0; i < operators.size(); i++) {
				AggregatePredicateCollectorSupplier<?, ?> aggregatePredicateCollectorSupplier = operators.get(i);
```

### BoundedWildcard
Can generalize to `? extends AggregatePredicateCollectorSupplier`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

	private BiConsumer<Entry, MutableBindingSet> makeBindSolution(
			List<AggregatePredicateCollectorSupplier<?, ?>> aggregates) {
		BiConsumer<Entry, MutableBindingSet> bindSolution = null;
		for (int i = 0; i < aggregates.size(); i++) {
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		@Override
		public void processAggregate(BindingSet s, Predicate<Value> distinctValue, CountCollector agv)
				throws QueryEvaluationException {
			Value value = evaluate(s);
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		@Override
		public void processAggregate(BindingSet s, Predicate<Value> distinctValue, ValueCollector min)
				throws QueryEvaluationException {
			Value v = evaluate(s);
```

### BoundedWildcard
Can generalize to `? super T`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
		private final Supplier<T> supplier;

		public AggregatePredicateCollectorSupplier(AggregateFunction<T, D> agg, LongFunction<Predicate<D>> predicate,
				Supplier<T> supplier, String name) {
			super();
```

### BoundedWildcard
Can generalize to `? extends Predicate`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		public Entry(BindingSet prototype, List<AggregateCollector> collectors,
				List<Predicate<?>> predicates)
				throws QueryEvaluationException {
			this.prototype = prototype;
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		@Override
		public void processAggregate(BindingSet s, Predicate<Value> distinctValue, IntegerCollector sum)
				throws QueryEvaluationException {
			if (sum.hasError()) {
```

### BoundedWildcard
Can generalize to `? super Key`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

	private void emptySolutionSpecialCase(List<AggregatePredicateCollectorSupplier<?, ?>> aggregates,
			Map<Key, Entry> entries) {
		// no solutions, but if we are not explicitly grouping and aggregates are present,
		// we still need to process them to produce a zero-result.
```

### BoundedWildcard
Can generalize to `? super Entry`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

	private void emptySolutionSpecialCase(List<AggregatePredicateCollectorSupplier<?, ?>> aggregates,
			Map<Key, Entry> entries) {
		// no solutions, but if we are not explicitly grouping and aggregates are present,
		// we still need to process them to produce a zero-result.
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		@Override
		public void processAggregate(BindingSet s, Predicate<Value> distinctValue, StringBuilderCollector collector)
				throws QueryEvaluationException {
			Value v = evaluate(s);
```

### BoundedWildcard
Can generalize to `? super Value`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		@Override
		public void processAggregate(BindingSet s, Predicate<Value> distinctValue, ValueCollector max)
				throws QueryEvaluationException {
			Value v = evaluate(s);
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java

		@Override
		public void processAggregate(BindingSet s, Predicate<BindingSet> distinctValue, CountCollector agv)
				throws QueryEvaluationException {
			// wildcard count
```

### BoundedWildcard
Can generalize to `? extends AggregatePredicateCollectorSupplier`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
	}

	private List<AggregateCollector> makeCollectors(List<AggregatePredicateCollectorSupplier<?, ?>> aggregates) {
		List<AggregateCollector> collectors = new ArrayList<>(aggregates.size());
		for (AggregatePredicateCollectorSupplier<?, ?> a : aggregates) {
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
	 * @return list
	 */
	protected List<BindingSet> makeList(List<BindingSet> key) {
		return new ArrayList<>(key);
	}
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
	}

	protected void add(List<BindingSet> leftArgResults, BindingSet b) throws QueryEvaluationException {
		leftArgResults.add(b);
	}
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
	 * @return the removed BindingSet
	 */
	protected BindingSet removeFirstElement(List<BindingSet> list) throws QueryEvaluationException {
		return list.remove(0);
	}
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
	}

	protected void addAll(List<BindingSet> hashTableValues, List<BindingSet> values) throws QueryEvaluationException {
		hashTableValues.addAll(values);
	}
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
	}

	protected void addAll(List<BindingSet> hashTableValues, List<BindingSet> values) throws QueryEvaluationException {
		hashTableValues.addAll(values);
	}
```

### BoundedWildcard
Can generalize to `? super BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
	}

	protected void put(Map<BindingSet, List<BindingSet>> hashTable, BindingSet hashKey, List<BindingSet> hashValue)
			throws QueryEvaluationException {
		hashTable.put(hashKey, hashValue);
```

### BoundedWildcard
Can generalize to `? super List`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/BottomUpJoinIterator.java`
#### Snippet
```java
	}

	protected void put(Map<BindingSet, List<BindingSet>> hashTable, BindingSet hashKey, List<BindingSet> hashValue)
			throws QueryEvaluationException {
		hashTable.put(hashKey, hashValue);
```

### BoundedWildcard
Can generalize to `? super ValueExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/ConjunctiveConstraintSplitterOptimizer.java`
#### Snippet
```java
		}

		protected void getConjunctiveConstraints(ValueExpr valueExpr, List<ValueExpr> conjunctiveConstraints) {
			if (valueExpr instanceof And) {
				And and = (And) valueExpr;
```

### BoundedWildcard
Can generalize to `? extends Map`>
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
			CloseableIteration<BindingSet, QueryEvaluationException> leftIter, Set<String> leftBindingNames,
			CloseableIteration<BindingSet, QueryEvaluationException> rightIter, Set<String> rightBindingNames,
			boolean leftJoin, IntFunction<Map<BindingSetHashKey, List<BindingSet>>> mapMaker,
			IntFunction<List<BindingSet>> mapValueMaker
	) throws QueryEvaluationException {
```

### BoundedWildcard
Can generalize to `? extends List`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
			CloseableIteration<BindingSet, QueryEvaluationException> rightIter, Set<String> rightBindingNames,
			boolean leftJoin, IntFunction<Map<BindingSetHashKey, List<BindingSet>>> mapMaker,
			IntFunction<List<BindingSet>> mapValueMaker
	) throws QueryEvaluationException {
		this.leftIter = leftIter;
```

### BoundedWildcard
Can generalize to `? super E`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
	}

	protected <E> void addAll(Collection<E> col, List<E> values) throws QueryEvaluationException {
		col.addAll(values);
	}
```

### BoundedWildcard
Can generalize to `? extends E`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
	}

	protected <E> void addAll(Collection<E> col, List<E> values) throws QueryEvaluationException {
		col.addAll(values);
	}
```

### BoundedWildcard
Can generalize to `? super BindingSetHashKey`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
	}

	protected void putHashTableEntry(Map<BindingSetHashKey, List<BindingSet>> nextHashTable, BindingSetHashKey hashKey,
			List<BindingSet> hashValue, boolean newEntry) throws QueryEvaluationException {
		// by default, we use a standard memory hash map
```

### BoundedWildcard
Can generalize to `? super List`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
	}

	protected void putHashTableEntry(Map<BindingSetHashKey, List<BindingSet>> nextHashTable, BindingSetHashKey hashKey,
			List<BindingSet> hashValue, boolean newEntry) throws QueryEvaluationException {
		// by default, we use a standard memory hash map
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
	@Override
	public final CloseableIteration<BindingSet, QueryEvaluationException> evaluate(Service service,
			CloseableIteration<BindingSet, QueryEvaluationException> bindings, String baseUri)
			throws QueryEvaluationException {
		if (!bindings.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/TupleFunctionFederatedService.java`
#### Snippet
```java
	@Override
	public final CloseableIteration<BindingSet, QueryEvaluationException> evaluate(Service service,
			CloseableIteration<BindingSet, QueryEvaluationException> bindings, String baseUri)
			throws QueryEvaluationException {
		if (!bindings.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
	private final Iteration<BindingSet, QueryEvaluationException> sourceIter;

	public DescribeIteration(Iteration<BindingSet, QueryEvaluationException> sourceIter, EvaluationStrategy strategy,
			Set<String> describeExprNames, BindingSet parentBindings) {
		this.strategy = strategy;
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
	private final Iteration<BindingSet, QueryEvaluationException> sourceIter;

	public DescribeIteration(Iteration<BindingSet, QueryEvaluationException> sourceIter, EvaluationStrategy strategy,
			Set<String> describeExprNames, BindingSet parentBindings) {
		this.strategy = strategy;
```

### BoundedWildcard
Can generalize to `? extends Iterator`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
		private E next;

		public SortedIterators(Comparator<E> comparator, boolean distinct, List<Iterator<E>> iterators) {
			this.iterators = iterators;
			this.distinct = distinct;
```

### BoundedWildcard
Can generalize to `? extends BindingSet`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
	}

	public OrderIterator(CloseableIteration<BindingSet, QueryEvaluationException> iter,
			Comparator<BindingSet> comparator, long limit, boolean distinct, long iterationSyncThreshold) {
		this.iter = iter;
```

### BoundedWildcard
Can generalize to `? extends QueryEvaluationException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
	}

	public OrderIterator(CloseableIteration<BindingSet, QueryEvaluationException> iter,
			Comparator<BindingSet> comparator, long limit, boolean distinct, long iterationSyncThreshold) {
		this.iter = iter;
```

### BoundedWildcard
Can generalize to `? extends SearchQueryEvaluator`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
	 * @throws SailException
	 */
	private void evaluateLuceneQueries(Collection<SearchQueryEvaluator> queries) throws SailException {
		// TODO: optimize lucene queries here
		// - if they refer to the same subject, merge them into one lucene query
```

### BoundedWildcard
Can generalize to `? super SearchQueryEvaluator`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/GeoRelationQuerySpecBuilder.java`
#### Snippet
```java

	@Override
	public void process(TupleExpr tupleExpr, BindingSet bindings, final Collection<SearchQueryEvaluator> results)
			throws SailException {

```

### BoundedWildcard
Can generalize to `? super SearchQueryEvaluator`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/DistanceQuerySpecBuilder.java`
#### Snippet
```java

	@Override
	public void process(TupleExpr tupleExpr, BindingSet bindings, final Collection<SearchQueryEvaluator> results)
			throws SailException {

```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		private TupleExpr getNextSubselect(List<TupleExpr> currentList, List<TupleExpr> joinArgs) {

			// determine union of names of all elements currently in the list: this
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		private TupleExpr getNextSubselect(List<TupleExpr> currentList, List<TupleExpr> joinArgs) {

			// determine union of names of all elements currently in the list: this
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected int getForeignVarFreq(List<Var> ownUnboundVars, Map<Var, Integer> varFreqMap) {
			if (ownUnboundVars.isEmpty()) {
				return 0;
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected List<Var> getUnboundVars(List<Var> vars) {
			int size = vars.size();
			if (size == 0) {
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java

		@Deprecated(forRemoval = true, since = "4.1.0")
		protected List<Var> getUnboundVars(Iterable<Var> vars) {

			List<Var> ret = null;
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		private List<TupleExpr> getExtensionTupleExprs(List<TupleExpr> expressions) {
			if (expressions.isEmpty()) {
				return List.of();
```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		private int countConstantVars(List<Var> vars) {
			int size = 0;

```

### BoundedWildcard
Can generalize to `? extends Var`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected <M extends Map<Var, Integer>> void fillVarFreqMap(List<Var> varList, M varFreqMap) {
			if (varList.isEmpty()) {
				return;
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		 * bound in other tuple expressions over variables with a fixed value.
		 */
		protected TupleExpr selectNextTupleExpr(List<TupleExpr> expressions, Map<TupleExpr, Double> cardinalityMap,
				Map<TupleExpr, List<Var>> varsMap, Map<Var, Integer> varFreqMap) {
			if (expressions.size() == 1) {
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected List<Extension> getExtensions(List<TupleExpr> expressions) {
			List<Extension> extensions = new ArrayList<>();
			for (TupleExpr expr : expressions) {
```

### BoundedWildcard
Can generalize to `? extends List`<var></var>
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java

		protected double getTupleExprCost(TupleExpr tupleExpr, Map<TupleExpr, Double> cardinalityMap,
				Map<TupleExpr, List<Var>> varsMap, Map<Var, Integer> varFreqMap) {

			// BindingSetAssignment has a typical constant cost. This cost is not based on statistics so is much more
```

### BoundedWildcard
Can generalize to `? extends TupleExpr`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
		}

		protected List<TupleExpr> getSubSelects(List<TupleExpr> expressions) {
			if (expressions.isEmpty()) {
				return List.of();
```

### BoundedWildcard
Can generalize to `? extends StatementPattern`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/QuerySpecBuilder.java`
#### Snippet
```java
	 * the patter from the arraylist, to be able to check if some patterns are added without a MATCHES property.
	 */
	private StatementPattern getPattern(Var subjectVar, ArrayList<StatementPattern> patterns)
			throws IllegalArgumentException {
		StatementPattern result = null;
```

### BoundedWildcard
Can generalize to `? super SearchQueryEvaluator`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/QuerySpecBuilder.java`
#### Snippet
```java
	 */
	@Override
	public void process(TupleExpr tupleExpr, BindingSet bindings, Collection<SearchQueryEvaluator> result)
			throws SailException {
		// find Lucene-related StatementPatterns
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
	 */
	@Override
	public final synchronized void addRemoveStatements(Collection<Statement> added, Collection<Statement> removed)
			throws IOException {
		// Buffer per resource
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
	 */
	@Override
	public final synchronized void addRemoveStatements(Collection<Statement> added, Collection<Statement> removed)
			throws IOException {
		// Buffer per resource
```

### BoundedWildcard
Can generalize to `? extends Statement`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
	 */
	@Override
	public final synchronized void addDocuments(Resource subject, List<Statement> statements) throws IOException {

		String resourceId = SearchFields.getResourceID(subject);
```

### BoundedWildcard
Can generalize to `? extends Set`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
	 */
	private boolean copyDocument(SearchDocument newDocument, SearchDocument document,
			Map<String, Set<String>> removedProperties) {
		// track if newDocument is actually different from document
		boolean mutated = false;
```

### BoundedWildcard
Can generalize to `? super String`
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchDocument.java`
#### Snippet
```java
	}

	private static void addField(String name, String value, Map<String, Object> document) {
		Object oldValue = document.get(name);
		Object newValue;
```

### BoundedWildcard
Can generalize to `? extends Artist`
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
	}

	private void listPaintingsByArtist(Map<Artist, Set<Painting>> paintingsMap) {
		for (Artist a : paintingsMap.keySet()) {
			System.out.println(String.format("%s %s", a.getFirstName(), a.getLastName()));
```

### BoundedWildcard
Can generalize to `? extends Set`
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
	}

	private void listPaintingsByArtist(Map<Artist, Set<Painting>> paintingsMap) {
		for (Artist a : paintingsMap.keySet()) {
			System.out.println(String.format("%s %s", a.getFirstName(), a.getLastName()));
```

### BoundedWildcard
Can generalize to `? extends ValueExpr`
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
	}

	private void writeAsFunction(String name, List<ValueExpr> args) {
		builder.append(name);
		builder.append("(");
```

### BoundedWildcard
Can generalize to `? super EmptyPropertyPathBuilder`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/TriplePattern.java`
#### Snippet
```java
	 * @return this triple pattern
	 */
	default TriplePattern andHas(Consumer<EmptyPropertyPathBuilder> propertyPathConfigurer, RdfObject... objects) {
		EmptyPropertyPathBuilder pathBuilder = new EmptyPropertyPathBuilder();
		propertyPathConfigurer.accept(pathBuilder);
```

### BoundedWildcard
Can generalize to `? super EmptyPropertyPathBuilder`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/rdf/RdfSubject.java`
#### Snippet
```java
	 * @return a new {@link TriplePattern} with this subject, and the given predicate path and object(s)
	 */
	default TriplePattern has(Consumer<EmptyPropertyPathBuilder> propertyPathConfigurer, RdfObject... objects) {
		EmptyPropertyPathBuilder pathBuilder = new EmptyPropertyPathBuilder();
		propertyPathConfigurer.accept(pathBuilder);
```

## RuleId[id=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `SPARQLOperation` has no concrete subclass
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
 */
@Deprecated
public abstract class SPARQLOperation implements Operation {

	private static final Executor executor = Executors.newCachedThreadPool();
```

### AbstractClassNeverImplemented
Abstract class `Protocol` has no concrete subclass
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
import org.eclipse.rdf4j.rio.helpers.NTriplesUtil;

public abstract class Protocol {

	/**
```

### AbstractClassNeverImplemented
Abstract class `AggregateOperatorBase` has no concrete subclass
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/AggregateOperatorBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class AggregateOperatorBase extends AbstractAggregateOperator {

	private static final long serialVersionUID = 1L;
```

### AbstractClassNeverImplemented
Abstract class `SPARQL11SyntaxTest` has no concrete subclass
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxTest.java`
#### Snippet
```java
 */
@Deprecated
public abstract class SPARQL11SyntaxTest extends TestCase {

	/*-----------*
```

### AbstractClassNeverImplemented
Abstract class `SPARQLUpdateConformanceTest` has no concrete subclass
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
 */
@Deprecated
public abstract class SPARQLUpdateConformanceTest extends TestCase {

	/*-----------*
```

### AbstractClassNeverImplemented
Abstract class `AbstractModelTest` has no concrete subclass
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelTest.java`
#### Snippet
```java
 */
@Deprecated
public abstract class AbstractModelTest extends ModelTest {

}
```

### AbstractClassNeverImplemented
Abstract class `ComplexSPARQLQueryTest` has no concrete subclass
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
 */
@Deprecated(since = "4.0.2", forRemoval = true)
public abstract class ComplexSPARQLQueryTest {

	@BeforeAll
```

### AbstractClassNeverImplemented
Abstract class `SPARQLJSONParserBase` has no concrete subclass
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/SPARQLJSONParserBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class SPARQLJSONParserBase extends AbstractSPARQLJSONParser {

}
```

### AbstractClassNeverImplemented
Abstract class `PropertyPaths` has no concrete subclass
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/propertypath/builder/PropertyPaths.java`
#### Snippet
```java
import org.eclipse.rdf4j.sparqlbuilder.rdf.Iri;

abstract class PropertyPaths {
	public static SequencePath pSeq(PropertyPath left, PropertyPath right) {
		return new SequencePath(left, right);
```

### AbstractClassNeverImplemented
Abstract class `FilterIterator` has no concrete subclass
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/FilterIterator.java`
#### Snippet
```java
 * implement the <var>accept</var> method to indicate which objects should be returned.
 */
public abstract class FilterIterator<E> implements Iterator<E> {

	private final Iterator<? extends E> filteredIter;
```

### AbstractClassNeverImplemented
Abstract class `NotifyingSailConnectionBase` has no concrete subclass
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/NotifyingSailConnectionBase.java`
#### Snippet
```java
 */
@Deprecated(forRemoval = true)
public abstract class NotifyingSailConnectionBase extends AbstractNotifyingSailConnection {

	public NotifyingSailConnectionBase(AbstractSail sailBase) {
```

### AbstractClassNeverImplemented
Abstract class `ASTVisitorBase` has no concrete subclass
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ASTVisitorBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class ASTVisitorBase extends AbstractASTVisitor {

}
```

### AbstractClassNeverImplemented
Abstract class `AbstractQueryPreparer` has no concrete subclass
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/AbstractQueryPreparer.java`
#### Snippet
```java
import org.eclipse.rdf4j.rio.RDFHandlerException;

public abstract class AbstractQueryPreparer implements QueryPreparer {

	private final TripleSource tripleSource;
```

### AbstractClassNeverImplemented
Abstract class `FederatedServiceResolverBase` has no concrete subclass
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/FederatedServiceResolverBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class FederatedServiceResolverBase extends AbstractFederatedServiceResolver {

}
```

### AbstractClassNeverImplemented
Abstract class `QUERY_KEYS` has no concrete subclass
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/dao/ArtistDao.java`
#### Snippet
```java
	}

	static abstract class QUERY_KEYS {
		public static final String ARTISTS_WITHOUT_PAINTINGS = "artists-without-paintings";
	}
```

### AbstractClassNeverImplemented
Abstract class `SPARQLXMLParserBase` has no concrete subclass
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/SPARQLXMLParserBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class SPARQLXMLParserBase extends AbstractSPARQLXMLParser {

}
```

## RuleId[id=MissortedModifiers]
### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/config/BaseSailSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/base#evaluationStrategyFactory</var>
	 */
	public final static IRI EVALUATION_STRATEGY_FACTORY;

	static {
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
		final private Resource subject;
		final private IRI predicate;
		final private Value object;
		final private Resource context;

```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
		final private IRI predicate;
		final private Value object;
		final private Resource context;

		// true if the context is the union of all contexts
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
	public static class SimpleStatementPattern {
		final private Resource subject;
		final private IRI predicate;
		final private Value object;
		final private Resource context;
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java

		// true if the context is the union of all contexts
		final private boolean allContexts;

		public SimpleStatementPattern(Resource subject, IRI predicate, Value object, Resource context,
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java

	public static class SimpleStatementPattern {
		final private Resource subject;
		final private IRI predicate;
		final private Value object;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
	private static final String ROW_IDX_VAR = "__rowIdx";

	final static Logger logger = LoggerFactory.getLogger(RepositoryFederatedService.class);

	/**
```

### MissortedModifiers
Missorted modifiers `static public`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/paths/Path.java`
#### Snippet
```java
	}

	static public Path buildPath(ShapeSource shapeSource, Resource id) {
		if (id == null) {
			return null;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/SparqlFragment.java`
#### Snippet
```java
public class SparqlFragment {

	final static Pattern REGEX_INDENT = Pattern.compile("(?m)^");

	private final String fragment;
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java

	@Override
	synchronized public void close() throws SailException {
		if (closed) {
			return;
```

### MissortedModifiers
Missorted modifiers `transient private`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
		private Boolean parallelValidation;
		private IsolationLevel isolationLevel;
		transient private Settings previous = null;

		@Deprecated(since = "4.0.0", forRemoval = true)
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

abstract public class Shape implements ConstraintComponent, Identifiable {

	private static final Logger logger = LoggerFactory.getLogger(Shape.class);
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PlanNodeHelper.java`
#### Snippet
```java
public class PlanNodeHelper {

	private final static SimpleDataset rdf4jNilDataset;

	static {
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TargetChainPopper.java`
#### Snippet
```java
		return new LoggingCloseableIteration(this, validationExecutionLogger) {

			final private CloseableIteration<? extends ValidationTuple, SailException> parentIterator = parent
					.iterator();
			Iterator<ValidationTuple> iterator = Collections.emptyIterator();
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
public abstract class FilterPlanNode implements MultiStreamPlanNode, PlanNode {

	static private final Logger logger = LoggerFactory.getLogger(FilterPlanNode.class);

	PlanNode parent;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationExecutionLogger.java`
#### Snippet
```java
public class ValidationExecutionLogger {

	private final static Logger logger = LoggerFactory.getLogger(ValidationExecutionLogger.class);

	private final List<LogStatement> list = new ArrayList<>();
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
public class InnerJoin implements MultiStreamPlanNode, PlanNode {

	static private final Logger logger = LoggerFactory.getLogger(InnerJoin.class);
	private StackTraceElement[] stackTrace;
	private boolean printed = false;
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ExternalFilterByQuery.java`
#### Snippet
```java
public class ExternalFilterByQuery extends FilterPlanNode {

	static private final Logger logger = LoggerFactory.getLogger(ExternalFilterByQuery.class);

	private final SailConnection connection;
```

### MissortedModifiers
Missorted modifiers `static final private`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EmptyNode.java`
#### Snippet
```java
	private boolean printed = false;

	static final private EmptyNode instance = new EmptyNode();

	private EmptyNode() {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
public class BulkedExternalInnerJoin extends AbstractBulkJoinPlanNode {

	private final static Resource[] allContext = {};
	private static final Function<BindingSet, ValidationTuple> propertyShapeScopeAllContextMapper = b -> new ValidationTuple(
			b.getValue("a"), b.getValue("c"), ConstraintComponent.Scope.propertyShape, true, allContext);
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	public final static IRI TRANSACTIONAL_VALIDATION_LIMIT = create("transactionalValidationLimit");

	public final static IRI SHAPES_GRAPH = create("shapesGraph");

	private static IRI create(String localName) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	public final static IRI DASH_DATA_SHAPES = create("dashDataShapes");

	public final static IRI VALIDATION_RESULTS_LIMIT_TOTAL = create("validationResultsLimitTotal");
	public final static IRI VALIDATION_RESULTS_LIMIT_PER_CONSTRAINT = create("validationResultsLimitPerConstraint");
	public final static IRI TRANSACTIONAL_VALIDATION_LIMIT = create("transactionalValidationLimit");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#globalLogValidationExecution</code>
	 */
	public final static IRI GLOBAL_LOG_VALIDATION_EXECUTION = create("globalLogValidationExecution");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#logValidationPlans</code>
	 */
	public final static IRI LOG_VALIDATION_PLANS = create("logValidationPlans");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#performanceLogging</code>
	 */
	public final static IRI PERFORMANCE_LOGGING = create("performanceLogging");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	public final static IRI VALIDATION_RESULTS_LIMIT_TOTAL = create("validationResultsLimitTotal");
	public final static IRI VALIDATION_RESULTS_LIMIT_PER_CONSTRAINT = create("validationResultsLimitPerConstraint");
	public final static IRI TRANSACTIONAL_VALIDATION_LIMIT = create("transactionalValidationLimit");

	public final static IRI SHAPES_GRAPH = create("shapesGraph");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java

	public final static IRI VALIDATION_RESULTS_LIMIT_TOTAL = create("validationResultsLimitTotal");
	public final static IRI VALIDATION_RESULTS_LIMIT_PER_CONSTRAINT = create("validationResultsLimitPerConstraint");
	public final static IRI TRANSACTIONAL_VALIDATION_LIMIT = create("transactionalValidationLimit");

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	public final static IRI SERIALIZABLE_VALIDATION = create("serializableValidation");

	public final static IRI ECLIPSE_RDF4J_SHACL_EXTENSIONS = create("eclipseRdf4jShaclExtensions");

	public final static IRI DASH_DATA_SHAPES = create("dashDataShapes");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	public final static IRI ECLIPSE_RDF4J_SHACL_EXTENSIONS = create("eclipseRdf4jShaclExtensions");

	public final static IRI DASH_DATA_SHAPES = create("dashDataShapes");

	public final static IRI VALIDATION_RESULTS_LIMIT_TOTAL = create("validationResultsLimitTotal");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#validationEnabled</code>
	 */
	public final static IRI VALIDATION_ENABLED = create("validationEnabled");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#rdfsSubClassReasoning</code>
	 */
	public final static IRI RDFS_SUB_CLASS_REASONING = create("rdfsSubClassReasoning");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#serializableValidation</code>
	 */
	public final static IRI SERIALIZABLE_VALIDATION = create("serializableValidation");

	public final static IRI ECLIPSE_RDF4J_SHACL_EXTENSIONS = create("eclipseRdf4jShaclExtensions");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#cacheSelectNodes</code>
	 */
	public final static IRI CACHE_SELECT_NODES = create("cacheSelectNodes");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#logValidationViolations</code>
	 */
	public final static IRI LOG_VALIDATION_VIOLATIONS = create("logValidationViolations");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
	 * <code>http://rdf4j.org/config/sail/shacl#parallelValidation</code>
	 */
	public final static IRI PARALLEL_VALIDATION = create("parallelValidation");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailConfig.java`
#### Snippet
```java
	public static final boolean ECLIPSE_RDF4J_SHACL_EXTENSIONS_DEFAULT = false;
	public static final boolean DASH_DATA_SHAPES_DEFAULT = false;
	public final static long VALIDATION_RESULTS_LIMIT_TOTAL_DEFAULT = 1_000_000;
	public final static long VALIDATION_RESULTS_LIMIT_PER_CONSTRAINT_DEFAULT = 1_000;
	public final static long TRANSACTIONAL_VALIDATION_LIMIT_DEFAULT = 500_000;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailConfig.java`
#### Snippet
```java
	public static final boolean DASH_DATA_SHAPES_DEFAULT = false;
	public final static long VALIDATION_RESULTS_LIMIT_TOTAL_DEFAULT = 1_000_000;
	public final static long VALIDATION_RESULTS_LIMIT_PER_CONSTRAINT_DEFAULT = 1_000;
	public final static long TRANSACTIONAL_VALIDATION_LIMIT_DEFAULT = 500_000;
	public final static Set<IRI> SHAPES_GRAPHS_DEFAULT = Set.of(RDF4J.SHACL_SHAPE_GRAPH);
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailConfig.java`
#### Snippet
```java
	public final static long VALIDATION_RESULTS_LIMIT_PER_CONSTRAINT_DEFAULT = 1_000;
	public final static long TRANSACTIONAL_VALIDATION_LIMIT_DEFAULT = 500_000;
	public final static Set<IRI> SHAPES_GRAPHS_DEFAULT = Set.of(RDF4J.SHACL_SHAPE_GRAPH);

	private boolean parallelValidation = PARALLEL_VALIDATION_DEFAULT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailConfig.java`
#### Snippet
```java
	public final static long VALIDATION_RESULTS_LIMIT_TOTAL_DEFAULT = 1_000_000;
	public final static long VALIDATION_RESULTS_LIMIT_PER_CONSTRAINT_DEFAULT = 1_000;
	public final static long TRANSACTIONAL_VALIDATION_LIMIT_DEFAULT = 500_000;
	public final static Set<IRI> SHAPES_GRAPHS_DEFAULT = Set.of(RDF4J.SHACL_SHAPE_GRAPH);

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/shape/ShapeSource.java`
#### Snippet
```java

	class ShapesGraph {
		private final static Resource[] allContext = {};

		private final Resource[] dataGraph;
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/UserProvidedClientProvider.java`
#### Snippet
```java
public class UserProvidedClientProvider implements ClientProvider {

	final private Client client;

	transient boolean closed;
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/UserProvidedClientProvider.java`
#### Snippet
```java

	@Override
	synchronized public void close() throws Exception {
		if (!closed) {
			closed = true;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/shape/ForwardChainingShapeSource.java`
#### Snippet
```java

	// SHACL Vocabulary from W3C - https://www.w3.org/ns/shacl.ttl
	private final static IRI shaclVocabularyGraph = iri(RDF4J.NAMESPACE, "shaclVocabularyGraph");
	private final static SchemaCachingRDFSInferencer shaclVocabulary = createShaclVocabulary();
	private static final Resource[] defaultContext = { RDF4J.SHACL_SHAPE_GRAPH };
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/shape/ForwardChainingShapeSource.java`
#### Snippet
```java
	// SHACL Vocabulary from W3C - https://www.w3.org/ns/shacl.ttl
	private final static IRI shaclVocabularyGraph = iri(RDF4J.NAMESPACE, "shaclVocabularyGraph");
	private final static SchemaCachingRDFSInferencer shaclVocabulary = createShaclVocabulary();
	private static final Resource[] defaultContext = { RDF4J.SHACL_SHAPE_GRAPH };

```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/SingletonClientProvider.java`
#### Snippet
```java

	@Override
	synchronized public void close() throws Exception {
		if (!closed) {
			closed = true;
```

### MissortedModifiers
Missorted modifiers `transient private`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/SingletonClientProvider.java`
#### Snippet
```java
public class SingletonClientProvider implements ClientProvider {

	transient private Client client;
	private transient boolean closed = false;
	private final String hostname;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/config/ElasticsearchStoreSchema.java`
#### Snippet
```java
	public final static IRI port = vf.createIRI(NAMESPACE, "port");
	public final static IRI index = vf.createIRI(NAMESPACE, "index");
	public final static IRI clusterName = vf.createIRI(NAMESPACE, "clusterName");

}
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/config/ElasticsearchStoreSchema.java`
#### Snippet
```java
	public final static IRI hostname = vf.createIRI(NAMESPACE, "hostname");
	public final static IRI port = vf.createIRI(NAMESPACE, "port");
	public final static IRI index = vf.createIRI(NAMESPACE, "index");
	public final static IRI clusterName = vf.createIRI(NAMESPACE, "clusterName");

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/config/ElasticsearchStoreSchema.java`
#### Snippet
```java

	public final static IRI hostname = vf.createIRI(NAMESPACE, "hostname");
	public final static IRI port = vf.createIRI(NAMESPACE, "port");
	public final static IRI index = vf.createIRI(NAMESPACE, "index");
	public final static IRI clusterName = vf.createIRI(NAMESPACE, "clusterName");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/config/ElasticsearchStoreSchema.java`
#### Snippet
```java
	public static final String PREFIX = "ess";

	public final static IRI hostname = vf.createIRI(NAMESPACE, "hostname");
	public final static IRI port = vf.createIRI(NAMESPACE, "port");
	public final static IRI index = vf.createIRI(NAMESPACE, "index");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
	private Set<ElasticsearchId> deleteStatementBuffer = new HashSet<>();

	private final static ElasticsearchValueFactory vf = (ElasticsearchValueFactory) ElasticsearchValueFactory
			.getInstance();

```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java

	@Override
	synchronized public void addStatement(ExtensibleStatement statement) {
		if (addStatementBuffer.size() >= BUFFER_THRESHOLD) {
			flushAddStatementBuffer();
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java

	@Override
	synchronized public void removeStatement(ExtensibleStatement statement) {

		ElasticsearchId elasticsearchIdStatement;
```

### MissortedModifiers
Missorted modifiers `synchronized private`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
	}

	synchronized private void flushRemoveStatementBuffer() {

		if (deleteStatementBuffer.isEmpty()) {
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java

	@Override
	synchronized public void clear(boolean inferred, Resource[] contexts) {

		BulkByScrollResponse response = new DeleteByQueryRequestBuilder(clientProvider.getClient(),
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
		 * Default timeout setting for transaction cache entries (in seconds).
		 */
		public final static int DEFAULT = 60;
	}

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 * Default timeout setting for transaction cache entries (in seconds).
	 */
	public final static int DEFAULT_TIMEOUT = Protocol.TIMEOUT.DEFAULT;

	/**
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/AbstractForwardChainingInferencerConnection.java`
#### Snippet
```java
	 *-----------*/

	static private final Logger logger = LoggerFactory.getLogger(AbstractForwardChainingInferencerConnection.class);

	/*-----------*
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/config/CustomGraphQueryInferencerSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/customGraphQueryInferencer#queryLanguage</var>
	 */
	public final static IRI QUERY_LANGUAGE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/config/CustomGraphQueryInferencerSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/customGraphQueryInferencer#matcherQuery</var>
	 */
	public final static IRI MATCHER_QUERY;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/config/CustomGraphQueryInferencerSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/customGraphQueryInferencer#ruleQuery</var>
	 */
	public final static IRI RULE_QUERY;

	/**
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencerConnection.java`
#### Snippet
```java
class ForwardChainingRDFSInferencerConnection extends AbstractForwardChainingInferencerConnection {

	static private final Logger logger = LoggerFactory.getLogger(ForwardChainingRDFSInferencerConnection.class);

	/*-----------*
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStoreRevision.java`
#### Snippet
```java
	 *-----------*/

	transient private final ValueStore valueStore;

	/*--------------*
```

### MissortedModifiers
Missorted modifiers `static public`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	 * @return inferencer
	 */
	static public SchemaCachingRDFSInferencer fastInstantiateFrom(
			SchemaCachingRDFSInferencer sailToInstantiateFrom, NotifyingSail store) {
		return fastInstantiateFrom(sailToInstantiateFrom, store, true);
```

### MissortedModifiers
Missorted modifiers `static public`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	 * @return inferencer
	 */
	static public SchemaCachingRDFSInferencer fastInstantiateFrom(
			SchemaCachingRDFSInferencer sailToInstantiateFrom, NotifyingSail store,
			boolean useAllRdfsRules) {
```

### MissortedModifiers
Missorted modifiers `volatile protected`
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java

	// the SPIN sail will add inferred statements that it wants to be used for further inference.
	volatile protected boolean useInferredToCreateSchema;

	// Schema cache
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#namespaceCacheSize</tt>
	 */
	public final static IRI NAMESPACE_CACHE_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#valueDBSize</tt>
	 */
	public final static IRI VALUE_DB_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#namespaceIDCacheSize</tt>
	 */
	public final static IRI NAMESPACE_ID_CACHE_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#tripleDBSize</tt>
	 */
	public final static IRI TRIPLE_DB_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#autoGrow</tt>
	 */
	public final static IRI AUTO_GROW;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#valueIDCacheSize</tt>
	 */
	public final static IRI VALUE_ID_CACHE_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#valueCacheSize</tt>
	 */
	public final static IRI VALUE_CACHE_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#forceSync</tt>
	 */
	public final static IRI FORCE_SYNC;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
	 * <tt>http://rdf4j.org/config/sail/lmdb#tripleIndexes</tt>
	 */
	public final static IRI TRIPLE_INDEXES;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/config/HTTPRepositorySchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/http#repositoryURL</var>
	 */
	public final static IRI REPOSITORYURL;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/config/HTTPRepositorySchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/http#password</var>
	 */
	public final static IRI PASSWORD;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/config/HTTPRepositorySchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/http#username</var>
	 */
	public final static IRI USERNAME;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/BasicParserSettings.java`
#### Snippet
```java
public class BasicParserSettings {

	private final static Logger log = LoggerFactory.getLogger(BasicParserSettings.class);

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/XMLReaderBasedParser.java`
#### Snippet
```java
public abstract class XMLReaderBasedParser extends AbstractRDFParser {

	private final static Set<RioSetting<Boolean>> compulsoryXmlFeatureSettings = new HashSet<>(
			Arrays.asList(XMLParserSettings.SECURE_PROCESSING, XMLParserSettings.DISALLOW_DOCTYPE_DECL,
					XMLParserSettings.EXTERNAL_GENERAL_ENTITIES, XMLParserSettings.EXTERNAL_PARAMETER_ENTITIES,
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java

	// static UUID as prefix together with a thread safe incrementing long ensures a unique identifier.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
	// static UUID as prefix together with a thread safe incrementing long ensures a unique identifier.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

	private final MessageDigest md5;
```

### MissortedModifiers
Missorted modifiers `final static`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java

	// Logger for static methods which are not overridden
	private final static Logger LOGGER = LoggerFactory.getLogger(SPARQLQueryTest.class);

	protected final String testURI;
```

### MissortedModifiers
Missorted modifiers `private @Autowired`
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/uuidsource/sequence/UUIDSequence.java`
#### Snippet
```java
public class UUIDSequence implements UUIDSource {
	private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
	private @Autowired RDF4JTemplate rdf4JTemplate;
	private final int prefetchCount;
	private final Map<RepositoryConnection, Queue<IRI>> prefetchedUUIDs = Collections
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemIRI.java`
#### Snippet
```java
	 * The object that created this MemURI.
	 */
	transient private final Object creator;

	/**
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemIRI.java`
#### Snippet
```java
	 * The list of statements for which this MemURI is the object.
	 */
	transient private final MemStatementList objectStatements = new MemStatementList();

	/*--------------*
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemIRI.java`
#### Snippet
```java
	 * The list of statements for which this MemURI is the predicate.
	 */
	transient private final MemStatementList predicateStatements = new MemStatementList();

	/**
```

### MissortedModifiers
Missorted modifiers `transient final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemResource.java`
#### Snippet
```java
	 * The list of statements for which this MemURI is the subject.
	 */
	transient final MemStatementList subjectStatements = new MemStatementList();

	/**
```

### MissortedModifiers
Missorted modifiers `transient final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemResource.java`
#### Snippet
```java
	 * The list of statements for which this MemURI represents the context.
	 */
	transient final MemStatementList contextStatements = new MemStatementList();

	public MemStatementList getSubjectStatementList() {
```

### MissortedModifiers
Missorted modifiers `transient private`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemBNode.java`
#### Snippet
```java
	 * The object that created this MemBNode.
	 */
	transient final private Object creator;

	/**
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemBNode.java`
#### Snippet
```java
	 * The list of statements for which this MemBNode is the object.
	 */
	transient private final MemStatementList objectStatements = new MemStatementList();
	/**
	 * The blank node's identifier.
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemTriple.java`
#### Snippet
```java
	 * The list of statements for which this MemTriple is the object.
	 */
	transient private final MemStatementList objectStatements = new MemStatementList();

	public MemTriple(Object creator, MemResource subject, MemIRI predicate, MemValue object) {
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemTriple.java`
#### Snippet
```java
	private static final long serialVersionUID = -9085188980084028689L;

	transient private final Object creator;

	private final MemResource subject;
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemLiteral.java`
#### Snippet
```java
	 * The object that created this MemLiteral.
	 */
	transient private final Object creator;

	/**
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemLiteral.java`
#### Snippet
```java
	 * The list of statements for which this MemLiteral is the object.
	 */
	transient private final MemStatementList objectStatements = new MemStatementList();

	/*--------------*
```

### MissortedModifiers
Missorted modifiers `transient private`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/CalendarMemLiteral.java`
#### Snippet
```java
	 *-----------*/

	transient private XMLGregorianCalendar calendar;

	/*--------------*
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementIteratorCache.java`
#### Snippet
```java
public class MemStatementIteratorCache {

	private final static Logger logger = LoggerFactory.getLogger(MemStatementIteratorCache.class);

	// the number of times an iterator needs to be used before it will be cached
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/config/MemoryStoreSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/memory#syncDelay</var>
	 */
	public final static IRI SYNC_DELAY;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/config/MemoryStoreSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/memory#persist</var>
	 */
	public final static IRI PERSIST;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/GZipUtil.java`
#### Snippet
```java
	 * little endian).
	 */
	private final static byte MAGIC_NUMBER[] = { (byte) 0x1f, (byte) 0x8b };

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ZipUtil.java`
#### Snippet
```java
	 * Magic number for ZIP files (4 bytes: <var>0x04034b50</var>).
	 */
	private final static byte MAGIC_NUMBER[] = { (byte) 0x50, (byte) 0x4B, (byte) 0x03, (byte) 0x04 };

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
			@SuppressWarnings("FieldMayBeFinal")
			private volatile int snapshot;
			private final static VarHandle SNAPSHOT;

			static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
class MemorySailStore implements SailStore {

	private final static Logger logger = LoggerFactory.getLogger(MemorySailStore.class);
	private static final Runtime RUNTIME = Runtime.getRuntime();

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/SPARQLStarResultsJSONConstants.java`
#### Snippet
```java
	 * Key name of the JSON object for the triple's subject - Apache Jena dialect
	 */
	final static String SUBJECT_JENA = "subject";

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/SPARQLStarResultsJSONConstants.java`
#### Snippet
```java
	 * Key name of the JSON object for the triple's predicate - Apache Jena dialect
	 */
	final static String PREDICATE_JENA = "predicate";

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/SPARQLStarResultsJSONConstants.java`
#### Snippet
```java
	 * Key name of the JSON object for the triple's object - Apache Jena dialect
	 */
	final static String OBJECT_JENA = "object";

}
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/SPARQLStarResultsJSONConstants.java`
#### Snippet
```java
	 * Type string for serialized {@link org.eclipse.rdf4j.model.Triple} value - Stardog dialect
	 */
	final static String TRIPLE_STARDOG = "statement";

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/BackgroundResultExecutor.java`
#### Snippet
```java
public class BackgroundResultExecutor implements AutoCloseable {

	private final static ConcurrentCleaner cleaner = new ConcurrentCleaner();

	private final Logger logger = LoggerFactory.getLogger(BackgroundResultExecutor.class);
```

### MissortedModifiers
Missorted modifiers `synchronized private`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
	}

	synchronized private void synchronizedUpgrade() {
		if (model == null) {
			// make statements unmodifiable first, to increase chance of an early failure if the user is doing
```

### MissortedModifiers
Missorted modifiers `volatile private`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
	final Set<Namespace> namespaces = new LinkedHashSet<>();

	volatile private Model model = null;

	private final ModelFactory modelFactory;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java

	// static UUID as prefix together with a thread safe incrementing long ensures unique blank nodes.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	// static UUID as prefix together with a thread safe incrementing long ensures unique blank nodes.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

	private static final DatatypeFactory datatypeFactory;
```

### MissortedModifiers
Missorted modifiers `transient private`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	// Cache Optional instance for the language, or null if not yet computed. Marked as transient because Optional is
	// not serializable.
	transient private Optional<String> optionalLanguageCache = null;

	/**
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Set<Resource> subjects() {
		return delegate.subjects();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public int hashCode() {
		return delegate.hashCode();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Set<IRI> predicates() {
		return delegate.predicates();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Set<Resource> contexts() {
		return delegate.contexts();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public void setNamespace(Namespace namespace) {
		delegate.setNamespace(namespace);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean retainAll(Collection<?> c) {
		return delegate.retainAll(c);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Optional<Namespace> getNamespace(String prefix) {
		return delegate.getNamespace(prefix);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public void forEach(Consumer<? super Statement> action) {
		delegate.forEach(action);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean equals(Object o) {
		return delegate.equals(o);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public void clear() {
		delegate.clear();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Optional<Namespace> removeNamespace(String prefix) {
		return delegate.removeNamespace(prefix);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public int size() {
		return delegate.size();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Namespace setNamespace(String prefix, String name) {
		return delegate.setNamespace(prefix, name);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Model unmodifiable() {
		return delegate.unmodifiable();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean contains(Resource subj, IRI pred, Value obj, Resource... contexts) {
		return delegate.contains(subj, pred, obj, contexts);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Set<Namespace> getNamespaces() {
		return delegate.getNamespaces();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean removeAll(Collection<?> c) {
		return delegate.removeAll(c);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Model filter(Resource subj, IRI pred, Value obj, Resource... contexts) {
		return delegate.filter(subj, pred, obj, contexts);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean remove(Object o) {
		return delegate.remove(o);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean addAll(Collection<? extends Statement> c) {
		return delegate.addAll(c);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean add(Resource subj, IRI pred, Value obj, Resource... contexts) {
		return delegate.add(subj, pred, obj, contexts);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean isEmpty() {
		return delegate.isEmpty();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean remove(Resource subj, IRI pred, Value obj, Resource... contexts) {
		return delegate.remove(subj, pred, obj, contexts);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Iterator<Statement> iterator() {
		return delegate.iterator();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean clear(Resource... context) {
		return delegate.clear(context);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean containsAll(Collection<?> c) {
		return delegate.containsAll(c);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean add(Statement statement) {
		return delegate.add(statement);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Set<Value> objects() {
		return delegate.objects();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Object[] toArray() {
		return delegate.toArray();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Stream<Statement> parallelStream() {
		return delegate.parallelStream();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean removeIf(Predicate<? super Statement> filter) {
		return delegate.removeIf(filter);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Spliterator<Statement> spliterator() {
		return delegate.spliterator();
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public <T> T[] toArray(T[] a) {
		return delegate.toArray(a);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean contains(Object o) {
		return delegate.contains(o);
	}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public Stream<Statement> stream() {
		return delegate.stream();
	}
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC32.java`
#### Snippet
```java
 */
class CRC32 implements Checksum {
	private final static int[] TABLE = new int[] {
			0x00000000, 0xF26B8303, 0xE13B70F7, 0x1350F3F4, 0xC79A971F, 0x35F1141C, 0x26A1E7E8, 0xD4CA64EB,
			0x8AD958CF, 0x78B2DBCC, 0x6BE22838, 0x9989AB3B, 0x4D43CFD0, 0xBF284CD3, 0xAC78BF27, 0x5E133C24,
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTDictionary.java`
#### Snippet
```java
	protected final static byte[] DICT_FORMAT = "<http://purl.org/HDT/hdt#dictionaryFour>"
			.getBytes(StandardCharsets.US_ASCII);
	protected final static String DICT_MAPPING = "mapping";
	protected final static String DICT_ELEMENTS = "elements";

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTDictionary.java`
#### Snippet
```java
			.getBytes(StandardCharsets.US_ASCII);
	protected final static String DICT_MAPPING = "mapping";
	protected final static String DICT_ELEMENTS = "elements";

	@Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTDictionary.java`
#### Snippet
```java
 */
class HDTDictionary extends HDTPart {
	protected final static byte[] DICT_FORMAT = "<http://purl.org/HDT/hdt#dictionaryFour>"
			.getBytes(StandardCharsets.US_ASCII);
	protected final static String DICT_MAPPING = "mapping";
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriples.java`
#### Snippet
```java
	protected final static byte[] FORMAT_LIST = "<http://purl.org/HDT/hdt#triplesList>"
			.getBytes(StandardCharsets.US_ASCII);
	protected final static byte[] FORMAT_BITMAP = "<http://purl.org/HDT/hdt#triplesBitmap>"
			.getBytes(StandardCharsets.US_ASCII);
	protected final static String ORDER = "order";
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriples.java`
#### Snippet
```java
	}

	protected final static byte[] FORMAT_LIST = "<http://purl.org/HDT/hdt#triplesList>"
			.getBytes(StandardCharsets.US_ASCII);
	protected final static byte[] FORMAT_BITMAP = "<http://purl.org/HDT/hdt#triplesBitmap>"
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriples.java`
#### Snippet
```java
	protected final static byte[] FORMAT_BITMAP = "<http://purl.org/HDT/hdt#triplesBitmap>"
			.getBytes(StandardCharsets.US_ASCII);
	protected final static String ORDER = "order";
	protected final static String NUM = "numTriples";

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriples.java`
#### Snippet
```java
			.getBytes(StandardCharsets.US_ASCII);
	protected final static String ORDER = "order";
	protected final static String NUM = "numTriples";

	private Order order;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTBitmap.java`
#### Snippet
```java
 */
class HDTBitmap extends HDTPart {
	protected final static int BITMAP1 = 1;

	private int bits;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTHeader.java`
#### Snippet
```java

	protected final static byte[] HEADER_FORMAT = "ntriples".getBytes(StandardCharsets.US_ASCII);
	protected final static String HEADER_LENGTH = "length";

	private byte[] headerData;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTHeader.java`
#### Snippet
```java
class HDTHeader extends HDTPart {

	protected final static byte[] HEADER_FORMAT = "ntriples".getBytes(StandardCharsets.US_ASCII);
	protected final static String HEADER_LENGTH = "length";

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC16.java`
#### Snippet
```java
class CRC16 implements Checksum {
	// note that Java considers short to be signed, hence the need to cast values larger than 32767
	private final static short[] TABLE = new short[] {
			(short) 0x0000, (short) 0xC0C1, (short) 0xC181, (short) 0x0140, (short) 0xC301, (short) 0x03C0,
			(short) 0x0280, (short) 0xC241,
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTGlobal.java`
#### Snippet
```java
	protected final static byte[] GLOBAL_FORMAT = "<http://purl.org/HDT/hdt#HDTv1>".getBytes(StandardCharsets.US_ASCII);
	protected final static String GLOBAL_BASEURI = "BaseUri";
	protected final static String GLOBAL_SOFTWARE = "Software";

	@Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTGlobal.java`
#### Snippet
```java
 */
class HDTGlobal extends HDTPart {
	protected final static byte[] GLOBAL_FORMAT = "<http://purl.org/HDT/hdt#HDTv1>".getBytes(StandardCharsets.US_ASCII);
	protected final static String GLOBAL_BASEURI = "BaseUri";
	protected final static String GLOBAL_SOFTWARE = "Software";
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTGlobal.java`
#### Snippet
```java
class HDTGlobal extends HDTPart {
	protected final static byte[] GLOBAL_FORMAT = "<http://purl.org/HDT/hdt#HDTv1>".getBytes(StandardCharsets.US_ASCII);
	protected final static String GLOBAL_BASEURI = "BaseUri";
	protected final static String GLOBAL_SOFTWARE = "Software";

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java

	// TODO: make configurable, buffer for reading object values
	private final static int BUFLEN = 1 * 1024 * 1024;
	// for debugging purposes
	protected final String name;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTPart.java`
#### Snippet
```java
	}

	protected final static byte[] COOKIE = "$HDT".getBytes(StandardCharsets.US_ASCII);

	// TODO: make configurable, buffer for reading object values
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC8.java`
#### Snippet
```java
class CRC8 implements Checksum {
	// note that Java considers bytes to be signed, hence the need to cast values larger than 127
	private final static byte[] TABLE = new byte[] {
			(byte) 0x00, (byte) 0x07, (byte) 0x0E, (byte) 0x09, (byte) 0x1C, (byte) 0x1B, (byte) 0x12, (byte) 0x15,
			(byte) 0x38, (byte) 0x3F, (byte) 0x36, (byte) 0x31, (byte) 0x24, (byte) 0x23, (byte) 0x2A, (byte) 0x2D,
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	}

	private final static Pattern P_DURATION = Pattern.compile(
			"-?P((\\d)+Y)?((\\d)+M)?((\\d)+D)?((T(\\d)+H((\\d)+M)?((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+M((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+(\\.(\\d)+)?S))?");
	private final static Pattern P_DAYTIMEDURATION = Pattern.compile(
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_DAYTIMEDURATION = Pattern.compile(
			"-?P((\\d)+D)?((T(\\d)+H((\\d)+M)?((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+M((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+(\\.(\\d)+)?S))?");
	private final static Pattern P_YEARMONTHDURATION = Pattern.compile("-?P((\\d)+Y)?((\\d)+M)?");
	private final static Pattern P_TIMEZONE = Pattern.compile(".*(Z|[+-]((0\\d|1[0-3]):[0-5]\\d|14:00))$");
	private final static Pattern P_DATE = Pattern.compile("-?\\d{4,}-\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_YEARMONTHDURATION = Pattern.compile("-?P((\\d)+Y)?((\\d)+M)?");
	private final static Pattern P_TIMEZONE = Pattern.compile(".*(Z|[+-]((0\\d|1[0-3]):[0-5]\\d|14:00))$");
	private final static Pattern P_DATE = Pattern.compile("-?\\d{4,}-\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_TIME = Pattern.compile("\\d\\d:\\d\\d:\\d\\d(\\.\\d+)?(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GDAY = Pattern.compile("---\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			"-?P((\\d)+D)?((T(\\d)+H((\\d)+M)?((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+M((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+(\\.(\\d)+)?S))?");
	private final static Pattern P_YEARMONTHDURATION = Pattern.compile("-?P((\\d)+Y)?((\\d)+M)?");
	private final static Pattern P_TIMEZONE = Pattern.compile(".*(Z|[+-]((0\\d|1[0-3]):[0-5]\\d|14:00))$");
	private final static Pattern P_DATE = Pattern.compile("-?\\d{4,}-\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_TIME = Pattern.compile("\\d\\d:\\d\\d:\\d\\d(\\.\\d+)?(Z|([+\\-])\\d\\d:\\d\\d)?");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_DURATION = Pattern.compile(
			"-?P((\\d)+Y)?((\\d)+M)?((\\d)+D)?((T(\\d)+H((\\d)+M)?((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+M((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+(\\.(\\d)+)?S))?");
	private final static Pattern P_DAYTIMEDURATION = Pattern.compile(
			"-?P((\\d)+D)?((T(\\d)+H((\\d)+M)?((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+M((\\d)+(\\.(\\d)+)?S)?)|(T(\\d)+(\\.(\\d)+)?S))?");
	private final static Pattern P_YEARMONTHDURATION = Pattern.compile("-?P((\\d)+Y)?((\\d)+M)?");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_GMONTHDAY = Pattern.compile("--\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GYEAR = Pattern.compile("-?\\d{4,}(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GYEARMONTH = Pattern.compile("-?\\d{4,}-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");

	private static final Set<IRI> primitiveDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_TIMEZONE = Pattern.compile(".*(Z|[+-]((0\\d|1[0-3]):[0-5]\\d|14:00))$");
	private final static Pattern P_DATE = Pattern.compile("-?\\d{4,}-\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_TIME = Pattern.compile("\\d\\d:\\d\\d:\\d\\d(\\.\\d+)?(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GDAY = Pattern.compile("---\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GMONTH = Pattern.compile("--\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_GDAY = Pattern.compile("---\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GMONTH = Pattern.compile("--\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GMONTHDAY = Pattern.compile("--\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GYEAR = Pattern.compile("-?\\d{4,}(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GYEARMONTH = Pattern.compile("-?\\d{4,}-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_DATE = Pattern.compile("-?\\d{4,}-\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_TIME = Pattern.compile("\\d\\d:\\d\\d:\\d\\d(\\.\\d+)?(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GDAY = Pattern.compile("---\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GMONTH = Pattern.compile("--\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GMONTHDAY = Pattern.compile("--\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_GMONTH = Pattern.compile("--\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GMONTHDAY = Pattern.compile("--\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GYEAR = Pattern.compile("-?\\d{4,}(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GYEARMONTH = Pattern.compile("-?\\d{4,}-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_TIME = Pattern.compile("\\d\\d:\\d\\d:\\d\\d(\\.\\d+)?(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GDAY = Pattern.compile("---\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GMONTH = Pattern.compile("--\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GMONTHDAY = Pattern.compile("--\\d\\d-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");
	private final static Pattern P_GYEAR = Pattern.compile("-?\\d{4,}(Z|([+\\-])\\d\\d:\\d\\d)?");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
public class ArrangedWriter extends AbstractRDFWriter {

	private final static int DEFAULT_QUEUE_SIZE = 100;

	private final RDFWriter delegate;
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/SPARQLResultsXSVMappingStrategy.java`
#### Snippet
```java
 * @author Andrew Rucker Jones
 */
abstract public class SPARQLResultsXSVMappingStrategy implements MappingStrategy<BindingSet> {

	protected List<String> bindingNames = null;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/contextaware#removeContext</var>
	 */
	public final static IRI REMOVE_CONTEXT;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI ARCHIVE_CONTEXT;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/contextaware#includeInferred</var>
	 */
	public final static IRI INCLUDE_INFERRED;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/contextaware#maxQueryTime</var>
	 */
	public final static IRI MAX_QUERY_TIME;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/contextaware#readContext</var>
	 */
	public final static IRI READ_CONTEXT;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/contextaware#queryLanguage</var>
	 */
	public final static IRI QUERY_LANGUAGE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI ADD_CONTEXT;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/contextaware#insertContext</var>
	 */
	public final static IRI INSERT_CONTEXT;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/contextaware#base</var>
	 */
	public final static IRI BASE_URI;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	private final int maximumUrlLength;

	final static Logger logger = LoggerFactory.getLogger(SPARQLProtocolSession.class);

	private ValueFactory valueFactory;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository#delegate</var>
	 */
	public final static IRI DELEGATE;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository#RepositoryContext</var>
	 */
	public final static IRI REPOSITORY_CONTEXT;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository#repositoryImpl</var>
	 */
	public final static IRI REPOSITORYIMPL;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository#repositoryID</var>
	 */
	public final static IRI REPOSITORYID;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository#repositoryType</var>
	 */
	public final static IRI REPOSITORYTYPE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository#Repository</var>
	 */
	public final static IRI REPOSITORY;

	/**
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailStore.java`
#### Snippet
```java

	@Override
	synchronized public void close() throws SailException {
		closed = true;
		if (evaluationStatisticsMaintainerThread != null) {
```

### MissortedModifiers
Missorted modifiers `synchronized private`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleSailStore.java`
#### Snippet
```java
	}

	synchronized private void startEvaluationStatisticsMaintainerThread() {
		if (!closed) {
			return;
```

### MissortedModifiers
Missorted modifiers `synchronized protected`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java

	@Override
	synchronized protected void initializeInternal() throws SailException {
		if (sailStore != null) {
			sailStore.close();
```

### MissortedModifiers
Missorted modifiers `synchronized protected`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ExtensibleStore.java`
#### Snippet
```java

	@Override
	synchronized protected void shutDownInternal() throws SailException {
		sailStore.close();
		sailStore = null;
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleEvaluationStatistics.java`
#### Snippet
```java

	@Override
	abstract protected CardinalityCalculator createCardinalityCalculator();

}
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
	}

	synchronized public void clearCache() {
		cache.invalidateAll();
		// overflow is not a problem since we use == to compare in submitToCache
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
	}

	synchronized public void submitToCache(Long localCacheTicket, PartialStatement partialStatement,
			List<ExtensibleStatement> statements) {
		if (localCacheTicket == cacheTicket && statements != null) {
```

### MissortedModifiers
Missorted modifiers `synchronized private`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
	}

	synchronized private CloseableIteration<? extends ExtensibleStatement, SailException> getCached(
			PartialStatement partialStatement) {
		List<ExtensibleStatement> statements = cache.getIfPresent(partialStatement);
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/proxy#proxiedID</var>
	 */
	public final static IRI PROXIED_ID = SimpleValueFactory.getInstance().createIRI(NAMESPACE, "proxiedID");

	/*-----------*
```

### MissortedModifiers
Missorted modifiers `volatile private`
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/ExtensibleDynamicEvaluationStatistics.java`
#### Snippet
```java
	private final HLL[][] subjectPredicateIndex_removed = new HLL[64][64];
	private final HLL[][] predicateObjectIndex_removed = new HLL[64][64];
	volatile private Thread queueConsumingThread;

	public ExtensibleDynamicEvaluationStatistics(ExtensibleSailStore extensibleSailStore) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/SailConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail#connectionTimeOut</var>
	 */
	public final static IRI CONNECTION_TIME_OUT;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/SailConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail#delegate</var>
	 */
	public final static IRI DELEGATE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/SailConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail#sailType</var>
	 */
	public final static IRI SAILTYPE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/SailConfigSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail#iterationCacheSyncTreshold</var>
	 */
	public final static IRI ITERATION_CACHE_SYNC_THRESHOLD;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSail.java`
#### Snippet
```java
	 * Default connection timeout on shutdown: 20,000 milliseconds.
	 */
	protected final static long DEFAULT_CONNECTION_TIMEOUT = 20000L;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java
	}

	private final static class CleanableState<E, X extends Exception> implements Runnable {

		private final CloseableIteration<E, X> iteration;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/LockManager.java`
#### Snippet
```java
public class LockManager {

	private final static Logger logger = LoggerFactory.getLogger(LockManager.class);

	final StampedLock lock = new StampedLock();
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockDiagnostics.java`
#### Snippet
```java

	private final static LockDiagnostics[] legacyTracking = { releaseAbandoned, detectStalledOrDeadlock, stackTrace };
	private final static LockDiagnostics[] noTracking = { releaseAbandoned };

	public static LockDiagnostics[] fromLegacyTracking(boolean trackLocks) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockDiagnostics.java`
#### Snippet
```java
	stackTrace;

	private final static LockDiagnostics[] legacyTracking = { releaseAbandoned, detectStalledOrDeadlock, stackTrace };
	private final static LockDiagnostics[] noTracking = { releaseAbandoned };

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/ExclusiveLockManager.java`
#### Snippet
```java
public class ExclusiveLockManager {

	private final static Logger logger = LoggerFactory.getLogger(ExclusiveLockManager.class);

	// the underlying lock object
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/CleanerTupleQueryResult.java`
#### Snippet
```java
	}

	private final static class CleanableState implements Runnable {

		private final TupleQueryResult iteration;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/ConcurrentCleaner.java`
#### Snippet
```java
	private static final int MAX = 128;

	private final static Cleaner[] cleaner;

	private static final int mask;
```

### MissortedModifiers
Missorted modifiers `synchronized static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/ConcurrentCleaner.java`
#### Snippet
```java
	}

	private synchronized static Cleaner instantiateCleaner(int index) {
		if (ConcurrentCleaner.cleaner[index] == null) {
			ConcurrentCleaner.cleaner[index] = Cleaner.create();
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/CleanerGraphQueryResult.java`
#### Snippet
```java
	}

	private final static class CleanableState implements Runnable {

		private final GraphQueryResult iteration;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockCleaner.java`
#### Snippet
```java
public class LockCleaner<T extends Lock> implements LockMonitoring<T> {

	private final static ConcurrentCleaner cleaner = new ConcurrentCleaner();
	private final Logger logger;
	private final Lock.ExtendedSupplier<T> supplier;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockTracking.java`
#### Snippet
```java
	private final Logger logger;

	private final static ConcurrentCleaner cleaner = new ConcurrentCleaner();
	private final static AtomicLong seq = new AtomicLong();

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockTracking.java`
#### Snippet
```java

	private final static ConcurrentCleaner cleaner = new ConcurrentCleaner();
	private final static AtomicLong seq = new AtomicLong();

	private final ReentrantLock staleLoggingLock = new ReentrantLock();
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

	private final static String newLine = System.getProperty("line.separator");

	private final String id = "UUID_" + uniqueIdPrefix + uniqueIdSuffix.incrementAndGet();
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
	 * @return Human readable number. Eg. 12.1M for 1212213.4 and UNKNOWN for -1.
	 */
	static private String toHumanReadableNumber(Double number) {
		String humanReadbleString;
		if (number == null) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java

	// static UUID as prefix together with a thread safe incrementing long ensures a unique identifier.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

```

### MissortedModifiers
Missorted modifiers `static private`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
	 * @return Human readable number. Eg. 12.1M for 1212213.4 and UNKNOWN for -1.
	 */
	static private String toHumanReadableNumber(Long number) {
		String humanReadbleString;
		if (number == null) {
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
	 * @return Human readable time.
	 */
	static private String toHumanReadableTime(Double millis) {
		String humanReadbleString;

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
	// static UUID as prefix together with a thread safe incrementing long ensures a unique identifier.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

	private final static String newLine = System.getProperty("line.separator");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResults.java`
#### Snippet
```java
		}

		private final static class CleanableState implements Runnable {

			private final GraphQueryResult iteration;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResults.java`
#### Snippet
```java
public class QueryResults extends Iterations {

	private final static Cleaner cleaner = Cleaner.create();

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/config/SailRepositorySchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/sail#sailImpl</var>
	 */
	public final static IRI SAILIMPL;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/config/ProxyRepositorySchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/repository/proxy#proxiedID</var>
	 */
	public final static IRI PROXIED_ID;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/BasicQueryWriterSettings.java`
#### Snippet
```java
	 * Defaults to false.
	 */
	public final static RioSetting<Boolean> ADD_SESAME_QNAME = new RioSettingImpl<>(
			"org.eclipse.rdf4j.query.resultio.addsesameqname", "Add Sesame QName", false);

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	// static UUID as prefix together with a thread safe incrementing long ensures a unique identifier.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

	/*-----------*
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java

	// static UUID as prefix together with a thread safe incrementing long ensures a unique identifier.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

```

### MissortedModifiers
Missorted modifiers `final static`
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractTupleQueryResultWriterTest.java`
#### Snippet
```java
public abstract class AbstractTupleQueryResultWriterTest {

	protected final static ValueFactory vf = SimpleValueFactory.getInstance();

	@Test
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void AdditiveExpression() throws ParseException {
		MultiplicativeExpression();
		label_29: while (true) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void TriplesBlock() throws ParseException {
		TriplesSameSubjectPath();
		while (jj_2_2()) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GraphRef() throws ParseException {
		jj_consume_token(GRAPH);
		IRIref();
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void RDFTermFunction() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case IS_IRI: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void TriplesNode() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case LPAREN: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Load() throws ParseException {/* @bgen(jjtree) Load */
		ASTLoad jjtn000 = new ASTLoad(JJTLOAD);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BindingSet() throws ParseException {/* @bgen(jjtree) BindingSet */
		ASTBindingSet jjtn000 = new ASTBindingSet(JJTBINDINGSET);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void IRIFunc() throws ParseException {/* @bgen(jjtree) IRIFunc */
		ASTIRIFunc jjtn000 = new ASTIRIFunc(JJTIRIFUNC);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Query() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case SELECT: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ExistsFunc() throws ParseException {/* @bgen(jjtree) ExistsFunc */
		ASTExistsFunc jjtn000 = new ASTExistsFunc(JJTEXISTSFUNC);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Expression() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case TRIPLE_OPEN: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Avg() throws ParseException {/* @bgen(jjtree) Avg */
		ASTAvg jjtn000 = new ASTAvg(JJTAVG);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PrimaryExpression() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case LPAREN: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void StrDt() throws ParseException {/* @bgen(jjtree) StrDt */
		ASTStrDt jjtn000 = new ASTStrDt(JJTSTRDT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SubSelect() throws ParseException {/* @bgen(jjtree) SelectQuery */
		ASTSelectQuery jjtn000 = new ASTSelectQuery(JJTSELECTQUERY);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void LowerCase() throws ParseException {/* @bgen(jjtree) LowerCase */
		ASTLowerCase jjtn000 = new ASTLowerCase(JJTLOWERCASE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Move() throws ParseException {/* @bgen(jjtree) Move */
		ASTMove jjtn000 = new ASTMove(JJTMOVE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void In() throws ParseException {/* @bgen(jjtree) In */
		ASTIn jjtn000 = new ASTIn(JJTIN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ConstructQuery() throws ParseException {/* @bgen(jjtree) ConstructQuery */
		ASTConstructQuery jjtn000 = new ASTConstructQuery(JJTCONSTRUCTQUERY);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Max() throws ParseException {/* @bgen(jjtree) Max */
		ASTMax jjtn000 = new ASTMax(JJTMAX);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void UpperCase() throws ParseException {/* @bgen(jjtree) UpperCase */
		ASTUpperCase jjtn000 = new ASTUpperCase(JJTUPPERCASE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Copy() throws ParseException {/* @bgen(jjtree) Copy */
		ASTCopy jjtn000 = new ASTCopy(JJTCOPY);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void StrBefore() throws ParseException {/* @bgen(jjtree) StrBefore */
		ASTStrBefore jjtn000 = new ASTStrBefore(JJTSTRBEFORE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PrefixedName() throws ParseException {/* @bgen(jjtree) QName */
		ASTQName jjtn000 = new ASTQName(JJTQNAME);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Concat() throws ParseException {/* @bgen(jjtree) Concat */
		ASTConcat jjtn000 = new ASTConcat(JJTCONCAT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	 * Get the next Token.
	 */
	final public Token getNextToken() {
		if (token.next != null) {
			token = token.next;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BindingsClause() throws ParseException {/* @bgen(jjtree) BindingsClause */
		ASTBindingsClause jjtn000 = new ASTBindingsClause(JJTBINDINGSCLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BaseDecl() throws ParseException {/* @bgen(jjtree) BaseDecl */
		ASTBaseDecl jjtn000 = new ASTBaseDecl(JJTBASEDECL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void QuadData() throws ParseException {
		jj_consume_token(LBRACE);
		Quads();
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PathPrimary() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case Q_IRI_REF:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void DatasetClause() throws ParseException {/* @bgen(jjtree) DatasetClause */
		ASTDatasetClause jjtn000 = new ASTDatasetClause(JJTDATASETCLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Timezone() throws ParseException {/* @bgen(jjtree) Timezone */
		ASTTimezone jjtn000 = new ASTTimezone(JJTTIMEZONE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Coalesce() throws ParseException {/* @bgen(jjtree) Coalesce */
		ASTCoalesce jjtn000 = new ASTCoalesce(JJTCOALESCE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Object() throws ParseException {
		GraphNode();
	}
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	final private int[] jj_la1 = new int[184];
	static private int[] jj_la1_0;
	static private int[] jj_la1_1;
	static private int[] jj_la1_2;
	static private int[] jj_la1_3;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ObjectPath() throws ParseException {
		GraphNodePath();
	}
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Floor() throws ParseException {/* @bgen(jjtree) Floor */
		ASTFloor jjtn000 = new ASTFloor(JJTFLOOR);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Replace() throws ParseException {/* @bgen(jjtree) Replace */
		ASTReplace jjtn000 = new ASTReplace(JJTREPLACE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PathNegatedPropertySet() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case INVERSE:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SHA224() throws ParseException {/* @bgen(jjtree) SHA224 */
		ASTSHA224 jjtn000 = new ASTSHA224(JJTSHA224);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GraphNodePath() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case NIL:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void TripleRef() throws ParseException {/* @bgen(jjtree) TripleRef */
		ASTTripleRef jjtn000 = new ASTTripleRef(JJTTRIPLEREF);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	static private int[] jj_la1_1;
	static private int[] jj_la1_2;
	static private int[] jj_la1_3;
	static private int[] jj_la1_4;
	static private int[] jj_la1_5;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BrackettedExpression() throws ParseException {
		jj_consume_token(LPAREN);
		Expression();
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Ceil() throws ParseException {/* @bgen(jjtree) Ceil */
		ASTCeil jjtn000 = new ASTCeil(JJTCEIL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Seconds() throws ParseException {/* @bgen(jjtree) Seconds */
		ASTSeconds jjtn000 = new ASTSeconds(JJTSECONDS);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Limit() throws ParseException {/* @bgen(jjtree) Limit */
		ASTLimit jjtn000 = new ASTLimit(JJTLIMIT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void IsBlank() throws ParseException {/* @bgen(jjtree) IsBlank */
		ASTIsBlank jjtn000 = new ASTIsBlank(JJTISBLANK);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void MinusGraphPattern() throws ParseException {/* @bgen(jjtree) MinusGraphPattern */
		ASTMinusGraphPattern jjtn000 = new ASTMinusGraphPattern(JJTMINUSGRAPHPATTERN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void If() throws ParseException {/* @bgen(jjtree) If */
		ASTIf jjtn000 = new ASTIf(JJTIF);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Offset() throws ParseException {/* @bgen(jjtree) Offset */
		ASTOffset jjtn000 = new ASTOffset(JJTOFFSET);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GraphTerm() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case Q_IRI_REF:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Substr() throws ParseException {/* @bgen(jjtree) Substr */
		ASTSubstr jjtn000 = new ASTSubstr(JJTSUBSTR);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BlankNodePropertyListPath() throws ParseException {/* @bgen(jjtree) BlankNodePropertyList */
		ASTBlankNodePropertyList jjtn000 = new ASTBlankNodePropertyList(JJTBLANKNODEPROPERTYLIST);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BooleanLiteral() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case TRUE: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GroupClause() throws ParseException {/* @bgen(jjtree) GroupClause */
		ASTGroupClause jjtn000 = new ASTGroupClause(JJTGROUPCLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Minutes() throws ParseException {/* @bgen(jjtree) Minutes */
		ASTMinutes jjtn000 = new ASTMinutes(JJTMINUTES);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void IsIRI() throws ParseException {/* @bgen(jjtree) IsIRI */
		ASTIsIRI jjtn000 = new ASTIsIRI(JJTISIRI);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void NumericExpression() throws ParseException {
		AdditiveExpression();
	}
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ConditionalAndExpression() throws ParseException {
		ValueLogical();
		if (((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) == AND) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void DeleteWhere() throws ParseException {/* @bgen(jjtree) DeleteWhere */
		ASTDeleteWhere jjtn000 = new ASTDeleteWhere(JJTDELETEWHERE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Min() throws ParseException {/* @bgen(jjtree) Min */
		ASTMin jjtn000 = new ASTMin(JJTMIN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GroupCondition() throws ParseException {/* @bgen(jjtree) GroupCondition */
		ASTGroupCondition jjtn000 = new ASTGroupCondition(JJTGROUPCONDITION);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PropertyList() throws ParseException {/* @bgen(jjtree) PropertyList */
		ASTPropertyList jjtn000 = new ASTPropertyList(JJTPROPERTYLIST);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Modify() throws ParseException {/* @bgen(jjtree) Modify */
		ASTModify jjtn000 = new ASTModify(JJTMODIFY);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void NumericLiteralPositive() throws ParseException {/* @bgen(jjtree) NumericLiteral */
		ASTNumericLiteral jjtn000 = new ASTNumericLiteral(JJTNUMERICLITERAL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void RDFLiteral() throws ParseException {/* @bgen(jjtree) RDFLiteral */
		ASTRDFLiteral jjtn000 = new ASTRDFLiteral(JJTRDFLITERAL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void StringFunction() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case SUBSTR: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void WithClause() throws ParseException {/* @bgen(jjtree) DatasetClause */
		ASTDatasetClause jjtn000 = new ASTDatasetClause(JJTDATASETCLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void FunctionCall() throws ParseException {/* @bgen(jjtree) FunctionCall */
		ASTFunctionCall jjtn000 = new ASTFunctionCall(JJTFUNCTIONCALL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Rand() throws ParseException {/* @bgen(jjtree) Rand */
		ASTRand jjtn000 = new ASTRand(JJTRAND);
		jjtree.openNodeScope(jjtn000);
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void OrderCondition() throws ParseException {/* @bgen(jjtree) OrderCondition */
		ASTOrderCondition jjtn000 = new ASTOrderCondition(JJTORDERCONDITION);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void StrStarts() throws ParseException {/* @bgen(jjtree) StrStarts */
		ASTStrStarts jjtn000 = new ASTStrStarts(JJTSTRSTARTS);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BindingValue() throws ParseException {/* @bgen(jjtree) BindingValue */
		ASTBindingValue jjtn000 = new ASTBindingValue(JJTBINDINGVALUE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Collection() throws ParseException {/* @bgen(jjtree) Collection */
		ASTCollection jjtn000 = new ASTCollection(JJTCOLLECTION);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void HavingClause() throws ParseException {/* @bgen(jjtree) HavingClause */
		ASTHavingClause jjtn000 = new ASTHavingClause(JJTHAVINGCLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void MultiplicativeExpression() throws ParseException {
		UnaryExpression();
		label_30: while (true) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SolutionModifier() throws ParseException {
		if (((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) == GROUP) {
			GroupClause();
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void NumericLiteralNegative() throws ParseException {/* @bgen(jjtree) NumericLiteral */
		ASTNumericLiteral jjtn000 = new ASTNumericLiteral(JJTNUMERICLITERAL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PrefixDecl() throws ParseException {/* @bgen(jjtree) PrefixDecl */
		ASTPrefixDecl jjtn000 = new ASTPrefixDecl(JJTPREFIXDECL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void StrAfter() throws ParseException {/* @bgen(jjtree) StrAfter */
		ASTStrAfter jjtn000 = new ASTStrAfter(JJTSTRAFTER);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void EncodeForURI() throws ParseException {/* @bgen(jjtree) EncodeForURI */
		ASTEncodeForURI jjtn000 = new ASTEncodeForURI(JJTENCODEFORURI);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SHA512() throws ParseException {/* @bgen(jjtree) SHA512 */
		ASTSHA512 jjtn000 = new ASTSHA512(JJTSHA512);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	private int jj_la;
	private int jj_gen;
	final private int[] jj_la1 = new int[184];
	static private int[] jj_la1_0;
	static private int[] jj_la1_1;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PropertyListPath() throws ParseException {/* @bgen(jjtree) PropertyListPath */
		ASTPropertyListPath jjtn000 = new ASTPropertyListPath(JJTPROPERTYLISTPATH);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void FilterOrBind() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case FILTER: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void DeleteData() throws ParseException {/* @bgen(jjtree) DeleteData */
		ASTDeleteData jjtn000 = new ASTDeleteData(JJTDELETEDATA);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Filter() throws ParseException {
		jj_consume_token(FILTER);
		Constraint();
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Tz() throws ParseException {/* @bgen(jjtree) Tz */
		ASTTz jjtn000 = new ASTTz(JJTTZ);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Var() throws ParseException {/* @bgen(jjtree) Var */
		ASTVar jjtn000 = new ASTVar(JJTVAR);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	 * Disable tracing.
	 */
	final public void disable_tracing() {
	}

```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void CollectionPath() throws ParseException {/* @bgen(jjtree) Collection */
		ASTCollection jjtn000 = new ASTCollection(JJTCOLLECTION);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PathMod() throws ParseException {/* @bgen(jjtree) PathMod */
		ASTPathMod jjtn000 = new ASTPathMod(JJTPATHMOD);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public Token GroupGraphPattern() throws ParseException, Error {/* @bgen(jjtree) GraphPatternGroup */
		ASTGraphPatternGroup jjtn000 = new ASTGraphPatternGroup(JJTGRAPHPATTERNGROUP);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GroupConcat() throws ParseException {/* @bgen(jjtree) GroupConcat */
		ASTGroupConcat jjtn000 = new ASTGroupConcat(JJTGROUPCONCAT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void WhereClause() throws ParseException {/* @bgen(jjtree) WhereClause */
		ASTWhereClause jjtn000 = new ASTWhereClause(JJTWHERECLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	private int jj_gen;
	final private int[] jj_la1 = new int[184];
	static private int[] jj_la1_0;
	static private int[] jj_la1_1;
	static private int[] jj_la1_2;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void UnaryExpression() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case NOT: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ObjectList() throws ParseException {/* @bgen(jjtree) ObjectList */
		ASTObjectList jjtn000 = new ASTObjectList(JJTOBJECTLIST);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void DataBlock() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case VAR1:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	 * Enable tracing.
	 */
	final public void enable_tracing() {
	}

```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BuiltInCall() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case BOUND: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void InsertClause() throws ParseException {/* @bgen(jjtree) InsertClause */
		ASTInsertClause jjtn000 = new ASTInsertClause(JJTINSERTCLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void NumericFunction() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case RAND: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void IRI() throws ParseException {/* @bgen(jjtree) IRI */
		ASTIRI jjtn000 = new ASTIRI(JJTIRI);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void RegexExpression() throws ParseException {/* @bgen(jjtree) RegexExpression */
		ASTRegexExpression jjtn000 = new ASTRegexExpression(JJTREGEXEXPRESSION);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ConstTripleRef() throws ParseException {/* @bgen(jjtree) ConstTripleRef */
		ASTConstTripleRef jjtn000 = new ASTConstTripleRef(JJTCONSTTRIPLEREF);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void StrEnds() throws ParseException {/* @bgen(jjtree) StrEnds */
		ASTStrEnds jjtn000 = new ASTStrEnds(JJTSTRENDS);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Abs() throws ParseException {/* @bgen(jjtree) Abs */
		ASTAbs jjtn000 = new ASTAbs(JJTABS);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void LangMatches() throws ParseException {/* @bgen(jjtree) LangMatches */
		ASTLangMatches jjtn000 = new ASTLangMatches(JJTLANGMATCHES);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void NumericLiteral() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case INTEGER:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SHA256() throws ParseException {/* @bgen(jjtree) SHA256 */
		ASTSHA256 jjtn000 = new ASTSHA256(JJTSHA256);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Prolog() throws ParseException {
		label_1: while (true) {
			switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Year() throws ParseException {/* @bgen(jjtree) Year */
		ASTYear jjtn000 = new ASTYear(JJTYEAR);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ArgList() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case NIL:
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final private JJCalls[] jj_2_rtns = new JJCalls[7];
	private boolean jj_rescan = false;
	private int jj_gc = 0;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BasicGraphPattern() throws ParseException {/* @bgen(jjtree) BasicGraphPattern */
		ASTBasicGraphPattern jjtn000 = new ASTBasicGraphPattern(JJTBASICGRAPHPATTERN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Str() throws ParseException {/* @bgen(jjtree) Str */
		ASTStr jjtn000 = new ASTStr(JJTSTR);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void VerbSimple() throws ParseException {
		Var();
	}
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Clear() throws ParseException {/* @bgen(jjtree) Clear */
		ASTClear jjtn000 = new ASTClear(JJTCLEAR);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PathOneInPropertySet() throws ParseException {/* @bgen(jjtree) PathOneInPropertySet */
		ASTPathOneInPropertySet jjtn000 = new ASTPathOneInPropertySet(JJTPATHONEINPROPERTYSET);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void LimitOffsetClauses() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case LIMIT: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GraphGraphPattern() throws ParseException {/* @bgen(jjtree) GraphGraphPattern */
		ASTGraphGraphPattern jjtn000 = new ASTGraphGraphPattern(JJTGRAPHGRAPHPATTERN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SimpleBindingSet() throws ParseException {/* @bgen(jjtree) BindingSet */
		ASTBindingSet jjtn000 = new ASTBindingSet(JJTBINDINGSET);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void TriplesSameSubject() throws ParseException {/* @bgen(jjtree) TriplesSameSubject */
		ASTTriplesSameSubject jjtn000 = new ASTTriplesSameSubject(JJTTRIPLESSAMESUBJECT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SelectQuery() throws ParseException {/* @bgen(jjtree) SelectQuery */
		ASTSelectQuery jjtn000 = new ASTSelectQuery(JJTSELECTQUERY);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Bound() throws ParseException {/* @bgen(jjtree) Bound */
		ASTBound jjtn000 = new ASTBound(JJTBOUND);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Create() throws ParseException {/* @bgen(jjtree) Create */
		ASTCreate jjtn000 = new ASTCreate(JJTCREATE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void IsLiteral() throws ParseException {/* @bgen(jjtree) IsLiteral */
		ASTIsLiteral jjtn000 = new ASTIsLiteral(JJTISLITERAL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Construct() throws ParseException {/* @bgen(jjtree) Construct */
		ASTConstruct jjtn000 = new ASTConstruct(JJTCONSTRUCT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BlankNode() throws ParseException {/* @bgen(jjtree) BlankNode */
		ASTBlankNode jjtn000 = new ASTBlankNode(JJTBLANKNODE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PathElt() throws ParseException {/* @bgen(jjtree) PathElt */
		ASTPathElt jjtn000 = new ASTPathElt(JJTPATHELT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ConditionalOrExpression() throws ParseException {
		ConditionalAndExpression();
		if (((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) == OR) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ServiceGraphPattern() throws ParseException {/* @bgen(jjtree) ServiceGraphPattern */
		ASTServiceGraphPattern jjtn000 = new ASTServiceGraphPattern(JJTSERVICEGRAPHPATTERN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	static private final class LookaheadSuccess extends java.lang.Error {
		@Override
		public Throwable fillInStackTrace() {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GraphOrDefault() throws ParseException {/* @bgen(jjtree) GraphOrDefault */
		ASTGraphOrDefault jjtn000 = new ASTGraphOrDefault(JJTGRAPHORDEFAULT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void StrLang() throws ParseException {/* @bgen(jjtree) StrLang */
		ASTStrLang jjtn000 = new ASTStrLang(JJTSTRLANG);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public ASTUpdateContainer UpdateContainer() throws ParseException {/* @bgen(jjtree) UpdateContainer */
		ASTUpdateContainer jjtn000 = new ASTUpdateContainer(JJTUPDATECONTAINER);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
// grammar
//
	final public ASTUpdateSequence UpdateSequence() throws ParseException {/* @bgen(jjtree) UpdateSequence */
		ASTUpdateSequence jjtn000 = new ASTUpdateSequence(JJTUPDATESEQUENCE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Month() throws ParseException {/* @bgen(jjtree) Month */
		ASTMonth jjtn000 = new ASTMonth(JJTMONTH);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Quads() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case LPAREN:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Bind() throws ParseException {/* @bgen(jjtree) Bind */
		ASTBind jjtn000 = new ASTBind(JJTBIND);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SHA1() throws ParseException {/* @bgen(jjtree) SHA1 */
		ASTSHA1 jjtn000 = new ASTSHA1(JJTSHA1);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PathAlternative() throws ParseException {/* @bgen(jjtree) PathAlternative */
		ASTPathAlternative jjtn000 = new ASTPathAlternative(JJTPATHALTERNATIVE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GraphPatternNotTriples() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case OPTIONAL: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Aggregate() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case COUNT: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public ASTQueryContainer QueryContainer() throws ParseException {/* @bgen(jjtree) QueryContainer */
		ASTQueryContainer jjtn000 = new ASTQueryContainer(JJTQUERYCONTAINER);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ConstructClause() throws ParseException {
		jj_consume_token(LBRACE);
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Round() throws ParseException {/* @bgen(jjtree) Round */
		ASTRound jjtn000 = new ASTRound(JJTROUND);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	 * Trace enabled.
	 */
	final public boolean trace_enabled() {
		return trace_enabled;
	}
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	 * WHERE keyword is required in modify operations
	 */
	final public void ModifyWhereClause() throws ParseException {/* @bgen(jjtree) WhereClause */
		ASTWhereClause jjtn000 = new ASTWhereClause(JJTWHERECLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Hours() throws ParseException {/* @bgen(jjtree) Hours */
		ASTHours jjtn000 = new ASTHours(JJTHOURS);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void TriplesNodePath() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case LPAREN: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void StrLen() throws ParseException {/* @bgen(jjtree) StrLen */
		ASTStrLen jjtn000 = new ASTStrLen(JJTSTRLEN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void AskQuery() throws ParseException {/* @bgen(jjtree) AskQuery */
		ASTAskQuery jjtn000 = new ASTAskQuery(JJTASKQUERY);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ValueLogical() throws ParseException {
		RelationalExpression();
	}
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void TriplesSameSubjectPath() throws ParseException {/* @bgen(jjtree) TriplesSameSubjectPath */
		ASTTriplesSameSubjectPath jjtn000 = new ASTTriplesSameSubjectPath(JJTTRIPLESSAMESUBJECTPATH);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Datatype() throws ParseException {/* @bgen(jjtree) Datatype */
		ASTDatatype jjtn000 = new ASTDatatype(JJTDATATYPE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void NotIn() throws ParseException {/* @bgen(jjtree) NotIn */
		ASTNotIn jjtn000 = new ASTNotIn(JJTNOTIN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void UsingClause() throws ParseException {/* @bgen(jjtree) DatasetClause */
		ASTDatasetClause jjtn000 = new ASTDatasetClause(JJTDATASETCLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void UnaryMinus() throws ParseException {/* @bgen(jjtree) NumericLiteral */
		ASTNumericLiteral jjtn000 = new ASTNumericLiteral(JJTNUMERICLITERAL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Path() throws ParseException {
		PathAlternative();
	}
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GroupOrUnionGraphPattern() throws ParseException {
		GroupGraphPattern();
		if (((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) == UNION) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void DeleteClause() throws ParseException {/* @bgen(jjtree) DeleteClause */
		ASTDeleteClause jjtn000 = new ASTDeleteClause(JJTDELETECLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Contains() throws ParseException {/* @bgen(jjtree) Contains */
		ASTContains jjtn000 = new ASTContains(JJTCONTAINS);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void OrderClause() throws ParseException {/* @bgen(jjtree) OrderClause */
		ASTOrderClause jjtn000 = new ASTOrderClause(JJTORDERCLAUSE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void DateTimeFunction() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case NOW: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ProjectionElem() throws ParseException {/* @bgen(jjtree) ProjectionElem */
		ASTProjectionElem jjtn000 = new ASTProjectionElem(JJTPROJECTIONELEM);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void VarOrIRIref() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case VAR1:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void PathSequence() throws ParseException {/* @bgen(jjtree) PathSequence */
		ASTPathSequence jjtn000 = new ASTPathSequence(JJTPATHSEQUENCE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	static private final LookaheadSuccess jj_ls = new LookaheadSuccess();

	private boolean jj_scan_token(int kind) {
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	static private int[] jj_la1_3;
	static private int[] jj_la1_4;
	static private int[] jj_la1_5;

	static {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Now() throws ParseException {/* @bgen(jjtree) Now */
		ASTNow jjtn000 = new ASTNow(JJTNOW);
		jjtree.openNodeScope(jjtn000);
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Update() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case LOAD: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Sample() throws ParseException {/* @bgen(jjtree) Sample */
		ASTSample jjtn000 = new ASTSample(JJTSAMPLE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SHA384() throws ParseException {/* @bgen(jjtree) SHA384 */
		ASTSHA384 jjtn000 = new ASTSHA384(JJTSHA384);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void DescribeQuery() throws ParseException {/* @bgen(jjtree) DescribeQuery */
		ASTDescribeQuery jjtn000 = new ASTDescribeQuery(JJTDESCRIBEQUERY);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void IRIref() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case Q_IRI_REF: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void InlineData() throws ParseException {/* @bgen(jjtree) InlineData */
		ASTInlineData jjtn000 = new ASTInlineData(JJTINLINEDATA);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void UnparsedQuadDataBlock() throws ParseException {/* @bgen(jjtree) UnparsedQuadDataBlock */
		ASTUnparsedQuadDataBlock jjtn000 = new ASTUnparsedQuadDataBlock(JJTUNPARSEDQUADDATABLOCK);
		jjtree.openNodeScope(jjtn000);
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void IsNumeric() throws ParseException {/* @bgen(jjtree) IsNumeric */
		ASTIsNumeric jjtn000 = new ASTIsNumeric(JJTISNUMERIC);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void HashFunction() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case MD5: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Sum() throws ParseException {/* @bgen(jjtree) Sum */
		ASTSum jjtn000 = new ASTSum(JJTSUM);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void VarOrTerm() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case VAR1:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Lang() throws ParseException {/* @bgen(jjtree) Lang */
		ASTLang jjtn000 = new ASTLang(JJTLANG);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Describe() throws ParseException {/* @bgen(jjtree) Describe */
		ASTDescribe jjtn000 = new ASTDescribe(JJTDESCRIBE);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void InlineDataFull() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case NIL: {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void NotExistsFunc() throws ParseException {/* @bgen(jjtree) NotExistsFunc */
		ASTNotExistsFunc jjtn000 = new ASTNotExistsFunc(JJTNOTEXISTSFUNC);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Drop() throws ParseException {/* @bgen(jjtree) Drop */
		ASTDrop jjtn000 = new ASTDrop(JJTDROP);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void RelationalExpression() throws ParseException {
		NumericExpression();
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void InsertData() throws ParseException {/* @bgen(jjtree) InsertData */
		ASTInsertData jjtn000 = new ASTInsertData(JJTINSERTDATA);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void STRUUID() throws ParseException {/* @bgen(jjtree) STRUUID */
		ASTSTRUUID jjtn000 = new ASTSTRUUID(JJTSTRUUID);
		jjtree.openNodeScope(jjtn000);
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Select() throws ParseException {/* @bgen(jjtree) Select */
		ASTSelect jjtn000 = new ASTSelect(JJTSELECT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void NumericLiteralUnsigned() throws ParseException {/* @bgen(jjtree) NumericLiteral */
		ASTNumericLiteral jjtn000 = new ASTNumericLiteral(JJTNUMERICLITERAL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Verb() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case Q_IRI_REF:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void OptionalGraphPattern() throws ParseException {/* @bgen(jjtree) OptionalGraphPattern */
		ASTOptionalGraphPattern jjtn000 = new ASTOptionalGraphPattern(JJTOPTIONALGRAPHPATTERN);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	 * Get the specific Token.
	 */
	final public Token getToken(int index) {
		Token t = token;
		for (int i = 0; i < index; i++) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BNodeFunc() throws ParseException {/* @bgen(jjtree) BNodeFunc */
		ASTBNodeFunc jjtn000 = new ASTBNodeFunc(JJTBNODEFUNC);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void VerbPath() throws ParseException {
		Path();
	}
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void TriplesTemplate() throws ParseException {
		TriplesSameSubject();
		while (jj_2_3()) {
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void MD5() throws ParseException {/* @bgen(jjtree) MD5 */
		ASTMD5 jjtn000 = new ASTMD5(JJTMD5);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Count() throws ParseException {/* @bgen(jjtree) Count */
		ASTCount jjtn000 = new ASTCount(JJTCOUNT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java

	/// SPARQL 1.1 Update productions
	final public void GraphRefAll() throws ParseException {/* @bgen(jjtree) GraphRefAll */
		ASTGraphRefAll jjtn000 = new ASTGraphRefAll(JJTGRAPHREFALL);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void BlankNodePropertyList() throws ParseException {/* @bgen(jjtree) BlankNodePropertyList */
		ASTBlankNodePropertyList jjtn000 = new ASTBlankNodePropertyList(JJTBLANKNODEPROPERTYLIST);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Add() throws ParseException {/* @bgen(jjtree) Add */
		ASTAdd jjtn000 = new ASTAdd(JJTADD);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Constraint() throws ParseException {/* @bgen(jjtree) Constraint */
		ASTConstraint jjtn000 = new ASTConstraint(JJTCONSTRAINT);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void SameTerm() throws ParseException {/* @bgen(jjtree) SameTerm */
		ASTSameTerm jjtn000 = new ASTSameTerm(JJTSAMETERM);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void QuadsNotTriples() throws ParseException {/* @bgen(jjtree) QuadsNotTriples */
		ASTQuadsNotTriples jjtn000 = new ASTQuadsNotTriples(JJTQUADSNOTTRIPLES);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GraphPattern() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case LPAREN:
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void Day() throws ParseException {/* @bgen(jjtree) Day */
		ASTDay jjtn000 = new ASTDay(JJTDAY);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	static private int[] jj_la1_0;
	static private int[] jj_la1_1;
	static private int[] jj_la1_2;
	static private int[] jj_la1_3;
	static private int[] jj_la1_4;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void GraphNode() throws ParseException {
		switch ((jj_ntk == -1) ? jj_ntk_f() : jj_ntk) {
		case NIL:
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	static private int[] jj_la1_2;
	static private int[] jj_la1_3;
	static private int[] jj_la1_4;
	static private int[] jj_la1_5;

```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void UUID() throws ParseException {/* @bgen(jjtree) UUID */
		ASTUUID jjtn000 = new ASTUUID(JJTUUID);
		jjtree.openNodeScope(jjtn000);
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void ObjectListPath() throws ParseException {/* @bgen(jjtree) ObjectList */
		ASTObjectList jjtn000 = new ASTObjectList(JJTOBJECTLIST);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void InlineDataOneVar() throws ParseException {
		Var();
		jj_consume_token(LBRACE);
```

### MissortedModifiers
Missorted modifiers `final public`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	final public void String() throws ParseException {/* @bgen(jjtree) String */
		ASTString jjtn000 = new ASTString(JJTSTRING);
		boolean jjtc000 = true;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SequentialRecordCache.java`
#### Snippet
```java
	private final static AtomicLong TEMP_FILE_COUNTER = new AtomicLong();
	private final static String TEMP_FILE_PREFIX = "txncache" + UUID.randomUUID().toString().replace("-", "");
	private final static String TEMP_FILE_SUFFIX = ".dat";

	private static final EnumSet<StandardOpenOption> FILE_OPEN_OPTIONS = EnumSet.of(
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SequentialRecordCache.java`
#### Snippet
```java

	private final static AtomicLong TEMP_FILE_COUNTER = new AtomicLong();
	private final static String TEMP_FILE_PREFIX = "txncache" + UUID.randomUUID().toString().replace("-", "");
	private final static String TEMP_FILE_SUFFIX = ".dat";

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SequentialRecordCache.java`
#### Snippet
```java
final class SequentialRecordCache extends AbstractRecordCache {

	private final static AtomicLong TEMP_FILE_COUNTER = new AtomicLong();
	private final static String TEMP_FILE_PREFIX = "txncache" + UUID.randomUUID().toString().replace("-", "");
	private final static String TEMP_FILE_SUFFIX = ".dat";
```

### MissortedModifiers
Missorted modifiers `transient private final`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStoreRevision.java`
#### Snippet
```java
	 *-----------*/

	transient private final ValueStore valueStore;

	/*--------------*
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeStore.java`
#### Snippet
```java
	 * taking a pointer which might make it hard to get a phantom reference.
	 */
	final static class MemoryOverflowIntoNativeStore extends MemoryOverflowModel {
		private static final long serialVersionUID = 1L;

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultIO.java`
#### Snippet
```java
public class QueryResultIO {

	private final static ConcurrentCleaner cleaner = new ConcurrentCleaner();

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/native#namespaceIDCacheSize</var>
	 */
	public final static IRI NAMESPACE_ID_CACHE_SIZE;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/native#namespaceCacheSize</var>
	 */
	public final static IRI NAMESPACE_CACHE_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/native#valueIDCacheSize</var>
	 */
	public final static IRI VALUE_ID_CACHE_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/native#tripleIndexes</var>
	 */
	public final static IRI TRIPLE_INDEXES;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/native#forceSync</var>
	 */
	public final static IRI FORCE_SYNC;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreSchema.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/config/sail/native#valueCacheSize</var>
	 */
	public final static IRI VALUE_CACHE_SIZE;

	/**
```

### MissortedModifiers
Missorted modifiers `synchronized private`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/DataFile.java`
#### Snippet
```java
	}

	synchronized private void flush() throws IOException {
		int position = buffer.position();
		if (position == 0) {
```

### MissortedModifiers
Missorted modifiers `static private`
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/ReasoningUpdateBenchmark.java`
#### Snippet
```java
	public String param;

	static private final IRI schemaGraph = SimpleValueFactory.getInstance().createIRI("http://example.org/schemaGraph");

	@Benchmark
```

### MissortedModifiers
Missorted modifiers `synchronized private`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/IDFile.java`
#### Snippet
```java
	}

	synchronized private Long[] getCacheLine(int cacheLookupIndex) {
		if (cacheLookupIndex == gcReducingCacheIndex) {
			return gcReducingCache;
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/IDFile.java`
#### Snippet
```java
	}

	synchronized public void clearCache() {
		cache.invalidateAll();
		gcReducingCacheIndex = -1;
```

### MissortedModifiers
Missorted modifiers `static private`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
	}

	static private class HasBinding implements Predicate<BindingSet> {

		private final String variableName;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/EvaluationStatistics.java`
#### Snippet
```java
	// static UUID as prefix together with a thread safe incrementing long ensures a unique identifier.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

	private CardinalityCalculator calculator;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/EvaluationStatistics.java`
#### Snippet
```java

	// static UUID as prefix together with a thread safe incrementing long ensures a unique identifier.
	private final static String uniqueIdPrefix = UUID.randomUUID().toString().replace("-", "");
	private final static AtomicLong uniqueIdSuffix = new AtomicLong();

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/OrderComparator.java`
#### Snippet
```java
public class OrderComparator implements Comparator<BindingSet> {

	private final static Logger logger = LoggerFactory.getLogger(OrderComparator.class);

	private final ValueComparator cmp;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/TupleFunctionRegistry.java`
#### Snippet
```java
public class TupleFunctionRegistry extends ServiceRegistry<String, TupleFunction> {

	private final static TupleFunctionRegistry defaultRegistry = new TupleFunctionRegistry();

	/**
```

### MissortedModifiers
Missorted modifiers `synchronized public`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
	@Deprecated(forRemoval = true, since = "4.0.0")
	@Override
	synchronized public UUID getUUID() {
		if (uuid == null) {
			uuid = UUID.randomUUID();
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
public class DescribeIteration extends LookAheadIteration<BindingSet, QueryEvaluationException> {

	protected final static String VARNAME_SUBJECT = "subject";

	protected final static String VARNAME_PREDICATE = "predicate";
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
	protected final static String VARNAME_SUBJECT = "subject";

	protected final static String VARNAME_PREDICATE = "predicate";

	protected final static String VARNAME_OBJECT = "object";
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
	protected final static String VARNAME_PREDICATE = "predicate";

	protected final static String VARNAME_OBJECT = "object";

	private final List<String> describeExprNames;
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailConnection.java`
#### Snippet
```java
	 * the buffer that collects operations
	 */
	final private LuceneSailBuffer buffer;

	/**
```

### MissortedModifiers
Missorted modifiers `final private`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java
	 */

	final static private Logger logger = LoggerFactory.getLogger(LuceneSail.class);

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SESAMEQNAME.java`
#### Snippet
```java

	/** <var>http://www.openrdf.org/schema/qname#qname</var> */
	public final static IRI QNAME;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/QuerySpecBuilder.java`
#### Snippet
```java
public class QuerySpecBuilder implements SearchQueryInterpreter {

	private final static Logger logger = LoggerFactory.getLogger(QuerySpecBuilder.class);

	private final boolean incompleteQueryFails;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RSX.java`
#### Snippet
```java
	/** <var>http://rdf4j.org/shacl-extensions#targetShape</var> */
	public final static IRI targetShape = create("targetShape");
	public final static IRI dataGraph = create("dataGraph");
	public final static IRI shapesGraph = create("shapesGraph");
	public final static IRI valueConformsToXsdDatatypeFunction = create("valueConformsToXsdDatatypeFunction");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RSX.java`
#### Snippet
```java
	public final static IRI targetShape = create("targetShape");
	public final static IRI dataGraph = create("dataGraph");
	public final static IRI shapesGraph = create("shapesGraph");
	public final static IRI valueConformsToXsdDatatypeFunction = create("valueConformsToXsdDatatypeFunction");

```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RSX.java`
#### Snippet
```java
	public final static IRI dataGraph = create("dataGraph");
	public final static IRI shapesGraph = create("shapesGraph");
	public final static IRI valueConformsToXsdDatatypeFunction = create("valueConformsToXsdDatatypeFunction");

	private static IRI create(String localName) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RSX.java`
#### Snippet
```java

	/** <var>http://rdf4j.org/shacl-extensions#targetShape</var> */
	public final static IRI targetShape = create("targetShape");
	public final static IRI dataGraph = create("dataGraph");
	public final static IRI shapesGraph = create("shapesGraph");
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#topDataProperty */
	public final static IRI TOPDATAPROPERTY;

	/** http://www.w3.org/2002/07/owl#topObjectProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#qualifiedCardinality */
	public final static IRI QUALIFIEDCARDINALITY;

	/** http://www.w3.org/2002/07/owl#ReflexiveProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#onProperties */
	public final static IRI ONPROPERTIES;

	/** http://www.w3.org/2002/07/owl#propertyChainAxiom */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#sourceIndividual */
	public final static IRI SOURCEINDIVIDUAL;

	/** http://www.w3.org/2002/07/owl#targetIndividual */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#DeprecatedProperty */
	public final static IRI DEPRECATEDPROPERTY;

	/** http://www.w3.org/2002/07/owl#AnnotationProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#minCardinality */
	public final static IRI MINCARDINALITY;

	/** http://www.w3.org/2002/07/owl#maxCardinality */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#hasValue */
	public final static IRI HASVALUE;

	/** http://www.w3.org/2002/07/owl#disjointWith */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#inverseOf */
	public final static IRI INVERSEOF;

	/** http://www.w3.org/2002/07/owl#TransitiveProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#datatypeComplementOf */
	public final static IRI DATATYPECOMPLEMENTOF;

	/** http://www.w3.org/2002/07/owl#deprecated */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#equivalentClass */
	public final static IRI EQUIVALENTCLASS;

	/** http://www.w3.org/2002/07/owl#equivalentProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#annotatedSource */
	public final static IRI ANNOTATEDSOURCE;

	/** http://www.w3.org/2002/07/owl#annotatedTarget */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#AllDifferent */
	public final static IRI ALLDIFFERENT;

	/** http://www.w3.org/2002/07/owl#distinctMembers */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#DeprecatedClass */
	public final static IRI DEPRECATEDCLASS;

	/** http://www.w3.org/2002/07/owl#DeprecatedProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#differentFrom */
	public final static IRI DIFFERENTFROM;

	/** http://www.w3.org/2002/07/owl#AllDifferent */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#TransitiveProperty */
	public final static IRI TRANSITIVEPROPERTY;

	/** http://www.w3.org/2002/07/owl#SymmetricProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#hasSelf */
	public final static IRI HASSELF;

	/** http://www.w3.org/2002/07/owl#IrreflexiveProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#AllDisjointProperties */
	public final static IRI ALLDISJOINTPROPERTIES;

	/** http://www.w3.org/2002/07/owl#annotatedProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#allValuesFrom */
	public final static IRI ALLVALUESFROM;

	/** http://www.w3.org/2002/07/owl#someValuesFrom */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#DataRange */
	public final static IRI DATARANGE;

	/** http://www.w3.org/2002/07/owl#datatypeComplementOf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#propertyChainAxiom */
	public final static IRI PROPERTYCHAINAXIOM;

	/** http://www.w3.org/2002/07/owl#propertyDisjointWith */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#onDatatype */
	public final static IRI ONDATATYPE;

	/** http://www.w3.org/2002/07/owl#onProperties */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#intersectionOf */
	public final static IRI INTERSECTIONOF;

	/** http://www.w3.org/2002/07/owl#versionInfo */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#distinctMembers */
	public final static IRI DISTINCTMEMBERS;

	/** http://www.w3.org/2002/07/owl#ObjectProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#Annotation */
	public final static IRI ANNOTATION;

	/** http://www.w3.org/2002/07/owl#assertionProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#topObjectProperty */
	public final static IRI TOPOBJECTPROPERTY;

	/** http://www.w3.org/2002/07/owl#withRestrictions */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#equivalentProperty */
	public final static IRI EQUIVALENTPROPERTY;

	/** http://www.w3.org/2002/07/owl#sameAs */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#priorVersion */
	public final static IRI PRIORVERSION;

	/** http://www.w3.org/2002/07/owl#backwardCompatibleWith */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#backwardCompatibleWith */
	public final static IRI BACKWARDCOMPATIBLEWITH;

	/** http://www.w3.org/2002/07/owl#incompatibleWith */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#assertionProperty */
	public final static IRI ASSERTIONPROPERTY;

	/** http://www.w3.org/2002/07/owl#AsymmetricProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#complementOf */
	public final static IRI COMPLEMENTOF;

	// OWL 2
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#ObjectProperty */
	public final static IRI OBJECTPROPERTY;

	/** http://www.w3.org/2002/07/owl#DatatypeProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#Ontology */
	public final static IRI ONTOLOGY;

	/** http://www.w3.org/2002/07/owl#imports */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#DatatypeProperty */
	public final static IRI DATATYPEPROPERTY;

	/** http://www.w3.org/2002/07/owl#inverseOf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#propertyDisjointWith */
	public final static IRI PROPERTYDISJOINTWITH;

	/** http://www.w3.org/2002/07/owl#qualifiedCardinality */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#Axiom */
	public final static IRI AXIOM;

	/** http://www.w3.org/2002/07/owl#bottomDataProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#sameAs */
	public final static IRI SAMEAS;

	/** http://www.w3.org/2002/07/owl#differentFrom */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#FunctionalProperty */
	public final static IRI FUNCTIONALPROPERTY;

	/** http://www.w3.org/2002/07/owl#InverseFunctionalProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#targetIndividual */
	public final static IRI TARGETINDIVIDUAL;

	/** http://www.w3.org/2002/07/owl#targetValue */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#oneOf */
	public final static IRI ONEOF;

	/** http://www.w3.org/2002/07/owl#hasValue */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#someValuesFrom */
	public final static IRI SOMEVALUESFROM;

	/** http://www.w3.org/2002/07/owl#minCardinality */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java
	/** http://www.w3.org/2002/07/owl#Individual */
	@Deprecated
	public final static IRI INDIVIDUAL;

	/** http://www.w3.org/2002/07/owl#Thing */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#incompatibleWith */
	public final static IRI INCOMPATIBLEWITH;

	/** http://www.w3.org/2002/07/owl#DeprecatedClass */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#minQualifiedCardinality */
	public final static IRI MINQUALIFIEDCARDINALITY;

	/** http://www.w3.org/2002/07/owl#NamedIndividual */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#bottomObjectProperty */
	public final static IRI BOTTOMOBJECTPROPERTY;

	/** http://www.w3.org/2002/07/owl#DataRange */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#versionInfo */
	public final static IRI VERSIONINFO;

	/** http://www.w3.org/2002/07/owl#versionIRI */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#withRestrictions */
	public final static IRI WITHRESTRICTIONS;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#versionIRI */
	public final static IRI VERSIONIRI;

	/** http://www.w3.org/2002/07/owl#priorVersion */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#Restriction */
	public final static IRI RESTRICTION;

	/** http://www.w3.org/2002/07/owl#onProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#NamedIndividual */
	public final static IRI NAMEDINDIVIDUAL;

	/** http://www.w3.org/2002/07/owl#NegativePropertyAssertion */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#members */
	public final static IRI MEMBERS;

	/** http://www.w3.org/2002/07/owl#minQualifiedCardinality */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#hasKey */
	public final static IRI HASKEY;

	/** http://www.w3.org/2002/07/owl#hasSelf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#annotatedProperty */
	public final static IRI ANNOTATEDPROPERTY;

	/** http://www.w3.org/2002/07/owl#annotatedSource */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#unionOf */
	public final static IRI UNIONOF;

	/** http://www.w3.org/2002/07/owl#complementOf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#deprecated */
	public final static IRI DEPRECATED;

	/** http://www.w3.org/2002/07/owl#disjointUnionOf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#disjointWith */
	public final static IRI DISJOINTWITH;

	/** http://www.w3.org/2002/07/owl#unionOf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#NegativePropertyAssertion */
	public final static IRI NEGATIVEPROPERTYASSERTION;

	/** http://www.w3.org/2002/07/owl#onClass */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#onClass */
	public final static IRI ONCLASS;

	/** http://www.w3.org/2002/07/owl#onDataRange */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#Class */
	public final static IRI CLASS;

	/** http://www.w3.org/2002/07/owl#Individual */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#disjointUnionOf */
	public final static IRI DISJOINTUNIONOF;

	/** http://www.w3.org/2002/07/owl#hasKey */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#SymmetricProperty */
	public final static IRI SYMMETRICPROPERTY;

	/** http://www.w3.org/2002/07/owl#FunctionalProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#targetValue */
	public final static IRI TARGETVALUE;

	/** http://www.w3.org/2002/07/owl#topDataProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#annotatedTarget */
	public final static IRI ANNOTATEDTARGET;

	/** http://www.w3.org/2002/07/owl#Annotation */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#onDataRange */
	public final static IRI ONDATARANGE;

	/** http://www.w3.org/2002/07/owl#onDatatype */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#bottomDataProperty */
	public final static IRI BOTTOMDATAPROPERTY;

	/** http://www.w3.org/2002/07/owl#bottomObjectProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#AsymmetricProperty */
	public final static IRI ASYMMETRICPROPERTY;

	/** http://www.w3.org/2002/07/owl#Axiom */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#ReflexiveProperty */
	public final static IRI REFLEXIVEPROPERTY;

	/** http://www.w3.org/2002/07/owl#sourceIndividual */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#InverseFunctionalProperty */
	public final static IRI INVERSEFUNCTIONALPROPERTY;

	/** http://www.w3.org/2002/07/owl#Restriction */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#maxQualifiedCardinality */
	public final static IRI MAXQUALIFIEDCARDINALITY;

	/** http://www.w3.org/2002/07/owl#members */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#AllDisjointClasses */
	public final static IRI ALLDISJOINTCLASSES;

	/** http://www.w3.org/2002/07/owl#AllDisjointProperties */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#OntologyProperty */
	public final static IRI ONTOLOGYPROPERTY;

	// OWL DL and OWL Full
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#imports */
	public final static IRI IMPORTS;

	/** http://www.w3.org/2002/07/owl#intersectionOf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#onProperty */
	public final static IRI ONPROPERTY;

	/** http://www.w3.org/2002/07/owl#allValuesFrom */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#cardinality */
	public final static IRI CARDINALITY;

	/** http://www.w3.org/2002/07/owl#Ontology */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#AnnotationProperty */
	public final static IRI ANNOTATIONPROPERTY;

	/** http://www.w3.org/2002/07/owl#OntologyProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#IrreflexiveProperty */
	public final static IRI IRREFLEXIVEPROPERTY;

	/** http://www.w3.org/2002/07/owl#maxQualifiedCardinality */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java

	/** http://www.w3.org/2002/07/owl#maxCardinality */
	public final static IRI MAXCARDINALITY;

	/** http://www.w3.org/2002/07/owl#cardinality */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#unsignedInt</var> */
	public final static IRI UNSIGNED_INT = CoreDatatype.XSD.UNSIGNED_INT.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#unsignedShort</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#QName</var> */
	public final static IRI QNAME = CoreDatatype.XSD.QNAME.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#NOTATION</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#base64Binary</var> */
	public final static IRI BASE64BINARY = CoreDatatype.XSD.BASE64BINARY.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#hexBinary</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#ENTITY</var> */
	public final static IRI ENTITY = CoreDatatype.XSD.ENTITY.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#ENTITIES</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#integer</var> */
	public final static IRI INTEGER = CoreDatatype.XSD.INTEGER.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#long</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#NMTOKEN</var> */
	public final static IRI NMTOKEN = CoreDatatype.XSD.NMTOKEN.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#NMTOKENS</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#unsignedLong</var> */
	public final static IRI UNSIGNED_LONG = CoreDatatype.XSD.UNSIGNED_LONG.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#unsignedInt</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#duration</var> */
	public final static IRI DURATION = CoreDatatype.XSD.DURATION.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#dateTime</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#double</var> */
	public final static IRI DOUBLE = CoreDatatype.XSD.DOUBLE.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#anyURI</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#string</var> */
	public final static IRI STRING = CoreDatatype.XSD.STRING.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#boolean</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#date</var> */
	public final static IRI DATE = CoreDatatype.XSD.DATE.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#gYearMonth</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#dayTimeDuration</var> */
	public final static IRI DAYTIMEDURATION = CoreDatatype.XSD.DAYTIMEDURATION.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#time</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#unsignedShort</var> */
	public final static IRI UNSIGNED_SHORT = CoreDatatype.XSD.UNSIGNED_SHORT.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#unsignedByte</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#dateTimeStamp</var> */
	public final static IRI DATETIMESTAMP = CoreDatatype.XSD.DATETIMESTAMP.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#dayTimeDuration</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#byte</var> */
	public final static IRI BYTE = CoreDatatype.XSD.BYTE.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#nonPositiveInteger</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#nonNegativeInteger</var> */
	public final static IRI NON_NEGATIVE_INTEGER = CoreDatatype.XSD.NON_NEGATIVE_INTEGER.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#positiveInteger</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#Name</var> */
	public final static IRI NAME = CoreDatatype.XSD.NAME.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#NCName</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#NCName</var> */
	public final static IRI NCNAME = CoreDatatype.XSD.NCNAME.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#ID</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#short</var> */
	public final static IRI SHORT = CoreDatatype.XSD.SHORT.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#byte</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#hexBinary</var> */
	public final static IRI HEXBINARY = CoreDatatype.XSD.HEXBINARY.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#float</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#language</var> */
	public final static IRI LANGUAGE = CoreDatatype.XSD.LANGUAGE.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#NMTOKEN</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#positiveInteger</var> */
	public final static IRI POSITIVE_INTEGER = CoreDatatype.XSD.POSITIVE_INTEGER.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#unsignedLong</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#gMonth</var> */
	public final static IRI GMONTH = CoreDatatype.XSD.GMONTH.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#string</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#gDay</var> */
	public final static IRI GDAY = CoreDatatype.XSD.GDAY.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#gMonth</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#gYearMonth</var> */
	public final static IRI GYEARMONTH = CoreDatatype.XSD.GYEARMONTH.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#gYear</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java
	 * An immutable {@link Namespace} constant that represents the XML Schema namespace.
	 */
	public final static Namespace NS = Vocabularies.createNamespace(PREFIX, NAMESPACE);

	/*
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#yearMonthDuration</var> */
	public final static IRI YEARMONTHDURATION = CoreDatatype.XSD.YEARMONTHDURATION.getIri();

	private static IRI create(String localName) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#gMonthDay</var> */
	public final static IRI GMONTHDAY = CoreDatatype.XSD.GMONTHDAY.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#gDay</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#time</var> */
	public final static IRI TIME = CoreDatatype.XSD.TIME.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#date</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#anyURI</var> */
	public final static IRI ANYURI = CoreDatatype.XSD.ANYURI.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#QName</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java
	 * Recommended prefix for XML Schema datatypes: "xsd"
	 */
	public final static String PREFIX = "xsd";

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#ID</var> */
	public final static IRI ID = CoreDatatype.XSD.ID.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#IDREF</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#token</var> */
	public final static IRI TOKEN = CoreDatatype.XSD.TOKEN.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#language</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#decimal</var> */
	public final static IRI DECIMAL = CoreDatatype.XSD.DECIMAL.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#double</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#IDREF</var> */
	public final static IRI IDREF = CoreDatatype.XSD.IDREF.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#IDREFS</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#nonPositiveInteger</var> */
	public final static IRI NON_POSITIVE_INTEGER = CoreDatatype.XSD.NON_POSITIVE_INTEGER.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#negativeInteger</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#float</var> */
	public final static IRI FLOAT = CoreDatatype.XSD.FLOAT.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#decimal</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#NMTOKENS</var> */
	public final static IRI NMTOKENS = CoreDatatype.XSD.NMTOKENS.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#Name</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#unsignedByte</var> */
	public final static IRI UNSIGNED_BYTE = CoreDatatype.XSD.UNSIGNED_BYTE.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#yearMonthDuration</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#gYear</var> */
	public final static IRI GYEAR = CoreDatatype.XSD.GYEAR.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#gMonthDay</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#boolean</var> */
	public final static IRI BOOLEAN = CoreDatatype.XSD.BOOLEAN.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#base64Binary</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#NOTATION</var> */
	public final static IRI NOTATION = CoreDatatype.XSD.NOTATION.getIri();

	/*
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#IDREFS</var> */
	public final static IRI IDREFS = CoreDatatype.XSD.IDREFS.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#ENTITY</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#long</var> */
	public final static IRI LONG = CoreDatatype.XSD.LONG.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#int</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java
	 * The XML Schema namespace (<var>http://www.w3.org/2001/XMLSchema#</var>).
	 */
	public final static String NAMESPACE = CoreDatatype.XSD.NAMESPACE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#normalizedString</var> */
	public final static IRI NORMALIZEDSTRING = CoreDatatype.XSD.NORMALIZEDSTRING.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#token</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#ENTITIES</var> */
	public final static IRI ENTITIES = CoreDatatype.XSD.ENTITIES.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#integer</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#dateTime</var> */
	public final static IRI DATETIME = CoreDatatype.XSD.DATETIME.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#dateTimeStamp</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#int</var> */
	public final static IRI INT = CoreDatatype.XSD.INT.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#short</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	/** <var>http://www.w3.org/2001/XMLSchema#negativeInteger</var> */
	public final static IRI NEGATIVE_INTEGER = CoreDatatype.XSD.NEGATIVE_INTEGER.getIri();

	/** <var>http://www.w3.org/2001/XMLSchema#nonNegativeInteger</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#li */
	public final static IRI LI = Vocabularies.createIRI(RDF.NAMESPACE, "li");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#List */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Property */
	public final static IRI PROPERTY = Vocabularies.createIRI(RDF.NAMESPACE, "Property");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#XMLLiteral */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#type */
	public final static IRI TYPE = Vocabularies.createIRI(RDF.NAMESPACE, "type");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Property */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#List */
	public final static IRI LIST = Vocabularies.createIRI(RDF.NAMESPACE, "List");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#first */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#predicate */
	public final static IRI PREDICATE = Vocabularies.createIRI(RDF.NAMESPACE, "predicate");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#object */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#subject */
	public final static IRI SUBJECT = Vocabularies.createIRI(RDF.NAMESPACE, "subject");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#predicate */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#first */
	public final static IRI FIRST = Vocabularies.createIRI(RDF.NAMESPACE, "first");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#rest */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#XMLLiteral */
	public final static IRI XMLLITERAL = CoreDatatype.RDF.XMLLITERAL.getIri();

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#subject */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Statement */
	public final static IRI STATEMENT = Vocabularies.createIRI(RDF.NAMESPACE, "Statement");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Bag */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#object */
	public final static IRI OBJECT = Vocabularies.createIRI(RDF.NAMESPACE, "object");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Statement */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#value */
	public final static IRI VALUE = Vocabularies.createIRI(RDF.NAMESPACE, "value");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#li */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Bag */
	public final static IRI BAG = Vocabularies.createIRI(RDF.NAMESPACE, "Bag");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Alt */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#rest */
	public final static IRI REST = Vocabularies.createIRI(RDF.NAMESPACE, "rest");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#nil */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Alt */
	public final static IRI ALT = Vocabularies.createIRI(RDF.NAMESPACE, "Alt");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Seq */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#nil */
	public final static IRI NIL = Vocabularies.createIRI(RDF.NAMESPACE, "nil");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#langString */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#Seq */
	public final static IRI SEQ = Vocabularies.createIRI(RDF.NAMESPACE, "Seq");

	/** http://www.w3.org/1999/02/22-rdf-syntax-ns#value */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SESAME.java`
#### Snippet
```java

	/** <var>http://www.openrdf.org/schema/sesame#directType</var> */
	public final static IRI DIRECTTYPE;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SESAME.java`
#### Snippet
```java
	 * <var>http://www.openrdf.org/schema/sesame#wildcard</var>
	 */
	public final static IRI WILDCARD;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SESAME.java`
#### Snippet
```java

	/** <var>http://www.openrdf.org/schema/sesame#directSubClassOf</var> */
	public final static IRI DIRECTSUBCLASSOF;

	/** <var>http://www.openrdf.org/schema/sesame#directSubPropertyOf</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SESAME.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI NIL;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SESAME.java`
#### Snippet
```java

	/** <var>http://www.openrdf.org/schema/sesame#directSubPropertyOf</var> */
	public final static IRI DIRECTSUBPROPERTYOF;

	/** <var>http://www.openrdf.org/schema/sesame#directType</var> */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VANN.java`
#### Snippet
```java

	/** A reference to a resource that provides information on how this resource is to be used. */
	public final static IRI USAGE_NOTE;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VANN.java`
#### Snippet
```java

	/** The preferred namespace URI to use when using terms from this vocabulary in an XML document. */
	public final static IRI PREFERRED_NAMESPACE_URI;

	/** A group of related terms in a vocabulary. */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VANN.java`
#### Snippet
```java
	 * A reference to a resource that describes changes between this version of a vocabulary and the previous.
	 */
	public final static IRI CHANGES;

	/** A reference to a resource that provides an example of how this resource can be used. */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VANN.java`
#### Snippet
```java

	/** A group of related terms in a vocabulary. */
	public final static IRI TERM_GROUP;

	/** A reference to a resource that provides information on how this resource is to be used. */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VANN.java`
#### Snippet
```java

	/** The preferred namespace prefix to ue when using terms from this vocabulary in an XML document. */
	public final static IRI PREFERRED_NAMESPACE_PREFIX;

	/** The preferred namespace URI to use when using terms from this vocabulary in an XML document. */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VANN.java`
#### Snippet
```java

	/** A reference to a resource that provides an example of how this resource can be used. */
	public final static IRI EXAMPLE;

	/** The preferred namespace prefix to ue when using terms from this vocabulary in an XML document. */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI ERROR;

	public final static IRI IRI_TEMPLATE;

	public final static IRI IRI_TEMPLATE_MAPPING;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI VARIABLE_REPRESENTATION_PROP;

	public final static IRI VIEW;

	public final static IRI WRITABLE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI COLLECTION_PROP;

	public final static IRI DESCRIPTION;

	public final static IRI ENTRYPOINT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI RETURNS;

	public final static IRI RETURNS_HEADER;

	public final static IRI SEARCH;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI SUPPORTED_CLASS;

	public final static IRI SUPPORTED_OPERATION;

	public final static IRI SUPPORTED_PROPERTY_PROP;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI SEARCH;

	public final static IRI STATUS_CODE;

	public final static IRI SUPPORTED_CLASS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI PREVIOUS;

	public final static IRI PROPERTY;

	public final static IRI READABLE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI IRI_TEMPLATE;

	public final static IRI IRI_TEMPLATE_MAPPING;

	public final static IRI LINK;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI ENTRYPOINT;

	public final static IRI EXPECTS;

	public final static IRI EXPECTS_HEADER;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI API_DOCUMENTATION_PROP;

	public final static IRI COLLECTION_PROP;

	public final static IRI DESCRIPTION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java

	// ----- Classes ------
	public final static IRI API_DOCUMENTATION;

	public final static IRI CLASS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI RESOURCE;

	public final static IRI STATUS;

	public final static IRI SUPPORTED_PROPERTY;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI STATUS;

	public final static IRI SUPPORTED_PROPERTY;

	public final static IRI TEMPLATED_LINK;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI LAST;

	public final static IRI LIMIT;

	public final static IRI MAPPING;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI TOTAL_ITEMS;

	public final static IRI VARIABLE;

	public final static IRI VARIABLE_REPRESENTATION_PROP;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI TEMPLATED_LINK;

	public final static IRI VARIABLE_REPRESENTATION;

	// ----- Properties ------
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI PROPERTY;

	public final static IRI READABLE;

	public final static IRI REQUIRED;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI SUPPORTED_OPERATION;

	public final static IRI SUPPORTED_PROPERTY_PROP;

	public final static IRI TEMPLATE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI TITLE;

	public final static IRI TOTAL_ITEMS;

	public final static IRI VARIABLE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI OPERATION_PROP;

	public final static IRI PAGE_INDEX;

	public final static IRI PAGE_REFERENCE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI PAGE_REFERENCE;

	public final static IRI POSSIBLE_STATUS;

	public final static IRI PREVIOUS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI POSSIBLE_STATUS;

	public final static IRI PREVIOUS;

	public final static IRI PROPERTY;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI MEMBER;

	public final static IRI METHOD;

	public final static IRI NEXT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI OPERATION;

	public final static IRI PARTIAL_COLLECTION_VIEW;

	public final static IRI RESOURCE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI PARTIAL_COLLECTION_VIEW;

	public final static IRI RESOURCE;

	public final static IRI STATUS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI FREETEXT_QUERY;

	public final static IRI LAST;

	public final static IRI LIMIT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI OFFSET;

	public final static IRI OPERATION_PROP;

	public final static IRI PAGE_INDEX;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI READABLE;

	public final static IRI REQUIRED;

	public final static IRI RETURNS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI REQUIRED;

	public final static IRI RETURNS;

	public final static IRI RETURNS_HEADER;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI IRI_TEMPLATE_MAPPING;

	public final static IRI LINK;

	public final static IRI OPERATION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI RETURNS_HEADER;

	public final static IRI SEARCH;

	public final static IRI STATUS_CODE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI PAGE_INDEX;

	public final static IRI PAGE_REFERENCE;

	public final static IRI POSSIBLE_STATUS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI STATUS_CODE;

	public final static IRI SUPPORTED_CLASS;

	public final static IRI SUPPORTED_OPERATION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI MAPPING;

	public final static IRI MEMBER;

	public final static IRI METHOD;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI TEMPLATE;

	public final static IRI TITLE;

	public final static IRI TOTAL_ITEMS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI VIEW;

	public final static IRI WRITABLE;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI COLLECTION;

	public final static IRI ERROR;

	public final static IRI IRI_TEMPLATE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI EXPECTS;

	public final static IRI EXPECTS_HEADER;

	public final static IRI FIRST;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI API_DOCUMENTATION;

	public final static IRI CLASS;

	public final static IRI COLLECTION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI CLASS;

	public final static IRI COLLECTION;

	public final static IRI ERROR;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI SUPPORTED_PROPERTY_PROP;

	public final static IRI TEMPLATE;

	public final static IRI TITLE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java

	// ----- Properties ------
	public final static IRI API_DOCUMENTATION_PROP;

	public final static IRI COLLECTION_PROP;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI DESCRIPTION;

	public final static IRI ENTRYPOINT;

	public final static IRI EXPECTS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI NEXT;

	public final static IRI OFFSET;

	public final static IRI OPERATION_PROP;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI LIMIT;

	public final static IRI MAPPING;

	public final static IRI MEMBER;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI LINK;

	public final static IRI OPERATION;

	public final static IRI PARTIAL_COLLECTION_VIEW;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI FIRST;

	public final static IRI FREETEXT_QUERY;

	public final static IRI LAST;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI EXPECTS_HEADER;

	public final static IRI FIRST;

	public final static IRI FREETEXT_QUERY;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI METHOD;

	public final static IRI NEXT;

	public final static IRI OFFSET;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI VARIABLE;

	public final static IRI VARIABLE_REPRESENTATION_PROP;

	public final static IRI VIEW;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
	public final static IRI SUPPORTED_PROPERTY;

	public final static IRI TEMPLATED_LINK;

	public final static IRI VARIABLE_REPRESENTATION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI KNOWS;

	public final static IRI LAST_NAME;

	public final static IRI LOGO;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI FAMILYNAME;

	public final static IRI FIRST_NAME;

	public final static IRI FOCUS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI WORKPLACE_HOMEPAGE;

	public final static IRI YAHOO_CHAT_ID;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI LOGO;

	public final static IRI MADE;

	public final static IRI MAKER;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI BASED_NEAR;

	public final static IRI BIRTHDAY;

	public final static IRI CURRENT_PROJECT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI SKYPE_ID;

	public final static IRI STATUS;

	public final static IRI SURNAME;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI LABEL_PROPERTY;

	public final static IRI ONLINE_ACCOUNT;

	public final static IRI ONLINE_CHAT_ACCOUNT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI STATUS;

	public final static IRI SURNAME;

	public final static IRI THEME;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI GIVENNAME;

	public final static IRI HOLDS_ACCOUNT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI FUNDED_BY;

	public final static IRI GEEKCODE;

	public final static IRI GENDER;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI FOCUS;

	public final static IRI FUNDED_BY;

	public final static IRI GEEKCODE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI PHONE;

	public final static IRI PLAN;

	public final static IRI PRIMARY_TOPIC;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI PAST_PROJECT;

	public final static IRI PHONE;

	public final static IRI PLAN;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI INTEREST;

	public final static IRI IS_PRIMARY_TOPIC_OF;

	public final static IRI JABBER_ID;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI FIRST_NAME;

	public final static IRI FOCUS;

	public final static IRI FUNDED_BY;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI SHA1;

	public final static IRI SKYPE_ID;

	public final static IRI STATUS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI DOCUMENT;

	public final static IRI GROUP;

	public final static IRI IMAGE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI HOMEPAGE;

	public final static IRI ICQ_CHAT_ID;

	public final static IRI IMG;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI TOPIC;

	public final static IRI TOPIC_INTEREST;

	public final static IRI WEBLOG;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI GROUP;

	public final static IRI IMAGE;

	public final static IRI LABEL_PROPERTY;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI AGE;

	public final static IRI AIM_CHAT_ID;

	public final static IRI BASED_NEAR;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI LAST_NAME;

	public final static IRI LOGO;

	public final static IRI MADE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI PLAN;

	public final static IRI PRIMARY_TOPIC;

	public final static IRI PUBLICATIONS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI PERSON;

	public final static IRI PERSONAL_PROFILE_DOCUMENT;

	public final static IRI PROJECT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI TIPJAR;

	public final static IRI TITLE;

	public final static IRI TOPIC;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI GIVENNAME;

	public final static IRI HOLDS_ACCOUNT;

	public final static IRI HOMEPAGE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ACCOUNT;

	public final static IRI ACCOUNT_NAME;

	public final static IRI ACCOUNT_SERVICE_HOMEPAGE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI IMG;

	public final static IRI INTEREST;

	public final static IRI IS_PRIMARY_TOPIC_OF;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI PRIMARY_TOPIC;

	public final static IRI PUBLICATIONS;

	public final static IRI SCHOOL_HOMEPAGE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI MADE;

	public final static IRI MAKER;

	public static final IRI MBOX;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI NAME;

	public final static IRI NICK;

	public final static IRI OPENID;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI BIRTHDAY;

	public final static IRI CURRENT_PROJECT;

	public final static IRI DEPICTION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI GENDER;

	public final static IRI GIVEN_NAME;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ONLINE_CHAT_ACCOUNT;

	public final static IRI ONLINE_ECOMMERCE_ACCOUNT;

	public final static IRI ONLINE_GAMING_ACCOUNT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI WORK_INFO_HOMEPAGE;

	public final static IRI WORKPLACE_HOMEPAGE;

	public final static IRI YAHOO_CHAT_ID;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public static final IRI MYERS_BRIGGS;

	public final static IRI NAME;

	public final static IRI NICK;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI JABBER_ID;

	public final static IRI KNOWS;

	public final static IRI LAST_NAME;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI IMAGE;

	public final static IRI LABEL_PROPERTY;

	public final static IRI ONLINE_ACCOUNT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ACCOUNT_NAME;

	public final static IRI ACCOUNT_SERVICE_HOMEPAGE;

	public final static IRI AGE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ONLINE_ACCOUNT;

	public final static IRI ONLINE_CHAT_ACCOUNT;

	public final static IRI ONLINE_ECOMMERCE_ACCOUNT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI THEME;

	public final static IRI THUMBNAIL;

	public final static IRI TIPJAR;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java

	// ----- Properties ------
	public final static IRI ACCOUNT;

	public final static IRI ACCOUNT_NAME;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ICQ_CHAT_ID;

	public final static IRI IMG;

	public final static IRI INTEREST;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI WEBLOG;

	public final static IRI WORK_INFO_HOMEPAGE;

	public final static IRI WORKPLACE_HOMEPAGE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI SURNAME;

	public final static IRI THEME;

	public final static IRI THUMBNAIL;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI TOPIC_INTEREST;

	public final static IRI WEBLOG;

	public final static IRI WORK_INFO_HOMEPAGE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI NICK;

	public final static IRI OPENID;

	public final static IRI PAGE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI HOLDS_ACCOUNT;

	public final static IRI HOMEPAGE;

	public final static IRI ICQ_CHAT_ID;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI PAGE;

	public final static IRI PAST_PROJECT;

	public final static IRI PHONE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI DEPICTION;

	public final static IRI DEPICTS;

	public final static IRI DNA_CHECKSUM;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI TITLE;

	public final static IRI TOPIC;

	public final static IRI TOPIC_INTEREST;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ONLINE_GAMING_ACCOUNT;

	public final static IRI ORGANIZATION;

	public final static IRI PERSON;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI SCHOOL_HOMEPAGE;

	public final static IRI SHA1;

	public final static IRI SKYPE_ID;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI CURRENT_PROJECT;

	public final static IRI DEPICTION;

	public final static IRI DEPICTS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI AIM_CHAT_ID;

	public final static IRI BASED_NEAR;

	public final static IRI BIRTHDAY;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI PERSONAL_PROFILE_DOCUMENT;

	public final static IRI PROJECT;

	// ----- Properties ------
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI DEPICTS;

	public final static IRI DNA_CHECKSUM;

	public final static IRI FAMILY_NAME;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI GEEKCODE;

	public final static IRI GENDER;

	public final static IRI GIVEN_NAME;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI FAMILYNAME;

	public final static IRI FIRST_NAME;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java

	// ----- Classes ------
	public final static IRI AGENT;

	public final static IRI DOCUMENT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI IS_PRIMARY_TOPIC_OF;

	public final static IRI JABBER_ID;

	public final static IRI KNOWS;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ORGANIZATION;

	public final static IRI PERSON;

	public final static IRI PERSONAL_PROFILE_DOCUMENT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI PUBLICATIONS;

	public final static IRI SCHOOL_HOMEPAGE;

	public final static IRI SHA1;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ONLINE_ECOMMERCE_ACCOUNT;

	public final static IRI ONLINE_GAMING_ACCOUNT;

	public final static IRI ORGANIZATION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI AGENT;

	public final static IRI DOCUMENT;

	public final static IRI GROUP;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI ACCOUNT_SERVICE_HOMEPAGE;

	public final static IRI AGE;

	public final static IRI AIM_CHAT_ID;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI THUMBNAIL;

	public final static IRI TIPJAR;

	public final static IRI TITLE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI OPENID;

	public final static IRI PAGE;

	public final static IRI PAST_PROJECT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	public final static IRI DNA_CHECKSUM;

	public final static IRI FAMILY_NAME;

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI TESTRESULT;

	public final static IRI OUTCOME;

	public final static IRI SOFTWARE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI PASS;

	public final static IRI FAIL;

	public final static IRI CANNOTTELL;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	// Outcome values

	public final static IRI PASS;

	public final static IRI FAIL;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI ASSERTOR;

	public final static IRI ASSERTION;

	public final static IRI ASSERTEDBY;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI TEST;

	public final static IRI TEST_SUBJECT;

	public final static IRI RESULT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI RESULT;

	public final static IRI MODE;

	public final static IRI TESTRESULT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI ASSERTION;

	public final static IRI ASSERTEDBY;

	public final static IRI SUBJECT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI MODE;

	public final static IRI TESTRESULT;

	public final static IRI OUTCOME;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI TEST_SUBJECT;

	public final static IRI RESULT;

	public final static IRI MODE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI AUTOMATIC;

	public final static IRI SEMIAUTOMATIC;

	public final static IRI NOTAVAILABLE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI FAIL;

	public final static IRI CANNOTTELL;

	public final static IRI NOTAPPLICABLE;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	// Test modes

	public final static IRI MANUAL;

	public final static IRI AUTOMATIC;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI MANUAL;

	public final static IRI AUTOMATIC;

	public final static IRI SEMIAUTOMATIC;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI OUTCOME;

	public final static IRI SOFTWARE;

	// Outcome values
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI NOTAVAILABLE;

	public final static IRI HEURISTIC;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public static final String PREFIX = "earl";

	public final static IRI ASSERTOR;

	public final static IRI ASSERTION;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI SUBJECT;

	public final static IRI TEST;

	public final static IRI TEST_SUBJECT;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI SEMIAUTOMATIC;

	public final static IRI NOTAVAILABLE;

	public final static IRI HEURISTIC;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI NOTAPPLICABLE;

	public final static IRI NOTTESTED;

	// Test modes
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI CANNOTTELL;

	public final static IRI NOTAPPLICABLE;

	public final static IRI NOTTESTED;
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
	public final static IRI ASSERTEDBY;

	public final static IRI SUBJECT;

	public final static IRI TEST;
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/dao/ArtistDao.java`
#### Snippet
```java
	}

	static abstract class QUERY_KEYS {
		public static final String ARTISTS_WITHOUT_PAINTINGS = "artists-without-paintings";
	}
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#subPropertyOf */
	public final static IRI SUBPROPERTYOF;

	/** http://www.w3.org/2000/01/rdf-schema#domain */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#ContainerMembershipProperty */
	public final static IRI CONTAINERMEMBERSHIPPROPERTY;

	static {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#seeAlso */
	public final static IRI SEEALSO;

	/** http://www.w3.org/2000/01/rdf-schema#ContainerMembershipProperty */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#comment */
	public final static IRI COMMENT;

	/** http://www.w3.org/2000/01/rdf-schema#label */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#isDefinedBy */
	public final static IRI ISDEFINEDBY;

	/** http://www.w3.org/2000/01/rdf-schema#seeAlso */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#Resource */
	public final static IRI RESOURCE;

	/** http://www.w3.org/2000/01/rdf-schema#Literal */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#Datatype */
	public final static IRI DATATYPE;

	/** http://www.w3.org/2000/01/rdf-schema#Container */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#subClassOf */
	public final static IRI SUBCLASSOF;

	/** http://www.w3.org/2000/01/rdf-schema#subPropertyOf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#Container */
	public final static IRI CONTAINER;

	/** http://www.w3.org/2000/01/rdf-schema#member */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#member */
	public final static IRI MEMBER;

	/** http://www.w3.org/2000/01/rdf-schema#isDefinedBy */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#Class */
	public final static IRI CLASS;

	/** http://www.w3.org/2000/01/rdf-schema#subClassOf */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#label */
	public final static IRI LABEL;

	/** http://www.w3.org/2000/01/rdf-schema#Datatype */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#Literal */
	public final static IRI LITERAL;

	/** http://www.w3.org/2000/01/rdf-schema#Class */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#domain */
	public final static IRI DOMAIN;

	/** http://www.w3.org/2000/01/rdf-schema#range */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java

	/** http://www.w3.org/2000/01/rdf-schema#range */
	public final static IRI RANGE;

	/** http://www.w3.org/2000/01/rdf-schema#comment */
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF4J.java`
#### Snippet
```java
	 * @since 3.3.2
	 */
	public final static IRI NIL = create("nil");

	private static IRI create(String localName) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF4J.java`
#### Snippet
```java
	 */
	public final static IRI SHACL_SHAPE_GRAPH = create("SHACLShapeGraph");
	public final static IRI TRUNCATED = create("truncated");

	/**
```

### MissortedModifiers
Missorted modifiers `final static`
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF4J.java`
#### Snippet
```java
	 * <var>http://rdf4j.org/schema/rdf4j#SHACLShapeGraph</var>
	 */
	public final static IRI SHACL_SHAPE_GRAPH = create("SHACLShapeGraph");
	public final static IRI TRUNCATED = create("truncated");

```

## RuleId[id=NegativeIntConstantInLongContext]
### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC32.java`
#### Snippet
```java
	@Override
	public void reset() {
		value = 0xFFFFFFFF;
	}
}
```

## RuleId[id=RedundantStreamOptionalCall]
### RedundantStreamOptionalCall
Redundant 'map()' call
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java

								// now we only have conflicts
								.map(statement -> {
									// TODO handle conflict. Probably by doing something to change to id, mark it as a
									// conflict. Store all the conflicts in memory, to check against and refresh them
									// from
									// disc when we boot

									return statement;

								})
								.collect(Collectors.toSet());

```

## RuleId[id=WhileLoopSpinsOnField]
### WhileLoopSpinsOnField
`while` loop spins on field
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnBufferedPlanNode.java`
#### Snippet
```java

			private void calculateNext() {
				while (next == null) {
					boolean success = parent.incrementIterator();
					if (!success) {
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailConcurrencyTest.java`
#### Snippet
```java
			try {
				try (SailConnection connection = store.getConnection()) {
					while (continueRunning) {
						connection.begin();
						for (int i = 0; i < 10; i++) {
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailConcurrencyTest.java`
#### Snippet
```java
			try {
				try (SailConnection connection = store.getConnection()) {
					while (continueRunning) {
						try (CloseableIteration<? extends Resource, SailException> contextIter = connection
								.getContextIDs()) {
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailClosingIteration.java`
#### Snippet
```java
			}
			if (!extraOrdinaryExceptions.isEmpty()) {
				throw new UndeclaredThrowableException(extraOrdinaryExceptions.get(0));
			}
		}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
				// Try not to clobber the initial exception that may be more useful
				if (!foundException) {
					throw new QueryEvaluationException(e);
				}
			}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbStatementIterator.java`
#### Snippet
```java
				recordIt.close();
			} catch (IOException e) {
				throw causeIOException(e);
			}
		}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java
						while (!asyncTransactionFinished) {
							if (tripleStoreException != null) {
								throw wrapTripleStoreException();
							} else {
								Thread.yield();
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java
					while (!opQueue.add(ROLLBACK_TRANSACTION)) {
						if (tripleStoreException != null) {
							throw wrapTripleStoreException();
						} else {
							Thread.yield();
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
					} finally {
						if (!exceptions.isEmpty()) {
							throw new UndeclaredThrowableException(exceptions.get(0));
						}
					}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
					Thread.currentThread().interrupt();
				}
				throw convert(e);
			}
		}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
				}
				if (!collectedExceptions.isEmpty()) {
					throw new UndeclaredThrowableException(collectedExceptions.get(0));
				}
			} finally {
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeStatementIterator.java`
#### Snippet
```java
				btreeIter.close();
			} catch (IOException e) {
				throw causeIOException(e);
			}
		}
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/HasValueConstraintComponent.java`
#### Snippet
```java
		}

		if (scope == Scope.propertyShape) {
			Path path = getTargetChain().getPath().get();

```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedURI.java`
#### Snippet
```java
		if (_parseScheme(uri)) {
			// A scheme was found; _scheme and _schemeSpecificPart are now set
			if (_schemeSpecificPart.startsWith("/")) {
				// Hierachical URI
				String rest = _schemeSpecificPart;
```

## RuleId[id=SimplifyOptionalCallChains]
### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LanguageInFilter.java`
#### Snippet
```java

		Optional<String> language = ((Literal) t.getValue()).getLanguage();
		if (!language.isPresent()) {
			return false;
		}
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NonUniqueTargetLang.java`
#### Snippet
```java
				Optional<String> lang = ((Literal) value).getLanguage();

				if (!lang.isPresent()) {
					next = null;
				} else if (!seenLanguages.contains(lang.get())) {
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/UniqueLangConstraintComponent.java`
#### Snippet
```java
		Optional<Path> path = getTargetChain().getPath();

		if (!path.isPresent() || scope != Scope.propertyShape) {
			throw new IllegalStateException("UniqueLang only operates on paths");
		}
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/RDFParserHelper.java`
#### Snippet
```java
				// Removes datatype for langString datatype with no language tag when VERIFY_DATATYPE_VALUES is False.
				if ((workingDatatype == null || RDF.LANGSTRING.equals(workingDatatype))
						&& (!workingLang.isPresent() || workingLang.get().isEmpty())
						&& !parserConfig.get(BasicParserSettings.VERIFY_DATATYPE_VALUES)) {
					workingLang = Optional.ofNullable(null);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/Model.java`
#### Snippet
```java
	default Namespace setNamespace(String prefix, String name) {
		Optional<? extends Namespace> result = getNamespace(prefix);
		if (!result.isPresent() || !result.get().getName().equals(name)) {
			result = Optional.of(new ModelNamespace(prefix, name));
			setNamespace(result.get());
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
		// Because we have previously already checked that the models are the same size, we don't have to check both
		// ways to establish model equality.
		return !missingInModel2.isPresent();
	}

```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalRDFParser.java`
#### Snippet
```java
			// In RDF-1.1, RDF-1.0 Plain Literals are now Typed Literals with
			// type xsd:String
			if (!literal.getLanguage().isPresent() && datatype == null) {
				datatype = XSD.STRING.stringValue();
			}
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/string/Contains.java`
#### Snippet
```java

			if (leftLit.getLanguage().isPresent()) {
				if (!rightLit.getLanguage().isPresent() || rightLit.getLanguage().equals(leftLit.getLanguage())) {

					String leftLexVal = leftLit.getLabel();
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
		// we reject literals that aren't in the list of the indexed lang
		if (indexedLangs != null
				&& (!literal.getLanguage().isPresent()
						|| !indexedLangs.contains(literal.getLanguage().get().toLowerCase()
						))) {
```

## RuleId[id=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java
		}

		if (super.getBaseSail().getDataDir() != null) {
			String path = super.getBaseSail().getDataDir().getPath();
			if (path.endsWith("/")) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java

		if (super.getBaseSail().getDataDir() != null) {
			String path = super.getBaseSail().getDataDir().getPath();
			if (path.endsWith("/")) {
				path = path.substring(0, path.length() - 1);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Regex.java`
#### Snippet
```java

	public void setPatternArg(ValueExpr rightArg) {
		super.setRightArg(rightArg);
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Regex.java`
#### Snippet
```java

	public void setArg(ValueExpr leftArg) {
		super.setLeftArg(leftArg);
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Regex.java`
#### Snippet
```java

	public ValueExpr getPatternArg() {
		return super.getRightArg();
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Regex.java`
#### Snippet
```java

	public ValueExpr getArg() {
		return super.getLeftArg();
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
		Object shape = null;
		try {
			shape = super.parseLuceneQueryShape(SearchFields.getPropertyField(geoProperty), wkt);
		} catch (java.text.ParseException e) {
			logger.error("error while parsing wkt geometry", e);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
			}

			return super.resolveURI(uri);
		}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
		final IRI[] removeContexts = getRemoveContexts();
		if (isAllContext(contexts) && removeContexts.length == 1) {
			super.remove(new ConvertingIteration<Statement, Statement, E>(statementIter) {

				@Override
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
		final IRI insertContext = getInsertContext();
		if (isNilContext(contexts)) {
			super.add(new ConvertingIteration<Statement, Statement, E>(statementIter) {

				@Override
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/WildcardProjectionProcessor.java`
#### Snippet
```java
			if (first instanceof ASTTripleRef) {
				ASTTripleRef triple = (ASTTripleRef) first;
				super.visit(triple, data);
			}
			Node aliasNode = node.jjtGetChild(1);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTQueryContainer.java`
#### Snippet
```java

	public ASTQuery getQuery() {
		return super.jjtGetChild(ASTQuery.class);
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTOperationContainer.java`
#### Snippet
```java

	public ASTOperation getOperation() {
		return super.jjtGetChild(ASTOperation.class);
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTOperationContainer.java`
#### Snippet
```java

	public List<ASTPrefixDecl> getPrefixDeclList() {
		return super.jjtGetChildren(ASTPrefixDecl.class);
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTOperationContainer.java`
#### Snippet
```java

	public ASTBaseDecl getBaseDecl() {
		return super.jjtGetChild(ASTBaseDecl.class);
	}

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ConstantOptimizer.java`
#### Snippet
```java
		@Override
		public void meet(Regex node) {
			super.meetNode(node);

			if (isConstant(node.getArg()) && isConstant(node.getPatternArg()) && isConstant(node.getFlagsArg())) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
	@Override
	public BindingSet next() throws QueryEvaluationException {
		if (!super.hasIterator()) {
			super.setIterator(createIterator());
		}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
	public BindingSet next() throws QueryEvaluationException {
		if (!super.hasIterator()) {
			super.setIterator(createIterator());
		}
		return super.next();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
	@Override
	public boolean hasNext() throws QueryEvaluationException {
		if (!super.hasIterator()) {
			super.setIterator(createIterator());
		}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
	public boolean hasNext() throws QueryEvaluationException {
		if (!super.hasIterator()) {
			super.setIterator(createIterator());
		}
		return super.hasNext();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
		Shape shape = null;
		try {
			shape = super.parseQueryShape(SearchFields.getPropertyField(geoProperty), wkt);
		} catch (ParseException e) {
			logger.error("error while parsing wkt geometry", e);
```

## RuleId[id=JUnitMalformedDeclaration]
### JUnitMalformedDeclaration
Method `suite` should be non-private, static and have no parameters
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxTest.java`
#### Snippet
```java
	}

	public static Test suite(Factory factory, boolean useRemote) throws Exception {
		// manifest of W3C Data Access Working Group SPARQL syntax tests
		final File tmpDir;
```

### JUnitMalformedDeclaration
Method `suite` should be non-private, static and have no parameters
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	}

	public static TestSuite suite(String manifestFileURL, Factory factory, boolean approvedOnly) throws Exception {
		LOGGER.info("Building test suite for {}", manifestFileURL);

```

### JUnitMalformedDeclaration
Method `suite` should be non-private, static and have no parameters
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
	}

	public static TestSuite suite(String manifestFileURL, Factory factory) throws Exception {
		return suite(manifestFileURL, factory, true);
	}
```

### JUnitMalformedDeclaration
Method `suite` should be non-private, static and have no parameters
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
	}

	public static TestSuite suite(String manifestFileURL, Factory factory, boolean approvedOnly) throws Exception {
		logger.info("Building test suite for {}", manifestFileURL);

```

### JUnitMalformedDeclaration
Method `suite` should be non-private, static and have no parameters
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLUpdateConformanceTest.java`
#### Snippet
```java
	}

	public static TestSuite suite(String manifestFileURL, Factory factory) throws Exception {
		return suite(manifestFileURL, factory, true);
	}
```

## RuleId[id=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/config/BaseSailConfig.java`
#### Snippet
```java

			Models.objectLiteral(graph.getStatements(implNode, EVALUATION_STRATEGY_FACTORY, null))
					.ifPresent(factoryClassName -> {
						setEvaluationStrategyFactoryClassName(factoryClassName.stringValue());
					});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/PropertyShape.java`
#### Snippet
```java
			if (!(constraintComponent instanceof PropertyShape)) {
				validationPlanNode = new ValidationReportNode(validationPlanNode, t -> {
					return new ValidationResult(t.getActiveTarget(), t.getValue(), this,
							constraintComponent.getConstraintComponent(), getSeverity(), t.getScope(), t.getContexts(),
							getContexts());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ValidationQuery.java`
#### Snippet
```java

		return new ValidationReportNode(select, t -> {
			return new ValidationResult(t.getActiveTarget(), t.getValue(), shape,
					constraintComponent_validationReport, severity, t.getScope(), t.getContexts(), shapesGraphs);
		});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/NodeShape.java`
#### Snippet
```java

				validationPlanNode = new ValidationReportNode(validationPlanNode, t -> {
					return new ValidationResult(t.getActiveTarget(), t.getActiveTarget(), this,
							constraintComponent.getConstraintComponent(), getSeverity(), t.getScope(), t.getContexts(),
							getContexts());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetNode.java`
#### Snippet
```java
	@Override
	public void toModel(Resource subject, IRI predicate, Model model, Set<Resource> cycleDetection) {
		targetNodes.forEach(t -> {
			model.add(subject, getPredicate(), t);
		});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
			return collect.stream().flatMap(s -> {
				List<Shape> temp = new ArrayList<>();
				s.target.forEach(target -> {
					s.constraintComponents.forEach(constraintComponent -> {

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
		}

		target.forEach(t -> {
			t.toModel(getId(), null, model, cycleDetection);
		});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetSubjectsOf.java`
#### Snippet
```java
	@Override
	public void toModel(Resource subject, IRI predicate, Model model, Set<Resource> cycleDetection) {
		targetSubjectsOf.forEach(t -> {
			model.add(subject, getPredicate(), t);
		});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetObjectsOf.java`
#### Snippet
```java
	@Override
	public void toModel(Resource subject, IRI predicate, Model model, Set<Resource> cycleDetection) {
		targetObjectsOf.forEach(t -> {
			model.add(subject, getPredicate(), t);
		});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTuple.java`
#### Snippet
```java

			return compressedTuples.stream().flatMap(t1 -> {
				return t1.pop()
						.stream()
						.map(t -> new ValidationTuple(t.validationResults, t.chain, t.scope,
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/DashHasValueInConstraintComponent.java`
#### Snippet
```java

			PlanNode invalidTargets = new GroupByFilter(joined, group -> {
				return group.stream().map(ValidationTuple::getValue).noneMatch(hasValueIn::contains);
			});

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/HasValueConstraintComponent.java`
#### Snippet
```java

			PlanNode invalidTargets = new GroupByFilter(joined, group -> {
				return group.stream().map(ValidationTuple::getValue).noneMatch(v -> hasValue.equals(v));
			});

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/ClosedConstraintComponent.java`
#### Snippet
```java

		paths = property.stream().flatMap(r -> {
			return shapeSource.getObjects(r, ShapeSource.Predicates.PATH)
					.map(o -> ((Resource) o))
					.map(path -> Path.buildPath(shapeSource, path));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
						})
						.collect(Collectors.toList())
						.forEach(builderAndSha -> {

							bulkRequest.add(clientProvider.getClient()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
		do {

			deleteStatementBuffer.forEach(statement -> {

				bulkRequest.add(clientProvider.getClient()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
			logger.debug("Add inferred rdfs:subClassOf statements");

			calculatedTypes.forEach((subClass, superClasses) -> {
				superClasses.forEach(superClass -> {
					conn.addInferredStatementInternal(subClass, RDFS.SUBCLASSOF, superClass, DEFAULT_CONTEXT);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java

			calculatedTypes.forEach((subClass, superClasses) -> {
				superClasses.forEach(superClass -> {
					conn.addInferredStatementInternal(subClass, RDFS.SUBCLASSOF, superClass, DEFAULT_CONTEXT);
				});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
			logger.debug("Add inferred rdfs:subPropertyOf statements");

			calculatedProperties.forEach((sub, sups) -> {
				sups.forEach(sup -> {
					conn.addInferredStatementInternal(sub, RDFS.SUBPROPERTYOF, sup, DEFAULT_CONTEXT);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java

			calculatedProperties.forEach((sub, sups) -> {
				sups.forEach(sup -> {
					conn.addInferredStatementInternal(sub, RDFS.SUBPROPERTYOF, sup, DEFAULT_CONTEXT);
				});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/RDF4JTemplate.java`
#### Snippet
```java
	public void deleteTriplesWithSubject(IRI id) {
		consumeConnection(
				con -> {
					con.remove(id, null, null);
				});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
		}
		if (pingDelay > 0) {
			ping = pingScheduler.schedule(() -> {
				executeTransactionPing();
			}, pingDelay, TimeUnit.MILLISECONDS);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	public Set<String> getRepositoryIDs() throws RepositoryException {
		Set<String> idSet = new LinkedHashSet<>();
		getAllRepositoryInfos().forEach(info -> {
			idSet.add(info.getId());
		});
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
		final GeoPoint srcPoint = new GeoPoint(lat, lon);
		return Iterables.transform(hits, (Function<SearchHit, DocumentDistance>) hit -> {
			return new ElasticsearchDocumentDistance(hit, geoContextMapper, fieldName, units, srcPoint, unit);
		});
	}
```

## RuleId[id=EmptyMethod]
### EmptyMethod
Method only calls its super
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ResourceUtil.java`
#### Snippet
```java

		@Override
		protected Class<?>[] getClassContext() {
			return super.getClassContext();
		}
```

### EmptyMethod
The method is empty
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
	 */
	@Deprecated
	public void initialize() {
	}

```

### EmptyMethod
All implementations of this method are empty
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/QueryContextInitializer.java`
#### Snippet
```java
	 * @param ctx
	 */
	void destroy(QueryContext ctx);
}

```

### EmptyMethod
Method only calls its super
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/UnionScopeChangeOptimizer.java`
#### Snippet
```java

		@Override
		public void meet(Projection subselect) {
			// do not check deeper in the tree
			return;
```

### EmptyMethod
The method is empty
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
	 * Utility methods to clear-up in case not using in-memory cache.
	 */
	protected void disposeCache(Iterator<BindingSet> iter) {
	}

```

### EmptyMethod
The method is empty
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
	 * Utility methods to clear-up in case not using in-memory hash table.
	 */
	protected void disposeHashTable(Map<BindingSetHashKey, List<BindingSet>> map) {
	}

```

### EmptyMethod
The method is empty
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java
	 * Utility methods to clear-up in case not using in-memory hash table.
	 */
	protected <E> void closeHashValue(Iterator<E> iter) {
	}

```

### EmptyMethod
The method is empty
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
	}

	protected void increment() throws QueryEvaluationException {
		// give subclasses a chance to stop query evaluation
	}
```

### EmptyMethod
The method is empty
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
	}

	protected void decrement(int amount) throws QueryEvaluationException {
		// let subclasses know that the expected result size is smaller
	}
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/BNodeGenerator.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/BindingSetAssignment.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/DeleteData.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/EmptySet.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/InsertData.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/ProjectionElem.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
All implementations of this method are empty
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/QueryModelVisitor.java`
#### Snippet
```java

	@Deprecated(forRemoval = true)
	void meet(Like node) throws X;

	void meet(Load load) throws X;
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/SingletonSet.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/ValueConstant.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
Empty method overrides empty method
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Var.java`
#### Snippet
```java

	@Override
	public <X extends Exception> void visitChildren(QueryModelVisitor<X> visitor) throws X {
		// no-op
	}
```

### EmptyMethod
The method is empty
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractSimpleQueryModelVisitor.java`
#### Snippet
```java
	 * @param node The node that is being visited.
	 */
	protected void meetUpdateExpr(UpdateExpr node) throws X {

	}
```

### EmptyMethod
All implementations of this method are empty
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/Node.java`
#### Snippet
```java
	 * added to it.
	 */
	void jjtOpen();

	/**
```

### EmptyMethod
All implementations of this method are empty
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/Node.java`
#### Snippet
```java
	 * This method is called after all the child nodes have been added.
	 */
	void jjtClose();

	/**
```

### EmptyMethod
The method is empty
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	 * Enable tracing.
	 */
	final public void enable_tracing() {
	}

```

### EmptyMethod
The method is empty
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	 * Disable tracing.
	 */
	final public void disable_tracing() {
	}

```

### EmptyMethod
The method is empty
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
	}

	void SkipLexicalActions(Token matchedToken) {
	}

```

### EmptyMethod
The method is empty
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
	}

	void TokenLexicalActions(Token matchedToken) {
	}

```

### EmptyMethod
Method only calls its super
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/TokenMgrError.java`
#### Snippet
```java
	 */
	@Override
	public String getMessage() {
		return super.getMessage();
	}
```

### EmptyMethod
The method is empty
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
	}

	public void done() {
		// no-op
	}
```

### EmptyMethod
All implementations of this method are empty
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/evaluationstatistics/DynamicStatistics.java`
#### Snippet
```java
	void remove(ExtensibleStatement statement);

	void removeByQuery(Resource subj, IRI pred, Value obj, boolean inferred, Resource... contexts);

	/**
```

### EmptyMethod
The method is empty
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ContextStore.java`
#### Snippet
```java
	}

	void close() {
	}

```

### EmptyMethod
The method is empty
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/NamespaceStore.java`
#### Snippet
```java
	}

	public void close() {
	}

```

### EmptyMethod
The method is empty
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ContextStore.java`
#### Snippet
```java
	}

	void close() {
	}

```

### EmptyMethod
The method is empty
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NamespaceStore.java`
#### Snippet
```java
	}

	public void close() {
	}

```

### EmptyMethod
Method only calls its super
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailBaseConfiguration.java`
#### Snippet
```java

	@Override
	public IsolationLevel getDefaultIsolationLevel() {
		return super.getDefaultIsolationLevel();
	}
```

### EmptyMethod
Method only calls its super
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/GroupGraphPattern.java`
#### Snippet
```java

	@Override
	public boolean isEmpty() {
		return super.isEmpty();
	}
```

### EmptyMethod
The method is empty
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/RDF4JCRUDDao.java`
#### Snippet
```java
	 * Callback invoked after a successful insert/update.
	 */
	protected void postProcessUpdate(INPUT input, Map<String, Value> bindings) {
		// empty default implementation
	}
```

### EmptyMethod
Method only calls its super
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java
	@BeforeAll
	@Override
	public void setUp() throws Exception {
		super.setUp();
	}
```

### EmptyMethod
Method only calls its super
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java
	@AfterAll
	@Override
	public void tearDown() throws Exception {
		super.tearDown();
	}
```

## RuleId[id=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/TripleSourceIterationWrapper.java`
#### Snippet
```java

	private final CloseableIteration<? extends T, SailException> delegate;
	private boolean closed = false;

	public TripleSourceIterationWrapper(CloseableIteration<? extends T, SailException> delegate) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/binary/src/main/java/org/eclipse/rdf4j/rio/binary/BinaryRDFWriter.java`
#### Snippet
```java
	private final Map<Value, ValueMeta> valueMeta;

	private int nextId = 0;

	private final Queue<Integer> idPool;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLRepository.java`
#### Snippet
```java
	 * @see #enableQuadMode(boolean)
	 */
	private boolean quadMode = false;
	/**
	 * The HTTP client that takes care of the client-server communication.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLCrossProductIteration.java`
#### Snippet
```java
	protected final CloseableIteration<BindingSet, QueryEvaluationException> resultIteration;

	protected Iterator<BindingSet> inputBindingsIterator = null;

	protected BindingSet currentInputBinding = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/SPARQLCrossProductIteration.java`
#### Snippet
```java
	protected Iterator<BindingSet> inputBindingsIterator = null;

	protected BindingSet currentInputBinding = null;

	public SPARQLCrossProductIteration(CloseableIteration<BindingSet, QueryEvaluationException> resultIteration,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
	 * @deprecated Use {@link AbstractCloseableIteration#isClosed()} instead.
	 */
	protected volatile boolean closed = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
	 * @deprecated Use {@link #isFinished()} instead.
	 */
	protected volatile boolean finished = false;

	protected final QueueCursor<CloseableIteration<T, QueryEvaluationException>> rightQueue = new QueueCursor<>(1024);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailBaseConfiguration.java`
#### Snippet
```java
	private long validationResultsLimitPerConstraint = ShaclSailConfig.VALIDATION_RESULTS_LIMIT_PER_CONSTRAINT_DEFAULT;
	private long transactionalValidationLimit = ShaclSailConfig.TRANSACTIONAL_VALIDATION_LIMIT_DEFAULT;
	private boolean logValidationExecution = false;
	private Set<IRI> shapesGraphs = ShaclSailConfig.SHAPES_GRAPHS_DEFAULT;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSail.java`
#### Snippet
```java
		private final Supplier<ExecutorService> supplier;
		ExecutorService delegate;
		boolean initialized = false;

		public RevivableExecutorService(Supplier<ExecutorService> supplier) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/RepositoryFederatedService.java`
#### Snippet
```java
	protected boolean shutDown;

	private RepositoryConnection managedConn = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclProperties.java`
#### Snippet
```java
	private boolean uniqueLang = false;

	boolean closed = false;
	private Resource ignoredProperties;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclProperties.java`
#### Snippet
```java

	private final List<String> pattern = new ArrayList<>();
	private String flags = null;

	private final Set<Resource> targetClass = new HashSet<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclProperties.java`
#### Snippet
```java
	private final List<Resource> target = new ArrayList<>();

	private boolean deactivated = false;

	private boolean uniqueLang = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclProperties.java`
#### Snippet
```java
	private boolean deactivated = false;

	private boolean uniqueLang = false;

	boolean closed = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/StatementMatcher.java`
#### Snippet
```java

		// Best effort to store the highest value of all counters
		private static volatile int max = 0;

		private int counter = -1;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	RdfsSubClassOfReasoner rdfsSubClassOfReasoner;

	private boolean prepareHasBeenCalled = false;

	private Lock exclusiveSerializableValidationLock;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java

	private boolean shapeRefreshNeeded = false;
	private boolean shapesModifiedInCurrentTransaction = false;

	public final ShaclSail sail;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	private final HashSet<Statement> removedStatementsSet = new HashSet<>();

	private boolean shapeRefreshNeeded = false;
	private boolean shapesModifiedInCurrentTransaction = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
	private boolean connectionListenerActive = false;

	private IsolationLevel currentIsolationLevel = null;

	private Settings transactionSettings;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java
		private Boolean parallelValidation;
		private IsolationLevel isolationLevel;
		transient private Settings previous = null;

		@Deprecated(since = "4.0.0", forRemoval = true)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java

	// used to determine if we are currently registered as a connection listener (getting added/removed notifications)
	private boolean connectionListenerActive = false;

	private IsolationLevel currentIsolationLevel = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SingletonBindingSet.java`
#### Snippet
```java

	private Binding cachedBinding;
	private int cachedHashCode = 0;

	public SingletonBindingSet(String bindingName, Value value) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SimpleBindingSet.java`
#### Snippet
```java
	private final Binding[] bindings;

	private int cachedHashCode = 0;

	public SimpleBindingSet(Set<String> bindingNamesSet, List<String> varNamesList, List<Value> values) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TargetChainPopper.java`
#### Snippet
```java
	private final PlanNode parent;
	private StackTraceElement[] stackTrace;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedSplitter.java`
#### Snippet
```java
		private final BufferedSplitter bufferedSplitter;
		public final boolean cached;
		private boolean printed = false;

		private ValidationExecutionLogger validationExecutionLogger;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnionNode.java`
#### Snippet
```java
	private StackTraceElement[] stackTrace;
	private final PlanNode[] nodes;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToPropertyShape.java`
#### Snippet
```java
	private StackTraceElement[] stackTrace;
	PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
	}

	boolean printed = false;

	@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
			}

			boolean closed = false;

			@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationReportNode.java`
#### Snippet
```java
	private final Function<ValidationTuple, ValidationResult> validationResultFunction;
	PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
	private final PlanNode right;
	private final boolean useAsFilter;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToNodeShape.java`
#### Snippet
```java
	private StackTraceElement[] stackTrace;
	PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TrimToTarget.java`
#### Snippet
```java
	private StackTraceElement[] stackTrace;
	PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TrimToTarget.java`
#### Snippet
```java
	private ValidationExecutionLogger validationExecutionLogger;

	boolean keepPath = false;

	public TrimToTarget(PlanNode parent) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
	final PlanNode parent;
	private final On on;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;
	private final Resource[] dataGraph;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
		return new LoggingCloseableIteration(this, validationExecutionLogger) {

			ValidationTuple next = null;

			final CloseableIteration<? extends ValidationTuple, SailException> parentIterator = parent.iterator();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
			final CloseableIteration<? extends ValidationTuple, SailException> parentIterator = parent.iterator();

			List<IRI> filterOnPredicates = null;

			void calculateNext() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TupleMapper.java`
#### Snippet
```java
	PlanNode parent;
	Function<ValidationTuple, ValidationTuple> function;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
	private final PlanNode left;
	private final PlanNode right;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
	private final Dataset dataset;
	private StackTraceElement[] stackTrace;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
		return new LoggingCloseableIteration(this, validationExecutionLogger) {

			CloseableIteration<? extends BindingSet, QueryEvaluationException> bindingSet = null;

			private void init() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
			Iterator<ValidationTuple> sortedTuplesIterator;

			boolean closed = false;

			@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java

	private final PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
		PushablePlanNode delegate;

		boolean recentlyPushed = false;

		public NotifyingPushablePlanNode(PushablePlanNode delegate) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
	static private final Logger logger = LoggerFactory.getLogger(InnerJoin.class);
	private StackTraceElement[] stackTrace;
	private boolean printed = false;

	private final PlanNode left;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java
	private final Function<Long, Boolean> filter;
	PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
	private final boolean useAsFilter;
	private StackTraceElement[] stackTrace;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LoggingCloseableIteration.java`
#### Snippet
```java
	private final ValidationExecutionLogger validationExecutionLogger;
	private final PlanNode planNode;
	private boolean empty = false;
	private boolean closed;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Unique.java`
#### Snippet
```java

	private final PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NotValuesIn.java`
#### Snippet
```java
	private final PlanNode parent;
	private final PlanNode notIn;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java
	private final Function<Collection<ValidationTuple>, Boolean> filter;
	PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ReduceTargets.java`
#### Snippet
```java
	private final PlanNode parentToReduce;
	private final PlanNode reductionSource;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
			final CloseableIteration<? extends ValidationTuple, SailException> parentIterator = parent.iterator();

			Resource[] filterOnObject = null;
			IRI filterOnPredicate = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
	private final boolean returnMatching;
	private StackTraceElement[] stackTrace;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;
	private final Resource[] dataGraph;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
		return new LoggingCloseableIteration(this, validationExecutionLogger) {

			ValidationTuple next = null;

			final CloseableIteration<? extends ValidationTuple, SailException> parentIterator = parent.iterator();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java

			Resource[] filterOnObject = null;
			IRI filterOnPredicate = null;

			void calculateNext() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NonUniqueTargetLang.java`
#### Snippet
```java
public class NonUniqueTargetLang implements PlanNode {
	PlanNode parent;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EmptyNode.java`
#### Snippet
```java
public class EmptyNode implements PlanNode {

	private boolean printed = false;

	static final private EmptyNode instance = new EmptyNode();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java
	private final Function<Statement, ValidationTuple> mapper;

	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
	private final Dataset dataset;
	private final Resource[] dataGraph;
	private TupleExpr parsedQuery = null;
	private final boolean skipBasedOnPreviousConnection;
	private final SailConnection previousStateConnection;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
	private final SailConnection previousStateConnection;
	private final String query;
	private boolean printed = false;

	public BulkedExternalInnerJoin(PlanNode leftNode, SailConnection connection, Resource[] dataGraph, String query,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
	private TupleExpr parsedQuery;
	private final String query;
	private boolean printed = false;

	public BulkedExternalLeftOuterJoin(PlanNode leftNode, SailConnection connection, Resource[] dataGraph,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValuesBackedNode.java`
#### Snippet
```java

	private final ConstraintComponent.Scope scope;
	boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/ValidationReport.java`
#### Snippet
```java
public class ValidationReport {

	protected Resource id = null;

	protected boolean conforms = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/ValidationReport.java`
#### Snippet
```java

	protected final List<ValidationResult> validationResult = new ArrayList<>();
	protected boolean truncated = false;

	public ValidationReport() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java
	private long counter = 0;
	private boolean conforms = true;
	private boolean truncated = false;

	private Iterator<ValidationResult> next = Collections.emptyIterator();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/results/lazy/ValidationResultIterator.java`
#### Snippet
```java

	private final long limit;
	private long counter = 0;
	private boolean conforms = true;
	private boolean truncated = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
	private final ConstraintComponent.Scope scope;
	private StackTraceElement[] stackTrace;
	private boolean printed = false;
	private ValidationExecutionLogger validationExecutionLogger;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
			List<ValidationTuple> bulk = new ArrayList<>(bulkSize);

			TupleExpr parsedQuery = null;

			public void calculateNext() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/SingletonClientProvider.java`
#### Snippet
```java

	transient private Client client;
	private transient boolean closed = false;
	private final String hostname;
	private final int port;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchHelper.java`
#### Snippet
```java

			SearchHit next;
			boolean empty = false;

			private void calculateNext() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchHelper.java`
#### Snippet
```java
			Iterator<SearchHit> items;
			String scrollId;
			long itemsRetrieved = 0;
			final int size = 1000;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/rio/trix/src/main/java/org/eclipse/rdf4j/rio/trix/TriXWriter.java`
#### Snippet
```java

	private boolean convertRDFStar;
	private Resource currentContext = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/rio/trix/src/main/java/org/eclipse/rdf4j/rio/trix/TriXWriter.java`
#### Snippet
```java
	private final XMLWriter xmlWriter;

	private boolean inActiveContext = false;

	private boolean convertRDFStar;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionSAXParser.java`
#### Snippet
```java
	private List<Binding> bindings;

	private SPARQLUpdateOperation currentSPARQLUpdate = null;

	private SimpleDataset currentDataset;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/AbstractAggregateOperator.java`
#### Snippet
```java
	private static final long serialVersionUID = 4016064683034358205L;

	private boolean distinct = false;

	protected AbstractAggregateOperator(ValueExpr arg) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Var.java`
#### Snippet
```java
	private QueryModelNode parent;

	private int cachedHashCode = 0;

	@Deprecated(forRemoval = true, since = "4.1.0")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Var.java`
#### Snippet
```java
	private Value value;

	private boolean anonymous = false;

	private boolean constant = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Var.java`
#### Snippet
```java
	private boolean anonymous = false;

	private boolean constant = false;

	private QueryModelNode parent;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Projection.java`
#### Snippet
```java
	private ProjectionElemList projElemList;

	private Var projectionContext = null;

	private boolean subquery = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/BNodeGenerator.java`
#### Snippet
```java
public class BNodeGenerator extends AbstractQueryModelNode implements ValueExpr {

	private ValueExpr nodeIdExpr = null;

	/*--------------*
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/QueryModelTreeToGenericPlanNode.java`
#### Snippet
```java
public class QueryModelTreeToGenericPlanNode extends AbstractQueryModelVisitor<RuntimeException> {

	GenericPlanNode top = null;
	QueryModelNode topTupleExpr;
	ArrayDeque<GenericPlanNode> planNodes = new ArrayDeque<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/QueryModelTreePrinter.java`
#### Snippet
```java
	private final StringBuilder sb;

	private int indentLevel = 0;

	/*--------------*
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/list/Index.java`
#### Snippet
```java
				new Iterator<List<Value>>() {

					int pos = 0;

					@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/Split.java`
#### Snippet
```java
				SingleValueToListTransformer.transform(new Iterator<>() {

					int pos = 0;

					@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/DirectTypeHierarchyInferencer.java`
#### Snippet
```java
		 * Flag indicating whether an update of the inferred statements is needed.
		 */
		private boolean updateNeeded = false;

		public DirectTypeHierarchyInferencerConnection(InferencerConnection con) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/CustomGraphQueryInferencer.java`
#### Snippet
```java
		 * Flag indicating whether an update of the inferred statements is needed.
		 */
		private boolean updateNeeded = false;

		private Connection(InferencerConnection con) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencer.java`
#### Snippet
```java
	// Inferred statements can either be added to the default context
	// or to the context that the original inserted statement has
	private boolean addInferredStatementsToDefaultContext = false;
	private volatile boolean unmodifiable;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencerConnection.java`
#### Snippet
```java
	private boolean schemaChange;

	private long addedInferredStatementsCount = 0;

	SchemaCachingRDFSInferencerConnection(SchemaCachingRDFSInferencer sail, InferencerConnection connection) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/SchemaCachingRDFSInferencerConnection.java`
#### Snippet
```java
	}

	private boolean inferredCleared = false;

	@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbRecordIterator.java`
#### Snippet
```java
	private final long[] quad = new long[4];

	private boolean fetchNext = false;

	private final StampedLock txnLock;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbRecordIterator.java`
#### Snippet
```java
	private final int dbi;

	private boolean closed = false;

	private final MDBVal keyData;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java
		private final T[] elements;
		private volatile int head = 0;
		private volatile int tail = 0;

		CircularBuffer(int size) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java
	 */
	abstract static class StatefulOperation implements Operation {
		volatile boolean finished = false;
	}

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java

		private final T[] elements;
		private volatile int head = 0;
		private volatile int tail = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ConcurrentCache.java`
#### Snippet
```java
	private final int capacity;

	private volatile int cleanupTick = 0;

	protected final ConcurrentHashMap<K, V> cache;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/model/LmdbBNode.java`
#### Snippet
```java
	private volatile long internalID;

	private volatile boolean initialized = false;

	/*--------------*
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbStore.java`
#### Snippet
```java

	// indicates if a datadir is temporary (i.e. will be deleted on shutdown)
	private boolean isTmpDatadir = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/model/LmdbIRI.java`
#### Snippet
```java
	private volatile long internalID;

	private volatile boolean initialized = false;

	/*--------------*
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/model/LmdbLiteral.java`
#### Snippet
```java
	private volatile long internalID;

	private volatile boolean initialized = false;

	/*--------------*
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreConfig.java`
#### Snippet
```java
	private long valueDBSize = -1;

	private boolean forceSync = false;

	private int valueCacheSize = -1;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
	private File dataDir;

	private volatile Boolean compatibleMode = null;

	private volatile Map<String, String> additionalHttpHeaders = Collections.emptyMap();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	private long baseline = 0;

	private long maxBlockSize = 0;

	SimpleValueFactory vf = SimpleValueFactory.getInstance();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/MemoryOverflowModel.java`
#### Snippet
```java
	transient SailSourceModel disk;

	private long baseline = 0;

	private long maxBlockSize = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
	private final Pool pool = new Pool();

	private TxnRecordCache recordCache = null;

	static final Comparator<ByteBuffer> COMPARATOR = new Comparator<ByteBuffer>() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/TimeLimitRDFHandler.java`
#### Snippet
```java
	private final InterruptTask interruptTask;

	private volatile boolean isInterrupted = false;

	private final AtomicBoolean ended = new AtomicBoolean(false);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/CodePointIterator.java`
#### Snippet
```java
	private final String source;

	private int index = 0;

	public CodePointIterator(String source) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/SimpleParseLocationListener.java`
#### Snippet
```java
	private long lineNo = 0;

	private long columnNo = 0;

	@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/SimpleParseLocationListener.java`
#### Snippet
```java
public class SimpleParseLocationListener implements ParseLocationListener {

	private long lineNo = 0;

	private long columnNo = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionObject.java`
#### Snippet
```java
	private int propagationBehavior = PROPAGATION_REQUIRED;

	private boolean readOnly = false;

	public TransactionObject(RepositoryConnection connection) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionObject.java`
#### Snippet
```java
	private String name = "";

	private boolean rollbackOnly = false;

	private int timeout = TIMEOUT_DEFAULT;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TxProperties.java`
#### Snippet
```java
@ConfigurationProperties(prefix = "rdf4j.spring.tx")
public class TxProperties {
	private boolean enabled = false;

	public boolean isEnabled() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/RelationMapBuilder.java`
#### Snippet
```java
	private GraphPattern[] constraints = new GraphPattern[0];
	private final RDF4JTemplate rdf4JTemplate;
	private boolean isRelationOptional = false;
	private boolean isSubjectKeyed = true;
	private final BindingsBuilder bindingsBuilder = new BindingsBuilder();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/pool/PoolProperties.java`
#### Snippet
```java
@ConfigurationProperties(prefix = "rdf4j.spring.pool")
public class PoolProperties {
	private boolean enabled = false;
	/**
	 * Maximum number of connections pooled.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/repository/inmemory/InMemoryRepositoryProperties.java`
#### Snippet
```java
	 * Should a SHACL Sail be used?
	 */
	private boolean useShaclSail = false;

	public boolean isUseShaclSail() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/repository/remote/RemoteRepositoryProperties.java`
#### Snippet
```java
	@NotBlank
	@Length(min = 1)
	private String name = null;

	public String getManagerUrl() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/repository/remote/RemoteRepositoryProperties.java`
#### Snippet
```java
	@NotBlank
	@Pattern(regexp = "^(https?|ftp|file)://[-a-zA-Z0-9+&@#/%?=~_|!:,.;]*[-a-zA-Z0-9+&@#/%=~_|]")
	private String managerUrl = null;
	/**
	 * Name of the repository
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/uuidsource/simple/SimpleRepositoryUUIDSourceProperties.java`
#### Snippet
```java
public class SimpleRepositoryUUIDSourceProperties {
	@NotBlank
	private boolean enabled = false;

	public boolean isEnabled() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ResultCacheProperties.java`
#### Snippet
```java
	 * no global result cache is used.
	 */
	private boolean assumeNoOtherRepositoryClients = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/ResultCacheProperties.java`
#### Snippet
```java
public class ResultCacheProperties {

	private boolean enabled = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/OperationLogProperties.java`
#### Snippet
```java
@ConfigurationProperties(prefix = "rdf4j.spring.operationlog")
public class OperationLogProperties {
	private boolean enabled = false;

	public boolean isEnabled() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/log/OperationExecutionStats.java`
#### Snippet
```java
	private final long start;
	private Long end = null;
	private boolean failed = false;

	public OperationExecutionStats(String operation, int bindingsHashCode) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/log/OperationExecutionStats.java`
#### Snippet
```java
	private final String operation;
	private final long start;
	private Long end = null;
	private boolean failed = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionalRepositoryConnection.java`
#### Snippet
```java
public class TransactionalRepositoryConnection extends RepositoryConnectionWrapper {

	TransactionObject transactionObject = null;

	public TransactionalRepositoryConnection(Repository repository) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/log/jmx/OperationLogJmxProperties.java`
#### Snippet
```java
@ConfigurationProperties(prefix = "rdf4j.spring.operationlog.jmx")
public class OperationLogJmxProperties {
	boolean enabled = false;

	public boolean isEnabled() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/log/jmx/AggregatedOperationStats.java`
#### Snippet
```java
	private int count = 0;
	private int failed = 0;
	private long cumulativeTime = 0;
	private Integer uniqueBindingsCount = null;
	private Set<Integer> bindingsHashcodes = new HashSet<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/log/jmx/AggregatedOperationStats.java`
#### Snippet
```java
 */
public class AggregatedOperationStats implements Cloneable {
	private String operation = null;
	private int count = 0;
	private int failed = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/log/jmx/AggregatedOperationStats.java`
#### Snippet
```java
public class AggregatedOperationStats implements Cloneable {
	private String operation = null;
	private int count = 0;
	private int failed = 0;
	private long cumulativeTime = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/log/jmx/AggregatedOperationStats.java`
#### Snippet
```java
	private int failed = 0;
	private long cumulativeTime = 0;
	private Integer uniqueBindingsCount = null;
	private Set<Integer> bindingsHashcodes = new HashSet<>();

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/log/jmx/AggregatedOperationStats.java`
#### Snippet
```java
	private String operation = null;
	private int count = 0;
	private int failed = 0;
	private long cumulativeTime = 0;
	private Integer uniqueBindingsCount = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/resultcache/CachingRepositoryConnection.java`
#### Snippet
```java
	private final LRUResultCache<ReusableGraphQueryResult> globalGraphQueryResultCache;
	private final ResultCacheProperties properties;
	private boolean clearGlobalResultCacheOnClose = false;

	public CachingRepositoryConnection(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationcache/OperationCacheProperties.java`
#### Snippet
```java
@ConfigurationProperties(prefix = "rdf4j.spring.operationcache")
public class OperationCacheProperties {
	private boolean enabled = false;

	public boolean isEnabled() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * The current indentation level, i.e. the number of tabs to indent a start or end tag.
	 */
	protected int _indentLevel = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * Flag indicating whether the output should be printed pretty, i.e. adding newlines and indentation.
	 */
	private boolean _prettyPrint = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/SimpleSAXParser.java`
#### Snippet
```java
	 * Flag indicating whether leading and trailing whitespace in text elements should be preserved.
	 */
	private boolean preserveWhitespace = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/SimpleSAXParser.java`
#### Snippet
```java
		 * The tag name of a deferred start tag.
		 */
		private String deferredStartTag = null;

		/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/SimpleSAXParser.java`
#### Snippet
```java
		 * The attributes of a deferred start tag.
		 */
		private Map<String, String> deferredAttributes = null;

		/*--------------*
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemoryStore.java`
#### Snippet
```java
	private SailStore store;

	private volatile boolean persist = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemoryStore.java`
#### Snippet
```java
	 * @see #setSyncDelay
	 */
	private volatile long syncDelay = 0L;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemIRI.java`
#### Snippet
```java
	 *---------*/

	transient SoftReference<String> toStringCache = null;

	@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemIRI.java`
#### Snippet
```java
	 * The MemURI's hash code, 0 if not yet initialized.
	 */
	private volatile int hashCode = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementIterator.java`
#### Snippet
```java
	 * Flag indicating whether this iteration has been closed.
	 */
	private boolean closed = false;

	private final MemStatementIteratorCache iteratorCache;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementIterator.java`
#### Snippet
```java
	}

	private int cachedHashCode = 0;

	@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/WeakObjectRegistry.java`
#### Snippet
```java
		Iterator<E> currentIterator;
		boolean[] readLocks;
		boolean init = false;

		public AutoCloseableIterator(Map<E, WeakReference<E>>[] objectMap, AdderBasedReadWriteLock[] locks) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/config/MemoryStoreConfig.java`
#### Snippet
```java
public class MemoryStoreConfig extends BaseSailConfig {

	private boolean persist = false;

	private long syncDelay = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/config/MemoryStoreConfig.java`
#### Snippet
```java
	private boolean persist = false;

	private long syncDelay = 0L;

	public MemoryStoreConfig() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IndentingWriter.java`
#### Snippet
```java
	 * Flag indicating whether indentation has been written for the current line.
	 */
	private boolean indentationWritten = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IndentingWriter.java`
#### Snippet
```java
	 * The current indentation level, i.e. the number of tabs to indent a start or end tag.
	 */
	protected int indentationLevel = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
	 * inferred statements.
	 */
	private volatile boolean mayHaveInferred = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
	private final class MemorySailSink implements SailSink {

		private volatile boolean closed = false;

		private final boolean explicit;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/AbstractSPARQLJSONWriter.java`
#### Snippet
```java
	protected boolean headerComplete = false;

	protected boolean tupleVariablesFound = false;

	protected boolean linksFound = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/AbstractSPARQLJSONWriter.java`
#### Snippet
```java
	protected boolean headerOpen = false;

	protected boolean headerComplete = false;

	protected boolean tupleVariablesFound = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/AbstractSPARQLJSONWriter.java`
#### Snippet
```java
	protected boolean documentOpen = false;

	protected boolean headerOpen = false;

	protected boolean headerComplete = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/AbstractSPARQLJSONWriter.java`
#### Snippet
```java
	protected boolean firstTupleWritten = false;

	protected boolean documentOpen = false;

	protected boolean headerOpen = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/AbstractSPARQLJSONWriter.java`
#### Snippet
```java
	protected boolean tupleVariablesFound = false;

	protected boolean linksFound = false;

	protected final JsonGenerator jg;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/AbstractSPARQLJSONWriter.java`
#### Snippet
```java
			.build();

	protected boolean firstTupleWritten = false;

	protected boolean documentOpen = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/DynamicModel.java`
#### Snippet
```java
	final Set<Namespace> namespaces = new LinkedHashSet<>();

	volatile private Model model = null;

	private final ModelFactory modelFactory;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java

	// Cached CoreDatatype, or null if not yet computed.
	private CoreDatatype coreDatatype = null;

	/*--------------*
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	// Cache Optional instance for the language, or null if not yet computed. Marked as transient because Optional is
	// not serializable.
	transient private Optional<String> optionalLanguageCache = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDateTime.java`
#### Snippet
```java
	 * Flag indicating whether the values have been normalized.
	 */
	private boolean isNormalized = false;

	/*--------------*
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC32.java`
#### Snippet
```java
	};

	private long value = 0;

	@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC16.java`
#### Snippet
```java
	};

	private long value = 0;

	@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/CRC8.java`
#### Snippet
```java
	};

	private long value = 0;

	@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriplesSectionBitmap.java`
#### Snippet
```java

	private int sizeY = 0;
	private int sizeZ = 0;

	private int posX = 1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriplesSectionBitmap.java`
#### Snippet
```java
	private HDTArray arrZ;

	private int sizeY = 0;
	private int sizeZ = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriplesSectionBitmap.java`
#### Snippet
```java
	private int posX = 1;
	private int posY = 0;
	private int posZ = 0;

	@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriplesSectionBitmap.java`
#### Snippet
```java

	private int posX = 1;
	private int posY = 0;
	private int posZ = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
	private int targetQueueSize;

	private int queueSize = 0;

	private final Deque<SubjectInContext> stack = new LinkedList<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/SPARQLResultsXSVMappingStrategy.java`
#### Snippet
```java
abstract public class SPARQLResultsXSVMappingStrategy implements MappingStrategy<BindingSet> {

	protected List<String> bindingNames = null;
	protected final ValueFactory valueFactory;
	protected static final Pattern numberPattern = Pattern.compile("^[-+]?[\\d.].*");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
	private List<String> bindingNames;

	protected boolean tupleVariablesFound = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/GraphManagementQuery.java`
#### Snippet
```java
	private static final String SILENT = "SILENT";

	private boolean silent = false;

	GraphManagementQuery() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/DestinationSourceManagementQuery.java`
#### Snippet
```java
	private Optional<Iri> from = Optional.empty();
	private Optional<Iri> to = Optional.empty();
	private boolean fromDefault = false, toDefault = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/DestinationSourceManagementQuery.java`
#### Snippet
```java
	private Optional<Iri> from = Optional.empty();
	private Optional<Iri> to = Optional.empty();
	private boolean fromDefault = false, toDefault = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/ModifyQuery.java`
#### Snippet
```java
	private Optional<Iri> with = Optional.empty();
	private Optional<Iri> using = Optional.empty();
	private boolean usingNamed = false;

	private Optional<TriplesTemplate> deleteTriples = Optional.empty();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/Aggregate.java`
#### Snippet
```java
	private String separator;
	private boolean isDistinct = false;
	private boolean countAll = false;

	Aggregate(SparqlAggregate aggregate) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/Aggregate.java`
#### Snippet
```java

	private String separator;
	private boolean isDistinct = false;
	private boolean countAll = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/StandardQueryElementCollection.java`
#### Snippet
```java
	private Function<String, String> wrapperMethod = Function.identity();

	private boolean printBodyIfEmpty = false;
	private boolean printNameIfEmpty = true;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/propertypath/builder/EmptyPropertyPathBuilder.java`
#### Snippet
```java
 */
public class EmptyPropertyPathBuilder {
	private NegatedPropertySetBuilder negatedPropertySetBuilder = null;
	private PropertyPathBuilder propertyPathBuilder = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/propertypath/builder/EmptyPropertyPathBuilder.java`
#### Snippet
```java
public class EmptyPropertyPathBuilder {
	private NegatedPropertySetBuilder negatedPropertySetBuilder = null;
	private PropertyPathBuilder propertyPathBuilder = null;

	public EmptyPropertyPathBuilder() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/GroupGraphPattern.java`
#### Snippet
```java
	private Optional<GraphName> from = Optional.empty();
	private List<Filter> filters = new ArrayList<>();
	protected boolean isOptional = false;

	GroupGraphPattern() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareRepository.java`
#### Snippet
```java
	private IRI[] archiveContexts = ALL_CONTEXTS;

	private IRI insertContext = null;

	public ContextAwareRepository() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java
	private Boolean includeInferred = true;

	private int maxQueryTime = 0;

	private QueryLanguage queryLanguage = QueryLanguage.SPARQL;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java
	private IRI[] archiveContexts = ALL_CONTEXTS;

	private IRI insertContext = null;

	public ContextAwareConfig() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/AbstractCloseableIteration.java`
#### Snippet
```java
	 * Flag indicating whether this iteration has been closed.
	 */
	private boolean closed = false;

	/*---------*
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
	private IRI[] archiveContexts = ALL_CONTEXTS;

	private IRI insertContext = null;

	public ContextAwareConnection(Repository repository) throws RepositoryException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/base/AbstractRepository.java`
#### Snippet
```java
public abstract class AbstractRepository implements Repository {

	private volatile boolean initialized = false;

	private final Object initLock = new Object();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
	 * Flag indicating whether this iteration has been closed.
	 */
	private boolean closed = false;

	private DualUnionIteration(CloseableIteration<? extends E, X> iteration1,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSail.java`
#### Snippet
```java
	 * until {@link #shutDown() shutdown}.
	 */
	private volatile boolean initialized = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java

		private final CloseableIteration<E, X> iteration;
		private boolean closed = false;

		public CleanableState(CloseableIteration<E, X> iteration) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/CleanerTupleQueryResult.java`
#### Snippet
```java

		private final TupleQueryResult iteration;
		private boolean closed = false;

		public CleanableState(TupleQueryResult iteration) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/CleanerGraphQueryResult.java`
#### Snippet
```java

		private final GraphQueryResult iteration;
		private boolean closed = false;

		public CleanableState(GraphQueryResult iteration) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockTracking.java`
#### Snippet
```java
	private int currentWaitToCollect;
	private long previousCleanup = 0;
	private long previousActiveLocksSignature = 0;

	public LockTracking(boolean stacktrace, String alias, Logger logger, int waitToCollect,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/LockTracking.java`
#### Snippet
```java
	private final String alias;
	private int currentWaitToCollect;
	private long previousCleanup = 0;
	private long previousActiveLocksSignature = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/AbstractOperation.java`
#### Snippet
```java
	protected boolean includeInferred = true;

	private int maxExecutionTimeSeconds = 0;

	/*--------------*
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/AbstractOperation.java`
#### Snippet
```java
	protected final MapBindingSet bindings = new MapBindingSet();

	protected Dataset dataset = null;

	protected boolean includeInferred = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResults.java`
#### Snippet
```java

			private final GraphQueryResult iteration;
			private boolean closed = false;

			public CleanableState(GraphQueryResult iteration) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/MutableTupleQueryResult.java`
#### Snippet
```java
	 * The index of the next element that will be returned by a call to {@link #next()}.
	 */
	private int currentIndex = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLWriter.java`
#### Snippet
```java
	protected final Writer writer;
	protected String defaultNamespace;
	protected boolean headerWritten = false;
	protected Resource lastWrittenSubject = null;
	protected char quote = '"';
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLWriter.java`
#### Snippet
```java
	protected String defaultNamespace;
	protected boolean headerWritten = false;
	protected Resource lastWrittenSubject = null;
	protected char quote = '"';
	protected boolean entityQuote = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLWriter.java`
#### Snippet
```java
	protected Resource lastWrittenSubject = null;
	protected char quote = '"';
	protected boolean entityQuote = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
	 * tag and an immediately following end tag to a single call to emptyElement().
	 */
	private ElementInfo deferredElement = null;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
	 * Flag indicating whether we're currently parsing an XML literal.
	 */
	private boolean parseLiteralMode = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/util/RDFXMLPrettyWriter.java`
#### Snippet
```java
		private IRI type = null;

		private boolean isWritten = false;

		/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/util/RDFXMLPrettyWriter.java`
#### Snippet
```java

		// type == null means that we use <rdf:Description>
		private IRI type = null;

		private boolean isWritten = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
		 * Flag indicating whether this PropertyElement has an attribute <var>rdf:parseType="Collection"</var>.
		 */
		private boolean parseCollection = false;

		/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
		private final Resource resource;

		private boolean isVolatile = false;

		private int liCounter = 1;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTDescribe.java`
#### Snippet
```java
public class ASTDescribe extends SimpleNode {

	private boolean wildcard = false;

	public ASTDescribe(int id) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTSelect.java`
#### Snippet
```java
public class ASTSelect extends SimpleNode {

	private boolean distinct = false;

	private boolean reduced = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTSelect.java`
#### Snippet
```java
	private boolean reduced = false;

	private boolean wildcard = false;

	public ASTSelect(int id) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ASTSelect.java`
#### Snippet
```java
	private boolean distinct = false;

	private boolean reduced = false;

	private boolean wildcard = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java

	int curLexState = 0;
	int defaultLexState = 0;
	int jjnewStateCnt;
	int jjround;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
	}

	int curLexState = 0;
	int defaultLexState = 0;
	int jjnewStateCnt;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/helpers/QueryResultCollector.java`
#### Snippet
```java
	private Boolean value = null;

	private boolean endQueryResultFound = false;

	private List<String> bindingNames = Collections.emptyList();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/helpers/QueryResultCollector.java`
#### Snippet
```java
public class QueryResultCollector implements TupleQueryResultHandler, BooleanQueryResultHandler {

	private boolean hasBooleanSet = false;

	private Boolean value = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/helpers/QueryResultCollector.java`
#### Snippet
```java
	private boolean hasBooleanSet = false;

	private Boolean value = null;

	private boolean endQueryResultFound = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	final private JJCalls[] jj_2_rtns = new JJCalls[7];
	private boolean jj_rescan = false;
	private int jj_gc = 0;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java

	final private JJCalls[] jj_2_rtns = new JJCalls[7];
	private boolean jj_rescan = false;
	private int jj_gc = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/InMemRecordCache.java`
#### Snippet
```java
		return new RecordIterator() {

			private int index = 0;

			@Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeStore.java`
#### Snippet
```java

	// indicates if a datadir is temporary (i.e. will be deleted on shutdown)
	private boolean isTmpDatadir = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeStore.java`
#### Snippet
```java
	 * performance. By default, this feature is disabled.
	 */
	private volatile boolean forceSync = false;

	private volatile int valueCacheSize = ValueStore.VALUE_CACHE_SIZE;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ConcurrentCache.java`
#### Snippet
```java
	private final int capacity;

	private volatile int cleanupTick = 0;

	protected final ConcurrentHashMap<K, V> cache;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/RangeIterator.java`
#### Snippet
```java
	private volatile int currentIdx;

	private volatile boolean closed = false;

	public RangeIterator(BTree tree, byte[] searchKey, byte[] searchMask, byte[] minValue, byte[] maxValue) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/AllocatedNodesList.java`
#### Snippet
```java
	 * Flag indicating whether the set of allocated nodes has changed and needs to be written to file.
	 */
	private boolean needsSync = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/MemoryOverflowModel.java`
#### Snippet
```java
	private long baseline = 0;

	private long maxBlockSize = 0;

	SimpleValueFactory vf = SimpleValueFactory.getInstance();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/MemoryOverflowModel.java`
#### Snippet
```java
	private transient volatile SailSourceModel disk;

	private long baseline = 0;

	private long maxBlockSize = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreConfig.java`
#### Snippet
```java
	private String tripleIndexes;

	private boolean forceSync = false;

	private int valueCacheSize = -1;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
		 * The old value that has been replaced by the insertion of a new value.
		 */
		byte[] oldValue = null;

		/**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
		 * The value that was removed from a child node due to overflow.
		 */
		byte[] overflowValue = null;

		/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
		 * The nodeID to the right of 'overflowValue' that was removed from a child node due to overflow.
		 */
		int overflowNodeID = 0;
	}

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/HashFile.java`
#### Snippet
```java
	private final BitSet poorMansBloomFilter;

	boolean loadedHashFileFromDisk = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
	private class ArrayBindingSetIterator implements Iterator<Binding> {

		private int index = 0;

		public ArrayBindingSetIterator() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/ArrayBindingSet.java`
#### Snippet
```java
	}

	List<String> sortedBindingNames = null;

	public List<String> getSortedBindingNames() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/UnionScopeChangeOptimizer.java`
#### Snippet
```java
	private static class UnionArgChecker extends AbstractQueryModelVisitor<RuntimeException> {

		boolean containsBindOrValues = false;

		@Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/EvaluationStatistics.java`
#### Snippet
```java
	private static class ServiceNodeAnalyzer extends AbstractSimpleQueryModelVisitor<RuntimeException> {

		private int count = 0;

		private ServiceNodeAnalyzer() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		private final QueryEvaluationContext context;
		private final CloseableIteration<? extends Statement, ? extends QueryEvaluationException> iteration;
		private boolean closed = false;

		private ConvertStatementToBindingSetIterator(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
		private final BindingSet bindings;
		private final CloseableIteration<? extends Statement, ? extends QueryEvaluationException> iteration;
		private boolean closed = false;

		private JoinStatementWithBindingSetIterator(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/UnionScopeChangeOptimizer.java`
#### Snippet
```java
	private static class UnionArgChecker extends AbstractSimpleQueryModelVisitor<RuntimeException> {

		boolean containsBindOrValues = false;

		private UnionArgChecker() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/CrossProductIteration.java`
#### Snippet
```java
	protected final CloseableIteration<BindingSet, QueryEvaluationException> resultIteration;

	protected Iterator<BindingSet> inputBindingsIterator = null;

	protected BindingSet currentInputBinding = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/CrossProductIteration.java`
#### Snippet
```java
	protected Iterator<BindingSet> inputBindingsIterator = null;

	protected BindingSet currentInputBinding = null;

	public CrossProductIteration(CloseableIteration<BindingSet, QueryEvaluationException> resultIteration,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/StandardQueryOptimizerPipeline.java`
#### Snippet
```java
public class StandardQueryOptimizerPipeline implements QueryOptimizerPipeline {

	private static boolean assertsEnabled = false;

	static {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultWriter.java`
#### Snippet
```java
	private List<String> bindingNames;

	private boolean documentStarted = false;

	protected boolean tupleVariablesFound = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultWriter.java`
#### Snippet
```java
	private boolean documentStarted = false;

	protected boolean tupleVariablesFound = false;

	public BinaryQueryResultWriter(OutputStream out) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java
	private Map<IRI, IRI> indexedFieldsMapping;

	private IRI indexId = null;

	private IndexableStatementFilter filter = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java
	private IRI indexId = null;

	private IndexableStatementFilter filter = null;

	private final AtomicBoolean closed = new AtomicBoolean(false);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
	 * Whether or not the query is distinct
	 */
	protected boolean mDistinct = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
	 * Whether or not the query is reduced
	 */
	protected boolean mReduced = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/AbstractSPARQLXMLWriter.java`
#### Snippet
```java
	protected boolean headerComplete = false;

	protected boolean tupleVariablesFound = false;

	/**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/AbstractSPARQLXMLWriter.java`
#### Snippet
```java
	protected XMLWriter xmlWriter;

	protected boolean documentOpen = false;

	protected boolean headerOpen = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/AbstractSPARQLXMLWriter.java`
#### Snippet
```java
	protected boolean headerOpen = false;

	protected boolean headerComplete = false;

	protected boolean tupleVariablesFound = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/AbstractSPARQLXMLWriter.java`
#### Snippet
```java
	protected boolean documentOpen = false;

	protected boolean headerOpen = false;

	protected boolean headerComplete = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedBooleanQuery.java`
#### Snippet
```java
	// ASK queries do not have the root projection node
	// We consider them as SELECT * WHERE {...} LIMIT 1 queries
	public Projection projection = null;

	public SerializableParsedBooleanQuery() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedTupleQuery.java`
#### Snippet
```java
	public Group groupBy = null;
	public Order orderBy = null;
	public Filter having = null;
	public QueryModifier modifier = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedTupleQuery.java`
#### Snippet
```java
	}

	public Projection projection = null;
	public Group groupBy = null;
	public Order orderBy = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedTupleQuery.java`
#### Snippet
```java
	public Order orderBy = null;
	public Filter having = null;
	public QueryModifier modifier = null;

	public SerializableParsedTupleQuery() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedTupleQuery.java`
#### Snippet
```java
	public Projection projection = null;
	public Group groupBy = null;
	public Order orderBy = null;
	public Filter having = null;
	public QueryModifier modifier = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedTupleQuery.java`
#### Snippet
```java

	public Projection projection = null;
	public Group groupBy = null;
	public Order orderBy = null;
	public Filter having = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/AbstractSerializableParsedQuery.java`
#### Snippet
```java
	public BindingSetAssignment bindings = null;
	public Map<String, ExtensionElem> extensionElements = Maps.newHashMap();
	public Dataset dataset = null;
	public Map<String, Var> nonAnonymousVars = Maps.newHashMap();

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/AbstractSerializableParsedQuery.java`
#### Snippet
```java
	public TupleExpr whereClause = null;
	public Slice limit = null;
	public BindingSetAssignment bindings = null;
	public Map<String, ExtensionElem> extensionElements = Maps.newHashMap();
	public Dataset dataset = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/AbstractSerializableParsedQuery.java`
#### Snippet
```java
	 */
	public Map<Projection, SerializableParsedTupleQuery> subQueriesByProjection = new HashMap<>();
	public TupleExpr whereClause = null;
	public Slice limit = null;
	public BindingSetAssignment bindings = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/AbstractSerializableParsedQuery.java`
#### Snippet
```java
	public Map<Projection, SerializableParsedTupleQuery> subQueriesByProjection = new HashMap<>();
	public TupleExpr whereClause = null;
	public Slice limit = null;
	public BindingSetAssignment bindings = null;
	public Map<String, ExtensionElem> extensionElements = Maps.newHashMap();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedUpdate.java`
#### Snippet
```java

	// public MultiProjection projection = null;
	public UpdateExpr updateExpr = null;

//    public Group groupBy = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedConstructQuery.java`
#### Snippet
```java

	public MultiProjection projection = null;
	public Group groupBy = null;
	public Order orderBy = null;
	public Filter having = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedConstructQuery.java`
#### Snippet
```java
	public Group groupBy = null;
	public Order orderBy = null;
	public Filter having = null;
	public boolean describe = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedConstructQuery.java`
#### Snippet
```java
	public MultiProjection projection = null;
	public Group groupBy = null;
	public Order orderBy = null;
	public Filter having = null;
	public boolean describe = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedConstructQuery.java`
#### Snippet
```java
	public Order orderBy = null;
	public Filter having = null;
	public boolean describe = false;

	public SerializableParsedConstructQuery() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/SerializableParsedConstructQuery.java`
#### Snippet
```java
class SerializableParsedConstructQuery extends AbstractSerializableParsedQuery {

	public MultiProjection projection = null;
	public Group groupBy = null;
	public Order orderBy = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/base/NotifyingRepositoryWrapper.java`
#### Snippet
```java
	private boolean activated;

	private boolean defaultReportDeltas = false;

	private final Set<RepositoryListener> listeners = new CopyOnWriteArraySet<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/base/NotifyingRepositoryConnectionWrapper.java`
#### Snippet
```java
	 *-----------*/

	private boolean activated = false;

	private boolean reportDeltas = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/repository/event/src/main/java/org/eclipse/rdf4j/repository/event/base/NotifyingRepositoryConnectionWrapper.java`
#### Snippet
```java
	private boolean activated = false;

	private boolean reportDeltas = false;

	private final Set<RepositoryConnectionListener> listeners = new CopyOnWriteArraySet<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
	private final Map<AbstractSerializableParsedQuery, Set<String>> renderedExtensionElements = Maps.newHashMap();

	private boolean insideFunction = false;

	public PreprocessedQuerySerializer() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
	private final Map<Projection, SerializableParsedTupleQuery> queriesByProjection = new HashMap<>();

	private AbstractSerializableParsedQuery currentQueryProfile = null;

	private SerializableParsedUpdate currentUpdate = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
	private AbstractSerializableParsedQuery currentQueryProfile = null;

	private SerializableParsedUpdate currentUpdate = null;

	protected StringBuilder builder;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java

	public Slice currentSlice = null;
	public SerializableParsedTupleQuery.QueryModifier currentModifier = null;

	public MultiProjection graphQueryProjection = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	public SerializableParsedTupleQuery.QueryModifier currentModifier = null;

	public MultiProjection graphQueryProjection = null;

	public ParsedQueryPreprocessor() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	public SerializableParsedTupleQuery currentQueryProfile = new SerializableParsedTupleQuery();

	public Slice currentSlice = null;
	public SerializableParsedTupleQuery.QueryModifier currentModifier = null;

```

## RuleId[id=RedundantImplements]
### RedundantImplements
Redundant interface declaration `AutoCloseable`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnManager.java`
#### Snippet
```java
	}

	class Txn implements Closeable, AutoCloseable {

		private long txn;
```

### RedundantImplements
Redundant interface declaration `AutoCloseable`
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResult.java`
#### Snippet
```java
 */
public interface QueryResult<T>
		extends AutoCloseable, CloseableIteration<T, QueryEvaluationException>, Iterable<T> {

	@Override
```

## RuleId[id=ExceptionNameDoesntEndWithException]
### ExceptionNameDoesntEndWithException
Exception class name `IllegalArgumentExceptionWithoutStackTrace` does not end with 'Exception'
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	}

	private static class IllegalArgumentExceptionWithoutStackTrace extends IllegalArgumentException {
		public IllegalArgumentExceptionWithoutStackTrace(String msg) {
			super(msg);
```

## RuleId[id=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/TripleSourceIterationWrapper.java`
#### Snippet
```java
			throw e;
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/TripleSourceIterationWrapper.java`
#### Snippet
```java
			throw e;
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/TripleSourceIterationWrapper.java`
#### Snippet
```java
			throw e;
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetTripleSource.java`
#### Snippet
```java
			}

			if (t instanceof SailException) {
				throw new QueryEvaluationException(t);
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetTripleSource.java`
#### Snippet
```java
				statements.close();
			}
			if (t instanceof SailException) {
				throw new QueryEvaluationException(t);
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/evaluation/SailTripleSource.java`
#### Snippet
```java
				iter.close();
			}
			if (t instanceof SailException) {
				throw new QueryEvaluationException(t);
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
			handleBindings();
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
				}
			} catch (Exception e) {
				if (e instanceof InterruptedException) {
					Thread.currentThread().interrupt();
				}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java
		} catch (Exception e) {
			logger.warn("Failed to rollback LMDB transaction", e);
			throw e instanceof SailException ? (SailException) e : new SailException(e);
		} finally {
			tripleStoreException = null;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/BackgroundResultExecutor.java`
#### Snippet
```java
					onclose.close();
				} catch (Exception e) {
					if (e instanceof InterruptedException) {
						Thread.currentThread().interrupt();
					}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
				throw new RepositoryException("Malformed query result from server", e);
			} catch (QueryResultHandlerException e) {
				if (e instanceof TupleQueryResultHandlerException) {
					throw (TupleQueryResultHandlerException) e;
				} else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/TimeLimitIteration.java`
#### Snippet
```java
			close();
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/TimeLimitIteration.java`
#### Snippet
```java
					close();
				} catch (Exception e) {
					if (e instanceof InterruptedException) {
						Thread.currentThread().interrupt();
					}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
			return false;
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
				Thread.currentThread().interrupt();
			}
			if (e instanceof RuntimeException) {
				throw (RuntimeException) e;
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
			}
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationSpliterator.java`
#### Snippet
```java
				Thread.currentThread().interrupt();
			}
			if (e instanceof RuntimeException) {
				throw (RuntimeException) e;
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/QueueIteration.java`
#### Snippet
```java
				throw exceptions.remove();
			} catch (Exception e) {
				if (e instanceof InterruptedException || Thread.interrupted()) {
					Thread.currentThread().interrupt();
				} else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
				Iterations.closeCloseable(iter);
			} catch (Exception e) {
				if (e instanceof InterruptedException) {
					Thread.currentThread().interrupt();
				}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
			return result;
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
			throw e;
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
			throw e;
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
						throw e;
					} catch (Exception e) {
						if (e instanceof InterruptedException) {
							Thread.currentThread().interrupt();
						}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/Iterations.java`
#### Snippet
```java
				throw e;
			} catch (Exception e) {
				if (e instanceof InterruptedException) {
					Thread.currentThread().interrupt();
				}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/SilentIteration.java`
#### Snippet
```java
			super.handleClose();
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/SilentIteration.java`
#### Snippet
```java
			return super.hasNext();
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/SilentIteration.java`
#### Snippet
```java
			throw e;
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
						Iterations.closeCloseable(argIter.next());
					} catch (Throwable e) {
						if (e instanceof InterruptedException) {
							Thread.currentThread().interrupt();
						}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
			}
		} catch (Exception e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
				Thread.currentThread().interrupt();
			}
			if (e instanceof RuntimeException) {
				throw (RuntimeException) e;
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
			}
		} catch (Throwable e) {
			if (e instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
				iteration.close();
			} catch (Exception ex) {
				if (e instanceof InterruptedException) {
					Thread.currentThread().interrupt();
				}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
				throw new RuntimeException(ex);
			}
			if (e instanceof Error) {
				throw (Error) e;
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/CloseableIterationSpliterator.java`
#### Snippet
```java
				throw (Error) e;
			}
			if (e instanceof RuntimeException) {
				throw (RuntimeException) e;
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/CleanerIteration.java`
#### Snippet
```java
					iteration.close();
				} catch (Exception e) {
					if (e instanceof InterruptedException) {
						Thread.currentThread().interrupt();
					}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/CleanerTupleQueryResult.java`
#### Snippet
```java
					iteration.close();
				} catch (Exception e) {
					if (e instanceof InterruptedException) {
						Thread.currentThread().interrupt();
					}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/diagnostics/CleanerGraphQueryResult.java`
#### Snippet
```java
					iteration.close();
				} catch (Exception e) {
					if (e instanceof InterruptedException) {
						Thread.currentThread().interrupt();
					}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSailConnection.java`
#### Snippet
```java
						entry.getKey().close();
					} catch (Exception e) {
						if (e instanceof InterruptedException) {
							Thread.currentThread().interrupt();
						}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/query/src/main/java/org/eclipse/rdf4j/query/QueryResults.java`
#### Snippet
```java
						iteration.close();
					} catch (Exception e) {
						if (e instanceof InterruptedException) {
							Thread.currentThread().interrupt();
						}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte001`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.clearNodeScope(jjtn001);
				jjtc001 = false;
				if (jjte001 instanceof RuntimeException) {
					{
						throw (RuntimeException) jjte001;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte001`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
					}
				}
				if (jjte001 instanceof ParseException) {
					{
						throw (ParseException) jjte001;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.popNode();
			}
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte001`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.clearNodeScope(jjtn001);
				jjtc001 = false;
				if (jjte001 instanceof RuntimeException) {
					{
						throw (RuntimeException) jjte001;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte001`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
					}
				}
				if (jjte001 instanceof ParseException) {
					{
						throw (ParseException) jjte001;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte001`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.clearNodeScope(jjtn001);
				jjtc001 = false;
				if (jjte001 instanceof RuntimeException) {
					{
						throw (RuntimeException) jjte001;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte001`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
					}
				}
				if (jjte001 instanceof ParseException) {
					{
						throw (ParseException) jjte001;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.popNode();
			}
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte007`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
					jjtree.clearNodeScope(jjtn007);
					jjtc007 = false;
					if (jjte007 instanceof RuntimeException) {
						{
							throw (RuntimeException) jjte007;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte007`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
						}
					}
					if (jjte007 instanceof ParseException) {
						{
							throw (ParseException) jjte007;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte008`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
					jjtree.clearNodeScope(jjtn008);
					jjtc008 = false;
					if (jjte008 instanceof RuntimeException) {
						{
							throw (RuntimeException) jjte008;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte008`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
						}
					}
					if (jjte008 instanceof ParseException) {
						{
							throw (ParseException) jjte008;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.popNode();
			}
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.popNode();
			}
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte001`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.clearNodeScope(jjtn001);
				jjtc001 = false;
				if (jjte001 instanceof RuntimeException) {
					{
						throw (RuntimeException) jjte001;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte001`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
					}
				}
				if (jjte001 instanceof ParseException) {
					{
						throw (ParseException) jjte001;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {

				throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java

			}
			if (jjte000 instanceof ParseException) {
				throw (ParseException) jjte000;
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				jjtree.popNode();
			}
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
			jjtree.clearNodeScope(jjtn000);
			jjtc000 = false;
			if (jjte000 instanceof RuntimeException) {
				{
					throw (RuntimeException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `jjte000`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
				}
			}
			if (jjte000 instanceof ParseException) {
				{
					throw (ParseException) jjte000;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultIO.java`
#### Snippet
```java
			parser.parseQueryResult(in);
		} catch (QueryResultHandlerException e) {
			if (e instanceof TupleQueryResultHandlerException) {
				throw (TupleQueryResultHandlerException) e;
			} else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultIO.java`
#### Snippet
```java
			if (e.getCause() instanceof IOException) {
				throw (IOException) e.getCause();
			} else if (e instanceof TupleQueryResultHandlerException) {
				throw (TupleQueryResultHandlerException) e;
			} else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultIO.java`
#### Snippet
```java
				parser.setQueryResultHandler(qrBuilder).parseQueryResult(in);
			} catch (QueryResultHandlerException e) {
				if (e instanceof TupleQueryResultHandlerException) {
					throw (TupleQueryResultHandlerException) e;
				} else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
				iteration.close();
			}
			if (t instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementPatternQueryEvaluationStep.java`
#### Snippet
```java
				iteration.close();
			}
			if (t instanceof InterruptedException) {
				Thread.currentThread().interrupt();
			}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/optimizer/QueryJoinOptimizer.java`
#### Snippet
```java
						tupleExpr.visit(this);
					} catch (Exception e) {
						if (e instanceof InterruptedException) {
							Thread.currentThread().interrupt();
						}
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/SPARQLResultsXMLParser.java`
#### Snippet
```java
			parseQueryResultInternal(in, false, true);
		} catch (QueryResultHandlerException e) {
			if (e instanceof TupleQueryResultHandlerException) {
				throw (TupleQueryResultHandlerException) e;
			} else {
```

## RuleId[id=ArrayEquality]
### ArrayEquality
Array objects are compared using `!=`, not 'Arrays.equals()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTuple.java`
#### Snippet
```java
		Resource[] contexts;

		if (this.contexts != right.contexts) {
			assert this.contexts != null;
			assert right.contexts != null;
```

### ArrayEquality
Array objects are compared using `!=`, not 'Arrays.equals()'
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementList.java`
#### Snippet
```java
								// inserting into it
								MemStatement[] statementsAfterInsert = getStatements();
								if (statementsAfterInsert != statements
										&& STATEMENTS_ARRAY.getAcquire(statements, i) != st) {
									// we wrote into an array while it was growing and our write was lost
```

### ArrayEquality
Array objects are compared using `!=`, not 'Arrays.equals()'
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementList.java`
#### Snippet
```java
				if (STATEMENTS.compareAndSet(this, statements, null)) {// Grow array
					MemStatement[] newArray = new MemStatement[Math.max(4, length * 2)];
					if (statements != EMPTY_ARRAY) {
						System.arraycopy(statements, 0, newArray, 0, length);
					}
```

### ArrayEquality
Array objects are compared using `!=`, not 'Arrays.equals()'
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemStatementList.java`
#### Snippet
```java
		long writeLock = addRemoveLock.writeLock();
		try {
			if (getStatements() != statements) {
				return false;
			}
```

## RuleId[id=StringBufferReplaceableByStringBuilder]
### StringBufferReplaceableByStringBuilder
`StringBuffer buf` may be declared as 'StringBuilder'
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/AbstractStringReplacer.java`
#### Snippet
```java
		String s = ((Literal) args[0]).getLabel();
		String regex = (args.length == 2) ? ((Literal) args[1]).getLabel() : ".";
		StringBuffer buf = new StringBuffer(s.length());
		Matcher matcher = Pattern.compile(regex).matcher(s);
		while (matcher.find()) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer result` may be declared as 'StringBuilder'
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/ConfigTemplate.java`
#### Snippet
```java

	public String render(Map<String, String> valueMap) {
		StringBuffer result = new StringBuffer(template.length());
		Matcher matcher = TOKEN_PATTERN.matcher(template);
		while (matcher.find()) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer aBuffer` may be declared as 'StringBuilder'
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/RenderUtils.java`
#### Snippet
```java
		theString = theString.replaceAll("\"", "\\\\\"");

		StringBuffer aBuffer = new StringBuffer();
		Matcher aMatcher = Pattern.compile("\\\\([^tnrbf\"'\\\\])").matcher(theString);
		while (aMatcher.find()) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer aBody` may be declared as 'StringBuilder'
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SPARQLQueryRenderer.java`
#### Snippet
```java
			tupleExpr = ((QueryRoot) tupleExpr).getArg();
		}
		StringBuffer aBody = new StringBuffer(mRenderer.render(tupleExpr));

		boolean aFirst;
```

### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SparqlTupleExprRenderer.java`
#### Snippet
```java

	String renderPattern(StatementPattern thePattern) throws Exception {
		StringBuffer sb = new StringBuffer();
		sb.append(renderValueExpr(thePattern.getSubjectVar())).append(" ");
		sb.append(renderValueExpr(thePattern.getPredicateVar())).append(" ");
```

## RuleId[id=RedundantStringFormatCall]
### RedundantStringFormatCall
Redundant call to `format()`
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
		} else {
			for (Artist artist : a) {
				System.out.println(String.format("%s %s", artist.getFirstName(), artist.getLastName()));
			}
		}
```

### RedundantStringFormatCall
Redundant call to `format()`
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
	private void listPaintingsByArtist(Map<Artist, Set<Painting>> paintingsMap) {
		for (Artist a : paintingsMap.keySet()) {
			System.out.println(String.format("%s %s", a.getFirstName(), a.getLastName()));
			for (Painting p : paintingsMap.get(a)) {
				System.out.println(String.format("\t%s (%s)", p.getTitle(), p.getTechnique()));
```

### RedundantStringFormatCall
Redundant call to `format()`
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/ArtDemoCli.java`
#### Snippet
```java
			System.out.println(String.format("%s %s", a.getFirstName(), a.getLastName()));
			for (Painting p : paintingsMap.get(a)) {
				System.out.println(String.format("\t%s (%s)", p.getTitle(), p.getTechnique()));
			}
		}
```

## RuleId[id=FuseStreamOperations]
### FuseStreamOperations
Stream may be extended replacing unmodifiableList
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/ComponentTemporalAmount.java`
#### Snippet
```java
				.stream()
				.sorted(comparing(TemporalUnit::getDuration).reversed()) // as per getUnits() specs
				.collect(toList())
		);

```

### FuseStreamOperations
Stream may be extended replacing 'toArray'
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/PropertyPaths.java`
#### Snippet
```java

	public static RdfPredicate path(IRI... aElements) {
		return path(Arrays.stream(aElements).map(Rdf::iri).collect(Collectors.toList()).toArray(new RdfPredicate[0]));
	}

```

## RuleId[id=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `actualSize` of exception class
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/exception/IncorrectResultSetSizeException.java`
#### Snippet
```java
public class IncorrectResultSetSizeException extends RDF4JDaoException {
	int expectedSize;
	int actualSize;

	public IncorrectResultSetSizeException(int expectedSize, int actualSize) {
```

### NonFinalFieldOfException
Non-final field `expectedSize` of exception class
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/exception/IncorrectResultSetSizeException.java`
#### Snippet
```java
 */
public class IncorrectResultSetSizeException extends RDF4JDaoException {
	int expectedSize;
	int actualSize;

```

### NonFinalFieldOfException
Non-final field `manager` of exception class
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/SailLockedException.java`
#### Snippet
```java
	private final String requestedBy;

	private LockManager manager;

	public SailLockedException(String requestedBy) {
```

### NonFinalFieldOfException
Non-final field `lockedBy` of exception class
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/SailLockedException.java`
#### Snippet
```java
	private static final long serialVersionUID = -2465202131214972460L;

	private String lockedBy;

	private final String requestedBy;
```

### NonFinalFieldOfException
Non-final field `tokenImage` of exception class
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
	 * array is defined in the generated ...Constants interface.
	 */
	public String[] tokenImage;

	/**
```

### NonFinalFieldOfException
Non-final field `currentToken` of exception class
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
	 * the token following this token will (therefore) be the first error token.
	 */
	public Token currentToken;

	/**
```

### NonFinalFieldOfException
Non-final field `expectedTokenSequences` of exception class
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
	 * their ordinal values) that is expected at this point of the parse.
	 */
	public int[][] expectedTokenSequences;

	/**
```

### NonFinalFieldOfException
Non-final field `EOL` of exception class
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
	 * The end of line string for this machine.
	 */
	protected static String EOL = System.getProperty("line.separator", "\n");

	/**
```

### NonFinalFieldOfException
Non-final field `lineNo` of exception class
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultParseException.java`
#### Snippet
```java
	 *-----------*/

	private long lineNo = -1L;

	private long columnNo = -1L;
```

### NonFinalFieldOfException
Non-final field `columnNo` of exception class
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultParseException.java`
#### Snippet
```java
	private long lineNo = -1L;

	private long columnNo = -1L;

	/*--------------*
```

## RuleId[id=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceBranch.java`
#### Snippet
```java
		Set<Resource> deprecatedContexts = change.getDeprecatedContexts();
		if (deprecatedContexts != null && !deprecatedContexts.isEmpty()) {
			sink.clear(deprecatedContexts.toArray(new Resource[0]));
		}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailDatasetImpl.java`
#### Snippet
```java
			List<Resource> remaining = new ArrayList<>(Arrays.asList(contexts));
			remaining.removeAll(deprecatedContexts);
			iter = derivedFrom.getStatements(subj, pred, obj, remaining.toArray(new Resource[0]));
		} else {
			iter = derivedFrom.getStatements(subj, pred, obj, contexts);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/Changeset.java`
#### Snippet
```java
				Resource[] contexts;
				if (p.isAllContexts()) {
					contexts = new Resource[0];
				} else {
					contexts = new Resource[] { context };
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/SparqlFragment.java`
#### Snippet
```java
		}

		SparqlFragment union = union(workingSet.toArray(new String[0]));
		union.addStatementMatchers(getStatementMatchers(sparqlFragments));
		return union;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java

	private Settings transactionSettings;
	private TransactionSetting[] transactionSettingsRaw = new TransactionSetting[0];
	private volatile boolean closed;

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
					if (!alreadySorted && sortedTuples.size() > 1) {
						if (sortedTuples.size() > 8192) { // MIN_ARRAY_SORT_GRAN in Arrays.parallelSort(...)
							ValidationTuple[] objects = sortedTuples.toArray(new ValidationTuple[0]);
							Arrays.parallelSort(objects, ValidationTuple::compareActiveTarget);
							sortedTuples = Arrays.asList(objects);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java

					if (filterOnPredicate == null) {
						filterOnObject = new Resource[0];
					} else {
						filterOnObject = FilterByPredicateObject.this.filterOnObject.stream()
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTuple.java`
#### Snippet
```java

	public ValidationTuple(List<Value> chain, ConstraintComponent.Scope scope, boolean hasValue, Resource[] contexts) {
		this.chain = chain.toArray(new Value[0]);
		this.scope = scope;
		this.propertyShapeScopeWithValue = hasValue;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTuple.java`
#### Snippet
```java
						}

						return new ValidationTuple(t.validationResults, chain.toArray(new Value[0]), scope,
								propertyShapeScopeWithValue,
								Set.of(), t.contexts);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java

		if (encodedValues == null) {
			result = new Resource[0];
		} else {
			result = new Resource[encodedValues.length];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionSAXParser.java`
#### Snippet
```java
			currentDataset = null;
		} else if (TransactionXMLConstants.BINDINGS.equals(tagName)) {
			Binding b[] = bindings.toArray(new Binding[0]);
			currentSPARQLUpdate.setBindings(b);
			bindings.clear();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/ProjectionElemList.java`
#### Snippet
```java
	private static final long serialVersionUID = 167331362220688635L;

	private ProjectionElem[] elements = {};
	private List<ProjectionElem> elementsList = Collections.emptyList();

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/ProjectionElemList.java`
#### Snippet
```java

		this.elementsList = Collections.unmodifiableList(elements);
		this.elements = this.elementsList.toArray(new ProjectionElem[0]);
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/SailSourceModel.java`
#### Snippet
```java
		}
		if (contexts.length == 0) {
			return new Resource[0];
		}
		Resource[] result = new Resource[contexts.length];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/RelationMapBuilder.java`
#### Snippet
```java
			.createIRI("urn:java:relationDaoSupport:Nothing");
	private final RdfPredicate predicate;
	private GraphPattern[] constraints = new GraphPattern[0];
	private final RDF4JTemplate rdf4JTemplate;
	private boolean isRelationOptional = false;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
					Resource[] contexts;
					if (ctxVar == null) {
						contexts = new Resource[0];
					} else {
						contexts = new Resource[] { (Resource) ctxVar.getValue() };
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
			}
		}
		int[][] dest = list.toArray(new int[][] {});
		Arrays.sort(dest, CMP);
		return dest;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
	private String[] listPctEncodings(String path) {
		if (path == null || path.indexOf('%') < 0) {
			return new String[0];
		}
		List<String> list = new ArrayList<>();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
			list.add(path.substring(start, pct + 3));
		}
		return list.toArray(new String[0]);
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/PropertyPaths.java`
#### Snippet
```java

	public static RdfPredicate path(IRI... aElements) {
		return path(Arrays.stream(aElements).map(Rdf::iri).collect(Collectors.toList()).toArray(new RdfPredicate[0]));
	}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/Expressions.java`
#### Snippet
```java
			rdfValueOptions.add(iri(option));
		}
		return rdfValueOptions.toArray(new RdfValue[0]);
	}
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/constraint/propertypath/builder/NegatedPropertySetBuilder.java`
#### Snippet
```java
	public NegatedPropertySet build() {
		return new NegatedPropertySet(
				propertySet.toArray(new PredicatePathOrInversePredicatePath[0]));
	}
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
		IRI p = st.getPredicate();
		Value o = isBlank(st.getObject()) ? null : st.getObject();
		Resource[] g = isBlank(st.getContext()) ? new Resource[0] : new Resource[] { st.getContext() };
		List<Statement> result = new ArrayList<>();

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/AbstractRDFInserter.java`
#### Snippet
```java
	 * corresponding contexts.
	 */
	protected Resource[] contexts = new Resource[0];

	/**
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/RDFSailRemover.java`
#### Snippet
```java
	 * from the corresponding contexts.
	 */
	private Resource[] contexts = new Resource[0];

	/*--------------*
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepositoryConnection.java`
#### Snippet
```java
		try {
			// always call receiveTransactionSettings(...) before calling begin();
			sailConnection.setTransactionSettings(new TransactionSetting[0]);

			if (level != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailRepositoryConnection.java`
#### Snippet
```java
		try {
			// always call receiveTransactionSettings(...) before calling begin();
			sailConnection.setTransactionSettings(new TransactionSetting[0]);

			if (getIsolationLevel() != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
	private IRI[] getDefaultRemoveGraphs(Dataset dataset) {
		if (dataset == null) {
			return new IRI[0];
		}
		Set<IRI> set = new HashSet<>(dataset.getDefaultRemoveGraphs());
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
		Set<IRI> set = new HashSet<>(dataset.getDefaultRemoveGraphs());
		if (set.isEmpty()) {
			return new IRI[0];
		}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `spring-components/spring-boot-sparql-web/src/main/java/org/eclipse/rdf4j/http/server/readonly/QueryResponder.java`
#### Snippet
```java

	private String[] toArray(String namedGraphUri) {
		String[] namedGraphUris = new String[] {};
		if (namedGraphUri != null) {
			namedGraphUris = new String[] { namedGraphUri };
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/SailSourceModel.java`
#### Snippet
```java
		}
		if (contexts.length == 0) {
			return new Resource[0];
		}
		Resource[] result = new Resource[contexts.length];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ArrayBindingBasedQueryEvaluationContext.java`
#### Snippet
```java
		};
		node.visit(queryModelVisitorBase);
		return varNames.keySet().toArray(new String[0]);
	}
}
```

## RuleId[id=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `contexts`, unclear if a varargs or non-varargs call is desired
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/LoggingRepositoryConnection.java`
#### Snippet
```java
	public void clear(Resource... contexts) throws RepositoryException {
		operationLog.runWithLog(
				PseudoOperation.forClear(contexts), () -> getDelegate().clear((contexts)));
	}

```

### NullArgumentToVariableArgMethod
Confusing argument `contexts`, unclear if a varargs or non-varargs call is desired
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/operationlog/LoggingRepositoryConnection.java`
#### Snippet
```java
	public long size(Resource... contexts) throws RepositoryException {
		return operationLog.runWithLog(
				PseudoOperation.forSize(contexts), () -> getDelegate().size(contexts));
	}

```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/RioSetting.java`
#### Snippet
```java
 * @author Peter Ansell
 */
public interface RioSetting<T extends Object> extends Serializable {

	/**
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/WriterConfig.java`
#### Snippet
```java

	@Override
	public <T extends Object> WriterConfig set(RioSetting<T> setting, T value) {
		super.set(setting, value);
		return this;
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/ParserConfig.java`
#### Snippet
```java

	@Override
	public <T extends Object> ParserConfig set(RioSetting<T> setting, T value) {
		super.set(setting, value);
		return this;
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/RioConfig.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	public <T extends Object> T get(RioSetting<T> setting) {
		Object result = settings.get(setting);

```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/RioConfig.java`
#### Snippet
```java
	 * @return True if the setting has been explicitly set, or false otherwise.
	 */
	public <T extends Object> boolean isSet(RioSetting<T> setting) {
		return settings.containsKey(setting) || systemPropertyCache.containsKey(setting)
				|| hasSystemPropertyOverride(setting);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/RioConfig.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	public <T extends Object> RioConfig set(RioSetting<T> setting, T value) {

		if (value == null) {
```

## RuleId[id=NonExceptionNameEndsWithException]
### NonExceptionNameEndsWithException
Non-exception class name `ValidationException` ends with 'Exception'
in `core/common/exception/src/main/java/org/eclipse/rdf4j/common/exception/ValidationException.java`
#### Snippet
```java
 */
@Experimental
public interface ValidationException {

	/**
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
		if (Literals.isLanguageLiteral(lit)) {
			sb.append('@');
			sb.append(lit.getLanguage().get());
		} else if (!lit.getDatatype().equals(XSD.STRING)) {
			// Don't append type if it's xsd:string, this keeps it compatible with RDF 1.0
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
		if (Literals.isLanguageLiteral(lit)) {
			sb.append('@');
			sb.append(lit.getLanguage().get());
		} else {
			sb.append("^^<");
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
				if (Literals.isLanguageLiteral(lit)) {
					qb.append("@");
					qb.append(lit.getLanguage().get());
				} else {
					qb.append("^^<" + lit.getDatatype().stringValue() + ">");
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
					.map(constraintComponent -> constraintComponent.getPreferredValidationApproach(connectionsGroup))
					.reduce(ValidationApproach::reducePreferred)
					.get();
		}

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetSubjectsOf.java`
#### Snippet
```java
						+ ".\n}")
				.reduce((a, b) -> a + " UNION " + b)
				.get();
	}

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetObjectsOf.java`
#### Snippet
```java
						+ ".\n}")
				.reduce((a, b) -> a + " UNION " + b)
				.get();
	}

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
		queryParserFactory = QueryParserRegistry.getInstance()
				.get(QueryLanguage.SPARQL)
				.get();

		this.removedStatementMatchers = removedStatementMatchers != null
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MinCountConstraintComponent.java`
#### Snippet
```java
			if (connectionsGroup.hasAddedStatements()) {
				PlanNode addedByPath = getTargetChain().getPath()
						.get()
						.getAdded(connectionsGroup, validationSettings.getDataGraph(), null);
				LeftOuterJoin leftOuterJoin = new LeftOuterJoin(target, addedByPath);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MinCountConstraintComponent.java`
#### Snippet
```java

			PlanNode addedByPath = getTargetChain().getPath()
					.get()
					.getAdded(connectionsGroup, validationSettings.getDataGraph(), null);
			LeftOuterJoin leftOuterJoin = new LeftOuterJoin(target, addedByPath);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MinCountConstraintComponent.java`
#### Snippet
```java
				connectionsGroup.getBaseConnection(),
				validationSettings.getDataGraph(), getTargetChain().getPath()
						.get()
						.getTargetQueryFragment(new StatementMatcher.Variable("a"), new StatementMatcher.Variable("c"),
								connectionsGroup.getRdfsSubClassOfReasoner(), stableRandomVariableProvider),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/QualifiedMaxCountConstraintComponent.java`
#### Snippet
```java
					connectionsGroup.getBaseConnection(),
					validationSettings.getDataGraph(), getTargetChain().getPath()
							.get()
							.getTargetQueryFragment(new StatementMatcher.Variable("a"),
									new StatementMatcher.Variable("c"),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/QualifiedMaxCountConstraintComponent.java`
#### Snippet
```java
				connectionsGroup.getBaseConnection(),
				validationSettings.getDataGraph(), getTargetChain().getPath()
						.get()
						.getTargetQueryFragment(new StatementMatcher.Variable("a"), new StatementMatcher.Variable("c"),
								connectionsGroup.getRdfsSubClassOfReasoner(), stableRandomVariableProvider),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/ClassConstraintComponent.java`
#### Snippet
```java

		if (scope == Scope.propertyShape) {
			Path path = getTargetChain().getPath().get();

			PlanNode addedTargets;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MaxCountConstraintComponent.java`
#### Snippet
```java
							scope, false, null));

			PlanNode addedByPath = path.get().getAdded(connectionsGroup, validationSettings.getDataGraph(), null);

			addedByPath = Unique.getInstance(new TrimToTarget(addedByPath), false);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MaxCountConstraintComponent.java`
#### Snippet
```java
					connectionsGroup.getBaseConnection(),
					validationSettings.getDataGraph(), getTargetChain().getPath()
							.get()
							.getTargetQueryFragment(new StatementMatcher.Variable("a"),
									new StatementMatcher.Variable("c"),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/MaxCountConstraintComponent.java`
#### Snippet
```java
					connectionsGroup.getBaseConnection(),
					validationSettings.getDataGraph(), getTargetChain().getPath()
							.get()
							.getTargetQueryFragment(new StatementMatcher.Variable("a"),
									new StatementMatcher.Variable("c"),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/DashHasValueInConstraintComponent.java`
#### Snippet
```java

		if (scope == Scope.propertyShape) {
			Path path = getTargetChain().getPath().get();

			PlanNode addedTargets;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/QualifiedMinCountConstraintComponent.java`
#### Snippet
```java
					connectionsGroup.getBaseConnection(),
					validationSettings.getDataGraph(), getTargetChain().getPath()
							.get()
							.getTargetQueryFragment(new StatementMatcher.Variable("a"),
									new StatementMatcher.Variable("c"),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/QualifiedMinCountConstraintComponent.java`
#### Snippet
```java
					connectionsGroup.getBaseConnection(),
					validationSettings.getDataGraph(), getTargetChain().getPath()
							.get()
							.getTargetQueryFragment(new StatementMatcher.Variable("a"),
									new StatementMatcher.Variable("c"),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/HasValueConstraintComponent.java`
#### Snippet
```java

		if (scope == Scope.propertyShape) {
			Path path = getTargetChain().getPath().get();

			PlanNode addedTargets;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java
		} else {

			PlanNode invalidValuesDirectOnPath = path.get()
					.getAdded(connectionsGroup, validationSettings.getDataGraph(),
							planNode -> getFilterAttacherWithNegation(negatePlan, planNode));
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java
				planNode = new BulkedExternalInnerJoin(overrideTargetPlanNode,
						connectionsGroup.getBaseConnection(),
						validationSettings.getDataGraph(), path.get()
								.getTargetQueryFragment(new StatementMatcher.Variable("a"),
										new StatementMatcher.Variable("c"),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/rio/trix/src/main/java/org/eclipse/rdf4j/rio/trix/TriXWriter.java`
#### Snippet
```java

			if (Literals.isLanguageLiteral(literal)) {
				xmlWriter.setAttribute(LANGUAGE_ATT, literal.getLanguage().get());
				xmlWriter.textElement(PLAIN_LITERAL_TAG, literal.getLabel());
			} else {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionWriter.java`
#### Snippet
```java
		if (literal != null) {
			if (Literals.isLanguageLiteral(literal)) {
				xmlWriter.setAttribute(TransactionXMLConstants.LANG_ATT, literal.getLanguage().get());
			} else {
				xmlWriter.setAttribute(TransactionXMLConstants.DATATYPE_ATT, literal.getDatatype().toString());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionWriter.java`
#### Snippet
```java
						Literal literal = (Literal) binding.getValue();
						if (Literals.isLanguageLiteral(literal)) {
							xmlWriter.setAttribute(TransactionXMLConstants.LANGUAGE_ATT, literal.getLanguage().get());
						} else {
							xmlWriter.setAttribute(TransactionXMLConstants.DATA_TYPE_ATT,
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/Concat.java`
#### Snippet
```java
				if (useLanguageTag && Literals.isLanguageLiteral(lit)) {
					if (languageTag == null) {
						languageTag = lit.getLanguage().get();
					} else if (!languageTag.equals(lit.getLanguage().get())) {
						languageTag = null;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/Concat.java`
#### Snippet
```java
					if (languageTag == null) {
						languageTag = lit.getLanguage().get();
					} else if (!languageTag.equals(lit.getLanguage().get())) {
						languageTag = null;
						useLanguageTag = false;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java

		if (Literals.isLanguageLiteral(l)) {
			return new LmdbLiteral(revision, l.getLabel(), l.getLanguage().get());
		} else if (l.getCoreDatatype() != CoreDatatype.NONE) {
			return new LmdbLiteral(revision, l.getLabel(), l.getCoreDatatype());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/RDFParserHelper.java`
#### Snippet
```java
			boolean recognisedLanguage = false;
			for (LanguageHandler nextHandler : parserConfig.get(BasicParserSettings.LANGUAGE_HANDLERS)) {
				if (nextHandler.isRecognizedLanguage(workingLang.get())) {
					recognisedLanguage = true;
					if (parserConfig.get(BasicParserSettings.VERIFY_LANGUAGE_TAGS)) {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
			// Append the literal's language
			appendable.append("@");
			appendable.append(lit.getLanguage().get());
		} else {
			// SES-1917 : In RDF-1.1, all literals have a type, and if they are not
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

		assertFalse(testModel.getNamespace(RDF.PREFIX).isPresent());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertFalse(testModel.getNamespace(DC.PREFIX).isPresent());
		assertFalse(testModel.getNamespace(SKOS.PREFIX).isPresent());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(5, namespaces.size());

		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.getNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.getNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.getNamespace(SKOS.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.getNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.getNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE), testModel.getNamespace(SESAME.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.getNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE), testModel.getNamespace(SESAME.PREFIX).get());
	}
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.getNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE), testModel.getNamespace(SESAME.PREFIX).get());
	}

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(5, namespaces.size());

		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.getNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.getNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.getNamespace(SKOS.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.getNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.getNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE), testModel.getNamespace(SESAME.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.getNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.getNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE), testModel.getNamespace(SESAME.PREFIX).get());
	}
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.getNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.getNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE), testModel.getNamespace(SESAME.PREFIX).get());
	}

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertFalse(testModel.removeNamespace(RDF.NAMESPACE).isPresent());
		assertFalse(testModel.removeNamespace(RDFS.NAMESPACE).isPresent());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.removeNamespace(DC.PREFIX).get());
		assertFalse(testModel.removeNamespace(SKOS.NAMESPACE).isPresent());
		assertFalse(testModel.removeNamespace(SESAME.NAMESPACE).isPresent());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(5, namespaces.size());

		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.removeNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.removeNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.removeNamespace(DC.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.removeNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.removeNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.removeNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.removeNamespace(SKOS.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(RDF.PREFIX, RDF.NAMESPACE), testModel.removeNamespace(RDF.PREFIX).get());
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.removeNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.removeNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.removeNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(RDFS.PREFIX, RDFS.NAMESPACE), testModel.removeNamespace(RDFS.PREFIX).get());
		assertEquals(new SimpleNamespace(DC.PREFIX, DC.NAMESPACE), testModel.removeNamespace(DC.PREFIX).get());
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.removeNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE),
				testModel.removeNamespace(SESAME.PREFIX).get());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java
		assertEquals(new SimpleNamespace(SKOS.PREFIX, SKOS.NAMESPACE), testModel.removeNamespace(SKOS.PREFIX).get());
		assertEquals(new SimpleNamespace(SESAME.PREFIX, SESAME.NAMESPACE),
				testModel.removeNamespace(SESAME.PREFIX).get());

		Set<Namespace> namespacesAfter = testModel.getNamespaces();
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionalRepositoryConnectionFactory.java`
#### Snippet
```java
		transactionData.set(txObj);
		TransactionalRepositoryConnection txCon = findWrapper(wrappedCon, TransactionalRepositoryConnection.class)
				.get();
		txCon.setTransactionObject(txObj);
		logger.debug("Transaction created");
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/FileIO.java`
#### Snippet
```java
				dataOut.writeByte(LANG_LITERAL_MARKER);
				writeString(label, dataOut);
				writeString(lit.getLanguage().get(), dataOut);
			} else {
				dataOut.writeByte(DATATYPE_LITERAL_MARKER);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/MemValueFactory.java`
#### Snippet
```java

			if (Literals.isLanguageLiteral(literal)) {
				return new MemLiteral(this, label, literal.getLanguage().get());
			} else {
				try {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java
				break;
			}
			Statement statement = statementSupplier.get(container, annotatedMembershipPredicate, null, contexts).get();

			collectionConsumer.accept(statement);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
			// Append the literal's language
			writer.write("@");
			writer.write(lit.getLanguage().get());
		} else if (!xsdStringToPlainLiteral || !XSD.STRING.equals(datatype)) {
			// Append the literal's datatype (possibly written as an abbreviated
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVWriter.java`
#### Snippet
```java
			// Append the literal's language
			writer.write("@");
			writer.write(lit.getLanguage().get());
		} else if (!XSD.STRING.equals(datatype) || !xsdStringToPlainLiteral()) {
			writer.write("\"");
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
												.map(Object::toString)
												.reduce((a, b) -> a + " , \n\t" + b)
												.get());

								failure.set(true);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLWriter.java`
#### Snippet
```java
				// language attribute
				if (Literals.isLanguageLiteral(objLit)) {
					writeAttribute("xml:lang", objLit.getLanguage().get());
				} else {
					CoreDatatype coreDatatype = objLit.getCoreDatatype();
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/util/RDFXMLPrettyWriter.java`
#### Snippet
```java
			// language attribute
			if (Literals.isLanguageLiteral(objLit)) {
				writeAttribute("xml:lang", objLit.getLanguage().get());
			} else {
				if (isXmlLiteral) {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java

		if (Literals.isLanguageLiteral(l)) {
			return new NativeLiteral(revision, l.getLabel(), l.getLanguage().get());
		} else {
			NativeIRI datatype = getNativeURI(l.getDatatype());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/string/Concat.java`
#### Snippet
```java
				if (useLanguageTag && Literals.isLanguageLiteral(lit)) {
					if (commonLanguageTag == null) {
						commonLanguageTag = lit.getLanguage().get();
					} else if (!commonLanguageTag.equals(lit.getLanguage().orElse(null))) {
						commonLanguageTag = null;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultWriter.java`
#### Snippet
```java

		if (Literals.isLanguageLiteral(literal)) {
			writeString(literal.getLanguage().get());
		} else {
			writeQName(datatype);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
	public static boolean isSPOElemList(ProjectionElemList theList) {
		return theList.getElements().size() == 3
				&& theList.getElements().get(0).getProjectionAlias().get().equalsIgnoreCase("subject")
				&& theList.getElements().get(1).getProjectionAlias().get().equalsIgnoreCase("predicate")
				&& theList.getElements().get(2).getProjectionAlias().get().equalsIgnoreCase("object");
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
		return theList.getElements().size() == 3
				&& theList.getElements().get(0).getProjectionAlias().get().equalsIgnoreCase("subject")
				&& theList.getElements().get(1).getProjectionAlias().get().equalsIgnoreCase("predicate")
				&& theList.getElements().get(2).getProjectionAlias().get().equalsIgnoreCase("object");
	}
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/BaseTupleExprRenderer.java`
#### Snippet
```java
				&& theList.getElements().get(0).getProjectionAlias().get().equalsIgnoreCase("subject")
				&& theList.getElements().get(1).getProjectionAlias().get().equalsIgnoreCase("predicate")
				&& theList.getElements().get(2).getProjectionAlias().get().equalsIgnoreCase("object");
	}

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/AbstractSPARQLXMLWriter.java`
#### Snippet
```java
	private void writeLiteral(Literal literal) throws IOException {
		if (Literals.isLanguageLiteral(literal)) {
			xmlWriter.setAttribute(LITERAL_LANG_ATT, literal.getLanguage().get());
		}
		// Only enter this section for non-language literals now, as the
```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `replacement != null` is always `true`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
		for (String name : bindings.getBindingNames()) {
			String replacement = valueToString(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				select = select.replaceAll(pattern, "(" + Matcher.quoteReplacement(replacement) + " as ?" + name + ")");
```

### ConstantValue
Condition `replacement != null` is always `true`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
		for (String name : bindings.getBindingNames()) {
			String replacement = valueToString(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				// we use Matcher.quoteReplacement to make sure things like newlines
```

### ConstantValue
Condition `replacement != null` is always `true`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
		for (String name : bindings.getBindingNames()) {
			String replacement = getReplacement(bindings.getValue(name));
			if (replacement != null) {
				String pattern = ".*[\\?\\$]" + name + "\\W.*";
				if (Pattern.compile(pattern, Pattern.MULTILINE | Pattern.DOTALL).matcher(select).matches()) {
```

### ConstantValue
Condition `replacement != null` is always `true`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/SPARQLOperation.java`
#### Snippet
```java
		for (String name : bindings.getBindingNames()) {
			String replacement = getReplacement(bindings.getValue(name));
			if (replacement != null) {
				String pattern = "[\\?\\$]" + name + "(?=\\W)";
				select = select.replaceAll(pattern, "");
```

### ConstantValue
Condition `result != null` is always `false`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
			if (!allGood) {
				try {
					if (result != null) {
						result.close();
					}
```

### ConstantValue
Condition `result != null` is always `false`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
			if (!allGood) {
				try {
					if (result != null) {
						result.close();
					}
```

### ConstantValue
Condition `result != null` is always `false`
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
			if (!allGood) {
				try {
					if (result != null) {
						result.close();
					}
```

### ConstantValue
Value `trackResultSize` is always 'true'
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java
			EvaluationStrategy strategy = getEvaluationStrategy(dataset, tripleSource);
			if (trackResultSize) {
				strategy.setTrackResultSize(trackResultSize);
			}

```

### ConstantValue
Value `trackTime` is always 'true'
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java

			if (trackTime) {
				strategy.setTrackTime(trackTime);
			}

```

### ConstantValue
Value `subject` is always 'null'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/RSXTargetShape.java`
#### Snippet
```java

		return this.targetShape
				.buildSparqlValidNodes_rsx_targetShape(subject, object, rdfsSubClassOfReasoner, null,
						stableRandomVariableProvider)
				.getFragment();
```

### ConstantValue
Value `subject` is always 'null'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/RSXTargetShape.java`
#### Snippet
```java

		return this.targetShape
				.buildSparqlValidNodes_rsx_targetShape(subject, object, rdfsSubClassOfReasoner, null,
						stableRandomVariableProvider)
				.getStatementMatchers()
```

### ConstantValue
Value `deactivated` is always 'true'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java

		if (deactivated) {
			modelBuilder.add(SHACL.DEACTIVATED, deactivated);
		}

```

### ConstantValue
Condition `equivalent` is always `true`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/TargetChainRetriever.java`
#### Snippet
```java
		boolean equivalent = true;

		if (equivalent && currentStatementMatcher.getSubjectValue() == null
				&& !currentStatementMatcher.subjectIsWildcard()) {
			equivalent = Objects.equals(bindings.getBinding(currentStatementMatcher.getSubjectName()),
```

### ConstantValue
Value `propertyShapeScopeWithValue` is always 'false'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTuple.java`
#### Snippet
```java
			return Collections
					.singletonList(new ValidationTuple(this.validationResults, chain, scope,
							propertyShapeScopeWithValue, Set.of(), contexts));

		} else {
```

### ConstantValue
Value `negatePlan` is always 'false'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/SimpleAbstractConstraintComponent.java`
#### Snippet
```java

		return String.join("\n",
				"BIND((" + getSparqlFilterExpression(variable.getName(), negatePlan) + ") as " + tempVar + ")",
				"FILTER(COALESCE(" + tempVar + ", true))"
		);
```

### ConstantValue
Value `includePropertyShapeValues` is always 'false'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
						targetChainSize = next.getFullChainSize(true);
					} else {
						targetChainSize = next.getFullChainSize(includePropertyShapeValues);
					}

```

### ConstantValue
Value `includePropertyShapeValues` is always 'false'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
									temp = t.getFullChainSize(true);
								} else {
									temp = t.getFullChainSize(includePropertyShapeValues);
								}

```

### ConstantValue
Value `includePropertyShapeValues` is always 'false'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
									temp = t.getFullChainSize(true);
								} else {
									temp = t.getFullChainSize(includePropertyShapeValues);
								}

```

### ConstantValue
Value `hasStatement` is always 'false'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/data/VerySimpleRdfsBackwardsChainingConnection.java`
#### Snippet
```java
			Set<Resource> types = rdfsSubClassOfReasoner.backwardsChain((Resource) obj);
			if (types.size() == 1) {
				return hasStatement;
			}
			if (types.size() > 10) {
```

### ConstantValue
Value `equal` is always 'false'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/If.java`
#### Snippet
```java
			boolean equal = condition.equals(o.getCondition());
			if (!equal) {
				return equal;
			}

```

### ConstantValue
Value `equal` is always 'false'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/If.java`
#### Snippet
```java
			equal = (result == null) ? o.getResult() == null : result.equals(o.getResult());
			if (!equal) {
				return equal;
			}

```

### ConstantValue
Condition `!hasWithClause` is always `true`
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserUpdate.java`
#### Snippet
```java
		// if there are default graphs in the SPARQL update but it's not a WITH
		// clause, it's a USING clause
		final boolean hasUsingClause = !hasWithClause && sparqlDefaultGraphs != null ? sparqlDefaultGraphs.size() > 0
				: false;

```

### ConstantValue
Value `hasWithClause` is always 'false'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserUpdate.java`
#### Snippet
```java
		// if there are default graphs in the SPARQL update but it's not a WITH
		// clause, it's a USING clause
		final boolean hasUsingClause = !hasWithClause && sparqlDefaultGraphs != null ? sparqlDefaultGraphs.size() > 0
				: false;

```

### ConstantValue
Condition `!hasWithClause` is always `true`
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserUpdate.java`
#### Snippet
```java
		// if there are default graphs in the SPARQL update but it's not a WITH
		// clause, it's a USING clause
		final boolean hasUsingClause = !hasWithClause && sparqlDefaultGraphs != null ? sparqlDefaultGraphs.size() > 0
				: false;

```

### ConstantValue
Value `hasWithClause` is always 'false'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserUpdate.java`
#### Snippet
```java
		// if there are default graphs in the SPARQL update but it's not a WITH
		// clause, it's a USING clause
		final boolean hasUsingClause = !hasWithClause && sparqlDefaultGraphs != null ? sparqlDefaultGraphs.size() > 0
				: false;

```

### ConstantValue
Condition `cInt >= 0x0` is always `true`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
			} else if (c == '\t') {
				appendable.append("\\t");
			} else if (cInt >= 0x0 && cInt <= 0x8 || cInt == 0xB || cInt == 0xC || cInt >= 0xE && cInt <= 0x1F
					|| cInt >= 0x7F && cInt <= 0xFFFF) {
				if (escapeUnicode) {
```

### ConstantValue
Condition `cInt >= 0xE` is always `true`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
			} else if (c == '\t') {
				appendable.append("\\t");
			} else if (cInt >= 0x0 && cInt <= 0x8 || cInt == 0xB || cInt == 0xC || cInt >= 0xE && cInt <= 0x1F
					|| cInt >= 0x7F && cInt <= 0xFFFF) {
				if (escapeUnicode) {
```

### ConstantValue
Condition `cInt <= 0xFFFF` is always `true` when reached
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
				appendable.append("\\t");
			} else if (cInt >= 0x0 && cInt <= 0x8 || cInt == 0xB || cInt == 0xC || cInt >= 0xE && cInt <= 0x1F
					|| cInt >= 0x7F && cInt <= 0xFFFF) {
				if (escapeUnicode) {
					appendable.append("\\u");
```

### ConstantValue
Condition `cInt >= 0x10000` is always `false`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
					appendable.append(c);
				}
			} else if (cInt >= 0x10000 && cInt <= 0x10FFFF) {
				if (escapeUnicode) {
					appendable.append("\\U");
```

### ConstantValue
Condition `cInt >= 0x10000 && cInt <= 0x10FFFF` is always `false`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
					appendable.append(c);
				}
			} else if (cInt >= 0x10000 && cInt <= 0x10FFFF) {
				if (escapeUnicode) {
					appendable.append("\\U");
```

### ConstantValue
Condition `parsedOp == null` is always `false`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else {
				parsedOp = parseRDF(queryResource, queryType, store);
				if (parsedOp == null && input.canFallback) {
					parsedOp = parseText(queryResource, queryType, store);
				}
```

### ConstantValue
Condition `parsedOp == null && input.canFallback` is always `false`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
			} else {
				parsedOp = parseRDF(queryResource, queryType, store);
				if (parsedOp == null && input.canFallback) {
					parsedOp = parseText(queryResource, queryType, store);
				}
```

### ConstantValue
Value `writeLocked` is always 'true'
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/WeakObjectRegistry.java`
#### Snippet
```java
				if (unlockedSum == lockedSum) {
					// No active readers.
					return writeLocked;
				} else {
					Thread.onSpinWait();
```

### ConstantValue
Value `prioritiseCleaning` is always 'false'
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/MemorySailStore.java`
#### Snippet
```java
			}

			prioritiseCleaning = prioritiseSnapshotCleaningIfLowOnMemory(prioritiseCleaning);

			// Sets used to keep track of which lists have already been processed
```

### ConstantValue
Condition `_path == null` is always `false`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
		// Remove any '.' segments:
		_path = _path.replace("/./", "/");
		if (_path == null) {
			return null;
		}
```

### ConstantValue
Condition `language == null` is always `true` when reached
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/AbstractSPARQLJSONParser.java`
#### Snippet
```java
				// For broken SPARQL endpoints which return LANGSTRING without a language, fall back
				// to using STRING as the datatype
				if (RDF.LANGSTRING.equals(datatypeIri) && language == null) {
					logger.debug(
							"rdf:langString typed literal missing language tag: '{}'. Falling back to xsd:string.",
```

### ConstantValue
Condition `datatype == null` is always `false`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
		if (org.eclipse.rdf4j.model.vocabulary.RDF.LANGSTRING.equals(datatype.getIri())) {
			throw new IllegalArgumentException("datatype rdf:langString requires a language tag");
		} else if (datatype == null) {
			setDatatype(CoreDatatype.XSD.STRING);
		} else {
```

### ConstantValue
Condition `totalStrings > Integer.MAX_VALUE` is always `false`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTDictionarySectionPFC.java`
#### Snippet
```java

			long val = VByte.decode(cis);
			if (totalStrings > Integer.MAX_VALUE) {
				throw new UnsupportedOperationException(getDebugPartStr() + " max number of strings exceeded: " + val);
			}
```

### ConstantValue
Condition `b > Integer.MAX_VALUE` is always `false`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTBitmap.java`
#### Snippet
```java

			long b = (int) VByte.decode(cis);
			if (b > Integer.MAX_VALUE) {
				throw new UnsupportedOperationException("Maximum number of entries in bitmap exceeded: " + b);
			}
```

### ConstantValue
Condition `i < buffer.length` is always `true`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/VByte.java`
#### Snippet
```java
		do {
			buffer[i] = b[start + i];
		} while (i < buffer.length && hasNext(buffer[i++]));
		return decode(buffer, i);
	}
```

### ConstantValue
Condition `i < buffer.length` is always `true`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/VByte.java`
#### Snippet
```java
		do {
			buffer[i] = (byte) is.read();
		} while (i < buffer.length && hasNext(buffer[i++]));
		return decode(buffer, i);
	}
```

### ConstantValue
Condition `bytes > Integer.MAX_VALUE` is always `false`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTArrayLog64.java`
#### Snippet
```java
			// read bytes, minimum 1
			long bytes = (nrbits * entries + 7) / 8;
			if (bytes > Integer.MAX_VALUE) {
				throw new UnsupportedOperationException("Maximum number of bytes in array exceeded: " + bytes);
			}
```

### ConstantValue
Condition `c == '.'` is always `false`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
		}

		if (c == '.') {
			reportError("IRI must not end in a '.'", BasicParserSettings.VERIFY_URI_SYNTAX);
			uriIsIllegal = true;
```

### ConstantValue
Value `val` is always 'null'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
				writeResource((Resource) val, inlineBNodes);
			} else {
				writeLiteral((Literal) val);
			}
			assert lastWrittenSubject.equals(stack.peekLast());
```

### ConstantValue
Condition `parsedIri != null` is always `true`
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/rdf/Rdf.java`
#### Snippet
```java
			ParsedIRI parsedIri = ParsedIRI.create(iriString);

			if (parsedIri != null) {
				return () -> "<" + iriString + ">";
			} else {
```

### ConstantValue
Value `iter` is always 'null'
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
		if (resultIter == null) {
			// Underlying iterator has not yet been initialized
			resultIter = iter;
			if (resultIter == null) {
				resultIter = iter = createIteration();
```

### ConstantValue
Condition `resultIter == null` is always `true`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			// Underlying iterator has not yet been initialized
			resultIter = iter;
			if (resultIter == null) {
				resultIter = iter = createIteration();
			}
```

### ConstantValue
Value `iter` is always 'null'
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
		if (resultIter == null) {
			// Underlying iterator has not yet been initialized
			resultIter = iter;
			if (resultIter == null) {
				resultIter = iter = createIteration();
```

### ConstantValue
Condition `resultIter == null` is always `true`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
			// Underlying iterator has not yet been initialized
			resultIter = iter;
			if (resultIter == null) {
				resultIter = iter = createIteration();
			}
```

### ConstantValue
Condition `iter != null` is always `true`
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/LimitIteration.java`
#### Snippet
```java
		super(iter);

		assert iter != null;
		assert limit >= 0;

```

### ConstantValue
Value `inferred` is always 'false'
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCommittedWrapper.java`
#### Snippet
```java
								}

								if (!inferred && inferred != statement.isInferred()) {
									return false;
								}
```

### ConstantValue
Condition `"James Leigh".compareTo("James Leigh Hunt") < 0` is always `true`
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/SparqlOrderByTest.java`
#### Snippet
```java
	@Test
	public void testQuery1() throws Exception {
		assertTrue("James Leigh".compareTo("James Leigh Hunt") < 0);
		assertResult(query1, Arrays.asList("James Leigh", "James Leigh", "James Leigh Hunt", "Megan Leigh"));
	}
```

### ConstantValue
Value `timedOut` is always 'true'
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/ExplanationImpl.java`
#### Snippet
```java
		this.genericPlanNode = genericPlanNode;
		if (timedOut) {
			genericPlanNode.setTimedOut(timedOut);
		}
	}
```

### ConstantValue
Condition `number == Double.POSITIVE_INFINITY` is always `false`
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
		if (number == null) {
			humanReadbleString = UNKNOWN;
		} else if (number == Double.POSITIVE_INFINITY) {
			humanReadbleString = "∞";
		} else if (number > 1_000_000) {
```

### ConstantValue
Condition `c == 'U'` is always `true`
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java
							| hexval(ReadByte()) << 4 | hexval(ReadByte()));
					column += 4;
				} else if (c == 'U') {
					String hex = new String(new char[] { ReadByte(), ReadByte(), ReadByte(), ReadByte(), ReadByte(),
							ReadByte(), ReadByte(), ReadByte() });
```

### ConstantValue
Condition `result != null` is always `false`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailGraphQuery.java`
#### Snippet
```java
			if (!allGood) {
				try {
					if (result != null) {
						result.close();
					}
```

### ConstantValue
Condition `result != null` is always `false`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/evaluation/RepositoryTripleSource.java`
#### Snippet
```java
			if (!allGood) {
				try {
					if (result != null) {
						result.close();
					}
```

### ConstantValue
Condition `result != null` is always `false`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
			if (!allGood) {
				try {
					if (result != null) {
						result.close();
					}
```

### ConstantValue
Condition `subject != null` is always `true`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

		Statement st = null;
		if (subject != null && predicate != null && object != null) {
			if (context != null) {
				st = vf.createStatement(subject, predicate, object, context);
```

### ConstantValue
Condition `subject != null && predicate != null && object != null` is always `true`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

		Statement st = null;
		if (subject != null && predicate != null && object != null) {
			if (context != null) {
				st = vf.createStatement(subject, predicate, object, context);
```

### ConstantValue
Condition `predicate != null` is always `true`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

		Statement st = null;
		if (subject != null && predicate != null && object != null) {
			if (context != null) {
				st = vf.createStatement(subject, predicate, object, context);
```

### ConstantValue
Condition `object != null` is always `true`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java

		Statement st = null;
		if (subject != null && predicate != null && object != null) {
			if (context != null) {
				st = vf.createStatement(subject, predicate, object, context);
```

### ConstantValue
Condition `updateExpr instanceof Load` is always `false`
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/helpers/SailUpdateExecutor.java`
#### Snippet
```java
			} else if (updateExpr instanceof Move) {
				executeMove((Move) updateExpr, uc, maxExecutionTime);
			} else if (updateExpr instanceof Load) {
				throw new SailException("load operations can not be handled directly by the SAIL");
			}
```

### ConstantValue
Condition `input instanceof NamespaceAware` is always `true`
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalRDFParser.java`
#### Snippet
```java
			handleStatement(result, (Statement) input);
		} else if (input instanceof Model) {
			if (input instanceof NamespaceAware) {
				final Set<Namespace> namespaces = ((NamespaceAware) input).getNamespaces();
				for (final Namespace nextNs : namespaces) {
```

### ConstantValue
Value `inframe` is always 'null'
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDWriter.java`
#### Snippet
```java
			final Object inframe = null;
			if (mode == JSONLDMode.FLATTEN) {
				output = JsonLdProcessor.flatten(output, inframe, opts);
			}
			if (mode == JSONLDMode.COMPACT) {
```

### ConstantValue
Value `forceSync` is always 'true'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreConfig.java`
#### Snippet
```java
		}
		if (forceSync) {
			m.add(implNode, FORCE_SYNC, vf.createLiteral(forceSync));
		}
		if (valueCacheSize >= 0) {
```

### ConstantValue
Condition `result != null` is always `false`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/AbstractQueryPreparer.java`
#### Snippet
```java
				if (!allGood) {
					try {
						if (result != null) {
							result.close();
						}
```

### ConstantValue
Condition `result != null` is always `false`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/AbstractQueryPreparer.java`
#### Snippet
```java
				if (!allGood) {
					try {
						if (result != null) {
							result.close();
						}
```

### ConstantValue
Value `leftLangLit` is always 'false'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtil.java`
#### Snippet
```java
						} else {
							return otherCases(leftLit, rightLit, operator, leftCoreDatatype, rightCoreDatatype,
									leftLangLit, rightLangLit);
						}

```

### ConstantValue
Value `rightLangLit` is always 'false'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtil.java`
#### Snippet
```java
						} else {
							return otherCases(leftLit, rightLit, operator, leftCoreDatatype, rightCoreDatatype,
									leftLangLit, rightLangLit);
						}

```

### ConstantValue
Value `leftLangLit` is always 'false'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtility.java`
#### Snippet
```java
				try {
					Order order = handleCommonDatatype(leftLit, rightLit, strict, leftXSDDatatype, rightXSDDatatype,
							leftLangLit, rightLangLit, commonDatatype);

					if (order == Order.illegalArgument) {
```

### ConstantValue
Value `rightLangLit` is always 'false'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtility.java`
#### Snippet
```java
				try {
					Order order = handleCommonDatatype(leftLit, rightLit, strict, leftXSDDatatype, rightXSDDatatype,
							leftLangLit, rightLangLit, commonDatatype);

					if (order == Order.illegalArgument) {
```

### ConstantValue
Condition `subselects == null` is always `false`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java

			List<TupleExpr> result = new ArrayList<>();
			if (subselects == null || subselects.isEmpty()) {
				return result;
			}
```

### ConstantValue
Condition `subj != null` is always `true` when reached
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ZeroLengthPathIteration.java`
#### Snippet
```java
		} else {
			if (result != null) {
				if (obj == null && subj != null) {
					setObject.accept(subj, result);
				} else if (subj == null && obj != null) {
```

### ConstantValue
Condition `obj != null` is always `true` when reached
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ZeroLengthPathIteration.java`
#### Snippet
```java
				if (obj == null && subj != null) {
					setObject.accept(subj, result);
				} else if (subj == null && obj != null) {
					setSubject.accept(obj, result);
				} else if (subj != null && subj.equals(obj)) {
```

### ConstantValue
Condition `subj != null` is always `true`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ZeroLengthPathIteration.java`
#### Snippet
```java
				} else if (subj == null && obj != null) {
					setSubject.accept(obj, result);
				} else if (subj != null && subj.equals(obj)) {
					// empty bindings
					// (result but nothing to bind as subjectVar and objVar are both fixed)
```

### ConstantValue
Value `startVarFixed` is always 'true'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java

				if (startVarFixed && endVarFixed && currentLength > 2) {
					v1 = getVarValue(startVar, startVarFixed, nextElement);
					v2 = nextElement.getValue("END_" + JOINVAR_PREFIX + this.hashCode());
				} else if (startVarFixed && endVarFixed && currentLength == 2) {
```

### ConstantValue
Value `startVarFixed` is always 'true'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java
					v2 = nextElement.getValue("END_" + JOINVAR_PREFIX + this.hashCode());
				} else if (startVarFixed && endVarFixed && currentLength == 2) {
					v1 = getVarValue(startVar, startVarFixed, nextElement);
					v2 = nextElement.getValue(JOINVAR_PREFIX + (currentLength - 1) + "_" + this.hashCode());
				} else {
```

### ConstantValue
Value `endVarFixed` is always 'true'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/PathIteration.java`
#### Snippet
```java

					if (startVarFixed && endVarFixed) {
						Value endValue = getVarValue(endVar, endVarFixed, nextElement);
						if (endValue.equals(v2)) {
							add(reportedValues, vp);
```

### ConstantValue
Condition `formatVersion > FORMAT_VERSION && formatVersion < 1` is always `false`
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java
		// version 2)
		formatVersion = this.in.readInt();
		if (formatVersion > FORMAT_VERSION && formatVersion < 1) {
			throw new QueryResultParseException("Incompatible format version: " + formatVersion);
		}
```

### ConstantValue
Condition `formatVersion < 1` is always `false` when reached
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultParser.java`
#### Snippet
```java
		// version 2)
		formatVersion = this.in.readInt();
		if (formatVersion > FORMAT_VERSION && formatVersion < 1) {
			throw new QueryResultParseException("Incompatible format version: " + formatVersion);
		}
```

### ConstantValue
Condition `xmlLang == null` is always `true` when reached
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/SPARQLResultsSAXParser.java`
#### Snippet
```java
				// For broken SPARQL endpoints which return LANGSTRING without a language, fall back
				// to using STRING as the datatype
				if (RDF.LANGSTRING.equals(datatypeIri) && xmlLang == null) {
					logger.debug(
							"rdf:langString typed literal missing language tag: '{}'. Falling back to xsd:string.",
```

### ConstantValue
Value `result` is always 'false'
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/AbstractSPARQLXMLParser.java`
#### Snippet
```java
		}

		return result;
	}

```

## RuleId[id=MethodOverridesStaticMethod]
### MethodOverridesStaticMethod
Method `getInstance()` tries to override a static method of a superclass
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchValueFactory.java`
#### Snippet
```java
	 * @return a singleton instance of SimpleValueFactory.
	 */
	public static SimpleValueFactory getInstance() {
		return sharedInstance;
	}
```

### MethodOverridesStaticMethod
Method `data()` tries to override a static method of a superclass
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxComplianceTest.java`
#### Snippet
```java

	@Parameterized.Parameters(name = "{0}")
	public static Collection<Object[]> data() {
		return Arrays.asList(getTestData());
	}
```

### MethodOverridesStaticMethod
Method `parameters()` tries to override a static method of a superclass
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RDFSchemaRepositoryConnectionTest.java`
#### Snippet
```java
public abstract class RDFSchemaRepositoryConnectionTest extends RepositoryConnectionTest {

	public static IsolationLevel[] parameters() {
		return new IsolationLevel[] { IsolationLevels.NONE, IsolationLevels.READ_COMMITTED,
				IsolationLevels.SNAPSHOT_READ,
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
		if (subject != null) {
			if (subject instanceof BNode) {
				qb.append("_:" + subject.stringValue() + " ");
			} else {
				qb.append("<" + subject.stringValue() + "> ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
				qb.append("_:" + subject.stringValue() + " ");
			} else {
				qb.append("<" + subject.stringValue() + "> ");
			}
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java

		if (predicate != null) {
			qb.append("<" + predicate.stringValue() + "> ");
		} else {
			qb.append("?pred");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
					qb.append(lit.getLanguage().get());
				} else {
					qb.append("^^<" + lit.getDatatype().stringValue() + ">");
				}
				qb.append(" ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
				qb.append(" ");
			} else if (object instanceof BNode) {
				qb.append("_:" + object.stringValue() + " ");
			} else {
				qb.append("<" + object.stringValue() + "> ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
				qb.append("_:" + object.stringValue() + " ");
			} else {
				qb.append("<" + object.stringValue() + "> ");
			}
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
						namedGraph = "urn:nodeid:" + context.stringValue();
					}
					qb.append("    GRAPH <" + namedGraph + "> { \n");
				}
				createBGP(qb, subject, predicate, object);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
						namedGraph = "urn:nodeid:" + context.stringValue();
					}
					qb.append("    GRAPH <" + namedGraph + "> { \n");
				}
				createDataBody(qb, statements, true);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
						namedGraph = "urn:nodeid:" + context.stringValue();
					}
					qb.append("    GRAPH <" + namedGraph + "> { \n");
				}
				createDataBody(qb, statements, true);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
						namedGraph = "urn:nodeid:" + context.stringValue();
					}
					qb.append("    GRAPH <" + namedGraph + "> { \n");
				}
			}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
			}
			if (st.getSubject() instanceof BNode) {
				qb.append("_:" + st.getSubject().stringValue() + " ");
			} else {
				qb.append("<" + st.getSubject().stringValue() + "> ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
				qb.append("_:" + st.getSubject().stringValue() + " ");
			} else {
				qb.append("<" + st.getSubject().stringValue() + "> ");
			}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
			}

			qb.append("<" + st.getPredicate().stringValue() + "> ");

			if (st.getObject() instanceof Literal) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
					qb.append(lit.getLanguage().get());
				} else {
					qb.append("^^<" + lit.getDatatype().stringValue() + ">");
				}
				qb.append(" ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
				qb.append(" ");
			} else if (st.getObject() instanceof BNode) {
				qb.append("_:" + st.getObject().stringValue() + " ");
			} else {
				qb.append("<" + st.getObject().stringValue() + "> ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
				qb.append("_:" + st.getObject().stringValue() + " ");
			} else {
				qb.append("<" + st.getObject().stringValue() + "> ");
			}
			qb.append(". \n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java

		if (contexts.length == 0) {
			sparqlTransaction.append(clearMode + " ALL ");
			sparqlTransaction.append("; ");
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
			for (Resource context : contexts) {
				if (context == null) {
					sparqlTransaction.append(clearMode + " DEFAULT ");
					sparqlTransaction.append("; ");
				} else if (context instanceof IRI) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
					sparqlTransaction.append("; ");
				} else if (context instanceof IRI) {
					sparqlTransaction.append(clearMode + " GRAPH <" + context.stringValue() + "> ");
					sparqlTransaction.append("; ");
				} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnBufferedPlanNode.java`
#### Snippet
```java
		parent.getPlanAsGraphvizDot(stringBuilder);

		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedPlanNode.java`
#### Snippet
```java
		parent.getPlanAsGraphvizDot(stringBuilder);

		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TargetChainPopper.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TargetChainPopper.java`
#### Snippet
```java
				.append("\n");

		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedSplitter.java`
#### Snippet
```java
			}
			printed = true;
			stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
					.append("\n");
			stringBuilder.append(bufferedSplitter.parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BufferedSplitter.java`
#### Snippet
```java
			stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
					.append("\n");
			stringBuilder.append(bufferedSplitter.parent.getId() + " -> " + getId()).append("\n");
			bufferedSplitter.parent.getPlanAsGraphvizDot(stringBuilder);
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnionNode.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		for (PlanNode node : nodes) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnionNode.java`
#### Snippet
```java
				.append("\n");
		for (PlanNode node : nodes) {
			stringBuilder.append(node.getId() + " -> " + getId()).append("\n");
			node.getPlanAsGraphvizDot(stringBuilder);

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToPropertyShape.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToPropertyShape.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		if (trueNode != null) {
			stringBuilder.append(getId() + " -> " + trueNode.getId() + " [label=\"true values\"]").append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		if (trueNode != null) {
			stringBuilder.append(getId() + " -> " + trueNode.getId() + " [label=\"true values\"]").append("\n");

		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterPlanNode.java`
#### Snippet
```java
		}
		if (falseNode != null) {
			stringBuilder.append(getId() + " -> " + falseNode.getId() + " [label=\"false values\"]").append("\n");

		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
		left.getPlanAsGraphvizDot(stringBuilder);

		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
		stringBuilder.append(right.getId() + " -> " + getId() + " [label=\"right\"];").append("\n");
		right.getPlanAsGraphvizDot(stringBuilder);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoin.java`
#### Snippet
```java
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
		stringBuilder.append(right.getId() + " -> " + getId() + " [label=\"right\"];").append("\n");
		right.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AllTargetsPlanNode.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(select.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/AllTargetsPlanNode.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(select.getId() + " -> " + getId()).append("\n");
		select.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToNodeShape.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ShiftToNodeShape.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SetFilterNode.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/SetFilterNode.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TrimToTarget.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TrimToTarget.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");

		// added/removed connections are always newly minted per plan node, so we instead need to compare the underlying
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
		// sail
		if (connection instanceof MemoryStoreConnection) {
			stringBuilder.append(System.identityHashCode(((MemoryStoreConnection) connection).getSail()) + " -> "
					+ getId() + " [label=\"filter source\"]").append("\n");
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicate.java`
#### Snippet
```java
					+ getId() + " [label=\"filter source\"]").append("\n");
		} else {
			stringBuilder.append(System.identityHashCode(connection) + " -> " + getId() + " [label=\"filter source\"]")
					.append("\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TupleMapper.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/TupleMapper.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
		left.getPlanAsGraphvizDot(stringBuilder);

		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
		stringBuilder.append(right.getId() + " -> " + getId() + " [label=\"right\"];").append("\n");
		right.getPlanAsGraphvizDot(stringBuilder);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
		stringBuilder.append(right.getId() + " -> " + getId() + " [label=\"right\"];").append("\n");
		right.getPlanAsGraphvizDot(stringBuilder);

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
		if (connection instanceof MemoryStoreConnection) {
			stringBuilder
					.append(System.identityHashCode(((MemoryStoreConnection) connection).getSail()) + " -> " + getId())
					.append("\n");
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
					.append("\n");
		} else {
			stringBuilder.append(System.identityHashCode(connection) + " -> " + getId()).append("\n");
		}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Sort.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
		left.getPlanAsGraphvizDot(stringBuilder);

		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
		stringBuilder.append(right.getId() + " -> " + getId() + " [label=\"right\"];").append("\n");
		right.getPlanAsGraphvizDot(stringBuilder);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
		stringBuilder.append(right.getId() + " -> " + getId() + " [label=\"right\"];").append("\n");
		right.getPlanAsGraphvizDot(stringBuilder);

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java

		if (discardedRight != null) {
			stringBuilder.append(getId() + " -> " + (discardedRight).getId() + " [label=\"discardedRight\"];")
					.append("\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
		}
		if (discardedLeft != null) {
			stringBuilder.append(getId() + " -> " + (discardedLeft).getId() + " [label=\"discardedLeft\"];")
					.append("\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java

		if (joined != null) {
			stringBuilder.append(getId() + " -> " + (joined).getId() + " [label=\"joined\"];").append("\n");
		}
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByCountFilter.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
		left.getPlanAsGraphvizDot(stringBuilder);

		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
		stringBuilder.append(right.getId() + " -> " + getId() + " [label=\"right\"];").append("\n");
		right.getPlanAsGraphvizDot(stringBuilder);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/EqualsJoinValue.java`
#### Snippet
```java
				.append("\n");
		stringBuilder.append(left.getId() + " -> " + getId() + " [label=\"left\"];").append("\n");
		stringBuilder.append(right.getId() + " -> " + getId() + " [label=\"right\"];").append("\n");
		right.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Unique.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Unique.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/GroupByFilter.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");

		// added/removed connections are always newly minted per plan node, so we instead need to compare the underlying
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
		// sail
		if (connection instanceof MemoryStoreConnection) {
			stringBuilder.append(System.identityHashCode(((MemoryStoreConnection) connection).getSail()) + " -> "
					+ getId() + " [label=\"filter source\"]").append("\n");
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/FilterByPredicateObject.java`
#### Snippet
```java
					+ getId() + " [label=\"filter source\"]").append("\n");
		} else {
			stringBuilder.append(System.identityHashCode(connection) + " -> " + getId() + " [label=\"filter source\"]")
					.append("\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NonUniqueTargetLang.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/NonUniqueTargetLang.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(parent.getId() + " -> " + getId()).append("\n");
		parent.getPlanAsGraphvizDot(stringBuilder);
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java
		if (connection instanceof MemoryStoreConnection) {
			stringBuilder
					.append(System.identityHashCode(((MemoryStoreConnection) connection).getSail()) + " -> " + getId())
					.append("\n");
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/UnorderedSelect.java`
#### Snippet
```java
					.append("\n");
		} else {
			stringBuilder.append(System.identityHashCode(connection) + " -> " + getId()).append("\n");
		}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(leftNode.getId() + " -> " + getId() + " [label=\"left\"]").append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");
		stringBuilder.append(leftNode.getId() + " -> " + getId() + " [label=\"left\"]").append("\n");

		// added/removed connections are always newly minted per plan node, so we instead need to compare the underlying
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
		// sail
		if (connection instanceof MemoryStoreConnection) {
			stringBuilder.append(System.identityHashCode(((MemoryStoreConnection) connection).getSail()) + " -> "
					+ getId() + " [label=\"right\"]").append("\n");
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java
					+ getId() + " [label=\"right\"]").append("\n");
		} else {
			stringBuilder.append(System.identityHashCode(connection) + " -> " + getId() + " [label=\"right\"]")
					.append("\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalInnerJoin.java`
#### Snippet
```java

			stringBuilder
					.append(System.identityHashCode(previousStateConnection) + " -> " + getId()
							+ " [label=\"skip if not present\"]")
					.append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
		printed = true;

		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
		// sail
		if (connection instanceof MemoryStoreConnection) {
			stringBuilder.append(System.identityHashCode(((MemoryStoreConnection) connection).getSail()) + " -> "
					+ getId() + " [label=\"right\"]").append("\n");
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
					+ getId() + " [label=\"right\"]").append("\n");
		} else {
			stringBuilder.append(System.identityHashCode(connection) + " -> " + getId() + " [label=\"right\"]")
					.append("\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BulkedExternalLeftOuterJoin.java`
#### Snippet
```java
		}

		stringBuilder.append(leftNode.getId() + " -> " + getId() + " [label=\"left\"]").append("\n");

	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValuesBackedNode.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
		}
		printed = true;
		stringBuilder.append(getId() + " [label=\"" + StringEscapeUtils.escapeJava(this.toString()) + "\"];")
				.append("\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
		if (connection instanceof MemoryStoreConnection) {
			stringBuilder
					.append(System.identityHashCode(((MemoryStoreConnection) connection).getSail()) + " -> " + getId())
					.append("\n");
		} else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
					.append("\n");
		} else {
			stringBuilder.append(System.identityHashCode(connection) + " -> " + getId()).append("\n");
		}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("SELECT ?Resource ?Score ?Snippet \n");
			buffer.append("WHERE { \n");
			buffer.append("  ?Resource <" + MATCHES + "> [\n ");
			buffer.append("    <" + QUERY + "> \"charly\"; ");
			buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("WHERE { \n");
			buffer.append("  ?Resource <" + MATCHES + "> [\n ");
			buffer.append("    <" + QUERY + "> \"charly\"; ");
			buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
			buffer.append("    <" + SNIPPET + "> ?Snippet; ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("  ?Resource <" + MATCHES + "> [\n ");
			buffer.append("    <" + QUERY + "> \"charly\"; ");
			buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
			buffer.append("    <" + SNIPPET + "> ?Snippet; ");
			buffer.append("    <" + SCORE + "> ?Score ]. } ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("    <" + QUERY + "> \"charly\"; ");
			buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
			buffer.append("    <" + SNIPPET + "> ?Snippet; ");
			buffer.append("    <" + SCORE + "> ?Score ]. } ");
			String q = buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
			buffer.append("    <" + SNIPPET + "> ?Snippet; ");
			buffer.append("    <" + SCORE + "> ?Score ]. } ");
			String q = buffer.toString();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("SELECT ?Resource ?Score ?Snippet \n");
			buffer.append("WHERE { \n");
			buffer.append("  ?Resource <" + MATCHES + "> [\n ");
			buffer.append("    <" + QUERY + "> \"charly\"; ");
			buffer.append("    <" + SNIPPET + "> ?Snippet; ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("WHERE { \n");
			buffer.append("  ?Resource <" + MATCHES + "> [\n ");
			buffer.append("    <" + QUERY + "> \"charly\"; ");
			buffer.append("    <" + SNIPPET + "> ?Snippet; ");
			buffer.append("    <" + SCORE + "> ?Score ].} ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("  ?Resource <" + MATCHES + "> [\n ");
			buffer.append("    <" + QUERY + "> \"charly\"; ");
			buffer.append("    <" + SNIPPET + "> ?Snippet; ");
			buffer.append("    <" + SCORE + "> ?Score ].} ");
			String q = buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			buffer.append("    <" + QUERY + "> \"charly\"; ");
			buffer.append("    <" + SNIPPET + "> ?Snippet; ");
			buffer.append("    <" + SCORE + "> ?Score ].} ");
			String q = buffer.toString();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		query.append("PREFIX lucenesail: <" + LuceneSailSchema.NAMESPACE + "> \n");
		query.append("CONSTRUCT { \n");
		query.append("    ?r <" + PREDICATE_3 + "> ?r2 ;\n");
		query.append("       <" + score + "> ?s } \n");
		query.append("WHERE { \n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		query.append("CONSTRUCT { \n");
		query.append("    ?r <" + PREDICATE_3 + "> ?r2 ;\n");
		query.append("       <" + score + "> ?s } \n");
		query.append("WHERE { \n");
		query.append("    ?r lucenesail:matches ?match. ?match lucenesail:query \"four\"; \n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		query.append("    ?r lucenesail:matches ?match. ?match lucenesail:query \"four\"; \n");
		query.append("                                         lucenesail:score ?s. \n");
		query.append("    ?r <" + PREDICATE_3 + "> ?r2. }\n");

		int r = 0;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Resource ?Matching ?Score ");
		buffer.append("WHERE { ?Resource <" + PREDICATE_3 + "> ?Matching. ");
		buffer.append(" ?Matching <" + MATCHES + "> [ ");
		buffer.append(" <" + QUERY + "> \"one\"; ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("SELECT ?Resource ?Matching ?Score ");
		buffer.append("WHERE { ?Resource <" + PREDICATE_3 + "> ?Matching. ");
		buffer.append(" ?Matching <" + MATCHES + "> [ ");
		buffer.append(" <" + QUERY + "> \"one\"; ");
		buffer.append(" <" + SCORE + "> ?Score ]. } ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("WHERE { ?Resource <" + PREDICATE_3 + "> ?Matching. ");
		buffer.append(" ?Matching <" + MATCHES + "> [ ");
		buffer.append(" <" + QUERY + "> \"one\"; ");
		buffer.append(" <" + SCORE + "> ?Score ]. } ");
		String q = buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append(" ?Matching <" + MATCHES + "> [ ");
		buffer.append(" <" + QUERY + "> \"one\"; ");
		buffer.append(" <" + SCORE + "> ?Score ]. } ");
		String q = buffer.toString();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("SELECT ?Resource ?Score \n");
		buffer.append("WHERE {\n");
		buffer.append("  ?Resource <" + MATCHES + "> [\n ");
		buffer.append("    <" + QUERY + "> \"one\"; ");
		buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("WHERE {\n");
		buffer.append("  ?Resource <" + MATCHES + "> [\n ");
		buffer.append("    <" + QUERY + "> \"one\"; ");
		buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
		buffer.append("    <" + SCORE + "> ?Score ].} ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("  ?Resource <" + MATCHES + "> [\n ");
		buffer.append("    <" + QUERY + "> \"one\"; ");
		buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
		buffer.append("    <" + SCORE + "> ?Score ].} ");
		String q = buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("    <" + QUERY + "> \"one\"; ");
		buffer.append("    <" + PROPERTY + "> <" + PREDICATE_1 + ">; ");
		buffer.append("    <" + SCORE + "> ?Score ].} ");
		String q = buffer.toString();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Subject ?Score\n");
		buffer.append("WHERE { ?Subject <" + MATCHES + "> [\n");
		buffer.append("                              <" + QUERY + "> ?Query; \n");
		buffer.append("                              <" + SCORE + "> ?Score ].}");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("SELECT ?Subject ?Score\n");
		buffer.append("WHERE { ?Subject <" + MATCHES + "> [\n");
		buffer.append("                              <" + QUERY + "> ?Query; \n");
		buffer.append("                              <" + SCORE + "> ?Score ].}");
		QUERY_STRING = buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("WHERE { ?Subject <" + MATCHES + "> [\n");
		buffer.append("                              <" + QUERY + "> ?Query; \n");
		buffer.append("                              <" + SCORE + "> ?Score ].}");
		QUERY_STRING = buffer.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("SELECT ?Resource ?Score \n");
		buffer.append("WHERE { \n");
		buffer.append("  ?Resource <" + MATCHES + "> [\n");
		buffer.append("    <" + QUERY + "> \"one~0.8\"; ");
		buffer.append("    <" + SCORE + "> ?Score ].} ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("WHERE { \n");
		buffer.append("  ?Resource <" + MATCHES + "> [\n");
		buffer.append("    <" + QUERY + "> \"one~0.8\"; ");
		buffer.append("    <" + SCORE + "> ?Score ].} ");
		String q = buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("  ?Resource <" + MATCHES + "> [\n");
		buffer.append("    <" + QUERY + "> \"one~0.8\"; ");
		buffer.append("    <" + SCORE + "> ?Score ].} ");
		String q = buffer.toString();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		StringBuilder buffer = new StringBuilder();
		buffer.append("SELECT ?Resource ?Matching ?Score ");
		buffer.append("WHERE { ?Resource <" + PREDICATE_3 + "> ?Matching. ");
		buffer.append(" ?Matching <" + MATCHES + "> [ ");
		buffer.append(" <" + QUERY + "> \"two\"; ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("SELECT ?Resource ?Matching ?Score ");
		buffer.append("WHERE { ?Resource <" + PREDICATE_3 + "> ?Matching. ");
		buffer.append(" ?Matching <" + MATCHES + "> [ ");
		buffer.append(" <" + QUERY + "> \"two\"; ");
		buffer.append(" <" + SCORE + "> ?Score ]. } ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("WHERE { ?Resource <" + PREDICATE_3 + "> ?Matching. ");
		buffer.append(" ?Matching <" + MATCHES + "> [ ");
		buffer.append(" <" + QUERY + "> \"two\"; ");
		buffer.append(" <" + SCORE + "> ?Score ]. } ");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append(" ?Matching <" + MATCHES + "> [ ");
		buffer.append(" <" + QUERY + "> \"two\"; ");
		buffer.append(" <" + SCORE + "> ?Score ]. } ");

		String q = buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("SELECT ?Resource ?Property \n");
		buffer.append("WHERE {\n");
		buffer.append("  ?Resource <" + MATCHES + "> [\n ");
		buffer.append("    <" + QUERY + "> \"one\";\n ");
		buffer.append("    <" + PROPERTY + "> ?Property ]. } ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("WHERE {\n");
		buffer.append("  ?Resource <" + MATCHES + "> [\n ");
		buffer.append("    <" + QUERY + "> \"one\";\n ");
		buffer.append("    <" + PROPERTY + "> ?Property ]. } ");
		String q = buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		buffer.append("  ?Resource <" + MATCHES + "> [\n ");
		buffer.append("    <" + QUERY + "> \"one\";\n ");
		buffer.append("    <" + PROPERTY + "> ?Property ]. } ");
		String q = buffer.toString();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java
		IRI newGraph = f.createIRI(EX_NS, "new-graph");

		update.append("CREATE GRAPH <" + newGraph + "> ");

		Update operation = con.prepareUpdate(QueryLanguage.SPARQL, update.toString());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
			char c = unescaped.charAt(i);
			if (!isUnreserved(c) && !reserved.contains(c)) {
				escaped.append("%" + Integer.toHexString((int) c));
			} else {
				escaped.append(c);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/query/src/main/java/org/eclipse/rdf4j/query/explanation/GenericPlanNode.java`
#### Snippet
```java
		} else {
			plans.forEach(
					child -> sb.append(Arrays.stream(child.getHumanReadable(prettyBoxDrawingType + 1).split(newLine))
							.map(c -> "   " + c)
							.reduce((a, b) -> a + newLine + b)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
	private void appendStartTag(String qName, Attributes attributes) {
		// Write start of start tag
		charBuf.append("<" + qName);

		// Write any new namespace prefix definitions
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
	 */
	private void appendEndTag(String qName) {
		charBuf.append("</" + qName + ">");
	}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
				if ((ch = str.charAt(i)) < 0x20 || ch > 0x7e) {
					String s = "0000" + Integer.toString(ch, 16);
					retval.append("\\u" + s.substring(s.length() - 4, s.length()));
				} else {
					retval.append(ch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/datetime/Timezone.java`
#### Snippet
```java
						tzDuration.append("PT");
						if (hours > 0) {
							tzDuration.append(hours + "H");
						}
						if (minutes > 0) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/datetime/Timezone.java`
#### Snippet
```java
						}
						if (minutes > 0) {
							tzDuration.append(minutes + "M");
						}
						if (timezoneOffset == 0) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/QuerySpec.java`
#### Snippet
```java
		StringBuilder buffer = new StringBuilder();
		buffer.append("QuerySpec\n");
		buffer.append("   queryString=\"" + queryString + "\"\n");
		buffer.append("   propertyURI=" + propertyURI + "\n");
		buffer.append("   subject=" + subject + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/QuerySpec.java`
#### Snippet
```java
		buffer.append("QuerySpec\n");
		buffer.append("   queryString=\"" + queryString + "\"\n");
		buffer.append("   propertyURI=" + propertyURI + "\n");
		buffer.append("   subject=" + subject + "\n");
		append(matchesPattern, buffer);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/QuerySpec.java`
#### Snippet
```java
		buffer.append("   queryString=\"" + queryString + "\"\n");
		buffer.append("   propertyURI=" + propertyURI + "\n");
		buffer.append("   subject=" + subject + "\n");
		append(matchesPattern, buffer);
		append(queryPattern, buffer);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/SPARQLQueryRenderer.java`
#### Snippet
```java
						}

						aQuery.append("?" + aElem.getName());
					}
				}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
					ValueExpr expr = valueMap.get(elem.getName());
					if (expr instanceof BNodeGenerator) {
						builder.append("_:" + elem.getName());
					} else {
						valueMap.get(elem.getName()).visit(this);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
					}
				} else {
					builder.append("?" + elem.getName());
				}
				builder.append(" ");
```

## RuleId[id=IOResource]
### IOResource
'LmdbRecordIterator' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TripleStore.java`
#### Snippet
```java
						RecordIterator[] sourceIter = { null };
						try {
							sourceIter[0] = new LmdbRecordIterator(pool, sourceIndex, false, -1, -1, -1, -1,
									explicit, txnManager.createTxn(txn));

```

### IOResource
'BufferedInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java
			throws IOException, RDFParseException, RDFHandlerException {
		if (!in.markSupported()) {
			in = new BufferedInputStream(in, 1024);
		}

```

## RuleId[id=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Array `defaultIgnoredTests` is always empty
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL12QueryComplianceTest.java`
#### Snippet
```java
	public void setUp() throws Exception {
		super.setUp();
		for (String defaultIgnoredTest : defaultIgnoredTests) {
			addIgnoredTest(defaultIgnoredTest);
		}
```

### RedundantOperationOnEmptyContainer
Collection `emptySet` is always empty
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
		if (!isResourceURIResource(subj, pred, ctx)) {
			Set<Statement> emptySet = Collections.emptySet();
			return emptySet.iterator();
		}
		StatementTree tree = choose(subj, pred, obj, ctx);
```

## RuleId[id=OptionalIsPresent]
### OptionalIsPresent
Can be replaced with single expression in functional style
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailValidationReportHelper.java`
#### Snippet
```java
	public static void printValidationReport(Throwable t, OutputStream out) {
		Optional<Model> reportOpt = getValidationReport(t);
		if (reportOpt.isPresent()) {
			Rio.write(reportOpt.get(), out, RDFFormat.TURTLE, WRITER_CONFIG);
		}
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/config/CustomGraphQueryInferencerConfig.java`
#### Snippet
```java

			Optional<Resource> object = Models.objectResource(m.getStatements(implNode, RULE_QUERY, null));
			if (object.isPresent()) {
				Models.objectLiteral(m.getStatements(object.get(), SP.TEXT_PROPERTY, null))
						.ifPresent(lit -> setRuleQuery(lit.stringValue()));
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/config/CustomGraphQueryInferencerConfig.java`
#### Snippet
```java

			object = Models.objectResource(m.getStatements(implNode, MATCHER_QUERY, null));
			if (object.isPresent()) {
				Models.objectLiteral(m.getStatements(object.get(), SP.TEXT_PROPERTY, null))
						.ifPresent(lit -> setMatcherQuery(lit.stringValue()));
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/RioFileTypeDetector.java`
#### Snippet
```java
		Optional<RDFFormat> result = Rio.getParserFormatForFileName(path.getFileName().toString());

		if (result.isPresent()) {
			return result.get().getDefaultMIMEType();
		}
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/service/ServiceRegistry.java`
#### Snippet
```java
					Optional<S> oldService = add(service);

					if (oldService.isPresent()) {
						logger.warn("New service {} replaces existing service {}", service.getClass(),
								oldService.get().getClass());
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/LexicalValueComparator.java`
#### Snippet
```java

			if (leftLanguage.isPresent()) {
				if (rightLanguage.isPresent()) {
					result = leftLanguage.get().compareTo(rightLanguage.get());
				} else {
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/config/SailRepositoryConfig.java`
#### Snippet
```java
		try {
			Optional<Resource> sailImplNode = Models.objectResource(model.getStatements(repImplNode, SAILIMPL, null));
			if (sailImplNode.isPresent()) {
				Models.objectLiteral(model.getStatements(sailImplNode.get(), SAILTYPE, null)).ifPresent(typeLit -> {
					SailFactory factory = SailRegistry.getInstance()
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/FilteredModel.java`
#### Snippet
```java
	private boolean matches(Resource[] stContext, Resource... contexts) {
		Objects.requireNonNull(stContext);
		if (stContext.length > 0) {
			for (Resource c : stContext) {
				if (!matches(c, contexts)) {
```

### RedundantLengthCheck
Redundant array length check
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	private static boolean contentTypeIs(HttpResponse response, String contentType) {
		Header[] headers = response.getHeaders("Content-Type");
		if (headers.length == 0) {
			return false;
		}
```

## RuleId[id=FieldMayBeStatic]
### FieldMayBeStatic
Field `maxPendingSize` may be 'static'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
	private Model pendingRemoves;

	private final int maxPendingSize = DEFAULT_MAX_PENDING_SIZE;

	private final boolean quadMode;
```

### FieldMayBeStatic
Field `size` may be 'static'
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchHelper.java`
#### Snippet
```java
			String scrollId;
			long itemsRetrieved = 0;
			final int size = 1000;

			{
```

### FieldMayBeStatic
Field `indentString` may be 'static'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/QueryModelTreePrinter.java`
#### Snippet
```java
	 *-----------*/

	private final String indentString = "   ";

	private final StringBuilder sb;
```

### FieldMayBeStatic
Field `enableMultiThreading` may be 'static'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java
	private volatile boolean nextTransactionAsync;

	private final boolean enableMultiThreading = true;

	/**
```

### FieldMayBeStatic
Field `STATEMENTS_PER_CACHE_ITEM_LIMIT` may be 'static'
in `core/sail/extensible-store/src/main/java/org/eclipse/rdf4j/sail/extensiblestore/ReadCache.java`
#### Snippet
```java
	DataStructureInterface delegate;

	final int STATEMENTS_PER_CACHE_ITEM_LIMIT = 100000;

	// A soft values-based cache of unlimited size. We currently handle retention and removal manually.
```

### FieldMayBeStatic
Field `query1` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/SparqlOrderByTest.java`
#### Snippet
```java
	}

	private final String query1 = "PREFIX foaf:    <http://xmlns.com/foaf/0.1/>\n" + "SELECT ?name\n"
			+ "WHERE { ?x foaf:name ?name }\n" + "ORDER BY ?name\n";

```

### FieldMayBeStatic
Field `query2` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/SparqlOrderByTest.java`
#### Snippet
```java
			+ "WHERE { ?x foaf:name ?name }\n" + "ORDER BY ?name\n";

	private final String query2 = "PREFIX     :    <http://example.org/ns#>\n"
			+ "PREFIX foaf:    <http://xmlns.com/foaf/0.1/>\n" + "PREFIX xsd:     <http://www.w3.org/2001/XMLSchema#>\n"
			+ "SELECT ?name\n" + "WHERE { ?x foaf:name ?name ; :empId ?emp }\n" + "ORDER BY DESC(?emp)\n";
```

### FieldMayBeStatic
Field `query3` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/SparqlOrderByTest.java`
#### Snippet
```java
			+ "SELECT ?name\n" + "WHERE { ?x foaf:name ?name ; :empId ?emp }\n" + "ORDER BY DESC(?emp)\n";

	private final String query3 = "PREFIX     :    <http://example.org/ns#>\n"
			+ "PREFIX foaf:    <http://xmlns.com/foaf/0.1/>\n" + "SELECT ?name\n"
			+ "WHERE { ?x foaf:name ?name ; :empId ?emp }\n" + "ORDER BY ?name DESC(?emp)\n";
```

### FieldMayBeStatic
Field `NS` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/DeleteInsertTest.java`
#### Snippet
```java
	private Repository repo;

	private final String NS = "http://example.org/";

	private RepositoryConnection con;
```

### FieldMayBeStatic
Field `NS` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/DeadLockTest.java`
#### Snippet
```java
	private final IsolationLevel level = IsolationLevels.SNAPSHOT_READ;

	private final String NS = "http://rdf.example.org/";

	private IRI PAINTER;
```

### FieldMayBeStatic
Field `NS` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/LinearTest.java`
#### Snippet
```java
	private final IsolationLevel level = IsolationLevels.READ_COMMITTED;

	private final String NS = "http://rdf.example.org/";

	private ValueFactory lf;
```

### FieldMayBeStatic
Field `NS` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/ModificationTest.java`
#### Snippet
```java
	private final IsolationLevel level = IsolationLevels.SNAPSHOT_READ;

	private final String NS = "http://rdf.example.org/";

	private IRI PAINTER;
```

### FieldMayBeStatic
Field `NS` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/MonotonicTest.java`
#### Snippet
```java
	private final IsolationLevel level = IsolationLevels.SNAPSHOT_READ;

	private final String NS = "http://rdf.example.org/";

	private ValueFactory lf;
```

### FieldMayBeStatic
Field `NS` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SerializableTest.java`
#### Snippet
```java
	private final IsolationLevel level = IsolationLevels.SERIALIZABLE;

	private final String NS = "http://rdf.example.org/";

	private ValueFactory lf;
```

### FieldMayBeStatic
Field `NS` may be 'static'
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
	private final IsolationLevel level = IsolationLevels.SNAPSHOT;

	private final String NS = "http://rdf.example.org/";

	private ValueFactory lf;
```

### FieldMayBeStatic
Field `blockSize` may be 'static'
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/datastore/IDFile.java`
#### Snippet
```java

	// We choose a cacheLineSize of 4KB since this is a typical file system block size.
	private final int blockSize = 4 * 1024; // 4KB
	private final int cacheLineSize = (int) (blockSize / ITEM_SIZE);
	private final int cacheLineShift = LongMath.log2(cacheLineSize, RoundingMode.UNNECESSARY);
```

### FieldMayBeStatic
Field `queryAnalyzer` may be 'static'
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
	private String analyzer;

	private final String queryAnalyzer = "standard";

	private Function<? super String, ? extends SpatialContext> geoContextMapper;
```

## RuleId[id=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `BaseSailSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/config/BaseSailSchema.java`
#### Snippet
```java
 * Defines constants for the BaseSail schema.
 */
public class BaseSailSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `BinaryRDFConstants` has only 'static' members, and lacks a 'private' constructor
in `core/rio/binary/src/main/java/org/eclipse/rdf4j/rio/binary/BinaryRDFConstants.java`
#### Snippet
```java
import java.nio.charset.StandardCharsets;

class BinaryRDFConstants {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RDFJSONUtility` has only 'static' members, and lacks a 'private' constructor
in `core/rio/rdfjson/src/main/java/org/eclipse/rdf4j/rio/rdfjson/RDFJSONUtility.java`
#### Snippet
```java
 * @author Peter Ansell
 */
class RDFJSONUtility {

	public static final String NULL = "null";
```

### UtilityClassWithoutPrivateConstructor
Class `QueryStringUtil` has only 'static' members, and lacks a 'private' constructor
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/query/QueryStringUtil.java`
#### Snippet
```java
 * @see SPARQLBooleanQuery
 */
public class QueryStringUtil {

	// TODO maybe add BASE declaration here as well?
```

### UtilityClassWithoutPrivateConstructor
Class `ConnectionHelper` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ConnectionHelper.java`
#### Snippet
```java
import org.eclipse.rdf4j.sail.SailException;

class ConnectionHelper {

	static CloseableIteration<Statement, SailException> getCloseableIteration(
```

### UtilityClassWithoutPrivateConstructor
Class `ShaclSailValidationReportHelper` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailValidationReportHelper.java`
#### Snippet
```java
 */
@InternalUseOnly
public class ShaclSailValidationReportHelper {
	private static final WriterConfig WRITER_CONFIG = new WriterConfig();

```

### UtilityClassWithoutPrivateConstructor
Class `ShaclAstLists` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/ShaclAstLists.java`
#### Snippet
```java
 */
@InternalUseOnly
public class ShaclAstLists {

	public static void listToRdf(Collection<? extends Value> values, Resource head, Model model) {
```

### UtilityClassWithoutPrivateConstructor
Class `SparqlQueryParserCache` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/SparqlQueryParserCache.java`
#### Snippet
```java
import com.google.common.cache.CacheBuilder;

public class SparqlQueryParserCache {

	private static final Cache<String, TupleExpr> PARSER_QUERY_CACHE = CacheBuilder.newBuilder()
```

### UtilityClassWithoutPrivateConstructor
Class `Formatter` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Formatter.java`
#### Snippet
```java
import org.eclipse.rdf4j.model.vocabulary.SHACL;

public class Formatter {

	public static String prefix(Value in) {
```

### UtilityClassWithoutPrivateConstructor
Class `Factory` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
	}

	public static class Factory {

		public static List<ContextWithShapes> getShapes(ShapeSource shapeSource, ShaclSail shaclSail) {
```

### UtilityClassWithoutPrivateConstructor
Class `PlanNodeHelper` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/PlanNodeHelper.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.impl.SimpleDataset;

public class PlanNodeHelper {

	private final static SimpleDataset rdf4jNilDataset;
```

### UtilityClassWithoutPrivateConstructor
Class `ValidationTupleHelper` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/ValidationTupleHelper.java`
#### Snippet
```java
 * @author Håvard Ottestad
 */
public class ValidationTupleHelper {

	public static ValidationTuple join(ValidationTuple left, ValidationTuple right) {
```

### UtilityClassWithoutPrivateConstructor
Class `ShaclSailSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/config/ShaclSailSchema.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class ShaclSailSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ShapeSourceHelper` has only 'static' members, and lacks a 'private' constructor
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/shape/ShapeSourceHelper.java`
#### Snippet
```java
import org.eclipse.rdf4j.sail.SailConnection;

class ShapeSourceHelper {

	static Value getFirst(SailConnection connection, Resource subject, Resource[] context) {
```

### UtilityClassWithoutPrivateConstructor
Class `ElasticsearchStoreSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/config/ElasticsearchStoreSchema.java`
#### Snippet
```java
 * @author Håvard Mikkelsen Ottestad
 */
public class ElasticsearchStoreSchema {

	private static final ValueFactory vf = SimpleValueFactory.getInstance();
```

### UtilityClassWithoutPrivateConstructor
Class `ElasticsearchHelper` has only 'static' members, and lacks a 'private' constructor
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchHelper.java`
#### Snippet
```java
import org.elasticsearch.search.sort.SortOrder;

class ElasticsearchHelper {

	static CloseableIteration<SearchHit, RuntimeException> getScrollingIterator(QueryBuilder queryBuilder,
```

### UtilityClassWithoutPrivateConstructor
Class `TupleExprs` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/TupleExprs.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class TupleExprs {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RDFSRules` has only 'static' members, and lacks a 'private' constructor
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/RDFSRules.java`
#### Snippet
```java
 * added to capture list membership property assertions.
 */
class RDFSRules {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `CustomGraphQueryInferencerSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/config/CustomGraphQueryInferencerSchema.java`
#### Snippet
```java
 * @author Dale Visser
 */
public class CustomGraphQueryInferencerSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `LmdbStoreSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/config/LmdbStoreSchema.java`
#### Snippet
```java
 * {@link LmdbStore}s.
 */
public class LmdbStoreSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `HTTPRepositorySchema` has only 'static' members, and lacks a 'private' constructor
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/config/HTTPRepositorySchema.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class HTTPRepositorySchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `HTTPRepositorySettings` has only 'static' members, and lacks a 'private' constructor
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/helpers/HTTPRepositorySettings.java`
#### Snippet
```java
 * @author Jacek Grzebyta
 */
public class HTTPRepositorySettings {

	private static final Logger log = LoggerFactory.getLogger(HTTPRepositorySettings.class);
```

### UtilityClassWithoutPrivateConstructor
Class `QueryPrologLexer` has only 'static' members, and lacks a 'private' constructor
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/QueryPrologLexer.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class QueryPrologLexer {

	public enum TokenType {
```

### UtilityClassWithoutPrivateConstructor
Class `QueryParserUtil` has only 'static' members, and lacks a 'private' constructor
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/QueryParserUtil.java`
#### Snippet
```java
 * Utility class for creating query parsers and parsing queries in various query languages.
 */
public class QueryParserUtil {

	public static QueryParser createParser(QueryLanguage ql) throws UnsupportedQueryLanguageException {
```

### UtilityClassWithoutPrivateConstructor
Class `RDFStarUtil` has only 'static' members, and lacks a 'private' constructor
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/RDFStarUtil.java`
#### Snippet
```java
 * @author Pavel Mihaylov
 */
public class RDFStarUtil {
	/**
	 * IRI prefix for RDF-star triples encoded as IRIs.
```

### UtilityClassWithoutPrivateConstructor
Class `TurtleParserSettings` has only 'static' members, and lacks a 'private' constructor
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/TurtleParserSettings.java`
#### Snippet
```java
 * @author Peter Ansell
 */
public class TurtleParserSettings {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RDFParserHelper` has only 'static' members, and lacks a 'private' constructor
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/RDFParserHelper.java`
#### Snippet
```java
 * @author Peter Ansell
 */
public class RDFParserHelper {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SPARQL10ManifestTest` has only 'static' members, and lacks a 'private' constructor
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL10ManifestTest.java`
#### Snippet
```java

@Deprecated
public class SPARQL10ManifestTest {

	static final Logger logger = LoggerFactory.getLogger(SPARQL10ManifestTest.class);
```

### UtilityClassWithoutPrivateConstructor
Class `SPARQL11ManifestTest` has only 'static' members, and lacks a 'private' constructor
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11ManifestTest.java`
#### Snippet
```java
 */
@Deprecated
public class SPARQL11ManifestTest {

	static final Logger logger = LoggerFactory.getLogger(SPARQL11ManifestTest.class);
```

### UtilityClassWithoutPrivateConstructor
Class `Rio` has only 'static' members, and lacks a 'private' constructor
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/Rio.java`
#### Snippet
```java
 * @author Peter Ansell
 */
public class Rio {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `EX` has only 'static' members, and lacks a 'private' constructor
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/vocabulary/EX.java`
#### Snippet
```java
 *
 */
public class EX {

	public static final String NAMESPACE = "http://example.org/";
```

### UtilityClassWithoutPrivateConstructor
Class `IsolationLevelAdapter` has only 'static' members, and lacks a 'private' constructor
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/IsolationLevelAdapter.java`
#### Snippet
```java
 * @since 4.0.0
 */
public class IsolationLevelAdapter {
	static IsolationLevel adaptToRdfIsolation(Sail sail, int springIsolation) {
		switch (springIsolation) {
```

### UtilityClassWithoutPrivateConstructor
Class `ExceptionMapper` has only 'static' members, and lacks a 'private' constructor
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/exception/mapper/ExceptionMapper.java`
#### Snippet
```java
 * @since 4.0.0
 */
public class ExceptionMapper {

	public static RDF4JSpringException mapException(String message, Exception e) {
```

### UtilityClassWithoutPrivateConstructor
Class `OperationUtils` has only 'static' members, and lacks a 'private' constructor
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/operation/OperationUtils.java`
#### Snippet
```java
 * @since 4.0.0
 */
public class OperationUtils {
	private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

```

### UtilityClassWithoutPrivateConstructor
Class `RepositoryConnectionWrappingUtils` has only 'static' members, and lacks a 'private' constructor
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/RepositoryConnectionWrappingUtils.java`
#### Snippet
```java
 * @since 4.0.0
 */
public class RepositoryConnectionWrappingUtils {
	private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

```

### UtilityClassWithoutPrivateConstructor
Class `TypeMappingUtils` has only 'static' members, and lacks a 'private' constructor
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/TypeMappingUtils.java`
#### Snippet
```java
 * @since 4.0.0
 */
public class TypeMappingUtils {
	public static Iri toIri(IRI from) {
		return Rdf.iri(from.toString());
```

### UtilityClassWithoutPrivateConstructor
Class `ObjectMapUtils` has only 'static' members, and lacks a 'private' constructor
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/ObjectMapUtils.java`
#### Snippet
```java
 * @since 4.0.0
 */
public class ObjectMapUtils {
	public static IRI getIRI(Map<String, Object> map, String key) {
		return toIRI((String) map.get(key));
```

### UtilityClassWithoutPrivateConstructor
Class `QueryResultUtils` has only 'static' members, and lacks a 'private' constructor
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/util/QueryResultUtils.java`
#### Snippet
```java
 */
@Experimental
public class QueryResultUtils {

	public static Optional<Value> getValueOptional(BindingSet resultRow, String varName) {
```

### UtilityClassWithoutPrivateConstructor
Class `DocumentUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/DocumentUtil.java`
#### Snippet
```java
 * @author Herko ter Horst
 */
public class DocumentUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `XMLReaderFactory` has only 'static' members, and lacks a 'private' constructor
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLReaderFactory.java`
#### Snippet
```java
 * falls back on the Xerces 2 SAX Parser.
 */
public class XMLReaderFactory {

	public static final String XERCES_SAXPARSER = "org.apache.xerces.parsers.SAXParser";
```

### UtilityClassWithoutPrivateConstructor
Class `XMLUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLUtil.java`
#### Snippet
```java
 * Utility methods for handling and processing XML data.
 */
public class XMLUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `CoreDatatypeHelper` has only 'static' members, and lacks a 'private' constructor
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/CoreDatatypeHelper.java`
#### Snippet
```java

@InternalUseOnly
class CoreDatatypeHelper {

	private static Map<IRI, CoreDatatype> reverseLookup;
```

### UtilityClassWithoutPrivateConstructor
Class `MemoryStoreSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/config/MemoryStoreSchema.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class MemoryStoreSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `FileUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/FileUtil.java`
#### Snippet
```java
 * Utility methods for operations on Files.
 */
public class FileUtil {
	/**
	 * Deletes the specified directory and any files and directories in it recursively.
```

### UtilityClassWithoutPrivateConstructor
Class `GZipUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/GZipUtil.java`
#### Snippet
```java
 * GZip-related utilities.
 */
public class GZipUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `MavenUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/MavenUtil.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class MavenUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ResourceUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ResourceUtil.java`
#### Snippet
```java
 * ResourceUtil is a utility class for retrieving resources (images, property-files, etc) from the classpath.
 */
public class ResourceUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ZipUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ZipUtil.java`
#### Snippet
```java
 * Zip-related utilities.
 */
public class ZipUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ObjectUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/ObjectUtil.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class ObjectUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ByteArrayUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/ByteArrayUtil.java`
#### Snippet
```java
 * Class providing utility methods for handling byte arrays.
 */
public class ByteArrayUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `IOUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
 * Utility methods for I/O working with Readers, Writers, InputStreams and OutputStreams.
 */
public class IOUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SPARQLStarResultsJSONConstants` has only 'static' members, and lacks a 'private' constructor
in `core/queryresultio/sparqljson/src/main/java/org/eclipse/rdf4j/query/resultio/sparqljson/SPARQLStarResultsJSONConstants.java`
#### Snippet
```java
 * @see <a href="https://w3c.github.io/rdf-star/cg-spec/">RDF-star and SPARQL-star Draft Community Group Report</a>
 */
final class SPARQLStarResultsJSONConstants {
	static TupleQueryResultFormat QUERY_RESULT_FORMAT = TupleQueryResultFormat.JSON_STAR;

```

### UtilityClassWithoutPrivateConstructor
Class `HttpClientBuilders` has only 'static' members, and lacks a 'private' constructor
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/util/HttpClientBuilders.java`
#### Snippet
```java
 * @see HttpClientDependent
 */
public class HttpClientBuilders {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SolrUtil` has only 'static' members, and lacks a 'private' constructor
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrUtil.java`
#### Snippet
```java
 * Utility for Solr handling
 */
public class SolrUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `URIUtil` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class URIUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Literals` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
 * @See {@link Values}
 */
public class Literals {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Vocabularies` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Vocabularies.java`
#### Snippet
```java
 * @author Bart Hanssens
 */
public class Vocabularies {
	/**
	 * Get all the {@link IRI IRIs} of the classes and properties of a vocabulary.
```

### UtilityClassWithoutPrivateConstructor
Class `GraphComparisons` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
 *           blank nodes. ACM Transactions on the Web (TWEB), 11(4), 1-62.
 */
class GraphComparisons {

	private static final Logger logger = LoggerFactory.getLogger(GraphComparisons.class);
```

### UtilityClassWithoutPrivateConstructor
Class `RDFContainers` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/rdf-schema/#ch_container">RDF Schema 1.1 section on Collection vocabulary</a>
 */
public class RDFContainers {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `HDTDictionarySectionFactory` has only 'static' members, and lacks a 'private' constructor
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTDictionarySectionFactory.java`
#### Snippet
```java
 * @author Bart Hanssens
 */
class HDTDictionarySectionFactory {
	/**
	 * Create a dictionary section from input stream. The name an starting position are provided for debugging purposes.
```

### UtilityClassWithoutPrivateConstructor
Class `HDTTriplesSectionFactory` has only 'static' members, and lacks a 'private' constructor
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriplesSectionFactory.java`
#### Snippet
```java
 * @author Bart Hanssens
 */
class HDTTriplesSectionFactory {

	protected static HDTTriplesSection parse(String str) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `HDTArrayFactory` has only 'static' members, and lacks a 'private' constructor
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTArrayFactory.java`
#### Snippet
```java
 * @author Bart Hanssens
 */
class HDTArrayFactory {

	protected static HDTArray parse(InputStream is) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `VByte` has only 'static' members, and lacks a 'private' constructor
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/VByte.java`
#### Snippet
```java
 * @see <a href="https://nlp.stanford.edu/IR-book/html/htmledition/variable-byte-codes-1.html">Variable byte codes</a>
 */
public class VByte {
	/**
	 * Checks if the most significant bit is set. If this bit is zero, then the next byte must also be read to decode
```

### UtilityClassWithoutPrivateConstructor
Class `Statements` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Statements {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `TurtleUtil` has only 'static' members, and lacks a 'private' constructor
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
 * @see <a href="http://www.w3.org/TR/turtle/">Turtle: Terse RDF Triple Language</a>
 */
public class TurtleUtil {

	private static final Logger logger = LoggerFactory.getLogger(TurtleUtil.class);
```

### UtilityClassWithoutPrivateConstructor
Class `RDFCollections` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFCollections.java`
#### Snippet
```java
 *      vocabulary</a>
 */
public class RDFCollections {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `XMLDatatypeUtil` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class XMLDatatypeUtil {

	private static final IllegalArgumentExceptionWithoutStackTrace VALUE_SMALLER_THAN_MINIMUM_VALUE_EXCEPTION = new IllegalArgumentExceptionWithoutStackTrace(
```

### UtilityClassWithoutPrivateConstructor
Class `PropertyPaths` has only 'static' members, and lacks a 'private' constructor
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/PropertyPaths.java`
#### Snippet
```java
 */
@Deprecated(since = "4.0")
public class PropertyPaths {
	/**
	 * Construct property paths for use with the {@link SparqlBuilder}
```

### UtilityClassWithoutPrivateConstructor
Class `SparqlBuilderUtils` has only 'static' members, and lacks a 'private' constructor
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/util/SparqlBuilderUtils.java`
#### Snippet
```java
 * Utility functions for the SparqlBuilder
 */
public class SparqlBuilderUtils {
	private static final String PAD = " ";

```

### UtilityClassWithoutPrivateConstructor
Class `ContextAwareSchema` has only 'static' members, and lacks a 'private' constructor
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
 * @author James Leigh
 */
public class ContextAwareSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Models` has only 'static' members, and lacks a 'private' constructor
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
 * @see org.eclipse.rdf4j.model.util.ModelBuilder
 */
public class Models {

	/*
```

### UtilityClassWithoutPrivateConstructor
Class `Iterators` has only 'static' members, and lacks a 'private' constructor
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/Iterators.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0", forRemoval = true)
public class Iterators {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RepositoryUtil` has only 'static' members, and lacks a 'private' constructor
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RepositoryUtil.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class RepositoryUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Connections` has only 'static' members, and lacks a 'private' constructor
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Connections.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Connections {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RepositoryConfigSchema` has only 'static' members, and lacks a 'private' constructor
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigSchema.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class RepositoryConfigSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RepositoryConfigUtil` has only 'static' members, and lacks a 'private' constructor
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/config/RepositoryConfigUtil.java`
#### Snippet
```java
import org.eclipse.rdf4j.repository.RepositoryResult;

public class RepositoryConfigUtil {

	public static RepositoryConfig getRepositoryConfig(Model model, String repositoryID) {
```

### UtilityClassWithoutPrivateConstructor
Class `RepositoryProvider` has only 'static' members, and lacks a 'private' constructor
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryProvider.java`
#### Snippet
```java
 * @author James Leigh
 */
public class RepositoryProvider {

	private static final String REPOSITORIES = "repositories/";
```

### UtilityClassWithoutPrivateConstructor
Class `ASCIIUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/text/src/main/java/org/eclipse/rdf4j/common/text/ASCIIUtil.java`
#### Snippet
```java
 * Utility methods for ASCII character checking.
 */
public class ASCIIUtil {
	/**
	 * Checks whether the supplied character is a letter or number.
```

### UtilityClassWithoutPrivateConstructor
Class `StringUtil` has only 'static' members, and lacks a 'private' constructor
in `core/common/text/src/main/java/org/eclipse/rdf4j/common/text/StringUtil.java`
#### Snippet
```java
import java.util.Locale;

public class StringUtil {
	private static final char[] IRI_DONT_ESCAPE = new char[] { '_', '~', '.', '-', '!', '$', '&', '\'', '(',
			')', '*', '+', ',', ';', '=', ':', '/', '?', '#', '@', '%', '[', ']' };
```

### UtilityClassWithoutPrivateConstructor
Class `Example07WriteTurtle` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example07WriteTurtle.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example07WriteTurtle {

	public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `Example03LiteralDatatypes` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example03LiteralDatatypes.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example03LiteralDatatypes {

	public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `Example10PropertyValues` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example10PropertyValues.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example10PropertyValues {

	public static void main(String[] args) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `Example06WriteRdfXml` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example06WriteRdfXml.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example06WriteRdfXml {

	public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `Example05BlankNode` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example05BlankNode.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example05BlankNode {

	public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `Example01BuildModel` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example01BuildModel.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example01BuildModel {

	public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `Example08ReadTurtle` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example08ReadTurtle.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example08ReadTurtle {

	public static void main(String[] args) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `Example12BuildModelWithNamedGraphs` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example12BuildModelWithNamedGraphs.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example12BuildModelWithNamedGraphs {

	public static void main(String[] args)
```

### UtilityClassWithoutPrivateConstructor
Class `Example04LanguageTags` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example04LanguageTags.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example04LanguageTags {

	public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `Example09Filter` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example09Filter.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example09Filter {

	public static void main(String[] args) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `Example02BuildModel` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example02BuildModel.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example02BuildModel {

	public static void main(String[] args) {
```

### UtilityClassWithoutPrivateConstructor
Class `Example11SinglePropertyValue` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/Example11SinglePropertyValue.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example11SinglePropertyValue {

	public static void main(String[] args)
```

### UtilityClassWithoutPrivateConstructor
Class `EX` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/model/vocabulary/EX.java`
#### Snippet
```java
 * various places in your code.
 */
public class EX {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Example16SPARQLConstructQuery` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example16SPARQLConstructQuery.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example16SPARQLConstructQuery {

	public static void main(String[] args)
```

### UtilityClassWithoutPrivateConstructor
Class `Example14AddRDFToDatabase` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example14AddRDFToDatabase.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example14AddRDFToDatabase {

	public static void main(String[] args)
```

### UtilityClassWithoutPrivateConstructor
Class `Example13AddRDFToDatabase` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example13AddRDFToDatabase.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example13AddRDFToDatabase {

	public static void main(String[] args)
```

### UtilityClassWithoutPrivateConstructor
Class `Example15SimpleSPARQLQuery` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/eclipse/rdf4j/examples/repository/Example15SimpleSPARQLQuery.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class Example15SimpleSPARQLQuery {

	public static void main(String[] args)
```

### UtilityClassWithoutPrivateConstructor
Class `SailConfigSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/SailConfigSchema.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class SailConfigSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SailConfigUtil` has only 'static' members, and lacks a 'private' constructor
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/config/SailConfigUtil.java`
#### Snippet
```java
import org.eclipse.rdf4j.model.util.Models;

public class SailConfigUtil {

	public static SailImplConfig parseRepositoryImpl(Model m, Resource implNode) throws SailConfigException {
```

### UtilityClassWithoutPrivateConstructor
Class `SailUtil` has only 'static' members, and lacks a 'private' constructor
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/SailUtil.java`
#### Snippet
```java
 * Defines utility methods for working with Sails.
 */
public class SailUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Properties` has only 'static' members, and lacks a 'private' constructor
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/Properties.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class Properties {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `DAWGTestResultSetSchema` has only 'static' members, and lacks a 'private' constructor
in `core/query/src/main/java/org/eclipse/rdf4j/query/dawg/DAWGTestResultSetSchema.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class DAWGTestResultSetSchema {

	public static final String NAMESPACE = "http://www.w3.org/2001/sw/DataAccess/tests/result-set#";
```

### UtilityClassWithoutPrivateConstructor
Class `DAWGTestResultSetUtil` has only 'static' members, and lacks a 'private' constructor
in `core/query/src/main/java/org/eclipse/rdf4j/query/dawg/DAWGTestResultSetUtil.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class DAWGTestResultSetUtil {

	public static TupleQueryResult toTupleQueryResult(Iterable<? extends Statement> dawgGraph)
```

### UtilityClassWithoutPrivateConstructor
Class `DatasetDeclProcessor` has only 'static' members, and lacks a 'private' constructor
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/DatasetDeclProcessor.java`
#### Snippet
```java
 */
@InternalUseOnly
public class DatasetDeclProcessor {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `PrefixDeclProcessor` has only 'static' members, and lacks a 'private' constructor
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/PrefixDeclProcessor.java`
#### Snippet
```java
 */
@InternalUseOnly
public class PrefixDeclProcessor {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `BaseDeclProcessor` has only 'static' members, and lacks a 'private' constructor
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/BaseDeclProcessor.java`
#### Snippet
```java
 */
@InternalUseOnly
public class BaseDeclProcessor {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `StringEscapesProcessor` has only 'static' members, and lacks a 'private' constructor
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/StringEscapesProcessor.java`
#### Snippet
```java
 */
@InternalUseOnly
public class StringEscapesProcessor {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SailRepositorySchema` has only 'static' members, and lacks a 'private' constructor
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/config/SailRepositorySchema.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class SailRepositorySchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ProxyRepositorySchema` has only 'static' members, and lacks a 'private' constructor
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/config/ProxyRepositorySchema.java`
#### Snippet
```java
 * @author Dale Visser
 */
public class ProxyRepositorySchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `JSONLDHierarchicalProcessor` has only 'static' members, and lacks a 'private' constructor
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDHierarchicalProcessor.java`
#### Snippet
```java
 * @author Yasen Marinov
 */
public class JSONLDHierarchicalProcessor {

	public static final String ID = "@id";
```

### UtilityClassWithoutPrivateConstructor
Class `ReadOnlySparqlApplication` has only 'static' members, and lacks a 'private' constructor
in `spring-components/spring-boot-sparql-web/src/main/java/org/eclipse/rdf4j/http/server/readonly/ReadOnlySparqlApplication.java`
#### Snippet
```java
@Experimental
@SpringBootApplication
public class ReadOnlySparqlApplication {
	public static void main(String[] args) {
		SpringApplication.run(ReadOnlySparqlApplication.class, args);
```

### UtilityClassWithoutPrivateConstructor
Class `QueryResultIO` has only 'static' members, and lacks a 'private' constructor
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultIO.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class QueryResultIO {

	private final static ConcurrentCleaner cleaner = new ConcurrentCleaner();
```

### UtilityClassWithoutPrivateConstructor
Class `NativeStoreSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/config/NativeStoreSchema.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class NativeStoreSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `Main` has only 'static' members, and lacks a 'private' constructor
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/Main.java`
#### Snippet
```java
import org.openjdk.jmh.runner.options.OptionsBuilder;

public class Main {

	public static void main(String[] args) throws RunnerException {
```

### UtilityClassWithoutPrivateConstructor
Class `RDFSizeBenchmarks` has only 'static' members, and lacks a 'private' constructor
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/rio/RDFSizeBenchmarks.java`
#### Snippet
```java
 * @author Frens Jan Rumph
 */
public class RDFSizeBenchmarks {

	public static void main(String[] args) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `RegexValueEvaluationStepSupplier` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/RegexValueEvaluationStepSupplier.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.impl.EmptyBindingSet;

public class RegexValueEvaluationStepSupplier {
	/**
	 * Returns value evaluation steps that determines whether the two operands match according to the <code>regex</code>
```

### UtilityClassWithoutPrivateConstructor
Class `UnboundTest` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/UnboundTest.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.impl.QueryEvaluationContext;

class UnboundTest {
	public static Predicate<BindingSet> s(QueryEvaluationContext context, Var s) {

```

### UtilityClassWithoutPrivateConstructor
Class `MathUtil` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/MathUtil.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class MathUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `StatementConvertor` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/StatementConvertor.java`
#### Snippet
```java
import org.eclipse.rdf4j.query.algebra.evaluation.impl.QueryEvaluationContext;

class StatementConvertor {
	public static BiConsumer<MutableBindingSet, Statement> s(QueryEvaluationContext context, Var s) {
		BiConsumer<Value, MutableBindingSet> setS = context.addBinding(s.getName());
```

### UtilityClassWithoutPrivateConstructor
Class `XMLDatatypeMathUtil` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/XMLDatatypeMathUtil.java`
#### Snippet
```java
 * @author Thomas Pellissier Tanon
 */
public class XMLDatatypeMathUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `LimitedSizeIteratorUtil` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/limited/iterator/LimitedSizeIteratorUtil.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0", forRemoval = true)
public class LimitedSizeIteratorUtil {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `QueryEvaluationUtil` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtil.java`
#### Snippet
```java
 * @author Arjohn Kampman
 */
public class QueryEvaluationUtil {

	public static final ValueExprEvaluationException INDETERMINATE_DATE_TIME_EXCEPTION = new ValueExprEvaluationException(
```

### UtilityClassWithoutPrivateConstructor
Class `QueryEvaluationUtility` has only 'static' members, and lacks a 'private' constructor
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/util/QueryEvaluationUtility.java`
#### Snippet
```java
 */
@InternalUseOnly()
public class QueryEvaluationUtility {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `LuceneSailSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailSchema.java`
#### Snippet
```java
 * LuceneSailSchema defines predicates that can be used for expressing a Lucene query in a RDF query.
 */
public class LuceneSailSchema {

	public static final String NAMESPACE = "http://www.openrdf.org/contrib/lucenesail#";
```

### UtilityClassWithoutPrivateConstructor
Class `SearchIndexUtils` has only 'static' members, and lacks a 'private' constructor
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/SearchIndexUtils.java`
#### Snippet
```java
 * @version 2.3
 */
public class SearchIndexUtils {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `LuceneSailConfigSchema` has only 'static' members, and lacks a 'private' constructor
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/config/LuceneSailConfigSchema.java`
#### Snippet
```java
 * {@link org.eclipse.rdf4j.sail.lucene.config.LuceneSailFactory}s to initialize {@link LuceneSail}s.
 */
public class LuceneSailConfigSchema {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `FN` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FN.java`
#### Snippet
```java
 * @see <a href="http://www.w3.org/TR/xpath-functions/">XPath functions</a>
 */
public class FN {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SKOS` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SKOS.java`
#### Snippet
```java
 * @see <a href="http://www.w3.org/TR/skos-reference/">SKOS Simple Knowledge Organization System Reference</a>
 */
public class SKOS {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `LOCN` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/LOCN.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/ns/locn">ISA Programme Location Core Vocabulary</a>
 */
public class LOCN {
	/**
	 * The LOCN namespace: http://www.w3.org/ns/locn#
```

### UtilityClassWithoutPrivateConstructor
Class `SESAMEQNAME` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SESAMEQNAME.java`
#### Snippet
```java
 * @author Peter Ansell
 */
public class SESAMEQNAME {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `LDP` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/LDP.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/ldp/">Linked Data Platform</a>
 */
public class LDP {
	/**
	 * The LDP namespace: http://www.w3.org/ns/ldp#
```

### UtilityClassWithoutPrivateConstructor
Class `TIME` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/TIME.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/owl-time/">Time Ontology in OWL</a>
 */
public class TIME {
	/**
	 * The Time namespace: http://www.w3.org/2006/time#
```

### UtilityClassWithoutPrivateConstructor
Class `DC` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/DC.java`
#### Snippet
```java
 * @see <a href="http://dublincore.org/documents/dces/">Dublin Core Metadata Element Set, Version 1.1</a>
 */
public class DC {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SPIN` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SPIN.java`
#### Snippet
```java
 * @version 1.4.0
 */
public class SPIN {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `DOAP` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/DOAP.java`
#### Snippet
```java
 * @see <a href="https://github.com/ewilderj/doap/wiki">Description of a Project</a>
 */
public class DOAP {
	/**
	 * The DOAP namespace: http://usefulinc.com/ns/doap#
```

### UtilityClassWithoutPrivateConstructor
Class `ROV` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ROV.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/vocab-regorg/">Registered Organization Vocabulary</a>
 */
public class ROV {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RSX` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RSX.java`
#### Snippet
```java
 * Constants for the Eclipse RDF4J SHACL Extensions.
 */
public class RSX {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `PROV` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/PROV.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/prov-overview/">Provenance Ontology</a>
 */
public class PROV {
	/**
	 * The PROV-O namespace: http://www.w3.org/ns/prov#
```

### UtilityClassWithoutPrivateConstructor
Class `ORG` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ORG.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/vocab-org/">Organization Ontology</a>
 */
public class ORG {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `OWL` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/owl2-overview/">OWL 2 Web Ontology Language Document Overview</a>
 */
public class OWL {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `VOID` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VOID.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/void/">Vocabulary of Interlinked Datasets</a>
 */
public class VOID {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `DCTERMS` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/DCTERMS.java`
#### Snippet
```java
 * @see <a href="http://dublincore.org/documents/dcmi-terms/">DCMI Metadata Terms</a>
 */
public class DCTERMS {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RDF` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF.java`
#### Snippet
```java
 * @see <a href="http://www.w3.org/TR/REC-rdf-syntax/">RDF/XML Syntax Specification (Revised)</a>
 */
public class RDF {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SESAME` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SESAME.java`
#### Snippet
```java
 * Defines constants for the Sesame schema namespace.
 */
public class SESAME {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `VANN` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VANN.java`
#### Snippet
```java
 * @see <a href="http://purl.org/vocab/vann/">A vocabulary for annotating vocabulary descriptions (VANN)</a>
 */
public class VANN {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `DASH` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/DASH.java`
#### Snippet
```java
import org.eclipse.rdf4j.model.Namespace;

public class DASH {

	public static final String NAMESPACE = "http://datashapes.org/dash#";
```

### UtilityClassWithoutPrivateConstructor
Class `GEOF` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/GEOF.java`
#### Snippet
```java
 *      "http://www.opengeospatial.org/standards/geosparql">http://www.opengeospatial.org/standards/geosparql</a>
 */
public class GEOF {

	public static final String NAMESPACE = "http://www.opengis.net/def/function/geosparql/";
```

### UtilityClassWithoutPrivateConstructor
Class `SHACL` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SHACL.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/2017/WD-shacl-20170303/">Shapes Constraint Language</a>
 */
public class SHACL {
	/**
	 * The SHACL namespace: http://www.w3.org/ns/shacl#
```

### UtilityClassWithoutPrivateConstructor
Class `HYDRA` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/HYDRA.java`
#### Snippet
```java
 * @see <a href="http://www.hydra-cg.com/spec/latest/core/">Hydra Core Vocabulary</a>
 */
public class HYDRA {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `GEO` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/GEO.java`
#### Snippet
```java
 *      "http://www.opengeospatial.org/standards/geosparql">http://www.opengeospatial.org/standards/geosparql</a>
 */
public class GEO {

	public static final String NAMESPACE = CoreDatatype.GEO.NAMESPACE;
```

### UtilityClassWithoutPrivateConstructor
Class `WGS84` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/WGS84.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/2003/01/geo/">Basic Geo (WGS84 lat/long) Vocabulary</a>
 */
public class WGS84 {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SKOSXL` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SKOSXL.java`
#### Snippet
```java
 *      Reference</a>
 */
public class SKOSXL {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `VCARD4` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/2006/vcard/ns.ttl">vCard OWL file</a>
 */
public class VCARD4 {
	/**
	 * The VCARD4 namespace: http://www.w3.org/2006/vcard/ns#
```

### UtilityClassWithoutPrivateConstructor
Class `FOAF` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
 * <a href="http://xmlns.com/foaf/spec/">http://xmlns.com/foaf/spec/</a>, version 0.99, 14 January 2014
 */
public class FOAF {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `EARL` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/EARL.java`
#### Snippet
```java
 * Constants for EARL primitives and for the EARL namespace.
 */
public class EARL {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `SP` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SP.java`
#### Snippet
```java
 * @version 1.4.0
 */
public class SP {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `ODRL2` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/odrl-vocab/">Open Digital Rights Language</a>
 */
public class ODRL2 {
	/**
	 * The ODRL 2.2 namespace: http://www.w3.org/ns/odrl/2/
```

### UtilityClassWithoutPrivateConstructor
Class `DCAT` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/DCAT.java`
#### Snippet
```java
 * @see <a href="https://www.w3.org/TR/vocab-dcat-2/">Data Catalog Vocabulary version 2</a>
 */
public class DCAT {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `RDFS` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDFS.java`
#### Snippet
```java
 * @see <a href="http://www.w3.org/TR/rdf-schema/">RDF Vocabulary Description Language 1.0: RDF Schema (RDFS)</a>
 */
public class RDFS {

	/**
```

### UtilityClassWithoutPrivateConstructor
Class `EX` has only 'static' members, and lacks a 'private' constructor
in `spring-components/rdf4j-spring-demo/src/main/java/org/eclipse/rdf4j/spring/demo/model/EX.java`
#### Snippet
```java
 * @since 4.0.0
 */
public class EX {
	private static final Namespace base = new SimpleNamespace("ex", "http://example.org/");
	public static final IRI Artist = Values.iri(base, "Artist");
```

### UtilityClassWithoutPrivateConstructor
Class `RDF4J` has only 'static' members, and lacks a 'private' constructor
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/RDF4J.java`
#### Snippet
```java
 * @author Jeen Broekstra
 */
public class RDF4J {

	/**
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Method invocation `isRefback` may produce `NullPointerException`
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceBranch.java`
#### Snippet
```java
			while (changes.size() > 1) {
				Changeset pop = changes.removeLast();
				if (changes.peekLast().isRefback()) {
					changes.addLast(pop);
					break;
```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `core/rio/rdfjson/src/main/java/org/eclipse/rdf4j/rio/rdfjson/RDFJSONParser.java`
#### Snippet
```java
									+ " predicate=" + predStr, jp.getCurrentLocation());
						}
						object = createNode(nextValue.substring(2));
					} else if (RDFJSONUtility.URI.equals(nextType)) {
						if (nextLanguage != null) {
```

### DataFlowIssue
Method `rollbackInternal` is too complex to analyze by data flow algorithm
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java

	@Override
	protected void rollbackInternal() throws SailException {
		synchronized (datasets) {
			SailDataset toCloseDataset = null;
```

### DataFlowIssue
Method `endUpdateInternal` is too complex to analyze by data flow algorithm
in `core/sail/base/src/main/java/org/eclipse/rdf4j/sail/base/SailSourceConnection.java`
#### Snippet
```java

	@Override
	protected void endUpdateInternal(UpdateContext op) throws SailException {
		synchronized (datasets) {
			if (inferredOnlySink == null && explicitOnlyDataset == null && inferredOnlyDataset == null
```

### DataFlowIssue
Method `close` is too complex to analyze by data flow algorithm
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailConnection.java`
#### Snippet
```java

	@Override
	synchronized public void close() throws SailException {
		if (closed) {
			return;
```

### DataFlowIssue
Method invocation `isEnabled` will produce `NullPointerException`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/DebugPlanNode.java`
#### Snippet
```java
	public CloseableIteration<? extends ValidationTuple, SailException> iterator() {

		if (validationExecutionLogger == null && validationExecutionLogger.isEnabled()) {
			throw new IllegalStateException("Did not receive validationExecutionLogger before .iterator() was called!");
		}
```

### DataFlowIssue
Method invocation `sameTargetAs` may produce `NullPointerException`
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/LeftOuterJoin.java`
#### Snippet
```java
					if (nextRight != null) {

						if (nextLeft.sameTargetAs(nextRight)) {
							next = ValidationTupleHelper.join(nextLeft, nextRight);
							prevLeft = nextLeft;
```

### DataFlowIssue
Stream might have already been linked or consumed
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/shape/BackwardChainingShapeSource.java`
#### Snippet
```java
				.stream()) {

			return stream
					.collect(Collectors.groupingBy(Statement::getSubject))
					.entrySet()
```

### DataFlowIssue
Stream might have already been linked or consumed
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/shape/SailConnectionShapeSource.java`
#### Snippet
```java
				.stream()) {

			return stream
					.collect(Collectors.groupingBy(Statement::getSubject))
					.entrySet()
```

### DataFlowIssue
Stream might have already been linked or consumed
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/wrapper/shape/RepositoryConnectionShapeSource.java`
#### Snippet
```java
		try (Stream<? extends Statement> stream = connection.getStatements(null, SHACL.SHAPES_GRAPH, null, false)
				.stream()) {
			return stream
					.collect(Collectors.groupingBy(Statement::getSubject))
					.entrySet()
```

### DataFlowIssue
Argument `ElasticsearchNamespaceStore.class.getClassLoader() .getResourceAsStream("elasticsearchStoreName...` might be null
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchNamespaceStore.java`
#### Snippet
```java
	static {
		try {
			MAPPING = IOUtils.toString(ElasticsearchNamespaceStore.class.getClassLoader()
					.getResourceAsStream("elasticsearchStoreNamespaceMapping.json"), StandardCharsets.UTF_8);
		} catch (IOException e) {
			throw new IllegalStateException(e);
```

### DataFlowIssue
Argument `ElasticsearchDataStructure.class.getClassLoader() .getResourceAsStream("elasticsearchStoreMappi...` might be null
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
	static {
		try {
			MAPPING = IOUtils.toString(ElasticsearchDataStructure.class.getClassLoader()
					.getResourceAsStream("elasticsearchStoreMapping.json"), StandardCharsets.UTF_8);
		} catch (IOException e) {
			throw new IllegalStateException(e);
```

### DataFlowIssue
Method invocation `evaluate` may produce `NullPointerException`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/function/spif/Invoke.java`
#### Snippet
```java
		System.arraycopy(args, 1, funcArgs, 0, funcArgs.length);
		Function function = FunctionRegistry.getInstance().get(func.stringValue()).orElse(null);
		return function.evaluate(valueFactory, funcArgs);
	}
}
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
			if (mdb_cursor_get(cursor, keyData, valueData, MDB_NEXT) == 0) {
				Varint.readListUnsigned(keyData.mv_data(), quad);
				byte op = valueData.mv_data().get(0);
				Record r = new Record();
				r.quad = quad;
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java

			boolean foundExplicit = mdb_get(writeTxn, dbiExplicit, keyVal, dataVal) == 0 &&
					(dataVal.mv_data().get(0) & 0b1) != 0;
			boolean foundImplicit = !foundExplicit && mdb_get(writeTxn, dbiInferred, keyVal, dataVal) == 0 &&
					(dataVal.mv_data().get(0) & 0b1) != 0;
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/TxnRecordCache.java`
#### Snippet
```java
					(dataVal.mv_data().get(0) & 0b1) != 0;
			boolean foundImplicit = !foundExplicit && mdb_get(writeTxn, dbiInferred, keyVal, dataVal) == 0 &&
					(dataVal.mv_data().get(0) & 0b1) != 0;

			boolean found = foundExplicit || foundImplicit;
```

### DataFlowIssue
Method invocation `remaining` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
			MDBVal valueData = MDBVal.calloc(stack);
			if (mdb_get(txn, dbi, keyData, valueData) == 0) {
				byte[] valueBytes = new byte[valueData.mv_data().remaining()];
				valueData.mv_data().get(valueBytes);
				return valueBytes;
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
			if (mdb_get(txn, dbi, keyData, valueData) == 0) {
				byte[] valueBytes = new byte[valueData.mv_data().remaining()];
				valueData.mv_data().get(valueBytes);
				return valueBytes;
			}
```

### DataFlowIssue
Argument `idVal.mv_data()` might be null
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
				MDBVal idVal = MDBVal.calloc(stack);
				if (mdb_get(txn, dbi, dataVal, idVal) == 0) {
					return data2id(idVal.mv_data());
				}
				if (!create) {
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
					idVal.mv_data(dataVal.mv_data());
					if (mdb_get(txn, dbi, idVal, dataVal) == 0 &&
							dataVal.mv_data().compareTo(dataBb) == 0) {
						return data2id(idVal.mv_data());
					}
```

### DataFlowIssue
Argument `idVal.mv_data()` might be null
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
					if (mdb_get(txn, dbi, idVal, dataVal) == 0 &&
							dataVal.mv_data().compareTo(dataBb) == 0) {
						return data2id(idVal.mv_data());
					}
				} else {
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
						// store mapping of ID -> data
						E(mdb_put(writeTxn, dbi, idVal, dataVal, MDB_RESERVE));
						dataVal.mv_data().put(data);
						return null;
					});
```

### DataFlowIssue
Method invocation `position` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
							// use only ID part of key for lookup of data
							ByteBuffer hashIdBb = hashVal.mv_data();
							hashIdBb.position(hashLength);
							idVal.mv_data(hashIdBb);
							if (mdb_get(txn, dbi, idVal, dataVal) == 0 &&
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
							idVal.mv_data(hashIdBb);
							if (mdb_get(txn, dbi, idVal, dataVal) == 0 &&
									dataVal.mv_data().compareTo(dataBb) == 0) {
								// id was found if stored value is equal to requested value
								return data2id(hashIdBb);
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
					// store mapping of ID -> data
					E(mdb_put(txn, dbi, idVal, dataVal, MDB_RESERVE));
					dataVal.mv_data().put(data);
					return null;
				});
```

### DataFlowIssue
Argument `keyData.mv_data()` might be null
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
						mdb_cursor_get(cursor, keyData, valueData, MDB_PREV) == 0) {
					// remove lower 2 type bits
					nextId = (data2id(keyData.mv_data()) >> 2) + 1;
				} else {
					nextId = 1;
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/PositiveParserTest.java`
#### Snippet
```java
				ntriplesParser.parse(in, baseURL);
			} finally {
				in.close();
			}

```

### DataFlowIssue
Passing `null` argument to `Optional`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/RDFParserHelper.java`
#### Snippet
```java
						&& (!workingLang.isPresent() || workingLang.get().isEmpty())
						&& !parserConfig.get(BasicParserSettings.VERIFY_DATATYPE_VALUES)) {
					workingLang = Optional.ofNullable(null);
					workingDatatype = null;
				}
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
			InputStream in = this.getClass().getResourceAsStream(inputURL);
			rdfjsonParser.parse(in, baseURL);
			in.close();

			// Parse expected output data
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
				in = this.getClass().getResourceAsStream(outputURL);
				ntriplesParser.parse(in, baseURL);
				in.close();

				// Check equality of the two models
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfjson/RDFJSONParserTestCase.java`
#### Snippet
```java
				InputStream in = this.getClass().getResourceAsStream(inputURL);
				rdfjsonParser.parse(in, baseURL);
				in.close();

				fail("Parser parses erroneous data without reporting errors");
```

### DataFlowIssue
Method invocation `toExternalForm` may produce `NullPointerException`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL10QueryComplianceTest.java`
#### Snippet
```java
				SPARQL10QueryComplianceTest.class.getClassLoader()
						.getResource("testcases-sparql-1.0-w3c/data-r2/manifest-evaluation.ttl")
						.toExternalForm());
		while (!manifests.isEmpty()) {
			String pop = manifests.pop();
```

### DataFlowIssue
Method invocation `toExternalForm` may produce `NullPointerException`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxComplianceTest.java`
#### Snippet
```java
				SPARQL11SyntaxComplianceTest.class.getClassLoader()
						.getResource("testcases-sparql-1.1-w3c/manifest-all.ttl")
						.toExternalForm());
		while (!manifests.isEmpty()) {
			String pop = manifests.pop();
```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
						Var objVar = getVar(obj);
						tupleNode = new ArbitraryLengthPath(subjVar,
								new StatementPattern(subjVar, getVar(subPath), objVar), objVar, minPath.longValue());
					} else {
						throw new UnsupportedOperationException(types.toString());
```

### DataFlowIssue
Method invocation `getProtocol` may produce `NullPointerException`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL10ManifestTest.java`
#### Snippet
```java
					.getResource("/testcases-sparql-1.0-w3c/data-r2/manifest-evaluation.ttl");

			if ("jar".equals(url.getProtocol())) {
				// Extract manifest files to a temporary directory
				try {
```

### DataFlowIssue
Method invocation `toExternalForm` may produce `NullPointerException`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLSyntaxComplianceTest.java`
#### Snippet
```java
				SPARQLSyntaxComplianceTest.class.getClassLoader()
						.getResource("testcases-sparql-1.1-w3c/manifest-all.ttl")
						.toExternalForm());
		while (!manifests.isEmpty()) {
			String pop = manifests.pop();
```

### DataFlowIssue
Method invocation `getProtocol` may produce `NullPointerException`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxTest.java`
#### Snippet
```java
		} else {
			URL url = SPARQL11SyntaxTest.class.getResource("/testcases-sparql-1.1-w3c/");
			if ("jar".equals(url.getProtocol())) {
				try {
					tmpDir = Files.createTempDirectory("sparql-syntax").toFile();
```

### DataFlowIssue
Method invocation `toExternalForm` may produce `NullPointerException`
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11UpdateComplianceTest.java`
#### Snippet
```java
				SPARQL11UpdateComplianceTest.class.getClassLoader()
						.getResource("testcases-sparql-1.1-w3c/manifest-all.ttl")
						.toExternalForm());
		while (!manifests.isEmpty()) {
			String pop = manifests.pop();
```

### DataFlowIssue
Method invocation `toExternalForm` may produce `NullPointerException`
in `testsuites/geosparql/src/main/java/org/eclipse/rdf4j/testsuite/query/algebra/geosparql/GeoSPARQLManifestTest.java`
#### Snippet
```java
				GeoSPARQLManifestTest.class.getClassLoader()
						.getResource("testcases-geosparql/functions/manifest.ttl")
						.toExternalForm());
		while (!manifests.isEmpty()) {
			String pop = manifests.pop();
```

### DataFlowIssue
Variable is already assigned to this value
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/TransactionalRepositoryConnection.java`
#### Snippet
```java
	public TransactionalRepositoryConnection(Repository repository, RepositoryConnection delegate) {
		super(repository, delegate);
		this.transactionObject = transactionObject;
	}

```

### DataFlowIssue
Method invocation `getProtocol` may produce `NullPointerException`
in `testsuites/shacl/src/main/java/org/eclipse/rdf4j/testsuite/shacl/manifest/SHACLManifestTestSuiteFactory.java`
#### Snippet
```java
		}
		URL url = SHACLManifestTestSuiteFactory.class.getResource("/data-shapes-test-suite/tests/manifest.ttl");
		if ("jar".equals(url.getProtocol())) {
			// Extract manifest files to a temporary directory
			try {
```

### DataFlowIssue
Dereference of `files` may produce `NullPointerException`
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/FileUtil.java`
#### Snippet
```java

		File[] files = dir.listFiles();
		for (File file : files) {
			if (file.isDirectory()) {
				deleteDir(file);
```

### DataFlowIssue
Variable is already assigned to this value
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

			HttpRequestBase method = new HttpGet(url.build());
			method = applyAdditionalHeaders(method);

			try {
```

### DataFlowIssue
Variable is already assigned to this value
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java

			HttpRequestBase method = useTransaction ? new HttpPut(url.build()) : new HttpGet(url.build());
			method = applyAdditionalHeaders(method);

			try {
```

### DataFlowIssue
Condition `obj instanceof Value` is redundant and can be replaced with a null check
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
		Resource s = subj instanceof Resource ? (Resource) subj : BEFORE;
		IRI p = pred instanceof IRI ? (IRI) pred : BEFORE;
		Value o = obj instanceof Value ? obj : BEFORE;
		Resource c = ctx instanceof Resource ? (Resource) ctx : BEFORE;
		return new TreeStatement(s, p, o, c);
```

### DataFlowIssue
Method invocation `compare` may produce `NullPointerException`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
		public SortedSet<Statement> tailSet(Statement fromElement) {
			boolean fromInclusive = true;
			if (comparator().compare(fromElement, lo) < 0) {
				fromElement = lo;
				fromInclusive = loInclusive;
```

### DataFlowIssue
Condition `obj instanceof Value` is redundant and can be replaced with a null check
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
		Resource s = subj instanceof Resource ? (Resource) subj : AFTER;
		IRI p = pred instanceof IRI ? (IRI) pred : AFTER;
		Value o = obj instanceof Value ? obj : AFTER;
		Resource c = ctx instanceof Resource ? (Resource) ctx : AFTER;
		return new TreeStatement(s, p, o, c);
```

### DataFlowIssue
Method invocation `compare` may produce `NullPointerException`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
		public SortedSet<Statement> headSet(Statement toElement) {
			boolean toInclusive = false;
			if (comparator().compare(hi, toElement) < 0) {
				toElement = hi;
				toInclusive = hiInclusive;
```

### DataFlowIssue
Method invocation `compare` may produce `NullPointerException`
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/TreeModel.java`
#### Snippet
```java
			boolean fromInclusive = true;
			boolean toInclusive = false;
			if (comparator().compare(fromElement, lo) < 0) {
				fromElement = lo;
				fromInclusive = loInclusive;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java

	public static String getLabel(Optional<Value> v, String fallback) {
		return v != null ? getLabel(v.orElseGet(null), fallback) : fallback;
	}

```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java

		int cnt = 0;
		int size = shared.size();

		while (section.hasNext()) {
```

### DataFlowIssue
Method invocation `hasNext` may produce `NullPointerException`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java
		int size = shared.size();

		while (section.hasNext()) {
			int[] t = section.next();
			byte[] s = getSO(t[0], size, shared, subjects);
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTParser.java`
#### Snippet
```java
			int[] t = section.next();
			byte[] s = getSO(t[0], size, shared, subjects);
			byte[] p = predicates.get(t[1]);
			byte[] o = getSO(t[2], size, shared, objects);
			Statement stmt = valueFactory.createStatement(createSubject(s), createPredicate(p), createObject(o));
```

### DataFlowIssue
Casting `datatype` to `IRI` may produce `ClassCastException`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleParser.java`
#### Snippet
```java
				reportFatalError("Illegal datatype value: " + datatype);
			}
			return createLiteral(label, null, (IRI) datatype, getLineNumber(), -1);
		} else {
			return createLiteral(label, null, null, getLineNumber(), -1);
```

### DataFlowIssue
Condition `val instanceof Resource` is redundant and can be replaced with a null check
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
		if (isHanging()) {
			Value val = stack.pollLast();
			if (val instanceof Resource) {
				writeResource((Resource) val, inlineBNodes);
			} else {
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
				writeResource((Resource) val, inlineBNodes);
			} else {
				writeLiteral((Literal) val);
			}
			assert lastWrittenSubject.equals(stack.peekLast());
```

### DataFlowIssue
Condition `object instanceof Value` is redundant and can be replaced with a null check
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
			}
			Value object = object(model.filter(s.getSubject(), RDF.OBJECT, null)).orElse(null);
			if (!(object instanceof Value)) {
				return;
			}
```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
						Charset charset = Charset.forName(charsetParam.getValue());
						logger.debug("response charset is {}", charset);
						return Optional.ofNullable(charset);
					} catch (IllegalArgumentException e) {
						// continue
```

### DataFlowIssue
Variable is already assigned to this value
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
		if (resultIter == null) {
			// Underlying iterator has not yet been initialized
			resultIter = iter;
			if (resultIter == null) {
				resultIter = iter = createIteration();
```

### DataFlowIssue
Variable is already assigned to this value
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
		if (resultIter == null) {
			// Underlying iterator has not yet been initialized
			resultIter = iter;
			if (resultIter == null) {
				resultIter = iter = createIteration();
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/TupleQueryResultTest.java`
#### Snippet
```java
				evaluate = tupleQuery.evaluate();
			} finally {
				evaluate.close();
			}
		});
```

### DataFlowIssue
Argument `defaultGraph` might be null
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		try (InputStream defaultGraph = RepositoryConnectionTest.class
				.getResourceAsStream(TEST_DIR_PREFIX + "default-graph.ttl");
				InputStreamReader reader = new InputStreamReader(defaultGraph)) {
			testCon.begin();
			testCon.add(reader, "", RDFFormat.TURTLE);
```

### DataFlowIssue
Argument `RepositoryConnectionTest.class.getResourceAsStream(TEST_DIR_PREFIX + "default-graph.ttl")` might be null
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java

		try (Reader defaultGraph = new InputStreamReader(
				RepositoryConnectionTest.class.getResourceAsStream(TEST_DIR_PREFIX + "default-graph.ttl"),
				StandardCharsets.UTF_8)) {
			testCon.add(defaultGraph, "", RDFFormat.TURTLE);
```

### DataFlowIssue
Argument `graph1Stream` might be null
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		try (InputStream graph1Stream = RepositoryConnectionTest.class
				.getResourceAsStream(TEST_DIR_PREFIX + "graph1.ttl");
				Reader graph1 = new InputStreamReader(graph1Stream, StandardCharsets.UTF_8)) {
			testCon.add(graph1, "", RDFFormat.TURTLE, context1);
		}
```

### DataFlowIssue
Argument `graph2Stream` might be null
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
		try (InputStream graph2Stream = RepositoryConnectionTest.class
				.getResourceAsStream(TEST_DIR_PREFIX + "graph2.ttl");
				Reader graph2 = new InputStreamReader(graph2Stream, StandardCharsets.UTF_8)) {
			testCon.add(graph2, "", RDFFormat.TURTLE, context2);
		}
```

### DataFlowIssue
Condition `bindingSet.getValue("object") instanceof Value` is redundant and can be replaced with a null check
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/SailGraphQuery.java`
#### Snippet
```java
					return bindingSet.getValue("subject") instanceof Resource
							&& bindingSet.getValue("predicate") instanceof IRI
							&& bindingSet.getValue("object") instanceof Value
							&& (context == null || context instanceof Resource);
				}
```

### DataFlowIssue
Condition `objectsPredSubjPairs.get(i) instanceof Map` is redundant and can be replaced with a null check
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDHierarchicalProcessor.java`
#### Snippet
```java
						ArrayList<Map<String, Object>> objectsPredSubjPairs = (ArrayList<Map<String, Object>>) object;
						for (int i = 0; i < objectsPredSubjPairs.size(); i++) {
							if (objectsPredSubjPairs.get(i) instanceof Map
									&& objectsPredSubjPairs.get(i).get(ID) != null) {
								String objectsPredId = objectsPredSubjPairs.get(i).get(ID).toString();
```

### DataFlowIssue
Argument `ReasoningUpdateBenchmark.class.getClassLoader().getResourceAsStream(fn)` might be null
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/ReasoningUpdateBenchmark.java`
#### Snippet
```java
		String content = cache.computeIfAbsent(filename, (fn) -> {
			try {
				return IOUtils.toString(ReasoningUpdateBenchmark.class.getClassLoader().getResourceAsStream(fn),
						StandardCharsets.UTF_8);
			} catch (IOException e) {
```

### DataFlowIssue
Argument `rightSibling` might be null
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/BTree.java`
#### Snippet
```java
						leftSibling.mergeWithRightSibling(parentNode.removeValueRight(childIdx - 1), childNode);
					} else {
						childNode.mergeWithRightSibling(parentNode.removeValueRight(childIdx), rightSibling);
					}
				}
```

### DataFlowIssue
Variable is already assigned to this value
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/QueryOrderBenchmark.java`
#### Snippet
```java
		StringBuilder rq = new StringBuilder("SELECT * { ?s ?p ?o } ORDER BY ?o");
		if (limit > 0) {
			rq = rq.append(" LIMIT ").append(limit);
		}
		long count = 0;
```

### DataFlowIssue
Variable is already assigned to this value
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/QueryOrderBenchmark.java`
#### Snippet
```java
		StringBuilder rq = new StringBuilder("SELECT DISTINCT ?s ?o { ?s ?p ?o } ORDER BY ?o");
		if (limit > 0) {
			rq = rq.append(" LIMIT ").append(limit);
		}
		long count = 0;
```

### DataFlowIssue
Condition `bindingSet.getValue("object") instanceof Value` is redundant and can be replaced with a null check
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/AbstractQueryPreparer.java`
#### Snippet
```java
						return bindingSet.getValue("subject") instanceof Resource
								&& bindingSet.getValue("predicate") instanceof IRI
								&& bindingSet.getValue("object") instanceof Value
								&& (context == null || context instanceof Resource);
					}
```

### DataFlowIssue
Variable is already assigned to this value
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/UnionScopeChangeOptimizer.java`
#### Snippet
```java
				}

				checker.containsBindOrValues = false;
				union.getRightArg().visit(checker);

```

### DataFlowIssue
Condition `args[2] instanceof Value` is redundant and can be replaced with a null check
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/triple/StatementFunction.java`
#### Snippet
```java
			throw new ValueExprEvaluationException("arg2 must be IRI");
		}
		if (!(args[2] instanceof Value)) {
			throw new ValueExprEvaluationException("arg3 must be Value");
		}
```

### DataFlowIssue
Array access `maxUnionTupleTuple[0]` may produce `NullPointerException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java

			// add the pair to the result list.
			result.add(maxUnionTupleTuple[0]);
			result.add(maxUnionTupleTuple[1]);

```

### DataFlowIssue
Variable is already assigned to this value
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
						if (unionSize > currentUnionSize) {
							selected = candidate;
							currentJoinSize = joinSize;
							currentUnionSize = unionSize;
						}
```

### DataFlowIssue
Method invocation `setCostEstimate` may produce `NullPointerException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/QueryJoinOptimizer.java`
#### Snippet
```java
			}

			result.setCostEstimate(lowestCost);

			return result;
```

### DataFlowIssue
Method invocation `getHashSet` may produce `NullPointerException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
		public boolean addAll(Collection<? extends T> arg0) {
			if (wrapped instanceof HashSet && arg0.size() > SWITCH_TO_DISK_BASED_SET_AT_SIZE) {
				Set<T> disk = db.getHashSet(setName);
				disk.addAll(wrapped);
				wrapped = disk;
```

### DataFlowIssue
Method invocation `getHashSet` may produce `NullPointerException`
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/GroupIterator.java`
#### Snippet
```java
		public boolean add(T e) {
			if (wrapped instanceof HashSet && wrapped.size() > SWITCH_TO_DISK_BASED_SET_AT_SIZE) {
				Set<T> disk = db.getHashSet(setName);
				disk.addAll(wrapped);
				wrapped = disk;
```

### DataFlowIssue
Method `handleClose` is too complex to analyze by data flow algorithm
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/HashJoinIteration.java`
#### Snippet
```java

	@Override
	protected void handleClose() throws QueryEvaluationException {
		try {
			super.handleClose();
```

### DataFlowIssue
Method invocation `getObjectVar` may produce `NullPointerException`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/QuerySpecBuilder.java`
#### Snippet
```java
				TupleFunctionCall funcCall = new TupleFunctionCall();
				funcCall.setURI(LuceneSailSchema.SEARCH.toString());
				funcCall.addArg(queryPattern.getObjectVar());
				if (subject != null) {
					funcCall.addArg(matchesPattern.getSubjectVar());
```

### DataFlowIssue
Variable is already assigned to this value
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
				hits = query(query.getSubject(), query.getQueryString(), query.getPropertyURI(), highlight);
			} else {
				hits = null;
			}
		} catch (Exception e) {
```

### DataFlowIssue
Method invocation `getSubject` may produce `NullPointerException`
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/SPARQLResultsSAXParser.java`
#### Snippet
```java
		case S_TAG:
			currentTriple = tripleStack.peek();
			if (currentTriple.getSubject() != null) {
				throw new SAXException("RDF-star triple subject defined twice");
			}
```

### DataFlowIssue
Method invocation `getPredicate` may produce `NullPointerException`
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/SPARQLResultsSAXParser.java`
#### Snippet
```java
		case P_TAG:
			currentTriple = tripleStack.peek();
			if (currentTriple.getPredicate() != null) {
				throw new SAXException("RDF-star triple predicate defined twice");
			}
```

### DataFlowIssue
Method invocation `getObject` may produce `NullPointerException`
in `core/queryresultio/sparqlxml/src/main/java/org/eclipse/rdf4j/query/resultio/sparqlxml/SPARQLResultsSAXParser.java`
#### Snippet
```java
		case O_TAG:
			currentTriple = tripleStack.peek();
			if (currentTriple.getObject() != null) {
				throw new SAXException("RDF-star triple object defined twice");
			}
```

### DataFlowIssue
Passing `null` argument to `Optional`
in `core/sail/model/src/main/java/org/eclipse/rdf4j/sail/model/SailModel.java`
#### Snippet
```java
		try {
			String name = conn.getNamespace(prefix);
			return (name != null) ? Optional.of(new SimpleNamespace(prefix, name)) : Optional.ofNullable(null);
		} catch (SailException e) {
			throw new ModelException(e);
```

### DataFlowIssue
Condition `elem.getExpr() instanceof ValueExpr` is redundant and can be replaced with a null check
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
			ext.getElements()
					.stream()
					.filter(elem -> (elem.getExpr() instanceof ValueExpr))
					.forEach(elem -> valueMap.put(elem.getName(),
							(ValueExpr) elem.getExpr()));
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'ServiceJoinConversionIteration' is still used
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/ServiceJoinConversionIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class ServiceJoinConversionIteration
		extends ConvertingIteration<BindingSet, BindingSet, QueryEvaluationException> {

```

### DeprecatedIsStillUsed
Deprecated member 'getValidationReport' is still used
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailValidationException.java`
#### Snippet
```java
	 */
	@Deprecated
	public ValidationReport getValidationReport() {
		return validationReport;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'toStatementIteration' is still used
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	protected Iteration<Statement, QueryEvaluationException> toStatementIteration(TupleQueryResult iter,
			final Resource subj, final IRI pred, final Value obj) {

```

### DeprecatedIsStillUsed
Deprecated member 'TIMEOUT' is still used
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/Protocol.java`
#### Snippet
```java
	 */
	@Deprecated
	public enum TIMEOUT {
		CACHE;

```

### DeprecatedIsStillUsed
Deprecated member 'CompareAny' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAny.java`
#### Snippet
```java

@Deprecated(forRemoval = true, since = "4.2.1")
public class CompareAny extends CompareSubQueryValueOperator {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'QueryModelNodeBase' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/QueryModelNodeBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class QueryModelNodeBase extends AbstractQueryModelNode {

	private static final long serialVersionUID = 1L;
```

### DeprecatedIsStillUsed
Deprecated member 'GraphPatternGroupable' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/GraphPatternGroupable.java`
#### Snippet
```java
 */
@Deprecated
public interface GraphPatternGroupable {

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'SubQueryValueOperator' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/SubQueryValueOperator.java`
#### Snippet
```java

@Deprecated(forRemoval = true, since = "4.2.1")
public abstract class SubQueryValueOperator extends AbstractQueryModelNode implements ValueExpr {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'CompareSubQueryValueOperator' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareSubQueryValueOperator.java`
#### Snippet
```java

@Deprecated(forRemoval = true, since = "4.2.1")
public abstract class CompareSubQueryValueOperator extends SubQueryValueOperator {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'setAnonymous' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/Var.java`
#### Snippet
```java

	@Deprecated(forRemoval = true, since = "4.1.0")
	public void setAnonymous(boolean anonymous) {
		this.cachedHashCode = 0;
		this.anonymous = anonymous;
```

### DeprecatedIsStillUsed
Deprecated member 'CompareAll' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/CompareAll.java`
#### Snippet
```java

@Deprecated(forRemoval = true, since = "4.2.1")
public class CompareAll extends CompareSubQueryValueOperator {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'setContextVar' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/StatementPattern.java`
#### Snippet
```java

	@Deprecated(since = "4.0.0", forRemoval = true)
	public void setContextVar(Var context) {
		if (context != null) {
			context.setParentNode(this);
```

### DeprecatedIsStillUsed
Deprecated member 'setPredicateVar' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/StatementPattern.java`
#### Snippet
```java

	@Deprecated(since = "4.0.0", forRemoval = true)
	public void setPredicateVar(Var predicate) {
		Objects.requireNonNull(predicate).setParentNode(this);
		predicateVar = predicate;
```

### DeprecatedIsStillUsed
Deprecated member 'setObjectVar' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/StatementPattern.java`
#### Snippet
```java

	@Deprecated(since = "4.0.0", forRemoval = true)
	public void setObjectVar(Var object) {
		Objects.requireNonNull(object).setParentNode(this);
		objectVar = object;
```

### DeprecatedIsStillUsed
Deprecated member 'setSubjectVar' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/StatementPattern.java`
#### Snippet
```java

	@Deprecated(since = "4.0.0", forRemoval = true)
	public void setSubjectVar(Var subject) {
		Objects.requireNonNull(subject).setParentNode(this);
		subjectVar = subject;
```

### DeprecatedIsStillUsed
Deprecated member 'QueryModelVisitorBase' is still used
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/QueryModelVisitorBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class QueryModelVisitorBase<X extends Exception> extends AbstractQueryModelVisitor<X> {

}
```

### DeprecatedIsStillUsed
Deprecated member 'ForwardChainingRDFSInferencerFactory' is still used
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/config/ForwardChainingRDFSInferencerFactory.java`
#### Snippet
```java
 */
@Deprecated
public class ForwardChainingRDFSInferencerFactory implements SailFactory {

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'ForwardChainingRDFSInferencer' is still used
in `core/sail/inferencer/src/main/java/org/eclipse/rdf4j/sail/inferencer/fc/ForwardChainingRDFSInferencer.java`
#### Snippet
```java
 */
@Deprecated
public class ForwardChainingRDFSInferencer extends AbstractForwardChainingInferencer {
	/*--------------*
	 * Constructors *
```

### DeprecatedIsStillUsed
Deprecated member 'QueryInterruptIteration' is still used
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserQuery.java`
#### Snippet
```java

	@Deprecated(since = "4.1.0")
	protected class QueryInterruptIteration extends TimeLimitIteration<BindingSet, QueryEvaluationException> {

		public QueryInterruptIteration(Iteration<? extends BindingSet, ? extends QueryEvaluationException> iter,
```

### DeprecatedIsStillUsed
Deprecated member 'QueryInterruptIteration' is still used
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserQuery.java`
#### Snippet
```java

	@Deprecated(since = "4.1.0")
	protected class QueryInterruptIteration extends TimeLimitIteration<BindingSet, QueryEvaluationException> {

		public QueryInterruptIteration(Iteration<? extends BindingSet, ? extends QueryEvaluationException> iter,
```

### DeprecatedIsStillUsed
Deprecated member 'ParsedURI' is still used
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedURI.java`
#### Snippet
```java
 */
@Deprecated
public class ParsedURI implements java.lang.Cloneable {

	/*
```

### DeprecatedIsStillUsed
Deprecated member 'createNumericLiteral' is still used
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java

	@Deprecated(since = "4.0.0", forRemoval = true)
	protected Literal createNumericLiteral(Number number, XSD.Datatype datatype) {
		if (number instanceof BigDecimal) {
			return new DecimalLiteral((BigDecimal) number, datatype);
```

### DeprecatedIsStillUsed
Deprecated member 'sendTransaction' is still used
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
	 */
	@Deprecated
	public void sendTransaction(final Iterable<? extends TransactionOperation> txn)
			throws IOException, RepositoryException, UnauthorizedException {
		checkRepositoryURL();
```

### DeprecatedIsStillUsed
Deprecated member 'ContextStatement' is still used
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/ContextStatement.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0", forRemoval = true)
public class ContextStatement extends SimpleStatement {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'canCreateLiteral' is still used
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
	 */
	@Deprecated
	public static boolean canCreateLiteral(Object object) {
		if (object == null) {
			// Cannot create a literal from a null
```

### DeprecatedIsStillUsed
Deprecated member 'createLiteralOrFail' is still used
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
	 */
	@Deprecated
	public static Literal createLiteralOrFail(ValueFactory valueFactory, Object object) throws LiteralUtilException {
		return createLiteral(valueFactory, object, true);
	}
```

### DeprecatedIsStillUsed
Deprecated member 'toStatement' is still used
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
	 * @deprecated since 3.7.0 - use {@link #statement(Triple, Resource)} instead
	 */
	public static Statement toStatement(Triple triple, Resource context) {
		return statement(SimpleValueFactory.getInstance(), triple, context);
	}
```

### DeprecatedIsStillUsed
Deprecated member 'toTriple' is still used
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
	 */
	@Deprecated
	public static Triple toTriple(Statement statement) {
		return toTriple(SimpleValueFactory.getInstance(), statement);
	}
```

### DeprecatedIsStillUsed
Deprecated member 'writeResource' is still used
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
	 */
	@Deprecated
	protected void writeResource(Resource res) throws IOException {
		writeResource(res, false);
	}
```

### DeprecatedIsStillUsed
Deprecated member 'ARCHIVE_CONTEXT' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI ARCHIVE_CONTEXT;

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'ADD_CONTEXT' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareSchema.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI ADD_CONTEXT;

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'setAddContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareRepository.java`
#### Snippet
```java
	 */
	@Deprecated
	public void setAddContexts(IRI... addContexts) {
		this.addContexts = addContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'getArchiveContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareRepository.java`
#### Snippet
```java
	 */
	@Deprecated
	public IRI[] getArchiveContexts() {
		return archiveContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'getAddContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareRepository.java`
#### Snippet
```java
	 */
	@Deprecated
	public IRI[] getAddContexts() {
		return addContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'setArchiveContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareRepository.java`
#### Snippet
```java
	 */
	@Deprecated
	public void setArchiveContexts(IRI... archiveContexts) {
		this.archiveContexts = archiveContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'SimpleStatement' is still used
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleStatement.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0", forRemoval = true)
public class SimpleStatement extends AbstractStatement {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'getAddContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java
	 */
	@Deprecated
	public IRI[] getAddContexts() {
		return addContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'setAddContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java
	 */
	@Deprecated
	public void setAddContexts(IRI... addContexts) {
		this.addContexts = addContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'getArchiveContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java
	 */
	@Deprecated
	public IRI[] getArchiveContexts() {
		return archiveContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'setArchiveContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java
	 */
	@Deprecated
	public void setArchiveContexts(IRI... archiveContexts) {
		this.archiveContexts = archiveContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'sendTupleQuery' is still used
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.2")
	public TupleQueryResult sendTupleQuery(QueryLanguage ql, String query, Dataset dataset, boolean includeInferred,
			WeakReference<?> callerRef,
			Binding... bindings) throws IOException, RepositoryException, MalformedQueryException,
```

### DeprecatedIsStillUsed
Deprecated member 'sendTupleQuery' is still used
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.2")
	public TupleQueryResult sendTupleQuery(QueryLanguage ql, String query, String baseURI, Dataset dataset,
			boolean includeInferred, int maxQueryTime, WeakReference<?> callerRef, Binding... bindings)
			throws IOException, RepositoryException,
```

### DeprecatedIsStillUsed
Deprecated member 'sendGraphQuery' is still used
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.2")
	public GraphQueryResult sendGraphQuery(QueryLanguage ql, String query, String baseURI, Dataset dataset,
			boolean includeInferred, int maxQueryTime, WeakReference<?> callerRef, Binding... bindings)
			throws IOException, RepositoryException,
```

### DeprecatedIsStillUsed
Deprecated member 'ReducedIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ReducedIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class ReducedIteration<E, X extends Exception> extends FilterIteration<E, X> {

	private E previousObject;
```

### DeprecatedIsStillUsed
Deprecated member 'IteratorIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IteratorIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class IteratorIteration<E, X extends Exception> implements Iteration<E, X> {

	private final Iterator<? extends E> iter;
```

### DeprecatedIsStillUsed
Deprecated member 'TimeLimitIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/TimeLimitIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public abstract class TimeLimitIteration<E, X extends Exception> extends IterationWrapper<E, X> {

	private static final Timer timer = new Timer("TimeLimitIteration", true);
```

### DeprecatedIsStillUsed
Deprecated member 'DelayedIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DelayedIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public abstract class DelayedIteration<E, X extends Exception> extends AbstractCloseableIteration<E, X> {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'setArchiveContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
	 */
	@Deprecated
	public void setArchiveContexts(IRI... archiveContexts) {
		this.archiveContexts = archiveContexts;
		if (next != null) {
```

### DeprecatedIsStillUsed
Deprecated member 'setAddContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
	 */
	@Deprecated
	public void setAddContexts(IRI... addContexts) {
		this.addContexts = addContexts;
		if (isNilContext(addContexts)) {
```

### DeprecatedIsStillUsed
Deprecated member 'getArchiveContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
	 */
	@Deprecated
	public IRI[] getArchiveContexts() {
		return archiveContexts;
	}
```

### DeprecatedIsStillUsed
Deprecated member 'getAddContexts' is still used
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/ContextAwareConnection.java`
#### Snippet
```java
	 */
	@Deprecated
	public IRI[] getAddContexts() {
		if (isNilContext(addContexts)) {
			return new IRI[] { getInsertContext() };
```

### DeprecatedIsStillUsed
Deprecated member 'QueueIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/QueueIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public abstract class QueueIteration<E, T extends Exception> extends LookAheadIteration<E, T> {

	private final AtomicBoolean done = new AtomicBoolean(false);
```

### DeprecatedIsStillUsed
Deprecated member 'MinusIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/MinusIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class MinusIteration<E, X extends Exception> extends FilterIteration<E, X> {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'OffsetIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/OffsetIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class OffsetIteration<E, X extends Exception> extends FilterIteration<E, X> {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'LimitIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/LimitIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class LimitIteration<E, X extends Exception> extends IterationWrapper<E, X> {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'IntersectIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IntersectIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class IntersectIteration<E, X extends Exception> extends FilterIteration<E, X> {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'SilentIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/SilentIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class SilentIteration<T, E extends Exception> extends IterationWrapper<T, E> {

	private static final Logger logger = LoggerFactory.getLogger(SilentIteration.class);
```

### DeprecatedIsStillUsed
Deprecated member 'UnionIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/UnionIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class UnionIteration<E, X extends Exception> extends LookAheadIteration<E, X> {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'DualUnionIteration' is still used
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/DualUnionIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class DualUnionIteration<E, X extends Exception> implements CloseableIteration<E, X> {

	private CloseableIteration<? extends E, X> iteration1;
```

### DeprecatedIsStillUsed
Deprecated member 'gsub' is still used
in `core/common/text/src/main/java/org/eclipse/rdf4j/common/text/StringUtil.java`
#### Snippet
```java
	 */
	@Deprecated
	public static String gsub(String olds, String news, String text) {
		if (olds == null || olds.length() == 0) {
			// Nothing to substitute.
```

### DeprecatedIsStillUsed
Deprecated member 'updateLock' is still used
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSailConnection.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0")
	protected final ReentrantLock updateLock = new ReentrantLock();

	@Deprecated(since = "4.1.0", forRemoval = true)
```

### DeprecatedIsStillUsed
Deprecated member 'connectionLock' is still used
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/AbstractSailConnection.java`
#### Snippet
```java

	@Deprecated(since = "4.1.0", forRemoval = true)
	protected final ReentrantReadWriteLock connectionLock = new ReentrantReadWriteLock();

	@InternalUseOnly
```

### DeprecatedIsStillUsed
Deprecated member 'QueueCursor' is still used
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/QueueCursor.java`
#### Snippet
```java
	 *             {@link java.lang.ref.Cleaner}.
	 */
	public QueueCursor(BlockingQueue<E> queue) {
		super(queue);
	}
```

### DeprecatedIsStillUsed
Deprecated member 'DatasetImpl' is still used
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/DatasetImpl.java`
#### Snippet
```java
 */
@Deprecated
public class DatasetImpl extends SimpleDataset {

	private static final long serialVersionUID = 1L;
```

### DeprecatedIsStillUsed
Deprecated member 'WildcardProjectionProcessor' is still used
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/WildcardProjectionProcessor.java`
#### Snippet
```java
 */
@Deprecated
public class WildcardProjectionProcessor extends AbstractASTVisitor {

	public static void process(ASTOperationContainer container) throws MalformedQueryException {
```

### DeprecatedIsStillUsed
Deprecated member 'BackgroundGraphResult' is still used
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/BackgroundGraphResult.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class BackgroundGraphResult extends IterationWrapper<Statement, QueryEvaluationException>
		implements GraphQueryResult, Runnable, RDFHandler {

```

### DeprecatedIsStillUsed
Deprecated member 'BackgroundTupleResult' is still used
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/helpers/BackgroundTupleResult.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class BackgroundTupleResult extends IteratingTupleQueryResult implements Runnable, TupleQueryResultHandler {

	private final TupleQueryResultParser parser;
```

### DeprecatedIsStillUsed
Deprecated member 'IteratingGraphQueryResult' is still used
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/IteratingGraphQueryResult.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class IteratingGraphQueryResult extends IterationWrapper<Statement, QueryEvaluationException>
		implements GraphQueryResult {

```

### DeprecatedIsStillUsed
Deprecated member 'parseTupleBackground' is still used
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultIO.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.2")
	public static TupleQueryResult parseTupleBackground(InputStream in, QueryResultFormat format,
			WeakReference<?> callerReference) throws IOException,
			QueryResultParseException, TupleQueryResultHandlerException, UnsupportedQueryResultFormatException {
```

### DeprecatedIsStillUsed
Deprecated member 'parseTuple' is still used
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/QueryResultIO.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.2")
	public static TupleQueryResult parseTuple(InputStream in, QueryResultFormat format,
			WeakReference<?> callerReference) throws IOException,
			QueryResultParseException, TupleQueryResultHandlerException, UnsupportedQueryResultFormatException {
```

### DeprecatedIsStillUsed
Deprecated member 'DelayedEvaluationIteration' is still used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/QueryEvaluationStep.java`
#### Snippet
```java
	 */
	@Deprecated(since = "4.1.0", forRemoval = true)
	class DelayedEvaluationIteration
			extends DelayedIteration<BindingSet, QueryEvaluationException> {
		private final QueryEvaluationStep arg;
```

### DeprecatedIsStillUsed
Deprecated member 'ExtensionIterator' is still used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ExtensionIterator.java`
#### Snippet
```java

@Deprecated(since = "4.1.0")
public class ExtensionIterator extends ConvertingIteration<BindingSet, BindingSet, QueryEvaluationException> {

	private final Consumer<MutableBindingSet> setter;
```

### DeprecatedIsStillUsed
Deprecated member 'ProjectionIterator' is still used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/ProjectionIterator.java`
#### Snippet
```java

@Deprecated(since = "4.1.0")
public class ProjectionIterator extends ConvertingIteration<BindingSet, BindingSet, QueryEvaluationException> {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'FilterIterator' is still used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/FilterIterator.java`
#### Snippet
```java

@Deprecated(since = "4.1.0")
public class FilterIterator extends FilterIteration<BindingSet, QueryEvaluationException> {

	private final QueryValueEvaluationStep condition;
```

### DeprecatedIsStillUsed
Deprecated member 'SPARQLMinusIteration' is still used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/SPARQLMinusIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class SPARQLMinusIteration<X extends Exception> extends FilterIteration<BindingSet, X> {

	/*-----------*
```

### DeprecatedIsStillUsed
Deprecated member 'DescribeIteration' is still used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/DescribeIteration.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class DescribeIteration extends LookAheadIteration<BindingSet, QueryEvaluationException> {

	protected final static String VARNAME_SUBJECT = "subject";
```

### DeprecatedIsStillUsed
Deprecated member 'JoinExecutorBase' is still used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/JoinExecutorBase.java`
#### Snippet
```java
 */
@Deprecated
public abstract class JoinExecutorBase<T> extends org.eclipse.rdf4j.repository.sparql.federation.JoinExecutorBase<T> {

	public JoinExecutorBase(CloseableIteration<T, QueryEvaluationException> leftIter, TupleExpr rightArg,
```

### DeprecatedIsStillUsed
Deprecated member 'OrderIterator' is still used
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
 */
@Deprecated(since = "4.1.0")
public class OrderIterator extends DelayedIteration<BindingSet, QueryEvaluationException> {

	private static class SerializedQueue<E extends Serializable> extends AbstractQueue<E> implements Closeable {
```

### DeprecatedIsStillUsed
Deprecated member 'LuceneSailBuffer' is still used
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailBuffer.java`
#### Snippet
```java
@Deprecated
@InternalUseOnly
public class LuceneSailBuffer {

	private static class ContextAwareStatementImpl implements Statement {
```

### DeprecatedIsStillUsed
Deprecated member 'MapOfListMaps' is still used
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/MapOfListMaps.java`
#### Snippet
```java
@Deprecated
@InternalUseOnly
public class MapOfListMaps<Index1Type, Index2Type, DataType> {

	private final Map<Index1Type, Map<Index2Type, List<DataType>>> data;
```

### DeprecatedIsStillUsed
Deprecated member 'PREFER_EMPTY_CONTAINER' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/LDP.java`
#### Snippet
```java
	/** ldp:PreferEmptyContainer */
	@Deprecated
	public static final IRI PREFER_EMPTY_CONTAINER;

	/** ldp:PreferMembership */
```

### DeprecatedIsStillUsed
Deprecated member 'IN_XSDDATE_TIME' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/TIME.java`
#### Snippet
```java
	/** time:inXSDDateTime */
	@Deprecated
	public static final IRI IN_XSDDATE_TIME;

	/** time:inXSDDateTimeStamp */
```

### DeprecatedIsStillUsed
Deprecated member 'JANUARY' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/TIME.java`
#### Snippet
```java
	/** time:January */
	@Deprecated
	public static final IRI JANUARY;

	/** time:MonthOfYear */
```

### DeprecatedIsStillUsed
Deprecated member 'XSD_DATE_TIME' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/TIME.java`
#### Snippet
```java
	/** time:xsdDateTime */
	@Deprecated
	public static final IRI XSD_DATE_TIME;

	/** time:year */
```

### DeprecatedIsStillUsed
Deprecated member 'EVAL_CLASS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SPIN.java`
#### Snippet
```java

	@Deprecated
	public static final IRI EVAL_CLASS;

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'INDIVIDUAL' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/OWL.java`
#### Snippet
```java
	/** http://www.w3.org/2002/07/owl#Individual */
	@Deprecated
	public final static IRI INDIVIDUAL;

	/** http://www.w3.org/2002/07/owl#Thing */
```

### DeprecatedIsStillUsed
Deprecated member 'WAIT_FOR_RELOCATING_SHARDS_KEY' is still used
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
	 */
	@Deprecated
	public static final String WAIT_FOR_RELOCATING_SHARDS_KEY = "waitForRelocatingShards";

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'version' is still used
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchDocument.java`
#### Snippet
```java

	@Deprecated
	private long version;

	private final long seqNo;
```

### DeprecatedIsStillUsed
Deprecated member 'LONGITUDE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:longitude */
	@Deprecated
	public static final IRI LONGITUDE;

	/** vcard:mailer */
```

### DeprecatedIsStillUsed
Deprecated member 'PREF' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Pref */
	@Deprecated
	public static final IRI PREF;

	/** vcard:RelatedType */
```

### DeprecatedIsStillUsed
Deprecated member 'INTL' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Intl */
	@Deprecated
	public static final IRI INTL;

	/** vcard:Kin */
```

### DeprecatedIsStillUsed
Deprecated member 'GIVENNAME' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI GIVENNAME;

	public final static IRI HOLDS_ACCOUNT;
```

### DeprecatedIsStillUsed
Deprecated member 'PARCEL' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Parcel */
	@Deprecated
	public static final IRI PARCEL;

	/** vcard:Parent */
```

### DeprecatedIsStillUsed
Deprecated member 'FAMILYNAME' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/FOAF.java`
#### Snippet
```java
	 */
	@Deprecated
	public final static IRI FAMILYNAME;

	public final static IRI FIRST_NAME;
```

### DeprecatedIsStillUsed
Deprecated member 'LABEL_PROP' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:label */
	@Deprecated
	public static final IRI LABEL_PROP;

	/** vcard:language */
```

### DeprecatedIsStillUsed
Deprecated member 'MAILER' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:mailer */
	@Deprecated
	public static final IRI MAILER;

	/** vcard:n */
```

### DeprecatedIsStillUsed
Deprecated member 'AGENT_PROP' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:agent */
	@Deprecated
	public static final IRI AGENT_PROP;

	/** vcard:anniversary */
```

### DeprecatedIsStillUsed
Deprecated member 'CAR' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Car */
	@Deprecated
	public static final IRI CAR;

	/** vcard:Cell */
```

### DeprecatedIsStillUsed
Deprecated member 'MODEM' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Modem */
	@Deprecated
	public static final IRI MODEM;

	/** vcard:Msg */
```

### DeprecatedIsStillUsed
Deprecated member 'BBS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:BBS */
	@Deprecated
	public static final IRI BBS;

	/** vcard:Car */
```

### DeprecatedIsStillUsed
Deprecated member 'EXTENDED_ADDRESS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:extended-address */
	@Deprecated
	public static final IRI EXTENDED_ADDRESS;

	/** vcard:family-name */
```

### DeprecatedIsStillUsed
Deprecated member 'PCS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:PCS */
	@Deprecated
	public static final IRI PCS;

	/** vcard:Pager */
```

### DeprecatedIsStillUsed
Deprecated member 'INTERNET' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Internet */
	@Deprecated
	public static final IRI INTERNET;

	/** vcard:Intl */
```

### DeprecatedIsStillUsed
Deprecated member 'POST_OFFICE_BOX' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:post-office-box */
	@Deprecated
	public static final IRI POST_OFFICE_BOX;

	/** vcard:postal-code */
```

### DeprecatedIsStillUsed
Deprecated member 'X400' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:X400 */
	@Deprecated
	public static final IRI X400;

	// Properties
```

### DeprecatedIsStillUsed
Deprecated member 'TEL' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Tel */
	@Deprecated
	public static final IRI TEL;

	/** vcard:TelephoneType */
```

### DeprecatedIsStillUsed
Deprecated member 'POSTAL' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Postal */
	@Deprecated
	public static final IRI POSTAL;

	/** vcard:Pref */
```

### DeprecatedIsStillUsed
Deprecated member 'ISDN' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:ISDN */
	@Deprecated
	public static final IRI ISDN;

	/** vcard:Individual */
```

### DeprecatedIsStillUsed
Deprecated member 'EMAIL' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Email */
	@Deprecated
	public static final IRI EMAIL;

	/** vcard:Emergency */
```

### DeprecatedIsStillUsed
Deprecated member 'MSG' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:Msg */
	@Deprecated
	public static final IRI MSG;

	/** vcard:Muse */
```

### DeprecatedIsStillUsed
Deprecated member 'LATITUDE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/VCARD4.java`
#### Snippet
```java
	/** vcard:latitude */
	@Deprecated
	public static final IRI LATITUDE;

	/** vcard:locality */
```

### DeprecatedIsStillUsed
Deprecated member 'INSERT_CLASS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SP.java`
#### Snippet
```java
	 */
	@Deprecated
	public static final IRI INSERT_CLASS;

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'LET_CLASS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SP.java`
#### Snippet
```java
	 */
	@Deprecated
	public static final IRI LET_CLASS;

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'DELETE_CLASS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SP.java`
#### Snippet
```java
	 */
	@Deprecated
	public static final IRI DELETE_CLASS;

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'AS_PROPERTY' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/SP.java`
#### Snippet
```java
	 */
	@Deprecated
	public static final IRI AS_PROPERTY;

	/**
```

### DeprecatedIsStillUsed
Deprecated member 'COMMERCIALIZE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:commercialize */
	@Deprecated
	public static final IRI COMMERCIALIZE;

	/** odrl:compensate */
```

### DeprecatedIsStillUsed
Deprecated member 'PROXIMITY' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:proximity */
	@Deprecated
	public static final IRI PROXIMITY;

	/** odrl:refinement */
```

### DeprecatedIsStillUsed
Deprecated member 'APPEND_TO' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:appendTo */
	@Deprecated
	public static final IRI APPEND_TO;

	/** odrl:archive */
```

### DeprecatedIsStillUsed
Deprecated member 'ASSET_SCOPE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:AssetScope */
	@Deprecated
	public static final IRI ASSET_SCOPE;

	/** odrl:ConflictTerm */
```

### DeprecatedIsStillUsed
Deprecated member 'LEND' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:lend */
	@Deprecated
	public static final IRI LEND;

	/** odrl:license */
```

### DeprecatedIsStillUsed
Deprecated member 'SUPPORT' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:support */
	@Deprecated
	public static final IRI SUPPORT;

	/** odrl:synchronize */
```

### DeprecatedIsStillUsed
Deprecated member 'PAYEE_PARTY' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:payeeParty */
	@Deprecated
	public static final IRI PAYEE_PARTY;

	/** odrl:permission */
```

### DeprecatedIsStillUsed
Deprecated member 'INDIVIDUAL' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:Individual */
	@Deprecated
	public static final IRI INDIVIDUAL;

	/** odrl:absolutePosition */
```

### DeprecatedIsStillUsed
Deprecated member 'AD_HOC_SHARE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:adHocShare */
	@Deprecated
	public static final IRI AD_HOC_SHARE;

	/** odrl:aggregate */
```

### DeprecatedIsStillUsed
Deprecated member 'SHARE_ALIKE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:shareAlike */
	@Deprecated
	public static final IRI SHARE_ALIKE;

	/** odrl:spatial */
```

### DeprecatedIsStillUsed
Deprecated member 'DEVICE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:device */
	@Deprecated
	public static final IRI DEVICE;

	/** odrl:digitize */
```

### DeprecatedIsStillUsed
Deprecated member 'PARTY_SCOPE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:PartyScope */
	@Deprecated
	public static final IRI PARTY_SCOPE;

	/** odrl:Permission */
```

### DeprecatedIsStillUsed
Deprecated member 'INHERIT_RELATION' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:inheritRelation */
	@Deprecated
	public static final IRI INHERIT_RELATION;

	/** odrl:leftOperand */
```

### DeprecatedIsStillUsed
Deprecated member 'PAY' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:pay */
	@Deprecated
	public static final IRI PAY;

	/** odrl:payAmount */
```

### DeprecatedIsStillUsed
Deprecated member 'WRITE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:write */
	@Deprecated
	public static final IRI WRITE;

	/** odrl:writeTo */
```

### DeprecatedIsStillUsed
Deprecated member 'ATTACH_SOURCE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:attachSource */
	@Deprecated
	public static final IRI ATTACH_SOURCE;

	/** odrl:attribute */
```

### DeprecatedIsStillUsed
Deprecated member 'ALL2ND_CONNECTIONS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:All2ndConnections */
	@Deprecated
	public static final IRI ALL2ND_CONNECTIONS;

	/** odrl:AllConnections */
```

### DeprecatedIsStillUsed
Deprecated member 'WRITE_TO' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:writeTo */
	@Deprecated
	public static final IRI WRITE_TO;

	static {
```

### DeprecatedIsStillUsed
Deprecated member 'EXPORT' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:export */
	@Deprecated
	public static final IRI EXPORT;

	/** odrl:extract */
```

### DeprecatedIsStillUsed
Deprecated member 'ATTACH_POLICY' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:attachPolicy */
	@Deprecated
	public static final IRI ATTACH_POLICY;

	/** odrl:attachSource */
```

### DeprecatedIsStillUsed
Deprecated member 'EXTRACT_PAGE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:extractPage */
	@Deprecated
	public static final IRI EXTRACT_PAGE;

	/** odrl:extractWord */
```

### DeprecatedIsStillUsed
Deprecated member 'SECONDARY_USE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:secondaryUse */
	@Deprecated
	public static final IRI SECONDARY_USE;

	/** odrl:sell */
```

### DeprecatedIsStillUsed
Deprecated member 'SHARE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:share */
	@Deprecated
	public static final IRI SHARE;

	/** odrl:shareAlike */
```

### DeprecatedIsStillUsed
Deprecated member 'EXTRACT_WORD' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:extractWord */
	@Deprecated
	public static final IRI EXTRACT_WORD;

	/** odrl:fileFormat */
```

### DeprecatedIsStillUsed
Deprecated member 'UNDEFINED' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:undefined */
	@Deprecated
	public static final IRI UNDEFINED;

	/** odrl:unit */
```

### DeprecatedIsStillUsed
Deprecated member 'SCOPE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:scope */
	@Deprecated
	public static final IRI SCOPE;

	/** odrl:source */
```

### DeprecatedIsStillUsed
Deprecated member 'PREVIEW' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:preview */
	@Deprecated
	public static final IRI PREVIEW;

	/** odrl:print */
```

### DeprecatedIsStillUsed
Deprecated member 'ALL_GROUPS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:AllGroups */
	@Deprecated
	public static final IRI ALL_GROUPS;

	/** odrl:Group */
```

### DeprecatedIsStillUsed
Deprecated member 'EXTRACT_CHAR' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:extractChar */
	@Deprecated
	public static final IRI EXTRACT_CHAR;

	/** odrl:extractPage */
```

### DeprecatedIsStillUsed
Deprecated member 'APPEND' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:append */
	@Deprecated
	public static final IRI APPEND;

	/** odrl:appendTo */
```

### DeprecatedIsStillUsed
Deprecated member 'LEASE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:lease */
	@Deprecated
	public static final IRI LEASE;

	/** odrl:lend */
```

### DeprecatedIsStillUsed
Deprecated member 'ALL_CONNECTIONS' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:AllConnections */
	@Deprecated
	public static final IRI ALL_CONNECTIONS;

	/** odrl:AllGroups */
```

### DeprecatedIsStillUsed
Deprecated member 'IGNORE' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:ignore */
	@Deprecated
	public static final IRI IGNORE;

	/** odrl:include */
```

### DeprecatedIsStillUsed
Deprecated member 'INHERIT_ALLOWED' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:inheritAllowed */
	@Deprecated
	public static final IRI INHERIT_ALLOWED;

	/** odrl:inheritFrom */
```

### DeprecatedIsStillUsed
Deprecated member 'UNDEFINED_TERM' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:UndefinedTerm */
	@Deprecated
	public static final IRI UNDEFINED_TERM;

	// Properties
```

### DeprecatedIsStillUsed
Deprecated member 'TIMED_COUNT' is still used
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/ODRL2.java`
#### Snippet
```java
	/** odrl:timedCount */
	@Deprecated
	public static final IRI TIMED_COUNT;

	/** odrl:trackedParty */
```

## RuleId[id=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ContextStore.java`
#### Snippet
```java
	 */
	void increment(Resource context) {
		contextInfoMap.merge(context, 1L, (size, one) -> size + one);
		contentsChanged = true;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/opbuilder/OperationBuilder.java`
#### Snippet
```java
	public SUB withNullableBindings(Map<String, Value> bindings) {
		if (bindings != null) {
			bindings.forEach((key, value) -> bindingsBuilder.add(key, value));
		}
		return (SUB) this;
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/opbuilder/OperationBuilder.java`
#### Snippet
```java

	public SUB withBindings(Map<String, Value> bindings) {
		bindings.forEach((key, value) -> bindingsBuilder.add(key, value));
		return (SUB) this;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/Model.java`
#### Snippet
```java
	 */
	default Set<Resource> contexts() {
		Set<Resource> subjects = stream().map(st -> st.getContext()).collect(Collectors.toSet());
		return subjects;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/RDF4JProtocolSession.java`
#### Snippet
```java
		if (pingDelay > 0) {
			ping = pingScheduler.schedule(() -> {
				executeTransactionPing();
			}, pingDelay, TimeUnit.MILLISECONDS);
		}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/LinkedHashModel.java`
#### Snippet
```java
	public Optional<Namespace> removeNamespace(String prefix) {
		Optional<Namespace> result = getNamespace(prefix);
		result.ifPresent(namespace -> namespaces.remove(namespace));
		return result;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/ModelCollector.java`
#### Snippet
```java
	@Override
	public Supplier<Model> supplier() {
		return () -> factory.createEmptyModel();
	}

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
												: lang.toLowerCase(),
										StandardCharsets.UTF_8))
								.ifPresent(h -> hashSequence.add(h));
						hashSequence
								.add(hashFunction.hashString(l.getDatatype().stringValue(), StandardCharsets.UTF_8));
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
					.stream()
					.map(h -> new BigInteger(1, h.asBytes()))
					.reduce(BigInteger.ZERO, (v1, v2) -> v1.add(v2));
			return size;
		}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java
		Objects.requireNonNull(collection, "collection may not be null");

		consumeValues(m, container, containerType, v -> collection.add(v), contexts);

		return collection;
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java
			Resource... contexts) {
		Objects.requireNonNull(sourceModel, "input model may not be null");
		extract(containerType, sourceModel, container, st -> sink.add(st), contexts);
		return sink;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java

		Objects.requireNonNull(sink);
		consumeContainer(containerType, values, container, st -> sink.add(st), vf, contexts);
		return sink;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFContainers.java`
#### Snippet
```java

		Objects.requireNonNull(sink);
		consumeContainer(containerType, values, container, st -> sink.add(st), contexts);
		return sink;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
			Value object, C collection, Resource... contexts) {
		Objects.requireNonNull(collection);
		consume(vf, subject, predicate, object, st -> collection.add(st), contexts);
		return collection;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFCollections.java`
#### Snippet
```java
			Resource... contexts) {
		Objects.requireNonNull(sourceModel, "input model may not be null");
		extract(sourceModel, head, st -> sink.add(st), contexts);
		return sink;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFCollections.java`
#### Snippet
```java
		Objects.requireNonNull(collection, "collection may not be null");

		consumeValues(m, head, v -> collection.add(v), contexts);

		return collection;
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/RDFCollections.java`
#### Snippet
```java
			ValueFactory valueFactory, Resource... contexts) {
		Objects.requireNonNull(sink);
		consumeCollection(values, head, st -> sink.add(st), valueFactory, contexts);
		return sink;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/UpdateQuery.java`
#### Snippet
```java
		return prefix(Arrays
				.stream(namespaces)
				.map(n -> SparqlBuilder.prefix(n))
				.collect(Collectors.toList())
				.toArray(new Prefix[namespaces.length])
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/OuterQuery.java`
#### Snippet
```java
		return prefix(Arrays
				.stream(namespaces)
				.map(n -> SparqlBuilder.prefix(n))
				.collect(Collectors.toList())
				.toArray(new Prefix[namespaces.length])
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Set<Literal> objectLiterals(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
				.filter(o -> o instanceof Literal)
				.map(l -> (Literal) l)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Set<IRI> objectIRIs(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
				.filter(o -> o instanceof IRI)
				.map(r -> (IRI) r)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Set<Resource> objectResources(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
				.filter(o -> o instanceof Resource)
				.map(r -> (Resource) r)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 */
	public static Optional<IRI> predicate(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false).map(st -> st.getPredicate()).findAny();
	}

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Set<BNode> subjectBNodes(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getSubject())
				.filter(o -> o instanceof BNode)
				.map(r -> (BNode) r)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Set<IRI> subjectIRIs(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getSubject())
				.filter(o -> o instanceof IRI)
				.map(r -> (IRI) r)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Optional<IRI> subjectIRI(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getSubject())
				.filter(s -> s instanceof IRI)
				.map(s -> (IRI) s)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 */
	public static Optional<Resource> subject(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false).map(st -> st.getSubject()).findAny();
	}

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Optional<IRI> objectIRI(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
				.filter(o -> o instanceof IRI)
				.map(r -> (IRI) r)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Optional<BNode> subjectBNode(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getSubject())
				.filter(s -> s instanceof BNode)
				.map(s -> (BNode) s)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Optional<Literal> objectLiteral(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
				.filter(o -> o instanceof Literal)
				.map(l -> (Literal) l)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	 */
	public static Optional<Value> object(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false).map(st -> st.getObject()).findAny();
	}

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
	public static Optional<Resource> objectResource(Iterable<Statement> statements) {
		return StreamSupport.stream(statements.spliterator(), false)
				.map(st -> st.getObject())
				.filter(o -> o instanceof Resource)
				.map(r -> (Resource) r)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/repository/contextaware/src/main/java/org/eclipse/rdf4j/repository/contextaware/config/ContextAwareConfig.java`
#### Snippet
```java

			Models.objectIRI(model.getStatements(resource, INSERT_CONTEXT, null))
					.ifPresent(iri -> setInsertContext(iri));
		} catch (ArrayStoreException e) {
			throw new RepositoryConfigException(e);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/Connections.java`
#### Snippet
```java
			C statementCollection, Resource... contexts) throws RepositoryException {
		Objects.requireNonNull(statementCollection, "statementCollection may not be null");
		consumeRDFCollection(conn, head, st -> statementCollection.add(st), contexts);
		return statementCollection;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalTripleCallback.java`
#### Snippet
```java

	public JSONLDInternalTripleCallback(RDFHandler nextHandler, ValueFactory vf) {
		this(nextHandler, vf, new ParserConfig(), new ParseErrorLogger(), nodeID -> vf.createBNode(nodeID),
				() -> vf.createBNode());
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalTripleCallback.java`
#### Snippet
```java
	public JSONLDInternalTripleCallback(RDFHandler nextHandler, ValueFactory vf) {
		this(nextHandler, vf, new ParserConfig(), new ParseErrorLogger(), nodeID -> vf.createBNode(nodeID),
				() -> vf.createBNode());
	}

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDParser.java`
#### Snippet
```java
		try {
			final JSONLDInternalTripleCallback callback = new JSONLDInternalTripleCallback(getRDFHandler(),
					valueFactory, getParserConfig(), getParseErrorListener(), nodeID -> createNode(nodeID),
					() -> createNode());

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDParser.java`
#### Snippet
```java
			final JSONLDInternalTripleCallback callback = new JSONLDInternalTripleCallback(getRDFHandler(),
					valueFactory, getParserConfig(), getParseErrorListener(), nodeID -> createNode(nodeID),
					() -> createNode());

			final JsonLdOptions options = baseURI != null ? new JsonLdOptions(baseURI) : new JsonLdOptions();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDHierarchicalProcessor.java`
#### Snippet
```java
				.stream()
				.sorted(Map.Entry.comparingByValue((o1, o2) -> ((Map) o2).size() - ((Map) o1).size()))
				.map(entry -> entry.getKey())
				.collect(Collectors.toList());
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/NativeStore.java`
#### Snippet
```java
			final NativeSailStore mainStore = new NativeSailStore(dataDir, tripleIndexes, forceSync, valueCacheSize,
					valueIDCacheSize, namespaceCacheSize, namespaceIDCacheSize);
			this.store = new SnapshotSailStore(mainStore, () -> new MemoryOverflowIntoNativeStore()) {

				@Override
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ContextStore.java`
#### Snippet
```java
	 */
	void increment(Resource context) {
		contextInfoMap.merge(context, 1L, (size, one) -> size + one);
		contentsChanged = true;
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/OrderIterator.java`
#### Snippet
```java
						return e;
					}
				}).filter(exec -> exec != null).findFirst().ifPresent(exec -> {
					throw new QueryEvaluationException(exec);
				});
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	@Override
	public void meet(Coalesce node) throws RuntimeException {
		node.getArguments().stream().forEach(arg -> ensureNonAnonymousVar(arg));
		super.meet(node);
	}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/ParsedQueryPreprocessor.java`
#### Snippet
```java
	@Override
	public void meet(FunctionCall node) throws RuntimeException {
		node.getArgs().stream().forEach(arg -> ensureNonAnonymousVar(arg));
		super.meet(node);
	}
```

## RuleId[id=ComparatorCombinators]
### ComparatorCombinators
Can be replaced with 'Comparator.comparing'
in `core/spin/src/main/java/org/eclipse/rdf4j/spin/SpinParser.java`
#### Snippet
```java
	public static List<IRI> orderArguments(Set<IRI> args) {
		SortedSet<IRI> sortedArgs = new TreeSet<>(
				(IRI uri1, IRI uri2) -> uri1.getLocalName().compareTo(uri2.getLocalName()));
		sortedArgs.addAll(args);

```

### ComparatorCombinators
Can be replaced with 'Comparator.comparingInt'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java
	private static final long serialVersionUID = -5681843777254402303L;

	private static final Comparator<int[]> CMP = (int[] o1, int[] o2) -> o1[0] - o2[0];

	private static final int EOF = 0;
```

## RuleId[id=AbstractMethodCallInConstructor]
### AbstractMethodCallInConstructor
Call to 'abstract' method `prepareNamedSparqlSuppliers()` during object construction
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/RDF4JDao.java`
#### Snippet
```java
	public RDF4JDao(RDF4JTemplate rdf4JTemplate) {
		this.rdf4JTemplate = rdf4JTemplate;
		prepareNamedSparqlSuppliers(new NamedSparqlSupplierPreparer());
	}

```

### AbstractMethodCallInConstructor
Call to 'abstract' method `getWriterPreference()` during object construction
in `core/sail/api/src/main/java/org/eclipse/rdf4j/common/concurrent/locks/AbstractReadWriteLockManager.java`
#### Snippet
```java

		// WRITE_PREFERENCE can not be negative or 0.
		this.writePreference = Math.max(1, getWriterPreference());

		boolean releaseAbandoned = false;
```

## RuleId[id=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'validationReport' in a Serializable class
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ShaclSailValidationException.java`
#### Snippet
```java
public class ShaclSailValidationException extends SailException implements ValidationException {

	private final ValidationReport validationReport;

	ShaclSailValidationException(ValidationReport validationReport) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'repositoryConnectionFactory' in a Serializable class
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/tx/RDF4JRepositoryTransactionManager.java`
#### Snippet
```java
	private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

	private final TransactionalRepositoryConnectionFactory repositoryConnectionFactory;

	public RDF4JRepositoryTransactionManager(
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'optional' in a Serializable class
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/CoreDatatype.java`
#### Snippet
```java

		// Creating optionals are expensive so we precompute one
		private final Optional<GEO> optional;

		GEO(IRI iri) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'optional' in a Serializable class
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/CoreDatatype.java`
#### Snippet
```java

		// Creating optionals are expensive so we precompute one
		private final Optional<XSD> optional;

		XSD(IRI iri, boolean primitive, boolean duration, boolean integer, boolean derived, boolean decimal,
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'optional' in a Serializable class
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/base/CoreDatatype.java`
#### Snippet
```java

		// Creating optionals are expensive so we precompute one
		private final Optional<RDF> optional;

		RDF(IRI iri) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'remoteValidation' in a Serializable class
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/shacl/RemoteShaclValidationException.java`
#### Snippet
```java
	private static final long serialVersionUID = 1546454692754781492L;

	private final RemoteValidation remoteValidation;

	public RemoteShaclValidationException(StringReader stringReader, String s, RDFFormat format) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'iter' in a Serializable class
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/EmptyBindingSet.java`
#### Snippet
```java
	}

	private final EmptyBindingIterator iter = new EmptyBindingIterator();

	@Override
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'formats' in a Serializable class
in `spring-components/spring-boot-sparql-web/src/main/java/org/eclipse/rdf4j/http/server/readonly/sparql/QueryTypes.java`
#### Snippet
```java
	};

	private final FileFormat[] formats;
	private final Predicate<Query> typeChecker;

```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
					try {
						next.shutDown();
					} catch (RepositoryException e) {

					}
```

### CatchMayIgnoreException
Empty `catch` block
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
			parser.parse(in);
			fail("Did not find expected parse exception");
		} catch (QueryResultParseException expected) {

		}
```

### CatchMayIgnoreException
Empty `catch` block
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
			parser.parse(in);
			fail("Did not find expected parse exception");
		} catch (QueryResultParseException e) {

		}
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is set
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	 * repository configurations.
	 *
	 * @return a list of repository ID strings.
	 * @throws RepositoryException
	 * @see {@link #getInitializedRepositoryIDs()}
```

### MismatchedJavadocCode
Method is specified to return 'false' but its return type is not boolean
in `examples/src/main/java/org/eclipse/rdf4j/examples/function/ExistingPalindromeFunction.java`
#### Snippet
```java
	 *
	 * @return A boolean literal representing true if the input argument is a palindrome and exists in the database,
	 *         false otherwise.
	 * @throws ValueExprEvaluationException if more than one argument is supplied or if the supplied argument is not a
	 *                                      literal.
```

### MismatchedJavadocCode
Method is specified to return 'false' but its return type is not boolean
in `examples/src/main/java/org/eclipse/rdf4j/examples/function/PalindromeFunction.java`
#### Snippet
```java
	 * Executes the palindrome function.
	 *
	 * @return A boolean literal representing true if the input argument is a palindrome, false otherwise.
	 * @throws ValueExprEvaluationException if more than one argument is supplied or if the supplied argument is not a
	 *                                      literal.
```

## RuleId[id=AssignmentToForLoopParameter]
### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleUtil.java`
#### Snippet
```java
			if (Character.isHighSurrogate((char) codePoint)) {
				// surrogate pair, skip second member in char sequence.
				i++;
			}
		}
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/common/text/src/main/java/org/eclipse/rdf4j/common/text/StringUtil.java`
#### Snippet
```java
							// U+10000 - U+10FFFF
							int code = str.codePointAt(i);
							i++;
							appendable.append("\\U000").append(hex(code));
						} else {
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/ReasoningUpdateBenchmark.java`
#### Snippet
```java

			connection.begin();
			connection.add(resourceAsStream("data" + i++ + ".ttl"), "", RDFFormat.TURTLE);
			connection.commit();
		}
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailBuffer.java`
#### Snippet
```java
				while (i > 0) {
					operations.remove(i);
					i--;
				}
				return;
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchStore.java`
#### Snippet
```java
			if (LocalDateTime.now().isAfter(tenMinFromNow)) {
				logger.error(
						"Could not connect to Elasticsearch after " + time + " " + timeUnit.toString() + " of trying!");

				try {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchStore.java`
#### Snippet
```java
				}
				throw new RuntimeException(
						"Could not connect to Elasticsearch after " + time + " " + timeUnit.toString() + " of trying!");

			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java

					if (o instanceof IRI) {
						stringBuilder.append("IRI<").append(o.toString()).append(">");
					} else if (o instanceof BNode) {
						stringBuilder.append("Bnode<").append(o.toString()).append(">");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
						stringBuilder.append("IRI<").append(o.toString()).append(">");
					} else if (o instanceof BNode) {
						stringBuilder.append("Bnode<").append(o.toString()).append(">");
					} else if (o instanceof Literal) {
						stringBuilder.append("Literal<").append(o.toString()).append(">");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchDataStructure.java`
#### Snippet
```java
						stringBuilder.append("Bnode<").append(o.toString()).append(">");
					} else if (o instanceof Literal) {
						stringBuilder.append("Literal<").append(o.toString()).append(">");
					} else if (o instanceof Boolean) {
						stringBuilder.append("Boolean<").append(o).append(">");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
				message.append(" =======================\n");

				System.out.print(message.toString());
			} else if (missingBindings.isEmpty() && unexpectedBindings.isEmpty()) {
				message.append("unexpected duplicate in result.\n");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLQueryTest.java`
#### Snippet
```java
				message.append(" =======================\n");

				System.out.print(message.toString());
			}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/key/CompositeKey2.java`
#### Snippet
```java
					String.format(
							"Refusing to replace key2 - it is already set to value '%s'",
							this.key2.toString()));
		}
		return new CompositeKey2<>(this.key1, key2);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/key/CompositeKey2.java`
#### Snippet
```java
					String.format(
							"Refusing to replace key1 - it is already set to value '%s'",
							this.key1.toString()));
		}
		return new CompositeKey2<>(key1, this.key2);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/dao/support/UpdateWithModelBuilder.java`
#### Snippet
```java
			StringWriter sw = new StringWriter();
			Rio.write(model, sw, RDFFormat.TURTLE);
			logger.debug("adding the following triples:\n{}", sw.toString());
		}
		con.add(model);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/RDF4JTemplate.java`
#### Snippet
```java
			StringWriter out = new StringWriter();
			Rio.write(report, out, RDFFormat.TURTLE);
			logger.error("Validation report:\n{}", out.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneIndex.java`
#### Snippet
```java
			return ShapeField.QueryRelation.DISJOINT;
		default:
			throw new IllegalArgumentException("The geo function [" + op.toString() + "] is not supported");
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/ModelException.java`
#### Snippet
```java
		if (!v1.toString().equals(v2.toString())) {
			sb.append("Object is both ");
			sb.append(v1.toString());
			sb.append(" and ");
			sb.append(v2.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/ModelException.java`
#### Snippet
```java
			sb.append(v1.toString());
			sb.append(" and ");
			sb.append(v2.toString());
		} else if (!v1.getClass().getName().equals(v2.getClass().getName())) {
			sb.append("Object is both ");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
				return subject.toString();
			} else {
				return subject.toString() + " [" + context.toString() + "]";
			}
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/graphpattern/GroupGraphPattern.java`
#### Snippet
```java
			pattern.append(SparqlBuilderUtils.getBracedString(innerPattern.toString()));
		} else {
			pattern.append(innerPattern.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
				}

				logger.debug("SHACL validation report is serialised as: " + serialisation.toString());

				Optional<RDFFormat> rdfFormat = RDFFormat.matchMIMEType(serialisation.toString(), rdfFormats);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
		File configFile = new File(dataDir, CFG_FILE);
		Model model = getModelFactory().createEmptyModel();
		String ns = configFile.toURI().toString() + "#";
		config.export(model, SimpleValueFactory.getInstance().createIRI(ns, config.getID()));
		File part = new File(configFile.getParentFile(), configFile.getName() + ".part");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/SimpleBinding.java`
#### Snippet
```java
	@Override
	public String toString() {
		return name + "=" + value.toString();
	}
}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
							opentag++;
							int endOfFirstStartTag = charBuf.substring(i).indexOf(">");
							charBuf.insert(endOfFirstStartTag + i, contextPrefixes.toString());
						} else {
							opentag--;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		IRI subj = vf.createIRI(EXAMPLE_NS + PICASSO);
		IRI pred = vf.createIRI(EXAMPLE_NS + PAINTS);
		Literal obj = vf.createLiteral("guernica" + sb.toString(), "es");

		testValueRoundTrip(subj, pred, obj);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		IRI subj = vf.createIRI(EXAMPLE_NS + PICASSO);
		IRI pred = vf.createIRI(EXAMPLE_NS + PAINTS);
		Literal obj = vf.createLiteral("guernica" + sb.toString());

		testValueRoundTrip(subj, pred, obj);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		IRI subj = vf.createIRI(EXAMPLE_NS + PICASSO);
		IRI pred = vf.createIRI(EXAMPLE_NS + PAINTS);
		Literal obj = vf.createLiteral("guernica" + sb.toString());

		testValueRoundTrip(subj, pred, obj);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/RDFStoreTest.java`
#### Snippet
```java
		IRI subj = vf.createIRI(EXAMPLE_NS + PICASSO);
		IRI pred = vf.createIRI(EXAMPLE_NS + PAINTS);
		IRI obj = vf.createIRI(EXAMPLE_NS + GUERNICA + sb.toString());

		testValueRoundTrip(subj, pred, obj);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/rdfterm/UUID.java`
#### Snippet
```java
		}

		IRI uri = valueFactory.createIRI("urn:uuid:" + java.util.UUID.randomUUID().toString());

		return uri;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/RenderUtils.java`
#### Snippet
```java
		if (value instanceof IRI) {
			IRI aURI = (IRI) value;
			builder.append("<").append(aURI.toString()).append(">");
		} else if (value instanceof BNode) {
			builder.append("_:").append(((BNode) value).getID());
```

## RuleId[id=Java9CollectionFactory]
### Java9CollectionFactory
Can be replaced with 'List.copyOf()' call
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/helpers/QueryResultCollector.java`
#### Snippet
```java
	public void startQueryResult(List<String> bindingNames) throws TupleQueryResultHandlerException {
		endQueryResultFound = false;
		this.bindingNames = Collections.unmodifiableList(new ArrayList<>(bindingNames));
		bindingSets = new ArrayList<>();
	}
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `NotifyingPushablePlanNode` may be 'static'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/InnerJoin.java`
#### Snippet
```java
	}

	class NotifyingPushablePlanNode implements PushablePlanNode {
		PushablePlanNode delegate;

```

### InnerClassMayBeStatic
Inner class `CircularBuffer` may be 'static'
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/LmdbSailStore.java`
#### Snippet
```java
	 * @param <T> Type of elements within this buffer
	 */
	final class CircularBuffer<T> {

		private final T[] elements;
```

### InnerClassMayBeStatic
Inner class `QueryInterruptIteration` may be 'static'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/impl/AbstractParserQuery.java`
#### Snippet
```java

	@Deprecated(since = "4.1.0")
	protected class QueryInterruptIteration extends TimeLimitIteration<BindingSet, QueryEvaluationException> {

		public QueryInterruptIteration(Iteration<? extends BindingSet, ? extends QueryEvaluationException> iter,
```

### InnerClassMayBeStatic
Inner class `QueryInterruptIteration` may be 'static'
in `core/queryparser/api/src/main/java/org/eclipse/rdf4j/query/parser/impl/AbstractParserQuery.java`
#### Snippet
```java

	@Deprecated(since = "4.1.0")
	protected class QueryInterruptIteration extends TimeLimitIteration<BindingSet, QueryEvaluationException> {

		public QueryInterruptIteration(Iteration<? extends BindingSet, ? extends QueryEvaluationException> iter,
```

### InnerClassMayBeStatic
Inner class `SubjectInContext` may be 'static'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
	}

	private class SubjectInContext {

		private final Resource subject;
```

### InnerClassMayBeStatic
Inner class `SameTermCollector` may be 'static'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	}

	protected class SameTermCollector extends AbstractQueryModelVisitor<VisitorException> {

		private final Set<SameTerm> collectedSameTerms = new HashSet<>();
```

### InnerClassMayBeStatic
Inner class `VarCollector` may be 'static'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/TupleExprBuilder.java`
#### Snippet
```java
	}

	protected class VarCollector extends AbstractQueryModelVisitor<VisitorException> {

		private final Set<Var> collectedVars = new HashSet<>();
```

### InnerClassMayBeStatic
Inner class `ParentFixingVisitor` may be 'static'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ParentReferenceCleaner.java`
#### Snippet
```java
	}

	private class ParentFixingVisitor extends AbstractQueryModelVisitor<RuntimeException> {

		private final ArrayDeque<QueryModelNode> ancestors = new ArrayDeque<>();
```

### InnerClassMayBeStatic
Inner class `VarNameCollector` may be 'static'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ConstantOptimizer.java`
#### Snippet
```java
	}

	protected class VarNameCollector extends AbstractQueryModelVisitor<RuntimeException> {

		final Set<String> varNames = new HashSet<>();
```

### InnerClassMayBeStatic
Inner class `VariableFinder` may be 'static'
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/ProjectionRemovalOptimizer.java`
#### Snippet
```java
	}

	private class VariableFinder extends AbstractQueryModelVisitor<RuntimeException> {
		private final Set<Var> vars = new HashSet<>();

```

## RuleId[id=NestedSynchronizedStatement]
### NestedSynchronizedStatement
Nested `synchronized` statement
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
		synchronized (transactionLock) {
			if (!isActive()) {
				synchronized (transactionLock) {
					sparqlTransaction = new StringBuilder();
					this.pendingAdds = getModelFactory().createEmptyModel();
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
		synchronized (transactionLock) {
			if (isActive()) {
				synchronized (transactionLock) {
					sparqlTransaction = null;
					pendingAdds = getModelFactory().createEmptyModel();
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
		synchronized (transactionLock) {
			if (isActive()) {
				synchronized (transactionLock) {
					flushPendingAdds();
					flushPendingRemoves();
```

## RuleId[id=StringEqualsEmptyString]
### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/Select.java`
#### Snippet
```java
		assert this.connection != null;
		this.mapper = mapper;
		if (queryFragment.trim().equals("")) {
			logger.error("Query is empty", new Throwable("This throwable is just to log the stack trace"));

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/planNodes/BindSelect.java`
#### Snippet
```java
		this.source = PlanNodeHelper.handleSorting(this, source);

		if (query.trim().equals("")) {
			throw new IllegalStateException();
		}
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
		String msg = "Namespace prefix '" + prefix + "' used but not defined";

		if ("".equals(prefix)) {
			msg = "Default namespace used but not defined";
		}
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java

	private String normalizePath(String path) {
		if ("".equals(path)) {
			return isScheme("http") || isScheme("https") ? "/" : "";
		} else if (isScheme("file")) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		// check prefix
		String prefix = split[0];
		if (!"".equals(prefix)) {
			if (isNotPrefixStartChar(prefix.charAt(0))) {
				return false;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		String name = split[1];

		if (!"".equals(name)) {
			// check name
			if (isNotNameStartChar(name.charAt(0))) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/csv/SPARQLResultsCSVMappingStrategy.java`
#### Snippet
```java
			if (valueString.startsWith("_:")) {
				v = valueFactory.createBNode(valueString.substring(2));
			} else if (!"".equals(valueString)) {
				if (numberPattern.matcher(valueString).matches()) {
					v = parseNumberPatternMatch(valueString);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVMappingStrategy.java`
#### Snippet
```java
		} else if (valueString.startsWith("\"")) {
			v = parseLiteral(valueString);
		} else if (!"".equals(valueString)) {
			if (numberPattern.matcher(valueString).matches()) {
				v = parseNumberPatternMatch(valueString);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
		String attName = "xmlns";

		if (!"".equals(prefix)) {
			attName += ":" + prefix;
		}
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
				// A limited set of unqualified attributes must be supported by
				// parsers, as is specified in section 6.1.4 of the spec
				if ("".equals(namespace)) {
					if (localName.equals("ID") || localName.equals("about") || localName.equals("resource")
							|| localName.equals("parseType") || localName.equals("type")) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/SAXFilter.java`
#### Snippet
```java
				}

				if ("".equals(namespace)) {
					rdfParser.reportError("unqualified attribute '" + qName + "' not allowed",
							XMLParserSettings.FAIL_ON_INVALID_QNAME);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
			// element name is uri's type
			IRI className;
			if ("".equals(namespaceURI)) {
				// No namespace, use base URI
				className = buildResourceFromLocalName(localName);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java

	void setXMLLang(String xmlLang) {
		if ("".equals(xmlLang)) {
			this.xmlLang = null;
		} else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java
					}

					if (!datablock.equals("")) {
						parser.parse(new StringReader(datablock), "");
					}
```

## RuleId[id=UnnecessaryBoxing]
### UnnecessaryBoxing
Unnecessary boxing
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java
					NativeValue nv = getNativeValue(value);
					nv.setInternalID(id, revision);
					valueIDCache.put(nv, Integer.valueOf(id));
				}
			}
```

### UnnecessaryBoxing
Unnecessary boxing
in `core/queryresultio/binary/src/main/java/org/eclipse/rdf4j/query/resultio/binary/BinaryQueryResultWriter.java`
#### Snippet
```java
		writeString(namespace);

		Integer result = new Integer(nextNamespaceID);
		namespaceTable.put(namespace, result);

```

## RuleId[id=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `endpointToService`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	public FederatedService getService(String serviceUrl) throws QueryEvaluationException {
		FederatedService service;
		synchronized (endpointToService) {
			service = endpointToService.get(serviceUrl);
			if (service == null) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `endpointToService`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	 */
	public void registerService(String serviceUrl, FederatedService service) {
		synchronized (endpointToService) {
			endpointToService.put(serviceUrl, service);
		}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `endpointToService`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	public void unregisterService(String serviceUrl) {
		FederatedService service;
		synchronized (endpointToService) {
			service = endpointToService.remove(serviceUrl);
		}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `endpointToService`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java

	public void unregisterAll() {
		synchronized (endpointToService) {
			for (FederatedService service : endpointToService.values()) {
				try {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `endpointToService`
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/AbstractFederatedServiceResolver.java`
#### Snippet
```java
	 */
	public boolean hasService(String serviceUrl) {
		synchronized (endpointToService) {
			return endpointToService.containsKey(serviceUrl);
		}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	 */
	public Collection<Repository> getInitializedRepositories() {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return new ArrayList<>(initializedRepositories.values());
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		// FIXME: uninitialized, removed repositories won't be cleaned up.
		try {
			synchronized (initializedRepositories) {
				Iterator<Map.Entry<String, Repository>> iter = initializedRepositories.entrySet().iterator();

```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

	protected void updateInitializedRepositories() {
		synchronized (initializedRepositories) {
			Iterator<Repository> iter = initializedRepositories.values().iterator();
			while (iter.hasNext()) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
		boolean isRemoved = hasRepositoryConfig(repositoryID);

		synchronized (initializedRepositories) {
			if (isRemoved) {
				logger.debug("Shutdown repository {} after removal of configuration.", repositoryID);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	@Override
	public Repository getRepository(String identity) throws RepositoryConfigException, RepositoryException {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			Repository result = initializedRepositories.get(identity);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

	Repository getInitializedRepository(String repositoryID) {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return initializedRepositories.get(repositoryID);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java

	Repository removeInitializedRepository(String repositoryID) {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return initializedRepositories.remove(repositoryID);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	 */
	public void shutDown() {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			for (Repository repository : initializedRepositories.values()) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `initializedRepositories`
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/RepositoryManager.java`
#### Snippet
```java
	 */
	public Set<String> getInitializedRepositoryIDs() {
		synchronized (initializedRepositories) {
			updateInitializedRepositories();
			return new HashSet<>(initializedRepositories.keySet());
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'context != null' covered by subsequent condition 'context instanceof IRI'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLConnection.java`
#### Snippet
```java
				}
				createBGP(qb, subject, predicate, object);
				if (context != null && context instanceof IRI) {
					qb.append(" } \n");
				}
```

### ConditionCoveredByFurtherCondition
Condition 'obj == null' covered by subsequent condition '!(obj instanceof BindingSetAssignment)'
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/BindingSetAssignment.java`
#### Snippet
```java
			return true;
		}
		if (obj == null || !(obj instanceof BindingSetAssignment)) {
			return false;
		}
```

### ConditionCoveredByFurtherCondition
Condition 'encoding != null' covered by subsequent condition '"base64".equalsIgnoreCase(...)'
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionSAXParser.java`
#### Snippet
```java
			String encoding = atts.get(TransactionXMLConstants.ENCODING_ATT);

			if (encoding != null && "base64".equalsIgnoreCase(encoding)) {
				text = new String(javax.xml.bind.DatatypeConverter.parseBase64Binary(text));
			}
```

### ConditionCoveredByFurtherCondition
Condition 'path.length() == 0' covered by subsequent condition 'path.indexOf(...) \< 0'
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedIRI.java`
#### Snippet
```java

	private String pctEncodingNormalization(String path) {
		if (path == null || path.length() == 0 || path.indexOf('%') < 0) {
			return path; // no pct encodings
		}
```

### ConditionCoveredByFurtherCondition
Condition 'o == null' covered by subsequent condition '!(o instanceof AddRemoveOperation)'
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailBuffer.java`
#### Snippet
```java
		// check if the last operation was adding/Removing triples
		Operation o = (operations.isEmpty()) ? null : operations.get(operations.size() - 1);
		if ((o == null) || !(o instanceof AddRemoveOperation)) {
			o = new AddRemoveOperation(useType);
			operations.add(o);
```

### ConditionCoveredByFurtherCondition
Condition 'o == null' covered by subsequent condition '!(o instanceof AddRemoveOperation)'
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSailBuffer.java`
#### Snippet
```java
		// check if the last operation was adding/Removing triples
		Operation o = (operations.isEmpty()) ? null : operations.get(operations.size() - 1);
		if ((o == null) || !(o instanceof AddRemoveOperation)) {
			o = new AddRemoveOperation(useType);
			operations.add(o);
```

## RuleId[id=MissingDeprecatedAnnotation]
### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
	 * @deprecated Use {@link AbstractCloseableIteration#isClosed()} instead.
	 */
	protected volatile boolean closed = false;

	/**
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
	 * @deprecated No replacement, don't use static shared int variables.
	 */
	protected static int NEXT_JOIN_ID = 1;

	/* Constants */
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/federation/JoinExecutorBase.java`
#### Snippet
```java
	 * @deprecated Use {@link #isFinished()} instead.
	 */
	protected volatile boolean finished = false;

	protected final QueueCursor<CloseableIteration<T, QueryEvaluationException>> rightQueue = new QueueCursor<>(1024);
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
	 * @deprecated Use {@link #statement(ValueFactory, Triple, Resource)} instead
	 */
	public static Statement toStatement(ValueFactory vf, Triple triple, Resource context) {
		return statement(vf, triple, context);
	}
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
	 * @deprecated since 3.7.0 - use {@link #statement(Triple, Resource)} instead
	 */
	public static Statement toStatement(Triple triple, Resource context) {
		return statement(SimpleValueFactory.getInstance(), triple, context);
	}
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Statements.java`
#### Snippet
```java
	 * @deprecated Use {@link #statement(Triple)} instead
	 */
	public static Statement toStatement(Triple triple) {
		return statement(triple);
	}
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `core/query/src/main/java/org/eclipse/rdf4j/query/impl/QueueCursor.java`
#### Snippet
```java
	 *             {@link java.lang.ref.Cleaner}.
	 */
	public QueueCursor(BlockingQueue<E> queue) {
		super(queue);
	}
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `core/queryresultio/api/src/main/java/org/eclipse/rdf4j/query/resultio/BooleanQueryResultWriter.java`
#### Snippet
```java
	 * @deprecated Use {@link #handleBoolean(boolean)} instead.
	 */
	void write(boolean value) throws IOException;

	/**
```

## RuleId[id=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail.elasticsearchstore` is unnecessary and can be removed
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/config/ElasticsearchStoreFactory.java`
#### Snippet
```java

/**
 * A {@link SailFactory} that creates {@link org.eclipse.rdf4j.sail.elasticsearchstore.ElasticsearchStore}s based on RDF
 * configuration data.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.bind` is unnecessary, and can be replaced with an import
in `core/http/protocol/src/main/java/org/eclipse/rdf4j/http/protocol/transaction/TransactionSAXParser.java`
#### Snippet
```java

			if (encoding != null && "base64".equalsIgnoreCase(encoding)) {
				text = new String(javax.xml.bind.DatatypeConverter.parseBase64Binary(text));
			}
			Literal lit;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.federation` is unnecessary and can be removed
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/FederatedServiceResolver.java`
#### Snippet
```java

	/**
	 * Retrieve the {@link org.eclipse.rdf4j.query.algebra.evaluation.federation.FederatedService} registered for
	 * serviceUrl. If there is no service registered for serviceUrl, a new
	 * {@link org.eclipse.rdf4j.query.algebra.evaluation.federation.SPARQLFederatedService} is created and registered.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.federation` is unnecessary and can be removed
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/federation/FederatedServiceResolver.java`
#### Snippet
```java
	 *
	 * @param serviceUrl locator for the federation service
	 * @return the {@link org.eclipse.rdf4j.query.algebra.evaluation.federation.FederatedService}, created fresh if
	 *         necessary
	 * @throws QueryEvaluationException If there was an exception generated while retrieving the service.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra` is unnecessary and can be removed
in `core/queryalgebra/model/src/main/java/org/eclipse/rdf4j/query/algebra/helpers/AbstractQueryModelVisitor.java`
#### Snippet
```java

	/**
	 * Method called by all <var>meet</var> methods with a {@link org.eclipse.rdf4j.query.algebra.NAryValueOperator}
	 * node as argument. Forwards the call to {@link #meetNode} by default.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.common.concurrent.locks` is unnecessary, and can be replaced with an import
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailGeoSPARQLTest.java`
#### Snippet
```java
		MemoryStore memoryStore = new MemoryStore();
		// enable lock tracking
		org.eclipse.rdf4j.common.concurrent.locks.Properties.setLockTrackingEnabled(true);
		sail = new LuceneSail();
		configure(sail);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.common.concurrent.locks` is unnecessary, and can be replaced with an import
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailGeoSPARQLTest.java`
#### Snippet
```java
			repository.shutDown();
		}
		org.eclipse.rdf4j.common.concurrent.locks.Properties.setLockTrackingEnabled(false);

	}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.common.concurrent.locks` is unnecessary, and can be replaced with an import
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
			repository.shutDown();
		}
		org.eclipse.rdf4j.common.concurrent.locks.Properties.setLockTrackingEnabled(false);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.common.concurrent.locks` is unnecessary, and can be replaced with an import
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
		MemoryStore memoryStore = new MemoryStore();
		// enable lock tracking
		org.eclipse.rdf4j.common.concurrent.locks.Properties.setLockTrackingEnabled(true);
		sail = new LuceneSail();
		configure(sail);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.http.protocol` is unnecessary and can be removed
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPUpdate.java`
#### Snippet
```java
 *
 * @author Jeen Broekstra
 * @see org.eclipse.rdf4j.http.protocol.UnauthorizedException
 */
public class HTTPUpdate extends AbstractHTTPUpdate {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary, and can be replaced with an import
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
	private byte[] literal2legacy(Literal literal) throws IOException {
		IRI dt = literal.getDatatype();
		if (org.eclipse.rdf4j.model.vocabulary.XSD.STRING.equals(dt)
				|| org.eclipse.rdf4j.model.vocabulary.RDF.LANGSTRING.equals(dt)) {
			return literal2data(literal.getLabel(), literal.getLanguage(), null, false);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary, and can be replaced with an import
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
		IRI dt = literal.getDatatype();
		if (org.eclipse.rdf4j.model.vocabulary.XSD.STRING.equals(dt)
				|| org.eclipse.rdf4j.model.vocabulary.RDF.LANGSTRING.equals(dt)) {
			return literal2data(literal.getLabel(), literal.getLanguage(), null, false);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary, and can be replaced with an import
in `core/sail/lmdb/src/main/java/org/eclipse/rdf4j/sail/lmdb/ValueStore.java`
#### Snippet
```java
				return new LmdbLiteral(revision, label, datatype, id);
			} else {
				return new LmdbLiteral(revision, label, org.eclipse.rdf4j.model.vocabulary.XSD.STRING, id);
			}
		} else {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.http.protocol` is unnecessary and can be removed
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
 * @author Jeen Broekstra
 * @author Herko ter Horst
 * @see org.eclipse.rdf4j.http.protocol.UnauthorizedException
 */
public class HTTPRepository extends AbstractRepository implements HttpClientDependent, SessionManagerDependent {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.rio` is unnecessary and can be removed
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/ParseErrorListener.java`
#### Snippet
```java
	 * @param lineNo A line number related to the error, or -1 if not available or applicable.
	 * @param colNo  A column number related to the error, or -1 if not available or applicable.
	 * @see org.eclipse.rdf4j.rio.RDFParser#setStopAtFirstError
	 */
	void error(String msg, long lineNo, long colNo);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/RDFWriterFactory.java`
#### Snippet
```java
	 * @throws URISyntaxException
	 * @throws UnsupportedOperationException if the RDFWriter the specific format does not support writing to a
	 *                                       {@link java.io.Writer}
	 */
	RDFWriter getWriter(Writer writer, String baseURI) throws URISyntaxException;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/RDFWriterFactory.java`
#### Snippet
```java
	 * @param writer The Writer to write the RDF to.
	 * @throws UnsupportedOperationException if the RDFWriter the specific format does not support writing to a
	 *                                       {@link java.io.Writer}
	 */
	RDFWriter getWriter(Writer writer);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.rio.helpers` is unnecessary and can be removed
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/TurtleWriterSettings.java`
#### Snippet
```java
	/**
	 * Boolean setting for Turtle/TriG Writer to determine if the abbreviated syntax for numeric datatypes is to be used
	 * when {@link org.eclipse.rdf4j.rio.helpers.BasicWriterSettings.html#PRETTY_PRINT} is <code>true</code>.
	 * <p>
	 * This setting has no effect when pretty print is false.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.xml.security` is unnecessary, and can be replaced with an import
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/rdfxml/RDFXMLParserTestCase.java`
#### Snippet
```java

		public CanonXMLValueFactory() throws InvalidCanonicalizerException, ParserConfigurationException {
			org.apache.xml.security.Init.init();

			c14n = Canonicalizer.getInstance(Canonicalizer.ALGO_ID_C14N_EXCL_OMIT_COMMENTS);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.rio.helpers` is unnecessary and can be removed
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/AbstractRDFParser.java`
#### Snippet
```java
	 * messages or exceptions that may be generated during the creation of the literal.
	 *
	 * @see org.eclipse.rdf4j.rio.helpers.RDFParserHelper#createLiteral(String, String, IRI, ParserConfig,
	 *      ParseErrorListener, ValueFactory, long, long)
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@BeforeEach
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/SPARQLUpdateTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@AfterEach
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link java.util.Set#remove(java.lang.Object)}.
	 */
	@Disabled("TODO: Implement me!")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#removeNamespace(java.lang.String)}.
	 */
	@Disabled("TODO: Implement me!")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespace(java.lang.String)}.
	 */
	@Disabled("TODO: Implement me!")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link java.util.Set#add(java.lang.Object)}.
	 */
	@Disabled("TODO: Implement me!")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@BeforeAll
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@AfterAll
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(java.lang.String, java.lang.String)} .
	 */
	@Disabled("TODO: Implement me!")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(java.lang.String, java.lang.String)} .
	 */
	@Disabled("TODO: Implement me!")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/AbstractModelPerformanceTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link java.util.Set#contains(java.lang.Object)}.
	 */
	@Disabled("TODO: Implement me!")
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#removeNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#removeNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@AfterEach
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(java.lang.String, java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(java.lang.String, java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(java.lang.String, java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespaces()}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@BeforeEach
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#removeNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#removeNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(org.eclipse.rdf4j.model.Namespace)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(org.eclipse.rdf4j.model.Namespace)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespaces()}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(org.eclipse.rdf4j.model.Namespace)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#setNamespace(org.eclipse.rdf4j.model.Namespace)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#getNamespaces()}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#removeNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelNamespacesTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#removeNamespace(java.lang.String)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@BeforeEach
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@AfterEach
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#filter(Resource, IRI, Value, Resource...)}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#objects()}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@BeforeEach
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#subjects()}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#contexts()}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@AfterEach
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#predicates()}.
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `testsuites/model/src/main/java/org/eclipse/rdf4j/testsuite/model/ModelTest.java`
#### Snippet
```java

	/**
	 * Test method for {@link org.eclipse.rdf4j.model.Model#contains(Resource, IRI, Value, Resource...)} .
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * @param value The value of the attribute. The integer value will be transformed to a string using the method
	 *              <var>String.valueOf(int)</var>.
	 * @see java.lang.String#valueOf(int)
	 */
	public void setAttribute(String name, int value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * @param value The value of the attribute. The boolean value will be transformed to a string using the method
	 *              <var>String.valueOf(boolean)</var>.
	 * @see java.lang.String#valueOf(boolean)
	 */
	public void setAttribute(String name, boolean value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 *
	 * @param elName The element name.
	 * @see #setAttribute(java.lang.String, java.lang.String)
	 */
	public void startTag(String elName) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 *
	 * @param elName The element name.
	 * @see #setAttribute(java.lang.String, java.lang.String)
	 */
	public void startTag(String elName) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * @param elName The element name.
	 * @param text   The text.
	 * @see #setAttribute(java.lang.String, java.lang.String)
	 */
	public void unescapedTextElement(String elName, String text) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * @param elName The element name.
	 * @param text   The text.
	 * @see #setAttribute(java.lang.String, java.lang.String)
	 */
	public void unescapedTextElement(String elName, String text) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 *
	 * @param elName The element name.
	 * @see #setAttribute(java.lang.String, java.lang.String)
	 */
	public void emptyElement(String elName) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 *
	 * @param elName The element name.
	 * @see #setAttribute(java.lang.String, java.lang.String)
	 */
	public void emptyElement(String elName) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * @param elName The element name.
	 * @param text   The text.
	 * @see #setAttribute(java.lang.String, java.lang.String)
	 */
	public void textElement(String elName, String text) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * @param elName The element name.
	 * @param text   The text.
	 * @see #setAttribute(java.lang.String, java.lang.String)
	 */
	public void textElement(String elName, String text) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * @param value  The boolean value. The integer value will be transformed to a string using the method
	 *               <var>String.valueOf(boolean)</var>.
	 * @see java.lang.String#valueOf(boolean)
	 */
	public void textElement(String elName, boolean value) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLWriter.java`
#### Snippet
```java
	 * @param value  The value. The integer value will be transformed to a string using the method
	 *               <var>String.valueOf(int)</var>.
	 * @see java.lang.String#valueOf(int)
	 */
	public void textElement(String elName, int value) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.parsers` is unnecessary, and can be replaced with an import
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLReaderFactory.java`
#### Snippet
```java
		if (reader == null) {
			try {
				javax.xml.parsers.SAXParserFactory factory = javax.xml.parsers.SAXParserFactory.newInstance();
				factory.setNamespaceAware(true);

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.parsers` is unnecessary, and can be replaced with an import
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/XMLReaderFactory.java`
#### Snippet
```java
		if (reader == null) {
			try {
				javax.xml.parsers.SAXParserFactory factory = javax.xml.parsers.SAXParserFactory.newInstance();
				factory.setNamespaceAware(true);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.xml.sax` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/SimpleSAXParser.java`
#### Snippet
```java
	 * @throws SAXException If the SimpleSAXParser was unable to create an XMLReader.
	 * @see #setListener
	 * @see org.xml.sax.XMLReader
	 * @see org.eclipse.rdf4j.common.xml.XMLReaderFactory
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.common.xml` is unnecessary and can be removed
in `core/common/xml/src/main/java/org/eclipse/rdf4j/common/xml/SimpleSAXParser.java`
#### Snippet
```java
	 * @see #setListener
	 * @see org.xml.sax.XMLReader
	 * @see org.eclipse.rdf4j.common.xml.XMLReaderFactory
	 */
	public SimpleSAXParser() throws SAXException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/model-api/src/main/java/org/eclipse/rdf4j/model/Model.java`
#### Snippet
```java

/**
 * An RDF Model, represented as a {@link java.util.Set} of {@link Statement}s with predictable iteration order.
 * <p>
 * Additional utility functionality for working with {@code Model} objects is available in the
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/sail/memory/src/main/java/org/eclipse/rdf4j/sail/memory/model/CalendarMemLiteral.java`
#### Snippet
```java
	}

	private void readObject(java.io.ObjectInputStream in) throws IOException {
		try {
			in.defaultReadObject();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.locationtech.spatial4j.context` is unnecessary and can be removed
in `core/queryalgebra/geosparql/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/geosparql/SpatialSupport.java`
#### Snippet
```java

/**
 * This class is responsible for creating the {@link org.locationtech.spatial4j.context.SpatialContext},
 * {@link SpatialAlgebra} and {@link WktWriter} that will be used. It will first try to load a subclass of itself called
 * "org.eclipse.rdf4j.query.algebra.evaluation.function.geosparql.SpatialSupportInitializer" . This is not provided, and
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/lang/service/ServiceRegistry.java`
#### Snippet
```java

	protected ServiceRegistry(Class<S> serviceClass) {
		ServiceLoader<S> loader = java.util.ServiceLoader.load(serviceClass, serviceClass.getClassLoader());

		Iterator<S> services = loader.iterator();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/io/IOUtil.java`
#### Snippet
```java
	 * @return The total number of characters written.
	 * @throws IOException If an I/O error occurred while trying to write the data to the file.
	 * @see java.io.FileWriter
	 */
	public static final long transfer(Reader reader, File file) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/common/io/src/main/java/org/eclipse/rdf4j/common/net/ParsedURI.java`
#### Snippet
```java
 */
@Deprecated
public class ParsedURI implements java.lang.Cloneable {

	/*
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.impl` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleIRI.java`
#### Snippet
```java
	 * @param iriString A String representing a valid, absolute IRI. May not be <code>null</code>.
	 * @throws IllegalArgumentException If the supplied IRI is not a valid (absolute) IRI.
	 * @see org.eclipse.rdf4j.model.impl.SimpleValueFactory#createIRI(String)
	 */
	protected SimpleIRI(String iriString) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Literal createLiteral(double value) {
		return createFPLiteral(value, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype.DOUBLE);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Literal createLiteral(long value) {
		return createIntegerLiteral(value, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype.LONG);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Literal createLiteral(byte value) {
		return createIntegerLiteral(value, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype.BYTE);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Literal createLiteral(BigDecimal bigDecimal) {
		return createNumericLiteral(bigDecimal, org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Literal createLiteral(int value) {
		return createIntegerLiteral(value, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype.INT);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Literal createLiteral(float value) {
		return createFPLiteral(value, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype.FLOAT);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Literal createLiteral(BigInteger bigInteger) {
		return createIntegerLiteral(bigInteger, org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleValueFactory.java`
#### Snippet
```java
	@Override
	public Literal createLiteral(short value) {
		return createIntegerLiteral(value, org.eclipse.rdf4j.model.vocabulary.XSD.Datatype.SHORT);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	protected SimpleLiteral(String label) {
		setLabel(label);
		setDatatype(org.eclipse.rdf4j.model.vocabulary.XSD.STRING);
		optionalLanguageCache = Optional.empty();
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	 * label.
	 *
	 * @see org.eclipse.rdf4j.rio.ntriples.NTriplesUtil#toNTriplesString(org.eclipse.rdf4j.model.Literal)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
			sb.append('@').append(language);
			return sb.toString();
		} else if (org.eclipse.rdf4j.model.vocabulary.XSD.STRING.equals(datatype) || datatype == null) {
			StringBuilder sb = new StringBuilder(label.length() + 2);
			sb.append('"').append(label).append('"');
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary, and can be replaced with an import
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	protected SimpleLiteral(String label, XSD.Datatype datatype) {
		setLabel(label);
		if (org.eclipse.rdf4j.model.vocabulary.RDF.LANGSTRING.equals(datatype.getIri())) {
			throw new IllegalArgumentException("datatype rdf:langString requires a language tag");
		} else if (datatype == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary, and can be replaced with an import
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
	protected SimpleLiteral(String label, IRI datatype) {
		setLabel(label);
		if (org.eclipse.rdf4j.model.vocabulary.RDF.LANGSTRING.equals(datatype)) {
			throw new IllegalArgumentException("datatype rdf:langString requires a language tag");
		} else if (datatype == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleLiteral.java`
#### Snippet
```java
		CoreDatatype coreDatatype = getCoreDatatype();

		return org.eclipse.rdf4j.model.vocabulary.XSD.Datatype.from(coreDatatype.asXSDDatatype().orElse(null));
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.impl` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/GenericStatement.java`
#### Snippet
```java
	 * Note that creating an objects directly via this constructor is not the recommended approach. Instead, use a
	 * {@link org.eclipse.rdf4j.model.ValueFactory ValueFactory} (obtained from your repository or by using
	 * {@link org.eclipse.rdf4j.model.impl.SimpleValueFactory#getInstance()}) to create new Statement objects.
	 *
	 * @param subject   The statement's subject, must not be <var>null</var>.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
				 * java.net.URI javadoc ( https://docs.oracle.com/javase/8/docs/api/java/net/URI.html ) for details."
				 */
				final java.net.URI uri = new java.net.URI(escaped);
				valid = uri.isAbsolute();
			} catch (URISyntaxException e) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/URIUtil.java`
#### Snippet
```java
				 * java.net.URI javadoc ( https://docs.oracle.com/javase/8/docs/api/java/net/URI.html ) for details."
				 */
				final java.net.URI uri = new java.net.URI(escaped);
				valid = uri.isAbsolute();
			} catch (URISyntaxException e) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Namespaces.java`
#### Snippet
```java
			 */
			@Override
			public Set<java.util.Map.Entry<String, String>> entrySet() {
				Set<java.util.Map.Entry<String, String>> result = new LinkedHashSet<>();
				for (Namespace nextNamespace : namespaces) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Namespaces.java`
#### Snippet
```java
			@Override
			public Set<java.util.Map.Entry<String, String>> entrySet() {
				Set<java.util.Map.Entry<String, String>> result = new LinkedHashSet<>();
				for (Namespace nextNamespace : namespaces) {
					AbstractMap.SimpleImmutableEntry<String, String> nextEntry = new SimpleImmutableEntry<>(
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java

	/**
	 * Retrieves the {@link org.eclipse.rdf4j.model.vocabulary.XSD.Datatype} value for the supplied Literal, if it has
	 * one.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Literals.java`
#### Snippet
```java
	 *
	 * @param l a Literal
	 * @return an Optional {@link org.eclipse.rdf4j.model.vocabulary.XSD.Datatype} enum, if one is available. Note that
	 *         the absence of this enum does <i>not</i> indicate that the literal has no datatype, merely that it has no
	 *         cached enum representation of that datatype.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.util` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/ModelException.java`
#### Snippet
```java

/**
 * An exception thrown by {@link org.eclipse.rdf4j.model.Model Model} and {@link org.eclipse.rdf4j.model.util.Models
 * Models} when specific conditions are not met.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.rio.hdt` is unnecessary and can be removed
in `core/rio/hdt/src/main/java/org/eclipse/rdf4j/rio/hdt/HDTTriplesSectionBitmap.java`
#### Snippet
```java
 * <p>
 * The array contains numeric references to the predicates and objects in the HDT dictionaries, starting with the
 * entries in the "shared" {@link org.eclipse.rdf4j.rio.hdt.HDTDictionary Dictionary} These positions are counted
 * starting with 1, not 0.
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private final static Pattern P_GYEARMONTH = Pattern.compile("-?\\d{4,}-\\d\\d(Z|([+\\-])\\d\\d:\\d\\d)?");

	private static final Set<IRI> primitiveDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> primitiveDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> primitiveDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private static final Set<IRI> primitiveDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
			org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI, org.eclipse.rdf4j.model.vocabulary.XSD.QNAME,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.STRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
			org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI, org.eclipse.rdf4j.model.vocabulary.XSD.QNAME,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
			org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI, org.eclipse.rdf4j.model.vocabulary.XSD.QNAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NOTATION);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN, org.eclipse.rdf4j.model.vocabulary.XSD.BASE64BINARY,
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
			org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI, org.eclipse.rdf4j.model.vocabulary.XSD.QNAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NOTATION);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
			org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI, org.eclipse.rdf4j.model.vocabulary.XSD.QNAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NOTATION);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.HEXBINARY, org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT,
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
			org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI, org.eclipse.rdf4j.model.vocabulary.XSD.QNAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NOTATION);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL, org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE,
			org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI, org.eclipse.rdf4j.model.vocabulary.XSD.QNAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NOTATION);

	private static final Set<IRI> derivedDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.NORMALIZEDSTRING,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static boolean isFloatingPointDatatype(IRI datatype) {
		return datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)
				|| datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	public static boolean isFloatingPointDatatype(IRI datatype) {
		return datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)
				|| datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static String normalize(String value, IRI datatype) {
		if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL)) {
			return normalizeDecimal(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL)) {
			return normalizeDecimal(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
			return normalizeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
			return normalizeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
			return normalizeNegativeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
			return normalizeNegativeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
			return normalizeNonPositiveInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
			return normalizeNonPositiveInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
			return normalizeNonNegativeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
			return normalizeNonNegativeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
			return normalizePositiveInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
			return normalizePositiveInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
			return normalizeLong(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
			return normalizeLong(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
			return normalizeInt(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
			return normalizeInt(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
			return normalizeShort(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
			return normalizeShort(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
			return normalizeByte(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
			return normalizeByte(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
			return normalizeUnsignedLong(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
			return normalizeUnsignedLong(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
			return normalizeUnsignedInt(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
			return normalizeUnsignedInt(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
			return normalizeUnsignedShort(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
			return normalizeUnsignedShort(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
			return normalizeUnsignedByte(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
			return normalizeUnsignedByte(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
			return normalizeFloat(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
			return normalizeFloat(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
			return normalizeDouble(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
			return normalizeDouble(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN)) {
			return normalizeBoolean(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN)) {
			return normalizeBoolean(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)) {
			return normalizeDateTime(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)) {
			return normalizeDateTime(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI)) {
			return collapseWhiteSpace(value);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	public static int compare(String value1, String value2, IRI datatype) {
		if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL)) {
			return compareDecimals(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL)) {
			return compareDecimals(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
			return compareIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
			return compareIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
			return compareNegativeIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
			return compareNegativeIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
			return compareNonPositiveIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
			return compareNonPositiveIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
			return compareNonNegativeIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
			return compareNonNegativeIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
			return comparePositiveIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
			return comparePositiveIntegers(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
			return compareLongs(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
			return compareLongs(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
			return compareInts(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
			return compareInts(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
			return compareShorts(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
			return compareShorts(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
			return compareBytes(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
			return compareBytes(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
			return compareUnsignedLongs(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
			return compareUnsignedLongs(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
			return compareUnsignedInts(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
			return compareUnsignedInts(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
			return compareUnsignedShorts(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
			return compareUnsignedShorts(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
			return compareUnsignedBytes(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
			return compareUnsignedBytes(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
			return compareFloats(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
			return compareFloats(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
			return compareDoubles(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
			return compareDoubles(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)
				|| datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP)) {
			return compareDateTime(value1, value2);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			return compareDoubles(value1, value2);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)
				|| datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP)) {
			return compareDateTime(value1, value2);
		} else {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);

	private static final Set<IRI> integerDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> integerDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> integerDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private static final Set<IRI> integerDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private static final Set<IRI> integerDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE);

	private static final Set<IRI> calendarDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE);

	private static final Set<IRI> calendarDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> calendarDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> calendarDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private static final Set<IRI> calendarDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DATE, org.eclipse.rdf4j.model.vocabulary.XSD.TIME,
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY, org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR,
			org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH, org.eclipse.rdf4j.model.vocabulary.XSD.GDAY,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);

	private static final Set<IRI> durationDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static boolean isValidValue(String value, IRI datatype) {
		if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL)) {
			return isValidDecimal(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL)) {
			return isValidDecimal(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
			return isValidInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER)) {
			return isValidInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
			return isValidNegativeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER)) {
			return isValidNegativeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
			return isValidNonPositiveInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER)) {
			return isValidNonPositiveInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
			return isValidNonNegativeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER)) {
			return isValidNonNegativeInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
			return isValidPositiveInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER)) {
			return isValidPositiveInteger(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
			return isValidLong(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LONG)) {
			return isValidLong(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
			return isValidInt(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.INT)) {
			return isValidInt(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
			return isValidShort(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.SHORT)) {
			return isValidShort(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
			return isValidByte(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BYTE)) {
			return isValidByte(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
			return isValidUnsignedLong(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG)) {
			return isValidUnsignedLong(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
			return isValidUnsignedInt(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT)) {
			return isValidUnsignedInt(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
			return isValidUnsignedShort(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT)) {
			return isValidUnsignedShort(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
			return isValidUnsignedByte(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE)) {
			return isValidUnsignedByte(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
			return isValidFloat(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.FLOAT)) {
			return isValidFloat(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
			return isValidDouble(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DOUBLE)) {
			return isValidDouble(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN)) {
			return isValidBoolean(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.BOOLEAN)) {
			return isValidBoolean(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)) {
			return isValidDateTime(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIME)) {
			return isValidDateTime(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP)) {
			return isValidDateTimeStamp(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP)) {
			return isValidDateTimeStamp(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATE)) {
			return isValidDate(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.TIME)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DATE)) {
			return isValidDate(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.TIME)) {
			return isValidTime(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GDAY)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.TIME)) {
			return isValidTime(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GDAY)) {
			return isValidGDay(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GDAY)) {
			return isValidGDay(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH)) {
			return isValidGMonth(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GMONTH)) {
			return isValidGMonth(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY)) {
			return isValidGMonthDay(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GMONTHDAY)) {
			return isValidGMonthDay(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR)) {
			return isValidGYear(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GYEAR)) {
			return isValidGYear(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH)) {
			return isValidGYearMonth(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.GYEARMONTH)) {
			return isValidGYearMonth(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION)) {
			return isValidDuration(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION)) {
			return isValidDuration(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION)) {
			return isValidDayTimeDuration(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION)) {
			return isValidDayTimeDuration(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION)) {
			return isValidYearMonthDuration(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.QNAME)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION)) {
			return isValidYearMonthDuration(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.QNAME)) {
			return isValidQName(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.QNAME)) {
			return isValidQName(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI)) {
			return isValidAnyURI(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LANGUAGE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.ANYURI)) {
			return isValidAnyURI(value);
		} else if (datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.LANGUAGE)) {
			return Literals.isValidLanguageTag(value);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	 */
	public static boolean isDecimalDatatype(IRI datatype) {
		return datatype.equals(org.eclipse.rdf4j.model.vocabulary.XSD.DECIMAL) || isIntegerDatatype(datatype);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);

	private static final Set<IRI> durationDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> durationDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private static final Set<IRI> durationDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.DURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION);

	/**
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NOTATION);

	private static final Set<IRI> derivedDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.NORMALIZEDSTRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.TOKEN, org.eclipse.rdf4j.model.vocabulary.XSD.LANGUAGE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKEN,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> derivedDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.NORMALIZEDSTRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.TOKEN, org.eclipse.rdf4j.model.vocabulary.XSD.LANGUAGE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKEN,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java

	private static final Set<IRI> derivedDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.NORMALIZEDSTRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.TOKEN, org.eclipse.rdf4j.model.vocabulary.XSD.LANGUAGE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKEN,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
	private static final Set<IRI> derivedDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.NORMALIZEDSTRING,
			org.eclipse.rdf4j.model.vocabulary.XSD.TOKEN, org.eclipse.rdf4j.model.vocabulary.XSD.LANGUAGE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKEN,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.TOKEN, org.eclipse.rdf4j.model.vocabulary.XSD.LANGUAGE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKEN,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.TOKEN, org.eclipse.rdf4j.model.vocabulary.XSD.LANGUAGE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKEN,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKEN,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKEN,
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NMTOKENS, org.eclipse.rdf4j.model.vocabulary.XSD.NAME,
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NCNAME, org.eclipse.rdf4j.model.vocabulary.XSD.ID,
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.IDREF, org.eclipse.rdf4j.model.vocabulary.XSD.IDREFS,
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.ENTITY, org.eclipse.rdf4j.model.vocabulary.XSD.ENTITIES,
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.LONG, org.eclipse.rdf4j.model.vocabulary.XSD.INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.SHORT, org.eclipse.rdf4j.model.vocabulary.XSD.BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.NON_NEGATIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.POSITIVE_INTEGER,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_LONG, org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_INT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_SHORT,
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.UNSIGNED_BYTE,
			org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/datatypes/XMLDatatypeUtil.java`
#### Snippet
```java
			org.eclipse.rdf4j.model.vocabulary.XSD.DAYTIMEDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.YEARMONTHDURATION,
			org.eclipse.rdf4j.model.vocabulary.XSD.DATETIMESTAMP);

	private static final Set<IRI> integerDatatypes = Set.of(org.eclipse.rdf4j.model.vocabulary.XSD.INTEGER,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query` is unnecessary and can be removed
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/csv/SPARQLResultsCSVMappingStrategy.java`
#### Snippet
```java
/**
 * Implements a {@link com.opencsv.bean.MappingStrategy} to allow opencsv to work in parallel. This is where the input
 * is converted into {@link org.eclipse.rdf4j.query.BindingSet}s.
 *
 * @author Andrew Rucker Jones
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query` is unnecessary and can be removed
in `core/queryresultio/text/src/main/java/org/eclipse/rdf4j/query/resultio/text/tsv/SPARQLResultsTSVMappingStrategy.java`
#### Snippet
```java
/**
 * Implements a {@link com.opencsv.bean.MappingStrategy} to allow opencsv to work in parallel. This is where the input
 * is converted into {@link org.eclipse.rdf4j.query.BindingSet}s.
 *
 * @author Andrew Rucker Jones
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sparqlbuilder.rdf` is unnecessary and can be removed
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/Query.java`
#### Snippet
```java
	/**
	 * A shortcut. Each call to this method returns a new
	 * {@link org.eclipse.rdf4j.sparqlbuilder.rdf.RdfBlankNode.LabeledBlankNode RdfBlankNode.LabeledBlankNode} that is
	 * unique (i.e., has a unique alias) to this query instance.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sparqlbuilder.rdf` is unnecessary and can be removed
in `core/sparqlbuilder/src/main/java/org/eclipse/rdf4j/sparqlbuilder/core/query/Query.java`
#### Snippet
```java
	 * unique (i.e., has a unique alias) to this query instance.
	 *
	 * @return a {@link org.eclipse.rdf4j.sparqlbuilder.rdf.RdfBlankNode.LabeledBlankNode RdfBlankNode.LabeledBlankNode}
	 *         object that is unique to this query instance
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.impl` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleStatement.java`
#### Snippet
```java
 * context. For statements that do have an associated context, {@link ContextStatement} can be used.
 *
 * @see org.eclipse.rdf4j.model.impl.SimpleValueFactory
 * @deprecated Use {@link GenericStatement instead}
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.impl` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleStatement.java`
#### Snippet
```java
	 * Note that creating SimpleStatement objects directly via this constructor is not the recommended approach.
	 * Instead, use a {@link org.eclipse.rdf4j.model.ValueFactory ValueFactory} (obtained from your repository or by
	 * using {@link org.eclipse.rdf4j.model.impl.SimpleValueFactory#getInstance()}) to create new Statement objects.
	 *
	 * @param subject   The statement's subject, must not be <var>null</var>.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.impl` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/impl/SimpleStatement.java`
#### Snippet
```java
	 * @param predicate The statement's predicate, must not be <var>null</var>.
	 * @param object    The statement's object, must not be <var>null</var>.
	 * @see org.eclipse.rdf4j.model.impl.SimpleValueFactory#createStatement(Resource, IRI, Value)
	 */
	protected SimpleStatement(Resource subject, IRI predicate, Value object) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
 * @author Jeen Broekstra
 * @author Arjohn Kampman
 * @see org.eclipse.rdf4j.model.Model
 * @see org.eclipse.rdf4j.model.util.ModelBuilder
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.util` is unnecessary and can be removed
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/Models.java`
#### Snippet
```java
 * @author Arjohn Kampman
 * @see org.eclipse.rdf4j.model.Model
 * @see org.eclipse.rdf4j.model.util.ModelBuilder
 */
public class Models {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/Iterators.java`
#### Snippet
```java

	/**
	 * Closes the given iterator, swallowing any IOExceptions, if it implements {@link java.io.Closeable} else do
	 * nothing.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iterator/Iterators.java`
#### Snippet
```java

	/**
	 * Closes the given iterator if it implements {@link java.io.Closeable} else do nothing.
	 *
	 * @param iter The iterator to close.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
		if (username != null && password != null) {
			logger.debug("Setting username '{}' and password for server at {}.", username, url);
			java.net.URI requestURI = java.net.URI.create(url);
			String host = requestURI.getHost();
			int port = requestURI.getPort();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
		if (username != null && password != null) {
			logger.debug("Setting username '{}' and password for server at {}.", username, url);
			java.net.URI requestURI = java.net.URI.create(url);
			String host = requestURI.getHost();
			int port = requestURI.getPort();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/IterationWrapper.java`
#### Snippet
```java
	 * Returns the next element from the wrapped Iteration.
	 *
	 * @throws java.util.NoSuchElementException If all elements have been returned or it has been closed.
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ExceptionConvertingIteration.java`
#### Snippet
```java
	 *
	 * @throws X
	 * @throws java.util.NoSuchElementException If all elements have been returned.
	 * @throws IllegalStateException            If the Iteration has been closed.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/common/iterator/src/main/java/org/eclipse/rdf4j/common/iteration/ConvertingIteration.java`
#### Snippet
```java
	 *
	 * @throws X
	 * @throws java.util.NoSuchElementException If all elements have been returned.
	 * @throws IllegalStateException            If the iteration has been closed.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java
	 * @param url        The URL of the RDF data.
	 * @param baseURI    The base URI to resolve any relative URIs that are in the data against. This defaults to the
	 *                   value of {@link java.net.URL#toExternalForm() url.toExternalForm()} if the value is set to
	 *                   <var>null</var>.
	 * @param dataFormat The serialization format of the data. If set to <var>null</var>, the format will be
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java

		boolean followRedirects = HttpURLConnection.getFollowRedirects();
		int maxRedirects = java.security.AccessController.doPrivileged(
				(PrivilegedAction<Integer>) () -> Integer.valueOf(System.getProperty("http.maxRedirects", "20")));

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/util/RDFLoader.java`
#### Snippet
```java
	 * @param file       A file containing RDF data.
	 * @param baseURI    The base URI to resolve any relative URIs that are in the data against. This defaults to the
	 *                   value of {@link java.io.File#toURI() file.toURI()} if the value is set to <var>null</var>.
	 * @param dataFormat The serialization format of the data.
	 * @param rdfHandler Receives RDF parser events.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java
	 * @param url        The URL of the RDF data.
	 * @param baseURI    The base URI to resolve any relative URIs that are in the data against. This defaults to the
	 *                   value of {@link java.net.URL#toExternalForm() url.toExternalForm()} if the value is set to
	 *                   <var>null</var>.
	 *                   <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.common.iteration` is unnecessary and can be removed
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java
	 *
	 * @param statements The statements to remove. In case the iteration is a
	 *                   {@link org.eclipse.rdf4j.common.iteration.CloseableIteration}, it will be closed before this
	 *                   method returns.
	 * @param contexts   The context(s) to remove the data from. Note that this parameter is a vararg and as such is
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java
	 * @param file       A file containing RDF data.
	 * @param baseURI    The base URI to resolve any relative URIs that are in the data against. This defaults to the
	 *                   value of {@link java.io.File#toURI() file.toURI()} if the value is set to <var>null</var>.
	 *                   <p>
	 *                   Note that if the data contains an embedded base URI, that embedded base URI will overrule the
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.common.iteration` is unnecessary and can be removed
in `core/repository/api/src/main/java/org/eclipse/rdf4j/repository/RepositoryConnection.java`
#### Snippet
```java
	 *
	 * @param statements The statements to add. In case the iteration is a
	 *                   {@link org.eclipse.rdf4j.common.iteration.CloseableIteration}, it will be closed before this
	 *                   method returns.
	 * @param contexts   The contexts to add the statements to. Note that this parameter is a vararg and as such is
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.repository.manager` is unnecessary and can be removed
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/util/TypeFilteringRepositoryManager.java`
#### Snippet
```java
	/**
	 * @param httpClient
	 * @see org.eclipse.rdf4j.repository.manager.RepositoryManager#setHttpClient(org.apache.http.client.HttpClient)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.http.client` is unnecessary and can be removed
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/util/TypeFilteringRepositoryManager.java`
#### Snippet
```java
	/**
	 * @param httpClient
	 * @see org.eclipse.rdf4j.repository.manager.RepositoryManager#setHttpClient(org.apache.http.client.HttpClient)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.repository.manager` is unnecessary and can be removed
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/util/TypeFilteringRepositoryManager.java`
#### Snippet
```java

	/**
	 * @see org.eclipse.rdf4j.repository.manager.RepositoryManager#getHttpClient()
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/rio/datatypes/src/main/java/org/eclipse/rdf4j/rio/datatypes/RDFDatatypeHandler.java`
#### Snippet
```java
		}

		return org.eclipse.rdf4j.model.vocabulary.RDF.LANGSTRING.equals(datatypeUri)
				|| org.eclipse.rdf4j.model.vocabulary.RDF.XMLLITERAL.equals(datatypeUri)
				|| org.eclipse.rdf4j.model.vocabulary.RDF.HTML.equals(datatypeUri);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/rio/datatypes/src/main/java/org/eclipse/rdf4j/rio/datatypes/RDFDatatypeHandler.java`
#### Snippet
```java

		return org.eclipse.rdf4j.model.vocabulary.RDF.LANGSTRING.equals(datatypeUri)
				|| org.eclipse.rdf4j.model.vocabulary.RDF.XMLLITERAL.equals(datatypeUri)
				|| org.eclipse.rdf4j.model.vocabulary.RDF.HTML.equals(datatypeUri);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/rio/datatypes/src/main/java/org/eclipse/rdf4j/rio/datatypes/RDFDatatypeHandler.java`
#### Snippet
```java
		return org.eclipse.rdf4j.model.vocabulary.RDF.LANGSTRING.equals(datatypeUri)
				|| org.eclipse.rdf4j.model.vocabulary.RDF.XMLLITERAL.equals(datatypeUri)
				|| org.eclipse.rdf4j.model.vocabulary.RDF.HTML.equals(datatypeUri);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.repository.config` is unnecessary and can be removed
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
	/**
	 * Creates the stack of Repository objects for the repository represented by the specified
	 * {@link org.eclipse.rdf4j.repository.config.RepositoryImplConfig}. Uses a
	 * {@link org.eclipse.rdf4j.repository.config.RepositoryFactory} to create the repository and initialize it.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.repository.config` is unnecessary and can be removed
in `core/repository/manager/src/main/java/org/eclipse/rdf4j/repository/manager/LocalRepositoryManager.java`
#### Snippet
```java
	 * Creates the stack of Repository objects for the repository represented by the specified
	 * {@link org.eclipse.rdf4j.repository.config.RepositoryImplConfig}. Uses a
	 * {@link org.eclipse.rdf4j.repository.config.RepositoryFactory} to create the repository and initialize it.
	 *
	 * @param config The node representing the to-be-created repository in the configuration.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.junit` is unnecessary, and can be replaced with an import
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
			Value obj = stmts.next().getObject();
			if (stmts.hasNext()) {
				org.junit.Assert.fail("multiple literals: " + obj + " and " + stmts.next());
			}
			return (Literal) obj;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail` is unnecessary and can be removed
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/NotifyingSailConnectionWrapper.java`
#### Snippet
```java

/**
 * An implementation of the {@link org.eclipse.rdf4j.sail.NotifyingSailConnection} interface that wraps another
 * {@link org.eclipse.rdf4j.sail.NotifyingSailConnection} object and forwards any method calls to the wrapped
 * transaction.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail` is unnecessary and can be removed
in `core/sail/api/src/main/java/org/eclipse/rdf4j/sail/helpers/NotifyingSailConnectionWrapper.java`
#### Snippet
```java
/**
 * An implementation of the {@link org.eclipse.rdf4j.sail.NotifyingSailConnection} interface that wraps another
 * {@link org.eclipse.rdf4j.sail.NotifyingSailConnection} object and forwards any method calls to the wrapped
 * transaction.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/EvaluationStrategyTest.java`
#### Snippet
```java

	/**
	 * @throws java.lang.Exception
	 */
	@BeforeEach
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
 *
 * @author Arjohn Kampman
 * @see org.eclipse.rdf4j.model.ValueFactory
 * @see org.eclipse.rdf4j.rio.RDFHandler
 * @see org.eclipse.rdf4j.rio.ParseErrorListener
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java
	}

	public static void main(String[] args) throws java.io.IOException {
		System.out.println("Your SPARQL query:");

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/Token.java`
#### Snippet
```java
 */

public class Token implements java.io.Serializable {

	/**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java
						return '\\';
					}
				} catch (java.io.IOException e) {
					// We are returning one backslash so we should only backup (count-1)
					if (backSlashCnt > 1) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/UnicodeEscapeStream.java`
#### Snippet
```java
					column += hex.length();
				}
			} catch (java.io.IOException | IllegalArgumentException e) {
				throw new Error("Invalid escape character at line " + line + " column " + column + ".", e);
			}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/CharStream.java`
#### Snippet
```java
	 * between two successive calls to this method to implement backup correctly.
	 */
	char BeginToken() throws java.io.IOException;

	/**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/CharStream.java`
#### Snippet
```java
	 * the class implementing this interface. Can throw any java.io.IOException.
	 */
	char readChar() throws java.io.IOException;

	/**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JJTSyntaxTreeBuilderState.java`
#### Snippet
```java

public class JJTSyntaxTreeBuilderState {
	private java.util.List<Node> nodes;
	private java.util.List<Integer> marks;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JJTSyntaxTreeBuilderState.java`
#### Snippet
```java
public class JJTSyntaxTreeBuilderState {
	private java.util.List<Node> nodes;
	private java.util.List<Integer> marks;

	private int sp; // number of nodes on stack
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JJTSyntaxTreeBuilderState.java`
#### Snippet
```java

	public JJTSyntaxTreeBuilderState() {
		nodes = new java.util.ArrayList<Node>();
		marks = new java.util.ArrayList<Integer>();
		sp = 0;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JJTSyntaxTreeBuilderState.java`
#### Snippet
```java
	public JJTSyntaxTreeBuilderState() {
		nodes = new java.util.ArrayList<Node>();
		marks = new java.util.ArrayList<Integer>();
		sp = 0;
		mk = 0;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			throw new Error("Internal Error");
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
			try {
				curChar = input_stream.readChar();
			} catch (java.io.IOException e) {
				break;
			}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				try {
					curChar = input_stream.readChar();
				} catch (java.io.IOException e) {
					throw new Error("Internal Error : Please send a bug report.");
				}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(9);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(2);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(1);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
	 * Set debug output.
	 */
	public void setDebugStream(java.io.PrintStream ds) {
		debugStream = ds;
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(8);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
	 * Debug output.
	 */
	public java.io.PrintStream debugStream = System.out;

	/**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(3);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(5);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(4);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
				input_stream.readChar();
				input_stream.backup(1);
			} catch (java.io.IOException e1) {
				EOFSeen = true;
				error_after = curPos <= 1 ? "" : input_stream.GetImage();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(0);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(6);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(11);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(12);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(10);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilderTokenManager.java`
#### Snippet
```java
		try {
			curChar = input_stream.readChar();
		} catch (java.io.IOException e) {
			return jjMoveNfa_0(7);
		}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	};

	static int hexval(char c) throws java.io.IOException {
		switch (c) {
		case '0':
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
		}

		throw new java.io.IOException(); // Should never come here
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.Reader dstream) {
		this(dstream, 1, 1, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.Reader dstream, int startline, int startcolumn) {
		this(dstream, startline, startcolumn, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, String encoding, int startline, int startcolumn, int buffersize)
			throws java.io.UnsupportedEncodingException {
		this(encoding == null ? new java.io.InputStreamReader(dstream)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, String encoding, int startline, int startcolumn, int buffersize)
			throws java.io.UnsupportedEncodingException {
		this(encoding == null ? new java.io.InputStreamReader(dstream)
				: new java.io.InputStreamReader(dstream, encoding), startline, startcolumn, buffersize);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	public JavaCharStream(java.io.InputStream dstream, String encoding, int startline, int startcolumn, int buffersize)
			throws java.io.UnsupportedEncodingException {
		this(encoding == null ? new java.io.InputStreamReader(dstream)
				: new java.io.InputStreamReader(dstream, encoding), startline, startcolumn, buffersize);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
			throws java.io.UnsupportedEncodingException {
		this(encoding == null ? new java.io.InputStreamReader(dstream)
				: new java.io.InputStreamReader(dstream, encoding), startline, startcolumn, buffersize);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	/** @return starting character for token. */
	@Override
	public char BeginToken() throws java.io.IOException {
		if (inBuf > 0) {
			--inBuf;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, String encoding, int startline, int startcolumn)
			throws java.io.UnsupportedEncodingException {
		this(dstream, encoding, startline, startcolumn, 4096);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, String encoding, int startline, int startcolumn)
			throws java.io.UnsupportedEncodingException {
		this(dstream, encoding, startline, startcolumn, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	/** Read a character. */
	@Override
	public char readChar() throws java.io.IOException {
		if (inBuf > 0) {
			--inBuf;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
						return '\\';
					}
				} catch (java.io.IOException e) {
					// We are returning one backslash so we should only backup (count-1)
					if (backSlashCnt > 1) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

				column += 4;
			} catch (java.io.IOException e) {
				throw new Error("Invalid escape character at line " + line + " column " + column + ".");
			}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, String encoding) throws java.io.UnsupportedEncodingException {
		ReInit(dstream, encoding, 1, 1, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, String encoding) throws java.io.UnsupportedEncodingException {
		ReInit(dstream, encoding, 1, 1, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	}

	protected char ReadByte() throws java.io.IOException {
		if (++nextCharInd >= maxNextCharInd) {
			FillBuff();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, String encoding, int startline, int startcolumn, int buffersize)
			throws java.io.UnsupportedEncodingException {
		ReInit(encoding == null ? new java.io.InputStreamReader(dstream)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, String encoding, int startline, int startcolumn, int buffersize)
			throws java.io.UnsupportedEncodingException {
		ReInit(encoding == null ? new java.io.InputStreamReader(dstream)
				: new java.io.InputStreamReader(dstream, encoding), startline, startcolumn, buffersize);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	public void ReInit(java.io.InputStream dstream, String encoding, int startline, int startcolumn, int buffersize)
			throws java.io.UnsupportedEncodingException {
		ReInit(encoding == null ? new java.io.InputStreamReader(dstream)
				: new java.io.InputStreamReader(dstream, encoding), startline, startcolumn, buffersize);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
			throws java.io.UnsupportedEncodingException {
		ReInit(encoding == null ? new java.io.InputStreamReader(dstream)
				: new java.io.InputStreamReader(dstream, encoding), startline, startcolumn, buffersize);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, String encoding) throws java.io.UnsupportedEncodingException {
		this(dstream, encoding, 1, 1, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, String encoding) throws java.io.UnsupportedEncodingException {
		this(dstream, encoding, 1, 1, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.Reader dstream, int startline, int startcolumn, int buffersize) {
		inputStream = dstream;
		line = startline;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	protected boolean prevCharIsLF;

	protected java.io.Reader inputStream;

	protected char[] nextCharBuf;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, int startline, int startcolumn) {
		this(dstream, startline, startcolumn, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.Reader dstream) {
		ReInit(dstream, 1, 1, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, int startline, int startcolumn) {
		ReInit(dstream, startline, startcolumn, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream) {
		ReInit(dstream, 1, 1, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	}

	protected void FillBuff() throws java.io.IOException {
		int i;
		if (maxNextCharInd == nextCharBuf.length) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
				maxNextCharInd += i;
			}
		} catch (java.io.IOException e) {
			if (bufpos != 0) {
				--bufpos;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.Reader dstream, int startline, int startcolumn, int buffersize) {
		inputStream = dstream;
		line = startline;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, String encoding, int startline, int startcolumn)
			throws java.io.UnsupportedEncodingException {
		ReInit(dstream, encoding, startline, startcolumn, 4096);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, String encoding, int startline, int startcolumn)
			throws java.io.UnsupportedEncodingException {
		ReInit(dstream, encoding, startline, startcolumn, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream) {
		this(dstream, 1, 1, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.Reader dstream, int startline, int startcolumn) {
		ReInit(dstream, startline, startcolumn, 4096);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, int startline, int startcolumn, int buffersize) {
		ReInit(new java.io.InputStreamReader(dstream), startline, startcolumn, buffersize);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	/** Reinitialise. */
	public void ReInit(java.io.InputStream dstream, int startline, int startcolumn, int buffersize) {
		ReInit(new java.io.InputStreamReader(dstream), startline, startcolumn, buffersize);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java

	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, int startline, int startcolumn, int buffersize) {
		this(new java.io.InputStreamReader(dstream), startline, startcolumn, buffersize);
	}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/JavaCharStream.java`
#### Snippet
```java
	/** Constructor. */
	public JavaCharStream(java.io.InputStream dstream, int startline, int startcolumn, int buffersize) {
		this(new java.io.InputStreamReader(dstream), startline, startcolumn, buffersize);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.repository` is unnecessary and can be removed
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/ProxyRepository.java`
#### Snippet
```java
/**
 * <p>
 * {@link org.eclipse.rdf4j.repository.Repository} implementation that takes a
 * {@link org.eclipse.rdf4j.repository.RepositoryResolver} instance and the id of a managed repository, and delegate all
 * calls through to the given repository.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.repository` is unnecessary and can be removed
in `core/repository/sail/src/main/java/org/eclipse/rdf4j/repository/sail/ProxyRepository.java`
#### Snippet
```java
 * <p>
 * {@link org.eclipse.rdf4j.repository.Repository} implementation that takes a
 * {@link org.eclipse.rdf4j.repository.RepositoryResolver} instance and the id of a managed repository, and delegate all
 * calls through to the given repository.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `com.github.jsonldjava.core` is unnecessary, and can be replaced with an import
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDInternalRDFParser.java`
#### Snippet
```java
 * @author Peter Ansell
 */
class JSONLDInternalRDFParser implements com.github.jsonldjava.core.RDFParser {

	public void setPrefix(RDFDataset result, String fullUri, String prefix) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDWriter.java`
#### Snippet
```java

	/**
	 * Create a SesameJSONLDWriter using a {@link java.io.Writer}
	 *
	 * @param writer The Writer to write to.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDWriter.java`
#### Snippet
```java

	/**
	 * Create a JSONLDWriter using a {@link java.io.OutputStream}
	 *
	 * @param outputStream The OutputStream to write to.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDWriter.java`
#### Snippet
```java

	/**
	 * Create a SesameJSONLDWriter using a {@link java.io.OutputStream}
	 *
	 * @param outputStream The OutputStream to write to.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/rio/jsonld/src/main/java/org/eclipse/rdf4j/rio/jsonld/JSONLDWriter.java`
#### Snippet
```java

	/**
	 * Create a SesameJSONLDWriter using a {@link java.io.Writer}
	 *
	 * @param writer  The Writer to write to.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	static private final class LookaheadSuccess extends java.lang.Error {
		@Override
		public Throwable fillInStackTrace() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	private final java.util.List<int[]> jj_expentries = new java.util.ArrayList<>();
	private int[] jj_expentry;
	private int jj_kind = -1;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/SyntaxTreeBuilder.java`
#### Snippet
```java
	}

	private final java.util.List<int[]> jj_expentries = new java.util.ArrayList<>();
	private int[] jj_expentry;
	private int jj_kind = -1;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/ValueStore.java`
#### Snippet
```java
				try {
					if (id == getNamespaceID(namespace, false)
							&& java.net.URI.create(namespace + "part").isAbsolute()) {
						continue;
					}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
	/**
	 * Tests that the parser returned for a TupleQueryResultFormat is not able to parse a BooleanQueryResultFormat using
	 * the deprecated {@link TupleQueryResultParser#parse(java.io.InputStream)} method, and that it does indeed through
	 * an exception of type {@link QueryResultParseException}.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.resultio` is unnecessary and can be removed
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
	/**
	 * Test method for
	 * {@link org.eclipse.rdf4j.query.resultio.QueryResultIO#getParserFormatForFileName(java.lang.String)} .
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
	/**
	 * Test method for
	 * {@link org.eclipse.rdf4j.query.resultio.QueryResultIO#getParserFormatForFileName(java.lang.String)} .
	 */
	@Test
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `testsuites/queryresultio/src/main/java/org/eclipse/rdf4j/testsuite/query/resultio/AbstractQueryResultIOTest.java`
#### Snippet
```java
	/**
	 * Tests that the parser returned for a BooleanQueryResultFormat is not able to parse a TupleQueryResultFormat using
	 * the deprecated {@link BooleanQueryResultParser#parse(java.io.InputStream)} method, and that it does indeed
	 * through an exception of type {@link QueryResultParseException}.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/QueryContextInitializer.java`
#### Snippet
```java

/**
 * Callback for configuring/customising a {@link org.eclipse.rdf4j.query.algebra.evaluation.QueryContext}.
 */
public interface QueryContextInitializer {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.federation` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/EvaluationStrategy.java`
#### Snippet
```java
	 * @return the {@link FederatedService} registered for the serviceUrl.
	 * @throws QueryEvaluationException
	 * @see org.eclipse.rdf4j.query.algebra.evaluation.federation.FederatedServiceResolver#getService(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/EvaluationStrategy.java`
#### Snippet
```java
	 * @return the {@link FederatedService} registered for the serviceUrl.
	 * @throws QueryEvaluationException
	 * @see org.eclipse.rdf4j.query.algebra.evaluation.federation.FederatedServiceResolver#getService(java.lang.String)
	 */
	@Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.function` is unnecessary, and can be replaced with an import
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/evaluationsteps/JoinQueryEvaluationStep.java`
#### Snippet
```java
public class JoinQueryEvaluationStep implements QueryEvaluationStep {

	private final java.util.function.Function<BindingSet, CloseableIteration<BindingSet, QueryEvaluationException>> eval;

	public JoinQueryEvaluationStep(EvaluationStrategy strategy, Join join, QueryEvaluationContext context) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.function` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/FloatCast.java`
#### Snippet
```java

/**
 * A {@link org.eclipse.rdf4j.query.algebra.evaluation.function.Function} that tries to cast its argument to an
 * <var>xsd:float</var>.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.function` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/BooleanCast.java`
#### Snippet
```java

/**
 * A {@link org.eclipse.rdf4j.query.algebra.evaluation.function.Function} that tries to cast its argument to an
 * <var>xsd:boolean</var>.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.function` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/DecimalCast.java`
#### Snippet
```java

/**
 * A {@link org.eclipse.rdf4j.query.algebra.evaluation.function.Function} that tries to cast its argument to an
 * <var>xsd:decimal</var>.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.function` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/DateTimeCast.java`
#### Snippet
```java

/**
 * A {@link org.eclipse.rdf4j.query.algebra.evaluation.function.Function} that tries to cast its argument to an
 * <var>xsd:dateTime</var>.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.function` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/DoubleCast.java`
#### Snippet
```java

/**
 * A {@link org.eclipse.rdf4j.query.algebra.evaluation.function.Function} that tries to cast its argument to an
 * <var>xsd:double</var>.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.function` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/StringCast.java`
#### Snippet
```java

/**
 * A {@link org.eclipse.rdf4j.query.algebra.evaluation.function.Function} that tries to cast its argument to an
 * <var>xsd:string</var>.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.function` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/IntegerCast.java`
#### Snippet
```java

/**
 * A {@link org.eclipse.rdf4j.query.algebra.evaluation.function.Function} that tries to cast its argument to an
 * <var>xsd:integer</var> .
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra.evaluation.function` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/function/xsd/CastFunction.java`
#### Snippet
```java

/**
 * Abstract superclass for {@link org.eclipse.rdf4j.query.algebra.evaluation.function.Function}s that cast an argument
 * to an XML Schema datatype.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		// Naive implementation that walks over all statements matching (x rdf:type rdf:Statement)
		// and filter those that do not match the bindings for subject, predicate and object vars (if bound)
		final org.eclipse.rdf4j.query.algebra.Var subjVar = ref.getSubjectVar();
		final org.eclipse.rdf4j.query.algebra.Var predVar = ref.getPredicateVar();
		final org.eclipse.rdf4j.query.algebra.Var objVar = ref.getObjectVar();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		// and filter those that do not match the bindings for subject, predicate and object vars (if bound)
		final org.eclipse.rdf4j.query.algebra.Var subjVar = ref.getSubjectVar();
		final org.eclipse.rdf4j.query.algebra.Var predVar = ref.getPredicateVar();
		final org.eclipse.rdf4j.query.algebra.Var objVar = ref.getObjectVar();
		final org.eclipse.rdf4j.query.algebra.Var extVar = ref.getExprVar();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		final org.eclipse.rdf4j.query.algebra.Var subjVar = ref.getSubjectVar();
		final org.eclipse.rdf4j.query.algebra.Var predVar = ref.getPredicateVar();
		final org.eclipse.rdf4j.query.algebra.Var objVar = ref.getObjectVar();
		final org.eclipse.rdf4j.query.algebra.Var extVar = ref.getExprVar();
		// whether the TripleSouce support access to RDF star
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.query.algebra` is unnecessary and can be removed
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/StrictEvaluationStrategy.java`
#### Snippet
```java
		final org.eclipse.rdf4j.query.algebra.Var predVar = ref.getPredicateVar();
		final org.eclipse.rdf4j.query.algebra.Var objVar = ref.getObjectVar();
		final org.eclipse.rdf4j.query.algebra.Var extVar = ref.getExprVar();
		// whether the TripleSouce support access to RDF star
		final boolean sourceSupportsRdfStar = tripleSource instanceof RDFStarTripleSource;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.function` is unnecessary, and can be replaced with an import
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/FilterIterator.java`
#### Snippet
```java

		@Override
		public java.util.function.Function<BindingSet, Binding> getBinding(String variableName) {
			if (isVariableInScope(variableName)) {
				return context.getBinding(variableName);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.function` is unnecessary, and can be replaced with an import
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/iterator/FilterIterator.java`
#### Snippet
```java

		@Override
		public java.util.function.Function<BindingSet, Value> getValue(String variableName) {
			if (isVariableInScope(variableName)) {
				return context.getValue(variableName);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/SearchIndex.java`
#### Snippet
```java
	 * Begins a transaction.
	 *
	 * @throws java.io.IOException
	 */
	void begin() throws IOException;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/SearchIndex.java`
#### Snippet
```java
	 *
	 * @param statement
	 * @throws java.io.IOException
	 */
	void removeStatement(Statement statement) throws IOException;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/SearchIndex.java`
#### Snippet
```java
	 * Clears the indexes.
	 *
	 * @throws java.io.IOException
	 */
	void clear() throws IOException;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail.lucene` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/TypeBacktraceMode.java`
#### Snippet
```java

/**
 * Option to describe how the {@link org.eclipse.rdf4j.sail.lucene.LuceneSail} should handle the add of a new type
 * statement in a connection if the {@link LuceneSail#INDEXEDTYPES} property is defined.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java

	/**
	 * The method is relocated to {@link SearchIndexUtils#createSearchIndex(java.util.Properties) }.
	 *
	 * @param parameters
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java
 * <li>Delete the folder where the data is stored while the application is not running</li>
 * <li>Call the repository's
 * <code>{@link org.eclipse.rdf4j.repository.RepositoryConnection#clear(org.eclipse.rdf4j.model.Resource[])}</code>
 * method with no arguments. <code>clear()</code>. This will delete the index.</li>
 * </ul>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail.lucene` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java
 * <h2>Handling of Contexts</h2> Each lucene document contains a field for every contextIDs that contributed to the
 * document. <b>NULL</b> contexts are marked using the String
 * {@link org.eclipse.rdf4j.sail.lucene.SearchFields#CONTEXT_NULL} ("null") and stored in the lucene field
 * {@link org.eclipse.rdf4j.sail.lucene.SearchFields#CONTEXT_FIELD_NAME} ("context"). This means that when
 * adding/appending to a document, all additional context-uris are added to the document. When deleting individual
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail.lucene` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java
 * document. <b>NULL</b> contexts are marked using the String
 * {@link org.eclipse.rdf4j.sail.lucene.SearchFields#CONTEXT_NULL} ("null") and stored in the lucene field
 * {@link org.eclipse.rdf4j.sail.lucene.SearchFields#CONTEXT_FIELD_NAME} ("context"). This means that when
 * adding/appending to a document, all additional context-uris are added to the document. When deleting individual
 * triples, the context is ignored. In clear(Resource ...) we make a query on all Lucene-Documents that were possibly
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail.lucene` is unnecessary and can be removed
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/LuceneSail.java`
#### Snippet
```java
	public static final String INDEXEDLANG = "indexedlang";
	/**
	 * See {@link org.eclipse.rdf4j.sail.lucene.TypeBacktraceMode}
	 */
	public static final String INDEX_TYPE_BACKTRACE_MODE = "indexBacktraceMode";
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.rio` is unnecessary and can be removed
in `core/rio/nquads/src/main/java/org/eclipse/rdf4j/rio/nquads/NQuadsWriter.java`
#### Snippet
```java

/**
 * RDFWriter implementation for the {@link org.eclipse.rdf4j.rio.RDFFormat#NQUADS N-Quads} RDF format.
 *
 * @author Joshua Shinavier
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java

	/**
	 * Set the parameter "waitForActiveShards=" to configure if {@link #initialize(java.util.Properties) initialization}
	 * should wait until the specified number of shards to be active. Does not wait by default.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java

	/**
	 * Set the parameter "waitForStatus=" to configure if {@link #initialize(java.util.Properties) initialization}
	 * should wait for a particular health status. The value can be one of "green" or "yellow". Does not wait by
	 * default.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java

	/**
	 * Set the parameter "waitForNoRelocatingShards=true|false" to configure if {@link #initialize(java.util.Properties)
	 * initialization} should wait until the are no relocating shards. Defaults to false, meaning the operation does not
	 * wait on there being no more relocating shards. Set to true to wait until the number of relocating shards in the
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java

	/**
	 * Set the parameter "waitForNodes=" to configure if {@link #initialize(java.util.Properties) initialization} should
	 * wait until the specified number of nodes are available. Does not wait by default.
	 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java

	/**
	 * Set the parameter "waitForRelocatingShards=" to configure if {@link #initialize(java.util.Properties)
	 * initialization} should wait until the specified number of nodes are relocating. Does not wait by default.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.vocabulary` is unnecessary and can be removed
in `core/model-vocabulary/src/main/java/org/eclipse/rdf4j/model/vocabulary/XSD.java`
#### Snippet
```java

	private static IRI create(String localName) {
		return Vocabularies.createIRI(org.eclipse.rdf4j.model.vocabulary.XSD.NAMESPACE, localName);
	}

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/RenderUtils.java`
#### Snippet
```java

	/**
	 * Return the SPARQL query string rendering of the {@link org.eclipse.rdf4j.model.Value}
	 *
	 * @param theValue the value to render
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model` is unnecessary and can be removed
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/RenderUtils.java`
#### Snippet
```java

	/**
	 * Append the SPARQL query string rendering of the {@link org.eclipse.rdf4j.model.Value} to the supplied
	 * {@link StringBuilder}.
	 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.common.annotation` is unnecessary, and can be replaced with an import
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/package-info.java`
#### Snippet
```java
 *          from one release to the next.
 */
@org.eclipse.rdf4j.common.annotation.Experimental
package org.eclipse.rdf4j.queryrender.sparql.experimental;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail` is unnecessary and can be removed
in `core/sail/model/src/main/java/org/eclipse/rdf4j/sail/model/SailModel.java`
#### Snippet
```java

/**
 * Model implementation for a {@link org.eclipse.rdf4j.sail.SailConnection}. All
 * {@link org.eclipse.rdf4j.sail.SailException}s are wrapped in a {@link org.eclipse.rdf4j.model.util.ModelException}.
 * Not thread-safe.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.sail` is unnecessary and can be removed
in `core/sail/model/src/main/java/org/eclipse/rdf4j/sail/model/SailModel.java`
#### Snippet
```java
/**
 * Model implementation for a {@link org.eclipse.rdf4j.sail.SailConnection}. All
 * {@link org.eclipse.rdf4j.sail.SailException}s are wrapped in a {@link org.eclipse.rdf4j.model.util.ModelException}.
 * Not thread-safe.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.rdf4j.model.util` is unnecessary and can be removed
in `core/sail/model/src/main/java/org/eclipse/rdf4j/sail/model/SailModel.java`
#### Snippet
```java
/**
 * Model implementation for a {@link org.eclipse.rdf4j.sail.SailConnection}. All
 * {@link org.eclipse.rdf4j.sail.SailException}s are wrapped in a {@link org.eclipse.rdf4j.model.util.ModelException}.
 * Not thread-safe.
 *
```

## RuleId[id=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/sail/elasticsearch-store/src/main/java/org/eclipse/rdf4j/sail/elasticsearchstore/ElasticsearchStore.java`
#### Snippet
```java
					throw new RuntimeException(e2);
				}
				e.printStackTrace();
			}

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLSyntaxComplianceTest.java`
#### Snippet
```java
		} catch (MalformedQueryException e) {
			if (positiveTest) {
				e.printStackTrace();
				fail("Positive test case failed: " + e.getMessage());
			}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQL11SyntaxTest.java`
#### Snippet
```java
		} catch (MalformedQueryException e) {
			if (positiveTest) {
				e.printStackTrace();
				fail("Positive test case failed: " + e.getMessage());
			}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/DefaultGraphTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/DefaultGraphTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/DefaultGraphTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/UnionTest.java`
#### Snippet
```java
			assertEquals(2, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/UnionTest.java`
#### Snippet
```java
			assertEquals(3, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ValuesTest.java`
#### Snippet
```java
			assertEquals(1, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/PropertyPathTest.java`
#### Snippet
```java
			assertEquals("urn:s1", x.stringValue());
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/PropertyPathTest.java`
#### Snippet
```java

		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/PropertyPathTest.java`
#### Snippet
```java
			assertNotEquals(l1, l2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/OptionalTest.java`
#### Snippet
```java
			assertEquals(1, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/OptionalTest.java`
#### Snippet
```java

		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/BuiltinFunctionTest.java`
#### Snippet
```java
			}
		} catch (Exception e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/BuiltinFunctionTest.java`
#### Snippet
```java
			assertEquals(d1, d2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/BuiltinFunctionTest.java`
#### Snippet
```java
			}
		} catch (Exception e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/BuiltinFunctionTest.java`
#### Snippet
```java
			assertFalse(r1.equals(r2) && r1.equals(r3));
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/BuiltinFunctionTest.java`
#### Snippet
```java
			assertNotEquals(uuid1, uuid2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/BuiltinFunctionTest.java`
#### Snippet
```java
			assertNotEquals(uid1, uid2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			assertEquals(2, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			assertEquals(4, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			assertEquals(4, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/ArbitraryLengthPathTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			assertThat(getIntValue(s.getValue("c"), 0)).isEqualTo(3);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/sparql/tests/AggregateTest.java`
#### Snippet
```java
			assertThat(result.get(0).getValue("max")).isEqualTo(literal(Float.POSITIVE_INFINITY));
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (Exception e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(4, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals("urn:s1", x.stringValue());
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(2, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotEquals(uuid1, uuid2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(3, count.intValue());
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java

		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotEquals(l1, l2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertNotEquals(uid1, uid2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(1, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(vf.createLiteral(Float.POSITIVE_INFINITY), result.get(0).getValue("max"));
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(1, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (Exception e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(d1, d2);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(2, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(3, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertEquals(4, count);
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			}
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/ComplexSPARQLQueryTest.java`
#### Snippet
```java
			assertFalse(r1.equals(r2) && r1.equals(r3));
		} catch (QueryEvaluationException e) {
			e.printStackTrace();
			fail(e.getMessage());
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/IsolationLevelTest.java`
#### Snippet
```java
			} catch (RepositoryException e) {
				// it is okay to abort after a dirty read
				e.printStackTrace();
				return;
			}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(16, size(a, null, null, null, false));
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
			assertEquals(13, size(a, null, null, null, false));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(10, size(a, null, null, null, false));
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
			assertEquals(7, size(a, null, null, null, false));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(3, size(a, null, RDF.TYPE, PAINTING, false));
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
			assertEquals(0, size(a, null, RDF.TYPE, PAINTING, false));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(12, size(a, null, null, null, false));
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
			assertEquals(9, size(a, null, null, null, false));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(1, size);
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
		}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(12, size(a, null, null, null, false));
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
			assertEquals(9, size(a, null, null, null, false));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(10, size(a, null, null, null, false));
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
			assertEquals(7, size(a, null, null, null, false));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(3, size(a, null, RDF.TYPE, PAINTING, false));
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
			assertEquals(0, size(a, null, RDF.TYPE, PAINTING, false));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/optimistic/SnapshotTest.java`
#### Snippet
```java
			assertEquals(16, size(a, null, null, null, false));
		} catch (RepositoryException e) {
			e.printStackTrace();
			assertTrue(e.getCause() instanceof SailConflictException);
			assertEquals(13, size(a, null, null, null, false));
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/repository/src/main/java/org/eclipse/rdf4j/testsuite/repository/RepositoryConnectionTest.java`
#### Snippet
```java
			}
		} catch (RepositoryException e) {
			e.printStackTrace();
			// shouldn't happen
			fail(e.getMessage());
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailConcurrencyTest.java`
#### Snippet
```java
	protected synchronized void fail(String message, Throwable t) {
		System.err.println(message);
		t.printStackTrace();
		m_failed = true;
	}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/sail/src/main/java/org/eclipse/rdf4j/testsuite/sail/SailIsolationLevelTest.java`
#### Snippet
```java
			} catch (SailException e) {
				// it is okay to abort after a dirty read
				e.printStackTrace();
				return;
			}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/SPARQLParser.java`
#### Snippet
```java
					} catch (Exception e) {
						System.err.println(e.getMessage());
						e.printStackTrace();
					}
				}
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testsuites/benchmark/src/main/java/org/eclipse/rdf4j/benchmark/rio/util/DataSetGenerator.java`
#### Snippet
```java
			avgWordSize /= totalWords;
		} catch (IOException e) {
			e.printStackTrace();
		}
	}
```

## RuleId[id=ReplaceNullCheck]
### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/repository/sparql/src/main/java/org/eclipse/rdf4j/repository/sparql/SPARQLRepository.java`
#### Snippet
```java
	 */
	public void setAdditionalHttpHeaders(Map<String, String> additionalHttpHeaders) {
		if (additionalHttpHeaders == null) {
			this.additionalHttpHeaders = Collections.emptyMap();
		} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/PropertyShape.java`
#### Snippet
```java

		if (subject != null) {
			if (predicate == null) {
				model.add(subject, SHACL.PROPERTY, getId());
			} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/NodeShape.java`
#### Snippet
```java

		if (subject != null) {
			if (predicate == null) {
				model.add(subject, SHACL.NODE, getId());
			} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Stream.ofNullable()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/targets/EffectiveTarget.java`
#### Snippet
```java
	private Stream<EffectiveTargetObject> getOptionalAsStream() {
		Stream<EffectiveTargetObject> optional;
		if (this.optional != null) {
			optional = Stream.of(this.optional);
		} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/NotConstraintComponent.java`
#### Snippet
```java

		PlanNodeProvider planNodeProvider;
		if (overrideTargetNode != null) {
			planNodeProvider = overrideTargetNode;
		} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/OrConstraintComponent.java`
#### Snippet
```java
		PlanNodeProvider planNodeProvider;

		if (overrideTargetNode != null) {
			planNodeProvider = overrideTargetNode;
		} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/rio/trix/src/main/java/org/eclipse/rdf4j/rio/trix/TriXParser.java`
#### Snippet
```java
			Exception wrappedExc = e.getException();

			if (wrappedExc == null) {
				reportFatalError(e, e.getLineNumber(), e.getColumnNumber());
			} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/repository/http/src/main/java/org/eclipse/rdf4j/repository/http/HTTPRepository.java`
#### Snippet
```java
	 */
	public void setAdditionalHttpHeaders(Map<String, String> additionalHttpHeaders) {
		if (additionalHttpHeaders == null) {
			this.additionalHttpHeaders = Collections.emptyMap();
		} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `spring-components/rdf4j-spring/src/main/java/org/eclipse/rdf4j/spring/support/RDF4JTemplate.java`
#### Snippet
```java
		this.operationInstantiator = operationInstantiator;
		this.resourceLoader = resourceLoader;
		if (uuidSource == null) {
			this.uuidSource = new DefaultUUIDSource();
		} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SharedHttpClientSessionManager.java`
#### Snippet
```java
	private CloseableHttpClient createHttpClient() {
		HttpClientBuilder nextHttpClientBuilder = httpClientBuilder;
		if (nextHttpClientBuilder != null) {
			return nextHttpClientBuilder.build();
		}
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/http/client/src/main/java/org/eclipse/rdf4j/http/client/SPARQLProtocolSession.java`
#### Snippet
```java
	 */
	public void setAdditionalHttpHeaders(Map<String, String> additionalHttpHeaders) {
		if (additionalHttpHeaders == null) {
			this.additionalHttpHeaders = Collections.emptyMap();
		} else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/rio/rdfxml/src/main/java/org/eclipse/rdf4j/rio/rdfxml/RDFXMLParser.java`
#### Snippet
```java
			Exception wrappedExc = e.getException();

			if (wrappedExc == null) {
				reportFatalError(e, e.getLineNumber(), e.getColumnNumber());
			} else {
```

## RuleId[id=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/ArrangedWriter.java`
#### Snippet
```java
		}
		Set<Statement> set = stmtBySubject.get(key);
		if (set == null) {
			stmtBySubject.put(key, set = new TreeSet<>(comparator));
		}
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/util/MapOfListMaps.java`
#### Snippet
```java
		tmpList = intermediateMap.get(key2);

		if (tmpList == null) {
			tmpList = new ArrayList<>();
			intermediateMap.put(key2, tmpList);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/AbstractSearchIndex.java`
#### Snippet
```java
									String field = SearchFields.getPropertyField(r.getPredicate());
									Set<String> removedValues = removedOfResource.get(field);
									if (removedValues == null) {
										removedValues = new HashSet<>();
										removedOfResource.put(field, removedValues);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `core/queryrender/src/main/java/org/eclipse/rdf4j/queryrender/sparql/experimental/PreprocessedQuerySerializer.java`
#### Snippet
```java
	protected void setExtensionElemAlreadyRendered(ExtensionElem element) {
		Set<String> alreadyRenderedList = this.renderedExtensionElements.get(this.currentQueryProfile);
		if (alreadyRenderedList == null) {
			alreadyRenderedList = Sets.newHashSet();
			this.renderedExtensionElements.put(this.currentQueryProfile, alreadyRenderedList);
```

## RuleId[id=Convert2Lambda]
### Convert2Lambda
Anonymous new Function\<\>() can be replaced with lambda
in `core/sail/lucene/src/main/java/org/eclipse/rdf4j/sail/lucene/impl/LuceneQuery.java`
#### Snippet
```java
			docs = index.search(query);
		}
		return Iterables.transform(Arrays.asList(docs.scoreDocs), new Function<>() {

			@Override
```

### Convert2Lambda
Anonymous new Comparator\>() can be replaced with lambda
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/GraphComparisons.java`
#### Snippet
```java
			final List<Collection<BNode>> sortedPartitions = new ArrayList<>(
					getCurrentHashCodeMapping().asMap().values());
			Collections.sort(sortedPartitions, new Comparator<Collection<BNode>>() {
				public int compare(Collection<BNode> a, Collection<BNode> b) {
					int result = a.size() - b.size();
```

### Convert2Lambda
Anonymous new QueryEvaluationStep() can be replaced with lambda
in `core/queryalgebra/evaluation/src/main/java/org/eclipse/rdf4j/query/algebra/evaluation/impl/TupleFunctionEvaluationStrategy.java`
#### Snippet
```java
		}

		return new QueryEvaluationStep() {

			@Override
```

### Convert2Lambda
Anonymous new Function\<\>() can be replaced with lambda
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchQuery.java`
#### Snippet
```java
			hits = index.search(request, qb);
		}
		return Iterables.transform(hits, new Function<>() {

			@Override
```

### Convert2Lambda
Anonymous new Function\<\>() can be replaced with lambda
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
			hits = search(request, qb);
		}
		return Iterables.transform(hits, new Function<>() {

			@Override
```

### Convert2Lambda
Anonymous new Function\<\>() can be replaced with lambda
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
	protected Iterable<? extends SearchDocument> getDocuments(String resourceId) throws IOException {
		SearchHits hits = getDocuments(QueryBuilders.termQuery(SearchFields.URI_FIELD_NAME, resourceId));
		return Iterables.transform(hits, new Function<>() {

			@Override
```

### Convert2Lambda
Anonymous new Function\<\>() can be replaced with lambda
in `core/sail/elasticsearch/src/main/java/org/eclipse/rdf4j/sail/elasticsearch/ElasticsearchIndex.java`
#### Snippet
```java
		SearchRequestBuilder request = client.prepareSearch();
		SearchHits hits = search(request, QueryBuilders.boolQuery().must(qb).filter(fb));
		return Iterables.transform(hits, new Function<>() {

			@Override
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `testsuites/lucene/src/main/java/org/eclipse/testsuite/rdf4j/sail/lucene/AbstractLuceneSailTest.java`
#### Snippet
```java
					if (statement.getSubject().equals(SUBJECT_3) && statement.getPredicate().equals(score)) {
						r |= 4;
						continue;
					}
				}
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/ast/ParseException.java`
#### Snippet
```java
					retval.append(ch);
				}
				continue;
			}
		}
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `core/sail/nativerdf/src/main/java/org/eclipse/rdf4j/sail/nativerdf/btree/RangeIterator.java`
#### Snippet
```java
					// Value doesn't match search key/mask
					value = findNext(false);
					continue;
				} else {
					// Matching value found
```

## RuleId[id=RegExpUnnecessaryNonCapturingGroup]
### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\\\\u[0-9A-Fa-f]{4}|\\\\U[0-9A-Fa-f]{8})`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java

	private static final String HEX = "[0-9A-Fa-f]";
	private static final String UCHAR = "(?:\\\\u" + HEX + "{4}|\\\\U" + HEX + "{8})";
	private static final String IRI = "<(?:[^\u0000-\u0020<>\"{}|^`\\\\]|" + UCHAR + ")*>";

```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[A-Za-zÀ-ÖØ-öø-˿Ͱ-ͽͿ-῿‌-‍⁰-↏Ⰰ-⿯、-퟿豈-﷏ﷰ-�𐀀-󯿿]|_)`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
			+ "\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD"
			+ "\uD800\uDC00-\uDB7F\uDFFF]"; // <- \u10000-\uEFFFF expressed with surrogate pairs
	private static final String PN_CHARS_U = "(?:" + PN_CHARS_BASE + "|_)";
	private static final String PN_CHARS = "(?:" + PN_CHARS_U + "|[0-9\u0300-\u036F\u203F-\u2040\u00B7-])";
	private static final String BNODE_ID = "(?:" + PN_CHARS_U + "|[0-9])(?:(?:" + PN_CHARS + "|\\.)*" + PN_CHARS + ")?";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[A-Za-zÀ-ÖØ-öø-˿Ͱ-ͽͿ-῿‌-‍⁰-↏Ⰰ-⿯、-퟿豈-﷏ﷰ-�𐀀-󯿿]|_)|[0-9\u0300-\u036F\u203F-\u2040\u00B7-])`
in `core/rio/api/src/main/java/org/eclipse/rdf4j/rio/helpers/NTriplesUtil.java`
#### Snippet
```java
			+ "\uD800\uDC00-\uDB7F\uDFFF]"; // <- \u10000-\uEFFFF expressed with surrogate pairs
	private static final String PN_CHARS_U = "(?:" + PN_CHARS_BASE + "|_)";
	private static final String PN_CHARS = "(?:" + PN_CHARS_U + "|[0-9\u0300-\u036F\u203F-\u2040\u00B7-])";
	private static final String BNODE_ID = "(?:" + PN_CHARS_U + "|[0-9])(?:(?:" + PN_CHARS + "|\\.)*" + PN_CHARS + ")?";
	private static final String BNODE = "_:" + BNODE_ID;
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy fields 'target' and 'constraintComponents'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/Shape.java`
#### Snippet
```java
	}

	public Shape(Shape shape) {
		this.deactivated = shape.deactivated;
		this.message = shape.message;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'xone'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/XoneConstraintComponent.java`
#### Snippet
```java
	}

	public XoneConstraintComponent(XoneConstraintComponent xoneConstraintComponent) {
		super(xoneConstraintComponent.getId());
	}
```

### CopyConstructorMissesField
Copy constructor does not copy field 'not'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/NotConstraintComponent.java`
#### Snippet
```java
	}

	public NotConstraintComponent(NotConstraintComponent notConstraintComponent) {
		super(notConstraintComponent.getId());
	}
```

### CopyConstructorMissesField
Copy constructor does not copy field 'and'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/AndConstraintComponent.java`
#### Snippet
```java
	}

	public AndConstraintComponent(AndConstraintComponent andConstraintComponent) {
		super(andConstraintComponent.getId());
	}
```

### CopyConstructorMissesField
Copy constructor does not copy field 'or'
in `core/sail/shacl/src/main/java/org/eclipse/rdf4j/sail/shacl/ast/constraintcomponents/OrConstraintComponent.java`
#### Snippet
```java
	}

	public OrConstraintComponent(OrConstraintComponent orConstraintComponent) {
		super(orConstraintComponent.getId());
	}
```

### CopyConstructorMissesField
Copy constructor does not copy field 'constraints'
in `core/queryparser/sparql/src/main/java/org/eclipse/rdf4j/query/parser/sparql/GraphPattern.java`
#### Snippet
```java
	 * Creates a new graph pattern that inherits the context and scope from a parent graph pattern.
	 */
	public GraphPattern(GraphPattern parent) {
		contextVar = parent.contextVar;
		spScope = parent.spScope;
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `core/sail/solr/src/main/java/org/eclipse/rdf4j/sail/solr/SolrIndex.java`
#### Snippet
```java

		@Override
		public boolean equals(Object other) {
			return s.equals(other);
		}
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `core/model/src/main/java/org/eclipse/rdf4j/model/util/SynchronizedModel.java`
#### Snippet
```java

	@Override
	synchronized public boolean equals(Object o) {
		return delegate.equals(o);
	}
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `core/sail/lucene-api/src/main/java/org/eclipse/rdf4j/sail/lucene/BindingSetCollection.java`
#### Snippet
```java

	@Override
	public boolean equals(Object o) {
		return bindingSets.equals(o);
	}
```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'graphURI' to 'IRI'
in `testsuites/sparql/src/main/java/org/eclipse/rdf4j/testsuite/query/parser/sparql/manifest/SPARQLComplianceTest.java`
#### Snippet
```java

			for (Resource graphURI : graphURIs) {
				upload(((IRI) graphURI), graphURI);
			}
		}
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'val' to 'Resource'
in `core/rio/turtle/src/main/java/org/eclipse/rdf4j/rio/turtle/TurtleWriter.java`
#### Snippet
```java
			Value val = stack.pollLast();
			if (val instanceof Resource) {
				writeResource((Resource) val, inlineBNodes);
			} else {
				writeLiteral((Literal) val);
```

## RuleId[id=TestCaseWithNoTestMethods]
### TestCaseWithNoTestMethods
Test class `NegativeParserTest` has no tests
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/NegativeParserTest.java`
#### Snippet
```java
import junit.framework.TestCase;

public class NegativeParserTest extends TestCase {

	/*-----------*
```

### TestCaseWithNoTestMethods
Test class `PositiveParserTest` has no tests
in `testsuites/rio/src/main/java/org/eclipse/rdf4j/testsuite/rio/PositiveParserTest.java`
#### Snippet
```java
import junit.framework.TestCase;

public class PositiveParserTest extends TestCase {

	/*-----------*
```

