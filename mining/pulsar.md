# pulsar 
 
# Bad smells
I found 8275 bad smells with 755 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| RedundantFieldInitialization | 1059 | false |
| ReturnNull | 731 | false |
| UnnecessaryFullyQualifiedName | 605 | false |
| MissortedModifiers | 524 | false |
| DefaultAnnotationParam | 365 | false |
| BoundedWildcard | 335 | false |
| PublicFieldAccessedInSynchronizedContext | 300 | false |
| SystemOutErr | 252 | false |
| UnstableApiUsage | 223 | false |
| CodeBlock2Expr | 217 | true |
| FieldAccessedSynchronizedAndUnsynchronized | 180 | false |
| OptionalUsedAsFieldOrParameterType | 161 | false |
| SynchronizeOnThis | 157 | false |
| UnusedAssignment | 157 | false |
| UnnecessaryQualifierForThis | 141 | false |
| EnhancedSwitchMigration | 136 | false |
| UtilityClassWithoutPrivateConstructor | 129 | true |
| Convert2MethodRef | 121 | false |
| SizeReplaceableByIsEmpty | 120 | true |
| SimplifyOptionalCallChains | 106 | false |
| AssignmentToMethodParameter | 99 | false |
| PatternVariableCanBeUsed | 98 | false |
| UnstableTypeUsedInSignature | 96 | false |
| TextBlockMigration | 93 | false |
| DataFlowIssue | 84 | false |
| ConstantValue | 75 | false |
| MissingSerialAnnotation | 73 | false |
| UnnecessaryLocalVariable | 69 | true |
| UnnecessaryToStringCall | 69 | true |
| DeprecatedIsStillUsed | 68 | false |
| ZeroLengthArrayInitialization | 62 | false |
| NonProtectedConstructorInAbstractClass | 53 | true |
| StaticCallOnSubclass | 52 | false |
| UnnecessaryReturn | 50 | true |
| ClassCanBeRecord | 49 | false |
| DynamicRegexReplaceableByCompiledPattern | 47 | false |
| InstanceofCatchParameter | 42 | false |
| BusyWait | 40 | false |
| UnnecessarySemicolon | 34 | false |
| RedundantImplements | 32 | false |
| OptionalGetWithoutIsPresent | 32 | false |
| RefusedBequest | 31 | false |
| NestedAssignment | 28 | false |
| OptionalContainsCollection | 28 | false |
| SimplifyStreamApiCallChains | 27 | false |
| IgnoreResultOfCall | 25 | false |
| UnnecessarySuperQualifier | 24 | false |
| ReplaceAssignmentWithOperatorAssignment | 22 | false |
| NonSerializableFieldInSerializableClass | 22 | false |
| ThrowablePrintStackTrace | 22 | false |
| RedundantMethodOverride | 21 | false |
| AssignmentToLambdaParameter | 21 | false |
| FinalStaticMethod | 20 | false |
| IOResource | 20 | false |
| TrivialStringConcatenation | 19 | false |
| RedundantSuppression | 19 | false |
| ToArrayCallWithZeroLengthArrayArgument | 18 | true |
| DuplicateThrows | 18 | false |
| FieldMayBeStatic | 17 | false |
| EmptyMethod | 16 | false |
| MismatchedCollectionQueryUpdate | 15 | false |
| Lombok | 15 | false |
| ReplaceNullCheck | 15 | false |
| InnerClassMayBeStatic | 14 | true |
| NonSynchronizedMethodOverridesSynchronizedMethod | 13 | false |
| CatchMayIgnoreException | 13 | false |
| AssignmentToStaticFieldFromInstanceMethod | 12 | false |
| IfStatementWithIdenticalBranches | 12 | false |
| OptionalIsPresent | 12 | false |
| UnnecessaryModifier | 11 | true |
| IntegerMultiplicationImplicitCastToLong | 11 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 11 | false |
| StringBufferReplaceableByString | 11 | false |
| InfiniteLoopStatement | 11 | false |
| SynchronizeOnNonFinalField | 11 | false |
| Convert2Lambda | 11 | false |
| UseBulkOperation | 10 | false |
| ExceptionNameDoesntEndWithException | 10 | false |
| PointlessArithmeticExpression | 9 | false |
| ClassNameSameAsAncestorName | 9 | false |
| UnnecessaryContinue | 9 | false |
| EmptyStatementBody | 8 | false |
| RegExpRedundantEscape | 8 | false |
| KeySetIterationMayUseEntrySet | 8 | false |
| RedundantModifiersValueLombok | 8 | false |
| UseOfPropertiesAsHashtable | 8 | false |
| StringConcatenationInsideStringBufferAppend | 8 | false |
| SetReplaceableByEnumSet | 8 | false |
| WrapperTypeMayBePrimitive | 7 | false |
| MarkedForRemoval | 7 | false |
| NullableProblems | 7 | false |
| ReadWriteStringCanBeUsed | 7 | false |
| AbstractClassNeverImplemented | 7 | false |
| WhileCanBeForeach | 6 | false |
| StringOperationCanBeSimplified | 6 | false |
| DuplicateExpressions | 6 | false |
| Java8MapForEach | 6 | false |
| MismatchedJavadocCode | 6 | false |
| CStyleArrayDeclaration | 5 | false |
| ObjectNotify | 5 | false |
| MethodOverloadsParentMethod | 5 | false |
| RedundantArrayCreation | 5 | false |
| NonShortCircuitBoolean | 5 | false |
| NonFinalFieldOfException | 5 | false |
| Java8MapApi | 5 | false |
| JavaLangImport | 4 | false |
| NonStrictComparisonCanBeEquality | 4 | true |
| EnumSwitchStatementWhichMissesCases | 4 | false |
| DuplicateBranchesInSwitch | 4 | false |
| RegExpSimplifiable | 4 | false |
| ThrowablePrintedToSystemOut | 4 | false |
| FunctionalExpressionCanBeFolded | 4 | false |
| RedundantStringFormatCall | 4 | false |
| StringRepeatCanBeUsed | 3 | false |
| SocketResource | 3 | false |
| ThrowFromFinallyBlock | 3 | false |
| MethodOverridesStaticMethod | 3 | false |
| SuspiciousMethodCalls | 3 | false |
| Java9CollectionFactory | 3 | false |
| CastCanBeRemovedNarrowingVariableType | 3 | false |
| CommentedOutCode | 2 | false |
| OptionalAssignedToNull | 2 | false |
| DoubleBraceInitialization | 2 | false |
| ManualArrayToCollectionCopy | 2 | false |
| ObviousNullCheck | 2 | false |
| ShiftOutOfRange | 2 | false |
| SwitchStatementWithConfusingDeclaration | 2 | false |
| ComparatorMethodParameterNotUsed | 2 | false |
| JavaReflectionInvocation | 2 | false |
| CaughtExceptionImmediatelyRethrown | 2 | false |
| ObsoleteCollection | 2 | false |
| CastToIncompatibleInterface | 2 | false |
| WhileLoopSpinsOnField | 2 | false |
| NullArgumentToVariableArgMethod | 2 | false |
| NonExceptionNameEndsWithException | 2 | false |
| TypeParameterExtendsObject | 2 | false |
| RedundantCollectionOperation | 2 | false |
| NestedSynchronizedStatement | 2 | false |
| StringEqualsEmptyString | 2 | false |
| ConditionCoveredByFurtherCondition | 2 | false |
| CopyConstructorMissesField | 2 | false |
| ThreadStartInConstruction | 2 | false |
| EqualsWhichDoesntCheckParameterClass | 2 | false |
| StaticInitializerReferencesSubClass | 1 | false |
| EqualsBetweenInconvertibleTypes | 1 | false |
| AccessStaticViaInstance | 1 | false |
| SimplifiableBooleanExpression | 1 | false |
| EqualsAndHashcode | 1 | false |
| TypeParameterHidesVisibleType | 1 | false |
| UnnecessaryCallToStringValueOf | 1 | false |
| ProtectedMemberInFinalClass | 1 | true |
| UtilityClassWithPublicConstructor | 1 | false |
| UseCompareMethod | 1 | false |
| RedundantFileCreation | 1 | false |
| WaitNotInLoop | 1 | false |
| SillyAssignment | 1 | false |
| SlowListContainsAll | 1 | false |
| ArrayEquality | 1 | false |
| StringBufferReplaceableByStringBuilder | 1 | false |
| FuseStreamOperations | 1 | false |
| RedundantUnmodifiable | 1 | false |
| RedundantLengthCheck | 1 | false |
| BoxingBoxedValue | 1 | false |
| AbstractMethodCallInConstructor | 1 | false |
## RuleId[ruleID=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Option\[getOptions().size()\]'
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/seq/HdfsSequentialTextSink.java`
#### Snippet
```java
                .createWriter(
                   getConfiguration(),
                   getOptions().toArray(new Option[getOptions().size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Option\[options.size()\]'
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/seq/HdfsAbstractSequenceFileSink.java`
#### Snippet
```java
        List<Option> options = getOptions();
        return SequenceFile.createWriter(getConfiguration(),
                options.toArray(new Option[options.size()]));
     }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Option\[options.size()\]'
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/seq/HdfsAbstractSequenceFileSink.java`
#### Snippet
```java
        List<Option> options = getOptions();
        return SequenceFile.createWriter(getConfiguration(),
                options.toArray(new Option[options.size()]));
     }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Option\[getOptions().size()\]'
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/seq/HdfsSequentialTextSink.java`
#### Snippet
```java
                .createWriter(
                   getConfiguration(),
                   getOptions().toArray(new Option[getOptions().size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ServerConnector\[connectors.size()\]'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/WebServer.java`
#### Snippet
```java
        // Limit number of concurrent HTTP connections to avoid getting out of file descriptors
        connectors.stream().forEach(c -> c.setAcceptQueueSize(config.getHttpServerAcceptQueueSize()));
        server.setConnectors(connectors.toArray(new ServerConnector[connectors.size()]));

        filterInitializer = new FilterInitializer(config, authenticationService);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Handler\[handlers.size()\]'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/WebServer.java`
#### Snippet
```java

        ContextHandlerCollection contexts = new ContextHandlerCollection();
        contexts.setHandlers(handlers.toArray(new Handler[handlers.size()]));

        HandlerCollection handlerCollection = new HandlerCollection();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ServerConnector\[connectors.size()\]'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/WebService.java`
#### Snippet
```java
        // Limit number of concurrent HTTP connections to avoid getting out of file descriptors
        connectors.forEach(c -> c.setAcceptQueueSize(config.getHttpServerAcceptQueueSize()));
        server.setConnectors(connectors.toArray(new ServerConnector[connectors.size()]));

        filterInitializer = new FilterInitializer(pulsar);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Handler\[handlers.size()\]'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/WebService.java`
#### Snippet
```java

            ContextHandlerCollection contexts = new ContextHandlerCollection();
            contexts.setHandlers(handlers.toArray(new Handler[handlers.size()]));

            HandlerCollection handlerCollection = new HandlerCollection();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[multiHosts.size()\]'
in `pulsar-common/src/main/java/org/apache/pulsar/common/net/ServiceURI.java`
#### Snippet
```java
                        serviceURI.getServiceInfos(),
                        serviceURI.getServiceUser(),
                        multiHosts.toArray(new String[multiHosts.size()]),
                        serviceURI.getServicePath(),
                        serviceURI.getUri());
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[serviceHosts.size()\]'
in `pulsar-common/src/main/java/org/apache/pulsar/common/net/ServiceURI.java`
#### Snippet
```java
            serviceInfos,
            serviceUser,
            serviceHosts.toArray(new String[serviceHosts.size()]),
            servicePath,
            uri);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new X509Certificate\[collection.size()\]'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
            cf = CertificateFactory.getInstance("X.509");
            Collection<X509Certificate> collection = (Collection<X509Certificate>) cf.generateCertificates(inStream);
            return collection.toArray(new X509Certificate[collection.size()]);
        } catch (CertificateException | IOException e) {
            throw new KeyManagementException("Certificate loading error", e);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[protocols.size()\]'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
    private static void setupProtocols(SslContextBuilder builder, Set<String> protocols) {
        if (protocols != null && protocols.size() > 0) {
            builder.protocols(protocols.toArray(new String[protocols.size()]));
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Handler\[handlers.size()\]'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/WorkerServer.java`
#### Snippet
```java

        ContextHandlerCollection contexts = new ContextHandlerCollection();
        contexts.setHandlers(handlers.toArray(new Handler[handlers.size()]));
        HandlerCollection handlerCollection = new HandlerCollection();
        handlerCollection.setHandlers(new Handler[]{contexts, new DefaultHandler(), requestLogHandler});
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ServerConnector\[connectors.size()\]'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/WorkerServer.java`
#### Snippet
```java
        // Limit number of concurrent HTTP connections to avoid getting out of file descriptors
        connectors.forEach(c -> c.setAcceptQueueSize(workerConfig.getHttpServerAcceptQueueSize()));
        server.setConnectors(connectors.toArray(new ServerConnector[connectors.size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new File\[files.size()\]'
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
        }

        ClassLoader functionInstanceClsLoader = loadJar(root, files.toArray(new File[files.size()]));

        System.out.println("Using function root classloader: " + root);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Handler\[handlers.size()\]'
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/ProxyServer.java`
#### Snippet
```java

        ContextHandlerCollection contexts = new ContextHandlerCollection();
        contexts.setHandlers(handlers.toArray(new Handler[handlers.size()]));

        HandlerCollection handlerCollection = new HandlerCollection();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ServerConnector\[connectors.size()\]'
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/ProxyServer.java`
#### Snippet
```java
        // file descriptors
        connectors.stream().forEach(c -> c.setAcceptQueueSize(config.getHttpServerAcceptQueueSize()));
        server.setConnectors(connectors.toArray(new ServerConnector[connectors.size()]));

        if (config.getMaxConcurrentHttpRequests() > 0) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[parsed.size()\]'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
        }

        String[] argv = parsed.toArray(new String[parsed.size()]);
        return argv;
    }
```

## RuleId[ruleID=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ResourceAvailabilityRanker.java`
#### Snippet
```java
            int percentAvailable = 0;
            if (entry.getValue().limit > 0 && entry.getValue().limit > entry.getValue().usage) {
                Double temp = ((entry.getValue().limit - entry.getValue().usage) / entry.getValue().limit) * 100;
                percentAvailable = temp.intValue();
            }
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            throws ExecutionException, InterruptedException {
        int backlogQuotaLimitInSecond = getBacklogQuota(BacklogQuotaType.message_age).getLimitTime();
        Long ledgerId = slowestPosition.getLedgerId();
        if (((ManagedLedgerImpl) ledger).getLedgersInfo().lastKey().equals(ledgerId)) {
            return CompletableFuture.completedFuture(false);
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/PoliciesUtil.java`
#### Snippet
```java
                    "Invalid number of bundles. Number of bundles has to be in the range of (0, 2^32].");
        }
        Long maxVal = MAX_BUNDLES;
        Long segSize = maxVal / numBundles;
        List<String> partitions = new ArrayList<>();
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/PoliciesUtil.java`
#### Snippet
```java
        }
        Long maxVal = MAX_BUNDLES;
        Long segSize = maxVal / numBundles;
        List<String> partitions = new ArrayList<>();
        partitions.add(String.format("0x%08x", 0L));
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/PoliciesUtil.java`
#### Snippet
```java
        List<String> partitions = new ArrayList<>();
        partitions.add(String.format("0x%08x", 0L));
        Long curPartition = segSize;
        for (int i = 0; i < numBundles; i++) {
            if (i != numBundles - 1) {
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
                    } else {
                        // Lower bits are full. Need to expand and create another HOB node.
                        Long newHighBits = highBits + 1;
                        return createHOBPathAndGenerateId(ledgerPrefix, newHighBits.intValue());
                    }
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
            Long offloadAfterElapsedInMillis = OffloadPoliciesImpl.DEFAULT_OFFLOAD_DELETION_LAG_IN_MILLIS;
            if (StringUtils.isNotEmpty(offloadAfterElapsedStr)) {
                Long offloadAfterElapsed;
                try {
                    offloadAfterElapsed = TimeUnit.SECONDS.toMillis(
```

## RuleId[ruleID=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `pulsar-io/data-generator/src/main/java/org/apache/pulsar/io/datagenerator/Person.java`
#### Snippet
```java
        FEMALE;

        private Sex() {
        }
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `pulsar-io/batch-data-generator/src/main/java/org/apache/pulsar/io/batchdatagenerator/Person.java`
#### Snippet
```java
        FEMALE;

        private Sex() {
        }
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
     * @param <T> - The incoming type of data in the range object.
     */
    public interface RangeProcessor<T extends Comparable<T>> {
        /**
         *
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
     * @param <T> the type of the result.
     */
    public interface LongPairConsumer<T> {
        T apply(long key, long value);
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairSet.java`
#### Snippet
```java
     * Predicate to checks for a key-value pair where both of them have long types.
     */
    public interface LongPairPredicate {
        boolean test(long v1, long v2);
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairSet.java`
#### Snippet
```java
     */
    @FunctionalInterface
    public interface LongPairFunction<T> {

        /**
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketError.java`
#### Snippet
```java
    }

    private WebSocketError(int code, String description) {
        this.code = code;
        this.description = description;
```

### UnnecessaryModifier
Modifier `transient` is redundant for a 'static' field
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketReaderServlet.java`
#### Snippet
```java

public class WebSocketReaderServlet extends WebSocketServlet {
    private static final transient long serialVersionUID = 1L;

    public static final String SERVLET_PATH = "/ws/reader";
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Mode.java`
#### Snippet
```java
    PERSISTENT(0), NON_PERSISTENT(1), ALL(2),;
    private final int value;
    private Mode(int value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/naming/TopicDomain.java`
#### Snippet
```java
    private String value;

    private TopicDomain(String value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Topics.java`
#### Snippet
```java
        public final String value;

        private QueryParam(String value) {
            this.value = value;
        }
```

## RuleId[ruleID=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`j * 1` can be replaced with 'j'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionKey.java`
#### Snippet
```java
  public ByteBuffer keyAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer keyInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
  public byte value(int j) { int o = __offset(6); return o != 0 ? bb.get(__vector(o) + j * 1) : 0; }
  public int valueLength() { int o = __offset(6); return o != 0 ? __vector_len(o) : 0; }
  public ByteBuffer valueAsByteBuffer() { return __vector_as_bytebuffer(6, 1); }
```

### PointlessArithmeticExpression
`j * 1` can be replaced with 'j'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/Message.java`
#### Snippet
```java
  public KeyValue properties(KeyValue obj, int j) { int o = __offset(6); return o != 0 ? obj.__assign(__indirect(__vector(o) + j * 4), bb) : null; }
  public int propertiesLength() { int o = __offset(6); return o != 0 ? __vector_len(o) : 0; }
  public byte payload(int j) { int o = __offset(8); return o != 0 ? bb.get(__vector(o) + j * 1) : 0; }
  public int payloadLength() { int o = __offset(8); return o != 0 ? __vector_len(o) : 0; }
  public ByteBuffer payloadAsByteBuffer() { return __vector_as_bytebuffer(8, 1); }
```

### PointlessArithmeticExpression
`j * 1` can be replaced with 'j'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionCtx.java`
#### Snippet
```java
  public EncryptionKey keys(EncryptionKey obj, int j) { int o = __offset(4); return o != 0 ? obj.__assign(__indirect(__vector(o) + j * 4), bb) : null; }
  public int keysLength() { int o = __offset(4); return o != 0 ? __vector_len(o) : 0; }
  public byte param(int j) { int o = __offset(6); return o != 0 ? bb.get(__vector(o) + j * 1) : 0; }
  public int paramLength() { int o = __offset(6); return o != 0 ? __vector_len(o) : 0; }
  public ByteBuffer paramAsByteBuffer() { return __vector_as_bytebuffer(6, 1); }
```

### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java
            bootstrap.childOption(ChannelOption.TCP_NODELAY, true);
            bootstrap.childOption(ChannelOption.RCVBUF_ALLOCATOR,
                    new AdaptiveRecvByteBufAllocator(1024, 16 * 1024, 1 * 1024 * 1024));

            EventLoopUtil.enableTriggeredMode(bootstrap);
```

### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java
        bootstrap.childOption(ChannelOption.TCP_NODELAY, true);
        bootstrap.childOption(ChannelOption.RCVBUF_ALLOCATOR,
                new AdaptiveRecvByteBufAllocator(1024, 16 * 1024, 1 * 1024 * 1024));

        Class<? extends ServerSocketChannel> serverSocketChannelClass =
```

### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            bootstrap.childOption(ChannelOption.TCP_NODELAY, true);
            bootstrap.childOption(ChannelOption.RCVBUF_ALLOCATOR,
                    new AdaptiveRecvByteBufAllocator(1024, 16 * 1024, 1 * 1024 * 1024));
            EventLoopUtil.enableTriggeredMode(bootstrap);
            DefaultThreadFactory defaultThreadFactory =
```

### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        bootstrap.childOption(ChannelOption.TCP_NODELAY, true);
        bootstrap.childOption(ChannelOption.RCVBUF_ALLOCATOR,
            new AdaptiveRecvByteBufAllocator(1024, 16 * 1024, 1 * 1024 * 1024));
        bootstrap.channel(EventLoopUtil.getServerSocketChannelClass(workerGroup));
        EventLoopUtil.enableTriggeredMode(bootstrap);
```

### PointlessArithmeticExpression
`1 * 60 * 1000L` can be replaced with '60 \* 1000L'
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
    // thread will not sleep between refresh attempts any less than 1 minute (60*1000 milliseconds = 1 minute).
    // Change the '1' to e.g. 5, to change this to 5 minutes.
    private static final long MIN_TIME_BEFORE_RELOGIN = 1 * 60 * 1000L;

    private long getRefreshTime(KerberosTicket tgt) {
```

### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "In the UniformLoadShedder strategy, the minimum throughput that triggers unload."
    )
    private int minUnloadMessageThroughput = 1 * 1024 * 1024;

    @FieldContext(
```

## RuleId[ruleID=MarkedForRemoval]
### MarkedForRemoval
'strictConfig()' is deprecated and marked for removal
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorFactory.java`
#### Snippet
```java

            Injector injector = app
                    .strictConfig()
                    .doNotInitializeLogging()
                    .setRequiredConfigurationProperties(config)
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarClassLoader.java`
#### Snippet
```java
        throws IOException {
        File unpacked = NarUnpacker.unpackNar(narPath, getNarExtractionDirectory(narExtractionDirectory));
        return AccessController.doPrivileged(new PrivilegedAction<NarClassLoader>() {
            @SneakyThrows
            @Override
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarClassLoader.java`
#### Snippet
```java
        throws IOException {
        File unpacked = NarUnpacker.unpackNar(narPath, getNarExtractionDirectory(narExtractionDirectory));
        return AccessController.doPrivileged(new PrivilegedAction<NarClassLoader>() {
            @SneakyThrows
            @Override
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarClassLoader.java`
#### Snippet
```java
import java.nio.file.Files;
import java.nio.file.Paths;
import java.security.AccessController;
import java.security.PrivilegedAction;
import java.util.ArrayList;
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ClassLoaderUtils.java`
#### Snippet
```java
import java.net.URL;
import java.net.URLClassLoader;
import java.security.AccessController;
import java.security.PrivilegedAction;
import lombok.extern.slf4j.Slf4j;
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ClassLoaderUtils.java`
#### Snippet
```java
    public static ClassLoader loadJar(File jar) throws MalformedURLException {
        java.net.URL url = jar.toURI().toURL();
        return AccessController.doPrivileged(
            (PrivilegedAction<URLClassLoader>) () -> new URLClassLoader(new URL[]{url}));
    }
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ClassLoaderUtils.java`
#### Snippet
```java
    public static ClassLoader loadJar(File jar) throws MalformedURLException {
        java.net.URL url = jar.toURI().toURL();
        return AccessController.doPrivileged(
            (PrivilegedAction<URLClassLoader>) () -> new URLClassLoader(new URL[]{url}));
    }
```

## RuleId[ruleID=EmptyStatementBody]
### EmptyStatementBody
`for` statement has empty body
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
                    // CHECKSTYLE.OFF: EmptyStatement
                    int arrayDimension;
                    for (arrayDimension = 0; className.charAt(arrayDimension) == '['; ++arrayDimension) {
                    }
                    // CHECKSTYLE.ON: EmptyStatement
```

### EmptyStatementBody
`for` statement has empty body
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
    public BitSetRecyclable resetWords(long[] words) {
        int n;
        for (n = words.length; n > 0 && words[n - 1] == 0; n--)
            ;
        long[] longs = Arrays.copyOf(words, n);
```

### EmptyStatementBody
`for` statement has empty body
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
        bb = bb.slice().order(ByteOrder.LITTLE_ENDIAN);
        int n;
        for (n = bb.remaining(); n > 0 && bb.get(n - 1) == 0; n--)
            ;
        long[] words = new long[(n + 7) / 8];
```

### EmptyStatementBody
`for` statement has empty body
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
        lb = lb.slice();
        int n;
        for (n = lb.remaining(); n > 0 && lb.get(n - 1) == 0; n--)
            ;
        long[] words = new long[n];
```

### EmptyStatementBody
`for` statement has empty body
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
    public static BitSetRecyclable valueOf(long[] longs) {
        int n;
        for (n = longs.length; n > 0 && longs[n - 1] == 0; n--)
            ;
        return new BitSetRecyclable(Arrays.copyOf(longs, n));
```

### EmptyStatementBody
`if` statement has empty body
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
        // handle LH invalidation
        readResult.exceptionally(exception -> {
            if (exception instanceof BKException
                    && ((BKException) exception).getCode() == BKException.Code.TooManyRequestsException) {
            } else {
```

### EmptyStatementBody
`while` statement has empty body
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
            // Users wishing to stop this should Ctrl + C and use another
            // Controller to send new commands.
            while (true) {}
        }
    }
```

### EmptyStatementBody
`if` statement has empty body
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java
    static {
        // DO NOT EDIT - Load Conscrypt provider
        if (SecurityUtility.CONSCRYPT_PROVIDER != null) {
        }
    }
```

## RuleId[ruleID=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/ManagedCursorMetrics.java`
#### Snippet
```java
            Iterator<ManagedCursor> cursorIterator = cursorContainer.iterator();

            while (cursorIterator.hasNext()) {
                ManagedCursorImpl cursor = (ManagedCursorImpl) cursorIterator.next();
                ManagedCursorMXBean cStats = cursor.getStats();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageRedeliveryTracker.java`
#### Snippet
```java
                log.info("[{}] {} messages will be re-delivered", consumerBase, messageIds.size());
                Iterator<MessageId> iterator = messageIds.iterator();
                while (iterator.hasNext()) {
                    MessageId messageId = iterator.next();
                    ackTimeoutMessages.remove(messageId);
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlockAwareSegmentInputStreamImpl.java`
#### Snippet
```java
            List<ByteBuf> entries = Lists.newLinkedList();
            Iterator<LedgerEntry> iterator = ledgerEntriesOnce.iterator();
            while (iterator.hasNext()) {
                LedgerEntry entry = iterator.next();
                ByteBuf buf = entry.getEntryBuffer().retain();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `tiered-storage/file-system/src/main/java/org/apache/bookkeeper/mledger/offload/filesystem/impl/FileSystemManagedLedgerOffloader.java`
#### Snippet
```java
            if (ledgerReader.fileSystemWriteException == null) {
                Iterator<LedgerEntry> iterator = ledgerEntriesOnce.iterator();
                while (iterator.hasNext()) {
                    LedgerEntry entry = iterator.next();
                    long entryId = entry.getEntryId();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BKCluster.java`
#### Snippet
```java
            Iterator<NetworkInterface> var1 = Collections.list(nifs).iterator();

            while (var1.hasNext()) {
                NetworkInterface nif = var1.next();
                if (nif.isLoopback()) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/Actions.java`
#### Snippet
```java
    public void run() throws InterruptedException {
        Iterator<Action> it = this.actions.iterator();
        while (it.hasNext()) {
            Action action = it.next();

```

## RuleId[ruleID=StaticInitializerReferencesSubClass]
### StaticInitializerReferencesSubClass
Referencing subclass UnAckedMessageTrackerDisabled from superclass UnAckedMessageTracker initializer might lead to class loading deadlock
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageTracker.java`
#### Snippet
```java

    public static final UnAckedMessageTrackerDisabled UNACKED_MESSAGE_TRACKER_DISABLED =
            new UnAckedMessageTrackerDisabled();
    protected final long ackTimeoutMillis;
    protected final long tickDurationInMs;
```

## RuleId[ruleID=CommentedOutCode]
### CommentedOutCode
Commented out code (2 lines)
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java
        // create a ZooKeeper server(dataDir, dataLogDir, port)
        LOG.info("Starting ZK server");
        // ServerStats.registerAsConcrete();
        // ClientBase.setupTestEnv();

```

### CommentedOutCode
Commented out code (5 lines)
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java
        secondary = validateList(secondary);

        // System.out.println("namespaces = " + namespaces);
        // System.out.println("primary = " + primary);
        // System.out.println("secondary = " + secondary);
```

## RuleId[ruleID=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of parameter `passwd`
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java

    @Override
    public LedgerHandle createLedger(DigestType digestType, byte passwd[])
            throws BKException, InterruptedException {
        return createLedger(3, 2, digestType, passwd);
```

### CStyleArrayDeclaration
C-style array declaration of parameter `passwd`
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java

    @Override
    public LedgerHandle createLedger(int ensSize, int qSize, DigestType digestType, byte passwd[])
            throws BKException, InterruptedException {
        return createLedger(ensSize, qSize, qSize, digestType, passwd);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `ingestFiles`
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/FileUtils.java`
#### Snippet
```java
        // ensure the specified directory is actually a directory and that it exists
        if (null != directory && directory.isDirectory()) {
            final File ingestFiles[] = directory.listFiles();
            if (ingestFiles == null) {
                // null if abstract pathname does not denote a directory, or if an I/O error occurs
```

### CStyleArrayDeclaration
C-style array declaration of parameter `args`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
        this(args, new Properties());
    }
    public PulsarShell(String args[], Properties props) throws IOException {
        properties = props;
        mainCommander = new JCommander();
```

### CStyleArrayDeclaration
C-style array declaration of parameter `args`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
    private ExecState execState = ExecState.IDLE;

    public PulsarShell(String args[]) throws IOException {
        this(args, new Properties());
    }
```

## RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `records` from instance context
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/SinkOfFlume.java`
#### Snippet
```java
    @Override
    public synchronized void start() {
        records = new LinkedBlockingQueue<Map<String, Object>>();
        this.counter = new SinkCounter("flume-sink");
    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `records` from instance context
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/sink/AbstractSink.java`
#### Snippet
```java
    public void open(Map<String, Object> config, SinkContext sinkContext) throws Exception {

        records = new LinkedBlockingQueue<>();

        FlumeConfig flumeConfig = FlumeConfig.load(config);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `instance` from instance context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TransactionBufferClientStatsImpl.java`
#### Snippet
```java
    public void close() {
        if (this.closed.compareAndSet(false, true)) {
            instance = null;
            CollectorRegistry.defaultRegistry.unregister(this.abortFailed);
            CollectorRegistry.defaultRegistry.unregister(this.commitFailed);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `maxIntervalForSuppressingReportsMSecs` from instance context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupService.java`
#### Snippet
```java
                        timeUnitScale);
            this.resourceUsagePublishPeriodInSeconds = newPeriodInSeconds;
            maxIntervalForSuppressingReportsMSecs =
                    this.resourceUsagePublishPeriodInSeconds * MaxUsageReportSuppressRounds;
        }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `maxIntervalForSuppressingReportsMSecs` from instance context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupService.java`
#### Snippet
```java
                    periodInSecs,
                    this.timeUnitScale);
        maxIntervalForSuppressingReportsMSecs =
                this.resourceUsagePublishPeriodInSeconds * MaxUsageReportSuppressRounds;

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `maxMessageSize` from instance context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java
                          + "server frame size {}", ctx.channel(), connected.getMaxMessageSize());
            }
            maxMessageSize = connected.getMaxMessageSize();
            ctx.pipeline().replace("frameDecoder", "newFrameDecoder", new LengthFieldBasedFrameDecoder(
                connected.getMaxMessageSize() + Commands.MESSAGE_SIZE_FRAME_PADDING, 0, 4, 0, 4));
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `messageFormatter` from instance context
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java

            if (arguments.formatPayload) {
                messageFormatter = getMessageFormatter(arguments.formatterClass);
            }
        } else {
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `totalConnectionNum` from instance context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/limiter/ConnectionController.java`
#### Snippet
```java
                }
                if (maxConnectionsLimitEnabled) {
                    totalConnectionNum--;
                }
            } finally {
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `totalConnectionNum` from instance context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/limiter/ConnectionController.java`
#### Snippet
```java
                }
                if (maxConnectionsLimitEnabled) {
                    totalConnectionNum++;
                }
                if (maxConnectionsLimitEnabled && totalConnectionNum > maxConnections) {
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `keyGenerator` from instance context
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
                return;
            }
            keyGenerator = KeyGenerator.getInstance("AES");
            int aesKeyLength = Cipher.getMaxAllowedKeyLength("AES");
            if (aesKeyLength <= 128) {
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `jaasCredentialsContainer` from instance context
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
                    log.info("JAAS loginContext is: {}.", loginContextName);
                    try {
                        jaasCredentialsContainer = new JAASCredentialsContainer(
                            loginContextName,
                            new ClientCallbackHandler(),
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `initializedJAAS` from instance context
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
                            new ClientCallbackHandler(),
                            configuration);
                        initializedJAAS = true;
                    } catch (LoginException e) {
                        log.error("JAAS login in client failed", e);
```

## RuleId[ruleID=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/EnvVarResolverProperties.java`
#### Snippet
```java
        Preconditions.checkNotNull(input);
        // match ${ENV_VAR_NAME}
        Pattern p = Pattern.compile("\\$\\{(\\w+)\\}");
        Matcher m = p.matcher(input);
        StringBuffer sb = new StringBuffer();
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java
                indexName = indexName.substring(0, indexName.length() - 1);
            }
            if (indexName.length() <= 0 || !indexName.matches("[a-zA-Z\\.0-9][a-zA-Z_\\.\\-\\+0-9]*")) {
                throw new RuntimeException(new IOException("Cannot convert the topic name='"
                        + topicName + "' to a valid elasticsearch index name"));
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java
                indexName = indexName.substring(0, indexName.length() - 1);
            }
            if (indexName.length() <= 0 || !indexName.matches("[a-zA-Z\\.0-9][a-zA-Z_\\.\\-\\+0-9]*")) {
                throw new RuntimeException(new IOException("Cannot convert the topic name='"
                        + topicName + "' to a valid elasticsearch index name"));
```

### RegExpRedundantEscape
Redundant character escape `\\+` in RegExp
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java
                indexName = indexName.substring(0, indexName.length() - 1);
            }
            if (indexName.length() <= 0 || !indexName.matches("[a-zA-Z\\.0-9][a-zA-Z_\\.\\-\\+0-9]*")) {
                throw new RuntimeException(new IOException("Cannot convert the topic name='"
                        + topicName + "' to a valid elasticsearch index name"));
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java

        Map<String, Map<String, String>> redirects = new HashMap<>();
        Pattern redirectPattern = Pattern.compile("^httpReverseProxy\\.([^\\.]*)\\.(.+)$");
        Map<String, List<Matcher>> groups = properties.stringPropertyNames().stream()
            .map((s) -> redirectPattern.matcher(s))
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/KerberosName.java`
#### Snippet
```java
     */
    private static final Pattern ruleParser =
        Pattern.compile("\\s*((DEFAULT)|(RULE:\\[(\\d*):([^\\]]*)](\\(([^)]*)\\))?"
            + "(s/([^/]*)/([^/]*)/(g)?)?))");

```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java

    private static String toValidPodName(String ori) {
        return ori.toLowerCase().replaceAll("[^a-z0-9-\\.]", "-");
    }

```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java

    private static String toValidLabelName(String ori) {
        return left(ori.toLowerCase().replaceAll("[^a-zA-Z0-9-_\\.]", "-").replaceAll("^[^a-zA-Z0-9]", "0")
                .replaceAll("[^a-zA-Z0-9]$", "0"), maxLabelSize);
    }
```

## RuleId[ruleID=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `ackFutures.keySet()` may be replaced with 'entrySet()' iteration
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TransactionsBase.java`
#### Snippet
```java
                Map<String, TransactionInBufferStats> producedPartitions = new HashMap<>();

                for (String topic : ackFutures.keySet()) {
                    Map<String, TransactionInPendingAckStats> subs = new HashMap<>();
                    for (String sub : ackFutures.get(topic).keySet()) {
```

### KeySetIterationMayUseEntrySet
Iteration over `produceFutures.keySet()` may be replaced with 'entrySet()' iteration
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TransactionsBase.java`
#### Snippet
```java
                }

                for (String topic : produceFutures.keySet()) {
                    try {
                        producedPartitions.put(topic, produceFutures.get(topic).get());
```

### KeySetIterationMayUseEntrySet
Iteration over `stats.subscriptions.keySet()` may be replaced with 'entrySet()' iteration
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/TopicStatsImpl.java`
#### Snippet
```java

        if (this.subscriptions.size() != stats.subscriptions.size()) {
            for (String subscription : stats.subscriptions.keySet()) {
                SubscriptionStatsImpl subscriptionStats = new SubscriptionStatsImpl();
                this.subscriptions.put(subscription, subscriptionStats.add(stats.subscriptions.get(subscription)));
```

### KeySetIterationMayUseEntrySet
Iteration over `stats.subscriptions.keySet()` may be replaced with 'entrySet()' iteration
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/TopicStatsImpl.java`
#### Snippet
```java
            }
        } else {
            for (String subscription : stats.subscriptions.keySet()) {
                if (this.subscriptions.get(subscription) != null) {
                    this.subscriptions.get(subscription).add(stats.subscriptions.get(subscription));
```

### KeySetIterationMayUseEntrySet
Iteration over `stats.replication.keySet()` may be replaced with 'entrySet()' iteration
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/TopicStatsImpl.java`
#### Snippet
```java
        }
        if (this.replication.size() != stats.replication.size()) {
            for (String repl : stats.replication.keySet()) {
                ReplicatorStatsImpl replStats = new ReplicatorStatsImpl();
                this.replication.put(repl, replStats.add(stats.replication.get(repl)));
```

### KeySetIterationMayUseEntrySet
Iteration over `stats.replication.keySet()` may be replaced with 'entrySet()' iteration
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/TopicStatsImpl.java`
#### Snippet
```java
            }
        } else {
            for (String repl : stats.replication.keySet()) {
                if (this.replication.get(repl) != null) {
                    this.replication.get(repl).add(stats.replication.get(repl));
```

### KeySetIterationMayUseEntrySet
Iteration over `producersMap.keySet()` may be replaced with 'values()' iteration
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
                long totalSent = 0;
                while (true) {
                    for (String topic : producersMap.keySet()) {
                        if (arguments.testTime > 0 && System.nanoTime() > testEndTime) {
                            log.info("------------- DONE (reached the maximum duration: [{} seconds] of production) "
```

### KeySetIterationMayUseEntrySet
Iteration over `allBookies` may be replaced with 'entrySet()' iteration
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
                    secondaryIsolationGroup = isolationGroups.getRight();
                }
                for (String group : allBookies) {
                    Set<String> bookiesInGroup = allGroupsBookieMapping.get(group).keySet();
                    if (!primaryIsolationGroup.contains(group)) {
```

## RuleId[ruleID=UnnecessaryQualifierForThis]
### UnnecessaryQualifierForThis
Qualifier `PulsarOffsetBackingStore` on 'this' is unnecessary in this context
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarOffsetBackingStore.java`
#### Snippet
```java
                outstandingReadToEnd = future;
                future.whenComplete((result, cause) -> {
                    synchronized (PulsarOffsetBackingStore.this) {
                        outstandingReadToEnd = null;
                    }
```

### UnnecessaryQualifierForThis
Qualifier `PulsarMockLedgerHandle` on 'this' is unnecessary in this context
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java
                        fenced = true;
                        cb.addComplete(PulsarMockBookKeeper.getExceptionCode(exception),
                                       PulsarMockLedgerHandle.this, LedgerHandle.INVALID_ENTRY_ID, ctx);
                    } else {
                        cb.addComplete(BKException.Code.OK, PulsarMockLedgerHandle.this, entryId, ctx);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarMockLedgerHandle` on 'this' is unnecessary in this context
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java
                                       PulsarMockLedgerHandle.this, LedgerHandle.INVALID_ENTRY_ID, ctx);
                    } else {
                        cb.addComplete(BKException.Code.OK, PulsarMockLedgerHandle.this, entryId, ctx);
                    }
                }, bk.executor);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarMockLedgerHandle` on 'this' is unnecessary in this context
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java
                    if (exception != null) {
                        cb.readComplete(PulsarMockBookKeeper.getExceptionCode(exception),
                                PulsarMockLedgerHandle.this, null, ctx);
                    } else {
                        cb.readComplete(BKException.Code.OK, PulsarMockLedgerHandle.this, res, ctx);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarMockLedgerHandle` on 'this' is unnecessary in this context
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java
                                PulsarMockLedgerHandle.this, null, ctx);
                    } else {
                        cb.readComplete(BKException.Code.OK, PulsarMockLedgerHandle.this, res, ctx);
                    }
                }, bk.executor);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarMockBookKeeper` on 'this' is unnecessary in this context
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
                    log.info("Creating ledger {}", id);
                    PulsarMockLedgerHandle lh =
                            new PulsarMockLedgerHandle(PulsarMockBookKeeper.this, id, digestType, passwd);
                    ledgers.put(id, lh);
                    return FutureUtils.value(lh);
```

### UnnecessaryQualifierForThis
Qualifier `MockZooKeeper` on 'this' is unnecessary in this context
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java

        executor.execute(() -> {
            synchronized (MockZooKeeper.this) {
                persistentWatchers.add(new PersistentWatcher(basePath, watcher, mode));
            }
```

### UnnecessaryQualifierForThis
Qualifier `WebServer` on 'this' is unnecessary in this context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/WebServer.java`
#### Snippet
```java
                .filter(c -> c instanceof ServerConnector)
                .findFirst().ifPresent(c -> {
                        WebServer.this.externalServicePort = ((ServerConnector) c).getPort();
                    });

```

### UnnecessaryQualifierForThis
Qualifier `TransactionMetadataStoreService` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
                            TransactionTimeoutTracker timeoutTracker = timeoutTrackerFactory.newTracker(tcId);
                            TransactionRecoverTracker recoverTracker =
                                    new TransactionRecoverTrackerImpl(TransactionMetadataStoreService.this,
                                    timeoutTracker, tcId.getId());
                            return transactionMetadataStoreProvider
```

### UnnecessaryQualifierForThis
Qualifier `PulsarService` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java

    public Supplier<NamespaceService> getNamespaceServiceProvider() throws PulsarServerException {
        return () -> new NamespaceService(PulsarService.this);
    }

```

### UnnecessaryQualifierForThis
Qualifier `PersistentStickyKeyDispatcherMultipleConsumers` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
        // from the delete operation that was completed
        topic.getBrokerService().getTopicOrderedExecutor().execute(safeRun(() -> {
            synchronized (PersistentStickyKeyDispatcherMultipleConsumers.this) {
                if (recentlyJoinedConsumers != null && !recentlyJoinedConsumers.isEmpty()
                        && removeConsumersFromRecentJoinedConsumers()) {
```

### UnnecessaryQualifierForThis
Qualifier `PersistentStreamingDispatcherSingleActiveConsumer` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        topic.getBrokerService().executor().schedule(() -> {
            topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
                synchronized (PersistentStreamingDispatcherSingleActiveConsumer.this) {
                    Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);
                    if (currentConsumer != null && !havePendingRead) {
```

### UnnecessaryQualifierForThis
Qualifier `PersistentDispatcherSingleActiveConsumer` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            // Jump again into dispatcher dedicated thread
            topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
                synchronized (PersistentDispatcherSingleActiveConsumer.this) {
                    Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);
                    // we should retry the read if we have an active consumer and there is no pending read
```

### UnnecessaryQualifierForThis
Qualifier `PersistentDispatcherSingleActiveConsumer` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                    topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName,
                        SafeRun.safeRun(() -> {
                            synchronized (PersistentDispatcherSingleActiveConsumer.this) {
                                Consumer newConsumer = getActiveConsumer();
                                readMoreEntries(newConsumer);
```

### UnnecessaryQualifierForThis
Qualifier `PersistentStreamingDispatcherMultipleConsumers` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
        topic.getBrokerService().executor().schedule(() -> {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topic.getName(), SafeRun.safeRun(() -> {
                synchronized (PersistentStreamingDispatcherMultipleConsumers.this) {
                    if (!havePendingRead) {
                        log.info("[{}] Scheduling read operation", name);
```

### UnnecessaryQualifierForThis
Qualifier `StreamingEntryReader` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
            // Don't block caller thread, complete cancel read with dispatcher dedicated thread.
            topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topic.getName(), SafeRun.safeRun(() -> {
                synchronized (StreamingEntryReader.this) {
                    if (STATE_UPDATER.compareAndSet(this, State.Canceling, State.Canceled)) {
                        internalCancelReadRequests();
```

### UnnecessaryQualifierForThis
Qualifier `ServerCnx` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                             CompletableFuture<Producer> producerFuture){
        CompletableFuture<Void> producerQueuedFuture = new CompletableFuture<>();
        Producer producer = new Producer(topic, ServerCnx.this, producerId, producerName,
                getPrincipal(), isEncrypted, metadata, schemaVersion, epoch,
                userProvidedProducerName, producerAccessMode, topicEpoch, supportsPartialProducer);
```

### UnnecessaryQualifierForThis
Qualifier `ServerCnx` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                            }

                            SubscriptionOption option = SubscriptionOption.builder().cnx(ServerCnx.this)
                                    .subscriptionName(subscriptionName)
                                    .consumerId(consumerId).subType(subType).priorityLevel(priorityLevel)
```

### UnnecessaryQualifierForThis
Qualifier `PersistentDispatcherMultipleConsumers` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

        topic.getBrokerService().executor().schedule(() -> {
            synchronized (PersistentDispatcherMultipleConsumers.this) {
                // If it's a replay read we need to retry even if there's already
                // another scheduled read, otherwise we'd be stuck until
```

### UnnecessaryQualifierForThis
Qualifier `TopicPoliciesSystemTopicClient` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TopicPoliciesSystemTopicClient.java`
#### Snippet
```java
                        log.debug("[{}] A new reader is created", topicName);
                    }
                    return new TopicPolicyReader(reader, TopicPoliciesSystemTopicClient.this);
                });
    }
```

### UnnecessaryQualifierForThis
Qualifier `TopicPoliciesSystemTopicClient` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TopicPoliciesSystemTopicClient.java`
#### Snippet
```java
                        log.debug("[{}] A new writer is created", topicName);
                    }
                    return new TopicPolicyWriter(producer, TopicPoliciesSystemTopicClient.this);
                });
    }
```

### UnnecessaryQualifierForThis
Qualifier `TopicPolicyWriter` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TopicPoliciesSystemTopicClient.java`
#### Snippet
```java
        public CompletableFuture<Void> closeAsync() {
            return producer.closeAsync().whenComplete((r, ex) -> {
                systemTopicClient.getWriters().remove(TopicPolicyWriter.this);
            });
        }
```

### UnnecessaryQualifierForThis
Qualifier `TopicPolicyReader` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TopicPoliciesSystemTopicClient.java`
#### Snippet
```java
        public CompletableFuture<Void> closeAsync() {
            return reader.closeAsync().whenComplete((r, ex) -> {
                systemTopic.getReaders().remove(TopicPolicyReader.this);
            });
        }
```

### UnnecessaryQualifierForThis
Qualifier `PersistentSubscription` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    public CompletableFuture<Void> addConsumer(Consumer consumer) {
        return pendingAckHandle.pendingAckHandleFuture().thenCompose(future -> {
            synchronized (PersistentSubscription.this) {
                cursor.updateLastActive();
                if (IS_FENCED_UPDATER.get(this) == TRUE) {
```

### UnnecessaryQualifierForThis
Qualifier `PersistentSubscription` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java

    private void resetCursor(Position finalPosition, CompletableFuture<Void> future) {
        if (!IS_FENCED_UPDATER.compareAndSet(PersistentSubscription.this, FALSE, TRUE)) {
            future.completeExceptionally(new SubscriptionBusyException("Failed to fence subscription"));
            return;
```

### UnnecessaryQualifierForThis
Qualifier `PersistentSubscription` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
            if (throwable != null) {
                log.error("[{}][{}] Failed to disconnect consumer from subscription", topicName, subName, throwable);
                IS_FENCED_UPDATER.set(PersistentSubscription.this, FALSE);
                future.completeExceptionally(
                        new SubscriptionBusyException("Failed to disconnect consumers from subscription"));
```

### UnnecessaryQualifierForThis
Qualifier `PersistentSubscription` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
            } catch (Exception e) {
                log.error("[{}][{}] Error while resetting cursor", topicName, subName, e);
                IS_FENCED_UPDATER.set(PersistentSubscription.this, FALSE);
                future.completeExceptionally(new BrokerServiceException(e));
            }
```

### UnnecessaryQualifierForThis
Qualifier `NonPersistentTopic` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
                    replicators.computeIfAbsent(remoteCluster, r -> {
                        try {
                            return new NonPersistentReplicator(NonPersistentTopic.this, localCluster,
                                    remoteCluster, brokerService, (PulsarClientImpl) replicationClient);
                        } catch (PulsarServerException e) {
```

### UnnecessaryQualifierForThis
Qualifier `NonPersistentTopic` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
                            // deadlock. so, execute it in different thread
                            brokerService.executor().execute(() -> {
                                brokerService.removeTopicFromCache(NonPersistentTopic.this);
                                unregisterTopicPolicyListener();
                                log.info("[{}] Topic deleted", topic);
```

### UnnecessaryQualifierForThis
Qualifier `NonPersistentTopic` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
            // so, execute it in different thread
            brokerService.executor().execute(() -> {
                brokerService.removeTopicFromCache(NonPersistentTopic.this);
                unregisterTopicPolicyListener();
                closeFuture.complete(null);
```

### UnnecessaryQualifierForThis
Qualifier `NonPersistentTopic` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
        log.info("[{}] Starting replicator to remote: {}", topic, remoteCluster);
        String localCluster = brokerService.pulsar().getConfiguration().getClusterName();
        return addReplicationCluster(remoteCluster, NonPersistentTopic.this, localCluster);
    }

```

### UnnecessaryQualifierForThis
Qualifier `ModularLoadManagerImpl` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java

            try {
                scheduler.execute(ModularLoadManagerImpl.this::updateAll);
            } catch (RejectedExecutionException e) {
                // Executor is shutting down
```

### UnnecessaryQualifierForThis
Qualifier `TopicTransactionBufferRecover` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
                PositionImpl currentLoadPosition = (PositionImpl) this.startReadCursorPosition;
                FillEntryQueueCallback fillEntryQueueCallback = new FillEntryQueueCallback(entryQueue,
                        managedCursor, TopicTransactionBufferRecover.this);
                if (lastConfirmedEntry.getEntryId() != -1) {
                    while (lastConfirmedEntry.compareTo(currentLoadPosition) > 0
```

### UnnecessaryQualifierForThis
Qualifier `TopicTransactionBuffer` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
    public void run(Timeout timeout) {
        if (checkIfReady()) {
            synchronized (TopicTransactionBuffer.this) {
                takeSnapshotByTimeout();
            }
```

### UnnecessaryQualifierForThis
Qualifier `TopicTransactionBuffer` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
            this.snapshotAbortedTxnProcessor.takeAbortedTxnsSnapshot(this.maxReadPosition);
        }
        this.timer.newTimeout(TopicTransactionBuffer.this,
                takeSnapshotIntervalTime, TimeUnit.MILLISECONDS);
    }
```

### UnnecessaryQualifierForThis
Qualifier `TopicTransactionBuffer` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
        // when ongoing transaction is empty, proved that lastAddConfirm is can read max position, because callback
        // thread is the same tread, in this time the lastAddConfirm don't content transaction message.
        synchronized (TopicTransactionBuffer.this) {
            if (checkIfNoSnapshot()) {
                this.maxReadPosition = position;
```

### UnnecessaryQualifierForThis
Qualifier `TopicTransactionBuffer` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
                    snapshotAbortedTxnProcessor.takeAbortedTxnsSnapshot(maxReadPosition).thenRun(() -> {
                        if (changeToReadyStateFromNoSnapshot()) {
                            timer.newTimeout(TopicTransactionBuffer.this,
                                    takeSnapshotIntervalTime, TimeUnit.MILLISECONDS);
                        }
```

### UnnecessaryQualifierForThis
Qualifier `TransactionBufferHandlerImpl` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TransactionBufferHandlerImpl.java`
#### Snippet
```java
            if (ex == null) {
                if (clientCnx.ctx().channel().isActive()) {
                    clientCnx.registerTransactionBufferHandler(TransactionBufferHandlerImpl.this);
                    outstandingRequests.put(op.requestId, op);
                    timer.newTimeout(timeout -> {
```

### UnnecessaryQualifierForThis
Qualifier `PersistentTopic` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    protected CompletableFuture<Void> addReplicationCluster(String remoteCluster, ManagedCursor cursor,
            String localCluster) {
        return AbstractReplicator.validatePartitionedTopicAsync(PersistentTopic.this.getName(), brokerService)
                .thenCompose(__ -> brokerService.pulsar().getPulsarResources().getClusterResources()
                        .getClusterAsync(remoteCluster)
```

### UnnecessaryQualifierForThis
Qualifier `PersistentTopic` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                    Replicator replicator = replicators.computeIfAbsent(remoteCluster, r -> {
                        try {
                            return new GeoPersistentReplicator(PersistentTopic.this, cursor, localCluster,
                                    remoteCluster, brokerService, (PulsarClientImpl) replicationClient);
                        } catch (PulsarServerException e) {
```

### UnnecessaryQualifierForThis
Qualifier `PersistentTopic` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    protected CompletableFuture<Void> addShadowReplicationCluster(String shadowTopic, ManagedCursor cursor) {
        String localCluster = brokerService.pulsar().getConfiguration().getClusterName();
        return AbstractReplicator.validatePartitionedTopicAsync(PersistentTopic.this.getName(), brokerService)
                .thenCompose(__ -> brokerService.pulsar().getPulsarResources().getClusterResources()
                        .getClusterAsync(localCluster)
```

### UnnecessaryQualifierForThis
Qualifier `PersistentTopic` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                    Replicator replicator = shadowReplicators.computeIfAbsent(shadowTopic, r -> {
                        try {
                            return new ShadowReplicator(shadowTopic, PersistentTopic.this, cursor, brokerService,
                                    (PulsarClientImpl) replicationClient);
                        } catch (PulsarServerException e) {
```

### UnnecessaryQualifierForThis
Qualifier `PendingAckHandleDisabled` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleDisabled.java`
#### Snippet
```java

    private final CompletableFuture<PendingAckHandle> pendingAckHandleCompletableFuture =
            CompletableFuture.completedFuture(PendingAckHandleDisabled.this);

    @Override
```

### UnnecessaryQualifierForThis
Qualifier `BrokerService` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            }
        }
        return topicClazz == NonPersistentTopic.class ? (T) new NonPersistentTopic(topic, BrokerService.this)
                : (T) new PersistentTopic(topic, ledger, brokerService);
    }
```

### UnnecessaryQualifierForThis
Qualifier `TopicListWatcher` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TopicListWatcher.java`
#### Snippet
```java

                    .thenAccept(response -> {
                        synchronized (TopicListWatcher.this) {
                            if (!changeToReadyState()) {
                                // Watcher was closed while reconnecting, close the connection to make sure the broker
```

### UnnecessaryQualifierForThis
Qualifier `TopicListWatcher` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TopicListWatcher.java`
#### Snippet
```java
                                && PulsarClientException.isRetriableError(e.getCause())
                                && System.currentTimeMillis()
                                    < CREATE_WATCHER_DEADLINE_UPDATER.get(TopicListWatcher.this)) {
                            reconnectLater(e.getCause());
                        } else if (!watcherFuture.isDone()) {
```

### UnnecessaryQualifierForThis
Qualifier `PendingAckHandleImpl` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
    public synchronized void completeHandleFuture() {
        if (!this.pendingAckHandleCompletableFuture.isDone()) {
            this.pendingAckHandleCompletableFuture.complete(PendingAckHandleImpl.this);
        }
        if (recoverTime.getRecoverStartTime() == 0L) {
```

### UnnecessaryQualifierForThis
Qualifier `PendingAckHandleImpl` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
            pendingAckStoreFuture.thenAccept(pendingAckStore ->
                    pendingAckStore.appendCommitMark(txnID, AckType.Individual).thenAccept(v -> {
                        synchronized (PendingAckHandleImpl.this) {
                            if (individualAckOfTransaction != null && individualAckOfTransaction.containsKey(txnID)) {
                                HashMap<PositionImpl, PositionImpl> pendingAckMessageForCurrentTxn =
```

### UnnecessaryQualifierForThis
Qualifier `PendingAckHandleImpl` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
            pendingAckStoreFuture.thenAccept(pendingAckStore ->
                    pendingAckStore.appendAbortMark(txnId, AckType.Individual).thenAccept(v -> {
                        synchronized (PendingAckHandleImpl.this) {
                            HashMap<PositionImpl, PositionImpl> pendingAckMessageForCurrentTxn =
                                    individualAckOfTransaction.get(txnId);
```

### UnnecessaryQualifierForThis
Qualifier `PendingAckHandleImpl` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
    public CompletableFuture<Void> close() {
        changeToCloseState();
        synchronized (PendingAckHandleImpl.this) {
            if (this.pendingAckStoreFuture != null) {
                return this.pendingAckStoreFuture.thenAccept(PendingAckStore::closeAsync);
```

### UnnecessaryQualifierForThis
Qualifier `org.apache.pulsar.broker.transaction.pendingack.impl.PendingAckHandleImpl` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        this.pendingAckStoreFuture.thenAccept(pendingAckStore ->
                pendingAckStore.appendIndividualAck(txnID, positions).thenAccept(v -> {
                    synchronized (org.apache.pulsar.broker.transaction.pendingack.impl.PendingAckHandleImpl.this) {
                        for (MutablePair<PositionImpl, Integer> positionIntegerMutablePair : positions) {

```

### UnnecessaryQualifierForThis
Qualifier `PendingAckHandleImpl` on 'this' is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
                    }
                }).exceptionally(e -> {
                    synchronized (PendingAckHandleImpl.this) {
                        // we also modify the in memory state when append fail,
                        // because we don't know the persistent state, when were replay it,
```

### UnnecessaryQualifierForThis
Qualifier `ClientCnx` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java
        if (rejectedRequests == 0) {
            // schedule timer
            eventLoopGroup.schedule(() -> NUMBER_OF_REJECTED_REQUESTS_UPDATER.set(ClientCnx.this, 0),
                                    rejectedRequestResetTimeSec, TimeUnit.SECONDS);
        } else if (rejectedRequests >= maxNumberOfRejectedRequestPerConnection) {
```

### UnnecessaryQualifierForThis
Qualifier `ClientCnx` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java
        } else if (rejectedRequests >= maxNumberOfRejectedRequestPerConnection) {
            log.error("{} Close connection because received {} rejected request in {} seconds ", ctx.channel(),
                      NUMBER_OF_REJECTED_REQUESTS_UPDATER.get(ClientCnx.this), rejectedRequestResetTimeSec);
            ctx.close();
        }
```

### UnnecessaryQualifierForThis
Qualifier `PersistentAcknowledgmentsGroupingTracker` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
            // client cnx don't support ack receipt, if we don't complete the future, the client will block.
            if (ackReceiptEnabled) {
                synchronized (PersistentAcknowledgmentsGroupingTracker.this) {
                    if (!this.currentCumulativeAckFuture.isDone()) {
                        this.currentCumulativeAckFuture.complete(null);
```

### UnnecessaryQualifierForThis
Qualifier `PatternMultiTopicsConsumerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PatternMultiTopicsConsumerImpl.java`
#### Snippet
```java
                // schedule the next re-check task
                this.recheckPatternTimeout = client.timer()
                        .newTimeout(PatternMultiTopicsConsumerImpl.this,
                        Math.max(1, conf.getPatternAutoDiscoveryPeriod()), TimeUnit.SECONDS);
            });
```

### UnnecessaryQualifierForThis
Qualifier `PartitionedProducerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
                    setState(State.Ready);
                    log.info("[{}] Created partitioned producer", topic);
                    producerCreatedFuture().complete(PartitionedProducerImpl.this);
                } else {
                    closeRunnable.run();
```

### UnnecessaryQualifierForThis
Qualifier `ConsumerBase` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
            receivedConsumer.increaseAvailablePermits((MessageImpl<?>) (msg instanceof TopicMessageImpl
                                ? ((TopicMessageImpl<T>) msg).getMessage() : msg));
            listener.received(ConsumerBase.this, msg);
        } catch (Throwable t) {
            log.error("[{}][{}] Message listener error in processing message: {}", topic, subscription,
```

### UnnecessaryQualifierForThis
Qualifier `ZeroQueueConsumerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ZeroQueueConsumerImpl.java`
#### Snippet
```java
                waitingOnListenerForZeroQueueSize = true;
                trackMessage(message);
                listener.received(ZeroQueueConsumerImpl.this, beforeConsume(message));
            } catch (Throwable t) {
                log.error("[{}][{}] Message listener error in processing unqueued message: {}", topic, subscription,
```

### UnnecessaryQualifierForThis
Qualifier `PulsarClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
        CompletableFuture<Consumer<T>> consumerSubscribedFuture = new CompletableFuture<>();

        ConsumerBase<T> consumer = new MultiTopicsConsumerImpl<>(PulsarClientImpl.this, conf,
                externalExecutorProvider, consumerSubscribedFuture, schema, interceptors,
                true /* createTopicIfDoesNotExist */);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                ConsumerBase<T> consumer = new PatternMultiTopicsConsumerImpl<>(conf.getTopicsPattern(),
                        getTopicsResult.getTopicsHash(),
                        PulsarClientImpl.this,
                        conf,
                        externalExecutorProvider,
```

### UnnecessaryQualifierForThis
Qualifier `PulsarClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
            ConsumerBase<T> consumer;
            if (metadata.partitions > 0) {
                consumer = MultiTopicsConsumerImpl.createPartitionedConsumer(PulsarClientImpl.this, conf,
                        externalExecutorProvider, consumerSubscribedFuture, metadata.partitions, schema, interceptors);
            } else {
```

### UnnecessaryQualifierForThis
Qualifier `PulsarClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
            } else {
                int partitionIndex = TopicName.getPartitionIndex(topic);
                consumer = ConsumerImpl.newConsumerImpl(PulsarClientImpl.this, topic, conf, externalExecutorProvider,
                        partitionIndex, false, consumerSubscribedFuture, null, schema, interceptors,
                        true /* createTopicIfDoesNotExist */);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                                                  CompletableFuture<Producer<T>> producerCreatedFuture,
                                                  Optional<String> overrideProducerName) {
        return new ProducerImpl<>(PulsarClientImpl.this, topic, conf, producerCreatedFuture, partitionIndex, schema,
                interceptors, overrideProducerName);
    }
```

### UnnecessaryQualifierForThis
Qualifier `PulsarClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                                                                                producerCreatedFuture,
                                                                        PartitionedTopicMetadata metadata) {
        return new PartitionedProducerImpl<>(PulsarClientImpl.this, topic, conf, metadata.partitions,
                producerCreatedFuture, schema, interceptors);
    }
```

### UnnecessaryQualifierForThis
Qualifier `PulsarClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
            ConsumerBase<T> consumer;
            if (metadata.partitions > 0) {
                reader = new MultiTopicsReaderImpl<>(PulsarClientImpl.this,
                        conf, externalExecutorProvider, consumerSubscribedFuture, schema);
                consumer = ((MultiTopicsReaderImpl<T>) reader).getMultiTopicsConsumer();
```

### UnnecessaryQualifierForThis
Qualifier `PulsarClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                consumer = ((MultiTopicsReaderImpl<T>) reader).getMultiTopicsConsumer();
            } else {
                reader = new ReaderImpl<>(PulsarClientImpl.this, conf, externalExecutorProvider,
                        consumerSubscribedFuture, schema);
                consumer = ((ReaderImpl<T>) reader).getConsumer();
```

### UnnecessaryQualifierForThis
Qualifier `ProducerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            // race condition since we also write the message on the socket from this thread
            cnx.ctx().channel().eventLoop().execute(() -> {
                synchronized (ProducerImpl.this) {
                    failPendingMessages(null, ex);
                }
```

### UnnecessaryQualifierForThis
Qualifier `ProducerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            }
            cnx.ctx().channel().eventLoop().execute(() -> {
                synchronized (ProducerImpl.this) {
                    recoverProcessOpSendMsgFrom(cnx, msg, expectedCnxEpoch);
                }
```

### UnnecessaryQualifierForThis
Qualifier `ProducerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                    // We are now reconnected to broker and clear to send messages. Re-send all pending messages and
                    // set the cnx pointer so that new messages will be sent immediately
                    synchronized (ProducerImpl.this) {
                        if (getState() == State.Closing || getState() == State.Closed) {
                            // Producer was closed while reconnecting, close the connection to make sure the broker
```

### UnnecessaryQualifierForThis
Qualifier `ProducerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                    } else if (producerCreatedFuture.isDone() || //
                               (cause instanceof PulsarClientException && PulsarClientException.isRetriableError(cause)
                                && System.currentTimeMillis() < PRODUCER_DEADLINE_UPDATER.get(ProducerImpl.this))) {
                        // Either we had already created the producer once (producerCreatedFuture.isDone()) or we are
                        // still within the initial timeout budget and we are dealing with a retriable error
```

### UnnecessaryQualifierForThis
Qualifier `ProducerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                    }
                    if (changeToReadyState()) {
                        producerCreatedFuture.complete(ProducerImpl.this);
                        scheduleBatchFlushTask(0);
                        return;
```

### UnnecessaryQualifierForThis
Qualifier `ProducerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Void> flushAsync() {
        synchronized (ProducerImpl.this) {
            if (isBatchMessagingEnabled()) {
                batchMessageAndSend(false);
```

### UnnecessaryQualifierForThis
Qualifier `ProducerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    protected void triggerFlush() {
        if (isBatchMessagingEnabled()) {
            synchronized (ProducerImpl.this) {
                batchMessageAndSend(false);
            }
```

### UnnecessaryQualifierForThis
Qualifier `TransactionImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionImpl.java`
#### Snippet
```java
        CompletableFuture<Void> completableFuture = new CompletableFuture<>();
        if (checkIfOpen(completableFuture)) {
            synchronized (TransactionImpl.this) {
                // we need to issue the request to TC to register the acked topic
                return registerSubscriptionMap.compute(Pair.of(topic, subscription), (key, future) -> {
```

### UnnecessaryQualifierForThis
Qualifier `TransactionImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionImpl.java`
#### Snippet
```java
        CompletableFuture<Void> completableFuture = new CompletableFuture<>();
        if (checkIfOpen(completableFuture)) {
            synchronized (TransactionImpl.this) {
                // we need to issue the request to TC to register the produced topic
                return registerPartitionMap.compute(topic, (key, future) -> {
```

### UnnecessaryQualifierForThis
Qualifier `TransactionCoordinatorClientImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionCoordinatorClientImpl.java`
#### Snippet
```java
                    }

                    STATE_UPDATER.set(TransactionCoordinatorClientImpl.this, State.READY);

                    return FutureUtil.waitForAll(connectFutureList);
```

### UnnecessaryQualifierForThis
Qualifier `ConsumerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        // Second : we should synchronized `ClientCnx cnx = cnx()` to
        // prevent use old cnx to send redeliverUnacknowledgedMessages to a old broker
        synchronized (ConsumerImpl.this) {
            ClientCnx cnx = cnx();
            // V1 don't support redeliverUnacknowledgedMessages
```

### UnnecessaryQualifierForThis
Qualifier `ConsumerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

            cnx.sendRequestWithId(request, requestId).thenRun(() -> {
                synchronized (ConsumerImpl.this) {
                    if (changeToReadyState()) {
                        consumerIsReconnectedToBroker(cnx, currentSize);
```

### UnnecessaryQualifierForThis
Qualifier `ConsumerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                if (e.getCause() instanceof PulsarClientException
                        && PulsarClientException.isRetriableError(e.getCause())
                        && System.currentTimeMillis() < SUBSCRIBE_DEADLINE_UPDATER.get(ConsumerImpl.this)) {
                    reconnectLater(e.getCause());
                } else if (!subscribeFuture.isDone()) {
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
    public void find() {
        if (remainingEntries.get() <= 0) {
            log.warn("[{}] Scan abort after reading too many entries", OpScan.this.cursor);
            callback.scanComplete(lastSeenPosition, ScanOutcome.ABORTED, OpScan.this.ctx);
            return;
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
        if (remainingEntries.get() <= 0) {
            log.warn("[{}] Scan abort after reading too many entries", OpScan.this.cursor);
            callback.scanComplete(lastSeenPosition, ScanOutcome.ABORTED, OpScan.this.ctx);
            return;
        }
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
        }
        if (System.currentTimeMillis() - startTime > timeOutMs) {
            log.warn("[{}] Scan abort after hitting the deadline", OpScan.this.cursor);
            callback.scanComplete(lastSeenPosition, ScanOutcome.ABORTED, OpScan.this.ctx);
            return;
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
        if (System.currentTimeMillis() - startTime > timeOutMs) {
            log.warn("[{}] Scan abort after hitting the deadline", OpScan.this.cursor);
            callback.scanComplete(lastSeenPosition, ScanOutcome.ABORTED, OpScan.this.ctx);
            return;
        }
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
        if (cursor.hasMoreEntries(searchPosition)) {
            OpReadEntry opReadEntry = OpReadEntry.create(cursor, searchPosition, batchSize,
            this, OpScan.this.ctx, null);
            ledger.asyncReadEntries(opReadEntry);
        } else {
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
            ledger.asyncReadEntries(opReadEntry);
        } else {
            callback.scanComplete(lastSeenPosition, ScanOutcome.COMPLETED, OpScan.this.ctx);
        }
    }
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
                for (Entry entry : entriesFiltered) {
                    if (remainingEntries.decrementAndGet() <= 0) {
                        log.warn("[{}] Scan abort after reading too many entries", OpScan.this.cursor);
                        callback.scanComplete(lastSeenPosition, ScanOutcome.ABORTED, OpScan.this.ctx);
                        return;
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
                    if (remainingEntries.decrementAndGet() <= 0) {
                        log.warn("[{}] Scan abort after reading too many entries", OpScan.this.cursor);
                        callback.scanComplete(lastSeenPosition, ScanOutcome.ABORTED, OpScan.this.ctx);
                        return;
                    }
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
                    }
                    if (!condition.test(entry)) {
                        log.warn("[{}] Scan abort due to user code", OpScan.this.cursor);
                        callback.scanComplete(lastSeenPosition, ScanOutcome.USER_INTERRUPTED, OpScan.this.ctx);
                        return;
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
                    if (!condition.test(entry)) {
                        log.warn("[{}] Scan abort due to user code", OpScan.this.cursor);
                        callback.scanComplete(lastSeenPosition, ScanOutcome.USER_INTERRUPTED, OpScan.this.ctx);
                        return;
                    }
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
            if (searchPosition.compareTo((PositionImpl) lastPositionForBatch) == 0) {
                // we have reached the end of the ledger, as we are not doing progress
                callback.scanComplete(lastSeenPosition, ScanOutcome.COMPLETED, OpScan.this.ctx);
                return;
            }
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
            log.error("Unhandled error", t);
            callback.scanFailed(ManagedLedgerException.getManagedLedgerException(t),
                    Optional.ofNullable(lastSeenPosition), OpScan.this.ctx);
            return;
        } finally {
```

### UnnecessaryQualifierForThis
Qualifier `OpScan` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java
    @Override
    public void readEntriesFailed(ManagedLedgerException exception, Object ctx) {
        callback.scanFailed(exception, Optional.ofNullable(searchPosition), OpScan.this.ctx);
    }

```

### UnnecessaryQualifierForThis
Qualifier `OpReadEntry` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpReadEntry.java`
#### Snippet
```java
            cursor.ledger.getExecutor().execute(safeRun(() -> {
                readPosition = cursor.ledger.startReadOperationOnLedger(nextReadPosition);
                cursor.ledger.asyncReadEntries(OpReadEntry.this);
            }));
        } else {
```

### UnnecessaryQualifierForThis
Qualifier `OpAddEntry` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpAddEntry.java`
#### Snippet
```java

    void close() {
        STATE_UPDATER.set(OpAddEntry.this, State.CLOSED);
    }

```

### UnnecessaryQualifierForThis
Qualifier `OpAddEntry` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpAddEntry.java`
#### Snippet
```java
    public void addComplete(int rc, final LedgerHandle lh, long entryId, Object ctx) {

        if (!STATE_UPDATER.compareAndSet(OpAddEntry.this, State.INITIATED, State.COMPLETED)) {
            log.warn("[{}] The add op is terminal legacy callback for entry {}-{} adding.", ml.getName(), lh.getId(),
                    entryId);
```

### UnnecessaryQualifierForThis
Qualifier `OpAddEntry` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpAddEntry.java`
#### Snippet
```java
            log.warn("[{}] The add op is terminal legacy callback for entry {}-{} adding.", ml.getName(), lh.getId(),
                    entryId);
            OpAddEntry.this.recycle();
            return;
        }
```

### UnnecessaryQualifierForThis
Qualifier `OpAddEntry` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpAddEntry.java`
#### Snippet
```java

    public void initiate() {
        if (STATE_UPDATER.compareAndSet(OpAddEntry.this, State.OPEN, State.INITIATED)) {

            ByteBuf duplicateBuffer = data.retainedDuplicate();
```

### UnnecessaryQualifierForThis
Qualifier `NonDurableCursorImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/NonDurableCursorImpl.java`
#### Snippet
```java
        lastMarkDeleteEntry = mdEntry;
        // it is important to advance cursor so the retention can kick in as expected.
        ledger.onCursorMarkDeletePositionUpdated(NonDurableCursorImpl.this, mdEntry.newPosition);

        callback.markDeleteComplete(ctx);
```

### UnnecessaryQualifierForThis
Qualifier `OpFindNewest` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java
            ledger.asyncReadEntry(searchPosition, this, null);
        } else {
            callback.findEntryComplete(lastMatchedPosition, OpFindNewest.this.ctx);
        }
    }
```

### UnnecessaryQualifierForThis
Qualifier `OpFindNewest` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java
                // Otherwise, a message before the expiration date will be deleted due to message TTL.
                // cf. https://github.com/apache/pulsar/issues/5579
                callback.findEntryComplete(null, OpFindNewest.this.ctx);
                return;
            } else {
```

### UnnecessaryQualifierForThis
Qualifier `OpFindNewest` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java
        case checkLast:
            if (condition.test(entry)) {
                callback.findEntryComplete(position, OpFindNewest.this.ctx);
                return;
            } else {
```

### UnnecessaryQualifierForThis
Qualifier `OpFindNewest` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java

            if (max <= min) {
                callback.findEntryComplete(lastMatchedPosition, OpFindNewest.this.ctx);
                return;
            }
```

### UnnecessaryQualifierForThis
Qualifier `OpFindNewest` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java
    @Override
    public void readEntryFailed(ManagedLedgerException exception, Object ctx) {
        callback.findEntryFailed(exception, Optional.ofNullable(searchPosition), OpFindNewest.this.ctx);
    }

```

### UnnecessaryQualifierForThis
Qualifier `PendingRead` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
            // on the results of the callback
            handle.whenComplete((___, error) -> {
                synchronized (PendingRead.this) {
                    completed = true;
                    synchronized (ledgerCache) {
```

### UnnecessaryQualifierForThis
Qualifier `PendingRead` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java

            handle.thenAcceptAsync(entriesToReturn -> {
                synchronized (PendingRead.this) {
                    if (callbacks.size() == 1) {
                        ReadEntriesCallbackWithContext first = callbacks.get(0);
```

### UnnecessaryQualifierForThis
Qualifier `PendingRead` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
            }, rangeEntryCache.getManagedLedger().getExecutor()
                    .chooseThread(rangeEntryCache.getManagedLedger().getName())).exceptionally(exception -> {
                synchronized (PendingRead.this) {
                    for (ReadEntriesCallbackWithContext callback : callbacks) {
                        ManagedLedgerException mlException = createManagedLedgerException(exception);
```

### UnnecessaryQualifierForThis
Qualifier `MultiTopicsConsumerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
        if (conf.getTopicNames().isEmpty()) {
            setState(State.Ready);
            subscribeFuture().complete(MultiTopicsConsumerImpl.this);
            return;
        }
```

### UnnecessaryQualifierForThis
Qualifier `MultiTopicsConsumerImpl` on 'this' is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                log.info("[{}] [{}] Created topics consumer with {} sub-consumers",
                    topic, subscription, allTopicPartitionsNumber.get());
                subscribeFuture().complete(MultiTopicsConsumerImpl.this);
            })
            .exceptionally(ex -> {
```

### UnnecessaryQualifierForThis
Qualifier `PulsarZooKeeperClient` on 'super' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        ZooKeeper zkHandle = zk.get();
        if (null == zkHandle) {
            return PulsarZooKeeperClient.super.getState();
        } else {
            return zkHandle.getState();
```

### UnnecessaryQualifierForThis
Qualifier `PulsarZooKeeperClient` on 'super' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        ZooKeeper zkHandle = zk.get();
        if (null == zkHandle) {
            return PulsarZooKeeperClient.super.toString();
        } else {
            return zkHandle.toString();
```

### UnnecessaryQualifierForThis
Qualifier `LockManagerImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LockManagerImpl.java`
#### Snippet
```java
        CompletableFuture<ResourceLock<T>> result = new CompletableFuture<>();
        lock.acquire(value).thenRun(() -> {
            synchronized (LockManagerImpl.this) {
                if (state == State.Ready) {
                    locks.put(path, lock);
```

### UnnecessaryQualifierForThis
Qualifier `LockManagerImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LockManagerImpl.java`
#### Snippet
```java
                    lock.getLockExpiredFuture().thenRun(() -> {
                        log.info("Released resource lock on {}", path);
                        synchronized (LockManagerImpl.this) {
                            locks.remove(path, lock);
                        }
```

### UnnecessaryQualifierForThis
Qualifier `LeaderElectionImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
                            .exceptionally(ex -> {
                                log.warn("Leader election for path {} has failed", path, ex);
                                synchronized (LeaderElectionImpl.this) {
                                    try {
                                        stateChangesListener.accept(leaderElectionState);
```

### UnnecessaryQualifierForThis
Qualifier `LeaderElectionImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
        store.put(path, payload, Optional.of(-1L), EnumSet.of(CreateOption.Ephemeral))
                .thenAccept(stat -> {
                    synchronized (LeaderElectionImpl.this) {
                        if (internalState == InternalState.ElectionInProgress) {
                            // Do a get() in order to force a notification later, if the z-node disappears
```

### UnnecessaryQualifierForThis
Qualifier `LeaderElectionImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
                            cache.get(path)
                                    .thenRun(() -> {
                                        synchronized (LeaderElectionImpl.this) {
                                            log.info("Acquired leadership on {}", path);
                                            internalState = InternalState.LeaderIsPresent;
```

### UnnecessaryQualifierForThis
Qualifier `ResourceLockImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
                    }

                    synchronized (ResourceLockImpl.this) {
                        if (newValue.equals(existingValue)) {
                            // The lock value is still the same, that means that we're the
```

### UnnecessaryQualifierForThis
Qualifier `ResourceLockImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
        }
        revalidateFuture.exceptionally(ex -> {
            synchronized (ResourceLockImpl.this) {
                Throwable realCause = FutureUtil.unwrapCompletionException(ex);
                if (!revalidateAfterReconnection || realCause instanceof BadVersionException
```

### UnnecessaryQualifierForThis
Qualifier `ResourceLockImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
                    // Continue assuming we hold the lock, until we can revalidate it, either
                    // on Reconnected or SessionReestablished events.
                    ResourceLockImpl.this.revalidateAfterReconnection = true;
                    log.warn("Failed to revalidate the lock at {}. Retrying later on reconnection {}", path,
                            realCause.getMessage());
```

### UnnecessaryQualifierForThis
Qualifier `ResourceLockImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
        store.put(path, payload, Optional.of(version), EnumSet.of(CreateOption.Ephemeral))
                .thenAccept(stat -> {
                    synchronized (ResourceLockImpl.this) {
                        state = State.Valid;
                        version = stat.getVersion();
```

### UnnecessaryQualifierForThis
Qualifier `ResourceLockImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
        store.delete(path, Optional.of(version))
                .thenRun(() -> {
                    synchronized (ResourceLockImpl.this) {
                        state = State.Released;
                    }
```

### UnnecessaryQualifierForThis
Qualifier `ResourceLockImpl` on 'this' is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
                    if (ex.getCause() instanceof MetadataStoreException.NotFoundException) {
                        // The lock is not there on release. We can anyway proceed
                        synchronized (ResourceLockImpl.this) {
                            state = State.Released;
                        }
```

### UnnecessaryQualifierForThis
Qualifier `PulsarWorkerService` on 'this' is unnecessary in this context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
        this.dlogUri = dlogUri;
        this.workerStatsManager = new WorkerStatsManager(workerConfig, runAsStandalone);
        this.functions = new FunctionsImpl(() -> PulsarWorkerService.this);
        this.functionsV2 = new FunctionsImplV2(() -> PulsarWorkerService.this);
        this.sinks = new SinksImpl(() -> PulsarWorkerService.this);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarWorkerService` on 'this' is unnecessary in this context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
        this.workerStatsManager = new WorkerStatsManager(workerConfig, runAsStandalone);
        this.functions = new FunctionsImpl(() -> PulsarWorkerService.this);
        this.functionsV2 = new FunctionsImplV2(() -> PulsarWorkerService.this);
        this.sinks = new SinksImpl(() -> PulsarWorkerService.this);
        this.sources = new SourcesImpl(() -> PulsarWorkerService.this);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarWorkerService` on 'this' is unnecessary in this context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
        this.functions = new FunctionsImpl(() -> PulsarWorkerService.this);
        this.functionsV2 = new FunctionsImplV2(() -> PulsarWorkerService.this);
        this.sinks = new SinksImpl(() -> PulsarWorkerService.this);
        this.sources = new SourcesImpl(() -> PulsarWorkerService.this);
        this.workers = new WorkerImpl(() -> PulsarWorkerService.this);
```

### UnnecessaryQualifierForThis
Qualifier `PulsarWorkerService` on 'this' is unnecessary in this context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
        this.functionsV2 = new FunctionsImplV2(() -> PulsarWorkerService.this);
        this.sinks = new SinksImpl(() -> PulsarWorkerService.this);
        this.sources = new SourcesImpl(() -> PulsarWorkerService.this);
        this.workers = new WorkerImpl(() -> PulsarWorkerService.this);
    }
```

### UnnecessaryQualifierForThis
Qualifier `PulsarWorkerService` on 'this' is unnecessary in this context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
        this.sinks = new SinksImpl(() -> PulsarWorkerService.this);
        this.sources = new SourcesImpl(() -> PulsarWorkerService.this);
        this.workers = new WorkerImpl(() -> PulsarWorkerService.this);
    }

```

### UnnecessaryQualifierForThis
Qualifier `RuntimeSpawner` on 'this' is unnecessary in this context
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeSpawner.java`
#### Snippet
```java
            processLivenessCheckTimer = InstanceCache.getInstanceCache().getScheduledExecutorService()
                    .scheduleAtFixedRate(catchingAndLoggingThrowables(() -> {
                        Runtime runtime = RuntimeSpawner.this.runtime;
                        if (runtime != null && !runtime.isAlive()) {
                            log.error("{}/{}/{} Function Container is dead with following exception. Restarting.",
```

### UnnecessaryQualifierForThis
Qualifier `ManagedCursorImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

            readPosition = newReadPosition;
            ledger.onCursorReadPositionUpdated(ManagedCursorImpl.this, newReadPosition);
        } finally {
            lock.writeLock().unlock();
```

### UnnecessaryQualifierForThis
Qualifier `ManagedCursorImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        CompletableFuture<Void> updateCursorPropertiesResult = new CompletableFuture<>();

        final Stat lastCursorLedgerStat = ManagedCursorImpl.this.cursorLedgerStat;

        Map<String, String> newProperties = updateFunction.apply(ManagedCursorImpl.this.cursorProperties);
```

### UnnecessaryQualifierForThis
Qualifier `ManagedCursorImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        final Stat lastCursorLedgerStat = ManagedCursorImpl.this.cursorLedgerStat;

        Map<String, String> newProperties = updateFunction.apply(ManagedCursorImpl.this.cursorProperties);
        ManagedCursorInfo copy = ManagedCursorInfo
                .newBuilder(ManagedCursorImpl.this.managedCursorInfo)
```

### UnnecessaryQualifierForThis
Qualifier `ManagedCursorImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        Map<String, String> newProperties = updateFunction.apply(ManagedCursorImpl.this.cursorProperties);
        ManagedCursorInfo copy = ManagedCursorInfo
                .newBuilder(ManagedCursorImpl.this.managedCursorInfo)
                .clearCursorProperties()
                .addAllCursorProperties(buildStringPropertiesMap(newProperties))
```

### UnnecessaryQualifierForThis
Qualifier `ManagedCursorImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            }
            readPosition = newReadPosition;
            ledger.onCursorReadPositionUpdated(ManagedCursorImpl.this, newReadPosition);
        } finally {
            lock.writeLock().unlock();
```

### UnnecessaryQualifierForThis
Qualifier `ManagedCursorImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                            ledger.getName(), currentReadPosition, newReadPosition, markDeletePosition);
                }
                ledger.onCursorReadPositionUpdated(ManagedCursorImpl.this, newReadPosition);
                return newReadPosition;
            } else {
```

### UnnecessaryQualifierForThis
Qualifier `ManagedCursorImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                // If we've had a write error, the ledger will be automatically closed, we need to create a new one,
                // in the meantime the mark-delete will be queued.
                STATE_UPDATER.compareAndSet(ManagedCursorImpl.this, State.Open, State.NoLedger);

                mbean.persistToLedger(false);
```

### UnnecessaryQualifierForThis
Qualifier `ManagedLedgerImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    @SuppressWarnings("checkstyle:fallthrough")
    private void deleteAllLedgers(DeleteLedgerCallback callback, Object ctx) {
        List<LedgerInfo> ledgers = Lists.newArrayList(ManagedLedgerImpl.this.ledgers.values());
        AtomicInteger ledgersToDelete = new AtomicInteger(ledgers.size());
        if (ledgers.isEmpty()) {
```

### UnnecessaryQualifierForThis
Qualifier `ManagedLedgerImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                            }
                            Optional<Throwable> errorToReport = firstError;
                            synchronized (ManagedLedgerImpl.this) {
                                // if the ledger doesn't exist anymore, ignore the error
                                if (ledgers.containsKey(ledgerId)) {
```

### UnnecessaryQualifierForThis
Qualifier `ManagedLedgerImpl` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        // First remove the consumer form the MetaStore. If this operation succeeds and the next one (removing the
        // ledger from BK) don't, we end up having a loose ledger leaked but the state will be consistent.
        store.asyncRemoveCursor(ManagedLedgerImpl.this.name, consumerName, new MetaStoreCallback<Void>() {
            @Override
            public void operationComplete(Void result, Stat stat) {
```

### UnnecessaryQualifierForThis
Qualifier `ReadEntryCallbackWrapper` on 'this' is unnecessary in this context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        private boolean recycle(long readOpCount) {
            if (readOpCount != -1
                    && READ_OP_COUNT_UPDATER.compareAndSet(ReadEntryCallbackWrapper.this, readOpCount, -1)) {
                createdTime = -1;
                readEntryCallback = null;
```

### UnnecessaryQualifierForThis
Qualifier `LocalRunner` on 'this' is unnecessary in this context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
        shutdownHook = new Thread(() -> {
            try {
                LocalRunner.this.close();
            } catch (Exception exception) {
                log.warn("Encountered exception when closing localrunner", exception);
```

### UnnecessaryQualifierForThis
Qualifier `AuthenticationListState` on 'this' is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderList.java`
#### Snippet
```java
                as -> {
                    AuthData ad = as.authenticate(authData);
                    AuthenticationListState.this.authState = as;
                    return ad;
                }
```

## RuleId[ruleID=ObjectNotify]
### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `pulsar-io/nsq/src/main/java/org/apache/pulsar/io/nsq/NSQSource.java`
#### Snippet
```java
        LOG.info("Source closed");
        synchronized (waitObject) {
            waitObject.notify();
        }
    }
```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHose.java`
#### Snippet
```java
        LOG.info("Source closed");
        synchronized (waitObject) {
            waitObject.notify();
        }
    }
```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/ErrorNotifierImpl.java`
#### Snippet
```java
    public synchronized void triggerError(Throwable th) {
        error.set(th);
        this.notify();
    }

```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/ErrorNotifierImpl.java`
#### Snippet
```java
    public synchronized void close() {
        isRunning = false;
        this.notify();
    }
}
```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
    public synchronized void stop() {
        if (running.compareAndSet(true, false)) {
            this.notify();
            try {
                Runtime.getRuntime().removeShutdownHook(shutdownHook);
```

## RuleId[ruleID=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`flumeConfig.getZkConnString().length() > 0` can be replaced with '!flumeConfig.getZkConnString().isEmpty()'
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/FlumeConnector.java`
#### Snippet
```java
        String agentName = flumeConfig.getName();
        boolean reload = !flumeConfig.getNoReloadConf();
        boolean isZkConfigured = flumeConfig.getZkConnString().length() > 0;
        if (isZkConfigured) {
            // get options
```

### SizeReplaceableByIsEmpty
`eventList.size() > 0` can be replaced with '!eventList.isEmpty()'
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/sink/SourceOfFlume.java`
#### Snippet
```java
                }
            }
            if (eventList.size() > 0) {
                counter.addToEventReceivedCount(eventList.size());
                getChannelProcessor().processEventBatch(eventList);
```

### SizeReplaceableByIsEmpty
`incomingList.size() > 0` can be replaced with '!incomingList.isEmpty()'
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java

    private void flush() {
        if (incomingList.size() > 0 && isFlushing.compareAndSet(false, true)) {
            boolean needAnotherRound;
            final Deque<Record<T>> swapList = new LinkedList<>();
```

### SizeReplaceableByIsEmpty
`docsToInsert.size() > 0` can be replaced with '!docsToInsert.isEmpty()'
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
        }

        if (docsToInsert.size() > 0) {
            collection.insertMany(docsToInsert).subscribe(new DocsToInsertSubscriber(docsToInsert, recordsToInsert));
        }
```

### SizeReplaceableByIsEmpty
`recordsToSet.size() > 0` can be replaced with '!recordsToSet.isEmpty()'
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSink.java`
#### Snippet
```java

        try {
            if (recordsToSet.size() > 0) {
                if (log.isDebugEnabled()) {
                    log.debug("Calling mset with {} values", recordsToSet.size());
```

### SizeReplaceableByIsEmpty
`metadata.size() > 0` can be replaced with '!metadata.isEmpty()'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
            }
            int metadataOffset = -1;
            if (metadata.size() > 0) {
                metadataOffset = EncryptionKey.createMetadataVector(builder, metadataOffsets);
            }
```

### SizeReplaceableByIsEmpty
`result.size() != 0` can be replaced with '!result.isEmpty()'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
            try {
                Map<ByteBuffer, ByteBuffer> result = offsetStore.get(req).get();
                if (result != null && result.size() != 0) {
                    Optional<ByteBuffer> val = result.entrySet().stream()
                            .filter(entry -> entry.getKey().equals(key))
```

### SizeReplaceableByIsEmpty
`schema.getFields().size() > 0` can be replaced with '!schema.getFields().isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroRowDecoderFactory.java`
#### Snippet
```java
                                TypeSignatureParameter.typeParameter(valueType)));
            case RECORD:
                if (schema.getFields().size() > 0) {
                    return RowType.from(schema.getFields().stream()
                            .map(field -> new RowType.Field(Optional.of(field.name()),
```

### SizeReplaceableByIsEmpty
`schema.getFields().size() > 0` can be replaced with '!schema.getFields().isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonRowDecoderFactory.java`
#### Snippet
```java
                        TypeSignatureParameter.typeParameter(valueType)));
            case RECORD:
                if (schema.getFields().size() > 0) {
                    return RowType.from(schema.getFields().stream()
                            .map(field -> new RowType.Field(Optional.of(field.name()),
```

### SizeReplaceableByIsEmpty
`matchedTopics.size() == 0` can be replaced with 'matchedTopics.isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
                .collect(Collectors.toList());

        if (matchedTopics.size() == 0) {
            log.error("Table %s not found", String.format("%s/%s", namespace, schemaTableName.getTableName()));
            throw new TableNotFoundException(schemaTableName);
```

### SizeReplaceableByIsEmpty
`s.length() > 0` can be replaced with '!s.isEmpty()'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/BrokerProxyValidator.java`
#### Snippet
```java

    private static List<String> parseCommaSeparatedConfigValue(String configValue) {
        return Arrays.stream(configValue.split(SEPARATOR)).map(String::trim).filter(s -> s.length() > 0)
                .collect(Collectors.toList());
    }
```

### SizeReplaceableByIsEmpty
`keyColumnHandles.size() > 0` can be replaced with '!keyColumnHandles.isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
                    .collect(toImmutableSet());
            PulsarRowDecoder keyDecoder = null;
            if (keyColumnHandles.size() > 0) {
                keyDecoder = decoderFactory.createRowDecoder(topicName,
                        kvSchemaInfo.getKey(), keyColumnHandles
```

### SizeReplaceableByIsEmpty
`valueColumnHandles.size() > 0` can be replaced with '!valueColumnHandles.isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
                    .collect(toImmutableSet());
            PulsarRowDecoder valueDecoder = null;
            if (valueColumnHandles.size() > 0) {
                valueDecoder = decoderFactory.createRowDecoder(topicName,
                        kvSchemaInfo.getValue(),
```

### SizeReplaceableByIsEmpty
`keyColumnHandles.size() > 0` can be replaced with '!keyColumnHandles.isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java

            Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodedKey;
            if (keyColumnHandles.size() > 0) {
                decodedKey = keyDecoder.decodeRow(keyByteBuf);
                decodedKey.ifPresent(currentRowValuesMap::putAll);
```

### SizeReplaceableByIsEmpty
`valueColumnHandles.size() > 0` can be replaced with '!valueColumnHandles.isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
                decodedKey.ifPresent(currentRowValuesMap::putAll);
            }
            if (valueColumnHandles.size() > 0) {
                Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodedValue =
                        valueDecoder.decodeRow(valueByteBuf);
```

### SizeReplaceableByIsEmpty
`queue.size() == 0` can be replaced with 'queue.isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            return true;
        }
        return cacheSizeAllocator.getAvailableCacheSize() > 0 || queue.size() == 0;
    }

```

### SizeReplaceableByIsEmpty
`activeNamespaces.size() > 0` can be replaced with '!activeNamespaces.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
                    return;
                }
                if (activeNamespaces.size() > 0) {
                    log.warn("{}/{} Active-namespaces {}", tenant, cluster, activeNamespaces);
                    checkNs.completeExceptionally(new RestException(Status.PRECONDITION_FAILED, "Active namespaces"));
```

### SizeReplaceableByIsEmpty
`conf.getBookkeeperClientAuthenticationPlugin().trim().length() > 0` can be replaced with '!conf.getBookkeeperClientAuthenticationPlugin().trim().isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/BookKeeperClientFactoryImpl.java`
#### Snippet
```java
        ClientConfiguration bkConf = new ClientConfiguration();
        if (conf.getBookkeeperClientAuthenticationPlugin() != null
                && conf.getBookkeeperClientAuthenticationPlugin().trim().length() > 0) {
            bkConf.setClientAuthProviderFactoryClass(conf.getBookkeeperClientAuthenticationPlugin());
            bkConf.setProperty(conf.getBookkeeperClientAuthenticationParametersName(),
```

### SizeReplaceableByIsEmpty
`initialBundles.getBoundaries().size() == 0` can be replaced with 'initialBundles.getBoundaries().isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/Namespaces.java`
#### Snippet
```java
            Policies policies = new Policies();
            if (initialBundles != null && initialBundles.getNumBundles() > 0) {
                if (initialBundles.getBoundaries() == null || initialBundles.getBoundaries().size() == 0) {
                    policies.bundles = getBundles(initialBundles.getNumBundles());
                } else {
```

### SizeReplaceableByIsEmpty
`nonexistentClusters.size() > 0` can be replaced with '!nonexistentClusters.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TenantsBase.java`
#### Snippet
```java
                    .filter(cluster -> !(availableClusters.contains(cluster) || GLOBAL_CLUSTER.equals(cluster)))
                    .collect(Collectors.toList());
            if (nonexistentClusters.size() > 0) {
                log.warn("[{}] Failed to validate due to clusters {} do not exist", clientAppId(), nonexistentClusters);
                throw new RestException(Status.PRECONDITION_FAILED, "Clusters do not exist");
```

### SizeReplaceableByIsEmpty
`values.size() > 0` can be replaced with '!values.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/AbstractMetrics.java`
#### Snippet
```java
    protected double sum(List<Double> values) {
        double sum = 0;
        if (values.size() > 0) {
            for (Double value : values) {
                sum += value;
```

### SizeReplaceableByIsEmpty
`values.size() > 0` can be replaced with '!values.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/AbstractMetrics.java`
#### Snippet
```java
    protected double average(List<Double> values) {
        double average = 0;
        if (values.size() > 0) {
            double sum = 0;
            for (Double value : values) {
```

### SizeReplaceableByIsEmpty
`segmentMetadataList.size() == 0` can be replaced with 'segmentMetadataList.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/MutableBucket.java`
#### Snippet
```java

            // Move first segment of bucket snapshot to sharedBucketPriorityQueue
            if (segmentMetadataList.size() == 0) {
                sharedQueue.add(timestamp, ledgerId, entryId);
            }
```

### SizeReplaceableByIsEmpty
`transactionBufferSystemTopicClient.getReaders().size() == 0` can be replaced with 'transactionBufferSystemTopicClient.getReaders().isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicTxnBufferSnapshotService.java`
#### Snippet
```java

    public void removeClient(TopicName topicName, SystemTopicClientBase<T> transactionBufferSystemTopicClient) {
        if (transactionBufferSystemTopicClient.getReaders().size() == 0
                && transactionBufferSystemTopicClient.getWriters().size() == 0) {
            clients.remove(topicName);
```

### SizeReplaceableByIsEmpty
`transactionBufferSystemTopicClient.getWriters().size() == 0` can be replaced with 'transactionBufferSystemTopicClient.getWriters().isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicTxnBufferSnapshotService.java`
#### Snippet
```java
    public void removeClient(TopicName topicName, SystemTopicClientBase<T> transactionBufferSystemTopicClient) {
        if (transactionBufferSystemTopicClient.getReaders().size() == 0
                && transactionBufferSystemTopicClient.getWriters().size() == 0) {
            clients.remove(topicName);
        }
```

### SizeReplaceableByIsEmpty
`rangeMap.size() > 0` can be replaced with '!rangeMap.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/HashRangeExclusiveStickyKeyConsumerSelector.java`
#### Snippet
```java
    @Override
    public Consumer select(int hash) {
        if (rangeMap.size() > 0) {
            int slot = hash % rangeSize;
            Map.Entry<Integer, Consumer> ceilingEntry = rangeMap.ceilingEntry(slot);
```

### SizeReplaceableByIsEmpty
`listeners.size() == 0` can be replaced with 'listeners.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarMetadataEventSynchronizer.java`
#### Snippet
```java
                            listeners.size());
                    try {
                        if (listeners.size() == 0) {
                            c.acknowledgeAsync(msg);
                            return;
```

### SizeReplaceableByIsEmpty
`schemaAndMetadataList.size() == 0` can be replaced with 'schemaAndMetadataList.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
            List<SchemaAndMetadata> schemaAndMetadataList,
            SchemaData schemaData) {
        if (schemaAndMetadataList == null || schemaAndMetadataList.size() == 0) {
            return CompletableFuture.completedFuture(null);
        }
```

### SizeReplaceableByIsEmpty
`schemaAndMetadataList.size() != 0` can be replaced with '!schemaAndMetadataList.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
            }
            CompletableFuture<Void> checkCompatibilityFuture = new CompletableFuture<>();
            if (schemaAndMetadataList.size() != 0) {
                if (isTransitiveStrategy(strategy)) {
                    checkCompatibilityFuture =
```

### SizeReplaceableByIsEmpty
`bitSet.length() > 0` can be replaced with '!bitSet.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
            if (isExcluded) {
                bitSet.clear(0, Math.max(batchIndex + 1, 0));
                if (bitSet.length() > 0) {
                    ackSet = bitSet.toLongArray();
                    seekPosition = PositionImpl.get(messageId.getLedgerId(),
```

### SizeReplaceableByIsEmpty
`issuedReads.size() == 0` can be replaced with 'issuedReads.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
                dispatcher.notifyConsumersEndOfTopic();
                cleanQueue(pendingReads);
                if (issuedReads.size() == 0) {
                    dispatcher.canReadMoreEntries(true);
                }
```

### SizeReplaceableByIsEmpty
`propertiesMap.size() == 0` can be replaced with 'propertiesMap.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/ManagedLedgerInterceptorImpl.java`
#### Snippet
```java
    @Override
    public void onManagedLedgerPropertiesInitialize(Map<String, String> propertiesMap) {
        if (propertiesMap == null || propertiesMap.size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`this.outputProcessors.size() == 0` can be replaced with 'this.outputProcessors.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/ManagedLedgerInterceptorImpl.java`
#### Snippet
```java
    @Override
    public PayloadProcessorHandle processPayloadBeforeEntryCache(ByteBuf ledgerData){
        if (this.outputProcessors == null || this.outputProcessors.size() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`this.inputProcessors.size() == 0` can be replaced with 'this.inputProcessors.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/ManagedLedgerInterceptorImpl.java`
#### Snippet
```java
    @Override
    public PayloadProcessorHandle processPayloadBeforeLedgerWrite(OpAddEntry op, ByteBuf ledgerData) {
        if (this.inputProcessors == null || this.inputProcessors.size() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`recentlyJoinedConsumers.size() > 0` can be replaced with '!recentlyJoinedConsumers.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
            LinkedHashMap<Consumer, PositionImpl> recentlyJoinedConsumers = keySharedDispatcher
                    .getRecentlyJoinedConsumers();
            if (recentlyJoinedConsumers != null && recentlyJoinedConsumers.size() > 0) {
                recentlyJoinedConsumers.forEach((k, v) -> {
                    subStats.consumersAfterMarkDeletePosition.put(k.consumerName(), v.toString());
```

### SizeReplaceableByIsEmpty
`topics.size() == 0` can be replaced with 'topics.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                            .thenApply(allTopicsInThisBundle -> {
                                Map<String, Long> topicHashPositions = new HashMap<>();
                                if (topics == null || topics.size() == 0) {
                                    allTopicsInThisBundle.forEach(t -> {
                                        topicHashPositions.put(t,
```

### SizeReplaceableByIsEmpty
`splitBoundaries.size() == 0` can be replaced with 'splitBoundaries.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        if (splitAlgorithmName
                                .equalsIgnoreCase(NamespaceBundleSplitAlgorithm.SPECIFIED_POSITIONS_DIVIDE)
                                && (splitBoundaries == null || splitBoundaries.size() == 0)) {
                            throw new RestException(Status.PRECONDITION_FAILED,
                                    "With specified_positions_divide split algorithm, splitBoundaries must not be "
```

### SizeReplaceableByIsEmpty
`policies.bundles.getBoundaries().size() == 0` can be replaced with 'policies.bundles.getBoundaries().isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

                if (policies.bundles != null && policies.bundles.getNumBundles() > 0) {
                    if (policies.bundles.getBoundaries() == null || policies.bundles.getBoundaries().size() == 0) {
                        policies.bundles = getBundles(policies.bundles.getNumBundles());
                    } else {
```

### SizeReplaceableByIsEmpty
`brokers.size() == 0` can be replaced with 'brokers.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/BrokerVersionFilter.java`
#### Snippet
```java
            throw new BrokerFilterBadVersionException("Unable to determine latest version since broker set was null");
        }
        if (brokers.size() == 0) {
            throw new BrokerFilterBadVersionException("Unable to determine latest version since broker set was empty");
        }
```

### SizeReplaceableByIsEmpty
`brokerVersion.length() == 0` can be replaced with 'brokerVersion.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/BrokerVersionFilter.java`
#### Snippet
```java

            String brokerVersion = data.getLocalData().getBrokerVersionString();
            if (null == brokerVersion || brokerVersion.length() == 0) {
                LOG.warn("No version string in load report for broker [{}]; disabling PreferLaterVersions feature",
                        broker);
```

### SizeReplaceableByIsEmpty
`bundlesToBeSplit.size() > 0` can be replaced with '!bundlesToBeSplit.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
        }

        if (bundlesToBeSplit.size() > 0) {
            for (String bundleName : bundlesToBeSplit) {
                try {
```

### SizeReplaceableByIsEmpty
`finalCandidates.size() > 0` can be replaced with '!finalCandidates.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
        }

        if (finalCandidates.size() > 0) {
            if (this.getLoadBalancerPlacementStrategy().equals(LOADBALANCER_STRATEGY_LLS)
                    || this.getLoadBalancerPlacementStrategy().equals(LOADBALANCER_STRATEGY_LEAST_MSG)) {
```

### SizeReplaceableByIsEmpty
`candidates.size() > 0` can be replaced with '!candidates.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
        // "boundedness" affects how two ranks are compared to see which one is better
        boolean unboundedRanks = getLoadBalancerPlacementStrategy().equals(LOADBALANCER_STRATEGY_LEAST_MSG);
        long randomBrokerIndex = (candidates.size() > 0) ? (this.brokerRotationCursor % candidates.size()) : 0;

        // find the least loaded & not-idle broker
```

### SizeReplaceableByIsEmpty
`entryQueue.size() == 0` can be replaced with 'entryQueue.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
                            this, System.nanoTime(), PositionImpl.LATEST);
                } else {
                    if (entryQueue.size() == 0) {
                        isReadable = false;
                    }
```

### SizeReplaceableByIsEmpty
`producers.size() > 0` can be replaced with '!producers.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            // close all producers
            CompletableFuture<Void> disconnectProducersFuture;
            if (producers.size() > 0) {
                List<CompletableFuture<Void>> futures = new ArrayList<>();
                // send migration url metadata to producers before disconnecting them
```

### SizeReplaceableByIsEmpty
`splitBoundaries.size() == 0` can be replaced with 'splitBoundaries.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
            CompletableFuture<List<NamespaceBundle>> updateFuture = new CompletableFuture<>();
            if (ex == null) {
                if (splitBoundaries == null || splitBoundaries.size() == 0) {
                    LOG.info("[{}] No valid boundary found in {} to split bundle {}",
                            bundle.getNamespaceObject().toString(), boundaries, bundle.getBundleRange());
```

### SizeReplaceableByIsEmpty
`positions.size() == 0` can be replaced with 'positions.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/SpecifiedPositionsBundleSplitAlgorithm.java`
#### Snippet
```java
        NamespaceBundle bundle = bundleSplitOption.getBundle();
        List<Long> positions = bundleSplitOption.getPositions();
        if (positions == null || positions.size() == 0) {
            throw new IllegalArgumentException("SplitBoundaries can't be empty");
        }
```

### SizeReplaceableByIsEmpty
`splitBoundaries.size() == 0` can be replaced with 'splitBoundaries.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/SpecifiedPositionsBundleSplitAlgorithm.java`
#### Snippet
```java
                    .collect(Collectors.toList());

            if (splitBoundaries.size() == 0) {
                return CompletableFuture.completedFuture(null);
            }
```

### SizeReplaceableByIsEmpty
`splitBoundaries.size() > 0` can be replaced with '!splitBoundaries.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
        checkArgument(canSplitBundle(targetBundle),
                "%s bundle can't be split further since range not larger than 1", targetBundle);
        if (splitBoundaries != null && splitBoundaries.size() > 0) {
            Collections.sort(splitBoundaries);
            checkArgument(splitBoundaries.get(0) > targetBundle.getLowerEndpoint()
```

### SizeReplaceableByIsEmpty
`splitBoundaries.size() == 0` can be replaced with 'splitBoundaries.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
                    long minVal = sourceBundle.partitions[i];
                    partitions[pos++] = minVal;
                    if (splitBoundaries == null || splitBoundaries.size() == 0) {
                        long maxVal = sourceBundle.partitions[i + 1];
                        long segSize = (maxVal - minVal) / numBundles;
```

### SizeReplaceableByIsEmpty
`entries.size() > 0` can be replaced with '!entries.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactedTopicImpl.java`
#### Snippet
```java
            return readEntries(
                    context.ledger, context.ledger.getLastAddConfirmed(), context.ledger.getLastAddConfirmed())
                    .thenCompose(entries -> entries.size() > 0
                            ? CompletableFuture.completedFuture(entries.get(0))
                            : CompletableFuture.completedFuture(null));
```

### SizeReplaceableByIsEmpty
`bindAddresses.size() == 0` can be replaced with 'bindAddresses.isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

        // create a channel for each bind address
        if (bindAddresses.size() == 0) {
            throw new IllegalArgumentException("At least one broker bind address must be configured");
        }
```

### SizeReplaceableByIsEmpty
`ackSet.size() > 0` can be replaced with '!ackSet.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessagePayloadContextImpl.java`
#### Snippet
```java
        context.redeliveryCount = redeliveryCount;
        context.acker = BatchMessageAcker.newAcker(context.getNumMessages());
        context.ackBitSet = (ackSet != null && ackSet.size() > 0)
                ? BitSetRecyclable.valueOf(SafeCollectionUtils.longListToArray(ackSet))
                : null;
```

### SizeReplaceableByIsEmpty
`topicNames.size() > 0` can be replaced with '!topicNames.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ReaderBuilderImpl.java`
#### Snippet
```java
    @Override
    public ReaderBuilder<T> topics(List<String> topicNames) {
        checkArgument(topicNames != null && topicNames.size() > 0,
                "Passed in topicNames should not be null or empty.");
        topicNames.forEach(topicName ->
```

### SizeReplaceableByIsEmpty
`messageIds.size() > 0` can be replaced with '!messageIds.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageRedeliveryTracker.java`
#### Snippet
```java
            }
        } finally {
            if (messageIds.size() > 0) {
                consumerBase.onAckTimeoutSend(messageIds);
                consumerBase.redeliverUnacknowledgedMessages(messageIds);
```

### SizeReplaceableByIsEmpty
`secondary.size() > 0` can be replaced with '!secondary.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AutoClusterFailover.java`
#### Snippet
```java
        public ServiceUrlProvider build() {
            Objects.requireNonNull(primary, "primary service url shouldn't be null");
            checkArgument(secondary != null && secondary.size() > 0,
                    "secondary cluster service url shouldn't be null and should set at least one");
            checkArgument(failoverDelayNs > 0, "failoverDelay should > 0");
```

### SizeReplaceableByIsEmpty
`interceptorList.size() == 0` can be replaced with 'interceptorList.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerBuilderImpl.java`
#### Snippet
```java
        }

        return interceptorList == null || interceptorList.size() == 0
                ? client.createProducerAsync(conf, schema, null)
                : client.createProducerAsync(conf, schema, new ProducerInterceptors(interceptorList));
```

### SizeReplaceableByIsEmpty
`conf.getTopicNames().size() > 0` can be replaced with '!conf.getTopicNames().isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBuilderImpl.java`
#### Snippet
```java
        }
        CompletableFuture<Void> applyDLQConfig;
        if (conf.isRetryEnable() && conf.getTopicNames().size() > 0) {
            TopicName topicFirst = TopicName.get(conf.getTopicNames().iterator().next());
            //Issue 9327: do compatibility check in case of the default retry and dead letter topic name changed
```

### SizeReplaceableByIsEmpty
`interceptorList.size() == 0` can be replaced with 'interceptorList.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBuilderImpl.java`
#### Snippet
```java
        }
        return applyDLQConfig.thenCompose(__ -> {
            if (interceptorList == null || interceptorList.size() == 0) {
                return client.subscribeAsync(conf, schema, null);
            } else {
```

### SizeReplaceableByIsEmpty
`entriesToAck.size() > 0` can be replaced with '!entriesToAck.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
        }

        if (entriesToAck.size() > 0) {

            newMessageAckCommandAndWrite(cnx, consumer.consumerId, 0L, 0L,
```

### SizeReplaceableByIsEmpty
`messageIds.size() != 0` can be replaced with '!messageIds.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
            Optional<Lock> readLock = acquireReadLock();
            try {
                if (messageIds.size() != 0) {
                    addListAcknowledgment(messageIds);
                    return readLock.map(__ -> currentIndividualAckFuture)
```

### SizeReplaceableByIsEmpty
`incomingMessages.size() > 0` can be replaced with '!incomingMessages.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ZeroQueueConsumerImpl.java`
#### Snippet
```java
    private Message<T> fetchSingleMessageFromBroker() throws PulsarClientException {
        // Just being cautious
        if (incomingMessages.size() > 0) {
            log.error("The incoming message queue should never be greater than 0 when Queue size is 0");
            incomingMessages.forEach(Message::release);
```

### SizeReplaceableByIsEmpty
`remaining.length() > 0` can be replaced with '!remaining.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/KerberosName.java`
#### Snippet
```java
        List<Rule> result = new ArrayList<Rule>();
        String remaining = rules.trim();
        while (remaining.length() > 0) {
            Matcher matcher = ruleParser.matcher(remaining);
            if (!matcher.lookingAt()) {
```

### SizeReplaceableByIsEmpty
`cmd.getObjects().size() > 0` can be replaced with '!cmd.getObjects().isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
        }
        sb.append(module);
        if (cmd.getObjects().size() > 0
                && cmd.getObjects().get(0).getClass().getName().equals("com.beust.jcommander.JCommander")) {
            JCommander cmdObj = (JCommander) cmd.getObjects().get(0);
```

### SizeReplaceableByIsEmpty
`options.size() > 0` can be replaced with '!options.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
                    sb.append(module).append(" ").append(subK).append(" options").append("\n```\n\n");
                    List<ParameterDescription> options = cmdObj.getCommands().get(subK).getParameters();
                    if (options.size() > 0) {
                        sb.append("|Flag|Description|Default|\n");
                        sb.append("|---|---|---|\n");
```

### SizeReplaceableByIsEmpty
`commandNames.size() == 0` can be replaced with 'commandNames.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
        }

        if (commandNames.size() == 0) {
            for (Map.Entry<String, JCommander> cmd : jcommander.getCommands().entrySet()) {
                if (cmd.getKey().equals(name)) {
```

### SizeReplaceableByIsEmpty
`list.size() == 0` can be replaced with 'list.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SafeCollectionUtils.java`
#### Snippet
```java

    public static long[] longListToArray(List<Long> list) {
        return list == null || list.size() == 0 ? new long[0] : list.stream().mapToLong(l->l).toArray();
    }
}
```

### SizeReplaceableByIsEmpty
`futures.size() == 0` can be replaced with 'futures.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FutureUtil.java`
#### Snippet
```java
                    .findFirst();
            if (!value.isPresent()) {
                if (futures.size() == 0) {
                    return CompletableFuture.completedFuture(Optional.empty());
                }
```

### SizeReplaceableByIsEmpty
`namespace.length() == 0` can be replaced with 'namespace.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/naming/NamespaceName.java`
#### Snippet
```java
        }

        if (namespace.length() == 0) {
            return Optional.empty();
        }
```

### SizeReplaceableByIsEmpty
`protocols.size() > 0` can be replaced with '!protocols.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/KeyStoreSSLContext.java`
#### Snippet
```java
        this.needClientAuth = requireTrustedClientCertOnConnect;

        if (protocols != null && protocols.size() > 0) {
            this.protocols = protocols;
        } else {
```

### SizeReplaceableByIsEmpty
`ciphers.size() > 0` can be replaced with '!ciphers.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/KeyStoreSSLContext.java`
#### Snippet
```java
        }

        if (ciphers != null && ciphers.size() > 0) {
            this.ciphers = ciphers;
        } else {
```

### SizeReplaceableByIsEmpty
`ackSet.size() > 0` can be replaced with '!ackSet.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        BatchMessageAcker acker = BatchMessageAcker.newAcker(batchSize);
        BitSetRecyclable ackBitSet = null;
        if (ackSet != null && ackSet.size() > 0) {
            ackBitSet = BitSetRecyclable.valueOf(SafeCollectionUtils.longListToArray(ackSet));
        }
```

### SizeReplaceableByIsEmpty
`ciphers.size() > 0` can be replaced with '!ciphers.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java

    private static void setupCiphers(SslContextBuilder builder, Set<String> ciphers) {
        if (ciphers != null && ciphers.size() > 0) {
            builder.ciphers(ciphers);
        }
```

### SizeReplaceableByIsEmpty
`protocols.size() > 0` can be replaced with '!protocols.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java

    private static void setupProtocols(SslContextBuilder builder, Set<String> protocols) {
        if (protocols != null && protocols.size() > 0) {
            builder.protocols(protocols.toArray(new String[protocols.size()]));
        }
```

### SizeReplaceableByIsEmpty
`interceptorNames.size() > 0` can be replaced with '!interceptorNames.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/intercept/BrokerEntryMetadataUtils.java`
#### Snippet
```java
            Set<String> interceptorNames, ClassLoader classLoader) {
        Set<T> interceptors = new LinkedHashSet<>();
        if (interceptorNames != null && interceptorNames.size() > 0) {
            for (String interceptorName : interceptorNames) {
                try {
```

### SizeReplaceableByIsEmpty
`interceptorNames.size() > 0` can be replaced with '!interceptorNames.isEmpty()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/intercept/BrokerEntryMetadataUtils.java`
#### Snippet
```java
            Set<String> interceptorNames, ClassLoader classLoader) {
        Set<BrokerEntryMetadataInterceptor> interceptors = new HashSet<>();
        if (interceptorNames != null && interceptorNames.size() > 0) {
            for (String interceptorName : interceptorNames) {
                try {
```

### SizeReplaceableByIsEmpty
`ledgerRetryMap.size() > 0` can be replaced with '!ledgerRetryMap.isEmpty()'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerOfflineBacklog.java`
#### Snippet
```java

        // go through ledgers where LAC was -1
        if (accurate && ledgerRetryMap.size() > 0) {
            ledgerRetryMap.forEach((cursorName, ledgerId) -> {
                if (log.isDebugEnabled()) {
```

### SizeReplaceableByIsEmpty
`ledgers.size() > 0` can be replaced with '!ledgers.isEmpty()'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ReadOnlyManagedLedgerImpl.java`
#### Snippet
```java

                // Last ledger stat may be zeroed, we must update it
                if (ledgers.size() > 0 && ledgers.lastEntry().getValue().getEntries() == 0) {
                    long lastLedgerId = ledgers.lastKey();

```

### SizeReplaceableByIsEmpty
`messages.size() > 0` can be replaced with '!messages.isEmpty()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                // Call receiveAsync() if the incoming queue is not full. Because this block is run with
                // thenAcceptAsync, there is no chance for recursion that would lead to stack overflow.
                receiveMessageFromConsumer(consumer, messages.size() > 0);
            }
        }, internalPinnedExecutor).exceptionally(ex -> {
```

### SizeReplaceableByIsEmpty
`entries.size() == 0` can be replaced with 'entries.isEmpty()'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2BuilderImpl.java`
#### Snippet
```java
        long offset;
        if (firstEntryId == 0) {
            checkState(entries.size() == 0);
            offset = 0;
        } else {
```

### SizeReplaceableByIsEmpty
`entries.size() > 0` can be replaced with '!entries.isEmpty()'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2BuilderImpl.java`
#### Snippet
```java
            offset = 0;
        } else {
            checkState(entries.size() > 0);
            offset = entries.get(entries.size() - 1).getOffset() + lastBlockSize;
        }
```

### SizeReplaceableByIsEmpty
`data.size() == 0` can be replaced with 'data.isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/AbstractHierarchicalLedgerManager.java`
#### Snippet
```java
                            final AsyncCallback.VoidCallback finalCb, final Object context,
                            final int successRc, final int failureRc) {
            if (data == null || data.size() == 0) {
                finalCb.processResult(successRc, null, context);
                return;
```

### SizeReplaceableByIsEmpty
`curBatch.size() > 0` can be replaced with '!curBatch.isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
            @Override
            public boolean hasNext() {
                if (curBatch.size() > 0) {
                    return true;
                }
```

### SizeReplaceableByIsEmpty
`queue.size() > 0` can be replaced with '!queue.isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
                }

                while (queue.size() > 0 && curBatch.size() == 0) {
                    String parent = queue.remove();
                    try {
```

### SizeReplaceableByIsEmpty
`curBatch.size() == 0` can be replaced with 'curBatch.isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
                }

                while (queue.size() > 0 && curBatch.size() == 0) {
                    String parent = queue.remove();
                    try {
```

### SizeReplaceableByIsEmpty
`curBatch.size() > 0` can be replaced with '!curBatch.isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
                    }
                }
                return curBatch.size() > 0;
            }

```

### SizeReplaceableByIsEmpty
`curBatch.size() > 0` can be replaced with '!curBatch.isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
            @Override
            public UnderreplicatedLedger next() {
                assert curBatch.size() > 0;
                return curBatch.remove();
            }
```

### SizeReplaceableByIsEmpty
`children.size() > 0` can be replaced with '!children.isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        Collections.shuffle(children);

        while (children.size() > 0) {
            String tryChild = children.get(0);
            String tryPath = parent + "/" + tryChild;
```

### SizeReplaceableByIsEmpty
`drainOpStatusMap.size() > 0` can be replaced with '!drainOpStatusMap.isEmpty()'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/SchedulerManager.java`
#### Snippet
```java
        int numRemovedWorkerIds = 0;

        if (drainOpStatusMap.size() > 0) {
            val currentMembership = membershipManager.getCurrentMembership()
                    .stream().map(WorkerInfo::getWorkerId).collect(Collectors.toSet());
```

### SizeReplaceableByIsEmpty
`opts.getExtraAnnotations().size() > 0` can be replaced with '!opts.getExtraAnnotations().isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/BasicKubernetesManifestCustomizer.java`
#### Snippet
```java

    private V1ObjectMeta updateMeta(RuntimeOpts opts, V1ObjectMeta meta) {
        if (opts.getExtraAnnotations() != null && opts.getExtraAnnotations().size() > 0) {
            opts.getExtraAnnotations().forEach(meta::putAnnotationsItem);
        }
```

### SizeReplaceableByIsEmpty
`opts.getExtraLabels().size() > 0` can be replaced with '!opts.getExtraLabels().isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/BasicKubernetesManifestCustomizer.java`
#### Snippet
```java
            opts.getExtraAnnotations().forEach(meta::putAnnotationsItem);
        }
        if (opts.getExtraLabels() != null && opts.getExtraLabels().size() > 0) {
            opts.getExtraLabels().forEach(meta::putLabelsItem);
        }
```

### SizeReplaceableByIsEmpty
`opts.getNodeSelectorLabels().size() > 0` can be replaced with '!opts.getNodeSelectorLabels().isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/BasicKubernetesManifestCustomizer.java`
#### Snippet
```java
        V1PodSpec ps = pt.getSpec();
        if (ps != null) {
            if (opts.getNodeSelectorLabels() != null && opts.getNodeSelectorLabels().size() > 0) {
                opts.getNodeSelectorLabels().forEach(ps::putNodeSelectorItem);
            }
```

### SizeReplaceableByIsEmpty
`opts.getTolerations().size() > 0` can be replaced with '!opts.getTolerations().isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/BasicKubernetesManifestCustomizer.java`
#### Snippet
```java
                opts.getNodeSelectorLabels().forEach(ps::putNodeSelectorItem);
            }
            if (opts.getTolerations() != null && opts.getTolerations().size() > 0) {
                opts.getTolerations().forEach(ps::addTolerationsItem);
            }
```

### SizeReplaceableByIsEmpty
`response.getItems().size() > 0` can be replaced with '!response.getItems().isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                    }

                    if (response.getItems().size() > 0) {
                        return Actions.ActionResult.builder()
                                .success(false)
```

### SizeReplaceableByIsEmpty
`sample.labelNames.size() > 0` can be replaced with '!sample.labelNames.isEmpty()'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/PrometheusTextFormat.java`
#### Snippet
```java
            for (Collector.MetricFamilySamples.Sample sample : metricFamilySamples.samples) {
                writer.write(sample.name);
                if (sample.labelNames.size() > 0) {
                    writer.write('{');
                    for (int i = 0; i < sample.labelNames.size(); ++i) {
```

### SizeReplaceableByIsEmpty
`toOffload.size() > 0` can be replaced with '!toOffload.isEmpty()'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        }

        if (toOffload.size() > 0) {
            log.info("[{}] Going to automatically offload ledgers {}"
                            + ", total size = {}, already offloaded = {}, to offload = {}",
```

### SizeReplaceableByIsEmpty
`parts.get(8).length() > 0` can be replaced with '!parts.get(8).isEmpty()'
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ConsumerHandler.java`
#### Snippet
```java
        checkArgument(parts.get(domainIndex).equals("persistent")
                || parts.get(domainIndex).equals("non-persistent"));
        checkArgument(parts.get(8).length() > 0, "Empty subscription name");

        return Codec.decode(parts.get(8));
```

### SizeReplaceableByIsEmpty
`arguments.commandNames.size() == 0` can be replaced with 'arguments.commandNames.isEmpty()'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/CmdGenerateDocumentation.java`
#### Snippet
```java
        }

        if (arguments.commandNames.size() == 0) {
            for (Map.Entry<String, JCommander> cmd : jc.getCommands().entrySet()) {
                generateDocument(cmd.getKey(), jc);
```

### SizeReplaceableByIsEmpty
`propertiesMap.size() == 0` can be replaced with 'propertiesMap.isEmpty()'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionSequenceIdGenerator.java`
#### Snippet
```java
    @Override
    public void onManagedLedgerPropertiesInitialize(Map<String, String> propertiesMap) {
        if (propertiesMap == null || propertiesMap.size() == 0) {
            return;
        }
```

### SizeReplaceableByIsEmpty
`entryQueue.size() > 0` can be replaced with '!entryQueue.isEmpty()'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionLogImpl.java`
#### Snippet
```java

        public void start() {
            while (fillEntryQueueCallback.fillQueue() || entryQueue.size() > 0) {
                Entry entry = entryQueue.poll();
                if (entry != null) {
```

### SizeReplaceableByIsEmpty
`options.size() > 0` can be replaced with '!options.isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdGenerateDocument.java`
#### Snippet
```java
                        .append(subK).append(" options").append("\n```\n\n");
                List<ParameterDescription> options = cmdObj.jcommander.getCommands().get(subK).getParameters();
                if (options.size() > 0) {
                    sb.append("**Options:**\n\n");
                    sb.append("|Flag|Description|Default|");
```

### SizeReplaceableByIsEmpty
`msg.getProperties().size() > 0` can be replaced with '!msg.getProperties().isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                    System.out.println("Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId());
                }
                if (msg.getProperties().size() > 0) {
                    System.out.println("Properties:");
                    print(msg.getProperties());
```

### SizeReplaceableByIsEmpty
`commandNames.size() == 0` can be replaced with 'commandNames.isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdGenerateDocumentation.java`
#### Snippet
```java
        PulsarClientTool pulsarClientTool = new PulsarClientTool(new Properties());
        JCommander commander = pulsarClientTool.jcommander;
        if (commandNames.size() == 0) {
            for (Map.Entry<String, JCommander> cmd : commander.getCommands().entrySet()) {
                if (cmd.getKey().equals("generate_documentation")) {
```

### SizeReplaceableByIsEmpty
`messages.size() > 0` can be replaced with '!messages.isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
        }

        if (messages.size() > 0){
            messages = messages.stream().map(str -> str.split(separator)).flatMap(Stream::of).toList();
        }
```

### SizeReplaceableByIsEmpty
`messages.size() == 0` can be replaced with 'messages.isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
        }

        if (messages.size() == 0 && messageFileNames.size() == 0) {
            throw (new ParameterException("Please supply message content with either --messages or --files"));
        }
```

### SizeReplaceableByIsEmpty
`messageFileNames.size() == 0` can be replaced with 'messageFileNames.isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
        }

        if (messages.size() == 0 && messageFileNames.size() == 0) {
            throw (new ParameterException("Please supply message content with either --messages or --files"));
        }
```

### SizeReplaceableByIsEmpty
`topics.size() > 0` can be replaced with '!topics.isEmpty()'
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/NamespacesImpl.java`
#### Snippet
```java
        NamespaceName ns = NamespaceName.get(namespace);
        WebTarget path = namespacePath(ns, bundle, "topicHashPositions");
        if (topics != null && topics.size() > 0) {
            path = path.queryParam("topics", topics.stream().map(Codec::encode).toArray());
        }
```

### SizeReplaceableByIsEmpty
`splitBoundaries.size() == 0` can be replaced with 'splitBoundaries.isEmpty()'
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/NamespacesImpl.java`
#### Snippet
```java
                .queryParam("splitAlgorithmName", splitAlgorithmName);

        return (splitBoundaries == null || splitBoundaries.size() == 0)
                ? asyncPutRequest(path, Entity.entity("", MediaType.APPLICATION_JSON))
                : asyncPutRequest(path, Entity.entity(splitBoundaries, MediaType.APPLICATION_JSON));
```

### SizeReplaceableByIsEmpty
`splitBoundaries.size() == 0` can be replaced with 'splitBoundaries.isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
            }
            bundle = bundleType != null ? bundleType.toString() : bundle;
            if (splitBoundaries == null || splitBoundaries.size() == 0) {
                getAdmin().namespaces().splitNamespaceBundle(
                        namespace, bundle, unload, splitAlgorithmName);
```

### SizeReplaceableByIsEmpty
`properties.size() == 0` can be replaced with 'properties.isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        void run() throws Exception {
            String namespace = validateNamespace(params);
            if (properties.size() == 0) {
                throw new ParameterException(String.format("Required at least one property for the namespace, "
                        + "but found %d.", properties.size()));
```

### SizeReplaceableByIsEmpty
`ciphers.size() > 0` can be replaced with '!ciphers.isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java
            this.sslCtxRefresher = sslCtxRefresher;

            if (ciphers != null && ciphers.size() > 0) {
                this.setIncludeCipherSuites(ciphers.toArray(new String[0]));
            }
```

### SizeReplaceableByIsEmpty
`protocols.size() > 0` can be replaced with '!protocols.isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java
            }

            if (protocols != null && protocols.size() > 0) {
                this.setIncludeProtocols(protocols.toArray(new String[0]));
            }
```

### SizeReplaceableByIsEmpty
`message.getProperties().size() > 0` can be replaced with '!message.getProperties().isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }

                if (message.getProperties().size() > 0) {
                    System.out.println("Properties:");
                    print(message.getProperties());
```

### SizeReplaceableByIsEmpty
`message.getProperties().size() > 0` can be replaced with '!message.getProperties().isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }

                if (message.getProperties().size() > 0) {
                    System.out.println("Properties:");
                    print(msg.getProperties());
```

### SizeReplaceableByIsEmpty
`message.getProperties().size() > 0` can be replaced with '!message.getProperties().isEmpty()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            }

            if (message.getProperties().size() > 0) {
                System.out.println("Properties:");
                print(message.getProperties());
```

### SizeReplaceableByIsEmpty
`error.length() > 0` can be replaced with '!error.isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/common/configuration/PulsarConfigurationLoader.java`
#### Snippet
```java
            }
        }
        if (error.length() > 0) {
            throw new IllegalArgumentException(error.substring(0, error.length() - 1));
        }
```

### SizeReplaceableByIsEmpty
`str.length() == 0` can be replaced with 'str.isEmpty()'
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/SaslRoleTokenSigner.java`
#### Snippet
```java
     */
    public String sign(String str) {
        if (str == null || str.length() == 0) {
            throw new IllegalArgumentException("NULL or empty string to sign");
        }
```

### SizeReplaceableByIsEmpty
`subject.getPrincipals().size() > 0` can be replaced with '!subject.getPrincipals().isEmpty()'
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/PulsarSaslServer.java`
#### Snippet
```java
        throws IOException {
        SaslServerCallbackHandler callbackHandler = new SaslServerCallbackHandler(allowedIdsPattern);
        if (subject.getPrincipals().size() > 0) {
            try {
                final Object[] principals = subject.getPrincipals().toArray();
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/SaslRoleToken.java`
#### Snippet
```java
     */
    private static void checkForIllegalArgument(String value, String name) {
        if (value == null || value.length() == 0 || value.contains(ATTR_SEPARATOR)) {
            throw new IllegalArgumentException(name + ILLEGAL_ARG_MSG);
        }
```

### SizeReplaceableByIsEmpty
`messages.size() > 0` can be replaced with '!messages.isEmpty()'
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
                        try {
                            List<Message<byte[]>> messages = getMessagesFromHttpResponse(tn.toString(), response);
                            if (messages.size() > 0) {
                                future.complete(messages.get(0));
                            } else {
```

## RuleId[ruleID=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `buildtools/src/main/java/org/apache/pulsar/tests/EnumValuesDataProvider.java`
#### Snippet
```java
public abstract class EnumValuesDataProvider {
    @DataProvider
    public static final Object[][] values(Method testMethod) {
        Class<?> enumClass = Arrays.stream(testMethod.getParameterTypes())
                .findFirst()
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/validator/StringSchemaDataValidator.java`
#### Snippet
```java
class StringSchemaDataValidator implements SchemaDataValidator {

    public static final StringSchemaDataValidator of() {
        return INSTANCE;
    }
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AuthenticationUtil.java`
#### Snippet
```java
     */
    @SuppressWarnings("deprecation")
    public static final Authentication create(String authPluginClassName, Map<String, String> authParams)
            throws UnsupportedAuthenticationException {
        try {
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AuthenticationUtil.java`
#### Snippet
```java
     */
    @SuppressWarnings("deprecation")
    public static final Authentication create(String authPluginClassName, String authParamsString)
            throws UnsupportedAuthenticationException {
        try {
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/MessageIdUtils.java`
#### Snippet
```java

public class MessageIdUtils {
    public static final long getOffset(MessageId messageId) {
        MessageIdImpl msgId = (MessageIdImpl) messageId;
        long ledgerId = msgId.getLedgerId();
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/MessageIdUtils.java`
#### Snippet
```java
    }

    public static final MessageId getMessageId(long offset) {
        // Demultiplex ledgerId and entryId from offset
        long ledgerId = offset >>> 28;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/client/api/url/URL.java`
#### Snippet
```java
     * @return java.net.URL instance
     */
    public static final java.net.URL createURL(String spec)
            throws MalformedURLException, URISyntaxException, InstantiationException, IllegalAccessException {
        return new URL(spec).url;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
    private static final int R = 47;

    static final long hash(long key1, long key2) {
        long hash = key1 * HashMixer;
        hash ^= hash >>> R;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
    private static final int R = 47;

    static final <K> long hash(K key) {
        long hash = key.hashCode() * HashMixer;
        hash ^= hash >>> R;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
    }

    static final int signSafeMod(long n, int max) {
        return (int) (n & (max - 1)) << 1;
    }
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashSet.java`
#### Snippet
```java
    private static final int R = 47;

    static final <K> long hash(K key) {
        long hash = key.hashCode() * HashMixer;
        hash ^= hash >>> R;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashSet.java`
#### Snippet
```java
    }

    static final int signSafeMod(long n, int max) {
        return (int) n & (max - 1);
    }
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongPairSet.java`
#### Snippet
```java
    private static final int R = 47;

    static final long hash(long key1, long key2) {
        long hash = key1 * HashMixer;
        hash ^= hash >>> R;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongPairSet.java`
#### Snippet
```java
    }

    static final int signSafeMod(long n, int max) {
        return (int) (n & (max - 1)) << 1;
    }
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
    private static final int R = 47;

    static final long hash(long key) {
        long hash = key * HashMixer;
        hash ^= hash >>> R;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
    }

    static final int signSafeMod(long n, int max) {
        return (int) n & (max - 1);
    }
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongLongPairHashMap.java`
#### Snippet
```java
    private static final int R = 47;

    static final long hash(long key1, long key2) {
        long hash = key1 * HashMixer;
        hash ^= hash >>> R;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongLongPairHashMap.java`
#### Snippet
```java
    }

    static final int signSafeMod(long n, int max) {
        return (int) (n & (max - 1)) << 2;
    }
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java
    }

    public static final MessageId getMessageId(long sequenceId) {
        // Demultiplex ledgerId and entryId from offset
        long ledgerId = sequenceId >>> 28;
```

### FinalStaticMethod
'static' method declared `final`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java
    }

    public static final long getSequenceId(MessageId messageId) {
        MessageIdImpl msgId = (MessageIdImpl) ((messageId instanceof TopicMessageIdImpl)
                ? ((TopicMessageIdImpl) messageId).getInnerMessageId()
```

## RuleId[ruleID=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileRecord.java`
#### Snippet
```java
        this.setProperty(FILE_NAME, srcFile.getName());
        this.setProperty(FILE_ABSOLUTE_PATH, srcFile.getAbsolutePath());
        this.setProperty(FILE_MODIFIED_TIME, srcFile.lastModified() + "");
    }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/elastic/ElasticSearchJavaRestClient.java`
#### Snippet
```java
                .index(index)
                .settings(new IndexSettings.Builder()
                        .numberOfShards(config.getIndexNumberOfShards() + "")
                        .numberOfReplicas(config.getIndexNumberOfReplicas() + "")
                        .build()
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/elastic/ElasticSearchJavaRestClient.java`
#### Snippet
```java
                .settings(new IndexSettings.Builder()
                        .numberOfShards(config.getIndexNumberOfShards() + "")
                        .numberOfReplicas(config.getIndexNumberOfReplicas() + "")
                        .build()
                )
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
            for (int index = 0; index < messages.size(); index++) {
                ProducerAck produceMessageResult = new ProducerAck();
                produceMessageResult.setMessageId(partition + "");
                produceMessageResults.add(produceMessageResult);
                publishResults.add(publishSingleMessageToPartition(topicName.getPartition(partition).toString(),
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
            for (int index = 0; index < messages.size(); index++) {
                ProducerAck produceMessageResult = new ProducerAck();
                produceMessageResult.setMessageId(partitionIndexes.get(index % (int) partitionIndexes.size()) + "");
                produceMessageResults.add(produceMessageResult);
                publishResults.add(publishSingleMessageToPartition(topicName.getPartition(partitionIndexes
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    private static void setProperty(Properties properties, String key, Object value) {
        if (value != null) {
            properties.setProperty(key, "" + value);
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
                Location loc = new LocationBuilder()
                        .scope(LocationScope.HOST)
                        .id(UUID.randomUUID() + "")
                        .description("Transient " + config.getBucket())
                        .build();
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceConfig.java`
#### Snippet
```java
     */
    public String getInstanceName() {
        return "" + instanceId;
    }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionSequenceIdGenerator.java`
#### Snippet
```java
    @Override
    public void onUpdateManagedLedgerInfo(Map<String, String> propertiesMap) {
        propertiesMap.put(MAX_LOCAL_TXN_ID, sequenceId.get() + "");
    }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
                                                                                                   boolean metadata) {
        WebTarget path = adminV3Transactions.path("coordinatorInternalStats");
        path = path.path(coordinatorId + "");
        path = path.queryParam("metadata", metadata);
        return asyncGetRequest(path, new FutureCallback<TransactionCoordinatorInternalStats>(){});
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
        WebTarget path = adminV3Transactions.path("transactionInBufferStats");
        path = path.path(topicName.getRestPath(false));
        path = path.path(txnID.getMostSigBits() + "");
        path = path.path(txnID.getLeastSigBits() + "");
        return asyncGetRequest(path, new FutureCallback<TransactionInBufferStats>(){});
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
        path = path.path(topicName.getRestPath(false));
        path = path.path(txnID.getMostSigBits() + "");
        path = path.path(txnID.getLeastSigBits() + "");
        return asyncGetRequest(path, new FutureCallback<TransactionInBufferStats>(){});
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
    public CompletableFuture<TransactionMetadata> getTransactionMetadataAsync(TxnID txnID) {
        WebTarget path = adminV3Transactions.path("transactionMetadata");
        path = path.path(txnID.getMostSigBits() + "");
        path = path.path(txnID.getLeastSigBits() + "");
        return asyncGetRequest(path, new FutureCallback<TransactionMetadata>(){});
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
        WebTarget path = adminV3Transactions.path("transactionMetadata");
        path = path.path(txnID.getMostSigBits() + "");
        path = path.path(txnID.getLeastSigBits() + "");
        return asyncGetRequest(path, new FutureCallback<TransactionMetadata>(){});
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
        path = path.path(TopicName.get(topic).getRestPath(false));
        path = path.path(subName);
        path = path.path(txnID.getMostSigBits() + "");
        path = path.path(txnID.getLeastSigBits() + "");
        return asyncGetRequest(path, new FutureCallback<TransactionInPendingAckStats>(){});
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
        path = path.path(subName);
        path = path.path(txnID.getMostSigBits() + "");
        path = path.path(txnID.getLeastSigBits() + "");
        return asyncGetRequest(path, new FutureCallback<TransactionInPendingAckStats>(){});
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
            Integer coordinatorId, long timeout, TimeUnit timeUnit) {
        WebTarget path = adminV3Transactions.path("slowTransactions");
        path = path.path(timeUnit.toMillis(timeout) + "");
        if (coordinatorId != null) {
            path = path.queryParam("coordinatorId", coordinatorId);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsServlet.java`
#### Snippet
```java
                    log.error("Failed to send metrics, "
                            + "likely the client or this server closed "
                            + "the connection due to a timeout ({} ms elapsed): {}", time, e + "");
                } else {
                    log.error("Failed to generate prometheus stats, {} ms elapsed", time, e);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsServlet.java`
#### Snippet
```java
                    // java.lang.IllegalStateException: AsyncContext completed and/or Request lifecycle recycled
                    log.error("Failed to generate prometheus stats, "
                            + "this is likely due to metricsServletTimeoutMs: {} ms elapsed: {}", time, e + "");
                }
            }
```

## RuleId[ruleID=NullableProblems]
### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@lombok.NonNull'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/TopicConsumerConfigurationData.java`
#### Snippet
```java
        @RequiredArgsConstructor
        class TopicsPattern implements TopicNameMatcher {
            @NonNull
            private final Pattern topicsPattern;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@lombok.NonNull'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/TopicConsumerConfigurationData.java`
#### Snippet
```java
        @RequiredArgsConstructor
        class TopicName implements TopicNameMatcher {
            @NonNull
            private final String topicName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@lombok.NonNull'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java
            "%s(defaultPublicKey=%s, defaultPrivateKey=%s, publicKeys=%s, privateKeys=%s)";

    @NonNull
    private String defaultPublicKey;
    @NonNull
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@lombok.NonNull'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java
    private String defaultPrivateKey;

    @NonNull
    private Map<String, String> publicKeys = new HashMap<>();
    @NonNull
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@lombok.NonNull'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java
    @NonNull
    private Map<String, String> publicKeys = new HashMap<>();
    @NonNull
    private Map<String, String> privateKeys = new HashMap<>();

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@lombok.NonNull'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java
    @NonNull
    private String defaultPublicKey;
    @NonNull
    private String defaultPrivateKey;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@lombok.NonNull'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/common/configuration/BindAddress.java`
#### Snippet
```java
    @Getter
    @Setter
    @NonNull
    private URI address;
}
```

## RuleId[ruleID=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
                        format("unsupported field-type %s for %s", field.getType(), field.getName()));
            }
        } else if (Number.class.isAssignableFrom(field.getType()) || fieldType.getClass().equals(String.class)) {
            field.set(obj, null);
        }
```

## RuleId[ruleID=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `List.size()` is ignored
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/MessageUtils.java`
#### Snippet
```java
                            columns = rowData.getAfterColumnsList();
                        }
                        columns.size();
                        for (CanalEntry.Column column : columns) {
                            Map<String, String> row = genColumn(column);
```

### IgnoreResultOfCall
Result of `Objects.nonNull()` is ignored
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadLocalStateCleaner.java`
#### Snippet
```java
    public <T> void cleanupThreadLocal(ThreadLocal<?> threadLocal, Thread thread,
                                       BiConsumer<Thread, T> cleanedValueListener) {
        Objects.nonNull(threadLocal);
        Objects.nonNull(thread);
        try {
```

### IgnoreResultOfCall
Result of `Objects.nonNull()` is ignored
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadLocalStateCleaner.java`
#### Snippet
```java
                                       BiConsumer<Thread, T> cleanedValueListener) {
        Objects.nonNull(threadLocal);
        Objects.nonNull(thread);
        try {
            Object threadLocalMap = GET_THREADLOCAL_MAP_METHOD.invoke(threadLocal, thread);
```

### IgnoreResultOfCall
Result of `Objects.nonNull()` is ignored
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadLocalStateCleaner.java`
#### Snippet
```java
    // cleanup thread local state on all active threads
    public <T> void cleanupThreadLocal(ThreadLocal<?> threadLocal, BiConsumer<Thread, T> cleanedValueListener) {
        Objects.nonNull(threadLocal);
        for (Thread thread : ThreadUtils.getAllThreads()) {
            cleanupThreadLocal(threadLocal, thread, cleanedValueListener);
```

### IgnoreResultOfCall
Result of `Objects.nonNull()` is ignored
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadLocalStateCleaner.java`
#### Snippet
```java
    public <T> T getThreadLocalValue(ThreadLocal<?> threadLocal, Thread thread)
            throws InvocationTargetException, IllegalAccessException {
        Objects.nonNull(threadLocal);
        Objects.nonNull(thread);
        Object threadLocalMap = GET_THREADLOCAL_MAP_METHOD.invoke(threadLocal, thread);
```

### IgnoreResultOfCall
Result of `Objects.nonNull()` is ignored
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadLocalStateCleaner.java`
#### Snippet
```java
            throws InvocationTargetException, IllegalAccessException {
        Objects.nonNull(threadLocal);
        Objects.nonNull(thread);
        Object threadLocalMap = GET_THREADLOCAL_MAP_METHOD.invoke(threadLocal, thread);
        if (threadLocalMap != null) {
```

### IgnoreResultOfCall
Result of `Objects.nonNull()` is ignored
in `buildtools/src/main/java/org/apache/pulsar/tests/FastThreadLocalStateCleaner.java`
#### Snippet
```java

    public void cleanupAllFastThreadLocals(Thread thread, BiConsumer<Thread, Object> cleanedValueListener) {
        Objects.nonNull(thread);
        try {
            Object internalThreadLocalMap;
```

### IgnoreResultOfCall
Result of `ListeningScheduledExecutorService.schedule()` is ignored
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
            log.debug("not enough data, delay schedule for part: {} length: {}", partId, dataObjectLength);
            scheduler.chooseThread(segmentInfo)
                    .schedule(() -> {
                        streamingOffloadLoop(partId, dataObjectLength);
                    }, 100, TimeUnit.MILLISECONDS);
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
        if (pkgFile.exists()) {
            log.warn("Function package exists already {} deleting it", pkgFile);
            pkgFile.delete();
        }

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
            }
        } finally {
            tempPkgFile.delete();
        }

```

### IgnoreResultOfCall
Result of `File.setExecutable()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java

        if (details.getRuntime() == Function.FunctionDetails.Runtime.GO && !pkgFile.canExecute()) {
            pkgFile.setExecutable(true);
            log.info("Golang function package file {} is set to executable", pkgFile);
        }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
            File pkgDir = new File(workerConfig.getDownloadDirectory(),
                    getDownloadPackagePath(functionMetaData, instanceId));
            pkgDir.mkdirs();
            File pkgFile = new File(
                    pkgDir,
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
            if (componentPackageFile != null && componentPackageFile.exists()) {
                if (sinkPkgUrl == null || !sinkPkgUrl.startsWith(Utils.FILE)) {
                    componentPackageFile.delete();
                }
            }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
            if (functionPackageFile != null && functionPackageFile.exists()) {
                if (!transformFunction.startsWith(Utils.FILE)) {
                    functionPackageFile.delete();
                }
            }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
            if (componentPackageFile != null && componentPackageFile.exists()) {
                if ((sinkPkgUrl != null && !sinkPkgUrl.startsWith(Utils.FILE)) || uploadedInputStream != null) {
                    componentPackageFile.delete();
                }
            }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImpl.java`
#### Snippet
```java
            if (componentPackageFile != null && componentPackageFile.exists()) {
                if ((functionPkgUrl != null && !functionPkgUrl.startsWith(Utils.FILE)) || uploadedInputStream != null) {
                    componentPackageFile.delete();
                }
            }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImpl.java`
#### Snippet
```java
            if (componentPackageFile != null && componentPackageFile.exists()) {
                if (functionPkgUrl == null || !functionPkgUrl.startsWith(Utils.FILE)) {
                    componentPackageFile.delete();
                }
            }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
            if (componentPackageFile != null && componentPackageFile.exists()) {
                if (sourcePkgUrl == null || !sourcePkgUrl.startsWith(Utils.FILE)) {
                    componentPackageFile.delete();
                }
            }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
            if (componentPackageFile != null && componentPackageFile.exists()) {
                if ((sourcePkgUrl != null && !sourcePkgUrl.startsWith(Utils.FILE)) || uploadedInputStream != null) {
                    componentPackageFile.delete();
                }
            }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntime.java`
#### Snippet
```java
        try {
            byte[] errorBytes = new byte[errorStream.available()];
            errorStream.read(errorBytes);
            String errorMessage = new String(errorBytes);
            deathException = new RuntimeException(errorMessage);
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
            if (uploadedInputStream != null) {
                targetArray = new byte[uploadedInputStream.available()];
                uploadedInputStream.read(targetArray);
            } else {
                targetArray = input.getBytes();
```

### IgnoreResultOfCall
Result of `RateLimiter.tryAcquire()` is ignored
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java

                        if (rateLimiter != null){
                            rateLimiter.tryAcquire();
                        }
                        if (!arguments.isDisableTransaction) {
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/FunctionsImpl.java`
#### Snippet
```java
            if (!file.exists()) {
                if (file.getParentFile() != null && !file.getParentFile().exists()) {
                    file.getParentFile().mkdirs();
                }
                file.createNewFile();
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/FunctionsImpl.java`
#### Snippet
```java
                    file.getParentFile().mkdirs();
                }
                file.createNewFile();
            }
            FileChannel os = new FileOutputStream(new File(destinationPath)).getChannel();
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
        try (InputStream stream = (InputStream) response.getEntity()) {
            byte[] data = new byte[stream.available()];
            stream.read(data);

            Map<String, String> properties = new TreeMap<>();
```

## RuleId[ruleID=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'com.yahoo.athenz.zts.ZTSClient.setPrefetchAutoEnable(boolean)' accessed via instance reference
in `pulsar-client-auth-athenz/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationAthenz.java`
#### Snippet
```java
                    privateKey, keyId);
            ztsClient = new ZTSClient(ztsUrl, tenantDomain, tenantService, siaProvider);
            ztsClient.setPrefetchAutoEnable(this.autoPrefetchEnabled);
        }
        return ztsClient;
```

## RuleId[ruleID=ClassNameSameAsAncestorName]
### ClassNameSameAsAncestorName
Class name `AwsDefaultProviderChainPlugin` is the same as one of its superclass' names
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/AwsDefaultProviderChainPlugin.java`
#### Snippet
```java
 */
@Deprecated
public class AwsDefaultProviderChainPlugin extends org.apache.pulsar.io.aws.AwsDefaultProviderChainPlugin
        implements AwsCredentialProviderPlugin {
}
```

### ClassNameSameAsAncestorName
Class name `STSAssumeRoleProviderPlugin` is the same as one of its superclass' names
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/STSAssumeRoleProviderPlugin.java`
#### Snippet
```java
 */
@Deprecated
public class STSAssumeRoleProviderPlugin extends org.apache.pulsar.io.aws.STSAssumeRoleProviderPlugin
        implements AwsCredentialProviderPlugin {
}
```

### ClassNameSameAsAncestorName
Class name `AwsCredentialProviderPlugin` is the same as one of its superclass' names
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/AwsCredentialProviderPlugin.java`
#### Snippet
```java
 */
@Deprecated
public interface AwsCredentialProviderPlugin extends org.apache.pulsar.io.aws.AwsCredentialProviderPlugin {
}

```

### ClassNameSameAsAncestorName
Class name `Functions` is the same as one of its superclass' names
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/Functions.java`
#### Snippet
```java
@Produces(MediaType.APPLICATION_JSON)
@Consumes(MediaType.APPLICATION_JSON)
public class Functions extends  org.apache.pulsar.broker.admin.v2.Functions{
}

```

### ClassNameSameAsAncestorName
Class name `Builder` is the same as one of its superclass' names
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/metrics/Summary.java`
#### Snippet
```java
    private static final List<DataSketchesSummaryLogger> LOGGERS = new ArrayList<>();

    public static class Builder extends SimpleCollector.Builder<Builder, Summary> {
        private final List<Double> quantiles = new ArrayList<>();

```

### ClassNameSameAsAncestorName
Class name `Builder` is the same as one of its superclass' names
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/metrics/ObserverGauge.java`
#### Snippet
```java
public class ObserverGauge extends SimpleCollector<ObserverGauge.Child> implements Collector.Describable {

    public static class Builder extends SimpleCollector.Builder<Builder, ObserverGauge> {
        private Supplier<Number> supplier;

```

### ClassNameSameAsAncestorName
Class name `Hash` is the same as one of its superclass' names
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/Hash.java`
#### Snippet
```java
package org.apache.pulsar.client.impl;

public interface Hash extends org.apache.pulsar.common.util.Hash {

    /**
```

### ClassNameSameAsAncestorName
Class name `Server` is the same as one of its superclass' names
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java
    }

    private static class Server extends SslContextFactory.Server {
        private final SslContextAutoRefreshBuilder<SSLContext> sslCtxRefresher;

```

### ClassNameSameAsAncestorName
Class name `HttpServletRequestWrapper` is the same as one of its superclass' names
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderToken.java`
#### Snippet
```java
    }

    private static final class HttpServletRequestWrapper extends javax.servlet.http.HttpServletRequestWrapper {
        private final HttpServletRequest request;

```

## RuleId[ruleID=RedundantMethodOverride]
### RedundantMethodOverride
Method `validate()` only delegates to its super method
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSourceConfig.java`
#### Snippet
```java

    @Override
    public void validate() {
        super.validate();
    }
```

### RedundantMethodOverride
Method `getKey()` is identical to its super method
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/data/TwitterRecord.java`
#### Snippet
```java

    @Override
    public Optional<String> getKey() {
        // TODO: Could use user or tweet ID as key here
        return Optional.empty();
```

### RedundantMethodOverride
Method `validateBundleOwnership()` only delegates to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
    // This is a stub method for Mockito
    @Override
    protected void validateBundleOwnership(String property, String cluster, String namespace, boolean authoritative,
            boolean readOnly, NamespaceBundle bundle) {
        super.validateBundleOwnership(property, cluster, namespace, authoritative, readOnly, bundle);
```

### RedundantMethodOverride
Method `validateSuperUserAccess()` only delegates to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
    // This is a stub method for Mockito
    @Override
    public void validateSuperUserAccess() {
        super.validateSuperUserAccess();
    }
```

### RedundantMethodOverride
Method `canUnsubscribe()` is identical to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public synchronized boolean canUnsubscribe(Consumer consumer) {
        return consumerList.size() == 1 && consumerSet.contains(consumer);
    }
```

### RedundantMethodOverride
Method `isConsumerConnected()` is identical to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public boolean isConsumerConnected() {
        return !consumerList.isEmpty();
    }
```

### RedundantMethodOverride
Method `getConsumers()` is identical to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public CopyOnWriteArrayList<Consumer> getConsumers() {
        return consumerList;
    }
```

### RedundantMethodOverride
Method `canUnsubscribe()` is identical to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public synchronized boolean canUnsubscribe(Consumer consumer) {
        return consumerList.size() == 1 && consumerSet.contains(consumer);
    }
```

### RedundantMethodOverride
Method `isConsumerConnected()` is identical to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public boolean isConsumerConnected() {
        return !consumerList.isEmpty();
    }
```

### RedundantMethodOverride
Method `getConsumers()` is identical to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public CopyOnWriteArrayList<Consumer> getConsumers() {
        return consumerList;
    }
```

### RedundantMethodOverride
Method `removeProducer()` is identical to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java

    @Override
    public void removeProducer(Producer producer) {
        checkArgument(producer.getTopic() == this);
        if (producers.remove(producer.getProducerName(), producer)) {
```

### RedundantMethodOverride
Method `isSystemTopic()` is identical to its super method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

    @Override
    public boolean isSystemTopic() {
        return false;
    }
```

### RedundantMethodOverride
Method `equals()` only delegates to its super method
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ChunkMessageIdImpl.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o) {
        return super.equals(o);
    }
```

### RedundantMethodOverride
Method `getCommandData()` is identical to its super method
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataKeyStoreTls.java`
#### Snippet
```java

    @Override
    public String getCommandData() {
        return null;
    }
```

### RedundantMethodOverride
Method `initialize()` only delegates to its super method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ShadowManagedLedgerImpl.java`
#### Snippet
```java

    @Override
    synchronized void initialize(ManagedLedgerInitializeLedgerCallback callback, Object ctx) {
        // TODO: ShadowManagedLedger has different initialize process from normal ManagedLedger,
        //  which is complicated and will be implemented in the next PRs.
```

### RedundantMethodOverride
Method `initialize()` only delegates to its super method
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarMetadataBookieDriver.java`
#### Snippet
```java

    @Override
    protected void initialize(AbstractConfiguration serverConfiguration) throws MetadataException {
        super.initialize(serverConfiguration);
    }
```

### RedundantMethodOverride
Method `createRegistrationManager()` only delegates to its super method
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarMetadataBookieDriver.java`
#### Snippet
```java

    @Override
    public RegistrationManager createRegistrationManager() {
        return super.createRegistrationManager();
    }
```

### RedundantMethodOverride
Method `sendOutputMessage()` only delegates to its super method
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java

        @Override
        public void sendOutputMessage(TypedMessageBuilder<T> msg, AbstractSinkRecord<T> record) {
            super.sendOutputMessage(msg, record);
        }
```

### RedundantMethodOverride
Method `compareTo()` only delegates to its super method
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnBatchedPositionImpl.java`
#### Snippet
```java
     *    {@link Object#hashCode()} & {@link  Object#equals(Object)} to determine whether the keys are the same.
     */
    public int compareTo(PositionImpl that) {
        return super.compareTo(that);
    }
```

### RedundantMethodOverride
Method `equals()` only delegates to its super method
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnBatchedPositionImpl.java`
#### Snippet
```java
     */
    @Override
    public boolean equals(Object o) {
        return super.equals(o);

```

### RedundantMethodOverride
Method `hashCode()` only delegates to its super method
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnBatchedPositionImpl.java`
#### Snippet
```java
     */
    @Override
    public int hashCode() {
        return super.hashCode();
    }
```

## RuleId[ruleID=OptionalAssignedToNull]
### OptionalAssignedToNull
Null is used for 'Optional' type in declaration
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java

    public List<Long> getSchemaLedgerList(String key) throws IOException {
        Optional<LocatorEntry> locatorEntry = null;
        try {
            locatorEntry = getLocator(key).get();
```

### OptionalAssignedToNull
Null is used for 'Optional' type in assignment
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
        topic = null;
        payload = null;
        encryptionCtx = null;
        redeliveryCount = 0;
        uncompressedSize = 0;
```

## RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
1 \<\< (28 - maxBatchBitsForOffset): integer shift implicitly cast to long
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
                    long entryId = messageSequenceRef.getEntryId();

                    if (entryId > (1 << (28 - maxBatchBitsForOffset))) {
                        log.error("EntryId of the message {} over max, chance of duplicate offsets", entryId);
                    }
```

### IntegerMultiplicationImplicitCastToLong
managedLedgerCacheSizeMB \* 1024 \* 1024: integer multiplication implicitly cast to long
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java
    @Config("pulsar.managed-ledger-cache-size-MB")
    public PulsarConnectorConfig setManagedLedgerCacheSizeMB(int managedLedgerCacheSizeMB) {
        this.managedLedgerCacheSizeMB = managedLedgerCacheSizeMB * 1024 * 1024;
        return this;
    }
```

### IntegerMultiplicationImplicitCastToLong
retention.getRetentionSizeInMB() \* 1024 \* 1024: integer multiplication implicitly cast to long
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java

        if (retention.getRetentionSizeInMB() > 0
                && quota.getLimitSize() >= (retention.getRetentionSizeInMB() * 1024 * 1024)) {
            return false;
        }
```

### IntegerMultiplicationImplicitCastToLong
conf.getLoadBalancerNamespaceBundleMaxBandwidthMbytes() \* LoadManagerShared.MIBI: integer multiplication implicitly cast to long
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/BundleSplitterTask.java`
#### Snippet
```java
        long maxBundleSessions = conf.getLoadBalancerNamespaceBundleMaxSessions();
        long maxBundleMsgRate = conf.getLoadBalancerNamespaceBundleMaxMsgRate();
        long maxBundleBandwidth = conf.getLoadBalancerNamespaceBundleMaxBandwidthMbytes() * LoadManagerShared.MIBI;

        loadData.getBrokerData().forEach((broker, brokerData) -> {
```

### IntegerMultiplicationImplicitCastToLong
2 \* pulsarService.getConfig().getResourceUsageTransportPublishIntervalInSecs(): integer multiplication implicitly cast to long
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceUsageTopicTransportManager.java`
#### Snippet
```java
            recdUsageInfo.parseFrom(Unpooled.wrappedBuffer(msg.getData()), msg.getData().length);
            if (timeDelta > TimeUnit.SECONDS.toMillis(
            2 * pulsarService.getConfig().getResourceUsageTransportPublishIntervalInSecs())) {
                LOG.error("Stale resource usage msg from broker {} publish time {} current time{}",
                recdUsageInfo.getBroker(), publishTime, currentTime);
```

### IntegerMultiplicationImplicitCastToLong
zk.getSessionTimeout() \* 5: integer multiplication implicitly cast to long
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKSessionWatcher.java`
#### Snippet
```java
    public ZKSessionWatcher(ZooKeeper zk, Consumer<SessionEvent> sessionListener) {
        this.zk = zk;
        this.monitorTimeoutMillis = zk.getSessionTimeout() * 5 / 6;
        this.tickTimeMillis = zk.getSessionTimeout() / 15;
        this.sessionListener = sessionListener;
```

### IntegerMultiplicationImplicitCastToLong
expectedHealthCheckInterval \* 1000: integer multiplication implicitly cast to long
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
                null, // we really dont use this in thread container
                containerFactory,
                expectedHealthCheckInterval * 1000);

        server = ServerBuilder.forPort(port)
```

### IntegerMultiplicationImplicitCastToLong
3 \* expectedHealthCheckInterval \* 1000: integer multiplication implicitly cast to long
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
                        try {
                            if (System.currentTimeMillis() - lastHealthCheckTs
                                    > 3 * expectedHealthCheckInterval * 1000) {
                                log.info("Haven't received health check from spawner in a while. Stopping instance...");
                                close();
```

### IntegerMultiplicationImplicitCastToLong
expectedHealthCheckInterval \* 1000: integer multiplication implicitly cast to long
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
                            log.error("Error occurred when checking for latest health check", e);
                        }
                    }, expectedHealthCheckInterval * 1000, expectedHealthCheckInterval * 1000, TimeUnit.MILLISECONDS);
        }

```

### IntegerMultiplicationImplicitCastToLong
expectedHealthCheckInterval \* 1000: integer multiplication implicitly cast to long
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
                            log.error("Error occurred when checking for latest health check", e);
                        }
                    }, expectedHealthCheckInterval * 1000, expectedHealthCheckInterval * 1000, TimeUnit.MILLISECONDS);
        }

```

### IntegerMultiplicationImplicitCastToLong
config.getLedgerRolloverTimeout() \* 1000: integer multiplication implicitly cast to long
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        if (ledger.getFactory().isMetadataServiceAvailable()
                && (lh.getLastAddConfirmed() >= config.getMetadataMaxEntriesPerLedger()
                || lastLedgerSwitchTimestamp < (now - config.getLedgerRolloverTimeout() * 1000))
                && (STATE_UPDATER.get(this) != State.Closed && STATE_UPDATER.get(this) != State.Closing)) {
            // It's safe to modify the timestamp since this method will be only called from a callback, implying that
```

## RuleId[ruleID=JavaLangImport]
### JavaLangImport
Unnecessary import from the 'java.lang' package
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionKey.java`
#### Snippet
```java

import java.nio.*;
import java.lang.*;

import com.google.flatbuffers.*;
```

### JavaLangImport
Unnecessary import from the 'java.lang' package
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/KeyValue.java`
#### Snippet
```java

import java.nio.*;
import java.lang.*;

import com.google.flatbuffers.*;
```

### JavaLangImport
Unnecessary import from the 'java.lang' package
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/Message.java`
#### Snippet
```java

import java.nio.*;
import java.lang.*;

import com.google.flatbuffers.*;
```

### JavaLangImport
Unnecessary import from the 'java.lang' package
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionCtx.java`
#### Snippet
```java

import java.nio.*;
import java.lang.*;

import com.google.flatbuffers.*;
```

## RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`path = path + parentVersion` could be simplified to 'path += parentVersion'
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java
                MockZNode parentNode = tree.get(parent);
                int parentVersion = tree.get(parent).getVersion();
                path = path + parentVersion;

                // Update parent version
```

### ReplaceAssignmentWithOperatorAssignment
`rank = rank * resourcesWithHighUsage * 2` could be simplified to 'rank \*= resourcesWithHighUsage \* 2'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/PulsarResourceDescription.java`
#### Snippet
```java
        }
        if (resourcesWithHighUsage > 0) {
            rank = rank * resourcesWithHighUsage * 2;
        }
        return rank;
```

### ReplaceAssignmentWithOperatorAssignment
`availabilityRank = availabilityRank / (resourcesWithLowAvailability * 2)` could be simplified to 'availabilityRank /= (resourcesWithLowAvailability \* 2)'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ResourceAvailabilityRanker.java`
#### Snippet
```java
        }
        if (resourcesWithLowAvailability > 0) {
            availabilityRank = availabilityRank / (resourcesWithLowAvailability * 2);
            log.debug("Total Resource with Low availability - [{}]", resourcesWithLowAvailability);
        }
```

### ReplaceAssignmentWithOperatorAssignment
`totalCpuUsage = totalCpuUsage + resUsage.getCpu().usage` could be simplified to 'totalCpuUsage += resUsage.getCpu().usage'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
                totalMsgRateIn += loadReport.getMsgRateIn();
                totalMsgRateOut += loadReport.getMsgRateOut();
                totalCpuUsage = totalCpuUsage + resUsage.getCpu().usage;
                totalMemoryUsage = totalMemoryUsage + resUsage.getMemory().usage;
            }
```

### ReplaceAssignmentWithOperatorAssignment
`totalMemoryUsage = totalMemoryUsage + resUsage.getMemory().usage` could be simplified to 'totalMemoryUsage += resUsage.getMemory().usage'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
                totalMsgRateOut += loadReport.getMsgRateOut();
                totalCpuUsage = totalCpuUsage + resUsage.getCpu().usage;
                totalMemoryUsage = totalMemoryUsage + resUsage.getMemory().usage;
            }

```

### ReplaceAssignmentWithOperatorAssignment
`interval = interval * 2` could be simplified to 'interval \*= 2'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/Backoff.java`
#### Snippet
```java
        long interval = defaultInterval;
        for (int i = 1; i < failedAttempts; i++) {
            interval = interval * 2;
            if (interval > maxBackoffInterval) {
                interval = maxBackoffInterval;
```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 2` could be simplified to 'index += 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
                return true;
            }
            index = index + 2;
        }
        return false;
```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 2` could be simplified to 'index += 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
                removedValues++;
            }
            index = index + 2;
        }

```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 2` could be simplified to 'index += 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
                    removeAtWithoutLock(index);
                }
                index = index + 2;
            }
            deleteItemsIndex = deleteItemsIndex + 2;
```

### ReplaceAssignmentWithOperatorAssignment
`deleteItemsIndex = deleteItemsIndex + 2` could be simplified to 'deleteItemsIndex += 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
                index = index + 2;
            }
            deleteItemsIndex = deleteItemsIndex + 2;
        }
        return removedValues;
```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 2` could be simplified to 'index += 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
                removed = true;
            }
            index = index + 2;
        }

```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 2` could be simplified to 'index += 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
        for (int i = 0; i < this.size; i++) {
            processor.accept(data[index], data[index + 1]);
            index = index + 2;
        }
    }
```

### ReplaceAssignmentWithOperatorAssignment
`this.capacity = capacity * 2` could be simplified to 'this.capacity \*= 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java

    private void expandArray() {
        this.capacity = capacity * 2;
        long[] newData = new long[2 * this.capacity];

```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 2` could be simplified to 'index += 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
            newData[index] = data[index];
            newData[index + 1] = data[index + 1];
            index = index + 2;
        }
        Arrays.fill(newData, index, newData.length, EmptyItem);
```

### ReplaceAssignmentWithOperatorAssignment
`index = index + 2` could be simplified to 'index += 2'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
            data[index] = -1;
            data[index + 1] = -1;
            index = index + 2;
        }

```

### ReplaceAssignmentWithOperatorAssignment
`avgProcessLatency = avgProcessLatency / nonNullInstances` could be simplified to 'avgProcessLatency /= nonNullInstances'
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/FunctionStatsImpl.java`
#### Snippet
```java
        // calculate average from sum
        if (nonNullInstances > 0) {
            avgProcessLatency = avgProcessLatency / nonNullInstances;
        } else {
            avgProcessLatency = null;
```

### ReplaceAssignmentWithOperatorAssignment
`reconsumetimes = reconsumetimes + 1` could be simplified to 'reconsumetimes += 1'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                    reconsumetimes = Integer.parseInt(
                            propertiesMap.get(RetryMessageUtil.SYSTEM_PROPERTY_RECONSUMETIMES));
                    reconsumetimes = reconsumetimes + 1;
                }
                propertiesMap.put(RetryMessageUtil.SYSTEM_PROPERTY_RECONSUMETIMES, String.valueOf(reconsumetimes));
```

### ReplaceAssignmentWithOperatorAssignment
`numProducers = numProducers + entry.getValue().producerCount` could be simplified to 'numProducers += entry.getValue().producerCount'
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
        if (this.bundleStats != null) {
            for (Map.Entry<String, NamespaceBundleStats> entry : this.bundleStats.entrySet()) {
                numProducers = numProducers + entry.getValue().producerCount;
            }
        }
```

### ReplaceAssignmentWithOperatorAssignment
`numConsumers = numConsumers + entry.getValue().consumerCount` could be simplified to 'numConsumers += entry.getValue().consumerCount'
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
        if (this.bundleStats != null) {
            for (Map.Entry<String, NamespaceBundleStats> entry : this.bundleStats.entrySet()) {
                numConsumers = numConsumers + entry.getValue().consumerCount;
            }
        }
```

### ReplaceAssignmentWithOperatorAssignment
`streamingOffset = streamingOffset + lastStreamingBlockSize` could be simplified to 'streamingOffset += lastStreamingBlockSize'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2BuilderImpl.java`
#### Snippet
```java
        checkState(dataHeaderLength > 0);

        streamingOffset = streamingOffset + lastStreamingBlockSize;
        lastStreamingBlockSize = blockSize;

```

### ReplaceAssignmentWithOperatorAssignment
`this.logDirectory = this.logDirectory + "/functions"` could be simplified to 'this.logDirectory += "/functions"'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntimeFactory.java`
#### Snippet
```java
            }
        }
        this.logDirectory = this.logDirectory + "/functions";

        if (this.extraDependenciesDir == null) {
```

### ReplaceAssignmentWithOperatorAssignment
`f = f * (float) Math.pow(10, mag)` could be simplified to 'f \*= (float) Math.pow(10, mag)'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/DefaultMessageFormatter.java`
#### Snippet
```java
        float f = r.nextFloat();
        int mag = (int) Math.abs(size);
        f = f * (float) Math.pow(10, mag);
        if (size < 0 && ((int) f) % 2 == 1) {
            return f * -1;
```

## RuleId[ruleID=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/http/NettyHttpServerHandler.java`
#### Snippet
```java

        if (msg instanceof HttpRequest) {
            HttpRequest request = this.request = (HttpRequest) msg;

            if (HttpUtil.is100ContinueExpected(request)) {
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/CmdUtility.java`
#### Snippet
```java
            reader = new BufferedReader(new InputStreamReader(proc.getInputStream(), UTF_8));
            String line = null;
            while ((line = reader.readLine()) != null) {
                if (writer != null) {
                    writer.write(line);
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/EntryBatchSizes.java`
#### Snippet
```java

    public int setBatchSize(int entryIdx, int batchSize) {
        return sizes[entryIdx] = batchSize;
    }

```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
        validateNamespacePolicyOperation(namespaceName, PolicyName.SUBSCRIPTION_AUTH_MODE, PolicyOperation.WRITE);
        validatePoliciesReadOnlyAccess();
        SubscriptionAuthMode authMode = subscriptionAuthMode == null ? subscriptionAuthMode = SubscriptionAuthMode.None
                : subscriptionAuthMode;
        try {
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        if (ledgerInfo != null && ledgerInfo.hasTimestamp() && ledgerInfo.getTimestamp() > 0
                && ((ManagedLedgerImpl) ledger).getClock().millis() - ledgerInfo.getTimestamp()
                > backlogQuotaLimitInSecond * 1000 && (result = slowestPosition.compareTo(
                new PositionImpl(ledgerInfo.getLedgerId(), ledgerInfo.getEntries() - 1))) <= 0) {
            if (result < 0) {
                if (log.isDebugEnabled()) {
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    public synchronized void triggerOffload(MessageIdImpl messageId) throws AlreadyRunningException {
        if (currentOffload.isDone()) {
            CompletableFuture<MessageIdImpl> promise = currentOffload = new CompletableFuture<>();
            log.info("[{}] Starting offload operation at messageId {}", topic, messageId);
            getManagedLedger().asyncOffloadPrefix(
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleStatsImpl.java`
#### Snippet
```java
        String namespace;
        if (StringUtils.isBlank(topic)) {
            namespace = topic = "unknown";
        } else {
            try {
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroup.java`
#### Snippet
```java
        rgRemoteUsageReportsMessages.labels(this.ruConsumer.getID(), monClass.name(), broker).inc(newMessageCount);

        oldByteCount = oldMessageCount = -1;
        if (oldUsageStats != null) {
            oldByteCount = oldUsageStats.usedValues.bytes;
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroup.java`
#### Snippet
```java
            bytesUsed = monEntity.usedLocallySinceLastReport.bytes;
            messagesUsed = monEntity.usedLocallySinceLastReport.messages;
            monEntity.usedLocallySinceLastReport.bytes = monEntity.usedLocallySinceLastReport.messages = 0;

            monEntity.totalUsedLocally.bytes += bytesUsed;
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupService.java`
#### Snippet
```java
        ServiceConfiguration config = this.pulsar.getConfiguration();
        long periodInSecs = config.getResourceUsageTransportPublishIntervalInSecs();
        this.aggregateLocalUsagePeriodInSeconds = this.resourceUsagePublishPeriodInSeconds = periodInSecs;
        this.aggregateLocalUsagePeriodicTask = this.pulsar.getExecutor().scheduleAtFixedRate(
                    catchingAndLoggingThrowables(this::aggregateResourceGroupLocalUsages),
```

### NestedAssignment
Result of assignment expression used
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/TwoPhaseCompactor.java`
#### Snippet
```java
                    if (keyAndSize == null) { // pass through messages without a key
                        messageToAdd = Optional.of(m);
                    } else if ((msg = latestForKey.get(keyAndSize.getLeft())) != null
                            && msg.equals(id)) { // consider message only if present into latestForKey map
                        if (keyAndSize.getRight() <= 0) {
```

### NestedAssignment
Result of assignment expression used
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarUnpacker.java`
#### Snippet
```java
            byte[] bytes = new byte[65536];
            int numRead;
            while ((numRead = in.read(bytes)) != -1) {
                fos.write(bytes, 0, numRead);
            }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarClassLoader.java`
#### Snippet
```java
            new InputStreamReader(new FileInputStream(serviceDefPath), StandardCharsets.UTF_8))) {
            String line;
            while ((line = reader.readLine()) != null) {
                line = line.trim();
                if (!line.isEmpty() && !line.startsWith("#")) {
```

### NestedAssignment
Result of assignment expression used
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
            while (true) {
                if (++i < 0) break;
                if ((i = nextSetBit(i)) < 0) break;
                int endOfRun = nextClearBit(i);
                do { b.append(", ").append(i); }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        ChunkedMessageCtx chunkedMsgCtx = null;
        String messageUUID;
        while ((messageUUID = pendingChunkedMessageUuidQueue.peek()) != null) {
            chunkedMsgCtx = StringUtils.isNotBlank(messageUUID) ? chunkedMessagesMap.get(messageUUID) : null;
            if (chunkedMsgCtx != null && System
```

### NestedAssignment
Result of assignment expression used
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/ByteBufPair.java`
#### Snippet
```java
        b1.release();
        b2.release();
        b1 = b2 = null;
        recyclerHandle.recycle(this);
    }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java

            // Jump to the first line after -----BEGIN [RSA] PRIVATE KEY-----
            while ((currentLine = reader.readLine()) != null && !currentLine.startsWith("-----BEGIN")) {
                reader.readLine();
            }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java

            // Stop (and skip) at the last line that has, say, -----END [RSA] PRIVATE KEY-----
            while ((currentLine = reader.readLine()) != null && !currentLine.startsWith("-----END")) {
                sb.append(currentLine);
            }
```

### NestedAssignment
Result of assignment expression used
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedInputStreamImpl.java`
#### Snippet
```java
        this.bufferSize = bufferSize;
        this.cursor = 0;
        this.bufferOffsetStart = this.bufferOffsetEnd = -1;
    }

```

### NestedAssignment
Result of assignment expression used
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedInputStreamImpl.java`
#### Snippet
```java
            buffer.readerIndex((int) newIndex);
        } else {
            bufferOffsetStart = bufferOffsetEnd = -1;
            this.cursor = position;
            buffer.clear();
```

### NestedAssignment
Result of assignment expression used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
        }
        Map<String, Assignment> worker;
        if (workerId != null && ((worker = workerIdToAssignments.get(workerId)) != null && worker.isEmpty())) {
            this.workerIdToAssignments.remove(workerId);
        }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/WorkerUtils.java`
#### Snippet
```java
            int read = 0;
            byte[] bytes = new byte[1024];
            while ((read = in.read(bytes)) != -1) {
                outputStream.write(bytes, 0, read);
            }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/WorkerUtils.java`
#### Snippet
```java
                    int read = 0;
                    byte[] bytes = new byte[1024];
                    while ((read = uploadedInputStream.read(bytes)) != -1) {
                        out.write(bytes, 0, read);
                    }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeUtils.java`
#### Snippet
```java
        BufferedReader rd = new BufferedReader(new InputStreamReader(conn.getInputStream()));
        String line;
        while ((line = rd.readLine()) != null) {
            result.append(line + System.lineSeparator());
        }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
    public static boolean isBlank(String str) {
        int strLen;
        if (str != null && (strLen = str.length()) != 0) {
            for (int i = 0; i < strLen; ++i) {
                if (!Character.isWhitespace(str.charAt(i))) {
```

### NestedAssignment
Result of assignment expression used
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
        }
        try {
            return localCluster = pulsarResources.getClusterResources().getCluster(config.getClusterName())
                    .orElseThrow(() -> new NotFoundException("Cluster " + config.getClusterName()));
        } catch (Exception e) {
            throw new PulsarServerException(e);
```

### NestedAssignment
Result of assignment expression used
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
        final DataInputStream inputStream = new DataInputStream(socket.getInputStream());
        int command;
        while ((command = inputStream.read()) != -1) {
            handle((byte) command, inputStream, new DataOutputStream(socket.getOutputStream()));
        }
```

### NestedAssignment
Result of assignment expression used
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/DLOutputStream.java`
#### Snippet
```java
            try {
                int read = 0;
                while ((read = inputStream.read(readBuffer)) != -1) {
                    log.info("write something into the ledgers offset: {}, length: {}", offset, read);
                    ByteBuf writeBuf = Unpooled.copiedBuffer(readBuffer, 0, read);
```

## RuleId[ruleID=SimplifiableBooleanExpression]
### SimplifiableBooleanExpression
`(null != client && isRecoverableException(e)) || null == client` can be simplified to 'null == client\|\|isRecoverableException(e)'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
                    boolean rethrow = true;
                    long elapsedTime = MathUtils.elapsedMSec(startTimeNanos);
                    if (((null != client && isRecoverableException(e)) || null == client)
                            && retryPolicy.allowRetry(attempts, elapsedTime)) {
                        rethrow = false;
```

## RuleId[ruleID=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `namespaces` are queried, but never updated
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarInitialNamespaceSetup.java`
#### Snippet
```java

        @Parameter(description = "tenant/namespace", required = true)
        private List<String> namespaces;

        @Parameter(names = { "-h", "--help" }, description = "Show this help message")
```

### MismatchedCollectionQueryUpdate
Contents of collection `args` are queried, but never updated
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java

        @Parameter(description = "The token string", arity = 1)
        private java.util.List<String> args;

        @Parameter(names = {"-i",
```

### MismatchedCollectionQueryUpdate
Contents of collection `args` are queried, but never updated
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java

        @Parameter(description = "The token string", arity = 1)
        private java.util.List<String> args;

        @Parameter(names = {"-i",
```

### MismatchedCollectionQueryUpdate
Contents of collection `commandNames` are queried, but never updated
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/tools/GenerateDocsCommand.java`
#### Snippet
```java
                description = "List of command names"
        )
        private List<String> commandNames = new ArrayList<>();
    }

```

### MismatchedCollectionQueryUpdate
Contents of collection `JAVA_CLASS_SCHEMA_TYPES` are updated, but never queried
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/SchemaUtils.java`
#### Snippet
```java
     * Maps the java classes to the corresponding {@link SchemaType}.
     */
    private static final Map<Class<?>, SchemaType> JAVA_CLASS_SCHEMA_TYPES = new HashMap<>();

    static {
```

### MismatchedCollectionQueryUpdate
Contents of collection `autoRecoveryProcesses` are updated, but never queried
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BKCluster.java`
#### Snippet
```java
    }

    private final Map<BookieServer, AutoRecoveryMain> autoRecoveryProcesses = new HashMap<>();

    @Getter
```

### MismatchedCollectionQueryUpdate
Contents of collection `commandNames` are queried, but never updated
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/CmdGenerateDocumentation.java`
#### Snippet
```java

        @Parameter(names = {"-n", "--command-names"}, description = "List of command names")
        private List<String> commandNames = new ArrayList<>();

    }
```

### MismatchedCollectionQueryUpdate
Contents of collection `params` are queried, but never updated
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
    private class CmdAllocatorStats extends CliCommand {
        @Parameter(description = "allocator-name", required = true)
        private List<String> params;

        @Override
```

### MismatchedCollectionQueryUpdate
Contents of collection `subTypes` are queried, but never updated
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--types", "-t"}, description = "Subscription types enabled list (comma separated values)."
                + " Possible values: (Exclusive, Shared, Failover, Key_Shared).", required = true)
        private List<String> subTypes;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### MismatchedCollectionQueryUpdate
Contents of collection `mainOptions` are queried, but never updated
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

    @Parameter(description = "TopicName", required = true)
    private List<String> mainOptions = new ArrayList<String>();

    @Parameter(names = { "-t", "--subscription-type" }, description = "Subscription type.")
```

### MismatchedCollectionQueryUpdate
Contents of collection `properties` are queried, but never updated
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
    @Parameter(names = { "-p", "--properties"}, description = "Properties to add, Comma separated "
            + "key=value string, like k1=v1,k2=v2.")
    private List<String> properties = new ArrayList<>();

    @Parameter(names = { "-k", "--key"}, description = "message key to add ")
```

### MismatchedCollectionQueryUpdate
Contents of collection `mainOptions` are queried, but never updated
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java

    @Parameter(description = "TopicName", required = true)
    private List<String> mainOptions;

    @Parameter(names = { "-m", "--messages" },
```

### MismatchedCollectionQueryUpdate
Contents of collection `subTypes` are queried, but never updated
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = {"--types", "-t"}, description = "Subscription types enabled list (comma separated values)."
                + " Possible values: (Exclusive, Shared, Failover, Key_Shared).", required = true)
        private List<String> subTypes;

        @Override
```

### MismatchedCollectionQueryUpdate
Contents of collection `clusters` are queried, but never updated
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--clusters", "-c" },
                description = "List of clusters this namespace will be assigned", required = false)
        private java.util.List<String> clusters;

        @Parameter(names = { "--bundles", "-b" }, description = "number of bundles to activate", required = false)
```

### MismatchedCollectionQueryUpdate
Contents of collection `subTypes` are queried, but never updated
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = {"--types", "-t"}, description = "Subscription types enabled list (comma separated values)."
                + " Possible values: (Exclusive, Shared, Failover, Key_Shared).", required = true)
        private List<String> subTypes;

        @Override
```

## RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `waitObject` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/nsq/src/main/java/org/apache/pulsar/io/nsq/NSQSource.java`
#### Snippet
```java
    private Subscriber subscriber;

    private Object waitObject;

    @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `counter` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/sink/SourceOfFlume.java`
#### Snippet
```java
    private int maxBatchDurationMillis;

    private SourceCounter counter;

    private final List<Event> eventList = new ArrayList<Event>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lifecycleState` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/PollingPropertiesFileConfigurationProvider.java`
#### Snippet
```java
    private final int interval;
    private final CounterGroup counterGroup;
    private LifecycleState lifecycleState;

    private ScheduledExecutorService executorService;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `incomingList` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java

    // for flush
    private Deque<Record<T>> incomingList;
    private AtomicBoolean isFlushing;
    private int batchSize;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `batchSize` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java
    private Deque<Record<T>> incomingList;
    private AtomicBoolean isFlushing;
    private int batchSize;
    private ScheduledExecutorService flushExecutor;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `counter` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/SinkOfFlume.java`
#### Snippet
```java
    private long batchSize;

    private SinkCounter counter = null;

    @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectorConfig` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConnector.java`
#### Snippet
```java
    // Hadoop Configuration, Filesystem, and UserGroupInformation (optional)
    protected final AtomicReference<HdfsResources> hdfsResources = new AtomicReference<>();
    protected AbstractHdfsConfig connectorConfig;
    protected CompressionCodecFactory compressionCodecFactory;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectorConfig` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConnector.java`
#### Snippet
```java
    // Hadoop Configuration, Filesystem, and UserGroupInformation (optional)
    protected final AtomicReference<HdfsResources> hdfsResources = new AtomicReference<>();
    protected AbstractHdfsConfig connectorConfig;
    protected CompressionCodecFactory compressionCodecFactory;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `incomingList` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseAbstractSink.java`
#### Snippet
```java
    private long batchTimeMs;
    private int batchSize;
    private List<Record<T>> incomingList;
    private ScheduledExecutorService flushExecutor;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `incomingList` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
    private MongoCollection<Document> collection;

    private List<Record<byte[]>> incomingList;

    private ScheduledExecutorService flushExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `incomingList` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSink.java`
#### Snippet
```java
    private int batchSize;

    private List<Record<byte[]>> incomingList;

    private ScheduledExecutorService flushExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `waitObject` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHose.java`
#### Snippet
```java

    // ----- Runtime fields
    private Object waitObject;

    private final ObjectMapper mapper = new ObjectMapper().configure(
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `incomingList` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/BatchSink.java`
#### Snippet
```java
public abstract class BatchSink<T, R> implements Sink<R> {
    private int batchSize;
    private List<Record<R>> incomingList;
    private ScheduledExecutorService flushExecutor;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentBatch` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java

    // pulsar io related variables
    private Iterator<SourceRecord> currentBatch = null;
    private CompletableFuture<Void> flushFuture;
    private OffsetBackingStore offsetStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `flushFuture` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
    // pulsar io related variables
    private Iterator<SourceRecord> currentBatch = null;
    private CompletableFuture<Void> flushFuture;
    private OffsetBackingStore offsetStore;
    private OffsetStorageReader offsetReader;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceTask` is accessed in both synchronized and unsynchronized contexts
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
    private SourceTaskContext sourceTaskContext;
    @Getter
    private SourceTask sourceTask;
    public Converter keyConverter;
    public Converter valueConverter;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `defaultResponse` is accessed in both synchronized and unsynchronized contexts
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
    final AtomicLong sequence = new AtomicLong(3);

    CompletableFuture<Void> defaultResponse = CompletableFuture.completedFuture(null);
    private static final List<BookieId> ensemble = Collections.unmodifiableList(Lists.newArrayList(
            new BookieSocketAddress("192.0.2.1", 1234).toBookieId(),
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `instance` is accessed in both synchronized and unsynchronized contexts
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java

    @VisibleForTesting
    static PulsarConnectorCache instance;

    private final MetadataStoreExtended metadataStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `offloaderScheduler` is accessed in both synchronized and unsynchronized contexts
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java

    private final StatsProvider statsProvider;
    private OrderedScheduler offloaderScheduler;
    private final LedgerOffloaderStats offloaderStats;
    private OffloadersCache offloadersCache = new OffloadersCache();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `persistentWatchers` is accessed in both synchronized and unsynchronized contexts
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java
    }

    private List<PersistentWatcher> persistentWatchers;

    public static MockZooKeeper newInstance() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectionPool` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(ProxyConnection.class);
    // ConnectionPool is used by the proxy to issue lookup requests
    private ConnectionPool connectionPool;
    private final AtomicLong requestIdGenerator =
            new AtomicLong(ThreadLocalRandom.current().nextLong(0, Long.MAX_VALUE / 2));
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lookupProxyHandler` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private State state;

    private LookupProxyHandler lookupProxyHandler = null;
    @Getter
    private DirectProxyHandler directProxyHandler = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `hasProxyToBrokerUrl` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    AuthenticationState authState;
    private ClientConfigurationData clientConf;
    private boolean hasProxyToBrokerUrl;
    private int protocolVersionToAdvertise;
    private String proxyToBrokerUrl;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `directProxyHandler` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private LookupProxyHandler lookupProxyHandler = null;
    @Getter
    private DirectProxyHandler directProxyHandler = null;
    private final BrokerProxyValidator brokerProxyValidator;
    private final ConnectionController connectionController;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `protocolVersionToAdvertise` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private ClientConfigurationData clientConf;
    private boolean hasProxyToBrokerUrl;
    private int protocolVersionToAdvertise;
    private String proxyToBrokerUrl;
    private HAProxyMessage haProxyMessage;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `authMethod` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    String clientAuthMethod;

    private String authMethod = "none";
    AuthenticationProvider authenticationProvider;
    AuthenticationState authState;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientConf` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    AuthenticationProvider authenticationProvider;
    AuthenticationState authState;
    private ClientConfigurationData clientConf;
    private boolean hasProxyToBrokerUrl;
    private int protocolVersionToAdvertise;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientAuthData` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private final ConnectionController connectionController;
    String clientAuthRole;
    AuthData clientAuthData;
    String clientAuthMethod;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `proxyToBrokerUrl` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private boolean hasProxyToBrokerUrl;
    private int protocolVersionToAdvertise;
    private String proxyToBrokerUrl;
    private HAProxyMessage haProxyMessage;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientAuthMethod` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    String clientAuthRole;
    AuthData clientAuthData;
    String clientAuthMethod;

    private String authMethod = "none";
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientAuthRole` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private final BrokerProxyValidator brokerProxyValidator;
    private final ConnectionController connectionController;
    String clientAuthRole;
    AuthData clientAuthData;
    String clientAuthMethod;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `state` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private final DnsAddressResolverGroup dnsAddressResolverGroup;
    AuthenticationDataSource authenticationData;
    private State state;

    private LookupProxyHandler lookupProxyHandler = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metricsServlet` is accessed in both synchronized and unsynchronized contexts
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java
    private AdditionalServlets proxyAdditionalServlets;

    private PrometheusMetricsServlet metricsServlet;
    private List<PrometheusRawMetricsProvider> pendingMetricsProviders;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `timeout` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/AbstractDelayedDeliveryTracker.java`
#### Snippet
```java

    // Current timeout or null if not set
    protected Timeout timeout;

    // Timestamp at which the timeout is currently set
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastTickRun` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/AbstractDelayedDeliveryTracker.java`
#### Snippet
```java

    // Last time the TimerTask was triggered for this class
    private long lastTickRun;

    protected long tickTimeMillis;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentTimeoutTarget` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/AbstractDelayedDeliveryTracker.java`
#### Snippet
```java

    // Timestamp at which the timeout is currently set
    private long currentTimeoutTarget;

    // Last time the TimerTask was triggered for this class
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `brokerServiceUrl` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private String webServiceAddress;
    private String webServiceAddressTls;
    private String brokerServiceUrl;
    private String brokerServiceUrlTls;
    private final String brokerVersion;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `webServiceAddressTls` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private final String advertisedAddress;
    private String webServiceAddress;
    private String webServiceAddressTls;
    private String brokerServiceUrl;
    private String brokerServiceUrlTls;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `offloaderScheduler` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private final ScheduledExecutorService loadManagerExecutor;
    private ScheduledExecutorService compactorExecutor;
    private OrderedScheduler offloaderScheduler;
    private OffloadersCache offloadersCache = new OffloadersCache();
    private LedgerOffloader defaultOffloader;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `compactorExecutor` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private OrderedExecutor orderedExecutor;
    private final ScheduledExecutorService loadManagerExecutor;
    private ScheduledExecutorService compactorExecutor;
    private OrderedScheduler offloaderScheduler;
    private OffloadersCache offloadersCache = new OffloadersCache();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `config` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(PulsarService.class);
    private static final double GRACEFUL_SHUTDOWN_TIMEOUT_RATIO_OF_TOTAL_TIMEOUT = 0.5d;
    private ServiceConfiguration config = null;
    private NamespaceService nsService = null;
    private ManagedLedgerStorage managedLedgerClientFactory = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `offloadersCache` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private ScheduledExecutorService compactorExecutor;
    private OrderedScheduler offloaderScheduler;
    private OffloadersCache offloadersCache = new OffloadersCache();
    private LedgerOffloader defaultOffloader;
    private LedgerOffloaderStats offloaderStats;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `client` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private final AtomicReference<LoadManager> loadManager = new AtomicReference<>();
    private PulsarAdmin adminClient = null;
    private PulsarClient client = null;
    private final String bindAddress;
    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `offloaderStats` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private OffloadersCache offloadersCache = new OffloadersCache();
    private LedgerOffloader defaultOffloader;
    private LedgerOffloaderStats offloaderStats;
    private Map<NamespaceName, LedgerOffloader> ledgerOffloaderMap = new ConcurrentHashMap<>();
    private ScheduledFuture<?> loadReportTask = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `webServiceAddress` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
     */
    private final String advertisedAddress;
    private String webServiceAddress;
    private String webServiceAddressTls;
    private String brokerServiceUrl;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `compactor` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private TopicPoliciesService topicPoliciesService = TopicPoliciesService.DISABLED;
    private BookKeeperClientFactory bkClientFactory;
    private Compactor compactor;
    private ResourceUsageTransportManager resourceUsageTransportManager;
    private ResourceGroupService resourceGroupServiceManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metricsServlet` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    // packages management service
    private Optional<PackagesManagement> packagesManagement = Optional.empty();
    private PulsarPrometheusMetricsServlet metricsServlet;
    private List<PrometheusRawMetricsProvider> pendingMetricsProviders;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `schemaStorage` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private String brokerServiceUrlTls;
    private final String brokerVersion;
    private SchemaStorage schemaStorage = null;
    private SchemaRegistryService schemaRegistryService = null;
    private final WorkerConfig workerConfig;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `brokerServiceUrlTls` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private String webServiceAddressTls;
    private String brokerServiceUrl;
    private String brokerServiceUrlTls;
    private final String brokerVersion;
    private SchemaStorage schemaStorage = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `adminClient` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private ScheduledFuture<?> loadResourceQuotaTask = null;
    private final AtomicReference<LoadManager> loadManager = new AtomicReference<>();
    private PulsarAdmin adminClient = null;
    private PulsarClient client = null;
    private final String bindAddress;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isClosed` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
            .newUpdater(Producer.class, "pendingPublishAcks");

    private boolean isClosed = false;
    private final CompletableFuture<Void> closeFuture;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metricsCollection` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarStats.java`
#### Snippet
```java
    private Map<String, NamespaceBundleStats> bundleStats;
    private List<Metrics> tempMetricsCollection;
    private List<Metrics> metricsCollection;
    private List<NonPersistentTopic> tempNonPersistentTopics;
    private final BrokerOperabilityMetrics brokerOperabilityMetrics;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tempMetricsCollection` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarStats.java`
#### Snippet
```java
    private final ClusterReplicationMetrics clusterReplicationMetrics;
    private Map<String, NamespaceBundleStats> bundleStats;
    private List<Metrics> tempMetricsCollection;
    private List<Metrics> metricsCollection;
    private List<NonPersistentTopic> tempNonPersistentTopics;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bundleStats` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarStats.java`
#### Snippet
```java
    private NamespaceStats nsStats;
    private final ClusterReplicationMetrics clusterReplicationMetrics;
    private Map<String, NamespaceBundleStats> bundleStats;
    private List<Metrics> tempMetricsCollection;
    private List<Metrics> metricsCollection;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replicatorId` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
    protected final PulsarClientImpl replicationClient;
    protected final PulsarClientImpl client;
    protected String replicatorId;

    protected volatile ProducerImpl producer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `preciseTopicPublishRateLimitingEnable` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    protected volatile ResourceGroupPublishLimiter resourceGroupPublishLimiter;

    protected boolean preciseTopicPublishRateLimitingEnable;

    @Getter
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dispatchRateLimiterOnByte` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/DispatchRateLimiter.java`
#### Snippet
```java
    private final BrokerService brokerService;
    private RateLimiter dispatchRateLimiterOnMessage;
    private RateLimiter dispatchRateLimiterOnByte;

    public DispatchRateLimiter(PersistentTopic topic, Type type) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dispatchRateLimiterOnMessage` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/DispatchRateLimiter.java`
#### Snippet
```java

    private final BrokerService brokerService;
    private RateLimiter dispatchRateLimiterOnMessage;
    private RateLimiter dispatchRateLimiterOnByte;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subscribeRate` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/SubscribeRateLimiter.java`
#### Snippet
```java
    private final ScheduledExecutorService executorService;
    private ScheduledFuture<?> resetTask;
    private SubscribeRate subscribeRate;

    public SubscribeRateLimiter(PersistentTopic topic) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subscribeRateLimiter` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/SubscribeRateLimiter.java`
#### Snippet
```java
    private final String topicName;
    private final BrokerService brokerService;
    private ConcurrentHashMap<ConsumerIdentifier, RateLimiter> subscribeRateLimiter;
    private final ScheduledExecutorService executorService;
    private ScheduledFuture<?> resetTask;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `managedCursor` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/MessageDeduplication.java`
#### Snippet
```java
    private final PersistentTopic topic;
    private final ManagedLedger managedLedger;
    private ManagedCursor managedCursor;

    enum Status {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `startTimeMillis` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/ReplicatedSubscriptionsSnapshotBuilder.java`
#### Snippet
```java
    private boolean firstRoundComplete;

    private long startTimeMillis;
    private final long timeoutMillis;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dispatchRateLimiter` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
    protected final ManagedCursor cursor;

    protected Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();
    private final Object dispatchRateLimiterLock = new Object();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentReadSizeByte` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
    private final PersistentTopic topic;

    private AtomicInteger currentReadSizeByte = new AtomicInteger(0);

    private volatile State state;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pendingReads` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java

    // Queue for read request that's wait for new entries from managed ledger.
    private ConcurrentLinkedQueue<PendingReadEntryRequest> pendingReads = new ConcurrentLinkedQueue<>();

    private final ManagedCursorImpl cursor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `issuedReads` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java

    // Queue for read request issued yet waiting for complete from managed ledger.
    private ConcurrentLinkedQueue<PendingReadEntryRequest> issuedReads = new ConcurrentLinkedQueue<>();

    // Queue for read request that's wait for new entries from managed ledger.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `delayedDeliveryTracker` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    protected final RedeliveryTracker redeliveryTracker;

    private Optional<DelayedDeliveryTracker> delayedDeliveryTracker = Optional.empty();

    protected volatile boolean havePendingRead = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `selfOwnerInfo` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/OwnershipCache.java`
#### Snippet
```java
     * The NamespaceEphemeralData objects that can be associated with the current owner.
     */
    private NamespaceEphemeralData selfOwnerInfo;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `systemBean` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/GenericBrokerHostUsageImpl.java`
#### Snippet
```java
    private double cpuUsageSum = 0d;
    private int cpuUsageCount = 0;
    private OperatingSystemMXBean systemBean;
    private SystemResourceUsage usage;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `realtimeAvgResourceQuota` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private double realtimeMemoryLoadFactor = 25.0;
    // realtime average resource quota
    private ResourceQuota realtimeAvgResourceQuota = null;
    // realtime resource quota calculated from the latest load reports
    private AtomicReference<Map<String, ResourceQuota>> realtimeResourceQuotas = new AtomicReference<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `resourceUnitRankings` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private Map<ResourceUnit, LoadReport> currentLoadReports;
    // load ranking for each broker from multiple perspective
    private Map<ResourceUnit, ResourceUnitRanking> resourceUnitRankings;
    // sorted load ranking on one single dimension
    private AtomicReference<Map<Long, Set<ResourceUnit>>> sortedRankings = new AtomicReference<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `loadBalancingMetrics` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private long brokerRotationCursor = 0;
    // load balancing metrics
    private AtomicReference<List<Metrics>> loadBalancingMetrics = new AtomicReference<>();

    // Cache of brokers to be used in applying policies and determining final candidates.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `loadReports` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private final PlacementStrategy placementStrategy;

    private LockManager<LoadReport> loadReports;
    private ResourceLock<LoadReport> brokerLock;
    private MetadataCache<Map<String, String>> dynamicConfigurationCache;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `realtimeCpuLoadFactor` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java

    // CPU usage per msg/sec
    private double realtimeCpuLoadFactor = 0.025;
    // memory usage per 500 (topics + producers + consumers)
    private double realtimeMemoryLoadFactor = 25.0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sortedRankings` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private Map<ResourceUnit, ResourceUnitRanking> resourceUnitRankings;
    // sorted load ranking on one single dimension
    private AtomicReference<Map<Long, Set<ResourceUnit>>> sortedRankings = new AtomicReference<>();
    // rotation cursor between brokers
    private long brokerRotationCursor = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pulsar` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private static final Logger log = LoggerFactory.getLogger(SimpleLoadManagerImpl.class);
    private SimpleResourceAllocationPolicies policies;
    private PulsarService pulsar;

    // average JVM heap usage for
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `policies` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java

    private static final Logger log = LoggerFactory.getLogger(SimpleLoadManagerImpl.class);
    private SimpleResourceAllocationPolicies policies;
    private PulsarService pulsar;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentLoadReports` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private long avgJvmHeapUsageMBytes = 0;
    // load report got from each broker
    private Map<ResourceUnit, LoadReport> currentLoadReports;
    // load ranking for each broker from multiple perspective
    private Map<ResourceUnit, ResourceUnitRanking> resourceUnitRankings;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `realtimeMemoryLoadFactor` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private double realtimeCpuLoadFactor = 0.025;
    // memory usage per 500 (topics + producers + consumers)
    private double realtimeMemoryLoadFactor = 25.0;
    // realtime average resource quota
    private ResourceQuota realtimeAvgResourceQuota = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastResourceQuotaUpdateTimestamp` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private AtomicReference<Map<String, ResourceQuota>> realtimeResourceQuotas = new AtomicReference<>();
    // timestamp when the resource quotas were re-calculated
    private long lastResourceQuotaUpdateTimestamp = -1;

    public static final long RESOURCE_QUOTA_GO_UP_TIMEWINDOW = TimeUnit.MINUTES.toMillis(30);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `realtimeResourceQuotas` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private ResourceQuota realtimeAvgResourceQuota = null;
    // realtime resource quota calculated from the latest load reports
    private AtomicReference<Map<String, ResourceQuota>> realtimeResourceQuotas = new AtomicReference<>();
    // timestamp when the resource quotas were re-calculated
    private long lastResourceQuotaUpdateTimestamp = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `instance` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TransactionBufferClientStatsImpl.java`
#### Snippet
```java
    private final boolean exposeTopicLevelMetrics;

    private static TransactionBufferClientStats instance;

    private TransactionBufferClientStatsImpl(boolean exposeTopicLevelMetrics,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `conf` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java

    // Service configuration belonging to the pulsar service.
    private ServiceConfiguration conf;

    // The default bundle stats which are used to initialize historic data.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `policies` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java

    // Policies used to determine which brokers are available for particular namespaces.
    private SimpleResourceAllocationPolicies policies;

    // Pulsar service used to initialize this.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bundleSplitStrategy` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java

    // Strategy to use for splitting bundles.
    private BundleSplitStrategy bundleSplitStrategy;

    // Service configuration belonging to the pulsar service.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `brokerToFailureDomainMap` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
    private final BrokerTopicLoadingPredicate brokerTopicLoadingPredicate;

    private Map<String, String> brokerToFailureDomainMap;

    private SessionEvent lastMetadataSessionEvent = SessionEvent.Reconnected;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pulsar` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java

    // Pulsar service used to initialize this.
    private PulsarService pulsar;

    // Executor service used to regularly update broker data.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bundleUnloadMetrics` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
    private AtomicReference<List<Metrics>> loadBalancingMetrics = new AtomicReference<>();
    // record bundle unload metrics
    private AtomicReference<List<Metrics>> bundleUnloadMetrics = new AtomicReference<>();
    // record bundle split metrics
    private AtomicReference<List<Metrics>> bundleSplitMetrics = new AtomicReference<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `localData` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java

    // Local data for the broker this is running on.
    private LocalBrokerData localData;

    // Load data comprising data available for each broker.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bundleSplitMetrics` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
    private AtomicReference<List<Metrics>> bundleUnloadMetrics = new AtomicReference<>();
    // record bundle split metrics
    private AtomicReference<List<Metrics>> bundleSplitMetrics = new AtomicReference<>();
    // record bundle metrics
    private AtomicReference<List<Metrics>> bundleMetrics = new AtomicReference<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `placementStrategy` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java

    // Strategy used to determine where new topics should be placed.
    private ModularLoadManagerStrategy placementStrategy;

    // Policies used to determine which brokers are available for particular namespaces.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `committedAtLedgerId` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
        private final SortedMap<Long, ByteBuf> entries;
        private TxnStatus status;
        private long committedAtLedgerId = -1L;
        private long committedAtEntryId = -1L;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `committedAtEntryId` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
        private TxnStatus status;
        private long committedAtLedgerId = -1L;
        private long committedAtEntryId = -1L;

        TxnBuffer(TxnID txnid) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `status` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
        private final TxnID txnid;
        private final SortedMap<Long, ByteBuf> entries;
        private TxnStatus status;
        private long committedAtLedgerId = -1L;
        private long committedAtEntryId = -1L;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentTimeout` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/timeout/TransactionTimeoutTrackerImpl.java`
#### Snippet
```java
    private final long tickTimeMillis;
    private final Clock clock;
    private Timeout currentTimeout;
    private static final long INITIAL_TIMEOUT = 1L;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentCompaction` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

    private static final long COMPACTION_NEVER_RUN = -0xfebecffeL;
    private CompletableFuture<Long> currentCompaction = CompletableFuture.completedFuture(COMPACTION_NEVER_RUN);
    private final CompactedTopic compactedTopic;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dispatchRateLimiter` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    public boolean msgChunkPublished;

    private Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();
    private final Object dispatchRateLimiterLock = new Object();
    private Optional<SubscribeRateLimiter> subscribeRateLimiter = Optional.empty();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subscribeRateLimiter` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    private Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();
    private final Object dispatchRateLimiterLock = new Object();
    private Optional<SubscribeRateLimiter> subscribeRateLimiter = Optional.empty();
    private final long backloggedCursorThresholdEntries;
    public static final int MESSAGE_RATE_BACKOFF_MS = 1000;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `compactedTopicContext` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactedTopicImpl.java`
#### Snippet
```java

    private PositionImpl compactionHorizon = null;
    private CompletableFuture<CompactedTopicContext> compactedTopicContext = null;

    public CompactedTopicImpl(BookKeeper bk) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `compactionHorizon` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactedTopicImpl.java`
#### Snippet
```java
    private final BookKeeper bk;

    private PositionImpl compactionHorizon = null;
    private CompletableFuture<CompactedTopicContext> compactedTopicContext = null;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `individualAckPositions` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
     *         If it does not exits the map, the position will be added to the map.
     */
    private ConcurrentSkipListMap<PositionImpl, MutablePair<PositionImpl, Integer>> individualAckPositions;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `individualAckOfTransaction` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
     *     two positions bit set.
     */
    private LinkedMap<TxnID, HashMap<PositionImpl, PositionImpl>> individualAckOfTransaction;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pendingAckStoreFuture` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
    private final PersistentSubscription persistentSubscription;

    private CompletableFuture<PendingAckStore> pendingAckStoreFuture;

    private final CompletableFuture<PendingAckHandle> pendingAckHandleCompletableFuture = new CompletableFuture<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bitSetRecyclable` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java

    private volatile MessageIdImpl messageId = DEFAULT_MESSAGE_ID;
    private BitSetRecyclable bitSetRecyclable = null;
    private boolean flushRequired = false;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nackedMessages` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/NegativeAcksTracker.java`
#### Snippet
```java
class NegativeAcksTracker implements Closeable {

    private HashMap<MessageId, Long> nackedMessages = null;

    private final ConsumerBase<?> consumer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lookup` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java

    private final boolean createdScheduledProviders;
    private LookupService lookup;
    private final ConnectionPool cnxPool;
    @Getter
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `flow` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/AuthenticationOAuth2.java`
#### Snippet
```java

    final Clock clock;
    Flow flow;
    transient CachedToken cachedToken;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `properties` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java

    private String topic; // only set for incoming messages
    private transient Map<String, String> properties;
    private int redeliveryCount;
    private int uncompressedSize;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectedSince` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java

    private String connectionId;
    private String connectedSince;
    private final int partitionIndex;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `batchFlushTask` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    // batchingMaxPublishDelayMicros to send a batch.
    // Only update from within synchronized block on this producer.
    private ScheduledFuture<?> batchFlushTask;
    // The time, in nanos, of the last batch send. This field ensures that we don't deliver batches via the
    // batchFlushTask before the batchingMaxPublishDelayMicros duration has passed.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastBatchSendNanoTime` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    // The time, in nanos, of the last batch send. This field ensures that we don't deliver batches via the
    // batchFlushTask before the batchingMaxPublishDelayMicros duration has passed.
    private long lastBatchSendNanoTime;

    private Optional<Long> topicEpoch = Optional.empty();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `producerName` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java

    // Globally unique producer name
    private String producerName;
    private final boolean userProvidedProducerName;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectionId` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private final boolean userProvidedProducerName;

    private String connectionId;
    private String connectedSince;
    private final int partitionIndex;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastSendFuture` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java

    private final BatchMessageContainerBase batchMessageContainer;
    private CompletableFuture<MessageId> lastSendFuture = CompletableFuture.completedFuture(null);
    private LastSendFutureWrapper lastSendFutureWrapper = LastSendFutureWrapper.create(lastSendFuture);

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastSendFutureWrapper` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private final BatchMessageContainerBase batchMessageContainer;
    private CompletableFuture<MessageId> lastSendFuture = CompletableFuture.completedFuture(null);
    private LastSendFutureWrapper lastSendFutureWrapper = LastSendFutureWrapper.create(lastSendFuture);

    // Globally unique producer name
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `topicEpoch` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private long lastBatchSendNanoTime;

    private Optional<Long> topicEpoch = Optional.empty();
    private final List<Throwable> previousExceptions = new CopyOnWriteArrayList<Throwable>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `rateTime` is accessed in both synchronized and unsynchronized contexts
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/RateLimiter.java`
#### Snippet
```java
public class RateLimiter implements AutoCloseable{
    private final ScheduledExecutorService executorService;
    private long rateTime;
    private TimeUnit timeUnit;
    private final boolean externalExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `timeUnit` is accessed in both synchronized and unsynchronized contexts
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/RateLimiter.java`
#### Snippet
```java
    private final ScheduledExecutorService executorService;
    private long rateTime;
    private TimeUnit timeUnit;
    private final boolean externalExecutor;
    private ScheduledFuture<?> renewTask;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `capacity` is accessed in both synchronized and unsynchronized contexts
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java

    private long[] data;
    private int capacity;
    private static final AtomicIntegerFieldUpdater<GrowablePriorityLongPairQueue> SIZE_UPDATER =
            AtomicIntegerFieldUpdater.newUpdater(GrowablePriorityLongPairQueue.class, "size");
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tlsKeyStore` is accessed in both synchronized and unsynchronized contexts
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
    protected String tlsKeyStoreType;
    protected String tlsKeyStorePassword;
    protected FileModifiedTimeUpdater tlsKeyStore;

    protected AuthenticationDataProvider authData;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `instance` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/LedgerOffloaderStatsImpl.java`
#### Snippet
```java


    private static LedgerOffloaderStats instance;
    public static synchronized LedgerOffloaderStats getInstance(boolean exposeTopicLevelMetrics,
                                                                ScheduledExecutorService scheduler, int interval) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `size` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/RangeCache.java`
#### Snippet
```java
    // Map from key to nodes inside the linked list
    private final ConcurrentNavigableMap<Key, Value> entries;
    private AtomicLong size; // Total size of values stored in cache
    private final Weighter<Value> weighter; // Weighter object used to extract the size from values
    private final TimestampExtractor<Value> timestampExtractor; // Extract the timestamp associated with a value
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `paused` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    AtomicInteger allTopicPartitionsNumber;

    private boolean paused = false;
    private final Object pauseMutex = new Object();
    // timeout related to auto check and subscribe partition increasement
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `unAckedMessageTracker` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    CompletableFuture<Void> partitionsAutoUpdateFuture = null;
    private final MultiTopicConsumerStatsRecorderImpl stats;
    private UnAckedMessageTracker unAckedMessageTracker;
    private final ConsumerConfigurationData<T> internalConfig;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bufferLength` is accessed in both synchronized and unsynchronized contexts
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
    private final ConcurrentMap<BlobStoreLocation, BlobStore> blobStores = new ConcurrentHashMap<>();
    private OffloadSegmentInfoImpl segmentInfo;
    private AtomicLong bufferLength = new AtomicLong(0);
    private AtomicLong segmentLength = new AtomicLong(0);
    private final long maxBufferLength;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `segmentInfo` is accessed in both synchronized and unsynchronized contexts
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java

    private final ConcurrentMap<BlobStoreLocation, BlobStore> blobStores = new ConcurrentHashMap<>();
    private OffloadSegmentInfoImpl segmentInfo;
    private AtomicLong bufferLength = new AtomicLong(0);
    private AtomicLong segmentLength = new AtomicLong(0);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `reader` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionAssignmentTailer.java`
#### Snippet
```java
    private final WorkerConfig workerConfig;
    private final ErrorNotifier errorNotifier;
    private Reader<byte[]> reader;
    private volatile boolean isRunning = false;
    private volatile boolean exitOnEndOfTopic = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tailerThread` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionAssignmentTailer.java`
#### Snippet
```java
    private volatile boolean exitOnEndOfTopic = false;
    private CompletableFuture<Void> exitFuture;
    private Thread tailerThread;

    @Getter
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastMessageId` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionAssignmentTailer.java`
#### Snippet
```java

    @Getter
    private MessageId lastMessageId = null;

    public FunctionAssignmentTailer(
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `exitFuture` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionAssignmentTailer.java`
#### Snippet
```java
    private volatile boolean isRunning = false;
    private volatile boolean exitOnEndOfTopic = false;
    private CompletableFuture<Void> exitFuture;
    private Thread tailerThread;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `exclusiveLeaderProducer` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionMetaDataManager.java`
#### Snippet
```java
    // Note that this variable serves a double duty. A non-null value
    // implies we are the leader, while a null value means we are not the leader
    private Producer exclusiveLeaderProducer;
    @Getter
    private volatile MessageId lastMessageSeen = MessageId.earliest;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `functionMetaDataTopicTailer` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionMetaDataManager.java`
#### Snippet
```java
    private final ErrorNotifier errorNotifier;

    private FunctionMetaDataTopicTailer functionMetaDataTopicTailer;
    // The producer of the metadata topic when we are the leader.
    // Note that this variable serves a double duty. A non-null value
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `functionActioner` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
    @Setter
    @Getter
    private FunctionActioner functionActioner;

    @Getter
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `workerIdToAssignments` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
    // all assignments
    // WorkerId -> Function Fully Qualified InstanceId -> List<Assignments>
    Map<String, Map<String, Assignment>> workerIdToAssignments = new ConcurrentHashMap<>();

    // All the runtime info related to functions executed by this worker
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `runtimeFactory` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java

    @Getter
    private RuntimeFactory runtimeFactory;

    private MembershipManager membershipManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isInitializePhase` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
    private PulsarWorkerService workerService;

    boolean isInitializePhase = false;

    @Getter
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `membershipManager` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
    private RuntimeFactory runtimeFactory;

    private MembershipManager membershipManager;

    private final PulsarAdmin functionAdmin;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `executorService` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/SchedulerManager.java`
#### Snippet
```java
    private final ErrorNotifier errorNotifier;
    private final WorkerStatsManager workerStatsManager;
    private ThreadPoolExecutor executorService;
    private final PulsarClient pulsarClient;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `exclusiveProducer` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/SchedulerManager.java`
#### Snippet
```java
    private final IScheduler scheduler;

    private Producer<byte[]> exclusiveProducer;

    private ScheduledExecutorService scheduledExecutorService;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastMessageProduced` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/SchedulerManager.java`
#### Snippet
```java

    @Getter
    private MessageId lastMessageProduced = null;

    private MessageId metadataTopicLastMessage = MessageId.earliest;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `instance` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceCache.java`
#### Snippet
```java
public class InstanceCache {

    private static InstanceCache instance;

    @Getter
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceContext` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java

  private Map<String, Object> config;
  private SourceContext sourceContext;
  private BatchSourceTriggerer discoveryTriggerer; // Only init in instance 0
  private Consumer<byte[]> intermediateTopicConsumer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `intermediateTopicName` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java
  private String batchSourceClassName;
  private BatchSource<T> batchSource;
  private String intermediateTopicName;
  private volatile Exception currentError = null;
  private volatile boolean isRunning = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `discoveryThread` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java
  private volatile Exception currentError = null;
  private volatile boolean isRunning = false;
  private ExecutorService discoveryThread;

  @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `instanceConfig` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    private ManagedChannel[] channel;
    private InstanceControlGrpc.InstanceControlFutureStub[] stub;
    private InstanceConfig instanceConfig;
    private final String jobNamespace;
    private final String jobName;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `grpcPort` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    private final Optional<KubernetesFunctionAuthProvider> functionAuthDataCacheProvider;
    private final AuthenticationConfig authConfig;
    private Integer grpcPort;
    private Integer metricsPort;
    private String narExtractionDirectory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stub` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    @Getter
    private ManagedChannel[] channel;
    private InstanceControlGrpc.InstanceControlFutureStub[] stub;
    private InstanceConfig instanceConfig;
    private final String jobNamespace;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channel` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    private List<String> processArgs;
    @Getter
    private ManagedChannel[] channel;
    private InstanceControlGrpc.InstanceControlFutureStub[] stub;
    private InstanceConfig instanceConfig;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stateManager` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    private final String stateStorageServiceUrl;
    private StateStoreProvider stateStoreProvider;
    private StateManager stateManager;

    private JavaInstance javaInstance;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `collectorRegistry` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    private final SecretsProvider secretsProvider;

    private FunctionCollectorRegistry collectorRegistry;
    private final String[] metricsLabels;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stats` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java

    // function stats
    private ComponentStatsManager stats;

    private Record<?> currentRecord;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sinkSchemaInfoProvider` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    private Class<?> sinkTypeArg;
    private final AtomicReference<Schema<?>> sinkSchema = new AtomicReference<>();
    private SinkSchemaInfoProvider sinkSchemaInfoProvider = null;

    public JavaInstanceRunnable(InstanceConfig instanceConfig,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `source` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    private Record<?> currentRecord;

    private Source source;
    private Sink sink;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sink` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java

    private Source source;
    private Sink sink;

    private final SecretsProvider secretsProvider;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isInitialized` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    // a flog to determine if member variables have been initialized as part of setup().
    // used for out of band API calls like operations involving stats
    private transient boolean isInitialized = false;

    // a read write lock for stats operations
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sinkTypeArg` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    private final ReadWriteLock statsLock = new ReentrantReadWriteLock();

    private Class<?> sinkTypeArg;
    private final AtomicReference<Schema<?>> sinkSchema = new AtomicReference<>();
    private SinkSchemaInfoProvider sinkSchemaInfoProvider = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `javaInstance` is accessed in both synchronized and unsynchronized contexts
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    private StateManager stateManager;

    private JavaInstance javaInstance;
    @Getter
    private Throwable deathException;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pulsarResources` is accessed in both synchronized and unsynchronized contexts
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java

    private final ScheduledExecutorService executor;
    private PulsarResources pulsarResources;
    private MetadataStoreExtended configMetadataStore;
    private ServiceConfiguration config;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pulsarClient` is accessed in both synchronized and unsynchronized contexts
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
    AuthenticationService authenticationService;
    AuthorizationService authorizationService;
    PulsarClient pulsarClient;

    private final ScheduledExecutorService executor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `config` is accessed in both synchronized and unsynchronized contexts
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
    private PulsarResources pulsarResources;
    private MetadataStoreExtended configMetadataStore;
    private ServiceConfiguration config;
    @Getter
    private Optional<CryptoKeyReader> cryptoKeyReader = Optional.empty();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `checkLedgerRollTask` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    private ScheduledFuture<?> timeoutTask;
    private ScheduledFuture<?> checkLedgerRollTask;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `config` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private final BookKeeper.DigestType digestType;

    protected ManagedLedgerConfig config;
    protected Map<String, String> propertiesMap;
    protected final MetaStore store;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `managedLedgerInterceptor` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    volatile PositionImpl lastConfirmedEntry;

    private ManagedLedgerInterceptor managedLedgerInterceptor;

    private volatile long lastAddEntryTimeMs = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentLedgerSize` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private volatile LedgerHandle currentLedger;
    private long currentLedgerEntries = 0;
    private long currentLedgerSize = 0;
    private volatile long lastLedgerCreatedTimestamp = 0;
    private volatile long lastLedgerCreationFailureTimestamp = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentLedgerEntries` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            .completedFuture(PositionImpl.LATEST);
    private volatile LedgerHandle currentLedger;
    private long currentLedgerEntries = 0;
    private long currentLedgerSize = 0;
    private volatile long lastLedgerCreatedTimestamp = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `timeoutTask` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    final EntryCache entryCache;

    private ScheduledFuture<?> timeoutTask;
    private ScheduledFuture<?> checkLedgerRollTask;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maximumRolloverTimeMs` is accessed in both synchronized and unsynchronized contexts
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    private static final Random random = new Random(System.currentTimeMillis());
    private long maximumRolloverTimeMs;
    protected final Supplier<Boolean> mlOwnershipChecker;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metricsCollection` is accessed in both synchronized and unsynchronized contexts
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/stats/ProxyStats.java`
#### Snippet
```java
    private final JvmMetrics jvmMetrics;
    private ConcurrentOpenHashMap<String, ProxyNamespaceStats> topicStats;
    private List<Metrics> metricsCollection;
    private List<Metrics> tempMetricsCollection;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tempMetricsCollection` is accessed in both synchronized and unsynchronized contexts
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/stats/ProxyStats.java`
#### Snippet
```java
    private ConcurrentOpenHashMap<String, ProxyNamespaceStats> topicStats;
    private List<Metrics> metricsCollection;
    private List<Metrics> tempMetricsCollection;

    public ProxyStats(WebSocketService service) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `topicStats` is accessed in both synchronized and unsynchronized contexts
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/stats/ProxyStats.java`
#### Snippet
```java
    private final WebSocketService service;
    private final JvmMetrics jvmMetrics;
    private ConcurrentOpenHashMap<String, ProxyNamespaceStats> topicStats;
    private List<Metrics> metricsCollection;
    private List<Metrics> tempMetricsCollection;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `rackawarePolicy` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java

    private MetadataCache<BookiesRackConfiguration> bookieMappingCache = null;
    private ITopologyAwareEnsemblePlacementPolicy<BookieNode> rackawarePolicy = null;
    private List<BookieId> bookieAddressListLastTime = new ArrayList<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `allowedOffset` is accessed in both synchronized and unsynchronized contexts
in `pulsar-broker-auth-athenz/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderAthenz.java`
#### Snippet
```java

    private List<String> domainNameList = null;
    private int allowedOffset = 30;

    @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cipher` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
    private static final int tagLen = 16 * 8;
    private byte[] iv = new byte[IV_LEN];
    private Cipher cipher;
    MessageDigest digest;
    private String logCtx;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `logCtx` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
    private Cipher cipher;
    MessageDigest digest;
    private String logCtx;

    // Data key which is used to encrypt message
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `iv` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
    private static KeyGenerator keyGenerator;
    private static final int tagLen = 16 * 8;
    private byte[] iv = new byte[IV_LEN];
    private Cipher cipher;
    MessageDigest digest;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dataKey` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java

    // Data key which is used to encrypt message
    private SecretKey dataKey;
    private LoadingCache<ByteBuffer, SecretKey> dataKeyCache;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `encryptedDataKeyMap` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java

    // Map of key name and encrypted gcm key, metadata pair which is sent with encrypted message
    private ConcurrentHashMap<String, EncryptionKeyInfo> encryptedDataKeyMap;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jaasCredentialsContainer` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    // this is a static object that shares amongst client.
    private static JAASCredentialsContainer jaasCredentialsContainer;
    private static volatile boolean initializedJAAS = false;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `loginContextName` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java

    private Map<String, String> configuration;
    private String loginContextName;
    private String serverType = null;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `configuration` is accessed in both synchronized and unsynchronized contexts
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
    private static volatile boolean initializedJAAS = false;

    private Map<String, String> configuration;
    private String loginContextName;
    private String serverType = null;
```

## RuleId[ruleID=EqualsAndHashcode]
### EqualsAndHashcode
Class has `hashCode()` defined but does not define `equals()`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java

@Slf4j
public class ResourceLockImpl<T> implements ResourceLock<T> {

    private final MetadataStoreExtended store;
```

## RuleId[ruleID=StringRepeatCanBeUsed]
### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/utils/FixedColumnLengthTableMaker.java`
#### Snippet
```java
    // Helper function to pad with white space.
    private void addSpace(final int amount, final StringBuilder builder) {
        for (int i = 0; i < amount; ++i) {
            builder.append(' ');
        }
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/utils/FixedColumnLengthTableMaker.java`
#### Snippet
```java
    // Helper function to add top and bottom borders.
    private void addHorizontalBorder(final int length, final StringBuilder builder, final char borderChar) {
        for (int i = 0; i < length; ++i) {
            builder.append(borderChar);
        }
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/utils/PaddingDecimalFormat.java`
#### Snippet
```java
        if (padLength > 0) {
            StringBuilder pad = new StringBuilder(padLength);
            for (int i = 0; i < padLength; i++) {
                pad.append(' ');
            }
```

## RuleId[ruleID=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java
    }

    private void validateAdminAndClientPermission(TopicName topic) throws RestException, Exception {
        try {
            validateTopicOperation(topic, TopicOperation.LOOKUP);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NettyServerSslContextBuilder.java`
#### Snippet
```java
    @Override
    public synchronized SslContext update()
            throws SSLException, FileNotFoundException, GeneralSecurityException, IOException {
        this.sslNettyContext =
                SecurityUtility.createNettySslContextForServer(this.sslProvider, tlsAllowInsecureConnection,
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NettyServerSslContextBuilder.java`
#### Snippet
```java
    @Override
    public synchronized SslContext update()
            throws SSLException, FileNotFoundException, GeneralSecurityException, IOException {
        this.sslNettyContext =
                SecurityUtility.createNettySslContextForServer(this.sslProvider, tlsAllowInsecureConnection,
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NettyClientSslContextRefresher.java`
#### Snippet
```java
    @Override
    public synchronized SslContext update()
            throws SSLException, FileNotFoundException, GeneralSecurityException, IOException {
        if (authData != null && authData.hasDataForTls()) {
            this.sslNettyContext = authData.getTlsTrustStoreStream() == null
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NettyClientSslContextRefresher.java`
#### Snippet
```java
    @Override
    public synchronized SslContext update()
            throws SSLException, FileNotFoundException, GeneralSecurityException, IOException {
        if (authData != null && authData.hasDataForTls()) {
            this.sslNettyContext = authData.getTlsTrustStoreStream() == null
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            Set<String> ciphers,
                                                            Set<String> protocols)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {

        if (StringUtils.isNotBlank(trustCertsFilePath)) {
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            Set<String> ciphers,
                                                            Set<String> protocols)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {

        if (StringUtils.isNotBlank(trustCertsFilePath)) {
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            Set<String> ciphers,
                                                            Set<String> protocols)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {
        X509Certificate[] certificates = loadCertificatesFromPemFile(certFilePath);
        PrivateKey privateKey = loadPrivateKeyFromPemFile(keyFilePath);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            Set<String> ciphers,
                                                            Set<String> protocols)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {
        X509Certificate[] certificates = loadCertificatesFromPemFile(certFilePath);
        PrivateKey privateKey = loadPrivateKeyFromPemFile(keyFilePath);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java

    private static void setupTrustCerts(SslContextBuilder builder, boolean allowInsecureConnection,
            InputStream trustCertsStream) throws IOException, FileNotFoundException {
        if (allowInsecureConnection) {
            builder.trustManager(InsecureTrustManagerFactory.INSTANCE);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            PrivateKey privateKey, Set<String> ciphers,
                                                            Set<String> protocols)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {
        SslContextBuilder builder = SslContextBuilder.forClient().sslProvider(sslProvider);
        setupTrustCerts(builder, allowInsecureConnection, trustCertsStream);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            PrivateKey privateKey, Set<String> ciphers,
                                                            Set<String> protocols)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {
        SslContextBuilder builder = SslContextBuilder.forClient().sslProvider(sslProvider);
        setupTrustCerts(builder, allowInsecureConnection, trustCertsStream);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            Set<String> ciphers, Set<String> protocols,
                                                            boolean requireTrustedClientCertOnConnect)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {
        X509Certificate[] certificates = loadCertificatesFromPemFile(certFilePath);
        PrivateKey privateKey = loadPrivateKeyFromPemFile(keyFilePath);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            Set<String> ciphers, Set<String> protocols,
                                                            boolean requireTrustedClientCertOnConnect)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {
        X509Certificate[] certificates = loadCertificatesFromPemFile(certFilePath);
        PrivateKey privateKey = loadPrivateKeyFromPemFile(keyFilePath);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            String trustCertsFilePath, Set<String> ciphers,
                                                            Set<String> protocols)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {
        return createNettySslContextForClient(sslProvider, allowInsecureConnection, trustCertsFilePath,
                (Certificate[]) null,
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                                            String trustCertsFilePath, Set<String> ciphers,
                                                            Set<String> protocols)
            throws GeneralSecurityException, SSLException, FileNotFoundException, IOException {
        return createNettySslContextForClient(sslProvider, allowInsecureConnection, trustCertsFilePath,
                (Certificate[]) null,
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReportDeserializer.java`
#### Snippet
```java
    @Override
    public LoadManagerReport deserialize(JsonParser jsonParser, DeserializationContext deserializationContext)
            throws IOException, JsonProcessingException {
        ObjectMapper mapper = ObjectMapperFactory.getThreadLocal();
        ObjectNode root = ObjectMapperFactory.getThreadLocal().readTree(jsonParser);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
     * @return 0 for success, < 0 otherwise
     */
    public int run() throws PulsarClientException, IOException {
        if (mainOptions.size() != 1) {
            throw (new ParameterException("Please provide one and only one topic name."));
```

## RuleId[ruleID=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseAbstractSink.java`
#### Snippet
```java
        List<Put> puts = new ArrayList<>();
        List<Record<T>>  toFlushList;
        synchronized (this) {
            if (incomingList.isEmpty()) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseAbstractSink.java`
#### Snippet
```java
    public void write(Record<T> record) throws Exception {
        int number;
        synchronized (this) {
            if (null != record) {
                incomingList.add(record);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
        int currentSize;

        synchronized (this) {
            incomingList.add(record);
            currentSize = incomingList.size();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
        final List<Record<byte[]>> recordsToInsert;

        synchronized (this) {
            if (incomingList.isEmpty()) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSink.java`
#### Snippet
```java
        final List<Record<byte[]>> recordsToFlush;

        synchronized (this) {
            if (incomingList.isEmpty()) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSink.java`
#### Snippet
```java
    public void write(Record<byte[]> record) throws Exception {
        int currentSize;
        synchronized (this) {
            incomingList.add(record);
            currentSize = incomingList.size();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/BatchSink.java`
#### Snippet
```java
        List<Record<R>>  toFlushList;

        synchronized (this) {
            if (incomingList.isEmpty()) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/BatchSink.java`
#### Snippet
```java
    public final void write(Record<R> record) {
        int currentSize;
        synchronized (this) {
            if (null != record) {
                incomingList.add(record);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java
        String indexName = indexName(record);
        if (!indexCache.contains(indexName)) {
            synchronized (this) {
                if (!indexCache.contains(indexName)) {
                    createIndexIfNeeded(indexName);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarOffsetBackingStore.java`
#### Snippet
```java

    void readToEnd(CompletableFuture<Void> future) {
        synchronized (this) {
            if (outstandingReadToEnd != null) {
                outstandingReadToEnd.whenComplete((result, cause) -> {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarOffsetBackingStore.java`
#### Snippet
```java
                outstandingReadToEnd = future;
                future.whenComplete((result, cause) -> {
                    synchronized (PulsarOffsetBackingStore.this) {
                        outstandingReadToEnd = null;
                    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
        } catch (Exception e) {
        }
        synchronized (this) {
            defaultResponse = FutureUtils.exception(new BKException.BKClientClosedException());
        }
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java

    public static void shutdown() throws Exception {
        synchronized (PulsarConnectorCache.class) {
            if (instance != null) {
                instance.statsProvider.stop();
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java

    public static PulsarConnectorCache getConnectorCache(PulsarConnectorConfig pulsarConnectorConfig) throws Exception {
        synchronized (PulsarConnectorCache.class) {
            if (instance == null) {
                instance = new PulsarConnectorCache(pulsarConnectorConfig);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java

        executor.execute(() -> {
            synchronized (MockZooKeeper.this) {
                persistentWatchers.add(new PersistentWatcher(basePath, watcher, mode));
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
                        offloadPolicies.getManagedLedgerOffloadDriver());

                synchronized (this) {
                    Offloaders offloaders = offloadersCache.getOrLoadOffloaders(
                            offloadPolicies.getOffloadersDirectory(), config.getNarExtractionDirectory());
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java
    @Override
    public void run(Timeout timeout) throws Exception {
        synchronized (this) {
            if (timeout == null || timeout.isCancelled()) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
        // Check the close future to avoid grabbing the mutex every time the pending acks goes down to 0
        if (newPendingPublishAcks == 0 && !closeFuture.isDone()) {
            synchronized (this) {
                if (isClosed && !closeFuture.isDone()) {
                    closeNow(true);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicBasedTopicPoliciesService.java`
#### Snippet
```java
            return result;
        }
        synchronized (this) {
            if (readerCaches.get(namespace) != null) {
                ownedBundlesCountPerNamespace.get(namespace).incrementAndGet();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicBasedTopicPoliciesService.java`
#### Snippet
```java
    private void createSystemTopicFactoryIfNeeded() throws PulsarServerException {
        if (namespaceEventsSystemTopicFactory == null) {
            synchronized (this) {
                if (namespaceEventsSystemTopicFactory == null) {
                    try {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
        // from the delete operation that was completed
        topic.getBrokerService().getTopicOrderedExecutor().execute(safeRun(() -> {
            synchronized (PersistentStickyKeyDispatcherMultipleConsumers.this) {
                if (recentlyJoinedConsumers != null && !recentlyJoinedConsumers.isEmpty()
                        && removeConsumersFromRecentJoinedConsumers()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        topic.getBrokerService().executor().schedule(() -> {
            topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
                synchronized (PersistentStreamingDispatcherSingleActiveConsumer.this) {
                    Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);
                    if (currentConsumer != null && !havePendingRead) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java

        if (consumer.getAvailablePermits() > 0) {
            synchronized (this) {
                if (havePendingRead) {
                    if (log.isDebugEnabled()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            // Jump again into dispatcher dedicated thread
            topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
                synchronized (PersistentDispatcherSingleActiveConsumer.this) {
                    Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);
                    // we should retry the read if we have an active consumer and there is no pending read
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                    topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName,
                        SafeRun.safeRun(() -> {
                            synchronized (PersistentDispatcherSingleActiveConsumer.this) {
                                Consumer newConsumer = getActiveConsumer();
                                readMoreEntries(newConsumer);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java

        if (consumer.getAvailablePermits() > 0) {
            synchronized (this) {
                if (havePendingRead) {
                    if (log.isDebugEnabled()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/MessageDeduplication.java`
#### Snippet
```java
    public CompletableFuture<Void> checkStatus() {
        boolean shouldBeEnabled = isDeduplicationEnabled();
        synchronized (this) {
            if (status == Status.Recovering || status == Status.Removing) {
                // If there's already a transition happening, check later for status
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
        topic.getBrokerService().executor().schedule(() -> {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topic.getName(), SafeRun.safeRun(() -> {
                synchronized (PersistentStreamingDispatcherMultipleConsumers.this) {
                    if (!havePendingRead) {
                        log.info("[{}] Scheduling read operation", name);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java
        // cursor close handles pending delete (ack) operations
        closeSubscriptionFuture.thenCompose(v -> topic.unsubscribe(subName)).thenAccept(v -> {
            synchronized (this) {
                (dispatcher != null ? dispatcher.close() : CompletableFuture.completedFuture(null)).thenRun(() -> {
                    log.info("[{}][{}] Successfully deleted subscription", topicName, subName);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
            // Don't block caller thread, complete cancel read with dispatcher dedicated thread.
            topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topic.getName(), SafeRun.safeRun(() -> {
                synchronized (StreamingEntryReader.this) {
                    if (STATE_UPDATER.compareAndSet(this, State.Canceling, State.Canceled)) {
                        internalCancelReadRequests();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

        topic.getBrokerService().executor().schedule(() -> {
            synchronized (PersistentDispatcherMultipleConsumers.this) {
                // If it's a replay read we need to retry even if there's already
                // another scheduled read, otherwise we'd be stuck until
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

        Optional<DelayedDeliveryTracker> delayedDeliveryTracker;
        synchronized (this) {
            delayedDeliveryTracker = this.delayedDeliveryTracker;
            this.delayedDeliveryTracker = Optional.empty();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        }

        synchronized (this) {
            if (!delayedDeliveryTracker.isPresent()) {
                if (!msgMetadata.hasDeliverAtTime()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Void> close() {
        synchronized (this) {
            if (dispatcher != null && dispatcher.isConsumerConnected()) {
                return FutureUtil.failedFuture(new SubscriptionBusyException("Subscription has active consumers"));
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    public CompletableFuture<Void> addConsumer(Consumer consumer) {
        return pendingAckHandle.pendingAckHandleFuture().thenCompose(future -> {
            synchronized (PersistentSubscription.this) {
                cursor.updateLastActive();
                if (IS_FENCED_UPDATER.get(this) == TRUE) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java

        // Lock the Subscription object before locking the Dispatcher object to avoid deadlocks
        synchronized (this) {
            if (dispatcher != null && dispatcher.isConsumerConnected()) {
                disconnectFuture = dispatcher.disconnectActiveConsumers(true);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        // cursor close handles pending delete (ack) operations
        closeSubscriptionFuture.thenCompose(v -> topic.unsubscribe(subName)).thenAccept(v -> {
            synchronized (this) {
                (dispatcher != null ? dispatcher.close() : CompletableFuture.completedFuture(null)).thenRun(() -> {
                    log.info("[{}][{}] Successfully deleted subscription", topicName, subName);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                // If cursor is not durable, we need to clean up the subscription as well
                this.close().thenRun(() -> {
                    synchronized (this) {
                        if (dispatcher != null) {
                            dispatcher.close().thenRun(() -> {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
        public TransactionBufferReader newReader(long sequenceId) throws
                TransactionBufferException.TransactionNotSealedException {
            synchronized (this) {
                if (TxnStatus.COMMITTED != status) {
                    // the transaction is not committed yet, hence the buffer is not sealed
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
        public void appendEntry(long sequenceId, ByteBuf entry) throws
                TransactionBufferException.TransactionSealedException {
            synchronized (this) {
                if (TxnStatus.OPEN != status) {
                    // the transaction is not open anymore, reject the append operations
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
                    @Override
                    public void noNeedToRecover() {
                        synchronized (TopicTransactionBuffer.this) {
                            maxReadPosition = (PositionImpl) topic.getManagedLedger().getLastConfirmedEntry();
                            if (!changeToNoSnapshotState()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
                    @Override
                    public void addComplete(Position position, ByteBuf entryData, Object ctx) {
                        synchronized (TopicTransactionBuffer.this) {
                            updateMaxReadPosition(txnID);
                            handleLowWaterMark(txnID, lowWaterMark);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
    public void run(Timeout timeout) {
        if (checkIfReady()) {
            synchronized (TopicTransactionBuffer.this) {
                takeSnapshotByTimeout();
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
        // when ongoing transaction is empty, proved that lastAddConfirm is can read max position, because callback
        // thread is the same tread, in this time the lastAddConfirm don't content transaction message.
        synchronized (TopicTransactionBuffer.this) {
            if (checkIfNoSnapshot()) {
                this.maxReadPosition = position;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
            @Override
            public void addComplete(Position position, ByteBuf entryData, Object ctx) {
                synchronized (TopicTransactionBuffer.this) {
                    handleTransactionMessage(txnId, position);
                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
                    @Override
                    public void addComplete(Position position, ByteBuf entryData, Object ctx) {
                        synchronized (TopicTransactionBuffer.this) {
                            updateMaxReadPosition(txnID);
                            snapshotAbortedTxnProcessor.putAbortedTxnAndPosition(txnID, maxReadPosition);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
                    @Override
                    public void recoverComplete() {
                        synchronized (TopicTransactionBuffer.this) {
                            if (ongoingTxns.isEmpty()) {
                                maxReadPosition = (PositionImpl) topic.getManagedLedger().getLastConfirmedEntry();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/timeout/TransactionTimeoutTrackerImpl.java`
#### Snippet
```java
    @Override
    public void run(Timeout timeout) {
        synchronized (this){
            while (!priorityQueue.isEmpty()){
                long timeoutTime = priorityQueue.peekN1();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/timeout/TransactionTimeoutTrackerImpl.java`
#### Snippet
```java
            return CompletableFuture.completedFuture(false);
        }
        synchronized (this){
            long nowTime = clock.millis();
            long transactionTimeoutTime = nowTime + timeout;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/timeout/TransactionTimeoutTrackerImpl.java`
#### Snippet
```java
    @Override
    public void start() {
        synchronized (this) {
            if (currentTimeout == null && !priorityQueue.isEmpty()) {
                this.currentTimeout = this.timer.newTimeout(this,
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        long pending = pendingWriteOps.decrementAndGet();
        if (pending == 0 && isFenced && !isClosingOrDeleting) {
            synchronized (this) {
                if (isFenced && !isClosingOrDeleting) {
                    messageDeduplication.resetHighestSequenceIdPushed();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    public synchronized LongRunningProcessStatus compactionStatus() {
        final CompletableFuture<Long> current;
        synchronized (this) {
            current = currentCompaction;
        }
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/MLPendingAckStoreProvider.java`
#### Snippet
```java

    public static void closeBufferedWriterMetrics() {
        synchronized (MLPendingAckStoreProvider.class){
            if (bufferedWriterMetrics == DisabledTxnLogBufferedWriterMetricsStats.DISABLED_BUFFERED_WRITER_METRICS){
                return;
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/MLPendingAckStoreProvider.java`
#### Snippet
```java
            return;
        }
        synchronized (MLPendingAckStoreProvider.class){
            if (bufferedWriterMetrics != DisabledTxnLogBufferedWriterMetricsStats.DISABLED_BUFFERED_WRITER_METRICS){
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/client/impl/RawReaderImpl.java`
#### Snippet
```java
        private void reset() {
            List<CompletableFuture<RawMessage>> toError = new ArrayList<>();
            synchronized (this) {
                while (!pendingRawReceives.isEmpty()) {
                    toError.add(pendingRawReceives.remove());
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/client/impl/RawReaderImpl.java`
#### Snippet
```java
            RawMessageAndCnx messageAndCnx = null;

            synchronized (this) {
                if (!pendingRawReceives.isEmpty()
                    && !incomingRawMessages.isEmpty()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactedTopicImpl.java`
#### Snippet
```java
                                       boolean isFirstRead,
                                       ReadEntriesCallback callback, Consumer consumer) {
        synchronized (this) {
            PositionImpl cursorPosition;
            if (isFirstRead && MessageId.earliest.equals(consumer.getStartMessageId())){
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactedTopicImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<CompactedTopicContext> newCompactedLedger(Position p, long compactedLedgerId) {
        synchronized (this) {
            compactionHorizon = (PositionImpl) p;

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TopicListWatcher.java`
#### Snippet
```java

        // synchronized this, because redeliverUnAckMessage eliminate the epoch inconsistency between them
        synchronized (this) {
            setClientCnx(cnx);
            BaseCommand watchRequest = Commands.newWatchTopicList(requestId, watcherId, namespace.toString(),
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TopicListWatcher.java`
#### Snippet
```java

                    .thenAccept(response -> {
                        synchronized (TopicListWatcher.this) {
                            if (!changeToReadyState()) {
                                // Watcher was closed while reconnecting, close the connection to make sure the broker
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
            pendingAckStoreFuture.thenAccept(pendingAckStore ->
                    pendingAckStore.appendCommitMark(txnID, AckType.Individual).thenAccept(v -> {
                        synchronized (PendingAckHandleImpl.this) {
                            if (individualAckOfTransaction != null && individualAckOfTransaction.containsKey(txnID)) {
                                HashMap<PositionImpl, PositionImpl> pendingAckMessageForCurrentTxn =
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
            pendingAckStoreFuture.thenAccept(pendingAckStore ->
                    pendingAckStore.appendAbortMark(txnId, AckType.Individual).thenAccept(v -> {
                        synchronized (PendingAckHandleImpl.this) {
                            HashMap<PositionImpl, PositionImpl> pendingAckMessageForCurrentTxn =
                                    individualAckOfTransaction.get(txnId);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
    public CompletableFuture<Void> close() {
        changeToCloseState();
        synchronized (PendingAckHandleImpl.this) {
            if (this.pendingAckStoreFuture != null) {
                return this.pendingAckStoreFuture.thenAccept(PendingAckStore::closeAsync);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        this.pendingAckStoreFuture.thenAccept(pendingAckStore ->
                pendingAckStore.appendIndividualAck(txnID, positions).thenAccept(v -> {
                    synchronized (org.apache.pulsar.broker.transaction.pendingack.impl.PendingAckHandleImpl.this) {
                        for (MutablePair<PositionImpl, Integer> positionIntegerMutablePair : positions) {

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
                    }
                }).exceptionally(e -> {
                    synchronized (PendingAckHandleImpl.this) {
                        // we also modify the in memory state when append fail,
                        // because we don't know the persistent state, when were replay it,
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
            // client cnx don't support ack receipt, if we don't complete the future, the client will block.
            if (ackReceiptEnabled) {
                synchronized (PersistentAcknowledgmentsGroupingTracker.this) {
                    if (!this.currentCumulativeAckFuture.isDone()) {
                        this.currentCumulativeAckFuture.complete(null);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
        long timeToWaitMs;
        boolean hasPendingReceives = false;
        synchronized (this) {
            // If it's closing/closed we need to ignore this timeout and not schedule next timeout.
            if (getState() == State.Closing || getState() == State.Closed) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ZeroQueueConsumerImpl.java`
#### Snippet
```java
            // if cnx is null or if the connection breaks the connectionOpened function will send the flow again
            waitingOnReceiveForZeroQueueSize = true;
            synchronized (this) {
                if (isConnected()) {
                    increaseAvailablePermits(cnx());
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ZeroQueueConsumerImpl.java`
#### Snippet
```java
                ClientCnx msgCnx = ((MessageImpl<?>) message).getCnx();
                // synchronized need to prevent race between connectionOpened and the check "msgCnx == cnx()"
                synchronized (this) {
                    // if message received due to an old flow - discard it and wait for the message from the
                    // latest flow command
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    void ackReceived(ClientCnx cnx, long sequenceId, long highestSequenceId, long ledgerId, long entryId) {
        OpSendMsg op = null;
        synchronized (this) {
            op = pendingMessages.peek();
            if (op == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            // race condition since we also write the message on the socket from this thread
            cnx.ctx().channel().eventLoop().execute(() -> {
                synchronized (ProducerImpl.this) {
                    failPendingMessages(null, ex);
                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
        long timeToWaitMs;

        synchronized (this) {
            // If it's closing/closed we need to ignore this timeout and not schedule next timeout.
            if (getState() == State.Closing || getState() == State.Closed) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            }
            cnx.ctx().channel().eventLoop().execute(() -> {
                synchronized (ProducerImpl.this) {
                    recoverProcessOpSendMsgFrom(cnx, msg, expectedCnxEpoch);
                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java

        final long epoch;
        synchronized (this) {
            // Because the state could have been updated while retrieving the connection, we set it back to connecting,
            // as long as the change from current state to connecting is a valid state change.
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                    // We are now reconnected to broker and clear to send messages. Re-send all pending messages and
                    // set the cnx pointer so that new messages will be sent immediately
                    synchronized (ProducerImpl.this) {
                        if (getState() == State.Closing || getState() == State.Closed) {
                            // Producer was closed while reconnecting, close the connection to make sure the broker
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                    }
                    if (cause instanceof PulsarClientException.ProducerBlockedQuotaExceededException) {
                        synchronized (this) {
                            log.warn("[{}] [{}] Topic backlog quota exceeded. Throwing Exception on producer.", topic,
                                    producerName);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                    if (cause instanceof PulsarClientException.TopicTerminatedException) {
                        setState(State.Terminated);
                        synchronized (this) {
                            failPendingMessages(cnx(), (PulsarClientException) cause);
                        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                    } else if (cause instanceof PulsarClientException.ProducerFencedException) {
                        setState(State.ProducerFenced);
                        synchronized (this) {
                            failPendingMessages(cnx(), (PulsarClientException) cause);
                        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private void resendMessages(ClientCnx cnx, long expectedEpoch) {
        cnx.ctx().channel().eventLoop().execute(() -> {
            synchronized (this) {
                if (getState() == State.Closing || getState() == State.Closed) {
                    // Producer was closed while reconnecting, close the connection to make sure the broker
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    public int getPendingQueueSize() {
        if (isBatchMessagingEnabled()) {
            synchronized (this) {
                return pendingMessages.messagesCount() + batchMessageContainer.getNumMessagesInBatch();
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Void> flushAsync() {
        synchronized (ProducerImpl.this) {
            if (isBatchMessagingEnabled()) {
                batchMessageAndSend(false);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            log.info("[{}] [{}] The topic has been terminated", topic, producerName);
            setClientCnx(null);
            synchronized (this) {
                failPendingMessages(cnx,
                        new PulsarClientException.TopicTerminatedException(
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                    return;
                }
                synchronized (this) {
                    serializeAndSendMessage(msg, payload, sequenceId, uuid, chunkId, totalChunks,
                            readStartIndex, payloadChunkSize, compressedPayload, compressed,
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    protected void triggerFlush() {
        if (isBatchMessagingEnabled()) {
            synchronized (ProducerImpl.this) {
                batchMessageAndSend(false);
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/customroute/PartialRoundRobinMessageRouterImpl.java`
#### Snippet
```java
        if (partialList.isEmpty()
                || partialList.size() < numPartitionsLimit && partialList.size() < metadata.numPartitions()) {
            synchronized (this) {
                if (partialList.isEmpty()) {
                    partialList.addAll(IntStream.range(0, metadata.numPartitions()).boxed()
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionImpl.java`
#### Snippet
```java
        CompletableFuture<Void> completableFuture = new CompletableFuture<>();
        if (checkIfOpen(completableFuture)) {
            synchronized (TransactionImpl.this) {
                // we need to issue the request to TC to register the acked topic
                return registerSubscriptionMap.compute(Pair.of(topic, subscription), (key, future) -> {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionImpl.java`
#### Snippet
```java
        CompletableFuture<Void> completableFuture = new CompletableFuture<>();
        if (checkIfOpen(completableFuture)) {
            synchronized (TransactionImpl.this) {
                // we need to issue the request to TC to register the produced topic
                return registerPartitionMap.compute(topic, (key, future) -> {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
        }
        log.info("Initiating logout for {}", container.getPrincipal());
        synchronized (this) {
            //clear up the kerberos state. But the tokens are not cleared! As per
            //the Java kerberos login module code, only the kerberos credentials
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/RateLimiter.java`
#### Snippet
```java
            rateLimitFunction.apply();
        }
        notifyAll();
    }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/RateLimiter.java`
#### Snippet
```java
            canAcquire = acquirePermit < 0 || acquiredPermits < this.permits;
            if (!canAcquire) {
                wait();
            } else {
                acquiredPermits += acquirePermit;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        // Second : we should synchronized `ClientCnx cnx = cnx()` to
        // prevent use old cnx to send redeliverUnacknowledgedMessages to a old broker
        synchronized (ConsumerImpl.this) {
            ClientCnx cnx = cnx();
            // V1 don't support redeliverUnacknowledgedMessages
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

        int currentSize;
        synchronized (this) {
            currentSize = incomingMessages.size();
            startMessageId = clearReceiverQueue();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

        // synchronized this, because redeliverUnAckMessage eliminate the epoch inconsistency between them
        synchronized (this) {
            setClientCnx(cnx);
            ByteBuf request = Commands.newSubscribe(topic, subscription, consumerId, requestId, getSubType(),
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

            cnx.sendRequestWithId(request, requestId).thenRun(() -> {
                synchronized (ConsumerImpl.this) {
                    if (changeToReadyState()) {
                        consumerIsReconnectedToBroker(cnx, currentSize);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/InflightReadsLimiter.java`
#### Snippet
```java
            return;
        }
        synchronized (this) {
            remainingBytes += handle.acquiredPermits;
            updateMetrics();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/InflightReadsLimiter.java`
#### Snippet
```java
            return DISABLED;
        }
        synchronized (this) {
            try {
                if (current == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
            // on the results of the callback
            handle.whenComplete((___, error) -> {
                synchronized (PendingRead.this) {
                    completed = true;
                    synchronized (ledgerCache) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java

            handle.thenAcceptAsync(entriesToReturn -> {
                synchronized (PendingRead.this) {
                    if (callbacks.size() == 1) {
                        ReadEntriesCallbackWithContext first = callbacks.get(0);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
            }, rangeEntryCache.getManagedLedger().getExecutor()
                    .chooseThread(rangeEntryCache.getManagedLedger().getName())).exceptionally(exception -> {
                synchronized (PendingRead.this) {
                    for (ReadEntriesCallbackWithContext callback : callbacks) {
                        ManagedLedgerException mlException = createManagedLedgerException(exception);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
        CompletableFuture<Void> future = client.getLeaseClient().grant(leaseTTLSeconds)
                .thenAccept(lease -> {
                    synchronized (this) {
                        this.leaseId = lease.getID();

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerAuditorManager.java`
#### Snippet
```java
        log.info("Auditor leader election state: {} -- BookieId: {}", state, bookieId);

        synchronized (this) {
            this.leaderElectionState = state;
            notifyAll();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerAuditorManager.java`
#### Snippet
```java
        synchronized (this) {
            this.leaderElectionState = state;
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerAuditorManager.java`
#### Snippet
```java
        LeaderElectionState les = leaderElection.elect(bookieId).join();

        synchronized (this) {
            leaderElectionState = les;
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerAuditorManager.java`
#### Snippet
```java
        while (true) {
            try {
                synchronized (this) {
                    if (leaderElectionState == LeaderElectionState.Leading) {
                        return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerAuditorManager.java`
#### Snippet
```java
                        return;
                    } else {
                        wait();
                    }
                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LockManagerImpl.java`
#### Snippet
```java
    public CompletableFuture<Void> asyncClose() {
        Map<String, ResourceLock<T>> locks;
        synchronized (this) {
            if (state != State.Ready) {
                return CompletableFuture.completedFuture(null);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LockManagerImpl.java`
#### Snippet
```java
        CompletableFuture<ResourceLock<T>> result = new CompletableFuture<>();
        lock.acquire(value).thenRun(() -> {
            synchronized (LockManagerImpl.this) {
                if (state == State.Ready) {
                    locks.put(path, lock);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LockManagerImpl.java`
#### Snippet
```java
                    lock.getLockExpiredFuture().thenRun(() -> {
                        log.info("Released resource lock on {}", path);
                        synchronized (LockManagerImpl.this) {
                            locks.remove(path, lock);
                        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
            ReplicationException.UnavailableException {
        log.debug("notifyLostBookieRecoveryDelayChanged()");
        synchronized (this) {
            lostBookieRecoveryDelayListener = cb;
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
                }

                synchronized (this) {
                    // nothing found, wait for a watcher to trigger
                    this.wait(1000);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
                synchronized (this) {
                    // nothing found, wait for a watcher to trigger
                    this.wait(1000);
                }
            } catch (ExecutionException ee) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        }

        synchronized (this) {
            replicationEnabledListener = cb;
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
    private void handleNotification(Notification n) {
        if (n.getPath().startsWith(basePath)) {
            synchronized (this) {
                // Notify that there were some changes on the under-replicated z-nodes
                notifyAll();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
            synchronized (this) {
                // Notify that there were some changes on the under-replicated z-nodes
                notifyAll();

                if (n.getType() == NotificationType.Deleted) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
        }

        synchronized (this) {
            if (internalState != InternalState.LeaderIsPresent) {
                // Ignoring notification since we're not trying to become leader
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
                            .exceptionally(ex -> {
                                log.warn("Leader election for path {} has failed", path, ex);
                                synchronized (LeaderElectionImpl.this) {
                                    try {
                                        stateChangesListener.accept(leaderElectionState);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
        store.put(path, payload, Optional.of(-1L), EnumSet.of(CreateOption.Ephemeral))
                .thenAccept(stat -> {
                    synchronized (LeaderElectionImpl.this) {
                        if (internalState == InternalState.ElectionInProgress) {
                            // Do a get() in order to force a notification later, if the z-node disappears
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
                            cache.get(path)
                                    .thenRun(() -> {
                                        synchronized (LeaderElectionImpl.this) {
                                            log.info("Acquired leadership on {}", path);
                                            internalState = InternalState.LeaderIsPresent;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
                    }

                    synchronized (ResourceLockImpl.this) {
                        if (newValue.equals(existingValue)) {
                            // The lock value is still the same, that means that we're the
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
        }
        revalidateFuture.exceptionally(ex -> {
            synchronized (ResourceLockImpl.this) {
                Throwable realCause = FutureUtil.unwrapCompletionException(ex);
                if (!revalidateAfterReconnection || realCause instanceof BadVersionException
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
        store.put(path, payload, Optional.of(version), EnumSet.of(CreateOption.Ephemeral))
                .thenAccept(stat -> {
                    synchronized (ResourceLockImpl.this) {
                        state = State.Valid;
                        version = stat.getVersion();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
        store.delete(path, Optional.of(version))
                .thenRun(() -> {
                    synchronized (ResourceLockImpl.this) {
                        state = State.Released;
                    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
                    if (ex.getCause() instanceof MetadataStoreException.NotFoundException) {
                        // The lock is not there on release. We can anyway proceed
                        synchronized (ResourceLockImpl.this) {
                            state = State.Released;
                        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/ErrorNotifierImpl.java`
#### Snippet
```java
    public synchronized void triggerError(Throwable th) {
        error.set(th);
        this.notify();
    }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/ErrorNotifierImpl.java`
#### Snippet
```java
    public synchronized void waitForError() throws Exception {
        while (isRunning && error.get() == null) {
            this.wait();
        }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/ErrorNotifierImpl.java`
#### Snippet
```java
    public synchronized void close() {
        isRunning = false;
        this.notify();
    }
}
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java
    @Override
    public void becameActive(Consumer<?> consumer, int partitionId) {
        synchronized (this) {
            if (isLeader) {
                return;
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceCache.java`
#### Snippet
```java

    public static InstanceCache getInstanceCache() {
        synchronized (InstanceCache.class) {
            if (instance == null) {
                instance = new InstanceCache();
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceCache.java`
#### Snippet
```java

    public static void shutdown() {
        synchronized (InstanceCache.class) {
            if (instance != null) {
                instance.scheduledExecutorService.shutdown();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    public void join() throws Exception {
        // K8 functions never return
        this.wait();
    }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionCollectorRegistryImpl.java`
#### Snippet
```java

    public Collector registerIfNotExist(String metricName, Collector collector) {
        synchronized (this) {
            Collector existingCollector = namesToCollectors.get(metricName);
            if (existingCollector == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                            public void operationFailed(ManagedLedgerException exception) {
                                log.warn("[{}] Lazy recovery for cursor {} failed", name, cursorName, exception);
                                synchronized (ManagedLedgerImpl.this) {
                                    uninitializedCursors.remove(cursor.getName()).completeExceptionally(exception);
                                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                            }
                            Optional<Throwable> errorToReport = firstError;
                            synchronized (ManagedLedgerImpl.this) {
                                // if the ledger doesn't exist anymore, ignore the error
                                if (ledgers.containsKey(ledgerId)) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    long estimateBacklogFromPosition(PositionImpl pos) {
        synchronized (this) {
            LedgerInfo ledgerInfo = ledgers.get(pos.getLedgerId());
            if (ledgerInfo == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                        : getLastPositionAndCounter());

                synchronized (ManagedLedgerImpl.this) {
                    addCursor(cursor);
                    uninitializedCursors.remove(cursorName).complete(cursor);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

        Queue<LedgerInfo> ledgersToOffload = new ConcurrentLinkedQueue<>();
        synchronized (this) {
            log.info("[{}] Start ledgersOffload. ledgers={} totalSize={}", name, ledgers.keySet(),
                    TOTAL_SIZE_UPDATER.get(this));
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

            // Subtract size of ledgers that were already fully consumed but not trimmed yet
            synchronized (this) {
                size = getTotalSize();
                size -= ledgers.values().stream().filter(li -> li.getLedgerId() < slowestConsumerLedgerId)
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

            LedgerInfo ledgerInfo = null;
            synchronized (this) {
                ledgerInfo = ledgers.get(pos.getLedgerId());
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

        log.info("[{}] Opened new cursor: {}", name, cursor);
        synchronized (this) {
            addCursor(cursor);
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                ? config.getLedgerOffloader().getOffloadPolicies()
                : null);
        synchronized (this) {
            if (log.isDebugEnabled()) {
                log.debug("[{}] Start TrimConsumedLedgers. ledgers={} totalSize={}", name, ledgers.keySet(),
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                    }, null);
                    if (e instanceof BadVersionException) {
                        synchronized (ManagedLedgerImpl.this) {
                            log.error(
                                "[{}] Failed to update ledger list. z-node version mismatch. Closing managed ledger",
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                    metadataMutex.unlock();

                    synchronized (ManagedLedgerImpl.this) {
                        lastLedgerCreationFailureTimestamp = clock.millis();
                        STATE_UPDATER.set(ManagedLedgerImpl.this, State.ClosedLedger);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                                        cursorName, cursor.getMarkDeletedPosition(), cursorCount.get() - 1);
                                cursor.setActive();
                                synchronized (ManagedLedgerImpl.this) {
                                    addCursor(cursor);
                                    uninitializedCursors.remove(cursor.getName()).complete(cursor);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                    metadataMutex.unlock();
                    updateLedgersIdsComplete();
                    synchronized (ManagedLedgerImpl.this) {
                        mbean.addLedgerSwitchLatencySample(System.currentTimeMillis()
                                - lastLedgerCreationInitiationTimestamp, TimeUnit.MILLISECONDS);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                log.warn("[{}] Failed to open cursor: {}", name, cursor);

                synchronized (ManagedLedgerImpl.this) {
                    uninitializedCursors.remove(cursorName).completeExceptionally(exception);
                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private void tryTransformLedgerInfo(long ledgerId, LedgerInfoTransformation transformation,
            CompletableFuture<Void> finalPromise) {
        synchronized (this) {
            if (!metadataMutex.tryLock()) {
                // retry in 100 milliseconds
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
    public synchronized void stop() {
        if (running.compareAndSet(true, false)) {
            this.notify();
            try {
                Runtime.getRuntime().removeShutdownHook(shutdownHook);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
    public void start(boolean blocking) throws Exception {
        List<RuntimeSpawner> local = new LinkedList<>();
        synchronized (this) {
            if (!running.compareAndSet(false, true)) {
                throw new IllegalArgumentException("Pulsar Function local run already started!");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                close();
            } else {
                synchronized (this) {
                    while (running.get()) {
                        this.wait();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                synchronized (this) {
                    while (running.get()) {
                        this.wait();
                    }
                }
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStoreProvider.java`
#### Snippet
```java
            return;
        }
        synchronized (MLTransactionMetadataStoreProvider.class){
            if (bufferedWriterMetrics != DisabledTxnLogBufferedWriterMetricsStats.DISABLED_BUFFERED_WRITER_METRICS) {
                return;
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStoreProvider.java`
#### Snippet
```java

    public static void closeBufferedWriterMetrics() {
        synchronized (MLTransactionMetadataStoreProvider.class){
            if (bufferedWriterMetrics == DisabledTxnLogBufferedWriterMetricsStats.DISABLED_BUFFERED_WRITER_METRICS) {
                return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnMetaImpl.java`
#### Snippet
```java
    public List<TransactionSubscription> ackedPartitions() {
        List<TransactionSubscription> returnedPartitions;
        synchronized (this) {
            returnedPartitions = new ArrayList<>(ackedPartitions.size());
            returnedPartitions.addAll(ackedPartitions);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnMetaImpl.java`
#### Snippet
```java
    public List<String> producedPartitions() {
        List<String> returnedPartitions;
        synchronized (this) {
            returnedPartitions = new ArrayList<>(producedPartitions.size());
            returnedPartitions.addAll(producedPartitions);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java
        bookieMappingCache.get(BOOKIE_INFO_ROOT_PATH)
                .thenAccept(optVal -> {
                    synchronized (this) {
                        LOG.info("Bookie rack info updated to {}. Notifying rackaware policy.", optVal);
                        this.updateRacksWithHost(optVal.orElseGet(BookiesRackConfiguration::new));
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-broker-auth-athenz/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderAthenz.java`
#### Snippet
```java

            // Synchronize for non-thread safe static calls inside athenz library
            synchronized (this) {
                PublicKey ztsPublicKey = AuthZpeClient.getZtsPublicKey(token.getKeyId());

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
        // init the static jaasCredentialsContainer that shares amongst client.
        if (!initializedJAAS) {
            synchronized (this) {
                if (jaasCredentialsContainer == null) {
                    log.info("JAAS loginContext is: {}.", loginContextName);
```

## RuleId[ruleID=DoubleBraceInitialization]
### DoubleBraceInitialization
Double brace initialization
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
        // write the load factors
        setDynamicConfigurationToStore(LOADBALANCER_DYNAMIC_SETTING_LOAD_FACTOR_CPU_ZPATH,
                new HashMap<String, String>() {{
                    put(SETTING_NAME_LOAD_FACTOR_CPU, Double.toString(realtimeCpuLoadFactor));
                }});
```

### DoubleBraceInitialization
Double brace initialization
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
                }});
        setDynamicConfigurationToStore(LOADBALANCER_DYNAMIC_SETTING_LOAD_FACTOR_MEM_ZPATH,
                new HashMap<String, String>() {{
                    put(SETTING_NAME_LOAD_FACTOR_MEM, Double.toString(realtimeMemoryLoadFactor));
                }});
```

## RuleId[ruleID=UnstableTypeUsedInSignature]
### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.net.HostAndPort'
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
    }

    public List<HostAndPort> getHostAndPorts() {
        List<HostAndPort> hostAndPorts = Lists.newArrayList();
        Preconditions.checkNotNull(redisHosts, "redisHosts property not set.");
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionMetadataStoreProvider'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java


    public TransactionMetadataStoreService(TransactionMetadataStoreProvider transactionMetadataStoreProvider,
                                           PulsarService pulsarService, TransactionBufferClient tbClient,
                                           HashedWheelTimer timer) {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
    }

    public CompletableFuture<TransactionMetadataStore> openTransactionMetadataStore(TransactionCoordinatorID tcId) {
        final Timer brokerClientSharedTimer =
                pulsarService.getBrokerClientSharedTimer();
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
    }

    public CompletableFuture<Void> handleTcClientConnect(TransactionCoordinatorID tcId) {
        CompletableFuture<Void> completableFuture = new CompletableFuture<>();
        internalPinnedExecutor.execute(() -> {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TxnMeta'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
    }

    public CompletableFuture<TxnMeta> getTxnMeta(TxnID txnId) {
        TransactionCoordinatorID tcId = getTcIdFromTxnId(txnId);
        TransactionMetadataStore store = stores.get(tcId);
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
    }

    public CompletableFuture<TxnID> newTransaction(TransactionCoordinatorID tcId, long timeoutInMills) {
        TransactionMetadataStore store = stores.get(tcId);
        if (store == null) {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java

    @VisibleForTesting
    public Map<TransactionCoordinatorID, TransactionMetadataStore> getStores() {
        return Collections.unmodifiableMap(stores);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java

    // when managedLedger fence will remove this tc and reload
    public void handleOpFail(Throwable e, TransactionCoordinatorID tcId) {
        if (e instanceof ManagedLedgerException.ManagedLedgerFencedException) {
            removeTransactionMetadataStore(tcId);
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(TransactionMetadataStoreService.class);

    private final Map<TransactionCoordinatorID, TransactionMetadataStore> stores;
    private final TransactionMetadataStoreProvider transactionMetadataStoreProvider;
    private final PulsarService pulsarService;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
    }

    public CompletableFuture<Void> removeTransactionMetadataStore(TransactionCoordinatorID tcId) {
        final Semaphore tcLoadSemaphore = this.tcLoadSemaphores
                .computeIfAbsent(tcId.getId(), (id) -> new Semaphore(1));
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/cache/BundlesQuotas.java`
#### Snippet
```java
    }

    public BundlesQuotas(MetadataStore localStore) {
        this.resourceQuotaCache = localStore.getMetadataCache(ResourceQuota.class);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.delayed.DelayedDeliveryTrackerFactory'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/DelayedDeliveryTrackerLoader.java`
#### Snippet
```java
@UtilityClass
public class DelayedDeliveryTrackerLoader {
    public static DelayedDeliveryTrackerFactory loadDelayedDeliveryTrackerFactory(ServiceConfiguration conf)
            throws IOException {
        try {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.delayed.DelayedDeliveryTracker'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/InMemoryDelayedDeliveryTrackerFactory.java`
#### Snippet
```java

    @Override
    public DelayedDeliveryTracker newTracker(PersistentDispatcherMultipleConsumers dispatcher) {
        return new InMemoryDelayedDeliveryTracker(dispatcher, timer, tickTimeMillis,
                isDelayedDeliveryDeliverAtTimeStrict, fixedDelayDetectionLookahead);
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionBufferProvider'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java

    private TransactionMetadataStoreService transactionMetadataStoreService;
    private TransactionBufferProvider transactionBufferProvider;
    private TransactionBufferClient transactionBufferClient;
    private HashedWheelTimer transactionTimer;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionBufferProvider'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    }

    public TransactionBufferProvider getTransactionBufferProvider() {
        return transactionBufferProvider;
    }
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private CoordinationService coordinationService;
    private TransactionBufferSnapshotServiceFactory transactionBufferSnapshotServiceFactory;
    private MetadataStore configurationMetadataStore;
    private PulsarMetadataEventSynchronizer configMetadataSynchronizer;
    private boolean shouldShutdownConfigurationMetadataStore;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    }

    public MetadataStore createConfigurationMetadataStore(PulsarMetadataEventSynchronizer synchronizer)
            throws MetadataStoreException {
        return MetadataStoreFactory.create(config.getConfigurationMetadataStoreUrl(),
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionMeta'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/TransactionCursor.java`
#### Snippet
```java
     * @return
     */
    CompletableFuture<TransactionMeta> getTxnMeta(TxnID txnID, boolean createIfNotExist);

    /**
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionBufferReader'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TransactionBufferDisable.java`
#### Snippet
```java

    @Override
    public CompletableFuture<TransactionBufferReader> openTransactionBufferReader(TxnID txnID, long startSequenceId) {
        return FutureUtil.failedFuture(new BrokerServiceException.NotAllowedException("Transaction buffer disable!"));
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionMeta'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TransactionBufferDisable.java`
#### Snippet
```java

    @Override
    public CompletableFuture<TransactionMeta> getTransactionMeta(TxnID txnID) {
        return CompletableFuture.completedFuture(null);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionBuffer'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBufferProvider.java`
#### Snippet
```java

    @Override
    public TransactionBuffer newTransactionBuffer(Topic originTopic) {
        return new TopicTransactionBuffer((PersistentTopic) originTopic);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionBuffer'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBufferProvider.java`
#### Snippet
```java

    @Override
    public TransactionBuffer newTransactionBuffer(Topic originTopic) {
        return new InMemTransactionBuffer(originTopic);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionEntry'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBufferReader.java`
#### Snippet
```java

    @Override
    public synchronized CompletableFuture<List<TransactionEntry>> readNext(int numEntries) {
        CompletableFuture<List<TransactionEntry>> readFuture = new CompletableFuture<>();

```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionBufferReader'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java

    @Override
    public CompletableFuture<TransactionBufferReader> openTransactionBufferReader(TxnID txnID, long startSequenceId) {
        return null;
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionMeta'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java

    @Override
    public CompletableFuture<TransactionMeta> getTransactionMeta(TxnID txnID) {
        return CompletableFuture.completedFuture(null);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/timeout/TransactionTimeoutTrackerFactoryImpl.java`
#### Snippet
```java

    @Override
    public TransactionTimeoutTracker newTracker(TransactionCoordinatorID tcID) {
        return new TransactionTimeoutTrackerImpl(tcID.getId(), timer, tickTimeMillis, transactionMetadataStoreService);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionTimeoutTracker'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/recover/TransactionRecoverTrackerImpl.java`
#### Snippet
```java
    private final Set<Long> abortingTransactions;

    public TransactionRecoverTrackerImpl(TransactionMetadataStoreService transactionMetadataStoreService,
                                  TransactionTimeoutTracker timeoutTracker, long tcId) {
        this.tcId = tcId;
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.broker.transaction.buffer.TransactionBuffer'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

    @Getter
    protected final TransactionBuffer transactionBuffer;

    // Record the last time a data message (ie: not an internal Pulsar marker) is published on the topic
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.broker.delayed.DelayedDeliveryTrackerFactory'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    }

    public DelayedDeliveryTrackerFactory getDelayedDeliveryTrackerFactory() {
        return delayedDeliveryTrackerFactory;
    }
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.broker.delayed.DelayedDeliveryTrackerFactory'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    private final ReadWriteLock lock = new ReentrantReadWriteLock();

    private final DelayedDeliveryTrackerFactory delayedDeliveryTrackerFactory;
    private final ServerBootstrap defaultServerBootstrap;
    private final List<EventLoopGroup> protocolHandlersWorkerGroups = new ArrayList<>();
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/MetaStoreImpl.java`
#### Snippet
```java
    }

    public MetaStoreImpl(MetadataStore store, OrderedExecutor executor, String ledgerInfoCompressionType,
                         String cursorInfoCompressionType) {
        this.store = store;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/MetaStoreImpl.java`
#### Snippet
```java
    private final CompressionType cursorInfoCompressionType;

    public MetaStoreImpl(MetadataStore store, OrderedExecutor executor) {
        this.store = store;
        this.executor = executor;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreFactory.java`
#### Snippet
```java
     *             if the metadata store initialization fails
     */
    public static MetadataStore create(String metadataURL, MetadataStoreConfig metadataStoreConfig)
            throws MetadataStoreException {
        return MetadataStoreFactoryImpl.create(metadataURL, metadataStoreConfig);
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/MetadataStoreFactoryImpl.java`
#### Snippet
```java
public class MetadataStoreFactoryImpl {

    public static MetadataStore create(String metadataURL, MetadataStoreConfig metadataStoreConfig) throws
            MetadataStoreException {
        return newInstance(metadataURL, metadataStoreConfig, false);
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
    private LedgerManager.LedgerRange nextRange = null;

    public LegacyHierarchicalLedgerRangeIterator(MetadataStore store, String ledgersRoot) {
        this.store = store;
        this.ledgersRoot = ledgersRoot;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationClient.java`
#### Snippet
```java
    private final ScheduledExecutorService executor;

    public PulsarRegistrationClient(MetadataStore store,
                                    String ledgersRootPath) {
        this.store = store;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
    }

    public MetadataCacheImpl(MetadataStore store, MetadataSerde<T> serde, MetadataCacheConfig cacheConfig) {
        this.store = store;
        this.serde = serde;
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
public class MetadataCacheImpl<T> implements MetadataCache<T>, Consumer<Notification> {
    @Getter
    private final MetadataStore store;
    private final MetadataSerde<T> serde;

```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
    private final AsyncLoadingCache<String, Optional<CacheGetResult<T>>> objCache;

    public MetadataCacheImpl(MetadataStore store, TypeReference<T> typeRef, MetadataCacheConfig cacheConfig) {
        this(store, new JSONMetadataSerdeTypeRef<>(typeRef), cacheConfig);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
    }

    public MetadataCacheImpl(MetadataStore store, JavaType type, MetadataCacheConfig cacheConfig) {
        this(store, new JSONMetadataSerdeSimpleType<>(type), cacheConfig);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
    getLatestSinkExceptions();

    public String getStatsAsString() throws IOException {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
    getLatestSourceExceptions();

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
    getLatestUserExceptions();

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    protected final FunctionCollectorRegistry collectorRegistry;

    protected final EvictingQueue emptyQueue = EvictingQueue.create(0);

    public static final String USER_METRIC_PREFIX = "user_metric_";
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
    getLatestSystemExceptions();

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSourceExceptions() {
        return latestSourceExceptions;
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSinkExceptions() {
        return EvictingQueue.create(0);
    }
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSystemExceptions() {
        return latestSystemExceptions;
    }
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSourceExceptions =
            EvictingQueue.create(10);

```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestUserExceptions() {
        return EvictingQueue.create(0);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSinkExceptions() {
        return latestSinkExceptions;
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestUserExceptions() {
        return emptyQueue;
    }
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSinkExceptions =
            EvictingQueue.create(10);

```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSystemExceptions() {
        return latestSystemExceptions;
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSourceExceptions() {
        return emptyQueue;
    }
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);

```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSinkExceptions() {
        return emptyQueue;
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSourceExceptions() {
        return emptyQueue;
    }
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'com.google.common.collect.EvictingQueue'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java

    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestUserExceptions =
            EvictingQueue.create(10);
    @Getter
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/InMemTransactionMetadataStoreProvider.java`
#### Snippet
```java

    @Override
    public CompletableFuture<TransactionMetadataStore> openStore(TransactionCoordinatorID transactionCoordinatorId,
                                                                 ManagedLedgerFactory managedLedgerFactory,
                                                                 ManagedLedgerConfig managedLedgerConfig,
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStoreProvider.java`
#### Snippet
```java

    @Override
    public CompletableFuture<TransactionMetadataStore> openStore(TransactionCoordinatorID transactionCoordinatorId,
                                                                 ManagedLedgerFactory managedLedgerFactory,
                                                                 ManagedLedgerConfig managedLedgerConfig,
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
        }

        public TransactionMetadataStoreStateException(TransactionCoordinatorID tcID,
                                                      TransactionMetadataStoreState.State expectedState,
                                                      TransactionMetadataStoreState.State currentState,
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
        }

        public CoordinatorNotFoundException(TransactionCoordinatorID tcId) {
            super(String.format("Transaction coordinator with id %s not found!", tcId.getId()));
        }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionLogImpl.java`
#### Snippet
```java
    }

    public static TopicName getMLTransactionLogName(TransactionCoordinatorID tcID) {
        return TopicName.get(TopicDomain.persistent.value(),
                NamespaceName.SYSTEM_NAMESPACE, TRANSACTION_LOG_PREFIX + tcID.getId());
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionLogImpl.java`
#### Snippet
```java
    private final TxnLogBufferedWriterMetricsStats bufferedWriterMetrics;

    public MLTransactionLogImpl(TransactionCoordinatorID tcID,
                                ManagedLedgerFactory managedLedgerFactory,
                                ManagedLedgerConfig managedLedgerConfig,
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionMetadataStore'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStore.java`
#### Snippet
```java
    }

    public CompletableFuture<TransactionMetadataStore> init(TransactionRecoverTracker recoverTracker) {
        CompletableFuture<TransactionMetadataStore> completableFuture = new CompletableFuture<>();
        if (!changeToInitializingState()) {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStore.java`
#### Snippet
```java
    private final long maxActiveTransactionsPerCoordinator;

    public MLTransactionMetadataStore(TransactionCoordinatorID tcID,
                                      MLTransactionLogImpl mlTransactionLog,
                                      TransactionTimeoutTracker timeoutTracker,
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TxnMeta'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStore.java`
#### Snippet
```java

    @Override
    public CompletableFuture<TxnMeta> getTxnMeta(TxnID txnID) {
        Pair<TxnMeta, List<Position>> txnMetaListPair = txnMetaMap.get(txnID.getLeastSigBits());
        CompletableFuture<TxnMeta> completableFuture = new CompletableFuture<>();
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TransactionCoordinatorID'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStore.java`
#### Snippet
```java

    @Override
    public TransactionCoordinatorID getTransactionCoordinatorID() {
        return tcID;
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.transaction.coordinator.TxnMeta'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStore.java`
#### Snippet
```java

    @Override
    public List<TxnMeta> getSlowTransactions(long timeout) {
        List<TxnMeta> txnMetas = new ArrayList<>();
        txnMetaMap.forEach((k, v) -> {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java
    private Map<String, BookieInfo> bookieInfoMap = new HashMap<>();

    public static MetadataStore createMetadataStore(Configuration conf) throws MetadataException {
        MetadataStore store;
        Object storeProperty = conf.getProperty(METADATA_STORE_INSTANCE);
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.broker.web.plugin.servlet.AdditionalServlet'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServletWithClassLoader.java`
#### Snippet
```java
public class AdditionalServletWithClassLoader implements AdditionalServlet {

    private final AdditionalServlet servlet;
    private final NarClassLoader classLoader;

```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.bookkeeper.net.DNSToSwitchMapping'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java

    @Override
    public RackawareEnsemblePlacementPolicyImpl initialize(ClientConfiguration conf,
            Optional<DNSToSwitchMapping> optionalDnsResolver, HashedWheelTimer timer, FeatureProvider featureProvider,
            StatsLogger statsLogger, BookieAddressResolver bookieAddressResolver) {
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/PulsarResources.java`
#### Snippet
```java
    private final Optional<MetadataStore> localMetadataStore;
    @Getter
    private final Optional<MetadataStore> configurationMetadataStore;

    public PulsarResources(MetadataStore localMetadataStore, MetadataStore configurationMetadataStore) {
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/PulsarResources.java`
#### Snippet
```java
    private final Optional<MetadataStore> configurationMetadataStore;

    public PulsarResources(MetadataStore localMetadataStore, MetadataStore configurationMetadataStore) {
        this(localMetadataStore, configurationMetadataStore, DEFAULT_OPERATION_TIMEOUT_SEC);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/PulsarResources.java`
#### Snippet
```java
        this(localMetadataStore, configurationMetadataStore, DEFAULT_OPERATION_TIMEOUT_SEC);
    }
    public PulsarResources(MetadataStore localMetadataStore, MetadataStore configurationMetadataStore,
            int operationTimeoutSec) {
        if (configurationMetadataStore != null) {
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/PulsarResources.java`
#### Snippet
```java
    private final TopicResources topicResources;
    @Getter
    private final Optional<MetadataStore> localMetadataStore;
    @Getter
    private final Optional<MetadataStore> configurationMetadataStore;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/ResourceGroupResources.java`
#### Snippet
```java
    private static final String BASE_PATH = "/admin/resourcegroups";

    public ResourceGroupResources(MetadataStore store, int operationTimeoutSec) {
        super(store, ResourceGroup.class, operationTimeoutSec);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/LoadManagerReportResources.java`
#### Snippet
```java
public class LoadManagerReportResources extends BaseResources<LoadManagerReport> {

    public LoadManagerReportResources(MetadataStore configurationStore, int operationTimeoutSec) {
        super(configurationStore, LoadManagerReport.class, operationTimeoutSec);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BookieResources.java`
#### Snippet
```java
    private static final String AVAILABLE_BOOKIES_ROOT = "/ledgers/available";

    public BookieResources(MetadataStore store, int operationTimeoutSec) {
        super(store, BookiesRackConfiguration.class, operationTimeoutSec);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java
    private static final String BROKER_SERVICE_CONFIGURATION_PATH = "/admin/configuration";

    public DynamicConfigurationResources(MetadataStore store, int operationTimeoutSec) {
        super(store, new TypeReference<Map<String, String>>() {
        }, operationTimeoutSec);
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/LocalPoliciesResources.java`
#### Snippet
```java
public class LocalPoliciesResources extends BaseResources<LocalPolicies> {

    public LocalPoliciesResources(MetadataStore localStore, int operationTimeoutSec) {
        super(localStore, LocalPolicies.class, operationTimeoutSec);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/TenantResources.java`
#### Snippet
```java
@Slf4j
public class TenantResources extends BaseResources<TenantInfo> {
    public TenantResources(MetadataStore store, int operationTimeoutSec) {
        super(store, TenantInfo.class, operationTimeoutSec);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BrokerResources.java`
#### Snippet
```java
public class BrokerResources extends BaseResources<ClusterData> {

    public BrokerResources(MetadataStore store, int operationTimeoutSec) {
        super(store, ClusterData.class, operationTimeoutSec);
    }
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/TopicResources.java`
#### Snippet
```java
    private final Map<BiConsumer<String, NotificationType>, Pattern> topicListeners;

    public TopicResources(MetadataStore store) {
        this.store = store;
        topicListeners = new ConcurrentHashMap<>();
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/ClusterResources.java`
#### Snippet
```java
    private FailureDomainResources failureDomainResources;

    public ClusterResources(MetadataStore store, int operationTimeoutSec) {
        super(store, ClusterData.class, operationTimeoutSec);
        this.failureDomainResources = new FailureDomainResources(store, FailureDomainImpl.class, operationTimeoutSec);
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/ClusterResources.java`
#### Snippet
```java
        public static final String FAILURE_DOMAIN = "failureDomain";

        public FailureDomainResources(MetadataStore store, Class<FailureDomainImpl> clazz,
                int operationTimeoutSec) {
            super(store, clazz, operationTimeoutSec);
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BaseResources.java`
#### Snippet
```java
    private int operationTimeoutSec;

    public BaseResources(MetadataStore store, Class<T> clazz, int operationTimeoutSec) {
        this.store = store;
        this.cache = store.getMetadataCache(clazz);
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BaseResources.java`
#### Snippet
```java
    }

    public BaseResources(MetadataStore store, TypeReference<T> typeRef, int operationTimeoutSec) {
        this.store = store;
        this.cache = store.getMetadataCache(typeRef);
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BaseResources.java`
#### Snippet
```java

    @Getter
    private final MetadataStore store;
    @Getter
    private final MetadataCache<T> cache;
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
        private static final String PARTITIONED_TOPIC_PATH = "/admin/partitioned-topics";

        public PartitionedTopicResources(MetadataStore configurationStore, int operationTimeoutSec) {
            super(configurationStore, PartitionedTopicMetadata.class, operationTimeoutSec);
        }
```

### UnstableTypeUsedInSignature
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
    private final IsolationPolicyResources isolationPolicies;
    private final PartitionedTopicResources partitionedTopicResources;
    private final MetadataStore configurationStore;

    private static final String POLICIES_READONLY_FLAG_PATH = "/admin/flags/policies-readonly";
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
        private static final String NAMESPACE_ISOLATION_POLICIES = "namespaceIsolationPolicies";

        public IsolationPolicyResources(MetadataStore store, int operationTimeoutSec) {
            super(store, new TypeReference<Map<String, NamespaceIsolationDataImpl>>() {
            }, operationTimeoutSec);
```

### UnstableTypeUsedInSignature
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'org.apache.pulsar.metadata.api.MetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
    private static final String BUNDLE_DATA_BASE_PATH = "/loadbalance/bundle-data";

    public NamespaceResources(MetadataStore configurationStore, int operationTimeoutSec) {
        super(configurationStore, Policies.class, operationTimeoutSec);
        this.configurationStore = configurationStore;
```

## RuleId[ruleID=UnusedAssignment]
### UnusedAssignment
Variable `added` initializer `false` is redundant
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java
                File file = workQueue.take();

                boolean added = false;
                do {
                    added = inProcess.add(file);
```

### UnusedAssignment
Variable `removed` initializer `false` is redundant
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java
        } finally {

            boolean removed = false;
            do {
                removed = inProcess.remove(file);
```

### UnusedAssignment
Variable `added` initializer `false` is redundant
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java
            } while (!removed);

            boolean added = false;
            do {
                added = recentlyProcessed.add(file);
```

### UnusedAssignment
Variable `entrys` initializer `null` is redundant
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/MessageUtils.java`
#### Snippet
```java

            List<FlatMessage> flatMessages = new ArrayList<>();
            List<CanalEntry.Entry> entrys = null;
            if (message.isRaw()) {
                List<ByteString> rawEntries = message.getRawEntries();
```

### UnusedAssignment
Variable `event` initializer `null` is redundant
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/SinkOfFlume.java`
#### Snippet
```java
        Channel channel = getChannel();
        Transaction transaction = null;
        Event event = null;

        try {
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/AbstractSource.java`
#### Snippet
```java
                    while (blockingQueue != null && !blockingQueue.isEmpty()) {
                        ByteArrayOutputStream bos = new ByteArrayOutputStream();
                        ObjectOutput out = null;
                        out = new ObjectOutputStream(bos);
                        Map<String, Object> message = blockingQueue.take();
```

### UnusedAssignment
Variable `listener` initializer `null` is redundant
in `pulsar-io/aerospike/src/main/java/org/apache/pulsar/io/aerospike/AerospikeAbstractSink.java`
#### Snippet
```java
                keyValue.getKey().toString());
        Bin bin = new Bin(aerospikeSinkConfig.getColumnName(), Value.getAsBlob(keyValue.getValue()));
        AWriteListener listener = null;
        try {
            listener = queue.take();
```

### UnusedAssignment
Variable `schemaInfo` initializer `null` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaSchemaWrappedSchema.java`
#### Snippet
```java
public class KafkaSchemaWrappedSchema implements Schema<byte[]>, Serializable {

    private SchemaInfo schemaInfo = null;

    public KafkaSchemaWrappedSchema(org.apache.pulsar.kafka.shade.avro.Schema schema,
```

### UnusedAssignment
Variable `key` initializer `null` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
    public Pair<String, String> extractIdAndDocument(Record<GenericObject> record) throws JsonProcessingException {
        if (elasticSearchConfig.isSchemaEnable()) {
            Object key = null;
            GenericObject value = null;
            Schema<?> keySchema = null;
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
        if (elasticSearchConfig.isSchemaEnable()) {
            Object key = null;
            GenericObject value = null;
            Schema<?> keySchema = null;
            Schema<?> valueSchema = null;
```

### UnusedAssignment
Variable `valueSchema` initializer `null` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
            GenericObject value = null;
            Schema<?> keySchema = null;
            Schema<?> valueSchema = null;
            if (record.getSchema() != null && record.getSchema() instanceof KeyValueSchema) {
                KeyValueSchema<GenericObject, GenericObject> keyValueSchema = (KeyValueSchema) record.getSchema();
```

### UnusedAssignment
Variable `valBytes` initializer `null` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarOffsetBackingStore.java`
#### Snippet
```java
            ByteBuf bb = Unpooled.wrappedBuffer(key);
            byte[] keyBytes = ByteBufUtil.getBytes(bb);
            byte[] valBytes = null;
            if (value != null) {
                bb = Unpooled.wrappedBuffer(value);
```

### UnusedAssignment
Variable `timestamp` initializer `null` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
        taskContext.updateLastOffset(new TopicPartition(topic, partition), offset);

        Long timestamp = null;
        TimestampType timestampType = TimestampType.NO_TIMESTAMP_TYPE;
        if (sourceRecord.getEventTime().isPresent()) {
```

### UnusedAssignment
Variable `pulsarTopicList` initializer `null` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
                // no-op for now but add pulsar connector specific system tables here
            } else {
                List<String> pulsarTopicList = null;
                try {
                    pulsarTopicList = this.pulsarAdmin.topics()
```

### UnusedAssignment
Variable `proxyExtensions` initializer `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java
    private PulsarResources pulsarResources;
    @Getter
    private ProxyExtensions proxyExtensions = null;

    private final EventLoopGroup acceptorGroup;
```

### UnusedAssignment
Variable `namespaceName` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarInitialNamespaceSetup.java`
#### Snippet
```java
            PulsarResources pulsarResources = new PulsarResources(null, configStore);
            for (String namespace : arguments.namespaces) {
                NamespaceName namespaceName = null;
                try {
                    namespaceName = NamespaceName.get(namespace);
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/CmdUtility.java`
#### Snippet
```java
            proc = pb.start();
            reader = new BufferedReader(new InputStreamReader(proc.getInputStream(), UTF_8));
            String line = null;
            while ((line = reader.readLine()) != null) {
                if (writer != null) {
```

### UnusedAssignment
Variable `policies` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/NonPersistentTopics.java`
#### Snippet
```java
        log.info("[{}] list of topics on namespace {}/{}/{}", clientAppId(), property, cluster, namespace);

        Policies policies = null;
        NamespaceName nsName = null;
        try {
```

### UnusedAssignment
Variable `nsName` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/NonPersistentTopics.java`
#### Snippet
```java

        Policies policies = null;
        NamespaceName nsName = null;
        try {
            validateNamespaceName(property, cluster, namespace);
```

### UnusedAssignment
Variable `policies` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/NonPersistentTopics.java`
#### Snippet
```java
            @ApiParam(value = "Include system topic")
            @QueryParam("includeSystemTopic") boolean includeSystemTopic) {
        Policies policies = null;
        try {
            validateNamespaceName(tenant, namespace);
```

### UnusedAssignment
Variable `statFuture` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/NonPersistentTopics.java`
#### Snippet
```java

                FutureUtil.waitForAll(topicStatsFutureList).handle((result, exception) -> {
                    CompletableFuture<TopicStats> statFuture = null;
                    for (int i = 0; i < topicStatsFutureList.size(); i++) {
                        statFuture = topicStatsFutureList.get(i);
```

### UnusedAssignment
Variable `bundleData` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
                        .getBundlesAsync(namespaceName)
                        .thenCompose(bundles -> {
                    BundlesData bundleData = null;
                    try {
                        bundleData = bundles.getBundlesData();
```

### UnusedAssignment
Variable `ratePeriodInSeconds` initializer `1` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/NamespaceStats.java`
#### Snippet
```java
    public long[] addLatencyBucket = new long[ENTRY_LATENCY_BUCKETS_USEC.length + 1];
    public static final String[] ADD_LATENCY_BUCKET_KEYS = new String[ENTRY_LATENCY_BUCKETS_USEC.length + 1];
    private int ratePeriodInSeconds = 1;

    static {
```

### UnusedAssignment
Variable `info` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/MBeanStatsGenerator.java`
#### Snippet
```java
        ObjectName objName = instance.getObjectName();

        MBeanInfo info = null;

        try {
```

### UnusedAssignment
Variable `metrics` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/MBeanStatsGenerator.java`
#### Snippet
```java
        }

        Metrics metrics = null;

        // create a metrics instance by MBean dimension
```

### UnusedAssignment
Variable `config` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(PulsarService.class);
    private static final double GRACEFUL_SHUTDOWN_TIMEOUT_RATIO_OF_TOTAL_TIMEOUT = 0.5d;
    private ServiceConfiguration config = null;
    private NamespaceService nsService = null;
    private ManagedLedgerStorage managedLedgerClientFactory = null;
```

### UnusedAssignment
Variable `ackedCount` initializer `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
            MessageIdData msgId = ack.getMessageIdAt(i);
            PositionImpl position;
            long ackedCount = 0;
            long batchSize = getBatchSize(msgId);
            Consumer ackOwnerConsumer = getAckOwnerConsumer(msgId.getLedgerId(), msgId.getEntryId());
```

### UnusedAssignment
Variable `ackedCount` initializer `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
            PositionImpl position = PositionImpl.get(msgId.getLedgerId(), msgId.getEntryId());
            // acked count at least one
            long ackedCount = 0;
            long batchSize = 0;
            if (msgId.hasBatchSize()) {
```

### UnusedAssignment
Variable `locatorEntry` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java

    public List<Long> getSchemaLedgerList(String key) throws IOException {
        Optional<LocatorEntry> locatorEntry = null;
        try {
            locatorEntry = getLocator(key).get();
```

### UnusedAssignment
Variable `statFuture` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

            FutureUtil.waitForAll(topicStatsFutureList).handle((result, exception) -> {
                CompletableFuture<TopicStats> statFuture = null;
                for (int i = 0; i < topicStatsFutureList.size(); i++) {
                    statFuture = topicStatsFutureList.get(i);
```

### UnusedAssignment
Variable `statFuture` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

            FutureUtil.waitForAll(topicStatsFutureList).handle((result, exception) -> {
                CompletableFuture<PersistentTopicInternalStats> statFuture = null;
                for (int i = 0; i < topicStatsFutureList.size(); i++) {
                    statFuture = topicStatsFutureList.get(i);
```

### UnusedAssignment
Variable `msg` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
        CompletableFuture<Consumer> consumerFuture = consumers.get(consumerId);
        Consumer consumer = consumerFuture.getNow(null);
        ByteBuf msg = null;

        if (consumer == null) {
```

### UnusedAssignment
Variable `error` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    // We need to wait until the previous producer creation request
                    // either complete or fails.
                    ServerError error = null;
                    if (!existingProducerFuture.isDone()) {
                        error = ServerError.ServiceNotReady;
```

### UnusedAssignment
Variable `role` initializer `""` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java

            if (log.isDebugEnabled()) {
                String role = "";
                if (authState != null && authState.isComplete()) {
                    role = authState.getAuthRole();
```

### UnusedAssignment
Variable `isPublishRateExceeded` initializer `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java

    public void startSendOperation(Producer producer, int msgSize, int numMessages) {
        boolean isPublishRateExceeded = false;
        if (preciseTopicPublishRateLimitingEnable) {
            boolean isPreciseTopicPublishRateExceeded =
```

### UnusedAssignment
Variable `result` initializer `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
            ManagedLedgerImpl managedLedger = ((ManagedLedgerImpl) cursor.getManagedLedger());
            PositionImpl markDeletedPosition = (PositionImpl) cursor.getMarkDeletedPosition();
            long result = 0;
            try {
                result = managedLedger.getEarliestMessagePublishTimeOfPos(markDeletedPosition).get();
```

### UnusedAssignment
The value `SubscriptionAuthMode.None` assigned to `subscriptionAuthMode` is never used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
        validateNamespacePolicyOperation(namespaceName, PolicyName.SUBSCRIPTION_AUTH_MODE, PolicyOperation.WRITE);
        validatePoliciesReadOnlyAccess();
        SubscriptionAuthMode authMode = subscriptionAuthMode == null ? subscriptionAuthMode = SubscriptionAuthMode.None
                : subscriptionAuthMode;
        try {
```

### UnusedAssignment
Variable `brokerVersionVersion` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/BrokerVersionFilter.java`
#### Snippet
```java
            }

            Version brokerVersionVersion = null;
            try {
                brokerVersionVersion = Version.valueOf(brokerVersion);
```

### UnusedAssignment
Variable `latestVersion` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/BrokerVersionFilter.java`
#### Snippet
```java
        }

        com.github.zafarkhaja.semver.Version latestVersion = null;
        try {
            latestVersion = getLatestVersionNumber(brokers, loadData);
```

### UnusedAssignment
Variable `selectedBroker` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private synchronized ResourceUnit getLeastLoadedBroker(ServiceUnitId serviceUnit,
            Map<Long, Set<ResourceUnit>> availableBrokers) {
        ResourceUnit selectedBroker = null;
        // If the broker is already assigned, return that candidate.
        for (final Map.Entry<ResourceUnit, ResourceUnitRanking> entry : resourceUnitRankings.entrySet()) {
```

### UnusedAssignment
Variable `realtimeAvgResourceQuota` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private double realtimeMemoryLoadFactor = 25.0;
    // realtime average resource quota
    private ResourceQuota realtimeAvgResourceQuota = null;
    // realtime resource quota calculated from the latest load reports
    private AtomicReference<Map<String, ResourceQuota>> realtimeResourceQuotas = new AtomicReference<>();
```

### UnusedAssignment
Variable `totalMsgRate` initializer `0.0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
            double totalMsgRateIn = 0.0;
            double totalMsgRateOut = 0.0;
            double totalMsgRate = 0.0;
            double totalCpuUsage = 0.0;
            double totalMemoryUsage = 0.0;
```

### UnusedAssignment
Variable `preAllocatedBundles` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
                // calculate rankings
                Set<String> loadedBundles = loadReport.getBundles();
                Set<String> preAllocatedBundles = null;
                if (resourceUnitRankings.containsKey(resourceUnit)) {
                    preAllocatedBundles = resourceUnitRankings.get(resourceUnit).getPreAllocatedBundles();
```

### UnusedAssignment
Variable `finalRank` initializer `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
                double loadPercentage = ranking.getEstimatedLoadPercentage();
                long maxCapacity = ranking.estimateMaxCapacity(defaultResourceQuota);
                long finalRank = 0;
                if (strategy.equals(LOADBALANCER_STRATEGY_LLS)) {
                    finalRank = (long) loadPercentage;
```

### UnusedAssignment
Variable `backlogEstimate` initializer `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            if (isCompactionEnabled() && currentCompaction.isDone()) {

                long backlogEstimate = 0;

                PersistentSubscription compactionSub = subscriptions.get(COMPACTION_SUBSCRIPTION);
```

### UnusedAssignment
Variable `cursor` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

                Position startPosition = new PositionImpl(ledgerId, entryId);
                ManagedCursor cursor = null;
                try {
                    cursor = ledger.newNonDurableCursor(startPosition, subscriptionName, initialPosition,
```

### UnusedAssignment
Variable `nsFullBundle` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
    public boolean registerNamespace(NamespaceName nsname, boolean ensureOwned) throws PulsarServerException {
        try {
            NamespaceBundle nsFullBundle = null;

            // all pre-registered namespace is assumed to have bundles disabled
```

### UnusedAssignment
Variable `adminClient` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java

    public void unloadSLANamespace() throws Exception {
        PulsarAdmin adminClient = null;
        NamespaceName namespaceName = getSLAMonitorNamespace(host, config);

```

### UnusedAssignment
Variable `candidateBroker` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
            return;
        }
        String candidateBroker = null;
        String candidateBrokerAdvertisedAddr = null;

```

### UnusedAssignment
Variable `authorizationService` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    private final ConcurrentLinkedQueue<TopicLoadingContext> pendingTopicLoadingQueue;

    private AuthorizationService authorizationService = null;
    private final ScheduledExecutorService statsUpdater;
    @Getter
```

### UnusedAssignment
Variable `newRange` initializer `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java
            long upperBound = partitions[i];
            checkArgument(upperBound >= lowerBound);
            Range<Long> newRange = null;
            if (i != partitions.length - 1) {
                newRange = Range.range(lowerBound, BoundType.CLOSED, upperBound, BoundType.OPEN);
```

### UnusedAssignment
Variable `partitionStats` initializer `Collections.emptyMap()` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedTopicProducerStatsRecorderImpl.java`
#### Snippet
```java

    private static final long serialVersionUID = 1L;
    private Map<String, ProducerStats> partitionStats = Collections.emptyMap();
    private final DoubleAdder sendMsgsRateAggregate;
    private final DoubleAdder sendBytesRateAggregate;
```

### UnusedAssignment
Variable `sslCtx` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/HttpClient.java`
#### Snippet
```java

                if (conf.isUseKeyStoreTls()) {
                    SSLContext sslCtx = null;
                    KeyStoreParams params = authData.hasDataForTls() ? authData.getTlsKeyStoreParams() :
                            new KeyStoreParams(conf.getTlsKeyStoreType(), conf.getTlsKeyStorePath(),
```

### UnusedAssignment
Variable `sslCtx` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/HttpClient.java`
#### Snippet
```java
                        sslProvider = SslProvider.valueOf(conf.getSslProvider());
                    }
                    SslContext sslCtx = null;
                    if (authData.hasDataForTls()) {
                        sslCtx = authData.getTlsTrustStoreStream() == null
```

### UnusedAssignment
Variable `recheckPatternTimeout` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PatternMultiTopicsConsumerImpl.java`
#### Snippet
```java
    private final CompletableFuture<TopicListWatcher> watcherFuture;
    protected NamespaceName namespaceName;
    private volatile Timeout recheckPatternTimeout = null;
    private volatile String topicsHash;

```

### UnusedAssignment
Variable `op` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java

    void ackReceived(ClientCnx cnx, long sequenceId, long highestSequenceId, long ledgerId, long entryId) {
        OpSendMsg op = null;
        synchronized (this) {
            op = pendingMessages.peek();
```

### UnusedAssignment
Variable `validateDefaults` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/util/SchemaUtil.java`
#### Snippet
```java
            return parseAvroSchema(schemaDefinition.getJsonDef());
        } else if (pojo != null) {
            ThreadLocal<Boolean> validateDefaults = null;

            try {
```

### UnusedAssignment
Variable `schemaInfo` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
                        + "using AutoConsumeSchema because SchemaInfoProvider is not set yet");
            } else {
                SchemaInfo schemaInfo = null;
                try {
                    schemaInfo = schemaInfoProvider.getSchemaByVersion(schemaVersion.bytes()).get();
```

### UnusedAssignment
Variable `outputBytes` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/writer/AvroWriter.java`
#### Snippet
```java
    @Override
    public synchronized byte[] write(T message) {
        byte[] outputBytes = null;
        try {
            writer.write(message, this.encoder);
```

### UnusedAssignment
Variable `uri` initializer `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/net/ServiceURI.java`
#### Snippet
```java
                                           String[] serviceInfos,
                                           String hostname) {
        URI uri = null;
        try {
            uri = URI.create("dummyscheme://" + hostname);
```

### UnusedAssignment
Variable `keyStore` initializer `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/KeyStoreHolder.java`
#### Snippet
```java
public class KeyStoreHolder {

    private KeyStore keyStore = null;

    public KeyStoreHolder() throws KeyStoreException {
```

### UnusedAssignment
Variable `canAcquire` initializer `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/RateLimiter.java`
#### Snippet
```java
        }

        boolean canAcquire = false;
        do {
            canAcquire = acquirePermit < 0 || acquiredPermits < this.permits;
```

### UnusedAssignment
Variable `result` initializer `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NamespaceBundleStatsComparator.java`
#### Snippet
```java
    // sort in reverse order, maximum loaded should be on top
    public int compare(String a, String b) {
        int result = 0;
        if (this.resType == ResourceType.CPU) {
            result = map.get(a).compareByMsgRate(map.get(b));
```

### UnusedAssignment
Variable `privateKey` initializer `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/KeyManagerProxy.java`
#### Snippet
```java

        X509Certificate certificate;
        PrivateKey privateKey = null;
        KeyStore keyStore;
        try (InputStream publicCertStream = new FileInputStream(certFile.getFileName());
```

### UnusedAssignment
Variable `threadSafe` initializer `true` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java

    protected final NavigableMap<Long, BitSet> rangeBitSetMap = new ConcurrentSkipListMap<>();
    private boolean threadSafe = true;
    private final int bitSetSize;
    private final LongPairConsumer<T> consumer;
```

### UnusedAssignment
Variable `seek` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        return seekAsyncCheckState(seekBy).orElseGet(() -> {
            long requestId = client.newRequestId();
            ByteBuf seek = null;
            if (messageId instanceof BatchMessageIdImpl) {
                BatchMessageIdImpl msgId = (BatchMessageIdImpl) messageId;
```

### UnusedAssignment
Variable `bitSetRecyclable` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                                                                        ValidationError validationError,
                                                                        Map<String, Long> properties, TxnID txnID) {
        BitSetRecyclable bitSetRecyclable = null;
        long ledgerId;
        long entryId;
```

### UnusedAssignment
Variable `expireTimeOfIncompleteChunkedMessageMillis` initializer `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            ConcurrentOpenHashMap.<String, ChunkedMessageCtx>newBuilder().build();
    private int pendingChunkedMessageCount = 0;
    protected long expireTimeOfIncompleteChunkedMessageMillis = 0;
    private final AtomicBoolean expireChunkMessageTaskScheduled = new AtomicBoolean(false);
    private final int maxPendingChunkedMessage;
```

### UnusedAssignment
Variable `chunkedMsgCtx` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            return;
        }
        ChunkedMessageCtx chunkedMsgCtx = null;
        String messageUUID;
        while ((messageUUID = pendingChunkedMessageUuidQueue.peek()) != null) {
```

### UnusedAssignment
Variable `policies` initializer `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicies.java`
#### Snippet
```java
public class NamespaceIsolationPolicies {

    private Map<String, NamespaceIsolationDataImpl> policies = null;

    public NamespaceIsolationPolicies() {
```

### UnusedAssignment
Variable `currentLine` initializer `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
            }
            StringBuilder sb = new StringBuilder();
            String currentLine = null;

            // Jump to the first line after -----BEGIN [RSA] PRIVATE KEY-----
```

### UnusedAssignment
Variable `trustManagers` initializer `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
            throws GeneralSecurityException {
        KeyStoreHolder ksh = new KeyStoreHolder();
        TrustManager[] trustManagers = null;
        KeyManager[] keyManagers = null;
        Provider provider = resolveProvider(providerName);
```

### UnusedAssignment
Variable `keyManagers` initializer `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
        KeyStoreHolder ksh = new KeyStoreHolder();
        TrustManager[] trustManagers = null;
        KeyManager[] keyManagers = null;
        Provider provider = resolveProvider(providerName);

```

### UnusedAssignment
The value `systemResourceUsage.bandwidthOut.percentUsage()` assigned to `maxUsage` is never used
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java

        if (systemResourceUsage.bandwidthOut.percentUsage() > maxUsage) {
            maxUsage = systemResourceUsage.bandwidthOut.percentUsage();
            type = ResourceType.BandwidthOut;
        }
```

### UnusedAssignment
Variable `cachedData` initializer `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
        // retain actually the full 64Kb even for a small entry
        int size = entry.getLength();
        ByteBuf cachedData = null;
        try {
            cachedData = ALLOCATOR.directBuffer(size, size);
```

### UnusedAssignment
Variable `paused` initializer `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    AtomicInteger allTopicPartitionsNumber;

    private boolean paused = false;
    private final Object pauseMutex = new Object();
    // timeout related to auto check and subscribe partition increasement
```

### UnusedAssignment
Variable `startMessageId` initializer `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    private final ConsumerConfigurationData<T> internalConfig;

    private volatile BatchMessageIdImpl startMessageId = null;
    private final long startMessageRollbackDurationInSec;
    MultiTopicsConsumerImpl(PulsarClientImpl client, ConsumerConfigurationData<T> conf,
```

### UnusedAssignment
Variable `entriesByteBuf` initializer `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlockAwareSegmentInputStreamImpl.java`
#### Snippet
```java
    static final int ENTRY_HEADER_SIZE = 4 /* entry size */ + 8 /* entry id */;
    // Keep a list of all entries ByteBuf, each ByteBuf contains 2 buf: entry header and entry content.
    private List<ByteBuf> entriesByteBuf = null;
    private LedgerOffloaderStats offloaderStats;
    private String topicName;
```

### UnusedAssignment
Variable `groupedReaders` initializer `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java
            }
            List<LedgerEntry> entries = new ArrayList<LedgerEntry>();
            List<GroupedReader> groupedReaders = null;
            try {
                groupedReaders = getGroupedReader(firstEntry, lastEntry);
```

### UnusedAssignment
Variable `state` initializer `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java
    private final List<DataInputStream> dataStreams;
    private final ExecutorService executor;
    private State state = null;

    enum State {
```

### UnusedAssignment
Variable `state` initializer `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImpl.java`
#### Snippet
```java
    }

    private State state = null;

    private BlobStoreBackedReadHandleImpl(long ledgerId, OffloadIndexBlock index,
```

### UnusedAssignment
Variable `jcloudCred` initializer `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
                config.setProviderCredentials(() -> {
                    AWSCredentials newCreds = authChain.getCredentials();
                    Credentials jcloudCred = null;

                    if (newCreds instanceof AWSSessionCredentials) {
```

### UnusedAssignment
Variable `managedLedgerOffloadPrefetchRounds` initializer `1` is redundant
in `tiered-storage/file-system/src/main/java/org/apache/bookkeeper/mledger/offload/filesystem/impl/FileSystemManagedLedgerOffloader.java`
#### Snippet
```java
        volatile Exception fileSystemWriteException = null;
        private OrderedScheduler assignmentScheduler;
        private int managedLedgerOffloadPrefetchRounds = 1;
        private final LedgerOffloaderStats offloaderStats;

```

### UnusedAssignment
Variable `hasMoreElements` initializer `false` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
    private synchronized void preload() throws IOException {
        while (nextRange == null && !iteratorDone) {
            boolean hasMoreElements = false;
            try {
                if (l1NodesIter == null) {
```

### UnusedAssignment
Variable `ledgerNodes` initializer `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
                .append(level1).append("/").append(level2);
        String nodePath = nodeBuilder.toString();
        List<String> ledgerNodes = null;
        try {
            ledgerNodes = store.getChildren(nodePath).get();
```

### UnusedAssignment
Variable `ledgerIdGenPath` initializer `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java

    private static String createLedgerPrefix(String ledgersPath, String idGenZnodeName) {
        String ledgerIdGenPath = null;
        if (StringUtils.isBlank(idGenZnodeName)) {
            ledgerIdGenPath = ledgersPath;
```

### UnusedAssignment
Variable `mpu` initializer `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
            log.info("ledger {} dataBlockKey {} indexBlockKey {}", readHandle.getId(), dataBlockKey, indexBlockKey);

            MultipartUpload mpu = null;
            List<MultipartPart> parts = Lists.newArrayList();

```

### UnusedAssignment
The value `null` assigned to `mpu` is never used
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                String etag = writeBlobStore.completeMultipartUpload(mpu, parts);
                log.info("Ledger {}, upload finished, etag {}", readHandle.getId(), etag);
                mpu = null;
            } catch (Throwable t) {
                try {
```

### UnusedAssignment
Variable `bytes` initializer `null` is redundant
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java

    public static byte[] toByteArray(Object obj) throws IOException {
        byte[] bytes = null;
        try (ByteArrayOutputStream bos = new ByteArrayOutputStream();
             ObjectOutputStream oos = new ObjectOutputStream(bos)) {
```

### UnusedAssignment
Variable `topicStats` initializer `null` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/MembershipManager.java`
#### Snippet
```java

        List<WorkerInfo> workerIds = new LinkedList<>();
        TopicStats topicStats = null;
        try {
            topicStats = this.pulsarAdmin.topics().getStats(this.workerConfig.getClusterCoordinationTopic());
```

### UnusedAssignment
Variable `topicStats` initializer `null` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/MembershipManager.java`
#### Snippet
```java

    public WorkerInfo getLeader() {
        TopicStats topicStats = null;
        try {
            topicStats = this.pulsarAdmin.topics().getStats(this.workerConfig.getClusterCoordinationTopic());
```

### UnusedAssignment
The value `sub.getConsumers().stream()
.map(consumerStats -> consumerStats.getMetadata())
.collect(Collectors.toList())` assigned to `existingConsumers` is never used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
                        sub = stats.getSubscriptions().get(subscriptionName);
                        if (sub != null) {
                            existingConsumers = sub.getConsumers().stream()
                              .map(consumerStats -> consumerStats.getMetadata())
                              .collect(Collectors.toList());
```

### UnusedAssignment
Variable `needsScheduling` initializer `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionMetaDataManager.java`
#### Snippet
```java
        log.debug("Process update request: {}", updateRequestFs);

        boolean needsScheduling = false;

        // Worker doesn't know about the function so far
```

### UnusedAssignment
Variable `read` initializer `0` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/WorkerUtils.java`
#### Snippet
```java
        DistributedLogManager dlm = namespace.openLog(packagePath);
        try (InputStream in = new DLInputStream(dlm)) {
            int read = 0;
            byte[] bytes = new byte[1024];
            while ((read = in.read(bytes)) != -1) {
```

### UnusedAssignment
Variable `read` initializer `0` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/WorkerUtils.java`
#### Snippet
```java

                try (OutputStream out = new DLOutputStream(dlm, writer)) {
                    int read = 0;
                    byte[] bytes = new byte[1024];
                    while ((read = uploadedInputStream.read(bytes)) != -1) {
```

### UnusedAssignment
Variable `functionDetails` initializer `null` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
        }

        Function.FunctionDetails functionDetails = null;
        boolean isPkgUrlProvided = isNotBlank(sourcePkgUrl);
        File componentPackageFile = null;
```

### UnusedAssignment
Variable `token` initializer `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/ClearTextFunctionTokenAuthProvider.java`
#### Snippet
```java
                                                    AuthenticationDataSource authenticationDataSource)
            throws Exception {
        String token = null;
        try {
            token = getToken(authenticationDataSource);
```

### UnusedAssignment
Variable `startPosition` initializer `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            FindEntryCallback callback, Object ctx) {
        OpFindNewest op;
        PositionImpl startPosition = null;
        long max = 0;
        switch (constraint) {
```

### UnusedAssignment
Variable `max` initializer `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        OpFindNewest op;
        PositionImpl startPosition = null;
        long max = 0;
        switch (constraint) {
        case SearchAllAvailableEntries:
```

### UnusedAssignment
Variable `skippedEntries` initializer `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

        if (!newMarkDeletePosition.equals(oldMarkDeletePosition)) {
            long skippedEntries = 0;
            if (newMarkDeletePosition.getLedgerId() == oldMarkDeletePosition.getLedgerId()
                    && newMarkDeletePosition.getEntryId() == oldMarkDeletePosition.getEntryId() + 1) {
```

### UnusedAssignment
The value `coreClient.createNamespacedService(jobNamespace, service, null, null, null)` assigned to `response` is never used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                    final V1Service response;
                    try {
                        response = coreClient.createNamespacedService(jobNamespace, service, null, null, null);
                    } catch (ApiException e) {
                        // already exists
```

### UnusedAssignment
The value `appsClient.createNamespacedStatefulSet(jobNamespace, statefulSet, null, null, null)` assigned to `response` is never used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                    final V1StatefulSet response;
                    try {
                        response = appsClient.createNamespacedStatefulSet(jobNamespace, statefulSet, null, null, null);
                    } catch (ApiException e) {
                        // already exists
```

### UnusedAssignment
Variable `clientBuilder` initializer `null` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceUtils.java`
#### Snippet
```java
                                                          AuthenticationConfig authConfig,
                                                          Optional<Long> memoryLimit) throws PulsarClientException {
        ClientBuilder clientBuilder = null;
        if (isNotBlank(pulsarServiceUrl)) {
            clientBuilder = PulsarClient.builder()
```

### UnusedAssignment
Variable `pulsarAdminBuilder` initializer `null` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceUtils.java`
#### Snippet
```java
    public static PulsarAdmin createPulsarAdminClient(String pulsarWebServiceUrl, AuthenticationConfig authConfig)
            throws PulsarClientException {
        PulsarAdminBuilder pulsarAdminBuilder = null;
        if (isNotBlank(pulsarWebServiceUrl)) {
            pulsarAdminBuilder = PulsarAdmin.builder().serviceHttpUrl(pulsarWebServiceUrl);
```

### UnusedAssignment
Variable `windowEvents` initializer `null` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowManager.java`
#### Snippet
```java
    @Override
    public boolean onTrigger() {
        List<Event<T>> windowEvents = null;
        List<Event<T>> expired = null;

```

### UnusedAssignment
Variable `expired` initializer `null` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowManager.java`
#### Snippet
```java
    public boolean onTrigger() {
        List<Event<T>> windowEvents = null;
        List<Event<T>> expired = null;

        lock.lock();
```

### UnusedAssignment
Variable `rawPayload` initializer `null` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ProducerHandler.java`
#### Snippet
```java
        }
        ProducerMessage sendRequest;
        byte[] rawPayload = null;
        String requestContext = null;
        try {
```

### UnusedAssignment
Variable `inactiveLedgerRollOverTimeMs` initializer `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    private volatile long lastAddEntryTimeMs = 0;
    private long inactiveLedgerRollOverTimeMs = 0;

    protected static final int DEFAULT_LEDGER_DELETE_RETRIES = 3;
```

### UnusedAssignment
Variable `maxBacklogBetweenCursorsForCaching` initializer `1000` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private int minBacklogCursorsForCaching = 0;
    private int minBacklogEntriesForCaching = 1000;
    private int maxBacklogBetweenCursorsForCaching = 1000;

    private static final Random random = new Random(System.currentTimeMillis());
```

### UnusedAssignment
Variable `size` initializer `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                return 0;
            }
            long size = 0;
            final long slowestConsumerLedgerId = pos.getLedgerId();

```

### UnusedAssignment
Variable `ledgerInfo` initializer `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            }

            LedgerInfo ledgerInfo = null;
            synchronized (this) {
                ledgerInfo = ledgers.get(pos.getLedgerId());
```

### UnusedAssignment
Variable `cursorsInSameBacklogRange` initializer `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            }
        }
        int cursorsInSameBacklogRange = 0;
        for (java.util.Map.Entry<ManagedCursorImpl, Long> cursor : cursorBacklogMap.entrySet()) {
            cursorsInSameBacklogRange = 0;
```

### UnusedAssignment
Variable `slowestReaderLedgerId` initializer `-1` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            }

            long slowestReaderLedgerId = -1;
            if (!cursors.hasDurableCursors()) {
                // At this point the lastLedger will be pointing to the
```

### UnusedAssignment
Variable `minBacklogCursorsForCaching` initializer `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private volatile long lastOffloadFailureTimestamp = 0;

    private int minBacklogCursorsForCaching = 0;
    private int minBacklogEntriesForCaching = 1000;
    private int maxBacklogBetweenCursorsForCaching = 1000;
```

### UnusedAssignment
Variable `minBacklogEntriesForCaching` initializer `1000` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    private int minBacklogCursorsForCaching = 0;
    private int minBacklogEntriesForCaching = 1000;
    private int maxBacklogBetweenCursorsForCaching = 1000;

```

### UnusedAssignment
Variable `instanceIdOffset` initializer `0` is redundant
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
    protected String tlsTrustCertFilePath;
    @Parameter(names = "--instanceIdOffset", description = "Start the instanceIds from this offset", hidden = true)
    protected int instanceIdOffset = 0;
    @Parameter(names = "--runtime", description = "Function runtime to use (Thread/Process)", hidden = true,
            converter = RuntimeConverter.class)
```

### UnusedAssignment
Variable `isAuthorized` initializer `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/admin/WebSocketWebResource.java`
#### Snippet
```java
     */
    protected void validateUserAccess(TopicName topic) {
        boolean isAuthorized = false;

        try {
```

### UnusedAssignment
Variable `totalTxnOpSuccessNum` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
            double throughput = bytesReceived.sumThenReset() / elapsed * 8 / 1024 / 1024;
            double rateAck = messageAck.sumThenReset() / elapsed;
            long totalTxnOpSuccessNum = 0;
            long totalTxnOpFailNum = 0;
            double rateOpenTxn = 0;
```

### UnusedAssignment
Variable `totalTxnOpFailNum` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
            double rateAck = messageAck.sumThenReset() / elapsed;
            long totalTxnOpSuccessNum = 0;
            long totalTxnOpFailNum = 0;
            double rateOpenTxn = 0;
            reportHistogram = recorder.getIntervalHistogram(reportHistogram);
```

### UnusedAssignment
Variable `rateOpenTxn` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
            long totalTxnOpSuccessNum = 0;
            long totalTxnOpFailNum = 0;
            double rateOpenTxn = 0;
            reportHistogram = recorder.getIntervalHistogram(reportHistogram);

```

### UnusedAssignment
Variable `totalEndTxnSuccess` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        double rate = totalMessagesReceived.sum() / elapsed;
        double throughput = totalBytesReceived.sum() / elapsed * 8 / 1024 / 1024;
        long totalEndTxnSuccess = 0;
        long totalEndTxnFail = 0;
        long numTransactionOpenFailed = 0;
```

### UnusedAssignment
Variable `totalEndTxnFail` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        double throughput = totalBytesReceived.sum() / elapsed * 8 / 1024 / 1024;
        long totalEndTxnSuccess = 0;
        long totalEndTxnFail = 0;
        long numTransactionOpenFailed = 0;
        long numTransactionOpenSuccess = 0;
```

### UnusedAssignment
Variable `numTransactionOpenFailed` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        long totalEndTxnSuccess = 0;
        long totalEndTxnFail = 0;
        long numTransactionOpenFailed = 0;
        long numTransactionOpenSuccess = 0;
        long totalnumMessageAckFailed = 0;
```

### UnusedAssignment
Variable `numTransactionOpenSuccess` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        long totalEndTxnFail = 0;
        long numTransactionOpenFailed = 0;
        long numTransactionOpenSuccess = 0;
        long totalnumMessageAckFailed = 0;
        double rateAck = totalMessageAck.sum() / elapsed;
```

### UnusedAssignment
Variable `rateOpenTxn` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        long totalnumMessageAckFailed = 0;
        double rateAck = totalMessageAck.sum() / elapsed;
        double rateOpenTxn = 0;
        if (arguments.isEnableTransaction) {
            totalEndTxnSuccess = totalEndTxnOpSuccessNum.sum();
```

### UnusedAssignment
Variable `output` initializer `null` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
            expiredTuples, Long referenceTime) {

        X output = null;
        try {
            output = this.process(
```

### UnusedAssignment
Variable `totalTxnSuccess` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        double rate = totalMessagesSent.sum() / elapsed;
        double throughput = totalBytesSent.sum() / elapsed / 1024 / 1024 * 8;
        long totalTxnSuccess = 0;
        long totalTxnFail = 0;
        double rateOpenTxn = 0;
```

### UnusedAssignment
Variable `totalTxnFail` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        double throughput = totalBytesSent.sum() / elapsed / 1024 / 1024 * 8;
        long totalTxnSuccess = 0;
        long totalTxnFail = 0;
        double rateOpenTxn = 0;
        long numTransactionOpenFailed = 0;
```

### UnusedAssignment
Variable `rateOpenTxn` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        long totalTxnSuccess = 0;
        long totalTxnFail = 0;
        double rateOpenTxn = 0;
        long numTransactionOpenFailed = 0;
        long numTransactionOpenSuccess = 0;
```

### UnusedAssignment
Variable `numTransactionOpenFailed` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        long totalTxnFail = 0;
        double rateOpenTxn = 0;
        long numTransactionOpenFailed = 0;
        long numTransactionOpenSuccess = 0;

```

### UnusedAssignment
Variable `numTransactionOpenSuccess` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        double rateOpenTxn = 0;
        long numTransactionOpenFailed = 0;
        long numTransactionOpenSuccess = 0;

        if (arguments.isEnableTransaction) {
```

### UnusedAssignment
Variable `totalTxnOpSuccess` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            double elapsed = (now - oldTime) / 1e9;
            long total = totalMessagesSent.sum();
            long totalTxnOpSuccess = 0;
            long totalTxnOpFail = 0;
            double rateOpenTxn = 0;
```

### UnusedAssignment
Variable `totalTxnOpFail` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            long total = totalMessagesSent.sum();
            long totalTxnOpSuccess = 0;
            long totalTxnOpFail = 0;
            double rateOpenTxn = 0;
            double rate = messagesSent.sumThenReset() / elapsed;
```

### UnusedAssignment
Variable `rateOpenTxn` initializer `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            long totalTxnOpSuccess = 0;
            long totalTxnOpFail = 0;
            double rateOpenTxn = 0;
            double rate = messagesSent.sumThenReset() / elapsed;
            double failureRate = messagesFailed.sumThenReset() / elapsed;
```

### UnusedAssignment
Variable `byteBuf` initializer `null` is redundant
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnLogBufferedWriter.java`
#### Snippet
```java
        if (dataLength >= batchedWriteMaxSize){
            trigFlushByLargeSingleData();
            ByteBuf byteBuf = null;
            try {
                byteBuf = dataSerializer.serialize(data);
```

### UnusedAssignment
Variable `sslCtx` initializer `null` is redundant
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/http/AsyncHttpConnector.java`
#### Snippet
```java
                        sslProvider = SslProvider.valueOf(conf.getSslProvider());
                    }
                    SslContext sslCtx = null;
                    if (authData.hasDataForTls()) {
                        sslCtx = authData.getTlsTrustStoreStream() == null
```

### UnusedAssignment
Variable `strategy` initializer `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
            String namespace = validateNamespace(params);

            SchemaAutoUpdateCompatibilityStrategy strategy = null;
            String strategyStr = strategyParam != null ? strategyParam.toUpperCase() : "";
            if (disabled) {
```

### UnusedAssignment
Variable `deleteMode` initializer `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                        + "disable-delete-while-inactive");
            }
            InactiveTopicDeleteMode deleteMode = null;
            try {
                deleteMode = InactiveTopicDeleteMode.valueOf(inactiveTopicDeleteMode);
```

### UnusedAssignment
Variable `deleteMode` initializer `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                        + "or disable-delete-while-inactive");
            }
            InactiveTopicDeleteMode deleteMode = null;
            try {
                deleteMode = InactiveTopicDeleteMode.valueOf(inactiveTopicDeleteMode);
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderBasic.java`
#### Snippet
```java
        }

        @Cleanup BufferedReader reader = null;
        try {
            byte[] bytes = readData(data);
```

### UnusedAssignment
Variable `secret` initializer `null` is redundant
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderSasl.java`
#### Snippet
```java
        }
        String saslJaasServerRoleTokenSignerSecretPath = config.getSaslJaasServerRoleTokenSignerSecretPath();
        byte[] secret = null;
        if (StringUtils.isNotBlank(saslJaasServerRoleTokenSignerSecretPath)) {
            secret = readSecretFromUrl(saslJaasServerRoleTokenSignerSecretPath);
```

### UnusedAssignment
Variable `publishThrottlingRateInMsg` initializer `-1` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/PublishRate.java`
#### Snippet
```java
public class PublishRate {

    public int publishThrottlingRateInMsg = -1;
    public long publishThrottlingRateInByte = -1;

```

### UnusedAssignment
Variable `publishThrottlingRateInByte` initializer `-1` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/PublishRate.java`
#### Snippet
```java

    public int publishThrottlingRateInMsg = -1;
    public long publishThrottlingRateInByte = -1;

    public PublishRate() {
```

### UnusedAssignment
Variable `entity` initializer `null` is redundant
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java

        final CompletableFuture<AnalyzeSubscriptionBacklogResult> future = new CompletableFuture<>();
        Entity entity = null;
        if (startPosition.isPresent()) {
            ResetCursorData resetCursorData = new ResetCursorData(startPosition.get());
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ConfigValidation.java`
#### Snippet
```java
    public static void validateConfig(Object config, Class annotationClass) {
        for (Field field : config.getClass().getDeclaredFields()) {
            Object value = null;
            field.setAccessible(true);
            try {
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ConfigValidation.java`
#### Snippet
```java
        for (Method method : validatorClass.getDeclaredMethods()) {

            Object value = null;
            try {
                value = (Object) method.invoke(v);
```

### UnusedAssignment
Variable `o` initializer `null` is redundant
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ConfigValidation.java`
#### Snippet
```java
                    Class<Validator> clazz = (Class<Validator>) validatorClass
                            .getMethod(ConfigValidationAnnotations.ValidatorParams.VALIDATOR_CLASS).invoke(v);
                    Validator o = null;
                    Map<String, Object> params = getParamsFromAnnotation(validatorClass, v);
                    //two constructor signatures used to initialize validators.
```

### UnusedAssignment
Variable `objectClass` initializer `null` is redundant
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ValidatorImpls.java`
#### Snippet
```java
            int count = 0;
            for (Class<?> classImplements : classesImplements) {
                Class<?> objectClass = null;
                try {
                    objectClass = loadClass(className);
```

### UnusedAssignment
Variable `read` initializer `0` is redundant
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/DLOutputStream.java`
#### Snippet
```java
            List<LogRecord> records = new ArrayList<>();
            try {
                int read = 0;
                while ((read = inputStream.read(readBuffer)) != -1) {
                    log.info("write something into the ledgers offset: {}, length: {}", offset, read);
```

### UnusedAssignment
Variable `dataKeyCipher` initializer `null` is redundant
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
        }

        Cipher dataKeyCipher = null;
        byte[] encryptedKey;

```

### UnusedAssignment
Variable `publicKey` initializer `null` is redundant
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java

        Reader keyReader = new StringReader(new String(keyBytes));
        PublicKey publicKey = null;
        try (PEMParser pemReader = new PEMParser(keyReader)) {
            Object pemObj = pemReader.readObject();
```

### UnusedAssignment
Variable `keyInfo` initializer `null` is redundant
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
            Object pemObj = pemReader.readObject();
            JcaPEMKeyConverter pemConverter = new JcaPEMKeyConverter();
            SubjectPublicKeyInfo keyInfo = null;
            X9ECParameters ecParam = null;

```

### UnusedAssignment
Variable `dataKeyCipher` initializer `null` is redundant
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java

        // Decrypt data key to decrypt messages
        Cipher dataKeyCipher = null;
        byte[] dataKeyValue = null;
        byte[] keyDigest = null;
```

### UnusedAssignment
Variable `dataKeyValue` initializer `null` is redundant
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
        // Decrypt data key to decrypt messages
        Cipher dataKeyCipher = null;
        byte[] dataKeyValue = null;
        byte[] keyDigest = null;

```

### UnusedAssignment
Variable `keyDigest` initializer `null` is redundant
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
        Cipher dataKeyCipher = null;
        byte[] dataKeyValue = null;
        byte[] keyDigest = null;

        try {
```

### UnusedAssignment
Variable `rand` initializer `null` is redundant
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
    private static final SecureRandom secureRandom;
    static {
        SecureRandom rand = null;
        try {
            rand = SecureRandom.getInstance("NativePRNGNonBlocking");
```

### UnusedAssignment
Variable `privateKey` initializer `null` is redundant
in `pulsar-client-auth-athenz/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationAthenz.java`
#### Snippet
```java

    private PrivateKey loadPrivateKey(String privateKeyURL) {
        PrivateKey privateKey = null;
        try {
            URLConnection urlConnection = new URL(privateKeyURL).openConnection();
```

## RuleId[ruleID=SocketResource]
### SocketResource
'ServerSocket' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
     */
    public void run() throws Exception {
        final ServerSocket serverSocket = new ServerSocket(port);

        while (true) {
```

### SocketResource
'Socket' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
            // has not been tested or considered and is not recommended.
            log.info("Listening for controller command...");
            final Socket socket = serverSocket.accept();
            log.info("Connected to {}", socket.getInetAddress().getHostName());
            executor.submit(() -> {
```

### SocketResource
'Socket' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
        log.info("Found {} clients:", clients.length);
        for (int i = 0; i < clients.length; ++i) {
            sockets[i] = new Socket(clients[i], clientPort);
            inputStreams[i] = new DataInputStream(sockets[i].getInputStream());
            outputStreams[i] = new DataOutputStream(sockets[i].getOutputStream());
```

## RuleId[ruleID=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
                    .castToString(conf.getProperty(ISOLATION_BOOKIE_GROUPS));
            if (!isolationGroupsString.isEmpty()) {
                for (String isolationGroup : isolationGroupsString.split(",")) {
                    primaryIsolationGroups.add(isolationGroup);
                }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
                    .castToString(conf.getProperty(SECONDARY_ISOLATION_BOOKIE_GROUPS));
            if (!secondaryIsolationGroupsString.isEmpty()) {
                for (String isolationGroup : secondaryIsolationGroupsString.split(",")) {
                    secondaryIsolationGroups.add(isolationGroup);
                }
```

## RuleId[ruleID=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java
        INSERT,
        UPSERT,
        UPDATE;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/extensions/ProxyExtensionsUtils.java`
#### Snippet
```java
                .narFile(new File(narPath))
                .extractionDirectory(narExtractionDirectory)
                .build();) {
            return getProxyExtensionDefinition(ncl);
        }
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SendMessageInfo.java`
#### Snippet
```java
        protected SendMessageInfo initialValue() throws Exception {
            return new SendMessageInfo();
        };
    };

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java

    enum State {
        Issued, Canceling, Canceled, Completed;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/OwnedBundle.java`
#### Snippet
```java
        this.bundle = suName;
        IS_ACTIVE_UPDATER.set(this, TRUE);
    };

    /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnxIdleState.java`
#### Snippet
```java
        RELEASING,
        /** The connection has already been released. **/
        RELEASED;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageIdImpl.java`
#### Snippet
```java
        protected MessageIdData initialValue() throws Exception {
            return new MessageIdData();
        };
    };

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/HandlerState.java`
#### Snippet
```java
        RegisteringSchema, // Handler is registering schema
        ProducerFenced, // The producer has been fenced by the broker
    };

    public HandlerState(PulsarClientImpl client, String topic) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AbstractSchema.java`
#### Snippet
```java
    void validate(ByteBuf byteBuf) {
        throw new SchemaSerializationException("This method is not supported");
    };

    /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-common/src/main/java/org/apache/pulsar/common/api/raw/MessageParser.java`
#### Snippet
```java
                protected SingleMessageMetadata initialValue() throws Exception {
                    return new SingleMessageMetadata();
                };
            };

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/FileUtils.java`
#### Snippet
```java

    public static boolean mayBeANarArchive(File jarFile) {
        try (ZipFile zipFile = new ZipFile(jarFile);) {
            ZipEntry entry = zipFile.getEntry("META-INF/bundled-dependencies");
            if (entry == null || !entry.isDirectory()) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-common/src/main/java/org/apache/pulsar/client/admin/internal/data/AuthPoliciesImpl.java`
#### Snippet
```java
    public static class AuthPoliciesImplBuilder implements AuthPolicies.Builder {
        private Map<String, Set<AuthAction>> namespaceAuthentication = new TreeMap<>();
        private Map<String, Map<String, Set<AuthAction>>> topicAuthentication = new TreeMap<>();;
        private Map<String, Set<String>> subscriptionAuthentication = new TreeMap<>();;

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-common/src/main/java/org/apache/pulsar/client/admin/internal/data/AuthPoliciesImpl.java`
#### Snippet
```java
        private Map<String, Set<AuthAction>> namespaceAuthentication = new TreeMap<>();
        private Map<String, Map<String, Set<AuthAction>>> topicAuthentication = new TreeMap<>();;
        private Map<String, Set<String>> subscriptionAuthentication = new TreeMap<>();;

        AuthPoliciesImplBuilder() {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
    public enum ChecksumType {
        Crc32c,
        None;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/WorkerServiceLoader.java`
#### Snippet
```java
                .narFile(new File(narPath))
                .extractionDirectory(narExtractionDirectory)
                .build();) {
            return getWorkerServiceDefinition(ncl);
        }
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
        }
        return this.initializedDlogMetadata;
    };

    /******** security settings for Pulsar broker client. **********/
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/ConsumerCryptoFailureAction.java`
#### Snippet
```java
     * encryption and compression information in it using which application can decrypt consumed message payload.
     */
    CONSUME;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnLogBufferedWriter.java`
#### Snippet
```java
        OPEN,
        CLOSING,
        CLOSED;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/ConfigStore.java`
#### Snippet
```java
        Set<String> keys = new HashSet<>();
        StringBuilder builder = new StringBuilder();
        try (Scanner scanner = new Scanner(entry.getValue());) {
            while (scanner.hasNextLine()) {
                String line = scanner.nextLine().trim();
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/ConfigStore.java`
#### Snippet
```java

    static String getProperty(ConfigEntry entry, String propertyName) {
        try (Scanner scanner = new Scanner(entry.getValue());) {
            while (scanner.hasNextLine()) {
                String line = scanner.nextLine().trim();
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/ConfigStore.java`
#### Snippet
```java
    static void cleanupValue(ConfigEntry entry) {
        StringBuilder builder = new StringBuilder();
        try (Scanner scanner = new Scanner(entry.getValue());) {
            while (scanner.hasNextLine()) {
                String line = scanner.nextLine().trim();
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/FileConfigStore.java`
#### Snippet
```java

    private void write() throws IOException {
        try (final BufferedOutputStream bufferedOutputStream = new BufferedOutputStream(new FileOutputStream(file));) {
            mapper.writeValue(bufferedOutputStream, fileConfig);
        }
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/FileConfigStore.java`
#### Snippet
```java

    private void read() throws IOException {
        try (final BufferedInputStream buffered = new BufferedInputStream(new FileInputStream(file));) {
            try {
                fileConfig = mapper.readValue(buffered, FileConfig.class);
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/JCommanderCompleter.java`
#### Snippet
```java
        enum Type {
            FILES,
            CONFIGS;
        }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
            }

            try (Consumer<?> consumer = builder.subscribe();) {
                RateLimiter limiter = (this.consumeRate > 0) ? RateLimiter.create(this.consumeRate) : null;
                while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
                producerBuilder.defaultCryptoKeyReader(this.encKeyValue);
            }
            try (Producer<?> producer = producerBuilder.create();) {

                List<byte[]> messageBodies = generateMessageBodies(this.messages, this.messageFileNames);
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServletUtils.java`
#### Snippet
```java
                .narFile(new File(narPath))
                .extractionDirectory(narExtractionDirectory)
                .build();) {
            return getAdditionalServletDefinition(ncl);
        }
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/limiter/ConnectionController.java`
#### Snippet
```java

    enum State {
        OK, REACH_MAX_CONNECTION_PER_IP, REACH_MAX_CONNECTION;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
            } else {
                lines = new ArrayList<>();
                try (Scanner scanner = new Scanner(mainOptions.inlineCommand);) {
                    while (scanner.hasNextLine()) {
                        String line = scanner.nextLine().trim();
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationDataSource.java`
#### Snippet
```java
     * Subscription name can be necessary for consumption.
     */
    default void setSubscription(String subscription) { };
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/LongRunningProcessStatus.java`
#### Snippet
```java
        SUCCESS,
        ERROR
    };

    public Status status;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/stats/PositionInPendingAckStats.java`
#### Snippet
```java
        PendingAckNotReady,
        InvalidPosition
    };

    public PositionInPendingAckStats() {}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    public enum BundleType {
        LARGEST, HOT;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Topics.java`
#### Snippet
```java
            throws PulsarAdminException {
        createSubscription(topic, subscriptionName, messageId, false);
    };

    /**
```

## RuleId[ruleID=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
'collect(toList())' can be replaced with 'toList()'
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java
                        .stream()
                        .map(this::createMutation)
                        .collect(Collectors.toList());
                // bind each record value
                PreparedStatement statement;
```

### SimplifyStreamApiCallChains
'Arrays.asList().stream()' can be replaced with 'Arrays.stream()'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/opensearch/OpenSearchHighLevelRestClient.java`
#### Snippet
```java
                                convertBulkResponse(BulkResponse bulkRequest) {
                                    return Arrays.asList(bulkRequest.getItems())
                                            .stream()
                                            .map(itemResponse ->
                                                    BulkProcessor.BulkOperationResult.builder()
```

### SimplifyStreamApiCallChains
'collect(toList())' can be replaced with 'toList()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
        List<String> matchedTopics = topicsSetWithoutPartition.stream()
                .filter(t -> TopicName.get(t).getLocalName().equalsIgnoreCase(schemaTableName.getTableName()))
                .collect(Collectors.toList());

        if (matchedTopics.size() == 0) {
```

### SimplifyStreamApiCallChains
'collect(toList())' can be replaced with 'toList()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
            for (String tenant : tenants) {
                prestoSchemas.addAll(pulsarAdmin.namespaces().getNamespaces(tenant).stream().map(namespace ->
                    rewriteNamespaceDelimiterIfNeeded(namespace, pulsarConnectorConfig)).collect(Collectors.toList()));
            }
        } catch (PulsarAdminException e) {
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
            PulsarInternalColumn.getInternalFields()
                    .stream()
                    .forEach(pulsarInternalColumn -> builder.add(pulsarInternalColumn.getColumnMetadata(false)));
        }
        return builder.build();
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/WebServer.java`
#### Snippet
```java

        // Limit number of concurrent HTTP connections to avoid getting out of file descriptors
        connectors.stream().forEach(c -> c.setAcceptQueueSize(config.getHttpServerAcceptQueueSize()));
        server.setConnectors(connectors.toArray(new ServerConnector[connectors.size()]));

```

### SimplifyStreamApiCallChains
'collect(toList())' can be replaced with 'toList()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/NonPersistentTopics.java`
#### Snippet
```java
                        topicList.addAll(topicMap.keys().stream()
                                .filter(name -> !TopicName.get(name).isPersistent())
                                .collect(Collectors.toList()));
                    }
                    asyncResponse.resume(topicList);
```

### SimplifyStreamApiCallChains
'collect(toList())' can be replaced with 'toList()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                                        info.underReplicated =
                                            !bookies.containsAll(ensembles.stream().map(BookieId::toString)
                                                .collect(Collectors.toList()));
                                    }
                                    return null;
```

### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
            LOG.warn("Broker {} ({}) couldn't be found in available brokers {}",
                    candidateBroker, candidateBrokerHostAndPort,
                    availableBrokers.stream().collect(Collectors.joining(",")));
            return false;
        }
```

### SimplifyStreamApiCallChains
'filter()' and 'map()' can be swapped
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                    serviceUnit.includes(SystemTopicNames.TRANSACTION_COORDINATOR_ASSIGN
                            .getPartition((int) (store.getTransactionCoordinatorID().getId()))))
                    .map(TransactionMetadataStore::getTransactionCoordinatorID)
                    .forEach(tcId -> closeFutures.add(metadataStoreService.removeTransactionMetadataStore(tcId)));
        }
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            if (!isEmpty(topics)) {
                // add topic under new split bundles which already updated into NamespaceBundleFactory.bundleCache
                topics.stream().forEach(t -> {
                    addTopicToStatsMaps(TopicName.get(t.getName()), t);
                });
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBuilderImpl.java`
#### Snippet
```java
        checkArgument(topicNames != null && !topicNames.isEmpty(),
                "Passed in topicNames list should not be null or empty.");
        topicNames.stream().forEach(topicName ->
                checkArgument(StringUtils.isNotBlank(topicName), "topicNames cannot have blank topic"));
        conf.getTopicNames().addAll(topicNames.stream().map(StringUtils::trim).collect(Collectors.toList()));
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PatternMultiTopicsConsumerImpl.java`
#### Snippet
```java

            List<CompletableFuture<Void>> futures = Lists.newArrayListWithExpectedSize(partitionedTopics.size());
            removedTopics.stream().forEach(topic -> futures.add(removeConsumerAsync(topic)));
            FutureUtil.waitForAll(futures)
                .thenAccept(finalFuture -> removeFuture.complete(null))
```

### SimplifyStreamApiCallChains
'filter()' and 'map()' can be swapped
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java
        String[] unResolvedFileDescriptNames = fileDescriptor.getDependencies().stream().
                filter(item -> !fileDescriptorCache.containsKey(item.getFullName()))
                .map(Descriptors.FileDescriptor::getFullName).toArray(String[]::new);
        if (unResolvedFileDescriptNames.length == 0) {
            fileDescriptorCache.put(fileDescriptor.getFullName(), fileDescriptor.toProto());
```

### SimplifyStreamApiCallChains
Can be replaced with 'List.contains()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/SSLContextValidatorEngine.java`
#### Snippet
```java
            throws SSLException {
        SSLContextValidatorEngine clientEngine = new SSLContextValidatorEngine(clientSslEngineSupplier);
        if (Arrays.stream(clientEngine.sslEngine.getEnabledProtocols()).anyMatch(s -> s.equals("TLSv1.3"))) {
            throw new IllegalStateException("This validator doesn't support TLSv1.3");
        }
```

### SimplifyStreamApiCallChains
'collect(toList())' can be replaced with 'toList()'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/LedgerOffloaderStatsImpl.java`
#### Snippet
```java
        String namespace = this.getNamespace(topic);
        List<String> topics = this.offloadAndReadOffloadBytesMap.keySet().stream()
                .filter(topicName -> topicName.contains(namespace)).collect(Collectors.toList());

        long totalBytes = 0;
```

### SimplifyStreamApiCallChains
'collect(toList())' can be replaced with 'toList()'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/LedgerOffloaderStatsImpl.java`
#### Snippet
```java
        String namespace = this.getNamespace(topic);
        List<String> topics = this.offloadAndReadOffloadBytesMap.keySet().stream()
                .filter(topicName -> topicName.contains(namespace)).collect(Collectors.toList());

        long totalBytes = 0;
```

### SimplifyStreamApiCallChains
Can be replaced with 'java.util.ArrayList' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    // get topics name
    public List<String> getPartitionedTopics() {
        return partitionedTopics.keySet().stream().collect(Collectors.toList());
    }

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
            try {
                CONSUMER_EPOCH.incrementAndGet(this);
                consumers.values().stream().forEach(consumer -> {
                    consumer.redeliverUnacknowledgedMessages();
                    consumer.unAckedChunkedMessageIdSequenceMap.clear();
```

### SimplifyStreamApiCallChains
Can be replaced with 'java.util.ArrayList' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    // get partitioned consumers
    public List<ConsumerImpl<T>> getConsumers() {
        return consumers.values().stream().collect(Collectors.toList());
    }

```

### SimplifyStreamApiCallChains
Can be replaced with 'java.util.ArrayList' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    // get partitioned topics name
    public List<String> getPartitions() {
        return consumers.keySet().stream().collect(Collectors.toList());
    }

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
                .setType(getSchemaType(schemaInfo.getType()));

        schemaInfo.getProperties().entrySet().stream().forEach(entry -> {
            if (entry.getKey() != null && entry.getValue() != null) {
                schema.addProperty()
```

### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/AsyncContextFunction.java`
#### Snippet
```java
            }

            String inputTopics = context.getInputTopics().stream().collect(Collectors.joining(", "));
            String funcName = context.getFunctionName();

```

### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/ContextFunction.java`
#### Snippet
```java
    public Void process(String input, Context context) {
        Logger log = context.getLogger();
        String inputTopics = context.getInputTopics().stream().collect(Collectors.joining(", "));
        String functionName = context.getFunctionName();

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/ProxyServer.java`
#### Snippet
```java
        // Limit number of concurrent HTTP connections to avoid getting out of
        // file descriptors
        connectors.stream().forEach(c -> c.setAcceptQueueSize(config.getHttpServerAcceptQueueSize()));
        server.setConnectors(connectors.toArray(new ServerConnector[connectors.size()]));

```

### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
            }
            execState = ExecState.RUNNING;
            final String line = words.stream().collect(Collectors.joining(" "));
            if (StringUtils.isBlank(line)) {
                continue;
```

### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
                    String command = lines.get(index++).trim();
                    final List<String> words = substituteVariables(reader.parseLine(command), variables);
                    command = words.stream().collect(Collectors.joining(" "));
                    if (finalCommandsInfo != null) {
                        finalCommandsInfo.executingCommand = command;
```

## RuleId[ruleID=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/AbstractSource.java`
#### Snippet
```java
                        out.flush();
                        byte[] m = bos.toByteArray();
                        String m1 = new String(m);
                        bos.close();
                        FlumeRecord flumeRecord = new FlumeRecord<>();
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java

        System.getenv().entrySet().stream()
            .filter(p -> p.getKey().toString().startsWith("jclouds"))
            .forEach(jcloudsProp -> {
                overrides.setProperty(jcloudsProp.getKey().toString(), jcloudsProp.getValue().toString());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
            .filter(p -> p.getKey().toString().startsWith("jclouds"))
            .forEach(jcloudsProp -> {
                overrides.setProperty(jcloudsProp.getKey().toString(), jcloudsProp.getValue().toString());
            });

```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
            .filter(p -> p.getKey().toString().startsWith("jclouds"))
            .forEach(jcloudsProp -> {
                overrides.setProperty(jcloudsProp.getKey().toString(), jcloudsProp.getValue().toString());
            });

```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/BaseResource.java`
#### Snippet
```java
    public static String getReasonFromServer(WebApplicationException e) {
        try {
            return e.getResponse().readEntity(ErrorData.class).reason.toString();
        } catch (Exception ex) {
            try {
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
                    return false;
                }
                value = new String(bout.toByteArray(), StandardCharsets.UTF_8);
            } else {
                print("At least one between --file, --url or --value is required.");
```

## RuleId[ruleID=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
    private static final Optional<Long> RECORD_SEQUENCE = Optional.empty();

    public abstract class AbstractKafkaSourceRecord<T> implements Record {
        @Getter
        Optional<String> key;
```

## RuleId[ruleID=MethodOverloadsParentMethod]
### MethodOverloadsParentMethod
Method `add()` overloads a compatible method of a superclass, when overriding might have been intended
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/NonPersistentPublisherStatsImpl.java`
#### Snippet
```java
    public double msgDropRate;

    public NonPersistentPublisherStatsImpl add(NonPersistentPublisherStatsImpl stats) {
        Objects.requireNonNull(stats);
        super.add(stats);
```

### MethodOverloadsParentMethod
Method `add()` overloads a compatible method of a superclass, when overriding might have been intended
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/NonPersistentReplicatorStatsImpl.java`
#### Snippet
```java
    public double msgDropRate;

    public NonPersistentReplicatorStatsImpl add(NonPersistentReplicatorStatsImpl stats) {
        Objects.requireNonNull(stats);
        super.add(stats);
```

### MethodOverloadsParentMethod
Method `add()` overloads a compatible method of a superclass, when overriding might have been intended
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/NonPersistentSubscriptionStatsImpl.java`
#### Snippet
```java
    // if the stats are added for the 1st time, we will need to make a copy of these stats and add it to the current
    // stats
    public NonPersistentSubscriptionStatsImpl add(NonPersistentSubscriptionStatsImpl stats) {
        Objects.requireNonNull(stats);
        super.add(stats);
```

### MethodOverloadsParentMethod
Method `addPublisher()` overloads a compatible method of a superclass, when overriding might have been intended
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/NonPersistentTopicStatsImpl.java`
#### Snippet
```java
    }

    public void addPublisher(NonPersistentPublisherStatsImpl stats) {
        if (stats.isSupportsPartialProducer() && stats.getProducerName() != null) {
            nonPersistentPublishersMap.put(stats.getProducerName(), stats);
```

### MethodOverloadsParentMethod
Method `add()` overloads a compatible method of a superclass, when overriding might have been intended
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/NonPersistentTopicStatsImpl.java`
#### Snippet
```java
    // if the stats are added for the 1st time, we will need to make a copy of these stats and add it to the current
    // stats.
    public NonPersistentTopicStatsImpl add(NonPersistentTopicStats ts) {
        NonPersistentTopicStatsImpl stats = (NonPersistentTopicStatsImpl) ts;
        Objects.requireNonNull(stats);
```

## RuleId[ruleID=OptionalContainsCollection]
### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRowDecoder.java`
#### Snippet
```java
     * @return
     */
    Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodeRow(ByteBuf byteBuf);

}
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroRowDecoder.java`
#### Snippet
```java
     */
    @Override
    public Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodeRow(ByteBuf byteBuf) {
        GenericRecord avroRecord;
        try {
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonRowDecoder.java`
#### Snippet
```java
     */
    @Override
    public Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodeRow(ByteBuf byteBuf) {
        GenericJsonRecord record = (GenericJsonRecord) genericJsonSchema.decode(byteBuf);
        JsonNode tree = record.getJsonNode();
```

### OptionalContainsCollection
'Optional' contains collection `Set`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
    public List<ConnectorTableLayoutResult> getTableLayouts(ConnectorSession session, ConnectorTableHandle table,
                                                            Constraint constraint,
                                                            Optional<Set<ColumnHandle>> desiredColumns) {

        PulsarTableHandle handle = convertTableHandle(table);
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/primitive/PulsarPrimitiveRowDecoder.java`
#### Snippet
```java

    @Override
    public Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodeRow(ByteBuf byteBuf) {
        if (columnHandle == null) {
            return Optional.empty();
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeRowDecoder.java`
#### Snippet
```java
     */
    @Override
    public Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodeRow(ByteBuf byteBuf) {
        DynamicMessage dynamicMessage;
        try {
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            }

            Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodedKey;
            if (keyColumnHandles.size() > 0) {
                decodedKey = keyDecoder.decodeRow(keyByteBuf);
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            }
            if (valueColumnHandles.size() > 0) {
                Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodedValue =
                        valueDecoder.decodeRow(valueByteBuf);
                decodedValue.ifPresent(currentRowValuesMap::putAll);
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
                                    .equals(col.getHandleKeyValueType()))
                            .collect(toImmutableSet()));
            Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodedValue =
                    messageDecoder.decodeRow(this.currentMessage.getData());
            decodedValue.ifPresent(currentRowValuesMap::putAll);
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SubscriptionOption.java`
#### Snippet
```java
    private boolean replicatedSubscriptionStateArg;
    private KeySharedMeta keySharedMeta;
    private Optional<Map<String, String>> subscriptionProperties;
    private long consumerEpoch;

```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SubscriptionOption.java`
#### Snippet
```java
    private long consumerEpoch;

    public static Optional<Map<String, String>> getPropertiesMap(List<KeyValue> list) {
        if (list == null) {
            return Optional.of(Collections.emptyMap());
```

### OptionalContainsCollection
'Optional' contains array `MessageMetadata[]`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java
     *   EntryBatchIndexesAcks, ManagedCursor, boolean, Consumer)
     */
    public int filterEntriesForConsumer(Optional<MessageMetadata[]> optMetadataArray, int startOffset,
             List<? extends Entry> entries, EntryBatchSizes batchSizes, SendMessageInfo sendMessageInfo,
             EntryBatchIndexesAcks indexesAcks, ManagedCursor cursor, boolean isReplayRead, Consumer consumer) {
```

### OptionalContainsCollection
'Optional' contains collection `List`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
    }

    protected CompletableFuture<Optional<List<SubType>>> internalGetSubscriptionTypesEnabled(boolean isGlobal) {
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenApply(op -> op.map(TopicPolicies::getSubscriptionTypesEnabled));
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    consumerEpoch = DEFAULT_CONSUMER_EPOCH;
                }
                Optional<Map<String, String>> subscriptionProperties = SubscriptionOption.getPropertiesMap(
                        subscribe.getSubscriptionPropertiesList());
                service.isAllowAutoTopicCreationAsync(topicName.toString())
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
     */
    private void updateDynamicServiceConfiguration() {
        Optional<Map<String, String>> configCache = Optional.empty();

        try {
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    private void createDynamicConfigPathIfNotExist() {
        try {
            Optional<Map<String, String>> configCache =
                    pulsar().getPulsarResources().getDynamicConfigResources().getDynamicConfiguration();

```

### OptionalContainsCollection
'Optional' contains array `byte[]`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private final Map<String, String> metadata;

    private Optional<byte[]> schemaVersion = Optional.empty();

    private final ConnectionHandler connectionHandler;
```

### OptionalContainsCollection
'Optional' contains collection `BookiesRackConfiguration`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
        try {
            if (bookieMappingCache != null) {
                CompletableFuture<Optional<BookiesRackConfiguration>> future =
                        bookieMappingCache.get(BookieRackAffinityMapping.BOOKIE_INFO_ROOT_PATH);

```

### OptionalContainsCollection
'Optional' contains collection `BookiesRackConfiguration`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
                        bookieMappingCache.get(BookieRackAffinityMapping.BOOKIE_INFO_ROOT_PATH);

                Optional<BookiesRackConfiguration> optRes = (future.isDone() && !future.isCompletedExceptionally())
                        ? future.join() : Optional.empty();

```

### OptionalContainsCollection
'Optional' contains collection `BookiesRackConfiguration`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BookieResources.java`
#### Snippet
```java
    }

    public CompletableFuture<Optional<BookiesRackConfiguration>> get() {
        return getAsync(BookieRackAffinityMapping.BOOKIE_INFO_ROOT_PATH);
    }
```

### OptionalContainsCollection
'Optional' contains collection `BookiesRackConfiguration`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BookieResources.java`
#### Snippet
```java
    }

    public CompletableFuture<Void> update(Function<Optional<BookiesRackConfiguration>,
            BookiesRackConfiguration> modifyFunction) {
        return getCache().readModifyUpdateOrCreate(BookieRackAffinityMapping.BOOKIE_INFO_ROOT_PATH,
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java

    public CompletableFuture<Void> setDynamicConfigurationWithCreateAsync(
            Function<Optional<Map<String, String>>, Map<String, String>> createFunction) {
        return super.setWithCreateAsync(BROKER_SERVICE_CONFIGURATION_PATH, createFunction);
    }
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java

    public void setDynamicConfigurationWithCreate(
                                 Function<Optional<Map<String, String>>, Map<String, String>> createFunction)
            throws MetadataStoreException {
        super.setWithCreate(BROKER_SERVICE_CONFIGURATION_PATH, createFunction);
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java
    }

    public Optional<Map<String, String>> getDynamicConfiguration() throws MetadataStoreException {
        return get(BROKER_SERVICE_CONFIGURATION_PATH);
    }
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java
    }

    public CompletableFuture<Optional<Map<String, String>>> getDynamicConfigurationAsync() {
        return getAsync(BROKER_SERVICE_CONFIGURATION_PATH);
    }
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
        public Optional<NamespaceIsolationPolicies> getIsolationDataPolicies(String cluster)
                throws MetadataStoreException {
            Optional<Map<String, NamespaceIsolationDataImpl>> data =
                    super.get(joinPath(BASE_CLUSTERS_PATH, cluster, NAMESPACE_ISOLATION_POLICIES));
            return data.isPresent() ? Optional.of(new NamespaceIsolationPolicies(data.get())) : Optional.empty();
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java

        public void setIsolationDataWithCreate(String cluster,
                                     Function<Optional<Map<String, NamespaceIsolationDataImpl>>, Map<String,
                                             NamespaceIsolationDataImpl>> createFunction)
                throws MetadataStoreException {
```

### OptionalContainsCollection
'Optional' contains collection `Map`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java

        public CompletableFuture<Void> setIsolationDataWithCreateAsync(String cluster,
                                                                       Function<Optional<Map<String,
                                                                       NamespaceIsolationDataImpl>>,
                                                                       Map<String, NamespaceIsolationDataImpl>>
                                                                               createFunction) {
```

## RuleId[ruleID=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/UnrecognizedPropertyExceptionMapper.java`
#### Snippet
```java
    public Response toResponse(UnrecognizedPropertyException exception) {
        String response = String.format("Unknown property %s, perhaps you want to use one of these: %s",
                exception.getPropertyName(), String.valueOf(exception.getKnownPropertyIds()));
        return Response.status(Response.Status.BAD_REQUEST).entity(response).type(MediaType.TEXT_PLAIN).build();
    }
```

## RuleId[ruleID=ObviousNullCheck]
### ObviousNullCheck
Redundant null-check: a value of primitive type 'int' is never null
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
    public void validate() {
        Preconditions.checkNotNull(redisHosts, "redisHosts property not set.");
        Preconditions.checkNotNull(redisDatabase, "redisDatabase property not set.");
        Preconditions.checkNotNull(clientMode, "clientMode property not set.");
    }
```

### ObviousNullCheck
Redundant null-check: a value of primitive type 'int' is never null
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java
    public void validate() {
        Preconditions.checkNotNull(host, "host property not set.");
        Preconditions.checkNotNull(port, "port property not set.");
        Preconditions.checkNotNull(virtualHost, "virtualHost property not set.");
        Preconditions.checkNotNull(connectionName, "connectionName property not set.");
```

## RuleId[ruleID=ShiftOutOfRange]
### ShiftOutOfRange
Shift operation `>>>` by out-of-bounds value {Integer.MIN_VALUE..-1}
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
            return length() - 1;

        long word = words[u] & (WORD_MASK >>> -(fromIndex+1));

        while (true) {
```

### ShiftOutOfRange
Shift operation `>>>` by out-of-bounds value {Integer.MIN_VALUE..-1}
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
            return fromIndex;

        long word = ~words[u] & (WORD_MASK >>> -(fromIndex+1));

        while (true) {
```

## RuleId[ruleID=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `register()` overrides synchronized method
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeperSession.java`
#### Snippet
```java

    @Override
    public void register(Watcher watcher) {
        mockZooKeeper.register(watcher);
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `close()` overrides synchronized method
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeperSession.java`
#### Snippet
```java

    @Override
    public void close() throws InterruptedException {
        mockZooKeeper.close();
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `getLength()` overrides synchronized method
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java

    @Override
    public long getLength() {
        long length = 0;
        for (LedgerEntryImpl entry : entries) {
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `isClosed()` overrides synchronized method
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java

    @Override
    public boolean isClosed() {
        return readHandle.isClosed();
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `getLastAddConfirmed()` overrides synchronized method
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java

    @Override
    public long getLastAddConfirmed() {
        if (bk.checkReturnEmptyLedger()) {
            return -1;
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `close()` overrides synchronized method
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java

    @Override
    public void close() throws InterruptedException {
        shutdown();
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `register()` overrides synchronized method
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java

    @Override
    public void register(Watcher watcher) {
        lock();
        sessionWatcher = watcher;
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `start()` overrides synchronized method
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java

        @Override
        public void start() {
            if (isRunning.compareAndSet(false, true)) {
                super.start();
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `getCause()` overrides synchronized method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/RestException.java`
#### Snippet
```java

    @Override
    public Throwable getCause() {
        return cause;
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `getOutstandingAcks()` overrides synchronized method
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageAckerDisabled.java`
#### Snippet
```java

    @Override
    public int getOutstandingAcks() {
        return 0;
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `ackCumulative()` overrides synchronized method
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageAckerDisabled.java`
#### Snippet
```java

    @Override
    public boolean ackCumulative(int batchIndex) {
        return true;
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `ackIndividual()` overrides synchronized method
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageAckerDisabled.java`
#### Snippet
```java

    @Override
    public boolean ackIndividual(int batchIndex) {
        return true;
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `close()` overrides synchronized method
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java

    @Override
    public void close() throws InterruptedException {
        closed.set(true);
        connectExecutor.shutdown();
```

## RuleId[ruleID=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/stats/MetadataStoreStats.java`
#### Snippet
```java
    private static final String STATUS_FAIL = "fail";

    protected static final String PREFIX = "pulsar_metadata_store_";

    private static final Histogram OPS_LATENCY = Histogram
```

## RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `msgBytes` declared in one 'switch' branch and used in another
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ParserProxyHandler.java`
#### Snippet
```java
                    topicName = TopicName.get(ParserProxyHandler.producerHashMap.get(cmd.getSend().getProducerId() + ","
                            + ctx.channel().id()));
                    MutableLong msgBytes = new MutableLong(0);
                    MessageParser.parseMessage(topicName, -1L,
                            -1L, buffer, (message) -> {
```

### SwitchStatementWithConfusingDeclaration
Local variable `topicStats` declared in one 'switch' branch and used in another
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ParserProxyHandler.java`
#### Snippet
```java
                            }, maxMessageSize);
                    // update topic stats
                    TopicStats topicStats = this.service.getTopicStats().computeIfAbsent(topicName.toString(),
                        topic -> new TopicStats());
                    topicStats.getMsgInRate().recordMultipleEvents(messages.size(), msgBytes.longValue());
```

## RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `records` accessed in synchronized context
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/SinkOfFlume.java`
#### Snippet
```java
    @Override
    public synchronized void start() {
        records = new LinkedBlockingQueue<Map<String, Object>>();
        this.counter = new SinkCounter("flume-sink");
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectorConfig` accessed in synchronized context
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConnector.java`
#### Snippet
```java
            if (SecurityUtil.isSecurityEnabled(config)) {
                ugi = SecurityUtil.loginKerberos(config,
                        connectorConfig.getKerberosUserPrincipal(), connectorConfig.getKeytab());
                fs = getFileSystemAsUser(config, ugi);
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectorConfig` accessed in synchronized context
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConnector.java`
#### Snippet
```java
            if (SecurityUtil.isSecurityEnabled(config)) {
                ugi = SecurityUtil.loginKerberos(config,
                        connectorConfig.getKerberosUserPrincipal(), connectorConfig.getKeytab());
                fs = getFileSystemAsUser(config, ugi);
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectorConfig` accessed in synchronized context
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConnector.java`
#### Snippet
```java
            if (SecurityUtil.isSecurityEnabled(config)) {
                ugi = SecurityUtil.loginKerberos(config,
                        connectorConfig.getKerberosUserPrincipal(), connectorConfig.getKeytab());
                fs = getFileSystemAsUser(config, ugi);
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectorConfig` accessed in synchronized context
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConnector.java`
#### Snippet
```java
            if (SecurityUtil.isSecurityEnabled(config)) {
                ugi = SecurityUtil.loginKerberos(config,
                        connectorConfig.getKerberosUserPrincipal(), connectorConfig.getKeytab());
                fs = getFileSystemAsUser(config, ugi);
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `offsetWriter` accessed in synchronized context
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSource.java`
#### Snippet
```java
    public synchronized KafkaSourceRecord processSourceRecord(final SourceRecord srcRecord) {
        KafkaSourceRecord record = new KafkaSourceRecord(srcRecord);
        offsetWriter.offset(srcRecord.sourcePartition(), srcRecord.sourceOffset());
        return record;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultResponse` accessed in synchronized context
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
        }
        synchronized (this) {
            defaultResponse = FutureUtils.exception(new BKException.BKClientClosedException());
        }
        for (PulsarMockLedgerHandle ledger : ledgers.values()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultResponse` accessed in synchronized context
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
    public synchronized CompletableFuture<Void> promiseAfter(int steps) {
        while (failures.size() <= steps) {
            failures.add(defaultResponse);
        }
        CompletableFuture<Void> promise = new CompletableFuture<>();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultResponse` accessed in synchronized context
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java

    synchronized CompletableFuture<Void> getProgrammedFailure() {
        return failures.isEmpty() ? defaultResponse : failures.remove(0);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java
    public static void shutdown() throws Exception {
        synchronized (PulsarConnectorCache.class) {
            if (instance != null) {
                instance.statsProvider.stop();
                instance.managedLedgerFactory.shutdown();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java
        synchronized (PulsarConnectorCache.class) {
            if (instance != null) {
                instance.statsProvider.stop();
                instance.managedLedgerFactory.shutdown();
                instance.metadataStore.close();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java
            if (instance != null) {
                instance.statsProvider.stop();
                instance.managedLedgerFactory.shutdown();
                instance.metadataStore.close();
                instance.offloaderScheduler.shutdown();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java
                instance.statsProvider.stop();
                instance.managedLedgerFactory.shutdown();
                instance.metadataStore.close();
                instance.offloaderScheduler.shutdown();
                instance.offloadersCache.close();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java
                instance.managedLedgerFactory.shutdown();
                instance.metadataStore.close();
                instance.offloaderScheduler.shutdown();
                instance.offloadersCache.close();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java
                instance.metadataStore.close();
                instance.offloaderScheduler.shutdown();
                instance.offloadersCache.close();
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java
    public static PulsarConnectorCache getConnectorCache(PulsarConnectorConfig pulsarConnectorConfig) throws Exception {
        synchronized (PulsarConnectorCache.class) {
            if (instance == null) {
                instance = new PulsarConnectorCache(pulsarConnectorConfig);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorCache.java`
#### Snippet
```java
        synchronized (PulsarConnectorCache.class) {
            if (instance == null) {
                instance = new PulsarConnectorCache(pulsarConnectorConfig);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.clientAuthData` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
        if (service.getConfiguration().isAuthenticationEnabled()) {
            if (service.getConfiguration().isForwardAuthorizationCredentials()) {
                this.clientAuthData = clientData;
                this.clientAuthMethod = authMethod;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.clientAuthMethod` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
            if (service.getConfiguration().isForwardAuthorizationCredentials()) {
                this.clientAuthData = clientData;
                this.clientAuthMethod = authMethod;
            }
            clientCnxSupplier = () -> new ProxyClientCnx(clientConf, service.getWorkerGroup(), clientAuthRole,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remoteAddress` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
        } else {
            LOG.error("BUG! Connection Pool has already been created for proxy connection to {} state {} role {}",
                    remoteAddress, state, clientAuthRole);
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `clientAuthRole` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
        } else {
            LOG.error("BUG! Connection Pool has already been created for proxy connection to {} state {} role {}",
                    remoteAddress, state, clientAuthRole);
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remoteAddress` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java

        LOG.info("[{}] complete connection, init proxy handler. authenticated with {} role {}, hasProxyToBrokerUrl: {}",
                remoteAddress, authMethod, clientAuthRole, hasProxyToBrokerUrl);
        if (hasProxyToBrokerUrl) {
            // Optimize proxy connection to fail-fast if the target broker isn't active
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `clientAuthRole` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java

        LOG.info("[{}] complete connection, init proxy handler. authenticated with {} role {}, hasProxyToBrokerUrl: {}",
                remoteAddress, authMethod, clientAuthRole, hasProxyToBrokerUrl);
        if (hasProxyToBrokerUrl) {
            // Optimize proxy connection to fail-fast if the target broker isn't active
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remoteAddress` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
                state = State.Closing;
                LOG.warn("[{}] Target broker '{}' isn't available. authenticated with {} role {}.",
                        remoteAddress, proxyToBrokerUrl, authMethod, clientAuthRole);
                ctx()
                        .writeAndFlush(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `clientAuthRole` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
                state = State.Closing;
                LOG.warn("[{}] Target broker '{}' isn't available. authenticated with {} role {}.",
                        remoteAddress, proxyToBrokerUrl, authMethod, clientAuthRole);
                ctx()
                        .writeAndFlush(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ctx` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
            state = State.ProxyConnectingToBroker;
            brokerProxyValidator.resolveAndCheckTargetAddress(proxyToBrokerUrl)
                    .thenAcceptAsync(this::connectToBroker, ctx.executor())
                    .exceptionally(throwable -> {
                        if (throwable instanceof TargetAddressDeniedException
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ctx` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
            state = State.ProxyLookupRequests;
            lookupProxyHandler = new LookupProxyHandler(service, this);
            ctx.writeAndFlush(Commands.newConnected(protocolVersionToAdvertise, false))
                    .addListener(ChannelFutureListener.FIRE_EXCEPTION_ON_FAILURE);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remoteAddress` accessed in synchronized context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java

        service.getClientCnxs().remove(this);
        LOG.info("[{}] Connection closed", remoteAddress);

        if (connectionPool != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.timeout` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/AbstractDelayedDeliveryTracker.java`
#### Snippet
```java
            lastTickRun = clock.millis();
            currentTimeoutTarget = -1;
            this.timeout = null;
            dispatcher.readMoreEntries();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stickyKeyConsumerSelector` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                && consumer.getKeySharedMeta().getHashRangesList() != null
                && consumer.getKeySharedMeta().getHashRangesList().size() > 0) {
            stickyKeyConsumerSelector = new HashRangeExclusiveStickyKeyConsumerSelector();
            stickyKeyConsumerSelector.addConsumer(consumer);
            isKeyHashRangeFiltered = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stickyKeyConsumerSelector` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                && consumer.getKeySharedMeta().getHashRangesList().size() > 0) {
            stickyKeyConsumerSelector = new HashRangeExclusiveStickyKeyConsumerSelector();
            stickyKeyConsumerSelector.addConsumer(consumer);
            isKeyHashRangeFiltered = true;
        } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isKeyHashRangeFiltered` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            stickyKeyConsumerSelector = new HashRangeExclusiveStickyKeyConsumerSelector();
            stickyKeyConsumerSelector.addConsumer(consumer);
            isKeyHashRangeFiltered = true;
        } else {
            isKeyHashRangeFiltered = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isKeyHashRangeFiltered` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            isKeyHashRangeFiltered = true;
        } else {
            isKeyHashRangeFiltered = false;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isFirstRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java

        if (consumers.isEmpty()) {
            isFirstRead = true;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    @Override
    public synchronized void resetCloseFuture() {
        closeFuture = null;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java

    public synchronized CompletableFuture<Void> disconnectActiveConsumers(boolean isResetCursor) {
        closeFuture = new CompletableFuture<>();
        if (activeConsumer != null) {
            activeConsumer.disconnect(isResetCursor);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            activeConsumer.disconnect(isResetCursor);
        }
        closeFuture.complete(null);
        return closeFuture;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        }
        closeFuture.complete(null);
        return closeFuture;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
     */
    public synchronized CompletableFuture<Void> disconnectAllConsumers(boolean isResetCursor) {
        closeFuture = new CompletableFuture<>();

        if (!consumers.isEmpty()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        } else {
            // no consumer connected, complete disconnect immediately
            closeFuture.complete(null);
        }
        return closeFuture;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            closeFuture.complete(null);
        }
        return closeFuture;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        }

        if (closeFuture == null && !consumers.isEmpty()) {
            pickAndScheduleActiveConsumer();
            return;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        cancelPendingRead();

        if (consumers.isEmpty() && closeFuture != null && !closeFuture.isDone()) {
            // Control reaches here only when closeFuture is created
            // and no more connected consumers left.
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        cancelPendingRead();

        if (consumers.isEmpty() && closeFuture != null && !closeFuture.isDone()) {
            // Control reaches here only when closeFuture is created
            // and no more connected consumers left.
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `closeFuture` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            // Control reaches here only when closeFuture is created
            // and no more connected consumers left.
            closeFuture.complete(null);
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `replicatorId` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
            disconnectFuture.completeExceptionally(new TopicBusyException("Cannot close a replicator with backlog"));
            if (log.isDebugEnabled()) {
                log.debug("[{}] Replicator disconnect failed since topic has backlog", replicatorId);
            }
            return disconnectFuture;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `replicatorId` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
        if (STATE_UPDATER.compareAndSet(this, State.Starting, State.Stopping)
                || STATE_UPDATER.compareAndSet(this, State.Started, State.Stopping)) {
            log.info("[{}] Disconnect replicator at position {} with backlog {}", replicatorId,
                    getReplicatorReadPosition(), getNumberOfEntriesInBacklog());
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `producer` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java

    protected synchronized CompletableFuture<Void> closeProducerAsync() {
        if (producer == null) {
            STATE_UPDATER.set(this, State.Stopped);
            return CompletableFuture.completedFuture(null);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `producer` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
            return CompletableFuture.completedFuture(null);
        }
        CompletableFuture<Void> future = producer.closeAsync();
        future.thenRun(() -> {
            STATE_UPDATER.set(this, State.Stopped);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `replicatorId` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
                log.debug(
                        "[{}] waiting for producer to close before attempting to reconnect, retrying in {} s",
                        replicatorId, waitTimeMs / 1000.0);
            }
            // BackOff before retrying
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `replicatorId` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
                // Already running
                if (log.isDebugEnabled()) {
                    log.debug("[{}] Replicator was already running", replicatorId);
                }
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `replicatorId` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
                }
            } else {
                log.info("[{}] Replicator already being started. Replicator state: {}", replicatorId, state);
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `replicatorId` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
        }

        log.info("[{}] Starting replicator", replicatorId);
        producerBuilder.createAsync().thenAccept(producer -> {
            readEntries(producer);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `preciseTopicPublishRateLimitingEnable` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
            if (publishRate.publishThrottlingRateInByte > 0 || publishRate.publishThrottlingRateInMsg > 0) {
                log.info("Enabling publish rate limiting {} ", publishRate);
                if (!preciseTopicPublishRateLimitingEnable) {
                    this.brokerService.setupTopicPublishRateLimiterMonitor();
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.topicPublishRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                }

                if (this.topicPublishRateLimiter == null
                    || this.topicPublishRateLimiter == PublishRateLimiter.DISABLED_RATE_LIMITER) {
                    // create new rateLimiter if rate-limiter is disabled
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.topicPublishRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java

                if (this.topicPublishRateLimiter == null
                    || this.topicPublishRateLimiter == PublishRateLimiter.DISABLED_RATE_LIMITER) {
                    // create new rateLimiter if rate-limiter is disabled
                    if (preciseTopicPublishRateLimitingEnable) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `preciseTopicPublishRateLimitingEnable` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                    || this.topicPublishRateLimiter == PublishRateLimiter.DISABLED_RATE_LIMITER) {
                    // create new rateLimiter if rate-limiter is disabled
                    if (preciseTopicPublishRateLimitingEnable) {
                        this.topicPublishRateLimiter = new PrecisPublishLimiter(publishRate,
                            () -> this.enableCnxAutoRead(), brokerService.pulsar().getExecutor());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.topicPublishRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                    // create new rateLimiter if rate-limiter is disabled
                    if (preciseTopicPublishRateLimitingEnable) {
                        this.topicPublishRateLimiter = new PrecisPublishLimiter(publishRate,
                            () -> this.enableCnxAutoRead(), brokerService.pulsar().getExecutor());
                    } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.topicPublishRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                            () -> this.enableCnxAutoRead(), brokerService.pulsar().getExecutor());
                    } else {
                        this.topicPublishRateLimiter = new PublishRateLimiterImpl(publishRate);
                    }
                } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.topicPublishRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                    }
                } else {
                    this.topicPublishRateLimiter.update(publishRate);
                }
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `topicPublishRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                    log.debug("Disabling publish throttling for {}", this.topic);
                }
                if (topicPublishRateLimiter != null) {
                    topicPublishRateLimiter.close();
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `topicPublishRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                }
                if (topicPublishRateLimiter != null) {
                    topicPublishRateLimiter.close();
                }
                this.topicPublishRateLimiter = PublishRateLimiter.DISABLED_RATE_LIMITER;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.topicPublishRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                    topicPublishRateLimiter.close();
                }
                this.topicPublishRateLimiter = PublishRateLimiter.DISABLED_RATE_LIMITER;
                enableProducerReadForPublishRateLimiting();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.minReplayedPosition` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
                redeliveryMessages.add(replayPosition.getLedgerId(), replayPosition.getEntryId());

                if (this.minReplayedPosition != null) {
                    // If relayPosition is a new entry wither smaller position is inserted for redelivery during this
                    // async read, it is possible that this relayPosition should dispatch to consumer first. So in
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minReplayedPosition` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
                    // order to preserver order delivery, we need to discard this read result, and try to trigger a
                    // replay read, that containing "relayPosition", by calling readMoreEntries.
                    if (replayPosition.compareTo(minReplayedPosition) < 0) {
                        if (log.isDebugEnabled()) {
                            log.debug("[{}] Position {} (<{}) is inserted for relay during current {} read, "
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minReplayedPosition` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
                            log.debug("[{}] Position {} (<{}) is inserted for relay during current {} read, "
                                            + "discard this read and retry with readMoreEntries.",
                                    name, replayPosition, minReplayedPosition, readType);
                        }
                        if (readType == ReadType.Normal) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        if (ctx.isLast()) {
            readFailureBackoff.reduceToHalf();
            havePendingRead = false;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isFirstRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        }

        isFirstRead = false;

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        isFirstRead = false;

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
                log.debug("[{}-{}] Increasing read batch size from {} to {}", name,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            if (log.isDebugEnabled()) {
                log.debug("[{}-{}] Increasing read batch size from {} to {}", name,
                        ((Consumer) ctx.ctx).consumerName(), readBatchSize, newReadBatchSize);
            }
            readBatchSize = newReadBatchSize;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                        ((Consumer) ctx.ctx).consumerName(), readBatchSize, newReadBatchSize);
            }
            readBatchSize = newReadBatchSize;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isKeyHashRangeFiltered` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);

        if (isKeyHashRangeFiltered) {
            byte[] key = peekStickyKey(entry.getDataBuffer());
            Consumer consumer = stickyKeyConsumerSelector.select(key);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stickyKeyConsumerSelector` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        if (isKeyHashRangeFiltered) {
            byte[] key = peekStickyKey(entry.getDataBuffer());
            Consumer consumer = stickyKeyConsumerSelector.select(key);
            // Skip the entry if it's not for current active consumer.
            if (consumer == null || currentConsumer != consumer) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            entry.release();
            streamingEntryReader.cancelReadRequests();
            havePendingRead = false;
            if (currentConsumer != null) {
                notifyActiveConsumerChanged(currentConsumer);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                synchronized (PersistentStreamingDispatcherSingleActiveConsumer.this) {
                    Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);
                    if (currentConsumer != null && !havePendingRead) {
                        if (log.isDebugEnabled()) {
                            log.debug("[{}-{}] Scheduling read ", name, currentConsumer);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                    } else {
                        log.info("[{}-{}] Skipping read as we still havePendingRead {}", name,
                                currentConsumer, havePendingRead);
                    }
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        if (consumer.getAvailablePermits() > 0) {
            synchronized (this) {
                if (havePendingRead) {
                    if (log.isDebugEnabled()) {
                        log.debug("[{}] Skipping read for the topic, Due to we have pending read.", topic.getName());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                    log.debug("[{}-{}] Schedule read of {} messages", name, consumer, messagesToRead);
                }
                havePendingRead = true;

                if (consumer.readCompacted()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isFirstRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java

                if (consumer.readCompacted()) {
                    topic.getCompactedTopic().asyncReadEntriesOrWait(cursor, messagesToRead, isFirstRead,
                            this, consumer);
                } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java

    private synchronized void internalReadEntriesFailed(ManagedLedgerException exception, Object ctx) {
        havePendingRead = false;
        ReadEntriesCtx readEntriesCtx = (ReadEntriesCtx) ctx;
        Consumer c = readEntriesCtx.getConsumer();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java

        // Reduce read batch size to avoid flooding bookies with retries
        readBatchSize = serviceConfig.getDispatcherMinReadBatchSize();

        topic.getBrokerService().executor().schedule(() -> {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                    Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);
                    // we should retry the read if we have an active consumer and there is no pending read
                    if (currentConsumer != null && !havePendingRead) {
                        if (log.isDebugEnabled()) {
                            log.debug("[{}-{}] Retrying read operation", name, c);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                    } else {
                        log.info("[{}-{}] Skipping read retry: Current Consumer {}, havePendingRead {}", name, c,
                                currentConsumer, havePendingRead);
                    }
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        }

        havePendingRead = false;
        isFirstRead = false;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isFirstRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java

        havePendingRead = false;
        isFirstRead = false;

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        isFirstRead = false;

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
                log.debug("[{}-{}] Increasing read batch size from {} to {}", name, readConsumer, readBatchSize,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
                log.debug("[{}-{}] Increasing read batch size from {} to {}", name, readConsumer, readBatchSize,
                        newReadBatchSize);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            }

            readBatchSize = newReadBatchSize;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isKeyHashRangeFiltered` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);

        if (isKeyHashRangeFiltered) {
            Iterator<Entry> iterator = entries.iterator();
            while (iterator.hasNext()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stickyKeyConsumerSelector` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                Entry entry = iterator.next();
                byte[] key = peekStickyKey(entry.getDataBuffer());
                Consumer consumer = stickyKeyConsumerSelector.select(key);
                // Skip the entry if it's not for current active consumer.
                if (consumer == null || currentConsumer != consumer) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        }
        cursor.cancelPendingReadRequest();
        havePendingRead = false;
        cursor.rewind();
        if (log.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java

    private synchronized void internalConsumerFlow(Consumer consumer) {
        if (havePendingRead) {
            if (log.isDebugEnabled()) {
                log.debug("[{}-{}] Ignoring flow control message since we already have a pending read req", name,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
        if (consumer.getAvailablePermits() > 0) {
            synchronized (this) {
                if (havePendingRead) {
                    if (log.isDebugEnabled()) {
                        log.debug("[{}] Skipping read for the topic, Due to we have pending read.", topic.getName());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                    log.debug("[{}-{}] Schedule read of {} messages", name, consumer, messagesToRead);
                }
                havePendingRead = true;
                if (consumer.readCompacted()) {
                    topic.getCompactedTopic().asyncReadEntriesOrWait(cursor, messagesToRead, isFirstRead,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isFirstRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
                havePendingRead = true;
                if (consumer.readCompacted()) {
                    topic.getCompactedTopic().asyncReadEntriesOrWait(cursor, messagesToRead, isFirstRead,
                            this, consumer);
                } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatchRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
    public void initializeDispatchRateLimiterIfNeeded() {
        synchronized (dispatchRateLimiterLock) {
            if (!dispatchRateLimiter.isPresent()
                && DispatchRateLimiter.isDispatchRateEnabled(topic.getReplicatorDispatchRate())) {
                this.dispatchRateLimiter = Optional.of(new DispatchRateLimiter(topic, Type.REPLICATOR));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.dispatchRateLimiter` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
            if (!dispatchRateLimiter.isPresent()
                && DispatchRateLimiter.isDispatchRateEnabled(topic.getReplicatorDispatchRate())) {
                this.dispatchRateLimiter = Optional.of(new DispatchRateLimiter(topic, Type.REPLICATOR));
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topic.getName(), SafeRun.safeRun(() -> {
                synchronized (PersistentStreamingDispatcherMultipleConsumers.this) {
                    if (!havePendingRead) {
                        log.info("[{}] Scheduling read operation", name);
                        readMoreEntries();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalAvailablePermits` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
        }
        // totalAvailablePermits may be updated by other threads
        int currentTotalAvailablePermits = totalAvailablePermits;
        if (currentTotalAvailablePermits > 0 && isAtleastOneConsumerAvailable()) {
            Pair<Integer, Long> calculateResult = calculateToRead(currentTotalAvailablePermits);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingReplayRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
                }

                havePendingReplayRead = true;
                Set<? extends Position> deletedMessages = topic.isDelayedDeliveryEnabled()
                        ? asyncReplayEntriesInOrder(messagesToReplayNow) : asyncReplayEntries(messagesToReplayNow);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingReplayRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
                // next entries as readCompletedEntries-callback was never called
                if ((messagesToReplayNow.size() - deletedMessages.size()) == 0) {
                    havePendingReplayRead = false;
                    // We should not call readMoreEntries() recursively in the same thread
                    // as there is a risk of StackOverflowError
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalUnackedMessages` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
            } else if (BLOCKED_DISPATCHER_ON_UNACKMSG_UPDATER.get(this) == TRUE) {
                log.debug("[{}] Dispatcher read is blocked due to unackMessages {} reached to max {}", name,
                        totalUnackedMessages, topic.getMaxUnackedMessagesOnSubscription());
            } else if (!havePendingRead) {
                if (log.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
                log.debug("[{}] Dispatcher read is blocked due to unackMessages {} reached to max {}", name,
                        totalUnackedMessages, topic.getMaxUnackedMessagesOnSubscription());
            } else if (!havePendingRead) {
                if (log.isDebugEnabled()) {
                    log.debug("[{}] Schedule read of {} messages for {} consumers", name, messagesToRead,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
                            consumerList.size());
                }
                havePendingRead = true;
                streamingEntryReader.asyncReadEntries(messagesToRead, bytesToRead,
                        ReadType.Normal);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
            readFailureBackoff.reduceToHalf();
            if (readType == ReadType.Normal) {
                havePendingRead = false;
            } else {
                havePendingReplayRead = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingReplayRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
                havePendingRead = false;
            } else {
                havePendingReplayRead = false;
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
        }

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
                log.debug("[{}] Increasing read batch size from {} to {}", name, readBatchSize, newReadBatchSize);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
                log.debug("[{}] Increasing read batch size from {} to {}", name, readBatchSize, newReadBatchSize);
            }
            readBatchSize = newReadBatchSize;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
                log.debug("[{}] Increasing read batch size from {} to {}", name, readBatchSize, newReadBatchSize);
            }
            readBatchSize = newReadBatchSize;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `shouldRewindBeforeReadingOrReplaying` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
        }

        if (shouldRewindBeforeReadingOrReplaying && readType == ReadType.Normal) {
            // All consumers got disconnected before the completion of the read operation
            entry.release();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `shouldRewindBeforeReadingOrReplaying` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
            entry.release();
            cursor.rewind();
            shouldRewindBeforeReadingOrReplaying = false;
            readMoreEntries();
            return;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        ReadType readType = (ReadType) ctx;
        if (readType == ReadType.Normal) {
            havePendingRead = false;
        } else {
            havePendingReplayRead = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingReplayRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            havePendingRead = false;
        } else {
            havePendingReplayRead = false;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        }

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

        if (readBatchSize < serviceConfig.getDispatcherMaxReadBatchSize()) {
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
                log.debug("[{}] Increasing read batch size from {} to {}", name, readBatchSize, newReadBatchSize);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            int newReadBatchSize = Math.min(readBatchSize * 2, serviceConfig.getDispatcherMaxReadBatchSize());
            if (log.isDebugEnabled()) {
                log.debug("[{}] Increasing read batch size from {} to {}", name, readBatchSize, newReadBatchSize);
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            }

            readBatchSize = newReadBatchSize;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `shouldRewindBeforeReadingOrReplaying` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        readFailureBackoff.reduceToHalf();

        if (shouldRewindBeforeReadingOrReplaying && readType == ReadType.Normal) {
            // All consumers got disconnected before the completion of the read operation
            entries.forEach(Entry::release);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `shouldRewindBeforeReadingOrReplaying` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            entries.forEach(Entry::release);
            cursor.rewind();
            shouldRewindBeforeReadingOrReplaying = false;
            readMoreEntries();
            return;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalAvailablePermits` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        // totalAvailablePermits may be updated by other threads
        int firstAvailableConsumerPermits = getFirstAvailableConsumerPermits();
        int currentTotalAvailablePermits = Math.max(totalAvailablePermits, firstAvailableConsumerPermits);
        if (currentTotalAvailablePermits > 0 && firstAvailableConsumerPermits > 0) {
            Pair<Integer, Long> calculateResult = calculateToRead(currentTotalAvailablePermits);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingReplayRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                }

                havePendingReplayRead = true;
                minReplayedPosition = messagesToReplayNow.first();
                Set<? extends Position> deletedMessages = topic.isDelayedDeliveryEnabled()
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minReplayedPosition` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

                havePendingReplayRead = true;
                minReplayedPosition = messagesToReplayNow.first();
                Set<? extends Position> deletedMessages = topic.isDelayedDeliveryEnabled()
                        ? asyncReplayEntriesInOrder(messagesToReplayNow) : asyncReplayEntries(messagesToReplayNow);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingReplayRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                // next entries as readCompletedEntries-callback was never called
                if ((messagesToReplayNow.size() - deletedMessages.size()) == 0) {
                    havePendingReplayRead = false;
                    readMoreEntriesAsync();
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalUnackedMessages` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                if (log.isDebugEnabled()) {
                    log.debug("[{}] Dispatcher read is blocked due to unackMessages {} reached to max {}", name,
                            totalUnackedMessages, topic.getMaxUnackedMessagesOnSubscription());
                }
            } else if (!havePendingRead) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                            totalUnackedMessages, topic.getMaxUnackedMessagesOnSubscription());
                }
            } else if (!havePendingRead) {
                if (log.isDebugEnabled()) {
                    log.debug("[{}] Schedule read of {} messages for {} consumers", name, messagesToRead,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                            consumerList.size());
                }
                havePendingRead = true;
                NavigableSet<PositionImpl> toReplay = getMessagesToReplayNow(1);
                if (!toReplay.isEmpty()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minReplayedPosition` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                NavigableSet<PositionImpl> toReplay = getMessagesToReplayNow(1);
                if (!toReplay.isEmpty()) {
                    minReplayedPosition = toReplay.first();
                    redeliveryMessages.add(minReplayedPosition.getLedgerId(), minReplayedPosition.getEntryId());
                } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minReplayedPosition` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                if (!toReplay.isEmpty()) {
                    minReplayedPosition = toReplay.first();
                    redeliveryMessages.add(minReplayedPosition.getLedgerId(), minReplayedPosition.getEntryId());
                } else {
                    minReplayedPosition = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minReplayedPosition` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                if (!toReplay.isEmpty()) {
                    minReplayedPosition = toReplay.first();
                    redeliveryMessages.add(minReplayedPosition.getLedgerId(), minReplayedPosition.getEntryId());
                } else {
                    minReplayedPosition = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minReplayedPosition` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                    redeliveryMessages.add(minReplayedPosition.getLedgerId(), minReplayedPosition.getEntryId());
                } else {
                    minReplayedPosition = null;
                }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        }
        if (consumerList.isEmpty()) {
            if (havePendingRead || havePendingReplayRead) {
                // There is a pending read from previous run. We must wait for it to complete and then rewind
                shouldRewindBeforeReadingOrReplaying = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingReplayRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        }
        if (consumerList.isEmpty()) {
            if (havePendingRead || havePendingReplayRead) {
                // There is a pending read from previous run. We must wait for it to complete and then rewind
                shouldRewindBeforeReadingOrReplaying = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `shouldRewindBeforeReadingOrReplaying` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            if (havePendingRead || havePendingReplayRead) {
                // There is a pending read from previous run. We must wait for it to complete and then rewind
                shouldRewindBeforeReadingOrReplaying = true;
            } else {
                cursor.rewind();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `shouldRewindBeforeReadingOrReplaying` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            } else {
                cursor.rewind();
                shouldRewindBeforeReadingOrReplaying = false;
            }
            redeliveryMessages.clear();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `shouldRewindBeforeReadingOrReplaying` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        }

        if (shouldRewindBeforeReadingOrReplaying) {
            shouldRewindBeforeReadingOrReplaying = false;
            cursor.rewind();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `shouldRewindBeforeReadingOrReplaying` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

        if (shouldRewindBeforeReadingOrReplaying) {
            shouldRewindBeforeReadingOrReplaying = false;
            cursor.rewind();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

        if (readType == ReadType.Normal) {
            havePendingRead = false;
        } else {
            havePendingReplayRead = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingReplayRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            havePendingRead = false;
        } else {
            havePendingReplayRead = false;
            if (exception instanceof ManagedLedgerException.InvalidReplayPositionException) {
                PositionImpl markDeletePosition = (PositionImpl) cursor.getMarkDeletedPosition();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readBatchSize` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        }

        readBatchSize = serviceConfig.getDispatcherMinReadBatchSize();

        topic.getBrokerService().executor().schedule(() -> {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                // another scheduled read, otherwise we'd be stuck until
                // more messages are published.
                if (!havePendingRead || readType == ReadType.Replay) {
                    log.info("[{}] Retrying read operation", name);
                    readMoreEntries();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `havePendingRead` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                    readMoreEntries();
                } else {
                    log.info("[{}] Skipping read retry: havePendingRead {}", name, havePendingRead, exception);
                }
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalAvailablePermits` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        }

        totalAvailablePermits += additionalNumberOfMessages;

        if (log.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalAvailablePermits` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            log.debug("[{}-{}] Trigger new read after receiving flow control message with permits {} "
                            + "after adding {} permits", name, consumer,
                    totalAvailablePermits, additionalNumberOfMessages);
        }
        readMoreEntries();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalAvailablePermits` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                    closeFuture.complete(null);
                }
                totalAvailablePermits = 0;
            } else {
                if (log.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalAvailablePermits` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                    addMessageToReplay(ledgerId, entryId, stickyKeyHash);
                });
                totalAvailablePermits -= consumer.getAvailablePermits();
                if (log.isDebugEnabled()) {
                    log.debug("[{}] Decreased totalAvailablePermits by {} in PersistentDispatcherMultipleConsumers. "
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalAvailablePermits` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                    log.debug("[{}] Decreased totalAvailablePermits by {} in PersistentDispatcherMultipleConsumers. "
                                    + "New dispatcher permit count is {}", name, consumer.getAvailablePermits(),
                            totalAvailablePermits);
                }
                readMoreEntries();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `totalAvailablePermits` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
        while (entriesToDispatch > 0) {
            firstAvailableConsumerPermits = getFirstAvailableConsumerPermits();
            currentTotalAvailablePermits = Math.max(totalAvailablePermits, firstAvailableConsumerPermits);
            dispatchMessage = currentTotalAvailablePermits > 0 && firstAvailableConsumerPermits > 0;
            if (!dispatchMessage) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    public CompletableFuture<Void> close() {
        synchronized (this) {
            if (dispatcher != null && dispatcher.isConsumerConnected()) {
                return FutureUtil.failedFuture(new SubscriptionBusyException("Subscription has active consumers"));
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    public CompletableFuture<Void> close() {
        synchronized (this) {
            if (dispatcher != null && dispatcher.isConsumerConnected()) {
                return FutureUtil.failedFuture(new SubscriptionBusyException("Subscription has active consumers"));
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    @Override
    public synchronized long getNumberOfEntriesDelayed() {
        if (dispatcher != null) {
            return dispatcher.getNumberOfDelayedMessages();
        } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    public synchronized long getNumberOfEntriesDelayed() {
        if (dispatcher != null) {
            return dispatcher.getNumberOfDelayedMessages();
        } else {
            return 0;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                }

                if (dispatcher == null || !dispatcher.isConsumerConnected()) {
                    Dispatcher previousDispatcher = null;
                    boolean useStreamingDispatcher = topic.getBrokerService().getPulsar()
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                }

                if (dispatcher == null || !dispatcher.isConsumerConnected()) {
                    Dispatcher previousDispatcher = null;
                    boolean useStreamingDispatcher = topic.getBrokerService().getPulsar()
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                    switch (consumer.subType()) {
                        case Exclusive:
                            if (dispatcher == null || dispatcher.getType() != SubType.Exclusive) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                    switch (consumer.subType()) {
                        case Exclusive:
                            if (dispatcher == null || dispatcher.getType() != SubType.Exclusive) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                        case Exclusive:
                            if (dispatcher == null || dispatcher.getType() != SubType.Exclusive) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
                                        ? new PersistentStreamingDispatcherSingleActiveConsumer(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                            if (dispatcher == null || dispatcher.getType() != SubType.Exclusive) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
                                        ? new PersistentStreamingDispatcherSingleActiveConsumer(
                                        cursor, SubType.Exclusive, 0, topic, this)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                            break;
                        case Shared:
                            if (dispatcher == null || dispatcher.getType() != SubType.Shared) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                            break;
                        case Shared:
                            if (dispatcher == null || dispatcher.getType() != SubType.Shared) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                        case Shared:
                            if (dispatcher == null || dispatcher.getType() != SubType.Shared) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
                                        ? new PersistentStreamingDispatcherMultipleConsumers(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                            if (dispatcher == null || dispatcher.getType() != SubType.Shared) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
                                        ? new PersistentStreamingDispatcherMultipleConsumers(
                                        topic, cursor, this)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                            }

                            if (dispatcher == null || dispatcher.getType() != SubType.Failover) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                            }

                            if (dispatcher == null || dispatcher.getType() != SubType.Failover) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java

                            if (dispatcher == null || dispatcher.getType() != SubType.Failover) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
                                        ? new PersistentStreamingDispatcherSingleActiveConsumer(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                            if (dispatcher == null || dispatcher.getType() != SubType.Failover) {
                                previousDispatcher = dispatcher;
                                dispatcher = useStreamingDispatcher
                                        ? new PersistentStreamingDispatcherSingleActiveConsumer(
                                        cursor, SubType.Failover, partitionIndex, topic, this) :
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                        case Key_Shared:
                            KeySharedMeta ksm = consumer.getKeySharedMeta();
                            if (dispatcher == null || dispatcher.getType() != SubType.Key_Shared
                                    || !((PersistentStickyKeyDispatcherMultipleConsumers) dispatcher)
                                    .hasSameKeySharedPolicy(ksm)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                        case Key_Shared:
                            KeySharedMeta ksm = consumer.getKeySharedMeta();
                            if (dispatcher == null || dispatcher.getType() != SubType.Key_Shared
                                    || !((PersistentStickyKeyDispatcherMultipleConsumers) dispatcher)
                                    .hasSameKeySharedPolicy(ksm)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                            KeySharedMeta ksm = consumer.getKeySharedMeta();
                            if (dispatcher == null || dispatcher.getType() != SubType.Key_Shared
                                    || !((PersistentStickyKeyDispatcherMultipleConsumers) dispatcher)
                                    .hasSameKeySharedPolicy(ksm)) {
                                previousDispatcher = dispatcher;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                                    || !((PersistentStickyKeyDispatcherMultipleConsumers) dispatcher)
                                    .hasSameKeySharedPolicy(ksm)) {
                                previousDispatcher = dispatcher;
                                dispatcher = new PersistentStickyKeyDispatcherMultipleConsumers(topic, cursor, this,
                                        topic.getBrokerService().getPulsar().getConfiguration(), ksm);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                                    .hasSameKeySharedPolicy(ksm)) {
                                previousDispatcher = dispatcher;
                                dispatcher = new PersistentStickyKeyDispatcherMultipleConsumers(topic, cursor, this,
                                        topic.getBrokerService().getPulsar().getConfiguration(), ksm);
                            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                    }
                } else {
                    if (consumer.subType() != dispatcher.getType()) {
                        return FutureUtil.failedFuture(
                                new SubscriptionBusyException("Subscription is of different type"));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java

                try {
                    dispatcher.addConsumer(consumer);
                    return CompletableFuture.completedFuture(null);
                } catch (BrokerServiceException brokerServiceException) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        // Lock the Subscription object before locking the Dispatcher object to avoid deadlocks
        synchronized (this) {
            if (dispatcher != null && dispatcher.isConsumerConnected()) {
                disconnectFuture = dispatcher.disconnectActiveConsumers(true);
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        // Lock the Subscription object before locking the Dispatcher object to avoid deadlocks
        synchronized (this) {
            if (dispatcher != null && dispatcher.isConsumerConnected()) {
                disconnectFuture = dispatcher.disconnectActiveConsumers(true);
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        synchronized (this) {
            if (dispatcher != null && dispatcher.isConsumerConnected()) {
                disconnectFuture = dispatcher.disconnectActiveConsumers(true);
            } else {
                disconnectFuture = CompletableFuture.completedFuture(null);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    @Override
    public synchronized Dispatcher getDispatcher() {
        return this.dispatcher;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        closeSubscriptionFuture.thenCompose(v -> topic.unsubscribe(subName)).thenAccept(v -> {
            synchronized (this) {
                (dispatcher != null ? dispatcher.close() : CompletableFuture.completedFuture(null)).thenRun(() -> {
                    log.info("[{}][{}] Successfully deleted subscription", topicName, subName);
                    deleteFuture.complete(null);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        closeSubscriptionFuture.thenCompose(v -> topic.unsubscribe(subName)).thenAccept(v -> {
            synchronized (this) {
                (dispatcher != null ? dispatcher.close() : CompletableFuture.completedFuture(null)).thenRun(() -> {
                    log.info("[{}][{}] Successfully deleted subscription", topicName, subName);
                    deleteFuture.complete(null);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        IS_FENCED_UPDATER.set(this, TRUE);

        (dispatcher != null ? dispatcher.close() : CompletableFuture.completedFuture(null))
                .thenCompose(v -> close()).thenRun(() -> {
                    log.info("[{}][{}] Successfully disconnected and closed subscription", topicName, subName);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        IS_FENCED_UPDATER.set(this, TRUE);

        (dispatcher != null ? dispatcher.close() : CompletableFuture.completedFuture(null))
                .thenCompose(v -> close()).thenRun(() -> {
                    log.info("[{}][{}] Successfully disconnected and closed subscription", topicName, subName);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    public synchronized void removeConsumer(Consumer consumer, boolean isResetCursor) throws BrokerServiceException {
        cursor.updateLastActive();
        if (dispatcher != null) {
            dispatcher.removeConsumer(consumer);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        cursor.updateLastActive();
        if (dispatcher != null) {
            dispatcher.removeConsumer(consumer);
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        msgOutFromRemovedConsumer.add(stats.msgOutCounter);

        if (dispatcher != null && dispatcher.getConsumers().isEmpty()) {
            deactivateCursor();
            topic.getManagedLedger().removeWaitingCursor(cursor);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        msgOutFromRemovedConsumer.add(stats.msgOutCounter);

        if (dispatcher != null && dispatcher.getConsumers().isEmpty()) {
            deactivateCursor();
            topic.getManagedLedger().removeWaitingCursor(cursor);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                this.close().thenRun(() -> {
                    synchronized (this) {
                        if (dispatcher != null) {
                            dispatcher.close().thenRun(() -> {
                                log.info("[{}][{}] Successfully closed dispatcher for reader", topicName, subName);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatcher` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                    synchronized (this) {
                        if (dispatcher != null) {
                            dispatcher.close().thenRun(() -> {
                                log.info("[{}][{}] Successfully closed dispatcher for reader", topicName, subName);
                            }).exceptionally(ex -> {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isFenced` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

    private synchronized void fence() {
        isFenced = true;
        ScheduledFuture<?> monitoringTask = this.fencedTopicMonitoringTask;
        if (monitoringTask == null || monitoringTask.isDone()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isFenced` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        if (pending == 0 && isFenced && !isClosingOrDeleting) {
            synchronized (this) {
                if (isFenced && !isClosingOrDeleting) {
                    messageDeduplication.resetHighestSequenceIdPushed();
                    log.info("[{}] Un-fencing topic...", topic);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isFenced` accessed in synchronized context
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

    private synchronized void unfence() {
        isFenced = false;
        cancelFencedTopicMonitoringTask();
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `batchReceiveTimeout` accessed in synchronized context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
            }
            if (hasPendingReceives) {
                batchReceiveTimeout = client.timer().newTimeout(this::pendingBatchReceiveTask,
                        timeToWaitMs, TimeUnit.MILLISECONDS);
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `batchReceiveTimeout` accessed in synchronized context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
                        timeToWaitMs, TimeUnit.MILLISECONDS);
            } else {
                batchReceiveTimeout = null;
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.cachedToken` accessed in synchronized context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/AuthenticationOAuth2.java`
#### Snippet
```java
    @Override
    public synchronized AuthenticationDataProvider getAuthData() throws PulsarClientException {
        if (this.cachedToken == null || this.cachedToken.isExpired()) {
            TokenResult tr = this.flow.authenticate();
            this.cachedToken = new CachedToken(tr);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.cachedToken` accessed in synchronized context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/AuthenticationOAuth2.java`
#### Snippet
```java
    @Override
    public synchronized AuthenticationDataProvider getAuthData() throws PulsarClientException {
        if (this.cachedToken == null || this.cachedToken.isExpired()) {
            TokenResult tr = this.flow.authenticate();
            this.cachedToken = new CachedToken(tr);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.flow` accessed in synchronized context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/AuthenticationOAuth2.java`
#### Snippet
```java
    public synchronized AuthenticationDataProvider getAuthData() throws PulsarClientException {
        if (this.cachedToken == null || this.cachedToken.isExpired()) {
            TokenResult tr = this.flow.authenticate();
            this.cachedToken = new CachedToken(tr);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.cachedToken` accessed in synchronized context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/AuthenticationOAuth2.java`
#### Snippet
```java
        if (this.cachedToken == null || this.cachedToken.isExpired()) {
            TokenResult tr = this.flow.authenticate();
            this.cachedToken = new CachedToken(tr);
        }
        return this.cachedToken.getAuthData();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.cachedToken` accessed in synchronized context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/AuthenticationOAuth2.java`
#### Snippet
```java
            this.cachedToken = new CachedToken(tr);
        }
        return this.cachedToken.getAuthData();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsKeyStoreType` accessed in synchronized context
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
        if (isServer) {
            this.keyStoreSSLContext = KeyStoreSSLContext.createServerKeyStoreSslContext(tlsProvider,
                    tlsKeyStoreType, tlsKeyStore.getFileName(), tlsKeyStorePassword,
                    tlsAllowInsecureConnection,
                    tlsTrustStoreType, tlsTrustStore.getFileName(), tlsTrustStorePassword,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsKeyStore` accessed in synchronized context
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
        if (isServer) {
            this.keyStoreSSLContext = KeyStoreSSLContext.createServerKeyStoreSslContext(tlsProvider,
                    tlsKeyStoreType, tlsKeyStore.getFileName(), tlsKeyStorePassword,
                    tlsAllowInsecureConnection,
                    tlsTrustStoreType, tlsTrustStore.getFileName(), tlsTrustStorePassword,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsKeyStorePassword` accessed in synchronized context
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
        if (isServer) {
            this.keyStoreSSLContext = KeyStoreSSLContext.createServerKeyStoreSslContext(tlsProvider,
                    tlsKeyStoreType, tlsKeyStore.getFileName(), tlsKeyStorePassword,
                    tlsAllowInsecureConnection,
                    tlsTrustStoreType, tlsTrustStore.getFileName(), tlsTrustStorePassword,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsRequireTrustedClientCertOnConnect` accessed in synchronized context
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
                    tlsAllowInsecureConnection,
                    tlsTrustStoreType, tlsTrustStore.getFileName(), tlsTrustStorePassword,
                    tlsRequireTrustedClientCertOnConnect, tlsCiphers, tlsProtocols);
        } else {
            KeyStoreParams authParams = authData.getTlsKeyStoreParams();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `authData` accessed in synchronized context
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
                    tlsRequireTrustedClientCertOnConnect, tlsCiphers, tlsProtocols);
        } else {
            KeyStoreParams authParams = authData.getTlsKeyStoreParams();
            this.keyStoreSSLContext = KeyStoreSSLContext.createClientKeyStoreSslContext(tlsProvider,
                    authParams != null ? authParams.getKeyStoreType() : tlsKeyStoreType,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsKeyStoreType` accessed in synchronized context
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
            KeyStoreParams authParams = authData.getTlsKeyStoreParams();
            this.keyStoreSSLContext = KeyStoreSSLContext.createClientKeyStoreSslContext(tlsProvider,
                    authParams != null ? authParams.getKeyStoreType() : tlsKeyStoreType,
                    authParams != null ? authParams.getKeyStorePath() : tlsKeyStore.getFileName(),
                    authParams != null ? authParams.getKeyStorePassword() : tlsKeyStorePassword,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsKeyStore` accessed in synchronized context
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
            this.keyStoreSSLContext = KeyStoreSSLContext.createClientKeyStoreSslContext(tlsProvider,
                    authParams != null ? authParams.getKeyStoreType() : tlsKeyStoreType,
                    authParams != null ? authParams.getKeyStorePath() : tlsKeyStore.getFileName(),
                    authParams != null ? authParams.getKeyStorePassword() : tlsKeyStorePassword,
                    tlsAllowInsecureConnection,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsKeyStorePassword` accessed in synchronized context
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/NettySSLContextAutoRefreshBuilder.java`
#### Snippet
```java
                    authParams != null ? authParams.getKeyStoreType() : tlsKeyStoreType,
                    authParams != null ? authParams.getKeyStorePath() : tlsKeyStore.getFileName(),
                    authParams != null ? authParams.getKeyStorePassword() : tlsKeyStorePassword,
                    tlsAllowInsecureConnection,
                    tlsTrustStoreType, tlsTrustStore.getFileName(), tlsTrustStorePassword,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lastDequeuedMessageId` accessed in synchronized context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        ClientCnx currentCnx = cnx();
        ClientCnx msgCnx = ((MessageImpl<?>) msg).getCnx();
        lastDequeuedMessageId = msg.getMessageId();

        if (msgCnx != currentCnx) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `markDeletePosition` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/NonDurableCursorImpl.java`
#### Snippet
```java
    @Override
    public synchronized String toString() {
        return MoreObjects.toStringHelper(this).add("ledger", ledger.getName()).add("ackPos", markDeletePosition)
                .add("readPos", readPosition).toString();
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readPosition` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/NonDurableCursorImpl.java`
#### Snippet
```java
    public synchronized String toString() {
        return MoreObjects.toStringHelper(this).add("ledger", ledger.getName()).add("ackPos", markDeletePosition)
                .add("readPos", readPosition).toString();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rootIterator` accessed in synchronized context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LongHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
    public synchronized LedgerManager.LedgerRange next() throws IOException {
        bootstrap();
        return rootIterator.next();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rootIterator` accessed in synchronized context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LongHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
    public synchronized boolean hasNext() throws IOException {
        bootstrap();
        return rootIterator.hasNext();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `servletContext` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/WorkerReadinessResource.java`
#### Snippet
```java
  public synchronized WorkerService get() {
    if (this.workerService == null) {
      this.workerService = (WorkerService) servletContext.getAttribute(ATTRIBUTE_WORKER_SERVICE);
    }
    return this.workerService;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `servletContext` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/FunctionApiResource.java`
#### Snippet
```java
    public synchronized WorkerService get() {
        if (this.workerService == null) {
            this.workerService = (WorkerService) servletContext.getAttribute(ATTRIBUTE_FUNCTION_WORKER);
        }
        return this.workerService;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this .workerIdToAssignments` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
                // only run the termination logic if
                // this is the last/only instance from a function left on the worker
                Collection<Assignment> assignments = findFunctionAssignments(tenant, namespace, name, this
                        .workerIdToAssignments);
                if (assignments.size() > 1) {
                    this.conditionallyStopFunction(functionRuntimeInfo);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `workerIdToAssignments` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java

        String workerId = null;
        for (Entry<String, Map<String, Assignment>> workerAssignments : workerIdToAssignments.entrySet()) {
            if (workerAssignments.getValue().remove(fullyQualifiedInstanceId) != null) {
                workerId = workerAssignments.getKey();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `workerIdToAssignments` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
        }
        Map<String, Assignment> worker;
        if (workerId != null && ((worker = workerIdToAssignments.get(workerId)) != null && worker.isEmpty())) {
            this.workerIdToAssignments.remove(workerId);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.workerIdToAssignments` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
        Map<String, Assignment> worker;
        if (workerId != null && ((worker = workerIdToAssignments.get(workerId)) != null && worker.isEmpty())) {
            this.workerIdToAssignments.remove(workerId);
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.workerIdToAssignments` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
    public synchronized Collection<Assignment> findFunctionAssignments(String tenant,
                                                                       String namespace, String functionName) {
        return findFunctionAssignments(tenant, namespace, functionName, this.workerIdToAssignments);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.workerIdToAssignments` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java

        boolean exists = false;
        for (Map<String, Assignment> entry : this.workerIdToAssignments.values()) {
            if (entry.containsKey(FunctionCommon.getFullyQualifiedInstanceId(newAssignment.getInstance()))) {
                exists = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.workerIdToAssignments` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
    public synchronized Map<String, Map<String, Assignment>> getCurrentAssignments() {
        Map<String, Map<String, Assignment>> copy = new HashMap<>();
        for (Map.Entry<String, Map<String, Assignment>> entry : this.workerIdToAssignments.entrySet()) {
            Map<String, Assignment> tmp = new HashMap<>();
            tmp.putAll(entry.getValue());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `servletContext` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/WorkerStatsApiV2Resource.java`
#### Snippet
```java
    public synchronized WorkerService get() {
        if (this.workerService == null) {
            this.workerService = (WorkerService) servletContext.getAttribute(ATTRIBUTE_WORKERSTATS_SERVICE);
        }
        return this.workerService;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `servletContext` accessed in synchronized context
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/WorkerApiV2Resource.java`
#### Snippet
```java
    public synchronized WorkerService get() {
        if (this.workerService == null) {
            this.workerService = (WorkerService) servletContext.getAttribute(ATTRIBUTE_WORKER_SERVICE);
        }
        return this.workerService;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

            default:
                log.error("[{}][{}] Invalid cursor state: {}", ledger.getName(), name, state);
                callback.markDeleteFailed(new ManagedLedgerException("Cursor was in invalid state: " + state), ctx);
                break;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            default:
                log.error("[{}][{}] Invalid cursor state: {}", ledger.getName(), name, state);
                callback.markDeleteFailed(new ManagedLedgerException("Cursor was in invalid state: " + state), ctx);
                break;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingCallbacks` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                    // and not add it to the list
                    entry.release();
                    if (--pendingCallbacks == 0) {
                        callback.readEntriesFailed(exception.get(), ctx);
                    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingCallbacks` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                } else {
                    entries.add(entry);
                    if (--pendingCallbacks == 0) {
                        if (sortEntries) {
                            entries.sort(ENTRY_COMPARATOR);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `markDeletePosition` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    public synchronized String toString() {
        return MoreObjects.toStringHelper(this).add("ledger", ledger.getName()).add("name", name)
                .add("ackPos", markDeletePosition).add("readPos", readPosition).toString();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readPosition` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    public synchronized String toString() {
        return MoreObjects.toStringHelper(this).add("ledger", ledger.getName()).add("name", name)
                .add("ackPos", markDeletePosition).add("readPos", readPosition).toString();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingCallbacks` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                    entries.forEach(Entry::release);
                }
                if (--pendingCallbacks == 0) {
                    callback.readEntriesFailed(exception.get(), ctx);
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `discoverInProgress` accessed in synchronized context
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java
  private synchronized void triggerDiscover(String discoveredEvent) {

    if (discoverInProgress) {
      log.info("Discovery is already in progress");
      return;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `discoverInProgress` accessed in synchronized context
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java
      return;
    } else {
      discoverInProgress = true;
    }
    // Run this code asynchronous so it doesn't block processing of the tasks
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pulsarClient` accessed in synchronized context
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
    public synchronized PulsarClient getPulsarClient() throws IOException {
        // Do lazy initialization of client
        if (pulsarClient == null) {
            if (localCluster == null) {
                // If not explicitly set, read clusters data from ZK
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pulsarClient` accessed in synchronized context
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
                localCluster = retrieveClusterData();
            }
            pulsarClient = createClientInstance(localCluster);
        }
        return pulsarClient;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pulsarClient` accessed in synchronized context
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
            pulsarClient = createClientInstance(localCluster);
        }
        return pulsarClient;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        synchronized (activeCursors) {
            if (activeCursors.get(cursor.getName()) == null) {
                Position positionForOrdering = config.isCacheEvictionByMarkDeletedPosition()
                        ? cursor.getMarkDeletedPosition()
                        : cursor.getReadPosition();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        }

        if (state == State.Terminated) {
            // When recovering a terminated managed ledger, we don't need to create
            // a new ledger for writing, since no more writes are allowed.
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        mbean.startDataLedgerCreateOp();

        asyncCreateLedger(bookKeeper, config, digestType, (rc, lh, ctx) -> {

            if (checkAndCompleteLedgerOpTask(rc, lh, ctx)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            if (State.CreatingLedger == state) {
                long elapsedMs = System.currentTimeMillis() - this.lastLedgerCreationInitiationTimestamp;
                if (elapsedMs > TimeUnit.SECONDS.toMillis(2 * config.getMetadataOperationsTimeoutSeconds())) {
                    log.info("[{}] Ledger creation was initiated {} ms ago but it never completed and creation timeout"
                        + " task didn't kick in as well. Force to fail the create ledger operation.", name, elapsedMs);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                this.lastLedgerCreationInitiationTimestamp = System.currentTimeMillis();
                mbean.startDataLedgerCreateOp();
                asyncCreateLedger(bookKeeper, config, digestType, this, Collections.emptyMap());
            }
        } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    @Override
    public synchronized void asyncTerminate(TerminateCallback callback, Object ctx) {
        if (state.isFenced()) {
            callback.terminateFailed(new ManagedLedgerFencedException(), ctx);
            return;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            callback.terminateFailed(new ManagedLedgerFencedException(), ctx);
            return;
        } else if (state == State.Terminated) {
            if (log.isDebugEnabled()) {
                log.debug("[{}] Ignoring request to terminate an already terminated managed ledger", name);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lastConfirmedEntry` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                log.debug("[{}] Ignoring request to terminate an already terminated managed ledger", name);
            }
            callback.terminateComplete(lastConfirmedEntry, ctx);
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

        log.info("[{}] Terminating managed ledger", name);
        state = State.Terminated;

        LedgerHandle lh = currentLedger;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lastConfirmedEntry` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            }

            PositionImpl currentLastConfirmedEntry = lastConfirmedEntry;
            // Update metadata
            for (LedgerInfo ls : ledgersToDelete) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                newInfoBuilder.getOffloadContextBuilder().setBookkeeperDeleted(true);
                String driverName = OffloadUtils.getOffloadDriverName(ls,
                        config.getLedgerOffloader().getOffloadDriverName());
                Map<String, String> driverMetadata = OffloadUtils.getOffloadDriverMetadata(ls,
                        config.getLedgerOffloader().getOffloadDriverMetadata());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                        config.getLedgerOffloader().getOffloadDriverName());
                Map<String, String> driverMetadata = OffloadUtils.getOffloadDriverMetadata(ls,
                        config.getLedgerOffloader().getOffloadDriverMetadata());
                OffloadUtils.setOffloadDriverMetadata(newInfoBuilder, driverName, driverMetadata);
                ledgers.put(ls.getLedgerId(), newInfoBuilder.build());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

        // Fetch the list of existing ledgers in the managed ledger
        store.getManagedLedgerInfo(name, config.isCreateIfMissing(), config.getProperties(),
                new MetaStoreCallback<ManagedLedgerInfo>() {
            @Override
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

        // Fetch the list of existing ledgers in the managed ledger
        store.getManagedLedgerInfo(name, config.isCreateIfMissing(), config.getProperties(),
                new MetaStoreCallback<ManagedLedgerInfo>() {
            @Override
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `config` accessed in synchronized context
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            log.debug("[{}] Creating new cursor: {}", name, cursorName);
        }
        final ManagedCursorImpl cursor = new ManagedCursorImpl(bookKeeper, config, this, cursorName);
        CompletableFuture<ManagedCursor> cursorFuture = new CompletableFuture<>();
        uninitializedCursors.put(cursorName, cursorFuture);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            String transformFunctionFile = null;
            int parallelism;
            if (functionConfig != null) {
                FunctionConfigUtils.inferMissingArguments(functionConfig, true);
                parallelism = functionConfig.getParallelism();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            int parallelism;
            if (functionConfig != null) {
                FunctionConfigUtils.inferMissingArguments(functionConfig, true);
                parallelism = functionConfig.getParallelism();
                if (functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            if (functionConfig != null) {
                FunctionConfigUtils.inferMissingArguments(functionConfig, true);
                parallelism = functionConfig.getParallelism();
                if (functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA) {
                    userCodeFile = functionConfig.getJar();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                FunctionConfigUtils.inferMissingArguments(functionConfig, true);
                parallelism = functionConfig.getParallelism();
                if (functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA) {
                    userCodeFile = functionConfig.getJar();
                    userCodeClassLoader = extractClassLoader(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                parallelism = functionConfig.getParallelism();
                if (functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA) {
                    userCodeFile = functionConfig.getJar();
                    userCodeClassLoader = extractClassLoader(
                        userCodeFile, ComponentType.FUNCTION, functionConfig.getClassName());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    userCodeFile = functionConfig.getJar();
                    userCodeClassLoader = extractClassLoader(
                        userCodeFile, ComponentType.FUNCTION, functionConfig.getClassName());
                    functionDetails = FunctionConfigUtils.convert(
                        functionConfig,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                        userCodeFile, ComponentType.FUNCTION, functionConfig.getClassName());
                    functionDetails = FunctionConfigUtils.convert(
                        functionConfig,
                        FunctionConfigUtils.validateJavaFunction(functionConfig, getCurrentOrUserCodeClassLoader()));
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.GO) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    functionDetails = FunctionConfigUtils.convert(
                        functionConfig,
                        FunctionConfigUtils.validateJavaFunction(functionConfig, getCurrentOrUserCodeClassLoader()));
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.GO) {
                    userCodeFile = functionConfig.getGo();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                        functionConfig,
                        FunctionConfigUtils.validateJavaFunction(functionConfig, getCurrentOrUserCodeClassLoader()));
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.GO) {
                    userCodeFile = functionConfig.getGo();
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.PYTHON) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                        FunctionConfigUtils.validateJavaFunction(functionConfig, getCurrentOrUserCodeClassLoader()));
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.GO) {
                    userCodeFile = functionConfig.getGo();
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.PYTHON) {
                    userCodeFile = functionConfig.getPy();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.GO) {
                    userCodeFile = functionConfig.getGo();
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.PYTHON) {
                    userCodeFile = functionConfig.getPy();
                } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    userCodeFile = functionConfig.getGo();
                } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.PYTHON) {
                    userCodeFile = functionConfig.getPy();
                } else {
                    throw new UnsupportedOperationException();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java

                if (functionDetails == null) {
                    functionDetails = FunctionConfigUtils.convert(functionConfig, getCurrentOrUserCodeClassLoader());
                }
            } else if (sourceConfig != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sourceConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    functionDetails = FunctionConfigUtils.convert(functionConfig, getCurrentOrUserCodeClassLoader());
                }
            } else if (sourceConfig != null) {
                inferMissingArguments(sourceConfig);
                userCodeFile = sourceConfig.getArchive();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sourceConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                }
            } else if (sourceConfig != null) {
                inferMissingArguments(sourceConfig);
                userCodeFile = sourceConfig.getArchive();
                parallelism = sourceConfig.getParallelism();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sourceConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            } else if (sourceConfig != null) {
                inferMissingArguments(sourceConfig);
                userCodeFile = sourceConfig.getArchive();
                parallelism = sourceConfig.getParallelism();
                userCodeClassLoader = extractClassLoader(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sourceConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                inferMissingArguments(sourceConfig);
                userCodeFile = sourceConfig.getArchive();
                parallelism = sourceConfig.getParallelism();
                userCodeClassLoader = extractClassLoader(
                    userCodeFile, ComponentType.SOURCE, sourceConfig.getClassName());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sourceConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                parallelism = sourceConfig.getParallelism();
                userCodeClassLoader = extractClassLoader(
                    userCodeFile, ComponentType.SOURCE, sourceConfig.getClassName());
                functionDetails = SourceConfigUtils.convert(
                    sourceConfig,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sourceConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    userCodeFile, ComponentType.SOURCE, sourceConfig.getClassName());
                functionDetails = SourceConfigUtils.convert(
                    sourceConfig,
                    SourceConfigUtils.validateAndExtractDetails(sourceConfig, getCurrentOrUserCodeClassLoader(), true));
            } else if (sinkConfig != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sourceConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                functionDetails = SourceConfigUtils.convert(
                    sourceConfig,
                    SourceConfigUtils.validateAndExtractDetails(sourceConfig, getCurrentOrUserCodeClassLoader(), true));
            } else if (sinkConfig != null) {
                inferMissingArguments(sinkConfig);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    sourceConfig,
                    SourceConfigUtils.validateAndExtractDetails(sourceConfig, getCurrentOrUserCodeClassLoader(), true));
            } else if (sinkConfig != null) {
                inferMissingArguments(sinkConfig);
                userCodeFile = sinkConfig.getArchive();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    SourceConfigUtils.validateAndExtractDetails(sourceConfig, getCurrentOrUserCodeClassLoader(), true));
            } else if (sinkConfig != null) {
                inferMissingArguments(sinkConfig);
                userCodeFile = sinkConfig.getArchive();
                transformFunctionFile = sinkConfig.getTransformFunction();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            } else if (sinkConfig != null) {
                inferMissingArguments(sinkConfig);
                userCodeFile = sinkConfig.getArchive();
                transformFunctionFile = sinkConfig.getTransformFunction();
                parallelism = sinkConfig.getParallelism();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                inferMissingArguments(sinkConfig);
                userCodeFile = sinkConfig.getArchive();
                transformFunctionFile = sinkConfig.getTransformFunction();
                parallelism = sinkConfig.getParallelism();
                userCodeClassLoader = extractClassLoader(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                userCodeFile = sinkConfig.getArchive();
                transformFunctionFile = sinkConfig.getTransformFunction();
                parallelism = sinkConfig.getParallelism();
                userCodeClassLoader = extractClassLoader(
                    userCodeFile, ComponentType.SINK, sinkConfig.getClassName());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                parallelism = sinkConfig.getParallelism();
                userCodeClassLoader = extractClassLoader(
                    userCodeFile, ComponentType.SINK, sinkConfig.getClassName());
                if (isNotEmpty(sinkConfig.getTransformFunction())) {
                    transformFunctionCodeClassLoader = extractClassLoader(
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                userCodeClassLoader = extractClassLoader(
                    userCodeFile, ComponentType.SINK, sinkConfig.getClassName());
                if (isNotEmpty(sinkConfig.getTransformFunction())) {
                    transformFunctionCodeClassLoader = extractClassLoader(
                        sinkConfig.getTransformFunction(),
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                if (isNotEmpty(sinkConfig.getTransformFunction())) {
                    transformFunctionCodeClassLoader = extractClassLoader(
                        sinkConfig.getTransformFunction(),
                        ComponentType.FUNCTION,
                        sinkConfig.getTransformFunctionClassName());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                        sinkConfig.getTransformFunction(),
                        ComponentType.FUNCTION,
                        sinkConfig.getTransformFunctionClassName());
                }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java

                functionDetails = SinkConfigUtils.convert(
                    sinkConfig,
                    SinkConfigUtils.validateAndExtractDetails(sinkConfig, getCurrentOrUserCodeClassLoader(),
                        functionClassLoader, true));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                functionDetails = SinkConfigUtils.convert(
                    sinkConfig,
                    SinkConfigUtils.validateAndExtractDetails(sinkConfig, getCurrentOrUserCodeClassLoader(),
                        functionClassLoader, true));
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `clientAuthPlugin` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java

            AuthenticationConfig authConfig = AuthenticationConfig.builder().clientAuthenticationPlugin
                    (clientAuthPlugin)
                    .clientAuthenticationParameters(clientAuthParams).useTls(useTls)
                    .tlsAllowInsecureConnection(tlsAllowInsecureConnection)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `clientAuthParams` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            AuthenticationConfig authConfig = AuthenticationConfig.builder().clientAuthenticationPlugin
                    (clientAuthPlugin)
                    .clientAuthenticationParameters(clientAuthParams).useTls(useTls)
                    .tlsAllowInsecureConnection(tlsAllowInsecureConnection)
                    .tlsHostnameVerificationEnable(tlsHostNameVerificationEnabled)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `useTls` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            AuthenticationConfig authConfig = AuthenticationConfig.builder().clientAuthenticationPlugin
                    (clientAuthPlugin)
                    .clientAuthenticationParameters(clientAuthParams).useTls(useTls)
                    .tlsAllowInsecureConnection(tlsAllowInsecureConnection)
                    .tlsHostnameVerificationEnable(tlsHostNameVerificationEnabled)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsAllowInsecureConnection` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    (clientAuthPlugin)
                    .clientAuthenticationParameters(clientAuthParams).useTls(useTls)
                    .tlsAllowInsecureConnection(tlsAllowInsecureConnection)
                    .tlsHostnameVerificationEnable(tlsHostNameVerificationEnabled)
                    .tlsTrustCertsFilePath(tlsTrustCertFilePath).build();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsHostNameVerificationEnabled` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    .clientAuthenticationParameters(clientAuthParams).useTls(useTls)
                    .tlsAllowInsecureConnection(tlsAllowInsecureConnection)
                    .tlsHostnameVerificationEnable(tlsHostNameVerificationEnabled)
                    .tlsTrustCertsFilePath(tlsTrustCertFilePath).build();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tlsTrustCertFilePath` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    .tlsAllowInsecureConnection(tlsAllowInsecureConnection)
                    .tlsHostnameVerificationEnable(tlsHostNameVerificationEnabled)
                    .tlsTrustCertsFilePath(tlsTrustCertFilePath).build();

            String serviceUrl = DEFAULT_SERVICE_URL;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `brokerServiceUrl` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java

            String serviceUrl = DEFAULT_SERVICE_URL;
            if (brokerServiceUrl != null) {
                serviceUrl = brokerServiceUrl;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `brokerServiceUrl` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            String serviceUrl = DEFAULT_SERVICE_URL;
            if (brokerServiceUrl != null) {
                serviceUrl = brokerServiceUrl;
            }
            if (webServiceUrl == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `webServiceUrl` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                serviceUrl = brokerServiceUrl;
            }
            if (webServiceUrl == null) {
                webServiceUrl = DEFAULT_WEB_SERVICE_URL;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `webServiceUrl` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            }
            if (webServiceUrl == null) {
                webServiceUrl = DEFAULT_WEB_SERVICE_URL;
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sourceConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            }

            if ((sourceConfig != null || sinkConfig != null
                    || functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA)
                    && (runtimeEnv == null || runtimeEnv == RuntimeEnv.THREAD)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sinkConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            }

            if ((sourceConfig != null || sinkConfig != null
                    || functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA)
                    && (runtimeEnv == null || runtimeEnv == RuntimeEnv.THREAD)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `functionConfig` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java

            if ((sourceConfig != null || sinkConfig != null
                    || functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA)
                    && (runtimeEnv == null || runtimeEnv == RuntimeEnv.THREAD)) {
                // By default run java functions as threads
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `runtimeEnv` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            if ((sourceConfig != null || sinkConfig != null
                    || functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA)
                    && (runtimeEnv == null || runtimeEnv == RuntimeEnv.THREAD)) {
                // By default run java functions as threads
                startThreadedMode(functionDetails, parallelism, instanceIdOffset, serviceUrl,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `runtimeEnv` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            if ((sourceConfig != null || sinkConfig != null
                    || functionConfig.getRuntime() == FunctionConfig.Runtime.JAVA)
                    && (runtimeEnv == null || runtimeEnv == RuntimeEnv.THREAD)) {
                // By default run java functions as threads
                startThreadedMode(functionDetails, parallelism, instanceIdOffset, serviceUrl,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instanceIdOffset` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                    && (runtimeEnv == null || runtimeEnv == RuntimeEnv.THREAD)) {
                // By default run java functions as threads
                startThreadedMode(functionDetails, parallelism, instanceIdOffset, serviceUrl,
                        stateStorageServiceUrl, authConfig, userCodeFile, transformFunctionFile);
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stateStorageServiceUrl` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                // By default run java functions as threads
                startThreadedMode(functionDetails, parallelism, instanceIdOffset, serviceUrl,
                        stateStorageServiceUrl, authConfig, userCodeFile, transformFunctionFile);
            } else {
                startProcessMode(functionDetails, parallelism, instanceIdOffset, serviceUrl,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instanceIdOffset` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                        stateStorageServiceUrl, authConfig, userCodeFile, transformFunctionFile);
            } else {
                startProcessMode(functionDetails, parallelism, instanceIdOffset, serviceUrl,
                        stateStorageServiceUrl, authConfig, userCodeFile, transformFunctionFile);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stateStorageServiceUrl` accessed in synchronized context
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            } else {
                startProcessMode(functionDetails, parallelism, instanceIdOffset, serviceUrl,
                        stateStorageServiceUrl, authConfig, userCodeFile, transformFunctionFile);
            }
            local.addAll(spawners);
```

## RuleId[ruleID=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    public ManagedLedgerFactoryImpl(MetadataStoreExtended metadataStore, ClientConfiguration bkClientConfiguration,
                                    ManagedLedgerFactoryConfig config)
            throws Exception {
```

### RedundantSuppression
Redundant suppression
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java

    @SuppressWarnings({ "unchecked", "rawtypes" })
    void asyncReadEntry0(ReadHandle lh, long firstEntry, long lastEntry, boolean shouldCacheEntry,
            final ReadEntriesCallback callback, Object ctx) {
        asyncReadEntry0WithLimits(lh, firstEntry, lastEntry, shouldCacheEntry, callback, ctx, null);
```

### RedundantSuppression
Redundant suppression
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/BookKeeperClientFactoryImpl.java`
#### Snippet
```java
@SuppressWarnings("deprecation")
@Slf4j
public class BookKeeperClientFactoryImpl implements BookKeeperClientFactory {

    @Override
```

### RedundantSuppression
Redundant suppression
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    protected CompletableFuture<Void> internalDeleteNamespaceAsync(boolean force) {
        CompletableFuture<Policies> preconditionCheck = precheckWhenDeleteNamespace(namespaceName, force);
        return preconditionCheck
```

### RedundantSuppression
Redundant suppression
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    protected CompletableFuture<DispatchRate> internalGetTopicDispatchRateAsync() {
        return validateNamespacePolicyOperationAsync(namespaceName, PolicyName.RATE, PolicyOperation.READ)
                .thenCompose(__ -> getNamespacePoliciesAsync(namespaceName))
```

### RedundantSuppression
Redundant suppression
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ResourceQuotasBase.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    protected CompletableFuture<ResourceQuota> internalGetNamespaceBundleResourceQuota(String bundleRange) {
        return getNamespaceBundleRangeAsync(bundleRange, false)
                .thenCompose(nsBundle -> pulsar().getBrokerService().getBundlesQuotas().getResourceQuota(nsBundle));
```

### RedundantSuppression
Redundant suppression
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ResourceQuotasBase.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    protected CompletableFuture<Void> internalSetNamespaceBundleResourceQuota(String bundleRange, ResourceQuota quota) {
        return getNamespaceBundleRangeAsync(bundleRange, true)
                .thenCompose(nsBundle ->
```

### RedundantSuppression
Redundant suppression
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ResourceQuotasBase.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    protected CompletableFuture<Void> internalRemoveNamespaceBundleResourceQuota(String bundleRange) {
        return getNamespaceBundleRangeAsync(bundleRange, true)
                .thenCompose(nsBundle ->
```

### RedundantSuppression
Redundant suppression
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    private CompletableFuture<PartitionedTopicMetadata> createDefaultPartitionedTopicAsync(TopicName topicName,
                                                                                        Optional<Policies> policies) {
        final int defaultNumPartitions = pulsar.getBrokerService().getDefaultNumPartitions(topicName, policies);
```

### RedundantSuppression
Redundant suppression
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/MBeanStatsGenerator.java`
#### Snippet
```java
        List<Metrics> metricsCollection = new ArrayList<Metrics>();

        @SuppressWarnings("unchecked")
        Set<ObjectInstance> instances = mbs.queryMBeans(null, null);

        for (ObjectInstance instance : instances) {
```

### RedundantSuppression
Redundant suppression
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ReaderBuilderImpl.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public ReaderBuilder<T> clone() {
        return new ReaderBuilderImpl<>(client, conf.clone(), schema);
    }
```

### RedundantSuppression
Redundant suppression
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/ClustersImpl.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public Set<String> getPeerClusterNames(String cluster) throws PulsarAdminException {
        return sync(() -> getPeerClusterNamesAsync(cluster));
    }
```

### RedundantSuppression
Redundant suppression
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/utils/DefaultImplementation.java`
#### Snippet
```java
@SuppressWarnings("unchecked")
@UtilityClass
public class DefaultImplementation {
    private static final Class<PulsarAdminBuilder> ADMIN_CLIENT_BUILDER_IMPL = ReflectionUtils.newClassInstance(
            "org.apache.pulsar.client.admin.internal.PulsarAdminBuilderImpl");
```

### RedundantSuppression
Redundant suppression
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @VisibleForTesting
    Crypto initializeCrypto() throws ClassNotFoundException {
        if (pulsarSinkConfig.getProducerConfig() == null
                || pulsarSinkConfig.getProducerConfig().getCryptoConfig() == null
```

### RedundantSuppression
Redundant suppression
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/MultiConsumerPulsarSource.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private Map<String, PulsarSourceConsumerConfig<T>> setupConsumerConfigs() throws ClassNotFoundException {
        Map<String, PulsarSourceConsumerConfig<T>> configs = new TreeMap<>();

```

### RedundantSuppression
Redundant suppression
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/TopicSchema.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private static <T> Schema<T> newSchemaInstance(Class<T> clazz, SchemaType type) {
        return newSchemaInstance(clazz, type, new ConsumerConfig());
    }
```

### RedundantSuppression
Redundant suppression
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/TopicSchema.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private <T> Schema<T> newSchemaInstance(String topic, Class<T> clazz, String schemaTypeOrClassName, boolean input,
                                            ClassLoader classLoader) {
        return newSchemaInstance(topic, clazz, new ConsumerConfig(schemaTypeOrClassName), input, classLoader);
```

### RedundantSuppression
Redundant suppression
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/TopicSchema.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private <T> Schema<T> newSchemaInstance(String topic, Class<T> clazz, String schemaTypeOrClassName, boolean input) {
        return newSchemaInstance(topic, clazz, new ConsumerConfig(schemaTypeOrClassName), input,
                Thread.currentThread().getContextClassLoader());
```

### RedundantSuppression
Redundant suppression
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/TopicSchema.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private <T> Schema<T> newSchemaInstance(String topic, Class<T> clazz, ConsumerConfig conf, boolean input) {
        return newSchemaInstance(topic, clazz, conf, input, Thread.currentThread().getContextClassLoader());
    }
```

## RuleId[ruleID=UtilityClassWithPublicConstructor]
### UtilityClassWithPublicConstructor
Class `JavaInstanceMain` has only 'static' members, and a 'public' constructor
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
 *
 */
public class JavaInstanceMain {

    private static final String FUNCTIONS_INSTANCE_CLASSPATH = "pulsar.functions.instance.classpath";
```

## RuleId[ruleID=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `pulsar-io/cassandra/src/main/java/org/apache/pulsar/io/cassandra/CassandraAbstractSink.java`
#### Snippet
```java
    private void createClient(String roots) {
        String[] hosts = roots.split(",");
        if (hosts.length <= 0) {
            throw new RuntimeException("Invalid cassandra roots");
        }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `pulsar-io/aerospike/src/main/java/org/apache/pulsar/io/aerospike/AerospikeAbstractSink.java`
#### Snippet
```java
    private void createClient(NioEventLoops eventLoops) {
        String[] hosts = aerospikeSinkConfig.getSeedHosts().split(",");
        if (hosts.length <= 0) {
            throw new RuntimeException("Invalid Seed Hosts");
        }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java
                indexName = indexName.substring(0, indexName.length() - 1);
            }
            if (indexName.length() <= 0 || !indexName.matches("[a-zA-Z\\.0-9][a-zA-Z_\\.\\-\\+0-9]*")) {
                throw new RuntimeException(new IOException("Cannot convert the topic name='"
                        + topicName + "' to a valid elasticsearch index name"));
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
        String[] hierarchy = functionDetails.getClassName().split("\\.");
        String fileName;
        if (hierarchy.length <= 0) {
            fileName = functionDetails.getClassName();
        } else if (hierarchy.length == 1) {
```

## RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileRecord.java`
#### Snippet
```java
    public static final String FILE_MODIFIED_TIME = "file.modified.time";

    private final Optional<String> key;
    private final byte[] value;
    private final HashMap<String, String> userProperties = new HashMap<String, String> ();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/tcp/NettyTCPServerHandler.java`
#### Snippet
```java
    @Data
    private static class NettyTCPRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/udp/NettyUDPServerHandler.java`
#### Snippet
```java
    @Data
    private static class NettyUDPRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/http/NettyHttpServerHandler.java`
#### Snippet
```java
    @Data
    private static class NettyHttpRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSource.java`
#### Snippet
```java
    @Data
    private static class DocRecord implements Record<byte[]> {
        private final Optional<String> key;
        private final byte[] value;
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisRecord.java`
#### Snippet
```java

    private static final CharsetDecoder decoder = StandardCharsets.UTF_8.newDecoder();
    private final Optional<String> key;
    private final byte[] value;
    private final HashMap<String, String> userProperties = new HashMap<>();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/StreamsRecord.java`
#### Snippet
```java

    private static final CharsetDecoder decoder = StandardCharsets.UTF_8.newDecoder();
    private final Optional<String> key;
    private final byte[] value;
    private final Map<String, String> properties = new HashMap<> ();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSource.java`
#### Snippet
```java
    @Data
    private static class RabbitMQRecord implements Record<byte[]> {
        private final Optional<String> key;
        private final byte[] value;
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'destinationTopic'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
        Optional<String> partitionId;
        @Getter
        Optional<String> destinationTopic;
        @Getter
        Optional<Integer> partitionIndex;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'partitionId'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
        Optional<Long> eventTime;
        @Getter
        Optional<String> partitionId;
        @Getter
        Optional<String> destinationTopic;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'partitionIndex'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
        Optional<String> destinationTopic;
        @Getter
        Optional<Integer> partitionIndex;

        KafkaSchemaWrappedSchema keySchema;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'eventTime'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
        Optional<String> topicName;
        @Getter
        Optional<Long> eventTime;
        @Getter
        Optional<String> partitionId;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'topicName'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
        T value;
        @Getter
        Optional<String> topicName;
        @Getter
        Optional<Long> eventTime;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'RECORD_SEQUENCE'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java

    private static final Map<String, String> PROPERTIES = Collections.emptyMap();
    private static final Optional<Long> RECORD_SEQUENCE = Optional.empty();

    public abstract class AbstractKafkaSourceRecord<T> implements Record {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'key'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
    public abstract class AbstractKafkaSourceRecord<T> implements Record {
        @Getter
        Optional<String> key;
        @Getter
        T value;
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'ensemblePlacementPolicyClass'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/BookKeeperClientFactory.java`
#### Snippet
```java

    BookKeeper create(ServiceConfiguration conf, MetadataStoreExtended store, EventLoopGroup eventLoopGroup,
                      Optional<Class<? extends EnsemblePlacementPolicy>> ensemblePlacementPolicyClass,
                      Map<String, Object> ensemblePlacementPolicyProperties,
                      StatsLogger statsLogger) throws IOException;
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'ensemblePlacementPolicyClass'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/BookKeeperClientFactory.java`
#### Snippet
```java
public interface BookKeeperClientFactory {
    BookKeeper create(ServiceConfiguration conf, MetadataStoreExtended store, EventLoopGroup eventLoopGroup,
                      Optional<Class<? extends EnsemblePlacementPolicy>> ensemblePlacementPolicyClass,
                      Map<String, Object> ensemblePlacementPolicyProperties) throws IOException;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'webServicePortTls'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "The port for serving https requests"
    )
    private Optional<Integer> webServicePortTls = Optional.empty();

    @FieldContext(
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'webServicePort'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "The port for serving http requests"
    )
    private Optional<Integer> webServicePort = Optional.ofNullable(8080);
    @FieldContext(
        category = CATEGORY_SERVER,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'servicePortTls'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "The port for serving tls secured binary protobuf request"
    )
    private Optional<Integer> servicePortTls = Optional.empty();

    @FieldContext(
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'proxyLogLevel'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                    + " 2: Parse and log channel info, command info and message body"
    )
    private Optional<Integer> proxyLogLevel = Optional.ofNullable(0);

    @FieldContext(
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'servicePort'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "The port for serving binary protobuf request"
    )
    private Optional<Integer> servicePort = Optional.ofNullable(6650);
    @FieldContext(
        category = CATEGORY_SERVER,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'compactorMXBean'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/TopicStats.java`
#### Snippet
```java

    public static void printTopicStats(PrometheusMetricStreams stream, TopicStats stats,
                                       Optional<CompactorMXBean> compactorMXBean, String cluster, String namespace,
                                       String topic, boolean splitTopicAndPartitionIndexLabel) {
        writeMetric(stream, "pulsar_subscriptions_count", stats.subscriptionsCount,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'compactorMXBean'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/NamespaceStatsAggregator.java`
#### Snippet
```java
    private static void getTopicStats(Topic topic, TopicStats stats, boolean includeConsumerMetrics,
                                      boolean includeProducerMetrics, boolean getPreciseBacklog,
                                      boolean subscriptionBacklogSize, Optional<CompactorMXBean> compactorMXBean) {
        stats.reset();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionWorkerService'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    }

    public PulsarService(ServiceConfiguration config, Optional<WorkerService> functionWorkerService,
                         Consumer<Integer> processTerminator) {
        this(config, new WorkerConfig(), functionWorkerService, processTerminator);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'packagesManagement'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java

    // packages management service
    private Optional<PackagesManagement> packagesManagement = Optional.empty();
    private PulsarPrometheusMetricsServlet metricsServlet;
    private List<PrometheusRawMetricsProvider> pendingMetricsProviders;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionWorkerService'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    public PulsarService(ServiceConfiguration config,
                         WorkerConfig workerConfig,
                         Optional<WorkerService> functionWorkerService,
                         Consumer<Integer> processTerminator) {
        state = State.Init;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'functionWorkerService'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private SchemaRegistryService schemaRegistryService = null;
    private final WorkerConfig workerConfig;
    private final Optional<WorkerService> functionWorkerService;
    private ProtocolHandlers protocolHandlers = null;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'position'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Subscription.java`
#### Snippet
```java
    CompletableFuture<Void> endTxn(long txnidMostBits, long txnidLeastBits, int txnAction, long lowWaterMark);

    CompletableFuture<AnalyzeBacklogResult> analyzeBacklog(Optional<Position> position);

    default int getNumberOfSameAddressConsumers(final String clientAddress) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'topicEpoch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
            boolean userProvidedProducerName,
            ProducerAccessMode accessMode,
            Optional<Long> topicEpoch,
            boolean supportsPartialProducer) {
        final ServiceConfiguration serviceConf =  cnx.getBrokerService().pulsar().getConfiguration();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'topicEpoch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java

    private final ProducerAccessMode accessMode;
    private Optional<Long> topicEpoch;

    private final Map<String, String> metadata;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'clusterUrl'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
    }

    public void topicMigrated(Optional<ClusterUrl> clusterUrl) {
        if (clusterUrl.isPresent()) {
            ClusterUrl url = clusterUrl.get();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'topicEpoch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarCommandSender.java`
#### Snippet
```java

    void sendProducerSuccessResponse(long requestId, String producerName, long lastSequenceId,
                                     SchemaVersion schemaVersion, Optional<Long> topicEpoch,
                                     boolean isProducerReady);

```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for field 'subscriptionProperties'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SubscriptionOption.java`
#### Snippet
```java
    private boolean replicatedSubscriptionStateArg;
    private KeySharedMeta keySharedMeta;
    private Optional<Map<String, String>> subscriptionProperties;
    private long consumerEpoch;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'clusterUrl'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
    }

    public void topicMigrated(Optional<ClusterUrl> clusterUrl) {
        if (clusterUrl.isPresent()) {
            ClusterUrl url = clusterUrl.get();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'optMetadataArray'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java
     *   EntryBatchIndexesAcks, ManagedCursor, boolean, Consumer)
     */
    public int filterEntriesForConsumer(Optional<MessageMetadata[]> optMetadataArray, int startOffset,
             List<? extends Entry> entries, EntryBatchSizes batchSizes, SendMessageInfo sendMessageInfo,
             EntryBatchIndexesAcks indexesAcks, ManagedCursor cursor, boolean isReplayRead, Consumer consumer) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'optPolicies'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    }

    public void updateResourceGroupLimiter(Optional<Policies> optPolicies) {
        Policies policies;
        try {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'topicEpoch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    protected volatile long publishRateLimitedTimes = 0;

    protected volatile Optional<Long> topicEpoch = Optional.empty();
    private volatile boolean hasExclusiveProducer;
    // pointer to the exclusive producer
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'currentEpoch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    protected abstract CompletableFuture<Long> setTopicEpoch(long newEpoch);

    protected abstract CompletableFuture<Long> incrementTopicEpoch(Optional<Long> currentEpoch);

    @Override
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'policies'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/SubscribeRateLimiter.java`
#### Snippet
```java
    }

    public static SubscribeRate getPoliciesSubscribeRate(final String cluster, final Optional<Policies> policies,
            String topicName) {
        // return policy-subscribe rate only if it's enabled in policies
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'dispatchRateLimiter'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    protected final PersistentTopic topic;
    protected final String name;
    private Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();

    protected volatile boolean havePendingRead = false;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'position'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

    protected void internalAnalyzeSubscriptionBacklog(AsyncResponse asyncResponse, String subName,
                                                      Optional<Position> position,
                                                      boolean authoritative) {
        CompletableFuture<Void> future;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'position'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
    private void internalAnalyzeSubscriptionBacklogForNonPartitionedTopic(AsyncResponse asyncResponse,
                                                                          String subName,
                                                                          Optional<Position> position,
                                                                          boolean authoritative) {
        validateTopicOwnershipAsync(topicName, authoritative)
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'bundle'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
    }

    protected CompletableFuture<List<String>> internalGetListAsync(Optional<String> bundle) {
        return validateNamespaceOperationAsync(namespaceName, NamespaceOperation.GET_TOPICS)
            .thenCompose(__ -> namespaceResources().namespaceExistsAsync(namespaceName))
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'bundle'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
    private static final Version LEAST_SUPPORTED_CLIENT_VERSION_PREFIX = Version.forIntegers(1, 21);

    protected List<String> internalGetList(Optional<String> bundle) {
        validateNamespaceOperation(namespaceName, NamespaceOperation.GET_TOPICS);

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'dispatchRateLimiter'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
    protected final ManagedCursor cursor;

    protected Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();
    private final Object dispatchRateLimiterLock = new Object();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'topic'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java

    private static void logAuthException(SocketAddress remoteAddress, String operation,
                                         String principal, Optional<TopicName> topic, Throwable ex) {
        String topicString = topic.map(t -> ", topic=" + t.toString()).orElse("");
        if (ex instanceof AuthenticationException) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'namespaceName'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java

    private static void logNamespaceNameAuthException(SocketAddress remoteAddress, String operation,
                                         String principal, Optional<NamespaceName> namespaceName, Throwable ex) {
        String namespaceNameString = namespaceName.map(t -> ", namespace=" + t.toString()).orElse("");
        if (ex instanceof AuthenticationException) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'delayedDeliveryTracker'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    protected final RedeliveryTracker redeliveryTracker;

    private Optional<DelayedDeliveryTracker> delayedDeliveryTracker = Optional.empty();

    protected volatile boolean havePendingRead = false;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'dispatchRateLimiter'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            AtomicIntegerFieldUpdater.newUpdater(PersistentDispatcherMultipleConsumers.class,
                    "blockedDispatcherOnUnackedMsgs");
    protected Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();
    private AtomicBoolean isRescheduleReadInProgress = new AtomicBoolean(false);
    protected final ExecutorService dispatchMessagesThread;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'topicEpoch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                             boolean userProvidedProducerName, TopicName topicName,
                             ProducerAccessMode producerAccessMode,
                             Optional<Long> topicEpoch, boolean supportsPartialProducer,
                             CompletableFuture<Producer> producerFuture){
        CompletableFuture<Void> producerQueuedFuture = new CompletableFuture<>();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'overrideBrokerNicSpeedGbps'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LinuxBrokerHostUsageImpl.java`
#### Snippet
```java
    private OperatingSystemMXBean systemBean;
    private SystemResourceUsage usage;
    private final Optional<Double> overrideBrokerNicSpeedGbps;
    private final boolean isCGroupsEnabled;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'overrideBrokerNicSpeedGbps'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LinuxBrokerHostUsageImpl.java`
#### Snippet
```java

    public LinuxBrokerHostUsageImpl(int hostUsageCheckIntervalMin,
                                    Optional<Double> overrideBrokerNicSpeedGbps,
                                    ScheduledExecutorService executorService) {
        this.systemBean = (OperatingSystemMXBean) ManagementFactory.getOperatingSystemMXBean();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'replicatedSubscriptionsController'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            (MessageIdImpl) MessageId.earliest);

    private volatile Optional<ReplicatedSubscriptionsController> replicatedSubscriptionsController = Optional.empty();

    private static final FastThreadLocal<TopicStatsHelper> threadLocalTopicStats =
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'dispatchRateLimiter'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    public boolean msgChunkPublished;

    private Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();
    private final Object dispatchRateLimiterLock = new Object();
    private Optional<SubscribeRateLimiter> subscribeRateLimiter = Optional.empty();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'subscribeRateLimiter'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    private Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();
    private final Object dispatchRateLimiterLock = new Object();
    private Optional<SubscribeRateLimiter> subscribeRateLimiter = Optional.empty();
    private final long backloggedCursorThresholdEntries;
    public static final int MESSAGE_RATE_BACKOFF_MS = 1000;
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'localPolicies'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
    }

    private NamespaceBundles getBundles(NamespaceName nsname, Optional<Pair<LocalPolicies, Long>> localPolicies) {
        return new NamespaceBundles(nsname, this, localPolicies);
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'policies'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    }

    public int getDefaultNumPartitions(final TopicName topicName, final Optional<Policies> policies) {
        AutoTopicCreationOverride autoTopicCreationOverride = getAutoTopicCreationOverride(topicName, policies);
        if (autoTopicCreationOverride != null) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'policies'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    }

    public boolean isDefaultTopicTypePartitioned(final TopicName topicName, final Optional<Policies> policies) {
        AutoTopicCreationOverride autoTopicCreationOverride = getAutoTopicCreationOverride(topicName, policies);
        if (autoTopicCreationOverride != null) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'clusterDataOp'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    }

    public PulsarAdmin getClusterPulsarAdmin(String cluster, Optional<ClusterData> clusterDataOp) {
        PulsarAdmin admin = clusterAdmins.get(cluster);
        if (admin != null) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'policies'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    @SuppressWarnings("deprecation")
    private CompletableFuture<PartitionedTopicMetadata> createDefaultPartitionedTopicAsync(TopicName topicName,
                                                                                        Optional<Policies> policies) {
        final int defaultNumPartitions = pulsar.getBrokerService().getDefaultNumPartitions(topicName, policies);
        final int maxPartitions = pulsar().getConfig().getMaxNumPartitionsPerPartitionedTopic();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'clusterDataOp'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    }

    public PulsarClient getReplicationClient(String cluster, Optional<ClusterData> clusterDataOp) {
        PulsarClient client = replicationClients.get(cluster);
        if (client != null) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'policies'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

    private AutoTopicCreationOverride getAutoTopicCreationOverride(final TopicName topicName,
                                                                   Optional<Policies> policies) {
        // If namespace policies have the field set, it will override the broker-level setting
        if (policies.isPresent() && policies.get().autoTopicCreationOverride != null) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'policies'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

    private CompletableFuture<Boolean> isAllowAutoTopicCreationAsync(final TopicName topicName,
                                                                     final Optional<Policies> policies) {
        if (policies.isPresent() && policies.get().deleted) {
            log.info("Preventing AutoTopicCreation on a namespace that is being deleted {}",
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'localPolicies'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java

    NamespaceBundles(NamespaceName nsname, NamespaceBundleFactory factory,
                     Optional<Pair<LocalPolicies, Long>> localPolicies, Collection<Long> partitions) {
        this(nsname, factory, localPolicies, getPartitions(partitions));
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'localPolicies'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java

    NamespaceBundles(NamespaceName nsname, NamespaceBundleFactory factory,
                     Optional<Pair<LocalPolicies, Long>> localPolicies, long[] partitions) {
        // check input arguments
        this.nsname = Objects.requireNonNull(nsname);
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'localPolicies'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java

    public NamespaceBundles(NamespaceName nsname, NamespaceBundleFactory factory,
                            Optional<Pair<LocalPolicies, Long>> localPolicies) {
        this(nsname, factory, localPolicies, getPartitions(localPolicies.map(Pair::getLeft)));
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for field 'localPolicies'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java
    private final NamespaceBundle fullBundle;

    private final Optional<Pair<LocalPolicies, Long>> localPolicies;

    public NamespaceBundles(NamespaceName nsname, NamespaceBundleFactory factory,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'lp'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java
    }

    private static long[] getPartitions(Optional<LocalPolicies> lp) {
        return getPartitions(lp.map(x -> x.bundles).orElse(null));
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'firstMessageId'
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/TwoPhaseCompactor.java`
#### Snippet
```java

    private void phaseOneLoop(RawReader reader,
                              Optional<MessageId> firstMessageId,
                              Optional<MessageId> toMessageId,
                              MessageId lastMessageId,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'toMessageId'
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/TwoPhaseCompactor.java`
#### Snippet
```java
    private void phaseOneLoop(RawReader reader,
                              Optional<MessageId> firstMessageId,
                              Optional<MessageId> toMessageId,
                              MessageId lastMessageId,
                              Map<String, MessageId> latestForKey,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'topicEpoch'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerResponse.java`
#### Snippet
```java
    private byte[] schemaVersion;

    private Optional<Long> topicEpoch;

    // Shadow the default getter generated by lombok. In broker, if the schema version is an empty byte array, it means
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'overrideProducerName'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
    }

    private ProducerImpl<T> createProducer(final int partitionIndex, final Optional<String> overrideProducerName) {
        return producers.computeIfAbsent(partitionIndex, (idx) -> {
            String partitionName = TopicName.get(topic).getPartition(idx).toString();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'overrideProducerName'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                                                  ProducerInterceptors interceptors,
                                                  CompletableFuture<Producer<T>> producerCreatedFuture,
                                                  Optional<String> overrideProducerName) {
        return new ProducerImpl<>(PulsarClientImpl.this, topic, conf, producerCreatedFuture, partitionIndex, schema,
                interceptors, overrideProducerName);
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'addressResolver'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConnectionPool.java`
#### Snippet
```java
    public ConnectionPool(ClientConfigurationData conf, EventLoopGroup eventLoopGroup,
                          Supplier<ClientCnx> clientCnxSupplier,
                          Optional<AddressResolver<InetSocketAddress>> addressResolver)
            throws PulsarClientException {
        this.eventLoopGroup = eventLoopGroup;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java

    MessageImpl(String topic, BatchMessageIdImpl batchMessageIdImpl, MessageMetadata batchMetadata,
            SingleMessageMetadata singleMessageMetadata, ByteBuf payload, Optional<EncryptionContext> encryptionCtx,
            ClientCnx cnx, Schema<T> schema, int redeliveryCount,
                boolean keepMessageInDirectMemory, long consumerEpoch) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
    private SchemaInfo schemaInfoForReplicator;
    private SchemaState schemaState = SchemaState.None;
    private Optional<EncryptionContext> encryptionCtx = Optional.empty();

    private String topic; // only set for incoming messages
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java

    MessageImpl(String topic, BatchMessageIdImpl batchMessageIdImpl, MessageMetadata msgMetadata,
            SingleMessageMetadata singleMessageMetadata, ByteBuf payload, Optional<EncryptionContext> encryptionCtx,
            ClientCnx cnx, Schema<T> schema, long consumerEpoch) {
        this(topic, batchMessageIdImpl, msgMetadata, singleMessageMetadata, payload, encryptionCtx, cnx, schema, 0,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java

    static <T> void init(MessageImpl<T> msg, String topic, MessageIdImpl messageId, MessageMetadata msgMetadata,
            ByteBuf payload, Optional<EncryptionContext> encryptionCtx, ClientCnx cnx, Schema<T> schema,
            int redeliveryCount, boolean poolMessage, long consumerEpoch) {
        init(msg, topic, null /* batchMessageIdImpl */, msgMetadata, null /* singleMessageMetadata */, payload,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java

    public static <T> MessageImpl<T> create(String topic, MessageIdImpl messageId, MessageMetadata msgMetadata,
            ByteBuf payload, Optional<EncryptionContext> encryptionCtx, ClientCnx cnx, Schema<T> schema,
            int redeliveryCount, boolean pooledMessage, long consumerEpoch) {
        if (pooledMessage) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
    private static <T> void init(MessageImpl<T> msg, String topic, BatchMessageIdImpl batchMessageIdImpl,
            MessageMetadata msgMetadata, SingleMessageMetadata singleMessageMetadata, ByteBuf payload,
            Optional<EncryptionContext> encryptionCtx, ClientCnx cnx, Schema<T> schema, int redeliveryCount,
            boolean poolMessage, long consumerEpoch) {
        msg.msgMetadata.clear();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
    public static <T> MessageImpl<T> create(String topic, BatchMessageIdImpl batchMessageIdImpl,
            MessageMetadata batchMetadata, SingleMessageMetadata singleMessageMetadata, ByteBuf payload,
            Optional<EncryptionContext> encryptionCtx, ClientCnx cnx, Schema<T> schema, int redeliveryCount,
            boolean pooledMessage, long consumerEpoch) {
        if (pooledMessage) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java

    MessageImpl(String topic, MessageIdImpl messageId, MessageMetadata msgMetadata, ByteBuf payload,
                Optional<EncryptionContext> encryptionCtx, ClientCnx cnx, Schema<T> schema, int redeliveryCount,
                boolean pooledMessage, long consumerEpoch) {
        this.msgMetadata = new MessageMetadata();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'encryptionCtx'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java

    MessageImpl(String topic, MessageIdImpl messageId, MessageMetadata msgMetadata, ByteBuf payload,
                Optional<EncryptionContext> encryptionCtx, ClientCnx cnx, Schema<T> schema) {
        this(topic, messageId, msgMetadata, payload, encryptionCtx, cnx, schema, 0, false, DEFAULT_CONSUMER_EPOCH);
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'overrideProducerName'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    public ProducerImpl(PulsarClientImpl client, String topic, ProducerConfigurationData conf,
                        CompletableFuture<Producer<T>> producerCreatedFuture, int partitionIndex, Schema<T> schema,
                        ProducerInterceptors interceptors, Optional<String> overrideProducerName) {
        super(client, topic, conf, producerCreatedFuture, schema, interceptors);
        this.producerId = client.newProducerId();
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'schemaVersion'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private final Map<String, String> metadata;

    private Optional<byte[]> schemaVersion = Optional.empty();

    private final ConnectionHandler connectionHandler;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'semaphore'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java

    private final OpSendMsgQueue pendingMessages;
    private final Optional<Semaphore> semaphore;
    private volatile Timeout sendTimeout = null;
    private final long lookupDeadline;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'topicEpoch'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private long lastBatchSendNanoTime;

    private Optional<Long> topicEpoch = Optional.empty();
    private final List<Throwable> previousExceptions = new CopyOnWriteArrayList<Throwable>();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'restrictToCluster'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Markers.java`
#### Snippet
```java
            };

    private static ByteBuf newMessage(MarkerType markerType, Optional<String> restrictToCluster, ByteBuf payload) {
        MessageMetadata msgMetadata = LOCAL_MESSAGE_METADATA.get()
                .clear()
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'failedReadPosition'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/AsyncCallbacks.java`
#### Snippet
```java
        void findEntryComplete(Position position, Object ctx);

        void findEntryFailed(ManagedLedgerException exception, Optional<Position> failedReadPosition, Object ctx);
    }

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'failedReadPosition'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/AsyncCallbacks.java`
#### Snippet
```java
        void scanComplete(Position position, ScanOutcome scanOutcome, Object ctx);

        void scanFailed(ManagedLedgerException exception, Optional<Position> failedReadPosition, Object ctx);
    }

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'startingPosition'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedCursor.java`
#### Snippet
```java
     * @throws ManagedLedgerException
     */
    default CompletableFuture<ScanOutcome> scan(Optional<Position> startingPosition,
                                                Predicate<Entry> condition,
                                                int batchSize, long maxEntries, long timeOutMs) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'expectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStore.java`
#### Snippet
```java
     * @return a future to track the async request
     */
    CompletableFuture<Stat> put(String path, byte[] value, Optional<Long> expectedVersion);

    /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'expectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStore.java`
#### Snippet
```java
     * @return a future to track the async request
     */
    CompletableFuture<Void> delete(String path, Optional<Long> expectedVersion);

    /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'expectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/extended/MetadataStoreExtended.java`
#### Snippet
```java
     * @return a future to track the async request
     */
    CompletableFuture<Stat> put(String path, byte[] value, Optional<Long> expectedVersion,
            EnumSet<CreateOption> options);

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'topicEpoch'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java

    public static ByteBuf newProducerSuccess(long requestId, String producerName, long lastSequenceId,
            SchemaVersion schemaVersion, Optional<Long> topicEpoch, boolean isProducerReady) {
        return serializeWithSize(newProducerSuccessCommand(requestId, producerName, lastSequenceId, schemaVersion,
                topicEpoch, isProducerReady));
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'version'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
    }

    public static ByteBuf newGetSchema(long requestId, String topic, Optional<SchemaVersion> version) {
        BaseCommand cmd = localCmd(Type.GET_SCHEMA);
        CommandGetSchema schema = cmd.setGetSchema()
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'topicEpoch'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java

    public static BaseCommand newProducerSuccessCommand(long requestId, String producerName, long lastSequenceId,
            SchemaVersion schemaVersion, Optional<Long> topicEpoch, boolean isProducerReady) {
        BaseCommand cmd = localCmd(Type.PRODUCER_SUCCESS);
        CommandProducerSuccess ps = cmd.setProducerSuccess()
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'topicEpoch'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
                                      boolean encrypted, Map<String, String> metadata, SchemaInfo schemaInfo,
                                      long epoch, boolean userProvidedProducerName,
                                      ProducerAccessMode accessMode, Optional<Long> topicEpoch, boolean isTxnEnabled) {
        return newProducer(topic, producerId, requestId, producerName, encrypted, metadata, schemaInfo, epoch,
                userProvidedProducerName, accessMode, topicEpoch, isTxnEnabled, null);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'topicEpoch'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
                                      boolean encrypted, Map<String, String> metadata, SchemaInfo schemaInfo,
                                      long epoch, boolean userProvidedProducerName,
                                      ProducerAccessMode accessMode, Optional<Long> topicEpoch, boolean isTxnEnabled,
                                      String initialSubscriptionName) {
        BaseCommand cmd = localCmd(Type.PRODUCER);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'optExpectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/batching/OpDelete.java`
#### Snippet
```java
public class OpDelete implements MetadataOp {
    private final String path;
    private final Optional<Long> optExpectedVersion;
    public final long created = System.currentTimeMillis();

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'optExpectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/batching/OpPut.java`
#### Snippet
```java
    private final String path;
    private final byte[] data;
    private final Optional<Long> optExpectedVersion;
    private final EnumSet<CreateOption> options;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'expectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
    }

    protected abstract CompletableFuture<Void> storeDelete(String path, Optional<Long> expectedVersion);

    @Override
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'expectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
    }

    private CompletableFuture<Void> deleteInternal(String path, Optional<Long> expectedVersion) {
        // Ensure caches are invalidated before the operation is confirmed
        return storeDelete(path, expectedVersion).thenRun(() -> {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'optExpectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
    }

    protected abstract CompletableFuture<Stat> storePut(String path, byte[] data, Optional<Long> optExpectedVersion,
                                                        EnumSet<CreateOption> options);

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'optExpectedVersion'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java

    }
    public final CompletableFuture<Stat> putInternal(String path, byte[] data, Optional<Long> optExpectedVersion,
            Set<CreateOption> options) {
        // Ensure caches are invalidated before the operation is confirmed
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'synchronizer'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
    }

    protected void registerSyncLister(Optional<MetadataEventSynchronizer> synchronizer) {
        if (synchronizer.isPresent()) {
            synchronizer.get().registerSyncListener(event -> {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'proposedValue'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
    private LeaderElectionState leaderElectionState;
    private Optional<Long> version = Optional.empty();
    private Optional<T> proposedValue;

    private final ScheduledExecutorService executor;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'version'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java

    private LeaderElectionState leaderElectionState;
    private Optional<Long> version = Optional.empty();
    private Optional<T> proposedValue;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionAuthenticationSpec'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/FunctionAuthUtils.java`
#### Snippet
```java

    public static FunctionAuthData getFunctionAuthData(
            Optional<Function.FunctionAuthenticationSpec> functionAuthenticationSpec) {
        return functionAuthenticationSpec
                .map(authenticationSpec -> FunctionAuthData.builder().data(authenticationSpec.getData().toByteArray())
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionAuthData'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/FunctionAuthProvider.java`
#### Snippet
```java
     * @throws Exception
     */
    void cleanUpAuthData(Function.FunctionDetails funcDetails, Optional<FunctionAuthData> functionAuthData)
            throws Exception;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'existingFunctionAuthData'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/FunctionAuthProvider.java`
#### Snippet
```java

    Optional<FunctionAuthData> updateAuthData(Function.FunctionDetails funcDetails,
                                              Optional<FunctionAuthData> existingFunctionAuthData,
                                              AuthenticationDataSource authenticationDataSource) throws Exception;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionAuthData'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/FunctionAuthProvider.java`
#### Snippet
```java
     * @param functionAuthData function authentication data that is provider specific
     */
    void configureAuthenticationConfig(AuthenticationConfig authConfig, Optional<FunctionAuthData> functionAuthData);

    /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionAuthData'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/KubernetesFunctionAuthProvider.java`
#### Snippet
```java
     * @param functionAuthData function auth data
     */
    void configureAuthDataStatefulSet(V1StatefulSet statefulSet, Optional<FunctionAuthData> functionAuthData);

    static KubernetesFunctionAuthProvider getAuthProvider(String className) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'authProvider'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeFactory.java`
#### Snippet
```java
                    ConnectorsManager connectorsManager,
                    FunctionsManager functionsManager,
                    Optional<FunctionAuthProvider> authProvider,
                    Optional<RuntimeCustomizer> runtimeCustomizer) throws Exception;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'runtimeCustomizer'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeFactory.java`
#### Snippet
```java
                    FunctionsManager functionsManager,
                    Optional<FunctionAuthProvider> authProvider,
                    Optional<RuntimeCustomizer> runtimeCustomizer) throws Exception;

    /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'functionsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntimeFactory.java`
#### Snippet
```java
    private ClassLoader rootClassLoader;
    private Optional<ConnectorsManager> connectorsManager;
    private Optional<FunctionsManager> functionsManager;

    /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'connectorsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntimeFactory.java`
#### Snippet
```java
    private SecretsProviderConfigurator secretsProviderConfigurator;
    private ClassLoader rootClassLoader;
    private Optional<ConnectorsManager> connectorsManager;
    private Optional<FunctionsManager> functionsManager;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'memoryLimit'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntimeFactory.java`
#### Snippet
```java
    }

    private void initialize(String threadGroupName, Optional<ThreadRuntimeFactoryConfig.MemoryLimit> memoryLimit,
                            String pulsarServiceUrl, AuthenticationConfig authConfig, String stateStorageImplClass,
                            String storageServiceUrl,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'connectorsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntimeFactory.java`
#### Snippet
```java
                            FunctionCollectorRegistry collectorRegistry, String narExtractionDirectory,
                            ClassLoader rootClassLoader, boolean exposePulsarAdminClientEnabled,
                            String pulsarWebServiceUrl, Optional<ConnectorsManager> connectorsManager,
                            Optional<FunctionsManager> functionsManager)
            throws PulsarClientException {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntimeFactory.java`
#### Snippet
```java
                            ClassLoader rootClassLoader, boolean exposePulsarAdminClientEnabled,
                            String pulsarWebServiceUrl, Optional<ConnectorsManager> connectorsManager,
                            Optional<FunctionsManager> functionsManager)
            throws PulsarClientException {

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'memoryLimit'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntimeFactory.java`
#### Snippet
```java
    }

    private Optional<Long> calculateClientMemoryLimit(Optional<ThreadRuntimeFactoryConfig.MemoryLimit> memoryLimit) {
        if (memoryLimit.isPresent()) {

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'functionsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
    private final String narExtractionDirectory;
    private final Optional<ConnectorsManager> connectorsManager;
    private final Optional<FunctionsManager> functionsManager;

    ThreadRuntime(InstanceConfig instanceConfig,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'connectorsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
                  FunctionCollectorRegistry collectorRegistry,
                  String narExtractionDirectory,
                  Optional<ConnectorsManager> connectorsManager,
                  Optional<FunctionsManager> functionsManager) {
        this.instanceConfig = instanceConfig;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
                  String narExtractionDirectory,
                  Optional<ConnectorsManager> connectorsManager,
                  Optional<FunctionsManager> functionsManager) {
        this.instanceConfig = instanceConfig;
        if (instanceConfig.getFunctionDetails().getRuntime() != Function.FunctionDetails.Runtime.JAVA) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'connectorsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
    private final FunctionCollectorRegistry collectorRegistry;
    private final String narExtractionDirectory;
    private final Optional<ConnectorsManager> connectorsManager;
    private final Optional<FunctionsManager> functionsManager;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'connectorsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
                                                      String narExtractionDirectory,
                                                      FunctionCacheManager fnCache,
                                                      Optional<ConnectorsManager> connectorsManager,
                                                      Optional<FunctionsManager> functionsManager,
                                                      Function.FunctionDetails.ComponentType componentType)
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionsManager'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
                                                      FunctionCacheManager fnCache,
                                                      Optional<ConnectorsManager> connectorsManager,
                                                      Optional<FunctionsManager> functionsManager,
                                                      Function.FunctionDetails.ComponentType componentType)
            throws Exception {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionAuthProvider'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntimeFactory.java`
#### Snippet
```java
                                 SecretsProviderConfigurator secretsProviderConfigurator,
                                 boolean authenticationEnabled,
                                 Optional<FunctionAuthProvider> functionAuthProvider,
                                 Optional<RuntimeCustomizer> runtimeCustomizer) {

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'runtimeCustomizer'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntimeFactory.java`
#### Snippet
```java
                                 boolean authenticationEnabled,
                                 Optional<FunctionAuthProvider> functionAuthProvider,
                                 Optional<RuntimeCustomizer> runtimeCustomizer) {

        initialize(pulsarServiceUrl, pulsarWebServiceUrl, stateStorageServiceUrl, authConfig, javaInstanceJarFile,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionAuthProvider'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntimeFactory.java`
#### Snippet
```java
                            SecretsProviderConfigurator secretsProviderConfigurator,
                            boolean authenticationEnabled,
                            Optional<FunctionAuthProvider> functionAuthProvider,
                            Optional<RuntimeCustomizer> runtimeCustomizer) {
        this.pulsarServiceUrl = pulsarServiceUrl;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'runtimeCustomizer'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntimeFactory.java`
#### Snippet
```java
                            boolean authenticationEnabled,
                            Optional<FunctionAuthProvider> functionAuthProvider,
                            Optional<RuntimeCustomizer> runtimeCustomizer) {
        this.pulsarServiceUrl = pulsarServiceUrl;
        this.pulsarWebServiceUrl = pulsarWebServiceUrl;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'authProvider'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntimeFactory.java`
#### Snippet
```java
    @ToString.Exclude
    @EqualsAndHashCode.Exclude
    private Optional<FunctionAuthProvider> authProvider;
    @ToString.Exclude
    @EqualsAndHashCode.Exclude
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'runtimeCustomizer'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntimeFactory.java`
#### Snippet
```java
    @ToString.Exclude
    @EqualsAndHashCode.Exclude
    private Optional<RuntimeCustomizer> runtimeCustomizer;

    @VisibleForTesting
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'schemaType'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/TopicSchema.java`
#### Snippet
```java
    }

    public Schema<?> getSchema(String topic, Class<?> clazz, Optional<SchemaType> schemaType) {
        return cachedSchemas.computeIfAbsent(topic, key -> {
            // If schema type was not provided, try to get it from schema registry, or fallback to default types
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'functionAuthDataCacheProvider'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    private double memoryOverCommitRatio;
    private int gracePeriodSeconds;
    private final Optional<KubernetesFunctionAuthProvider> functionAuthDataCacheProvider;
    private final AuthenticationConfig authConfig;
    private Integer grpcPort;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'functionAuthDataCacheProvider'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                      double memoryOverCommitRatio,
                      int gracePeriodSeconds,
                      Optional<KubernetesFunctionAuthProvider> functionAuthDataCacheProvider,
                      boolean authenticationEnabled,
                      Integer grpcPort,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'manifestCustomizer'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                      Integer grpcPort,
                      String narExtractionDirectory,
                      Optional<KubernetesManifestCustomizer> manifestCustomizer,
                      String functionInstanceClassPath,
                      String downloadDirectory) throws Exception {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'manifestCustomizer'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    private Integer metricsPort;
    private String narExtractionDirectory;
    private final Optional<KubernetesManifestCustomizer> manifestCustomizer;
    private String functionInstanceClassPath;
    private String downloadDirectory;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'manifestCustomizer'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntimeFactory.java`
#### Snippet
```java
    @ToString.Exclude
    @EqualsAndHashCode.Exclude
    private Optional<KubernetesManifestCustomizer> manifestCustomizer;

    @ToString.Exclude
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'authProvider'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntimeFactory.java`
#### Snippet
```java
    @ToString.Exclude
    @EqualsAndHashCode.Exclude
    private Optional<KubernetesFunctionAuthProvider> authProvider;
    @ToString.Exclude
    @EqualsAndHashCode.Exclude
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'memoryLimit'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceUtils.java`
#### Snippet
```java
    public static PulsarClient createPulsarClient(String pulsarServiceUrl,
                                                  AuthenticationConfig authConfig,
                                                  Optional<Long> memoryLimit) throws PulsarClientException {
        return createPulsarClientBuilder(pulsarServiceUrl, authConfig, memoryLimit).build();
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'memoryLimit'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceUtils.java`
#### Snippet
```java
    public static ClientBuilder createPulsarClientBuilder(String pulsarServiceUrl,
                                                          AuthenticationConfig authConfig,
                                                          Optional<Long> memoryLimit) throws PulsarClientException {
        ClientBuilder clientBuilder = null;
        if (isNotBlank(pulsarServiceUrl)) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'cryptoKeyReader'
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
    private ServiceConfiguration config;
    @Getter
    private Optional<CryptoKeyReader> cryptoKeyReader = Optional.empty();

    private ClusterData localCluster;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'firstError'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    private void offloadLoop(CompletableFuture<PositionImpl> promise, Queue<LedgerInfo> ledgersToOffload,
            PositionImpl firstUnoffloaded, Optional<Throwable> firstError) {
        State currentState = getState();
        if (currentState == State.Closed) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'offloadPolicies'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    }

    boolean isOffloadedNeedsDelete(OffloadContext offload, Optional<OffloadPolicies> offloadPolicies) {
        long elapsedMs = clock.millis() - offload.getTimestamp();
        return offloadPolicies.filter(policies -> offload.getComplete() && !offload.getBookkeeperDeleted()
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'webServicePort'
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java

    @FieldContext(doc = "Port to use to server HTTP request")
    private Optional<Integer> webServicePort = Optional.of(8080);

    @FieldContext(doc = "Port to use to server HTTPS request")
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'webServicePortTls'
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java

    @FieldContext(doc = "Port to use to server HTTPS request")
    private Optional<Integer> webServicePortTls = Optional.empty();

    @FieldContext(doc = "Hostname or IP address the service binds on, default is 0.0.0.0.")
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'batchSize'
in `pulsar-client-api/src/main/java/org/apache/pulsar/common/api/EncryptionContext.java`
#### Snippet
```java
    private CompressionType compressionType;
    private int uncompressedMessageSize;
    private Optional<Integer> batchSize;

    /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'port'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfigurationUtils.java`
#### Snippet
```java
    }

    private static URI createUriOrNull(String scheme, String hostname, Optional<Integer> port) {
        return port.map(p -> URI.create(String.format("%s://%s:%d", scheme, hostname, p))).orElse(null);
    }
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'configurationMetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/PulsarResources.java`
#### Snippet
```java
    private final Optional<MetadataStore> localMetadataStore;
    @Getter
    private final Optional<MetadataStore> configurationMetadataStore;

    public PulsarResources(MetadataStore localMetadataStore, MetadataStore configurationMetadataStore) {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'localMetadataStore'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/PulsarResources.java`
#### Snippet
```java
    private final TopicResources topicResources;
    @Getter
    private final Optional<MetadataStore> localMetadataStore;
    @Getter
    private final Optional<MetadataStore> configurationMetadataStore;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'version'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/LocalPoliciesResources.java`
#### Snippet
```java

    public CompletableFuture<Void> setLocalPoliciesWithVersion(NamespaceName ns, LocalPolicies policies,
                                                               Optional<Long> version) {
        try {
            byte[] content = ObjectMapperFactory.getThreadLocal().writeValueAsBytes(policies);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'expiryTime'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/utils/AuthTokenUtils.java`
#### Snippet
```java
    }

    public static String createToken(Key signingKey, String subject, Optional<Date> expiryTime) {
        JwtBuilder builder = Jwts.builder()
                .setSubject(subject)
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'startPosition'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Topics.java`
#### Snippet
```java
     */
    AnalyzeSubscriptionBacklogResult analyzeSubscriptionBacklog(String topic, String subscriptionName,
                                                                Optional<MessageId> startPosition)
            throws PulsarAdminException;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'startPosition'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Topics.java`
#### Snippet
```java
    CompletableFuture<AnalyzeSubscriptionBacklogResult> analyzeSubscriptionBacklogAsync(String topic,
                                                                           String subscriptionName,
                                                                           Optional<MessageId> startPosition);

    /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'loadBalancerOverrideBrokerNicSpeedGbps'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Option to override the auto-detected network interfaces max speed"
    )
    private Optional<Double> loadBalancerOverrideBrokerNicSpeedGbps = Optional.empty();

    @FieldContext(
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'webServicePort'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "The port for serving http requests"
    )
    private Optional<Integer> webServicePort = Optional.of(8080);
    @FieldContext(
        category = CATEGORY_SERVER,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'webServicePortTls'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "The port for serving https requests"
    )
    private Optional<Integer> webServicePortTls = Optional.empty();

    @FieldContext(
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'brokerServicePortTls'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " If set, defines a server binding for bindAddress:brokerServicePortTls."
    )
    private Optional<Integer> brokerServicePortTls = Optional.empty();
    @FieldContext(
        category = CATEGORY_SERVER,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'brokerServicePort'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    )

    private Optional<Integer> brokerServicePort = Optional.of(6650);
    @FieldContext(
        category = CATEGORY_SERVER,
```

## RuleId[ruleID=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `buildtools/src/main/java/org/apache/pulsar/tests/AnnotationListener.java`
#### Snippet
```java

    public AnnotationListener() {
        System.out.println("Created annotation listener");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `buildtools/src/main/java/org/apache/pulsar/tests/PulsarTestListener.java`
#### Snippet
```java
    @Override
    public void onTestSkipped(ITestResult result) {
        System.out.format("~~~~~~~~~ SKIPPED -- %s.%s(%s)-------\n", result.getTestClass(),
                result.getMethod().getMethodName(), Arrays.toString(result.getParameters()));
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `buildtools/src/main/java/org/apache/pulsar/tests/PulsarTestListener.java`
#### Snippet
```java
    @Override
    public void onTestStart(ITestResult result) {
        System.out.format("------- Starting test %s.%s(%s)-------\n", result.getTestClass(),
                result.getMethod().getMethodName(), Arrays.toString(result.getParameters()));
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `buildtools/src/main/java/org/apache/pulsar/tests/PulsarTestListener.java`
#### Snippet
```java
    public void onTestFailure(ITestResult result) {
        if (!(result.getThrowable() instanceof SkipException)) {
            System.out.format("!!!!!!!!! FAILURE-- %s.%s(%s)-------\n", result.getTestClass(),
                    result.getMethod().getMethodName(), Arrays.toString(result.getParameters()));
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `buildtools/src/main/java/org/apache/pulsar/tests/PulsarTestListener.java`
#### Snippet
```java
            result.getThrowable().printStackTrace();
            if (result.getThrowable() instanceof ThreadTimeoutException) {
                System.out.println("====== THREAD DUMPS ======");
                System.out.println(ThreadDumpUtil.buildThreadDiagnosticString());
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `buildtools/src/main/java/org/apache/pulsar/tests/PulsarTestListener.java`
#### Snippet
```java
            if (result.getThrowable() instanceof ThreadTimeoutException) {
                System.out.println("====== THREAD DUMPS ======");
                System.out.println(ThreadDumpUtil.buildThreadDiagnosticString());
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `buildtools/src/main/java/org/apache/pulsar/tests/PulsarTestListener.java`
#### Snippet
```java
    @Override
    public void onTestSuccess(ITestResult result) {
        System.out.format("------- SUCCESS -- %s.%s(%s)-------\n", result.getTestClass(),
                result.getMethod().getMethodName(), Arrays.toString(result.getParameters()));
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyServiceStarter.java`
#### Snippet
```java
                FixedDateFormat.FixedFormat.ISO8601_OFFSET_DATE_TIME_HHMM.getPattern());
            Thread.setDefaultUncaughtExceptionHandler((thread, exception) -> {
                System.out.printf("%s [%s] error Uncaught exception in thread %s: %s%n", dateFormat.format(new Date()),
                        thread.getContextClassLoader(), thread.getName(), exception.getMessage());
                exception.printStackTrace(System.out);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyServiceStarter.java`
#### Snippet
```java
                System.out.printf("%s [%s] error Uncaught exception in thread %s: %s%n", dateFormat.format(new Date()),
                        thread.getContextClassLoader(), thread.getName(), exception.getMessage());
                exception.printStackTrace(System.out);
            });

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarTransactionCoordinatorMetadataSetup.java`
#### Snippet
```java

        if (arguments.configurationStore == null) {
            System.err.println("Configuration store address argument is required (--configuration-store)");
            jcommander.usage();
            System.exit(1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarTransactionCoordinatorMetadataSetup.java`
#### Snippet
```java

        if (arguments.numTransactionCoordinators <= 0) {
            System.err.println("Number of transaction coordinators must greater than 0");
            System.exit(1);
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarTransactionCoordinatorMetadataSetup.java`
#### Snippet
```java
        }

        System.out.println("Transaction coordinator metadata setup success");
    }
}
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarInitialNamespaceSetup.java`
#### Snippet
```java

        if (arguments.configurationStore == null) {
            System.err.println("Configuration store address argument is required (--configuration-store)");
            jcommander.usage();
            return 1;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarInitialNamespaceSetup.java`
#### Snippet
```java
                    namespaceName = NamespaceName.get(namespace);
                } catch (Exception e) {
                    System.out.println("Invalid namespace name.");
                    return 1;
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarInitialNamespaceSetup.java`
#### Snippet
```java
        }

        System.out.println("Initial namespace setup success");
        return 0;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarVersionStarter.java`
#### Snippet
```java
            return;
        }
        System.out.println("Current version of pulsar is: " + PulsarVersion.getVersion());
        System.out.println("Git Revision " + PulsarVersion.getGitSha());
        System.out.println("Git Branch " + PulsarVersion.getGitBranch());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarVersionStarter.java`
#### Snippet
```java
        }
        System.out.println("Current version of pulsar is: " + PulsarVersion.getVersion());
        System.out.println("Git Revision " + PulsarVersion.getGitSha());
        System.out.println("Git Branch " + PulsarVersion.getGitBranch());
        System.out.println("Built by " + PulsarVersion.getBuildUser() + " on " + PulsarVersion.getBuildHost() + " at "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarVersionStarter.java`
#### Snippet
```java
        System.out.println("Current version of pulsar is: " + PulsarVersion.getVersion());
        System.out.println("Git Revision " + PulsarVersion.getGitSha());
        System.out.println("Git Branch " + PulsarVersion.getGitBranch());
        System.out.println("Built by " + PulsarVersion.getBuildUser() + " on " + PulsarVersion.getBuildHost() + " at "
                + PulsarVersion.getBuildTime());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarVersionStarter.java`
#### Snippet
```java
        System.out.println("Git Revision " + PulsarVersion.getGitSha());
        System.out.println("Git Branch " + PulsarVersion.getGitBranch());
        System.out.println("Built by " + PulsarVersion.getBuildUser() + " on " + PulsarVersion.getBuildHost() + " at "
                + PulsarVersion.getBuildTime());
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java

        if (arguments.metadataStoreUrl == null && arguments.zookeeper == null) {
            System.err.println("Metadata store address argument is required (--metadata-store)");
            jcommander.usage();
            System.exit(1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        if (arguments.configurationMetadataStore == null && arguments.configurationStore == null
                && arguments.globalZookeeper == null) {
            System.err.println(
                    "Configuration metadata store address argument is required (--configuration-metadata-store)");
            jcommander.usage();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        if (arguments.configurationMetadataStore != null && (arguments.configurationStore != null
                || arguments.globalZookeeper != null)) {
            System.err.println("Configuration metadata store argument (--configuration-metadata-store) "
                    + "supersedes the deprecated (--global-zookeeper and --configuration-store) argument");
            jcommander.usage();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java

        if (arguments.numTransactionCoordinators <= 0) {
            System.err.println("Number of transaction coordinators must greater than 0");
            System.exit(1);
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
            initializeCluster(arguments, bundleNumberForDefaultNamespace);
        } catch (Exception e) {
            System.err.println("Unexpected error occured.");
            e.printStackTrace(System.err);
            System.err.println("Terminating JVM...");
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        } catch (Exception e) {
            System.err.println("Unexpected error occured.");
            e.printStackTrace(System.err);
            System.err.println("Terminating JVM...");
            ShutdownUtil.triggerImmediateForcefulShutdown();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
            System.err.println("Unexpected error occured.");
            e.printStackTrace(System.err);
            System.err.println("Terminating JVM...");
            ShutdownUtil.triggerImmediateForcefulShutdown();
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
            FixedDateFormat.FixedFormat.ISO8601_OFFSET_DATE_TIME_HHMM.getPattern());
        Thread.setDefaultUncaughtExceptionHandler((thread, exception) -> {
            System.out.println(String.format("%s [%s] error Uncaught exception in thread %s: %s",
                    dateFormat.format(new Date()), thread.getContextClassLoader(),
                    thread.getName(), exception.getMessage()));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
                    dateFormat.format(new Date()), thread.getContextClassLoader(),
                    thread.getName(), exception.getMessage()));
            exception.printStackTrace(System.out);
        });

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
                Files.write(Paths.get(outputFile), encoded);
            } else {
                System.out.write(encoded);
            }
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            System.err.println(e);
            String chosenCommand = jcommander.getParsedCommand();
            usageFormatter.usage(chosenCommand);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
            genDocCmd.run(null);
        } else {
            System.err.println("Invalid command: " + cmd);
            System.exit(1);
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
        public void run() throws Exception {
            if (secretKey == null && publicKey == null) {
                System.err.println(
                        "Either --secret-key or --public-key needs to be passed for signing a token");
                System.exit(1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
                System.exit(1);
            } else if (secretKey != null && publicKey != null) {
                System.err.println(
                        "Only one of --secret-key and --public-key needs to be passed for signing a token");
                System.exit(1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
                token = System.getenv("TOKEN");
            } else {
                System.err.println(
                        "Token needs to be either passed as an argument or through `--stdin`,"
                                + " `--token-file` or by the `TOKEN` environment variable");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
                    .parse(token);

            System.out.println(jwt.getBody());
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
                token = System.getenv("TOKEN");
            } else {
                System.err.println(
                        "Token needs to be either passed as an argument or through `--stdin`,"
                                + " `--token-file` or by the `TOKEN` environment variable");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java

            String[] parts = token.split("\\.");
            System.out.println(new String(Decoders.BASE64URL.decode(parts[0])));
            System.out.println("---");
            System.out.println(new String(Decoders.BASE64URL.decode(parts[1])));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
            String[] parts = token.split("\\.");
            System.out.println(new String(Decoders.BASE64URL.decode(parts[0])));
            System.out.println("---");
            System.out.println(new String(Decoders.BASE64URL.decode(parts[1])));
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
            System.out.println(new String(Decoders.BASE64URL.decode(parts[0])));
            System.out.println("---");
            System.out.println(new String(Decoders.BASE64URL.decode(parts[1])));
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
        public void run() throws Exception {
            if (secretKey == null && privateKey == null) {
                System.err.println(
                        "Either --secret-key or --private-key needs to be passed for signing a token");
                System.exit(1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
                System.exit(1);
            } else if (secretKey != null && privateKey != null) {
                System.err.println(
                        "Only one of --secret-key and --private-key needs to be passed for signing a token");
                System.exit(1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java

            String token = AuthTokenUtils.createToken(signingKey, subject, optExpiryTime);
            System.out.println(token);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/tools/BrokerTool.java`
#### Snippet
```java
            .withDescription(NAME + " is used for operations on a specific broker")
            .withFlags(new CliFlags())
            .withConsole(System.out)
            .addCommand(new LoadReportCommand())
            .addCommand(new GenerateDocsCommand());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/KerberosName.java`
#### Snippet
```java
        int i = 0;
        for (Rule r: rules) {
            System.out.println(++i + " " + r);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/KerberosName.java`
#### Snippet
```java
        for (String arg: args) {
            KerberosName name = new KerberosName(arg);
            System.out.println("Name: " + name + " to " + name.getShortName());
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/BaseGenerateDocumentation.java`
#### Snippet
```java
            jcommander.parse(Arrays.copyOfRange(args, 0, args.length));
        } catch (Exception e) {
            System.err.println(e.getMessage());
            jcommander.usage();
            return false;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/BaseGenerateDocumentation.java`
#### Snippet
```java
        if (classNames != null) {
            for (String className : classNames) {
                System.out.println(generateDocumentByClassName(className));
            }
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
            tmpCmd.parse(args);
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            tmpCmd.usage();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            tmpCmd.usage();
            return false;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
                    continue;
                }
                System.out.println(generateDocument(cmd.getKey(), jcommander));
            }
        } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
                    continue;
                }
                System.out.println(generateDocument(commandName, jcommander));
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
                bs.createContainerInLocation(loc, config.getBucket());
            }
            System.out.println("Returning " + bs);
            return bs;
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
                close();
            } catch (Exception ex) {
                System.err.println(ex);
            }
        }));
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
            InstanceCache.shutdown();
        } catch (Exception ex) {
            System.err.println(ex);
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeUtils.java`
#### Snippet
```java
            new JmxCollector("{}").register(registry);
        } catch (MalformedObjectNameException ex) {
            System.err.println(ex);
        }
        // Add the default exports from io.prometheus.client.hotspot.DefaultExports
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
                }
            } else {
                System.out.println(
                        String.format("[WARN] %s on functions instance classpath does not exist", f.getAbsolutePath()));
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
        ClassLoader functionInstanceClsLoader = loadJar(root, files.toArray(new File[files.size()]));

        System.out.println("Using function root classloader: " + root);
        System.out.println("Using function instance classloader: " + functionInstanceClsLoader);

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java

        System.out.println("Using function root classloader: " + root);
        System.out.println("Using function instance classloader: " + functionInstanceClsLoader);

        // use the function instance classloader to create org.apache.pulsar.functions.runtime.JavaInstanceStarter
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
                main.getClass().getDeclaredMethod("start", String[].class, ClassLoader.class, ClassLoader.class);

        System.out.println("Starting function instance...");
        method.invoke(main, args, functionInstanceClsLoader, root);
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/RawFileKeyReader.java`
#### Snippet
```java
            keyInfo.setKey(Files.readAllBytes(Paths.get(publicKeyFile)));
        } catch (IOException e) {
            System.out.println("ERROR: Failed to read public key from file " + publicKeyFile);
            e.printStackTrace();
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/RawFileKeyReader.java`
#### Snippet
```java
            keyInfo.setKey(Files.readAllBytes(Paths.get(privateKeyFile)));
        } catch (IOException e) {
            System.out.println("ERROR: Failed to read private key from file " + privateKeyFile);
            e.printStackTrace();
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java
                }
            } catch (IllegalArgumentException e) {
                System.out.println("Incorrect proxyProtocol name '" + proxyProtocolString + "'");
                e.printStackTrace();
                exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java

        if (arguments.topics.size() != 1) {
            System.err.println("Only one topic name is allowed");
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/CmdGenerateDocumentation.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/CmdGenerateDocumentation.java`
#### Snippet
```java
                        .append("|").append(option.getDefault()).append("|\n")
        );
        System.out.println(sb.toString());
        return sb.toString();
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java

        if (arguments.metadataStoreUrl == null && arguments.zookeeperServers == null) {
            System.err.println("Metadata store address argument is required (--metadata-store)");
            jc.usage();
            PerfClientUtils.exit(1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
                arguments.topic = defaultTopics;
            } else {
                System.out.println("The size of topics list should be equal to --num-topics");
                jc.usage();
                PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java

        if (arguments.subscriptionType == SubscriptionType.Exclusive && arguments.numConsumers > 1) {
            System.out.println("Only one consumer is allowed when subscriptionType is Exclusive");
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
                arguments.subscriptions = defaultSubscriptions;
            } else {
                System.out.println("The size of subscriptions list should be equal to --num-subscriptions");
                jc.usage();
                PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/BrokerMonitor.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
                arguments.topic = defaultTopics;
            } else {
                System.out.println("The size of topics list should be equal to --num-topics");
                jc.usage();
                PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
            jc.parse(args);
        } catch (Exception ex) {
            System.out.println(ex.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
        while (true) {
            // Print the very simple prompt.
            System.out.println();
            System.out.print("> ");
            read(inReader.readLine().split("\\s+"));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
            // Print the very simple prompt.
            System.out.println();
            System.out.print("> ");
            read(inReader.readLine().split("\\s+"));
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            jc.parse(args);
        } catch (ParameterException e) {
            System.out.println(e.getMessage());
            jc.usage();
            PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                arguments.topics = defaultTopics;
            } else {
                System.out.println("The size of topics list should be equal to --num-topic");
                jc.usage();
                PerfClientUtils.exit(-1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminSupplier.java`
#### Snippet
```java
                admin = adminBuilder.build();
            } catch (Exception ex) {
                System.err.println(ex.getClass() + ": " + ex.getMessage());
                throw new RuntimeException("Not able to create pulsar admin: " + ex.getMessage(), ex);
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdProxyStats.java`
#### Snippet
```java
        GsonBuilder builder = new GsonBuilder();
        Gson gson = indent ? builder.setPrettyPrinting().create() : builder.create();
        System.out.println(gson.toJson(json));
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CustomCommandsUtils.java`
#### Snippet
```java
            return (DecoratedCommand) ctClass.toClass().getConstructor().newInstance();
        } catch (Throwable t) {
            t.printStackTrace(System.out);
            throw new RuntimeException(t);
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                    getAdmin().sinks().restartSink(tenant, namespace, sinkName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                getAdmin().sinks().restartSink(tenant, namespace, sinkName);
            }
            System.out.println("Restarted successfully");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
            SinkConfig sinkConfig = getAdmin().sinks().getSink(tenant, namespace, sinkName);
            Gson gson = new GsonBuilder().setPrettyPrinting().create();
            System.out.println(gson.toJson(sinkConfig));
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                    getAdmin().sinks().startSink(tenant, namespace, sinkName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                getAdmin().sinks().startSink(tenant, namespace, sinkName);
            }
            System.out.println("Started successfully");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
            getAdmin().sinks().getBuiltInSinks().stream().filter(x -> isNotBlank(x.getSinkClass()))
                    .forEach(connector -> {
                        System.out.println(connector.getName());
                        System.out.println(WordUtils.wrap(connector.getDescription(), 80));
                        System.out.println("----------------------------------------");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                    .forEach(connector -> {
                        System.out.println(connector.getName());
                        System.out.println(WordUtils.wrap(connector.getDescription(), 80));
                        System.out.println("----------------------------------------");
                    });
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                        System.out.println(connector.getName());
                        System.out.println(WordUtils.wrap(connector.getDescription(), 80));
                        System.out.println("----------------------------------------");
                    });
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
            List<String> sinks = getAdmin().sinks().listSinks(tenant, namespace);
            Gson gson = new GsonBuilder().setPrettyPrinting().create();
            System.out.println(gson.toJson(sinks));
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                processArguments();
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                String chosenCommand = jcommander.getParsedCommand();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                String chosenCommand = jcommander.getParsedCommand();
                getUsageFormatter().usage(chosenCommand);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                    getAdmin().sinks().stopSink(tenant, namespace, sinkName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
                getAdmin().sinks().stopSink(tenant, namespace, sinkName);
            }
            System.out.println("Stopped successfully");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java
        lastExitCode = 0;
        if (args.length == 0) {
            System.out.println("Usage: pulsar-admin CONF_FILE_PATH [options] [command] [command options]");
            exit(0);
            return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java
            pulsarAdminSupplier.rootParamsUpdated(rootParams);
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            jcommander.usage();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            jcommander.usage();
            return false;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java

        if (isBlank(rootParams.serviceUrl)) {
            System.out.println("Can't find any admin url to use");
            jcommander.usage();
            return false;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java

        if (rootParams.version) {
            System.out.println("Current version of pulsar admin client is: " + PulsarVersion.getVersion());
            return true;
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java
            ShutdownUtil.triggerImmediateForcefulShutdown(code);
        } else {
            System.out.println("Exit code is " + code + " (System.exit not called, as we are in test mode)");
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java
                cause = e;
            }
            System.err.println(cause.getClass() + ": " + cause.getMessage());
            System.exit(1);
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
            jcommander.parse(args);
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            tryShowCommandUsage();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            tryShowCommandUsage();
            return false;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
                return true;
            } catch (ParameterException e) {
                System.err.println(e.getMessage());
                System.err.println();
                return false;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
            } catch (ParameterException e) {
                System.err.println(e.getMessage());
                System.err.println();
                return false;
            } catch (ConnectException e) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
                return false;
            } catch (ConnectException e) {
                System.err.println(e.getMessage());
                System.err.println();
                System.err.println("Error connecting to: " + getAdmin().getServiceUrl());
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
            } catch (ConnectException e) {
                System.err.println(e.getMessage());
                System.err.println();
                System.err.println("Error connecting to: " + getAdmin().getServiceUrl());
                return false;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
                System.err.println(e.getMessage());
                System.err.println();
                System.err.println("Error connecting to: " + getAdmin().getServiceUrl());
                return false;
            } catch (PulsarAdminException e) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
                return false;
            } catch (PulsarAdminException e) {
                System.err.println(e.getHttpError());
                System.err.println();
                System.err.println("Reason: " + e.getMessage());
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
            } catch (PulsarAdminException e) {
                System.err.println(e.getHttpError());
                System.err.println();
                System.err.println("Reason: " + e.getMessage());
                return false;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
                System.err.println(e.getHttpError());
                System.err.println();
                System.err.println("Reason: " + e.getMessage());
                return false;
            } catch (Exception e) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
        } catch (Exception e) {
            // it is caused by an invalid command, the invalid command can not be parsed
            System.err.println("Invalid command, please use `pulsar-admin --help` to check out how to use");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
            input.setProperties(schemaDefinition.getProperties());
            if (dryRun) {
                System.out.println(topic);
                System.out.println(MAPPER.writerWithDefaultPrettyPrinter()
                                         .writeValueAsString(input));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
            if (dryRun) {
                System.out.println(topic);
                System.out.println(MAPPER.writerWithDefaultPrettyPrinter()
                                         .writeValueAsString(input));
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
            }
            if (version == null && !all) {
                System.out.println(getAdmin().schemas().getSchemaInfoWithVersion(topic));
            } else if (!all) {
                if (version < 0) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
                    throw new ParameterException("Option --version must be greater than 0, but found " + version);
                }
                System.out.println(getAdmin().schemas().getSchemaInfo(topic, version));
            } else {
                print(getAdmin().schemas().getAllSchemas(topic));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokers.java`
#### Snippet
```java
        @Override
        void run() throws Exception {
            System.out.println(getAdmin().brokers().getVersion());
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokers.java`
#### Snippet
```java
        void run() throws Exception {
            getAdmin().brokers().backlogQuotaCheckAsync();
            System.out.println("ok");
        }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokers.java`
#### Snippet
```java
        void run() throws Exception {
            getAdmin().brokers().healthcheck(topicVersion);
            System.out.println("ok");
        }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokers.java`
#### Snippet
```java
        void run() throws Exception {
            getAdmin().brokers().shutDownBrokerGracefully(maxConcurrentUnloadPerSec, forcedTerminateTopic);
            System.out.println("Successfully trigger broker shutdown gracefully");
        }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
            JsonArray metrics = new Gson().fromJson(s, JsonArray.class);

            try (Writer out = new OutputStreamWriter(System.out, StandardCharsets.UTF_8);
                 JsonWriter jsonWriter = new JsonWriter(out)) {
                for (int i = 0; i < metrics.size(); i++) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
            String s = getAdmin().brokerStats().getTopics();
            JsonObject result = new Gson().fromJson(s, JsonObject.class);
            try (Writer out = new OutputStreamWriter(System.out, StandardCharsets.UTF_8);
                 JsonWriter jsonWriter = new JsonWriter(out)) {
                if (indent) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
            ObjectMapper mapper = ObjectMapperFactory.create();
            ObjectWriter writer = mapper.writerWithDefaultPrettyPrinter();
            try (Writer out = new OutputStreamWriter(System.out, StandardCharsets.UTF_8)) {
                out.write(writer.writeValueAsString(stats));
                out.flush();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
            String s = getAdmin().brokerStats().getMBeans();
            JsonArray result = new Gson().fromJson(s, JsonArray.class);
            try (Writer out = new OutputStreamWriter(System.out, StandardCharsets.UTF_8);
                 JsonWriter jsonWriter = new JsonWriter(out)) {
                if (indent) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdGenerateDocument.java`
#### Snippet
```java
                );
            });
            System.out.println(sb);
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdGenerateDocument.java`
#### Snippet
```java
            tool = new PulsarAdminTool(new Properties());
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            baseJcommander.usage();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdGenerateDocument.java`
#### Snippet
```java
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            baseJcommander.usage();
            return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdGenerateDocument.java`
#### Snippet
```java
                }
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                baseJcommander.usage();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdGenerateDocument.java`
#### Snippet
```java
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                baseJcommander.usage();
                return;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                    getAdmin().sources().startSource(tenant, namespace, sourceName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                getAdmin().sources().startSource(tenant, namespace, sourceName);
            }
            System.out.println("Started successfully");
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                processArguments();
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                String chosenCommand = jcommander.getParsedCommand();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                String chosenCommand = jcommander.getParsedCommand();
                getUsageFormatter().usage(chosenCommand);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                    getAdmin().sources().restartSource(tenant, namespace, sourceName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                getAdmin().sources().restartSource(tenant, namespace, sourceName);
            }
            System.out.println("Restarted successfully");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
            List<String> sources = getAdmin().sources().listSources(tenant, namespace);
            Gson gson = new GsonBuilder().setPrettyPrinting().create();
            System.out.println(gson.toJson(sources));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
            getAdmin().sources().getBuiltInSources().stream().filter(x -> !StringUtils.isEmpty(x.getSourceClass()))
                    .forEach(connector -> {
                        System.out.println(connector.getName());
                        System.out.println(WordUtils.wrap(connector.getDescription(), 80));
                        System.out.println("----------------------------------------");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                    .forEach(connector -> {
                        System.out.println(connector.getName());
                        System.out.println(WordUtils.wrap(connector.getDescription(), 80));
                        System.out.println("----------------------------------------");
                    });
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                        System.out.println(connector.getName());
                        System.out.println(WordUtils.wrap(connector.getDescription(), 80));
                        System.out.println("----------------------------------------");
                    });
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                    getAdmin().sources().stopSource(tenant, namespace, sourceName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                getAdmin().sources().stopSource(tenant, namespace, sourceName);
            }
            System.out.println("Stopped successfully");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
            SourceConfig sourceConfig = getAdmin().sources().getSource(tenant, namespace, sourceName);
            Gson gson = new GsonBuilder().setPrettyPrinting().create();
            System.out.println(gson.toJson(sourceConfig));
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
                processArguments();
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                String chosenCommand = jcommander.getParsedCommand();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                String chosenCommand = jcommander.getParsedCommand();
                getUsageFormatter().usage(chosenCommand);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CliCommand.java`
#### Snippet
```java
        try {
            if (item instanceof String) {
                System.out.println(item);
            } else {
                System.out.println(writer.writeValueAsString(item));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CliCommand.java`
#### Snippet
```java
                System.out.println(item);
            } else {
                System.out.println(writer.writeValueAsString(item));
            }
        } catch (Exception e) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
        @Override
        public boolean run(String[] args) {
            System.err.println("WARN: The properties subcommand is deprecated. Please use tenants instead");
            return super.run(args);
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
            try {
                MessageId lastMessageId = getPersistentTopics().terminateTopicAsync(persistentTopic).get();
                System.out.println("Topic successfully terminated at " + lastMessageId);
            } catch (InterruptedException | ExecutionException e) {
                throw new PulsarAdminException(e);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

            getPersistentTopics().triggerCompaction(persistentTopic);
            System.out.println("Topic compaction requested for " + persistentTopic);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
            for (Message<byte[]> msg : messages) {
                if (++position != 1) {
                    System.out.println("-------------------------------------------------------------------------\n");
                }
                if (msg.getMessageId() instanceof BatchMessageIdImpl) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                if (msg.getMessageId() instanceof BatchMessageIdImpl) {
                    BatchMessageIdImpl msgId = (BatchMessageIdImpl) msg.getMessageId();
                    System.out.println("Batch Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId() + ":"
                            + msgId.getBatchIndex());
                } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                } else {
                    MessageIdImpl msgId = (MessageIdImpl) msg.getMessageId();
                    System.out.println("Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId());
                }
                if (msg.getProperties().size() > 0) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                }
                if (msg.getProperties().size() > 0) {
                    System.out.println("Properties:");
                    print(msg.getProperties());
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                }
                ByteBuf data = Unpooled.wrappedBuffer(msg.getData());
                System.out.println(ByteBufUtil.prettyHexDump(data));
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

            ByteBuf date = Unpooled.wrappedBuffer(message.getData());
            System.out.println(ByteBufUtil.prettyHexDump(date));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
            String result = getPersistentTopics().getInternalInfo(persistentTopic);
            Gson gson = new GsonBuilder().setPrettyPrinting().create();
            System.out.println(gson.toJson(result));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                switch (status.status) {
                case NOT_RUN:
                    System.out.println("Compaction has not been run for " + persistentTopic
                                       + " since broker startup");
                    break;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                    break;
                case RUNNING:
                    System.out.println("Compaction is currently running");
                    break;
                case SUCCESS:
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                    break;
                case SUCCESS:
                    System.out.println("Compaction was a success");
                    break;
                case ERROR:
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                    break;
                case ERROR:
                    System.out.println("Error in compaction");
                    throw new PulsarAdminException("Error compacting: " + status.lastError);
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/IOUtils.java`
#### Snippet
```java
    public static boolean confirmPrompt(String prompt) throws IOException {
        while (true) {
            System.out.print(prompt + " (Y or N) ");
            StringBuilder responseBuilder = new StringBuilder();
            while (true) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/IOUtils.java`
#### Snippet
```java
                return false;
            }
            System.out.println("Invalid input: " + response);
            // else ask them again
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdGenerateDocumentation.java`
#### Snippet
```java
                        .append("|").append(option.getDefault()).append("|\n")
        );
        System.out.println(sb.toString());
        return sb.toString();
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
                this.rootParams.proxyProtocol = ProxyProtocol.valueOf(proxyProtocolString.toUpperCase());
            } catch (IllegalArgumentException e) {
                System.out.println("Incorrect proxyProtocol name '" + proxyProtocolString + "'");
                e.printStackTrace();
                System.exit(-1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
        if (isNotBlank(rootParams.proxyServiceURL)) {
            if (rootParams.proxyProtocol == null) {
                System.out.println("proxy-protocol must be provided with proxy-url");
                System.exit(-1);
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
    public static void main(String[] args) throws Exception {
        if (args.length == 0) {
            System.out.println("Usage: pulsar-client CONF_FILE_PATH [options] [command] [command options]");
            System.exit(-1);
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java

            if (rootParams.version) {
                System.out.println("Current version of pulsar client is: " + PulsarVersion.getVersion());
                return 0;
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
                                     // it will default to the values passed in by the constructor
            } catch (UnsupportedAuthenticationException exp) {
                System.out.println("Failed to load an authentication plugin");
                exp.printStackTrace();
                return -1;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            System.out.println(e.getMessage());
            String chosenCommand = jcommander.getParsedCommand();
            if (e instanceof ParameterException) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
                            numMessagesConsumed += 1;
                            if (!hideContent) {
                                System.out.println(MESSAGE_BOUNDARY);
                                String output = this.interpretMessage(msg, displayHex);
                                System.out.println(output);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
                                System.out.println(MESSAGE_BOUNDARY);
                                String output = this.interpretMessage(msg, displayHex);
                                System.out.println(output);
                            } else if (numMessagesConsumed % 1000 == 0) {
                                System.out.println("Received " + numMessagesConsumed + " messages");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
                                System.out.println(output);
                            } else if (numMessagesConsumed % 1000 == 0) {
                                System.out.println("Received " + numMessagesConsumed + " messages");
                            }
                            consumer.acknowledge(msg);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
                    try {
                        String output = interpretByteArray(displayHex, Base64.getDecoder().decode(msg));
                        System.out.println(output); // print decode
                    } catch (Exception e) {
                        System.out.println(msg);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
                        System.out.println(output); // print decode
                    } catch (Exception e) {
                        System.out.println(msg);
                    }
                    numMessagesConsumed += 1;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                processArguments();
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                String chosenCommand = jcommander.getParsedCommand();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            } catch (Exception e) {
                System.err.println(e.getMessage());
                System.err.println();
                String chosenCommand = jcommander.getParsedCommand();
                getUsageFormatter().usage(chosenCommand);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                    getAdmin().functions().startFunction(tenant, namespace, functionName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                getAdmin().functions().startFunction(tenant, namespace, functionName);
            }
            System.out.println("Started successfully");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            FunctionConfig functionConfig = getAdmin().functions().getFunction(tenant, namespace, functionName);
            Gson gson = new GsonBuilder().setPrettyPrinting().create();
            System.out.println(gson.toJson(functionConfig));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            getAdmin().functions().getBuiltInFunctions()
                    .forEach(function -> {
                        System.out.println(function.getName());
                        System.out.println(WordUtils.wrap(function.getDescription(), 80));
                        System.out.println("----------------------------------------");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                    .forEach(function -> {
                        System.out.println(function.getName());
                        System.out.println(WordUtils.wrap(function.getDescription(), 80));
                        System.out.println("----------------------------------------");
                    });
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                        System.out.println(function.getName());
                        System.out.println(WordUtils.wrap(function.getDescription(), 80));
                        System.out.println("----------------------------------------");
                    });
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            String retval = getAdmin().functions()
                    .triggerFunction(tenant, namespace, functionName, topic, triggerValue, triggerFile);
            System.out.println(retval);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                                                       .getFunctionState(tenant, namespace, functionName, key);
                    Gson gson = new GsonBuilder().setPrettyPrinting().create();
                    System.out.println(gson.toJson(functionState));
                } catch (PulsarAdminException pae) {
                    if (pae.getStatusCode() == 404 && watch) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                } catch (PulsarAdminException pae) {
                    if (pae.getStatusCode() == 404 && watch) {
                        System.err.println(pae.getMessage());
                    } else {
                        throw pae;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                    getAdmin().functions().stopFunction(tenant, namespace, functionName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                getAdmin().functions().stopFunction(tenant, namespace, functionName);
            }
            System.out.println("Stopped successfully");
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                            .restartFunction(tenant, namespace, functionName, Integer.parseInt(instanceId));
                } catch (NumberFormatException e) {
                    System.err.println("instance-id must be a number");
                }
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                getAdmin().functions().restartFunction(tenant, namespace, functionName);
            }
            System.out.println("Restarted successfully");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
            String namespace = validateNamespace(params);

            System.out.println(getAdmin().namespaces().getSchemaValidationEnforced(namespace, applied));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        void run() throws PulsarAdminException {
            String namespace = validateNamespace(params);
            System.out.println(getAdmin().namespaces().getSchemaAutoUpdateCompatibilityStrategy(namespace)
                               .toString().toUpperCase());
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
            Long lag = getAdmin().namespaces().getOffloadDeleteLagMs(namespace);
            if (lag != null) {
                System.out.println(TimeUnit.MINUTES.convert(lag, TimeUnit.MILLISECONDS) + " minute(s)");
            } else {
                System.out.println("Unset for namespace. Defaulting to broker setting.");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                System.out.println(TimeUnit.MINUTES.convert(lag, TimeUnit.MILLISECONDS) + " minute(s)");
            } else {
                System.out.println("Unset for namespace. Defaulting to broker setting.");
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        void run() throws PulsarAdminException {
            String namespace = validateNamespace(params);
            System.out.println(getAdmin().namespaces().getSchemaCompatibilityStrategy(namespace)
                    .toString().toUpperCase());
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
            String namespace = validateNamespace(params);

            System.out.println(getAdmin().namespaces().getIsAllowAutoUpdateSchema(namespace));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            for (Map.Entry<Integer, MessageId> entry: messageIds.entrySet()) {
                String topicName = persistentTopic + "-partition-" + entry.getKey();
                System.out.println("Topic " + topicName +  " succesfully terminated at " + entry.getValue());
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

            if (messageId == null) {
                System.out.println("Nothing to offload");
                return;
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

            getTopics().triggerOffload(persistentTopic, messageId);
            System.out.println("Offload triggered for " + persistentTopic + " for messages before " + messageId);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                switch (status.status) {
                case NOT_RUN:
                    System.out.println("Compaction has not been run for " + persistentTopic
                                       + " since broker startup");
                    break;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                    break;
                case RUNNING:
                    System.out.println("Compaction is currently running");
                    break;
                case SUCCESS:
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                    break;
                case SUCCESS:
                    System.out.println("Compaction was a success");
                    break;
                case ERROR:
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                    break;
                case ERROR:
                    System.out.println("Error in compaction");
                    throw new PulsarAdminException("Error compacting: " + status.lastError);
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

            getTopics().triggerCompaction(persistentTopic);
            System.out.println("Topic compaction requested for " + persistentTopic);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        void run() throws PulsarAdminException {
            String topic = validateTopicName(params);
            System.out.println(getAdmin().topics().getSchemaValidationEnforced(topic, applied));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            try {
                MessageId lastMessageId = getTopics().terminateTopicAsync(persistentTopic).get();
                System.out.println("Topic successfully terminated at " + lastMessageId);
            } catch (InterruptedException | ExecutionException e) {
                throw new PulsarAdminException(e);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            MessageId messageId = getTopics().getMessageIdByTimestamp(persistentTopic, timestamp);
            if (messageId == null) {
                System.out.println("Cannot find any messages based on timestamp " + timestamp);
            } else {
                print(messageId);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            Map<String, String> result = getTopics().getSubscriptionProperties(topic, subscriptionName);
            // Ensure we are using JSON and not Java toString()
            System.out.println(ObjectMapperFactory.getThreadLocal().writeValueAsString(result));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            String internalInfo = getTopics().getInternalInfo(topic);
            if (internalInfo == null) {
                System.out.println("Did not find any internal metadata info");
                return;
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            JsonObject result = JsonParser.parseString(internalInfo).getAsJsonObject();
            Gson gson = new GsonBuilder().setPrettyPrinting().create();
            System.out.println(gson.toJson(result));
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            MessageImpl message = (MessageImpl) getTopics().getMessageById(persistentTopic, ledgerId, entryId);
            if (message == null) {
                System.out.println("Cannot find any messages based on ledgerId:"
                        + ledgerId + " entryId:" + entryId);
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                if (message.getMessageId() instanceof BatchMessageIdImpl) {
                    BatchMessageIdImpl msgId = (BatchMessageIdImpl) message.getMessageId();
                    System.out.println("Batch Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId() + ":"
                            + msgId.getBatchIndex());
                } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                } else {
                    MessageIdImpl msgId = (MessageIdImpl) message.getMessageId();
                    System.out.println("Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId());
                }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }

                System.out.println("Publish time: " + message.getPublishTime());
                System.out.println("Event time: " + message.getEventTime());

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

                System.out.println("Publish time: " + message.getPublishTime());
                System.out.println("Event time: " + message.getEventTime());

                if (message.getDeliverAtTime() != 0) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

                if (message.getDeliverAtTime() != 0) {
                    System.out.println("Deliver at time: " + message.getDeliverAtTime());
                }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                if (message.getBrokerEntryMetadata() != null) {
                    if (message.getBrokerEntryMetadata().hasBrokerTimestamp()) {
                        System.out.println("Broker entry metadata timestamp: "
                                + message.getBrokerEntryMetadata().getBrokerTimestamp());
                    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                    }
                    if (message.getBrokerEntryMetadata().hasIndex()) {
                        System.out.println("Broker entry metadata index: "
                                + message.getBrokerEntryMetadata().getIndex());
                    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

                if (message.getProperties().size() > 0) {
                    System.out.println("Properties:");
                    print(message.getProperties());
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }
                ByteBuf date = Unpooled.wrappedBuffer(message.getData());
                System.out.println(ByteBufUtil.prettyHexDump(date));
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                MessageImpl message = (MessageImpl) msg;
                if (++position != 1) {
                    System.out.println("-------------------------------------------------------------------------\n");
                }
                if (message.getMessageId() instanceof BatchMessageIdImpl) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                if (message.getMessageId() instanceof BatchMessageIdImpl) {
                    BatchMessageIdImpl msgId = (BatchMessageIdImpl) message.getMessageId();
                    System.out.println("Batch Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId() + ":"
                            + msgId.getBatchIndex());
                } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                } else {
                    MessageIdImpl msgId = (MessageIdImpl) msg.getMessageId();
                    System.out.println("Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId());
                }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }

                System.out.println("Publish time: " + message.getPublishTime());
                System.out.println("Event time: " + message.getEventTime());

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

                System.out.println("Publish time: " + message.getPublishTime());
                System.out.println("Event time: " + message.getEventTime());

                if (message.getDeliverAtTime() != 0) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

                if (message.getDeliverAtTime() != 0) {
                    System.out.println("Deliver at time: " + message.getDeliverAtTime());
                }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                if (message.getBrokerEntryMetadata() != null) {
                    if (message.getBrokerEntryMetadata().hasBrokerTimestamp()) {
                        System.out.println("Broker entry metadata timestamp: "
                                + message.getBrokerEntryMetadata().getBrokerTimestamp());
                    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                    }
                    if (message.getBrokerEntryMetadata().hasIndex()) {
                        System.out.println("Broker entry metadata index: "
                                + message.getBrokerEntryMetadata().getIndex());
                    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

                if (message.getProperties().size() > 0) {
                    System.out.println("Properties:");
                    print(msg.getProperties());
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }
                ByteBuf data = Unpooled.wrappedBuffer(msg.getData());
                System.out.println(ByteBufUtil.prettyHexDump(data));
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                switch (status.getStatus()) {
                case NOT_RUN:
                    System.out.println("Offload has not been run for " + persistentTopic
                                       + " since broker startup");
                    break;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                    break;
                case RUNNING:
                    System.out.println("Offload is currently running");
                    break;
                case SUCCESS:
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                    break;
                case SUCCESS:
                    System.out.println("Offload was a success");
                    break;
                case ERROR:
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                    break;
                case ERROR:
                    System.out.println("Error in offload");
                    throw new PulsarAdminException("Error offloading: " + status.getLastError());
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            if (message.getMessageId() instanceof BatchMessageIdImpl) {
                BatchMessageIdImpl msgId = (BatchMessageIdImpl) message.getMessageId();
                System.out.println("Batch Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId() + ":"
                        + msgId.getBatchIndex());
            } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            } else {
                MessageIdImpl msgId = (MessageIdImpl) message.getMessageId();
                System.out.println("Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId());
            }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            }

            System.out.println("Publish time: " + message.getPublishTime());
            System.out.println("Event time: " + message.getEventTime());

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

            System.out.println("Publish time: " + message.getPublishTime());
            System.out.println("Event time: " + message.getEventTime());

            if (message.getDeliverAtTime() != 0) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

            if (message.getDeliverAtTime() != 0) {
                System.out.println("Deliver at time: " + message.getDeliverAtTime());
            }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            if (message.getBrokerEntryMetadata() != null) {
                if (message.getBrokerEntryMetadata().hasBrokerTimestamp()) {
                    System.out.println("Broker entry metadata timestamp: "
                            + message.getBrokerEntryMetadata().getBrokerTimestamp());
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }
                if (message.getBrokerEntryMetadata().hasIndex()) {
                    System.out.println("Broker entry metadata index: " + message.getBrokerEntryMetadata().getIndex());
                }
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

            if (message.getProperties().size() > 0) {
                System.out.println("Properties:");
                print(message.getProperties());
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            }
            ByteBuf data = Unpooled.wrappedBuffer(message.getData());
            System.out.println(ByteBufUtil.prettyHexDump(data));
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
            mainCommander.parse(args);
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            mainCommander.usage();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
        } catch (Exception e) {
            System.err.println(e.getMessage());
            System.err.println();
            mainCommander.usage();
            exit(1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
        pulsarShellDir = computePulsarShellFile();
        Files.createDirectories(pulsarShellDir.toPath());
        System.out.println(String.format("Using directory: %s", pulsarShellDir.getAbsolutePath()));

        ConfigStore.ConfigEntry defaultConfig = null;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools-customcommand-example/src/main/java/org/apache/pulsar/admin/cli/examples/MyCommandFactory.java`
#### Snippet
```java
                    Map<String, Object> parameters, CommandExecutionContext context)
                throws Exception {
              System.out.println(
                  "Execute: " + parameters + " properties " + context.getConfiguration());
              String destination = parameters.getOrDefault("topic", "").toString();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools-customcommand-example/src/main/java/org/apache/pulsar/admin/cli/examples/MyCommandFactory.java`
#### Snippet
```java
              String destination = parameters.getOrDefault("topic", "").toString();
              TopicStats stats = context.getPulsarAdmin().topics().getStats(destination);
              System.out.println("Topic stats: " + stats);
              return false;
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `pulsar-client-tools-customcommand-example/src/main/java/org/apache/pulsar/admin/cli/examples/MyCommandFactory.java`
#### Snippet
```java
                    Map<String, Object> parameters, CommandExecutionContext context)
                    throws Exception {
                System.out.println(
                        "Execute: " + parameters + " properties " + context.getConfiguration());
                return false;
```

## RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java
                    Configurables.configure(source, context);
                    String[] channelNames = context.getString(
                            BasicConfigurationConstants.CONFIG_CHANNELS).split("\\s+");
                    List<Channel> sourceChannels =
                            getSourceChannels(channelComponentMap, source, Arrays.asList(channelNames));
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java
                indexName = indexName.substring(0, indexName.length() - 1);
            }
            if (indexName.length() <= 0 || !indexName.matches("[a-zA-Z\\.0-9][a-zA-Z_\\.\\-\\+0-9]*")) {
                throw new RuntimeException(new IOException("Cannot convert the topic name='"
                        + topicName + "' to a valid elasticsearch index name"));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
        try {
            return sanitizedTopicCache.get(name, () -> {
                String sanitizedName = name.replaceAll("[^a-zA-Z0-9_]", "_");
                if (sanitizedName.matches("^[^a-zA-Z_].*")) {
                    sanitizedName = "_" + sanitizedName;
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
            return sanitizedTopicCache.get(name, () -> {
                String sanitizedName = name.replaceAll("[^a-zA-Z0-9_]", "_");
                if (sanitizedName.matches("^[^a-zA-Z_].*")) {
                    sanitizedName = "_" + sanitizedName;
                }
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
            List<String> allTopics = this.pulsarAdmin.topics().getList(namespace, TopicDomain.persistent);
            topicsSetWithoutPartition = allTopics.stream()
                    .map(t -> t.split(TopicName.PARTITIONED_TOPIC_SUFFIX)[0])
                    .collect(Collectors.toSet());
        } catch (PulsarAdminException e) {
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/BrokerProxyValidator.java`
#### Snippet
```java

    private static List<String> parseCommaSeparatedConfigValue(String configValue) {
        return Arrays.stream(configValue.split(SEPARATOR)).map(String::trim).filter(s -> s.length() > 0)
                .collect(Collectors.toList());
    }
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
            String clusterName = producerName.substring(replicatorPrefix.length());
            return clusterName.contains(REPL_PRODUCER_NAME_DELIMITER)
                    ? clusterName.split(REPL_PRODUCER_NAME_DELIMITER)[0] : clusterName;
        }
        return null;
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
            try {
                // Version < 1.20 for cpp-client is not allowed
                String[] tokens = userAgent.split(DEPRECATED_CLIENT_VERSION_PREFIX);
                String[] splits = tokens.length > 1 ? tokens[1].split("-")[0].trim().split("\\.") : null;
                if (splits != null && splits.length > 1) {
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LinuxInfoUtils.java`
#### Snippet
```java
                return ResourceUsage.empty();
            }
            String[] words = first.get().split("\\s+");
            long total = Arrays.stream(words)
                    .filter(s -> !s.contains("cpu"))
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java
            //trim package
            String[] paths = StringUtils.removeFirst(schemaData.getRootMessageTypeName(), fileDescriptor.getPackage())
                    .replaceFirst("\\.", "").split("\\.");
            //extract root message
            descriptor = fileDescriptor.findMessageTypeByName(paths[0]);
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/BaseGenerateDocumentation.java`
#### Snippet
```java
            String name = StringUtils.isBlank(modelProperty.name()) ? field.getName() : modelProperty.name();
            sb.append("### ").append(name).append("\n");
            sb.append(modelProperty.value().replace(">", "\\>")).append("\n\n");
            sb.append("**Type**: `").append(field.getType().getCanonicalName()).append("`\n\n");
            sb.append("**Default**: `").append(field.get(obj)).append("`\n\n");
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/BaseGenerateDocumentation.java`
#### Snippet
```java

            sb.append("### ").append(field.getName()).append("\n");
            sb.append(fieldContext.doc().replace(">", "\\>")).append("\n\n");
            sb.append("**Type**: `").append(field.getType().getCanonicalName()).append("`\n\n");
            sb.append("**Default**: `").append(field.get(obj)).append("`\n\n");
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
                    options.forEach((option) ->
                            sb.append("| `").append(option.getNames())
                                    .append("` | ").append(option.getDescription().replace("\n", " "))
                                    .append("|").append(option.getDefault()).append("|\n")
                    );
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
            options.forEach((option) ->
                    sb.append("| `").append(option.getNames())
                            .append("` | ").append(option.getDescription().replace("\n", " "))
                            .append("|").append(option.getDefault()).append("|\n")
            );
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/KeyStoreSSLContext.java`
#### Snippet
```java
            this.protocols = protocols;
        } else {
            this.protocols = new HashSet<>(Arrays.asList(DEFAULT_SSL_ENABLED_PROTOCOLS.split("\\s*,\\s*")));
        }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java

    private Client newEtcdClient(String metadataURL, MetadataStoreConfig conf) throws IOException {
        String etcdUrl = metadataURL.replaceFirst(ETCD_SCHEME_IDENTIFIER, "");
        ClientBuilder clientBuilder = Client.builder()
                .endpoints(etcdUrl.split(","));
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
     */
    public boolean isLedgerParentNode(String path) {
        return path.matches(StringUtils.HIERARCHICAL_LEDGER_PARENT_NODE_REGEX);
    }
}
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LongHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
     */
    public boolean isLedgerParentNode(String path) {
        return path.matches(StringUtils.LONGHIERARCHICAL_LEDGER_PARENT_NODE_REGEX);
    }
}
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/AbstractMetadataDriver.java`
#### Snippet
```java
            try {
                url = conf.getMetadataServiceUri()
                        .replaceFirst(METADATA_STORE_SCHEME + ":", "")
                        .replace(";", ",");
            } catch (Exception e) {
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java

    private boolean isLedgerParentNode(String path) {
        return path.matches(StringUtils.LEGACYHIERARCHICAL_LEDGER_PARENT_NODE_REGEX);
    }
}
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java

        if (isBuiltin) {
            String builtin = sourceConfig.getArchive().replaceFirst("^builtin://", "");
            sourceSpecBuilder.setBuiltin(builtin);
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java

        if (isBuiltin) {
            String builtin = sinkConfig.getArchive().replaceFirst("^builtin://", "");
            sinkSpecBuilder.setBuiltin(builtin);
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java
        if (!isEmpty(sinkConfig.getTransformFunction())
                && sinkConfig.getTransformFunction().startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
            functionDetailsBuilder.setBuiltin(sinkConfig.getTransformFunction().replaceFirst("^builtin://", ""));
        }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java

        if (isBuiltin) {
            String builtin = functionConfig.getJar().replaceFirst("^builtin://", "");
            functionDetailsBuilder.setBuiltin(builtin);
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
            String archive = sinkConfig.getArchive();
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

                Connector connector = worker().getConnectorsManager().getConnector(archive);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImpl.java`
#### Snippet
```java
        if (!StringUtils.isEmpty(archive)) {
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

                FunctionsManager functionsManager = worker().getFunctionsManager();
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
            String archive = sourceConfig.getArchive();
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

                Connector connector = worker().getConnectorsManager().getConnector(archive);
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeUtils.java`
#### Snippet
```java
     */
    public static String[] splitRuntimeArgs(String input) {
        return input.split("\\s(?=([^\"]*\"[^\"]*\")*[^\"]*$)");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
        if (!StringUtils.isEmpty(archive)) {
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

                FunctionArchive function = worker().getFunctionsManager().getFunction(archive);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java

    private Path getBuiltinArchivePath(String pkgPath, FunctionDetails.ComponentType componentType) {
        String type = pkgPath.replaceFirst("^builtin://", "");
        if (!FunctionDetails.ComponentType.FUNCTION.equals(componentType)) {
            Connector connector = worker().getConnectorsManager().getConnector(type);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java

    private static String toValidPodName(String ori) {
        return ori.toLowerCase().replaceAll("[^a-z0-9-\\.]", "-");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java

    private static String toValidLabelName(String ori) {
        return left(ori.toLowerCase().replaceAll("[^a-zA-Z0-9-_\\.]", "-").replaceAll("^[^a-zA-Z0-9]", "0")
                .replaceAll("[^a-zA-Z0-9]$", "0"), maxLabelSize);
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java

    private static String toValidLabelName(String ori) {
        return left(ori.toLowerCase().replaceAll("[^a-zA-Z0-9-_\\.]", "-").replaceAll("^[^a-zA-Z0-9]", "0")
                .replaceAll("[^a-zA-Z0-9]$", "0"), maxLabelSize);
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    private static String toValidLabelName(String ori) {
        return left(ori.toLowerCase().replaceAll("[^a-zA-Z0-9-_\\.]", "-").replaceAll("^[^a-zA-Z0-9]", "0")
                .replaceAll("[^a-zA-Z0-9]$", "0"), maxLabelSize);
    }

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/WordCountFunction.java`
#### Snippet
```java
    @Override
    public Void process(String input, Context context) {
        Arrays.asList(input.split("\\s+")).forEach(word -> context.incrCounter(word, 1));
        return null;
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
        TreeMap<String, Connector> connectors = getConnectors();

        String source = sourceType.replaceFirst("^builtin://", "");
        Connector connector = connectors.get(source);
        if (connector != null && connector.getConnectorDefinition().getSourceClass() != null) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
        TreeMap<String, Connector> connectors = getConnectors();

        String sink = sinkType.replaceFirst("^builtin://", "");
        Connector connector = connectors.get(sink);
        if (connector != null && connector.getConnectorDefinition().getSinkClass() != null) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
        TreeMap<String, FunctionArchive> functions = getFunctions();

        String functionName = functionType.replaceFirst("^builtin://", "");
        FunctionArchive function = functions.get(functionName);
        if (function != null && function.getFunctionDefinition().getFunctionClass() != null) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
                            arguments.proxyURL = webServiceUrl;
                        } else if (webServiceUrl.startsWith("http://") || webServiceUrl.startsWith("https://")) {
                            arguments.proxyURL = webServiceUrl.replaceFirst("^http", "ws");
                        }
                    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/DefaultMessageFormatter.java`
#### Snippet
```java
        String sMessage = new String(message, StandardCharsets.UTF_8);
        if (producerName != null && !producerName.isEmpty()) {
            sMessage = sMessage.replaceAll("%p", producerName);
        }
        sMessage = sMessage.replaceAll("%i", String.valueOf(msgId));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/DefaultMessageFormatter.java`
#### Snippet
```java
            sMessage = sMessage.replaceAll("%p", producerName);
        }
        sMessage = sMessage.replaceAll("%i", String.valueOf(msgId));
        sMessage = sMessage.replaceAll("%t", String.valueOf(System.nanoTime()));

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/DefaultMessageFormatter.java`
#### Snippet
```java
        }
        sMessage = sMessage.replaceAll("%i", String.valueOf(msgId));
        sMessage = sMessage.replaceAll("%t", String.valueOf(System.nanoTime()));

        int idx = sMessage.indexOf("%");
```

### DynamicRegexReplaceableByCompiledPattern
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
                }
                sb.append("{\"attempt\":").append(i++)
                    .append(",\"error\":\"").append(t.toString().replace("\"", "\\\""))
                    .append("\"}");
            }
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
                    String line = scriptReader.readLine();
                    while (line != null) {
                        read(line.split("\\s+"));
                        line = scriptReader.readLine();
                    }
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
            System.out.println();
            System.out.print("> ");
            read(inReader.readLine().split("\\s+"));
        }
    }
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdGenerateDocument.java`
#### Snippet
```java
                                || !ele.getParameterAnnotation().hidden()
                ).forEach((option) -> {
                            String[] descriptions = option.getDescription().replace("\n", " ").split(" #");
                            sb.append("| `").append(option.getNames())
                                    .append("` | ").append(descriptions[0])
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java
            if (StringUtils.isNotBlank(metadataServiceUri)) {
                try {
                    url = metadataServiceUri.replaceFirst(METADATA_STORE_SCHEME + ":", "")
                            .replace(";", ",");
                } catch (Exception e) {
```

## RuleId[ruleID=ComparatorMethodParameterNotUsed]
### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/PulsarResourceDescription.java`
#### Snippet
```java

    @Override
    public int compareTo(ResourceDescription o) {
        if (o.getResourceUsage().size() > resourceUsageByName.size()) {
            return -1;
```

### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NamespaceBundleStatsComparator.java`
#### Snippet
```java

    // sort in reverse order, maximum loaded should be on top
    public int compare(String a, String b) {
        int result = 0;
        if (this.resType == ResourceType.CPU) {
```

## RuleId[ruleID=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `PushSource()` of an abstract class should not be declared 'public'
in `pulsar-io/core/src/main/java/org/apache/pulsar/io/core/PushSource.java`
#### Snippet
```java
    private static final int DEFAULT_QUEUE_LENGTH = 1000;

    public PushSource() {
        this.queue = new LinkedBlockingQueue<>(this.getQueueLength());
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `BatchPushSource()` of an abstract class should not be declared 'public'
in `pulsar-io/core/src/main/java/org/apache/pulsar/io/core/BatchPushSource.java`
#### Snippet
```java
    private final NullRecord nullRecord = new NullRecord();

    public BatchPushSource() {
        this.queue = new LinkedBlockingQueue<>(this.getQueueLength());
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `LogicalTypeConverter()` of an abstract class should not be declared 'public'
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
        final Conversion<T> conversion;

        public LogicalTypeConverter(Conversion<T> conversion) {
            this.conversion = conversion;
        }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHdfsConnector()` of an abstract class should not be declared 'public'
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConnector.java`
#### Snippet
```java
    protected CompressionCodecFactory compressionCodecFactory;

    public AbstractHdfsConnector() {
       hdfsResources.set(new HdfsResources(null, null, null));
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHdfsConnector()` of an abstract class should not be declared 'public'
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConnector.java`
#### Snippet
```java
    protected CompressionCodecFactory compressionCodecFactory;

    public AbstractHdfsConnector() {
       hdfsResources.set(new HdfsResources(null, null, null));
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `MongoAbstractConfig()` of an abstract class should not be declared 'public'
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoAbstractConfig.java`
#### Snippet
```java

    @JsonCreator
    public MongoAbstractConfig(
            @JsonProperty("mongoUri") String mongoUri,
            @JsonProperty("database") String database,
```

### NonProtectedConstructorInAbstractClass
Constructor `MongoAbstractConfig()` of an abstract class should not be declared 'public'
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoAbstractConfig.java`
#### Snippet
```java
    private final long batchTimeMs;

    public MongoAbstractConfig() {
        this(null, null, null, DEFAULT_BATCH_SIZE, DEFAULT_BATCH_TIME_MS);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConfigurationProvider()` of an abstract class should not be declared 'public'
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java
    private final Map<Class<? extends Channel>, Map<String, Channel>> channelCache;

    public AbstractConfigurationProvider(String agentName) {
        super();
        this.agentName = agentName;
```

### NonProtectedConstructorInAbstractClass
Constructor `LogicalTypeConverter()` of an abstract class should not be declared 'public'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
        final Conversion<T> conversion;

        public LogicalTypeConverter(Conversion<T> conversion) {
            this.conversion = conversion;
        }
```

### NonProtectedConstructorInAbstractClass
Constructor `LogicalTypeConverter()` of an abstract class should not be declared 'public'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
        final Conversion<T> conversion;

        public LogicalTypeConverter(Conversion<T> conversion) {
            this.conversion = conversion;
        }
```

### NonProtectedConstructorInAbstractClass
Constructor `RestClient()` of an abstract class should not be declared 'public'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/RestClient.java`
#### Snippet
```java
    private final ScheduledExecutorService executorService;

    public RestClient(ElasticSearchConfig elasticSearchConfig, BulkProcessor.Listener bulkProcessorListener) {
        this.config = elasticSearchConfig;
        this.configCallback = new ConfigCallback();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDelayedDeliveryTracker()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/AbstractDelayedDeliveryTracker.java`
#### Snippet
```java
    }

    public AbstractDelayedDeliveryTracker(PersistentDispatcherMultipleConsumers dispatcher, Timer timer,
                                          long tickTimeMillis, Clock clock,
                                          boolean isDelayedDeliveryDeliverAtTimeStrict) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDelayedDeliveryTracker()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/AbstractDelayedDeliveryTracker.java`
#### Snippet
```java
    private final boolean isDelayedDeliveryDeliverAtTimeStrict;

    public AbstractDelayedDeliveryTracker(PersistentDispatcherMultipleConsumers dispatcher, Timer timer,
                                          long tickTimeMillis,
                                          boolean isDelayedDeliveryDeliverAtTimeStrict) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDispatcherSingleActiveConsumer()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    protected boolean isFirstRead = true;

    public AbstractDispatcherSingleActiveConsumer(SubType subscriptionType, int partitionIndex,
                                                  String topicName, Subscription subscription,
                                                  ServiceConfiguration serviceConfig, ManagedCursor cursor) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractReplicator()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
    }

    public AbstractReplicator(String localCluster, String localTopicName, String remoteCluster, String remoteTopicName,
                              String replicatorPrefix, BrokerService brokerService, PulsarClientImpl replicationClient)
            throws PulsarServerException {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTopic()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    protected Map<String, EntryFilterWithClassLoader> entryFilters;

    public AbstractTopic(String topic, BrokerService brokerService) {
        this.topic = topic;
        this.brokerService = brokerService;
```

### NonProtectedConstructorInAbstractClass
Constructor `PersistentReplicator()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
    protected volatile boolean fetchSchemaInProgress = false;

    public PersistentReplicator(String localCluster, PersistentTopic localTopic, ManagedCursor cursor,
                                   String remoteCluster, String remoteTopic,
                                   BrokerService brokerService, PulsarClientImpl replicationClient)
```

### NonProtectedConstructorInAbstractClass
Constructor `SystemTopicClientBase()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/SystemTopicClientBase.java`
#### Snippet
```java
    protected final List<Reader<T>> readers;

    public SystemTopicClientBase(PulsarClient client, TopicName topicName) {
        this.client = client;
        this.topicName = topicName;
```

### NonProtectedConstructorInAbstractClass
Constructor `DeviationShedder()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/DeviationShedder.java`
#### Snippet
```java
     * Initialize this DeviationShedder.
     */
    public DeviationShedder() {
        bundleTreeSetCache = new TreeSet<>();
        metricTreeSetCache = new TreeSet<>();
```

### NonProtectedConstructorInAbstractClass
Constructor `TopicTransactionBufferState()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBufferState.java`
#### Snippet
```java
    private volatile State state = null;

    public TopicTransactionBufferState(State state) {
        STATE_UPDATER.set(this, state);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/TransactionException.java`
#### Snippet
```java
    }

    public TransactionException(String message, Throwable cause) {
        super(message, cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/TransactionException.java`
#### Snippet
```java
    private static final long serialVersionUID = 0L;

    public TransactionException(String message) {
        super(message);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/TransactionException.java`
#### Snippet
```java
    }

    public TransactionException(Throwable cause) {
        super(cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionPendingAckException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/pendingack/TransactionPendingAckException.java`
#### Snippet
```java
    }

    public TransactionPendingAckException(Throwable cause) {
        super(cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionPendingAckException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/pendingack/TransactionPendingAckException.java`
#### Snippet
```java
    }

    public TransactionPendingAckException(String message, Throwable cause) {
        super(message, cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionPendingAckException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/pendingack/TransactionPendingAckException.java`
#### Snippet
```java
    private static final long serialVersionUID = 0L;

    public TransactionPendingAckException(String message) {
        super(message);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionBufferException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
    private static final long serialVersionUID = 0L;

    public TransactionBufferException(String message) {
        super(message);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionBufferException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
    }

    public TransactionBufferException(Throwable cause) {
        super(cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionBufferException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
    }

    public TransactionBufferException(String message, Throwable cause) {
        super(message, cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionCoordinatorException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/coordinator/TransactionCoordinatorException.java`
#### Snippet
```java
    }

    public TransactionCoordinatorException(Throwable cause) {
        super(cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionCoordinatorException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/coordinator/TransactionCoordinatorException.java`
#### Snippet
```java
    private static final long serialVersionUID = 0L;

    public TransactionCoordinatorException(String message) {
        super(message);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionCoordinatorException()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/coordinator/TransactionCoordinatorException.java`
#### Snippet
```java
    }

    public TransactionCoordinatorException(String message, Throwable cause) {
        super(message, cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `PendingAckHandleState()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleState.java`
#### Snippet
```java
    protected volatile State state = null;

    public PendingAckHandleState(State state) {
        STATE_UPDATER.set(this, state);

```

### NonProtectedConstructorInAbstractClass
Constructor `Compactor()` of an abstract class should not be declared 'public'
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/Compactor.java`
#### Snippet
```java
    protected final CompactorMXBeanImpl mxBean;

    public Compactor(ServiceConfiguration conf,
                     PulsarClient pulsar,
                     BookKeeper bk,
```

### NonProtectedConstructorInAbstractClass
Constructor `HandlerState()` of an abstract class should not be declared 'public'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/HandlerState.java`
#### Snippet
```java
    };

    public HandlerState(PulsarClientImpl client, String topic) {
        this.client = client;
        this.topic = topic;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractStructSchema()` of an abstract class should not be declared 'public'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AbstractStructSchema.java`
#### Snippet
```java
    protected SchemaInfoProvider schemaInfoProvider;

    public AbstractStructSchema(SchemaInfo schemaInfo) {
        this.schemaInfo = schemaInfo;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AvroBaseStructSchema()` of an abstract class should not be declared 'public'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AvroBaseStructSchema.java`
#### Snippet
```java
    protected final Schema schema;

    public AvroBaseStructSchema(SchemaInfo schemaInfo) {
        super(schemaInfo);
        this.schema = parseAvroSchema(new String(schemaInfo.getSchema(), UTF_8));
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMultiVersionReader()` of an abstract class should not be declared 'public'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/reader/AbstractMultiVersionReader.java`
#### Snippet
```java
            });

    public AbstractMultiVersionReader(SchemaReader<T> providerSchemaReader) {
        this.providerSchemaReader = providerSchemaReader;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMultiVersionAvroBaseReader()` of an abstract class should not be declared 'public'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/reader/AbstractMultiVersionAvroBaseReader.java`
#### Snippet
```java
    protected Schema readerSchema;

    public AbstractMultiVersionAvroBaseReader(SchemaReader<T> providerSchemaReader, Schema readerSchema) {
        super(providerSchemaReader);
        this.readerSchema = readerSchema;
```

### NonProtectedConstructorInAbstractClass
Constructor `BaseGenerateDocumentation()` of an abstract class should not be declared 'public'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/BaseGenerateDocumentation.java`
#### Snippet
```java
    boolean help;

    public BaseGenerateDocumentation() {
        jcommander = new JCommander();
        jcommander.setProgramName("pulsar-generateDocumentation");
```

### NonProtectedConstructorInAbstractClass
Constructor `SslContextAutoRefreshBuilder()` of an abstract class should not be declared 'public'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SslContextAutoRefreshBuilder.java`
#### Snippet
```java
    protected long lastRefreshTime;

    public SslContextAutoRefreshBuilder(
            long certRefreshInSec) {
        this.refreshTime = TimeUnit.SECONDS.toMillis(certRefreshInSec);
```

### NonProtectedConstructorInAbstractClass
Constructor `PulsarHandler()` of an abstract class should not be declared 'public'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/PulsarHandler.java`
#### Snippet
```java
    }

    public PulsarHandler(int keepAliveInterval, TimeUnit unit) {
        this.keepAliveIntervalSeconds = unit.toSeconds(keepAliveInterval);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ComponentImpl()` of an abstract class should not be declared 'public'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
    protected final Function.FunctionDetails.ComponentType componentType;

    public ComponentImpl(Supplier<PulsarWorkerService> workerServiceSupplier,
                         Function.FunctionDetails.ComponentType componentType) {
        this.workerServiceSupplier = workerServiceSupplier;
```

### NonProtectedConstructorInAbstractClass
Constructor `PushPulsarSource()` of an abstract class should not be declared 'public'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/PushPulsarSource.java`
#### Snippet
```java
    private static final int DEFAULT_QUEUE_LENGTH = 1000;

    public PushPulsarSource(PulsarClient pulsarClient,
                            PulsarSourceConfig pulsarSourceConfig,
                            Map<String, String> properties,
```

### NonProtectedConstructorInAbstractClass
Constructor `ComponentStatsManager()` of an abstract class should not be declared 'public'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    }

    public ComponentStatsManager(FunctionCollectorRegistry collectorRegistry,
                                 String[] metricsLabels,
                                 ScheduledExecutorService scheduledExecutorService) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWebSocketHandler()` of an abstract class should not be declared 'public'
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/AbstractWebSocketHandler.java`
#### Snippet
```java
    private static final String PULSAR_AUTH_METHOD_NAME = "X-Pulsar-Auth-Method-Name";

    public AbstractWebSocketHandler(WebSocketService service,
                                    HttpServletRequest request,
                                    ServletUpgradeResponse response) {
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionMetadataStoreState()` of an abstract class should not be declared 'public'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/TransactionMetadataStoreState.java`
#### Snippet
```java
    private volatile State state = null;

    public TransactionMetadataStoreState(State state) {
        STATE_UPDATER.set(this, state);

```

### NonProtectedConstructorInAbstractClass
Constructor `CoordinatorException()` of an abstract class should not be declared 'public'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
    }

    public CoordinatorException(String message, Throwable cause) {
        super(message, cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `CoordinatorException()` of an abstract class should not be declared 'public'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
    private static final long serialVersionUID = 0L;

    public CoordinatorException(String message) {
        super(message);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `CoordinatorException()` of an abstract class should not be declared 'public'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
    }

    public CoordinatorException(Throwable cause) {
        super(cause);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `CmdBase()` of an abstract class should not be declared 'public'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
    private boolean help;

    public CmdBase(String cmdName, Supplier<PulsarAdmin> adminSupplier) {
        this.adminSupplier = adminSupplier;
        jcommander = new JCommander();
```

### NonProtectedConstructorInAbstractClass
Constructor `Validator()` of an abstract class should not be declared 'public'
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/Validator.java`
#### Snippet
```java
 */
public abstract class Validator {
    public Validator(Map<String, Object> params) {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `Validator()` of an abstract class should not be declared 'public'
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/Validator.java`
#### Snippet
```java
    }

    public Validator() {
    }

```

## RuleId[ruleID=JavaReflectionInvocation]
### JavaReflectionInvocation
Argument is not assignable to 'org.conscrypt.ConscryptHostnameVerifier'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                                TrustManager.class,
                                Class.forName("org.conscrypt.ConscryptHostnameVerifier")
                        }).invoke(null, trustManager, defaultHostnameVerifier);
                    }
                }
```

### JavaReflectionInvocation
Argument is not assignable to 'org.conscrypt.ConscryptHostnameVerifier'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                            .getMethod("setDefaultHostnameVerifier",
                                    new Class[]{Class.forName("org.conscrypt.ConscryptHostnameVerifier")});
            setDefaultHostnameVerifierMethod.invoke(null, wrappedHostnameVerifier);
        } catch (Exception e) {
            log.warn("Unable to set default hostname verifier for Conscrypt", e);
```

## RuleId[ruleID=RedundantModifiersValueLombok]
### RedundantModifiersValueLombok
@Value already marks non-static, package-local fields private.
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/BytesWithKafkaSchema.java`
#### Snippet
```java
@Value
public class BytesWithKafkaSchema {
    private final ByteBuffer value;
    private final int schemaId;
}
```

### RedundantModifiersValueLombok
@Value already marks non-static fields final.
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/BytesWithKafkaSchema.java`
#### Snippet
```java
@Value
public class BytesWithKafkaSchema {
    private final ByteBuffer value;
    private final int schemaId;
}
```

### RedundantModifiersValueLombok
@Value already marks non-static, package-local fields private.
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/BytesWithKafkaSchema.java`
#### Snippet
```java
public class BytesWithKafkaSchema {
    private final ByteBuffer value;
    private final int schemaId;
}

```

### RedundantModifiersValueLombok
@Value already marks non-static fields final.
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/BytesWithKafkaSchema.java`
#### Snippet
```java
public class BytesWithKafkaSchema {
    private final ByteBuffer value;
    private final int schemaId;
}

```

### RedundantModifiersValueLombok
@Value already marks non-static, package-local fields private.
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
    private static class PendingReadKey {
        private final long startEntry;
        private final long endEntry;
        long size() {
            return endEntry - startEntry + 1;
```

### RedundantModifiersValueLombok
@Value already marks non-static fields final.
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
    private static class PendingReadKey {
        private final long startEntry;
        private final long endEntry;
        long size() {
            return endEntry - startEntry + 1;
```

### RedundantModifiersValueLombok
@Value already marks non-static, package-local fields private.
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
    @Value
    private static class PendingReadKey {
        private final long startEntry;
        private final long endEntry;
        long size() {
```

### RedundantModifiersValueLombok
@Value already marks non-static fields final.
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
    @Value
    private static class PendingReadKey {
        private final long startEntry;
        private final long endEntry;
        long size() {
```

## RuleId[ruleID=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `pulsar-io/debezium/core/src/main/java/org/apache/pulsar/io/debezium/PulsarDatabaseHistory.java`
#### Snippet
```java
                } catch (IOException ioe) {
                    log.error("Error while deserializing history record '{}'", msg.getValue(), ioe);
                } catch (final Exception e) {
                    throw e;
                }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `wae` is immediately rethrown
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
            log.warn("Unable to get web service url");
            throw new RestException(Status.PRECONDITION_FAILED, "Failed to find ownership for ServiceUnit:" + bundle);
        } catch (WebApplicationException wae) {
            throw wae;
        }
```

## RuleId[ruleID=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `keyList`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcUtils.java`
#### Snippet
```java
                tableId, Lists.newArrayList(), Lists.newArrayList(), Lists.newArrayList());

        keyList = keyList == null ? Collections.emptyList() : keyList;
        nonKeyList = nonKeyList == null ? Collections.emptyList() : nonKeyList;

```

### AssignmentToMethodParameter
Assignment to method parameter `nonKeyList`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcUtils.java`
#### Snippet
```java

        keyList = keyList == null ? Collections.emptyList() : keyList;
        nonKeyList = nonKeyList == null ? Collections.emptyList() : nonKeyList;

        try (ResultSet rs = connection.getMetaData().getColumns(
```

### AssignmentToMethodParameter
Assignment to method parameter `query`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/elastic/ElasticSearchJavaRestClient.java`
#### Snippet
```java
        client.indices().refresh(refreshRequest);

        query = query.replace("/", "\\/");
        return client.search(new SearchRequest.Builder().index(indexName)
                .q(query)
```

### AssignmentToMethodParameter
Assignment to method parameter `messageId`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
        try {
            try {
                messageId = (MessageId) messageId.getClass().getDeclaredMethod("getInnerMessageId").invoke(messageId);
            } catch (NoSuchMethodException noSuchMethodException) {
                // not a TopicMessageIdImpl
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java
                MockZNode parentNode = tree.get(parent);
                int parentVersion = tree.get(parent).getVersion();
                path = path + parentVersion;

                // Update parent version
```

### AssignmentToMethodParameter
Assignment to method parameter `info`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ParserProxyHandler.java`
#### Snippet
```java
                        .append(new String(ByteBufUtil.getBytes(message.getData()), StandardCharsets.UTF_8));
            }
            info = infoBuilder.toString();
        }
        // log conn format is like from source to target
```

### AssignmentToMethodParameter
Assignment to method parameter `quota`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
        }
        if (quota == null) {
            quota = pulsar().getBrokerService().getBacklogQuotaManager().getDefaultQuota();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `policies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
                                @ApiParam(value = "Policies for the namespace") Policies policies) {
        validateNamespaceName(tenant, namespace);
        policies = getDefaultPolicesIfNull(policies);
        internalCreateNamespace(policies)
                .thenAccept(__ -> response.resume(Response.noContent().build()))
```

### AssignmentToMethodParameter
Assignment to method parameter `policies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
    private Policies getDefaultPolicesIfNull(Policies policies) {
        if (policies == null) {
            policies = new Policies();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `listenerName`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/v1/TopicLookup.java`
#### Snippet
```java
        TopicName topicName = getTopicName(topicDomain, property, cluster, namespace, encodedTopic);
        if (StringUtils.isEmpty(listenerName) && StringUtils.isNotEmpty(listenerNameHeader)) {
            listenerName = listenerNameHeader;
        }
        internalLookupTopicAsync(topicName, authoritative, listenerName)
```

### AssignmentToMethodParameter
Assignment to method parameter `listenerName`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/v2/TopicLookup.java`
#### Snippet
```java
        TopicName topicName = getTopicName(topicDomain, tenant, namespace, encodedTopic);
        if (StringUtils.isEmpty(listenerName) && StringUtils.isNotEmpty(listenerNameHeader)) {
            listenerName = listenerNameHeader;
        }
        internalLookupTopicAsync(topicName, authoritative, listenerName)
```

### AssignmentToMethodParameter
Assignment to method parameter `messagesToRead`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java
                                                           long bytesToRead, long availablePermitsOnByte) {
        if (availablePermitsOnMsg > 0) {
            messagesToRead = Math.min(messagesToRead, availablePermitsOnMsg);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `bytesToRead`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java

        if (availablePermitsOnByte > 0) {
            bytesToRead = Math.min(bytesToRead, availablePermitsOnByte);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `subscriptionAuthMode`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
        validateNamespacePolicyOperation(namespaceName, PolicyName.SUBSCRIPTION_AUTH_MODE, PolicyOperation.WRITE);
        validatePoliciesReadOnlyAccess();
        SubscriptionAuthMode authMode = subscriptionAuthMode == null ? subscriptionAuthMode = SubscriptionAuthMode.None
                : subscriptionAuthMode;
        try {
```

### AssignmentToMethodParameter
Assignment to method parameter `subscription`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
            if (subscription != null) {
                if (subscription.startsWith(pulsar().getConfiguration().getReplicatorPrefix())) {
                    subscription = PersistentReplicator.getRemoteCluster(subscription);
                }
                for (Topic topic : topicList) {
```

### AssignmentToMethodParameter
Assignment to method parameter `newSample`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java

    private double timeSmoothValue(double oldValue, double newSample, double minValue, double maxValue, long timePast) {
        newSample = Math.max(minValue, newSample);
        if (maxValue > 0) {
            newSample = Math.min(maxValue, newSample);
```

### AssignmentToMethodParameter
Assignment to method parameter `newSample`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
        newSample = Math.max(minValue, newSample);
        if (maxValue > 0) {
            newSample = Math.min(maxValue, newSample);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `topic`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleStatsImpl.java`
#### Snippet
```java
        String namespace;
        if (StringUtils.isBlank(topic)) {
            namespace = topic = "unknown";
        } else {
            try {
```

### AssignmentToMethodParameter
Assignment to method parameter `argNumBundles`
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
                            && splitBoundaries.get(splitBoundaries.size() - 1) < targetBundle.getUpperEndpoint(),
                    "The given fixed keys must between the key range of the %s bundle", targetBundle);
            argNumBundles = splitBoundaries.size() + 1;
        }
        Objects.requireNonNull(targetBundle, "can't split null bundle");
```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
            return true;
        } else {
            position = individualAckPositions.get(position).left;
            if (position.hasAckSet()) {
                BitSetRecyclable bitSetRecyclable = BitSetRecyclable.valueOf(position.getAckSet());
```

### AssignmentToMethodParameter
Assignment to method parameter `msgId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageIdImpl.java`
#### Snippet
```java
    protected MessageIdData writeMessageIdData(MessageIdData msgId, int batchIndex, int batchSize) {
        if (msgId == null) {
            msgId = LOCAL_MESSAGE_ID.get()
                    .clear();
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `messageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/NegativeAcksTracker.java`
#### Snippet
```java
        if (messageId instanceof TopicMessageIdImpl) {
            TopicMessageIdImpl topicMessageId = (TopicMessageIdImpl) messageId;
            messageId = topicMessageId.getInnerMessageId();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `messageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/NegativeAcksTracker.java`
#### Snippet
```java
        if (messageId instanceof BatchMessageIdImpl) {
            BatchMessageIdImpl batchMessageId = (BatchMessageIdImpl) messageId;
            messageId = new MessageIdImpl(batchMessageId.getLedgerId(), batchMessageId.getEntryId(),
                    batchMessageId.getPartitionIndex());
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `schema`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                return FutureUtil.failedFuture(e.getCause());
            }
            schema = schema.clone();
            if (schema.requireFetchingSchemaInfo()) {
                @SuppressWarnings("rawtypes") Schema finalSchema = schema;
```

### AssignmentToMethodParameter
Assignment to method parameter `from`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            if (from != null) {
                if (op.msg == from) {
                    from = null;
                } else {
                    continue;
```

### AssignmentToMethodParameter
Assignment to method parameter `schemaVersion`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
    public void fetchSchemaIfNeeded(SchemaVersion schemaVersion) throws SchemaSerializationException {
        if (schemaVersion == null) {
            schemaVersion = BytesSchemaVersion.of(new byte[0]);
        }
        if (!schemaMap.containsKey(schemaVersion)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bytes`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericProtobufNativeReader.java`
#### Snippet
```java
        try {
            if (!(bytes.length == length && offset == 0)) { //skip unnecessary bytes copy
                bytes = Arrays.copyOfRange(bytes, offset, offset + length);
            }
            return new GenericProtobufNativeRecord(schemaVersion, descriptor, fields,
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/JsonRecordBuilderImpl.java`
#### Snippet
```java
            }
            GenericJsonRecord genericJsonRecord = (GenericJsonRecord) value;
            value = genericJsonRecord.getJsonNode();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroReader.java`
#### Snippet
```java
        try {
            if (offset == 0 && this.offset > 0) {
                offset = this.offset;
            }
            Decoder decoder = DecoderFactory.get().binaryDecoder(bytes, offset, length - offset, null);
```

### AssignmentToMethodParameter
Assignment to method parameter `schemaVersion`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaImpl.java`
#### Snippet
```java
        setSchemaInfoProviderOnSubschemas();
        if (schemaVersion == null) {
            schemaVersion = BytesSchemaVersion.of(new byte[0]);
        }
        if (schemaInfoProvider == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java

    private int fmix(int h) {
        h ^= h >>> 16;
        h *= 0x85ebca6b;
        h ^= h >>> 13;
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java
    private int fmix(int h) {
        h ^= h >>> 16;
        h *= 0x85ebca6b;
        h ^= h >>> 13;
        h *= 0xc2b2ae35;
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java
        h ^= h >>> 16;
        h *= 0x85ebca6b;
        h ^= h >>> 13;
        h *= 0xc2b2ae35;
        h ^= h >>> 16;
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java
        h *= 0x85ebca6b;
        h ^= h >>> 13;
        h *= 0xc2b2ae35;
        h ^= h >>> 16;

```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java
        h ^= h >>> 13;
        h *= 0xc2b2ae35;
        h ^= h >>> 16;

        return h;
```

### AssignmentToMethodParameter
Assignment to method parameter `h1`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java

    private int mixH1(int h1, int k1) {
        h1 ^= k1;
        h1 = Integer.rotateLeft(h1, 13);
        return h1 * 5 + 0xe6546b64;
```

### AssignmentToMethodParameter
Assignment to method parameter `h1`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java
    private int mixH1(int h1, int k1) {
        h1 ^= k1;
        h1 = Integer.rotateLeft(h1, 13);
        return h1 * 5 + 0xe6546b64;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `k1`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java

    private int mixK1(int k1) {
        k1 *= C1;
        k1 = Integer.rotateLeft(k1, 15);
        k1 *= C2;
```

### AssignmentToMethodParameter
Assignment to method parameter `k1`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java
    private int mixK1(int k1) {
        k1 *= C1;
        k1 = Integer.rotateLeft(k1, 15);
        k1 *= C2;
        return k1;
```

### AssignmentToMethodParameter
Assignment to method parameter `k1`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Murmur3_32Hash.java`
#### Snippet
```java
        k1 *= C1;
        k1 = Integer.rotateLeft(k1, 15);
        k1 *= C2;
        return k1;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `args`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
        try {
            if (args == null) {
                args = new String[]{};
            }
            tmpCmd.parse(args);
```

### AssignmentToMethodParameter
Assignment to method parameter `to`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
        }

        to = (Class<T>) wrap(to);
        // Can we cast? Then just do it.
        if (to.isAssignableFrom(from.getClass())) {
```

### AssignmentToMethodParameter
Assignment to method parameter `num`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NumberFormat.java`
#### Snippet
```java
        while (num != 0) {
            out.writeByte((int) ('0' + num % 10));
            num /= 10;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `num`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NumberFormat.java`
#### Snippet
```java
        if (num < 0) {
            out.writeByte('-');
            num += encounteredMinValue ? 1 : 0;
            num *= -1;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `num`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NumberFormat.java`
#### Snippet
```java
            out.writeByte('-');
            num += encounteredMinValue ? 1 : 0;
            num *= -1;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `tupleIdx`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/TripleLongPriorityQueue.java`
#### Snippet
```java

            swap(tupleIdx, parentIdx);
            tupleIdx = parentIdx;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `tupleIdx`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/TripleLongPriorityQueue.java`
#### Snippet
```java

            swap(tupleIdx, swapIdx);
            tupleIdx = swapIdx;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java

                        if (value == null) {
                            value = valueProvider.apply(key);
                        }

```

### AssignmentToMethodParameter
Assignment to method parameter `bucket`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongLongPairHashMap.java`
#### Snippet
```java

                // Cleanup all the buckets that were in `DeletedKey` state, so that we can reduce unnecessary expansions
                bucket = (bucket - 4) & (table.length - 1);
                while (table[bucket] == DeletedKey) {
                    table[bucket] = EmptyKey;
```

### AssignmentToMethodParameter
Assignment to method parameter `bucket`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongLongPairHashMap.java`
#### Snippet
```java
                    --usedBuckets;

                    bucket = (bucket - 4) & (table.length - 1);
                }
            } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `toIndex`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
        // An optimization
        if (toIndex > len)
            toIndex = len;

        BitSetRecyclable result = new BitSetRecyclable(toIndex - fromIndex);
```

### AssignmentToMethodParameter
Assignment to method parameter `toIndex`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
        int endWordIndex = wordIndex(toIndex - 1);
        if (endWordIndex >= wordsInUse) {
            toIndex = length();
            endWordIndex = wordsInUse - 1;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `bb`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
     */
    public static BitSetRecyclable valueOf(ByteBuffer bb) {
        bb = bb.slice().order(ByteOrder.LITTLE_ENDIAN);
        int n;
        for (n = bb.remaining(); n > 0 && bb.get(n - 1) == 0; n--)
```

### AssignmentToMethodParameter
Assignment to method parameter `lb`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
     */
    public static BitSetRecyclable valueOf(LongBuffer lb) {
        lb = lb.slice();
        int n;
        for (n = lb.remaining(); n > 0 && lb.get(n - 1) == 0; n--)
```

### AssignmentToMethodParameter
Assignment to method parameter `completeTopicName`
in `pulsar-common/src/main/java/org/apache/pulsar/common/naming/TopicName.java`
#### Snippet
```java
                String[] parts = StringUtils.split(completeTopicName, '/');
                if (parts.length == 3) {
                    completeTopicName = TopicDomain.persistent.name() + "://" + completeTopicName;
                } else if (parts.length == 1) {
                    completeTopicName = TopicDomain.persistent.name() + "://"
```

### AssignmentToMethodParameter
Assignment to method parameter `completeTopicName`
in `pulsar-common/src/main/java/org/apache/pulsar/common/naming/TopicName.java`
#### Snippet
```java
                    completeTopicName = TopicDomain.persistent.name() + "://" + completeTopicName;
                } else if (parts.length == 1) {
                    completeTopicName = TopicDomain.persistent.name() + "://"
                        + PUBLIC_TENANT + "/" + DEFAULT_NAMESPACE + "/" + parts[0];
                } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `nsLevelPolicies`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
        }
        if (nsLevelPolicies == null) {
            nsLevelPolicies = new OffloadPoliciesImpl();
        }
        if (nsLevelPolicies.getManagedLedgerOffloadThresholdInBytes() == null
```

### AssignmentToMethodParameter
Assignment to method parameter `compressedPayload`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

        compressedPayload.release();
        compressedPayload = chunkedMsgCtx.chunkedMsgBuffer;
        ByteBuf uncompressedPayload = uncompressPayloadIfNeeded(messageId, msgMetadata, compressedPayload, cnx, false);
        compressedPayload.release();
```

### AssignmentToMethodParameter
Assignment to method parameter `delayTime`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        }
        if (delayTime < 0) {
            delayTime = 0;
        }
        if (retryLetterProducer == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `messageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        if (possibleSendToDeadLetterTopicMessages != null) {
            if (messageId instanceof BatchMessageIdImpl) {
                messageId = new MessageIdImpl(messageId.getLedgerId(), messageId.getEntryId(),
                        getPartitionIndex());
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/schema/BytesSchemaVersion.java`
#### Snippet
```java

        if (off + len > b.length) {
            len = b.length - off;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `encodedNio`
in `pulsar-common/src/main/java/org/apache/pulsar/common/compression/AirliftUtils.java`
#### Snippet
```java
            copy.put(encodedNio);
            copy.flip();
            encodedNio = copy;
        }
        return encodedNio;
```

### AssignmentToMethodParameter
Assignment to method parameter `topic`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/LedgerOffloaderStatsImpl.java`
#### Snippet
```java

    private void addOrUpdateTopicAccess(String topic) {
        topic = StringUtils.isBlank(topic) ? UNKNOWN : topic;
        this.topicAccess.put(topic, System.currentTimeMillis());
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `topic`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/LedgerOffloaderStatsImpl.java`
#### Snippet
```java
    @Override
    public void recordReadOffloadBytes(String topic, long size) {
        topic = StringUtils.isBlank(topic) ? UNKNOWN : topic;
        Pair<LongAdder, LongAdder> pair = this.offloadAndReadOffloadBytesMap
                .computeIfAbsent(topic, __ -> new ImmutablePair<>(new LongAdder(), new LongAdder()));
```

### AssignmentToMethodParameter
Assignment to method parameter `topic`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/LedgerOffloaderStatsImpl.java`
#### Snippet
```java
    @Override
    public void recordOffloadBytes(String topic, long size) {
        topic = StringUtils.isBlank(topic) ? UNKNOWN : topic;
        Pair<LongAdder, LongAdder> pair = this.offloadAndReadOffloadBytesMap
                .computeIfAbsent(topic, __ -> new ImmutablePair<>(new LongAdder(), new LongAdder()));
```

### AssignmentToMethodParameter
Assignment to method parameter `maxPosition`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpReadEntry.java`
#### Snippet
```java
        op.entries = new ArrayList<>();
        if (maxPosition == null) {
            maxPosition = PositionImpl.LATEST;
        }
        op.maxPosition = maxPosition;
```

### AssignmentToMethodParameter
Assignment to method parameter `ledgerEntry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheManagerImpl.java`
#### Snippet
```java
                    .processPayloadBeforeEntryCache(duplicateBuffer);
            if (processorHandle != null) {
                ledgerEntry  = LedgerEntryImpl.create(ledgerEntry.getLedgerId(), ledgerEntry.getEntryId(),
                        ledgerEntry.getLength(), processorHandle.getProcessedPayload());
            } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockUtils.java`
#### Snippet
```java
        }
        if (name.endsWith("-index")) {
            name = name.substring(0, name.length() - "-index".length());
        }
        int pos = name.indexOf("-ledger-");
```

### AssignmentToMethodParameter
Assignment to method parameter `lastEntry`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java
                groupedReaders.add(new GroupedReader(ledgerId, startEntryId, lastEntry, index, inputStreams.get(i),
                        dataStreams.get(i)));
                lastEntry = startEntryId - 1;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/LocalMemoryMetadataStore.java`
#### Snippet
```java

            if (options.contains(CreateOption.Sequential)) {
                path += Long.toString(sequentialIdGenerator.getAndIncrement());
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/RocksdbMetadataStore.java`
#### Snippet
```java
                    metaValue.ephemeral = options.contains(CreateOption.Ephemeral);
                    if (options.contains(CreateOption.Sequential)) {
                        path += sequentialIdGenerator.getAndIncrement();
                        pathBytes = toBytes(path);
                        transaction.put(SEQUENTIAL_ID_KEY, toBytes(sequentialIdGenerator.get()));
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BookieServiceInfoSerde.java`
#### Snippet
```java
        // src/main/java/org/apache/bookkeeper/discover/ZKRegistrationClient.java#L258
        if (path == null) {
            path = "";
        }
        int last = path.lastIndexOf("/");
```

### AssignmentToMethodParameter
Assignment to method parameter `error`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/Exceptions.java`
#### Snippet
```java
                }
            }
            error = error.getCause();
        }
        return false;
```

### AssignmentToMethodParameter
Assignment to method parameter `functionDetails`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java

    public static FunctionConfig convertFromDetails(FunctionDetails functionDetails) {
        functionDetails = validateFunctionDetails(functionDetails);
        FunctionConfig functionConfig = new FunctionConfig();
        functionConfig.setTenant(functionDetails.getTenant());
```

### AssignmentToMethodParameter
Assignment to method parameter `rootClassLoader`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntimeFactory.java`
#### Snippet
```java

        if (rootClassLoader == null) {
            rootClassLoader = Thread.currentThread().getContextClassLoader();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                        position);
            }
            position = nextExistingLedger != null ? PositionImpl.get(nextExistingLedger, -1) : position;
        }
        if (position.compareTo(ledger.getLastPosition()) > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            log.warn("[{}] [{}] Current position {} is ahead of last position {}", ledger.getName(), name, position,
                    ledger.getLastPosition());
            position = ledger.getLastPosition();
        }
        log.info("[{}] Cursor {} recovered to position {}", ledger.getName(), name, position);
```

### AssignmentToMethodParameter
Assignment to method parameter `newMarkDeletePosition`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                    Range<PositionImpl> rangeToBeMarkDeleted = individualDeletedMessages.rangeContaining(
                            positionAfterNewMarkDelete.getLedgerId(), positionAfterNewMarkDelete.getEntryId());
                    newMarkDeletePosition = rangeToBeMarkDeleted.upperEndpoint();
                    positionAfterNewMarkDelete = ledger.getNextValidPosition(newMarkDeletePosition);
                    // check if next valid position is also deleted and part of the deleted-range
```

### AssignmentToMethodParameter
Assignment to method parameter `archive`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
        if (!StringUtils.isEmpty(archive)) {
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

                FunctionArchive function = worker().getFunctionsManager().getFunction(archive);
```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            // The ledger pointed by this position does not exist anymore. It was deleted because it was empty. We need
            // to skip on the next available ledger
            position = new PositionImpl(ledgerId, 0);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `msg`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
    // wrap an exception to enriching more info messages.
    public static Throwable wrap(Throwable t, String msg) {
        msg += "\n" + t.getMessage();
        // wrap an exception with new message info
        if (t instanceof TimeoutException) {
```

### AssignmentToMethodParameter
Assignment to method parameter `target`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/WebTargets.java`
#### Snippet
```java
                    encode = part;
                }
                target = target.path(encode);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `args`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java

        PulsarAdminTool tool = new PulsarAdminTool(properties);
        args = Arrays.copyOfRange(args, 1, args.length);
        if (tool.run(args)) {
            exit(0);
```

### AssignmentToMethodParameter
Assignment to method parameter `namespaces`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java

        // validate
        namespaces = validateList(namespaces);
        if (namespaces.isEmpty()) {
            throw new ParameterException("unable to parse namespaces parameter list: " + namespaces);
```

### AssignmentToMethodParameter
Assignment to method parameter `primary`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java
        }

        primary = validateList(primary);
        if (primary.isEmpty()) {
            throw new ParameterException("unable to parse primary parameter list: " + namespaces);
```

### AssignmentToMethodParameter
Assignment to method parameter `secondary`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java
        }

        secondary = validateList(secondary);

        // System.out.println("namespaces = " + namespaces);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
        }
        if (value instanceof byte[]) {
            value = interpretByteArray(displayHex, (byte[]) value);
        }
        return ImmutableMap.of("value", value.toString(), "type", value.getClass());
```

### AssignmentToMethodParameter
Assignment to method parameter `excludeBookies`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
        Set<BookieId> blacklistedBookies = getBlacklistedBookies(ensembleSize, customMetadata);
        if (excludeBookies == null) {
            excludeBookies = new HashSet<BookieId>();
        }
        excludeBookies.addAll(blacklistedBookies);
```

### AssignmentToMethodParameter
Assignment to method parameter `excludeBookies`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
        Set<BookieId> blacklistedBookies = getBlacklistedBookies(ensembleSize, customMetadata);
        if (excludeBookies == null) {
            excludeBookies = new HashSet<BookieId>();
        }
        excludeBookies.addAll(blacklistedBookies);
```

### AssignmentToMethodParameter
Assignment to method parameter `ledgers`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        long suffixSize = 0L;

        ledgers = Lists.reverse(ledgers);
        long previousLedger = ledgers.get(0).ledgerId;
        for (PersistentTopicInternalStats.LedgerInfo l : ledgers) {
```

### AssignmentToMethodParameter
Assignment to method parameter `string`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
    private static String substituteVariables(String string, Map<String, String> vars) {
        for (Substitutor stringSubstitutor : SUBSTITUTORS) {
            string = stringSubstitutor.replace(string, vars);
        }
        return string;
```

### AssignmentToMethodParameter
Assignment to method parameter `tn`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
        public CompletableFuture<Boolean> isPartitionedTopicBeingDeletedAsync(TopicName tn) {
            if (tn.isPartitioned()) {
                tn = TopicName.get(tn.getPartitionedTopicName());
            }
            return getPartitionedTopicMetadataAsync(tn)
```

### AssignmentToMethodParameter
Assignment to method parameter `authData`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationService.java`
#### Snippet
```java
                if (authData == null) {
                    AuthenticationState authenticationState = providerToUse.newHttpAuthState(request);
                    authData = authenticationState.getAuthDataSource();
                }
                // Backward compatible, the authData value was null in the previous implementation
```

### AssignmentToMethodParameter
Assignment to method parameter `keyConfUrl`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/utils/AuthTokenUtils.java`
#### Snippet
```java
            try {
                if (keyConfUrl.startsWith("file:")) {
                    keyConfUrl = keyConfUrl.trim();
                }
                return IOUtils.toByteArray(URL.createURL(keyConfUrl));
```

### AssignmentToMethodParameter
Assignment to method parameter `properties`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
        WebTarget path = topicPath(tn, "properties");
        if (properties == null) {
            properties = new HashMap<>();
        }
        return asyncPutRequest(path, Entity.entity(properties, MediaType.APPLICATION_JSON));
```

### AssignmentToMethodParameter
Assignment to method parameter `subscriptionProperties`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
                "properties");
        if (subscriptionProperties == null) {
            subscriptionProperties = new HashMap<>();
        }
        return asyncPutRequest(path, Entity.entity(subscriptionProperties, MediaType.APPLICATION_JSON));
```

### AssignmentToMethodParameter
Assignment to method parameter `properties`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
        TopicName tn = validateTopic(topic);
        WebTarget path = topicPath(tn);
        properties = properties == null ? new HashMap<>() : properties;
        return asyncPutRequest(path, Entity.entity(properties, MediaType.APPLICATION_JSON));
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `previousRespHeaders`
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
        // role token expired in last check. remove role token, new sasl client, restart auth.
        if (isRoleTokenExpired(previousRespHeaders)) {
            previousRespHeaders = null;
            saslRoleToken = null;
            authData = getAuthData(hostName);
```

### AssignmentToMethodParameter
Assignment to method parameter `authData`
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
            previousRespHeaders = null;
            saslRoleToken = null;
            authData = getAuthData(hostName);
        }

```

## RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `namespaceToBundleRange`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
                                                ConcurrentOpenHashSet<String>>newBuilder()
                                                .build());
                synchronized (namespaceToBundleRange) {
                    namespaceToBundleRange.computeIfAbsent(namespaceName,
                            k -> ConcurrentOpenHashSet.<String>newBuilder().build())
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `preallocatedBundleData`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
            // Remove all loaded bundles from the preallocated maps.
            final Map<String, BundleData> preallocatedBundleData = brokerData.getPreallocatedBundleData();
            synchronized (preallocatedBundleData) {
                for (String preallocatedBundleName : brokerData.getPreallocatedBundleData().keySet()) {
                    if (brokerData.getLocalData().getBundles().contains(preallocatedBundleName)) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `namespaceToBundleRange`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
                                            ConcurrentOpenHashSet<String>>newBuilder()
                                            .build());
            synchronized (namespaceToBundleRange) {
                namespaceToBundleRange.clear();
                LoadManagerShared.fillNamespaceToBundlesMap(statsMap.keySet(), namespaceToBundleRange);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `txnBuffer`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
        try {
            TxnBuffer txnBuffer = getTxnBufferOrThrowNotFoundException(txnID);
            synchronized (txnBuffer) {
                // the committed position should be generated
                long committedAtLedgerId = -1L;
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `localLock`
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarUnpacker.java`
#### Snippet
```java
        // prevent OverlappingFileLockException by ensuring that one thread tries to create a lock in this JVM
        Object localLock = CURRENT_JVM_FILE_LOCKS.computeIfAbsent(lockFile.getAbsolutePath(), key -> new Object());
        synchronized (localLock) {
            // create file lock that ensures that other processes
            // using the same lock file don't execute concurrently
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `ledgerCache`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
    private FindPendingReadOutcome findPendingRead(PendingReadKey key, Map<PendingReadKey,
            PendingRead> ledgerCache, AtomicBoolean created) {
        synchronized (ledgerCache) {
            PendingRead existing = ledgerCache.get(key);
            if (existing != null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `store`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/RocksdbMetadataStore.java`
#### Snippet
```java
        RocksdbMetadataStore store = instancesCache.get(metadataStoreUri);
        if (store != null) {
            synchronized (store) {
                if (store.referenceCount > 0) {
                    // Reuse the same store instance
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `listenerSet`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
                    // notify `null` as indicator that a ledger is deleted
                    // make this behavior consistent with `NodeDeleted` watched event.
                    synchronized (listenerSet) {
                        for (BookkeeperInternalCallbacks.LedgerMetadataListener listener : listenerSet) {
                            listener.onChanged(ledgerId, null);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `listenerSet`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
            return;
        }
        synchronized (listenerSet) {
            if (listenerSet.remove(listener)) {
                if (log.isDebugEnabled()) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `listenerSet`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
        Set<BookkeeperInternalCallbacks.LedgerMetadataListener> listenerSet =
                listeners.computeIfAbsent(ledgerId, k -> new HashSet<>());
        synchronized (listenerSet) {
            listenerSet.add(listener);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `listenerSet`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
                Set<BookkeeperInternalCallbacks.LedgerMetadataListener> listenerSet = listeners.get(ledgerId);
                if (listenerSet != null) {
                    synchronized (listenerSet) {
                        if (log.isDebugEnabled()) {
                            log.debug("Removed ledger metadata listeners on ledger {} : {}",
```

## RuleId[ruleID=ReturnNull]
### ReturnNull
Return of `null`
in `pulsar-io/core/src/main/java/org/apache/pulsar/io/core/BatchPushSource.java`
#### Snippet
```java
        }
        if (record instanceof NullRecord) {
            return null;
        } else {
            return record;
```

### ReturnNull
Return of `null`
in `pulsar-io/core/src/main/java/org/apache/pulsar/io/core/BatchPushSource.java`
#### Snippet
```java
        @Override
        public Object getValue() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-io/core/src/main/java/org/apache/pulsar/io/core/BatchPushSource.java`
#### Snippet
```java
        @Override
        public Object getValue() {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java
    private Stream<String> getLines(File file) throws IOException {
        if (file == null) {
            return null;
        } else if (GZipFiles.isGzip(file)) {
            return GZipFiles.lines(Paths.get(file.getAbsolutePath()));
```

### ReturnNull
Return of `null`
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
    public static JsonNode toJson(GenericRecord genericRecord) {
        if (genericRecord == null) {
            return null;
        }
        ObjectNode objectNode = jsonNodeFactory.objectNode();
```

### ReturnNull
Return of `null`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
    static Object convertAvroField(Object avroValue, Schema schema) {
        if (avroValue == null) {
            return null;
        }
        switch (schema.getType()) {
```

### ReturnNull
Return of `null`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
    private static Object getValueFromJsonNode(final JsonNode fn) {
        if (fn == null || fn.isNull()) {
            return null;
        }
        if (fn.isContainerNode()) {
```

### ReturnNull
Return of `null`
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/MessageUtils.java`
#### Snippet
```java
        try {
            if (message == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConnector.java`
#### Snippet
```java
                    clazz = Class.forName(name, true, classLoader);
                } catch (ClassNotFoundException | NoClassDefFoundError e) {
                    return null;
                }
                // two putters can race here, but they'll put the same class
```

### ReturnNull
Return of `null`
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConnector.java`
#### Snippet
```java
    protected CompressionCodec getCompressionCodec() {
       if (connectorConfig.getCompression() == null) {
           return null;
       }

```

### ReturnNull
Return of `null`
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConnector.java`
#### Snippet
```java
                    clazz = Class.forName(name, true, classLoader);
                } catch (ClassNotFoundException | NoClassDefFoundError e) {
                    return null;
                }
                // two putters can race here, but they'll put the same class
```

### ReturnNull
Return of `null`
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConnector.java`
#### Snippet
```java
    protected CompressionCodec getCompressionCodec() {
       if (connectorConfig.getCompression() == null) {
           return null;
       }

```

### ReturnNull
Return of `null`
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaBytesSource.java`
#### Snippet
```java
        // BytesWithKafkaSchema in case of ExtractKafkaAvroSchemaDeserializer
        if (value == null) {
            return null;
        } else if (value instanceof BytesWithKafkaSchema) {
            return ((BytesWithKafkaSchema) value).getValue();
```

### ReturnNull
Return of `null`
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaBytesSource.java`
#### Snippet
```java
        public BytesWithKafkaSchema deserialize(String topic, byte[] payload) {
            if (payload == null) {
                return null;
            } else {
                try {
```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionKey.java`
#### Snippet
```java
  public ByteBuffer valueInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 6, 1); }
  public KeyValue metadata(int j) { return metadata(new KeyValue(), j); }
  public KeyValue metadata(KeyValue obj, int j) { int o = __offset(8); return o != 0 ? obj.__assign(__indirect(__vector(o) + j * 4), bb) : null; }
  public int metadataLength() { int o = __offset(8); return o != 0 ? __vector_len(o) : 0; }

```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionKey.java`
#### Snippet
```java
  public EncryptionKey __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  public String key() { int o = __offset(4); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer keyAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer keyInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/KeyValue.java`
#### Snippet
```java
  public KeyValue __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  public String key() { int o = __offset(4); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer keyAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer keyInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/KeyValue.java`
#### Snippet
```java
  public ByteBuffer keyAsByteBuffer() { return __vector_as_bytebuffer(4, 1); }
  public ByteBuffer keyInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 4, 1); }
  public String value() { int o = __offset(6); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer valueAsByteBuffer() { return __vector_as_bytebuffer(6, 1); }
  public ByteBuffer valueInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 6, 1); }
```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/Message.java`
#### Snippet
```java

  public EncryptionCtx encryptionCtx() { return encryptionCtx(new EncryptionCtx()); }
  public EncryptionCtx encryptionCtx(EncryptionCtx obj) { int o = __offset(4); return o != 0 ? obj.__assign(__indirect(o + bb_pos), bb) : null; }
  public KeyValue properties(int j) { return properties(new KeyValue(), j); }
  public KeyValue properties(KeyValue obj, int j) { int o = __offset(6); return o != 0 ? obj.__assign(__indirect(__vector(o) + j * 4), bb) : null; }
```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/Message.java`
#### Snippet
```java
  public EncryptionCtx encryptionCtx(EncryptionCtx obj) { int o = __offset(4); return o != 0 ? obj.__assign(__indirect(o + bb_pos), bb) : null; }
  public KeyValue properties(int j) { return properties(new KeyValue(), j); }
  public KeyValue properties(KeyValue obj, int j) { int o = __offset(6); return o != 0 ? obj.__assign(__indirect(__vector(o) + j * 4), bb) : null; }
  public int propertiesLength() { int o = __offset(6); return o != 0 ? __vector_len(o) : 0; }
  public byte payload(int j) { int o = __offset(8); return o != 0 ? bb.get(__vector(o) + j * 1) : 0; }
```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionCtx.java`
#### Snippet
```java
  public ByteBuffer paramAsByteBuffer() { return __vector_as_bytebuffer(6, 1); }
  public ByteBuffer paramInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 6, 1); }
  public String algo() { int o = __offset(8); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer algoAsByteBuffer() { return __vector_as_bytebuffer(8, 1); }
  public ByteBuffer algoInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 8, 1); }
```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionCtx.java`
#### Snippet
```java

  public EncryptionKey keys(int j) { return keys(new EncryptionKey(), j); }
  public EncryptionKey keys(EncryptionKey obj, int j) { int o = __offset(4); return o != 0 ? obj.__assign(__indirect(__vector(o) + j * 4), bb) : null; }
  public int keysLength() { int o = __offset(4); return o != 0 ? __vector_len(o) : 0; }
  public byte param(int j) { int o = __offset(6); return o != 0 ? bb.get(__vector(o) + j * 1) : 0; }
```

### ReturnNull
Return of `null`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
    public static JsonNode toJson(GenericRecord genericRecord) {
        if (genericRecord == null) {
            return null;
        }
        ObjectNode objectNode = jsonNodeFactory.objectNode();
```

### ReturnNull
Return of `null`
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/BatchSink.java`
#### Snippet
```java
            return record.getField(fieldMatch);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
    public static JsonNode toJson(GenericRecord genericRecord) {
        if (genericRecord == null) {
            return null;
        }
        ObjectNode objectNode = jsonNodeFactory.objectNode();
```

### ReturnNull
Return of `null`
in `pulsar-io/batch-data-generator/src/main/java/org/apache/pulsar/io/batchdatagenerator/BatchDataGeneratorSource.java`
#### Snippet
```java
            return () -> new Person(fairy.person());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/elastic/ElasticBulkProcessor.java`
#### Snippet
```java
                listener.afterBulk(executionId, convertBulkRequest(bulkRequest), convertBulkResponse(bulkResponse));
                listenerCalledPromise.complete(null);
                return null;
            }).exceptionally(ex -> {
                this.semaphore.release();
```

### ReturnNull
Return of `null`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/elastic/ElasticBulkProcessor.java`
#### Snippet
```java
                log.warn("Failed to execute bulk request " + executionId, ex);
                listenerCalledPromise.complete(null);
                return null;
            });
            if (config.getBulkConcurrentRequests() == 0) {
```

### ReturnNull
Return of `null`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/RestClient.java`
#### Snippet
```java
        private CredentialsProvider buildCredentialsProvider(ElasticSearchConfig config) {
            if (StringUtils.isEmpty(config.getUsername()) || StringUtils.isEmpty(config.getPassword())) {
                return null;
            }
            CredentialsProvider credentialsProvider = new BasicCredentialsProvider();
```

### ReturnNull
Return of `null`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/RestClient.java`
#### Snippet
```java
        private List<Header> buildDefaultHeaders(ElasticSearchConfig config) {
            if (StringUtils.isEmpty(config.getToken()) && StringUtils.isEmpty(config.getApiKey())) {
                return null;
            }
            List<Header> headers = new ArrayList<>();
```

### ReturnNull
Return of `null`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaSchemaWrappedSchema.java`
#### Snippet
```java
    @Override
    public Schema<byte[]> clone() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
    public static JsonNode extractJsonNode(Schema<?> schema, Object val) {
        if (val == null) {
            return null;
        }
        switch (schema.getSchemaInfo().getType()) {
```

### ReturnNull
Return of `null`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
        if (kafkaSchema == null) {
            if (jsonNode == null || jsonNode.isNull()) {
                return null;
            }
            throw new DataException("Don't know how to convert " + jsonNode
```

### ReturnNull
Return of `null`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
        if (kafkaSchema == null) {
            if (avroRecord == null) {
                return null;
            }
            throw new DataException("Don't know how to convert " + avroRecord + " to Connect data (schema is null).");
```

### ReturnNull
Return of `null`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
        }
        if (kafkaSchema.isOptional()) {
            return null;
        }
        throw new DataException("Invalid null value for required " + kafkaSchema.type() + " field");
```

### ReturnNull
Return of `null`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
        if (kafkaSchema == null) {
            if (genericRecord == null) {
                return null;
            }
            throw new DataException("Don't know how to convert " + genericRecord + " to Connect data (schema is null).");
```

### ReturnNull
Return of `null`
in `testmocks/src/main/java/org/apache/bookkeeper/test/ServerTester.java`
#### Snippet
```java
            return autoRecovery.getAuditor();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `testmocks/src/main/java/org/apache/bookkeeper/test/ServerTester.java`
#### Snippet
```java
            return autoRecovery.getReplicationWorker();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `testmocks/src/main/java/org/apache/bookkeeper/client/TestStatsProvider.java`
#### Snippet
```java
        public OpStatsData toOpStatsData() {
            // Not supported at this time
            return null;
        }

```

### ReturnNull
Return of `null`
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
        @Override
        public LedgerManagerFactory getLedgerManagerFactory() throws MetadataException {
            return null;
        }

```

### ReturnNull
Return of `null`
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
        @Override
        public LayoutManager getLayoutManager() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
        } catch (Throwable t) {
            log.error("Exception:", t);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `buildtools/src/main/java/org/apache/pulsar/tests/MockitoThreadLocalStateCleaner.java`
#### Snippet
```java
                LOG.warn("Cannot find Mockito's ThreadSafeMockingProgress.MOCKING_PROGRESS_PROVIDER field."
                        + " This might be due to using an unsupported Mockito version.");
                return null;
            }
        } catch (IllegalAccessException | ClassNotFoundException e) {
```

### ReturnNull
Return of `null`
in `buildtools/src/main/java/org/apache/pulsar/tests/MockitoThreadLocalStateCleaner.java`
#### Snippet
```java
        } catch (IllegalAccessException | ClassNotFoundException e) {
            LOG.warn("Cannot find Mockito's ThreadSafeMockingProgress.MOCKING_PROGRESS_PROVIDER thread local", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadDumpUtil.java`
#### Snippet
```java
            return stringWriter.toString();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `buildtools/src/main/java/org/apache/pulsar/tests/RetryAnalyzer.java`
#### Snippet
```java
                                            .replace('/', '.');
                                } else {
                                    return null;
                                }
                            })
```

### ReturnNull
Return of `null`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
            } catch (NoSuchMethodException noSuchMethodException) {
                // not a BatchMessageIdImpl, returning null to use the standard sequenceId
                return null;
            }

```

### ReturnNull
Return of `null`
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadLocalStateCleaner.java`
#### Snippet
```java
            return (T) threadLocalEntryValueField.get(entry);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadLocalStateCleaner.java`
#### Snippet
```java
            return getCurrentValue(threadLocal, threadLocalMap);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `buildtools/src/main/java/org/apache/pulsar/tests/FastThreadLocalStateCleaner.java`
#### Snippet
```java
                LOG.warn("Cannot find InternalThreadLocalMap.slowThreadLocalMap field."
                        + " This might be due to using an unsupported netty-common version.");
                return null;
            }
        } catch (IllegalAccessException | ClassNotFoundException e) {
```

### ReturnNull
Return of `null`
in `buildtools/src/main/java/org/apache/pulsar/tests/FastThreadLocalStateCleaner.java`
#### Snippet
```java
        } catch (IllegalAccessException | ClassNotFoundException e) {
            LOG.warn("Cannot find InternalThreadLocalMap.slowThreadLocalMap thread local", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java
                return createStatForZNode(tree.get(path));
            } else {
                return null;
            }
        } finally {
```

### ReturnNull
Return of `null`
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java
                return createStatForZNode(tree.get(path));
            } else {
                return null;
            }
        } finally {
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java

        if (schemaTableName.getSchemaName().equals(INFORMATION_SCHEMA)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/primitive/PulsarPrimitiveRowDecoderFactory.java`
#### Snippet
```java
            default:
                log.error("Can't convert type: %s for %s", pulsarType, fieldName);
                return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
            checkState(parentBlockBuilder != null, "parent block builder is null");
            parentBlockBuilder.appendNull();
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
            return blockBuilder.getObject(0, Block.class);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
            checkState(parentBlockBuilder != null, "parentBlockBuilder is null");
            parentBlockBuilder.appendNull();
            return null;
        }
        List<?> list = (List<?>) value;
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
        if (parentBlockBuilder != null) {
            type.writeObject(parentBlockBuilder, blockBuilder.build());
            return null;
        }
        return blockBuilder.build();
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
            checkState(parentBlockBuilder != null, "parentBlockBuilder is null");
            parentBlockBuilder.appendNull();
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
            return blockBuilder.getObject(0, Block.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
        for (String pathElement : Splitter.on('/').omitEmptyStrings().split(columnMapping)) {
            if (value == null) {
                return null;
            }
            value = ((GenericRecord) value).get(pathElement);
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
        }
        serializePrimitive(builder, value, type, columnName);
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
                checkState(parentBlockBuilder != null, "parentBlockBuilder is null");
                parentBlockBuilder.appendNull();
                return null;
            }
            checkState(value instanceof ObjectNode, "Json map node must  is ObjectNode type");
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
                return blockBuilder.getObject(0, Block.class);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
            }
            serializePrimitive(builder, value, type, columnName);
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
                checkState(parentBlockBuilder != null, "parentBlockBuilder is null");
                parentBlockBuilder.appendNull();
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
            if (parentBlockBuilder != null) {
                type.writeObject(parentBlockBuilder, blockBuilder.build());
                return null;
            }
            return blockBuilder.build();
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
                checkState(parentBlockBuilder != null, "parent block builder is null");
                parentBlockBuilder.appendNull();
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
                return blockBuilder.getObject(0, Block.class);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarSplitManager.java`
#### Snippet
```java
                }
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
            checkState(parentBlockBuilder != null, "parentBlockBuilder is null");
            parentBlockBuilder.appendNull();
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
            return blockBuilder.getObject(0, Block.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
        for (String pathElement : Splitter.on('/').omitEmptyStrings().split(columnMapping)) {
            if (value == null) {
                return null;
            }
            value = ((DynamicMessage) value).getField(((DynamicMessage) value).getDescriptorForType()
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
            checkState(parentBlockBuilder != null, "parent block builder is null");
            parentBlockBuilder.appendNull();
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
            return blockBuilder.getObject(0, Block.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
        }
        serializePrimitive(builder, value, type, columnName);
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
            checkState(parentBlockBuilder != null, "parentBlockBuilder is null");
            parentBlockBuilder.appendNull();
            return null;
        }
        List<?> list = (List<?>) value;
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
        if (parentBlockBuilder != null) {
            type.writeObject(parentBlockBuilder, blockBuilder.build());
            return null;
        }
        return blockBuilder.build();
```

### ReturnNull
Return of `null`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/LookupProxyHandler.java`
#### Snippet
```java
                    clientRequestId, ServerError.ServiceNotReady, e.getMessage()
            ));
            return null;
        }
        return this.connectWithTLS ? availableBroker.getPulsarServiceUrlTls() : availableBroker.getPulsarServiceUrl();
```

### ReturnNull
Return of `null`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/LookupProxyHandler.java`
#### Snippet
```java
            proxyConnection.ctx().writeAndFlush(
                    Commands.newError(clientRequestId, ServerError.MetadataError, e.getMessage()));
            return null;
        }
        return InetSocketAddress.createUnresolved(brokerURI.getHost(), brokerURI.getPort());
```

### ReturnNull
Return of `null`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyServiceStarter.java`
#### Snippet
```java
                    .build();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/AdminProxyHandler.java`
#### Snippet
```java
                LOG.warn("[{}:{}] Failed to get next active broker {}", request.getRemoteAddr(),
                        request.getRemotePort(), e.getMessage(), e);
                return null;
            }
        } else {
```

### ReturnNull
Return of `null`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/AdminProxyHandler.java`
#### Snippet
```java

        if (!validateDestination(rewrittenUrl.getHost(), rewrittenUrl.getPort())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/extensions/ProxyExtensionsUtils.java`
#### Snippet
```java
        } catch (Throwable t) {
            rethrowIOException(t);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
    private SchemaInfo getBytesSchemaInfo(SchemaType schemaType, String schemaName) {
        if (!schemaType.equals(SchemaType.BYTES) && !schemaType.equals(SchemaType.NONE)) {
            return null;
        }
        if (schemaName.equals(Schema.BYTES.getSchemaInfo().getName())) {
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            // If the message is out of the split range, we only care about the incomplete chunked messages.
            message.release();
            return null;
        }
        if (chunkId == 0) {
```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            chunkedMessagesMap.remove(uuid);
            message.release();
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
        if (chunkId != (numChunks - 1)) {
            message.release();
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/extensions/ProxyExtensions.java`
#### Snippet
```java
        ProxyExtensionWithClassLoader h = extensions.get(extension);
        if (null == h) {
            return null;
        } else {
            return h.getExtension();
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
                return endTransaction(txnID, TxnAction.ABORT_VALUE, true);
            } else {
                return null;
            }
        }).exceptionally(e -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/SchemasResource.java`
#### Snippet
```java
                    }
                    resumeAsyncResponseExceptionally(response, ex);
                    return null;
                });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/NonPersistentTopics.java`
#### Snippet
```java
                log.info("[{}] Namespace bundle is not owned by any broker {}/{}/{}/{}", clientAppId(), property,
                    cluster, namespace, bundleRange);
                return null;
            }
            NamespaceBundle nsBundle = validateNamespaceBundleOwnership(fqnn, policies.bundles, bundleRange,
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/SchemasResource.java`
#### Snippet
```java
                    }
                    resumeAsyncResponseExceptionally(response, ex);
                    return null;
                });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
                        , keySchema, valueSchema);
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/NonPersistentTopics.java`
#### Snippet
```java
                            } catch (Exception e) {
                                asyncResponse.resume(new RestException(e));
                                return null;
                            }
                        }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/NonPersistentTopics.java`
#### Snippet
```java
                                        new RestException(Status.NOT_FOUND,
                                                "Internal topics have not been generated yet"));
                                return null;
                            }
                        } catch (Exception e) {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/NonPersistentTopics.java`
#### Snippet
```java
                        } catch (Exception e) {
                            asyncResponse.resume(new RestException(e));
                            return null;
                        }
                    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/NonPersistentTopics.java`
#### Snippet
```java
                    }
                    asyncResponse.resume(stats);
                    return null;
                });
            }).exceptionally(ex -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/TimeWindow.java`
#### Snippet
```java

        if (millis < 0) {
            return null;
        }
        int idx = calculateTimeIdx(millis);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/MBeanStatsGenerator.java`
#### Snippet
```java
        } catch (Exception e) {
            // [Bug 6158364] skipping MBean if access failed
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ClustersBase.java`
#### Snippet
```java
                                            .exceptionally(ex -> {
                                                log.warn("Failed to get domain {}", domainName, ex);
                                                return null;
                                            })).collect(Collectors.toList());
                            return FutureUtil.waitForAll(futures)
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
        AdvertisedListener internalListener = ServiceConfigurationUtils.getInternalListener(config, "pulsar");
        return internalListener.getBrokerServiceUrl() != null
                ? internalListener.getBrokerServiceUrl().toString() : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    public String getStatusFilePath() {
        if (config == null) {
            return null;
        }
        return config.getStatusFilePath();
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
                state = State.Closed;
                isClosedCondition.signalAll();
                return null;
            });
            return closeFuture;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
            // shutdown the shutdown executor
            shutdownExecutor.shutdownNow();
            return null;
        });
        return future;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
                    : webAddress(ServiceConfigurationUtils.getWebServiceAddress(config), getListenPortHTTP().get());
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
                });
            }
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
                    : webAddressTls(ServiceConfigurationUtils.getWebServiceAddress(config), getListenPortHTTPS().get());
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
        AdvertisedListener internalListener = ServiceConfigurationUtils.getInternalListener(config, "pulsar+ssl");
        return internalListener.getBrokerServiceUrlTls() != null
                ? internalListener.getBrokerServiceUrlTls().toString() : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/MutableBucket.java`
#### Snippet
```java
            TripleLongPriorityQueue sharedQueue) {
        if (priorityQueue.isEmpty()) {
            return null;
        }
        long numMessages = 0;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/HashRangeAutoSplitStickyKeyConsumerSelector.java`
#### Snippet
```java
            return rangeMap.ceilingEntry(slot).getValue();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/EntryBatchIndexesAcks.java`
#### Snippet
```java
    public long[] getAckSet(int entryIdx) {
        Pair<Integer, long[]> pair = indexesAcks[entryIdx];
        return pair == null ? null : pair.getRight();
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SharedConsumerAssignor.java`
#### Snippet
```java
            if (metadata.getChunkId() != 0) {
                // Not the first chunk, skip it
                return null;
            }
            consumer = defaultConsumer;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SharedConsumerAssignor.java`
#### Snippet
```java
        final int permits = consumerToPermits.computeIfAbsent(consumer, Consumer::getAvailablePermits);
        if (permits <= 0) {
            return null;
        }
        if (metadata.getChunkId() == metadata.getNumChunksFromMsg() - 1) {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SharedConsumerAssignor.java`
#### Snippet
```java
            final Consumer consumer = defaultSelector.get();
            if (consumer == null) {
                return null;
            }
            final int permits = consumerToPermits.computeIfAbsent(consumer, Consumer::getAvailablePermits);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SharedConsumerAssignor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherMultipleConsumers.java`
#### Snippet
```java
        if (consumerList.isEmpty() || IS_CLOSED_UPDATER.get(this) == TRUE) {
            // abort read if no consumers are connected or if disconnect is initiated
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherMultipleConsumers.java`
#### Snippet
```java

        // couldn't find available consumer
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherMultipleConsumers.java`
#### Snippet
```java
        if (consumerList.isEmpty() || IS_CLOSED_UPDATER.get(this) == TRUE) {
            // abort read if no consumers are connected of if disconnect is initiated
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ConsistentHashingStickyKeyConsumerSelector.java`
#### Snippet
```java
        try {
            if (hashRing.isEmpty()) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/HashRangeExclusiveStickyKeyConsumerSelector.java`
#### Snippet
```java
                return ceilingConsumer;
            } else {
                return null;
            }
        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/HashRangeExclusiveStickyKeyConsumerSelector.java`
#### Snippet
```java
            }
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getMaxProducers", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
                .thenApply(asyncResponse::resume).exceptionally(ex -> {
                    handleTopicPolicyException("getAutoSubscriptionCreation", ex, asyncResponse);
                    return null;
                });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getSubscriptionLevelDispatchRate", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getPersistence", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getMaxConsumers", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getSubscriptionDispatchRate", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
                                : otherLevelTTL;
                    }
                    return null;
                })))
            .exceptionally(ex -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getOffloadPolicies", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getCompactionThreshold", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .thenApply(asyncResponse::resume).exceptionally(ex -> {
                handleTopicPolicyException("getMaxUnackedMessagesOnConsumer", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Topic.java`
#### Snippet
```java
         */
        default String getOriginalProducerName() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getDelayedDeliveryPolicies", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getDeduplication", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .thenApply(asyncResponse::resume).exceptionally(ex -> {
                handleTopicPolicyException("getInactiveTopicPolicies", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Topic.java`
#### Snippet
```java

        default Object getProperty(String propertyName) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
                .thenApply(asyncResponse::resume).exceptionally(ex -> {
            handleTopicPolicyException("getSubscribeRate", ex, asyncResponse);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
                            return getNamespacePolicies(namespaceName).replication_clusters;
                        }
                        return null;
                    }));
                })
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Topic.java`
#### Snippet
```java

        default String getProducerName() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getDispatchRate", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getMaxUnackedMessagesOnSubscription", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            .exceptionally(ex -> {
                handleTopicPolicyException("getReplicatorDispatchRate", ex, asyncResponse);
                return null;
            });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
                return this.propertyMap.get(propertyName);
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
                    ? clusterName.split(REPL_PRODUCER_NAME_DELIMITER)[0] : clusterName;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
    private long[] getCursorAckSet(PositionImpl position) {
        if (!(subscription instanceof PersistentSubscription)) {
            return null;
        }
        return (((PersistentSubscription) subscription).getCursor()).getDeletedBatchIndexesAsLongArray(position);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java

    protected String getSubscriptionName() {
        return subscription == null ? null : subscription.getName();
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/TopicPoliciesService.java`
#### Snippet
```java
        public TopicPolicies getTopicPolicies(TopicName topicName, boolean isGlobal)
                throws TopicPoliciesCacheNotInitException {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/TopicPoliciesService.java`
#### Snippet
```java
        @Override
        public TopicPolicies getTopicPoliciesIfExists(TopicName topicName) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/TopicPoliciesService.java`
#### Snippet
```java
        @Override
        public TopicPolicies getTopicPolicies(TopicName topicName) throws TopicPoliciesCacheNotInitException {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarMetadataEventSynchronizer.java`
#### Snippet
```java
                                        log.warn("Failed to synchronize {} for {}", msg.getMessageId(), topicName,
                                                ex.getCause());
                                        return null;
                                    });
                        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarMetadataEventSynchronizer.java`
#### Snippet
```java
                                    .thenApply(__ -> c.acknowledgeAsync(msg)).exceptionally(ex -> {
                                        log.warn("Failed to synchronize {} for {}", msg.getMessageId(), topicName);
                                        return null;
                                    });
                        }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/DefaultSchemaRegistryService.java`
#### Snippet
```java
    @Override
    public SchemaVersion versionFromBytes(byte[] version) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
            return ((AbstractDispatcherSingleActiveConsumer) dispatcher).getActiveConsumer();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java

    private SchemaCompatibilityStrategy formatSchemaCompatibilityStrategy(SchemaCompatibilityStrategy strategy) {
        return strategy == SchemaCompatibilityStrategy.UNDEFINED ? null : strategy;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
        }
        if (subTypes.isEmpty()) {
            return null;
        } else {
            return subTypes;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                        hasExclusiveProducer = false;
                        exclusiveProducerName = null;
                        return null;
                    });

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                            hasExclusiveProducer = false;
                            exclusiveProducerName = null;
                            return null;
                        });

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                        hasExclusiveProducer = false;
                        exclusiveProducerName = null;
                        return null;
                    });

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
        LocatorEntry entry = locatorEntry.orElse(null);
        return entry != null ? entry.locator.getIndexList().stream().map(i -> i.getPosition().getLedgerId())
                .collect(Collectors.toList()) : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
        return deleteSchema(key, forcefully).thenApply(version -> {
            if (version == null) {
                return null;
            }
            return new LongSchemaVersion(version);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
                    log.debug("Schema data in bookkeeper may be deleted by other operations.", t);
                }
                return null;
            }
            // rethrow other cases
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/SubscribeRateLimiter.java`
#### Snippet
```java
            if (p.clusterSubscribeRate != null) {
                SubscribeRate subscribeRate = p.clusterSubscribeRate.get(cluster);
                return isSubscribeRateEnabled(subscribeRate) ? subscribeRate : null;
            } else {
                return null;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/SubscribeRateLimiter.java`
#### Snippet
```java
                return isSubscribeRateEnabled(subscribeRate) ? subscribeRate : null;
            } else {
                return null;
            }
        }).orElse(null);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
                if (recoverable) {
                    schemaResult.completeExceptionally(ex.getCause());
                    return null;
                }
                // clean the schema list for recoverable and delete the schema from zk
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
                            schemaId, (th == null ? "successful" : "failed, " + th.getCause().getMessage()));
                    schemaResult.complete(list);
                    return null;
                });
                return null;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
                    return null;
                });
                return null;
            }
            // trim the deleted schema and return the result if schema is retrieved successfully
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
            List<SchemaAndMetadata> trimmed = trimDeletedSchemaAndGetList(list);
            schemaResult.complete(trimmed);
            return null;
        });
        return schemaResult;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
        return getSchema(schemaId, SchemaVersion.Latest).thenApply((schema) -> {
            if (schema != null && schema.schema.isDeleted()) {
                return null;
            } else {
                return schema;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return namespacePolicy == null ? subscribeRate() : namespacePolicy;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                                clientAppId(), topicNamePartition, subName, timestamp, ex);
                                            future.completeExceptionally(ex);
                                            return null;
                                        }
                                    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    }

                                    return null;
                                });
                        } catch (Exception e) {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                .thenCompose(n -> getTopicPoliciesAsyncWithRetry(topicName).thenApply(op -> {
                    if (!op.isPresent()) {
                        return null;
                    }
                    SchemaCompatibilityStrategy strategy = op.get().getSchemaCompatibilityStrategy();
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                    }
                    SchemaCompatibilityStrategy strategy = op.get().getSchemaCompatibilityStrategy();
                    return SchemaCompatibilityStrategy.isUndefined(strategy) ? null : strategy;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return namespacePolicy == null ? dispatchRate() : namespacePolicy;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                                    }

                                                    return null;
                                                });
                                    } catch (Exception e) {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return maxUnackedNum == null ? config().getMaxUnackedMessagesPerSubscription() : maxUnackedNum;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                        : (AutoSubscriptionCreationOverrideImpl) namespacePolicy;
                            }
                            return null;
                        }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                }).thenApply(position -> {
                    if (position == null) {
                        return null;
                    } else {
                        return new MessageIdImpl(position.getLedgerId(), position.getEntryId(),
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                                .resume(new RestException(Status.NOT_FOUND,
                                                        "Topic or subscription not found"));
                                        return null;
                                    } else if (t instanceof PreconditionFailedException) {
                                        asyncResponse.resume(new RestException(Status.PRECONDITION_FAILED,
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                        asyncResponse.resume(new RestException(Status.PRECONDITION_FAILED,
                                                "Cannot enable/disable replicated subscriptions on non-global topics"));
                                        return null;
                                    } else {
                                        log.warn("[{}] Failed to change replicated subscription status to {} - {} {}",
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                                clientAppId(), enabled, topicName, subName, t);
                                        asyncResponse.resume(new RestException(t));
                                        return null;
                                    }
                                }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

                                asyncResponse.resume(Response.noContent().build());
                                return null;
                            });
                        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                            getSubNotFoundErrorMessage(topicName.toString(), subName)));
                                    return null;
                                } else if (t instanceof PreconditionFailedException) {
                                    asyncResponse.resume(new RestException(Status.PRECONDITION_FAILED,
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    asyncResponse.resume(new RestException(Status.PRECONDITION_FAILED,
                                            "Subscription has active connected consumers"));
                                    return null;
                                } else {
                                    log.error("[{}] Failed to delete subscription {} {}",
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                            clientAppId(), topicName, subName, t);
                                    asyncResponse.resume(new RestException(t));
                                    return null;
                                }
                            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

                            asyncResponse.resume(Response.noContent().build());
                            return null;
                        });
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                       asyncResponse.resume(Response.noContent().build());
                                   }
                                   return null;
                               });
                           } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return namespacePolicy == null ? subscriptionDispatchRate() : namespacePolicy;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            : policies;
                }
                return null;
            }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                                                    getSubNotFoundErrorMessage(topicName.toString(),
                                                                            subName)));
                                                            return null;
                                                        } else {
                                                            log.error("[{}] Failed to expire messages up "
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                                                    expireTimeInSeconds, topicName, t);
                                                            asyncResponse.resume(new RestException(t));
                                                            return null;
                                                        }
                                                    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                                    }
                                                    asyncResponse.resume(Response.noContent().build());
                                                    return null;
                                                });
                                            });
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                config().isBrokerDeleteInactiveTopicsEnabled()) : policies;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    clientAppId(), expireTimeInSeconds, topicName, throwable);
                            asyncResponse.resume(new RestException(throwable));
                            return null;
                        }
                        asyncResponse.resume(Response.noContent().build());
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        }
                        asyncResponse.resume(Response.noContent().build());
                        return null;
                    });
                        })
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return namespacePolicy == null ? replicatorDispatchRate() : namespacePolicy;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return enabled == null ? config().isBrokerDeduplicationEnabled() : enabled;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                            getSubNotFoundErrorMessage(topicName.toString(), subName)));
                                    return null;
                                } else {
                                    log.error("[{}] Failed to delete subscription forcefully {} {}",
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                            clientAppId(), topicName, subName, t);
                                    asyncResponse.resume(new RestException(t));
                                    return null;
                                }
                            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

                            asyncResponse.resume(Response.noContent().build());
                            return null;
                        });
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return maxConsumer == null ? config().getMaxConsumersPerTopic() : maxConsumer;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        } catch (Exception e) {
                            asyncResponse.resume(new RestException(e));
                            return null;
                        }
                    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    new RestException(Status.NOT_FOUND,
                                            "Internal topics have not been generated yet"));
                            return null;
                        }
                    } catch (Exception e) {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                    } catch (Exception e) {
                        asyncResponse.resume(new RestException(e));
                        return null;
                    }
                }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                }
                asyncResponse.resume(stats);
                return null;
            });
        }).exceptionally(ex -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                : namespacePolicy;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            }
                            asyncResponse.resume(messageIds);
                            return null;
                        });
                    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                            topicName, t);
                                    asyncResponse.resume(new RestException(t));
                                    return null;
                                }
                                asyncResponse.resume(Response.noContent().build());
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                }
                                asyncResponse.resume(Response.noContent().build());
                                return null;
                            });
                        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                return entryFilters;
                            }
                            return null;
                        })));

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                }
                                asyncResponse.resume(status);
                                return null;
                            });
                        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                : namespacePolicy;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                            getSubNotFoundErrorMessage(topicName.toString(), subName)));
                                    return null;
                                } else {
                                    log.error("[{}] Failed to get properties for subscription {} {}",
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                            clientAppId(), topicName, subName, t);
                                    asyncResponse.resume(new RestException(t));
                                    return null;
                                }
                            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

                            asyncResponse.resume(Response.ok(aggregatedResult).build());
                            return null;
                        });
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                jsonMapper().writer().writeValue(output, partitionedManagedLedgerInfo);
                            });
                            return null;
                        });
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                if (th instanceof NotFoundException) {
                                    asyncResponse.resume(new RestException(Status.NOT_FOUND, th.getMessage()));
                                    return null;
                                } else if (th instanceof WebApplicationException) {
                                    asyncResponse.resume(th);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                } else if (th instanceof WebApplicationException) {
                                    asyncResponse.resume(th);
                                    return null;
                                } else {
                                    log.error("[{}] Failed to trigger compaction on topic {}",
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                            clientAppId(), topicName, exception);
                                    asyncResponse.resume(new RestException(exception));
                                    return null;
                                }
                            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            }
                            asyncResponse.resume(Response.noContent().build());
                            return null;
                        });
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return maxUnacked == null ? config().getMaxUnackedMessagesPerConsumer() : maxUnacked;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return maxProducer == null ? config().getMaxProducersPerTopic() : maxProducer;
                    }
                    return null;
                }));
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        } catch (Exception e) {
                            asyncResponse.resume(new RestException(e));
                            return null;
                        }
                    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                asyncResponse.resume(!stats.partitions.isEmpty() ? stats
                        : new RestException(Status.NOT_FOUND, "Internal topics have not been generated yet"));
                return null;
            });
        }).exceptionally(ex -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                            getSubNotFoundErrorMessage(topicName.toString(), subName)));
                                    return null;
                                } else if (t instanceof PreconditionFailedException) {
                                    asyncResponse.resume(new RestException(Status.PRECONDITION_FAILED,
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    asyncResponse.resume(new RestException(Status.PRECONDITION_FAILED,
                                            "Subscription has active connected consumers"));
                                    return null;
                                } else {
                                    log.error("[{}] Failed to update properties for subscription {} {}",
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                            clientAppId(), topicName, subName, t);
                                    asyncResponse.resume(new RestException(t));
                                    return null;
                                }
                            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

                            asyncResponse.resume(Response.noContent().build());
                            return null;
                        });
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
        @Override
        public SendCallback getNextSendCallback() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
        @Override
        public MessageImpl<?> getNextMessage() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentReplicator.java`
#### Snippet
```java
    protected Position getReplicatorReadPosition() {
        // No-op
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentReplicator.java`
#### Snippet
```java
        @Override
        public SendCallback getNextSendCallback() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentReplicator.java`
#### Snippet
```java
        @Override
        public MessageImpl<?> getNextMessage() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java
    @Override
    public SubType getType() {
        return dispatcher != null ? dispatcher.getType() : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/protocol/ProtocolHandlerUtils.java`
#### Snippet
```java
        } catch (Throwable t) {
            rethrowIOException(t);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/protocol/ProtocolHandlers.java`
#### Snippet
```java
        ProtocolHandlerWithClassLoader h = handlers.get(protocol);
        if (null == h) {
            return null;
        } else {
            return h.getHandler();
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
            }

            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    lookupSemaphore.release();
                }
                return null;
            }).exceptionally(ex -> {
                logNamespaceNameAuthException(remoteAddress, "GetTopicsOfNamespace", getPrincipal(),
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                commandSender.sendErrorResponse(requestId, ServerError.AuthorizationError, msg);
                lookupSemaphore.release();
                return null;
            });
        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    consumers.remove(consumerId, consumerFuture);
                    commandSender.sendErrorResponse(requestId, ServerError.MetadataError, msg);
                    return null;
                }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                        commandSender.sendSuccessResponse(requestId);
                    }
                    return null;
                }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                ctx.writeAndFlush(Commands.newError(requestId, ServerError.AuthorizationError, msg));
            }
            return null;
        }).exceptionally(ex -> {
            logAuthException(remoteAddress, "subscribe", getPrincipal(), Optional.of(topicName), ex);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
            consumers.remove(consumerId, consumerFuture);
            commandSender.sendErrorResponse(requestId, ServerError.AuthorizationError, ex.getMessage());
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                log.warn("[{}] {} with role {}", remoteAddress, msg, getPrincipal());
                ctx.writeAndFlush(Commands.newError(requestId, ServerError.AuthorizationError, msg));
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    commandSender.sendProducerSuccessResponse(requestId, producer.getProducerName(),
                            producer.getSchemaVersion());
                    return null;
                } else {
                    // There was an early request to create a producer with same producerId.
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                            remoteAddress, topicName, producerId);
                    commandSender.sendErrorResponse(requestId, error, "Producer is already present on the connection");
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                                topicName, producerId, exception);
                        producers.remove(producerId, producerFuture);
                        return null;
                    });

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                return null;
            });
            return null;
        }).exceptionally(ex -> {
            logAuthException(remoteAddress, "producer", getPrincipal(), Optional.of(topicName), ex);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
            logAuthException(remoteAddress, "producer", getPrincipal(), Optional.of(topicName), ex);
            commandSender.sendErrorResponse(requestId, ServerError.AuthorizationError, ex.getMessage());
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    lookupSemaphore.release();
                }
                return null;
            }).exceptionally(ex -> {
                logNamespaceNameAuthException(remoteAddress, "watchTopicList", getPrincipal(),
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                commandSender.sendErrorResponse(requestId, ServerError.AuthorizationError, msg);
                lookupSemaphore.release();
                return null;
            });
        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                                }
                                lookupSemaphore.release();
                                return null;
                            });
                } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    lookupSemaphore.release();
                }
                return null;
            }).exceptionally(ex -> {
                logAuthException(remoteAddress, "partition-metadata", getPrincipal(), Optional.of(topicName), ex);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                        requestId));
                lookupSemaphore.release();
                return null;
            });
        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    }
                    commandSender.sendGetOrCreateSchemaErrorResponse(requestId, errorCode, message);
                    return null;
                }).thenAccept(schemaVersion -> {
                    commandSender.sendGetOrCreateSchemaResponse(requestId, schemaVersion);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                                }
                                lookupSemaphore.release();
                                return null;
                            });
                } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    lookupSemaphore.release();
                }
                return null;
            }).exceptionally(ex -> {
                logAuthException(remoteAddress, "lookup", getPrincipal(), Optional.of(topicName), ex);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                ctx.writeAndFlush(newLookupErrorResponse(ServerError.AuthorizationError, msg, requestId));
                lookupSemaphore.release();
                return null;
            });
        } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
            return inetAddress.getAddress().getHostAddress();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/BrokerInterceptorUtils.java`
#### Snippet
```java
        } catch (Throwable t) {
            rethrowIOException(t);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/ManagedLedgerInterceptorImpl.java`
#### Snippet
```java
    public PayloadProcessorHandle processPayloadBeforeEntryCache(ByteBuf ledgerData){
        if (this.outputProcessors == null || this.outputProcessors.size() == 0) {
            return null;
        }
        return processPayload(this.outputProcessors, null, ledgerData);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/ManagedLedgerInterceptorImpl.java`
#### Snippet
```java
    public PayloadProcessorHandle processPayloadBeforeLedgerWrite(OpAddEntry op, ByteBuf ledgerData) {
        if (this.inputProcessors == null || this.inputProcessors.size() == 0) {
            return null;
        }
        return processPayload(this.inputProcessors, op.getCtx(), ledgerData);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    @Override
    public SubType getType() {
        return dispatcher != null ? dispatcher.getType() : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/OwnedBundle.java`
#### Snippet
```java
                    // clean up topics that failed to unload from the broker ownership cache
                    pulsar.getBrokerService().cleanUnloadedTopicFromCache(bundle);
                    return null;
                })
                .thenCompose(v -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/OwnershipCache.java`
#### Snippet
```java
            }
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/NoopLoadManager.java`
#### Snippet
```java
    @Override
    public LoadManagerReport generateLoadReport() throws Exception {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                if (exception.getCause() instanceof PulsarAdminException) {
                    asyncResponse.resume(new RestException((PulsarAdminException) exception.getCause()));
                    return null;
                } else {
                    asyncResponse.resume(new RestException(exception.getCause()));
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                } else {
                    asyncResponse.resume(new RestException(exception.getCause()));
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                    namespaceName);
            asyncResponse.resume(Response.noContent().build());
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                log.info("[{}] Successfully remove offload configuration: namespace={}", clientAppId(), namespaceName);
                asyncResponse.resume(Response.noContent().build());
                return null;
            }).exceptionally(e -> {
                log.error("[{}] Failed to remove offload configuration for namespace {}", clientAppId(), namespaceName,
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        ? new RestException(Status.CONFLICT, "Concurrent modification")
                        : new RestException(e));
                return null;
            });
        } catch (Exception e) {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        namespaceName, offloadPolicies);
                asyncResponse.resume(Response.noContent().build());
                return null;
            }).exceptionally(e -> {
                log.error("[{}] Failed to update offload configuration for namespace {}", clientAppId(), namespaceName,
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        e);
                asyncResponse.resume(new RestException(e));
                return null;
            });
        } catch (Exception e) {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                if (exception.getCause() instanceof PulsarAdminException) {
                    asyncResponse.resume(new RestException((PulsarAdminException) exception.getCause()));
                    return null;
                } else {
                    asyncResponse.resume(new RestException(exception.getCause()));
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                } else {
                    asyncResponse.resume(new RestException(exception.getCause()));
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                    subscription, namespaceName);
            asyncResponse.resume(Response.noContent().build());
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                if (exception.getCause() instanceof PulsarAdminException) {
                    asyncResponse.resume(new RestException((PulsarAdminException) exception.getCause()));
                    return null;
                } else {
                    asyncResponse.resume(new RestException(exception.getCause()));
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                } else {
                    asyncResponse.resume(new RestException(exception.getCause()));
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                    clientAppId(), subscription, namespaceName);
            asyncResponse.resume(Response.noContent().build());
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/WRRPlacementStrategy.java`
#### Snippet
```java
    public ResourceUnit findBrokerForPlacement(Multimap<Long, ResourceUnit> finalCandidates) {
        if (finalCandidates.isEmpty()) {
            return null;
        }
        log.debug("Total Final Candidates selected - [{}]", finalCandidates.size());
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleResourceUnit.java`
#### Snippet
```java
    public ResourceDescription getAvailableResource() {
        // TODO Auto-generated method stub
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadCalculatorImpl.java`
#### Snippet
```java
    @Override
    public ResourceDescription getResourceDescription(ServiceRequest srvRequest) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
                            log.error("[{}] Replicator startup failed {}", topic, remoteCluster, e);
                        }
                        return null;
                    });

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
        } catch (InterruptedException | ExecutionException e) {
            log.error("[{}] Fail to get stats", topic, e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TransactionBufferDisable.java`
#### Snippet
```java
    @Override
    public TransactionBufferStats getStats(boolean lowWaterMarks) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TransactionBufferDisable.java`
#### Snippet
```java
    @Override
    public TransactionInBufferStats getTransactionInBufferStats(TxnID txnID) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
            // No available broker found
            log.warn("No broker available to acquire service unit: [{}]", serviceUnit);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
        } catch (Exception e) {
            log.warn("Failed to get local-broker data for {}", broker, e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
    @Override
    public TransactionInBufferStats getTransactionInBufferStats(TxnID txnID) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
    @Override
    public TransactionBufferStats getStats(boolean lowWaterMarks) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
    @Override
    public CompletableFuture<TransactionBufferReader> openTransactionBufferReader(TxnID txnID, long startSequenceId) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Void> purgeTxns(List<Long> dataLedgers) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        } catch (InterruptedException | ExecutionException e) {
            log.error("[{}] Fail to get stats", topic, e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                                        info.metadata = lMetadata;
                                    }
                                    return null;
                                }));
                                futures.add(ml.getEnsemblesAsync(li.getLedgerId()).handle((ensembles, ex) -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                                                .collect(Collectors.toList()));
                                    }
                                    return null;
                                }));
                            }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            FutureUtil.waitForAll(futures).handle((res, ex) -> {
                statFuture.complete(stats);
                return null;
            })).exceptionally(e -> {
            statFuture.completeExceptionally(e);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                            log.error("[{}] Replicator startup failed {}", topic, remoteCluster, e);
                        }
                        return null;
                    });

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            }
            subscriptionFuture.complete(finalSubscription);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                            log.error("[{}] ShadowReplicator startup failed {}", topic, shadowTopic, e);
                        }
                        return null;
                    });

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        if (!isMigrated() && clusterUrl.isPresent()) {
            log.info("{} triggering topic migration", topic);
            return ledger.asyncMigrate().thenCompose(r -> null);
        } else {
            return CompletableFuture.completedFuture(null);
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                    log.error("[{}] Failed to create subscription: {}", topic, subscriptionName, ex);
                }
                return null;
            });
            return future;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
            return String.format("http://%s", m.group(1));
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
            return String.format("http://%s", m.group(1));
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
            return String.format("http://%s", m.group(1));
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleDisabled.java`
#### Snippet
```java
    @Override
    public PositionInPendingAckStats checkPositionInPendingAckState(PositionImpl position, Integer batchIndex) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleDisabled.java`
#### Snippet
```java
    @Override
    public TransactionInPendingAckStats getTransactionInPendingAckStats(TxnID txnID) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleDisabled.java`
#### Snippet
```java
    @Override
    public TransactionPendingAckStats getStats(boolean lowWaterMarks) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroup.java`
#### Snippet
```java
                log.debug("Doing nothing for monClass={}; only Publish is functional", monClass);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleSplitAlgorithm.java`
#### Snippet
```java
    static NamespaceBundleSplitAlgorithm of(String algorithmName) {
        if (algorithmName == null) {
            return null;
        }
        switch (algorithmName) {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleSplitAlgorithm.java`
#### Snippet
```java
                return FLOW_OR_QPS_EQUALLY_DIVIDE_ALGO;
            default:
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupService.java`
#### Snippet
```java
        ResourceGroup retrievedRG = this.getResourceGroupInternal(resourceGroupName);
        if (retrievedRG == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
    public NamespaceBundle getBundle(TopicName topic) {
        NamespaceBundles bundles = bundlesCache.synchronous().get(topic.getNamespaceObject());
        return bundles != null ? bundles.findBundle(topic) : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
            }

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                            topicLoadSemaphore.release();
                            createPendingLoadTopic();
                            return null;
                        });
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                }
                createPendingLoadTopic();
                return null;
            });
        }).exceptionally(e -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                                    log.info("Event loops shutdown completed.");
                                }
                                return null;
                            })
                            .thenCompose(__ -> {
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                                        }
                                    }
                                    return null;
                                });
                                return combined;
```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        }
        log.debug("No autoSubscriptionCreateOverride policy found for {}", topicName);
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        }
        log.debug("No autoTopicCreateOverride policy found for {}", topicName);
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessagePayloadContextImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TopicListWatcher.java`
#### Snippet
```java
                }
                cleanupAtClose(closeFuture, ignoreException ? null : exception);
                return null;
            });
        }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageContainerImpl.java`
#### Snippet
```java
                discard(new PulsarClientException.InvalidMessageException(
                    "Message size is bigger than " + ClientCnx.getMaxMessageSize() + " bytes"));
                return null;
            }
            lowestSequenceId = -1L;
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageContainerImpl.java`
#### Snippet
```java
            discard(new PulsarClientException.InvalidMessageException(
                    "Message size is bigger than " + ClientCnx.getMaxMessageSize() + " bytes"));
            return null;
        }
        messageMetadata.setNumMessagesInBatch(numMessagesInBatch);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageIdImpl.java`
#### Snippet
```java
            return convertToMessageIdImpl(((TopicMessageIdImpl) messageId).getInnerMessageId());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/TwoPhaseCompactor.java`
#### Snippet
```java
            return Pair.of(msgMetadata.getPartitionKey(), size);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerStatsDisabled.java`
#### Snippet
```java
    @Override
    public Map<Long, Integer> getMsgNumInSubReceiverQueue() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerStatsDisabled.java`
#### Snippet
```java
    @Override
    public Integer getMsgNumInReceiverQueue() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ReaderInterceptorUtil.java`
#### Snippet
```java
            Reader<T> reader, List<ReaderInterceptor<T>> interceptorList) {
        if (interceptorList == null || interceptorList.isEmpty()) {
            return null;
        }
        List<ConsumerInterceptor<T>> consumerInterceptorList = new ArrayList<>(interceptorList.size());
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerStatsRecorderImpl.java`
#### Snippet
```java
            return ((ConsumerBase<?>) consumer).incomingMessages.size();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerStatsRecorderImpl.java`
#### Snippet
```java
            );
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ControlledClusterFailover.java`
#### Snippet
```java
            } catch (JsonProcessingException e) {
                log.warn("Failed to write as json. ", e);
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ControlledClusterFailover.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TypedMessageBuilderImpl.java`
#### Snippet
```java
                return this;
            } else {
                return null;
            }
        }).orElseGet(() -> {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TopicMessageImpl.java`
#### Snippet
```java
            return message.getSchemaInternal();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerResponse.java`
#### Snippet
```java
            return schemaVersion;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
        } else {
            // Return null to indicate nothing to be flushed
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PatternMultiTopicsConsumerImpl.java`
#### Snippet
```java
            watcherFuture.exceptionally(ex -> {
                log.debug("Unable to create topic list watcher. Falling back to only polling for new topics", ex);
                return null;
            });
        } else {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
    public synchronized ProducerStatsRecorderImpl getStats() {
        if (stats == null) {
            return null;
        }
        stats.reset();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java

        // Should not happen since we cover all cases above
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
            // no entry available
            if (opBatchReceive == null) {
                return null;
            }
            // skip entries where future is null or has been completed (cancel / timeout)
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
                    producerCreatedFuture().completeExceptionally(createFail.get());
                    client.cleanupProducer(this);
                    return null;
                });
            };
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
                createProducer(indexList.get(i), name).producerCreatedFuture().handle((prod, createException) -> {
                    afterCreatingProducer.accept(false, createException);
                    return null;
                });
            }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
                });
            }
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
                    }

                    return null;
                });
            }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
                        // call interceptor with the metadata change
                        onPartitionsChange(topic, currentPartitionNumber);
                        return null;
                    }
                } else {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ZeroQueueConsumerImpl.java`
#### Snippet
```java
                            format("Unsupported Batch message with 0 size receiver queue for [%s]-[%s] ",
                                    subscription, consumerName)));
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataKeyStoreTls.java`
#### Snippet
```java
    @Override
    public String getCommandData() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
            return Mode.ALL;
        default:
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                closeFuture.complete(null);
            }, "pulsar-client-shutdown-thread").start();
            return null;
        });
        return closeFuture;
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataTls.java`
#### Snippet
```java
    @Override
    public InputStream getTlsTrustStoreStream() {
        return trustStoreStreamProvider != null ? trustStoreStreamProvider.get() : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataTls.java`
#### Snippet
```java
    @Override
    public String getTlsCerificateFilePath() {
        return certFile != null ? certFile.getFileName() : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataTls.java`
#### Snippet
```java
    @Override
    public String getTlsPrivateKeyFilePath() {
        return keyFile != null ? keyFile.getFileName() : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
    public byte[] getKeyBytes() {
        if (!msgMetadata.hasPartitionKey() || msgMetadata.isNullPartitionKey()) {
            return null;
        } else if (hasBase64EncodedKey()) {
            return Base64.getDecoder().decode(getKey());
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
        } else {
            if (msgMetadata.isNullValue()) {
                return null;
            }
            // check if the schema passed in from client supports schema versioning or not
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
    public byte[] getData() {
        if (msgMetadata.isNullValue()) {
            return null;
        }
        if (payload.isDirect()) {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java

    public SchemaInfo getSchemaInfoForReplicator() {
        return msgMetadata.hasReplicatedFrom() ? this.schemaInfoForReplicator : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
            return msgMetadata.getPartitionKey();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
        if (msgMetadata.hasSchemaVersion()) {
            byte[] schemaVersion = msgMetadata.getSchemaVersion();
            return (schemaVersion.length == 0) ? null : schemaVersion;
        } else {
            return null;
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
            return (schemaVersion.length == 0) ? null : schemaVersion;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
            return msgMetadata.getProducerName();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
            return msgMetadata.getReplicatedFrom();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
    private ByteBuffer getByteBuffer() {
        if (msgMetadata.isNullValue()) {
            return null;
        }
        return this.payload.nioBuffer();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
            return msgMetadata.getOrderingKey();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
    public SchemaInfo getSchemaInfo() {
        if (schema == null) {
            return null;
        }
        ensureSchemaIsLoaded();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            return connectionHandler.cnx();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java

    public String getConnectionId() {
        return cnx() != null ? connectionId : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                }
            });
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java

    public String getConnectedSince() {
        return cnx() != null ? connectedSince : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            }

            return null;
        });

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/IntSchema.java`
#### Snippet
```java
    public Integer decode(ByteBuf byteBuf) {
        if (null == byteBuf) {
            return null;
        }
        validate(byteBuf);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/IntSchema.java`
#### Snippet
```java
    public Integer decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        }
        validate(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/IntSchema.java`
#### Snippet
```java
    public byte[] encode(Integer message) {
        if (null == message) {
            return null;
        } else {
            return new byte[] {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/BytesSchema.java`
#### Snippet
```java
    public byte[] decode(ByteBuf byteBuf) {
        if (byteBuf == null) {
            return null;
        }
        int size = byteBuf.readableBytes();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/FloatSchema.java`
#### Snippet
```java
    public Float decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        }
        validate(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/FloatSchema.java`
#### Snippet
```java
    public Float decode(ByteBuf byteBuf) {
        if (null == byteBuf) {
            return null;
        }
        validate(byteBuf);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/FloatSchema.java`
#### Snippet
```java
    public byte[] encode(Float message) {
        if (null == message) {
            return null;
        } else {
            long bits = Float.floatToRawIntBits(message);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/StringSchema.java`
#### Snippet
```java
    public String decode(ByteBuf byteBuf) {
        if (null == byteBuf) {
            return null;
        } else {
            int size = byteBuf.readableBytes();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/StringSchema.java`
#### Snippet
```java
    public byte[] encode(String message) {
        if (null == message) {
            return null;
        } else {
            return message.getBytes(charset);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/StringSchema.java`
#### Snippet
```java
    public String decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        } else {
            return new String(bytes, charset);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalDateSchema.java`
#### Snippet
```java
   public LocalDate decode(byte[] bytes) {
      if (null == bytes) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalDateSchema.java`
#### Snippet
```java
   public byte[] encode(LocalDate message) {
      if (null == message) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalDateSchema.java`
#### Snippet
```java
   public LocalDate decode(ByteBuf byteBuf) {
      if (null == byteBuf) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteBufferSchema.java`
#### Snippet
```java
    public ByteBuffer decode(ByteBuf byteBuffer) {
        if (null == byteBuffer) {
            return null;
        } else {
            int size = byteBuffer.readableBytes();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteBufferSchema.java`
#### Snippet
```java
    public ByteBuffer decode(byte[] data) {
        if (null == data) {
            return null;
        } else {
            return ByteBuffer.wrap(data);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteBufferSchema.java`
#### Snippet
```java
    public byte[] encode(ByteBuffer data) {
        if (data == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteSchema.java`
#### Snippet
```java
    public byte[] encode(Byte message) {
        if (null == message) {
            return null;
        } else {
            return new byte[]{message};
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteSchema.java`
#### Snippet
```java
    public Byte decode(ByteBuf byteBuf) {
        if (null == byteBuf) {
            return null;
        }
        validate(byteBuf);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteSchema.java`
#### Snippet
```java
    public Byte decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        }
        validate(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LongSchema.java`
#### Snippet
```java
    public Long decode(ByteBuf byteBuf) {
        if (null == byteBuf) {
            return null;
        }
        validate(byteBuf);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LongSchema.java`
#### Snippet
```java
    public byte[] encode(Long data) {
        if (null == data) {
            return null;
        } else {
            return new byte[] {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LongSchema.java`
#### Snippet
```java
    public Long decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        }
        validate(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/InstantSchema.java`
#### Snippet
```java
   public Instant decode(ByteBuf byteBuf) {
      if (null == byteBuf) {
         return null;
      }
      long epochSecond = byteBuf.readLong();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/InstantSchema.java`
#### Snippet
```java
   public byte[] encode(Instant message) {
      if (null == message) {
         return null;
      }
      // Instant is accurate to nanoseconds and requires two value storage.
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/InstantSchema.java`
#### Snippet
```java
   public Instant decode(byte[] bytes) {
      if (null == bytes) {
         return null;
      }
      ByteBuffer buffer = ByteBuffer.wrap(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AbstractStructSchema.java`
#### Snippet
```java
    public T decode(ByteBuffer buffer, byte[] schemaVersion) {
        if (buffer == null) {
            return null;
        }
        List<ByteBuffer> buffers = Collections.singletonList(buffer);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AbstractStructSchema.java`
#### Snippet
```java
    public T decode(ByteBuffer buffer) {
        if (buffer == null) {
            return null;
        }
        List<ByteBuffer> buffers = Collections.singletonList(buffer);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/DateSchema.java`
#### Snippet
```java
   public Date decode(ByteBuf byteBuf) {
      if (null == byteBuf) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/DateSchema.java`
#### Snippet
```java
   public Date decode(byte[] bytes) {
      if (null == bytes) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/DateSchema.java`
#### Snippet
```java
   public byte[] encode(Date message) {
      if (null == message) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/BooleanSchema.java`
#### Snippet
```java
    public byte[] encode(Boolean message) {
        if (null == message) {
            return null;
        } else {
            return new byte[]{(byte) (message ? 1 : 0)};
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/BooleanSchema.java`
#### Snippet
```java
    public Boolean decode(ByteBuf byteBuf) {
        if (null == byteBuf) {
            return null;
        }
        return byteBuf.getBoolean(0);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/BooleanSchema.java`
#### Snippet
```java
    public Boolean decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        }
        validate(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/TimeSchema.java`
#### Snippet
```java
   public byte[] encode(Time message) {
      if (null == message) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/TimeSchema.java`
#### Snippet
```java
   public Time decode(ByteBuf byteBuf) {
      if (null == byteBuf) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/TimeSchema.java`
#### Snippet
```java
   public Time decode(byte[] bytes) {
      if (null == bytes) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/GenericObjectWrapper.java`
#### Snippet
```java
    @Override
    public Object getField(String fieldName) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalDateTimeSchema.java`
#### Snippet
```java
   public byte[] encode(LocalDateTime message) {
      if (null == message) {
         return null;
      }
      //LocalDateTime is accurate to nanoseconds and requires two value storage.
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalDateTimeSchema.java`
#### Snippet
```java
   public LocalDateTime decode(ByteBuf byteBuf) {
      if (null == byteBuf) {
         return null;
      }
      long epochDay = byteBuf.getLong(0);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalDateTimeSchema.java`
#### Snippet
```java
   public LocalDateTime decode(byte[] bytes) {
      if (null == bytes) {
         return null;
      }
      ByteBuffer buffer = ByteBuffer.wrap(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/TimestampSchema.java`
#### Snippet
```java
   public byte[] encode(Timestamp message) {
      if (null == message) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/TimestampSchema.java`
#### Snippet
```java
   public Timestamp decode(ByteBuf byteBuf) {
      if (null == byteBuf) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/TimestampSchema.java`
#### Snippet
```java
   public Timestamp decode(byte[] bytes) {
      if (null == bytes) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ShortSchema.java`
#### Snippet
```java
    public byte[] encode(Short message) {
        if (null == message) {
            return null;
        } else {
            return new byte[] {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ShortSchema.java`
#### Snippet
```java
    public Short decode(ByteBuf byteBuf) {
        if (null == byteBuf) {
            return null;
        }
        validate(byteBuf);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ShortSchema.java`
#### Snippet
```java
    public Short decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        }
        validate(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalTimeSchema.java`
#### Snippet
```java
   public byte[] encode(LocalTime message) {
      if (null == message) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalTimeSchema.java`
#### Snippet
```java
   public LocalTime decode(ByteBuf byteBuf) {
      if (null == byteBuf) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/LocalTimeSchema.java`
#### Snippet
```java
   public LocalTime decode(byte[] bytes) {
      if (null == bytes) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteBufSchema.java`
#### Snippet
```java
    public ByteBuf decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        } else {
            return Unpooled.wrappedBuffer(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteBufSchema.java`
#### Snippet
```java
    public byte[] encode(ByteBuf message) {
        if (message == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/DoubleSchema.java`
#### Snippet
```java
    public byte[] encode(Double message) {
        if (null == message) {
            return null;
        } else {
            long bits = Double.doubleToLongBits(message);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/DoubleSchema.java`
#### Snippet
```java
    public Double decode(ByteBuf byteBuf) {
        if (null == byteBuf) {
            return null;
        }
        validate(byteBuf);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/DoubleSchema.java`
#### Snippet
```java
    public Double decode(byte[] bytes) {
        if (null == bytes) {
            return null;
        }
        validate(bytes);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
    public SchemaInfo getSchemaInfo() {
        if (!schemaMap.containsKey(SchemaVersion.Latest)) {
            return null;
        }
        return schemaMap.get(SchemaVersion.Latest).getSchemaInfo();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
            return schemaMap.get(sv).getSchemaInfo();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericJsonRecord.java`
#### Snippet
```java
        JsonNode fn = jn.get(fieldName);
        if (fn == null) {
            return null;
        }
        if (fn.isContainerNode()) {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericJsonRecord.java`
#### Snippet
```java
            }
        } else if (fn.isNull()) {
            return null;
        } else {
            return fn.asText();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaImpl.java`
#### Snippet
```java
        } else {
            if (message.getValue() == null) {
                return null;
            }
            return valueSchema.encode(message.getValue());
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/api/raw/MessageParser.java`
#### Snippet
```java
            log.error("[{}] Got corrupted payload message size {} at {}:{}", topic, payloadSize,
                    ledgerId, entryId);
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/api/raw/MessageParser.java`
#### Snippet
```java
            log.error("[{}] Failed to decompress message with {} at {}:{} : {}", topic,
                    msgMetadata.getCompression(), ledgerId, entryId, e.getMessage(), e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaInfo.java`
#### Snippet
```java
        if (keySchemaInfo == null || valueSchemaInfo == null) {
            // schema is not ready
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/api/raw/RawMessageImpl.java`
#### Snippet
```java
            return msgMetadata.getMetadata().getUuid();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/api/raw/RawMessageImpl.java`
#### Snippet
```java
            return msgMetadata.getMetadata().getSchemaVersion();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/SchemaUtils.java`
#### Snippet
```java
            }
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarClassLoader.java`
#### Snippet
```java

        // not found in the nar. try system native dir
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/net/ServiceURI.java`
#### Snippet
```java
    public String getServiceScheme() {
        if (null == serviceName) {
            return null;
        } else {
            if (serviceInfos.length == 0) {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/SaslConstants.java`
#### Snippet
```java
            .getAppConfigurationEntry(configurationEntry);
        if (entries == null) {
            return null;
        }
        for (AppConfigurationEntry entry : entries) {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/SaslConstants.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/PublicSuffixMatcher.java`
#### Snippet
```java
    public String getDomainRoot(final String domain, final DomainType expectedType) {
        if (domain == null) {
            return null;
        }
        if (domain.startsWith(".")) {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/PublicSuffixMatcher.java`
#### Snippet
```java
        }
        if (domain.startsWith(".")) {
            return null;
        }
        String domainName = null;
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/TlsHostnameVerifier.java`
#### Snippet
```java
    static String extractCN(final String subjectPrincipal) throws SSLException {
        if (subjectPrincipal == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/TlsHostnameVerifier.java`
#### Snippet
```java
                }
            }
            return null;
        } catch (final InvalidNameException e) {
            throw new SSLException(subjectPrincipal + " is not a valid X500 distinguished name");
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
            }
        }
        return null;
    }
    // LoginThread will sleep until 80% of time from last refresh to
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ClassLoaderUtils.java`
#### Snippet
```java
            return loadJar(packageFile);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FileModifiedTimeUpdater.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/BaseGenerateDocumentation.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
        String v = trim(val);
        if (io.netty.util.internal.StringUtil.isNullOrEmpty(v)) {
            return null;
        } else {
            return Double.valueOf(v);
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
        String v = trim(val);
        if (io.netty.util.internal.StringUtil.isNullOrEmpty(v)) {
            return null;
        } else {
            return Integer.valueOf(v);
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
        requireNonNull(to);
        if (from == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
            return createInstance(userClassName, ClassLoaderUtils.loadJar(jar));
        } catch (Exception ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ThreadDumpUtil.java`
#### Snippet
```java
            return stringWriter.toString();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        public Range<T> lastRange() {
            if (set.asRanges().isEmpty()) {
                return null;
            }
            List<Range<T>> list = Lists.newArrayList(set.asRanges().iterator());
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
                return set.span();
            } catch (NoSuchElementException e) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
                return iterable.next();
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
            return new LongPair(data[0], data[1]);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
            return item;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
                        } else if (storedKey == EmptyKey) {
                            // Not found
                            return null;
                        }
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
                        } else if (storedKey == EmptyKey) {
                            // Not found
                            return null;
                        }
                    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
                        table[bucket + 1] = value;
                        SIZE_UPDATER.incrementAndGet(this);
                        return valueProvider != null ? value : null;
                    } else if (storedKey == DeletedKey) {
                        // The bucket contained a different deleted key
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
                            return storedValue;
                        } else {
                            return null;
                        }
                    } else if (storedKey == EmptyKey) {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
                    } else if (storedKey == EmptyKey) {
                        // Key wasn't found
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowableArrayBlockingQueue.java`
#### Snippet
```java
                return data[headIndex.value];
            } else {
                return null;
            }
        } finally {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowableArrayBlockingQueue.java`
#### Snippet
```java
                return item;
            } else {
                return null;
            }
        } finally {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                        // The values we have read are consistent
                        if (storedKey == key) {
                            return storedValue != DeletedValue ? storedValue : null;
                        } else if (storedValue == EmptyValue) {
                            // Not found
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                        } else if (storedValue == EmptyValue) {
                            // Not found
                            return null;
                        }
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java

                        if (storedKey == key) {
                            return storedValue != DeletedValue ? storedValue : null;
                        } else if (storedValue == EmptyValue) {
                            // Not found
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                        } else if (storedValue == EmptyValue) {
                            // Not found
                            return null;
                        }
                    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                            SIZE_UPDATER.incrementAndGet(this);
                            ++usedBuckets;
                            return valueProvider != null ? values[bucket] : null;
                        } else if (storedValue == DeletedValue) {
                            values[bucket] = value != null ? value : valueProvider.apply(key);
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                            values[bucket] = value != null ? value : valueProvider.apply(key);
                            SIZE_UPDATER.incrementAndGet(this);
                            return valueProvider != null ? values[bucket] : null;
                        } else if (!onlyIfAbsent) {
                            // Over written an old value for same key
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                        values[bucket] = value != null ? value : valueProvider.apply(key);
                        SIZE_UPDATER.incrementAndGet(this);
                        return valueProvider != null ? values[bucket] : null;
                    } else if (storedValue == DeletedValue) {
                        // The bucket contained a different deleted key
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                        if (value == null || value.equals(storedValue)) {
                            if (storedValue == EmptyValue || storedValue == DeletedValue) {
                                return null;
                            }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                            return storedValue;
                        } else {
                            return null;
                        }
                    } else if (storedValue == EmptyValue) {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                    } else if (storedValue == EmptyValue) {
                        // Key wasn't found
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
    public Range<T> firstRange() {
        if (rangeBitSetMap.isEmpty()) {
            return null;
        }
        Entry<Long, BitSet> firstSet = rangeBitSetMap.firstEntry();
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
    public Range<T> lastRange() {
        if (rangeBitSetMap.isEmpty()) {
            return null;
        }
        Entry<Long, BitSet> lastSet = rangeBitSetMap.lastEntry();
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
    public Range<T> span() {
        if (rangeBitSetMap.isEmpty()) {
            return null;
        }
        Entry<Long, BitSet> firstSet = rangeBitSetMap.firstEntry();
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
            if (!rangeBitSet.get(getSafeEntry(value))) {
                // if position is not part of any range then return null
                return null;
            }
            int lowerValue = rangeBitSet.previousClearBit(getSafeEntry(value)) + 1;
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
            return Range.closed(lower, upper);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongLongPairHashMap.java`
#### Snippet
```java
                        } else if (storedKey1 == EmptyKey) {
                            // Not found
                            return null;
                        }
                    } else {
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongLongPairHashMap.java`
#### Snippet
```java
                        } else if (storedKey1 == EmptyKey) {
                            // Not found
                            return null;
                        }
                    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmMetrics.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
            }
            if (allConfigValuesAreNull) {
                return null;
            } else {
                return offloadPolicies;
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
        } catch (Exception e) {
            log.error("Failed to merge configuration.", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/TopicPolicies.java`
#### Snippet
```java

    public Set<String> getReplicationClustersSet() {
        return replicationClusters != null ? Sets.newTreeSet(this.replicationClusters) : null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            message = incomingMessages.poll(timeout, unit);
            if (message == null) {
                return null;
            }
            messageProcessed(message);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                throw PulsarClientException.unwrap(e);
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                trackMessage(msgId);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            compressedPayload.release();
            increaseAvailablePermits(cnx);
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                            consumerName, startMessageId);
                }
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            if (singleMessageMetadata != null && singleMessageMetadata.isCompactedOut()) {
                // message has been compacted out, so don't send to the user
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

            if (ackBitSet != null && !ackBitSet.get(index)) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                throw PulsarClientException.unwrap(e);
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                    messageId);
            discardCorruptedMessage(messageId, currentCnx, ValidationError.UncompressedSizeCorruption);
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                    messageId, e.getMessage(), e);
            discardCorruptedMessage(messageId, currentCnx, ValidationError.DecompressionError);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                            topic, subscription, consumerName);
                    discardMessage(messageId, currentCnx, ValidationError.DecryptionError);
                    return null;
                case FAIL:
                    MessageId m = new MessageIdImpl(messageId.getLedgerId(), messageId.getEntryId(), partitionIndex);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                            topic, subscription, consumerName, m);
                    unAckedMessageTracker.add(m, redeliveryCount);
                    return null;
            }
        }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                        topic, subscription, consumerName, messageId);
                discardMessage(messageId, currentCnx, ValidationError.DecryptionError);
                return null;
            case FAIL:
                MessageId m = new MessageIdImpl(messageId.getLedgerId(), messageId.getEntryId(), partitionIndex);
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                        topic, subscription, consumerName, m);
                unAckedMessageTracker.add(m, redeliveryCount);
                return null;
        }
        return null;
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                return null;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            return message.getTopicName();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                            .setEntryId(messageId.getEntryId());
                }
                return null;
            });
        }).collect(Collectors.toList());
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                }
                cleanupAtClose(closeFuture, ignoreException ? null : exception);
                return null;
            });
        }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            return (MessageImpl<?>) message;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            return message.getMessageId().toString();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/ConsumerStatsImpl.java`
#### Snippet
```java

    public String getClientVersion() {
        return clientVersionOffset == -1 ? null
                : stringBuffer.substring(clientVersionOffset, clientVersionOffset + clientVersionLength);
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/ConsumerStatsImpl.java`
#### Snippet
```java

    public String getConnectedSince() {
        return connectedSinceOffset == -1 ? null
                : stringBuffer.substring(connectedSinceOffset, connectedSinceOffset + connectedSinceLength);
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/ConsumerStatsImpl.java`
#### Snippet
```java

    public String getAddress() {
        return addressOffset == -1 ? null : stringBuffer.substring(addressOffset, addressOffset + addressLength);
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/PublisherStatsImpl.java`
#### Snippet
```java

    public String getClientVersion() {
        return clientVersionOffset == -1 ? null
                : stringBuffer.substring(clientVersionOffset, clientVersionOffset + clientVersionLength);
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/PublisherStatsImpl.java`
#### Snippet
```java

    public String getConnectedSince() {
        return connectedSinceOffset == -1 ? null
                : stringBuffer.substring(connectedSinceOffset, connectedSinceOffset + connectedSinceLength);
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/PublisherStatsImpl.java`
#### Snippet
```java

    public String getProducerName() {
        return producerNameOffset == -1 ? null
                : stringBuffer.substring(producerNameOffset, producerNameOffset + producerNameLength);
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/PublisherStatsImpl.java`
#### Snippet
```java

    public String getAddress() {
        return addressOffset == -1 ? null : stringBuffer.substring(addressOffset, addressOffset + addressLength);
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicies.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicies.java`
#### Snippet
```java
    public NamespaceIsolationPolicy getPolicyByName(String policyName) {
        if (policies.get(policyName) == null) {
            return null;
        }
        return new NamespaceIsolationPolicyImpl(policies.get(policyName));
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/schema/BytesSchemaVersion.java`
#### Snippet
```java

    public static BytesSchemaVersion of(byte[] bytes) {
        return bytes != null ? new BytesSchemaVersion(bytes) : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/intercept/ManagedLedgerPayloadProcessor.java`
#### Snippet
```java
     */
    default Processor outputProcessor() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/intercept/ManagedLedgerPayloadProcessor.java`
#### Snippet
```java
     */
    default Processor inputProcessor() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
    public static X509Certificate[] loadCertificatesFromPemStream(InputStream inStream) throws KeyManagementException  {
        if (inStream == null) {
            return null;
        }
        CertificateFactory cf;
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
    public static PrivateKey loadPrivateKeyFromPemStream(InputStream inStream) throws KeyManagementException {
        if (inStream == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
    public static PrivateKey loadPrivateKeyFromPemFile(String keyFilePath) throws KeyManagementException {
        if (keyFilePath == null || keyFilePath.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                log.warn("Conscrypt isn't available. Using JDK default security provider.", e);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
        } catch (ReflectiveOperationException e) {
            log.warn("Unable to get security provider for class {}", CONSCRYPT_PROVIDER_CLASS, e);
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
    @Override
    public ResourceUsage getBandwidthIn() {
        return systemResourceUsage != null ? systemResourceUsage.bandwidthIn : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
    @Override
    public ResourceUsage getCpu() {
        return systemResourceUsage != null ? systemResourceUsage.cpu : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
    @Override
    public ResourceUsage getMemory() {
        return systemResourceUsage != null ? systemResourceUsage.memory : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
    @Override
    public ResourceUsage getBandwidthOut() {
        return systemResourceUsage != null ? systemResourceUsage.bandwidthOut : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
    public TreeMap<String, NamespaceBundleStats> getSortedBundleStats(ResourceType resType) {
        if (bundleStats == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
    @Override
    public ResourceUsage getDirectMemory() {
        return systemResourceUsage != null ? systemResourceUsage.directMemory : null;
    }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerException.java`
#### Snippet
```java
    public synchronized Throwable fillInStackTrace() {
        // Disable stack traces to be filled in
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorContainer.java`
#### Snippet
```java
        long stamp = rwLock.readLock();
        try {
            return heap.isEmpty() ? null : heap.get(0).position;
        } finally {
            rwLock.unlockRead(stamp);
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorContainer.java`
#### Snippet
```java
    private Item getRight(Item item) {
        int i = item.idx * 2 + 2;
        return i < heap.size() ? heap.get(i) : null;
    }
}
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorContainer.java`
#### Snippet
```java
        try {
            Item item = cursors.get(name);
            return item != null ? item.cursor : null;
        } finally {
            rwLock.unlockRead(stamp);
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorContainer.java`
#### Snippet
```java
        long stamp = rwLock.readLock();
        try {
            return heap.isEmpty() ? null : heap.get(0).cursor;
        } finally {
            rwLock.unlockRead(stamp);
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorContainer.java`
#### Snippet
```java
            Item item = cursors.get(cursor.getName());
            if (item == null || item.idx == -1) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorContainer.java`
#### Snippet
```java
    private Item getLeft(Item item) {
        int i = item.idx * 2 + 1;
        return i < heap.size() ? heap.get(i) : null;
    }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/NullLedgerOffloader.java`
#### Snippet
```java
    @Override
    public OffloadPoliciesImpl getOffloadPolicies() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/MetaStoreImpl.java`
#### Snippet
```java
            return metadataBytes;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
        } catch (Throwable t) {
            log.warn("[{}] Failed to allocate buffer for entry cache: {}", ml.getName(), t.getMessage());
            return null;
        }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
                pendingReadsManager.invalidateLedger(lh.getId());
            }
            return null;
        });
        return readResult;
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
                originalCallback.readEntriesFailed(
                        new ManagedLedgerException.TooManyRequestsException(message), ctx);
                return null;
            }
            ml.getExecutor().submitOrdered(lh.getId(), () -> {
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
                asyncReadEntry0WithLimits(lh, firstEntry, lastEntry, shouldCacheEntry,
                        originalCallback, ctx, newHandle);
                return null;
            });
            return null;
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
                return null;
            });
            return null;
        } else {
            callback = new AsyncCallbacks.ReadEntriesCallback() {
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/RangeCache.java`
#### Snippet
```java
        Value value = entries.get(key);
        if (value == null) {
            return null;
        } else {
            try {
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/RangeCache.java`
#### Snippet
```java
            } catch (Throwable t) {
                // Value was already destroyed between get() and retain()
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
                return new PendingReadKey(other.endEntry + 1, endEntry);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
                return new PendingReadKey(startEntry, other.startEntry - 1);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/intercept/ManagedLedgerInterceptor.java`
#### Snippet
```java
    default PayloadProcessorHandle processPayloadBeforeLedgerWrite(OpAddEntry ledgerWriteOp,
                                                                   ByteBuf dataToBeStoredInLedger){
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/intercept/ManagedLedgerInterceptor.java`
#### Snippet
```java
     */
    default PayloadProcessorHandle processPayloadBeforeEntryCache(ByteBuf dataReadFromLedger){
        return null;
    }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/offload/OffloaderUtils.java`
#### Snippet
```java
            rethrowIOException(t);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    public synchronized ConsumerStats getStats() {
        if (stats == null) {
            return null;
        }
        stats.reset();
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
            return TopicName.get(topic);
        } catch (Exception ignored) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                throw PulsarClientException.unwrap(e);
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                    PulsarClientException.wrap(((Throwable) e).getCause(),
                            String.format("Failed to subscribe %s with %d partitions", topicName, numPartitions)));
                return null;
            });
        return consumer;
```

### ReturnNull
Return of `null`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    private String getFullTopicName(String topic) {
        TopicName topicName = getTopicName(topic);
        return (topicName != null) ? topicName.toString() : null;
    }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockImpl.java`
#### Snippet
```java
        public String toSafeString() {
            // TODO Auto-generated method stub
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockImpl.java`
#### Snippet
```java
        public byte[] getPassword() {
            // TODO Auto-generated method stub
            return null;
        }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockUtils.java`
#### Snippet
```java
    public static String parseContextUuid(String name, Long ledgerId) {
        if (ledgerId == null || name == null) {
            return null;
        }
        int pos = name.indexOf("-ledger-" + ledgerId);
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockUtils.java`
#### Snippet
```java
        int pos = name.indexOf("-ledger-" + ledgerId);
        if (pos <= 0) {
            return null;
        }
        return name.substring(0, pos);
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockUtils.java`
#### Snippet
```java
    public static Long parseLedgerId(String name) {
        if (name == null || name.isEmpty()) {
            return null;
        }
        if (name.endsWith("-index")) {
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockUtils.java`
#### Snippet
```java
        int pos = name.indexOf("-ledger-");
        if (pos < 0) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockUtils.java`
#### Snippet
```java
            return Long.parseLong(name.substring(pos + 8));
        } catch (NumberFormatException err) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
        @Override
        public String toSafeString() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
            return result;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
        @Override
        public NavigableMap<Long, ? extends List<BookieId>> getAllEnsembles() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
        @Override
        public State getState() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
        @Override
        public List<BookieId> getEnsembleAt(long entryId) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
        @Override
        public DigestType getDigestType() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
        @Override
        public Map<String, byte[]> getCustomMetadata() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
    public static JCloudBlobStoreProvider getProvider(String driver) {
        if (StringUtils.isEmpty(driver)) {
            return null;
        }
        for (JCloudBlobStoreProvider provider : JCloudBlobStoreProvider.values()) {
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java

            default:
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java

            default:
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
        final MessageMetadata localMetadata = peekMessageMetadata(metadataAndPayload, subscription, consumerId);
        if (localMetadata == null) {
            return null;
        }
        final MessageMetadata metadata = new MessageMetadata();
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
        } catch (Throwable t) {
            log.error("[{}] [{}] Failed to parse message metadata", subscription, consumerId, t);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
            return brokerEntryMetadata;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/RocksdbMetadataStore.java`
#### Snippet
```java
        public static MetaValue parse(byte[] dataBytes) throws MetadataStoreException {
            if (dataBytes == null) {
                return null;
            }
            if (dataBytes.length < 4) {
```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java

    private File readFile(String path) {
        return StringUtils.isEmpty(path) ? null : new File(path);
    }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BKCluster.java`
#### Snippet
```java
        Pattern pattern = Pattern.compile(".*bookieHost: \"(.*?)\".*", Pattern.DOTALL);
        Matcher m = pattern.matcher(cookie.toString());
        return m.find() ? m.group(1) : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BKCluster.java`
#### Snippet
```java
        } catch (SocketException var3) {
            log.warn("Exception while figuring out loopback interface. Will use null.", var3);
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BKCluster.java`
#### Snippet
```java

        log.warn("Unable to deduce loopback interface. Will use null");
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
        if (idx <= 0) {
            // No parent
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
                    return Long.parseLong(t.replace(hobPrefix, ""));
                } catch (NumberFormatException e) {
                    return null;
                }
            }).filter((t) -> t != null)
```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
                                            return Long.parseLong(t.replace(hobPrefix, ""));
                                        } catch (NumberFormatException e) {
                                            return null;
                                        }
                                    })
```

### ReturnNull
Return of `null`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                if (!canContinue) {
                    log.info("Iteration stopped by the OffloadedLedgerMetadataConsumer");
                    return null;
                }
            } catch (Exception err) {
```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationClient.java`
#### Snippet
```java
    private static BookieId stripBookieIdFromPath(String path) {
        if (path == null) {
            return null;
        }
        final int slash = path.lastIndexOf('/');
```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationClient.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
                    log.debug("Ledger: {} is not marked underreplicated", ledgerId);
                }
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
            if (!optRes.isPresent()) {
                // this is ok.
                return null;
            }

```

### ReturnNull
Return of `null`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
                                    }
                                }
                                return null;
                            });
                }
```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java
            }
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java
            return new Gson().fromJson(batchSourceConfigJson, BatchSourceConfig.class);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/ValidatorUtils.java`
#### Snippet
```java
        } catch (IllegalArgumentException e) {
            // schemaType is not referring to builtin type
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/CryptoUtils.java`
#### Snippet
```java
    public static CryptoConfig convertFromSpec(Function.CryptoSpec spec) {
        if (spec == null || isEmpty(spec.getCryptoKeyReaderClassName())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/Worker.java`
#### Snippet
```java
            return new AuthorizationService(getServiceConfiguration(), this.pulsarResources);
            }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/io/ConnectorUtils.java`
#### Snippet
```java
        } catch (Throwable t) {
            log.warn("Failed to load connector from {}", archive, t);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.GO) {
            doGolangChecks(functionConfig);
            return null;
        } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.PYTHON) {
            doPythonChecks(functionConfig);
```

### ReturnNull
Return of `null`
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        } else if (functionConfig.getRuntime() == FunctionConfig.Runtime.PYTHON) {
            doPythonChecks(functionConfig);
            return null;
        } else {
            throw new IllegalArgumentException("Function language runtime is either not set or cannot be determined");
```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/dlog/DLInputStream.java`
#### Snippet
```java
    } catch (EndOfStreamException e) {
      eos = true;
      return null;
    }
  }
```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/dlog/DLInputStream.java`
#### Snippet
```java
        return new LogRecordWithInputStream(record);
      } else {
        return null;
      }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/FunctionApiResource.java`
#### Snippet
```java
        return httpRequest != null
                ? (String) httpRequest.getAttribute(AuthenticationFilter.AuthenticatedRoleAttributeName)
                : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/SchedulerManager.java`
#### Snippet
```java
            FunctionDetails funDetails = funInstance.getFunctionMetaData().getFunctionDetails();
            return HEARTBEAT_TENANT.equals(funDetails.getTenant())
                    && HEARTBEAT_NAMESPACE.equals(funDetails.getNamespace()) ? funDetails.getName() : null;
        }
        return null;
```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/SchedulerManager.java`
#### Snippet
```java
                    && HEARTBEAT_NAMESPACE.equals(funDetails.getNamespace()) ? funDetails.getName() : null;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/WorkerStatsApiV2Resource.java`
#### Snippet
```java
        return httpRequest != null
                ? (String) httpRequest.getAttribute(AuthenticationFilter.AuthenticatedRoleAttributeName)
                : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/WorkerApiV2Resource.java`
#### Snippet
```java
        return httpRequest != null
                ? (String) httpRequest.getAttribute(AuthenticationFilter.AuthenticatedRoleAttributeName)
                : null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/WorkerServiceLoader.java`
#### Snippet
```java
        } catch (Throwable t) {
            rethrowIOException(t);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        if (config.isDeletionAtBatchIndexLevelEnabled()) {
            BitSetRecyclable bitSet = batchDeletedIndexes.get(position);
            return bitSet == null ? null : bitSet.toLongArray();
        } else {
            return null;
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            return bitSet == null ? null : bitSet.toLongArray();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    public Position getNextLedgerPosition(long currentLedgerId) {
        Long nextExistingLedger = ledger.getNextValidLedger(currentLedgerId);
        return nextExistingLedger != null ? PositionImpl.get(nextExistingLedger, 0) : null;
    }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    public long[] getBatchPositionAckSet(Position position) {
        if (!(position instanceof PositionImpl)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            BitSetRecyclable bitSetRecyclable = batchDeletedIndexes.get(position);
            if (bitSetRecyclable == null) {
                return null;
            } else {
                return bitSetRecyclable.toLongArray();
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            }
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    public Position getFirstPosition() {
        Long firstLedgerId = ledger.getLedgersInfo().firstKey();
        return firstLedgerId == null ? null : new PositionImpl(firstLedgerId, 0);
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
    public Throwable getDeathException() {
        if (isAlive()) {
            return null;
        } else if (null != javaInstanceRunnable) {
            return javaInstanceRunnable.getDeathException();
```

### ReturnNull
Return of `null`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
            return javaInstanceRunnable.getDeathException();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
            MessageId msgId = producer.send(targetArray);
            if (reader == null) {
                return null;
            }
            long curTime = System.currentTimeMillis();
```

### ReturnNull
Return of `null`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-functions/secrets/src/main/java/org/apache/pulsar/functions/secretsprovider/ClearTextSecretsProvider.java`
#### Snippet
```java
            return pathToSecret.toString();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/secrets/src/main/java/org/apache/pulsar/functions/secretsproviderconfigurator/DefaultSecretsProviderConfigurator.java`
#### Snippet
```java
    @Override
    public Map<String, String> getSecretsProviderConfig(Function.FunctionDetails functionDetails) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/secrets/src/main/java/org/apache/pulsar/functions/secretsproviderconfigurator/KubernetesSecretsProviderConfigurator.java`
#### Snippet
```java
    @Override
    public Map<String, String> getSecretsProviderConfig(Function.FunctionDetails functionDetails) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/api-java/src/main/java/org/apache/pulsar/functions/api/Record.java`
#### Snippet
```java

    default Schema<T> getSchema() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/api-java/src/main/java/org/apache/pulsar/functions/api/utils/JavaSerDe.java`
#### Snippet
```java
            log.info("Exception during serialization", ex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/SerDeSchema.java`
#### Snippet
```java
    public SchemaInfo getSchemaInfo() {
        // Do not persist schema information
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/LogAppender.java`
#### Snippet
```java
    @Override
    public Layout<? extends Serializable> getLayout() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            }
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                return KubernetesSecretsTokenAuthProvider.class.getName();
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java
                || pulsarSinkConfig.getProducerConfig().getCryptoConfig() == null
                || isEmpty(pulsarSinkConfig.getProducerConfig().getCryptoConfig().getCryptoKeyReaderClassName())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java
        if (Void.class.equals(typeArg)) {
            // return type is 'void', so there's no schema to check
            return null;
        }
        ConsumerConfig consumerConfig = new ConsumerConfig();
```

### ReturnNull
Return of `null`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    @Override
    public Throwable getDeathException() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
            return RuntimeUtils.getPrometheusMetrics(metricsPort);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/AbstractSinkRecord.java`
#### Snippet
```java
    protected static <T> Schema<T> getRecordSchema(Record<T> record) {
        if (record == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/AbstractSinkRecord.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/state/StateStoreProvider.java`
#### Snippet
```java
        @Override
        public <T extends StateStore> T getStateStore(String tenant, String namespace, String name) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstance.java`
#### Snippet
```java
                    }
                }, executor);
                return null;
            } catch (InterruptedException ie) {
                log.warn("Exception while put Async requests", ie);
```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceUtils.java`
#### Snippet
```java
    private static <T> T createInstance(String className, ClassLoader clsLoader, Class<T> baseClass) {
        if (StringUtils.isEmpty(className)) {
            return null;
        } else {
            return Reflections.createInstance(className, baseClass, clsLoader);
```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/state/BKStateStoreImpl.java`
#### Snippet
```java
                            return result;
                        }
                        return null;
                    } finally {
                        if (data != null) {
```

### ReturnNull
Return of `null`
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/MergeTopicFunction.java`
#### Snippet
```java
            if (!msg.getReaderSchema().isPresent()) {
                log.warn("The reader schema is null.");
                return null;
            }
            log.info("process message with reader schema {}", msg.getReaderSchema().get());
```

### ReturnNull
Return of `null`
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/MergeTopicFunction.java`
#### Snippet
```java
            log.warn("context current record message is not present.");
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/CursorManagementFunction.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/HostAppenderFunction.java`
#### Snippet
```java
            return input + InetAddress.getLocalHost().getHostName();
        } catch (UnknownHostException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/ContextImpl.java`
#### Snippet
```java
            return secretsProvider.provideSecret(secretName, secretsMap.get(secretName));
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
                    .build();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        Long ledgerId = ledgers.firstKey();
        if (ledgerId == null) {
            return null;
        }
        if (ledgerId > lastConfirmedEntry.getLedgerId()) {
```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    PositionImpl getMarkDeletePositionOfSlowestConsumer() {
        ManagedCursor slowestCursor = getSlowestConsumer();
        return slowestCursor == null ? null : (PositionImpl) slowestCursor.getMarkDeletedPosition();
    }

```

### ReturnNull
Return of `null`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                ledgerMetadataFutures.put(ledgerId, this.getLedgerMetadata(ledgerId).exceptionally(throwable -> {
                    log.warn("Getting metadata for ledger {} failed.", ledgerId, throwable);
                    return null;
                }));
            });
```

### ReturnNull
Return of `null`
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            return connector.getClassLoader();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            return connector.getClassLoader();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            return function.getClassLoader();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ReaderHandler.java`
#### Snippet
```java
            return ((ReaderImpl<?>) reader).getConsumer();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/AbstractWebSocketHandler.java`
#### Snippet
```java

    protected String checkAuthentication() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default Certificate[] getTlsCertificates() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default Set<Map.Entry<String, String>> getHttpHeaders() throws Exception {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default KeyStoreParams getTlsKeyStoreParams() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default String getTlsCerificateFilePath() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default String getHttpAuthType() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default String getTlsPrivateKeyFilePath() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default String getCommandData() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default PrivateKey getTlsPrivateKey() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationDataProvider.java`
#### Snippet
```java
     */
    default InputStream getTlsTrustStoreStream() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Range.java`
#### Snippet
```java
            return Range.of(start, end);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/internal/PulsarClientImplementationBinding.java`
#### Snippet
```java
    static byte[] getBytes(ByteBuffer byteBuffer) {
        if (byteBuffer == null) {
            return null;
        }
        if (byteBuffer.hasArray() && byteBuffer.arrayOffset() == 0
```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Schema.java`
#### Snippet
```java
    default T decode(ByteBuffer data) {
        if (data == null) {
            return null;
        }
        return decode(getBytes(data));
```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Schema.java`
#### Snippet
```java
    default T decode(ByteBuffer data, byte[] schemaVersion) {
        if (data == null) {
            return null;
        }
        return decode(getBytes(data), schemaVersion);
```

### ReturnNull
Return of `null`
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
            return (IMessageFormatter) clz.getDeclaredConstructor().newInstance();
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
                    // try to get a new Producer.
                    wellnessFlag.value = false;
                    return null;
                };
                while (!stop.get() && wellnessFlag.value) {
```

### ReturnNull
Return of `null`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
            e = t.getCause();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
            this.windowManager.add(record, System.currentTimeMillis(), record);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/InMemTransactionMetadataStore.java`
#### Snippet
```java
    @Override
    public TransactionCoordinatorStats getCoordinatorStats() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/InMemTransactionMetadataStore.java`
#### Snippet
```java
    @Override
    public List<TxnMeta> getSlowTransactions(long timeout) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            return (IMessageFormatter) clz.getDeclaredConstructor().newInstance();
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/ComponentResource.java`
#### Snippet
```java
            return auth.newRequestHeader(targetUrl, authData, null);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/http/AsyncHttpConnector.java`
#### Snippet
```java
            log.error(e.getMessage());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/BaseResource.java`
#### Snippet
```java
    public PulsarAdminException getApiException(Response response) {
        if (response.getStatus() == Response.Status.OK.getStatusCode()) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/AdminShell.java`
#### Snippet
```java
    @Override
    public String getServiceUrl() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ClientShell.java`
#### Snippet
```java
    @Override
    public String getAdminUrl() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/ConfigStore.java`
#### Snippet
```java
                            return split[1];
                        }
                        return null;
                    }
                }
```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/ConfigStore.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
    @Override
    public String getAdminUrl() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
    @Override
    public String getServiceUrl() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/FileConfigStore.java`
#### Snippet
```java
            return getConfig(fileConfig.last);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/NamespacesImpl.java`
#### Snippet
```java
    public CompletableFuture<String> getPropertyAsync(String namespace, String key) {
        return asyncGetNamespaceParts(new FutureCallback<String>(){}, namespace, "property", key)
                .thenApply(value -> StringUtils.isEmpty(value) ? null : value);
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java
            // since different placement policy will have different default rack,
            // don't be smart here and just return null
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServlets.java`
#### Snippet
```java

        if (additionalServletDirectory == null || additionalServlets == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServlets.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServletUtils.java`
#### Snippet
```java
        } catch (Throwable t) {
            rethrowIOException(t);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            previousLedger = l.ledgerId;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/TenantResources.java`
#### Snippet
```java
                                    checkNs.complete(null);
                                }
                                return null;
                            }).exceptionally(ex2 -> {
                                if (ex2.getCause() instanceof MetadataStoreException.ContentDeserializationException) {
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/TenantResources.java`
#### Snippet
```java
                                    checkNs.completeExceptionally(ex2);
                                }
                                return null;
                            });
                        });
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/MetadataStoreCacheLoader.java`
#### Snippet
```java
                        return null;
                    });
                    return null;
                }).exceptionally(ex -> {
                    log.warn("Error updating broker info after broker list changed", ex);
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/MetadataStoreCacheLoader.java`
#### Snippet
```java
                }).exceptionally(ex -> {
                    log.warn("Error updating broker info after broker list changed", ex);
                    return null;
                });
            }
```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
    private static String getHostFromUrl(String url) {
        if (url == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
            return URI.create(url).getHost();
        } catch (IllegalArgumentException iea) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
        final JCommander commander = mainCommander.getCommands().get(providerCmd);
        if (commander == null) {
            return null;
        }
        return (ShellCommandsProvider) commander.getObjects().get(0);
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/OneStageAuthenticationState.java`
#### Snippet
```java
    @Override
    public AuthData authenticate(AuthData authData) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationDataCommand.java`
#### Snippet
```java
        } catch (SSLPeerUnverifiedException e) {
            log.error("Failed to verify the peer's identity", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationDataSource.java`
#### Snippet
```java
     */
    default String getSubscription() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationDataSource.java`
#### Snippet
```java
     */
    default String getCommandData() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationDataSource.java`
#### Snippet
```java
     */
    default String getHttpAuthType() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationDataSource.java`
#### Snippet
```java
     */
    default Certificate[] getTlsCertificates() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationDataSource.java`
#### Snippet
```java
     */
    default SocketAddress getPeerAddress() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationDataSource.java`
#### Snippet
```java
     */
    default String getHttpHeader(String name) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderToken.java`
#### Snippet
```java
            String token = new String(authData.getBytes(), UTF_8);
            checkExpiration(token);
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderToken.java`
#### Snippet
```java
            return tokenAudienceClaim;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderToken.java`
#### Snippet
```java
                return (String) list.get(0);
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderToken.java`
#### Snippet
```java
            return tokenAudience;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/PulsarAuthorizationProvider.java`
#### Snippet
```java
                    log.warn("Client with Role - {} failed to get permissions for topic - {}. {}", role, topicName,
                            ex.getMessage());
                    return null;
                });
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Mode.java`
#### Snippet
```java
                return ALL;
            default :
                return null;

        }
```

### ReturnNull
Return of `null`
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderSasl.java`
#### Snippet
```java

        if (tokenStr == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderSasl.java`
#### Snippet
```java
            return SASL_AUTH_ROLE_TOKEN_EXPIRED;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderSasl.java`
#### Snippet
```java
        String id = request.getHeader(SASL_STATE_SERVER);
        if (id == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderSasl.java`
#### Snippet
```java
            log.error("[{}] Wrong Id String in Token {}. e:", request.getRequestURI(),
                id, e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/conf/InternalConfigurationData.java`
#### Snippet
```java
            return "zk:" + zookeeperServers;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/SubscribeRate.java`
#### Snippet
```java
            return subscribeRate;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/AutoFailoverPolicyType.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/PublishRate.java`
#### Snippet
```java
            return publishRate;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/SchemaCompatibilityStrategy.java`
#### Snippet
```java
    public static SchemaCompatibilityStrategy fromAutoUpdatePolicy(SchemaAutoUpdateCompatibilityStrategy strategy) {
        if (strategy == null) {
            return null;
        }
        switch (strategy) {
```

### ReturnNull
Return of `null`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/impl/DispatchRateImpl.java`
#### Snippet
```java
            return dispatchRate;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
                    future.completeExceptionally(ex);
                }
                return null;
            }
            for (int i = 0; i < Math.min(r.size(), numMessages); i++) {
```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
            }
            peekMessagesAsync(topic, subName, numMessages - r.size(), messages, future, nthMessage + 1);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
    public CompletableFuture<String> getShadowSourceAsync(String shadowTopic) {
        return getPropertiesAsync(shadowTopic).thenApply(
                properties -> properties != null ? properties.get(PROPERTY_SHADOW_SOURCE_KEY) : null);
    }

```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
                    future.completeExceptionally(ex);
                }
                return null;
            }
            future.complete(r);
```

### ReturnNull
Return of `null`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
            }
            future.complete(r);
            return null;
        });
        return future;
```

### ReturnNull
Return of `null`
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
                    @Override
                    public SecretKey load(ByteBuffer key) {
                        return null;
                    }

```

## RuleId[ruleID=AssignmentToLambdaParameter]
### AssignmentToLambdaParameter
Assignment to lambda parameter `message`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java

                                                    if (message.getNumChunksFromMsg() > 1)  {
                                                        message = processChunkedMessages(message);
                                                    } else if (entryExceedSplitEndPosition(entry)) {
                                                        // skip no chunk or no multi chunk message
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `message`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
                                                        // that exceed split end position
                                                        message.release();
                                                        message = null;
                                                    }
                                                    if (message != null) {
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/PersistentTopics.java`
#### Snippet
```java
                        log.warn("[{}] Failed to get topic backlog {}: Namespace does not exist", clientAppId(),
                                namespaceName);
                        ex = new RestException(Response.Status.NOT_FOUND, "Namespace does not exist");
                    } else if (!isRedirectException(ex)) {
                        log.error("[{}] Failed to get estimated backlog for topic {}", clientAppId(), encodedTopic, ex);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/PersistentTopics.java`
#### Snippet
```java
                    Throwable t = FutureUtil.unwrapCompletionException(ex);
                    if (!force && (t instanceof BrokerServiceException.TopicBusyException)) {
                        ex = new RestException(Response.Status.PRECONDITION_FAILED,
                                t.getMessage());
                    }
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/PersistentTopics.java`
#### Snippet
```java
                    }
                    if (isManagedLedgerNotFoundException(t)) {
                        ex = new RestException(Response.Status.NOT_FOUND,
                                getTopicNotFoundErrorMessage(topicName.toString()));
                    } else if (!isRedirectException(ex)) {
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `v`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ConsistentHashingStickyKeyConsumerSelector.java`
#### Snippet
```java
                        v.removeIf(c -> c.equals(consumer));
                        if (v.isEmpty()) {
                            v = null;
                        }
                        return v;
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
                    Throwable t = FutureUtil.unwrapCompletionException(ex);
                    if (!force && (t instanceof BrokerServiceException.TopicBusyException)) {
                        ex = new RestException(Response.Status.PRECONDITION_FAILED,
                                t.getMessage());
                    }
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
                    }
                    if (isManagedLedgerNotFoundException(t)) {
                        ex = new RestException(Response.Status.NOT_FOUND,
                                getTopicNotFoundErrorMessage(topicName.toString()));
                    } else if (!isRedirectException(ex)) {
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
                        log.warn("[{}] Failed to get topic backlog {}: Namespace does not exist", clientAppId(),
                                namespaceName);
                        ex = new RestException(Response.Status.NOT_FOUND, "Namespace does not exist");
                    } else if (!isRedirectException(ex)) {
                        log.error("[{}] Failed to get estimated backlog for topic {}", clientAppId(), encodedTopic, ex);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `topicListeners`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicBasedTopicPoliciesService.java`
#### Snippet
```java
        listeners.compute(topicName, (k, topicListeners) -> {
            if (topicListeners == null) {
                topicListeners = new CopyOnWriteArrayList<>();
            }
            topicListeners.add(listener);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `topicListeners`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicBasedTopicPoliciesService.java`
#### Snippet
```java
                topicListeners.remove(listener);
                if (topicListeners.isEmpty()) {
                    topicListeners = null;
                }
            }
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `properties`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                }).thenAccept((Map<String, String> properties) -> {
                    if (properties == null) {
                        properties = Collections.emptyMap();
                    }
                    asyncResponse.resume(Response.ok(properties).build());
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                        // do-nothing
                    } else {
                        ex = handleTxnException(ex, BaseCommand.Type.NEW_TXN.name(), requestId);

                        commandSender.sendNewTxnErrorResponse(requestId, tcId.getId(),
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                                txnID.getLeastSigBits(), txnID.getMostSigBits()));
                    } else {
                        ex = handleTxnException(ex, BaseCommand.Type.ADD_SUBSCRIPTION_TO_TXN.name(), requestId);

                        ctx.writeAndFlush(Commands.newAddSubscriptionToTxnResponse(requestId,
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                                txnID.getLeastSigBits(), txnID.getMostSigBits()));
                    } else {
                        ex = handleTxnException(ex, BaseCommand.Type.ADD_PARTITION_TO_TXN.name(), requestId);

                        ctx.writeAndFlush(Commands.newAddPartitionToTxnResponse(requestId, txnID.getMostSigBits(),
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                                txnID, txnAction);
                    } else {
                        ex = handleTxnException(ex, BaseCommand.Type.END_TXN.name(), requestId);
                        commandSender.sendEndTxnErrorResponse(requestId, txnID,
                                BrokerServiceException.getClientErrorCode(ex), ex.getMessage());
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `policies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
            updatePolicies(namespaceName, policies -> {
                try {
                    policies = policyTransformation.apply(policies);
                } catch (Exception e) {
                    exception.setValue(e);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `throwable`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                        })
                        .exceptionally(throwable -> {
                            throwable = throwable.getCause();
                            if (throwable instanceof NotAllowedException) {
                              publishContext.completed((NotAllowedException) throwable, -1, -1);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `throwable`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                              return null;
                            } else if (!(throwable instanceof ManagedLedgerException)) {
                                throwable = new ManagedLedgerException(throwable);
                            }
                            addFailed((ManagedLedgerException) throwable, publishContext);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `value`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/LocalMemoryMetadataStore.java`
#### Snippet
```java
            STATIC_INSTANCE.compute(name, (key, value) -> {
                if (value == null) {
                    value = new HashSet<>();
                }
                value.forEach(v -> {
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `ex`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
        store.put(ledgerPrefix + formatHalfId(hob), new byte[0], Optional.empty())
                .whenComplete((__, ex) -> {
                    ex = FutureUtil.unwrapCompletionException(ex);
                    if (ex != null && !(ex instanceof MetadataStoreException.BadVersionException)) {
                        // BadVersion is OK here because we can have multiple threads (or nodes) trying to create the
```

## RuleId[ruleID=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `nonKey` is redundant
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java
    private static List<String> getListFromConfig(String jdbcSinkConfig) {
        List<String> nonKeyList = Lists.newArrayList();
        String nonKey = jdbcSinkConfig;
        if (nonKey != null && !nonKey.isEmpty()) {
            nonKeyList = Arrays.asList(nonKey.split(","));
```

### UnnecessaryLocalVariable
Local variable `record` is redundant
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaStringSource.java`
#### Snippet
```java
    @Override
    public KafkaRecord buildRecord(ConsumerRecord<Object, Object> consumerRecord) {
        KafkaRecord record = new KafkaRecord(consumerRecord,
                new String((byte[]) consumerRecord.value(), StandardCharsets.UTF_8),
                Schema.STRING,
```

### UnnecessaryLocalVariable
Local variable `cfg` is redundant
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSinkConfig.java`
#### Snippet
```java
    public static MongoSinkConfig load(String yamlFile) throws IOException {
        final ObjectMapper mapper = new ObjectMapper(new YAMLFactory());
        final MongoSinkConfig cfg = mapper.readValue(new File(yamlFile), MongoSinkConfig.class);

        return cfg;
```

### UnnecessaryLocalVariable
Local variable `cfg` is redundant
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSinkConfig.java`
#### Snippet
```java
    public static MongoSinkConfig load(Map<String, Object> map) throws IOException {
        final ObjectMapper mapper = new ObjectMapper();
        final MongoSinkConfig cfg = mapper.readValue(new ObjectMapper().writeValueAsString(map), MongoSinkConfig.class);

        return cfg;
```

### UnnecessaryLocalVariable
Local variable `cfg` is redundant
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSourceConfig.java`
#### Snippet
```java
    public static MongoSourceConfig load(Map<String, Object> map) throws IOException {
        final ObjectMapper mapper = new ObjectMapper();
        final MongoSourceConfig cfg =
                mapper.readValue(new ObjectMapper().writeValueAsString(map), MongoSourceConfig.class);

```

### UnnecessaryLocalVariable
Local variable `cfg` is redundant
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSourceConfig.java`
#### Snippet
```java
    public static MongoSourceConfig load(String yamlFile) throws IOException {
        final ObjectMapper mapper = new ObjectMapper(new YAMLFactory());
        final MongoSourceConfig cfg = mapper.readValue(new File(yamlFile), MongoSourceConfig.class);

        return cfg;
```

### UnnecessaryLocalVariable
Local variable `o` is redundant
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionKey.java`
#### Snippet
```java
  public static void startMetadataVector(FlatBufferBuilder builder, int numElems) { builder.startVector(4, numElems, 4); }
  public static int endEncryptionKey(FlatBufferBuilder builder) {
    int o = builder.endObject();
    return o;
  }
```

### UnnecessaryLocalVariable
Local variable `o` is redundant
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/KeyValue.java`
#### Snippet
```java
  public static void addValue(FlatBufferBuilder builder, int valueOffset) { builder.addOffset(1, valueOffset, 0); }
  public static int endKeyValue(FlatBufferBuilder builder) {
    int o = builder.endObject();
    return o;
  }
```

### UnnecessaryLocalVariable
Local variable `o` is redundant
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/Message.java`
#### Snippet
```java
  public static void startPayloadVector(FlatBufferBuilder builder, int numElems) { builder.startVector(1, numElems, 1); }
  public static int endMessage(FlatBufferBuilder builder) {
    int o = builder.endObject();
    return o;
  }
```

### UnnecessaryLocalVariable
Local variable `o` is redundant
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/fbs/EncryptionCtx.java`
#### Snippet
```java
  public static void addIsBatchMessage(FlatBufferBuilder builder, boolean isBatchMessage) { builder.addBoolean(6, isBatchMessage, false); }
  public static int endEncryptionCtx(FlatBufferBuilder builder) {
    int o = builder.endObject();
    return o;
  }
```

### UnnecessaryLocalVariable
Local variable `offset` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
    // for tests
    private Long currentOffset(TopicPartition topicPartition) {
        Long offset = currentOffsets.computeIfAbsent(topicPartition, kv -> {
            List<ByteBuffer> req = Lists.newLinkedList();
            ByteBuffer key = topicPartitionAsKey(topicPartition);
```

### UnnecessaryLocalVariable
Local variable `offset` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
                    // Use less than 32 bits to represent entry id since it will get
                    // rolled over way before overflowing the max int range
                    long offset = (ledgerId << 28) | entryId;
                    return offset;
                }
```

### UnnecessaryLocalVariable
Local variable `cursor` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            throws ManagedLedgerException, InterruptedException {

        ReadOnlyCursor cursor = managedLedgerFactory.openReadOnlyCursor(topicName.getPersistenceNamingEncoding(),
                startPosition, managedLedgerConfig);

```

### UnnecessaryLocalVariable
Local variable `clusters` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
        try {
            // Remove "global" cluster from returned list
            Set<String> clusters = clusterResources().list().stream()
                    .filter(cluster -> !Constants.GLOBAL_CLUSTER.equals(cluster)).collect(Collectors.toSet());
            return clusters;
```

### UnnecessaryLocalVariable
Local variable `resourceGroup` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ResourceGroupsBase.java`
#### Snippet
```java
        try {
            validateSuperUserAccess();
            ResourceGroup resourceGroup = resourceGroupResources().getResourceGroup(rgName)
                    .orElseThrow(() -> new RestException(Response.Status.NOT_FOUND, "ResourceGroup does not exist"));
            return resourceGroup;
```

### UnnecessaryLocalVariable
Local variable `metrics` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/BrokerStatsBase.java`
#### Snippet
```java
        validateSuperUserAccess();
        try {
            Collection<Metrics> metrics = pulsar().getMetricsGenerator().generate();
            return metrics;
        } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `metrics` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/BrokerStatsBase.java`
#### Snippet
```java
        validateSuperUserAccess();
        try {
            Collection<Metrics> metrics = MBeanStatsGenerator.generate(pulsar());
            return metrics;
        } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `existFuture` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java
                    // in the broker, it doesn't have metadata. If the topic is non-persistent and non-partitioned,
                    // we'll return the true flag.
                    CompletableFuture<Boolean> existFuture = (!topicName.isPersistent() && !topicName.isPartitioned())
                            ? CompletableFuture.completedFuture(true)
                            : pulsar().getNamespaceService().checkTopicExists(topicName)
```

### UnnecessaryLocalVariable
Local variable `preconditionCheck` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

    private CompletableFuture<Policies> precheckWhenDeleteNamespace(NamespaceName nsName, boolean force) {
        CompletableFuture<Policies> preconditionCheck =
                validateTenantOperationAsync(nsName.getTenant(), TenantOperation.DELETE_NAMESPACE)
                        .thenCompose(__ -> validatePoliciesReadOnlyAccessAsync())
```

### UnnecessaryLocalVariable
Local variable `bookkeeperAffinityGroup` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
        }
        try {
            final BookieAffinityGroupData bookkeeperAffinityGroup = getLocalPolicies().getLocalPolicies(namespaceName)
                    .orElseThrow(() -> new RestException(Status.NOT_FOUND,
                            "Namespace local-policies does not exist")).bookieAffinityGroup;
```

### UnnecessaryLocalVariable
Local variable `transactionBufferProvider` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/TransactionBufferProvider.java`
#### Snippet
```java
    static TransactionBufferProvider newProvider(String providerClassName) throws IOException {
        try {
            TransactionBufferProvider transactionBufferProvider = Reflections.createInstance(providerClassName,
                    TransactionBufferProvider.class, Thread.currentThread().getContextClassLoader());
            return transactionBufferProvider;
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
        }

        double result = (1 - weight) * oldValue + weight * newSample;
        return result;
    }
```

### UnnecessaryLocalVariable
Local variable `metrics` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java

    public List<Metrics> getLoadBalancingMetrics() {
        List<Metrics> metrics = this.loadBalancingMetrics.get();
        return metrics;
    }
```

### UnnecessaryLocalVariable
Local variable `newQuota` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
            if (totalBundles > 30 && this.realtimeAvgResourceQuota.getDynamic()) {
                ResourceQuota oldQuota = this.realtimeAvgResourceQuota;
                ResourceQuota newQuota = timeSmoothQuota(oldQuota, totalMsgRateIn / totalBundles,
                        totalMsgRateOut / totalBundles, totalBandwidthIn / totalBundles,
                        totalBandwidthOut / totalBundles, totalMemoryUsage / totalBundles, timePast);
```

### UnnecessaryLocalVariable
Local variable `ackStoreProvider` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/TransactionPendingAckStoreProvider.java`
#### Snippet
```java
    static TransactionPendingAckStoreProvider newProvider(String providerClassName) throws IOException {
        try {
            TransactionPendingAckStoreProvider ackStoreProvider = Reflections.createInstance(providerClassName,
                    TransactionPendingAckStoreProvider.class,
                    Thread.currentThread().getContextClassLoader());
```

### UnnecessaryLocalVariable
Local variable `candidateBrokerHostAndPort` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
            throw new IllegalArgumentException("'" + candidateBroker + "' isn't an URI.");
        }
        String candidateBrokerHostAndPort = candidateBroker.substring(uriSeparatorPos + 3);
        return candidateBrokerHostAndPort;
    }
```

### UnnecessaryLocalVariable
Local variable `executor` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
        ConsumerImpl receivedConsumer = (msg instanceof TopicMessageImpl) ? ((TopicMessageImpl) msg).receivedByconsumer
                : null;
        ExecutorService executor = receivedConsumer != null && receivedConsumer.externalPinnedExecutor != null
                ? receivedConsumer.externalPinnedExecutor
                : externalPinnedExecutor;
```

### UnnecessaryLocalVariable
Local variable `executor` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
        ConsumerImpl receivedConsumer = (msg instanceof TopicMessageImpl) ? ((TopicMessageImpl) msg).receivedByconsumer
                : null;
        ExecutorService executor = receivedConsumer != null && receivedConsumer.internalPinnedExecutor != null
                ? receivedConsumer.internalPinnedExecutor
                : internalPinnedExecutor;
```

### UnnecessaryLocalVariable
Local variable `descriptor` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchema.java`
#### Snippet
```java
        try {
            Method method = pojo.getMethod("getDescriptor");
            Descriptors.Descriptor descriptor = (Descriptors.Descriptor) method.invoke(null);
            return descriptor;
        } catch (IllegalAccessException | InvocationTargetException | NoSuchMethodException e) {
```

### UnnecessaryLocalVariable
Local variable `offset` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/MessageIdUtils.java`
#### Snippet
```java
        // Use less than 32 bits to represent entry id since it will get
        // rolled over way before overflowing the max int range
        long offset = (ledgerId << 28) | entryId;
        return offset;
    }
```

### UnnecessaryLocalVariable
Local variable `payload` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/api/raw/MessageParser.java`
#### Snippet
```java
    public static void parseMessage(TopicName topicName, long ledgerId, long entryId, ByteBuf headersAndPayload,
            MessageProcessor processor, int maxMessageSize) throws IOException {
        ByteBuf payload = headersAndPayload;
        ByteBuf uncompressedPayload = null;
        ReferenceCountedMessageMetadata refCntMsgMetadata = null;
```

### UnnecessaryLocalVariable
Local variable `uncompressedPayload` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/api/raw/MessageParser.java`
#### Snippet
```java

        try {
            ByteBuf uncompressedPayload = codec.decode(payload, uncompressedSize);
            return uncompressedPayload;
        } catch (IOException e) {
```

### UnnecessaryLocalVariable
Local variable `numSections` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
        checkArgument(shrinkFactor > 1);

        int numSections = concurrencyLevel;
        int perSectionExpectedItems = expectedItems / numSections;
        int perSectionCapacity = (int) (perSectionExpectedItems / mapFillFactor);
```

### UnnecessaryLocalVariable
Local variable `newTable` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java

        private void shrinkToInitCapacity() {
            Object[] newTable = new Object[2 * initCapacity];

            table = newTable;
```

### UnnecessaryLocalVariable
Local variable `newValues` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashSet.java`
#### Snippet
```java

        private void shrinkToInitCapacity() {
            V[] newValues = (V[]) new Object[initCapacity];

            values = newValues;
```

### UnnecessaryLocalVariable
Local variable `numSections` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashSet.java`
#### Snippet
```java
        checkArgument(shrinkFactor > 1);

        int numSections = concurrencyLevel;
        int perSectionExpectedItems = expectedItems / numSections;
        int perSectionCapacity = (int) (perSectionExpectedItems / mapFillFactor);
```

### UnnecessaryLocalVariable
Local variable `numSections` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongPairSet.java`
#### Snippet
```java
        checkArgument(shrinkFactor > 1);

        int numSections = concurrencyLevel;
        int perSectionExpectedItems = expectedItems / numSections;
        int perSectionCapacity = (int) (perSectionExpectedItems / SetFillFactor);
```

### UnnecessaryLocalVariable
Local variable `numSections` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
        checkArgument(shrinkFactor > 1);

        int numSections = concurrencyLevel;
        int perSectionExpectedItems = expectedItems / numSections;
        int perSectionCapacity = (int) (perSectionExpectedItems / mapFillFactor);
```

### UnnecessaryLocalVariable
Local variable `key` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
            // to set
        } else {
            long key = lowerKey;
            BitSet rangeBitSet = rangeBitSetMap.computeIfAbsent(key, (k) -> createNewBitSet());
            rangeBitSet.set((int) lowerValue, (int) upperValue + 1);
```

### UnnecessaryLocalVariable
Local variable `numSections` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongLongPairHashMap.java`
#### Snippet
```java
        checkArgument(shrinkFactor > 1);

        int numSections = concurrencyLevel;
        int perSectionExpectedItems = expectedItems / numSections;
        int perSectionCapacity = (int) (perSectionExpectedItems / mapFillFactor);
```

### UnnecessaryLocalVariable
Local variable `uncompressedPayload` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        }
        try {
            ByteBuf uncompressedPayload = codec.decode(payload, uncompressedSize);
            return uncompressedPayload;
        } catch (IOException e) {
```

### UnnecessaryLocalVariable
Local variable `newAverageMsgSize` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/TopicStatsImpl.java`
#### Snippet
```java
        this.msgOutCounter += stats.msgOutCounter;
        this.waitingPublishers += stats.waitingPublishers;
        double newAverageMsgSize = (this.averageMsgSize * (this.count - 1) + stats.averageMsgSize) / this.count;
        this.averageMsgSize = newAverageMsgSize;
        this.storageSize += stats.storageSize;
```

### UnnecessaryLocalVariable
Local variable `newAverageMsgSize` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/PublisherStatsImpl.java`
#### Snippet
```java
        this.msgRateIn += stats.msgRateIn;
        this.msgThroughputIn += stats.msgThroughputIn;
        double newAverageMsgSize = (this.averageMsgSize * (this.count - 1) + stats.averageMsgSize) / this.count;
        this.averageMsgSize = newAverageMsgSize;
        return this;
```

### UnnecessaryLocalVariable
Local variable `capacity` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/ResourceUnitRanking.java`
#### Snippet
```java
        double bandwidthInLimit = systemResourceUsage.bandwidthIn.limit * KBITS_TO_BYTES;

        long capacity = calculateBrokerCapacity(defaultQuota, systemResourceUsage.cpu.limit,
                systemResourceUsage.memory.limit, bandwidthOutLimit, bandwidthInLimit);
        return capacity;
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
                public void readEntriesComplete(List<Entry> entries, Object ctx) {
                    if (!entries.isEmpty()) {
                        long size = entries.get(0).getLength();
                        estimatedEntrySize = size;

```

### UnnecessaryLocalVariable
Local variable `ledgerId` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
        try {
            String[] parts = nodeName.split(ledgerPrefix);
            long ledgerId = Long.parseLong(parts[parts.length - 1]);
            return ledgerId;
        } catch (NumberFormatException e) {
```

### UnnecessaryLocalVariable
Local variable `offset` is redundant
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java
        // Use less than 32 bits to represent entry id since it will get
        // rolled over way before overflowing the max int range
        long offset = (ledgerId << 28) | entryId;
        return offset;
    }
```

### UnnecessaryLocalVariable
Local variable `finalMergedConfig` is redundant
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java
        }
        if (!newConfig.getInputSpecs().isEmpty()) {
            SinkConfig finalMergedConfig = mergedConfig;
            newConfig.getInputSpecs().forEach((topicName, consumerConfig) -> {
                if (!existingConfig.getInputSpecs().containsKey(topicName)) {
```

### UnnecessaryLocalVariable
Local variable `runtimeSpawner` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
                instanceId, workerConfig.getPulsarFunctionsCluster());

        RuntimeSpawner runtimeSpawner = new RuntimeSpawner(instanceConfig, packageFile,
                functionMetaData.getPackageLocation().getOriginalFileName(),
                transformFunctionPackageFile,
```

### UnnecessaryLocalVariable
Local variable `redirect` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/WorkerImpl.java`
#### Snippet
```java
            throw new RestException(Status.INTERNAL_SERVER_ERROR, "Leader cannot be determined");
        }
        URI redirect = UriBuilder.fromUri(uri)
                .host(workerInfo.getWorkerHostname())
                .port(workerInfo.getPort())
```

### UnnecessaryLocalVariable
Local variable `workers` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/WorkerImpl.java`
#### Snippet
```java
        }

        List<WorkerInfo> workers = worker().getMembershipManager().getCurrentMembership();
        return workers;
    }
```

### UnnecessaryLocalVariable
Local variable `functionStatus` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImplV2.java`
#### Snippet
```java


        InstanceCommunication.FunctionStatus functionStatus = InstanceCommunication.FunctionStatus.newBuilder()
                .setRunning(functionInstanceStatus.isRunning())
                .setFailureException(functionInstanceStatus.getError())
```

### UnnecessaryLocalVariable
Local variable `config` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
                    String.format(ComponentTypeUtils.toString(componentType) + " %s doesn't exist", componentName));
        }
        SinkConfig config = SinkConfigUtils.convertFromDetails(functionMetaData.getFunctionDetails());
        return config;
    }
```

### UnnecessaryLocalVariable
Local variable `finalFunctionDetails` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
            // cache auth if need
            if (worker().getWorkerConfig().isAuthenticationEnabled()) {
                Function.FunctionDetails finalFunctionDetails = functionDetails;
                worker().getFunctionRuntimeManager()
                        .getRuntimeFactory()
```

### UnnecessaryLocalVariable
Local variable `finalFunctionDetails` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
            // update auth data if need
            if (worker().getWorkerConfig().isAuthenticationEnabled()) {
                Function.FunctionDetails finalFunctionDetails = functionDetails;
                worker().getFunctionRuntimeManager()
                        .getRuntimeFactory()
```

### UnnecessaryLocalVariable
Local variable `finalFunctionDetails` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImpl.java`
#### Snippet
```java
            // update auth data if need
            if (worker().getWorkerConfig().isAuthenticationEnabled()) {
                Function.FunctionDetails finalFunctionDetails = functionDetails;
                worker().getFunctionRuntimeManager()
                        .getRuntimeFactory()
```

### UnnecessaryLocalVariable
Local variable `finalFunctionDetails` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImpl.java`
#### Snippet
```java
            // cache auth if need
            if (worker().getWorkerConfig().isAuthenticationEnabled()) {
                Function.FunctionDetails finalFunctionDetails = functionDetails;
                worker().getFunctionRuntimeManager()
                        .getRuntimeFactory()
```

### UnnecessaryLocalVariable
Local variable `config` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
                    String.format(ComponentTypeUtils.toString(componentType) + " %s doesn't exist", componentName));
        }
        SourceConfig config = SourceConfigUtils.convertFromDetails(functionMetaData.getFunctionDetails());
        return config;
    }
```

### UnnecessaryLocalVariable
Local variable `finalFunctionDetails` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
            // update auth data if need
            if (worker().getWorkerConfig().isAuthenticationEnabled()) {
                Function.FunctionDetails finalFunctionDetails = functionDetails;
                worker().getFunctionRuntimeManager()
                        .getRuntimeFactory()
```

### UnnecessaryLocalVariable
Local variable `tableName` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java

        String tableNs = getStateNamespace(tenant, namespace);
        String tableName = functionName;

        String stateStorageServiceUrl = worker().getWorkerConfig().getStateStorageServiceUrl();
```

### UnnecessaryLocalVariable
Local variable `tableName` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java

        String tableNs = getStateNamespace(tenant, namespace);
        String tableName = functionName;

        String stateStorageServiceUrl = worker().getWorkerConfig().getStateStorageServiceUrl();
```

### UnnecessaryLocalVariable
Local variable `tableName` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/state/BKStateStoreProviderImpl.java`
#### Snippet
```java
                                  String name) throws Exception {
        final String tableNs = FunctionCommon.getStateNamespace(tenant, namespace);
        final String tableName = name;
        try (StorageAdminClient storageAdminClient = new SimpleStorageAdminClientImpl(
             StorageClientSettings.newBuilder().serviceUri(stateStorageServiceUrl).build(),
```

### UnnecessaryLocalVariable
Local variable `windowConfig` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
            throw new IllegalArgumentException("Window Configs cannot be found");
        }
        WindowConfig windowConfig = new Gson().fromJson(
                (new Gson().toJson(context.getUserConfigValue(WindowConfig.WINDOW_CONFIG_KEY).get())),
                WindowConfig.class);
```

### UnnecessaryLocalVariable
Local variable `pulsarClient` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                        messageBuilder.key(String.valueOf(totalSent));
                    }
                    PulsarClient pulsarClient = client;
                    messageBuilder.sendAsync().thenRun(() -> {
                        bytesSent.add(payloadData.length);
```

### UnnecessaryLocalVariable
Local variable `path` is redundant
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/ClustersImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<List<String>> getClustersAsync() {
        WebTarget path = this.adminClusters;
        return asyncGetRequest(path, new FutureCallback<List<String>>(){});
    }
```

### UnnecessaryLocalVariable
Local variable `json` is redundant
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/ProxyStatsImpl.java`
#### Snippet
```java
    public String getConnections() throws PulsarAdminException {
        try {
            String json = request(adminProxyStats.path("/connections")).get(String.class);
            return json;
        } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `json` is redundant
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/ProxyStatsImpl.java`
#### Snippet
```java
    public String getTopics() throws PulsarAdminException {
        try {
            String json = request(adminProxyStats.path("/topics")).get(String.class);
            return json;
        } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `argv` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
        }

        String[] argv = parsed.toArray(new String[parsed.size()]);
        return argv;
    }
```

### UnnecessaryLocalVariable
Local variable `authenticated` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderList.java`
#### Snippet
```java
    @Override
    public boolean authenticateHttpRequest(HttpServletRequest request, HttpServletResponse response) throws Exception {
        Boolean authenticated = applyAuthProcessor(
            providers,
            provider -> {
```

## RuleId[ruleID=UseCompareMethod]
### UseCompareMethod
Expression can be replaced with 'Long.compare'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ResourceAvailabilityRanker.java`
#### Snippet
```java

    public int compare(ResourceDescription rd1, ResourceDescription rd2) {
        return ((Long) getRank(rd1)).compareTo(getRank(rd2));
    }
}
```

## RuleId[ruleID=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileListingThread.java`
#### Snippet
```java

            try {
                sleep(pollingInterval - 1);
            } catch (InterruptedException e) {
                // Just ignore
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/CanalAbstractSource.java`
#### Snippet
```java
                    if (batchId == -1 || size == 0) {
                        try {
                            Thread.sleep(1000);
                        } catch (InterruptedException e) {
                        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/Application.java`
#### Snippet
```java
                    logger.info("Waiting for channel: " + ch.getName()
                            + " to start. Sleeping for 500 ms");
                    Thread.sleep(500);
                } catch (InterruptedException e) {
                    logger.error("Interrupted while waiting for channel to start.", e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/HdfsSyncThread.java`
#### Snippet
```java
       while (keepRunning) {
         try {
            Thread.sleep(syncInterval);
            ackRecords();
         } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/HdfsSyncThread.java`
#### Snippet
```java
       while (keepRunning) {
         try {
            Thread.sleep(syncInterval);
            ackRecords();
         } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-io/batch-data-generator/src/main/java/org/apache/pulsar/io/batchdatagenerator/BatchDataGeneratorPushSource.java`
#### Snippet
```java
    try {
      for (int i = 0; i < maxRecordsPerCycle; i++) {
        Thread.sleep(50);
        Record<Person> record = () -> new Person(fairy.person());
        consume(record);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
                List<SourceRecord> recordList = sourceTask.poll();
                if (recordList == null || recordList.isEmpty()) {
                    Thread.sleep(1000);
                    continue;
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
                                                                    messageQueueCacheSizeAllocator, messageQueue)
                                                                    || !messageQueue.offer(message)) {
                                                                Thread.sleep(1);
                                                            } else {
                                                                messageQueueCacheSizeAllocator.allocate(
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            } else {
                try {
                    Thread.sleep(1);
                    // stats for time spent wait to read from message queue because its empty
                    metricsTracker.register_MESSAGE_QUEUE_DEQUEUE_WAIT_TIME(1);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
                    if (read <= 0) {
                        try {
                            Thread.sleep(1);
                        } catch (InterruptedException e) {
                            return;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
                        } else {
                            try {
                                Thread.sleep(1);
                            } catch (InterruptedException e) {
                                //no-op
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/MLPendingAckStore.java`
#### Snippet
```java
                    } else {
                        try {
                            Thread.sleep(1);
                        } catch (InterruptedException e) {
                            if (Thread.interrupted()) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java
            }
            try {
                Thread.sleep(250);
            } catch (InterruptedException e) {
                // ignore
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
                log.info("TGT refresh sleeping until: {}", until.toString());
                try {
                    Thread.sleep(nextRefresh - now);
                } catch (InterruptedException ie) {
                    Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
                            // sleep for 10 seconds
                            try {
                                Thread.sleep(10 * 1000);
                            } catch (InterruptedException ie) {
                                Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
                            // sleep for 10 seconds.
                            try {
                                Thread.sleep(10 * 1000);
                            } catch (InterruptedException e) {
                                Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/Actions.java`
#### Snippet
```java
                }

                Thread.sleep(action.sleepBetweenInvocationsMs);
            }
        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
                }
                retries++;
                Thread.sleep(1000L);
            }
        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/WorkerUtils.java`
#### Snippet
```java
                    }
                }
                Thread.sleep(sleepInBetweenMs);
            } while (isLeader.get());
        } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntime.java`
#### Snippet
```java
            // gracefully terminate at first
            while (process.isAlive()) {
                Thread.sleep(100);
                if (i > 100) {
                    break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
                                log.trace("------------- DONE (reached the maximum number: [{}] of production) "
                                        + "--------------", arguments.numMessages);
                                Thread.sleep(10000);
                                PerfClientUtils.exit(0);
                            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java

                        if (producersMap.get(topic).getSocket().getSession() == null) {
                            Thread.sleep(10000);
                            PerfClientUtils.exit(0);
                        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
        while (true) {
            try {
                Thread.sleep(5000);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java

                } catch (Exception e) {
                    Thread.sleep(10000);
                }
            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
                                            + "production) --------------", arguments.testTime);
                                    isDone.set(true);
                                    Thread.sleep(5000);
                                    PerfClientUtils.exit(0);
                                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
                                            + "--------------", numMessagesForThisThread);
                                    isDone.set(true);
                                    Thread.sleep(5000);
                                    PerfClientUtils.exit(0);
                                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
        while (true) {
            try {
                Thread.sleep(10000);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        while (true) {
            try {
                Thread.sleep(10000);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/BrokerMonitor.java`
#### Snippet
```java
            brokerWatcher.updateBrokers(BROKER_ROOT);
            while (true) {
                Thread.sleep(GLOBAL_STATS_PRINT_PERIOD_MILLIS);
                printGlobalData();
            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
        while (true) {
            try {
                Thread.sleep(10000);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
                            Integer.toString(j));
                    trade(arguments, topic, random.nextInt(clients.length));
                    Thread.sleep(arguments.separation);
                }
            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
        while (executing.get()) {
            try {
                Thread.sleep(10000);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionLogImpl.java`
#### Snippet
```java
                } else {
                    try {
                        Thread.sleep(1);
                    } catch (InterruptedException e) {
                        //no-op
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                                    + "--------------", arguments.testTime);
                            doneLatch.countDown();
                            Thread.sleep(5000);
                            PerfClientUtils.exit(0);
                        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                                    , numMessages);
                            doneLatch.countDown();
                            Thread.sleep(5000);
                            PerfClientUtils.exit(0);
                        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        while (true) {
            try {
                Thread.sleep(10000);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                LongRunningProcessStatus status = getPersistentTopics().compactionStatus(persistentTopic);
                while (wait && status.status == LongRunningProcessStatus.Status.RUNNING) {
                    Thread.sleep(1000);
                    status = getPersistentTopics().compactionStatus(persistentTopic);
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                }
                if (watch) {
                    Thread.sleep(1000);
                }
            } while (watch);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                LongRunningProcessStatus status = getTopics().compactionStatus(persistentTopic);
                while (wait && status.status == LongRunningProcessStatus.Status.RUNNING) {
                    Thread.sleep(1000);
                    status = getTopics().compactionStatus(persistentTopic);
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                OffloadProcessStatus status = getTopics().offloadStatus(persistentTopic);
                while (wait && status.getStatus() == LongRunningProcessStatus.Status.RUNNING) {
                    Thread.sleep(1000);
                    status = getTopics().offloadStatus(persistentTopic);
                }
```

## RuleId[ruleID=RedundantFileCreation]
### RedundantFileCreation
`new File` is redundant
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/FunctionsImpl.java`
#### Snippet
```java
                file.createNewFile();
            }
            FileChannel os = new FileOutputStream(new File(destinationPath)).getChannel();

            RequestBuilder builder = get(target.getUri().toASCIIString());
```

## RuleId[ruleID=WaitNotInLoop]
### WaitNotInLoop
Call to `wait()` is not in loop
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    public void join() throws Exception {
        // K8 functions never return
        this.wait();
    }

```

## RuleId[ruleID=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java

        String kafkaConnectorFQClassName = kafkaSinkConfig.getKafkaConnectorSinkClass();
        kafkaSinkConfig.getKafkaConnectorConfigProperties().forEach(props::put);

        Class<?> clazz = Class.forName(kafkaConnectorFQClassName);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarStats.java`
#### Snippet
```java
            });
            if (clusterReplicationMetrics.isMetricsEnabled()) {
                clusterReplicationMetrics.get().forEach(clusterMetric -> tempMetricsCollection.add(clusterMetric));
                clusterReplicationMetrics.reset();
            }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarStats.java`
#### Snippet
```java
            }
            brokerOperabilityMetrics.getMetrics()
                    .forEach(brokerOperabilityMetric -> tempMetricsCollection.add(brokerOperabilityMetric));

            // json end
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                AuthPolicies auth = policies.get().auth_policies;
                // First add namespace level permissions
                auth.getNamespaceAuthentication().forEach(permissions::put);

                // Then add topic level permissions
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupConfigListener.java`
#### Snippet
```java

            for (String rgName : rgList) {
                newSet.add(rgName);
            }

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowableArrayBlockingQueue.java`
#### Snippet
```java
    public List<T> toList() {
        List<T> list = new ArrayList<>(size());
        forEach(list::add);
        return list;
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionMetaDataManager.java`
#### Snippet
```java
        }
        for (FunctionMetaData functionMetaData : this.functionMetaDataMap.get(tenant).get(namespace).values()) {
            ret.add(functionMetaData);
        }
        return ret;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
            if (!isolationGroupsString.isEmpty()) {
                for (String isolationGroup : isolationGroupsString.split(",")) {
                    primaryIsolationGroups.add(isolationGroup);
                }
            }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
            if (!secondaryIsolationGroupsString.isEmpty()) {
                for (String isolationGroup : secondaryIsolationGroupsString.split(",")) {
                    secondaryIsolationGroups.add(isolationGroup);
                }
            }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
                            Map<Integer, MessageId> messageIdImpl = new HashMap<>();
                            for (Map.Entry<Integer, MessageIdImpl> entry: messageId.entrySet()) {
                                messageIdImpl.put(entry.getKey(), entry.getValue());
                            }
                            future.complete(messageIdImpl);
```

## RuleId[ruleID=UnstableApiUsage]
### UnstableApiUsage
'com.google.common.net.HostAndPort' is marked unstable with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
    }

    public List<HostAndPort> getHostAndPorts() {
        List<HostAndPort> hostAndPorts = Lists.newArrayList();
        Preconditions.checkNotNull(redisHosts, "redisHosts property not set.");
```

### UnstableApiUsage
'com.google.common.net.HostAndPort' is marked unstable with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java

    public List<HostAndPort> getHostAndPorts() {
        List<HostAndPort> hostAndPorts = Lists.newArrayList();
        Preconditions.checkNotNull(redisHosts, "redisHosts property not set.");
        String[] hosts = StringUtils.split(redisHosts, ",");
```

### UnstableApiUsage
'com.google.common.net.HostAndPort' is marked unstable with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
        String[] hosts = StringUtils.split(redisHosts, ",");
        for (String host : hosts) {
            HostAndPort hostAndPort = HostAndPort.fromString(host);
            hostAndPorts.add(hostAndPort);
        }
```

### UnstableApiUsage
'com.google.common.net.HostAndPort' is marked unstable with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
        String[] hosts = StringUtils.split(redisHosts, ",");
        for (String host : hosts) {
            HostAndPort hostAndPort = HostAndPort.fromString(host);
            hostAndPorts.add(hostAndPort);
        }
```

### UnstableApiUsage
'fromString(java.lang.String)' is declared in unstable class 'com.google.common.net.HostAndPort' marked with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
        String[] hosts = StringUtils.split(redisHosts, ",");
        for (String host : hosts) {
            HostAndPort hostAndPort = HostAndPort.fromString(host);
            hostAndPorts.add(hostAndPort);
        }
```

### UnstableApiUsage
'com.google.common.net.HostAndPort' is marked unstable with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisSession.java`
#### Snippet
```java
    }

    private static List<RedisURI> redisURIs(List<HostAndPort> hostAndPorts, RedisSinkConfig config) {
        List<RedisURI> redisURIs = Lists.newArrayList();
        for (HostAndPort hostAndPort : hostAndPorts) {
```

### UnstableApiUsage
'com.google.common.net.HostAndPort' is marked unstable with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisSession.java`
#### Snippet
```java
    private static List<RedisURI> redisURIs(List<HostAndPort> hostAndPorts, RedisSinkConfig config) {
        List<RedisURI> redisURIs = Lists.newArrayList();
        for (HostAndPort hostAndPort : hostAndPorts) {
            RedisURI.Builder builder = RedisURI.builder();
            builder.withHost(hostAndPort.getHost());
```

### UnstableApiUsage
'getHost()' is declared in unstable class 'com.google.common.net.HostAndPort' marked with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisSession.java`
#### Snippet
```java
        for (HostAndPort hostAndPort : hostAndPorts) {
            RedisURI.Builder builder = RedisURI.builder();
            builder.withHost(hostAndPort.getHost());
            builder.withPort(hostAndPort.getPort());
            builder.withDatabase(config.getRedisDatabase());
```

### UnstableApiUsage
'getPort()' is declared in unstable class 'com.google.common.net.HostAndPort' marked with @Beta
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisSession.java`
#### Snippet
```java
            RedisURI.Builder builder = RedisURI.builder();
            builder.withHost(hostAndPort.getHost());
            builder.withPort(hostAndPort.getPort());
            builder.withDatabase(config.getRedisDatabase());
            if (!StringUtils.isBlank(config.getRedisPassword())) {
```

### UnstableApiUsage
'com.google.common.hash.Hasher' is marked unstable with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                    && idHashingAlgorithm != null
                    && idHashingAlgorithm != ElasticSearchConfig.IdHashingAlgorithm.NONE) {
                Hasher hasher;
                switch (idHashingAlgorithm) {
                    case SHA256:
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                switch (idHashingAlgorithm) {
                    case SHA256:
                        hasher = Hashing.sha256().newHasher();
                        break;
                    case SHA512:
```

### UnstableApiUsage
'sha256()' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                switch (idHashingAlgorithm) {
                    case SHA256:
                        hasher = Hashing.sha256().newHasher();
                        break;
                    case SHA512:
```

### UnstableApiUsage
'newHasher()' is unstable because its signature references unstable interface 'com.google.common.hash.Hasher' marked with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                switch (idHashingAlgorithm) {
                    case SHA256:
                        hasher = Hashing.sha256().newHasher();
                        break;
                    case SHA512:
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                        break;
                    case SHA512:
                        hasher = Hashing.sha512().newHasher();
                        break;
                    default:
```

### UnstableApiUsage
'sha512()' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                        break;
                    case SHA512:
                        hasher = Hashing.sha512().newHasher();
                        break;
                    default:
```

### UnstableApiUsage
'newHasher()' is unstable because its signature references unstable interface 'com.google.common.hash.Hasher' marked with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                        break;
                    case SHA512:
                        hasher = Hashing.sha512().newHasher();
                        break;
                    default:
```

### UnstableApiUsage
'putString(java.lang.CharSequence, java.nio.charset.Charset)' is declared in unstable interface 'com.google.common.hash.Hasher' marked with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                                + idHashingAlgorithm);
                }
                hasher.putString(id, StandardCharsets.UTF_8);
                id = base64Encoder.encodeToString(hasher.hash().asBytes());
            }
```

### UnstableApiUsage
'hash()' is declared in unstable interface 'com.google.common.hash.Hasher' marked with @Beta
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                }
                hasher.putString(id, StandardCharsets.UTF_8);
                id = base64Encoder.encodeToString(hasher.hash().asBytes());
            }

```

### UnstableApiUsage
'BookKeeper(org.apache.bookkeeper.conf.ClientConfiguration, org.apache.zookeeper.ZooKeeper, io.netty.channel.EventLoopGroup, io.netty.buffer.ByteBufAllocator, org.apache.bookkeeper.stats.StatsLogger, org.apache.bookkeeper.net.DNSToSwitchMapping, io.netty.util.HashedWheelTimer, ...)' is unstable because its signature references unstable interface 'org.apache.bookkeeper.net.DNSToSwitchMapping' marked with @Beta
in `testmocks/src/main/java/org/apache/bookkeeper/client/BookKeeperTestClient.java`
#### Snippet
```java
    public BookKeeperTestClient(ClientConfiguration conf, TestStatsProvider statsProvider)
            throws IOException, InterruptedException, BKException {
        super(conf, null, null, new UnpooledByteBufAllocator(false),
                statsProvider == null ? NullStatsLogger.INSTANCE : statsProvider.getStatsLogger(""),
                null, null, null);
```

### UnstableApiUsage
'BookKeeper(org.apache.bookkeeper.conf.ClientConfiguration, org.apache.zookeeper.ZooKeeper, io.netty.channel.EventLoopGroup, io.netty.buffer.ByteBufAllocator, org.apache.bookkeeper.stats.StatsLogger, org.apache.bookkeeper.net.DNSToSwitchMapping, io.netty.util.HashedWheelTimer, ...)' is unstable because its signature references unstable interface 'org.apache.bookkeeper.net.DNSToSwitchMapping' marked with @Beta
in `testmocks/src/main/java/org/apache/bookkeeper/client/BookKeeperTestClient.java`
#### Snippet
```java
    public BookKeeperTestClient(ClientConfiguration conf, ZooKeeper zkc)
            throws IOException, InterruptedException, BKException {
        super(conf, zkc, null, new UnpooledByteBufAllocator(false),
                NullStatsLogger.INSTANCE, null, null, null);
        this.statsProvider = statsProvider;
```

### UnstableApiUsage
'com.google.common.collect.TreeRangeMap' is marked unstable with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java
        this.maxNumBuckets = maxNumBuckets;
        this.sharedBucketPriorityQueue = new TripleLongPriorityQueue();
        this.immutableBuckets = TreeRangeMap.create();
        this.snapshotSegmentLastIndexTable = HashBasedTable.create();
        this.numberDelayedMessages = 0L;
```

### UnstableApiUsage
'create()' is declared in unstable class 'com.google.common.collect.TreeRangeMap' marked with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java
        this.maxNumBuckets = maxNumBuckets;
        this.sharedBucketPriorityQueue = new TripleLongPriorityQueue();
        this.immutableBuckets = TreeRangeMap.create();
        this.snapshotSegmentLastIndexTable = HashBasedTable.create();
        this.numberDelayedMessages = 0L;
```

### UnstableApiUsage
'put(com.google.common.collect.Range, V)' is declared in unstable interface 'com.google.common.collect.RangeMap' marked with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java
        if (immutableBucketDelayedIndexPair != null) {
            ImmutableBucket immutableBucket = immutableBucketDelayedIndexPair.getLeft();
            immutableBuckets.put(Range.closed(immutableBucket.startLedgerId, immutableBucket.endLedgerId),
                    immutableBucket);

```

### UnstableApiUsage
'asMapOfRanges()' is declared in unstable interface 'com.google.common.collect.RangeMap' marked with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java

    private Optional<ImmutableBucket> findImmutableBucket(long ledgerId) {
        if (immutableBuckets.asMapOfRanges().isEmpty()) {
            return Optional.empty();
        }
```

### UnstableApiUsage
'get(K)' is declared in unstable interface 'com.google.common.collect.RangeMap' marked with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java
        }

        return Optional.ofNullable(immutableBuckets.get(ledgerId));
    }

```

### UnstableApiUsage
'asMapOfRanges()' is declared in unstable interface 'com.google.common.collect.RangeMap' marked with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java
            lastMutableBucket.resetLastMutableBucketRange();

            if (immutableBuckets.asMapOfRanges().size() > maxNumBuckets) {
                // TODO merge bucket snapshot (synchronize operate)
            }
```

### UnstableApiUsage
'asMapOfRanges()' is declared in unstable interface 'com.google.common.collect.RangeMap' marked with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java
    private void cleanImmutableBuckets(boolean delete) {
        if (immutableBuckets != null) {
            Iterator<ImmutableBucket> iterator = immutableBuckets.asMapOfRanges().values().iterator();
            while (iterator.hasNext()) {
                ImmutableBucket bucket = iterator.next();
```

### UnstableApiUsage
'com.google.common.collect.RangeMap' is marked unstable with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketDelayedDeliveryTracker.java`
#### Snippet
```java
    private final TripleLongPriorityQueue sharedBucketPriorityQueue;

    private final RangeMap<Long, ImmutableBucket> immutableBuckets;

    private final Table<Long, Long, ImmutableBucket> snapshotSegmentLastIndexTable;
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
@Slf4j
public class SchemaRegistryServiceImpl implements SchemaRegistryService {
    private static HashFunction hashFunction = Hashing.sha256();
    private final Map<SchemaType, SchemaCompatibilityCheck> compatibilityChecks;
    private final SchemaStorage schemaStorage;
```

### UnstableApiUsage
'sha256()' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
@Slf4j
public class SchemaRegistryServiceImpl implements SchemaRegistryService {
    private static HashFunction hashFunction = Hashing.sha256();
    private final Map<SchemaType, SchemaCompatibilityCheck> compatibilityChecks;
    private final SchemaStorage schemaStorage;
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
        this.config = pulsar.getConfiguration();
        this.loadManager = pulsar.getLoadManager();
        this.bundleFactory = new NamespaceBundleFactory(pulsar, Hashing.crc32());
        this.ownershipCache = new OwnershipCache(pulsar, bundleFactory, this);
        this.namespaceClients =
```

### UnstableApiUsage
'crc32()' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
        this.config = pulsar.getConfiguration();
        this.loadManager = pulsar.getLoadManager();
        this.bundleFactory = new NamespaceBundleFactory(pulsar, Hashing.crc32());
        this.ownershipCache = new OwnershipCache(pulsar, bundleFactory, this);
        this.namespaceClients =
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/RateLimiter.java`
#### Snippet
```java
 * acquire 10 permits at any time with in that 1 second.
 *
 * <p>Comparison with other RateLimiter such as {@link com.google.common.util.concurrent.RateLimiter}
 * <ul>
 * <li><b>Per second rate-limiting:</b> Per second rate-limiting not satisfied by Guava-RateLimiter</li>
```

### UnstableApiUsage
'remove(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java

        public void remove(Range<T> range) {
            set.remove(range);
        }

```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public Range<T> firstRange() {
            Iterator<Range<T>> iterable = set.asRanges().iterator();
            if (iterable.hasNext()) {
                return iterable.next();
```

### UnstableApiUsage
'remove(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public void removeAtMost(long key, long value) {
            set.remove(Range.atMost(consumer.apply(key, value)));
        }

```

### UnstableApiUsage
'contains(C)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java

        public boolean contains(T position) {
            return set.contains(position);
        }

```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public int size() {
            return set.asRanges().size();
        }

```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
    class DefaultRangeSet<T extends Comparable<T>> implements LongPairRangeSet<T> {

        RangeSet<T> set = TreeRangeSet.create();

        private final LongPairConsumer<T> consumer;
```

### UnstableApiUsage
'com.google.common.collect.TreeRangeSet' is marked unstable with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
    class DefaultRangeSet<T extends Comparable<T>> implements LongPairRangeSet<T> {

        RangeSet<T> set = TreeRangeSet.create();

        private final LongPairConsumer<T> consumer;
```

### UnstableApiUsage
'create()' is declared in unstable class 'com.google.common.collect.TreeRangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
    class DefaultRangeSet<T extends Comparable<T>> implements LongPairRangeSet<T> {

        RangeSet<T> set = TreeRangeSet.create();

        private final LongPairConsumer<T> consumer;
```

### UnstableApiUsage
'toString()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public String toString() {
            return set.toString();
        }
    }
```

### UnstableApiUsage
'isEmpty()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public boolean isEmpty() {
            return set.isEmpty();
        }

```

### UnstableApiUsage
'add(com.google.common.collect.Range)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public void addOpenClosed(long key1, long value1, long key2, long value2) {
            set.add(Range.openClosed(consumer.apply(key1, value1), consumer.apply(key2, value2)));
        }

```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public Range<T> lastRange() {
            if (set.asRanges().isEmpty()) {
                return null;
            }
```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
                return null;
            }
            List<Range<T>> list = Lists.newArrayList(set.asRanges().iterator());
            return list.get(list.size() - 1);
        }
```

### UnstableApiUsage
'span()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        public Range<T> span() {
            try {
                return set.span();
            } catch (NoSuchElementException e) {
                return null;
```

### UnstableApiUsage
'rangeContaining(C)' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java

        public Range<T> rangeContaining(T position) {
            return set.rangeContaining(position);
        }

```

### UnstableApiUsage
'clear()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public void clear() {
            set.clear();
        }

```

### UnstableApiUsage
'asRanges()' is declared in unstable interface 'com.google.common.collect.RangeSet' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        @Override
        public Set<Range<T>> asRanges() {
            return set.asRanges();
        }

```

### UnstableApiUsage
'com.google.common.collect.RangeSet' is marked unstable with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
/**
 * A Concurrent set comprising zero or more ranges of type {@link LongPair}. This can be alternative of
 * {@link com.google.common.collect.RangeSet} and can be used if {@code range} type is {@link LongPair}
 *
 * <pre>
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/topics/TopicList.java`
#### Snippet
```java

    public static String calculateHash(List<String> topics) {
        return Hashing.crc32c().hashBytes(topics.stream()
                .sorted()
                .collect(Collectors.joining(","))
```

### UnstableApiUsage
'crc32c()' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/topics/TopicList.java`
#### Snippet
```java

    public static String calculateHash(List<String> topics) {
        return Hashing.crc32c().hashBytes(topics.stream()
                .sorted()
                .collect(Collectors.joining(","))
```

### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/schema/SchemaHash.java`
#### Snippet
```java
public class SchemaHash {

    private static final HashFunction hashFunction = Hashing.sha256();
    private static final SchemaHash EMPTY_SCHEMA_HASH = new SchemaHash(hashFunction.hashBytes(new byte[0]), null);

```

### UnstableApiUsage
'sha256()' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/schema/SchemaHash.java`
#### Snippet
```java
public class SchemaHash {

    private static final HashFunction hashFunction = Hashing.sha256();
    private static final SchemaHash EMPTY_SCHEMA_HASH = new SchemaHash(hashFunction.hashBytes(new byte[0]), null);

```

### UnstableApiUsage
'com.google.common.io.CountingInputStream' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/StreamingDataBlockHeaderImpl.java`
#### Snippet
```java
    // Construct DataBlockHeader from InputStream, which contains `HEADER_MAX_SIZE` bytes readable.
    public static StreamingDataBlockHeaderImpl fromStream(InputStream stream) throws IOException {
        CountingInputStream countingStream = new CountingInputStream(stream);
        DataInputStream dis = new DataInputStream(countingStream);
        int magic = dis.readInt();
```

### UnstableApiUsage
'CountingInputStream(java.io.InputStream)' is declared in unstable class 'com.google.common.io.CountingInputStream' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/StreamingDataBlockHeaderImpl.java`
#### Snippet
```java
    // Construct DataBlockHeader from InputStream, which contains `HEADER_MAX_SIZE` bytes readable.
    public static StreamingDataBlockHeaderImpl fromStream(InputStream stream) throws IOException {
        CountingInputStream countingStream = new CountingInputStream(stream);
        DataInputStream dis = new DataInputStream(countingStream);
        int magic = dis.readInt();
```

### UnstableApiUsage
'com.google.common.io.CountingInputStream' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/StreamingDataBlockHeaderImpl.java`
#### Snippet
```java
    // Construct DataBlockHeader from InputStream, which contains `HEADER_MAX_SIZE` bytes readable.
    public static StreamingDataBlockHeaderImpl fromStream(InputStream stream) throws IOException {
        CountingInputStream countingStream = new CountingInputStream(stream);
        DataInputStream dis = new DataInputStream(countingStream);
        int magic = dis.readInt();
```

### UnstableApiUsage
'getCount()' is declared in unstable class 'com.google.common.io.CountingInputStream' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/StreamingDataBlockHeaderImpl.java`
#### Snippet
```java
        long firstEntryId = dis.readLong();
        long ledgerId = dis.readLong();
        long toSkip = headerLen - countingStream.getCount();
        if (dis.skip(toSkip) != toSkip) {
            throw new EOFException("Header was too small");
```

### UnstableApiUsage
'com.google.common.io.CountingInputStream' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockHeaderImpl.java`
#### Snippet
```java
    // Construct DataBlockHeader from InputStream, which contains `HEADER_MAX_SIZE` bytes readable.
    public static DataBlockHeader fromStream(InputStream stream) throws IOException {
        CountingInputStream countingStream = new CountingInputStream(stream);
        DataInputStream dis = new DataInputStream(countingStream);
        int magic = dis.readInt();
```

### UnstableApiUsage
'CountingInputStream(java.io.InputStream)' is declared in unstable class 'com.google.common.io.CountingInputStream' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockHeaderImpl.java`
#### Snippet
```java
    // Construct DataBlockHeader from InputStream, which contains `HEADER_MAX_SIZE` bytes readable.
    public static DataBlockHeader fromStream(InputStream stream) throws IOException {
        CountingInputStream countingStream = new CountingInputStream(stream);
        DataInputStream dis = new DataInputStream(countingStream);
        int magic = dis.readInt();
```

### UnstableApiUsage
'com.google.common.io.CountingInputStream' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockHeaderImpl.java`
#### Snippet
```java
    // Construct DataBlockHeader from InputStream, which contains `HEADER_MAX_SIZE` bytes readable.
    public static DataBlockHeader fromStream(InputStream stream) throws IOException {
        CountingInputStream countingStream = new CountingInputStream(stream);
        DataInputStream dis = new DataInputStream(countingStream);
        int magic = dis.readInt();
```

### UnstableApiUsage
'getCount()' is declared in unstable class 'com.google.common.io.CountingInputStream' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockHeaderImpl.java`
#### Snippet
```java
        long blockLen = dis.readLong();
        long firstEntryId = dis.readLong();
        long toSkip = headerLen - countingStream.getCount();
        if (dis.skip(toSkip) != toSkip) {
            throw new EOFException("Header was too small");
```

### UnstableApiUsage
'AnonymousProviderMetadata(org.jclouds.apis.ApiMetadata, java.lang.String)' is unstable because its signature references unstable interface 'org.jclouds.apis.ApiMetadata' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
    },

    TRANSIENT("transient", new AnonymousProviderMetadata(new TransientApiMetadata(), "")) {
        @Override
        public void validate(TieredStorageConfiguration config) throws IllegalArgumentException {
```

### UnstableApiUsage
'AnonymousProviderMetadata(org.jclouds.apis.ApiMetadata, java.lang.String)' is unstable because its signature references unstable interface 'org.jclouds.apis.ApiMetadata' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
    },

    S3("S3", new AnonymousProviderMetadata(new S3ApiMetadata(), "")) {
        @Override
        public BlobStore getBlobStore(TieredStorageConfiguration config) {
```

### UnstableApiUsage
'AnonymousProviderMetadata(org.jclouds.apis.ApiMetadata, java.lang.String)' is unstable because its signature references unstable interface 'org.jclouds.apis.ApiMetadata' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
     * https://www.alibabacloud.com/help/doc-detail/64919.htm
     */
    ALIYUN_OSS("aliyun-oss", new AnonymousProviderMetadata(new S3ApiMetadata(), "")) {
        @Override
        public void validate(TieredStorageConfiguration config) throws IllegalArgumentException {
```

### UnstableApiUsage
'buildView(java.lang.Class)' is unstable because its signature references unstable interface 'org.jclouds.View' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
            ShadedJCloudsUtils.addStandardModules(contextBuilder);
            BlobStoreContext ctx = contextBuilder
                    .buildView(BlobStoreContext.class);

            BlobStore bs = ctx.getBlobStore();
```

### UnstableApiUsage
'buildView(java.lang.Class)' is unstable because its signature references unstable interface 'org.jclouds.View' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
            return contextBuilder
                    .credentialsSupplier(config.getCredentials()::get)
                    .buildView(BlobStoreContext.class)
                    .getBlobStore();
        } else {
```

### UnstableApiUsage
'buildView(java.lang.Class)' is unstable because its signature references unstable interface 'org.jclouds.View' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
            log.warn("The credentials is null. driver: {}, bucket: {}", config.getDriver(), config.getBucket());
            return contextBuilder
                    .buildView(BlobStoreContext.class)
                    .getBlobStore();
        }
```

### UnstableApiUsage
'buildView(java.lang.Class)' is unstable because its signature references unstable interface 'org.jclouds.View' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
                return contextBuilder
                        .credentialsSupplier(config.getCredentials()::get)
                        .buildView(BlobStoreContext.class)
                        .getBlobStore();
        } else {
```

### UnstableApiUsage
'buildView(java.lang.Class)' is unstable because its signature references unstable interface 'org.jclouds.View' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
            log.warn("The credentials is null. driver: {}, bucket: {}", config.getDriver(), config.getBucket());
            return contextBuilder
                    .buildView(BlobStoreContext.class)
                    .getBlobStore();
        }
```

### UnstableApiUsage
'buildView(java.lang.Class)' is unstable because its signature references unstable interface 'org.jclouds.View' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
                return contextBuilder
                        .credentials(credentials.identity, credentials.credential)
                        .buildView(BlobStoreContext.class)
                        .getBlobStore();
            } else {
```

### UnstableApiUsage
'buildView(java.lang.Class)' is unstable because its signature references unstable interface 'org.jclouds.View' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
                log.warn("The credentials is null. driver: {}, bucket: {}", config.getDriver(), config.getBucket());
                return contextBuilder
                        .buildView(BlobStoreContext.class)
                        .getBlobStore();
            }
```

### UnstableApiUsage
'org.jclouds.osgi.ApiRegistry' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java

        if (getDriver().equalsIgnoreCase(AWS_S3.getDriver())) {
            ApiRegistry.registerApi(new S3ApiMetadata());
            ProviderRegistry.registerProvider(new AWSS3ProviderMetadata());
        } else if (getDriver().equalsIgnoreCase(GOOGLE_CLOUD_STORAGE.getDriver())) {
```

### UnstableApiUsage
'registerApi(org.jclouds.apis.ApiMetadata)' is declared in unstable class 'org.jclouds.osgi.ApiRegistry' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java

        if (getDriver().equalsIgnoreCase(AWS_S3.getDriver())) {
            ApiRegistry.registerApi(new S3ApiMetadata());
            ProviderRegistry.registerProvider(new AWSS3ProviderMetadata());
        } else if (getDriver().equalsIgnoreCase(GOOGLE_CLOUD_STORAGE.getDriver())) {
```

### UnstableApiUsage
'org.jclouds.osgi.ProviderRegistry' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
        if (getDriver().equalsIgnoreCase(AWS_S3.getDriver())) {
            ApiRegistry.registerApi(new S3ApiMetadata());
            ProviderRegistry.registerProvider(new AWSS3ProviderMetadata());
        } else if (getDriver().equalsIgnoreCase(GOOGLE_CLOUD_STORAGE.getDriver())) {
            ProviderRegistry.registerProvider(new GoogleCloudStorageProviderMetadata());
```

### UnstableApiUsage
'registerProvider(org.jclouds.providers.ProviderMetadata)' is declared in unstable class 'org.jclouds.osgi.ProviderRegistry' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
        if (getDriver().equalsIgnoreCase(AWS_S3.getDriver())) {
            ApiRegistry.registerApi(new S3ApiMetadata());
            ProviderRegistry.registerProvider(new AWSS3ProviderMetadata());
        } else if (getDriver().equalsIgnoreCase(GOOGLE_CLOUD_STORAGE.getDriver())) {
            ProviderRegistry.registerProvider(new GoogleCloudStorageProviderMetadata());
```

### UnstableApiUsage
'org.jclouds.osgi.ProviderRegistry' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
            ProviderRegistry.registerProvider(new AWSS3ProviderMetadata());
        } else if (getDriver().equalsIgnoreCase(GOOGLE_CLOUD_STORAGE.getDriver())) {
            ProviderRegistry.registerProvider(new GoogleCloudStorageProviderMetadata());
        }

```

### UnstableApiUsage
'registerProvider(org.jclouds.providers.ProviderMetadata)' is declared in unstable class 'org.jclouds.osgi.ProviderRegistry' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
            ProviderRegistry.registerProvider(new AWSS3ProviderMetadata());
        } else if (getDriver().equalsIgnoreCase(GOOGLE_CLOUD_STORAGE.getDriver())) {
            ProviderRegistry.registerProvider(new GoogleCloudStorageProviderMetadata());
        }

```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java

        final ZooWorker worker;
        final RateLimiter rateLimiter;
        final Runnable that;

```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        public void run() {
            if (null != rateLimiter) {
                rateLimiter.acquire();
            }
            zkRun();
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        this.connectRetryPolicy = connectRetryPolicy;
        this.operationRetryPolicy = operationRetryPolicy;
        this.rateLimiter = rate > 0 ? RateLimiter.create(rate) : null;
        this.retryExecutor =
                Executors.newScheduledThreadPool(retryExecThreadCount,
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        this.connectRetryPolicy = connectRetryPolicy;
        this.operationRetryPolicy = operationRetryPolicy;
        this.rateLimiter = rate > 0 ? RateLimiter.create(rate) : null;
        this.retryExecutor =
                Executors.newScheduledThreadPool(retryExecThreadCount,
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
                                               ZooWorker.ZooCallable<T> proc,
                                               RetryPolicy retryPolicy,
                                               RateLimiter rateLimiter,
                                               OpStatsLogger statsLogger)
                throws KeeperException, InterruptedException {
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
                    log.debug("Execute {} at {} retry attempt.", proc, attempts);
                    if (null != rateLimiter) {
                        rateLimiter.acquire();
                    }
                    result = proc.call();
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java

        ZkRetryRunnable(RetryPolicy retryPolicy,
                        RateLimiter rateLimiter,
                        OpStatsLogger statsLogger) {
            this.worker = new ZooWorker(retryPolicy, statsLogger);
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java

    // rate limiter
    private final RateLimiter rateLimiter;

    // retry polices
```

### UnstableApiUsage
'initiateMultipartUpload(java.lang.String, org.jclouds.blobstore.domain.BlobMetadata, org.jclouds.blobstore.options.PutOptions)' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
        Blob blob = blobBuilder.build();
        streamingMpu = blobStore
                .initiateMultipartUpload(config.getBucket(), blob.getMetadata(), new PutOptions());

        scheduler.chooseThread(segmentInfo).execute(() -> {
```

### UnstableApiUsage
'schedule(java.lang.Runnable, long, java.util.concurrent.TimeUnit)' is unstable because its signature references unstable interface 'com.google.common.util.concurrent.ListenableScheduledFuture' marked with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
            log.debug("not enough data, delay schedule for part: {} length: {}", partId, dataObjectLength);
            scheduler.chooseThread(segmentInfo)
                    .schedule(() -> {
                        streamingOffloadLoop(partId, dataObjectLength);
                    }, 100, TimeUnit.MILLISECONDS);
```

### UnstableApiUsage
'completeMultipartUpload(org.jclouds.blobstore.domain.MultipartUpload, java.util.List)' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
    private void buildIndexAndCompleteResult(long dataObjectLength) {
        try {
            blobStore.completeMultipartUpload(streamingMpu, streamingParts);
            streamingIndexBuilder.withDataObjectLength(dataObjectLength);
            final OffloadIndexBlockV2 index = streamingIndexBuilder.buildV2();
```

### UnstableApiUsage
'initiateMultipartUpload(java.lang.String, org.jclouds.blobstore.domain.BlobMetadata, org.jclouds.blobstore.options.PutOptions)' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                Blob blob = blobBuilder.build();
                log.info("initiateMultipartUpload bucket {}, metadata {} ", config.getBucket(), blob.getMetadata());
                mpu = writeBlobStore.initiateMultipartUpload(config.getBucket(), blob.getMetadata(), new PutOptions());
            } catch (Throwable t) {
                promise.completeExceptionally(t);
```

### UnstableApiUsage
'uploadMultipartPart(org.jclouds.blobstore.domain.MultipartUpload, int, org.jclouds.io.Payload)' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                        partPayload.getContentMetadata().setContentLength((long) blockSize);
                        partPayload.getContentMetadata().setContentType("application/octet-stream");
                        parts.add(writeBlobStore.uploadMultipartPart(mpu, partId, partPayload));
                        log.debug("UploadMultipartPart. container: {}, blobName: {}, partId: {}, mpu: {}",
                                config.getBucket(), dataBlockKey, partId, mpu.id());
```

### UnstableApiUsage
'completeMultipartUpload(org.jclouds.blobstore.domain.MultipartUpload, java.util.List)' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                }

                String etag = writeBlobStore.completeMultipartUpload(mpu, parts);
                log.info("Ledger {}, upload finished, etag {}", readHandle.getId(), etag);
                mpu = null;
```

### UnstableApiUsage
'abortMultipartUpload(org.jclouds.blobstore.domain.MultipartUpload)' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                try {
                    if (mpu != null) {
                        writeBlobStore.abortMultipartUpload(mpu);
                    }
                } catch (Throwable throwable) {
```

### UnstableApiUsage
'uploadMultipartPart(org.jclouds.blobstore.domain.MultipartUpload, int, org.jclouds.io.Payload)' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
            Payload partPayload = Payloads.newInputStreamPayload(payloadStream);
            partPayload.getContentMetadata().setContentType("application/octet-stream");
            streamingParts.add(blobStore.uploadMultipartPart(streamingMpu, partId, partPayload));
            streamingIndexBuilder.withDataBlockHeaderLength(StreamingDataBlockHeaderImpl.getDataStartOffset());
            streamingIndexBuilder.addBlock(blockLedgerId, beginEntryId, partId, blockSize);
```

### UnstableApiUsage
'abortMultipartUpload(org.jclouds.blobstore.domain.MultipartUpload)' is marked unstable with @Beta
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                    config.getBucket(), streamingDataBlockKey, partId, streamingMpu.id());
        } catch (Throwable e) {
            blobStore.abortMultipartUpload(streamingMpu);
            offloadResult.completeExceptionally(e);
            return;
```

### UnstableApiUsage
'com.google.common.io.MoreFiles' is marked unstable with @Beta
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
        if (pkgDir.exists()) {
            try {
                MoreFiles.deleteRecursively(
                        Paths.get(pkgDir.toURI()), RecursiveDeleteOption.ALLOW_INSECURE);
            } catch (IOException e) {
```

### UnstableApiUsage
'deleteRecursively(java.nio.file.Path, com.google.common.io.RecursiveDeleteOption...)' is declared in unstable class 'com.google.common.io.MoreFiles' marked with @Beta
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
        if (pkgDir.exists()) {
            try {
                MoreFiles.deleteRecursively(
                        Paths.get(pkgDir.toURI()), RecursiveDeleteOption.ALLOW_INSECURE);
            } catch (IOException e) {
```

### UnstableApiUsage
'com.google.common.io.RecursiveDeleteOption' is marked unstable with @Beta
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
            try {
                MoreFiles.deleteRecursively(
                        Paths.get(pkgDir.toURI()), RecursiveDeleteOption.ALLOW_INSECURE);
            } catch (IOException e) {
                log.warn("Failed to delete package for function: {}",
```

### UnstableApiUsage
'ALLOW_INSECURE' is declared in unstable enum 'com.google.common.io.RecursiveDeleteOption' marked with @Beta
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
            try {
                MoreFiles.deleteRecursively(
                        Paths.get(pkgDir.toURI()), RecursiveDeleteOption.ALLOW_INSECURE);
            } catch (IOException e) {
                log.warn("Failed to delete package for function: {}",
```

### UnstableApiUsage
'tryAcquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

        // Apply rate limiting to mark-delete operations
        if (markDeleteLimiter != null && !markDeleteLimiter.tryAcquire()) {
            isDirty = true;
            updateLastMarkDeleteEntryToLatest(newMarkDeletePosition, null);
```

### UnstableApiUsage
'getRate()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    @Override
    public double getThrottleMarkDelete() {
        return this.markDeleteLimiter.getRate();
    }

```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    private final ReadWriteLock lock = new ReentrantReadWriteLock();

    private RateLimiter markDeleteLimiter;
    // The cursor is considered "dirty" when there are mark-delete updates that are only applied in memory,
    // because of the rate limiting.
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

        if (config.getThrottleMarkDelete() > 0.0) {
            markDeleteLimiter = RateLimiter.create(config.getThrottleMarkDelete());
        } else {
            // Disable mark-delete rate limiter
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

        if (config.getThrottleMarkDelete() > 0.0) {
            markDeleteLimiter = RateLimiter.create(config.getThrottleMarkDelete());
        } else {
            // Disable mark-delete rate limiter
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        if (throttleMarkDelete > 0.0) {
            if (markDeleteLimiter == null) {
                markDeleteLimiter = RateLimiter.create(throttleMarkDelete);
            } else {
                this.markDeleteLimiter.setRate(throttleMarkDelete);
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        if (throttleMarkDelete > 0.0) {
            if (markDeleteLimiter == null) {
                markDeleteLimiter = RateLimiter.create(throttleMarkDelete);
            } else {
                this.markDeleteLimiter.setRate(throttleMarkDelete);
```

### UnstableApiUsage
'setRate(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                markDeleteLimiter = RateLimiter.create(throttleMarkDelete);
            } else {
                this.markDeleteLimiter.setRate(throttleMarkDelete);
            }
        } else {
```

### UnstableApiUsage
'tryAcquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

        // Apply rate limiting to mark-delete operations
        if (markDeleteLimiter != null && !markDeleteLimiter.tryAcquire()) {
            isDirty = true;
            updateLastMarkDeleteEntryToLatest(newPosition, properties);
```

### UnstableApiUsage
'com.google.common.base.Utf8' is marked unstable with @Beta
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
                    } else {
                        byte[] bytes = ByteBufUtil.getBytes(kv.value());
                        if (Utf8.isWellFormed(bytes)) {
                            value = new FunctionState(key, new String(bytes, UTF_8),
                                    null, null, kv.version());
```

### UnstableApiUsage
'isWellFormed(byte\[\])' is declared in unstable class 'com.google.common.base.Utf8' marked with @Beta
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
                    } else {
                        byte[] bytes = ByteBufUtil.getBytes(kv.value());
                        if (Utf8.isWellFormed(bytes)) {
                            value = new FunctionState(key, new String(bytes, UTF_8),
                                    null, null, kv.version());
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    getLatestSourceExceptions();

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
    getLatestSinkExceptions();

```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    getLatestSystemExceptions();

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
    getLatestSourceExceptions();

```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    public abstract double getAvgProcessLatency1min();

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
    getLatestUserExceptions();

```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    protected final FunctionCollectorRegistry collectorRegistry;

    protected final EvictingQueue emptyQueue = EvictingQueue.create(0);

    public static final String USER_METRIC_PREFIX = "user_metric_";
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    protected final FunctionCollectorRegistry collectorRegistry;

    protected final EvictingQueue emptyQueue = EvictingQueue.create(0);

    public static final String USER_METRIC_PREFIX = "user_metric_";
```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    protected final FunctionCollectorRegistry collectorRegistry;

    protected final EvictingQueue emptyQueue = EvictingQueue.create(0);

    public static final String USER_METRIC_PREFIX = "user_metric_";
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
    getLatestUserExceptions();

    public abstract EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation>
    getLatestSystemExceptions();

```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSourceExceptions() {
        return latestSourceExceptions;
    }
```

### UnstableApiUsage
'add(E)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
        long ts = System.currentTimeMillis();
        InstanceCommunication.FunctionStatus.ExceptionInformation info = getExceptionInfo(ex, ts);
        latestSourceExceptions.add(info);

        // report exception throw prometheus
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSinkExceptions() {
        return EvictingQueue.create(0);
    }
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSinkExceptions() {
        return EvictingQueue.create(0);
    }
}
```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSinkExceptions() {
        return EvictingQueue.create(0);
    }
}
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSourceExceptions =
```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSourceExceptions =
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSystemExceptions() {
        return latestSystemExceptions;
    }
```

### UnstableApiUsage
'add(E)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
        long ts = System.currentTimeMillis();
        InstanceCommunication.FunctionStatus.ExceptionInformation info = getExceptionInfo(ex, ts);
        latestSystemExceptions.add(info);

        // report exception throw prometheus
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSourceExceptions =
            EvictingQueue.create(10);

```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSourceExceptions =
            EvictingQueue.create(10);

    protected final RateLimiter sysExceptionRateLimiter;
```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSourceExceptions =
            EvictingQueue.create(10);

    protected final RateLimiter sysExceptionRateLimiter;
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestUserExceptions() {
        return EvictingQueue.create(0);
    }
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestUserExceptions() {
        return EvictingQueue.create(0);
    }

```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestUserExceptions() {
        return EvictingQueue.create(0);
    }

```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSinkExceptions() {
        return latestSinkExceptions;
    }
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestUserExceptions() {
        return emptyQueue;
    }
```

### UnstableApiUsage
'add(E)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
        long ts = System.currentTimeMillis();
        InstanceCommunication.FunctionStatus.ExceptionInformation info = getExceptionInfo(ex, ts);
        latestSinkExceptions.add(info);

        // report exception throw prometheus
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSinkExceptions =
```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSinkExceptions =
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSinkExceptions =
            EvictingQueue.create(10);

```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSinkExceptions =
            EvictingQueue.create(10);

    private final RateLimiter sysExceptionRateLimiter;
```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSinkExceptions =
            EvictingQueue.create(10);

    private final RateLimiter sysExceptionRateLimiter;
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSystemExceptions() {
        return latestSystemExceptions;
    }
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSourceExceptions() {
        return emptyQueue;
    }
```

### UnstableApiUsage
'add(E)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
        long ts = System.currentTimeMillis();
        InstanceCommunication.FunctionStatus.ExceptionInformation info = getExceptionInfo(ex, ts);
        latestSystemExceptions.add(info);

        // report exception throw prometheus
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);

```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);

    private final RateLimiter userExceptionRateLimiter;
```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);

    private final RateLimiter userExceptionRateLimiter;
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSinkExceptions() {
        return emptyQueue;
    }
```

### UnstableApiUsage
'add(E)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java
        long ts = System.currentTimeMillis();
        InstanceCommunication.FunctionStatus.ExceptionInformation info = getExceptionInfo(ex, ts);
        latestUserExceptions.add(info);

        // report exception throw prometheus
```

### UnstableApiUsage
'add(E)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java
        long ts = System.currentTimeMillis();
        InstanceCommunication.FunctionStatus.ExceptionInformation info = getExceptionInfo(ex, ts);
        latestSystemExceptions.add(info);

        // report exception throw prometheus
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java

    @Override
    public EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> getLatestSourceExceptions() {
        return emptyQueue;
    }
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java

    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestUserExceptions =
            EvictingQueue.create(10);
    @Getter
```

### UnstableApiUsage
'com.google.common.collect.EvictingQueue' is marked unstable with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestUserExceptions =
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
```

### UnstableApiUsage
'create(int)' is declared in unstable class 'com.google.common.collect.EvictingQueue' marked with @Beta
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionStatsManager.java`
#### Snippet
```java
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestUserExceptions =
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
        executor.submit(() -> {
            try {
                RateLimiter rateLimiter = RateLimiter.create(arguments.msgRate);
                long startTime = System.nanoTime();
                long testEndTime = startTime + (long) (arguments.testTime * 1e9);
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
        executor.submit(() -> {
            try {
                RateLimiter rateLimiter = RateLimiter.create(arguments.msgRate);
                long startTime = System.nanoTime();
                long testEndTime = startTime + (long) (arguments.testTime * 1e9);
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
        executor.submit(() -> {
            try {
                RateLimiter rateLimiter = RateLimiter.create(arguments.msgRate);
                long startTime = System.nanoTime();
                long testEndTime = startTime + (long) (arguments.testTime * 1e9);
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
                        }

                        rateLimiter.acquire();

                        if (producersMap.get(topic).getSocket().getSession() == null) {
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
                while (!stop.get() && wellnessFlag.value) {
                    producer.sendAsync(payload.get()).exceptionally(exceptionHandler);
                    rateLimiter.acquire();
                }
                producer.closeAsync();
```

### UnstableApiUsage
'setRate(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
        // Change the message rate/size according to the given configuration.
        public void change(final TradeConfiguration tradeConf) {
            rateLimiter.setRate(tradeConf.rate);
            this.payload.set(payloadCache.computeIfAbsent(tradeConf.size, byte[]::new));
        }
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
        Future<Consumer<byte[]>> consumerFuture;
        final AtomicBoolean stop;
        final RateLimiter rateLimiter;

        // Creating a byte[] for every message is stressful for a client
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
            // in the cache.
            this.payload.set(payloadCache.computeIfAbsent(tradeConf.size, byte[]::new));
            rateLimiter = RateLimiter.create(tradeConf.rate);
            stop = new AtomicBoolean(false);
        }
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
            // in the cache.
            this.payload.set(payloadCache.computeIfAbsent(tradeConf.size, byte[]::new));
            rateLimiter = RateLimiter.create(tradeConf.rate);
            stop = new AtomicBoolean(false);
        }
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
                try {
                    final double msgRate = arguments.msgRate / (double) arguments.numThreads;
                    final RateLimiter rateLimiter = RateLimiter.create(msgRate);

                    // Acquire 1 sec worth of messages to have a slower ramp-up
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
                try {
                    final double msgRate = arguments.msgRate / (double) arguments.numThreads;
                    final RateLimiter rateLimiter = RateLimiter.create(msgRate);

                    // Acquire 1 sec worth of messages to have a slower ramp-up
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
                try {
                    final double msgRate = arguments.msgRate / (double) arguments.numThreads;
                    final RateLimiter rateLimiter = RateLimiter.create(msgRate);

                    // Acquire 1 sec worth of messages to have a slower ramp-up
```

### UnstableApiUsage
'acquire(int)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java

                    // Acquire 1 sec worth of messages to have a slower ramp-up
                    rateLimiter.acquire((int) msgRate);
                    final long startTime = System.nanoTime();
                    final long testEndTime = startTime + (long) (arguments.testTime * 1e9);
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java

                            semaphore.acquire();
                            rateLimiter.acquire();

                            final long sendTime = System.nanoTime();
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        final Recorder qRecorder = arguments.autoScaledReceiverQueueSize
                ? new Recorder(arguments.receiverQueueSize, 5) : null;
        final RateLimiter limiter = arguments.rate > 0 ? RateLimiter.create(arguments.rate) : null;
        long startTime = System.nanoTime();
        long testEndTime = startTime + (long) (arguments.testTime * 1e9);
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        final Recorder qRecorder = arguments.autoScaledReceiverQueueSize
                ? new Recorder(arguments.receiverQueueSize, 5) : null;
        final RateLimiter limiter = arguments.rate > 0 ? RateLimiter.create(arguments.rate) : null;
        long startTime = System.nanoTime();
        long testEndTime = startTime + (long) (arguments.testTime * 1e9);
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        final Recorder qRecorder = arguments.autoScaledReceiverQueueSize
                ? new Recorder(arguments.receiverQueueSize, 5) : null;
        final RateLimiter limiter = arguments.rate > 0 ? RateLimiter.create(arguments.rate) : null;
        long startTime = System.nanoTime();
        long testEndTime = startTime + (long) (arguments.testTime * 1e9);
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java

                if (limiter != null) {
                    limiter.acquire();
                }

```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
        log.info("Starting Pulsar performance reader with config: {}", w.writeValueAsString(arguments));

        final RateLimiter limiter = arguments.rate > 0 ? RateLimiter.create(arguments.rate) : null;
        ReaderListener<byte[]> listener = (reader, msg) -> {
            messagesReceived.increment();
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
        log.info("Starting Pulsar performance reader with config: {}", w.writeValueAsString(arguments));

        final RateLimiter limiter = arguments.rate > 0 ? RateLimiter.create(arguments.rate) : null;
        ReaderListener<byte[]> listener = (reader, msg) -> {
            messagesReceived.increment();
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
        log.info("Starting Pulsar performance reader with config: {}", w.writeValueAsString(arguments));

        final RateLimiter limiter = arguments.rate > 0 ? RateLimiter.create(arguments.rate) : null;
        ReaderListener<byte[]> listener = (reader, msg) -> {
            messagesReceived.increment();
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java

            if (limiter != null) {
                limiter.acquire();
            }

```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
        AtomicBoolean executing = new AtomicBoolean(true);

            RateLimiter rateLimiter = arguments.openTxnRate > 0
                    ? RateLimiter.create(arguments.openTxnRate)
                    : null;
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java

            RateLimiter rateLimiter = arguments.openTxnRate > 0
                    ? RateLimiter.create(arguments.openTxnRate)
                    : null;
            for (int i = 0; i < arguments.numTestThreads; i++) {
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java

            RateLimiter rateLimiter = arguments.openTxnRate > 0
                    ? RateLimiter.create(arguments.openTxnRate)
                    : null;
            for (int i = 0; i < arguments.numTestThreads; i++) {
```

### UnstableApiUsage
'tryAcquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java

                        if (rateLimiter != null){
                            rateLimiter.tryAcquire();
                        }
                        if (!arguments.isDisableTransaction) {
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            log.info("Created {} producers", producers.size());

            RateLimiter rateLimiter = RateLimiter.create(msgRate);

            long startTime = System.nanoTime();
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            log.info("Created {} producers", producers.size());

            RateLimiter rateLimiter = RateLimiter.create(msgRate);

            long startTime = System.nanoTime();
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            log.info("Created {} producers", producers.size());

            RateLimiter rateLimiter = RateLimiter.create(msgRate);

            long startTime = System.nanoTime();
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                        }
                    }
                    rateLimiter.acquire();
                    //if transaction is disable, transaction will be null.
                    Transaction transaction = transactionAtomicReference.get();
```

### UnstableApiUsage
'shutdownAndAwaitTermination(java.util.concurrent.ExecutorService, java.time.Duration)' is marked unstable with @Beta
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStore.java`
#### Snippet
```java
                }
                // Shutdown the ExecutorService
                MoreExecutors.shutdownAndAwaitTermination(internalPinnedExecutor, Duration.ofSeconds(5L));
                return CompletableFuture.completedFuture(null);
            });
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

            try (Consumer<?> consumer = builder.subscribe();) {
                RateLimiter limiter = (this.consumeRate > 0) ? RateLimiter.create(this.consumeRate) : null;
                while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
                    if (limiter != null) {
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

            try (Consumer<?> consumer = builder.subscribe();) {
                RateLimiter limiter = (this.consumeRate > 0) ? RateLimiter.create(this.consumeRate) : null;
                while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
                    if (limiter != null) {
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

            try (Consumer<?> consumer = builder.subscribe();) {
                RateLimiter limiter = (this.consumeRate > 0) ? RateLimiter.create(this.consumeRate) : null;
                while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
                    if (limiter != null) {
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
                while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
                    if (limiter != null) {
                        limiter.acquire();
                    }

```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

        try {
            RateLimiter limiter = (this.consumeRate > 0) ? RateLimiter.create(this.consumeRate) : null;
            while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
                if (limiter != null) {
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

        try {
            RateLimiter limiter = (this.consumeRate > 0) ? RateLimiter.create(this.consumeRate) : null;
            while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
                if (limiter != null) {
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

        try {
            RateLimiter limiter = (this.consumeRate > 0) ? RateLimiter.create(this.consumeRate) : null;
            while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
                if (limiter != null) {
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
            while (this.numMessagesToConsume == 0 || numMessagesConsumed < this.numMessagesToConsume) {
                if (limiter != null) {
                    limiter.acquire();
                }
                String msg = consumerSocket.receive(5, TimeUnit.SECONDS);
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
        try {
            List<byte[]> messageBodies = generateMessageBodies(this.messages, this.messageFileNames);
            RateLimiter limiter = (this.publishRate > 0) ? RateLimiter.create(this.publishRate) : null;
            for (int i = 0; i < this.numTimesProduce; i++) {
                int index = i * 10;
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
        try {
            List<byte[]> messageBodies = generateMessageBodies(this.messages, this.messageFileNames);
            RateLimiter limiter = (this.publishRate > 0) ? RateLimiter.create(this.publishRate) : null;
            for (int i = 0; i < this.numTimesProduce; i++) {
                int index = i * 10;
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
        try {
            List<byte[]> messageBodies = generateMessageBodies(this.messages, this.messageFileNames);
            RateLimiter limiter = (this.publishRate > 0) ? RateLimiter.create(this.publishRate) : null;
            for (int i = 0; i < this.numTimesProduce; i++) {
                int index = i * 10;
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
                for (byte[] content : messageBodies) {
                    if (limiter != null) {
                        limiter.acquire();
                    }
                    produceSocket.send(index++, content).get(30, TimeUnit.SECONDS);
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java

                List<byte[]> messageBodies = generateMessageBodies(this.messages, this.messageFileNames);
                RateLimiter limiter = (this.publishRate > 0) ? RateLimiter.create(this.publishRate) : null;

                Map<String, String> kvMap = new HashMap<>();
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java

                List<byte[]> messageBodies = generateMessageBodies(this.messages, this.messageFileNames);
                RateLimiter limiter = (this.publishRate > 0) ? RateLimiter.create(this.publishRate) : null;

                Map<String, String> kvMap = new HashMap<>();
```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java

                List<byte[]> messageBodies = generateMessageBodies(this.messages, this.messageFileNames);
                RateLimiter limiter = (this.publishRate > 0) ? RateLimiter.create(this.publishRate) : null;

                Map<String, String> kvMap = new HashMap<>();
```

### UnstableApiUsage
'acquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
                    for (byte[] content : messageBodies) {
                        if (limiter != null) {
                            limiter.acquire();
                        }

```

### UnstableApiUsage
'org.apache.bookkeeper.client.ITopologyAwareEnsemblePlacementPolicy' is unstable because its signature references unstable interface 'org.apache.bookkeeper.net.Node' marked with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java

    @Override
    public void registerRackChangeListener(ITopologyAwareEnsemblePlacementPolicy<BookieNode> rackawarePolicy) {
        this.rackawarePolicy = rackawarePolicy;
    }
```

### UnstableApiUsage
Overridden method 'reloadCachedMappings()' is declared in unstable interface 'org.apache.bookkeeper.net.DNSToSwitchMapping' marked with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java

    @Override
    public void reloadCachedMappings() {
        // no-op
    }
```

### UnstableApiUsage
Overridden method 'resolve(java.util.List)' is declared in unstable interface 'org.apache.bookkeeper.net.DNSToSwitchMapping' marked with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java

    @Override
    public synchronized List<String> resolve(List<String> bookieAddressList) {
        List<String> racks = new ArrayList<>(bookieAddressList.size());
        for (String bookieAddress : bookieAddressList) {
```

### UnstableApiUsage
'org.apache.bookkeeper.client.ITopologyAwareEnsemblePlacementPolicy' is unstable because its signature references unstable interface 'org.apache.bookkeeper.net.Node' marked with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java

    private MetadataCache<BookiesRackConfiguration> bookieMappingCache = null;
    private ITopologyAwareEnsemblePlacementPolicy<BookieNode> rackawarePolicy = null;
    private List<BookieId> bookieAddressListLastTime = new ArrayList<>();

```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/RateLimitingFilter.java`
#### Snippet
```java

    public RateLimitingFilter(double rateLimit) {
        limiter = RateLimiter.create(rateLimit);
    }

```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/RateLimitingFilter.java`
#### Snippet
```java

    public RateLimitingFilter(double rateLimit) {
        limiter = RateLimiter.create(rateLimit);
    }

```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/RateLimitingFilter.java`
#### Snippet
```java
public class RateLimitingFilter implements Filter {

    private final RateLimiter limiter;

    public RateLimitingFilter(double rateLimit) {
```

### UnstableApiUsage
'tryAcquire()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/RateLimitingFilter.java`
#### Snippet
```java
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain)
            throws IOException, ServletException {
        if (limiter.tryAcquire()) {
            chain.doFilter(request, response);
        } else {
```

### UnstableApiUsage
'org.apache.bookkeeper.net.DNSToSwitchMapping' is marked unstable with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
    @Override
    public RackawareEnsemblePlacementPolicyImpl initialize(ClientConfiguration conf,
            Optional<DNSToSwitchMapping> optionalDnsResolver, HashedWheelTimer timer, FeatureProvider featureProvider,
            StatsLogger statsLogger, BookieAddressResolver bookieAddressResolver) {
        MetadataStore store;
```

### UnstableApiUsage
'initialize(org.apache.bookkeeper.conf.ClientConfiguration, java.util.Optional, io.netty.util.HashedWheelTimer, org.apache.bookkeeper.feature.FeatureProvider, org.apache.bookkeeper.stats.StatsLogger, org.apache.bookkeeper.proto.BookieAddressResolver)' is unstable because its signature references unstable interface 'org.apache.bookkeeper.net.DNSToSwitchMapping' marked with @Beta
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
        }
        defaultIsolationGroups = ImmutablePair.of(primaryIsolationGroups, secondaryIsolationGroups);
        return super.initialize(conf, optionalDnsResolver, timer, featureProvider, statsLogger, bookieAddressResolver);
    }

```

### UnstableApiUsage
'getLogMetadataStore()' is unstable because its signature references unstable interface 'org.apache.distributedlog.metadata.LogMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
    @Override
    public CompletableFuture<List<String>> listAsync(String path) {
        return namespace.getNamespaceDriver().getLogMetadataStore().getLogs(path)
            .thenApply(logs -> {
                ArrayList<String> packages = new ArrayList<>();
```

### UnstableApiUsage
'getLogs(java.lang.String)' is declared in unstable interface 'org.apache.distributedlog.metadata.LogMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
    @Override
    public CompletableFuture<List<String>> listAsync(String path) {
        return namespace.getNamespaceDriver().getLogMetadataStore().getLogs(path)
            .thenApply(logs -> {
                ArrayList<String> packages = new ArrayList<>();
```

### UnstableApiUsage
'getLogMetadataStore()' is unstable because its signature references unstable interface 'org.apache.distributedlog.metadata.LogMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Void> deleteAsync(String path) {
        return namespace.getNamespaceDriver().getLogMetadataStore().getLogLocation(path)
            .thenCompose(uri -> uri.map(value -> namespace.getNamespaceDriver()
                .getLogStreamMetadataStore(NamespaceDriver.Role.WRITER).deleteLog(value, path))
```

### UnstableApiUsage
'getLogLocation(java.lang.String)' is declared in unstable interface 'org.apache.distributedlog.metadata.LogMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Void> deleteAsync(String path) {
        return namespace.getNamespaceDriver().getLogMetadataStore().getLogLocation(path)
            .thenCompose(uri -> uri.map(value -> namespace.getNamespaceDriver()
                .getLogStreamMetadataStore(NamespaceDriver.Role.WRITER).deleteLog(value, path))
```

### UnstableApiUsage
'getLogStreamMetadataStore(org.apache.distributedlog.namespace.NamespaceDriver.Role)' is unstable because its signature references unstable interface 'org.apache.distributedlog.metadata.LogStreamMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
        return namespace.getNamespaceDriver().getLogMetadataStore().getLogLocation(path)
            .thenCompose(uri -> uri.map(value -> namespace.getNamespaceDriver()
                .getLogStreamMetadataStore(NamespaceDriver.Role.WRITER).deleteLog(value, path))
                .orElse(null));
    }
```

### UnstableApiUsage
'deleteLog(java.net.URI, java.lang.String)' is declared in unstable interface 'org.apache.distributedlog.metadata.LogStreamMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
        return namespace.getNamespaceDriver().getLogMetadataStore().getLogLocation(path)
            .thenCompose(uri -> uri.map(value -> namespace.getNamespaceDriver()
                .getLogStreamMetadataStore(NamespaceDriver.Role.WRITER).deleteLog(value, path))
                .orElse(null));
    }
```

### UnstableApiUsage
'getLogMetadataStore()' is unstable because its signature references unstable interface 'org.apache.distributedlog.metadata.LogMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
    public CompletableFuture<Boolean> existAsync(String path) {
        CompletableFuture<Boolean> result = new CompletableFuture<>();
        namespace.getNamespaceDriver().getLogMetadataStore().getLogLocation(path)
            .whenComplete((uriOptional, throwable) -> {
                if (throwable != null) {
```

### UnstableApiUsage
'getLogLocation(java.lang.String)' is declared in unstable interface 'org.apache.distributedlog.metadata.LogMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
    public CompletableFuture<Boolean> existAsync(String path) {
        CompletableFuture<Boolean> result = new CompletableFuture<>();
        namespace.getNamespaceDriver().getLogMetadataStore().getLogLocation(path)
            .whenComplete((uriOptional, throwable) -> {
                if (throwable != null) {
```

### UnstableApiUsage
'getLogStreamMetadataStore(org.apache.distributedlog.namespace.NamespaceDriver.Role)' is unstable because its signature references unstable interface 'org.apache.distributedlog.metadata.LogStreamMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
                if (uriOptional.isPresent()) {
                    namespace.getNamespaceDriver()
                        .getLogStreamMetadataStore(NamespaceDriver.Role.WRITER)
                        .logExists(uriOptional.get(), path)
                        .whenComplete((ignore, e) -> {
```

### UnstableApiUsage
'logExists(java.net.URI, java.lang.String)' is declared in unstable interface 'org.apache.distributedlog.metadata.LogStreamMetadataStore' marked with @Beta
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/BookKeeperPackagesStorage.java`
#### Snippet
```java
                    namespace.getNamespaceDriver()
                        .getLogStreamMetadataStore(NamespaceDriver.Role.WRITER)
                        .logExists(uriOptional.get(), path)
                        .whenComplete((ignore, e) -> {
                            if (e != null) {
```

## RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
### EnumSwitchStatementWhichMissesCases
`switch (kafkaSchema.type()) { case INT8: if (!(nativeObject inst...` statement on enum type 'org.apache.kafka.connect.data.Schema.Type' misses cases: 'BOOLEAN', 'STRING', 'BYTES', 'ARRAY', 'MAP', ...
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
switch (kafkaSchema.type()) {
                case INT8:
                    if (!(nativeObject instanceof Byte)) {
                        if (log.isDebugEnabled()) {
                            log.debug("nativeObject of type {} converted to Byte", nativeObject.getClass());
                        }
                        return num.byteValue();
                    }
                    break;
                case INT16:
                    if (!(nativeObject instanceof Short)) {
                        if (log.isDebugEnabled()) {
                            log.debug("nativeObject of type {} converted to Short", nativeObject.getClass());
                        }
                        return num.shortValue();
                    }
                    break;
                case INT32:
                    if (!(nativeObject instanceof Integer)) {
                        if (log.isDebugEnabled()) {
                            log.debug("nativeObject of type {} converted to Integer", nativeObject.getClass());
                        }
                        return num.intValue();
                    }
                    break;
                case INT64:
                    if (!(nativeObject instanceof Long)) {
                        if (log.isDebugEnabled()) {
                            log.debug("nativeObject of type {} converted to Long", nativeObject.getClass());
                        }
                        return num.longValue();
                    }
                    break;
                case FLOAT32:
                    if (!(nativeObject instanceof Float)) {
                        if (log.isDebugEnabled()) {
                            log.debug("nativeObject of type {} converted to Float", nativeObject.getClass());
                        }
                        return num.floatValue();
                    }
                    break;
                case FLOAT64:
                    if (!(nativeObject instanceof Double)) {
                        if (log.isDebugEnabled()) {
                            log.debug("nativeObject of type {} converted to Double", nativeObject.getClass());
                        }
                        return num.doubleValue();
                    }
                    break;
            }
```

### EnumSwitchStatementWhichMissesCases
`switch (state) { case Initializing: addCommitTxnRequest(...` statement on enum type 'org.apache.pulsar.broker.transaction.pendingack.impl.PendingAckHandleState.State' misses cases: 'Ready', and 'Close'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        internalPinnedExecutor.execute(() -> {
            if (!checkIfReady()) {
                switch (state) {
                    case Initializing:
                        addCommitTxnRequest(txnID, properties, lowWaterMark, commitFuture);
                        return;
                    case None:
                        addCommitTxnRequest(txnID, properties, lowWaterMark, commitFuture);
                        initPendingAckStore();
                        return;
                    case Error:
                        if (state == State.Error) {
                            commitFuture.completeExceptionally(
                                    new ServiceUnitNotReadyException("PendingAckHandle not replay error!"));
                        } else {
                            commitFuture.completeExceptionally(
                                    new ServiceUnitNotReadyException("PendingAckHandle have been closed!"));
                        }
                        return;
                }
            }
            internalCommitTxn(txnID, properties, lowWaterMark, commitFuture);
```

### EnumSwitchStatementWhichMissesCases
`switch (getState()) { case Ready: case Connecting: break; //...` statement on enum type 'org.apache.pulsar.client.impl.HandlerState.State' misses case 'RegisteringSchema'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
        }

        switch (getState()) {
            case Ready:
            case Connecting:
                break; // Ok
            case Closing:
            case Closed:
                return FutureUtil.failedFuture(
                        new PulsarClientException.AlreadyClosedException("Producer already closed"));
            case ProducerFenced:
                return FutureUtil.failedFuture(
                        new PulsarClientException.ProducerFencedException("Producer was fenced"));
            case Terminated:
                return FutureUtil.failedFuture(
                        new PulsarClientException.TopicTerminatedException("Topic was terminated"));
            case Failed:
            case Uninitialized:
                return FutureUtil.failedFuture(new PulsarClientException.NotConnectedException());
        }

        return producers.get(partition).internalSendWithTxnAsync(message, txn);
```

### EnumSwitchStatementWhichMissesCases
`switch (op.getType()) { case PUT: { OpPut put = op.asPut...` statement on enum type 'org.apache.pulsar.metadata.impl.batching.MetadataOp.Type' misses cases: 'GET', and 'GET_CHILDREN'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
            // First, set all the conditions
            ops.forEach(op -> {
                switch (op.getType()) {
                    case PUT: {
                        OpPut put = op.asPut();
                        ByteSequence key = ByteSequence.from(put.getPath(), StandardCharsets.UTF_8);
                        if (put.getOptExpectedVersion().isPresent()) {
                            long expectedVersion = put.getOptExpectedVersion().get();
                            if (expectedVersion == -1L) {
                                // Check that key does not exist
                                txn.If(new Cmp(key, Cmp.Op.EQUAL, CmpTarget.createRevision(0)));
                            } else {
                                txn.If(new Cmp(key, Cmp.Op.EQUAL, CmpTarget.version(expectedVersion + 1)));
                            }
                        }
                        break;
                    }
                    case DELETE: {
                        OpDelete del = op.asDelete();
                        ByteSequence key = ByteSequence.from(del.getPath(), StandardCharsets.UTF_8);
                        if (del.getOptExpectedVersion().isPresent()) {
                            txn.If(new Cmp(key, Cmp.Op.EQUAL,
                                    CmpTarget.version(del.getOptExpectedVersion().get() + 1)));
                        }
                        break;
                    }
                }
            });

```

## RuleId[ruleID=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `getQueue()` declared in class 'org.apache.pulsar.io.flume.sink.AbstractSink' but referenced via subclass 'org.apache.pulsar.io.flume.sink.StringSink'
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/sink/SourceOfFlume.java`
#### Snippet
```java
            while (eventList.size() < this.getBatchSize()
                    && System.currentTimeMillis() < maxBatchEndTime) {
                BlockingQueue<Object> blockingQueue = StringSink.getQueue();
                while (blockingQueue != null && !blockingQueue.isEmpty()) {
                    Object message = blockingQueue.take();
```

### StaticCallOnSubclass
Static method `getQueue()` declared in class 'org.apache.pulsar.io.flume.source.AbstractSinkOfFlume' but referenced via subclass 'org.apache.pulsar.io.flume.source.SinkOfFlume'
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/AbstractSource.java`
#### Snippet
```java
                log.info("start flume receive from sink process");
                while (running) {
                    BlockingQueue<Map<String, Object>> blockingQueue = SinkOfFlume.getQueue();
                    while (blockingQueue != null && !blockingQueue.isEmpty()) {
                        ByteArrayOutputStream bos = new ByteArrayOutputStream();
```

### StaticCallOnSubclass
Static method `of()` declared in class 'org.apache.pulsar.client.impl.schema.generic.GenericSchemaImpl' but referenced via subclass 'org.apache.pulsar.client.impl.schema.generic.GenericJsonSchema'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroRowDecoderFactory.java`
#### Snippet
```java
        Schema schema;
        try {
            schema = GenericJsonSchema.of(schemaInfo).getAvroSchema();
        } catch (SchemaParseException ex) {
            throw new TrinoException(NOT_SUPPORTED, "Topic "
```

### StaticCallOnSubclass
Static method `of()` declared in class 'org.apache.pulsar.client.impl.schema.generic.GenericSchemaImpl' but referenced via subclass 'org.apache.pulsar.client.impl.schema.generic.GenericAvroSchema'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroRowDecoderFactory.java`
#### Snippet
```java
    public PulsarRowDecoder createRowDecoder(TopicName topicName, SchemaInfo schemaInfo,
                                             Set<DecoderColumnHandle> columns) {
        return new PulsarAvroRowDecoder((GenericAvroSchema) GenericAvroSchema.of(schemaInfo), columns);
    }

```

### StaticCallOnSubclass
Static method `of()` declared in class 'org.apache.pulsar.client.impl.schema.generic.GenericSchemaImpl' but referenced via subclass 'org.apache.pulsar.client.impl.schema.generic.GenericJsonSchema'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonRowDecoderFactory.java`
#### Snippet
```java
        Schema schema;
        try {
            schema = GenericJsonSchema.of(schemaInfo).getAvroSchema();
        } catch (SchemaParseException ex) {
            throw new TrinoException(NOT_SUPPORTED, "Topic "
```

### StaticCallOnSubclass
Static method `of()` declared in class 'org.apache.pulsar.client.impl.schema.generic.GenericSchemaImpl' but referenced via subclass 'org.apache.pulsar.client.impl.schema.generic.GenericJsonSchema'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonRowDecoderFactory.java`
#### Snippet
```java
    public PulsarJsonRowDecoder createRowDecoder(TopicName topicName, SchemaInfo schemaInfo,
                                                 Set<DecoderColumnHandle> columns) {
        return new PulsarJsonRowDecoder((GenericJsonSchema) GenericJsonSchema.of(schemaInfo), columns);
    }

```

### StaticCallOnSubclass
Static method `getRemoteCluster()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
    private PersistentReplicator getReplicatorReference(String replName, PersistentTopic topic) {
        try {
            String remoteCluster = PersistentReplicator.getRemoteCluster(replName);
            PersistentReplicator repl = (PersistentReplicator) topic.getPersistentReplicator(remoteCluster);
            return checkNotNull(repl);
```

### StaticCallOnSubclass
Static method `getRemoteCluster()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                boolean issued;
                if (subName.startsWith(topic.getReplicatorPrefix())) {
                    String remoteCluster = PersistentReplicator.getRemoteCluster(subName);
                    PersistentReplicator repl = (PersistentReplicator) topic
                            .getPersistentReplicator(remoteCluster);
```

### StaticCallOnSubclass
Static method `getRemoteCluster()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                             }
                             if (subName.startsWith(topic.getReplicatorPrefix())) {
                                 String remoteCluster = PersistentReplicator.getRemoteCluster(subName);
                                 PersistentReplicator repl =
                                         (PersistentReplicator) topic.getPersistentReplicator(remoteCluster);
```

### StaticCallOnSubclass
Static method `getRemoteCluster()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                    try {
                        if (subName.startsWith(topic.getReplicatorPrefix())) {
                            String remoteCluster = PersistentReplicator.getRemoteCluster(subName);
                            PersistentReplicator repl = (PersistentReplicator)
                                    topic.getPersistentReplicator(remoteCluster);
```

### StaticCallOnSubclass
Static method `getRemoteCluster()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                    };
                    if (subName.startsWith(topic.getReplicatorPrefix())) {
                        String remoteCluster = PersistentReplicator.getRemoteCluster(subName);
                        PersistentReplicator repl =
                            (PersistentReplicator) topic.getPersistentReplicator(remoteCluster);
```

### StaticCallOnSubclass
Static method `getClientErrorCode()` declared in class 'org.apache.pulsar.broker.service.BrokerServiceException' but referenced via subclass 'org.apache.pulsar.broker.service.BrokerServiceException.ServiceUnitNotReadyException'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                            if (producerFuture.completeExceptionally(exception)) {
                                commandSender.sendErrorResponse(requestId,
                                        ServiceUnitNotReadyException.getClientErrorCode(cause),
                                        cause.getMessage());
                            }
```

### StaticCallOnSubclass
Static method `getRemoteCluster()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
            if (subscription != null) {
                if (subscription.startsWith(pulsar().getConfiguration().getReplicatorPrefix())) {
                    subscription = PersistentReplicator.getRemoteCluster(subscription);
                }
                for (Topic topic : topicList) {
```

### StaticCallOnSubclass
Static method `getReplicatorName()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.nonpersistent.NonPersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
        final CompletableFuture<Void> future = new CompletableFuture<>();

        String name = NonPersistentReplicator.getReplicatorName(replicatorPrefix, remoteCluster);

        replicators.get(remoteCluster).disconnect().thenRun(() -> {
```

### StaticCallOnSubclass
Static method `getReplicatorName()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        final CompletableFuture<Void> future = new CompletableFuture<>();

        String name = PersistentReplicator.getReplicatorName(replicatorPrefix, remoteCluster);

        replicators.get(remoteCluster).disconnect().thenRun(() -> {
```

### StaticCallOnSubclass
Static method `getRemoteCluster()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            if (cursor.getName().startsWith(replicatorPrefix)) {
                String localCluster = brokerService.pulsar().getConfiguration().getClusterName();
                String remoteCluster = PersistentReplicator.getRemoteCluster(cursor.getName());
                futures.add(addReplicationCluster(remoteCluster, cursor, localCluster));
            }
```

### StaticCallOnSubclass
Static method `getReplicatorName()` declared in class 'org.apache.pulsar.broker.service.AbstractReplicator' but referenced via subclass 'org.apache.pulsar.broker.service.persistent.PersistentReplicator'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        final CompletableFuture<Void> future = new CompletableFuture<>();

        String name = PersistentReplicator.getReplicatorName(replicatorPrefix, remoteCluster);
        ledger.asyncOpenCursor(name, new OpenCursorCallback() {
            @Override
```

### StaticCallOnSubclass
Static method `of()` declared in class 'org.apache.pulsar.client.impl.schema.generic.GenericSchemaImpl' but referenced via subclass 'org.apache.pulsar.client.impl.schema.generic.GenericAvroSchema'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroSchema.java`
#### Snippet
```java
    public org.apache.pulsar.client.api.Schema<GenericRecord> clone() {
        org.apache.pulsar.client.api.Schema<GenericRecord> schema =
                GenericAvroSchema.of(schemaInfo,
                        ((AbstractMultiVersionGenericReader) reader).useProvidedSchemaAsReaderSchema);
        if (schemaInfoProvider != null) {
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpAddEntry.java`
#### Snippet
```java
            log.debug("Successfully closed ledger {}", lh.getId());
        } else {
            log.warn("Error when closing ledger {}. Status={}", lh.getId(), BKException.getMessage(rc));
        }

```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerOfflineBacklog.java`
#### Snippet
```java
                        if (rc != BKException.Code.OK) {
                            log.warn("[{}] Error opening metadata ledger {} for cursor {}: {}", managedLedgerName,
                                    ledgerId, cursorName, BKException.getMessage(rc));
                            cursorCounter.countDown();
                            return;
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerOfflineBacklog.java`
#### Snippet
```java
                                if (log.isDebugEnabled()) {
                                    log.debug("[{}] Opened ledger {}: {}", managedLedgerName, id,
                                            BKException.getMessage(rc));
                                }
                                if (rc == BKException.Code.OK) {
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerOfflineBacklog.java`
#### Snippet
```java
                                } else {
                                    log.error("[{}] Failed to open ledger {}: {}", managedLedgerName, id,
                                            BKException.getMessage(rc));
                                    mlMetaCounter.countDown();
                                }
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerOfflineBacklog.java`
#### Snippet
```java
                                    if (rc != BKException.Code.OK) {
                                        log.warn("[{}] Error reading from metadata ledger {} for cursor {}: {}",
                                                managedLedgerName, ledgerId, cursorName, BKException.getMessage(rc));
                                        // indicate that this cursor should be excluded
                                        offlineTopicStats.addCursorDetails(cursorName, errorInReadingCursor,
```

### StaticCallOnSubclass
Static method `elapsedMSec()` declared in class 'org.apache.bookkeeper.common.util.MathUtils' but referenced via subclass 'org.apache.bookkeeper.util.MathUtils'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java

        public boolean allowRetry(int rc) {
            elapsedTimeMs = MathUtils.elapsedMSec(startTimeNanos);
            if (!ZooWorker.isRecoverableException(rc)) {
                if (KeeperException.Code.OK.intValue() == rc) {
```

### StaticCallOnSubclass
Static method `elapsedMicroSec()` declared in class 'org.apache.bookkeeper.common.util.MathUtils' but referenced via subclass 'org.apache.bookkeeper.util.MathUtils'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
            if (!ZooWorker.isRecoverableException(rc)) {
                if (KeeperException.Code.OK.intValue() == rc) {
                    statsLogger.registerSuccessfulEvent(MathUtils.elapsedMicroSec(startTimeNanos),
                            TimeUnit.MICROSECONDS);
                } else {
```

### StaticCallOnSubclass
Static method `elapsedMicroSec()` declared in class 'org.apache.bookkeeper.common.util.MathUtils' but referenced via subclass 'org.apache.bookkeeper.util.MathUtils'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
                            TimeUnit.MICROSECONDS);
                } else {
                    statsLogger.registerFailedEvent(MathUtils.elapsedMicroSec(startTimeNanos),
                            TimeUnit.MICROSECONDS);
                }
```

### StaticCallOnSubclass
Static method `nowInNano()` declared in class 'org.apache.bookkeeper.common.util.MathUtils' but referenced via subclass 'org.apache.bookkeeper.util.MathUtils'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
            this.retryPolicy = retryPolicy;
            this.statsLogger = statsLogger;
            this.startTimeNanos = MathUtils.nowInNano();
        }

```

### StaticCallOnSubclass
Static method `nowInNano()` declared in class 'org.apache.bookkeeper.common.util.MathUtils' but referenced via subclass 'org.apache.bookkeeper.util.MathUtils'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
            boolean isDone = false;
            int attempts = 0;
            long startTimeNanos = MathUtils.nowInNano();
            while (!isDone) {
                try {
```

### StaticCallOnSubclass
Static method `elapsedMicroSec()` declared in class 'org.apache.bookkeeper.common.util.MathUtils' but referenced via subclass 'org.apache.bookkeeper.util.MathUtils'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
                    result = proc.call();
                    isDone = true;
                    statsLogger.registerSuccessfulEvent(MathUtils.elapsedMicroSec(startTimeNanos),
                            TimeUnit.MICROSECONDS);
                } catch (KeeperException e) {
```

### StaticCallOnSubclass
Static method `elapsedMSec()` declared in class 'org.apache.bookkeeper.common.util.MathUtils' but referenced via subclass 'org.apache.bookkeeper.util.MathUtils'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
                    ++attempts;
                    boolean rethrow = true;
                    long elapsedTime = MathUtils.elapsedMSec(startTimeNanos);
                    if (((null != client && isRecoverableException(e)) || null == client)
                            && retryPolicy.allowRetry(attempts, elapsedTime)) {
```

### StaticCallOnSubclass
Static method `elapsedMicroSec()` declared in class 'org.apache.bookkeeper.common.util.MathUtils' but referenced via subclass 'org.apache.bookkeeper.util.MathUtils'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
                    }
                    if (rethrow) {
                        statsLogger.registerFailedEvent(MathUtils.elapsedMicroSec(startTimeNanos),
                                TimeUnit.MICROSECONDS);
                        log.debug("Stopped executing {} after {} attempts.", proc, attempts);
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            if (rc != BKException.Code.OK) {
                log.warn("[{}] Failed to delete ledger {}: {}", ledger.getName(), lh.getId(),
                        BKException.getMessage(rc));
                if (!isNoSuchLedgerExistsException(rc)) {
                    ledger.getScheduledExecutor().schedule(safeRun(() -> asyncDeleteLedger(lh, retry - 1)),
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            } else {
                log.warn("[{}][{}] Failed to delete ledger {}: {}", ledger.getName(), name, cursorLedger.getId(),
                        BKException.getMessage(rc));
                if (!isNoSuchLedgerExistsException(rc)) {
                    ledger.getScheduledExecutor().schedule(safeRun(() -> asyncDeleteCursorLedger(retry - 1)),
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                if (rc != BKException.Code.OK) {
                    log.warn("[{}] Error creating ledger for cursor {}: {}", ledger.getName(), name,
                            BKException.getMessage(rc));
                    future.completeExceptionally(new ManagedLedgerException(BKException.getMessage(rc)));
                    return;
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                    log.warn("[{}] Error creating ledger for cursor {}: {}", ledger.getName(), name,
                            BKException.getMessage(rc));
                    future.completeExceptionally(new ManagedLedgerException(BKException.getMessage(rc)));
                    return;
                }
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            if (isBkErrorNotRecoverable(rc)) {
                log.error("[{}] Error opening metadata ledger {} for consumer {}: {}", ledger.getName(), ledgerId, name,
                        BKException.getMessage(rc));
                // Rewind to oldest entry available
                initialize(getRollbackPosition(info), Collections.emptyMap(), Collections.emptyMap(), callback);
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            } else if (rc != BKException.Code.OK) {
                log.warn("[{}] Error opening metadata ledger {} for consumer {}: {}", ledger.getName(), ledgerId, name,
                        BKException.getMessage(rc));
                callback.operationFailed(new ManagedLedgerException(BKException.getMessage(rc)));
                return;
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                log.warn("[{}] Error opening metadata ledger {} for consumer {}: {}", ledger.getName(), ledgerId, name,
                        BKException.getMessage(rc));
                callback.operationFailed(new ManagedLedgerException(BKException.getMessage(rc)));
                return;
            }
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                if (isBkErrorNotRecoverable(rc1)) {
                    log.error("[{}] Error reading from metadata ledger {} for consumer {}: {}", ledger.getName(),
                            ledgerId, name, BKException.getMessage(rc1));
                    // Rewind to oldest entry available
                    initialize(getRollbackPosition(info), Collections.emptyMap(), cursorProperties, callback);
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                } else if (rc1 != BKException.Code.OK) {
                    log.warn("[{}] Error reading from metadata ledger {} for consumer {}: {}", ledger.getName(),
                            ledgerId, name, BKException.getMessage(rc1));

                    callback.operationFailed(createManagedLedgerException(rc1));
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                } else {
                    log.warn("[{}][{}] Failed to close cursor-ledger {}: {}", ledger.getName(), name,
                            cursorLedger.getId(), BKException.getMessage(rc));
                    callback.closeFailed(createManagedLedgerException(rc), ctx);
                }
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            } else {
                log.warn("[{}] Error updating cursor {} position {} in meta-ledger {}: {}", ledger.getName(), name,
                        position, lh1.getId(), BKException.getMessage(rc));
                // If we've had a write error, the ledger will be automatically closed, we need to create a new one,
                // in the meantime the mark-delete will be queued.
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                    // Handle error
                    log.warn("[{}] Failed to delete ledger {} -- {}", name, ls.getLedgerId(),
                            BKException.getMessage(rc) + " code " + rc);
                    int toDelete = ledgersToDelete.get();
                    if (toDelete != -1 && ledgersToDelete.compareAndSet(toDelete, -1)) {
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                            mbean.endDataLedgerOpenOp();
                            if (log.isDebugEnabled()) {
                                log.debug("[{}] Opened ledger {}: {}", name, id, BKException.getMessage(rc));
                            }
                            if (rc == BKException.Code.OK) {
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                                initializeBookKeeper(callback);
                            } else {
                                log.error("[{}] Failed to open ledger {}: {}", name, id, BKException.getMessage(rc));
                                callback.initializeFailed(createManagedLedgerException(rc));
                                return;
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        mbean.endDataLedgerCreateOp();
        if (rc != BKException.Code.OK) {
            log.error("[{}] Error creating ledger rc={} {}", name, rc, BKException.getMessage(rc));
            ManagedLedgerException status = createManagedLedgerException(rc);

```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        } else if (isBkErrorNotRecoverable(bkErrorCode)) {
            if (isLedgerNotExistException(bkErrorCode)) {
                return new LedgerNotExistException(BKException.getMessage(bkErrorCode));
            } else {
                return new NonRecoverableLedgerException(BKException.getMessage(bkErrorCode));
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                return new LedgerNotExistException(BKException.getMessage(bkErrorCode));
            } else {
                return new NonRecoverableLedgerException(BKException.getMessage(bkErrorCode));
            }
        } else {
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            }
        } else {
            return new ManagedLedgerException(BKException.getMessage(bkErrorCode) + " error code: " + bkErrorCode);
        }
    }
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                log.warn("[{}] Ledger was already deleted {}", name, ledgerId);
            } else if (rc != BKException.Code.OK) {
                log.error("[{}] Error deleting ledger {} : {}", name, ledgerId, BKException.getMessage(rc));
                scheduledExecutor.schedule(safeRun(() -> asyncDeleteLedger(ledgerId, retry - 1)),
                        DEFAULT_LEDGER_DELETE_BACKOFF_TIME_SEC, TimeUnit.SECONDS);
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                        if (rc1 != BKException.Code.OK) {
                            log.warn("[{}] Failed to delete ledger {}: {}", name, lh.getId(),
                                    BKException.getMessage(rc1));
                        }
                    }, null);
```

### StaticCallOnSubclass
Static method `getMessage()` declared in class 'org.apache.bookkeeper.client.api.BKException' but referenced via subclass 'org.apache.bookkeeper.client.BKException'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                        log.debug("Successfully closed ledger {}", lh.getId());
                    } else {
                        log.warn("Error when closing ledger {}. Status={}", lh.getId(), BKException.getMessage(rc));
                    }

```

## RuleId[ruleID=ObsoleteCollection]
### ObsoleteCollection
Obsolete collection type `Stack` used
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/StatsOutputStream.java`
#### Snippet
```java

public class StatsOutputStream extends SimpleTextOutputStream {
    private final Stack<Boolean> separators = new Stack<>();

    public StatsOutputStream(ByteBuf buffer) {
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/StatsOutputStream.java`
#### Snippet
```java

public class StatsOutputStream extends SimpleTextOutputStream {
    private final Stack<Boolean> separators = new Stack<>();

    public StatsOutputStream(ByteBuf buffer) {
```

## RuleId[ruleID=CastToIncompatibleInterface]
### CastToIncompatibleInterface
Cast to incompatible interface `org.apache.avro.generic.GenericRecord`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroReader.java`
#### Snippet
```java
            Decoder decoder = DecoderFactory.get().binaryDecoder(bytes, offset, length - offset, null);
            org.apache.avro.generic.GenericRecord avroRecord =
                    (org.apache.avro.generic.GenericRecord) reader.read(
                    null,
                    decoder);
```

### CastToIncompatibleInterface
Cast to incompatible interface `org.apache.avro.generic.GenericRecord`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroReader.java`
#### Snippet
```java
            Decoder decoder = DecoderFactory.get().binaryDecoder(inputStream, null);
            org.apache.avro.generic.GenericRecord avroRecord =
                    (org.apache.avro.generic.GenericRecord) reader.read(
                            null,
                            decoder);
```

## RuleId[ruleID=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/ByteBufferSchemaWrapper.java`
#### Snippet
```java

    @Override
    public Schema<ByteBuffer> clone() {
        return new ByteBufferSchemaWrapper(original);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaSchemaWrappedSchema.java`
#### Snippet
```java

    @Override
    public Schema<byte[]> clone() {
        return null;
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ReaderBuilderImpl.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public ReaderBuilder<T> clone() {
        return new ReaderBuilderImpl<>(client, conf.clone(), schema);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerBuilderImpl.java`
#### Snippet
```java

    @Override
    public ProducerBuilder<T> clone() {
        return new ProducerBuilderImpl<>(client, conf.clone(), schema);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientBuilderImpl.java`
#### Snippet
```java

    @Override
    public ClientBuilder clone() {
        return new ClientBuilderImpl(conf.clone());
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBuilderImpl.java`
#### Snippet
```java

    @Override
    public ConsumerBuilder<T> clone() {
        return new ConsumerBuilderImpl<>(client, conf.clone(), schema);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/DefaultCryptoKeyReaderBuilder.java`
#### Snippet
```java

    @Override
    public DefaultCryptoKeyReaderBuilder clone() {
        return new DefaultCryptoKeyReaderBuilder(conf.clone());
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java

    @Override
    public DefaultCryptoKeyReaderConfigurationData clone() {
        DefaultCryptoKeyReaderConfigurationData clone = new DefaultCryptoKeyReaderConfigurationData();

```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AbstractSchema.java`
#### Snippet
```java

    @Override
    public Schema<T> clone() {
        return this;
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/NativeAvroBytesSchema.java`
#### Snippet
```java

    @Override
    public Schema<byte[]> clone() {
        return new NativeAvroBytesSchema(nativeSchema);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AvroSchema.java`
#### Snippet
```java

    @Override
    public Schema<T> clone() {
        if (isCustomReaderAndWriter) {
            return new AvroSchema<>(reader, writer, schemaInfo);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoProduceBytesSchema.java`
#### Snippet
```java

    @Override
    public Schema<byte[]> clone() {
        return new AutoProduceBytesSchema<>(schema.clone());
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
    }

    public Schema<GenericRecord> clone() {
        AutoConsumeSchema schema = new AutoConsumeSchema();
        schema.configureSchemaInfo(topicName, componentName, null);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroSchema.java`
#### Snippet
```java

    @Override
    public org.apache.pulsar.client.api.Schema<GenericRecord> clone() {
        org.apache.pulsar.client.api.Schema<GenericRecord> schema =
                GenericAvroSchema.of(schemaInfo,
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericJsonSchema.java`
#### Snippet
```java
    }

    public Schema<GenericRecord> clone() {
        Schema<GenericRecord> schema = of(this.schemaInfo,
                ((AbstractMultiVersionGenericReader) this.reader).useProvidedSchemaAsReaderSchema);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaImpl.java`
#### Snippet
```java

    @Override
    public Schema<KeyValue<K, V>> clone() {
        return KeyValueSchemaImpl.of(keySchema.clone(), valueSchema.clone(), keyValueEncodingType);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaInfo.java`
#### Snippet
```java

        @Override
        public Schema<SchemaInfo> clone() {
            return this;
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/ClusterDataImpl.java`
#### Snippet
```java

    @Override
    public ClusterDataImplBuilder clone() {
        return builder()
                .serviceUrl(serviceUrl)
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/SerDeSchema.java`
#### Snippet
```java

    @Override
    public Schema<T> clone() {
        return this;
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/PulsarAdminBuilderImpl.java`
#### Snippet
```java

    @Override
    public PulsarAdminBuilder clone() {
        return new PulsarAdminBuilderImpl(conf.clone());
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new PreconditionFailedException(getCause(), getHttpError(), getStatusCode());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java
     * @return a new PulsarAdminException
     */
    protected PulsarAdminException clone() {
        return new PulsarAdminException(getMessage(), getCause(), httpError, statusCode);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new ConnectException(getMessage(), getCause());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new NotFoundException(getCause(), getHttpError(), getStatusCode());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new ConflictException(getCause(), getHttpError(), getStatusCode());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new GettingAuthenticationDataException(getCause());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new ServerSideErrorException(getCause(), getMessage(), getHttpError(), getStatusCode());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new TimeoutException(getCause());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new NotAllowedException(getCause(), getHttpError(), getStatusCode());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new NotAuthorizedException(getCause(), getHttpError(), getStatusCode());
        }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/PulsarAdminException.java`
#### Snippet
```java

        @Override
        protected PulsarAdminException clone() {
            return new HttpErrorException(getCause());
        }
```

## RuleId[ruleID=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
                Class<?> clazz = Class.forName(config.getResourceUsageTransportClassName());
                Constructor<?> ctor = clazz.getConstructor(PulsarService.class);
                Object object = ctor.newInstance(new Object[]{this});
                this.resourceUsageTransportManager = (ResourceUsageTopicTransportManager) object;
            }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
                    conscryptClazz
                            .getMethod("setDefaultHostnameVerifier",
                                    new Class[]{Class.forName("org.conscrypt.ConscryptHostnameVerifier")});
            setDefaultHostnameVerifierMethod.invoke(null, wrappedHostnameVerifier);
        } catch (Exception e) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
                final String ansiLog = new AttributedStringBuilder()
                        .style(LOG_STYLE)
                        .append(String.format("[%d/%d] %s %s", new Object[]{index++, commandsInfo.totalCommands,
                                icon, executedCommand.command}))
                        .toAnsi();
                output(ansiLog, terminal);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
            final String ansiLog = new AttributedStringBuilder()
                    .style(LOG_STYLE)
                    .append(String.format("[%d/%d] Executing %s", new Object[]{index,
                            commandsInfo.totalCommands, commandsInfo.executingCommand}))
                    .toAnsi();
            output(ansiLog, terminal);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
                        finalCommandsInfo.executingCommand = command;
                    } else {
                        output(String.format("[%d/%d] Executing %s", new Object[]{index,
                                lines.size(), command}), terminal);
                    }
                    return words;
```

## RuleId[ruleID=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
        switch (componentType) {
            case FUNCTION:
                component = "function";
                break;
            case SOURCE:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `pulsar-client-api/src/main/java/org/apache/pulsar/common/schema/SchemaType.java`
#### Snippet
```java
    public static SchemaType valueOf(int value) {
        switch (value) {
          case 0: return NONE;
          case 1: return STRING;
          case 2: return JSON;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/util/TransactionUtil.java`
#### Snippet
```java
                return newStatus == COMMITTED;
            case ABORTING:
                return newStatus == ABORTING || newStatus == ABORTED;
            case ABORTED:
                return newStatus == ABORTED;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/util/TransactionUtil.java`
#### Snippet
```java
                return newStatus == ABORTING || newStatus == ABORTED;
            case ABORTED:
                return newStatus == ABORTED;
            default:
                throw new IllegalArgumentException("Unknown txn status : " + newStatus);
```

## RuleId[ruleID=RegExpSimplifiable]
### RegExpSimplifiable
`[\\p{C}]` can be simplified to '\\p{C}'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
    private ObjectMapper sortedObjectMapper;
    private List<String> primaryFields = null;
    private final Pattern nonPrintableCharactersPattern = Pattern.compile("[\\p{C}]");
    private final Base64.Encoder base64Encoder = Base64.getEncoder().withoutPadding();

```

### RegExpSimplifiable
`{1}` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/InetAddressUtils.java`
#### Snippet
```java

    private static final Pattern IPV4_MAPPED_IPV6_PATTERN = // TODO does not allow for redundant leading zeros
            Pattern.compile("^::[fF]{4}:" + IPV4_BASIC_PATTERN_STRING + "$");

    private static final Pattern IPV6_STD_PATTERN =
```

### RegExpSimplifiable
`{1}` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/InetAddressUtils.java`
#### Snippet
```java

    private static final Pattern IPV4_PATTERN =
        Pattern.compile("^" + IPV4_BASIC_PATTERN_STRING + "$");

    private static final Pattern IPV4_MAPPED_IPV6_PATTERN = // TODO does not allow for redundant leading zeros
```

### RegExpSimplifiable
`{1}` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/InetAddressUtils.java`
#### Snippet
```java

    private static final String IPV4_BASIC_PATTERN_STRING =
            "(([1-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){1}" + // initial first field, 1-255
            "(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){2}" + // following 2 fields, 0-255 followed by .
             "([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])"; // final field, 0-255
```

## RuleId[ruleID=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `Paths.get(data)`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderBasic.java`
#### Snippet
```java
        if (data.startsWith("data:") || data.startsWith("file:")) {
            return IOUtils.toByteArray(URL.createURL(data));
        } else if (Files.exists(Paths.get(data))) {
            return Files.readAllBytes(Paths.get(data));
        } else if (org.apache.commons.codec.binary.Base64.isBase64(data)) {
```

### DuplicateExpressions
Multiple occurrences of `Paths.get(data)`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderBasic.java`
#### Snippet
```java
            return IOUtils.toByteArray(URL.createURL(data));
        } else if (Files.exists(Paths.get(data))) {
            return Files.readAllBytes(Paths.get(data));
        } else if (org.apache.commons.codec.binary.Base64.isBase64(data)) {
            return Base64.getDecoder().decode(data);
```

### DuplicateExpressions
Multiple occurrences of `Paths.get(keyConfUrl)`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/utils/AuthTokenUtils.java`
#### Snippet
```java
                throw new IOException(e);
            }
        } else if (Files.exists(Paths.get(keyConfUrl))) {
            // Assume the key content was passed in a valid file path
            return Files.readAllBytes(Paths.get(keyConfUrl));
```

### DuplicateExpressions
Multiple occurrences of `Paths.get(keyConfUrl)`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/utils/AuthTokenUtils.java`
#### Snippet
```java
        } else if (Files.exists(Paths.get(keyConfUrl))) {
            // Assume the key content was passed in a valid file path
            return Files.readAllBytes(Paths.get(keyConfUrl));
        } else if (Base64.isBase64(keyConfUrl.getBytes())) {
            // Assume the key content was passed in base64
```

### DuplicateExpressions
Multiple occurrences of `Paths.get(secretConfUrl)`
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderSasl.java`
#### Snippet
```java
            URI filePath = URI.create(secretConfUrl);
            return Files.readAllBytes(Paths.get(filePath));
        } else if (Files.exists(Paths.get(secretConfUrl))) {
            // Assume the key content was passed in a valid file path
            return Files.readAllBytes(Paths.get(secretConfUrl));
```

### DuplicateExpressions
Multiple occurrences of `Paths.get(secretConfUrl)`
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderSasl.java`
#### Snippet
```java
        } else if (Files.exists(Paths.get(secretConfUrl))) {
            // Assume the key content was passed in a valid file path
            return Files.readAllBytes(Paths.get(secretConfUrl));
        } else {
            String msg = "Role token signer secret file " + secretConfUrl + " doesn't exist";
```

## RuleId[ruleID=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageIdImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder()
          .append(ledgerId)
          .append(':')
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java

    private String buildError(long requestId, String errorMsg) {
        return new StringBuilder().append("{\"errorMsg\":\"").append(errorMsg)
            .append("\",\"reqId\":").append(requestId)
            .append(", \"remote\":\"").append(remoteAddress)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageIdImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder()
          .append(ledgerId)
          .append(':')
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `pulsar-common/src/main/java/org/apache/pulsar/common/api/raw/RawMessageIdImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder()
                .append('(')
                .append(ledgerId)
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java

    private String getKeyName(String property) {
        StringBuilder sb = new StringBuilder();
        sb.append(OFFLOADER_PROPERTY_PREFIX)
          .append(StringUtils.capitalize(property));
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
        switch (getProvider()) {
            case AWS_S3:
                return new StringBuilder().append("s3ManagedLedgerOffload")
                                          .append(StringUtils.capitalize(property))
                                          .toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java

            case GOOGLE_CLOUD_STORAGE:
                return new StringBuilder().append("gcsManagedLedgerOffload")
                                          .append(StringUtils.capitalize(property))
                                          .toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `pulsar-client-api/src/main/java/org/apache/pulsar/common/schema/KeyValue.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("(key = \"")
          .append(key)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java
                logger = loggers.compute(event,
                        (k, v) -> LoggerFactory.getLogger(
                                new StringBuilder("stevlog.").append(component)
                                .append(".").append(event).toString()));
            }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java
                logger = loggers.compute(event,
                        (k, v) -> LoggerFactory.getLogger(
                                new StringBuilder("stevlog.").append(event).toString()));
            }
        }
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/SaslRoleToken.java`
#### Snippet
```java
     */
    private void generateToken() {
        StringBuilder sb = new StringBuilder();
        sb.append(USER_ROLE).append("=").append(getUserRole()).append(ATTR_SEPARATOR);
        sb.append(SESSION).append("=").append(getSession()).append(ATTR_SEPARATOR);
```

## RuleId[ruleID=ReadWriteStringCanBeUsed]
### ReadWriteStringCanBeUsed
Can be replaced with 'Files.readString()'
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
                token = r.readLine();
            } else if (tokenFile != null) {
                token = new String(Files.readAllBytes(Paths.get(tokenFile)), StandardCharsets.UTF_8);
            } else if (System.getenv("TOKEN") != null) {
                token = System.getenv("TOKEN");
```

### ReadWriteStringCanBeUsed
Can be replaced with 'Files.readString()'
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
                token = r.readLine();
            } else if (tokenFile != null) {
                token = new String(Files.readAllBytes(Paths.get(tokenFile)), StandardCharsets.UTF_8);
            } else if (System.getenv("TOKEN") != null) {
                token = System.getenv("TOKEN");
```

### ReadWriteStringCanBeUsed
Can be replaced with 'Files.readString()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LinuxInfoUtils.java`
#### Snippet
```java

    private static String readTrimStringFromFile(Path path) throws IOException {
        return new String(Files.readAllBytes(path), StandardCharsets.UTF_8).trim();
    }

```

### ReadWriteStringCanBeUsed
Can be replaced with 'Files.readString()'
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
            // here escaping the default payload delimiter to correct value
            String delimiter = arguments.payloadDelimiter.equals("\\n") ? "\n" : arguments.payloadDelimiter;
            String[] payloadList = new String(Files.readAllBytes(payloadFilePath), StandardCharsets.UTF_8)
                    .split(delimiter);
            log.info("Reading payloads from {} and {} records read", payloadFilePath.toAbsolutePath(),
```

### ReadWriteStringCanBeUsed
Can be replaced with 'Files.readString()'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            // here escaping the default payload delimiter to correct value
            String delimiter = arguments.payloadDelimiter.equals("\\n") ? "\n" : arguments.payloadDelimiter;
            String[] payloadList = new String(Files.readAllBytes(payloadFilePath),
                    StandardCharsets.UTF_8).split(delimiter);
            log.info("Reading payloads from {} and {} records read", payloadFilePath.toAbsolutePath(),
```

### ReadWriteStringCanBeUsed
Can be replaced with 'Files.readString()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
                    return false;
                }
                value = new String(Files.readAllBytes(f.toPath()), StandardCharsets.UTF_8);
            } else if (url != null) {
                final ByteArrayOutputStream bout = new ByteArrayOutputStream();
```

### ReadWriteStringCanBeUsed
Can be replaced with 'Files.readString()'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
        if (configFile != null) {
            final String defaultConfigValue =
                    new String(Files.readAllBytes(new File(configFile).toPath()), StandardCharsets.UTF_8);
            defaultConfig = new ConfigStore.ConfigEntry(ConfigStore.DEFAULT_CONFIG, defaultConfigValue);
        }
```

## RuleId[ruleID=NonShortCircuitBoolean]
### NonShortCircuitBoolean
Non-short-circuit boolean expression `this.isReplicated |= stats.isReplicated`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/SubscriptionStatsImpl.java`
#### Snippet
```java
        this.msgRateExpired += stats.msgRateExpired;
        this.totalMsgExpired += stats.totalMsgExpired;
        this.isReplicated |= stats.isReplicated;
        this.isDurable |= stats.isDurable;
        if (this.consumers.size() != stats.consumers.size()) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `this.isDurable |= stats.isDurable`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/SubscriptionStatsImpl.java`
#### Snippet
```java
        this.totalMsgExpired += stats.totalMsgExpired;
        this.isReplicated |= stats.isReplicated;
        this.isDurable |= stats.isDurable;
        if (this.consumers.size() != stats.consumers.size()) {
            for (int i = 0; i < stats.consumers.size(); i++) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `this.allowOutOfOrderDelivery |= stats.allowOutOfOrderDelivery`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/SubscriptionStatsImpl.java`
#### Snippet
```java
            }
        }
        this.allowOutOfOrderDelivery |= stats.allowOutOfOrderDelivery;
        this.consumersAfterMarkDeletePosition.putAll(stats.consumersAfterMarkDeletePosition);
        this.nonContiguousDeletedMessagesRanges += stats.nonContiguousDeletedMessagesRanges;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `this.connected &= stats.connected`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/ReplicatorStatsImpl.java`
#### Snippet
```java
        this.msgRateExpired += stats.msgRateExpired;
        this.replicationBacklog += stats.replicationBacklog;
        this.connected &= stats.connected;
        this.replicationDelayInSeconds = Math.max(this.replicationDelayInSeconds, stats.replicationDelayInSeconds);
        return this;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `t instanceof ExecutionException | t instanceof CompletionException`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/transaction/TransactionCoordinatorClientException.java`
#### Snippet
```java
        } else if (t instanceof InvalidTxnStatusException) {
            return (InvalidTxnStatusException) t;
        } else if (t instanceof ExecutionException | t instanceof CompletionException) {
            // Generic exception
            return unwrap(t.getCause());
```

## RuleId[ruleID=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/Application.java`
#### Snippet
```java
        } catch (InterruptedException e) {
            logger.info("Interrupted while trying to handle configuration event");
            return;
        } finally {
            // If interrupted while trying to lock, we don't own the lock, so must not attempt to unlock
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
                    log.error("No offsets to commit!");
                    flushFuture.completeExceptionally(new Exception("No Offsets Added Error"));
                    return;
                }
            }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/StatsOutputStream.java`
#### Snippet
```java
    private void checkSeparator() {
        if (separators.isEmpty()) {
            return;
        } else if (separators.peek() == Boolean.TRUE) {
            write(",");
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java
        } catch (Throwable t) {
            log.warn("Failed to process replicated subscription snapshot at {} -- {}", pos, t.getMessage(), t);
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarMetadataEventSynchronizer.java`
#### Snippet
```java
        if (!StringUtils.isNotBlank(topicName)) {
            log.info("Metadata synchronizer is disabled");
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/AlwaysSchemaValidator.java`
#### Snippet
```java
    @Override
    public void validate(Schema toValidate, Iterable<Schema> existing) {
        return;
    }
}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                  () -> this.enableCnxAutoRead());
                log.info("Using resource group {} rate limiter for topic {}", rgName, topic);
                return;
            }
        } else {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
                    if (!schemaFuture.isCompletedExceptionally()) {
                        list.add(schemaFuture.getNow(null));
                        return;
                    }
                });
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                "Internal topics have not been generated yet"));
                        return;
                    } else {
                        asyncResponse.resume(new RestException(pae));
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                    } else {
                        asyncResponse.resume(new RestException(pae));
                        return;
                    }
                } else {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                } else {
                    asyncResponse.resume(new RestException(ex));
                    return;
                }
            } else {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        resumeAsyncResponseExceptionally(asyncResponse,
                                new RestException(Status.CONFLICT, e.getMessage()));
                        return;
                    } catch (Exception e) {
                        log.warn("Unexpected error triggering offload", e);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        log.warn("Unexpected error triggering offload", e);
                        resumeAsyncResponseExceptionally(asyncResponse, new RestException(e));
                        return;
                    }
                }).exceptionally(ex -> {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        resumeAsyncResponseExceptionally(asyncResponse,
                                new RestException(Status.CONFLICT, e.getMessage()));
                        return;
                    } catch (Exception e) {
                        log.error("[{}] Failed to trigger compaction on topic {}", clientAppId(),
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                topicName, e);
                        resumeAsyncResponseExceptionally(asyncResponse, new RestException(e));
                        return;
                    }
                }).exceptionally(ex -> {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            + " succeeded on other partitions, please check stats of individual partition."
                    ));
                    return;
                }
            }).exceptionally(e -> {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    log.warn("[{}] {} {}({}) {}", clientAppId(), msg, topicName, messageId, subName);
                                    asyncResponse.resume(new RestException(Status.METHOD_NOT_ALLOWED, msg));
                                    return;
                                } else if (messageId.getPartitionIndex() != topicName.getPartitionIndex()) {
                                    String msg = "Invalid parameter for expire message by position, partition index of "
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                    log.warn("[{}] {} {}({}).", clientAppId(), msg, topicName, messageId);
                                    asyncResponse.resume(new RestException(Status.PRECONDITION_FAILED, msg));
                                    return;
                                } else {
                                    internalExpireMessagesNonPartitionedTopicByPosition(asyncResponse, subName,
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                asyncResponse.resume(new RestException(Status.METHOD_NOT_ALLOWED,
                        "Reset-cursor at position is not allowed for partitioned-topic"));
                return;
            } else {
                validateTopicOwnershipAsync(topicName, authoritative)
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentReplicator.java`
#### Snippet
```java
            STATE_UPDATER.set(this, State.Stopping);
            closeProducerAsync();
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        } else if (System.nanoTime() - lastActive < TimeUnit.SECONDS.toNanos(maxInactiveDurationInSec)) {
            // Gc interval did not expire yet
            return;
        } else if (shouldTopicBeRetained()) {
            // Topic activity is still within the retention period
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        } else if (shouldTopicBeRetained()) {
            // Topic activity is still within the retention period
            return;
        } else {
            CompletableFuture<Void> replCloseFuture = new CompletableFuture<>();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                                    urlTls == null ? null : urlTls.toString())));
                        }
                        return;
                    } else {
                        future.complete(Optional.of(new LookupResult(nsData.get())));
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                                        new PulsarServerException("the broker do not have "
                                                + options.getAdvertisedListenerName() + " listener"));
                                return;
                            } else {
                                URI url = listener.getBrokerServiceUrl();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                                                url == null ? null : url.toString(),
                                                urlTls == null ? null : urlTls.toString())));
                                return;
                            }
                        } else {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                        } else {
                            lookupFuture.complete(Optional.of(new LookupResult(ownerInfo)));
                            return;
                        }
                    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        }
        if (recoverTime.getRecoverStartTime() == 0L) {
            return;
        } else {
            recoverTime.setRecoverEndTime(System.currentTimeMillis());
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java
            log.error("{} Error mutual verify: {}", ctx.channel(), e);
            connectionFuture.completeExceptionally(e);
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                        subscribeFuture.completeExceptionally(error);
                    }
                    return;
                });
            });
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                                + " allTopicPartitionsNumber: {}",
                    topic, subscription, topicName, numPartitions, allTopicPartitionsNumber.get());
                return;
            })
            .exceptionally(ex -> {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LongHierarchicalLedgerManager.java`
#### Snippet
```java
            if ((level == 0) && !isLedgerParentNode(lNode)) {
                cb.processResult(successRc, null, context);
                return;
            } else if (level < 3) {
                asyncProcessLevelNodes(nodePath,
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                this.offloaderStats.recordOffloadError(topicName);
                promise.completeExceptionally(t);
                return;
            }
        });
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
            blobStore.abortMultipartUpload(streamingMpu);
            offloadResult.completeExceptionally(e);
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
            if (!store.exists(lostBookieRecoveryDelayPath).get()) {
                cb.operationComplete(0, null);
                return;
            }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
                        + "since DISABLE_NODE node is deleted");
                cb.operationComplete(0, null);
                return;
            }
        } catch (ExecutionException ee) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
        if (assignedWorkerId.equals(workerId)) {
            stopFunction(FunctionCommon.getFullyQualifiedInstanceId(assignment.getInstance()), true);
            return;
        } else {
            // query other worker
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
            }
        }
        return;
    }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                        DEFAULT_LEDGER_DELETE_BACKOFF_TIME_SEC, TimeUnit.SECONDS);
                }
                return;
            } else {
                log.info("[{}][{}] Successfully closed & deleted ledger {} in cursor", ledger.getName(), name,
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                                log.error("[{}] Failed to open ledger {}: {}", name, id, BKException.getMessage(rc));
                                callback.initializeFailed(createManagedLedgerException(rc));
                                return;
                            }
                        }));
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            if (recycle(reOpCount)) {
                callback.readEntriesComplete(returnedEntries, cbCtx);
                return;
            } else {
                if (log.isDebugEnabled()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                }
                returnedEntries.forEach(Entry::release);
                return;
            }
        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            if (recycle(reOpCount)) {
                callback.readEntriesFailed(exception, cbCtx);
                return;
            } else {
                if (log.isDebugEnabled()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                    log.debug("[{}] read entry already completed for {}-{}", name, ledgerId, entryId);
                }
                return;
            }
        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            if (recycle(reOpCount)) {
                callback.readEntryComplete(entry, cbCtx);
                return;
            } else {
                if (log.isDebugEnabled()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                }
                entry.release();
                return;
            }
        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            if (recycle(reOpCount)) {
                callback.readEntryFailed(exception, cbCtx);
                return;
            } else {
                if (log.isDebugEnabled()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/AuthenticationFilter.java`
#### Snippet
```java
                LOG.error("[{}] Error performing authentication for HTTP", request.getRemoteAddr(), e);
            }
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
                log.warn("Failed to perform http request", throwable);
                authFuture.completeExceptionally(new PulsarClientException(throwable));
                return;
            }
        });
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
                    authFuture.completeExceptionally(new PulsarClientException("Sasl Auth request failed: "
                            + response.getStatus()));
                    return;
                } else {
                    if (response.getHeaderString(SASL_AUTH_ROLE_TOKEN) != null) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
                    }
                    authFuture.complete(getHeaders(response));
                    return;
                }
            }
```

## RuleId[ruleID=TextBlockMigration]
### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSinkConfig.java`
#### Snippet
```java
        required = false,
        defaultValue = "ONLY_RAW_PAYLOAD",
        help = "Message format in which kinesis sink converts pulsar messages and publishes to kinesis streams.\n"
            + "  #\n"
            + "  # The available messages formats are: \n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java
        required = false,
        defaultValue = "LATEST",
        help = "Used to specify the position in the stream where the connector should start from.\n"
                + "  #\n"
                + "  # The available options are: \n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java
        required = false,
        defaultValue = "LATEST",
        help = "Used to specify the position in the stream where the connector should start from.\n"
                + "  #\n"
                + "  # The available options are: \n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaConnectSinkConfig.java`
#### Snippet
```java
            defaultValue = "false",
            help = "Some connectors cannot handle pulsar topic names like persistent://a/b/topic"
                    + " and do not sanitize the topic name themselves. \n"
                    + "If enabled, all non alpha-digital characters in topic name will be replaced with underscores. \n"
                    + "In some cases it may result in topic name collisions (topic_a and topic.a will become the same)")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            minValue = 1,
            category = CATEGORY_HTTP,
            doc = "Http proxy timeout.\n\n"
                    + "The timeout value for HTTP proxy is in millisecond."
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_SASL_AUTH,
            doc = "Path to file containing the secret to be used to SaslRoleTokenSigner\n"
                    + "The secret can be specified like:\n"
                    + "saslJaasServerRoleTokenSignerSecretPath=file:///my/saslRoleTokenSignerSecret.key."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_TLS,
        doc = "Accept untrusted TLS certificate from client.\n\n"
            + "If true, a client with a cert which cannot be verified with the `tlsTrustCertsFilePath`"
            + " cert will be allowed to connect to the server, though the cert will not be used for"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            category = CATEGORY_BROKER_DISCOVERY,
            required = false,
            doc = "The metadata store URL. \n"
                    + " Examples: \n"
                    + "  * zk:my-zk-1:2181,my-zk-2:2181,my-zk-3:2181\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        category = CATEGORY_TLS,
        doc = "Specify the tls protocols the broker will use to negotiate during TLS handshake"
            + " (a comma-separated list of protocol names).\n\n"
            + "Examples:- [TLSv1.3, TLSv1.2]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
            doc = "Specify the TLS provider for the broker service: \n"
                    + "When using TLS authentication with CACert, the valid value is either OPENSSL or JDK.\n"
                    + "When using TLS authentication with KeyStore, available values can be SunJSSE, Conscrypt and etc."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        category = CATEGORY_TLS,
        doc = "Specify the tls cipher the proxy will use to negotiate during TLS Handshake"
            + " (a comma-separated list of ciphers).\n\n"
            + "Examples:- [TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_TLS,
        doc = "Whether client certificates are required for TLS.\n\n"
            + " Connections are rejected if the client certificate isn't trusted"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_TLS,
            doc = "Specify the tls cipher the proxy's web service will use to negotiate during TLS Handshake.\n\n"
                    + "Example:- [TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            category = CATEGORY_KEYSTORE_TLS,
            doc = "Specify the tls cipher the proxy will use to negotiate during TLS Handshake"
                  + " (a comma-separated list of ciphers).\n\n"
                  + "Examples:- [TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256].\n"
                  + " used by the Pulsar proxy to authenticate with Pulsar brokers"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_TLS,
            doc = "Specify the tls protocols the proxy's web service will use to negotiate during TLS Handshake.\n\n"
                    + "Example:- [TLSv1.3, TLSv1.2]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            category = CATEGORY_KEYSTORE_TLS,
            doc = "Specify the tls protocols the broker will use to negotiate during TLS handshake"
                  + " (a comma-separated list of protocol names).\n\n"
                  + "Examples:- [TLSv1.3, TLSv1.2] \n"
                  + " used by the Pulsar proxy to authenticate with Pulsar brokers"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                doc = @FieldContext(
                    category = CATEGORY_TOKEN_AUTH,
                    doc = "Asymmetric public/private key pair.\n\n"
                        + "Configure the public key to be used to validate auth tokens"
                        + " The key can be specified like:\n\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                doc = @FieldContext(
                    category = CATEGORY_TOKEN_AUTH,
                    doc = "Symmetric key.\n\n"
                        + "Configure the secret key to be used to validate auth tokens"
                        + "The key can be specified like:\n\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        minValue = 1,
        category = CATEGORY_HTTP,
        doc = "Http output buffer size.\n\n"
            + "The amount of data that will be buffered for http requests "
            + "before it is flushed to the channel. A larger buffer size may "
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_TLS,
        doc = "Path for the trusted TLS certificate file.\n\n"
            + "This cert is used to verify that any certs presented by connecting clients"
            + " are signed by a certificate authority. If this verification fails, then the"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            minValue = 1,
            category = CATEGORY_HTTP,
            doc = "Http input buffer max size.\n\n"
                    + "The maximum amount of data that will be buffered for incoming http requests "
                    + "so that the request body can be replayed when the backend broker "
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value =
                    "You can submit a source (in any languages that you are familiar with) to a Pulsar cluster. "
                            + "Follow the steps below.\n"
                            + "1. Create a JSON object using some of the following parameters.\n"
                            + "A JSON value presenting configuration payload of a Pulsar Source."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(
                    value = "A JSON value presenting configuration payload of a Pulsar Source."
                            + " An example of the expected functions can be found here.\n"
                            + "- **classname**\n"
                            + "  The class name of a Pulsar Source if archive is file-url-path (file://).\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(
                    value = "A JSON value presenting configuration payload of a Pulsar Function."
                            + " An example of the expected Pulsar Function can be found here.\n"
                            + "- **autoAck**\n"
                            + "  Whether or not the framework acknowledges messages automatically.\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @FormDataParam("url") String functionPkgUrl,
            @ApiParam(
                    value = "You can submit a function (in any languages that you are familiar with) \n"
                            + "to a Pulsar cluster. Follow the steps below. \n"
                            + "1. Create a JSON object using some of the following parameters.\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                             @ApiParam(value =
                                     "You can submit a sink (in any languages that you are familiar with) "
                                             + "to a Pulsar cluster. Follow the steps below.\n"
                                             + "1. Create a JSON object using some of the following parameters.\n"
                                             + "A JSON value presenting config payload of a Pulsar Sink."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                           @ApiParam(value =
                                   "A JSON value presenting config payload of a Pulsar Sink."
                                           + " All available configuration options are:\n"
                                           + "- **classname**\n"
                                           + "   The class name of a Pulsar Sink if"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactorTool.java`
#### Snippet
```java
        if (isBlank(brokerConfig.getMetadataStoreUrl())) {
            throw new IllegalArgumentException(
                    String.format("Need to specify `metadataStoreUrl` or `zookeeperServers` in configuration file \n"
                                    + "or specify configuration file path from command line.\n"
                                    + "now configuration file path is=[%s]\n",
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_WORKER_SECURITY,
            doc = "Specify the tls protocols the proxy's web service will use to negotiate during TLS Handshake.\n\n"
                    + "Example:- [TLSv1.3, TLSv1.2]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_WORKER_SECURITY,
            doc = "Path to file containing the secret to be used to SaslRoleTokenSigner\n"
                    + "The secret can be specified like:\n"
                    + "saslJaasServerRoleTokenSignerSecretPath=file:///my/saslRoleTokenSignerSecret.key."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_FUNC_RUNTIME_MNG,
        doc = "Any config the secret provider configurator might need. \n\nThis is passed on"
            + " to the init method of the SecretsProviderConfigurator"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_WORKER_SECURITY,
            doc = "Specify the tls cipher the proxy's web service will use to negotiate during TLS Handshake.\n\n"
                    + "Example:- [TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
            doc = "Specify the TLS provider for the function worker service: \n"
                    + "When using TLS authentication with CACert, the valid value is either OPENSSL or JDK.\n"
                    + "When using TLS authentication with KeyStore, available values can be SunJSSE, Conscrypt and etc."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java

    @FieldContext(
            doc = "Specify the tls cipher the proxy's web service will use to negotiate during TLS Handshake.\n\n"
                    + "Example:- [TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java

    @FieldContext(
            doc = "Specify the tls protocols the proxy's web service will use to negotiate during TLS Handshake.\n\n"
                    + "Example:- [TLSv1.3, TLSv1.2]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
    // JCommander arguments for accepting user input.
    private static class ShellArguments {
        @Parameter(description = "Command arguments:\n" + "trade tenant namespace topic\n"
                + "change tenant namespace topic\n" + "stop tenant namespace topic\n"
                + "trade_group tenant group_name num_namespaces\n" + "change_group tenant group_name\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java

            final String welcomeMessage =
                    String.format("Welcome to Pulsar shell!\n  %s: %s\n  %s: %s\n\n"
                                    + "Type %s to get started or try the autocompletion (TAB button).\n"
                                    + "Type %s or %s to end the shell session.\n",
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_METRICS,
        doc = "Enable expose the precise backlog stats.\n"
                + " Set false to use published counter and consumed counter to calculate,\n"
                + " this would be more efficient but may be inaccurate. Default is false."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_STORAGE_ML,
        dynamic = true,
        doc = "Amount of memory to use for caching data payload in managed ledger. \n\nThis"
            + " memory is allocated from JVM direct memory and it's shared across all the topics"
            + " running in the same broker. By default, uses 1/5th of available direct memory")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_POLICIES,
        doc = "Default backlog quota retention policy. Default is producer_request_hold\n\n"
            + "'producer_request_hold' Policy which holds producer's send request until the"
            + "resource becomes available (or holding times out)\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            category = CATEGORY_KEYSTORE_TLS,
            doc = "Specify the tls protocols the broker will use to negotiate during TLS handshake"
                  + " (a comma-separated list of protocol names).\n\n"
                  + "Examples:- [TLSv1.3, TLSv1.2] \n"
                  + " used by the internal client to authenticate with Pulsar brokers"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_POLICIES,
        doc = "Max number of unacknowledged messages allowed per broker. \n\n"
            + " Once this limit reaches, broker will stop dispatching messages to all shared subscription "
            + " which has higher number of unack messages until subscriptions start acknowledging messages "
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        dynamic = true,
        category = CATEGORY_POLICIES,
        doc = "Default number of message-bytes dispatching throttling-limit for every replicator in replication. \n\n"
            + "Using a value of 0, is disabling replication message-byte dispatch-throttling")
    private long dispatchThrottlingRatePerReplicatorInByte = 0;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_AUTHORIZATION,
        doc = "Role names that are treated as `proxy roles`. \n\nIf the broker sees"
            + " a request with role as proxyRoles - it will demand to see the original"
            + " client role or certificate.")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            category = CATEGORY_SERVER,
            required = false,
            doc = "The metadata store URL. \n"
                    + " Examples: \n"
                    + "  * zk:my-zk-1:2181,my-zk-2:2181,my-zk-3:2181\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_STORAGE_ML,
        doc = "Default type of checksum to use when writing to BookKeeper. \n\nDefault is `CRC32C`."
            + " Other possible options are `CRC32`, `MAC` or `DUMMY` (no checksum)."
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_SERVER,
        doc = "Max number of consumers with the same IP address allowed to connect to topic."
            + " \n\nOnce this limit reaches, Broker will reject new consumers until the number of"
            + " connected consumers with the same IP address decrease."
            + " Using a value of 0, is disabling maxSameAddressConsumersPerTopic-limit check.")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            category = CATEGORY_POLICIES,
            dynamic = true,
            doc = "Metadata of inactive partitioned topic will not be automatically cleaned up by default.\n"
            + "Note: If `allowAutoTopicCreation` and this option are enabled at the same time,\n"
            + "it may appear that a partitioned topic has just been deleted but is automatically created as a "
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
        doc = "Minimum number of racks per write quorum. \n\nBK rack-aware bookie selection policy will try to"
            + " get bookies from at least 'bookkeeperClientMinNumRacksPerWriteQuorum' racks for a write quorum.")
    private int bookkeeperClientMinNumRacksPerWriteQuorum = 2;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_TLS,
        doc = "Specify the tls protocols the broker will use to negotiate during TLS Handshake.\n\n"
            + "Example:- [TLSv1.3, TLSv1.2]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_STORAGE_BK,
        doc = "Enforces rack-aware bookie selection policy to pick bookies from "
            + "'bookkeeperClientMinNumRacksPerWriteQuorum' racks for  a writeQuorum. \n\n"
            + "If BK can't find bookie then it would throw BKNotEnoughBookiesException instead of picking random one.")
    private boolean bookkeeperClientEnforceMinNumRacksPerWriteQuorum = false;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
        doc = "Enable rack-aware bookie selection policy. \n\nBK will chose bookies from"
            + " different racks when forming a new bookie ensemble")
    private boolean bookkeeperClientRackawarePolicyEnabled = true;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_SERVER,
        dynamic = true,
        doc = "Max memory size for broker handling messages sending from producers.\n\n"
            + " If the processing message size exceed this value, broker will stop read data"
            + " from the connection. The processing messages means messages are sends to broker"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        dynamic = true,
        category = CATEGORY_POLICIES,
        doc = "Default number of message dispatching throttling-limit for every replicator in replication. \n\n"
            + "Using a value of 0, is disabling replication message dispatch-throttling")
    private int dispatchThrottlingRatePerReplicatorInMsg = 0;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_LOAD_BALANCER,
        dynamic = true,
        doc = "Load shedding interval. \n\nBroker periodically checks whether some traffic"
            + " should be offload from some over-loaded broker to other under-loaded brokers"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        dynamic = true,
        category = CATEGORY_POLICIES,
        doc = "Default number of message-bytes dispatching throttling-limit for a subscription. \n\n"
            + "Using a value of 0, is disabling default message-byte dispatch-throttling.")
    private long dispatchThrottlingRatePerSubscriptionInByte = 0;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_SERVER,
        doc = "Max number of producers with the same IP address allowed to connect to topic."
            + " \n\nOnce this limit reaches, Broker will reject new producers until the number of"
            + " connected producers with the same IP address decrease."
            + " Using a value of 0, is disabling maxSameAddressProducersPerTopic-limit check.")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_SERVER,
        doc = "Max number of subscriptions allowed to subscribe to topic. \n\nOnce this limit reaches, "
                + " broker will reject new subscription until the number of subscribed subscriptions decrease.\n"
                + " Using a value of 0, is disabling maxSubscriptionsPerTopic limit check."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_POLICIES,
        doc = "Set the default behavior for message deduplication in the broker.\n\n"
            + "This can be overridden per-namespace. If enabled, broker will reject"
            + " messages that were already stored in the topic"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
        doc = "Enable bookies health check. \n\n Bookies that have more than the configured"
            + " number of failure within the interval will be quarantined for some time."
            + " During this period, new ledgers won't be created on these bookies")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_STORAGE_ML,
        doc = "Max number of entries to append to a ledger before triggering a rollover.\n\n"
            + "A ledger rollover is triggered after the min rollover time has passed"
            + " and one of the following conditions is true:"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_POLICIES,
        doc = "Number of entries after which a dedup info snapshot is taken.\n\n"
            + "A bigger interval will lead to less snapshots being taken though it would"
            + " increase the topic recovery time, when the entries published after the"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        doc = "There are two policies to apply when broker metadata session expires: session expired happens, "
        + "\"shutdown\" or \"reconnect\". \n\n"
        + " With \"shutdown\", the broker will be restarted.\n\n"
        + " With \"reconnect\", the broker will keep serving the topics, while attempting to recreate a new session."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_SCHEMA,
        doc = "Enforce schema validation on following cases:\n\n"
            + " - if a producer without a schema attempts to produce to a topic with schema, the producer will be\n"
            + "   failed to connect. PLEASE be carefully on using this, since non-java clients don't support schema.\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(category = CATEGORY_STORAGE_ML,
            doc = "New entries check delay for the cursor under the managed ledger. \n"
                    + "If no new messages in the topic, the cursor will try to check again after the delay time. \n"
                    + "For consumption latency sensitive scenario, can set to a smaller value or set to 0.\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_STORAGE_ML,
        deprecated = true,
        doc = "Max number of `acknowledgment holes` that can be stored in Zookeeper.\n\n"
            + "If number of unack message range is higher than this limit then broker will persist"
            + " unacked ranges into bookkeeper to avoid additional data overhead into zookeeper.\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_POLICIES,
        dynamic = true,
        doc = "Max duration of topic inactivity in seconds, default is not present\n"
        + "If not present, 'brokerDeleteInactiveTopicsFrequencySeconds' will be used\n"
        + "Topics that are inactive for longer than this value will be deleted"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_TLS,
            doc = "Specify the tls cipher the proxy's web service will use to negotiate during TLS Handshake.\n\n"
                    + "Example:- [TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_METRICS,
            doc = "Enable splitting topic and partition label in Prometheus.\n"
                    + " If enabled, a topic name will split into 2 parts, one is topic name without partition index,\n"
                    + " another one is partition index, e.g. (topic=xxx, partition=0).\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
            doc = "Specify the TLS provider for the broker service: \n"
                    + "When using TLS authentication with CACert, the valid value is either OPENSSL or JDK.\n"
                    + "When using TLS authentication with KeyStore, available values can be SunJSSE, Conscrypt and etc."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_SERVER,
        doc = "Max number of consumers allowed to connect to topic. \n\nOnce this limit reaches,"
            + " Broker will reject new consumers until the number of connected consumers decrease."
            + " Using a value of 0, is disabling maxConsumersPerTopic-limit check.")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_METRICS,
        doc = "Time in milliseconds that metrics endpoint would time out. Default is 30s.\n"
                + " Increase it if there are a lot of topics to expose topic-level metrics.\n"
                + " Set it to 0 to disable timeout."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_TLS,
            doc = "Specify the tls protocols the proxy's web service will use to negotiate during TLS Handshake.\n\n"
                    + "Example:- [TLSv1.3, TLSv1.2]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_STORAGE_ML,
        doc = "Max number of `acknowledgment holes` that are going to be persistently stored.\n\n"
            + "When acknowledging out of order, a consumer will leave holes that are supposed"
            + " to be quickly filled by acking all the messages. The information of which"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        dynamic = true,
        category = CATEGORY_POLICIES,
        doc = "Default number of message dispatching throttling-limit for a subscription. \n\n"
            + "Using a value of 0, is disabling default message dispatch-throttling.")
    private int dispatchThrottlingRatePerSubscriptionInMsg = 0;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_AUTHORIZATION,
        doc = "Allow wildcard matching in authorization\n\n"
            + "(wildcard matching only applicable if wildcard-char: * presents at first"
            + " or last position eg: *.pulsar.service, pulsar.service.*)")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_POLICIES,
        doc = "Max number of unacknowledged messages allowed to receive messages by a consumer on"
            + " a shared subscription.\n\n Broker will stop sending messages to consumer once,"
            + " this limit reaches until consumer starts acknowledging messages back and unack count"
            + " reaches to `maxUnackedMessagesPerConsumer/2`. Using a value of 0, it is disabling "
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            category = CATEGORY_AUTHENTICATION,
            dynamic = true,
            doc = "Authentication settings of the broker itself. \n\nUsed when the broker connects"
                    + " to other brokers, either in same or other clusters. "
                    + "Default uses plugin which disables authentication"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_REPLICATION,
        doc = "Max number of connections to open for each broker in a remote cluster.\n\n"
            + "More connections host-to-host lead to better throughput over high-latency links"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_SERVER,
        doc = "Max number of consumers allowed to connect to subscription. \n\nOnce this limit reaches,"
            + " Broker will reject new consumers until the number of connected consumers decrease."
            + " Using a value of 0, is disabling maxConsumersPerSubscription-limit check.")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_STORAGE_BK,
        required = false,
        doc = "Enable bookie isolation by specifying a list of bookie groups to choose from. \n\n"
            + "Any bookie outside the specified groups will not be used by the broker")
    private String bookkeeperClientIsolationGroups;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        dynamic = true,
        category = CATEGORY_STORAGE_ML,
        doc = "Skip reading non-recoverable/unreadable data-ledger under managed-ledger's list.\n\n"
            + " It helps when data-ledgers gets corrupted at bookkeeper and managed-cursor is stuck at that ledger."
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category =  CATEGORY_HTTP,
        doc = "If true, the broker will reject all HTTP requests using the TRACE and TRACK verbs.\n"
        + " This setting may be necessary if the broker is deployed into an environment that uses http port\n"
        + " scanning and flags web servers allowing the TRACE method as insecure."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        dynamic = true,
        category = CATEGORY_POLICIES,
        doc = "Default number of message-bytes dispatching throttling-limit for every topic. \n\n"
            + "Using a value of 0, is disabling default message-byte dispatch-throttling")
    private long dispatchThrottlingRatePerTopicInByte = 0;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_STORAGE_ML,
            doc = "Max number of `acknowledgment holes` that can be stored in MetadataStore.\n\n"
                    + "If number of unack message range is higher than this limit then broker will persist"
                    + " unacked ranges into bookkeeper to avoid additional data overhead into MetadataStore.")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_SASL_AUTH,
            doc = "Path to file containing the secret to be used to SaslRoleTokenSigner\n"
                    + "The secret can be specified like:\n"
                    + "saslJaasServerRoleTokenSignerSecretPath=file:///my/saslRoleTokenSignerSecret.key."
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        dynamic = true,
        category = CATEGORY_POLICIES,
        doc = "Default number of message dispatching throttling-limit for every topic. \n\n"
            + "Using a value of 0, is disabling default message dispatch-throttling")
    private int dispatchThrottlingRatePerTopicInMsg = 0;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_TLS,
        doc = "Specify the tls cipher the broker will use to negotiate during TLS Handshake.\n\n"
            + "Example:- [TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256]"
    )
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_POLICIES,
        dynamic = true,
        doc = "Set the inactive topic delete mode. Default is delete_when_no_subscriptions\n"
        + "'delete_when_no_subscriptions' mode only delete the topic which has no subscriptions and no active "
        + "producers\n"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
        doc = "Enable region-aware bookie selection policy. \n\nBK will chose bookies from"
            + " different regions and racks when forming a new bookie ensemble")
    private boolean bookkeeperClientRegionawarePolicyEnabled = false;
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_SERVER,
        doc = "Max number of producers allowed to connect to topic. \n\nOnce this limit reaches,"
            + " Broker will reject new producers until the number of connected producers decrease."
            + " Using a value of 0, is disabling maxProducersPerTopic-limit check.")
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_POLICIES,
        doc = "Max number of unacknowledged messages allowed per shared subscription. \n\n"
            + " Broker will stop dispatching messages to all consumers of the subscription once this "
            + " limit reaches until consumer starts acknowledging messages back and unack count reaches"
```

### TextBlockMigration
Concatenation can be replaced with text block
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            category = CATEGORY_KEYSTORE_TLS,
            doc = "Specify the tls cipher the internal client will use to negotiate during TLS Handshake"
                  + " (a comma-separated list of ciphers).\n\n"
                  + "Examples:- [TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256].\n"
                  + " used by the internal client to authenticate with Pulsar brokers"
```

## RuleId[ruleID=SillyAssignment]
### SillyAssignment
Variable 'statsProvider' is assigned to itself
in `testmocks/src/main/java/org/apache/bookkeeper/client/BookKeeperTestClient.java`
#### Snippet
```java
        super(conf, zkc, null, new UnpooledByteBufAllocator(false),
                NullStatsLogger.INSTANCE, null, null, null);
        this.statsProvider = statsProvider;
    }

```

## RuleId[ruleID=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `EnumValuesDataProvider` has no concrete subclass
in `buildtools/src/main/java/org/apache/pulsar/tests/EnumValuesDataProvider.java`
#### Snippet
```java
 * Supports currently a single Enum parameter for a test method.
 */
public abstract class EnumValuesDataProvider {
    @DataProvider
    public static final Object[][] values(Method testMethod) {
```

### AbstractClassNeverImplemented
Abstract class `DeviationShedder` has no concrete subclass
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/DeviationShedder.java`
#### Snippet
```java
 * deviation is above some threshold.
 */
public abstract class DeviationShedder implements LoadSheddingStrategy {
    // A Set of pairs is used in favor of a Multimap for simplicity.
    protected TreeSet<Pair<Double, String>> metricTreeSetCache;
```

### AbstractClassNeverImplemented
Abstract class `StructSchema` has no concrete subclass
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/StructSchema.java`
#### Snippet
```java
 */
@Deprecated
public abstract class StructSchema<T> extends AbstractStructSchema<T> {

    protected final Schema schema;
```

### AbstractClassNeverImplemented
Abstract class `MetricsMixIn` has no concrete subclass
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/MetricsMixIn.java`
#### Snippet
```java
import java.util.Map;

public abstract class MetricsMixIn {
    @JsonInclude(content = JsonInclude.Include.NON_EMPTY)
    Map<String, String> dimensions;
```

### AbstractClassNeverImplemented
Abstract class `ResourceQuotaMixIn` has no concrete subclass
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/ResourceQuotaMixIn.java`
#### Snippet
```java
import com.fasterxml.jackson.annotation.JsonIgnore;

public abstract class ResourceQuotaMixIn {
    @JsonIgnore
    public abstract boolean isValid();
```

### AbstractClassNeverImplemented
Abstract class `JsonIgnorePropertiesMixIn` has no concrete subclass
in `pulsar-common/src/main/java/org/apache/pulsar/common/functions/JsonIgnorePropertiesMixIn.java`
#### Snippet
```java

@JsonIgnoreProperties(ignoreUnknown = true)
public abstract class JsonIgnorePropertiesMixIn {

}
```

### AbstractClassNeverImplemented
Abstract class `AirliftUtils` has no concrete subclass
in `pulsar-common/src/main/java/org/apache/pulsar/common/compression/AirliftUtils.java`
#### Snippet
```java
 * Utilities.
 */
public abstract class AirliftUtils {

    static ByteBuffer ensureAirliftSupported(ByteBuffer encodedNio) {
```

## RuleId[ruleID=BoundedWildcard]
### BoundedWildcard
Can generalize to `? extends File`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/ProcessedFileThread.java`
#### Snippet
```java
    private final String processedFileSuffix;

    public ProcessedFileThread(FileSourceConfig fileConfig, BlockingQueue<File> recentlyProcessed) {
        keepOriginal = Optional.ofNullable(fileConfig.getKeepFile()).orElse(false);
        processedFileSuffix = fileConfig.getProcessedFileSuffix();
```

### BoundedWildcard
Can generalize to `? extends File`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java

    public FileConsumerThread(PushSource<byte[]> source,
            BlockingQueue<File> workQueue,
            BlockingQueue<File> inProcess,
            BlockingQueue<File> recentlyProcessed) {
```

### BoundedWildcard
Can generalize to `? super File`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java
    public FileConsumerThread(PushSource<byte[]> source,
            BlockingQueue<File> workQueue,
            BlockingQueue<File> inProcess,
            BlockingQueue<File> recentlyProcessed) {
        this.source = source;
```

### BoundedWildcard
Can generalize to `? super File`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java
            BlockingQueue<File> workQueue,
            BlockingQueue<File> inProcess,
            BlockingQueue<File> recentlyProcessed) {
        this.source = source;
        this.workQueue = workQueue;
```

### BoundedWildcard
Can generalize to `? super File`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileListingThread.java`
#### Snippet
```java

    public FileListingThread(FileSourceConfig fileConfig,
            BlockingQueue<File> workQueue,
            BlockingQueue<File> inProcess,
            BlockingQueue<File> recentlyProcessed) {
```

### BoundedWildcard
Can generalize to `? extends GenericRecord`
in `pulsar-io/solr/src/main/java/org/apache/pulsar/io/solr/SolrGenericRecordSink.java`
#### Snippet
```java
public class SolrGenericRecordSink extends SolrAbstractSink<GenericRecord> {
    @Override
    public SolrInputDocument convert(Record<GenericRecord> message) {
        SolrInputDocument doc = new SolrInputDocument();
        GenericRecord record = message.getValue();
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
    private static void fillKeyValueSchemaData(org.apache.pulsar.client.api.Schema<GenericObject> schema,
                                        GenericObject record,
                                        Map<String, Object> data) {
        if (record == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends GenericObject`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java

    @Override
    public Mutation createMutation(Record<GenericObject> message) {
        final GenericObject record = message.getValue();
        Function<String, Object> recordValueGetter;
```

### BoundedWildcard
Can generalize to `? extends GenericObject`
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/HttpSink.java`
#### Snippet
```java

    @Override
    public void write(Record<GenericObject> record) throws Exception {
        Object json = toJsonSerializable(record.getSchema(), record.getValue().getNativeObject());
        byte[] bytes = mapper.writeValueAsBytes(json);
```

### BoundedWildcard
Can generalize to `? extends Record`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java
    }

    private void executeBatch(Deque<Record<T>> swapList, PreparedStatement statement) throws SQLException {
        final int[] results = statement.executeBatch();
        Map<Integer, Integer> failuresMapping = null;
```

### BoundedWildcard
Can generalize to `? extends LifecycleAware`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/Application.java`
#### Snippet
```java
    }

    public Application(List<LifecycleAware> components) {
        this.components = components;
        supervisor = new LifecycleSupervisor();
```

### BoundedWildcard
Can generalize to `? extends ColumnId`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcUtils.java`
#### Snippet
```java
    }

    public static String combationWhere(List<ColumnId> columnIds) {
        StringBuilder builder = new StringBuilder();
        if (!columnIds.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends GenericRecord`
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseGenericRecordSink.java`
#### Snippet
```java
public class HbaseGenericRecordSink extends HbaseAbstractSink<GenericRecord> {
    @Override
    public void bindValue(Record<GenericRecord> message, List<Put> puts) throws Exception {
        GenericRecord record = message.getValue();

```

### BoundedWildcard
Can generalize to `? super Put`
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseGenericRecordSink.java`
#### Snippet
```java
public class HbaseGenericRecordSink extends HbaseAbstractSink<GenericRecord> {
    @Override
    public void bindValue(Record<GenericRecord> message, List<Put> puts) throws Exception {
        GenericRecord record = message.getValue();

```

### BoundedWildcard
Can generalize to `? extends Record`
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/HdfsSyncThread.java`
#### Snippet
```java
    private boolean keepRunning = true;

    public HdfsSyncThread(Syncable stream, BlockingQueue<Record<V>> unackedRecords, long syncInterval) {
      this.stream = stream;
      this.unackedRecords = unackedRecords;
```

### BoundedWildcard
Can generalize to `? extends Record`
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/HdfsSyncThread.java`
#### Snippet
```java
    private boolean keepRunning = true;

    public HdfsSyncThread(Syncable stream, BlockingQueue<Record<V>> unackedRecords, long syncInterval) {
      this.stream = stream;
      this.unackedRecords = unackedRecords;
```

### BoundedWildcard
Can generalize to `? extends MongoClient`
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSource.java`
#### Snippet
```java
    }

    public MongoSource(Supplier<MongoClient> clientProvider) {
        this.clientProvider = clientProvider;
    }
```

### BoundedWildcard
Can generalize to `? extends MongoClient`
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
    }

    public MongoSink(Supplier<MongoClient> clientProvider) {
        this.clientProvider = clientProvider;
    }
```

### BoundedWildcard
Can generalize to `? extends Record`
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
        final List<Integer> idxToFail = Lists.newArrayList();

        public DocsToInsertSubscriber(List<Document> docsToInsert, List<Record<byte[]>> recordsToInsert) {
            this.docsToInsert = docsToInsert;
            this.recordsToInsert = recordsToInsert;
```

### BoundedWildcard
Can generalize to `? super KinesisRecord`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisRecordProcessor.java`
#### Snippet
```java
    private long nextCheckpointTimeInNanos;
    private String kinesisShardId;
    public KinesisRecordProcessor(LinkedBlockingQueue<KinesisRecord> queue, KinesisSourceConfig config) {
        this.queue = queue;
        this.checkpointInterval = config.getCheckpointInterval();
```

### BoundedWildcard
Can generalize to `? super Class`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java

    private Channel getOrCreateChannel(
            ListMultimap<Class<? extends Channel>, String> channelsNotReused,
            String name, String type)
            throws FlumeException {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java

    private void loadSinkGroups(AgentConfiguration agentConf,
                                Map<String, Sink> sinks, Map<String, SinkRunner> sinkRunnerMap)
            throws InstantiationException {
        Set<String> sinkGroupNames = agentConf.getSinkgroupSet();
```

### BoundedWildcard
Can generalize to `? super SinkRunner`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java

    private void loadSinkGroups(AgentConfiguration agentConf,
                                Map<String, Sink> sinks, Map<String, SinkRunner> sinkRunnerMap)
            throws InstantiationException {
        Set<String> sinkGroupNames = agentConf.getSinkgroupSet();
```

### BoundedWildcard
Can generalize to `? extends ChannelComponent`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java

    private void loadSinks(AgentConfiguration agentConf,
                           Map<String, ChannelComponent> channelComponentMap, Map<String, SinkRunner> sinkRunnerMap)
            throws InstantiationException {
        Set<String> sinkNames = agentConf.getSinkSet();
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java
    private void loadSources(AgentConfiguration agentConf,
                             Map<String, ChannelComponent> channelComponentMap,
                             Map<String, SourceRunner> sourceRunnerMap)
            throws InstantiationException {

```

### BoundedWildcard
Can generalize to `? super SourceRunner`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java
    private void loadSources(AgentConfiguration agentConf,
                             Map<String, ChannelComponent> channelComponentMap,
                             Map<String, SourceRunner> sourceRunnerMap)
            throws InstantiationException {

```

### BoundedWildcard
Can generalize to `? extends ChannelComponent`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java

    private List<Channel> getSourceChannels(
            Map<String, ChannelComponent> channelComponentMap,
            Source source, Collection<String> channelNames) throws InstantiationException {
        List<Channel> sourceChannels = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java

    private void loadChannels(AgentConfiguration agentConf,
                              Map<String, ChannelComponent> channelComponentMap)
            throws InstantiationException {
        LOGGER.info("Creating channels");
```

### BoundedWildcard
Can generalize to `? super ChannelComponent`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java

    private void loadChannels(AgentConfiguration agentConf,
                              Map<String, ChannelComponent> channelComponentMap)
            throws InstantiationException {
        LOGGER.info("Creating channels");
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/common/src/main/java/org/apache/pulsar/io/common/IOConfigUtils.java`
#### Snippet
```java

    private static <T> T loadWithSecrets(Map<String, Object> map, Class<T> clazz,
                                         Function<String, String> secretsGetter) {
        Map<String, Object> configs = new HashMap<>(map);

```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/debezium/core/src/main/java/org/apache/pulsar/io/debezium/DebeziumSource.java`
#### Snippet
```java
    }

    public static void setConfigIfNull(Map<String, Object> config, String key, String value) {
        config.putIfAbsent(key, value);
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/debezium/core/src/main/java/org/apache/pulsar/io/debezium/DebeziumSource.java`
#### Snippet
```java
    private static final String DEFAULT_HISTORY_TOPIC = "debezium-history-topic";

    public static void throwExceptionIfConfigNotMatch(Map<String, Object> config,
                                                       String key,
                                                       String value) throws IllegalArgumentException {
```

### BoundedWildcard
Can generalize to `? super StreamsRecord`
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/StreamsRecordProcessor.java`
#### Snippet
```java
    private long nextCheckpointTimeInNanos;
    private String kinesisShardId;
    public StreamsRecordProcessor(LinkedBlockingQueue<StreamsRecord> queue, DynamoDBSourceConfig config) {
        this.queue = queue;
        this.checkpointInterval = config.getCheckpointInterval();
```

### BoundedWildcard
Can generalize to `? extends GenericObject`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
    }

    public static String serializeRecordToJsonExpandingValue(ObjectMapper mapper, Record<GenericObject> record,
                                                             boolean flatten)
            throws JsonProcessingException {
```

### BoundedWildcard
Can generalize to `? extends GenericRecord`
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBGenericRecordSink.java`
#### Snippet
```java

    @Override
    protected Point buildPoint(Record<GenericRecord> message) throws Exception {
        Map<String, String> tags;
        Map<String, Object> fields = Maps.newHashMap();
```

### BoundedWildcard
Can generalize to `? extends Point`
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBAbstractSink.java`
#### Snippet
```java

    @Override
    protected void writePoints(List<Point> points) throws Exception {
        BatchPoints.Builder batchBuilder = BatchPoints
                .database(influxDatabase)
```

### BoundedWildcard
Can generalize to `? extends GenericRecord`
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSink.java`
#### Snippet
```java

    @Override
    protected final Point buildPoint(Record<GenericRecord> record) {
        val genericRecord = record.getValue();

```

### BoundedWildcard
Can generalize to `? extends BulkProcessor.BulkOperationRequest`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java

            @Override
            public void afterBulk(long executionId, List<BulkProcessor.BulkOperationRequest> bulkOperationList, Throwable throwable) {
                log.warn("Bulk request id={} failed:", executionId, throwable);
                for (BulkProcessor.BulkOperationRequest operation: bulkOperationList) {
```

### BoundedWildcard
Can generalize to `? extends BulkProcessor.BulkOperationRequest`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java

            @Override
            public void afterBulk(long executionId, List<BulkProcessor.BulkOperationRequest> bulkOperationList,
                                  List<BulkProcessor.BulkOperationResult> results) {
                if (log.isTraceEnabled()) {
```

### BoundedWildcard
Can generalize to `? extends BulkProcessor.BulkOperationResult`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java
            @Override
            public void afterBulk(long executionId, List<BulkProcessor.BulkOperationRequest> bulkOperationList,
                                  List<BulkProcessor.BulkOperationResult> results) {
                if (log.isTraceEnabled()) {
                    log.trace("Bulk request id={} size={}:", executionId, bulkOperationList.size());
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSource.java`
#### Snippet
```java
    private static final String JSON_WITH_ENVELOPE_CONFIG = "json-with-envelope";

    public void open(Map<String, Object> config, SourceContext sourceContext) throws Exception {
        if (config.get(JSON_WITH_ENVELOPE_CONFIG) != null) {
            jsonWithEnvelope = Boolean.parseBoolean(config.get(JSON_WITH_ENVELOPE_CONFIG).toString());
```

### BoundedWildcard
Can generalize to `? extends GenericObject`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
     * @return A pair for _id and _source
     */
    public Pair<String, String> extractIdAndDocument(Record<GenericObject> record) throws JsonProcessingException {
        if (elasticSearchConfig.isSchemaEnable()) {
            Object key = null;
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/batch-discovery-triggerers/src/main/java/org/apache/pulsar/io/batchdiscovery/ImmediateTriggerer.java`
#### Snippet
```java

  @Override
  public void start(Consumer<String> consumer) {
    consumer.accept("");
  }
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-io/batch-discovery-triggerers/src/main/java/org/apache/pulsar/io/batchdiscovery/CronTriggerer.java`
#### Snippet
```java

  @Override
  public void start(Consumer<String> trigger) {

    scheduler.initialize();
```

### BoundedWildcard
Can generalize to `? super ByteBuffer`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
    }

    private void fillOffsetMap(Map<ByteBuffer, ByteBuffer> offsetMap, TopicPartition topicPartition, long l) {
        ByteBuffer key = topicPartitionAsKey(topicPartition);
        ByteBuffer value = ByteBuffer.allocate(Long.BYTES);
```

### BoundedWildcard
Can generalize to `? super ByteBuffer`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
    }

    private void fillOffsetMap(Map<ByteBuffer, ByteBuffer> offsetMap, TopicPartition topicPartition, long l) {
        ByteBuffer key = topicPartitionAsKey(topicPartition);
        ByteBuffer value = ByteBuffer.allocate(Long.BYTES);
```

### BoundedWildcard
Can generalize to `? extends OffsetAndMetadata`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
    }

    public void flushOffsets(Map<TopicPartition, OffsetAndMetadata> offsets) throws Exception {
        Map<ByteBuffer, ByteBuffer> offsetMap = Maps.newHashMapWithExpectedSize(offsets.size());

```

### BoundedWildcard
Can generalize to `? super Collection`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
    public PulsarKafkaSinkTaskContext(Map<String, String> config,
                                      SinkContext ctx,
                                      Consumer<Collection<TopicPartition>> onPartitionChange) {
        this.config = config;
        this.ctx = ctx;
```

### BoundedWildcard
Can generalize to `? super String`
in `testmocks/src/main/java/org/apache/bookkeeper/client/TestStatsProvider.java`
#### Snippet
```java
    }

    public void forEachOpStatLogger(BiConsumer<String, TestOpStatsLogger> f) {
        for (Map.Entry<String, TestOpStatsLogger> entry : opStatLoggerMap.entrySet()) {
            f.accept(entry.getKey(), entry.getValue());
```

### BoundedWildcard
Can generalize to `? super TestOpStatsLogger`
in `testmocks/src/main/java/org/apache/bookkeeper/client/TestStatsProvider.java`
#### Snippet
```java
    }

    public void forEachOpStatLogger(BiConsumer<String, TestOpStatsLogger> f) {
        for (Map.Entry<String, TestOpStatsLogger> entry : opStatLoggerMap.entrySet()) {
            f.accept(entry.getKey(), entry.getValue());
```

### BoundedWildcard
Can generalize to `? extends LedgerEntryImpl`
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockReadHandle.java`
#### Snippet
```java

    PulsarMockReadHandle(PulsarMockBookKeeper bk, long ledgerId, LedgerMetadata metadata,
                         List<LedgerEntryImpl> entries) {
        this.bk = bk;
        this.ledgerId = ledgerId;
```

### BoundedWildcard
Can generalize to `? super Record`
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
    protected void ackUntil(Record<GenericObject> lastNotFlushed,
                          Map<TopicPartition, OffsetAndMetadata> committedOffsets,
                          java.util.function.Consumer<Record<GenericObject>> cb) {
        // lastNotFlushed is needed in case of default preCommit() implementation
        // which calls flush() and returns currentOffsets passed to it.
```

### BoundedWildcard
Can generalize to `? super Thread`
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadLocalStateCleaner.java`
#### Snippet
```java

    private <T> void callCleanedValueListener(ThreadLocal<?> threadLocal, Thread thread,
                                              BiConsumer<Thread, T> cleanedValueListener, Object threadLocalMap)
            throws IllegalAccessException, InvocationTargetException {
        T currentValue = getCurrentValue(threadLocal, threadLocalMap);
```

### BoundedWildcard
Can generalize to `? super Thread`
in `buildtools/src/main/java/org/apache/pulsar/tests/FastThreadLocalStateCleaner.java`
#### Snippet
```java
    }

    public void cleanupAllFastThreadLocals(Thread thread, BiConsumer<Thread, Object> cleanedValueListener) {
        Objects.nonNull(thread);
        try {
```

### BoundedWildcard
Can generalize to `? extends DecoderColumnHandle`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/primitive/PulsarPrimitiveRowDecoderFactory.java`
#### Snippet
```java
    @Override
    public PulsarRowDecoder createRowDecoder(TopicName topicName, SchemaInfo schemaInfo,
                                             Set<DecoderColumnHandle> columns) {
        if (columns.size() == 1) {
            return new PulsarPrimitiveRowDecoder((AbstractSchema<?>) AutoConsumeSchema.getSchema(schemaInfo),
```

### BoundedWildcard
Can generalize to `? extends InetSocketAddress`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/BrokerProxyValidator.java`
#### Snippet
```java
    private final boolean allowAnyHostName;

    public BrokerProxyValidator(AddressResolver<InetSocketAddress> inetSocketAddressResolver, String allowedHostNames,
                                String allowedIPAddresses, String allowedTargetPorts) {
        this.inetSocketAddressResolver = inetSocketAddressResolver;
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java

        @Override
        public void readEntriesComplete(List<Entry> entries, Object ctx) {

            entryQueue.fill(new MessagePassingQueue.Supplier<Entry>() {
```

### BoundedWildcard
Can generalize to `? extends PulsarColumnHandle`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
    }

    private void initialize(List<PulsarColumnHandle> columnHandles, PulsarSplit pulsarSplit, PulsarConnectorConfig
        pulsarConnectorConfig, ManagedLedgerFactory managedLedgerFactory, ManagedLedgerConfig managedLedgerConfig,
                            PulsarConnectorMetricsTracker pulsarConnectorMetricsTracker) {
```

### BoundedWildcard
Can generalize to `? extends Map`>
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java

    // This call is used for starting additional protocol handlers
    public void startProxyExtensions(Map<String, Map<InetSocketAddress,
            ChannelInitializer<SocketChannel>>> protocolHandlers, ServerBootstrap serverBootstrap) {

        protocolHandlers.forEach((extensionName, initializers) -> {
```

### BoundedWildcard
Can generalize to `? extends Pair`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/WebServer.java`
#### Snippet
```java

    public void addServlet(String basePath, ServletHolder servletHolder,
                           List<Pair<String, Object>> attributes, boolean requireAuthentication) {
        Optional<String> existingPath = servletPaths.stream().filter(p -> p.startsWith(basePath)).findFirst();
        if (existingPath.isPresent()) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/ConcurrentBitmapSortedLongPairSet.java`
#### Snippet
```java

    public <T extends Comparable<T>> NavigableSet<T> items(int numberOfItems,
                                                           LongPairSet.LongPairFunction<T> longPairConverter) {
        NavigableSet<T> items = new TreeSet<>();
        lock.readLock().lock();
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
    }

    public <T> T sync(Supplier<CompletableFuture<T>> supplier) {
        try {
            return supplier.get().get(config().getMetadataStoreOperationTimeoutSeconds(), SECONDS);
```

### BoundedWildcard
Can generalize to `? extends List`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java

    // Release lookup semaphore and add result to redirectAddresses if current broker doesn't own the topic.
    private synchronized void completeLookup(Pair<List<String>, Boolean> result, List<String> redirectAddresses,
                                              CompletableFuture<Void> future) {
        pulsar().getBrokerService().getLookupRequestSemaphore().release();
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java

    // Release lookup semaphore and add result to redirectAddresses if current broker doesn't own the topic.
    private synchronized void completeLookup(Pair<List<String>, Boolean> result, List<String> redirectAddresses,
                                              CompletableFuture<Void> future) {
        pulsar().getBrokerService().getLookupRequestSemaphore().release();
```

### BoundedWildcard
Can generalize to `? extends ProducerAck`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java

    // Process results for all message publishing attempts
    private void processPublishMessageResults(List<ProducerAck> produceMessageResults,
                                              List<CompletableFuture<PositionImpl>> publishResults) {
        // process publish message result
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
    // Process results for all message publishing attempts
    private void processPublishMessageResults(List<ProducerAck> produceMessageResults,
                                              List<CompletableFuture<PositionImpl>> publishResults) {
        // process publish message result
        for (int index = 0; index < publishResults.size(); index++) {
```

### BoundedWildcard
Can generalize to `? super Boolean`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java

    private void processLookUpResult(List<String> redirectAddresses,  AsyncResponse asyncResponse,
                                     CompletableFuture<Boolean> future) {
        // Current broker doesn't own the topic or any partition of the topic, redirect client to a broker
        // that own partition of the topic or know who own partition of the topic.
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/OffloaderObjectsScannerUtils.java`
#### Snippet
```java
    private static String checkLedgerShouldBeOnTieredStorage(long ledgerId, String uuid,
            String managedLedgerName,
            Map<String, Object> data,
            ManagedLedgerFactory managedLedgerFactory)
        throws InterruptedException, ManagedLedgerException  {
```

### BoundedWildcard
Can generalize to `? extends SchemaAndMetadata`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SchemasResourceBase.java`
#### Snippet
```java
    }

    protected GetAllVersionsSchemaResponse convertToAllVersionsSchemaResponse(List<SchemaAndMetadata> schemas) {
        if (isNull(schemas)) {
            throw new RestException(Response.Status.NOT_FOUND.getStatusCode(), "Schemas not found");
```

### BoundedWildcard
Can generalize to `? extends Metrics`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/MetricsGenerator.java`
#### Snippet
```java
    }

    private Collection<Metrics> merge(List<Metrics> metricsCollection) {

        // map by dimension map -> metrics
```

### BoundedWildcard
Can generalize to `? super T`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/TimeWindow.java`
#### Snippet
```java
     * @return
     */
    public synchronized WindowWrap<T> current(Function<T, T> function) {
        long millis = System.currentTimeMillis();

```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/TimeWindow.java`
#### Snippet
```java
     * @return
     */
    public synchronized WindowWrap<T> current(Function<T, T> function) {
        long millis = System.currentTimeMillis();

```

### BoundedWildcard
Can generalize to `? extends Collector.MetricFamilySamples`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/DimensionStats.java`
#### Snippet
```java
        private final List<Collector.MetricFamilySamples> samples;

        public DimensionStatsSnapshot(List<Collector.MetricFamilySamples> samples) {
            this.samples = samples;
        }
```

### BoundedWildcard
Can generalize to `? super Metrics`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/AbstractMetrics.java`
#### Snippet
```java
     * @param ledger
     */
    protected void populateDimensionMap(Map<Metrics, List<ManagedLedgerImpl>> ledgersByDimensionMap, Metrics metrics,
            ManagedLedgerImpl ledger) {
        ledgersByDimensionMap.computeIfAbsent(metrics, __ -> new ArrayList<>()).add(ledger);
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/AbstractMetrics.java`
#### Snippet
```java
    }

    protected void populateAggregationMapWithSum(Map<String, Double> map, String mkey, double value) {
        Double val = map.getOrDefault(mkey, 0.0);
        map.put(mkey, val + value);
```

### BoundedWildcard
Can generalize to `? super Metrics`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/AbstractMetrics.java`
#### Snippet
```java
    }

    protected void populateDimensionMap(Map<Metrics, List<TopicStats>> topicsStatsByDimensionMap,
            Metrics metrics, TopicStats destStats) {
        topicsStatsByDimensionMap.computeIfAbsent(metrics, __ -> new ArrayList<>()).add(destStats);
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/AbstractMetrics.java`
#### Snippet
```java
    }

    protected void populateMaxMap(Map<String, Long> map, String mkey, long value) {
        Long existingValue = map.get(mkey);
        if (existingValue == null || value > existingValue) {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/AbstractMetrics.java`
#### Snippet
```java
        }

        public void populateBucketEntries(Map<String, Double> map, long[] bucketValues, int period) {
            // bucket values should be one more that the boundaries to have the last element as OVERFLOW
            if (bucketValues != null && bucketValues.length != boundaries.length + 1) {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/metrics/AbstractMetrics.java`
#### Snippet
```java
    }

    protected void populateAggregationMap(Map<String, List<Double>> map, String mkey, double value) {
        map.computeIfAbsent(mkey, __ -> new ArrayList<>()).add(value);
    }
```

### BoundedWildcard
Can generalize to `? extends NamespaceIsolationDataImpl`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ClustersBase.java`
#### Snippet
```java
    private BrokerNamespaceIsolationData internalGetBrokerNsIsolationData(
            String broker,
            Map<String, NamespaceIsolationDataImpl> policies) {
        BrokerNamespaceIsolationData.Builder brokerIsolationData =
                BrokerNamespaceIsolationData.builder().brokerName(broker);
```

### BoundedWildcard
Can generalize to `? extends PrometheusRawMetricsProvider`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsGenerator.java`
#### Snippet
```java
    private static ByteBuf generate0(PulsarService pulsar, boolean includeTopicMetrics, boolean includeConsumerMetrics,
                                     boolean includeProducerMetrics, boolean splitTopicAndPartitionIndexLabel,
                                     List<PrometheusRawMetricsProvider> metricsProviders) throws IOException {
        //Use unpooled buffers here to avoid direct buffer usage increasing.
        //when write out 200MB data, MAX_COMPONENTS = 64 needn't mem_copy. see: CompositeByteBuf#consolidateIfNeeded()
```

### BoundedWildcard
Can generalize to `? extends Metrics`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsGenerator.java`
#### Snippet
```java
    }

    private static void parseMetricsToPrometheusMetrics(Collection<Metrics> metrics, String cluster,
                                                        Collector.Type metricType, SimpleTextOutputStream stream) {
        Set<String> names = new HashSet<>();
```

### BoundedWildcard
Can generalize to `? extends CompactorMXBean`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/TopicStats.java`
#### Snippet
```java

    public static void printTopicStats(PrometheusMetricStreams stream, TopicStats stats,
                                       Optional<CompactorMXBean> compactorMXBean, String cluster, String namespace,
                                       String topic, boolean splitTopicAndPartitionIndexLabel) {
        writeMetric(stream, "pulsar_subscriptions_count", stats.subscriptionsCount,
```

### BoundedWildcard
Can generalize to `? super AggregatedReplicationStats`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/NamespaceStatsAggregator.java`
#### Snippet
```java
    private static void writeReplicationStat(PrometheusMetricStreams stream, String metricName,
                                             AggregatedNamespaceStats namespaceStats,
                                             Function<AggregatedReplicationStats, Number> sampleValueFunction,
                                             String cluster, String namespace) {
        if (!namespaceStats.replicationStats.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends Number`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/NamespaceStatsAggregator.java`
#### Snippet
```java
    private static void writeReplicationStat(PrometheusMetricStreams stream, String metricName,
                                             AggregatedNamespaceStats namespaceStats,
                                             Function<AggregatedReplicationStats, Number> sampleValueFunction,
                                             String cluster, String namespace) {
        if (!namespaceStats.replicationStats.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends CompactorMXBean`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/NamespaceStatsAggregator.java`
#### Snippet
```java
    private static void getTopicStats(Topic topic, TopicStats stats, boolean includeConsumerMetrics,
                                      boolean includeProducerMetrics, boolean getPreciseBacklog,
                                      boolean subscriptionBacklogSize, Optional<CompactorMXBean> compactorMXBean) {
        stats.reset();

```

### BoundedWildcard
Can generalize to `? super Integer`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
                         WorkerConfig workerConfig,
                         Optional<WorkerService> functionWorkerService,
                         Consumer<Integer> processTerminator) {
        state = State.Init;

```

### BoundedWildcard
Can generalize to `? extends Position`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/InMemoryRedeliveryTracker.java`
#### Snippet
```java

    @Override
    public void removeBatch(List<Position> positions) {
        if (positions != null) {
            positions.forEach(this::remove);
```

### BoundedWildcard
Can generalize to `? extends EntryFilterWithClassLoader`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/EntryFilterSupport.java`
#### Snippet
```java

    private static EntryFilter.FilterResult getFilterResult(FilterContext filterContext, Entry entry,
                                                            List<EntryFilterWithClassLoader> entryFilters) {
        for (EntryFilter entryFilter : entryFilters) {
            EntryFilter.FilterResult filterResult =
```

### BoundedWildcard
Can generalize to `? extends PulsarEvent`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicBasedTopicPoliciesService.java`
#### Snippet
```java
    }

    private void fetchTopicPoliciesAsyncAndCloseReader(SystemTopicClient.Reader<PulsarEvent> reader,
                                                       TopicName topicName, TopicPolicies policies,
                                                       CompletableFuture<TopicPolicies> future) {
```

### BoundedWildcard
Can generalize to `? super TopicPolicies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicBasedTopicPoliciesService.java`
#### Snippet
```java
    private void fetchTopicPoliciesAsyncAndCloseReader(SystemTopicClient.Reader<PulsarEvent> reader,
                                                       TopicName topicName, TopicPolicies policies,
                                                       CompletableFuture<TopicPolicies> future) {
        reader.hasMoreEventsAsync().whenComplete((hasMore, ex) -> {
            if (ex != null) {
```

### BoundedWildcard
Can generalize to `? extends PulsarEvent`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicBasedTopicPoliciesService.java`
#### Snippet
```java
    }

    private void notifyListener(Message<PulsarEvent> msg) {
        // delete policies
        if (msg.getValue() == null) {
```

### BoundedWildcard
Can generalize to `? extends PulsarEvent`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/SystemTopicBasedTopicPoliciesService.java`
#### Snippet
```java
    }

    private void refreshTopicPoliciesCache(Message<PulsarEvent> msg) {
        // delete policies
        if (msg.getValue() == null) {
```

### BoundedWildcard
Can generalize to `? extends Consumer`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java
    }

    public static void checkAndApplyReachedEndOfTopicOrTopicMigration(PersistentTopic topic, List<Consumer> consumers) {
        if (topic.isMigrated()) {
            consumers.forEach(c -> c.topicMigrated(topic.getMigratedClusterUrl()));
```

### BoundedWildcard
Can generalize to `? extends ConcurrentOpenHashMap`>
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarStats.java`
#### Snippet
```java

    public synchronized void updateStats(
            ConcurrentOpenHashMap<String, ConcurrentOpenHashMap<String, ConcurrentOpenHashMap<String, Topic>>>
                    topicsMap) {

```

### BoundedWildcard
Can generalize to `? super ByteBuf`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarStats.java`
#### Snippet
```java
    }

    public void getDimensionMetrics(Consumer<ByteBuf> consumer) {
        bufferLock.readLock().lock();
        try {
```

### BoundedWildcard
Can generalize to `? super Consumer`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractSubscription.java`
#### Snippet
```java
    }

    private long sumConsumers(ToLongFunction<Consumer> toCounter) {
        return Optional.ofNullable(getDispatcher())
                .map(dispatcher -> dispatcher.getConsumers().stream().mapToLong(toCounter).sum())
```

### BoundedWildcard
Can generalize to `? super TopicListWatcher`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/TopicListService.java`
#### Snippet
```java


    public void initializeTopicsListWatcher(CompletableFuture<TopicListWatcher> watcherFuture,
            NamespaceName namespace, long watcherId, Pattern topicsPattern) {
        namespaceService.getListOfPersistentTopics(namespace).
```

### BoundedWildcard
Can generalize to `? extends SchemaData`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/AvroSchemaBasedCompatibilityCheck.java`
#### Snippet
```java

    @Override
    public void checkCompatible(Iterable<SchemaData> from, SchemaData to, SchemaCompatibilityStrategy strategy)
            throws IncompatibleSchemaException {
        LinkedList<Schema> fromList = new LinkedList<>();
```

### BoundedWildcard
Can generalize to `? extends SchemaData`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/ProtobufNativeSchemaCompatibilityCheck.java`
#### Snippet
```java

    @Override
    public void checkCompatible(Iterable<SchemaData> from, SchemaData to, SchemaCompatibilityStrategy strategy)
            throws IncompatibleSchemaException {
        for (SchemaData schemaData : from) {
```

### BoundedWildcard
Can generalize to `? super AbstractSubscription`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    }

    private long sumSubscriptions(ToLongFunction<AbstractSubscription> toCounter) {
        return getSubscriptions().values().stream()
                .map(AbstractSubscription.class::cast)
```

### BoundedWildcard
Can generalize to `? extends SchemaData`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/KeyValueSchemaCompatibilityCheck.java`
#### Snippet
```java

    @Override
    public void checkCompatible(Iterable<SchemaData> from, SchemaData to, SchemaCompatibilityStrategy strategy)
            throws IncompatibleSchemaException {
        if (strategy == SchemaCompatibilityStrategy.ALWAYS_COMPATIBLE) {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    protected synchronized boolean trySendMessagesToConsumers(ReadType readType, List<Entry> entries) {
        long totalMessagesSent = 0;
        long totalBytesSent = 0;
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
    }

    private int getRestrictedMaxEntriesForConsumer(Consumer consumer, List<Entry> entries, int maxMessages,
            ReadType readType, Set<Integer> stickyKeyHashes) {
        if (maxMessages == 0) {
```

### BoundedWildcard
Can generalize to `? extends Policies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/SubscribeRateLimiter.java`
#### Snippet
```java
    }

    public static SubscribeRate getPoliciesSubscribeRate(final String cluster, final Optional<Policies> policies,
            String topicName) {
        // return policy-subscribe rate only if it's enabled in policies
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    }

    protected void dispatchEntriesToConsumer(Consumer currentConsumer, List<Entry> entries,
                                             EntryBatchSizes batchSizes, EntryBatchIndexesAcks batchIndexesAcks,
                                             SendMessageInfo sendMessageInfo, long epoch) {
```

### BoundedWildcard
Can generalize to `? extends SchemaAndMetadata`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
    private CompletableFuture<Void> checkCompatibilityWithAll(String schemaId, SchemaData schema,
                                                              SchemaCompatibilityStrategy strategy,
                                                              List<SchemaAndMetadata> schemaAndMetadataList) {
        CompletableFuture<Void> result = new CompletableFuture<>();
        result.whenComplete((v, t) -> {
```

### BoundedWildcard
Can generalize to `? extends SchemaAndMetadata`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<SchemaVersion> getSchemaVersionBySchemaData(
            List<SchemaAndMetadata> schemaAndMetadataList,
            SchemaData schemaData) {
        if (schemaAndMetadataList == null || schemaAndMetadataList.size() == 0) {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java

    private void resumeAsyncResponse(AsyncResponse asyncResponse, Set<String> subscriptions,
                                     List<CompletableFuture<Object>> subscriptionFutures) {
        FutureUtil.waitForAll(subscriptionFutures).whenComplete((r, ex) -> {
            if (ex != null) {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/ShadowReplicator.java`
#### Snippet
```java

    @Override
    protected boolean replicateEntries(List<Entry> entries) {
        boolean atLeastOneMessageSentForReplication = false;

```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/GeoPersistentReplicator.java`
#### Snippet
```java

    @Override
    protected boolean replicateEntries(List<Entry> entries) {
        boolean atLeastOneMessageSentForReplication = false;
        boolean isEnableReplicatedSubscriptions =
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public void sendMessages(List<Entry> entries) {
        if (entries.isEmpty()) {
            return;
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public void sendMessages(List<Entry> entries) {
        Consumer consumer = TOTAL_AVAILABLE_PERMITS_UPDATER.get(this) > 0 ? getNextConsumer() : null;
        if (consumer != null) {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java

    @Override
    public void sendMessages(List<Entry> entries) {
        Consumer currentConsumer = ACTIVE_CONSUMER_UPDATER.get(this);
        if (currentConsumer != null && currentConsumer.getAvailablePermits() > 0 && currentConsumer.isWritable()) {
```

### BoundedWildcard
Can generalize to `? extends PendingReadEntryRequest`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
    }

    private void cleanQueue(Queue<PendingReadEntryRequest> queue) {
        while (!queue.isEmpty()) {
            PendingReadEntryRequest pendingReadEntryRequest = queue.poll();
```

### BoundedWildcard
Can generalize to `? extends PositionImpl`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    @Override
    public synchronized void redeliverUnacknowledgedMessages(Consumer consumer, List<PositionImpl> positions) {
        positions.forEach(position -> {
            // TODO: We want to pass a sticky key hash as a third argument to guarantee the order of the messages
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    private boolean sendChunkedMessagesToConsumers(ReadType readType,
                                                   List<Entry> entries,
                                                   MessageMetadata[] metadataArray) {
        final List<EntryAndMetadata> originalEntryAndMetadataList = new ArrayList<>(metadataArray.length);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    }

    private void recordRateLimitMetrics(ConcurrentLongHashMap<CompletableFuture<Producer>> producers) {
        producers.forEach((key, producerFuture) -> {
            if (producerFuture != null && producerFuture.isDone()) {
```

### BoundedWildcard
Can generalize to `? extends ManagedLedgerPayloadProcessor.Processor`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/ManagedLedgerInterceptorImpl.java`
#### Snippet
```java
    }

    private PayloadProcessorHandle processPayload(Set<ManagedLedgerPayloadProcessor.Processor> processors,
                                                  Object context, ByteBuf payload) {

```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/ManagedLedgerInterceptorImpl.java`
#### Snippet
```java

    @Override
    public void onUpdateManagedLedgerInfo(Map<String, String> propertiesMap) {
        for (BrokerEntryMetadataInterceptor interceptor : brokerEntryMetadataInterceptors) {
            if (interceptor instanceof AppendIndexMetadataInterceptor) {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/ManagedLedgerInterceptorImpl.java`
#### Snippet
```java

    @Override
    public void onUpdateManagedLedgerInfo(Map<String, String> propertiesMap) {
        for (BrokerEntryMetadataInterceptor interceptor : brokerEntryMetadataInterceptors) {
            if (interceptor instanceof AppendIndexMetadataInterceptor) {
```

### BoundedWildcard
Can generalize to `? extends PositionImpl`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    }

    private void trimByMarkDeletePosition(List<PositionImpl> positions) {
        positions.removeIf(position -> cursor.getMarkDeletedPosition() != null
                && position.compareTo((PositionImpl) cursor.getMarkDeletedPosition()) <= 0);
```

### BoundedWildcard
Can generalize to `? extends AdvertisedListener`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceEphemeralData.java`
#### Snippet
```java

    public NamespaceEphemeralData(String brokerUrl, String brokerUrlTls, String httpUrl, String httpUrlTls,
                                  boolean disabled, Map<String, AdvertisedListener> advertisedListeners) {
        this.nativeUrl = brokerUrl;
        this.nativeUrlTls = brokerUrlTls;
```

### BoundedWildcard
Can generalize to `? extends LoadManager`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LoadResourceQuotaUpdaterTask.java`
#### Snippet
```java
    private final AtomicReference<LoadManager> loadManager;

    public LoadResourceQuotaUpdaterTask(AtomicReference<LoadManager> loadManager) {
        this.loadManager = loadManager;
    }
```

### BoundedWildcard
Can generalize to `? extends LoadManager`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LoadReportUpdaterTask.java`
#### Snippet
```java
    private final AtomicReference<LoadManager> loadManager;

    public LoadReportUpdaterTask(AtomicReference<LoadManager> manager) {
        loadManager = manager;
    }
```

### BoundedWildcard
Can generalize to `? extends BrokerInterceptorWithClassLoader`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/BrokerInterceptors.java`
#### Snippet
```java
    private final Map<String, BrokerInterceptorWithClassLoader> interceptors;

    public BrokerInterceptors(Map<String, BrokerInterceptorWithClassLoader> interceptors) {
        this.interceptors = interceptors;
    }
```

### BoundedWildcard
Can generalize to `? extends LoadManager`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LoadSheddingTask.java`
#### Snippet
```java
    private volatile ScheduledFuture<?> future;

    public LoadSheddingTask(AtomicReference<LoadManager> loadManager,
                            ScheduledExecutorService loadManagerExecutor,
                            ServiceConfiguration config) {
```

### BoundedWildcard
Can generalize to `? super Policies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java


    private <T> void mutatePolicy(Function<Policies, Policies> policyTransformation,
                                  Function<Policies, T> getter,
                                  String policyName) {
```

### BoundedWildcard
Can generalize to `? extends Policies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java


    private <T> void mutatePolicy(Function<Policies, Policies> policyTransformation,
                                  Function<Policies, T> getter,
                                  String policyName) {
```

### BoundedWildcard
Can generalize to `? super Policies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

    private <T> void mutatePolicy(Function<Policies, Policies> policyTransformation,
                                  Function<Policies, T> getter,
                                  String policyName) {
        try {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBufferReader.java`
#### Snippet
```java
    // the iterator should hold the references to the entries
    // so when the reader is closed, all the entries can be released.
    public InMemTransactionBufferReader(TxnID txnId, Iterator<Entry<Long, ByteBuf>> entries, long committedAtLedgerId,
                                        long committedAtEntryId) {
        this.txnId = txnId;
```

### BoundedWildcard
Can generalize to `? extends ConcurrentOpenHashMap`>
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
    public static CompletableFuture<Map<String, Integer>> getAntiAffinityNamespaceOwnedBrokers(
            final PulsarService pulsar, final String namespaceName,
            final ConcurrentOpenHashMap<String, ConcurrentOpenHashMap<String, ConcurrentOpenHashSet<String>>>
                    brokerToNamespaceToBundleRange) {

```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
    // The brokers are put into brokerCandidateCache.
    public static void applyNamespacePolicies(final ServiceUnitId serviceUnit,
            final SimpleResourceAllocationPolicies policies, final Set<String> brokerCandidateCache,
            final Set<String> availableBrokers, final BrokerTopicLoadingPredicate brokerTopicLoadingPredicate) {
        Set<String> primariesCache = localPrimariesCache.get();
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
     */
    public static void fillNamespaceToBundlesMap(final Set<String> bundles,
            final ConcurrentOpenHashMap<String, ConcurrentOpenHashSet<String>> target) {
        bundles.forEach(bundleName -> {
            final String namespaceName = getNamespaceNameFromBundleName(bundleName);
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
            final String assignedBundleName,
            final Set<String> candidates,
            final ConcurrentOpenHashMap<String, ConcurrentOpenHashMap<String, ConcurrentOpenHashSet<String>>>
                    brokerToNamespaceToBundleRange) {
        if (candidates.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java

        @Override
        public void readEntriesComplete(List<Entry> entries, Object ctx) {
            entryQueue.fill(new MessagePassingQueue.Supplier<Entry>() {
                private int i = 0;
```

### BoundedWildcard
Can generalize to `? super Subscription`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    }

    private void resetSubscriptionCursor(Subscription subscription, CompletableFuture<Subscription> subscriptionFuture,
                                         long startMessageRollbackDurationSec) {
        long timestamp = System.currentTimeMillis()
```

### BoundedWildcard
Can generalize to `? super Optional`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java

    private void searchForCandidateBroker(NamespaceBundle bundle,
                                          CompletableFuture<Optional<LookupResult>> lookupFuture,
                                          LookupOptions options) {
        if (null == pulsar.getLeaderElectionService()) {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/MLPendingAckStore.java`
#### Snippet
```java

        @Override
        public void readEntriesComplete(List<Entry> entries, Object ctx) {
            entryQueue.fill(new MessagePassingQueue.Supplier<Entry>() {
                private int i = 0;
```

### BoundedWildcard
Can generalize to `? super Optional`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

    private void createPersistentTopic(final String topic, boolean createIfMissing,
                                       CompletableFuture<Optional<Topic>> topicFuture,
                                       Map<String, String> properties) {
        TopicName topicName = TopicName.get(topic);
```

### BoundedWildcard
Can generalize to `? extends Map`>
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    // This call is used for starting additional protocol handlers
    public void startProtocolHandlers(
        Map<String, Map<InetSocketAddress, ChannelInitializer<SocketChannel>>> protocolHandlers) {

        protocolHandlers.forEach((protocol, initializers) -> {
```

### BoundedWildcard
Can generalize to `? super Topic`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
     * Iterates over all loaded topics in the broker.
     */
    public void forEachTopic(Consumer<Topic> consumer) {
        topics.forEach((n, t) -> {
            Optional<Topic> topic = extractTopic(t);
```

### BoundedWildcard
Can generalize to `? extends ClusterData`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    }

    public PulsarAdmin getClusterPulsarAdmin(String cluster, Optional<ClusterData> clusterDataOp) {
        PulsarAdmin admin = clusterAdmins.get(cluster);
        if (admin != null) {
```

### BoundedWildcard
Can generalize to `? super PersistentTopic`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    }

    public void forEachPersistentTopic(Consumer<PersistentTopic> consumer) {
        topics.values().stream().map(BrokerService::extractTopic)
                .map(topicOp -> topicOp.filter(topic -> topic instanceof PersistentTopic))
```

### BoundedWildcard
Can generalize to `? extends ClusterData`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    }

    public PulsarClient getReplicationClient(String cluster, Optional<ClusterData> clusterDataOp) {
        PulsarClient client = replicationClients.get(cluster);
        if (client != null) {
```

### BoundedWildcard
Can generalize to `? extends PersistentDispatcherMultipleConsumers`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
     * @param dispatcherList
     */
    public void unblockDispatchersOnUnAckMessages(List<PersistentDispatcherMultipleConsumers> dispatcherList) {
        lock.writeLock().lock();
        try {
```

### BoundedWildcard
Can generalize to `? extends Policies`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

    private AutoTopicCreationOverride getAutoTopicCreationOverride(final TopicName topicName,
                                                                   Optional<Policies> policies) {
        // If namespace policies have the field set, it will override the broker-level setting
        if (policies.isPresent() && policies.get().autoTopicCreationOverride != null) {
```

### BoundedWildcard
Can generalize to `? extends PositionImpl`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java

    public void internalCumulativeAcknowledgeMessage(TxnID txnID,
                                                     List<PositionImpl> positions,
                                                     CompletableFuture<Void> completableFuture) {
        if (txnID == null) {
```

### BoundedWildcard
Can generalize to `? extends MutablePair`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
    }

    private void handleIndividualAck(TxnID txnID, List<MutablePair<PositionImpl, Integer>> positions) {
        for (int i = 0; i < positions.size(); i++) {
            if (log.isDebugEnabled()) {
```

### BoundedWildcard
Can generalize to `? extends LocalPolicies`
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java
    }

    private static long[] getPartitions(Optional<LocalPolicies> lp) {
        return getPartitions(lp.map(x -> x.bundles).orElse(null));
    }
```

### BoundedWildcard
Can generalize to `? extends MessageId`
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/TwoPhaseCompactor.java`
#### Snippet
```java
    }

    private void phaseTwoLoop(RawReader reader, MessageId to, Map<String, MessageId> latestForKey,
                              LedgerHandle lh, Semaphore outstanding, CompletableFuture<Void> promise) {
        if (promise.isDone()) {
```

### BoundedWildcard
Can generalize to `? extends MessageId`
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/TwoPhaseCompactor.java`
#### Snippet
```java
    private void phaseOneLoop(RawReader reader,
                              Optional<MessageId> firstMessageId,
                              Optional<MessageId> toMessageId,
                              MessageId lastMessageId,
                              Map<String, MessageId> latestForKey,
```

### BoundedWildcard
Can generalize to `? super PhaseOneResult`
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/TwoPhaseCompactor.java`
#### Snippet
```java
                              MessageId lastMessageId,
                              Map<String, MessageId> latestForKey,
                              CompletableFuture<PhaseOneResult> loopPromise) {
        if (loopPromise.isDone()) {
            return;
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/HttpClient.java`
#### Snippet
```java
    }

    public <T> CompletableFuture<T> get(String path, Class<T> clazz) {
        final CompletableFuture<T> future = new CompletableFuture<>();
        try {
```

### BoundedWildcard
Can generalize to `? extends ProducerInterceptor`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerInterceptors.java`
#### Snippet
```java
    private final List<ProducerInterceptor> interceptors;

    public ProducerInterceptors(List<ProducerInterceptor> interceptors) {
        this.interceptors = interceptors;
    }
```

### BoundedWildcard
Can generalize to `? extends Authentication`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AutoClusterFailover.java`
#### Snippet
```java

    private void probeAndUpdateServiceUrl(List<String> targetServiceUrls,
                                          Map<String, Authentication> authentications,
                                          Map<String, String> tlsTrustCertsFilePaths,
                                          Map<String, String> tlsTrustStorePaths,
```

### BoundedWildcard
Can generalize to `? extends ReaderInterceptor`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ReaderInterceptorUtil.java`
#### Snippet
```java

    public static <T> ConsumerInterceptors<T> convertToConsumerInterceptors(
            Reader<T> reader, List<ReaderInterceptor<T>> interceptorList) {
        if (interceptorList == null || interceptorList.isEmpty()) {
            return null;
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TableViewImpl.java`
#### Snippet
```java

    @Override
    public void forEach(BiConsumer<String, T> action) {
        data.forEach(action);
    }
```

### BoundedWildcard
Can generalize to `? super T`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TableViewImpl.java`
#### Snippet
```java

    @Override
    public void forEach(BiConsumer<String, T> action) {
        data.forEach(action);
    }
```

### BoundedWildcard
Can generalize to `? super Reader`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TableViewImpl.java`
#### Snippet
```java
    }

    private void readAllExistingMessages(Reader<T> reader, CompletableFuture<Reader<T>> future, long startTime,
                                         AtomicLong messagesRead) {
        reader.hasMessageAvailableAsync()
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TableViewImpl.java`
#### Snippet
```java
    }

    private void handleMessage(Message<T> msg) {
        try {
            if (msg.hasKey()) {
```

### BoundedWildcard
Can generalize to `? super GetTopicsResult`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BinaryProtoLookupService.java`
#### Snippet
```java
                                         Backoff backoff,
                                         AtomicLong remainingTime,
                                         CompletableFuture<GetTopicsResult> getTopicsResultFuture,
                                         Mode mode,
                                         String topicsPattern,
```

### BoundedWildcard
Can generalize to `? super TopicConsumerBuilder`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBuilderImpl.java`
#### Snippet
```java
    @Override
    public ConsumerBuilder<T> topicConfiguration(String topicName,
                                                 java.util.function.Consumer<TopicConsumerBuilder<T>> builderConsumer) {
        builderConsumer.accept(topicConfiguration(topicName));
        return this;
```

### BoundedWildcard
Can generalize to `? super TopicConsumerBuilder`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBuilderImpl.java`
#### Snippet
```java
    @Override
    public ConsumerBuilder<T> topicConfiguration(Pattern topicsPattern,
                                                 java.util.function.Consumer<TopicConsumerBuilder<T>> builderConsumer) {
        builderConsumer.accept(topicConfiguration(topicsPattern));
        return this;
```

### BoundedWildcard
Can generalize to `? super MessageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageTracker.java`
#### Snippet
```java
    }

    public static void addChunkedMessageIdsAndRemoveFromSequenceMap(MessageId messageId, Set<MessageId> messageIds,
                                                                    ConsumerBase<?> consumerBase) {
        if (messageId instanceof MessageIdImpl) {
```

### BoundedWildcard
Can generalize to `? extends ConsumerInterceptor`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerInterceptors.java`
#### Snippet
```java
    private final List<ConsumerInterceptor<T>> interceptors;

    public ConsumerInterceptors(List<ConsumerInterceptor<T>> interceptors) {
        this.interceptors = interceptors;
    }
```

### BoundedWildcard
Can generalize to `? super MessageIdImpl`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
            MessageIdImpl msgId,
            @Nullable BatchMessageIdImpl batchMessageId,
            Function<MessageIdImpl, CompletableFuture<Void>> individualAckFunction,
            Function<BatchMessageIdImpl, CompletableFuture<Void>> batchAckFunction) {
        if (batchMessageId != null) {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
            MessageIdImpl msgId,
            @Nullable BatchMessageIdImpl batchMessageId,
            Function<MessageIdImpl, CompletableFuture<Void>> individualAckFunction,
            Function<BatchMessageIdImpl, CompletableFuture<Void>> batchAckFunction) {
        if (batchMessageId != null) {
```

### BoundedWildcard
Can generalize to `? super BatchMessageIdImpl`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
            @Nullable BatchMessageIdImpl batchMessageId,
            Function<MessageIdImpl, CompletableFuture<Void>> individualAckFunction,
            Function<BatchMessageIdImpl, CompletableFuture<Void>> batchAckFunction) {
        if (batchMessageId != null) {
            consumer.onAcknowledge(batchMessageId, null);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
            @Nullable BatchMessageIdImpl batchMessageId,
            Function<MessageIdImpl, CompletableFuture<Void>> individualAckFunction,
            Function<BatchMessageIdImpl, CompletableFuture<Void>> batchAckFunction) {
        if (batchMessageId != null) {
            consumer.onAcknowledge(batchMessageId, null);
```

### BoundedWildcard
Can generalize to `? extends MessageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
    }

    private void addListAcknowledgment(List<MessageId> messageIds) {
        for (MessageId messageId : messageIds) {
            if (messageId instanceof BatchMessageIdImpl) {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PatternMultiTopicsConsumerImpl.java`
#### Snippet
```java

    static CompletableFuture<Void> updateSubscriptions(Pattern topicsPattern,
                                                       java.util.function.Consumer<String> topicsHashSetter,
                                                       GetTopicsResult getTopicsResult,
                                                       TopicsChangedListener topicsChangedListener,
```

### BoundedWildcard
Can generalize to `? extends MessageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
    }

    protected void onAcknowledgeCumulative(List<MessageId> messageIds, Throwable exception) {
        if (interceptors != null) {
            messageIds.forEach(messageId -> interceptors.onAcknowledgeCumulative(this, messageId, exception));
```

### BoundedWildcard
Can generalize to `? super Message`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
    }

    protected void completePendingReceive(CompletableFuture<Message<T>> receivedFuture, Message<T> message) {
        getInternalExecutor(message).execute(() -> {
            if (!receivedFuture.complete(message)) {
```

### BoundedWildcard
Can generalize to `? super Messages`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
    }

    protected void completePendingBatchReceive(CompletableFuture<Messages<T>> future, Messages<T> messages) {
        if (!future.complete(messages)) {
            log.warn("Race condition detected. batch receive future was already completed (cancelled={}) and messages"
```

### BoundedWildcard
Can generalize to `? extends MessageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
    }

    protected void onAcknowledge(List<MessageId> messageIds, Throwable exception) {
        if (interceptors != null) {
            messageIds.forEach(messageId -> interceptors.onAcknowledge(this, messageId, exception));
```

### BoundedWildcard
Can generalize to `? super PartitionedTopicMetadata`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                                             Backoff backoff,
                                             AtomicLong remainingTime,
                                             CompletableFuture<PartitionedTopicMetadata> future,
                                             List<Throwable> previousExceptions) {
        long startTime = System.nanoTime();
```

### BoundedWildcard
Can generalize to `? extends InetSocketAddress`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConnectionPool.java`
#### Snippet
```java
     * address is working.
     */
    private CompletableFuture<Channel> connectToResolvedAddresses(Iterator<InetSocketAddress> unresolvedAddresses,
                                                                  InetSocketAddress sniHost) {
        CompletableFuture<Channel> future = new CompletableFuture<>();
```

### BoundedWildcard
Can generalize to `? extends ByteArrayInputStream`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataTls.java`
#### Snippet
```java
    }

    public AuthenticationDataTls(Supplier<ByteArrayInputStream> certStreamProvider,
            Supplier<ByteArrayInputStream> keyStreamProvider, Supplier<ByteArrayInputStream> trustStoreStreamProvider)
            throws KeyManagementException {
```

### BoundedWildcard
Can generalize to `? extends ByteArrayInputStream`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataTls.java`
#### Snippet
```java

    public AuthenticationDataTls(Supplier<ByteArrayInputStream> certStreamProvider,
            Supplier<ByteArrayInputStream> keyStreamProvider, Supplier<ByteArrayInputStream> trustStoreStreamProvider)
            throws KeyManagementException {
        if (certStreamProvider == null || certStreamProvider.get() == null) {
```

### BoundedWildcard
Can generalize to `? extends ByteArrayInputStream`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataTls.java`
#### Snippet
```java

    public AuthenticationDataTls(Supplier<ByteArrayInputStream> certStreamProvider,
            Supplier<ByteArrayInputStream> keyStreamProvider, Supplier<ByteArrayInputStream> trustStoreStreamProvider)
            throws KeyManagementException {
        if (certStreamProvider == null || certStreamProvider.get() == null) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConfigurationDataUtils.java`
#### Snippet
```java
    public static <T> T loadData(Map<String, Object> config,
                                 T existingData,
                                 Class<T> dataCls) {
        ObjectMapper mapper = getThreadLocal();
        try {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/SchemaDefinitionBuilderImpl.java`
#### Snippet
```java

    @Override
    public SchemaDefinitionBuilder<T> withProperties(Map<String, String> properties) {
        this.properties = properties;
        if (properties.containsKey(ALWAYS_ALLOW_NULL)) {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/SchemaDefinitionBuilderImpl.java`
#### Snippet
```java

    @Override
    public SchemaDefinitionBuilder<T> withProperties(Map<String, String> properties) {
        this.properties = properties;
        if (properties.containsKey(ALWAYS_ALLOW_NULL)) {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java

    private static void deserializeFileDescriptor(FileDescriptorProto fileDescriptorProto,
                                                  Map<String, Descriptors.FileDescriptor> fileDescriptorCache,
                                                  Map<String, FileDescriptorProto> fileDescriptorProtoCache) {
        fileDescriptorProto.getDependencyList().forEach(dependencyFileDescriptorName -> {
```

### BoundedWildcard
Can generalize to `? super Descriptors.FileDescriptor`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java

    private static void deserializeFileDescriptor(FileDescriptorProto fileDescriptorProto,
                                                  Map<String, Descriptors.FileDescriptor> fileDescriptorCache,
                                                  Map<String, FileDescriptorProto> fileDescriptorProtoCache) {
        fileDescriptorProto.getDependencyList().forEach(dependencyFileDescriptorName -> {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java

    private static void serializeFileDescriptor(Descriptors.FileDescriptor fileDescriptor,
                                                Map<String, FileDescriptorProto> fileDescriptorCache) {
        fileDescriptor.getDependencies().forEach(dependency -> {
                    if (!fileDescriptorCache.containsKey(dependency.getFullName())) {
```

### BoundedWildcard
Can generalize to `? super FileDescriptorProto`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java

    private static void serializeFileDescriptor(Descriptors.FileDescriptor fileDescriptor,
                                                Map<String, FileDescriptorProto> fileDescriptorCache) {
        fileDescriptor.getDependencies().forEach(dependency -> {
                    if (!fileDescriptorCache.containsKey(dependency.getFullName())) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/reader/JacksonJsonReader.java`
#### Snippet
```java
    private final ObjectMapper objectMapper;

    public JacksonJsonReader(ObjectMapper objectMapper, Class<T> pojo) {
        this.pojo = pojo;
        this.objectMapper = objectMapper;
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/reader/JsonReader.java`
#### Snippet
```java
    private final ObjectMapper objectMapper;

    public JsonReader(ObjectMapper objectMapper, Class<T> pojo) {
        this.pojo = pojo;
        this.objectMapper = objectMapper;
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/ObjectCache.java`
#### Snippet
```java
    }

    ObjectCache(Supplier<T> supplier, long cacheDuration, TimeUnit unit, Clock clock) {
        this.supplier = supplier;
        this.cachedInstance = null;
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/RetryUtil.java`
#### Snippet
```java
    }

    private static <T> void executeWithRetry(Supplier<CompletableFuture<T>> supplier, Backoff backoff,
                                             ScheduledExecutorService scheduledExecutorService,
                                             CompletableFuture<T> callback) {
```

### BoundedWildcard
Can generalize to `? super T`
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/RetryUtil.java`
#### Snippet
```java
    private static <T> void executeWithRetry(Supplier<CompletableFuture<T>> supplier, Backoff backoff,
                                             ScheduledExecutorService scheduledExecutorService,
                                             CompletableFuture<T> callback) {
        supplier.get().whenComplete((result, e) -> {
            if (e != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaInfo.java`
#### Snippet
```java
                                                                    String schemaTypeProperty,
                                                                    String schemaPropsProperty,
                                                                    Map<String, String> parentSchemaProperties) {
        parentSchemaProperties.put(schemaNameProperty, schemaInfo.getName());
        parentSchemaProperties.put(schemaTypeProperty, String.valueOf(schemaInfo.getType()));
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaInfo.java`
#### Snippet
```java
                                                                    String schemaTypeProperty,
                                                                    String schemaPropsProperty,
                                                                    Map<String, String> parentSchemaProperties) {
        parentSchemaProperties.put(schemaNameProperty, schemaInfo.getName());
        parentSchemaProperties.put(schemaTypeProperty, String.valueOf(schemaInfo.getType()));
```

### BoundedWildcard
Can generalize to `? extends SchemaInfo`
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/SchemaUtils.java`
#### Snippet
```java
     */
    public static String convertKeyValueSchemaInfoDataToString(
            KeyValue<SchemaInfo, SchemaInfo> kvSchemaInfo) throws IOException {
        ObjectMapper objectMapper = ObjectMapperFactory.create();
        KeyValue<Object, Object> keyValue = new KeyValue<>(
```

### BoundedWildcard
Can generalize to `? extends SchemaInfo`
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/SchemaUtils.java`
#### Snippet
```java
     */
    public static String convertKeyValueSchemaInfoDataToString(
            KeyValue<SchemaInfo, SchemaInfo> kvSchemaInfo) throws IOException {
        ObjectMapper objectMapper = ObjectMapperFactory.create();
        KeyValue<Object, Object> keyValue = new KeyValue<>(
```

### BoundedWildcard
Can generalize to `? extends File`
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/FileUtils.java`
#### Snippet
```java
     * @throws IOException if issues deleting files
     */
    public static void deleteFiles(final Collection<File> files, final boolean recurse) throws IOException {
        for (final File file : files) {
            FileUtils.deleteFile(file, recurse);
```

### BoundedWildcard
Can generalize to `? extends NamespaceBundleStats`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NamespaceBundleStatsComparator.java`
#### Snippet
```java
    ResourceType resType;

    public NamespaceBundleStatsComparator(Map<String, NamespaceBundleStats> map, ResourceType resType) {
        this.map = map;
        this.resType = resType;
```

### BoundedWildcard
Can generalize to `? extends Pair`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/BaseGenerateDocumentation.java`
#### Snippet
```java
    }

    protected void writeDocListByFieldContext(List<Pair<Field, FieldContextWrapper>> fieldList,
                                              StringBuilder sb, Object obj) throws Exception {
        for (Pair<Field, FieldContextWrapper> fieldPair : fieldList) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
     * @return The converted set with type {@code <T>}.
     */
    public static <T> Set<T> stringToSet(String val, Class<T> type) {
        String[] tokens = trim(val).split(",");
        return Arrays.stream(tokens).map(t -> {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
     * @return The converted list with type {@code <T>}.
     */
    public static <T> List<T> stringToList(String val, Class<T> type) {
        String[] tokens = trim(val).split(",");
        return Arrays.stream(tokens).map(t -> {
```

### BoundedWildcard
Can generalize to `? extends K`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
    }

    private static <K, V> Map<K, V> stringToMap(String strValue, Class<K> keyType, Class<V> valueType) {
        String[] tokens = trim(strValue).split(",");
        Map<K, V> map = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? extends V`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
    }

    private static <K, V> Map<K, V> stringToMap(String strValue, Class<K> keyType, Class<V> valueType) {
        String[] tokens = trim(strValue).split(",");
        Map<K, V> map = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? extends V`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/netty/NettyFutureUtil.java`
#### Snippet
```java
     * @return converted future instance
     */
    public static <V> CompletableFuture<V> toCompletableFuture(Future<V> future) {
        Objects.requireNonNull(future, "future cannot be null");

```

### BoundedWildcard
Can generalize to `? extends Throwable`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FutureUtil.java`
#### Snippet
```java
    public static <T> CompletableFuture<T> addTimeoutHandling(CompletableFuture<T> future, Duration timeout,
                                               ScheduledExecutorService executor,
                                               Supplier<Throwable> exceptionSupplier) {
        ScheduledFuture<?> scheduledFuture = executor.schedule(() -> {
            if (!future.isDone()) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentSortedLongPairSet.java`
#### Snippet
```java

    @Override
    public <T> Set<T> items(int numberOfItems, LongPairFunction<T> longPairConverter) {
        NavigableSet<T> items = new TreeSet<>();
        forEach((i1, i2) -> {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/LongPairRangeSet.java`
#### Snippet
```java
        private final LongPairConsumer<T> consumer;

        public DefaultRangeSet(LongPairConsumer<T> consumer) {
            this.consumer = consumer;
        }
```

### BoundedWildcard
Can generalize to `? super K`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
        }

        V put(K key, V value, int keyHash, boolean onlyIfAbsent, Function<K, V> valueProvider) {
            long stamp = writeLock();
            int bucket = signSafeMod(keyHash, capacity);
```

### BoundedWildcard
Can generalize to `? extends V`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
        }

        V put(K key, V value, int keyHash, boolean onlyIfAbsent, Function<K, V> valueProvider) {
            long stamp = writeLock();
            int bucket = signSafeMod(keyHash, capacity);
```

### BoundedWildcard
Can generalize to `? super V`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashSet.java`
#### Snippet
```java
        }

        int removeIf(Predicate<V> filter) {
            long stamp = writeLock();

```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongPairSet.java`
#### Snippet
```java

    @Override
    public <T> Set<T> items(int numberOfItems, LongPairFunction<T> longPairConverter) {
        Set<T> items = new HashSet<>();
        for (int i = 0; i < sections.length; i++) {
```

### BoundedWildcard
Can generalize to `? extends V`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
        }

        V put(long key, V value, int keyHash, boolean onlyIfAbsent, LongFunction<V> valueProvider) {
            int bucket = keyHash;

```

### BoundedWildcard
Can generalize to `? super V`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
        }

        public void forEach(EntryProcessor<V> processor) {
            long stamp = tryOptimisticRead();

```

### BoundedWildcard
Can generalize to `? extends LongPair`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
    }

    public void remove(Range<LongPair> range) {
        LongPair lowerEndpoint = range.hasLowerBound() ? range.lowerEndpoint() : LongPair.earliest;
        LongPair upperEndpoint = range.hasUpperBound() ? range.upperEndpoint() : LongPair.latest;
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
    }

    public ConcurrentOpenLongPairRangeSet(int size, boolean threadSafe, LongPairConsumer<T> consumer) {
        this.threadSafe = threadSafe;
        this.bitSetSize = size;
```

### BoundedWildcard
Can generalize to `? extends LongPair`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
     * no-op.
     */
    public void add(Range<LongPair> range) {
        LongPair lowerEndpoint = range.hasLowerBound() ? range.lowerEndpoint() : LongPair.earliest;
        LongPair upperEndpoint = range.hasUpperBound() ? range.upperEndpoint() : LongPair.latest;
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

    @Override
    public CompletableFuture<Void> seekAsync(Function<String, Object> function) {
        if (function == null) {
            return FutureUtil.failedFuture(new PulsarClientException("Function must be set"));
```

### BoundedWildcard
Can generalize to `? super MessageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
    }

    private int removeExpiredMessagesFromQueue(Set<MessageId> messageIds) {
        int messagesFromQueue = 0;
        Message<T> peek = incomingMessages.peek();
```

### BoundedWildcard
Can generalize to `? super Boolean`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
    }

    private void completehasMessageAvailableWithValue(CompletableFuture<Boolean> future, boolean value) {
        internalPinnedExecutor.execute(() -> {
            future.complete(value);
```

### BoundedWildcard
Can generalize to `? super BrokerStatus`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicies.java`
#### Snippet
```java
    }

    public void assignBroker(NamespaceName nsname, BrokerStatus brkStatus, SortedSet<BrokerStatus> primaryCandidates,
            SortedSet<BrokerStatus> secondaryCandidates, SortedSet<BrokerStatus> sharedCandidates) {
        NamespaceIsolationPolicy nsPolicy = this.getPolicyByNamespace(nsname);
```

### BoundedWildcard
Can generalize to `? super BrokerStatus`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicies.java`
#### Snippet
```java

    public void assignBroker(NamespaceName nsname, BrokerStatus brkStatus, SortedSet<BrokerStatus> primaryCandidates,
            SortedSet<BrokerStatus> secondaryCandidates, SortedSet<BrokerStatus> sharedCandidates) {
        NamespaceIsolationPolicy nsPolicy = this.getPolicyByNamespace(nsname);
        BrokerAssignment brokerAssignment = this.getBrokerAssignment(nsPolicy, brkStatus.getBrokerAddress());
```

### BoundedWildcard
Can generalize to `? super BrokerStatus`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicies.java`
#### Snippet
```java

    public void assignBroker(NamespaceName nsname, BrokerStatus brkStatus, SortedSet<BrokerStatus> primaryCandidates,
            SortedSet<BrokerStatus> secondaryCandidates, SortedSet<BrokerStatus> sharedCandidates) {
        NamespaceIsolationPolicy nsPolicy = this.getPolicyByNamespace(nsname);
        BrokerAssignment brokerAssignment = this.getBrokerAssignment(nsPolicy, brkStatus.getBrokerAddress());
```

### BoundedWildcard
Can generalize to `? extends BrokerStatus`
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicyImpl.java`
#### Snippet
```java

    @Override
    public SortedSet<BrokerStatus> getAvailablePrimaryBrokers(SortedSet<BrokerStatus> primaryCandidates) {
        SortedSet<BrokerStatus> availablePrimaries = new TreeSet<BrokerStatus>();
        for (BrokerStatus status : primaryCandidates) {
```

### BoundedWildcard
Can generalize to `? extends BundleData`
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/TimeAverageBrokerData.java`
#### Snippet
```java
     *            The stats to use when a bundle belonging to this broker is not found in the bundle data map.
     */
    public void reset(final Set<String> bundles, final Map<String, BundleData> data,
            final NamespaceBundleStats defaultStats) {
        shortTermMsgThroughputIn = 0;
```

### BoundedWildcard
Can generalize to `? extends NamespaceBundleStats`
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
    private double preAllocatedMsgRateOut;

    public void setBundleStats(Map<String, NamespaceBundleStats> stats) {
        bundleStats = (stats == null) ? null : new HashMap<String, NamespaceBundleStats>(stats);
    }
```

### BoundedWildcard
Can generalize to `? super Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java

    public OpScan(ManagedCursorImpl cursor, int batchSize,
                  PositionImpl startPosition, Predicate<Entry> condition,
                  ScanCallback callback, Object ctx, long maxEntries, long timeOutMs) {
        this.batchSize = batchSize;
```

### BoundedWildcard
Can generalize to `? super Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java
    State state;

    public OpFindNewest(ManagedCursorImpl cursor, PositionImpl startPosition, Predicate<Entry> condition,
            long numberOfEntries, FindEntryCallback callback, Object ctx) {
        this.cursor = cursor;
```

### BoundedWildcard
Can generalize to `? super Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java
    }

    public OpFindNewest(ManagedLedgerImpl ledger, PositionImpl startPosition, Predicate<Entry> condition,
                        long numberOfEntries, FindEntryCallback callback, Object ctx) {
        this.cursor = null;
```

### BoundedWildcard
Can generalize to `? super List`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/MetaStoreImpl.java`
#### Snippet
```java

    @Override
    public void getCursors(String ledgerName, MetaStoreCallback<List<String>> callback) {
        if (log.isDebugEnabled()) {
            log.debug("[{}] Get cursors list", ledgerName);
```

### BoundedWildcard
Can generalize to `? extends EntryCache`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/EntryCacheDefaultEvictionPolicy.java`
#### Snippet
```java

    @Override
    public void doEviction(List<EntryCache> caches, long sizeToFree) {
        checkArgument(sizeToFree > 0);
        checkArgument(!caches.isEmpty());
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java

                @Override
                public void readEntriesComplete(List<Entry> entries, Object ctx) {
                    if (!entries.isEmpty()) {
                        long size = entries.get(0).getLength();
```

### BoundedWildcard
Can generalize to `? super Value`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/RangeCache.java`
#### Snippet
```java
     *            a custom weighter to compute the size of each stored value
     */
    public RangeCache(Weighter<Value> weighter, TimestampExtractor<Value> timestampExtractor) {
        this.size = new AtomicLong(0);
        this.entries = new ConcurrentSkipListMap<>();
```

### BoundedWildcard
Can generalize to `? super Value`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/RangeCache.java`
#### Snippet
```java
     *            a custom weighter to compute the size of each stored value
     */
    public RangeCache(Weighter<Value> weighter, TimestampExtractor<Value> timestampExtractor) {
        this.size = new AtomicLong(0);
        this.entries = new ConcurrentSkipListMap<>();
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/SafeRun.java`
#### Snippet
```java
     * @return
     */
    public static SafeRunnable safeRun(Runnable runnable, Consumer<Throwable> exceptionHandler) {
        return new SafeRunnable() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java

                                        @Override
                                        public void readEntriesComplete(List<Entry> entriesFromLeft,
                                                                        Object dummyCtx5) {
                                            List<Entry> finalResult =
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
                                    new AsyncCallbacks.ReadEntriesCallback() {
                                @Override
                                public void readEntriesComplete(List<Entry> entriesFromLeft, Object dummyCtx1) {
                                    AsyncCallbacks.ReadEntriesCallback readFromRightCallback =
                                            new AsyncCallbacks.ReadEntriesCallback() {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
                AsyncCallbacks.ReadEntriesCallback wrappedCallback = new AsyncCallbacks.ReadEntriesCallback() {
                    @Override
                    public void readEntriesComplete(List<Entry> entries, Object ctx) {
                        PendingReadKey missingOnLeft = findBestCandidateOutcome.missingOnLeft;
                        PendingReadKey missingOnRight = findBestCandidateOutcome.missingOnRight;
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java

                                        @Override
                                        public void readEntriesComplete(List<Entry> entriesFromRight,
                                                                        Object dummyCtx7) {
                                            List<Entry> finalResult =
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
                                            new AsyncCallbacks.ReadEntriesCallback() {
                                        @Override
                                        public void readEntriesComplete(List<Entry> entriesFromRight,
                                                                        Object dummyCtx2) {
                                            List<Entry> finalResult =
```

### BoundedWildcard
Can generalize to `? extends List`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
        }

        public void attach(CompletableFuture<List<EntryImpl>> handle) {
            // when the future is done remove this from the map
            // new reads will go to a new instance
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/Futures.java`
#### Snippet
```java
    }

    public static CompletableFuture<Void> waitForAll(List<CompletableFuture<Void>> futures) {
        final CompletableFuture<Void> compositeFuture = new CompletableFuture<>();
        final AtomicInteger count = new AtomicInteger(futures.size());
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/Futures.java`
#### Snippet
```java
    }

    public static <T> CompletableFuture<T> executeWithRetry(Supplier<CompletableFuture<T>> op,
                                                            Class<? extends Exception> needRetryExceptionClass,
                                                            int maxRetryTimes) {
```

### BoundedWildcard
Can generalize to `? super MessageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    }

    private void removeExpiredMessagesFromQueue(Set<MessageId> messageIds) {
        Message<T> peek = incomingMessages.peek();
        if (peek != null) {
```

### BoundedWildcard
Can generalize to `? extends ConsumerImpl`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    }

    private void startReceivingMessages(List<ConsumerImpl<T>> newConsumers) {
        if (log.isDebugEnabled()) {
            log.debug("[{}] startReceivingMessages for {} new consumers in topics consumer, state: {}",
```

### BoundedWildcard
Can generalize to `? super Consumer`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
            ConsumerConfigurationData<T> conf,
            ExecutorProvider executorProvider,
            CompletableFuture<Consumer<T>> subscribeFuture,
            int numPartitions,
            Schema<T> schema, ConsumerInterceptors<T> interceptors) {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BufferedOffloadStream.java`
#### Snippet
```java
    private final long beginEntryId;

    public BufferedOffloadStream(int blockSize, List<Entry> entries, long ledgerId, long beginEntryId) {
        this.ledgerId = ledgerId;
        this.beginEntryId = beginEntryId;
```

### BoundedWildcard
Can generalize to `? extends OffloadIndexEntryImpl`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockImpl.java`
#### Snippet
```java
    public static OffloadIndexBlockImpl get(LedgerMetadata metadata, long dataObjectLength,
                                            long dataHeaderLength,
                                            List<OffloadIndexEntryImpl> entries) {
        OffloadIndexBlockImpl block = RECYCLER.get();
        block.indexEntries = Maps.newTreeMap();
```

### BoundedWildcard
Can generalize to `? extends List`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
    public static OffloadIndexBlockV2Impl get(Map<Long, LedgerInfo> metadata, long dataObjectLength,
                                              long dataHeaderLength,
                                              Map<Long, List<OffloadIndexEntryImpl>> entries) {
        OffloadIndexBlockV2Impl block = RECYCLER.get();
        block.indexEntries = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? extends OffloadIndexBlockV2`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java
    }

    private BlobStoreBackedReadHandleImplV2(long ledgerId, List<OffloadIndexBlockV2> indices,
                                            List<BackedInputStream> inputStreams,
                                            ExecutorService executor) {
```

### BoundedWildcard
Can generalize to `? extends BackedInputStream`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java

    private BlobStoreBackedReadHandleImplV2(long ledgerId, List<OffloadIndexBlockV2> indices,
                                            List<BackedInputStream> inputStreams,
                                            ExecutorService executor) {
        this.ledgerId = ledgerId;
```

### BoundedWildcard
Can generalize to `? super String`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
    private JCloudBlobStoreProvider provider;

    public TieredStorageConfiguration(Map<String, String> configProperties) {
        if (configProperties != null) {
            this.configProperties = configProperties;
```

### BoundedWildcard
Can generalize to `? super SessionEvent`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKSessionWatcher.java`
#### Snippet
```java
    private long disconnectedAt = 0;

    public ZKSessionWatcher(ZooKeeper zk, Consumer<SessionEvent> sessionListener) {
        this.zk = zk;
        this.monitorTimeoutMillis = zk.getSessionTimeout() * 5 / 6;
```

### BoundedWildcard
Can generalize to `? extends MetadataOp`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java

    @Override
    protected void batchOperation(List<MetadataOp> ops) {
        try {
            zkc.multi(ops.stream().map(this::convertOp).collect(Collectors.toList()), (rc, path, ctx, results) -> {
```

### BoundedWildcard
Can generalize to `? super SessionEvent`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdSessionWatcher.java`
#### Snippet
```java

    public EtcdSessionWatcher(Client client, long sessionTimeoutMillis,
                              Consumer<SessionEvent> sessionListener) {
        this.client = client;
        this.monitorTimeoutMillis = sessionTimeoutMillis * 5 / 6;
```

### BoundedWildcard
Can generalize to `? extends MetadataOp`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java

    private void handleBatchOperationResult(TxnResponse txnResponse,
                                            List<MetadataOp> ops) {
        executor.execute(() -> {
            if (!txnResponse.isSucceeded()) {
```

### BoundedWildcard
Can generalize to `? super MetadataOp`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/batching/AbstractBatchedMetadataStore.java`
#### Snippet
```java
    }

    private void enqueue(MessagePassingQueue<MetadataOp> queue, MetadataOp op) {
        if (enabled) {
            if (!queue.offer(op)) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/JSONMetadataSerdeTypeRef.java`
#### Snippet
```java
    private final TypeReference<T> typeRef;

    public JSONMetadataSerdeTypeRef(TypeReference<T> typeRef) {
        this.typeRef = typeRef;
    }
```

### BoundedWildcard
Can generalize to `? extends MetadataEventSynchronizer`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
    }

    protected void registerSyncLister(Optional<MetadataEventSynchronizer> synchronizer) {
        if (synchronizer.isPresent()) {
            synchronizer.get().registerSyncListener(event -> {
```

### BoundedWildcard
Can generalize to `? super Long`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/AbstractHierarchicalLedgerManager.java`
#### Snippet
```java
     */
    protected void asyncProcessLedgersInSingleNode(
            final String path, final BookkeeperInternalCallbacks.Processor<Long> processor,
            final AsyncCallback.VoidCallback finalCb, final Object ctx,
            final int successRc, final int failureRc) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/AbstractHierarchicalLedgerManager.java`
#### Snippet
```java
         *          RC passed to final callback on failure
         */
        public void process(final List<T> data, final BookkeeperInternalCallbacks.Processor<T> processor,
                            final AsyncCallback.VoidCallback finalCb, final Object context,
                            final int successRc, final int failureRc) {
```

### BoundedWildcard
Can generalize to `? super T`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/AbstractHierarchicalLedgerManager.java`
#### Snippet
```java
         *          RC passed to final callback on failure
         */
        public void process(final List<T> data, final BookkeeperInternalCallbacks.Processor<T> processor,
                            final AsyncCallback.VoidCallback finalCb, final Object context,
                            final int successRc, final int failureRc) {
```

### BoundedWildcard
Can generalize to `? super Optional`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java

    @Override
    public CompletableFuture<T> readModifyUpdateOrCreate(String path, Function<Optional<T>, T> modifyFunction) {
        return executeWithRetry(() -> objCache.get(path)
                .thenCompose(optEntry -> {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java

    @Override
    public CompletableFuture<T> readModifyUpdateOrCreate(String path, Function<Optional<T>, T> modifyFunction) {
        return executeWithRetry(() -> objCache.get(path)
                .thenCompose(optEntry -> {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
    }

    private CompletableFuture<T> executeWithRetry(Supplier<CompletableFuture<T>> op, String key) {
        CompletableFuture<T> result = new CompletableFuture<>();
        op.get().thenAccept(result::complete).exceptionally((ex) -> {
```

### BoundedWildcard
Can generalize to `? super T`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java

    @Override
    public CompletableFuture<T> readModifyUpdate(String path, Function<T, T> modifyFunction) {
        return executeWithRetry(() -> objCache.get(path)
                .thenCompose(optEntry -> {
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java

    @Override
    public CompletableFuture<T> readModifyUpdate(String path, Function<T, T> modifyFunction) {
        return executeWithRetry(() -> objCache.get(path)
                .thenCompose(optEntry -> {
```

### BoundedWildcard
Can generalize to `? super LeaderElectionState`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java

    LeaderElectionImpl(MetadataStoreExtended store, Class<T> clazz, String path,
            Consumer<LeaderElectionState> stateChangesListener,
                       ScheduledExecutorService executor) {
        this.path = path;
```

### BoundedWildcard
Can generalize to `? super Long`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/CoordinationServiceImpl.java`
#### Snippet
```java
    }

    private void internalGetNextCounterValueWithRetry(String path, CompletableFuture<Long> future, int count) {
        if (count == 0) {
            log.error("The number of retries has exhausted when get next counter value from path {}", path);
```

### BoundedWildcard
Can generalize to `? extends PulsarWorkerService`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/WorkerImpl.java`
#### Snippet
```java
    private final Supplier<PulsarWorkerService> workerServiceSupplier;

    public WorkerImpl(Supplier<PulsarWorkerService> workerServiceSupplier) {
        this.workerServiceSupplier = workerServiceSupplier;
    }
```

### BoundedWildcard
Can generalize to `? extends FunctionAuthData`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/ClearTextFunctionTokenAuthProvider.java`
#### Snippet
```java
    @Override
    public void configureAuthenticationConfig(AuthenticationConfig authConfig,
                                              Optional<FunctionAuthData> functionAuthData) {
        if (!functionAuthData.isPresent()) {
            // if auth data is not present maybe user is trying to use anonymous role thus don't pass in any auth config
```

### BoundedWildcard
Can generalize to `? super com.google.protobuf.Empty`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

        public void resetMetrics(com.google.protobuf.Empty request,
                                 io.grpc.stub.StreamObserver<com.google.protobuf.Empty> responseObserver) {
            Runtime runtime = runtimeSpawner.getRuntime();
            if (runtime != null) {
```

### BoundedWildcard
Can generalize to `? extends FunctionAuthData`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/KubernetesSecretsTokenAuthProvider.java`
#### Snippet
```java

    @Override
    public void configureAuthDataStatefulSet(V1StatefulSet statefulSet, Optional<FunctionAuthData> functionAuthData) {
        if (!functionAuthData.isPresent()) {
            return;
```

### BoundedWildcard
Can generalize to `? extends ThreadRuntimeFactoryConfig.MemoryLimit`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntimeFactory.java`
#### Snippet
```java
    }

    private Optional<Long> calculateClientMemoryLimit(Optional<ThreadRuntimeFactoryConfig.MemoryLimit> memoryLimit) {
        if (memoryLimit.isPresent()) {

```

### BoundedWildcard
Can generalize to `? extends PositionImpl`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    }

    void initializeCursorPosition(Pair<PositionImpl, Long> lastPositionCounter) {
        readPosition = ledger.getNextValidPosition(lastPositionCounter.getLeft());
        ledger.onCursorReadPositionUpdated(this, readPosition);
```

### BoundedWildcard
Can generalize to `? extends Position`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

    @Override
    public void asyncDelete(Iterable<Position> positions, AsyncCallbacks.DeleteCallback callback, Object ctx) {
        if (isClosed()) {
            callback.deleteFailed(new ManagedLedgerException
```

### BoundedWildcard
Can generalize to `? super Position`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

    @Override
    public CompletableFuture<ScanOutcome> scan(Optional<Position> position,
                                               Predicate<Entry> condition,
                                               int batchSize, long maxEntries, long timeOutMs) {
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

    @Override
    public void trimDeletedEntries(List<Entry> entries) {
        entries.removeIf(entry -> {
            boolean isDeleted = ((PositionImpl) entry.getPosition()).compareTo(markDeletePosition) <= 0
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java

    @Override
    public void write(Record<T> record) {
        AbstractSinkRecord<T> sinkRecord = (AbstractSinkRecord<T>) record;
        TypedMessageBuilder<T> msg = pulsarSinkProcessor.newMessage(sinkRecord);
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstance.java`
#### Snippet
```java
    public JavaExecutionResult handleMessage(Record<?> record, Object input,
                                             JavaInstanceRunnable.AsyncResultConsumer asyncResultConsumer,
                                             Consumer<Throwable> asyncFailureHandler) {
        if (context != null) {
            context.setCurrentMessageContext(record);
```

### BoundedWildcard
Can generalize to `? extends Collector.MetricFamilySamples`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/PrometheusTextFormat.java`
#### Snippet
```java
     * Write out the text version 0.0.4 of the given MetricFamilySamples.
     */
    public static void write004(Writer writer, Enumeration<Collector.MetricFamilySamples> mfs) throws IOException {
        /*
         * See http://prometheus.io/docs/instrumenting/exposition_formats/ for the output format specification.
```

### BoundedWildcard
Can generalize to `? super ByteBuf`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/state/BKStateStoreImpl.java`
#### Snippet
```java

    public BKStateStoreImpl(String tenant, String namespace, String name,
                            Table<ByteBuf, ByteBuf> table) {
        this.tenant = tenant;
        this.namespace = namespace;
```

### BoundedWildcard
Can generalize to `? extends Record`
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/window/LoggingWindowFunction.java`
#### Snippet
```java

    @Override
    public Void process(Collection<Record<String>> inputs, WindowContext context) throws Exception {
        Logger log = context.getLogger();
        for (Record<String> record : inputs) {
```

### BoundedWildcard
Can generalize to `? extends Record`
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/window/UserMetricWindowFunction.java`
#### Snippet
```java

    @Override
    public Void process(Collection<Record<String>> inputs, WindowContext context) throws Exception {

        for (Record<String> record : inputs) {
```

### BoundedWildcard
Can generalize to `? extends Record`
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/window/ContextWindowFunction.java`
#### Snippet
```java
public class ContextWindowFunction implements WindowFunction<Integer, Integer> {
    @Override
    public Integer process(Collection<Record<Integer>> integers, WindowContext context) {
        Integer retval = 0;
        for (Record<Integer> record : integers) {
```

### BoundedWildcard
Can generalize to `? extends Record`
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/window/WordCountWindowFunction.java`
#### Snippet
```java
public class WordCountWindowFunction implements WindowFunction<String, Void> {
    @Override
    public Void process(Collection<Record<String>> inputs, WindowContext context) throws Exception {
        for (Record<String> input : inputs) {
            Arrays.asList(input.getValue().split("\\.")).forEach(word -> context.incrCounter(word, 1));
```

### BoundedWildcard
Can generalize to `? super T`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/ContextImpl.java`
#### Snippet
```java
        }

        public void setUnderlyingBuilder(TypedMessageBuilder<T> underlyingBuilder) {
            this.underlyingBuilder = underlyingBuilder;
        }
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

        @Override
        public void readEntriesComplete(List<Entry> returnedEntries, Object ctx) {
            long reOpCount = reOpCount(ctx);
            ReadEntriesCallback callback = this.readEntriesCallback;
```

### BoundedWildcard
Can generalize to `? extends Range`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/KeySharedPolicy.java`
#### Snippet
```java
        }

        public KeySharedPolicySticky ranges(List<Range> ranges) {
            this.ranges.addAll(ranges);
            return this;
```

### BoundedWildcard
Can generalize to `? super Message`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/MessagePayloadProcessor.java`
#### Snippet
```java
                                MessagePayloadContext context,
                                Schema<T> schema,
                                Consumer<Message<T>> messageConsumer) {
            if (context.isBatch()) {
                final int numMessages = context.getNumMessages();
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java


    public static <T> Map<Integer, List<T>> allocateToThreads(List<T> managedLedgers, int numThreads) {

        Map<Integer, List<T>> map = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? extends Event`>
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java

            @Override
            public void onActivation(List<Event<Record<T>>> tuples, List<Event<Record<T>>> newTuples,
                                     List<Event<Record<T>>>
                                             expiredTuples, Long referenceTime) {
```

### BoundedWildcard
Can generalize to `? extends Event`>
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java

            @Override
            public void onActivation(List<Event<Record<T>>> tuples, List<Event<Record<T>>> newTuples,
                                     List<Event<Record<T>>>
                                             expiredTuples, Long referenceTime) {
```

### BoundedWildcard
Can generalize to `? extends Event`>
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
            @Override
            public void onActivation(List<Event<Record<T>>> tuples, List<Event<Record<T>>> newTuples,
                                     List<Event<Record<T>>>
                                             expiredTuples, Long referenceTime) {
                processWindow(
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionSequenceIdGenerator.java`
#### Snippet
```java
    // roll over ledger will update sequenceId to managedLedger properties
    @Override
    public void onUpdateManagedLedgerInfo(Map<String, String> propertiesMap) {
        propertiesMap.put(MAX_LOCAL_TXN_ID, sequenceId.get() + "");
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionSequenceIdGenerator.java`
#### Snippet
```java
    // roll over ledger will update sequenceId to managedLedger properties
    @Override
    public void onUpdateManagedLedgerInfo(Map<String, String> propertiesMap) {
        propertiesMap.put(MAX_LOCAL_TXN_ID, sequenceId.get() + "");
    }
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionLogImpl.java`
#### Snippet
```java

        @Override
        public void readEntriesComplete(List<Entry> entries, Object ctx) {
            entryQueue.fill(new MessagePassingQueue.Supplier<Entry>() {
                private int i = 0;
```

### BoundedWildcard
Can generalize to `? super T`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/http/AsyncHttpConnector.java`
#### Snippet
```java

    private <T> void retryOperation(
            final CompletableFuture<T> resultFuture,
            final Supplier<CompletableFuture<T>> operation,
            final int retries) {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/http/AsyncHttpConnector.java`
#### Snippet
```java
    private <T> void retryOperation(
            final CompletableFuture<T> resultFuture,
            final Supplier<CompletableFuture<T>> operation,
            final int retries) {

```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/BaseResource.java`
#### Snippet
```java
    }

    protected <T> T sync(Supplier<CompletableFuture<T>> executor) throws PulsarAdminException {
        try {
            return executor.get().get(this.readTimeoutMs, TimeUnit.MILLISECONDS);
```

### BoundedWildcard
Can generalize to `? super Response`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/BaseResource.java`
#### Snippet
```java
    }

    private <T> CompletableFuture<T> asyncGetRequest(final WebTarget target, Function<Response, T> readResponse) {
        final CompletableFuture<T> future = new CompletableFuture<>();
        asyncGetRequest(target,
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/BaseResource.java`
#### Snippet
```java
    }

    private <T> CompletableFuture<T> asyncGetRequest(final WebTarget target, Function<Response, T> readResponse) {
        final CompletableFuture<T> future = new CompletableFuture<>();
        asyncGetRequest(target,
```

### BoundedWildcard
Can generalize to `? extends T`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/BaseResource.java`
#### Snippet
```java
    }

    protected <T> CompletableFuture<T> asyncGetRequest(final WebTarget target, GenericType<T> type) {
        return asyncGetRequest(target, response -> response.readEntity(type));
    }
```

### BoundedWildcard
Can generalize to `? extends CustomCommand`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CustomCommandsUtils.java`
#### Snippet
```java
    public static class CmdBaseAdapter extends CmdBase {
        public CmdBaseAdapter(String cmdName, Supplier<PulsarAdmin> adminSupplier,
                              List<CustomCommand> customCommands, CommandExecutionContext context) {
            super(cmdName, adminSupplier);
            for (CustomCommand command : customCommands) {
```

### BoundedWildcard
Can generalize to `? extends PulsarAdmin`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
    private boolean help;

    public CmdBase(String cmdName, Supplier<PulsarAdmin> adminSupplier) {
        this.adminSupplier = adminSupplier;
        jcommander = new JCommander();
```

### BoundedWildcard
Can generalize to `? extends Completer`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/OptionStrictArgumentCompleter.java`
#### Snippet
```java
     * @param completers    The embedded completers
     */
    public OptionStrictArgumentCompleter(final Collection<Completer> completers) {
        Objects.requireNonNull(completers);
        this.completers.addAll(completers);
```

### BoundedWildcard
Can generalize to `? extends Completer`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/JCommanderCompleter.java`
#### Snippet
```java
    }

    private static void addCompletersForCommand(List<Completer> preCompleters,
                                                List<Completer> postCompleters,
                                                List<Completer> result,
```

### BoundedWildcard
Can generalize to `? extends Completer`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/JCommanderCompleter.java`
#### Snippet
```java

    private static void addCompletersForCommand(List<Completer> preCompleters,
                                                List<Completer> postCompleters,
                                                List<Completer> result,
                                                JCommander command,
```

### BoundedWildcard
Can generalize to `? super Completer`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/JCommanderCompleter.java`
#### Snippet
```java
    private static void addCompletersForCommand(List<Completer> preCompleters,
                                                List<Completer> postCompleters,
                                                List<Completer> result,
                                                JCommander command,
                                                ShellContext shellContext) {
```

### BoundedWildcard
Can generalize to `? extends Set`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java

    private Set<BookieId> getBlacklistedBookiesWithIsolationGroups(int ensembleSize,
        Pair<Set<String>, Set<String>> isolationGroups) {
        Set<BookieId> blacklistedBookies = new HashSet<>();
        if (isolationGroups != null && isolationGroups.getLeft().contains(PULSAR_SYSTEM_TOPIC_ISOLATION_GROUP))  {
```

### BoundedWildcard
Can generalize to `? extends Set`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java

    private Set<BookieId> getBlacklistedBookiesWithIsolationGroups(int ensembleSize,
        Pair<Set<String>, Set<String>> isolationGroups) {
        Set<BookieId> blacklistedBookies = new HashSet<>();
        if (isolationGroups != null && isolationGroups.getLeft().contains(PULSAR_SYSTEM_TOPIC_ISOLATION_GROUP))  {
```

### BoundedWildcard
Can generalize to `? extends SSLContext`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java
        private final SslContextAutoRefreshBuilder<SSLContext> sslCtxRefresher;

        public Server(String sslProviderString, SslContextAutoRefreshBuilder<SSLContext> sslCtxRefresher,
                      boolean requireTrustedClientCertOnConnect, Set<String> ciphers, Set<String> protocols) {
            super();
```

### BoundedWildcard
Can generalize to `? extends AdditionalServletWithClassLoader`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServlets.java`
#### Snippet
```java
    private final Map<String, AdditionalServletWithClassLoader> servlets;

    public AdditionalServlets(Map<String, AdditionalServletWithClassLoader> servlets) {
        this.servlets = servlets;
    }
```

### BoundedWildcard
Can generalize to `? extends PrometheusRawMetricsProvider`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsGeneratorUtils.java`
#### Snippet
```java

    public static void generate(String cluster, OutputStream out,
                                List<PrometheusRawMetricsProvider> metricsProviders)
            throws IOException {
        ByteBuf buf = ByteBufAllocator.DEFAULT.heapBuffer();
```

### BoundedWildcard
Can generalize to `? extends PersistentTopicInternalStats.LedgerInfo`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    }

    static MessageId findFirstLedgerWithinThreshold(List<PersistentTopicInternalStats.LedgerInfo> ledgers,
                                                    long sizeThreshold) {
        long suffixSize = 0L;
```

### BoundedWildcard
Can generalize to `? super Notification`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/ClusterResources.java`
#### Snippet
```java
        }

        public void registerListener(Consumer<Notification> listener) {
            getStore().registerListener(n -> {
                // Prefilter the notification just for failure domains
```

### BoundedWildcard
Can generalize to `? super Map`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
    }

    public void run(Function<Map<String, ShellCommandsProvider>, InteractiveLineReader> readerBuilder,
                    Function<Map<String, ShellCommandsProvider>, Terminal> terminalBuilder) throws Exception {
        this.readerBuilder = readerBuilder;
```

### BoundedWildcard
Can generalize to `? extends InteractiveLineReader`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
    }

    public void run(Function<Map<String, ShellCommandsProvider>, InteractiveLineReader> readerBuilder,
                    Function<Map<String, ShellCommandsProvider>, Terminal> terminalBuilder) throws Exception {
        this.readerBuilder = readerBuilder;
```

### BoundedWildcard
Can generalize to `? super Map`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java

    public void run(Function<Map<String, ShellCommandsProvider>, InteractiveLineReader> readerBuilder,
                    Function<Map<String, ShellCommandsProvider>, Terminal> terminalBuilder) throws Exception {
        this.readerBuilder = readerBuilder;
        /**
```

### BoundedWildcard
Can generalize to `? extends Terminal`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java

    public void run(Function<Map<String, ShellCommandsProvider>, InteractiveLineReader> readerBuilder,
                    Function<Map<String, ShellCommandsProvider>, Terminal> terminalBuilder) throws Exception {
        this.readerBuilder = readerBuilder;
        /**
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
    private static void registerProvider(ShellCommandsProvider provider,
                                         JCommander commander,
                                         Map<String, ShellCommandsProvider> providerMap) {

        final String name = provider.getName();
```

### BoundedWildcard
Can generalize to `? super ShellCommandsProvider`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
    private static void registerProvider(ShellCommandsProvider provider,
                                         JCommander commander,
                                         Map<String, ShellCommandsProvider> providerMap) {

        final String name = provider.getName();
```

### BoundedWildcard
Can generalize to `? super String`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/MultiRolesTokenAuthorizationProvider.java`
#### Snippet
```java
    }

    public CompletableFuture<Boolean> authorize(AuthenticationDataSource authenticationData, Function<String,
            CompletableFuture<Boolean>> authorizeFunc) {
        Set<String> roles = getRoles(authenticationData);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/MultiRolesTokenAuthorizationProvider.java`
#### Snippet
```java

    public CompletableFuture<Boolean> authorize(AuthenticationDataSource authenticationData, Function<String,
            CompletableFuture<Boolean>> authorizeFunc) {
        Set<String> roles = getRoles(authenticationData);
        if (roles.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends Claims`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderToken.java`
#### Snippet
```java
    }

    private String getPrincipal(Jwt<?, Claims> jwt) {
        try {
            return jwt.getBody().get(roleClaim, String.class);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java

        public CompletableFuture<Void> runWithMarkDeleteAsync(TopicName topic,
                                                              Supplier<CompletableFuture<Void>> supplier) {
            CompletableFuture<Void> future = new CompletableFuture<>();

```

### BoundedWildcard
Can generalize to `? extends W`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderList.java`
#### Snippet
```java
    }

    static <T, W> T applyAuthProcessor(List<W> processors, AuthProcessor<T, W> authFunc)
        throws AuthenticationException {
        AuthenticationException authenticationException = null;
```

### BoundedWildcard
Can generalize to `? super W`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderList.java`
#### Snippet
```java
    }

    static <T, W> T applyAuthProcessor(List<W> processors, AuthProcessor<T, W> authFunc)
        throws AuthenticationException {
        AuthenticationException authenticationException = null;
```

### BoundedWildcard
Can generalize to `? extends Date`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/utils/AuthTokenUtils.java`
#### Snippet
```java
    }

    public static String createToken(Key signingKey, String subject, Optional<Date> expiryTime) {
        JwtBuilder builder = Jwts.builder()
                .setSubject(subject)
```

### BoundedWildcard
Can generalize to `? super String`
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/EventResourcesImpl.java`
#### Snippet
```java
    }

    public static void forEach(List<Object> list, BiConsumer<String, String> process) {
        for (int i = 0; i < list.size() - 1; i += 2) {
            String key = String.valueOf(list.get(i));
```

### BoundedWildcard
Can generalize to `? super String`
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/EventResourcesImpl.java`
#### Snippet
```java
    }

    public static void forEach(List<Object> list, BiConsumer<String, String> process) {
        for (int i = 0; i < list.size() - 1; i += 2) {
            String key = String.valueOf(list.get(i));
```

### BoundedWildcard
Can generalize to `? extends BrokerStatus`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/impl/MinAvailablePolicy.java`
#### Snippet
```java

    @Override
    public boolean shouldFailoverToSecondary(SortedSet<BrokerStatus> primaryCandidates) {
        int numAvailablePrimaryBrokers = 0;
        for (BrokerStatus brkStatus : primaryCandidates) {
```

### BoundedWildcard
Can generalize to `? super List`>
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java

    private void peekMessagesAsync(String topic, String subName, int numMessages,
            List<Message<byte[]>> messages, CompletableFuture<List<Message<byte[]>>> future, int nthMessage) {
        if (numMessages <= 0) {
            future.complete(messages);
```

### BoundedWildcard
Can generalize to `? extends MessageId`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
    public CompletableFuture<AnalyzeSubscriptionBacklogResult> analyzeSubscriptionBacklogAsync(String topic,
                                                                                String subscriptionName,
                                                                                Optional<MessageId> startPosition) {
        TopicName topicName = validateTopic(topic);
        String encodedSubName = Codec.encode(subscriptionName);
```

### BoundedWildcard
Can generalize to `? super Map`
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
                                    AuthenticationDataProvider authData,
                                    Map<String, String> previousResHeaders,
                                    CompletableFuture<Map<String, String>> authFuture) {
        // a new request for sasl auth
        Builder builder = newRequestBuilder(client.target(requestUrl), authData, previousResHeaders);
```

## RuleId[ruleID=MissortedModifiers]
### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}")
    public Response listFunctions(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace) {
        return functions().listFunctions(tenant, namespace, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}")
    public Response listFunctions(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace) {
        return functions().listFunctions(tenant, namespace, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) {
        return functions().restartFunctionInstances(tenant, namespace, functionName, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) {
        return functions().restartFunctionInstances(tenant, namespace, functionName, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}")
    public Response deregisterFunction(final @PathParam("tenant") String tenant,
                                       final @PathParam("namespace") String namespace,
                                       final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    public Response deregisterFunction(final @PathParam("tenant") String tenant,
                                       final @PathParam("namespace") String namespace,
                                       final @PathParam("functionName") String functionName) {
        return functions().deregisterFunction(tenant, namespace, functionName, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response deregisterFunction(final @PathParam("tenant") String tenant,
                                       final @PathParam("namespace") String namespace,
                                       final @PathParam("functionName") String functionName) {
        return functions().deregisterFunction(tenant, namespace, functionName, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response updateFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response updateFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response updateFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                   final @PathParam("functionName") String functionName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("functionDetails") String functionDetailsJson) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("functionDetails") String functionDetailsJson) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionDetails")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("functionDetails") String functionDetailsJson) {

        return functions().updateFunction(tenant, namespace, functionName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName) {
        return functions().stopFunctionInstances(tenant, namespace, functionName, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName) {
        return functions().stopFunctionInstances(tenant, namespace, functionName, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/status")
    public Response getFunctionInstanceStatus(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/status")
    public Response getFunctionInstanceStatus(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName,
                                              final @PathParam("instanceId") String instanceId) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response getFunctionInstanceStatus(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName,
                                              final @PathParam("instanceId") String instanceId) throws IOException {

```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName,
                                              final @PathParam("instanceId") String instanceId) throws IOException {

        return functions().getFunctionInstanceStatus(tenant, namespace, functionName, instanceId, uri.getRequestUri(),
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/upload")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response uploadFunction(final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("path") String path) {
        return functions().uploadFunction(uploadedInputStream, path, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("path")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response uploadFunction(final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("path") String path) {
        return functions().uploadFunction(uploadedInputStream, path, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("instanceId") String instanceId) {
        return functions().stopFunctionInstance(tenant, namespace, functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("instanceId") String instanceId) {
        return functions().stopFunctionInstance(tenant, namespace, functionName,
                instanceId, uri.getRequestUri(), clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}/state/{key}")
    public Response getFunctionState(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/state/{key}")
    public Response getFunctionState(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @PathParam("key") String key) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response getFunctionState(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @PathParam("key") String key) {
        return functions().getFunctionState(tenant, namespace, functionName, key, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("key")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @PathParam("key") String key) {
        return functions().getFunctionState(tenant, namespace, functionName, key, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}/status")
    public Response getFunctionStatus(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/status")
    public Response getFunctionStatus(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStatusV2(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response getFunctionStatus(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStatusV2(
                tenant, namespace, functionName, uri.getRequestUri(), clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}")
    public Response getFunctionInfo(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    public Response getFunctionInfo(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) throws IOException {

```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response getFunctionInfo(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) throws IOException {

        return functions().getFunctionInfo(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response registerFunction(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response registerFunction(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response registerFunction(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @FormDataParam("data") InputStream uploadedInputStream,
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @FormDataParam("data") InputStream uploadedInputStream,
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                     final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                     final @PathParam("functionName") String functionName,
                                     final @FormDataParam("data") InputStream uploadedInputStream,
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                     final @FormDataParam("url") String functionPkgUrl,
                                     final @FormDataParam("functionDetails") String functionDetailsJson) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                     final @FormDataParam("data") InputStream uploadedInputStream,
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                     final @FormDataParam("url") String functionPkgUrl,
                                     final @FormDataParam("functionDetails") String functionDetailsJson) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionDetails")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                     final @FormDataParam("url") String functionPkgUrl,
                                     final @FormDataParam("functionDetails") String functionDetailsJson) {

        return functions().registerFunction(tenant, namespace, functionName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @QueryParam("path")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    )
    @Path("/download")
    public Response downloadFunction(final @QueryParam("path") String path) {
        return functions().downloadFunction(path, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @PathParam("instanceId") String instanceId) {
        return functions().restartFunctionInstance(tenant, namespace, functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @PathParam("instanceId") String instanceId) {
        return functions().restartFunctionInstance(tenant, namespace, functionName,
                instanceId, uri.getRequestUri(), clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/trigger")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response triggerFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response triggerFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @FormDataParam("data") String triggerValue,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
    public Response triggerFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @FormDataParam("data") String triggerValue,
                                    final @FormDataParam("dataStream") InputStream triggerStream,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @FormDataParam("data") String triggerValue,
                                    final @FormDataParam("dataStream") InputStream triggerStream,
                                    final @FormDataParam("topic") String topic) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("dataStream")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                    final @PathParam("functionName") String functionName,
                                    final @FormDataParam("data") String triggerValue,
                                    final @FormDataParam("dataStream") InputStream triggerStream,
                                    final @FormDataParam("topic") String topic) {
        return functions().triggerFunction(tenant, namespace, functionName,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("topic")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java
                                    final @FormDataParam("data") String triggerValue,
                                    final @FormDataParam("dataStream") InputStream triggerStream,
                                    final @FormDataParam("topic") String topic) {
        return functions().triggerFunction(tenant, namespace, functionName,
                triggerValue, triggerStream, topic, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("type")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    )
    public void listPackageVersion(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    public void listPackageVersion(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        @Suspended AsyncResponse asyncResponse
```

### MissortedModifiers
Missorted modifiers `final @PathParam("packageName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        @Suspended AsyncResponse asyncResponse
    ) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("type")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    )
    public StreamingOutput download(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    public StreamingOutput download(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version
```

### MissortedModifiers
Missorted modifiers `final @PathParam("packageName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version
        ) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("version")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version
        ) {
        return internalDownload(type, tenant, namespace, packageName, version);
```

### MissortedModifiers
Missorted modifiers `final @PathParam("type")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    @ApiOperation(value = "Delete a package with the package name.")
    public void delete(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    public void delete(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("packageName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        @Suspended AsyncResponse asyncResponse
```

### MissortedModifiers
Missorted modifiers `final @PathParam("version")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        @Suspended AsyncResponse asyncResponse
    ){
```

### MissortedModifiers
Missorted modifiers `final @PathParam("type")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    )
    public void getMeta(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    public void getMeta(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("packageName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        @Suspended AsyncResponse asyncResponse
```

### MissortedModifiers
Missorted modifiers `final @PathParam("version")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        @Suspended AsyncResponse asyncResponse
    ) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("type")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    )
    public void listPackages(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    public void listPackages(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        @Suspended AsyncResponse asyncResponse
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        @Suspended AsyncResponse asyncResponse
    ) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("type")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void upload(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    public void upload(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("packageName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        final @FormDataParam("metadata") PackageMetadata packageMetadata,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("version")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        final @FormDataParam("metadata") PackageMetadata packageMetadata,
        final @FormDataParam("file") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("metadata")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        final @FormDataParam("metadata") PackageMetadata packageMetadata,
        final @FormDataParam("file") InputStream uploadedInputStream,
        @Suspended AsyncResponse asyncResponse) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("file")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("version") String version,
        final @FormDataParam("metadata") PackageMetadata packageMetadata,
        final @FormDataParam("file") InputStream uploadedInputStream,
        @Suspended AsyncResponse asyncResponse) {
        if (packageMetadata != null) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("type")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void updateMeta(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
    public void updateMeta(
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("type") String type,
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("packageName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("tenant") String tenant,
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        final PackageMetadata metadata,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("version")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v3/Packages.java`
#### Snippet
```java
        final @PathParam("namespace") String namespace,
        final @PathParam("packageName") String packageName,
        final @PathParam("version") String version,
        final PackageMetadata metadata,
        @Suspended AsyncResponse asyncResponse
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void stopSource(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) {
        sources().stopFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public SourceConfig getSourceInfo(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) throws IOException {
        return sources().getSourceInfo(tenant, namespace, sourceName);
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("name")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public List<ConfigFieldDefinition> getSourceConfigDefinition(
            @ApiParam(value = "The name of the builtin source")
            final @PathParam("name") String name) throws IOException {
        return sources().getSourceConfigDefinition(name);
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void startSource(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) {
        sources().startFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/{instanceId}/status")
    public SourceStatus.SourceInstanceStatus.SourceInstanceStatusData getSourceInstanceStatus(
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public SourceStatus.SourceInstanceStatus.SourceInstanceStatusData getSourceInstanceStatus(
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source"
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source"
                    + " (if instance-id is not provided, the stats of all instances is returned).") final @PathParam(
```

### MissortedModifiers
Missorted modifiers `final @PathParam(
"instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source"
                    + " (if instance-id is not provided, the stats of all instances is returned).") final @PathParam(
                    "instanceId") String instanceId) throws IOException {
        return sources().getSourceInstanceStatus(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public List<String> listSources(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace) {
        return sources().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSource(
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void stopSource(
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source (if instance-id is not provided,"
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source (if instance-id is not provided,"
                    + " the stats of all instances is returned).") final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source (if instance-id is not provided,"
                    + " the stats of all instances is returned).") final @PathParam("instanceId") String instanceId) {
        sources().stopFunctionInstance(tenant, namespace, sourceName, instanceId,
                uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void registerSource(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String sourcePkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("sourceName") String sourceName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String sourcePkgUrl,
            @ApiParam(value =
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String sourcePkgUrl,
            @ApiParam(value =
                    "You can submit a source (in any languages that you are familiar with) to a Pulsar cluster. "
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("sourceConfig")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
                    )
            )
            final @FormDataParam("sourceConfig") SourceConfig sourceConfig) {
        sources().registerSource(tenant, namespace, sourceName, uploadedInputStream, fileDetail,
            sourcePkgUrl, sourceConfig, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public SourceStatus getSourceStatus(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) throws IOException {
        return sources().getSourceStatus(tenant, namespace, sourceName, uri.getRequestUri(), clientAppId(),
                clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void deregisterSource(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) {
        sources().deregisterFunction(tenant, namespace, sourceName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSource(
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void startSource(
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source (if instance-id is not provided,"
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source (if instance-id is not provided,"
                    + " the stats of all instances is returned).") final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source (if instance-id is not provided,"
                    + " the stats of all instances is returned).") final @PathParam("instanceId") String instanceId) {
        sources().startFunctionInstance(tenant, namespace, sourceName, instanceId,
                uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void restartSource(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName) {
        sources().restartFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSource(
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void restartSource(
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source"
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Source") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source"
                    + " (if instance-id is not provided, the stats of all instances is returned).") final @PathParam(
```

### MissortedModifiers
Missorted modifiers `final @PathParam(
"instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Source") final @PathParam("sourceName") String sourceName,
            @ApiParam(value = "The instanceId of a Pulsar Source"
                    + " (if instance-id is not provided, the stats of all instances is returned).") final @PathParam(
                    "instanceId") String instanceId) {
        sources().restartFunctionInstance(tenant, namespace, sourceName, instanceId,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    public void updateSource(
            @ApiParam(value = "The tenant of a Pulsar Source")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Source")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Source")
            final @PathParam("sourceName") String sourceName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String sourcePkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @PathParam("sourceName") String sourceName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String sourcePkgUrl,
            @ApiParam(
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String sourcePkgUrl,
            @ApiParam(
                    value = "A JSON value presenting configuration payload of a Pulsar Source."
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("sourceConfig")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
                    )
            )
            final @FormDataParam("sourceConfig") SourceConfig sourceConfig,
            @ApiParam(value = "Update options for Pulsar Source")
            final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("updateOptions")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
            final @FormDataParam("sourceConfig") SourceConfig sourceConfig,
            @ApiParam(value = "Update options for Pulsar Source")
            final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {
        sources().updateSource(tenant, namespace, sourceName, uploadedInputStream, fileDetail,
            sourcePkgUrl, sourceConfig, clientAppId(), clientAuthData(), updateOptions);
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public StreamingOutput downloadFunction(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
            @ApiParam(value = "Whether to download the transform-function")
            final @QueryParam("transform-function") boolean transformFunction) {
```

### MissortedModifiers
Missorted modifiers `final @QueryParam("transform-function")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("functionName") String functionName,
            @ApiParam(value = "Whether to download the transform-function")
            final @QueryParam("transform-function") boolean transformFunction) {

        return functions()
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void restartFunction(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) {
        functions().restartFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public FunctionStatus getFunctionStatus(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStatus(tenant, namespace, functionName, uri.getRequestUri(),
                clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void deregisterFunction(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) {
        functions().deregisterFunction(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void updateFunction(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("functionName") String functionName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String functionPkgUrl,
            @ApiParam(
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String functionPkgUrl,
            @ApiParam(
                    value = "A JSON value presenting configuration payload of a Pulsar Function."
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionConfig")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
                    )
            )
            final @FormDataParam("functionConfig") FunctionConfig functionConfig,
            @ApiParam(value = "The update options is for the Pulsar Function that needs to be updated.")
            final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("updateOptions")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @FormDataParam("functionConfig") FunctionConfig functionConfig,
            @ApiParam(value = "The update options is for the Pulsar Function that needs to be updated.")
            final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) throws IOException {

        functions().updateFunction(tenant, namespace, functionName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartFunction(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void restartFunction(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value =
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value =
                    "The instanceId of a Pulsar Function (if instance-id is not provided, all instances are restarted")
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value =
                    "The instanceId of a Pulsar Function (if instance-id is not provided, all instances are restarted")
            final @PathParam("instanceId") String instanceId) {
        functions().restartFunctionInstance(tenant, namespace, functionName, instanceId,
                uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public FunctionConfig getFunctionInfo(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionInfo(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Path("/upload")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void uploadFunction(final @FormDataParam("data") InputStream uploadedInputStream,
                               final @FormDataParam("path") String path) {
        functions().uploadFunction(uploadedInputStream, path, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("path")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void uploadFunction(final @FormDataParam("data") InputStream uploadedInputStream,
                               final @FormDataParam("path") String path) {
        functions().uploadFunction(uploadedInputStream, path, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Path("/leader/{tenant}/{namespace}/{functionName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateFunctionOnWorkerLeader(final @PathParam("tenant") String tenant,
                                             final @PathParam("namespace") String namespace,
                                             final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateFunctionOnWorkerLeader(final @PathParam("tenant") String tenant,
                                             final @PathParam("namespace") String namespace,
                                             final @PathParam("functionName") String functionName,
                                             final @FormDataParam("functionMetaData")
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void updateFunctionOnWorkerLeader(final @PathParam("tenant") String tenant,
                                             final @PathParam("namespace") String namespace,
                                             final @PathParam("functionName") String functionName,
                                             final @FormDataParam("functionMetaData")
                                                         InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionMetaData")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
                                             final @PathParam("namespace") String namespace,
                                             final @PathParam("functionName") String functionName,
                                             final @FormDataParam("functionMetaData")
                                                         InputStream uploadedInputStream,
                                             final @FormDataParam("delete") boolean delete) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("delete")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
                                             final @FormDataParam("functionMetaData")
                                                         InputStream uploadedInputStream,
                                             final @FormDataParam("delete") boolean delete) {

        functions().updateFunctionOnWorkerLeader(tenant, namespace, functionName, uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @QueryParam("path")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    )
    @Path("/download")
    public StreamingOutput downloadFunction(final @QueryParam("path") String path) {
        return functions().downloadFunction(path, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/state/{key}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void putFunctionState(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void putFunctionState(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("key") String key,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void putFunctionState(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("key") String key,
                                 final @FormDataParam("state") FunctionState stateJson) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("key")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("key") String key,
                                 final @FormDataParam("state") FunctionState stateJson) {
        functions().putFunctionState(tenant, namespace, functionName, key, stateJson, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("state")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("key") String key,
                                 final @FormDataParam("state") FunctionState stateJson) {
        functions().putFunctionState(tenant, namespace, functionName, key, stateJson, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void startFunction(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) {
        functions().startFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/status")
    public FunctionStatus.FunctionInstanceStatus.FunctionInstanceStatusData getFunctionInstanceStatus(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public FunctionStatus.FunctionInstanceStatus.FunctionInstanceStatusData getFunctionInstanceStatus(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function (if instance-id is not provided,"
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function (if instance-id is not provided,"
                    + " the stats of all instances is returned") final @PathParam("instanceId")
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function (if instance-id is not provided,"
                    + " the stats of all instances is returned") final @PathParam("instanceId")
                    String instanceId) throws IOException {
        return functions().getFunctionInstanceStatus(tenant, namespace, functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void registerFunction(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("functionName") String functionName,
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String functionPkgUrl,
            @ApiParam(
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @FormDataParam("data") InputStream uploadedInputStream,
            final @FormDataParam("data") FormDataContentDisposition fileDetail,
            final @FormDataParam("url") String functionPkgUrl,
            @ApiParam(
                    value = "You can submit a function (in any languages that you are familiar with) \n"
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionConfig")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
                    )
            )
            final @FormDataParam("functionConfig") FunctionConfig functionConfig) {
        functions().registerFunction(tenant, namespace, functionName, uploadedInputStream, fileDetail,
            functionPkgUrl, functionConfig, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public FunctionState getFunctionState(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName,
            @ApiParam(value = "The stats key")
            final @PathParam("key") String key) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("key")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("functionName") String functionName,
            @ApiParam(value = "The stats key")
            final @PathParam("key") String key) {
        return functions().getFunctionState(tenant, namespace, functionName, key, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public List<String> listFunctions(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace) {
        return functions().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public FunctionStatsImpl getFunctionStats(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStats(tenant, namespace, functionName,
                uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public String triggerFunction(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public String triggerFunction(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The value with which you want to trigger the Pulsar Function") final @FormDataParam(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The value with which you want to trigger the Pulsar Function") final @FormDataParam(
                    "data") String triggerValue,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam(
"data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The value with which you want to trigger the Pulsar Function") final @FormDataParam(
                    "data") String triggerValue,
            @ApiParam(value = "The path to the file that contains the data with"
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("dataStream")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
                    "data") String triggerValue,
            @ApiParam(value = "The path to the file that contains the data with"
                    + " which you'd like to trigger the Pulsar Function") final @FormDataParam("dataStream")
                    InputStream triggerStream,
            @ApiParam(value = "The specific topic name that the Pulsar Function"
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("topic")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
                    InputStream triggerStream,
            @ApiParam(value = "The specific topic name that the Pulsar Function"
                    + " consumes from which you want to inject the data to") final @FormDataParam("topic")
                    String topic) {
        return functions().triggerFunction(tenant, namespace, functionName, triggerValue,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startFunction(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void startFunction(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function"
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function"
                    + " (if instance-id is not provided, all instances sre started. ") final @PathParam("instanceId")
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function"
                    + " (if instance-id is not provided, all instances sre started. ") final @PathParam("instanceId")
                    String instanceId) {
        functions().startFunctionInstance(tenant, namespace, functionName, instanceId,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/stats")
    public FunctionInstanceStatsDataImpl getFunctionInstanceStats(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public FunctionInstanceStatsDataImpl getFunctionInstanceStats(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function"
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function"
                    + " (if instance-id is not provided, the stats of all instances is returned") final @PathParam(
```

### MissortedModifiers
Missorted modifiers `final @PathParam(
"instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value = "The instanceId of a Pulsar Function"
                    + " (if instance-id is not provided, the stats of all instances is returned") final @PathParam(
                    "instanceId") String instanceId) throws IOException {
        return functions().getFunctionsInstanceStats(tenant, namespace, functionName, instanceId,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void stopFunction(
            @ApiParam(value = "The tenant of a Pulsar Function")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function")
            final @PathParam("functionName") String functionName) {
        functions().stopFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopFunction(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
    public void stopFunction(
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value =
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value = "The tenant of a Pulsar Function") final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Function") final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Function") final @PathParam("functionName") String functionName,
            @ApiParam(value =
                    "The instanceId of a Pulsar Function (if instance-id is not provided, all instances are stopped. ")
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java
            @ApiParam(value =
                    "The instanceId of a Pulsar Function (if instance-id is not provided, all instances are stopped. ")
            final @PathParam("instanceId") String instanceId) {
        functions().stopFunctionInstance(tenant, namespace, functionName, instanceId,
                uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}")
    public void deregisterSink(@ApiParam(value = "The tenant of a Pulsar Sink")
                               final @PathParam("tenant") String tenant,
                               @ApiParam(value = "The namespace of a Pulsar Sink")
                               final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                               final @PathParam("tenant") String tenant,
                               @ApiParam(value = "The namespace of a Pulsar Sink")
                               final @PathParam("namespace") String namespace,
                               @ApiParam(value = "The name of a Pulsar Sink")
                               final @PathParam("sinkName") String sinkName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                               final @PathParam("namespace") String namespace,
                               @ApiParam(value = "The name of a Pulsar Sink")
                               final @PathParam("sinkName") String sinkName) {
        sinks().deregisterFunction(tenant, namespace, sinkName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSink(@ApiParam(value = "The tenant of a Pulsar Sink")
                          final @PathParam("tenant") String tenant,
                          @ApiParam(value = "The namespace of a Pulsar Sink")
                          final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                          final @PathParam("tenant") String tenant,
                          @ApiParam(value = "The namespace of a Pulsar Sink")
                          final @PathParam("namespace") String namespace,
                          @ApiParam(value = "The name of a Pulsar Sink")
                          final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                          final @PathParam("namespace") String namespace,
                          @ApiParam(value = "The name of a Pulsar Sink")
                          final @PathParam("sinkName") String sinkName,
                          @ApiParam(value = "The instanceId of a Pulsar Sink")
                          final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                          final @PathParam("sinkName") String sinkName,
                          @ApiParam(value = "The instanceId of a Pulsar Sink")
                          final @PathParam("instanceId") String instanceId) {
        sinks().startFunctionInstance(tenant, namespace, sinkName, instanceId,
                uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSink(@ApiParam(value = "The tenant of a Pulsar Sink")
                         final @PathParam("tenant") String tenant,
                         @ApiParam(value = "The namespace of a Pulsar Sink")
                         final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                         final @PathParam("tenant") String tenant,
                         @ApiParam(value = "The namespace of a Pulsar Sink")
                         final @PathParam("namespace") String namespace,
                         @ApiParam(value = "The name of a Pulsar Sink")
                         final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                         final @PathParam("namespace") String namespace,
                         @ApiParam(value = "The name of a Pulsar Sink")
                         final @PathParam("sinkName") String sinkName,
                         @ApiParam(value = "The instanceId of a Pulsar Sink")
                         final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                         final @PathParam("sinkName") String sinkName,
                         @ApiParam(value = "The instanceId of a Pulsar Sink")
                         final @PathParam("instanceId") String instanceId) {
        sinks().stopFunctionInstance(tenant, namespace,
                sinkName, instanceId, uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSink(@ApiParam(value = "The tenant of a Pulsar Sink")
                            final @PathParam("tenant") String tenant,
                            @ApiParam(value = "The namespace of a Pulsar Sink")
                            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                            final @PathParam("tenant") String tenant,
                            @ApiParam(value = "The namespace of a Pulsar Sink")
                            final @PathParam("namespace") String namespace,
                            @ApiParam(value = "The name of a Pulsar Sink")
                            final @PathParam("sinkName") String sinkName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                            final @PathParam("namespace") String namespace,
                            @ApiParam(value = "The name of a Pulsar Sink")
                            final @PathParam("sinkName") String sinkName) {
        sinks().restartFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSink(@ApiParam(value = "The tenant of a Pulsar Sink")
                          final @PathParam("tenant") String tenant,
                          @ApiParam(value = "The namespace of a Pulsar Sink")
                          final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                          final @PathParam("tenant") String tenant,
                          @ApiParam(value = "The namespace of a Pulsar Sink")
                          final @PathParam("namespace") String namespace,
                          @ApiParam(value = "The name of a Pulsar Sink")
                          final @PathParam("sinkName") String sinkName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                          final @PathParam("namespace") String namespace,
                          @ApiParam(value = "The name of a Pulsar Sink")
                          final @PathParam("sinkName") String sinkName) {
        sinks().startFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}")
    public List<String> listSinks(@ApiParam(value = "The tenant of a Pulsar Sink")
                                  final @PathParam("tenant") String tenant,
                                  @ApiParam(value = "The namespace of a Pulsar Sink")
                                  final @PathParam("namespace") String namespace) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                                  final @PathParam("tenant") String tenant,
                                  @ApiParam(value = "The namespace of a Pulsar Sink")
                                  final @PathParam("namespace") String namespace) {
        return sinks().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/status")
    public SinkStatus getSinkStatus(@ApiParam(value = "The tenant of a Pulsar Sink")
                                    final @PathParam("tenant") String tenant,
                                    @ApiParam(value = "The namespace of a Pulsar Sink")
                                    final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                                    final @PathParam("tenant") String tenant,
                                    @ApiParam(value = "The namespace of a Pulsar Sink")
                                    final @PathParam("namespace") String namespace,
                                    @ApiParam(value = "The name of a Pulsar Sink")
                                    final @PathParam("sinkName") String sinkName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                                    final @PathParam("namespace") String namespace,
                                    @ApiParam(value = "The name of a Pulsar Sink")
                                    final @PathParam("sinkName") String sinkName) throws IOException {
        return sinks().getSinkStatus(tenant, namespace, sinkName, uri.getRequestUri(), clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void registerSink(@ApiParam(value = "The tenant of a Pulsar Sink") final @PathParam("tenant") String tenant,
                             @ApiParam(value = "The namespace of a Pulsar Sink") final @PathParam("namespace")
                                     String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void registerSink(@ApiParam(value = "The tenant of a Pulsar Sink") final @PathParam("tenant") String tenant,
                             @ApiParam(value = "The namespace of a Pulsar Sink") final @PathParam("namespace")
                                     String namespace,
                             @ApiParam(value = "The name of a Pulsar Sink") final @PathParam("sinkName")
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                             @ApiParam(value = "The namespace of a Pulsar Sink") final @PathParam("namespace")
                                     String namespace,
                             @ApiParam(value = "The name of a Pulsar Sink") final @PathParam("sinkName")
                                         String sinkName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                             @ApiParam(value = "The name of a Pulsar Sink") final @PathParam("sinkName")
                                         String sinkName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String sinkPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                                         String sinkName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String sinkPkgUrl,
                             @ApiParam(value =
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String sinkPkgUrl,
                             @ApiParam(value =
                                     "You can submit a sink (in any languages that you are familiar with) "
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("sinkConfig")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                                    )
                             )
                             final @FormDataParam("sinkConfig") SinkConfig sinkConfig) {
        sinks().registerSink(tenant, namespace, sinkName, uploadedInputStream, fileDetail,
                sinkPkgUrl, sinkConfig, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateSink(@ApiParam(value = "The tenant of a Pulsar Sink") final @PathParam("tenant") String tenant,
                           @ApiParam(value = "The namespace of a Pulsar Sink") final @PathParam("namespace")
                                   String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateSink(@ApiParam(value = "The tenant of a Pulsar Sink") final @PathParam("tenant") String tenant,
                           @ApiParam(value = "The namespace of a Pulsar Sink") final @PathParam("namespace")
                                   String namespace,
                           @ApiParam(value = "The name of a Pulsar Sink") final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                           @ApiParam(value = "The namespace of a Pulsar Sink") final @PathParam("namespace")
                                   String namespace,
                           @ApiParam(value = "The name of a Pulsar Sink") final @PathParam("sinkName") String sinkName,
                           final @FormDataParam("data") InputStream uploadedInputStream,
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                                   String namespace,
                           @ApiParam(value = "The name of a Pulsar Sink") final @PathParam("sinkName") String sinkName,
                           final @FormDataParam("data") InputStream uploadedInputStream,
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
                           final @FormDataParam("url") String sinkPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                           @ApiParam(value = "The name of a Pulsar Sink") final @PathParam("sinkName") String sinkName,
                           final @FormDataParam("data") InputStream uploadedInputStream,
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
                           final @FormDataParam("url") String sinkPkgUrl,
                           @ApiParam(value =
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                           final @FormDataParam("data") InputStream uploadedInputStream,
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
                           final @FormDataParam("url") String sinkPkgUrl,
                           @ApiParam(value =
                                   "A JSON value presenting config payload of a Pulsar Sink."
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("sinkConfig")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                               )
                           )
                           final @FormDataParam("sinkConfig") SinkConfig sinkConfig,
                           @ApiParam(value = "Update options for the Pulsar Sink")
                           final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("updateOptions")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                           final @FormDataParam("sinkConfig") SinkConfig sinkConfig,
                           @ApiParam(value = "Update options for the Pulsar Sink")
                           final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {
         sinks().updateSink(tenant, namespace, sinkName, uploadedInputStream, fileDetail,
                sinkPkgUrl, sinkConfig, clientAppId(), clientAuthData(), updateOptions);
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSink(@ApiParam(value = "The tenant of a Pulsar Sink")
                            final @PathParam("tenant") String tenant,
                            @ApiParam(value = "The namespace of a Pulsar Sink")
                            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                            final @PathParam("tenant") String tenant,
                            @ApiParam(value = "The namespace of a Pulsar Sink")
                            final @PathParam("namespace") String namespace,
                            @ApiParam(value = "The name of a Pulsar Sink")
                            final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                            final @PathParam("namespace") String namespace,
                            @ApiParam(value = "The name of a Pulsar Sink")
                            final @PathParam("sinkName") String sinkName,
                            @ApiParam(value = "The instanceId of a Pulsar Sink")
                            final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                            final @PathParam("sinkName") String sinkName,
                            @ApiParam(value = "The instanceId of a Pulsar Sink")
                            final @PathParam("instanceId") String instanceId) {
        sinks().restartFunctionInstance(tenant, namespace, sinkName, instanceId,
                uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    public SinkStatus.SinkInstanceStatus.SinkInstanceStatusData getSinkInstanceStatus(
            @ApiParam(value = "The tenant of a Pulsar Sink")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Sink")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of a Pulsar Sink")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Sink")
            final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of a Pulsar Sink")
            final @PathParam("sinkName") String sinkName,
            @ApiParam(value = "The instanceId of a Pulsar Sink")
            final @PathParam("instanceId") String instanceId) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
            final @PathParam("sinkName") String sinkName,
            @ApiParam(value = "The instanceId of a Pulsar Sink")
            final @PathParam("instanceId") String instanceId) throws IOException {
        return sinks().getSinkInstanceStatus(
            tenant, namespace, sinkName, instanceId, uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}")
    public SinkConfig getSinkInfo(@ApiParam(value = "The tenant of a Pulsar Sink")
                                  final @PathParam("tenant") String tenant,
                                  @ApiParam(value = "The namespace of a Pulsar Sink")
                                  final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                                  final @PathParam("tenant") String tenant,
                                  @ApiParam(value = "The namespace of a Pulsar Sink")
                                  final @PathParam("namespace") String namespace,
                                  @ApiParam(value = "The name of a Pulsar Sink")
                                  final @PathParam("sinkName") String sinkName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                                  final @PathParam("namespace") String namespace,
                                  @ApiParam(value = "The name of a Pulsar Sink")
                                  final @PathParam("sinkName") String sinkName) throws IOException {
        return sinks().getSinkInfo(tenant, namespace, sinkName);
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSink(@ApiParam(value = "The tenant of a Pulsar Sink")
                         final @PathParam("tenant") String tenant,
                         @ApiParam(value = "The namespace of a Pulsar Sink")
                         final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                         final @PathParam("tenant") String tenant,
                         @ApiParam(value = "The namespace of a Pulsar Sink")
                         final @PathParam("namespace") String namespace,
                         @ApiParam(value = "The name of a Pulsar Sink")
                         final @PathParam("sinkName") String sinkName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
                         final @PathParam("namespace") String namespace,
                         @ApiParam(value = "The name of a Pulsar Sink")
                         final @PathParam("sinkName") String sinkName) {
        sinks().stopFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("name")`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    public List<ConfigFieldDefinition> getSinkConfigDefinition(
            @ApiParam(value = "The name of the builtin sink")
            final @PathParam("name") String name) throws IOException {
        return sinks().getSinkConfigDefinition(name);
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/status")
    public Response getFunctionInstanceStatus(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/status")
    public Response getFunctionInstanceStatus(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName,
                                              final @PathParam("instanceId") String instanceId) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response getFunctionInstanceStatus(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName,
                                              final @PathParam("instanceId") String instanceId) throws IOException {

```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName,
                                              final @PathParam("instanceId") String instanceId) throws IOException {

        return functions().getFunctionInstanceStatus(tenant, namespace, functionName, instanceId, uri.getRequestUri(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}")
    public Response getFunctionInfo(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    public Response getFunctionInfo(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) throws IOException {

```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response getFunctionInfo(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) throws IOException {

        return functions().getFunctionInfo(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("instanceId") String instanceId) {
        return functions().stopFunctionInstance(tenant, namespace, functionName, instanceId, uri.getRequestUri(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("instanceId") String instanceId) {
        return functions().stopFunctionInstance(tenant, namespace, functionName, instanceId, uri.getRequestUri(),
                clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/trigger")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response triggerFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response triggerFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @FormDataParam("data") String triggerValue,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response triggerFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @FormDataParam("data") String triggerValue,
                                    final @FormDataParam("dataStream") InputStream triggerStream,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @FormDataParam("data") String triggerValue,
                                    final @FormDataParam("dataStream") InputStream triggerStream,
                                    final @FormDataParam("topic") String topic) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("dataStream")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                    final @PathParam("functionName") String functionName,
                                    final @FormDataParam("data") String triggerValue,
                                    final @FormDataParam("dataStream") InputStream triggerStream,
                                    final @FormDataParam("topic") String topic) {
        return functions().triggerFunction(tenant, namespace, functionName, triggerValue, triggerStream, topic,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("topic")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                    final @FormDataParam("data") String triggerValue,
                                    final @FormDataParam("dataStream") InputStream triggerStream,
                                    final @FormDataParam("topic") String topic) {
        return functions().triggerFunction(tenant, namespace, functionName, triggerValue, triggerStream, topic,
                clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}/status")
    public Response getFunctionStatus(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/status")
    public Response getFunctionStatus(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStatusV2(tenant, namespace, functionName, uri.getRequestUri(), clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response getFunctionStatus(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStatusV2(tenant, namespace, functionName, uri.getRequestUri(), clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName) {
        return functions().stopFunctionInstances(tenant, namespace, functionName, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response stopFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName) {
        return functions().stopFunctionInstances(tenant, namespace, functionName, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}")
    public Response listFunctions(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace) {
        return functions().listFunctions(tenant, namespace, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}")
    public Response listFunctions(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace) {
        return functions().listFunctions(tenant, namespace, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response updateFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response updateFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response updateFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                   final @PathParam("functionName") String functionName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("functionDetails") String functionDetailsJson) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("functionDetails") String functionDetailsJson) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionDetails")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("functionDetails") String functionDetailsJson) {

        return functions().updateFunction(tenant, namespace, functionName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/upload")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response uploadFunction(final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("path") String path) {
        return functions().uploadFunction(uploadedInputStream, path, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("path")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response uploadFunction(final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("path") String path) {
        return functions().uploadFunction(uploadedInputStream, path, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response registerFunction(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response registerFunction(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response registerFunction(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @FormDataParam("data") InputStream uploadedInputStream,
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @FormDataParam("data") InputStream uploadedInputStream,
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                     final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                     final @PathParam("functionName") String functionName,
                                     final @FormDataParam("data") InputStream uploadedInputStream,
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                     final @FormDataParam("url") String functionPkgUrl,
                                     final @FormDataParam("functionDetails") String functionDetailsJson) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                     final @FormDataParam("data") InputStream uploadedInputStream,
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                     final @FormDataParam("url") String functionPkgUrl,
                                     final @FormDataParam("functionDetails") String functionDetailsJson) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionDetails")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                     final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                     final @FormDataParam("url") String functionPkgUrl,
                                     final @FormDataParam("functionDetails") String functionDetailsJson) {

        return functions().registerFunction(tenant, namespace, functionName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) {
        return functions().restartFunctionInstances(tenant, namespace, functionName, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName) {
        return functions().restartFunctionInstances(tenant, namespace, functionName, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @QueryParam("path")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    )
    @Path("/download")
    public Response downloadFunction(final @QueryParam("path") String path) {
        return functions().downloadFunction(path, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response restartFunction(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @PathParam("instanceId") String instanceId) {
        return functions().restartFunctionInstance(tenant, namespace, functionName, instanceId, uri.getRequestUri(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("functionName") String functionName,
                                    final @PathParam("instanceId") String instanceId) {
        return functions().restartFunctionInstance(tenant, namespace, functionName, instanceId, uri.getRequestUri(),
                clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}/state/{key}")
    public Response getFunctionState(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/state/{key}")
    public Response getFunctionState(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @PathParam("key") String key) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response getFunctionState(final @PathParam("tenant") String tenant,
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @PathParam("key") String key) {
        return functions().getFunctionState(tenant, namespace, functionName, key, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("key")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
                                     final @PathParam("namespace") String namespace,
                                     final @PathParam("functionName") String functionName,
                                     final @PathParam("key") String key) {
        return functions().getFunctionState(tenant, namespace, functionName, key, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    })
    @Path("/{tenant}/{namespace}/{functionName}")
    public Response deregisterFunction(final @PathParam("tenant") String tenant,
                                       final @PathParam("namespace") String namespace,
                                       final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    public Response deregisterFunction(final @PathParam("tenant") String tenant,
                                       final @PathParam("namespace") String namespace,
                                       final @PathParam("functionName") String functionName) {
        return functions().deregisterFunction(tenant, namespace, functionName, clientAppId());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java
    public Response deregisterFunction(final @PathParam("tenant") String tenant,
                                       final @PathParam("namespace") String namespace,
                                       final @PathParam("functionName") String functionName) {
        return functions().deregisterFunction(tenant, namespace, functionName, clientAppId());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Produces(MediaType.APPLICATION_JSON)
    @Path("/{tenant}/{namespace}")
    public List<String> listFunctions(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace) {
        return functions().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}")
    public List<String> listFunctions(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace) {
        return functions().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/upload")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void uploadFunction(final @FormDataParam("data") InputStream uploadedInputStream,
                               final @FormDataParam("path") String path) {
        functions().uploadFunction(uploadedInputStream, path, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("path")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void uploadFunction(final @FormDataParam("data") InputStream uploadedInputStream,
                               final @FormDataParam("path") String path) {
        functions().uploadFunction(uploadedInputStream, path, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopFunction(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopFunction(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("functionName") String functionName) {
        functions().stopFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void stopFunction(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("functionName") String functionName) {
        functions().stopFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartFunction(final @PathParam("tenant") String tenant,
                                final @PathParam("namespace") String namespace,
                                final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartFunction(final @PathParam("tenant") String tenant,
                                final @PathParam("namespace") String namespace,
                                final @PathParam("functionName") String functionName,
                                final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void restartFunction(final @PathParam("tenant") String tenant,
                                final @PathParam("namespace") String namespace,
                                final @PathParam("functionName") String functionName,
                                final @PathParam("instanceId") String instanceId) {
        functions().restartFunctionInstance(tenant, namespace, functionName, instanceId,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                final @PathParam("namespace") String namespace,
                                final @PathParam("functionName") String functionName,
                                final @PathParam("instanceId") String instanceId) {
        functions().restartFunctionInstance(tenant, namespace, functionName, instanceId,
                this.uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public StreamingOutput downloadFunction(
            @ApiParam(value = "The tenant of functions")
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of functions")
            final @PathParam("namespace") String namespace,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            @ApiParam(value = "The namespace of functions")
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of functions")
            final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            @ApiParam(value = "The name of functions")
            final @PathParam("functionName") String functionName,
            @ApiParam(value = "Whether to download the transform function")
            final @QueryParam("transform-function") boolean transformFunction) {
```

### MissortedModifiers
Missorted modifiers `final @QueryParam("transform-function")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
            final @PathParam("functionName") String functionName,
            @ApiParam(value = "Whether to download the transform function")
            final @QueryParam("transform-function") boolean transformFunction) {

        return functions()
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @GET
    @Path("/{tenant}/{namespace}/{functionName}")
    public FunctionConfig getFunctionInfo(final @PathParam("tenant") String tenant,
                                          final @PathParam("namespace") String namespace,
                                          final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    public FunctionConfig getFunctionInfo(final @PathParam("tenant") String tenant,
                                          final @PathParam("namespace") String namespace,
                                          final @PathParam("functionName") String functionName) {
        return functions().getFunctionInfo(tenant, namespace, functionName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public FunctionConfig getFunctionInfo(final @PathParam("tenant") String tenant,
                                          final @PathParam("namespace") String namespace,
                                          final @PathParam("functionName") String functionName) {
        return functions().getFunctionInfo(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateFunction(final @PathParam("tenant") String tenant,
                               final @PathParam("namespace") String namespace,
                               final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateFunction(final @PathParam("tenant") String tenant,
                               final @PathParam("namespace") String namespace,
                               final @PathParam("functionName") String functionName,
                               final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void updateFunction(final @PathParam("tenant") String tenant,
                               final @PathParam("namespace") String namespace,
                               final @PathParam("functionName") String functionName,
                               final @FormDataParam("data") InputStream uploadedInputStream,
                               final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                               final @PathParam("namespace") String namespace,
                               final @PathParam("functionName") String functionName,
                               final @FormDataParam("data") InputStream uploadedInputStream,
                               final @FormDataParam("data") FormDataContentDisposition fileDetail,
                               final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                               final @PathParam("functionName") String functionName,
                               final @FormDataParam("data") InputStream uploadedInputStream,
                               final @FormDataParam("data") FormDataContentDisposition fileDetail,
                               final @FormDataParam("url") String functionPkgUrl,
                               final @FormDataParam("functionConfig") FunctionConfig functionConfig,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                               final @FormDataParam("data") InputStream uploadedInputStream,
                               final @FormDataParam("data") FormDataContentDisposition fileDetail,
                               final @FormDataParam("url") String functionPkgUrl,
                               final @FormDataParam("functionConfig") FunctionConfig functionConfig,
                               final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionConfig")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                               final @FormDataParam("data") FormDataContentDisposition fileDetail,
                               final @FormDataParam("url") String functionPkgUrl,
                               final @FormDataParam("functionConfig") FunctionConfig functionConfig,
                               final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("updateOptions")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                               final @FormDataParam("url") String functionPkgUrl,
                               final @FormDataParam("functionConfig") FunctionConfig functionConfig,
                               final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {

        functions().updateFunction(tenant, namespace, functionName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/state/{key}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void putFunctionState(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void putFunctionState(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("key") String key,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void putFunctionState(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("key") String key,
                                 final @FormDataParam("state") FunctionState stateJson) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("key")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("key") String key,
                                 final @FormDataParam("state") FunctionState stateJson) throws IOException {
        functions().putFunctionState(tenant, namespace, functionName, key, stateJson, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("state")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                 final @PathParam("functionName") String functionName,
                                 final @PathParam("key") String key,
                                 final @FormDataParam("state") FunctionState stateJson) throws IOException {
        functions().putFunctionState(tenant, namespace, functionName, key, stateJson, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/status")
    public FunctionStatus.FunctionInstanceStatus.FunctionInstanceStatusData getFunctionInstanceStatus(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public FunctionStatus.FunctionInstanceStatus.FunctionInstanceStatusData getFunctionInstanceStatus(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName,
            final @PathParam("instanceId") String instanceId) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName,
            final @PathParam("instanceId") String instanceId) throws IOException {
        return functions().getFunctionInstanceStatus(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName,
            final @PathParam("instanceId") String instanceId) throws IOException {
        return functions().getFunctionInstanceStatus(
                tenant, namespace, functionName, instanceId, uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartFunction(final @PathParam("tenant") String tenant,
                                final @PathParam("namespace") String namespace,
                                final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartFunction(final @PathParam("tenant") String tenant,
                                final @PathParam("namespace") String namespace,
                                final @PathParam("functionName") String functionName) {
        functions().restartFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void restartFunction(final @PathParam("tenant") String tenant,
                                final @PathParam("namespace") String namespace,
                                final @PathParam("functionName") String functionName) {
        functions().restartFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @DELETE
    @Path("/{tenant}/{namespace}/{functionName}")
    public void deregisterFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    public void deregisterFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName) {
        functions().deregisterFunction(tenant, namespace, functionName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void deregisterFunction(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("functionName") String functionName) {
        functions().deregisterFunction(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Produces(MediaType.APPLICATION_JSON)
    @Path("/{tenant}/{namespace}/{functionName}/stats")
    public FunctionStatsImpl getFunctionStats(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/stats")
    public FunctionStatsImpl getFunctionStats(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStats(tenant, namespace, functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public FunctionStatsImpl getFunctionStats(final @PathParam("tenant") String tenant,
                                              final @PathParam("namespace") String namespace,
                                              final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStats(tenant, namespace, functionName,
                uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/leader/{tenant}/{namespace}/{functionName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateFunctionOnWorkerLeader(final @PathParam("tenant") String tenant,
                                             final @PathParam("namespace") String namespace,
                                             final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateFunctionOnWorkerLeader(final @PathParam("tenant") String tenant,
                                             final @PathParam("namespace") String namespace,
                                             final @PathParam("functionName") String functionName,
                                             final @FormDataParam("functionMetaData")
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void updateFunctionOnWorkerLeader(final @PathParam("tenant") String tenant,
                                             final @PathParam("namespace") String namespace,
                                             final @PathParam("functionName") String functionName,
                                             final @FormDataParam("functionMetaData")
                                                     InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionMetaData")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                             final @PathParam("namespace") String namespace,
                                             final @PathParam("functionName") String functionName,
                                             final @FormDataParam("functionMetaData")
                                                     InputStream uploadedInputStream,
                                             final @FormDataParam("delete") boolean delete) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("delete")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                             final @FormDataParam("functionMetaData")
                                                     InputStream uploadedInputStream,
                                             final @FormDataParam("delete") boolean delete) {

        functions().updateFunctionOnWorkerLeader(tenant, namespace, functionName, uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @GET
    @Path("/{tenant}/{namespace}/{functionName}/state/{key}")
    public FunctionState getFunctionState(final @PathParam("tenant") String tenant,
                                          final @PathParam("namespace") String namespace,
                                          final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/state/{key}")
    public FunctionState getFunctionState(final @PathParam("tenant") String tenant,
                                          final @PathParam("namespace") String namespace,
                                          final @PathParam("functionName") String functionName,
                                          final @PathParam("key") String key) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public FunctionState getFunctionState(final @PathParam("tenant") String tenant,
                                          final @PathParam("namespace") String namespace,
                                          final @PathParam("functionName") String functionName,
                                          final @PathParam("key") String key) throws IOException {
        return functions().getFunctionState(tenant, namespace, functionName, key, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("key")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                          final @PathParam("namespace") String namespace,
                                          final @PathParam("functionName") String functionName,
                                          final @PathParam("key") String key) throws IOException {
        return functions().getFunctionState(tenant, namespace, functionName, key, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopFunction(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopFunction(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("functionName") String functionName,
                             final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void stopFunction(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("functionName") String functionName,
                             final @PathParam("instanceId") String instanceId) {
        functions().stopFunctionInstance(tenant, namespace, functionName, instanceId,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                             final @PathParam("namespace") String namespace,
                             final @PathParam("functionName") String functionName,
                             final @PathParam("instanceId") String instanceId) {
        functions().stopFunctionInstance(tenant, namespace, functionName, instanceId,
                this.uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/start")
    @Consumes(MediaType.APPLICATION_JSON)
    public void startFunction(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("functionName") String functionName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startFunction(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("functionName") String functionName) {
        functions().startFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void startFunction(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("functionName") String functionName) {
        functions().startFunctionInstances(tenant, namespace, functionName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/start")
    @Consumes(MediaType.APPLICATION_JSON)
    public void startFunction(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startFunction(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("functionName") String functionName,
                              final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void startFunction(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("functionName") String functionName,
                              final @PathParam("instanceId") String instanceId) {
        functions().startFunctionInstance(tenant, namespace, functionName, instanceId,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                              final @PathParam("namespace") String namespace,
                              final @PathParam("functionName") String functionName,
                              final @PathParam("instanceId") String instanceId) {
        functions().startFunctionInstance(tenant, namespace, functionName, instanceId,
                this.uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/status")
    public FunctionStatus getFunctionStatus(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public FunctionStatus getFunctionStatus(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStatus(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName) throws IOException {
        return functions().getFunctionStatus(
                tenant, namespace, functionName, uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void registerFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void registerFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public void registerFunction(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @FormDataParam("data") InputStream uploadedInputStream,
                                 final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("functionName") String functionName,
                                 final @FormDataParam("data") InputStream uploadedInputStream,
                                 final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                 final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                 final @PathParam("functionName") String functionName,
                                 final @FormDataParam("data") InputStream uploadedInputStream,
                                 final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                 final @FormDataParam("url") String functionPkgUrl,
                                 final @FormDataParam("functionConfig") FunctionConfig functionConfig) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                 final @FormDataParam("data") InputStream uploadedInputStream,
                                 final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                 final @FormDataParam("url") String functionPkgUrl,
                                 final @FormDataParam("functionConfig") FunctionConfig functionConfig) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("functionConfig")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                 final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                 final @FormDataParam("url") String functionPkgUrl,
                                 final @FormDataParam("functionConfig") FunctionConfig functionConfig) {

        functions().registerFunction(tenant, namespace, functionName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @QueryParam("path")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @GET
    @Path("/download")
    public StreamingOutput downloadFunction(final @QueryParam("path") String path) {
        return functions().downloadFunction(path, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/trigger")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public String triggerFunction(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace,
                                  final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public String triggerFunction(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace,
                                  final @PathParam("functionName") String functionName,
                                  final @FormDataParam("data") String input,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public String triggerFunction(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace,
                                  final @PathParam("functionName") String functionName,
                                  final @FormDataParam("data") String input,
                                  final @FormDataParam("dataStream") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                  final @PathParam("namespace") String namespace,
                                  final @PathParam("functionName") String functionName,
                                  final @FormDataParam("data") String input,
                                  final @FormDataParam("dataStream") InputStream uploadedInputStream,
                                  final @FormDataParam("topic") String topic) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("dataStream")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                  final @PathParam("functionName") String functionName,
                                  final @FormDataParam("data") String input,
                                  final @FormDataParam("dataStream") InputStream uploadedInputStream,
                                  final @FormDataParam("topic") String topic) {
        return functions().triggerFunction(tenant, namespace, functionName, input,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("topic")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
                                  final @FormDataParam("data") String input,
                                  final @FormDataParam("dataStream") InputStream uploadedInputStream,
                                  final @FormDataParam("topic") String topic) {
        return functions().triggerFunction(tenant, namespace, functionName, input,
                uploadedInputStream, topic, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{functionName}/{instanceId}/stats")
    public FunctionInstanceStatsDataImpl getFunctionInstanceStats(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
    public FunctionInstanceStatsDataImpl getFunctionInstanceStats(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName,
            final @PathParam("instanceId") String instanceId) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("functionName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName,
            final @PathParam("instanceId") String instanceId) throws IOException {
        return functions().getFunctionsInstanceStats(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            final @PathParam("functionName") String functionName,
            final @PathParam("instanceId") String instanceId) throws IOException {
        return functions().getFunctionsInstanceStats(
                tenant, namespace, functionName, instanceId, uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void registerSink(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void registerSink(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("sinkName") String sinkName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void registerSink(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("sinkName") String sinkName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                             final @PathParam("namespace") String namespace,
                             final @PathParam("sinkName") String sinkName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                             final @PathParam("sinkName") String sinkName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String functionPkgUrl,
                             final @FormDataParam("sinkConfig") SinkConfig sinkConfig) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String functionPkgUrl,
                             final @FormDataParam("sinkConfig") SinkConfig sinkConfig) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("sinkConfig")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String functionPkgUrl,
                             final @FormDataParam("sinkConfig") SinkConfig sinkConfig) {

        sinks().registerSink(tenant, namespace, sinkName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/{instanceId}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSink(final @PathParam("tenant") String tenant,
                         final @PathParam("namespace") String namespace,
                         final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSink(final @PathParam("tenant") String tenant,
                         final @PathParam("namespace") String namespace,
                         final @PathParam("sinkName") String sinkName,
                         final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void stopSink(final @PathParam("tenant") String tenant,
                         final @PathParam("namespace") String namespace,
                         final @PathParam("sinkName") String sinkName,
                         final @PathParam("instanceId") String instanceId) {
        sinks().stopFunctionInstance(tenant, namespace, sinkName, instanceId, this.uri.getRequestUri(), clientAppId(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                         final @PathParam("namespace") String namespace,
                         final @PathParam("sinkName") String sinkName,
                         final @PathParam("instanceId") String instanceId) {
        sinks().stopFunctionInstance(tenant, namespace, sinkName, instanceId, this.uri.getRequestUri(), clientAppId(),
                clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/{instanceId}/start")
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSink(final @PathParam("tenant") String tenant,
                          final @PathParam("namespace") String namespace,
                          final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSink(final @PathParam("tenant") String tenant,
                          final @PathParam("namespace") String namespace,
                          final @PathParam("sinkName") String sinkName,
                          final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void startSink(final @PathParam("tenant") String tenant,
                          final @PathParam("namespace") String namespace,
                          final @PathParam("sinkName") String sinkName,
                          final @PathParam("instanceId") String instanceId) {
        sinks().startFunctionInstance(tenant, namespace, sinkName, instanceId, this.uri.getRequestUri(), clientAppId(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                          final @PathParam("namespace") String namespace,
                          final @PathParam("sinkName") String sinkName,
                          final @PathParam("instanceId") String instanceId) {
        sinks().startFunctionInstance(tenant, namespace, sinkName, instanceId, this.uri.getRequestUri(), clientAppId(),
                clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSink(final @PathParam("tenant") String tenant,
                         final @PathParam("namespace") String namespace,
                         final @PathParam("sinkName") String sinkName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSink(final @PathParam("tenant") String tenant,
                         final @PathParam("namespace") String namespace,
                         final @PathParam("sinkName") String sinkName) {
        sinks().stopFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void stopSink(final @PathParam("tenant") String tenant,
                         final @PathParam("namespace") String namespace,
                         final @PathParam("sinkName") String sinkName) {
        sinks().stopFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/start")
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSink(final @PathParam("tenant") String tenant,
                          final @PathParam("namespace") String namespace,
                          final @PathParam("sinkName") String sinkName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSink(final @PathParam("tenant") String tenant,
                          final @PathParam("namespace") String namespace,
                          final @PathParam("sinkName") String sinkName) {
        sinks().startFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void startSink(final @PathParam("tenant") String tenant,
                          final @PathParam("namespace") String namespace,
                          final @PathParam("sinkName") String sinkName) {
        sinks().startFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @DELETE
    @Path("/{tenant}/{namespace}/{sinkName}")
    public void deregisterSink(final @PathParam("tenant") String tenant,
                               final @PathParam("namespace") String namespace,
                               final @PathParam("sinkName") String sinkName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}")
    public void deregisterSink(final @PathParam("tenant") String tenant,
                               final @PathParam("namespace") String namespace,
                               final @PathParam("sinkName") String sinkName) {
        sinks().deregisterFunction(tenant, namespace, sinkName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void deregisterSink(final @PathParam("tenant") String tenant,
                               final @PathParam("namespace") String namespace,
                               final @PathParam("sinkName") String sinkName) {
        sinks().deregisterFunction(tenant, namespace, sinkName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateSink(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateSink(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sinkName") String sinkName,
                           final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void updateSink(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sinkName") String sinkName,
                           final @FormDataParam("data") InputStream uploadedInputStream,
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sinkName") String sinkName,
                           final @FormDataParam("data") InputStream uploadedInputStream,
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
                           final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                           final @PathParam("sinkName") String sinkName,
                           final @FormDataParam("data") InputStream uploadedInputStream,
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
                           final @FormDataParam("url") String functionPkgUrl,
                           final @FormDataParam("sinkConfig") SinkConfig sinkConfig,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                           final @FormDataParam("data") InputStream uploadedInputStream,
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
                           final @FormDataParam("url") String functionPkgUrl,
                           final @FormDataParam("sinkConfig") SinkConfig sinkConfig,
                           final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("sinkConfig")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                           final @FormDataParam("data") FormDataContentDisposition fileDetail,
                           final @FormDataParam("url") String functionPkgUrl,
                           final @FormDataParam("sinkConfig") SinkConfig sinkConfig,
                           final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("updateOptions")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                           final @FormDataParam("url") String functionPkgUrl,
                           final @FormDataParam("sinkConfig") SinkConfig sinkConfig,
                           final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {

        sinks().updateSink(tenant, namespace, sinkName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/{instanceId}/status")
    public SinkStatus.SinkInstanceStatus.SinkInstanceStatusData getSinkInstanceStatus(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public SinkStatus.SinkInstanceStatus.SinkInstanceStatusData getSinkInstanceStatus(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("sinkName") String sinkName,
            final @PathParam("instanceId") String instanceId) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("sinkName") String sinkName,
            final @PathParam("instanceId") String instanceId) throws IOException {
        return sinks()
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            final @PathParam("sinkName") String sinkName,
            final @PathParam("instanceId") String instanceId) throws IOException {
        return sinks()
                .getSinkInstanceStatus(tenant, namespace, sinkName, instanceId, uri.getRequestUri(), clientAppId(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/{instanceId}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSink(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sinkName") String sinkName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSink(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sinkName") String sinkName,
                            final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void restartSink(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sinkName") String sinkName,
                            final @PathParam("instanceId") String instanceId) {
        sinks().restartFunctionInstance(tenant, namespace, sinkName, instanceId, this.uri.getRequestUri(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sinkName") String sinkName,
                            final @PathParam("instanceId") String instanceId) {
        sinks().restartFunctionInstance(tenant, namespace, sinkName, instanceId, this.uri.getRequestUri(),
                clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSink(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sinkName") String sinkName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSink(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sinkName") String sinkName) {
        sinks().restartFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public void restartSink(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sinkName") String sinkName) {
        sinks().restartFunctionInstances(tenant, namespace, sinkName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("name")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/builtinsinks/{name}/configdefinition")
    public List<ConfigFieldDefinition> getSinkConfigDefinition(
            @ApiParam(value = "The name of the builtin sink") final @PathParam("name") String name) throws IOException {
        return sinks().getSinkConfigDefinition(name);
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @GET
    @Path("/{tenant}/{namespace}")
    public List<String> listSink(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace) {
        return sinks().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}")
    public List<String> listSink(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace) {
        return sinks().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Produces(MediaType.APPLICATION_JSON)
    @Path("/{tenant}/{namespace}/{sinkName}/status")
    public SinkStatus getSinkStatus(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("sinkName") String sinkName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}/status")
    public SinkStatus getSinkStatus(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("sinkName") String sinkName) throws IOException {
        return sinks().getSinkStatus(tenant, namespace, sinkName, uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public SinkStatus getSinkStatus(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("sinkName") String sinkName) throws IOException {
        return sinks().getSinkStatus(tenant, namespace, sinkName, uri.getRequestUri(), clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @GET
    @Path("/{tenant}/{namespace}/{sinkName}")
    public SinkConfig getSinkInfo(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace,
                                  final @PathParam("sinkName") String sinkName)
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sinkName}")
    public SinkConfig getSinkInfo(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace,
                                  final @PathParam("sinkName") String sinkName)
            throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sinkName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    public SinkConfig getSinkInfo(final @PathParam("tenant") String tenant,
                                  final @PathParam("namespace") String namespace,
                                  final @PathParam("sinkName") String sinkName)
            throws IOException {
        return sinks().getSinkInfo(tenant, namespace, sinkName);
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Produces(MediaType.APPLICATION_JSON)
    @Path("/{tenant}/{namespace}/{sourceName}")
    public SourceConfig getSourceInfo(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("sourceName") String sourceName)
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}")
    public SourceConfig getSourceInfo(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("sourceName") String sourceName)
            throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public SourceConfig getSourceInfo(final @PathParam("tenant") String tenant,
                                      final @PathParam("namespace") String namespace,
                                      final @PathParam("sourceName") String sourceName)
            throws IOException {
        return sources().getSourceInfo(tenant, namespace, sourceName);
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/{instanceId}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSource(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSource(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("sourceName") String sourceName,
                              final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void restartSource(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("sourceName") String sourceName,
                              final @PathParam("instanceId") String instanceId) {
        sources().restartFunctionInstance(tenant, namespace, sourceName, instanceId, this.uri.getRequestUri(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                              final @PathParam("namespace") String namespace,
                              final @PathParam("sourceName") String sourceName,
                              final @PathParam("instanceId") String instanceId) {
        sources().restartFunctionInstance(tenant, namespace, sourceName, instanceId, this.uri.getRequestUri(),
                clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/{instanceId}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSource(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSource(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sourceName") String sourceName,
                           final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void stopSource(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sourceName") String sourceName,
                           final @PathParam("instanceId") String instanceId) {
        sources().stopFunctionInstance(tenant, namespace, sourceName, instanceId, this.uri.getRequestUri(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sourceName") String sourceName,
                           final @PathParam("instanceId") String instanceId) {
        sources().stopFunctionInstance(tenant, namespace, sourceName, instanceId, this.uri.getRequestUri(),
                clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("name")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/builtinsources/{name}/configdefinition")
    public List<ConfigFieldDefinition> getSourceConfigDefinition(
            @ApiParam(value = "The name of the builtin source") final @PathParam("name") String name)
            throws IOException {
        return sources().getSourceConfigDefinition(name);
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Produces(MediaType.APPLICATION_JSON)
    @Path("/{tenant}/{namespace}")
    public List<String> listSources(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace) {
        return sources().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}")
    public List<String> listSources(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace) {
        return sources().listFunctions(tenant, namespace, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/{instanceId}/status")
    public SourceStatus.SourceInstanceStatus.SourceInstanceStatusData getSourceInstanceStatus(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public SourceStatus.SourceInstanceStatus.SourceInstanceStatusData getSourceInstanceStatus(
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("sourceName") String sourceName,
            final @PathParam("instanceId") String instanceId) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
            final @PathParam("tenant") String tenant,
            final @PathParam("namespace") String namespace,
            final @PathParam("sourceName") String sourceName,
            final @PathParam("instanceId") String instanceId) throws IOException {
        return sources().getSourceInstanceStatus(
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
            final @PathParam("namespace") String namespace,
            final @PathParam("sourceName") String sourceName,
            final @PathParam("instanceId") String instanceId) throws IOException {
        return sources().getSourceInstanceStatus(
            tenant, namespace, sourceName, instanceId, uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/stop")
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSource(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sourceName") String sourceName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void stopSource(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sourceName") String sourceName) {
        sources().stopFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void stopSource(final @PathParam("tenant") String tenant,
                           final @PathParam("namespace") String namespace,
                           final @PathParam("sourceName") String sourceName) {
        sources().stopFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateSource(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void updateSource(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("sourceName") String sourceName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void updateSource(final @PathParam("tenant") String tenant,
                             final @PathParam("namespace") String namespace,
                             final @PathParam("sourceName") String sourceName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                             final @PathParam("namespace") String namespace,
                             final @PathParam("sourceName") String sourceName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                             final @PathParam("sourceName") String sourceName,
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String functionPkgUrl,
                             final @FormDataParam("sourceConfig") SourceConfig sourceConfig,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                             final @FormDataParam("data") InputStream uploadedInputStream,
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String functionPkgUrl,
                             final @FormDataParam("sourceConfig") SourceConfig sourceConfig,
                             final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("sourceConfig")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                             final @FormDataParam("data") FormDataContentDisposition fileDetail,
                             final @FormDataParam("url") String functionPkgUrl,
                             final @FormDataParam("sourceConfig") SourceConfig sourceConfig,
                             final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("updateOptions")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                             final @FormDataParam("url") String functionPkgUrl,
                             final @FormDataParam("sourceConfig") SourceConfig sourceConfig,
                             final @FormDataParam("updateOptions") UpdateOptionsImpl updateOptions) {

        sources().updateSource(tenant, namespace, sourceName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/start")
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSource(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sourceName") String sourceName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSource(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sourceName") String sourceName) {
        sources().startFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void startSource(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sourceName") String sourceName) {
        sources().startFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void registerSource(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public void registerSource(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("sourceName") String sourceName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void registerSource(final @PathParam("tenant") String tenant,
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("sourceName") String sourceName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                                   final @PathParam("namespace") String namespace,
                                   final @PathParam("sourceName") String sourceName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("data")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                                   final @PathParam("sourceName") String sourceName,
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("sourceConfig") SourceConfig sourceConfig) {
```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("url")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                                   final @FormDataParam("data") InputStream uploadedInputStream,
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("sourceConfig") SourceConfig sourceConfig) {

```

### MissortedModifiers
Missorted modifiers `final @FormDataParam("sourceConfig")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                                   final @FormDataParam("data") FormDataContentDisposition fileDetail,
                                   final @FormDataParam("url") String functionPkgUrl,
                                   final @FormDataParam("sourceConfig") SourceConfig sourceConfig) {

        sources().registerSource(tenant, namespace, sourceName, uploadedInputStream, fileDetail,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/{instanceId}/start")
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSource(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sourceName") String sourceName,
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void startSource(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sourceName") String sourceName,
                            final @PathParam("instanceId") String instanceId) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void startSource(final @PathParam("tenant") String tenant,
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sourceName") String sourceName,
                            final @PathParam("instanceId") String instanceId) {
        sources().startFunctionInstance(tenant, namespace, sourceName, instanceId, this.uri.getRequestUri(),
```

### MissortedModifiers
Missorted modifiers `final @PathParam("instanceId")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
                            final @PathParam("namespace") String namespace,
                            final @PathParam("sourceName") String sourceName,
                            final @PathParam("instanceId") String instanceId) {
        sources().startFunctionInstance(tenant, namespace, sourceName, instanceId, this.uri.getRequestUri(),
                clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Produces(MediaType.APPLICATION_JSON)
    @Path("/{tenant}/{namespace}/{sourceName}/status")
    public SourceStatus getSourceStatus(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("sourceName") String sourceName) throws IOException {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/status")
    public SourceStatus getSourceStatus(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("sourceName") String sourceName) throws IOException {
        return sources()
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public SourceStatus getSourceStatus(final @PathParam("tenant") String tenant,
                                    final @PathParam("namespace") String namespace,
                                    final @PathParam("sourceName") String sourceName) throws IOException {
        return sources()
                .getSourceStatus(tenant, namespace, sourceName, uri.getRequestUri(), clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}/restart")
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSource(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("sourceName") String sourceName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Consumes(MediaType.APPLICATION_JSON)
    public void restartSource(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("sourceName") String sourceName) {
        sources().restartFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void restartSource(final @PathParam("tenant") String tenant,
                              final @PathParam("namespace") String namespace,
                              final @PathParam("sourceName") String sourceName) {
        sources().restartFunctionInstances(tenant, namespace, sourceName, clientAppId(), clientAuthData());
    }
```

### MissortedModifiers
Missorted modifiers `final @PathParam("tenant")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @DELETE
    @Path("/{tenant}/{namespace}/{sourceName}")
    public void deregisterSource(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("sourceName") String sourceName) {
```

### MissortedModifiers
Missorted modifiers `final @PathParam("namespace")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @Path("/{tenant}/{namespace}/{sourceName}")
    public void deregisterSource(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("sourceName") String sourceName) {
        sources().deregisterFunction(tenant, namespace, sourceName, clientAppId(), clientAuthData());
```

### MissortedModifiers
Missorted modifiers `final @PathParam("sourceName")`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    public void deregisterSource(final @PathParam("tenant") String tenant,
                                 final @PathParam("namespace") String namespace,
                                 final @PathParam("sourceName") String sourceName) {
        sources().deregisterFunction(tenant, namespace, sourceName, clientAppId(), clientAuthData());
    }
```

## RuleId[ruleID=ThrowablePrintedToSystemOut]
### ThrowablePrintedToSystemOut
'Throwable' argument `e` to 'System.err.println()' call
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            System.err.println(e);
            String chosenCommand = jcommander.getParsedCommand();
            usageFormatter.usage(chosenCommand);
```

### ThrowablePrintedToSystemOut
'Throwable' argument `ex` to 'System.err.println()' call
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
                close();
            } catch (Exception ex) {
                System.err.println(ex);
            }
        }));
```

### ThrowablePrintedToSystemOut
'Throwable' argument `ex` to 'System.err.println()' call
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
            InstanceCache.shutdown();
        } catch (Exception ex) {
            System.err.println(ex);
        }
    }
```

### ThrowablePrintedToSystemOut
'Throwable' argument `ex` to 'System.err.println()' call
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeUtils.java`
#### Snippet
```java
            new JmxCollector("{}").register(registry);
        } catch (MalformedObjectNameException ex) {
            System.err.println(ex);
        }
        // Add the default exports from io.prometheus.client.hotspot.DefaultExports
```

## RuleId[ruleID=FunctionalExpressionCanBeFolded]
### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupPublishLimiter.java`
#### Snippet
```java
                            .rateTime(1L)
                            .timeUnit(TimeUnit.SECONDS)
                            .rateLimitFunction(this::apply)
                            .build();
                }
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupPublishLimiter.java`
#### Snippet
```java
                            .rateTime(1L)
                            .timeUnit(TimeUnit.SECONDS)
                            .rateLimitFunction(this::apply)
                            .build();
                }
```

### FunctionalExpressionCanBeFolded
Lambda can be replaced with call qualifier
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/MLPendingAckStore.java`
#### Snippet
```java
                                     List<PendingAckMetadataEntry> logList) {
        Stream<PendingAckMetadata> pendingAckMetaStream = logList.stream()
                .filter(log -> bothNotAbortAndCommitPredicate.test(log))
                .flatMap(log -> log.getPendingAckMetadatasList().stream());
        handleMetadataEntry(logPosition, pendingAckMetaStream);
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        topics.forEach((n, t) -> {
            Optional<Topic> topic = extractTopic(t);
            topic.ifPresent(consumer::accept);
        });
    }
```

## RuleId[ruleID=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/nsq/src/main/java/org/apache/pulsar/io/nsq/NSQSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "pulsar-transport-<topic>",
            help = "The channel to use on the topic you want to transport"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "All the table fields are discovered automatically. 'excludeNonDeclaredFields' indicates if the "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java
    private String nonKey;
    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Fields used in where condition of update and delete Events. A comma-separated list."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "INSERT",
            help = "If it is configured as UPSERT, the sink will use upsert semantics rather than "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java
    private int timeoutMs = 500;
    @FieldDoc(
        required = false,
        defaultValue = "200",
        help = "Enable batch mode by number of operations. This value is the max number of operations "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java
    private String tableName;
    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Fields used in update events. A comma-separated list."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Use the JDBC batch API. This option is suggested to improve write performance."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "500",
        help = "Enable batch mode by time. After timeoutMs milliseconds the operations queue will be flushed."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "true",
            help = "Enable transactions of the database."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "FAIL",
            help = "How to handle records with null values, possible options are DELETE or FAIL."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java
    private String userName;
    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/solr/src/main/java/org/apache/pulsar/io/solr/SolrSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/solr/src/main/java/org/apache/pulsar/io/solr/SolrSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/solr/src/main/java/org/apache/pulsar/io/solr/SolrSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "10",
        help = "Commit within milli seconds for solr update, if none passes defaults to 10 ms")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/CanalSourceConfig.java`
#### Snippet
```java
    private String singleHostname;
    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "The mysql database port")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/CanalSourceConfig.java`
#### Snippet
```java
    private String destination;
    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "The mysql database hostname")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/CanalSourceConfig.java`
#### Snippet
```java
    private String zkServers;
    @FieldDoc(
        required = false,
        defaultValue = "1000",
        help = "The batch size to fetch from canal.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "JAAS login context parameters for SASL connections in the format used by JAAS configuration files.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Protocol used to communicate with Kafka brokers.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The list of protocols enabled for SSL connections.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The location of the trust store file.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "SASL mechanism used for Kafka client connections.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The endpoint identification algorithm to validate server hostname using server certificate.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The password for the trust store file.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The list of protocols enabled for SSL connections.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The location of the trust store file.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "SASL mechanism used for Kafka client connections.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Protocol used to communicate with Kafka brokers.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The password for the trust store file.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The endpoint identification algorithm to validate server hostname using server certificate.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "JAAS login context parameters for SASL connections in the format used by JAAS configuration files.")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "false",
        help = "Flag to enable a keepalive to Redis")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "2147483647",
        help = "The maximum number of queued requests to Redis")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "10000L",
        help = "The amount of time in milliseconds to wait before timing out when connecting")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "false",
        help = "Flag to enable TCP no delay should be used")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "true",
        help = "Flag to determine if the Redis client should automatically reconnect")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "Standalone",
        help = "The client mode to use when interacting with the Redis cluster. Possible values [Standalone, Cluster]")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "10000L",
        help = "The amount of time in milliseconds before an operation is marked as timed out")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "200",
        help = "The batch size of write to Redis database"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "1000L",
        help = "The Redis operation time in milliseconds")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/BaseKinesisConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "Appropriate aws region. E.g. us-west-1, us-west-2"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/BaseKinesisConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "Kinesis end-point url. It can be found at https://docs.aws.amazon.com/general/latest/gr/rande.html"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/BaseKinesisConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "Fully-Qualified class name of implementation of AwsCredentialProviderPlugin."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Tell to Kinesis Client to skip certificate validation. This is useful while performing local tests, it's recommended to always validate certificates in production environments."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "ONLY_RAW_PAYLOAD",
        help = "Message format in which kinesis sink converts pulsar messages and publishes to kinesis streams.\n"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Kinesis end-point port. It can be found at https://docs.aws.amazon.com/general/latest/gr/rande.html"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "Dynamo end-point url. It can be found at https://docs.aws.amazon.com/general/latest/gr/rande.html"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "1000",
        help = "The maximum number of AWS Records that can be buffered inside the connector. "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "Cloudwatch end-point url. It can be found at https://docs.aws.amazon.com/general/latest/gr/rande.html"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "60000",
        help = "The frequency of the Kinesis stream checkpointing (in milliseconds)"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "Apache Pulsar IO Connector",
        help = "Name of the Amazon Kinesis application. By default the application name is included "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "3000",
        help = "The amount of time to delay between requests when the connector encounters a Throttling"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "true",
        help = "When true, uses Kinesis enhanced fan-out, when false, uses polling"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "If the initalPositionInStream is set to 'AT_TIMESTAMP', then this "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "LATEST",
        help = "Used to specify the position in the stream where the connector should start from.\n"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "3",
        help = "The number of re-attempts to make when the connector encounters an "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHoseConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "Keywords to track. Phrases of keywords are specified by a comma-separated list."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHoseConfig.java`
#### Snippet
```java
    // then we estimate the createdTime of each firehose event to be current time.
    @FieldDoc(
        required = false,
        defaultValue = "false",
        help = "Most firehose events have null createdAt time.If this parameter is set to true, "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHoseConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "pulsario-twitter-source",
        help = "The Twitter Firehose Client name"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHoseConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = Constants.STREAM_HOST,
        help = "The Twitter Firehose stream hosts that the connector connects to"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHoseConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "A comma separated list of user IDs, indicating the users to return statuses for in the stream."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHoseConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "50000",
        help = "The Twitter Firehose client buffer size"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Dynamodb streams end-point url. It can be found at "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "60000",
        help = "The frequency of the stream checkpointing (in milliseconds)"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "1000",
        help = "The maximum number of AWS Records that can be buffered inside the connector. "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "3",
        help = "The number of re-attempts to make when the connector encounters an "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "If the initalPositionInStream is set to 'AT_TIMESTAMP', then this "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "json-parameters to initialize `AwsCredentialsProviderPlugin`")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "3000",
        help = "The amount of time to delay between requests when the connector encounters a Throttling"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "Apache Pulsar IO Connector",
        help = "Name of the dynamodb consumer application. By default the application name is included "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "LATEST",
        help = "Used to specify the position in the stream where the connector should start from.\n"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Appropriate aws region. E.g. us-west-1, us-west-2"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Fully-Qualified class name of implementation of AwsCredentialProviderPlugin."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "Cloudwatch end-point url. It can be found at https://docs.aws.amazon.com/general/latest/gr/rande.html"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "Dynamo end-point url. It can be found at https://docs.aws.amazon.com/general/latest/gr/rande.html"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "1000L",
        help = "The InfluxDB operation time in milliseconds")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "false",
        help = "Flag to determine if gzip should be enabled")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "NONE",
        help = "The log level for InfluxDB request and response. Possible values [NONE, BASIC, HEADERS, FULL]")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "0",
        help = "Initially requested maximum channel number. 0 for unlimited")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "0",
        help = "Initially requested maximum frame size, in octets. 0 for unlimited")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "ONE",
        help = "The consistency level for writing data to InfluxDB. Possible values [ALL, ANY, ONE, QUORUM]")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "guest",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "200",
        help = "The batch size of write to InfluxDB database"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "10000",
        help = "The AMQP0-9-1 protocol handshake timeout in milliseconds")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "autogen",
        help = "The retention policy for the InfluxDB database")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "60",
        help = "The requested heartbeat timeout in seconds")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "60000",
        help = "Connection TCP establishment timeout in milliseconds. 0 for infinite")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The RabbitMQ queue name from which messages should be read from or written to")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "guest",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "0",
        help = "Maximum number of messages that the server will deliver, 0 for unlimited")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "false",
        help = "Set true if the settings should be applied to the entire channel rather than each consumer")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSourceConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Set true if the queue should be declared passively - ie to preserve durability/timeout settings")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "topic",
        help = "The exchange type to publish the messages on")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The routing key used for publishing the messages")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "NONE",
            help = "The log level for InfluxDB request and response. Possible values [NONE, BASIC, HEADERS, FULL]")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "1000L",
            help = "The InfluxDB operation time in milliseconds")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "ONE",
            help = "The timestamp precision for writing data to InfluxDB. Possible values [ns, us, ms, s]")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "200",
            help = "The batch size of write to InfluxDB database"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSinkConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Flag to determine if gzip should be enabled")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/data-generator/src/main/java/org/apache/pulsar/io/datagenerator/DataGeneratorSourceConfig.java`
#### Snippet
```java
    required = true,
    defaultValue = "50",
    sensitive = false,
    help = "How long to sleep between emitting messages"
  )
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Keystore password"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "Truststore password"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Whether or not to disable the node certificate validation. "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The path to the truststore file"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "None",
            help = "SSL Provider"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The path to the keystore file"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "true",
            help = "Whether or not to validate node hostnames when using SSL. "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Enable SSL/TLS"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "TLSv1.2",
            help = "Comma separated list of enabled SSL/TLS protocols"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "SSL/TLS cipher suites"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "AUTO",
            help = "Specify compatibility mode with the ElasticSearch cluster. "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
    // connection settings, see https://www.elastic.co/guide/en/elasticsearch/client/java-rest/current/java-rest-low-config.html
    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Enable elasticsearch request compression."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            help = "The comma separated ordered list of field names used to build the Elasticsearch document _id from the record value. If this list is a singleton, the field is converted as a string. If this list has 2 or more fields, the generated _id is a string representation of a JSON array of the field values."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "60000",
            help = "The socket timeout in milliseconds waiting to read the elasticsearch response."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "0",
        help = "The number of replicas of the index"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "true",
            help = "Whether to ignore the record key to build the Elasticsearch document _id. If primaryFields is defined, the connector extract the primary fields from the payload to build the document _id. If no primaryFields are provided, elasticsearch auto generates a random document _id."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "86400",
            help = "The maximum retry time interval in seconds for retrying an elasticsearch request."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "1",
        help = "The number of shards of the index"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "FAIL",
            help = "How to handle elasticsearch rejected documents due to some malformation. Possible options are IGNORE, DELETE or FAIL. Default is FAIL the Elasticsearch document."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "1",
            help = "The maximum number of retries for elasticsearch requests. Use -1 to disable it."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "1000",
            help = "The bulk flush interval flushing any bulk request pending if the interval passes. -1 or zero means the scheduled flushing is disabled."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "true",
            help = "If stripNulls is false, elasticsearch _source includes 'null' for empty fields (for example {\"foo\": null}), otherwise null fields are stripped."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "5",
            help = "Idle connection timeout to prevent a read timeout."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "5",
            help = "The maximum size in megabytes of elasticsearch bulk requests.Use -1 to disable it."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
    // bulk settings, see https://www.elastic.co/guide/en/elasticsearch/client/java-rest/master/java-rest-high-document-bulk.html#java-rest-high-document-bulk-processor
    @FieldDoc(
            required = false,
            defaultValue = "1000",
            help = "The maximum number of actions per elasticsearch bulk request. Use -1 to disable it."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "1000",
            help = "The time in milliseconds for getting a connection from the elasticsearch connection pool."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        help = "The index name to which the connector writes messages. The default value is the topic name."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
     */
    @FieldDoc(
            required = false,
            defaultValue = "0",
            help = "The maximum number of in flight elasticsearch bulk requests. The default 0 allows the execution of a single request. A value of 1 means 1 concurrent request is allowed to be executed while accumulating new bulk requests."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Enable the elasticsearch bulk processor to flush write requests based on the number or size of requests, or after a given period."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "_doc",
        help = "The type name that the connector writes messages to, with the default value set to _doc."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "IGNORE",
            help = "How to handle records with null values, possible options are IGNORE, DELETE or FAIL. Default is IGNORE the message."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "",
            sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "",
        sensitive = true,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
        required = false,
        defaultValue = "false",
        help = "Sets whether the Sink has to take into account the Schema or if it should simply copy the raw message to Elastichsearch"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "5000",
            help = "The elasticsearch client connection timeout in milliseconds."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "100",
            help = "The base time in milliseconds to wait when retrying an elasticsearch request."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java

    @FieldDoc(
            required = false,
            defaultValue = "false",
            help = "Create the index if it does not exist"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java

        @Parameter(names = { "-md",
                "--metadata-store" }, description = "Metadata Store service url. eg: zk:my-zk:2181", required = false)
        private String metadataStoreUrl;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        @Parameter(names = {"-tb",
                "--broker-service-url-tls"},
                description = "Broker-service URL for new cluster with TLS encryption", required = false)
        private String clusterBrokerServiceUrlTls;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java

        @Parameter(names = { "-ub",
                "--broker-service-url" }, description = "Broker-service URL for new cluster", required = false)
        private String clusterBrokerServiceUrl;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java

        @Parameter(names = { "-pu",
                "--proxy-url" }, description = "Proxy-server URL to which to connect.", required = false)
        private String clusterProxyUrl;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        @Parameter(names = {"-cms",
                "--configuration-metadata-store"}, description = "Configuration Metadata Store connection string",
                hidden = false)
        private String configurationMetadataStore;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        @Parameter(names = { "-zk",
                "--zookeeper" }, description = "Local ZooKeeper quorum connection string",
                required = false,
                hidden = true
                )
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        @Parameter(names = {"-gzk",
                "--global-zookeeper"},
                description = "Global ZooKeeper quorum connection string", required = false, hidden = true)
        private String globalZookeeper;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        @Parameter(names = {"-tw",
                "--web-service-url-tls"},
                description = "Web-service URL for new cluster with TLS encryption", required = false)
        private String clusterWebServiceUrlTls;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
                "--default-namespace-bundle-number"},
                description = "The bundle numbers for the default namespaces(public/default), default is 16",
                required = false)
        private int numberOfDefaultNamespaceBundles;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
                "--proxy-protocol" },
                description = "Proxy protocol to select type of routing at proxy. Possible Values: [SNI]",
                required = false)
        private ProxyProtocol clusterProxyProtocol;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_BROKER_DISCOVERY,
            required = false,
            doc = "The metadata store URL for the configuration data. If empty, we fall back to use metadataStoreUrl"
    )
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_BROKER_DISCOVERY,
            required = false,
            doc = "The metadata store URL. \n"
                    + " Examples: \n"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/PersistentTopics.java`
#### Snippet
```java
    public void getList(@Suspended final AsyncResponse asyncResponse, @PathParam("property") String property,
            @PathParam("cluster") String cluster, @PathParam("namespace") String namespace,
            @ApiParam(value = "Specify the bundle name", required = false)
            @QueryParam("bundle") String bundle) {
        validateNamespaceName(property, cluster, namespace);
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/NonPersistentTopics.java`
#### Snippet
```java
            @ApiParam(value = "Specify the namespace", required = true)
            @PathParam("namespace") String namespace,
            @ApiParam(value = "Specify the bundle name", required = false)
            @QueryParam("bundle") String nsBundle,
            @ApiParam(value = "Include system topic")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop function instance", response = Void.class)
    @ApiResponses(value = { @ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart all function instances", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart function instance", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 307, message = "Current broker doesn't serve the namespace of this function"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Functions.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop all function instances", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop all instances of a Pulsar Source", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop instance of a Pulsar Source", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start an instance of a Pulsar Source", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start all instances of a Pulsar Source", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart all instances of a Pulsar Source", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart an instance of a Pulsar Source", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 307, message = "Current broker doesn't serve the namespace of this source"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SourcesBase.java`
#### Snippet
```java
    @ApiOperation(
            value = "Reload the built-in connectors, including Sources and Sinks",
            response = Void.class
    )
    @ApiResponses(value = {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart all instances of a Pulsar Function", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart an instance of a Pulsar Function", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 307, message = "Current broker doesn't serve the namespace of this function"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start an instance of a Pulsar Function", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop all instances of a Pulsar Function", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop an instance of a Pulsar Function", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/FunctionsBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start all instances of a Pulsar Function", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start an instance of a Pulsar Sink", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid start request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop an instance of a Pulsar Sink", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid stop request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart all instances of a Pulsar Sink", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid restart request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart an instance of a Pulsar Sink", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 307, message = "Current broker doesn't serve the namespace of this sink"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start all instances of a Pulsar Sink", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid start request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop all instances of a Pulsar Sink", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid stop request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/SinksBase.java`
#### Snippet
```java
    @ApiOperation(
            value = "Reload the built-in connectors, including Sources and Sinks",
            response = Void.class
    )
    @ApiResponses(value = {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
            @ApiParam(value = "Specify the namespace", required = true)
            @PathParam("namespace") String namespace,
            @ApiParam(value = "Specify the bundle name", required = false)
            @QueryParam("bundle") String bundle,
            @ApiParam(value = "Include system topic")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/FunctionInstanceStatsDataBaseImpl.java`
#### Snippet
```java
 */
@Data
@JsonInclude(JsonInclude.Include.ALWAYS)
@JsonPropertyOrder({ "receivedTotal", "processedSuccessfullyTotal", "systemExceptionsTotal",
        "userExceptionsTotal", "avgProcessLatency" })
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/FunctionInstanceStatsImpl.java`
#### Snippet
```java
 */
@Data
@JsonInclude(JsonInclude.Include.ALWAYS)
@JsonPropertyOrder({ "instanceId", "metrics" })
public class FunctionInstanceStatsImpl implements FunctionInstanceStats {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/FunctionStatsImpl.java`
#### Snippet
```java
 */
@Data
@JsonInclude(JsonInclude.Include.ALWAYS)
@JsonPropertyOrder({"receivedTotal", "processedSuccessfullyTotal", "systemExceptionsTotal", "userExceptionsTotal",
        "avgProcessLatency", "1min", "lastInvocation", "instances"})
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/FunctionInstanceStatsDataImpl.java`
#### Snippet
```java
@EqualsAndHashCode(callSuper = true)
@Data
@JsonInclude(JsonInclude.Include.ALWAYS)
@JsonPropertyOrder({ "receivedTotal", "processedSuccessfullyTotal", "systemExceptionsTotal",
        "userExceptionsTotal", "avgProcessLatency", "1min", "lastInvocation", "userMetrics" })
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/ClusterDataImpl.java`
#### Snippet
```java
            name = "listenerName",
            value = "listenerName when client would like to connect to cluster",
            example = ""
    )
    private String listenerName;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart all function instances", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop function instance", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart function instance", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 307, message = "Current broker doesn't serve the namespace of this function"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/FunctionsApiV2Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop all function instances", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop function instance", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start all function instances", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start function instance", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop all function instances", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart function instance", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 307, message = "Current broker doesn't serve the namespace of this function"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/FunctionsApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart all function instances", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 400, message = "Invalid request"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop sink instance", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart sink instance", response = Void.class)
    @ApiResponses(value = {
        @ApiResponse(code = 307, message = "Current broker doesn't serve the namespace of this sink"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start sink instance", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart all sink instances", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop all sink instances", response = Void.class)
    @ApiResponses(value = { @ApiResponse(code = 400, message = "Invalid request"),
    @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java
    @ApiOperation(
            value = "Reload the built-in connectors, including Sources and Sinks",
            response = Void.class
    )
    @ApiResponses(value = {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinksApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start all sink instances", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart source instance", response = Void.class)
    @ApiResponses(value = {
            @ApiResponse(code = 307, message = "Current broker doesn't serve the namespace of this source"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start source instance", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop source instance", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Restart all source instances", response = Void.class)
    @ApiResponses(value = { @ApiResponse(code = 400, message = "Invalid request"),
    @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Stop all source instances", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java

    @POST
    @ApiOperation(value = "Start all source instances", response = Void.class)
    @ApiResponses(value = {@ApiResponse(code = 400, message = "Invalid request"),
            @ApiResponse(code = 404, message = "The function does not exist"),
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourcesApiV3Resource.java`
#### Snippet
```java
    @ApiOperation(
            value = "Reload the built-in connectors, including Sources and Sinks",
            response = Void.class
    )
    @ApiResponses(value = {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

    @Parameter(names = "--expose_pulsaradmin", description = "Whether the pulsar admin client "
            + "exposed to function context, default is disabled.", required = false)
    public Boolean exposePulsarAdminClientEnabled = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
    public int maxPendingAsyncRequests = 1000;

    @Parameter(names = "--web_serviceurl", description = "Pulsar Web Service Url", required = false)
    public String webServiceUrl = null;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

    @Parameter(names = "--pending_async_requests", description = "Max pending async requests per instance",
            required = false)
    public int maxPendingAsyncRequests = 1000;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

    @Parameter(names = "--state_storage_impl_class", description = "State Storage Service "
            + "Implementation class\n", required = false)
    public String stateStorageImplClass;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
    public int expectedHealthCheckInterval;

    @Parameter(names = "--secrets_provider", description = "The classname of the secrets provider", required = false)
    public String secretsProviderClassName;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
    public String stateStorageImplClass;

    @Parameter(names = "--state_storage_serviceurl", description = "State Storage Service Url\n", required = false)
    public String stateStorageServiceUrl;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

    @Parameter(names = "--nar_extraction_directory", description = "The directory where "
            + "extraction of nar packages happen", required = false)
    public String narExtractionDirectory = NarClassLoader.DEFAULT_NAR_EXTRACTION_DIR;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

    @Parameter(names = "--secrets_provider_config", description = "The config that needs to be "
            + "passed to secrets provider", required = false)
    public String secretsProviderConfig;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_WORKER,
            required = false,
            deprecated = true,
            doc = "Configuration store connection string (as a comma-separated list). Deprecated in favor of "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_WORKER,
            required = false,
            doc = "Configuration store connection string (as a comma-separated list)"
    )
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
    protected SinkConfig sinkConfig;
    @Parameter(names = "--stateStorageImplClass", description = "The implemenatation class "
            + "state storage service (by default Apache BookKeeper)", hidden = true, required = false)
    protected String stateStorageImplClass;
    @Parameter(names = "--stateStorageServiceUrl", description = "The URL for the state storage service "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdProxyStats.java`
#### Snippet
```java
    @Parameters(commandDescription = "dump connections metrics for Monitoring")
    private class CmdConnectionMetrics extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdProxyStats.java`
#### Snippet
```java
    @Parameters(commandDescription = "dump topics metrics for Monitoring")
    private class CmdTopicsMetrics extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTransactions.java`
#### Snippet
```java
    @Parameters(commandDescription = "Get transaction coordinator stats")
    private class GetCoordinatorStats extends CliCommand {
        @Parameter(names = {"-c", "--coordinator-id"}, description = "The coordinator id", required = false)
        private Integer coordinatorId;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTransactions.java`
#### Snippet
```java
    @Parameters(commandDescription = "Get slow transactions.")
    private class GetSlowTransactions extends CliCommand {
        @Parameter(names = {"-c", "--coordinator-id"}, description = "The coordinator id", required = false)
        private Integer coordinatorId;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java
                + "create a topic. If there are not enough primary brokers, topics are assigned to secondary brokers. "
                + "If there are not enough secondary brokers, topics are assigned to other brokers which do not have "
                + "any isolation policies.", required = true, splitter = CommaParameterSplitter.class)
        private List<String> primary;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java

        @Parameter(names = "--namespaces", description = "comma separated namespaces-regex list",
                required = true, splitter = CommaParameterSplitter.class)
        private List<String> namespaces;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java

        @Parameter(names = "--secondary", description = "comma separated secondary-broker-regex list",
                required = false, splitter = CommaParameterSplitter.class)
        private List<String> secondary = new ArrayList<String>(); // optional

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java

        @Parameter(names = "--secondary", description = "comma separated secondary-broker-regex list",
                required = false, splitter = CommaParameterSplitter.class)
        private List<String> secondary = new ArrayList<String>(); // optional

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
        private Long version;

        @Parameter(names = {"-a", "--all-version"}, description = "all version", required = false)
        private boolean all = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
        private java.util.List<String> params;

        @Parameter(names = {"-v", "--version"}, description = "version", required = false)
        private Long version;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBookies.java`
#### Snippet
```java
        private static final String PATH_SEPARATOR = "/";

        @Parameter(names = { "-g", "--group" }, description = "Bookie group name", required = false)
        private String group = "default";

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBookies.java`
#### Snippet
```java
        private String bookieRack;

        @Parameter(names = {"-hn", "--hostname"}, description = "Bookie host name", required = false)
        private String bookieHost;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceGroups.java`
#### Snippet
```java
        @Parameter(names = { "--byte-dispatch-rate",
                "-bd" }, description = "byte-dispatch-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private Long dispatchRateInBytes;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceGroups.java`
#### Snippet
```java
        @Parameter(names = { "--byte-publish-rate",
                "-bp" }, description = "byte-publish-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private Long publishRateInBytes;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceGroups.java`
#### Snippet
```java

        @Parameter(names = { "--byte-publish-rate",
                "-bp" }, description = "byte-publish-rate ", required = false)
        private Long publishRateInBytes;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceGroups.java`
#### Snippet
```java

        @Parameter(names = { "--msg-publish-rate",
                "-mp" }, description = "message-publish-rate ", required = false)
        private Integer publishRateInMsgs;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceGroups.java`
#### Snippet
```java
        @Parameter(names = { "--msg-publish-rate",
                "-mp" }, description = "message-publish-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private Integer publishRateInMsgs;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceGroups.java`
#### Snippet
```java

        @Parameter(names = { "--byte-dispatch-rate",
                "-bd" }, description = "byte-dispatch-rate ", required = false)
        private Long dispatchRateInBytes;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceGroups.java`
#### Snippet
```java

        @Parameter(names = { "--msg-dispatch-rate",
                "-md" }, description = "message-dispatch-rate ", required = false)
        private Integer dispatchRateInMsgs;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceGroups.java`
#### Snippet
```java
        @Parameter(names = { "--msg-dispatch-rate",
                "-md" }, description = "message-dispatch-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private Integer dispatchRateInMsgs;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
    @Parameters(commandDescription = "dump mbean stats")
    private class CmdDumpMBeans extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
    @Parameters(commandDescription = "dump metrics for Monitoring")
    private class CmdMonitoringMetrics extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
    @Parameters(commandDescription = "dump topics stats")
    private class CmdTopics extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                        + " in both long-term storage and BookKeeper, you can set where to read messages from with"
                        + " the option `tiered-storage-first` or `bookkeeper-first`.",
                required = false
        )
        private String offloadReadPriorityStr;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--dispatch-rate-period",
                "-dt" }, description = "dispatch-rate-period in second type "
                + "(default 1 second will be overwrite if not passed)", required = false)
        private int dispatchRatePeriodSec = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--relative-to-publish-rate",
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))", required = false)
        private boolean relativeToPublishRate = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "--subscribe-rate",
                "-sr" }, description = "subscribe-rate (default -1 will be overwrite if not passed)", required = false)
        private int subscribeRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--byte-publish-rate", "-b"}, description = "byte-publish-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private long bytePublishRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--byte-dispatch-rate",
                "-bd" }, description = "byte-dispatch-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private long byteDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--subscribe-rate-period",
                "-st" }, description = "subscribe-rate-period in second type "
                + "(default 30 second will be overwrite if not passed)", required = false)
        private int subscribeRatePeriodSec = 30;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--msg-publish-rate", "-m"}, description = "message-publish-rate (default -1 will be "
                + "overwrite if not passed)", required = false)
        private int msgPublishRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--msg-dispatch-rate",
                "-md" }, description = "message-dispatch-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private int msgDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-key-store",
                description = "TLS KeyStore path for internal client", required = false)
        protected String brokerClientTlsKeyStore;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        protected String serviceUrlTls;

        @Parameter(names = "--broker-url", description = "broker-service-url", required = false)
        protected String brokerServiceUrl;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        private String domainName;

        @Parameter(names = "--broker-list", description = "Comma separated broker list", required = false)
        private String brokerList;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--listener-name",
                description = "listenerName when client would like to connect to cluster", required = false)
        protected String listenerName;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-key-store-type",
                description = "TLS TrustStore type configuration for internal client eg: JKS", required = false)
        protected String brokerClientTlsKeyStoreType;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-key-filepath",
                description = "path for the TLS private key file", required = false)
        protected String brokerClientKeyFilePath;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        protected String serviceUrl;

        @Parameter(names = "--url-secure", description = "service-url for secure connection", required = false)
        protected String serviceUrlTls;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-trust-certs-filepath",
                description = "path for the trusted TLS certificate file", required = false)
        protected String brokerClientTrustCertsFilePath;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-enable-keystore",
                description = "Whether use KeyStore type to authenticate", required = false)
        protected Boolean brokerClientTlsEnabledWithKeyStore;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        protected String authenticationPlugin;

        @Parameter(names = "--auth-parameters", description = "authentication parameters", required = false)
        protected String authenticationParameters;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        private String domainName;

        @Parameter(names = "--broker-list", description = "Comma separated broker list", required = false)
        private String brokerList;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--broker-url-secure", description = "New migrated cluster broker service url secure",
                required = false)
        private String brokerServiceUrlTls;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--broker-url-secure",
                description = "broker-service-url for secure connection", required = false)
        protected String brokerServiceUrlTls;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        protected java.util.List<String> params;

        @Parameter(names = "--url", description = "service-url", required = false)
        protected String serviceUrl;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-trust-store",
                description = "TLS TrustStore path for internal client", required = false)
        protected String brokerClientTlsTrustStore;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        protected Boolean brokerClientTlsEnabled;

        @Parameter(names = "--tls-allow-insecure", description = "Allow insecure tls connection", required = false)
        protected Boolean tlsAllowInsecureConnection;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = { "-a", "--all" },
                description = "Delete all data (tenants) of the cluster", required = false)
        private boolean deleteAll = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-certs-filepath",
                description = "path for the TLS certificate file", required = false)
        protected String brokerClientCertificateFilePath;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        protected ProxyProtocol proxyProtocol;

        @Parameter(names = "--tls-enable", description = "Enable tls connection", required = false)
        protected Boolean brokerClientTlsEnabled;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--proxy-protocol",
                description = "protocol to decide type of proxy routing eg: SNI", required = false)
        protected ProxyProtocol proxyProtocol;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-trust-store-pwd",
                description = "TLS TrustStore password for internal client", required = false)
        protected String brokerClientTlsTrustStorePassword;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        protected String proxyServiceUrl;

        @Parameter(names = "--auth-plugin", description = "authentication plugin", required = false)
        protected String authenticationPlugin;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-key-store-pwd",
                description = "TLS KeyStore password for internal client", required = false)
        protected String brokerClientTlsKeyStorePassword;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java

        @Parameter(names = "--tls-trust-store-type",
                description = "TLS TrustStore type configuration for internal client eg: JKS", required = false)
        protected String brokerClientTlsTrustStoreType;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        private boolean migrated;

        @Parameter(names = "--broker-url", description = "New migrated cluster broker service url", required = false)
        private String brokerServiceUrl;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
                "-c" }, description = "Comma separated allowed clusters. "
                + "If empty, the tenant will have access to all clusters",
                required = false, splitter = CommaParameterSplitter.class)
        private java.util.List<String> allowedClusters;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
                "-c" }, description = "Comma separated allowed clusters. "
                + "If empty, the tenant will have access to all clusters",
                required = false, splitter = CommaParameterSplitter.class)
        private java.util.List<String> allowedClusters;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
        @Parameter(names = { "--admin-roles",
                "-r" }, description = "Comma separated list of auth principal allowed to administrate the tenant",
                required = false, splitter = CommaParameterSplitter.class)
        private java.util.List<String> adminRoles;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
        @Parameter(names = { "--admin-roles",
                "-r" }, description = "Comma separated list of auth principal allowed to administrate the tenant",
                required = false, splitter = CommaParameterSplitter.class)
        private java.util.List<String> adminRoles;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
                "-c" }, description = "Comma separated allowed clusters. "
                + "If omitted, the current set of clusters will be preserved",
                required = false, splitter = CommaParameterSplitter.class)
        private java.util.List<String> allowedClusters;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
                "-c" }, description = "Comma separated allowed clusters. "
                + "If omitted, the current set of clusters will be preserved",
                required = false, splitter = CommaParameterSplitter.class)
        private java.util.List<String> allowedClusters;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
                "-r" }, description = "Comma separated list of auth principal allowed to administrate the tenant. "
                + "If empty the current set of roles won't be modified",
                required = false, splitter = CommaParameterSplitter.class)
        private java.util.List<String> adminRoles;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
                "-r" }, description = "Comma separated list of auth principal allowed to administrate the tenant. "
                + "If empty the current set of roles won't be modified",
                required = false, splitter = CommaParameterSplitter.class)
        private java.util.List<String> adminRoles;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(names = { "-w", "--wait-complete" },
                   description = "Wait for compaction to complete", required = false)
        private boolean wait = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        private String subName;

        @Parameter(names = { "-n", "--count" }, description = "Number of messages (default 1)", required = false)
        private int numMessages = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        @Parameter(names = { "--messageId",
                "-m" }, description = "messageId where to create the subscription. "
                + "It can be either 'latest', 'earliest' or (ledgerId:entryId)", required = false)
        private String messageIdStr = "latest";

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(names = { "--messageId",
                "-m" }, description = "messageId to reset back to (ledgerId:entryId)", required = false)
        private String resetMessageIdStr;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        @Parameter(names = { "--time",
                "-t" }, description = "time in minutes to reset back to "
                + "(or minutes, hours,days,weeks eg: 100m, 3h, 2d, 5w)", required = false)
        private String resetTimeStr;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
                required = false, splitter = NoSplitter.class)
        private java.util.List<String> properties;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
                names = "--path",
                description = "Path or functionPkgUrl to store the content",
                listConverter = StringConverter.class, required = false, hidden = true)
        protected String path;
        @Parameter(
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--relative-to-publish-rate",
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))", required = false)
        private boolean relativeToPublishRate = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--aws-id", "-i"},
                description = "AWS Credential Id to use when using driver S3 or aws-s3",
                required = false)
        private String awsId;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--msg-dispatch-rate",
                "-md" }, description = "message-dispatch-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private int msgDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--split-boundaries",
                "-sb" }, description = "Specified split boundary for bundle split, will split one bundle "
                + "to multi bundles only works with specified_positions_divide algorithm", required = false)
        private List<Long> splitBoundaries;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--msg-dispatch-rate",
            "-md" }, description = "message-dispatch-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private int msgDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                description = "The list of topics(both non-partitioned topic and partitioned topic) to get positions "
                        + "in this bundle, if none topic provided, will get the positions of all topics in this bundle",
                required = false)
        private List<String> topics;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = { "--bundle", "-b" },
                description = "{start-boundary}_{end-boundary} format namespace bundle",
                required = false)
        private String bundle;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--num-partitions", "-n" }, description = "Default number of partitions of topic to "
                + "be auto-created, applicable to partitioned topics only", required = false)
        private Integer defaultNumPartitions = null;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--offloadAfterElapsed", "-oae"},
                description = "Offload after elapsed in minutes (or minutes, hours,days,weeks eg: 100m, 3h, 2d, 5w).",
                required = false)
        private String offloadAfterElapsedStr;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--byte-dispatch-rate",
            "-bd" }, description = "byte-dispatch-rate (default -1 will be overwrite if not passed)", required = false)
        private long byteDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--dispatch-rate-period",
            "-dt" }, description = "dispatch-rate-period in second type "
                + "(default 1 second will be overwrite if not passed)", required = false)
        private int dispatchRatePeriodSec = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--offloadAfterThresholdInSeconds", "-oats"},
                description = "Offload after threshold seconds (eg: 1,5,10)",
                required = false
        )
        private String offloadAfterThresholdInSecondsStr;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--msg-dispatch-rate",
            "-md" }, description = "message-dispatch-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private int msgDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                + "namespace bundle. Valid options are: [range_equally_divide, topic_count_equally_divide, "
                + "specified_positions_divide, flow_or_qps_equally_divide]. Use broker side config if absent"
                , required = false)
        private String splitAlgorithmName;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--s3-role", "-ro"},
                description = "S3 Role used for STSAssumeRoleSessionCredentialsProvider",
                required = false)
        private String s3Role;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                description = "Alternative endpoint to connect to, "
                        + "s3 default is s3ManagedLedgerOffloadServiceEndpoint in broker.conf",
                required = false)
        private String endpoint;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--subscribe-rate",
                "-sr" }, description = "subscribe-rate (default -1 will be overwrite if not passed)", required = false)
        private int subscribeRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = {"-rs", "--roles"},
                description = "Client roles to which grant permissions (comma separated roles)",
                required = true, splitter = CommaParameterSplitter.class)
        private List<String> roles;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--bundle",
                "-b" }, description = "{start-boundary}_{end-boundary} "
                        + "(mutually exclusive with --bundle-type)", required = false)
        private String bundle;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

         @Parameter(names = { "--msg-publish-rate",
            "-m" }, description = "message-publish-rate (default -1 will be overwrite if not passed)", required = false)
        private int msgPublishRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--subscribe-rate-period",
                "-st" }, description = "subscribe-rate-period in second type "
                + "(default 30 second will be overwrite if not passed)", required = false)
        private int subscribeRatePeriodSec = 30;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--unload",
                "-u" }, description = "Unload newly split bundles after splitting old bundle", required = false)
        private boolean unload;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--offloadAfterThreshold", "-oat"},
                description = "Offload after threshold size (eg: 1M, 5M)",
                required = false)
        private String offloadAfterThresholdStr;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--dispatch-rate-period",
                "-dt" }, description = "dispatch-rate-period in second type "
                + "(default 1 second will be overwrite if not passed)", required = false)
        private int dispatchRatePeriodSec = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--byte-dispatch-rate",
                "-bd" }, description = "byte-dispatch-rate "
                + "(default -1 will be overwrite if not passed)", required = false)
        private long byteDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--tenant",
                "-p" }, description = "tenant is only used for authorization. "
                + "Client has to be admin of any of the tenant to access this api", required = false)
        private String tenant;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--clusters", "-c" },
                description = "List of clusters this namespace will be assigned", required = false)
        private java.util.List<String> clusters;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        private java.util.List<String> clusters;

        @Parameter(names = { "--bundles", "-b" }, description = "number of bundles to activate", required = false)
        private int numBundles = 0;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--dispatch-rate-period",
            "-dt" }, description = "dispatch-rate-period in second type "
                + "(default 1 second will be overwrite if not passed)", required = false)
        private int dispatchRatePeriodSec = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--readBufferSize", "-rbs"},
                description = "Read buffer size (eg: 1M, 5M), default is 1MB",
                required = false)
        private String readBufferSizeStr;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--maxBlockSize", "-mbs"},
                description = "Max block size (eg: 32M, 64M), default is 64MB",
                required = false)
        private String maxBlockSizeStr;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--aws-secret", "-s"},
                description = "AWS Credential Secret to use when using driver S3 or aws-s3",
                required = false)
        private String awsSecret;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                        + "For messages that exist in both long-term storage and BookKeeper, you can set where to "
                        + "read messages from with the option `tiered-storage-first` or `bookkeeper-first`.",
                required = false
        )
        private String offloadReadPriorityStr;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--bundle-type",
        "-bt" }, description = "bundle type (mutually exclusive with --bundle)", required = false)
        private BundleType bundleType;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                + "messages should be written. If you want to verify whether there are enough bookies in groups, "
                + "use `--secondary-group` flag. Messages in this namespace are stored in secondary groups. "
                + "If a group does not contain enough bookies, a topic cannot be created.", required = false)
        private String bookieAffinityGroupNameSecondary;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                description = "The long term storage region, "
                         + "default is s3ManagedLedgerOffloadRegion or gcsManagedLedgerOffloadRegion in broker.conf",
                required = false)
        private String region;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                names = {"--s3-role-session-name", "-rsn"},
                description = "S3 role session name used for STSAssumeRoleSessionCredentialsProvider",
                required = false)
        private String s3RoleSessionName;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--byte-dispatch-rate",
            "-bd" }, description = "byte-dispatch-rate (default -1 will be overwrite if not passed)", required = false)
        private long byteDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--relative-to-publish-rate",
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))", required = false)
        private boolean relativeToPublishRate = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

         @Parameter(names = { "--byte-publish-rate",
            "-b" }, description = "byte-publish-rate (default -1 will be overwrite if not passed)", required = false)
        private long bytePublishRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--subscribe-rate",
                "-sr" }, description = "subscribe-rate (default -1 will be overwrite if not passed)", required = false)
        private int subscribeRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--position",
                "-p" }, description = "message position to reset back to (ledgerId:entryId)", required = false)
        private String messagePosition;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
                required = false, splitter = NoSplitter.class)
        private java.util.List<String> properties;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--position",
                "-p" }, description = "message position to start the scan from (ledgerId:entryId)", required = false)
        private String messagePosition;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--msg-publish-rate",
            "-m" }, description = "message-publish-rate (default -1 will be overwrite if not passed)", required = false)
        private int msgPublishRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"--clear", "-c"}, description = "Remove all properties",
                required = false)
        private boolean clear;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "--time",
                "-t" }, description = "time in minutes to reset back to "
                + "(or minutes, hours, days, weeks eg: 100m, 3h, 2d, 5w)", required = false)
        private String resetTimeStr;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "--relative-to-publish-rate",
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))", required = false)
        private boolean relativeToPublishRate = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--byte-dispatch-rate",
            "-bd" }, description = "byte-dispatch-rate (default -1 will be overwrite if not passed)", required = false)
        private long byteDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-w", "--wait-complete" },
                   description = "Wait for offloading to complete", required = false)
        private boolean wait = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-m", "--messagePosition" },
                description = "The position of messages (default 1)", required = false)
        private long messagePosition = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

         @Parameter(names = { "--byte-publish-rate",
            "-b" }, description = "byte-publish-rate (default -1 will be overwrite if not passed)", required = false)
        private long bytePublishRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--byte-dispatch-rate",
            "-bd" }, description = "byte-dispatch-rate (default -1 will be overwrite if not passed)", required = false)
        private long byteDispatchRate = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "--dispatch-rate-period",
                "-dt" }, description = "dispatch-rate-period in second type "
                + "(default 1 second will be overwrite if not passed)", required = false)
        private int dispatchRatePeriodSec = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
                required = false, splitter = NoSplitter.class)
        private java.util.List<String> properties;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        private String subName;

        @Parameter(names = { "-n", "--count" }, description = "Number of messages (default 1)", required = false)
        private int numMessages = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
            required = false, splitter = NoSplitter.class)
        private java.util.List<String> properties;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-w", "--wait-complete" },
                   description = "Wait for compaction to complete", required = false)
        private boolean wait = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        private String messageIdStr = "latest";

        @Parameter(names = { "-r", "--replicated" }, description = "replicated subscriptions", required = false)
        private boolean replicated = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-m" , "--messageId" }, description = "messageId where to create the subscription. "
                + "It can be either 'latest', 'earliest' or (ledgerId:entryId)", required = false)
        private String messageIdStr = "latest";

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_STORAGE_BK,
            required = false,
            doc = "Enable bookie secondary-isolation group if bookkeeperClientIsolationGroups doesn't have enough "
                    + "bookie available."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_SERVER,
            required = false,
            doc = "The metadata store URL. \n"
                    + " Examples: \n"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(
        dynamic = false,
        category = CATEGORY_SERVER,
        doc = "Whether the broker should count filtered entries in dispatch rate limit calculations. When disabled, "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(
            dynamic = false,
            category = CATEGORY_POLICIES,
            doc = "Enables evaluating subscription pattern on broker side."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_SERVER,
        required = false,
        deprecated = true,
        doc = "Configuration store connection string (as a comma-separated list). Deprecated in favor of "
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
            category = CATEGORY_SERVER,
            required = false,
            doc = "The metadata store URL for the configuration data. If empty, we fall back to use metadataStoreUrl"
    )
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_SERVER,
        required = false,
        deprecated = true,
        doc = "Global Zookeeper quorum connection string (as a comma-separated list)."
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    private int replicationConnectionsPerBroker = 16;
    @FieldContext(
        required = false,
        category = CATEGORY_REPLICATION,
        doc = "replicator prefix used for replicator producer name and cursor name"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
        required = false,
        doc = "Enable bookie isolation by specifying a list of bookie groups to choose from. \n\n"
            + "Any bookie outside the specified groups will not be used by the broker")
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(
            dynamic = false,
            category = CATEGORY_POLICIES,
            doc = "Max length of subscription pattern"
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(
        category = CATEGORY_SERVER,
        required = false,
        deprecated = true,
        doc = "The Zookeeper quorum connection string (as a comma-separated list). Deprecated in favour of "
```

## RuleId[ruleID=WhileLoopSpinsOnField]
### WhileLoopSpinsOnField
`while` loop spins on field
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/HdfsSyncThread.java`
#### Snippet
```java
    @Override
    public void run() {
       while (keepRunning) {
         try {
            Thread.sleep(syncInterval);
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/HdfsSyncThread.java`
#### Snippet
```java
    @Override
    public void run() {
       while (keepRunning) {
         try {
            Thread.sleep(syncInterval);
```

## RuleId[ruleID=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/writer/AvroWriter.java`
#### Snippet
```java
                outputBytes = this.byteArrayOutputStream.toByteArray();
            } catch (Exception ex) {
                throw new SchemaSerializationException(ex);
            }
            this.byteArrayOutputStream.reset();
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
                    loader.close();
                } catch (IOException e) {
                    throw new UncheckedIOException(e);
                }
            }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
                    loader.close();
                } catch (IOException e) {
                    throw new UncheckedIOException(e);
                }
            }
```

## RuleId[ruleID=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConnector.java`
#### Snippet
```java
            }

            if (clazz == null) {
                try {
                    clazz = Class.forName(name, true, classLoader);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConnector.java`
#### Snippet
```java
            }

            if (clazz == null) {
                try {
                    clazz = Class.forName(name, true, classLoader);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                if (ex instanceof PulsarAdminException) {
                    PulsarAdminException pae = (PulsarAdminException) ex;
                    if (pae.getStatusCode() == Status.NOT_FOUND.getStatusCode()) {
                        asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                "Internal topics have not been generated yet"));
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            if (exception != null) {
                                Throwable t = exception.getCause();
                                if (t instanceof NotFoundException) {
                                    asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                            getSubNotFoundErrorMessage(topicName.toString(), subName)));
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            if (exception != null) {
                                Throwable t = exception.getCause();
                                if (t instanceof NotFoundException) {
                                    asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                            getSubNotFoundErrorMessage(topicName.toString(), subName)));
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                future.whenComplete((r, ex) -> {
                                    if (ex != null) {
                                        if (ex instanceof PulsarAdminException) {
                                            asyncResponse.resume(new RestException((PulsarAdminException) ex));
                                            return;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                                    if (exception != null) {
                                                        Throwable t = exception.getCause();
                                                        if (t instanceof NotFoundException) {
                                                            asyncResponse.resume(new RestException(Status.NOT_FOUND,
                                                                    getSubNotFoundErrorMessage(topicName.toString(),
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                            AdvertisedListener listener =
                                    ownerInfo.getAdvertisedListeners().get(options.getAdvertisedListenerName());
                            if (listener == null) {
                                lookupFuture.completeExceptionally(
                                        new PulsarServerException("the broker do not have "
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java

                    if (key.equals(storedKey)) {
                        if (!onlyIfAbsent) {
                            // Over written an old value for same key
                            table[bucket + 1] = value;
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerOfflineBacklog.java`
#### Snippet
```java
        boolean toIncluded = range.upperBoundType() == BoundType.CLOSED;

        if (fromPosition.getLedgerId() == toPosition.getLedgerId()) {
            // If the 2 positions are in the same ledger
            long count = toPosition.getEntryId() - fromPosition.getEntryId() - 1;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                                                    TransactionImpl txn) {
        List<CompletableFuture<Void>> resultFutures = new ArrayList<>();
        if (ackType == AckType.Cumulative) {
            messageIdList.forEach(messageId -> resultFutures.add(doAcknowledge(messageId, ackType, properties, txn)));
            return CompletableFuture.allOf(resultFutures.toArray(new CompletableFuture[0]));
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        boolean toIncluded = range.upperBoundType() == BoundType.CLOSED;

        if (fromPosition.getLedgerId() == toPosition.getLedgerId()) {
            // If the 2 positions are in the same ledger
            long count = toPosition.getEntryId() - fromPosition.getEntryId() - 1;
```

## RuleId[ruleID=SimplifyOptionalCallChains]
### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchClient.java`
#### Snippet
```java
            return indexNameFormatter.indexName(record);
        }
        if (!record.getTopicName().isPresent()) {
            throw new IOException("Elasticsearch index name configuration and topic name are empty");
        }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java

        List<SchemaTableName> tableNames;
        if (!prefix.getTable().isPresent()) {
            tableNames = listTables(session, prefix.getSchema());
        } else {
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/stats/ProxyStats.java`
#### Snippet
```java

        Optional<Integer> logLevel = proxyService().getConfiguration().getProxyLogLevel();
        if (!logLevel.isPresent() || logLevel.get() < 2) {
            throw new RestException(Status.PRECONDITION_FAILED, "Proxy doesn't have logging level 2");
        }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
        Optional<PartitionedTopicMetadata> getResult =
                partitionedTopicResources.getPartitionedTopicMetadataAsync(topicName).get();
        if (!getResult.isPresent()) {
            partitionedTopicResources.createPartitionedTopic(topicName, new PartitionedTopicMetadata(numPartitions));
        } else {
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java
            Optional<PartitionedTopicMetadata> getResult =
                    partitionedTopicResources.getPartitionedTopicMetadataAsync(TRANSACTION_COORDINATOR_ASSIGN).get();
            if (!getResult.isPresent()) {
                partitionedTopicResources.createPartitionedTopic(TRANSACTION_COORDINATOR_ASSIGN,
                        new PartitionedTopicMetadata(1));
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
        return pulsar.getPulsarResources().getTenantResources().getTenantAsync(tenant)
                .thenCompose(tenantInfoOptional -> {
                    if (!tenantInfoOptional.isPresent()) {
                        throw new RestException(Status.NOT_FOUND, "Tenant does not exist");
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
        return pulsar().getPulsarResources().getTenantResources().getTenantAsync(tenant)
                .thenAccept(tenantInfo -> {
                    if (!tenantInfo.isPresent()) {
                        throw new RestException(Status.NOT_FOUND, "Tenant does not exist");
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
                .thenApply(webUrl -> {
                    // Ensure we get a url
                    if (webUrl == null || !webUrl.isPresent()) {
                        log.info("Unable to get web service url");
                        throw new RestException(Status.PRECONDITION_FAILED,
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
            Optional<URL> webUrl = nsService.getWebServiceUrl(bundle, options);
            // Ensure we get a url
            if (webUrl == null || !webUrl.isPresent()) {
                log.warn("Unable to get web service url");
                throw new RestException(Status.PRECONDITION_FAILED,
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
        return pulsar.getPulsarResources().getClusterResources().getClusterAsync(currentCluster)
                .thenCompose(cluster -> {
                    if (!cluster.isPresent() || cluster.get().getPeerClusterNames() == null) {
                        return CompletableFuture.completedFuture(null);
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
                            return pulsar.getPulsarResources().getClusterResources().getClusterAsync(peerCluster)
                                    .thenApply(ret -> {
                                        if (!ret.isPresent()) {
                                            throw new RestException(Status.NOT_FOUND,
                                                    "Peer cluster " + peerCluster + " data not found");
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
        return nsService.getWebServiceUrlAsync(bundle, options)
                .thenCompose(webUrl -> {
                    if (webUrl == null || !webUrl.isPresent()) {
                        log.warn("Unable to get web service url");
                        throw new RestException(Status.PRECONDITION_FAILED,
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
        .thenAccept(t -> {
            // TODO: Check message backlog and fail if backlog too large.
            if (!t.isPresent()) {
                // Topic not found, and remove from owning partition list.
                publishResult.completeExceptionally(new BrokerServiceException.TopicNotFoundException("Topic not "
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
            pulsar().getBrokerService().getTopic(topicPartitionName, false)
            .thenAccept(topic -> {
                if (!topic.isPresent()) {
                    future.completeExceptionally(new BrokerServiceException.TopicNotFoundException(
                            "Topic " + topicPartitionName + " not found"));
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java

        lookupFuture.thenAccept(optionalResult -> {
            if (optionalResult == null || !optionalResult.isPresent()) {
                if (log.isDebugEnabled()) {
                    log.debug("Fail to lookup topic for rest produce message request for topic {}.",
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
   protected void validateClusterExists(String cluster) {
        try {
            if (!clusterResources().getCluster(cluster).isPresent()) {
                throw new RestException(Status.PRECONDITION_FAILED, "Cluster " + cluster + " does not exist.");
            }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TransactionsBase.java`
#### Snippet
```java
            }
            return topicFuture.thenCompose(optionalTopic -> {
                if (!optionalTopic.isPresent()) {
                    return FutureUtil.failedFuture(new RestException(NOT_FOUND, "Topic not found"));
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/BrokersBase.java`
#### Snippet
```java
        return pulsar().getBrokerService().getTopic(topicName, true)
            .thenCompose(topicOptional -> {
                if (!topicOptional.isPresent()) {
                    LOG.error("[{}] Fail to run health check while get topic {}. because get null value.",
                            clientAppId(), topicName);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TenantsBase.java`
#### Snippet
```java
                .thenCompose(__ -> tenantResources().getTenantAsync(tenant))
                .thenCompose(tenantAdmin -> {
                    if (!tenantAdmin.isPresent()) {
                        throw new RestException(Status.NOT_FOUND, "Tenant " + tenant + " not found");
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TenantsBase.java`
#### Snippet
```java
                .thenCompose(__ -> tenantResources().getTenantAsync(tenant))
                .thenApply(tenantInfo -> {
                    if (!tenantInfo.isPresent()) {
                        throw new RestException(Status.NOT_FOUND, "Tenant does not exist");
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ClustersBase.java`
#### Snippet
```java
            return namespaceIsolationPolicies().getIsolationDataPoliciesAsync(cluster)
                    .thenApply(namespaceIsolationPolicies -> {
                        if (!namespaceIsolationPolicies.isPresent()) {
                            throw new RestException(Status.NOT_FOUND,
                                    "NamespaceIsolationPolicies for cluster " + cluster + " does not exist");
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ClustersBase.java`
#### Snippet
```java
                return clusterResources().getClusterAsync(peerCluster)
                        .thenAccept(peerClusterOpt -> {
                            if (!peerClusterOpt.isPresent()) {
                                throw new RestException(PRECONDITION_FAILED,
                                        "Peer cluster " + peerCluster + " does not exist");
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
            }

            if (!config.getWebServicePort().isPresent() && !config.getWebServicePortTls().isPresent()) {
                throw new IllegalArgumentException("webServicePort/webServicePortTls must be present");
            }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
            }

            if (!config.getWebServicePort().isPresent() && !config.getWebServicePortTls().isPresent()) {
                throw new IllegalArgumentException("webServicePort/webServicePortTls must be present");
            }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
            }

            if (!config.getLoadBalancerOverrideBrokerNicSpeedGbps().isPresent()
                    && config.isLoadBalancerEnabled()
                    && LinuxInfoUtils.isLinux()
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java
                            }

                            if (!lookupResult.isPresent()) {
                                lookupfuture.complete(newLookupErrorResponse(ServerError.ServiceNotReady,
                                        "No broker was available to own " + topicName, requestId));
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java

                    return lookupFuture.thenApply(optionalResult -> {
                        if (optionalResult == null || !optionalResult.isPresent()) {
                            log.warn("No broker was found available for topic {}", topicName);
                            throw new WebApplicationException(Response.Status.SERVICE_UNAVAILABLE);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
                    log.debug("[{}] Got schema locator {}", schemaId, locator);
                }
                if (!locator.isPresent()) {
                    return completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
                        future.completeExceptionally(ex);
                    } else {
                        if (!locator.isPresent()) {
                            future.complete(null);
                            return;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
            }

            if (!locator.isPresent()) {
                return completedFuture(null);
            }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
            }

            if (!locator.isPresent()) {
                ledgerIdsFuture.complete(Collections.emptyList());
                return;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
            }

            if (!locator.isPresent()) {
                result.complete(Collections.emptyList());
                return;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    @Override
    public boolean initializeDispatchRateLimiterIfNeeded() {
        if (!dispatchRateLimiter.isPresent() && DispatchRateLimiter.isDispatchRateEnabled(
                topic.getSubscriptionDispatchRate(getSubscriptionName()))) {
            this.dispatchRateLimiter =
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
                .thenCompose(op -> {
                    if (!op.isPresent()) {
                        return CompletableFuture.completedFuture(null);
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                PolicyOperation.READ)
                .thenCompose(n -> getTopicPoliciesAsyncWithRetry(topicName).thenApply(op -> {
                    if (!op.isPresent()) {
                        return null;
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        pulsar().getBrokerService().getTopicIfExists(topicName.toString())
                .thenAccept(opt -> {
                    if (!opt.isPresent()) {
                        throw new RestException(Status.NOT_FOUND,
                                getTopicNotFoundErrorMessage(topicName.toString()));
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        pulsar().getBrokerService().getTopicIfExists(topicName.toString())
            .thenAccept(opt -> {
                if (!opt.isPresent()) {
                    throw new RestException(Status.NOT_FOUND,
                        getTopicNotFoundErrorMessage(topicName.toString()));
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                .thenCompose(__ -> namespaceResources().getPoliciesAsync(namespaceName)
            .thenApply(policies -> {
                if (!policies.isPresent()) {
                    throw new RestException(Status.NOT_FOUND, "Namespace does not exist");
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return pulsar().getBrokerService().getTopicIfExists(topicName.toString())
                .thenApply(opt -> {
                    if (!opt.isPresent()) {
                        throw new RestException(Status.NOT_FOUND,
                                getTopicNotFoundErrorMessage(topicName.toString()));
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
                .thenCompose(op -> {
                    if (!op.isPresent()) {
                        return CompletableFuture.completedFuture(null);
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        return getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
            .thenCompose(op -> {
                if (!op.isPresent()) {
                    return CompletableFuture.completedFuture(null);
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        getTopicPoliciesAsyncWithRetry(topicName, isGlobal)
                        .thenCompose(op -> {
                            if (!op.isPresent()) {
                                return CompletableFuture.completedFuture(null);
                            }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
    public void initializeDispatchRateLimiterIfNeeded() {
        synchronized (dispatchRateLimiterLock) {
            if (!dispatchRateLimiter.isPresent()
                && DispatchRateLimiter.isDispatchRateEnabled(topic.getReplicatorDispatchRate())) {
                this.dispatchRateLimiter = Optional.of(new DispatchRateLimiter(topic, Type.REPLICATOR));
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    @Override
    public boolean initializeDispatchRateLimiterIfNeeded() {
        if (!dispatchRateLimiter.isPresent() && DispatchRateLimiter.isDispatchRateEnabled(
                topic.getSubscriptionDispatchRate(getSubscriptionName()))) {
            this.dispatchRateLimiter =
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

        synchronized (this) {
            if (!delayedDeliveryTracker.isPresent()) {
                if (!msgMetadata.hasDeliverAtTime()) {
                    // No need to initialize the tracker here
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                                service.getTopic(topicName.toString(), createTopicIfDoesNotExist))
                        .thenCompose(optTopic -> {
                            if (!optTopic.isPresent()) {
                                return FutureUtil
                                        .failedFuture(new TopicNotFoundException(
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/OwnershipCache.java`
#### Snippet
```java
    public CompletableFuture<Boolean> checkOwnershipAsync(NamespaceBundle bundle) {
        Optional<CompletableFuture<OwnedBundle>> ownedBundleFuture = getOwnedBundleAsync(bundle);
        if (!ownedBundleFuture.isPresent()) {
            return CompletableFuture.completedFuture(false);
        }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LinuxInfoUtils.java`
#### Snippet
```java
        try (Stream<String> stream = Files.lines(Paths.get(PROC_STAT_PATH))) {
            Optional<String> first = stream.findFirst();
            if (!first.isPresent()) {
                log.error("[LinuxInfo] Failed to read CPU usage from /proc/stat, because of empty values.");
                return ResourceUsage.empty();
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                .thenCompose(__ -> namespaceResources().getPoliciesAsync(namespaceName))
                .thenApply(policiesOpt -> {
                    if (!policiesOpt.isPresent()) {
                        throw new RestException(Response.Status.NOT_FOUND, "Namespace policies does not exist");
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
                .getPoliciesAsync(TopicName.get(topic).getNamespaceObject())
                .thenCompose(optPolicies -> {
                    if (!optPolicies.isPresent()) {
                        log.warn("[{}] Policies not present and isEncryptionRequired will be set to false", topic);
                        isEncryptionRequired = false;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
            Optional<LocalPolicies> nsPolicies = pulsar.getPulsarResources().getLocalPolicies()
                    .getLocalPolicies(NamespaceName.get(namespace));
            if (!nsPolicies.isPresent() || StringUtils.isBlank(nsPolicies.get().namespaceAntiAffinityGroup)) {
                return true;
            }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
                }

                if (!broker.isPresent()) {
                    // No brokers available
                    return broker;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
                String key = String.format("%s/%s", LoadManager.LOADBALANCE_BROKERS_ROOT, broker);
                Optional<LocalBrokerData> localData = brokersData.readLock(key).get();
                if (!localData.isPresent()) {
                    brokerDataMap.remove(broker);
                    log.info("[{}] Broker load report is not present", broker);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
        pulsar.getPulsarResources().getLocalPolicies().getLocalPoliciesAsync(NamespaceName.get(namespaceName))
                .thenAccept(policies -> {
            if (!policies.isPresent() || StringUtils.isBlank(policies.get().namespaceAntiAffinityGroup)) {
                antiAffinityNsBrokersResult.complete(null);
                return;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        synchronized (dispatchRateLimiterLock) {
            // dispatch rate limiter for topic
            if (!dispatchRateLimiter.isPresent()
                && DispatchRateLimiter.isDispatchRateEnabled(topicPolicies.getDispatchRate().get())) {
                this.dispatchRateLimiter = Optional.of(new DispatchRateLimiter(this, Type.TOPIC));
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                .getPoliciesAsync(TopicName.get(topic).getNamespaceObject())
                .thenAccept(optPolicies -> {
                    if (!optPolicies.isPresent()) {
                        isEncryptionRequired = false;
                        updatePublishDispatcher();
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
    public CompletableFuture<Boolean> isServiceUnitActiveAsync(TopicName topicName) {
        Optional<CompletableFuture<OwnedBundle>> res = ownershipCache.getOwnedBundleAsync(getBundle(topicName));
        if (!res.isPresent()) {
            return CompletableFuture.completedFuture(false);
        }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
    private Optional<Pair<String, String>> getLeastLoadedFromLoadManager(ServiceUnitId serviceUnit) throws Exception {
        Optional<ResourceUnit> leastLoadedBroker = loadManager.get().getLeastLoaded(serviceUnit);
        if (!leastLoadedBroker.isPresent()) {
            LOG.warn("No broker is available for {}", serviceUnit);
            return Optional.empty();
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
            // First check if we or someone else already owns the bundle
            ownershipCache.getOwnerAsync(bundle).thenAccept(nsData -> {
                if (!nsData.isPresent()) {
                    // No one owns this bundle

```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java

        NamespaceBundle nsFullBundle = getFullBundle(namespaceName);
        if (!getOwner(nsFullBundle).isPresent()) {
            // No one owns the namespace so no point trying to unload it
            // Next lookup will assign the bundle to this broker.
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                        if (!leaderBrokerActive) {
                            makeLoadManagerDecisionOnThisBroker = true;
                            if (!currentLeader.isPresent()) {
                                LOG.warn(
                                        "The information about the current leader broker wasn't available. "
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                    if (makeLoadManagerDecisionOnThisBroker) {
                        Optional<Pair<String, String>> availableBroker = getLeastLoadedFromLoadManager(bundle);
                        if (!availableBroker.isPresent()) {
                            LOG.warn("Load manager didn't return any available broker. "
                                            + "Returning empty result to lookup. NamespaceBundle[{}]",
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
        return pulsar.getPulsarResources().getNamespaceResources().getPoliciesAsync(namespace)
                .thenCompose(optPolicies -> {
                    if (!optPolicies.isPresent()) {
                        return CompletableFuture.completedFuture(getBundles(namespace, Optional.empty()));
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            dynamicConfigResources.getDynamicConfigurationAsync()
                    .thenAccept(optMap -> {
                        if (!optMap.isPresent()) {
                            return;
                        }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        pulsar.getPulsarResources().getLocalPolicies().getLocalPoliciesAsync(namespace)
                .thenAcceptAsync(optLocalPolicies -> {
                    if (!optLocalPolicies.isPresent()) {
                        return;
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

            // create dynamic-config if not exist.
            if (!configCache.isPresent()) {
                pulsar().getPulsarResources().getDynamicConfigResources()
                        .setDynamicConfigurationWithCreate(n -> new HashMap<>());
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        pulsar.getPulsarResources().getNamespaceResources().getPoliciesAsync(namespace)
                .thenAcceptAsync(optPolicies -> {
                    if (!optPolicies.isPresent()) {
                        return;
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            if (topicFuture != null) {
                if (topicFuture.isCompletedExceptionally()
                        || (topicFuture.isDone() && !topicFuture.getNow(Optional.empty()).isPresent())) {
                    // Exceptional topics should be recreated.
                    topics.remove(topicName.toString(), topicFuture);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                        } else {
                            return topicFuture.thenCompose(value -> {
                                if (!value.isPresent()) {
                                    // retry and create topic
                                    return getTopic(topicName, createIfMissing, properties);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

            // create dynamic-config if not exist.
            if (!configCache.isPresent()) {
                pulsar().getPulsarResources().getDynamicConfigResources()
                        .setDynamicConfigurationWithCreate(n -> new HashMap<>());
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        // Check whether there are auth policies for the topic
        pulsar.getPulsarResources().getNamespaceResources().getPoliciesAsync(namespaceName).thenAccept(optPolicies -> {
            if (!optPolicies.isPresent() || !optPolicies.get().auth_policies.getTopicAuthentication()
                    .containsKey(topic)) {
                // if there is no auth policy for the topic, just complete and return
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
        try {
            Optional<GetResult> res = store.get(path).get();
            if (!res.isPresent()) {
                throw new BookieException.CookieNotFoundException(bookieId.toString());
            }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
        cache.getWithStats(ledgerPath)
                .thenAccept(optRes -> {
                    if (!optRes.isPresent()) {
                        if (log.isDebugEnabled()) {
                            log.debug("No such ledger: {} at path {}", ledgerId, ledgerPath);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java

            // If we didn't get any valid IDs from the directory...
            if (!largest.isPresent()) {
                // else, Start at HOB-0000000001;
                return createHOBPathAndGenerateId(ledgerPrefix, 1);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        // get the existing underreplicated ledger data
        store.get(path).thenAccept(optRes -> {
            if (!optRes.isPresent()) {
                tryMarkLedgerUnderreplicatedAsync(path, missingReplicas, finalFuture);
                return;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        try {
            Optional<GetResult> optRes = store.get(placementPolicyCheckCtimePath).get();
            if (!optRes.isPresent()) {
                log.warn("placementPolicyCheckCtimeZnode is not yet available");
                return -1;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        try {
            Optional<GetResult> optRes = store.get(checkAllLedgersCtimePath).get();
            if (!optRes.isPresent()) {
                log.warn("checkAllLedgersCtimeZnode is not yet available");
                return -1;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java

                    Optional<GetResult> optRes = store.get(parent + "/" + tryChild).get();
                    if (!optRes.isPresent()) {
                        if (log.isDebugEnabled()) {
                            log.debug("{}/{} doesn't exist", parent, tryChild);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        try {
            Optional<GetResult> optRes = store.get(replicasCheckCtimePath).get();
            if (!optRes.isPresent()) {
                log.warn("placementPolicyCheckCtimeZnode is not yet available");
                return -1;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java

            Optional<GetResult> optRes = store.get(path).get();
            if (!optRes.isPresent()) {
                if (log.isDebugEnabled()) {
                    log.debug("Ledger: {} is not marked underreplicated", ledgerId);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        try {
            Optional<GetResult> optRes = store.get(getUrLedgerLockPath(urLockPath, ledgerId)).get();
            if (!optRes.isPresent()) {
                // this is ok.
                return null;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
        return store.get(path)
                .thenCompose(optRes -> {
                    if (!optRes.isPresent()) {
                        return FutureUtils.value(Optional.empty());
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
        return executeWithRetry(() -> objCache.get(path)
                .thenCompose(optEntry -> {
                    if (!optEntry.isPresent()) {
                        return FutureUtils.exception(new NotFoundException(""));
                    }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
        return store.get(path)
                .thenCompose(optGetResult -> {
                    if (!optGetResult.isPresent()) {
                        // The lock just disappeared, try to acquire it again
                        // Reset the expectation on the version
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/ClearTextFunctionTokenAuthProvider.java`
#### Snippet
```java
    public void configureAuthenticationConfig(AuthenticationConfig authConfig,
                                              Optional<FunctionAuthData> functionAuthData) {
        if (!functionAuthData.isPresent()) {
            // if auth data is not present maybe user is trying to use anonymous role thus don't pass in any auth config
            authConfig.setClientAuthenticationPlugin(null);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/KubernetesSecretsTokenAuthProvider.java`
#### Snippet
```java
    public void configureAuthenticationConfig(AuthenticationConfig authConfig,
                                              Optional<FunctionAuthData> functionAuthData) {
        if (!functionAuthData.isPresent()) {
            // if auth data is not present maybe user is trying to use anonymous role thus don't pass in any auth config
            authConfig.setClientAuthenticationPlugin(null);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/KubernetesSecretsTokenAuthProvider.java`
#### Snippet
```java
    public void cleanUpAuthData(Function.FunctionDetails funcDetails, Optional<FunctionAuthData> functionAuthData)
            throws Exception {
        if (!functionAuthData.isPresent()) {
            return;
        }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/KubernetesSecretsTokenAuthProvider.java`
#### Snippet
```java
    @Override
    public void configureAuthDataStatefulSet(V1StatefulSet statefulSet, Optional<FunctionAuthData> functionAuthData) {
        if (!functionAuthData.isPresent()) {
            return;
        }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java
        @Override
        public TypedMessageBuilder<T> newMessage(AbstractSinkRecord<T> record) {
            if (!record.getPartitionId().isPresent()) {
                throw new RuntimeException(
                        "PartitionId needs to be specified for every record while in Effectively-once mode");
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java
        public void sendOutputMessage(TypedMessageBuilder<T> msg, AbstractSinkRecord<T> record) {

            if (!record.getRecordSequence().isPresent()) {
                throw new RuntimeException(
                        "RecordSequence needs to be specified for every record while in Effectively-once mode");
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/MergeTopicFunction.java`
#### Snippet
```java
        if (context.getCurrentRecord().getMessage().isPresent()) {
            Message<?> msg =  context.getCurrentRecord().getMessage().get();
            if (!msg.getReaderSchema().isPresent()) {
                log.warn("The reader schema is null.");
                return null;
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
    private WindowConfig getWindowConfigs(Context context) {

        if (!context.getUserConfigValue(WindowConfig.WINDOW_CONFIG_KEY).isPresent()) {
            throw new IllegalArgumentException("Window Configs cannot be found");
        }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/IsolatedBookieEnsemblePlacementPolicy.java`
#### Snippet
```java
                        ? future.join() : Optional.empty();

                if (!optRes.isPresent()) {
                    return blacklistedBookies;
                }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/validator/MultipleListenerValidator.java`
#### Snippet
```java
            }
            String listenerName = StringUtils.trim(str.substring(0, index));
            if (!firstListenerName.isPresent()) {
                firstListenerName = Optional.of(listenerName);
            }
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/PulsarAuthorizationProvider.java`
#### Snippet
```java
        return pulsarResources.getNamespaceResources().getPoliciesAsync(topicName.getNamespaceObject())
                .thenCompose(policies -> {
                    if (!policies.isPresent()) {
                        if (log.isDebugEnabled()) {
                            log.debug("Policies node couldn't be found for topic : {}", topicName);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/PulsarAuthorizationProvider.java`
#### Snippet
```java
        return pulsarResources.getNamespaceResources().getPoliciesAsync(topicName.getNamespaceObject())
                .thenApply(policies -> {
                    if (!policies.isPresent()) {
                        if (log.isDebugEnabled()) {
                            log.debug("Policies node couldn't be found for topic : {}", topicName);
```

### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/PulsarAuthorizationProvider.java`
#### Snippet
```java
                                                                       AuthAction authAction) {
        return pulsarResources.getNamespaceResources().getPoliciesAsync(namespaceName).thenApply(policies -> {
            if (!policies.isPresent()) {
                if (log.isDebugEnabled()) {
                    log.debug("Policies node couldn't be found for namespace : {}", namespaceName);
```

## RuleId[ruleID=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBAbstractSink.java`
#### Snippet
```java
        influxDBSinkConfig.validate();

        super.init(influxDBSinkConfig.getBatchTimeMs(), influxDBSinkConfig.getBatchSize());

        try {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSink.java`
#### Snippet
```java
        InfluxDBSinkConfig influxDBSinkConfig = InfluxDBSinkConfig.load(config);
        influxDBSinkConfig.validate();
        super.init(influxDBSinkConfig.getBatchTimeMs(), influxDBSinkConfig.getBatchSize());

        influxDBClient = influxDBClientBuilder.build(influxDBSinkConfig);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/AdminProxyHandler.java`
#### Snippet
```java
                : config.getFunctionWorkerWebServiceURL();

        super.setTimeout(config.getHttpProxyTimeout());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
        public CompletableFuture<Void> close() {
            if (isRunning.compareAndSet(true, false)) {
                super.interrupt();
            }
            return closeHandle;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/StatsOutputStream.java`
#### Snippet
```java
    public StatsOutputStream writeItem(double d) {
        checkSeparator();
        super.write(d);
        return this;
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/StatsOutputStream.java`
#### Snippet
```java
    public StatsOutputStream writeItem(long n) {
        checkSeparator();
        super.write(n);
        return this;
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/StatsOutputStream.java`
#### Snippet
```java
    public StatsOutputStream writeItem(boolean value) {
        checkSeparator();
        super.write(value);
        return this;
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ChunkMessageIdImpl.java`
#### Snippet
```java

        // write last chunk message id
        MessageIdData msgId = super.writeMessageIdData(null, -1, 0);

        // write first chunk message id
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedTopicProducerStatsRecorderImpl.java`
#### Snippet
```java

    void updateCumulativeStats(String partition, ProducerStats stats) {
        super.updateCumulativeStats(stats);
        if (stats == null) {
            return;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicConsumerStatsRecorderImpl.java`
#### Snippet
```java

    public void updateCumulativeStats(String partition, ConsumerStats stats) {
        super.updateCumulativeStats(stats);
        partitionStats.put(partition, stats);
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarMetadataClientDriver.java`
#### Snippet
```java
                                           StatsLogger statsLogger,
                                           Optional<Object> optionalCtx) throws MetadataException {
        super.initialize(clientConfiguration);
        return this;
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/FunctionCollectorRegistryImpl.java`
#### Snippet
```java
            if (existingCollector == null) {
                namesToCollectors.put(metricName, collector);
                super.register(collector);
                return collector;
            }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/admin/v1/WebSocketProxyStatsV1.java`
#### Snippet
```java
    public ProxyTopicStat getStats(@PathParam("tenant") String tenant, @PathParam("cluster") String cluster,
            @PathParam("namespace") String namespace, @PathParam("topic") @Encoded String encodedTopic) {
        return super.internalGetStats(
                TopicName.get("persistent", tenant, cluster, namespace, decode(encodedTopic)));
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/admin/v2/WebSocketProxyStatsV2.java`
#### Snippet
```java
    public ProxyTopicStat getStats(@PathParam("domain") String domain, @PathParam("tenant") String tenant,
            @PathParam("namespace") String namespace, @PathParam("topic") @Encoded String encodedTopic) {
        return super.internalGetStats(TopicName.get(domain, tenant, namespace, decode(encodedTopic)));
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java
    public CompletableFuture<Void> setDynamicConfigurationWithCreateAsync(
            Function<Optional<Map<String, String>>, Map<String, String>> createFunction) {
        return super.setWithCreateAsync(BROKER_SERVICE_CONFIGURATION_PATH, createFunction);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java
                                 Function<Optional<Map<String, String>>, Map<String, String>> createFunction)
            throws MetadataStoreException {
        super.setWithCreate(BROKER_SERVICE_CONFIGURATION_PATH, createFunction);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java
    public CompletableFuture<Void> setDynamicConfigurationAsync(
            Function<Map<String, String>, Map<String, String>> updateFunction){
        return super.setAsync(BROKER_SERVICE_CONFIGURATION_PATH, updateFunction);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/DynamicConfigurationResources.java`
#### Snippet
```java
            Function<Map<String, String>, Map<String, String>> updateFunction)
            throws MetadataStoreException {
        super.set(BROKER_SERVICE_CONFIGURATION_PATH, updateFunction);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BrokerResources.java`
#### Snippet
```java

    public Set<String> listActiveBrokers() throws MetadataStoreException {
        return new HashSet<>(super.getChildren(BASE_CLUSTERS_PATH));
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/ClusterResources.java`
#### Snippet
```java

    public Set<String> list() throws MetadataStoreException {
        return new HashSet<>(super.getChildren(BASE_CLUSTERS_PATH));
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
                throws MetadataStoreException {
            Optional<Map<String, NamespaceIsolationDataImpl>> data =
                    super.get(joinPath(BASE_CLUSTERS_PATH, cluster, NAMESPACE_ISOLATION_POLICIES));
            return data.isPresent() ? Optional.of(new NamespaceIsolationPolicies(data.get())) : Optional.empty();
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
    public boolean namespaceExists(NamespaceName ns) throws MetadataStoreException {
        String path = joinPath(BASE_POLICIES_PATH, ns.toString());
        return super.exists(path) && super.getChildren(path).isEmpty();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
    public boolean namespaceExists(NamespaceName ns) throws MetadataStoreException {
        String path = joinPath(BASE_POLICIES_PATH, ns.toString());
        return super.exists(path) && super.getChildren(path).isEmpty();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java

    public CompletableFuture<Boolean> getPoliciesReadOnlyAsync() {
        return super.existsAsync(POLICIES_READONLY_FLAG_PATH);
    }

```

## RuleId[ruleID=SlowListContainsAll]
### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
            }

            if (!NamespaceBundleSplitAlgorithm.AVAILABLE_ALGORITHMS.containsAll(
                    brokerConfig.getSupportedNamespaceBundleSplitAlgorithms())) {
                throw new IllegalArgumentException(
```

## RuleId[ruleID=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/ProcessedFileThread.java`
#### Snippet
```java
    public void run() {
        try {
            while (true) {
                File file = recentlyProcessed.take();
                handle(file);
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java
    public void run() {
        try {
            while (true) {
                File file = workQueue.take();

```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileListingThread.java`
#### Snippet
```java

    public void run() {
        while (true) {
            if ((queueLastUpdated.get() < System.currentTimeMillis() - pollingInterval) && listingLock.tryLock()) {
                try {
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
            AsyncResultConsumer asyncResultConsumer = this::handleResult;

            while (true) {
                currentRecord = readInput();

```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
                // Send messages on all topics/producers
                long totalSent = 0;
                while (true) {
                    for (String topic : producersMap.keySet()) {
                        if (arguments.testTime > 0 && System.nanoTime() > testEndTime) {
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
        final ServerSocket serverSocket = new ServerSocket(port);

        while (true) {
            // Technically, two controllers can be connected simultaneously, but
            // non-sequential handling of commands
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
                    // Send messages on all topics/producers
                    long totalSent = 0;
                    while (true) {
                        for (int j = 0; j < nunManagedLedgersForThisThread; j++) {
                            if (arguments.testTime > 0) {
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/BrokerMonitor.java`
#### Snippet
```java
            final BrokerWatcher brokerWatcher = new BrokerWatcher(zkClient);
            brokerWatcher.updateBrokers(BROKER_ROOT);
            while (true) {
                Thread.sleep(GLOBAL_STATS_PRINT_PERIOD_MILLIS);
                printGlobalData();
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
            // Users wishing to stop this should Ctrl + C and use another
            // Controller to send new commands.
            while (true) {}
        }
    }
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
    public void run() throws Exception {
        BufferedReader inReader = new BufferedReader(new InputStreamReader(System.in));
        while (true) {
            // Print the very simple prompt.
            System.out.println();
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
            AtomicLong numMessageSend = new AtomicLong(0);
            Semaphore numMsgPerTxnLimit = new Semaphore(arguments.numMessagesPerTransaction);
            while (true) {
                for (Producer<byte[]> producer : producers) {
                    if (arguments.testTime > 0) {
```

## RuleId[ruleID=Lombok]
### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/util/CmdGenerateDocumentation.java`
#### Snippet
```java
import org.apache.pulsar.proxy.server.ProxyConfiguration;

@Data
@Parameters(commandDescription = "Generate documentation automatically.")
@Slf4j
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/CmdGenerateDocumentation.java`
#### Snippet
```java
import org.apache.pulsar.websocket.service.WebSocketProxyConfiguration;

@Data
@Parameters(commandDescription = "Generate documentation automatically.")
@Slf4j
```

### Lombok
Not generated 'isResourceGroupRateLimitingEnabled'(): A method with similar name 'isResourceGroupRateLimitingEnabled' already exists
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    protected boolean preciseTopicPublishRateLimitingEnable;

    @Getter
    protected boolean resourceGroupRateLimitingEnabled;

```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/CmdGenerateDocumentation.java`
#### Snippet
```java
import org.apache.pulsar.common.util.BaseGenerateDocumentation;

@Data
@Parameters(commandDescription = "Generate documentation automatically.")
@Slf4j
```

### Lombok
Not generated 'getMsgDropRate'(): A method with similar name 'getMsgDropRate' already exists
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/stats/NonPersistentTopicStatsImpl.java`
#### Snippet
```java
     * messages per connection.
     **/
    @Getter
    public double msgDropRate;

```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/SingleConsumerPulsarSourceConfig.java`
#### Snippet
```java
import org.apache.pulsar.common.util.ObjectMapperFactory;

@Data
public class SingleConsumerPulsarSourceConfig extends PulsarSourceConfig {

```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/MultiConsumerPulsarSourceConfig.java`
#### Snippet
```java
import org.apache.pulsar.common.util.ObjectMapperFactory;

@Data
public class MultiConsumerPulsarSourceConfig extends PulsarSourceConfig {

```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java

    @Deprecated
    @Data
    /**
     * @Deprecated in favor for using functionRuntimeFactoryClassName and functionRuntimeFactoryConfigs
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java

    @Deprecated
    @Data
    /**
     * @Deprecated in favor for using functionRuntimeFactoryClassName and functionRuntimeFactoryConfigs
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java

    @Deprecated
    @Data
    /**
     * @Deprecated in favor for using functionRuntimeFactoryClassName and functionRuntimeFactoryConfigs
```

### Lombok
Not generated 'getLatestSystemExceptions'(): A method with similar name 'getLatestSystemExceptions' already exists
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    private Counter.Child statTotalWrittenChild1min;

    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
```

### Lombok
Not generated 'getLatestSourceExceptions'(): A method with similar name 'getLatestSourceExceptions' already exists
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SourceStatsManager.java`
#### Snippet
```java
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSourceExceptions =
            EvictingQueue.create(10);
```

### Lombok
Not generated 'getLatestSystemExceptions'(): A method with similar name 'getLatestSystemExceptions' already exists
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
    private Counter.Child statTotalWrittenChild1min;

    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
```

### Lombok
Not generated 'getLatestSinkExceptions'(): A method with similar name 'getLatestSinkExceptions' already exists
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/SinkStatsManager.java`
#### Snippet
```java
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSystemExceptions =
            EvictingQueue.create(10);
    @Getter
    private EvictingQueue<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSinkExceptions =
            EvictingQueue.create(10);
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/impl/MinAvailablePolicy.java`
#### Snippet
```java
 * Implementation of min available policy.
 */
@Data
@AllArgsConstructor
public class MinAvailablePolicy extends AutoFailoverPolicy {
```

## RuleId[ruleID=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-io/aws/src/main/java/org/apache/pulsar/io/aws/STSAssumeRoleProviderPlugin.java`
#### Snippet
```java
    public software.amazon.awssdk.auth.credentials.AwsCredentialsProvider getV2CredentialsProvider() {
        StsClient client = StsClient.create();
        return StsAssumeRoleCredentialsProvider.builder().stsClient(client).refreshRequest((req) -> {
            req.roleArn(roleArn).roleSessionName(roleSessionName).build();
        }).build();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-io/nsq/src/main/java/org/apache/pulsar/io/nsq/NSQSource.java`
#### Snippet
```java

        Thread runnerThread = new Thread(() -> {
            subscriber.subscribe(config.getTopic(), config.getChannel(), (byte[]data) ->{
                consume(new NSQRecord(data));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
                if (t instanceof MongoBulkWriteException) {
                    // With this exception, we are aware of the items that have not been inserted.
                    ((MongoBulkWriteException) t).getWriteErrors().forEach(err -> {
                        idxToFail.add(err.getIndex());
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSource.java`
#### Snippet
```java
        schedulerThread.setDaemon(true);
        threadEx = null;
        schedulerThread.setUncaughtExceptionHandler((t, ex) -> {
            threadEx = ex;
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
    @Override
    public void offset(Map<TopicPartition, Long> map) {
        map.forEach((key, value) -> {
            seekAndUpdateOffset(key, value);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `buildtools/src/main/java/org/apache/pulsar/tests/FastThreadLocalCleanupListener.java`
#### Snippet
```java
        if (FAST_THREAD_LOCAL_CLEANUP_ENABLED && FastThreadLocalStateCleaner.isEnabled()) {
            LOG.info("Cleaning up FastThreadLocal thread local state.");
            CLEANER.cleanupAllFastThreadLocals((thread, value) -> {
                LOG.info("Cleaning FastThreadLocal state for thread {}, instance of class {}, value is {}", thread,
                        value.getClass().getName(), value);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
                .collect(Collectors.toList());

        configs.forEach(x -> {
            x.put(PulsarKafkaWorkerConfig.OFFSET_STORAGE_TOPIC_CONFIG, kafkaSinkConfig.getOffsetStorageTopic());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            // start deserialize thread
            deserializeEntries = new DeserializeEntries();
            deserializeEntries.setUncaughtExceptionHandler((t, ex) -> {
                deserializingError = ex;
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java
                }
            });
            this.topicStats.forEach((topic, stats) -> {
                stats.calculate();
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java
            ChannelInitializer<SocketChannel>>> protocolHandlers, ServerBootstrap serverBootstrap) {

        protocolHandlers.forEach((extensionName, initializers) -> {
            initializers.forEach((address, initializer) -> {
                try {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/WebServer.java`
#### Snippet
```java
            Arrays.stream(server.getConnectors())
                .filter(c -> c instanceof ServerConnector)
                .findFirst().ifPresent(c -> {
                        WebServer.this.externalServicePort = ((ServerConnector) c).getPort();
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
    public void close () {
        this.internalPinnedExecutor.shutdown();
        stores.forEach((tcId, metadataStore) -> {
            metadataStore.closeAsync().whenComplete((v, ex) -> {
                if (ex != null) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/SchemasResource.java`
#### Snippet
```java
        validateTopicName(tenant, cluster, namespace, topic);
        deleteSchemaAsync(authoritative, force)
                .thenAccept(version -> {
                    response.resume(DeleteSchemaResponse.builder().version(getLongSchemaVersion(version)).build());
                })
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/WebService.java`
#### Snippet
```java
        context.addServlet(servletHolder, MATCH_ALL);
        if (attributeMap != null) {
            attributeMap.forEach((key, value) -> {
                context.setAttribute(key, value);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/SchemasResource.java`
#### Snippet
```java
        validateTopicName(tenant, namespace, topic);
        deleteSchemaAsync(authoritative, force)
                .thenAccept(version -> {
                    response.resume(DeleteSchemaResponse.builder().version(getLongSchemaVersion(version)).build());
                })
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java

        FutureUtil.waitForAll(lookupFutures)
        .thenRun(() -> {
            processLookUpResult(redirectAddresses, asyncResponse, future);
        }).exceptionally(e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Bookies.java`
#### Snippet
```java

        getPulsarResources().getBookieResources().get()
                .thenAccept(b -> {
                    asyncResponse.resume(b.orElseGet(() -> new BookiesRackConfiguration()));
                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java

            if (!replicatedClusters.isEmpty()) {
                replicatedClusters.forEach(cluster -> {
                    pulsar().getPulsarResources().getClusterResources().getClusterAsync(cluster)
                            .thenAccept(clusterDataOp -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
                replicatedClusters.forEach(cluster -> {
                    pulsar().getPulsarResources().getClusterResources().getClusterAsync(cluster)
                            .thenAccept(clusterDataOp -> {
                                ((TopicsImpl) pulsar().getBrokerService()
                                        .getClusterPulsarAdmin(cluster, clusterDataOp).topics())
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/BrokersBase.java`
#### Snippet
```java
                                }).thenCompose(reader -> producer.sendAsync(messageStr)
                                        .thenCompose(__ -> healthCheckRecursiveReadNext(reader, messageStr))
                                        .whenComplete((__, ex) -> {
                                            closeAndReCheck(producer, reader, topicOptional.get(), subscriptionName)
                                                    .whenComplete((unused, innerEx) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ClustersBase.java`
#### Snippet
```java
        validateSuperUserAccessAsync()
                .thenCompose(__ -> clusterResources().getClusterAsync(cluster))
                .thenAccept(clusterData -> {
                    asyncResponse.resume(clusterData
                            .orElseThrow(() -> new RestException(Status.NOT_FOUND, "Cluster does not exist")));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/metrics/PrometheusMetricsProvider.java`
#### Snippet
```java
    @VisibleForTesting
    void rotateLatencyCollection() {
        opStats.forEach((name, metric) -> {
            metric.rotateLatencyCollection();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java

    public PulsarService(ServiceConfiguration config) {
        this(config, Optional.empty(), (exitCode) -> {
                LOG.info("Process termination requested with code {}. "
                         + "Ignoring, as this constructor is intended for tests. ", exitCode);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
        preValidation(authoritative)
            .thenCompose(__ -> internalGetSubscriptionTypesEnabled(isGlobal))
            .thenAccept(op -> {
                asyncResponse.resume(op.isPresent() ? op.get()
                        : Response.noContent().build());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
        preValidation(authoritative)
            .thenCompose(__ -> internalGetMaxMessageSize(isGlobal))
            .thenAccept(policies -> {
                asyncResponse.resume(policies.isPresent() ? policies.get() : Response.noContent().build());
            })
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
        preValidation(authoritative)
                .thenCompose(__ -> getTopicPoliciesAsyncWithRetry(topicName))
                .thenAccept(op -> {
                    asyncResponse.resume(op.map(TopicPolicies::getReplicationClustersSet).orElseGet(() -> {
                        if (applied) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BacklogQuotaManager.java`
#### Snippet
```java
        });

        FutureUtil.waitForAll(futures).thenRun(() -> {
            log.info("All producers on topic [{}] are disconnected", persistentTopic.getName());
        }).exceptionally(exception -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java

        log.info("[{}] Starting replicator", replicatorId);
        producerBuilder.createAsync().thenAccept(producer -> {
            readEntries(producer);
        }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
                            final String path = getSchemaPath(schemaId);
                            store.delete(path, Optional.empty())
                                    .thenRun(() -> {
                                        future.complete(version);
                                    }).exceptionally(zkException -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    public void canReadMoreEntries(boolean withBackoff) {
        havePendingRead = false;
        topic.getBrokerService().executor().schedule(() -> {
            topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
                synchronized (PersistentStreamingDispatcherSingleActiveConsumer.this) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    @Override
    public void readEntryComplete(Entry entry, PendingReadEntryRequest ctx) {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(name, safeRun(() -> {
            internalReadEntryComplete(entry, ctx);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    @Override
    public void readEntriesFailed(ManagedLedgerException exception, Object ctx) {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
            internalReadEntriesFailed(exception, ctx);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    @Override
    public void redeliverUnacknowledgedMessages(Consumer consumer, long consumerEpoch) {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
            internalRedeliverUnacknowledgedMessages(consumer, consumerEpoch);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    @Override
    public void consumerFlow(Consumer consumer, int additionalNumberOfMessages) {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
            internalConsumerFlow(consumer);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    @Override
    public void readEntriesComplete(final List<Entry> entries, Object obj) {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topicName, SafeRun.safeRun(() -> {
            internalReadEntriesComplete(entries, obj);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/CompactorSubscription.java`
#### Snippet
```java
        // And we can only delete the previous ledger after the mark delete succeed, otherwise we will loss the
        // compacted data if mark delete failed.
        compactedTopic.newCompactedLedger(position, compactedLedgerId).thenAccept(previousContext -> {
            cursor.asyncMarkDelete(position, properties, new MarkDeleteCallback() {
                @Override
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            topicPolicies.setReplicationClusters(null);
                            return pulsar().getTopicPoliciesService().updateTopicPoliciesAsync(topicName, topicPolicies)
                                    .thenRun(() -> {
                                        log.info("[{}] Successfully set replication clusters for namespace={}, "
                                                        + "topic={}, clusters={}",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                    }
                })
                .thenAccept(__ -> {
                   internalAnalyzeSubscriptionBacklogForNonPartitionedTopic(asyncResponse, subName,
                           position, authoritative);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                }
                            }
                            asyncResponse.resume((StreamingOutput) output -> {
                                jsonMapper().writer().writeValue(output, partitionedManagedLedgerInfo);
                            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
            future = CompletableFuture.completedFuture(null);
        }
        future.thenAccept(__ -> {
            getPartitionedTopicMetadataAsync(topicName, authoritative, false)
                    .thenAccept(partitionMetadata -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        validateAdminAccessForTenantAsync(pulsar,
                                clientAppId, originalPrincipal, topicName.getTenant(), authenticationData)
                                .thenRun(() -> {
                                    authorizationFuture.complete(null);
                                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            topicPolicies.setReplicationClusters(clusterIds);
                            return pulsar().getTopicPoliciesService().updateTopicPoliciesAsync(topicName, topicPolicies)
                                    .thenRun(() -> {
                                        log.info("[{}] Successfully set replication clusters for namespace={}, "
                                                        + "topic={}, clusters={}",
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
        getPartitionedTopicMetadataAsync(topicName, false, false).thenAccept(metadata -> {
            if (metadata != null) {
                tryCreatePartitionsAsync(metadata.partitions).thenAccept(v -> {
                    asyncResponse.resume(Response.noContent().build());
                }).exceptionally(e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        @Override
                        public void getInfoComplete(ManagedLedgerInfo info, Object ctx) {
                            asyncResponse.resume((StreamingOutput) output -> {
                                jsonMapper().writer().writeValue(output, info);
                            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
    public void canReadMoreEntries(boolean withBackoff) {
        havePendingRead = false;
        topic.getBrokerService().executor().schedule(() -> {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(topic.getName(), SafeRun.safeRun(() -> {
                synchronized (PersistentStreamingDispatcherMultipleConsumers.this) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java

        if (closeIfConsumersConnected) {
            this.disconnect().thenRun(() -> {
                closeSubscriptionFuture.complete(null);
            }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java
            });
        } else {
            this.close().thenRun(() -> {
                closeSubscriptionFuture.complete(null);
            }).exceptionally(exception -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java

            if (previousDispatcher != null) {
                previousDispatcher.close().thenRun(() -> {
                    log.info("[{}][{}] Successfully closed previous dispatcher", topicName, subName);
                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
    public void readEntryFailed(ManagedLedgerException exception, Object ctx) {
        // Don't block caller thread, complete read entry fail with dispatcher dedicated thread.
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(dispatcher.getName(), SafeRun.safeRun(() -> {
            internalReadEntryFailed(exception, ctx);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
    public void readEntryComplete(Entry entry, Object ctx) {
        // Don't block caller thread, complete read entry with dispatcher dedicated thread.
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(dispatcher.getName(), SafeRun.safeRun(() -> {
            internalReadEntryComplete(entry, ctx);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
    @Override
    public void entriesAvailable() {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(dispatcher.getName(), SafeRun.safeRun(() -> {
            internalEntriesAvailable();
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    });

                    schemaVersionFuture.thenAccept(schemaVersion -> {
                        topic.checkIfTransactionBufferRecoverCompletely(isTxnEnabled).thenAccept(future -> {
                            CompletableFuture<Subscription> createInitSubFuture;
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                    commandSender.sendGetOrCreateSchemaErrorResponse(requestId, errorCode, message);
                    return null;
                }).thenAccept(schemaVersion -> {
                    commandSender.sendGetOrCreateSchemaResponse(requestId, schemaVersion);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                final long sequenceId = send.getSequenceId();
                final long highestSequenceId = send.getHighestSequenceId();
                service.getTopicOrderedExecutor().executeOrdered(producer.getTopic().getName(), SafeRun.safeRun(() -> {
                    commandSender.sendSendReceiptResponse(producerId, sequenceId, highestSequenceId, -1, -1);
                }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
                    log.debug("[{}] Consumer are left, reading more entries", name);
                }
                consumer.getPendingAcks().forEach((ledgerId, entryId, batchSize, stickyKeyHash) -> {
                    addMessageToReplay(ledgerId, entryId, stickyKeyHash);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            // remove positions first from replay list first : sendMessages recycles entries
            if (readType == ReadType.Replay) {
                entriesForThisConsumer.forEach(entry -> {
                    redeliveryMessages.remove(entry.getLedgerId(), entry.getEntryId());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    @Override
    public void consumerFlow(Consumer consumer, int additionalNumberOfMessages) {
        topic.getBrokerService().executor().execute(safeRun(() -> {
            internalConsumerFlow(consumer, additionalNumberOfMessages);
        }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TopicPoliciesSystemTopicClient.java`
#### Snippet
```java
        @Override
        public CompletableFuture<Void> closeAsync() {
            return producer.closeAsync().whenComplete((r, ex) -> {
                systemTopicClient.getWriters().remove(TopicPolicyWriter.this);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TopicPoliciesSystemTopicClient.java`
#### Snippet
```java
        @Override
        public CompletableFuture<Void> closeAsync() {
            return reader.closeAsync().whenComplete((r, ex) -> {
                systemTopic.getReaders().remove(TopicPolicyReader.this);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java

                    if (previousDispatcher != null) {
                        previousDispatcher.close().thenRun(() -> {
                            log.info("[{}][{}] Successfully closed previous dispatcher", topicName, subName);
                        }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                    .getRecentlyJoinedConsumers();
            if (recentlyJoinedConsumers != null && recentlyJoinedConsumers.size() > 0) {
                recentlyJoinedConsumers.forEach((k, v) -> {
                    subStats.consumersAfterMarkDeletePosition.put(k.consumerName(), v.toString());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java

        if (closeIfConsumersConnected) {
            this.disconnect().thenRun(() -> {
                closeSubscriptionFuture.complete(null);
            }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
            });
        } else {
            this.close().thenRun(() -> {
                closeSubscriptionFuture.complete(null);
            }).exceptionally(exception -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        }
        return cursor.setCursorProperties(newSubscriptionProperties)
                .thenRun(() -> {
                    this.subscriptionProperties = newSubscriptionProperties;
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                    synchronized (this) {
                        if (dispatcher != null) {
                            dispatcher.close().thenRun(() -> {
                                log.info("[{}][{}] Successfully closed dispatcher for reader", topicName, subName);
                            }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                    .thenCompose(__ -> authService.grantSubscriptionPermissionAsync(namespaceName, subscription,
                            roles, null))
                    .thenAccept(unused -> {
                        log.info("[{}] Successfully granted permission on subscription for role {}:{} - "
                                + "namespaceName {}", clientAppId(), roles, subscription, namespaceName);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                .thenCompose(__ -> getNamespacePoliciesAsync(namespaceName))
                .thenCompose(policies -> {
                    return validateNamespaceBundleOwnershipAsync(namespaceName, policies.bundles, bundleRange,
                            false, true)
                            .thenCompose(nsBundle ->
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                                Map<String, Long> topicHashPositions = new HashMap<>();
                                if (topics == null || topics.size() == 0) {
                                    allTopicsInThisBundle.forEach(t -> {
                                        topicHashPositions.put(t,
                                                pulsar().getNamespaceService().getNamespaceBundleFactory()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                                                    .filter(t -> TopicName.get(t).getPartitionedTopicName()
                                                            .equals(TopicName.get(topic).getPartitionedTopicName()))
                                                    .forEach(partition -> {
                                                        topicHashPositions.put(partition,
                                                                pulsar().getNamespaceService()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                .thenCompose(__ -> getBundleRangeAsync(bundleName))
                .thenCompose(bundleRange -> {
                    return getNamespacePoliciesAsync(namespaceName)
                            .thenCompose(policies ->
                                    validateNamespaceBundleOwnershipAsync(namespaceName, policies.bundles, bundleRange,
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

   protected void internalGetProperties(AsyncResponse asyncResponse) {
       getNamespacePoliciesAsync(namespaceName).thenAccept(policies -> {
           asyncResponse.resume(policies.properties);
       }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
    protected void internalSetReplicatorDispatchRate(AsyncResponse asyncResponse, DispatchRateImpl dispatchRate) {
        validateSuperUserAccessAsync()
                .thenAccept(__ -> {
                    log.info("[{}] Set namespace replicator dispatch-rate {}/{}",
                            clientAppId(), namespaceName, dispatchRate);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        return  old;
                    }).thenCompose(ignore -> {
                        return internalDeleteTopicsAsync(allUserCreatedTopics);
                    }).thenCompose(ignore -> {
                        return internalDeletePartitionedTopicsAsync(allUserCreatedPartitionTopics);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        return internalDeleteTopicsAsync(allUserCreatedTopics);
                    }).thenCompose(ignore -> {
                        return internalDeletePartitionedTopicsAsync(allUserCreatedPartitionTopics);
                    }).thenCompose(ignore -> {
                        return internalDeleteTopicsAsync(allSystemTopics);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        return internalDeletePartitionedTopicsAsync(allUserCreatedPartitionTopics);
                    }).thenCompose(ignore -> {
                        return internalDeleteTopicsAsync(allSystemTopics);
                    }).thenCompose(ignore__ -> {
                        return internalDeletePartitionedTopicsAsync(allPartitionedSystemTopics);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        return internalDeleteTopicsAsync(allSystemTopics);
                    }).thenCompose(ignore__ -> {
                        return internalDeletePartitionedTopicsAsync(allPartitionedSystemTopics);
                    });
                })
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

   protected void internalGetProperty(String key, AsyncResponse asyncResponse) {
        getNamespacePoliciesAsync(namespaceName).thenAccept(policies -> {
            asyncResponse.resume(policies.properties.get(key));
        }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
                producers.values().forEach(producer -> futures.add(producer.disconnect()));
                subscriptions.forEach((s, sub) -> futures.add(sub.disconnect()));
                FutureUtil.waitForAll(futures).thenRun(() -> {
                    closeClientFuture.complete(null);
                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
        if (url.isPresent()) {
            this.migrated = true;
            producers.forEach((__, producer) -> {
                producer.topicMigrated(url);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
                producer.topicMigrated(url);
            });
            subscriptions.forEach((__, sub) -> {
                sub.getConsumers().forEach((consumer) -> {
                    consumer.topicMigrated(url);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
            });
            subscriptions.forEach((__, sub) -> {
                sub.getConsumers().forEach((consumer) -> {
                    consumer.topicMigrated(url);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
        return FutureUtil.waitForAll(producerCheckFutures).thenCompose((__) -> {
            List<CompletableFuture<Void>> consumerCheckFutures = new ArrayList<>();
            subscriptions.forEach((subName, sub) -> sub.getConsumers().forEach(consumer -> {
                consumerCheckFutures.add(consumer.checkPermissionsAsync());
            }));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
        String name = NonPersistentReplicator.getReplicatorName(replicatorPrefix, remoteCluster);

        replicators.get(remoteCluster).disconnect().thenRun(() -> {
            log.info("[{}] Successfully removed replicator {}", name, remoteCluster);

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/SingleSnapshotAbortedTxnProcessorImpl.java`
#### Snippet
```java
            snapshot.setTopicName(topic.getName());
            return writer.deleteAsync(snapshot.getTopicName(), snapshot);
        }).thenRun(() -> {
            log.info("[{}] Successes to delete the aborted transaction snapshot", this.topic);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
            final Multimap<String, String> bundlesToUnload = strategy.findBundlesForUnloading(loadData, conf);

            bundlesToUnload.asMap().forEach((broker, bundles) -> {
                bundles.forEach(bundle -> {
                    final String namespaceName = LoadManagerShared.getNamespaceNameFromBundleName(bundle);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
        if (t.getPath().startsWith(LoadManager.LOADBALANCE_BROKERS_ROOT)) {
            brokersData.listLocks(LoadManager.LOADBALANCE_BROKERS_ROOT)
                    .thenAccept(brokers -> {
                        reapDeadBrokerPreallocations(brokers);
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
        // register listeners for domain changes
        pulsar.getPulsarResources().getClusterResources().getFailureDomainResources()
                .registerListener(__ -> {
                    scheduler.execute(() -> refreshBrokerToFailureDomainMap());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
        public void close() {
            synchronized (entries) {
                entries.forEach((sequenceId, buffer) -> {
                    buffer.release();
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
            final Map<String, Integer> brokerToAntiAffinityNamespaceCount = new ConcurrentHashMap<>();
            final List<CompletableFuture<Void>> futures = new ArrayList<>();
            brokerToNamespaceToBundleRange.forEach((broker, nsToBundleRange) -> {
                nsToBundleRange.forEach((ns, bundleRange) -> {
                    if (bundleRange.isEmpty()) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                            }
                        });
                        FutureUtil.waitForAll(getLedgerMetadataFutures).thenRun(() -> {
                            schemaStoreLedgersFuture.complete(null);
                        }).exceptionally(e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        String name = PersistentReplicator.getReplicatorName(replicatorPrefix, remoteCluster);

        replicators.get(remoteCluster).disconnect().thenRun(() -> {

            ledger.asyncDeleteCursor(name, new DeleteCursorCallback() {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

        return FutureUtil.waitForAll(producerCheckFutures).thenCompose((__) -> {
            return updateSubscriptionsDispatcherRateLimiter().thenCompose((___) -> {
                replicators.forEach((name, replicator) -> replicator.updateRateLimiter());
                shadowReplicators.forEach((name, replicator) -> replicator.updateRateLimiter());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                    producers.values().forEach(producer -> futures.add(producer.disconnect()));
                }
                FutureUtil.waitForAll(futures).thenRun(() -> {
                    closeClientFuture.complete(null);
                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        final CompletableFuture<Void> future = new CompletableFuture<>();
        String name = ShadowReplicator.getShadowReplicatorName(replicatorPrefix, shadowTopic);
        shadowReplicators.get(shadowTopic).disconnect().thenRun(() -> {

            ledger.asyncDeleteCursor(name, new DeleteCursorCallback() {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                    } else {
                        // Now, no one owns the namespace yet. Hence, we will try to dynamically assign it
                        pulsar.getExecutor().execute(() -> {
                            searchForCandidateBroker(bundle, future, options);
                        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                                    }
                                    updateNamespaceBundles(nsname, splittedBundles.getLeft()).thenCompose(__ -> {
                                        return updateNamespaceBundlesForPolicies(nsname, splittedBundles.getLeft());
                                    }).thenRun(() -> {
                                        bundleFactory.invalidateBundleCache(bundle.getNamespaceObject());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                                    .containsKey(namespaceName.toString())) {
                                pulsar.getBrokerService().getMultiLayerTopicMap().get(namespaceName.toString())
                                        .forEach((__, bundle) -> {
                                            bundle.forEach((topicName, topic) -> {
                                                if (topic instanceof NonPersistentTopic
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/Compactor.java`
#### Snippet
```java
        mxBean.addCompactionStartOp(reader.getTopic());
        doCompaction(reader, bk).whenComplete(
                (ledgerId, exception) -> {
                    reader.closeAsync().whenComplete((v, exception2) -> {
                        if (exception2 != null) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
    public CompletableFuture<NamespaceBundle> getBundleWithHighestTopicsAsync(NamespaceName nsname) {
        return pulsar.getPulsarResources().getTopicResources().listPersistentTopicsAsync(nsname).thenCompose(topics -> {
            return bundlesCache.get(nsname).handle((bundles, e) -> {
                Map<String, Integer> countMap = new HashMap<>();
                NamespaceBundle resultBundle = null;
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        return pulsar.getNamespaceService().checkTopicExists(topicName)
                .thenCompose(topicExists -> {
                    return fetchPartitionedTopicMetadataAsync(topicName)
                            .thenCompose(metadata -> {
                                CompletableFuture<PartitionedTopicMetadata> future = new CompletableFuture<>();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                    final ClusterReplicationMetrics clusterReplicationMetrics = pulsarStats
                            .getClusterReplicationMetrics();
                    replicationClients.forEach((cluster, client) -> {
                        clusterReplicationMetrics.remove(clusterReplicationMetrics.getKeyName(namespaceName,
                                cluster));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        Map<String, Map<InetSocketAddress, ChannelInitializer<SocketChannel>>> protocolHandlers) {

        protocolHandlers.forEach((protocol, initializers) -> {
            initializers.forEach((address, initializer) -> {
                try {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            if (t instanceof PersistentTopic) {
                Optional.ofNullable(((PersistentTopic) t).getManagedLedger()).ifPresent(
                        managedLedger -> {
                            managedLedger.trimConsumedLedgersInBackground(Futures.NULL_PROMISE);
                        }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            if (isPersistentTopic) {
                return topics.computeIfAbsent(topicName.toString(), (k) -> {
                    return this.loadOrCreatePersistentTopic(k, createIfMissing, properties);
                });
            } else {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        ConcurrentOpenHashMap<String, Topic> topicMap = getMultiLayerTopicMap()
                .computeIfAbsent(bundle.getNamespaceObject().toString(), k -> {
                    return ConcurrentOpenHashMap
                            .<String, ConcurrentOpenHashMap<String, Topic>>newBuilder().build();
                }).computeIfAbsent(bundle.toString(), k -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                            .<String, ConcurrentOpenHashMap<String, Topic>>newBuilder().build();
                }).computeIfAbsent(bundle.toString(), k -> {
                    return ConcurrentOpenHashMap.<String, Topic>newBuilder().build();
                });

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

        Map<String, TopicStatsImpl> topicStatsMap = new HashMap<>();
        topicMap.forEach((name, topic) -> {
            topicStatsMap.put(name,
                    topic.getStats(false, false, false));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        Map<String, String> configMap = new HashMap<>();
        ConcurrentOpenHashMap<String, Object> runtimeConfigurationMap = getRuntimeConfigurationMap();
        runtimeConfigurationMap.forEach((key, value) -> {
            configMap.put(key, String.valueOf(value));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                                                        + " topic", topic, FutureUtil.getException(topicFuture));
                                                persistentTopic.stopReplProducers()
                                                        .whenCompleteAsync((v, exception) -> {
                                                            topics.remove(topic, topicFuture);
                                                        }, executor());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        TopicName topicName = TopicName.get(topic);
        return pulsar.getNamespaceService().getBundleAsync(topicName)
                .thenAccept(namespaceBundle -> {
                    removeTopicFromCache(topic, namespaceBundle, createTopicFuture);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        if (!data.replication_clusters.contains(localCluster)) {
            pulsar().getNamespaceService().getNamespaceBundleFactory()
                    .getBundlesAsync(namespace).thenAccept(bundles -> {
                bundles.getBundles().forEach(bundle -> {
                    pulsar.getNamespaceService().isNamespaceBundleOwned(bundle).thenAccept(isExist -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            pulsar().getNamespaceService().getNamespaceBundleFactory()
                    .getBundlesAsync(namespace).thenAccept(bundles -> {
                bundles.getBundles().forEach(bundle -> {
                    pulsar.getNamespaceService().isNamespaceBundleOwned(bundle).thenAccept(isExist -> {
                        if (isExist) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    public int getNumberOfNamespaceBundles() {
        this.numberOfNamespaceBundles = 0;
        this.multiLayerTopicsMap.forEach((namespaceName, bundles) -> {
            this.numberOfNamespaceBundles += bundles.size();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        lock.readLock().lock();
        try {
            forEachTopic(topic -> {
                topic.getSubscriptions().forEach((subName, persistentSubscription) -> {
                    if (persistentSubscription.getDispatcher() instanceof PersistentDispatcherMultipleConsumers) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        }
        CompletableFuture<Void> isOwner = checkTopicNsOwnership(topic);
        isOwner.thenRun(() -> {
            nonPersistentTopic.initialize()
                    .thenAccept(__ -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                (maxConcurrentTopicLoadRequest) -> topicLoadRequestSemaphore.set(
                        new Semaphore((int) maxConcurrentTopicLoadRequest, false)));
        registerConfigurationListener("loadManagerClassName", className -> {
            pulsar.getExecutor().execute(() -> {
                try {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

        //  add listener to notify broker managedLedgerCacheSizeMB dynamic config
        registerConfigurationListener("managedLedgerCacheSizeMB", (managedLedgerCacheSizeMB) -> {
            managedLedgerFactory.getEntryCacheManager()
                    .updateCacheSizeAndThreshold(((int) managedLedgerCacheSizeMB) * 1024L * 1024L);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        //  add listener to notify broker managedLedgerCacheEvictionWatermark dynamic config
        registerConfigurationListener(
                "managedLedgerCacheEvictionWatermark", (cacheEvictionWatermark) -> {
            managedLedgerFactory.getEntryCacheManager()
                    .updateCacheEvictionWatermark((double) cacheEvictionWatermark);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        //  add listener to notify broker managedLedgerCacheEvictionTimeThresholdMillis dynamic config
        registerConfigurationListener(
                "managedLedgerCacheEvictionTimeThresholdMillis", (cacheEvictionTimeThresholdMills) -> {
            managedLedgerFactory.updateCacheEvictionTimeThreshold(TimeUnit.MILLISECONDS
                    .toNanos((long) cacheEvictionTimeThresholdMills));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

        // add listener to update message-dispatch-rate in msg for topic
        registerConfigurationListener("dispatchThrottlingRatePerTopicInMsg", (dispatchRatePerTopicInMsg) -> {
            updateTopicMessageDispatchRate();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        });
        // add listener to update message-dispatch-rate in byte for topic
        registerConfigurationListener("dispatchThrottlingRatePerTopicInByte", (dispatchRatePerTopicInByte) -> {
            updateTopicMessageDispatchRate();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        });
        // add listener to update managed-ledger config to skipNonRecoverableLedgers
        registerConfigurationListener("autoSkipNonRecoverableData", (skipNonRecoverableLedger) -> {
            updateManagedLedgerConfig();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        });
        // add listener to update message-dispatch-rate in msg for subscription
        registerConfigurationListener("dispatchThrottlingRatePerSubscriptionInMsg", (dispatchRatePerTopicInMsg) -> {
            updateSubscriptionMessageDispatchRate();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        });
        // add listener to update message-dispatch-rate in byte for subscription
        registerConfigurationListener("dispatchThrottlingRatePerSubscriptionInByte", (dispatchRatePerTopicInByte) -> {
            updateSubscriptionMessageDispatchRate();
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        // add listener to update message-dispatch-rate in msg for replicator
        registerConfigurationListener("dispatchThrottlingRatePerReplicatorInMsg",
                (dispatchRatePerTopicInMsg) -> {
                    updateReplicatorMessageDispatchRate();
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        // add listener to update message-dispatch-rate in byte for replicator
        registerConfigurationListener("dispatchThrottlingRatePerReplicatorInByte",
                (dispatchRatePerTopicInByte) -> {
                    updateReplicatorMessageDispatchRate();
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        // add listener to notify broker publish-rate monitoring
        registerConfigurationListener("brokerPublisherThrottlingTickTimeMillis",
                (publisherThrottlingTickTimeMillis) -> {
                    setupBrokerPublishRateLimiterMonitor();
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        if (!preciseTopicPublishRateLimitingEnable) {
            registerConfigurationListener("topicPublisherThrottlingTickTimeMillis",
                    (publisherThrottlingTickTimeMillis) -> {
                        setupTopicPublishRateLimiterMonitor();
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

        // add listener to notify partitioned topic defaultNumPartitions changed
        registerConfigurationListener("defaultNumPartitions", defaultNumPartitions -> {
            this.updateDefaultNumPartitions((int) defaultNumPartitions);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

        // add listener to notify partitioned topic maxNumPartitionsPerPartitionedTopic changed
        registerConfigurationListener("maxNumPartitionsPerPartitionedTopic", maxNumPartitions -> {
            this.updateMaxNumPartitionsPerPartitionedTopic((int) maxNumPartitions);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java

        // add listener to notify web service httpRequestsFailOnUnknownPropertiesEnabled changed.
        registerConfigurationListener("httpRequestsFailOnUnknownPropertiesEnabled", enabled -> {
            pulsar.getWebService().updateHttpRequestsFailOnUnknownPropertiesEnabled((boolean) enabled);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            if (!isEmpty(topics)) {
                // add topic under new split bundles which already updated into NamespaceBundleFactory.bundleCache
                topics.stream().forEach(t -> {
                    addTopicToStatsMaps(TopicName.get(t.getName()), t);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageKeyBasedContainer.java`
#### Snippet
```java
            // Therefore, we should update the `sequence_id` field to the highest sequence id and remove the
            // `highest_sequence_id` field to allow the weak order.
            batches.values().forEach(batchMessageContainer -> {
                batchMessageContainer.setLowestSequenceId(batchMessageContainer.getHighestSequenceId());
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TransactionMetaStoreHandler.java`
#### Snippet
```java
                ByteBuf request = Commands.newTcClientConnectRequest(transactionCoordinatorId, requestId);

                cnx.sendRequestWithId(request, requestId).thenRun(() -> {
                    internalPinnedExecutor.execute(() -> {
                        LOG.info("Transaction coordinator client connect success! tcId : {}", transactionCoordinatorId);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TransactionMetaStoreHandler.java`
#### Snippet
```java
                    }
                    pendingRequests.put(requestId, op);
                    timer.newTimeout(timeout -> {
                                internalPinnedExecutor.execute(() -> {
                                    if (!pendingRequests.containsKey(requestId)) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TransactionMetaStoreHandler.java`
#### Snippet
```java
                    }
                    pendingRequests.put(requestId, op);
                    timer.newTimeout(timeout -> {
                                internalPinnedExecutor.execute(() -> {
                                    if (!pendingRequests.containsKey(requestId)) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TransactionMetaStoreHandler.java`
#### Snippet
```java
                    }
                    pendingRequests.put(requestId, op);
                    timer.newTimeout(timeout -> {
                                internalPinnedExecutor.execute(() -> {
                                    if (!pendingRequests.containsKey(requestId)) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TransactionMetaStoreHandler.java`
#### Snippet
```java
                    }
                    pendingRequests.put(requestId, op);
                    timer.newTimeout(timeout -> {
                                internalPinnedExecutor.execute(() -> {
                                    if (!pendingRequests.containsKey(requestId)) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
                                    callMessageListener(finalMsg));
                        } else {
                            getExternalExecutor(msg).execute(() -> {
                                callMessageListener(finalMsg);
                            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                op = OpSendMsg.create(msg, null, sequenceId, callback);
                final MessageMetadata finalMsgMetadata = msgMetadata;
                op.rePopulate = () -> {
                    op.cmd = sendMessage(producerId, sequenceId, numMessages, msg.getMessageId(), finalMsgMetadata,
                            encryptedPayload);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionImpl.java`
#### Snippet
```java
            CompletableFuture<Void> abortFuture = new CompletableFuture<>();
            this.state = State.ABORTING;
            opFuture.whenComplete((v, e) -> {
                tcClient.abortAsync(txnId).whenComplete((vx, ex) -> {

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/client/impl/schema/SchemaUtils.java`
#### Snippet
```java
            sortedProperties.putAll(properties);
            JsonObject object = new JsonObject();
            sortedProperties.forEach((key, value) -> {
                object.add(key, (value != null) ? new JsonPrimitive(value) : null);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/GracefulExecutorServicesTerminationHandler.java`
#### Snippet
```java
                Thread shutdownWaitingThread = new Thread(this::awaitShutdown, getClass().getSimpleName());
                shutdownWaitingThread.setDaemon(false);
                shutdownWaitingThread.setUncaughtExceptionHandler((thread, exception) -> {
                  log.error("Uncaught exception in shutdown thread {}", thread, exception);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
        String[] tokens = trim(val).split(",");
        return Arrays.stream(tokens).map(t -> {
            return convert(trim(t), type);
        }).collect(Collectors.toCollection(LinkedHashSet::new));
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
        String[] tokens = trim(val).split(",");
        return Arrays.stream(tokens).map(t -> {
            return convert(trim(t), type);
        }).collect(Collectors.toList());
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentSortedLongPairSet.java`
#### Snippet
```java
        sb.append('{');
        final AtomicBoolean first = new AtomicBoolean(true);
        longPairSets.forEach((key, longPairSet) -> {
            longPairSet.forEach((item1, item2) -> {
                if (!first.getAndSet(false)) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                                        .value(retryMessage.getData())
                                        .properties(propertiesMap);
                        typedMessageBuilderNew.sendAsync().thenAccept(msgId -> {
                            doAcknowledge(finalMessageId, ackType, Collections.emptyMap(), null).thenAccept(v -> {
                                result.complete(null);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                                        .properties(propertiesMap);
                        typedMessageBuilderNew.sendAsync().thenAccept(msgId -> {
                            doAcknowledge(finalMessageId, ackType, Collections.emptyMap(), null).thenAccept(v -> {
                                result.complete(null);
                            }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

    private void completehasMessageAvailableWithValue(CompletableFuture<Boolean> future, boolean value) {
        internalPinnedExecutor.execute(() -> {
            future.complete(value);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                    .map(messageId -> (MessageIdImpl) messageId)
                    .collect(Collectors.toSet()), MAX_REDELIVER_UNACKNOWLEDGED);
            batches.forEach(ids -> {
                getRedeliveryMessageIdData(ids).thenAccept(messageIdData -> {
                    if (!messageIdData.isEmpty()) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Markers.java`
#### Snippet
```java
                .setSubscriptionName(subscriptionName);

        clusterIds.forEach((cluster, msgId) -> {
            update.addCluster()
                    .setCluster(cluster)
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Markers.java`
#### Snippet
```java
                .setEntryId(entryId);

        clusterIds.forEach((cluster, msgId) -> {
            snapshot.addCluster()
                    .setCluster(cluster)
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
        msgRateIn = 0.0;
        if (this.bundleStats != null) {
            this.bundleStats.forEach((bundle, stats) -> {
                msgRateIn += stats.msgRateIn;
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
        msgRateOut = 0.0;
        if (this.bundleStats != null) {
            this.bundleStats.forEach((bundle, stats) -> {
                msgRateOut += stats.msgRateOut;
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LoadReport.java`
#### Snippet
```java
        numTopics = 0;
        if (this.bundleStats != null) {
            this.bundleStats.forEach((bundle, stats) -> {
                numTopics += stats.topics;
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ReadOnlyManagedLedgerImpl.java`
#### Snippet
```java
                    bookKeeper.newOpenLedgerOp().withRecovery(false).withLedgerId(lastLedgerId)
                            .withDigestType(config.getDigestType()).withPassword(config.getPassword()).execute()
                            .thenAccept(readHandle -> {
                                readHandle.readLastAddConfirmedAsync().thenAccept(lastAddConfirmed -> {
                                    LedgerInfo info = LedgerInfo.newBuilder().setLedgerId(lastLedgerId)
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/MetaStoreImpl.java`
#### Snippet
```java
                                        ManagedLedgerInfo.Builder ledgerBuilder = ManagedLedgerInfo.newBuilder();
                                        if (properties != null) {
                                            properties.forEach((k, v) -> {
                                                ledgerBuilder.addProperties(
                                                        MLDataFormats.KeyValue.newBuilder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
        }

        cursorLedgerDeleteFuture.thenRun(() -> {
            store.asyncRemoveCursor(managedLedgerName, cursorName, new MetaStoreCallback<Void>() {
                @Override
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
                        .map(e -> deleteCursor(bkc, managedLedgerName, e.getKey(), e.getValue()))
                        .collect(Collectors.toList());
                Futures.waitForAll(futures).thenRun(() -> {
                    deleteManagedLedgerData(bkc, managedLedgerName, info, mlConfigFuture, callback, ctx);
                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                }

                internalPinnedExecutor.execute(() -> {
                    receiveMessageFromConsumer(consumer, true);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                    return false;
                }
            }).collect(Collectors.toList()).forEach(consumer2 -> {
                consumer2.closeAsync().whenComplete((r, ex) -> {
                    consumer2.subscribeFuture().completeExceptionally(error);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
                    .writeInt(ledgerMetadataByte.length)
                    .writeBytes(ledgerMetadataByte);
            ledgerIndexEntries.values().forEach(idxEntry -> {
                out.writeLong(idxEntry.getEntryId())
                        .writeInt(idxEntry.getPartId())
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
            final TreeMap<Long, OffloadIndexEntryImpl> inLedger = block.indexEntries
                    .getOrDefault(ledgerId, new TreeMap<>());
            list.forEach(indexEntry -> {
                inLedger.put(indexEntry.getEntryId(), indexEntry);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
        System.getProperties().entrySet().stream()
            .filter(p -> p.getKey().toString().startsWith("jclouds"))
            .forEach(jcloudsProp -> {
                overrides.setProperty(jcloudsProp.getKey().toString(), jcloudsProp.getValue().toString());
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
        System.getenv().entrySet().stream()
            .filter(p -> p.getKey().toString().startsWith("jclouds"))
            .forEach(jcloudsProp -> {
                overrides.setProperty(jcloudsProp.getKey().toString(), jcloudsProp.getValue().toString());
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
                CreateMode createMode = getCreateMode(opPut.getOptions());
                asyncCreateFullPathOptimistic(zkc, opPut.getPath(), opPut.getData(), createMode,
                        (rc, path1, ctx, name) -> {
                            execute(() -> {
                                Code code = Code.get(rc);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
                        });
            } else {
                zkc.setData(opPut.getPath(), opPut.getData(), expectedVersion, (rc, path1, ctx, stat) -> {
                    execute(() -> {
                        Code code = Code.get(rc);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
                        // There is the chance that we caused a connection reset by sending or requesting a batch
                        // that passed the max ZK limit. Retry with the individual operations
                        executor.schedule(() -> {
                            ops.forEach(o -> batchOperation(Collections.singletonList(o)));
                        }, 100, TimeUnit.MILLISECONDS);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java

        try {
            zkc.exists(path, null, (rc, path1, ctx, stat) -> {
                execute(() -> {
                    Code code = Code.get(rc);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java

        try {
            zkc.delete(op.getPath(), expectedVersion, (rc, path1, ctx) -> {
                execute(() -> {
                    Code code = Code.get(rc);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdSessionWatcher.java`
#### Snippet
```java
            CompletableFuture<SessionEvent> future = new CompletableFuture<>();
            client.getKVClient().get(ByteSequence.from("/".getBytes(StandardCharsets.UTF_8)))
                    .thenRun(() -> {
                        future.complete(SessionEvent.Reconnected);
                    }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
        }
        if (!properties.isEmpty()) {
            properties.forEach((k, v) -> {
                ack.addProperty().setKey(k).setValue(v);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
            });

            txn.commit().thenAccept(txnResponse -> {
                handleBatchOperationResult(txnResponse, ops);
            }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
                } else {
                    log.warn("Failed to commit: {}", cause.getMessage());
                    executor.execute(() -> {
                        ops.forEach(o -> o.getFuture().completeExceptionally(ex));
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
            // Re-create the lease before notifying that we are reconnected
            createLease(true)
                    .thenRun(() -> {
                        super.receivedSessionEvent(event);
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
        store.put(getLedgerPath(ledgerId),
                        data, Optional.of(zv.getLongVersion()))
                .thenAccept(stat -> {
                    promise.complete(new Versioned<>(metadata, new LongVersion(stat.getVersion())));
                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
        isConnected = event.isConnected();
        try {
            executor.execute(() -> {
                sessionListeners.forEach(l -> {
                    try {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
            log.debug("not enough data, delay schedule for part: {} length: {}", partId, dataObjectLength);
            scheduler.chooseThread(segmentInfo)
                    .schedule(() -> {
                        streamingOffloadLoop(partId, dataObjectLength);
                    }, 100, TimeUnit.MILLISECONDS);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationClient.java`
#### Snippet
```java
        if (n.getType() == NotificationType.Created || n.getType() == NotificationType.Deleted) {
            if (n.getPath().startsWith(bookieReadonlyRegistrationPath)) {
                getReadOnlyBookies().thenAccept(bookies -> {
                    readOnlyBookiesWatchers.keySet()
                            .forEach(w -> executor.execute(() -> w.onBookiesChanged(bookies)));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java

            store.put(path, newUrLedgerData, Optional.of(optRes.get().getStat().getVersion()))
                    .thenRun(() -> {
                        FutureUtils.complete(finalFuture, null);
                    }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        final byte[] urLedgerData = builder.build().toString().getBytes(UTF_8);
        store.put(path, urLedgerData, Optional.of(-1L))
                .thenRun(() -> {
                    FutureUtils.complete(finalFuture, null);
                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
                    }

                    return store.put(path, newValue, Optional.of(expectedVersion)).thenAccept(__ -> {
                        refresh(path);
                    }).thenApply(__ -> newValueObj);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
                    }

                    return store.put(path, newValue, Optional.of(expectedVersion)).thenAccept(__ -> {
                        refresh(path);
                    }).thenApply(__ -> newValueObj);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
            }
            CompletableFuture<Void> newFuture = new CompletableFuture<>();
            revalidateFuture.whenComplete((unused, throwable) -> {
                doRevalidate(newValue).thenRun(() -> newFuture.complete(null))
                        .exceptionally(throwable1 -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java
        }
        if (sinkConfig.getTopicToSerdeClassName() != null) {
            sinkConfig.getTopicToSerdeClassName().forEach((topicName, serde) -> {
                sourceSpecBuilder.putInputSpecs(topicName,
                        Function.ConsumerSpec.newBuilder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java
        }
        if (sinkConfig.getTopicToSchemaType() != null) {
            sinkConfig.getTopicToSchemaType().forEach((topicName, schemaType) -> {
                sourceSpecBuilder.putInputSpecs(topicName,
                        Function.ConsumerSpec.newBuilder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java

        if (newConfig.getInputs() != null) {
            newConfig.getInputs().forEach((topicName -> {
                newConfig.getInputSpecs().put(topicName,
                        ConsumerConfig.builder().isRegexPattern(false).build());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java
        }
        if (newConfig.getTopicToSerdeClassName() != null) {
            newConfig.getTopicToSerdeClassName().forEach((topicName, serdeClassName) -> {
                newConfig.getInputSpecs().put(topicName,
                        ConsumerConfig.builder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java
        }
        if (newConfig.getTopicToSchemaType() != null) {
            newConfig.getTopicToSchemaType().forEach((topicName, schemaClassname) -> {
                newConfig.getInputSpecs().put(topicName,
                        ConsumerConfig.builder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
                clusterServiceCoordinator.addTask("drain-worker-list-probe-periodic-check",
                        workerConfig.getWorkerListProbeIntervalSec() * 1000L,
                        () -> {
                                schedulerManager.updateWorkerDrainMap();
                        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        // implements SerDe class
        if (functionConfig.getCustomSerdeInputs() != null) {
            functionConfig.getCustomSerdeInputs().forEach((topicName, inputSerializer) -> {
                ValidatorUtils.validateSerde(inputSerializer, typeArgs[0], clsLoader, true);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        }
        if (functionConfig.getCustomSerdeInputs() != null) {
            functionConfig.getCustomSerdeInputs().forEach((topicName, serdeClassName) -> {
                sourceSpecBuilder.putInputSpecs(topicName,
                        Function.ConsumerSpec.newBuilder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java

        if (newConfig.getInputs() != null) {
            newConfig.getInputs().forEach((topicName -> {
                newConfig.getInputSpecs().put(topicName,
                        ConsumerConfig.builder().isRegexPattern(false).build());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        }
        if (newConfig.getCustomSerdeInputs() != null) {
            newConfig.getCustomSerdeInputs().forEach((topicName, serdeClassName) -> {
                newConfig.getInputSpecs().put(topicName,
                        ConsumerConfig.builder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        }
        if (newConfig.getCustomSchemaInputs() != null) {
            newConfig.getCustomSchemaInputs().forEach((topicName, schemaClassname) -> {
                newConfig.getInputSpecs().put(topicName,
                        ConsumerConfig.builder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/MultiConsumerPulsarSource.java`
#### Snippet
```java

        // Check new config with schema types or classnames
        pulsarSourceConfig.getTopicSchema().forEach((topic, conf) -> {
            configs.put(topic, buildPulsarSourceConsumerConfig(topic, conf, typeArg));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
            });

            sourceSpec.getTopicsToSerDeClassNameMap().forEach((topic, serde) -> {
                topicSchema.put(topic,
                        ConsumerConfig.builder()
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
                functionStatusBuilder.setNumSuccessfullyProcessed((long) stats.getTotalProcessedSuccessfully());
                functionStatusBuilder.setNumUserExceptions((long) stats.getTotalUserExceptions());
                stats.getLatestUserExceptions().forEach(ex -> {
                    functionStatusBuilder.addLatestUserExceptions(ex);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
                });
                functionStatusBuilder.setNumSystemExceptions((long) stats.getTotalSysExceptions());
                stats.getLatestSystemExceptions().forEach(ex -> {
                    functionStatusBuilder.addLatestSystemExceptions(ex);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
                    functionStatusBuilder.addLatestSystemExceptions(ex);
                });
                stats.getLatestSourceExceptions().forEach(ex -> {
                    functionStatusBuilder.addLatestSourceExceptions(ex);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
                    functionStatusBuilder.addLatestSourceExceptions(ex);
                });
                stats.getLatestSinkExceptions().forEach(ex -> {
                    functionStatusBuilder.addLatestSinkExceptions(ex);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                if (!ledgers.isEmpty()) {
                    final long id = ledgers.lastKey();
                    OpenCallback opencb = (rc, lh, ctx1) -> {
                        executor.executeOrdered(name, safeRun(() -> {
                            mbean.endDataLedgerOpenOp();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                .thenCompose(readHandle -> config.getLedgerOffloader().offload(readHandle, uuid, extraMetadata))
                .thenCompose((ignore) -> {
                        return Retries.run(Backoff.exponentialJittered(TimeUnit.SECONDS.toMillis(1),
                                                                       TimeUnit.SECONDS.toHours(1)).limit(10),
                                           FAIL_ON_CONFLICT,
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    static final Predicate<Throwable> FAIL_ON_CONFLICT = (throwable) -> {
        return !(throwable instanceof OffloadConflict) && Retries.NonFatalPredicate.test(throwable);
    };

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            } else {
                iterator.remove();
                bookKeeper.asyncDeleteLedger(li.getLedgerId(), (rc, ctx) -> {
                    log.info("[{}] Deleted empty ledger ledgerId={} rc={}", name, li.getLedgerId(), rc);
                }, null);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        }
        CompletableFuture<Void> future = new CompletableFuture();
        FutureUtil.waitForAll(futures).thenAccept(p -> {
            internalTrimLedgers(true, future);
        }).exceptionally(e -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            }

            ledgerCache.forEach((ledgerId, readHandle) -> {
                invalidateReadHandle(ledgerId);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                    ? Math.max(config.getAddEntryTimeoutSeconds(), config.getReadEntryTimeoutSeconds())
                    : timeoutSec;
            this.timeoutTask = this.scheduledExecutor.scheduleAtFixedRate(safeRun(() -> {
                checkTimeouts();
            }), timeoutSec, timeoutSec, TimeUnit.SECONDS);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/AbstractWebSocketHandler.java`
#### Snippet
```java

        this.queryParams = new TreeMap<>();
        request.getParameterMap().forEach((key, values) -> {
            queryParams.put(key, values[0]);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/admin/WebSocketProxyStatsBase.java`
#### Snippet
```java

        if (service().getConsumers().containsKey(topicNameStr)) {
            service().getConsumers().get(topicNameStr).forEach(handler -> {
                topicStat.consumerStats.add(new ConsumerStats(handler));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/admin/WebSocketProxyStatsBase.java`
#### Snippet
```java

        if (service().getReaders().containsKey(topicNameStr)) {
            service().getReaders().get(topicNameStr).forEach(handler -> {
                topicStat.consumerStats.add(new ConsumerStats(handler));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/TenantResources.java`
#### Snippet
```java

            } else {
                children.forEach(ns -> {
                    namespaces.add(NamespaceName.get(tenant, clusterOrNamespace, ns).toString());
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/TenantResources.java`
#### Snippet
```java
                            });
                        });
                FutureUtil.waitForAll(activeNamespaceListFuture).thenAccept(r -> {
                    activeNamespaceFuture.complete(null);
                }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/MetadataStoreCacheLoader.java`
#### Snippet
```java
            if (LOADBALANCE_BROKERS_ROOT.equals(n.getPath()) && NotificationType.ChildrenChanged.equals(n.getType())) {
                loadReportResources.getChildrenAsync(LOADBALANCE_BROKERS_ROOT).thenApplyAsync((brokerNodes)->{
                    updateBrokerList(brokerNodes).thenRun(() -> {
                        log.info("Successfully updated broker info {}", brokerNodes);
                    }).exceptionally(ex -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
                            .setKey(keyName)
                            .setValue(keyInfo.getKey());
                    keyInfo.getMetadata().forEach((key, value) -> {
                        encKey.addMetadata()
                                .setKey(key)
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java

        Map<String, String> keyMeta = new HashMap<String, String>();
        encKeyMeta.forEach(kv -> {
            keyMeta.put(kv.getKey(), kv.getValue());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
            previousResHeaders);

        headers.forEach(entry -> {
            builder.header(entry.getKey(), entry.getValue());
        });
```

## RuleId[ruleID=UseOfPropertiesAsHashtable]
### UseOfPropertiesAsHashtable
Call to `Hashtable.putAll()` on properties object
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaAbstractSink.java`
#### Snippet
```java
        }
        if (kafkaSinkConfig.getProducerConfigProperties() != null) {
            props.putAll(kafkaSinkConfig.getProducerConfigProperties());
        }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.putAll()` on properties object
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaAbstractSource.java`
#### Snippet
```java
        Properties props = new Properties();
        if (kafkaSourceConfig.getConsumerConfigProperties() != null) {
            props.putAll(kafkaSourceConfig.getConsumerConfigProperties());
        }
        props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, kafkaSourceConfig.getBootstrapServers());
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
                    properties.getProperty(DATA_READ_PRIORITY_NAME_IN_CONF_FILE));
        } else {
            object = properties.get(field.getName());
        }
        return value((String) object, field);
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
                try {
                    f.setAccessible(true);
                    f.set(data, value((String) properties.get(f.getName()), f));
                } catch (Exception e) {
                    throw new IllegalArgumentException(
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
                    throw new IllegalArgumentException(
                            String.format("failed to initialize %s field while setting value %s",
                                    f.getName(), properties.get(f.getName())), e);
                }
            }
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketService.java`
#### Snippet
```java
        authenticationService = new AuthenticationService(this.config);
        // initialize crypto key reader
        String cryptoFactoryClassName = (String) config.getProperties().get("cryptoKeyReaderFactoryClassName");
        if (StringUtils.isNotBlank(cryptoFactoryClassName)) {
            try {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `pulsar-package-management/core/src/main/java/org/apache/pulsar/packages/management/core/impl/DefaultPackagesStorageConfiguration.java`
#### Snippet
```java
    @Override
    public String getProperty(String key) {
        return (String) properties.get(key);
    }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    public Object getProperty(String key) {
        return properties.get(key);
    }

```

## RuleId[ruleID=EmptyMethod]
### EmptyMethod
Method only calls its super
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
    // This is a stub method for Mockito
    @Override
    public void validateSuperUserAccess() {
        super.validateSuperUserAccess();
    }
```

### EmptyMethod
Method only calls its super
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/BrokerStats.java`
#### Snippet
```java
    // missing
    @ApiResponses(value = { @ApiResponse(code = 403, message = "Don't have admin permission") })
    public StreamingOutput getTopics2() throws Exception {
        return super.getTopics2();
    }
```

### EmptyMethod
All implementations of this method are empty
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/bucket/BucketSnapshotStorage.java`
#### Snippet
```java
     * Start the bucket snapshot storage service.
     */
    void start() throws Exception;

    /**
```

### EmptyMethod
All implementations of this method are empty
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LoadCalculator.java`
#### Snippet
```java
    ResourceDescription getResourceDescription(ServiceRequest srvRequest);

    void recalibrateResourceUsagePerServiceUnit(LoadReport loadReport);
}

```

### EmptyMethod
All implementations of this method are empty
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/ResourceDescription.java`
#### Snippet
```java
public abstract class ResourceDescription implements Comparable<ResourceDescription> {

    public abstract void removeUsage(ResourceDescription rd);

    public abstract void addUsage(ResourceDescription rd);
```

### EmptyMethod
All implementations of this method are empty
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/ResourceDescription.java`
#### Snippet
```java
    public abstract void removeUsage(ResourceDescription rd);

    public abstract void addUsage(ResourceDescription rd);

    public abstract int getUsagePct();
```

### EmptyMethod
Method only calls its super
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/AlwaysSchemaValidator.java`
#### Snippet
```java

    @Override
    public void validate(Schema toValidate, Iterable<Schema> existing) {
        return;
    }
```

### EmptyMethod
All implementations of this method are empty
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/PartitionedTopicStats.java`
#### Snippet
```java
    PartitionedTopicMetadata getMetadata();

    Map<String, ? extends TopicStats> getPartitions();

    TopicStats add(TopicStats ts);
```

### EmptyMethod
All implementations of this method are empty
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/ReaderInterceptor.java`
#### Snippet
```java
     * Close the interceptor.
     */
    void close();

    /**
```

### EmptyMethod
The method is empty
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctionWorker.java`
#### Snippet
```java
        }

        void processArguments() throws Exception {
        }

```

### EmptyMethod
Method only calls its super
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
         */
        @Override
        public int hashCode() {
            return super.hashCode();
        }
```

### EmptyMethod
All implementations of this method are empty
in `pulsar-functions/api-java/src/main/java/org/apache/pulsar/functions/api/StateStore.java`
#### Snippet
```java
     * @param ctx the state store context
     */
    void init(StateStoreContext ctx);

    @Override
```

### EmptyMethod
Method only calls its super
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSourceConfig.java`
#### Snippet
```java

    @Override
    public void validate() {
        super.validate();
    }
```

### EmptyMethod
Method only calls its super
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarMetadataBookieDriver.java`
#### Snippet
```java

    @Override
    public RegistrationManager createRegistrationManager() {
        return super.createRegistrationManager();
    }
```

### EmptyMethod
The method is empty
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarMetadataClientDriver.java`
#### Snippet
```java
    }

    public static void init() {
        // cause <cinit> to be invoked
    }
```

### EmptyMethod
Method only calls its super
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnBatchedPositionImpl.java`
#### Snippet
```java
     */
    @Override
    public int hashCode() {
        return super.hashCode();
    }
```

## RuleId[ruleID=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/docs/src/main/java/org/apache/pulsar/io/docs/ConnectorDocGenerator.java`
#### Snippet
```java
            required = true
        )
        String outputDir = null;

        @Parameter(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/docs/src/main/java/org/apache/pulsar/io/docs/ConnectorDocGenerator.java`
#### Snippet
```java
            },
            description = "Show this help message")
        boolean help = false;

    }
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java
                    + "during insertion, it is suggested to set this value to `true`."
    )
    private boolean excludeNonDeclaredFields = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java
            help = "Use the JDBC batch API. This option is suggested to improve write performance."
    )
    private boolean useJdbcBatch = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/CanalAbstractSource.java`
#### Snippet
```java
    protected Thread thread = null;

    protected volatile boolean running = false;

    private CanalConnector connector;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/CanalAbstractSource.java`
#### Snippet
```java
public abstract class CanalAbstractSource<V> extends PushSource<V> {

    protected Thread thread = null;

    protected volatile boolean running = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/SinkOfFlume.java`
#### Snippet
```java
    private long batchSize;

    private SinkCounter counter = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/AbstractSource.java`
#### Snippet
```java
    protected Thread thread = null;

    protected volatile boolean running = false;

    protected final Thread.UncaughtExceptionHandler handler =
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/source/AbstractSource.java`
#### Snippet
```java
            .getLogger(AbstractSource.class);

    protected Thread thread = null;

    protected volatile boolean running = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/seq/HdfsAbstractSequenceFileSink.java`
#### Snippet
```java
    protected AtomicLong counter;
    protected FSDataOutputStream hdfsStream;
    protected Writer writer = null;

    public abstract KeyValue<HdfsK, HdfsV> convert(KeyValue<K, V> kv);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/seq/HdfsAbstractSequenceFileSink.java`
#### Snippet
```java
    protected AtomicLong counter;
    protected FSDataOutputStream hdfsStream;
    protected Writer writer = null;

    public abstract KeyValue<HdfsK, HdfsV> convert(KeyValue<K, V> kv);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java
            "If true the Kafka message headers will be copied into Pulsar message properties. Since Pulsar properties "
                + "is a Map<String, String>, byte array values in the Kafka headers will be base64 encoded. ")
    private boolean copyHeadersEnabled = false;

    public static KafkaSourceConfig load(String yamlFile) throws IOException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
        defaultValue = "false",
        help = "Flag to enable a keepalive to Redis")
    private boolean keepAlive = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
        defaultValue = "0",
        help = "The Redis database to connect to")
    private int redisDatabase = 0;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
        defaultValue = "false",
        help = "Flag to enable TCP no delay should be used")
    private boolean tcpNoDelay = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaAbstractSource.java`
#### Snippet
```java

    private volatile Consumer<Object, Object> consumer;
    private volatile boolean running = false;
    private KafkaSourceConfig kafkaSourceConfig;
    private Thread runnerThread;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Backoff.java`
#### Snippet
```java

    private long firstBackoffTimeInMillis;
    private boolean mandatoryStopMade = false;

    private static final Random random = new Random();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSinkConfig.java`
#### Snippet
```java
        defaultValue = "false",
        help = "A flag to tell Pulsar IO to retain ordering when moving messages from Pulsar to Kinesis")
    private boolean retainOrdering = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSinkConfig.java`
#### Snippet
```java
            help = "When set to true and the message format is FULL_MESSAGE_IN_JSON_EXPAND_VALUE the output JSON will be flattened."
    )
    private boolean jsonFlatten = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java
        defaultValue = "false",
        help = "Flag to determine if gzip should be enabled")
    private boolean gzipEnable = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java
        defaultValue = "0",
        help = "Initially requested maximum channel number. 0 for unlimited")
    private int requestedChannelMax = 0;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java
        defaultValue = "0",
        help = "Initially requested maximum frame size, in octets. 0 for unlimited")
    private int requestedFrameMax = 0;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSink.java`
#### Snippet
```java

        private Record<GenericObject> resultContext;
        private long startTime = 0;
        private final Handle<ProducerSendCallback> recyclerHandle;
        private KinesisSink kinesisSink;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSourceConfig.java`
#### Snippet
```java
        defaultValue = "0",
        help = "Maximum number of messages that the server will deliver, 0 for unlimited")
    private int prefetchCount = 0;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSourceConfig.java`
#### Snippet
```java
        defaultValue = "false",
        help = "Set true if the settings should be applied to the entire channel rather than each consumer")
    private boolean prefetchGlobal = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSourceConfig.java`
#### Snippet
```java
            defaultValue = "false",
            help = "Set true if the queue should be declared passively - ie to preserve durability/timeout settings")
    private boolean passive = false;

    public static RabbitMQSourceConfig load(String yamlFile) throws IOException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSinkConfig.java`
#### Snippet
```java
            defaultValue = "false",
            help = "Flag to determine if gzip should be enabled")
    private boolean gzipEnable = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java
            help = "Enable SSL/TLS"
    )
    private boolean enabled = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
            help = "Enable elasticsearch request compression."
    )
    private boolean compressionEnabled = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
        help = "The number of replicas of the index"
    )
    private int indexNumberOfReplicas = 0;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
        help = "Sets whether the Sink has to take into account the Schema or if it should simply copy the raw message to Elastichsearch"
    )
    private boolean schemaEnable = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
                    + "not consider the properties order."
    )
    private boolean canonicalKeyFields = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
            help = "Create the index if it does not exist"
    )
    private boolean createIndexIfNeeded = false;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
            help = "The maximum number of in flight elasticsearch bulk requests. The default 0 allows the execution of a single request. A value of 1 means 1 concurrent request is allowed to be executed while accumulating new bulk requests."
    )
    private int bulkConcurrentRequests = 0;

    @FieldDoc(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
            help = "Enable the elasticsearch bulk processor to flush write requests based on the number or size of requests, or after a given period."
    )
    private boolean bulkEnabled = false;

    // bulk settings, see https://www.elastic.co/guide/en/elasticsearch/client/java-rest/master/java-rest-high-document-bulk.html#java-rest-high-document-bulk-processor
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
            help = "When the message key schema is AVRO or JSON, copy the message key fields into the Elasticsearch _source."
    )
    private boolean copyKeyFields = false;

    public enum MalformedDocAction {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/elastic/ElasticBulkProcessor.java`
#### Snippet
```java
    private final List<BulkOperationWithPulsarRecord> pendingOperations = new ArrayList<>();
    private final BulkRequestHandler bulkRequestHandler;
    private volatile boolean closed = false;
    private final ReentrantLock lock;
    private final ExecutorService internalExecutorService;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSource.java`
#### Snippet
```java
                    .expireAfterAccess(30, TimeUnit.MINUTES).build();

    private boolean jsonWithEnvelope = false;
    private static final String JSON_WITH_ENVELOPE_CONFIG = "json-with-envelope";

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaSchemaWrappedSchema.java`
#### Snippet
```java
public class KafkaSchemaWrappedSchema implements Schema<byte[]>, Serializable {

    private SchemaInfo schemaInfo = null;

    public KafkaSchemaWrappedSchema(org.apache.pulsar.kafka.shade.avro.Schema schema,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
    private final ObjectMapper objectMapper = new ObjectMapper();
    private ObjectMapper sortedObjectMapper;
    private List<String> primaryFields = null;
    private final Pattern nonPrintableCharactersPattern = Pattern.compile("[\\p{C}]");
    private final Base64.Encoder base64Encoder = Base64.getEncoder().withoutPadding();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaConnectSinkConfig.java`
#### Snippet
```java
                    + "If enabled, all non alpha-digital characters in topic name will be replaced with underscores. \n"
                    + "In some cases it may result in topic name collisions (topic_a and topic.a will become the same)")
    private boolean sanitizeTopicName = false;

    public static PulsarKafkaConnectSinkConfig load(String yamlFile) throws IOException {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarOffsetBackingStore.java`
#### Snippet
```java
    private Producer<byte[]> producer;
    private Reader<byte[]> reader;
    private volatile CompletableFuture<Void> outstandingReadToEnd = null;

    public PulsarOffsetBackingStore(PulsarClient client) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java

    // pulsar io related variables
    private Iterator<SourceRecord> currentBatch = null;
    private CompletableFuture<Void> flushFuture;
    private OffsetBackingStore offsetStore;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeperSession.java`
#### Snippet
```java
    private MockZooKeeper mockZooKeeper;

    private long sessionId = 0L;

    private static final Objenesis objenesis = new ObjenesisStd();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java
    @VisibleForTesting
    @Getter
    boolean fenced = false;

    public PulsarMockLedgerHandle(PulsarMockBookKeeper bk, long id,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
    protected String topicName;

    private boolean sanitizeTopicName = false;
    private final Cache<String, String> sanitizedTopicCache =
            CacheBuilder.newBuilder().maximumSize(1000)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
    protected final ConcurrentLinkedDeque<Record<GenericObject>> pendingFlushQueue = new ConcurrentLinkedDeque<>();
    private final AtomicBoolean isFlushRunning = new AtomicBoolean(false);
    private volatile boolean isRunning = false;

    private final Properties props = new Properties();
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long numMessagedDerserializedPerBatch = 0L;
    private long readAttemptsSuccessSum = 0L;
    private long readAttemptsFailSum = 0L;
    private long readLatencySuccessSum = 0L;
    private long readLatencyFailSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long messageQueueEnqueueWaitTimeSum = 0L;
    private long numMessagesDerserializedSum = 0L;
    private long numMessagedDerserializedPerBatch = 0L;
    private long readAttemptsSuccessSum = 0L;
    private long readAttemptsFailSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long bytesReadSum = 0L;
    private long entryDeserializeTimeStartTime;
    private long entryDeserializeTimeSum = 0L;
    private long messageQueueEnqueueWaitTimeStartTime;
    private long messageQueueEnqueueWaitTimeSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long messageQueueEnqueueWaitTimeStartTime;
    private long messageQueueEnqueueWaitTimeSum = 0L;
    private long numMessagesDerserializedSum = 0L;
    private long numMessagedDerserializedPerBatch = 0L;
    private long readAttemptsSuccessSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long messageQueueDequeueWaitTimeSum = 0L;
    private long recordDeserializeTimeStartTime;
    private long recordDeserializeTimeSum = 0L;

    public PulsarConnectorMetricsTracker(StatsProvider statsProvider) {
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long readLatencySuccessSum = 0L;
    private long readLatencyFailSum = 0L;
    private long numEntriesPerBatchSum = 0L;
    private long messageQueueDequeueWaitTimeSum = 0L;
    private long recordDeserializeTimeStartTime;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long readAttemptsSuccessSum = 0L;
    private long readAttemptsFailSum = 0L;
    private long readLatencySuccessSum = 0L;
    private long readLatencyFailSum = 0L;
    private long numEntriesPerBatchSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    // internal tracking variables
    private long entryQueueDequeueWaitTimeStartTime;
    private long entryQueueDequeueWaitTimeSum = 0L;
    private long bytesReadSum = 0L;
    private long entryDeserializeTimeStartTime;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long readLatencyFailSum = 0L;
    private long numEntriesPerBatchSum = 0L;
    private long messageQueueDequeueWaitTimeSum = 0L;
    private long recordDeserializeTimeStartTime;
    private long recordDeserializeTimeSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long readAttemptsFailSum = 0L;
    private long readLatencySuccessSum = 0L;
    private long readLatencyFailSum = 0L;
    private long numEntriesPerBatchSum = 0L;
    private long messageQueueDequeueWaitTimeSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long numMessagesDerserializedSum = 0L;
    private long numMessagedDerserializedPerBatch = 0L;
    private long readAttemptsSuccessSum = 0L;
    private long readAttemptsFailSum = 0L;
    private long readLatencySuccessSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long entryQueueDequeueWaitTimeStartTime;
    private long entryQueueDequeueWaitTimeSum = 0L;
    private long bytesReadSum = 0L;
    private long entryDeserializeTimeStartTime;
    private long entryDeserializeTimeSum = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorMetricsTracker.java`
#### Snippet
```java
    private long entryDeserializeTimeSum = 0L;
    private long messageQueueEnqueueWaitTimeStartTime;
    private long messageQueueEnqueueWaitTimeSum = 0L;
    private long numMessagesDerserializedSum = 0L;
    private long numMessagedDerserializedPerBatch = 0L;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java

    //--- Ledger metrics ---
    private boolean exposeTopicLevelMetricsInPrometheus = false;
    private boolean exposeManagedLedgerMetricsInPrometheus = false;
    private int managedLedgerStatsPeriodSeconds = 60;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java
public class PulsarConnectorConfig implements AutoCloseable {

    private boolean hasMetadataUrl = false;

    private String brokerServiceUrl = "http://localhost:8080";
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java

    // --- Bookkeeper
    private int bookkeeperThrottleValue = 0;
    private int bookkeeperNumIOThreads = 2 * Runtime.getRuntime().availableProcessors();
    private int bookkeeperNumWorkerThreads = Runtime.getRuntime().availableProcessors();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java

    // --- Ledger Offloading ---
    private String managedLedgerOffloadDriver = null;
    private int managedLedgerOffloadMaxThreads = 2;
    private String offloadersDirectory = "./offloaders";
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java
    private int bookkeeperNumWorkerThreads = Runtime.getRuntime().availableProcessors();
    private boolean bookkeeperUseV2Protocol = true;
    private int bookkeeperExplicitInterval = 0;

    // --- ManagedLedger
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java

    // --- ManagedLedger
    private long managedLedgerCacheSizeMB = 0L;
    private int managedLedgerNumSchedulerThreads = Runtime.getRuntime().availableProcessors();

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java
    private Boolean tlsHostnameVerificationEnable;

    private boolean namespaceDelimiterRewriteEnable = false;
    private String rewriteNamespaceDelimiter = "/";

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java
    //--- Ledger metrics ---
    private boolean exposeTopicLevelMetricsInPrometheus = false;
    private boolean exposeManagedLedgerMetricsInPrometheus = false;
    private int managedLedgerStatsPeriodSeconds = 60;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorConfig.java`
#### Snippet
```java
    private String rewriteNamespaceDelimiter = "/";

    private boolean authorizationEnabled = false;

    // --- Ledger Offloading ---
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java

    private Watcher sessionWatcher;
    private long sessionId = 0L;
    private int readOpDelayMs;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyServiceStarter.java`
#### Snippet
```java

    @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
    private boolean generateDocs = false;

    private ProxyConfiguration config;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyServiceStarter.java`
#### Snippet
```java

    @Parameter(names = { "-h", "--help" }, description = "Show this help message")
    private boolean help = false;

    @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/DirectProxyHandler.java`
#### Snippet
```java
    @Getter
    Channel outboundChannel;
    boolean isTlsOutboundChannel = false;
    @Getter
    private final Rate inboundChannelRequestsRate;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/AdminProxyHandler.java`
#### Snippet
```java
        static final int MIN_REPLAY_BODY_BUFFER_SIZE = 64;
        private boolean bodyBufferAvailable = false;
        private boolean bodyBufferMaxSizeReached = false;
        private final ByteArrayOutputStream bodyBuffer;
        private final long httpInputMaxReplayBufferSize;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/AdminProxyHandler.java`
#### Snippet
```java
    protected class ReplayableProxyContentProvider extends ProxyInputStreamContentProvider {
        static final int MIN_REPLAY_BODY_BUFFER_SIZE = 64;
        private boolean bodyBufferAvailable = false;
        private boolean bodyBufferMaxSizeReached = false;
        private final ByteArrayOutputStream bodyBuffer;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private static final byte[] EMPTY_CREDENTIALS = new byte[0];

    boolean isTlsInboundChannel = false;

    enum State {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private LookupProxyHandler lookupProxyHandler = null;
    @Getter
    private DirectProxyHandler directProxyHandler = null;
    private final BrokerProxyValidator brokerProxyValidator;
    private final ConnectionController connectionController;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
    private State state;

    private LookupProxyHandler lookupProxyHandler = null;
    @Getter
    private DirectProxyHandler directProxyHandler = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java

            entryQueue.fill(new MessagePassingQueue.Supplier<Entry>() {
                private int i = 0;
                @Override
                public Entry get() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
    private PulsarSqlSchemaInfoProvider schemaInfoProvider;

    private FieldValueProvider[] currentRowValues = null;

    PulsarDispatchingRowDecoderFactory decoderFactory;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
    private RawMessage currentMessage;
    private int maxBatchSize;
    private long completedBytes = 0;
    private ReadEntries readEntries;
    private DeserializeEntries deserializeEntries;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java

        // indicate whether there are any additional entries left to read
        private boolean isDone = false;

        //num of outstanding read requests
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
    // are empty or not
    private final long splitSize;
    private long entriesProcessed = 0;
    private int partition = -1;
    private volatile Throwable deserializingError;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java
    private PulsarResources pulsarResources;
    @Getter
    private ProxyExtensions proxyExtensions = null;

    private final EventLoopGroup acceptorGroup;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarTransactionCoordinatorMetadataSetup.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarTransactionCoordinatorMetadataSetup.java`
#### Snippet
```java

        @Parameter(names = { "-h", "--help" }, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/WebServer.java`
#### Snippet
```java
    private final ProxyConfiguration config;
    protected int externalServicePort;
    private URI serviceURI = null;

    private ServerConnector connector;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarInitialNamespaceSetup.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarInitialNamespaceSetup.java`
#### Snippet
```java

        @Parameter(names = { "-h", "--help" }, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarVersionStarter.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarVersionStarter.java`
#### Snippet
```java
    private static class Arguments {
        @Parameter(names = {"-h", "--help"}, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataTeardown.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataTeardown.java`
#### Snippet
```java

        @Parameter(names = { "-h", "--help" }, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandaloneStarter.java`
#### Snippet
```java

    @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
    private boolean generateDocs = false;

    public PulsarStandaloneStarter(String[] args) throws Exception {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java

        @Parameter(names = { "-h", "--help" }, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java

        @Parameter(names = {"-h", "--help"}, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java

        @Parameter(names = {"-rfw", "--run-functions-worker"}, description = "Run functions worker with Broker")
        private boolean runFunctionsWorker = false;

        @Parameter(names = {"-fwc", "--functions-worker-conf"}, description = "Configuration file for Functions Worker")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java

        @Parameter(names = {"-rb", "--run-bookie"}, description = "Run Bookie together with Broker")
        private boolean runBookie = false;

        @Parameter(names = {"-ra", "--run-bookie-autorecovery"},
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
        @Parameter(names = {"-ra", "--run-bookie-autorecovery"},
                description = "Run Bookie Autorecovery together with broker")
        private boolean runBookieAutoRecovery = false;

        @Parameter(names = {"-bc", "--bookie-conf"}, description = "Configuration file for Bookie")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/RestException.java`
#### Snippet
```java
@SuppressWarnings("serial")
public class RestException extends WebApplicationException {
    private Throwable cause = null;
    static String getExceptionData(Throwable t) {
        StringWriter writer = new StringWriter();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
        @Parameter(names = {
                "-b", "--base64"}, description = "Encode the key in base64")
        boolean base64 = false;

        public void run() throws IOException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
    public static class Arguments {
        @Parameter(names = {"-h", "--help"}, description = "Show this help message")
        private boolean help = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java

    @Parameter(names = {"-nfw", "--no-functions-worker"}, description = "Run functions worker with Broker")
    private boolean noFunctionsWorker = false;

    @Parameter(names = {"-fwc", "--functions-worker-conf"}, description = "Configuration file for Functions Worker")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java

    @Parameter(names = { "--only-broker" }, description = "Only start Pulsar broker service (no ZK, BK)")
    private boolean onlyBroker = false;

    @Parameter(names = {"-nfw", "--no-functions-worker"}, description = "Run functions worker with Broker")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java

    @Parameter(names = { "-a", "--advertised-address" }, description = "Standalone broker advertised address")
    private String advertisedAddress = null;

    @Parameter(names = { "-h", "--help" }, description = "Show this help message")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java

    @Parameter(names = { "--no-broker" }, description = "Only start ZK and BK services, no broker")
    private boolean noBroker = false;

    @Parameter(names = { "--only-broker" }, description = "Only start Pulsar broker service (no ZK, BK)")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java

    @Parameter(names = { "-h", "--help" }, description = "Show this help message")
    private boolean help = false;

    private boolean usingNewDefaultsPIP117;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java

    @Parameter(names = { "--wipe-data" }, description = "Clean up previous ZK/BK data")
    private boolean wipeData = false;

    @Parameter(names = { "--num-bookies" }, description = "Number of local Bookies")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java

    @Parameter(names = {"-nss", "--no-stream-storage"}, description = "Disable stream storage")
    private boolean noStreamStorage = false;

    @Parameter(names = { "--stream-storage-port" }, description = "Local bookies stream storage port")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "Enable or disable the WebSocket servlet"
    )
    private boolean webSocketServiceEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            + " client authentication"
    )
    private boolean tlsAllowInsecureConnection = false;
    @FieldContext(
        category = CATEGORY_TLS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "Whether the Pulsar proxy use KeyStore type to authenticate with Pulsar brokers"
    )
    private boolean brokerClientTlsEnabledWithKeyStore = false;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                  + " used by the Pulsar proxy to authenticate with Pulsar brokers"
    )
    private String brokerClientTlsTrustStorePassword = null;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                    + "When using TLS authentication with KeyStore, available values can be SunJSSE, Conscrypt and etc."
    )
    private String tlsProvider = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String brokerClientTlsKeyStorePassword = null;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "Whether authentication is enabled for the Pulsar proxy"
    )
    private boolean authenticationEnabled = false;
    @FieldContext(
        category = CATEGORY_AUTHENTICATION,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                  + " used by the Pulsar proxy to authenticate with Pulsar brokers"
    )
    private String brokerClientTlsTrustStore = null;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "Whether the hostname is validated when the proxy creates a TLS connection with brokers"
    )
    private boolean tlsHostnameVerificationEnabled = false;
    @FieldContext(
        category = CATEGORY_TLS,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "TLS KeyStore password for proxy"
    )
    private String tlsKeyStorePassword = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "Enable the enforcement of limits on the incoming HTTP requests"
    )
    private boolean httpRequestsLimitEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "TLS KeyStore path for proxy"
    )
    private String tlsKeyStore = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "Enable TLS with KeyStore type configuration for proxy"
    )
    private boolean tlsEnabledWithKeyStore = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            + " Connections are rejected if the client certificate isn't trusted"
    )
    private boolean tlsRequireTrustedClientCertOnConnect = false;

    // KeyStore TLS config variables
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                    + "zookeeperServers and configurationStoreServers must be configured in proxy configuration "
                    + "for retrieving the active brokers.")
    private boolean checkActiveBrokers = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "Whether TLS is enabled when communicating with Pulsar brokers"
    )
    private boolean tlsEnabledWithBroker = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "The maximum number of connections per IP. If it exceeds, new connections are rejected."
    )
    private int maxConcurrentInboundConnectionsPerIp = 0;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            + "to take effect"
    )
    private boolean forwardAuthorizationCredentials = false;
    @FieldContext(
        category = CATEGORY_AUTHENTICATION,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "Whether authorization is enforced by the Pulsar proxy"
    )
    private boolean authorizationEnabled = false;
    @FieldContext(
        category = CATEGORY_AUTHORIZATION,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "TLS TrustStore path for proxy"
    )
    private String tlsTrustStore = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "The TLS Provider used by the Pulsar proxy to authenticate with Pulsar brokers"
    )
    private String brokerClientSslProvider = null;

    // needed when client auth is required
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "TLS TrustStore password for proxy, null means empty password."
    )
    private String tlsTrustStorePassword = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java

    @Deprecated
    private boolean tlsEnabledInProxy = false;
    @FieldContext(
        category = CATEGORY_TLS,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            doc = "When this parameter is not empty, unauthenticated users perform as anonymousUserRole"
    )
    private String anonymousUserRole = null;

    // TLS
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                    + " used by the Pulsar proxy to authenticate with Pulsar brokers"
    )
    private String brokerClientTlsKeyStore = null;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/delayed/InMemoryDelayedDeliveryTracker.java`
#### Snippet
```java
    // If new added messages are lower than this, it means the delivery is requested
    // to be out-of-order. It gets reset to 0, once the tracker is emptied.
    private long highestDeliveryTimeTracked = 0;

    // Track whether we have seen all messages with fixed delay so far.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private final WorkerConfig workerConfig;
    private final Optional<WorkerService> functionWorkerService;
    private ProtocolHandlers protocolHandlers = null;

    private final Consumer<Integer> processTerminator;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private ServiceConfiguration config = null;
    private NamespaceService nsService = null;
    private ManagedLedgerStorage managedLedgerClientFactory = null;
    private LeaderElectionService leaderElectionService = null;
    private BrokerService brokerService = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private LedgerOffloaderStats offloaderStats;
    private Map<NamespaceName, LedgerOffloader> ledgerOffloaderMap = new ConcurrentHashMap<>();
    private ScheduledFuture<?> loadReportTask = null;
    private LoadSheddingTask loadSheddingTask = null;
    private ScheduledFuture<?> loadResourceQuotaTask = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private ScheduledFuture<?> loadReportTask = null;
    private LoadSheddingTask loadSheddingTask = null;
    private ScheduledFuture<?> loadResourceQuotaTask = null;
    private final AtomicReference<LoadManager> loadManager = new AtomicReference<>();
    private PulsarAdmin adminClient = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private ManagedLedgerStorage managedLedgerClientFactory = null;
    private LeaderElectionService leaderElectionService = null;
    private BrokerService brokerService = null;
    private WebService webService = null;
    private WebSocketService webSocketService = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(PulsarService.class);
    private static final double GRACEFUL_SHUTDOWN_TIMEOUT_RATIO_OF_TOTAL_TIMEOUT = 0.5d;
    private ServiceConfiguration config = null;
    private NamespaceService nsService = null;
    private ManagedLedgerStorage managedLedgerClientFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private NamespaceService nsService = null;
    private ManagedLedgerStorage managedLedgerClientFactory = null;
    private LeaderElectionService leaderElectionService = null;
    private BrokerService brokerService = null;
    private WebService webService = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private final AtomicReference<LoadManager> loadManager = new AtomicReference<>();
    private PulsarAdmin adminClient = null;
    private PulsarClient client = null;
    private final String bindAddress;
    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private BrokerService brokerService = null;
    private WebService webService = null;
    private WebSocketService webSocketService = null;
    private TopicPoliciesService topicPoliciesService = TopicPoliciesService.DISABLED;
    private BookKeeperClientFactory bkClientFactory;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private String brokerServiceUrlTls;
    private final String brokerVersion;
    private SchemaStorage schemaStorage = null;
    private SchemaRegistryService schemaRegistryService = null;
    private final WorkerConfig workerConfig;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private Map<NamespaceName, LedgerOffloader> ledgerOffloaderMap = new ConcurrentHashMap<>();
    private ScheduledFuture<?> loadReportTask = null;
    private LoadSheddingTask loadSheddingTask = null;
    private ScheduledFuture<?> loadResourceQuotaTask = null;
    private final AtomicReference<LoadManager> loadManager = new AtomicReference<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private ScheduledFuture<?> loadResourceQuotaTask = null;
    private final AtomicReference<LoadManager> loadManager = new AtomicReference<>();
    private PulsarAdmin adminClient = null;
    private PulsarClient client = null;
    private final String bindAddress;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private LeaderElectionService leaderElectionService = null;
    private BrokerService brokerService = null;
    private WebService webService = null;
    private WebSocketService webSocketService = null;
    private TopicPoliciesService topicPoliciesService = TopicPoliciesService.DISABLED;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private static final double GRACEFUL_SHUTDOWN_TIMEOUT_RATIO_OF_TOTAL_TIMEOUT = 0.5d;
    private ServiceConfiguration config = null;
    private NamespaceService nsService = null;
    private ManagedLedgerStorage managedLedgerClientFactory = null;
    private LeaderElectionService leaderElectionService = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
    private final String brokerVersion;
    private SchemaStorage schemaStorage = null;
    private SchemaRegistryService schemaRegistryService = null;
    private final WorkerConfig workerConfig;
    private final Optional<WorkerService> functionWorkerService;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PublishRateLimiterImpl.java`
#### Snippet
```java

public class PublishRateLimiterImpl implements PublishRateLimiter {
    protected volatile int publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
    protected volatile boolean publishThrottlingEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PublishRateLimiterImpl.java`
#### Snippet
```java
public class PublishRateLimiterImpl implements PublishRateLimiter {
    protected volatile int publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
    protected volatile boolean publishThrottlingEnabled = false;
    protected volatile boolean publishRateExceeded = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PublishRateLimiterImpl.java`
#### Snippet
```java
    protected volatile long publishMaxByteRate = 0;
    protected volatile boolean publishThrottlingEnabled = false;
    protected volatile boolean publishRateExceeded = false;
    protected volatile LongAdder currentPublishMsgCount = new LongAdder();
    protected volatile LongAdder currentPublishByteCount = new LongAdder();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PublishRateLimiterImpl.java`
#### Snippet
```java
    protected volatile int publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
    protected volatile boolean publishThrottlingEnabled = false;
    protected volatile boolean publishRateExceeded = false;
    protected volatile LongAdder currentPublishMsgCount = new LongAdder();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherMultipleConsumers.java`
#### Snippet
```java
    protected final CopyOnWriteArrayList<Consumer> consumerList = new CopyOnWriteArrayList<>();
    protected final ObjectSet<Consumer> consumerSet = new ObjectHashSet<>();
    protected volatile int currentConsumerRoundRobinIndex = 0;

    protected static final int FALSE = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
    private final Rate msgDrop;

    private volatile long pendingPublishAcks = 0;
    private static final AtomicLongFieldUpdater<Producer> pendingPublishAcksUpdater = AtomicLongFieldUpdater
            .newUpdater(Producer.class, "pendingPublishAcks");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Producer.java`
#### Snippet
```java
            .newUpdater(Producer.class, "pendingPublishAcks");

    private boolean isClosed = false;
    private final CompletableFuture<Void> closeFuture;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    protected final CopyOnWriteArrayList<Consumer> consumers;
    protected StickyKeyConsumerSelector stickyKeyConsumerSelector;
    protected boolean isKeyHashRangeFiltered = false;
    protected CompletableFuture<Void> closeFuture = null;
    protected final int partitionIndex;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    protected StickyKeyConsumerSelector stickyKeyConsumerSelector;
    protected boolean isKeyHashRangeFiltered = false;
    protected CompletableFuture<Void> closeFuture = null;
    protected final int partitionIndex;
    protected final ManagedCursor cursor;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractDispatcherSingleActiveConsumer.java`
#### Snippet
```java
            ACTIVE_CONSUMER_UPDATER = AtomicReferenceFieldUpdater.newUpdater(
            AbstractDispatcherSingleActiveConsumer.class, Consumer.class, "activeConsumer");
    private volatile Consumer activeConsumer = null;
    protected final CopyOnWriteArrayList<Consumer> consumers;
    protected StickyKeyConsumerSelector stickyKeyConsumerSelector;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PrecisPublishLimiter.java`
#### Snippet
```java
public class PrecisPublishLimiter implements PublishRateLimiter {
    protected volatile int publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
    // precise mode for publish rate limiter
    private volatile RateLimiter topicPublishRateLimiterOnMessage;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PrecisPublishLimiter.java`
#### Snippet
```java

public class PrecisPublishLimiter implements PublishRateLimiter {
    protected volatile int publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
    // precise mode for publish rate limiter
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
            AtomicIntegerFieldUpdater.newUpdater(Consumer.class, "unackedMessages");
    private volatile int unackedMessages = 0;
    private volatile boolean blockedConsumerOnUnackedMsgs = false;

    private final Map<String, String> metadata;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
    private static final AtomicIntegerFieldUpdater<Consumer> UNACKED_MESSAGES_UPDATER =
            AtomicIntegerFieldUpdater.newUpdater(Consumer.class, "unackedMessages");
    private volatile int unackedMessages = 0;
    private volatile boolean blockedConsumerOnUnackedMsgs = false;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
    private static final AtomicIntegerFieldUpdater<Consumer> PERMITS_RECEIVED_WHILE_CONSUMER_BLOCKED_UPDATER =
            AtomicIntegerFieldUpdater.newUpdater(Consumer.class, "permitsReceivedWhileConsumerBlocked");
    private volatile int permitsReceivedWhileConsumerBlocked = 0;

    private final ConcurrentLongLongPairHashMap pendingAcks;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
    private static final AtomicIntegerFieldUpdater<Consumer> MESSAGE_PERMITS_UPDATER =
            AtomicIntegerFieldUpdater.newUpdater(Consumer.class, "messagePermits");
    private volatile int messagePermits = 0;
    // It starts keep tracking of messagePermits once consumer gets blocked, as consumer needs two separate counts:
    // messagePermits (1) before and (2) after being blocked: to dispatch only blockedPermit number of messages at the
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/PulsarMetadataEventSynchronizer.java`
#### Snippet
```java
    listeners = new CopyOnWriteArrayList<>();

    private volatile boolean started = false;
    public static final String SUBSCRIPTION_NAME = "metadata-syncer";
    private static final int MAX_PRODUCER_PENDING_SIZE = 1000;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    private static final AtomicLongFieldUpdater<AbstractTopic> USAGE_COUNT_UPDATER =
            AtomicLongFieldUpdater.newUpdater(AbstractTopic.class, "usageCount");
    private volatile long usageCount = 0;

    private Map<String/*subscription*/, SubscriptionPolicies> subscriptionPolicies = Collections.emptyMap();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java

    // Whether messages published must be encrypted or not in this topic
    protected volatile boolean isEncryptionRequired = false;

    protected volatile Boolean isAllowAutoUpdateSchema;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    // Flag to signal that producer of this topic has published batch-message so, broker should not allow consumer which
    // doesn't support batch-message
    protected volatile boolean hasBatchMessagePublished = false;

    protected StatsBuckets addEntryLatencyStatsUsec = new StatsBuckets(ENTRY_LATENCY_BUCKETS_USEC);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
    private static final AtomicLongFieldUpdater<AbstractTopic> RATE_LIMITED_UPDATER =
            AtomicLongFieldUpdater.newUpdater(AbstractTopic.class, "publishRateLimitedTimes");
    protected volatile long publishRateLimitedTimes = 0;

    protected volatile Optional<Long> topicEpoch = Optional.empty();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
    private final StickyKeyConsumerSelector selector;

    private boolean isDispatcherStuckOnReplays = false;
    private final KeySharedMode keySharedMode;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    protected volatile int readBatchSize;
    protected final Backoff readFailureBackoff;
    private volatile ScheduledFuture<?> readOnActiveConsumerTask = null;

    private final RedeliveryTracker redeliveryTracker;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java
    private Optional<DispatchRateLimiter> dispatchRateLimiter = Optional.empty();

    protected volatile boolean havePendingRead = false;

    protected volatile int readBatchSize;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/MessageDeduplication.java`
#### Snippet
```java

    // The timestamp when the snapshot was taken by the scheduled task last time
    private volatile long lastSnapshotTimestamp = 0L;

    // Max number of producer for which to persist the sequence id information
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentMessageFinder.java`
#### Snippet
```java
    private final String subName;
    private final String topicName;
    private long timestamp = 0;

    private static final int FALSE = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/ReplicatedSubscriptionsController.java`
#### Snippet
```java

    // The timestamp of when the last snapshot was initiated
    private volatile long lastCompletedSnapshotStartTime = 0;

    private String lastCompletedSnapshotId;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
    protected final Rate msgExpired = new Rate();

    protected int messageTTLInSeconds = 0;

    private final Backoff readFailureBackoff = new Backoff(1, TimeUnit.SECONDS,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
            AtomicIntegerFieldUpdater
                    .newUpdater(PersistentReplicator.class, "pendingMessages");
    private volatile int pendingMessages = 0;

    private static final int FALSE = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
    private final ReplicatorStatsImpl stats = new ReplicatorStatsImpl();

    protected volatile boolean fetchSchemaInProgress = false;

    public PersistentReplicator(String localCluster, PersistentTopic localTopic, ManagedCursor cursor,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStreamingDispatcherMultipleConsumers.java`
#### Snippet
```java
    implements StreamingDispatcher {

    private int sendingTaskCounter = 0;
    private final StreamingEntryReader streamingEntryReader = new StreamingEntryReader((ManagedCursorImpl) cursor,
            this, topic);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    protected final Subscription subscription;

    private CompletableFuture<Void> closeFuture = null;
    private final String name;
    protected final Rate msgDrop;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            .newUpdater(NonPersistentDispatcherMultipleConsumers.class, "totalAvailablePermits");
    @SuppressWarnings("unused")
    private volatile int totalAvailablePermits = 0;

    private final RedeliveryTracker redeliveryTracker;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java
    private final boolean isDurable;

    private KeySharedMode keySharedMode = null;

    public NonPersistentSubscription(NonPersistentTopic topic, String subscriptionName, boolean isDurable,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    private int nonPersistentPendingMessages = 0;
    private final int maxNonPersistentPendingMessages;
    private String originalPrincipal = null;
    private Set<String> proxyRoles;
    private final boolean schemaValidationEnforced;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java

    // Flag to manage throttling-publish-buffer by atomically enable/disable read-channel.
    private boolean autoReadDisabledPublishBufferLimiting = false;
    private final long maxPendingBytesPerThread;
    private final long resumeThresholdPendingBytesPerThread;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    private final int maxPendingSendRequests;
    private final int resumeReadsThreshold;
    private int pendingSendRequest = 0;
    private final String replicatorPrefix;
    private String clientVersion = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    private int pendingSendRequest = 0;
    private final String replicatorPrefix;
    private String clientVersion = null;
    private int nonPersistentPendingMessages = 0;
    private final int maxNonPersistentPendingMessages;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java

    // Flag to manage throttling-rate by atomically enable/disable read-channel.
    private volatile boolean autoReadDisabledRateLimiting = false;
    private FeatureFlags features;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    private final String replicatorPrefix;
    private String clientVersion = null;
    private int nonPersistentPendingMessages = 0;
    private final int maxNonPersistentPendingMessages;
    private String originalPrincipal = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    private AuthenticationState originalAuthState;
    private AuthenticationDataSource originalAuthData;
    private boolean pendingAuthChallengeResponse = false;

    // Max number of pending requests per connections. If multiple producers are sharing the same connection the flow
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    private State state;
    private volatile boolean isActive = true;
    private String authRole = null;
    private volatile AuthenticationDataSource authenticationData;
    private AuthenticationProvider authenticationProvider;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    protected volatile boolean havePendingReplayRead = false;
    protected volatile PositionImpl minReplayedPosition = null;
    protected boolean shouldRewindBeforeReadingOrReplaying = false;
    protected final String name;
    private boolean sendInProgress = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    protected volatile boolean havePendingRead = false;
    protected volatile boolean havePendingReplayRead = false;
    protected volatile PositionImpl minReplayedPosition = null;
    protected boolean shouldRewindBeforeReadingOrReplaying = false;
    protected final String name;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    protected boolean shouldRewindBeforeReadingOrReplaying = false;
    protected final String name;
    private boolean sendInProgress = false;
    protected static final AtomicIntegerFieldUpdater<PersistentDispatcherMultipleConsumers>
            TOTAL_AVAILABLE_PERMITS_UPDATER =
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            AtomicIntegerFieldUpdater.newUpdater(PersistentDispatcherMultipleConsumers.class,
                    "totalUnackedMessages");
    protected volatile int totalUnackedMessages = 0;
    private volatile int blockedDispatcherOnUnackedMsgs = FALSE;
    protected static final AtomicIntegerFieldUpdater<PersistentDispatcherMultipleConsumers>
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    protected volatile Range<PositionImpl> lastIndividualDeletedRangeFromCursorRecovery;

    private CompletableFuture<Void> closeFuture = null;
    protected final MessageRedeliveryController redeliveryMessages;
    protected final RedeliveryTracker redeliveryTracker;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
    private Optional<DelayedDeliveryTracker> delayedDeliveryTracker = Optional.empty();

    protected volatile boolean havePendingRead = false;
    protected volatile boolean havePendingReplayRead = false;
    protected volatile PositionImpl minReplayedPosition = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
            AtomicIntegerFieldUpdater.newUpdater(PersistentDispatcherMultipleConsumers.class,
                    "totalAvailablePermits");
    protected volatile int totalAvailablePermits = 0;
    protected volatile int readBatchSize;
    protected final Backoff readFailureBackoff;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java

    protected volatile boolean havePendingRead = false;
    protected volatile boolean havePendingReplayRead = false;
    protected volatile PositionImpl minReplayedPosition = null;
    protected boolean shouldRewindBeforeReadingOrReplaying = false;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    private volatile long lastExpireTimestamp = 0L;
    private volatile long lastConsumedFlowTimestamp = 0L;
    private volatile long lastMarkDeleteAdvancedTimestamp = 0L;

    // for connected subscriptions, message expiry will be checked if the backlog is greater than this threshold
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
    private PersistentMessageExpiryMonitor expiryMonitor;

    private volatile long lastExpireTimestamp = 0L;
    private volatile long lastConsumedFlowTimestamp = 0L;
    private volatile long lastMarkDeleteAdvancedTimestamp = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java

    private volatile long lastExpireTimestamp = 0L;
    private volatile long lastConsumedFlowTimestamp = 0L;
    private volatile long lastMarkDeleteAdvancedTimestamp = 0L;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LoadSheddingTask.java`
#### Snippet
```java
    private final ServiceConfiguration config;

    private volatile boolean isCancel = false;

    private volatile ScheduledFuture<?> future;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/PulsarLoadReportImpl.java`
#### Snippet
```java
    private static final Logger log = LoggerFactory.getLogger(PulsarLoadReportImpl.class);
    public Map<ServiceUnit, ServiceRequest> requestPerServiceUnit;
    private SimpleResourceUnit resourceUnit = null;
    private PulsarResourceDescription resourceDescription = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/PulsarLoadReportImpl.java`
#### Snippet
```java
    public Map<ServiceUnit, ServiceRequest> requestPerServiceUnit;
    private SimpleResourceUnit resourceUnit = null;
    private PulsarResourceDescription resourceDescription = null;

    private PulsarLoadReportImpl() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
    private volatile long entriesAddedCounter = 0;

    private volatile boolean migrated = false;
    private static final FastThreadLocal<TopicStats> threadLocalTopicStats = new FastThreadLocal<TopicStats>() {
        @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
    private static final AtomicLongFieldUpdater<NonPersistentTopic> ENTRIES_ADDED_COUNTER_UPDATER =
            AtomicLongFieldUpdater.newUpdater(NonPersistentTopic.class, "entriesAddedCounter");
    private volatile long entriesAddedCounter = 0;

    private volatile boolean migrated = false;
```

### RedundantFieldInitialization
Field initialization to `0d` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/GenericBrokerHostUsageImpl.java`
#### Snippet
```java
    private static final int CPU_CHECK_MILLIS = 1000;
    private double totalCpuLimit;
    private double cpuUsageSum = 0d;
    private int cpuUsageCount = 0;
    private OperatingSystemMXBean systemBean;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/GenericBrokerHostUsageImpl.java`
#### Snippet
```java
    private double totalCpuLimit;
    private double cpuUsageSum = 0d;
    private int cpuUsageCount = 0;
    private OperatingSystemMXBean systemBean;
    private SystemResourceUsage usage;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ThresholdShedder.java`
#### Snippet
```java
    private static final long LOAD_LOG_SAMPLE_DELAY_IN_SEC = 5 * 60; // 5 mins
    private final Map<String, Double> brokerAvgResourceUsage = new HashMap<>();
    private long lastSampledLoadLogTS = 0;


```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private double realtimeMemoryLoadFactor = 25.0;
    // realtime average resource quota
    private ResourceQuota realtimeAvgResourceQuota = null;
    // realtime resource quota calculated from the latest load reports
    private AtomicReference<Map<String, ResourceQuota>> realtimeResourceQuotas = new AtomicReference<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private AtomicReference<Map<Long, Set<ResourceUnit>>> sortedRankings = new AtomicReference<>();
    // rotation cursor between brokers
    private long brokerRotationCursor = 0;
    // load balancing metrics
    private AtomicReference<List<Metrics>> loadBalancingMetrics = new AtomicReference<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private long lastResourceUsageTimestamp = -1;
    // flag to force update load report
    private boolean forceLoadReportUpdate = false;
    // check if given broker can load persistent/non-persistent topic
    private final BrokerTopicLoadingPredicate brokerTopicLoadingPredicate;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java

    // average JVM heap usage for
    private long avgJvmHeapUsageMBytes = 0;
    // load report got from each broker
    private Map<ResourceUnit, LoadReport> currentLoadReports;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBufferState.java`
#### Snippet
```java

    @SuppressWarnings("unused")
    private volatile State state = null;

    public TopicTransactionBufferState(State state) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
    private AtomicReference<List<Metrics>> bundleMetrics = new AtomicReference<>();

    private long bundleSplitCount = 0;
    private long unloadBrokerCount = 0;
    private long unloadBundleCount = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
    private long bundleSplitCount = 0;
    private long unloadBrokerCount = 0;
    private long unloadBundleCount = 0;

    private final Lock lock = new ReentrantLock();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java

    private long bundleSplitCount = 0;
    private long unloadBrokerCount = 0;
    private long unloadBundleCount = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TopicTransactionBuffer.java`
#### Snippet
```java
        public void readEntriesComplete(List<Entry> entries, Object ctx) {
            entryQueue.fill(new MessagePassingQueue.Supplier<Entry>() {
                private int i = 0;
                @Override
                public Entry get() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    private final AtomicLong pendingWriteOps = new AtomicLong(0);
    private volatile double lastUpdatedAvgPublishRateInMsg = 0;
    private volatile double lastUpdatedAvgPublishRateInByte = 0;

    private volatile boolean isClosingOrDeleting = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    private volatile double lastUpdatedAvgPublishRateInByte = 0;

    private volatile boolean isClosingOrDeleting = false;

    private ScheduledFuture<?> fencedTopicMonitoringTask = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

    // Record the last time a data message (ie: not an internal Pulsar marker) is published on the topic
    private volatile long lastDataMessagePublishedTimestamp = 0;

    private static class TopicStatsHelper {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

    private final AtomicLong pendingWriteOps = new AtomicLong(0);
    private volatile double lastUpdatedAvgPublishRateInMsg = 0;
    private volatile double lastUpdatedAvgPublishRateInByte = 0;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    private volatile boolean isClosingOrDeleting = false;

    private ScheduledFuture<?> fencedTopicMonitoringTask = null;

    @Getter
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleState.java`
#### Snippet
```java

    @SuppressWarnings("unused")
    protected volatile State state = null;

    public PendingAckHandleState(State state) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupPublishLimiter.java`
#### Snippet
```java

public class ResourceGroupPublishLimiter implements PublishRateLimiter, RateLimitFunction, AutoCloseable  {
    protected volatile long publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
    protected volatile boolean publishThrottlingEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupPublishLimiter.java`
#### Snippet
```java
    protected volatile long publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
    protected volatile boolean publishThrottlingEnabled = false;
    private volatile RateLimiter publishRateLimiterOnMessage;
    private volatile RateLimiter publishRateLimiterOnByte;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupPublishLimiter.java`
#### Snippet
```java
public class ResourceGroupPublishLimiter implements PublishRateLimiter, RateLimitFunction, AutoCloseable  {
    protected volatile long publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
    protected volatile boolean publishThrottlingEnabled = false;
    private volatile RateLimiter publishRateLimiterOnMessage;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundle.java`
#### Snippet
```java
    // Issue#596: remove this once we remove broker persistent/non-persistent mode configuration
    // it is used by load-manager while considering bundle ownership
    private boolean hasNonPersistentTopic = false;
    private final String key;
    private final String bundleRange;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/MLPendingAckStore.java`
#### Snippet
```java
        public void readEntriesComplete(List<Entry> entries, Object ctx) {
            entryQueue.fill(new MessagePassingQueue.Supplier<Entry>() {
                private int i = 0;
                @Override
                public Entry get() {
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactionRecord.java`
#### Snippet
```java
    private volatile long lastCompactionFailedTimestamp = 0L;
    @Getter
    private volatile long lastCompactionDurationTimeInMills = 0L;

    private LongAdder lastCompactionRemovedEventCountOp = new LongAdder();
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactionRecord.java`
#### Snippet
```java
    private volatile long lastCompactionSucceedTimestamp = 0L;
    @Getter
    private volatile long lastCompactionFailedTimestamp = 0L;
    @Getter
    private volatile long lastCompactionDurationTimeInMills = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactionRecord.java`
#### Snippet
```java
    private volatile long lastCompactionRemovedEventCount = 0L;
    @Getter
    private volatile long lastCompactionSucceedTimestamp = 0L;
    @Getter
    private volatile long lastCompactionFailedTimestamp = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactionRecord.java`
#### Snippet
```java

    @Getter
    private volatile long lastCompactionRemovedEventCount = 0L;
    @Getter
    private volatile long lastCompactionSucceedTimestamp = 0L;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactorTool.java`
#### Snippet
```java

        @Parameter(names = {"-h", "--help"}, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactorTool.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactedTopicImpl.java`
#### Snippet
```java

    private PositionImpl compactionHorizon = null;
    private CompletableFuture<CompactedTopicContext> compactedTopicContext = null;

    public CompactedTopicImpl(BookKeeper bk) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactedTopicImpl.java`
#### Snippet
```java
    private final BookKeeper bk;

    private PositionImpl compactionHorizon = null;
    private CompletableFuture<CompactedTopicContext> compactedTopicContext = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    private ScheduledExecutorService deduplicationSnapshotMonitor;
    protected volatile PublishRateLimiter brokerPublishRateLimiter = PublishRateLimiter.DISABLED_RATE_LIMITER;
    protected volatile DispatchRateLimiter brokerDispatchRateLimiter = null;

    private DistributedIdGenerator producerNameGenerator;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    @Getter
    private final ConcurrentOpenHashMap<String, ConcurrentOpenHashSet<Integer>> owningTopics;
    private int numberOfNamespaceBundles = 0;

    private final EventLoopGroup acceptorGroup;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    private final ConcurrentLinkedQueue<TopicLoadingContext> pendingTopicLoadingQueue;

    private AuthorizationService authorizationService = null;
    private final ScheduledExecutorService statsUpdater;
    @Getter
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    protected static class PublishRateLimiterMonitor {
        private final String name;
        private ScheduledExecutorService scheduler = null;
        private long tickTimeMs = 0;
        private Runnable refreshTask;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        private final String name;
        private ScheduledExecutorService scheduler = null;
        private long tickTimeMs = 0;
        private Runnable refreshTask;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TopicListWatcher.java`
#### Snippet
```java
    private final Pattern topicsPattern;
    private final long watcherId;
    private volatile long createWatcherDeadline = 0;
    private final NamespaceName namespace;
    // TODO maintain the value based on updates from broker and warn the user if inconsistent with hash from polling
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AbstractBatchMessageContainer.java`
#### Snippet
```java
    protected int maxNumMessagesInBatch;
    protected int maxBytesInBatch;
    protected int numMessagesInBatch = 0;
    protected long currentBatchSizeBytes = 0;
    protected int batchAllocatedSizeBytes = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AbstractBatchMessageContainer.java`
#### Snippet
```java
    protected int maxBytesInBatch;
    protected int numMessagesInBatch = 0;
    protected long currentBatchSizeBytes = 0;
    protected int batchAllocatedSizeBytes = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AbstractBatchMessageContainer.java`
#### Snippet
```java
    protected int numMessagesInBatch = 0;
    protected long currentBatchSizeBytes = 0;
    protected int batchAllocatedSizeBytes = 0;

    protected long currentTxnidMostBits = -1L;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageContainerImpl.java`
#### Snippet
```java
    private ByteBuf batchedMessageMetadataAndPayload;
    private List<MessageImpl<?>> messages = new ArrayList<>(maxMessagesNum);
    protected SendCallback previousCallback = null;
    // keep track of callbacks for individual messages being published in a batch
    protected SendCallback firstCallback;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageContainerImpl.java`
#### Snippet
```java
    private final ByteBufAllocator allocator;
    private static final int SHRINK_COOLING_OFF_PERIOD = 10;
    private int consecutiveShrinkTime = 0;

    public BatchMessageContainerImpl() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedTopicProducerStatsRecorderImpl.java`
#### Snippet
```java
    private final DoubleAdder sendMsgsRateAggregate;
    private final DoubleAdder sendBytesRateAggregate;
    private int partitions = 0;

    public PartitionedTopicProducerStatsRecorderImpl() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AutoClusterFailover.java`
#### Snippet
```java
        private String primary;
        private List<String> secondary;
        private Map<String, Authentication> secondaryAuthentications = null;
        private Map<String, String> secondaryTlsTrustCertsFilePaths = null;
        private Map<String, String> secondaryTlsTrustStorePaths = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AutoClusterFailover.java`
#### Snippet
```java
        private Map<String, String> secondaryTlsTrustCertsFilePaths = null;
        private Map<String, String> secondaryTlsTrustStorePaths = null;
        private Map<String, String> secondaryTlsTrustStorePasswords = null;
        private FailoverPolicy failoverPolicy = FailoverPolicy.ORDER;
        private long failoverDelayNs;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AutoClusterFailover.java`
#### Snippet
```java
        private Map<String, Authentication> secondaryAuthentications = null;
        private Map<String, String> secondaryTlsTrustCertsFilePaths = null;
        private Map<String, String> secondaryTlsTrustStorePaths = null;
        private Map<String, String> secondaryTlsTrustStorePasswords = null;
        private FailoverPolicy failoverPolicy = FailoverPolicy.ORDER;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AutoClusterFailover.java`
#### Snippet
```java
        private List<String> secondary;
        private Map<String, Authentication> secondaryAuthentications = null;
        private Map<String, String> secondaryTlsTrustCertsFilePaths = null;
        private Map<String, String> secondaryTlsTrustStorePaths = null;
        private Map<String, String> secondaryTlsTrustStorePasswords = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java
    protected String proxyToTargetBrokerAddress = null;
    // Remote hostName with which client is connected
    protected String remoteHostName = null;

    private ScheduledFuture<?> timeoutTask;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java
    private final long operationTimeoutMs;

    protected String proxyToTargetBrokerAddress = null;
    // Remote hostName with which client is connected
    protected String remoteHostName = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java
            AtomicIntegerFieldUpdater.newUpdater(ClientCnx.class, "numberOfRejectRequests");
    @SuppressWarnings("unused")
    private volatile int numberOfRejectRequests = 0;

    @Getter
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TableViewConfigurationData.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private String topicName = null;
    private String subscriptionName = null;
    private long autoUpdatePartitionsSeconds = 60;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TableViewConfigurationData.java`
#### Snippet
```java

    private String topicName = null;
    private String subscriptionName = null;
    private long autoUpdatePartitionsSeconds = 60;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ResetCursorData.java`
#### Snippet
```java
    protected long entryId;
    protected int partitionIndex = -1;
    protected boolean isExcluded = false;
    protected int batchIndex = -1;
    protected Map<String, String> properties;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/RoundRobinPartitionMessageRouterImpl.java`
#### Snippet
```java
            AtomicIntegerFieldUpdater.newUpdater(RoundRobinPartitionMessageRouterImpl.class, "partitionIndex");
    @SuppressWarnings("unused")
    private volatile int partitionIndex = 0;

    private final int startPtnIdx;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/HandlerState.java`
#### Snippet
```java
            AtomicReferenceFieldUpdater.newUpdater(HandlerState.class, State.class, "state");
    @SuppressWarnings("unused")
    private volatile State state = null;

    enum State {
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConnectionHandler.java`
#### Snippet
```java
    // Start with -1L because it gets incremented before sending on the first connection
    private volatile long epoch = -1L;
    protected volatile long lastConnectionClosedTimestamp = 0L;

    interface Connection {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConnectionHandler.java`
#### Snippet
```java
            AtomicReferenceFieldUpdater.newUpdater(ConnectionHandler.class, ClientCnx.class, "clientCnx");
    @SuppressWarnings("unused")
    private volatile ClientCnx clientCnx = null;

    protected final HandlerState state;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ControlledClusterFailover.java`
#### Snippet
```java
        private String defaultServiceUrl;
        private String urlProvider;
        private Map<String, String> header = null;
        private long interval = 30_000;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ControlledClusterFailover.java`
#### Snippet
```java
    private final ScheduledExecutorService executor;
    private final long interval;
    private ObjectMapper objectMapper = null;
    private final AsyncHttpClient httpClient;
    private final BoundRequestBuilder requestBuilder;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnackMessageIdWrapper.java`
#### Snippet
```java
    private final Recycler.Handle<UnackMessageIdWrapper> recyclerHandle;
    private MessageId messageId;
    private int redeliveryCount = 0;

    private UnackMessageIdWrapper(Recycler.Handle<UnackMessageIdWrapper> recyclerHandle) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/Backoff.java`
#### Snippet
```java

    private long firstBackoffTimeInMillis;
    private boolean mandatoryStopMade = false;

    private static final Random random = new Random();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java
    private volatile MessageIdImpl messageId = DEFAULT_MESSAGE_ID;
    private BitSetRecyclable bitSetRecyclable = null;
    private boolean flushRequired = false;

    public synchronized void update(final MessageIdImpl messageId, final BitSetRecyclable bitSetRecyclable) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PersistentAcknowledgmentsGroupingTracker.java`
#### Snippet
```java

    private volatile MessageIdImpl messageId = DEFAULT_MESSAGE_ID;
    private BitSetRecyclable bitSetRecyclable = null;
    private boolean flushRequired = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PatternMultiTopicsConsumerImpl.java`
#### Snippet
```java
    private final CompletableFuture<TopicListWatcher> watcherFuture;
    protected NamespaceName namespaceName;
    private volatile Timeout recheckPatternTimeout = null;
    private volatile String topicsHash;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java

    // timeout related to auto check and subscribe partition increasement
    private volatile Timeout partitionsAutoUpdateTimeout = null;
    TopicsPartitionChangedListener topicsPartitionChangedListener;
    CompletableFuture<Void> partitionsAutoUpdateFuture = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PartitionedProducerImpl.java`
#### Snippet
```java
    private volatile Timeout partitionsAutoUpdateTimeout = null;
    TopicsPartitionChangedListener topicsPartitionChangedListener;
    CompletableFuture<Void> partitionsAutoUpdateFuture = null;

    public PartitionedProducerImpl(PulsarClientImpl client, String topic, ProducerConfigurationData conf,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
    private static final AtomicLongFieldUpdater<ConsumerBase> INCOMING_MESSAGES_SIZE_UPDATER = AtomicLongFieldUpdater
            .newUpdater(ConsumerBase.class, "incomingMessagesSize");
    protected volatile long incomingMessagesSize = 0;
    protected volatile Timeout batchReceiveTimeout = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBase.java`
#### Snippet
```java
            .newUpdater(ConsumerBase.class, "incomingMessagesSize");
    protected volatile long incomingMessagesSize = 0;
    protected volatile Timeout batchReceiveTimeout = null;

    // Only work when subscription type is Failover or Exclusive
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/BatchMessageAcker.java`
#### Snippet
```java
    private final int batchSize;
    private final BitSet bitSet;
    private volatile boolean prevBatchCumulativelyAcked = false;

    BatchMessageAcker(BitSet bitSet, int batchSize) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/NegativeAcksTracker.java`
#### Snippet
```java
class NegativeAcksTracker implements Closeable {

    private HashMap<MessageId, Long> nackedMessages = null;

    private final ConsumerBase<?> consumer;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ZeroQueueConsumerImpl.java`
#### Snippet
```java

    private volatile boolean waitingOnReceiveForZeroQueueSize = false;
    private volatile boolean waitingOnListenerForZeroQueueSize = false;

    public ZeroQueueConsumerImpl(PulsarClientImpl client, String topic, ConsumerConfigurationData<T> conf,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ZeroQueueConsumerImpl.java`
#### Snippet
```java
    private final Lock zeroQueueLock = new ReentrantLock();

    private volatile boolean waitingOnReceiveForZeroQueueSize = false;
    private volatile boolean waitingOnListenerForZeroQueueSize = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationToken.java`
#### Snippet
```java

    private static final long serialVersionUID = 1L;
    private Supplier<String> tokenSupplier = null;

    public AuthenticationToken() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/ClientCredentialsFlow.java`
#### Snippet
```java
    private transient ClientCredentialsExchanger exchanger;

    private boolean initialized = false;

    @Builder
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
        int numMessagesInBatch = 1;
        long highestSequenceId;
        int totalChunks = 0;
        int chunkId = -1;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
        long lastSentAt;
        int retryCount;
        long batchSizeByte = 0;
        int numMessagesInBatch = 1;
        long highestSequenceId;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
            .newUpdater(ProducerImpl.class, "producerDeadline");
    @SuppressWarnings("unused")
    private volatile long producerDeadline = 0; // gets set on first successful connection

    private final BatchMessageContainerBase batchMessageContainer;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
        sendAsync(interceptorMessage, new SendCallback() {
            SendCallback nextCallback = null;
            MessageImpl<?> nextMsg = null;
            long createdAt = System.nanoTime();

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    protected static class OpSendMsgQueue implements Iterable<OpSendMsg> {
        private final Queue<OpSendMsg> delegate = new ArrayDeque<>();
        private int forEachDepth = 0;
        private List<OpSendMsg> postponedOpSendMgs;
        private final AtomicInteger messagesCount = new AtomicInteger(0);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
        }
        sendAsync(interceptorMessage, new SendCallback() {
            SendCallback nextCallback = null;
            MessageImpl<?> nextMsg = null;
            long createdAt = System.nanoTime();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private final OpSendMsgQueue pendingMessages;
    private final Optional<Semaphore> semaphore;
    private volatile Timeout sendTimeout = null;
    private final long lookupDeadline;
    private int chunkMaxMessageSize;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
    private final MessageCrypto msgCrypto;

    private ScheduledFuture<?> keyGeneratorTask = null;

    private final Map<String, String> metadata;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ReaderConfigurationData.java`
#### Snippet
```java
                    + "leads to a subscription call throwing a `PulsarClientException`."
    )
    private boolean readCompacted = false;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ReaderConfigurationData.java`
#### Snippet
```java
            value = "Interface that abstracts the access to a key store."
    )
    private CryptoKeyReader cryptoKeyReader = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ReaderConfigurationData.java`
#### Snippet
```java
            value = "Reader name"
    )
    private String readerName = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ReaderConfigurationData.java`
#### Snippet
```java
    private int maxPendingChunkedMessage = 10;

    private boolean autoAckOldestChunkedMessageOnQueueFull = false;

    private long expireTimeOfIncompleteChunkedMessageMillis = TimeUnit.MINUTES.toMillis(1);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ReaderConfigurationData.java`
#### Snippet
```java
    private transient List<Range> keyHashRanges;

    private boolean poolMessages = false;

    private boolean autoUpdatePartitions = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ReaderConfigurationData.java`
#### Snippet
```java
            value = "Prefix of subscription role."
    )
    private String subscriptionRolePrefix = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ReaderConfigurationData.java`
#### Snippet
```java
                    + "`messageId`."
    )
    private boolean resetIncludeHead = false;

    private transient List<Range> keyHashRanges;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ReaderConfigurationData.java`
#### Snippet
```java
            value = "Subscription name"
    )
    private String subscriptionName = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java
    private ProducerAccessMode accessMode = ProducerAccessMode.Shared;

    private boolean lazyStartPartitionedProducers = false;

    private SortedMap<String, String> properties = new TreeMap<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java
            value = "Producer name"
    )
    private String producerName = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java
            value = "Topic name"
    )
    private String topicName = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java
                    + "* `pulsar.CustomPartition`: a custom partitioning scheme"
    )
    private MessageRoutingMode messageRoutingMode = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java

    @JsonIgnore
    private transient MessageCrypto messageCrypto = null;

    private Set<String> encryptionKeys = new TreeSet<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java
                    + "The `MaxPendingMessages` parameter determines the size of the outgoing message queue."
    )
    private boolean blockIfQueueFull = false;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java

    @JsonIgnore
    private MessageRouter customMessageRouter = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java
            value = "Enable chunking of messages."
    )
    private boolean chunkingEnabled = false;
    private int chunkMaxMessageSize = -1;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java
                    + " If this field is not set, the initial subscription is not created."
    )
    private String initialSubscriptionName = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ProducerConfigurationData.java`
#### Snippet
```java

    // Cannot use Optional<Long> since it's not serializable
    private Long initialSequenceId = null;

    private boolean autoUpdatePartitions = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
                    + " by their broker."
    )
    private boolean autoAckOldestChunkedMessageOnQueueFull = false;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
            value = "Timeout of unacked messages"
    )
    private long ackTimeoutMillis = 0;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
            value = "Consumer name"
    )
    private String consumerName = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
    private boolean replicateSubscriptionState = false;

    private boolean resetIncludeHead = false;

    @JsonIgnore
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java

    @JsonIgnore
    private CryptoKeyReader cryptoKeyReader = null;

    @JsonIgnore
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
    private boolean batchIndexAckEnabled = false;

    private boolean ackReceiptEnabled = false;

    private boolean poolMessages = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
    private boolean startPaused = false;

    private boolean autoScaledReceiverQueueSizeEnabled = false;

    private List<TopicConsumerConfigurationData> topicConfigurations = new ArrayList<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java

    @JsonIgnore
    private transient MessageCrypto messageCrypto = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
    private transient KeySharedPolicy keySharedPolicy;

    private boolean batchIndexAckEnabled = false;

    private boolean ackReceiptEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
                    + "Order in which a broker dispatches messages to consumers is: C1, C2, C3, C1, C4, C5, C4."
    )
    private int priorityLevel = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
                    + " clusters."
    )
    private boolean replicateSubscriptionState = false;

    private boolean resetIncludeHead = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java

    @JsonIgnore
    private transient MessagePayloadProcessor payloadProcessor = null;

    private boolean startPaused = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
                    + "leads to a subscription call throwing a `PulsarClientException`."
    )
    private boolean readCompacted = false;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
    private transient MessagePayloadProcessor payloadProcessor = null;

    private boolean startPaused = false;

    private boolean autoScaledReceiverQueueSizeEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
    private boolean ackReceiptEnabled = false;

    private boolean poolMessages = false;

    @JsonIgnore
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ConsumerConfigurationData.java`
#### Snippet
```java
    private transient DeadLetterPolicy deadLetterPolicy;

    private boolean retryEnable = false;

    @JsonIgnore
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "The TLS provider used by an internal client to authenticate with other Pulsar brokers."
    )
    private String sslProvider = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Whether to use TLS."
    )
    private boolean useTls = false;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "The Pulsar client dns lookup bind address, default behavior is bind on 0.0.0.0"
    )
    private String dnsLookupBindAddress = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Path to the trusted TLS certificate file."
    )
    private String tlsTrustCertsFilePath = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Path to the TLS certificate file."
    )
    private String tlsCertificateFilePath = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Path of TLS TrustStore."
    )
    private String tlsTrustStorePath = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Path to the TLS key file."
    )
    private String tlsKeyFilePath = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Set TLS using KeyStore way."
    )
    private boolean useKeyStoreTls = false;
    @ApiModelProperty(
            name = "sslProvider",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Whether to enable transaction."
    )
    private boolean enableTransaction = false;

    @JsonIgnore
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
    )
    @Secret
    private String tlsTrustStorePassword = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
    )
    @Secret
    private String tlsKeyStorePassword = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Path of TLS KeyStore."
    )
    private String tlsKeyStorePath = null;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Whether to enable BusyWait for EpollEventLoopGroup."
    )
    private boolean enableBusyWait = false;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Whether the client accepts untrusted TLS certificates from the broker."
    )
    private boolean tlsAllowInsecureConnection = false;

    @ApiModelProperty(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
            value = "Whether the hostname is validated when the proxy creates a TLS connection with brokers."
    )
    private boolean tlsHostnameVerificationEnable = false;
    @ApiModelProperty(
            name = "concurrentLookupRequest",
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/ClientConfigurationData.java`
#### Snippet
```java
                    + " default value is 0."
    )
    private int dnsLookupBindPort = 0;

    // socks5
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/SchemaDefinitionBuilderImpl.java`
#### Snippet
```java
     * If value is true, use JSR310 conversion in the Avro schema. Otherwise, use Joda time conversion.
     */
    private boolean jsr310ConversionEnabled = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/SchemaDefinitionBuilderImpl.java`
#### Snippet
```java
     * The flag of message decode whether by schema version.
     */
    private boolean supportSchemaVersioning = false;

    private SchemaReader<T> reader;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/FieldSchemaBuilderImpl.java`
#### Snippet
```java
    private SchemaType type;
    private boolean optional = false;
    private Object defaultVal = null;
    private final Map<String, String> properties = new HashMap<>();
    private String doc;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/FieldSchemaBuilderImpl.java`
#### Snippet
```java

    private SchemaType type;
    private boolean optional = false;
    private Object defaultVal = null;
    private final Map<String, String> properties = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/TimedCompletableFuture.java`
#### Snippet
```java
public class TimedCompletableFuture<T> extends CompletableFuture<T> {

    private volatile boolean hasGotResponse = false;

    public void markAsResponded() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/customroute/PartialRoundRobinMessageRouterImpl.java`
#### Snippet
```java
            AtomicIntegerFieldUpdater.newUpdater(PartialRoundRobinMessageRouterImpl.class, "partitionIndex");
    @SuppressWarnings("unused")
    private volatile int partitionIndex = 0;

    public PartialRoundRobinMessageRouterImpl(final int numPartitionsLimit) {
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionImpl.java`
#### Snippet
```java
    private CompletableFuture<Void> opFuture;

    private volatile long opCount = 0L;
    private static final AtomicLongFieldUpdater<TransactionImpl> OP_COUNT_UPDATE =
            AtomicLongFieldUpdater.newUpdater(TransactionImpl.class, "opCount");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionImpl.java`
#### Snippet
```java
        AtomicReferenceFieldUpdater.newUpdater(TransactionImpl.class, State.class, "state");

    private volatile boolean hasOpsFailed = false;
    private final Timeout timeout;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/client/api/url/DataURLStreamHandler.java`
#### Snippet
```java
     */
    static class DataURLConnection extends URLConnection {
        private boolean parsed = false;
        private String contentType;
        private byte[] data;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/KeyStoreHolder.java`
#### Snippet
```java
public class KeyStoreHolder {

    private KeyStore keyStore = null;

    public KeyStoreHolder() throws KeyStoreException {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
    private static final AtomicIntegerFieldUpdater<GrowablePriorityLongPairQueue> SIZE_UPDATER =
            AtomicIntegerFieldUpdater.newUpdater(GrowablePriorityLongPairQueue.class, "size");
    private volatile int size = 0;
    private static final long EmptyItem = -1L;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowableArrayBlockingQueue.java`
#### Snippet
```java
    private static final AtomicIntegerFieldUpdater<GrowableArrayBlockingQueue> SIZE_UPDATER = AtomicIntegerFieldUpdater
            .newUpdater(GrowableArrayBlockingQueue.class, "size");
    private volatile int size = 0;

    public GrowableArrayBlockingQueue() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java

    // caching place-holder for cpu-optimization to avoid calculating ranges again
    private volatile int cachedSize = 0;
    private volatile String cachedToString = "[]";
    private volatile boolean updatedAfterCachedForSize = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/SSLContextValidatorEngine.java`
#### Snippet
```java
    private ByteBuffer appBuffer;
    private ByteBuffer netBuffer;
    private boolean finished = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/Rate.java`
#### Snippet
```java

    // Computed stats
    private long count = 0L;
    private double rate = 0.0d;
    private double valueRate = 0.0d;
```

### RedundantFieldInitialization
Field initialization to `0.0d` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/Rate.java`
#### Snippet
```java
    private long count = 0L;
    private double rate = 0.0d;
    private double valueRate = 0.0d;
    private double averageValue = 0.0d;
    private long lastCalculatedTime = System.nanoTime();
```

### RedundantFieldInitialization
Field initialization to `0.0d` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/Rate.java`
#### Snippet
```java
    private double rate = 0.0d;
    private double valueRate = 0.0d;
    private double averageValue = 0.0d;
    private long lastCalculatedTime = System.nanoTime();

```

### RedundantFieldInitialization
Field initialization to `0.0d` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/Rate.java`
#### Snippet
```java
    // Computed stats
    private long count = 0L;
    private double rate = 0.0d;
    private double valueRate = 0.0d;
    private double averageValue = 0.0d;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
    }

    private Handle<BitSetRecyclable> recyclerHandle = null;

    private static final Recycler<BitSetRecyclable> RECYCLER = new Recycler<BitSetRecyclable>() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
     * the user knows what he's doing and try harder to preserve it.
     */
    private transient boolean sizeIsSticky = false;

    /* use serialVersionUID from JDK 1.0.2 for interoperability */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
     * The number of words in the logical size of this BitSet.
     */
    private transient int wordsInUse = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java
    private volatile long currentYoungGcCount = 0;
    private volatile long accumulatedYoungGcTime = 0;
    private volatile long currentYoungGcTime = 0;

    private volatile long accumulatedOldGcCount = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java
    private volatile long currentOldGcTime = 0;

    private static ObjectName youngGenName = null;
    private static ObjectName oldGenName = null;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java

    private volatile long accumulatedOldGcCount = 0;
    private volatile long currentOldGcCount = 0;
    private volatile long accumulatedOldGcTime = 0;
    private volatile long currentOldGcTime = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java
    private volatile long accumulatedYoungGcCount = 0;
    private volatile long currentYoungGcCount = 0;
    private volatile long accumulatedYoungGcTime = 0;
    private volatile long currentYoungGcTime = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java
    private volatile long currentYoungGcTime = 0;

    private volatile long accumulatedOldGcCount = 0;
    private volatile long currentOldGcCount = 0;
    private volatile long accumulatedOldGcTime = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java
    private volatile long accumulatedOldGcCount = 0;
    private volatile long currentOldGcCount = 0;
    private volatile long accumulatedOldGcTime = 0;
    private volatile long currentOldGcTime = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java

    private volatile long accumulatedYoungGcCount = 0;
    private volatile long currentYoungGcCount = 0;
    private volatile long accumulatedYoungGcTime = 0;
    private volatile long currentYoungGcTime = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java
public class JvmG1GCMetricsLogger implements JvmGCMetricsLogger {

    private volatile long accumulatedYoungGcCount = 0;
    private volatile long currentYoungGcCount = 0;
    private volatile long accumulatedYoungGcTime = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java

    private static ObjectName youngGenName = null;
    private static ObjectName oldGenName = null;

    static {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmG1GCMetricsLogger.java`
#### Snippet
```java
    private volatile long currentOldGcCount = 0;
    private volatile long accumulatedOldGcTime = 0;
    private volatile long currentOldGcTime = 0;

    private static ObjectName youngGenName = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmDefaultGCMetricsLogger.java`
#### Snippet
```java
    private volatile long currentFullGcCount = 0;
    private volatile long accumulatedFullGcTime = 0;
    private volatile long currentFullGcTime = 0;

    private static Object /*sun.management.HotspotRuntimeMBean*/ runtime;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmDefaultGCMetricsLogger.java`
#### Snippet
```java
    private volatile long accumulatedFullGcCount = 0;
    private volatile long currentFullGcCount = 0;
    private volatile long accumulatedFullGcTime = 0;
    private volatile long currentFullGcTime = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmDefaultGCMetricsLogger.java`
#### Snippet
```java
        volatile long currentGcCount = 0;
        volatile long accumulatedGcTime = 0;
        volatile long currentGcTime = 0;
    }

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmDefaultGCMetricsLogger.java`
#### Snippet
```java
     */
    static class GCMetrics {
        volatile long accumulatedGcCount = 0;
        volatile long currentGcCount = 0;
        volatile long accumulatedGcTime = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmDefaultGCMetricsLogger.java`
#### Snippet
```java
        volatile long accumulatedGcCount = 0;
        volatile long currentGcCount = 0;
        volatile long accumulatedGcTime = 0;
        volatile long currentGcTime = 0;
    }
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmDefaultGCMetricsLogger.java`
#### Snippet
```java
    private static final Logger log = LoggerFactory.getLogger(JvmDefaultGCMetricsLogger.class);

    private volatile long accumulatedFullGcCount = 0;
    private volatile long currentFullGcCount = 0;
    private volatile long accumulatedFullGcTime = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmDefaultGCMetricsLogger.java`
#### Snippet
```java
    static class GCMetrics {
        volatile long accumulatedGcCount = 0;
        volatile long currentGcCount = 0;
        volatile long accumulatedGcTime = 0;
        volatile long currentGcTime = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmDefaultGCMetricsLogger.java`
#### Snippet
```java

    private volatile long accumulatedFullGcCount = 0;
    private volatile long currentFullGcCount = 0;
    private volatile long accumulatedFullGcTime = 0;
    private volatile long currentFullGcTime = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String gcsManagedLedgerOffloadBucket = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String s3ManagedLedgerOffloadCredentialId = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String s3ManagedLedgerOffloadRole = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String fileSystemProfilePath = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String gcsManagedLedgerOffloadServiceAccountKeyFile = null;

    // file system config, set by service configuration
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String s3ManagedLedgerOffloadBucket = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String fileSystemURI = null;

    // --------- new offload configurations ---------
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String managedLedgerOffloadDriver = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String s3ManagedLedgerOffloadCredentialSecret = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String gcsManagedLedgerOffloadRegion = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String s3ManagedLedgerOffloadServiceEndpoint = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/OffloadPoliciesImpl.java`
#### Snippet
```java
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
    private String s3ManagedLedgerOffloadRegion = null;
    @Configuration
    @JsonProperty(access = JsonProperty.Access.READ_WRITE)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
        protected int lastChunkedMessageId = -1;
        protected MessageIdImpl[] chunkedMessageIds;
        protected long receivedTime = 0;

        static ChunkedMessageCtx get(int numChunksFromMsg, ByteBuf chunkedMsgBuffer) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
    protected ConcurrentOpenHashMap<String, ChunkedMessageCtx> chunkedMessagesMap =
            ConcurrentOpenHashMap.<String, ChunkedMessageCtx>newBuilder().build();
    private int pendingChunkedMessageCount = 0;
    protected long expireTimeOfIncompleteChunkedMessageMillis = 0;
    private final AtomicBoolean expireChunkMessageTaskScheduled = new AtomicBoolean(false);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            .newUpdater(ConsumerImpl.class, "availablePermits");
    @SuppressWarnings("unused")
    private volatile int availablePermits = 0;

    protected volatile MessageId lastDequeuedMessageId = MessageId.earliest;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            .newUpdater(ConsumerImpl.class, "subscribeDeadline");
    @SuppressWarnings("unused")
    private volatile long subscribeDeadline = 0; // gets set on first successful connection

    private final int partitionIndex;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            ConcurrentOpenHashMap.<String, ChunkedMessageCtx>newBuilder().build();
    private int pendingChunkedMessageCount = 0;
    protected long expireTimeOfIncompleteChunkedMessageMillis = 0;
    private final AtomicBoolean expireChunkMessageTaskScheduled = new AtomicBoolean(false);
    private final int maxPendingChunkedMessage;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/ClusterDataImpl.java`
#### Snippet
```java
        private boolean brokerClientTlsEnabled = false;
        private boolean tlsAllowInsecureConnection = false;
        private boolean brokerClientTlsEnabledWithKeyStore = false;
        private String brokerClientTlsTrustStoreType = "JKS";
        private String brokerClientTlsTrustStore;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/ClusterDataImpl.java`
#### Snippet
```java
        private LinkedHashSet<String> peerClusterNames;
        private boolean brokerClientTlsEnabled = false;
        private boolean tlsAllowInsecureConnection = false;
        private boolean brokerClientTlsEnabledWithKeyStore = false;
        private String brokerClientTlsTrustStoreType = "JKS";
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/ClusterDataImpl.java`
#### Snippet
```java
        private ProxyProtocol proxyProtocol;
        private LinkedHashSet<String> peerClusterNames;
        private boolean brokerClientTlsEnabled = false;
        private boolean tlsAllowInsecureConnection = false;
        private boolean brokerClientTlsEnabledWithKeyStore = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/PulsarHandler.java`
#### Snippet
```java
    private int remoteEndpointProtocolVersion = ProtocolVersion.v0.getValue();
    private final long keepAliveIntervalSeconds;
    private volatile boolean waitingForPingResponse = false;
    private ScheduledFuture<?> keepAliveTask;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicies.java`
#### Snippet
```java
public class NamespaceIsolationPolicies {

    private Map<String, NamespaceIsolationDataImpl> policies = null;

    public NamespaceIsolationPolicies() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-common/src/main/java/org/apache/pulsar/common/functions/UpdateOptionsImpl.java`
#### Snippet
```java
            value = "Whether or not to update the auth data",
            name = "update-auth-data")
    private boolean updateAuthData = false;
}

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerFactoryConfig.java`
#### Snippet
```java
     * Maximum number of (estimated) data in-flight reading from storage and the cache.
     */
    private long managedLedgerMaxReadsInFlightSize = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerFactoryConfig.java`
#### Snippet
```java
     * Whether we should make a copy of the entry payloads when inserting in cache.
     */
    private boolean copyEntriesInCache = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    @Getter
    @Setter
    private boolean cacheEvictionByMarkDeletedPosition = false;
    private int minimumBacklogCursorsForCaching = 0;
    private int minimumBacklogEntriesForCaching = 1000;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    @Setter
    private boolean cacheEvictionByMarkDeletedPosition = false;
    private int minimumBacklogCursorsForCaching = 0;
    private int minimumBacklogEntriesForCaching = 1000;
    private int maxBacklogBetweenCursorsForCaching = 1000;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    private ManagedLedgerInterceptor managedLedgerInterceptor;
    private Map<String, String> properties;
    private int inactiveLedgerRollOverTimeMs = 0;
    @Getter
    @Setter
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    private int maxEntriesPerLedger = 50000;
    private int maxSizePerLedgerMb = 100;
    private int minimumRolloverTimeMs = 0;
    private long maximumRolloverTimeMs = TimeUnit.HOURS.toMillis(4);
    private int ensembleSize = 3;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    private int metadataMaxEntriesPerLedger = 50000;
    private int ledgerRolloverTimeout = 4 * 3600;
    private double throttleMarkDelete = 0;
    private long retentionTimeMs = 0;
    private int retentionSizeInMB = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    private int ledgerRolloverTimeout = 4 * 3600;
    private double throttleMarkDelete = 0;
    private long retentionTimeMs = 0;
    private int retentionSizeInMB = 0;
    private boolean autoSkipNonRecoverableData;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    private int retentionSizeInMB = 0;
    private boolean autoSkipNonRecoverableData;
    private boolean lazyCursorRecovery = false;
    private long metadataOperationsTimeoutSeconds = 60;
    private long readEntryTimeoutSeconds = 120;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    private int maxUnackedRangesToPersist = 10000;
    private int maxBatchDeletedIndexToPersist = 10000;
    private boolean persistentUnackedRangesWithMultipleEntriesEnabled = false;
    private boolean deletionAtBatchIndexLevelEnabled = true;
    private int maxUnackedRangesToPersistInMetadataStore = 1000;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/ManagedLedgerConfig.java`
#### Snippet
```java
    private double throttleMarkDelete = 0;
    private long retentionTimeMs = 0;
    private int retentionSizeInMB = 0;
    private boolean autoSkipNonRecoverableData;
    private boolean lazyCursorRecovery = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OffloadSegmentInfoImpl.java`
#### Snippet
```java
    private volatile long endLedgerId;
    private volatile long endEntryId;
    volatile boolean closed = false;
    public final Map<String, String> driverMetadata;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpScan.java`
#### Snippet
```java

    PositionImpl searchPosition;
    Position lastSeenPosition = null;

    public OpScan(ManagedCursorImpl cursor, int batchSize,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpAddEntry.java`
#### Snippet
```java
    ByteBuf data;
    private int dataLength;
    private ManagedLedgerInterceptor.PayloadProcessorHandle payloadProcessorHandle = null;

    private static final AtomicReferenceFieldUpdater<OpAddEntry, OpAddEntry.State> STATE_UPDATER =
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java
    long min;
    long max;
    Position lastMatchedPosition = null;
    State state;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedger l = null;
            ManagedLedgerException e = null;
        }
        final Result r = new Result();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
            throws InterruptedException, ManagedLedgerException {
        class Result {
            ReadOnlyCursor c = null;
            ManagedLedgerException e = null;
        }
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
            throws InterruptedException, ManagedLedgerException {
        class Result {
            ManagedLedgerException e = null;
        }
        final Result r = new Result();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
        class Result {
            ReadOnlyCursor c = null;
            ManagedLedgerException e = null;
        }
        final Result r = new Result();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
            throws InterruptedException, ManagedLedgerException {
        class Result {
            ManagedLedger l = null;
            ManagedLedgerException e = null;
        }
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedgerInfo info = null;
            ManagedLedgerException e = null;
        }
        final Result r = new Result();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerFactoryImpl.java`
#### Snippet
```java
    public ManagedLedgerInfo getManagedLedgerInfo(String name) throws InterruptedException, ManagedLedgerException {
        class Result {
            ManagedLedgerInfo info = null;
            ManagedLedgerException e = null;
        }
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/StatsBuckets.java`
#### Snippet
```java
    private final long[] values;
    private long count = 0;
    private long sum = 0;

    public StatsBuckets(long... boundaries) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/StatsBuckets.java`
#### Snippet
```java

    private final long[] values;
    private long count = 0;
    private long sum = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
        final Map<PendingReadKey, PendingRead> ledgerCache;
        final List<ReadEntriesCallbackWithContext> callbacks = new ArrayList<>(1);
        boolean completed = false;

        public PendingRead(PendingReadKey key,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    private volatile Timeout partitionsAutoUpdateTimeout = null;
    TopicsPartitionChangedListener topicsPartitionChangedListener;
    CompletableFuture<Void> partitionsAutoUpdateFuture = null;
    private final MultiTopicConsumerStatsRecorderImpl stats;
    private UnAckedMessageTracker unAckedMessageTracker;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    AtomicInteger allTopicPartitionsNumber;

    private boolean paused = false;
    private final Object pauseMutex = new Object();
    // timeout related to auto check and subscribe partition increasement
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    private final ConsumerConfigurationData<T> internalConfig;

    private volatile BatchMessageIdImpl startMessageId = null;
    private final long startMessageRollbackDurationInSec;
    MultiTopicsConsumerImpl(PulsarClientImpl client, ConsumerConfigurationData<T> conf,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    private final Object pauseMutex = new Object();
    // timeout related to auto check and subscribe partition increasement
    private volatile Timeout partitionsAutoUpdateTimeout = null;
    TopicsPartitionChangedListener topicsPartitionChangedListener;
    CompletableFuture<Void> partitionsAutoUpdateFuture = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2BuilderImpl.java`
#### Snippet
```java
    private int lastBlockSize;
    private int lastStreamingBlockSize;
    private long streamingOffset = 0;
    private final SortedMap<Long, List<OffloadIndexEntryImpl>> entryMap = new TreeMap<>();

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BufferedOffloadStream.java`
#### Snippet
```java
    private final List<Entry> entryBuffer;
    private final InputStream blockHead;
    int offset = 0;
    static final int NOT_INITIALIZED = -1;
    int validDataOffset = NOT_INITIALIZED;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlockAwareSegmentInputStreamImpl.java`
#### Snippet
```java
    // tracking read status for both header and entries.
    // Bytes that already been read from this InputStream
    private int bytesReadOffset = 0;
    // Byte from this index is all padding byte
    private int dataBlockFullOffset;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlockAwareSegmentInputStreamImpl.java`
#### Snippet
```java
    private LedgerOffloaderStats offloaderStats;
    private String topicName;
    private int currentOffset = 0;
    private final AtomicBoolean close = new AtomicBoolean(false);

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlockAwareSegmentInputStreamImpl.java`
#### Snippet
```java
    static final int ENTRY_HEADER_SIZE = 4 /* entry size */ + 8 /* entry id */;
    // Keep a list of all entries ByteBuf, each ByteBuf contains 2 buf: entry header and entry content.
    private List<ByteBuf> entriesByteBuf = null;
    private LedgerOffloaderStats offloaderStats;
    private String topicName;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java
    private final List<DataInputStream> dataStreams;
    private final ExecutorService executor;
    private State state = null;

    enum State {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImpl.java`
#### Snippet
```java
    }

    private State state = null;

    private BlobStoreBackedReadHandleImpl(long ledgerId, OffloadIndexBlock index,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKSessionWatcher.java`
#### Snippet
```java
    private final ScheduledFuture<?> task;

    private long disconnectedAt = 0;

    public ZKSessionWatcher(ZooKeeper zk, Consumer<SessionEvent> sessionListener) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tiered-storage/file-system/src/main/java/org/apache/bookkeeper/mledger/offload/filesystem/impl/FileSystemManagedLedgerOffloader.java`
#### Snippet
```java
        private final String storageBasePath;
        private final Configuration configuration;
        volatile Exception fileSystemWriteException = null;
        private OrderedScheduler assignmentScheduler;
        private int managedLedgerOffloadPrefetchRounds = 1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdSessionWatcher.java`
#### Snippet
```java
    private final ScheduledFuture<?> task;

    private long disconnectedAt = 0;

    public EtcdSessionWatcher(Client client, long sessionTimeoutMillis,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BKCluster.java`
#### Snippet
```java
        private int numBookies = 1;
        private String dataDir;
        private int bkPort = 0;

        private boolean clearOldData;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BKCluster.java`
#### Snippet
```java

    @Getter
    boolean isAutoRecoveryEnabled = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        StatsLogger statsLogger = NullStatsLogger.INSTANCE;
        int retryExecThreadCount = DEFAULT_RETRY_EXECUTOR_THREAD_COUNT;
        double requestRateLimit = 0;
        boolean allowReadOnlyMode = false;

```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        int attempts = 0;
        long startTimeNanos;
        long elapsedTimeMs = 0L;
        final RetryPolicy retryPolicy;
        final OpStatsLogger statsLogger;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        String connectString = null;
        int sessionTimeoutMs = 10000;
        Set<Watcher> watchers = null;
        RetryPolicy connectRetryPolicy = null;
        RetryPolicy operationRetryPolicy = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        Set<Watcher> watchers = null;
        RetryPolicy connectRetryPolicy = null;
        RetryPolicy operationRetryPolicy = null;
        StatsLogger statsLogger = NullStatsLogger.INSTANCE;
        int retryExecThreadCount = DEFAULT_RETRY_EXECUTOR_THREAD_COUNT;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
    @Slf4j
    static final class ZooWorker {
        int attempts = 0;
        long startTimeNanos;
        long elapsedTimeMs = 0L;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        int sessionTimeoutMs = 10000;
        Set<Watcher> watchers = null;
        RetryPolicy connectRetryPolicy = null;
        RetryPolicy operationRetryPolicy = null;
        StatsLogger statsLogger = NullStatsLogger.INSTANCE;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
     */
    public static class Builder {
        String connectString = null;
        int sessionTimeoutMs = 10000;
        Set<Watcher> watchers = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
        int retryExecThreadCount = DEFAULT_RETRY_EXECUTOR_THREAD_COUNT;
        double requestRateLimit = 0;
        boolean allowReadOnlyMode = false;

        private Builder() {}
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
    private String curL1Nodes = "";
    private boolean iteratorDone = false;
    private LedgerManager.LedgerRange nextRange = null;

    public LegacyHierarchicalLedgerRangeIterator(MetadataStore store, String ledgersRoot) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java

    private Iterator<String> l1NodesIter = null;
    private Iterator<String> l2NodesIter = null;
    private String curL1Nodes = "";
    private boolean iteratorDone = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
    private Iterator<String> l2NodesIter = null;
    private String curL1Nodes = "";
    private boolean iteratorDone = false;
    private LedgerManager.LedgerRange nextRange = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
    private final String ledgersRoot;

    private Iterator<String> l1NodesIter = null;
    private Iterator<String> l2NodesIter = null;
    private String curL1Nodes = "";
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
    String streamingDataBlockKey;
    String streamingDataIndexKey;
    MultipartUpload streamingMpu = null;
    List<MultipartPart> streamingParts = Lists.newArrayList();

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/ResourceLockImpl.java`
#### Snippet
```java
    private long version;
    private final CompletableFuture<Void> expiredFuture;
    private boolean revalidateAfterReconnection = false;
    private CompletableFuture<Void> revalidateFuture;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionWorkerStarter.java`
#### Snippet
```java

        @Parameter(names = {"-h", "--help"}, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionWorkerStarter.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionAssignmentTailer.java`
#### Snippet
```java
    private Reader<byte[]> reader;
    private volatile boolean isRunning = false;
    private volatile boolean exitOnEndOfTopic = false;
    private CompletableFuture<Void> exitFuture;
    private Thread tailerThread;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionAssignmentTailer.java`
#### Snippet
```java

    @Getter
    private MessageId lastMessageId = null;

    public FunctionAssignmentTailer(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionAssignmentTailer.java`
#### Snippet
```java
    private final ErrorNotifier errorNotifier;
    private Reader<byte[]> reader;
    private volatile boolean isRunning = false;
    private volatile boolean exitOnEndOfTopic = false;
    private CompletableFuture<Void> exitFuture;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
    private MembershipManager membershipManager;
    private SchedulerManager schedulerManager;
    private volatile boolean isInitialized = false;
    private ScheduledExecutorService statsUpdater;
    private AuthenticationService authenticationService;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java
    private final WorkerConfig workerConfig;
    private final PulsarClient pulsarClient;
    private boolean isLeader = false;

    static final String COORDINATION_TOPIC_SUBSCRIPTION = "participants";
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/dlog/DLInputStream.java`
#### Snippet
```java
  private final DistributedLogManager dlm;
  private LogReader reader;
  private boolean eos = false;

  // Cache the input stream for a log record.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/dlog/DLInputStream.java`
#### Snippet
```java
public class DLInputStream extends InputStream {

  private LogRecordWithInputStream currentLogRecord = null;
  private final DistributedLogManager dlm;
  private LogReader reader;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
    private PulsarWorkerService workerService;

    boolean isInitializePhase = false;

    @Getter
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/SchedulerManager.java`
#### Snippet
```java

    @Getter
    private MessageId lastMessageProduced = null;

    private MessageId metadataTopicLastMessage = MessageId.earliest;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/SchedulerManager.java`
#### Snippet
```java
    private final Lock schedulerLock = new ReentrantLock(true);

    private volatile boolean isRunning = false;

    AtomicBoolean isCompactionNeeded = new AtomicBoolean(false);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

    @Parameter(names = "--web_serviceurl", description = "Pulsar Web Service Url", required = false)
    public String webServiceUrl = null;

    @Parameter(names = "--expose_pulsaradmin", description = "Whether the pulsar admin client "
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
    private RuntimeSpawner runtimeSpawner;
    private ThreadRuntimeFactory containerFactory;
    private Long lastHealthCheckTs = null;
    private HTTPServer metricsServer;
    private ScheduledFuture healthCheckTimer;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedgerException exception = null;
            Position position = null;
        }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    protected static final AtomicReferenceFieldUpdater<ManagedCursorImpl, State> STATE_UPDATER =
        AtomicReferenceFieldUpdater.newUpdater(ManagedCursorImpl.class, State.class, "state");
    protected volatile State state = null;

    protected final ManagedCursorMXBean mbean;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        AtomicIntegerFieldUpdater.newUpdater(ManagedCursorImpl.class, "pendingMarkDeletedSubmittedCount");
    @SuppressWarnings("unused")
    private volatile int pendingMarkDeletedSubmittedCount = 0;
    private volatile long lastLedgerSwitchTimestamp;
    private final Clock clock;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    public void close() throws InterruptedException, ManagedLedgerException {
        class Result {
            ManagedLedgerException exception = null;
        }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            ManagedLedgerException exception = null;
            List<Entry> entries = null;
        }
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    public void clearBacklog() throws InterruptedException, ManagedLedgerException {
        class Result {
            ManagedLedgerException exception = null;
        }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        AtomicReferenceFieldUpdater.newUpdater(ManagedCursorImpl.class, OpReadEntry.class, "waitingReadOp");
    @SuppressWarnings("unused")
    private volatile OpReadEntry waitingReadOp = null;

    public static final int FALSE = 0;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    // defined before the lambda cannot be mutated
    private static class InvidualDeletedMessagesHandlingState {
        long totalEntriesToSkip = 0L;
        long deletedMessages = 0L;
        PositionImpl startPosition;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedgerException exception = null;
            List<Entry> entries = null;
        }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    public void resetCursor(Position newPos) throws ManagedLedgerException, InterruptedException {
        class Result {
            ManagedLedgerException exception = null;
        }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            ManagedLedgerException exception = null;
            List<Entry> entries = null;
        }
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    private static class InvidualDeletedMessagesHandlingState {
        long totalEntriesToSkip = 0L;
        long deletedMessages = 0L;
        PositionImpl startPosition;
        PositionImpl endPosition;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedgerException exception = null;
            List<Entry> entries = null;
        }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    private int individualDeletedMessagesSerializedSize;
    private static final String COMPACTION_CURSOR_NAME = "__compaction";
    private volatile boolean cacheReadEntry = false;

    class MarkDeleteEntry {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            throws InterruptedException, ManagedLedgerException {
        class Result {
            ManagedLedgerException exception = null;
        }

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        AtomicIntegerFieldUpdater.newUpdater(ManagedCursorImpl.class, "pendingReadOps");
    @SuppressWarnings("unused")
    private volatile int pendingReadOps = 0;

    private static final AtomicLongFieldUpdater<ManagedCursorImpl> MSG_CONSUMED_COUNTER_UPDATER =
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            ManagedLedgerException exception = null;
            Position position = null;
        }
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            ManagedLedgerException exception = null;
            List<Entry> entries = null;
        }
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

        class Result {
            ManagedLedgerException exception = null;
        }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            ManagedLedgerException exception = null;
            Entry entry = null;
        }
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedgerException exception = null;
            List<Entry> entries = null;
        }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java

        class Result {
            ManagedLedgerException exception = null;
        }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedgerException exception = null;
            Entry entry = null;
        }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    private volatile boolean isDirty = false;

    private boolean alwaysInactive = false;

    private static final long NO_MAX_SIZE_LIMIT = -1L;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    // The cursor is considered "dirty" when there are mark-delete updates that are only applied in memory,
    // because of the rate limiting.
    private volatile boolean isDirty = false;

    private boolean alwaysInactive = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceConfig.java`
#### Snippet
```java
    // Whether the pulsar admin client exposed to function context, default is disabled.
    @Getter
    private boolean exposePulsarAdminClientEnabled = false;
    private int metricsPort;
    private List<String> additionalJavaRuntimeArguments = Collections.emptyList();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String tlsTrustStorePassword = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            doc = "Enable the enforcement of limits on the incoming HTTP requests"
        )
    private boolean httpRequestsLimitEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            doc = "Whether the '/metrics' endpoint should return default prometheus metrics. Defaults to false."
    )
    private boolean includeStandardPrometheusMetrics = false;
    @FieldContext(
        category = CATEGORY_WORKER,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @Deprecated
    private boolean tlsEnabled = false;
    @FieldContext(
        category = CATEGORY_WORKER_SECURITY,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
        doc = "Enforce authentication"
    )
    private boolean authenticationEnabled = false;
    @FieldContext(
        category = CATEGORY_WORKER_SECURITY,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
        doc = "Require trusted client cert on connect"
    )
    private boolean tlsRequireTrustedClientCertOnConnect = false;
    @FieldContext(
        category = CATEGORY_CLIENT_SECURITY,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            doc = "Flag indicates enabling or disabling function worker using unified PackageManagement service."
    )
    private boolean  functionsWorkerEnablePackageManagement = false;
    @FieldContext(
        category = CATEGORY_FUNC_RUNTIME_MNG,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            doc = "Whether to enable the broker client authentication used by function workers to talk to brokers"
    )
    private Boolean brokerClientAuthenticationEnabled = null;
    public boolean isBrokerClientAuthenticationEnabled() {
        if (brokerClientAuthenticationEnabled != null) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    // @deprecated use "pulsar+ssl://" in serviceUrl to enable
    @Deprecated
    private boolean useTls = false;
    @FieldContext(
        category = CATEGORY_SECURITY,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
        doc = "Enforce authorization on accessing functions admin-api"
    )
    private boolean authorizationEnabled = false;
    @FieldContext(
            category = CATEGORY_WORKER_SECURITY,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            doc = "TLS TrustStore path in function worker"
    )
    private String tlsTrustStore = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String tlsKeyStorePassword = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                    + " that have the granularity set must be the same multiples of their granularities."
    )
    private boolean functionInstanceResourceChangeInLockStep = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            doc = "Enable TLS with KeyStore type configuration in function worker"
    )
    private boolean tlsEnabledWithKeyStore = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
        doc = "Whether to enable hostname verification on TLS connections"
    )
    private boolean tlsEnableHostnameVerification = false;
    @FieldContext(
            category = CATEGORY_SECURITY,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            doc = "TLS KeyStore path in function worker"
    )
    private String tlsKeyStore = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                    + "When using TLS authentication with KeyStore, available values can be SunJSSE, Conscrypt and etc."
    )
    private String tlsProvider = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
        doc = "Accept untrusted TLS certificate from client"
    )
    private boolean tlsAllowInsecureConnection = false;
    @FieldContext(
        category = CATEGORY_WORKER_SECURITY,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
            doc = "Enable to expose Pulsar Admin Client from Function Context, default is disabled"
    )
    private boolean exposeAdminClientEnabled = false;

    public static WorkerConfig load(String yamlFile) throws IOException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java
  }

  volatile boolean discoverInProgress = false;
  private synchronized void triggerDiscover(String discoveredEvent) {

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java
  private String intermediateTopicName;
  private volatile Exception currentError = null;
  private volatile boolean isRunning = false;
  private ExecutorService discoveryThread;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java
  private BatchSource<T> batchSource;
  private String intermediateTopicName;
  private volatile Exception currentError = null;
  private volatile boolean isRunning = false;
  private ExecutorService discoveryThread;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    private Class<?> sinkTypeArg;
    private final AtomicReference<Schema<?>> sinkSchema = new AtomicReference<>();
    private SinkSchemaInfoProvider sinkSchemaInfoProvider = null;

    public JavaInstanceRunnable(InstanceConfig instanceConfig,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    // a flog to determine if member variables have been initialized as part of setup().
    // used for out of band API calls like operations involving stats
    private transient boolean isInitialized = false;

    // a read write lock for stats operations
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/InitializableFunction.java`
#### Snippet
```java
public class InitializableFunction extends TypedMessageBuilderPublish {

    private boolean initialized = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private long lastOffloadLedgerId = 0;
    private volatile long lastOffloadSuccessTimestamp = 0;
    private volatile long lastOffloadFailureTimestamp = 0;

    private int minBacklogCursorsForCaching = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedgerException status = null;
            Position position = null;
        }
        final Result result = new Result();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    // last read-operation's callback to check read-timeout on it.
    private volatile ReadEntryCallbackWrapper lastReadCallback = null;
    private static final AtomicReferenceFieldUpdater<ManagedLedgerImpl, ReadEntryCallbackWrapper>
            LAST_READ_CALLBACK_UPDATER = AtomicReferenceFieldUpdater
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            .newUpdater(ManagedLedgerImpl.class, "totalSize");
    @SuppressWarnings("unused")
    private volatile long totalSize = 0;

    // Cursors that are waiting to be notified when new entries are persisted
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private long currentLedgerSize = 0;
    private volatile long lastLedgerCreatedTimestamp = 0;
    private volatile long lastLedgerCreationFailureTimestamp = 0;
    private long lastLedgerCreationInitiationTimestamp = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            .newUpdater(ManagedLedgerImpl.class, "numberOfEntries");
    @SuppressWarnings("unused")
    private volatile long numberOfEntries = 0;
    static final AtomicLongFieldUpdater<ManagedLedgerImpl> TOTAL_SIZE_UPDATER = AtomicLongFieldUpdater
            .newUpdater(ManagedLedgerImpl.class, "totalSize");
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        final CountDownLatch latch = new CountDownLatch(1);
        class Result {
            ManagedLedgerException exception = null;
        }
        final Result result = new Result();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private static final AtomicLongFieldUpdater<ManagedLedgerImpl> READ_OP_COUNT_UPDATER = AtomicLongFieldUpdater
            .newUpdater(ManagedLedgerImpl.class, "readOpCount");
    private volatile long readOpCount = 0;
    protected static final AtomicLongFieldUpdater<ManagedLedgerImpl> ADD_OP_COUNT_UPDATER = AtomicLongFieldUpdater
            .newUpdater(ManagedLedgerImpl.class, "addOpCount");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            AtomicReferenceFieldUpdater.newUpdater(ManagedLedgerImpl.class, State.class, "state");
    protected volatile State state = null;
    private volatile boolean migrated = false;

    @Getter
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            ManagedLedgerException exception = null;
        }
        final Result result = new Result();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private long lastLedgerCreationInitiationTimestamp = 0;

    private long lastOffloadLedgerId = 0;
    private volatile long lastOffloadSuccessTimestamp = 0;
    private volatile long lastOffloadFailureTimestamp = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        class Result {
            ManagedCursor cursor = null;
            ManagedLedgerException exception = null;
        }
        final Result result = new Result();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            ManagedCursor cursor = null;
            ManagedLedgerException exception = null;
        }
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private volatile long lastLedgerCreatedTimestamp = 0;
    private volatile long lastLedgerCreationFailureTimestamp = 0;
    private long lastLedgerCreationInitiationTimestamp = 0;

    private long lastOffloadLedgerId = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        class Result {
            ManagedLedgerException status = null;
            Position position = null;
        }
        final Result result = new Result();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    private long lastOffloadLedgerId = 0;
    private volatile long lastOffloadSuccessTimestamp = 0;
    private volatile long lastOffloadFailureTimestamp = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    private volatile long lastAddEntryTimeMs = 0;
    private long inactiveLedgerRollOverTimeMs = 0;

    protected static final int DEFAULT_LEDGER_DELETE_RETRIES = 3;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    protected static final AtomicLongFieldUpdater<ManagedLedgerImpl> ADD_OP_COUNT_UPDATER = AtomicLongFieldUpdater
            .newUpdater(ManagedLedgerImpl.class, "addOpCount");
    private volatile long addOpCount = 0;

    // last read-operation's callback to check read-timeout on it.
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private ManagedLedgerInterceptor managedLedgerInterceptor;

    private volatile long lastAddEntryTimeMs = 0;
    private long inactiveLedgerRollOverTimeMs = 0;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        // position
        class Result {
            ManagedLedgerException status = null;
            Position position = null;
        }
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private volatile LedgerHandle currentLedger;
    private long currentLedgerEntries = 0;
    private long currentLedgerSize = 0;
    private volatile long lastLedgerCreatedTimestamp = 0;
    private volatile long lastLedgerCreationFailureTimestamp = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        // position
        class Result {
            ManagedLedgerException status = null;
            Position position = null;
        }
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private static final AtomicReferenceFieldUpdater<ManagedLedgerImpl, State> STATE_UPDATER =
            AtomicReferenceFieldUpdater.newUpdater(ManagedLedgerImpl.class, State.class, "state");
    protected volatile State state = null;
    private volatile boolean migrated = false;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private volatile long lastOffloadFailureTimestamp = 0;

    private int minBacklogCursorsForCaching = 0;
    private int minBacklogEntriesForCaching = 1000;
    private int maxBacklogBetweenCursorsForCaching = 1000;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            ManagedLedgerException exception = null;
        }
        final Result result = new Result();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            .completedFuture(PositionImpl.LATEST);
    private volatile LedgerHandle currentLedger;
    private long currentLedgerEntries = 0;
    private long currentLedgerSize = 0;
    private volatile long lastLedgerCreatedTimestamp = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        class Result {
            Position lastPosition = null;
            ManagedLedgerException exception = null;
        }
        final Result result = new Result();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            .newUpdater(ManagedLedgerImpl.class, "entriesAddedCounter");
    @SuppressWarnings("unused")
    private volatile long entriesAddedCounter = 0;

    static final AtomicLongFieldUpdater<ManagedLedgerImpl> NUMBER_OF_ENTRIES_UPDATER = AtomicLongFieldUpdater
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        final CountDownLatch counter = new CountDownLatch(1);
        class Result {
            Position lastPosition = null;
            ManagedLedgerException exception = null;
        }
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    private long currentLedgerEntries = 0;
    private long currentLedgerSize = 0;
    private volatile long lastLedgerCreatedTimestamp = 0;
    private volatile long lastLedgerCreationFailureTimestamp = 0;
    private long lastLedgerCreationInitiationTimestamp = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ProducerHandler.java`
#### Snippet
```java
    private final LongAdder numBytesSent;
    private final StatsBuckets publishLatencyStatsUSec;
    private volatile long msgPublishedCounter = 0;
    private static final AtomicLongFieldUpdater<ProducerHandler> MSG_PUBLISHED_COUNTER_UPDATER =
            AtomicLongFieldUpdater.newUpdater(ProducerHandler.class, "msgPublishedCounter");
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
    protected String tlsTrustCertFilePath;
    @Parameter(names = "--instanceIdOffset", description = "Start the instanceIds from this offset", hidden = true)
    protected int instanceIdOffset = 0;
    @Parameter(names = "--runtime", description = "Function runtime to use (Thread/Process)", hidden = true,
            converter = RuntimeConverter.class)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
    protected String brokerServiceUrl;
    @Parameter(names = "--webServiceUrl", description = "The URL for the Pulsar web service", hidden = true)
    protected String webServiceUrl = null;
    @Parameter(names = "--clientAuthPlugin", description = "Client authentication plugin using which "
            + "function-process can connect to broker", hidden = true)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ReaderHandler.java`
#### Snippet
```java
    private final LongAdder numMsgsDelivered;
    private final LongAdder numBytesDelivered;
    private volatile long msgDeliveredCounter = 0;
    private static final AtomicLongFieldUpdater<ReaderHandler> MSG_DELIVERED_COUNTER_UPDATER =
            AtomicLongFieldUpdater.newUpdater(ReaderHandler.class, "msgDeliveredCounter");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketServiceStarter.java`
#### Snippet
```java

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
        private boolean generateDocs = false;
    }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketServiceStarter.java`
#### Snippet
```java

        @Parameter(names = {"-h", "--help"}, description = "Show this help message")
        private boolean help = false;

        @Parameter(names = {"-g", "--generate-docs"}, description = "Generate docs")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/stats/StatsBuckets.java`
#### Snippet
```java
    private final long[] values;
    private long count = 0;
    private long sum = 0;

    public StatsBuckets(List<Long> boundaries) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/stats/StatsBuckets.java`
#### Snippet
```java

    private final long[] values;
    private long count = 0;
    private long sum = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ConsumerHandler.java`
#### Snippet
```java
    private Consumer<byte[]> consumer;

    private int maxPendingMessages = 0;
    private final AtomicInteger pendingMessages = new AtomicInteger();
    private final boolean pullMode;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ConsumerHandler.java`
#### Snippet
```java
    private final LongAdder numBytesDelivered;
    private final LongAdder numMsgsAcked;
    private volatile long msgDeliveredCounter = 0;
    private static final AtomicLongFieldUpdater<ConsumerHandler> MSG_DELIVERED_COUNTER_UPDATER =
            AtomicLongFieldUpdater.newUpdater(ConsumerHandler.class, "msgDeliveredCounter");
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ConsumerHandler.java`
#### Snippet
```java
public class ConsumerHandler extends AbstractWebSocketHandler {

    private String subscription = null;
    private SubscriptionType subscriptionType;
    private SubscriptionMode subscriptionMode;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/KeySharedPolicy.java`
#### Snippet
```java
    protected KeySharedMode keySharedMode;

    protected boolean allowOutOfOrderDelivery = false;

    public static final int DEFAULT_HASH_RANGE_SIZE = 2 << 15;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java
    @FieldContext(doc = "Specify whether client certificates are required for "
            + "TLS rejecting the connection if the client certificate is not trusted")
    private boolean tlsRequireTrustedClientCertOnConnect = false;

    @FieldContext(doc = "TLS cert refresh duration (in seconds). 0 means checking every new connection.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java

    @FieldContext(doc = "When this parameter is not empty, unauthenticated users perform as anonymousUserRole")
    private String anonymousUserRole = null;

    /* --- TLS --- */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java
            doc = "TLS KeyStore path in WebSocket"
    )
    private String tlsKeyStore = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String tlsKeyStorePassword = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java
            doc = "Enable TLS with KeyStore type configuration for WebSocket"
    )
    private boolean tlsEnabledWithKeyStore = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String tlsTrustStorePassword = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java
            doc = "TLS TrustStore path in WebSocket"
    )
    private String tlsTrustStore = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java

    @FieldContext(doc = "Accept untrusted TLS certificate from client")
    private boolean tlsAllowInsecureConnection = false;

    @FieldContext(doc = "Specify whether client certificates are required for "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java

    @FieldContext(doc = "Enable TLS of broker client")
    private boolean brokerClientTlsEnabled = false;

    @FieldContext(doc = "Path for the TLS certificate file")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java
    /* --- TLS --- */
    @Deprecated
    private boolean tlsEnabled = false;

    @FieldContext(doc = "Enable TLS of broker client")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketProxyConfiguration.java`
#### Snippet
```java
            + "(wildcard matching only applicable if wildcard-char: "
            + "presents at first or last position. For example: *.pulsar.service,pulsar.service.*)")
    private boolean authorizationAllowWildcardsMatching = false;

    @FieldContext(doc = "Proxy authentication settings used to connect to brokers")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java

    @Parameter(names = { "--listener-name" }, description = "Listener name for the broker.")
    public String listenerName = null;

    @Parameter(names = {"-lt", "--num-listener-threads"}, description = "Set the number of threads"
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java

    @Parameter(names = { "--proxy-protocol" }, description = "Proxy protocol to select type of routing at proxy.")
    ProxyProtocol proxyProtocol = null;

    public abstract void fillArgumentsFromProperties(Properties prop);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java

    @Parameter(names = { "--proxy-url" }, description = "Proxy-server URL to which to connect.")
    String proxyServiceURL = null;

    @Parameter(names = { "--proxy-protocol" }, description = "Proxy protocol to select type of routing at proxy.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java
    @Parameter(names = {
            "--tls-enable-hostname-verification" }, description = "Enable TLS hostname verification")
    public Boolean tlsHostnameVerificationEnable = null;

    @Parameter(names = { "-c",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java

    @Parameter(names = {"-bw", "--busy-wait"}, description = "Enable Busy-Wait on the Pulsar client")
    public boolean enableBusyWait = false;

    @Parameter(names = { "--listener-name" }, description = "Listener name for the broker.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java
            "--stats-interval-seconds" },
            description = "Statistics Interval Seconds. If 0, statistics will be disabled")
    public long statsIntervalSeconds = 0;

    @Parameter(names = {"-ioThreads", "--num-io-threads"}, description = "Set the number of threads to be "
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java
    @Parameter(names = {
            "--tls-allow-insecure" }, description = "Allow insecure TLS connection")
    public Boolean tlsAllowInsecureConnection = null;

    @Parameter(names = {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
        @Parameter(names = { "-time",
                "--test-duration" }, description = "Test duration in secs. If <= 0, it will keep publishing")
        public long testTime = 0;
    }

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
                "--num-messages" }, description = "Number of messages to publish in total. If <= 0, it will keep"
                + " publishing")
        public long numMessages = 0;

        @Parameter(names = { "-f", "--payload-file" }, description = "Use payload from a file instead of empty buffer")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
                description = "Format %i as a message index in the stream from producer and/or %t as the timestamp"
                        + " nanoseconds")
        public boolean formatPayload = false;

        @Parameter(names = {"-fc", "--format-class"}, description = "Custom Formatter class name")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java

        @Parameter(names = { "-f", "--payload-file" }, description = "Use payload from a file instead of empty buffer")
        public String payloadFilename = null;

        @Parameter(names = { "-e", "--payload-delimiter" },
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
    private static final LongAdder totalMessagesSent = new LongAdder();
    private static final LongAdder totalBytesSent = new LongAdder();
    private static IMessageFormatter messageFormatter = null;
    private JCommander jc;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
                "--num-messages" },
                description = "Number of messages to publish in total. If <= 0, it will keep publishing")
        public long numMessages = 0;

        @Parameter(names = { "-e", "--ensemble-size" }, description = "Ledger ensemble size")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
        @Parameter(names = { "-time",
                "--test-duration" }, description = "Test duration in secs. If <= 0, it will keep publishing")
        public long testTime = 0;

    }
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
                "--num-messages"},
                description = "Number of messages to consume in total. If <= 0, it will keep consuming")
        public long numMessages = 0;

        @Parameter(names = { "--auth_plugin" }, description = "Authentication plugin class name", hidden = true)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        @Parameter(names = {"-ntxn"}, description = "The number of opened transactions, 0 means keeping open."
                + "(After --txn-enable setting to true, -ntxn takes effect.)")
        public long totalNumTxn = 0;

        @Parameter(names = {"-abort"}, description = "Abort the transaction. (After --txn-enable "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        @Parameter(names = { "-ac",
                "--auto_ack_chunk_q_full" }, description = "Auto ack for oldest message on queue is full")
        private boolean autoAckOldestChunkedMessageOnQueueFull = false;

        @Parameter(names = { "-e",
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
                "--encryption-key-value-file" },
                description = "The file which contains the private key to decrypt payload")
        public String encKeyFile = null;

        @Parameter(names = { "-time",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java

        @Parameter(names = { "--replicated" }, description = "Whether the subscription status should be replicated")
        public boolean replicatedSubscription = false;

        @Parameter(names = { "--acks-delay-millis" }, description = "Acknowledgements grouping delay in millis")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java

        @Parameter(names = { "-r", "--rate" }, description = "Simulate a slow message consumer (rate in msg/s)")
        public double rate = 0;

        @Parameter(names = { "-q", "--receiver-queue-size" }, description = "Size of the receiver queue")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java

        @Parameter(names = { "-mc", "--max_chunked_msg" }, description = "Max pending chunk messages")
        private int maxPendingChunkedMessage = 0;

        @Parameter(names = { "-ac",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        @Parameter(names = {"-aq", "--auto-scaled-receiver-queue-size"},
                description = "Enable autoScaledReceiverQueueSize")
        public boolean autoScaledReceiverQueueSize = false;

        @Parameter(names = { "--replicated" }, description = "Whether the subscription status should be replicated")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java

        @Parameter(names = { "--histogram-file" }, description = "HdrHistogram output file")
        public String histogramFile = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java

        @Parameter(names = {"--batch-index-ack" }, description = "Enable or disable the batch index acknowledgment")
        public boolean batchIndexAck = false;

        @Parameter(names = { "-pm", "--pool-messages" }, description = "Use the pooled message", arity = 1)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java

        @Parameter(names = {"-txn", "--txn-enable"}, description = "Enable or disable the transaction")
        public boolean isEnableTransaction = false;

        @Parameter(names = {"-ntxn"}, description = "The number of opened transactions, 0 means keeping open."
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        @Parameter(names = {"-abort"}, description = "Abort the transaction. (After --txn-enable "
                + "setting to true, -abort takes effect)")
        public boolean isAbortTransaction = false;

        @Parameter(names = { "--histogram-file" }, description = "HdrHistogram output file")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
                "--expire_time_incomplete_chunked_messages" },
                description = "Expire time in ms for incomplete chunk messages")
        private long expireTimeOfIncompleteChunkedMessageMs = 0;

        @Parameter(names = { "-v",
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
        @Parameter(names = { "-time",
                "--test-duration" }, description = "Test duration in secs. If <= 0, it will keep consuming")
        public long testTime = 0;

        @Parameter(names = {"--batch-index-ack" }, description = "Enable or disable the batch index acknowledgment")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/BrokerMonitor.java`
#### Snippet
```java

        @Parameter(names = { "--connect-string" }, description = "Zookeeper connect string", required = true)
        public String connectString = null;
    }

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/utils/FixedColumnLengthTableMaker.java`
#### Snippet
```java
     * Format String to apply to decimal entries. If set to null, no special formatting is applied.
     */
    public String decimalFormatter = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/utils/FixedColumnLengthTableMaker.java`
#### Snippet
```java
     * If not null, lengthFunction should give the length for the given column index.
     */
    public Function<Integer, Integer> lengthFunction = null;

    // Helper function to add top and bottom borders.
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/utils/FixedColumnLengthTableMaker.java`
#### Snippet
```java
     * The amount of spacing to pad left of an element with.
     */
    public int leftPadding = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java

        @Parameter(names = { "-r", "--rate" }, description = "Simulate a slow message reader (rate in msg/s)")
        public double rate = 0;

        @Parameter(names = { "-m",
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
                "--num-messages"}, description = "Number of messages to consume in total. If <= 0, "
                + "it will keep consuming")
        public long numMessages = 0;

        @Parameter(names = {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
        @Parameter(names = { "-time",
                "--test-duration" }, description = "Test duration in secs. If <= 0, it will keep consuming")
        public long testTime = 0;

        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/TransactionMetadataStoreState.java`
#### Snippet
```java

    @SuppressWarnings("unused")
    private volatile State state = null;

    public TransactionMetadataStoreState(State state) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/TxnLogBufferedWriterConfig.java`
#### Snippet
```java
    private int batchedWriteMaxSize = 1024 * 1024 * 4;
    private int batchedWriteMaxDelayInMillis = 1;
    private boolean batchEnabled = false;
}

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
        @Parameter(names = { "--separation" }, description = "Separation time in ms for trade_group actions "
                + "(0 for no separation)")
        int separation = 0;

        @Parameter(names = { "--size" }, description = "Message size in bytes")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java

        @Parameter(names = {"--txn-disable"}, description = "Disable transaction")
        public boolean isDisableTransaction = false;

        @Parameter(names = {"-abort"}, description = "Abort the transaction. (After --txn-disEnable "
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
        @Parameter(names = {"-time",
                "--test-duration"}, description = "Test duration (in second). 0 means keeping publishing")
        public long testTime = 0;

        @Parameter(names = {"-ss",
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
                "--partitions"}, description = "Create partitioned topics with a given number of partitions, 0 means"
                + "not trying to create a topic")
        public Integer partitions = null;

        @Parameter(names = {"-time",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
        @Parameter(names = {"-abort"}, description = "Abort the transaction. (After --txn-disEnable "
                + "setting to false, -abort takes effect)")
        public boolean isAbortTransaction = false;

        @Parameter(names = "-txnRate", description = "Set the rate of opened transaction or task. 0 means no limit")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
                + "If transaction disabled, it means the number of tasks. The task or transaction produces or "
                + "consumes a specified number of messages.")
        public long numTransactions = 0;

        @Parameter(names = {"-nmp", "--numMessage-perTransaction-produce"},
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java

        @Parameter(names = "-txnRate", description = "Set the rate of opened transaction or task. 0 means no limit")
        public int openTxnRate = 0;

        @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionLogImpl.java`
#### Snippet
```java
        public void readEntriesComplete(List<Entry> entries, Object ctx) {
            entryQueue.fill(new MessagePassingQueue.Supplier<Entry>() {
                private int i = 0;
                @Override
                public Entry get() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = {"-mk", "--message-key-generation-mode"}, description = "The generation mode of message key"
                + ", valid options are: [autoIncrement, random]")
        public String messageKeyGenerationMode = null;

        @Parameter(names = { "-am", "--access-mode" }, description = "Producer access mode")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                "--encryption-key-value-file" },
                description = "The file which contains the public key to encrypt payload")
        public String encKeyFile = null;

        @Parameter(names = { "-d",
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = { "-f", "--payload-file" }, description = "Use payload from an UTF-8 encoded text file and "
                + "a payload will be randomly selected when publishing messages")
        public String payloadFilename = null;

        @Parameter(names = { "-e", "--payload-delimiter" }, description = "The delimiter used to split lines when "
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
    private static final LongAdder numTxnOpSuccess = new LongAdder();

    private static IMessageFormatter messageFormatter = null;

    @Parameters(commandDescription = "Test pulsar producer performance.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = { "-set",
                "--set-event-time" }, description = "Set the eventTime on messages")
        public boolean setEventTime = false;

        @Parameter(names = { "-ef",
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java

        @Parameter(names = { "--histogram-file" }, description = "HdrHistogram output file")
        public String histogramFile = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                description = "Format %i as a message index in the stream from producer and/or %t as the timestamp"
                        + " nanoseconds.")
        public boolean formatPayload = false;

        @Parameter(names = {"-fc", "--format-class"}, description = "Custom Formatter class name")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java

        @Parameter(names = {"-txn", "--txn-enable"}, description = "Enable or disable the transaction")
        public boolean isEnableTransaction = false;

        @Parameter(names = {"-abort"}, description = "Abort the transaction. (After --txn-enable "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = { "-ef",
                "--exit-on-failure" }, description = "Exit from the process on publish failure (default: disable)")
        public boolean exitOnFailure = false;

        @Parameter(names = {"-mk", "--message-key-generation-mode"}, description = "The generation mode of message key"
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = { "-np", "--partitions" }, description = "Create partitioned topics with the given number "
                + "of partitions, set 0 to not try to create the topic")
        public Integer partitions = null;

        @Parameter(names = { "-m",
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                "--num-messages" }, description = "Number of messages to publish in total. If <= 0, it will keep "
                + "publishing")
        public long numMessages = 0;

        @Parameter(names = { "-z", "--compression" }, description = "Compress messages payload")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
                "--chunking" }, description = "Should split the message and publish in chunks if message size is "
                + "larger than allowed max size")
        private boolean chunkingAllowed = false;

        @Parameter(names = { "-o", "--max-outstanding" }, description = "Max number of outstanding messages")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java

        @Parameter(names = { "-pn", "--producer-name" }, description = "Producer Name")
        public String producerName = null;

        @Parameter(names = { "-au", "--admin-url" }, description = "Pulsar Admin URL")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = { "-d",
                "--delay" }, description = "Mark messages with a given delay in seconds")
        public long delay = 0;

        @Parameter(names = { "-set",
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java

        @Parameter(names = { "-k", "--encryption-key-name" }, description = "The public key name to encrypt payload")
        public String encKeyName = null;

        @Parameter(names = { "-v",
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = { "-time",
                "--test-duration" }, description = "Test duration in secs. If <= 0, it will keep publishing")
        public long testTime = 0;

        @Parameter(names = "--warmup-time", description = "Warm-up time in seconds (Default: 1 sec)")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = {"--send-timeout"}, description = "Set the sendTimeout value default 0 to keep "
                + "compatibility with previous version of pulsar-perf")
        public int sendTimeout = 0;

        @Parameter(names = { "-pn", "--producer-name" }, description = "Producer Name")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
        @Parameter(names = {"-abort"}, description = "Abort the transaction. (After --txn-enable "
                + "setting to true, -abort takes effect)")
        public boolean isAbortTransaction = false;

        @Parameter(names = { "--histogram-file" }, description = "HdrHistogram output file")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/PulsarAdminBuilderImpl.java`
#### Snippet
```java
    protected ClientConfigurationData conf;

    private ClassLoader clientBuilderClassLoader = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdProxyStats.java`
#### Snippet
```java
    private class CmdConnectionMetrics extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdProxyStats.java`
#### Snippet
```java
    private class CmdTopicsMetrics extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTransactions.java`
#### Snippet
```java

        @Parameter(names = { "-m", "--metadata" }, description = "Flag to include ledger metadata")
        private boolean metadata = false;
        @Override
        void run() throws Exception {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTransactions.java`
#### Snippet
```java

        @Parameter(names = { "-m", "--metadata" }, description = "Flag to include ledger metadata")
        private boolean metadata = false;
        @Override
        void run() throws Exception {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java

        @Parameter(names = { "--auth-plugin" }, description = "Authentication plugin class name.")
        String authPluginClassName = null;

        @Parameter(names = { "--request-timeout" }, description = "Request time out in seconds for "
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java
                        + "of method `configure` in authentication plugin class, for example \"key1:val1,key2:val2\" "
                        + "or \"{\"key1\":\"val1\",\"key2\":\"val2\"}.")
        String authParams = null;

        @Parameter(names = { "--tls-allow-insecure" }, description = "Allow TLS insecure connection")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java

        @Parameter(names = { "--admin-url" }, description = "Admin Service URL to which to connect.")
        String serviceUrl = null;

        @Parameter(names = { "--auth-plugin" }, description = "Authentication plugin class name.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java

        @Parameter(names = {"-a", "--all-version"}, description = "all version", required = false)
        private boolean all = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
        @Parameter(names = { "-n", "--dry-run"},
                   description = "dost not apply to schema registry, just prints the post schema payload")
        private boolean dryRun = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
                + "all resources (including metastore and ledger), otherwise only do a mark deletion"
                + " and not remove any resources indeed")
        private boolean force = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java
        @Parameter(names = { "--bandwidthOut",
                "-bo" }, description = "expected outbound bandwidth (bytes/second)", required = true)
        private long bandwidthOut = 0;

        @Parameter(names = { "--memory", "-mem" }, description = "expected memory usage (Mbytes)", required = true)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java
        @Parameter(names = { "--msgRateOut",
                "-mo" }, description = "expected outgoing messages per second", required = true)
        private long msgRateOut = 0;

        @Parameter(names = { "--bandwidthIn",
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java

        @Parameter(names = { "--memory", "-mem" }, description = "expected memory usage (Mbytes)", required = true)
        private long memory = 0;

        @Parameter(names = { "--dynamic",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java
        @Parameter(names = { "--dynamic",
                "-d" }, description = "dynamic (allow to be dynamically re-calculated) or not")
        private boolean dynamic = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java
        @Parameter(names = { "--bandwidthIn",
                "-bi" }, description = "expected inbound bandwidth (bytes/second)", required = true)
        private long bandwidthIn = 0;

        @Parameter(names = { "--bandwidthOut",
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java
        @Parameter(names = { "--msgRateIn",
                "-mi" }, description = "expected incoming messages per second", required = true)
        private long msgRateIn = 0;

        @Parameter(names = { "--msgRateOut",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
    private class CmdDumpMBeans extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
    private class CmdMonitoringMetrics extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBrokerStats.java`
#### Snippet
```java
    private class CmdTopics extends CliCommand {
        @Parameter(names = { "-i", "--indent" }, description = "Indent JSON output", required = false)
        private boolean indent = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--global", "-g"}, description = "Whether to set this policy globally.")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. ")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Parameter(names = {"--subscription", "-s"},
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"-ap", "--applied"}, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. ")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies", arity = 0)
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--global", "-g"}, description = "Whether to set this policy globally.")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                description = "Time limit in second (or minutes, hours, days, weeks eg: 100m, 3h, 2d, 5w), "
                        + "non-positive number for disabling time limit.")
        private String limitTimeStr = null;

        @Parameter(names = { "-p", "--policy" }, description = "Retention policy to enforce when the limit is reached. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                "-rp"}, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))")
        private boolean relativeToPublishRate = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Parameter(names = {"--subscription", "-s"},
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
                + "If set to true, broker returns global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
                + "If set to true, broker returns global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))", required = false)
        private boolean relativeToPublishRate = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                + "If set to true, the removing operation is replicated to other clusters asynchronously"
                + "If set to false or not set, the topic retention policy is replicated to local clusters.")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously", arity = 0)
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                + "If set to true, the broker returns global topic policies"
                + "If set to false or not set, the broker returns local topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable deduplication")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable deduplication")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--global", "-g"}, description = "Whether to set this policy globally.")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--global", "-g"}, description = "Whether to set this policy globally.")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                + "If set to true, the policy is replicated to other clusters asynchronously, "
                + "If set to false or not set, the topic retention policy is replicated to local clusters.")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))")
        private boolean relativeToPublishRate = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;
        @Override
        void run() throws PulsarAdminException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Parameter(names = {"--subscription", "-s"},
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--applied", "-a"}, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
                + "If set to true, broker returns global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable delayed delivery messages")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable delayed delivery messages")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. ")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"-ap", "--applied"}, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;
        @Override
        void run() throws PulsarAdminException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable deduplication")
        private boolean disable = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
                + "If set to true, the removing operation will be replicate to other clusters asynchronously"
                , arity = 0)
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable delayed delivery messages")
        private boolean disable = false;

        @Parameter(names = { "--time", "-t" }, description = "The tick time for when retrying on "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. ")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
                + "If set to true, broker returns global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "--enable-delete-while-inactive", "-e" }, description = "Enable delete while inactive")
        private boolean enableDeleteWhileInactive = false;

        @Parameter(names = { "--disable-delete-while-inactive", "-d" }, description = "Disable delete while inactive")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--enable", "-e"}, description = "Enable allowAutoSubscriptionCreation on topic")
        private boolean enable = false;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
                + "If set to true, broker returned global topic policies")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. ")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. "
                + "If set to true, the policy will be replicate to other clusters asynchronously")
        private boolean isGlobal = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "--disable-delete-while-inactive", "-d" }, description = "Disable delete while inactive")
        private boolean disableDeleteWhileInactive = false;

        @Parameter(names = {"--max-inactive-duration", "-t"},
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;
        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
                + "If set to true, broker returned global topic policies")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        @Parameter(names = { "-a", "--all" },
                description = "Delete all data (tenants) of the cluster", required = false)
        private boolean deleteAll = false;

        void run() throws PulsarAdminException {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTenants.java`
#### Snippet
```java
        @Parameter(names = { "-f",
                "--force" }, description = "Delete a tenant forcefully by deleting all namespaces under it.")
        private boolean force = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        @Parameter(names = { "-w", "--wait-complete" },
                   description = "Wait for compaction to complete", required = false)
        private boolean wait = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(names = { "-gpb", "--get-precise-backlog" }, description = "Set true to get precise backlog")
        private boolean getPreciseBacklog = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(names = "--per-partition", description = "Get per partition stats")
        private boolean perPartition = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        @Parameter(names = { "-f",
            "--force" }, description = "Disconnect and close all consumers and delete subscription forcefully")
        private boolean force = false;

        @Parameter(names = { "-s", "--subscription" }, description = "Subscription to be deleted", required = true)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        @Parameter(names = { "-m",
        "--metadata" }, description = "Flag to include ledger metadata")
        private boolean metadata = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        @Parameter(names = "--force",
                description = "Close all producer/consumer/replicator and delete topic forcefully")
        private boolean force = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        @Parameter(names = "--force",
                description = "Close all producer/consumer/replicator and delete topic forcefully")
        private boolean force = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
    public static class RootParams {
        @Parameter(names = { "--url" }, description = "Broker URL to which to connect.")
        String serviceURL = null;

        @Parameter(names = { "--proxy-url" }, description = "Proxy-server URL to which to connect.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java

        @Parameter(names = { "--listener-name" }, description = "Listener name for the broker.")
        String listenerName = null;

        @Parameter(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java

        @Parameter(names = { "--proxy-protocol" }, description = "Proxy protocol to select type of routing at proxy.")
        ProxyProtocol proxyProtocol = null;

        @Parameter(names = { "--auth-plugin" }, description = "Authentication plugin class name.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
                    + "of method `configure` in authentication plugin class, for example \"key1:val1,key2:val2\" "
                    + "or \"{\"key1\":\"val1\",\"key2\":\"val2\"}.")
        String authParams = null;

        @Parameter(names = { "-v", "--version" }, description = "Get version of pulsar client")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java

        @Parameter(names = { "--auth-plugin" }, description = "Authentication plugin class name.")
        String authPluginClassName = null;

        @Parameter(names = { "--listener-name" }, description = "Listener name for the broker.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java

        @Parameter(names = { "--proxy-url" }, description = "Proxy-server URL to which to connect.")
        String proxyServiceURL = null;

        @Parameter(names = { "--proxy-protocol" }, description = "Proxy protocol to select type of routing at proxy.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

    @Parameter(names = { "-mc", "--max_chunked_msg" }, description = "Max pending chunk messages")
    private int maxPendingChunkedMessage = 0;

    @Parameter(names = { "-ac",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

    @Parameter(names = { "--regex" }, description = "Indicate the topic name is a regex pattern")
    private boolean isRegex = false;

    @Parameter(names = {"-q", "--queue-size"}, description = "Consumer receiver queue size.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
    @Parameter(names = { "-r", "--rate" }, description = "Rate (in msg/sec) at which to consume, "
            + "value 0 means to consume messages as fast as possible.")
    private double consumeRate = 0;

    @Parameter(names = { "--regex" }, description = "Indicate the topic name is a regex pattern")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

    @Parameter(names = { "--hide-content" }, description = "Do not write the message to console.")
    private boolean hideContent = false;

    @Parameter(names = { "-r", "--rate" }, description = "Rate (in msg/sec) at which to consume, "
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

    @Parameter(names = {"-q", "--queue-size"}, description = "Consumer receiver queue size.")
    private int receiverQueueSize = 0;

    @Parameter(names = { "-mc", "--max_chunked_msg" }, description = "Max pending chunk messages")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

    @Parameter(names = { "--hex" }, description = "Display binary messages in hex.")
    private boolean displayHex = false;

    @Parameter(names = { "--hide-content" }, description = "Do not write the message to console.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
    @Parameter(names = { "-ac",
            "--auto_ack_chunk_q_full" }, description = "Auto ack for oldest message on queue is full")
    private boolean autoAckOldestChunkedMessageOnQueueFull = false;

    @Parameter(names = { "-ekv",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
            "--chunking" }, description = "Should split the message and publish in chunks if message size is "
            + "larger than allowed max size")
    private boolean chunkingAllowed = false;

    @Parameter(names = { "-s", "--separator" },
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
            "--encryption-key-value" }, description = "The URI of public key to encrypt payload, for example "
                    + "file:///path/to/public.key or data:application/x-pem-file;base64,*****")
    private String encKeyValue = null;

    @Parameter(names = { "-dr",
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
    @Parameter(names = { "-kvet", "--key-value-encoding-type"},
            description = "Key Value Encoding Type (it can be separated or inline)")
    private String keyValueEncodingType = null;

    @Parameter(names = { "-ekn", "--encryption-key-name" }, description = "The public key name to encrypt payload")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java

    @Parameter(names = { "-db", "--disable-batching" }, description = "Disable batch sending of messages")
    private boolean disableBatching = false;

    @Parameter(names = { "-c",
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
               description = "Rate (in msg/sec) at which to produce,"
                       + " value 0 means to produce messages as fast as possible.")
    private double publishRate = 0;

    @Parameter(names = { "-db", "--disable-batching" }, description = "Disable batch sending of messages")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
    @Parameter(names = { "-dr",
            "--disable-replication" }, description = "Disable geo-replication for messages.")
    private boolean disableReplication = false;

    private ClientBuilder clientBuilder;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java

    @Parameter(names = { "-ekn", "--encryption-key-name" }, description = "The public key name to encrypt payload")
    private String encKeyName = null;

    @Parameter(names = { "-ekv",
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
        @Parameter(names = "--hostname_verification_enabled",
                description = "Enable hostname verification", hidden = true)
        protected Boolean deprecatedTlsHostNameVerificationEnabled = null;
        @Parameter(names = "--hostname-verification-enabled", description = "Enable hostname verification"
                + " #Java, Python")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
        @Parameter(names = "--autoAck",
                description = "Whether or not the framework acknowledges messages automatically", hidden = true)
        protected Boolean deprecatedAutoAck = null;
        @Parameter(names = "--auto-ack",
                description = "Whether or not the framework acknowledges messages automatically"
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
        protected String brokerServiceUrl;
        @Parameter(names = "--web-service-url", description = "The URL for Pulsar web service #Java, Python")
        protected String webServiceUrl = null;
        // for backwards compatibility purposes
        @Parameter(names = "--clientAuthPlugin", description = "Client authentication plugin using "
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java

        @Parameter(names = { "-s", "--state" }, description = "The FunctionState that needs to be put", required = true)
        private String state = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
        // for backwards compatibility purposes
        @Parameter(names = "--tls_allow_insecure", description = "Allow insecure tls connection", hidden = true)
        protected Boolean deprecatedTlsAllowInsecureConnection = null;
        @Parameter(names = "--tls-allow-insecure", description = "Allow insecure tls connection #Java, Python")
        protected boolean tlsAllowInsecureConnection;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
        @Parameter(names = { "-w", "--watch" }, description = "Watch for changes in the value associated with a key "
                + "for a Pulsar Function")
        private boolean watch = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
        // for backwards compatibility purposes
        @Parameter(names = "--instanceIdOffset", description = "Start the instanceIds from this offset", hidden = true)
        protected Integer deprecatedInstanceIdOffset = null;
        @Parameter(names = "--instance-id-offset", description = "Start the instanceIds from this offset #Java, Python")
        protected Integer instanceIdOffset = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java

        @Parameter(names = { "-k", "--key" }, description = "Key name of State")
        private String key = null;

        @Parameter(names = { "-w", "--watch" }, description = "Watch for changes in the value associated with a key "
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
        // for backwards compatibility purposes
        @Parameter(names = "--use_tls", description = "Use tls connection", hidden = true)
        protected Boolean deprecatedUseTls = null;
        @Parameter(names = "--use-tls", description = "Use tls connection #Java, Python")
        protected boolean useTls;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java
    public static final String METADATA_STORE_INSTANCE = "METADATA_STORE_INSTANCE";

    private MetadataCache<BookiesRackConfiguration> bookieMappingCache = null;
    private ITopologyAwareEnsemblePlacementPolicy<BookieNode> rackawarePolicy = null;
    private List<BookieId> bookieAddressListLastTime = new ArrayList<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/BookieRackAffinityMapping.java`
#### Snippet
```java

    private MetadataCache<BookiesRackConfiguration> bookieMappingCache = null;
    private ITopologyAwareEnsemblePlacementPolicy<BookieNode> rackawarePolicy = null;
    private List<BookieId> bookieAddressListLastTime = new ArrayList<>();

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))", required = false)
        private boolean relativeToPublishRate = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--disable-delete-while-inactive", "-d" }, description = "Disable delete while inactive")
        private boolean disableDeleteWhileInactive = false;

        @Parameter(names = {"--max-inactive-duration", "-t"}, description = "Max duration of topic inactivity in "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable allowAutoTopicCreation on namespace")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable allowAutoTopicCreation on namespace")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable schema validation enforced")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable schema validation enforced")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                description = "Time limit in second (or minutes, hours, days, weeks eg: 100m, 3h, 2d, 5w), "
                        + "non-positive number for disabling time limit.")
        private String limitTimeStr = null;

        @Parameter(names = { "-p", "--policy" }, description = "Retention policy to enforce when the limit is reached. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable deduplication")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable deduplication")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--bundles", "-b" }, description = "number of bundles to activate", required = false)
        private int numBundles = 0;

        private static final long MAX_BUNDLES = ((long) 1) << 32;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable delayed delivery messages")
        private boolean disable = false;

        @Parameter(names = { "--time", "-t" }, description = "The tick time for when retrying on "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable deduplication")
        private boolean disable = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable schema validation enforced")
        private boolean disable = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                   description = "Compatibility level required for new schemas created via a Producer. "
                                 + "Possible values (Full, Backward, Forward).")
        private String strategyParam = null;

        @Parameter(names = { "--disabled", "-d" }, description = "Disable automatic schema updates")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--num-partitions", "-n" }, description = "Default number of partitions of topic to "
                + "be auto-created, applicable to partitioned topics only", required = false)
        private Integer defaultNumPartitions = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "-f",
                "--force" }, description = "Delete namespace forcefully by force deleting all topics under it")
        private boolean force = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                        + "ALWAYS_INCOMPATIBLE,"
                        + "ALWAYS_COMPATIBLE).")
        private String strategyParam = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--disabled", "-d" }, description = "Disable automatic schema updates")
        private boolean disabled = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable schema validation enforced")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable schema validation enforced")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable message encryption required")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable message encryption required")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable allowAutoSubscriptionCreation on namespace")
        private boolean enable = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable delayed delivery messages")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable delayed delivery messages")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable message encryption required")
        private boolean disable = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))", required = false)
        private boolean relativeToPublishRate = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the namespace")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable schema validation enforced")
        private boolean disable = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable allowAutoTopicCreation on namespace")
        private boolean disable = false;

        @Parameter(names = { "--type", "-t" }, description = "Type of topic to be auto-created. "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(names = { "--enable-delete-while-inactive", "-e" }, description = "Enable delete while inactive")
        private boolean enableDeleteWhileInactive = false;

        @Parameter(names = { "--disable-delete-while-inactive", "-d" }, description = "Disable delete while inactive")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsServlet.java`
#### Snippet
```java
    protected List<PrometheusRawMetricsProvider> metricsProviders;

    private ExecutorService executor = null;

    public PrometheusMetricsServlet(long metricsServletTimeoutMs, String cluster) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable delayed delivery messages")
        private boolean disable = false;

        @Parameter(names = { "--time", "-t" },
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-e", "--exclude-reset-position" },
                description = "Exclude the reset position, start consume messages from the next position.")
        private boolean excludeResetPosition = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-gpb",
            "--get-precise-backlog" }, description = "Set true to get precise backlog")
        private boolean getPreciseBacklog = false;

        @Parameter(names = { "-sbs",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-s",
                                "--sort-by-broker" }, description = "Sort partitioned-topic by Broker Url")
        protected boolean sortByBroker = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-w", "--wait-complete" },
                   description = "Wait for offloading to complete", required = false)
        private boolean wait = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-gpb",
            "--get-precise-backlog" }, description = "Set true to get precise backlog")
        private boolean getPreciseBacklog = false;

        @Parameter(names = { "-sbs",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-etb",
                "--get-earliest-time-in-backlog" }, description = "Set true to get earliest time in backlog")
        private boolean getEarliestTimeInBacklog = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                "--get-subscription-backlog-size" }, description = "Set true to get backlog size for each subscription"
        + ", locking required.")
        private boolean subscriptionBacklogSize = false;

        @Parameter(names = { "-etb",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"-ap", "--applied"}, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-w", "--wait-complete" },
                   description = "Wait for compaction to complete", required = false)
        private boolean wait = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-e", "--exclude-reset-position" },
                description = "Exclude the reset position, start consume messages from the next position.")
        private boolean excludeResetPosition = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-f",
                "--force" }, description = "Close all producer/consumer/replicator and delete topic forcefully")
        private boolean force = false;

        @Parameter(names = {"-d", "--deleteSchema"}, description = "Delete schema while deleting topic, "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable delayed delivery messages")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable delayed delivery messages")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"-ap", "--applied"}, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--enable-delete-while-inactive", "-e" }, description = "Enable delete while inactive")
        private boolean enableDeleteWhileInactive = false;

        @Parameter(names = { "--disable-delete-while-inactive", "-d" }, description = "Disable delete while inactive")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable replication")
        private boolean disable = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable deduplication")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable deduplication")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = {"-d", "--deleteSchema"}, description = "Delete schema while deleting topic, "
                + "but the parameter is invalid and the schema is always deleted", hidden = true)
        private boolean deleteSchema = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = {"-d", "--deleteSchema"}, description = "Delete schema while deleting topic, "
                + "but the parameter is invalid and the schema is always deleted", hidden = true)
        private boolean deleteSchema = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))", required = false)
        private boolean relativeToPublishRate = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable schema validation enforced")
        private boolean enable = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-m",
        "--metadata" }, description = "Flag to include ledger metadata")
        private boolean metadata = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))")
        private boolean relativeToPublishRate = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = "--per-partition", description = "Get per partition stats")
        private boolean perPartition = false;

        @Parameter(names = { "-gpb",
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                description = "Time limit in second (or minutes, hours, days, weeks eg: 100m, 3h, 2d, 5w), "
                        + "non-positive number for disabling time limit.")
        private String limitTimeStr = null;

        @Parameter(names = { "-p", "--policy" },
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--disable-delete-while-inactive", "-d" }, description = "Disable delete while inactive")
        private boolean disableDeleteWhileInactive = false;

        @Parameter(names = {"--max-inactive-duration", "-t"}, description = "Max duration of topic inactivity "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--enable", "-e" }, description = "Enable replication")
        private boolean enable = false;

        @Parameter(names = { "--disable", "-d" }, description = "Disable replication")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                "--get-subscription-backlog-size" }, description = "Set true to get backlog size for each subscription"
                + ", locking required.")
        private boolean subscriptionBacklogSize = false;

        @Parameter(names = { "-etb",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-r", "--replicated" }, description = "replicated subscriptions", required = false)
        private boolean replicated = false;

        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "--disable", "-d" }, description = "Disable deduplication")
        private boolean disable = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-t", "--expireTime" }, description = "Expire messages older than time in seconds "
                + "(or minutes, hours, days, weeks eg: 100m, 3h, 2d, 5w)")
        private String expireTimeStr = null;

        @Parameter(names = { "--position",
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
        private boolean applied = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-f",
            "--force" }, description = "Disconnect and close all consumers and delete subscription forcefully")
        private boolean force = false;

        @Parameter(names = { "-s", "--subscription" }, description = "Subscription to be deleted", required = true)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-f",
                "--force" }, description = "Close all producer/consumer/replicator and delete topic forcefully")
        private boolean force = false;

        @Parameter(names = {"-d", "--deleteSchema"}, description = "Delete schema while deleting topic, "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                "-rp" }, description = "dispatch rate relative to publish-rate (if publish-relative flag is enabled "
                + "then broker will apply throttling value to (publish-rate + dispatch rate))")
        private boolean relativeToPublishRate = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = { "-etb",
                "--get-earliest-time-in-backlog" }, description = "Set true to get earliest time in backlog")
        private boolean getEarliestTimeInBacklog = false;

        @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/limiter/ConnectionController.java`
#### Snippet
```java
        private static final Map<String, MutableInt> CONNECTIONS = new HashMap<>();
        private static final ReentrantLock lock = new ReentrantLock();
        private static int totalConnectionNum = 0;

        private final int maxConnections;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
            final CommandsInfo finalCommandsInfo = commandsInfo;
            commandReader = new CommandReader() {
                private int index = 0;

                @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/EventResourcesImpl.java`
#### Snippet
```java
public class EventResourcesImpl implements EventResources {
    private final EventResourcesImpl parent;
    private List<Object> resources = null;

    public EventResourcesImpl(EventResourcesImpl parent) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java
    private Level level = Level.INFO;
    private Throwable throwable = null;
    private Instant startTime = null;
    private final EventResourcesImpl resources;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java
    private final String id;
    private final Clock clock;
    private String traceId = null;
    private String parentId = null;
    private List<Object> attributes = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java
    private List<Object> attributes = null;
    private Level level = Level.INFO;
    private Throwable throwable = null;
    private Instant startTime = null;
    private final EventResourcesImpl resources;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java
    private final Clock clock;
    private String traceId = null;
    private String parentId = null;
    private List<Object> attributes = null;
    private Level level = Level.INFO;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java
    private String traceId = null;
    private String parentId = null;
    private List<Object> attributes = null;
    private Level level = Level.INFO;
    private Throwable throwable = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_producers_per_topic = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_consumers_per_topic = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    @SuppressWarnings("checkstyle:MemberName")
    public EntryFilters entryFilters = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    @SuppressWarnings("checkstyle:MemberName")
    public Long compaction_threshold = null;
    @SuppressWarnings("checkstyle:MemberName")
    public long offload_threshold = -1;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Map<String, DispatchRateImpl> replicatorDispatchRate = new HashMap<>();
    public Map<String, SubscribeRate> clusterSubscribeRate = new HashMap<>();
    public PersistencePolicies persistence = null;

    // If set, it will override the broker settings for enabling deduplication
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Map<String, Integer> latency_stats_sample_rate = new HashMap<>();
    @SuppressWarnings("checkstyle:MemberName")
    public Integer message_ttl_in_seconds = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer subscription_expiration_time_minutes = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    @SuppressWarnings("checkstyle:MemberName")
    public boolean schema_validation_enforced = false;

    @SuppressWarnings("checkstyle:MemberName")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public OffloadPolicies offload_policies = null;

    public Integer deduplicationSnapshotIntervalSeconds = null;

    @SuppressWarnings("checkstyle:MemberName")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    @SuppressWarnings("checkstyle:MemberName")
    public OffloadPolicies offload_policies = null;

    public Integer deduplicationSnapshotIntervalSeconds = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Integer subscription_expiration_time_minutes = null;
    @SuppressWarnings("checkstyle:MemberName")
    public RetentionPolicies retention_policies = null;
    public boolean deleted = false;
    public static final String FIRST_BOUNDARY = "0x00000000";
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public DelayedDeliveryPolicies delayed_delivery_policies = null;
    @SuppressWarnings("checkstyle:MemberName")
    public InactiveTopicPolicies inactive_topic_policies = null;
    @SuppressWarnings("checkstyle:MemberName")
    public SubscriptionAuthMode subscription_auth_mode = SubscriptionAuthMode.None;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    @SuppressWarnings("checkstyle:MemberName")
    public boolean encryption_required = false;
    @SuppressWarnings("checkstyle:MemberName")
    public DelayedDeliveryPolicies delayed_delivery_policies = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Integer max_unacked_messages_per_subscription = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_subscriptions_per_topic = null;

    @SuppressWarnings("checkstyle:MemberName")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    @SuppressWarnings("checkstyle:MemberName")
    public RetentionPolicies retention_policies = null;
    public boolean deleted = false;
    public static final String FIRST_BOUNDARY = "0x00000000";
    public static final String LAST_BOUNDARY = "0xffffffff";
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Integer max_consumers_per_subscription = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_unacked_messages_per_consumer = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_unacked_messages_per_subscription = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Integer max_producers_per_topic = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_consumers_per_topic = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_consumers_per_subscription = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public long offload_threshold_in_seconds = -1;
    @SuppressWarnings("checkstyle:MemberName")
    public Long offload_deletion_lag_ms = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_topics_per_namespace = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Integer max_unacked_messages_per_consumer = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_unacked_messages_per_subscription = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_subscriptions_per_topic = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    // If set, it will override the broker settings for enabling deduplication
    public Boolean deduplicationEnabled = null;
    // If set, it will override the broker settings for allowing auto topic creation
    public AutoTopicCreationOverride autoTopicCreationOverride = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Integer message_ttl_in_seconds = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer subscription_expiration_time_minutes = null;
    @SuppressWarnings("checkstyle:MemberName")
    public RetentionPolicies retention_policies = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    @SuppressWarnings("checkstyle:MemberName")
    public String resource_group_name = null;

    public enum BundleType {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    @SuppressWarnings("checkstyle:MemberName")
    @Deprecated
    public SchemaAutoUpdateCompatibilityStrategy schema_auto_update_compatibility_strategy = null;

    @SuppressWarnings("checkstyle:MemberName")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public boolean encryption_required = false;
    @SuppressWarnings("checkstyle:MemberName")
    public DelayedDeliveryPolicies delayed_delivery_policies = null;
    @SuppressWarnings("checkstyle:MemberName")
    public InactiveTopicPolicies inactive_topic_policies = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public AutoTopicCreationOverride autoTopicCreationOverride = null;
    // If set, it will override the broker settings for allowing auto subscription creation
    public AutoSubscriptionCreationOverride autoSubscriptionCreationOverride = null;
    public Map<String, PublishRate> publishMaxMessageRate = new HashMap<>();

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Integer max_consumers_per_topic = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_consumers_per_subscription = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_unacked_messages_per_consumer = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Boolean deduplicationEnabled = null;
    // If set, it will override the broker settings for allowing auto topic creation
    public AutoTopicCreationOverride autoTopicCreationOverride = null;
    // If set, it will override the broker settings for allowing auto subscription creation
    public AutoSubscriptionCreationOverride autoSubscriptionCreationOverride = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Long offload_deletion_lag_ms = null;
    @SuppressWarnings("checkstyle:MemberName")
    public Integer max_topics_per_namespace = null;

    @SuppressWarnings("checkstyle:MemberName")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java

    @SuppressWarnings("checkstyle:MemberName")
    public Boolean is_allow_auto_update_schema = null;

    @SuppressWarnings("checkstyle:MemberName")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/impl/DispatchRateImpl.java`
#### Snippet
```java
        private int dispatchThrottlingRateInMsg = -1;
        private long dispatchThrottlingRateInByte = -1;
        private boolean relativeToPublishRate = false; /* throttles dispatch relatively publish-rate */
        private int ratePeriodInSecond = 1; /* by default dispatch-rate will be calculate per 1 second */

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/impl/BundlesDataImpl.java`
#### Snippet
```java
    public static class BundlesDataImplBuilder implements BundlesData.Builder {
        private List<String> boundaries;
        private int numBundles = 0;

        public BundlesDataImplBuilder boundaries(List<String> boundaries) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-tools-api/src/main/java/org/apache/pulsar/admin/cli/extensions/ParameterDescriptor.java`
#### Snippet
```java
    private ParameterType type = ParameterType.STRING;
    @Builder.Default
    private  boolean required = false;
}

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-auth-athenz/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderAthenz.java`
#### Snippet
```java
    private static final String SYS_PROP_ALLOWED_OFFSET = "pulsar.athenz.role.token_allowed_offset";

    private List<String> domainNameList = null;
    private int allowedOffset = 30;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ValidatorImpls.java`
#### Snippet
```java
        }

        private HashSet<String> acceptedValues = null;

        public StringValidator(Map<String, Object> params) {
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-package-management/bookkeeper-storage/src/main/java/org/apache/pulsar/packages/management/storage/bookkeeper/DLOutputStream.java`
#### Snippet
```java
    private final DistributedLogManager distributedLogManager;
    private final AsyncLogWriter writer;
    private long offset = 0L;

    private DLOutputStream(DistributedLogManager distributedLogManager, AsyncLogWriter writer) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
    private Map<String, String> configuration;
    private String loginContextName;
    private String serverType = null;

    public AuthenticationSasl() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
    // this is a static object that shares amongst client.
    private static JAASCredentialsContainer jaasCredentialsContainer;
    private static volatile boolean initializedJAAS = false;

    private Map<String, String> configuration;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
    }

    private String saslRoleToken = null;
    private Client client = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java

    private String saslRoleToken = null;
    private Client client = null;

    // role token exists but expired return true
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-auth-athenz/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationAthenz.java`
#### Snippet
```java
    private PrivateKey privateKey;
    private String keyId = "0";
    private String roleHeader = null;
    // If auto prefetching is enabled, application will not complete until the static method
    // ZTSClient.cancelPrefetch() is called.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-client-auth-athenz/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationAthenz.java`
#### Snippet
```java
    private static final String APPLICATION_X_PEM_FILE = "application/x-pem-file";

    private transient ZTSClient ztsClient = null;
    private String ztsUrl;
    private String tenantDomain;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-client-auth-athenz/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationAthenz.java`
#### Snippet
```java
    // ZTSClient.cancelPrefetch() is called.
    // cf. https://github.com/AthenZ/athenz/issues/544
    private boolean autoPrefetchEnabled = false;
    private volatile long cachedRoleTokenTimestamp;
    private String roleToken;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "If true, export managed cursor metrics"
    )
    private boolean exposeManagedCursorMetricsInPrometheus = false;
    @FieldContext(
            category = CATEGORY_METRICS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            category = CATEGORY_SERVER,
            doc = "Enable or disable exposing broker entry metadata to client.")
    private boolean exposingBrokerEntryMetadataToClientEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(category = CATEGORY_STORAGE_BK, doc = "Enable/disable disk weight based placement. Default is false")
    private boolean bookkeeperDiskWeightBasedPlacementEnabled = false;

    @FieldContext(category = CATEGORY_STORAGE_BK, doc = "Set the interval to check the need for sending an explicit "
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_STORAGE_BK,
        doc = "Enable/disable reordering read sequence on reading entries")
    private boolean bookkeeperClientReorderReadSequenceEnabled = false;
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "'authenticationEnabled' must also be set for this to take effect."
    )
    private boolean authenticateMetricsEndpoint = false;
    @FieldContext(
        category = CATEGORY_METRICS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Whether internal client use KeyStore type to authenticate with other Pulsar brokers"
    )
    private boolean brokerClientTlsEnabledWithKeyStore = false;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " back and unack count reaches to `limit/2`. Using a value of 0, is disabling unackedMessage-limit"
            + " check and broker doesn't block dispatchers")
    private int maxUnackedMessagesPerBroker = 0;
    @FieldContext(
        category = CATEGORY_POLICIES,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " 0 means retention is disabled. -1 means data is not removed by size quota"
    )
    private int defaultRetentionSizeInMB = 0;
    @FieldContext(
        category = CATEGORY_SERVER,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "If true, export buffered metrics"
    )
    private boolean metricsBufferResponse = false;
    @FieldContext(
        category = CATEGORY_METRICS,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Default number of message-bytes dispatching throttling-limit for every replicator in replication. \n\n"
            + "Using a value of 0, is disabling replication message-byte dispatch-throttling")
    private long dispatchThrottlingRatePerReplicatorInByte = 0;
    @FieldContext(
            dynamic = true,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "the broker with the highest load will be triggered to unload"
    )
    private boolean lowerBoundarySheddingEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "clusters on different cloud platforms."
    )
    private String metadataSyncEventTopic = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + "in subsequent release."
    )
    private boolean streamingDispatch = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " connected consumers with the same IP address decrease."
            + " Using a value of 0, is disabling maxSameAddressConsumersPerTopic-limit check.")
    private int maxSameAddressConsumersPerTopic = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Allow forced deletion of tenants. Default is false."
    )
    private boolean forceDeleteTenantAllowed = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
         doc = "Precise dispatcher flow control according to history message number of each entry"
    )
    private boolean preciseDispatcherFlowControl = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "The maximum number of connections in the broker. If it exceeds, new connections are rejected."
    )
    private int brokerMaxConnections = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + " Using a value of 0, is disabling maxTopicsPerNamespace-limit check."
    )
    private int maxTopicsPerNamespace = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "The default is to evict through readPosition."
    )
    private boolean cacheEvictionByMarkDeletedPosition = false;

    /**** --- Transaction config variables. --- ****/
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Driver to use to offload old data to long term storage"
    )
    private String managedLedgerOffloadDriver = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String tlsTrustStorePassword = null;

    /**** --- Config variables used for internal client/admin to auth with other broker. --- ****/
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Enable transaction coordinator in broker"
    )
    private boolean transactionCoordinatorEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Default number of message dispatching throttling-limit for every replicator in replication. \n\n"
            + "Using a value of 0, is disabling replication message dispatch-throttling")
    private int dispatchThrottlingRatePerReplicatorInMsg = 0;
    @FieldContext(
        dynamic = true,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + " Using a value of 0, is disabling maxSubscriptionsPerTopic limit check."
    )
    private int maxSubscriptionsPerTopic = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + "Disable rollover with value 0 (Default value 0)"
        )
    private int managedLedgerInactiveLedgerRolloverTimeSeconds = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "use only brokers running the latest software version (to minimize impact to bundles)"
    )
    private boolean preferLaterVersions = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Configuration file path for local metadata store. It's supported by RocksdbMetadataStore for now."
    )
    private String metadataStoreConfigPath = null;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Enable precise rate limit for topic publish"
    )
    private boolean preciseTopicPublishRateLimiterEnable = false;
    @FieldContext(
        category = CATEGORY_SERVER,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Enable TLS with KeyStore type configuration in broker"
    )
    private boolean tlsEnabledWithKeyStore = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "Using a value of 0, is disabling default message-byte dispatch-throttling"
    )
    private long dispatchThrottlingRateInByte = 0;
    @FieldContext(
        category = CATEGORY_SERVER,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + "If try to create or update partitioned topics by exceeded number of partitions, then fail."
    )
    private int maxNumPartitionsPerPartitionedTopic = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + " efficiently, aka batched log. see: https://github.com/apache/pulsar/issues/15370. Default false"
    )
    private boolean transactionLogBatchedWriteEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Flag indicates enabling or disabling function worker using unified PackageManagement service."
    )
    private boolean functionsWorkerEnablePackageManagement = false;

    /**** --- Broker Web Stats. --- ****/
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Enable expose the broker bundles metrics."
    )
    private boolean exposeBundlesMetricsInPrometheus = false;

    /**** --- Functions. --- ****/
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "If true, aggregate publisher stats of PartitionedTopicStats by producerName"
    )
    private boolean aggregatePublisherStatsByProducerName = false;

    /**** --- Ledger Offloading. --- ****/
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String tlsKeyStorePassword = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        + "Topics that are inactive for longer than this value will be deleted"
    )
    private Integer brokerDeleteInactiveTopicsMaxInactiveDurationSeconds = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + " Default is false."
    )
    private boolean splitTopicAndPartitionLabelInPrometheus = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "(Default =0 to disable backlog reach cache)"
    )
    private int managedLedgerMinimumBacklogCursorsForCaching = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Flag indicates whether to run broker in standalone mode"
    )
    private boolean isRunningStandalone = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(category = CATEGORY_STORAGE_ML, doc = "Whether we should make a copy of the entry payloads when "
            + "inserting in cache")
    private boolean managedLedgerCacheCopyEntries = false;

    @FieldContext(category = CATEGORY_STORAGE_ML, doc = "Maximum buffer size for bytes read from storage."
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + " If it is set to 0 or a negative number, the fenced topic will not be closed."
    )
    private int topicFencingTimeoutSeconds = 0;

    /**** --- Messaging Protocol. --- ****/
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enable to run bookie autorecovery along with broker"
    )
    private boolean enableRunBookieAutoRecoveryTogether = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Flag to skip broker shutdown when broker handles Out of memory error"
    )
    private boolean skipBrokerShutdownOnOOM = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Flag indicates enabling or disabling function worker on brokers"
    )
    private boolean functionsWorkerEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " Broker will reject new consumers until the number of connected consumers decrease."
            + " Using a value of 0, is disabling maxConsumersPerTopic-limit check.")
    private int maxConsumersPerTopic = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + " Locking is used for fetching the status so default to false."
    )
    private boolean exposeSubscriptionBacklogSizeInPrometheus = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enforce authorization"
    )
    private boolean authorizationEnabled = false;
    @FieldContext(
        category = CATEGORY_AUTHORIZATION,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(category = CATEGORY_SERVER, doc = "Enable or disable strict bookie affinity.")
    private boolean strictBookieAffinityEnabled = false;

    /***** --- TLS. --- ****/
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String brokerClientTlsKeyStorePassword = null;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "(disable default-ttl with value 0)"
        )
    private int ttlDurationDefaultInSeconds = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(category = CATEGORY_STORAGE_BK, doc = "Enable tls authentication with bookie")
    private boolean bookkeeperTLSClientAuthentication = false;

    @FieldContext(category = CATEGORY_STORAGE_BK, doc = "Supported type: PEM, JKS, PKCS12. Default value: PEM")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enable check for minimum allowed client library version"
    )
    private boolean clientLibraryVersionCheckEnabled = false;
    @FieldContext(
        category = CATEGORY_SERVER,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enable the packages management service or not"
    )
    private boolean enablePackagesManagement = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " It helps when data-ledgers gets corrupted at bookkeeper and managed-cursor is stuck at that ledger."
    )
    private boolean autoSkipNonRecoverableData = false;
    @FieldContext(
        category = CATEGORY_STORAGE_ML,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Whether allow topic level entry filters policies overrides broker configuration."
    )
    private boolean allowOverrideEntryFilters = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Speculative reads are initiated if a read request doesn't complete within"
            + " a certain time Using a value of 0, is disabling the speculative reads")
    private int bookkeeperClientSpeculativeReadTimeoutInMillis = 0;
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "The caveat is now when recovered ledger is ready to write we're not sure if all old consumers last mark "
            + "delete position can be recovered or not.")
    private boolean lazyCursorRecovery = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " This is the memory retained by data read from storage (or cache) until it has been delivered to the"
            + " Consumer Netty channel. Use O to disable")
    private long managedLedgerMaxReadsInFlightSizeInMB = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Default number of message dispatching throttling-limit for every topic. \n\n"
            + "Using a value of 0, is disabling default message dispatch-throttling")
    private int dispatchThrottlingRatePerTopicInMsg = 0;
    @FieldContext(
        dynamic = true,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "The maximum number of namespaces that each tenant can create."
            + "This configuration is not precise control, in a concurrent scenario, the threshold will be exceeded")
    private int maxNamespacesPerTenant = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + "This configuration is not precise control, in a concurrent scenario, the threshold will be exceeded."
    )
    private int maxTenants = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enable cluster's failure-domain which can distribute brokers into logical region"
    )
    private boolean failureDomainsEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + " if cluster is marked migrated. Disable with value 0. (Default disabled)."
    )
    private int clusterMigrationCheckDurationSeconds = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "TLS KeyStore path in broker"
    )
    private String tlsKeyStore = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "whether expose managed ledger client stats to prometheus"
    )
    private boolean bookkeeperClientExposeStatsToPrometheus = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enable authentication"
    )
    private boolean authenticationEnabled = false;
    @FieldContext(
        category = CATEGORY_AUTHENTICATION,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        category = CATEGORY_SERVER,
        doc = "Enforce producer to publish encrypted messages.(default disable).")
    private boolean encryptionRequireOnProducer = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + " this would be more efficient but may be inaccurate. Default is false."
    )
    private boolean exposePreciseBacklogInPrometheus = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    )
    @Deprecated
    private boolean tlsEnabled = false;
    @FieldContext(
        category = CATEGORY_TLS,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "Using a value of 0, is disabling compression check."
    )
    private long brokerServiceCompactionThresholdInBytes = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(category = CATEGORY_SERVER, doc = "Whether to enable the acknowledge of batch local index")
    private boolean acknowledgmentAtBatchIndexLevelEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "(0 to disable it)"
        )
    private long managedLedgerReadEntryTimeoutSeconds = 0;

    @FieldContext(category = CATEGORY_STORAGE_ML,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "update dispatch-rate relatively to publish-rate: "
                    + "throttle-dispatch-rate = (publish-rate + configured dispatch-rate) ")
    private boolean dispatchThrottlingRateRelativeToPublishRate = false;
    @FieldContext(
        dynamic = true,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " value is set, broker will throttle the subscribe requests for one consumer. Otherwise, the"
            + " throttling will be disabled. The default value of this setting is 0 - throttling is disabled.")
    private int subscribeThrottlingRatePerConsumer = 0;
    @FieldContext(
        minValue = 1,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "TLS TrustStore path in broker"
    )
    private String tlsTrustStore = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(category = CATEGORY_STORAGE_BK, doc = "Set the interval to check the need for sending an explicit "
            + "LAC")
    private int bookkeeperExplicitLacIntervalInMills = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enable the WebSocket API service in broker"
    )
    private boolean webSocketServiceEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "The read failure backoff mandatory stop time in milliseconds. By default it is 0s."
    )
    private int dispatcherReadFailureBackoffMandatoryStopTimeInMs = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "when broker publish rate limiting enabled. (Disable message rate limit with value 0)"
    )
    private int brokerPublisherThrottlingMaxMessageRate = 0;
    @FieldContext(
        category = CATEGORY_SERVER,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "The TLS Provider used by internal client to authenticate with other Pulsar brokers"
    )
    private String brokerClientSslProvider = null;
    @FieldContext(
            category = CATEGORY_AUTHENTICATION,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "non-partitioned topic."
    )
    private boolean brokerDeleteInactivePartitionedTopicMetadataEnabled = false;
    @FieldContext(
        category = CATEGORY_POLICIES,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "The maximum number of connections per IP. If it exceeds, new connections are rejected."
    )
    private int brokerMaxConnectionsPerIp = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "may be as late as the deliverAt time plus the tickTimeMillis for the topic plus the "
            + "delayedDeliveryTickTimeMillis.")
    private boolean isDelayedDeliveryDeliverAtTimeStrict = false;

    @FieldContext(category = CATEGORY_SERVER, doc = """
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "'bookkeeperClientMinNumRacksPerWriteQuorum' racks for  a writeQuorum. \n\n"
            + "If BK can't find bookie then it would throw BKNotEnoughBookiesException instead of picking random one.")
    private boolean bookkeeperClientEnforceMinNumRacksPerWriteQuorum = false;
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + " Default false."
    )
    private boolean transactionPendingAckBatchedWriteEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "If true, export consumer level metrics otherwise namespace level"
    )
    private boolean exposeConsumerLevelMetricsInPrometheus = false;
    @FieldContext(
            category = CATEGORY_METRICS,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + " used by the internal client to authenticate with Pulsar brokers"
    )
    private String brokerClientTlsTrustStore = null;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Default number of message-bytes dispatching throttling-limit for a subscription. \n\n"
            + "Using a value of 0, is disabling default message-byte dispatch-throttling.")
    private long dispatchThrottlingRatePerSubscriptionInByte = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " connected producers with the same IP address decrease."
            + " Using a value of 0, is disabling maxSameAddressProducersPerTopic-limit check.")
    private int maxSameAddressProducersPerTopic = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Throttle value for bookkeeper client"
    )
    private int bookkeeperClientThrottleValue = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "If this flag is set then the broker authenticates the original Auth data"
            + " else it just accepts the originalPrincipal and authorizes it (if required)")
    private boolean authenticateOriginalAuthData = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " messages that were already stored in the topic"
    )
    private boolean brokerDeduplicationEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "when topic publish rate limiting enabled. (Disable byte rate limit with value 0)"
    )
    private int maxPublishRatePerTopicInMessages = 0;
    @FieldContext(
        category = CATEGORY_SERVER,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "Using a value of 0, is disabling default message-byte dispatch-throttling"
    )
    private int dispatchThrottlingRateInMsg = 0;
    @FieldContext(
            category = CATEGORY_SERVER,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "@deprecated - Use brokerClientTlsEnabled instead."
    )
    private boolean replicationTlsEnabled = false;
    @FieldContext(
        category = CATEGORY_POLICIES,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "and BK client threads to only have few CPU cores busy."
    )
    private boolean enableBusyWait = false;

    @FieldContext(category = CATEGORY_SERVER, doc = "Max concurrent web requests")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Configure the cache eviction frequency for the managed ledger cache.")
    @Deprecated
    private double managedLedgerCacheEvictionFrequency = 0;

    @FieldContext(category = CATEGORY_STORAGE_ML,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "each relevant rate limit."
    )
    private boolean dispatchThrottlingForFilteredEntriesEnabled = false;

    // <-- dispatcher read settings -->
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "or different clusters (replication)"
    )
    private boolean brokerClientTlsEnabled = false;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Enable the enforcement of limits on the incoming HTTP requests"
        )
    private boolean httpRequestsLimitEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                  + " used by the internal client to authenticate with Pulsar brokers"
    )
    private String brokerClientTlsKeyStore = null;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "(Default is disabled)"
        )
    private boolean unblockStuckSubscriptionEnabled = false;
    @FieldContext(
            category = CATEGORY_POLICIES,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "   if you enable this setting, it will cause non-java clients failed to produce."
    )
    private boolean isSchemaValidationEnforced = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                  + "ledger was closed, which is of coarser granularity."
    )
    private boolean preciseTimeBasedBacklogQuotaCheck = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Accept untrusted TLS certificate from client"
    )
    private boolean tlsAllowInsecureConnection = false;
    @FieldContext(
        category = CATEGORY_TLS,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "clusters on different cloud platforms."
    )
    private String configurationMetadataSyncEventTopic = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "When using TLS authentication with KeyStore, available values can be SunJSSE, Conscrypt and etc."
    )
    private String tlsProvider = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Allow forced deletion of namespaces. Default is false."
    )
    private boolean forceDeleteNamespaceAllowed = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Default number of message dispatching throttling-limit for a subscription. \n\n"
            + "Using a value of 0, is disabling default message dispatch-throttling.")
    private int dispatchThrottlingRatePerSubscriptionInMsg = 0;
    @FieldContext(
        dynamic = true,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @FieldContext(category = CATEGORY_STORAGE_ML,
            doc = "Add entry timeout when broker tries to publish message to bookkeeper.(0 to disable it)")
    private long managedLedgerAddEntryTimeoutSeconds = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    )
    @ToString.Exclude
    private String brokerClientTlsTrustStorePassword = null;
    @FieldContext(
            category = CATEGORY_KEYSTORE_TLS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "(wildcard matching only applicable if wildcard-char: * presents at first"
            + " or last position eg: *.pulsar.service, pulsar.service.*)")
    private boolean authorizationAllowWildcardsMatching = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " 0 means retention is disabled. -1 means data is not removed by time quota"
    )
    private int defaultRetentionTimeInMinutes = 0;
    @FieldContext(
        category = CATEGORY_POLICIES,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " Broker will reject new consumers until the number of connected consumers decrease."
            + " Using a value of 0, is disabling maxConsumersPerSubscription-limit check.")
    private int maxConsumersPerSubscription = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Specify whether Client certificates are required for TLS Reject.\n"
            + "the Connection if the Client Certificate is not trusted")
    private boolean tlsRequireTrustedClientCertOnConnect = false;

    /***** --- Authentication. --- ****/
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "When there are a large number of share or key share consumers in the cluster, "
                    + "it can be enabled to reduce the memory consumption caused by pendingAcks.")
    private boolean autoShrinkForConsumerPendingAcksMap = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Admin API fail on unknown request parameter in request-body. see PIP-179. Default false."
        )
    private boolean httpRequestsFailOnUnknownPropertiesEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "when topic publish rate limiting enabled. (Disable byte rate limit with value 0)"
    )
    private long maxPublishRatePerTopicInBytes = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "If enabled, the maximum \"acknowledgment holes\" will not be limited and \"acknowledgment holes\" "
                + "are stored in multiple entries.")
    private boolean persistentUnackedRangesWithMultipleEntriesEnabled = false;
    @Deprecated
    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "If true, export producer level metrics otherwise namespace level"
    )
    private boolean exposeProducerLevelMetricsInPrometheus = false;
    @FieldContext(
            category = CATEGORY_METRICS,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        + " scanning and flags web servers allowing the TRACE method as insecure."
    )
    private boolean disableHttpDebugMethods = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Default number of message-bytes dispatching throttling-limit for every topic. \n\n"
            + "Using a value of 0, is disabling default message-byte dispatch-throttling")
    private long dispatchThrottlingRatePerTopicInByte = 0;
    @FieldContext(
            dynamic = true,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enable to run bookie along with broker"
    )
    private boolean enableRunBookieTogether = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "The max active transactions per transaction coordinator, default value 0 indicates no limit."
    )
    private long maxActiveTransactionsPerCoordinator = 0L;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + "when broker publish rate limiting enabled. (Disable byte rate limit with value 0)"
    )
    private long brokerPublisherThrottlingMaxByteRate = 0;
    @FieldContext(
            category = CATEGORY_SERVER,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @FieldContext(category = CATEGORY_SERVER, doc = "Enable share the delayed message index across subscriptions")
    private boolean delayedDeliverySharedIndexEnabled = false;

    @FieldContext(category = CATEGORY_SERVER, doc = "Size of the lookahead window to use "
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "When this parameter is not empty, unauthenticated users perform as anonymousUserRole"
    )
    private String anonymousUserRole = null;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + "take effect."
    )
    private boolean enableNamespaceIsolationUpdateOnTime = false;

    @FieldContext(category = CATEGORY_SERVER, doc = "Enable or disable strict bookie affinity.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " When it is 0, inactive subscriptions are not deleted automatically"
    )
    private int subscriptionExpirationTimeMinutes = 0;
    @FieldContext(
            category = CATEGORY_POLICIES,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
        doc = "Enable region-aware bookie selection policy. \n\nBK will chose bookies from"
            + " different regions and racks when forming a new bookie ensemble")
    private boolean bookkeeperClientRegionawarePolicyEnabled = false;
    @FieldContext(
        category = CATEGORY_STORAGE_BK,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "to handle a large number of aborted transactions."
    )
    private boolean transactionBufferSegmentedSnapshotEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " Broker will reject new producers until the number of connected producers decrease."
            + " Using a value of 0, is disabling maxProducersPerTopic-limit check.")
    private int maxProducersPerTopic = 0;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Apply dispatch rate limiting on batch message instead individual "
                    + "messages with in batch message. (Default is disabled)")
        private boolean dispatchThrottlingOnBatchMessageEnabled = false;

    @FieldContext(
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Use separated IO threads for BookKeeper client. Default is false, which will use Pulsar IO threads"
    )
    private boolean bookkeeperClientSeparatedIoThreadsEnabled = false;

    /**** --- Managed Ledger. --- ****/
```

## RuleId[ruleID=RedundantImplements]
### RedundantImplements
Redundant interface declaration `Sink`
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/text/HdfsAbstractTextFileSink.java`
#### Snippet
```java
 * @param <V>
 */
public abstract class HdfsAbstractTextFileSink<K, V> extends HdfsAbstractSink<K, V> implements Sink<V> {

    private static final Logger LOG = LoggerFactory.getLogger(HdfsAbstractTextFileSink.class);
```

### RedundantImplements
Redundant interface declaration `Sink`
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/text/HdfsStringSink.java`
#### Snippet
```java
 * A Simple Sink class for Hdfs Text File.
 */
public class HdfsStringSink extends HdfsAbstractTextFileSink<String, String> implements Sink<String> {
    @Override
    public KeyValue<String, String> extractKeyValue(Record<String> record) {
```

### RedundantImplements
Redundant interface declaration `Sink`
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/seq/HdfsAbstractSequenceFileSink.java`
#### Snippet
```java
 */
public abstract class HdfsAbstractSequenceFileSink<K, V, HdfsK, HdfsV>
    extends HdfsAbstractSink<K, V> implements Sink<V> {

    private static final Logger LOG = LoggerFactory.getLogger(HdfsAbstractSequenceFileSink.class);
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/HdfsSinkConfig.java`
#### Snippet
```java
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
public class HdfsSinkConfig extends AbstractHdfsConfig implements Serializable {

    private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/HdfsSinkConfig.java`
#### Snippet
```java
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
public class HdfsSinkConfig extends AbstractHdfsConfig implements Serializable {

    private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `Sink`
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/seq/HdfsAbstractSequenceFileSink.java`
#### Snippet
```java
 */
public abstract class HdfsAbstractSequenceFileSink<K, V, HdfsK, HdfsV>
    extends HdfsAbstractSink<K, V> implements Sink<V> {

    private static final Logger LOG = LoggerFactory.getLogger(HdfsAbstractSequenceFileSink.class);
```

### RedundantImplements
Redundant interface declaration `Sink`
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/text/HdfsStringSink.java`
#### Snippet
```java
 * A Simple Sink class for Hdfs Text File.
 */
public class HdfsStringSink extends HdfsAbstractTextFileSink<String, String> implements Sink<String> {
    @Override
    public KeyValue<String, String> extractKeyValue(Record<String> record) {
```

### RedundantImplements
Redundant interface declaration `Sink`
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/text/HdfsAbstractTextFileSink.java`
#### Snippet
```java
 * @param <V>
 */
public abstract class HdfsAbstractTextFileSink<K, V> extends HdfsAbstractSink<K, V> implements Sink<V> {

    private static final Logger LOG = LoggerFactory.getLogger(HdfsAbstractTextFileSink.class);
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSinkConfig.java`
#### Snippet
```java
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
public class RedisSinkConfig extends RedisAbstractConfig implements Serializable {

    private static final long serialVersionUID = 4686456460365805717L;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSinkConfig.java`
#### Snippet
```java
@Data
@EqualsAndHashCode(callSuper = true)
public class KinesisSinkConfig extends BaseKinesisConfig implements Serializable {

    private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java
@Data
@EqualsAndHashCode(callSuper = true)
public class KinesisSourceConfig extends BaseKinesisConfig implements Serializable {

    private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSourceConfig.java`
#### Snippet
```java
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
public class RabbitMQSourceConfig extends RabbitMQAbstractConfig implements Serializable {

    private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSinkConfig.java`
#### Snippet
```java
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
public class RabbitMQSinkConfig extends RabbitMQAbstractConfig implements Serializable {

    private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `Dispatcher`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherSingleActiveConsumer.java`
#### Snippet
```java

public class PersistentDispatcherSingleActiveConsumer extends AbstractDispatcherSingleActiveConsumer
        implements Dispatcher, ReadEntriesCallback {

    private final AtomicBoolean isRescheduleReadInProgress = new AtomicBoolean(false);
```

### RedundantImplements
Redundant interface declaration `Subscription`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class NonPersistentSubscription extends AbstractSubscription implements Subscription {
    private final NonPersistentTopic topic;
    private volatile NonPersistentDispatcher dispatcher;
```

### RedundantImplements
Redundant interface declaration `Dispatcher`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentDispatcherMultipleConsumers.java`
#### Snippet
```java
 */
public class PersistentDispatcherMultipleConsumers extends AbstractDispatcherMultipleConsumers
        implements Dispatcher, ReadEntriesCallback {

    protected final PersistentTopic topic;
```

### RedundantImplements
Redundant interface declaration `Subscription`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class PersistentSubscription extends AbstractSubscription implements Subscription {
    protected final PersistentTopic topic;
    protected final ManagedCursor cursor;
```

### RedundantImplements
Redundant interface declaration `Topic`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class NonPersistentTopic extends AbstractTopic implements Topic, TopicPolicyListener<TopicPolicies> {

    // Subscriptions to this topic
```

### RedundantImplements
Redundant interface declaration `TopicPolicyListener`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class NonPersistentTopic extends AbstractTopic implements Topic, TopicPolicyListener<TopicPolicies> {

    // Subscriptions to this topic
```

### RedundantImplements
Redundant interface declaration `Topic`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class PersistentTopic extends AbstractTopic implements Topic, AddEntryCallback {

    // Managed ledger associated with the topic
```

### RedundantImplements
Redundant interface declaration `AutoCloseable`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupPublishLimiter.java`
#### Snippet
```java
import org.apache.pulsar.common.util.RateLimiter;

public class ResourceGroupPublishLimiter implements PublishRateLimiter, RateLimitFunction, AutoCloseable  {
    protected volatile long publishMaxMessageRate = 0;
    protected volatile long publishMaxByteRate = 0;
```

### RedundantImplements
Redundant interface declaration `MessageId`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ChunkMessageIdImpl.java`
#### Snippet
```java
import org.apache.pulsar.common.api.proto.MessageIdData;

public class ChunkMessageIdImpl extends MessageIdImpl implements MessageId {
    private final MessageIdImpl firstChunkMsgId;

```

### RedundantImplements
Redundant interface declaration `SchemaReader`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/MultiVersionGenericProtobufNativeReader.java`
#### Snippet
```java
public class MultiVersionGenericProtobufNativeReader
    extends AbstractMultiVersionReader<GenericRecord>
    implements SchemaReader<GenericRecord> {

    // the flag controls whether to use the provided schema as reader schema
```

### RedundantImplements
Redundant interface declaration `Position`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/PositionImplRecyclable.java`
#### Snippet
```java
import org.apache.bookkeeper.mledger.Position;

public class PositionImplRecyclable extends PositionImpl implements Position {

    private final Handle<PositionImplRecyclable> recyclerHandle;
```

### RedundantImplements
Redundant interface declaration `ReferenceCounted`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/EntryImpl.java`
#### Snippet
```java

public final class EntryImpl extends AbstractCASReferenceCounted implements Entry, Comparable<EntryImpl>,
        ReferenceCounted {

    private static final Recycler<EntryImpl> RECYCLER = new Recycler<EntryImpl>() {
```

### RedundantImplements
Redundant interface declaration `Closeable`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/OffloadIndexBlock.java`
#### Snippet
```java
 */
@Unstable
public interface OffloadIndexBlock extends Closeable, OffloadIndexBlockV2 {

    /**
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/JCloudBlobStoreProvider.java`
#### Snippet
```java
 */
@Slf4j
public enum JCloudBlobStoreProvider implements Serializable, ConfigValidation, BlobStoreBuilder, CredentialBuilder  {

    AWS_S3("aws-s3", new AWSS3ProviderMetadata()) {
```

### RedundantImplements
Redundant interface declaration `MetadataStoreExtended`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
@Slf4j
public class ZKMetadataStore extends AbstractBatchedMetadataStore
        implements MetadataStoreExtended, MetadataStoreLifecycle {

    public static final String ZK_SCHEME_IDENTIFIER = "zk:";
```

### RedundantImplements
Redundant interface declaration `MetadataStoreExtended`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/LocalMemoryMetadataStore.java`
#### Snippet
```java

@Slf4j
public class LocalMemoryMetadataStore extends AbstractMetadataStore implements MetadataStoreExtended {

    static final String MEMORY_SCHEME_IDENTIFIER = "memory:";
```

### RedundantImplements
Redundant interface declaration `AutoCloseable`
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/PulsarZooKeeperClient.java`
#### Snippet
```java
 */
@Slf4j
public class PulsarZooKeeperClient extends ZooKeeper implements Watcher, AutoCloseable {

    private static final int DEFAULT_RETRY_EXECUTOR_THREAD_COUNT = 1;
```

### RedundantImplements
Redundant interface declaration `Sinks`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/SinksImpl.java`
#### Snippet
```java

@Slf4j
public class SinksImpl extends ComponentResource implements Sinks, Sink {

    private final WebTarget sink;
```

### RedundantImplements
Redundant interface declaration `Sources`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/SourcesImpl.java`
#### Snippet
```java

@Slf4j
public class SourcesImpl extends ComponentResource implements Sources, Source {

    private final WebTarget source;
```

## RuleId[ruleID=ExceptionNameDoesntEndWithException]
### ExceptionNameDoesntEndWithException
Exception class name `SubscriptionInvalidCursorPosition` does not end with 'Exception'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerServiceException.java`
#### Snippet
```java
    }

    public static class SubscriptionInvalidCursorPosition extends BrokerServiceException {
        public SubscriptionInvalidCursorPosition(String msg) {
            super(msg);
```

### ExceptionNameDoesntEndWithException
Exception class name `NoMatchingRule` does not end with 'Exception'
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/KerberosName.java`
#### Snippet
```java

    @SuppressWarnings({"serial", "checkstyle:JavadocType"})
    public static class NoMatchingRule extends IOException {
        NoMatchingRule(String msg) {
            super(msg);
```

### ExceptionNameDoesntEndWithException
Exception class name `BadFormatString` does not end with 'Exception'
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/KerberosName.java`
#### Snippet
```java

    @SuppressWarnings({"serial", "checkstyle:JavadocType"})
    public static class BadFormatString extends IOException {
        BadFormatString(String msg) {
            super(msg);
```

### ExceptionNameDoesntEndWithException
Exception class name `NotLeaderAnymore` does not end with 'Exception'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/WorkerUtils.java`
#### Snippet
```java
    }

    public static class NotLeaderAnymore extends Exception {

    }
```

### ExceptionNameDoesntEndWithException
Exception class name `MarkDeletingMarkedPosition` does not end with 'Exception'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    }

    private final class MarkDeletingMarkedPosition extends IllegalArgumentException {
        public MarkDeletingMarkedPosition(String s) {
            super(s);
```

### ExceptionNameDoesntEndWithException
Exception class name `OffloadConflict` does not end with 'Exception'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
    };

    static class OffloadConflict extends ManagedLedgerException {
        OffloadConflict(String msg) {
            super(msg);
```

### ExceptionNameDoesntEndWithException
Exception class name `MemoryBufferIsFullError` does not end with 'Exception'
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
     * Memory buffer full error thrown by Pulsar client.
     */
    public static class MemoryBufferIsFullError extends PulsarClientException {
        /**
         * Constructs an {@code MemoryBufferIsFullError} with the specified detail message.
```

### ExceptionNameDoesntEndWithException
Exception class name `ProducerQueueIsFullError` does not end with 'Exception'
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
     * Full producer queue error thrown by Pulsar client.
     */
    public static class ProducerQueueIsFullError extends PulsarClientException {
        /**
         * Constructs an {@code ProducerQueueIsFullError} with the specified detail message.
```

### ExceptionNameDoesntEndWithException
Exception class name `InvalidServiceURL` does not end with 'Exception'
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
     * Invalid Service URL exception thrown by Pulsar client.
     */
    public static class InvalidServiceURL extends PulsarClientException {
        /**
         * Constructs an {@code InvalidServiceURL} with the specified cause.
```

### ExceptionNameDoesntEndWithException
Exception class name `ProducerBlockedQuotaExceededError` does not end with 'Exception'
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
     * Producer blocked quota exceeded error thrown by Pulsar client.
     */
    public static class ProducerBlockedQuotaExceededError extends PulsarClientException {
        /**
         * Constructs an {@code ProducerBlockedQuotaExceededError} with the specified detail message.
```

## RuleId[ruleID=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/PollingZooKeeperConfigurationProvider.java`
#### Snippet
```java
        } catch (Exception e) {
            lifecycleState = LifecycleState.ERROR;
            if (e instanceof RuntimeException) {
                throw (RuntimeException) e;
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
                            index, topicName);
                }
                if (e instanceof BrokerServiceException.TopicNotFoundException) {
                    // Topic ownership might changed, force to look up again.
                    pulsar().getBrokerService().getOwningTopics().remove(topicName.getPartitionedTopicName());
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TransactionsBase.java`
#### Snippet
```java
            asyncResponse.resume(transactionMetadataFuture.get(10, TimeUnit.SECONDS));
        } catch (Exception e) {
            if (e instanceof ExecutionException) {
                if (e.getCause() instanceof CoordinatorNotFoundException
                        || e.getCause() instanceof TransactionNotFoundException) {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
        } catch (Exception e) {
            PulsarServerException pse;
            if (e instanceof CompletionException && e.getCause() instanceof MetadataStoreException) {
                pse = new PulsarServerException(MetadataStoreException.unwrap(e));
            } else if (e.getCause() instanceof CompletionException
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/plugin/EntryFilterProvider.java`
#### Snippet
```java
            return new EntryFilterWithClassLoader(pi, ncl);
        } catch (Exception e) {
            if (e instanceof IOException) {
                throw (IOException) e;
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
        } catch (Exception e) {
            log.warn("Failed to get list of schema-storage ledger for {}, the exception as follow: \n {}", key,
                    (e instanceof ExecutionException ? e.getCause() : e));
            throw new IOException("Failed to get schema ledger for" + key);
        }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
                    result.complete(null);
                } catch (Exception e) {
                    if (e instanceof IncompatibleSchemaException) {
                        result.completeExceptionally(e);
                    } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
                        consumer.close();
                    } catch (BrokerServiceException e) {
                        if (e instanceof ConsumerBusyException) {
                            log.warn("[{}][{}] Consumer {} {} already connected", topic, subscriptionName, consumerId,
                                    consumerName);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java
                            log.warn("[{}][{}] Consumer {} {} already connected", topic, subscriptionName, consumerId,
                                    consumerName);
                        } else if (e instanceof SubscriptionBusyException) {
                            log.warn("[{}][{}] {}", topic, subscriptionName, e.getMessage());
                        }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
        } catch (Exception e) {
            log.warn("Error writing broker data on metadata store", e);
            if (e instanceof ConcurrentModificationException) {
                throw (ConcurrentModificationException) e;
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
        } catch (Exception e) {
            log.warn("Error when attempting to update local broker data", e);
            if (e instanceof ConcurrentModificationException) {
                throw (ConcurrentModificationException) e;
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                            consumer.close();
                        } catch (BrokerServiceException e) {
                            if (e instanceof ConsumerBusyException) {
                                log.warn("[{}][{}] Consumer {} {} already connected",
                                        topic, subscriptionName, consumerId, consumerName);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                                log.warn("[{}][{}] Consumer {} {} already connected",
                                        topic, subscriptionName, consumerId, consumerName);
                            } else if (e instanceof SubscriptionBusyException) {
                                log.warn("[{}][{}] {}", topic, subscriptionName, e.getMessage());
                            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex1`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceUsageTopicTransportManager.java`
#### Snippet
```java
                  new TenantInfoImpl(Sets.newHashSet(config.getSuperUserRoles()), Sets.newHashSet(cluster)));
            } catch (PulsarAdminException ex1) {
                if (!(ex1 instanceof PulsarAdminException.ConflictException)) {
                    LOG.error("Unexpected exception {} when creating tenant {}", ex1, tenant);
                    throw ex1;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex1`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceUsageTopicTransportManager.java`
#### Snippet
```java
                admin.namespaces().createNamespace(namespace);
            } catch (PulsarAdminException ex1) {
                if (!(ex1 instanceof PulsarAdminException.ConflictException)) {
                    LOG.error("Unexpected exception {} when creating namespace {}", ex1, namespace);
                    throw ex1;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
        } catch (Exception e) {
            LOG.info("failed to derive bundle for {}", nsname, e);
            throw new IllegalStateException(e instanceof ExecutionException ? e.getCause() : e);
        }
    }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/HttpClient.java`
#### Snippet
```java
        } catch (Exception e) {
            log.warn("[{}]PulsarClientImpl: {}", path, e.getMessage());
            if (e instanceof PulsarClientException) {
                future.completeExceptionally(e);
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/reader/AbstractMultiVersionReader.java`
#### Snippet
```java
                    getSchemaReader(schemaVersion).read(bytes);
        } catch (ExecutionException | AvroTypeException e) {
            if (e instanceof AvroTypeException) {
                throw new SchemaSerializationException(e);
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
                    }
                } catch (InterruptedException | ExecutionException e) {
                    if (e instanceof InterruptedException) {
                        Thread.currentThread().interrupt();
                    }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaImpl.java`
#### Snippet
```java
                configureSchemaInfo(topicName, "topic", schemaInfo);
            } catch (InterruptedException | ExecutionException e) {
                if (e instanceof InterruptedException) {
                    Thread.currentThread().interrupt();
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlockAwareSegmentInputStreamImpl.java`
#### Snippet
```java
        } catch (InterruptedException | ExecutionException e) {
            log.error("Exception when get CompletableFuture<LedgerEntries>. ", e);
            if (e instanceof InterruptedException) {
                Thread.currentThread().interrupt();
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `tiered-storage/file-system/src/main/java/org/apache/bookkeeper/mledger/offload/filesystem/impl/FileSystemManagedLedgerOffloader.java`
#### Snippet
```java
                log.error("Exception when get CompletableFuture<LedgerEntries> : ManagerLedgerName: {}, "
                        + "LedgerId: {}, UUID: {} ", topicName, ledgerId, uuid, e);
                if (e instanceof InterruptedException) {
                    Thread.currentThread().interrupt();
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `err`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
            } catch (Exception err) {
                log.error("Error in the OffloadedLedgerMetadataConsumer", err);
                if (err instanceof InterruptedException) {
                    Thread.currentThread().interrupt();
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `th`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionAssignmentTailer.java`
#### Snippet
```java
                        errorNotifier.triggerError(th);
                    } else {
                        if (!(th instanceof InterruptedException || th.getCause() instanceof InterruptedException)) {
                            log.warn("Encountered error when assignment tailer is not running", th);
                        }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
                }
            } catch (PulsarAdminException e) {
                if (e instanceof PulsarAdminException.NotFoundException) {
                    return Actions.ActionResult.builder()
                      .success(true)
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
                pulsarAdmin.topics().delete(topic, true);
            } catch (PulsarAdminException e) {
                if (e instanceof PulsarAdminException.NotFoundException) {
                    return Actions.ActionResult.builder()
                      .success(true)
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/PulsarWorkerService.java`
#### Snippet
```java
            getBrokerAdmin().topics().createNonPartitionedTopic(topic);
        } catch (PulsarAdminException e) {
            if (e instanceof PulsarAdminException.ConflictException) {
                log.warn("Failed to create topic '{}': {}", topic, e.getMessage());
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `th`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionMetaDataTopicTailer.java`
#### Snippet
```java
                    errorNotifier.triggerError(th);
                } else {
                    if (!(th instanceof InterruptedException || th.getCause() instanceof InterruptedException)) {
                        log.warn("Encountered error when metadata tailer is not running", th);
                    }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/batch/BatchSourceExecutor.java`
#### Snippet
```java
        batchSource.discover(task -> taskEater(discoveredEvent, task));
      } catch (Exception e) {
        if (isRunning || !(e instanceof InterruptedException)) {
          log.error("Encountered error during task discovery", e);
          setCurrentError(e);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/internal/ReflectionUtils.java`
#### Snippet
```java
            return s.get();
        } catch (Throwable t) {
            if (t instanceof InvocationTargetException) {
                // exception is thrown during invocation
                Throwable cause = t.getCause();
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `throwable`
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
                                    break;
                                    } catch (Exception throwable){
                                        if (throwable instanceof InterruptedException && !executing.get()) {
                                            break;
                                        }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/PulsarAdminTool.java`
#### Snippet
```java
        } catch (Exception e) {
            Throwable cause;
            if (e instanceof InvocationTargetException && null != e.getCause()) {
                cause = e.getCause();
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/CmdUtils.java`
#### Snippet
```java
            return ObjectMapperFactory.getThreadLocalYaml().readValue(new File(file), clazz);
        } catch (Exception ex) {
            if (ex instanceof UnrecognizedPropertyException) {
                UnrecognizedPropertyException unrecognizedPropertyException = (UnrecognizedPropertyException) ex;

```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/CmdUtils.java`
#### Snippet
```java
                        unrecognizedPropertyException.getKnownPropertyIds());
                throw new ParameterException(exceptionMessage);
            } else if (ex instanceof InvalidFormatException) {

                InvalidFormatException invalidFormatException = (InvalidFormatException) ex;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/CustomCommandFactoryProvider.java`
#### Snippet
```java
           return (CustomCommandFactory) factory;
        } catch (Exception e) {
            if (e instanceof IOException) {
                throw (IOException) e;
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
            jcommander.getConsole().println(e.getMessage());
            String chosenCommand = jcommander.getParsedCommand();
            if (e instanceof ParameterException) {
                try {
                    jcommander.getUsageFormatter().usage(chosenCommand);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
            System.out.println(e.getMessage());
            String chosenCommand = jcommander.getParsedCommand();
            if (e instanceof ParameterException) {
                try {
                    usageFormatter.usage(chosenCommand);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/AuthenticationFilter.java`
#### Snippet
```java
            HttpServletResponse httpResponse = (HttpServletResponse) response;
            httpResponse.sendError(HttpServletResponse.SC_UNAUTHORIZED, "Authentication required");
            if (e instanceof AuthenticationException) {
                LOG.warn("[{}] Failed to authenticate HTTP request: {}", request.getRemoteAddr(), e.getMessage());
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsServlet.java`
#### Snippet
```java
                long end = System.currentTimeMillis();
                long time = end - start;
                if (e instanceof EOFException) {
                    // NO STACKTRACE
                    log.error("Failed to send metrics, "
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderToken.java`
#### Snippet
```java
            return jwt;
        } catch (JwtException e) {
            if (e instanceof ExpiredJwtException) {
                expiredTokenMetrics.inc();
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderList.java`
#### Snippet
```java
                    return provider.authenticateHttpRequest(request, response);
                } catch (Exception e) {
                    if (e instanceof AuthenticationException) {
                        throw (AuthenticationException) e;
                    } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/utils/ReflectionUtils.java`
#### Snippet
```java
            return s.get();
        } catch (Throwable t) {
            if (t instanceof InvocationTargetException) {
                // exception is thrown during invocation
                Throwable cause = t.getCause();
```

## RuleId[ruleID=ArrayEquality]
### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/schema/BytesSchemaVersion.java`
#### Snippet
```java

            // short circuit equal case
            if (buffer1 == buffer2
                && offset1 == offset2
                && length1 == length2) {
```

## RuleId[ruleID=StringBufferReplaceableByStringBuilder]
### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/EnvVarResolverProperties.java`
#### Snippet
```java
        Pattern p = Pattern.compile("\\$\\{(\\w+)\\}");
        Matcher m = p.matcher(input);
        StringBuffer sb = new StringBuffer();
        while (m.find()) {
            String envVarName = m.group(1);
```

## RuleId[ruleID=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `cause` of exception class
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/RestException.java`
#### Snippet
```java
@SuppressWarnings("serial")
public class RestException extends WebApplicationException {
    private Throwable cause = null;
    static String getExceptionData(Throwable t) {
        StringWriter writer = new StringWriter();
```

### NonFinalFieldOfException
Non-final field `recoverable` of exception class
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/exceptions/SchemaException.java`
#### Snippet
```java

    private static final long serialVersionUID = -6587520779026691815L;
    private boolean recoverable;

    public SchemaException(boolean recoverable, String message) {
```

### NonFinalFieldOfException
Non-final field `error` of exception class
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/protocol/TokenExchangeException.java`
#### Snippet
```java
 */
public class TokenExchangeException extends Exception {
    private TokenError error;

    public TokenExchangeException(TokenError error) {
```

### NonFinalFieldOfException
Non-final field `sequenceId` of exception class
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
@SuppressWarnings("serial")
public class PulsarClientException extends IOException {
    private long sequenceId = -1;
    private Collection<Throwable> previous;

```

### NonFinalFieldOfException
Non-final field `previous` of exception class
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/PulsarClientException.java`
#### Snippet
```java
public class PulsarClientException extends IOException {
    private long sequenceId = -1;
    private Collection<Throwable> previous;

    /**
```

## RuleId[ruleID=RedundantStringFormatCall]
### RedundantStringFormatCall
Redundant call to `format()`
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
            FixedDateFormat.FixedFormat.ISO8601_OFFSET_DATE_TIME_HHMM.getPattern());
        Thread.setDefaultUncaughtExceptionHandler((thread, exception) -> {
            System.out.println(String.format("%s [%s] error Uncaught exception in thread %s: %s",
                    dateFormat.format(new Date()), thread.getContextClassLoader(),
                    thread.getName(), exception.getMessage()));
```

### RedundantStringFormatCall
Redundant call to `format()`
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
            } else {
                System.out.println(
                        String.format("[WARN] %s on functions instance classpath does not exist", f.getAbsolutePath()));
            }
        }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

            if (expireTimeInSeconds >= 0 && isNotBlank(messagePosition)) {
                throw new ParameterException(String.format("Can't expire message by time and "
                        + "by message position at the same time."));
            }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
        pulsarShellDir = computePulsarShellFile();
        Files.createDirectories(pulsarShellDir.toPath());
        System.out.println(String.format("Using directory: %s", pulsarShellDir.getAbsolutePath()));

        ConfigStore.ConfigEntry defaultConfig = null;
```

## RuleId[ruleID=FuseStreamOperations]
### FuseStreamOperations
Stream may be extended replacing 'toArray'
in `buildtools/src/main/java/org/apache/pulsar/tests/EnumValuesDataProvider.java`
#### Snippet
```java
        return Stream.of(enumValues)
                .map(enumValue -> new Object[]{enumValue})
                .collect(Collectors.toList())
                .toArray(new Object[0][]);
    }
```

## RuleId[ruleID=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-io/aws/src/main/java/org/apache/pulsar/io/aws/AbstractAwsConnector.java`
#### Snippet
```java
            Class<?> clazz = Class.forName(pluginFQClassName);
            Constructor<?> ctor = clazz.getConstructor();
            final AwsCredentialProviderPlugin plugin = (AwsCredentialProviderPlugin) ctor.newInstance(new Object[] {});
            plugin.init(param);
            return plugin;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaBytesSource.java`
#### Snippet
```java
                    .type(SchemaType.AVRO)
                    .properties(Collections.emptyMap())
                    .schema(new byte[0])
                    .build());
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `buildtools/src/main/java/org/apache/pulsar/tests/EnumValuesDataProvider.java`
#### Snippet
```java
                .map(enumValue -> new Object[]{enumValue})
                .collect(Collectors.toList())
                .toArray(new Object[0][]);
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/BrokerProxyValidator.java`
#### Snippet
```java
        if (allowedTargetPortsStrings.contains(ALLOW_ANY)) {
            allowAnyTargetPort = true;
            this.allowedTargetPorts = new int[0];
        } else {
            allowAnyTargetPort = false;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
                        new String(schema.getSchemaInfo().getSchema()));
            }
            return new byte[0];
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/tools/GenerateDocsCommand.java`
#### Snippet
```java
            ArrayList<String> args = new ArrayList(flags.commandNames);
            args.add(0, "-n");
            cmd.run(args.toArray(new String[0]));
        }
        return true;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
        byte[] deletedEntry = deleted(schemaId, user).toByteArray();
        return schemaStorage
                .put(schemaId, deletedEntry, new byte[]{})
                .whenComplete((v, t) -> {
                    if (t != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            }
            CompletableFuture<Void> shutdownFuture =
                    CompletableFuture.allOf(shutdownEventLoops.toArray(new CompletableFuture[0]))
                            .handle((v, t) -> {
                                if (t != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java
            zkConnectionWatcher.waitForConnection();
            if (zkc.exists("/ledgers", false) == null) {
                zkc.create("/ledgers", new byte[0], Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
            }
            if (zkc.exists("/ledgers/available", false) == null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java
            }
            if (zkc.exists("/ledgers/available", false) == null) {
                zkc.create("/ledgers/available", new byte[0], Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
            }
            if (zkc.exists("/ledgers/available/readonly", false) == null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java
            }
            if (zkc.exists("/ledgers/available/readonly", false) == null) {
                zkc.create("/ledgers/available/readonly", new byte[0], Ids.OPEN_ACL_UNSAFE, CreateMode.PERSISTENT);
            }
            if (zkc.exists("/bookies", false) == null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessagePayloadImpl.java`
#### Snippet
```java
            return bytes;
        } else {
            return new byte[0];
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
        // The Schema.BYTES will not be uploaded to the broker and store in the schema storage,
        // if the schema version in the message metadata is empty byte[], it means its schema is Schema.BYTES.
        schemaMap.put(BytesSchemaVersion.of(new byte[0]), Schema.BYTES);
        return schemaMap;
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
    public void fetchSchemaIfNeeded(SchemaVersion schemaVersion) throws SchemaSerializationException {
        if (schemaVersion == null) {
            schemaVersion = BytesSchemaVersion.of(new byte[0]);
        }
        if (!schemaMap.containsKey(schemaVersion)) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/AutoConsumeSchema.java`
#### Snippet
```java
            return BytesSchemaVersion.of(schemaVersion);
        }
        return BytesSchemaVersion.of(new byte[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/KeyValueSchemaImpl.java`
#### Snippet
```java
        setSchemaInfoProviderOnSubschemas();
        if (schemaVersion == null) {
            schemaVersion = BytesSchemaVersion.of(new byte[0]);
        }
        if (schemaInfoProvider == null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarClassLoader.java`
#### Snippet
```java
    private NarClassLoader(final File narWorkingDirectory, Set<String> additionalJars, ClassLoader parent)
            throws ClassNotFoundException, IOException {
        super(new URL[0], parent);
        this.narWorkingDirectory = narWorkingDirectory;

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/net/ServiceURI.java`
#### Snippet
```java
        } else {
            serviceName = null;
            serviceInfos = new String[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/KerberosName.java`
#### Snippet
```java
        getDefaultRealmMethod = classRef.getDeclaredMethod("getDefaultRealm"
        );
        return (String) getDefaultRealmMethod.invoke(kerbConf, new Object[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
        try {
            if (args == null) {
                args = new String[]{};
            }
            tmpCmd.parse(args);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SafeCollectionUtils.java`
#### Snippet
```java

    public static long[] longListToArray(List<Long> list) {
        return list == null || list.size() == 0 ? new long[0] : list.stream().mapToLong(l->l).toArray();
    }
}
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FutureUtil.java`
#### Snippet
```java
     */
    public static CompletableFuture<Object> waitForAny(Collection<? extends CompletableFuture<?>> futures) {
        return CompletableFuture.anyOf(futures.toArray(new CompletableFuture[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FutureUtil.java`
#### Snippet
```java
    public static CompletableFuture<Void> waitForAllAndSupportCancel(
                                                    Collection<? extends CompletableFuture<?>> futures) {
        CompletableFuture[] futuresArray = futures.toArray(new CompletableFuture[0]);
        CompletableFuture<Void> combinedFuture = CompletableFuture.allOf(futuresArray);
        whenCancelledOrTimedOut(combinedFuture, () -> {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FutureUtil.java`
#### Snippet
```java
     */
    public static CompletableFuture<Void> waitForAll(Collection<? extends CompletableFuture<?>> futures) {
        return CompletableFuture.allOf(futures.toArray(new CompletableFuture[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
        int n = wordsInUse;
        if (n == 0)
            return new byte[0];
        int len = 8 * (n-1);
        for (long x = words[n - 1]; x != 0; x >>>= 8)
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/KeyStoreSSLContext.java`
#### Snippet
```java

    private SSLEngine configureSSLEngine(SSLEngine sslEngine) {
        sslEngine.setEnabledProtocols(protocols.toArray(new String[0]));
        if (this.ciphers != null) {
            sslEngine.setEnabledCipherSuites(this.ciphers.toArray(new String[0]));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/KeyStoreSSLContext.java`
#### Snippet
```java
        sslEngine.setEnabledProtocols(protocols.toArray(new String[0]));
        if (this.ciphers != null) {
            sslEngine.setEnabledCipherSuites(this.ciphers.toArray(new String[0]));
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                ChunkMessageIdImpl msgId = (ChunkMessageIdImpl) messageId;
                seek = Commands.newSeek(consumerId, requestId, msgId.getFirstChunkMessageId().getLedgerId(),
                        msgId.getFirstChunkMessageId().getEntryId(), new long[0]);
            } else {
                MessageIdImpl msgId = (MessageIdImpl) messageId;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            } else {
                MessageIdImpl msgId = (MessageIdImpl) messageId;
                seek = Commands.newSeek(consumerId, requestId, msgId.getLedgerId(), msgId.getEntryId(), new long[0]);
            }
            return seekAsyncInternal(requestId, seek, messageId, seekBy);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/schema/SchemaHash.java`
#### Snippet
```java
    // Shouldn't call this method frequently, otherwise will bring performance regression
    public static SchemaHash of(byte[] schemaBytes, SchemaType schemaType) {
        return new SchemaHash(hashFunction.hashBytes(schemaBytes == null ? new byte[0] : schemaBytes), schemaType);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/MetaStoreImpl.java`
#### Snippet
```java
                            log.info("Creating '{}'", path);

                            store.put(path, new byte[0], Optional.of(-1L))
                                    .thenAccept(stat -> {
                                        ManagedLedgerInfo.Builder ledgerBuilder = ManagedLedgerInfo.newBuilder();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java

        CompletableFuture
            .allOf(messageIdFutures.values().toArray(new CompletableFuture<?>[0]))
            .whenComplete((ignore, ex) -> {
                Builder<String, MessageId> builder = ImmutableMap.builder();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
        if (ackType == AckType.Cumulative) {
            messageIdList.forEach(messageId -> resultFutures.add(doAcknowledge(messageId, ackType, properties, txn)));
            return CompletableFuture.allOf(resultFutures.toArray(new CompletableFuture[0]));
        } else {
            if (getState() != State.Ready) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                        .thenAccept((res) -> messageIdList.forEach(unAckedMessageTracker::remove)));
            });
            return CompletableFuture.allOf(resultFutures.toArray(new CompletableFuture[0]));
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockV2Impl.java`
#### Snippet
```java
        @Override
        public byte[] getPassword() {
            return new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java

                // Create parent nodes as "CONTAINER" so that ZK will automatically delete them when they're empty
                asyncCreateFullPathOptimistic(zk, parent, new byte[0], CreateMode.CONTAINER,
                        (rc1, path1, ctx1, name1) -> {
                            if (rc1 != Code.OK.intValue() && rc1 != Code.NODEEXISTS.intValue()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
                    .build()) {
                if (chrootZk.exists(chrootPath, false) == null) {
                    createFullPathOptimistic(chrootZk, chrootPath, new byte[0], CreateMode.PERSISTENT);
                    log.info("Created zookeeper chroot path {} successfully", chrootPath);
                }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java
        challenge.setProtocolVersion(versionToAdvertise)
                .setChallenge()
                .setAuthData(brokerData != null ? brokerData.getBytes() : new byte[0])
                .setAuthMethodName(authMethod);
        return serializeWithSize(cmd);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
                parent = "/";
            }
            return super.storePut(parent, new byte[0], Optional.empty(), EnumSet.noneOf(CreateOption.class))
                    // Then create the unique key with the version added in the path
                    .thenComposeAsync(
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
        }

        store.put(ledgersRootPath, new byte[0], Optional.empty()).get();

        // create INSTANCEID
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
        // Create ledgers root node if not exists
        if (!ledgerRootExists) {
            store.put(ledgersRootPath, new byte[0], Optional.empty()).get();
        }
        // create available bookies node if not exists
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
        // create available bookies node if not exists
        if (!availableNodeExists) {
            store.put(bookieRegistrationPath, new byte[0], Optional.empty()).get();
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
        // create readonly bookies node if not exists
        if (!store.exists(bookieReadonlyRegistrationPath).get()) {
            store.put(bookieReadonlyRegistrationPath, new byte[0], Optional.empty()).get();
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
        final String ledgerPrefix = this.shortIdGenPath + "/" + SHORT_ID_PREFIX;

        return store.put(ledgerPrefix, new byte[0], Optional.of(-1L),
                EnumSet.of(CreateOption.Ephemeral, CreateOption.Sequential))
                .thenCompose(stat -> {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
                        if (ledgerId < 0 || ledgerId >= Integer.MAX_VALUE) {
                            // 31-bit IDs overflowed. Start using 63-bit ids.
                            return store.put(ledgerIdGenPath, new byte[0], Optional.empty())
                                    .thenCompose(__ -> generateLongLedgerId());
                        } else {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java

        CompletableFuture<Long> future = new CompletableFuture<>();
        store.put(ledgerPrefix + formatHalfId(hob), new byte[0], Optional.empty())
                .whenComplete((__, ex) -> {
                    ex = FutureUtil.unwrapCompletionException(ex);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
    public CompletableFuture<Long> generateLedgerIdImpl(final String prefix) {
        return store
                .put(prefix, new byte[0], Optional.of(-1L), EnumSet.of(CreateOption.Ephemeral, CreateOption.Sequential))
                .thenCompose(stat -> {
                    // delete the znode for id generation
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
                    } else {
                        CompletableFuture<Void> future = new CompletableFuture<>();
                        store.put(shortIdGenPath, new byte[0], Optional.of(-1L))
                                .whenComplete((stat, throwable) -> {
                                    Throwable cause = FutureUtil.unwrapCompletionException(throwable);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/CoordinationServiceImpl.java`
#### Snippet
```java
        String counterBasePath = path + "/-";
        return store
                .put(counterBasePath, new byte[0], Optional.of(-1L),
                        EnumSet.of(CreateOption.Ephemeral, CreateOption.Sequential))
                .thenApply(stat -> {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/CoordinationServiceImpl.java`
#### Snippet
```java
                        return incrementCounter(path);
                    } else {
                        return store.put(path, new byte[0], Optional.empty())
                                .thenCompose(__ -> incrementCounter(path));
                    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/CryptoUtils.java`
#### Snippet
```java
                .consumerCryptoFailureAction(getConsumerCryptoFailureAction(spec.getConsumerCryptoFailureAction()))
                .producerCryptoFailureAction(getProducerCryptoFailureAction(spec.getProducerCryptoFailureAction()))
                .encryptionKeys(spec.getProducerEncryptionKeyNameList().toArray(new String[0]));

        return bldr.build();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/ContextImpl.java`
#### Snippet
```java

        try {
            CompletableFuture.allOf(futures.toArray(new CompletableFuture[0])).get();
        } catch (InterruptedException | ExecutionException e) {
            logger.warn("Failed to close producers", e);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client-api/src/main/java/org/apache/pulsar/common/schema/KeyValue.java`
#### Snippet
```java
        byte [] keyBytes;
        if (key == null) {
            keyBytes = new byte[0];
        } else {
            keyBytes = keyWriter.encode(key);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client-api/src/main/java/org/apache/pulsar/common/schema/KeyValue.java`
#### Snippet
```java
        byte [] valueBytes;
        if (value == null) {
            valueBytes = new byte[0];
        } else {
            valueBytes = valueWriter.encode(value);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/DisabledTxnLogBufferedWriterMetricsStats.java`
#### Snippet
```java

    private DisabledTxnLogBufferedWriterMetricsStats() {
        super("disabled", new String[0], new String[0], DisabledCollectorRegistry.INSTANCE);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/DisabledTxnLogBufferedWriterMetricsStats.java`
#### Snippet
```java

    private DisabledTxnLogBufferedWriterMetricsStats() {
        super("disabled", new String[0], new String[0], DisabledCollectorRegistry.INSTANCE);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java

            if (ciphers != null && ciphers.size() > 0) {
                this.setIncludeCipherSuites(ciphers.toArray(new String[0]));
            }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java

            if (protocols != null && protocols.size() > 0) {
                this.setIncludeProtocols(protocols.toArray(new String[0]));
            }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/TopicResources.java`
#### Snippet
```java
    public CompletableFuture<Void> createPersistentTopicAsync(TopicName topic) {
        String path = MANAGED_LEDGER_PATH + "/" + topic.getPersistenceNamingEncoding();
        return store.put(path, new byte[0], Optional.of(-1L))
                .thenApply(__ -> null);
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationState.java`
#### Snippet
```java
                return result != null
                        ? CompletableFuture.completedFuture(result)
                        : CompletableFuture.completedFuture(AuthData.of(new byte[0]));
            }
        } catch (Exception e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/SaslAuthenticationDataProvider.java`
#### Snippet
```java
        if (Arrays.equals(commandData.getBytes(), AuthData.INIT_AUTH_DATA_BYTES)) {
            if (pulsarSaslClient.hasInitialResponse()) {
                return pulsarSaslClient.evaluateChallenge(AuthData.of(new byte[0]));
            }
            return AuthData.of(new byte[0]);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/SaslAuthenticationDataProvider.java`
#### Snippet
```java
                return pulsarSaslClient.evaluateChallenge(AuthData.of(new byte[0]));
            }
            return AuthData.of(new byte[0]);
        }

```

## RuleId[ruleID=RedundantUnmodifiable]
### RedundantUnmodifiable
Redundant usage of the 'unmodifiableMap' wrapper
in `pulsar-common/src/main/java/org/apache/pulsar/policies/data/loadbalancer/LocalBrokerData.java`
#### Snippet
```java
            final String pulsarServiceUrlTls) {
        this(webServiceUrl, webServiceUrlTls, pulsarServiceUrl, pulsarServiceUrlTls,
                Collections.unmodifiableMap(Collections.emptyMap()));
    }

```

## RuleId[ruleID=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/utils/ReflectionUtils.java`
#### Snippet
```java
        return catchExceptions(
                () -> (T) ReflectionUtils.getStaticMethod(
                        className, "builder", null)
                        .invoke(null, null));
    }
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/utils/ReflectionUtils.java`
#### Snippet
```java
                () -> (T) ReflectionUtils.getStaticMethod(
                        className, "builder", null)
                        .invoke(null, null));
    }

```

## RuleId[ruleID=NonExceptionNameEndsWithException]
### NonExceptionNameEndsWithException
Non-exception class name `SupplierWithException` ends with 'Exception'
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/internal/ReflectionUtils.java`
#### Snippet
```java
@UtilityClass
class ReflectionUtils {
    interface SupplierWithException<T> {
        T get() throws Exception;
    }
```

### NonExceptionNameEndsWithException
Non-exception class name `SupplierWithException` ends with 'Exception'
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/utils/ReflectionUtils.java`
#### Snippet
```java
@UtilityClass
public class ReflectionUtils {
    interface SupplierWithException<T> {
        T get() throws Exception;
    }
```

## RuleId[ruleID=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java

    @Getter
    private final ConcurrentLongHashMap<TimedCompletableFuture<? extends Object>> pendingRequests =
            ConcurrentLongHashMap.<TimedCompletableFuture<? extends Object>>newBuilder()
                    .expectedItems(16)
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java
    @Getter
    private final ConcurrentLongHashMap<TimedCompletableFuture<? extends Object>> pendingRequests =
            ConcurrentLongHashMap.<TimedCompletableFuture<? extends Object>>newBuilder()
                    .expectedItems(16)
                    .concurrencyLevel(1)
```

## RuleId[ruleID=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
            }
        } else {
            if (sourceRecord.getMessage().get().hasBase64EncodedKey()) {
                key = sourceRecord.getMessage().get().getKeyBytes();
                keySchema = Schema.BYTES_SCHEMA;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
            // publishTime is not a log append time.
            // keep timestampType = TimestampType.NO_TIMESTAMP_TYPE
            timestamp = sourceRecord.getMessage().get().getPublishTime();
        }
        return new SinkRecord(topic,
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
            cb.accept(r);
            pendingFlushQueue.remove(r);
            currentBatchSize.addAndGet(-1 * r.getMessage().get().size());
            if (r == lastNotFlushed) {
                break;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
            tableNames = listTables(session, prefix.getSchema());
        } else {
            tableNames = ImmutableList.of(new SchemaTableName(prefix.getSchema().get(), prefix.getTable().get()));
        }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
                valueByteBuf = dataPayload.readSlice(valueLength);
            } else {
                keyByteBuf = this.currentMessage.getKeyBytes().get();
                valueByteBuf = this.currentMessage.getData();
            }
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java

        if (proxyConfig.getServicePort().isPresent()) {
            this.serviceUrl = String.format("pulsar://%s:%d/", hostname, getListenPort().get());
        } else {
            this.serviceUrl = null;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyService.java`
#### Snippet
```java

        if (proxyConfig.getServicePortTls().isPresent()) {
            this.serviceUrlTls = String.format("pulsar+ssl://%s:%d/", hostname, getListenPortTls().get());
        } else {
            this.serviceUrlTls = null;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataTeardown.java`
#### Snippet
```java
                    final String topicRoot = namespaceRoot + "/" + topic;
                    try {
                        SchemaLocator.parseFrom(metadataStore.get(topicRoot).join().get().getValue())
                                .getIndexList().stream()
                                .map(indexEntry -> indexEntry.getPosition().getLedgerId())
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
            return internalListener.getBrokerHttpUrl() != null
                    ? internalListener.getBrokerHttpUrl().toString()
                    : webAddress(ServiceConfigurationUtils.getWebServiceAddress(config), getListenPortHTTP().get());
        } else {
            return null;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
        return String.format("%s:%s", advertisedAddress, config.getWebServicePort().isPresent()
                ? config.getWebServicePort().get()
                : config.getWebServicePortTls().get());
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
            return internalListener.getBrokerHttpsUrl() != null
                    ? internalListener.getBrokerHttpsUrl().toString()
                    : webAddressTls(ServiceConfigurationUtils.getWebServiceAddress(config), getListenPortHTTPS().get());
        } else {
            return null;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                    return FutureUtil.failedFuture(new ProducerFencedException(
                            String.format("Topic epoch has already moved. Current epoch: %d, Producer epoch: %d",
                                    topicEpoch.get(), producer.getTopicEpoch().get())));
                } else {
                    // There are currently no existing producers
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                        return FutureUtil.failedFuture(new ProducerFencedException(
                                String.format("Topic epoch has already moved. Current epoch: %d, Producer epoch: %d",
                                        topicEpoch.get(), producer.getTopicEpoch().get())));
                    } else {
                        // There are currently no existing producers
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                    return FutureUtil.failedFuture(new ProducerFencedException(
                            String.format("Topic epoch has already moved. Current epoch: %d, Producer epoch: %d",
                                    topicEpoch.get(), producer.getTopicEpoch().get())));
                } else {
                    // There are currently no existing producers
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
                    try {
                        String key = String.format("%s/%s", LOADBALANCE_BROKERS_ROOT, broker);
                        LoadReport lr = loadReports.readLock(key).join().get();
                        ResourceUnit ru = new SimpleResourceUnit(String.format("http://%s", lr.getName()),
                                fromLoadReport(lr));
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            // Force to start the replication controller
            checkReplicatedSubscriptionControllerState(true /* shouldBeEnabled */);
            ctrl = replicatedSubscriptionsController.get();
        }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupNamespaceConfigListener.java`
#### Snippet
```java
                return;
            }
            Policies policy = optionalPolicies.get();
            reconcileNamespaceResourceGroup(nsName, policy);
        });
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupConfigListener.java`
#### Snippet
```java
                return;
            }
            ResourceGroup rg = optionalRg.get();
            try {
                LOG.info("Updating resource group {}, {}", rgName, rg);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupConfigListener.java`
#### Snippet
```java
                pulsarService.getPulsarResources().getResourcegroupResources()
                    .getResourceGroupAsync(rgName).thenAcceptAsync(optionalRg -> {
                    ResourceGroup rg = optionalRg.get();
                    createResourceGroup(rgName, rg);
                }).exceptionally((ex1) -> {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java
            Method method = serverCnxn.getClass().getMethod("close", disconnectReasonClass);
            method.invoke(serverCnxn, Stream.of(disconnectReasonClass.getEnumConstants()).filter(s ->
                    s.toString().equals("CONNECTION_CLOSE_FORCED")).findFirst().get());
        } catch (Exception ex) {
            throw new RuntimeException(ex);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                    deadLetterProducer.thenAccept(dlqProducer -> {
                        TypedMessageBuilder<byte[]> typedMessageBuilderNew =
                                dlqProducer.newMessage(Schema.AUTO_PRODUCE_BYTES(retryMessage.getReaderSchema().get()))
                                        .value(retryMessage.getData())
                                        .properties(propertiesMap);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                    String originTopicNameStr = getOriginTopicNameStr(message);
                    TypedMessageBuilder<byte[]> typedMessageBuilderNew =
                            producerDLQ.newMessage(Schema.AUTO_PRODUCE_BYTES(message.getReaderSchema().get()))
                            .value(message.getData())
                            .properties(getPropertiesMap(message, originMessageIdStr, originTopicNameStr));
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
        Set<CreateOption> options = event.getOptions() != null ? event.getOptions()
                : Collections.emptySet();
        String currentClusterName = getMetadataEventSynchronizer().get().getClusterName();
        // ignore event from the unknown cluster
        if (sourceClusterName == null || currentClusterName == null) {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
                store.put(layoutPath, builder.build().toString().getBytes(UTF_8), Optional.of(-1L)).join();
            } else {
                byte[] layoutData = store.get(layoutPath).join().get().getValue();

                LedgerRereplicationLayoutFormat.Builder builder = LedgerRereplicationLayoutFormat.newBuilder();
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
        log.debug("getLostBookieRecoveryDelay()");
        try {
            byte[] data = store.get(lostBookieRecoveryDelayPath).get().get().getValue();
            return Integer.parseInt(new String(data, UTF_8));
        } catch (ExecutionException ee) {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java
        byte[] payload;
        try {
            payload = serde.serialize(path, proposedValue.get());
        } catch (Throwable t) {
            return FutureUtils.exception(t);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/scheduler/RoundRobinScheduler.java`
#### Snippet
```java
    private Map.Entry<String, List<Instance>> findWorkerWithMostAssignments(
            Map<String, List<Instance>> workerToAssignmentMap) {
        return workerToAssignmentMap.entrySet().stream().max(Comparator.comparingInt(o -> o.getValue().size())).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/scheduler/RoundRobinScheduler.java`
#### Snippet
```java
    private Map.Entry<String, List<Instance>> findWorkerWithLeastAssignments(
            Map<String, List<Instance>> workerToAssignmentMap) {
        return workerToAssignmentMap.entrySet().stream().min(Comparator.comparingInt(o -> o.getValue().size())).get();

    }
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java
            PulsarRecord<T> pulsarRecord = (PulsarRecord<T>) sinkRecord.getSourceRecord();
            // forward user properties to sink-topic
            msg.property("__pfn_input_topic__", pulsarRecord.getTopicName().get())
                    .property("__pfn_input_msg_id__",
                            new String(Base64.getEncoder().encode(pulsarRecord.getMessageId().toByteArray()),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/TypedMessageBuilderPublish.java`
#### Snippet
```java

        Map<String, String> properties = new HashMap<>();
        properties.put("input_topic", context.getCurrentRecord().getTopicName().get());
        properties.putAll(context.getCurrentRecord().getProperties());

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
        if (isEventTime()) {
            long ts = this.timestampExtractor.extractTimestamp(record.getValue());
            if (this.waterMarkEventGenerator.track(record.getTopicName().get(), ts)) {
                this.windowManager.add(record, ts, record);
            } else {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/validator/MultipleListenerValidator.java`
#### Snippet
```java
        }
        if (StringUtils.isBlank(config.getInternalListenerName())) {
            config.setInternalListenerName(firstListenerName.get());
        }
        if (!listeners.containsKey(config.getInternalListenerName())) {
```

## RuleId[ruleID=ConstantValue]
### ConstantValue
Value `gzipStream` is always 'null'
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/utils/GZipFiles.java`
#### Snippet
```java
        gzipStream = new GZIPInputStream(Files.newInputStream(path));
      } catch (IOException e) {
        closeSafely(gzipStream);
        throw new UncheckedIOException(e);
      }
```

### ConstantValue
Value `zipStream` is always 'null'
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/utils/ZipFiles.java`
#### Snippet
```java
          zipStream = new ZipInputStream(Files.newInputStream(path));
        } catch (IOException e) {
          closeSafely(zipStream);
          throw new UncheckedIOException(e);
        }
```

### ConstantValue
Condition `maxSize != null` is always `true`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileListingThread.java`
#### Snippet
```java
                    return false;
                }
                if (maxSize != null && maxSize < file.length()) {
                    return false;
                }
```

### ConstantValue
Condition `maxAge != null` is always `true`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileListingThread.java`
#### Snippet
```java
                    return false;
                }
                if (maxAge != null && maxAge < fileAge) {
                    return false;
                }
```

### ConstantValue
Condition `listing != null` is always `true`
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileListingThread.java`
#### Snippet
```java
                    final Set<File> listing = performListing(directory, fileFilterRef.get(), recurseDirs);

                    if (listing != null && !listing.isEmpty()) {

                        // Remove any files that have been or are currently being processed.
```

### ConstantValue
Value `recurseSubdirectories` is always 'true'
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileListingThread.java`
#### Snippet
```java
            if (child.isDirectory()) {
                if (recurseSubdirectories) {
                    queue.addAll(performListing(child, filter, recurseSubdirectories));
                }
            } else if (filter.accept(child)) {
```

### ConstantValue
Value `statsProvider` is always 'null'
in `testmocks/src/main/java/org/apache/bookkeeper/client/BookKeeperTestClient.java`
#### Snippet
```java
        super(conf, zkc, null, new UnpooledByteBufAllocator(false),
                NullStatsLogger.INSTANCE, null, null, null);
        this.statsProvider = statsProvider;
    }

```

### ConstantValue
Condition `pkg.getName() != null` is always `true` when reached
in `buildtools/src/main/java/org/apache/pulsar/tests/FastThreadLocalCleanupListener.java`
#### Snippet
```java
        }
        Package pkg = clazz.getPackage();
        if (pkg != null && pkg.getName() != null) {
            return pkg.getName()
                    .startsWith(FAST_THREAD_LOCAL_CLEANUP_PACKAGE);
```

### ConstantValue
Condition `bkEnsemblePolicyToBkClientMap != null` is always `true`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/ManagedLedgerClientFactory.java`
#### Snippet
```java
                log.warn("Encountered exceptions on closing bookkeeper client", ree);
            }
            if (bkEnsemblePolicyToBkClientMap != null) {
                bkEnsemblePolicyToBkClientMap.forEach((policy, bk) -> {
                    try {
```

### ConstantValue
Condition `!(e instanceof BrokerServiceException.TopicFencedException && e instanceof ManagedLedgerException)` is always `true`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
    // Return error code depends on exception we got indicating if client should retry with same broker.
    private void extractException(Exception e, ProducerAck produceMessageResult) {
        if (!(e instanceof BrokerServiceException.TopicFencedException && e instanceof ManagedLedgerException)) {
            produceMessageResult.setErrorCode(2);
        } else {
```

### ConstantValue
Condition `e instanceof BrokerServiceException.TopicFencedException && e instanceof ManagedLedgerException` is always `false`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
    // Return error code depends on exception we got indicating if client should retry with same broker.
    private void extractException(Exception e, ProducerAck produceMessageResult) {
        if (!(e instanceof BrokerServiceException.TopicFencedException && e instanceof ManagedLedgerException)) {
            produceMessageResult.setErrorCode(2);
        } else {
```

### ConstantValue
Condition `e instanceof ManagedLedgerException` is always `false` when reached
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
    // Return error code depends on exception we got indicating if client should retry with same broker.
    private void extractException(Exception e, ProducerAck produceMessageResult) {
        if (!(e instanceof BrokerServiceException.TopicFencedException && e instanceof ManagedLedgerException)) {
            produceMessageResult.setErrorCode(2);
        } else {
```

### ConstantValue
Value `e` is always 'null'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TransactionsBase.java`
#### Snippet
```java
            FutureUtil.waitForAll(producedPartitionsFutures).whenComplete((x, t) -> {
                if (t != null) {
                    transactionMetadataFuture.completeExceptionally(e);
                    return;
                }
```

### ConstantValue
Condition `ENTRY_LATENCY_BUCKETS_USEC.length > 0` is always `true` when reached
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/NamespaceStats.java`
#### Snippet
```java
            String key;
            // example of key : "<metric_key>_0.0_0.5"
            if (i == 0 && ENTRY_LATENCY_BUCKETS_USEC.length > 0) {
                key = String.format("%s_0.0_%1.1f",
                        BRK_ADD_ENTRY_LATENCY_PREFIX, ENTRY_LATENCY_BUCKETS_USEC[i] / 1000.0);
```

### ConstantValue
Condition `filter != null` is always `true`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/plugin/EntryFilterProvider.java`
#### Snippet
```java
            EntryFilterWithClassLoader filter;
            filter = load(metaData, conf.getNarExtractionDirectory());
            if (filter != null) {
                builder.put(filterName, filter);
            }
```

### ConstantValue
Condition `filter != null` is always `true`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/plugin/EntryFilterProvider.java`
#### Snippet
```java
            EntryFilterWithClassLoader filter;
            filter = load(metaData, conf.getNarExtractionDirectory());
            if (filter != null) {
                builder.put(filterName, filter);
            }
```

### ConstantValue
Value `atLeastOneMessageSentForReplication` is always 'true'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
            if (log.isDebugEnabled()) {
                log.debug("[{}] Pausing replication traffic. at-least-one: {} is-writable: {}", replicatorId,
                        atLeastOneMessageSentForReplication, isWritable());
            }
        } else {
```

### ConstantValue
Condition `requestCommand instanceof CommandPartitionedTopicMetadata` is always `false`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                ctx.writeAndFlush(Commands.newLookupErrorResponse(ServerError.InvalidTopicName,
                        "Invalid topic name: " + t.getMessage(), requestId));
            } else if (requestCommand instanceof CommandPartitionedTopicMetadata) {
                ctx.writeAndFlush(Commands.newPartitionMetadataResponse(ServerError.InvalidTopicName,
                        "Invalid topic name: " + t.getMessage(), requestId));
```

### ConstantValue
Condition `interceptors != null` is always `true`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/BrokerInterceptors.java`
#### Snippet
```java

        Map<String, BrokerInterceptorWithClassLoader> interceptors = builder.build();
        if (interceptors != null && !interceptors.isEmpty()) {
            return new BrokerInterceptors(interceptors);
        } else {
```

### ConstantValue
Condition `null == latestVersion` is always `false`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/BrokerVersionFilter.java`
#### Snippet
```java
        }

        if (null == latestVersion) {
            throw new BrokerFilterBadVersionException("Unable to determine latest broker version");
        }
```

### ConstantValue
Value `readCompacted` is always 'false'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentTopic.java`
#### Snippet
```java

            Consumer consumer = new Consumer(subscription, subType, topic, consumerId, priorityLevel, consumerName,
                    false, cnx, cnx.getAuthRole(), metadata, readCompacted, keySharedMeta,
                    MessageId.latest, DEFAULT_CONSUMER_EPOCH);
            if (isMigrated()) {
```

### ConstantValue
Condition `newSample < oldValue` is always `true`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
        if (newSample >= oldValue) {
            weight = Math.min(1, Math.max(0, (double) timePast / RESOURCE_QUOTA_GO_UP_TIMEWINDOW));
        } else if (newSample < oldValue) {
            weight = Math.min(1, Math.max(0, (double) timePast / RESOURCE_QUOTA_GO_DOWN_TIMEWINDOW));
        }
```

### ConstantValue
Value `replicated` is always 'true'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                if (replicated && !subscription.isReplicated()) {
                    // Flip the subscription state
                    subscription.setReplicated(replicated);
                }

```

### ConstantValue
Value `resourceGroupName` is always 'null'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupService.java`
#### Snippet
```java
    private ResourceGroup getResourceGroupInternal(String resourceGroupName) {
        if (resourceGroupName == null) {
            throw new IllegalArgumentException("Invalid null resource group name: " + resourceGroupName);
        }

```

### ConstantValue
Condition `field != null` is always `true`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            try {
                Field field = ServiceConfiguration.class.getDeclaredField(key);
                if (field != null && field.isAnnotationPresent(FieldContext.class)) {
                    field.setAccessible(true);
                    field.set(pulsar().getConfiguration(), FieldParser.value(value, field));
```

### ConstantValue
Value `createIfMissing` is always 'true'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                                if (!value.isPresent()) {
                                    // retry and create topic
                                    return getTopic(topicName, createIfMissing, properties);
                                } else {
                                    // in-progress future completed successfully
```

### ConstantValue
Condition `conf.getCustomMessageRouter() != null` is always `true` when reached
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerBuilderImpl.java`
#### Snippet
```java
        if (conf.getMessageRoutingMode() == null && conf.getCustomMessageRouter() == null) {
            messageRoutingMode(MessageRoutingMode.RoundRobinPartition);
        } else if (conf.getMessageRoutingMode() == null && conf.getCustomMessageRouter() != null) {
            messageRoutingMode(MessageRoutingMode.CustomPartition);
        } else if (conf.getMessageRoutingMode() == MessageRoutingMode.CustomPartition
```

### ConstantValue
Value `pooledMessage` is always 'true'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
            MessageImpl<T> msg = (MessageImpl<T>) RECYCLER.get();
            init(msg, topic, messageId, msgMetadata, payload, encryptionCtx, cnx, schema, redeliveryCount,
                    pooledMessage, consumerEpoch);
            return msg;
        } else {
```

### ConstantValue
Value `pooledMessage` is always 'false'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
        } else {
            return new MessageImpl<>(topic, messageId, msgMetadata, payload, encryptionCtx, cnx, schema,
                    redeliveryCount, pooledMessage, consumerEpoch);
        }
    }
```

### ConstantValue
Value `pooledMessage` is always 'true'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
            MessageImpl<T> msg = (MessageImpl<T>) RECYCLER.get();
            init(msg, topic, batchMessageIdImpl, batchMetadata, singleMessageMetadata, payload, encryptionCtx, cnx,
                    schema, redeliveryCount, pooledMessage, consumerEpoch);
            return msg;
        } else {
```

### ConstantValue
Value `pooledMessage` is always 'false'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessageImpl.java`
#### Snippet
```java
        } else {
            return new MessageImpl<>(topic, batchMessageIdImpl, batchMetadata, singleMessageMetadata, payload,
                    encryptionCtx, cnx, schema, redeliveryCount, pooledMessage, consumerEpoch);
        }
    }
```

### ConstantValue
Condition `defaultPublicKey != null` is always `true`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java
        DefaultCryptoKeyReaderConfigurationData clone = new DefaultCryptoKeyReaderConfigurationData();

        if (defaultPublicKey != null) {
            clone.setDefaultPublicKey(defaultPublicKey);
        }
```

### ConstantValue
Condition `defaultPrivateKey != null` is always `true`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java
        }

        if (defaultPrivateKey != null) {
            clone.setDefaultPrivateKey(defaultPrivateKey);
        }
```

### ConstantValue
Condition `publicKeys != null` is always `true`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java
        }

        if (publicKeys != null) {
            clone.setPublicKeys(new HashMap<String, String>(publicKeys));
        }
```

### ConstantValue
Condition `privateKeys != null` is always `true`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/conf/DefaultCryptoKeyReaderConfigurationData.java`
#### Snippet
```java
        }

        if (privateKeys != null) {
            clone.setPrivateKeys(new HashMap<String, String>(privateKeys));
        }
```

### ConstantValue
Value `recurse` is always 'true'
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/FileUtils.java`
#### Snippet
```java
        final File[] list = file.listFiles();
        if (file.isDirectory() && recurse && list != null) {
            FileUtils.deleteFiles(Arrays.asList(list), recurse);
        }
        //now delete the file itself regardless of whether it is plain file or a directory
```

### ConstantValue
Value `recurse` is always 'true'
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/FileUtils.java`
#### Snippet
```java
                }
                if (ingestFile.isDirectory() && recurse) {
                    FileUtils.deleteFilesInDirectory(ingestFile, filter, logger, recurse, deleteEmptyDirectories);
                    String[] ingestFileList = ingestFile.list();
                    if (deleteEmptyDirectories && ingestFileList != null && ingestFileList.length == 0) {
```

### ConstantValue
Condition `subjectAlts != null` is always `true`
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/TlsHostnameVerifier.java`
#### Snippet
```java
        final HostNameType hostType = determineHostFormat(host);
        final List<SubjectName> subjectAlts = getSubjectAltNames(cert);
        if (subjectAlts != null && !subjectAlts.isEmpty()) {
            switch (hostType) {
            case IPv4:
```

### ConstantValue
Value `hostname` is always 'null'
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/TlsHostnameVerifier.java`
#### Snippet
```java
    static String normaliseAddress(final String hostname) {
        if (hostname == null) {
            return hostname;
        }
        try {
```

### ConstantValue
Condition `retry >= 0` is always `true`
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
                String kinitArgs = "-R";
                int retry = 1;
                while (retry >= 0) {
                    try {
                        log.info("running ticket cache refresh command: {} {}", cmd, kinitArgs);
```

### ConstantValue
Condition `retry >= 0` is always `true`
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
            try {
                int retry = 1;
                while (retry >= 0) {
                    try {
                        reLogin();
```

### ConstantValue
Condition `fieldType.getClass().equals(String.class)` is always `false` when reached
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
                        format("unsupported field-type %s for %s", field.getType(), field.getName()));
            }
        } else if (Number.class.isAssignableFrom(field.getType()) || fieldType.getClass().equals(String.class)) {
            field.set(obj, null);
        }
```

### ConstantValue
Condition `toString != null` is always `true`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
            StringBuilder toString = new StringBuilder();
            AtomicBoolean first = new AtomicBoolean(true);
            if (toString != null) {
                toString.append("[");
            }
```

### ConstantValue
Condition `rangeBitSet != null` is always `true`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenLongPairRangeSet.java`
#### Snippet
```java
            if (isValid(upperKey, upperValue)) {
                BitSet rangeBitSet = rangeBitSetMap.computeIfAbsent(upperKey, (key) -> createNewBitSet());
                if (rangeBitSet != null) {
                    rangeBitSet.set(0, (int) upperValue + 1);
                }
```

### ConstantValue
Condition `wordsInUse >= 0` is always `true`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
    private void checkInvariants() {
        assert(wordsInUse == 0 || words[wordsInUse - 1] != 0);
        assert(wordsInUse >= 0 && wordsInUse <= words.length);
        assert(wordsInUse == words.length || words[wordsInUse] == 0);
    }
```

### ConstantValue
Value `bitSetRecyclable` is always 'null'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
            ledgerId = singleMessage.getLedgerId();
            entryId = singleMessage.getEntryId();
            cmd = Commands.newAck(consumerId, ledgerId, entryId, bitSetRecyclable, ackType,
                    validationError, properties, txnID.getLeastSigBits(), txnID.getMostSigBits(), requestId);
        }
```

### ConstantValue
Value `certificates` is always 'null'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java

        if (certFilePath == null || certFilePath.isEmpty()) {
            return certificates;
        }

```

### ConstantValue
Condition `reminderOnLeft == null` is always `true` when reached
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
                        foundButMissingSomethingOnBoth = new FindPendingReadOutcome(entry.getValue(),
                                reminderOnLeft, reminderOnRight);
                    } else if (reminderOnRight != null && reminderOnLeft == null) {
                        foundButMissingSomethingOnRight = new FindPendingReadOutcome(entry.getValue(),
                                null, reminderOnRight);
```

### ConstantValue
Condition `reminderOnRight == null` is always `true` when reached
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
                        foundButMissingSomethingOnRight = new FindPendingReadOutcome(entry.getValue(),
                                null, reminderOnRight);
                    } else if (reminderOnLeft != null && reminderOnRight == null) {
                        foundButMissingSomethingOnLeft = new FindPendingReadOutcome(entry.getValue(),
                                reminderOnLeft, null);
```

### ConstantValue
Condition `entryId < nextExpectedId` is always `true`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java
                                            .getDataOffset());
                            continue;
                        } else if (entryId < nextExpectedId
                                && !groupedReader.index.getIndexEntryForEntry(groupedReader.ledgerId, nextExpectedId)
                                .equals(
```

### ConstantValue
Condition `!(t instanceof CompletionException)` is always `true` when reached
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreException.java`
#### Snippet
```java
        } else if (t instanceof InterruptedException) {
            return new MetadataStoreException(t);
        } else if (!(t instanceof ExecutionException) && !(t instanceof CompletionException)) {
            // Generic exception
            return new MetadataStoreException(t);
```

### ConstantValue
Condition `t instanceof CompletionException` is always `false` when reached
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreException.java`
#### Snippet
```java
        } else if (t instanceof InterruptedException) {
            return new MetadataStoreException(t);
        } else if (!(t instanceof ExecutionException) && !(t instanceof CompletionException)) {
            // Generic exception
            return new MetadataStoreException(t);
```

### ConstantValue
Condition `extraMetadata != null` is always `true`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                Map<String, String> objectMetadata = new HashMap<>(userMetadata);
                objectMetadata.put("role", "data");
                if (extraMetadata != null) {
                   objectMetadata.putAll(extraMetadata);
                }
```

### ConstantValue
Condition `extraMetadata != null` is always `true`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                Map<String, String> objectMetadata = new HashMap<>(userMetadata);
                objectMetadata.put("role", "index");
                if (extraMetadata != null) {
                    objectMetadata.putAll(extraMetadata);
                }
```

### ConstantValue
Condition `tempPkgFos != null` is always `true`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
                    tempPkgFos,
                    pkgLocationPath);
            if (tempPkgFos != null) {
                tempPkgFos.close();
            }
```

### ConstantValue
Value `needsScheduling` is always 'true'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionMetaDataManager.java`
#### Snippet
```java
        }

        return needsScheduling;
    }

```

### ConstantValue
Result of `functionConfig.getForwardSourceMessageProperty()` is always 'true'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        }
        if (functionConfig.getForwardSourceMessageProperty() == Boolean.TRUE) {
            sinkSpecBuilder.setForwardSourceMessageProperty(functionConfig.getForwardSourceMessageProperty());
        }
        if (functionConfig.getCustomSchemaOutputs() != null && functionConfig.getOutput() != null) {
```

### ConstantValue
Condition `functionMetaDataManagerExclusiveProducer != null` is always `false`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java
                        scheduleManagerExclusiveProducer.close();
                    }
                    if (functionMetaDataManagerExclusiveProducer != null) {
                        functionMetaDataManagerExclusiveProducer.close();
                    }
```

### ConstantValue
Condition `namespaces != null` is always `true`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
                String qualifiedNamespaceWithCluster = String.format("%s/%s/%s", tenant,
                        worker().getWorkerConfig().getPulsarFunctionsCluster(), namespace);
                if (namespaces != null && !namespaces.contains(qualifiedNamespaceWithCluster)) {
                    log.error("{}/{}/{} Namespace {} does not exist", tenant, namespace, sinkName, namespace);
                    throw new RestException(Response.Status.BAD_REQUEST, "Namespace does not exist");
```

### ConstantValue
Condition `namespaces != null` is always `true`
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
                String qualifiedNamespaceWithCluster = String.format("%s/%s/%s", tenant,
                        worker().getWorkerConfig().getPulsarFunctionsCluster(), namespace);
                if (namespaces != null && !namespaces.contains(qualifiedNamespaceWithCluster)) {
                    log.error("{}/{}/{} Namespace {} does not exist", tenant, namespace, sourceName, namespace);
                    throw new RestException(Response.Status.BAD_REQUEST, "Namespace does not exist");
```

### ConstantValue
Condition `finalPosition == null` is always `false`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                if (position == null) {
                    finalPosition = startPosition;
                    if (finalPosition == null) {
                        log.warn("[{}] Unable to find position for predicate {}.", name, predicate);
                        future.complete(null);
```

### ConstantValue
Condition `stats == null` is always `false`
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/admin/WebSocketProxyStatsBase.java`
#### Snippet
```java
        validateUserAccess(topicName);
        ProxyTopicStat stats = getStat(topicName);
        if (stats == null) {
            throw new RestException(Status.NOT_FOUND, "Topic does not exist");
        }
```

### ConstantValue
Condition `t instanceof CoordinatorNotFoundException` is always `false`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/transaction/TransactionCoordinatorClientException.java`
#### Snippet
```java
            Thread.currentThread().interrupt();
            return new TransactionCoordinatorClientException(t);
        } else if (t instanceof CoordinatorNotFoundException) {
            return (CoordinatorNotFoundException) t;
        } else if (t instanceof InvalidTxnStatusException) {
```

### ConstantValue
Condition `t instanceof InvalidTxnStatusException` is always `false`
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/transaction/TransactionCoordinatorClientException.java`
#### Snippet
```java
        } else if (t instanceof CoordinatorNotFoundException) {
            return (CoordinatorNotFoundException) t;
        } else if (t instanceof InvalidTxnStatusException) {
            return (InvalidTxnStatusException) t;
        } else if (t instanceof ExecutionException | t instanceof CompletionException) {
```

### ConstantValue
Condition `t instanceof CompletionException` is always `false` when reached
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/transaction/TransactionCoordinatorClientException.java`
#### Snippet
```java
        } else if (t instanceof InvalidTxnStatusException) {
            return (InvalidTxnStatusException) t;
        } else if (t instanceof ExecutionException | t instanceof CompletionException) {
            // Generic exception
            return unwrap(t.getCause());
```

### ConstantValue
Condition `conf != null` is always `true`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/http/AsyncHttpConnector.java`
#### Snippet
```java

        serviceNameResolver = new PulsarServiceNameResolver();
        if (conf != null && StringUtils.isNotBlank(conf.getServiceUrl())) {
            serviceNameResolver.updateServiceUrl(conf.getServiceUrl());
            if (conf.getServiceUrl().startsWith("https://")) {
```

### ConstantValue
Condition `namespaces != null` is always `true`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java
        NamespaceIsolationData.Builder nsIsolationDataBuilder = NamespaceIsolationData.builder();

        if (namespaces != null) {
            nsIsolationDataBuilder.namespaces(namespaces);
        }
```

### ConstantValue
Condition `primary != null` is always `true`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaceIsolationPolicy.java`
#### Snippet
```java
        }

        if (primary != null) {
            nsIsolationDataBuilder.primary(primary);
        }
```

### ConstantValue
Condition `nv != null` is always `true`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/NameValueParameterSplitter.java`
#### Snippet
```java
            if (nvpair != null) {
                String[] nv = nvpair.split("=");
                if (nv != null && nv.length == 2) {
                    nv[0] = nv[0].trim();
                    nv[1] = nv[1].trim();
```

### ConstantValue
Condition `factory != null` is always `true`
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/CustomCommandFactoryProvider.java`
#### Snippet
```java
            }
            CustomCommandFactory factory = load(metaData, narExtractionDirectory);
            if (factory != null) {
                result.add(factory);
            }
```

### ConstantValue
Value `config` is always 'null'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
            final ConfigStore.ConfigEntry config = configStore.getConfig(cloneFrom);
            if (config == null) {
                print("Config '" + config + "' does not exist.");
                return false;
            }
```

### ConstantValue
Value `advertisedAddress` is always 'null'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfigurationUtils.java`
#### Snippet
```java
        }

        return getDefaultOrConfiguredAddress(advertisedAddress);
    }

```

### ConstantValue
Condition `numBundles > MAX_BUNDLES` is always `false` when reached
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        void run() throws PulsarAdminException {
            String namespace = validateNamespace(params);
            if (numBundles < 0 || numBundles > MAX_BUNDLES) {
                throw new ParameterException(
                        "Invalid number of bundles. Number of bundles has to be in the range of (0, 2^32].");
```

### ConstantValue
Result of `properties.size()` is always '0'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
            if (properties.size() == 0) {
                throw new ParameterException(String.format("Required at least one property for the namespace, "
                        + "but found %d.", properties.size()));
            }
            Map<String, String> map = parseListKeyValueMap(properties);
```

### ConstantValue
Condition `servlets != null` is always `true`
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServlets.java`
#### Snippet
```java

        Map<String, AdditionalServletWithClassLoader> servlets = builder.build();
        if (servlets != null && !servlets.isEmpty()) {
            return new AdditionalServlets(servlets);
        }
```

## RuleId[ruleID=MethodOverridesStaticMethod]
### MethodOverridesStaticMethod
Method `jsonMapper()` tries to override a static method of a superclass
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java
    }

    public static ObjectMapper jsonMapper() {
        return ObjectMapperFactory.getThreadLocal();
    }
```

### MethodOverridesStaticMethod
Method `of()` tries to override a static method of a superclass
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericProtobufNativeSchema.java`
#### Snippet
```java
    }

    public static GenericSchema of(SchemaInfo schemaInfo, boolean useProvidedSchemaAsReaderSchema) {
        return new GenericProtobufNativeSchema(schemaInfo, useProvidedSchemaAsReaderSchema);
    }
```

### MethodOverridesStaticMethod
Method `of()` tries to override a static method of a superclass
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericProtobufNativeSchema.java`
#### Snippet
```java
    }

    public static GenericSchema of(SchemaInfo schemaInfo) {
        return new GenericProtobufNativeSchema(schemaInfo);
    }
```

## RuleId[ruleID=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `OutputStreamWriter.append()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
                        public void finished(int total, int errors, int unknown) throws Exception {
                            out.append("]\n");
                            out.append("\"total\": " + total + ",\n");
                            out.append("\"errors\": " + errors + ",\n");
                            out.append("\"unknown\": " + unknown + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `OutputStreamWriter.append()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
                            out.append("]\n");
                            out.append("\"total\": " + total + ",\n");
                            out.append("\"errors\": " + errors + ",\n");
                            out.append("\"unknown\": " + unknown + "\n");
                        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `OutputStreamWriter.append()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
                            out.append("\"total\": " + total + ",\n");
                            out.append("\"errors\": " + errors + ",\n");
                            out.append("\"unknown\": " + unknown + "\n");
                        }
                    });
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java

                    if (jarClassLoaderException != null) {
                        errorMsg.append(
                                " Attempts to load it as a JAR package produced error: " + jarClassLoaderException
                                        .getMessage());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java

                    if (narClassLoaderException != null) {
                        errorMsg.append(
                                " Attempts to load it as a NAR package produced error: " + narClassLoaderException
                                        .getMessage());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeUtils.java`
#### Snippet
```java
        String line;
        while ((line = rd.readLine()) != null) {
            result.append(line + System.lineSeparator());
        }
        rd.close();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdUsageFormatter.java`
#### Snippet
```java
    @Override
    public void appendCommands(StringBuilder out, int indentCount, int descriptionIndent, String indent) {
        out.append(indent + "  Commands:\n");

        for (Map.Entry<JCommander.ProgramName, JCommander> commands : commander.getRawCommands().entrySet()) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/config/ConfigStore.java`
#### Snippet
```java
            }
            if (!keys.contains(propertyName)) {
                builder.append(propertyName + "=" + propertyValue);
                builder.append(System.lineSeparator());
            }
```

## RuleId[ruleID=IOResource]
### IOResource
'PushbackInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/utils/GZipFiles.java`
#### Snippet
```java
    public static boolean isGzip(File f) {
        try (InputStream input = new FileInputStream(f)) {
            PushbackInputStream pb = new PushbackInputStream(input, 2);
            byte[] signature = new byte[2];
            int len = pb.read(signature); //read the signature
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadDumpUtil.java`
#### Snippet
```java
    public static String buildThreadDiagnosticString() {
        StringWriter sw = new StringWriter();
        PrintWriter output = new PrintWriter(sw);

        output.println(buildThreadDump());
```

### IOResource
'OutputStreamWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
            StreamingOutput output = (outputStream) -> {
                try {
                    OutputStreamWriter out = new OutputStreamWriter(outputStream, StandardCharsets.UTF_8);
                    out.append("{objects:[\n");
                    internalScanOffloadedLedgers(new OffloaderObjectsScannerUtils.ScannerResultSink() {
```

### IOResource
'TableViewImpl' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TableViewBuilderImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<TableView<T>> createAsync() {
       return new TableViewImpl<>(client, schema, conf).start();
    }

```

### IOResource
'MultiTopicsReaderImpl' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
        CompletableFuture<Reader<T>> readerFuture = new CompletableFuture<>();
        CompletableFuture<Consumer<T>> consumerSubscribedFuture = new CompletableFuture<>();
        MultiTopicsReaderImpl<T> reader = new MultiTopicsReaderImpl<>(this,
                conf, externalExecutorProvider, consumerSubscribedFuture, schema);
        ConsumerBase<T> consumer = reader.getMultiTopicsConsumer();
```

### IOResource
'MultiTopicsReaderImpl' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
            ConsumerBase<T> consumer;
            if (metadata.partitions > 0) {
                reader = new MultiTopicsReaderImpl<>(PulsarClientImpl.this,
                        conf, externalExecutorProvider, consumerSubscribedFuture, schema);
                consumer = ((MultiTopicsReaderImpl<T>) reader).getMultiTopicsConsumer();
```

### IOResource
'ReaderImpl' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImpl.java`
#### Snippet
```java
                consumer = ((MultiTopicsReaderImpl<T>) reader).getMultiTopicsConsumer();
            } else {
                reader = new ReaderImpl<>(PulsarClientImpl.this, conf, externalExecutorProvider,
                        consumerSubscribedFuture, schema);
                consumer = ((ReaderImpl<T>) reader).getConsumer();
```

### IOResource
'DnsAddressResolverGroup' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConnectionPool.java`
#### Snippet
```java
        // to prevent cache stampede / thundering herds problem when a DNS entry expires while the system
        // is under high load
        return new DnsAddressResolverGroup(dnsNameResolverBuilder).getResolver(eventLoopGroup.next());
    }

```

### IOResource
'RandomAccessFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarUnpacker.java`
#### Snippet
```java
            // create file lock that ensures that other processes
            // using the same lock file don't execute concurrently
            try (FileChannel channel = new RandomAccessFile(lockFile, "rw").getChannel();
                 FileLock lock = channel.lock()) {
                File narWorkingDirectory = new File(parentDirectory, md5Sum);
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ThreadDumpUtil.java`
#### Snippet
```java
    public static String buildThreadDiagnosticString() {
        StringWriter sw = new StringWriter();
        PrintWriter output = new PrintWriter(sw);

        output.println(buildThreadDump());
```

### IOResource
'DataInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/StreamingDataBlockHeaderImpl.java`
#### Snippet
```java
    public static StreamingDataBlockHeaderImpl fromStream(InputStream stream) throws IOException {
        CountingInputStream countingStream = new CountingInputStream(stream);
        DataInputStream dis = new DataInputStream(countingStream);
        int magic = dis.readInt();
        if (magic != MAGIC_WORD) {
```

### IOResource
'DataInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockHeaderImpl.java`
#### Snippet
```java
    public static DataBlockHeader fromStream(InputStream stream) throws IOException {
        CountingInputStream countingStream = new CountingInputStream(stream);
        DataInputStream dis = new DataInputStream(countingStream);
        int magic = dis.readInt();
        if (magic != MAGIC_WORD) {
```

### IOResource
'PulsarLedgerManager' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManagerFactory.java`
#### Snippet
```java
            throws InterruptedException, IOException {
        @Cleanup
        PulsarLedgerManager ledgerManager = new PulsarLedgerManager(store, ledgerRootPath);

        /*
```

### IOResource
'ServerSocket' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
     */
    public void run() throws Exception {
        final ServerSocket serverSocket = new ServerSocket(port);

        while (true) {
```

### IOResource
'Socket' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
        log.info("Found {} clients:", clients.length);
        for (int i = 0; i < clients.length; ++i) {
            sockets[i] = new Socket(clients[i], clientPort);
            inputStreams[i] = new DataInputStream(sockets[i].getInputStream());
            outputStreams[i] = new DataOutputStream(sockets[i].getOutputStream());
```

### IOResource
'DataInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
        for (int i = 0; i < clients.length; ++i) {
            sockets[i] = new Socket(clients[i], clientPort);
            inputStreams[i] = new DataInputStream(sockets[i].getInputStream());
            outputStreams[i] = new DataOutputStream(sockets[i].getOutputStream());
            log.info("Connected to {}", clients[i]);
```

### IOResource
'DataOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
            sockets[i] = new Socket(clients[i], clientPort);
            inputStreams[i] = new DataInputStream(sockets[i].getInputStream());
            outputStreams[i] = new DataOutputStream(sockets[i].getOutputStream());
            log.info("Connected to {}", clients[i]);
        }
```

### IOResource
'FileOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/PackagesImpl.java`
#### Snippet
```java
            }

            FileChannel os = new FileOutputStream(destinyPath.toFile()).getChannel();
            RequestBuilder builder = get(webTarget.getUri().toASCIIString());

```

### IOResource
'FileOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/FunctionsImpl.java`
#### Snippet
```java
                file.createNewFile();
            }
            FileChannel os = new FileOutputStream(new File(destinationPath)).getChannel();

            RequestBuilder builder = get(target.getUri().toASCIIString());
```

### IOResource
'URLClassLoader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java

            File file  = new File(jarFilePath);
            ClassLoader cl = new URLClassLoader(new URL[]{ file.toURI().toURL() });
            Class cls = cl.loadClass(className);

```

## RuleId[ruleID=OptionalIsPresent]
### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TransactionsBase.java`
#### Snippet
```java
            }
            return topicFuture.thenCompose(optionalTopic -> {
                if (!optionalTopic.isPresent()) {
                    return FutureUtil.failedFuture(new RestException(NOT_FOUND, "Topic not found"));
                }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/cache/BundlesQuotas.java`
#### Snippet
```java
        return resourceQuotaCache.get(RESOURCE_QUOTA_ROOT + "/" + bundle)
                .thenCompose(optResourceQuota -> {
                    if (optResourceQuota.isPresent()) {
                        return CompletableFuture.completedFuture(optResourceQuota.get());
                    } else {
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/FaultInjectionMetadataStore.java`
#### Snippet
```java
    public CompletableFuture<Void> delete(String path, Optional<Long> expectedVersion) {
        Optional<MetadataStoreException> ex = programmedFailure(OperationType.DELETE, path);
        if (ex.isPresent()) {
            return FutureUtil.failedFuture(ex.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/FaultInjectionMetadataStore.java`
#### Snippet
```java
    public CompletableFuture<Void> deleteRecursive(String path) {
        Optional<MetadataStoreException> ex = programmedFailure(OperationType.DELETE, path);
        if (ex.isPresent()) {
            return FutureUtil.failedFuture(ex.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/FaultInjectionMetadataStore.java`
#### Snippet
```java
    public CompletableFuture<Stat> put(String path, byte[] value, Optional<Long> expectedVersion) {
        Optional<MetadataStoreException> ex = programmedFailure(OperationType.PUT, path);
        if (ex.isPresent()) {
            return FutureUtil.failedFuture(ex.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/FaultInjectionMetadataStore.java`
#### Snippet
```java
    public CompletableFuture<List<String>> getChildren(String path) {
        Optional<MetadataStoreException> ex = programmedFailure(OperationType.GET_CHILDREN, path);
        if (ex.isPresent()) {
            return FutureUtil.failedFuture(ex.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/FaultInjectionMetadataStore.java`
#### Snippet
```java
                                       EnumSet<CreateOption> options) {
        Optional<MetadataStoreException> ex = programmedFailure(OperationType.PUT, path);
        if (ex.isPresent()) {
            return FutureUtil.failedFuture(ex.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/FaultInjectionMetadataStore.java`
#### Snippet
```java
    public CompletableFuture<Boolean> exists(String path) {
        Optional<MetadataStoreException> ex = programmedFailure(OperationType.EXISTS, path);
        if (ex.isPresent()) {
            return FutureUtil.failedFuture(ex.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/FaultInjectionMetadataStore.java`
#### Snippet
```java
    public CompletableFuture<Optional<GetResult>> get(String path) {
        Optional<MetadataStoreException> ex = programmedFailure(OperationType.GET, path);
        if (ex.isPresent()) {
            return FutureUtil.failedFuture(ex.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java

    protected void registerSyncLister(Optional<MetadataEventSynchronizer> synchronizer) {
        if (synchronizer.isPresent()) {
            synchronizer.get().registerSyncListener(event -> {
                CompletableFuture<Void> result = new CompletableFuture<>();
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/MetadataStoreCacheLoader.java`
#### Snippet
```java
                try {
                    Optional<LoadManagerReport> loadReport = loadReportFuture.get();
                    if (loadReport.isPresent()) {
                        newAvailableBrokers.add(loadReport.get());
                    }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
            Optional<Map<String, NamespaceIsolationDataImpl>> data =
                    super.get(joinPath(BASE_CLUSTERS_PATH, cluster, NAMESPACE_ISOLATION_POLICIES));
            return data.isPresent() ? Optional.of(new NamespaceIsolationPolicies(data.get())) : Optional.empty();
        }

```

## RuleId[ruleID=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/WebTargets.java`
#### Snippet
```java

    static WebTarget addParts(WebTarget target, String[] parts) {
        if (parts != null && parts.length > 0) {
            for (String part : parts) {
                String encode;
```

## RuleId[ruleID=Java8MapForEach]
### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
            .collect(Collectors.groupingBy((m) -> m.group(1))); // group by name

        groups.entrySet().forEach((e) -> {
                Map<String, String> keyToFullKey = e.getValue().stream().collect(
                        Collectors.toMap(m -> m.group(2), m -> m.group(0)));
```

### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                List<CompletableFuture<Void>> subscriptionFutures = new ArrayList<>();

                stats.getSubscriptions().entrySet().forEach(e -> {
                    String subscription = e.getKey();
                    SubscriptionStats ss = e.getValue();
```

### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerBuilderImpl.java`
#### Snippet
```java
    @Override
    public ProducerBuilder<T> properties(@NonNull Map<String, String> properties) {
        properties.entrySet().forEach(entry ->
            checkArgument(StringUtils.isNotBlank(entry.getKey()) && StringUtils.isNotBlank(entry.getValue()),
                    "properties' key/value cannot be blank"));
```

### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerBuilderImpl.java`
#### Snippet
```java
    @Override
    public ConsumerBuilder<T> properties(@NonNull Map<String, String> properties) {
        properties.entrySet().forEach(entry ->
                checkArgument(
                        StringUtils.isNotBlank(entry.getKey()) && StringUtils.isNotBlank(entry.getValue()),
```

### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockImpl.java`
#### Snippet
```java

        // write entries
        this.indexEntries.entrySet().forEach(entry ->
                out.writeLong(entry.getValue().getEntryId())
                .writeInt(entry.getValue().getPartId())
```

### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/protocol/Commands.java`
#### Snippet
```java

        if (!metadata.isEmpty()) {
            metadata.entrySet().forEach(e -> subscribe.addMetadata()
                    .setKey(e.getKey())
                    .setValue(e.getValue()));
```

## RuleId[ruleID=FieldMayBeStatic]
### FieldMayBeStatic
Field `throttlingErrorMessage` may be 'static'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/LookupProxyHandler.java`
#### Snippet
```java

public class LookupProxyHandler {
    private final String throttlingErrorMessage = "Too many concurrent lookup and partitionsMetadata requests";
    private final ProxyConnection proxyConnection;
    private final BrokerDiscoveryProvider discoveryProvider;
```

### FieldMayBeStatic
Field `maxRetry` may be 'static'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
public class StreamingEntryReader implements AsyncCallbacks.ReadEntryCallback, WaitingEntryCallBack {

    private final int maxRetry = 3;

    // Queue for read request issued yet waiting for complete from managed ledger.
```

### FieldMayBeStatic
Field `rejectedRequestResetTimeSec` may be 'static'
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ClientCnx.java`
#### Snippet
```java

    private final int maxNumberOfRejectedRequestPerConnection;
    private final int rejectedRequestResetTimeSec = 60;
    protected final int protocolVersion;
    private final long operationTimeoutMs;
```

### FieldMayBeStatic
Field `protocol` may be 'static'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/KeyStoreSSLContext.java`
#### Snippet
```java
    private SSLContext sslContext;

    private final String protocol = DEFAULT_SSL_PROTOCOL;
    private final String kmfAlgorithm = DEFAULT_SSL_KEYMANGER_ALGORITHM;
    private final String tmfAlgorithm = DEFAULT_SSL_TRUSTMANAGER_ALGORITHM;
```

### FieldMayBeStatic
Field `tmfAlgorithm` may be 'static'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/KeyStoreSSLContext.java`
#### Snippet
```java
    private final String protocol = DEFAULT_SSL_PROTOCOL;
    private final String kmfAlgorithm = DEFAULT_SSL_KEYMANGER_ALGORITHM;
    private final String tmfAlgorithm = DEFAULT_SSL_TRUSTMANAGER_ALGORITHM;

    // only init vars, before using it, need to call createSSLContext to create ssl context.
```

### FieldMayBeStatic
Field `kmfAlgorithm` may be 'static'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/keystoretls/KeyStoreSSLContext.java`
#### Snippet
```java

    private final String protocol = DEFAULT_SSL_PROTOCOL;
    private final String kmfAlgorithm = DEFAULT_SSL_KEYMANGER_ALGORITHM;
    private final String tmfAlgorithm = DEFAULT_SSL_TRUSTMANAGER_ALGORITHM;

```

### FieldMayBeStatic
Field `expireAfterWriteMillis` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataCacheConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final long expireAfterWriteMillis = 2 * DEFAULT_CACHE_REFRESH_TIME_MILLIS;
}

```

### FieldMayBeStatic
Field `refreshAfterWriteMillis` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataCacheConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final long refreshAfterWriteMillis = DEFAULT_CACHE_REFRESH_TIME_MILLIS;

    /**
```

### FieldMayBeStatic
Field `batchingMaxSizeKb` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final int batchingMaxSizeKb = 128;

    /**
```

### FieldMayBeStatic
Field `metadataStoreName` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final String metadataStoreName = "";

    /**
```

### FieldMayBeStatic
Field `sessionTimeoutMillis` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final int sessionTimeoutMillis = 30_000;

    /**
```

### FieldMayBeStatic
Field `batchingMaxDelayMillis` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final int batchingMaxDelayMillis = 5;

    /**
```

### FieldMayBeStatic
Field `batchingEnabled` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final boolean batchingEnabled = true;

    /**
```

### FieldMayBeStatic
Field `configFilePath` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final String configFilePath = null;

    /**
```

### FieldMayBeStatic
Field `batchingMaxOperations` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final int batchingMaxOperations = 1_000;

    /**
```

### FieldMayBeStatic
Field `allowReadOnlyOperations` may be 'static'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/MetadataStoreConfig.java`
#### Snippet
```java
     */
    @Builder.Default
    private final boolean allowReadOnlyOperations = false;

    /**
```

### FieldMayBeStatic
Field `logDirectory` may be 'static'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntimeFactory.java`
#### Snippet
```java
    private String javaInstanceJarFile;
    private String pythonInstanceFile;
    private final String logDirectory = "logs/functions";
    private Resources functionInstanceMinResources;
    private Resources functionInstanceMaxResources;
```

## RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `GZipFiles` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/utils/GZipFiles.java`
#### Snippet
```java
 * gzip-formatted files.
 */
public class GZipFiles {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ZipFiles` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/utils/ZipFiles.java`
#### Snippet
```java
 * zip-formatted files.
 */
public class ZipFiles {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `JsonConverter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
 * Convert an AVRO GenericRecord to a JsonNode.
 */
public class JsonConverter {

    private static final Map<String, LogicalTypeConverter<?>> logicalTypeConverters = new HashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `MessageUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/MessageUtils.java`
#### Snippet
```java
 * A Simple class for mysql binlog message to parse.
 */
public class MessageUtils {

    public static Map<String, String> genColumn(CanalEntry.Column column) {
```

### UtilityClassWithoutPrivateConstructor
Class `JdbcUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcUtils.java`
#### Snippet
```java

@Slf4j
public class JdbcUtils {

    @Data(staticConstructor = "of")
```

### UtilityClassWithoutPrivateConstructor
Class `SecurityUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/SecurityUtil.java`
#### Snippet
```java
 * interfering with each other.
 */
public class SecurityUtil {
    public static final String HADOOP_SECURITY_AUTHENTICATION = "hadoop.security.authentication";
    public static final String KERBEROS = "kerberos";
```

### UtilityClassWithoutPrivateConstructor
Class `SecurityUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/SecurityUtil.java`
#### Snippet
```java
 * interfering with each other.
 */
public class SecurityUtil {
    public static final String HADOOP_SECURITY_AUTHENTICATION = "hadoop.security.authentication";
    public static final String KERBEROS = "kerberos";
```

### UtilityClassWithoutPrivateConstructor
Class `IOConfigUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/common/src/main/java/org/apache/pulsar/io/common/IOConfigUtils.java`
#### Snippet
```java

@Slf4j
public class IOConfigUtils {
    public static <T> T loadWithSecrets(Map<String, Object> map, Class<T> clazz, SourceContext sourceContext) {
        return loadWithSecrets(map, clazz, secretName -> sourceContext.getSecret(secretName));
```

### UtilityClassWithoutPrivateConstructor
Class `JsonConverter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
 * Convert an AVRO GenericRecord to a JsonNode.
 */
public class JsonConverter {

    private static final Map<String, LogicalTypeConverter<?>> logicalTypeConverters = new HashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `SerDeUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/debezium/core/src/main/java/org/apache/pulsar/io/debezium/SerDeUtils.java`
#### Snippet
```java

@Slf4j
public class SerDeUtils {
    public static Object deserialize(String objectBase64Encoded, ClassLoader classLoader) {
        byte[] data = Base64.getDecoder().decode(objectBase64Encoded);
```

### UtilityClassWithoutPrivateConstructor
Class `Utils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
import org.apache.pulsar.io.kinesis.json.JsonRecord;

public class Utils {

    private static final String PAYLOAD_FIELD = "payloadBase64";
```

### UtilityClassWithoutPrivateConstructor
Class `JsonConverter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
 * Convert an AVRO GenericRecord to a JsonNode.
 */
public class JsonConverter {

    private static Map<String, LogicalTypeConverter<?>> logicalTypeConverters = new HashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `RestClientFactory` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/RestClientFactory.java`
#### Snippet
```java

@Slf4j
public class RestClientFactory {

    private static final ObjectMapper MAPPER = new ObjectMapper();
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarSchemaToKafkaSchema` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/PulsarSchemaToKafkaSchema.java`
#### Snippet
```java

@Slf4j
public class PulsarSchemaToKafkaSchema {
    private static final ImmutableMap<SchemaType, Schema> pulsarSchemaTypeToKafkaSchema;
    private static final ImmutableSet<String> kafkaLogicalSchemas;
```

### UtilityClassWithoutPrivateConstructor
Class `KafkaConnectData` has only 'static' members, and lacks a 'private' constructor
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java

@Slf4j
public class KafkaConnectData {

    private static List<Object> arrayToList(Object nativeObject, Schema kafkaValueSchema) {
```

### UtilityClassWithoutPrivateConstructor
Class `ThreadDumpUtil` has only 'static' members, and lacks a 'private' constructor
in `buildtools/src/main/java/org/apache/pulsar/tests/ThreadDumpUtil.java`
#### Snippet
```java
 * https://raw.githubusercontent.com/apache/hadoop/master/hadoop-common-project/hadoop-common/src/test/java/org/apache/hadoop/test/TimedOutTestsListener.java
 */
public class ThreadDumpUtil {
    private static final String INDENT = "    ";

```

### UtilityClassWithoutPrivateConstructor
Class `PulsarFieldValueProviders` has only 'static' members, and lacks a 'private' constructor
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarFieldValueProviders.java`
#### Snippet
```java
 * custom FieldValueProvider for Pulsar.
 */
public class PulsarFieldValueProviders {

    public static FieldValueProvider doubleValueProvider(double value) {
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarConnectorUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorUtils.java`
#### Snippet
```java
 * A helper class containing repeatable logic used in the other classes.
 */
public class PulsarConnectorUtils {

    public static Schema parseSchema(String schemaJson) {
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarTransactionCoordinatorMetadataSetup` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarTransactionCoordinatorMetadataSetup.java`
#### Snippet
```java
 * partitioned topic for transaction coordinator assign.
 */
public class PulsarTransactionCoordinatorMetadataSetup {

    private static class Arguments {
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarInitialNamespaceSetup` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarInitialNamespaceSetup.java`
#### Snippet
```java
 * Setup the initial namespace of the cluster without startup the Pulsar broker.
 */
public class PulsarInitialNamespaceSetup {

    private static class Arguments {
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarVersionStarter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarVersionStarter.java`
#### Snippet
```java
 * Pulsar version entry point.
 */
public class PulsarVersionStarter {

    private static class Arguments {
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarClusterMetadataTeardown` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataTeardown.java`
#### Snippet
```java
 * Teardown the metadata for a existed Pulsar cluster.
 */
public class PulsarClusterMetadataTeardown {

    private static class Arguments {
```

### UtilityClassWithoutPrivateConstructor
Class `CmdUtility` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/CmdUtility.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class CmdUtility {
    private static final Logger LOG = LoggerFactory.getLogger(CmdUtility.class);
    private static final Charset UTF_8 = StandardCharsets.UTF_8;
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarClusterMetadataSetup` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
 * Setup the metadata for a new Pulsar cluster.
 */
public class PulsarClusterMetadataSetup {

    private static final int DEFAULT_BUNDLE_NUMBER = 16;
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarBrokerStarter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class PulsarBrokerStarter {

    private static ServiceConfiguration loadConfig(String configFile) throws Exception {
```

### UtilityClassWithoutPrivateConstructor
Class `TokensCliUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java
import org.apache.pulsar.common.util.RelativeTimeUtil;

public class TokensCliUtils {

    public static class Arguments {
```

### UtilityClassWithoutPrivateConstructor
Class `AllocatorStatsGenerator` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/AllocatorStatsGenerator.java`
#### Snippet
```java
import org.apache.pulsar.common.stats.AllocatorStats.PoolSubpageStats;

public class AllocatorStatsGenerator {
    public static AllocatorStats generate(String allocatorName) {
        PooledByteBufAllocator allocator;
```

### UtilityClassWithoutPrivateConstructor
Class `TransactionAggregator` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/TransactionAggregator.java`
#### Snippet
```java

@Slf4j
public class TransactionAggregator {

    private static final FastThreadLocal<AggregatedTransactionCoordinatorStats> localTransactionCoordinatorStats =
```

### UtilityClassWithoutPrivateConstructor
Class `PrometheusMetricsGenerator` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsGenerator.java`
#### Snippet
```java
 */
@Slf4j
public class PrometheusMetricsGenerator {
    private static volatile TimeWindow<ByteBuf> timeWindow;
    private static final int MAX_COMPONENTS = 64;
```

### UtilityClassWithoutPrivateConstructor
Class `PrometheusTextFormatUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/metrics/PrometheusTextFormatUtil.java`
#### Snippet
```java
 * Logic to write metrics in Prometheus text format.
 */
public class PrometheusTextFormatUtil {
    static void writeGauge(Writer w, String name, String cluster, SimpleGauge<? extends Number> gauge) {
        // Example:
```

### UtilityClassWithoutPrivateConstructor
Class `BrokerTool` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/tools/BrokerTool.java`
#### Snippet
```java
 * <b>broker-tool</b> is used for operations on a specific broker.
 */
public class BrokerTool {

    public static final String NAME = "broker-tool";
```

### UtilityClassWithoutPrivateConstructor
Class `NamespaceStatsAggregator` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/NamespaceStatsAggregator.java`
#### Snippet
```java

@Slf4j
public class NamespaceStatsAggregator {

    private static final FastThreadLocal<AggregatedNamespaceStats> localNamespaceStats =
```

### UtilityClassWithoutPrivateConstructor
Class `EntryFilterProvider` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/plugin/EntryFilterProvider.java`
#### Snippet
```java

@Slf4j
public class EntryFilterProvider {

    @VisibleForTesting
```

### UtilityClassWithoutPrivateConstructor
Class `ServiceUnitUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/ServiceUnitUtils.java`
#### Snippet
```java
 * <code>ServiceUnit</code> related metadata operations.
 */
public final class ServiceUnitUtils {
    /**
     * <code>ZooKeeper</code> root path for namespace ownership info.
```

### UtilityClassWithoutPrivateConstructor
Class `TransactionBatchedWriteValidator` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/validator/TransactionBatchedWriteValidator.java`
#### Snippet
```java
import org.apache.pulsar.broker.ServiceConfiguration;

public class TransactionBatchedWriteValidator {

    public static void validate(ServiceConfiguration configuration){
```

### UtilityClassWithoutPrivateConstructor
Class `LinuxInfoUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LinuxInfoUtils.java`
#### Snippet
```java

@Slf4j
public class LinuxInfoUtils {

    // CGROUP
```

### UtilityClassWithoutPrivateConstructor
Class `RawBatchConverter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/client/impl/RawBatchConverter.java`
#### Snippet
```java
import org.apache.pulsar.common.protocol.Commands;

public class RawBatchConverter {

    public static boolean isReadableBatch(RawMessage msg) {
```

### UtilityClassWithoutPrivateConstructor
Class `CompactorTool` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactorTool.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class CompactorTool {

    private static class Arguments {
```

### UtilityClassWithoutPrivateConstructor
Class `AuthenticationUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/AuthenticationUtil.java`
#### Snippet
```java
import org.apache.pulsar.common.util.ObjectMapperFactory;

public class AuthenticationUtil {
    public static Map<String, String> configureFromJsonString(String authParamsString) throws IOException {
        ObjectMapper jsonMapper = ObjectMapperFactory.create();
```

### UtilityClassWithoutPrivateConstructor
Class `MessagePayloadUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MessagePayloadUtils.java`
#### Snippet
```java
import org.apache.pulsar.client.api.MessagePayload;

public class MessagePayloadUtils {

    public static ByteBuf convertToByteBuf(final MessagePayload payload) {
```

### UtilityClassWithoutPrivateConstructor
Class `ReaderInterceptorUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ReaderInterceptorUtil.java`
#### Snippet
```java
 * ReaderInterceptorUtil is used to wrap ReaderInterceptor by ConsumerInterceptor.
 */
public class ReaderInterceptorUtil {

    public static <T> ConsumerInterceptors<T> convertToConsumerInterceptors(
```

### UtilityClassWithoutPrivateConstructor
Class `AuthenticationFactoryOAuth2` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/AuthenticationFactoryOAuth2.java`
#### Snippet
```java
 * for OAuth 2.0 authentication methods.
 */
public final class AuthenticationFactoryOAuth2 {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ProtobufNativeSchemaUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java
 * {@link org.apache.pulsar.common.protocol.schema.ProtobufNativeSchemaData}.
 */
public class ProtobufNativeSchemaUtils {

    public static byte[] serialize(Descriptors.Descriptor descriptor) {
```

### UtilityClassWithoutPrivateConstructor
Class `SchemaUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/util/SchemaUtil.java`
#### Snippet
```java
import org.apache.pulsar.common.schema.SchemaType;

public class SchemaUtil {

    public static boolean getJsr310ConversionEnabledFromSchemaInfo(SchemaInfo schemaInfo) {
```

### UtilityClassWithoutPrivateConstructor
Class `MessageIdUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/MessageIdUtils.java`
#### Snippet
```java
import org.apache.pulsar.client.impl.MessageIdImpl;

public class MessageIdUtils {
    public static final long getOffset(MessageId messageId) {
        MessageIdImpl msgId = (MessageIdImpl) messageId;
```

### UtilityClassWithoutPrivateConstructor
Class `ConsumerName` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/ConsumerName.java`
#### Snippet
```java
import org.apache.commons.codec.digest.DigestUtils;

public class ConsumerName {
    public static String generateRandomName() {
        return DigestUtils.sha1Hex(UUID.randomUUID().toString()).substring(0, 5);
```

### UtilityClassWithoutPrivateConstructor
Class `RetryUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/RetryUtil.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class RetryUtil {
    private static final Logger log = LoggerFactory.getLogger(RetryUtil.class);

```

### UtilityClassWithoutPrivateConstructor
Class `ZStdRawCompressor` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/io/airlift/compress/zstd/ZStdRawCompressor.java`
#### Snippet
```java
 * Expose ZstdFrameCompressor which is a package protected class.
 */
public class ZStdRawCompressor {
    public static int compress(long inputAddress, long inputLimit,
            long outputAddress, long outputLimit, int compressionLevel) {
```

### UtilityClassWithoutPrivateConstructor
Class `FileUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/FileUtils.java`
#### Snippet
```java
 */
@Slf4j
public class FileUtils {

    public static final long MILLIS_BETWEEN_ATTEMPTS = 50L;
```

### UtilityClassWithoutPrivateConstructor
Class `NarUnpacker` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarUnpacker.java`
#### Snippet
```java
 */
@Slf4j
public class NarUnpacker {
    private static final ConcurrentHashMap<String, Object> CURRENT_JVM_FILE_LOCKS = new ConcurrentHashMap<>();

```

### UtilityClassWithoutPrivateConstructor
Class `SaslConstants` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/SaslConstants.java`
#### Snippet
```java
 * SASL Constants.
 */
public class SaslConstants {

    public static final String AUTH_METHOD_NAME = "sasl";
```

### UtilityClassWithoutPrivateConstructor
Class `Codec` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Codec.java`
#### Snippet
```java
/**
 */
public class Codec {
    private static final Logger LOG = LoggerFactory.getLogger(Codec.class);

```

### UtilityClassWithoutPrivateConstructor
Class `ClassLoaderUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ClassLoaderUtils.java`
#### Snippet
```java
 */
@Slf4j
public class ClassLoaderUtils {
    /**
     * Load a jar.
```

### UtilityClassWithoutPrivateConstructor
Class `ShutdownUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ShutdownUtil.java`
#### Snippet
```java

@Slf4j
public class ShutdownUtil {
    private static final Method log4j2ShutdownMethod;

```

### UtilityClassWithoutPrivateConstructor
Class `PortManager` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/PortManager.java`
#### Snippet
```java
import java.util.Set;

public class PortManager {

    private static final Set<Integer> PORTS = new HashSet<>();
```

### UtilityClassWithoutPrivateConstructor
Class `ObjectMapperFactory` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ObjectMapperFactory.java`
#### Snippet
```java
@SuppressWarnings("checkstyle:JavadocType")
@Slf4j
public class ObjectMapperFactory {
    public static ObjectMapper create() {
        ObjectMapper mapper = new ObjectMapper();
```

### UtilityClassWithoutPrivateConstructor
Class `DirectMemoryUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/DirectMemoryUtils.java`
#### Snippet
```java
// CHECKSTYLE.ON: IllegalImport

public class DirectMemoryUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `FieldParser` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
 *
 */
public final class FieldParser {

    private static final Map<String, Method> CONVERTERS = new HashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `Reflections` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
 * Utils related to reflections.
 */
public class Reflections {

    private static final Map<Class<?>, Constructor<?>> constructorCache =
```

### UtilityClassWithoutPrivateConstructor
Class `SafeCollectionUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SafeCollectionUtils.java`
#### Snippet
```java
 * Safe collection utils.
 */
public class SafeCollectionUtils {

    public static List<Long> longArrayToList(long[] array) {
```

### UtilityClassWithoutPrivateConstructor
Class `ThreadDumpUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ThreadDumpUtil.java`
#### Snippet
```java
 * https://raw.githubusercontent.com/apache/hadoop/master/hadoop-common-project/hadoop-common/src/test/java/org/apache/hadoop/test/TimedOutTestsListener.java
 */
public class ThreadDumpUtil {
    private static final String INDENT = "    ";

```

### UtilityClassWithoutPrivateConstructor
Class `NumberFormat` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/NumberFormat.java`
#### Snippet
```java
 * Custom number formatter for {@code io.netty.buffer.ByteBuf}.
 */
public class NumberFormat {

    static void format(ByteBuf out, long num) {
```

### UtilityClassWithoutPrivateConstructor
Class `NettyFutureUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/netty/NettyFutureUtil.java`
#### Snippet
```java
 * Contains utility methods for working with Netty Futures.
 */
public class NettyFutureUtil {
    /**
     * Converts a Netty {@link Future} to {@link CompletableFuture}.
```

### UtilityClassWithoutPrivateConstructor
Class `EventLoopUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/netty/EventLoopUtil.java`
#### Snippet
```java
@SuppressWarnings("checkstyle:JavadocType")
@Slf4j
public class EventLoopUtil {

    private static final String ENABLE_IO_URING = "pulsar.enableUring";
```

### UtilityClassWithoutPrivateConstructor
Class `FutureUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FutureUtil.java`
#### Snippet
```java
 * This class is aimed at simplifying work with {@code CompletableFuture}.
 */
public class FutureUtil {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `SystemTopicNames` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/naming/SystemTopicNames.java`
#### Snippet
```java
 * Encapsulate the parsing of the completeTopicName name.
 */
public class SystemTopicNames {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `SinkStatusUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/SinkStatusUtil.java`
#### Snippet
```java
import org.apache.pulsar.common.util.ObjectMapperFactory;

public class SinkStatusUtil {

    public static SinkStatus decode(String json) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `SourceStatusUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/SourceStatusUtil.java`
#### Snippet
```java
import org.apache.pulsar.common.util.ObjectMapperFactory;

public class SourceStatusUtil {

    public static SourceStatus decode(String json) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `PoliciesUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/PoliciesUtil.java`
#### Snippet
```java
 * Utils of Pulsar policies.
 */
public class PoliciesUtil {

    public static BundlesData defaultBundle() {
```

### UtilityClassWithoutPrivateConstructor
Class `FunctionStatusUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/data/FunctionStatusUtil.java`
#### Snippet
```java
import org.apache.pulsar.common.util.ObjectMapperFactory;

public class FunctionStatusUtil {
    public static FunctionStatus decode(String json) throws IOException {
        return ObjectMapperFactory.getThreadLocal().readValue(json, FunctionStatus.class);
```

### UtilityClassWithoutPrivateConstructor
Class `PolicyPath` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/path/PolicyPath.java`
#### Snippet
```java
 * Policy path utilities.
 */
public class PolicyPath {

    public static String path(String... parts) {
```

### UtilityClassWithoutPrivateConstructor
Class `SecurityUtility` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/SecurityUtility.java`
#### Snippet
```java
 */
@Slf4j
public class SecurityUtility {

    public static final Provider BC_PROVIDER = getProvider();
```

### UtilityClassWithoutPrivateConstructor
Class `BrokerEntryMetadataUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/intercept/BrokerEntryMetadataUtils.java`
#### Snippet
```java
 * A tool class for loading BrokerEntryMetadataInterceptor classes.
 */
public class BrokerEntryMetadataUtils<T> {

    private static final Logger log = LoggerFactory.getLogger(BrokerEntryMetadataUtils.class);
```

### UtilityClassWithoutPrivateConstructor
Class `Utils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-common/src/main/java/org/apache/pulsar/common/functions/Utils.java`
#### Snippet
```java
 * Helper class to work with configuration.
 */
public class Utils {
    public static final String HTTP = "http";
    public static final String FILE = "file";
```

### UtilityClassWithoutPrivateConstructor
Class `PositionAckSetUtil` has only 'static' members, and lacks a 'private' constructor
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/PositionAckSetUtil.java`
#### Snippet
```java
import org.apache.pulsar.common.util.collections.BitSetRecyclable;

public class PositionAckSetUtil {

    //This method is to compare two ack set whether overlap or not
```

### UtilityClassWithoutPrivateConstructor
Class `SafeRun` has only 'static' members, and lacks a 'private' constructor
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/SafeRun.java`
#### Snippet
```java
 * Static builders for {@link SafeRunnable}s.
 */
public class SafeRun {
    public static SafeRunnable safeRun(Runnable runnable) {
        return new SafeRunnable() {
```

### UtilityClassWithoutPrivateConstructor
Class `Futures` has only 'static' members, and lacks a 'private' constructor
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/Futures.java`
#### Snippet
```java
 * Conveniences to use with {@link CompletableFuture}.
 */
public class Futures {

    public static final CompletableFuture<Void> NULL_PROMISE = CompletableFuture.completedFuture(null);
```

### UtilityClassWithoutPrivateConstructor
Class `OffloaderUtils` has only 'static' members, and lacks a 'private' constructor
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/offload/OffloaderUtils.java`
#### Snippet
```java
 */
@Slf4j
public class OffloaderUtils {

    private static final String PULSAR_OFFLOADER_SERVICE_NAME = "pulsar-offloader.yaml";
```

### UtilityClassWithoutPrivateConstructor
Class `RetryMessageUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/RetryMessageUtil.java`
#### Snippet
```java
package org.apache.pulsar.client.util;

public class RetryMessageUtil {

    public static final String SYSTEM_PROPERTY_RECONSUMETIMES = "RECONSUMETIMES";
```

### UtilityClassWithoutPrivateConstructor
Class `DataBlockUtils` has only 'static' members, and lacks a 'private' constructor
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockUtils.java`
#### Snippet
```java
 *  </p>
 */
public class DataBlockUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `MetadataStoreFactoryImpl` has only 'static' members, and lacks a 'private' constructor
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/MetadataStoreFactoryImpl.java`
#### Snippet
```java
import org.apache.pulsar.metadata.api.extended.MetadataStoreExtended;

public class MetadataStoreFactoryImpl {

    public static MetadataStore create(String metadataURL, MetadataStoreConfig metadataStoreConfig) throws
```

### UtilityClassWithoutPrivateConstructor
Class `ComponentTypeUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/ComponentTypeUtils.java`
#### Snippet
```java
import org.apache.pulsar.functions.proto.Function.FunctionDetails;

public class ComponentTypeUtils {
    public static String toString(FunctionDetails.ComponentType componentType) {
        switch (componentType) {
```

### UtilityClassWithoutPrivateConstructor
Class `FunctionMetaDataUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionMetaDataUtils.java`
#### Snippet
```java
import org.apache.pulsar.functions.proto.Function;

public class FunctionMetaDataUtils {

    public static boolean canChangeState(Function.FunctionMetaData functionMetaData, int instanceId,
```

### UtilityClassWithoutPrivateConstructor
Class `Exceptions` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/Exceptions.java`
#### Snippet
```java
 * Util to handle exceptions.
 */
public class Exceptions {

    public static void rethrowIOException(Throwable cause)
```

### UtilityClassWithoutPrivateConstructor
Class `SourceConfigUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java

@Slf4j
public class SourceConfigUtils {

    @Getter
```

### UtilityClassWithoutPrivateConstructor
Class `ResourceConfigUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/ResourceConfigUtils.java`
#### Snippet
```java
import org.apache.pulsar.common.functions.Resources;

public class ResourceConfigUtils {
    public static void validate(Resources resources) {
        Double cpu = resources.getCpu();
```

### UtilityClassWithoutPrivateConstructor
Class `ValidatorUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/ValidatorUtils.java`
#### Snippet
```java

@Slf4j
public class ValidatorUtils {
    private static final String DEFAULT_SERDE = "org.apache.pulsar.functions.api.utils.DefaultSerDe";

```

### UtilityClassWithoutPrivateConstructor
Class `CryptoUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/CryptoUtils.java`
#### Snippet
```java
import org.apache.pulsar.functions.proto.Function;

public final class CryptoUtils {

    public static Function.CryptoSpec convert(CryptoConfig config) {
```

### UtilityClassWithoutPrivateConstructor
Class `WindowConfigUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/WindowConfigUtils.java`
#### Snippet
```java
import org.apache.pulsar.common.functions.WindowConfig;

public class WindowConfigUtils {

    public static final long DEFAULT_MAX_LAG_MS = 0; // no lag
```

### UtilityClassWithoutPrivateConstructor
Class `FunctionClassLoaders` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/functioncache/FunctionClassLoaders.java`
#### Snippet
```java
 * Class loaders for loading function code.
 */
public class FunctionClassLoaders {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `SinkConfigUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java

@Slf4j
public class SinkConfigUtils {

    @Getter
```

### UtilityClassWithoutPrivateConstructor
Class `FunctionWorkerStarter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionWorkerStarter.java`
#### Snippet
```java
 */
@Slf4j
public class FunctionWorkerStarter {

    private static class WorkerArguments {
```

### UtilityClassWithoutPrivateConstructor
Class `FunctionConfigUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java

@Slf4j
public class FunctionConfigUtils {

    @Getter
```

### UtilityClassWithoutPrivateConstructor
Class `FunctionsStatsGenerator` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionsStatsGenerator.java`
#### Snippet
```java
 * A class to generate stats for pulsar functions running on this broker.
 */
public class FunctionsStatsGenerator {

    private static final Logger log = LoggerFactory.getLogger(FunctionsStatsGenerator.class);
```

### UtilityClassWithoutPrivateConstructor
Class `WorkerServiceLoader` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/WorkerServiceLoader.java`
#### Snippet
```java
 */
@Slf4j
public class WorkerServiceLoader {

    static final String PULSAR_FN_WORKER_DEFINITION_FILE = "pulsar-functions-worker-service.yml";
```

### UtilityClassWithoutPrivateConstructor
Class `FunctionAuthUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/FunctionAuthUtils.java`
#### Snippet
```java
import org.apache.pulsar.functions.proto.Function;

public final class FunctionAuthUtils {

    public static FunctionAuthData getFunctionAuthData(
```

### UtilityClassWithoutPrivateConstructor
Class `RuntimeUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/RuntimeUtils.java`
#### Snippet
```java
 */
@Slf4j
public class RuntimeUtils {

    private static final String FUNCTIONS_EXTRA_DEPS_PROPERTY = "pulsar.functions.extra.dependencies.dir";
```

### UtilityClassWithoutPrivateConstructor
Class `PrometheusTextFormat` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/PrometheusTextFormat.java`
#### Snippet
```java
 * Provide Prometheus text format for a collection of metrics, without the HELP string.
 */
public class PrometheusTextFormat {
    /**
     * Write out the text version 0.0.4 of the given MetricFamilySamples.
```

### UtilityClassWithoutPrivateConstructor
Class `WindowUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowUtils.java`
#### Snippet
```java
package org.apache.pulsar.functions.windowing;

public class WindowUtils {
    public static String getFullyQualifiedName(String tenant, String namespace, String name) {
        return String.format("%s/%s/%s", tenant, namespace, name);
```

### UtilityClassWithoutPrivateConstructor
Class `JavaInstanceMain` has only 'static' members, and lacks a 'private' constructor
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
 *
 */
public class JavaInstanceMain {

    private static final String FUNCTIONS_INSTANCE_CLASSPATH = "pulsar.functions.instance.classpath";
```

### UtilityClassWithoutPrivateConstructor
Class `WebSocketServiceStarter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/service/WebSocketServiceStarter.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class WebSocketServiceStarter {
    private static class Arguments {
        @Parameter(description = "config file")
```

### UtilityClassWithoutPrivateConstructor
Class `AuthenticationFactory` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/AuthenticationFactory.java`
#### Snippet
```java
@InterfaceAudience.LimitedPrivate
@InterfaceStability.Stable
public final class AuthenticationFactory {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `PropertiesUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/internal/PropertiesUtils.java`
#### Snippet
```java
 * Internal utility methods for filtering and mapping {@link Properties} objects.
 */
public class PropertiesUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `DefaultImplementation` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/internal/DefaultImplementation.java`
#### Snippet
```java
 * it is not meant to be used by client applications.</b>
 */
public class DefaultImplementation {
    private static final PulsarClientImplementationBinding IMPLEMENTATION;
    static {
```

### UtilityClassWithoutPrivateConstructor
Class `CmdGenerateDocumentation` has only 'static' members, and lacks a 'private' constructor
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/CmdGenerateDocumentation.java`
#### Snippet
```java

@Slf4j
public class CmdGenerateDocumentation {

    @Parameters(commandDescription = "Generate documentation automatically.")
```

### UtilityClassWithoutPrivateConstructor
Class `ManagedLedgerWriter` has only 'static' members, and lacks a 'private' constructor
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class ManagedLedgerWriter {

    private static final ExecutorService executor = Executors
```

### UtilityClassWithoutPrivateConstructor
Class `PerformanceConsumer` has only 'static' members, and lacks a 'private' constructor
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceConsumer.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class PerformanceConsumer {
    private static final LongAdder messagesReceived = new LongAdder();
    private static final LongAdder bytesReceived = new LongAdder();
```

### UtilityClassWithoutPrivateConstructor
Class `PerformanceReader` has only 'static' members, and lacks a 'private' constructor
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceReader.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class PerformanceReader {
    private static final LongAdder messagesReceived = new LongAdder();
    private static final LongAdder bytesReceived = new LongAdder();
```

### UtilityClassWithoutPrivateConstructor
Class `TransactionUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/util/TransactionUtil.java`
#### Snippet
```java
 * An transaction util of {@link TransactionUtil}.
 */
public class TransactionUtil {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `PerformanceTransaction` has only 'static' members, and lacks a 'private' constructor
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class PerformanceTransaction {


```

### UtilityClassWithoutPrivateConstructor
Class `PerformanceProducer` has only 'static' members, and lacks a 'private' constructor
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceProducer.java`
#### Snippet
```java
 * A client program to test pulsar producer performance.
 */
public class PerformanceProducer {

    private static final ExecutorService executor = Executors
```

### UtilityClassWithoutPrivateConstructor
Class `IOUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/IOUtils.java`
#### Snippet
```java
import java.io.IOException;

public class IOUtils {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `CmdUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/CmdUtils.java`
#### Snippet
```java
import org.apache.pulsar.common.util.ObjectMapperFactory;

public class CmdUtils {
    public static <T> T loadConfig(String file, Class<T> clazz) throws IOException {
        try {
```

### UtilityClassWithoutPrivateConstructor
Class `SchemaExtractor` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/SchemaExtractor.java`
#### Snippet
```java
import org.apache.pulsar.client.api.schema.SchemaDefinition;

public class SchemaExtractor {

    public static String getJsonSchemaInfo(SchemaDefinition schemaDefinition) {
```

### UtilityClassWithoutPrivateConstructor
Class `CustomCommandFactoryProvider` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/CustomCommandFactoryProvider.java`
#### Snippet
```java

@Slf4j
public class CustomCommandFactoryProvider {

    @VisibleForTesting
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigurationStringUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/bookie/rackawareness/ConfigurationStringUtil.java`
#### Snippet
```java
import java.util.List;

public class ConfigurationStringUtil {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ServiceConfigurationUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfigurationUtils.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class ServiceConfigurationUtils {

    private static final Logger LOG = LoggerFactory.getLogger(ServiceConfigurationUtils.class);
```

### UtilityClassWithoutPrivateConstructor
Class `JettyRequestLogFactory` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/JettyRequestLogFactory.java`
#### Snippet
```java
 * Class to standardize initialization of a Jetty request logger for all pulsar components.
 */
public class JettyRequestLogFactory {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `JettySslContextFactory` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java

@Slf4j
public class JettySslContextFactory {
    static {
        // DO NOT EDIT - Load Conscrypt provider
```

### UtilityClassWithoutPrivateConstructor
Class `PrometheusMetricsGeneratorUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsGeneratorUtils.java`
#### Snippet
```java
 * Format specification can be found at {@link https://prometheus.io/docs/instrumenting/exposition_formats/}
 */
public class PrometheusMetricsGeneratorUtils {

    public static void generate(String cluster, OutputStream out,
```

### UtilityClassWithoutPrivateConstructor
Class `BindAddressValidator` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/validator/BindAddressValidator.java`
#### Snippet
```java
 * Validates bind address configurations.
 */
public class BindAddressValidator {

    private static final Pattern BIND_ADDRESSES_PATTERN = Pattern.compile("(?<name>\\w+):(?<url>.+)$");
```

### UtilityClassWithoutPrivateConstructor
Class `MultipleListenerValidator` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/validator/MultipleListenerValidator.java`
#### Snippet
```java
 * Validates multiple listener address configurations.
 */
public final class MultipleListenerValidator {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `AuthenticationMetrics` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/metrics/AuthenticationMetrics.java`
#### Snippet
```java
import io.prometheus.client.Counter;

public class AuthenticationMetrics {
    private static final Counter authSuccessMetrics = Counter.build()
            .name("pulsar_authentication_success_total")
```

### UtilityClassWithoutPrivateConstructor
Class `PulsarConfigurationLoader` has only 'static' members, and lacks a 'private' constructor
in `pulsar-broker-common/src/main/java/org/apache/pulsar/common/configuration/PulsarConfigurationLoader.java`
#### Snippet
```java
 *
 */
public class PulsarConfigurationLoader {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `AutoFailoverPolicyFactory` has only 'static' members, and lacks a 'private' constructor
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/impl/AutoFailoverPolicyFactory.java`
#### Snippet
```java
 * Factory to generate an instance of {@link AutoFailoverPolicy}.
 */
public class AutoFailoverPolicyFactory {

    public static AutoFailoverPolicy create(AutoFailoverPolicyData policyData) {
```

### UtilityClassWithoutPrivateConstructor
Class `ValidatorParams` has only 'static' members, and lacks a 'private' constructor
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ConfigValidationAnnotations.java`
#### Snippet
```java
     * Field names for annotations.
     */
    public static class ValidatorParams {
        static final String VALIDATOR_CLASS = "validatorClass";
        static final String TYPE = "type";
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigValidationUtils` has only 'static' members, and lacks a 'private' constructor
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ConfigValidationUtils.java`
#### Snippet
```java
 * Helper methods for ConfigValidationAnnotations.
 */
public class ConfigValidationUtils {
    /**
     * Returns a new NestableFieldValidator for a given class.
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigValidation` has only 'static' members, and lacks a 'private' constructor
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ConfigValidation.java`
#### Snippet
```java
 * The class that does the validation of all the members of a given object.
 */
public class ConfigValidation {

    private static final Class DEFAULT_ANNOTATION_CLASS = ConfigValidationAnnotations.class;
```

### UtilityClassWithoutPrivateConstructor
Class `PackageMetadataUtil` has only 'static' members, and lacks a 'private' constructor
in `pulsar-package-management/core/src/main/java/org/apache/pulsar/packages/management/core/common/PackageMetadataUtil.java`
#### Snippet
```java
import org.apache.pulsar.packages.management.core.exceptions.PackagesManagementException;

public class PackageMetadataUtil {
    public static PackageMetadata fromBytes(byte[] bytes) throws PackagesManagementException.MetadataFormatException {
        try {
```

## RuleId[ruleID=DataFlowIssue]
### DataFlowIssue
Passing a non-null argument to `Optional`
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/http/NettyHttpServerHandler.java`
#### Snippet
```java
            ByteBuf content = httpContent.content();
            if (content.isReadable()) {
                nettySource.consume(new NettyHttpRecord(Optional.ofNullable(""),
                        content.toString(CharsetUtil.UTF_8).getBytes(CharsetUtil.UTF_8)));
            }
```

### DataFlowIssue
Dereference of `lastException` may produce `NullPointerException`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/RandomExponentialRetry.java`
#### Snippet
```java
            }
        }
        throw lastException;
    }

```

### DataFlowIssue
Method invocation `value` may produce `NullPointerException`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/elastic/ElasticSearchJavaRestClient.java`
#### Snippet
```java
    public long totalHits(String indexName, String query) throws IOException {
        final SearchResponse<Map> searchResponse = search(indexName, query);
        return searchResponse.hits().total().value();
    }

```

### DataFlowIssue
Method invocation `getSchemaInfo` may produce `NullPointerException`
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                if (valueSchema != null) {
                    if (elasticSearchConfig.isCopyKeyFields()
                            && (keySchema.getSchemaInfo().getType().equals(SchemaType.AVRO)
                            || keySchema.getSchemaInfo().getType().equals(SchemaType.JSON))) {
                        JsonNode keyNode = extractJsonNode(keySchema, key);
```

### DataFlowIssue
Argument `keyNode` might be null
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                        JsonNode keyNode = extractJsonNode(keySchema, key);
                        JsonNode valueNode = extractJsonNode(valueSchema, value);
                        doc = stringify(JsonConverter.topLevelMerge(keyNode, valueNode));
                    } else {
                        doc = stringifyValue(valueSchema, value);
```

### DataFlowIssue
Condition `keyConverter instanceof AvroConverter` is redundant and can be replaced with a null check
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
                .newInstance();

        if (keyConverter instanceof AvroConverter) {
            keyConverter = new AvroConverter(new MockSchemaRegistryClient());
            config.put(AbstractKafkaAvroSerDeConfig.SCHEMA_REGISTRY_URL_CONFIG, "mock");
```

### DataFlowIssue
Condition `valueConverter instanceof AvroConverter` is redundant and can be replaced with a null check
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
            config.put(AbstractKafkaAvroSerDeConfig.SCHEMA_REGISTRY_URL_CONFIG, "mock");
        }
        if (valueConverter instanceof AvroConverter) {
            valueConverter = new AvroConverter(new MockSchemaRegistryClient());
            config.put(AbstractKafkaAvroSerDeConfig.SCHEMA_REGISTRY_URL_CONFIG, "mock");
```

### DataFlowIssue
Variable is already assigned to this value
in `testmocks/src/main/java/org/apache/bookkeeper/client/BookKeeperTestClient.java`
#### Snippet
```java
        super(conf, zkc, null, new UnpooledByteBufAllocator(false),
                NullStatsLogger.INSTANCE, null, null, null);
        this.statsProvider = statsProvider;
    }

```

### DataFlowIssue
Argument `committedOffsets` might be null
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
        } catch (Throwable t) {
            log.error("error flushing pending records", t);
            ackUntil(lastNotFlushed, committedOffsets, Record::fail);
        } finally {
            isFlushRunning.compareAndSet(true, false);
```

### DataFlowIssue
Variable is already assigned to this value
in `buildtools/src/main/java/org/apache/pulsar/tests/FastThreadLocalStateCleaner.java`
#### Snippet
```java
                threadLocalMapField = null;
                indexedVariablesField = null;
                unsetObject = null;
            }
        }
```

### DataFlowIssue
Casting `value` to `Double` may produce `ClassCastException`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java

        if (type instanceof DoubleType) {
            type.writeDouble(blockBuilder, (Double) value);
            return;
        }
```

### DataFlowIssue
Casting `value` to `Float` may produce `ClassCastException`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java

        if (type instanceof RealType) {
            type.writeLong(blockBuilder, floatToIntBits((Float) value));
            return;
        }
```

### DataFlowIssue
Casting `value` to `Double` may produce `ClassCastException`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java

        if (type instanceof DoubleType) {
            type.writeDouble(blockBuilder, (Double) value);
            return;
        }
```

### DataFlowIssue
Casting `value` to `Float` may produce `ClassCastException`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java

        if (type instanceof RealType) {
            type.writeLong(blockBuilder, floatToIntBits((Float) value));
            return;
        }
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/DirectProxyHandler.java`
#### Snippet
```java
                    int port = targetBrokerAddress.getPort();
                    SslHandler handler = tlsEnabledWithKeyStore
                            ? new SslHandler(clientSSLContextAutoRefreshBuilder.get().createSSLEngine(host, port))
                            : clientSslCtxRefresher.get().newHandler(ch.alloc(), host, port);
                    if (tlsHostnameVerificationEnabled) {
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/DirectProxyHandler.java`
#### Snippet
```java
                    SslHandler handler = tlsEnabledWithKeyStore
                            ? new SslHandler(clientSSLContextAutoRefreshBuilder.get().createSSLEngine(host, port))
                            : clientSslCtxRefresher.get().newHandler(ch.alloc(), host, port);
                    if (tlsHostnameVerificationEnabled) {
                        SecurityUtility.configureSSLHandler(handler);
```

### DataFlowIssue
Method invocation `decodeRow` may produce `NullPointerException`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodedKey;
            if (keyColumnHandles.size() > 0) {
                decodedKey = keyDecoder.decodeRow(keyByteBuf);
                decodedKey.ifPresent(currentRowValuesMap::putAll);
            }
```

### DataFlowIssue
Method invocation `decodeRow` may produce `NullPointerException`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarRecordCursor.java`
#### Snippet
```java
            if (valueColumnHandles.size() > 0) {
                Optional<Map<DecoderColumnHandle, FieldValueProvider>> decodedValue =
                        valueDecoder.decodeRow(valueByteBuf);
                decodedValue.ifPresent(currentRowValuesMap::putAll);
            }
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/extensions/ProxyExtensions.java`
#### Snippet
```java
            }

            if (!extension.accept(extensionName)) {
                extension.close();
                log.error("Malformed extension found for extensionName `" + extensionName + "`");
```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "The port for serving http requests"
    )
    private Optional<Integer> webServicePort = Optional.ofNullable(8080);
    @FieldContext(
        category = CATEGORY_SERVER,
```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                    + " 2: Parse and log channel info, command info and message body"
    )
    private Optional<Integer> proxyLogLevel = Optional.ofNullable(0);

    @FieldContext(
```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        doc = "The port for serving binary protobuf request"
    )
    private Optional<Integer> servicePort = Optional.ofNullable(6650);
    @FieldContext(
        category = CATEGORY_SERVER,
```

### DataFlowIssue
Method invocation `getSchema` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
                        KeyValueEncodingType.SEPARATED);
                return SchemaData.builder()
                        .data(schemaInfo.getSchema())
                        .isDeleted(false)
                        .user("Rest Producer")
```

### DataFlowIssue
Casting `message` to `MessageImpl` may produce `ClassCastException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
        checkArgument(message instanceof MessageImpl, "Message must be type of MessageImpl.");

        MessageImpl msg = (MessageImpl) message;
        MessageMetadata messageMetadata = msg.getMessageBuilder();
        ByteBuf payload = msg.getDataBuffer();
```

### DataFlowIssue
Method invocation `getMessageBuilder` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java

        MessageImpl msg = (MessageImpl) message;
        MessageMetadata messageMetadata = msg.getMessageBuilder();
        ByteBuf payload = msg.getDataBuffer();
        messageMetadata.setCompression(CompressionCodecProvider.convertToWireProtocol(CompressionType.NONE));
```

### DataFlowIssue
Variable is already assigned to this value
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
                                out.write(',');
                            } else {
                                first = true;
                            }
                            String json = ObjectMapperFactory.getThreadLocal().writeValueAsString(data);
```

### DataFlowIssue
Casting `topic` to `PersistentTopic` may produce `ClassCastException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/NamespaceStatsAggregator.java`
#### Snippet
```java
                    stats.compactionLatencyBuckets.addAll(compactionRecord.getCompactionLatencyStats());
                    stats.compactionLatencyBuckets.refresh();
                    PersistentTopic persistentTopic = (PersistentTopic) topic;
                    Optional<CompactedTopicContext> compactedTopicContext = persistentTopic
                            .getCompactedTopicContext();
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java
            EntryFilter.FilterResult filterResult = runFiltersForEntry(entry, msgMetadata, consumer);
            if (filterResult == EntryFilter.FilterResult.REJECT) {
                entriesToFiltered.add(entry.getPosition());
                entries.set(i, null);
                // FilterResult will be always `ACCEPTED` when there is No Filter
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractBaseDispatcher.java`
#### Snippet
```java
                continue;
            } else if (filterResult == EntryFilter.FilterResult.RESCHEDULE) {
                entriesToRedeliver.add((PositionImpl) entry.getPosition());
                entries.set(i, null);
                // FilterResult will be always `ACCEPTED` when there is No Filter
```

### DataFlowIssue
Method invocation `sendMessages` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
                        batchIndexesAcks, cursor, readType == ReadType.Replay, consumer);

                consumer.sendMessages(entriesWithSameKey, batchSizes, batchIndexesAcks,
                        sendMessageInfo.getTotalMessages(),
                        sendMessageInfo.getTotalBytes(), sendMessageInfo.getTotalChunkedMessages(),
```

### DataFlowIssue
Method invocation `getMessageDropRate` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java

        subStats.type = getTypeString();
        subStats.msgDropRate = dispatcher.getMessageDropRate().getValueRate();

        KeySharedMode keySharedMode = this.keySharedMode;
```

### DataFlowIssue
Dereference of `pendingReads.peek()` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
            // If new entries are available after we put request into pending queue, fire read.
            // Else register callback with managed ledger to get notify when new entries are available.
            if (managedLedger.hasMoreEntries(pendingReads.peek().position)) {
                entriesAvailable();
            } else if (managedLedger.isTerminated()) {
```

### DataFlowIssue
Dereference of `pendingReads.peek()` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
            // position of the last confirmed position, but it'll be an invalid position. So try to update the position.
            if (!managedLedger.isValidPosition(pendingReads.peek().position)) {
                pendingReads.peek().position = managedLedger.getNextValidPosition(pendingReads.peek().position);
            }
            while (!pendingReads.isEmpty() && managedLedger.hasMoreEntries(pendingReads.peek().position)) {
```

### DataFlowIssue
Dereference of `pendingReads.peek()` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
            // position of the last confirmed position, but it'll be an invalid position. So try to update the position.
            if (!managedLedger.isValidPosition(pendingReads.peek().position)) {
                pendingReads.peek().position = managedLedger.getNextValidPosition(pendingReads.peek().position);
            }
            while (!pendingReads.isEmpty() && managedLedger.hasMoreEntries(pendingReads.peek().position)) {
```

### DataFlowIssue
Dereference of `next` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
                // be all set to managed ledger's last confirmed position.
                if (!pendingReads.isEmpty()) {
                    pendingReads.peek().position = managedLedger.getNextValidPosition(next.position);
                }
            }
```

### DataFlowIssue
Dereference of `pendingReads.peek()` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
                            , cursor.getName(), pendingReads.size());
                }
                if (managedLedger.hasMoreEntries(pendingReads.peek().position)) {
                    entriesAvailable();
                } else {
```

### DataFlowIssue
Dereference of `pendingReads.peek()` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java

    private void internalCancelReadRequests() {
        Position readPosition = !issuedReads.isEmpty() ? issuedReads.peek().position : pendingReads.peek().position;
        cancelReadRequests(readPosition);
    }
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/protocol/ProtocolHandlers.java`
#### Snippet
```java
            }

            if (!handler.accept(protocol)) {
                handler.close();
                log.error("Malformed protocol handler found for protocol `" + protocol + "`");
```

### DataFlowIssue
Method invocation `hasNumMessagesInBatch` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
            MessageMetadata messageMetadata = Commands.peekMessageMetadata(metadataAndPayload, "", -1);
            int numMessages = 1;
            if (messageMetadata.hasNumMessagesInBatch()) {
                numMessages = messageMetadata.getNumMessagesInBatch();
            }
```

### DataFlowIssue
Method invocation `getKey` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/DeviationShedder.java`
#### Snippet
```java
            while (!(bundleTreeSetCache.isEmpty() || selected)) {
                Pair<Double, String> mostExpensivePair = bundleTreeSetCache.pollLast();
                double loadIncurred = mostExpensivePair.getKey();
                // When the bundle is moved, we want the now least loaded server
                // to have lower overall load than the
```

### DataFlowIssue
Method invocation `getMessage` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                            String msg1 = format(
                                    "failed to disable bundle %s under namespace [%s] with error %s",
                                    bundle.getNamespaceObject().toString(), bundle.toString(), ex.getMessage());
                            LOG.warn(msg1, e);
                            completionFuture.completeExceptionally(new ServiceUnitNotReadyException(msg1));
```

### DataFlowIssue
Dereference of `policy` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupNamespaceConfigListener.java`
#### Snippet
```java
        } catch (PulsarAdminException e) {
            LOG.error("Failed to {} namespace {} with resource group {}",
                    delete ? "unregister" : "register", ns, policy.resource_group_name, e);
        }
    }
```

### DataFlowIssue
Method invocation `getFullBundle` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java

    public NamespaceBundle getFullBundle(NamespaceName fqnn) throws Exception {
        return bundlesCache.synchronous().get(fqnn).getFullBundle();
    }

```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            double closeTopicsTimeSeconds = TimeUnit.NANOSECONDS.toMillis((System.nanoTime() - closeTopicsStartTime))
                    / 1000.0;
            log.info("Unloading {} namespace-bundles completed in {} seconds", serviceUnits.size(),
                    closeTopicsTimeSeconds);
        } catch (Exception e) {
```

### DataFlowIssue
Method invocation `lock` may produce `NullPointerException`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageRedeliveryTracker.java`
#### Snippet
```java
            @Override
            public void run(Timeout t) throws Exception {
                writeLock.lock();
                try {
                    HashSet<UnackMessageIdWrapper> headPartition = redeliveryTimePartitions.removeFirst();
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageRedeliveryTracker.java`
#### Snippet
```java
                    .putIfAbsent(messageIdWrapper, partition);
            if (previousPartition == null) {
                return partition.add(messageIdWrapper);
            } else {
                messageIdWrapper.recycle();
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageTracker.java`
#### Snippet
```java
            HashSet<MessageId> previousPartition = messageIdPartitionMap.putIfAbsent(messageId, partition);
            if (previousPartition == null) {
                return partition.add(messageId);
            } else {
                return false;
```

### DataFlowIssue
Variable is already assigned to this value
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ProducerImpl.java`
#### Snippet
```java
                        || schema.getSchemaInfo().getType() == SchemaType.NONE) {
                    // don't set schema info for Schema.BYTES
                    schemaInfo = null;
                } else {
                    schemaInfo = schema.getSchemaInfo();
```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FutureUtil.java`
#### Snippet
```java
                future.get();
            } catch (InterruptedException e) {
                return Optional.ofNullable(e);
            } catch (ExecutionException e) {
                return Optional.ofNullable(e.getCause());
```

### DataFlowIssue
Method invocation `getLedgerId` may produce `NullPointerException`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java

                    if (markDeletePosition != null && !(markDeletePosition.getEntryId() < 0
                            && markDeletePosition.getLedgerId() > lastMessageId.getLedgerId())) {
                        // we only care about comparing ledger ids and entry ids as mark delete position doesn't have
                        // other ids such as batch index
```

### DataFlowIssue
Method invocation `getLedgerId` may produce `NullPointerException`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                        // other ids such as batch index
                        int result = ComparisonChain.start()
                                .compare(markDeletePosition.getLedgerId(), lastMessageId.getLedgerId())
                                .compare(markDeletePosition.getEntryId(), lastMessageId.getEntryId())
                                .result();
```

### DataFlowIssue
Method invocation `getReaderSchema` may produce `NullPointerException`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                    deadLetterProducer.thenAccept(dlqProducer -> {
                        TypedMessageBuilder<byte[]> typedMessageBuilderNew =
                                dlqProducer.newMessage(Schema.AUTO_PRODUCE_BYTES(retryMessage.getReaderSchema().get()))
                                        .value(retryMessage.getData())
                                        .properties(propertiesMap);
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerImpl.java`
#### Snippet
```java
                } else {
                    TypedMessageBuilder<T> typedMessageBuilderNew = retryLetterProducer.newMessage()
                            .value(retryMessage.getValue())
                            .properties(propertiesMap);
                    if (delayTime > 0) {
```

### DataFlowIssue
Argument `MessageIdImpl.convertToMessageIdImpl(startMessageId)` might be null
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
        this.allTopicPartitionsNumber = new AtomicInteger(0);
        this.startMessageId = startMessageId != null
                ? new BatchMessageIdImpl(MessageIdImpl.convertToMessageIdImpl(startMessageId))
                : null;
        this.startMessageRollbackDurationInSec = startMessageRollbackDurationInSec;
```

### DataFlowIssue
Method invocation `getLedgerId` may produce `NullPointerException`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
            final Entry firstEntry = offloadBuffer.poll();
            entries.add(firstEntry);
            long blockLedgerId = firstEntry.getLedgerId();
            long blockEntryId = firstEntry.getEntryId();

```

### DataFlowIssue
Method invocation `getLength` may produce `NullPointerException`
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreManagedLedgerOffloader.java`
#### Snippet
```java
                    && blockEntrySize <= streamingBlockSize) {
                final Entry entryInBlock = offloadBuffer.poll();
                final int entrySize = entryInBlock.getLength();
                bufferLength.addAndGet(-entrySize);
                blockEntrySize += entrySize;
```

### DataFlowIssue
Variable is already assigned to this value
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java

    public static FunctionConfig convertFromDetails(FunctionDetails functionDetails) {
        functionDetails = validateFunctionDetails(functionDetails);
        FunctionConfig functionConfig = new FunctionConfig();
        functionConfig.setTenant(functionDetails.getTenant());
```

### DataFlowIssue
Method invocation `getTemplate` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/KubernetesSecretsTokenAuthProvider.java`
#### Snippet
```java
        }

        V1PodSpec podSpec = statefulSet.getSpec().getTemplate().getSpec();

        // configure pod mount secret with auth token
```

### DataFlowIssue
Method invocation `setVolumes` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/auth/KubernetesSecretsTokenAuthProvider.java`
#### Snippet
```java

        // configure pod mount secret with auth token
        podSpec.setVolumes(Collections.singletonList(
                new V1Volume()
                        .name(SECRET_NAME)
```

### DataFlowIssue
Method invocation `subMap` may produce `NullPointerException`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                            mdEntry.newPosition.getEntryId());
                    if (config.isDeletionAtBatchIndexLevelEnabled()) {
                        Map<PositionImpl, BitSetRecyclable> subMap = batchDeletedIndexes.subMap(PositionImpl.EARLIEST,
                                false, PositionImpl.get(mdEntry.newPosition.getLedgerId(),
                                mdEntry.newPosition.getEntryId()), true);
```

### DataFlowIssue
Method invocation `values` may produce `NullPointerException`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
                    individualDeletedMessages.clear();
                    if (config.isDeletionAtBatchIndexLevelEnabled()) {
                        batchDeletedIndexes.values().forEach(BitSetRecyclable::recycle);
                        batchDeletedIndexes.clear();
                        long[] resetWords = newReadPosition.ackSet;
```

### DataFlowIssue
Method invocation `putLimitsItem` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/BasicKubernetesManifestCustomizer.java`
#### Snippet
```java
            for (String resource : RESOURCES) {
                if (limits != null && limits.containsKey(resource)) {
                    containerResources.putLimitsItem(resource, limits.get(resource));
                }
                if (requests != null && requests.containsKey(resource)) {
```

### DataFlowIssue
Method invocation `putRequestsItem` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/BasicKubernetesManifestCustomizer.java`
#### Snippet
```java
                }
                if (requests != null && requests.containsKey(resource)) {
                    containerResources.putRequestsItem(resource, requests.get(resource));
                }
            }
```

### DataFlowIssue
Method invocation `getTemplate` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/BasicKubernetesManifestCustomizer.java`
#### Snippet
```java
        RuntimeOpts opts = mergeRuntimeOpts(runtimeOpts, getOptsFromDetails(funcDetails));
        statefulSet.setMetadata(updateMeta(opts, statefulSet.getMetadata()));
        V1PodTemplateSpec pt = statefulSet.getSpec().getTemplate();
        pt.setMetadata(updateMeta(opts, pt.getMetadata()));
        V1PodSpec ps = pt.getSpec();
```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                .map((customizer) -> customizer.customizeService(instanceConfig.getFunctionDetails(), service))
                .orElse(service);
        overridden.getMetadata().name(jobName);

        return overridden;
```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                .map((customizer) -> customizer.customizeStatefulSet(instanceConfig.getFunctionDetails(), statefulSet))
                .orElse(statefulSet);
        overridden.getMetadata().name(jobName);

        return statefulSet;
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                    return Actions.ActionResult.builder()
                            .success(false)
                            .errorMsg(response.getStatus().toString())
                            .build();
                })
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
                    return Actions.ActionResult.builder()
                            .success(false)
                            .errorMsg(response.getStatus().toString())
                            .build();
                })
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceUtils.java`
#### Snippet
```java
        Schema<?> schema = createInstance(schemaClassName, clsLoader, Schema.class);

        Class<?>[] inputSerdeTypeArgs = TypeResolver.resolveRawArguments(Schema.class, schema.getClass());
        if (input) {
            checkArgument(typeArg.isAssignableFrom(inputSerdeTypeArgs[0]),
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceUtils.java`
#### Snippet
```java
        SerDe<?> serDe = createInstance(serdeClassName, clsLoader, SerDe.class);

        Class<?>[] inputSerdeTypeArgs = TypeResolver.resolveRawArguments(SerDe.class, serDe.getClass());
        if (deser) {
            checkArgument(typeArg.isAssignableFrom(inputSerdeTypeArgs[0]),
```

### DataFlowIssue
Argument `f.listFiles()` might be null
in `pulsar-functions/runtime-all/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceMain.java`
#### Snippet
```java
            if (f.exists()) {
                if (f.isDirectory()) {
                    files.addAll(Arrays.asList(f.listFiles()));
                } else {
                    files.add(new File(entry));
```

### DataFlowIssue
Method invocation `getId` may produce `NullPointerException`
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
            lastLedgerCreationFailureTimestamp = clock.millis();
        } else {
            log.info("[{}] Created new ledger {}", name, lh.getId());
            LedgerInfo newLedger = LedgerInfo.newBuilder().setLedgerId(lh.getId()).setTimestamp(0).build();
            final MetaStoreCallback<Void> cb = new MetaStoreCallback<Void>() {
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
                            }
                        }
                        Transaction transaction = atomicReference.get();
                        for (List<Consumer<byte[]>> subscriptions : consumers) {
                                for (Consumer<byte[]> consumer : subscriptions) {
```

### DataFlowIssue
Dereference of `consumers` may produce `NullPointerException`
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
                        }
                        Transaction transaction = atomicReference.get();
                        for (List<Consumer<byte[]>> subscriptions : consumers) {
                                for (Consumer<byte[]> consumer : subscriptions) {
                                    for (int j = 0; j < arguments.numMessagesReceivedPerTransaction; j++) {
```

### DataFlowIssue
Method invocation `getMessageId` may produce `NullPointerException`
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
                                        long receiveTime = System.nanoTime();
                                        if (!arguments.isDisableTransaction) {
                                            consumer.acknowledgeAsync(message.getMessageId(), transaction)
                                                    .thenRun(() -> {
                                                        long latencyMicros = NANOSECONDS.toMicros(
```

### DataFlowIssue
Dereference of `producers` may produce `NullPointerException`
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceTransaction.java`
#### Snippet
```java
                        }

                        for (Producer<byte[]> producer : producers){
                            for (int j = 0; j < arguments.numMessagesProducedPerTransaction; j++) {
                                long sendTime = System.nanoTime();
```

### DataFlowIssue
Method invocation `getAutoCertRefreshSeconds` may produce `NullPointerException`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/PulsarAdminImpl.java`
#### Snippet
```java

        AsyncHttpConnectorProvider asyncConnectorProvider = new AsyncHttpConnectorProvider(clientConfigData,
                clientConfigData.getAutoCertRefreshSeconds());

        ClientConfig httpConfig = new ClientConfig();
```

### DataFlowIssue
Method invocation `getConnectionTimeoutMs` may produce `NullPointerException`
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/PulsarAdminImpl.java`
#### Snippet
```java
        ClientBuilder clientBuilder = ClientBuilder.newBuilder()
                .withConfig(httpConfig)
                .connectTimeout(this.clientConfigData.getConnectionTimeoutMs(), TimeUnit.MILLISECONDS)
                .readTimeout(this.clientConfigData.getReadTimeoutMs(), TimeUnit.MILLISECONDS)
                .register(JacksonConfigurator.class).register(JacksonFeature.class);
```

### DataFlowIssue
Variable is already assigned to this value
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/PublishRate.java`
#### Snippet
```java
    public PublishRate() {
        super();
        this.publishThrottlingRateInMsg = -1;
        this.publishThrottlingRateInByte = -1;
    }
```

### DataFlowIssue
Variable is already assigned to this value
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/PublishRate.java`
#### Snippet
```java
        super();
        this.publishThrottlingRateInMsg = -1;
        this.publishThrottlingRateInByte = -1;
    }

```

### DataFlowIssue
Variable is already assigned to this value
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
            entity = Entity.entity(resetCursorData, MediaType.APPLICATION_JSON);
        } else {
            entity = null;
        }

```

### DataFlowIssue
Variable is already assigned to this value
in `pulsar-config-validation/src/main/java/org/apache/pulsar/config/validation/ConfigValidation.java`
#### Snippet
```java
                value = (Object) method.invoke(v);
            } catch (IllegalArgumentException ex) {
                value = null;
            }
            if (value != null) {
```

### DataFlowIssue
Method invocation `getAlgorithm` may produce `NullPointerException`
in `pulsar-client-messagecrypto-bc/src/main/java/org/apache/pulsar/client/impl/crypto/MessageCryptoBc.java`
#### Snippet
```java
            // then we need to set it accordingly

            if (ecParam != null && ECDSA.equals(privateKey.getAlgorithm())) {
                ECParameterSpec ecSpec = new ECParameterSpec(ecParam.getCurve(), ecParam.getG(), ecParam.getN(),
                        ecParam.getH(), ecParam.getSeed());
```

### DataFlowIssue
Variable is already assigned to this value
in `pulsar-client-auth-athenz/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationAthenz.java`
#### Snippet
```java
            throw new IllegalArgumentException("Invalid privateKey format", e);
        } catch (CryptoException | InstantiationException | IllegalAccessException | IOException e) {
            privateKey = null;
        }
        return privateKey;
```

## RuleId[ruleID=MissingSerialAnnotation]
### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/nsq/src/main/java/org/apache/pulsar/io/nsq/NSQSourceConfig.java`
#### Snippet
```java
@Accessors(chain = true)
public class NSQSourceConfig implements Serializable {
    private static final long serialVersionUID = 1L;


```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileSourceConfig.java`
#### Snippet
```java
public class FileSourceConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/HttpSinkConfig.java`
#### Snippet
```java
public class HttpSinkConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcSinkConfig.java`
#### Snippet
```java
public class JdbcSinkConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/solr/src/main/java/org/apache/pulsar/io/solr/SolrSinkConfig.java`
#### Snippet
```java
public class SolrSinkConfig implements Serializable {

    private static final long serialVersionUID = -4849066206354610110L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/HbaseAbstractConfig.java`
#### Snippet
```java
public class HbaseAbstractConfig implements Serializable {

    private static final long serialVersionUID = -8945930873383593712L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConfig.java`
#### Snippet
```java
public abstract class AbstractHdfsConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseSinkConfig.java`
#### Snippet
```java
public class HbaseSinkConfig extends HbaseAbstractConfig {

    private static final long serialVersionUID = 1245636479605735555L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/HdfsSinkConfig.java`
#### Snippet
```java
public class HdfsSinkConfig extends AbstractHdfsConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConfig.java`
#### Snippet
```java
public abstract class AbstractHdfsConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/HdfsSinkConfig.java`
#### Snippet
```java
public class HdfsSinkConfig extends AbstractHdfsConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoAbstractConfig.java`
#### Snippet
```java
public abstract class MongoAbstractConfig implements Serializable {

    private static final long serialVersionUID = -3830568531897300005L;

    public static final int DEFAULT_BATCH_SIZE = 100;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSinkConfig.java`
#### Snippet
```java
public class KafkaSinkConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/KafkaSourceConfig.java`
#### Snippet
```java
public class KafkaSourceConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSinkConfig.java`
#### Snippet
```java
public class MongoSinkConfig extends MongoAbstractConfig {

    private static final long serialVersionUID = 8805978990904614084L;

    @JsonCreator
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSourceConfig.java`
#### Snippet
```java
public class MongoSourceConfig extends MongoAbstractConfig {

    private static final long serialVersionUID = 1152890092264945317L;

    public static final SyncType DEFAULT_SYNC_TYPE = SyncType.INCR_SYNC;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/NettySourceConfig.java`
#### Snippet
```java
public class NettySourceConfig implements Serializable {

    private static final long serialVersionUID = -7116130435021510496L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisAbstractConfig.java`
#### Snippet
```java
public class RedisAbstractConfig implements Serializable {

    private static final long serialVersionUID = -7860917032537872317L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/sink/RedisSinkConfig.java`
#### Snippet
```java
public class RedisSinkConfig extends RedisAbstractConfig implements Serializable {

    private static final long serialVersionUID = 4686456460365805717L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/BaseKinesisConfig.java`
#### Snippet
```java
public abstract class BaseKinesisConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSinkConfig.java`
#### Snippet
```java
public class KinesisSinkConfig extends BaseKinesisConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSourceConfig.java`
#### Snippet
```java
public class KinesisSourceConfig extends BaseKinesisConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/endpoint/SampleStatusesEndpoint.java`
#### Snippet
```java
     *
     */
    private static final long serialVersionUID = 1L;

    public StreamingEndpoint createEndpoint() {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHoseConfig.java`
#### Snippet
```java
public class TwitterFireHoseConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/dynamodb/src/main/java/org/apache/pulsar/io/dynamodb/DynamoDBSourceConfig.java`
#### Snippet
```java
public class DynamoDBSourceConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v1/InfluxDBSinkConfig.java`
#### Snippet
```java
public class InfluxDBSinkConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQAbstractConfig.java`
#### Snippet
```java
public class RabbitMQAbstractConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSourceConfig.java`
#### Snippet
```java
public class RabbitMQSourceConfig extends RabbitMQAbstractConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSinkConfig.java`
#### Snippet
```java
public class RabbitMQSinkConfig extends RabbitMQAbstractConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSinkConfig.java`
#### Snippet
```java
public class InfluxDBSinkConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/data-generator/src/main/java/org/apache/pulsar/io/datagenerator/DataGeneratorSourceConfig.java`
#### Snippet
```java
@Data
public class DataGeneratorSourceConfig implements Serializable {
  private static final long serialVersionUID = 1L;

  @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/cassandra/src/main/java/org/apache/pulsar/io/cassandra/CassandraSinkConfig.java`
#### Snippet
```java
public class CassandraSinkConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/aerospike/src/main/java/org/apache/pulsar/io/aerospike/AerospikeSinkConfig.java`
#### Snippet
```java
public class AerospikeSinkConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    private String seedHosts;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSslConfig.java`
#### Snippet
```java
public class ElasticSearchSslConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
public class ElasticSearchConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaConnectSinkConfig.java`
#### Snippet
```java
public class PulsarKafkaConnectSinkConfig implements Serializable {

    private static final long serialVersionUID = 1L;

    @FieldDoc(
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarConnectorModule.java`
#### Snippet
```java
    public static final class TypeDeserializer
            extends FromStringDeserializer<Type> {
        private static final long serialVersionUID = 1L;

        private final TypeManager typeManager;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/PulsarKeyException.java`
#### Snippet
```java

public class PulsarKeyException extends Exception {
    private static final long serialVersionUID = 1L;

    public PulsarKeyException(String msg) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/PulsarPrometheusMetricsServlet.java`
#### Snippet
```java
public class PulsarPrometheusMetricsServlet extends PrometheusMetricsServlet {

    private static final long serialVersionUID = 1L;

    private final PulsarService pulsar;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/exceptions/IncompatibleSchemaException.java`
#### Snippet
```java
public class IncompatibleSchemaException extends SchemaException {

    private static final long serialVersionUID = -6013970359956508359L;

    public IncompatibleSchemaException() {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/exceptions/SchemaException.java`
#### Snippet
```java
public class SchemaException extends BrokerServiceException {

    private static final long serialVersionUID = -6587520779026691815L;
    private boolean recoverable;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/exceptions/InvalidSchemaDataException.java`
#### Snippet
```java
public class InvalidSchemaDataException extends SchemaException {

    private static final long serialVersionUID = -2846364736743783766L;

    public InvalidSchemaDataException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/TransactionException.java`
#### Snippet
```java
    public static class TransactionStatusException extends TransactionException {

        private static final long serialVersionUID = 0L;

        public TransactionStatusException(TxnID txnId,
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/TransactionException.java`
#### Snippet
```java
public abstract class TransactionException extends Exception {

    private static final long serialVersionUID = 0L;

    public TransactionException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/TransactionException.java`
#### Snippet
```java
    public static class TransactionSealedException extends TransactionException {

        private static final long serialVersionUID = 5366602873819540477L;

        public TransactionSealedException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/TransactionException.java`
#### Snippet
```java
    public static class TransactionNotSealedException extends TransactionException {

        private static final long serialVersionUID = 0L;

        public TransactionNotSealedException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/pendingack/TransactionPendingAckException.java`
#### Snippet
```java
public abstract class TransactionPendingAckException extends TransactionException {

    private static final long serialVersionUID = 0L;

    public TransactionPendingAckException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
    public static class TransactionNotFoundException extends TransactionBufferException {

        private static final long serialVersionUID = 0L;

        public TransactionNotFoundException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
    public static class EndOfTransactionException extends TransactionBufferException {

        private static final long serialVersionUID = 0L;

        public EndOfTransactionException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
public abstract class TransactionBufferException extends TransactionException {

    private static final long serialVersionUID = 0L;

    public TransactionBufferException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
    public class NoTxnsCommittedAtLedgerException extends TransactionBufferException {

        private static final long serialVersionUID = 0L;

        public NoTxnsCommittedAtLedgerException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/coordinator/TransactionCoordinatorException.java`
#### Snippet
```java
    public static class UnsupportedTxnActionException extends TransactionCoordinatorException {

        private static final long serialVersionUID = 0L;

        public UnsupportedTxnActionException(TxnID txnId, int txnAction) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/coordinator/TransactionCoordinatorException.java`
#### Snippet
```java
public abstract class TransactionCoordinatorException extends TransactionException {

    private static final long serialVersionUID = 0L;

    public TransactionCoordinatorException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/BlobStoreLocation.java`
#### Snippet
```java
public class BlobStoreLocation implements Serializable {

    private static final long serialVersionUID = 1L;

    private final String providerName;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/ErrorNotifierShutdownServiceImpl.java`
#### Snippet
```java
@Slf4j
public class ErrorNotifierShutdownServiceImpl implements ErrorNotifier {
    private static final long serialVersionUID = 1L;
    private final ShutdownService shutdownService;

```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/ErrorNotifierImpl.java`
#### Snippet
```java
public class ErrorNotifierImpl implements ErrorNotifier {

    private static final long serialVersionUID = 1L;

    private final AtomicReference<Throwable> error = new AtomicReference<>();
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
public class WorkerConfig implements Serializable, PulsarConfiguration {

    private static final long serialVersionUID = 1L;

    @Category
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketProducerServlet.java`
#### Snippet
```java

public class WebSocketProducerServlet extends WebSocketServlet {
    private static final long serialVersionUID = 1L;

    public static final String SERVLET_PATH = "/ws/producer";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketReaderServlet.java`
#### Snippet
```java

public class WebSocketReaderServlet extends WebSocketServlet {
    private static final transient long serialVersionUID = 1L;

    public static final String SERVLET_PATH = "/ws/reader";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketConsumerServlet.java`
#### Snippet
```java

public class WebSocketConsumerServlet extends WebSocketServlet {
    private static final long serialVersionUID = 1L;

    public static final String SERVLET_PATH = "/ws/consumer";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/WebSocketPingPongServlet.java`
#### Snippet
```java

public class WebSocketPingPongServlet extends WebSocketServlet {
    private static final long serialVersionUID = 1L;

    public static final String SERVLET_PATH = "/ws/pingpong";
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-transaction/common/src/main/java/org/apache/pulsar/transaction/common/exception/TransactionConflictException.java`
#### Snippet
```java
public class TransactionConflictException extends Exception {

    private static final long serialVersionUID = 0L;

    public TransactionConflictException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
    public static class ReachMaxActiveTxnException extends CoordinatorException {

        private static final long serialVersionUID = 0L;

        public ReachMaxActiveTxnException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
    public static class TransactionNotFoundException extends CoordinatorException {

        private static final long serialVersionUID = 0L;

        public TransactionNotFoundException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
 */
public abstract class CoordinatorException extends Exception {
    private static final long serialVersionUID = 0L;

    public CoordinatorException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
    public static class TransactionMetadataStoreStateException extends CoordinatorException {

        private static final long serialVersionUID = 0L;

        public TransactionMetadataStoreStateException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/exceptions/CoordinatorException.java`
#### Snippet
```java
    public static class InvalidTxnStatusException extends CoordinatorException {

        private static final long serialVersionUID = 0L;

        public InvalidTxnStatusException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/PulsarServerException.java`
#### Snippet
```java

public class PulsarServerException extends IOException {
    private static final long serialVersionUID = 1;

    public PulsarServerException(String message) {
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsServlet.java`
#### Snippet
```java
public class PrometheusMetricsServlet extends HttpServlet {

    private static final long serialVersionUID = 1L;
    private static final int HTTP_STATUS_OK_200 = 200;
    private static final int HTTP_STATUS_INTERNAL_SERVER_ERROR_500 = 500;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/SaslAuthenticationDataProvider.java`
#### Snippet
```java
@Slf4j
public class SaslAuthenticationDataProvider implements AuthenticationDataProvider {
    private static final long serialVersionUID = 1L;

    private PulsarSaslClient pulsarSaslClient;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-client-auth-athenz/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationDataAthenz.java`
#### Snippet
```java

public class AuthenticationDataAthenz implements AuthenticationDataProvider {
    private static final long serialVersionUID = 1L;

    protected String roleToken;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationSasl.java`
#### Snippet
```java
@Slf4j
public class AuthenticationSasl implements Authentication, EncodedAuthenticationParameterSupport {
    private static final long serialVersionUID = 1L;
    // this is a static object that shares amongst client.
    private static JAASCredentialsContainer jaasCredentialsContainer;
```

### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `pulsar-client-auth-athenz/src/main/java/org/apache/pulsar/client/impl/auth/AuthenticationAthenz.java`
#### Snippet
```java
public class AuthenticationAthenz implements Authentication, EncodedAuthenticationParameterSupport {

    private static final long serialVersionUID = 1L;

    private static final String APPLICATION_X_PEM_FILE = "application/x-pem-file";
```

## RuleId[ruleID=BoxingBoxedValue]
### BoxingBoxedValue
Boxing of already boxed `(Boolean) value`
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/primitive/PulsarPrimitiveRowDecoder.java`
#### Snippet
```java
            Type type = columnHandle.getType();
            if (type instanceof BooleanType) {
                primitiveColumn.put(columnHandle, booleanValueProvider(Boolean.valueOf((Boolean) value)));
            } else if (type instanceof TinyintType || type instanceof SmallintType || type instanceof IntegerType
                    || type instanceof BigintType) {
```

## RuleId[ruleID=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'configurationStoreServers' is still used
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyServiceStarter.java`
#### Snippet
```java
                    description = "Configuration store connection string, "
                            + "please use --configuration-metadata-store instead")
    private String configurationStoreServers = "";
    @Parameter(names = { "-cms", "--configuration-metadata-store" },
            description = "The metadata store URL for the configuration data")
```

### DeprecatedIsStillUsed
Deprecated member 'zookeeperServers' is still used
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyServiceStarter.java`
#### Snippet
```java
    @Parameter(names = { "-zk", "--zookeeper-servers" },
            description = "Local zookeeper connection string, please use --metadata-store instead")
    private String zookeeperServers = "";
    @Parameter(names = { "-md", "--metadata-store" }, description = "Metadata Store service url. eg: zk:my-zk:2181")
    private String metadataStoreUrl = "";
```

### DeprecatedIsStillUsed
Deprecated member 'globalZookeeperServers' is still used
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyServiceStarter.java`
#### Snippet
```java
    @Parameter(names = { "-gzk", "--global-zookeeper-servers" },
            description = "Global zookeeper connection string, please use --configuration-metadata-store instead")
    private String globalZookeeperServers = "";

    @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'bookieMetadataServiceUri' is still used
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
            description = "The metadata service URI of the existing BookKeeper cluster that you want to use",
            hidden = true)
        private String bookieMetadataServiceUri;

        @Parameter(names = { "-pp",
```

### DeprecatedIsStillUsed
Deprecated member 'zookeeperServers' is still used
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    )
    @Deprecated
    private String zookeeperServers;

    @FieldContext(
```

### DeprecatedIsStillUsed
Deprecated member 'zooKeeperCacheExpirySeconds' is still used
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                + "@deprecated - Use metadataStoreCacheExpirySeconds instead."
    )
    private int zooKeeperCacheExpirySeconds = -1;

    @FieldContext(
```

### DeprecatedIsStillUsed
Deprecated member 'zookeeperSessionTimeoutMs' is still used
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
                + "@deprecated - Use metadataStoreSessionTimeoutMillis instead."
    )
    private int zookeeperSessionTimeoutMs = -1;

    @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'globalZookeeperServers' is still used
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    )
    @Deprecated
    private String globalZookeeperServers;

    @FieldContext(
```

### DeprecatedIsStillUsed
Deprecated member 'configurationStoreServers' is still used
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
    )
    @Deprecated
    private String configurationStoreServers;

    @FieldContext(
```

### DeprecatedIsStillUsed
Deprecated member 'validatePersistentTopicName' is still used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/AdminResource.java`
#### Snippet
```java

    @Deprecated
    protected void validatePersistentTopicName(String property, String cluster, String namespace, String encodedTopic) {
        validateTopicName(property, cluster, namespace, encodedTopic);
        if (topicName.getDomain() != TopicDomain.persistent) {
```

### DeprecatedIsStillUsed
Deprecated member 'beforeSendMessage' is still used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/BrokerInterceptor.java`
#### Snippet
```java
     */
    @Deprecated
    default void beforeSendMessage(Subscription subscription,
                                   Entry entry,
                                   long[] ackSet,
```

### DeprecatedIsStillUsed
Deprecated member 'internalSetSchemaAutoUpdateCompatibilityStrategy' is still used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

    @Deprecated
    protected void internalSetSchemaAutoUpdateCompatibilityStrategy(SchemaAutoUpdateCompatibilityStrategy strategy) {
        validateNamespacePolicyOperation(namespaceName, PolicyName.SCHEMA_COMPATIBILITY_STRATEGY,
                PolicyOperation.WRITE);
```

### DeprecatedIsStillUsed
Deprecated member 'internalGetSchemaAutoUpdateCompatibilityStrategy' is still used
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java

    @Deprecated
    protected SchemaAutoUpdateCompatibilityStrategy internalGetSchemaAutoUpdateCompatibilityStrategy() {
        validateNamespacePolicyOperation(namespaceName, PolicyName.SCHEMA_COMPATIBILITY_STRATEGY,
                PolicyOperation.READ);
```

### DeprecatedIsStillUsed
Deprecated member 'SYSTEM_PROPERTY_ORIGIN_MESSAGE_ID' is still used
in `pulsar-client/src/main/java/org/apache/pulsar/client/util/RetryMessageUtil.java`
#### Snippet
```java
    public static final String SYSTEM_PROPERTY_RETRY_TOPIC = "RETRY_TOPIC";
    @Deprecated
    public static final String SYSTEM_PROPERTY_ORIGIN_MESSAGE_ID = "ORIGIN_MESSAGE_IDY_TIME";
    public static final String PROPERTY_ORIGIN_MESSAGE_ID = "ORIGIN_MESSAGE_ID";
    public static final int MAX_RECONSUMETIMES = 16;
```

### DeprecatedIsStillUsed
Deprecated member 'getNativeUrl' is still used
in `pulsar-common/src/main/java/org/apache/pulsar/common/lookup/data/LookupData.java`
#### Snippet
```java
     */
    @Deprecated
    public String getNativeUrl() {
        return nativeUrl;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'SinkApiV3Resource' is still used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SinkApiV3Resource.java`
#### Snippet
```java
 * @deprecated in favor of {@link SinksApiV3Resource}
 */
public class SinkApiV3Resource extends SinksApiV3Resource {
}

```

### DeprecatedIsStillUsed
Deprecated member 'SourceApiV3Resource' is still used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v3/SourceApiV3Resource.java`
#### Snippet
```java
 * @deprecated in favor of {@link SourcesApiV3Resource}
 */
public class SourceApiV3Resource extends SourcesApiV3Resource {
}

```

### DeprecatedIsStillUsed
Deprecated member 'updateSink' is still used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/api/Sinks.java`
#### Snippet
```java
     */
    @Deprecated
    default void updateSink(String tenant,
                    String namespace,
                    String sinkName,
```

### DeprecatedIsStillUsed
Deprecated member 'registerSink' is still used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/api/Sinks.java`
#### Snippet
```java
     */
    @Deprecated
    default void registerSink(String tenant,
                      String namespace,
                      String sinkName,
```

### DeprecatedIsStillUsed
Deprecated member 'updateSource' is still used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/api/Sources.java`
#### Snippet
```java
     */
    @Deprecated
    default void updateSource(String tenant,
                      String namespace,
                      String sourceName,
```

### DeprecatedIsStillUsed
Deprecated member 'registerSource' is still used
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/api/Sources.java`
#### Snippet
```java
     */
    @Deprecated
    default void registerSource(String tenant,
                        String namespace,
                        String sourceName,
```

### DeprecatedIsStillUsed
Deprecated member 'zooKeeperOperationTimeoutSeconds' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                    + "@deprecated - Use metadataStoreOperationTimeoutSeconds instead."
    )
    private int zooKeeperOperationTimeoutSeconds = -1;

    @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'KubernetesContainerFactory' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
     * for specifying the function runtime and configs to use
     */
    public static class KubernetesContainerFactory extends KubernetesRuntimeFactoryConfig {

    }
```

### DeprecatedIsStillUsed
Deprecated member 'zooKeeperCacheExpirySeconds' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                    + "@deprecated - Use metadataStoreCacheExpirySeconds instead."
    )
    private int zooKeeperCacheExpirySeconds = -1;

    @FieldContext(
```

### DeprecatedIsStillUsed
Deprecated member 'tlsEnabled' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @Deprecated
    private boolean tlsEnabled = false;
    @FieldContext(
        category = CATEGORY_WORKER_SECURITY,
```

### DeprecatedIsStillUsed
Deprecated member 'clientAuthenticationPlugin' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @Deprecated
    private String clientAuthenticationPlugin;

    public String getBrokerClientAuthenticationPlugin() {
```

### DeprecatedIsStillUsed
Deprecated member 'ProcessContainerFactory' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
     * for specifying the function runtime and configs to use
     */
    public static class ProcessContainerFactory extends ProcessRuntimeFactoryConfig {

    }
```

### DeprecatedIsStillUsed
Deprecated member 'clientAuthenticationParameters' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @Deprecated
    private String clientAuthenticationParameters;
    @FieldContext(
            category = CATEGORY_CLIENT_SECURITY,
```

### DeprecatedIsStillUsed
Deprecated member 'ThreadContainerFactory' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
     * for specifying the function runtime and configs to use
     */
    public static class ThreadContainerFactory extends ThreadRuntimeFactoryConfig {

    }
```

### DeprecatedIsStillUsed
Deprecated member 'zooKeeperSessionTimeoutMillis' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                    + "@deprecated - Use metadataStoreSessionTimeoutMillis instead."
    )
    private long zooKeeperSessionTimeoutMillis = -1;

    @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'configurationStoreServers' is still used
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @Deprecated
    private String configurationStoreServers;
    @FieldContext(
            category = CATEGORY_WORKER,
```

### DeprecatedIsStillUsed
Deprecated member 'AUTO' is still used
in `pulsar-client-api/src/main/java/org/apache/pulsar/common/schema/SchemaType.java`
#### Snippet
```java
     */
    @Deprecated
    AUTO(-2),

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'zookeeperServers' is still used
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/ManagedLedgerWriter.java`
#### Snippet
```java
                description = "ZooKeeper connection string",
                hidden = true)
        public String zookeeperServers;

        @Parameter(names = {"-md",
```

### DeprecatedIsStillUsed
Deprecated member 'ProducerInterceptor' is still used
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/ProducerInterceptor.java`
#### Snippet
```java
@InterfaceStability.Stable
@Deprecated
public interface ProducerInterceptor<T> extends AutoCloseable {

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'getAppliedAdvertisedAddress' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfigurationUtils.java`
#### Snippet
```java
     */
    @Deprecated
    public static String getAppliedAdvertisedAddress(ServiceConfiguration configuration,
                                                     boolean ignoreAdvertisedListener) {
        Map<String, AdvertisedListener> result = MultipleListenerValidator
```

### DeprecatedIsStillUsed
Deprecated member 'PROXY_ADDITIONAL_SERVLETS' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServlets.java`
#### Snippet
```java

    @Deprecated
    private static final String PROXY_ADDITIONAL_SERVLETS = "proxyAdditionalServlets";

    @Getter
```

### DeprecatedIsStillUsed
Deprecated member 'PROXY_ADDITIONAL_SERVLET_DIRECTORY' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServlets.java`
#### Snippet
```java

    @Deprecated
    private static final String PROXY_ADDITIONAL_SERVLET_DIRECTORY = "proxyAdditionalServletDirectory";

    @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'EnableDeduplication' is still used
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    @Deprecated
    @Parameters(commandDescription = "Enable the deduplication policy for a topic")
    private class EnableDeduplication extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;
```

### DeprecatedIsStillUsed
Deprecated member 'DisableDeduplication' is still used
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    @Deprecated
    @Parameters(commandDescription = "Disable the deduplication policy for a topic")
    private class DisableDeduplication extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;
```

### DeprecatedIsStillUsed
Deprecated member 'sendAsync' is still used
in `pulsar-client-1x-base/pulsar-client-1x/src/main/java/org/apache/pulsar/client/api/Producer.java`
#### Snippet
```java
     */
    @Deprecated
    CompletableFuture<MessageId> sendAsync(Message<byte[]> message);

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'send' is still used
in `pulsar-client-1x-base/pulsar-client-1x/src/main/java/org/apache/pulsar/client/api/Producer.java`
#### Snippet
```java
     */
    @Deprecated
    MessageId send(Message<byte[]> message) throws PulsarClientException;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'ConfigurationCacheService' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/cache/ConfigurationCacheService.java`
#### Snippet
```java
 */
@Deprecated
public class ConfigurationCacheService {
}

```

### DeprecatedIsStillUsed
Deprecated member 'MessageBuilder' is still used
in `pulsar-client-1x-base/pulsar-client-1x/src/main/java/org/apache/pulsar/client/api/MessageBuilder.java`
#### Snippet
```java
 */
@Deprecated
public interface MessageBuilder {

    static MessageBuilder create() {
```

### DeprecatedIsStillUsed
Deprecated member 'ReaderConfiguration' is still used
in `pulsar-client-1x-base/pulsar-client-1x/src/main/java/org/apache/pulsar/client/api/ReaderConfiguration.java`
#### Snippet
```java
 */
@Deprecated
public class ReaderConfiguration implements Serializable {

    private final ReaderConfigurationData<byte[]> conf = new ReaderConfigurationData<>();
```

### DeprecatedIsStillUsed
Deprecated member 'ConsumerConfiguration' is still used
in `pulsar-client-1x-base/pulsar-client-1x/src/main/java/org/apache/pulsar/client/api/ConsumerConfiguration.java`
#### Snippet
```java
 */
@Deprecated
public class ConsumerConfiguration implements Serializable {

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'ClientConfiguration' is still used
in `pulsar-client-1x-base/pulsar-client-1x/src/main/java/org/apache/pulsar/client/api/ClientConfiguration.java`
#### Snippet
```java
 */
@Deprecated
public class ClientConfiguration implements Serializable {

    private static final long serialVersionUID = 1L;
```

### DeprecatedIsStillUsed
Deprecated member 'ProducerConfiguration' is still used
in `pulsar-client-1x-base/pulsar-client-1x/src/main/java/org/apache/pulsar/client/api/ProducerConfiguration.java`
#### Snippet
```java
@Deprecated
@EqualsAndHashCode
public class ProducerConfiguration implements Serializable {

    private static final long serialVersionUID = 1L;
```

### DeprecatedIsStillUsed
Deprecated member 'NonPersistentTopics' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/NonPersistentTopics.java`
#### Snippet
```java
 */
@Deprecated
public interface NonPersistentTopics {


```

### DeprecatedIsStillUsed
Deprecated member 'configurationStoreServers' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/conf/InternalConfigurationData.java`
#### Snippet
```java
    private String zookeeperServers;
    @Deprecated
    private String configurationStoreServers;
    private String metadataStoreUrl;
    private String configurationMetadataStoreUrl;
```

### DeprecatedIsStillUsed
Deprecated member 'getLedgersRootPath' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/conf/InternalConfigurationData.java`
#### Snippet
```java
    /** @deprecated */
    @Deprecated
    public String getLedgersRootPath() {
        return ledgersRootPath;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'zookeeperServers' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/conf/InternalConfigurationData.java`
#### Snippet
```java

    @Deprecated
    private String zookeeperServers;
    @Deprecated
    private String configurationStoreServers;
```

### DeprecatedIsStillUsed
Deprecated member 'ledgersRootPath' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/conf/InternalConfigurationData.java`
#### Snippet
```java
    private String configurationMetadataStoreUrl;
    @Deprecated
    private String ledgersRootPath;
    private String bookkeeperMetadataServiceUri;
    private String stateStorageServiceUrl;
```

### DeprecatedIsStillUsed
Deprecated member 'clusterDispatchRate' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    public Map<BacklogQuota.BacklogQuotaType, BacklogQuota> backlog_quota_map = new HashMap<>();
    @Deprecated
    public Map<String, DispatchRateImpl> clusterDispatchRate = new HashMap<>();
    public Map<String, DispatchRateImpl> topicDispatchRate = new HashMap<>();
    public Map<String, DispatchRateImpl> subscriptionDispatchRate = new HashMap<>();
```

### DeprecatedIsStillUsed
Deprecated member 'schema_auto_update_compatibility_strategy' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/Policies.java`
#### Snippet
```java
    @SuppressWarnings("checkstyle:MemberName")
    @Deprecated
    public SchemaAutoUpdateCompatibilityStrategy schema_auto_update_compatibility_strategy = null;

    @SuppressWarnings("checkstyle:MemberName")
```

### DeprecatedIsStillUsed
Deprecated member 'disableDeduplication' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Topics.java`
#### Snippet
```java
     */
    @Deprecated
    void disableDeduplication(String topic) throws PulsarAdminException;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'getLimit' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/impl/BacklogQuotaImpl.java`
#### Snippet
```java

    @Deprecated
    public long getLimit() {
        if (limitSize == null) {
            return limit;
```

### DeprecatedIsStillUsed
Deprecated member 'limit' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/impl/BacklogQuotaImpl.java`
#### Snippet
```java
     */
    @Deprecated
    private long limit;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'getLimit' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/BacklogQuota.java`
#### Snippet
```java
     */
    @Deprecated
    long getLimit();

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'setLimit' is still used
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/common/policies/data/impl/BacklogQuotaImpl.java`
#### Snippet
```java

    @Deprecated
    public void setLimit(long limit) {
        this.limit = limit;
        this.limitSize = limit;
```

### DeprecatedIsStillUsed
Deprecated member 'managedLedgerMaxUnackedRangesToPersistInZooKeeper' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            + " unacked ranges into bookkeeper to avoid additional data overhead into zookeeper.\n"
            + "@deprecated - use managedLedgerMaxUnackedRangesToPersistInMetadataStore.")
    private int managedLedgerMaxUnackedRangesToPersistInZooKeeper = -1;
    @FieldContext(
            category = CATEGORY_STORAGE_ML,
```

### DeprecatedIsStillUsed
Deprecated member 'zooKeeperCacheExpirySeconds' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "@deprecated - Use metadataStoreCacheExpirySeconds instead."
        )
    private int zooKeeperCacheExpirySeconds = -1;

    @FieldContext(
```

### DeprecatedIsStillUsed
Deprecated member 'zooKeeperSessionTimeoutMillis' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                + "@deprecated - Use metadataStoreSessionTimeoutMillis instead."
    )
    private long zooKeeperSessionTimeoutMillis = -1;

    @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'configurationStoreServers' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @Getter(AccessLevel.NONE)
    @Deprecated
    private String configurationStoreServers;

    @FieldContext(
```

### DeprecatedIsStillUsed
Deprecated member 'globalZookeeperServers' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @Getter(AccessLevel.NONE)
    @Deprecated
    private String globalZookeeperServers;

    @FieldContext(
```

### DeprecatedIsStillUsed
Deprecated member 'isSubscriptionKeySharedEnable' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
     */
    @Deprecated
    public boolean isSubscriptionKeySharedEnable() {
        return subscriptionKeySharedEnable && subscriptionTypesEnabled.contains("Key_Shared");
    }
```

### DeprecatedIsStillUsed
Deprecated member 'zooKeeperOperationTimeoutSeconds' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
                    + "@deprecated - Use metadataStoreOperationTimeoutSeconds instead."
        )
    private int zooKeeperOperationTimeoutSeconds = -1;

    @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'managedLedgerCacheEvictionFrequency' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
            doc = "Configure the cache eviction frequency for the managed ledger cache.")
    @Deprecated
    private double managedLedgerCacheEvictionFrequency = 0;

    @FieldContext(category = CATEGORY_STORAGE_ML,
```

### DeprecatedIsStillUsed
Deprecated member 'zookeeperServers' is still used
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java
    @Getter(AccessLevel.NONE)
    @Deprecated
    private String zookeeperServers;

    @FieldContext(
```

## RuleId[ruleID=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
                fillKeyValueSchemaData(valueSchema, value, data);
            }
            recordValueGetter = (k) -> data.get(k);
        } else {
            recordValueGetter = (key) -> ((GenericRecord) record).getField(key);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/PollingZooKeeperConfigurationProvider.java`
#### Snippet
```java
                agentNodeCache = new NodeCache(client, basePath + "/" + getAgentName());
                agentNodeCache.start();
                agentNodeCache.getListenable().addListener(() -> refreshConfiguration());
            } catch (Exception e) {
                client.close();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/seq/HdfsTextSink.java`
#### Snippet
```java
    @Override
    public KeyValue<String, String> extractKeyValue(Record<String> record) {
       String key = record.getKey().orElseGet(() -> record.getValue());
       return new KeyValue<>(key, record.getValue());
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/text/HdfsStringSink.java`
#### Snippet
```java
    @Override
    public KeyValue<String, String> extractKeyValue(Record<String> record) {
       String key = record.getKey().orElseGet(() -> record.getValue());
       return new KeyValue<>(key, record.getValue());
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/seq/HdfsTextSink.java`
#### Snippet
```java
    @Override
    public KeyValue<String, String> extractKeyValue(Record<String> record) {
       String key = record.getKey().orElseGet(() -> record.getValue());
       return new KeyValue<>(key, record.getValue());
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/text/HdfsStringSink.java`
#### Snippet
```java
    @Override
    public KeyValue<String, String> extractKeyValue(Record<String> record) {
       String key = record.getKey().orElseGet(() -> record.getValue());
       return new KeyValue<>(key, record.getValue());
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseAbstractSink.java`
#### Snippet
```java
            }

            toFlushList.forEach(tRecord -> tRecord.ack());
            puts.clear();
            toFlushList.clear();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseAbstractSink.java`
#### Snippet
```java
            toFlushList.clear();
        } catch (Exception e) {
            toFlushList.forEach(tRecord -> tRecord.fail());
            log.error("Hbase table put data exception ", e);
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseAbstractSink.java`
#### Snippet
```java
        incomingList = Lists.newArrayList();
        flushExecutor = Executors.newScheduledThreadPool(1);
        flushExecutor.scheduleAtFixedRate(() -> flush(), batchTimeMs, batchTimeMs, TimeUnit.MILLISECONDS);
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/hbase/src/main/java/org/apache/pulsar/io/hbase/sink/HbaseAbstractSink.java`
#### Snippet
```java

        if (number == batchSize) {
            flushExecutor.execute(() -> flush());
        }
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java

        if (currentSize == mongoSinkConfig.getBatchSize()) {
            flushExecutor.execute(() -> flush());
        }
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
        incomingList = Lists.newArrayList();
        flushExecutor = Executors.newScheduledThreadPool(1);
        flushExecutor.scheduleAtFixedRate(() -> flush(),
                mongoSinkConfig.getBatchTimeMs(), mongoSinkConfig.getBatchTimeMs(), TimeUnit.MILLISECONDS);
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/common/src/main/java/org/apache/pulsar/io/common/IOConfigUtils.java`
#### Snippet
```java

    public static <T> T loadWithSecrets(Map<String, Object> map, Class<T> clazz, SinkContext sinkContext) {
        return loadWithSecrets(map, clazz, secretName -> sinkContext.getSecret(secretName));
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/common/src/main/java/org/apache/pulsar/io/common/IOConfigUtils.java`
#### Snippet
```java
public class IOConfigUtils {
    public static <T> T loadWithSecrets(Map<String, Object> map, Class<T> clazz, SourceContext sourceContext) {
        return loadWithSecrets(map, clazz, secretName -> sourceContext.getSecret(secretName));
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/elastic/ElasticBulkProcessor.java`
#### Snippet
```java
        } else {
            return this.bulkSize > 0L
                    && pendingOperations.stream().mapToLong(op -> op.getEstimatedSizeInBytes()).sum() >= this.bulkSize;
        }
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
    public void offset(Map<TopicPartition, Long> map) {
        map.forEach((key, value) -> {
            seekAndUpdateOffset(key, value);
        });

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/PulsarKafkaSinkTaskContext.java`
#### Snippet
```java
                    Optional<ByteBuffer> val = result.entrySet().stream()
                            .filter(entry -> entry.getKey().equals(key))
                            .findFirst().map(entry -> entry.getValue());
                    if (val.isPresent()) {
                        long received = val.get().getLong();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/SchemasResource.java`
#### Snippet
```java
        validateTopicName(tenant, cluster, namespace, topic);
        getAllSchemasAsync(authoritative)
                .thenApply(schemaAndMetadata -> convertToAllVersionsSchemaResponse(schemaAndMetadata))
                .thenAccept(response::resume)
                .exceptionally(ex -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/SchemasResource.java`
#### Snippet
```java
        validateTopicName(tenant, cluster, namespace, topic);
        getSchemaAsync(authoritative, version)
                .thenApply(schemaAndMetadata -> convertToSchemaResponse(schemaAndMetadata))
                .thenAccept(response::resume)
                .exceptionally(ex -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/SchemasResource.java`
#### Snippet
```java
        validateTopicName(tenant, cluster, namespace, topic);
        getSchemaAsync(authoritative)
                .thenApply(schemaAndMetadata -> convertToSchemaResponse(schemaAndMetadata))
                .thenApply(response::resume)
                .exceptionally(ex -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConfiguration.java`
#### Snippet
```java
        Pattern redirectPattern = Pattern.compile("^httpReverseProxy\\.([^\\.]*)\\.(.+)$");
        Map<String, List<Matcher>> groups = properties.stringPropertyNames().stream()
            .map((s) -> redirectPattern.matcher(s))
            .filter(Matcher::matches)
            .collect(Collectors.groupingBy((m) -> m.group(1))); // group by name
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/WebService.java`
#### Snippet
```java
        if (attributeMap != null) {
            attributeMap.forEach((key, value) -> {
                context.setAttribute(key, value);
            });
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/SchemasResource.java`
#### Snippet
```java
        validateTopicName(tenant, namespace, topic);
        getAllSchemasAsync(authoritative)
                .thenApply(schemaAndMetadata -> convertToAllVersionsSchemaResponse(schemaAndMetadata))
                .thenAccept(response::resume)
                .exceptionally(ex -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/SchemasResource.java`
#### Snippet
```java
        validateTopicName(tenant, namespace, topic);
        getSchemaAsync(authoritative, version)
                .thenApply(schemaAndMetadata -> convertToSchemaResponse(schemaAndMetadata))
                .thenAccept(response::resume)
                .exceptionally(ex -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/SchemasResource.java`
#### Snippet
```java
        validateTopicName(tenant, namespace, topic);
        getSchemaAsync(authoritative)
                .thenApply(schemaAndMetadata -> convertToSchemaResponse(schemaAndMetadata))
                .thenApply(response::resume)
                .exceptionally(ex -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
                .readOnly(true)
                .loadTopicsInBundle(false).build();
        return nsService.getWebServiceUrlAsync(nsBundle, options).thenApply(optionUrl -> optionUrl.isPresent());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
                            "Topic " + topicPartitionName + " not found"));
                } else {
                    topic.get().addSchema(schemaData).thenAccept(schemaVersion -> future.complete(schemaVersion))
                    .exceptionally(exception -> {
                        future.completeExceptionally(exception);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/BookKeeperClientFactoryImpl.java`
#### Snippet
```java
        ClientConfiguration bkConf = createBkClientConfiguration(store, conf);
        if (properties != null) {
            properties.forEach((key, value) -> bkConf.setProperty(key, value));
        }
        if (ensemblePlacementPolicyClass.isPresent()) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Bookies.java`
#### Snippet
```java
        getPulsarResources().getBookieResources().get()
                .thenAccept(b -> {
                    asyncResponse.resume(b.orElseGet(() -> new BookiesRackConfiguration()));
                }).exceptionally(ex -> {
            asyncResponse.resume(ex);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Bookies.java`
#### Snippet
```java
                .update(optionalBookiesRackConfiguration -> {
                    BookiesRackConfiguration brc = optionalBookiesRackConfiguration
                            .orElseGet(() -> new BookiesRackConfiguration());

                    if (!brc.removeBookie(bookieAddress)) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Bookies.java`
#### Snippet
```java
        getPulsarResources().getBookieResources().get()
                .thenAccept(b -> {
                    Optional<BookieInfo> bi = b.orElseGet(() -> new BookiesRackConfiguration())
                            .getBookie(bookieAddress);
                    if (bi.isPresent()) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Bookies.java`
#### Snippet
```java
                .update(optionalBookiesRackConfiguration -> {
                    BookiesRackConfiguration brc = optionalBookiesRackConfiguration
                            .orElseGet(() -> new BookiesRackConfiguration());

                    brc.updateBookie(group, bookieAddress, bookieInfo);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/PersistentTopics.java`
#### Snippet
```java
        try {
            validateTopicName(property, cluster, namespace, encodedTopic);
            internalGetPermissionsOnTopic().thenAccept(permissions -> asyncResponse.resume(permissions))
                    .exceptionally(ex -> {
                        log.error("[{}] Failed to get permissions for topic {}", clientAppId(), topicName, ex);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/ResourceQuotasBase.java`
#### Snippet
```java

    public ResourceQuota getDefaultResourceQuota() {
        return sync(() -> getDefaultResourceQuotaAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TenantsBase.java`
#### Snippet
```java
                    return futures;
                })
                .thenCompose(futures -> FutureUtil.waitForAll(futures))
                .thenCompose(__ -> internalDeleteTenantAsync(tenant));
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/TenantsBase.java`
#### Snippet
```java
        if (info == null || info.getAllowedClusters().stream().filter(c -> !StringUtils.isBlank(c))
                .collect(Collectors.toSet()).isEmpty()
                || info.getAllowedClusters().stream().anyMatch(ac -> StringUtils.isBlank(ac))) {
            log.warn("[{}] Failed to validate due to clusters are empty", clientAppId());
            return FutureUtil.failedFuture(new RestException(Status.PRECONDITION_FAILED, "Clusters can not be empty"));
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
        validateNamespaceName(tenant, namespace);
        internalGetDeduplicationAsync()
                .thenAccept(deduplication -> asyncResponse.resume(deduplication))
                .exceptionally(ex -> {
                    log.error("Failed to get broker deduplication config for namespace {}", namespace, ex);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
        validateNamespaceName(tenant, namespace);
        internalGetTopicDispatchRateAsync()
                .thenAccept(dispatchRate -> asyncResponse.resume(dispatchRate))
                .exceptionally(ex -> {
                    resumeAsyncResponseExceptionally(asyncResponse, ex);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
        validateNamespaceName(tenant, namespace);
        internalGetSubscribeRateAsync()
                .thenAccept(subscribeRate -> asyncResponse.resume(subscribeRate))
                .exceptionally(ex -> {
                    log.error("[{}] Failed to get subscribe rate for namespace {}", clientAppId(), namespaceName, ex);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/tools/GenerateDocsCommand.java`
#### Snippet
```java
    public Boolean apply(CliFlags globalFlags, String[] args) {
        CliSpec<GenDocFlags> newSpec = CliSpec.newBuilder(spec)
                .withRunFunc(cmdFlags -> apply(cmdFlags))
                .build();
        return 0 == Cli.runCli(newSpec, args);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/tools/LoadReportCommand.java`
#### Snippet
```java
    public Boolean apply(CliFlags globalFlags, String[] args) {
        CliSpec<Flags> newSpec = CliSpec.newBuilder(spec)
            .withRunFunc(cmdFlags -> apply(cmdFlags))
            .build();
        return 0 == Cli.runCli(newSpec, args);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/v1/TopicLookup.java`
#### Snippet
```java
        }
        internalLookupTopicAsync(topicName, authoritative, listenerName)
                .thenAccept(lookupData -> asyncResponse.resume(lookupData))
                .exceptionally(ex -> {
                    if (log.isDebugEnabled()) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/v2/TopicLookup.java`
#### Snippet
```java
        }
        internalLookupTopicAsync(topicName, authoritative, listenerName)
                .thenAccept(lookupData -> asyncResponse.resume(lookupData))
                .exceptionally(ex -> {
                    if (log.isDebugEnabled()) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/PulsarService.java`
#### Snippet
```java
            new ClientConfiguration().getZkLedgersRootPath(),
            config.isBookkeeperMetadataStoreSeparated() ? config.getBookkeeperMetadataStoreUrl() : null,
            this.getWorkerConfig().map(wc -> wc.getStateStorageServiceUrl()).orElse(null));
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/PersistentTopics.java`
#### Snippet
```java
        try {
            validateTopicName(tenant, namespace, encodedTopic);
            internalGetPermissionsOnTopic().thenAccept(permissions -> asyncResponse.resume(permissions))
                    .exceptionally(ex -> {
                        log.error("[{}] Failed to get permissions for topic {}", clientAppId(), topicName, ex);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
        log.info("[{}] Starting replicator", replicatorId);
        producerBuilder.createAsync().thenAccept(producer -> {
            readEntries(producer);
        }).exceptionally(ex -> {
            if (STATE_UPDATER.compareAndSet(this, State.Starting, State.Stopped)) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                this.resourceGroupPublishLimiter = resourceGroup.getResourceGroupPublishLimiter();
                this.resourceGroupPublishLimiter.registerRateLimitFunction(this.getName(),
                  () -> this.enableCnxAutoRead());
                log.info("Using resource group {} rate limiter for topic {}", rgName, topic);
                return;
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
                    if (preciseTopicPublishRateLimitingEnable) {
                        this.topicPublishRateLimiter = new PrecisPublishLimiter(publishRate,
                            () -> this.enableCnxAutoRead(), brokerService.pulsar().getExecutor());
                    } else {
                        this.topicPublishRateLimiter = new PublishRateLimiterImpl(publishRate);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
            // no permits from rate limit
            topic.getBrokerService().executor().schedule(
                () -> readMoreEntries(), MESSAGE_RATE_BACKOFF_MS, TimeUnit.MILLISECONDS);
        } else {
            if (log.isDebugEnabled()) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/streamingdispatch/StreamingEntryReader.java`
#### Snippet
```java
    public void entriesAvailable() {
        topic.getBrokerService().getTopicOrderedExecutor().executeOrdered(dispatcher.getName(), SafeRun.safeRun(() -> {
            internalEntriesAvailable();
        }));
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/protocol/ProtocolHandlers.java`
#### Snippet
```java
        return handlers.entrySet().stream()
            .collect(Collectors.toMap(
                e -> e.getKey(),
                e -> e.getValue().getProtocolDataToAdvertise()
            ));
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
                        .thenCombine(internalGetNonPersistentTopics(policies),
                                (persistentTopics, nonPersistentTopics) ->
                                        ListUtils.union(persistentTopics, nonPersistentTopics));
            case NON_PERSISTENT:
                return internalGetNonPersistentTopics(policies);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
            brokersData.listLocks(LoadManager.LOADBALANCE_BROKERS_ROOT)
                    .thenAccept(brokers -> {
                        reapDeadBrokerPreallocations(brokers);
                    });

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
        pulsar.getPulsarResources().getClusterResources().getFailureDomainResources()
                .registerListener(__ -> {
                    scheduler.execute(() -> refreshBrokerToFailureDomainMap());
                });

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/InMemTransactionBuffer.java`
#### Snippet
```java
            synchronized (entries) {
                SortedMap<Long, ByteBuf> subEntries = entries.tailMap(sequenceId);
                subEntries.values().forEach(value -> value.retain());
                entriesToRead.putAll(subEntries);
            }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
            long totalTopics = brokerData != null && brokerData.getPreallocatedBundleData() != null
                    ? brokerData.getPreallocatedBundleData().values().stream()
                            .mapToLong((preAllocatedBundle) -> preAllocatedBundle.getTopics()).sum()
                            + brokerData.getLocalData().getNumTopics()
                    : 0;
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            Policies policies = brokerService.pulsar().getPulsarResources().getNamespaceResources()
                    .getPolicies(name.getNamespaceObject())
                    .orElseThrow(() -> new MetadataStoreException.NotFoundException());
            final int defaultExpirationTime = brokerService.pulsar().getConfiguration()
                    .getSubscriptionExpirationTimeMinutes();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    private Optional<CompactorMXBean> getCompactorMXBean() {
        Compactor compactor = brokerService.pulsar().getNullableCompactor();
        return Optional.ofNullable(compactor).map(c -> c.getStats());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/buffer/impl/TransactionBufferHandlerImpl.java`
#### Snippet
```java
        CompletableFuture<NamespaceBundle> nsBundle = namespaceService.getBundleAsync(TopicName.get(topic));
        return nsBundle
                .thenCompose(bundle -> namespaceService.getOwnerAsync(bundle))
                .thenCompose(data -> {
                    if (data.isPresent()) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java

    private CompletableFuture<Boolean> isTopicOwnedAsync(TopicName topic) {
        return getBundleAsync(topic).thenApply(bundle -> ownershipCache.isNamespaceBundleOwned(bundle));
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/MLPendingAckStore.java`
#### Snippet
```java
                                     List<PendingAckMetadataEntry> logList) {
        Stream<PendingAckMetadata> pendingAckMetaStream = logList.stream()
                .filter(log -> bothNotAbortAndCommitPredicate.test(log))
                .flatMap(log -> log.getPendingAckMetadatasList().stream());
        handleMetadataEntry(logPosition, pendingAckMetaStream);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleFactory.java`
#### Snippet
```java
                // If no local policies defined for namespace, copy from global config
                copyToLocalPolicies(namespace)
                        .thenAccept(b -> future.complete(b))
                        .exceptionally(ex -> {
                            handleLoadBundlesRetry(namespace, future, backoff, retryDeadline, ex);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                                                pulsar.getBrokerService()
                                                        .createDefaultPartitionedTopicAsync(topicName, policies)
                                                        .thenAccept(md -> future.complete(md))
                                                        .exceptionally(ex -> {
                                                            if (ex.getCause()
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                .getPartitionedTopicMetadataAsync(topicName).thenApply(metadata -> {
                    // if the partitioned topic is not found in metadata, then the topic is not partitioned
                    return metadata.orElseGet(() -> new PartitionedTopicMetadata());
                });
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            log.info("Starting blocking dispatchers with unacked msgs {} due to reached max broker limit {}",
                    maxUnackedMessages, maxUnackedMsgsPerDispatcher);
            executor().execute(() -> blockDispatchersWithLargeUnAckMessages());
        } else if (blockedDispatcherOnHighUnackedMsgs.get() && unAckedMessages < maxUnackedMessages / 2) {
            // unblock broker-dispatching if received enough acked messages back
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        if (pulsar().getConfiguration().isBrokerDeleteInactiveTopicsEnabled()) {
            int interval = pulsar().getConfiguration().getBrokerDeleteInactiveTopicsFrequencySeconds();
            inactivityMonitor.scheduleAtFixedRate(safeRun(() -> checkGC()), interval, interval,
                    TimeUnit.SECONDS);
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        int interval = pulsar().getConfiguration().getClusterMigrationCheckDurationSeconds();
        if (interval > 0) {
            inactivityMonitor.scheduleAtFixedRate(safeRun(() -> checkClusterMigration()), interval, interval,
                    TimeUnit.SECONDS);
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            dispatcherList.forEach(dispatcher -> {
                dispatcher.unBlockDispatcherOnUnackedMsgs();
                executor().execute(() -> dispatcher.readMoreEntries());
                log.info("[{}] Dispatcher is unblocked", dispatcher.getName());
                blockedDispatchers.remove(dispatcher);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
        int interval = pulsar().getConfiguration().getBrokerServiceCompactionMonitorIntervalInSeconds();
        if (interval > 0) {
            compactionMonitor.scheduleAtFixedRate(safeRun(() -> checkCompaction()),
                                                  interval, interval, TimeUnit.SECONDS);
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageRedeliveryTracker.java`
#### Snippet
```java
            redeliveryMessageIdPartitionMap.clear();
            redeliveryTimePartitions.forEach(tp -> {
                        tp.forEach(unackMessageIdWrapper -> unackMessageIdWrapper.recycle());
                        tp.clear();
                    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/UnAckedMessageTracker.java`
#### Snippet
```java
        try {
            messageIdPartitionMap.clear();
            timePartitions.forEach(tp -> tp.clear());
        } finally {
            writeLock.unlock();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/TrustManagerProxy.java`
#### Snippet
```java
            throw new IllegalArgumentException(e);
        }
        executor.scheduleWithFixedDelay(() -> updateTrustManagerSafely(), refreshDurationSec, refreshDurationSec,
                TimeUnit.SECONDS);
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/KeyManagerProxy.java`
#### Snippet
```java
            throw new IllegalArgumentException(e);
        }
        executor.scheduleWithFixedDelay(() -> updateKeyManagerSafely(), refreshDurationSec, refreshDurationSec,
                TimeUnit.SECONDS);
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FutureUtil.java`
#### Snippet
```java
            }
            Collection<CompletableFuture<?>> doneFutures = futures.stream()
                    .filter(f -> f.isDone())
                    .collect(Collectors.toList());
            futures.removeAll(doneFutures);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentSortedLongPairSet.java`
#### Snippet
```java
    @Override
    public Set<LongPair> items(int numberOfItems) {
        return items(numberOfItems, (item1, item2) -> new LongPair(item1, item2));
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashSet.java`
#### Snippet
```java
    public List<V> values() {
        List<V> values = new ArrayList<>();
        forEach(value -> values.add(value));
        return values;
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentLongPairSet.java`
#### Snippet
```java
     */
    public Set<LongPair> items(int numberOfItems) {
        return items(numberOfItems, (item1, item2) -> new LongPair(item1, item2));
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheImpl.java`
#### Snippet
```java
        } else {
            if (!cachedEntries.isEmpty()) {
                cachedEntries.forEach(entry -> entry.release());
            }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
    @Override
    public boolean isConnected() {
        return consumers.values().stream().allMatch(consumer -> consumer.isConnected());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                    .thenAccept(finalFuture -> {
                        List<ConsumerImpl<T>> newConsumerList = newPartitions.stream()
                            .map(partitionTopic -> consumers.get(partitionTopic))
                            .collect(Collectors.toList());
                        startReceivingMessages(newConsumerList);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
                consumers.get(topicName)
                    .redeliverUnacknowledgedMessages(messageIds1.stream()
                        .map(mid -> mid.getInnerMessageId()).collect(Collectors.toSet())));
        resumeReceivingFromPausedConsumersIfNeeded();
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlockAwareSegmentInputStreamImpl.java`
#### Snippet
```java
            dataBlockHeaderStream.close();
            if (!entriesByteBuf.isEmpty()) {
                entriesByteBuf.forEach(buf -> buf.release());
                entriesByteBuf.clear();
            }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
                                // The z-node does not exist, let's create it first
                                put(opPut.getPath(), opPut.getData(), Optional.of(-1L)).thenAccept(
                                                s -> future.complete(s))
                                        .exceptionally(ex -> {
                                            if (ex.getCause() instanceof BadVersionException) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
                }
                readLedgerMetadata(ledgerId)
                        .whenComplete((metadata, exception) -> handleMetadata(metadata, exception));
            } else {
                if (log.isDebugEnabled()) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java

    private static final ThreadLocal<StringBuilder> threadLocalNodeBuilder =
            ThreadLocal.withInitial(() -> new StringBuilder());

    /**
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
                    return null;
                }
            }).filter((t) -> t != null)
                    .reduce(Math::max);

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerIdGenerator.java`
#### Snippet
```java
                                        }
                                    })
                                    .filter((t) -> t != null)
                                    .sorted()
                                    .toArray();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/coordination/impl/LeaderElectionImpl.java`
#### Snippet
```java

                        elect()
                            .thenAccept(lse -> result.complete(lse))
                            .exceptionally(ex2 -> {
                                result.completeExceptionally(ex2);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/MembershipManager.java`
#### Snippet
```java

        Set<String> currentMembership = this.getCurrentMembership().stream()
                .map(entry -> entry.getWorkerId()).collect(Collectors.toSet());
        List<Function.FunctionMetaData> functionMetaDataList = functionMetaDataManager.getAllFunctionMetaData();
        Map<String, Function.FunctionMetaData> functionMetaDataMap = new HashMap<>();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/io/ConnectorUtils.java`
#### Snippet
```java
            return futures.stream()
                    .map(CompletableFuture::join)
                    .filter(entry -> entry != null)
                    .collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue, (a, b) -> a, TreeMap::new));
        } finally {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
                        if (sub != null) {
                            existingConsumers = sub.getConsumers().stream()
                              .map(consumerStats -> consumerStats.getMetadata())
                              .collect(Collectors.toList());
                        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/ConnectorsManager.java`
#### Snippet
```java

    public List<ConnectorDefinition> getConnectorDefinitions() {
        return connectors.values().stream().map(connector -> connector.getConnectorDefinition())
                .collect(Collectors.toList());
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            }
            // run with executor to prevent deadlock
            ledger.getExecutor().executeOrdered(ledger.getName(), safeRun(() -> mdEntry.triggerComplete()));
            return;
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            }
            // run with executor to prevent deadlock
            ledger.getExecutor().executeOrdered(ledger.getName(), safeRun(() -> mdEntry.triggerComplete()));
            return;
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/AddWindowFunction.java`
#### Snippet
```java
    @Override
    public Integer apply(Collection<Integer> integers) {
        return integers.stream().reduce(0, (x, y) -> x + y);
    }
}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                    : timeoutSec;
            this.timeoutTask = this.scheduledExecutor.scheduleAtFixedRate(safeRun(() -> {
                checkTimeouts();
            }), timeoutSec, timeoutSec, TimeUnit.SECONDS);
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ReaderHandler.java`
#### Snippet
```java
            if (pending < maxPendingMessages) {
                // Start next read in a separate thread to avoid recursion
                service.getExecutor().execute(() -> receiveMessage());
            }
        }).exceptionally(exception -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
                processWindow(
                        context,
                        tuples.stream().map(event -> event.get()).collect(Collectors.toList()),
                        newTuples.stream().map(event -> event.get()).collect(Collectors.toList()),
                        expiredTuples.stream().map(event -> event.get()).collect(Collectors.toList()),
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
                        context,
                        tuples.stream().map(event -> event.get()).collect(Collectors.toList()),
                        newTuples.stream().map(event -> event.get()).collect(Collectors.toList()),
                        expiredTuples.stream().map(event -> event.get()).collect(Collectors.toList()),
                        referenceTime);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/WindowFunctionExecutor.java`
#### Snippet
```java
                        tuples.stream().map(event -> event.get()).collect(Collectors.toList()),
                        newTuples.stream().map(event -> event.get()).collect(Collectors.toList()),
                        expiredTuples.stream().map(event -> event.get()).collect(Collectors.toList()),
                        referenceTime);
            }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/ResourceGroupsImpl.java`
#### Snippet
```java
    @Override
    public List<String> getResourceGroups() throws PulsarAdminException {
        return sync(() -> getResourceGroupsAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/WorkerImpl.java`
#### Snippet
```java
    @Override
    public Map<String, Collection<String>> getAssignments() throws PulsarAdminException {
        return sync(() -> getAssignmentsAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/WorkerImpl.java`
#### Snippet
```java
    @Override
    public WorkerInfo getClusterLeader() throws PulsarAdminException {
        return sync(() -> getClusterLeaderAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/WorkerImpl.java`
#### Snippet
```java
    @Override
    public List<WorkerInfo> getCluster() throws PulsarAdminException {
        return sync(() -> getClusterAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/WorkerImpl.java`
#### Snippet
```java
    @Override
    public List<WorkerFunctionInstanceStats> getFunctionsStats() throws PulsarAdminException {
        return sync(() -> getFunctionsStatsAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/WorkerImpl.java`
#### Snippet
```java
    @Override
    public Collection<Metrics> getMetrics() throws PulsarAdminException {
        return sync(() -> getMetricsAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/ResourceQuotasImpl.java`
#### Snippet
```java
    @Override
    public ResourceQuota getDefaultResourceQuota() throws PulsarAdminException {
        return sync(() -> getDefaultResourceQuotaAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/SinksImpl.java`
#### Snippet
```java
    @Override
    public List<ConnectorDefinition> getBuiltInSinks() throws PulsarAdminException {
        return sync(() -> getBuiltInSinksAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/SinksImpl.java`
#### Snippet
```java
    @Override
    public void reloadBuiltInSinks() throws PulsarAdminException {
        sync(() -> reloadBuiltInSinksAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/SourcesImpl.java`
#### Snippet
```java
    @Override
    public void reloadBuiltInSources() throws PulsarAdminException {
        sync(() -> reloadBuiltInSourcesAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/SourcesImpl.java`
#### Snippet
```java
    @Override
    public List<ConnectorDefinition> getBuiltInSources() throws PulsarAdminException {
        return sync(() -> getBuiltInSourcesAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TenantsImpl.java`
#### Snippet
```java
    @Override
    public List<String> getTenants() throws PulsarAdminException {
        return sync(() -> getTenantsAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
    @Override
    public Map<Integer, TransactionCoordinatorStats> getCoordinatorStats() throws PulsarAdminException {
        return sync(() -> getCoordinatorStatsAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TransactionsImpl.java`
#### Snippet
```java
    @Override
    public List<TransactionCoordinatorInfo> listTransactionCoordinators() throws PulsarAdminException {
        return sync(() -> listTransactionCoordinatorsAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/BrokersImpl.java`
#### Snippet
```java
    @Override
    public BrokerInfo getLeaderBroker() throws PulsarAdminException {
        return sync(() -> getLeaderBrokerAsync());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
                    .listConfigs()
                    .stream()
                    .map(e -> formatEntry(e))
                    .collect(Collectors.toList())
            );
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/BookieResources.java`
#### Snippet
```java
    public CompletableFuture<Set<String>> listAvailableBookiesAsync() {
        return getChildrenAsync(AVAILABLE_BOOKIES_ROOT)
                .thenApply(list -> new HashSet<>(list));
    }
}
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/MultiRolesTokenAuthorizationProvider.java`
#### Snippet
```java
        List<CompletableFuture<Boolean>> futures = new ArrayList<>(roles.size());
        roles.forEach(r -> futures.add(authorizeFunc.apply(r)));
        return FutureUtil.waitForAny(futures, ret -> (boolean) ret).thenApply(v -> v.isPresent());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/resources/NamespaceResources.java`
#### Snippet
```java
                        return CompletableFuture.completedFuture(false);
                    } else {
                        return getChildrenAsync(path).thenApply(children -> children.isEmpty());
                    }
                });
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderBasic.java`
#### Snippet
```java

        users = new HashMap<>();
        for (String line : reader.lines().toArray(s -> new String[s])) {
            List<String> splitLine = Arrays.asList(line.split(":"));
            if (splitLine.size() != 2) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java

class Slf4jEvent implements Event {
    private static final ThreadLocal<Map<Object, Logger>> loggersTLS = ThreadLocal.withInitial(() -> new HashMap<>());
    private static final Logger stringLogger = LoggerFactory.getLogger("stevlog");

```

## RuleId[ruleID=PatternVariableCanBeUsed]
### PatternVariableCanBeUsed
Variable 'decimal' can be replaced with pattern variable
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                BigDecimal decimal = (BigDecimal) value;
                return jsonNodeFactory.numberNode(decimal);
            }
```

### PatternVariableCanBeUsed
Variable 'epochMillis' can be replaced with pattern variable
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long epochMillis = (Long) value;
                return jsonNodeFactory.numberNode(epochMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'daysFromEpoch' can be replaced with pattern variable
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Integer daysFromEpoch = (Integer) value;
                return jsonNodeFactory.numberNode(daysFromEpoch);
            }
```

### PatternVariableCanBeUsed
Variable 'timeMicro' can be replaced with pattern variable
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long timeMicro = (Long) value;
                return jsonNodeFactory.numberNode(timeMicro);
            }
```

### PatternVariableCanBeUsed
Variable 'timeMillis' can be replaced with pattern variable
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Integer timeMillis = (Integer) value;
                return jsonNodeFactory.numberNode(timeMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'epochMillis' can be replaced with pattern variable
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long epochMillis = (Long) value;
                return jsonNodeFactory.numberNode(epochMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'httpContent' can be replaced with pattern variable
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/http/NettyHttpServerHandler.java`
#### Snippet
```java

        if (msg instanceof HttpContent) {
            HttpContent httpContent = (HttpContent) msg;

            ByteBuf content = httpContent.content();
```

### PatternVariableCanBeUsed
Variable 'trailer' can be replaced with pattern variable
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/http/NettyHttpServerHandler.java`
#### Snippet
```java

            if (msg instanceof LastHttpContent) {
                LastHttpContent trailer = (LastHttpContent) msg;

                if (!writeResponse(trailer, ctx)) {
```

### PatternVariableCanBeUsed
Variable 'epochMillis' can be replaced with pattern variable
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long epochMillis = (Long) value;
                return jsonNodeFactory.numberNode(epochMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'timeMillis' can be replaced with pattern variable
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Integer timeMillis = (Integer) value;
                return jsonNodeFactory.numberNode(timeMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'decimal' can be replaced with pattern variable
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                BigDecimal decimal = (BigDecimal) value;
                return jsonNodeFactory.numberNode(decimal);
            }
```

### PatternVariableCanBeUsed
Variable 'epochMillis' can be replaced with pattern variable
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long epochMillis = (Long) value;
                return jsonNodeFactory.numberNode(epochMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'timeMicro' can be replaced with pattern variable
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long timeMicro = (Long) value;
                return jsonNodeFactory.numberNode(timeMicro);
            }
```

### PatternVariableCanBeUsed
Variable 'daysFromEpoch' can be replaced with pattern variable
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Integer daysFromEpoch = (Integer) value;
                return jsonNodeFactory.numberNode(daysFromEpoch);
            }
```

### PatternVariableCanBeUsed
Variable 'failedException' can be replaced with pattern variable
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSink.java`
#### Snippet
```java
            if (exception instanceof UserRecordFailedException) {
                // If the exception is UserRecordFailedException, we need to extract it to see real error messages.
                UserRecordFailedException failedException = (UserRecordFailedException) exception;
                StringBuffer stringBuffer = new StringBuffer();
                failedException.getResult().getAttempts().forEach(attempt ->
```

### PatternVariableCanBeUsed
Variable 'tagsRecord' can be replaced with pattern variable
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSink.java`
#### Snippet
```java
        if (null != tagsField) {
            if (tagsField instanceof GenericRecord) { // JSONSchema
                GenericRecord tagsRecord = (GenericRecord) tagsField;
                for (Field field : tagsRecord.getFields()) {
                    val fieldName = field.getName();
```

### PatternVariableCanBeUsed
Variable 'columnsRecord' can be replaced with pattern variable
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/v2/InfluxDBSink.java`
#### Snippet
```java
        val columnsField = genericRecord.getField("fields");
        if (columnsField instanceof GenericRecord) { // JSONSchema
            val columnsRecord = (GenericRecord) columnsField;
            for (Field field : columnsRecord.getFields()) {
                val fieldName = field.getName();
```

### PatternVariableCanBeUsed
Variable 'epochMillis' can be replaced with pattern variable
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long epochMillis = (Long) value;
                return jsonNodeFactory.numberNode(epochMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'timeMillis' can be replaced with pattern variable
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Integer timeMillis = (Integer) value;
                return jsonNodeFactory.numberNode(timeMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'epochMillis' can be replaced with pattern variable
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long epochMillis = (Long) value;
                return jsonNodeFactory.numberNode(epochMillis);
            }
```

### PatternVariableCanBeUsed
Variable 'daysFromEpoch' can be replaced with pattern variable
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Integer daysFromEpoch = (Integer) value;
                return jsonNodeFactory.numberNode(daysFromEpoch);
            }
```

### PatternVariableCanBeUsed
Variable 'decimal' can be replaced with pattern variable
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                BigDecimal decimal = (BigDecimal) value;
                return jsonNodeFactory.numberNode(decimal);
            }
```

### PatternVariableCanBeUsed
Variable 'timeMicro' can be replaced with pattern variable
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
                            + value.getClass());
                }
                Long timeMicro = (Long) value;
                return jsonNodeFactory.numberNode(timeMicro);
            }
```

### PatternVariableCanBeUsed
Variable 'requestWithId' can be replaced with pattern variable
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/opensearch/OpenSearchHighLevelRestClient.java`
#### Snippet
```java
                                        final Record pulsarRecord;
                                        if (docWriteRequest instanceof DocWriteRequestWithPulsarRecord) {
                                            DocWriteRequestWithPulsarRecord requestWithId =
                                                    (DocWriteRequestWithPulsarRecord) docWriteRequest;
                                            pulsarRecord = requestWithId.getPulsarRecord();
```

### PatternVariableCanBeUsed
Variable 'num' can be replaced with pattern variable
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
            // Kafka's ConnectSchema expects exact type
            // https://github.com/apache/kafka/blob/trunk/connect/api/src/main/java/org/apache/kafka/connect/data/ConnectSchema.java#L47-L71
            Number num = (Number) nativeObject;
            switch (kafkaSchema.type()) {
                case INT8:
```

### PatternVariableCanBeUsed
Variable 'ch' can be replaced with pattern variable
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java

        if (nativeObject instanceof Character) {
            Character ch = (Character) nativeObject;
            if (kafkaSchema.type() == Schema.Type.STRING) {
                return ch.toString();
```

### PatternVariableCanBeUsed
Variable 'node' can be replaced with pattern variable
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java

        if (nativeObject instanceof JsonNode) {
            JsonNode node = (JsonNode) nativeObject;
            return jsonAsConnectData(node, kafkaSchema);
        }
```

### PatternVariableCanBeUsed
Variable 'arr' can be replaced with pattern variable
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
            case ARRAY:
                 if (nativeObject instanceof List) {
                    List arr = (List) nativeObject;
                    return arr.stream()
                            .map(x -> getKafkaConnectData(x, kafkaSchema.valueSchema()))
```

### PatternVariableCanBeUsed
Variable 'kv' can be replaced with pattern variable
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
                    return responseMap;
                } else if (nativeObject instanceof org.apache.pulsar.common.schema.KeyValue) {
                    org.apache.pulsar.common.schema.KeyValue kv =
                            (org.apache.pulsar.common.schema.KeyValue) nativeObject;
                    Map<Object, Object> responseMap = new HashMap<>();
```

### PatternVariableCanBeUsed
Variable 'avroRecord' can be replaced with pattern variable
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
            case STRUCT:
                if (nativeObject instanceof GenericData.Record) {
                    GenericData.Record avroRecord = (GenericData.Record) nativeObject;
                    return avroAsConnectData(avroRecord, kafkaSchema);
                } else if (nativeObject instanceof GenericRecord) {
```

### PatternVariableCanBeUsed
Variable 'pulsarGenericRecord' can be replaced with pattern variable
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
                    return avroAsConnectData(avroRecord, kafkaSchema);
                } else if (nativeObject instanceof GenericRecord) {
                    GenericRecord pulsarGenericRecord = (GenericRecord) nativeObject;
                    // Pulsar's GenericRecord
                    if (pulsarGenericRecord.getNativeObject() instanceof JsonNode
```

### PatternVariableCanBeUsed
Variable 'kv' can be replaced with pattern variable
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java

            if (nativeObject instanceof KeyValue) {
                KeyValue kv = (KeyValue) nativeObject;
                key = KafkaConnectData.getKafkaConnectData(kv.getKey(), keySchema);
                value = KafkaConnectData.getKafkaConnectData(kv.getValue(), valueSchema);
```

### PatternVariableCanBeUsed
Variable 'decimal' can be replaced with pattern variable
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroRowDecoderFactory.java`
#### Snippet
```java
                //  When the precision > 36, throw Exception.
                if (logicalType instanceof LogicalTypes.Decimal) {
                    LogicalTypes.Decimal decimal = (LogicalTypes.Decimal) logicalType;
                    return DecimalType.createDecimalType(decimal.getPrecision(), decimal.getScale());
                }
```

### PatternVariableCanBeUsed
Variable 'decimal' can be replaced with pattern variable
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonRowDecoderFactory.java`
#### Snippet
```java
                //  When the precision > 36, throw Exception.
                if (logicalType instanceof LogicalTypes.Decimal) {
                    LogicalTypes.Decimal decimal = (LogicalTypes.Decimal) logicalType;
                    return DecimalType.createDecimalType(decimal.getPrecision(), decimal.getScale());
                }
```

### PatternVariableCanBeUsed
Variable 'message' can be replaced with pattern variable
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeColumnDecoder.java`
#### Snippet
```java
            //return millisecond which parsed from protobuf/timestamp
            if (TimestampType.TIMESTAMP_MILLIS.equals(columnType) && value instanceof DynamicMessage) {
                DynamicMessage message = (DynamicMessage) value;
                int nanos = (int) message.getField(message.getDescriptorForType().findFieldByName("nanos"));
                long seconds = (long) message.getField(message.getDescriptorForType().findFieldByName("seconds"));
```

### PatternVariableCanBeUsed
Variable 'clientAddress' can be replaced with pattern variable
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/DirectProxyHandler.java`
#### Snippet
```java
            if (inboundChannel.remoteAddress() instanceof InetSocketAddress
                    && inboundChannel.localAddress() instanceof InetSocketAddress) {
                InetSocketAddress clientAddress = (InetSocketAddress) inboundChannel.remoteAddress();
                String sourceAddress = clientAddress.getAddress().getHostAddress();
                int sourcePort = clientAddress.getPort();
```

### PatternVariableCanBeUsed
Variable 'proxyAddress' can be replaced with pattern variable
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/DirectProxyHandler.java`
#### Snippet
```java
                String sourceAddress = clientAddress.getAddress().getHostAddress();
                int sourcePort = clientAddress.getPort();
                InetSocketAddress proxyAddress = (InetSocketAddress) inboundChannel.localAddress();
                String destinationAddress = proxyAddress.getAddress().getHostAddress();
                int destinationPort = proxyAddress.getPort();
```

### PatternVariableCanBeUsed
Variable 'ph' can be replaced with pattern variable
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/extensions/ProxyExtensionsUtils.java`
#### Snippet
```java
                    + " does not implement extension interface");
            }
            ProxyExtension ph = (ProxyExtension) extension;
            return new ProxyExtensionWithClassLoader(ph, ncl);
        } catch (Throwable t) {
```

### PatternVariableCanBeUsed
Variable 'e' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/RestException.java`
#### Snippet
```java
    private static Response getResponse(Throwable t) {
        if (t instanceof WebApplicationException) {
            WebApplicationException e = (WebApplicationException) t;
            return e.getResponse();
        } else {
```

### PatternVariableCanBeUsed
Variable 'req' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/MaxRequestSizeFilter.java`
#### Snippet
```java
    private static boolean isChunked(ServletRequest request) {
        if (request instanceof HttpServletRequest) {
            HttpServletRequest req = (HttpServletRequest) request;
            String encoding = req.getHeader("Transfer-Encoding");
            return encoding != null && encoding.contains("chunked");
```

### PatternVariableCanBeUsed
Variable 'persistentTopic' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/BookieClientStatsGenerator.java`
#### Snippet
```java
            pulsar.getBrokerService().forEachTopic(topic -> {
                if (topic instanceof PersistentTopic) {
                    PersistentTopic persistentTopic = (PersistentTopic) topic;
                    TopicName topicName = TopicName.get(persistentTopic.getName());
                    put(topicName, persistentTopic.getManagedLedger().getStats().getPendingBookieOpsStats());
```

### PatternVariableCanBeUsed
Variable 'output' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsGenerator.java`
#### Snippet
```java
        try {
            if (out instanceof HttpOutput) {
                HttpOutput output = (HttpOutput) out;
                //no mem_copy and memory allocations here
                ByteBuffer[] buffers = buffer.nioBuffers();
```

### PatternVariableCanBeUsed
Variable 'other' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/Consumer.java`
#### Snippet
```java
    public boolean equals(Object obj) {
        if (obj instanceof Consumer) {
            Consumer other = (Consumer) obj;
            return Objects.equals(cnx.clientAddress(), other.cnx.clientAddress()) && consumerId == other.consumerId;
        }
```

### PatternVariableCanBeUsed
Variable 'pi' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/plugin/EntryFilterProvider.java`
#### Snippet
```java
                        + " does not implement entry filter interface");
            }
            EntryFilter pi = (EntryFilter) filter;
            return new EntryFilterWithClassLoader(pi, ncl);
        } catch (Exception e) {
```

### PatternVariableCanBeUsed
Variable 'consumer' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/SubscribeRateLimiter.java`
#### Snippet
```java
        public boolean equals(Object obj) {
            if (obj instanceof ConsumerIdentifier) {
                ConsumerIdentifier consumer = (ConsumerIdentifier) obj;
                return Objects.equals(host, consumer.host)
                        && Objects.equals(consumerName, consumer.consumerName)
```

### PatternVariableCanBeUsed
Variable 'pae' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        topicName, ex.getMessage());
                if (ex instanceof PulsarAdminException) {
                    PulsarAdminException pae = (PulsarAdminException) ex;
                    if (pae.getStatusCode() == Status.NOT_FOUND.getStatusCode()) {
                        asyncResponse.resume(new RestException(Status.NOT_FOUND,
```

### PatternVariableCanBeUsed
Variable 'topic' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                    return;
                }
                PersistentTopic topic = (PersistentTopic) t;

                boolean issued;
```

### PatternVariableCanBeUsed
Variable 'topic' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                        return;
                    }
                    PersistentTopic topic = (PersistentTopic) t;
                    final List<CompletableFuture<Void>> futures =
                            new ArrayList<>((int) topic.getReplicators().size());
```

### PatternVariableCanBeUsed
Variable 'deletedEntry' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
                exception.getMessage(), exception);
        if (ctx instanceof PositionImpl) {
            PositionImpl deletedEntry = (PositionImpl) ctx;
            if (deletedEntry.compareTo((PositionImpl) cursor.getMarkDeletedPosition()) > 0) {
                brokerService.getPulsar().getExecutor().schedule(
```

### PatternVariableCanBeUsed
Variable 'ph' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/protocol/ProtocolHandlerUtils.java`
#### Snippet
```java
                    + " does not implement protocol handler interface");
            }
            ProtocolHandler ph = (ProtocolHandler) handler;
            return new ProtocolHandlerWithClassLoader(ph, ncl);
        } catch (Throwable t) {
```

### PatternVariableCanBeUsed
Variable 'tbqe' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
                Throwable cause = exception.getCause();
                if (cause instanceof BrokerServiceException.TopicBacklogQuotaExceededException) {
                    BrokerServiceException.TopicBacklogQuotaExceededException tbqe =
                            (BrokerServiceException.TopicBacklogQuotaExceededException) cause;
                    IllegalStateException illegalStateException = new IllegalStateException(tbqe);
```

### PatternVariableCanBeUsed
Variable 'inetAddress' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
            return proxyMessage.sourceAddress();
        } else if (remoteAddress instanceof InetSocketAddress) {
            InetSocketAddress inetAddress = (InetSocketAddress) remoteAddress;
            return inetAddress.getAddress().getHostAddress();
        } else {
```

### PatternVariableCanBeUsed
Variable 'pi' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/BrokerInterceptorUtils.java`
#### Snippet
```java
                        + " does not implement broker interceptor interface");
            }
            BrokerInterceptor pi = (BrokerInterceptor) interceptor;
            return new BrokerInterceptorWithClassLoader(pi, ncl);
        } catch (Throwable t) {
```

### PatternVariableCanBeUsed
Variable 'd' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        if (Subscription.isIndividualAckMode(subType)) {
            if (dispatcher instanceof PersistentDispatcherMultipleConsumers) {
                PersistentDispatcherMultipleConsumers d = (PersistentDispatcherMultipleConsumers) dispatcher;
                subStats.unackedMessages = d.getTotalUnackedMessages();
                subStats.blockedSubscriptionOnUnackedMsgs = d.isBlockedDispatcherOnUnackedMsgs();
```

### PatternVariableCanBeUsed
Variable 'keySharedDispatcher' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        subStats.isDurable = cursor.isDurable();
        if (getType() == SubType.Key_Shared && dispatcher instanceof PersistentStickyKeyDispatcherMultipleConsumers) {
            PersistentStickyKeyDispatcherMultipleConsumers keySharedDispatcher =
                    (PersistentStickyKeyDispatcherMultipleConsumers) dispatcher;

```

### PatternVariableCanBeUsed
Variable 'casted' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/LoadManager.java`
#### Snippet
```java
                    Thread.currentThread().getContextClassLoader());
            if (loadManagerInstance instanceof LoadManager) {
                final LoadManager casted = (LoadManager) loadManagerInstance;
                casted.initialize(pulsar);
                return casted;
```

### PatternVariableCanBeUsed
Variable 'other' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleResourceUnit.java`
#### Snippet
```java
            return false;
        }
        SimpleResourceUnit other = (SimpleResourceUnit) o;
        return this.resourceId.equals(other.resourceId);
    }
```

### PatternVariableCanBeUsed
Variable 'dispatcher' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                if (Subscription.isIndividualAckMode(subscription.getType())) {
                    if (subscription.getDispatcher() instanceof PersistentDispatcherMultipleConsumers) {
                        PersistentDispatcherMultipleConsumers dispatcher =
                                (PersistentDispatcherMultipleConsumers) subscription.getDispatcher();
                        topicStatsStream.writePair("blockedSubscriptionOnUnackedMsgs",
```

### PatternVariableCanBeUsed
Variable 'dispatcher' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
            if (sub != null) {
                if (sub.getDispatcher() instanceof PersistentDispatcherMultipleConsumers) {
                    PersistentDispatcherMultipleConsumers dispatcher = (PersistentDispatcherMultipleConsumers) sub
                            .getDispatcher();
                    cs.subscriptionHavePendingRead = dispatcher.havePendingRead;
```

### PatternVariableCanBeUsed
Variable 'dispatcher' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
                    cs.subscriptionHavePendingReplayRead = dispatcher.havePendingReplayRead;
                } else if (sub.getDispatcher() instanceof PersistentDispatcherSingleActiveConsumer) {
                    PersistentDispatcherSingleActiveConsumer dispatcher = (PersistentDispatcherSingleActiveConsumer) sub
                            .getDispatcher();
                    cs.subscriptionHavePendingRead = dispatcher.havePendingRead;
```

### PatternVariableCanBeUsed
Variable 'name' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
    public CompletableFuture<Optional<URL>> getWebServiceUrlAsync(ServiceUnitId suName, LookupOptions options) {
        if (suName instanceof TopicName) {
            TopicName name = (TopicName) suName;
            if (LOG.isDebugEnabled()) {
                LOG.debug("Getting web service URL of topic: {} - options: {}", name, options);
```

### PatternVariableCanBeUsed
Variable 'obj' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundle.java`
#### Snippet
```java
    public boolean equals(Object other) {
        if (other instanceof NamespaceBundle) {
            NamespaceBundle obj = (NamespaceBundle) other;
            return Objects.equals(this.nsname, obj.nsname)
                    && (Objects.equals(this.keyRange.lowerEndpoint(), obj.keyRange.lowerEndpoint())
```

### PatternVariableCanBeUsed
Variable 'topic1' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                                topic.ifPresent(t -> {
                                    if (t instanceof PersistentTopic) {
                                        PersistentTopic topic1 = (PersistentTopic) t;
                                        topic1.onLocalPoliciesUpdate();
                                    }
```

### PatternVariableCanBeUsed
Variable 'dispatcher' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                topic.getSubscriptions().forEach((subName, persistentSubscription) -> {
                    if (persistentSubscription.getDispatcher() instanceof PersistentDispatcherMultipleConsumers) {
                        PersistentDispatcherMultipleConsumers dispatcher =
                                (PersistentDispatcherMultipleConsumers) persistentSubscription
                                        .getDispatcher();
```

### PatternVariableCanBeUsed
Variable 'persistentTopic' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                try {
                    if (topic instanceof PersistentTopic) {
                        PersistentTopic persistentTopic = (PersistentTopic) topic;
                        // update skipNonRecoverableLedger configuration
                        persistentTopic.getManagedLedger().getConfig().setAutoSkipNonRecoverableData(
```

### PatternVariableCanBeUsed
Variable 'svc' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java
                LifecycleComponent subcomp = stack.getComponent(numComp);
                if (subcomp instanceof BookieService) {
                    BookieService svc = (BookieService) subcomp;
                    bookies[i] = svc.getServer();
                    break;
```

### PatternVariableCanBeUsed
Variable 'other' can be replaced with pattern variable
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java
    public boolean equals(Object obj) {
        if (obj != null && obj instanceof NamespaceBundles) {
            NamespaceBundles other = (NamespaceBundles) obj;
            return (Objects.equals(this.nsname, other.nsname) && Objects.equals(this.bundles, other.bundles));
        }
```

### PatternVariableCanBeUsed
Variable 'other' can be replaced with pattern variable
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/PositionImpl.java`
#### Snippet
```java
    public boolean equals(Object obj) {
        if (obj instanceof PositionImpl) {
            PositionImpl other = (PositionImpl) obj;
            return ledgerId == other.ledgerId && entryId == other.entryId;
        }
```

### PatternVariableCanBeUsed
Variable 'er' can be replaced with pattern variable
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
    private void handleDeleteResult(OpDelete op, OpResult opr) {
        if (opr instanceof OpResult.ErrorResult) {
            OpResult.ErrorResult er = (OpResult.ErrorResult) opr;
            Code code = Code.get(er.getErr());
            if (code == Code.RUNTIMEINCONSISTENCY || code == Code.OK) {
```

### PatternVariableCanBeUsed
Variable 'er' can be replaced with pattern variable
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
    private void handlePutResult(OpPut op, OpResult opr) {
        if (opr instanceof OpResult.ErrorResult) {
            OpResult.ErrorResult er = (OpResult.ErrorResult) opr;
            Code code = Code.get(er.getErr());
            if (code == Code.NONODE) {
```

### PatternVariableCanBeUsed
Variable 'cr' can be replaced with pattern variable
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
            }
        } else if (opr instanceof OpResult.CreateResult) {
            OpResult.CreateResult cr = (OpResult.CreateResult) opr;
            op.getFuture().complete(new Stat(cr.getPath(), 0, 0, 0, op.isEphemeral(), true));
        } else {
```

### PatternVariableCanBeUsed
Variable 'er' can be replaced with pattern variable
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
    private void handleGetChildrenResult(OpGetChildren op, OpResult opr) {
        if (opr instanceof OpResult.ErrorResult) {
            OpResult.ErrorResult er = (OpResult.ErrorResult) opr;
            Code code = Code.get(er.getErr());
            if (code == Code.NONODE) {
```

### PatternVariableCanBeUsed
Variable 'er' can be replaced with pattern variable
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
    private void handleGetResult(OpGet op, OpResult opr) {
        if (opr instanceof OpResult.ErrorResult) {
            OpResult.ErrorResult er = (OpResult.ErrorResult) opr;
            Code code = Code.get(er.getErr());
            if (code == Code.NONODE) {
```

### PatternVariableCanBeUsed
Variable 'zv' can be replaced with pattern variable
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
            return FutureUtil.failedFuture(new BKException.BKMetadataVersionException());
        }
        final LongVersion zv = (LongVersion) currentVersion;

        final byte[] data;
```

### PatternVariableCanBeUsed
Variable 'ph' can be replaced with pattern variable
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/WorkerServiceLoader.java`
#### Snippet
```java
                    + " does not implement worker service interface");
            }
            WorkerService ph = (WorkerService) handler;
            return new WorkerServiceWithClassLoader(ph, ncl);
        } catch (Throwable t) {
```

### PatternVariableCanBeUsed
Variable 'impl' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/PulsarSource.java`
#### Snippet
```java
        Schema<T> schema = null;
        if (message instanceof MessageImpl) {
            MessageImpl impl = (MessageImpl) message;
            schema = impl.getSchemaInternal();
        } else if (message instanceof TopicMessageImpl) {
```

### PatternVariableCanBeUsed
Variable 'impl' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/PulsarSource.java`
#### Snippet
```java
            schema = impl.getSchemaInternal();
        } else if (message instanceof TopicMessageImpl) {
            TopicMessageImpl impl = (TopicMessageImpl) message;
            schema = impl.getSchemaInternal();
        }
```

### PatternVariableCanBeUsed
Variable 'autoConsumeSchema' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/PulsarSource.java`
#### Snippet
```java
        // we don't want the Function/Sink to see AutoConsumeSchema
        if (schema instanceof AutoConsumeSchema) {
            AutoConsumeSchema autoConsumeSchema = (AutoConsumeSchema) schema;
            // we cannot use atSchemaVersion, because atSchemaVersion is only
            // able to decode data, here we want a Schema that
```

### PatternVariableCanBeUsed
Variable 'pulsarRecord' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/AbstractSinkRecord.java`
#### Snippet
```java
    public void cumulativeAck() {
        if (sourceRecord instanceof PulsarRecord) {
            PulsarRecord pulsarRecord = (PulsarRecord) sourceRecord;
            pulsarRecord.cumulativeAck();
        } else {
```

### PatternVariableCanBeUsed
Variable 'kvRecord' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/AbstractSinkRecord.java`
#### Snippet
```java

        if (record instanceof KVRecord) {
            KVRecord kvRecord = (KVRecord) record;
            return KeyValueSchemaImpl.of(kvRecord.getKeySchema(), kvRecord.getValueSchema(),
                    kvRecord.getKeyValueEncodingType());
```

### PatternVariableCanBeUsed
Variable 'pulsarRecord' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/AbstractSinkRecord.java`
#### Snippet
```java
    public void individualAck() {
        if (sourceRecord instanceof PulsarRecord) {
            PulsarRecord pulsarRecord = (PulsarRecord) sourceRecord;
            pulsarRecord.individualAck();
        } else {
```

### PatternVariableCanBeUsed
Variable 'manifestCustomizer' can be replaced with pattern variable
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntimeFactory.java`
#### Snippet
```java
                        + " must implement KubernetesManifestCustomizer");
            } else {
                KubernetesManifestCustomizer manifestCustomizer =
                        (KubernetesManifestCustomizer) runtimeCustomizer.get();
                this.manifestCustomizer = Optional.of(manifestCustomizer);
```

### PatternVariableCanBeUsed
Variable 'kubernetesFunctionAuthProvider' can be replaced with pattern variable
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntimeFactory.java`
#### Snippet
```java
                        + " must implement KubernetesFunctionAuthProvider");
            } else {
                KubernetesFunctionAuthProvider kubernetesFunctionAuthProvider =
                        (KubernetesFunctionAuthProvider) functionAuthProvider.get();
                kubernetesFunctionAuthProvider.initialize(coreClient, serverCaBytes,
```

### PatternVariableCanBeUsed
Variable 'record' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
        AbstractSinkRecord<?> sinkRecord;
        if (output instanceof Record) {
            Record record = (Record) output;
            if (sinkSchemaInfoProvider != null) {
                // Function and Sink coupled together so we need to encode with the Function Schema
```

### PatternVariableCanBeUsed
Variable 'kvSchema' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
        boolean isKeyValueSeparated = false;
        if (encodingSchema instanceof KeyValueSchema) {
            KeyValueSchema<?, ?> kvSchema = (KeyValueSchema<?, ?>) encodingSchema;
            // If the encoding is SEPARATED, it's easier to encode/decode with INLINE
            // and rebuild the SEPARATED KeyValueSchema after decoding
```

### PatternVariableCanBeUsed
Variable 'kvSchema' can be replaced with pattern variable
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
        final Schema<?> finalSchema;
        if (isKeyValueSeparated && schema instanceof KeyValueSchema) {
            KeyValueSchema<?, ?> kvSchema = (KeyValueSchema<?, ?>) schema;
            finalSchema = KeyValueSchemaImpl.of(kvSchema.getKeySchema(), kvSchema.getValueSchema(),
                KeyValueEncodingType.SEPARATED);
```

### PatternVariableCanBeUsed
Variable 'loadReport' can be replaced with pattern variable
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/BrokerMonitor.java`
#### Snippet
```java
                double maxUsage;
                if (data instanceof LoadReport) {
                    final LoadReport loadReport = (LoadReport) data;
                    numBundles = loadReport.getNumBundles();
                    messageRate = loadReport.getMsgRateIn() + loadReport.getMsgRateOut();
```

### PatternVariableCanBeUsed
Variable 'localData' can be replaced with pattern variable
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/BrokerMonitor.java`
#### Snippet
```java
                            systemResourceUsage.getBandwidthOut().percentUsage());
                } else if (data instanceof LocalBrokerData) {
                    final LocalBrokerData localData = (LocalBrokerData) data;
                    numBundles = localData.getNumBundles();
                    messageRate = localData.getMsgRateIn() + localData.getMsgRateOut();
```

### PatternVariableCanBeUsed
Variable 'msgId' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                }
                if (msg.getMessageId() instanceof BatchMessageIdImpl) {
                    BatchMessageIdImpl msgId = (BatchMessageIdImpl) msg.getMessageId();
                    System.out.println("Batch Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId() + ":"
                            + msgId.getBatchIndex());
```

### PatternVariableCanBeUsed
Variable 'unrecognizedPropertyException' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/CmdUtils.java`
#### Snippet
```java
        } catch (Exception ex) {
            if (ex instanceof UnrecognizedPropertyException) {
                UnrecognizedPropertyException unrecognizedPropertyException = (UnrecognizedPropertyException) ex;

                String exceptionMessage = String.format("Failed to parse config file %s. "
```

### PatternVariableCanBeUsed
Variable 'invalidFormatException' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/utils/CmdUtils.java`
#### Snippet
```java
            } else if (ex instanceof InvalidFormatException) {

                InvalidFormatException invalidFormatException = (InvalidFormatException) ex;
                String exceptionMessage = String.format("Failed to parse config file %s. %s on line: %d column: %d",
                        file,
```

### PatternVariableCanBeUsed
Variable 'cmdBase' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/JCommanderCompleter.java`
#### Snippet
```java

        if (command.getObjects().get(0) instanceof CmdBase) {
            CmdBase cmdBase = (CmdBase) command.getObjects().get(0);
            subCommands = cmdBase.getJcommander().getCommands();
            mainParameterValue = cmdBase.getJcommander().getMainParameter() == null ? null :
```

### PatternVariableCanBeUsed
Variable 'msgData' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java
            data = "null";
        } else if (value instanceof byte[]) {
            byte[] msgData = (byte[]) value;
            data = interpretByteArray(displayHex, msgData);
        } else if (value instanceof GenericObject) {
```

### PatternVariableCanBeUsed
Variable 'servlet' can be replaced with pattern variable
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServletUtils.java`
#### Snippet
```java
                        + " does not implement additional servlet interface");
            }
            AdditionalServlet servlet = (AdditionalServlet) additionalServlet;
            return new AdditionalServletWithClassLoader(servlet, ncl);
        } catch (Throwable t) {
```

### PatternVariableCanBeUsed
Variable 'cmdUsageFormatter' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        IUsageFormatter usageFormatter = jcommander.getUsageFormatter();
        if (usageFormatter instanceof CmdUsageFormatter) {
            CmdUsageFormatter cmdUsageFormatter = (CmdUsageFormatter) usageFormatter;
            cmdUsageFormatter.addDeprecatedCommand("enable-deduplication");
            cmdUsageFormatter.addDeprecatedCommand("disable-deduplication");
```

### PatternVariableCanBeUsed
Variable 'msgId' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
            } else {
                if (message.getMessageId() instanceof BatchMessageIdImpl) {
                    BatchMessageIdImpl msgId = (BatchMessageIdImpl) message.getMessageId();
                    System.out.println("Batch Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId() + ":"
                            + msgId.getBatchIndex());
```

### PatternVariableCanBeUsed
Variable 'msgId' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }
                if (message.getMessageId() instanceof BatchMessageIdImpl) {
                    BatchMessageIdImpl msgId = (BatchMessageIdImpl) message.getMessageId();
                    System.out.println("Batch Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId() + ":"
                            + msgId.getBatchIndex());
```

### PatternVariableCanBeUsed
Variable 'msgId' can be replaced with pattern variable
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

            if (message.getMessageId() instanceof BatchMessageIdImpl) {
                BatchMessageIdImpl msgId = (BatchMessageIdImpl) message.getMessageId();
                System.out.println("Batch Message ID: " + msgId.getLedgerId() + ":" + msgId.getEntryId() + ":"
                        + msgId.getBatchIndex());
```

## RuleId[ruleID=RedundantCollectionOperation]
### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/CmdGenerateDocs.java`
#### Snippet
```java
                    continue;
                }
                if (!jcommander.getCommands().keySet().contains(commandName)) {
                    continue;
                }
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            LAST_MARK_DELETE_ENTRY_UPDATER.updateAndGet(this, last -> {
                Map<String, Long> properties = last.properties;
                if (properties != null && properties.containsKey(key)) {
                    properties.remove(key);
                }
```

## RuleId[ruleID=AbstractMethodCallInConstructor]
### AbstractMethodCallInConstructor
Call to 'abstract' method `getProducerName()` during object construction
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractReplicator.java`
#### Snippet
```java
                .sendTimeout(0, TimeUnit.SECONDS) //
                .maxPendingMessages(producerQueueSize) //
                .producerName(getProducerName());
        STATE_UPDATER.set(this, State.Stopped);
    }
```

## RuleId[ruleID=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'key' in a Serializable class
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/tcp/NettyTCPServerHandler.java`
#### Snippet
```java
    @Data
    private static class NettyTCPRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
    }
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'key' in a Serializable class
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/udp/NettyUDPServerHandler.java`
#### Snippet
```java
    @Data
    private static class NettyUDPRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
    }
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'key' in a Serializable class
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/http/NettyHttpServerHandler.java`
#### Snippet
```java
    @Data
    private static class NettyHttpRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
    }
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'config' in a Serializable class
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/AdminProxyHandler.java`
#### Snippet
```java
    ));

    private final ProxyConfiguration config;
    private final BrokerDiscoveryProvider discoveryProvider;
    private final String brokerWebServiceUrl;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'discoveryProvider' in a Serializable class
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/AdminProxyHandler.java`
#### Snippet
```java

    private final ProxyConfiguration config;
    private final BrokerDiscoveryProvider discoveryProvider;
    private final String brokerWebServiceUrl;
    private final String functionWorkerWebServiceUrl;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'pulsar' in a Serializable class
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/PulsarPrometheusMetricsServlet.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private final PulsarService pulsar;
    private final boolean shouldExportTopicMetrics;
    private final boolean shouldExportConsumerMetrics;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'pulsarClient' in a Serializable class
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerStatsRecorderImpl.java`
#### Snippet
```java
    private Timeout statTimeout;
    private final Consumer<?> consumer;
    private PulsarClientImpl pulsarClient;
    private long oldTime;
    private long statsIntervalSeconds;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'error' in a Serializable class
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/protocol/TokenExchangeException.java`
#### Snippet
```java
 */
public class TokenExchangeException extends Exception {
    private TokenError error;

    public TokenExchangeException(TokenError error) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'membershipManager' in a Serializable class
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java
    private final FunctionRuntimeManager functionRuntimeManager;
    private final FunctionMetaDataManager functionMetaDataManager;
    private final MembershipManager membershipManager;
    private ConsumerImpl<byte[]> consumer;
    private final WorkerConfig workerConfig;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'functionRuntimeManager' in a Serializable class
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java
    private final ErrorNotifier errorNotifier;
    private final SchedulerManager schedulerManager;
    private final FunctionRuntimeManager functionRuntimeManager;
    private final FunctionMetaDataManager functionMetaDataManager;
    private final MembershipManager membershipManager;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'schedulerManager' in a Serializable class
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java
    private final FunctionAssignmentTailer functionAssignmentTailer;
    private final ErrorNotifier errorNotifier;
    private final SchedulerManager schedulerManager;
    private final FunctionRuntimeManager functionRuntimeManager;
    private final FunctionMetaDataManager functionMetaDataManager;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'consumer' in a Serializable class
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java
    private final FunctionMetaDataManager functionMetaDataManager;
    private final MembershipManager membershipManager;
    private ConsumerImpl<byte[]> consumer;
    private final WorkerConfig workerConfig;
    private final PulsarClient pulsarClient;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'functionAssignmentTailer' in a Serializable class
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java

    private final String consumerName;
    private final FunctionAssignmentTailer functionAssignmentTailer;
    private final ErrorNotifier errorNotifier;
    private final SchedulerManager schedulerManager;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'functionMetaDataManager' in a Serializable class
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/LeaderService.java`
#### Snippet
```java
    private final SchedulerManager schedulerManager;
    private final FunctionRuntimeManager functionRuntimeManager;
    private final FunctionMetaDataManager functionMetaDataManager;
    private final MembershipManager membershipManager;
    private ConsumerImpl<byte[]> consumer;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'pulsarSourceConfig' in a Serializable class
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/MultiConsumerPulsarSource.java`
#### Snippet
```java
public class MultiConsumerPulsarSource<T> extends PushPulsarSource<T> implements MessageListener<T> {

    private final MultiConsumerPulsarSourceConfig pulsarSourceConfig;
    private final ClassLoader functionClassLoader;
    private final List<Consumer<T>> inputConsumers = new LinkedList<>();
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'kubernetesContainerFactory' in a Serializable class
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @Deprecated
    private KubernetesContainerFactory kubernetesContainerFactory;

    @FieldContext(
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'processContainerFactory' in a Serializable class
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @Deprecated
    private ProcessContainerFactory processContainerFactory;

    @Deprecated
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'threadContainerFactory' in a Serializable class
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    )
    @Deprecated
    private ThreadContainerFactory threadContainerFactory;

    @Deprecated
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'functionInstanceMaxResources' in a Serializable class
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                    + "Support for this depends on function runtime."
    )
    private Resources functionInstanceMaxResources;
    @FieldContext(
            category = CATEGORY_FUNC_RUNTIME_MNG,
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'functionInstanceMinResources' in a Serializable class
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                    + "Support for this depends on function runtime."
    )
    private Resources functionInstanceMinResources;
    @FieldContext(
            category = CATEGORY_FUNC_RUNTIME_MNG,
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'functionInstanceResourceGranularities' in a Serializable class
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
                    + " the requested resource of the type must be a multiple of the granularity."
    )
    private Resources functionInstanceResourceGranularities;
    @FieldContext(
            category = CATEGORY_FUNC_RUNTIME_MNG,
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'pulsarSaslClient' in a Serializable class
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/SaslAuthenticationDataProvider.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private PulsarSaslClient pulsarSaslClient;

    public SaslAuthenticationDataProvider(PulsarSaslClient pulsarSaslClient) {
```

## RuleId[ruleID=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/extensions/ProxyExtension.java`
#### Snippet
```java
     * <p>NOTE: this method is called after {@link #start(ProxyService)}.
     *
     * @return the list of channel initializers for the ports that this extension listens on.
     */
    Map<InetSocketAddress, ChannelInitializer<SocketChannel>> newChannelInitializers();
```

### MismatchedJavadocCode
Method is specified to return 'true' but there's no such enum constant in MessageDupStatus
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/MessageDeduplication.java`
#### Snippet
```java
     * Assess whether the message was already stored in the topic.
     *
     * @return true if the message should be published or false if it was recognized as a duplicate
     */
    public MessageDupStatus isDuplicate(PublishContext publishContext, ByteBuf headersAndPayload) {
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/protocol/ProtocolHandler.java`
#### Snippet
```java
     * <p>NOTE: this method is called after {@link #start(BrokerService)}.
     *
     * @return the list of channel initializers for the ports that this protocol handler listens on.
     */
    Map<InetSocketAddress, ChannelInitializer<SocketChannel>> newChannelInitializers();
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/SystemTopicClient.java`
#### Snippet
```java
        /**
         * Check has more events available for the reader asynchronously.
         * @return true if has remaining events, otherwise false
         */
        CompletableFuture<Boolean> hasMoreEventsAsync();
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/TransactionTimeoutTracker.java`
#### Snippet
```java
     *            the absolute timestamp for transaction timeout
     *
     * @return true if the transaction was added to the tracker or false if had timed out
     */
    CompletableFuture<Boolean> addTransaction(long sequenceId, long timeout);
```

### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `pulsar-broker-common/src/main/java/org/apache/pulsar/common/configuration/PropertiesContext.java`
#### Snippet
```java
     * Return list of properties.
     *
     * @return list of fields
     */
    PropertyContext[] properties();
```

## RuleId[ruleID=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/CanalAbstractSource.java`
#### Snippet
```java
                        try {
                            Thread.sleep(1000);
                        } catch (InterruptedException e) {
                        }
                    } else {
```

### CatchMayIgnoreException
Empty `catch` block
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java
                try {
                    Thread.sleep(delayMillis);
                } catch (InterruptedException e) {
                }

```

### CatchMayIgnoreException
Empty `catch` block
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockLedgerHandle.java`
#### Snippet
```java
                try {
                    Thread.sleep(1);
                } catch (InterruptedException e) {
                }

```

### CatchMayIgnoreException
Empty `catch` block
in `testmocks/src/main/java/org/apache/bookkeeper/client/PulsarMockBookKeeper.java`
#### Snippet
```java
        try {
            super.close();
        } catch (Exception e) {
        }
        synchronized (this) {
```

### CatchMayIgnoreException
Empty `catch` block
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/tools/LoadReportCommand.java`
#### Snippet
```java
            try {
                TimeUnit.MILLISECONDS.sleep(flags.intervalMilliseconds);
            } catch (InterruptedException e) {
            }
            hostUsage.calculateBrokerHostUsage();
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
            try {
                rwBookie.release().get();
            } catch (ExecutionException ignore) {
                log.error("Cannot release correctly {}", rwBookie, ignore.getCause());
            } catch (InterruptedException ignore) {
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
            } catch (ExecutionException ignore) {
                log.error("Cannot release correctly {}", rwBookie, ignore.getCause());
            } catch (InterruptedException ignore) {
                log.error("Cannot release correctly {}", rwBookie, ignore);
                Thread.currentThread().interrupt();
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
            try {
                roBookie.release().get();
            } catch (ExecutionException ignore) {
                log.error("Cannot release correctly {}", roBookie, ignore.getCause());
            } catch (InterruptedException ignore) {
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarRegistrationManager.java`
#### Snippet
```java
            } catch (ExecutionException ignore) {
                log.error("Cannot release correctly {}", roBookie, ignore.getCause());
            } catch (InterruptedException ignore) {
                log.error("Cannot release correctly {}", roBookie, ignore);
                Thread.currentThread().interrupt();
```

### CatchMayIgnoreException
Empty `catch` block
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
                              .collect(Collectors.toList());
                        }
                    } catch (PulsarAdminException e1) {

                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
                    try {
                        stats = pulsarAdmin.topics().getStats(topic);
                    } catch (PulsarAdminException e1) {

                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
                        return true;
                    }
                } catch (PulsarAdminException.NotFoundException | InterruptedException | ExecutionException e) {

                }
```

### CatchMayIgnoreException
Empty `catch` block
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/MultiConsumerPulsarSource.java`
#### Snippet
```java
                try {
                    consumer.close();
                } catch (PulsarClientException e) {
                }
            });
```

## RuleId[ruleID=EnhancedSwitchMigration]
### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/JsonConverter.java`
#### Snippet
```java
            return jsonNodeFactory.nullNode();
        }
        switch(schema.getType()) {
            case NULL: // this should not happen
                return jsonNodeFactory.nullNode();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
            return;
        }
        switch (schema.getSchemaInfo().getType()) {
            case JSON:
                final JsonNode jsonNode = (JsonNode) record.getNativeObject();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
            return null;
        }
        switch (schema.getType()) {
            case NULL:
            case INT:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
            boolean isDelete = false;
            if (value == null) {
                switch (jdbcSinkConfig.getNullValueAction()) {
                    case DELETE:
                        isDelete = true;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
            mutationType = MutationType.valueOf(action);
        } else if (mutationType == null) {
            switch (jdbcSinkConfig.getInsertMode()) {
                case INSERT:
                    mutationType = MutationType.INSERT;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/BaseJdbcAutoSchemaSink.java`
#### Snippet
```java
    public void bindValue(PreparedStatement statement, Mutation mutation) throws Exception {
        final List<ColumnId> columns = new ArrayList<>();
        switch (mutation.getType()) {
            case INSERT:
                columns.addAll(tableDefinition.getColumns());
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/HttpSink.java`
#### Snippet
```java
            return val;
        }
        switch (schema.getSchemaInfo().getType()) {
            case KEY_VALUE:
                KeyValueSchema<?, ?> keyValueSchema = (KeyValueSchema<?, ?>) schema;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java
                PreparedStatement statement;
                for (Mutation mutation : mutations) {
                    switch (mutation.getType()) {
                        case DELETE:
                            statement = deleteStatement;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/server/NettyServer.java`
#### Snippet
```java
    public void run() {
        try {
            switch (type) {
                case UDP:
                    runUdp();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/json/JsonConverter.java`
#### Snippet
```java
            return jsonNodeFactory.nullNode();
        }
        switch(schema.getType()) {
            case NULL: // this should not happen
                return jsonNodeFactory.nullNode();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
            return val;
        }
        switch (schema.getSchemaInfo().getType()) {
            case KEY_VALUE:
                KeyValueSchema<GenericObject, GenericObject> keyValueSchema = (KeyValueSchema) schema;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
        int batchSize = ctx.getBatchSize().isPresent() ? ctx.getBatchSize().get() : 1;
        byte compressionType;
        switch (ctx.getCompressionType()) {
            case LZ4:
                compressionType = org.apache.pulsar.io.kinesis.fbs.CompressionType.LZ4;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/KinesisSink.java`
#### Snippet
```java
    public ByteBuffer createKinesisMessage(MessageFormat msgFormat, Record<GenericObject> record)
            throws JsonProcessingException {
        switch (msgFormat) {
            case FULL_MESSAGE_IN_JSON:
                return ByteBuffer.wrap(Utils.serializeRecordToJson(record).getBytes(StandardCharsets.UTF_8));
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/JsonConverter.java`
#### Snippet
```java
            return jsonNodeFactory.nullNode();
        }
        switch(schema.getType()) {
            case NULL: // this should not happen
                return jsonNodeFactory.nullNode();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
                    && idHashingAlgorithm != ElasticSearchConfig.IdHashingAlgorithm.NONE) {
                Hasher hasher;
                switch (idHashingAlgorithm) {
                    case SHA256:
                        hasher = Hashing.sha256().newHasher();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java
            return null;
        }
        switch (schema.getSchemaInfo().getType()) {
            case JSON:
                return (JsonNode) ((GenericRecord) val).getNativeObject();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchSink.java`
#### Snippet
```java

    public String stringifyKey(Schema<?> schema, Object val) throws JsonProcessingException {
        switch (schema.getSchemaInfo().getType()) {
            case INT8:
                return Byte.toString((Byte) val);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
            // https://github.com/apache/kafka/blob/trunk/connect/api/src/main/java/org/apache/kafka/connect/data/ConnectSchema.java#L47-L71
            Number num = (Number) nativeObject;
            switch (kafkaSchema.type()) {
                case INT8:
                    if (!(nativeObject instanceof Byte)) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
        }

        switch (kafkaSchema.type()) {
            case ARRAY:
                 if (nativeObject instanceof List) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
        }

        switch (kafkaSchema.type()) {
            case INT8:
                Preconditions.checkArgument(jsonNode.isNumber());
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroRowDecoderFactory.java`
#### Snippet
```java
        Schema.Type type = schema.getType();
        LogicalType logicalType  = schema.getLogicalType();
        switch (type) {
            case STRING:
            case ENUM:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonRowDecoderFactory.java`
#### Snippet
```java
        Schema.Type type = schema.getType();
        LogicalType logicalType  = schema.getLogicalType();
        switch (type) {
            case STRING:
            case ENUM:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/primitive/PulsarPrimitiveRowDecoderFactory.java`
#### Snippet
```java

    private Type parsePrimitivePrestoType(String fieldName, SchemaType pulsarType) {
        switch (pulsarType) {
            case BOOLEAN:
                return BooleanType.BOOLEAN;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeRowDecoderFactory.java`
#### Snippet
```java
        Descriptors.FieldDescriptor.JavaType type = field.getJavaType();
        Type dataType;
        switch (type) {
            case BOOLEAN:
                dataType = BooleanType.BOOLEAN;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ParserProxyHandler.java`
#### Snippet
```java
        }
        // log conn format is like from source to target
        switch (this.connType) {
            case ParserProxyHandler.FRONTEND_CONN:
                log.info(ParserProxyHandler.FRONTEND_CONN + ":{} cmd:{} msg:{}", "[" + conn.remoteAddress()
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/DirectProxyHandler.java`
#### Snippet
```java
        @Override
        public void channelRead(final ChannelHandlerContext ctx, Object msg) throws Exception {
            switch (state) {
            case Init:
                if (log.isDebugEnabled()) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/ProxyConnection.java`
#### Snippet
```java
            return;
        }
        switch (state) {
        case Init:
        case Connecting:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
                    long lowWaterMark = getLowWaterMark(txnID);
                    Stream<CompletableFuture<?>> onSubFutureStream = txnMeta.ackedPartitions().stream().map(tbSub -> {
                        switch (txnAction) {
                            case TxnAction.COMMIT_VALUE:
                                return tbClient.commitTxnOnSubscription(
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
                    Stream<CompletableFuture<?>> onTopicFutureStream =
                            txnMeta.producedPartitions().stream().map(partition -> {
                                switch (txnAction) {
                                    case TxnAction.COMMIT_VALUE:
                                        return tbClient.commitTxnOnTopic(partition, txnID.getMostSigBits(),
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
                                                  CompletableFuture<Void> future) {
        TxnStatus newStatus;
        switch (txnAction) {
            case TxnAction.COMMIT_VALUE:
                newStatus = COMMITTING;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
                                                          TxnID txnID, TxnStatus expectStatus) {
        boolean isLegal;
        switch (txnStatus) {
            case COMMITTING:
                isLegal =  (txnAction == TxnAction.COMMIT.getValue());
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/rest/TopicsBase.java`
#### Snippet
```java
    private byte[] encodeWithSchema(String input, Schema schema) {
        try {
            switch (schema.getSchemaInfo().getType()) {
                case INT8:
                    return schema.encode(Byte.parseByte(input));
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BacklogQuotaManager.java`
#### Snippet
```java
        log.info("Backlog quota type {} exceeded for topic [{}]. Applying [{}] policy", backlogQuotaType,
                persistentTopic.getName(), quota.getPolicy());
        switch (quota.getPolicy()) {
        case consumer_backlog_eviction:
            switch (backlogQuotaType) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BacklogQuotaManager.java`
#### Snippet
```java
        switch (quota.getPolicy()) {
        case consumer_backlog_eviction:
            switch (backlogQuotaType) {
                case destination_storage:
                        dropBacklogForSizeLimit(persistentTopic, quota);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/AvroSchemaBasedCompatibilityCheck.java`
#### Snippet
```java
    static SchemaValidator createSchemaValidator(SchemaCompatibilityStrategy compatibilityStrategy) {
        final SchemaValidatorBuilder validatorBuilder = new SchemaValidatorBuilder();
        switch (compatibilityStrategy) {
            case BACKWARD_TRANSITIVE:
                return createLatestOrAllValidator(validatorBuilder.canReadStrategy(), false);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
        lock.writeLock().lock();
        try {
            switch (producer.getAccessMode()) {
            case Shared:
                if (hasExclusiveProducer || !waitingExclusiveProducers.isEmpty()) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/ProtobufNativeSchemaCompatibilityCheck.java`
#### Snippet
```java
        Descriptor fromDescriptor = ProtobufNativeSchemaUtils.deserialize(from.getData());
        Descriptor toDescriptor = ProtobufNativeSchemaUtils.deserialize(to.getData());
        switch (strategy) {
            case BACKWARD_TRANSITIVE:
            case BACKWARD:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/DispatchRateLimiter.java`
#### Snippet
```java
    public void updateDispatchRate() {
        DispatchRate dispatchRate;
        switch (type) {
            case TOPIC:
                dispatchRate = topic.getDispatchRate();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/DispatchRateLimiter.java`
#### Snippet
```java
        ServiceConfiguration config = brokerService.pulsar().getConfiguration();

        switch (type) {
            case TOPIC:
                dispatchThrottlingRateInMsg = config.getDispatchThrottlingRatePerTopicInMsg();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
        this.nextStuckConsumers = new HashSet<>();
        this.keySharedMode = ksm.getKeySharedMode();
        switch (this.keySharedMode) {
        case AUTO_SPLIT:
            if (conf.isSubscriptionKeySharedUseConsistentHashing()) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java

    public static boolean isUsingAvroSchemaParser(SchemaType type) {
        switch (type) {
            case AVRO:
            case JSON:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/SchemaRegistryServiceImpl.java`
#### Snippet
```java
    public CompletableFuture<Void> checkCompatible(String schemaId, SchemaData schema,
                                                                    SchemaCompatibilityStrategy strategy) {
        switch (strategy) {
            case FORWARD_TRANSITIVE:
            case BACKWARD_TRANSITIVE:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/ReplicatedSubscriptionsController.java`
#### Snippet
```java

        try {
            switch (markerType) {
            case MarkerType.REPLICATED_SUBSCRIPTION_SNAPSHOT_REQUEST_VALUE:
                receivedSnapshotRequest(Markers.parseReplicatedSubscriptionsSnapshotRequest(payload));
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentReplicator.java`
#### Snippet
```java
        }

        switch (markerType) {
        case MarkerType.REPLICATED_SUBSCRIPTION_SNAPSHOT_REQUEST_VALUE:
        case MarkerType.REPLICATED_SUBSCRIPTION_SNAPSHOT_RESPONSE_VALUE:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentStickyKeyDispatcherMultipleConsumers.java`
#### Snippet
```java
        super(topic, subscription);
        this.keySharedMode = ksm.getKeySharedMode();
        switch (this.keySharedMode) {
            case STICKY:
                this.selector = new HashRangeExclusiveStickyKeyConsumerSelector();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java
        }

        switch (type) {
        case Exclusive:
            return "Exclusive";
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/nonpersistent/NonPersistentSubscription.java`
#### Snippet
```java
            Dispatcher previousDispatcher = null;

            switch (consumer.subType()) {
            case Exclusive:
                if (dispatcher == null || dispatcher.getType() != SubType.Exclusive) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
        }

        switch (type) {
            case Exclusive:
                return "Exclusive";
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                    boolean useStreamingDispatcher = topic.getBrokerService().getPulsar()
                            .getConfiguration().isStreamingDispatch();
                    switch (consumer.subType()) {
                        case Exclusive:
                            if (dispatcher == null || dispatcher.getType() != SubType.Exclusive) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentSubscription.java`
#### Snippet
```java
                filterResult = EntryFilter.FilterResult.ACCEPT;
            }
            switch (filterResult) {
                case REJECT:
                    rejected.incrementAndGet();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
    protected CompletableFuture<List<String>> internalGetListOfTopics(Policies policies,
                                                                      CommandGetTopicsOfNamespace.Mode mode) {
        switch (mode) {
            case ALL:
                return pulsar().getNamespaceService().getListOfPersistentTopics(namespaceName)
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/recover/TransactionRecoverTrackerImpl.java`
#### Snippet
```java
    @Override
    public void updateTransactionStatus(long sequenceId, TxnStatus txnStatus) throws InvalidTxnStatusException {
        switch (txnStatus) {
            case COMMITTING:
                openTransactions.remove(sequenceId);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        MessageDeduplication.MessageDupStatus status =
                messageDeduplication.isDuplicate(publishContext, headersAndPayload);
        switch (status) {
            case NotDup:
                asyncAddEntry(headersAndPayload, publishContext);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
        MessageDeduplication.MessageDupStatus status =
                messageDeduplication.isDuplicate(publishContext, headersAndPayload);
        switch (status) {
            case NotDup:
                transactionBuffer.appendBufferToTxn(txnID, publishContext.getSequenceId(), headersAndPayload)
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java

    public boolean isActive(InactiveTopicDeleteMode deleteMode) {
        switch (deleteMode) {
            case delete_when_no_subscriptions:
                if (!subscriptions.isEmpty()) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java

    public CompletableFuture<List<String>> getListOfTopics(NamespaceName namespaceName, Mode mode) {
        switch (mode) {
            case ALL:
                return getFullListOfTopics(namespaceName);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/MLPendingAckReplyCallBack.java`
#### Snippet
```java
                pendingAckMetadataEntry.getTxnidLeastBits());
        AckType ackType = pendingAckMetadataEntry.getAckType();
        switch (pendingAckMetadataEntry.getPendingAckOp()) {
            case ABORT:
                pendingAckHandle.handleAbort(txnID, ackType);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupNamespaceConfigListener.java`
#### Snippet
```java
            reloadAllNamespaceResourceGroups();
        } else if (parts.length == 5) {
            switch (notification.getType()) {
                case Modified: {
                    NamespaceName nsName = NamespaceResources.namespaceFromPath(notifyPath);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupConfigListener.java`
#### Snippet
```java
            loadAllResourceGroups();
        } else if (rgName.isPresent()) {
            switch (notification.getType()) {
            case Modified:
                updateResourceGroup(rgName.get());
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroup.java`
#### Snippet
```java
        Set<String> set;

        switch (refType) {
            default:
                return ResourceGroupOpStatus.NotSupported;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundleSplitAlgorithm.java`
#### Snippet
```java
            return null;
        }
        switch (algorithmName) {
            case RANGE_EQUALLY_DIVIDE_NAME:
                return RANGE_EQUALLY_DIVIDE_ALGO;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupService.java`
#### Snippet
```java
        final ResourceGroup rg = this.getResourceGroupInternal(rgName);
        if (rg != null) {
            switch (statsType) {
                default:
                    String errStr = "Unsupported statsType: " + statsType;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupService.java`
#### Snippet
```java
                                     long accByteCount, long accMesgCount, ResourceGroupMonitoringClass monClass) {
        ConcurrentHashMap<String, BytesAndMessagesCount> hm;
        switch (monClass) {
            default:
                log.error("updateStatsWithDiff: Unknown monitoring class={}; ignoring", monClass);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        internalPinnedExecutor.execute(() -> {
            if (!checkIfReady()) {
                switch (state) {
                    case Initializing:
                        addCommitTxnRequest(txnID, properties, lowWaterMark, commitFuture);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        internalPinnedExecutor.execute(() -> {
            if (!checkIfReady()) {
                switch (state) {
                    case Initializing:
                        addIndividualAcknowledgeMessageRequest(txnID, positions, completableFuture);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        internalPinnedExecutor.execute(() -> {
            if (!checkIfReady()) {
                switch (state) {
                    case Initializing:
                        addCumulativeAcknowledgeMessageRequest(txnID, positions, completableFuture);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        internalPinnedExecutor.execute(() -> {
            if (!checkIfReady()) {
                switch (state) {
                    case Initializing:
                        addAbortTxnRequest(txnId, consumer, lowWaterMark, abortFuture);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/NonDurableCursorImpl.java`
#### Snippet
```java
        if (startCursorPosition == null || startCursorPosition.compareTo(ledger.lastConfirmedEntry) > 0) {
            // Start from last entry
            switch (initialPosition) {
                case Latest:
                    initializeCursorPosition(ledger.getLastPositionAndCounter());
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/OpFindNewest.java`
#### Snippet
```java
    public void readEntryComplete(Entry entry, Object ctx) {
        final Position position = entry.getPosition();
        switch (state) {
        case checkFirst:
            if (!condition.test(entry)) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/Errors.java`
#### Snippet
```java

    public static boolean isNoSuchLedgerExistsException(int rc) {
        switch (rc) {
            case Code.NoSuchLedgerExistsOnMetadataServerException:
            case Code.NoSuchLedgerExistsException:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/offload/OffloadUtils.java`
#### Snippet
```java
        }

        switch (ledgerMetadataFormat.getDigestType()) {
            case HMAC:
                builder.withDigestType(DigestType.MAC);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockImpl.java`
#### Snippet
```java
        @Override
        public DigestType getDigestType() {
            switch (this.digestType) {
                case HMAC:
                    return DigestType.MAC;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/extended/SessionEvent.java`
#### Snippet
```java
     */
    public boolean isConnected() {
        switch (this) {
            case Reconnected:
            case SessionReestablished:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java

    private String getBackwardCompatibleKey(String property) {
        switch (getProvider()) {
            case AWS_S3:
                return new StringBuilder().append("s3ManagedLedgerOffload")
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKSessionWatcher.java`
#### Snippet
```java
            CompletableFuture<Watcher.Event.KeeperState> future = new CompletableFuture<>();
            zk.exists("/", false, (StatCallback) (rc, path, ctx, stat) -> {
                switch (KeeperException.Code.get(rc)) {
                case CONNECTIONLOSS:
                    future.complete(Watcher.Event.KeeperState.Disconnected);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKSessionWatcher.java`
#### Snippet
```java

    private void checkState(Watcher.Event.KeeperState zkClientState) {
        switch (zkClientState) {
        case Expired:
            if (currentStatus != SessionEvent.SessionLost) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java

    private Op convertOp(MetadataOp op) {
        switch (op.getType()) {
            case GET: {
                return Op.getData(op.asGet().getPath());
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
                    MetadataOp op = ops.get(i);

                    switch (op.getType()) {
                        case PUT:
                            handlePutResult(op.asPut(), opr);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java

        NotificationType type;
        switch (event.getType()) {
        case NodeCreated:
            type = NotificationType.Created;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/ZKMetadataStore.java`
#### Snippet
```java
        KeeperException ex = KeeperException.create(code, path);

        switch (code) {
        case BADVERSION:
            return new BadVersionException(ex);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdSessionWatcher.java`
#### Snippet
```java

    private void checkState(SessionEvent etcdlientState) {
        switch (etcdlientState) {
            case SessionLost:
                if (currentStatus != SessionEvent.SessionLost) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/RocksdbMetadataStore.java`
#### Snippet
```java
        // Configure log level
        String logLevel = System.getProperty("pulsar.log.level", "info");
        switch (logLevel) {
            case "debug":
                options.setInfoLogLevel(InfoLogLevel.DEBUG_LEVEL);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
            // First, set all the conditions
            ops.forEach(op -> {
                switch (op.getType()) {
                    case PUT: {
                        OpPut put = op.asPut();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
            // Then the requests
            ops.forEach(op -> {
                switch (op.getType()) {
                    case GET: {
                        txn.Then(
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/EtcdMetadataStore.java`
#### Snippet
```java
            int putIdx = 0;
            for (MetadataOp op : ops) {
                switch (op.getType()) {
                    case GET: {
                        OpGet get = op.asGet();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerManager.java`
#### Snippet
```java
        }

        switch (n.getType()) {
            case Modified:
                new ReadLedgerMetadataTask(ledgerId).run();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/cache/impl/MetadataCacheImpl.java`
#### Snippet
```java
    public void accept(Notification t) {
        String path = t.getPath();
        switch (t.getType()) {
        case Created:
        case Modified:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/ComponentTypeUtils.java`
#### Snippet
```java
public class ComponentTypeUtils {
    public static String toString(FunctionDetails.ComponentType componentType) {
        switch (componentType) {
            case FUNCTION:
                return "Function";
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/CryptoUtils.java`
#### Snippet
```java

    public static ProducerCryptoFailureAction getProducerCryptoFailureAction(Function.CryptoSpec.FailureAction action) {
        switch (action) {
            case FAIL:
                return ProducerCryptoFailureAction.FAIL;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/CryptoUtils.java`
#### Snippet
```java

    public static Function.CryptoSpec.FailureAction getProtoFailureAction(ProducerCryptoFailureAction action) {
        switch (action) {
            case FAIL:
                return Function.CryptoSpec.FailureAction.FAIL;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/CryptoUtils.java`
#### Snippet
```java

    public static ConsumerCryptoFailureAction getConsumerCryptoFailureAction(Function.CryptoSpec.FailureAction action) {
        switch (action) {
            case FAIL:
                return ConsumerCryptoFailureAction.FAIL;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/CryptoUtils.java`
#### Snippet
```java

    public static Function.CryptoSpec.FailureAction getProtoFailureAction(ConsumerCryptoFailureAction action) {
        switch (action) {
            case FAIL:
                return Function.CryptoSpec.FailureAction.FAIL;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
            fileName = hierarchy[hierarchy.length - 2];
        }
        switch (functionDetails.getRuntime()) {
            case JAVA:
                return fileName + ".jar";
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionMetaDataManager.java`
#### Snippet
```java
            log.debug("Received Service Request: {}", serviceRequest);
        }
        switch (serviceRequest.getServiceRequestType()) {
            case UPDATE:
                this.processUpdate(serviceRequest.getFunctionMetaData());
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
    private void inferringMissingTypeClassName(Function.FunctionDetails.Builder functionDetailsBuilder,
                                               ClassLoader classLoader) throws ClassNotFoundException {
        switch (functionDetailsBuilder.getComponentType()) {
            case FUNCTION:
                if ((functionDetailsBuilder.hasSource()
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        synchronized (pendingMarkDeleteOps) {
            // The state might have changed while we were waiting on the queue mutex
            switch (STATE_UPDATER.get(this)) {
            case Closed:
                callback.markDeleteFailed(new ManagedLedgerException
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        final AtomicBoolean notClosing = new AtomicBoolean(false);
        STATE_UPDATER.updateAndGet(this, state -> {
            switch (state){
                case Closing:
                case Closed: {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
        PositionImpl startPosition = null;
        long max = 0;
        switch (constraint) {
        case SearchAllAvailableEntries:
            startPosition = (PositionImpl) getFirstPosition();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
     */
    public static boolean isBkErrorNotRecoverable(int rc) {
        switch (rc) {
        case Code.NoSuchLedgerExistsException:
        case Code.NoSuchLedgerExistsOnMetadataServerException:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
            if (!isEmpty(builtin)) {
                File component;
                switch (componentType) {
                    case SOURCE:
                        component = worker().getConnectorsManager().getSourceArchive(builtin).toFile();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
                                    AuthenticationDataSource authenticationData) {
        try {
            switch (componentType) {
                case SINK:
                    return worker().getAuthorizationService().allowSinkOpsAsync(
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/secrets/src/main/java/org/apache/pulsar/functions/secretsproviderconfigurator/DefaultSecretsProviderConfigurator.java`
#### Snippet
```java
    @Override
    public String getSecretsProviderClassName(Function.FunctionDetails functionDetails) {
        switch (functionDetails.getRuntime()) {
            case JAVA:
                return ClearTextSecretsProvider.class.getName();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/secrets/src/main/java/org/apache/pulsar/functions/secretsproviderconfigurator/KubernetesSecretsProviderConfigurator.java`
#### Snippet
```java
    @Override
    public String getSecretsProviderClassName(Function.FunctionDetails functionDetails) {
        switch (functionDetails.getRuntime()) {
            case JAVA:
                return EnvironmentBasedSecretsProvider.class.getName();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/source/TopicSchema.java`
#### Snippet
```java

    private static <T> Schema<T> newSchemaInstance(Class<T> clazz, SchemaType type, ConsumerConfig conf) {
        switch (type) {
        case NONE:
            if (ByteBuffer.class.isAssignableFrom(clazz)) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java

        FunctionConfig.ProcessingGuarantees processingGuarantees = this.pulsarSinkConfig.getProcessingGuarantees();
        switch (processingGuarantees) {
            case ATMOST_ONCE:
                this.pulsarSinkProcessor = new PulsarSinkAtMostOnceProcessor(schema, crypto);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
        String imageName = null;
        if (functionDockerImages != null) {
            switch (runtime) {
                case JAVA:
                    if (functionDockerImages.get("JAVA") != null) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
        Function.FunctionDetails.ComponentType componentType = InstanceUtils.calculateSubjectType(functionDetails);
        String component;
        switch (componentType) {
            case FUNCTION:
                component = "function";
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/InstanceUtils.java`
#### Snippet
```java
                                                    String fullyQualifiedName, int instanceId) {
        Map<String, String> properties = new HashMap<>();
        switch (componentType) {
            case FUNCTION:
                properties.put("application", "pulsar-function");
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/PrometheusTextFormat.java`
#### Snippet
```java
        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            switch (c) {
            case '\\':
                writer.append("\\\\");
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
    private static <T> Schema<T> getSinkSchema(Record<?> record, Class<T> clazz) {
        SchemaType type = getSchemaTypeOrDefault(record, clazz);
        switch (type) {
            case NONE:
                if (ByteBuffer.class.isAssignableFrom(clazz)) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/stats/ComponentStatsManager.java`
#### Snippet
```java
                                  ScheduledExecutorService scheduledExecutorService,
                                  Function.FunctionDetails.ComponentType componentType) {
        switch (componentType) {
            case FUNCTION:
                return new FunctionStatsManager(collectorRegistry, metricsLabels, scheduledExecutorService);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/ContextImpl.java`
#### Snippet
```java
        this.metricsLabels = metricsLabels;
        String prefix;
        switch (componentType) {
            case FUNCTION:
                prefix = FunctionStatsManager.PULSAR_FUNCTION_METRICS_PREFIX;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/ContextImpl.java`
#### Snippet
```java

        Function.SourceSpec sourceSpec = config.getFunctionDetails().getSource();
        switch (sourceSpec.getSubscriptionType()) {
            case FAILOVER:
                subscriptionType = SubscriptionType.Failover;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
     */
    private static boolean isBkErrorNotRecoverable(int rc) {
        switch (rc) {
        case Code.NoSuchLedgerExistsException:
        case Code.NoSuchLedgerExistsOnMetadataServerException:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java

    private static boolean isLedgerNotExistException(int rc) {
        switch (rc) {
            case Code.NoSuchLedgerExistsException:
            case Code.NoSuchLedgerExistsOnMetadataServerException:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
    private ClassLoader isBuiltIn(String component, ComponentType componentType)
        throws IOException {
        switch (componentType) {
            case FUNCTION:
                return isBuiltInFunction(component);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                if (!file.exists()) {
                    String errorMsg;
                    switch (componentType) {
                        case FUNCTION:
                            errorMsg = "User jar";
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
                if (!(runtimeEnv == null || runtimeEnv == RuntimeEnv.THREAD)) {
                    String errorMsg;
                    switch (componentType) {
                        case FUNCTION:
                            errorMsg = "The jar property must be specified in FunctionConfig.";
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
        final TradeConfiguration tradeConf = new TradeConfiguration();
        tradeConf.command = command;
        switch (command) {
        case CHANGE_COMMAND:
            // Change the topic's settings if it exists.
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
                jc.parse(args);
                final String command = arguments.commandArguments.get(0);
                switch (command) {
                case "trade":
                    handleTrade(arguments);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/util/TransactionUtil.java`
#### Snippet
```java
    public static boolean canTransitionTo(TxnStatus currentStatus, TxnStatus newStatus) {

        switch (currentStatus) {
            case OPEN:
                return newStatus != COMMITTED && newStatus != ABORTED;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionMetadataStore.java`
#### Snippet
```java
                            transactionMetadataEntry.getTxnidLeastBits());
                        long transactionId = transactionMetadataEntry.getTxnidLeastBits();
                        switch (transactionMetadataEntry.getMetadataOp()) {
                            case NEW:
                                long txnSequenceId = transactionMetadataEntry.getTxnidLeastBits();
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CustomCommandsUtils.java`
#### Snippet
```java
            for (ParameterDescriptor parameterDescriptor : parameters) {
                CtClass fieldType;
                switch (parameterDescriptor.getType()) {
                    case BOOLEAN_FLAG:
                        //  command -parameter
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CliCommand.java`
#### Snippet
```java
                    s, "(4096, 100K, 10M, 16G, 2T)"));
        }
        switch (last) {
        case 'k':
        case 'K':
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
                }

                switch (status.status) {
                case NOT_RUN:
                    System.out.println("Compaction has not been run for " + persistentTopic
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdConsume.java`
#### Snippet
```java

    private static Map<String, Object> genericObjectToMap(GenericObject value, boolean displayHex) throws IOException {
        switch (value.getSchemaType()) {
            case AVRO:
            case JSON:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
    private static Schema<?> buildComponentSchema(String schema) {
        Schema<?> base;
        switch (schema) {
            case "string":
                base =  Schema.STRING;
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java

    static Schema<?> buildSchema(String keySchema, String schema, String keyValueEncodingType) {
        switch (keyValueEncodingType) {
            case KEY_VALUE_ENCODING_TYPE_NOT_SET:
                return buildComponentSchema(schema);
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdProduce.java`
#### Snippet
```java
                        }

                        switch (keyValueEncodingType) {
                            case KEY_VALUE_ENCODING_TYPE_NOT_SET:
                                if (key != null && !key.isEmpty()) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/stats/prometheus/PrometheusMetricsGeneratorUtils.java`
#### Snippet
```java

    static String getTypeStr(Collector.Type type) {
        switch (type) {
            case COUNTER:
                return "counter";
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }

                switch (status.status) {
                case NOT_RUN:
                    System.out.println("Compaction has not been run for " + persistentTopic
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
                }

                switch (status.getStatus()) {
                case NOT_RUN:
                    System.out.println("Offload has not been run for " + persistentTopic
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/PulsarAuthorizationProvider.java`
#### Snippet
```java
                        return CompletableFuture.completedFuture(true);
                    } else {
                        switch (operation) {
                            case LOOKUP:
                            case GET_STATS:
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/PulsarAuthorizationProvider.java`
#### Snippet
```java
                            // validate if subscription-auth mode is configured
                            if (policies.get().subscription_auth_mode != null) {
                                switch (policies.get().subscription_auth_mode) {
                                    case Prefix:
                                        if (!subscription.startsWith(role)) {
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/PulsarAuthorizationProvider.java`
#### Snippet
```java
                        return CompletableFuture.completedFuture(true);
                    } else {
                        switch (operation) {
                            case PACKAGES:
                                return allowTheSpecifiedActionOpsAsync(
```

### EnhancedSwitchMigration
Switch statement can be replaced with enhanced 'switch'
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/slf4j/Slf4jEvent.java`
#### Snippet
```java
                MDC.put("durationMs", String.valueOf(Duration.between(startTime, clock.instant()).toMillis()));
            }
            switch (level) {
            case ERROR:
                if (throwable != null) {
```

## RuleId[ruleID=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-io/docs/src/main/java/org/apache/pulsar/io/docs/ConnectorDocGenerator.java`
#### Snippet
```java
                    outputDir,
                    "pulsar-io-" + connectorDef.name()
                        + "-" + connectorDef.type().name().toLowerCase()).toString() + ".yml")) {
                PrintWriter pw = new PrintWriter(new OutputStreamWriter(fos, StandardCharsets.UTF_8));
                generateConnectorYaml(connectorClass, connectorDef, pw);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-io/canal/src/main/java/org/apache/pulsar/io/canal/MessageUtils.java`
#### Snippet
```java
                } catch (Exception e) {
                    throw new RuntimeException("ERROR ## parser of eromanga-event has an error, data:"
                            + entry.toString(), e);
                }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcUtils.java`
#### Snippet
```java
                whereJoiner.add(equals.toString());
            }));
            builder.append(whereJoiner.toString());
            return builder.toString();
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java
                    } else {
                        LOGGER.info("Channel {} connected to {}",
                                channelName, channelComponent.components.toString());
                        conf.addChannel(channelName, channelComponent.channel);
                    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/InfluxDBGenericRecordSink.java`
#### Snippet
```java
                sink = new org.apache.pulsar.io.influxdb.v1.InfluxDBGenericRecordSink();
            } catch (Exception e1) {
                throw new Exception("For InfluxDB V2: \n" + e.toString() + "\n"
                + "For InfluxDB V1: \n" + e1.toString());
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-io/influxdb/src/main/java/org/apache/pulsar/io/influxdb/InfluxDBGenericRecordSink.java`
#### Snippet
```java
            } catch (Exception e1) {
                throw new Exception("For InfluxDB V2: \n" + e.toString() + "\n"
                + "For InfluxDB V1: \n" + e1.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarSqlSchemaInfoProvider.java`
#### Snippet
```java
        } catch (PulsarAdminException e) {
            LOG.error("Can't get current schema for topic {}",
                    topicName.toString(), e);
            return FutureUtil.failedFuture(e.getCause());
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroRowDecoderFactory.java`
#### Snippet
```java
                    + topicName.toString() + " extractColumnMetadata failed.");
            throw new TrinoException(NOT_SUPPORTED, "Topic "
                    + topicName.toString() + " schema may contains cyclic definitions.", e);
        }
        return columnMetadata;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonRowDecoderFactory.java`
#### Snippet
```java
                    + topicName.toString() + " extractColumnMetadata failed.");
            throw new TrinoException(NOT_SUPPORTED, "Topic "
                    + topicName.toString() + " schema may contains cyclic definitions.", e);
        }
        return columnMetadata;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarMetadata.java`
#### Snippet
```java
        } else if (matchedTopics.size() != 1) {
            String errMsg = String.format("There are multiple topics %s matched the table name %s",
                    matchedTopics.toString(),
                    String.format("%s/%s", namespace, schemaTableName.getTableName()));
            log.error(errMsg);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/protobufnative/PulsarProtobufNativeRowDecoderFactory.java`
#### Snippet
```java
                break;
            default:
                throw new RuntimeException("Unknown type: " + type.toString() + " for FieldDescriptor: "
                        + field.getName());
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/server/LookupProxyHandler.java`
#### Snippet
```java
            clientCnx.newLookup(command, requestId).whenComplete((r, t) -> {
                if (t != null) {
                    log.warn("[{}] failed to get Partitioned metadata : {}", topicName.toString(),
                        t.getMessage(), t);
                    proxyConnection.ctx().writeAndFlush(Commands.newLookupErrorResponse(getServerError(t),
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
        @Parameter(names = {"-c", "--broker-conf"}, description = "Configuration file for Broker")
        private String brokerConfigFile =
                Paths.get("").toAbsolutePath().normalize().toString() + "/conf/broker.conf";

        @Parameter(names = {"-rb", "--run-bookie"}, description = "Run Bookie together with Broker")
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
        @Parameter(names = {"-bc", "--bookie-conf"}, description = "Configuration file for Bookie")
        private String bookieConfigFile =
                Paths.get("").toAbsolutePath().normalize().toString() + "/conf/bookkeeper.conf";

        @Parameter(names = {"-rfw", "--run-functions-worker"}, description = "Run functions worker with Broker")
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
        @Parameter(names = {"-fwc", "--functions-worker-conf"}, description = "Configuration file for Functions Worker")
        private String fnWorkerConfigFile =
                Paths.get("").toAbsolutePath().normalize().toString() + "/conf/functions_worker.yml";

        @Parameter(names = {"-h", "--help"}, description = "Show this help message")
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarStandalone.java`
#### Snippet
```java
    @Parameter(names = {"-fwc", "--functions-worker-conf"}, description = "Configuration file for Functions Worker")
    private String fnWorkerConfigFile =
            Paths.get("").toAbsolutePath().normalize().toString() + "/conf/functions_worker.yml";

    @Parameter(names = {"-nss", "--no-stream-storage"}, description = "Disable stream storage")
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/TransactionMetadataStoreService.java`
#### Snippet
```java
                        deque.add(completableFuture);
                        if (LOG.isDebugEnabled()) {
                            LOG.debug("Handle tc client connect added into pending queue! tcId : {}", tcId.toString());
                        }
                    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v1/NonPersistentTopics.java`
#### Snippet
```java
            throw wae;
        } catch (Exception e) {
            log.error("[{}] Failed to unload namespace bundle {}/{}", clientAppId(), fqnn.toString(), bundleRange, e);
            throw new RestException(e);
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
                Policies policies = policiesResult.get();
                if (!allowDeletedNamespace && policies.deleted) {
                    String msg = String.format("Namespace %s is deleted", namespace.toString());
                    log.warn(msg);
                    validationFuture.completeExceptionally(new RestException(Status.NOT_FOUND,
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
                    String msg = String.format(
                            "Namespace does not have any clusters configured : local_cluster=%s ns=%s",
                            localCluster, namespace.toString());
                    log.warn(msg);
                    validationFuture.completeExceptionally(new RestException(Status.PRECONDITION_FAILED, msg));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
                                            "Namespace missing local cluster name in clusters list: local_cluster=%s"
                                                    + " ns=%s clusters=%s",
                                            localCluster, namespace.toString(), policies.replication_clusters);
                                    log.warn(msg);
                                    validationFuture.completeExceptionally(new RestException(Status.PRECONDITION_FAILED,
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/web/PulsarWebResource.java`
#### Snippet
```java
                }
            } else {
                String msg = String.format("Namespace %s not found", namespace.toString());
                log.warn(msg);
                validationFuture.completeExceptionally(new RestException(Status.NOT_FOUND, "Namespace not found"));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java

                            if (log.isDebugEnabled()) {
                                log.debug("[{}] Lookup result {}", topicName.toString(), lookupResult);
                            }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java
                    if (ex instanceof CompletionException && ex.getCause() instanceof IllegalStateException) {
                        log.info("Failed to lookup {} for topic {} with error {}", clientAppId,
                                topicName.toString(), ex.getCause().getMessage());
                    } else {
                        log.warn("Failed to lookup {} for topic {} with error {}", clientAppId,
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java
                    } else {
                        log.warn("Failed to lookup {} for topic {} with error {}", clientAppId,
                                topicName.toString(), ex.getMessage(), ex);
                    }
                    lookupfuture.complete(
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java
        }).exceptionally(ex -> {
            if (ex instanceof CompletionException && ex.getCause() instanceof IllegalStateException) {
                log.info("Failed to lookup {} for topic {} with error {}", clientAppId, topicName.toString(),
                        ex.getCause().getMessage());
            } else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/TopicLookupBase.java`
#### Snippet
```java
                        ex.getCause().getMessage());
            } else {
                log.warn("Failed to lookup {} for topic {} with error {}", clientAppId, topicName.toString(),
                        ex.getMessage(), ex);
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                                == null ? "has no metadata" : "has zero partitions";
                        throw new RestException(Status.NOT_FOUND, String.format(
                                "Partitioned Topic not found: %s %s", topicName.toString(), topicErrorType));
                    }
                })
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    private static void logAuthException(SocketAddress remoteAddress, String operation,
                                         String principal, Optional<TopicName> topic, Throwable ex) {
        String topicString = topic.map(t -> ", topic=" + t.toString()).orElse("");
        if (ex instanceof AuthenticationException) {
            log.info("[{}] Failed to authenticate: operation={}, principal={}{}, reason={}",
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/ServerCnx.java`
#### Snippet
```java
    private static void logNamespaceNameAuthException(SocketAddress remoteAddress, String operation,
                                         String principal, Optional<NamespaceName> namespaceName, Throwable ex) {
        String namespaceNameString = namespaceName.map(t -> ", namespace=" + t.toString()).orElse("");
        if (ex instanceof AuthenticationException) {
            log.info("[{}] Failed to authenticate: operation={}, principal={}{}, reason={}",
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/NamespaceEventsSystemTopicFactory.java`
#### Snippet
```java
        TopicName topicName = TopicName.get(TopicDomain.persistent.value(), namespaceName,
                SystemTopicNames.TRANSACTION_BUFFER_SNAPSHOT);
        log.info("Create transaction buffer snapshot client, topicName : {}", topicName.toString());
        return new TransactionBufferSnapshotBaseSystemTopicClient(client, topicName,
                systemTopicTxnBufferSnapshotService, schemaType);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/NamespaceEventsSystemTopicFactory.java`
#### Snippet
```java
        TopicName topicName = TopicName.get(TopicDomain.persistent.value(), namespaceName,
                SystemTopicNames.NAMESPACE_EVENTS_LOCAL_NAME);
        log.info("Create topic policies system topic client {}", topicName.toString());
        return new TopicPoliciesSystemTopicClient(client, topicName);
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
        try {
            List<Topic> topicList = pulsar().getBrokerService().getAllTopicsFromNamespaceBundle(nsName.toString(),
                    nsName.toString() + "/" + bundleRange);
            List<CompletableFuture<Void>> futures = new ArrayList<>();
            if (subscription.startsWith(pulsar().getConfiguration().getReplicatorPrefix())) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/NamespacesBase.java`
#### Snippet
```java
        try {
            List<Topic> topicList = pulsar().getBrokerService().getAllTopicsFromNamespaceBundle(nsName.toString(),
                    nsName.toString() + "/" + bundleRange);

            List<CompletableFuture<Void>> futures = new ArrayList<>();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
                    if (LOG.isDebugEnabled()) {
                        LOG.debug("Added Primary Broker - [{}] as possible Candidates for"
                                + " namespace - [{}] with policies", brokerUrl.getHost(), namespace.toString());
                    }
                } else if (policies.isSecondaryBroker(namespace, brokerUrl.getHost())) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
                                "Added Shared Broker - [{}] as possible "
                                        + "Candidates for namespace - [{}] with policies",
                                brokerUrl.getHost(), namespace.toString());
                    }
                } else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
                    if (LOG.isDebugEnabled()) {
                        LOG.debug("Skipping Broker - [{}] not primary broker and not shared" + " for namespace - [{}] ",
                                brokerUrl.getHost(), namespace.toString());
                    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/LoadManagerShared.java`
#### Snippet
```java
                        "Not enough of primaries [{}] available for namespace - [{}], "
                                + "adding shared [{}] as possible candidate owners",
                        primariesCache.size(), namespace.toString(), secondaryCache.size());
                brokerCandidateCache.addAll(secondaryCache);
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
                            String msg1 = format(
                                    "failed to disable bundle %s under namespace [%s] with error %s",
                                    bundle.getNamespaceObject().toString(), bundle.toString(), ex.getMessage());
                            LOG.warn(msg1, e);
                            completionFuture.completeExceptionally(new ServiceUnitNotReadyException(msg1));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactorTool.java`
#### Snippet
```java
    private static class Arguments {
        @Parameter(names = {"-c", "--broker-conf"}, description = "Configuration file for Broker")
        private String brokerConfigFile = Paths.get("").toAbsolutePath().normalize().toString() + "/conf/broker.conf";

        @Parameter(names = {"-t", "--topic"}, description = "Topic to compact", required = true)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                                } catch (Exception e) {
                                    log.error("Failed to unload namespace-bundle {}-{} that not owned by {}, {}",
                                            namespace.toString(), bundle.toString(), localCluster, e.getMessage());
                                }
                            });
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
            TopicName topicName = TopicName.get(topic);
            if (serviceUnit.includes(topicName) && getTopicReference(topic).isPresent()) {
                log.info("[{}][{}] Clean unloaded topic from cache.", serviceUnit.toString(), topic);
                pulsar.getBrokerService().removeTopicFromCache(topicName.toString(), serviceUnit, null);
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ControlledClusterFailover.java`
#### Snippet
```java
                        && !controlledConfiguration.equals(currentControlledConfiguration)) {
                    log.info("Switch Pulsar service url from {} to {}",
                            currentControlledConfiguration, controlledConfiguration.toString());

                    Authentication authentication = null;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/auth/oauth2/protocol/DefaultMetadataResolver.java`
#### Snippet
```java

        } catch (IOException e) {
            throw new IOException("Cannot obtain authorization metadata from " + metadataUrl.toString(), e);
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/reader/MultiVersionAvroReader.java`
#### Snippet
```java
            LOG.info("Load schema reader for version({}), schema is : {}, schemaInfo: {}",
                    SchemaUtils.getStringSchemaVersion(schemaVersion.get()),
                    schemaInfo.getSchemaDefinition(), schemaInfo.toString());
            boolean jsr310ConversionEnabled = getJsr310ConversionEnabledFromSchemaInfo(schemaInfo);
            return new AvroReader<>(parseAvroSchema(schemaInfo.getSchemaDefinition()),
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/transaction/TransactionCoordinatorClientImpl.java`
#### Snippet
```java
    private String getTCAssignTopicName(int partition) {
        if (partition >= 0) {
            return SystemTopicNames.TRANSACTION_COORDINATOR_ASSIGN.toString()
                    + TopicName.PARTITIONED_TOPIC_SUFFIX + partition;
        } else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/KerberosName.java`
#### Snippet
```java
            }
        }
        throw new NoMatchingRule("No rules applied to " + toString());
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Codec.java`
#### Snippet
```java
            return URLEncoder.encode(s, StandardCharsets.UTF_8.toString());
        } catch (UnsupportedEncodingException e) {
            LOG.error(String.format("%s is Unknown", StandardCharsets.UTF_8.toString()) + "exception - [{}]", e);
            return s;
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Codec.java`
#### Snippet
```java
            return URLDecoder.decode(s, StandardCharsets.UTF_8.toString());
        } catch (UnsupportedEncodingException e) {
            LOG.error(String.format("%s is Unknown", StandardCharsets.UTF_8.toString()) + "exception - [{}]", e);
            return s;
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/TGTRefreshThread.java`
#### Snippet
```java
            } else if (now < nextRefresh) {
                Date until = new Date(nextRefresh);
                log.info("TGT refresh sleeping until: {}", until.toString());
                try {
                    Thread.sleep(nextRefresh - now);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-common/src/main/java/org/apache/pulsar/common/naming/NamespaceName.java`
#### Snippet
```java
        }
        NamedEntity.checkName(topic);
        return String.format("%s://%s/%s", domain.toString(), namespace, topic);
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-common/src/main/java/org/apache/pulsar/common/naming/TopicName.java`
#### Snippet
```java
            return this;
        }
        String partitionName = this.toString() + PARTITIONED_TOPIC_SUFFIX + index;
        return get(partitionName);
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicyImpl.java`
#### Snippet
```java
    public List<URL> findSecondaryBrokers(List<URL> availableBrokers, NamespaceName namespace) {
        if (!this.matchNamespaces(namespace.toString())) {
            throw new IllegalArgumentException("Namespace " + namespace.toString() + " does not match policy");
        }
        // find the available brokers that matches primary brokers regex list
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-common/src/main/java/org/apache/pulsar/common/policies/impl/NamespaceIsolationPolicyImpl.java`
#### Snippet
```java
    public List<URL> findPrimaryBrokers(List<URL> availableBrokers, NamespaceName namespace) {
        if (!this.matchNamespaces(namespace.toString())) {
            throw new IllegalArgumentException("Namespace " + namespace.toString() + " does not match policy");
        }
        // find the available brokers that matches primary brokers regex list
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiTopicsConsumerImpl.java`
#### Snippet
```java
            if (log.isDebugEnabled()) {
                log.debug("[{}]  run onTopicsExtended: {}, size: {}",
                    topic, topicsExtended.toString(), topicsExtended.size());
            }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/provider/TieredStorageConfiguration.java`
#### Snippet
```java
            });

        log.info("getOverrides: {}", overrides.toString());
        return overrides;
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java

    public static String getUniquePackageName(String packageName) {
        return String.format("%s-%s", UUID.randomUUID().toString(), packageName);
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
            callback.markDeleteFailed(
                    new ManagedLedgerException("Reset cursor in progress - unable to mark delete position "
                            + position.toString()),
                    ctx);
            return;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/CmdGenerateDocumentation.java`
#### Snippet
```java
                        .append("|").append(option.getDefault()).append("|\n")
        );
        System.out.println(sb.toString());
        return sb.toString();
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/PackagesImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<List<String>> listPackagesAsync(String type, String namespace) {
        WebTarget path = packages.path(type + "/" + NamespaceName.get(namespace).toString());
        return asyncGetRequest(path, new FutureCallback<List<String>>(){});
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/WebTargets.java`
#### Snippet
```java
                    encode = URLEncoder.encode(part, StandardCharsets.UTF_8.toString());
                } catch (UnsupportedEncodingException e) {
                    log.error("{} is Unknown exception - [{}]", StandardCharsets.UTF_8.toString(), e);
                    encode = part;
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/CmdGenerateDocumentation.java`
#### Snippet
```java
                        .append("|").append(option.getDefault()).append("|\n")
        );
        System.out.println(sb.toString());
        return sb.toString();
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authorization/PulsarAuthorizationProvider.java`
#### Snippet
```java
        }
        if (log.isDebugEnabled()) {
            log.debug("Topic [{}] does not belong to local cluster [{}]", topicName.toString(), conf.getClusterName());
        }
        return pulsarResources.getClusterResources().listAsync()
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-package-management/core/src/main/java/org/apache/pulsar/packages/management/core/common/PackageName.java`
#### Snippet
```java
        this.completeName = String.format("%s/%s/%s", tenant, namespace, name);
        this.completePackageName =
            String.format("%s://%s/%s/%s@%s", type.toString(), tenant, namespace, name, version);
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-package-management/core/src/main/java/org/apache/pulsar/packages/management/core/impl/PackagesManagementImpl.java`
#### Snippet
```java
                    if (t != null) {
                        future.completeExceptionally(new PackagesManagementException(
                            String.format("Update package '%s' metadata failed", packageName.toString()), t));
                    } else {
                        future.complete(null);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-package-management/core/src/main/java/org/apache/pulsar/packages/management/core/impl/PackagesManagementImpl.java`
#### Snippet
```java
        } catch (IOException e) {
            future.completeExceptionally(new PackagesManagementException(
                String.format("Read package '%s' metadata failed", packageName.toString()), e));
        }
        return future;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-package-management/core/src/main/java/org/apache/pulsar/packages/management/core/impl/PackagesManagementImpl.java`
#### Snippet
```java
                } catch (IOException e) {
                    future.completeExceptionally(new PackagesManagementException(
                        String.format("Read package '%s' metadata failed", packageName.toString()), e));
                }
            });
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-package-management/core/src/main/java/org/apache/pulsar/packages/management/core/impl/PackagesManagementImpl.java`
#### Snippet
```java
                } else {
                    future.completeExceptionally(
                        new NotFoundException(String.format("Package '%s' already exists", packageName.toString())));
                }
            });
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `pulsar-package-management/core/src/main/java/org/apache/pulsar/packages/management/core/impl/PackagesManagementImpl.java`
#### Snippet
```java
                } else {
                    future.completeExceptionally(
                        new NotFoundException(String.format("Package '%s' does not exist", packageName.toString())));
                }
            });
```

## RuleId[ruleID=SuspiciousMethodCalls]
### SuspiciousMethodCalls
'Map\>' may not contain values of type 'String'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
        // sanity check to make sure assignments and runtimeinfo is in sync
        if (functionRuntimeInfo == null) {
            if (this.workerIdToAssignments.containsValue(workerConfig.getWorkerId())
                    && this.workerIdToAssignments.get(workerConfig.getWorkerId())
                    .containsValue(fullyQualifiedInstanceId)) {
```

### SuspiciousMethodCalls
'Map' may not contain values of type 'String'
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionRuntimeManager.java`
#### Snippet
```java
            if (this.workerIdToAssignments.containsValue(workerConfig.getWorkerId())
                    && this.workerIdToAssignments.get(workerConfig.getWorkerId())
                    .containsValue(fullyQualifiedInstanceId)) {
                log.error("Assignments and RuntimeInfos are inconsistent."
                        + "FunctionRuntimeInfo missing for " + fullyQualifiedInstanceId);
```

### SuspiciousMethodCalls
'Map' may not contain keys of type 'String'
in `pulsar-client-admin/src/main/java/org/apache/pulsar/client/admin/internal/TopicsImpl.java`
#### Snippet
```java
        ListTopicsOptions options = ListTopicsOptions
                .builder()
                .bundle((String) params.get(QueryParam.Bundle.value))
                .build();
        return getListAsync(namespace, topicDomain, options);
```

## RuleId[ruleID=SetReplaceableByEnumSet]
### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
                                               @PathParam("namespace") String namespace) {
            validateNamespaceName(tenant, namespace);
            internalSetSubscriptionTypesEnabled(new HashSet<>());
    }

```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
                .thenCompose(__ -> getNamespacePoliciesAsync(namespaceName))
                .thenAccept(policies -> {
                    Set<SubscriptionType> subscriptionTypes = new HashSet<>();
                    policies.subscription_types_enabled.forEach(
                            subType -> subscriptionTypes.add(SubscriptionType.valueOf(subType)));
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
        }
        if (getMetadataEventSynchronizer().isPresent()) {
            MetadataEvent event = new MetadataEvent(path, null, new HashSet<>(),
                    expectedVersion.orElse(null), Instant.now().toEpochMilli(),
                    getMetadataEventSynchronizer().get().getClusterName(), NotificationType.Deleted);
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/AbstractMetadataStore.java`
#### Snippet
```java
            return FutureUtil.failedFuture(new MetadataStoreException.InvalidPathException(path));
        }
        HashSet<CreateOption> ops = new HashSet<>(options);
        if (getMetadataEventSynchronizer().isPresent()) {
            Long version = optExpectedVersion.isPresent() && optExpectedVersion.get() < 0 ? null
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
        void run() throws PulsarAdminException {
            String persistentTopic = validatePersistentTopic(params);
            Set<SubscriptionType> types = new HashSet<>();
            subTypes.forEach(s -> {
                SubscriptionType subType;
```

### SetReplaceableByEnumSet
`TreeSet<>` can be replaced with 'EnumSet'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CliCommand.java`
#### Snippet
```java

    static Set<AuthAction> getAuthActions(List<String> actions) {
        Set<AuthAction> res = new TreeSet<>();
        AuthAction authAction;
        for (String action : actions) {
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        void run() throws PulsarAdminException {
            String namespace = validateNamespace(params);
            Set<SubscriptionType> types = new HashSet<>();
            subTypes.forEach(s -> {
                SubscriptionType subType;
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        void run() throws PulsarAdminException {
            String persistentTopic = validatePersistentTopic(params);
            Set<SubscriptionType> types = new HashSet<>();
            subTypes.forEach(s -> {
                SubscriptionType subType;
```

## RuleId[ruleID=Java9CollectionFactory]
### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/DimensionStats.java`
#### Snippet
```java
    private final Summary summary;
    private static final double[] QUANTILES = {0.50, 0.75, 0.95, 0.99, 0.999, 0.9999};
    private static final List<String> QUANTILE_LABEL = Collections.unmodifiableList(Arrays.asList("quantile"));
    private static final List<List<String>> QUANTILE_LABEL_VALUES = Collections.unmodifiableList(
            Arrays.stream(QUANTILES).mapToObj(Collector::doubleToGoString)
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/kubernetes/KubernetesRuntime.java`
#### Snippet
```java
    private final AppsV1Api appsClient;
    private final CoreV1Api coreClient;
    static final List<String> TOLERATIONS = Collections.unmodifiableList(
            Arrays.asList(
                    "node.kubernetes.io/not-ready",
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `pulsar-websocket/src/main/java/org/apache/pulsar/websocket/ProducerHandler.java`
#### Snippet
```java
            AtomicLongFieldUpdater.newUpdater(ProducerHandler.class, "msgPublishedCounter");

    public static final List<Long> ENTRY_LATENCY_BUCKETS_USEC = Collections.unmodifiableList(Arrays.asList(
            500L, 1_000L, 5_000L, 10_000L, 20_000L, 50_000L, 100_000L, 200_000L, 1000_000L));

```

## RuleId[ruleID=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `DocsToInsertSubscriber` may be 'static'
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSink.java`
#### Snippet
```java
    }

    private class DocsToInsertSubscriber implements Subscriber<InsertManyResult> {
        final List<Document> docsToInsert;
        final List<Record<byte[]>> recordsToInsert;
```

### InnerClassMayBeStatic
Inner class `RabbitMQConsumer` may be 'static'
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSource.java`
#### Snippet
```java
    }

    private class RabbitMQConsumer extends DefaultConsumer {
        private RabbitMQSource source;

```

### InnerClassMayBeStatic
Inner class `AWriteListener` may be 'static'
in `pulsar-io/aerospike/src/main/java/org/apache/pulsar/io/aerospike/AerospikeAbstractSink.java`
#### Snippet
```java
    }

    private class AWriteListener implements WriteListener {
        private Record<byte[]> context;
        private BlockingQueue<AWriteListener> queue;
```

### InnerClassMayBeStatic
Inner class `TestCounter` may be 'static'
in `testmocks/src/main/java/org/apache/bookkeeper/client/TestStatsProvider.java`
#### Snippet
```java
     * In-memory counter.
     */
    public class TestCounter implements Counter {
        private AtomicLong val = new AtomicLong(0);
        private AtomicLong max = new AtomicLong(0);
```

### InnerClassMayBeStatic
Inner class `TestOpStatsLogger` may be 'static'
in `testmocks/src/main/java/org/apache/bookkeeper/client/TestStatsProvider.java`
#### Snippet
```java
     * In-memory StatsLogger.
     */
    public class TestOpStatsLogger implements OpStatsLogger {
        private long successCount;
        private long successValue;
```

### InnerClassMayBeStatic
Inner class `NoTxnsCommittedAtLedgerException` may be 'static'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
     * Exception is thrown when no transactions found committed at a given ledger.
     */
    public class NoTxnsCommittedAtLedgerException extends TransactionBufferException {

        private static final long serialVersionUID = 0L;
```

### InnerClassMayBeStatic
Inner class `TransactionBufferProviderException` may be 'static'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/exception/buffer/TransactionBufferException.java`
#### Snippet
```java
     * Transaction buffer provider exception.
     */
    public class TransactionBufferProviderException extends TransactionBufferException {

        public TransactionBufferProviderException(String message) {
```

### InnerClassMayBeStatic
Inner class `TopicInfo` may be 'static'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/FlowOrQpsEquallyDivideBundleSplitAlgorithm.java`
#### Snippet
```java
    private static final long MBytes = 1024 * 1024;

    class TopicInfo {
        String topicName;
        double msgRate;
```

### InnerClassMayBeStatic
Inner class `MarkDeletingMarkedPosition` may be 'static'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    }

    private final class MarkDeletingMarkedPosition extends IllegalArgumentException {
        public MarkDeletingMarkedPosition(String s) {
            super(s);
```

### InnerClassMayBeStatic
Inner class `MarkDeleteEntry` may be 'static'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedCursorImpl.java`
#### Snippet
```java
    private volatile boolean cacheReadEntry = false;

    class MarkDeleteEntry {
        final PositionImpl newPosition;
        final MarkDeleteCallback callback;
```

### InnerClassMayBeStatic
Inner class `Tuple` may be 'static'
in `pulsar-testclient/src/main/java/org/apache/pulsar/proxy/socket/client/PerformanceClient.java`
#### Snippet
```java
    }

    private class Tuple {
        private WebSocketClient produceClient;
        private ClientUpgradeRequest produceRequest;
```

### InnerClassMayBeStatic
Inner class `MutableBoolean` may be 'static'
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationClient.java`
#### Snippet
```java
        }

        private class MutableBoolean {
            public volatile boolean value = true;
        }
```

### InnerClassMayBeStatic
Inner class `BaseCommand` may be 'static'
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctionWorker.java`
#### Snippet
```java
     */
    @Getter
    abstract class BaseCommand extends CliCommand {
        @Override
        void run() throws Exception {
```

### InnerClassMayBeStatic
Inner class `AuthParams` may be 'static'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderBasic.java`
#### Snippet
```java
    }

    private class AuthParams {
        private String userId;
        private String password;
```

## RuleId[ruleID=NestedSynchronizedStatement]
### NestedSynchronizedStatement
Nested `synchronized` statement
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
                                                ConcurrentOpenHashSet<String>>newBuilder()
                                                .build());
                synchronized (namespaceToBundleRange) {
                    namespaceToBundleRange.computeIfAbsent(namespaceName,
                            k -> ConcurrentOpenHashSet.<String>newBuilder().build())
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java
                synchronized (PendingRead.this) {
                    completed = true;
                    synchronized (ledgerCache) {
                        ledgerCache.remove(key, this);
                    }
```

## RuleId[ruleID=StringEqualsEmptyString]
### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/jetty/tls/JettySslContextFactory.java`
#### Snippet
```java
            }

            if (sslProviderString != null && !sslProviderString.equals("")) {
                setProvider(sslProviderString);
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `pulsar-client-1x-base/pulsar-client-1x/src/main/java/org/apache/pulsar/client/api/ConsumerConfiguration.java`
#### Snippet
```java
     */
    public ConsumerConfiguration setConsumerName(String consumerName) {
        checkArgument(consumerName != null && !consumerName.equals(""));
        conf.setConsumerName(consumerName);
        return this;
```

## RuleId[ruleID=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `waitObject`
in `pulsar-io/nsq/src/main/java/org/apache/pulsar/io/nsq/NSQSource.java`
#### Snippet
```java
            //wait
            try {
                synchronized (waitObject) {
                    waitObject.wait();
                }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `waitObject`
in `pulsar-io/nsq/src/main/java/org/apache/pulsar/io/nsq/NSQSource.java`
#### Snippet
```java
    private void stopThread() {
        LOG.info("Source closed");
        synchronized (waitObject) {
            waitObject.notify();
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `incomingList`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java
    public void write(Record<T> record) throws Exception {
        int number;
        synchronized (incomingList) {
            incomingList.add(record);
            number = incomingList.size();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `incomingList`
in `pulsar-io/jdbc/core/src/main/java/org/apache/pulsar/io/jdbc/JdbcAbstractSink.java`
#### Snippet
```java
            final Deque<Record<T>> swapList = new LinkedList<>();

            synchronized (incomingList) {
                if (log.isDebugEnabled()) {
                    log.debug("Starting flush, queue size: {}", incomingList.size());
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `waitObject`
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHose.java`
#### Snippet
```java
            // just wait now
            try {
                synchronized (waitObject) {
                    waitObject.wait();
                }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `waitObject`
in `pulsar-io/twitter/src/main/java/org/apache/pulsar/io/twitter/TwitterFireHose.java`
#### Snippet
```java
    private void stopThread() {
        LOG.info("Source closed");
        synchronized (waitObject) {
            waitObject.notify();
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `currentLoadReports`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
        // overloadedRU --> bundleName
        Map<ResourceUnit, String> namespaceBundlesToBeUnloaded = new HashMap<>();
        synchronized (currentLoadReports) {
            for (Map.Entry<ResourceUnit, LoadReport> entry : currentLoadReports.entrySet()) {
                ResourceUnit overloadedRU = entry.getKey();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `currentLoadReports`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/SimpleLoadManagerImpl.java`
#### Snippet
```java
    private void updateRanking() {
        try {
            synchronized (currentLoadReports) {
                currentLoadReports.clear();
                for (String broker : getAvailableBrokers()) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `brokerToFailureDomainMap`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
        String clusterName = pulsar.getConfiguration().getClusterName();
        try {
            synchronized (brokerToFailureDomainMap) {
                Map<String, String> tempBrokerToFailureDomainMap = new HashMap<>();
                for (String domainName : fdr.listFailureDomains(clusterName)) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `bundleSplitStrategy`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/ModularLoadManagerImpl.java`
#### Snippet
```java
        }
        final boolean unloadSplitBundles = pulsar.getConfiguration().isLoadBalancerAutoUnloadSplitBundlesEnabled();
        synchronized (bundleSplitStrategy) {
            final Set<String> bundlesToBeSplit = bundleSplitStrategy.findBundlesToSplit(loadData, pulsar);
            NamespaceBundleFactory namespaceBundleFactory = pulsar.getNamespaceService().getNamespaceBundleFactory();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `subscribeRateLimiter`
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/PersistentTopic.java`
#### Snippet
```java
    public void updateSubscribeRateLimiter() {
        SubscribeRate subscribeRate = getSubscribeRate();
        synchronized (subscribeRateLimiter) {
            if (isSubscribeRateEnabled(subscribeRate)) {
                if (subscribeRateLimiter.isPresent()) {
```

## RuleId[ruleID=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'basicAuthSet \&\& tokenAuthSet \&\& apiKeySet' covered by subsequent condition 'basicAuthSet \&\& tokenAuthSet'
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/ElasticSearchConfig.java`
#### Snippet
```java
        boolean tokenAuthSet = StringUtils.isNotEmpty(token);
        boolean apiKeySet = StringUtils.isNotEmpty(apiKey);
        if ((basicAuthSet && tokenAuthSet && apiKeySet)
                || (basicAuthSet && tokenAuthSet)
                || (basicAuthSet && apiKeySet)
```

### ConditionCoveredByFurtherCondition
Condition 'obj != null' covered by subsequent condition 'obj instanceof NamespaceBundles'
in `pulsar-broker/src/main/java/org/apache/pulsar/common/naming/NamespaceBundles.java`
#### Snippet
```java
    @Override
    public boolean equals(Object obj) {
        if (obj != null && obj instanceof NamespaceBundles) {
            NamespaceBundles other = (NamespaceBundles) obj;
            return (Objects.equals(this.nsname, other.nsname) && Objects.equals(this.bundles, other.bundles));
```

## RuleId[ruleID=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `software.amazon.awssdk.auth.credentials` is unnecessary, and can be replaced with an import
in `pulsar-io/aws/src/main/java/org/apache/pulsar/io/aws/AwsDefaultProviderChainPlugin.java`
#### Snippet
```java

    @Override
    public software.amazon.awssdk.auth.credentials.AwsCredentialsProvider getV2CredentialsProvider() {
        return DefaultCredentialsProvider.create();
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `software.amazon.awssdk.auth.credentials` is unnecessary, and can be replaced with an import
in `pulsar-io/aws/src/main/java/org/apache/pulsar/io/aws/AwsCredentialProviderPlugin.java`
#### Snippet
```java
     * Defaults to an implementation that pulls credentials from a v1 provider
     */
    default software.amazon.awssdk.auth.credentials.AwsCredentialsProvider getV2CredentialsProvider() {
        // make a small wrapper to forward requests to v1, this allows
        // for this interface to not "break" for implementers
```

### UnnecessaryFullyQualifiedName
Qualifier `software.amazon.awssdk.auth.credentials` is unnecessary, and can be replaced with an import
in `pulsar-io/aws/src/main/java/org/apache/pulsar/io/aws/AwsCredentialProviderPlugin.java`
#### Snippet
```java
            AWSCredentials creds = v1Provider.getCredentials();
            if (creds instanceof AWSSessionCredentials) {
                return software.amazon.awssdk.auth.credentials.AwsSessionCredentials.create(
                        creds.getAWSAccessKeyId(),
                        creds.getAWSSecretKey(),
```

### UnnecessaryFullyQualifiedName
Qualifier `software.amazon.awssdk.auth.credentials` is unnecessary, and can be replaced with an import
in `pulsar-io/aws/src/main/java/org/apache/pulsar/io/aws/AwsCredentialProviderPlugin.java`
#### Snippet
```java
                        ((AWSSessionCredentials) creds).getSessionToken());
            } else {
                return software.amazon.awssdk.auth.credentials.AwsBasicCredentials.create(
                        creds.getAWSAccessKeyId(),
                        creds.getAWSSecretKey());
```

### UnnecessaryFullyQualifiedName
Qualifier `software.amazon.awssdk.auth.credentials` is unnecessary, and can be replaced with an import
in `pulsar-io/aws/src/main/java/org/apache/pulsar/io/aws/STSAssumeRoleProviderPlugin.java`
#### Snippet
```java

    @Override
    public software.amazon.awssdk.auth.credentials.AwsCredentialsProvider getV2CredentialsProvider() {
        StsClient client = StsClient.create();
        return StsAssumeRoleCredentialsProvider.builder().stsClient(client).refreshRequest((req) -> {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio.file` is unnecessary and can be removed
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/utils/GZipFiles.java`
#### Snippet
```java
    /**
     * Get a lazily loaded stream of lines from a gzipped file, similar to
     * {@link Files#lines(java.nio.file.Path)}.
     *
     * @param path
```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio.file` is unnecessary and can be removed
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/utils/ZipFiles.java`
#### Snippet
```java
    /**
     * Get a lazily loaded stream of lines from a gzipped file, similar to
     * {@link Files#lines(java.nio.file.Path)}.
     *
     * @param path
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary, and can be replaced with an import
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/HttpSink.java`
#### Snippet
```java
            case KEY_VALUE:
                KeyValueSchema<?, ?> keyValueSchema = (KeyValueSchema<?, ?>) schema;
                org.apache.pulsar.common.schema.KeyValue<?, ?> keyValue =
                    (org.apache.pulsar.common.schema.KeyValue<?, ?>) val;
                Map<String, Object> jsonKeyValue = new HashMap<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary, and can be replaced with an import
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/HttpSink.java`
#### Snippet
```java
                KeyValueSchema<?, ?> keyValueSchema = (KeyValueSchema<?, ?>) schema;
                org.apache.pulsar.common.schema.KeyValue<?, ?> keyValue =
                    (org.apache.pulsar.common.schema.KeyValue<?, ?>) val;
                Map<String, Object> jsonKeyValue = new HashMap<>();
                Object key = keyValue.getKey();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.generic` is unnecessary, and can be replaced with an import
in `pulsar-io/http/src/main/java/org/apache/pulsar/io/http/HttpSink.java`
#### Snippet
```java
                return jsonKeyValue;
            case AVRO:
                return JsonConverter.toJson((org.apache.avro.generic.GenericRecord) val);
            case JSON:
                return val;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/EnvVarResolverProperties.java`
#### Snippet
```java
/**
 * A class that extends the Java built-in Properties overriding.
 * {@link java.util.Properties#getProperty(String)} to allow ${ENV_VAR_NAME}-style environment
 * variable inclusions
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/PropertiesFileConfigurationProvider.java`
#### Snippet
```java
 * A configuration provider that uses properties file for specifying
 * configuration. The configuration files follow the Java properties file syntax
 * rules specified at {@link java.util.Properties#load(java.io.Reader)}. Every
 * configuration value specified in the properties file is prefixed by an
 * <em>Agent Name</em> which helps isolate an individual agent&apos;s namespace.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/PropertiesFileConfigurationProvider.java`
#### Snippet
```java
 * </p>
 *
 * @see java.util.Properties#load(java.io.Reader)
 */
public class PropertiesFileConfigurationProvider extends
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time.format` is unnecessary and can be removed
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/HdfsSinkConfig.java`
#### Snippet
```java
     * The pattern is the formatted pattern of {@link AbstractHdfsConfig#getDirectory()}'s subdirectory.
     *
     * @see java.time.format.DateTimeFormatter for pattern's syntax
     */
    private String subdirectoryPattern;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.generic` is unnecessary, and can be replaced with an import
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
                return jsonKeyValue;
            case AVRO:
                return JsonConverter.toJson((org.apache.avro.generic.GenericRecord) val);
            case JSON:
                return val;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.api` is unnecessary and can be removed
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
        int[] keysOffsets = new int[ctx.getKeys().size()];
        int keyIndex = 0;
        for (Entry<String, org.apache.pulsar.common.api.EncryptionContext.EncryptionKey> entry : ctx.getKeys()
                .entrySet()) {
            int key = builder.createString(entry.getKey());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.io.kinesis.fbs` is unnecessary, and can be replaced with an import
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
        switch (ctx.getCompressionType()) {
            case LZ4:
                compressionType = org.apache.pulsar.io.kinesis.fbs.CompressionType.LZ4;
                break;
        case ZLIB:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.io.kinesis.fbs` is unnecessary, and can be replaced with an import
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
                break;
        case ZLIB:
            compressionType = org.apache.pulsar.io.kinesis.fbs.CompressionType.ZLIB;
            break;
        default:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.io.kinesis.fbs` is unnecessary, and can be replaced with an import
in `pulsar-io/kinesis/src/main/java/org/apache/pulsar/io/kinesis/Utils.java`
#### Snippet
```java
            break;
        default:
            compressionType = org.apache.pulsar.io.kinesis.fbs.CompressionType.NONE;

        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.reflect` is unnecessary, and can be replaced with an import
in `pulsar-io/data-generator/src/main/java/org/apache/pulsar/io/datagenerator/Person.java`
#### Snippet
```java
    private Sex sex;
    private String telephoneNumber;
    @org.apache.avro.reflect.AvroSchema("{ \"type\": \"long\", \"logicalType\": \"timestamp-millis\" }")
    private long dateOfBirth;
    private Integer age;
```

### UnnecessaryFullyQualifiedName
Qualifier `com.rabbitmq.client` is unnecessary, and can be replaced with an import
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSource.java`
#### Snippet
```java
        );
        rabbitMQChannel.basicQos(rabbitMQSourceConfig.getPrefetchCount(), rabbitMQSourceConfig.isPrefetchGlobal());
        com.rabbitmq.client.Consumer consumer = new RabbitMQConsumer(this, rabbitMQChannel);
        rabbitMQChannel.basicConsume(rabbitMQSourceConfig.getQueueName(), consumer);
        logger.info("A consumer for queue {} has been successfully started.", rabbitMQSourceConfig.getQueueName());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.time.format` is unnecessary and can be removed
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/IndexNameFormatter.java`
#### Snippet
```java
 * The formatter finds the pattern %{+<date-format>} in the format string
 * and replace them with strings that are formatted from the event time of the record
 * using <date-format>. The format follows the rules of {@link java.time.format.DateTimeFormatter}.
 *
 * For example, suppose the event time of the record is 1645182000000L, the indexName
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.reflect` is unnecessary, and can be replaced with an import
in `pulsar-io/batch-data-generator/src/main/java/org/apache/pulsar/io/batchdatagenerator/Person.java`
#### Snippet
```java
    private Sex sex;
    private String telephoneNumber;
    @org.apache.avro.reflect.AvroSchema("""
            { "type": "long", "logicalType": "timestamp-millis" }""")
    private long dateOfBirth;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensearch.client` is unnecessary, and can be replaced with an import
in `pulsar-io/elastic-search/src/main/java/org/apache/pulsar/io/elasticsearch/client/opensearch/OpenSearchHighLevelRestClient.java`
#### Snippet
```java
                .setFailureListener(new org.opensearch.client.RestClient.FailureListener() {
                    @Override
                    public void onFailure(org.opensearch.client.Node node) {
                        log.warn("Node host={} failed", node.getHost());
                    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.math` is unnecessary, and can be replaced with an import
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
                return Time.toLogical(kafkaSchema, ((Number) nativeObject).intValue());
            } else if (Decimal.LOGICAL_NAME.equals(kafkaSchema.name())) {
                if (nativeObject instanceof java.math.BigDecimal) {
                    return nativeObject;
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary, and can be replaced with an import
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
                    }
                    return responseMap;
                } else if (nativeObject instanceof org.apache.pulsar.common.schema.KeyValue) {
                    org.apache.pulsar.common.schema.KeyValue kv =
                            (org.apache.pulsar.common.schema.KeyValue) nativeObject;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary, and can be replaced with an import
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
                    return responseMap;
                } else if (nativeObject instanceof org.apache.pulsar.common.schema.KeyValue) {
                    org.apache.pulsar.common.schema.KeyValue kv =
                            (org.apache.pulsar.common.schema.KeyValue) nativeObject;
                    Map<Object, Object> responseMap = new HashMap<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary, and can be replaced with an import
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/schema/KafkaConnectData.java`
#### Snippet
```java
                } else if (nativeObject instanceof org.apache.pulsar.common.schema.KeyValue) {
                    org.apache.pulsar.common.schema.KeyValue kv =
                            (org.apache.pulsar.common.schema.KeyValue) nativeObject;
                    Map<Object, Object> responseMap = new HashMap<>();
                    Object key = getKafkaConnectData(kv.getKey(), kafkaSchema.keySchema());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.zookeeper` is unnecessary and can be removed
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeperSession.java`
#### Snippet
```java

    @Override
    public List<OpResult> multi(Iterable<org.apache.zookeeper.Op> ops) throws InterruptedException, KeeperException {
        return mockZooKeeper.multi(ops);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.zookeeper` is unnecessary and can be removed
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeperSession.java`
#### Snippet
```java

    @Override
    public void multi(Iterable<org.apache.zookeeper.Op> ops, AsyncCallback.MultiCallback cb, Object ctx) {
        mockZooKeeper.multi(ops, cb, ctx);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.function` is unnecessary, and can be replaced with an import
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/KafkaConnectSink.java`
#### Snippet
```java
    protected void ackUntil(Record<GenericObject> lastNotFlushed,
                          Map<TopicPartition, OffsetAndMetadata> committedOffsets,
                          java.util.function.Consumer<Record<GenericObject>> cb) {
        // lastNotFlushed is needed in case of default preCommit() implementation
        // which calls flush() and returns currentOffsets passed to it.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.sql.presto` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarColumnHandle.java`
#### Snippet
```java

    /**
     * {@link org.apache.pulsar.sql.presto.PulsarColumnMetadata.DecoderExtraInfo#mapping}.
     */
    private String mapping;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.sql.presto` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarColumnHandle.java`
#### Snippet
```java

    /**
     * {@link org.apache.pulsar.sql.presto.PulsarColumnMetadata.DecoderExtraInfo#formatHint}.
     */
    private String formatHint;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.sql.presto` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarColumnHandle.java`
#### Snippet
```java
    private String mapping;
    /**
     * {@link org.apache.pulsar.sql.presto.PulsarColumnMetadata.DecoderExtraInfo#dataFormat}.
     */
    private String dataFormat;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarDispatchingRowDecoderFactory.java`
#### Snippet
```java

/**
 * dispatcher RowDecoderFactory for {@link org.apache.pulsar.common.schema.SchemaType}.
 */
public class PulsarDispatchingRowDecoderFactory {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.sql.presto` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarColumnMetadata.java`
#### Snippet
```java

    /**
     * Decoder extra info for {@link org.apache.pulsar.sql.presto.PulsarColumnHandle}
     * used by {@link io.trino.decoder.RowDecoder}.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
 * Copy from {@link io.trino.decoder.avro.AvroColumnDecoder} (presto-record-decoder-345)
 * with A little bit pulsar's extensions.
 * 1) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType}DATE,
 *  * {@link io.trino.spi.type.TimeType}.
 * 2) support {@link io.trino.spi.type.RealType}.
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
 * Copy from {@link io.trino.decoder.avro.AvroColumnDecoder} (presto-record-decoder-345)
 * with A little bit pulsar's extensions.
 * 1) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType}DATE,
 *  * {@link io.trino.spi.type.TimeType}.
 * 2) support {@link io.trino.spi.type.RealType}.
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
 * with A little bit pulsar's extensions.
 * 1) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType}DATE,
 *  * {@link io.trino.spi.type.TimeType}.
 * 2) support {@link io.trino.spi.type.RealType}.
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroColumnDecoder.java`
#### Snippet
```java
 * 1) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType}DATE,
 *  * {@link io.trino.spi.type.TimeType}.
 * 2) support {@link io.trino.spi.type.RealType}.
 */
public class PulsarAvroColumnDecoder {
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
 * Copy from {@link io.trino.decoder.json.DefaultJsonFieldDecoder} (presto-record-decoder-345)
 * with some pulsar's extensions.
 * 1) support {@link io.trino.spi.type.ArrayType}.
 * 2) support {@link io.trino.spi.type.MapType}.
 * 3) support {@link io.trino.spi.type.RowType}.
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
 * with some pulsar's extensions.
 * 1) support {@link io.trino.spi.type.ArrayType}.
 * 2) support {@link io.trino.spi.type.MapType}.
 * 3) support {@link io.trino.spi.type.RowType}.
 * 4) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType},
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
 * 1) support {@link io.trino.spi.type.ArrayType}.
 * 2) support {@link io.trino.spi.type.MapType}.
 * 3) support {@link io.trino.spi.type.RowType}.
 * 4) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType},
 * {@link io.trino.spi.type.TimeType}.
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
 * 2) support {@link io.trino.spi.type.MapType}.
 * 3) support {@link io.trino.spi.type.RowType}.
 * 4) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType},
 * {@link io.trino.spi.type.TimeType}.
 * 5) support {@link io.trino.spi.type.RealType}.
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
 * 2) support {@link io.trino.spi.type.MapType}.
 * 3) support {@link io.trino.spi.type.RowType}.
 * 4) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType},
 * {@link io.trino.spi.type.TimeType}.
 * 5) support {@link io.trino.spi.type.RealType}.
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
 * 3) support {@link io.trino.spi.type.RowType}.
 * 4) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType},
 * {@link io.trino.spi.type.TimeType}.
 * 5) support {@link io.trino.spi.type.RealType}.
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `io.trino.spi.type` is unnecessary and can be removed
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/json/PulsarJsonFieldDecoder.java`
#### Snippet
```java
 * 4) support {@link io.trino.spi.type.TimestampType},{@link io.trino.spi.type.DateType},
 * {@link io.trino.spi.type.TimeType}.
 * 5) support {@link io.trino.spi.type.RealType}.
 */
public class PulsarJsonFieldDecoder
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.common.collect` is unnecessary, and can be replaced with an import
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarSplitManager.java`
#### Snippet
```java
                            com.google.common.collect.Range<PositionImpl> posRange =
                                com.google.common.collect.Range.range(overallStartPos,
                                    com.google.common.collect.BoundType.CLOSED,
                                    overallEndPos, com.google.common.collect.BoundType.CLOSED);

```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.common.collect` is unnecessary, and can be replaced with an import
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarSplitManager.java`
#### Snippet
```java
                                com.google.common.collect.Range.range(overallStartPos,
                                    com.google.common.collect.BoundType.CLOSED,
                                    overallEndPos, com.google.common.collect.BoundType.CLOSED);

                            long numOfEntries = readOnlyCursor.getNumberOfEntries(posRange) - 1;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.server` is unnecessary, and can be replaced with an import
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarBrokerStarter.java`
#### Snippet
```java
                Objects.requireNonNull(bookieConfig, "No ServerConfiguration for Bookie");
                Objects.requireNonNull(bookieStatsProvider, "No Stats Provider for Bookie");
                bookieServer = org.apache.bookkeeper.server.Main
                        .buildBookieServer(new BookieConfiguration(bookieConfig));
            } else {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java

        @Parameter(description = "The token string", arity = 1)
        private java.util.List<String> args;

        @Parameter(names = {"-i",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-broker/src/main/java/org/apache/pulsar/utils/auth/tokens/TokensCliUtils.java`
#### Snippet
```java

        @Parameter(description = "The token string", arity = 1)
        private java.util.List<String> args;

        @Parameter(names = {"-i",
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.stats` is unnecessary and can be removed
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/WorkerStats.java`
#### Snippet
```java
            value = "Gets the metrics for Monitoring",
            notes = "Request should be executed by Monitoring agent on each worker to fetch the worker-metrics",
            response = org.apache.pulsar.common.stats.Metrics.class,
            responseContainer = "List")
    @ApiResponses(value = {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/MessageDeduplication.java`
#### Snippet
```java
                .toMillis(pulsar.getConfiguration().getBrokerDeduplicationProducerInactivityTimeoutMinutes());

        Iterator<java.util.Map.Entry<String, Long>> mapIterator = inactiveProducers.entrySet().iterator();
        boolean hasInactive = false;
        while (mapIterator.hasNext()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/persistent/MessageDeduplication.java`
#### Snippet
```java
        boolean hasInactive = false;
        while (mapIterator.hasNext()) {
            java.util.Map.Entry<String, Long> entry = mapIterator.next();
            String producerName = entry.getKey();
            long lastActiveTimestamp = entry.getValue();
```

### UnnecessaryFullyQualifiedName
Qualifier `com.github.zafarkhaja.semver` is unnecessary and can be removed
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/BrokerVersionFilter.java`
#### Snippet
```java
        }

        com.github.zafarkhaja.semver.Version latestVersion = null;
        try {
            latestVersion = getLatestVersionNumber(brokers, loadData);
```

### UnnecessaryFullyQualifiedName
Qualifier `com.github.zafarkhaja.semver` is unnecessary and can be removed
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/loadbalance/impl/BrokerVersionFilter.java`
#### Snippet
```java
            BrokerData data = loadData.getBrokerData().get(broker);
            String brokerVersion = data.getLocalData().getBrokerVersionString();
            com.github.zafarkhaja.semver.Version brokerVersionVersion = Version.valueOf(brokerVersion);

            if (brokerVersionVersion.equals(latestVersion)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.broker` is unnecessary and can be removed
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/namespace/NamespaceService.java`
#### Snippet
```java
 * <p/>
 *
 * @see org.apache.pulsar.broker.PulsarService
 */
public class NamespaceService implements AutoCloseable {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.broker.resourcegroup` is unnecessary and can be removed
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroupNamespaceConfigListener.java`
#### Snippet
```java
    public void reconcileNamespaceResourceGroup(NamespaceName ns, Policies policy) {
        boolean delete = false, add = false;
        org.apache.pulsar.broker.resourcegroup.ResourceGroup current = rgService
                .getNamespaceResourceGroup(ns);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.server` is unnecessary, and can be replaced with an import
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java

        try {
            bookieComponents[i] = org.apache.bookkeeper.server.Main
                    .buildBookieServer(new BookieConfiguration(bsConfs[i]));
            ComponentStarter.startComponent(bookieComponents[i]);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.server` is unnecessary, and can be replaced with an import
in `pulsar-broker/src/main/java/org/apache/pulsar/zookeeper/LocalBookkeeperEnsemble.java`
#### Snippet
```java
            FileUtils.deleteDirectory(bkDataDir);

            bookieComponents[i] = org.apache.bookkeeper.server.Main
                    .buildBookieServer(new BookieConfiguration(bsConfs[i]));
            ComponentStarter.startComponent(bookieComponents[i]);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.broker.transaction.pendingack.impl` is unnecessary and can be removed
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/pendingack/impl/PendingAckHandleImpl.java`
#### Snippet
```java
        this.pendingAckStoreFuture.thenAccept(pendingAckStore ->
                pendingAckStore.appendIndividualAck(txnID, positions).thenAccept(v -> {
                    synchronized (org.apache.pulsar.broker.transaction.pendingack.impl.PendingAckHandleImpl.this) {
                        for (MutablePair<PositionImpl, Integer> positionIntegerMutablePair : positions) {

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TypedMessageBuilderImpl.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    private <K, V> void setSeparateKeyValue(T value, KeyValueSchema<K, V> keyValueSchema) {
        checkArgument(value instanceof org.apache.pulsar.common.schema.KeyValue);
        org.apache.pulsar.common.schema.KeyValue<K, V> keyValue =
                (org.apache.pulsar.common.schema.KeyValue<K, V>) value;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TypedMessageBuilderImpl.java`
#### Snippet
```java
    private <K, V> void setSeparateKeyValue(T value, KeyValueSchema<K, V> keyValueSchema) {
        checkArgument(value instanceof org.apache.pulsar.common.schema.KeyValue);
        org.apache.pulsar.common.schema.KeyValue<K, V> keyValue =
                (org.apache.pulsar.common.schema.KeyValue<K, V>) value;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.schema` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TypedMessageBuilderImpl.java`
#### Snippet
```java
        checkArgument(value instanceof org.apache.pulsar.common.schema.KeyValue);
        org.apache.pulsar.common.schema.KeyValue<K, V> keyValue =
                (org.apache.pulsar.common.schema.KeyValue<K, V>) value;

        // set key as the message key
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImplementationBindingImpl.java`
#### Snippet
```java
    }

    public <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> newProtobufSchema(
            SchemaDefinition schemaDefinition) {
        return ProtobufSchema.of(schemaDefinition);
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarClientImplementationBindingImpl.java`
#### Snippet
```java
    }

    public <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> newProtobufNativeSchema(
            SchemaDefinition schemaDefinition) {
        return ProtobufNativeSchema.of(schemaDefinition);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/ConsumerInterceptors.java`
#### Snippet
```java

/**
 * A container that hold the list {@link org.apache.pulsar.client.api.ConsumerInterceptor} and wraps calls to the chain
 * of custom interceptors.
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.internal` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/PulsarServiceNameResolver.java`
#### Snippet
```java
        return numAddresses == 1
                ?
                0 : io.netty.util.internal.PlatformDependent.threadLocalRandom().nextInt(numAddresses);
    }
}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api.schema` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/SchemaDefinitionImpl.java`
#### Snippet
```java
/**
 * A json schema definition
 * {@link org.apache.pulsar.client.api.schema.SchemaDefinition} for the json schema definition.
 */
public class SchemaDefinitionImpl<T> implements SchemaDefinition<T> {
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufSchema.java`
#### Snippet
```java
    }

    public static <T extends com.google.protobuf.GeneratedMessageV3> ProtobufSchema<T> of(Class<T> pojo) {
        return of(pojo, new HashMap<>());
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufSchema.java`
#### Snippet
```java
        Class<T> pojo = schemaDefinition.getPojo();

        if (!com.google.protobuf.GeneratedMessageV3.class.isAssignableFrom(pojo)) {
            throw new IllegalArgumentException(com.google.protobuf.GeneratedMessageV3.class.getName()
                    + " is not assignable from " + pojo.getName());
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufSchema.java`
#### Snippet
```java

        if (!com.google.protobuf.GeneratedMessageV3.class.isAssignableFrom(pojo)) {
            throw new IllegalArgumentException(com.google.protobuf.GeneratedMessageV3.class.getName()
                    + " is not assignable from " + pojo.getName());
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufSchema.java`
#### Snippet
```java
    }

    private static <T> org.apache.avro.Schema createProtobufAvroSchema(Class<T> pojo) {
        return ProtobufData.get().getSchema(pojo);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufSchema.java`
#### Snippet
```java
    }

    public static <T extends com.google.protobuf.GeneratedMessageV3> ProtobufSchema<T> of(
            Class pojo, Map<String, String> properties){
        return ofGenericClass(pojo, properties);
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufSchema.java`
#### Snippet
```java
 * A schema implementation to deal with protobuf generated messages.
 */
public class ProtobufSchema<T extends com.google.protobuf.GeneratedMessageV3> extends AvroBaseStructSchema<T> {

    public static final String PARSING_INFO_PROPERTY = "__PARSING_INFO__";
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/RecordSchemaBuilderImpl.java`
#### Snippet
```java
        }

        org.apache.avro.Schema baseSchema = org.apache.avro.Schema.createRecord(
            schemaName != null ? schemaName : DEFAULT_SCHEMA_NAME,
            doc,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/RecordSchemaBuilderImpl.java`
#### Snippet
```java
        }

        org.apache.avro.Schema baseSchema = org.apache.avro.Schema.createRecord(
            schemaName != null ? schemaName : DEFAULT_SCHEMA_NAME,
            doc,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/RecordSchemaBuilderImpl.java`
#### Snippet
```java
        );

        List<org.apache.avro.Schema.Field> avroFields = new ArrayList<>();
        for (FieldSchemaBuilderImpl field : fields) {
            avroFields.add(field.build());
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java
/**
 * Protobuf-Native schema util used for serialize/deserialize
 * between {@link com.google.protobuf.Descriptors.Descriptor} and
 * {@link org.apache.pulsar.common.protocol.schema.ProtobufNativeSchemaData}.
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.protocol.schema` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java
 * Protobuf-Native schema util used for serialize/deserialize
 * between {@link com.google.protobuf.Descriptors.Descriptor} and
 * {@link org.apache.pulsar.common.protocol.schema.ProtobufNativeSchemaData}.
 */
public class ProtobufNativeSchemaUtils {
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.buffer` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ByteBufSchema.java`
#### Snippet
```java

/**
 * A variant `Bytes` schema that takes {@link io.netty.buffer.ByteBuf}.
 */
public class ByteBufSchema extends AbstractSchema<ByteBuf> {
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/reader/ProtobufReader.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class ProtobufReader<T extends com.google.protobuf.GeneratedMessageV3> implements SchemaReader<T> {
    private Parser<T> tParser;

```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/reader/ProtobufNativeReader.java`
#### Snippet
```java
package org.apache.pulsar.client.impl.schema.reader;

public class ProtobufNativeReader<T extends com.google.protobuf.GeneratedMessageV3> extends ProtobufReader<T> {

    public ProtobufNativeReader(T protoMessageInstance) {
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/writer/ProtobufNativeWriter.java`
#### Snippet
```java
package org.apache.pulsar.client.impl.schema.writer;

public class ProtobufNativeWriter<T extends com.google.protobuf.GeneratedMessageV3> extends ProtobufWriter<T> {

    public ProtobufNativeWriter() {
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/writer/ProtobufWriter.java`
#### Snippet
```java
import org.apache.pulsar.client.api.schema.SchemaWriter;

public class ProtobufWriter<T extends com.google.protobuf.GeneratedMessageV3> implements SchemaWriter<T> {

    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroSchema.java`
#### Snippet
```java

    @Override
    public org.apache.pulsar.client.api.Schema<GenericRecord> clone() {
        org.apache.pulsar.client.api.Schema<GenericRecord> schema =
                GenericAvroSchema.of(schemaInfo,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroSchema.java`
#### Snippet
```java
    @Override
    public org.apache.pulsar.client.api.Schema<GenericRecord> clone() {
        org.apache.pulsar.client.api.Schema<GenericRecord> schema =
                GenericAvroSchema.of(schemaInfo,
                        ((AbstractMultiVersionGenericReader) reader).useProvidedSchemaAsReaderSchema);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api.schema` is unnecessary and can be removed
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/AvroRecordBuilderImpl.java`
#### Snippet
```java

/**
 * Builder to build {@link org.apache.pulsar.client.api.schema.GenericRecord}.
 */
class AvroRecordBuilderImpl implements GenericRecordBuilder {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.generic` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java
    }

    public org.apache.avro.generic.GenericRecord getAvroRecord() {
        return record;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java

    public GenericAvroRecord(byte[] schemaVersion,
                      org.apache.avro.Schema schema,
                      List<Field> fields,
                      org.apache.avro.generic.GenericRecord record) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.generic` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java
                      org.apache.avro.Schema schema,
                      List<Field> fields,
                      org.apache.avro.generic.GenericRecord record) {
        super(schemaVersion, fields);
        this.schema = schema;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java
public class GenericAvroRecord extends VersionedGenericRecord {

    private final org.apache.avro.Schema schema;
    private final org.apache.avro.generic.GenericRecord record;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.generic` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java

    private final org.apache.avro.Schema schema;
    private final org.apache.avro.generic.GenericRecord record;

    public GenericAvroRecord(byte[] schemaVersion,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.generic` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java
        if (value instanceof Utf8) {
            return ((Utf8) value).toString();
        } else if (value instanceof org.apache.avro.generic.GenericRecord) {
            org.apache.avro.generic.GenericRecord avroRecord =
                (org.apache.avro.generic.GenericRecord) value;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.generic` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java
            return ((Utf8) value).toString();
        } else if (value instanceof org.apache.avro.generic.GenericRecord) {
            org.apache.avro.generic.GenericRecord avroRecord =
                (org.apache.avro.generic.GenericRecord) value;
            org.apache.avro.Schema recordSchema = avroRecord.getSchema();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro.generic` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java
        } else if (value instanceof org.apache.avro.generic.GenericRecord) {
            org.apache.avro.generic.GenericRecord avroRecord =
                (org.apache.avro.generic.GenericRecord) value;
            org.apache.avro.Schema recordSchema = avroRecord.getSchema();
            List<Field> fields = recordSchema.getFields()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericAvroRecord.java`
#### Snippet
```java
            org.apache.avro.generic.GenericRecord avroRecord =
                (org.apache.avro.generic.GenericRecord) value;
            org.apache.avro.Schema recordSchema = avroRecord.getSchema();
            List<Field> fields = recordSchema.getFields()
                .stream()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericJsonRecord.java`
#### Snippet
```java
    }

    private static org.apache.avro.Schema parseAvroSchema(String schemaJson) {
        final org.apache.avro.Schema.Parser parser = new org.apache.avro.Schema.Parser();
        parser.setValidateDefaults(false);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericJsonRecord.java`
#### Snippet
```java

    private static org.apache.avro.Schema parseAvroSchema(String schemaJson) {
        final org.apache.avro.Schema.Parser parser = new org.apache.avro.Schema.Parser();
        parser.setValidateDefaults(false);
        return parser.parse(schemaJson);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericJsonRecord.java`
#### Snippet
```java

    private static org.apache.avro.Schema parseAvroSchema(String schemaJson) {
        final org.apache.avro.Schema.Parser parser = new org.apache.avro.Schema.Parser();
        parser.setValidateDefaults(false);
        return parser.parse(schemaJson);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericJsonRecord.java`
#### Snippet
```java
        boolean isBinary = false;
        try {
            org.apache.avro.Schema schema = parseAvroSchema(schemaInfo.getSchemaDefinition());
            org.apache.avro.Schema.Field field = schema.getField(fieldName);
            if (field == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.avro` is unnecessary, and can be replaced with an import
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/generic/GenericJsonRecord.java`
#### Snippet
```java
        try {
            org.apache.avro.Schema schema = parseAvroSchema(schemaInfo.getSchemaDefinition());
            org.apache.avro.Schema.Field field = schema.getField(fieldName);
            if (field == null) {
                return false;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarClassLoader.java`
#### Snippet
```java
 *
 * <p>
 * <tt>NarClassLoader</tt> follows the delegation model described in {@link ClassLoader#findClass(java.lang.String)
 * ClassLoader.findClass(...)}; classes are first loaded from the parent <tt>ClassLoader</tt>, and only if they cannot
 * be found there does the <tt>NarClassLoader</tt> provide a definition. Specifically, this means that resources are
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.tls` is unnecessary and can be removed
in `pulsar-common/src/main/java/org/apache/pulsar/common/tls/PublicSuffixMatcher.java`
#### Snippet
```java
 * <a href="http://publicsuffix.org/">publicsuffix.org</a>
 *
 * @see org.apache.pulsar.common.tls.PublicSuffixList
 *
 * @since 4.4
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/KeyStoreHolder.java`
#### Snippet
```java
 * Holder for the secure key store.
 *
 * @see java.security.KeyStore
 */
public class KeyStoreHolder {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/ClassLoaderUtils.java`
#### Snippet
```java
     */
    public static ClassLoader loadJar(File jar) throws MalformedURLException {
        java.net.URL url = jar.toURI().toURL();
        return AccessController.doPrivileged(
            (PrivilegedAction<URLClassLoader>) () -> new URLClassLoader(new URL[]{url}));
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.internal` is unnecessary, and can be replaced with an import
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
    public static Double stringToDouble(String val) {
        String v = trim(val);
        if (io.netty.util.internal.StringUtil.isNullOrEmpty(v)) {
            return null;
        } else {
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.internal` is unnecessary, and can be replaced with an import
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java
    public static Integer stringToInteger(String val) {
        String v = trim(val);
        if (io.netty.util.internal.StringUtil.isNullOrEmpty(v)) {
            return null;
        } else {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
     * @return true if class can be loaded from jar and false if otherwise
     */
    public static boolean classExistsInJar(java.io.File jar, String fqcn) {
        java.net.URLClassLoader loader = null;
        try {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
     */
    public static boolean classExistsInJar(java.io.File jar, String fqcn) {
        java.net.URLClassLoader loader = null;
        try {
            loader = (URLClassLoader) ClassLoaderUtils.loadJar(jar);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
    }

    public static Object createInstance(String userClassName, java.io.File jar) {
        try {
            return createInstance(userClassName, ClassLoaderUtils.loadJar(jar));
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
     * @return true if class from jar implements interface xface and false if otherwise
     */
    public static boolean classInJarImplementsIface(java.io.File jar, String fqcn, Class xface) {
        boolean ret = false;
        java.net.URLClassLoader loader = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/Reflections.java`
#### Snippet
```java
    public static boolean classInJarImplementsIface(java.io.File jar, String fqcn, Class xface) {
        boolean ret = false;
        java.net.URLClassLoader loader = null;
        try {
            loader = (URLClassLoader) ClassLoaderUtils.loadJar(jar);
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.internal` is unnecessary, and can be replaced with an import
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowablePriorityLongPairQueue.java`
#### Snippet
```java
    public GrowablePriorityLongPairQueue(int initialCapacity) {
        checkArgument(initialCapacity > 0);
        this.capacity = io.netty.util.internal.MathUtil.findNextPositivePowerOfTwo(initialCapacity);
        data = new long[2 * capacity];
        Arrays.fill(data, 0, data.length, EmptyItem);
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.internal` is unnecessary, and can be replaced with an import
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/GrowableArrayBlockingQueue.java`
#### Snippet
```java
        tailIndex.value = 0;

        int capacity = io.netty.util.internal.MathUtil.findNextPositivePowerOfTwo(initialCapacity);
        data = (T[]) new Object[capacity];
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/BitSetRecyclable.java`
#### Snippet
```java
 * Provides {@link BitSetRecyclable#resetWords(long[])} method and leverage with netty recycler.
 */
public class BitSetRecyclable implements Cloneable, java.io.Serializable {
    /*
     * BitSets are packed into arrays of "words."  Currently a word is
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.internal` is unnecessary, and can be replaced with an import
in `pulsar-common/src/main/java/org/apache/pulsar/common/stats/JvmMetrics.java`
#### Snippet
```java

    public static long getJvmDirectMemoryUsed() {
        long usedDirectMemory = io.netty.util.internal.PlatformDependent.usedDirectMemory();
        if (usedDirectMemory != -1L) {
            return usedDirectMemory;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/LedgerMetadataUtils.java`
#### Snippet
```java
     * @param name the name of the cursor
     * @return an immutable map which describes the cursor
     * @see #buildBaseManagedLedgerMetadata(java.lang.String)
     */
    static Map<String, byte[]> buildAdditionalMetadataForCursor(String name) {
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.internal` is unnecessary, and can be replaced with an import
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/AbstractCASReferenceCounted.java`
#### Snippet
```java
    @Override
    public boolean release(int decrement) {
        return release0(io.netty.util.internal.ObjectUtil.checkPositive(decrement, "decrement"));
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.internal` is unnecessary, and can be replaced with an import
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/util/AbstractCASReferenceCounted.java`
#### Snippet
```java
    @Override
    public ReferenceCounted retain(int increment) {
        return retain0(io.netty.util.internal.ObjectUtil.checkPositive(increment, "increment"));
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.common.util` is unnecessary, and can be replaced with an import
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/offload/OffloadUtils.java`
#### Snippet
```java
    public static CompletableFuture<Void> cleanupOffloaded(long ledgerId, UUID uuid, ManagedLedgerConfig mlConfig,
                                     Map<String, String> offloadDriverMetadata, String cleanupReason,
                                     String name, org.apache.bookkeeper.common.util.OrderedScheduler executor) {
        log.info("[{}] Cleanup offload for ledgerId {} uuid {} because of the reason {}.",
                name, ledgerId, uuid.toString(), cleanupReason);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.client.api` is unnecessary and can be removed
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/OffloadIndexBlockImpl.java`
#### Snippet
```java
            this.digestType = ledgerMetadataFormat.getDigestType();
            this.ctime = ledgerMetadataFormat.getCtime();
            this.state = org.apache.bookkeeper.client.api.LedgerMetadata.State.valueOf(
                    ledgerMetadataFormat.getState().toString());

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.server` is unnecessary, and can be replaced with an import
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/BKCluster.java`
#### Snippet
```java
            throws Exception {
        LifecycleComponentStack server =
                org.apache.bookkeeper.server.Main.buildBookieServer(new BookieConfiguration(conf));

        BookieId address = BookieImpl.getBookieId(conf);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java

        boolean isBuiltin = !StringUtils.isEmpty(sourceConfig.getArchive()) && sourceConfig.getArchive()
                .startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN);

        if (sourceConfig.getTenant() != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.commons.lang3` is unnecessary and can be removed
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java
        // Sink spec classname should be empty so that the default pulsar sink will be used
        Function.SinkSpec.Builder sinkSpecBuilder = Function.SinkSpec.newBuilder();
        if (!org.apache.commons.lang3.StringUtils.isEmpty(sourceConfig.getSchemaType())) {
            sinkSpecBuilder.setSchemaType(sourceConfig.getSchemaType());
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.commons.lang3` is unnecessary and can be removed
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java
            sinkSpecBuilder.setSchemaType(sourceConfig.getSchemaType());
        }
        if (!org.apache.commons.lang3.StringUtils.isEmpty(sourceConfig.getSerdeClassName())) {
            sinkSpecBuilder.setSerDeClassName(sourceConfig.getSerdeClassName());
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.commons.lang3` is unnecessary and can be removed
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SourceConfigUtils.java`
#### Snippet
```java
        functionDetailsBuilder.setResources(bldr);

        if (!org.apache.commons.lang3.StringUtils.isEmpty(sourceConfig.getRuntimeFlags())) {
            functionDetailsBuilder.setRuntimeFlags(sourceConfig.getRuntimeFlags());
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.common.base` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/ResourceConfigUtils.java`
#### Snippet
```java
        Long ram = resources.getRam();
        Long disk = resources.getDisk();
        com.google.common.base.Preconditions.checkArgument(cpu == null || cpu > 0.0,
                "The cpu allocation for the function must be positive");
        com.google.common.base.Preconditions.checkArgument(ram == null || ram > 0L,
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.common.base` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/ResourceConfigUtils.java`
#### Snippet
```java
        com.google.common.base.Preconditions.checkArgument(cpu == null || cpu > 0.0,
                "The cpu allocation for the function must be positive");
        com.google.common.base.Preconditions.checkArgument(ram == null || ram > 0L,
                "The ram allocation for the function must be positive");
        com.google.common.base.Preconditions.checkArgument(disk == null || disk > 0L,
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.common.base` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/ResourceConfigUtils.java`
#### Snippet
```java
        com.google.common.base.Preconditions.checkArgument(ram == null || ram > 0L,
                "The ram allocation for the function must be positive");
        com.google.common.base.Preconditions.checkArgument(disk == null || disk > 0L,
                "The disk allocation for the function must be positive");
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java
        boolean isBuiltin =
                !org.apache.commons.lang3.StringUtils.isEmpty(sinkConfig.getArchive()) && sinkConfig.getArchive()
                        .startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN);

        if (sinkConfig.getTenant() != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/SinkConfigUtils.java`
#### Snippet
```java

        if (!isEmpty(sinkConfig.getTransformFunction())
                && sinkConfig.getTransformFunction().startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
            functionDetailsBuilder.setBuiltin(sinkConfig.getTransformFunction().replaceFirst("^builtin://", ""));
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/io/ConnectorUtils.java`
#### Snippet
```java
        Class configClass = classLoader.loadClass(configClassName);
        for (Field field : Reflections.getAllFields(configClass)) {
            if (java.lang.reflect.Modifier.isStatic(field.getModifiers())) {
                // We dont want static fields
                continue;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.functions.api` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
            if (!org.apache.pulsar.functions.api.Function.class.isAssignableFrom(functionClass)
                    && !java.util.function.Function.class.isAssignableFrom(functionClass)
                    && !org.apache.pulsar.functions.api.WindowFunction.class.isAssignableFrom(functionClass)) {
                throw new IllegalArgumentException(
                        String.format("Function class %s does not implement the correct interface",
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
                                             boolean forwardSourceMessagePropertyEnabled) {
        if (StringUtils.isEmpty(functionConfig.getName())) {
            org.apache.pulsar.common.functions.Utils.inferMissingFunctionName(functionConfig);
        }
        if (StringUtils.isEmpty(functionConfig.getTenant())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        }
        if (StringUtils.isEmpty(functionConfig.getTenant())) {
            org.apache.pulsar.common.functions.Utils.inferMissingTenant(functionConfig);
        }
        if (StringUtils.isEmpty(functionConfig.getNamespace())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        }
        if (StringUtils.isEmpty(functionConfig.getNamespace())) {
            org.apache.pulsar.common.functions.Utils.inferMissingNamespace(functionConfig);
        }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java

        boolean isBuiltin = !StringUtils.isEmpty(functionConfig.getJar())
                && functionConfig.getJar().startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN);

        FunctionDetails.Builder functionDetailsBuilder = FunctionDetails.newBuilder();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
        }

        if (!isEmpty(functionConfig.getPy()) && !org.apache.pulsar.common.functions.Utils
                .isFunctionPackageUrlSupported(functionConfig.getPy())
                && functionConfig.getPy().startsWith(BUILTIN)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary, and can be replaced with an import
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionConfigUtils.java`
#### Snippet
```java
            }
        }
        if (!isEmpty(functionConfig.getGo()) && !org.apache.pulsar.common.functions.Utils
                .isFunctionPackageUrlSupported(functionConfig.getGo())
                && functionConfig.getGo().startsWith(BUILTIN)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.stats` is unnecessary, and can be replaced with an import
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/WorkerImpl.java`
#### Snippet
```java

    @Override
    public List<org.apache.pulsar.common.stats.Metrics> getWorkerMetrics(final String clientRole) {
        if (!isWorkerServiceAvailable() || worker().getMetricsGenerator() == null) {
            throwUnavailableException();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.policies.data` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImplV2.java`
#### Snippet
```java
                                              final String instanceId, URI uri, String clientRole) throws IOException {

        org.apache.pulsar.common.policies.data.FunctionStatus.FunctionInstanceStatus.FunctionInstanceStatusData
                functionInstanceStatus = delegate.getFunctionInstanceStatus(tenant, namespace,
                functionName, instanceId, uri, clientRole, null);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.policies.data` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImplV2.java`
#### Snippet
```java

    private InstanceCommunication.FunctionStatus toProto(
            org.apache.pulsar.common.policies.data.FunctionStatus.FunctionInstanceStatus.FunctionInstanceStatusData
                    functionInstanceStatus, String instanceId) {
        List<InstanceCommunication.FunctionStatus.ExceptionInformation> latestSysExceptions =
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.stats` is unnecessary, and can be replaced with an import
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/WorkerStatsApiV2Resource.java`
#### Snippet
```java
            value = "Gets the metrics for Monitoring",
            notes = "Request should be executed by Monitoring agent on each worker to fetch the worker-metrics",
            response = org.apache.pulsar.common.stats.Metrics.class,
            responseContainer = "List")
    @ApiResponses(value = {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.stats` is unnecessary, and can be replaced with an import
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/v2/WorkerStatsApiV2Resource.java`
#### Snippet
```java
    })
    @Produces(MediaType.APPLICATION_JSON)
    public List<org.apache.pulsar.common.stats.Metrics> getMetrics() throws Exception {
        return workers().getWorkerMetrics(clientAppId());
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
        sinkConfig.setNamespace(namespace);
        sinkConfig.setName(sinkName);
        org.apache.pulsar.common.functions.Utils.inferMissingArguments(sinkConfig);

        ClassLoader classLoader = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SinksImpl.java`
#### Snippet
```java
        if (!StringUtils.isEmpty(sinkConfig.getArchive())) {
            String archive = sinkConfig.getArchive();
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/FunctionsImpl.java`
#### Snippet
```java
        // check if function is builtin and extract classloader
        if (!StringUtils.isEmpty(archive)) {
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
        sourceConfig.setNamespace(namespace);
        sourceConfig.setName(sourceName);
        org.apache.pulsar.common.functions.Utils.inferMissingArguments(sourceConfig);

        ClassLoader classLoader = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/SourcesImpl.java`
#### Snippet
```java
        if (!StringUtils.isEmpty(sourceConfig.getArchive())) {
            String archive = sourceConfig.getArchive();
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
        }

        public void resetMetrics(com.google.protobuf.Empty request,
                                 io.grpc.stub.StreamObserver<com.google.protobuf.Empty> responseObserver) {
            Runtime runtime = runtimeSpawner.getRuntime();
```

### UnnecessaryFullyQualifiedName
Qualifier `io.grpc.stub` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

        public void resetMetrics(com.google.protobuf.Empty request,
                                 io.grpc.stub.StreamObserver<com.google.protobuf.Empty> responseObserver) {
            Runtime runtime = runtimeSpawner.getRuntime();
            if (runtime != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

        public void resetMetrics(com.google.protobuf.Empty request,
                                 io.grpc.stub.StreamObserver<com.google.protobuf.Empty> responseObserver) {
            Runtime runtime = runtimeSpawner.getRuntime();
            if (runtime != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
                try {
                    runtime.resetMetrics().get();
                    responseObserver.onNext(com.google.protobuf.Empty.getDefaultInstance());
                    responseObserver.onCompleted();
                } catch (InterruptedException | ExecutionException e) {
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

        @Override
        public void healthCheck(com.google.protobuf.Empty request,
                io.grpc.stub.StreamObserver<org.apache.pulsar.functions.proto.InstanceCommunication.HealthCheckResult>
                responseObserver) {
```

### UnnecessaryFullyQualifiedName
Qualifier `io.grpc.stub` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
        @Override
        public void healthCheck(com.google.protobuf.Empty request,
                io.grpc.stub.StreamObserver<org.apache.pulsar.functions.proto.InstanceCommunication.HealthCheckResult>
                responseObserver) {
            log.debug("Received health check request...");
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

        @Override
        public void getMetrics(com.google.protobuf.Empty request,
                   io.grpc.stub.StreamObserver<org.apache.pulsar.functions.proto.InstanceCommunication.MetricsData>
                   responseObserver) {
```

### UnnecessaryFullyQualifiedName
Qualifier `io.grpc.stub` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
        @Override
        public void getMetrics(com.google.protobuf.Empty request,
                   io.grpc.stub.StreamObserver<org.apache.pulsar.functions.proto.InstanceCommunication.MetricsData>
                   responseObserver) {
            Runtime runtime = runtimeSpawner.getRuntime();
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java

        @Override
        public void getAndResetMetrics(com.google.protobuf.Empty request,
                   io.grpc.stub.StreamObserver<org.apache.pulsar.functions.proto.InstanceCommunication.MetricsData>
                   responseObserver) {
```

### UnnecessaryFullyQualifiedName
Qualifier `io.grpc.stub` is unnecessary and can be removed
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/JavaInstanceStarter.java`
#### Snippet
```java
        @Override
        public void getAndResetMetrics(com.google.protobuf.Empty request,
                   io.grpc.stub.StreamObserver<org.apache.pulsar.functions.proto.InstanceCommunication.MetricsData>
                   responseObserver) {
            Runtime runtime = runtimeSpawner.getRuntime();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.clients.exceptions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
        } catch (RestException e) {
            throw e;
        } catch (org.apache.bookkeeper.clients.exceptions.NamespaceNotFoundException | StreamNotFoundException e) {
            log.debug("State not found while processing getFunctionState request @ /{}/{}/{}/{}",
                    tenant, namespace, functionName, key, e);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
    protected ClassLoader getBuiltinFunctionClassLoader(String archive) {
        if (!StringUtils.isEmpty(archive)) {
            if (archive.startsWith(org.apache.pulsar.common.functions.Utils.BUILTIN)) {
                archive = archive.replaceFirst("^builtin://", "");

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.clients.exceptions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
        try (Table<ByteBuf, ByteBuf> table = result(storageClient.get().openTable(tableName))) {
            result(table.put(Unpooled.wrappedBuffer(key.getBytes(UTF_8)), value));
        } catch (org.apache.bookkeeper.clients.exceptions.NamespaceNotFoundException
                | org.apache.bookkeeper.clients.exceptions.StreamNotFoundException e) {
            log.debug("State not found while processing putFunctionState request @ /{}/{}/{}/{}",
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.bookkeeper.clients.exceptions` is unnecessary and can be removed
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/rest/api/ComponentImpl.java`
#### Snippet
```java
            result(table.put(Unpooled.wrappedBuffer(key.getBytes(UTF_8)), value));
        } catch (org.apache.bookkeeper.clients.exceptions.NamespaceNotFoundException
                | org.apache.bookkeeper.clients.exceptions.StreamNotFoundException e) {
            log.debug("State not found while processing putFunctionState request @ /{}/{}/{}/{}",
                    tenant, namespace, functionName, key, e);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.util` is unnecessary, and can be replaced with an import
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/sink/PulsarSink.java`
#### Snippet
```java
            }
            try {
                org.apache.pulsar.common.util.FutureUtil.waitForAll(closeFutures);
            } catch (Exception e) {
                log.warn("Failed to close all the producers", e);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.functions.windowing` is unnecessary, and can be replaced with an import
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstanceRunnable.java`
#### Snippet
```java
        Object object;
        if (instanceConfig.getFunctionDetails().getClassName()
                .equals(org.apache.pulsar.functions.windowing.WindowFunctionExecutor.class.getName())) {
            object = Reflections.createInstance(
                    instanceConfig.getFunctionDetails().getClassName(),
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.function` is unnecessary, and can be replaced with an import
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/WindowDurationFunction.java`
#### Snippet
```java
 * This functions collects the timestamp during the window operation.
 */
public class WindowDurationFunction implements java.util.function.Function<Collection<String>, String> {
    @Override
    public String apply(Collection<String> integers) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.function` is unnecessary, and can be replaced with an import
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/CommaWindowFunction.java`
#### Snippet
```java
 * Comma based aggregation window function example.
 */
public class CommaWindowFunction implements java.util.function.Function<Collection<String>, String> {
    @Override
    public String apply(Collection<String> integers) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        }
        int cursorsInSameBacklogRange = 0;
        for (java.util.Map.Entry<ManagedCursorImpl, Long> cursor : cursorBacklogMap.entrySet()) {
            cursorsInSameBacklogRange = 0;
            for (java.util.Map.Entry<ManagedCursorImpl, Long> other : cursorBacklogMap.entrySet()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
        for (java.util.Map.Entry<ManagedCursorImpl, Long> cursor : cursorBacklogMap.entrySet()) {
            cursorsInSameBacklogRange = 0;
            for (java.util.Map.Entry<ManagedCursorImpl, Long> other : cursorBacklogMap.entrySet()) {
                if (cursor.equals(other)) {
                    continue;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java
            }
        }, 30000, 30000);
        java.lang.Runtime.getRuntime().addShutdownHook(new Thread(statusCheckTimer::cancel));
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Consumer.java`
#### Snippet
```java
     * @throws PulsarClientException.AlreadyClosedException
     *             if the consumer was already closed
     * @throws org.apache.pulsar.client.api.PulsarClientException.TransactionConflictException
     *             if the ack with messageId is less than the messageId in pending ack state or ack with transaction is
     *             different from the transaction in pending ack.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Consumer.java`
#### Snippet
```java
     *             if the ack with messageId is less than the messageId in pending ack state or ack with transaction is
     *             different from the transaction in pending ack.
     * @throws org.apache.pulsar.client.api.PulsarClientException.NotAllowedException
     *             broker don't support transaction
     * @return {@link CompletableFuture} the future of the ack result
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Consumer.java`
#### Snippet
```java
     * @throws PulsarClientException.AlreadyClosedException
     *             if the consumer was already closed
     * @throws org.apache.pulsar.client.api.PulsarClientException.TransactionConflictException
     *             if the ack with messageId is less than the messageId in pending ack state or ack with transaction is
     *             different from the transaction in pending ack.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Consumer.java`
#### Snippet
```java
     *             if the ack with messageId is less than the messageId in pending ack state or ack with transaction is
     *             different from the transaction in pending ack.
     * @throws org.apache.pulsar.client.api.PulsarClientException.NotAllowedException
     *             broker don't support transaction
     * @return {@link CompletableFuture} the future of the ack result     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Consumer.java`
#### Snippet
```java
     * @throws PulsarClientException.AlreadyClosedException
     *             if the consumer was already closed
     * @throws org.apache.pulsar.client.api.PulsarClientException.TransactionConflictException
     *             if the ack with messageId is less than the messageId in pending ack state or ack with transaction is
     *             different from the transaction in pending ack.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Consumer.java`
#### Snippet
```java
     *             if the ack with messageId is less than the messageId in pending ack state or ack with transaction is
     *             different from the transaction in pending ack.
     * @throws org.apache.pulsar.client.api.PulsarClientException.NotAllowedException
     *             broker don't support transaction
     * @return {@link CompletableFuture} the future of the ack result
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Consumer.java`
#### Snippet
```java
     * @throws PulsarClientException.AlreadyClosedException
     *             if the consumer was already closed
     * @throws org.apache.pulsar.client.api.PulsarClientException.TransactionConflictException
     *             if the ack with messageId has been acked by another transaction
     * @throws org.apache.pulsar.client.api.PulsarClientException.NotAllowedException
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.client.api` is unnecessary and can be removed
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Consumer.java`
#### Snippet
```java
     * @throws org.apache.pulsar.client.api.PulsarClientException.TransactionConflictException
     *             if the ack with messageId has been acked by another transaction
     * @throws org.apache.pulsar.client.api.PulsarClientException.NotAllowedException
     *             broker don't support transaction
     *             don't find batch size in consumer pending ack
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/internal/PulsarClientImplementationBinding.java`
#### Snippet
```java
    <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> newProtobufSchema(SchemaDefinition schemaDefinition);

    <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> newProtobufNativeSchema(
            SchemaDefinition schemaDefinition);

```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/internal/PulsarClientImplementationBinding.java`
#### Snippet
```java
    <T> Schema<T> newAvroSchema(SchemaDefinition schemaDefinition);

    <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> newProtobufSchema(SchemaDefinition schemaDefinition);

    <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> newProtobufNativeSchema(
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Schema.java`
#### Snippet
```java
     * @return a Schema instance
     */
    static <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> PROTOBUF_NATIVE(
            SchemaDefinition<T> schemaDefinition) {
        return DefaultImplementation.getDefaultImplementation().newProtobufNativeSchema(schemaDefinition);
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Schema.java`
#### Snippet
```java
     * @return a Schema instance
     */
    static <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> PROTOBUF(Class<T> clazz) {
        return DefaultImplementation.getDefaultImplementation()
                .newProtobufSchema(SchemaDefinition.builder().withPojo(clazz).build());
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Schema.java`
#### Snippet
```java
     * @return a Schema instance
     */
    static <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> PROTOBUF_NATIVE(Class<T> clazz) {
        return DefaultImplementation.getDefaultImplementation()
                .newProtobufNativeSchema(SchemaDefinition.builder().withPojo(clazz).build());
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.protobuf` is unnecessary, and can be replaced with an import
in `pulsar-client-api/src/main/java/org/apache/pulsar/client/api/Schema.java`
#### Snippet
```java
     * @return a Schema instance
     */
    static <T extends com.google.protobuf.GeneratedMessageV3> Schema<T> PROTOBUF(SchemaDefinition<T> schemaDefinition) {
        return DefaultImplementation.getDefaultImplementation().newProtobufSchema(schemaDefinition);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSinks.java`
#### Snippet
```java
            }

            org.apache.pulsar.common.functions.Utils.inferMissingArguments(sinkConfig);

            if (!Utils.isFunctionPackageUrlSupported(sinkConfig.getArchive())
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
    private class ExtractSchema extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-j", "--jar" }, description = "jar filepath", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
    private class DeleteSchema extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-f",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
    private class UploadSchema extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-f", "--filename" }, description = "filename", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSchemas.java`
#### Snippet
```java
    private class GetSchema extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-v", "--version"}, description = "version", required = false)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java

        @Parameter(names = { "--namespace", "-n" }, description = "tenant/namespace", required = true)
        private java.util.List<String> names;

        @Parameter(names = { "--bundle", "-b" }, description = "{start-boundary}_{end-boundary}", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java
        @Parameter(names = { "--namespace",
                "-n" }, description = "tenant/namespace, must be specified together with '--bundle'")
        private java.util.List<String> names;

        @Parameter(names = { "--bundle",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdResourceQuotas.java`
#### Snippet
```java
        @Parameter(names = { "--namespace",
                "-n" }, description = "tenant/namespace, must be specified together with '--bundle'")
        private java.util.List<String> names;

        @Parameter(names = { "--bundle",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdGenerateDocument.java`
#### Snippet
```java
                + "Optional modules(clusters, tenants, brokers, broker-stats, namespaces, topics, schemas, bookies,"
                + "functions, ns-isolation-policy, resource-quotas, functions, sources, sinks)")
        private java.util.List<String> modules;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdSources.java`
#### Snippet
```java
                throw new ParameterException("Source archive not specified");
            }
            org.apache.pulsar.common.functions.Utils.inferMissingArguments(sourceConfig);
            if (!Utils.isFunctionPackageUrlSupported(sourceConfig.getArchive())
                    && !sourceConfig.getArchive().startsWith(Utils.BUILTIN)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetDeduplicationStatus extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. ")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetMaxUnackedMessagesPerConsumer extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemovePublishRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. ")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveEntryFiltersPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveOffloadPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetMessageTTL extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-t", "--ttl" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetMaxUnackedMessagesPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-m", "--maxNum"},
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-t", "--type"}, description = "Backlog quota type to remove")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetMaxProducers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveAutoSubscriptionCreation extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetBacklogQuotaMap extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-ap", "--applied"}, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetDeduplicationSnapshotInterval extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-i", "--interval"}, description =
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveSubscribeRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. ")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetEntryFiltersPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemovePersistence extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetDeduplicationStatus extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable deduplication")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetPublishRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--msg-publish-rate", "-m"}, description = "message-publish-rate (default -1 will be "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetSubscribeRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-consumers-per-subscription", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetPublishRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveCompactionThreshold extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;
        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
                + "If set to true, the removing operation will be replicate to other clusters asynchronously")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable-delete-while-inactive", "-e" }, description = "Enable delete while inactive")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--max-subscriptions-per-topic",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetMaxProducers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--max-producers", "-p"}, description = "Max producers for a topic", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveMaxUnackedMessagesPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetAutoSubscriptionCreation extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--applied", "-a"}, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveDeduplicationStatus extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetMaxConsumers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-consumers", "-c" }, description = "Max consumers for a topic", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. ")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetAutoSubscriptionCreation extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--enable", "-e"}, description = "Enable allowAutoSubscriptionCreation on topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetOffloadPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetMaxConsumers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetCompactionThreshold extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--threshold", "-t" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetDeduplicationSnapshotInterval extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetOffloadPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-d", "--driver"}, description = "ManagedLedger offload driver", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetMaxUnackedMessagesPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetMaxMessageSize extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to get this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetSchemaCompatibilityStrategy extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-ap", "--applied"}, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable delayed delivery messages")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetEntryFiltersPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;


```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetPersistence extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-e",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveSchemaCompatibilityStrategy extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetPersistence extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to get this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetSchemaCompatibilityStrategy extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--strategy", "-s"}, description = "Schema compatibility strategy: [UNDEFINED, "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveMaxUnackedMessagesPerConsumer extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetMessageTTL extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--types", "-t"}, description = "Subscription types enabled list (comma separated values)."
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetSubscribeRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--subscribe-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveMaxMessageSize extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--global", "-g"}, description = "Whether to remove this policy globally. ")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetMaxUnackedMessagesPerConsumer extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-m", "--maxNum"}, description = "max unacked messages num on consumer", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class GetCompactionThreshold extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetBacklogQuota extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-l", "--limit" }, description = "Size limit (eg: 10M, 16G)")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveMaxProducers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to set this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class SetMaxMessageSize extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--max-message-size", "-m"}, description = "Max message size for a topic", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopicPolicies.java`
#### Snippet
```java
    private class RemoveMaxConsumers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--global", "-g" }, description = "Whether to remove this policy globally. "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNonPersistentTopics.java`
#### Snippet
```java
    private class GetList extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNonPersistentTopics.java`
#### Snippet
```java
    private class GetInternalStats extends CliCommand {
        @Parameter(description = "non-persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNonPersistentTopics.java`
#### Snippet
```java
    private class GetStats extends CliCommand {
        @Parameter(description = "non-persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNonPersistentTopics.java`
#### Snippet
```java

        @Parameter(description = "non-persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNonPersistentTopics.java`
#### Snippet
```java

        @Parameter(description = "non-persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-p",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNonPersistentTopics.java`
#### Snippet
```java
    private class GetListInBundle extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-b",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNonPersistentTopics.java`
#### Snippet
```java
    private class Lookup extends CliCommand {
        @Parameter(description = "non-persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdClusters.java`
#### Snippet
```java
        void run() throws PulsarAdminException {
            String cluster = getOneArgument(params);
            java.util.LinkedHashSet<String> clusters = StringUtils.isBlank(peerClusterNames) ? null
                    : Sets.newLinkedHashSet(Arrays.asList(peerClusterNames.split(",")));
            getAdmin().clusters().updatePeerClusterNames(cluster, clusters);
```

### UnnecessaryFullyQualifiedName
Qualifier `com.google.common.base` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CliCommand.java`
#### Snippet
```java
        String[] messageId = resetMessageIdStr.split(":");
        try {
            com.google.common.base.Preconditions.checkArgument(messageId.length == 2);
            return new MessageIdImpl(Long.parseLong(messageId[0]), Long.parseLong(messageId[1]), partitionIndex);
        } catch (Exception e) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GetInternalStats extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-m",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class TruncateCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic\n", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class PeekMessages extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class Skip extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GetLastMessageId extends CliCommand {
        @Parameter(description = "persistent://property/cluster/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
                required = false, splitter = NoSplitter.class)
        private java.util.List<String> properties;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-p",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GetInternalInfo extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-p",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class PartitionedTopicListCmd extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class ExpireMessages extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class Terminate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class ListCmd extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class CreateSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class ExpireMessagesForAllSubscriptions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-t", "--expireTime" }, description = "Expire messages older than time in seconds "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class DeleteCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = "--force",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GetStats extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-gpb", "--get-precise-backlog" }, description = "Set true to get precise backlog")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GetMessageById extends CliCommand {
        @Parameter(description = "persistent://property/cluster/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-l", "--ledgerId" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GrantPermissions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = "--role", description = "Client role to which grant permissions", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GetPartitionedStats extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = "--per-partition", description = "Get per partition stats")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class RevokePermissions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = "--role", description = "Client role to which revoke permissions", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class UnloadCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = "--force",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class DeleteSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-f",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GetPartitionedStatsInternal extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class Compact extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class GetBundleRange extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class CompactionStatusCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-w", "--wait-complete" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class Lookup extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class ListSubscriptions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class Permissions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class ResetCursor extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdPersistentTopics.java`
#### Snippet
```java
    private class SkipAll extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s", "--subscription" }, description = "Subscription to be cleared", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.annotation` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/JCommanderCompleter.java`
#### Snippet
```java
    }

    @Retention(java.lang.annotation.RetentionPolicy.RUNTIME)
    @Target({ FIELD })
    public @interface ParameterCompleter {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            }
            if (StringUtils.isEmpty(functionConfig.getName())) {
                org.apache.pulsar.common.functions.Utils.inferMissingFunctionName(functionConfig);
            }
            if (StringUtils.isEmpty(functionConfig.getName())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            }
            if (StringUtils.isEmpty(functionConfig.getTenant())) {
                org.apache.pulsar.common.functions.Utils.inferMissingTenant(functionConfig);
            }
            if (StringUtils.isEmpty(functionConfig.getNamespace())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            }
            if (StringUtils.isEmpty(functionConfig.getNamespace())) {
                org.apache.pulsar.common.functions.Utils.inferMissingNamespace(functionConfig);
            }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
        protected void validateFunctionConfigs(FunctionConfig functionConfig) {
            if (StringUtils.isEmpty(functionConfig.getName())) {
                org.apache.pulsar.common.functions.Utils.inferMissingFunctionName(functionConfig);
            }
            if (StringUtils.isEmpty(functionConfig.getName())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            }
            if (StringUtils.isEmpty(functionConfig.getTenant())) {
                org.apache.pulsar.common.functions.Utils.inferMissingTenant(functionConfig);
            }
            if (StringUtils.isEmpty(functionConfig.getNamespace())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.functions` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdFunctions.java`
#### Snippet
```java
            }
            if (StringUtils.isEmpty(functionConfig.getNamespace())) {
                org.apache.pulsar.common.functions.Utils.inferMissingNamespace(functionConfig);
            }
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--subscribe-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-subscriptions-per-topic", "-m" }, description = "Max subscriptions per topic",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetMessageTTL extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--messageTTL", "-ttl" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GrantSubscriptionPermissions extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-s", "--subscription"},
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetAntiAffinityGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--group", "-g" }, description = "Anti-affinity group name", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveResourceGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetSubscriptionExpirationTime extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetDestinations extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SubscriptionPermissions extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetBookieAffinityGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemovePublishRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetTopicHashPositions extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetBacklogQuotaMap extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(description = "tenant/namespace\n", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--key", "-k"}, description = "Key of the property", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetRetention extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetPublishRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

         @Parameter(names = { "--msg-publish-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetPersistence extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetSchemaCompatibilityStrategy extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--compatibility", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetMaxUnackedMessagesPerSubscription extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveAutoSubscriptionCreation extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetMaxTopicsPerNamespace extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetOffloadThreshold extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--size", "-s" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class ClearBacklog extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--sub", "-s" }, description = "subscription name")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class Unsubscribe extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--sub", "-s" }, description = "subscription name", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetEncryptionRequired extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GrantPermissions extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = "--role", description = "Client role to which grant permissions", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetEntryFiltersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--entry-filters-name", "-efn" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetIsAllowAutoUpdateSchema extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetCompactionThreshold extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--threshold", "-t" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveMaxConsumersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveMaxTopicsPerNamespace extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetResourceGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetMaxConsumersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetSchemaCompatibilityStrategy extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetMaxUnackedMessagesPerConsumer extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class Permissions extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetSchemaAutoUpdateStrategy extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--compatibility", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveSubscribeRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetEntryFiltersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class Create extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--clusters", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveDeduplicationSnapshotInterval extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetMessageTTL extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = {"--properties", "-p"}, description = "key value pair properties(a=a,b=b,c=c)",
                required = true)
        private java.util.List<String> properties;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveEntryFiltersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetSubscriptionAuthMode extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-m", "--subscription-auth-mode" }, description = "Subscription authorization mode for "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetOffloadPolicies extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(description = "tenant/namespace\n", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--key", "-k"}, description = "Key of the property", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveSubscriptionExpirationTime extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetOffloadDeletionLag extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--lag", "-l" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveAutoTopicCreation extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
        @Parameter(names = { "--clusters", "-c" },
                description = "List of clusters this namespace will be assigned", required = false)
        private java.util.List<String> clusters;

        @Parameter(names = { "--bundles", "-b" }, description = "number of bundles to activate", required = false)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveMaxUnackedMessagesPerSubscription extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetMaxConsumersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-consumers-per-topic", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class Unload extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--bundle", "-b" }, description = "{start-boundary}_{end-boundary}")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetDeduplicationSnapshotInterval extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--interval", "-i"}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SplitBundle extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--bundle",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetAntiAffinityGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-consumers-per-subscription", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetPersistence extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-e",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetOffloadThreshold extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class DeleteBookieAffinityGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RevokePermissions extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = "--role", description = "Client role to which revoke permissions", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveBacklogQuota extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-t", "--type"}, description = "Backlog quota type to remove. Valid options are: "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(description = "tenant/namespace\n", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--key", "-k"}, description = "Key of the property", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetMaxUnackedMessagesPerSubscription extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-unacked-messages-per-subscription", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetResourceGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--resource-group-name", "-rgn" }, description = "ResourceGroup name", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetSchemaAutoUpdateStrategy extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetNamespacesPerProperty extends CliCommand {
        @Parameter(description = "tenant-name", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetMaxProducersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-producers-per-topic", "-p" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class ClearOffloadDeletionLag extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable delayed delivery messages")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetOffloadDeletionLag extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveCompactionThreshold extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetNamespacesPerCluster extends CliCommand {
        @Parameter(description = "tenant/cluster", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetDeduplication extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable deduplication")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetSchemaValidationEnforced extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable schema validation enforced")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetDeduplicationSnapshotInterval extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(description = "tenant/namespace\n", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveRetention extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetDeduplication extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetSubscribeRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetBundles extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveDeduplication extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class DeleteAntiAffinityGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveMaxUnackedMessagesPerConsumer extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetMaxUnackedMessagesPerConsumer extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-unacked-messages-per-topic", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveOffloadPolicies extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--types", "-t"}, description = "Subscription types enabled list (comma separated values)."
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetCompactionThreshold extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetOffloadPolicies extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetRetention extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--time",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetPublishRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

         @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveMaxProducersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetEncryptionRequired extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable message encryption required")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetTopics extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-m", "--mode"},
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(description = "tenant/namespace\n", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetPolicies extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetBacklogQuota extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-l", "--limit" }, description = "Size limit (eg: 10M, 16G)", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java

        @Parameter(description = "tenant/namespace\n", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--properties", "-p"}, description = "key value pair properties(a=a,b=b,c=c)",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetMaxProducersPerTopic extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable-delete-while-inactive", "-e" }, description = "Enable delete while inactive")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class Delete extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-f",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetDispatchRate extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetBookieAffinityGroup extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--primary-group",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetSubscriptionExpirationTime extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-t", "--time" }, description = "Subscription expiration time in minutes", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemovePersistence extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RevokeSubscriptionPermissions extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-s", "--subscription"}, description = "Subscription name for which permission "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetAutoTopicCreation extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetAutoSubscriptionCreation extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable allowAutoSubscriptionCreation on namespace")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveMessageTTL extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetSchemaValidationEnforced extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the namespace")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetSubscriptionAuthMode extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetReplicationClusters extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--clusters",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetMaxTopicsPerNamespace extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--max-topics-per-namespace", "-t"},
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetAutoSubscriptionCreation extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetIsAllowAutoUpdateSchema extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable schema validation enforced")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class GetReplicationClusters extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class RemoveSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdNamespaces.java`
#### Snippet
```java
    private class SetAutoTopicCreation extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable allowAutoTopicCreation on namespace")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable delayed delivery messages")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class ClearBacklog extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s", "--subscription" }, description = "Subscription to be cleared", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"--properties", "-p"}, description = "key value pair properties(eg: a=a b=b c=c)")
        private java.util.List<String> propertyList;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-p",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-t", "--type"}, description = "Backlog quota type to remove")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveCompactionThreshold extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GrantPermissions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-r", "--role"}, description = "Client role to which grant permissions", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetLastMessageId extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-ap", "--applied"}, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
                required = false, splitter = NoSplitter.class)
        private java.util.List<String> properties;

        @Parameter(names = {"--clear", "-c"}, description = "Remove all properties",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-consumers-per-subscription", "-c" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
            required = false, splitter = NoSplitter.class)
        private java.util.List<String> properties;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveDeduplicationStatus extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMaxUnackedMessagesOnSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetMaxMessageSize extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--max-message-size", "-m"}, description = "Max message size for a topic", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
                required = false, splitter = NoSplitter.class)
        private java.util.List<String> properties;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class EnableDeduplication extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMaxConsumers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class ExamineMessages extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-i", "--initialPosition" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-f",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--types", "-t"}, description = "Subscription types enabled list (comma separated values)."
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemovePersistence extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class TruncateCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic\n", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class Terminate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetReplicationClusters extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--clusters",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveMaxConsumers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetReplicationClusters extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveOffloadPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMessageById extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-l", "--ledgerId" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetStats extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-gpb",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetCompactionThreshold extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--threshold", "-t" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class Compact extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveReplicationClusters extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetMaxConsumers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--max-consumers", "-c" }, description = "Max consumers for a topic", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetBacklogQuota extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-l", "--limit" }, description = "Size limit (eg: 10M, 16G)")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class ResetCursor extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class DeleteSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-f",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetReplicatedSubscriptionStatus extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetShadowSource extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetMaxUnackedMessagesOnConsumer extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-m", "--maxNum"}, description = "max unacked messages num on consumer", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetDeduplicationStatus extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class AnalyzeBacklog extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s", "--subscription" }, description = "Subscription to be analyzed", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class UpdateSubscriptionProperties extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class UpdateProperties extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--property", "-p"}, description = "key value pair properties(-p a=b -p c=d)",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetDeduplicationSnapshotInterval extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetInternalStats extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-m",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class Permissions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    protected class PartitionedLookup extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/partitionedTopic", required = true)
        protected java.util.List<String> params;
        @Parameter(names = { "-s",
                                "--sort-by-broker" }, description = "Sort partitioned-topic by Broker Url")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveMaxMessageSize extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetMaxProducers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--max-producers", "-p"}, description = "Max producers for a topic", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--source", "-s"}, description = "source topic name", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMaxMessageSize extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetRetention extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--time",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"--metadata", "-m"}, description = "key value pair properties(a=a,b=b,c=c)")
        private java.util.List<String> metadata;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class PeekMessages extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetInternalInfo extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveProperties extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--key", "-k"}, description = "The key to remove in the properties of topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(names = {"--metadata", "-m"}, description = "key value pair properties(a=a,b=b,c=c)")
        private java.util.List<String> metadata;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class Lookup extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMaxProducers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetShadowTopics extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--topics",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetBacklogQuotaMap extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-ap", "--applied"}, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMessageId extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-d", "--datetime" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetPublishRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class OffloadStatusCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-w", "--wait-complete" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveMaxUnackedMessagesOnConsumer extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class ExpireMessagesForAllSubscriptions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-t", "--expireTime" }, description = "Expire messages older than time in seconds "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class DeleteCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-f",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetReplicatedSubscriptionStatus extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class UnloadCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetPartitionedStatsInternal extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RevokePermissions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-r", "--role"}, description = "Client role to which revoke permissions", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetMaxUnackedMessagesOnSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-m", "--maxNum"},
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetDelayedDelivery extends CliCommand {
        @Parameter(description = "tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class DisableDeduplication extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetShadowTopics extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class CompactionStatusCmd extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-w", "--wait-complete" },
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetPersistence extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class ExpireMessages extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-p",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetSubscriptionProperties extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class ListSubscriptions extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class Skip extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemovePublishRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetPersistence extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-e",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveReplicatorDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveMaxUnackedMessagesOnSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetSchemaValidationEnforced extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetCompactionThreshold extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class PartitionedTerminate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetSubscriptionTypesEnabled extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetMaxSubscriptionsPerTopic extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--max-subscriptions-per-topic", "-m"},
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetPublishRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-publish-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveSubscribeRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetBacklogSizeByMessageId extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--messageId",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetDeduplicationStatus extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable deduplication")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetDeduplicationSnapshotInterval extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-i", "--interval" }, description = "Deduplication snapshot interval for topic in second, "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java

        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"--metadata", "-m"}, description = "key value pair properties(a=a,b=b,c=c)")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveMaxProducers extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetOffloadPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-d", "--driver"}, description = "ManagedLedger offload driver", required = true)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetInactiveTopicPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable-delete-while-inactive", "-e" }, description = "Enable delete while inactive")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetSchemaValidationEnforced extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--enable", "-e" }, description = "Enable schema validation enforced")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class ListCmd extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = {"-td", "--topic-domain"},
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetSubscribeRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class CreateSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-s",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetOffloadPolicies extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMessageTTL extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class PartitionedTopicListCmd extends CliCommand {
        @Parameter(description = "tenant/namespace", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ist",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetSubscribeRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--subscribe-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetBundleRange extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetMessageTTL extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-t", "--ttl" }, description = "Message TTL for topic in second "
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMaxUnackedMessagesOnConsumer extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetPartitionedStats extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = "--per-partition", description = "Get per partition stats")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class SetSubscriptionDispatchRate extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "--msg-dispatch-rate",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetRetention extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Parameter(names = { "-ap", "--applied" }, description = "Get the applied policy of the topic")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveRetention extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class GetMaxConsumersPerSubscription extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdTopics.java`
#### Snippet
```java
    private class RemoveShadowTopics extends CliCommand {
        @Parameter(description = "persistent://tenant/namespace/topic", required = true)
        private java.util.List<String> params;

        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jline.reader` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
                    final String line = reader.readLine().trim();
                    return substituteVariables(reader.parseLine(line), variables);
                } catch (org.jline.reader.UserInterruptException
                        | org.jline.reader.EndOfFileException userInterruptException) {
                    throw new InterruptShellException();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jline.reader` is unnecessary, and can be replaced with an import
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/PulsarShell.java`
#### Snippet
```java
                    return substituteVariables(reader.parseLine(line), variables);
                } catch (org.jline.reader.UserInterruptException
                        | org.jline.reader.EndOfFileException userInterruptException) {
                    throw new InterruptShellException();
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/EventResources.java`
#### Snippet
```java
     * @param value a supplier which returns the value to be logged for
     *              this resource
     * @see #resource(java.lang.String,java.lang.Object)
     */
    EventResources resource(String key, Supplier<String> value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/EventResources.java`
#### Snippet
```java
     * @param value a supplier which returns the value to be logged for
     *              this resource
     * @see #resource(java.lang.String,java.lang.Object)
     */
    EventResources resource(String key, Supplier<String> value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/Event.java`
#### Snippet
```java
     * @param value a supplier which returns the value to be logged for
     *              this resource
     * @see #resource(java.lang.String,java.lang.Object)
     */
    Event resource(String key, Supplier<String> value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/Event.java`
#### Snippet
```java
     * @param value a supplier which returns the value to be logged for
     *              this resource
     * @see #resource(java.lang.String,java.lang.Object)
     */
    Event resource(String key, Supplier<String> value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/Event.java`
#### Snippet
```java
    /**
     * Add resources for the event from an EventResources object.
     * @see #resource(java.lang.String,java.lang.Object)
     * @return this
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `structured-event-log/src/main/java/org/apache/pulsar/structuredeventlog/Event.java`
#### Snippet
```java
    /**
     * Add resources for the event from an EventResources object.
     * @see #resource(java.lang.String,java.lang.Object)
     * @return this
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.policies.data` is unnecessary and can be removed
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Namespaces.java`
#### Snippet
```java
     * <p/>
     * To disable all new schema updates through the producer, set this to
     * {@link org.apache.pulsar.common.policies.data.SchemaAutoUpdateCompatibilityStrategy#AutoUpdateDisabled}.
     *
     * @param namespace The namespace in whose policy should be set
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.pulsar.common.policies.data` is unnecessary and can be removed
in `pulsar-client-admin-api/src/main/java/org/apache/pulsar/client/admin/Namespaces.java`
#### Snippet
```java
     * <p/>
     * If this is
     * {@link org.apache.pulsar.common.policies.data.SchemaAutoUpdateCompatibilityStrategy#AutoUpdateDisabled},
     * then all new schemas provided via the producer are rejected, and schemas must be updated through the REST api.
     *
```

### UnnecessaryFullyQualifiedName
Qualifier `com.fasterxml.jackson.annotation` is unnecessary, and can be replaced with an import
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    @ToString.Exclude
    @com.fasterxml.jackson.annotation.JsonIgnore
    private Properties properties = new Properties();

```

## RuleId[ruleID=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/ProcessedFileThread.java`
#### Snippet
```java
                }
            } catch (IOException e) {
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileConsumerThread.java`
#### Snippet
```java
             lines.forEachOrdered(line -> process(file, idx.getAndIncrement(), line));
        } catch (IOException e) {
            e.printStackTrace();
        } finally {

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/sink/HdfsSyncThread.java`
#### Snippet
```java
            return;
         } catch (IOException e) {
            e.printStackTrace();
         }
       }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/sink/HdfsSyncThread.java`
#### Snippet
```java
            return;
         } catch (IOException e) {
            e.printStackTrace();
         }
       }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `buildtools/src/main/java/org/apache/pulsar/tests/PulsarTestListener.java`
#### Snippet
```java
        }
        if (result.getThrowable() != null) {
            result.getThrowable().printStackTrace();
            if (result.getThrowable() instanceof ThreadTimeoutException) {
                System.out.println("====== THREAD DUMPS ======");
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/decoder/avro/PulsarAvroRowDecoder.java`
#### Snippet
```java
            avroRecord = record.getAvroRecord();
        } catch (Exception e) {
            e.printStackTrace();
            throw new TrinoException(GENERIC_INTERNAL_ERROR, "Decoding avro record failed.", e);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java
            logger.debug("descriptor '{}' serialized to '{}'.", descriptor.getFullName(), schemaDataBytes);
        } catch (Exception e) {
            e.printStackTrace();
            throw new SchemaSerializationException(e);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java
            fileDescriptorCache.put(fileDescriptor.getFullName(), fileDescriptor);
        } catch (Descriptors.DescriptorValidationException e) {
            e.printStackTrace();
            throw new SchemaSerializationException(e);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchemaUtils.java`
#### Snippet
```java
            logger.debug("deserialize '{}' to descriptor: '{}'.", schemaDataBytes, descriptor.getFullName());
        } catch (Exception e) {
            e.printStackTrace();
            throw new SchemaSerializationException(e);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/UserPublishFunction.java`
#### Snippet
```java
                context.newOutputMessage((String) topicToWrite.get(), Schema.STRING).value(input).sendAsync();
            } catch (PulsarClientException e) {
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/RawFileKeyReader.java`
#### Snippet
```java
        } catch (IOException e) {
            System.out.println("ERROR: Failed to read public key from file " + publicKeyFile);
            e.printStackTrace();
        }
        return keyInfo;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-functions/java-examples/src/main/java/org/apache/pulsar/functions/api/examples/RawFileKeyReader.java`
#### Snippet
```java
        } catch (IOException e) {
            System.out.println("ERROR: Failed to read private key from file " + privateKeyFile);
            e.printStackTrace();
        }
        return keyInfo;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/PerformanceBaseArguments.java`
#### Snippet
```java
            } catch (IllegalArgumentException e) {
                System.out.println("Incorrect proxyProtocol name '" + proxyProtocolString + "'");
                e.printStackTrace();
                exit(-1);
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
                }
            } catch (ParameterException ex) {
                ex.printStackTrace();
                jc.usage();
            } catch (Exception ex) {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-testclient/src/main/java/org/apache/pulsar/testclient/LoadSimulationController.java`
#### Snippet
```java
                jc.usage();
            } catch (Exception ex) {
                ex.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/admin/cli/CmdBase.java`
#### Snippet
```java
                return false;
            } catch (Exception e) {
                e.printStackTrace();
                return false;
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
                    jcommander.getUsageFormatter().usage(chosenCommand);
                } catch (ParameterException noCmd) {
                    e.printStackTrace();
                }
            } else {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/ConfigShell.java`
#### Snippet
```java
                }
            } else {
                e.printStackTrace();
            }
            return false;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
            } catch (IllegalArgumentException e) {
                System.out.println("Incorrect proxyProtocol name '" + proxyProtocolString + "'");
                e.printStackTrace();
                System.exit(-1);
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
            } catch (UnsupportedAuthenticationException exp) {
                System.out.println("Failed to load an authentication plugin");
                exp.printStackTrace();
                return -1;
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
                    usageFormatter.usage(chosenCommand);
                } catch (ParameterException noCmd) {
                    e.printStackTrace();
                }
            } else {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `pulsar-client-tools/src/main/java/org/apache/pulsar/client/cli/PulsarClientTool.java`
#### Snippet
```java
                }
            } else {
                e.printStackTrace();
            }
            return -1;
```

## RuleId[ruleID=ReplaceNullCheck]
### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `pulsar-io/core/src/main/java/org/apache/pulsar/io/core/BatchPushSource.java`
#### Snippet
```java
    public void consume(Record<T> record) {
        try {
            if (record != null) {
                queue.put(record);
            } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarColumnHandle.java`
#### Snippet
```java
        this.dataFormat = dataFormat;
        this.formatHint = formatHint;
        if (handleKeyValueType == null) {
            this.handleKeyValueType = HandleKeyValueType.NONE;
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java
    private void init(ExecutorService executor) {
        tree = Maps.newTreeMap();
        if (executor != null) {
            this.executor = executor;
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `pulsar-broker/src/main/java/org/apache/pulsar/PulsarClusterMetadataSetup.java`
#### Snippet
```java
            if (arguments.existingBkMetadataServiceUri != null) {
                uriStr = arguments.existingBkMetadataServiceUri;
            } else if (arguments.bookieMetadataServiceUri != null) {
                uriStr = arguments.bookieMetadataServiceUri;
            } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/v2/Namespaces.java`
#### Snippet
```java
                .thenCompose(__ -> getNamespacePoliciesAsync(namespaceName))
                .thenAccept(policies -> {
                    if (policies.is_allow_auto_update_schema == null) {
                        asyncResponse.resume(pulsar().getConfig().isAllowAutoUpdateSchemaEnabled());
                    } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/stats/prometheus/metrics/Summary.java`
#### Snippet
```java
    @Override
    protected Child newChild() {
        if (quantiles != null) {
            return new Child(quantiles);
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/AbstractTopic.java`
#### Snippet
```java
            return true;
        }
        if (isAllowAutoUpdateSchema == null) {
            return brokerService.pulsar().getConfig().isAllowAutoUpdateSchemaEnabled();
        }
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/admin/impl/PersistentTopicsBase.java`
#### Snippet
```java
                            if (applied) {
                                EntryFilters entryFilters = getNamespacePolicies(namespaceName).entryFilters;
                                if (entryFilters == null) {
                                    return new EntryFilters(String.join(",",
                                            pulsar().getConfiguration().getEntryFilterNames()));
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/RangeEntryCacheManagerImpl.java`
#### Snippet
```java
        EntryCache newEntryCache = new RangeEntryCacheImpl(this, ml, mlFactory.getConfig().isCopyEntriesInCache());
        EntryCache currentEntryCache = caches.putIfAbsent(ml.getName(), newEntryCache);
        if (currentEntryCache != null) {
            return currentEntryCache;
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/process/ProcessRuntimeFactory.java`
#### Snippet
```java
        if (this.logDirectory == null) {
            String envProcessContainerLogDirectory = System.getProperty("pulsar.functions.process.container.log.dir");
            if (null != envProcessContainerLogDirectory) {
                this.logDirectory = envProcessContainerLogDirectory;
            } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java
    private Boolean brokerClientAuthenticationEnabled = null;
    public boolean isBrokerClientAuthenticationEnabled() {
        if (brokerClientAuthenticationEnabled != null) {
            return brokerClientAuthenticationEnabled;
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/worker/WorkerConfig.java`
#### Snippet
```java

    public Boolean isInitializedDlogMetadata() {
        if (this.initializedDlogMetadata == null){
            return false;
        }
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/ManagedLedgerImpl.java`
#### Snippet
```java
                // beginning of the next ledger
                Long nextLedgerId = ledgers.ceilingKey(ledger.getId() + 1);
                if (nextLedgerId != null) {
                    opReadEntry.updateReadPosition(new PositionImpl(nextLedgerId, 0));
                } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `pulsar-package-management/filesystem-storage/src/main/java/org/apache/pulsar/packages/management/storage/filesystem/FileSystemPackagesStorage.java`
#### Snippet
```java
    FileSystemPackagesStorage(PackagesStorageConfiguration configuration) {
        String storagePath = configuration.getProperty(STORAGE_PATH);
        if (storagePath != null) {
            this.storagePath = new File(storagePath);
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/ServiceConfiguration.java`
#### Snippet
```java

    public int getBrokerDeleteInactiveTopicsMaxInactiveDurationSeconds() {
        if (brokerDeleteInactiveTopicsMaxInactiveDurationSeconds == null) {
            return brokerDeleteInactiveTopicsFrequencySeconds;
        } else {
```

## RuleId[ruleID=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `pulsar-io/hdfs2/src/main/java/org/apache/pulsar/io/hdfs2/AbstractHdfsConnector.java`
#### Snippet
```java
            synchronized (cacheClasses) {
                map = cacheClasses.get(classLoader);
                if (map == null) {
                    map = Collections.synchronizedMap(new WeakHashMap<>());
                    cacheClasses.put(classLoader, map);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `pulsar-io/hdfs3/src/main/java/org/apache/pulsar/io/hdfs3/AbstractHdfsConnector.java`
#### Snippet
```java
            synchronized (cacheClasses) {
                map = cacheClasses.get(classLoader);
                if (map == null) {
                    map = Collections.synchronizedMap(new WeakHashMap<>());
                    cacheClasses.put(classLoader, map);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `pulsar-io/flume/src/main/java/org/apache/pulsar/io/flume/node/AbstractConfigurationProvider.java`
#### Snippet
```java
        }
        Map<String, Channel> channelMap = channelCache.get(channelClass);
        if (channelMap == null) {
            channelMap = new HashMap<String, Channel>();
            channelCache.put(channelClass, channelMap);
```

### Java8MapApi
Can be replaced with single 'Map.getOrDefault' method call
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/FieldParser.java`
#### Snippet
```java

    private static Class<?> wrap(Class<?> type) {
        return WRAPPER_TYPES.containsKey(type) ? WRAPPER_TYPES.get(type) : type;
    }

```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationService.java`
#### Snippet
```java

                    List<AuthenticationProvider> providerList = providerMap.get(provider.getAuthMethodName());
                    if (null == providerList) {
                        providerList = new ArrayList<>(1);
                        providerMap.put(provider.getAuthMethodName(), providerList);
```

## RuleId[ruleID=Convert2Lambda]
### Convert2Lambda
Anonymous new FileFilter() can be replaced with lambda
in `pulsar-io/file/src/main/java/org/apache/pulsar/io/file/FileListingThread.java`
#### Snippet
```java
        final String processedFileSuffix = fileConfig.getProcessedFileSuffix();

        return new FileFilter() {
            @Override
            public boolean accept(final File file) {
```

### Convert2Lambda
Anonymous new Consumer() can be replaced with lambda
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarInternalColumn.java`
#### Snippet
```java
    public static Map<String, PulsarInternalColumn> getInternalFieldsMap() {
        ImmutableMap.Builder<String, PulsarInternalColumn> builder = ImmutableMap.builder();
        getInternalFields().forEach(new Consumer<PulsarInternalColumn>() {
            @Override
            public void accept(PulsarInternalColumn pulsarInternalColumn) {
```

### Convert2Lambda
Anonymous new Consumer() can be replaced with lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufSchema.java`
#### Snippet
```java
    private String getParsingInfo(T protoMessageInstance) {
        List<ProtoBufParsingInfo> protoBufParsingInfos = new LinkedList<>();
        protoMessageInstance.getDescriptorForType().getFields().forEach(new Consumer<Descriptors.FieldDescriptor>() {
            @Override
            public void accept(Descriptors.FieldDescriptor fieldDescriptor) {
```

### Convert2Lambda
Anonymous new Consumer() can be replaced with lambda
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/schema/ProtobufNativeSchema.java`
#### Snippet
```java
    private String getParsingInfo(T protoMessageInstance) {
        List<ProtoBufParsingInfo> protoBufParsingInfos = new LinkedList<>();
        protoMessageInstance.getDescriptorForType().getFields().forEach(new Consumer<Descriptors.FieldDescriptor>() {
            @Override
            public void accept(Descriptors.FieldDescriptor fieldDescriptor) {
```

### Convert2Lambda
Anonymous new PrivilegedAction() can be replaced with lambda
in `pulsar-common/src/main/java/org/apache/pulsar/common/nar/NarClassLoader.java`
#### Snippet
```java
        throws IOException {
        File unpacked = NarUnpacker.unpackNar(narPath, getNarExtractionDirectory(narExtractionDirectory));
        return AccessController.doPrivileged(new PrivilegedAction<NarClassLoader>() {
            @SneakyThrows
            @Override
```

### Convert2Lambda
Anonymous new Consumer\>() can be replaced with lambda
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
        if (details.getSource().getCleanupSubscription()) {
            Map<String, Function.ConsumerSpec> consumerSpecMap = details.getSource().getInputSpecsMap();
            consumerSpecMap.entrySet().forEach(new Consumer<Map.Entry<String, Function.ConsumerSpec>>() {
                @Override
                public void accept(Map.Entry<String, Function.ConsumerSpec> stringConsumerSpecEntry) {
```

### Convert2Lambda
Anonymous new Thread.UncaughtExceptionHandler() can be replaced with lambda
in `pulsar-functions/runtime/src/main/java/org/apache/pulsar/functions/runtime/thread/ThreadRuntime.java`
#### Snippet
```java
                        FunctionCommon.getFullyQualifiedName(instanceConfig.getFunctionDetails()),
                        instanceConfig.getInstanceId()));
        this.fnThread.setUncaughtExceptionHandler(new Thread.UncaughtExceptionHandler() {
            @Override
            public void uncaughtException(Thread t, Throwable e) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/windowing/triggers/TimeTriggerPolicy.java`
#### Snippet
```java

    private Runnable newTriggerTask() {
        return new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new PrivilegedExceptionAction() can be replaced with lambda
in `pulsar-broker-auth-sasl/src/main/java/org/apache/pulsar/broker/authentication/PulsarSaslServer.java`
#### Snippet
```java

                try {
                    return Subject.doAs(subject, new PrivilegedExceptionAction<SaslServer>() {
                            @Override
                            public SaslServer run() {
```

### Convert2Lambda
Anonymous new PrivilegedExceptionAction() can be replaced with lambda
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/PulsarSaslClient.java`
#### Snippet
```java

        try {
            this.saslClient = Subject.doAs(clientSubject, new PrivilegedExceptionAction<SaslClient>() {
                @Override
                public SaslClient run() throws SaslException {
```

### Convert2Lambda
Anonymous new PrivilegedExceptionAction() can be replaced with lambda
in `pulsar-client-auth-sasl/src/main/java/org/apache/pulsar/client/impl/auth/PulsarSaslClient.java`
#### Snippet
```java
        try {
            if (clientSubject != null) {
                final byte[] retval = Subject.doAs(clientSubject, new PrivilegedExceptionAction<byte[]>() {
                    @Override
                    public byte[] run() throws SaslException {
```

## RuleId[ruleID=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `pulsar-io/kafka-connect-adaptor/src/main/java/org/apache/pulsar/io/kafka/connect/AbstractKafkaConnectSource.java`
#### Snippet
```java
                if (processRecord.isEmpty()) {
                    outstandingRecords.decrementAndGet();
                    continue;
                } else {
                    return processRecord;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiMessageIdImpl.java`
#### Snippet
```java
                result = currentResult;
            } else if (currentResult == 0) {
                continue;
            } else if (result != currentResult) {
                throw new IllegalArgumentException(
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/MultiMessageIdImpl.java`
#### Snippet
```java
                    "Different MessageId in Map get different compare result");
            } else {
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java
                                            .getIndexEntryForEntry(groupedReader.ledgerId, nextExpectedId)
                                            .getDataOffset());
                            continue;
                        } else if (entryId < nextExpectedId
                                && !groupedReader.index.getIndexEntryForEntry(groupedReader.ledgerId, nextExpectedId)
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/BlobStoreBackedReadHandleImplV2.java`
#### Snippet
```java
                                            .getIndexEntryForEntry(groupedReader.ledgerId, nextExpectedId)
                                            .getDataOffset());
                            continue;
                        } else if (entryId > groupedReader.lastEntry) {
                            log.info("Expected to read {}, but read {}, which is greater than last entry {}",
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/LegacyHierarchicalLedgerRangeIterator.java`
#### Snippet
```java
            if (!l2NodesIter.hasNext()) {
                l2NodesIter = null;
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
            for (String missingReplica : missingReplicas) {
                if (existingUrLedgerFormat.getReplicaList().contains(missingReplica)) {
                    continue;
                } else {
                    builder.addReplica(missingReplica);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/Actions.java`
#### Snippet
```java
            if (action.getContinueOn() != null) {
                if (success == action.getContinueOn()) {
                    continue;
                } else {
                    // terminate
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/impl/MLTransactionLogImpl.java`
#### Snippet
```java
                        List<TransactionMetadataEntry> logs = deserializeEntry(entry);
                        if (logs.isEmpty()){
                            continue;
                        } else if (logs.size() == 1){
                            TransactionMetadataEntry log = logs.get(0);
```

## RuleId[ruleID=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy fields 'monitoringClassFields', 'ruConsumer' and 'ruPublisher'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/resourcegroup/ResourceGroup.java`
#### Snippet
```java
    // copy ctor: note does shallow copy.
    // The envisioned usage is for display of an RG's state, without scope for direct update.
    public ResourceGroup(ResourceGroup other) {
        this.resourceGroupName = other.resourceGroupName;
        this.rgs = other.rgs;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'ackSet'
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/PositionImpl.java`
#### Snippet
```java
    }

    public PositionImpl(PositionImpl other) {
        this.ledgerId = other.ledgerId;
        this.entryId = other.entryId;
```

## RuleId[ruleID=ClassCanBeRecord]
### ClassCanBeRecord
Class can be a record
in `pulsar-io/nsq/src/main/java/org/apache/pulsar/io/nsq/NSQSource.java`
#### Snippet
```java

    @Data
    private static class NSQRecord implements Record<byte[]> {
        private final byte[] value;
    }
```

### ClassCanBeRecord
Class can be a record
in `pulsar-io/kafka/src/main/java/org/apache/pulsar/io/kafka/BytesWithKafkaSchema.java`
#### Snippet
```java
 */
@Value
public class BytesWithKafkaSchema {
    private final ByteBuffer value;
    private final int schemaId;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/tcp/NettyTCPServerHandler.java`
#### Snippet
```java

    @Data
    private static class NettyTCPRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/udp/NettyUDPServerHandler.java`
#### Snippet
```java

    @Data
    private static class NettyUDPRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-io/netty/src/main/java/org/apache/pulsar/io/netty/http/NettyHttpServerHandler.java`
#### Snippet
```java

    @Data
    private static class NettyHttpRecord implements Record<byte[]>, Serializable {
        private final Optional<String> key;
        private final byte[] value;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-io/mongo/src/main/java/org/apache/pulsar/io/mongodb/MongoSource.java`
#### Snippet
```java

    @Data
    private static class DocRecord implements Record<byte[]> {
        private final Optional<String> key;
        private final byte[] value;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-io/redis/src/main/java/org/apache/pulsar/io/redis/RedisSession.java`
#### Snippet
```java
import org.apache.pulsar.io.redis.sink.RedisSinkConfig;

public class RedisSession {

    private final AbstractRedisClient client;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-io/rabbitmq/src/main/java/org/apache/pulsar/io/rabbitmq/RabbitMQSource.java`
#### Snippet
```java

    @Data
    private static class RabbitMQRecord implements Record<byte[]> {
        private final Optional<String> key;
        private final byte[] value;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarTopicDescription.java`
#### Snippet
```java
 * Represents the basic information about a pulsar topic.
 */
public class PulsarTopicDescription {
    private final String tableName;
    private final String topicName;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-sql/presto-pulsar/src/main/java/org/apache/pulsar/sql/presto/PulsarTableHandle.java`
#### Snippet
```java
 * Description of basic metadata of a table.
 */
public class PulsarTableHandle implements ConnectorTableHandle {

    /**
```

### ClassCanBeRecord
Class can be a record
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java
    }

    private static class Failure {
        final KeeperException.Code failReturnCode;
        final BiPredicate<Op, String> predicate;
```

### ClassCanBeRecord
Class can be a record
in `testmocks/src/main/java/org/apache/zookeeper/MockZooKeeper.java`
#### Snippet
```java
    @Data
    @AllArgsConstructor
    private static class PersistentWatcher {
        final String path;
        final Watcher watcher;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-proxy/src/main/java/org/apache/pulsar/proxy/extensions/ProxyExtensionWithClassLoader.java`
#### Snippet
```java
@Data
@RequiredArgsConstructor
class ProxyExtensionWithClassLoader implements ProxyExtension {

    private final ProxyExtension extension;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/RedirectData.java`
#### Snippet
```java
package org.apache.pulsar.broker.lookup;

public class RedirectData {
    private final String redirectLookupAddress;

```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/lookup/NamespaceData.java`
#### Snippet
```java
package org.apache.pulsar.broker.lookup;

public class NamespaceData {
    private final String brokerUrl;

```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/schema/BookkeeperSchemaStorage.java`
#### Snippet
```java
    }

    static class LocatorEntry {
        final SchemaStorageFormat.SchemaLocator locator;
        final long version;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/protocol/ProtocolHandlerWithClassLoader.java`
#### Snippet
```java
@Data
@RequiredArgsConstructor
class ProtocolHandlerWithClassLoader implements ProtocolHandler {

    private final ProtocolHandler handler;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TopicPoliciesSystemTopicClient.java`
#### Snippet
```java
    }

    private static class TopicPolicyReader implements Reader<PulsarEvent> {

        private final org.apache.pulsar.client.api.Reader<PulsarEvent> reader;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TopicPoliciesSystemTopicClient.java`
#### Snippet
```java
    }

    private static class TopicPolicyWriter implements Writer<PulsarEvent> {

        private final Producer<PulsarEvent> producer;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/systopic/TransactionBufferSnapshotBaseSystemTopicClient.java`
#### Snippet
```java
    }

    protected static class TransactionBufferSnapshotWriter<T> implements Writer<T> {

        protected final Producer<T> producer;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/intercept/BrokerInterceptorWithClassLoader.java`
#### Snippet
```java
@Data
@RequiredArgsConstructor
public class BrokerInterceptorWithClassLoader implements BrokerInterceptor {

    private final BrokerInterceptor interceptor;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/transaction/util/LogIndexLagBackoff.java`
#### Snippet
```java
import lombok.Getter;

public class LogIndexLagBackoff {

    @Getter
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/client/impl/RawReaderImpl.java`
#### Snippet
```java
    }

    private static class RawMessageAndCnx {
        final RawMessage msg;
        final ClientCnx cnx;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/CompactedTopicContext.java`
#### Snippet
```java

@Getter
public class CompactedTopicContext {

    final LedgerHandle ledger;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
    @AllArgsConstructor
    @Getter
    private static class TopicLoadingContext {
        private final String topic;
        private final CompletableFuture<Optional<Topic>> topicFuture;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker/src/main/java/org/apache/pulsar/compaction/TwoPhaseCompactor.java`
#### Snippet
```java
    }

    private static class PhaseOneResult {
        final MessageId from;
        final MessageId to; // last undeleted messageId
```

### ClassCanBeRecord
Class can be a record
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/LedgerOffloader.java`
#### Snippet
```java

    @ToString
    class OffloadResult {
        public final long beginLedger;
        public final long beginEntry;
```

### ClassCanBeRecord
Class can be a record
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/InflightReadsLimiter.java`
#### Snippet
```java
    @AllArgsConstructor
    @ToString
    static class Handle {
        final long acquiredPermits;
        final boolean success;
```

### ClassCanBeRecord
Class can be a record
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java

    @Value
    private static class PendingReadKey {
        private final long startEntry;
        private final long endEntry;
```

### ClassCanBeRecord
Class can be a record
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java

    @AllArgsConstructor
    private static final class ReadEntriesCallbackWithContext {
        final AsyncCallbacks.ReadEntriesCallback callback;
        final Object ctx;
```

### ClassCanBeRecord
Class can be a record
in `managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/cache/PendingReadsManager.java`
#### Snippet
```java

    @AllArgsConstructor
    private static final class FindPendingReadOutcome {
        final PendingRead pendingRead;
        final PendingReadKey missingOnLeft;
```

### ClassCanBeRecord
Class can be a record
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/StreamingDataBlockHeaderImpl.java`
#### Snippet
```java
 * The data block header in tiered storage for each data block.
 */
public class StreamingDataBlockHeaderImpl implements DataBlockHeader {
    // Magic Word for streaming data block.
    // It is a sequence of bytes used to identify the start of a block.
```

### ClassCanBeRecord
Class can be a record
in `tiered-storage/jcloud/src/main/java/org/apache/bookkeeper/mledger/offload/jcloud/impl/DataBlockHeaderImpl.java`
#### Snippet
```java
 * The data block header in tiered storage for each data block.
 */
public class DataBlockHeaderImpl implements DataBlockHeader {
    // Magic Word for data block.
    // It is a sequence of bytes used to identify the start of a block.
```

### ClassCanBeRecord
Class can be a record
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/CacheGetResult.java`
#### Snippet
```java
 */
@Data
public class CacheGetResult<T> {
    /**
     * The value of the key stored.
```

### ClassCanBeRecord
Class can be a record
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/Stat.java`
#### Snippet
```java
 */
@Data
public class Stat {

    /**
```

### ClassCanBeRecord
Class can be a record
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/Notification.java`
#### Snippet
```java

@Data
public final class Notification {
    /**
     * The type of the event being notified.
```

### ClassCanBeRecord
Class can be a record
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/api/GetResult.java`
#### Snippet
```java
 */
@Data
public class GetResult {
    /**
     * The value of the key stored.
```

### ClassCanBeRecord
Class can be a record
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/FaultInjectionMetadataStore.java`
#### Snippet
```java

    @Data
    private static class Failure {
        private final MetadataStoreException exception;
        private final BiPredicate<OperationType, String> predicate;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/impl/LocalMemoryMetadataStore.java`
#### Snippet
```java

    @Data
    private static class Value {
        final long version;
        final byte[] data;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-metadata/src/main/java/org/apache/pulsar/metadata/bookkeeper/PulsarLedgerUnderreplicationManager.java`
#### Snippet
```java
    private static final byte[] LOCK_DATA = getLockData();

    private static class Lock {
        private final String lockPath;
        private final long ledgerNodeVersion;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/FunctionActioner.java`
#### Snippet
```java
@Data
@Slf4j
public class FunctionActioner {

    private final WorkerConfig workerConfig;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-functions/worker/src/main/java/org/apache/pulsar/functions/worker/service/WorkerServiceWithClassLoader.java`
#### Snippet
```java
@Data
@RequiredArgsConstructor
public class WorkerServiceWithClassLoader implements WorkerService {

    private final WorkerService service;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-functions/instance/src/main/java/org/apache/pulsar/functions/instance/JavaInstance.java`
#### Snippet
```java

    @Data
    public static class AsyncFuncRequest {
        private final Record record;
        private final CompletableFuture processResult;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-functions/localrun/src/main/java/org/apache/pulsar/functions/LocalRunner.java`
#### Snippet
```java

    @Value
    private static class UserCodeClassLoader {
        ClassLoader classLoader;
        boolean classLoaderCreated;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-transaction/coordinator/src/main/java/org/apache/pulsar/transaction/coordinator/TransactionCoordinatorID.java`
#### Snippet
```java
@Beta
@Data
public class TransactionCoordinatorID {

    /**
```

### ClassCanBeRecord
Class can be a record
in `pulsar-client-tools/src/main/java/org/apache/pulsar/shell/JCommanderCompleter.java`
#### Snippet
```java
    @AllArgsConstructor
    @Getter
    public static class ShellContext {
        private final ConfigStore configStore;
    }
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServletWithClassLoader.java`
#### Snippet
```java
@Data
@RequiredArgsConstructor
public class AdditionalServletWithClassLoader implements AdditionalServlet {

    private final AdditionalServlet servlet;
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/web/plugin/servlet/AdditionalServlets.java`
#### Snippet
```java
 */
@Slf4j
public class AdditionalServlets implements AutoCloseable {

    private static final String ADDITIONAL_SERVLET_DIRECTORY = "additionalServletDirectory";
```

### ClassCanBeRecord
Class can be a record
in `pulsar-broker-common/src/main/java/org/apache/pulsar/broker/authentication/AuthenticationProviderList.java`
#### Snippet
```java
 */
@Slf4j
public class AuthenticationProviderList implements AuthenticationProvider {

    private interface AuthProcessor<T, W> {
```

## RuleId[ruleID=ThreadStartInConstruction]
### ThreadStartInConstruction
Call to `start()` during object construction
in `pulsar-common/src/main/java/org/apache/pulsar/common/sasl/JAASCredentialsContainer.java`
#### Snippet
```java
        }

        ticketRefreshThread.start();
    }

```

### ThreadStartInConstruction
Call to `start()` during object construction
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/GracefulExecutorServicesTerminationHandler.java`
#### Snippet
```java
                  log.error("Uncaught exception in shutdown thread {}", thread, exception);
                });
                shutdownWaitingThread.start();
                FutureUtil.whenCancelledOrTimedOut(future, () -> {
                    shutdownWaitingThread.interrupt();
```

## RuleId[ruleID=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `pulsar-client/src/main/java/org/apache/pulsar/client/impl/TopicMessageIdImpl.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj) {
        return messageId.equals(obj);
    }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `pulsar-common/src/main/java/org/apache/pulsar/common/util/collections/ConcurrentOpenHashMap.java`
#### Snippet
```java
        @SuppressFBWarnings
        @Override
        public boolean equals(Object obj) {
            return obj == null;
        }
```

## RuleId[ruleID=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'newTopic' to 'T'
in `pulsar-broker/src/main/java/org/apache/pulsar/broker/service/BrokerService.java`
#### Snippet
```java
                Topic newTopic = topicFactory.create(topic, ledger, brokerService, topicClazz);
                if (newTopic != null) {
                    return (T) newTopic;
                }
            } catch (Throwable e) {
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'narClassLoader' to 'NarClassLoader'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java
                        connectorClassName = FunctionUtils.getFunctionClass(narClassLoader);
                    } else if (componentType == ComponentType.SOURCE) {
                        connectorClassName = ConnectorUtils.getIOSourceClass((NarClassLoader) narClassLoader);
                    } else {
                        connectorClassName = ConnectorUtils.getIOSinkClass((NarClassLoader) narClassLoader);
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'narClassLoader' to 'NarClassLoader'
in `pulsar-functions/utils/src/main/java/org/apache/pulsar/functions/utils/FunctionCommon.java`
#### Snippet
```java
                        connectorClassName = ConnectorUtils.getIOSourceClass((NarClassLoader) narClassLoader);
                    } else {
                        connectorClassName = ConnectorUtils.getIOSinkClass((NarClassLoader) narClassLoader);
                    }
                } catch (IOException e) {
```

